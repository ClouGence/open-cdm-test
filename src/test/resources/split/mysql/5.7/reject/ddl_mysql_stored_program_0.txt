CREATE EVENT split57.ev_replica ON SCHEDULE EVERY 1 DAY DISABLE ON REPLICA DO SET @event_seen = 1;
CREATE FUNCTION IF NOT EXISTS f_ifne() RETURNS INT DETERMINISTIC NO SQL RETURN 0;
CREATE TABLE JSON_ARRAYAGG(a INT);
CREATE TABLE JSON_OBJECTAGG(a INT);
CREATE PROCEDURE IF NOT EXISTS p_ifne() BEGIN END;
CREATE DEFINER = CURRENT_USER TRIGGER IF NOT EXISTS split_trigger.trg_bi
BEFORE INSERT ON split_trigger.src
FOR EACH ROW
SET NEW.updated_at = CURRENT_TIMESTAMP;
CREATE PROCEDURE p15() BEGIN DECLARE done BOOL DEFAULT FALSE; DECLARE n INT; DECLARE c CURSOR FOR WITH RECURSIVE seq(n) AS (SELECT 1 UNION ALL SELECT n+1 FROM seq WHERE n<3) SELECT n FROM seq; DECLARE CONTINUE HANDLER FOR NOT FOUND SET done=TRUE; OPEN c; FETCH c INTO n; CLOSE c; END;
CREATE PROCEDURE p_jdv() BEGIN CREATE JSON DUALITY VIEW dv_in_proc AS SELECT JSON_DUALITY_OBJECT('_id':c1) FROM t1; DROP VIEW dv_in_proc; END;
------------------------------------------------------------------------------------------
[CREATE_EVENT] CREATE EVENT split57.ev_replica ON SCHEDULE EVERY 1 DAY DISABLE ON REPLICA DO SET @event_seen = 1;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION IF NOT EXISTS f_ifne() RETURNS INT DETERMINISTIC NO SQL RETURN 0;
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE JSON_ARRAYAGG(a INT);
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE JSON_OBJECTAGG(a INT);
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE IF NOT EXISTS p_ifne() BEGIN END;
----------
[CREATE_TRIGGER] CREATE DEFINER = CURRENT_USER TRIGGER IF NOT EXISTS split_trigger.trg_bi
BEFORE INSERT ON split_trigger.src
FOR EACH ROW
SET NEW.updated_at = CURRENT_TIMESTAMP;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE p15() BEGIN DECLARE done BOOL DEFAULT FALSE; DECLARE n INT; DECLARE c CURSOR FOR WITH RECURSIVE seq(n) AS (SELECT 1 UNION ALL SELECT n+1 FROM seq WHERE n<3) SELECT n FROM seq; DECLARE CONTINUE HANDLER FOR NOT FOUND SET done=TRUE; OPEN c; FETCH c INTO n; CLOSE c; END;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE p_jdv() BEGIN CREATE JSON DUALITY VIEW dv_in_proc AS SELECT JSON_DUALITY_OBJECT('_id':c1) FROM t1; DROP VIEW dv_in_proc; END;
