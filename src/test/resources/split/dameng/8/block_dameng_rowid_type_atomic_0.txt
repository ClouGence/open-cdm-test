DECLARE
  V ROWID;
BEGIN
  SELECT RID INTO V FROM CODEX_ROWID_ATOMIC WHERE ID=1;
  V:=CODEX_ROWID_ECHO(V);
END;
/
------------------------------------------------------------------------------------------
[BLOCK(PROGRAM_CONTROL,SELECT,CALL_PROG_OBJ)] DECLARE
  V ROWID;
BEGIN
  SELECT RID INTO V FROM CODEX_ROWID_ATOMIC WHERE ID=1;
  V:=CODEX_ROWID_ECHO(V);
END;
