ALTER PUBLICATION testpub5 SET TABLE testpub_rf_tbl1 WHERE (a IS DISTINCT FROM 5)
;
-- error: generated column "d" can't be in list
ALTER PUBLICATION testpub_fortable ADD TABLE testpub_tbl5 (a, d)
;
ALTER PUBLICATION pub3 ADD TABLES IN SCHEMA public
;
CREATE SCHEMA AUTHORIZATION CURRENT_ROLE
  CREATE TRIGGER schema_trig BEFORE INSERT ON schema_not_existing.tab
  EXECUTE FUNCTION schema_trig.no_func()
;
-- check create publication on an object which is not schema
CREATE PUBLICATION testpub1_forschema1 FOR TABLES IN SCHEMA testpub_view
;
ALTER TYPE int_custom SET (ANALYZE = int_custom_typanalyze_false)
;
CREATE TABLE FKTABLE (tid int, id int, foo int, FOREIGN KEY (tid, id) REFERENCES PKTABLE ON DELETE SET NULL (bar))
;
CREATE FUNCTION brintest_func() RETURNS int LANGUAGE sql IMMUTABLE RETURN 0
;
-- fail, it's still a shell:
ALTER TYPE myvarchar SET (storage = extended)
;
-- Now change the root filter to use a column "b"
-- (which is not in the replica identity)
ALTER PUBLICATION testpub6 SET TABLE rf_tbl_abcd_part_pk_1 WHERE (b > 99)
;
drop database test_database_exists (force)
;
-- test alter compression method for materialized views
ALTER MATERIALIZED VIEW compressmv ALTER COLUMN x SET COMPRESSION lz4
;
CREATE FUNCTION functest_IS_6()
    RETURNS int
    LANGUAGE SQL
    RETURN nextval('functest1')
;
CREATE SCHEMA AUTHORIZATION CURRENT_ROLE
  CREATE SEQUENCE schema_not_existing.seq
;
CREATE INDEX wowidx1 ON test_tsvector USING gist (a tsvector_ops(foo=1))
;
CREATE UNIQUE INDEX idx_method_payload_unique ON split_table_idx.method_table (payload) NULLS NOT DISTINCT
;
CREATE OR REPLACE FUNCTION "overlaps"(timestamptz, timestamptz,
  timestamptz, interval)
 RETURNS boolean
 LANGUAGE sql
 STABLE PARALLEL SAFE COST 1
RETURN ($1, $2) overlaps ($3, ($3 + $4))
;
-- New default is set, with dependency added.
ALTER TABLE am_partitioned SET ACCESS METHOD heap2
;
CREATE PUBLICATION testpub3_forschema FOR TABLES IN SCHEMA CURRENT_SCHEMA
;
CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_ROLE
  CREATE TABLE regress_schema_1.tab (id int)
;
-- failure - cannot use column list and schema together
CREATE PUBLICATION testpub_tbl9 FOR TABLES IN SCHEMA public, TABLE public.testpub_tbl7(a)
;
CREATE OR REPLACE FUNCTION replace( citext, citext, citext ) RETURNS TEXT
LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
RETURN pg_catalog.regexp_replace( $1::text, pg_catalog.regexp_replace($2::text, '([^a-zA-Z_0-9])', E'\\\\\\1', 'g'), $3::text, 'gi' )
;
create temp view v_cycle1 as
with recursive search_graph(f, t, label) as (
	select * from graph g
	union all
	select g.*
	from graph g, search_graph sg
	where g.f = sg.t
) cycle f, t set is_cycle using path
select f, t, label from search_graph
;
ALTER PUBLICATION testpub_foralltables SET TABLES IN SCHEMA pub_test
;
ALTER TABLE am_partitioned SET ACCESS METHOD heap
;
CREATE OR REPLACE FUNCTION longitude(earth)
RETURNS float8
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
RETURN degrees(atan2(public.cube_ll_coord($1, 2),
                     public.cube_ll_coord($1, 1)))
;
ALTER PUBLICATION pub1 ADD TABLE dump_test.test_seventh_table (col3, col2) WHERE (col1 = 1)
;
-- ok - only publish table with column list
ALTER PUBLICATION testpub_tbl9 SET TABLE public.testpub_tbl7(a)
;
ALTER PUBLICATION tap_pub_3 SET TABLE tab_rowfilter_partitioned WHERE (a < 5000), tab_rowfilter_less_10k WHERE (a < 6000)
;
-- test gist index

create index tstidx on ltreetest using gist (t gist_ltree_ops(siglen=0))
;
CREATE VIEW "public"."limit_thousand_v_1" AS
 SELECT "onek"."thousand"
   FROM "public"."onek"
  WHERE ("onek"."thousand" < 995)
  ORDER BY "onek"."thousand"
 OFFSET 10
 FETCH FIRST (5) ROWS WITH TIES
;
-- Switching to heap2 adds new dependency entry to the AM.
ALTER TABLE heaptable SET ACCESS METHOD heap2
;
ALTER PUBLICATION testpub5 SET TABLE testpub_rf_tbl1 WHERE (GREATEST(a, 10) > 10)
;
create index trgm_idx on test_trgm using gist (t gist_trgm_ops(siglen=2024))
;
CREATE OR REPLACE FUNCTION col_description(oid, integer)
 RETURNS text
 LANGUAGE sql
 STABLE PARALLEL SAFE STRICT
BEGIN ATOMIC
select description from pg_description
  where objoid = $1 and classoid = 'pg_class'::regclass and objsubid = $2;
END
;
ALTER PUBLICATION testpub_tbl9 SET TABLE public.testpub_tbl7(a)
;
CREATE OR REPLACE FUNCTION regexp_replace(string citext, pattern citext, replacement text, flags text) returns TEXT
LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
RETURN pg_catalog.regexp_replace( $1::text, $2::text, $3, CASE WHEN pg_catalog.strpos($4, 'c') = 0 THEN  $4 || 'i' ELSE $4 END)
;
DROP DATABASE IF EXISTS split_db18_icu WITH (FORCE)
;
CREATE PUBLICATION pub3 FOR TABLE t_1 (a), t_2
	  WITH (PUBLISH_VIA_PARTITION_ROOT)
;
CREATE FUNCTION functest_S_1(a text, b date) RETURNS boolean
    LANGUAGE SQL
    RETURN a = 'abcd' AND b > '2001-01-01'
;
-- failure - add a table with column list when there is already a schema in the
-- publication
ALTER PUBLICATION testpub_tbl9 ADD TABLE public.testpub_tbl7(a)
;
ALTER TABLE gtest30 ALTER COLUMN b DROP EXPRESSION
;
-- failure - cannot SET column list and schema together
ALTER PUBLICATION testpub_tbl9 SET TABLES IN SCHEMA public, TABLE public.testpub_tbl7(a)
;
ALTER SUBSCRIPTION split_sub DROP PUBLICATION split_pub_schema WITH (refresh = false)
;
ALTER SUBSCRIPTION regress_testsub DROP PUBLICATION testpub3 WITH (refresh = false)
;
-- ok - delete publications
ALTER SUBSCRIPTION regress_testsub DROP PUBLICATION testpub1, testpub2 WITH (refresh = false)
;
ALTER PUBLICATION testpub5 SET TABLE testpub_rf_tbl3 WHERE (1234)
;
CREATE PUBLICATION tap_pub_x FOR TABLE schema_rf_x.tab_rf_x WHERE (x > 10)
;
ALTER TABLE ONLY "public"."cmmove3" ALTER COLUMN "f1" SET COMPRESSION pglz
;
CREATE VIEW limit_thousand_v_3 AS SELECT thousand FROM onek WHERE thousand < 995
		ORDER BY thousand FETCH FIRST NULL ROWS WITH TIES
;
------------------------------------------------------------------------------------------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub5 SET TABLE testpub_rf_tbl1 WHERE (a IS DISTINCT FROM 5)
;
----------
[ALTER_PUB_SUB] -- error: generated column "d" can't be in list
ALTER PUBLICATION testpub_fortable ADD TABLE testpub_tbl5 (a, d)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub3 ADD TABLES IN SCHEMA public
;
----------
[CREATE_SCHEMA(CALL_PROG_OBJ)] CREATE SCHEMA AUTHORIZATION CURRENT_ROLE
  CREATE TRIGGER schema_trig BEFORE INSERT ON schema_not_existing.tab
  EXECUTE FUNCTION schema_trig.no_func()
;
----------
[CREATE_PUB_SUB] -- check create publication on an object which is not schema
CREATE PUBLICATION testpub1_forschema1 FOR TABLES IN SCHEMA testpub_view
;
----------
[ALTER_TYPE] ALTER TYPE int_custom SET (ANALYZE = int_custom_typanalyze_false)
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (tid int, id int, foo int, FOREIGN KEY (tid, id) REFERENCES PKTABLE ON DELETE SET NULL (bar))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION brintest_func() RETURNS int LANGUAGE sql IMMUTABLE RETURN 0
;
----------
[ALTER_TYPE] -- fail, it's still a shell:
ALTER TYPE myvarchar SET (storage = extended)
;
----------
[ALTER_PUB_SUB] -- Now change the root filter to use a column "b"
-- (which is not in the replica identity)
ALTER PUBLICATION testpub6 SET TABLE rf_tbl_abcd_part_pk_1 WHERE (b > 99)
;
----------
[DROP_CATALOG] drop database test_database_exists (force)
;
----------
[ALTER_VIEW|ALTER_COLUMN] -- test alter compression method for materialized views
ALTER MATERIALIZED VIEW compressmv ALTER COLUMN x SET COMPRESSION lz4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_IS_6()
    RETURNS int
    LANGUAGE SQL
    RETURN nextval('functest1')
;
----------
[CREATE_SCHEMA] CREATE SCHEMA AUTHORIZATION CURRENT_ROLE
  CREATE SEQUENCE schema_not_existing.seq
;
----------
[ADD_INDEX] CREATE INDEX wowidx1 ON test_tsvector USING gist (a tsvector_ops(foo=1))
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX idx_method_payload_unique ON split_table_idx.method_table (payload) NULLS NOT DISTINCT
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION "overlaps"(timestamptz, timestamptz,
  timestamptz, interval)
 RETURNS boolean
 LANGUAGE sql
 STABLE PARALLEL SAFE COST 1
RETURN ($1, $2) overlaps ($3, ($3 + $4))
;
----------
[ALTER_TABLE] -- New default is set, with dependency added.
ALTER TABLE am_partitioned SET ACCESS METHOD heap2
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub3_forschema FOR TABLES IN SCHEMA CURRENT_SCHEMA
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_ROLE
  CREATE TABLE regress_schema_1.tab (id int)
;
----------
[CREATE_PUB_SUB] -- failure - cannot use column list and schema together
CREATE PUBLICATION testpub_tbl9 FOR TABLES IN SCHEMA public, TABLE public.testpub_tbl7(a)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION replace( citext, citext, citext ) RETURNS TEXT
LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
RETURN pg_catalog.regexp_replace( $1::text, pg_catalog.regexp_replace($2::text, '([^a-zA-Z_0-9])', E'\\\\\\1', 'g'), $3::text, 'gi' )
;
----------
[CREATE_VIEW(SELECT)] create temp view v_cycle1 as
with recursive search_graph(f, t, label) as (
	select * from graph g
	union all
	select g.*
	from graph g, search_graph sg
	where g.f = sg.t
) cycle f, t set is_cycle using path
select f, t, label from search_graph
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_foralltables SET TABLES IN SCHEMA pub_test
;
----------
[ALTER_TABLE] ALTER TABLE am_partitioned SET ACCESS METHOD heap
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION longitude(earth)
RETURNS float8
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
RETURN degrees(atan2(public.cube_ll_coord($1, 2),
                     public.cube_ll_coord($1, 1)))
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub1 ADD TABLE dump_test.test_seventh_table (col3, col2) WHERE (col1 = 1)
;
----------
[ALTER_PUB_SUB] -- ok - only publish table with column list
ALTER PUBLICATION testpub_tbl9 SET TABLE public.testpub_tbl7(a)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_3 SET TABLE tab_rowfilter_partitioned WHERE (a < 5000), tab_rowfilter_less_10k WHERE (a < 6000)
;
----------
[ADD_INDEX] -- test gist index

create index tstidx on ltreetest using gist (t gist_ltree_ops(siglen=0))
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."limit_thousand_v_1" AS
 SELECT "onek"."thousand"
   FROM "public"."onek"
  WHERE ("onek"."thousand" < 995)
  ORDER BY "onek"."thousand"
 OFFSET 10
 FETCH FIRST (5) ROWS WITH TIES
;
----------
[ALTER_TABLE] -- Switching to heap2 adds new dependency entry to the AM.
ALTER TABLE heaptable SET ACCESS METHOD heap2
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub5 SET TABLE testpub_rf_tbl1 WHERE (GREATEST(a, 10) > 10)
;
----------
[ADD_INDEX] create index trgm_idx on test_trgm using gist (t gist_trgm_ops(siglen=2024))
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION col_description(oid, integer)
 RETURNS text
 LANGUAGE sql
 STABLE PARALLEL SAFE STRICT
BEGIN ATOMIC
select description from pg_description
  where objoid = $1 and classoid = 'pg_class'::regclass and objsubid = $2;
END
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_tbl9 SET TABLE public.testpub_tbl7(a)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_replace(string citext, pattern citext, replacement text, flags text) returns TEXT
LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
RETURN pg_catalog.regexp_replace( $1::text, $2::text, $3, CASE WHEN pg_catalog.strpos($4, 'c') = 0 THEN  $4 || 'i' ELSE $4 END)
;
----------
[DROP_CATALOG] DROP DATABASE IF EXISTS split_db18_icu WITH (FORCE)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub3 FOR TABLE t_1 (a), t_2
	  WITH (PUBLISH_VIA_PARTITION_ROOT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_S_1(a text, b date) RETURNS boolean
    LANGUAGE SQL
    RETURN a = 'abcd' AND b > '2001-01-01'
;
----------
[ALTER_PUB_SUB] -- failure - add a table with column list when there is already a schema in the
-- publication
ALTER PUBLICATION testpub_tbl9 ADD TABLE public.testpub_tbl7(a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest30 ALTER COLUMN b DROP EXPRESSION
;
----------
[ALTER_PUB_SUB] -- failure - cannot SET column list and schema together
ALTER PUBLICATION testpub_tbl9 SET TABLES IN SCHEMA public, TABLE public.testpub_tbl7(a)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION split_sub DROP PUBLICATION split_pub_schema WITH (refresh = false)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub DROP PUBLICATION testpub3 WITH (refresh = false)
;
----------
[ALTER_PUB_SUB] -- ok - delete publications
ALTER SUBSCRIPTION regress_testsub DROP PUBLICATION testpub1, testpub2 WITH (refresh = false)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub5 SET TABLE testpub_rf_tbl3 WHERE (1234)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_x FOR TABLE schema_rf_x.tab_rf_x WHERE (x > 10)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."cmmove3" ALTER COLUMN "f1" SET COMPRESSION pglz
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW limit_thousand_v_3 AS SELECT thousand FROM onek WHERE thousand < 995
		ORDER BY thousand FETCH FIRST NULL ROWS WITH TIES
;
