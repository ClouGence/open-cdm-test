ALTER TABLE public.test_replica_identity3 ADD CONSTRAINT test_replica_identity3_id_not_null NOT NULL id
;
CREATE TABLE testpub_gencol (a INT, b INT GENERATED ALWAYS AS (a + 1) VIRTUAL)
;
-- including storage and comments
CREATE TABLE ctlt1 (a text CHECK (length(a) > 2) ENFORCED PRIMARY KEY,
	b text CHECK (length(b) > 100) NOT ENFORCED)
;
CREATE TABLE temporal_rng3 (
  id INTEGER,
  CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
-- Two scalar columns:
CREATE TABLE temporal_fk2_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id1 int4range,
  parent_id2 int4range,
  CONSTRAINT temporal_fk2_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
CREATE TABLE temporal_unique (id int4range, valid_at daterange, a text, UNIQUE (id, valid_at WITHOUT OVERLAPS))
;
CREATE TABLE gtest22c (a int, b int GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
ALTER TABLE pg_temp.z ADD CONSTRAINT z_aa_not_null NOT NULL aa
;
-- Core must test WITHOUT OVERLAPS
-- with an int4range + daterange,
-- so here we do some simple tests
-- to make sure int + daterange works too,
-- since that is the expected use-case.
CREATE TABLE temporal_rng (
  id integer,
  valid_at daterange,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
alter table p1_c1 add constraint inh_check_constraint10 check (f1 < 10) not valid enforced
;
CREATE TABLE gtest_err_2a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (b * 2) VIRTUAL)
;
CREATE TABLE "generated_virtual_tests"."gtest3" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 3))
)
;
CREATE TABLE gtestc (f2 int GENERATED ALWAYS AS (f1+1) VIRTUAL) INHERITS(gtestp)
;
ALTER TABLE gtest25 ADD COLUMN x int GENERATED ALWAYS AS (b * 4) VIRTUAL
;
ALTER TABLE ATACC1 ADD CONSTRAINT ditto NOT NULL a
;
ALTER TABLE fk_partitioned_fk_2 ALTER CONSTRAINT fk_part_con ENFORCED
;
ALTER TABLE ONLY "public"."temporal_rng"
    ADD CONSTRAINT "temporal_rng_pk" PRIMARY KEY ("id", "valid_at" WITHOUT OVERLAPS)
;
create table p1_fail(f1 int constraint inh_check_constraint6 check (f1 < 10) not enforced) inherits(p1, p1_c1)
;
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
CREATE TABLE gtest23x (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) VIRTUAL REFERENCES gtest23a (x) ON DELETE SET NULL)
;
create table notnull_tbl2 (a serial, constraint foo not null a)
;
-- ALTER CONSTRAINT NO INHERIT should work on top-level constraints
alter table inh_nn1 alter constraint inh_nn1_f1_not_null no inherit
;
CREATE TABLE temporal_partitioned_mltrng (
  id int4range,
  valid_at datemultirange,
  name text,
  CONSTRAINT temporal_paritioned_mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
) PARTITION BY LIST (id)
;
CREATE TABLE temporal_rng (
  id integer,
  valid_at daterange,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
ALTER TABLE temporal_fk2_rng2rng
  ADD CONSTRAINT temporal_fk2_rng2rng_fk
  FOREIGN KEY (parent_id1, parent_id2, PERIOD valid_at)
  REFERENCES temporal_rng2 (id1, id2, PERIOD valid_at)
;
ALTER TABLE notnull_tbl1_upg ADD CONSTRAINT nn NOT NULL a NOT VALID
;
-- reference to system column not allowed in generated column
-- (except tableoid, which we test below)
CREATE TABLE gtest_err_6a (a int PRIMARY KEY, b bool GENERATED ALWAYS AS (xmin <> 37) VIRTUAL)
;
ALTER TABLE temporal_partitioned_fk_rng2rng
  DROP CONSTRAINT temporal_partitioned_fk_rng2rng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng
    ON DELETE SET NULL ON UPDATE SET NULL
;
-- Modifying other attributes of a constraint should not affect its enforceability, and vice versa
ALTER TABLE FKTABLE ADD CONSTRAINT fk_con FOREIGN KEY(ftest1, ftest2) REFERENCES PKTABLE NOT VALID NOT ENFORCED
;
ALTER TABLE "public"."insert_tbl"
    ADD CONSTRAINT "ne_insert_tbl_con" CHECK ((("x" + "z") = 1)) NOT ENFORCED
;
ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_rng
    ON DELETE SET NULL ON UPDATE SET NULL
;
CREATE TABLE gtest_rule (a int, b int GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
-- PK with two columns plus a multirange:
CREATE TABLE temporal_mltrng2 (
  id1 int4range,
  id2 int4range,
  valid_at datemultirange,
  CONSTRAINT temporal_mltrng2_pk PRIMARY KEY (id1, id2, valid_at WITHOUT OVERLAPS)
)
;
CREATE TABLE split_pg18.generated_virtual_table (
    id integer,
    amount integer,
    doubled integer GENERATED ALWAYS AS (amount * 2) VIRTUAL,
    CONSTRAINT amount_positive CHECK (amount > 0) NOT ENFORCED
)
;
CREATE TABLE gtest28a (
  a int,
  b int,
  c int,
  x int GENERATED ALWAYS AS (b * 2) VIRTUAL
)
;
create table notnull_tbl_fail (a int generated by default as identity not null no inherit)
;
CREATE TABLE temporal_fk2_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id1 int4range,
  parent_id2 int4range,
  CONSTRAINT temporal_fk2_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk2_mltrng2mltrng_fk FOREIGN KEY (parent_id1, parent_id2, PERIOD valid_at)
    REFERENCES temporal_mltrng2 (id1, id2, PERIOD valid_at)
)
;
CREATE TABLE "generated_virtual_tests"."gtest25" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 3)),
    "c" integer DEFAULT 42,
    "x" integer GENERATED ALWAYS AS (("c" * 4)),
    "d" double precision DEFAULT 101,
    "y" double precision GENERATED ALWAYS AS (("d" * (4)::double precision))
)
;
-- with mismatched PERIOD columns:

-- (parent_id, PERIOD valid_at) REFERENCES (id, valid_at)
-- REFERENCES part should specify PERIOD
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_mltrng (id, valid_at)
)
;
-- (parent_id, valid_at) REFERENCES (id, PERIOD valid_at)
-- FOREIGN KEY part should specify PERIOD
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id, valid_at)
    REFERENCES temporal_mltrng (id, PERIOD valid_at)
)
;
ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
  FOREIGN KEY (parent_id, PERIOD valid_at)
  REFERENCES temporal_rng
  ON DELETE RESTRICT
;
CREATE TABLE "generated_virtual_tests"."gtestxx_4" (
    "b" integer GENERATED ALWAYS AS (("a" * 2)),
    "a" integer NOT NULL
)
;
CREATE TABLE temporal_rng4 (
  id int4range,
  valid_at int4range_d,
  CONSTRAINT temporal_rng4_pk UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
-- generation expression must be immutable
CREATE TABLE gtest_err_4 (a int PRIMARY KEY, b double precision GENERATED ALWAYS AS (random()) VIRTUAL)
;
CREATE TABLE gtest2 (a int, b text GENERATED ALWAYS AS (a || ' sec') VIRTUAL)
;
create table gtest32 (
  a int primary key,
  b int generated always as (a * 2),
  c int generated always as (10 + 10),
  d int generated always as (coalesce(a, 100)),
  e int
)
;
CREATE TABLE "generated_virtual_tests"."gtest20b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2))
)
;
-- UNIQUE with a non-range column:

CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at TEXT,
  CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
ALTER TABLE temporal_rng2
  ADD CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
;
alter table p1 add constraint inh_check_constraint9 check (f1 < 10) not enforced
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.test_replica_identity3 ADD CONSTRAINT test_replica_identity3_id_not_null NOT NULL id
;
----------
[CREATE_TABLE] CREATE TABLE testpub_gencol (a INT, b INT GENERATED ALWAYS AS (a + 1) VIRTUAL)
;
----------
[CREATE_TABLE] -- including storage and comments
CREATE TABLE ctlt1 (a text CHECK (length(a) > 2) ENFORCED PRIMARY KEY,
	b text CHECK (length(b) > 100) NOT ENFORCED)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng3 (
  id INTEGER,
  CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] -- Two scalar columns:
CREATE TABLE temporal_fk2_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id1 int4range,
  parent_id2 int4range,
  CONSTRAINT temporal_fk2_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_unique (id int4range, valid_at daterange, a text, UNIQUE (id, valid_at WITHOUT OVERLAPS))
;
----------
[CREATE_TABLE] CREATE TABLE gtest22c (a int, b int GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_temp.z ADD CONSTRAINT z_aa_not_null NOT NULL aa
;
----------
[CREATE_TABLE] -- Core must test WITHOUT OVERLAPS
-- with an int4range + daterange,
-- so here we do some simple tests
-- to make sure int + daterange works too,
-- since that is the expected use-case.
CREATE TABLE temporal_rng (
  id integer,
  valid_at daterange,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1_c1 add constraint inh_check_constraint10 check (f1 < 10) not valid enforced
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_2a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (b * 2) VIRTUAL)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest3" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 3))
)
;
----------
[CREATE_TABLE] CREATE TABLE gtestc (f2 int GENERATED ALWAYS AS (f1+1) VIRTUAL) INHERITS(gtestp)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest25 ADD COLUMN x int GENERATED ALWAYS AS (b * 4) VIRTUAL
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ATACC1 ADD CONSTRAINT ditto NOT NULL a
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fk_partitioned_fk_2 ALTER CONSTRAINT fk_part_con ENFORCED
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."temporal_rng"
    ADD CONSTRAINT "temporal_rng_pk" PRIMARY KEY ("id", "valid_at" WITHOUT OVERLAPS)
;
----------
[CREATE_TABLE] create table p1_fail(f1 int constraint inh_check_constraint6 check (f1 < 10) not enforced) inherits(p1, p1_c1)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest23x (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) VIRTUAL REFERENCES gtest23a (x) ON DELETE SET NULL)
;
----------
[CREATE_TABLE] create table notnull_tbl2 (a serial, constraint foo not null a)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- ALTER CONSTRAINT NO INHERIT should work on top-level constraints
alter table inh_nn1 alter constraint inh_nn1_f1_not_null no inherit
;
----------
[CREATE_TABLE] CREATE TABLE temporal_partitioned_mltrng (
  id int4range,
  valid_at datemultirange,
  name text,
  CONSTRAINT temporal_paritioned_mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
) PARTITION BY LIST (id)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng (
  id integer,
  valid_at daterange,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk2_rng2rng
  ADD CONSTRAINT temporal_fk2_rng2rng_fk
  FOREIGN KEY (parent_id1, parent_id2, PERIOD valid_at)
  REFERENCES temporal_rng2 (id1, id2, PERIOD valid_at)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE notnull_tbl1_upg ADD CONSTRAINT nn NOT NULL a NOT VALID
;
----------
[CREATE_TABLE] -- reference to system column not allowed in generated column
-- (except tableoid, which we test below)
CREATE TABLE gtest_err_6a (a int PRIMARY KEY, b bool GENERATED ALWAYS AS (xmin <> 37) VIRTUAL)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT|ADD_CONSTRAINT] ALTER TABLE temporal_partitioned_fk_rng2rng
  DROP CONSTRAINT temporal_partitioned_fk_rng2rng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng
    ON DELETE SET NULL ON UPDATE SET NULL
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Modifying other attributes of a constraint should not affect its enforceability, and vice versa
ALTER TABLE FKTABLE ADD CONSTRAINT fk_con FOREIGN KEY(ftest1, ftest2) REFERENCES PKTABLE NOT VALID NOT ENFORCED
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."insert_tbl"
    ADD CONSTRAINT "ne_insert_tbl_con" CHECK ((("x" + "z") = 1)) NOT ENFORCED
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_rng
    ON DELETE SET NULL ON UPDATE SET NULL
;
----------
[CREATE_TABLE] CREATE TABLE gtest_rule (a int, b int GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
----------
[CREATE_TABLE] -- PK with two columns plus a multirange:
CREATE TABLE temporal_mltrng2 (
  id1 int4range,
  id2 int4range,
  valid_at datemultirange,
  CONSTRAINT temporal_mltrng2_pk PRIMARY KEY (id1, id2, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] CREATE TABLE split_pg18.generated_virtual_table (
    id integer,
    amount integer,
    doubled integer GENERATED ALWAYS AS (amount * 2) VIRTUAL,
    CONSTRAINT amount_positive CHECK (amount > 0) NOT ENFORCED
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest28a (
  a int,
  b int,
  c int,
  x int GENERATED ALWAYS AS (b * 2) VIRTUAL
)
;
----------
[CREATE_TABLE] create table notnull_tbl_fail (a int generated by default as identity not null no inherit)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk2_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id1 int4range,
  parent_id2 int4range,
  CONSTRAINT temporal_fk2_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk2_mltrng2mltrng_fk FOREIGN KEY (parent_id1, parent_id2, PERIOD valid_at)
    REFERENCES temporal_mltrng2 (id1, id2, PERIOD valid_at)
)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest25" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 3)),
    "c" integer DEFAULT 42,
    "x" integer GENERATED ALWAYS AS (("c" * 4)),
    "d" double precision DEFAULT 101,
    "y" double precision GENERATED ALWAYS AS (("d" * (4)::double precision))
)
;
----------
[CREATE_TABLE] -- with mismatched PERIOD columns:

-- (parent_id, PERIOD valid_at) REFERENCES (id, valid_at)
-- REFERENCES part should specify PERIOD
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_mltrng (id, valid_at)
)
;
----------
[CREATE_TABLE] -- (parent_id, valid_at) REFERENCES (id, PERIOD valid_at)
-- FOREIGN KEY part should specify PERIOD
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id, valid_at)
    REFERENCES temporal_mltrng (id, PERIOD valid_at)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
  FOREIGN KEY (parent_id, PERIOD valid_at)
  REFERENCES temporal_rng
  ON DELETE RESTRICT
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtestxx_4" (
    "b" integer GENERATED ALWAYS AS (("a" * 2)),
    "a" integer NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng4 (
  id int4range,
  valid_at int4range_d,
  CONSTRAINT temporal_rng4_pk UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] -- generation expression must be immutable
CREATE TABLE gtest_err_4 (a int PRIMARY KEY, b double precision GENERATED ALWAYS AS (random()) VIRTUAL)
;
----------
[CREATE_TABLE] CREATE TABLE gtest2 (a int, b text GENERATED ALWAYS AS (a || ' sec') VIRTUAL)
;
----------
[CREATE_TABLE] create table gtest32 (
  a int primary key,
  b int generated always as (a * 2),
  c int generated always as (10 + 10),
  d int generated always as (coalesce(a, 100)),
  e int
)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest20b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2))
)
;
----------
[CREATE_TABLE] -- UNIQUE with a non-range column:

CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at TEXT,
  CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_rng2
  ADD CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1 add constraint inh_check_constraint9 check (f1 < 10) not enforced
;
