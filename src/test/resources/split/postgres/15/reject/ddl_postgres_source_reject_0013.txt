CREATE TABLE gtest4 (
    a int,
    b double_int GENERATED ALWAYS AS ((a * 2, a * 3)) VIRTUAL
)
;
CREATE TABLE gtest23p (x int, y int GENERATED ALWAYS AS (x * 2) VIRTUAL, PRIMARY KEY (y))
;
-- VACUUM ONLY on a partitioned table does nothing, ensure we get a warning.
VACUUM ONLY vacparted
;
alter table p1 add constraint inh_check_constraint10 check (f1 < 10) not enforced
;
ALTER TABLE FKTABLE ADD CONSTRAINT fk_con FOREIGN KEY(ftest1, ftest2) REFERENCES PKTABLE NOT VALID NOT ENFORCED
;
create table notnull_tbl_fail (a serial, constraint foo not null a no inherit)
;
-- statistics on virtual generated column not allowed
CREATE TABLE ext_stats_test1 (x int, y int, z int GENERATED ALWAYS AS (x+y) VIRTUAL, w xid)
;
ALTER TABLE gtest27
  DROP COLUMN x,
  ALTER COLUMN a TYPE bigint,
  ALTER COLUMN b TYPE bigint,
  ADD COLUMN x bigint GENERATED ALWAYS AS ((a + b) * 2) VIRTUAL
;
CREATE TABLE "generated_virtual_tests"."gtest26" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2))
)
;
CREATE TABLE ATACC1 (a int NOT NULL NO INHERIT) PARTITION BY LIST (a)
;
ALTER TABLE notnull_tbl1_child2 ADD NOT NULL a NOT VALID
;
create table notnull_tbl_fail (a serial not null no inherit)
;
CREATE TABLE temporal_rng (
  -- Since we can't depend on having btree_gist here,
  -- use an int4range instead of an int.
  -- (The rangetypes regression test uses the same trick.)
  id int4range,
  valid_at daterange,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
  FOREIGN KEY (parent_id, PERIOD valid_at)
  REFERENCES temporal_rng
  ON UPDATE NO ACTION
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE gtest4 (
    a int,
    b double_int GENERATED ALWAYS AS ((a * 2, a * 3)) VIRTUAL
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest23p (x int, y int GENERATED ALWAYS AS (x * 2) VIRTUAL, PRIMARY KEY (y))
;
----------
[ADMIN_TABLE] -- VACUUM ONLY on a partitioned table does nothing, ensure we get a warning.
VACUUM ONLY vacparted
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1 add constraint inh_check_constraint10 check (f1 < 10) not enforced
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD CONSTRAINT fk_con FOREIGN KEY(ftest1, ftest2) REFERENCES PKTABLE NOT VALID NOT ENFORCED
;
----------
[CREATE_TABLE] create table notnull_tbl_fail (a serial, constraint foo not null a no inherit)
;
----------
[CREATE_TABLE] -- statistics on virtual generated column not allowed
CREATE TABLE ext_stats_test1 (x int, y int, z int GENERATED ALWAYS AS (x+y) VIRTUAL, w xid)
;
----------
[ALTER_TABLE|DROP_COLUMN|ALTER_COLUMN|ADD_COLUMN] ALTER TABLE gtest27
  DROP COLUMN x,
  ALTER COLUMN a TYPE bigint,
  ALTER COLUMN b TYPE bigint,
  ADD COLUMN x bigint GENERATED ALWAYS AS ((a + b) * 2) VIRTUAL
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest26" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2))
)
;
----------
[CREATE_TABLE] CREATE TABLE ATACC1 (a int NOT NULL NO INHERIT) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE notnull_tbl1_child2 ADD NOT NULL a NOT VALID
;
----------
[CREATE_TABLE] create table notnull_tbl_fail (a serial not null no inherit)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng (
  -- Since we can't depend on having btree_gist here,
  -- use an int4range instead of an int.
  -- (The rangetypes regression test uses the same trick.)
  id int4range,
  valid_at daterange,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
  FOREIGN KEY (parent_id, PERIOD valid_at)
  REFERENCES temporal_rng
  ON UPDATE NO ACTION
;
