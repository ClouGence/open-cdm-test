ALTER SUBSCRIPTION sub SKIP (lsn = '0/158A4A0')
;
PREPARE split_merge_plan (integer, text) AS MERGE INTO split_session_plan.plan_items t USING (VALUES ($1, $2)) AS s(id, note) ON t.id = s.id WHEN MATCHED THEN UPDATE SET note = s.note WHEN NOT MATCHED THEN INSERT (id, note) VALUES (s.id, s.note)
;
ALTER STATISTICS IF EXISTS ab1_a_b_stats SET STATISTICS 0
;
REINDEX (TABLESPACE regress_tblspace, CONCURRENTLY) TABLE tbspace_reindex_part
;
REINDEX (TABLESPACE regress_tblspace) TABLE pg_toast.pg_toast_1260
;
GRANT SET ON PARAMETER test_oat_hooks.user_var1 TO regress_role_joe
;
REINDEX (TABLESPACE regress_tblspace) TABLE regress_tblspace_test_tbl
;
REINDEX (CONCURRENTLY) SYSTEM postgres
;
REINDEX (TABLESPACE pg_global) TABLE pg_authid
;
-- null

GRANT TRUNCATE ON atest2 TO regress_priv_user4 GRANTED BY regress_priv_user5
;
------------------------------------------------------------------------------------------
[ADMIN_PUB_SUB] ALTER SUBSCRIPTION sub SKIP (lsn = '0/158A4A0')
;
----------
[UNSAFE] PREPARE split_merge_plan (integer, text) AS MERGE INTO split_session_plan.plan_items t USING (VALUES ($1, $2)) AS s(id, note) ON t.id = s.id WHEN MATCHED THEN UPDATE SET note = s.note WHEN NOT MATCHED THEN INSERT (id, note) VALUES (s.id, s.note)
;
----------
[ADMIN_PERFORMANCE] ALTER STATISTICS IF EXISTS ab1_a_b_stats SET STATISTICS 0
;
----------
[ADMIN] REINDEX (TABLESPACE regress_tblspace, CONCURRENTLY) TABLE tbspace_reindex_part
;
----------
[ADMIN] REINDEX (TABLESPACE regress_tblspace) TABLE pg_toast.pg_toast_1260
;
----------
[GRANT] GRANT SET ON PARAMETER test_oat_hooks.user_var1 TO regress_role_joe
;
----------
[ADMIN] REINDEX (TABLESPACE regress_tblspace) TABLE regress_tblspace_test_tbl
;
----------
[ADMIN] REINDEX (CONCURRENTLY) SYSTEM postgres
;
----------
[ADMIN] REINDEX (TABLESPACE pg_global) TABLE pg_authid
;
----------
[GRANT] -- null

GRANT TRUNCATE ON atest2 TO regress_priv_user4 GRANTED BY regress_priv_user5
;
