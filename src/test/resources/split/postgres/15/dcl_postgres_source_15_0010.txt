-- Creating privileges on an existent custom GUC should create precisely the
-- right privileges, not overly conservative ones.
GRANT SET ON PARAMETER test_oat_hooks.user_var2 TO regress_role_joe
;
REINDEX (TABLESPACE reindex_tbspace) INDEX CONCURRENTLY pg_toast.pg_toast_16540_index
;
-- ok

-- REINDEX (TABLESPACE)
-- catalogs and system tablespaces
-- system catalog, fail
REINDEX (TABLESPACE regress_tblspace) TABLE pg_am
;
-- Granting multiple privileges on a parameter should be reported correctly to
-- the OAT hook, but beware that WITH GRANT OPTION is not represented.
GRANT SET, ALTER SYSTEM ON PARAMETER none.such TO regress_role_joe
;
REINDEX (TABLESPACE pg_default, VERBOSE false) TABLE split_system_admin.maint_table
;
-- toast relations, fail
REINDEX (TABLESPACE regress_tblspace) INDEX pg_toast.pg_toast_1262_index
;
GRANT SET ON PARAMETER zero_damaged_pages TO regress_host_resource_admin
;
ALTER STATISTICS dump_test.test_ext_stats_opts SET STATISTICS 1000
;
REVOKE ALL PRIVILEGES ON FUNCTION testagg4(int2) FROM CURRENT_ROLE
;
ALTER USER MAPPING FOR CURRENT_ROLE SERVER sv3
 OPTIONS (SET user 'CURRENT_ROLE_alt')
;
------------------------------------------------------------------------------------------
[GRANT] -- Creating privileges on an existent custom GUC should create precisely the
-- right privileges, not overly conservative ones.
GRANT SET ON PARAMETER test_oat_hooks.user_var2 TO regress_role_joe
;
----------
[ADMIN] REINDEX (TABLESPACE reindex_tbspace) INDEX CONCURRENTLY pg_toast.pg_toast_16540_index
;
----------
[ADMIN] -- ok

-- REINDEX (TABLESPACE)
-- catalogs and system tablespaces
-- system catalog, fail
REINDEX (TABLESPACE regress_tblspace) TABLE pg_am
;
----------
[GRANT] -- Granting multiple privileges on a parameter should be reported correctly to
-- the OAT hook, but beware that WITH GRANT OPTION is not represented.
GRANT SET, ALTER SYSTEM ON PARAMETER none.such TO regress_role_joe
;
----------
[ADMIN] REINDEX (TABLESPACE pg_default, VERBOSE false) TABLE split_system_admin.maint_table
;
----------
[ADMIN] -- toast relations, fail
REINDEX (TABLESPACE regress_tblspace) INDEX pg_toast.pg_toast_1262_index
;
----------
[GRANT] GRANT SET ON PARAMETER zero_damaged_pages TO regress_host_resource_admin
;
----------
[ADMIN_PERFORMANCE] ALTER STATISTICS dump_test.test_ext_stats_opts SET STATISTICS 1000
;
----------
[REVOKE] REVOKE ALL PRIVILEGES ON FUNCTION testagg4(int2) FROM CURRENT_ROLE
;
----------
[GRANT] ALTER USER MAPPING FOR CURRENT_ROLE SERVER sv3
 OPTIONS (SET user 'CURRENT_ROLE_alt')
;
