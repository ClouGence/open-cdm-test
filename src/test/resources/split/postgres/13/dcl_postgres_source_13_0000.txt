ALTER STATISTICS IF EXISTS ab1_a_b_stats SET STATISTICS 0
;
ALTER STATISTICS ab1_a_b_stats SET STATISTICS -1
;
CREATE STATISTICS tst ON x + y FROM ext_stats_test
;
ALTER STATISTICS dump_test.test_ext_stats_opts SET STATISTICS 1000
;
ALTER STATISTICS split_stats_next.items_dep SET STATISTICS 100
;
ALTER STATISTICS ab1_a_b_stats SET STATISTICS 0
;
-- setting statistics target 0 skips the statistics, without printing any message, so check catalog
ALTER STATISTICS ab1_a_b_stats SET STATISTICS 0
;
CREATE STATISTICS tst ON (x, y) FROM ext_stats_test
;
------------------------------------------------------------------------------------------
[ADMIN_PERFORMANCE] ALTER STATISTICS IF EXISTS ab1_a_b_stats SET STATISTICS 0
;
----------
[ADMIN_PERFORMANCE] ALTER STATISTICS ab1_a_b_stats SET STATISTICS -1
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS tst ON x + y FROM ext_stats_test
;
----------
[ADMIN_PERFORMANCE] ALTER STATISTICS dump_test.test_ext_stats_opts SET STATISTICS 1000
;
----------
[ADMIN_PERFORMANCE] ALTER STATISTICS split_stats_next.items_dep SET STATISTICS 100
;
----------
[ADMIN_PERFORMANCE] ALTER STATISTICS ab1_a_b_stats SET STATISTICS 0
;
----------
[ADMIN_PERFORMANCE] -- setting statistics target 0 skips the statistics, without printing any message, so check catalog
ALTER STATISTICS ab1_a_b_stats SET STATISTICS 0
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS tst ON (x, y) FROM ext_stats_test
;
