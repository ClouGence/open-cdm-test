-- only accept valid partitioning strategy
CREATE TABLE partitioned (
    a int
) PARTITION BY MAGIC (a)
;
CREATE TABLE partitioned (
    a int
) PARTITION BY MAGIC (a)
;
-- (parent_id, PERIOD valid_at) REFERENCES (id)
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_mltrng (id)
)
;
CREATE TABLE gtest24 (a int PRIMARY KEY, b gtestdomain1 GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
ALTER TABLE dump_test.test_table_nn ADD CONSTRAINT nn NOT NULL col1 NOT VALID
;
CREATE TABLE "generated_virtual_tests"."gtest2" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULL::integer)
)
;
--
-- Explicit enforceability specification not allowed
---
CREATE DOMAIN constraint_enforced_dom AS int CONSTRAINT the_constraint CHECK (value > 0) ENFORCED
;
-- should fail because of duplicate referenced columns:
ALTER TABLE temporal_fk_mltrng2mltrng
  ADD CONSTRAINT temporal_fk_mltrng2mltrng_fk2
  FOREIGN KEY (parent_id, PERIOD parent_id)
  REFERENCES temporal_mltrng (id, PERIOD id)
;
ALTER TABLE ONLY "public"."temporal_mltrng2"
    ADD CONSTRAINT "temporal_mltrng2_pk" PRIMARY KEY ("id1", "id2", "valid_at" WITHOUT OVERLAPS)
;
create table notnull_tbl_fail (a int, primary key(a), not null a no inherit)
;
ALTER TABLE inh_nn_parent ADD CONSTRAINT nna NOT NULL a
;
CREATE TABLE temporal_partitioned_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_partitioned_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_partitioned_fk_rng2rng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng (id, PERIOD valid_at)
) PARTITION BY LIST (id)
;
ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk2
  FOREIGN KEY (parent_id, PERIOD parent_id)
  REFERENCES temporal_rng (id, PERIOD id)
;
CREATE TABLE split_alter18.current_table (
    id integer PRIMARY KEY,
    parent_id integer,
    amount integer NOT NULL,
    calc integer GENERATED ALWAYS AS (amount * 2) STORED,
    payload text,
    CONSTRAINT current_table_fk FOREIGN KEY (parent_id) REFERENCES split_alter18.parent_table(id) NOT ENFORCED
)
;
ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
  FOREIGN KEY (parent_id, PERIOD valid_at)
  REFERENCES temporal_rng
;
CREATE TABLE notnull_tbl1_2(a int, CONSTRAINT nn2 NOT NULL a, b int)
;
--
-- partitioned FK referenced deletes CASCADE
--

--
-- partitioned FK referenced updates SET NULL
--

ALTER TABLE temporal_partitioned_fk_rng2rng
  DROP CONSTRAINT temporal_partitioned_fk_rng2rng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng
    ON DELETE SET NULL ON UPDATE SET NULL
;
CREATE TABLE gtest_tableoid (
  a int PRIMARY KEY,
  b bool GENERATED ALWAYS AS (tableoid = 'gtest_tableoid'::regclass) VIRTUAL
)
;
CREATE TABLE gtest_normal_child (a int, b int GENERATED ALWAYS AS (a * 2) VIRTUAL) INHERITS (gtest_normal)
;
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id, PERIOD parent_id)
    REFERENCES temporal_mltrng (id, PERIOD id)
)
;
CREATE TABLE gtestm (
  a int PRIMARY KEY,
  b int GENERATED ALWAYS AS (a * 2) VIRTUAL
)
;
CREATE TABLE gtest_err_7b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (row_number() OVER (ORDER BY a)) VIRTUAL)
;
alter table constr_parent add not null a not valid
;
-- PK with one column plus a multirange:
CREATE TABLE temporal_mltrng (
  id int4range,
  valid_at datemultirange,
  CONSTRAINT temporal_mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
CREATE TEMP TABLE fktable2 (fk int references pktable ENFORCED NOT ENFORCED)
;
CREATE DOMAIN constraint_not_enforced_dom AS int CONSTRAINT the_constraint CHECK (value > 0) NOT ENFORCED
;
ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_rng
    ON DELETE CASCADE ON UPDATE CASCADE
;
CREATE TABLE gtest31_1 (a int, b text GENERATED ALWAYS AS ('hello') VIRTUAL, c text) PARTITION BY LIST (a)
;
CREATE TABLE gtest_child PARTITION OF gtest_parent (
    f3 WITH OPTIONS GENERATED ALWAYS AS (f2 * 2) VIRTUAL
) FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
create table inh_child (a int not null no inherit)
;
CREATE TABLE gtestnn_parent (
    f1 int,
    f2 bigint,
    f3 bigint GENERATED ALWAYS AS (nullif(f1, 1) + nullif(f2, 10)) VIRTUAL NOT NULL
) PARTITION BY RANGE (f1)
;
-- can't override
ALTER TABLE ATACC1 ADD CONSTRAINT ditto NOT NULL a
;
CREATE TABLE gtest23x (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) VIRTUAL REFERENCES gtest23a (x) ON UPDATE CASCADE)
;
-- PK with a non-range column:

CREATE TABLE temporal_rng (
  id int4range,
  valid_at TEXT,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
CREATE TABLE "generated_virtual_tests"."gtest_child2" (
    "f1" "date" CONSTRAINT "gtest_parent_f1_not_null" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2))
)
;
CREATE TABLE NE_CHECK_TBL (x int,
	CONSTRAINT CHECK_CON CHECK (x > 3) NOT ENFORCED)
;
ALTER TABLE fk_partitioned_fk ADD CONSTRAINT fk_partitioned_fk_a_b_fkey FOREIGN KEY (a, b)
	REFERENCES fk_notpartitioned_pk NOT ENFORCED
;
CREATE TABLE noinh_con_copy (a int CHECK (a > 0) NO INHERIT, b int not null,
	c int not null no inherit)
;
ALTER TABLE FKTABLE ALTER CONSTRAINT fktable_fk_fkey NOT ENFORCED
;
ALTER TABLE notnull_tbl1 ALTER CONSTRAINT nntbl1_a NO INHERIT
;
alter table p1 add constraint inh_check_constraint8 check (f1 < 10) enforced
;
CREATE TABLE ATACC2 (a int, CONSTRAINT a_is_not_null NOT NULL a NO INHERIT)
;
ALTER TABLE temporal_fk_mltrng2mltrng
  ADD CONSTRAINT temporal_fk_mltrng2mltrng_fk
  FOREIGN KEY (parent_id, PERIOD valid_at)
  REFERENCES temporal_mltrng (id, PERIOD valid_at)
  ON UPDATE RESTRICT
;
CREATE TABLE gtestm (
  id int PRIMARY KEY,
  f1 int,
  f2 int,
  f3 int GENERATED ALWAYS AS (f1 * 2) VIRTUAL,
  f4 int GENERATED ALWAYS AS (f2 * 2) VIRTUAL
)
;
CREATE TABLE "generated_virtual_tests"."gtestx" (
    "a" integer CONSTRAINT "gtest1_a_not_null" NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 22)),
    "x" integer
)
;
create table notnull_tbl_fail (like notnull_tbl1, constraint foo2 not null a)
;
-- test update
CREATE TABLE gtest3 (a int, b int GENERATED ALWAYS AS (a * 3) VIRTUAL)
;
CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at textrange2,
  CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
ALTER TABLE fk_partitioned_fk ALTER CONSTRAINT fk_partitioned_fk_a_b_fkey NOT ENFORCED
;
CREATE TABLE gtest_err_5b (a int PRIMARY KEY, b int GENERATED ALWAYS AS identity GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- only accept valid partitioning strategy
CREATE TABLE partitioned (
    a int
) PARTITION BY MAGIC (a)
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
    a int
) PARTITION BY MAGIC (a)
;
----------
[CREATE_TABLE] -- (parent_id, PERIOD valid_at) REFERENCES (id)
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_mltrng (id)
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest24 (a int PRIMARY KEY, b gtestdomain1 GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE dump_test.test_table_nn ADD CONSTRAINT nn NOT NULL col1 NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest2" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULL::integer)
)
;
----------
[CREATE_TYPE] --
-- Explicit enforceability specification not allowed
---
CREATE DOMAIN constraint_enforced_dom AS int CONSTRAINT the_constraint CHECK (value > 0) ENFORCED
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- should fail because of duplicate referenced columns:
ALTER TABLE temporal_fk_mltrng2mltrng
  ADD CONSTRAINT temporal_fk_mltrng2mltrng_fk2
  FOREIGN KEY (parent_id, PERIOD parent_id)
  REFERENCES temporal_mltrng (id, PERIOD id)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."temporal_mltrng2"
    ADD CONSTRAINT "temporal_mltrng2_pk" PRIMARY KEY ("id1", "id2", "valid_at" WITHOUT OVERLAPS)
;
----------
[CREATE_TABLE] create table notnull_tbl_fail (a int, primary key(a), not null a no inherit)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE inh_nn_parent ADD CONSTRAINT nna NOT NULL a
;
----------
[CREATE_TABLE] CREATE TABLE temporal_partitioned_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_partitioned_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_partitioned_fk_rng2rng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng (id, PERIOD valid_at)
) PARTITION BY LIST (id)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk2
  FOREIGN KEY (parent_id, PERIOD parent_id)
  REFERENCES temporal_rng (id, PERIOD id)
;
----------
[CREATE_TABLE] CREATE TABLE split_alter18.current_table (
    id integer PRIMARY KEY,
    parent_id integer,
    amount integer NOT NULL,
    calc integer GENERATED ALWAYS AS (amount * 2) STORED,
    payload text,
    CONSTRAINT current_table_fk FOREIGN KEY (parent_id) REFERENCES split_alter18.parent_table(id) NOT ENFORCED
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
  FOREIGN KEY (parent_id, PERIOD valid_at)
  REFERENCES temporal_rng
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1_2(a int, CONSTRAINT nn2 NOT NULL a, b int)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT|ADD_CONSTRAINT] --
-- partitioned FK referenced deletes CASCADE
--

--
-- partitioned FK referenced updates SET NULL
--

ALTER TABLE temporal_partitioned_fk_rng2rng
  DROP CONSTRAINT temporal_partitioned_fk_rng2rng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng
    ON DELETE SET NULL ON UPDATE SET NULL
;
----------
[CREATE_TABLE] CREATE TABLE gtest_tableoid (
  a int PRIMARY KEY,
  b bool GENERATED ALWAYS AS (tableoid = 'gtest_tableoid'::regclass) VIRTUAL
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_normal_child (a int, b int GENERATED ALWAYS AS (a * 2) VIRTUAL) INHERITS (gtest_normal)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id, PERIOD parent_id)
    REFERENCES temporal_mltrng (id, PERIOD id)
)
;
----------
[CREATE_TABLE] CREATE TABLE gtestm (
  a int PRIMARY KEY,
  b int GENERATED ALWAYS AS (a * 2) VIRTUAL
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_7b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (row_number() OVER (ORDER BY a)) VIRTUAL)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table constr_parent add not null a not valid
;
----------
[CREATE_TABLE] -- PK with one column plus a multirange:
CREATE TABLE temporal_mltrng (
  id int4range,
  valid_at datemultirange,
  CONSTRAINT temporal_mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE fktable2 (fk int references pktable ENFORCED NOT ENFORCED)
;
----------
[CREATE_TYPE] CREATE DOMAIN constraint_not_enforced_dom AS int CONSTRAINT the_constraint CHECK (value > 0) NOT ENFORCED
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk_rng2rng
  ADD CONSTRAINT temporal_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_rng
    ON DELETE CASCADE ON UPDATE CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE gtest31_1 (a int, b text GENERATED ALWAYS AS ('hello') VIRTUAL, c text) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child PARTITION OF gtest_parent (
    f3 WITH OPTIONS GENERATED ALWAYS AS (f2 * 2) VIRTUAL
) FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[CREATE_TABLE] create table inh_child (a int not null no inherit)
;
----------
[CREATE_TABLE] CREATE TABLE gtestnn_parent (
    f1 int,
    f2 bigint,
    f3 bigint GENERATED ALWAYS AS (nullif(f1, 1) + nullif(f2, 10)) VIRTUAL NOT NULL
) PARTITION BY RANGE (f1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- can't override
ALTER TABLE ATACC1 ADD CONSTRAINT ditto NOT NULL a
;
----------
[CREATE_TABLE] CREATE TABLE gtest23x (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) VIRTUAL REFERENCES gtest23a (x) ON UPDATE CASCADE)
;
----------
[CREATE_TABLE] -- PK with a non-range column:

CREATE TABLE temporal_rng (
  id int4range,
  valid_at TEXT,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest_child2" (
    "f1" "date" CONSTRAINT "gtest_parent_f1_not_null" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2))
)
;
----------
[CREATE_TABLE] CREATE TABLE NE_CHECK_TBL (x int,
	CONSTRAINT CHECK_CON CHECK (x > 3) NOT ENFORCED)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk ADD CONSTRAINT fk_partitioned_fk_a_b_fkey FOREIGN KEY (a, b)
	REFERENCES fk_notpartitioned_pk NOT ENFORCED
;
----------
[CREATE_TABLE] CREATE TABLE noinh_con_copy (a int CHECK (a > 0) NO INHERIT, b int not null,
	c int not null no inherit)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE FKTABLE ALTER CONSTRAINT fktable_fk_fkey NOT ENFORCED
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE notnull_tbl1 ALTER CONSTRAINT nntbl1_a NO INHERIT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1 add constraint inh_check_constraint8 check (f1 < 10) enforced
;
----------
[CREATE_TABLE] CREATE TABLE ATACC2 (a int, CONSTRAINT a_is_not_null NOT NULL a NO INHERIT)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk_mltrng2mltrng
  ADD CONSTRAINT temporal_fk_mltrng2mltrng_fk
  FOREIGN KEY (parent_id, PERIOD valid_at)
  REFERENCES temporal_mltrng (id, PERIOD valid_at)
  ON UPDATE RESTRICT
;
----------
[CREATE_TABLE] CREATE TABLE gtestm (
  id int PRIMARY KEY,
  f1 int,
  f2 int,
  f3 int GENERATED ALWAYS AS (f1 * 2) VIRTUAL,
  f4 int GENERATED ALWAYS AS (f2 * 2) VIRTUAL
)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtestx" (
    "a" integer CONSTRAINT "gtest1_a_not_null" NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 22)),
    "x" integer
)
;
----------
[CREATE_TABLE] create table notnull_tbl_fail (like notnull_tbl1, constraint foo2 not null a)
;
----------
[CREATE_TABLE] -- test update
CREATE TABLE gtest3 (a int, b int GENERATED ALWAYS AS (a * 3) VIRTUAL)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at textrange2,
  CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fk_partitioned_fk ALTER CONSTRAINT fk_partitioned_fk_a_b_fkey NOT ENFORCED
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_5b (a int PRIMARY KEY, b int GENERATED ALWAYS AS identity GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
