ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey NO INHERIT
;
ALTER TABLE pp_nn_1 ADD CONSTRAINT nn NOT NULL a NOT VALID
;
-- Can change enforceability and deferrability together
ALTER TABLE FKTABLE ALTER CONSTRAINT fk_con NOT ENFORCED NOT DEFERRABLE
;
-- virtual generated columns don't need a rewrite
ALTER TABLE has_volatile ADD col5 int GENERATED ALWAYS AS (tableoid::int + col2) VIRTUAL
;
ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey NOT ENFORCED
;
CREATE TABLE temporal_unique (id int4range NOT NULL, valid_at daterange NOT NULL, a text, UNIQUE (id, valid_at WITHOUT OVERLAPS))
;
-- fail - virtual generated column uses user-defined function
-- (Actually, this already fails at CREATE TABLE rather than at CREATE
-- PUBLICATION, but let's keep the test in case the former gets
-- relaxed sometime.)
CREATE TABLE testpub_rf_tbl6 (id int PRIMARY KEY, x int, y int GENERATED ALWAYS AS (x * testpub_rf_func2()) VIRTUAL)
;
ALTER TABLE temporal_mltrng ADD CONSTRAINT temporal_mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
;
-- UNIQUE with two columns plus a range:
CREATE TABLE temporal_rng3 (
  id1 int4range,
  id2 int4range,
  valid_at daterange,
  CONSTRAINT temporal_rng3_uq UNIQUE (id1, id2, valid_at WITHOUT OVERLAPS)
)
;
ALTER TABLE "public"."constr_parent"
    ADD CONSTRAINT "constr_parent_a_not_null" NOT NULL "a" NOT VALID
;
CREATE TABLE gtest31_1 (a int, b text GENERATED ALWAYS AS ('hello') VIRTUAL, c text)
;
-- Test not null inherit constraint which already exists on child table.
create table inh_nn1 (f1 int not null no inherit)
;
-- here, we do need a rewrite
ALTER TABLE has_volatile ALTER COLUMN col1 SET DATA TYPE float8,
  ADD COLUMN col6 float8 GENERATED ALWAYS AS (col1 * 4) VIRTUAL
;
-- succeeds
ALTER TABLE attmp3 ADD CONSTRAINT b_greater_than_ten_not_enforced CHECK (b > 10) NOT ENFORCED
;
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id)
    REFERENCES temporal_rng
)
;
ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_atcol1_not_null NOT NULL atcol1
;
CREATE TABLE gtest21a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (nullif(a, 0)) VIRTUAL NOT NULL)
;
create table notnull_tbl_fail (a int not null no inherit primary key)
;
ALTER TABLE gtest25 ADD COLUMN x int GENERATED ALWAYS AS (z * 4) VIRTUAL
;
ALTER TABLE temporal_partitioned_fk_rng2rng
  ALTER COLUMN parent_id SET DEFAULT '[-1,-1]',
  DROP CONSTRAINT temporal_partitioned_fk_rng2rng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng
    ON DELETE SET DEFAULT ON UPDATE SET DEFAULT
;
-- with mismatched PERIOD columns:

-- (parent_id, PERIOD valid_at) REFERENCES (id, valid_at)
-- REFERENCES part should specify PERIOD
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_rng (id, valid_at)
)
;
ALTER TABLE public.temporal3 ADD CONSTRAINT temporal3_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
;
CREATE TABLE gtest12 (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (gf1(b)) VIRTUAL)
;
ALTER TABLE notnull_parent_upg ADD CONSTRAINT nn NOT NULL a NOT VALID
;
ALTER TABLE temporal_rng3 ADD CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
;
CREATE TABLE testpub_rf_tbl7 (id int PRIMARY KEY, x int, y int GENERATED ALWAYS AS (x * 111) VIRTUAL)
;
-- Changing it back to ENFORCED will recreate the necessary FK triggers
-- that are deferrable and initially deferred
ALTER TABLE FKTABLE ALTER CONSTRAINT fktable_fk_fkey ENFORCED
;
CREATE TABLE split_pg18.temporal_child (
    account_id integer NOT NULL,
    valid_at tstzrange NOT NULL,
    note text,
    FOREIGN KEY (account_id, PERIOD valid_at) REFERENCES split_pg18.temporal_parent (account_id, PERIOD valid_at)
)
;
ALTER TABLE testschema.test_default_tab ADD CONSTRAINT test_default_tab_id_not_null NOT NULL id
;
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, PERIOD parent_id)
    REFERENCES temporal_rng (id, PERIOD id)
)
;
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_rng (id, valid_at)
)
;
ALTER TABLE public.atacc1 ADD CONSTRAINT atacc1_a_not_null NOT NULL a
;
CREATE TABLE gtest_part_key (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) VIRTUAL) PARTITION BY RANGE ((gtest_part_key))
;
-- can't create a primary key on a noinherit not-null
create table cnn_pk (a int not null no inherit)
;
CREATE TABLE temporal_partitioned_rng (
  id int4range,
  valid_at daterange,
  name text,
  CONSTRAINT temporal_paritioned_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
) PARTITION BY LIST (id)
;
ALTER TABLE gtest_tableoid ADD COLUMN
  c regclass GENERATED ALWAYS AS (tableoid) VIRTUAL
;
--
-- partitioned FK referenced updates CASCADE
--

ALTER TABLE temporal_partitioned_fk_rng2rng
  DROP CONSTRAINT temporal_partitioned_fk_rng2rng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng
    ON DELETE CASCADE ON UPDATE CASCADE
;
create table gtest34 (id int primary key, a int,
                      c int generated always as (a * 10) virtual)
;
-- (parent_id) REFERENCES (id, PERIOD valid_at)
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id)
    REFERENCES temporal_mltrng (id, PERIOD valid_at)
)
;
create table cnn_pk (a int not null no inherit)
;
ALTER TABLE ATACC1 ADD NOT NULL a NO INHERIT
;
CREATE TABLE gtest2 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (NULL) VIRTUAL)
;
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id)
    REFERENCES temporal_rng (id, PERIOD valid_at)
)
;
CREATE TABLE "generated_virtual_tests"."gtest_tableoid" (
    "a" integer NOT NULL,
    "b" boolean GENERATED ALWAYS AS (("tableoid" = ('"generated_virtual_tests"."gtest_tableoid"'::"regclass")::"oid")),
    "c" "regclass" GENERATED ALWAYS AS ("tableoid")
)
;
CREATE TABLE "generated_virtual_tests"."gtest28a" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2))
)
;
CREATE TABLE "generated_virtual_tests"."gtest30_1" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2))
)
;
CREATE TABLE gtest_child3 PARTITION OF gtest_parent (
    f3 GENERATED ALWAYS AS (f2 * 2) VIRTUAL  -- error
) FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
CREATE TABLE "generated_virtual_tests"."gtest28b" (
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2))
)
;
CREATE TABLE temporal_mltrng (
  id int4range,
  valid_at datemultirange,
  CONSTRAINT temporal_mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
CREATE TABLE "generated_virtual_tests"."gtest27" (
    "a" bigint,
    "b" bigint,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "x" bigint GENERATED ALWAYS AS ((("a" + "b") * 2))
)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey NO INHERIT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pp_nn_1 ADD CONSTRAINT nn NOT NULL a NOT VALID
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- Can change enforceability and deferrability together
ALTER TABLE FKTABLE ALTER CONSTRAINT fk_con NOT ENFORCED NOT DEFERRABLE
;
----------
[ALTER_TABLE|ADD_COLUMN] -- virtual generated columns don't need a rewrite
ALTER TABLE has_volatile ADD col5 int GENERATED ALWAYS AS (tableoid::int + col2) VIRTUAL
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey NOT ENFORCED
;
----------
[CREATE_TABLE] CREATE TABLE temporal_unique (id int4range NOT NULL, valid_at daterange NOT NULL, a text, UNIQUE (id, valid_at WITHOUT OVERLAPS))
;
----------
[CREATE_TABLE] -- fail - virtual generated column uses user-defined function
-- (Actually, this already fails at CREATE TABLE rather than at CREATE
-- PUBLICATION, but let's keep the test in case the former gets
-- relaxed sometime.)
CREATE TABLE testpub_rf_tbl6 (id int PRIMARY KEY, x int, y int GENERATED ALWAYS AS (x * testpub_rf_func2()) VIRTUAL)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_mltrng ADD CONSTRAINT temporal_mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
;
----------
[CREATE_TABLE] -- UNIQUE with two columns plus a range:
CREATE TABLE temporal_rng3 (
  id1 int4range,
  id2 int4range,
  valid_at daterange,
  CONSTRAINT temporal_rng3_uq UNIQUE (id1, id2, valid_at WITHOUT OVERLAPS)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."constr_parent"
    ADD CONSTRAINT "constr_parent_a_not_null" NOT NULL "a" NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE gtest31_1 (a int, b text GENERATED ALWAYS AS ('hello') VIRTUAL, c text)
;
----------
[CREATE_TABLE] -- Test not null inherit constraint which already exists on child table.
create table inh_nn1 (f1 int not null no inherit)
;
----------
[ALTER_TABLE|ALTER_COLUMN|ADD_COLUMN] -- here, we do need a rewrite
ALTER TABLE has_volatile ALTER COLUMN col1 SET DATA TYPE float8,
  ADD COLUMN col6 float8 GENERATED ALWAYS AS (col1 * 4) VIRTUAL
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- succeeds
ALTER TABLE attmp3 ADD CONSTRAINT b_greater_than_ten_not_enforced CHECK (b > 10) NOT ENFORCED
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id)
    REFERENCES temporal_rng
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_atcol1_not_null NOT NULL atcol1
;
----------
[CREATE_TABLE] CREATE TABLE gtest21a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (nullif(a, 0)) VIRTUAL NOT NULL)
;
----------
[CREATE_TABLE] create table notnull_tbl_fail (a int not null no inherit primary key)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest25 ADD COLUMN x int GENERATED ALWAYS AS (z * 4) VIRTUAL
;
----------
[ALTER_TABLE|ALTER_COLUMN|DROP_CONSTRAINT|ADD_CONSTRAINT] ALTER TABLE temporal_partitioned_fk_rng2rng
  ALTER COLUMN parent_id SET DEFAULT '[-1,-1]',
  DROP CONSTRAINT temporal_partitioned_fk_rng2rng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng
    ON DELETE SET DEFAULT ON UPDATE SET DEFAULT
;
----------
[CREATE_TABLE] -- with mismatched PERIOD columns:

-- (parent_id, PERIOD valid_at) REFERENCES (id, valid_at)
-- REFERENCES part should specify PERIOD
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_rng (id, valid_at)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.temporal3 ADD CONSTRAINT temporal3_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
;
----------
[CREATE_TABLE] CREATE TABLE gtest12 (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (gf1(b)) VIRTUAL)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE notnull_parent_upg ADD CONSTRAINT nn NOT NULL a NOT VALID
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_rng3 ADD CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
;
----------
[CREATE_TABLE] CREATE TABLE testpub_rf_tbl7 (id int PRIMARY KEY, x int, y int GENERATED ALWAYS AS (x * 111) VIRTUAL)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- Changing it back to ENFORCED will recreate the necessary FK triggers
-- that are deferrable and initially deferred
ALTER TABLE FKTABLE ALTER CONSTRAINT fktable_fk_fkey ENFORCED
;
----------
[CREATE_TABLE] CREATE TABLE split_pg18.temporal_child (
    account_id integer NOT NULL,
    valid_at tstzrange NOT NULL,
    note text,
    FOREIGN KEY (account_id, PERIOD valid_at) REFERENCES split_pg18.temporal_parent (account_id, PERIOD valid_at)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testschema.test_default_tab ADD CONSTRAINT test_default_tab_id_not_null NOT NULL id
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, PERIOD parent_id)
    REFERENCES temporal_rng (id, PERIOD id)
)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_rng (id, valid_at)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.atacc1 ADD CONSTRAINT atacc1_a_not_null NOT NULL a
;
----------
[CREATE_TABLE] CREATE TABLE gtest_part_key (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) VIRTUAL) PARTITION BY RANGE ((gtest_part_key))
;
----------
[CREATE_TABLE] -- can't create a primary key on a noinherit not-null
create table cnn_pk (a int not null no inherit)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_partitioned_rng (
  id int4range,
  valid_at daterange,
  name text,
  CONSTRAINT temporal_paritioned_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
) PARTITION BY LIST (id)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest_tableoid ADD COLUMN
  c regclass GENERATED ALWAYS AS (tableoid) VIRTUAL
;
----------
[ALTER_TABLE|DROP_CONSTRAINT|ADD_CONSTRAINT] --
-- partitioned FK referenced updates CASCADE
--

ALTER TABLE temporal_partitioned_fk_rng2rng
  DROP CONSTRAINT temporal_partitioned_fk_rng2rng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_rng2rng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_rng
    ON DELETE CASCADE ON UPDATE CASCADE
;
----------
[CREATE_TABLE] create table gtest34 (id int primary key, a int,
                      c int generated always as (a * 10) virtual)
;
----------
[CREATE_TABLE] -- (parent_id) REFERENCES (id, PERIOD valid_at)
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_mltrng2mltrng_fk FOREIGN KEY (parent_id)
    REFERENCES temporal_mltrng (id, PERIOD valid_at)
)
;
----------
[CREATE_TABLE] create table cnn_pk (a int not null no inherit)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ATACC1 ADD NOT NULL a NO INHERIT
;
----------
[CREATE_TABLE] CREATE TABLE gtest2 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (NULL) VIRTUAL)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id)
    REFERENCES temporal_rng (id, PERIOD valid_at)
)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest_tableoid" (
    "a" integer NOT NULL,
    "b" boolean GENERATED ALWAYS AS (("tableoid" = ('"generated_virtual_tests"."gtest_tableoid"'::"regclass")::"oid")),
    "c" "regclass" GENERATED ALWAYS AS ("tableoid")
)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest28a" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2))
)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest30_1" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2))
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child3 PARTITION OF gtest_parent (
    f3 GENERATED ALWAYS AS (f2 * 2) VIRTUAL  -- error
) FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest28b" (
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2))
)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_mltrng (
  id int4range,
  valid_at datemultirange,
  CONSTRAINT temporal_mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest27" (
    "a" bigint,
    "b" bigint,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "x" bigint GENERATED ALWAYS AS ((("a" + "b") * 2))
)
;
