CREATE STATISTICS tst ON x + y FROM ext_stats_test
;
CREATE STATISTICS tst ON (x, y) FROM ext_stats_test
;
-- ok - with lsn = NONE
ALTER SUBSCRIPTION regress_testsub SKIP (lsn = NONE)
;
GRANT SET ON PARAMETER auto_explain.log_format TO regress_user1
;
REVOKE ALTER SYSTEM ON PARAMETER enable_material FROM PUBLIC
;
-- fail, privileges remain
-- Use "revoke" to remove the privileges and allow the role to be dropped
REVOKE SET, ALTER SYSTEM ON PARAMETER
    autovacuum_work_mem, hash_mem_multiplier, max_stack_depth,
    shared_buffers, temp_file_limit, work_mem
FROM regress_host_resource_admin
;
REVOKE SET, ALTER SYSTEM ON PARAMETER
    autovacuum_work_mem, hash_mem_multiplier, max_stack_depth,
    shared_buffers, temp_file_limit, work_mem
FROM regress_host_resource_admin
;
ALTER SUBSCRIPTION sub SKIP (lsn = '0/158A4A0')
;
PREPARE split_merge_plan (integer, text) AS MERGE INTO split_session_plan.plan_items t USING (VALUES ($1, $2)) AS s(id, note) ON t.id = s.id WHEN MATCHED THEN UPDATE SET note = s.note WHEN NOT MATCHED THEN INSERT (id, note) VALUES (s.id, s.note)
;
GRANT SET ON PARAMETER test_oat_hooks.user_var1 TO regress_role_joe
;
------------------------------------------------------------------------------------------
[ADMIN_PERFORMANCE] CREATE STATISTICS tst ON x + y FROM ext_stats_test
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS tst ON (x, y) FROM ext_stats_test
;
----------
[ADMIN_PUB_SUB] -- ok - with lsn = NONE
ALTER SUBSCRIPTION regress_testsub SKIP (lsn = NONE)
;
----------
[GRANT] GRANT SET ON PARAMETER auto_explain.log_format TO regress_user1
;
----------
[REVOKE] REVOKE ALTER SYSTEM ON PARAMETER enable_material FROM PUBLIC
;
----------
[REVOKE] -- fail, privileges remain
-- Use "revoke" to remove the privileges and allow the role to be dropped
REVOKE SET, ALTER SYSTEM ON PARAMETER
    autovacuum_work_mem, hash_mem_multiplier, max_stack_depth,
    shared_buffers, temp_file_limit, work_mem
FROM regress_host_resource_admin
;
----------
[REVOKE] REVOKE SET, ALTER SYSTEM ON PARAMETER
    autovacuum_work_mem, hash_mem_multiplier, max_stack_depth,
    shared_buffers, temp_file_limit, work_mem
FROM regress_host_resource_admin
;
----------
[ADMIN_PUB_SUB] ALTER SUBSCRIPTION sub SKIP (lsn = '0/158A4A0')
;
----------
[UNSAFE] PREPARE split_merge_plan (integer, text) AS MERGE INTO split_session_plan.plan_items t USING (VALUES ($1, $2)) AS s(id, note) ON t.id = s.id WHEN MATCHED THEN UPDATE SET note = s.note WHEN NOT MATCHED THEN INSERT (id, note) VALUES (s.id, s.note)
;
----------
[GRANT] GRANT SET ON PARAMETER test_oat_hooks.user_var1 TO regress_role_joe
;
