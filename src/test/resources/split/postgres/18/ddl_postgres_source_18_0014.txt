create table inh_nn3 (f4 float, constraint nn3_f1 not null f1 no inherit) inherits (inh_nn1, inh_nn2)
;
CREATE TABLE gtest_err_5a (a int PRIMARY KEY, b int DEFAULT 5 GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
-- nope
ALTER TABLE notnull_tbl1_child2 ADD NOT NULL a NOT VALID
;
-- Disallow specifying conflicting NO INHERIT flags for the same constraint
CREATE TABLE inh_nn1 (a int primary key, b int, not null a no inherit)
;
ALTER PUBLICATION pub_mix_1 SET TABLE test_mix_1 (a, b)
;
alter table "catalog1".schema1."test" alter column "a1" drop expression
;
CREATE TABLE testpub_tbl5 (a int PRIMARY KEY, b text, c text,
    d int generated always as (a + length(b)) stored,
    e int generated always as (a + length(b)) virtual
)
;
alter table inh_nn1 alter constraint foo inherit
;
-- Two scalar columns:
CREATE TABLE temporal_fk2_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id1 int4range,
  parent_id2 int4range,
  CONSTRAINT temporal_fk2_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
-- drop schema that is not present in the publication
ALTER PUBLICATION testpub1_forschema DROP TABLES IN SCHEMA pub_test2
;
-- fail - can't set schema to 'FOR ALL TABLES' publication
ALTER PUBLICATION testpub_foralltables SET TABLES IN SCHEMA pub_test
;
ALTER PUBLICATION testpub_fortable SET TABLE testpub_tbl7 (a, b)
;
create table p1(f1 int constraint p1_a_check check (f1 > 0) not enforced)
;
CREATE TABLE cmdata(f1 text COMPRESSION pglz)
;
CREATE PUBLICATION testpub_rf_yes FOR TABLE testpub_rf_tbl1 WHERE (a > 1) WITH (publish = 'insert')
;
CREATE PUBLICATION tap_pub_not_used FOR TABLE tab_rowfilter_1 WHERE (a < 0)
;
create table inh_child (f1 int not null no inherit, f2 int)
;
CREATE FUNCTION "public"."fipshash"("text") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE STRICT LEAKPROOF PARALLEL SAFE
    RETURN "substr"("encode"("sha256"(($1)::"bytea"), 'hex'::"text"), 1, 32)
;
-- failure - specify a schema when there is already a column list in the
-- publication
ALTER PUBLICATION testpub_tbl9 ADD TABLES IN SCHEMA public
;
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, valid_at)
    REFERENCES temporal_rng (id, valid_at)
)
;
CREATE TABLE temporal_pk (id int4range, valid_at daterange, a text, PRIMARY KEY (id, valid_at WITHOUT OVERLAPS))
;
ALTER TABLE "public"."ne_check_tbl"
    ADD CONSTRAINT "check_con" CHECK (("x" > 3)) NOT ENFORCED
;
alter database abc refresh collation version
;
CREATE TABLE "generated_virtual_tests"."gtest1_1" (
    "a" integer CONSTRAINT "gtest1_a_not_null" NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2))
)
;
alter domain dnotnulltest add constraint dnotnulltest_notnull not null
;
CREATE OR REPLACE FUNCTION obj_description(oid)
 RETURNS text
 LANGUAGE sql
 STABLE PARALLEL SAFE STRICT
BEGIN ATOMIC
select description from pg_description where objoid = $1 and objsubid = 0;
END
;
ALTER TABLE split_alter18.current_table ALTER CONSTRAINT current_table_fk NOT ENFORCED
;
CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at daterange,
  CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
CREATE TABLE dump_test.test_table_tpk (
							col1 int4range,
							col2 tstzrange,
							CONSTRAINT test_table_tpk_pkey PRIMARY KEY (col1, col2 WITHOUT OVERLAPS))
;
-- references to other generated columns, including self-references
CREATE TABLE gtest_err_2a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (b * 2) VIRTUAL)
;
CREATE PUBLICATION tap_pub_allinschema FOR TABLES IN SCHEMA schema_rf_x, TABLE schema_rf_x.tab_rf_x WHERE (x > 10)
;
CREATE PUBLICATION testpub6 FOR TABLE testpub_rf_tbl3 WHERE (e =#> 27)
;
ALTER TABLE am_partitioned SET ACCESS METHOD heap2
;
ANALYZE ONLY only_inh_parent
;
create table notnull_tbl_fail (a serial, constraint foo not null a, constraint bar not null a)
;
create function rls_f(text) returns text
begin atomic
 select set_config('rls_test.blah', $1, true) || set_config('row_security', 'false', true) || string_agg(c, ',' order by c) from rls_t;
end
;
-- Reverting it back to ENFORCED will result in failure because constraint validation will be triggered,
-- as it was previously in a valid state.
ALTER TABLE FKTABLE ALTER CONSTRAINT fktable_ftest1_fkey ENFORCED
;
VACUUM ONLY vacparted
;
--
-- test PARTITION BY for ranges
--

-- temporal PRIMARY KEY:
CREATE TABLE temporal_partitioned (
  id int4range,
  valid_at daterange,
  name text,
  CONSTRAINT temporal_paritioned_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
) PARTITION BY LIST (id)
;
CREATE OR REPLACE FUNCTION earth() RETURNS float8
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
RETURN '6378168'::float8
;
create table notnull_tbl3 (constraint foo not null a, a int generated by default as identity)
;
ALTER PUBLICATION testpub6 SET TABLE rf_tbl_abcd_pk WHERE (d > 99)
;
CREATE TABLE r1 (a int, b int GENERATED ALWAYS AS (a * 10) VIRTUAL)
;
ALTER PUBLICATION testpub1_forschema ADD TABLES IN SCHEMA non_existent_schema
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.date() < $x' PASSING '12:34'::timetz AS x))
;
-- test compression with partition
CREATE TABLE cmpart(f1 text COMPRESSION lz4) PARTITION BY HASH(f1)
;
-- (parent_id, valid_at) REFERENCES (id, PERIOD valid_at)
-- FOREIGN KEY part should specify PERIOD
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, valid_at)
    REFERENCES temporal_rng (id, PERIOD valid_at)
)
;
CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
ALTER TABLE temporal_fk2_mltrng2mltrng
  ADD CONSTRAINT temporal_fk2_mltrng2mltrng_fk
  FOREIGN KEY (parent_id1, parent_id2, PERIOD valid_at)
  REFERENCES temporal_mltrng2 (id1, id2, PERIOD valid_at)
;
create view view_of_joins_2b as select * from tbl1 join tbl1a using (a) as x
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table inh_nn3 (f4 float, constraint nn3_f1 not null f1 no inherit) inherits (inh_nn1, inh_nn2)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_5a (a int PRIMARY KEY, b int DEFAULT 5 GENERATED ALWAYS AS (a * 2) VIRTUAL)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- nope
ALTER TABLE notnull_tbl1_child2 ADD NOT NULL a NOT VALID
;
----------
[CREATE_TABLE] -- Disallow specifying conflicting NO INHERIT flags for the same constraint
CREATE TABLE inh_nn1 (a int primary key, b int, not null a no inherit)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub_mix_1 SET TABLE test_mix_1 (a, b)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table "catalog1".schema1."test" alter column "a1" drop expression
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl5 (a int PRIMARY KEY, b text, c text,
    d int generated always as (a + length(b)) stored,
    e int generated always as (a + length(b)) virtual
)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] alter table inh_nn1 alter constraint foo inherit
;
----------
[CREATE_TABLE] -- Two scalar columns:
CREATE TABLE temporal_fk2_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id1 int4range,
  parent_id2 int4range,
  CONSTRAINT temporal_fk2_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[ALTER_PUB_SUB] -- drop schema that is not present in the publication
ALTER PUBLICATION testpub1_forschema DROP TABLES IN SCHEMA pub_test2
;
----------
[ALTER_PUB_SUB] -- fail - can't set schema to 'FOR ALL TABLES' publication
ALTER PUBLICATION testpub_foralltables SET TABLES IN SCHEMA pub_test
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_fortable SET TABLE testpub_tbl7 (a, b)
;
----------
[CREATE_TABLE] create table p1(f1 int constraint p1_a_check check (f1 > 0) not enforced)
;
----------
[CREATE_TABLE] CREATE TABLE cmdata(f1 text COMPRESSION pglz)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_rf_yes FOR TABLE testpub_rf_tbl1 WHERE (a > 1) WITH (publish = 'insert')
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_not_used FOR TABLE tab_rowfilter_1 WHERE (a < 0)
;
----------
[CREATE_TABLE] create table inh_child (f1 int not null no inherit, f2 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."fipshash"("text") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE STRICT LEAKPROOF PARALLEL SAFE
    RETURN "substr"("encode"("sha256"(($1)::"bytea"), 'hex'::"text"), 1, 32)
;
----------
[ALTER_PUB_SUB] -- failure - specify a schema when there is already a column list in the
-- publication
ALTER PUBLICATION testpub_tbl9 ADD TABLES IN SCHEMA public
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, valid_at)
    REFERENCES temporal_rng (id, valid_at)
)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_pk (id int4range, valid_at daterange, a text, PRIMARY KEY (id, valid_at WITHOUT OVERLAPS))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."ne_check_tbl"
    ADD CONSTRAINT "check_con" CHECK (("x" > 3)) NOT ENFORCED
;
----------
[ALTER_CATALOG] alter database abc refresh collation version
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest1_1" (
    "a" integer CONSTRAINT "gtest1_a_not_null" NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2))
)
;
----------
[ALTER_TYPE] alter domain dnotnulltest add constraint dnotnulltest_notnull not null
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION obj_description(oid)
 RETURNS text
 LANGUAGE sql
 STABLE PARALLEL SAFE STRICT
BEGIN ATOMIC
select description from pg_description where objoid = $1 and objsubid = 0;
END
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE split_alter18.current_table ALTER CONSTRAINT current_table_fk NOT ENFORCED
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at daterange,
  CONSTRAINT temporal_rng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_tpk (
							col1 int4range,
							col2 tstzrange,
							CONSTRAINT test_table_tpk_pkey PRIMARY KEY (col1, col2 WITHOUT OVERLAPS))
;
----------
[CREATE_TABLE] -- references to other generated columns, including self-references
CREATE TABLE gtest_err_2a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (b * 2) VIRTUAL)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_allinschema FOR TABLES IN SCHEMA schema_rf_x, TABLE schema_rf_x.tab_rf_x WHERE (x > 10)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub6 FOR TABLE testpub_rf_tbl3 WHERE (e =#> 27)
;
----------
[ALTER_TABLE] ALTER TABLE am_partitioned SET ACCESS METHOD heap2
;
----------
[ADMIN_TABLE] ANALYZE ONLY only_inh_parent
;
----------
[CREATE_TABLE] create table notnull_tbl_fail (a serial, constraint foo not null a, constraint bar not null a)
;
----------
[CREATE_PROG_OBJ] create function rls_f(text) returns text
begin atomic
 select set_config('rls_test.blah', $1, true) || set_config('row_security', 'false', true) || string_agg(c, ',' order by c) from rls_t;
end
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- Reverting it back to ENFORCED will result in failure because constraint validation will be triggered,
-- as it was previously in a valid state.
ALTER TABLE FKTABLE ALTER CONSTRAINT fktable_ftest1_fkey ENFORCED
;
----------
[ADMIN_TABLE] VACUUM ONLY vacparted
;
----------
[CREATE_TABLE] --
-- test PARTITION BY for ranges
--

-- temporal PRIMARY KEY:
CREATE TABLE temporal_partitioned (
  id int4range,
  valid_at daterange,
  name text,
  CONSTRAINT temporal_paritioned_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
) PARTITION BY LIST (id)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION earth() RETURNS float8
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
RETURN '6378168'::float8
;
----------
[CREATE_TABLE] create table notnull_tbl3 (constraint foo not null a, a int generated by default as identity)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub6 SET TABLE rf_tbl_abcd_pk WHERE (d > 99)
;
----------
[CREATE_TABLE] CREATE TABLE r1 (a int, b int GENERATED ALWAYS AS (a * 10) VIRTUAL)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub1_forschema ADD TABLES IN SCHEMA non_existent_schema
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.date() < $x' PASSING '12:34'::timetz AS x))
;
----------
[CREATE_TABLE] -- test compression with partition
CREATE TABLE cmpart(f1 text COMPRESSION lz4) PARTITION BY HASH(f1)
;
----------
[CREATE_TABLE] -- (parent_id, valid_at) REFERENCES (id, PERIOD valid_at)
-- FOREIGN KEY part should specify PERIOD
CREATE TABLE temporal_fk_rng2rng (
  id int4range,
  valid_at daterange,
  parent_id int4range,
  CONSTRAINT temporal_fk_rng2rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS),
  CONSTRAINT temporal_fk_rng2rng_fk FOREIGN KEY (parent_id, valid_at)
    REFERENCES temporal_rng (id, PERIOD valid_at)
)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_fk_mltrng2mltrng (
  id int4range,
  valid_at datemultirange,
  parent_id int4range,
  CONSTRAINT temporal_fk_mltrng2mltrng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_fk2_mltrng2mltrng
  ADD CONSTRAINT temporal_fk2_mltrng2mltrng_fk
  FOREIGN KEY (parent_id1, parent_id2, PERIOD valid_at)
  REFERENCES temporal_mltrng2 (id1, id2, PERIOD valid_at)
;
----------
[CREATE_VIEW(SELECT)] create view view_of_joins_2b as select * from tbl1 join tbl1a using (a) as x
;
