CREATE FUNCTION dump_test.ordering_func ()
			RETURNS SETOF dump_test.ordering_table
			LANGUAGE sql BEGIN ATOMIC
			SELECT * FROM dump_test.ordering_table GROUP BY id; END
;
CREATE TABLE UNIQUE_TBL (i int UNIQUE NULLS NOT DISTINCT, t text)
;
CREATE OR REPLACE FUNCTION earth_box(earth, float8)
RETURNS public.cube
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
RETURN public.cube_enlarge($1, gc_to_sec($2), 3)
;
CREATE TABLE gtest28 OF gtest_type (f1 WITH OPTIONS GENERATED ALWAYS AS (f2 *2) VIRTUAL)
;
CREATE OR REPLACE FUNCTION polygon(circle)
 RETURNS polygon
 LANGUAGE sql
 IMMUTABLE PARALLEL SAFE STRICT COST 1
RETURN polygon(12, $1)
;
CREATE TABLE fk_partitioned_fk_2 (b int, a int,
	CONSTRAINT fk_part_con FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk ON UPDATE CASCADE ON DELETE CASCADE NOT ENFORCED)
;
CREATE TABLE "generated_virtual_tests"."gtest21a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULLIF("a", 0)) NOT NULL
)
;
ANALYZE ONLY only_parted
;
create table notnull_tbl6 (like notnull_tbl1, constraint foo not null a)
;
CREATE TABLE gtest10 (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (b * 2) VIRTUAL)
;
-- alter publication drop schema
ALTER PUBLICATION testpub1_forschema DROP TABLES IN SCHEMA pub_test2
;
ALTER PUBLICATION testpub_tbl9 ADD TABLE public.testpub_tbl7(a)
;
ALTER TABLE temporal_mltrng3 ADD CONSTRAINT temporal_mltrng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
;
CREATE OR REPLACE FUNCTION translate( citext, citext, text ) RETURNS TEXT
LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
RETURN pg_catalog.translate( pg_catalog.translate( $1::text, pg_catalog.lower($2::text), $3), pg_catalog.upper($2::text), $3)
;
ALTER TABLE ONLY "public"."temporal_fk2_mltrng2mltrng"
    ADD CONSTRAINT "temporal_fk2_mltrng2mltrng_pk" PRIMARY KEY ("id", "valid_at" WITHOUT OVERLAPS)
;
ALTER TABLE FKTABLE ALTER CONSTRAINT fk_con NOT ENFORCED NOT DEFERRABLE
;
alter table inh_nn3 alter constraint nn3_f1 inherit
;
ALTER PUBLICATION split_pub_cols ADD TABLE split_fdw_repl.accounts_extra WHERE (amount >= 10)
;
CREATE FUNCTION _pg_numeric_precision(typid oid, typmod int4) RETURNS integer
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
RETURN
  CASE $1
         WHEN 21 /*int2*/ THEN 16
         WHEN 23 /*int4*/ THEN 32
         WHEN 20 /*int8*/ THEN 64
         WHEN 1700 /*numeric*/ THEN
              CASE WHEN $2 = -1
                   THEN null
                   ELSE (($2 - 4) >> 16) & 0xFFFF
                   END
         WHEN 700 /*float4*/ THEN 24 /*FLT_MANT_DIG*/
         WHEN 701 /*float8*/ THEN 53 /*DBL_MANT_DIG*/
         ELSE null
  END
;
ALTER TABLE range_parted2 DETACH PARTITION part_rp CONCURRENTLY
;
CREATE TABLE gtest_err_7a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (avg(a)) VIRTUAL)
;
ALTER PUBLICATION pub8 ADD TABLE test_part_c_1 (a)
;
ANALYZE ONLY vacparted(a,b)
;
ALTER TABLE pp_nn_1 ADD CONSTRAINT nn NOT NULL a NO INHERIT
;
--
-- partitioned FK referenced deletes CASCADE
--

--
-- partitioned FK referenced updates SET NULL
--

ALTER TABLE temporal_partitioned_fk_mltrng2mltrng
  DROP CONSTRAINT temporal_partitioned_fk_mltrng2mltrng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_mltrng2mltrng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_mltrng
    ON DELETE SET NULL ON UPDATE SET NULL
;
CREATE TABLE ctlt1 (a text CHECK (length(a) > 2) ENFORCED PRIMARY KEY,
	b text CHECK (length(b) > 100) NOT ENFORCED)
;
CREATE OR REPLACE FUNCTION
  pg_freespace(rel regclass, blkno OUT bigint, avail OUT int2)
RETURNS SETOF RECORD
LANGUAGE SQL PARALLEL SAFE
BEGIN ATOMIC
  SELECT blkno, pg_freespace($1, blkno) AS avail
  FROM generate_series('0'::bigint, pg_relation_size($1) / current_setting('block_size'::text)::bigint - '1'::bigint) AS blkno;
END
;
ALTER TABLE temporal3
  ADD COLUMN valid_at daterange,
  ADD CONSTRAINT temporal3_uq
  UNIQUE (id, valid_at WITHOUT OVERLAPS)
;
CREATE TABLE INSERT_TBL (x INT DEFAULT nextval('insert_seq'),
	y TEXT DEFAULT '-NULL-',
	z INT DEFAULT -1 * currval('insert_seq'),
	CONSTRAINT INSERT_TBL_CON CHECK (x >= 3 AND y <> 'check failed' AND x < 8),
	CHECK (x + z = 0) ENFORCED, /* no change it is a default */
	CONSTRAINT NE_INSERT_TBL_CON CHECK (x + z = 1) NOT ENFORCED)
;
ALTER TYPE myvarchar SET (
    send = myvarcharsend,
    receive = myvarcharrecv,
    typmod_in = varchartypmodin,
    typmod_out = varchartypmodout,
    -- these are bogus, but it's safe as long as we don't use the type:
    analyze = ts_typanalyze,
    subscript = raw_array_subscript_handler
)
;
ALTER PUBLICATION testpub_fortable SET TABLE testpub_tbl5 (a, a)
;
-- ok - conditional expressions are allowed
ALTER PUBLICATION testpub5 SET TABLE testpub_rf_tbl5 WHERE (a IS DOCUMENT)
;
CREATE TABLE notnull_tbl4_lk3 (LIKE notnull_tbl4 INCLUDING INDEXES, NOT NULL a)
;
CREATE OR REPLACE FUNCTION bit_length(bit)
 RETURNS integer
 LANGUAGE sql
 IMMUTABLE PARALLEL SAFE STRICT COST 1
RETURN length($1)
;
-- PK with a range column/PERIOD that isn't there:

CREATE TABLE temporal_rng (
  id INTEGER,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
/* Given an index's OID and an underlying-table column number, return the
 * column's position in the index (NULL if not there) */
CREATE FUNCTION _pg_index_position(oid, smallint) RETURNS int
    LANGUAGE sql STRICT STABLE
BEGIN ATOMIC
SELECT (ss.a).n FROM
  (SELECT information_schema._pg_expandarray(indkey) AS a
   FROM pg_catalog.pg_index WHERE indexrelid = $1) ss
  WHERE (ss.a).x = $2;
END
;
CREATE FUNCTION functest_S_xx(x anyarray) RETURNS anyelement
    LANGUAGE SQL
    RETURN x[1]
;
DROP DATABASE IF EXISTS split_db_probe WITH (FORCE)
;
-- should be able to drop schema from 'FOR TABLE' publication
ALTER PUBLICATION testpub_fortable DROP TABLES IN SCHEMA pub_test
;
ALTER PUBLICATION testpub1_forschema DROP TABLES IN SCHEMA non_existent_schema
;
CREATE PUBLICATION testpub7 FOR TABLE testpub_rf_tbl6 WHERE (y > 100)
;
CREATE TABLE notnull_part1_2_upg (a int, CONSTRAINT nn2 NOT NULL a, b int)
;
CREATE PUBLICATION pub1 for table t1(c1, c2, c3)
;
ALTER TABLE public.skip_wal_skip_rewrite_index ADD CONSTRAINT skip_wal_skip_rewrite_index_c_not_null NOT NULL c
;
CREATE TABLE gtest27 (
    a int,
    b int,
    x int GENERATED ALWAYS AS ((a + b) * 2) VIRTUAL
)
;
--
-- Similarly, check the merging of existing constraints; a parent constraint
-- marked as NOT ENFORCED can merge with an ENFORCED child constraint, but the
-- reverse is not allowed.
--
create table p1(f1 int constraint p1_a_check check (f1 > 0) not enforced)
;
CREATE OR REPLACE VIEW jsonb_table_view7 AS
SELECT sub.* FROM s,
	(values(23)) x(x),
	generate_series(13, 13) y,
	JSON_TABLE(js, '$' AS c1 PASSING x AS x, y AS y
	COLUMNS (
		xx1 int PATH '$.c',
		NESTED PATH '$.a.za[2]' COLUMNS (NESTED PATH '$.z22[*]' as z22 COLUMNS (c int PATH '$' WITHOUT WRAPPER OMIT QUOTES)),
		NESTED PATH '$.a.za[1]' COLUMNS (d json PATH '$ ? (@.z21[*] == ($"x" -1))' WITH WRAPPER),
		NESTED PATH '$.a.za[0]' COLUMNS (NESTED PATH '$.z1[*] ? (@ >= ($"x" -2))' as z1 COLUMNS (a int PATH '$' KEEP QUOTES)),
		NESTED PATH '$.a.za[1]' COLUMNS
			(NESTED PATH '$.z21[*] ? (@ >= ($"y" +121))' as z21 COLUMNS (b int PATH '$ ? (@ > ($"x" +111))' DEFAULT 0 ON EMPTY))
	)) sub
;
ALTER PUBLICATION testpub6 SET TABLE rf_tbl_abcd_pk (a)
;
CREATE PUBLICATION tap_pub_4a FOR TABLE tab_rowfilter_4 WHERE (c % 2 = 0)
;
CREATE TABLE "generated_virtual_tests"."gtest29" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 3))
)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION dump_test.ordering_func ()
			RETURNS SETOF dump_test.ordering_table
			LANGUAGE sql BEGIN ATOMIC
			SELECT * FROM dump_test.ordering_table GROUP BY id; END
;
----------
[CREATE_TABLE] CREATE TABLE UNIQUE_TBL (i int UNIQUE NULLS NOT DISTINCT, t text)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION earth_box(earth, float8)
RETURNS public.cube
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
RETURN public.cube_enlarge($1, gc_to_sec($2), 3)
;
----------
[CREATE_TABLE] CREATE TABLE gtest28 OF gtest_type (f1 WITH OPTIONS GENERATED ALWAYS AS (f2 *2) VIRTUAL)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION polygon(circle)
 RETURNS polygon
 LANGUAGE sql
 IMMUTABLE PARALLEL SAFE STRICT COST 1
RETURN polygon(12, $1)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_2 (b int, a int,
	CONSTRAINT fk_part_con FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk ON UPDATE CASCADE ON DELETE CASCADE NOT ENFORCED)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest21a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULLIF("a", 0)) NOT NULL
)
;
----------
[ADMIN_TABLE] ANALYZE ONLY only_parted
;
----------
[CREATE_TABLE] create table notnull_tbl6 (like notnull_tbl1, constraint foo not null a)
;
----------
[CREATE_TABLE] CREATE TABLE gtest10 (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (b * 2) VIRTUAL)
;
----------
[ALTER_PUB_SUB] -- alter publication drop schema
ALTER PUBLICATION testpub1_forschema DROP TABLES IN SCHEMA pub_test2
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_tbl9 ADD TABLE public.testpub_tbl7(a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal_mltrng3 ADD CONSTRAINT temporal_mltrng3_uq UNIQUE (id, valid_at WITHOUT OVERLAPS)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION translate( citext, citext, text ) RETURNS TEXT
LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
RETURN pg_catalog.translate( pg_catalog.translate( $1::text, pg_catalog.lower($2::text), $3), pg_catalog.upper($2::text), $3)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."temporal_fk2_mltrng2mltrng"
    ADD CONSTRAINT "temporal_fk2_mltrng2mltrng_pk" PRIMARY KEY ("id", "valid_at" WITHOUT OVERLAPS)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE FKTABLE ALTER CONSTRAINT fk_con NOT ENFORCED NOT DEFERRABLE
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] alter table inh_nn3 alter constraint nn3_f1 inherit
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION split_pub_cols ADD TABLE split_fdw_repl.accounts_extra WHERE (amount >= 10)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _pg_numeric_precision(typid oid, typmod int4) RETURNS integer
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
RETURN
  CASE $1
         WHEN 21 /*int2*/ THEN 16
         WHEN 23 /*int4*/ THEN 32
         WHEN 20 /*int8*/ THEN 64
         WHEN 1700 /*numeric*/ THEN
              CASE WHEN $2 = -1
                   THEN null
                   ELSE (($2 - 4) >> 16) & 0xFFFF
                   END
         WHEN 700 /*float4*/ THEN 24 /*FLT_MANT_DIG*/
         WHEN 701 /*float8*/ THEN 53 /*DBL_MANT_DIG*/
         ELSE null
  END
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE range_parted2 DETACH PARTITION part_rp CONCURRENTLY
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_7a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (avg(a)) VIRTUAL)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub8 ADD TABLE test_part_c_1 (a)
;
----------
[ADMIN_TABLE] ANALYZE ONLY vacparted(a,b)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pp_nn_1 ADD CONSTRAINT nn NOT NULL a NO INHERIT
;
----------
[ALTER_TABLE|DROP_CONSTRAINT|ADD_CONSTRAINT] --
-- partitioned FK referenced deletes CASCADE
--

--
-- partitioned FK referenced updates SET NULL
--

ALTER TABLE temporal_partitioned_fk_mltrng2mltrng
  DROP CONSTRAINT temporal_partitioned_fk_mltrng2mltrng_fk,
  ADD CONSTRAINT temporal_partitioned_fk_mltrng2mltrng_fk
    FOREIGN KEY (parent_id, PERIOD valid_at)
    REFERENCES temporal_partitioned_mltrng
    ON DELETE SET NULL ON UPDATE SET NULL
;
----------
[CREATE_TABLE] CREATE TABLE ctlt1 (a text CHECK (length(a) > 2) ENFORCED PRIMARY KEY,
	b text CHECK (length(b) > 100) NOT ENFORCED)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  pg_freespace(rel regclass, blkno OUT bigint, avail OUT int2)
RETURNS SETOF RECORD
LANGUAGE SQL PARALLEL SAFE
BEGIN ATOMIC
  SELECT blkno, pg_freespace($1, blkno) AS avail
  FROM generate_series('0'::bigint, pg_relation_size($1) / current_setting('block_size'::text)::bigint - '1'::bigint) AS blkno;
END
;
----------
[ALTER_TABLE|ADD_COLUMN|ADD_CONSTRAINT] ALTER TABLE temporal3
  ADD COLUMN valid_at daterange,
  ADD CONSTRAINT temporal3_uq
  UNIQUE (id, valid_at WITHOUT OVERLAPS)
;
----------
[CREATE_TABLE] CREATE TABLE INSERT_TBL (x INT DEFAULT nextval('insert_seq'),
	y TEXT DEFAULT '-NULL-',
	z INT DEFAULT -1 * currval('insert_seq'),
	CONSTRAINT INSERT_TBL_CON CHECK (x >= 3 AND y <> 'check failed' AND x < 8),
	CHECK (x + z = 0) ENFORCED, /* no change it is a default */
	CONSTRAINT NE_INSERT_TBL_CON CHECK (x + z = 1) NOT ENFORCED)
;
----------
[ALTER_TYPE] ALTER TYPE myvarchar SET (
    send = myvarcharsend,
    receive = myvarcharrecv,
    typmod_in = varchartypmodin,
    typmod_out = varchartypmodout,
    -- these are bogus, but it's safe as long as we don't use the type:
    analyze = ts_typanalyze,
    subscript = raw_array_subscript_handler
)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_fortable SET TABLE testpub_tbl5 (a, a)
;
----------
[ALTER_PUB_SUB] -- ok - conditional expressions are allowed
ALTER PUBLICATION testpub5 SET TABLE testpub_rf_tbl5 WHERE (a IS DOCUMENT)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl4_lk3 (LIKE notnull_tbl4 INCLUDING INDEXES, NOT NULL a)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION bit_length(bit)
 RETURNS integer
 LANGUAGE sql
 IMMUTABLE PARALLEL SAFE STRICT COST 1
RETURN length($1)
;
----------
[CREATE_TABLE] -- PK with a range column/PERIOD that isn't there:

CREATE TABLE temporal_rng (
  id INTEGER,
  CONSTRAINT temporal_rng_pk PRIMARY KEY (id, valid_at WITHOUT OVERLAPS)
)
;
----------
[CREATE_PROG_OBJ] /* Given an index's OID and an underlying-table column number, return the
 * column's position in the index (NULL if not there) */
CREATE FUNCTION _pg_index_position(oid, smallint) RETURNS int
    LANGUAGE sql STRICT STABLE
BEGIN ATOMIC
SELECT (ss.a).n FROM
  (SELECT information_schema._pg_expandarray(indkey) AS a
   FROM pg_catalog.pg_index WHERE indexrelid = $1) ss
  WHERE (ss.a).x = $2;
END
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_S_xx(x anyarray) RETURNS anyelement
    LANGUAGE SQL
    RETURN x[1]
;
----------
[DROP_CATALOG] DROP DATABASE IF EXISTS split_db_probe WITH (FORCE)
;
----------
[ALTER_PUB_SUB] -- should be able to drop schema from 'FOR TABLE' publication
ALTER PUBLICATION testpub_fortable DROP TABLES IN SCHEMA pub_test
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub1_forschema DROP TABLES IN SCHEMA non_existent_schema
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub7 FOR TABLE testpub_rf_tbl6 WHERE (y > 100)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_part1_2_upg (a int, CONSTRAINT nn2 NOT NULL a, b int)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub1 for table t1(c1, c2, c3)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.skip_wal_skip_rewrite_index ADD CONSTRAINT skip_wal_skip_rewrite_index_c_not_null NOT NULL c
;
----------
[CREATE_TABLE] CREATE TABLE gtest27 (
    a int,
    b int,
    x int GENERATED ALWAYS AS ((a + b) * 2) VIRTUAL
)
;
----------
[CREATE_TABLE] --
-- Similarly, check the merging of existing constraints; a parent constraint
-- marked as NOT ENFORCED can merge with an ENFORCED child constraint, but the
-- reverse is not allowed.
--
create table p1(f1 int constraint p1_a_check check (f1 > 0) not enforced)
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW jsonb_table_view7 AS
SELECT sub.* FROM s,
	(values(23)) x(x),
	generate_series(13, 13) y,
	JSON_TABLE(js, '$' AS c1 PASSING x AS x, y AS y
	COLUMNS (
		xx1 int PATH '$.c',
		NESTED PATH '$.a.za[2]' COLUMNS (NESTED PATH '$.z22[*]' as z22 COLUMNS (c int PATH '$' WITHOUT WRAPPER OMIT QUOTES)),
		NESTED PATH '$.a.za[1]' COLUMNS (d json PATH '$ ? (@.z21[*] == ($"x" -1))' WITH WRAPPER),
		NESTED PATH '$.a.za[0]' COLUMNS (NESTED PATH '$.z1[*] ? (@ >= ($"x" -2))' as z1 COLUMNS (a int PATH '$' KEEP QUOTES)),
		NESTED PATH '$.a.za[1]' COLUMNS
			(NESTED PATH '$.z21[*] ? (@ >= ($"y" +121))' as z21 COLUMNS (b int PATH '$ ? (@ > ($"x" +111))' DEFAULT 0 ON EMPTY))
	)) sub
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub6 SET TABLE rf_tbl_abcd_pk (a)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_4a FOR TABLE tab_rowfilter_4 WHERE (c % 2 = 0)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest29" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 3))
)
;
