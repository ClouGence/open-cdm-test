CREATE TABLE "public"."rtest_emp" (
    "ename" character(20),
    "salary" numeric
)
;
CREATE TABLE image (title text, raster lo)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_subscription
;
create table idxpart (a int, b int, c text) partition by range (a, b)
;
ALTER TABLE "S 1"."T 2" SET (autovacuum_enabled = 'false')
;
create table atacc1 (test int check (test>3), test2 int)
;
-- OK when the partition key collation is same as that of the join columns
CREATE TABLE pagg_tab5 (c text collate case_insensitive, b text collate case_insensitive) PARTITION BY LIST (c collate case_insensitive)
;
CREATE TEMPORARY VIEW vsubdepartment AS
	WITH RECURSIVE subdepartment AS
	(
		 
		SELECT * FROM department WHERE name = 'A'
		UNION ALL
		
		SELECT d.* FROM department AS d, subdepartment AS sd
			WHERE d.parent_department = sd.id
	)
	SELECT * FROM subdepartment
;
DROP SCHEMA IF EXISTS split_ts_probe CASCADE
;
VACUUM (PARALLEL 2) pg_catalog.pg_event_trigger
;
--
-- Test DISCARD TEMP
--
CREATE TEMP TABLE reset_test ( data text ) ON COMMIT DELETE ROWS
;
CREATE TABLE bt_name_heap (
	seqno 		name,
	random 		int4
)
;
CREATE OR REPLACE FUNCTION pgstattuple(IN relname text,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuple_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_partitioned_table
;
-- no way
drop index concurrently idxpart_a_idx
;
ANALYZE regress_indexing.pk2
;
alter table schema1.user_table add column address varchar(255)
;
-- try updating the cursor's current row

create temp table forc_test as
  select n as i, n as j from generate_series(1,10) n
;
create temp table ta (id int primary key, val int)
;
create table rp_prefix_test2_p1 partition of rp_prefix_test2 for values from (1, 1, 0) to (1, 1, 10)
;
DROP TABLE IF EXISTS do_write
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_constraint
;
ALTER OPERATOR >= (isbn13, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
ALTER TABLE pg_toast.pg_toast_28040 RENAME TO reind_con_toast
;
CREATE TABLE parent (parent_id SERIAL NOT NULL PRIMARY KEY)
;
CREATE TABLE concur_reindex_child PARTITION OF concur_reindex_part
  FOR VALUES FROM (0) TO (10)
;
-- Multi-level partitioning at referenced end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
  PARTITION BY LIST(f1)
;
drop domain weirdfloat
;
CREATE TABLE split_builtin_trigger.redundant_updates (
    id integer PRIMARY KEY,
    payload text
)
;
CREATE AGGREGATE myaggp06a(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
ALTER TABLE ONLY "public"."plt2" ATTACH PARTITION "public"."plt2_p3" DEFAULT
;
CREATE TABLE test_type_diff2 (int_two int2, int_four int4, int_eight int8)
;
CREATE DOMAIN addr_nsp.gendomain AS int4 CONSTRAINT domconstr CHECK (value > 0)
;
-- Similar to above, but the table being attached is a partitioned table
-- whose partition has still different attnos for the root partitioning
-- columns.
CREATE TABLE part_7 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
) PARTITION BY LIST (b)
;
CREATE TRIGGER trig_stmt_after AFTER DELETE OR INSERT OR UPDATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
drop table self_ref_trigger
;
CREATE TABLE tab_rowfilter_4 (c int primary key)
;
ALTER TABLE ONLY "public"."alpha_pos" ATTACH PARTITION "public"."alpha_pos_p1" FOR VALUES IN ('0001', '0003')
;
--
-- For pkeys:
-- 	ON DELETE/UPDATE (pkey1, pkey2) CASCADE:
-- 		fkeys (fkey1, fkey2) and fkeys2 (fkey21, fkey22)
--
create trigger check_pkeys_fkey_cascade
	after delete or update on pkeys
	for each row
	execute procedure
	check_foreign_key (2, 'cascade', 'pkey1', 'pkey2',
	'fkeys', 'fkey1', 'fkey2', 'fkeys2', 'fkey21', 'fkey22')
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_publication
;
--
-- regression test for 8.2 bug with improper re-ordering of left joins
--

create temp table tt3(f1 int, f2 text)
;
-- Check that comments are preserved
CREATE TABLE testcomment (i int)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shseclabel
;
VACUUM (FULL) pg_catalog.pg_ts_parser
;
CREATE TABLE "public"."pagg_tab_para_p1" (
    "x" integer,
    "y" integer
)
WITH ("parallel_workers"='0')
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_tablespace
;
DROP TABLE trans_baz
;
/*
 * 6.7
 * TIME_STAMP domain
 */

CREATE DOMAIN time_stamp AS timestamp(2) with time zone
    DEFAULT current_timestamp(2)
;
create function continue_error1() returns void as $$
begin
    begin
        continue;
    end;
end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "public"."rtest_emp" (
    "ename" character(20),
    "salary" numeric
)
;
----------
[CREATE_TABLE] CREATE TABLE image (title text, raster lo)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] create table idxpart (a int, b int, c text) partition by range (a, b)
;
----------
[ALTER_TABLE] ALTER TABLE "S 1"."T 2" SET (autovacuum_enabled = 'false')
;
----------
[CREATE_TABLE] create table atacc1 (test int check (test>3), test2 int)
;
----------
[CREATE_TABLE] -- OK when the partition key collation is same as that of the join columns
CREATE TABLE pagg_tab5 (c text collate case_insensitive, b text collate case_insensitive) PARTITION BY LIST (c collate case_insensitive)
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMPORARY VIEW vsubdepartment AS
	WITH RECURSIVE subdepartment AS
	(
		 
		SELECT * FROM department WHERE name = 'A'
		UNION ALL
		
		SELECT d.* FROM department AS d, subdepartment AS sd
			WHERE d.parent_department = sd.id
	)
	SELECT * FROM subdepartment
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_ts_probe CASCADE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_event_trigger
;
----------
[CREATE_TABLE] --
-- Test DISCARD TEMP
--
CREATE TEMP TABLE reset_test ( data text ) ON COMMIT DELETE ROWS
;
----------
[CREATE_TABLE] CREATE TABLE bt_name_heap (
	seqno 		name,
	random 		int4
)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pgstattuple(IN relname text,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuple_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_partitioned_table
;
----------
[DROP_INDEX] -- no way
drop index concurrently idxpart_a_idx
;
----------
[ADMIN_TABLE] ANALYZE regress_indexing.pk2
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table schema1.user_table add column address varchar(255)
;
----------
[CREATE_TABLE|SELECT] -- try updating the cursor's current row

create temp table forc_test as
  select n as i, n as j from generate_series(1,10) n
;
----------
[CREATE_TABLE] create temp table ta (id int primary key, val int)
;
----------
[CREATE_TABLE] create table rp_prefix_test2_p1 partition of rp_prefix_test2 for values from (1, 1, 0) to (1, 1, 10)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS do_write
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_constraint
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (isbn13, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28040 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE parent (parent_id SERIAL NOT NULL PRIMARY KEY)
;
----------
[CREATE_TABLE] CREATE TABLE concur_reindex_child PARTITION OF concur_reindex_part
  FOR VALUES FROM (0) TO (10)
;
----------
[CREATE_TABLE] -- Multi-level partitioning at referenced end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
  PARTITION BY LIST(f1)
;
----------
[DROP_TYPE] drop domain weirdfloat
;
----------
[CREATE_TABLE] CREATE TABLE split_builtin_trigger.redundant_updates (
    id integer PRIMARY KEY,
    payload text
)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp06a(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."plt2" ATTACH PARTITION "public"."plt2_p3" DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE test_type_diff2 (int_two int2, int_four int4, int_eight int8)
;
----------
[CREATE_TYPE] CREATE DOMAIN addr_nsp.gendomain AS int4 CONSTRAINT domconstr CHECK (value > 0)
;
----------
[CREATE_TABLE] -- Similar to above, but the table being attached is a partitioned table
-- whose partition has still different attnos for the root partitioning
-- columns.
CREATE TABLE part_7 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
) PARTITION BY LIST (b)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_stmt_after AFTER DELETE OR INSERT OR UPDATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
----------
[DROP_TABLE] drop table self_ref_trigger
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_4 (c int primary key)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."alpha_pos" ATTACH PARTITION "public"."alpha_pos_p1" FOR VALUES IN ('0001', '0003')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- For pkeys:
-- 	ON DELETE/UPDATE (pkey1, pkey2) CASCADE:
-- 		fkeys (fkey1, fkey2) and fkeys2 (fkey21, fkey22)
--
create trigger check_pkeys_fkey_cascade
	after delete or update on pkeys
	for each row
	execute procedure
	check_foreign_key (2, 'cascade', 'pkey1', 'pkey2',
	'fkeys', 'fkey1', 'fkey2', 'fkeys2', 'fkey21', 'fkey22')
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_publication
;
----------
[CREATE_TABLE] --
-- regression test for 8.2 bug with improper re-ordering of left joins
--

create temp table tt3(f1 int, f2 text)
;
----------
[CREATE_TABLE] -- Check that comments are preserved
CREATE TABLE testcomment (i int)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shseclabel
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_ts_parser
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_para_p1" (
    "x" integer,
    "y" integer
)
WITH ("parallel_workers"='0')
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_tablespace
;
----------
[DROP_TABLE] DROP TABLE trans_baz
;
----------
[CREATE_TYPE] /*
 * 6.7
 * TIME_STAMP domain
 */

CREATE DOMAIN time_stamp AS timestamp(2) with time zone
    DEFAULT current_timestamp(2)
;
----------
[CREATE_PROG_OBJ] create function continue_error1() returns void as $$
begin
    begin
        continue;
    end;
end;
$$ language plpgsql
;
