CREATE TEMP TABLE pg_temp_14.pg_temp_26781_2 (tid pg_catalog.tid)
;
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest2, ptest1))
;
-- Clean up
DROP TABLE psql_pipeline
;
-- include 10us sleep to ensure that runtime measures as more than zero
    CREATE FUNCTION test_stat_func() RETURNS VOID LANGUAGE plpgsql AS
      $$BEGIN PERFORM pg_sleep(10e-6); END;$$
;
-- also try noinherit, local, and local+inherited cases
ALTER TABLE test_inh_check ADD CONSTRAINT bnoinherit CHECK (b > 100) NO INHERIT
;
CREATE INDEX ON test3bpci (x bpchar_pattern_ops)
;
CREATE PUBLICATION tap_pub_B FOR TABLE tab_main WITH (publish_via_partition_root)
;
ALTER FUNCTION gbt_text_picksplit(internal, internal) PARALLEL SAFE
;
CREATE AGGREGATE "public"."sumdouble"(double precision) (
    SFUNC = "float8pl",
    STYPE = double precision,
    MSFUNC = "float8pl",
    MINVFUNC = "float8mi",
    MSTYPE = double precision
)
;
CREATE SCHEMA "fkpart4"
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "uint" WITH "simple"
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "asciiword" WITH "public"."ispell", "english_stem"
;
CREATE SEQUENCE "public"."sj_t4_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
create index ��ٸ����index2 on ��ٸ���� using hash (��Ƴ���)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub9
;
--
-- Operator functions:
--
---------------------------------------------------
-- EAN13:
CREATE FUNCTION isnlt(ean13, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION exist(hstore,text)
RETURNS bool
AS '$libdir/hstore','hstore_exists'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE pg_description ADD PRIMARY KEY USING INDEX pg_description_o_c_o_index
;
-- ok

ALTER PUBLICATION testpub2 ADD TABLE testpub_tbl1
;
ANALYZE pg_temp_3.pg_temp_27379
;
--    P    P        P    N
-- should ERROR: tf1p(anyarray, anyelement) not matched by tf1p(anyarray, int)
CREATE AGGREGATE myaggn19a(BASETYPE = anyelement, SFUNC = tf1p,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE VIEW pg_buffercache_numa AS
	SELECT P.* FROM pg_buffercache_numa_pages() AS P
	(bufferid integer, os_page_num bigint, numa_node integer)
;
--
-- postquel functions
--

CREATE FUNCTION hobbies(person)
   RETURNS setof hobbies_r
   AS 'select * from hobbies_r where person = $1.name'
   LANGUAGE SQL
;
-- PK with a custom range type:
CREATE TYPE textrange2 AS range (subtype=text, collation="C")
;
-- Make sure the partition columns are mapped correctly
create table idxpart (a int, b int, c text) partition by range (a)
;
ALTER TABLE pg_opfamily ADD UNIQUE USING INDEX pg_opfamily_am_name_nsp_index
;
CALL ptest5(10, 'Hello')
;
ANALYZE pg_temp_13.pg_temp_25047
;
CREATE TEMP TABLE pg_temp_18.pg_temp_27287_2 (tid pg_catalog.tid)
;
analyze sj
;
-- try adding an initially deferred constraint
ALTER TABLE unique_tbl DROP CONSTRAINT unique_tbl_i_key
;
-- min() and max() for xid8
create table xid8_t1 (x xid8)
;
COMMENT ON FUNCTION seg_left(seg, seg) IS
'is left of'
;
-- must be first write in xact
    DROP TABLE IF EXISTS dummy
;
CREATE TABLE "fkpart3"."pk3" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
CREATE FUNCTION merge_func (p_id integer, p_bal integer)
RETURNS INTEGER
LANGUAGE plpgsql
AS $$
DECLARE
 result integer;
BEGIN
MERGE INTO target t
USING (SELECT p_id AS sid) AS s
ON t.tid = s.sid
WHEN MATCHED THEN
	UPDATE SET balance = t.balance - p_bal;
IF FOUND THEN
	GET DIAGNOSTICS result := ROW_COUNT;
END IF;
RETURN result;
END;
$$
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub_mix_7, pub_mix_8
;
-- cleanup
drop table test_partitioned
;
-- fail

drop function rngfuncbar()
;
create table inserttest (f1 int, f2 int[],
                         f3 insert_test_type, f4 insert_test_type[])
;
CREATE AGGREGATE myaggn02b(*) (SFUNC = stfnp, STYPE = anyarray,
  INITCOND = '{}')
;
-- test language tags
CREATE COLLATION lt_insensitive (provider = icu, locale = 'en-u-ks-level1', deterministic = false)
;
ANALYZE public.prt1_p1
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_replication_origin
;
-- Test a not-null addition that must walk down the hierarchy
CREATE TABLE inh_parent ()
;
create index test_index on test_partition (a)
;
CREATE TABLE src (a int, b text)
;
CREATE TABLE btree_part (id int4) PARTITION BY RANGE (id)
;
CREATE TABLE "public"."rtest_empmass" (
    "ename" character(20),
    "salary" "money"
)
;
CREATE FUNCTION gbt_intv_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_14.pg_temp_26781_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest2, ptest1))
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE psql_pipeline
;
----------
[CREATE_PROG_OBJ] -- include 10us sleep to ensure that runtime measures as more than zero
    CREATE FUNCTION test_stat_func() RETURNS VOID LANGUAGE plpgsql AS
      $$BEGIN PERFORM pg_sleep(10e-6); END;$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- also try noinherit, local, and local+inherited cases
ALTER TABLE test_inh_check ADD CONSTRAINT bnoinherit CHECK (b > 100) NO INHERIT
;
----------
[ADD_INDEX] CREATE INDEX ON test3bpci (x bpchar_pattern_ops)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_B FOR TABLE tab_main WITH (publish_via_partition_root)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_text_picksplit(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."sumdouble"(double precision) (
    SFUNC = "float8pl",
    STYPE = double precision,
    MSFUNC = "float8pl",
    MINVFUNC = "float8mi",
    MSTYPE = double precision
)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "fkpart4"
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "uint" WITH "simple"
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "asciiword" WITH "public"."ispell", "english_stem"
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sj_t4_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ADD_INDEX] create index ��ٸ����index2 on ��ٸ���� using hash (��Ƴ���)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub9
;
----------
[CREATE_PROG_OBJ] --
-- Operator functions:
--
---------------------------------------------------
-- EAN13:
CREATE FUNCTION isnlt(ean13, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION exist(hstore,text)
RETURNS bool
AS '$libdir/hstore','hstore_exists'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_description ADD PRIMARY KEY USING INDEX pg_description_o_c_o_index
;
----------
[ALTER_PUB_SUB] -- ok

ALTER PUBLICATION testpub2 ADD TABLE testpub_tbl1
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27379
;
----------
[CREATE_PROG_OBJ] --    P    P        P    N
-- should ERROR: tf1p(anyarray, anyelement) not matched by tf1p(anyarray, int)
CREATE AGGREGATE myaggn19a(BASETYPE = anyelement, SFUNC = tf1p,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_buffercache_numa AS
	SELECT P.* FROM pg_buffercache_numa_pages() AS P
	(bufferid integer, os_page_num bigint, numa_node integer)
;
----------
[CREATE_PROG_OBJ] --
-- postquel functions
--

CREATE FUNCTION hobbies(person)
   RETURNS setof hobbies_r
   AS 'select * from hobbies_r where person = $1.name'
   LANGUAGE SQL
;
----------
[CREATE_POLICY] -- PK with a custom range type:
CREATE TYPE textrange2 AS range (subtype=text, collation="C")
;
----------
[CREATE_TABLE] -- Make sure the partition columns are mapped correctly
create table idxpart (a int, b int, c text) partition by range (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_opfamily ADD UNIQUE USING INDEX pg_opfamily_am_name_nsp_index
;
----------
[CALL_PROG_OBJ] CALL ptest5(10, 'Hello')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25047
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_18.pg_temp_27287_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] analyze sj
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- try adding an initially deferred constraint
ALTER TABLE unique_tbl DROP CONSTRAINT unique_tbl_i_key
;
----------
[CREATE_TABLE] -- min() and max() for xid8
create table xid8_t1 (x xid8)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_left(seg, seg) IS
'is left of'
;
----------
[DROP_TABLE] -- must be first write in xact
    DROP TABLE IF EXISTS dummy
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk3" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION merge_func (p_id integer, p_bal integer)
RETURNS INTEGER
LANGUAGE plpgsql
AS $$
DECLARE
 result integer;
BEGIN
MERGE INTO target t
USING (SELECT p_id AS sid) AS s
ON t.tid = s.sid
WHEN MATCHED THEN
	UPDATE SET balance = t.balance - p_bal;
IF FOUND THEN
	GET DIAGNOSTICS result := ROW_COUNT;
END IF;
RETURN result;
END;
$$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub_mix_7, pub_mix_8
;
----------
[DROP_TABLE] -- cleanup
drop table test_partitioned
;
----------
[DROP_PROG_OBJ] -- fail

drop function rngfuncbar()
;
----------
[CREATE_TABLE] create table inserttest (f1 int, f2 int[],
                         f3 insert_test_type, f4 insert_test_type[])
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn02b(*) (SFUNC = stfnp, STYPE = anyarray,
  INITCOND = '{}')
;
----------
[CREATE_POLICY] -- test language tags
CREATE COLLATION lt_insensitive (provider = icu, locale = 'en-u-ks-level1', deterministic = false)
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_p1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE] -- Test a not-null addition that must walk down the hierarchy
CREATE TABLE inh_parent ()
;
----------
[ADD_INDEX] create index test_index on test_partition (a)
;
----------
[CREATE_TABLE] CREATE TABLE src (a int, b text)
;
----------
[CREATE_TABLE] CREATE TABLE btree_part (id int4) PARTITION BY RANGE (id)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_empmass" (
    "ename" character(20),
    "salary" "money"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_intv_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
