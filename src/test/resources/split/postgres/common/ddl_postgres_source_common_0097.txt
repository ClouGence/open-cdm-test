create table trans_abc (a int)
;
CREATE TABLE "public"."prt2_m_p3" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
DROP TABLE fail_def_part
;
/*
 * text search configuration for greek language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * greek and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY greek_stem
	(TEMPLATE = snowball, Language = greek )
;
DROP TABLE ctv_data
;
CREATE FUNCTION _pg_truetypid(pg_attribute, pg_type) RETURNS oid
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT CASE WHEN $2.typtype = 'd' THEN $2.typbasetype ELSE $1.atttypid END$$
;
CREATE FUNCTION gbt_float8_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c5 SERIAL CHECK (c5 > 10)
;
CALL test_proc10(b => _b, a => _a)
;
CREATE TABLE itest7c (a int GENERATED ALWAYS AS IDENTITY) INHERITS (itest7b)
;
-- A constraint that's NO INHERIT can be dropped without damaging children
alter table inh_nn1 drop constraint inh_nn1_f1_not_null
;
CLUSTER abbrev_abort_uuids USING abbrev_abort_uuids__noabort_decreasing_idx
;
ANALYZE public.mlparted_defd
;
DROP TABLE test_jsonb_mutability
;
CREATE FUNCTION text_w_default_in(cstring)
   RETURNS text_w_default
   AS 'textin'
   LANGUAGE internal STABLE STRICT
;
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, create_slot = true)
;
ALTER TABLE plt1 DETACH PARTITION plt1_p3
;
VACUUM delete_test_table
;
ALTER EXTENSION test_pg_dump DROP CAST (text AS casttesttype)
;
CREATE FUNCTION gin_extract_value_text(text, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
create index j1_id1_idx on j1 (id1) where id1 % 1000 = 1
;
drop table attest
;
CREATE SUBSCRIPTION regress_mysub2 CONNECTION 'port=15035 host=/tmp/2MdPFByj7i dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub2_slot)
;
DROP PROCEDURE test_proc1
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_default_acl
;
ALTER TABLE pg_replication_origin ADD PRIMARY KEY USING INDEX pg_replication_origin_roiident_index
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_description
;
ALTER TABLE dump_test.test_table ALTER COLUMN col3 SET STORAGE MAIN
;
create domain insert_test_domain as insert_test_type
  check ((value).if1 is not null and (value).if2 is not null)
;
ANALYZE public.spgist_box_tbl
;
-- Test truncation of long foreign key names
create table pktable1 (a int primary key)
;
/*
 * 5.57
 * SQL_FEATURES table
 */

CREATE TABLE sql_features (
    feature_id          character_data,
    feature_name        character_data,
    sub_feature_id      character_data,
    sub_feature_name    character_data,
    is_supported        yes_or_no,
    is_verified_by      character_data,
    comments            character_data
)
;
CREATE AGGREGATE "public"."myaggp01a"(*) (
    SFUNC = "public"."stfnp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
CREATE TABLE "public"."pht1_e_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
ALTER OPERATOR FAMILY gist_cidr_ops USING gist ADD
	OPERATOR	6	<> (inet, inet)
;
ALTER TABLE concur_exprs_tab ALTER c2 TYPE TEXT
;
-- fails because of NO SCROLL specification

create or replace function sc_test() returns setof integer as $$
declare
  c refcursor;
  x integer;
begin
  open c scroll for select f1 from int4_tbl;
  fetch last from c into x;
  while found loop
    return next x;
    fetch prior from c into x;
  end loop;
  close c;
end;
$$ language plpgsql
;
create table parted_conflict (a int primary key, b text) partition by range (a)
;
TRUNCATE temporal_mltrng
;
/*
 * text search configuration for irish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * irish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
CREATE PUBLICATION testpub_forsystemtbl FOR TABLE pg_publication
;
ALTER TABLE gtest25 ADD COLUMN b int GENERATED ALWAYS AS (a * 3) STORED
;
CREATE VIEW nontemp4 AS SELECT * FROM t1 LEFT JOIN t2 ON t1.num = t2.num2 AND t2.value = 'xxx'
;
CREATE TABLE "public"."timetz_tbl" (
    "f1" time(2) with time zone
)
;
CREATE TEMP TABLE temptest(col int) ON COMMIT DROP
;
CREATE TEXT SEARCH DICTIONARY hunspell_err (
						Template=ispell,
						DictFile=ispell_sample,
						AffFile=hunspell_sample_num
)
;
CREATE TABLE tab2 (a int, gen1 int)
;
COMMENT ON TEXT SEARCH CONFIGURATION arabic IS 'configuration for arabic language'
;
TRUNCATE public.loc1 CONTINUE IDENTITY RESTRICT
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table trans_abc (a int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_m_p3" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[DROP_TABLE] DROP TABLE fail_def_part
;
----------
[CREATE_POLICY] /*
 * text search configuration for greek language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * greek and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY greek_stem
	(TEMPLATE = snowball, Language = greek )
;
----------
[DROP_TABLE] DROP TABLE ctv_data
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _pg_truetypid(pg_attribute, pg_type) RETURNS oid
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT CASE WHEN $2.typtype = 'd' THEN $2.typbasetype ELSE $1.atttypid END$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float8_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c5 SERIAL CHECK (c5 > 10)
;
----------
[CALL_PROG_OBJ] CALL test_proc10(b => _b, a => _a)
;
----------
[CREATE_TABLE] CREATE TABLE itest7c (a int GENERATED ALWAYS AS IDENTITY) INHERITS (itest7b)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- A constraint that's NO INHERIT can be dropped without damaging children
alter table inh_nn1 drop constraint inh_nn1_f1_not_null
;
----------
[ADMIN_TABLE] CLUSTER abbrev_abort_uuids USING abbrev_abort_uuids__noabort_decreasing_idx
;
----------
[ADMIN_TABLE] ANALYZE public.mlparted_defd
;
----------
[DROP_TABLE] DROP TABLE test_jsonb_mutability
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION text_w_default_in(cstring)
   RETURNS text_w_default
   AS 'textin'
   LANGUAGE internal STABLE STRICT
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, create_slot = true)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt1 DETACH PARTITION plt1_p3
;
----------
[ADMIN_TABLE] VACUUM delete_test_table
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP CAST (text AS casttesttype)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_text(text, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADD_INDEX] create index j1_id1_idx on j1 (id1) where id1 % 1000 = 1
;
----------
[DROP_TABLE] drop table attest
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_mysub2 CONNECTION 'port=15035 host=/tmp/2MdPFByj7i dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub2_slot)
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE test_proc1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_default_acl
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_replication_origin ADD PRIMARY KEY USING INDEX pg_replication_origin_roiident_index
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_description
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE dump_test.test_table ALTER COLUMN col3 SET STORAGE MAIN
;
----------
[CREATE_TYPE] create domain insert_test_domain as insert_test_type
  check ((value).if1 is not null and (value).if2 is not null)
;
----------
[ADMIN_TABLE] ANALYZE public.spgist_box_tbl
;
----------
[CREATE_TABLE] -- Test truncation of long foreign key names
create table pktable1 (a int primary key)
;
----------
[CREATE_TABLE] /*
 * 5.57
 * SQL_FEATURES table
 */

CREATE TABLE sql_features (
    feature_id          character_data,
    feature_name        character_data,
    sub_feature_id      character_data,
    sub_feature_name    character_data,
    is_supported        yes_or_no,
    is_verified_by      character_data,
    comments            character_data
)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp01a"(*) (
    SFUNC = "public"."stfnp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht1_e_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_cidr_ops USING gist ADD
	OPERATOR	6	<> (inet, inet)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE concur_exprs_tab ALTER c2 TYPE TEXT
;
----------
[CREATE_PROG_OBJ] -- fails because of NO SCROLL specification

create or replace function sc_test() returns setof integer as $$
declare
  c refcursor;
  x integer;
begin
  open c scroll for select f1 from int4_tbl;
  fetch last from c into x;
  while found loop
    return next x;
    fetch prior from c into x;
  end loop;
  close c;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] create table parted_conflict (a int primary key, b text) partition by range (a)
;
----------
[TRUNCATE_TABLE] TRUNCATE temporal_mltrng
;
----------
[CREATE_POLICY] /*
 * text search configuration for irish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * irish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_forsystemtbl FOR TABLE pg_publication
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest25 ADD COLUMN b int GENERATED ALWAYS AS (a * 3) STORED
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW nontemp4 AS SELECT * FROM t1 LEFT JOIN t2 ON t1.num = t2.num2 AND t2.value = 'xxx'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."timetz_tbl" (
    "f1" time(2) with time zone
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest(col int) ON COMMIT DROP
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hunspell_err (
						Template=ispell,
						DictFile=ispell_sample,
						AffFile=hunspell_sample_num
)
;
----------
[CREATE_TABLE] CREATE TABLE tab2 (a int, gen1 int)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION arabic IS 'configuration for arabic language'
;
----------
[TRUNCATE_TABLE] TRUNCATE public.loc1 CONTINUE IDENTITY RESTRICT
;
