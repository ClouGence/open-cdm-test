VACUUM (SKIP_LOCKED, FULL) part1, part2
;
ALTER EXTENSION test_pg_dump DROP SCHEMA test_pg_dump_s1
;
ALTER TABLE itest3 ALTER COLUMN a TYPE text
;
alter type insenum add value 'i7' before 'L2'
;
CREATE SUBSCRIPTION sub CONNECTION 'port=15704 host=/tmp/6n6yQarUSt dbname=postgres' PUBLICATION pub
;
CREATE FUNCTION gin_compare_prefix_time(time, time, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
CREATE OR REPLACE VIEW mysecview8 WITH (security_invoker=true)
       AS SELECT * FROM tbl1 WHERE a < 256
;
ANALYZE pg_temp_2.pg_temp_28105_2
;
CREATE MATERIALIZED VIEW mv_test2 AS SELECT moo, 2*moo FROM mvtest_vt2 UNION ALL SELECT moo, 3*moo FROM mvtest_vt2
;
create or replace function raise_test() returns void as $$
begin
  raise using message = 'custom' || ' message', errcode = '22012';
end;
$$ language plpgsql
;
CREATE VIEW ro_view7 AS WITH t AS (SELECT a, b FROM base_tbl) SELECT * FROM t
;
create type textrange as range (subtype = text, collation = "C")
;
DROP LANGUAGE alt_lang3 CASCADE
;
CREATE TABLE pgss_do_table (id INT)
;
DROP TABLE pagg_tab4
;
TRUNCATE TIMESTAMPTZ_TBL
;
CREATE MATERIALIZED VIEW split_view_probe.base_mv (id, qty) USING heap WITH (fillfactor = 100) AS SELECT id, qty FROM split_view_probe.base_table WITH NO DATA
;
DROP TABLE pg_temp_2.pg_temp_27071_2, pg_temp_2.pg_temp_27071
;
CREATE TABLE rls_child_tbl () INHERITS (rls_tbl)
;
CREATE TEMPORARY TABLE pgss_ctas_3 AS SELECT 1
;
ALTER TABLE pg_toast.pg_toast_30777 RENAME TO reind_con_toast
;
-- populate_record
CREATE TYPE jbpop AS (a text, b int, c timestamp)
;
create temp table twophase_tab (a int)
;
CREATE FUNCTION pg_catalog.pg_file_length(text)
RETURNS bigint
AS 'SELECT size FROM pg_catalog.pg_stat_file($1)'
LANGUAGE SQL VOLATILE STRICT
;
CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int, out f3 text)
RETURNS record
AS 'select $1+1' LANGUAGE sql
;
do $$ declare a int[];
begin a[1:2] := 4; raise notice 'a = %', a; end$$
;
CREATE TRIGGER trig_row_after_update
AFTER UPDATE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
alter table alterlock2
add constraint alterlock2nv foreign key (f1) references alterlock (f1) NOT VALID
;
ALTER TABLE tab1 ATTACH PARTITION tab1_1 FOR VALUES IN (1, 2, 3)
;
create index on tbl_ra (b)
;
-- fail
drop table trigpart2
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16531 host=/tmp/wfVN1kltEQ dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_forall
;
ALTER TABLE pg_toast.pg_toast_30675 RENAME TO reind_con_toast
;
/* contrib/pg_prewarm/pg_prewarm--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Register the function.
CREATE FUNCTION pg_prewarm(regclass,
						   mode text default 'buffer',
						   fork text default 'main',
						   first_block int8 default null,
						   last_block int8 default null)
RETURNS int8
AS '$libdir/pg_prewarm', 'pg_prewarm'
LANGUAGE C PARALLEL SAFE
;
CREATE TABLE notnull_tbl2 (a INTEGER CONSTRAINT blah NOT NULL, b INTEGER CONSTRAINT blah NOT NULL)
;
CREATE TEMP TABLE b (id int PRIMARY KEY, c_id int)
;
COMMENT ON FUNCTION _int_overlap(_int4, _int4) IS 'overlaps'
;
ALTER SEQUENCE ss1 OWNED BY deptest2.f1
;
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
--
-- test planner's ability to mark joins as unique
--

create table j1 (id int primary key)
;
create function dfunc(anyelement = 'World'::text) returns text as $$
  select 'Hello, ' || $1::text;
$$ language sql
;
alter table covidxpart3 drop c
;
CREATE OPERATOR #>=# (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_ge,
       COMMUTATOR = #<=#,
       NEGATOR = #<#,
       RESTRICT = scalargtsel,
       JOIN = scalargtjoinsel
)
;
ALTER INDEX dummy_test_idx RESET (option_bool)
;
CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((f3 * 3))
;
-- fail, not our index
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_othertable_pkey
;
ANALYZE pg_temp_13.pg_temp_26366_2
;
-- sequence data types
CREATE SEQUENCE sequence_test5 AS integer
;
CREATE TABLE noskip (id serial PRIMARY KEY)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED, FULL) part1, part2
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP SCHEMA test_pg_dump_s1
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest3 ALTER COLUMN a TYPE text
;
----------
[ALTER_TYPE] alter type insenum add value 'i7' before 'L2'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub CONNECTION 'port=15704 host=/tmp/6n6yQarUSt dbname=postgres' PUBLICATION pub
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_time(time, time, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW mysecview8 WITH (security_invoker=true)
       AS SELECT * FROM tbl1 WHERE a < 256
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_28105_2
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mv_test2 AS SELECT moo, 2*moo FROM mvtest_vt2 UNION ALL SELECT moo, 3*moo FROM mvtest_vt2
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise using message = 'custom' || ' message', errcode = '22012';
end;
$$ language plpgsql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view7 AS WITH t AS (SELECT a, b FROM base_tbl) SELECT * FROM t
;
----------
[CREATE_POLICY] create type textrange as range (subtype = text, collation = "C")
;
----------
[DROP_LANGUAGE] DROP LANGUAGE alt_lang3 CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE pgss_do_table (id INT)
;
----------
[DROP_TABLE] DROP TABLE pagg_tab4
;
----------
[TRUNCATE_TABLE] TRUNCATE TIMESTAMPTZ_TBL
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW split_view_probe.base_mv (id, qty) USING heap WITH (fillfactor = 100) AS SELECT id, qty FROM split_view_probe.base_table WITH NO DATA
;
----------
[DROP_TABLE] DROP TABLE pg_temp_2.pg_temp_27071_2, pg_temp_2.pg_temp_27071
;
----------
[CREATE_TABLE] CREATE TABLE rls_child_tbl () INHERITS (rls_tbl)
;
----------
[CREATE_TABLE|SELECT] CREATE TEMPORARY TABLE pgss_ctas_3 AS SELECT 1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30777 RENAME TO reind_con_toast
;
----------
[CREATE_TYPE] -- populate_record
CREATE TYPE jbpop AS (a text, b int, c timestamp)
;
----------
[CREATE_TABLE] create temp table twophase_tab (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_catalog.pg_file_length(text)
RETURNS bigint
AS 'SELECT size FROM pg_catalog.pg_stat_file($1)'
LANGUAGE SQL VOLATILE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int, out f3 text)
RETURNS record
AS 'select $1+1' LANGUAGE sql
;
----------
[BLOCK] do $$ declare a int[];
begin a[1:2] := 4; raise notice 'a = %', a; end$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_after_update
AFTER UPDATE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table alterlock2
add constraint alterlock2nv foreign key (f1) references alterlock (f1) NOT VALID
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab1 ATTACH PARTITION tab1_1 FOR VALUES IN (1, 2, 3)
;
----------
[ADD_INDEX] create index on tbl_ra (b)
;
----------
[DROP_TABLE] -- fail
drop table trigpart2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16531 host=/tmp/wfVN1kltEQ dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_forall
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30675 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_prewarm/pg_prewarm--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Register the function.
CREATE FUNCTION pg_prewarm(regclass,
						   mode text default 'buffer',
						   fork text default 'main',
						   first_block int8 default null,
						   last_block int8 default null)
RETURNS int8
AS '$libdir/pg_prewarm', 'pg_prewarm'
LANGUAGE C PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl2 (a INTEGER CONSTRAINT blah NOT NULL, b INTEGER CONSTRAINT blah NOT NULL)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE b (id int PRIMARY KEY, c_id int)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION _int_overlap(_int4, _int4) IS 'overlaps'
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE ss1 OWNED BY deptest2.f1
;
----------
[CREATE_TABLE] CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
----------
[CREATE_TABLE] --
-- test planner's ability to mark joins as unique
--

create table j1 (id int primary key)
;
----------
[CREATE_PROG_OBJ] create function dfunc(anyelement = 'World'::text) returns text as $$
  select 'Hello, ' || $1::text;
$$ language sql
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table covidxpart3 drop c
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #>=# (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_ge,
       COMMUTATOR = #<=#,
       NEGATOR = #<#,
       RESTRICT = scalargtsel,
       JOIN = scalargtjoinsel
)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx RESET (option_bool)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((f3 * 3))
;
----------
[ALTER_TABLE] -- fail, not our index
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_othertable_pkey
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26366_2
;
----------
[CREATE_SEQUENCE] -- sequence data types
CREATE SEQUENCE sequence_test5 AS integer
;
----------
[CREATE_TABLE] CREATE TABLE noskip (id serial PRIMARY KEY)
;
