ALTER PUBLICATION split_pub_cols SET (publish = 'insert, update, delete, truncate')
;
drop domain insert_test_domain
;
--
-- pgp armor
--
CREATE FUNCTION armor(bytea)
RETURNS text
AS '$libdir/pgcrypto', 'pg_armor'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create table revalidate_bug (c float8 unique)
;
-- fail, dup name
ALTER TABLE tt9 ADD UNIQUE(c)
;
CREATE SCHEMA "testxmlschema"
;
alter table only dropColumn drop column e
;
drop table oversized_column_default
;
CREATE EVENT TRIGGER end_rls_command ON ddl_command_end
    WHEN TAG IN ('CREATE POLICY', 'ALTER POLICY', 'DROP POLICY') EXECUTE PROCEDURE end_command()
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
DROP FOREIGN TABLE gft1
;
-- Create table with NOT NULL INVALID constraint, for pg_upgrade.
CREATE TABLE notnull_tbl1_upg (a int, b int)
;
/* helper function to create cursors for each page in [p_start, p_end] */
CREATE FUNCTION test_temp_pin(p_start int, p_end int)
RETURNS void
LANGUAGE plpgsql
AS $f$
  DECLARE
      cursorname text;
      query text;
  BEGIN
    FOR i IN p_start..p_end LOOP
       cursorname = 'c_'||i;
       query = format($q$DECLARE %I CURSOR FOR SELECT ctid FROM test_temp WHERE ctid >= '( %s, 1)'::tid $q$, cursorname, i);
       EXECUTE query;
       EXECUTE 'FETCH NEXT FROM '||cursorname;
       -- for test development
       -- RAISE NOTICE '%: %', cursorname, query;
    END LOOP;
  END;
$f$
;
DROP SCHEMA schema_rf_x
;
create trigger parted_trig_after_row after insert or update or delete on parted_trig
   for each row execute procedure trigger_notice()
;
CREATE TABLE "fkpart3"."pk51" (
    "a" integer CONSTRAINT "pk5_a_not_null" NOT NULL
)
;
CREATE FUNCTION gbt_numeric_union(internal, internal)
RETURNS gbtreekey_var
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- LOCALE conflicts with LC_COLLATE
CREATE COLLATION coll_dup_chk (LC_COLLATE = "POSIX", LOCALE = '')
;
ALTER TABLE pg_toast.pg_toast_30637 RENAME TO reind_con_toast
;
DROP VIEW json_array_subquery_view
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest_tableoid"
    ADD CONSTRAINT "gtest_tableoid_pkey" PRIMARY KEY ("a")
;
ALTER TABLESPACE split_ts_space SET (effective_io_concurrency = 2, maintenance_io_concurrency = 1)
;
-- test constraint error reporting in presence of dropped columns
create table atacc1 (id serial primary key, value int check (value < 10))
;
CREATE POLICY policy_stats ON tab_policy_stats USING (a = 5) WITH CHECK (b < 5)
;
create temp table rngfunc(f1 int8, f2 int8)
;
CREATE INDEX ON regress_pg_dump_schema.extdependtab (col2)
;
-- timestamptz check

CREATE TABLE timestamptztmp (a timestamptz)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=17495 host=/tmp/HOMNU2DPPZ dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
TRUNCATE guid1
;
/*
 * text search configuration for italian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * italian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY italian_stem
	(TEMPLATE = snowball, Language = italian , StopWords=italian)
;
create or replace function rttest()
returns setof int as $$
declare rc int;
  rca int[];
begin
  return query values(10),(20);
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query select * from (values(10),(20)) f(a) where false;
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query execute 'values(10),(20)';
  -- just for fun, let's use array elements as targets
  get diagnostics rca[1] = row_count;
  raise notice '% %', found, rca[1];
  return query execute 'select * from (values(10),(20)) f(a) where false';
  get diagnostics rca[2] = row_count;
  raise notice '% %', found, rca[2];
end;
$$ language plpgsql
;
create table like_op_noprune (a text) partition by list (a)
;
ANALYZE public.random_tbl
;
-- check that default_tablespace affects index additions in ALTER TABLE
CREATE TABLE testschema.test_tab(id int) TABLESPACE regress_tblspace
;
ALTER OPERATOR >= (issn13, issn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
alter table child inherit parent
;
CREATE INDEX test1x ON test1 (a)
;
CREATE TABLE quad_box_tbl (b box)
;
create temp table nt3 (
  id int primary key,
  nt2_id int,
  c1 boolean,
  foreign key (nt2_id) references nt2(id)
)
;
create table part3 partition of range_parted for values from ('b', 1) to ('b', 10)
;
create type jsb_ia2 as (a int[][])
;
CREATE TABLE testschema.dflt (a int PRIMARY KEY USING INDEX TABLESPACE regress_tblspace) PARTITION BY LIST (a)
;
drop table listp
;
DROP OPERATOR IF EXISTS + (no_such_type, no_such_type)
;
ANALYZE local_tbl
;
ALTER OPERATOR <= (ismn, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19143 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
CREATE FUNCTION _pg_datetime_precision(typid oid, typmod int4) RETURNS integer
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT
  CASE WHEN $1 IN (1082) /* date */
           THEN 0
       WHEN $1 IN (1083, 1114, 1184, 1266) /* time, timestamp, same + tz */
           THEN CASE WHEN $2 < 0 THEN 6 ELSE $2 END
       WHEN $1 IN (1186) /* interval */
           THEN CASE WHEN $2 < 0 OR $2 & 65535 = 65535 THEN 6 ELSE $2 & 65535 END
       ELSE null
  END$$
;
create index idxpart1_2_idx on idxpart1 ((b + a)) where a > 1
;
-- Check pruning for a partition tree containing only temporary relations
create temp table pp_temp_parent (a int) partition by list (a)
;
------------------------------------------------------------------------------------------
[ALTER_PUB_SUB] ALTER PUBLICATION split_pub_cols SET (publish = 'insert, update, delete, truncate')
;
----------
[DROP_TYPE] drop domain insert_test_domain
;
----------
[CREATE_PROG_OBJ] --
-- pgp armor
--
CREATE FUNCTION armor(bytea)
RETURNS text
AS '$libdir/pgcrypto', 'pg_armor'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table revalidate_bug (c float8 unique)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- fail, dup name
ALTER TABLE tt9 ADD UNIQUE(c)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "testxmlschema"
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table only dropColumn drop column e
;
----------
[DROP_TABLE] drop table oversized_column_default
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER end_rls_command ON ddl_command_end
    WHEN TAG IN ('CREATE POLICY', 'ALTER POLICY', 'DROP POLICY') EXECUTE PROCEDURE end_command()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
----------
[DROP_TABLE] DROP FOREIGN TABLE gft1
;
----------
[CREATE_TABLE] -- Create table with NOT NULL INVALID constraint, for pg_upgrade.
CREATE TABLE notnull_tbl1_upg (a int, b int)
;
----------
[CREATE_PROG_OBJ] /* helper function to create cursors for each page in [p_start, p_end] */
CREATE FUNCTION test_temp_pin(p_start int, p_end int)
RETURNS void
LANGUAGE plpgsql
AS $f$
  DECLARE
      cursorname text;
      query text;
  BEGIN
    FOR i IN p_start..p_end LOOP
       cursorname = 'c_'||i;
       query = format($q$DECLARE %I CURSOR FOR SELECT ctid FROM test_temp WHERE ctid >= '( %s, 1)'::tid $q$, cursorname, i);
       EXECUTE query;
       EXECUTE 'FETCH NEXT FROM '||cursorname;
       -- for test development
       -- RAISE NOTICE '%: %', cursorname, query;
    END LOOP;
  END;
$f$
;
----------
[DROP_SCHEMA] DROP SCHEMA schema_rf_x
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parted_trig_after_row after insert or update or delete on parted_trig
   for each row execute procedure trigger_notice()
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk51" (
    "a" integer CONSTRAINT "pk5_a_not_null" NOT NULL
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_numeric_union(internal, internal)
RETURNS gbtreekey_var
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_POLICY] -- LOCALE conflicts with LC_COLLATE
CREATE COLLATION coll_dup_chk (LC_COLLATE = "POSIX", LOCALE = '')
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30637 RENAME TO reind_con_toast
;
----------
[DROP_VIEW] DROP VIEW json_array_subquery_view
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest_tableoid"
    ADD CONSTRAINT "gtest_tableoid_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLESPACE] ALTER TABLESPACE split_ts_space SET (effective_io_concurrency = 2, maintenance_io_concurrency = 1)
;
----------
[CREATE_TABLE] -- test constraint error reporting in presence of dropped columns
create table atacc1 (id serial primary key, value int check (value < 10))
;
----------
[CREATE_POLICY] CREATE POLICY policy_stats ON tab_policy_stats USING (a = 5) WITH CHECK (b < 5)
;
----------
[CREATE_TABLE] create temp table rngfunc(f1 int8, f2 int8)
;
----------
[ADD_INDEX] CREATE INDEX ON regress_pg_dump_schema.extdependtab (col2)
;
----------
[CREATE_TABLE] -- timestamptz check

CREATE TABLE timestamptztmp (a timestamptz)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=17495 host=/tmp/HOMNU2DPPZ dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[TRUNCATE_TABLE] TRUNCATE guid1
;
----------
[CREATE_POLICY] /*
 * text search configuration for italian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * italian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY italian_stem
	(TEMPLATE = snowball, Language = italian , StopWords=italian)
;
----------
[CREATE_PROG_OBJ] create or replace function rttest()
returns setof int as $$
declare rc int;
  rca int[];
begin
  return query values(10),(20);
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query select * from (values(10),(20)) f(a) where false;
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query execute 'values(10),(20)';
  -- just for fun, let's use array elements as targets
  get diagnostics rca[1] = row_count;
  raise notice '% %', found, rca[1];
  return query execute 'select * from (values(10),(20)) f(a) where false';
  get diagnostics rca[2] = row_count;
  raise notice '% %', found, rca[2];
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] create table like_op_noprune (a text) partition by list (a)
;
----------
[ADMIN_TABLE] ANALYZE public.random_tbl
;
----------
[CREATE_TABLE] -- check that default_tablespace affects index additions in ALTER TABLE
CREATE TABLE testschema.test_tab(id int) TABLESPACE regress_tblspace
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (issn13, issn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ALTER_TABLE] alter table child inherit parent
;
----------
[ADD_INDEX] CREATE INDEX test1x ON test1 (a)
;
----------
[CREATE_TABLE] CREATE TABLE quad_box_tbl (b box)
;
----------
[CREATE_TABLE] create temp table nt3 (
  id int primary key,
  nt2_id int,
  c1 boolean,
  foreign key (nt2_id) references nt2(id)
)
;
----------
[CREATE_TABLE] create table part3 partition of range_parted for values from ('b', 1) to ('b', 10)
;
----------
[CREATE_TYPE] create type jsb_ia2 as (a int[][])
;
----------
[CREATE_TABLE] CREATE TABLE testschema.dflt (a int PRIMARY KEY USING INDEX TABLESPACE regress_tblspace) PARTITION BY LIST (a)
;
----------
[DROP_TABLE] drop table listp
;
----------
[DROP_PROG_OBJ] DROP OPERATOR IF EXISTS + (no_such_type, no_such_type)
;
----------
[ADMIN_TABLE] ANALYZE local_tbl
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ismn, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[BLOCK] DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19143 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _pg_datetime_precision(typid oid, typmod int4) RETURNS integer
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT
  CASE WHEN $1 IN (1082) /* date */
           THEN 0
       WHEN $1 IN (1083, 1114, 1184, 1266) /* time, timestamp, same + tz */
           THEN CASE WHEN $2 < 0 THEN 6 ELSE $2 END
       WHEN $1 IN (1186) /* interval */
           THEN CASE WHEN $2 < 0 OR $2 & 65535 = 65535 THEN 6 ELSE $2 & 65535 END
       ELSE null
  END$$
;
----------
[ADD_INDEX] create index idxpart1_2_idx on idxpart1 ((b + a)) where a > 1
;
----------
[CREATE_TABLE] -- Check pruning for a partition tree containing only temporary relations
create temp table pp_temp_parent (a int) partition by list (a)
;
