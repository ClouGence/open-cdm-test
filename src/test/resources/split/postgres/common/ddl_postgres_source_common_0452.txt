-- someday this should work
drop table attbl, atref
;
create table tab_batch_sharded_p0 partition of tab_batch_sharded
  for values with (modulus 2, remainder 0)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
CREATE FOREIGN TABLE ft7 (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text
) SERVER loopback3 OPTIONS (schema_name 'S 1', table_name 'T 4')
;
create schema fkpart3
  create table pkey (a int primary key)
  create table fk_part (a int, constraint fkey foreign key (a) references fkpart3.pkey deferrable initially immediate) partition by list (a)
  create table fk_part_1 partition of fkpart3.fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 partition of fkpart3.fk_part_1 for values in (1)
  create table fk_part_2 partition of fkpart3.fk_part for values in (2)
;
CREATE UNLOGGED TABLE tu1 AS SELECT 1 AS a
;
CREATE INDEX "gcircleind" ON "public"."circle_tbl" USING "gist" ("f1")
;
COMMENT ON TEXT SEARCH DICTIONARY lithuanian_stem IS 'snowball stemmer for lithuanian language'
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.time_tz()'))
;
CREATE TABLE cnn_pk (a int, b int, CONSTRAINT cnn_primarykey PRIMARY KEY (b))
;
DROP MATERIALIZED VIEW IF EXISTS no_such_schema.foo
;
--
-- CREATE_VIEW
-- Virtual class definitions
--	(this also tests the query rewrite system)
--

CREATE VIEW street AS
   SELECT r.name, r.thepath, c.cname AS cname
   FROM ONLY road r, real_city c
   WHERE c.outline ## r.thepath
;
VACUUM vacowned_parted
;
ALTER TABLE "public"."itest17" ALTER COLUMN "c" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest17_c_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
CREATE FUNCTION gin_extract_query_macaddr(macaddr, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE FUNCTION citextout(citext)
RETURNS cstring
AS 'textout'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26654_2 (tid pg_catalog.tid)
;
CREATE OPERATOR split_operator_probe.## (
    FUNCTION = split_operator_probe.int_add,
    LEFTARG = integer,
    RIGHTARG = integer
)
;
drop table i8mr_array
;
DROP FUNCTION g_cube_decompress(pg_catalog.internal)
;
ALTER FUNCTION functest_E_2(int) NOT LEAKPROOF
;
DROP TABLE target, target2
;
create domain d_fail as int4 not null null
;
CREATE VIEW pg_statio_sys_indexes AS
    SELECT * FROM pg_statio_all_indexes
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
--
-- cannot make same name conversion in same schema
--
CREATE CONVERSION myconv FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
-- Set boolean option to true without specifying value
ALTER TABLE reloptions_test SET (autovacuum_enabled, fillfactor=32)
;
create trigger tg_pline_bu before update
    on PLine for each row execute procedure tg_pline_bu()
;
CALL transaction_test1(9, $x$baz$x$)
;
---------------------------------------------
-- intbig
---------------------------------------------
-- define the GiST support methods

CREATE FUNCTION _intbig_in(cstring)
RETURNS intbig_gkey
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP TABLE clstr_3
;
CREATE OPERATOR FAMILY alt_opf19 USING btree
;
ALTER DATABASE regression_utf8 CONNECTION_LIMIT 123
;
create table tprt_4 partition of tprt for values from (2001) to (3001)
;
CREATE TEMP TABLE pg_temp_2.pg_temp_28694_2 (tid pg_catalog.tid)
;
CREATE POLICY p1 ON r2 USING (false)
;
ALTER TEXT SEARCH CONFIGURATION basque ADD MAPPING
    FOR word, hword_part, hword
	WITH basque_stem
;
DROP TABLE truncate_a,trunc_c,trunc_b,trunc_d,trunc_e CASCADE
;
-- Ensure inherited stats ARE applied to inherited query in partitioned table
CREATE TABLE stxdinp(i int, a int, b int) PARTITION BY RANGE (i)
;
-- ===================================================================
-- test cleanup of failed connections on abort
-- ===================================================================

CREATE VIEW my_backend_pid (pid) AS SELECT pg_backend_pid()
;
-- repeated rewrites in different transactions
VACUUM FULL pg_class
;
VACUUM (PARALLEL 0) pg_catalog.pg_index
;
CREATE OPERATOR CLASS float4_ops
DEFAULT FOR TYPE float4 USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btfloat4cmp(float4,float4),
    FUNCTION        2       gin_extract_value_float4(float4, internal),
    FUNCTION        3       gin_extract_query_float4(float4, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_float4(float4,float4,int2, internal),
STORAGE         float4
;
ALTER TABLE pg_toast.pg_toast_27879 RENAME TO reind_con_toast
;
DROP TABLE ext_stats_test
;
VACUUM (ANALYZE) vacowned_parted
;
DROP PUBLICATION testpub4
;
drop function anyctest(variadic anycompatiblearray)
;
CREATE TABLE "public"."beta_neg_p1" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
CREATE TABLE d3_listp (a int) PARTITION BY LIST(a)
;
ALTER TABLE attmp3 drop constraint attmpconstr
;
------------------------------------------------------------------------------------------
[DROP_TABLE] -- someday this should work
drop table attbl, atref
;
----------
[CREATE_TABLE] create table tab_batch_sharded_p0 partition of tab_batch_sharded
  for values with (modulus 2, remainder 0)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft7 (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text
) SERVER loopback3 OPTIONS (schema_name 'S 1', table_name 'T 4')
;
----------
[CREATE_SCHEMA] create schema fkpart3
  create table pkey (a int primary key)
  create table fk_part (a int, constraint fkey foreign key (a) references fkpart3.pkey deferrable initially immediate) partition by list (a)
  create table fk_part_1 partition of fkpart3.fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 partition of fkpart3.fk_part_1 for values in (1)
  create table fk_part_2 partition of fkpart3.fk_part for values in (2)
;
----------
[CREATE_TABLE|SELECT] CREATE UNLOGGED TABLE tu1 AS SELECT 1 AS a
;
----------
[ADD_INDEX] CREATE INDEX "gcircleind" ON "public"."circle_tbl" USING "gist" ("f1")
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY lithuanian_stem IS 'snowball stemmer for lithuanian language'
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.time_tz()'))
;
----------
[CREATE_TABLE] CREATE TABLE cnn_pk (a int, b int, CONSTRAINT cnn_primarykey PRIMARY KEY (b))
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW IF EXISTS no_such_schema.foo
;
----------
[CREATE_VIEW(SELECT)] --
-- CREATE_VIEW
-- Virtual class definitions
--	(this also tests the query rewrite system)
--

CREATE VIEW street AS
   SELECT r.name, r.thepath, c.cname AS cname
   FROM ONLY road r, real_city c
   WHERE c.outline ## r.thepath
;
----------
[ADMIN_TABLE] VACUUM vacowned_parted
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest17" ALTER COLUMN "c" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest17_c_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_macaddr(macaddr, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citextout(citext)
RETURNS cstring
AS 'textout'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26654_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR split_operator_probe.## (
    FUNCTION = split_operator_probe.int_add,
    LEFTARG = integer,
    RIGHTARG = integer
)
;
----------
[DROP_TABLE] drop table i8mr_array
;
----------
[DROP_PROG_OBJ] DROP FUNCTION g_cube_decompress(pg_catalog.internal)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_E_2(int) NOT LEAKPROOF
;
----------
[DROP_TABLE] DROP TABLE target, target2
;
----------
[CREATE_TYPE] create domain d_fail as int4 not null null
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_statio_sys_indexes AS
    SELECT * FROM pg_statio_all_indexes
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
----------
[CREATE_POLICY] --
-- cannot make same name conversion in same schema
--
CREATE CONVERSION myconv FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[ALTER_TABLE] -- Set boolean option to true without specifying value
ALTER TABLE reloptions_test SET (autovacuum_enabled, fillfactor=32)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_pline_bu before update
    on PLine for each row execute procedure tg_pline_bu()
;
----------
[CALL_PROG_OBJ] CALL transaction_test1(9, $x$baz$x$)
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------
-- intbig
---------------------------------------------
-- define the GiST support methods

CREATE FUNCTION _intbig_in(cstring)
RETURNS intbig_gkey
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE clstr_3
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf19 USING btree
;
----------
[ALTER_CATALOG] ALTER DATABASE regression_utf8 CONNECTION_LIMIT 123
;
----------
[CREATE_TABLE] create table tprt_4 partition of tprt for values from (2001) to (3001)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_2.pg_temp_28694_2 (tid pg_catalog.tid)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON r2 USING (false)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION basque ADD MAPPING
    FOR word, hword_part, hword
	WITH basque_stem
;
----------
[DROP_TABLE] DROP TABLE truncate_a,trunc_c,trunc_b,trunc_d,trunc_e CASCADE
;
----------
[CREATE_TABLE] -- Ensure inherited stats ARE applied to inherited query in partitioned table
CREATE TABLE stxdinp(i int, a int, b int) PARTITION BY RANGE (i)
;
----------
[CREATE_VIEW(SELECT)] -- ===================================================================
-- test cleanup of failed connections on abort
-- ===================================================================

CREATE VIEW my_backend_pid (pid) AS SELECT pg_backend_pid()
;
----------
[ADMIN_TABLE] -- repeated rewrites in different transactions
VACUUM FULL pg_class
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_index
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS float4_ops
DEFAULT FOR TYPE float4 USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btfloat4cmp(float4,float4),
    FUNCTION        2       gin_extract_value_float4(float4, internal),
    FUNCTION        3       gin_extract_query_float4(float4, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_float4(float4,float4,int2, internal),
STORAGE         float4
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27879 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE ext_stats_test
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) vacowned_parted
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub4
;
----------
[DROP_PROG_OBJ] drop function anyctest(variadic anycompatiblearray)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."beta_neg_p1" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE d3_listp (a int) PARTITION BY LIST(a)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE attmp3 drop constraint attmpconstr
;
