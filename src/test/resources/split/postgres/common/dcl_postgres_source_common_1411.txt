--
-- Check behavior when subtype lacks a hash function
--

set enable_sort = off
;
SET default_tablespace TO pg_global
;
EXECUTE dumpOpr('3520')
;
-- check updates of primary keys work correctly
BEGIN
;
SET LOCAL TIME ZONE 'Australia/Sydney'
;
ALTER FOREIGN DATA WRAPPER test_fdw VALIDATOR postgresql_fdw_validator
;
CREATE SERVER s2 FOREIGN DATA WRAPPER foo OPTIONS (host 'a', dbname 'b')
;
EXECUTE dumpFunc('28025')
;
DEALLOCATE pgsql_fdw_prep_101
;
--
-- Mixed ownership inheritance tree
--
create role regress_alice
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] --
-- Check behavior when subtype lacks a hash function
--

set enable_sort = off
;
----------
[SESSION_SETTING_WRITE] SET default_tablespace TO pg_global
;
----------
[UNSAFE] EXECUTE dumpOpr('3520')
;
----------
[TRANSACTION] -- check updates of primary keys work correctly
BEGIN
;
----------
[SESSION_SETTING_WRITE] SET LOCAL TIME ZONE 'Australia/Sydney'
;
----------
[SYSTEM_SETTING_WRITE] ALTER FOREIGN DATA WRAPPER test_fdw VALIDATOR postgresql_fdw_validator
;
----------
[SYSTEM_SETTING_WRITE] CREATE SERVER s2 FOREIGN DATA WRAPPER foo OPTIONS (host 'a', dbname 'b')
;
----------
[UNSAFE] EXECUTE dumpFunc('28025')
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_101
;
----------
[CREATE_ROLE] --
-- Mixed ownership inheritance tree
--
create role regress_alice
;
