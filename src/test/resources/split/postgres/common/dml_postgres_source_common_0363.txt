update domarrtest set
  testint4arr[1] = testint4arr[1] + 1,
  testint4arr[3] = testint4arr[3] - 1
where testchar4arr is null
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1100', relminmxid = '1'
WHERE oid = '"public"."textrange_test"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1665', relminmxid = '1'
WHERE oid = '"public"."reservations"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12767', relminmxid = '3'
WHERE oid = '"public"."part32_self_fk"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('7', 967)
;
INSERT INTO FKTABLE VALUES (NULL, 2, 3, 2)
;
INSERT INTO public.tht VALUES ('8', 228)
;
INSERT INTO temporal_rng (id, valid_at) VALUES ('[1,2)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT ON CONSTRAINT temporal_rng_pk DO UPDATE SET id = EXCLUDED.id + '[2,3)'
;
INSERT INTO public.tplain VALUES ('4', 194)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13915', relminmxid = '3'
WHERE oid = '"public"."utf8_verification_inputs"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('4', 754)
;
insert into other_partitioned_fk
  select 2048, x from generate_series(1,10) x
;
INSERT INTO public.tplain VALUES ('2', 242)
;
UPDATE ONLY "pg_temp_94"."tasks" SET "owner" = NULL WHERE $1 OPERATOR(pg_catalog.=) "owner"
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."tableam_parted_a_heap2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16407', relminmxid = '3'
WHERE oid = '"public"."prt1_e_p2"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (9, 259)
;
INSERT INTO J1_TBL VALUES (5, 0, 'five')
;
WITH wcte AS ( INSERT INTO child1 VALUES ( 42, 'new' ) RETURNING id AS newid )
UPDATE parent SET id = id + newid FROM wcte
;
-- inference fails:
insert into insertconflicttest values (22, 'Apricot') on conflict (upper(fruit)) do update set fruit = excluded.fruit
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"fkpart3"."fk2"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16511', relminmxid = '3'
WHERE oid = '"public"."truncate_b"'::pg_catalog.regclass
;
INSERT INTO pagg_tab_p3 SELECT i % 30, i % 50, to_char(i/30, 'FM0000') FROM generate_series(1, 3000) i WHERE (i % 30) < 30 and (i % 30) >= 20
;
INSERT INTO gtest_varlena (a) VALUES(NULL)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11144', relminmxid = '1'
WHERE oid = '29992'
;
-- badly formatted box inputs
INSERT INTO BOX_TBL (f1) VALUES ('(2.3, 4.5)')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17666', relminmxid = '3'
WHERE oid = '39939'
;
DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_9.pg_temp_26686_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
update test_jsonb_subscript set test_json['a'][10][10] = '1'
;
INSERT INTO public.tplain VALUES ('1', 91)
;
INSERT INTO public.tht VALUES ('5', 15)
;
INSERT INTO public.ths_p3 VALUES (1, 321)
;
INSERT INTO caster (citext)        VALUES ('foo'::bpchar)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."brin_test"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1349', relminmxid = '1'
WHERE oid = '"public"."polygon_tbl"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16966', relminmxid = '3'
WHERE oid = '38935'
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt3_n_p3"'::pg_catalog.regclass
;
UPDATE rw_view15 SET upper=DEFAULT WHERE a=3
;
-- example from https://unicode-org.github.io/icu/userguide/collation/customization/#syntax
INSERT INTO test7 VALUES ('Abernathy'), ('apple'), ('bird'), ('Boston'), ('Graham'), ('green')
;
-- ok, should pass the RLS check
UPDATE range_parted set a = 'b', c = 112 WHERE a = 'a' and c = 200
;
INSERT INTO public.tht VALUES ('1', 581)
;
WITH rand_value AS (SELECT string_agg(md5((-i)::text),'') AS val FROM generate_series(1,60) s(i))
INSERT INTO brintest_3
SELECT val, val, val, val FROM rand_value
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6747', relminmxid = '1'
WHERE oid = '25470'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '"public"."persons2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10073', relminmxid = '1'
WHERE oid = '"public"."rtest_order1"'::pg_catalog.regclass
;
INSERT INTO nummultirange_test VALUES(nummultirange(numrange(1.7, 1.7, '[]'), numrange(1.9, 2.1)))
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4113', relminmxid = '1'
WHERE oid = '"public"."check2_tbl"'::pg_catalog.regclass
;
INSERT INTO int4tbl (SELECT * FROM generate_series(1,1000) gs)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."covidxpart1"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (4, 74)
;
------------------------------------------------------------------------------------------
[UPDATE] update domarrtest set
  testint4arr[1] = testint4arr[1] + 1,
  testint4arr[3] = testint4arr[3] - 1
where testchar4arr is null
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '1100', relminmxid = '1'
WHERE oid = '"public"."textrange_test"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1665', relminmxid = '1'
WHERE oid = '"public"."reservations"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12767', relminmxid = '3'
WHERE oid = '"public"."part32_self_fk"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 967)
;
----------
[INSERT] INSERT INTO FKTABLE VALUES (NULL, 2, 3, 2)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 228)
;
----------
[MERGE|SELECT] INSERT INTO temporal_rng (id, valid_at) VALUES ('[1,2)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT ON CONSTRAINT temporal_rng_pk DO UPDATE SET id = EXCLUDED.id + '[2,3)'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 194)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13915', relminmxid = '3'
WHERE oid = '"public"."utf8_verification_inputs"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 754)
;
----------
[INSERT|SELECT] insert into other_partitioned_fk
  select 2048, x from generate_series(1,10) x
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 242)
;
----------
[UPDATE] UPDATE ONLY "pg_temp_94"."tasks" SET "owner" = NULL WHERE $1 OPERATOR(pg_catalog.=) "owner"
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."tableam_parted_a_heap2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16407', relminmxid = '3'
WHERE oid = '"public"."prt1_e_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 259)
;
----------
[INSERT] INSERT INTO J1_TBL VALUES (5, 0, 'five')
;
----------
[UPDATE|INSERT] WITH wcte AS ( INSERT INTO child1 VALUES ( 42, 'new' ) RETURNING id AS newid )
UPDATE parent SET id = id + newid FROM wcte
;
----------
[MERGE] -- inference fails:
insert into insertconflicttest values (22, 'Apricot') on conflict (upper(fruit)) do update set fruit = excluded.fruit
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"fkpart3"."fk2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '16511', relminmxid = '3'
WHERE oid = '"public"."truncate_b"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pagg_tab_p3 SELECT i % 30, i % 50, to_char(i/30, 'FM0000') FROM generate_series(1, 3000) i WHERE (i % 30) < 30 and (i % 30) >= 20
;
----------
[INSERT|SELECT] INSERT INTO gtest_varlena (a) VALUES(NULL)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11144', relminmxid = '1'
WHERE oid = '29992'
;
----------
[INSERT|SELECT] -- badly formatted box inputs
INSERT INTO BOX_TBL (f1) VALUES ('(2.3, 4.5)')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17666', relminmxid = '3'
WHERE oid = '39939'
;
----------
[DELETE|SELECT] DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_9.pg_temp_26686_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE] update test_jsonb_subscript set test_json['a'][10][10] = '1'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 91)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 15)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 321)
;
----------
[INSERT|SELECT] INSERT INTO caster (citext)        VALUES ('foo'::bpchar)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."brin_test"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1349', relminmxid = '1'
WHERE oid = '"public"."polygon_tbl"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16966', relminmxid = '3'
WHERE oid = '38935'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt3_n_p3"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE rw_view15 SET upper=DEFAULT WHERE a=3
;
----------
[INSERT] -- example from https://unicode-org.github.io/icu/userguide/collation/customization/#syntax
INSERT INTO test7 VALUES ('Abernathy'), ('apple'), ('bird'), ('Boston'), ('Graham'), ('green')
;
----------
[UPDATE] -- ok, should pass the RLS check
UPDATE range_parted set a = 'b', c = 112 WHERE a = 'a' and c = 200
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 581)
;
----------
[INSERT|SELECT] WITH rand_value AS (SELECT string_agg(md5((-i)::text),'') AS val FROM generate_series(1,60) s(i))
INSERT INTO brintest_3
SELECT val, val, val, val FROM rand_value
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6747', relminmxid = '1'
WHERE oid = '25470'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '"public"."persons2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10073', relminmxid = '1'
WHERE oid = '"public"."rtest_order1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO nummultirange_test VALUES(nummultirange(numrange(1.7, 1.7, '[]'), numrange(1.9, 2.1)))
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4113', relminmxid = '1'
WHERE oid = '"public"."check2_tbl"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO int4tbl (SELECT * FROM generate_series(1,1000) gs)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."covidxpart1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 74)
;
