CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE OPERATOR CLASS citext_ops
DEFAULT FOR TYPE CITEXT USING btree AS
    OPERATOR    1   <  (citext, citext),
    OPERATOR    2   <= (citext, citext),
    OPERATOR    3   =  (citext, citext),
    OPERATOR    4   >= (citext, citext),
    OPERATOR    5   >  (citext, citext),
    FUNCTION    1   citext_cmp(citext, citext)
;
CREATE FUNCTION ltree_cmp(ltree,ltree)
RETURNS int4
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE itest7a (b text) INHERITS (itest7)
;
DROP TABLE pg_temp_81.pg_temp_30251_2, pg_temp_81.pg_temp_30251
;
CREATE TEXT SEARCH DICTIONARY estonian_stem
	(TEMPLATE = snowball, Language = estonian )
;
-- some more tests to exercise tuple-routing with multi-level partitioning
create table part_gg partition of list_parted for values in ('gg') partition by range (b)
;
-- all documents are readable
CREATE POLICY p1 ON document FOR SELECT USING (true)
;
DROP TABLE partitioned, partitioned2
;
CREATE FUNCTION gin_page_opaque_info(IN page bytea,
    OUT rightlink bigint,
    OUT maxoff int4,
    OUT flags text[])
AS '$libdir/pageinspect', 'gin_page_opaque_info'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER TABLE ONLY "public"."alpha_neg" ATTACH PARTITION "public"."alpha_neg_p3" FOR VALUES FROM (300) TO (400)
;
ALTER TABLE "public"."itest7c" ALTER COLUMN "a" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest7c_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
drop table parted_replica_tab
;
drop function simplesql()
;
CREATE TABLESPACE reindex_tbspace LOCATION '/src/src/bin/scripts/tmp_check/t_090_reindexdb_main_data/regress_reindex_tbspace'
;
-- Change plt2_adv_p2_ext to the default partition
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p2_ext DEFAULT
;
create type textrange1 as range(subtype=text, multirange_type_name=int, collation="C")
;
/* contrib/postgres_fdw/postgres_fdw--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION postgres_fdw_handler()
RETURNS fdw_handler
AS '$libdir/postgres_fdw'
LANGUAGE C STRICT
;
CREATE TABLE fk_partitioned_pk_61 PARTITION OF fk_partitioned_pk_6 FOR VALUES IN (1)
;
create table rules_fooview_part partition of rules_fooview for values in (1)
;
-- check resolution of ambiguous DROP commands

CREATE PROCEDURE ptest10(IN a int, IN b int, IN c int)
LANGUAGE SQL AS $$ SELECT a + b - c $$
;
CREATE TABLE list_parted (
	a int NOT NULL,
	b char(2) COLLATE "C",
	CONSTRAINT check_a CHECK (a > 0)
) PARTITION BY LIST (a)
;
CREATE TEMPORARY TABLE y (a INTEGER)
;
CREATE UNIQUE INDEX mvtest_tvmm_expr ON mvtest_tvmm ((grandtot > 0))
;
CREATE TABLE test_replica_identity (
       id serial primary key,
       keya text not null,
       keyb text not null,
       nonkey text,
       CONSTRAINT test_replica_identity_unique_defer UNIQUE (keya, keyb) DEFERRABLE,
       CONSTRAINT test_replica_identity_unique_nondefer UNIQUE (keya, keyb)
)
;
-- Partition by multiple columns

CREATE TABLE pagg_tab_m (a int, b int, c int) PARTITION BY RANGE(a, ((a+b)/2))
;
CREATE FUNCTION isneq(ean13, ismn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION gbt_bool_consistent(internal,bool,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER INDEX pg_toast.pg_toast_30609_index RENAME TO reind_con_toast_idx
;
CREATE TABLE oer_test (a int)
;
--
--
--
-- float4 ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_float4_consistent(internal,float4,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- Reloptions on TOAST tables
ALTER TABLE pg_toast.pg_toast_2615 SET (fillfactor = '90')
;
CREATE TABLE somechild () INHERITS (someparent)
;
CREATE TABLE gtest_rule_log (op text, old_b int, new_b int)
;
-- OK
SECURITY LABEL ON DOMAIN dummy_seclabel_domain IS 'classified'
;
VACUUM (PARALLEL 0) pg_catalog.pg_ts_template
;
CREATE TEXT SEARCH DICTIONARY "public"."hunspell_invalid_1" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_long', afffile = 'ispell_sample' )
;
-- tablespace should not change if no rewrite
ALTER TABLE testschema.test_default_tab ALTER id TYPE bigint
;
CREATE MATERIALIZED VIEW pgss_materialized_view AS
  SELECT * FROM generate_series(1, 5) as id
;
TRUNCATE hash_cleanup_heap
;
-- 2. foreign key pointing into a single leaf partition
--
-- A cross-partition update that deletes from the pointed-to leaf partition
-- is allowed to succeed
ALTER TABLE fkpart11.fk DROP CONSTRAINT fkey
;
-- Check initial-positioning logic too

CREATE UNIQUE INDEX onek_nulltest ON onek_with_null (unique2)
;
CREATE UNIQUE INDEX test_replica_identity_expr ON test_replica_identity (keya, keyb, (3))
;
CREATE FOREIGN TABLE async_p_broken PARTITION OF async_pt FOR VALUES FROM (10000) TO (10001)
  SERVER loopback OPTIONS (table_name 'non_existent_table')
;
-- Test that UPDATE/DELETE with inherited target works with row-level triggers
CREATE TRIGGER trig_row_before
BEFORE UPDATE OR DELETE ON bar2
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
CREATE TYPE "public"."text_w_default"
;
--
-- Verify behavior of queries with wCTEs, where multiple transition
-- tuplestores can be active at the same time because there are
-- multiple DML statements that might fire triggers with transition
-- tables
--
create table table1 (a int)
;
drop table �ͪ�ߩ�Ѧ��듾�
;
CREATE INDEX ON quad_point_tbl_ord_seq1 USING spgist(p) INCLUDE(dist)
;
-- create some random data type
create domain posint as int check (value > 0)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS citext_ops
DEFAULT FOR TYPE CITEXT USING btree AS
    OPERATOR    1   <  (citext, citext),
    OPERATOR    2   <= (citext, citext),
    OPERATOR    3   =  (citext, citext),
    OPERATOR    4   >= (citext, citext),
    OPERATOR    5   >  (citext, citext),
    FUNCTION    1   citext_cmp(citext, citext)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_cmp(ltree,ltree)
RETURNS int4
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE itest7a (b text) INHERITS (itest7)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_81.pg_temp_30251_2, pg_temp_81.pg_temp_30251
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY estonian_stem
	(TEMPLATE = snowball, Language = estonian )
;
----------
[CREATE_TABLE] -- some more tests to exercise tuple-routing with multi-level partitioning
create table part_gg partition of list_parted for values in ('gg') partition by range (b)
;
----------
[CREATE_POLICY] -- all documents are readable
CREATE POLICY p1 ON document FOR SELECT USING (true)
;
----------
[DROP_TABLE] DROP TABLE partitioned, partitioned2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_page_opaque_info(IN page bytea,
    OUT rightlink bigint,
    OUT maxoff int4,
    OUT flags text[])
AS '$libdir/pageinspect', 'gin_page_opaque_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."alpha_neg" ATTACH PARTITION "public"."alpha_neg_p3" FOR VALUES FROM (300) TO (400)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest7c" ALTER COLUMN "a" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest7c_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[DROP_TABLE] drop table parted_replica_tab
;
----------
[DROP_PROG_OBJ] drop function simplesql()
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE reindex_tbspace LOCATION '/src/src/bin/scripts/tmp_check/t_090_reindexdb_main_data/regress_reindex_tbspace'
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Change plt2_adv_p2_ext to the default partition
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p2_ext DEFAULT
;
----------
[CREATE_POLICY] create type textrange1 as range(subtype=text, multirange_type_name=int, collation="C")
;
----------
[CREATE_PROG_OBJ] /* contrib/postgres_fdw/postgres_fdw--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION postgres_fdw_handler()
RETURNS fdw_handler
AS '$libdir/postgres_fdw'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_pk_61 PARTITION OF fk_partitioned_pk_6 FOR VALUES IN (1)
;
----------
[CREATE_TABLE] create table rules_fooview_part partition of rules_fooview for values in (1)
;
----------
[CREATE_PROG_OBJ] -- check resolution of ambiguous DROP commands

CREATE PROCEDURE ptest10(IN a int, IN b int, IN c int)
LANGUAGE SQL AS $$ SELECT a + b - c $$
;
----------
[CREATE_TABLE] CREATE TABLE list_parted (
	a int NOT NULL,
	b char(2) COLLATE "C",
	CONSTRAINT check_a CHECK (a > 0)
) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE y (a INTEGER)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX mvtest_tvmm_expr ON mvtest_tvmm ((grandtot > 0))
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_identity (
       id serial primary key,
       keya text not null,
       keyb text not null,
       nonkey text,
       CONSTRAINT test_replica_identity_unique_defer UNIQUE (keya, keyb) DEFERRABLE,
       CONSTRAINT test_replica_identity_unique_nondefer UNIQUE (keya, keyb)
)
;
----------
[CREATE_TABLE] -- Partition by multiple columns

CREATE TABLE pagg_tab_m (a int, b int, c int) PARTITION BY RANGE(a, ((a+b)/2))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ean13, ismn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bool_consistent(internal,bool,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30609_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE oer_test (a int)
;
----------
[CREATE_PROG_OBJ] --
--
--
-- float4 ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_float4_consistent(internal,float4,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE] -- Reloptions on TOAST tables
ALTER TABLE pg_toast.pg_toast_2615 SET (fillfactor = '90')
;
----------
[CREATE_TABLE] CREATE TABLE somechild () INHERITS (someparent)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_rule_log (op text, old_b int, new_b int)
;
----------
[SECURITY_LABEL] -- OK
SECURITY LABEL ON DOMAIN dummy_seclabel_domain IS 'classified'
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_ts_template
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."hunspell_invalid_1" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_long', afffile = 'ispell_sample' )
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- tablespace should not change if no rewrite
ALTER TABLE testschema.test_default_tab ALTER id TYPE bigint
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW pgss_materialized_view AS
  SELECT * FROM generate_series(1, 5) as id
;
----------
[TRUNCATE_TABLE] TRUNCATE hash_cleanup_heap
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- 2. foreign key pointing into a single leaf partition
--
-- A cross-partition update that deletes from the pointed-to leaf partition
-- is allowed to succeed
ALTER TABLE fkpart11.fk DROP CONSTRAINT fkey
;
----------
[ADD_INDEX] -- Check initial-positioning logic too

CREATE UNIQUE INDEX onek_nulltest ON onek_with_null (unique2)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX test_replica_identity_expr ON test_replica_identity (keya, keyb, (3))
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE async_p_broken PARTITION OF async_pt FOR VALUES FROM (10000) TO (10001)
  SERVER loopback OPTIONS (table_name 'non_existent_table')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Test that UPDATE/DELETE with inherited target works with row-level triggers
CREATE TRIGGER trig_row_before
BEFORE UPDATE OR DELETE ON bar2
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[CREATE_TYPE] CREATE TYPE "public"."text_w_default"
;
----------
[CREATE_TABLE] --
-- Verify behavior of queries with wCTEs, where multiple transition
-- tuplestores can be active at the same time because there are
-- multiple DML statements that might fire triggers with transition
-- tables
--
create table table1 (a int)
;
----------
[DROP_TABLE] drop table �ͪ�ߩ�Ѧ��듾�
;
----------
[ADD_INDEX] CREATE INDEX ON quad_point_tbl_ord_seq1 USING spgist(p) INCLUDE(dist)
;
----------
[CREATE_TYPE] -- create some random data type
create domain posint as int check (value > 0)
;
