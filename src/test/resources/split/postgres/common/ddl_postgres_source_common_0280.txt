CREATE INDEX ON test2 USING brin (a) WITH (pages_per_range=1) WHERE (a IS NULL)
;
DROP TABLE ffk, ffk1
;
-- DROP IDENTITY tests

ALTER TABLE itest4 ALTER COLUMN a DROP IDENTITY
;
ALTER TABLE ONLY "public"."btree_tall_tbl" ALTER COLUMN "t" SET STORAGE PLAIN
;
CREATE TABLE log_table (tstamp timestamp default timeofday()::timestamp)
;
CREATE FUNCTION "public"."tg_chkslotlink"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotlink isnull then
        new.slotlink := '';
    end if;
    return new;
end;
$$
;
CREATE TABLE brintest_expr (n int)
;
CREATE TABLE "public"."brinopers" (
    "colname" "name",
    "typ" "text",
    "op" "text"[],
    "value" "text"[],
    "matches" integer[],
    CONSTRAINT "brinopers_check" CHECK (("cardinality"("op") = "cardinality"("value"))),
    CONSTRAINT "brinopers_check1" CHECK (("cardinality"("op") = "cardinality"("matches")))
)
;
CREATE TABLE someparent (a int)
;
CREATE SEQUENCE x_seq
;
CREATE TABLE FKTABLE (ftest1 int REFERENCES pktable)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_index
;
create index minmaxtest1i on minmaxtest1(f1)
;
CREATE DATABASE db0
;
CREATE MATERIALIZED VIEW s2.t1_mv AS SELECT * FROM s2.t1
;
-- OK with more than one equal column: constraint is a proper superset of partition key
create table idxpart (a int4range, b int4range) partition by range (a)
;
CREATE TEXT SEARCH PARSER tspars_case ("Start" = start_function)
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp() < $.datetime("HH:MI TZH"))'))
;
CREATE TABLE "public"."infinite_interval_tbl" (
    "i" interval
)
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=15123 host=/tmp/sVwt2xKRMl dbname=postgres' PUBLICATION mypub
;
CREATE TABLE t_009_tbl (id int, msg text)
;
CREATE INDEX "iprt1_e_p2_ab2" ON "public"."prt1_e_p2" USING "btree" (((("a" + "b") / 2)))
;
-- fail
CALL priv_testproc1(6)
;
CREATE TABLE tab_rf_x (x int primary key)
;
CREATE SCHEMA s2
;
CREATE TABLE fk_partitioned_fk_6 (a int,
	FOREIGN KEY (a) REFERENCES fk_partitioned_pk_6,
	FOREIGN KEY (a) REFERENCES fk_partitioned_pk_6
) PARTITION BY LIST (a)
;
alter table rewriteme
 add column onemore int default 0,
 add column another int default -1,
 alter column foo type numeric(10,4)
;
CREATE TABLE "public"."alter_idx_rename_test_parted_2" (
    "a" integer
)
PARTITION BY LIST ("a")
;
CREATE SEQUENCE sequence_test4 INCREMENT BY -1
;
ALTER TABLE ONLY "public"."sj"
    ADD CONSTRAINT "sj_a_key" UNIQUE ("a")
;
CREATE INDEX idx_pgvector_items_ivfflat_cosine ON split_pgvector_probe.items USING ivfflat (embedding vector_cosine_ops) WITH (lists = 1)
;
VACUUM (ONLY_DATABASE_STATS) vactst
;
ALTER TABLE gtest28a DROP COLUMN a
;
-- make sure data are properly de-toasted in BRIN index
CREATE TABLE brintest_3 (a text, b text, c text, d text)
;
COMMENT ON FUNCTION tg_hub_adjustslots(bpchar, integer, integer) IS 'function with args'
;
-- JSON_TABLE: plan execution

CREATE TEMP TABLE jsonb_table_test (js jsonb)
;
drop domain ddef1 restrict
;
CREATE TABLE "public"."room" (
    "roomno" character(8),
    "comment" "text"
)
;
create table chi partition of middle for values from (1) to (5)
;
create table range_parted (a int, b int, c int) partition by range(a, b)
;
ALTER TABLE pg_toast.pg_toast_30954 RENAME TO reind_con_toast
;
CREATE FUNCTION "public"."test_ret_set_scalar"(integer, integer) RETURNS SETOF integer
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
	i int;
BEGIN
	FOR i IN $1 .. $2 LOOP
		RETURN NEXT i + 1;
	END LOOP;
	RETURN;
END;$_$
;
CREATE TEMP TABLE pg_temp_2.pg_temp_30214_2 (tid pg_catalog.tid)
;
--
-- Typanalyze function that returns false
--
CREATE FUNCTION int_custom_typanalyze_false(internal)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C STRICT
;
CREATE TRIGGER trigtest_before_stmt BEFORE INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH STATEMENT
EXECUTE PROCEDURE dummy_trigger()
;
COMMENT ON TYPE cube IS 'multi-dimensional cube ''(FLOAT-1, FLOAT-2, ..., FLOAT-N), (FLOAT-1, FLOAT-2, ..., FLOAT-N)'''
;
create table rtest_nothn3 (a int4, b text)
;
drop view agg_view1
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_replication_origin
;
create index on mclparted (a)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX ON test2 USING brin (a) WITH (pages_per_range=1) WHERE (a IS NULL)
;
----------
[DROP_TABLE] DROP TABLE ffk, ffk1
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- DROP IDENTITY tests

ALTER TABLE itest4 ALTER COLUMN a DROP IDENTITY
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."btree_tall_tbl" ALTER COLUMN "t" SET STORAGE PLAIN
;
----------
[CREATE_TABLE] CREATE TABLE log_table (tstamp timestamp default timeofday()::timestamp)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_chkslotlink"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotlink isnull then
        new.slotlink := '';
    end if;
    return new;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE brintest_expr (n int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brinopers" (
    "colname" "name",
    "typ" "text",
    "op" "text"[],
    "value" "text"[],
    "matches" integer[],
    CONSTRAINT "brinopers_check" CHECK (("cardinality"("op") = "cardinality"("value"))),
    CONSTRAINT "brinopers_check1" CHECK (("cardinality"("op") = "cardinality"("matches")))
)
;
----------
[CREATE_TABLE] CREATE TABLE someparent (a int)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE x_seq
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 int REFERENCES pktable)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_index
;
----------
[ADD_INDEX] create index minmaxtest1i on minmaxtest1(f1)
;
----------
[CREATE_CATALOG] CREATE DATABASE db0
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW s2.t1_mv AS SELECT * FROM s2.t1
;
----------
[CREATE_TABLE] -- OK with more than one equal column: constraint is a proper superset of partition key
create table idxpart (a int4range, b int4range) partition by range (a)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH PARSER tspars_case ("Start" = start_function)
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp() < $.datetime("HH:MI TZH"))'))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."infinite_interval_tbl" (
    "i" interval
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=15123 host=/tmp/sVwt2xKRMl dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_TABLE] CREATE TABLE t_009_tbl (id int, msg text)
;
----------
[ADD_INDEX] CREATE INDEX "iprt1_e_p2_ab2" ON "public"."prt1_e_p2" USING "btree" (((("a" + "b") / 2)))
;
----------
[CALL_PROG_OBJ] -- fail
CALL priv_testproc1(6)
;
----------
[CREATE_TABLE] CREATE TABLE tab_rf_x (x int primary key)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA s2
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_6 (a int,
	FOREIGN KEY (a) REFERENCES fk_partitioned_pk_6,
	FOREIGN KEY (a) REFERENCES fk_partitioned_pk_6
) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] alter table rewriteme
 add column onemore int default 0,
 add column another int default -1,
 alter column foo type numeric(10,4)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."alter_idx_rename_test_parted_2" (
    "a" integer
)
PARTITION BY LIST ("a")
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test4 INCREMENT BY -1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."sj"
    ADD CONSTRAINT "sj_a_key" UNIQUE ("a")
;
----------
[ADD_INDEX] CREATE INDEX idx_pgvector_items_ivfflat_cosine ON split_pgvector_probe.items USING ivfflat (embedding vector_cosine_ops) WITH (lists = 1)
;
----------
[ADMIN_TABLE] VACUUM (ONLY_DATABASE_STATS) vactst
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE gtest28a DROP COLUMN a
;
----------
[CREATE_TABLE] -- make sure data are properly de-toasted in BRIN index
CREATE TABLE brintest_3 (a text, b text, c text, d text)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION tg_hub_adjustslots(bpchar, integer, integer) IS 'function with args'
;
----------
[CREATE_TABLE] -- JSON_TABLE: plan execution

CREATE TEMP TABLE jsonb_table_test (js jsonb)
;
----------
[DROP_TYPE] drop domain ddef1 restrict
;
----------
[CREATE_TABLE] CREATE TABLE "public"."room" (
    "roomno" character(8),
    "comment" "text"
)
;
----------
[CREATE_TABLE] create table chi partition of middle for values from (1) to (5)
;
----------
[CREATE_TABLE] create table range_parted (a int, b int, c int) partition by range(a, b)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30954 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_ret_set_scalar"(integer, integer) RETURNS SETOF integer
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
	i int;
BEGIN
	FOR i IN $1 .. $2 LOOP
		RETURN NEXT i + 1;
	END LOOP;
	RETURN;
END;$_$
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_2.pg_temp_30214_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] --
-- Typanalyze function that returns false
--
CREATE FUNCTION int_custom_typanalyze_false(internal)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C STRICT
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trigtest_before_stmt BEFORE INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH STATEMENT
EXECUTE PROCEDURE dummy_trigger()
;
----------
[COMMENT_TYPE] COMMENT ON TYPE cube IS 'multi-dimensional cube ''(FLOAT-1, FLOAT-2, ..., FLOAT-N), (FLOAT-1, FLOAT-2, ..., FLOAT-N)'''
;
----------
[CREATE_TABLE] create table rtest_nothn3 (a int4, b text)
;
----------
[DROP_VIEW] drop view agg_view1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_replication_origin
;
----------
[ADD_INDEX] create index on mclparted (a)
;
