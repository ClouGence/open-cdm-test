CREATE FUNCTION "public"."logging_sfunc_nonstrict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE
    AS $_$ SELECT COALESCE($1, '') || '*' || quote_nullable($2) $_$
;
-- check that it fails on an unsupported relkind
create view vw as select 1
;
ALTER TABLE pg_temp_32.pg_temp_28153_2 ADD COLUMN newdata pg_temp_32.pg_temp_28153
;
CREATE INDEX "gtest22c_expr_idx" ON "public"."gtest22c" USING "btree" ((("b" * 3)))
;
--- test transforming from perl

CREATE FUNCTION perl2int(int) RETURNS bool
LANGUAGE plperl
TRANSFORM FOR TYPE bool
AS $$
return shift;
$$
;
CREATE TABLE dob_t1 (c1 int)
;
ALTER INDEX pg_toast.pg_toast_33112_index RENAME TO reind_con_toast_idx
;
-- This fails because we mixed up the column ordering
CREATE TEMP TABLE FKTABLE (ftest1 int, ftest2 inet)
;
ALTER INDEX pg_toast.pg_toast_30737_index RENAME TO reind_con_toast_idx
;
-- invalid: non-lowercase quoted identifiers
CREATE TEXT SEARCH DICTIONARY tsdict_case
(
	Template = ispell,
	"DictFile" = ispell_sample,
	"AffFile" = ispell_sample
)
;
CREATE TABLE fprt2_p2 (LIKE fprt2)
;
ALTER TABLE pg_toast.pg_toast_30522 RENAME TO reind_con_toast
;
CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE VIEW schema_not_existing.view AS SELECT 1
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE TABLE base_tbl2 (a int, b int, c text)
;
DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN UPDATE test2 SET x = x * 2 RETURNING x LOOP
        INSERT INTO test1 (a) VALUES (r.x);
        ROLLBACK;
    END LOOP;
END;
$$
;
ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE int COLLATE "C"
;
CREATE INDEX test1_a_hash ON test1 USING hash(a)
;
ANALYZE pg_temp_9.pg_temp_38079_2
;
-- range type doesn't fit

drop function polyf(x anycompatiblerange, y anycompatible, z anycompatible)
;
alter database abc with is_template true
;
-- If we add a password to the connstr it'll fail, because we don't allow passwords
-- in connstrs only in user mappings.

DO $d$
    BEGIN
        EXECUTE $$ALTER SERVER loopback_nopw OPTIONS (ADD password 'dummypw')$$;
    END;
$d$
;
DROP PROCEDURE testns.bar()
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29634 host=/tmp/ru_HfKRoh1 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_allinschema
;
-- can be pushed down
DROP TRIGGER trig_row_after_insert ON rem1
;
CREATE FUNCTION "public"."func_with_bad_set"() RETURNS integer
    LANGUAGE "sql"
    SET "default_text_search_config" TO 'no_such_config'
    AS $$ select 1 $$
;
CREATE TEMP TABLE pg_temp_9.pg_temp_38106_2 (tid pg_catalog.tid)
;
DROP VIEW fdv2
;
ANALYZE pg_temp_9.pg_temp_26570_2
;
ALTER TABLE attmp3 ADD CONSTRAINT IDENTITY check (b = boo(b)) NOT VALID
;
create function inoutparam_fail(inout i anyelement, out r anyrange)
  as $$ select $1, '[1,10]' $$ language sql
;
-- table rewrite should happen
ALTER TABLE t2 ADD COLUMN c domain3 default left(random()::text,3)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) information_schema.sql_implementation_info
;
CREATE TABLE "public"."pp1" (
    "f1" integer NOT NULL
)
;
ALTER TABLE ONLY "public"."idxpart_another_1"
    ADD CONSTRAINT "idxpart_another_1_pkey" PRIMARY KEY ("a", "b")
;
create table fkest1 (a int, b int, primary key(a,b))
;
-- error
CREATE TABLE gtest_normal_child (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
CREATE MATERIALIZED VIEW s3.t1_mv AS SELECT * FROM s3.t1
;
-- exercise city_budget type
CREATE TABLE city (
	name		name,
	location 	box,
	budget 		city_budget
)
;
--
-- tests for per-statement triggers
--

CREATE TABLE log_table (tstamp timestamp default timeofday()::timestamp)
;
create or replace function sc_test() returns setof integer as $$
declare
  c cursor for select * from generate_series(1, 10);
  x integer;
begin
  open c;
  loop
      move relative 2 in c;
      if not found then
          exit;
      end if;
      fetch next from c into x;
      if found then
          return next x;
      end if;
  end loop;
  close c;
end;
$$ language plpgsql
;
ANALYZE pg_catalog.pg_aggregate
;
CREATE FUNCTION isneq(ismn, ismn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- Create statement triggers on remote tables that "log" any INSERTs
-- performed on them.
CREATE TABLE cmdlog (cmd text)
;
CREATE SCHEMA "regress_indexing"
;
ALTER TABLE newly_indexed ADD extra int
;
-- Test run-time pruning using stable functions
create function list_part_fn(int) returns int as $$ begin return $1; end;$$ language plpgsql stable
;
CREATE TEXT SEARCH CONFIGURATION test_tsconfig_exists (COPY=english)
;
--
-- check for planner problems with complex inherited UPDATES
--

create table id (id serial primary key, name text)
;
DROP TABLE plt1_adv_p3
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."logging_sfunc_nonstrict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE
    AS $_$ SELECT COALESCE($1, '') || '*' || quote_nullable($2) $_$
;
----------
[CREATE_VIEW(SELECT)] -- check that it fails on an unsupported relkind
create view vw as select 1
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_32.pg_temp_28153_2 ADD COLUMN newdata pg_temp_32.pg_temp_28153
;
----------
[ADD_INDEX] CREATE INDEX "gtest22c_expr_idx" ON "public"."gtest22c" USING "btree" ((("b" * 3)))
;
----------
[CREATE_PROG_OBJ] --- test transforming from perl

CREATE FUNCTION perl2int(int) RETURNS bool
LANGUAGE plperl
TRANSFORM FOR TYPE bool
AS $$
return shift;
$$
;
----------
[CREATE_TABLE] CREATE TABLE dob_t1 (c1 int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33112_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- This fails because we mixed up the column ordering
CREATE TEMP TABLE FKTABLE (ftest1 int, ftest2 inet)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30737_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_POLICY] -- invalid: non-lowercase quoted identifiers
CREATE TEXT SEARCH DICTIONARY tsdict_case
(
	Template = ispell,
	"DictFile" = ispell_sample,
	"AffFile" = ispell_sample
)
;
----------
[CREATE_TABLE] CREATE TABLE fprt2_p2 (LIKE fprt2)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30522 RENAME TO reind_con_toast
;
----------
[CREATE_SCHEMA(SELECT)] CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE VIEW schema_not_existing.view AS SELECT 1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl2 (a int, b int, c text)
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN UPDATE test2 SET x = x * 2 RETURNING x LOOP
        INSERT INTO test1 (a) VALUES (r.x);
        ROLLBACK;
    END LOOP;
END;
$$
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE int COLLATE "C"
;
----------
[ADD_INDEX] CREATE INDEX test1_a_hash ON test1 USING hash(a)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38079_2
;
----------
[DROP_PROG_OBJ] -- range type doesn't fit

drop function polyf(x anycompatiblerange, y anycompatible, z anycompatible)
;
----------
[ALTER_CATALOG] alter database abc with is_template true
;
----------
[BLOCK] -- If we add a password to the connstr it'll fail, because we don't allow passwords
-- in connstrs only in user mappings.

DO $d$
    BEGIN
        EXECUTE $$ALTER SERVER loopback_nopw OPTIONS (ADD password 'dummypw')$$;
    END;
$d$
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE testns.bar()
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29634 host=/tmp/ru_HfKRoh1 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_allinschema
;
----------
[DROP_TRIGGER] -- can be pushed down
DROP TRIGGER trig_row_after_insert ON rem1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."func_with_bad_set"() RETURNS integer
    LANGUAGE "sql"
    SET "default_text_search_config" TO 'no_such_config'
    AS $$ select 1 $$
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_38106_2 (tid pg_catalog.tid)
;
----------
[DROP_VIEW] DROP VIEW fdv2
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26570_2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp3 ADD CONSTRAINT IDENTITY check (b = boo(b)) NOT VALID
;
----------
[CREATE_PROG_OBJ] create function inoutparam_fail(inout i anyelement, out r anyrange)
  as $$ select $1, '[1,10]' $$ language sql
;
----------
[ALTER_TABLE|ADD_COLUMN] -- table rewrite should happen
ALTER TABLE t2 ADD COLUMN c domain3 default left(random()::text,3)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) information_schema.sql_implementation_info
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pp1" (
    "f1" integer NOT NULL
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."idxpart_another_1"
    ADD CONSTRAINT "idxpart_another_1_pkey" PRIMARY KEY ("a", "b")
;
----------
[CREATE_TABLE] create table fkest1 (a int, b int, primary key(a,b))
;
----------
[CREATE_TABLE] -- error
CREATE TABLE gtest_normal_child (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW s3.t1_mv AS SELECT * FROM s3.t1
;
----------
[CREATE_TABLE] -- exercise city_budget type
CREATE TABLE city (
	name		name,
	location 	box,
	budget 		city_budget
)
;
----------
[CREATE_TABLE] --
-- tests for per-statement triggers
--

CREATE TABLE log_table (tstamp timestamp default timeofday()::timestamp)
;
----------
[CREATE_PROG_OBJ] create or replace function sc_test() returns setof integer as $$
declare
  c cursor for select * from generate_series(1, 10);
  x integer;
begin
  open c;
  loop
      move relative 2 in c;
      if not found then
          exit;
      end if;
      fetch next from c into x;
      if found then
          return next x;
      end if;
  end loop;
  close c;
end;
$$ language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_aggregate
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ismn, ismn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Create statement triggers on remote tables that "log" any INSERTs
-- performed on them.
CREATE TABLE cmdlog (cmd text)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "regress_indexing"
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE newly_indexed ADD extra int
;
----------
[CREATE_PROG_OBJ] -- Test run-time pruning using stable functions
create function list_part_fn(int) returns int as $$ begin return $1; end;$$ language plpgsql stable
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION test_tsconfig_exists (COPY=english)
;
----------
[CREATE_TABLE] --
-- check for planner problems with complex inherited UPDATES
--

create table id (id serial primary key, name text)
;
----------
[DROP_TABLE] DROP TABLE plt1_adv_p3
;
