ANALYZE pg_temp_4.pg_temp_26914_2
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_depend
;
drop aggregate newcnt (float4)
;
CREATE TABLE grandchild () INHERITS (child)
;
CREATE INDEX brin_test_a_idx ON brin_test USING brin (a) WITH (pages_per_range = 2)
;
AlTER EXTENSION pg_stat_statements UPDATE TO '1.11'
;
CREATE FUNCTION "public"."doubledecrement"("p1" "public"."pos_int") RETURNS "public"."pos_int"
    LANGUAGE "plpgsql"
    AS $$
declare v pos_int := 1;
begin
    v := p1 - 1;
    return v - 1;
end$$
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
CREATE FUNCTION isngt(ean13, issn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- alter type also works
drop domain testdomain3
;
ALTER SUBSCRIPTION regress_sub CONNECTION 'port=12755 host=/tmp/EDxB_oTMye dbname=postgres'
;
CREATE OPERATOR CLASS int4_custom_ops FOR TYPE int4 USING btree AS
	OPERATOR 1 < (int4, int4), OPERATOR 2 <= (int4, int4),
	OPERATOR 3 = (int4, int4), OPERATOR 4 >= (int4, int4),
	OPERATOR 5 > (int4, int4), FUNCTION 1 my_cmp(int4, int4)
;
ALTER INDEX "fkpart4"."droppk_pkey" ATTACH PARTITION "fkpart4"."droppk1_pkey"
;
CREATE PUBLICATION tap_pub_rep_full FOR TABLE test_replica_id_full
;
COMMENT ON TEXT SEARCH PARSER split_ts_probe.simple_parser IS 'parser probe'
;
CREATE FUNCTION int_custom_ge(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE "public"."notnull_tbl4_cld" (
    "a" integer CONSTRAINT "notnull_tbl4_a_not_null" NOT NULL
)
;
CREATE TABLE test_mix_3 (a int PRIMARY KEY, b int, c int)
;
create operator class alter1.ctype_hash_ops default for type alter1.ctype using hash as
  operator 1 alter1.=(alter1.ctype, alter1.ctype)
;
DROP VIEW jsonb_table_view7
;
CREATE FOREIGN TABLE agg_csv (
	a	int2,
	b	float4
) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/agg.csv', header 'true', delimiter ';', quote '@', escape '"', null '')
;
--
-- Tests for CREATE OR REPLACE TRIGGER
--
create table my_table (id integer)
;
DROP TABLE gtest23a
;
CREATE INDEX concur_reindex_part_index ON ONLY concur_reindex_part (c1)
;
CREATE TABLE notnull_part1_upg (a int, b int) PARTITION BY LIST (a)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
/* End test case for bug #18970 */

-- Test that ALTER TABLE rewrite preserves a clustered index
-- for normal indexes and indexes on constraints.
create table alttype_cluster (a int)
;
DROP DATABASE regression_invalid_interrupt
;
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_unique_defer
;
CREATE TABLE "public"."pagg_tab_ml_p3_s1" (
    "c" "text",
    "a" integer,
    "b" integer
)
;
-- ...and doesn't when the partition is detached along with its own partition
alter table target_parted detach partition attach_parted
;
alter table alterlock cluster on alterlock_pkey
;
ALTER TABLE pg_proc ADD UNIQUE USING INDEX pg_proc_proname_args_nsp_index
;
-- also OK
CREATE TEMP TABLE pg_temp.doubly_temp (a int primary key)
;
CREATE TABLE LINE_TBL (s line)
;
ALTER TABLE itest18 SET UNLOGGED
;
alter extension postgres_fdw drop operator class my_op_class using btree
;
-- Extract bucket and batch counts from an explain analyze plan.  In
-- general we can't make assertions about how many batches (or
-- buckets) will be required because it can vary, but we can in some
-- special cases and we can check for growth.
create or replace function find_hash(node json)
returns json language plpgsql
as
$$
declare
  x json;
  child json;
begin
  if node->>'Node Type' = 'Hash' then
    return node;
  else
    for child in select json_array_elements(node->'Plans')
    loop
      x := find_hash(child);
      if x is not null then
        return x;
      end if;
    end loop;
    return null;
  end if;
end;
$$
;
TRUNCATE t1
;
create table covidxpart (a int, b int) partition by list (a)
;
--
-- Materialized views
--

CREATE MATERIALIZED VIEW ddl_deparse_mv AS
  SELECT * FROM datatype_table LIMIT 1 WITH NO DATA
;
ALTER TABLE tab5_1 ADD COLUMN b int
;
CALL test_proc3($1)
;
CREATE TABLE test_primary_constraints(id int PRIMARY KEY)
;
DROP TABLE brintest_2
;
-- Make the same tests with domains over the array and composite fields

create domain insert_pos_ints as int[] check (value[1] > 0)
;
-- nor to int4
ALTER TABLE fktable ADD CONSTRAINT fk_2_1
FOREIGN KEY (x2) REFERENCES pktable(id1)
;
DROP TABLE room_reservation
;
create table at_test_sql_partop_1 (a int)
;
-- pg_prewarm() should fail if the target relation has no storage.
CREATE TABLE test (c1 int) PARTITION BY RANGE (c1)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_26914_2
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_depend
;
----------
[DROP_PROG_OBJ] drop aggregate newcnt (float4)
;
----------
[CREATE_TABLE] CREATE TABLE grandchild () INHERITS (child)
;
----------
[ADD_INDEX] CREATE INDEX brin_test_a_idx ON brin_test USING brin (a) WITH (pages_per_range = 2)
;
----------
[ALTER_LIBRARY] AlTER EXTENSION pg_stat_statements UPDATE TO '1.11'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."doubledecrement"("p1" "public"."pos_int") RETURNS "public"."pos_int"
    LANGUAGE "plpgsql"
    AS $$
declare v pos_int := 1;
begin
    v := p1 - 1;
    return v - 1;
end$$
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ean13, issn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TYPE] -- alter type also works
drop domain testdomain3
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_sub CONNECTION 'port=12755 host=/tmp/EDxB_oTMye dbname=postgres'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS int4_custom_ops FOR TYPE int4 USING btree AS
	OPERATOR 1 < (int4, int4), OPERATOR 2 <= (int4, int4),
	OPERATOR 3 = (int4, int4), OPERATOR 4 >= (int4, int4),
	OPERATOR 5 > (int4, int4), FUNCTION 1 my_cmp(int4, int4)
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart4"."droppk_pkey" ATTACH PARTITION "fkpart4"."droppk1_pkey"
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_rep_full FOR TABLE test_replica_id_full
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH PARSER split_ts_probe.simple_parser IS 'parser probe'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_ge(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl4_cld" (
    "a" integer CONSTRAINT "notnull_tbl4_a_not_null" NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE test_mix_3 (a int PRIMARY KEY, b int, c int)
;
----------
[CREATE_PROG_OBJ] create operator class alter1.ctype_hash_ops default for type alter1.ctype using hash as
  operator 1 alter1.=(alter1.ctype, alter1.ctype)
;
----------
[DROP_VIEW] DROP VIEW jsonb_table_view7
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE agg_csv (
	a	int2,
	b	float4
) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/agg.csv', header 'true', delimiter ';', quote '@', escape '"', null '')
;
----------
[CREATE_TABLE] --
-- Tests for CREATE OR REPLACE TRIGGER
--
create table my_table (id integer)
;
----------
[DROP_TABLE] DROP TABLE gtest23a
;
----------
[ADD_INDEX] CREATE INDEX concur_reindex_part_index ON ONLY concur_reindex_part (c1)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_part1_upg (a int, b int) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
----------
[CREATE_TABLE] /* End test case for bug #18970 */

-- Test that ALTER TABLE rewrite preserves a clustered index
-- for normal indexes and indexes on constraints.
create table alttype_cluster (a int)
;
----------
[DROP_CATALOG] DROP DATABASE regression_invalid_interrupt
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_unique_defer
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_ml_p3_s1" (
    "c" "text",
    "a" integer,
    "b" integer
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- ...and doesn't when the partition is detached along with its own partition
alter table target_parted detach partition attach_parted
;
----------
[ALTER_TABLE] alter table alterlock cluster on alterlock_pkey
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_proc ADD UNIQUE USING INDEX pg_proc_proname_args_nsp_index
;
----------
[CREATE_TABLE] -- also OK
CREATE TEMP TABLE pg_temp.doubly_temp (a int primary key)
;
----------
[CREATE_TABLE] CREATE TABLE LINE_TBL (s line)
;
----------
[ALTER_TABLE] ALTER TABLE itest18 SET UNLOGGED
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw drop operator class my_op_class using btree
;
----------
[CREATE_PROG_OBJ] -- Extract bucket and batch counts from an explain analyze plan.  In
-- general we can't make assertions about how many batches (or
-- buckets) will be required because it can vary, but we can in some
-- special cases and we can check for growth.
create or replace function find_hash(node json)
returns json language plpgsql
as
$$
declare
  x json;
  child json;
begin
  if node->>'Node Type' = 'Hash' then
    return node;
  else
    for child in select json_array_elements(node->'Plans')
    loop
      x := find_hash(child);
      if x is not null then
        return x;
      end if;
    end loop;
    return null;
  end if;
end;
$$
;
----------
[TRUNCATE_TABLE] TRUNCATE t1
;
----------
[CREATE_TABLE] create table covidxpart (a int, b int) partition by list (a)
;
----------
[CREATE_VIEW(SELECT)] --
-- Materialized views
--

CREATE MATERIALIZED VIEW ddl_deparse_mv AS
  SELECT * FROM datatype_table LIMIT 1 WITH NO DATA
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tab5_1 ADD COLUMN b int
;
----------
[CALL_PROG_OBJ] CALL test_proc3($1)
;
----------
[CREATE_TABLE] CREATE TABLE test_primary_constraints(id int PRIMARY KEY)
;
----------
[DROP_TABLE] DROP TABLE brintest_2
;
----------
[CREATE_TYPE] -- Make the same tests with domains over the array and composite fields

create domain insert_pos_ints as int[] check (value[1] > 0)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- nor to int4
ALTER TABLE fktable ADD CONSTRAINT fk_2_1
FOREIGN KEY (x2) REFERENCES pktable(id1)
;
----------
[DROP_TABLE] DROP TABLE room_reservation
;
----------
[CREATE_TABLE] create table at_test_sql_partop_1 (a int)
;
----------
[CREATE_TABLE] -- pg_prewarm() should fail if the target relation has no storage.
CREATE TABLE test (c1 int) PARTITION BY RANGE (c1)
;
