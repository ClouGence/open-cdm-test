CREATE TABLE fs_target (a int, b int, c text)
  WITH (autovacuum_enabled=off)
;
CREATE FOREIGN TABLE ft5 (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text
) SERVER loopback OPTIONS (schema_name 'S 1', table_name 'T 4')
;
ALTER TABLE T ALTER COLUMN c_text SET DEFAULT 'world',
              ALTER COLUMN c_int  SET DEFAULT 1
;
drop function sql_is_distinct_from(anyelement, anyelement)
;
alter table defcheck_def drop c
;
COMMENT ON COLUMN ctlt3.c IS 'C'
;
CREATE TABLE inh_parent (a int)
;
CREATE TABLE txn1(id int4 PRIMARY KEY, val TEXT)
;
alter table gs_data_1 set (autovacuum_enabled = 'false')
;
alter view my_locks set (autovacuum_enabled = false)
;
CREATE FUNCTION gin_extract_query_trgm(text, internal, int2, internal, internal, internal, internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE fktable ADD CONSTRAINT fk_2_3
FOREIGN KEY (x2) REFERENCES pktable(id3)
;
DROP TABLE pg_temp_9.pg_temp_38133_2, pg_temp_9.pg_temp_38133
;
CREATE AGGREGATE priv_testagg1a(priv_testdomain1) (sfunc = int4_sum, stype = bigint)
;
CREATE OPERATOR <% (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = word_similarity_op,
        COMMUTATOR = '%>',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
CREATE OPERATOR CLASS bit_ops
DEFAULT FOR TYPE bit USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       bitcmp(bit,bit),
    FUNCTION        2       gin_extract_value_bit(bit, internal),
    FUNCTION        3       gin_extract_query_bit(bit, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_bit(bit,bit,int2, internal),
STORAGE         bit
;
ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk NOT VALID
;
VACUUM (FREEZE) pg_catalog.pg_default_acl
;
CREATE VIEW pg_stat_checkpointer AS
    SELECT
        pg_stat_get_checkpointer_num_timed() AS num_timed,
        pg_stat_get_checkpointer_num_requested() AS num_requested,
        pg_stat_get_checkpointer_num_performed() AS num_done,
        pg_stat_get_checkpointer_restartpoints_timed() AS restartpoints_timed,
        pg_stat_get_checkpointer_restartpoints_requested() AS restartpoints_req,
        pg_stat_get_checkpointer_restartpoints_performed() AS restartpoints_done,
        pg_stat_get_checkpointer_write_time() AS write_time,
        pg_stat_get_checkpointer_sync_time() AS sync_time,
        pg_stat_get_checkpointer_buffers_written() AS buffers_written,
        pg_stat_get_checkpointer_slru_written() AS slru_written,
        pg_stat_get_checkpointer_stat_reset_time() AS stats_reset
;
create table child_30_35 partition of child_30_40
  for values from (30) to (35)
;
create table iboolpart_f partition of iboolpart for values in ('true')
;
-- Remove indexes from the partitioned table and its partitions
DROP INDEX pht1_c_idx CASCADE
;
-- check that leaf partitions are scanned when attaching a partitioned
-- table
CREATE TABLE part_5 (
	LIKE list_parted2
) PARTITION BY LIST (b)
;
create unique index capitals_names_unique on capitals (name)
;
CREATE TABLESPACE regress_ts4 LOCATION ''
;
CREATE TABLE foo (a int PRIMARY KEY, b text)
;
CREATE FUNCTION gin_extract_query_int2(int2, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
vacuum ��Ƒ�㑻�������
;
DROP SCHEMA IF EXISTS split_sequence_next CASCADE
;
ALTER OPERATOR FAMILY gist_time_ops USING gist ADD
	OPERATOR	6	<> (time, time) ,
	OPERATOR	15	<-> (time, time) FOR ORDER BY pg_catalog.interval_ops ,
	FUNCTION	8 (time, time) gbt_time_distance (internal, time, int2, oid, internal) ,
	FUNCTION	9 (time, time) gbt_time_fetch (internal)
;
-- array exists - array elements should behave as keys (for GIN index scans too)
CREATE INDEX jidx_array ON testjsonb USING gin((j->'array'))
;
CREATE FUNCTION gbt_inet_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- this revealed a different bug
CREATE FUNCTION testTextToJsonbObject(text) RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
my $x = shift;
return {a => $x};
$$
;
ALTER TABLE fkpart11.fk DROP CONSTRAINT fkey
;
ALTER FOREIGN TABLE ft2 OPTIONS (schema_name 'S 1', table_name 'T 1')
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest24ata"
    ADD CONSTRAINT "gtest24ata_pkey" PRIMARY KEY ("a")
;
VACUUM (FREEZE) vacparted_i
;
create event trigger regress_event_trigger_end on ddl_command_end
   execute function test_event_trigger()
;
DROP TRIGGER footrg_ondel ON footrg1
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_largeobject_metadata
;
ALTER INDEX pg_toast.pg_toast_30773_index RENAME TO reind_con_toast_idx
;
CREATE PUBLICATION testpib_ins_trunct WITH (publish = insert)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_statistic_ext_data
;
alter table atacc1 add constraint atacc1_constr_or check(test_a is not null or test_b < 10)
;
ALTER INDEX pg_toast.pg_toast_27567_index RENAME TO reind_con_toast_idx
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_opclass
;
-- error: not a function
ALTER PROCEDURE cp_testfunc1(int) VOLATILE
;
create index on t_gin_test_tbl using gin (i, j)
;
create domain mydomain as int4
;
CREATE TEMP TABLE parted_b (id int PRIMARY KEY) partition by range(id)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE fs_target (a int, b int, c text)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft5 (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text
) SERVER loopback OPTIONS (schema_name 'S 1', table_name 'T 4')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE T ALTER COLUMN c_text SET DEFAULT 'world',
              ALTER COLUMN c_int  SET DEFAULT 1
;
----------
[DROP_PROG_OBJ] drop function sql_is_distinct_from(anyelement, anyelement)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table defcheck_def drop c
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN ctlt3.c IS 'C'
;
----------
[CREATE_TABLE] CREATE TABLE inh_parent (a int)
;
----------
[CREATE_TABLE] CREATE TABLE txn1(id int4 PRIMARY KEY, val TEXT)
;
----------
[ALTER_TABLE] alter table gs_data_1 set (autovacuum_enabled = 'false')
;
----------
[ALTER_VIEW] alter view my_locks set (autovacuum_enabled = false)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_trgm(text, internal, int2, internal, internal, internal, internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_2_3
FOREIGN KEY (x2) REFERENCES pktable(id3)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_38133_2, pg_temp_9.pg_temp_38133
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE priv_testagg1a(priv_testdomain1) (sfunc = int4_sum, stype = bigint)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <% (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = word_similarity_op,
        COMMUTATOR = '%>',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS bit_ops
DEFAULT FOR TYPE bit USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       bitcmp(bit,bit),
    FUNCTION        2       gin_extract_value_bit(bit, internal),
    FUNCTION        3       gin_extract_query_bit(bit, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_bit(bit,bit,int2, internal),
STORAGE         bit
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk NOT VALID
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_default_acl
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_checkpointer AS
    SELECT
        pg_stat_get_checkpointer_num_timed() AS num_timed,
        pg_stat_get_checkpointer_num_requested() AS num_requested,
        pg_stat_get_checkpointer_num_performed() AS num_done,
        pg_stat_get_checkpointer_restartpoints_timed() AS restartpoints_timed,
        pg_stat_get_checkpointer_restartpoints_requested() AS restartpoints_req,
        pg_stat_get_checkpointer_restartpoints_performed() AS restartpoints_done,
        pg_stat_get_checkpointer_write_time() AS write_time,
        pg_stat_get_checkpointer_sync_time() AS sync_time,
        pg_stat_get_checkpointer_buffers_written() AS buffers_written,
        pg_stat_get_checkpointer_slru_written() AS slru_written,
        pg_stat_get_checkpointer_stat_reset_time() AS stats_reset
;
----------
[CREATE_TABLE] create table child_30_35 partition of child_30_40
  for values from (30) to (35)
;
----------
[CREATE_TABLE] create table iboolpart_f partition of iboolpart for values in ('true')
;
----------
[DROP_INDEX] -- Remove indexes from the partitioned table and its partitions
DROP INDEX pht1_c_idx CASCADE
;
----------
[CREATE_TABLE] -- check that leaf partitions are scanned when attaching a partitioned
-- table
CREATE TABLE part_5 (
	LIKE list_parted2
) PARTITION BY LIST (b)
;
----------
[ADD_INDEX] create unique index capitals_names_unique on capitals (name)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_ts4 LOCATION ''
;
----------
[CREATE_TABLE] CREATE TABLE foo (a int PRIMARY KEY, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_int2(int2, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] vacuum ��Ƒ�㑻�������
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_sequence_next CASCADE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_time_ops USING gist ADD
	OPERATOR	6	<> (time, time) ,
	OPERATOR	15	<-> (time, time) FOR ORDER BY pg_catalog.interval_ops ,
	FUNCTION	8 (time, time) gbt_time_distance (internal, time, int2, oid, internal) ,
	FUNCTION	9 (time, time) gbt_time_fetch (internal)
;
----------
[ADD_INDEX] -- array exists - array elements should behave as keys (for GIN index scans too)
CREATE INDEX jidx_array ON testjsonb USING gin((j->'array'))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_inet_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] -- this revealed a different bug
CREATE FUNCTION testTextToJsonbObject(text) RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
my $x = shift;
return {a => $x};
$$
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE fkpart11.fk DROP CONSTRAINT fkey
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE ft2 OPTIONS (schema_name 'S 1', table_name 'T 1')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest24ata"
    ADD CONSTRAINT "gtest24ata_pkey" PRIMARY KEY ("a")
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) vacparted_i
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger_end on ddl_command_end
   execute function test_event_trigger()
;
----------
[DROP_TRIGGER] DROP TRIGGER footrg_ondel ON footrg1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_largeobject_metadata
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30773_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpib_ins_trunct WITH (publish = insert)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_statistic_ext_data
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc1_constr_or check(test_a is not null or test_b < 10)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27567_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_opclass
;
----------
[ALTER_PROG_OBJ] -- error: not a function
ALTER PROCEDURE cp_testfunc1(int) VOLATILE
;
----------
[ADD_INDEX] create index on t_gin_test_tbl using gin (i, j)
;
----------
[CREATE_TYPE] create domain mydomain as int4
;
----------
[CREATE_TABLE] CREATE TEMP TABLE parted_b (id int PRIMARY KEY) partition by range(id)
;
