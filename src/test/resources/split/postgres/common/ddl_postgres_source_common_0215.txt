CREATE EXTENSION jsonb_plperlu CASCADE
;
CREATE TABLE pitest1_pfail (f1 date NOT NULL, f2 text, f3 bigint GENERATED ALWAYS AS IDENTITY)
;
-- Set up some simple test tables

CREATE TABLE SUBSELECT_TBL (
  f1 integer,
  f2 integer,
  f3 float
)
;
--
-- test partial btree indexes
--
-- As of 7.2, planner probably won't pick an indexscan without stats,
-- so ANALYZE first.  Also, we want to prevent it from picking a bitmapscan
-- followed by sort, because that could hide index ordering problems.
--
ANALYZE onek2
;
drop table inh_pp1, inh_cc1, inh_cc2
;
DROP PUBLICATION testpub5
;
DROP TABLE src, tgt
;
CREATE TYPE employee_type AS (name TEXT, salary NUMERIC)
;
CREATE INDEX t_b_partial_1_idx ON bitmap_split_or (b) WHERE a = 1
;
DROP TABLE atestp2
;
create table list_parted (
	a text,
	b int
) partition by list (lower(a))
;
-- collation override in plpgsql

CREATE FUNCTION mylt2 (x text, y text) RETURNS boolean LANGUAGE plpgsql AS $$
declare
  xx text := x;
  yy text := y;
begin
  return xx < yy;
end
$$
;
CREATE TABLE transition_table_status
(
      level int NOT NULL,
      node_no int NOT NULL,
      status int,
       PRIMARY KEY (level, node_no)
) WITHOUT OIDS
;
CREATE TABLE pagg_tab2_p2 PARTITION OF pagg_tab2 FOR VALUES FROM (10) TO (20)
;
CREATE PROCEDURE test_proc8b(INOUT a int, INOUT b int, INOUT c int)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %, c: %', a, b, c;
  a := a * 10;
  b := b + 10;
  c := c * -10;
END;
$$
;
-- remainder must be greater than or equal to zero and less than modulus
CREATE TABLE fail_part PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 8, REMAINDER 8)
;
drop function anyarray_anymultirange_func(anyarray, anymultirange)
;
/*
 * text search configuration for hindi language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hindi and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
CREATE TABLE DEFAULT_TBL (i int DEFAULT 100,
	x text DEFAULT 'vadim', f float8 DEFAULT 123.456)
;
ANALYZE pg_temp_12.pg_temp_26105
;
-- Test some other non-btree index types
create table idxpart (a int, b text, c int[]) partition by range (a)
;
-- ok
ALTER INDEX dummy_test_idx SET (option_string_val = true)
;
ALTER TABLE tx3 DROP COLUMN d
;
CREATE FUNCTION leak(integer,integer) RETURNS boolean
  AS 'int4lt'
  LANGUAGE internal IMMUTABLE STRICT
;
ALTER TABLE pg_toast.pg_toast_30877 RENAME TO reind_con_toast
;
CREATE SEQUENCE stats_import.testseq
;
DROP TABLE IF EXISTS d_listp, d_listp2, d_listp_foobar
;
ANALYZE public.shoelace_data
;
CREATE TABLE "public"."textrange_test" (
    "tr" "public"."textrange"
)
;
CREATE FUNCTION event_trigger_dummy_trigger()
 RETURNS trigger
 LANGUAGE plpgsql
AS $$
BEGIN
    RETURN new;
END; $$
;
DROP TABLE bigintp
;
ALTER TYPE "public"."insenum" ADD VALUE 'i14'
;
CALL ptest6(1, 2)
;
CREATE OPERATOR CLASS varchar_ops
DEFAULT FOR TYPE varchar USING gin
AS
    OPERATOR        1       <(text,text),
    OPERATOR        2       <=(text,text),
    OPERATOR        3       =(text,text),
    OPERATOR        4       >=(text,text),
    OPERATOR        5       >(text,text),
    FUNCTION        1       bttextcmp(text,text),
    FUNCTION        2       gin_extract_value_text(text, internal),
    FUNCTION        3       gin_extract_query_text(text, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_text(text,text,int2, internal),
STORAGE         varchar
;
CREATE CAST (text AS citext)    WITHOUT FUNCTION AS ASSIGNMENT
;
-- truncate a partition cascading to a partitioned table
CREATE TABLE ref_c (
    c INT PRIMARY KEY,
    a INT REFERENCES trunc_a(a) ON DELETE CASCADE
) PARTITION BY RANGE (c)
;
ALTER TABLE list_parted2 ATTACH PARTITION part_2 FOR VALUES IN (2)
;
CREATE TABLE split_cov.parent_table (id integer PRIMARY KEY, code text UNIQUE)
;
/* src/test/modules/worker_spi/worker_spi--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION worker_spi_launch(pg_catalog.int4)
RETURNS pg_catalog.int4 STRICT
AS '$libdir/worker_spi'
LANGUAGE C
;
-- Test pg_prewarm extension
CREATE EXTENSION pg_prewarm
;
CREATE PUBLICATION regress_test_pub
;
ANALYZE public.gtestxx_2
;
-- ISBN vs other types:
CREATE FUNCTION btisbncmp(isbn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE INDEX byteaidx ON byteatmp USING GIST ( a )
;
DROP TABLE mchash
;
CREATE FUNCTION int_custom_le(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
alter domain dcomptype drop constraint dcomptype_check
;
ALTER TABLE range_parted ENABLE ROW LEVEL SECURITY
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_enum
;
CREATE TRIGGER c1_update_trig
  AFTER UPDATE ON part_c_1_100 for each statement execute procedure trigfunc()
;
------------------------------------------------------------------------------------------
[CREATE_LIBRARY] CREATE EXTENSION jsonb_plperlu CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE pitest1_pfail (f1 date NOT NULL, f2 text, f3 bigint GENERATED ALWAYS AS IDENTITY)
;
----------
[CREATE_TABLE] -- Set up some simple test tables

CREATE TABLE SUBSELECT_TBL (
  f1 integer,
  f2 integer,
  f3 float
)
;
----------
[ADMIN_TABLE] --
-- test partial btree indexes
--
-- As of 7.2, planner probably won't pick an indexscan without stats,
-- so ANALYZE first.  Also, we want to prevent it from picking a bitmapscan
-- followed by sort, because that could hide index ordering problems.
--
ANALYZE onek2
;
----------
[DROP_TABLE] drop table inh_pp1, inh_cc1, inh_cc2
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub5
;
----------
[DROP_TABLE] DROP TABLE src, tgt
;
----------
[CREATE_TYPE] CREATE TYPE employee_type AS (name TEXT, salary NUMERIC)
;
----------
[ADD_INDEX] CREATE INDEX t_b_partial_1_idx ON bitmap_split_or (b) WHERE a = 1
;
----------
[DROP_TABLE] DROP TABLE atestp2
;
----------
[CREATE_TABLE] create table list_parted (
	a text,
	b int
) partition by list (lower(a))
;
----------
[CREATE_PROG_OBJ] -- collation override in plpgsql

CREATE FUNCTION mylt2 (x text, y text) RETURNS boolean LANGUAGE plpgsql AS $$
declare
  xx text := x;
  yy text := y;
begin
  return xx < yy;
end
$$
;
----------
[CREATE_TABLE] CREATE TABLE transition_table_status
(
      level int NOT NULL,
      node_no int NOT NULL,
      status int,
       PRIMARY KEY (level, node_no)
) WITHOUT OIDS
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab2_p2 PARTITION OF pagg_tab2 FOR VALUES FROM (10) TO (20)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc8b(INOUT a int, INOUT b int, INOUT c int)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %, c: %', a, b, c;
  a := a * 10;
  b := b + 10;
  c := c * -10;
END;
$$
;
----------
[CREATE_TABLE] -- remainder must be greater than or equal to zero and less than modulus
CREATE TABLE fail_part PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 8, REMAINDER 8)
;
----------
[DROP_PROG_OBJ] drop function anyarray_anymultirange_func(anyarray, anymultirange)
;
----------
[CREATE_POLICY] /*
 * text search configuration for hindi language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hindi and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
----------
[CREATE_TABLE] CREATE TABLE DEFAULT_TBL (i int DEFAULT 100,
	x text DEFAULT 'vadim', f float8 DEFAULT 123.456)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26105
;
----------
[CREATE_TABLE] -- Test some other non-btree index types
create table idxpart (a int, b text, c int[]) partition by range (a)
;
----------
[ALTER_INDEX] -- ok
ALTER INDEX dummy_test_idx SET (option_string_val = true)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tx3 DROP COLUMN d
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION leak(integer,integer) RETURNS boolean
  AS 'int4lt'
  LANGUAGE internal IMMUTABLE STRICT
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30877 RENAME TO reind_con_toast
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE stats_import.testseq
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS d_listp, d_listp2, d_listp_foobar
;
----------
[ADMIN_TABLE] ANALYZE public.shoelace_data
;
----------
[CREATE_TABLE] CREATE TABLE "public"."textrange_test" (
    "tr" "public"."textrange"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION event_trigger_dummy_trigger()
 RETURNS trigger
 LANGUAGE plpgsql
AS $$
BEGIN
    RETURN new;
END; $$
;
----------
[DROP_TABLE] DROP TABLE bigintp
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i14'
;
----------
[CALL_PROG_OBJ] CALL ptest6(1, 2)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS varchar_ops
DEFAULT FOR TYPE varchar USING gin
AS
    OPERATOR        1       <(text,text),
    OPERATOR        2       <=(text,text),
    OPERATOR        3       =(text,text),
    OPERATOR        4       >=(text,text),
    OPERATOR        5       >(text,text),
    FUNCTION        1       bttextcmp(text,text),
    FUNCTION        2       gin_extract_value_text(text, internal),
    FUNCTION        3       gin_extract_query_text(text, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_text(text,text,int2, internal),
STORAGE         varchar
;
----------
[CREATE_PROG_OBJ] CREATE CAST (text AS citext)    WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[CREATE_TABLE] -- truncate a partition cascading to a partitioned table
CREATE TABLE ref_c (
    c INT PRIMARY KEY,
    a INT REFERENCES trunc_a(a) ON DELETE CASCADE
) PARTITION BY RANGE (c)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 ATTACH PARTITION part_2 FOR VALUES IN (2)
;
----------
[CREATE_TABLE] CREATE TABLE split_cov.parent_table (id integer PRIMARY KEY, code text UNIQUE)
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/worker_spi/worker_spi--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION worker_spi_launch(pg_catalog.int4)
RETURNS pg_catalog.int4 STRICT
AS '$libdir/worker_spi'
LANGUAGE C
;
----------
[CREATE_LIBRARY] -- Test pg_prewarm extension
CREATE EXTENSION pg_prewarm
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION regress_test_pub
;
----------
[ADMIN_TABLE] ANALYZE public.gtestxx_2
;
----------
[CREATE_PROG_OBJ] -- ISBN vs other types:
CREATE FUNCTION btisbncmp(isbn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX byteaidx ON byteatmp USING GIST ( a )
;
----------
[DROP_TABLE] DROP TABLE mchash
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_le(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TYPE] alter domain dcomptype drop constraint dcomptype_check
;
----------
[ALTER_TABLE] ALTER TABLE range_parted ENABLE ROW LEVEL SECURITY
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_enum
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER c1_update_trig
  AFTER UPDATE ON part_c_1_100 for each statement execute procedure trigfunc()
;
