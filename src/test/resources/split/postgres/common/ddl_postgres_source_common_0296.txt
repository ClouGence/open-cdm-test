DROP TABLE lock_tbl2
;
CREATE INDEX fooindex ON foo (f1 desc, f2 asc, f3 nulls first, f4 nulls last)
;
CREATE TYPE casttesttype (
   internallength = variable,
   input = casttesttype_in,
   output = casttesttype_out,
   alignment = int4
)
;
CREATE TABLE gtest26 (
    a int PRIMARY KEY,
    b int GENERATED ALWAYS AS (a * 2) STORED
)
;
drop table event_trigger_fire2, event_trigger_fire3, event_trigger_fire4, event_trigger_fire5, event_trigger_fire6, event_trigger_fire7
;
create temp table temp_inh_oncommit_test (a int) on commit delete rows
;
analyze matest1
;
ALTER PUBLICATION split_pub_cols RENAME TO split_pub_renamed
;
ALTER TABLE pg_toast.pg_toast_28560 RENAME TO reind_con_toast
;
-- Check that we map relation oids to filenodes and back correctly.  Only
-- display bad mappings so the test output doesn't change all the time.  A
-- filenode function call can return NULL for a relation dropped concurrently
-- with the call's surrounding query, so ignore a NULL mapped_oid for
-- relations that no longer exist after all calls finish.
CREATE TEMP TABLE filenode_mapping AS
SELECT
    oid, mapped_oid, reltablespace, relfilenode, relname
FROM pg_class,
    pg_filenode_relation(reltablespace, pg_relation_filenode(oid)) AS mapped_oid
WHERE relkind IN ('r', 'i', 'S', 't', 'm') AND mapped_oid IS DISTINCT FROM oid
;
CREATE OPERATOR ## (
   leftarg = path,
   rightarg = path,
   function = path_inter,
   commutator = ##
)
;
DO $$
	BEGIN
		WHILE NOT EXISTS(SELECT * FROM pg_locks WHERE NOT granted AND relation = 'pg_tablespace'::regclass AND mode = 'AccessShareLock') LOOP
			PERFORM pg_sleep(.1);
		END LOOP;
	END$$
;
create table retain_test(a int)
;
CREATE TABLE xmldata(data xml)
;
DROP TABLE plt1_adv
;
ALTER INDEX pg_toast.pg_toast_30909_index RENAME TO reind_con_toast_idx
;
ANALYZE pg_temp_12.pg_temp_26420
;
CREATE EXTENSION amcheck WITH SCHEMA amcheck_schema
;
-- error from unique_idx2

DROP INDEX unique_idx1, unique_idx2
;
create or replace function array_elem_check(numeric) returns numeric as $$
declare
  x mynums;
begin
  x[1] := $1;
  return x[1];
end$$ language plpgsql
;
-- no-op, same schema
alter type alter1.ctype set schema alter2
;
-- Test default partitions
ALTER TABLE plt1_adv DETACH PARTITION plt1_adv_p1
;
/* src/test/modules/test_extensions/test_ext7--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- link some existing serial-owning table to the extension
alter extension test_ext7 add table old_table1
;
-- Invalid batch_size option
CREATE FOREIGN TABLE inv_bsz (c1 int )
	SERVER loopback OPTIONS (batch_size '100$%$#$#')
;
drop function dfunc(varchar, numeric)
;
DO $$
DECLARE
	objtype text;
BEGIN
	FOR objtype IN VALUES ('toast table'), ('index column'), ('sequence column'),
		('toast table column'), ('view column'), ('materialized view column')
	LOOP
		BEGIN
			PERFORM pg_get_object_address(objtype, '{one}', '{}');
		EXCEPTION WHEN invalid_parameter_value THEN
			RAISE WARNING 'error for %: %', objtype, sqlerrm;
		END;
	END LOOP;
END;
$$
;
-- This table is intentionally left around to exercise pg_dump/pg_upgrade
CREATE TABLE bit_defaults(
  b1 bit(4) DEFAULT '1001',
  b2 bit(4) DEFAULT B'0101',
  b3 bit varying(5) DEFAULT '1001',
  b4 bit varying(5) DEFAULT B'0101'
)
;
alter table atacc1 alter bar set not null
;
ALTER TABLE ONLY "public"."pht1_e" ATTACH PARTITION "public"."pht1_e_p3" FOR VALUES WITH (modulus 3, remainder 2)
;
CALL transaction_test7()
;
-- Multiple relations should use their options in isolation.
VACUUM vac_tab_on_toast_off, vac_tab_off_toast_on
;
--
-- Test keys that "look" different but compare as equal
--
create table pktable2 (a float8, b float8, primary key (a, b))
;
DROP OPERATOR FAMILY alt_opf7 USING btree
;
CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C_UTF8')
;
create function return_unnamed_refcursor() returns refcursor as $$
declare
    rc refcursor;
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
alter extension postgres_fdw add operator public.>^(int, int)
;
DROP VIEW json_table_view8, json_table_view9
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_shdepend
;
DROP TABLE pg_temp_11.pg_temp_41153_2, pg_temp_11.pg_temp_41153
;
ANALYZE pg_temp_11.pg_temp_41207_2
;
--  this should work, however:
CREATE TABLE error_tbl (b1 bool DEFAULT (1 IN (1, 2)))
;
CREATE TABLE trgfire5_40 PARTITION OF trgfire5 FOR VALUES IN (40)
;
CREATE TABLE stats_track_tab (x int)
;
CREATE OPERATOR <-> (
	LEFTARG = date,
	RIGHTARG = date,
	PROCEDURE = date_dist,
	COMMUTATOR = '<->'
)
;
CREATE POLICY p1 ON rls_tbl_force USING (c1 = 5) WITH CHECK (c1 < 5)
;
ANALYZE pg_catalog.pg_shseclabel
;
ALTER INDEX IF EXISTS __attmp_onek_unique1 RENAME TO onek_unique1
;
-- 100 distinct combinations, all in the MCV list
TRUNCATE mcv_lists
;
create function explain_filter(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just 'N'
        ln := regexp_replace(ln, '-?\m\d+\M', 'N', 'g');
        -- In sort output, the above won't match units-suffixed numbers
        ln := regexp_replace(ln, '\m\d+kB', 'NkB', 'g');
        -- Ignore text-mode buffers output because it varies depending
        -- on the system state
        CONTINUE WHEN (ln ~ ' +Buffers: .*');
        -- Ignore text-mode "Planning:" line because whether it's output
        -- varies depending on the system state
        CONTINUE WHEN (ln = 'Planning:');
        return next ln;
    end loop;
end;
$$
;
CREATE FUNCTION isn_out(upc)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE lock_tbl2
;
----------
[ADD_INDEX] CREATE INDEX fooindex ON foo (f1 desc, f2 asc, f3 nulls first, f4 nulls last)
;
----------
[CREATE_TYPE] CREATE TYPE casttesttype (
   internallength = variable,
   input = casttesttype_in,
   output = casttesttype_out,
   alignment = int4
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest26 (
    a int PRIMARY KEY,
    b int GENERATED ALWAYS AS (a * 2) STORED
)
;
----------
[DROP_TABLE] drop table event_trigger_fire2, event_trigger_fire3, event_trigger_fire4, event_trigger_fire5, event_trigger_fire6, event_trigger_fire7
;
----------
[CREATE_TABLE] create temp table temp_inh_oncommit_test (a int) on commit delete rows
;
----------
[ADMIN_TABLE] analyze matest1
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION split_pub_cols RENAME TO split_pub_renamed
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28560 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE|SELECT|METADATA] -- Check that we map relation oids to filenodes and back correctly.  Only
-- display bad mappings so the test output doesn't change all the time.  A
-- filenode function call can return NULL for a relation dropped concurrently
-- with the call's surrounding query, so ignore a NULL mapped_oid for
-- relations that no longer exist after all calls finish.
CREATE TEMP TABLE filenode_mapping AS
SELECT
    oid, mapped_oid, reltablespace, relfilenode, relname
FROM pg_class,
    pg_filenode_relation(reltablespace, pg_relation_filenode(oid)) AS mapped_oid
WHERE relkind IN ('r', 'i', 'S', 't', 'm') AND mapped_oid IS DISTINCT FROM oid
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ## (
   leftarg = path,
   rightarg = path,
   function = path_inter,
   commutator = ##
)
;
----------
[BLOCK] DO $$
	BEGIN
		WHILE NOT EXISTS(SELECT * FROM pg_locks WHERE NOT granted AND relation = 'pg_tablespace'::regclass AND mode = 'AccessShareLock') LOOP
			PERFORM pg_sleep(.1);
		END LOOP;
	END$$
;
----------
[CREATE_TABLE] create table retain_test(a int)
;
----------
[CREATE_TABLE] CREATE TABLE xmldata(data xml)
;
----------
[DROP_TABLE] DROP TABLE plt1_adv
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30909_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26420
;
----------
[CREATE_LIBRARY] CREATE EXTENSION amcheck WITH SCHEMA amcheck_schema
;
----------
[DROP_INDEX] -- error from unique_idx2

DROP INDEX unique_idx1, unique_idx2
;
----------
[CREATE_PROG_OBJ] create or replace function array_elem_check(numeric) returns numeric as $$
declare
  x mynums;
begin
  x[1] := $1;
  return x[1];
end$$ language plpgsql
;
----------
[ALTER_TYPE] -- no-op, same schema
alter type alter1.ctype set schema alter2
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Test default partitions
ALTER TABLE plt1_adv DETACH PARTITION plt1_adv_p1
;
----------
[ALTER_LIBRARY] /* src/test/modules/test_extensions/test_ext7--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- link some existing serial-owning table to the extension
alter extension test_ext7 add table old_table1
;
----------
[CREATE_TABLE] -- Invalid batch_size option
CREATE FOREIGN TABLE inv_bsz (c1 int )
	SERVER loopback OPTIONS (batch_size '100$%$#$#')
;
----------
[DROP_PROG_OBJ] drop function dfunc(varchar, numeric)
;
----------
[BLOCK] DO $$
DECLARE
	objtype text;
BEGIN
	FOR objtype IN VALUES ('toast table'), ('index column'), ('sequence column'),
		('toast table column'), ('view column'), ('materialized view column')
	LOOP
		BEGIN
			PERFORM pg_get_object_address(objtype, '{one}', '{}');
		EXCEPTION WHEN invalid_parameter_value THEN
			RAISE WARNING 'error for %: %', objtype, sqlerrm;
		END;
	END LOOP;
END;
$$
;
----------
[CREATE_TABLE] -- This table is intentionally left around to exercise pg_dump/pg_upgrade
CREATE TABLE bit_defaults(
  b1 bit(4) DEFAULT '1001',
  b2 bit(4) DEFAULT B'0101',
  b3 bit varying(5) DEFAULT '1001',
  b4 bit varying(5) DEFAULT B'0101'
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter bar set not null
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pht1_e" ATTACH PARTITION "public"."pht1_e_p3" FOR VALUES WITH (modulus 3, remainder 2)
;
----------
[CALL_PROG_OBJ] CALL transaction_test7()
;
----------
[ADMIN_TABLE] -- Multiple relations should use their options in isolation.
VACUUM vac_tab_on_toast_off, vac_tab_off_toast_on
;
----------
[CREATE_TABLE] --
-- Test keys that "look" different but compare as equal
--
create table pktable2 (a float8, b float8, primary key (a, b))
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf7 USING btree
;
----------
[CREATE_POLICY] CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C_UTF8')
;
----------
[CREATE_PROG_OBJ] create function return_unnamed_refcursor() returns refcursor as $$
declare
    rc refcursor;
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw add operator public.>^(int, int)
;
----------
[DROP_VIEW] DROP VIEW json_table_view8, json_table_view9
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_shdepend
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_41153_2, pg_temp_11.pg_temp_41153
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41207_2
;
----------
[CREATE_TABLE] --  this should work, however:
CREATE TABLE error_tbl (b1 bool DEFAULT (1 IN (1, 2)))
;
----------
[CREATE_TABLE] CREATE TABLE trgfire5_40 PARTITION OF trgfire5 FOR VALUES IN (40)
;
----------
[CREATE_TABLE] CREATE TABLE stats_track_tab (x int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = date,
	RIGHTARG = date,
	PROCEDURE = date_dist,
	COMMUTATOR = '<->'
)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON rls_tbl_force USING (c1 = 5) WITH CHECK (c1 < 5)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_shseclabel
;
----------
[RENAME_INDEX] ALTER INDEX IF EXISTS __attmp_onek_unique1 RENAME TO onek_unique1
;
----------
[TRUNCATE_TABLE] -- 100 distinct combinations, all in the MCV list
TRUNCATE mcv_lists
;
----------
[CREATE_PROG_OBJ] create function explain_filter(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just 'N'
        ln := regexp_replace(ln, '-?\m\d+\M', 'N', 'g');
        -- In sort output, the above won't match units-suffixed numbers
        ln := regexp_replace(ln, '\m\d+kB', 'NkB', 'g');
        -- Ignore text-mode buffers output because it varies depending
        -- on the system state
        CONTINUE WHEN (ln ~ ' +Buffers: .*');
        -- Ignore text-mode "Planning:" line because whether it's output
        -- varies depending on the system state
        CONTINUE WHEN (ln = 'Planning:');
        return next ln;
    end loop;
end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isn_out(upc)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
