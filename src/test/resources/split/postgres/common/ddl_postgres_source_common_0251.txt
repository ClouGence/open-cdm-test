-- Multi-layer partitioning works correctly in this case:
create table idxpart (a int, b int, primary key (a, b)) partition by range (a)
;
create function non_strict(text) returns text as
    'select coalesce($1, ''got passed a null'');'
    language sql called on null input
;
DROP FUNCTION ctoast_large_val()
;
CREATE TABLE clstr_1 (a INT PRIMARY KEY)
;
DROP SCHEMA addr_nsp CASCADE
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_foreign_data_wrapper
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29692 host=/tmp/KsgVe3OAnU dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_1, tap_pub_2, tap_pub_3, tap_pub_4a, tap_pub_4b, tap_pub_5a, tap_pub_5b, tap_pub_toast, tap_pub_inherits, tap_pub_viaroot_2, tap_pub_viaroot_1, tap_pub_parent_sync, tap_pub_child_sync, tap_pub_virtual
;
DROP TABLE triglog
;
CREATE TABLE prt4_n_p3 PARTITION OF prt4_n FOR VALUES FROM (500) TO (600)
;
--
-- Test of SECURITY LABEL statement without a plugin
--
SECURITY LABEL ON TABLE seclabel_tbl1 IS 'classified'
;
ALTER TABLE pg_operator ADD UNIQUE USING INDEX pg_operator_oprname_l_r_n_index
;
CREATE TEMP TABLE quad_box_tbl_ord_idx1 AS
SELECT rank() OVER (ORDER BY b <-> point '123,456') n, b <-> point '123,456' dist, id
FROM quad_box_tbl
;
ANALYZE public.xmltest2
;
CREATE TYPE gbtreekey2 (
	INTERNALLENGTH = 2,
	INPUT  = gbtreekey2_in,
	OUTPUT = gbtreekey2_out
)
;
CREATE TABLE "public"."alpha_neg_p2" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
ANALYZE public.tbl_include_pk
;
CREATE TABLE partitioned (
	a int,
	CONSTRAINT check_a CHECK (a > 0) NO INHERIT
) PARTITION BY RANGE (a)
;
ALTER TABLE ONLY "public"."pagg_tab1" ATTACH PARTITION "public"."pagg_tab1_p2" FOR VALUES FROM (10) TO (20)
;
create table bc (a int constraint check_a check (a <> 0), b int constraint check_b check (b <> 0)) inherits (ac)
;
ALTER TABLE ONLY "public"."pht2" ATTACH PARTITION "public"."pht2_p2" FOR VALUES WITH (modulus 3, remainder 1)
;
CREATE TABLE "public"."idxpart" (
    "a" integer
)
PARTITION BY RANGE ("a")
;
create function rangetypes_sql(q anyrange, b anyarray, out c anyelement)
  as $$ select upper($1) + $2[1] $$
  language sql
;
CREATE PUBLICATION regress_pub1_gen_to_nogen FOR TABLE tab_gen_to_nogen WITH (publish_generated_columns = none)
;
-- not an incomplete one though
alter table idxpart add unique (b, a)
;
ALTER TABLE ONLY "public"."constr_child2" INHERIT "public"."constr_parent2"
;
CREATE PROCEDURE "public"."ptest6a"(INOUT "a" "anyelement", INOUT "b" "anyelement")
    LANGUAGE "sql"
    AS $_$
SELECT $1, $1;
$_$
;
DROP TABLE fd_pt1 CASCADE
;
DROP SCHEMA IF EXISTS split_maint CASCADE
;
create rule update_id_ordered as on update to id_ordered
	do instead update id set name = new.name where id = old.id
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26213_2 (tid pg_catalog.tid)
;
create table idxpart (a int unique, b int) partition by range (a, b)
;
CREATE FUNCTION test_resowner_priorities(nkinds pg_catalog.int4, nresources pg_catalog.int4)
	RETURNS pg_catalog.void
	AS '$libdir/test_resowner' LANGUAGE C
;
ALTER TABLE pg_temp_9.pg_temp_26996_2 ADD COLUMN newdata pg_temp_9.pg_temp_26996
;
-- check that Append plan is used containing a MergeAppend for sub-partitions
-- that are unordered.
drop table mcrparted5
;
ANALYZE pg_temp_3.pg_temp_27598
;
-- verify that the "AFTER UPDATE OF columns" event is propagated correctly
create table parted_trigger (a int, b text) partition by range (a)
;
CREATE VIEW v AS SELECT * FROM sq_source WHERE sid < 2
;
ANALYZE public.int4_tbl
;
alter table parted_constr attach partition parted1_constr
  for values from ('aaaa') to ('bbbb')
;
ALTER TABLE itest7 ALTER COLUMN a RESTART
;
ANALYZE public.lseg_tbl
;
DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN SELECT * FROM test2 ORDER BY x LOOP
        INSERT INTO test1 (a) VALUES (r.x);
        IF r.x % 2 = 0 THEN
            COMMIT;
        ELSE
            ROLLBACK;
        END IF;
    END LOOP;
END;
$$
;
create table pk5 (like pk) partition by range (a)
;
DO LANGUAGE plpgsql $$
BEGIN
    BEGIN
        INSERT INTO test1 (a) VALUES (1);
        ROLLBACK;
        INSERT INTO test1 (a) VALUES (1/0);
        ROLLBACK;
    EXCEPTION
        WHEN division_by_zero THEN
            RAISE NOTICE 'caught division_by_zero';
    END;
END;
$$
;
DROP TABLE IF EXISTS t3 CASCADE
;
VACUUM (ANALYZE) pg_catalog.pg_opfamily
;
DROP TABLE testpub_unloggedtbl
;
create aggregate newavg2 (sfunc = int4pl,
			  basetype = int4,
			  stype = int4,
			  finalfunc = int2um,
			  initcond = '0')
;
CREATE TABLE pt_stats1 (a int, b int)
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- Multi-layer partitioning works correctly in this case:
create table idxpart (a int, b int, primary key (a, b)) partition by range (a)
;
----------
[CREATE_PROG_OBJ] create function non_strict(text) returns text as
    'select coalesce($1, ''got passed a null'');'
    language sql called on null input
;
----------
[DROP_PROG_OBJ] DROP FUNCTION ctoast_large_val()
;
----------
[CREATE_TABLE] CREATE TABLE clstr_1 (a INT PRIMARY KEY)
;
----------
[DROP_SCHEMA] DROP SCHEMA addr_nsp CASCADE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29692 host=/tmp/KsgVe3OAnU dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_1, tap_pub_2, tap_pub_3, tap_pub_4a, tap_pub_4b, tap_pub_5a, tap_pub_5b, tap_pub_toast, tap_pub_inherits, tap_pub_viaroot_2, tap_pub_viaroot_1, tap_pub_parent_sync, tap_pub_child_sync, tap_pub_virtual
;
----------
[DROP_TABLE] DROP TABLE triglog
;
----------
[CREATE_TABLE] CREATE TABLE prt4_n_p3 PARTITION OF prt4_n FOR VALUES FROM (500) TO (600)
;
----------
[SECURITY_LABEL] --
-- Test of SECURITY LABEL statement without a plugin
--
SECURITY LABEL ON TABLE seclabel_tbl1 IS 'classified'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_operator ADD UNIQUE USING INDEX pg_operator_oprname_l_r_n_index
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE quad_box_tbl_ord_idx1 AS
SELECT rank() OVER (ORDER BY b <-> point '123,456') n, b <-> point '123,456' dist, id
FROM quad_box_tbl
;
----------
[ADMIN_TABLE] ANALYZE public.xmltest2
;
----------
[CREATE_TYPE] CREATE TYPE gbtreekey2 (
	INTERNALLENGTH = 2,
	INPUT  = gbtreekey2_in,
	OUTPUT = gbtreekey2_out
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."alpha_neg_p2" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[ADMIN_TABLE] ANALYZE public.tbl_include_pk
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int,
	CONSTRAINT check_a CHECK (a > 0) NO INHERIT
) PARTITION BY RANGE (a)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab1" ATTACH PARTITION "public"."pagg_tab1_p2" FOR VALUES FROM (10) TO (20)
;
----------
[CREATE_TABLE] create table bc (a int constraint check_a check (a <> 0), b int constraint check_b check (b <> 0)) inherits (ac)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pht2" ATTACH PARTITION "public"."pht2_p2" FOR VALUES WITH (modulus 3, remainder 1)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart" (
    "a" integer
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_PROG_OBJ] create function rangetypes_sql(q anyrange, b anyarray, out c anyelement)
  as $$ select upper($1) + $2[1] $$
  language sql
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION regress_pub1_gen_to_nogen FOR TABLE tab_gen_to_nogen WITH (publish_generated_columns = none)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- not an incomplete one though
alter table idxpart add unique (b, a)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."constr_child2" INHERIT "public"."constr_parent2"
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest6a"(INOUT "a" "anyelement", INOUT "b" "anyelement")
    LANGUAGE "sql"
    AS $_$
SELECT $1, $1;
$_$
;
----------
[DROP_TABLE] DROP TABLE fd_pt1 CASCADE
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_maint CASCADE
;
----------
[CREATE_POLICY] create rule update_id_ordered as on update to id_ordered
	do instead update id set name = new.name where id = old.id
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26213_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table idxpart (a int unique, b int) partition by range (a, b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_resowner_priorities(nkinds pg_catalog.int4, nresources pg_catalog.int4)
	RETURNS pg_catalog.void
	AS '$libdir/test_resowner' LANGUAGE C
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_26996_2 ADD COLUMN newdata pg_temp_9.pg_temp_26996
;
----------
[DROP_TABLE] -- check that Append plan is used containing a MergeAppend for sub-partitions
-- that are unordered.
drop table mcrparted5
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27598
;
----------
[CREATE_TABLE] -- verify that the "AFTER UPDATE OF columns" event is propagated correctly
create table parted_trigger (a int, b text) partition by range (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v AS SELECT * FROM sq_source WHERE sid < 2
;
----------
[ADMIN_TABLE] ANALYZE public.int4_tbl
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_constr attach partition parted1_constr
  for values from ('aaaa') to ('bbbb')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest7 ALTER COLUMN a RESTART
;
----------
[ADMIN_TABLE] ANALYZE public.lseg_tbl
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN SELECT * FROM test2 ORDER BY x LOOP
        INSERT INTO test1 (a) VALUES (r.x);
        IF r.x % 2 = 0 THEN
            COMMIT;
        ELSE
            ROLLBACK;
        END IF;
    END LOOP;
END;
$$
;
----------
[CREATE_TABLE] create table pk5 (like pk) partition by range (a)
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    BEGIN
        INSERT INTO test1 (a) VALUES (1);
        ROLLBACK;
        INSERT INTO test1 (a) VALUES (1/0);
        ROLLBACK;
    EXCEPTION
        WHEN division_by_zero THEN
            RAISE NOTICE 'caught division_by_zero';
    END;
END;
$$
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS t3 CASCADE
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_opfamily
;
----------
[DROP_TABLE] DROP TABLE testpub_unloggedtbl
;
----------
[CREATE_PROG_OBJ] create aggregate newavg2 (sfunc = int4pl,
			  basetype = int4,
			  stype = int4,
			  finalfunc = int2um,
			  initcond = '0')
;
----------
[CREATE_TABLE] CREATE TABLE pt_stats1 (a int, b int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
