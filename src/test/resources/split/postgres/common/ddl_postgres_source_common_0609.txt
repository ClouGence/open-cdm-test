CREATE UNIQUE INDEX t_b_idx ON t (b, a)
;
CREATE PUBLICATION tap_pub_C FOR TABLE "tab'le"
;
alter table test alter column a1 type char(10)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_constraint
;
-- fails
DROP VIEW gtest1v
;
-- Clean up
DROP TABLE async_pt
;
DROP FUNCTION create_relfilenode_part
;
TRUNCATE tab1_2, tab2_1, tab3_1
;
ALTER FOREIGN TABLE agg_csv NO INHERIT agg
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=19285 host=/tmp/ZBWsOYky2b dbname=postgres' PUBLICATION pub1
;
create trigger parted_trig_odd after insert on parted_irreg for each row
  when (bark(new.b) AND new.a % 2 = 1) execute procedure trigger_notice_ab()
;
/*
 * text search configuration for armenian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * armenian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY armenian_stem
	(TEMPLATE = snowball, Language = armenian )
;
CREATE PROCEDURE "public"."ptest6b"("a" "anyelement", INOUT "b" "anyelement", INOUT "c" "anyarray")
    LANGUAGE "sql"
    AS $_$
SELECT $1, array[$1];
$_$
;
CREATE OR REPLACE FUNCTION chksetconfig()
 RETURNS TABLE (db name, "role" name, rolkeyword text, setconfig text[])
 AS $$
SELECT COALESCE(d.datname, 'ALL'), COALESCE(r.rolname, 'ALL'),
	   COALESCE(v.keyword, '-'), s.setconfig
 FROM pg_db_role_setting s
 LEFT JOIN pg_roles r ON (r.oid = s.setrole)
 LEFT JOIN pg_database d ON (d.oid = s.setdatabase)
 LEFT JOIN (VALUES(CURRENT_USER, 'current_user'),
             (SESSION_USER, 'session_user'))
      AS v(uname, keyword)
      ON (r.rolname = v.uname)
   WHERE (r.rolname) IN ('Public', 'current_user', 'regress_testrol1', 'regress_testrol2')
ORDER BY 1, 2;
$$ LANGUAGE SQL
;
CREATE TABLE hjtest_matchbits_t2(id int)
;
create table atacc1 (id serial primary key, value int check (value < 10))
;
ALTER TABLE ONLY "public"."pagg_tab2" ATTACH PARTITION "public"."pagg_tab2_p2" FOR VALUES FROM (10) TO (20)
;
--------------- geo_distance

CREATE FUNCTION geo_distance (point, point)
RETURNS float8
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE AS '$libdir/earthdistance'
;
alter table tab_part_attach attach partition tab_part_attach_1 for values in (1)
;
CREATE OR REPLACE FUNCTION functest1(a int) RETURNS int LANGUAGE SQL WINDOW AS 'SELECT $1'
;
CREATE TABLE bmscantest (a int, b int, t text)
;
drop table merge_source_table, merge_target_table
;
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS 'secret'
;
CREATE TABLESPACE regress_tblspace LOCATION ''
;
CREATE OPERATOR FAMILY "public"."part_test_text_ops" USING "hash"
;
CREATE PROCEDURE ptest1(x text)
LANGUAGE SQL
AS $$
INSERT INTO cp_test VALUES (1, x);
$$
;
ALTER TABLE ONLY "fkpart5"."pk1" ATTACH PARTITION "fkpart5"."pk11" FOR VALUES IN (1)
;
CREATE TABLE "public"."gtestxx_1" (
    "a" integer NOT NULL,
    "b" integer
)
;
-- error
CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
-- test with different parameter types
create function dfunc(a varchar, b numeric, c date = current_date)
  returns table (a varchar, b numeric, c date) as $$
  select $1, $2, $3;
$$ language sql
;
DROP TABLE testpub_tbl1
;
create function anyctest(anycompatible, anycompatiblemultirange)
returns anycompatiblemultirange as $$
  select $2
$$ language sql
;
CREATE EXTENSION test_ext5 SCHEMA test_ext
;
-- fail (not superuser)
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'unclassified'
;
ANALYZE public.pagg_tab_ml_p3_s1
;
CREATE TABLE part1 (
	a int NOT NULL CHECK (a = 1),
	b int NOT NULL CHECK (b >= 1 AND b <= 10)
)
;
-- fail

-- only owner can change policies
ALTER POLICY p1 ON document USING (true)
;
--
-- Check handling of indexes on system columns
--
CREATE TABLE syscol_table (a INT)
;
ALTER TABLE pp_enumpart
							RENAME TO "pp_enumpart
attack"
;
create table p1 (f1 int, f2 int)
;
CREATE TABLE pht1_e_p2 PARTITION OF pht1_e FOR VALUES WITH (MODULUS 3, REMAINDER 1)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_seclabel
;
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, PRIMARY KEY(ptest1, ptest2, ptest3) )
;
ALTER SUBSCRIPTION regress_testsub SET (create_slot = false)
;
create table lprt_a (a int not null)
;
create index �ͪ�ߩ�Ѧ��듾�index2 on �ͪ�ߩ�Ѧ��듾� using hash (��׾��ړ��)
;
ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg3
;
alter table cc1 alter column f1 drop not null
;
CREATE FUNCTION gbt_float8_consistent(internal,float8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE test1()
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE UNIQUE INDEX t_b_idx ON t (b, a)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_C FOR TABLE "tab'le"
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table test alter column a1 type char(10)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_constraint
;
----------
[DROP_VIEW] -- fails
DROP VIEW gtest1v
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE async_pt
;
----------
[DROP_PROG_OBJ] DROP FUNCTION create_relfilenode_part
;
----------
[TRUNCATE_TABLE] TRUNCATE tab1_2, tab2_1, tab3_1
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE agg_csv NO INHERIT agg
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=19285 host=/tmp/ZBWsOYky2b dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parted_trig_odd after insert on parted_irreg for each row
  when (bark(new.b) AND new.a % 2 = 1) execute procedure trigger_notice_ab()
;
----------
[CREATE_POLICY] /*
 * text search configuration for armenian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * armenian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY armenian_stem
	(TEMPLATE = snowball, Language = armenian )
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest6b"("a" "anyelement", INOUT "b" "anyelement", INOUT "c" "anyarray")
    LANGUAGE "sql"
    AS $_$
SELECT $1, array[$1];
$_$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION chksetconfig()
 RETURNS TABLE (db name, "role" name, rolkeyword text, setconfig text[])
 AS $$
SELECT COALESCE(d.datname, 'ALL'), COALESCE(r.rolname, 'ALL'),
	   COALESCE(v.keyword, '-'), s.setconfig
 FROM pg_db_role_setting s
 LEFT JOIN pg_roles r ON (r.oid = s.setrole)
 LEFT JOIN pg_database d ON (d.oid = s.setdatabase)
 LEFT JOIN (VALUES(CURRENT_USER, 'current_user'),
             (SESSION_USER, 'session_user'))
      AS v(uname, keyword)
      ON (r.rolname = v.uname)
   WHERE (r.rolname) IN ('Public', 'current_user', 'regress_testrol1', 'regress_testrol2')
ORDER BY 1, 2;
$$ LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE hjtest_matchbits_t2(id int)
;
----------
[CREATE_TABLE] create table atacc1 (id serial primary key, value int check (value < 10))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab2" ATTACH PARTITION "public"."pagg_tab2_p2" FOR VALUES FROM (10) TO (20)
;
----------
[CREATE_PROG_OBJ] --------------- geo_distance

CREATE FUNCTION geo_distance (point, point)
RETURNS float8
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE AS '$libdir/earthdistance'
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table tab_part_attach attach partition tab_part_attach_1 for values in (1)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION functest1(a int) RETURNS int LANGUAGE SQL WINDOW AS 'SELECT $1'
;
----------
[CREATE_TABLE] CREATE TABLE bmscantest (a int, b int, t text)
;
----------
[DROP_TABLE] drop table merge_source_table, merge_target_table
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS 'secret'
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspace LOCATION ''
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY "public"."part_test_text_ops" USING "hash"
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest1(x text)
LANGUAGE SQL
AS $$
INSERT INTO cp_test VALUES (1, x);
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart5"."pk1" ATTACH PARTITION "fkpart5"."pk11" FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtestxx_1" (
    "a" integer NOT NULL,
    "b" integer
)
;
----------
[CREATE_TABLE|SELECT] -- error
CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_PROG_OBJ] -- test with different parameter types
create function dfunc(a varchar, b numeric, c date = current_date)
  returns table (a varchar, b numeric, c date) as $$
  select $1, $2, $3;
$$ language sql
;
----------
[DROP_TABLE] DROP TABLE testpub_tbl1
;
----------
[CREATE_PROG_OBJ] create function anyctest(anycompatible, anycompatiblemultirange)
returns anycompatiblemultirange as $$
  select $2
$$ language sql
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext5 SCHEMA test_ext
;
----------
[SECURITY_LABEL] -- fail (not superuser)
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'unclassified'
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab_ml_p3_s1
;
----------
[CREATE_TABLE] CREATE TABLE part1 (
	a int NOT NULL CHECK (a = 1),
	b int NOT NULL CHECK (b >= 1 AND b <= 10)
)
;
----------
[ALTER_POLICY] -- fail

-- only owner can change policies
ALTER POLICY p1 ON document USING (true)
;
----------
[CREATE_TABLE] --
-- Check handling of indexes on system columns
--
CREATE TABLE syscol_table (a INT)
;
----------
[RENAME_TABLE] ALTER TABLE pp_enumpart
							RENAME TO "pp_enumpart
attack"
;
----------
[CREATE_TABLE] create table p1 (f1 int, f2 int)
;
----------
[CREATE_TABLE] CREATE TABLE pht1_e_p2 PARTITION OF pht1_e FOR VALUES WITH (MODULUS 3, REMAINDER 1)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_seclabel
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, PRIMARY KEY(ptest1, ptest2, ptest3) )
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (create_slot = false)
;
----------
[CREATE_TABLE] create table lprt_a (a int not null)
;
----------
[ADD_INDEX] create index �ͪ�ߩ�Ѧ��듾�index2 on �ͪ�ߩ�Ѧ��듾� using hash (��׾��ړ��)
;
----------
[RENAME_PROG_OBJ] ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg3
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table cc1 alter column f1 drop not null
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float8_consistent(internal,float8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE test1()
;
