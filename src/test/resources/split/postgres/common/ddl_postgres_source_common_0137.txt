CREATE FUNCTION intset(int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- aggregate that only cares about null/nonnull input
CREATE AGGREGATE newcnt ("any") (
   sfunc = int8inc_any, stype = int8,
   initcond = '0'
)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_foreign_server
;
CREATE TABLE cwi_test(a int) PARTITION BY hash (a)
;
drop schema abc,aac restrict
;
DROP FUNCTION wait_for_decode_stats(bool, bool)
;
CREATE INDEX tmp_idx1 ON tmp (a)
;
CREATE INDEX t1_hash ON s3.t1 USING HASH (i)
;
DROP SEQUENCE IF EXISTS test_seq CASCADE
;
-- Test case where the child constraint is invalid, the grandchild constraint
-- is valid, and the validation for the grandchild should be skipped when a
-- valid constraint is applied to the top parent.
CREATE TABLE fk_partitioned_fk_3 (a int, b int) PARTITION BY RANGE (a, b)
;
--
-- Hash partitioning.
--

-- Use hand-rolled hash functions and operator classes to get predictable
-- result on different machines.  See the definitions of
-- part_part_test_int4_ops and part_test_text_ops in insert.sql.

CREATE TABLE mchash (a int, b text, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_text_ops)
;
ALTER TABLE "S 1"."T 1" SET (autovacuum_enabled = 'false')
;
CREATE TABLE prt1_p3 PARTITION OF prt1 FOR VALUES FROM (500) TO (600)
;
--
-- Regression Test for DML Permissions
--

--
-- Setup
--
CREATE TABLE t1 (a int, junk int, b text)
;
CREATE TABLE test_foreign_constraints(id1 int REFERENCES test_primary_constraints(id))
;
create table constr_child (a int) inherits (constr_parent)
;
DROP TABLESPACE dropme_ts2
;
create index on idxpart2 (a) where b > 1000
;
CREATE OR REPLACE FUNCTION test_slru_page_delete(int) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_delete' LANGUAGE C
;
CREATE FUNCTION test_is_full()
RETURNS bool STRICT PARALLEL UNSAFE
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE EXTENSION test_ext1 SCHEMA has$dollar CASCADE
;
-- obsolete:
CREATE OPERATOR @ (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contains,
	COMMUTATOR = '~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
create function trap_timeout() returns void as $$
begin
  begin
    perform pg_sleep(10);
  exception
    when others then
      raise notice 'caught others: %', sqlerrm;
    when query_canceled then
      raise notice 'nyeah nyeah, can''t stop me';
  end;
  -- Abort transaction to abandon the statement_timeout setting.  Otherwise,
  -- the next top-level statement would be vulnerable to the timeout.
  raise exception 'end of function';
end$$ language plpgsql
;
CREATE TABLE tt2 (x int, y numeric(9,2))
;
DROP TABLE pg_temp_3.pg_temp_27374_2, pg_temp_3.pg_temp_27374
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_foreign_data_wrapper
;
CREATE TRIGGER "tg_wslot_bu" BEFORE UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_wslot_bu"()
;
CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LC_COLLATE = 'C' LC_CTYPE = 'C'
;
ANALYZE public.rtest_order2
;
CLUSTER test_maint_search_path.test_maint USING test_maint_idx
;
drop table key_desc, key_desc_1
;
--
-- Test various ways to create primary keys on partitions, linked to unique
-- indexes (without constraints) on the partitioned table.  Ideally these should
-- fail, but we don't dare change released behavior, so instead cope with it at
-- DETACH time.
CREATE TEMP TABLE t (a integer, b integer) PARTITION BY HASH (a, b)
;
CREATE OR REPLACE FUNCTION test_create_multixact() RETURNS xid
  AS 'MODULE_PATHNAME', 'test_create_multixact' LANGUAGE C
;
CREATE TABLE rf_tbl_abcd_part_pk (a int PRIMARY KEY, b int) PARTITION by RANGE (a)
;
-- expect NULL not error

drop domain mytext cascade
;
CREATE FUNCTION "public"."int42_in"("cstring") RETURNS "public"."int42"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int4in$$
;
COMMENT ON COLUMN default_test_row.f1 IS 'good comment'
;
CREATE PUBLICATION tap_pub_A FOR TABLE tab_full, test_tab
;
-- intentionally unsupported

DROP COLLATION mycoll1
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
-- Cases where the schema creation succeeds.
-- The schema created matches the role name.
CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE TABLE regress_create_schema_role.tab (id int)
;
analyze revalidate_bug
;
CREATE SCHEMA ext_cine_schema
;
create table list_parted (
	a	varchar
) partition by list (a)
;
CREATE TABLE public.tst_dom_constr (a monot_int)
;
CREATE INDEX "idxpart_a_idx" ON ONLY "public"."idxpart" USING "btree" ("a")
;
ALTER EXTENSION test_ext_cine UPDATE TO '1.1'
;
DROP TABLE accounts_ext
;
CREATE INDEX partidx_abc_idx ON idxpart (a, b, c)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_template
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION intset(int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- aggregate that only cares about null/nonnull input
CREATE AGGREGATE newcnt ("any") (
   sfunc = int8inc_any, stype = int8,
   initcond = '0'
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_foreign_server
;
----------
[CREATE_TABLE] CREATE TABLE cwi_test(a int) PARTITION BY hash (a)
;
----------
[DROP_SCHEMA] drop schema abc,aac restrict
;
----------
[DROP_PROG_OBJ] DROP FUNCTION wait_for_decode_stats(bool, bool)
;
----------
[ADD_INDEX] CREATE INDEX tmp_idx1 ON tmp (a)
;
----------
[ADD_INDEX] CREATE INDEX t1_hash ON s3.t1 USING HASH (i)
;
----------
[DROP_SEQUENCE] DROP SEQUENCE IF EXISTS test_seq CASCADE
;
----------
[CREATE_TABLE] -- Test case where the child constraint is invalid, the grandchild constraint
-- is valid, and the validation for the grandchild should be skipped when a
-- valid constraint is applied to the top parent.
CREATE TABLE fk_partitioned_fk_3 (a int, b int) PARTITION BY RANGE (a, b)
;
----------
[CREATE_TABLE] --
-- Hash partitioning.
--

-- Use hand-rolled hash functions and operator classes to get predictable
-- result on different machines.  See the definitions of
-- part_part_test_int4_ops and part_test_text_ops in insert.sql.

CREATE TABLE mchash (a int, b text, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_text_ops)
;
----------
[ALTER_TABLE] ALTER TABLE "S 1"."T 1" SET (autovacuum_enabled = 'false')
;
----------
[CREATE_TABLE] CREATE TABLE prt1_p3 PARTITION OF prt1 FOR VALUES FROM (500) TO (600)
;
----------
[CREATE_TABLE] --
-- Regression Test for DML Permissions
--

--
-- Setup
--
CREATE TABLE t1 (a int, junk int, b text)
;
----------
[CREATE_TABLE] CREATE TABLE test_foreign_constraints(id1 int REFERENCES test_primary_constraints(id))
;
----------
[CREATE_TABLE] create table constr_child (a int) inherits (constr_parent)
;
----------
[DROP_TABLESPACE] DROP TABLESPACE dropme_ts2
;
----------
[ADD_INDEX] create index on idxpart2 (a) where b > 1000
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_delete(int) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_delete' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_is_full()
RETURNS bool STRICT PARALLEL UNSAFE
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext1 SCHEMA has$dollar CASCADE
;
----------
[CREATE_PROG_OBJ] -- obsolete:
CREATE OPERATOR @ (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contains,
	COMMUTATOR = '~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_PROG_OBJ] create function trap_timeout() returns void as $$
begin
  begin
    perform pg_sleep(10);
  exception
    when others then
      raise notice 'caught others: %', sqlerrm;
    when query_canceled then
      raise notice 'nyeah nyeah, can''t stop me';
  end;
  -- Abort transaction to abandon the statement_timeout setting.  Otherwise,
  -- the next top-level statement would be vulnerable to the timeout.
  raise exception 'end of function';
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE tt2 (x int, y numeric(9,2))
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_27374_2, pg_temp_3.pg_temp_27374
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_wslot_bu" BEFORE UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_wslot_bu"()
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LC_COLLATE = 'C' LC_CTYPE = 'C'
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_order2
;
----------
[ADMIN_TABLE] CLUSTER test_maint_search_path.test_maint USING test_maint_idx
;
----------
[DROP_TABLE] drop table key_desc, key_desc_1
;
----------
[CREATE_TABLE] --
-- Test various ways to create primary keys on partitions, linked to unique
-- indexes (without constraints) on the partitioned table.  Ideally these should
-- fail, but we don't dare change released behavior, so instead cope with it at
-- DETACH time.
CREATE TEMP TABLE t (a integer, b integer) PARTITION BY HASH (a, b)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_create_multixact() RETURNS xid
  AS 'MODULE_PATHNAME', 'test_create_multixact' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE rf_tbl_abcd_part_pk (a int PRIMARY KEY, b int) PARTITION by RANGE (a)
;
----------
[DROP_TYPE] -- expect NULL not error

drop domain mytext cascade
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int42_in"("cstring") RETURNS "public"."int42"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int4in$$
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN default_test_row.f1 IS 'good comment'
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_A FOR TABLE tab_full, test_tab
;
----------
[DROP_POLICY] -- intentionally unsupported

DROP COLLATION mycoll1
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
----------
[CREATE_SCHEMA] -- Cases where the schema creation succeeds.
-- The schema created matches the role name.
CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE TABLE regress_create_schema_role.tab (id int)
;
----------
[ADMIN_TABLE] analyze revalidate_bug
;
----------
[CREATE_SCHEMA] CREATE SCHEMA ext_cine_schema
;
----------
[CREATE_TABLE] create table list_parted (
	a	varchar
) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_dom_constr (a monot_int)
;
----------
[ADD_INDEX] CREATE INDEX "idxpart_a_idx" ON ONLY "public"."idxpart" USING "btree" ("a")
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_ext_cine UPDATE TO '1.1'
;
----------
[DROP_TABLE] DROP TABLE accounts_ext
;
----------
[ADD_INDEX] CREATE INDEX partidx_abc_idx ON idxpart (a, b, c)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_template
;
