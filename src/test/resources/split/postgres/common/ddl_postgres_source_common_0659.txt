create trigger tg_chkslotname before insert
    on PLine for each row execute procedure tg_chkslotname('PL')
;
CREATE EXTENSION test_dsm_registry
;
/*
 * 5.18
 * COLUMN_DOMAIN_USAGE view
 */

CREATE VIEW column_domain_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(nt.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_type t, pg_namespace nt, pg_class c, pg_namespace nc,
         pg_attribute a

    WHERE t.typnamespace = nt.oid
          AND c.relnamespace = nc.oid
          AND a.attrelid = c.oid
          AND a.atttypid = t.oid
          AND t.typtype = 'd'
          AND c.relkind IN ('r', 'v', 'f', 'p')
          AND a.attnum > 0
          AND NOT a.attisdropped
          AND pg_has_role(t.typowner, 'USAGE')
;
CREATE VIEW role_usage_grants AS
    SELECT grantor,
           grantee,
           object_catalog,
           object_schema,
           object_name,
           object_type,
           privilege_type,
           is_grantable
    FROM usage_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
do $$
declare x constant int; y int;
begin
  for x, y in select 1, 2 loop  -- fail
  end loop;
end$$
;
CREATE TABLE part_hp PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
-- should fail
drop domain restrictedmultirange
;
ALTER INDEX pg_toast.pg_toast_30694_index RENAME TO reind_con_toast_idx
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_description
;
CREATE TABLE testpub_tbl3 (a int)
;
CREATE TABLE tr_etoomuch (id serial primary key, data int)
;
CREATE INDEX numericidx ON numerictmp USING gist ( a )
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=20571 host=/tmp/o2wey5DTaB dbname=postgres' PUBLICATION tap_pub
;
-- fail, type already present
DROP TYPE shell
;
ALTER INDEX pg_toast.pg_toast_27667_index RENAME TO reind_con_toast_idx
;
-- inverse boolean partitioning - a seemingly unlikely design, but we've got
-- code for it, so we'd better test it.
create table iboolpart (a bool) partition by list ((not a))
;
-- fail
alter table invalid_check_con_child drop constraint inh_check_constraint
;
CREATE UNIQUE INDEX t2_btree_unique ON s5.t2 USING BTREE (i)
;
CREATE FUNCTION check_foreign_key()
RETURNS trigger
AS '$libdir/refint'
LANGUAGE C
;
CREATE FUNCTION functest_F_4(int) RETURNS bool LANGUAGE 'sql'
       STRICT AS 'SELECT $1 = 50'
;
CREATE TABLE ctlt11a (LIKE ctlv1 INCLUDING ALL)
;
DROP FOREIGN TABLE remote_backend_pid
;
create temp table tidv (idv mycomptype)
;
CREATE TABLESPACE ts1 LOCATION ''
;
create type varbitrange as range (subtype = varbit)
;
/* End test case for bug #18970 */


create table alttype_cluster (a int)
;
CREATE TABLE measurement_y2006m02 (
    CHECK ( logdate >= DATE '2006-02-01' AND logdate < DATE '2006-03-01' )
) INHERITS (measurement) WITH (autovacuum_enabled=off)
;
ALTER TEXT SEARCH CONFIGURATION spanish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
ANALYZE array_op_test
;
create operator = (
    procedure = int8alias2eq,
    leftarg = int8alias2, rightarg = int8alias2,
    commutator = =,
    restrict = eqsel, join = eqjoinsel,
    merges
)
;
-- fails

-- SET STORAGE may need to add a TOAST table
create table test_storage (a text)
;
alter table pgbench_accounts add constraint pgbench_accounts_bid_fkey foreign key (bid) references pgbench_branches
;
CREATE TABLE dep1 (c1 int)
;
alter table fkpart0.fk_part_23 drop constraint fk_part_23_a_fkey
;
create index gist_pointidx on gist_point_tbl using gist(p)
;
create table test9s (a int not null, b int generated always as (a * 2) stored)
;
DROP TABLE guid1, guid2 CASCADE
;
CREATE TEXT SEARCH DICTIONARY "public"."synonym" (
    TEMPLATE = "pg_catalog"."synonym",
    synonyms = 'synonym_sample' )
;
/* contrib/pg_stat_statements/pg_stat_statements--1.10--1.11.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* Drop old versions */
DROP VIEW pg_stat_statements
;
DROP TABLE rf_bug
;
TRUNCATE ONLY trunc_fb, trunc_fa
;
CREATE TABLE "generated_stored_tests"."gtestxx_1" (
    "a" integer NOT NULL,
    "b" integer
)
;
alter text search parser alter1.prs set schema alter2
;
CREATE FUNCTION test1 (int) RETURNS int LANGUAGE internal
    AS 'nosuch'
;
CREATE TABLE fk_parted_pk_1 PARTITION OF fk_parted_pk FOR VALUES IN (1)
;
ALTER TABLE tab_rowfilter_toast ALTER COLUMN a SET STORAGE EXTERNAL
;
ALTER TABLE pg_toast.pg_toast_30558 RENAME TO reind_con_toast
;
-- the comparison operators have funky names (and are undocumented)
-- in an attempt to discourage anyone from actually using them. they
-- only exist to support the btree opclass

CREATE OPERATOR #<# (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_lt,
       COMMUTATOR = #>#,
       NEGATOR = #>=#,
       RESTRICT = scalarltsel,
       JOIN = scalarltjoinsel
)
;
CREATE FUNCTION isnlt(upc, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create domain pos_int as int4 check (value > 0) not null
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_chkslotname before insert
    on PLine for each row execute procedure tg_chkslotname('PL')
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_dsm_registry
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.18
 * COLUMN_DOMAIN_USAGE view
 */

CREATE VIEW column_domain_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(nt.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_type t, pg_namespace nt, pg_class c, pg_namespace nc,
         pg_attribute a

    WHERE t.typnamespace = nt.oid
          AND c.relnamespace = nc.oid
          AND a.attrelid = c.oid
          AND a.atttypid = t.oid
          AND t.typtype = 'd'
          AND c.relkind IN ('r', 'v', 'f', 'p')
          AND a.attnum > 0
          AND NOT a.attisdropped
          AND pg_has_role(t.typowner, 'USAGE')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW role_usage_grants AS
    SELECT grantor,
           grantee,
           object_catalog,
           object_schema,
           object_name,
           object_type,
           privilege_type,
           is_grantable
    FROM usage_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[BLOCK] do $$
declare x constant int; y int;
begin
  for x, y in select 1, 2 loop  -- fail
  end loop;
end$$
;
----------
[CREATE_TABLE] CREATE TABLE part_hp PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[DROP_TYPE] -- should fail
drop domain restrictedmultirange
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30694_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_description
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl3 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE tr_etoomuch (id serial primary key, data int)
;
----------
[ADD_INDEX] CREATE INDEX numericidx ON numerictmp USING gist ( a )
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=20571 host=/tmp/o2wey5DTaB dbname=postgres' PUBLICATION tap_pub
;
----------
[DROP_TYPE] -- fail, type already present
DROP TYPE shell
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27667_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- inverse boolean partitioning - a seemingly unlikely design, but we've got
-- code for it, so we'd better test it.
create table iboolpart (a bool) partition by list ((not a))
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- fail
alter table invalid_check_con_child drop constraint inh_check_constraint
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t2_btree_unique ON s5.t2 USING BTREE (i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION check_foreign_key()
RETURNS trigger
AS '$libdir/refint'
LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_F_4(int) RETURNS bool LANGUAGE 'sql'
       STRICT AS 'SELECT $1 = 50'
;
----------
[CREATE_TABLE] CREATE TABLE ctlt11a (LIKE ctlv1 INCLUDING ALL)
;
----------
[DROP_TABLE] DROP FOREIGN TABLE remote_backend_pid
;
----------
[CREATE_TABLE] create temp table tidv (idv mycomptype)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION ''
;
----------
[CREATE_TYPE] create type varbitrange as range (subtype = varbit)
;
----------
[CREATE_TABLE] /* End test case for bug #18970 */


create table alttype_cluster (a int)
;
----------
[CREATE_TABLE] CREATE TABLE measurement_y2006m02 (
    CHECK ( logdate >= DATE '2006-02-01' AND logdate < DATE '2006-03-01' )
) INHERITS (measurement) WITH (autovacuum_enabled=off)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION spanish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ADMIN_TABLE] ANALYZE array_op_test
;
----------
[CREATE_PROG_OBJ] create operator = (
    procedure = int8alias2eq,
    leftarg = int8alias2, rightarg = int8alias2,
    commutator = =,
    restrict = eqsel, join = eqjoinsel,
    merges
)
;
----------
[CREATE_TABLE] -- fails

-- SET STORAGE may need to add a TOAST table
create table test_storage (a text)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_accounts add constraint pgbench_accounts_bid_fkey foreign key (bid) references pgbench_branches
;
----------
[CREATE_TABLE] CREATE TABLE dep1 (c1 int)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table fkpart0.fk_part_23 drop constraint fk_part_23_a_fkey
;
----------
[ADD_INDEX] create index gist_pointidx on gist_point_tbl using gist(p)
;
----------
[CREATE_TABLE] create table test9s (a int not null, b int generated always as (a * 2) stored)
;
----------
[DROP_TABLE] DROP TABLE guid1, guid2 CASCADE
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."synonym" (
    TEMPLATE = "pg_catalog"."synonym",
    synonyms = 'synonym_sample' )
;
----------
[DROP_VIEW] /* contrib/pg_stat_statements/pg_stat_statements--1.10--1.11.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* Drop old versions */
DROP VIEW pg_stat_statements
;
----------
[DROP_TABLE] DROP TABLE rf_bug
;
----------
[TRUNCATE_TABLE] TRUNCATE ONLY trunc_fb, trunc_fa
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtestxx_1" (
    "a" integer NOT NULL,
    "b" integer
)
;
----------
[ALTER_POLICY] alter text search parser alter1.prs set schema alter2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1 (int) RETURNS int LANGUAGE internal
    AS 'nosuch'
;
----------
[CREATE_TABLE] CREATE TABLE fk_parted_pk_1 PARTITION OF fk_parted_pk FOR VALUES IN (1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tab_rowfilter_toast ALTER COLUMN a SET STORAGE EXTERNAL
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30558 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] -- the comparison operators have funky names (and are undocumented)
-- in an attempt to discourage anyone from actually using them. they
-- only exist to support the btree opclass

CREATE OPERATOR #<# (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_lt,
       COMMUTATOR = #>#,
       NEGATOR = #>=#,
       RESTRICT = scalarltsel,
       JOIN = scalarltjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(upc, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TYPE] create domain pos_int as int4 check (value > 0) not null
;
