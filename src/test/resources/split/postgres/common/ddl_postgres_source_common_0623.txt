ALTER TYPE "public"."insenum" ADD VALUE 'i28'
;
--
-- Ensure the enable_partition_prune GUC properly disables partition pruning.
--

create table pp_lp (a int, value int) partition by list (a)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_rewrite
;
ALTER VIEW rw_view15 ALTER COLUMN upper SET DEFAULT 'NOT SET'
;
-- wait until the worker has processed the tuple we just inserted
DO $$
DECLARE
	count int;
	loops int := 0;
BEGIN
	LOOP
		count := count(*) FROM schema4.counted WHERE type = 'delta';
		IF count = 0 OR loops > 120 * 10 THEN EXIT; END IF;
		PERFORM pg_sleep(0.1);
		loops := loops + 1;
	END LOOP;
END
$$
;
CREATE TABLE pagg_tab6_p1 PARTITION OF pagg_tab6 FOR VALUES IN ('a', 'b')
;
-- Once check_b is added to the parent, it should be made non-local for part_b
ALTER TABLE parted ADD CONSTRAINT check_b CHECK (b >= 0)
;
CREATE OR REPLACE FUNCTION schema_two.add(int, int) RETURNS int LANGUAGE plpgsql
  CALLED ON NULL INPUT
  AS $$ BEGIN RETURN coalesce($1,0) + coalesce($2,0); END; $$
;
CREATE TABLESPACE regress_tblspacewith LOCATION '/src/src/bin/pg_upgrade/tmp_check/regress/testtablespace' WITH (random_page_cost = 3.0)
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "uint" WITH "simple"
;
CREATE FUNCTION hs_contained(hstore,hstore)
RETURNS bool
AS '$libdir/hstore','hstore_contained'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE pg_am ADD PRIMARY KEY USING INDEX pg_am_oid_index
;
ALTER OPERATOR <= (ltree, ltree)
  SET (RESTRICT = scalarlesel, JOIN = scalarlejoinsel)
;
DROP TABLE temporal_rng2
;
ALTER OPERATOR <= (isbn13, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
-- nondeterministic collations
-- (not supported with libc provider)

CREATE COLLATION ctest_det (locale = 'en_US.utf8', deterministic = true)
;
-- relhassubclass of a partitioned index is false before creating any partition.
-- It will be set after the first partition is created.
create index idxpart_idx on idxpart (a)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_tablespace
;
CREATE OR REPLACE FUNCTION pg_create_logical_replication_slot(
    IN slot_name name, IN plugin name,
    IN temporary boolean DEFAULT false,
    OUT slot_name name, OUT lsn pg_lsn)
RETURNS RECORD
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_create_logical_replication_slot'
;
create temp table graph( f int, t int, label text )
;
COMMENT ON LARGE OBJECT 32034 IS 'I Wandered Lonely as a Cloud'
;
COMMENT ON SERVER s1 IS 'foreign server'
;
DROP FUNCTION b()
;
DROP FUNCTION base_fn_out(opaque)
;
-- Test that hash partitions continue to work after they're concurrently
-- detached (bugs #18371, #19070)
CREATE TABLE hash_parted2 (a int) PARTITION BY HASH(a)
;
create function add_group(grp anyarray, ad anyelement, size integer)
  returns anyarray
  as $$
begin
  if grp is null then
    return array[ad];
  end if;
  if array_upper(grp, 1) < size then
    return grp || ad;
  end if;
  return grp;
end;
$$
  language plpgsql immutable
;
ALTER INDEX concur_reindex_part_index ATTACH PARTITION concur_reindex_part_index_0
;
drop table basictest
;
ANALYZE public.part_rev
;
-- add NOT NULL constraint recursively
alter table inh_parent alter column f1 set not null
;
create index ��Ƒ�㑻�������index1 on ��Ƒ�㑻������� using btree(������)
;
-- partitioned rels cannot specify the default tablespace.  These fail:
CREATE TABLE testschema.dflt (a int PRIMARY KEY) PARTITION BY LIST (a) TABLESPACE pg_default
;
CREATE TABLE tab1_def PARTITION OF tab1 (c DEFAULT 'sub1_tab1') DEFAULT
;
--
-- SQLSTATE and SQLERRM test
--

create function excpt_test1() returns void as $$
begin
    raise notice '% %', sqlstate, sqlerrm;
end; $$ language plpgsql
;
DROP TABLE ctest
;
CREATE VIEW my_property_secure WITH (security_barrier) AS
       SELECT * FROM customer WHERE name = current_user
;
-- ALTER INDEX .. ATTACH, error cases
create table idxpart (a int, b int) partition by range (a, b)
;
create table parent_tab (id int) partition by range (id)
;
truncate parted_copytest
;
DROP INDEX wowidx2
;
ANALYZE pg_temp_6.pg_temp_33826
;
ALTER TABLE pg_toast.pg_toast_30465 RENAME TO reind_con_toast
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c1 SET STATISTICS 10000
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26101_2 (tid pg_catalog.tid)
;
ALTER TEXT SEARCH CONFIGURATION testcfg ADD MAPPING FOR word WITH simple
;
ANALYZE analyze_table
;
CREATE INDEX ti ON tststats.t (a, b)
;
CREATE RULE "rtest_sys_del" AS
    ON DELETE TO "public"."rtest_system" DO ( DELETE FROM "public"."rtest_interface"
  WHERE ("rtest_interface"."sysname" = "old"."sysname");
 DELETE FROM "public"."rtest_admin"
  WHERE ("rtest_admin"."sysname" = "old"."sysname");
)
;
CREATE TABLE ruletest1 (a int)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_init_privs
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i28'
;
----------
[CREATE_TABLE] --
-- Ensure the enable_partition_prune GUC properly disables partition pruning.
--

create table pp_lp (a int, value int) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_rewrite
;
----------
[ALTER_VIEW|ALTER_COLUMN] ALTER VIEW rw_view15 ALTER COLUMN upper SET DEFAULT 'NOT SET'
;
----------
[BLOCK] -- wait until the worker has processed the tuple we just inserted
DO $$
DECLARE
	count int;
	loops int := 0;
BEGIN
	LOOP
		count := count(*) FROM schema4.counted WHERE type = 'delta';
		IF count = 0 OR loops > 120 * 10 THEN EXIT; END IF;
		PERFORM pg_sleep(0.1);
		loops := loops + 1;
	END LOOP;
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab6_p1 PARTITION OF pagg_tab6 FOR VALUES IN ('a', 'b')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Once check_b is added to the parent, it should be made non-local for part_b
ALTER TABLE parted ADD CONSTRAINT check_b CHECK (b >= 0)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION schema_two.add(int, int) RETURNS int LANGUAGE plpgsql
  CALLED ON NULL INPUT
  AS $$ BEGIN RETURN coalesce($1,0) + coalesce($2,0); END; $$
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspacewith LOCATION '/src/src/bin/pg_upgrade/tmp_check/regress/testtablespace' WITH (random_page_cost = 3.0)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "uint" WITH "simple"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hs_contained(hstore,hstore)
RETURNS bool
AS '$libdir/hstore','hstore_contained'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_am ADD PRIMARY KEY USING INDEX pg_am_oid_index
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ltree, ltree)
  SET (RESTRICT = scalarlesel, JOIN = scalarlejoinsel)
;
----------
[DROP_TABLE] DROP TABLE temporal_rng2
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (isbn13, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_POLICY] -- nondeterministic collations
-- (not supported with libc provider)

CREATE COLLATION ctest_det (locale = 'en_US.utf8', deterministic = true)
;
----------
[ADD_INDEX] -- relhassubclass of a partitioned index is false before creating any partition.
-- It will be set after the first partition is created.
create index idxpart_idx on idxpart (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_tablespace
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_create_logical_replication_slot(
    IN slot_name name, IN plugin name,
    IN temporary boolean DEFAULT false,
    OUT slot_name name, OUT lsn pg_lsn)
RETURNS RECORD
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_create_logical_replication_slot'
;
----------
[CREATE_TABLE] create temp table graph( f int, t int, label text )
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 32034 IS 'I Wandered Lonely as a Cloud'
;
----------
[COMMENT_FOREIGN_SERVER] COMMENT ON SERVER s1 IS 'foreign server'
;
----------
[DROP_PROG_OBJ] DROP FUNCTION b()
;
----------
[DROP_PROG_OBJ] DROP FUNCTION base_fn_out(opaque)
;
----------
[CREATE_TABLE] -- Test that hash partitions continue to work after they're concurrently
-- detached (bugs #18371, #19070)
CREATE TABLE hash_parted2 (a int) PARTITION BY HASH(a)
;
----------
[CREATE_PROG_OBJ] create function add_group(grp anyarray, ad anyelement, size integer)
  returns anyarray
  as $$
begin
  if grp is null then
    return array[ad];
  end if;
  if array_upper(grp, 1) < size then
    return grp || ad;
  end if;
  return grp;
end;
$$
  language plpgsql immutable
;
----------
[ALTER_INDEX] ALTER INDEX concur_reindex_part_index ATTACH PARTITION concur_reindex_part_index_0
;
----------
[DROP_TABLE] drop table basictest
;
----------
[ADMIN_TABLE] ANALYZE public.part_rev
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- add NOT NULL constraint recursively
alter table inh_parent alter column f1 set not null
;
----------
[ADD_INDEX] create index ��Ƒ�㑻�������index1 on ��Ƒ�㑻������� using btree(������)
;
----------
[CREATE_TABLE] -- partitioned rels cannot specify the default tablespace.  These fail:
CREATE TABLE testschema.dflt (a int PRIMARY KEY) PARTITION BY LIST (a) TABLESPACE pg_default
;
----------
[CREATE_TABLE] CREATE TABLE tab1_def PARTITION OF tab1 (c DEFAULT 'sub1_tab1') DEFAULT
;
----------
[CREATE_PROG_OBJ] --
-- SQLSTATE and SQLERRM test
--

create function excpt_test1() returns void as $$
begin
    raise notice '% %', sqlstate, sqlerrm;
end; $$ language plpgsql
;
----------
[DROP_TABLE] DROP TABLE ctest
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW my_property_secure WITH (security_barrier) AS
       SELECT * FROM customer WHERE name = current_user
;
----------
[CREATE_TABLE] -- ALTER INDEX .. ATTACH, error cases
create table idxpart (a int, b int) partition by range (a, b)
;
----------
[CREATE_TABLE] create table parent_tab (id int) partition by range (id)
;
----------
[TRUNCATE_TABLE] truncate parted_copytest
;
----------
[DROP_INDEX] DROP INDEX wowidx2
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_6.pg_temp_33826
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30465 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c1 SET STATISTICS 10000
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26101_2 (tid pg_catalog.tid)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION testcfg ADD MAPPING FOR word WITH simple
;
----------
[ADMIN_TABLE] ANALYZE analyze_table
;
----------
[ADD_INDEX] CREATE INDEX ti ON tststats.t (a, b)
;
----------
[CREATE_POLICY] CREATE RULE "rtest_sys_del" AS
    ON DELETE TO "public"."rtest_system" DO ( DELETE FROM "public"."rtest_interface"
  WHERE ("rtest_interface"."sysname" = "old"."sysname");
 DELETE FROM "public"."rtest_admin"
  WHERE ("rtest_admin"."sysname" = "old"."sysname");
)
;
----------
[CREATE_TABLE] CREATE TABLE ruletest1 (a int)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_init_privs
;
