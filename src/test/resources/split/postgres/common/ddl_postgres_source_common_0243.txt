CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
ALTER TABLE pg_temp_95.pg_temp_33804_2 ADD COLUMN newdata pg_temp_95.pg_temp_33804
;
DROP TABLE pg_temp_38.pg_temp_28037_2, pg_temp_38.pg_temp_28037
;
ALTER TABLE pg_temp_13.pg_temp_25000_2 ADD COLUMN newdata pg_temp_13.pg_temp_25000
;
do $$ declare a int[];
begin a := array[1,2]; a[3] := 4; raise notice 'a = %', a; end$$
;
ANALYZE pg_temp_11.pg_temp_26640
;
CREATE FUNCTION _ltree_consistent(internal,_ltree,int2,oid,internal)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TEXT SEARCH CONFIGURATION nepali ADD MAPPING
    FOR word, hword_part, hword
	WITH nepali_stem
;
create or replace function part_hashtext_length(value text, seed int8)
RETURNS int8 AS $$
select length(coalesce(value, ''))::int8
$$ language sql immutable
;
-- check null bitmap alignment for table whose number of attributes is multiple of 8
create table test8 (f1 int, f2 int, f3 int, f4 int, f5 int, f6 int, f7 int, f8 int)
;
ALTER TABLE range_parted ATTACH PARTITION part1 FOR VALUES FROM (1, 1) TO (1, 10)
;
CREATE TYPE default_test_row AS (f1 text_w_default, f2 int42)
;
create or replace function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return false;
  else
    return $1::int = $2::int;
  end if;
end;
$$ language plpgsql immutable
;
DROP TABLE concur_reindex_tab, concur_reindex_tab2, concur_reindex_tab3
;
DROP TABLE attmp3
;
ALTER TABLE pg_toast.pg_toast_33450 RENAME TO reind_con_toast
;
-- (at this point bar2 contains tuple with natts=1)
alter table bar1 attach partition bar2 default
;
DROP TABLE pg_temp_18.pg_temp_26864_2, pg_temp_18.pg_temp_26864
;
/* contrib/lo/lo--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
--	Create the data type ... now just a domain over OID
--

CREATE DOMAIN lo AS pg_catalog.oid
;
create function halfsum_finalfn(state int4) returns int4 as
$$
begin
	if state is null then
		return NULL;
	else
		return state / 2;
	end if;
end
$$ language plpgsql
;
-- constraint not cover all values, should fail
alter table atacc1 add constraint atacc1_constr_or check(test_a is not null or test_b < 10)
;
drop table event_trigger_fire1
;
CREATE FUNCTION regexp_matches( citext, citext ) RETURNS SETOF TEXT[] AS $$
    SELECT pg_catalog.regexp_matches( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE ROWS 1
;
ALTER TABLE ONLY "public"."pagg_tab1" ATTACH PARTITION "public"."pagg_tab1_p3" FOR VALUES FROM (20) TO (30)
;
CREATE TABLE "public"."ramp" (
    "name" "text",
    "thepath" "path"
)
;
/*
 * Handle related functions
 */
CREATE FUNCTION handle_get_and_error()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE TABLE range3_default PARTITION OF range_parted3 DEFAULT
;
do $$
declare x record := row(1,2,3);
begin
  raise notice 'x = %', x;
end$$
;
DROP VIEW lock_view5
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_largeobject_metadata
;
-- This should fail because pg_buffercache_evict_relation() doesn't accept
-- local relations
CREATE TEMP TABLE temp_pg_buffercache()
;
CREATE PROCEDURE ptest3(y text)
LANGUAGE SQL
AS $$
CALL ptest1(y);
CALL ptest1($1);
$$
;
-- let's do one where the check fails when added
create table atacc1 ( test int )
;
CREATE OR REPLACE FUNCTION mvtest_drop_the_index()
  RETURNS bool AS $$
BEGIN
  EXECUTE 'DROP INDEX IF EXISTS mvtest_mvschema.mvtest_drop_idx';
  RETURN true;
END;
$$ LANGUAGE plpgsql
;
ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk1_pkey"
;
CREATE TABLE slow_emp4000 (
	home_base	 box
)
;
ALTER TABLE T ADD COLUMN c2 TIMESTAMP DEFAULT clock_timestamp()
;
CALL test_proc6(2, x, y)
;
drop function excpt_test3()
;
CREATE FUNCTION populate_record(anyelement,hstore)
RETURNS anyelement
AS '$libdir/hstore', 'hstore_populate_record'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_cast
;
ALTER TABLE fd_pt1 DROP COLUMN c6
;
ALTER TEXT SEARCH CONFIGURATION hindi ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH english_stem
;
CREATE TABLE functest1 (i int)
;
CREATE TABLE "public"."spgist_text_tbl" (
    "id" integer,
    "t" "text"
)
;
VACUUM (FULL) pg_catalog.pg_policy
;
CREATE FUNCTION hstore(text,text)
RETURNS hstore
AS '$libdir/hstore','hstore_from_text'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
CREATE TABLE "public"."prt1_n_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
DROP SCHEMA split_stats_move
;
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_5 FOR VALUES IN (4500)
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_95.pg_temp_33804_2 ADD COLUMN newdata pg_temp_95.pg_temp_33804
;
----------
[DROP_TABLE] DROP TABLE pg_temp_38.pg_temp_28037_2, pg_temp_38.pg_temp_28037
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_25000_2 ADD COLUMN newdata pg_temp_13.pg_temp_25000
;
----------
[BLOCK] do $$ declare a int[];
begin a := array[1,2]; a[3] := 4; raise notice 'a = %', a; end$$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26640
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_consistent(internal,_ltree,int2,oid,internal)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION nepali ADD MAPPING
    FOR word, hword_part, hword
	WITH nepali_stem
;
----------
[CREATE_PROG_OBJ] create or replace function part_hashtext_length(value text, seed int8)
RETURNS int8 AS $$
select length(coalesce(value, ''))::int8
$$ language sql immutable
;
----------
[CREATE_TABLE] -- check null bitmap alignment for table whose number of attributes is multiple of 8
create table test8 (f1 int, f2 int, f3 int, f4 int, f5 int, f6 int, f7 int, f8 int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE range_parted ATTACH PARTITION part1 FOR VALUES FROM (1, 1) TO (1, 10)
;
----------
[CREATE_TYPE] CREATE TYPE default_test_row AS (f1 text_w_default, f2 int42)
;
----------
[CREATE_PROG_OBJ] create or replace function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return false;
  else
    return $1::int = $2::int;
  end if;
end;
$$ language plpgsql immutable
;
----------
[DROP_TABLE] DROP TABLE concur_reindex_tab, concur_reindex_tab2, concur_reindex_tab3
;
----------
[DROP_TABLE] DROP TABLE attmp3
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33450 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- (at this point bar2 contains tuple with natts=1)
alter table bar1 attach partition bar2 default
;
----------
[DROP_TABLE] DROP TABLE pg_temp_18.pg_temp_26864_2, pg_temp_18.pg_temp_26864
;
----------
[CREATE_TYPE] /* contrib/lo/lo--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
--	Create the data type ... now just a domain over OID
--

CREATE DOMAIN lo AS pg_catalog.oid
;
----------
[CREATE_PROG_OBJ] create function halfsum_finalfn(state int4) returns int4 as
$$
begin
	if state is null then
		return NULL;
	else
		return state / 2;
	end if;
end
$$ language plpgsql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- constraint not cover all values, should fail
alter table atacc1 add constraint atacc1_constr_or check(test_a is not null or test_b < 10)
;
----------
[DROP_TABLE] drop table event_trigger_fire1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regexp_matches( citext, citext ) RETURNS SETOF TEXT[] AS $$
    SELECT pg_catalog.regexp_matches( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE ROWS 1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab1" ATTACH PARTITION "public"."pagg_tab1_p3" FOR VALUES FROM (20) TO (30)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ramp" (
    "name" "text",
    "thepath" "path"
)
;
----------
[CREATE_PROG_OBJ] /*
 * Handle related functions
 */
CREATE FUNCTION handle_get_and_error()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE range3_default PARTITION OF range_parted3 DEFAULT
;
----------
[BLOCK] do $$
declare x record := row(1,2,3);
begin
  raise notice 'x = %', x;
end$$
;
----------
[DROP_VIEW] DROP VIEW lock_view5
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_TABLE] -- This should fail because pg_buffercache_evict_relation() doesn't accept
-- local relations
CREATE TEMP TABLE temp_pg_buffercache()
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest3(y text)
LANGUAGE SQL
AS $$
CALL ptest1(y);
CALL ptest1($1);
$$
;
----------
[CREATE_TABLE] -- let's do one where the check fails when added
create table atacc1 ( test int )
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION mvtest_drop_the_index()
  RETURNS bool AS $$
BEGIN
  EXECUTE 'DROP INDEX IF EXISTS mvtest_mvschema.mvtest_drop_idx';
  RETURN true;
END;
$$ LANGUAGE plpgsql
;
----------
[ALTER_INDEX] ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk1_pkey"
;
----------
[CREATE_TABLE] CREATE TABLE slow_emp4000 (
	home_base	 box
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE T ADD COLUMN c2 TIMESTAMP DEFAULT clock_timestamp()
;
----------
[CALL_PROG_OBJ] CALL test_proc6(2, x, y)
;
----------
[DROP_PROG_OBJ] drop function excpt_test3()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION populate_record(anyelement,hstore)
RETURNS anyelement
AS '$libdir/hstore', 'hstore_populate_record'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_cast
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fd_pt1 DROP COLUMN c6
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION hindi ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH english_stem
;
----------
[CREATE_TABLE] CREATE TABLE functest1 (i int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."spgist_text_tbl" (
    "id" integer,
    "t" "text"
)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_policy
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore(text,text)
RETURNS hstore
AS '$libdir/hstore','hstore_from_text'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_n_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[DROP_SCHEMA] DROP SCHEMA split_stats_move
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_5 FOR VALUES IN (4500)
;
