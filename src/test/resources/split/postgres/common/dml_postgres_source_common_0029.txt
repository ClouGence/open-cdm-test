INSERT INTO public.ths_p1 VALUES (4, 434)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."notnull_tbl4_cld"'::pg_catalog.regclass
  AND attname IN ('a')
;
INSERT INTO public.tplain VALUES ('0', 210)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt2_e_p3"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."tableam_parted_d_heap2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
-- if an alias is specified, don't allow the original table name
-- to be referenced
DELETE FROM delete_test dt WHERE delete_test.a > 25
;
-- Test year field value with len > 4
INSERT INTO TIMESTAMPTZ_TST VALUES(1, 'Sat Mar 12 23:58:48 1000 IST')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9753', relminmxid = '3'
WHERE oid = '29032'
;
insert into child (a, b) values (NULL, 'foo')
;
insert into sometable values (1, 'a', repeat('ffoob',1000))
;
INSERT INTO tab_rep SELECT generate_series(11,20)
;
INSERT INTO public.tht VALUES ('7', 587)
;
-- date matches but no conflict
INSERT INTO temporal3 (id, valid_at) VALUES ('[2,3)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT (id, valid_at) DO NOTHING
;
-- insert some data and run analyze, to test that these cases build properly
INSERT INTO ab1
SELECT x / 10, x / 3,
    '2020-10-01'::timestamp + x * interval '1 day',
    '2020-10-01'::timestamptz + x * interval '1 day'
FROM generate_series(1, 100) x
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10722', relminmxid = '1'
WHERE oid = '"public"."rtest_view3"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10215', relminmxid = '1'
WHERE oid = '28799'
;
INSERT INTO voo VALUES(13,'zit2')
;
insert into WSlot values ('WS.102.3a', '102', '', '')
;
INSERT INTO num_exp_add VALUES (4,4,'15598922.8238')
;
INSERT INTO public.tht VALUES ('7', 557)
;
INSERT INTO public.tplain VALUES ('1', 671)
;
INSERT INTO public.tht VALUES ('6', 866)
;
INSERT INTO public.ths_p3 VALUES (5, 455)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."prt2_l_p1"'::pg_catalog.regclass
;
INSERT INTO inner_text VALUES ('a', NULL)
;
INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(1501,2000) s(i)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12662', relminmxid = '3'
WHERE oid = '"public"."part2_self_fk"'::pg_catalog.regclass
;
INSERT INTO num_input_test(n1) VALUES (' 0X_30b1_F33a_6DF0_bD4E_64DF_9BdA_7D15 ')
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO public.tht VALUES ('6', 446)
;
INSERT INTO gtest_normal (a) VALUES (1)
;
INSERT INTO public.ths_p1 VALUES (2, 192)
;
-- Cannot cast to path on assignment.
INSERT INTO caster (path)          VALUES ('((0,0),(1,1),(2,0))'::text)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtest2"'::pg_catalog.regclass
;
UPDATE rw_view15 SET upper='ROW 3' WHERE a=3
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11989', relminmxid = '3'
WHERE oid = '"testxmlschema"."test3"'::pg_catalog.regclass
;
-- insert values close to date minimum
INSERT INTO brin_date_test SELECT '4713-01-01 BC'::date + i FROM generate_series(1, 30) s(i)
;
INSERT INTO INTERVAL_TBL (f1) VALUES ('@ 34 year')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16935', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_p2"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('1', 701)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '738', relminmxid = '1'
WHERE oid = '"public"."char_tbl"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '761', relminmxid = '1'
WHERE oid = '"public"."tenk2"'::pg_catalog.regclass
;
INSERT INTO fkpart10.tbl1 VALUES (4)
;
UPDATE pg_proc SET proacl = null WHERE proname LIKE 'testagg_'
;
INSERT INTO pg_temp_10.pg_temp_26444_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_10.pg_temp_26444 AS newdata FROM public.mvtest_boxmv mv FULL JOIN pg_temp_10.pg_temp_26444 newdata ON (newdata.id OPERATOR(pg_catalog.=) mv.id AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO public.tht VALUES ('8', 878)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8987', relminmxid = '1'
WHERE oid = '"public"."rtest_view1"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8132', relminmxid = '1'
WHERE oid = '"public"."itest2"'::pg_catalog.regclass
;
INSERT INTO tru_rtable0 (SELECT x FROM generate_series(21,25) x)
;
-- date matches but no conflict
INSERT INTO temporal_rng (id, valid_at) VALUES ('[2,3)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT ON CONSTRAINT temporal_rng_pk DO NOTHING
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 434)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."notnull_tbl4_cld"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 210)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt2_e_p3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."tableam_parted_d_heap2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[DELETE] -- if an alias is specified, don't allow the original table name
-- to be referenced
DELETE FROM delete_test dt WHERE delete_test.a > 25
;
----------
[INSERT] -- Test year field value with len > 4
INSERT INTO TIMESTAMPTZ_TST VALUES(1, 'Sat Mar 12 23:58:48 1000 IST')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9753', relminmxid = '3'
WHERE oid = '29032'
;
----------
[INSERT|SELECT] insert into child (a, b) values (NULL, 'foo')
;
----------
[INSERT] insert into sometable values (1, 'a', repeat('ffoob',1000))
;
----------
[INSERT|SELECT] INSERT INTO tab_rep SELECT generate_series(11,20)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('7', 587)
;
----------
[MERGE|SELECT] -- date matches but no conflict
INSERT INTO temporal3 (id, valid_at) VALUES ('[2,3)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT (id, valid_at) DO NOTHING
;
----------
[INSERT|SELECT] -- insert some data and run analyze, to test that these cases build properly
INSERT INTO ab1
SELECT x / 10, x / 3,
    '2020-10-01'::timestamp + x * interval '1 day',
    '2020-10-01'::timestamptz + x * interval '1 day'
FROM generate_series(1, 100) x
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10722', relminmxid = '1'
WHERE oid = '"public"."rtest_view3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10215', relminmxid = '1'
WHERE oid = '28799'
;
----------
[INSERT] INSERT INTO voo VALUES(13,'zit2')
;
----------
[INSERT] insert into WSlot values ('WS.102.3a', '102', '', '')
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (4,4,'15598922.8238')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('7', 557)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 671)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 866)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 455)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."prt2_l_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO inner_text VALUES ('a', NULL)
;
----------
[INSERT|SELECT] INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(1501,2000) s(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12662', relminmxid = '3'
WHERE oid = '"public"."part2_self_fk"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO num_input_test(n1) VALUES (' 0X_30b1_F33a_6DF0_bD4E_64DF_9BdA_7D15 ')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 446)
;
----------
[INSERT|SELECT] INSERT INTO gtest_normal (a) VALUES (1)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 192)
;
----------
[INSERT|SELECT] -- Cannot cast to path on assignment.
INSERT INTO caster (path)          VALUES ('((0,0),(1,1),(2,0))'::text)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtest2"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE rw_view15 SET upper='ROW 3' WHERE a=3
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11989', relminmxid = '3'
WHERE oid = '"testxmlschema"."test3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- insert values close to date minimum
INSERT INTO brin_date_test SELECT '4713-01-01 BC'::date + i FROM generate_series(1, 30) s(i)
;
----------
[INSERT|SELECT] INSERT INTO INTERVAL_TBL (f1) VALUES ('@ 34 year')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16935', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 701)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '738', relminmxid = '1'
WHERE oid = '"public"."char_tbl"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '761', relminmxid = '1'
WHERE oid = '"public"."tenk2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO fkpart10.tbl1 VALUES (4)
;
----------
[UPDATE] UPDATE pg_proc SET proacl = null WHERE proname LIKE 'testagg_'
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_10.pg_temp_26444_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_10.pg_temp_26444 AS newdata FROM public.mvtest_boxmv mv FULL JOIN pg_temp_10.pg_temp_26444 newdata ON (newdata.id OPERATOR(pg_catalog.=) mv.id AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 878)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8987', relminmxid = '1'
WHERE oid = '"public"."rtest_view1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8132', relminmxid = '1'
WHERE oid = '"public"."itest2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO tru_rtable0 (SELECT x FROM generate_series(21,25) x)
;
----------
[MERGE|SELECT] -- date matches but no conflict
INSERT INTO temporal_rng (id, valid_at) VALUES ('[2,3)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT ON CONSTRAINT temporal_rng_pk DO NOTHING
;
