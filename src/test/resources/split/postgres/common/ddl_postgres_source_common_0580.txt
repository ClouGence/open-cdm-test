ANALYZE pg_temp_24.pg_temp_26555_2
;
DROP CAST (int4 AS casttesttype)
;
CREATE TABLE "public"."prt1_l_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY LIST ("c")
;
create table at_partitioned_0 partition of at_partitioned
  for values with (modulus 2, remainder 0)
;
TRUNCATE TABLE ONLY public.ths_p3
;
CREATE VIEW pg_stat_progress_copy AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        S.relid AS relid,
        CASE S.param5 WHEN 1 THEN 'COPY FROM'
                      WHEN 2 THEN 'COPY TO'
                      END AS command,
        CASE S.param6 WHEN 1 THEN 'FILE'
                      WHEN 2 THEN 'PROGRAM'
                      WHEN 3 THEN 'PIPE'
                      WHEN 4 THEN 'CALLBACK'
                      END AS "type",
        S.param1 AS bytes_processed,
        S.param2 AS bytes_total,
        S.param3 AS tuples_processed,
        S.param4 AS tuples_excluded,
        S.param7 AS tuples_skipped
    FROM pg_stat_get_progress_info('COPY') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
ANALYZE public.dept
;
CREATE TABLE "fkpart4"."droppk" (
    "a" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
CREATE TABLE "public"."covidxpart4" (
    "b" integer,
    "a" integer
)
;
COMMENT ON FUNCTION cube_contains(cube, cube) IS 'contains'
;
-- test case for a whole-row-variable bug
create function rngfunc1(n integer, out a text, out b text)
  returns setof record
  language sql
  as $$ select 'foo ' || i, 'bar ' || i from generate_series(1,$1) i $$
;
CREATE FUNCTION isnne(ean13, upc)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TEXT SEARCH CONFIGURATION indonesian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
DROP TABLE reindex_index_status
;
create type rngfunc2 as (a integer, b text)
;
alter table atacc1 add constraint atacc_test1 check (test1>3)
;
TRUNCATE public.tru_rtable1 CONTINUE IDENTITY RESTRICT
;
CREATE TABLE plt3_adv (a int, b int, c text) PARTITION BY LIST (c)
;
-- but SET isn't
create or replace function myfunc(int) returns text as $$
begin
  set work_mem = '2MB';
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_shdepend
;
alter table only renameColumn add column x int
;
CREATE TABLE clustertest (f1 int PRIMARY KEY)
;
CREATE FUNCTION "public"."list_partitioned_table"() RETURNS SETOF integer
    LANGUAGE "plpgsql"
    AS $$
DECLARE
    row partitioned_table%ROWTYPE;
    a_val partitioned_table.a%TYPE;
BEGIN
    FOR row IN SELECT * FROM partitioned_table ORDER BY a LOOP
        a_val := row.a;
        RETURN NEXT a_val;
    END LOOP;
    RETURN;
END; $$
;
CREATE TRIGGER trg_x_after AFTER INSERT ON x
FOR EACH ROW EXECUTE PROCEDURE fn_x_after()
;
CREATE TABLE plt1_adv_p1 PARTITION OF plt1_adv FOR VALUES IN ('0001', '0003')
;
-- ALTER FOREIGN TABLE
COMMENT ON FOREIGN TABLE ft1 IS 'foreign table'
;
VACUUM ANALYZE tst
;
CREATE SUBSCRIPTION regress_sub3 CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION regress_pub3 WITH (enabled = false)
;
-- Check each OID-containing system catalog to see if its lowest-numbered OID
-- is pinned.  If not, and if that OID was generated during initdb, then
-- perhaps initdb forgot to scan that catalog for pinnable entries.
-- Generally, it's okay for a catalog to be listed in the output of this
-- test if that catalog is scanned by initdb.c's setup_depend() function;
-- whatever OID the test is complaining about must have been added later
-- in initdb, where it intentionally isn't pinned.  Legitimate exceptions
-- to that rule are listed in the comments in setup_depend().
-- Currently, pg_rewrite is also listed by this check, even though it is
-- covered by setup_depend().  That happens because there are no rules in
-- the pinned data, but initdb creates some intentionally-not-pinned views.

do $$
declare relnm text;
  reloid oid;
  shared bool;
  lowoid oid;
  pinned bool;
begin
for relnm, reloid, shared in
  select relname, oid, relisshared from pg_class
  where EXISTS(
      SELECT * FROM pg_attribute
      WHERE attrelid = pg_class.oid AND attname = 'oid')
    and relkind = 'r' and oid < 16384 order by 1
loop
  execute 'select min(oid) from ' || relnm into lowoid;
  continue when lowoid is null or lowoid >= 16384;
  if shared then
    pinned := exists(select 1 from pg_shdepend
                     where refclassid = reloid and refobjid = lowoid
                     and deptype = 'p');
  else
    pinned := exists(select 1 from pg_depend
                     where refclassid = reloid and refobjid = lowoid
                     and deptype = 'p');
  end if;
  if not pinned then
    raise notice '% contains unpinned initdb-created object(s)', relnm;
  end if;
end loop;
end$$
;
CREATE FUNCTION dump_test.pltestlang_call_handler()
					   RETURNS LANGUAGE_HANDLER AS '$libdir/plpgsql',
					   'plpgsql_call_handler' LANGUAGE C
;
-- should fail
drop type intmultirange
;
CREATE FUNCTION index(ltree,ltree,int4)
RETURNS int4
AS '$libdir/ltree', 'ltree_index'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- prevent using prohibited expressions in the key
CREATE FUNCTION retset (a int) RETURNS SETOF int AS $$ SELECT 1; $$ LANGUAGE SQL IMMUTABLE
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_transform
;
create table child1 () inherits (parent)
;
create function psql_df_plpgsql ()
  returns void
  language plpgsql
  as $$ begin return; end; $$
;
-- both column has check constraints
alter table atacc1 add constraint atacc1_constr_b_valid check(test_b is not null)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_db_role_setting
;
CREATE TABLESPACE regress_ts2 LOCATION '/src/src/test/modules/test_misc/tmp_check/t_002_tablespace_main_data/ts2'
;
CREATE OPERATOR CLASS isbn_ops DEFAULT
	FOR TYPE isbn USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashisbn(isbn)
;
CREATE MATERIALIZED VIEW dump_test.ordering_view AS
				SELECT * FROM dump_test.ordering_table GROUP BY id
;
alter table only parent alter a set not null
;
CREATE SUBSCRIPTION regress_sub CONNECTION 'port=12754 host=/tmp/EDxB_oTMye dbname=postgres' PUBLICATION regress_pub WITH (two_phase = 'true', failover = 'true')
;
-- fails
alter table recur1 add column f2 recur1[]
;
ALTER TABLE ONLY "public"."bar1" ATTACH PARTITION "public"."bar2" DEFAULT
;
do $$ declare a complex[];
begin a[1:2].i := array[11,12]; raise notice 'a = %', a; end$$
;
ALTER TABLE test_oat_schema.test_oat_tab NO FORCE ROW LEVEL SECURITY
;
CREATE TABLE my_table (num integer, description text)
;
-- try disabling the buffer usage limit
VACUUM (BUFFER_USAGE_LIMIT 0) vac_option_tab
;
-- check attaching default partition fails if a default partition already
-- exists
CREATE TABLE fail_def_part (LIKE part_1 INCLUDING CONSTRAINTS)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_26555_2
;
----------
[DROP_PROG_OBJ] DROP CAST (int4 AS casttesttype)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_l_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY LIST ("c")
;
----------
[CREATE_TABLE] create table at_partitioned_0 partition of at_partitioned
  for values with (modulus 2, remainder 0)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE ONLY public.ths_p3
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_progress_copy AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        S.relid AS relid,
        CASE S.param5 WHEN 1 THEN 'COPY FROM'
                      WHEN 2 THEN 'COPY TO'
                      END AS command,
        CASE S.param6 WHEN 1 THEN 'FILE'
                      WHEN 2 THEN 'PROGRAM'
                      WHEN 3 THEN 'PIPE'
                      WHEN 4 THEN 'CALLBACK'
                      END AS "type",
        S.param1 AS bytes_processed,
        S.param2 AS bytes_total,
        S.param3 AS tuples_processed,
        S.param4 AS tuples_excluded,
        S.param7 AS tuples_skipped
    FROM pg_stat_get_progress_info('COPY') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
----------
[ADMIN_TABLE] ANALYZE public.dept
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart4"."droppk" (
    "a" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."covidxpart4" (
    "b" integer,
    "a" integer
)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_contains(cube, cube) IS 'contains'
;
----------
[CREATE_PROG_OBJ] -- test case for a whole-row-variable bug
create function rngfunc1(n integer, out a text, out b text)
  returns setof record
  language sql
  as $$ select 'foo ' || i, 'bar ' || i from generate_series(1,$1) i $$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ean13, upc)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION indonesian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[DROP_TABLE] DROP TABLE reindex_index_status
;
----------
[CREATE_TYPE] create type rngfunc2 as (a integer, b text)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_test1 check (test1>3)
;
----------
[TRUNCATE_TABLE] TRUNCATE public.tru_rtable1 CONTINUE IDENTITY RESTRICT
;
----------
[CREATE_TABLE] CREATE TABLE plt3_adv (a int, b int, c text) PARTITION BY LIST (c)
;
----------
[CREATE_PROG_OBJ] -- but SET isn't
create or replace function myfunc(int) returns text as $$
begin
  set work_mem = '2MB';
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_shdepend
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table only renameColumn add column x int
;
----------
[CREATE_TABLE] CREATE TABLE clustertest (f1 int PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."list_partitioned_table"() RETURNS SETOF integer
    LANGUAGE "plpgsql"
    AS $$
DECLARE
    row partitioned_table%ROWTYPE;
    a_val partitioned_table.a%TYPE;
BEGIN
    FOR row IN SELECT * FROM partitioned_table ORDER BY a LOOP
        a_val := row.a;
        RETURN NEXT a_val;
    END LOOP;
    RETURN;
END; $$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trg_x_after AFTER INSERT ON x
FOR EACH ROW EXECUTE PROCEDURE fn_x_after()
;
----------
[CREATE_TABLE] CREATE TABLE plt1_adv_p1 PARTITION OF plt1_adv FOR VALUES IN ('0001', '0003')
;
----------
[UNKNOWN] -- ALTER FOREIGN TABLE
COMMENT ON FOREIGN TABLE ft1 IS 'foreign table'
;
----------
[ADMIN_TABLE] VACUUM ANALYZE tst
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub3 CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION regress_pub3 WITH (enabled = false)
;
----------
[BLOCK] -- Check each OID-containing system catalog to see if its lowest-numbered OID
-- is pinned.  If not, and if that OID was generated during initdb, then
-- perhaps initdb forgot to scan that catalog for pinnable entries.
-- Generally, it's okay for a catalog to be listed in the output of this
-- test if that catalog is scanned by initdb.c's setup_depend() function;
-- whatever OID the test is complaining about must have been added later
-- in initdb, where it intentionally isn't pinned.  Legitimate exceptions
-- to that rule are listed in the comments in setup_depend().
-- Currently, pg_rewrite is also listed by this check, even though it is
-- covered by setup_depend().  That happens because there are no rules in
-- the pinned data, but initdb creates some intentionally-not-pinned views.

do $$
declare relnm text;
  reloid oid;
  shared bool;
  lowoid oid;
  pinned bool;
begin
for relnm, reloid, shared in
  select relname, oid, relisshared from pg_class
  where EXISTS(
      SELECT * FROM pg_attribute
      WHERE attrelid = pg_class.oid AND attname = 'oid')
    and relkind = 'r' and oid < 16384 order by 1
loop
  execute 'select min(oid) from ' || relnm into lowoid;
  continue when lowoid is null or lowoid >= 16384;
  if shared then
    pinned := exists(select 1 from pg_shdepend
                     where refclassid = reloid and refobjid = lowoid
                     and deptype = 'p');
  else
    pinned := exists(select 1 from pg_depend
                     where refclassid = reloid and refobjid = lowoid
                     and deptype = 'p');
  end if;
  if not pinned then
    raise notice '% contains unpinned initdb-created object(s)', relnm;
  end if;
end loop;
end$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dump_test.pltestlang_call_handler()
					   RETURNS LANGUAGE_HANDLER AS '$libdir/plpgsql',
					   'plpgsql_call_handler' LANGUAGE C
;
----------
[DROP_TYPE] -- should fail
drop type intmultirange
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION index(ltree,ltree,int4)
RETURNS int4
AS '$libdir/ltree', 'ltree_index'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- prevent using prohibited expressions in the key
CREATE FUNCTION retset (a int) RETURNS SETOF int AS $$ SELECT 1; $$ LANGUAGE SQL IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_transform
;
----------
[CREATE_TABLE] create table child1 () inherits (parent)
;
----------
[CREATE_PROG_OBJ] create function psql_df_plpgsql ()
  returns void
  language plpgsql
  as $$ begin return; end; $$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- both column has check constraints
alter table atacc1 add constraint atacc1_constr_b_valid check(test_b is not null)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_db_role_setting
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_ts2 LOCATION '/src/src/test/modules/test_misc/tmp_check/t_002_tablespace_main_data/ts2'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS isbn_ops DEFAULT
	FOR TYPE isbn USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashisbn(isbn)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW dump_test.ordering_view AS
				SELECT * FROM dump_test.ordering_table GROUP BY id
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table only parent alter a set not null
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub CONNECTION 'port=12754 host=/tmp/EDxB_oTMye dbname=postgres' PUBLICATION regress_pub WITH (two_phase = 'true', failover = 'true')
;
----------
[ALTER_TABLE|ADD_COLUMN] -- fails
alter table recur1 add column f2 recur1[]
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."bar1" ATTACH PARTITION "public"."bar2" DEFAULT
;
----------
[BLOCK] do $$ declare a complex[];
begin a[1:2].i := array[11,12]; raise notice 'a = %', a; end$$
;
----------
[ALTER_TABLE] ALTER TABLE test_oat_schema.test_oat_tab NO FORCE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] CREATE TABLE my_table (num integer, description text)
;
----------
[ADMIN_TABLE] -- try disabling the buffer usage limit
VACUUM (BUFFER_USAGE_LIMIT 0) vac_option_tab
;
----------
[CREATE_TABLE] -- check attaching default partition fails if a default partition already
-- exists
CREATE TABLE fail_def_part (LIKE part_1 INCLUDING CONSTRAINTS)
;
