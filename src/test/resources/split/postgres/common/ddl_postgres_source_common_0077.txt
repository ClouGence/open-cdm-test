create table part_1_10 partition of range_list_parted for values from (1) to (10) partition by list (b)
;
-- clock_timestamp() is volatile
ALTER TABLE T ADD COLUMN c2 TIMESTAMP DEFAULT clock_timestamp()
;
CREATE OPERATOR "public".= (
    FUNCTION = "public"."int8alias2eq",
    LEFTARG = "public"."int8alias2",
    RIGHTARG = "public"."int8alias2",
    COMMUTATOR = OPERATOR("public".=),
    MERGES,
    RESTRICT = "eqsel",
    JOIN = "eqjoinsel"
)
;
CREATE TABLE test(c1 int)
;
CREATE TEMP TABLE testnull(a int, b text)
;
CREATE TABLE notnull_tbl1 (a INTEGER NOT NULL NOT NULL)
;
CREATE FUNCTION hashismn(ismn)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (PARALLEL 2) pg_catalog.pg_shdepend
;
CREATE TABLE pxtest3(fff int)
;
CREATE TABLE pagg_tab_para(x int, y int) PARTITION BY RANGE(x)
;
create table mlparted_tab_part3 partition of mlparted_tab for values in (3)
;
DROP TABLE CASE2_TBL
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_user_mapping
;
DROP EXTENSION IF EXISTS test_extension_exists
;
CREATE TABLE firstparent (tomorrow date default now()::date + 1)
;
-- view on top of view

CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified')
;
CREATE TABLE collate_test1 (
    a int,
    b text COLLATE "C" NOT NULL
)
;
CREATE TABLE "public"."tbl_include_unique1" (
    "c1" integer,
    "c2" integer,
    "c3" integer,
    "c4" "box"
)
;
CREATE FUNCTION on_login_proc() RETURNS event_trigger AS $$
BEGIN
  INSERT INTO user_logins (who) VALUES (SESSION_USER);
  IF SESSION_USER = 'regress_mallory' THEN
    RAISE EXCEPTION 'Hello %! You are NOT welcome here!', SESSION_USER;
  END IF;
  RAISE NOTICE 'Hello %! You are welcome!', SESSION_USER;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
ALTER AGGREGATE split_agg_probe.sum_int(integer) RENAME TO sum_int_renamed
;
CREATE TABLE rule_merge1 (a int, b text)
;
CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a < b WITH CHECK OPTION
;
CREATE TABLE test9b (a int, b int)
;
-- ok to have a partition's own constraints though
ALTER TABLE fd_pt2_1 ALTER c3 SET NOT NULL
;
CREATE VIEW pg_stats_ext WITH (security_barrier) AS
    SELECT cn.nspname AS schemaname,
           c.relname AS tablename,
           sn.nspname AS statistics_schemaname,
           s.stxname AS statistics_name,
           pg_get_userbyid(s.stxowner) AS statistics_owner,
           ( SELECT array_agg(a.attname ORDER BY a.attnum)
             FROM unnest(s.stxkeys) k
                  JOIN pg_attribute a
                       ON (a.attrelid = s.stxrelid AND a.attnum = k)
           ) AS attnames,
           s.stxkind AS kinds,
           sd.stxdndistinct AS n_distinct,
           sd.stxddependencies AS dependencies,
           m.most_common_vals,
           m.most_common_val_nulls,
           m.most_common_freqs,
           m.most_common_base_freqs
    FROM pg_statistic_ext s JOIN pg_class c ON (c.oid = s.stxrelid)
         JOIN pg_statistic_ext_data sd ON (s.oid = sd.stxoid)
         LEFT JOIN pg_namespace cn ON (cn.oid = c.relnamespace)
         LEFT JOIN pg_namespace sn ON (sn.oid = s.stxnamespace)
         LEFT JOIN LATERAL
                   ( SELECT array_agg(values) AS most_common_vals,
                            array_agg(nulls) AS most_common_val_nulls,
                            array_agg(frequency) AS most_common_freqs,
                            array_agg(base_frequency) AS most_common_base_freqs
                     FROM pg_mcv_list_items(sd.stxdmcv)
                   ) m ON sd.stxdmcv IS NOT NULL
    WHERE NOT EXISTS
              ( SELECT 1
                FROM unnest(stxkeys) k
                     JOIN pg_attribute a
                          ON (a.attrelid = s.stxrelid AND a.attnum = k)
                WHERE NOT has_column_privilege(c.oid, a.attnum, 'select') )
    AND (c.relrowsecurity = false OR NOT row_security_active(c.oid))
;
-- Use a temp table so that effects of VACUUM are predictable
CREATE TEMP TABLE test1 (a int, b int)
;
--
-- bt_metap()
--
CREATE FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int4,
    OUT level int4,
    OUT fastroot int4,
    OUT fastlevel int4)
AS '$libdir/pageinspect', 'bt_metap'
LANGUAGE C STRICT PARALLEL SAFE
;
-- alter table / alter column [set/drop] not null tests
-- try altering system catalogs, should fail
alter table pg_class alter column relname drop not null
;
TRUNCATE test_tab1
;
CREATE TABLE FKTABLE (ftest1 cidr, ftest2 timestamp, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable)
;
CREATE INDEX t2_btree ON s4.t2 USING BTREE (i)
;
ANALYZE pg_temp_52.pg_temp_29691
;
CREATE UNIQUE INDEX bttest_unique_idx1
						ON bttest_unique1
						USING btree (i int4_custom_ops1)
						WITH (deduplicate_items = off)
;
-- not supported yet
ALTER TABLE regtest_table ALTER b DROP DEFAULT
;
ALTER INDEX pg_toast.pg_toast_27555_index RENAME TO reind_con_toast_idx
;
create table donothingbrtrig_test2 (c text, b text, a int)
;
drop type rngfunc_type cascade
;
DROP TABLE inh_nn_lvl1, inh_nn_lvl2, inh_nn_lvl3
;
-- max will be adjusted
ALTER SEQUENCE sequence_test12 AS int
;
CREATE TABLE "public"."prt2_m" (
    "a" integer,
    "b" integer,
    "c" integer
)
PARTITION BY RANGE (((("b" + "a") / 2)), "b")
;
CREATE TABLE tt8(a int)
;
alter extension postgres_fdw add operator public.=^(int, int)
;
-- 55
TRUNCATE tru_ftable
;
ALTER TABLE temporal3
  ADD CONSTRAINT temporal3_uq
  UNIQUE USING INDEX idx_temporal3_uq
;
CREATE TABLE prt1_l_p3_p2 PARTITION OF prt1_l_p3 FOR VALUES FROM (13) TO (25)
;
VACUUM (ANALYZE) pg_catalog.pg_ts_config_map
;
---
--- CREATE_RULE
---


CREATE RULE rule_1 AS
  ON INSERT
  TO datatype_table
  DO NOTHING
;
create table inh_parent (a int not null)
;
CREATE TABLE array_gin_test (a int[])
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table part_1_10 partition of range_list_parted for values from (1) to (10) partition by list (b)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- clock_timestamp() is volatile
ALTER TABLE T ADD COLUMN c2 TIMESTAMP DEFAULT clock_timestamp()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".= (
    FUNCTION = "public"."int8alias2eq",
    LEFTARG = "public"."int8alias2",
    RIGHTARG = "public"."int8alias2",
    COMMUTATOR = OPERATOR("public".=),
    MERGES,
    RESTRICT = "eqsel",
    JOIN = "eqjoinsel"
)
;
----------
[CREATE_TABLE] CREATE TABLE test(c1 int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE testnull(a int, b text)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1 (a INTEGER NOT NULL NOT NULL)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hashismn(ismn)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_shdepend
;
----------
[CREATE_TABLE] CREATE TABLE pxtest3(fff int)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_para(x int, y int) PARTITION BY RANGE(x)
;
----------
[CREATE_TABLE] create table mlparted_tab_part3 partition of mlparted_tab for values in (3)
;
----------
[DROP_TABLE] DROP TABLE CASE2_TBL
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_user_mapping
;
----------
[DROP_LIBRARY] DROP EXTENSION IF EXISTS test_extension_exists
;
----------
[CREATE_TABLE] CREATE TABLE firstparent (tomorrow date default now()::date + 1)
;
----------
[CREATE_TABLE] -- view on top of view

CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified')
;
----------
[CREATE_TABLE] CREATE TABLE collate_test1 (
    a int,
    b text COLLATE "C" NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tbl_include_unique1" (
    "c1" integer,
    "c2" integer,
    "c3" integer,
    "c4" "box"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION on_login_proc() RETURNS event_trigger AS $$
BEGIN
  INSERT INTO user_logins (who) VALUES (SESSION_USER);
  IF SESSION_USER = 'regress_mallory' THEN
    RAISE EXCEPTION 'Hello %! You are NOT welcome here!', SESSION_USER;
  END IF;
  RAISE NOTICE 'Hello %! You are welcome!', SESSION_USER;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
----------
[RENAME_PROG_OBJ] ALTER AGGREGATE split_agg_probe.sum_int(integer) RENAME TO sum_int_renamed
;
----------
[CREATE_TABLE] CREATE TABLE rule_merge1 (a int, b text)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a < b WITH CHECK OPTION
;
----------
[CREATE_TABLE] CREATE TABLE test9b (a int, b int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ok to have a partition's own constraints though
ALTER TABLE fd_pt2_1 ALTER c3 SET NOT NULL
;
----------
[CREATE_VIEW(SELECT,METADATA,PERFORMANCE)] CREATE VIEW pg_stats_ext WITH (security_barrier) AS
    SELECT cn.nspname AS schemaname,
           c.relname AS tablename,
           sn.nspname AS statistics_schemaname,
           s.stxname AS statistics_name,
           pg_get_userbyid(s.stxowner) AS statistics_owner,
           ( SELECT array_agg(a.attname ORDER BY a.attnum)
             FROM unnest(s.stxkeys) k
                  JOIN pg_attribute a
                       ON (a.attrelid = s.stxrelid AND a.attnum = k)
           ) AS attnames,
           s.stxkind AS kinds,
           sd.stxdndistinct AS n_distinct,
           sd.stxddependencies AS dependencies,
           m.most_common_vals,
           m.most_common_val_nulls,
           m.most_common_freqs,
           m.most_common_base_freqs
    FROM pg_statistic_ext s JOIN pg_class c ON (c.oid = s.stxrelid)
         JOIN pg_statistic_ext_data sd ON (s.oid = sd.stxoid)
         LEFT JOIN pg_namespace cn ON (cn.oid = c.relnamespace)
         LEFT JOIN pg_namespace sn ON (sn.oid = s.stxnamespace)
         LEFT JOIN LATERAL
                   ( SELECT array_agg(values) AS most_common_vals,
                            array_agg(nulls) AS most_common_val_nulls,
                            array_agg(frequency) AS most_common_freqs,
                            array_agg(base_frequency) AS most_common_base_freqs
                     FROM pg_mcv_list_items(sd.stxdmcv)
                   ) m ON sd.stxdmcv IS NOT NULL
    WHERE NOT EXISTS
              ( SELECT 1
                FROM unnest(stxkeys) k
                     JOIN pg_attribute a
                          ON (a.attrelid = s.stxrelid AND a.attnum = k)
                WHERE NOT has_column_privilege(c.oid, a.attnum, 'select') )
    AND (c.relrowsecurity = false OR NOT row_security_active(c.oid))
;
----------
[CREATE_TABLE] -- Use a temp table so that effects of VACUUM are predictable
CREATE TEMP TABLE test1 (a int, b int)
;
----------
[CREATE_PROG_OBJ] --
-- bt_metap()
--
CREATE FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int4,
    OUT level int4,
    OUT fastroot int4,
    OUT fastlevel int4)
AS '$libdir/pageinspect', 'bt_metap'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- alter table / alter column [set/drop] not null tests
-- try altering system catalogs, should fail
alter table pg_class alter column relname drop not null
;
----------
[TRUNCATE_TABLE] TRUNCATE test_tab1
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 cidr, ftest2 timestamp, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable)
;
----------
[ADD_INDEX] CREATE INDEX t2_btree ON s4.t2 USING BTREE (i)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_52.pg_temp_29691
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX bttest_unique_idx1
						ON bttest_unique1
						USING btree (i int4_custom_ops1)
						WITH (deduplicate_items = off)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- not supported yet
ALTER TABLE regtest_table ALTER b DROP DEFAULT
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27555_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table donothingbrtrig_test2 (c text, b text, a int)
;
----------
[DROP_TYPE] drop type rngfunc_type cascade
;
----------
[DROP_TABLE] DROP TABLE inh_nn_lvl1, inh_nn_lvl2, inh_nn_lvl3
;
----------
[ALTER_SEQUENCE] -- max will be adjusted
ALTER SEQUENCE sequence_test12 AS int
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_m" (
    "a" integer,
    "b" integer,
    "c" integer
)
PARTITION BY RANGE (((("b" + "a") / 2)), "b")
;
----------
[CREATE_TABLE] CREATE TABLE tt8(a int)
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw add operator public.=^(int, int)
;
----------
[TRUNCATE_TABLE] -- 55
TRUNCATE tru_ftable
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal3
  ADD CONSTRAINT temporal3_uq
  UNIQUE USING INDEX idx_temporal3_uq
;
----------
[CREATE_TABLE] CREATE TABLE prt1_l_p3_p2 PARTITION OF prt1_l_p3 FOR VALUES FROM (13) TO (25)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_ts_config_map
;
----------
[CREATE_POLICY] ---
--- CREATE_RULE
---


CREATE RULE rule_1 AS
  ON INSERT
  TO datatype_table
  DO NOTHING
;
----------
[CREATE_TABLE] create table inh_parent (a int not null)
;
----------
[CREATE_TABLE] CREATE TABLE array_gin_test (a int[])
;
