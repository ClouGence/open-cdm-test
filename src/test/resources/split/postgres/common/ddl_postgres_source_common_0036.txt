VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_parameter_acl
;
CREATE TRIGGER d15_insert_trig
  AFTER INSERT ON part_d_15_20 for each statement execute procedure trigfunc()
;
create function test_ret_rec_dyn(int) returns record as '
DECLARE
	retval RECORD;
BEGIN
	IF $1 > 10 THEN
		SELECT INTO retval 5, 10, 15;
		RETURN retval;
	ELSE
		SELECT INTO retval 50, 5::numeric, ''xxx''::text;
		RETURN retval;
	END IF;
END;' language plpgsql
;
-- Test row-level locks held by prepared transactions
CREATE TABLE pxtest_rowlock (id int PRIMARY KEY, data text)
;
SECURITY LABEL ON ROLE regress_seclabel_user1 IS 'classified'
;
-- DEFAULT expression
CREATE OR REPLACE FUNCTION ret_setint() RETURNS SETOF integer AS
$$
BEGIN
    RETURN QUERY EXECUTE 'select 1 union all select 1';
END;
$$
LANGUAGE plpgsql IMMUTABLE
;
CREATE FUNCTION dblink_connect (text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_tablespace
;
CREATE FUNCTION pgp_pub_decrypt_bytea(bytea, bytea)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_bytea'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create foreign table foo2 (f3 int) inherits (foo)
  server loopback options (table_name 'loct1')
;
create table convslot_test_parent (id int primary key, val int)
partition by range (id)
;
-- test dropped columns in dblink_build_sql_insert, dblink_build_sql_update
CREATE TEMP TABLE test_dropped
(
	col1 INT NOT NULL DEFAULT 111,
	id SERIAL PRIMARY KEY,
	col2 INT NOT NULL DEFAULT 112,
	col2b INT NOT NULL DEFAULT 113
)
;
create table some_table (id int, data text)
;
alter table idxpart attach partition idxpart0 default
;
CREATE TABLE tab5 (a int PRIMARY KEY, b int, d int)
;
CREATE INDEX idx_oid ON test_oid USING gin (i)
;
do $$
declare r1 two_int8s; r2 record; x int8;
begin
  r1 := retc(345);
  perform getq1(r1);
  x := getq1(r1);
  raise notice 'x = %', x;
  r2 := retc(346);
  perform getq1(r2);
  x := getq1(r2);
  raise notice 'x = %', x;
end$$
;
ALTER TABLE pg_temp_9.pg_temp_38079_2 ADD COLUMN newdata pg_temp_9.pg_temp_38079
;
-- Generated columns in column list, then set 'publish_generated_columns'='none'
ALTER PUBLICATION pub2 SET (publish_generated_columns = none)
;
CREATE TABLE iportaltest (
	i		int4,
	d		float4,
	p		polygon
)
;
-- should be rejected since its inherited
alter table gc1 drop column name
;
ALTER OPERATOR FAMILY gist_seg_ops USING gist drop function 4 (seg)
;
-- constraint now unneeded
ALTER TABLE main_table DROP CONSTRAINT main_table_a_key
;
CREATE SUBSCRIPTION testsub2 CONNECTION 'port=14075 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION testpub2
;
CREATE TABLE ref1 PARTITION OF ref FOR VALUES IN (1)
;
CREATE VIEW regtest_view AS SELECT * FROM regtest_table WHERE x < 100
;
CREATE VIEW tid_view_no_ctid AS SELECT a FROM tid_tab
;
-- Clean up
DROP EVENT TRIGGER regress_reindex_start
;
ALTER TABLE fd_pt1 ADD COLUMN c4 integer
;
ALTER INDEX pg_toast.pg_toast_28198_index RENAME TO reind_con_toast_idx
;
CREATE SEQUENCE s1.seq2
;
CREATE TABLE IF NOT EXISTS ext_cine_tab2 AS SELECT 42 AS y
;
VACUUM pg_catalog.pg_publication_namespace
;
DROP TABLE test_add_column
;
create table iocdu_tt_parted (a int primary key, b text) partition by list (a)
;
do $$ declare a int[];
begin a := array_agg(x) from (values(1),(2),(3)) v(x); raise notice 'a = %', a; end$$
;
ALTER TABLE tt9 ADD CONSTRAINT foo CHECK(c > 4)
;
CREATE FUNCTION "public"."return_refcursor"("rc" "refcursor") RETURNS "refcursor"
    LANGUAGE "plpgsql"
    AS $$
begin
    open rc for select a from rc_test;
    return rc;
end
$$
;
ALTER TYPE nosuchtype ADD ATTRIBUTE b text
;
ALTER INDEX t_a_idx ATTACH PARTITION tp_pkey
;
CREATE FUNCTION gin_leafpage_items(IN page bytea,
    OUT first_tid tid,
    OUT nbytes int2,
    OUT tids tid[])
RETURNS SETOF record
AS '$libdir/pageinspect', 'gin_leafpage_items'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TYPE city_budget (
   internallength = 16,
   input = int44in,
   output = int44out,
   element = int4,
   category = 'x',   -- just to verify the system will take it
   preferred = true  -- ditto
)
;
DROP TABLE rls_test_permissive
;
ALTER OPERATOR FAMILY alt_opf1 USING hash RENAME TO alt_opf2
;
ALTER EXTENSION hstore DROP TYPE split_extension_next.ext_mood
;
create table ext8_table1 (f1 posint)
;
create cast (xfloat8 as bigint) without function
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "numhword" WITH "simple"
;
create table child () inherits (parent)
;
CREATE OR REPLACE PROCEDURE sum_two(i int, j int) AS $$
DECLARE
  r int;
BEGIN
  SELECT (i + j)::int INTO r;
END; $$ LANGUAGE plpgsql
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_parameter_acl
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER d15_insert_trig
  AFTER INSERT ON part_d_15_20 for each statement execute procedure trigfunc()
;
----------
[CREATE_PROG_OBJ] create function test_ret_rec_dyn(int) returns record as '
DECLARE
	retval RECORD;
BEGIN
	IF $1 > 10 THEN
		SELECT INTO retval 5, 10, 15;
		RETURN retval;
	ELSE
		SELECT INTO retval 50, 5::numeric, ''xxx''::text;
		RETURN retval;
	END IF;
END;' language plpgsql
;
----------
[CREATE_TABLE] -- Test row-level locks held by prepared transactions
CREATE TABLE pxtest_rowlock (id int PRIMARY KEY, data text)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON ROLE regress_seclabel_user1 IS 'classified'
;
----------
[CREATE_PROG_OBJ] -- DEFAULT expression
CREATE OR REPLACE FUNCTION ret_setint() RETURNS SETOF integer AS
$$
BEGIN
    RETURN QUERY EXECUTE 'select 1 union all select 1';
END;
$$
LANGUAGE plpgsql IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_connect (text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_tablespace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgp_pub_decrypt_bytea(bytea, bytea)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_bytea'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create foreign table foo2 (f3 int) inherits (foo)
  server loopback options (table_name 'loct1')
;
----------
[CREATE_TABLE] create table convslot_test_parent (id int primary key, val int)
partition by range (id)
;
----------
[CREATE_TABLE] -- test dropped columns in dblink_build_sql_insert, dblink_build_sql_update
CREATE TEMP TABLE test_dropped
(
	col1 INT NOT NULL DEFAULT 111,
	id SERIAL PRIMARY KEY,
	col2 INT NOT NULL DEFAULT 112,
	col2b INT NOT NULL DEFAULT 113
)
;
----------
[CREATE_TABLE] create table some_table (id int, data text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart0 default
;
----------
[CREATE_TABLE] CREATE TABLE tab5 (a int PRIMARY KEY, b int, d int)
;
----------
[ADD_INDEX] CREATE INDEX idx_oid ON test_oid USING gin (i)
;
----------
[BLOCK] do $$
declare r1 two_int8s; r2 record; x int8;
begin
  r1 := retc(345);
  perform getq1(r1);
  x := getq1(r1);
  raise notice 'x = %', x;
  r2 := retc(346);
  perform getq1(r2);
  x := getq1(r2);
  raise notice 'x = %', x;
end$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_38079_2 ADD COLUMN newdata pg_temp_9.pg_temp_38079
;
----------
[ALTER_PUB_SUB] -- Generated columns in column list, then set 'publish_generated_columns'='none'
ALTER PUBLICATION pub2 SET (publish_generated_columns = none)
;
----------
[CREATE_TABLE] CREATE TABLE iportaltest (
	i		int4,
	d		float4,
	p		polygon
)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- should be rejected since its inherited
alter table gc1 drop column name
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_seg_ops USING gist drop function 4 (seg)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- constraint now unneeded
ALTER TABLE main_table DROP CONSTRAINT main_table_a_key
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub2 CONNECTION 'port=14075 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION testpub2
;
----------
[CREATE_TABLE] CREATE TABLE ref1 PARTITION OF ref FOR VALUES IN (1)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW regtest_view AS SELECT * FROM regtest_table WHERE x < 100
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW tid_view_no_ctid AS SELECT a FROM tid_tab
;
----------
[DROP_TRIGGER] -- Clean up
DROP EVENT TRIGGER regress_reindex_start
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE fd_pt1 ADD COLUMN c4 integer
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28198_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE s1.seq2
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE IF NOT EXISTS ext_cine_tab2 AS SELECT 42 AS y
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_publication_namespace
;
----------
[DROP_TABLE] DROP TABLE test_add_column
;
----------
[CREATE_TABLE] create table iocdu_tt_parted (a int primary key, b text) partition by list (a)
;
----------
[BLOCK] do $$ declare a int[];
begin a := array_agg(x) from (values(1),(2),(3)) v(x); raise notice 'a = %', a; end$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD CONSTRAINT foo CHECK(c > 4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."return_refcursor"("rc" "refcursor") RETURNS "refcursor"
    LANGUAGE "plpgsql"
    AS $$
begin
    open rc for select a from rc_test;
    return rc;
end
$$
;
----------
[ALTER_TYPE] ALTER TYPE nosuchtype ADD ATTRIBUTE b text
;
----------
[ALTER_INDEX] ALTER INDEX t_a_idx ATTACH PARTITION tp_pkey
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_leafpage_items(IN page bytea,
    OUT first_tid tid,
    OUT nbytes int2,
    OUT tids tid[])
RETURNS SETOF record
AS '$libdir/pageinspect', 'gin_leafpage_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TYPE] CREATE TYPE city_budget (
   internallength = 16,
   input = int44in,
   output = int44out,
   element = int4,
   category = 'x',   -- just to verify the system will take it
   preferred = true  -- ditto
)
;
----------
[DROP_TABLE] DROP TABLE rls_test_permissive
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf1 USING hash RENAME TO alt_opf2
;
----------
[ALTER_LIBRARY] ALTER EXTENSION hstore DROP TYPE split_extension_next.ext_mood
;
----------
[CREATE_TABLE] create table ext8_table1 (f1 posint)
;
----------
[CREATE_PROG_OBJ] create cast (xfloat8 as bigint) without function
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "numhword" WITH "simple"
;
----------
[CREATE_TABLE] create table child () inherits (parent)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE PROCEDURE sum_two(i int, j int) AS $$
DECLARE
  r int;
BEGIN
  SELECT (i + j)::int INTO r;
END; $$ LANGUAGE plpgsql
;
