GRANT SELECT("prosrc") ON TABLE "pg_catalog"."pg_proc" TO "zyc"
;
-- Should now have direct table access to parent, but see nothing and leak nothing
SET SESSION AUTHORIZATION regress_stats_user1
;
set local enable_hashjoin = on
;
EXECUTE dumpFunc('33956')
;
REINDEX TABLE pg_catalog.pg_am
;
CREATE STATISTICS mcv_lists_stats (mcv) ON a, b, c, ia FROM mcv_lists
;
create role regress_parallel_worker
;
LOCK TABLE "public"."polygon_tbl" IN ACCESS SHARE MODE
;
EXECUTE dumpFunc('35508')
;
-- failed (not owner)
ALTER STATISTICS alt_stat2 OWNER TO regress_alter_generic_user3
;
------------------------------------------------------------------------------------------
[GRANT] GRANT SELECT("prosrc") ON TABLE "pg_catalog"."pg_proc" TO "zyc"
;
----------
[SWITCH_USER] -- Should now have direct table access to parent, but see nothing and leak nothing
SET SESSION AUTHORIZATION regress_stats_user1
;
----------
[SESSION_SETTING_WRITE] set local enable_hashjoin = on
;
----------
[UNSAFE] EXECUTE dumpFunc('33956')
;
----------
[ADMIN] REINDEX TABLE pg_catalog.pg_am
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS mcv_lists_stats (mcv) ON a, b, c, ia FROM mcv_lists
;
----------
[CREATE_ROLE] create role regress_parallel_worker
;
----------
[SESSION_LOCK] LOCK TABLE "public"."polygon_tbl" IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpFunc('35508')
;
----------
[TRANSFER_PRIVILEGE] -- failed (not owner)
ALTER STATISTICS alt_stat2 OWNER TO regress_alter_generic_user3
;
