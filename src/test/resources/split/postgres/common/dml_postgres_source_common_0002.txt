-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."itest9"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2087', relminmxid = '1'
WHERE oid = '"public"."array_op_test"'::pg_catalog.regclass
;
-- Assign:
insert into insertconflicttest as i values (23, 'Avocado') on conflict (key) do update set fruit = excluded.*::text
  returning *
;
INSERT INTO public.tht VALUES ('6', 476)
;
-- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '479', datminmxid = '1'
WHERE datname = 'postgres'
;
insert into rules_src values(22,23), (33,default)
;
INSERT INTO public.ths_p2 VALUES (3, 353)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13582', relminmxid = '3'
WHERE oid = '34598'
;
-- insert with child check constraint error
INSERT INTO errtst_parent(partid, shdata, data) VALUES ( '0', '1', '10')
;
INSERT INTO tab2 VALUES (2, 'def', 6)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11100', relminmxid = '1'
WHERE oid = '29962'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14849', relminmxid = '3'
WHERE oid = '"fkpart3"."pk51"'::pg_catalog.regclass
;
INSERT INTO functional_dependencies (a, b, c, filler1)
     SELECT mod(i, 23), mod(i, 29), mod(i, 31), i FROM generate_series(1,5000) s(i)
;
-- failed
---

INSERT INTO t1 VALUES (4, 'abc')
;
DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_10.pg_temp_26308_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '20385', relminmxid = '3'
WHERE oid = '46405'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11548', relminmxid = '3'
WHERE oid = '"public"."clstr_tst"'::pg_catalog.regclass
;
update stmt_trig_on_empty_upd1 set a = a where false returning a+1 as aa
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1466', relminmxid = '1'
WHERE oid = '"public"."time_tbl"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart3"."pk1"'::pg_catalog.regclass
  AND attname IN ('a')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'name'
  AND attrelid = '"public"."student"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '25928'
;
INSERT INTO decoding(blah) VALUES ('afterbb')
;
update rem1 set f2 = f2 || f2
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2063', relminmxid = '1'
WHERE oid = '17613'
;
insert into gtest34p values (1, 5)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."plt1_p3"'::pg_catalog.regclass
;
-- OK
UPDATE rw_view2 SET s = s WHERE base_a = 1.1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10082', relminmxid = '1'
WHERE oid = '"public"."rtest_nothn2"'::pg_catalog.regclass
;
INSERT INTO tbl1 VALUES ('in C, after C was promoted')
;
INSERT INTO brin_hot SELECT *, 0 FROM generate_series(1, 235)
;
INSERT INTO rls_test_restrictive VALUES ('regress_r1','regress_s1',10)
;
INSERT INTO public.ths_p1 VALUES (8, 648)
;
DELETE FROM test_tab WHERE a > 2
;
INSERT INTO LINE_TBL VALUES (line(point '(1,0)', point '(1,0)'))
;
-- retry insert with a different random-looking (but deterministic) value
-- the value is different, and so should replace either min or max in the
-- brin summary
WITH rand_value AS (SELECT string_agg(md5((-i)::text),'') AS val FROM generate_series(1,60) s(i))
INSERT INTO brintest_3
SELECT val, val, val, val FROM rand_value
;
-- should fail
UPDATE rw_view2 SET a = a + 10
;
INSERT INTO public.ths_p1 VALUES (8, 468)
;
INSERT INTO public.ths_p3 VALUES (1, 961)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9869', relminmxid = '1'
WHERE oid = '"public"."rtest_comp"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3136', relminmxid = '1'
WHERE oid = '20134'
;
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.1........'
  AND attrelid = '"public"."test_type3"'::pg_catalog.regclass
;
update utrtest set a = 3 - a from (values (1), (2)) s(x) where a = s.x
  returning *, tableoid::regclass, cmin
;
INSERT INTO public.tplain VALUES ('5', 205)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17044', relminmxid = '3'
WHERE oid = '38518'
;
INSERT INTO num_input_test(n1) VALUES ('.000_000_000_123e1_0')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17267', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p3_s1"'::pg_catalog.regclass
;
-- attempt modifications which would break RI (should all fail)
DELETE FROM transition_table_level1
  WHERE level1_no = 25
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted1"'::pg_catalog.regclass
  AND attname = v.dname
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"public"."itest7b"'::pg_catalog.regclass
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."itest9"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2087', relminmxid = '1'
WHERE oid = '"public"."array_op_test"'::pg_catalog.regclass
;
----------
[MERGE] -- Assign:
insert into insertconflicttest as i values (23, 'Avocado') on conflict (key) do update set fruit = excluded.*::text
  returning *
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 476)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '479', datminmxid = '1'
WHERE datname = 'postgres'
;
----------
[INSERT] insert into rules_src values(22,23), (33,default)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 353)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13582', relminmxid = '3'
WHERE oid = '34598'
;
----------
[INSERT|SELECT] -- insert with child check constraint error
INSERT INTO errtst_parent(partid, shdata, data) VALUES ( '0', '1', '10')
;
----------
[INSERT] INSERT INTO tab2 VALUES (2, 'def', 6)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11100', relminmxid = '1'
WHERE oid = '29962'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '14849', relminmxid = '3'
WHERE oid = '"fkpart3"."pk51"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO functional_dependencies (a, b, c, filler1)
     SELECT mod(i, 23), mod(i, 29), mod(i, 31), i FROM generate_series(1,5000) s(i)
;
----------
[INSERT] -- failed
---

INSERT INTO t1 VALUES (4, 'abc')
;
----------
[DELETE|SELECT] DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_10.pg_temp_26308_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '20385', relminmxid = '3'
WHERE oid = '46405'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11548', relminmxid = '3'
WHERE oid = '"public"."clstr_tst"'::pg_catalog.regclass
;
----------
[UPDATE] update stmt_trig_on_empty_upd1 set a = a where false returning a+1 as aa
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1466', relminmxid = '1'
WHERE oid = '"public"."time_tbl"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart3"."pk1"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'name'
  AND attrelid = '"public"."student"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '25928'
;
----------
[INSERT|SELECT] INSERT INTO decoding(blah) VALUES ('afterbb')
;
----------
[UPDATE] update rem1 set f2 = f2 || f2
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2063', relminmxid = '1'
WHERE oid = '17613'
;
----------
[INSERT] insert into gtest34p values (1, 5)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."plt1_p3"'::pg_catalog.regclass
;
----------
[UPDATE] -- OK
UPDATE rw_view2 SET s = s WHERE base_a = 1.1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10082', relminmxid = '1'
WHERE oid = '"public"."rtest_nothn2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO tbl1 VALUES ('in C, after C was promoted')
;
----------
[INSERT|SELECT] INSERT INTO brin_hot SELECT *, 0 FROM generate_series(1, 235)
;
----------
[INSERT] INSERT INTO rls_test_restrictive VALUES ('regress_r1','regress_s1',10)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 648)
;
----------
[DELETE] DELETE FROM test_tab WHERE a > 2
;
----------
[INSERT] INSERT INTO LINE_TBL VALUES (line(point '(1,0)', point '(1,0)'))
;
----------
[INSERT|SELECT] -- retry insert with a different random-looking (but deterministic) value
-- the value is different, and so should replace either min or max in the
-- brin summary
WITH rand_value AS (SELECT string_agg(md5((-i)::text),'') AS val FROM generate_series(1,60) s(i))
INSERT INTO brintest_3
SELECT val, val, val, val FROM rand_value
;
----------
[UPDATE] -- should fail
UPDATE rw_view2 SET a = a + 10
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 468)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 961)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9869', relminmxid = '1'
WHERE oid = '"public"."rtest_comp"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3136', relminmxid = '1'
WHERE oid = '20134'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.1........'
  AND attrelid = '"public"."test_type3"'::pg_catalog.regclass
;
----------
[UPDATE|SELECT] update utrtest set a = 3 - a from (values (1), (2)) s(x) where a = s.x
  returning *, tableoid::regclass, cmin
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('5', 205)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17044', relminmxid = '3'
WHERE oid = '38518'
;
----------
[INSERT|SELECT] INSERT INTO num_input_test(n1) VALUES ('.000_000_000_123e1_0')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17267', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p3_s1"'::pg_catalog.regclass
;
----------
[DELETE] -- attempt modifications which would break RI (should all fail)
DELETE FROM transition_table_level1
  WHERE level1_no = 25
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted1"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"public"."itest7b"'::pg_catalog.regclass
;
