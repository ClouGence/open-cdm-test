alter table trigpart4 attach partition trigpart42 for values from (3500) to (4000)
;
drop table attbl, atref
;
ALTER TABLE test_add_column
	ADD c6 integer
;
CREATE FUNCTION text_w_default_out(text_w_default)
   RETURNS cstring
   AS 'textout'
   LANGUAGE internal STRICT IMMUTABLE
;
CREATE FUNCTION test_assign_ordered_named_pair(x int, y int, z int)
  RETURNS ordered_named_pair AS $$
declare v ordered_named_pair := row(x, y);
begin
v.j := z;
return v;
end
$$ LANGUAGE plpgsql
;
CREATE VIEW "public"."mvtest_tvv" AS
 SELECT "sum"("mvtest_tv"."totamt") AS "grandtot"
   FROM "public"."mvtest_tv"
;
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1, ftest2) references pktable
;
create domain mynums2 as mynums
;
CREATE DATABASE copy_invalid TEMPLATE regression_invalid
;
CREATE FUNCTION trigger_func_stats () RETURNS trigger LANGUAGE plpgsql
  AS $$ BEGIN return OLD; end; $$
;
ANALYZE public.point_tbl
;
--
-- OID
--

CREATE TABLE OID_TBL(f1 oid)
;
ALTER TABLE parent ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
CREATE TRIGGER ins_trig_before_row_insert
		  BEFORE INSERT ON ins_trig
		  FOR EACH ROW EXECUTE PROCEDURE ins_trig_before_row_trig()
;
CREATE TABLE zerocol()
;
-- attribute renaming

-- no such relation
alter table nonesuchrel rename column nonesuchatt to newnonesuchatt
;
CREATE TABLE another_parttbl2 PARTITION OF another_parttbl FOR VALUES IN (2)
;
DROP FUNCTION ri_parent()
;
CREATE TABLE tbl_heap_psql(f1 int, f2 char(100)) using heap_psql
;
CREATE FUNCTION similarity_dist(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk2_pkey"
;
ANALYZE sb_1, sb_2
;
ALTER TABLE ONLY "public"."regress_constr_partition1"
    ADD CONSTRAINT "regress_constr_partition1_pkey" PRIMARY KEY ("a")
;
CREATE TABLE child () INHERITS (parent)
;
ANALYZE pg_temp_24.pg_temp_27043_2
;
--
-- Test handling of OUT parameters, including polymorphic cases.
-- Note that RETURN is optional with OUT params; we try both ways.
--

-- wrong way to do it:
create function f1(in i int, out j int) returns int as $$
begin
  return i+1;
end$$ language plpgsql
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_policy
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.datetime() < $.datetime("HH:MI TZH"))'))
;
ALTER TABLE itest18 SET LOGGED
;
create function public.whoami() returns text
  as $$select 'public'::text$$ language sql
;
CREATE TABLE part10 PARTITION OF range_parted3 FOR VALUES FROM (1, minvalue) TO (1, 1)
;
CREATE TABLE test_pg_dump_t1 (c1 int)
;
DROP TABLE trans_foo
;
create function build_ordered_int8s_a(i int8, j int8) returns ordered_int8s[] as $$
begin return array[row(i,j), row(i,j+1)]; end
$$ language plpgsql
;
CREATE TABLE "public"."gtest22c" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 4)) STORED
)
;
-- remapping in both direction
CREATE TABLE errtst_child_reorder (
    data int NOT NULL DEFAULT 0,
    shdata int not null,
    partid int not null,
    CONSTRAINT shdata_small CHECK(shdata < 3),
    CHECK(data < 10)
)
;
CREATE TABLE pa_target (logts timestamp, tid integer, balance float, val text)
	PARTITION BY RANGE (logts)
;
COMMENT ON AGGREGATE newavg (int4) IS 'an agg comment'
;
DROP DATABASE testdb
;
--
-- test PRIMARY KEY and UNIQUE constraints' interaction with domains
--

-- range over domain:
CREATE DOMAIN int4_d as integer check (value <> 10)
;
ANALYZE vactst, does_not_exist, vacparted
;
ALTER TABLE pg_toast.pg_toast_28107 RENAME TO reind_con_toast
;
create table c1 (f1 int not null) inherits(p1)
;
ALTER TABLE parent RESET (fillfactor)
;
TRUNCATE lotest_stash_values
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_constraint
;
DROP TABLE pg_temp_9.pg_temp_26686_2, pg_temp_9.pg_temp_26686
;
ANALYZE public.ndistinct
;
DO LANGUAGE plpgsql $$
BEGIN
  COPY copy1 FROM '/src/src/pl/plpgsql/src/data/copy1.data';
END;
$$
;
create function f1(in i int, out j int, out k text) returns setof record as $$
begin
  j := i+1;
  k := 'foo';
  return next;
  j := j+1;
  k := 'foot';
  return next;
end$$ language plpgsql
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] alter table trigpart4 attach partition trigpart42 for values from (3500) to (4000)
;
----------
[DROP_TABLE] drop table attbl, atref
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD c6 integer
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION text_w_default_out(text_w_default)
   RETURNS cstring
   AS 'textout'
   LANGUAGE internal STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_assign_ordered_named_pair(x int, y int, z int)
  RETURNS ordered_named_pair AS $$
declare v ordered_named_pair := row(x, y);
begin
v.j := z;
return v;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."mvtest_tvv" AS
 SELECT "sum"("mvtest_tv"."totamt") AS "grandtot"
   FROM "public"."mvtest_tv"
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1, ftest2) references pktable
;
----------
[CREATE_TYPE] create domain mynums2 as mynums
;
----------
[CREATE_CATALOG] CREATE DATABASE copy_invalid TEMPLATE regression_invalid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trigger_func_stats () RETURNS trigger LANGUAGE plpgsql
  AS $$ BEGIN return OLD; end; $$
;
----------
[ADMIN_TABLE] ANALYZE public.point_tbl
;
----------
[CREATE_TABLE] --
-- OID
--

CREATE TABLE OID_TBL(f1 oid)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE parent ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER ins_trig_before_row_insert
		  BEFORE INSERT ON ins_trig
		  FOR EACH ROW EXECUTE PROCEDURE ins_trig_before_row_trig()
;
----------
[CREATE_TABLE] CREATE TABLE zerocol()
;
----------
[RENAME_COLUMN] -- attribute renaming

-- no such relation
alter table nonesuchrel rename column nonesuchatt to newnonesuchatt
;
----------
[CREATE_TABLE] CREATE TABLE another_parttbl2 PARTITION OF another_parttbl FOR VALUES IN (2)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION ri_parent()
;
----------
[CREATE_TABLE] CREATE TABLE tbl_heap_psql(f1 int, f2 char(100)) using heap_psql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION similarity_dist(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_INDEX] ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk2_pkey"
;
----------
[ADMIN_TABLE] ANALYZE sb_1, sb_2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."regress_constr_partition1"
    ADD CONSTRAINT "regress_constr_partition1_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE child () INHERITS (parent)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_27043_2
;
----------
[CREATE_PROG_OBJ] --
-- Test handling of OUT parameters, including polymorphic cases.
-- Note that RETURN is optional with OUT params; we try both ways.
--

-- wrong way to do it:
create function f1(in i int, out j int) returns int as $$
begin
  return i+1;
end$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_policy
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.datetime() < $.datetime("HH:MI TZH"))'))
;
----------
[ALTER_TABLE] ALTER TABLE itest18 SET LOGGED
;
----------
[CREATE_PROG_OBJ] create function public.whoami() returns text
  as $$select 'public'::text$$ language sql
;
----------
[CREATE_TABLE] CREATE TABLE part10 PARTITION OF range_parted3 FOR VALUES FROM (1, minvalue) TO (1, 1)
;
----------
[CREATE_TABLE] CREATE TABLE test_pg_dump_t1 (c1 int)
;
----------
[DROP_TABLE] DROP TABLE trans_foo
;
----------
[CREATE_PROG_OBJ] create function build_ordered_int8s_a(i int8, j int8) returns ordered_int8s[] as $$
begin return array[row(i,j), row(i,j+1)]; end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest22c" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 4)) STORED
)
;
----------
[CREATE_TABLE] -- remapping in both direction
CREATE TABLE errtst_child_reorder (
    data int NOT NULL DEFAULT 0,
    shdata int not null,
    partid int not null,
    CONSTRAINT shdata_small CHECK(shdata < 3),
    CHECK(data < 10)
)
;
----------
[CREATE_TABLE] CREATE TABLE pa_target (logts timestamp, tid integer, balance float, val text)
	PARTITION BY RANGE (logts)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE newavg (int4) IS 'an agg comment'
;
----------
[DROP_CATALOG] DROP DATABASE testdb
;
----------
[CREATE_TYPE] --
-- test PRIMARY KEY and UNIQUE constraints' interaction with domains
--

-- range over domain:
CREATE DOMAIN int4_d as integer check (value <> 10)
;
----------
[ADMIN_TABLE] ANALYZE vactst, does_not_exist, vacparted
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28107 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] create table c1 (f1 int not null) inherits(p1)
;
----------
[ALTER_TABLE] ALTER TABLE parent RESET (fillfactor)
;
----------
[TRUNCATE_TABLE] TRUNCATE lotest_stash_values
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_constraint
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_26686_2, pg_temp_9.pg_temp_26686
;
----------
[ADMIN_TABLE] ANALYZE public.ndistinct
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
  COPY copy1 FROM '/src/src/pl/plpgsql/src/data/copy1.data';
END;
$$
;
----------
[CREATE_PROG_OBJ] create function f1(in i int, out j int, out k text) returns setof record as $$
begin
  j := i+1;
  k := 'foo';
  return next;
  j := j+1;
  k := 'foot';
  return next;
end$$ language plpgsql
;
