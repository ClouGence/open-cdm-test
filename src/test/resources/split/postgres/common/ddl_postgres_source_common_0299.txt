ALTER PUBLICATION testpub_fortable ADD TABLE testpub_tbl6
;
CREATE TEMPORARY TABLE tt1 AS SELECT 1 AS a
;
create function polyf(anycompatiblemultirange) returns anycompatible
as 'select lower($1);' language sql
;
create foreign table remp1 (a int check (a in (1)), b text) server loopback options (table_name 'loct1')
;
-- ALTER TABLE when attaching or detaching an index to a partition.
create index idxpart_c on only idxpart (c)
;
ALTER TABLE fk_partitioned_fk_3_1 ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk
;
create rule rtest_emp_ins as on insert to rtest_emp do
	insert into rtest_emplog values (new.ename, current_user,
			'hired', new.salary, '0.00')
;
CREATE TYPE dump_test.range_type_custom AS RANGE (
							 subtype = int,
							 subtype_opclass = dump_test.op_class_custom)
;
ANALYZE pg_temp_4.pg_temp_37808_2
;
DROP TABLE range_parted2
;
CREATE OPERATOR > (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_gt,
	COMMUTATOR = '<', NEGATOR = '<=',
	RESTRICT = scalargtsel, JOIN = scalargtjoinsel
)
;
ALTER TEXT SEARCH CONFIGURATION basque ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
--
-- DATE
--

CREATE TABLE DATE_TBL (f1 date)
;
CREATE UNLOGGED SEQUENCE seq_unlogged
;
CREATE TABLE notnull_parent_upg (a int)
;
-- fail

-- invalid: non-lowercase quoted identifiers
CREATE COLLATION case_coll ("Lc_Collate" = "POSIX", "Lc_Ctype" = "POSIX")
;
create table tt11 (x int, y int)
;
CREATE RULE InsertRule AS
    ON INSERT TO rule_v1
    DO INSTEAD
        INSERT INTO rule_t1 VALUES(new.a)
;
ALTER TABLE ONLY "public"."mlparted_def" DROP COLUMN "........pg.dropped.6........"
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE FUNCTION isnge(ismn, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP TABLE invoice
;
VACUUM FULL regtest_ptable
;
ALTER LANGUAGE alt_lang1 RENAME TO alt_lang2
;
CREATE RULE "_RETURN" AS ON SELECT TO t DO INSTEAD
  SELECT * FROM generate_series(1,5) t0(c)
;
CREATE FUNCTION isneq(issn, issn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create table mcrparted7_gt_common_lt_d partition of mcrparted for values from ('common', maxvalue) to ('d', minvalue)
;
CREATE FUNCTION ok_cmp3 (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT public.ok_cmp($1, $2);
	$$
;
CREATE TABLE test_tbl3 (c) AS SELECT '(1)'::test_type3
;
CREATE FUNCTION gbt_uuid_union(internal, internal)
RETURNS gbtreekey32
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- test whole-row Vars in ON CONFLICT expressions
create table parted_conflict (a int, b text, c int) partition by range (a)
;
CREATE TABLE regress_tblspace_test_tbl (num1 bigint, num2 double precision, t text)
;
ALTER TABLE ONLY "public"."trigger_parted_p1" ATTACH PARTITION "public"."trigger_parted_p1_1" FOR VALUES IN (1)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
CREATE PUBLICATION tap_pub_B FOR TABLE tab_full, test_tab
;
CREATE VIEW aliased_view_2 AS
  select * from tt1 a1
    where exists (select 1 from tx1 where a1.f1 = tx1.x1)
;
VACUUM (FREEZE) pg_catalog.pg_auth_members
;
DROP TABLE table_dropped_index_no_pk
;
ANALYZE pg_temp_32.pg_temp_27252_2
;
vacuum analyze gist_point_tbl
;
CREATE TABLE partitioned (
	a1 int,
	a2 int
) PARTITION BY LIST (a1, a2)
;
ALTER TABLE ONLY "public"."notnull_part1_upg" ATTACH PARTITION "public"."notnull_part1_2_upg" FOR VALUES IN (3, 4)
;
--
-- Test user-defined range of floats
--

--should fail
create type float8range as range (subtype=float8, subtype_diff=float4mi)
;
CREATE FUNCTION functest_F_3(int) RETURNS bool LANGUAGE 'sql'
       RETURNS NULL ON NULL INPUT AS 'SELECT $1 < 50'
;
CREATE FUNCTION "public"."test_event_trigger_guc"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
	obj record;
BEGIN
	FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
	LOOP
		RAISE NOTICE '% dropped %', tg_tag, obj.object_type;
	END LOOP;
END;
$$
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."c_star" INHERIT "public"."a_star"
;
CREATE FUNCTION exists_any(hstore,text[])
RETURNS bool
AS '$libdir/hstore','hstore_exists_any'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
alter table pk2 alter a set not null
;
CREATE FUNCTION gbt_bit_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE bmscantest (a int, b int, t text) WITH (autovacuum_enabled = false)
;
------------------------------------------------------------------------------------------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_fortable ADD TABLE testpub_tbl6
;
----------
[CREATE_TABLE|SELECT] CREATE TEMPORARY TABLE tt1 AS SELECT 1 AS a
;
----------
[CREATE_PROG_OBJ] create function polyf(anycompatiblemultirange) returns anycompatible
as 'select lower($1);' language sql
;
----------
[CREATE_TABLE] create foreign table remp1 (a int check (a in (1)), b text) server loopback options (table_name 'loct1')
;
----------
[ADD_INDEX] -- ALTER TABLE when attaching or detaching an index to a partition.
create index idxpart_c on only idxpart (c)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk_3_1 ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk
;
----------
[CREATE_POLICY] create rule rtest_emp_ins as on insert to rtest_emp do
	insert into rtest_emplog values (new.ename, current_user,
			'hired', new.salary, '0.00')
;
----------
[CREATE_TYPE] CREATE TYPE dump_test.range_type_custom AS RANGE (
							 subtype = int,
							 subtype_opclass = dump_test.op_class_custom)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37808_2
;
----------
[DROP_TABLE] DROP TABLE range_parted2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_gt,
	COMMUTATOR = '<', NEGATOR = '<=',
	RESTRICT = scalargtsel, JOIN = scalargtjoinsel
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION basque ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_TABLE] --
-- DATE
--

CREATE TABLE DATE_TBL (f1 date)
;
----------
[CREATE_SEQUENCE] CREATE UNLOGGED SEQUENCE seq_unlogged
;
----------
[CREATE_TABLE] CREATE TABLE notnull_parent_upg (a int)
;
----------
[CREATE_POLICY] -- fail

-- invalid: non-lowercase quoted identifiers
CREATE COLLATION case_coll ("Lc_Collate" = "POSIX", "Lc_Ctype" = "POSIX")
;
----------
[CREATE_TABLE] create table tt11 (x int, y int)
;
----------
[CREATE_POLICY] CREATE RULE InsertRule AS
    ON INSERT TO rule_v1
    DO INSTEAD
        INSERT INTO rule_t1 VALUES(new.a)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def" DROP COLUMN "........pg.dropped.6........"
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ismn, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE invoice
;
----------
[ADMIN_TABLE] VACUUM FULL regtest_ptable
;
----------
[RENAME_PROG_OBJ] ALTER LANGUAGE alt_lang1 RENAME TO alt_lang2
;
----------
[CREATE_POLICY] CREATE RULE "_RETURN" AS ON SELECT TO t DO INSTEAD
  SELECT * FROM generate_series(1,5) t0(c)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(issn, issn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] create table mcrparted7_gt_common_lt_d partition of mcrparted for values from ('common', maxvalue) to ('d', minvalue)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ok_cmp3 (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT public.ok_cmp($1, $2);
	$$
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_tbl3 (c) AS SELECT '(1)'::test_type3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_uuid_union(internal, internal)
RETURNS gbtreekey32
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] -- test whole-row Vars in ON CONFLICT expressions
create table parted_conflict (a int, b text, c int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE regress_tblspace_test_tbl (num1 bigint, num2 double precision, t text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."trigger_parted_p1" ATTACH PARTITION "public"."trigger_parted_p1_1" FOR VALUES IN (1)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_B FOR TABLE tab_full, test_tab
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW aliased_view_2 AS
  select * from tt1 a1
    where exists (select 1 from tx1 where a1.f1 = tx1.x1)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_auth_members
;
----------
[DROP_TABLE] DROP TABLE table_dropped_index_no_pk
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_27252_2
;
----------
[ADMIN_TABLE] vacuum analyze gist_point_tbl
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a1 int,
	a2 int
) PARTITION BY LIST (a1, a2)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."notnull_part1_upg" ATTACH PARTITION "public"."notnull_part1_2_upg" FOR VALUES IN (3, 4)
;
----------
[CREATE_TYPE] --
-- Test user-defined range of floats
--

--should fail
create type float8range as range (subtype=float8, subtype_diff=float4mi)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_F_3(int) RETURNS bool LANGUAGE 'sql'
       RETURNS NULL ON NULL INPUT AS 'SELECT $1 < 50'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_event_trigger_guc"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
	obj record;
BEGIN
	FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
	LOOP
		RAISE NOTICE '% dropped %', tg_tag, obj.object_type;
	END LOOP;
END;
$$
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."c_star" INHERIT "public"."a_star"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION exists_any(hstore,text[])
RETURNS bool
AS '$libdir/hstore','hstore_exists_any'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table pk2 alter a set not null
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bit_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE bmscantest (a int, b int, t text) WITH (autovacuum_enabled = false)
;
