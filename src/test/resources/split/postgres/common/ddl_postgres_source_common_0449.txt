--error

TRUNCATE notnull_tbl1
;
CREATE FUNCTION grow_rel(rel regclass, nblocks int)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
CREATE TEXT SEARCH CONFIGURATION dummy_tst (COPY=english)
;
--
-- test multi inheritance tree
--
create table inh_parent(f1 int not null)
;
alter text search template alter1.tmpl set schema alter2
;
-- tricky behavior: according to SQL99, if a deferred constraint is set
-- to 'immediate' mode, it should be checked for validity *immediately*,
-- not when the current transaction commits (i.e. the mode change applies
-- retroactively)
CREATE TABLE pktable (
	id		INT4 PRIMARY KEY,
	other	INT4
)
;
-- fails
CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C.UTF8')
;
CALL test_proc6(2, 3, 4)
;
ALTER INDEX pg_toast.pg_toast_30533_index RENAME TO reind_con_toast_idx
;
DROP FUNCTION IF EXISTS test_ambiguous_funcname
;
CREATE FUNCTION gbt_uuid_same(gbtreekey32, gbtreekey32, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE "public"."pagg_tab_m_p2" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26514_2 (tid pg_catalog.tid)
;
VACUUM (PARALLEL 0) pg_catalog.pg_publication
;
alter table rewriteme add column baz int default 0
;
CREATE TYPE bogus AS ENUM('good', 'bad', 'ugly')
;
--
-- For fkeys2:
-- 	ON DELETE/UPDATE (pkey23) RESTRICT:
-- 		fkeys (fkey3)
--
create trigger check_fkeys2_fkey_restrict
	before delete or update on fkeys2
	for each row
	execute procedure check_foreign_key (1, 'restrict', 'pkey23', 'fkeys', 'fkey3')
;
VACUUM pg_shdepend
;
CREATE TABLE ctlt4 (a int, b text)
;
---
--- CREATE_RULE
---
--- Note: views' ON SELECT rules are tested elsewhere.
---


CREATE RULE rule_1 AS
  ON INSERT
  TO datatype_table
  DO NOTHING
;
CREATE TABLE trunc_trigger_log (tgop text, tglevel text, tgwhen text,
        tgargv text, tgtable name, rowcount bigint)
;
IMPORT FOREIGN SCHEMA nonesuch FROM SERVER nowhere INTO notthere
;
CREATE FUNCTION logging_msfunc_nonstrict(text, anyelement) RETURNS text AS
$$ SELECT COALESCE($1, '') || '+' || quote_nullable($2) $$
LANGUAGE SQL IMMUTABLE
;
ALTER TABLE attmp ADD COLUMN z int2[]
;
-- silently do nothing
DROP TABLE logged3
;
drop function excpt_test4()
;
-- check error cases that would require run-time type coercion
create function least_accum(int8, int8) returns int8 language sql as
  'select least($1, $2)'
;
-- fail
drop domain mydomain cascade
;
TRUNCATE TABLE test_tab_2
;
ALTER TABLE tab1 ATTACH PARTITION tab1_part1 FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
create or replace function trigger_notice() returns trigger as $$
  begin
    raise notice 'trigger % on % % % for %', TG_NAME, TG_TABLE_NAME, TG_WHEN, TG_OP, TG_LEVEL;
    if TG_LEVEL = 'ROW' then
       return NEW;
    end if;
    return null;
  end;
  $$ language plpgsql
;
CREATE FUNCTION gin_extract_value_cidr(cidr, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
create rule r4 as on delete to rules_src do notify rules_src_deletion
;
-- fail

DROP TABLE ext_cine_tab1
;
CREATE TABLE ab1 (a INTEGER, b INTEGER, c TIMESTAMP, d TIMESTAMPTZ)
;
create table coercepart_ab partition of coercepart for values in ('ab')
;
/*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
ALTER FOREIGN TABLE ft1 ADD COLUMN c5 integer DEFAULT 0
;
CREATE TABLE ATACC2 (TEST2 INT) INHERITS (ATACC1)
;
-- Fail min/max values check
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=2)
;
CREATE FOREIGN TABLE "x 4" (
  c1 double precision OPTIONS (column_name 'c1'),
  "C 2" text OPTIONS (column_name 'C 2') COLLATE pg_catalog."default",
  c3 character varying(42) OPTIONS (column_name 'c3') COLLATE pg_catalog."default"
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 'x 4')
;
-- set logged
CREATE UNLOGGED TABLE unlogged1(f1 SERIAL PRIMARY KEY, f2 TEXT)
;
CREATE OPERATOR @#@
        (leftarg = int8, rightarg = int8, procedure = int8xor)
;
CREATE TEMP TABLE pg_temp_14.pg_temp_27156_2 (tid pg_catalog.tid)
;
create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "C") to ('g')
;
DROP TRIGGER trig_row_after_update ON rem1
;
CREATE TEMP TABLE tp (a integer, b integer, PRIMARY KEY (a, b), UNIQUE (b, a))
;
ALTER OPERATOR FAMILY gist_oid_ops USING gist ADD
	FUNCTION	11  (oid, oid) gbt_oid_sortsupport (internal)
;
ANALYZE public.iface
;
ALTER TABLE pg_temp_10.pg_temp_26308_2 ADD COLUMN newdata pg_temp_10.pg_temp_26308
;
------------------------------------------------------------------------------------------
[TRUNCATE_TABLE] --error

TRUNCATE notnull_tbl1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION grow_rel(rel regclass, nblocks int)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION dummy_tst (COPY=english)
;
----------
[CREATE_TABLE] --
-- test multi inheritance tree
--
create table inh_parent(f1 int not null)
;
----------
[ALTER_POLICY] alter text search template alter1.tmpl set schema alter2
;
----------
[CREATE_TABLE] -- tricky behavior: according to SQL99, if a deferred constraint is set
-- to 'immediate' mode, it should be checked for validity *immediately*,
-- not when the current transaction commits (i.e. the mode change applies
-- retroactively)
CREATE TABLE pktable (
	id		INT4 PRIMARY KEY,
	other	INT4
)
;
----------
[CREATE_POLICY] -- fails
CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C.UTF8')
;
----------
[CALL_PROG_OBJ] CALL test_proc6(2, 3, 4)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30533_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS test_ambiguous_funcname
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_uuid_same(gbtreekey32, gbtreekey32, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_m_p2" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26514_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_publication
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table rewriteme add column baz int default 0
;
----------
[CREATE_TYPE] CREATE TYPE bogus AS ENUM('good', 'bad', 'ugly')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- For fkeys2:
-- 	ON DELETE/UPDATE (pkey23) RESTRICT:
-- 		fkeys (fkey3)
--
create trigger check_fkeys2_fkey_restrict
	before delete or update on fkeys2
	for each row
	execute procedure check_foreign_key (1, 'restrict', 'pkey23', 'fkeys', 'fkey3')
;
----------
[ADMIN_TABLE] VACUUM pg_shdepend
;
----------
[CREATE_TABLE] CREATE TABLE ctlt4 (a int, b text)
;
----------
[CREATE_POLICY] ---
--- CREATE_RULE
---
--- Note: views' ON SELECT rules are tested elsewhere.
---


CREATE RULE rule_1 AS
  ON INSERT
  TO datatype_table
  DO NOTHING
;
----------
[CREATE_TABLE] CREATE TABLE trunc_trigger_log (tgop text, tglevel text, tgwhen text,
        tgargv text, tgtable name, rowcount bigint)
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA nonesuch FROM SERVER nowhere INTO notthere
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION logging_msfunc_nonstrict(text, anyelement) RETURNS text AS
$$ SELECT COALESCE($1, '') || '+' || quote_nullable($2) $$
LANGUAGE SQL IMMUTABLE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN z int2[]
;
----------
[DROP_TABLE] -- silently do nothing
DROP TABLE logged3
;
----------
[DROP_PROG_OBJ] drop function excpt_test4()
;
----------
[CREATE_PROG_OBJ] -- check error cases that would require run-time type coercion
create function least_accum(int8, int8) returns int8 language sql as
  'select least($1, $2)'
;
----------
[DROP_TYPE] -- fail
drop domain mydomain cascade
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE test_tab_2
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab1 ATTACH PARTITION tab1_part1 FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[CREATE_PROG_OBJ] create or replace function trigger_notice() returns trigger as $$
  begin
    raise notice 'trigger % on % % % for %', TG_NAME, TG_TABLE_NAME, TG_WHEN, TG_OP, TG_LEVEL;
    if TG_LEVEL = 'ROW' then
       return NEW;
    end if;
    return null;
  end;
  $$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_cidr(cidr, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_POLICY] create rule r4 as on delete to rules_src do notify rules_src_deletion
;
----------
[DROP_TABLE] -- fail

DROP TABLE ext_cine_tab1
;
----------
[CREATE_TABLE] CREATE TABLE ab1 (a INTEGER, b INTEGER, c TIMESTAMP, d TIMESTAMPTZ)
;
----------
[CREATE_TABLE] create table coercepart_ab partition of coercepart for values in ('ab')
;
----------
[CREATE_POLICY] /*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD COLUMN c5 integer DEFAULT 0
;
----------
[CREATE_TABLE] CREATE TABLE ATACC2 (TEST2 INT) INHERITS (ATACC1)
;
----------
[CREATE_TABLE] -- Fail min/max values check
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=2)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE "x 4" (
  c1 double precision OPTIONS (column_name 'c1'),
  "C 2" text OPTIONS (column_name 'C 2') COLLATE pg_catalog."default",
  c3 character varying(42) OPTIONS (column_name 'c3') COLLATE pg_catalog."default"
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 'x 4')
;
----------
[CREATE_TABLE] -- set logged
CREATE UNLOGGED TABLE unlogged1(f1 SERIAL PRIMARY KEY, f2 TEXT)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @#@
        (leftarg = int8, rightarg = int8, procedure = int8xor)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_14.pg_temp_27156_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "C") to ('g')
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_after_update ON rem1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tp (a integer, b integer, PRIMARY KEY (a, b), UNIQUE (b, a))
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_oid_ops USING gist ADD
	FUNCTION	11  (oid, oid) gbt_oid_sortsupport (internal)
;
----------
[ADMIN_TABLE] ANALYZE public.iface
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_26308_2 ADD COLUMN newdata pg_temp_10.pg_temp_26308
;
