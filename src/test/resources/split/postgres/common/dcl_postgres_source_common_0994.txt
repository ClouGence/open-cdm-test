EXECUTE dumpTableAttach('31854')
;
CREATE STATISTICS s10 (ndistinct) ON (a+1), (b+100), (2*c) FROM ndistinct
;
DROP ROLE regress_rolecreator
;
ALTER FUNCTION "generated_stored_tests"."gtest_trigger_func4"() OWNER TO zyc
;
DEALLOCATE pgsql_fdw_prep_11
;
EXECUTE dumpTableAttach('38539')
;
ALTER TABLE clstr_3 OWNER TO regress_clstr_user
;
COPY rls_t1 (a) TO stdout
;
-- Both partitions allowed by IN clause, then both excluded again by <> clauses.
explain (analyze, costs off, summary off, timing off, buffers off)  execute q1 (1,2,2,1)
;
-- Test streaming of two-phase commits

SET synchronous_commit = on
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpTableAttach('31854')
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS s10 (ndistinct) ON (a+1), (b+100), (2*c) FROM ndistinct
;
----------
[DROP_ROLE] DROP ROLE regress_rolecreator
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "generated_stored_tests"."gtest_trigger_func4"() OWNER TO zyc
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_11
;
----------
[UNSAFE] EXECUTE dumpTableAttach('38539')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE clstr_3 OWNER TO regress_clstr_user
;
----------
[DATA_EXPORT] COPY rls_t1 (a) TO stdout
;
----------
[UNSAFE] -- Both partitions allowed by IN clause, then both excluded again by <> clauses.
explain (analyze, costs off, summary off, timing off, buffers off)  execute q1 (1,2,2,1)
;
----------
[SESSION_SETTING_WRITE] -- Test streaming of two-phase commits

SET synchronous_commit = on
;
