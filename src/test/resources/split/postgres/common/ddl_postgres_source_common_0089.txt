CREATE INDEX "clstr_tst_b_c" ON "public"."clstr_tst" USING "btree" ("b", "c")
;
DROP TABLE pg_temp_13.pg_temp_25000_2, pg_temp_13.pg_temp_25000
;
CREATE TABLE tab_full (a int PRIMARY KEY)
;
ALTER TABLE parent ALTER COLUMN a SET STORAGE PLAIN
;
ANALYZE pg_catalog.pg_trigger
;
ALTER TABLE measurement_y2007m01 INHERIT measurement
;
ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p2" FOR VALUES IN ('0004', '0005', '0006', '0007')
;
--
-- ISBN13 operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE VIEW dfview AS
   SELECT q1, q2,
     dfunc(q1,q2, flag := q1>q2) as c3,
     dfunc(q1, flag := q1<q2, b := q2) as c4
     FROM int8_tbl
;
CREATE TABLE "public"."prt1_m" (
    "a" integer,
    "b" integer,
    "c" integer
)
PARTITION BY RANGE ("a", ((("a" + "b") / 2)))
;
ALTER TABLE itest8
  ADD COLUMN f5 int GENERATED ALWAYS AS IDENTITY
;
alter table uv_pt11 add a int not null
;
CREATE TABLE tab_rowfilter_parent_sync (a int)
;
drop function dfunc()
;
/* contrib/pg_stat_statements/pg_stat_statements--1.9--1.10.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* First we have to remove them from the extension */
ALTER EXTENSION pg_stat_statements DROP VIEW pg_stat_statements
;
--
-- UTF-8
--
CREATE TABLE utf8_inputs (inbytes bytea, description text)
;
DROP TEXT SEARCH DICTIONARY IF EXISTS split_ts_probe.simple_dict_two CASCADE
;
CREATE TABLE hat_data (
	hat_name    char(10),
	hat_color   char(10)      
)
;
-- ******************************
-- * Create indices for faster checks
-- ******************************

CREATE UNIQUE INDEX num_exp_add_idx ON num_exp_add (id1, id2)
;
ALTER TEXT SEARCH CONFIGURATION russian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
CREATE TABLE prt2_m_p2 PARTITION OF prt2_m FOR VALUES FROM (250, 250) TO (500, 500)
;
ALTER TABLE replication_example RENAME COLUMN text TO somenum
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_auth_members
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_am
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
create table p1_c2() inherits (p1)
;
create unlogged table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=100)
;
CREATE TABLE sj_t4 (id serial, a int)
;
CREATE TEMP TABLE pg_temp_26.pg_temp_27076_2 (tid pg_catalog.tid)
;
DROP TRIGGER trig_d1_15 ON part_d_1_15
;
CREATE FUNCTION isnne(issn, issn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER FUNCTION gbt_bpchar_consistent(internal, character, smallint, oid, internal) PARALLEL SAFE
;
ANALYZE pg_temp_13.pg_temp_26049
;
CREATE TABLE amcheck_schema.pg_proc (junk text)
;
ALTER TABLE persons ADD COLUMN comment text
;
--
-- get_raw_page()
--
DROP FUNCTION get_raw_page(text, int4)
;
CREATE FUNCTION make_valid(isbn13)
	RETURNS isbn13
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- modulus 25 is factor of modulus of 50 but 10 is not a factor of 25.
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 25, REMAINDER 3)
;
--
-- Comparison functions for int_custom
--
-- These are required to create a btree operator class, which is needed
-- for the type to be usable in extended statistics objects.
--
CREATE FUNCTION int_custom_eq(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE inht1 (a int, b int)
;
ALTER TABLE ONLY "regress_indexing"."pk" ATTACH PARTITION "regress_indexing"."pk5" FOR VALUES FROM (4000) TO (5000)
;
ALTER TABLE ONLY tab1 ENABLE REPLICA TRIGGER sub1_tab1_log_op_trigger
;
create table rlp_default_null partition of rlp_default for values in (null)
;
ALTER TABLE split_alter_table.base_table DISABLE TRIGGER USER
;
CREATE TABLE a5 ()
;
alter table mlparted add constraint check_b check (a = 1 and b < 45)
;
-- save counters
CREATE TABLE prevstats AS
SELECT t.seq_scan, t.seq_tup_read, t.idx_scan, t.idx_tup_fetch,
       (b.heap_blks_read + b.heap_blks_hit) AS heap_blks,
       (b.idx_blks_read + b.idx_blks_hit) AS idx_blks,
       pg_stat_get_snapshot_timestamp() as snap_ts
  FROM pg_catalog.pg_stat_user_tables AS t,
       pg_catalog.pg_statio_user_tables AS b
 WHERE t.relname='tenk2' AND b.relname='tenk2'
;
CREATE TRIGGER trig_c1_100 BEFORE UPDATE OR INSERT ON part_c_1_100
   FOR EACH ROW EXECUTE PROCEDURE func_parted_mod_b()
;
CREATE TABLE t1 (num int, name text)
;
-- OK to add an exclusion constraint if partitioning by its equal column
create table idxpart (a int4range, exclude USING GIST (a with = )) partition by range (a)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX "clstr_tst_b_c" ON "public"."clstr_tst" USING "btree" ("b", "c")
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_25000_2, pg_temp_13.pg_temp_25000
;
----------
[CREATE_TABLE] CREATE TABLE tab_full (a int PRIMARY KEY)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE parent ALTER COLUMN a SET STORAGE PLAIN
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_trigger
;
----------
[ALTER_TABLE] ALTER TABLE measurement_y2007m01 INHERIT measurement
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p2" FOR VALUES IN ('0004', '0005', '0006', '0007')
;
----------
[CREATE_PROG_OBJ] --
-- ISBN13 operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW dfview AS
   SELECT q1, q2,
     dfunc(q1,q2, flag := q1>q2) as c3,
     dfunc(q1, flag := q1<q2, b := q2) as c4
     FROM int8_tbl
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_m" (
    "a" integer,
    "b" integer,
    "c" integer
)
PARTITION BY RANGE ("a", ((("a" + "b") / 2)))
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE itest8
  ADD COLUMN f5 int GENERATED ALWAYS AS IDENTITY
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table uv_pt11 add a int not null
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_parent_sync (a int)
;
----------
[DROP_PROG_OBJ] drop function dfunc()
;
----------
[ALTER_LIBRARY] /* contrib/pg_stat_statements/pg_stat_statements--1.9--1.10.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* First we have to remove them from the extension */
ALTER EXTENSION pg_stat_statements DROP VIEW pg_stat_statements
;
----------
[CREATE_TABLE] --
-- UTF-8
--
CREATE TABLE utf8_inputs (inbytes bytea, description text)
;
----------
[DROP_POLICY] DROP TEXT SEARCH DICTIONARY IF EXISTS split_ts_probe.simple_dict_two CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE hat_data (
	hat_name    char(10),
	hat_color   char(10)      
)
;
----------
[ADD_INDEX] -- ******************************
-- * Create indices for faster checks
-- ******************************

CREATE UNIQUE INDEX num_exp_add_idx ON num_exp_add (id1, id2)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION russian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_TABLE] CREATE TABLE prt2_m_p2 PARTITION OF prt2_m FOR VALUES FROM (250, 250) TO (500, 500)
;
----------
[RENAME_COLUMN] ALTER TABLE replication_example RENAME COLUMN text TO somenum
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_auth_members
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_am
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_TABLE] create table p1_c2() inherits (p1)
;
----------
[CREATE_TABLE] create unlogged table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=100)
;
----------
[CREATE_TABLE] CREATE TABLE sj_t4 (id serial, a int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_26.pg_temp_27076_2 (tid pg_catalog.tid)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_d1_15 ON part_d_1_15
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(issn, issn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bpchar_consistent(internal, character, smallint, oid, internal) PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26049
;
----------
[CREATE_TABLE] CREATE TABLE amcheck_schema.pg_proc (junk text)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE persons ADD COLUMN comment text
;
----------
[DROP_PROG_OBJ] --
-- get_raw_page()
--
DROP FUNCTION get_raw_page(text, int4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_valid(isbn13)
	RETURNS isbn13
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- modulus 25 is factor of modulus of 50 but 10 is not a factor of 25.
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 25, REMAINDER 3)
;
----------
[CREATE_PROG_OBJ] --
-- Comparison functions for int_custom
--
-- These are required to create a btree operator class, which is needed
-- for the type to be usable in extended statistics objects.
--
CREATE FUNCTION int_custom_eq(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE inht1 (a int, b int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "regress_indexing"."pk" ATTACH PARTITION "regress_indexing"."pk5" FOR VALUES FROM (4000) TO (5000)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY tab1 ENABLE REPLICA TRIGGER sub1_tab1_log_op_trigger
;
----------
[CREATE_TABLE] create table rlp_default_null partition of rlp_default for values in (null)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table DISABLE TRIGGER USER
;
----------
[CREATE_TABLE] CREATE TABLE a5 ()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table mlparted add constraint check_b check (a = 1 and b < 45)
;
----------
[CREATE_TABLE|METADATA] -- save counters
CREATE TABLE prevstats AS
SELECT t.seq_scan, t.seq_tup_read, t.idx_scan, t.idx_tup_fetch,
       (b.heap_blks_read + b.heap_blks_hit) AS heap_blks,
       (b.idx_blks_read + b.idx_blks_hit) AS idx_blks,
       pg_stat_get_snapshot_timestamp() as snap_ts
  FROM pg_catalog.pg_stat_user_tables AS t,
       pg_catalog.pg_statio_user_tables AS b
 WHERE t.relname='tenk2' AND b.relname='tenk2'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_c1_100 BEFORE UPDATE OR INSERT ON part_c_1_100
   FOR EACH ROW EXECUTE PROCEDURE func_parted_mod_b()
;
----------
[CREATE_TABLE] CREATE TABLE t1 (num int, name text)
;
----------
[CREATE_TABLE] -- OK to add an exclusion constraint if partitioning by its equal column
create table idxpart (a int4range, exclude USING GIST (a with = )) partition by range (a)
;
