CREATE FUNCTION _pg_expandarray(IN anyarray, OUT x anyelement, OUT n int)
    RETURNS SETOF RECORD
    LANGUAGE sql STRICT IMMUTABLE PARALLEL SAFE
    ROWS 100 SUPPORT pg_catalog.array_unnest_support
    AS 'SELECT * FROM pg_catalog.unnest($1) WITH ORDINALITY'
;
CREATE FUNCTION isngt(ismn13, ean13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- functional dependencies tests
CREATE TABLE functional_dependencies (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b TEXT,
    filler3 DATE,
    c INT,
    d TEXT
)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_publication_rel
;
create table boolrangep_tf partition of boolrangep for values from ('true', 'false', 0) to ('true', 'false', 100)
;
-- cannot add primary key on a column with an invalid not-null
ALTER TABLE notnull_tbl1 ADD PRIMARY KEY (a)
;
CREATE TABLE beta_pos_p3 PARTITION OF beta_pos FOR VALUES IN ('0007', '0009')
;
DROP SCHEMA IF EXISTS split_alter_table CASCADE
;
alter table p1_c1 add constraint inh_check_constraint1 check (f1 > 0)
;
ALTER SEQUENCE sequence_test2 RESTART WITH 0
;
--
-- Primary keys
--

CREATE TABLE PRIMARY_TBL (i int PRIMARY KEY, t text)
;
--
-- GIN over int[] and text[]
--
-- Note: GIN currently supports only bitmap scans, not plain indexscans
--

CREATE TABLE array_index_op_test (
	seqno		int4,
	i			int4[],
	t			text[]
)
;
--
-- gin_leafpage_items()
--
CREATE FUNCTION gin_leafpage_items(IN page bytea,
    OUT first_tid tid,
    OUT nbytes int2,
    OUT tids tid[])
RETURNS SETOF record
AS '$libdir/pageinspect', 'gin_leafpage_items'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE regress_roleoption.t2 (a int)
;
drop table trigpart4
;
ALTER INDEX "fkpart4"."droppk_pkey" ATTACH PARTITION "fkpart4"."droppk_d_pkey"
;
/*
 * 5.76
 * USER_DEFINED_TYPES view
 */

CREATE VIEW user_defined_types AS
    SELECT CAST(current_database() AS sql_identifier) AS user_defined_type_catalog,
           CAST(n.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(c.relname AS sql_identifier) AS user_defined_type_name,
           CAST('STRUCTURED' AS character_data) AS user_defined_type_category,
           CAST('YES' AS yes_or_no) AS is_instantiable,
           CAST(null AS yes_or_no) AS is_final,
           CAST(null AS character_data) AS ordering_form,
           CAST(null AS character_data) AS ordering_category,
           CAST(null AS sql_identifier) AS ordering_routine_catalog,
           CAST(null AS sql_identifier) AS ordering_routine_schema,
           CAST(null AS sql_identifier) AS ordering_routine_name,
           CAST(null AS character_data) AS reference_type,
           CAST(null AS character_data) AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(null AS sql_identifier) AS source_dtd_identifier,
           CAST(null AS sql_identifier) AS ref_dtd_identifier

    FROM pg_namespace n, pg_class c, pg_type t

    WHERE n.oid = c.relnamespace
          AND t.typrelid = c.oid
          AND c.relkind = 'c'
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
CREATE INDEX ON syscol_table (a) WHERE ctid >= '(1000,0)'
;
create table domnotnull
( col1 dnotnulltest
, col2 dnotnulltest
)
;
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest2, ftest1)
     references pktable(ptest1, ptest2)
;
create index idxpart1_tst1 on idxpart1 (b, a)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_statistic
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn13,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE TABLE foo_range_parted2 PARTITION OF foo_range_parted FOR VALUES FROM (10) TO (20)
;
CREATE TABLE "public"."notnull_tbl6" (
    "a" integer,
    "b" integer,
    CONSTRAINT "notnull_tbl6_a_check" CHECK (("a" > 0))
)
PARTITION BY LIST ("a")
;
DROP FOREIGN TABLE no_table
;
drop table parttmp
;
-- support routines for indexing

CREATE FUNCTION seg_cmp(seg, seg)
RETURNS int4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create type ddtest1 as (f1 posint)
;
CREATE TABLE "public"."prt1_l_p3" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("b")
;
ALTER TABLE test_replica_identity5 ALTER b DROP NOT NULL
;
CREATE TABLE plt1_p2 PARTITION OF plt1 FOR VALUES IN ('0001', '0005', '0002', '0009')
;
CREATE FUNCTION _ltxtq_exec(_ltree, ltxtquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP EXTENSION test_pg_dump
;
create table volatile_partbound_test1 partition of volatile_partbound_test for values from (minvalue) to (current_timestamp)
;
CREATE TEMP TABLE child1 ( ) INHERITS ( parent )
;
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('CREATE DATABASE')
   execute procedure test_event_trigger()
;
ANALYZE public.funcidx
;
CREATE SUBSCRIPTION tap_sub
     CONNECTION 'port=30210 host=/tmp/dAEADmw5Wx dbname=postgres'
     PUBLICATION tap_pub
     WITH (copy_data = off)
;
CREATE TABLE test_tbl2 OF test_type2
;
DROP TYPE jsrec_i_not_null
;
CREATE TABLE "public"."itest7a" (
    "a" integer CONSTRAINT "itest7_a_not_null" NOT NULL,
    "b" "text"
)
;
CREATE FUNCTION log_rewrite() RETURNS event_trigger
LANGUAGE plpgsql as
$func$

declare
   this_schema text;
begin
    select into this_schema relnamespace::regnamespace::text
    from pg_class
    where oid = pg_event_trigger_table_rewrite_oid();
    if this_schema = 'fast_default'
    then
        RAISE NOTICE 'rewriting table % for reason %',
          pg_event_trigger_table_rewrite_oid()::regclass,
          pg_event_trigger_table_rewrite_reason();
    end if;
end;
$func$
;
-- error, column c does not exist
ALTER TABLE itest4 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=22820 host=/tmp/ozTCxVdcWB dbname=postgres' PUBLICATION mypub
;
create table idxpart2 partition of idxpart for values from (10, 10) to (20, 20)
  partition by range (b)
;
--
-- Test Partition pruning for HASH partitioning
--
-- Use hand-rolled hash functions and operator classes to get predictable
-- result on different machines.  See the definitions of
-- part_part_test_int4_ops and part_test_text_ops in insert.sql.
--

create table hp (a int, b text, c int)
  partition by hash (a part_test_int4_ops, b part_test_text_ops)
;
-- Check bitmap scan can consider similar OR arguments separately without
-- grouping them into SAOP.
CREATE TABLE bitmap_split_or (a int NOT NULL, b int NOT NULL, c int NOT NULL)
;
CREATE DATABASE "	

 !""#$%&'()*+,-./0123456789:;<=>?"
;
CREATE FUNCTION int_custom_cmp(int_custom, int_custom)
RETURNS integer
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION _pg_expandarray(IN anyarray, OUT x anyelement, OUT n int)
    RETURNS SETOF RECORD
    LANGUAGE sql STRICT IMMUTABLE PARALLEL SAFE
    ROWS 100 SUPPORT pg_catalog.array_unnest_support
    AS 'SELECT * FROM pg_catalog.unnest($1) WITH ORDINALITY'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ismn13, ean13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- functional dependencies tests
CREATE TABLE functional_dependencies (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b TEXT,
    filler3 DATE,
    c INT,
    d TEXT
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_publication_rel
;
----------
[CREATE_TABLE] create table boolrangep_tf partition of boolrangep for values from ('true', 'false', 0) to ('true', 'false', 100)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- cannot add primary key on a column with an invalid not-null
ALTER TABLE notnull_tbl1 ADD PRIMARY KEY (a)
;
----------
[CREATE_TABLE] CREATE TABLE beta_pos_p3 PARTITION OF beta_pos FOR VALUES IN ('0007', '0009')
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_alter_table CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1_c1 add constraint inh_check_constraint1 check (f1 > 0)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test2 RESTART WITH 0
;
----------
[CREATE_TABLE] --
-- Primary keys
--

CREATE TABLE PRIMARY_TBL (i int PRIMARY KEY, t text)
;
----------
[CREATE_TABLE] --
-- GIN over int[] and text[]
--
-- Note: GIN currently supports only bitmap scans, not plain indexscans
--

CREATE TABLE array_index_op_test (
	seqno		int4,
	i			int4[],
	t			text[]
)
;
----------
[CREATE_PROG_OBJ] --
-- gin_leafpage_items()
--
CREATE FUNCTION gin_leafpage_items(IN page bytea,
    OUT first_tid tid,
    OUT nbytes int2,
    OUT tids tid[])
RETURNS SETOF record
AS '$libdir/pageinspect', 'gin_leafpage_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE regress_roleoption.t2 (a int)
;
----------
[DROP_TABLE] drop table trigpart4
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart4"."droppk_pkey" ATTACH PARTITION "fkpart4"."droppk_d_pkey"
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.76
 * USER_DEFINED_TYPES view
 */

CREATE VIEW user_defined_types AS
    SELECT CAST(current_database() AS sql_identifier) AS user_defined_type_catalog,
           CAST(n.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(c.relname AS sql_identifier) AS user_defined_type_name,
           CAST('STRUCTURED' AS character_data) AS user_defined_type_category,
           CAST('YES' AS yes_or_no) AS is_instantiable,
           CAST(null AS yes_or_no) AS is_final,
           CAST(null AS character_data) AS ordering_form,
           CAST(null AS character_data) AS ordering_category,
           CAST(null AS sql_identifier) AS ordering_routine_catalog,
           CAST(null AS sql_identifier) AS ordering_routine_schema,
           CAST(null AS sql_identifier) AS ordering_routine_name,
           CAST(null AS character_data) AS reference_type,
           CAST(null AS character_data) AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(null AS sql_identifier) AS source_dtd_identifier,
           CAST(null AS sql_identifier) AS ref_dtd_identifier

    FROM pg_namespace n, pg_class c, pg_type t

    WHERE n.oid = c.relnamespace
          AND t.typrelid = c.oid
          AND c.relkind = 'c'
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
----------
[ADD_INDEX] CREATE INDEX ON syscol_table (a) WHERE ctid >= '(1000,0)'
;
----------
[CREATE_TABLE] create table domnotnull
( col1 dnotnulltest
, col2 dnotnulltest
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest2, ftest1)
     references pktable(ptest1, ptest2)
;
----------
[ADD_INDEX] create index idxpart1_tst1 on idxpart1 (b, a)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_statistic
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn13,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE foo_range_parted2 PARTITION OF foo_range_parted FOR VALUES FROM (10) TO (20)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl6" (
    "a" integer,
    "b" integer,
    CONSTRAINT "notnull_tbl6_a_check" CHECK (("a" > 0))
)
PARTITION BY LIST ("a")
;
----------
[DROP_TABLE] DROP FOREIGN TABLE no_table
;
----------
[DROP_TABLE] drop table parttmp
;
----------
[CREATE_PROG_OBJ] -- support routines for indexing

CREATE FUNCTION seg_cmp(seg, seg)
RETURNS int4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TYPE] create type ddtest1 as (f1 posint)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_l_p3" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("b")
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_replica_identity5 ALTER b DROP NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE plt1_p2 PARTITION OF plt1 FOR VALUES IN ('0001', '0005', '0002', '0009')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltxtq_exec(_ltree, ltxtquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_LIBRARY] DROP EXTENSION test_pg_dump
;
----------
[CREATE_TABLE] create table volatile_partbound_test1 partition of volatile_partbound_test for values from (minvalue) to (current_timestamp)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE child1 ( ) INHERITS ( parent )
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('CREATE DATABASE')
   execute procedure test_event_trigger()
;
----------
[ADMIN_TABLE] ANALYZE public.funcidx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
     CONNECTION 'port=30210 host=/tmp/dAEADmw5Wx dbname=postgres'
     PUBLICATION tap_pub
     WITH (copy_data = off)
;
----------
[CREATE_TABLE] CREATE TABLE test_tbl2 OF test_type2
;
----------
[DROP_TYPE] DROP TYPE jsrec_i_not_null
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest7a" (
    "a" integer CONSTRAINT "itest7_a_not_null" NOT NULL,
    "b" "text"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION log_rewrite() RETURNS event_trigger
LANGUAGE plpgsql as
$func$

declare
   this_schema text;
begin
    select into this_schema relnamespace::regnamespace::text
    from pg_class
    where oid = pg_event_trigger_table_rewrite_oid();
    if this_schema = 'fast_default'
    then
        RAISE NOTICE 'rewriting table % for reason %',
          pg_event_trigger_table_rewrite_oid()::regclass,
          pg_event_trigger_table_rewrite_reason();
    end if;
end;
$func$
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- error, column c does not exist
ALTER TABLE itest4 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=22820 host=/tmp/ozTCxVdcWB dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_TABLE] create table idxpart2 partition of idxpart for values from (10, 10) to (20, 20)
  partition by range (b)
;
----------
[CREATE_TABLE] --
-- Test Partition pruning for HASH partitioning
--
-- Use hand-rolled hash functions and operator classes to get predictable
-- result on different machines.  See the definitions of
-- part_part_test_int4_ops and part_test_text_ops in insert.sql.
--

create table hp (a int, b text, c int)
  partition by hash (a part_test_int4_ops, b part_test_text_ops)
;
----------
[CREATE_TABLE] -- Check bitmap scan can consider similar OR arguments separately without
-- grouping them into SAOP.
CREATE TABLE bitmap_split_or (a int NOT NULL, b int NOT NULL, c int NOT NULL)
;
----------
[CREATE_CATALOG] CREATE DATABASE "	

 !""#$%&'()*+,-./0123456789:;<=>?"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_cmp(int_custom, int_custom)
RETURNS integer
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
