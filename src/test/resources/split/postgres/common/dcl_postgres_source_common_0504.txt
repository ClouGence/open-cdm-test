EXECUTE dumpFunc('16466')
;
EXECUTE dumpFunc('29262')
;
-- OAuth options are not allowed in either context
ALTER SERVER testserver1 OPTIONS (ADD oauth_issuer 'https://example.com')
;
-- Create a cursor with the BINARY option and check the pg_cursors view
BEGIN
;
-- failed (name conflict)
ALTER STATISTICS alt_stat1 RENAME TO alt_stat3
;
-- RETURNING
BEGIN
;
ALTER TABLE public.prt2_l_p2_p2 OWNER TO zyc
;
EXECUTE st3(10, 20)
;
ALTER TABLE public.skip_wal_skip_rewrite_index OWNER TO zyc
;
EXECUTE dumpFunc('39400')
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('16466')
;
----------
[UNSAFE] EXECUTE dumpFunc('29262')
;
----------
[SYSTEM_SETTING_WRITE] -- OAuth options are not allowed in either context
ALTER SERVER testserver1 OPTIONS (ADD oauth_issuer 'https://example.com')
;
----------
[TRANSACTION] -- Create a cursor with the BINARY option and check the pg_cursors view
BEGIN
;
----------
[ADMIN_PERFORMANCE] -- failed (name conflict)
ALTER STATISTICS alt_stat1 RENAME TO alt_stat3
;
----------
[TRANSACTION] -- RETURNING
BEGIN
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.prt2_l_p2_p2 OWNER TO zyc
;
----------
[UNSAFE] EXECUTE st3(10, 20)
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.skip_wal_skip_rewrite_index OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpFunc('39400')
;
