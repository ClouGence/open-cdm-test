insert into fkpart3.pkey values (1)
;
WITH simpletup AS (
  SELECT 2 k, 'Green' v),
upsert_cte AS (
  INSERT INTO withz VALUES(2, 'Blue') ON CONFLICT (k) DO
    UPDATE SET (k, v) = (SELECT k, v FROM simpletup WHERE simpletup.k = withz.k)
    RETURNING k, v)
INSERT INTO withz VALUES(2, 'Red') ON CONFLICT (k) DO
UPDATE SET (k, v) = (SELECT k, v FROM upsert_cte WHERE upsert_cte.k = withz.k)
RETURNING k, v
;
INSERT INTO b SELECT generate_series(1,1000) % 4
;
insert into gstest4
values (1,1,b'0000','1'), (2,2,b'0001','1'),
       (3,4,b'0010','2'), (4,8,b'0011','2'),
       (5,16,b'0000','2'), (6,32,b'0001','2'),
       (7,64,b'0010','1'), (8,128,b'0011','1')
;
INSERT INTO equipment_r (name, hobby) VALUES ('advil', 'posthacking')
;
INSERT INTO tab1 VALUES (1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3888', relminmxid = '1'
WHERE oid = '"public"."log_table"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '38512'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12597', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_4_2"'::pg_catalog.regclass
;
INSERT INTO vac_horizon_floor_table SELECT generate_series(1, 2000) % 3
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1594', relminmxid = '1'
WHERE oid = '"public"."nummultirange_test"'::pg_catalog.regclass
;
insert into PLine values ('PL.022', '-367', '', 'PS.first.tb6')
;
INSERT INTO public.tht VALUES ('9', 189)
;
insert into "My_Table" values ( 1 , 'a\\\\b' )
;
INSERT INTO mytable VALUES (1, 'avocado')
;
INSERT INTO pg_temp_3.pg_temp_27487_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_3.pg_temp_27487 AS newdata FROM public.sro_index_mv mv FULL JOIN pg_temp_3.pg_temp_27487 newdata ON (newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
insert into permtest_parent
  select 1, 'a', left(md5(i::text), 5) from generate_series(0, 100) i
;
INSERT INTO public.tplain VALUES ('8', 168)
;
INSERT INTO public.ths_p1 VALUES (4, 154)
;
INSERT INTO public.ths_p3 VALUES (9, 799)
;
INSERT INTO tab1 VALUES (4), (5)
;
INSERT INTO POINT_TBL(f1) VALUES (NULL)
;
-- fail

-- Show that RLS policy does not apply for direct inserts to children
-- This should fail with RLS POLICY pp1r violation.
INSERT INTO part_document VALUES (100, 55, 1, 'regress_rls_dave', 'testing RLS with partitions')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10510', relminmxid = '1'
WHERE oid = '"public"."rules_src"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('0', 440)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16234', relminmxid = '3'
WHERE oid = '"public"."pagg_tab2_p1"'::pg_catalog.regclass
;
update parent set val1 = 'b' where aid = 1
;
INSERT INTO public.tht VALUES ('9', 19)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part_p2_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
INSERT INTO public.tht VALUES ('9', 559)
;
INSERT INTO test_prepared1 VALUES (3)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14847', relminmxid = '3'
WHERE oid = '37020'
;
insert into trigger_ddl_table values (1, 42)
;
insert into uv_iocu_view (a, b) values ('xyxyxy', 3)
   on conflict (a) do update set b = cast(excluded.two as float)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17735', relminmxid = '3'
WHERE oid = '"public"."transition_table_level2"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '25828'
;
DELETE FROM tab_ins WHERE a > 0
;
INSERT INTO num_result SELECT t1.id, t2.id, t1.val + t2.val
    FROM num_data t1, num_data t2
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."beta_pos_p2"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('7', 817)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."notnull_tbl4_cld"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '760', relminmxid = '1'
WHERE oid = '"public"."emp"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted11"'::pg_catalog.regclass
  AND attname IN ('b', 'a', 'c')
;
UPDATE v1 SET a=a+1 WHERE snoop(a) AND leakproof(a) AND a = 8
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20940'
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"fkpart3"."pk51"'::pg_catalog.regclass AND
conname IN ('pk5_a_not_null')
;
INSERT INTO num_exp_add VALUES (8,8,'149762')
;
-- Zero radius

INSERT INTO CIRCLE_TBL VALUES ('<(3,5),NaN>')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10195', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_3"'::pg_catalog.regclass
;
update insupd_test set a = a + 1 returning a
;
------------------------------------------------------------------------------------------
[INSERT] insert into fkpart3.pkey values (1)
;
----------
[MERGE|SELECT] WITH simpletup AS (
  SELECT 2 k, 'Green' v),
upsert_cte AS (
  INSERT INTO withz VALUES(2, 'Blue') ON CONFLICT (k) DO
    UPDATE SET (k, v) = (SELECT k, v FROM simpletup WHERE simpletup.k = withz.k)
    RETURNING k, v)
INSERT INTO withz VALUES(2, 'Red') ON CONFLICT (k) DO
UPDATE SET (k, v) = (SELECT k, v FROM upsert_cte WHERE upsert_cte.k = withz.k)
RETURNING k, v
;
----------
[INSERT|SELECT] INSERT INTO b SELECT generate_series(1,1000) % 4
;
----------
[INSERT] insert into gstest4
values (1,1,b'0000','1'), (2,2,b'0001','1'),
       (3,4,b'0010','2'), (4,8,b'0011','2'),
       (5,16,b'0000','2'), (6,32,b'0001','2'),
       (7,64,b'0010','1'), (8,128,b'0011','1')
;
----------
[INSERT|SELECT] INSERT INTO equipment_r (name, hobby) VALUES ('advil', 'posthacking')
;
----------
[INSERT] INSERT INTO tab1 VALUES (1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3888', relminmxid = '1'
WHERE oid = '"public"."log_table"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '38512'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12597', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_4_2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO vac_horizon_floor_table SELECT generate_series(1, 2000) % 3
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1594', relminmxid = '1'
WHERE oid = '"public"."nummultirange_test"'::pg_catalog.regclass
;
----------
[INSERT] insert into PLine values ('PL.022', '-367', '', 'PS.first.tb6')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 189)
;
----------
[INSERT] insert into "My_Table" values ( 1 , 'a\\\\b' )
;
----------
[INSERT] INSERT INTO mytable VALUES (1, 'avocado')
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_3.pg_temp_27487_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_3.pg_temp_27487 AS newdata FROM public.sro_index_mv mv FULL JOIN pg_temp_3.pg_temp_27487 newdata ON (newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT|SELECT] insert into permtest_parent
  select 1, 'a', left(md5(i::text), 5) from generate_series(0, 100) i
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 168)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 154)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 799)
;
----------
[INSERT] INSERT INTO tab1 VALUES (4), (5)
;
----------
[INSERT|SELECT] INSERT INTO POINT_TBL(f1) VALUES (NULL)
;
----------
[INSERT] -- fail

-- Show that RLS policy does not apply for direct inserts to children
-- This should fail with RLS POLICY pp1r violation.
INSERT INTO part_document VALUES (100, 55, 1, 'regress_rls_dave', 'testing RLS with partitions')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10510', relminmxid = '1'
WHERE oid = '"public"."rules_src"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 440)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16234', relminmxid = '3'
WHERE oid = '"public"."pagg_tab2_p1"'::pg_catalog.regclass
;
----------
[UPDATE] update parent set val1 = 'b' where aid = 1
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 19)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part_p2_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 559)
;
----------
[INSERT] INSERT INTO test_prepared1 VALUES (3)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14847', relminmxid = '3'
WHERE oid = '37020'
;
----------
[INSERT] insert into trigger_ddl_table values (1, 42)
;
----------
[MERGE|SELECT] insert into uv_iocu_view (a, b) values ('xyxyxy', 3)
   on conflict (a) do update set b = cast(excluded.two as float)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '17735', relminmxid = '3'
WHERE oid = '"public"."transition_table_level2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '25828'
;
----------
[DELETE] DELETE FROM tab_ins WHERE a > 0
;
----------
[INSERT|SELECT] INSERT INTO num_result SELECT t1.id, t2.id, t1.val + t2.val
    FROM num_data t1, num_data t2
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."beta_pos_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('7', 817)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."notnull_tbl4_cld"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '760', relminmxid = '1'
WHERE oid = '"public"."emp"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted11"'::pg_catalog.regclass
  AND attname IN ('b', 'a', 'c')
;
----------
[UPDATE] UPDATE v1 SET a=a+1 WHERE snoop(a) AND leakproof(a) AND a = 8
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20940'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"fkpart3"."pk51"'::pg_catalog.regclass AND
conname IN ('pk5_a_not_null')
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (8,8,'149762')
;
----------
[INSERT] -- Zero radius

INSERT INTO CIRCLE_TBL VALUES ('<(3,5),NaN>')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10195', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_3"'::pg_catalog.regclass
;
----------
[UPDATE] update insupd_test set a = a + 1 returning a
;
