insert into bar values(1,1)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart3"."fk2"'::pg_catalog.regclass
  AND attname IN ('a')
;
INSERT INTO LSEG_TBL VALUES ('[(-10,2),(-10,3)]')
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"generated_stored_tests"."gtest_child2"'::pg_catalog.regclass AND
conname IN ('gtest_parent_f1_not_null')
;
-- Cannot cast to inet on assignment.
INSERT INTO caster (inet)          VALUES ('192.168.100.128'::text)
;
-- fail, leaves behind an invalid index on the leaf partition
insert into idxpart11 values (1, 0)
;
insert into inner_7597 values(0, null)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14717', relminmxid = '3'
WHERE oid = '"public"."prt1_n_p1"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (9, 869)
;
INSERT INTO fk_notpartitioned_pk VALUES (500, 501), (1500, 1501),
  (2500, 2502), (2501, 2503)
;
INSERT INTO public.tht VALUES ('5', 325)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10819', relminmxid = '3'
WHERE oid = '31416'
;
-- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.2........'
  AND attrelid = '"public"."part_7_a_null"'::pg_catalog.regclass
;
INSERT INTO savepoints VALUES (11)
;
insert into rtest_nothn1 values (40, 'want this')
;
INSERT INTO public.tht VALUES ('8', 688)
;
INSERT INTO public.tht VALUES ('2', 742)
;
insert into t1 ( id , t ) values ( default , 'd' )
;
-- a PK update shrinking the referenced multirange but still valid:
-- There are two references: one fulfilled by the first pk row,
-- the other fulfilled by both pk rows combined.
INSERT INTO temporal_mltrng (id, valid_at) VALUES
  ('[1,2)', datemultirange(daterange('2018-01-01', '2018-03-01'))),
  ('[1,2)', datemultirange(daterange('2018-03-01', '2018-06-01')))
;
INSERT INTO upsert(key, payload) VALUES('FOOFOO', 'insert2') ON CONFLICT (lower(key)) DO UPDATE set key = EXCLUDED.key, payload = upsert.payload || ' updated by insert2'
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."alpha_neg_p1"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('7', 127)
;
-- Check that temp table drop is transactional and preserves dirty
-- buffer contents
UPDATE test_temp SET cnt = cnt + 1 WHERE a = -1
;
insert into Room values ('004', 'Technical')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10237', relminmxid = '1'
WHERE oid = '"public"."brin_test_multi"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '887', relminmxid = '1'
WHERE oid = '"public"."num_exp_mul"'::pg_catalog.regclass
;
DELETE FROM ONLY "fkpart9"."fk" WHERE $1 OPERATOR(pg_catalog.=) "fk_a"
;
-- fail, but see columns being inserted or have SELECT
INSERT INTO t1 (c1) VALUES (5)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."mvtest_tvmm"'::pg_catalog.regclass
;
UPDATE tst_one_array SET b = '{4, 5, 6, 1}' WHERE a > 3
;
DELETE FROM public.sro_index_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_4.pg_temp_27609_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
-- this sequence of two deletes will work, since after the first there will be no (2,*) references
delete from pktable where base2=2
;
insert into idxpart2 values (501, 0)
;
INSERT INTO public.tht VALUES ('0', 260)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13310', relminmxid = '3'
WHERE oid = '"fkpart4"."dropfk"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."pht2_p3"'::pg_catalog.regclass
;
INSERT INTO PKTABLE VALUES (-1, -2, 'The Default!')
;
INSERT INTO enumtest_child VALUES ('blue')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1547', relminmxid = '1'
WHERE oid = '"public"."bt_txt_heap"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '589', relminmxid = '1'
WHERE oid = '"public"."num_data"'::pg_catalog.regclass
;
INSERT INTO pg_temp_9.pg_temp_38025_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_38025 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_9.pg_temp_38025 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO public.ths_p3 VALUES (1, 351)
;
-- these updates would leave lingering rows in the referencing table; disallow
UPDATE fk_notpartitioned_pk SET b = 502 WHERE a = 500
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10082', relminmxid = '1'
WHERE oid = '28592'
;
INSERT INTO savepoints VALUES (10)
;
INSERT INTO test1 values (2, 'ho there', '\\004\\003\\002\\001\\000')
;
update utrtest set a = 1 where a = 1 or a = 2 returning *
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1075', relminmxid = '1'
WHERE oid = '"public"."num_exp_mul"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('1', 741)
;
INSERT INTO public.ths_p3 VALUES (1, 681)
;
------------------------------------------------------------------------------------------
[INSERT] insert into bar values(1,1)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart3"."fk2"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[INSERT] INSERT INTO LSEG_TBL VALUES ('[(-10,2),(-10,3)]')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"generated_stored_tests"."gtest_child2"'::pg_catalog.regclass AND
conname IN ('gtest_parent_f1_not_null')
;
----------
[INSERT|SELECT] -- Cannot cast to inet on assignment.
INSERT INTO caster (inet)          VALUES ('192.168.100.128'::text)
;
----------
[INSERT] -- fail, leaves behind an invalid index on the leaf partition
insert into idxpart11 values (1, 0)
;
----------
[INSERT] insert into inner_7597 values(0, null)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14717', relminmxid = '3'
WHERE oid = '"public"."prt1_n_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 869)
;
----------
[INSERT] INSERT INTO fk_notpartitioned_pk VALUES (500, 501), (1500, 1501),
  (2500, 2502), (2501, 2503)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 325)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10819', relminmxid = '3'
WHERE oid = '31416'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.2........'
  AND attrelid = '"public"."part_7_a_null"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO savepoints VALUES (11)
;
----------
[INSERT] insert into rtest_nothn1 values (40, 'want this')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 688)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('2', 742)
;
----------
[INSERT|SELECT] insert into t1 ( id , t ) values ( default , 'd' )
;
----------
[INSERT|SELECT] -- a PK update shrinking the referenced multirange but still valid:
-- There are two references: one fulfilled by the first pk row,
-- the other fulfilled by both pk rows combined.
INSERT INTO temporal_mltrng (id, valid_at) VALUES
  ('[1,2)', datemultirange(daterange('2018-01-01', '2018-03-01'))),
  ('[1,2)', datemultirange(daterange('2018-03-01', '2018-06-01')))
;
----------
[MERGE|SELECT] INSERT INTO upsert(key, payload) VALUES('FOOFOO', 'insert2') ON CONFLICT (lower(key)) DO UPDATE set key = EXCLUDED.key, payload = upsert.payload || ' updated by insert2'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."alpha_neg_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 127)
;
----------
[UPDATE] -- Check that temp table drop is transactional and preserves dirty
-- buffer contents
UPDATE test_temp SET cnt = cnt + 1 WHERE a = -1
;
----------
[INSERT] insert into Room values ('004', 'Technical')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '10237', relminmxid = '1'
WHERE oid = '"public"."brin_test_multi"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '887', relminmxid = '1'
WHERE oid = '"public"."num_exp_mul"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM ONLY "fkpart9"."fk" WHERE $1 OPERATOR(pg_catalog.=) "fk_a"
;
----------
[INSERT|SELECT] -- fail, but see columns being inserted or have SELECT
INSERT INTO t1 (c1) VALUES (5)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."mvtest_tvmm"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE tst_one_array SET b = '{4, 5, 6, 1}' WHERE a > 3
;
----------
[DELETE|SELECT] DELETE FROM public.sro_index_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_4.pg_temp_27609_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[DELETE] -- this sequence of two deletes will work, since after the first there will be no (2,*) references
delete from pktable where base2=2
;
----------
[INSERT] insert into idxpart2 values (501, 0)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 260)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13310', relminmxid = '3'
WHERE oid = '"fkpart4"."dropfk"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."pht2_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO PKTABLE VALUES (-1, -2, 'The Default!')
;
----------
[INSERT] INSERT INTO enumtest_child VALUES ('blue')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1547', relminmxid = '1'
WHERE oid = '"public"."bt_txt_heap"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '589', relminmxid = '1'
WHERE oid = '"public"."num_data"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_9.pg_temp_38025_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_38025 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_9.pg_temp_38025 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 351)
;
----------
[UPDATE] -- these updates would leave lingering rows in the referencing table; disallow
UPDATE fk_notpartitioned_pk SET b = 502 WHERE a = 500
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10082', relminmxid = '1'
WHERE oid = '28592'
;
----------
[INSERT] INSERT INTO savepoints VALUES (10)
;
----------
[INSERT] INSERT INTO test1 values (2, 'ho there', '\\004\\003\\002\\001\\000')
;
----------
[UPDATE] update utrtest set a = 1 where a = 1 or a = 2 returning *
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1075', relminmxid = '1'
WHERE oid = '"public"."num_exp_mul"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 741)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 681)
;
