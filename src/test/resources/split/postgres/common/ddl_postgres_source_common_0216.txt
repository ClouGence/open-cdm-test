ALTER TABLE gtest20b ADD CONSTRAINT chk CHECK (b < 50) NOT VALID
;
CREATE TABLE testschema.part_56 PARTITION OF testschema.part FOR VALUES IN (5, 6)
  PARTITION BY LIST (a)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
CREATE OPERATOR CLASS alt_opc1 FOR TYPE macaddr USING hash AS STORAGE macaddr
;
call test_catalog.test.test_procedure(1,test_catalog.test.test_function(3))
;
CREATE INDEX "stats_test_idx1" ON "public"."stats_test_tab1" USING "btree" ("a")
;
CREATE TABLE stxdinh(a int, b int)
;
CREATE TABLE addr_nsp.gentable (
	a serial primary key CONSTRAINT a_chk CHECK (a > 0),
	b text DEFAULT 'hello')
;
CREATE FUNCTION gen_random_uuid()
RETURNS uuid
AS '$libdir/pgcrypto', 'pg_random_uuid'
LANGUAGE C VOLATILE PARALLEL SAFE
;
---------------------------------------------------
-- ISMN:
CREATE FUNCTION isnlt(ismn, ismn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER FUNCTION tuple_data_split(oid, bytea, integer, integer, text, bool) PARALLEL RESTRICTED
;
-- 0

-- cleanup
DROP FOREIGN TABLE tru_ftable_parent, tru_ftable_child, tru_pk_ftable,tru_ftable__p1,tru_ftable
;
CREATE FUNCTION ismn13_in(cstring)
	RETURNS ismn13
	AS '$libdir/isn', 'ismn_in'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
--
-- Tests for composite-type results
--

create type compostype as (x int, y varchar)
;
CREATE OPERATOR <@ (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_contained,
	COMMUTATOR = '@>',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE TABLE pk52 PARTITION OF pk5 FOR VALUES FROM (4500) TO (5000)
;
drop table trigpart3
;
CREATE TRIGGER "gtest4" AFTER INSERT OR UPDATE ON "public"."gtest26" FOR EACH ROW WHEN (("new"."b" < 0)) EXECUTE FUNCTION "public"."gtest_trigger_func"()
;
ANALYZE public.gs_data_1
;
ANALYZE public.pagg_tab1_p1
;
-- fails
ALTER TYPE test_typex DROP ATTRIBUTE a CASCADE
;
-- likewise, converting a partitioned table or partition to view is not allowed
create table rules_fooview (x int, y text) partition by list (x)
;
--
-- Regression Test for TRUNCATE
--

--
-- Setup
--
CREATE TABLE julio_claudians (name text, birth_date date)
;
ALTER FUNCTION gbt_macad8_compress(internal) PARALLEL SAFE
;
-- stable because depends on pg_trgm.similarity_threshold

CREATE OPERATOR % (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = similarity_op,
        COMMUTATOR = '%',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
create view tt15v as select row(i)::nestedcomposite from int8_tbl i
;
alter table attest drop a
;
-- check that message shown after failure to find a partition shows the
-- appropriate key description (or none) in various situations
create table key_desc (a int, b int) partition by list ((a+0))
;
create temporary table temp_parted (a int) partition by list (a)
;
CREATE FUNCTION "public"."trigtest"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
	raise notice '% % % %', TG_TABLE_NAME, TG_OP, TG_WHEN, TG_LEVEL;
	return new;
end;$$
;
CREATE TABLE "public"."test_storage" (
    "a" "text",
    "c" "text",
    "b" integer DEFAULT ("random"())::integer
)
;
CREATE FUNCTION int_custom_gt(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
alter table pg_class alter relname set not null
;
create domain vc4 as varchar(4)
;
CREATE RULE test_oat_notify AS
  ON UPDATE TO test_oat_schema.test_oat_tab
  DO ALSO NOTIFY test_oat_tab
;
-- test FOR-over-cursor

create or replace function forc01() returns void as $$
declare
  c cursor(r1 integer, r2 integer)
       for select * from generate_series(r1,r2) i;
  c2 cursor
       for select * from generate_series(41,43) i;
begin
  for r in c(5,7) loop
    raise notice '% from %', r.i, c;
  end loop;
  -- again, to test if cursor was closed properly
  for r in c(9,10) loop
    raise notice '% from %', r.i, c;
  end loop;
  -- and test a parameterless cursor
  for r in c2 loop
    raise notice '% from %', r.i, c2;
  end loop;
  -- and try it with a hand-assigned name
  raise notice 'after loop, c2 = %', c2;
  c2 := 'special_name';
  for r in c2 loop
    raise notice '% from %', r.i, c2;
  end loop;
  raise notice 'after loop, c2 = %', c2;
  -- and try it with a generated name
  -- (which we can't show in the output because it's variable)
  c2 := null;
  for r in c2 loop
    raise notice '%', r.i;
  end loop;
  raise notice 'after loop, c2 = %', c2;
  return;
end;
$$ language plpgsql
;
--
-- CREATE OR REPLACE VIEW
--

CREATE TABLE viewtest_tbl (a int, b int)
;
-- not supported by sepgsql

ALTER TABLE regtest_ptable SET TABLESPACE pg_default
;
drop extension earthdistance
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_foreign_data_wrapper
;
CREATE INDEX test1_b_gist ON test1 USING gist(b)
;
ALTER FUNCTION gbt_intv_compress(internal) PARALLEL SAFE
;
CREATE SUBSCRIPTION sub CONNECTION 'port=19451 host=/tmp/sYxMyq7DNU dbname=postgres' PUBLICATION pub
;
VACUUM (PARALLEL 0) pg_catalog.pg_collation
;
ALTER TABLE ONLY "public"."pagg_tab_ml_p3" ATTACH PARTITION "public"."pagg_tab_ml_p3_s2" FOR VALUES FROM (5) TO (10)
;
ALTER TYPE planets ADD VALUE 'saturn' BEFORE 'uranus'
;
CREATE TABLE tbspace_reindex_part_0_2 PARTITION OF tbspace_reindex_part_0
  FOR VALUES IN (2)
;
ANALYZE pg_temp_3.pg_temp_29555
;
-- check handling of a SQL function with multiple OUT params (bug #5777)

create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2.1) $$ language sql
;
create table pktable(ptest1 inet, primary key(base1, ptest1)) inherits (pktable_base)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE gtest20b ADD CONSTRAINT chk CHECK (b < 50) NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part_56 PARTITION OF testschema.part FOR VALUES IN (5, 6)
  PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS alt_opc1 FOR TYPE macaddr USING hash AS STORAGE macaddr
;
----------
[CALL_PROG_OBJ] call test_catalog.test.test_procedure(1,test_catalog.test.test_function(3))
;
----------
[ADD_INDEX] CREATE INDEX "stats_test_idx1" ON "public"."stats_test_tab1" USING "btree" ("a")
;
----------
[CREATE_TABLE] CREATE TABLE stxdinh(a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE addr_nsp.gentable (
	a serial primary key CONSTRAINT a_chk CHECK (a > 0),
	b text DEFAULT 'hello')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gen_random_uuid()
RETURNS uuid
AS '$libdir/pgcrypto', 'pg_random_uuid'
LANGUAGE C VOLATILE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISMN:
CREATE FUNCTION isnlt(ismn, ismn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION tuple_data_split(oid, bytea, integer, integer, text, bool) PARALLEL RESTRICTED
;
----------
[DROP_TABLE] -- 0

-- cleanup
DROP FOREIGN TABLE tru_ftable_parent, tru_ftable_child, tru_pk_ftable,tru_ftable__p1,tru_ftable
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ismn13_in(cstring)
	RETURNS ismn13
	AS '$libdir/isn', 'ismn_in'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TYPE] --
-- Tests for composite-type results
--

create type compostype as (x int, y varchar)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <@ (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_contained,
	COMMUTATOR = '@>',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE pk52 PARTITION OF pk5 FOR VALUES FROM (4500) TO (5000)
;
----------
[DROP_TABLE] drop table trigpart3
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "gtest4" AFTER INSERT OR UPDATE ON "public"."gtest26" FOR EACH ROW WHEN (("new"."b" < 0)) EXECUTE FUNCTION "public"."gtest_trigger_func"()
;
----------
[ADMIN_TABLE] ANALYZE public.gs_data_1
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab1_p1
;
----------
[ALTER_TYPE] -- fails
ALTER TYPE test_typex DROP ATTRIBUTE a CASCADE
;
----------
[CREATE_TABLE] -- likewise, converting a partitioned table or partition to view is not allowed
create table rules_fooview (x int, y text) partition by list (x)
;
----------
[CREATE_TABLE] --
-- Regression Test for TRUNCATE
--

--
-- Setup
--
CREATE TABLE julio_claudians (name text, birth_date date)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad8_compress(internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- stable because depends on pg_trgm.similarity_threshold

CREATE OPERATOR % (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = similarity_op,
        COMMUTATOR = '%',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
----------
[CREATE_VIEW(SELECT)] create view tt15v as select row(i)::nestedcomposite from int8_tbl i
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table attest drop a
;
----------
[CREATE_TABLE] -- check that message shown after failure to find a partition shows the
-- appropriate key description (or none) in various situations
create table key_desc (a int, b int) partition by list ((a+0))
;
----------
[CREATE_TABLE] create temporary table temp_parted (a int) partition by list (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."trigtest"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
	raise notice '% % % %', TG_TABLE_NAME, TG_OP, TG_WHEN, TG_LEVEL;
	return new;
end;$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_storage" (
    "a" "text",
    "c" "text",
    "b" integer DEFAULT ("random"())::integer
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_gt(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table pg_class alter relname set not null
;
----------
[CREATE_TYPE] create domain vc4 as varchar(4)
;
----------
[CREATE_POLICY] CREATE RULE test_oat_notify AS
  ON UPDATE TO test_oat_schema.test_oat_tab
  DO ALSO NOTIFY test_oat_tab
;
----------
[CREATE_PROG_OBJ] -- test FOR-over-cursor

create or replace function forc01() returns void as $$
declare
  c cursor(r1 integer, r2 integer)
       for select * from generate_series(r1,r2) i;
  c2 cursor
       for select * from generate_series(41,43) i;
begin
  for r in c(5,7) loop
    raise notice '% from %', r.i, c;
  end loop;
  -- again, to test if cursor was closed properly
  for r in c(9,10) loop
    raise notice '% from %', r.i, c;
  end loop;
  -- and test a parameterless cursor
  for r in c2 loop
    raise notice '% from %', r.i, c2;
  end loop;
  -- and try it with a hand-assigned name
  raise notice 'after loop, c2 = %', c2;
  c2 := 'special_name';
  for r in c2 loop
    raise notice '% from %', r.i, c2;
  end loop;
  raise notice 'after loop, c2 = %', c2;
  -- and try it with a generated name
  -- (which we can't show in the output because it's variable)
  c2 := null;
  for r in c2 loop
    raise notice '%', r.i;
  end loop;
  raise notice 'after loop, c2 = %', c2;
  return;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] --
-- CREATE OR REPLACE VIEW
--

CREATE TABLE viewtest_tbl (a int, b int)
;
----------
[ALTER_TABLE] -- not supported by sepgsql

ALTER TABLE regtest_ptable SET TABLESPACE pg_default
;
----------
[DROP_LIBRARY] drop extension earthdistance
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_foreign_data_wrapper
;
----------
[ADD_INDEX] CREATE INDEX test1_b_gist ON test1 USING gist(b)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_compress(internal) PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub CONNECTION 'port=19451 host=/tmp/sYxMyq7DNU dbname=postgres' PUBLICATION pub
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_collation
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml_p3" ATTACH PARTITION "public"."pagg_tab_ml_p3_s2" FOR VALUES FROM (5) TO (10)
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE 'saturn' BEFORE 'uranus'
;
----------
[CREATE_TABLE] CREATE TABLE tbspace_reindex_part_0_2 PARTITION OF tbspace_reindex_part_0
  FOR VALUES IN (2)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_29555
;
----------
[CREATE_PROG_OBJ] -- check handling of a SQL function with multiple OUT params (bug #5777)

create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2.1) $$ language sql
;
----------
[CREATE_TABLE] create table pktable(ptest1 inet, primary key(base1, ptest1)) inherits (pktable_base)
;
