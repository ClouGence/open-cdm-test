CREATE CAST (split_obj_priv.cast_mood AS text) WITH INOUT AS IMPLICIT
;
VACUUM (ANALYZE) pg_catalog.pg_parameter_acl
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = upc,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
create table test1 (a int)
;
-- procedure 1 appears twice in same statement
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
ANALYZE analyze_ftable
;
CREATE TABLE gtest2 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (NULL) STORED)
;
ALTER SUBSCRIPTION regress_testsub SET (run_as_owner = true)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_transform
;
ALTER FOREIGN TABLE agg_text OPTIONS (SET format 'text')
;
CREATE SCHEMA newschema6 AUTHORIZATION "public"
;
CREATE FUNCTION wait_for_backend_termination(int) RETURNS void AS $$
  BEGIN
    WHILE (SELECT count(*) FROM pg_stat_activity WHERE pid = $1) > 0
    LOOP
      PERFORM pg_stat_clear_snapshot();
    END LOOP;
  END
$$ LANGUAGE plpgsql
;
DROP TABLE check_fk_presence_1, check_fk_presence_2
;
CREATE DATABASE regression_createdb_target
TEMPLATE regression_createdb_source
STRATEGY wal_log
;
CREATE FUNCTION plpgsql_validator(oid) RETURNS void
  STRICT LANGUAGE c AS '$libdir/plpgsql'
;
create table HSlot (
    slotname	char(20),
    hubname	char(14),
    slotno	integer,
    slotlink	char(20)
)
;
CREATE TABLE deptest1 (f1 int unique)
;
CREATE FUNCTION hmac(text, text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_hmac'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ANALYZE fkpart6.pk11
;
do $$ declare c nested_int8s; begin d.c2.x = 1; end $$
;
CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (const_func())
;
DROP TRIGGER delete_when ON main_table
;
ALTER INDEX pg_toast.pg_toast_33416_index RENAME TO reind_con_toast_idx
;
CREATE INDEX "brin_test_multi_a_idx" ON "public"."brin_test_multi" USING "brin" ("a") WITH ("pages_per_range"='2')
;
ANALYZE regress_rls_schema.rls_tbl
;
CREATE FUNCTION dblink_build_sql_insert (text, int2vector, int, _text, _text)
RETURNS text
AS '$libdir/dblink','dblink_build_sql_insert'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE "public"."temporal_fk_mltrng2mltrng" (
    "id" "int4range" NOT NULL,
    "valid_at" "datemultirange" NOT NULL,
    "parent_id" "int4range"
)
;
-- fail

drop function anyctest(anycompatiblemultirange, anycompatiblemultirange)
;
CREATE FUNCTION worker_spi_launch(index int4,
  dboid oid DEFAULT 0,
  roleoid oid DEFAULT 0,
  flags text[] DEFAULT '{}')
RETURNS pg_catalog.int4 STRICT
AS '$libdir/worker_spi'
LANGUAGE C
;
ALTER INDEX pg_toast.pg_toast_27655_index RENAME TO reind_con_toast_idx
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub_mix_1, pub_mix_2
;
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, enabled = true)
;
ANALYZE pg_temp_3.pg_temp_27487
;
CREATE POLICY p2 ON t2 FOR ALL TO PUBLIC USING (a % 2 = 1)
;
ALTER TABLE split_alter_table.base_table ALTER COLUMN identity_col DROP IDENTITY IF EXISTS
;
COMMENT ON TEXT SEARCH CONFIGURATION danish IS 'configuration for danish language'
;
ANALYZE pg_catalog.pg_language
;
COMMENT ON CONSTRAINT "id_notnull_constraint" ON "public"."comment_test" IS 'NOT NULL constraint of comment_test'
;
ANALYZE public.itest2
;
CREATE AGGREGATE "public"."mysum2"("anyelement", "anyelement") (
    SFUNC = "public"."sum3",
    STYPE = "anyelement",
    INITCOND = '0'
)
;
ALTER OPERATOR FAMILY gist_numeric_ops USING gist ADD
	OPERATOR	6	<> (numeric, numeric) ,
	FUNCTION	9 (numeric, numeric) gbt_var_fetch (internal)
;
-- nothing to re-RAISE
create or replace function raise_test() returns void as $$
begin
  raise;
end;
$$ language plpgsql
;
ALTER TRIGGER modified_a ON main_table RENAME TO modified_modified_a
;
vacuum Ӌ��C���Z
;
ALTER TEXT SEARCH TEMPLATE alt_ts_temp2 SET SCHEMA alt_nsp2
;
CREATE FUNCTION test_assign_uint2(x int, y int) RETURNS uint2 AS $$
declare v uint2 := x;
begin
v := y;
return v;
end
$$ LANGUAGE plpgsql
;
CREATE OPERATOR - (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = intset_subtract
)
;
CREATE FUNCTION "public"."hobbies"("public"."person") RETURNS SETOF "public"."hobbies_r"
    LANGUAGE "sql"
    AS $_$select * from hobbies_r where person = $1.name$_$
;
CREATE TEXT SEARCH CONFIGURATION public.custom_search
  (COPY = pg_catalog.english)
;
ALTER SUBSCRIPTION regress_testsub_foo RENAME TO regress_testsub
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE CAST (split_obj_priv.cast_mood AS text) WITH INOUT AS IMPLICIT
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_parameter_acl
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = upc,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] create table test1 (a int)
;
----------
[ALTER_PROG_OBJ] -- procedure 1 appears twice in same statement
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
----------
[ADMIN_TABLE] ANALYZE analyze_ftable
;
----------
[CREATE_TABLE] CREATE TABLE gtest2 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (NULL) STORED)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (run_as_owner = true)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_transform
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE agg_text OPTIONS (SET format 'text')
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema6 AUTHORIZATION "public"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION wait_for_backend_termination(int) RETURNS void AS $$
  BEGIN
    WHILE (SELECT count(*) FROM pg_stat_activity WHERE pid = $1) > 0
    LOOP
      PERFORM pg_stat_clear_snapshot();
    END LOOP;
  END
$$ LANGUAGE plpgsql
;
----------
[DROP_TABLE] DROP TABLE check_fk_presence_1, check_fk_presence_2
;
----------
[CREATE_CATALOG] CREATE DATABASE regression_createdb_target
TEMPLATE regression_createdb_source
STRATEGY wal_log
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION plpgsql_validator(oid) RETURNS void
  STRICT LANGUAGE c AS '$libdir/plpgsql'
;
----------
[CREATE_TABLE] create table HSlot (
    slotname	char(20),
    hubname	char(14),
    slotno	integer,
    slotlink	char(20)
)
;
----------
[CREATE_TABLE] CREATE TABLE deptest1 (f1 int unique)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hmac(text, text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_hmac'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE fkpart6.pk11
;
----------
[BLOCK] do $$ declare c nested_int8s; begin d.c2.x = 1; end $$
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (const_func())
;
----------
[DROP_TRIGGER] DROP TRIGGER delete_when ON main_table
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33416_index RENAME TO reind_con_toast_idx
;
----------
[ADD_INDEX] CREATE INDEX "brin_test_multi_a_idx" ON "public"."brin_test_multi" USING "brin" ("a") WITH ("pages_per_range"='2')
;
----------
[ADMIN_TABLE] ANALYZE regress_rls_schema.rls_tbl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_build_sql_insert (text, int2vector, int, _text, _text)
RETURNS text
AS '$libdir/dblink','dblink_build_sql_insert'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE "public"."temporal_fk_mltrng2mltrng" (
    "id" "int4range" NOT NULL,
    "valid_at" "datemultirange" NOT NULL,
    "parent_id" "int4range"
)
;
----------
[DROP_PROG_OBJ] -- fail

drop function anyctest(anycompatiblemultirange, anycompatiblemultirange)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION worker_spi_launch(index int4,
  dboid oid DEFAULT 0,
  roleoid oid DEFAULT 0,
  flags text[] DEFAULT '{}')
RETURNS pg_catalog.int4 STRICT
AS '$libdir/worker_spi'
LANGUAGE C
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27655_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub_mix_1, pub_mix_2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, enabled = true)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27487
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON t2 FOR ALL TO PUBLIC USING (a % 2 = 1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE split_alter_table.base_table ALTER COLUMN identity_col DROP IDENTITY IF EXISTS
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION danish IS 'configuration for danish language'
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_language
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT "id_notnull_constraint" ON "public"."comment_test" IS 'NOT NULL constraint of comment_test'
;
----------
[ADMIN_TABLE] ANALYZE public.itest2
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."mysum2"("anyelement", "anyelement") (
    SFUNC = "public"."sum3",
    STYPE = "anyelement",
    INITCOND = '0'
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_numeric_ops USING gist ADD
	OPERATOR	6	<> (numeric, numeric) ,
	FUNCTION	9 (numeric, numeric) gbt_var_fetch (internal)
;
----------
[CREATE_PROG_OBJ] -- nothing to re-RAISE
create or replace function raise_test() returns void as $$
begin
  raise;
end;
$$ language plpgsql
;
----------
[RENAME_TRIGGER] ALTER TRIGGER modified_a ON main_table RENAME TO modified_modified_a
;
----------
[ADMIN_TABLE] vacuum Ӌ��C���Z
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH TEMPLATE alt_ts_temp2 SET SCHEMA alt_nsp2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_assign_uint2(x int, y int) RETURNS uint2 AS $$
declare v uint2 := x;
begin
v := y;
return v;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR - (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = intset_subtract
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."hobbies"("public"."person") RETURNS SETOF "public"."hobbies_r"
    LANGUAGE "sql"
    AS $_$select * from hobbies_r where person = $1.name$_$
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION public.custom_search
  (COPY = pg_catalog.english)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub_foo RENAME TO regress_testsub
;
