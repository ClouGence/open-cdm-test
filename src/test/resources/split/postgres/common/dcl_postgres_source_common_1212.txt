SET SESSION AUTHORIZATION regress_rls_dave
;
ALTER VIEW public.mvtest_tv OWNER TO zyc
;
SET search_path TO fkpart5
;
LOCK TABLE "public"."alpha_neg_p1" IN ACCESS SHARE MODE
;
EXECUTE pp
;
PREPARE pp AS
SELECT  xmltable.*
   FROM (SELECT data FROM xmldata) x,
        LATERAL XMLTABLE('/ROWS/ROW'
                         PASSING data
                         COLUMNS id int PATH '@id',
                                  _id FOR ORDINALITY,
                                  country_name text PATH 'COUNTRY_NAME' NOT NULL,
                                  country_id text PATH 'COUNTRY_ID',
                                  region_id int PATH 'REGION_ID',
                                  size float PATH 'SIZE',
                                  unit text PATH 'SIZE/@unit',
                                  premier_name text PATH 'PREMIER_NAME' DEFAULT 'not specified')
;
EXECUTE get_nnconstraint_info('{notnull_tbl1_child2}')
;
ALTER FUNCTION "public"."trigger_return_old"() OWNER TO zyc
;
COPY public.dropped_cols (a, b_drop, c) TO STDOUT
;
EXECUTE dumpOpr('27962')
;
------------------------------------------------------------------------------------------
[SWITCH_USER] SET SESSION AUTHORIZATION regress_rls_dave
;
----------
[TRANSFER_PRIVILEGE] ALTER VIEW public.mvtest_tv OWNER TO zyc
;
----------
[SESSION_SETTING_WRITE] SET search_path TO fkpart5
;
----------
[SESSION_LOCK] LOCK TABLE "public"."alpha_neg_p1" IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE pp
;
----------
[UNSAFE] PREPARE pp AS
SELECT  xmltable.*
   FROM (SELECT data FROM xmldata) x,
        LATERAL XMLTABLE('/ROWS/ROW'
                         PASSING data
                         COLUMNS id int PATH '@id',
                                  _id FOR ORDINALITY,
                                  country_name text PATH 'COUNTRY_NAME' NOT NULL,
                                  country_id text PATH 'COUNTRY_ID',
                                  region_id int PATH 'REGION_ID',
                                  size float PATH 'SIZE',
                                  unit text PATH 'SIZE/@unit',
                                  premier_name text PATH 'PREMIER_NAME' DEFAULT 'not specified')
;
----------
[UNSAFE] EXECUTE get_nnconstraint_info('{notnull_tbl1_child2}')
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."trigger_return_old"() OWNER TO zyc
;
----------
[DATA_EXPORT] COPY public.dropped_cols (a, b_drop, c) TO STDOUT
;
----------
[UNSAFE] EXECUTE dumpOpr('27962')
;
