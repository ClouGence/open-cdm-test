create function polyf(x anycompatiblerange, y anycompatiblearray) returns anycompatiblerange as $$
  select x
$$ language sql
;
CREATE OPERATOR ?@ (
        LEFTARG = _ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = _ltxtq_extract_exec
)
;
-- Introduce a NULL and DEFAULT partition so we can test more complex cases
create table mclparted_null partition of mclparted for values in(null)
;
ALTER TABLE pg_toast.pg_toast_30394 RENAME TO reind_con_toast
;
CREATE FUNCTION distance_taxicab(cube, cube)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- test where the updated table now has both an IMMEDIATE and a DEFERRED
-- constraint pointing into it
CREATE TABLE fkpart10.tbl5(f1 int REFERENCES fkpart10.tbl3)
;
CREATE POLICY p2 ON r1 FOR UPDATE USING (a < 20) WITH CHECK (true)
;
CREATE TABLE sch1.t1 (c1 int)
;
create table child (a float4) inherits (parent)
;
CREATE FUNCTION gbt_uuid_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
ALTER SEQUENCE seq_unlogged2 INCREMENT 2
;
CREATE TABLE "fkpart4"."dropfk" (
    "a" integer
)
;
DROP PUBLICATION split_pub18_cols
;
-- Generate COPY FROM report with FILE, with some excluded tuples.
truncate tab_progress_reporting
;
--
-- tests for refcursors
--
create table rc_test (a int, b int)
;
ALTER TYPE "public"."insenum" ADD VALUE 'i7'
;
SECURITY LABEL FOR 'dummy' ON ROLE regress_dummy_seclabel_user2 IS 'unclassified'
;
CREATE VIEW pg_indexes AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS tablename,
        I.relname AS indexname,
        T.spcname AS tablespace,
        pg_get_indexdef(I.oid) AS indexdef
    FROM pg_index X JOIN pg_class C ON (C.oid = X.indrelid)
         JOIN pg_class I ON (I.oid = X.indexrelid)
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
         LEFT JOIN pg_tablespace T ON (T.oid = I.reltablespace)
    WHERE C.relkind IN ('r', 'm', 'p') AND I.relkind IN ('i', 'I')
;
VACUUM (ANALYZE) information_schema.sql_implementation_info
;
CREATE TABLE upsert_test_1 PARTITION OF upsert_test FOR VALUES IN (1)
;
ALTER TABLE toasted_several ALTER COLUMN toasted_key SET STORAGE EXTERNAL
;
create trigger trig_table_insert_trig
  after insert on trig_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
-- bad aggregate type
drop aggregate newcnt (nonesuch)
;
CREATE FUNCTION _ltree_isparent(_ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table idxpart (a int, b int, c int) partition by range(a)
;
-- RLS
ALTER TABLE test_oat_schema.test_oat_tab ENABLE ROW LEVEL SECURITY
;
CREATE OPERATOR FAMILY "public"."part_test_int4_ops2" USING "hash"
;
DROP TABLE IF EXISTS toasted_key
;
ALTER INDEX pg_toast.pg_toast_30556_index RENAME TO reind_con_toast_idx
;
ALTER TABLE pg_toast.pg_toast_30775 RENAME TO reind_con_toast
;
-- should fail

DROP TABLE base_tbl CASCADE
;
CREATE EXTENSION xml2
;
--
-- Test ASSERT
--

do $$
begin
  assert 1=1;  -- should succeed
end;
$$
;
CREATE DOMAIN split_domain_type_next.email_domain AS text COLLATE "C" DEFAULT 'nobody@example.com' CONSTRAINT email_chk CHECK (VALUE LIKE '%@%') NOT NULL
;
CREATE FUNCTION pgstattuple_approx(IN reloid regclass,
    OUT table_len BIGINT,               -- physical table length in bytes
    OUT scanned_percent FLOAT8,         -- what percentage of the table's pages was scanned
    OUT approx_tuple_count BIGINT,      -- estimated number of live tuples
    OUT approx_tuple_len BIGINT,        -- estimated total length in bytes of live tuples
    OUT approx_tuple_percent FLOAT8,    -- live tuples in % (based on estimate)
    OUT dead_tuple_count BIGINT,        -- exact number of dead tuples
    OUT dead_tuple_len BIGINT,          -- exact total length in bytes of dead tuples
    OUT dead_tuple_percent FLOAT8,      -- dead tuples in % (based on estimate)
    OUT approx_free_space BIGINT,       -- estimated free space in bytes
    OUT approx_free_percent FLOAT8)     -- free space in % (based on estimate)
AS '$libdir/pgstattuple', 'pgstattuple_approx'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_28395 RENAME TO reind_con_toast
;
DROP TABLE am_partitioned
;
CREATE TABLE shoelace_data (
	sl_name    char(10),      
	sl_avail   integer,       
	sl_color   char(10),      
	sl_len     float,         
	sl_unit    char(8)        
)
;
-- retest using a quoted and schema qualified table
CREATE SCHEMA "MySchema"
;
DROP TABLE gtest21ax
;
create table s2.p1 (a int, b int) PARTITION BY list (a)
;
-- test that aggs with the same sfunc and initcond share the same agg state
create aggregate my_sum_init(int4)
(
   stype = avg_state,
   sfunc = avg_transfn,
   finalfunc = sum_finalfn,
   initcond = '(10,0)'
)
;
-- ERROR
DROP TABLE ref_table
;
CREATE FUNCTION functest_B_4(int) RETURNS bool LANGUAGE 'sql'
       VOLATILE AS 'SELECT $1 < 0'
;
CREATE FUNCTION isnne(ean13, isbn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE INDEX t1_gist ON s5.t1 USING GIST (b)
;
create table part_40_inf_ab partition of part_40_inf for values in ('ab')
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_config
;
ALTER TABLE pg_temp_9.pg_temp_26555_2 ADD COLUMN newdata pg_temp_9.pg_temp_26555
;
call p1()
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create function polyf(x anycompatiblerange, y anycompatiblearray) returns anycompatiblerange as $$
  select x
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ?@ (
        LEFTARG = _ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = _ltxtq_extract_exec
)
;
----------
[CREATE_TABLE] -- Introduce a NULL and DEFAULT partition so we can test more complex cases
create table mclparted_null partition of mclparted for values in(null)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30394 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION distance_taxicab(cube, cube)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] -- test where the updated table now has both an IMMEDIATE and a DEFERRED
-- constraint pointing into it
CREATE TABLE fkpart10.tbl5(f1 int REFERENCES fkpart10.tbl3)
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON r1 FOR UPDATE USING (a < 20) WITH CHECK (true)
;
----------
[CREATE_TABLE] CREATE TABLE sch1.t1 (c1 int)
;
----------
[CREATE_TABLE] create table child (a float4) inherits (parent)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_uuid_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE seq_unlogged2 INCREMENT 2
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart4"."dropfk" (
    "a" integer
)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION split_pub18_cols
;
----------
[TRUNCATE_TABLE] -- Generate COPY FROM report with FILE, with some excluded tuples.
truncate tab_progress_reporting
;
----------
[CREATE_TABLE] --
-- tests for refcursors
--
create table rc_test (a int, b int)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i7'
;
----------
[SECURITY_LABEL] SECURITY LABEL FOR 'dummy' ON ROLE regress_dummy_seclabel_user2 IS 'unclassified'
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW pg_indexes AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS tablename,
        I.relname AS indexname,
        T.spcname AS tablespace,
        pg_get_indexdef(I.oid) AS indexdef
    FROM pg_index X JOIN pg_class C ON (C.oid = X.indrelid)
         JOIN pg_class I ON (I.oid = X.indexrelid)
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
         LEFT JOIN pg_tablespace T ON (T.oid = I.reltablespace)
    WHERE C.relkind IN ('r', 'm', 'p') AND I.relkind IN ('i', 'I')
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) information_schema.sql_implementation_info
;
----------
[CREATE_TABLE] CREATE TABLE upsert_test_1 PARTITION OF upsert_test FOR VALUES IN (1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE toasted_several ALTER COLUMN toasted_key SET STORAGE EXTERNAL
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_table_insert_trig
  after insert on trig_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[DROP_PROG_OBJ] -- bad aggregate type
drop aggregate newcnt (nonesuch)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_isparent(_ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table idxpart (a int, b int, c int) partition by range(a)
;
----------
[ALTER_TABLE] -- RLS
ALTER TABLE test_oat_schema.test_oat_tab ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY "public"."part_test_int4_ops2" USING "hash"
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS toasted_key
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30556_index RENAME TO reind_con_toast_idx
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30775 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] -- should fail

DROP TABLE base_tbl CASCADE
;
----------
[CREATE_LIBRARY] CREATE EXTENSION xml2
;
----------
[BLOCK] --
-- Test ASSERT
--

do $$
begin
  assert 1=1;  -- should succeed
end;
$$
;
----------
[CREATE_TYPE] CREATE DOMAIN split_domain_type_next.email_domain AS text COLLATE "C" DEFAULT 'nobody@example.com' CONSTRAINT email_chk CHECK (VALUE LIKE '%@%') NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgstattuple_approx(IN reloid regclass,
    OUT table_len BIGINT,               -- physical table length in bytes
    OUT scanned_percent FLOAT8,         -- what percentage of the table's pages was scanned
    OUT approx_tuple_count BIGINT,      -- estimated number of live tuples
    OUT approx_tuple_len BIGINT,        -- estimated total length in bytes of live tuples
    OUT approx_tuple_percent FLOAT8,    -- live tuples in % (based on estimate)
    OUT dead_tuple_count BIGINT,        -- exact number of dead tuples
    OUT dead_tuple_len BIGINT,          -- exact total length in bytes of dead tuples
    OUT dead_tuple_percent FLOAT8,      -- dead tuples in % (based on estimate)
    OUT approx_free_space BIGINT,       -- estimated free space in bytes
    OUT approx_free_percent FLOAT8)     -- free space in % (based on estimate)
AS '$libdir/pgstattuple', 'pgstattuple_approx'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28395 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE am_partitioned
;
----------
[CREATE_TABLE] CREATE TABLE shoelace_data (
	sl_name    char(10),      
	sl_avail   integer,       
	sl_color   char(10),      
	sl_len     float,         
	sl_unit    char(8)        
)
;
----------
[CREATE_SCHEMA] -- retest using a quoted and schema qualified table
CREATE SCHEMA "MySchema"
;
----------
[DROP_TABLE] DROP TABLE gtest21ax
;
----------
[CREATE_TABLE] create table s2.p1 (a int, b int) PARTITION BY list (a)
;
----------
[CREATE_PROG_OBJ] -- test that aggs with the same sfunc and initcond share the same agg state
create aggregate my_sum_init(int4)
(
   stype = avg_state,
   sfunc = avg_transfn,
   finalfunc = sum_finalfn,
   initcond = '(10,0)'
)
;
----------
[DROP_TABLE] -- ERROR
DROP TABLE ref_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_B_4(int) RETURNS bool LANGUAGE 'sql'
       VOLATILE AS 'SELECT $1 < 0'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ean13, isbn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX t1_gist ON s5.t1 USING GIST (b)
;
----------
[CREATE_TABLE] create table part_40_inf_ab partition of part_40_inf for values in ('ab')
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_config
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_26555_2 ADD COLUMN newdata pg_temp_9.pg_temp_26555
;
----------
[CALL_PROG_OBJ] call p1()
;
