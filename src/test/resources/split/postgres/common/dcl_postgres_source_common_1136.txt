ALTER SERVER loopback2 OPTIONS (DROP parallel_commit)
;
begin isolation level serializable
;
EXECUTE dumpTableAttach('32466')
;
EXECUTE getDomainConstraints('36448')
;
ALTER SYSTEM SET synchronous_standby_names = '2(standby1,*,standby2)'
;
EXECUTE dumpTableAttach('18146')
;
EXECUTE dumpFunc('41207')
;
copy (select * from test1 join test2 using (id)) to stdout
;
-- Extended statistics with an attribute that generates invalid stats.
CREATE STATISTICS test_stats ON data, id FROM test_table
;
EXECUTE dumpOpr('798')
;
------------------------------------------------------------------------------------------
[SYSTEM_SETTING_WRITE] ALTER SERVER loopback2 OPTIONS (DROP parallel_commit)
;
----------
[TRANSACTION] begin isolation level serializable
;
----------
[UNSAFE] EXECUTE dumpTableAttach('32466')
;
----------
[UNSAFE] EXECUTE getDomainConstraints('36448')
;
----------
[SYSTEM_SETTING_WRITE] ALTER SYSTEM SET synchronous_standby_names = '2(standby1,*,standby2)'
;
----------
[UNSAFE] EXECUTE dumpTableAttach('18146')
;
----------
[UNSAFE] EXECUTE dumpFunc('41207')
;
----------
[DATA_EXPORT|SELECT] copy (select * from test1 join test2 using (id)) to stdout
;
----------
[ADMIN_PERFORMANCE] -- Extended statistics with an attribute that generates invalid stats.
CREATE STATISTICS test_stats ON data, id FROM test_table
;
----------
[UNSAFE] EXECUTE dumpOpr('798')
;
