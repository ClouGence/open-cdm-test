ALTER INDEX pg_toast.pg_toast_33462_index RENAME TO reind_con_toast_idx
;
CREATE MATERIALIZED VIEW heapmv USING heap AS SELECT * FROM heaptable
;
-- check INSERT...SELECT rule actions are disallowed on commands
-- that have modifyingCTEs
CREATE OR REPLACE RULE bug6051_ins AS ON INSERT TO bug6051 DO INSTEAD
 INSERT INTO bug6051_2
 SELECT NEW.i
;
DO LANGUAGE plpgsql 'BEGIN EXECUTE ''COPY copy1 FROM ''''/src/src/pl/plpgsql/src/data/copy1.data''''''; END'
;
CREATE UNLOGGED TABLE test_gist AS SELECT point(i,i) p, i::text t FROM
    generate_series(1,1000) i
;
-- Check index behavior for circles

CREATE INDEX gcircleind ON circle_tbl USING gist (f1)
;
DROP FOREIGN TABLE child_foreign
;
create temp table slave(f1 int references master deferrable)
;
CREATE FOREIGN TABLE batch_cp_upd_test3_f
	PARTITION OF batch_cp_upd_test
	FOR VALUES IN (3)
	SERVER loopback
	OPTIONS (table_name 'batch_cp_upd_test3', batch_size '1')
;
CREATE AGGREGATE "public"."aggfns"(integer, integer, "text") (
    SFUNC = "public"."aggfns_trans",
    STYPE = "public"."aggtype"[],
    SSPACE = 10000,
    INITCOND = '{}'
)
;
create type rposint as range (subtype = posint)
;
-- OK more than one equal column
create table idxpart (a int4range, b int4range) partition by range (a, b)
;
CREATE FUNCTION "public"."int8alias2in"("cstring") RETURNS "public"."int8alias2"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8in$$
;
CREATE FUNCTION gbt_macad_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE MATERIALIZED VIEW "public"."mvtest_bb" AS
 SELECT "mvtest_tvvmv"."grandtot"
   FROM "public"."mvtest_tvvmv"
  WITH NO DATA
;
CREATE CAST (isbn AS isbn13) WITHOUT FUNCTION AS ASSIGNMENT
;
do $$
declare r1 record;
        r2 r1%type[];
begin
end$$
;
ALTER AGGREGATE alt_agg3(int) RENAME TO alt_agg4
;
CALL test_proc9(_a, _b)
;
CREATE TABLE default_expr_agg (a int DEFAULT (select 1))
;
create function plpgsql_arr_domain_check(val int[]) returns boolean as $$
begin return val[1] > 0; end
$$ language plpgsql immutable
;
ALTER TABLE ONLY "public"."test_storage" ALTER COLUMN "c" SET STORAGE PLAIN
;
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_dba_t:s0
CREATE TABLE tpart_tens PARTITION OF tpart FOR VALUES FROM ('10') TO ('100')
;
-- ISMN13 vs other types:
CREATE FUNCTION btismn13cmp(ismn13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create index t2_z_uidx on t2 (z) where z > 0
;
CREATE INDEX "wowidx" ON "public"."test_tsvector" USING "gin" ("a")
;
-- Ensure parent and child are vacuumed
VACUUM only_inh_parent
;
create function f1() returns int
language plpgsql
as $$
begin
  create table event_trigger_fire6 (a int);
  return 0;
end $$
;
-- drop some tables with serial columns
drop table ext7_table1
;
TRUNCATE index_relfilenodes
;
CREATE TABLE "public"."aggtest" (
    "a" smallint,
    "b" real
)
;
VACUUM ANALYZE smalltbl
;
-- check that temp tables can be clustered
create temp table clstr_temp (col1 int primary key, col2 text)
;
-- Extension: record types returning
CREATE TYPE sqljsonb_rec AS (a int, t text, js json, jb jsonb, jsa json[])
;
ALTER OPERATOR >= (isbn, isbn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
-- now it works
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = true)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
DROP TABLE only_parted
;
create table pktable(ptest1 inet, ptest2 inet, primary key(base1, ptest1), foreign key(base2, ptest2) references
                                             pktable(ptest1, base1)) inherits (pktable_base)
;
--    P    P        N    N
-- should ERROR: ffnp(anyarray) not matched by ffnp(int[])
CREATE AGGREGATE myaggn17a(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
ALTER TABLE ALL IN TABLESPACE regress_tblspace_renamed SET TABLESPACE pg_default
;
create table t7()
;
CREATE SCHEMA regtest_schema_2
;
ALTER TABLE cnn_pk DROP CONSTRAINT cnn_primarykey
;
ANALYZE public.prt1_l_p3_p1
;
/*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
CREATE TABLE beta_pos_p2 PARTITION OF beta_pos FOR VALUES IN ('0004', '0006')
;
DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19433 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
CREATE AGGREGATE myaggp11b(BASETYPE = anyelement, SFUNC = tf1p, STYPE = int[],
  INITCOND = '{}')
;
alter index idxpart_pkey attach partition idxpart0_a_key
;
------------------------------------------------------------------------------------------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33462_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW heapmv USING heap AS SELECT * FROM heaptable
;
----------
[CREATE_POLICY] -- check INSERT...SELECT rule actions are disallowed on commands
-- that have modifyingCTEs
CREATE OR REPLACE RULE bug6051_ins AS ON INSERT TO bug6051 DO INSTEAD
 INSERT INTO bug6051_2
 SELECT NEW.i
;
----------
[BLOCK] DO LANGUAGE plpgsql 'BEGIN EXECUTE ''COPY copy1 FROM ''''/src/src/pl/plpgsql/src/data/copy1.data''''''; END'
;
----------
[CREATE_TABLE|SELECT] CREATE UNLOGGED TABLE test_gist AS SELECT point(i,i) p, i::text t FROM
    generate_series(1,1000) i
;
----------
[ADD_INDEX] -- Check index behavior for circles

CREATE INDEX gcircleind ON circle_tbl USING gist (f1)
;
----------
[DROP_TABLE] DROP FOREIGN TABLE child_foreign
;
----------
[CREATE_TABLE] create temp table slave(f1 int references master deferrable)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE batch_cp_upd_test3_f
	PARTITION OF batch_cp_upd_test
	FOR VALUES IN (3)
	SERVER loopback
	OPTIONS (table_name 'batch_cp_upd_test3', batch_size '1')
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."aggfns"(integer, integer, "text") (
    SFUNC = "public"."aggfns_trans",
    STYPE = "public"."aggtype"[],
    SSPACE = 10000,
    INITCOND = '{}'
)
;
----------
[CREATE_TYPE] create type rposint as range (subtype = posint)
;
----------
[CREATE_TABLE] -- OK more than one equal column
create table idxpart (a int4range, b int4range) partition by range (a, b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int8alias2in"("cstring") RETURNS "public"."int8alias2"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8in$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_bb" AS
 SELECT "mvtest_tvvmv"."grandtot"
   FROM "public"."mvtest_tvvmv"
  WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE CAST (isbn AS isbn13) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[BLOCK] do $$
declare r1 record;
        r2 r1%type[];
begin
end$$
;
----------
[RENAME_PROG_OBJ] ALTER AGGREGATE alt_agg3(int) RENAME TO alt_agg4
;
----------
[CALL_PROG_OBJ] CALL test_proc9(_a, _b)
;
----------
[CREATE_TABLE] CREATE TABLE default_expr_agg (a int DEFAULT (select 1))
;
----------
[CREATE_PROG_OBJ] create function plpgsql_arr_domain_check(val int[]) returns boolean as $$
begin return val[1] > 0; end
$$ language plpgsql immutable
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."test_storage" ALTER COLUMN "c" SET STORAGE PLAIN
;
----------
[CREATE_TABLE] -- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_dba_t:s0
CREATE TABLE tpart_tens PARTITION OF tpart FOR VALUES FROM ('10') TO ('100')
;
----------
[CREATE_PROG_OBJ] -- ISMN13 vs other types:
CREATE FUNCTION btismn13cmp(ismn13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] create index t2_z_uidx on t2 (z) where z > 0
;
----------
[ADD_INDEX] CREATE INDEX "wowidx" ON "public"."test_tsvector" USING "gin" ("a")
;
----------
[ADMIN_TABLE] -- Ensure parent and child are vacuumed
VACUUM only_inh_parent
;
----------
[CREATE_PROG_OBJ] create function f1() returns int
language plpgsql
as $$
begin
  create table event_trigger_fire6 (a int);
  return 0;
end $$
;
----------
[DROP_TABLE] -- drop some tables with serial columns
drop table ext7_table1
;
----------
[TRUNCATE_TABLE] TRUNCATE index_relfilenodes
;
----------
[CREATE_TABLE] CREATE TABLE "public"."aggtest" (
    "a" smallint,
    "b" real
)
;
----------
[ADMIN_TABLE] VACUUM ANALYZE smalltbl
;
----------
[CREATE_TABLE] -- check that temp tables can be clustered
create temp table clstr_temp (col1 int primary key, col2 text)
;
----------
[CREATE_TYPE] -- Extension: record types returning
CREATE TYPE sqljsonb_rec AS (a int, t text, js json, jb jsonb, jsa json[])
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (isbn, isbn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[CREATE_PUB_SUB] -- now it works
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = true)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
----------
[DROP_TABLE] DROP TABLE only_parted
;
----------
[CREATE_TABLE] create table pktable(ptest1 inet, ptest2 inet, primary key(base1, ptest1), foreign key(base2, ptest2) references
                                             pktable(ptest1, base1)) inherits (pktable_base)
;
----------
[CREATE_PROG_OBJ] --    P    P        N    N
-- should ERROR: ffnp(anyarray) not matched by ffnp(int[])
CREATE AGGREGATE myaggn17a(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ALTER_TABLE] ALTER TABLE ALL IN TABLESPACE regress_tblspace_renamed SET TABLESPACE pg_default
;
----------
[CREATE_TABLE] create table t7()
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regtest_schema_2
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE cnn_pk DROP CONSTRAINT cnn_primarykey
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_l_p3_p1
;
----------
[CREATE_POLICY] /*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
----------
[CREATE_TABLE] CREATE TABLE beta_pos_p2 PARTITION OF beta_pos FOR VALUES IN ('0004', '0006')
;
----------
[BLOCK] DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19433 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp11b(BASETYPE = anyelement, SFUNC = tf1p, STYPE = int[],
  INITCOND = '{}')
;
----------
[ALTER_INDEX] alter index idxpart_pkey attach partition idxpart0_a_key
;
