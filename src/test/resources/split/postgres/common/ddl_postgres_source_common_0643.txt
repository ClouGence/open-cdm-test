create table mcrparted3 partition of mcrparted for values from (11, 1, 1) to (20, 10, 10)
;
-- Identity columns have their own default
ALTER TABLE pitest1_p2 ALTER COLUMN f3 SET DEFAULT 10000
;
-- batch insert with foreign partitions.
-- This schema uses two partitions, one local and one remote with a modulo
-- to loop across all of them in batches.
create table tab_batch_local (id int, data text)
;
--Should be in testviewschm2
CREATE   VIEW  pubview AS SELECT * FROM tbl1 WHERE tbl1.a
BETWEEN (SELECT d FROM tbl2 WHERE c = 1) AND (SELECT e FROM tbl3 WHERE f = 2)
AND EXISTS (SELECT g FROM tbl4 LEFT JOIN tbl3 ON tbl4.h = tbl3.f)
;
CREATE TABLE tru_rtable0_child () INHERITS (tru_rtable0)
;
-- test hstore[] -> python
CREATE FUNCTION test1arr(val hstore[]) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
assert(val == [{'aa': 'bb', 'cc': None}, {'dd': 'ee'}])
return len(val)
$$
;
CREATE TABLE inhg (x text, LIKE inhx INCLUDING INDEXES, PRIMARY KEY(x))
;
ALTER TABLE logged3 SET UNLOGGED
;
CREATE TABLE test_replica_identity3 (id int NOT NULL)
;
CREATE TABLE "public"."prt2_n_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
-- Triggers
ALTER TABLE test_oat_schema.test_oat_tab DISABLE TRIGGER test_oat_trigger
;
create type float8range as range (subtype=float8, subtype_diff=float4mi)
;
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_3 FOR VALUES FROM (2000,2000) TO (3000,3000)
;
CREATE TABLE will_change (a int, b text)
;
CREATE TABLE committs_test(x integer, y timestamp with time zone)
;
create table some_tab_child () inherits (some_tab)
;
DROP TABLE alter2.tt8
;
CREATE TABLE BIT_SHIFT_TABLE(b BIT(16))
;
CREATE FUNCTION composite_f(a integer[], out x integer, out y integer) returns
record as $$            begin
        x = a[1];
        y = a[2];
    end;
$$ language plpgsql
;
CREATE TEMP TABLE skip_fetch (a INT, b INT) WITH (fillfactor=10)
;
CREATE COLLATION split_obj_priv.c_from_c FROM "C"
;
create function anyctest(anycompatible, anycompatible)
returns anycompatible as $$
  select greatest($1, $2)
$$ language sql
;
--
-- pgp_sym_decrypt(data, key)
--
CREATE FUNCTION pgp_sym_decrypt(bytea, text)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_sym_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP TABLE pg_temp_12.pg_temp_25367_2, pg_temp_12.pg_temp_25367
;
-- error
DROP TYPE base_type CASCADE
;
ANALYZE f_star
;
alter table parted_uniq_detach_test1 drop constraint parted_uniq_detach_test1_a_key
;
ALTER TABLE regtest_table ALTER b SET STATISTICS -1
;
CREATE FUNCTION isnne(issn13, issn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
---------------------------------------------------
-- UPC:
CREATE FUNCTION isnlt(upc, upc)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE SUBSCRIPTION admin_sub CONNECTION 'port=17787 host=/tmp/fICjfuB_Kh dbname=postgres' PUBLICATION alice
;
CREATE TABLE part_ab PARTITION OF list_parted2 FOR VALUES IN ('a', 'b')
;
create trigger trig_del_after_child after delete on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
CREATE INDEX bloomidx2 ON tst USING bloom (i, t) WITH (col1=0)
;
drop table list_part
;
ANALYZE concur_exprs_tab
;
CREATE INDEX abbrev_abort_uuids__noabort_increasing_idx ON abbrev_abort_uuids (noabort_increasing)
;
ALTER INDEX pg_toast.pg_toast_28456_index RENAME TO reind_con_toast_idx
;
CREATE CONSTRAINT TRIGGER trig_del_fk_parted AFTER DELETE ON fkpart11.fk_parted INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
CREATE POLICY p1 ON copy_t USING (a % 2 = 0)
;
create function sillyaddtwo(int) returns int language plpgsql as
$$ declare r mutable2; begin r.f1 := $1; return r.f1 + 2; end $$
;
CREATE TYPE ghstore (
        INTERNALLENGTH = -1,
        INPUT = ghstore_in,
        OUTPUT = ghstore_out
)
;
DROP TABLE schema_rf_x.tab_rf_x
;
CREATE TABLE pagg_tab2_p1 PARTITION OF pagg_tab2 FOR VALUES FROM (0) TO (10)
;
DROP TABLE tru_rtable0, tru_rtable1, tru_ptable, tru_ptable__p0, tru_pk_table, tru_fk_table,
tru_rtable_parent,tru_rtable_child, tru_rtable0_child
;
DROP TABLESPACE regress_pgbench_tap_1_ts
;
COMMENT ON OPERATOR ###### (int4, NONE) IS 'bad postfix'
;
CREATE TEMP TABLE pgss_dml_tab (a int, b char(20))
;
CREATE INDEX "idxpart22_a_idx" ON "public"."idxpart22" USING "btree" ("a")
;
ANALYZE pg_catalog.pg_ts_config
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table mcrparted3 partition of mcrparted for values from (11, 1, 1) to (20, 10, 10)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Identity columns have their own default
ALTER TABLE pitest1_p2 ALTER COLUMN f3 SET DEFAULT 10000
;
----------
[CREATE_TABLE] -- batch insert with foreign partitions.
-- This schema uses two partitions, one local and one remote with a modulo
-- to loop across all of them in batches.
create table tab_batch_local (id int, data text)
;
----------
[CREATE_VIEW(SELECT)] --Should be in testviewschm2
CREATE   VIEW  pubview AS SELECT * FROM tbl1 WHERE tbl1.a
BETWEEN (SELECT d FROM tbl2 WHERE c = 1) AND (SELECT e FROM tbl3 WHERE f = 2)
AND EXISTS (SELECT g FROM tbl4 LEFT JOIN tbl3 ON tbl4.h = tbl3.f)
;
----------
[CREATE_TABLE] CREATE TABLE tru_rtable0_child () INHERITS (tru_rtable0)
;
----------
[CREATE_PROG_OBJ] -- test hstore[] -> python
CREATE FUNCTION test1arr(val hstore[]) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
assert(val == [{'aa': 'bb', 'cc': None}, {'dd': 'ee'}])
return len(val)
$$
;
----------
[CREATE_TABLE] CREATE TABLE inhg (x text, LIKE inhx INCLUDING INDEXES, PRIMARY KEY(x))
;
----------
[ALTER_TABLE] ALTER TABLE logged3 SET UNLOGGED
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_identity3 (id int NOT NULL)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_n_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ALTER_TABLE] -- Triggers
ALTER TABLE test_oat_schema.test_oat_tab DISABLE TRIGGER test_oat_trigger
;
----------
[CREATE_TYPE] create type float8range as range (subtype=float8, subtype_diff=float4mi)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_3 FOR VALUES FROM (2000,2000) TO (3000,3000)
;
----------
[CREATE_TABLE] CREATE TABLE will_change (a int, b text)
;
----------
[CREATE_TABLE] CREATE TABLE committs_test(x integer, y timestamp with time zone)
;
----------
[CREATE_TABLE] create table some_tab_child () inherits (some_tab)
;
----------
[DROP_TABLE] DROP TABLE alter2.tt8
;
----------
[CREATE_TABLE] CREATE TABLE BIT_SHIFT_TABLE(b BIT(16))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION composite_f(a integer[], out x integer, out y integer) returns
record as $$            begin
        x = a[1];
        y = a[2];
    end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE skip_fetch (a INT, b INT) WITH (fillfactor=10)
;
----------
[CREATE_POLICY] CREATE COLLATION split_obj_priv.c_from_c FROM "C"
;
----------
[CREATE_PROG_OBJ] create function anyctest(anycompatible, anycompatible)
returns anycompatible as $$
  select greatest($1, $2)
$$ language sql
;
----------
[CREATE_PROG_OBJ] --
-- pgp_sym_decrypt(data, key)
--
CREATE FUNCTION pgp_sym_decrypt(bytea, text)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_sym_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE pg_temp_12.pg_temp_25367_2, pg_temp_12.pg_temp_25367
;
----------
[DROP_TYPE] -- error
DROP TYPE base_type CASCADE
;
----------
[ADMIN_TABLE] ANALYZE f_star
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table parted_uniq_detach_test1 drop constraint parted_uniq_detach_test1_a_key
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_table ALTER b SET STATISTICS -1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(issn13, issn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- UPC:
CREATE FUNCTION isnlt(upc, upc)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION admin_sub CONNECTION 'port=17787 host=/tmp/fICjfuB_Kh dbname=postgres' PUBLICATION alice
;
----------
[CREATE_TABLE] CREATE TABLE part_ab PARTITION OF list_parted2 FOR VALUES IN ('a', 'b')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_del_after_child after delete on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
----------
[ADD_INDEX] CREATE INDEX bloomidx2 ON tst USING bloom (i, t) WITH (col1=0)
;
----------
[DROP_TABLE] drop table list_part
;
----------
[ADMIN_TABLE] ANALYZE concur_exprs_tab
;
----------
[ADD_INDEX] CREATE INDEX abbrev_abort_uuids__noabort_increasing_idx ON abbrev_abort_uuids (noabort_increasing)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28456_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE CONSTRAINT TRIGGER trig_del_fk_parted AFTER DELETE ON fkpart11.fk_parted INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON copy_t USING (a % 2 = 0)
;
----------
[CREATE_PROG_OBJ] create function sillyaddtwo(int) returns int language plpgsql as
$$ declare r mutable2; begin r.f1 := $1; return r.f1 + 2; end $$
;
----------
[CREATE_TYPE] CREATE TYPE ghstore (
        INTERNALLENGTH = -1,
        INPUT = ghstore_in,
        OUTPUT = ghstore_out
)
;
----------
[DROP_TABLE] DROP TABLE schema_rf_x.tab_rf_x
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab2_p1 PARTITION OF pagg_tab2 FOR VALUES FROM (0) TO (10)
;
----------
[DROP_TABLE] DROP TABLE tru_rtable0, tru_rtable1, tru_ptable, tru_ptable__p0, tru_pk_table, tru_fk_table,
tru_rtable_parent,tru_rtable_child, tru_rtable0_child
;
----------
[DROP_TABLESPACE] DROP TABLESPACE regress_pgbench_tap_1_ts
;
----------
[COMMENT_PROG_OBJ] COMMENT ON OPERATOR ###### (int4, NONE) IS 'bad postfix'
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pgss_dml_tab (a int, b char(20))
;
----------
[ADD_INDEX] CREATE INDEX "idxpart22_a_idx" ON "public"."idxpart22" USING "btree" ("a")
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_ts_config
;
