create or replace view agg_view1 as
  select aggfns(a,b,c order by b+1)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c)
;
CREATE VIEW s2.t2_view AS (
				SELECT i*2, t FROM s2.t2
			)
;
DROP TABLE J2_TBL
;
CREATE TEMP TABLE x (
	a serial,
	b int,
	c text not null default 'stuff',
	d text,
	e text
)
;
DROP TABLE test_hash
;
CREATE FUNCTION gbt_enum_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE FUNCTION gist_page_items(IN page bytea,
    IN index_oid regclass,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT dead boolean,
    OUT keys text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'gist_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
create table filler (a int, b text)
;
create table part_abc_3_3 partition of part_abc_3 for values from (100) to (maxvalue)
;
CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=26249 host=/tmp/OCc4XFvOJI dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
CREATE TABLE inh_nn1 (a int not null)
;
CALL test_proc6(2, c => y, b => x)
;
CREATE FUNCTION test4() RETURNS trigger
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
assert(TD["new"] == {'a': 1, 'b': {'aa': 'bb', 'cc': None}})
if TD["new"]["a"] == 1:
    TD["new"]["b"] = {'a': 1, 'b': 'boo', 'c': None}

return "MODIFY"
$$
;
create schema fkpart0
  create table pkey (a int primary key)
  create table fk_part (a int) partition by list (a)
  create table fk_part_1 partition of fk_part
      (foreign key (a) references fkpart0.pkey) for values in (1)
  create table fk_part_23 partition of fk_part
      (foreign key (a) references fkpart0.pkey) for values in (2, 3)
      partition by list (a)
  create table fk_part_23_2 partition of fk_part_23 for values in (2)
;
CREATE FUNCTION _ltxtq_extract_exec(_ltree,ltxtquery)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table part_10_20_ab partition of part_10_20 for values in ('ab')
;
CREATE TABLE fk (a int) PARTITION BY LIST (a)
;
ALTER OPERATOR FAMILY gist_bool_ops USING gist ADD
	FUNCTION	11  (bool, bool) gbt_bool_sortsupport (internal)
;
-- Failure when using a non-btree index.
CREATE INDEX test1_a_hash ON test1 USING hash(a)
;
CREATE AGGREGATE myaggp04b(*) (SFUNC = stfp, STYPE = anyarray,
  INITCOND = '{}')
;
CREATE INDEX ptif_test3_index ON ptif_test3 (a)
;
-- Check that attr_needed is updated correctly after self-join removal. In this
-- test, the join of j1 with j2 is removed. k1.b is required at either j1 or j2.
-- If this info is lost, join targetlist for (k1, k2) will not contain k1.b.
-- Use index scan for k1 so that we don't get 'b' from physical tlist used for
-- seqscan. Also disable reordering of joins because this test depends on a
-- particular join tree.
create table sk (a int, b int)
;
VACUUM (PARALLEL 0) pg_catalog.pg_type
;
DROP TABLE pg_temp_4.pg_temp_37940_2, pg_temp_4.pg_temp_37940
;
DROP FUNCTION ri_child()
;
ANALYZE pg_temp_52.pg_temp_29691_2
;
CREATE TABLE dump_test.test_fifth_table (
							col1 integer,
							col2 boolean,
							col3 boolean,
							col4 bit(5),
							col5 float8
					   )
;
VACUUM (PARALLEL 0) pg_catalog.pg_amop
;
COMMENT ON COLUMN "public"."comment_test_child"."id" IS 'Column ''id'' on comment_test_child'
;
CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') PRIMARY KEY,
	c2 text OPTIONS (param2 'val2', param3 'val3'),
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
-- value exceeds max size error
VACUUM (BUFFER_USAGE_LIMIT 16777220) vac_option_tab
;
DO $$
BEGIN
    CALL transaction_test9b(report_count());
    INSERT INTO test2 VALUES(43);
    CALL transaction_test9b(report_count());
END
$$
;
CREATE TABLE "public"."tab_core_types" (
    "point" "point",
    "line" "line",
    "lseg" "lseg",
    "box" "box",
    "openedpath" "path",
    "closedpath" "path",
    "polygon" "polygon",
    "circle" "circle",
    "date" "date",
    "time" time without time zone,
    "timestamp" timestamp without time zone,
    "timetz" time with time zone,
    "timestamptz" timestamp with time zone,
    "interval" interval,
    "json" "json",
    "jsonb" "jsonb",
    "jsonpath" "jsonpath",
    "inet" "inet",
    "cidr" "cidr",
    "macaddr8" "macaddr8",
    "macaddr" "macaddr",
    "int2" smallint,
    "int4" integer,
    "int8" bigint,
    "float4" real,
    "float8" double precision,
    "pi" numeric,
    "char" "char",
    "bpchar" "bpchar",
    "varchar" character varying,
    "name" "name",
    "text" "text",
    "bool" boolean,
    "bytea" "bytea",
    "bit" bit(1),
    "varbit" bit varying,
    "money" "money",
    "refcursor" "refcursor",
    "int2vector" "int2vector",
    "oidvector" "oidvector",
    "aclitem" "aclitem",
    "tsvector" "tsvector",
    "tsquery" "tsquery",
    "uuid" "uuid",
    "regclass" "regclass",
    "regtype" "regtype",
    "regrole" "regrole",
    "oid" "oid",
    "tid" "tid",
    "xid" "xid",
    "cid" "cid",
    "txid_snapshot" "txid_snapshot",
    "pg_lsn" "pg_lsn",
    "cardinal_number" "information_schema"."cardinal_number",
    "character_data" "information_schema"."character_data",
    "sql_identifier" "information_schema"."sql_identifier",
    "time_stamp" "information_schema"."time_stamp",
    "yes_or_no" "information_schema"."yes_or_no",
    "planets" "public"."planets",
    "insenum" "public"."insenum",
    "int4range" "int4range",
    "int8range" "int8range",
    "float8range" "public"."float8range",
    "numrange" "numrange",
    "textrange" "public"."textrange",
    "cashrange" "public"."cashrange",
    "daterange" "daterange",
    "tsrange" "tsrange",
    "tstzrange" "tstzrange",
    "arrayrange" "public"."arrayrange"
)
;
CREATE SEQUENCE sequence_testx INCREMENT BY 1 MAXVALUE -20
;
DROP TABLE first_client_table, xy
;
CREATE FOREIGN TABLE ftprt1_p2 PARTITION OF fprt1 FOR VALUES FROM (250) TO (500)
	SERVER loopback OPTIONS (TABLE_NAME 'fprt1_p2')
;
CREATE TABLE inht3 (y int) INHERITS (inht1)
;
-- test multiple set not null at same time
-- test_a checked by atacc1_constr_a_valid, test_b should fail by table scan
alter table atacc1 alter test_a set not null, alter test_b set not null
;
ALTER INDEX pt_stats_index ATTACH PARTITION pt_stats2_index
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "file" WITH "simple"
;
-- setof does what's expected
create or replace function insert_tt2(text,text) returns setof int as
$$ insert into tt(data) values($1),($2) returning f1 $$
language sql
;
/*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
DROP TRIGGER c ON a
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_opclass
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c6 SET NOT NULL
;
ALTER TABLE pg_toast.pg_toast_27937 RENAME TO reind_con_toast
;
CLUSTER clstr_2 USING clstr_2_pkey
;
ALTER TABLE itest8
  ADD COLUMN f4 int
;
COMMENT ON AGGREGATE newavg (int4) IS NULL
;
create view view_of_joins as
select * from
  (select * from (tbl1 cross join tbl2) same) ss,
  (tbl3 cross join tbl4) same
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] create or replace view agg_view1 as
  select aggfns(a,b,c order by b+1)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW s2.t2_view AS (
				SELECT i*2, t FROM s2.t2
			)
;
----------
[DROP_TABLE] DROP TABLE J2_TBL
;
----------
[CREATE_TABLE] CREATE TEMP TABLE x (
	a serial,
	b int,
	c text not null default 'stuff',
	d text,
	e text
)
;
----------
[DROP_TABLE] DROP TABLE test_hash
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_enum_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gist_page_items(IN page bytea,
    IN index_oid regclass,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT dead boolean,
    OUT keys text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'gist_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table filler (a int, b text)
;
----------
[CREATE_TABLE] create table part_abc_3_3 partition of part_abc_3 for values from (100) to (maxvalue)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=26249 host=/tmp/OCc4XFvOJI dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[CREATE_TABLE] CREATE TABLE inh_nn1 (a int not null)
;
----------
[CALL_PROG_OBJ] CALL test_proc6(2, c => y, b => x)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test4() RETURNS trigger
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
assert(TD["new"] == {'a': 1, 'b': {'aa': 'bb', 'cc': None}})
if TD["new"]["a"] == 1:
    TD["new"]["b"] = {'a': 1, 'b': 'boo', 'c': None}

return "MODIFY"
$$
;
----------
[CREATE_SCHEMA] create schema fkpart0
  create table pkey (a int primary key)
  create table fk_part (a int) partition by list (a)
  create table fk_part_1 partition of fk_part
      (foreign key (a) references fkpart0.pkey) for values in (1)
  create table fk_part_23 partition of fk_part
      (foreign key (a) references fkpart0.pkey) for values in (2, 3)
      partition by list (a)
  create table fk_part_23_2 partition of fk_part_23 for values in (2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltxtq_extract_exec(_ltree,ltxtquery)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table part_10_20_ab partition of part_10_20 for values in ('ab')
;
----------
[CREATE_TABLE] CREATE TABLE fk (a int) PARTITION BY LIST (a)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bool_ops USING gist ADD
	FUNCTION	11  (bool, bool) gbt_bool_sortsupport (internal)
;
----------
[ADD_INDEX] -- Failure when using a non-btree index.
CREATE INDEX test1_a_hash ON test1 USING hash(a)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp04b(*) (SFUNC = stfp, STYPE = anyarray,
  INITCOND = '{}')
;
----------
[ADD_INDEX] CREATE INDEX ptif_test3_index ON ptif_test3 (a)
;
----------
[CREATE_TABLE] -- Check that attr_needed is updated correctly after self-join removal. In this
-- test, the join of j1 with j2 is removed. k1.b is required at either j1 or j2.
-- If this info is lost, join targetlist for (k1, k2) will not contain k1.b.
-- Use index scan for k1 so that we don't get 'b' from physical tlist used for
-- seqscan. Also disable reordering of joins because this test depends on a
-- particular join tree.
create table sk (a int, b int)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_type
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_37940_2, pg_temp_4.pg_temp_37940
;
----------
[DROP_PROG_OBJ] DROP FUNCTION ri_child()
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_52.pg_temp_29691_2
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_fifth_table (
							col1 integer,
							col2 boolean,
							col3 boolean,
							col4 bit(5),
							col5 float8
					   )
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_amop
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN "public"."comment_test_child"."id" IS 'Column ''id'' on comment_test_child'
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') PRIMARY KEY,
	c2 text OPTIONS (param2 'val2', param3 'val3'),
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[ADMIN_TABLE] -- value exceeds max size error
VACUUM (BUFFER_USAGE_LIMIT 16777220) vac_option_tab
;
----------
[BLOCK] DO $$
BEGIN
    CALL transaction_test9b(report_count());
    INSERT INTO test2 VALUES(43);
    CALL transaction_test9b(report_count());
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tab_core_types" (
    "point" "point",
    "line" "line",
    "lseg" "lseg",
    "box" "box",
    "openedpath" "path",
    "closedpath" "path",
    "polygon" "polygon",
    "circle" "circle",
    "date" "date",
    "time" time without time zone,
    "timestamp" timestamp without time zone,
    "timetz" time with time zone,
    "timestamptz" timestamp with time zone,
    "interval" interval,
    "json" "json",
    "jsonb" "jsonb",
    "jsonpath" "jsonpath",
    "inet" "inet",
    "cidr" "cidr",
    "macaddr8" "macaddr8",
    "macaddr" "macaddr",
    "int2" smallint,
    "int4" integer,
    "int8" bigint,
    "float4" real,
    "float8" double precision,
    "pi" numeric,
    "char" "char",
    "bpchar" "bpchar",
    "varchar" character varying,
    "name" "name",
    "text" "text",
    "bool" boolean,
    "bytea" "bytea",
    "bit" bit(1),
    "varbit" bit varying,
    "money" "money",
    "refcursor" "refcursor",
    "int2vector" "int2vector",
    "oidvector" "oidvector",
    "aclitem" "aclitem",
    "tsvector" "tsvector",
    "tsquery" "tsquery",
    "uuid" "uuid",
    "regclass" "regclass",
    "regtype" "regtype",
    "regrole" "regrole",
    "oid" "oid",
    "tid" "tid",
    "xid" "xid",
    "cid" "cid",
    "txid_snapshot" "txid_snapshot",
    "pg_lsn" "pg_lsn",
    "cardinal_number" "information_schema"."cardinal_number",
    "character_data" "information_schema"."character_data",
    "sql_identifier" "information_schema"."sql_identifier",
    "time_stamp" "information_schema"."time_stamp",
    "yes_or_no" "information_schema"."yes_or_no",
    "planets" "public"."planets",
    "insenum" "public"."insenum",
    "int4range" "int4range",
    "int8range" "int8range",
    "float8range" "public"."float8range",
    "numrange" "numrange",
    "textrange" "public"."textrange",
    "cashrange" "public"."cashrange",
    "daterange" "daterange",
    "tsrange" "tsrange",
    "tstzrange" "tstzrange",
    "arrayrange" "public"."arrayrange"
)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_testx INCREMENT BY 1 MAXVALUE -20
;
----------
[DROP_TABLE] DROP TABLE first_client_table, xy
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ftprt1_p2 PARTITION OF fprt1 FOR VALUES FROM (250) TO (500)
	SERVER loopback OPTIONS (TABLE_NAME 'fprt1_p2')
;
----------
[CREATE_TABLE] CREATE TABLE inht3 (y int) INHERITS (inht1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- test multiple set not null at same time
-- test_a checked by atacc1_constr_a_valid, test_b should fail by table scan
alter table atacc1 alter test_a set not null, alter test_b set not null
;
----------
[ALTER_INDEX] ALTER INDEX pt_stats_index ATTACH PARTITION pt_stats2_index
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "file" WITH "simple"
;
----------
[CREATE_PROG_OBJ] -- setof does what's expected
create or replace function insert_tt2(text,text) returns setof int as
$$ insert into tt(data) values($1),($2) returning f1 $$
language sql
;
----------
[CREATE_POLICY] /*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
----------
[DROP_TRIGGER] DROP TRIGGER c ON a
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_opclass
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c6 SET NOT NULL
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27937 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] CLUSTER clstr_2 USING clstr_2_pkey
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE itest8
  ADD COLUMN f4 int
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE newavg (int4) IS NULL
;
----------
[CREATE_VIEW(SELECT)] create view view_of_joins as
select * from
  (select * from (tbl1 cross join tbl2) same) ss,
  (tbl3 cross join tbl4) same
;
