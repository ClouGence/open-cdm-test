/* contrib/pgcrypto/pgcrypto--1.3.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION digest(text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_digest'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE t(a int)
;
CREATE TABLE functional_dependencies (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b TEXT,
    filler3 DATE,
    c INT,
    d TEXT
)
WITH (autovacuum_enabled = off)
;
CREATE AGGREGATE invalidsumdouble (float8)
(
    stype = float8,
    sfunc = float8pl,
    mstype = float8,
    msfunc = float8pl,
    minvfunc = float8mi_n
)
;
CREATE TABLE prt3_adv (a int, b int, c varchar) PARTITION BY RANGE (a)
;
drop function anyctest(anycompatiblemultirange, anycompatiblemultirange)
;
create unlogged table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=98) tablespace "pg_default"
;
CREATE TABLE "regress_indexing"."pk2" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer NOT NULL
)
;
ALTER TABLE pitest2_p1 ALTER COLUMN f3 SET INCREMENT BY 2
;
create function polyf(x anycompatible) returns anycompatiblerange as $$
  select array[x + 1, x + 2]
$$ language sql
;
CREATE INDEX concur_reindex_part_index_0 ON ONLY concur_reindex_part_0 (c1)
;
DROP FUNCTION IF EXISTS foo(no_such_type)
;
drop table inh_parent cascade
;
ALTER FOREIGN TABLE ft1 OPTIONS (SET table_name 'T 1')
;
create table mlparted5 partition of mlparted
  for values from (1, 40) to (1, 50) partition by range (c)
;
create table insupd_test ( a int , b int )
;
COMMENT ON LANGUAGE plpgsql IS 'PL/pgSQL procedural language'
;
ALTER TABLE pg_toast.pg_toast_27845 RENAME TO reind_con_toast
;
-- Not OK with just -|-
create table idxpart (a int4range, b int4range) partition by range (a, b)
;
DROP TABLE test_primary_constraints
;
alter table gc1 drop column name
;
analyze patest0
;
CREATE TABLE test_replica_identity_othertable (id serial primary key)
;
CREATE TABLE parttab2 (key INTEGER PRIMARY KEY, col1 text, col2 text)
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.time()'))
;
CREATE TRIGGER y_trig AFTER INSERT ON y FOR EACH STATEMENT
    EXECUTE PROCEDURE y_trigger()
;
-- Leave some constraints for the pg_upgrade test to pick up
CREATE DOMAIN constraint_comments_dom AS int
;
CREATE PUBLICATION testpub_xxx WITH (publish_generated_columns = stored, publish_generated_columns = none)
;
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 150, REMAINDER 3)
;
ANALYZE testxmlschema.test2
;
-- Functions
CREATE FUNCTION func_stats(a text DEFAULT 'a_data', b text DEFAULT lower('b_data'))
  RETURNS text AS $$ SELECT $1::text || '_' || $2::text; $$ LANGUAGE SQL
;
CREATE TEMP TABLE pg_temp_22.pg_temp_28002_2 (tid pg_catalog.tid)
;
create table tuplesest_parted (a int, b int, c float) partition by range(a)
;
ANALYZE public.found_test_tbl
;
ANALYZE public.rtest_system
;
CREATE AGGREGATE "public"."myaggp08a"("anyelement") (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
CREATE FUNCTION _ltree_union(internal, internal)
RETURNS ltree_gist
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP PUBLICATION regress_pub2_gen_to_nogen
;
-- ok (parent itself contains no violating rows)
alter table parent_noinh_convalid validate constraint check_a_is_2
;
--
-- CREATE OR REPLACE RULE
--

CREATE TABLE ruletest_tbl (a int, b int)
;
/* LIKE with other relation kinds */

CREATE TABLE ctlt4 (a int, b text)
;
DROP TABLE loct
;
TRUNCATE gtest26
;
CREATE DOMAIN "public"."testboolxmldomain" AS boolean
;
create table stable_qual_pruning3 partition of stable_qual_pruning
  for values from ('3000-02-01') to ('3000-03-01')
;
create function namedparmcursor_test9(p1 int) returns int4 as $$
declare
  c1 cursor (p1 int, p2 int, debug int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2
      and four = debug;
  p2 int4 := 1006;
  n int4;
begin
  -- use both supported syntaxes for named arguments
  open c1 (p1 := p1, p2 => p2, debug => 2);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
CREATE TABLE part4 PARTITION OF pa_target DEFAULT
  WITH (autovacuum_enabled=off)
;
create table cc1 (f2 text, f3 int) inherits (pp1)
;
--not-indexed
CREATE OPERATOR ^@ (
        LEFTARG = ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = ltxtq_exec,
	COMMUTATOR = '^@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
DROP SCHEMA IF EXISTS split_select_lock CASCADE
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] /* contrib/pgcrypto/pgcrypto--1.3.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION digest(text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_digest'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE t(a int)
;
----------
[CREATE_TABLE] CREATE TABLE functional_dependencies (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b TEXT,
    filler3 DATE,
    c INT,
    d TEXT
)
WITH (autovacuum_enabled = off)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE invalidsumdouble (float8)
(
    stype = float8,
    sfunc = float8pl,
    mstype = float8,
    msfunc = float8pl,
    minvfunc = float8mi_n
)
;
----------
[CREATE_TABLE] CREATE TABLE prt3_adv (a int, b int, c varchar) PARTITION BY RANGE (a)
;
----------
[DROP_PROG_OBJ] drop function anyctest(anycompatiblemultirange, anycompatiblemultirange)
;
----------
[CREATE_TABLE] create unlogged table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=98) tablespace "pg_default"
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk2" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer NOT NULL
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest2_p1 ALTER COLUMN f3 SET INCREMENT BY 2
;
----------
[CREATE_PROG_OBJ] create function polyf(x anycompatible) returns anycompatiblerange as $$
  select array[x + 1, x + 2]
$$ language sql
;
----------
[ADD_INDEX] CREATE INDEX concur_reindex_part_index_0 ON ONLY concur_reindex_part_0 (c1)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS foo(no_such_type)
;
----------
[DROP_TABLE] drop table inh_parent cascade
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE ft1 OPTIONS (SET table_name 'T 1')
;
----------
[CREATE_TABLE] create table mlparted5 partition of mlparted
  for values from (1, 40) to (1, 50) partition by range (c)
;
----------
[CREATE_TABLE] create table insupd_test ( a int , b int )
;
----------
[COMMENT_LANGUAGE] COMMENT ON LANGUAGE plpgsql IS 'PL/pgSQL procedural language'
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27845 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- Not OK with just -|-
create table idxpart (a int4range, b int4range) partition by range (a, b)
;
----------
[DROP_TABLE] DROP TABLE test_primary_constraints
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table gc1 drop column name
;
----------
[ADMIN_TABLE] analyze patest0
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_identity_othertable (id serial primary key)
;
----------
[CREATE_TABLE] CREATE TABLE parttab2 (key INTEGER PRIMARY KEY, col1 text, col2 text)
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.time()'))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER y_trig AFTER INSERT ON y FOR EACH STATEMENT
    EXECUTE PROCEDURE y_trigger()
;
----------
[CREATE_TYPE] -- Leave some constraints for the pg_upgrade test to pick up
CREATE DOMAIN constraint_comments_dom AS int
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_xxx WITH (publish_generated_columns = stored, publish_generated_columns = none)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 150, REMAINDER 3)
;
----------
[ADMIN_TABLE] ANALYZE testxmlschema.test2
;
----------
[CREATE_PROG_OBJ] -- Functions
CREATE FUNCTION func_stats(a text DEFAULT 'a_data', b text DEFAULT lower('b_data'))
  RETURNS text AS $$ SELECT $1::text || '_' || $2::text; $$ LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_22.pg_temp_28002_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table tuplesest_parted (a int, b int, c float) partition by range(a)
;
----------
[ADMIN_TABLE] ANALYZE public.found_test_tbl
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_system
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp08a"("anyelement") (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_union(internal, internal)
RETURNS ltree_gist
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_PUB_SUB] DROP PUBLICATION regress_pub2_gen_to_nogen
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- ok (parent itself contains no violating rows)
alter table parent_noinh_convalid validate constraint check_a_is_2
;
----------
[CREATE_TABLE] --
-- CREATE OR REPLACE RULE
--

CREATE TABLE ruletest_tbl (a int, b int)
;
----------
[CREATE_TABLE] /* LIKE with other relation kinds */

CREATE TABLE ctlt4 (a int, b text)
;
----------
[DROP_TABLE] DROP TABLE loct
;
----------
[TRUNCATE_TABLE] TRUNCATE gtest26
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."testboolxmldomain" AS boolean
;
----------
[CREATE_TABLE] create table stable_qual_pruning3 partition of stable_qual_pruning
  for values from ('3000-02-01') to ('3000-03-01')
;
----------
[CREATE_PROG_OBJ] create function namedparmcursor_test9(p1 int) returns int4 as $$
declare
  c1 cursor (p1 int, p2 int, debug int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2
      and four = debug;
  p2 int4 := 1006;
  n int4;
begin
  -- use both supported syntaxes for named arguments
  open c1 (p1 := p1, p2 => p2, debug => 2);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE part4 PARTITION OF pa_target DEFAULT
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE] create table cc1 (f2 text, f3 int) inherits (pp1)
;
----------
[CREATE_PROG_OBJ] --not-indexed
CREATE OPERATOR ^@ (
        LEFTARG = ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = ltxtq_exec,
	COMMUTATOR = '^@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_select_lock CASCADE
;
