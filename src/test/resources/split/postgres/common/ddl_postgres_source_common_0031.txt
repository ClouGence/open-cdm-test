ALTER TABLE pg_toast.pg_toast_30649 RENAME TO reind_con_toast
;
DROP TABLE force_snapshot
;
ALTER TABLE pg_shdescription ADD PRIMARY KEY USING INDEX pg_shdescription_o_c_index
;
CREATE VIEW pg_stat_wal_receiver AS
    SELECT
            s.pid,
            s.status,
            s.receive_start_lsn,
            s.receive_start_tli,
            s.received_lsn,
            s.received_tli,
            s.last_msg_send_time,
            s.last_msg_receipt_time,
            s.latest_end_lsn,
            s.latest_end_time,
            s.slot_name,
            s.sender_host,
            s.sender_port,
            s.conninfo
    FROM pg_stat_get_wal_receiver() s
    WHERE s.pid IS NOT NULL
;
/* LIKE with other relation kinds */
CREATE TABLE ctlt4 (a int, b text)
;
CREATE POLICY p2 ON z1 TO regress_rls_group2 USING (a % 2 = 1)
;
create temp table cc (f1 int references pp on update restrict on delete restrict)
;
CREATE FUNCTION gbt_int4_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
alter table if exists tcweatherdata
add column predictiontime timestamp
;
CREATE FUNCTION gbt_macad8_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE TABLE "public"."prt1_m_p3" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
CREATE INDEX test_i ON stats_import.test(id)
;
ALTER TABLE pg_toast.pg_toast_33600 RENAME TO reind_con_toast
;
CREATE SEQUENCE "public"."itest5_a_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
CREATE TABLE TIMETZ_TBL (f1 time(2) with time zone)
;
ANALYZE group_tbl
;
ANALYZE pg_temp_4.pg_temp_27609_2
;
--
-- IF EXISTS test
--
ALTER TABLE IF EXISTS tt8 ADD COLUMN f int
;
CREATE RULE "shoelace_ok_ins" AS
    ON INSERT TO "public"."shoelace_ok" DO INSTEAD  UPDATE "public"."shoelace" SET "sl_avail" = ("shoelace"."sl_avail" + "new"."ok_quant")
  WHERE ("shoelace"."sl_name" = "new"."ok_name")
;
ALTER SEQUENCE split_sequence_next.seq_full RESTART
;
CREATE FUNCTION gbt_ts_union(internal, internal)
RETURNS gbtreekey16
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER INDEX pg_toast.pg_toast_30806_index RENAME TO reind_con_toast_idx
;
CREATE TABLE tableam_tblas_heapx AS SELECT * FROM tableam_tbl_heapx
;
create table nantest2 ( id int4 , d numeric )
;
CREATE TABLE regress_roleoption.t4 (a int)
;
create table tt9 (x int, xx int, y int)
;
ALTER TABLE reloptions_test SET (autovacuum_enabled, fillfactor=32)
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN IF NOT EXISTS c3 integer, -- skipping because c3 already exists
	ADD COLUMN c4 integer
;
-- should work
DROP CAST (text AS casttesttype)
;
ALTER TABLE pg_trigger ADD PRIMARY KEY USING INDEX pg_trigger_oid_index
;
CREATE TABLE test_missing_target2 AS
SELECT count(*)
FROM test_missing_target x, test_missing_target y
	WHERE x.a = y.a
	GROUP BY x.b ORDER BY x.b
;
ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p1" FOR VALUES IN ('0000', '0001', '0002', '0003', '0004')
;
create type pp_rectype as (a int, b int)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_auth_members
;
CREATE TABLE "public"."bt_name_heap" (
    "seqno" "name",
    "random" integer
)
;
DROP TABLE temporal_partitioned
;
CREATE TABLE pg_catalog.test (a int)
;
CREATE TEXT SEARCH CONFIGURATION synonym_tst (
						COPY=english
)
;
CREATE TABLE pxtest1 (foobar VARCHAR(10))
;
COMMENT ON TRIGGER before_ins_stmt_trig ON main_table IS NULL
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdescription
;
ALTER INDEX pg_toast.pg_toast_30505_index RENAME TO reind_con_toast_idx
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0
;
--
-- partitioned FK referenced deletes NO ACTION
--

TRUNCATE temporal_partitioned_mltrng, temporal_partitioned_fk_mltrng2mltrng
;
-- not null not valid with partitions
CREATE TABLE atnnparted (id int, col1 int) PARTITION BY LIST (id)
;
ALTER TABLE ONLY "public"."tbl_include_box"
    ADD CONSTRAINT "tbl_include_box_idx_unique" PRIMARY KEY ("c1", "c2") INCLUDE ("c3", "c4")
;
VACUUM freespace_tab
;
DROP SCHEMA test_func_dep3
;
DROP TABLE hash_cleanup_heap
;
------------------------------------------------------------------------------------------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30649 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE force_snapshot
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_shdescription ADD PRIMARY KEY USING INDEX pg_shdescription_o_c_index
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_wal_receiver AS
    SELECT
            s.pid,
            s.status,
            s.receive_start_lsn,
            s.receive_start_tli,
            s.received_lsn,
            s.received_tli,
            s.last_msg_send_time,
            s.last_msg_receipt_time,
            s.latest_end_lsn,
            s.latest_end_time,
            s.slot_name,
            s.sender_host,
            s.sender_port,
            s.conninfo
    FROM pg_stat_get_wal_receiver() s
    WHERE s.pid IS NOT NULL
;
----------
[CREATE_TABLE] /* LIKE with other relation kinds */
CREATE TABLE ctlt4 (a int, b text)
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON z1 TO regress_rls_group2 USING (a % 2 = 1)
;
----------
[CREATE_TABLE] create temp table cc (f1 int references pp on update restrict on delete restrict)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int4_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table if exists tcweatherdata
add column predictiontime timestamp
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad8_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_m_p3" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
----------
[ADD_INDEX] CREATE INDEX test_i ON stats_import.test(id)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33600 RENAME TO reind_con_toast
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."itest5_a_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_TABLE] CREATE TABLE TIMETZ_TBL (f1 time(2) with time zone)
;
----------
[ADMIN_TABLE] ANALYZE group_tbl
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_27609_2
;
----------
[ALTER_TABLE|ADD_COLUMN] --
-- IF EXISTS test
--
ALTER TABLE IF EXISTS tt8 ADD COLUMN f int
;
----------
[CREATE_POLICY] CREATE RULE "shoelace_ok_ins" AS
    ON INSERT TO "public"."shoelace_ok" DO INSTEAD  UPDATE "public"."shoelace" SET "sl_avail" = ("shoelace"."sl_avail" + "new"."ok_quant")
  WHERE ("shoelace"."sl_name" = "new"."ok_name")
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE split_sequence_next.seq_full RESTART
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_ts_union(internal, internal)
RETURNS gbtreekey16
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30806_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tableam_tblas_heapx AS SELECT * FROM tableam_tbl_heapx
;
----------
[CREATE_TABLE] create table nantest2 ( id int4 , d numeric )
;
----------
[CREATE_TABLE] CREATE TABLE regress_roleoption.t4 (a int)
;
----------
[CREATE_TABLE] create table tt9 (x int, xx int, y int)
;
----------
[ALTER_TABLE] ALTER TABLE reloptions_test SET (autovacuum_enabled, fillfactor=32)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN IF NOT EXISTS c3 integer, -- skipping because c3 already exists
	ADD COLUMN c4 integer
;
----------
[DROP_PROG_OBJ] -- should work
DROP CAST (text AS casttesttype)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_trigger ADD PRIMARY KEY USING INDEX pg_trigger_oid_index
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_missing_target2 AS
SELECT count(*)
FROM test_missing_target x, test_missing_target y
	WHERE x.a = y.a
	GROUP BY x.b ORDER BY x.b
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p1" FOR VALUES IN ('0000', '0001', '0002', '0003', '0004')
;
----------
[CREATE_TYPE] create type pp_rectype as (a int, b int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_auth_members
;
----------
[CREATE_TABLE] CREATE TABLE "public"."bt_name_heap" (
    "seqno" "name",
    "random" integer
)
;
----------
[DROP_TABLE] DROP TABLE temporal_partitioned
;
----------
[CREATE_TABLE] CREATE TABLE pg_catalog.test (a int)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION synonym_tst (
						COPY=english
)
;
----------
[CREATE_TABLE] CREATE TABLE pxtest1 (foobar VARCHAR(10))
;
----------
[COMMENT_TRIGGER] COMMENT ON TRIGGER before_ins_stmt_trig ON main_table IS NULL
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdescription
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30505_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0
;
----------
[TRUNCATE_TABLE] --
-- partitioned FK referenced deletes NO ACTION
--

TRUNCATE temporal_partitioned_mltrng, temporal_partitioned_fk_mltrng2mltrng
;
----------
[CREATE_TABLE] -- not null not valid with partitions
CREATE TABLE atnnparted (id int, col1 int) PARTITION BY LIST (id)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."tbl_include_box"
    ADD CONSTRAINT "tbl_include_box_idx_unique" PRIMARY KEY ("c1", "c2") INCLUDE ("c3", "c4")
;
----------
[ADMIN_TABLE] VACUUM freespace_tab
;
----------
[DROP_SCHEMA] DROP SCHEMA test_func_dep3
;
----------
[DROP_TABLE] DROP TABLE hash_cleanup_heap
;
