create table rtest_view1 (a int4, b text, v bool)
;
-- cannot have default and identity
CREATE TABLE itest_err_3 (a int default 5 generated by default as identity)
;
vacuum (disable_page_skipping) matview_visibility_test
;
CREATE FUNCTION seg_left(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
TRUNCATE truncate_b
;
/*
 * text search configuration for nepali language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * nepali and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
CREATE TABLE "generated_stored_tests"."gtestxx_3" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER FUNCTION alt_func3(int) SET SCHEMA alt_nsp2
;
create schema abc create table test_user (id int primary key,name varchar(50))
;
DROP FUNCTION base_fn_in(cstring)
;
alter function simple1.simpletarget(int) immutable
;
CREATE FUNCTION lca(ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- Specifying name only for a non-Boolean option should fail
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
CREATE INDEX t2_gist ON s2.t2 USING GIST (b)
;
CREATE TABLE test_part (a int PRIMARY KEY, b text, c timestamptz) PARTITION BY LIST (a)
;
CREATE AGGREGATE myaggp10b(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  INITCOND = '{}')
;
ALTER TABLE ONLY "public"."pp1"
    ADD CONSTRAINT "pp1_pkey" PRIMARY KEY ("f1")
;
create temporary view vw_rngfunc as
  select * from rows from(getrngfunc9(1),
                      getrngfunc7(1) AS (rngfuncid int, rngfuncsubid int, rngfuncname text),
                      getrngfunc1(1))
                with ordinality as t1(a,b,c,d,e,f,g,n)
;
CREATE TABLE quuux_default1 PARTITION OF quuux_default (
	CONSTRAINT check_1 CHECK (a IS NOT NULL AND a = 1)
	) FOR VALUES IN ('b')
;
--    N    P        P    N
-- should ERROR: tf1p(int[],anyelement) not matched by tf1p(anyarray,int)
CREATE AGGREGATE myaggn11a(BASETYPE = anyelement, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
DROP TABLE base_tbl CASCADE
;
VACUUM (PARALLEL 0) pg_catalog.pg_foreign_server
;
ALTER TABLE tab7 DROP CONSTRAINT tab7_pkey
;
ALTER OPERATOR FAMILY gist_text_ops USING gist ADD
	OPERATOR	6	<> (text, text) ,
	FUNCTION	9 (text, text) gbt_var_fetch (internal)
;
TRUNCATE ft1
;
ALTER SUBSCRIPTION regress_mysub1 SET (two_phase = true)
;
CREATE TABLE batch_table_p2
	PARTITION OF batch_table
	FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."clstr_tst_inh" INHERIT "public"."clstr_tst"
;
create table part_abc (a int not null, b int not null, c int not null) partition by list (a)
;
CREATE PROCEDURE ptestx() LANGUAGE SQL WINDOW AS $$ INSERT INTO cp_test VALUES (1, 'a') $$
;
alter table inh_parent alter column f1 set not null
;
DROP TABLE tbl1
;
alter type insenum add value 'i3' before 'L2'
;
CREATE TABLE tab_part1 PARTITION OF tab_main FOR VALUES FROM (0) TO (5)
;
ALTER TABLE ONLY "public"."mlparted3" DROP COLUMN "........pg.dropped.7........"
;
create table parted_inval_tab_1_1 partition of parted_inval_tab_1
  for values from (0) to (10)
;
alter index idxpart_a_b_idx attach partition idxpart1_tst3
;
CREATE INDEX idx_timetz ON test_timetz USING gin (i)
;
CREATE TABLE tab5_1 PARTITION OF tab5 DEFAULT
;
CREATE OR REPLACE PROCEDURE overload(i int) AS $$
DECLARE
  r int;
BEGIN
  SELECT (i + i)::int INTO r;
END; $$ LANGUAGE plpgsql
;
ALTER TABLE fk ADD FOREIGN KEY (a) REFERENCES pk ON UPDATE CASCADE ON DELETE CASCADE
;
COMMENT ON LARGE OBJECT 34022 IS 'I Wandered Lonely as a Cloud'
;
ALTER SEQUENCE "public"."sj_t2_id_seq" OWNED BY "public"."sj_t2"."id"
;
create function mr_inoutparam_fail(inout i anyelement, out r anymultirange)
  as $$ select $1, '[1,10]' $$ language sql
;
ALTER TABLE ONLY "public"."clstr_tst_s" ALTER COLUMN "rf_a" SET DEFAULT "nextval"('"public"."clstr_tst_s_rf_a_seq"'::"regclass")
;
create index on idxpart (col_keep)
;
CREATE INDEX gcircleind ON circle_tbl USING gist (f1)
;
ALTER TABLE ONLY "public"."persons3"
    ADD CONSTRAINT "persons3_pkey" PRIMARY KEY ("id")
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table rtest_view1 (a int4, b text, v bool)
;
----------
[CREATE_TABLE] -- cannot have default and identity
CREATE TABLE itest_err_3 (a int default 5 generated by default as identity)
;
----------
[ADMIN_TABLE] vacuum (disable_page_skipping) matview_visibility_test
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_left(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[TRUNCATE_TABLE] TRUNCATE truncate_b
;
----------
[CREATE_POLICY] /*
 * text search configuration for nepali language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * nepali and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtestxx_3" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION alt_func3(int) SET SCHEMA alt_nsp2
;
----------
[CREATE_SCHEMA] create schema abc create table test_user (id int primary key,name varchar(50))
;
----------
[DROP_PROG_OBJ] DROP FUNCTION base_fn_in(cstring)
;
----------
[ALTER_PROG_OBJ] alter function simple1.simpletarget(int) immutable
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lca(ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Specifying name only for a non-Boolean option should fail
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
----------
[ADD_INDEX] CREATE INDEX t2_gist ON s2.t2 USING GIST (b)
;
----------
[CREATE_TABLE] CREATE TABLE test_part (a int PRIMARY KEY, b text, c timestamptz) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp10b(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  INITCOND = '{}')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."pp1"
    ADD CONSTRAINT "pp1_pkey" PRIMARY KEY ("f1")
;
----------
[CREATE_VIEW(SELECT)] create temporary view vw_rngfunc as
  select * from rows from(getrngfunc9(1),
                      getrngfunc7(1) AS (rngfuncid int, rngfuncsubid int, rngfuncname text),
                      getrngfunc1(1))
                with ordinality as t1(a,b,c,d,e,f,g,n)
;
----------
[CREATE_TABLE] CREATE TABLE quuux_default1 PARTITION OF quuux_default (
	CONSTRAINT check_1 CHECK (a IS NOT NULL AND a = 1)
	) FOR VALUES IN ('b')
;
----------
[CREATE_PROG_OBJ] --    N    P        P    N
-- should ERROR: tf1p(int[],anyelement) not matched by tf1p(anyarray,int)
CREATE AGGREGATE myaggn11a(BASETYPE = anyelement, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[DROP_TABLE] DROP TABLE base_tbl CASCADE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_foreign_server
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE tab7 DROP CONSTRAINT tab7_pkey
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_text_ops USING gist ADD
	OPERATOR	6	<> (text, text) ,
	FUNCTION	9 (text, text) gbt_var_fetch (internal)
;
----------
[TRUNCATE_TABLE] TRUNCATE ft1
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_mysub1 SET (two_phase = true)
;
----------
[CREATE_TABLE] CREATE TABLE batch_table_p2
	PARTITION OF batch_table
	FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."clstr_tst_inh" INHERIT "public"."clstr_tst"
;
----------
[CREATE_TABLE] create table part_abc (a int not null, b int not null, c int not null) partition by list (a)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptestx() LANGUAGE SQL WINDOW AS $$ INSERT INTO cp_test VALUES (1, 'a') $$
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table inh_parent alter column f1 set not null
;
----------
[DROP_TABLE] DROP TABLE tbl1
;
----------
[ALTER_TYPE] alter type insenum add value 'i3' before 'L2'
;
----------
[CREATE_TABLE] CREATE TABLE tab_part1 PARTITION OF tab_main FOR VALUES FROM (0) TO (5)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted3" DROP COLUMN "........pg.dropped.7........"
;
----------
[CREATE_TABLE] create table parted_inval_tab_1_1 partition of parted_inval_tab_1
  for values from (0) to (10)
;
----------
[ALTER_INDEX] alter index idxpart_a_b_idx attach partition idxpart1_tst3
;
----------
[ADD_INDEX] CREATE INDEX idx_timetz ON test_timetz USING gin (i)
;
----------
[CREATE_TABLE] CREATE TABLE tab5_1 PARTITION OF tab5 DEFAULT
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE PROCEDURE overload(i int) AS $$
DECLARE
  r int;
BEGIN
  SELECT (i + i)::int INTO r;
END; $$ LANGUAGE plpgsql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk ADD FOREIGN KEY (a) REFERENCES pk ON UPDATE CASCADE ON DELETE CASCADE
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 34022 IS 'I Wandered Lonely as a Cloud'
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."sj_t2_id_seq" OWNED BY "public"."sj_t2"."id"
;
----------
[CREATE_PROG_OBJ] create function mr_inoutparam_fail(inout i anyelement, out r anymultirange)
  as $$ select $1, '[1,10]' $$ language sql
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."clstr_tst_s" ALTER COLUMN "rf_a" SET DEFAULT "nextval"('"public"."clstr_tst_s_rf_a_seq"'::"regclass")
;
----------
[ADD_INDEX] create index on idxpart (col_keep)
;
----------
[ADD_INDEX] CREATE INDEX gcircleind ON circle_tbl USING gist (f1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."persons3"
    ADD CONSTRAINT "persons3_pkey" PRIMARY KEY ("id")
;
