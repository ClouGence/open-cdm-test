INSERT INTO oer_test VALUES (6)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16822', relminmxid = '3'
WHERE oid = '"public"."pht1_p1"'::pg_catalog.regclass
;
INSERT INTO pub_testpart2.child_parent1 values(1)
;
INSERT INTO public.ths_p3 VALUES (0, 730)
;
-- compressed external toast data
INSERT INTO toasttable(toasted_col2) SELECT repeat(string_agg(to_char(g.i, 'FM0000'), ''), 50) FROM generate_series(1, 500) g(i)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14410', relminmxid = '3'
WHERE oid = '"public"."xmltest2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest28b"'::pg_catalog.regclass
;
update PSlot set slotlink = blname where slotname = myname
;
insert into dropcol(key, keep1, keep2) values(1, '4', 4) on conflict(key)
    do update set keep1 = excluded.keep1, keep2 = excluded.keep2
    where excluded.keep1 is not null and excluded.keep2 is not null
          and dropcol.keep1 is not null and dropcol.keep2 is not null
    returning *
;
INSERT INTO num_exp_mul VALUES (7,2,'2851072985828710.485883795')
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"generated_stored_tests"."gtest_child3"'::pg_catalog.regclass AND
conkey IN ('{1}')
;
-- Generate a table with a mix of NULLs and non-NULL values (and data suitable
-- for the different opclasses we build later).
INSERT INTO brin_parallel_test
SELECT (CASE WHEN (mod(i,231) = 0) OR (i BETWEEN 3500 AND 4000) THEN NULL ELSE i END),
       (CASE WHEN (mod(i,233) = 0) OR (i BETWEEN 3750 AND 4250) THEN NULL ELSE encode(sha256(i::text::bytea), 'hex') END),
       (CASE WHEN (mod(i,233) = 0) OR (i BETWEEN 3850 AND 4500) THEN NULL ELSE (i/100) + mod(i,8) END)
  FROM generate_series(1,5000) S(i)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."itest10"'::pg_catalog.regclass
;
INSERT INTO transition_table_level1(level1_no)
  SELECT generate_series(201,1000)
;
INSERT INTO SYS_COL_CHECK_TBL VALUES ('Seattle', 'Washington', false, 100)
;
INSERT INTO pagg_tab1 SELECT i % 30, i % 20 FROM generate_series(0, 299, 2) i
;
INSERT INTO trunc_a VALUES (0), (5), (10), (15), (20), (25)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14659', relminmxid = '3'
WHERE oid = '34482'
;
INSERT INTO sql_packages VALUES ('PKG001', 'Enhanced datetime facilities', 'YES', NULL, '')
;
-- ok, partition key update, no constraint violation
UPDATE range_parted set d = d - 10 WHERE d > 10
;
INSERT INTO public.tplain VALUES ('9', 869)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15859', relminmxid = '3'
WHERE oid = '39594'
;
INSERT INTO test_tab VALUES (6, md5(6::text), -6, 6*2, -6*3)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15395', relminmxid = '3'
WHERE oid = '38922'
;
INSERT INTO insert_tbl (SELECT * FROM local_tbl UNION ALL SELECT * FROM remote_tbl)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1038', relminmxid = '1'
WHERE oid = '"public"."date_tbl"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (6, 246)
;
INSERT INTO public.ths_p3 VALUES (5, 855)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13727', relminmxid = '3'
WHERE oid = '"public"."test_type_diff2_c3"'::pg_catalog.regclass
;
INSERT INTO c1 SELECT 0, a / 3, a % 3 FROM generate_series(0, 9) a
;
INSERT INTO public.tht VALUES ('1', 321)
;
insert into x values (4,44)
;
INSERT INTO public.ths_p2 VALUES (7, 967)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '"public"."pht1_e_p3"'::pg_catalog.regclass
;
INSERT INTO temporal_mltrng (id, valid_at) VALUES ('[2,3)', datemultirange(daterange('2005-01-01', '2006-01-01'))) ON CONFLICT DO NOTHING
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9568', relminmxid = '1'
WHERE oid = '"public"."ec0"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10693', relminmxid = '3'
WHERE oid = '31172'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p1"'::pg_catalog.regclass
;
insert into domcontest values (5)
;
-- NaN radius

-- bad values

INSERT INTO CIRCLE_TBL VALUES ('<(-100,0),-100>')
;
-- UPDATE on a partition subtree has been seen to have problems.
insert into ab values (1,2)
;
INSERT INTO BOX_TBL (f1) VALUES ('((-8, 2), (-2, -10))')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17728', relminmxid = '3'
WHERE oid = '"public"."idxpart_another_1"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."pk3"'::pg_catalog.regclass
  AND attname IN ('a')
;
UPDATE fkpart11.pk SET a = 1 WHERE a = 2
;
insert into part4 values ('a', 10)
;
INSERT INTO trans_abc VALUES (7)
;
UPDATE pg_catalog.pg_amproc SET
		   amproc = 'bad_cmp1'::regproc
	WHERE amproc = 'ok_cmp1'::regproc
;
insert into op values (array[1,2])
;
INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_rewrite
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO oer_test VALUES (6)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16822', relminmxid = '3'
WHERE oid = '"public"."pht1_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO pub_testpart2.child_parent1 values(1)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 730)
;
----------
[INSERT|SELECT] -- compressed external toast data
INSERT INTO toasttable(toasted_col2) SELECT repeat(string_agg(to_char(g.i, 'FM0000'), ''), 50) FROM generate_series(1, 500) g(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14410', relminmxid = '3'
WHERE oid = '"public"."xmltest2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest28b"'::pg_catalog.regclass
;
----------
[UPDATE] update PSlot set slotlink = blname where slotname = myname
;
----------
[MERGE|SELECT] insert into dropcol(key, keep1, keep2) values(1, '4', 4) on conflict(key)
    do update set keep1 = excluded.keep1, keep2 = excluded.keep2
    where excluded.keep1 is not null and excluded.keep2 is not null
          and dropcol.keep1 is not null and dropcol.keep2 is not null
    returning *
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (7,2,'2851072985828710.485883795')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"generated_stored_tests"."gtest_child3"'::pg_catalog.regclass AND
conkey IN ('{1}')
;
----------
[INSERT|SELECT] -- Generate a table with a mix of NULLs and non-NULL values (and data suitable
-- for the different opclasses we build later).
INSERT INTO brin_parallel_test
SELECT (CASE WHEN (mod(i,231) = 0) OR (i BETWEEN 3500 AND 4000) THEN NULL ELSE i END),
       (CASE WHEN (mod(i,233) = 0) OR (i BETWEEN 3750 AND 4250) THEN NULL ELSE encode(sha256(i::text::bytea), 'hex') END),
       (CASE WHEN (mod(i,233) = 0) OR (i BETWEEN 3850 AND 4500) THEN NULL ELSE (i/100) + mod(i,8) END)
  FROM generate_series(1,5000) S(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."itest10"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO transition_table_level1(level1_no)
  SELECT generate_series(201,1000)
;
----------
[INSERT] INSERT INTO SYS_COL_CHECK_TBL VALUES ('Seattle', 'Washington', false, 100)
;
----------
[INSERT|SELECT] INSERT INTO pagg_tab1 SELECT i % 30, i % 20 FROM generate_series(0, 299, 2) i
;
----------
[INSERT] INSERT INTO trunc_a VALUES (0), (5), (10), (15), (20), (25)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14659', relminmxid = '3'
WHERE oid = '34482'
;
----------
[INSERT] INSERT INTO sql_packages VALUES ('PKG001', 'Enhanced datetime facilities', 'YES', NULL, '')
;
----------
[UPDATE] -- ok, partition key update, no constraint violation
UPDATE range_parted set d = d - 10 WHERE d > 10
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 869)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '15859', relminmxid = '3'
WHERE oid = '39594'
;
----------
[INSERT] INSERT INTO test_tab VALUES (6, md5(6::text), -6, 6*2, -6*3)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '15395', relminmxid = '3'
WHERE oid = '38922'
;
----------
[INSERT|SELECT] INSERT INTO insert_tbl (SELECT * FROM local_tbl UNION ALL SELECT * FROM remote_tbl)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1038', relminmxid = '1'
WHERE oid = '"public"."date_tbl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 246)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 855)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13727', relminmxid = '3'
WHERE oid = '"public"."test_type_diff2_c3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO c1 SELECT 0, a / 3, a % 3 FROM generate_series(0, 9) a
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 321)
;
----------
[INSERT] insert into x values (4,44)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 967)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '"public"."pht1_e_p3"'::pg_catalog.regclass
;
----------
[MERGE|SELECT] INSERT INTO temporal_mltrng (id, valid_at) VALUES ('[2,3)', datemultirange(daterange('2005-01-01', '2006-01-01'))) ON CONFLICT DO NOTHING
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9568', relminmxid = '1'
WHERE oid = '"public"."ec0"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10693', relminmxid = '3'
WHERE oid = '31172'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p1"'::pg_catalog.regclass
;
----------
[INSERT] insert into domcontest values (5)
;
----------
[INSERT] -- NaN radius

-- bad values

INSERT INTO CIRCLE_TBL VALUES ('<(-100,0),-100>')
;
----------
[INSERT] -- UPDATE on a partition subtree has been seen to have problems.
insert into ab values (1,2)
;
----------
[INSERT|SELECT] INSERT INTO BOX_TBL (f1) VALUES ('((-8, 2), (-2, -10))')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17728', relminmxid = '3'
WHERE oid = '"public"."idxpart_another_1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."pk3"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[UPDATE] UPDATE fkpart11.pk SET a = 1 WHERE a = 2
;
----------
[INSERT] insert into part4 values ('a', 10)
;
----------
[INSERT] INSERT INTO trans_abc VALUES (7)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_amproc SET
		   amproc = 'bad_cmp1'::regproc
	WHERE amproc = 'ok_cmp1'::regproc
;
----------
[INSERT] insert into op values (array[1,2])
;
----------
[INSERT|SELECT] INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_rewrite
;
