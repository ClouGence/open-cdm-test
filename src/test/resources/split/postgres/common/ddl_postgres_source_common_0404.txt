CREATE INDEX k ON testschema.tablespace_acl USING btree (c) TABLESPACE regress_tblspace
;
CREATE TABLE pgss_b (id integer PRIMARY KEY, a_id integer REFERENCES pgss_a)
;
ALTER TABLE IF EXISTS constraint_not_exist RENAME CONSTRAINT con3 TO con3foo
;
create trigger c after insert on middle
for each row execute procedure f()
;
ALTER DATABASE split_db_probe RESET ALL
;
CREATE MATERIALIZED VIEW citext_matview AS
  SELECT * FROM citext_table
;
DROP FUNCTION pg_stat_statements_reset()
;
create table minmaxtest(f1 int)
;
TRUNCATE TABLE d3_listp
;
ANALYZE student
;
CALL test_proc10(_a, _b)
;
CREATE TABLE tab_rep (a int primary key)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=22270 host=/tmp/Md8b06rvrF dbname=postgres' PUBLICATION tap_pub
;
ALTER TABLE part_b_10_b_20 ATTACH PARTITION part_c_1_100 FOR VALUES FROM (1) TO (100)
;
CREATE TABLE attmp (
	initial 	int4
)
;
CREATE PUBLICATION split_pub18_cols WITH (publish = 'insert', publish_generated_columns = stored)
;
DROP TABLE temp1
;
-- Check that we have the correct tuples estimate for an appendrel
create table tuplesest_parted (a int, b int, c float) partition by range(a)
;
CREATE OR REPLACE FUNCTION blurt_and_lock_123(text) RETURNS text IMMUTABLE LANGUAGE plpgsql AS $$
     BEGIN
        RAISE NOTICE 'blurt_and_lock_123() called for % in session %', $1, current_setting('spec.session')::int;

	-- depending on lock state, wait for lock 2 or 3
        IF pg_try_advisory_xact_lock(current_setting('spec.session')::int, 1) THEN
            RAISE NOTICE 'acquiring advisory lock on 2';
            PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 2);
        ELSE
            RAISE NOTICE 'acquiring advisory lock on 3';
            PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 3);
        END IF;
    RETURN $1;
    END;$$
;
CREATE TABLE ltable
	PARTITION OF parent
	FOR VALUES IN ('BBB')
;
alter table idxpart2 drop column col1, drop column col2
;
-- some additional tests not using base tables
CREATE VIEW mvtest_vt1 AS SELECT 1 moo
;
CREATE TRIGGER t AFTER UPDATE ON a EXECUTE PROCEDURE f()
;
alter table abc drop constraint if exists abc_fk
;
ALTER OPERATOR ^<@ (_ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
create trigger tg_phone_bu before update
    on PHone for each row execute procedure tg_phone_bu()
;
CREATE FUNCTION ean13_out(ean13)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- Ensure incremental sorts work for amcanorderbyop type indexes
create table point_table (a point, b int)
;
VACUUM (FREEZE) pg_catalog.pg_index
;
create table fktable2(f1 int references pktable2 deferrable initially deferred)
;
-- Fail while setting improper values
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=-30.1)
;
DROP TYPE test_typex
;
create table matest0(a int primary key)
;
-- "CLUSTER <tablename>" on a table that hasn't been clustered
CLUSTER clstr_2
;
CREATE TABLE default_expr_column (id int DEFAULT (id))
;
-- test inheritance
create table parent (a int)
;
COMMENT ON TEXT SEARCH CONFIGURATION serbian IS 'configuration for serbian language'
;
create function tftest(int) returns table(a int, b int) as $$
begin
  return query select $1, $1+i from generate_series(1,5) g(i);
end;
$$ language plpgsql immutable strict
;
--
-- test triggers with WHEN clause
--

CREATE TRIGGER modified_a BEFORE UPDATE OF a ON main_table
FOR EACH ROW WHEN (OLD.a <> NEW.a) EXECUTE PROCEDURE trigger_func('modified_a')
;
ALTER TABLE FKTABLE ADD CONSTRAINT fkdd2 FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION NOT DEFERRABLE
;
CREATE TYPE tt_t0 AS (z inet, x int, y numeric(8,2))
;
ALTER TABLE prt1 DETACH PARTITION prt1_p3
;
create index idxpart1_2_a_b on idxpart1 (a, b)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
CREATE FUNCTION icount(_int4)
RETURNS int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP SCHEMA test_roles_schema
;
-- We need 3 sub-partitions. 1 to validate pruning worked and another two
-- because a single remaining partition would be pulled up to the main Append.
create table rangep_0_to_100_1 partition of rangep_0_to_100 for values in(1)
;
CREATE TEMPORARY TABLE box_temp (f1 box)
;
ALTER TABLE tststats.priv_test_tbl INHERIT tststats.priv_test_parent_tbl
;
do $$ declare a int[] := array[1,2,3];
begin
  -- test scenarios for optimization of updates of R/W expanded objects
  a := array_append(a, 42);  -- optimizable using "transfer" method
  a := a || a[3];  -- optimizable using "inplace" method
  a := a[1] || a;  -- ditto, but let's test array_prepend
  a := a || a;     -- not optimizable
  raise notice 'a = %', a;
end$$
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX k ON testschema.tablespace_acl USING btree (c) TABLESPACE regress_tblspace
;
----------
[CREATE_TABLE] CREATE TABLE pgss_b (id integer PRIMARY KEY, a_id integer REFERENCES pgss_a)
;
----------
[RENAME_CONSTRAINT] ALTER TABLE IF EXISTS constraint_not_exist RENAME CONSTRAINT con3 TO con3foo
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger c after insert on middle
for each row execute procedure f()
;
----------
[ALTER_CATALOG] ALTER DATABASE split_db_probe RESET ALL
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW citext_matview AS
  SELECT * FROM citext_table
;
----------
[DROP_PROG_OBJ] DROP FUNCTION pg_stat_statements_reset()
;
----------
[CREATE_TABLE] create table minmaxtest(f1 int)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE d3_listp
;
----------
[ADMIN_TABLE] ANALYZE student
;
----------
[CALL_PROG_OBJ] CALL test_proc10(_a, _b)
;
----------
[CREATE_TABLE] CREATE TABLE tab_rep (a int primary key)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=22270 host=/tmp/Md8b06rvrF dbname=postgres' PUBLICATION tap_pub
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part_b_10_b_20 ATTACH PARTITION part_c_1_100 FOR VALUES FROM (1) TO (100)
;
----------
[CREATE_TABLE] CREATE TABLE attmp (
	initial 	int4
)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION split_pub18_cols WITH (publish = 'insert', publish_generated_columns = stored)
;
----------
[DROP_TABLE] DROP TABLE temp1
;
----------
[CREATE_TABLE] -- Check that we have the correct tuples estimate for an appendrel
create table tuplesest_parted (a int, b int, c float) partition by range(a)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION blurt_and_lock_123(text) RETURNS text IMMUTABLE LANGUAGE plpgsql AS $$
     BEGIN
        RAISE NOTICE 'blurt_and_lock_123() called for % in session %', $1, current_setting('spec.session')::int;

	-- depending on lock state, wait for lock 2 or 3
        IF pg_try_advisory_xact_lock(current_setting('spec.session')::int, 1) THEN
            RAISE NOTICE 'acquiring advisory lock on 2';
            PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 2);
        ELSE
            RAISE NOTICE 'acquiring advisory lock on 3';
            PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 3);
        END IF;
    RETURN $1;
    END;$$
;
----------
[CREATE_TABLE] CREATE TABLE ltable
	PARTITION OF parent
	FOR VALUES IN ('BBB')
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart2 drop column col1, drop column col2
;
----------
[CREATE_VIEW(SELECT)] -- some additional tests not using base tables
CREATE VIEW mvtest_vt1 AS SELECT 1 moo
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER t AFTER UPDATE ON a EXECUTE PROCEDURE f()
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table abc drop constraint if exists abc_fk
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^<@ (_ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_phone_bu before update
    on PHone for each row execute procedure tg_phone_bu()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ean13_out(ean13)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Ensure incremental sorts work for amcanorderbyop type indexes
create table point_table (a point, b int)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_index
;
----------
[CREATE_TABLE] create table fktable2(f1 int references pktable2 deferrable initially deferred)
;
----------
[CREATE_TABLE] -- Fail while setting improper values
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=-30.1)
;
----------
[DROP_TYPE] DROP TYPE test_typex
;
----------
[CREATE_TABLE] create table matest0(a int primary key)
;
----------
[ADMIN_TABLE] -- "CLUSTER <tablename>" on a table that hasn't been clustered
CLUSTER clstr_2
;
----------
[CREATE_TABLE] CREATE TABLE default_expr_column (id int DEFAULT (id))
;
----------
[CREATE_TABLE] -- test inheritance
create table parent (a int)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION serbian IS 'configuration for serbian language'
;
----------
[CREATE_PROG_OBJ] create function tftest(int) returns table(a int, b int) as $$
begin
  return query select $1, $1+i from generate_series(1,5) g(i);
end;
$$ language plpgsql immutable strict
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- test triggers with WHEN clause
--

CREATE TRIGGER modified_a BEFORE UPDATE OF a ON main_table
FOR EACH ROW WHEN (OLD.a <> NEW.a) EXECUTE PROCEDURE trigger_func('modified_a')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD CONSTRAINT fkdd2 FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION NOT DEFERRABLE
;
----------
[CREATE_TYPE] CREATE TYPE tt_t0 AS (z inet, x int, y numeric(8,2))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt1 DETACH PARTITION prt1_p3
;
----------
[ADD_INDEX] create index idxpart1_2_a_b on idxpart1 (a, b)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION icount(_int4)
RETURNS int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_SCHEMA] DROP SCHEMA test_roles_schema
;
----------
[CREATE_TABLE] -- We need 3 sub-partitions. 1 to validate pruning worked and another two
-- because a single remaining partition would be pulled up to the main Append.
create table rangep_0_to_100_1 partition of rangep_0_to_100 for values in(1)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE box_temp (f1 box)
;
----------
[ALTER_TABLE] ALTER TABLE tststats.priv_test_tbl INHERIT tststats.priv_test_parent_tbl
;
----------
[BLOCK] do $$ declare a int[] := array[1,2,3];
begin
  -- test scenarios for optimization of updates of R/W expanded objects
  a := array_append(a, 42);  -- optimizable using "transfer" method
  a := a || a[3];  -- optimizable using "inplace" method
  a := a[1] || a;  -- ditto, but let's test array_prepend
  a := a || a;     -- not optimizable
  raise notice 'a = %', a;
end$$
;
