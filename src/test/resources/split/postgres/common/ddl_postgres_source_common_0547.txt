CREATE FUNCTION "public"."mr_outparam_succeed3"("i" "anymultirange", OUT "r" "anyrange", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select range_merge($1), 'foo'::text $_$
;
ALTER TABLE hash_parted ATTACH PARTITION hpart_5 FOR VALUES WITH (MODULUS 4, REMAINDER 2)
;
ALTER TABLE ONLY "public"."prt1_e" ATTACH PARTITION "public"."prt1_e_p2" FOR VALUES FROM (250) TO (500)
;
-- someday this should work
alter table atref drop constraint atref_b_check
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_foreign_server
;
CREATE INDEX "iprt1_p2_a" ON "public"."prt1_p2" USING "btree" ("a")
;
VACUUM information_schema.sql_packages
;
-- should fail
alter table fkpart2.fk_part detach partition fkpart2.fk_part_1
;
CREATE FUNCTION gbt_inet_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE DOMAIN dump_test.us_postal_code AS TEXT
		               COLLATE "C"
					   DEFAULT '10014'
					   CHECK(VALUE ~ '^\d{5}$' OR
							 VALUE ~ '^\d{5}-\d{4}$')
;
DROP TABLE pxtest_rowlock
;
CREATE FUNCTION "public"."logging_msfunc_strict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$ SELECT $1 || '+' || quote_nullable($2) $_$
;
create type two_int4s as (f1 int4, f2 int4)
;
CREATE FUNCTION test_assign_nnint_container2(x int, y int, z int)
  RETURNS nnint_container AS $$
declare v nnint_container;
begin
v.f2 := z;
return v;
end
$$ LANGUAGE plpgsql
;
-- Should fail. Ensure that DROP requests for missing OPERATOR / FUNCTIONS
-- return appropriate message in ALTER OPERATOR FAMILY ... DROP OPERATOR / FUNCTION
CREATE OPERATOR FAMILY alt_opf18 USING btree
;
ALTER TABLE temporal3 RENAME COLUMN valid_at TO valid_thru
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
create temp table gstest_empty (a integer, b integer, v integer)
;
--
-- Test behavior of both volatile and stable functions inside a cursor;
-- in particular we want to see what happens during commit of a holdable
-- cursor
--

create temp table tt1(f1 int)
;
--
-- Now the operators:
--

--
-- EAN13 operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
do $$
declare tg array_test_table%rowtype[];
begin
  tg := array(select array_test_table from array_test_table);
  raise notice '%', tg;
  tg := array(select row(a,b) from array_test_table);
  raise notice '%', tg;
end;
$$
;
do $$
declare
  v int;
  v1 v%type;
  v2 v%type[];
  v3 v%type[1];
  v4 v%type[][];
  v5 v%type[1][3];
  v6 v%type array;
  v7 v%type array[];
  v8 v%type array[1];
  v9 v%type array[1][1];
  v10 pg_catalog.pg_class%rowtype[];
begin
  raise notice '%', pg_typeof(v1);
  raise notice '%', pg_typeof(v2);
  raise notice '%', pg_typeof(v3);
  raise notice '%', pg_typeof(v4);
  raise notice '%', pg_typeof(v5);
  raise notice '%', pg_typeof(v6);
  raise notice '%', pg_typeof(v7);
  raise notice '%', pg_typeof(v8);
  raise notice '%', pg_typeof(v9);
  raise notice '%', pg_typeof(v10);
end;
$$
;
ALTER TABLE pg_temp_11.pg_temp_26230_2 ADD COLUMN newdata pg_temp_11.pg_temp_26230
;
ALTER TABLE part DETACH PARTITION part2
;
drop table inh_parent_1 cascade
;
ALTER TABLE tbl_include_unique2 add UNIQUE (c1, c2) INCLUDE (c3, c4)
;
DROP TABLE bla
;
-- OK
CREATE TABLE selinto_schema.tmp3 (a,b,c)
	   AS SELECT oid,relname,relacl FROM pg_class
	   WHERE relname like '%c%'
;
CREATE OR REPLACE FUNCTION "S 1".F_BRTRIG() RETURNS trigger AS $$
BEGIN
    NEW.c3 = NEW.c3 || '_trig_update';
    RETURN NEW;
END;
$$ LANGUAGE plpgsql
;
ALTER TABLE ONLY parted_no_parts ADD CONSTRAINT check_a CHECK (a > 0)
;
--
-- TRIGGERS
--

create table pkeys (pkey1 int4 not null, pkey2 text not null)
;
CREATE TABLE p1 PARTITION OF pt FOR VALUES WITH (modulus 4, remainder 0)
;
ALTER TABLESPACE regress_tblspace RESET (random_page_cost = 2.0)
;
ALTER DATABASE regression_tbd RENAME TO regression_utf8
;
CREATE FOREIGN TABLE foreign_tbl (a int, b int)
  SERVER loopback OPTIONS (table_name 'base_tbl')
;
alter type insenum add value 'i19' before 'L2'
;
ALTER TABLE s.x ADD CONSTRAINT e2rows EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll WITH s.=)
  USING INDEX TABLESPACE regress_create_idx_tblspace
  WHERE (s.index_row_if(y))
;
ALTER TABLE "S 1"."T 4" SET (autovacuum_enabled = 'false')
;
DROP TRIGGER test_trigger_exists ON test_exists
;
create or replace function compos() returns compostype as $$
begin
  return (1, 'hello');
end;
$$ language plpgsql
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = true)
;
ALTER TABLE ONLY test_replica_identity4_1
  ADD CONSTRAINT test_replica_identity4_1_pkey PRIMARY KEY (id)
;
-- simple shadowing of input and output parameters
create or replace function shadowtest(in1 int)
	returns table (out1 int) as $$
declare
in1 int;
out1 int;
begin
end
$$ language plpgsql
;
truncate rem1
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26765_2 (tid pg_catalog.tid)
;
drop materialized view parallel_mat_view
;
CREATE FUNCTION pg_visibility_map(regclass, blkno bigint,
								  all_visible OUT boolean,
								  all_frozen OUT boolean)
RETURNS record
AS '$libdir/pg_visibility', 'pg_visibility_map'
LANGUAGE C STRICT
;
create function extractq2_2(t int8_tbl) returns table(ret1 int8) as $$
  select extractq2(t) offset 0
$$ language sql immutable
;
CREATE TEXT SEARCH CONFIGURATION evttrig_tscfg (COPY = pg_catalog.simple)
;
CREATE TABLE test_u AS SELECT a, b FROM collate_test1 UNION ALL SELECT a, b FROM collate_test2
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."mr_outparam_succeed3"("i" "anymultirange", OUT "r" "anyrange", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select range_merge($1), 'foo'::text $_$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE hash_parted ATTACH PARTITION hpart_5 FOR VALUES WITH (MODULUS 4, REMAINDER 2)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_e" ATTACH PARTITION "public"."prt1_e_p2" FOR VALUES FROM (250) TO (500)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- someday this should work
alter table atref drop constraint atref_b_check
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_foreign_server
;
----------
[ADD_INDEX] CREATE INDEX "iprt1_p2_a" ON "public"."prt1_p2" USING "btree" ("a")
;
----------
[ADMIN_TABLE] VACUUM information_schema.sql_packages
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- should fail
alter table fkpart2.fk_part detach partition fkpart2.fk_part_1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_inet_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_TYPE] CREATE DOMAIN dump_test.us_postal_code AS TEXT
		               COLLATE "C"
					   DEFAULT '10014'
					   CHECK(VALUE ~ '^\d{5}$' OR
							 VALUE ~ '^\d{5}-\d{4}$')
;
----------
[DROP_TABLE] DROP TABLE pxtest_rowlock
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."logging_msfunc_strict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$ SELECT $1 || '+' || quote_nullable($2) $_$
;
----------
[CREATE_TYPE] create type two_int4s as (f1 int4, f2 int4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_assign_nnint_container2(x int, y int, z int)
  RETURNS nnint_container AS $$
declare v nnint_container;
begin
v.f2 := z;
return v;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] -- Should fail. Ensure that DROP requests for missing OPERATOR / FUNCTIONS
-- return appropriate message in ALTER OPERATOR FAMILY ... DROP OPERATOR / FUNCTION
CREATE OPERATOR FAMILY alt_opf18 USING btree
;
----------
[RENAME_COLUMN] ALTER TABLE temporal3 RENAME COLUMN valid_at TO valid_thru
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_TABLE] create temp table gstest_empty (a integer, b integer, v integer)
;
----------
[CREATE_TABLE] --
-- Test behavior of both volatile and stable functions inside a cursor;
-- in particular we want to see what happens during commit of a holdable
-- cursor
--

create temp table tt1(f1 int)
;
----------
[CREATE_PROG_OBJ] --
-- Now the operators:
--

--
-- EAN13 operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[BLOCK] do $$
declare tg array_test_table%rowtype[];
begin
  tg := array(select array_test_table from array_test_table);
  raise notice '%', tg;
  tg := array(select row(a,b) from array_test_table);
  raise notice '%', tg;
end;
$$
;
----------
[BLOCK] do $$
declare
  v int;
  v1 v%type;
  v2 v%type[];
  v3 v%type[1];
  v4 v%type[][];
  v5 v%type[1][3];
  v6 v%type array;
  v7 v%type array[];
  v8 v%type array[1];
  v9 v%type array[1][1];
  v10 pg_catalog.pg_class%rowtype[];
begin
  raise notice '%', pg_typeof(v1);
  raise notice '%', pg_typeof(v2);
  raise notice '%', pg_typeof(v3);
  raise notice '%', pg_typeof(v4);
  raise notice '%', pg_typeof(v5);
  raise notice '%', pg_typeof(v6);
  raise notice '%', pg_typeof(v7);
  raise notice '%', pg_typeof(v8);
  raise notice '%', pg_typeof(v9);
  raise notice '%', pg_typeof(v10);
end;
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26230_2 ADD COLUMN newdata pg_temp_11.pg_temp_26230
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part DETACH PARTITION part2
;
----------
[DROP_TABLE] drop table inh_parent_1 cascade
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tbl_include_unique2 add UNIQUE (c1, c2) INCLUDE (c3, c4)
;
----------
[DROP_TABLE] DROP TABLE bla
;
----------
[CREATE_TABLE|SELECT] -- OK
CREATE TABLE selinto_schema.tmp3 (a,b,c)
	   AS SELECT oid,relname,relacl FROM pg_class
	   WHERE relname like '%c%'
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION "S 1".F_BRTRIG() RETURNS trigger AS $$
BEGIN
    NEW.c3 = NEW.c3 || '_trig_update';
    RETURN NEW;
END;
$$ LANGUAGE plpgsql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY parted_no_parts ADD CONSTRAINT check_a CHECK (a > 0)
;
----------
[CREATE_TABLE] --
-- TRIGGERS
--

create table pkeys (pkey1 int4 not null, pkey2 text not null)
;
----------
[CREATE_TABLE] CREATE TABLE p1 PARTITION OF pt FOR VALUES WITH (modulus 4, remainder 0)
;
----------
[ALTER_TABLESPACE] ALTER TABLESPACE regress_tblspace RESET (random_page_cost = 2.0)
;
----------
[RENAME_CATALOG] ALTER DATABASE regression_tbd RENAME TO regression_utf8
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE foreign_tbl (a int, b int)
  SERVER loopback OPTIONS (table_name 'base_tbl')
;
----------
[ALTER_TYPE] alter type insenum add value 'i19' before 'L2'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE s.x ADD CONSTRAINT e2rows EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll WITH s.=)
  USING INDEX TABLESPACE regress_create_idx_tblspace
  WHERE (s.index_row_if(y))
;
----------
[ALTER_TABLE] ALTER TABLE "S 1"."T 4" SET (autovacuum_enabled = 'false')
;
----------
[DROP_TRIGGER] DROP TRIGGER test_trigger_exists ON test_exists
;
----------
[CREATE_PROG_OBJ] create or replace function compos() returns compostype as $$
begin
  return (1, 'hello');
end;
$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = true)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY test_replica_identity4_1
  ADD CONSTRAINT test_replica_identity4_1_pkey PRIMARY KEY (id)
;
----------
[CREATE_PROG_OBJ] -- simple shadowing of input and output parameters
create or replace function shadowtest(in1 int)
	returns table (out1 int) as $$
declare
in1 int;
out1 int;
begin
end
$$ language plpgsql
;
----------
[TRUNCATE_TABLE] truncate rem1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26765_2 (tid pg_catalog.tid)
;
----------
[DROP_VIEW] drop materialized view parallel_mat_view
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_visibility_map(regclass, blkno bigint,
								  all_visible OUT boolean,
								  all_frozen OUT boolean)
RETURNS record
AS '$libdir/pg_visibility', 'pg_visibility_map'
LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] create function extractq2_2(t int8_tbl) returns table(ret1 int8) as $$
  select extractq2(t) offset 0
$$ language sql immutable
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION evttrig_tscfg (COPY = pg_catalog.simple)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_u AS SELECT a, b FROM collate_test1 UNION ALL SELECT a, b FROM collate_test2
;
