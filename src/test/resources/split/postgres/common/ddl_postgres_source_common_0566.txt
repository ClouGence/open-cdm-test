-- check if indexscans do the right things
CREATE INDEX fooi ON foo (f1)
;
-- changing the type of column c causes its single-column stats to be dropped,
-- giving a default estimate of 0.005 * 5000 = 25 for (c = 1); check multiple
-- clauses estimated with functional dependencies does not exceed this
ALTER TABLE functional_dependencies ALTER COLUMN c TYPE numeric
;
ALTER TEXT SEARCH TEMPLATE alt_ts_temp1 RENAME TO alt_ts_temp2
;
CREATE FUNCTION isnle(ismn13, ismn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
alter trigger b on middle rename to c
;
ALTER TABLE pg_conversion ADD UNIQUE USING INDEX pg_conversion_name_nsp_index
;
-- check that references to columns after dropped columns are handled correctly
create table dropcol(key int primary key, drop1 int, keep1 text, drop2 numeric, keep2 float)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER TABLE pg_toast.pg_toast_30487 RENAME TO reind_con_toast
;
CREATE TABLE regtest_ptable_1_tens PARTITION OF regtest_ptable_1 FOR VALUES FROM ('10') TO ('100')
;
CREATE FUNCTION inj_io_short_read_detach()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
ALTER INDEX pg_toast.pg_toast_33212_index RENAME TO reind_con_toast_idx
;
-- Should fail. Not allowed to have cross-type skip support function.
ALTER OPERATOR FAMILY alt_opf18 USING btree
  ADD FUNCTION 6 (int4, int2) btint4skipsupport(internal)
;
-- numeric check

CREATE TABLE numerictmp (a numeric)
;
create table PSlot (
    slotname	char(20),
    pfname	text,
    slotlink	char(20),
    backlink	char(20)
)
;
CREATE TABLE "public"."temporal_mltrng2" (
    "id1" "int4range" NOT NULL,
    "id2" "int4range" NOT NULL,
    "valid_at" "datemultirange" NOT NULL
)
;
CREATE TYPE mytype AS (a int)
;
--
  -- Setup to test an error from ExecUpdate and ExecDelete.
  --
  CREATE TABLE foo (a int, b text) PARTITION BY LIST(a)
;
-- create index matest2i on matest2 ((1-id));  -- intentionally missing
create index matest3i on matest3 ((1-id))
;
DROP TABLE test_replica_identity4
;
-- error
CREATE UNIQUE INDEX ON test3bpci (x)
;
CREATE TABLE split_lock_next.parent (id integer PRIMARY KEY)
;
CREATE FUNCTION gbt_uuid_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
DROP SUBSCRIPTION sub2
;
create table inh_nn1 (f1 int check(f1 > 5) primary key references inh_nn1, f2 int not null)
;
-- When a table is attached as partition to a partitioned table that has
-- a foreign key to another partitioned table, it acquires a clone of the
-- FK.  Upon detach, Postgres 14 and earlier remove the foreign key (newer
-- versions make it a standalone constraint.)
CREATE SCHEMA fkpart12
  CREATE TABLE fk_p ( id int, jd int, PRIMARY KEY(id, jd)) PARTITION BY list (id)
  CREATE TABLE fk_p_1 PARTITION OF fk_p FOR VALUES IN (1) PARTITION BY list (jd)
  CREATE TABLE fk_p_1_1 PARTITION OF fk_p_1 FOR VALUES IN (1)
  CREATE TABLE fk_p_1_2 PARTITION OF fk_p_1 FOR VALUES IN (2)
  CREATE TABLE fk_p_2 PARTITION OF fk_p FOR VALUES IN (2) PARTITION BY list (jd)
  CREATE TABLE fk_p_2_1 PARTITION OF fk_p_2 FOR VALUES IN (1)
  CREATE TABLE fk_p_2_2 PARTITION OF fk_p_2 FOR VALUES IN (2)
  CREATE TABLE fk_r_1 ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL)
  CREATE TABLE fk_r_2 ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL) PARTITION BY list (id)
  CREATE TABLE fk_r_2_1 PARTITION OF fk_r_2 FOR VALUES IN (2, 1)
  CREATE TABLE fk_r   ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL,
       FOREIGN KEY (p_id, p_jd) REFERENCES fk_p (id, jd)
  ) PARTITION BY list (id)
;
ALTER DOMAIN public.int_list_domain SET SCHEMA split_domain_type_next
;
ALTER SEQUENCE INSERT_SEQ RESTART WITH 4
;
CREATE TABLE brinopers (colname name, typ text,
	op text[], value text[], matches int[],
	check (cardinality(op) = cardinality(value)),
	check (cardinality(op) = cardinality(matches)))
;
DROP EVENT TRIGGER undroppable
;
CREATE TABLE t2 (num2 int, value text)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_seclabel
;
create temp table t1(f1 float8)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
DROP TABLE base_tbl3
;
CREATE INDEX partidx_abc_idx ON public.idxpart USING btree (a, b, c)
;
CREATE TABLE mvtest_v AS SELECT generate_series(1,10) AS a
;
CREATE POLICY p2 ON current_check FOR DELETE USING (currentid = 4 AND rlsuser = current_user)
;
CREATE TABLE test_is_json (js text)
;
ALTER TABLE t2 ADD COLUMN d domain4
;
CREATE INDEX idx_c_id_for_A_1509405683 ON A1 USING btree(c_id "pg_catalog"."int4_ops" asc NULLS LAST)
;
-- disallowed because of search_path issues with pg_dump
CREATE TABLE pg_catalog.new_system_table()
;
--
-- ISSN13 operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn13,
	RIGHTARG = issn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE TEMP TABLE tasks (
  id INT PRIMARY KEY,
  owner INT REFERENCES users ON UPDATE CASCADE ON DELETE SET NULL,
  worker INT REFERENCES users ON UPDATE CASCADE ON DELETE SET NULL,
  checked_by INT REFERENCES users ON UPDATE CASCADE ON DELETE SET NULL
)
;
CREATE TABLE public.tst_one_enum (
		a INTEGER PRIMARY KEY,
		b public.tst_enum_t
		)
;
DROP TRIGGER after_upd_row_trig ON main_table
;
CREATE OPERATOR <= (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = >,
    COMMUTATOR = >=,
    PROCEDURE  = citext_le,
    RESTRICT   = scalarltsel,
    JOIN       = scalarltjoinsel
)
;
CREATE MATERIALIZED VIEW ext_cine_mv AS SELECT 11 AS f1
;
CREATE VIEW pg_stat_user_functions AS
    SELECT
            P.oid AS funcid,
            N.nspname AS schemaname,
            P.proname AS funcname,
            pg_stat_get_function_calls(P.oid) AS calls,
            pg_stat_get_function_total_time(P.oid) AS total_time,
            pg_stat_get_function_self_time(P.oid) AS self_time
    FROM pg_proc P LEFT JOIN pg_namespace N ON (N.oid = P.pronamespace)
    WHERE P.prolang != 12  -- fast check to eliminate built-in functions
          AND pg_stat_get_function_calls(P.oid) IS NOT NULL
;
CREATE TABLE "public"."kd_point_tbl" (
    "p" "point"
)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] -- check if indexscans do the right things
CREATE INDEX fooi ON foo (f1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- changing the type of column c causes its single-column stats to be dropped,
-- giving a default estimate of 0.005 * 5000 = 25 for (c = 1); check multiple
-- clauses estimated with functional dependencies does not exceed this
ALTER TABLE functional_dependencies ALTER COLUMN c TYPE numeric
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH TEMPLATE alt_ts_temp1 RENAME TO alt_ts_temp2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ismn13, ismn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_TRIGGER] alter trigger b on middle rename to c
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_conversion ADD UNIQUE USING INDEX pg_conversion_name_nsp_index
;
----------
[CREATE_TABLE] -- check that references to columns after dropped columns are handled correctly
create table dropcol(key int primary key, drop1 int, keep1 text, drop2 numeric, keep2 float)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30487 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE regtest_ptable_1_tens PARTITION OF regtest_ptable_1 FOR VALUES FROM ('10') TO ('100')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION inj_io_short_read_detach()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33212_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_PROG_OBJ] -- Should fail. Not allowed to have cross-type skip support function.
ALTER OPERATOR FAMILY alt_opf18 USING btree
  ADD FUNCTION 6 (int4, int2) btint4skipsupport(internal)
;
----------
[CREATE_TABLE] -- numeric check

CREATE TABLE numerictmp (a numeric)
;
----------
[CREATE_TABLE] create table PSlot (
    slotname	char(20),
    pfname	text,
    slotlink	char(20),
    backlink	char(20)
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."temporal_mltrng2" (
    "id1" "int4range" NOT NULL,
    "id2" "int4range" NOT NULL,
    "valid_at" "datemultirange" NOT NULL
)
;
----------
[CREATE_TYPE] CREATE TYPE mytype AS (a int)
;
----------
[CREATE_TABLE] --
  -- Setup to test an error from ExecUpdate and ExecDelete.
  --
  CREATE TABLE foo (a int, b text) PARTITION BY LIST(a)
;
----------
[ADD_INDEX] -- create index matest2i on matest2 ((1-id));  -- intentionally missing
create index matest3i on matest3 ((1-id))
;
----------
[DROP_TABLE] DROP TABLE test_replica_identity4
;
----------
[ADD_INDEX] -- error
CREATE UNIQUE INDEX ON test3bpci (x)
;
----------
[CREATE_TABLE] CREATE TABLE split_lock_next.parent (id integer PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_uuid_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION sub2
;
----------
[CREATE_TABLE] create table inh_nn1 (f1 int check(f1 > 5) primary key references inh_nn1, f2 int not null)
;
----------
[CREATE_SCHEMA] -- When a table is attached as partition to a partitioned table that has
-- a foreign key to another partitioned table, it acquires a clone of the
-- FK.  Upon detach, Postgres 14 and earlier remove the foreign key (newer
-- versions make it a standalone constraint.)
CREATE SCHEMA fkpart12
  CREATE TABLE fk_p ( id int, jd int, PRIMARY KEY(id, jd)) PARTITION BY list (id)
  CREATE TABLE fk_p_1 PARTITION OF fk_p FOR VALUES IN (1) PARTITION BY list (jd)
  CREATE TABLE fk_p_1_1 PARTITION OF fk_p_1 FOR VALUES IN (1)
  CREATE TABLE fk_p_1_2 PARTITION OF fk_p_1 FOR VALUES IN (2)
  CREATE TABLE fk_p_2 PARTITION OF fk_p FOR VALUES IN (2) PARTITION BY list (jd)
  CREATE TABLE fk_p_2_1 PARTITION OF fk_p_2 FOR VALUES IN (1)
  CREATE TABLE fk_p_2_2 PARTITION OF fk_p_2 FOR VALUES IN (2)
  CREATE TABLE fk_r_1 ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL)
  CREATE TABLE fk_r_2 ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL) PARTITION BY list (id)
  CREATE TABLE fk_r_2_1 PARTITION OF fk_r_2 FOR VALUES IN (2, 1)
  CREATE TABLE fk_r   ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL,
       FOREIGN KEY (p_id, p_jd) REFERENCES fk_p (id, jd)
  ) PARTITION BY list (id)
;
----------
[ALTER_TYPE] ALTER DOMAIN public.int_list_domain SET SCHEMA split_domain_type_next
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE INSERT_SEQ RESTART WITH 4
;
----------
[CREATE_TABLE] CREATE TABLE brinopers (colname name, typ text,
	op text[], value text[], matches int[],
	check (cardinality(op) = cardinality(value)),
	check (cardinality(op) = cardinality(matches)))
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER undroppable
;
----------
[CREATE_TABLE] CREATE TABLE t2 (num2 int, value text)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_seclabel
;
----------
[CREATE_TABLE] create temp table t1(f1 float8)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
----------
[DROP_TABLE] DROP TABLE base_tbl3
;
----------
[ADD_INDEX] CREATE INDEX partidx_abc_idx ON public.idxpart USING btree (a, b, c)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE mvtest_v AS SELECT generate_series(1,10) AS a
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON current_check FOR DELETE USING (currentid = 4 AND rlsuser = current_user)
;
----------
[CREATE_TABLE] CREATE TABLE test_is_json (js text)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t2 ADD COLUMN d domain4
;
----------
[ADD_INDEX] CREATE INDEX idx_c_id_for_A_1509405683 ON A1 USING btree(c_id "pg_catalog"."int4_ops" asc NULLS LAST)
;
----------
[CREATE_TABLE] -- disallowed because of search_path issues with pg_dump
CREATE TABLE pg_catalog.new_system_table()
;
----------
[CREATE_PROG_OBJ] --
-- ISSN13 operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn13,
	RIGHTARG = issn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tasks (
  id INT PRIMARY KEY,
  owner INT REFERENCES users ON UPDATE CASCADE ON DELETE SET NULL,
  worker INT REFERENCES users ON UPDATE CASCADE ON DELETE SET NULL,
  checked_by INT REFERENCES users ON UPDATE CASCADE ON DELETE SET NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_one_enum (
		a INTEGER PRIMARY KEY,
		b public.tst_enum_t
		)
;
----------
[DROP_TRIGGER] DROP TRIGGER after_upd_row_trig ON main_table
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = >,
    COMMUTATOR = >=,
    PROCEDURE  = citext_le,
    RESTRICT   = scalarltsel,
    JOIN       = scalarltjoinsel
)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW ext_cine_mv AS SELECT 11 AS f1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_user_functions AS
    SELECT
            P.oid AS funcid,
            N.nspname AS schemaname,
            P.proname AS funcname,
            pg_stat_get_function_calls(P.oid) AS calls,
            pg_stat_get_function_total_time(P.oid) AS total_time,
            pg_stat_get_function_self_time(P.oid) AS self_time
    FROM pg_proc P LEFT JOIN pg_namespace N ON (N.oid = P.pronamespace)
    WHERE P.prolang != 12  -- fast check to eliminate built-in functions
          AND pg_stat_get_function_calls(P.oid) IS NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE "public"."kd_point_tbl" (
    "p" "point"
)
;
