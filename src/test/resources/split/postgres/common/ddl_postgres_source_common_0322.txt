CREATE TABLE pitest2_p1 PARTITION OF pitest2 FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
create table atacc2 (test2 int)
;
-- error
CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION nonexistent
;
CREATE SUBSCRIPTION sub CONNECTION 'port=13636 host=/tmp/dw4XcG0E_M dbname=postgres' PUBLICATION pub WITH (disable_on_error = true, streaming = on, two_phase = on)
;
ALTER TABLE temporal3 DROP COLUMN valid_at CASCADE
;
DROP TRIGGER IF EXISTS accounts_balance_constraint ON split_trigger_probe.accounts
;
create function outparam_succeed(i anyrange, out r anyrange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
ALTER TABLE a ENABLE TRIGGER t
;
create temp table defp (f1 int primary key)
;
ALTER TABLE pg_temp_10.pg_temp_26642_2 ADD COLUMN newdata pg_temp_10.pg_temp_26642
;
CREATE TRIGGER trig_instead_of_insert_tbl_view_2
  INSTEAD OF INSERT ON instead_of_insert_tbl_view_2
  FOR EACH ROW EXECUTE PROCEDURE fun_instead_of_insert_tbl()
;
CREATE FUNCTION ean13_out(isbn13)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE SCHEMA "mvtest_mvschema"
;
ALTER FOREIGN TABLE async_p1 OPTIONS (use_remote_estimate 'true')
;
CREATE TABLE taby (id int NOT NULL)
;
CREATE VIEW pg_stat_all_indexes AS
    SELECT
            C.oid AS relid,
            I.oid AS indexrelid,
            N.nspname AS schemaname,
            C.relname AS relname,
            I.relname AS indexrelname,
            pg_stat_get_numscans(I.oid) AS idx_scan,
            pg_stat_get_lastscan(I.oid) AS last_idx_scan,
            pg_stat_get_tuples_returned(I.oid) AS idx_tup_read,
            pg_stat_get_tuples_fetched(I.oid) AS idx_tup_fetch
    FROM pg_class C JOIN
            pg_index X ON C.oid = X.indrelid JOIN
            pg_class I ON I.oid = X.indexrelid
            LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm')
;
drop type textrange_supp
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext
;
DROP TABLE test_missing_target
;
COMMENT ON TYPE default_test_row IS NULL
;
DROP TABLE plt2_adv_p3
;
DO $$
DECLARE
  result integer;
BEGIN
MERGE INTO pa_target t
  USING pa_source s
  ON t.tid = s.sid
  WHEN MATCHED THEN
    UPDATE SET tid = tid + 1, balance = balance + delta, val = val || ' updated by merge'
  WHEN NOT MATCHED THEN
    INSERT VALUES (sid, delta, 'inserted by merge');
GET DIAGNOSTICS result := ROW_COUNT;
RAISE NOTICE 'ROW_COUNT = %', result;
END;
$$
;
-- polymorphic input

-- test functions with default parameters

-- test basic functionality
create function dfunc(a int = 1, int = 2) returns int as $$
  select $1 + $2;
$$ language sql
;
CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
create domain testdomain1 as int constraint unsigned check (value > 0)
;
-- float4 check

CREATE TABLE float4tmp (a float4)
;
CREATE TEMP TABLE foo (id integer)
;
create function xfloat8out(xfloat8) returns cstring immutable strict
  language internal as 'int8out'
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."d_star" INHERIT "public"."b_star"
;
ALTER POLICY p1 ON s1 USING (a in (select x from v2))
;
CREATE TABLE fk_partitioned_fk (a INT REFERENCES fk_notpartitioned_pk(a) PRIMARY KEY) PARTITION BY RANGE(a)
;
alter table part_column_drop drop column useless_1
;
CREATE TABLE num_exp_ln (id int4, expected numeric(210,10))
;
-- aggregate
DROP AGGREGATE test_aggregate_exists(*)
;
CREATE TABLE cic_test (a int)
;
-- Tables, indexes, triggers
CREATE TEMP TABLE tab_stats (a int, b char(20))
;
CREATE FUNCTION isneq(ean13, ismn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- OK

CREATE VIEW rw_view2 AS
  SELECT s, c, s/c t, a base_a, ctid
  FROM rw_view1
;
DROP DOMAIN IF EXISTS no_such_schema.foo
;
CREATE TABLE test_type_diff2_c1 (int_four int4, int_eight int8, int_two int2)
;
-- adding a new column as primary key to a non-empty table.
-- should fail unless the column has a non-null default value.
create table atacc1 ( test int )
;
CREATE CAST (hstore AS jsonb)
  WITH FUNCTION hstore_to_jsonb(hstore)
;
CREATE OPERATOR CLASS numeric_ops
DEFAULT FOR TYPE numeric USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       gin_numeric_cmp(numeric,numeric),
    FUNCTION        2       gin_extract_value_numeric(numeric, internal),
    FUNCTION        3       gin_extract_query_numeric(numeric, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_numeric(numeric,numeric,int2, internal),
STORAGE         numeric
;
drop index gist_pointidx2, gist_pointidx3, gist_pointidx4
;
ALTER FUNCTION int4_dist(integer, integer) PARALLEL SAFE
;
CREATE TABLE rls_test_restrictive (
    username        name,
    supervisor      name,
    data            integer
)
;
CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE TABLE regress_create_schema_role.tab (id int)
;
CREATE OR REPLACE FUNCTION hs_subxids (n integer)
    RETURNS void
    LANGUAGE plpgsql
    AS $$
    BEGIN
        IF n <= 0 THEN RETURN; END IF;
        INSERT INTO t_012_tbl VALUES (n);
        PERFORM hs_subxids(n - 1);
        RETURN;
    EXCEPTION WHEN raise_exception THEN NULL; END;
    $$
;
ALTER POLICY p1 ON event_trigger_test RENAME TO p2
;
CREATE FUNCTION bad_cmp2 (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT
			CASE WHEN $1 = $2 AND $1 = 400 THEN -1
			ELSE public.ok_cmp($1, $2)
		END;
	$$
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE pitest2_p1 PARTITION OF pitest2 FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[CREATE_TABLE] create table atacc2 (test2 int)
;
----------
[CREATE_SCHEMA] -- error
CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION nonexistent
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub CONNECTION 'port=13636 host=/tmp/dw4XcG0E_M dbname=postgres' PUBLICATION pub WITH (disable_on_error = true, streaming = on, two_phase = on)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE temporal3 DROP COLUMN valid_at CASCADE
;
----------
[DROP_TRIGGER] DROP TRIGGER IF EXISTS accounts_balance_constraint ON split_trigger_probe.accounts
;
----------
[CREATE_PROG_OBJ] create function outparam_succeed(i anyrange, out r anyrange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
----------
[ALTER_TABLE] ALTER TABLE a ENABLE TRIGGER t
;
----------
[CREATE_TABLE] create temp table defp (f1 int primary key)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_26642_2 ADD COLUMN newdata pg_temp_10.pg_temp_26642
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_instead_of_insert_tbl_view_2
  INSTEAD OF INSERT ON instead_of_insert_tbl_view_2
  FOR EACH ROW EXECUTE PROCEDURE fun_instead_of_insert_tbl()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ean13_out(isbn13)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "mvtest_mvschema"
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE async_p1 OPTIONS (use_remote_estimate 'true')
;
----------
[CREATE_TABLE] CREATE TABLE taby (id int NOT NULL)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_all_indexes AS
    SELECT
            C.oid AS relid,
            I.oid AS indexrelid,
            N.nspname AS schemaname,
            C.relname AS relname,
            I.relname AS indexrelname,
            pg_stat_get_numscans(I.oid) AS idx_scan,
            pg_stat_get_lastscan(I.oid) AS last_idx_scan,
            pg_stat_get_tuples_returned(I.oid) AS idx_tup_read,
            pg_stat_get_tuples_fetched(I.oid) AS idx_tup_fetch
    FROM pg_class C JOIN
            pg_index X ON C.oid = X.indrelid JOIN
            pg_class I ON I.oid = X.indexrelid
            LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm')
;
----------
[DROP_TYPE] drop type textrange_supp
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext
;
----------
[DROP_TABLE] DROP TABLE test_missing_target
;
----------
[COMMENT_TYPE] COMMENT ON TYPE default_test_row IS NULL
;
----------
[DROP_TABLE] DROP TABLE plt2_adv_p3
;
----------
[BLOCK] DO $$
DECLARE
  result integer;
BEGIN
MERGE INTO pa_target t
  USING pa_source s
  ON t.tid = s.sid
  WHEN MATCHED THEN
    UPDATE SET tid = tid + 1, balance = balance + delta, val = val || ' updated by merge'
  WHEN NOT MATCHED THEN
    INSERT VALUES (sid, delta, 'inserted by merge');
GET DIAGNOSTICS result := ROW_COUNT;
RAISE NOTICE 'ROW_COUNT = %', result;
END;
$$
;
----------
[CREATE_PROG_OBJ] -- polymorphic input

-- test functions with default parameters

-- test basic functionality
create function dfunc(a int = 1, int = 2) returns int as $$
  select $1 + $2;
$$ language sql
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
----------
[CREATE_TYPE] create domain testdomain1 as int constraint unsigned check (value > 0)
;
----------
[CREATE_TABLE] -- float4 check

CREATE TABLE float4tmp (a float4)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE foo (id integer)
;
----------
[CREATE_PROG_OBJ] create function xfloat8out(xfloat8) returns cstring immutable strict
  language internal as 'int8out'
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."d_star" INHERIT "public"."b_star"
;
----------
[ALTER_POLICY] ALTER POLICY p1 ON s1 USING (a in (select x from v2))
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk (a INT REFERENCES fk_notpartitioned_pk(a) PRIMARY KEY) PARTITION BY RANGE(a)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table part_column_drop drop column useless_1
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_ln (id int4, expected numeric(210,10))
;
----------
[DROP_PROG_OBJ] -- aggregate
DROP AGGREGATE test_aggregate_exists(*)
;
----------
[CREATE_TABLE] CREATE TABLE cic_test (a int)
;
----------
[CREATE_TABLE] -- Tables, indexes, triggers
CREATE TEMP TABLE tab_stats (a int, b char(20))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ean13, ismn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] -- OK

CREATE VIEW rw_view2 AS
  SELECT s, c, s/c t, a base_a, ctid
  FROM rw_view1
;
----------
[DROP_TYPE] DROP DOMAIN IF EXISTS no_such_schema.foo
;
----------
[CREATE_TABLE] CREATE TABLE test_type_diff2_c1 (int_four int4, int_eight int8, int_two int2)
;
----------
[CREATE_TABLE] -- adding a new column as primary key to a non-empty table.
-- should fail unless the column has a non-null default value.
create table atacc1 ( test int )
;
----------
[CREATE_PROG_OBJ] CREATE CAST (hstore AS jsonb)
  WITH FUNCTION hstore_to_jsonb(hstore)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS numeric_ops
DEFAULT FOR TYPE numeric USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       gin_numeric_cmp(numeric,numeric),
    FUNCTION        2       gin_extract_value_numeric(numeric, internal),
    FUNCTION        3       gin_extract_query_numeric(numeric, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_numeric(numeric,numeric,int2, internal),
STORAGE         numeric
;
----------
[DROP_INDEX] drop index gist_pointidx2, gist_pointidx3, gist_pointidx4
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION int4_dist(integer, integer) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE rls_test_restrictive (
    username        name,
    supervisor      name,
    data            integer
)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE TABLE regress_create_schema_role.tab (id int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION hs_subxids (n integer)
    RETURNS void
    LANGUAGE plpgsql
    AS $$
    BEGIN
        IF n <= 0 THEN RETURN; END IF;
        INSERT INTO t_012_tbl VALUES (n);
        PERFORM hs_subxids(n - 1);
        RETURN;
    EXCEPTION WHEN raise_exception THEN NULL; END;
    $$
;
----------
[ALTER_POLICY] ALTER POLICY p1 ON event_trigger_test RENAME TO p2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bad_cmp2 (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT
			CASE WHEN $1 = $2 AND $1 = 400 THEN -1
			ELSE public.ok_cmp($1, $2)
		END;
	$$
;
