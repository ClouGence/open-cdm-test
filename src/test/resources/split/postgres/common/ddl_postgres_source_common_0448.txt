ANALYZE pg_temp_13.pg_temp_26669
;
-- should trigger ddl_command_{start,end}
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS 'classified'
;
VACUUM (FULL) pg_catalog.pg_seclabel
;
--  relpages may be -1 for partitioned tables
CREATE TABLE stats_import.part_parent ( i integer ) PARTITION BY RANGE(i)
;
-- error

CREATE FUNCTION cp_testfunc1(a int) RETURNS int LANGUAGE SQL AS $$ SELECT a $$
;
--
-- test ALTER THING name DEPENDS ON EXTENSION
--

-- Common setup for all tests
CREATE TABLE test_extdep_commands (command text)
;
CREATE TABLE brin_summarize (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
ALTER TABLE s4.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
ALTER TABLE ONLY "fkpart3"."pk5" DROP COLUMN "........pg.dropped.2........"
;
CREATE INDEX ind ON concur_reindex_schema.tab (a)
;
-- fails
ALTER TABLE ONLY pitest3 ALTER COLUMN f3 DROP IDENTITY
;
ALTER TABLE trgfire ATTACH PARTITION trgfire3 FOR VALUES FROM (20) TO (30)
;
CREATE OPERATOR %> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = word_similarity_commutator_op,
        COMMUTATOR = '<%',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
VACUUM pg_catalog.pg_event_trigger
;
ALTER OPERATOR FAMILY gist_bytea_ops USING gist ADD
	FUNCTION	11  (bytea, bytea) gbt_bytea_sortsupport (internal)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
CREATE TABLE "public"."euc_jis_2004_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
CREATE TEMP TABLE reloptions_test(i INT NOT NULL, j text)
	WITH (vacuum_truncate=false,
	toast.vacuum_truncate=false,
	autovacuum_enabled=false)
;
CREATE OR REPLACE FUNCTION  random_string( INT ) RETURNS text AS $$
		SELECT string_agg(substring('0123456789abcdefghijklmnopqrstuvwxyz', ceil(random() * 36)::integer, 1), '') from generate_series(1, $1);
		$$ LANGUAGE SQL
;
-- a view should also be temporary if it references a temporary sequence
CREATE SEQUENCE seq1
;
ALTER TABLE pg_toast.pg_toast_27723 RENAME TO reind_con_toast
;
CREATE TABLE lotest_stash_values (loid oid, fd integer)
;
DO
LANGUAGE plpgsql
$$
DECLARE
    x int := 3;
    y int := 4;
BEGIN
    FOR i IN 1..5 LOOP
        CALL test_proc6(i, x, y);
        RAISE INFO 'x = %, y = %', x, y;
    END LOOP;
END;
$$
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_am
;
-- ok, child resolves ambiguous default

DROP TABLE firstparent, secondparent, jointchild, thirdparent, otherchild
;
alter type insenum add value 'i5' before 'L2'
;
CREATE VIEW v8 AS SELECT * FROM base_table WHERE EXISTS (SELECT 1)
;
create table pktable_base (base1 int not null)
;
CREATE UNIQUE INDEX concur_exprs_index_pred_2 ON public.concur_exprs_tab USING btree (((1 / c1))) WHERE ('-H'::text >= ((c2)::text COLLATE "C"))
;
CREATE TRIGGER transition_table_level1_ri_parent_del_trigger
  AFTER DELETE ON transition_table_level1
  REFERENCING OLD TABLE AS p
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level1_ri_parent_del_func()
;
ANALYZE flt
;
ALTER SUBSCRIPTION split_sub SET PUBLICATION split_pub_renamed WITH (refresh = false)
;
CREATE OR REPLACE FUNCTION regexp_match(string citext, pattern citext) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
-- add a primary key constraint (fails)
alter table atacc1 add constraint atacc_test1 primary key (test1)
;
ALTER TABLE mcv_lists ALTER COLUMN c TYPE numeric
;
DROP TABLE notnull_tbl1, notnull_tbl1_child, notnull_tbl1_child2
;
create table idxpart0 partition of idxpart (i) for values with (modulus 2, remainder 0)
;
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY, c_int INT DEFAULT 1)
;
-- Check default partition
create table part_def partition of range_parted default
;
-- mixing named and positional: same as previous test, but param1 is duplicated
create function namedparmcursor_test4() returns void as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
begin
    open c1(20, param1 := 21);
end
$$ language plpgsql
;
CREATE TABLE attmp2 (a int primary key)
;
TRUNCATE TABLE trunc_c,trunc_d,trunc_e,truncate_a
;
ALTER TABLE fk_partitioned_fk_3 DROP COLUMN fdrop1, DROP COLUMN fdrop2,
	DROP COLUMN fdrop3, DROP COLUMN fdrop4
;
DROP FUNCTION row_before_insupd_trigfunc
;
ALTER FUNCTION hash_bitmap_info(regclass, int8) PARALLEL RESTRICTED
;
--
-- pg_get_wal_records_info()
--
CREATE FUNCTION pg_get_wal_records_info(IN start_lsn pg_lsn,
    IN end_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_records_info'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP TABLE attmp
;
CREATE INDEX jidx ON testjsonb USING gin (j)
;
CREATE SUBSCRIPTION sub2
						 CONNECTION 'dbname=doesnotexist' PUBLICATION pub1
						 WITH (connect = false, origin = none, streaming = off)
;
ALTER TABLE tab_main ATTACH PARTITION tab_part2 FOR VALUES FROM (5) to (10)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26669
;
----------
[UNKNOWN] -- should trigger ddl_command_{start,end}
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS 'classified'
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_seclabel
;
----------
[CREATE_TABLE] --  relpages may be -1 for partitioned tables
CREATE TABLE stats_import.part_parent ( i integer ) PARTITION BY RANGE(i)
;
----------
[CREATE_PROG_OBJ] -- error

CREATE FUNCTION cp_testfunc1(a int) RETURNS int LANGUAGE SQL AS $$ SELECT a $$
;
----------
[CREATE_TABLE] --
-- test ALTER THING name DEPENDS ON EXTENSION
--

-- Common setup for all tests
CREATE TABLE test_extdep_commands (command text)
;
----------
[CREATE_TABLE] CREATE TABLE brin_summarize (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE s4.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "fkpart3"."pk5" DROP COLUMN "........pg.dropped.2........"
;
----------
[ADD_INDEX] CREATE INDEX ind ON concur_reindex_schema.tab (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails
ALTER TABLE ONLY pitest3 ALTER COLUMN f3 DROP IDENTITY
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE trgfire ATTACH PARTITION trgfire3 FOR VALUES FROM (20) TO (30)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR %> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = word_similarity_commutator_op,
        COMMUTATOR = '<%',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_event_trigger
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bytea_ops USING gist ADD
	FUNCTION	11  (bytea, bytea) gbt_bytea_sortsupport (internal)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
----------
[CREATE_TABLE] CREATE TABLE "public"."euc_jis_2004_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE reloptions_test(i INT NOT NULL, j text)
	WITH (vacuum_truncate=false,
	toast.vacuum_truncate=false,
	autovacuum_enabled=false)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION  random_string( INT ) RETURNS text AS $$
		SELECT string_agg(substring('0123456789abcdefghijklmnopqrstuvwxyz', ceil(random() * 36)::integer, 1), '') from generate_series(1, $1);
		$$ LANGUAGE SQL
;
----------
[CREATE_SEQUENCE] -- a view should also be temporary if it references a temporary sequence
CREATE SEQUENCE seq1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27723 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE lotest_stash_values (loid oid, fd integer)
;
----------
[BLOCK] DO
LANGUAGE plpgsql
$$
DECLARE
    x int := 3;
    y int := 4;
BEGIN
    FOR i IN 1..5 LOOP
        CALL test_proc6(i, x, y);
        RAISE INFO 'x = %, y = %', x, y;
    END LOOP;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_am
;
----------
[DROP_TABLE] -- ok, child resolves ambiguous default

DROP TABLE firstparent, secondparent, jointchild, thirdparent, otherchild
;
----------
[ALTER_TYPE] alter type insenum add value 'i5' before 'L2'
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v8 AS SELECT * FROM base_table WHERE EXISTS (SELECT 1)
;
----------
[CREATE_TABLE] create table pktable_base (base1 int not null)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX concur_exprs_index_pred_2 ON public.concur_exprs_tab USING btree (((1 / c1))) WHERE ('-H'::text >= ((c2)::text COLLATE "C"))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER transition_table_level1_ri_parent_del_trigger
  AFTER DELETE ON transition_table_level1
  REFERENCING OLD TABLE AS p
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level1_ri_parent_del_func()
;
----------
[ADMIN_TABLE] ANALYZE flt
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION split_sub SET PUBLICATION split_pub_renamed WITH (refresh = false)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_match(string citext, pattern citext) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a primary key constraint (fails)
alter table atacc1 add constraint atacc_test1 primary key (test1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE mcv_lists ALTER COLUMN c TYPE numeric
;
----------
[DROP_TABLE] DROP TABLE notnull_tbl1, notnull_tbl1_child, notnull_tbl1_child2
;
----------
[CREATE_TABLE] create table idxpart0 partition of idxpart (i) for values with (modulus 2, remainder 0)
;
----------
[CREATE_TABLE] CREATE TABLE T(pk INT NOT NULL PRIMARY KEY, c_int INT DEFAULT 1)
;
----------
[CREATE_TABLE] -- Check default partition
create table part_def partition of range_parted default
;
----------
[CREATE_PROG_OBJ] -- mixing named and positional: same as previous test, but param1 is duplicated
create function namedparmcursor_test4() returns void as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
begin
    open c1(20, param1 := 21);
end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE attmp2 (a int primary key)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE trunc_c,trunc_d,trunc_e,truncate_a
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk_partitioned_fk_3 DROP COLUMN fdrop1, DROP COLUMN fdrop2,
	DROP COLUMN fdrop3, DROP COLUMN fdrop4
;
----------
[DROP_PROG_OBJ] DROP FUNCTION row_before_insupd_trigfunc
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION hash_bitmap_info(regclass, int8) PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] --
-- pg_get_wal_records_info()
--
CREATE FUNCTION pg_get_wal_records_info(IN start_lsn pg_lsn,
    IN end_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_records_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE attmp
;
----------
[ADD_INDEX] CREATE INDEX jidx ON testjsonb USING gin (j)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2
						 CONNECTION 'dbname=doesnotexist' PUBLICATION pub1
						 WITH (connect = false, origin = none, streaming = off)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab_main ATTACH PARTITION tab_part2 FOR VALUES FROM (5) to (10)
;
