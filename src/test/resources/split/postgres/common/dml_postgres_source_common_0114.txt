-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."gtest20"'::pg_catalog.regclass
;
DELETE FROM rw_view1 WHERE a = 1.05
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16301', relminmxid = '3'
WHERE oid = '"public"."pt_part2"'::pg_catalog.regclass
;
INSERT into fk VALUES (4500)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '26001'
;
INSERT INTO nulltest values ('a', 'b', 'c', 'd', 'c')
;
-- fix the index on the leaf partition
delete from idxpart11 where b = 0
;
UPDATE pg_database SET datconnlimit = -2 WHERE datname = 'regression_invalid'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtestxx_2"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10080', relminmxid = '1'
WHERE oid = '28539'
;
INSERT INTO ref_tbl SELECT * FROM generate_series(1,10)
;
INSERT INTO brintest SELECT
	repeat(stringu1, 8)::bytea,
	substr(stringu1, 1, 1)::"char",
	stringu1::name, 142857 * tenthous,
	thousand,
	twothousand,
	repeat(stringu1, 8),
	unique1::oid,
	format('(%s,%s)', tenthous, twenty)::tid,
	(four + 1.0)/(hundred+1),
	odd::float8 / (tenthous + 1),
	format('%s:00:%s:00:%s:00', to_hex(odd), to_hex(even), to_hex(hundred))::macaddr,
	inet '10.2.3.4/24' + tenthous,
	cidr '10.2.3/24' + tenthous,
	substr(stringu1, 1, 1)::bpchar,
	date '1995-08-15' + tenthous,
	time '01:20:30' + thousand * interval '18.5 second',
	timestamp '1942-07-23 03:05:09' + tenthous * interval '36.38 hours',
	timestamptz '1972-10-10 03:00' + thousand * interval '1 hour',
	justify_days(justify_hours(tenthous * interval '12 minutes')),
	timetz '01:30:20+02' + hundred * interval '15 seconds',
	thousand::bit(10),
	tenthous::bit(16)::varbit,
	tenthous::numeric(36,30) * fivethous * even / (hundred + 1),
	format('%s%s-%s-%s-%s-%s%s%s', to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'))::uuid,
	int4range(thousand, twothousand),
	format('%s/%s%s', odd, even, tenthous)::pg_lsn,
	box(point(odd, even), point(thousand, twothousand))
FROM tenk1 ORDER BY unique2 LIMIT 100
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '27299'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1518', relminmxid = '1'
WHERE oid = '17320'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12833', relminmxid = '3'
WHERE oid = '"public"."found_test_tbl"'::pg_catalog.regclass
;
INSERT INTO foo VALUES (1,'b','{"a1","b1","c1"}')
;
UPDATE accounts SET balance = balance + 200 WHERE ctid = '(0,1)' OR ctid = '(0,3)' RETURNING accountid, balance
;
INSERT INTO public.ths_p1 VALUES (6, 136)
;
INSERT INTO public.sro_index_mv SELECT (diff.newdata).* FROM pg_temp_38.pg_temp_28037_2 diff WHERE tid IS NULL
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"regress_indexing"."pk2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."itest10"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '770', relminmxid = '1'
WHERE oid = '"public"."student"'::pg_catalog.regclass
;
INSERT INTO public.ths_p2 VALUES (7, 227)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '16832'
;
INSERT INTO num_input_test(n1) VALUES ('1.2e34_')
;
UPDATE CASE_TBL
  SET i = CASE WHEN i >= 3 THEN (- i)
                ELSE (2 * i) END
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO test_missing_target VALUES (6, 4, 'cccc', 'g')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13775', relminmxid = '3'
WHERE oid = '"public"."domain_test"'::pg_catalog.regclass
;
INSERT INTO public.test_arrays (a, b, c) VALUES
		('{1,2,3}', '{1.1, 1.2, 1.3}', '{"one", "two", "three"}'),
		('{3,1,2}', '{1.3, 1.1, 1.2}', '{"three", "one", "two"}')
;
INSERT INTO fkpart11.fk VALUES (1), (3)
;
-- trying replace assuming a composite object, but it's an element or a value

delete from test_jsonb_subscript
;
INSERT INTO public.tplain VALUES ('5', 265)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14896', relminmxid = '3'
WHERE oid = '"fkpart4"."droppk21"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."fk_partitioned_fk_3"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
insert into bar values(4,4)
;
INSERT INTO public.ths_p3 VALUES (0, 760)
;
INSERT INTO num_exp_log10 VALUES (1,'NaN')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14550', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16838', relminmxid = '3'
WHERE oid = '38687'
;
INSERT INTO caster (citext)        VALUES ('((0,0),(1,1),(2,0))'::path)
;
INSERT INTO public.tplain VALUES ('1', 331)
;
INSERT INTO public.tplain VALUES ('8', 528)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."gtest_normal"'::pg_catalog.regclass
;
insert into rtest_interface values ('notjw', 'eth0')
;
delete from pp where f1 = 13
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"public"."spgist_domain_tbl"'::pg_catalog.regclass
;
INSERT INTO num_exp_add VALUES (0,5,'16397.038491')
;
--
-- range UQ: test inserts
--

-- okay:
INSERT INTO temporal_rng3 (id, valid_at) VALUES ('[1,2)', daterange('2018-01-02', '2018-02-03'))
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10478', relminmxid = '3'
WHERE oid = '"fkpart5"."pk32"'::pg_catalog.regclass
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."gtest20"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM rw_view1 WHERE a = 1.05
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16301', relminmxid = '3'
WHERE oid = '"public"."pt_part2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT into fk VALUES (4500)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '26001'
;
----------
[INSERT] INSERT INTO nulltest values ('a', 'b', 'c', 'd', 'c')
;
----------
[DELETE] -- fix the index on the leaf partition
delete from idxpart11 where b = 0
;
----------
[UPDATE] UPDATE pg_database SET datconnlimit = -2 WHERE datname = 'regression_invalid'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtestxx_2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10080', relminmxid = '1'
WHERE oid = '28539'
;
----------
[INSERT|SELECT] INSERT INTO ref_tbl SELECT * FROM generate_series(1,10)
;
----------
[INSERT|SELECT] INSERT INTO brintest SELECT
	repeat(stringu1, 8)::bytea,
	substr(stringu1, 1, 1)::"char",
	stringu1::name, 142857 * tenthous,
	thousand,
	twothousand,
	repeat(stringu1, 8),
	unique1::oid,
	format('(%s,%s)', tenthous, twenty)::tid,
	(four + 1.0)/(hundred+1),
	odd::float8 / (tenthous + 1),
	format('%s:00:%s:00:%s:00', to_hex(odd), to_hex(even), to_hex(hundred))::macaddr,
	inet '10.2.3.4/24' + tenthous,
	cidr '10.2.3/24' + tenthous,
	substr(stringu1, 1, 1)::bpchar,
	date '1995-08-15' + tenthous,
	time '01:20:30' + thousand * interval '18.5 second',
	timestamp '1942-07-23 03:05:09' + tenthous * interval '36.38 hours',
	timestamptz '1972-10-10 03:00' + thousand * interval '1 hour',
	justify_days(justify_hours(tenthous * interval '12 minutes')),
	timetz '01:30:20+02' + hundred * interval '15 seconds',
	thousand::bit(10),
	tenthous::bit(16)::varbit,
	tenthous::numeric(36,30) * fivethous * even / (hundred + 1),
	format('%s%s-%s-%s-%s-%s%s%s', to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'), to_char(tenthous, 'FM0000'))::uuid,
	int4range(thousand, twothousand),
	format('%s/%s%s', odd, even, tenthous)::pg_lsn,
	box(point(odd, even), point(thousand, twothousand))
FROM tenk1 ORDER BY unique2 LIMIT 100
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '27299'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1518', relminmxid = '1'
WHERE oid = '17320'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12833', relminmxid = '3'
WHERE oid = '"public"."found_test_tbl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO foo VALUES (1,'b','{"a1","b1","c1"}')
;
----------
[UPDATE] UPDATE accounts SET balance = balance + 200 WHERE ctid = '(0,1)' OR ctid = '(0,3)' RETURNING accountid, balance
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 136)
;
----------
[INSERT|SELECT] INSERT INTO public.sro_index_mv SELECT (diff.newdata).* FROM pg_temp_38.pg_temp_28037_2 diff WHERE tid IS NULL
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"regress_indexing"."pk2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."itest10"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '770', relminmxid = '1'
WHERE oid = '"public"."student"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 227)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '16832'
;
----------
[INSERT|SELECT] INSERT INTO num_input_test(n1) VALUES ('1.2e34_')
;
----------
[UPDATE] UPDATE CASE_TBL
  SET i = CASE WHEN i >= 3 THEN (- i)
                ELSE (2 * i) END
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] INSERT INTO test_missing_target VALUES (6, 4, 'cccc', 'g')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13775', relminmxid = '3'
WHERE oid = '"public"."domain_test"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO public.test_arrays (a, b, c) VALUES
		('{1,2,3}', '{1.1, 1.2, 1.3}', '{"one", "two", "three"}'),
		('{3,1,2}', '{1.3, 1.1, 1.2}', '{"three", "one", "two"}')
;
----------
[INSERT] INSERT INTO fkpart11.fk VALUES (1), (3)
;
----------
[DELETE] -- trying replace assuming a composite object, but it's an element or a value

delete from test_jsonb_subscript
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('5', 265)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '14896', relminmxid = '3'
WHERE oid = '"fkpart4"."droppk21"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."fk_partitioned_fk_3"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[INSERT] insert into bar values(4,4)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 760)
;
----------
[INSERT] INSERT INTO num_exp_log10 VALUES (1,'NaN')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14550', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16838', relminmxid = '3'
WHERE oid = '38687'
;
----------
[INSERT|SELECT] INSERT INTO caster (citext)        VALUES ('((0,0),(1,1),(2,0))'::path)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 331)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 528)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."gtest_normal"'::pg_catalog.regclass
;
----------
[INSERT] insert into rtest_interface values ('notjw', 'eth0')
;
----------
[DELETE] delete from pp where f1 = 13
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"public"."spgist_domain_tbl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (0,5,'16397.038491')
;
----------
[INSERT|SELECT] --
-- range UQ: test inserts
--

-- okay:
INSERT INTO temporal_rng3 (id, valid_at) VALUES ('[1,2)', daterange('2018-01-02', '2018-02-03'))
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10478', relminmxid = '3'
WHERE oid = '"fkpart5"."pk32"'::pg_catalog.regclass
;
