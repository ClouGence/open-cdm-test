CLUSTER
;
CREATE INDEX t_idx ON t (i)
;
-- fail
CREATE OR REPLACE TRANSFORM FOR hstore LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
create function f1(x anyarray) returns anyelement as $$
begin
  return x[1];
end$$ language plpgsql
;
create trigger merge_target_table_insert_trig
  after insert on merge_target_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
DROP TABLE table_a, table_b, jointest
;
ALTER TEXT SEARCH CONFIGURATION armenian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
create temp sequence ts1
;
CREATE FUNCTION fsm_page_contents(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'fsm_page_contents'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP FUNCTION func_int_custom
;
ANALYZE pg_catalog.pg_attrdef
;
CREATE TRIGGER evtrg_nontemp_trig
  BEFORE INSERT ON evtrg_nontemp_table
  EXECUTE FUNCTION event_trigger_dummy_trigger()
;
alter operator family integer_ops using btree add
  operator 1 < (int8alias1, int8alias1)
;
CREATE AGGREGATE "public"."myaggp03b"(*) (
    SFUNC = "public"."stfp",
    STYPE = integer[],
    INITCOND = '{}'
)
;
VACUUM (PARALLEL 2) pg_catalog.pg_statistic
;
CREATE RULE "r4" AS
    ON DELETE TO "public"."rules_src" DO
 NOTIFY "rules_src_deletion"
;
ALTER OPERATOR <= (isbn13, isbn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE OPERATOR + (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	COMMUTATOR = +,
	PROCEDURE = intarray_push_array
)
;
CREATE VIEW pg_stat_activity AS
    SELECT
            S.datid AS datid,
            D.datname AS datname,
            S.pid,
            S.usesysid,
            U.rolname AS usename,
            S.application_name,
            S.client_addr,
            S.client_hostname,
            S.client_port,
            S.backend_start,
            S.xact_start,
            S.query_start,
            S.state_change,
            S.wait_event_type,
            S.wait_event,
            S.state,
            S.backend_xid,
            s.backend_xmin,
            S.query,
            S.backend_type
    FROM pg_stat_get_activity(NULL) AS S
        LEFT JOIN pg_database AS D ON (S.datid = D.oid)
        LEFT JOIN pg_authid AS U ON (S.usesysid = U.oid)
;
CREATE SCHEMA collate_tests
;
CREATE PROCEDURE split_prog.add_account(p_id integer, p_owner text)
LANGUAGE SQL
AS $$
    INSERT INTO split_prog.accounts(id, owner_name)
    VALUES (p_id, p_owner)
    ON CONFLICT (id) DO UPDATE SET owner_name = excluded.owner_name
$$
;
ALTER INDEX pg_toast.pg_toast_28643_index RENAME TO reind_con_toast_idx
;
/* End test case for bug #16242 */


create table alttype_cluster (a int)
;
CREATE FUNCTION cube_dim(cube)
RETURNS int4
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE "regress_indexing"."pk5" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
PARTITION BY RANGE ("a")
;
DROP TABLE public.tt
;
ALTER TABLE test_part_a_2 ADD PRIMARY KEY (b)
;
CREATE FUNCTION ll_to_earth(float8, float8)
RETURNS earth
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
AS 'SELECT cube(cube(cube(earth()*cos(radians($1))*cos(radians($2))),earth()*cos(radians($1))*sin(radians($2))),earth()*sin(radians($1)))::earth'
;
CREATE FUNCTION gbt_date_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE num_exp_mul (id1 int4, id2 int4, expected numeric(210,10))
;
-- Only parent is cleaned up.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = true,
    toast.vacuum_index_cleanup = false)
;
create type intrange as range(subtype=int)
;
ALTER TABLE atnnparted ATTACH PARTITION atnnpart1 FOR VALUES IN ('1')
;
ALTER SEQUENCE seq1 MAXVALUE 20
;
ALTER TABLE pg_toast.pg_toast_33159 RENAME TO reind_con_toast
;
ANALYZE public.spgist_text_tbl
;
create unique index on covidxpart4 (a)
;
CREATE FUNCTION crosstab3(text)
RETURNS setof tablefunc_crosstab_3
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
ALTER FOREIGN TABLE ft4 OPTIONS (ADD use_remote_estimate 'true')
;
-- test polymorphic params and defaults
create function dfunc(a anyelement, b anyelement = null, flag bool = true)
returns anyelement as $$
  select case when $3 then $1 else $2 end;
$$ language sql
;
DROP VIEW testpub_view
;
DROP DOMAIN domain4
;
CREATE TABLE part_c_100_200 (e varchar, c numeric, a text, b bigint, d int) PARTITION BY range (abs(d))
;
CREATE PROCEDURE f_print_x (x int)
AS $$
BEGIN
  RAISE NOTICE 'f_print_x(%)', x;
END
$$ LANGUAGE plpgsql
;
ALTER OPERATOR ^@> (_ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE PUBLICATION pub3 FOR TABLE tab3, tab4
;
CREATE OPERATOR <-> (
	LEFTARG = oid,
	RIGHTARG = oid,
	PROCEDURE = oid_dist,
	COMMUTATOR = '<->'
)
;
-- non-comparable types
create type testtype6 as (a int, b point)
;
VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_packages
;
create table parent (i int, c char(3))
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] CLUSTER
;
----------
[ADD_INDEX] CREATE INDEX t_idx ON t (i)
;
----------
[UNKNOWN] -- fail
CREATE OR REPLACE TRANSFORM FOR hstore LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
----------
[CREATE_PROG_OBJ] create function f1(x anyarray) returns anyelement as $$
begin
  return x[1];
end$$ language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger merge_target_table_insert_trig
  after insert on merge_target_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[DROP_TABLE] DROP TABLE table_a, table_b, jointest
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION armenian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_SEQUENCE] create temp sequence ts1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fsm_page_contents(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'fsm_page_contents'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP FUNCTION func_int_custom
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_attrdef
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER evtrg_nontemp_trig
  BEFORE INSERT ON evtrg_nontemp_table
  EXECUTE FUNCTION event_trigger_dummy_trigger()
;
----------
[ALTER_PROG_OBJ] alter operator family integer_ops using btree add
  operator 1 < (int8alias1, int8alias1)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp03b"(*) (
    SFUNC = "public"."stfp",
    STYPE = integer[],
    INITCOND = '{}'
)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_statistic
;
----------
[CREATE_POLICY] CREATE RULE "r4" AS
    ON DELETE TO "public"."rules_src" DO
 NOTIFY "rules_src_deletion"
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (isbn13, isbn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR + (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	COMMUTATOR = +,
	PROCEDURE = intarray_push_array
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_activity AS
    SELECT
            S.datid AS datid,
            D.datname AS datname,
            S.pid,
            S.usesysid,
            U.rolname AS usename,
            S.application_name,
            S.client_addr,
            S.client_hostname,
            S.client_port,
            S.backend_start,
            S.xact_start,
            S.query_start,
            S.state_change,
            S.wait_event_type,
            S.wait_event,
            S.state,
            S.backend_xid,
            s.backend_xmin,
            S.query,
            S.backend_type
    FROM pg_stat_get_activity(NULL) AS S
        LEFT JOIN pg_database AS D ON (S.datid = D.oid)
        LEFT JOIN pg_authid AS U ON (S.usesysid = U.oid)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA collate_tests
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE split_prog.add_account(p_id integer, p_owner text)
LANGUAGE SQL
AS $$
    INSERT INTO split_prog.accounts(id, owner_name)
    VALUES (p_id, p_owner)
    ON CONFLICT (id) DO UPDATE SET owner_name = excluded.owner_name
$$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28643_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] /* End test case for bug #16242 */


create table alttype_cluster (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_dim(cube)
RETURNS int4
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk5" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
PARTITION BY RANGE ("a")
;
----------
[DROP_TABLE] DROP TABLE public.tt
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE test_part_a_2 ADD PRIMARY KEY (b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ll_to_earth(float8, float8)
RETURNS earth
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
AS 'SELECT cube(cube(cube(earth()*cos(radians($1))*cos(radians($2))),earth()*cos(radians($1))*sin(radians($2))),earth()*sin(radians($1)))::earth'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_date_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_mul (id1 int4, id2 int4, expected numeric(210,10))
;
----------
[ALTER_TABLE] -- Only parent is cleaned up.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = true,
    toast.vacuum_index_cleanup = false)
;
----------
[CREATE_TYPE] create type intrange as range(subtype=int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE atnnparted ATTACH PARTITION atnnpart1 FOR VALUES IN ('1')
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE seq1 MAXVALUE 20
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33159 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] ANALYZE public.spgist_text_tbl
;
----------
[ADD_INDEX] create unique index on covidxpart4 (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION crosstab3(text)
RETURNS setof tablefunc_crosstab_3
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE ft4 OPTIONS (ADD use_remote_estimate 'true')
;
----------
[CREATE_PROG_OBJ] -- test polymorphic params and defaults
create function dfunc(a anyelement, b anyelement = null, flag bool = true)
returns anyelement as $$
  select case when $3 then $1 else $2 end;
$$ language sql
;
----------
[DROP_VIEW] DROP VIEW testpub_view
;
----------
[DROP_TYPE] DROP DOMAIN domain4
;
----------
[CREATE_TABLE] CREATE TABLE part_c_100_200 (e varchar, c numeric, a text, b bigint, d int) PARTITION BY range (abs(d))
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE f_print_x (x int)
AS $$
BEGIN
  RAISE NOTICE 'f_print_x(%)', x;
END
$$ LANGUAGE plpgsql
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^@> (_ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub3 FOR TABLE tab3, tab4
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = oid,
	RIGHTARG = oid,
	PROCEDURE = oid_dist,
	COMMUTATOR = '<->'
)
;
----------
[CREATE_TYPE] -- non-comparable types
create type testtype6 as (a int, b point)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_packages
;
----------
[CREATE_TABLE] create table parent (i int, c char(3))
;
