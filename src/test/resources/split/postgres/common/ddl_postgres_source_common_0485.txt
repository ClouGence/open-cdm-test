CREATE VIEW atest12sbv WITH (security_barrier=true) AS
  SELECT * FROM atest12 WHERE b <<< 5
;
create temp table snapshot_test (
	nr	integer,
	snap	txid_snapshot
)
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "numword" WITH "simple"
;
-- Leave around some objects for other tests
create table trigger_parted (a int primary key) partition by list (a)
;
CREATE SEQUENCE foo_seq
;
create table bool_rp_true_1k partition of bool_rp for values from (true,0) to (true,1000)
;
ALTER TABLESPACE split_ts_space RESET (random_page_cost, effective_io_concurrency)
;
alter table pp1 add column a1 int check (a1 > 0)
;
create temp table child (
    bid int not null primary key,
    aid int not null,
    val1 text)
;
-- fail
TRUNCATE TABLE truncate_a CASCADE
;
ALTER TABLE part ATTACH PARTITION part_rev FOR VALUES IN (3)
;
CREATE TYPE "public"."arrayrange" AS RANGE (
    subtype = integer[],
    multirange_type_name = "public"."arraymultirange"
)
;
-- multi-way join involving multiple merge joins
-- (this case used to have EPQ-related planning problems)
CREATE TABLE local_tbl (c1 int NOT NULL, c2 int NOT NULL, c3 text, CONSTRAINT local_tbl_pkey PRIMARY KEY (c1))
;
CREATE AGGREGATE testagg9(int2) (SFUNC = int2_sum, STYPE = int8)
;
CREATE TABLE part1 PARTITION OF part FOR VALUES FROM (1) to (100)
;
/*
 * 5.46
 * ROLE_UDT_GRANTS view
 */

CREATE VIEW role_udt_grants AS
    SELECT grantor,
           grantee,
           udt_catalog,
           udt_schema,
           udt_name,
           privilege_type,
           is_grantable
    FROM udt_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
-- this is the obvious (and wrong...) way to compare rows
CREATE FUNCTION mytrigger() RETURNS trigger LANGUAGE plpgsql as $$
begin
	if row(old.*) = row(new.*) then
		raise notice 'row % not changed', new.f1;
	else
		raise notice 'row % changed', new.f1;
	end if;
	return new;
end$$
;
CREATE TABLE "public"."idxpart_another" (
    "a" integer NOT NULL,
    "b" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
CREATE TABLE J1_TBL (
  i integer,
  j integer,
  t text
)
;
ALTER OPERATOR <@ (hstore, hstore)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "hword_part" WITH "english_stem"
;
COMMENT ON INDEX "public"."comment_test_child_fk" IS 'Index backing the FOREIGN KEY of comment_test_child'
;
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (1) TO (1)
;
CREATE VIEW pg_backend_memory_contexts AS
    SELECT * FROM pg_get_backend_memory_contexts()
;
DROP TABLE test11b
;
CREATE TABLE fail_part PARTITION OF list_parted2 FOR VALUES IN ('b', 'c')
;
create table fkpart1.fk_part_1_2 partition of fkpart1.fk_part_1 for values in (2)
;
CREATE TABLE stream_test1(data text)
;
ALTER TYPE bogus RENAME TO bogon
;
-- with inferred PK on the referenced table, and wrong column type:
ALTER TABLE temporal_fk_rng2rng
  DROP CONSTRAINT temporal_fk_rng2rng_fk,
  ALTER COLUMN valid_at TYPE tsrange USING tsrange(lower(valid_at), upper(valid_at))
;
drop index expr_part_comp_key_index
;
create table rp_prefix_test1_p1 partition of rp_prefix_test1 for values from (1, 'a') to (1, 'b')
;
DROP OPERATOR !==(bigint, bigint)
;
CREATE PUBLICATION split_pub FOR TABLE split_prog.accounts WITH (publish = 'insert, update, delete, truncate')
;
ANALYZE pg_temp_9.pg_temp_26520
;
ALTER TABLE pg_temp_9.pg_temp_38052_2 ADD COLUMN newdata pg_temp_9.pg_temp_38052
;
-- check calling logical decoding from pl/pgsql
CREATE FUNCTION slot_changes_wrapper(slot_name name) RETURNS SETOF TEXT AS $$
BEGIN
  RETURN QUERY
    SELECT data FROM pg_logical_slot_peek_changes(slot_name, NULL, NULL, 'include-xids', '0', 'skip-empty-xacts', '1');
END$$ LANGUAGE plpgsql
;
-- ======================================================

-- More column list tests for validating column references
CREATE TABLE rf_tbl_abcd_nopk(a int, b int, c int, d int)
;
CREATE UNIQUE INDEX t2_btree_unique ON s3.t2 USING BTREE (i)
;
create index gist_pointidx2 on gist_point_tbl using gist(p) with (buffering = on, fillfactor=50)
;
-- Create various object types that need extra handling by SET SCHEMA.

CREATE TABLE ess_table (f1 int primary key, f2 int, f3 text,
                        constraint ess_c check (f1 != f2))
;
ANALYZE public.covidxpart3
;
ANALYZE bitmap_split_or
;
alter table only renameColumnChild rename column a to d
;
CREATE SCHEMA newschema6 AUTHORIZATION PUBLIC
;
CREATE FUNCTION gbt_int4_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
alter table invalid_check_con_child drop constraint inh_check_constraint
;
CREATE VIEW mvtest_vt2 AS SELECT moo, 2*moo FROM mvtest_vt1 UNION ALL SELECT moo, 3*moo FROM mvtest_vt1
;
CREATE FUNCTION test_resowner_remember_between_phases()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE TABLE s1.t1(id integer)
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW atest12sbv WITH (security_barrier=true) AS
  SELECT * FROM atest12 WHERE b <<< 5
;
----------
[CREATE_TABLE] create temp table snapshot_test (
	nr	integer,
	snap	txid_snapshot
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "numword" WITH "simple"
;
----------
[CREATE_TABLE] -- Leave around some objects for other tests
create table trigger_parted (a int primary key) partition by list (a)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE foo_seq
;
----------
[CREATE_TABLE] create table bool_rp_true_1k partition of bool_rp for values from (true,0) to (true,1000)
;
----------
[ALTER_TABLESPACE] ALTER TABLESPACE split_ts_space RESET (random_page_cost, effective_io_concurrency)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table pp1 add column a1 int check (a1 > 0)
;
----------
[CREATE_TABLE] create temp table child (
    bid int not null primary key,
    aid int not null,
    val1 text)
;
----------
[TRUNCATE_TABLE] -- fail
TRUNCATE TABLE truncate_a CASCADE
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part ATTACH PARTITION part_rev FOR VALUES IN (3)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."arrayrange" AS RANGE (
    subtype = integer[],
    multirange_type_name = "public"."arraymultirange"
)
;
----------
[CREATE_TABLE] -- multi-way join involving multiple merge joins
-- (this case used to have EPQ-related planning problems)
CREATE TABLE local_tbl (c1 int NOT NULL, c2 int NOT NULL, c3 text, CONSTRAINT local_tbl_pkey PRIMARY KEY (c1))
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testagg9(int2) (SFUNC = int2_sum, STYPE = int8)
;
----------
[CREATE_TABLE] CREATE TABLE part1 PARTITION OF part FOR VALUES FROM (1) to (100)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 5.46
 * ROLE_UDT_GRANTS view
 */

CREATE VIEW role_udt_grants AS
    SELECT grantor,
           grantee,
           udt_catalog,
           udt_schema,
           udt_name,
           privilege_type,
           is_grantable
    FROM udt_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[CREATE_PROG_OBJ] -- this is the obvious (and wrong...) way to compare rows
CREATE FUNCTION mytrigger() RETURNS trigger LANGUAGE plpgsql as $$
begin
	if row(old.*) = row(new.*) then
		raise notice 'row % not changed', new.f1;
	else
		raise notice 'row % changed', new.f1;
	end if;
	return new;
end$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart_another" (
    "a" integer NOT NULL,
    "b" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_TABLE] CREATE TABLE J1_TBL (
  i integer,
  j integer,
  t text
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <@ (hstore, hstore)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "hword_part" WITH "english_stem"
;
----------
[COMMENT_INDEX] COMMENT ON INDEX "public"."comment_test_child_fk" IS 'Index backing the FOREIGN KEY of comment_test_child'
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (1) TO (1)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_backend_memory_contexts AS
    SELECT * FROM pg_get_backend_memory_contexts()
;
----------
[DROP_TABLE] DROP TABLE test11b
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF list_parted2 FOR VALUES IN ('b', 'c')
;
----------
[CREATE_TABLE] create table fkpart1.fk_part_1_2 partition of fkpart1.fk_part_1 for values in (2)
;
----------
[CREATE_TABLE] CREATE TABLE stream_test1(data text)
;
----------
[RENAME_TYPE] ALTER TYPE bogus RENAME TO bogon
;
----------
[ALTER_TABLE|DROP_CONSTRAINT|ALTER_COLUMN] -- with inferred PK on the referenced table, and wrong column type:
ALTER TABLE temporal_fk_rng2rng
  DROP CONSTRAINT temporal_fk_rng2rng_fk,
  ALTER COLUMN valid_at TYPE tsrange USING tsrange(lower(valid_at), upper(valid_at))
;
----------
[DROP_INDEX] drop index expr_part_comp_key_index
;
----------
[CREATE_TABLE] create table rp_prefix_test1_p1 partition of rp_prefix_test1 for values from (1, 'a') to (1, 'b')
;
----------
[DROP_PROG_OBJ] DROP OPERATOR !==(bigint, bigint)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION split_pub FOR TABLE split_prog.accounts WITH (publish = 'insert, update, delete, truncate')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26520
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_38052_2 ADD COLUMN newdata pg_temp_9.pg_temp_38052
;
----------
[CREATE_PROG_OBJ] -- check calling logical decoding from pl/pgsql
CREATE FUNCTION slot_changes_wrapper(slot_name name) RETURNS SETOF TEXT AS $$
BEGIN
  RETURN QUERY
    SELECT data FROM pg_logical_slot_peek_changes(slot_name, NULL, NULL, 'include-xids', '0', 'skip-empty-xacts', '1');
END$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] -- ======================================================

-- More column list tests for validating column references
CREATE TABLE rf_tbl_abcd_nopk(a int, b int, c int, d int)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t2_btree_unique ON s3.t2 USING BTREE (i)
;
----------
[ADD_INDEX] create index gist_pointidx2 on gist_point_tbl using gist(p) with (buffering = on, fillfactor=50)
;
----------
[CREATE_TABLE] -- Create various object types that need extra handling by SET SCHEMA.

CREATE TABLE ess_table (f1 int primary key, f2 int, f3 text,
                        constraint ess_c check (f1 != f2))
;
----------
[ADMIN_TABLE] ANALYZE public.covidxpart3
;
----------
[ADMIN_TABLE] ANALYZE bitmap_split_or
;
----------
[RENAME_COLUMN] alter table only renameColumnChild rename column a to d
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema6 AUTHORIZATION PUBLIC
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int4_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table invalid_check_con_child drop constraint inh_check_constraint
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mvtest_vt2 AS SELECT moo, 2*moo FROM mvtest_vt1 UNION ALL SELECT moo, 3*moo FROM mvtest_vt1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_resowner_remember_between_phases()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE s1.t1(id integer)
;
