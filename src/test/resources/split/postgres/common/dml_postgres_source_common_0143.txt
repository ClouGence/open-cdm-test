UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted_def1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2811', relminmxid = '1'
WHERE oid = '"public"."extra_wide_table"'::pg_catalog.regclass
;
INSERT INTO fewmore VALUES(1) RETURNING generate_series(1,3)
;
INSERT INTO public.tht VALUES ('3', 263)
;
UPDATE parent SET aux = 'bar'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."itest7"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('5', 865)
;
INSERT INTO parent_tbl VALUES ('AAA', 42)
;
UPDATE target_pa t SET balance = balance + 10, val = t.val || ' updated by update1_pa' WHERE t.key = 1
;
INSERT INTO public.ths_p3 VALUES (5, 435)
;
INSERT INTO num_exp_sub VALUES (7,8,'-83103366')
;
DELETE FROM public.sro_index_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_4.pg_temp_27293_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
UPDATE arrtest SET a[4] = NULL WHERE a[2] IS NULL
;
INSERT INTO pg_description  SELECT t.objoid, c.oid, t.objsubid, t.description   FROM tmp_pg_description t, pg_class c     WHERE c.relname = t.classname
;
INSERT INTO sql_implementation_info VALUES ('10003', 'CATALOG NAME', NULL, 'Y', NULL)
;
-- ok
INSERT INTO gtest1v VALUES (6, 66), (7, 77)
;
DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_14.pg_temp_26714_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13128', relminmxid = '3'
WHERE oid = '33617'
;
UPDATE ONLY "public"."fktable" SET "fk_id_del_set_null" = NULL WHERE $1 OPERATOR(pg_catalog.=) "tid" AND $2 OPERATOR(pg_catalog.=) "fk_id_del_set_null"
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3176', relminmxid = '1'
WHERE oid = '"public"."tbl_include_unique1"'::pg_catalog.regclass
;
INSERT INTO trans_abc VALUES (10)
;
UPDATE ONLY "fkpart11"."fk_another" SET "a" = $1 WHERE $2 OPERATOR(pg_catalog.=) "a"
;
INSERT INTO FLOAT4_TBL(f1) VALUES ('10e400')
;
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted2"'::pg_catalog.regclass
  AND attname = v.dname
;
insert into t(a, b) select i / 10, i from generate_series(1, 1000) n(i)
;
INSERT INTO public.tht VALUES ('1', 681)
;
INSERT INTO newly_created_in_ts VALUES (1, 'row for new table')
;
INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_amop
;
INSERT INTO prt2_n SELECT i, i, to_char(i/50, 'FM0000') FROM generate_series(0, 599, 2) i
;
INSERT INTO temporal3 (id, valid_at) VALUES ('[1,2)', daterange('2010-01-01', '2020-01-01')) ON CONFLICT ON CONSTRAINT temporal3_uq DO NOTHING
;
INSERT INTO plt1_adv SELECT i, i, to_char(i % 10, 'FM0000') FROM generate_series(1, 299) i WHERE i % 10 IN (1, 3)
;
INSERT INTO public.tplain VALUES ('1', 211)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15528', relminmxid = '3'
WHERE oid = '"public"."transition_table_level2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14690', relminmxid = '3'
WHERE oid = '"public"."idxpart21"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('7', 287)
;
DELETE FROM public.order_summary mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_11.pg_temp_41153_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8777', relminmxid = '1'
WHERE oid = '28082'
;
INSERT INTO public.mvtest_mv_foo SELECT (diff.newdata).* FROM pg_temp_11.pg_temp_26571_2 diff WHERE tid IS NULL
;
update ft2 set c2 = 42 where c2 = 0
;
update shipped_view set value = 11
    from int4_tbl a join int4_tbl b
      on (a.f1 = (select f1 from int4_tbl c where c.f1=b.f1))
    where ordnum = a.f1
;
DELETE FROM target_tg t WHERE t.key = 1
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14574', relminmxid = '3'
WHERE oid = '36549'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."itest5"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6265', relminmxid = '1'
WHERE oid = '23685'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15859', relminmxid = '3'
WHERE oid = '"public"."big5_inputs"'::pg_catalog.regclass
;
UPDATE testpub_tbl6 SET a = 1
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."plt1_p2"'::pg_catalog.regclass
;
INSERT INTO CHAR_TBL (f1) VALUES ('1')
;
INSERT INTO public.tplain VALUES ('9', 69)
;
INSERT INTO collate_test1 VALUES (1, 'abc'), (2, '�bc'), (3, 'bbc'), (4, 'ABC')
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted_def1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2811', relminmxid = '1'
WHERE oid = '"public"."extra_wide_table"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO fewmore VALUES(1) RETURNING generate_series(1,3)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('3', 263)
;
----------
[UPDATE] UPDATE parent SET aux = 'bar'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."itest7"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('5', 865)
;
----------
[INSERT] INSERT INTO parent_tbl VALUES ('AAA', 42)
;
----------
[UPDATE] UPDATE target_pa t SET balance = balance + 10, val = t.val || ' updated by update1_pa' WHERE t.key = 1
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 435)
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (7,8,'-83103366')
;
----------
[DELETE|SELECT] DELETE FROM public.sro_index_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_4.pg_temp_27293_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE] UPDATE arrtest SET a[4] = NULL WHERE a[2] IS NULL
;
----------
[INSERT|SELECT] INSERT INTO pg_description  SELECT t.objoid, c.oid, t.objsubid, t.description   FROM tmp_pg_description t, pg_class c     WHERE c.relname = t.classname
;
----------
[INSERT] INSERT INTO sql_implementation_info VALUES ('10003', 'CATALOG NAME', NULL, 'Y', NULL)
;
----------
[INSERT] -- ok
INSERT INTO gtest1v VALUES (6, 66), (7, 77)
;
----------
[DELETE|SELECT] DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_14.pg_temp_26714_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13128', relminmxid = '3'
WHERE oid = '33617'
;
----------
[UPDATE] UPDATE ONLY "public"."fktable" SET "fk_id_del_set_null" = NULL WHERE $1 OPERATOR(pg_catalog.=) "tid" AND $2 OPERATOR(pg_catalog.=) "fk_id_del_set_null"
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3176', relminmxid = '1'
WHERE oid = '"public"."tbl_include_unique1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO trans_abc VALUES (10)
;
----------
[UPDATE] UPDATE ONLY "fkpart11"."fk_another" SET "a" = $1 WHERE $2 OPERATOR(pg_catalog.=) "a"
;
----------
[INSERT|SELECT] INSERT INTO FLOAT4_TBL(f1) VALUES ('10e400')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted2"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT|SELECT] insert into t(a, b) select i / 10, i from generate_series(1, 1000) n(i)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 681)
;
----------
[INSERT] INSERT INTO newly_created_in_ts VALUES (1, 'row for new table')
;
----------
[INSERT|SELECT] INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_amop
;
----------
[INSERT|SELECT] INSERT INTO prt2_n SELECT i, i, to_char(i/50, 'FM0000') FROM generate_series(0, 599, 2) i
;
----------
[MERGE|SELECT] INSERT INTO temporal3 (id, valid_at) VALUES ('[1,2)', daterange('2010-01-01', '2020-01-01')) ON CONFLICT ON CONSTRAINT temporal3_uq DO NOTHING
;
----------
[INSERT|SELECT] INSERT INTO plt1_adv SELECT i, i, to_char(i % 10, 'FM0000') FROM generate_series(1, 299) i WHERE i % 10 IN (1, 3)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 211)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15528', relminmxid = '3'
WHERE oid = '"public"."transition_table_level2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14690', relminmxid = '3'
WHERE oid = '"public"."idxpart21"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('7', 287)
;
----------
[DELETE|SELECT] DELETE FROM public.order_summary mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_11.pg_temp_41153_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8777', relminmxid = '1'
WHERE oid = '28082'
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_mv_foo SELECT (diff.newdata).* FROM pg_temp_11.pg_temp_26571_2 diff WHERE tid IS NULL
;
----------
[UPDATE] update ft2 set c2 = 42 where c2 = 0
;
----------
[UPDATE|SELECT] update shipped_view set value = 11
    from int4_tbl a join int4_tbl b
      on (a.f1 = (select f1 from int4_tbl c where c.f1=b.f1))
    where ordnum = a.f1
;
----------
[DELETE] DELETE FROM target_tg t WHERE t.key = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14574', relminmxid = '3'
WHERE oid = '36549'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."itest5"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6265', relminmxid = '1'
WHERE oid = '23685'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '15859', relminmxid = '3'
WHERE oid = '"public"."big5_inputs"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE testpub_tbl6 SET a = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."plt1_p2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO CHAR_TBL (f1) VALUES ('1')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 69)
;
----------
[INSERT] INSERT INTO collate_test1 VALUES (1, 'abc'), (2, '�bc'), (3, 'bbc'), (4, 'ABC')
;
