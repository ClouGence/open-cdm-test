ALTER TABLE taby ADD PRIMARY KEY (id)
;
CREATE TABLE pq_pipeline_tst (id int)
;
CREATE POLICY p2 ON s2 USING (x in (select a from s1 where b like '%22%'))
;
CREATE TRIGGER trig_stmt_before
	BEFORE DELETE OR INSERT OR UPDATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
ANALYZE vacowned_parted
;
CREATE TABLE CHAR_TBL(f1 char)
;
COMMENT ON OPERATOR ###### (int4, NONE) IS 'bad right unary'
;
ALTER PUBLICATION pub_empty RENAME TO pub_for_all_tables
;
create rule "_RETURN" as on select to rules_fooview_part do instead
  select 1 as x, 'aaa'::text as y
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c7 OPTIONS (ADD p1 'v1', ADD p2 'v2'),
                        ALTER COLUMN c8 OPTIONS (ADD p1 'v1', ADD p2 'v2')
;
DROP TABLE IF EXISTS users, orders
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub_mix_3, pub_mix_4
;
ALTER TABLE constraint_rename_test RENAME CONSTRAINT con2foo TO con2bar
;
ALTER TABLE rls_tbl_force FORCE ROW LEVEL SECURITY
;
comment on index at_partitioned_1_id_name_key is 'child 1 index'
;
CREATE INDEX t_b_c_idx ON bitmap_split_or (b, c)
;
-- Triggers on leaf partition part_d_1_15
CREATE TRIGGER d1_delete_trig
  AFTER DELETE ON part_d_1_15 for each statement execute procedure trigfunc()
;
DROP TABLE serializable_update_tab
;
--CREATE OPERATOR = (
--	LEFTARG = _int4,
--	RIGHTARG = _int4,
--	PROCEDURE = _int_same,
--	COMMUTATOR = '=',
--	NEGATOR = '<>',
--	RESTRICT = eqsel,
--	JOIN = eqjoinsel,
--	SORT1 = '<',
--	SORT2 = '<'
--);

--CREATE OPERATOR <> (
--	LEFTARG = _int4,
--	RIGHTARG = _int4,
--	PROCEDURE = _int_different,
--	COMMUTATOR = '<>',
--	NEGATOR = '=',
--	RESTRICT = neqsel,
--	JOIN = neqjoinsel
--);

CREATE OPERATOR @> (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contains,
	COMMUTATOR = '<@',
	RESTRICT = _int_contains_sel,
	JOIN = _int_contains_joinsel
)
;
create function pslot_slotlink_view(bpchar)
returns text as '
declare
    psrec	record;
    sltype	char(2);
    retval	text;
begin
    select into psrec * from PSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if psrec.slotlink = '''' then
        return ''-'';
    end if;
    sltype := substr(psrec.slotlink, 1, 2);
    if sltype = ''PS'' then
	retval := trim(psrec.slotlink) || '' -> '';
	return retval || pslot_backlink_view(psrec.slotlink);
    end if;
    if sltype = ''HS'' then
        retval := comment from Hub H, HSlot HS
			where HS.slotname = psrec.slotlink
			  and H.name = HS.hubname;
        retval := retval || '' slot '';
	retval := retval || slotno::text from HSlot
			where slotname = psrec.slotlink;
	return retval;
    end if;
    return psrec.slotlink;
end;
' language plpgsql
;
--
-- gist_page_items_bytea()
--
CREATE FUNCTION gist_page_items_bytea(IN page bytea,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT dead boolean,
    OUT key_data bytea)
RETURNS SETOF record
AS '$libdir/pageinspect', 'gist_page_items_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE COLLATION mycoll2 ( LC_COLLATE = "POSIX", LC_CTYPE = "POSIX" )
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_sequence
;
DROP SCHEMA sts_sch1, sts_sch2 CASCADE
;
-- This triggers an update of pg_index.indisreplident for parted_replica_idx.
alter table only parted_replica_tab_1 replica identity
  using index parted_replica_idx_1
;
-- It's alright though, if no partitions are yet created
CREATE TABLE parted_no_parts (a int) PARTITION BY LIST (a)
;
ALTER TABLE ONLY "public"."gtest23q"
    ADD CONSTRAINT "gtest23q_b_fkey" FOREIGN KEY ("b") REFERENCES "public"."gtest23p"("y")
;
ALTER INDEX pg_toast.pg_toast_27632_index RENAME TO reind_con_toast_idx
;
IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest1
;
DROP TYPE mood
;
ALTER TABLE ctlt1 ADD CONSTRAINT cc CHECK (length(b) > 100) NOT VALID
;
cluster clstr_4 using cluster_sort
;
CALL overload('A')
;
--
-- Test the interaction between transition tables and both kinds of
-- inheritance.  We'll dump the contents of the transition tables in a
-- format that shows the attribute order, so that we can distinguish
-- tuple formats (though not dropped attributes).
--

create or replace function dump_insert() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, new table = %',
                 TG_NAME,
                 (select string_agg(new_table::text, ', ' order by a) from new_table);
    return null;
  end;
$$
;
create index on gexec_test(b)
;
-- Test case for partition hierarchy
CREATE TABLE parent_tbl (a text, b int) PARTITION BY LIST (a)
;
CREATE FUNCTION strict_word_similarity_dist_op(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP TABLE pred_parent, pred_child
;
VACUUM (FREEZE) does_not_exist, vaccluster
;
-- rename back to keep the rest simple
ALTER PUBLICATION testpub_foo RENAME TO testpub_default
;
CREATE TABLE r2 (a int REFERENCES r1 ON UPDATE CASCADE)
;
CREATE TABLE test12fk (a int, b text COLLATE case_insensitive REFERENCES test12pk (x) ON UPDATE CASCADE)
;
DROP PUBLICATION pub2
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_proc
;
CREATE TABLESPACE regress_badspace LOCATION '/no/such/location'
;
ALTER TABLE split_alter_table.base_table ADD CONSTRAINT base_table_code_unique UNIQUE USING INDEX base_table_code_idx
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "word" WITH "public"."ispell", "english_stem"
;
-- omit COLUMN
ALTER TABLE test_add_column
	ADD IF NOT EXISTS c6 integer
;
CREATE TRIGGER before_ins_stmt_trig BEFORE INSERT ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('before_view_ins_stmt')
;
CREATE TEXT SEARCH CONFIGURATION finnish
	(PARSER = default)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE taby ADD PRIMARY KEY (id)
;
----------
[CREATE_TABLE] CREATE TABLE pq_pipeline_tst (id int)
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON s2 USING (x in (select a from s1 where b like '%22%'))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_stmt_before
	BEFORE DELETE OR INSERT OR UPDATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
----------
[ADMIN_TABLE] ANALYZE vacowned_parted
;
----------
[CREATE_TABLE] CREATE TABLE CHAR_TBL(f1 char)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON OPERATOR ###### (int4, NONE) IS 'bad right unary'
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub_empty RENAME TO pub_for_all_tables
;
----------
[CREATE_POLICY] create rule "_RETURN" as on select to rules_fooview_part do instead
  select 1 as x, 'aaa'::text as y
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c7 OPTIONS (ADD p1 'v1', ADD p2 'v2'),
                        ALTER COLUMN c8 OPTIONS (ADD p1 'v1', ADD p2 'v2')
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS users, orders
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub_mix_3, pub_mix_4
;
----------
[RENAME_CONSTRAINT] ALTER TABLE constraint_rename_test RENAME CONSTRAINT con2foo TO con2bar
;
----------
[ALTER_TABLE] ALTER TABLE rls_tbl_force FORCE ROW LEVEL SECURITY
;
----------
[COMMENT_INDEX] comment on index at_partitioned_1_id_name_key is 'child 1 index'
;
----------
[ADD_INDEX] CREATE INDEX t_b_c_idx ON bitmap_split_or (b, c)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Triggers on leaf partition part_d_1_15
CREATE TRIGGER d1_delete_trig
  AFTER DELETE ON part_d_1_15 for each statement execute procedure trigfunc()
;
----------
[DROP_TABLE] DROP TABLE serializable_update_tab
;
----------
[CREATE_PROG_OBJ] --CREATE OPERATOR = (
--	LEFTARG = _int4,
--	RIGHTARG = _int4,
--	PROCEDURE = _int_same,
--	COMMUTATOR = '=',
--	NEGATOR = '<>',
--	RESTRICT = eqsel,
--	JOIN = eqjoinsel,
--	SORT1 = '<',
--	SORT2 = '<'
--);

--CREATE OPERATOR <> (
--	LEFTARG = _int4,
--	RIGHTARG = _int4,
--	PROCEDURE = _int_different,
--	COMMUTATOR = '<>',
--	NEGATOR = '=',
--	RESTRICT = neqsel,
--	JOIN = neqjoinsel
--);

CREATE OPERATOR @> (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contains,
	COMMUTATOR = '<@',
	RESTRICT = _int_contains_sel,
	JOIN = _int_contains_joinsel
)
;
----------
[CREATE_PROG_OBJ] create function pslot_slotlink_view(bpchar)
returns text as '
declare
    psrec	record;
    sltype	char(2);
    retval	text;
begin
    select into psrec * from PSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if psrec.slotlink = '''' then
        return ''-'';
    end if;
    sltype := substr(psrec.slotlink, 1, 2);
    if sltype = ''PS'' then
	retval := trim(psrec.slotlink) || '' -> '';
	return retval || pslot_backlink_view(psrec.slotlink);
    end if;
    if sltype = ''HS'' then
        retval := comment from Hub H, HSlot HS
			where HS.slotname = psrec.slotlink
			  and H.name = HS.hubname;
        retval := retval || '' slot '';
	retval := retval || slotno::text from HSlot
			where slotname = psrec.slotlink;
	return retval;
    end if;
    return psrec.slotlink;
end;
' language plpgsql
;
----------
[CREATE_PROG_OBJ] --
-- gist_page_items_bytea()
--
CREATE FUNCTION gist_page_items_bytea(IN page bytea,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT dead boolean,
    OUT key_data bytea)
RETURNS SETOF record
AS '$libdir/pageinspect', 'gist_page_items_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE COLLATION mycoll2 ( LC_COLLATE = "POSIX", LC_CTYPE = "POSIX" )
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_sequence
;
----------
[DROP_SCHEMA] DROP SCHEMA sts_sch1, sts_sch2 CASCADE
;
----------
[ALTER_TABLE] -- This triggers an update of pg_index.indisreplident for parted_replica_idx.
alter table only parted_replica_tab_1 replica identity
  using index parted_replica_idx_1
;
----------
[CREATE_TABLE] -- It's alright though, if no partitions are yet created
CREATE TABLE parted_no_parts (a int) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest23q"
    ADD CONSTRAINT "gtest23q_b_fkey" FOREIGN KEY ("b") REFERENCES "public"."gtest23p"("y")
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27632_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest1
;
----------
[DROP_TYPE] DROP TYPE mood
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ctlt1 ADD CONSTRAINT cc CHECK (length(b) > 100) NOT VALID
;
----------
[ADMIN_TABLE] cluster clstr_4 using cluster_sort
;
----------
[CALL_PROG_OBJ] CALL overload('A')
;
----------
[CREATE_PROG_OBJ] --
-- Test the interaction between transition tables and both kinds of
-- inheritance.  We'll dump the contents of the transition tables in a
-- format that shows the attribute order, so that we can distinguish
-- tuple formats (though not dropped attributes).
--

create or replace function dump_insert() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, new table = %',
                 TG_NAME,
                 (select string_agg(new_table::text, ', ' order by a) from new_table);
    return null;
  end;
$$
;
----------
[ADD_INDEX] create index on gexec_test(b)
;
----------
[CREATE_TABLE] -- Test case for partition hierarchy
CREATE TABLE parent_tbl (a text, b int) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION strict_word_similarity_dist_op(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE pred_parent, pred_child
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) does_not_exist, vaccluster
;
----------
[ALTER_PUB_SUB] -- rename back to keep the rest simple
ALTER PUBLICATION testpub_foo RENAME TO testpub_default
;
----------
[CREATE_TABLE] CREATE TABLE r2 (a int REFERENCES r1 ON UPDATE CASCADE)
;
----------
[CREATE_TABLE] CREATE TABLE test12fk (a int, b text COLLATE case_insensitive REFERENCES test12pk (x) ON UPDATE CASCADE)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION pub2
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_proc
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_badspace LOCATION '/no/such/location'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE split_alter_table.base_table ADD CONSTRAINT base_table_code_unique UNIQUE USING INDEX base_table_code_idx
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "word" WITH "public"."ispell", "english_stem"
;
----------
[ALTER_TABLE|ADD_COLUMN] -- omit COLUMN
ALTER TABLE test_add_column
	ADD IF NOT EXISTS c6 integer
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER before_ins_stmt_trig BEFORE INSERT ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('before_view_ins_stmt')
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION finnish
	(PARSER = default)
;
