ANALYZE pg_temp_9.pg_temp_37969_2
;
CREATE TABLE bigintp_10 PARTITION OF bigintp FOR VALUES IN (10)
;
CREATE FUNCTION regexp_match( citext, citext ) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
alter table fkpart2.fk_part_1 drop constraint fkey
;
create table depth2() inherits (depth1)
;
CREATE FUNCTION voidtest2(a int, b int) RETURNS VOID LANGUAGE SQL AS
$$ SELECT voidtest1(a + b) $$
;
create aggregate my_sum_init(int4)
(
   stype = avg_state,
   sfunc = avg_transfn,
   finalfunc = sum_finalfn,
   initcond = '(10,0)'
)
;
CREATE TABLE mchash (a int, b text, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_text_ops)
;
-- Short format:

CREATE FUNCTION isbn_in(cstring)
	RETURNS isbn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE OR REPLACE FUNCTION ctoast_large_val() RETURNS TEXT LANGUAGE SQL AS 'select array_agg(md5(g::text))::text from generate_series(1, 256) g'
;
-- fails
ALTER TABLE pitest2_p1 ALTER COLUMN f3 SET INCREMENT BY 2
;
ALTER TABLE pg_temp_13.pg_temp_25980_2 ADD COLUMN newdata pg_temp_13.pg_temp_25980
;
CREATE TABLE colors (
	id INT GENERATED BY DEFAULT AS IDENTITY (INCREMENT BY 3 START WITH 7),
	name VARCHAR(10)
)
;
CREATE OPERATOR ~~ (
    PROCEDURE = texticlike,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = !~~,
    RESTRICT  = iclikesel,
    JOIN      = iclikejoinsel
)
;
CREATE INDEX idx_postgis_places_geom_3857_gist ON split_postgis.places USING gist (ST_Transform(geom, 3857))
;
DROP DATABASE lakh
;
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key2 CHECK(c > 6)
;
CREATE INDEX "idx" ON "public"."cmdata" USING "btree" ("f1")
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_subscription_rel
;
ALTER TABLE functional_dependencies ALTER COLUMN c TYPE numeric
;
CREATE INDEX ON syscol_table (ctid)
;
DROP TABLE atestc
;
-- Simple ALTER INDEX
alter index btree_idx1 set (vacuum_cleanup_index_scale_factor = 70.0)
;
--
-- Verify that EXCLUDED does not allow system column references. These
-- do not make sense because EXCLUDED isn't an already stored tuple
-- (and thus doesn't have a ctid etc).
--
create table syscolconflicttest(key int4, data text)
;
CREATE TABLE transition_table_level2
(
      level2_no serial NOT NULL ,
      parent_no int NOT NULL,
      level1_node_name varchar(255),
       PRIMARY KEY (level2_no)
) WITHOUT OIDS
;
ALTER INDEX pg_toast.pg_toast_30496_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION dblink_close (text)
RETURNS text
AS '$libdir/dblink','dblink_close'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
create temp table outer_7597 (f1 int4, f2 int4)
;
CREATE FUNCTION _ltree_r_risparent(ltree,_ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE issue_changes_copy (
    uuid, kee,
    issue_key,
    user_login,
    change_type,
    change_data,
    created_at,
    updated_at,
    issue_change_creation_date
)
AS (
    SELECT CAST (id AS VARCHAR (40)) AS uuid, kee, issue_key, user_login, change_type, change_data, created_at, updated_at, issue_change_creation_date FROM issue_changes)
;
/*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
DROP OPERATOR IF EXISTS split_operator_probe_next.## (integer, integer), split_operator_probe.!! (NONE, integer), split_operator_probe.=== (integer, integer) RESTRICT
;
CREATE TABLE "public"."pht1_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
ALTER TABLE t1 DROP COLUMN junk1
;
CREATE FUNCTION "public"."int8alias1lt"("public"."int8alias1", "public"."int8alias1") RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8lt$$
;
/*
 * 5.10
 * ASSERTIONS view
 */

-- feature not supported


/*
 * 5.11
 * ATTRIBUTES view
 */

CREATE VIEW attributes AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nc.nspname AS sql_identifier) AS udt_schema,
           CAST(c.relname AS sql_identifier) AS udt_name,
           CAST(a.attname AS sql_identifier) AS attribute_name,
           CAST(a.attnum AS cardinal_number) AS ordinal_position,
           CAST(pg_get_expr(ad.adbin, ad.adrelid) AS character_data) AS attribute_default,
           CAST(CASE WHEN a.attnotnull OR (t.typtype = 'd' AND t.typnotnull) THEN 'NO' ELSE 'YES' END
             AS yes_or_no)
             AS is_nullable, -- This column was apparently removed between SQL:2003 and SQL:2008.

           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nt.nspname = 'pg_catalog' THEN format_type(a.atttypid, null)
                  ELSE 'USER-DEFINED' END
             AS character_data)
             AS data_type,

           CAST(
             _pg_char_max_length(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS character_maximum_length,

           CAST(
             _pg_char_octet_length(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS character_octet_length,

           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,

           CAST(CASE WHEN nco.nspname IS NOT NULL THEN current_database() END AS sql_identifier) AS collation_catalog,
           CAST(nco.nspname AS sql_identifier) AS collation_schema,
           CAST(co.collname AS sql_identifier) AS collation_name,

           CAST(
             _pg_numeric_precision(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_precision,

           CAST(
             _pg_numeric_precision_radix(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_precision_radix,

           CAST(
             _pg_numeric_scale(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_scale,

           CAST(
             _pg_datetime_precision(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS datetime_precision,

           CAST(
             _pg_interval_type(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS character_data)
             AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,

           CAST(current_database() AS sql_identifier) AS attribute_udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS attribute_udt_schema,
           CAST(t.typname AS sql_identifier) AS attribute_udt_name,

           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,

           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST(a.attnum AS sql_identifier) AS dtd_identifier,
           CAST('NO' AS yes_or_no) AS is_derived_reference_attribute

    FROM (pg_attribute a LEFT JOIN pg_attrdef ad ON attrelid = adrelid AND attnum = adnum)
         JOIN (pg_class c JOIN pg_namespace nc ON (c.relnamespace = nc.oid)) ON a.attrelid = c.oid
         JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON a.atttypid = t.oid
         LEFT JOIN (pg_collation co JOIN pg_namespace nco ON (co.collnamespace = nco.oid))
           ON a.attcollation = co.oid AND (nco.nspname, co.collname) <> ('pg_catalog', 'default')

    WHERE a.attnum > 0 AND NOT a.attisdropped
          AND c.relkind IN ('c')
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_type_privilege(c.reltype, 'USAGE'))
;
-- Restore plt2_adv_p3
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p3 FOR VALUES IN ('0007', '0009')
;
CREATE UNLOGGED TABLE unlogged1 (a int) PARTITION BY RANGE (a)
;
CREATE EXTENSION IF NOT EXISTS file_fdw
;
alter table only renameColumn rename column a to d
;
ALTER POLICY p ON tbl1 TO regress_rls_frank USING (true)
;
-- mcv with arrays
CREATE TABLE mcv_lists_arrays (
    a TEXT[],
    b NUMERIC[],
    c INT[]
)
;
CREATE TABLE prt2_e (a int, b int, c int) PARTITION BY RANGE(((b + a)/2))
;
drop function error_trap_test()
;
ALTER TABLE pg_toast.pg_toast_30694 RENAME TO reind_con_toast
;
create index on idxpart1 (a, a)
;
-- test effects of TRUNCATE on n_live_tup/n_dead_tup counters
CREATE TABLE trunc_stats_test(id serial)
;
DROP FUNCTION unlck()
;
-- invalid: non-lowercase quoted identifiers
ALTER OPERATOR & (bit, bit) SET ("Restrict" = _int_contsel, "Join" = _int_contjoinsel)
;
create index on mcrparted2 (a, abs(b), c)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_37969_2
;
----------
[CREATE_TABLE] CREATE TABLE bigintp_10 PARTITION OF bigintp FOR VALUES IN (10)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regexp_match( citext, citext ) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table fkpart2.fk_part_1 drop constraint fkey
;
----------
[CREATE_TABLE] create table depth2() inherits (depth1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION voidtest2(a int, b int) RETURNS VOID LANGUAGE SQL AS
$$ SELECT voidtest1(a + b) $$
;
----------
[CREATE_PROG_OBJ] create aggregate my_sum_init(int4)
(
   stype = avg_state,
   sfunc = avg_transfn,
   finalfunc = sum_finalfn,
   initcond = '(10,0)'
)
;
----------
[CREATE_TABLE] CREATE TABLE mchash (a int, b text, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_text_ops)
;
----------
[CREATE_PROG_OBJ] -- Short format:

CREATE FUNCTION isbn_in(cstring)
	RETURNS isbn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION ctoast_large_val() RETURNS TEXT LANGUAGE SQL AS 'select array_agg(md5(g::text))::text from generate_series(1, 256) g'
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails
ALTER TABLE pitest2_p1 ALTER COLUMN f3 SET INCREMENT BY 2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_25980_2 ADD COLUMN newdata pg_temp_13.pg_temp_25980
;
----------
[CREATE_TABLE] CREATE TABLE colors (
	id INT GENERATED BY DEFAULT AS IDENTITY (INCREMENT BY 3 START WITH 7),
	name VARCHAR(10)
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~~ (
    PROCEDURE = texticlike,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = !~~,
    RESTRICT  = iclikesel,
    JOIN      = iclikejoinsel
)
;
----------
[ADD_INDEX] CREATE INDEX idx_postgis_places_geom_3857_gist ON split_postgis.places USING gist (ST_Transform(geom, 3857))
;
----------
[DROP_CATALOG] DROP DATABASE lakh
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key2 CHECK(c > 6)
;
----------
[ADD_INDEX] CREATE INDEX "idx" ON "public"."cmdata" USING "btree" ("f1")
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_subscription_rel
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE functional_dependencies ALTER COLUMN c TYPE numeric
;
----------
[ADD_INDEX] CREATE INDEX ON syscol_table (ctid)
;
----------
[DROP_TABLE] DROP TABLE atestc
;
----------
[ALTER_INDEX] -- Simple ALTER INDEX
alter index btree_idx1 set (vacuum_cleanup_index_scale_factor = 70.0)
;
----------
[CREATE_TABLE] --
-- Verify that EXCLUDED does not allow system column references. These
-- do not make sense because EXCLUDED isn't an already stored tuple
-- (and thus doesn't have a ctid etc).
--
create table syscolconflicttest(key int4, data text)
;
----------
[CREATE_TABLE] CREATE TABLE transition_table_level2
(
      level2_no serial NOT NULL ,
      parent_no int NOT NULL,
      level1_node_name varchar(255),
       PRIMARY KEY (level2_no)
) WITHOUT OIDS
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30496_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_close (text)
RETURNS text
AS '$libdir/dblink','dblink_close'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] create temp table outer_7597 (f1 int4, f2 int4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_r_risparent(ltree,_ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE issue_changes_copy (
    uuid, kee,
    issue_key,
    user_login,
    change_type,
    change_data,
    created_at,
    updated_at,
    issue_change_creation_date
)
AS (
    SELECT CAST (id AS VARCHAR (40)) AS uuid, kee, issue_key, user_login, change_type, change_data, created_at, updated_at, issue_change_creation_date FROM issue_changes)
;
----------
[CREATE_POLICY] /*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR IF EXISTS split_operator_probe_next.## (integer, integer), split_operator_probe.!! (NONE, integer), split_operator_probe.=== (integer, integer) RESTRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht1_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE t1 DROP COLUMN junk1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int8alias1lt"("public"."int8alias1", "public"."int8alias1") RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8lt$$
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.10
 * ASSERTIONS view
 */

-- feature not supported


/*
 * 5.11
 * ATTRIBUTES view
 */

CREATE VIEW attributes AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nc.nspname AS sql_identifier) AS udt_schema,
           CAST(c.relname AS sql_identifier) AS udt_name,
           CAST(a.attname AS sql_identifier) AS attribute_name,
           CAST(a.attnum AS cardinal_number) AS ordinal_position,
           CAST(pg_get_expr(ad.adbin, ad.adrelid) AS character_data) AS attribute_default,
           CAST(CASE WHEN a.attnotnull OR (t.typtype = 'd' AND t.typnotnull) THEN 'NO' ELSE 'YES' END
             AS yes_or_no)
             AS is_nullable, -- This column was apparently removed between SQL:2003 and SQL:2008.

           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nt.nspname = 'pg_catalog' THEN format_type(a.atttypid, null)
                  ELSE 'USER-DEFINED' END
             AS character_data)
             AS data_type,

           CAST(
             _pg_char_max_length(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS character_maximum_length,

           CAST(
             _pg_char_octet_length(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS character_octet_length,

           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,

           CAST(CASE WHEN nco.nspname IS NOT NULL THEN current_database() END AS sql_identifier) AS collation_catalog,
           CAST(nco.nspname AS sql_identifier) AS collation_schema,
           CAST(co.collname AS sql_identifier) AS collation_name,

           CAST(
             _pg_numeric_precision(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_precision,

           CAST(
             _pg_numeric_precision_radix(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_precision_radix,

           CAST(
             _pg_numeric_scale(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_scale,

           CAST(
             _pg_datetime_precision(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS datetime_precision,

           CAST(
             _pg_interval_type(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS character_data)
             AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,

           CAST(current_database() AS sql_identifier) AS attribute_udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS attribute_udt_schema,
           CAST(t.typname AS sql_identifier) AS attribute_udt_name,

           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,

           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST(a.attnum AS sql_identifier) AS dtd_identifier,
           CAST('NO' AS yes_or_no) AS is_derived_reference_attribute

    FROM (pg_attribute a LEFT JOIN pg_attrdef ad ON attrelid = adrelid AND attnum = adnum)
         JOIN (pg_class c JOIN pg_namespace nc ON (c.relnamespace = nc.oid)) ON a.attrelid = c.oid
         JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON a.atttypid = t.oid
         LEFT JOIN (pg_collation co JOIN pg_namespace nco ON (co.collnamespace = nco.oid))
           ON a.attcollation = co.oid AND (nco.nspname, co.collname) <> ('pg_catalog', 'default')

    WHERE a.attnum > 0 AND NOT a.attisdropped
          AND c.relkind IN ('c')
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_type_privilege(c.reltype, 'USAGE'))
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Restore plt2_adv_p3
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p3 FOR VALUES IN ('0007', '0009')
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE unlogged1 (a int) PARTITION BY RANGE (a)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION IF NOT EXISTS file_fdw
;
----------
[RENAME_COLUMN] alter table only renameColumn rename column a to d
;
----------
[ALTER_POLICY] ALTER POLICY p ON tbl1 TO regress_rls_frank USING (true)
;
----------
[CREATE_TABLE] -- mcv with arrays
CREATE TABLE mcv_lists_arrays (
    a TEXT[],
    b NUMERIC[],
    c INT[]
)
;
----------
[CREATE_TABLE] CREATE TABLE prt2_e (a int, b int, c int) PARTITION BY RANGE(((b + a)/2))
;
----------
[DROP_PROG_OBJ] drop function error_trap_test()
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30694 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] create index on idxpart1 (a, a)
;
----------
[CREATE_TABLE] -- test effects of TRUNCATE on n_live_tup/n_dead_tup counters
CREATE TABLE trunc_stats_test(id serial)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION unlck()
;
----------
[ALTER_PROG_OBJ] -- invalid: non-lowercase quoted identifiers
ALTER OPERATOR & (bit, bit) SET ("Restrict" = _int_contsel, "Join" = _int_contjoinsel)
;
----------
[ADD_INDEX] create index on mcrparted2 (a, abs(b), c)
;
