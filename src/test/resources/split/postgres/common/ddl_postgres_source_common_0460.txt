DROP INDEX CONCURRENTLY ginidx
;
ALTER TEXT SEARCH CONFIGURATION hindi ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
ALTER VIEW gtest1v ALTER COLUMN b SET DEFAULT 100
;
CREATE SCHEMA fast_default
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
CREATE EXTENSION test_pg_dump
;
DROP FUNCTION event_trigger_dummy_trigger()
;
-- fail - origin must be either none or any
CREATE SUBSCRIPTION regress_testsub4 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false, origin = foo)
;
CREATE SCHEMA amcheck_schema
;
---
-- Test that ALTER TABLE rewrite preserves nondefault replica identity
---

-- constraint variant
CREATE TABLE test_replica_identity2 (id int UNIQUE NOT NULL)
;
create trigger check_pkeys_fkey_cascade
	after delete or update on pkeys
	for each row
	execute procedure
	check_foreign_key (2, 'cascade', 'pkey1', 'pkey2',
	'fkeys', 'fkey1', 'fkey2', 'fkeys2', 'fkey21', 'fkey22')
;
-- These things should work through a view, as well
create view insertconflictview as select * from insertconflicttest
;
CREATE FUNCTION transition_table_level1_ri_parent_upd_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
  DECLARE
    x int;
  BEGIN
    WITH p AS (SELECT level1_no, sum(delta) cnt
                 FROM (SELECT level1_no, 1 AS delta FROM i
                       UNION ALL
                       SELECT level1_no, -1 AS delta FROM d) w
                 GROUP BY level1_no
                 HAVING sum(delta) < 0)
    SELECT level1_no
      FROM p JOIN transition_table_level2 c ON c.parent_no = p.level1_no
      INTO x;
    IF FOUND THEN
      RAISE EXCEPTION 'RI error';
    END IF;
    RETURN NULL;
  END;
$$
;
CREATE FUNCTION difference(text,text) RETURNS int
AS '$libdir/fuzzystrmatch', 'difference'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE INDEX collate_test1_idx3 ON collate_test1 ((b COLLATE "C"))
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_aggregate
;
ALTER TABLE alter_idx_rename_test_parted_idx_2 RENAME TO alter_idx_rename_test_parted_idx_3
;
-- infer anyelement+anymultirange from anyelement+anymultirange
create function mr_table_succeed(i anyelement, r anymultirange) returns table(i anyelement, r anymultirange)
  as $$ select $1, $2 $$ language sql
;
DROP TRIGGER trans_deletetrig ON range_parted
;
ALTER TABLE public.gtest23b ADD CONSTRAINT gtest23b_b_fkey FOREIGN KEY (b) REFERENCES gtest23a(x)
;
ALTER TABLE replication_example ADD COLUMN zaphod2 int
;
ANALYZE public.alpha_neg_p2
;
CREATE TEMPORARY TABLE invalidate_catalog_cache()
;
CREATE TABLE gtest_err_5a (a int PRIMARY KEY, b int DEFAULT 5 GENERATED ALWAYS AS (a * 2) STORED)
;
create temp table c (
     name char not null,
     a char,
     constraint c_pk primary key (name)
)
;
ALTER EXTENSION test_parser ADD text search parser testparser
;
ALTER TABLE ONLY "public"."student" INHERIT "public"."person"
;
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
ALTER OPERATOR @ (ltree, ltxtquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
ALTER TABLE ONLY "public"."mlparted2" DROP COLUMN "........pg.dropped.5........"
;
CREATE TABLE unused ()
;
ANALYZE pg_temp_38.pg_temp_28037
;
drop table uv_iocu_tab
;
CREATE SCHEMA test_view_schema
    CREATE TEMP VIEW testview AS SELECT 1
;
CREATE TABLE test_io_bulkwrite_strategy AS SELECT i FROM generate_series(1,100)i
;
-- test for falsely detected recursion
DROP TABLE connectby_int
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
DROP TABLE s
;
drop table refd_table, trig_table
;
ALTER TABLE pg_toast.pg_toast_30660 RENAME TO reind_con_toast
;
create function f () returns trigger as
$$ begin return new; end; $$
language plpgsql
;
create table join_foo as select generate_series(1, 3) as id, 'xxxxx'::text as t
;
CREATE SUBSCRIPTION regress_sub4 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub4 WITH (failover = true)
;
ALTER TABLE ONLY "public"."d_star" INHERIT "public"."b_star"
;
-- varbit check

CREATE TABLE varbittmp (a varbit)
;
alter domain dcomptype add constraint c1 check ((value).r > 0)
;
-- check that partition bound is compatible
CREATE TABLE list_parted (
	a int NOT NULL,
	b char(2) COLLATE "C",
	CONSTRAINT check_a CHECK (a > 0)
) PARTITION BY LIST (a)
;
create function myintout(myint) returns cstring strict immutable language
  internal as 'int4out'
;
-- add column to empty table
ALTER TABLE itest13 ADD COLUMN b int GENERATED BY DEFAULT AS IDENTITY
;
-- cleanup
drop table mcrparted
;
------------------------------------------------------------------------------------------
[DROP_INDEX] DROP INDEX CONCURRENTLY ginidx
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION hindi ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ALTER_VIEW|ALTER_COLUMN] ALTER VIEW gtest1v ALTER COLUMN b SET DEFAULT 100
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fast_default
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_pg_dump
;
----------
[DROP_PROG_OBJ] DROP FUNCTION event_trigger_dummy_trigger()
;
----------
[CREATE_PUB_SUB] -- fail - origin must be either none or any
CREATE SUBSCRIPTION regress_testsub4 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false, origin = foo)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA amcheck_schema
;
----------
[CREATE_TABLE] ---
-- Test that ALTER TABLE rewrite preserves nondefault replica identity
---

-- constraint variant
CREATE TABLE test_replica_identity2 (id int UNIQUE NOT NULL)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger check_pkeys_fkey_cascade
	after delete or update on pkeys
	for each row
	execute procedure
	check_foreign_key (2, 'cascade', 'pkey1', 'pkey2',
	'fkeys', 'fkey1', 'fkey2', 'fkeys2', 'fkey21', 'fkey22')
;
----------
[CREATE_VIEW(SELECT)] -- These things should work through a view, as well
create view insertconflictview as select * from insertconflicttest
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION transition_table_level1_ri_parent_upd_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
  DECLARE
    x int;
  BEGIN
    WITH p AS (SELECT level1_no, sum(delta) cnt
                 FROM (SELECT level1_no, 1 AS delta FROM i
                       UNION ALL
                       SELECT level1_no, -1 AS delta FROM d) w
                 GROUP BY level1_no
                 HAVING sum(delta) < 0)
    SELECT level1_no
      FROM p JOIN transition_table_level2 c ON c.parent_no = p.level1_no
      INTO x;
    IF FOUND THEN
      RAISE EXCEPTION 'RI error';
    END IF;
    RETURN NULL;
  END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION difference(text,text) RETURNS int
AS '$libdir/fuzzystrmatch', 'difference'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX collate_test1_idx3 ON collate_test1 ((b COLLATE "C"))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_aggregate
;
----------
[RENAME_TABLE] ALTER TABLE alter_idx_rename_test_parted_idx_2 RENAME TO alter_idx_rename_test_parted_idx_3
;
----------
[CREATE_PROG_OBJ] -- infer anyelement+anymultirange from anyelement+anymultirange
create function mr_table_succeed(i anyelement, r anymultirange) returns table(i anyelement, r anymultirange)
  as $$ select $1, $2 $$ language sql
;
----------
[DROP_TRIGGER] DROP TRIGGER trans_deletetrig ON range_parted
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.gtest23b ADD CONSTRAINT gtest23b_b_fkey FOREIGN KEY (b) REFERENCES gtest23a(x)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE replication_example ADD COLUMN zaphod2 int
;
----------
[ADMIN_TABLE] ANALYZE public.alpha_neg_p2
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE invalidate_catalog_cache()
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_5a (a int PRIMARY KEY, b int DEFAULT 5 GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_TABLE] create temp table c (
     name char not null,
     a char,
     constraint c_pk primary key (name)
)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_parser ADD text search parser testparser
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."student" INHERIT "public"."person"
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @ (ltree, ltxtquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted2" DROP COLUMN "........pg.dropped.5........"
;
----------
[CREATE_TABLE] CREATE TABLE unused ()
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_38.pg_temp_28037
;
----------
[DROP_TABLE] drop table uv_iocu_tab
;
----------
[CREATE_SCHEMA(SELECT)] CREATE SCHEMA test_view_schema
    CREATE TEMP VIEW testview AS SELECT 1
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_io_bulkwrite_strategy AS SELECT i FROM generate_series(1,100)i
;
----------
[DROP_TABLE] -- test for falsely detected recursion
DROP TABLE connectby_int
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
----------
[DROP_TABLE] DROP TABLE s
;
----------
[DROP_TABLE] drop table refd_table, trig_table
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30660 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] create function f () returns trigger as
$$ begin return new; end; $$
language plpgsql
;
----------
[CREATE_TABLE|SELECT] create table join_foo as select generate_series(1, 3) as id, 'xxxxx'::text as t
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub4 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub4 WITH (failover = true)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."d_star" INHERIT "public"."b_star"
;
----------
[CREATE_TABLE] -- varbit check

CREATE TABLE varbittmp (a varbit)
;
----------
[ALTER_TYPE] alter domain dcomptype add constraint c1 check ((value).r > 0)
;
----------
[CREATE_TABLE] -- check that partition bound is compatible
CREATE TABLE list_parted (
	a int NOT NULL,
	b char(2) COLLATE "C",
	CONSTRAINT check_a CHECK (a > 0)
) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] create function myintout(myint) returns cstring strict immutable language
  internal as 'int4out'
;
----------
[ALTER_TABLE|ADD_COLUMN] -- add column to empty table
ALTER TABLE itest13 ADD COLUMN b int GENERATED BY DEFAULT AS IDENTITY
;
----------
[DROP_TABLE] -- cleanup
drop table mcrparted
;
