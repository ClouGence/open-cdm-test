CREATE FUNCTION "public"."use_refcursor"("rc" "refcursor") RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
declare
    rc refcursor;
    x record;
begin
    rc := return_unnamed_refcursor();
    fetch next from rc into x;
    return x.a;
end
$$
;
CREATE OR REPLACE FUNCTION pgstatindex(IN relname text,
    OUT version INT,
    OUT tree_level INT,
    OUT index_size BIGINT,
    OUT root_block_no BIGINT,
    OUT internal_pages BIGINT,
    OUT leaf_pages BIGINT,
    OUT empty_pages BIGINT,
    OUT deleted_pages BIGINT,
    OUT avg_leaf_density FLOAT8,
    OUT leaf_fragmentation FLOAT8)
AS '$libdir/pgstattuple', 'pgstatindex_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_28519 RENAME TO reind_con_toast
;
CREATE TABLE funcidx (x int)
;
ALTER TABLE r2 DISABLE ROW LEVEL SECURITY
;
CREATE FUNCTION ad_eq(arrdomain, arrdomain) returns boolean as
  'begin return array_eq($1, $2); end' language plpgsql
;
DROP TYPE IF EXISTS basket
;
CREATE TABLE table_fillfactor (
  n char(1000)
) with (fillfactor=10, autovacuum_enabled=off)
;
CREATE FUNCTION texticregexeq(citext, text)
RETURNS bool AS 'texticregexeq'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TRIGGER parent_tbl_update_trig
  AFTER UPDATE ON parent_tbl REFERENCING OLD TABLE AS old_table NEW TABLE AS new_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
CREATE TRIGGER t1 BEFORE INSERT ON pg_description EXECUTE FUNCTION tf1()
;
CREATE TABLE "public"."tenk2" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
DROP TRANSFORM IF EXISTS FOR hstore LANGUAGE fake_lang
;
-- Test not-null restrictions

create domain dnotnull varchar(15) NOT NULL
;
truncate pgbench_history
;
CREATE TABLE range_parted4 (a int, b int, c int) PARTITION BY RANGE (abs(a), abs(b), c)
;
CREATE TABLE "public"."iface" (
    "slotname" character(20),
    "sysname" "text",
    "ifname" "text",
    "slotlink" character(20)
)
;
-- fails

ALTER TYPE test_type ADD ATTRIBUTE b text
;
create table pp_recpart_11 partition of pp_recpart for values in ('(1,1)')
;
ALTER TABLE pg_ts_parser ADD PRIMARY KEY USING INDEX pg_ts_parser_oid_index
;
CREATE TABLE public.tab_rf_partition (LIKE schema_rf_x.tab_rf_partitioned)
;
CREATE SEQUENCE uv_seq
;
CREATE SCHEMA test_pg_dump_s1
;
create function sillysrf(int) returns setof int as
  'values (1),(10),(2),($1)' language sql immutable
;
DROP FUNCTION IF EXISTS test_function_exists(int, text, int[])
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=25957 host=/tmp/Y3mX4chFuk dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
CREATE PUBLICATION testpub4 FOR TABLE ONLY testpub_tbl3
;
VACUUM (ANALYZE) pg_catalog.pg_conversion
;
-- Regression tests for bugs:

-- Check that arguments that are R/W expanded datums aren't corrupted by
-- multiple uses.  This test knows that array_append() returns a R/W datum
-- and will modify a R/W array input in-place.  We use SETOF to prevent
-- inlining of the SQL function.
CREATE FUNCTION double_append(anyarray, anyelement) RETURNS SETOF anyarray
LANGUAGE SQL IMMUTABLE AS
$$ SELECT array_append($1, $2) || array_append($1, $2) $$
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE TRIGGER sub1_tab1_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1
  FOR EACH ROW EXECUTE PROCEDURE sub1_trigger_activity_func()
;
DROP TYPE test_type_empty
;
ALTER TABLE itest14 ALTER id ADD GENERATED BY DEFAULT AS IDENTITY
;
ALTER TABLE inh_test INHERIT part_2
;
/* contrib/pg_trgm/pg_trgm--1.3.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Deprecated function
CREATE FUNCTION set_limit(float4)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT VOLATILE PARALLEL UNSAFE
;
CREATE VIEW pg_stat_ssl AS
    SELECT
            S.pid,
            S.ssl,
            S.sslversion AS version,
            S.sslcipher AS cipher,
            S.sslbits AS bits,
            S.sslcompression AS compression,
            S.ssl_client_dn AS client_dn,
            S.ssl_client_serial AS client_serial,
            S.ssl_issuer_dn AS issuer_dn
    FROM pg_stat_get_activity(NULL) AS S
    WHERE S.client_port IS NOT NULL
;
CREATE TABLE test_extdep_commands (command text)
;
CREATE FUNCTION g_cube_compress(internal)
RETURNS internal
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) corruption_test
;
create table idxpart2 (col1 int, col2 int, b int, a int)
;
-- Make sure a table named excluded is handled properly
create table excluded(key int primary key, data text)
;
CREATE TABLE "public"."pagg_tab1" (
    "x" integer,
    "y" integer
)
PARTITION BY RANGE ("x")
;
CREATE FUNCTION intarray_del_elem(_int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE import_source."x 6" (c1 int, c2 int generated always as (c1 * 2) stored)
;
CREATE TABLE jointest AS SELECT generate_series(1,10) AS id, 0 AS data
;
ALTER TABLE ab1 ALTER a SET STATISTICS 0
;
/*
 * 24.6
 * FOREIGN_DATA_WRAPPERS view
 */
CREATE VIEW foreign_data_wrappers AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           authorization_identifier,
           CAST(NULL AS character_data) AS library_name,
           foreign_data_wrapper_language
    FROM _pg_foreign_data_wrappers w
;
CREATE TRIGGER row_before_insupd_trigger BEFORE INSERT OR UPDATE ON child_tbl FOR EACH ROW EXECUTE PROCEDURE row_before_insupd_trigfunc()
;
CREATE SCHEMA my_schema_1
;
--
-- Test extension with objects outside the extension's schema.
--
CREATE SCHEMA test_func_dep1
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."use_refcursor"("rc" "refcursor") RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
declare
    rc refcursor;
    x record;
begin
    rc := return_unnamed_refcursor();
    fetch next from rc into x;
    return x.a;
end
$$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pgstatindex(IN relname text,
    OUT version INT,
    OUT tree_level INT,
    OUT index_size BIGINT,
    OUT root_block_no BIGINT,
    OUT internal_pages BIGINT,
    OUT leaf_pages BIGINT,
    OUT empty_pages BIGINT,
    OUT deleted_pages BIGINT,
    OUT avg_leaf_density FLOAT8,
    OUT leaf_fragmentation FLOAT8)
AS '$libdir/pgstattuple', 'pgstatindex_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28519 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE funcidx (x int)
;
----------
[ALTER_TABLE] ALTER TABLE r2 DISABLE ROW LEVEL SECURITY
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ad_eq(arrdomain, arrdomain) returns boolean as
  'begin return array_eq($1, $2); end' language plpgsql
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS basket
;
----------
[CREATE_TABLE] CREATE TABLE table_fillfactor (
  n char(1000)
) with (fillfactor=10, autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION texticregexeq(citext, text)
RETURNS bool AS 'texticregexeq'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER parent_tbl_update_trig
  AFTER UPDATE ON parent_tbl REFERENCING OLD TABLE AS old_table NEW TABLE AS new_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER t1 BEFORE INSERT ON pg_description EXECUTE FUNCTION tf1()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tenk2" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
----------
[UNKNOWN] DROP TRANSFORM IF EXISTS FOR hstore LANGUAGE fake_lang
;
----------
[CREATE_TYPE] -- Test not-null restrictions

create domain dnotnull varchar(15) NOT NULL
;
----------
[TRUNCATE_TABLE] truncate pgbench_history
;
----------
[CREATE_TABLE] CREATE TABLE range_parted4 (a int, b int, c int) PARTITION BY RANGE (abs(a), abs(b), c)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."iface" (
    "slotname" character(20),
    "sysname" "text",
    "ifname" "text",
    "slotlink" character(20)
)
;
----------
[ALTER_TYPE] -- fails

ALTER TYPE test_type ADD ATTRIBUTE b text
;
----------
[CREATE_TABLE] create table pp_recpart_11 partition of pp_recpart for values in ('(1,1)')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_parser ADD PRIMARY KEY USING INDEX pg_ts_parser_oid_index
;
----------
[CREATE_TABLE] CREATE TABLE public.tab_rf_partition (LIKE schema_rf_x.tab_rf_partitioned)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE uv_seq
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_pg_dump_s1
;
----------
[CREATE_PROG_OBJ] create function sillysrf(int) returns setof int as
  'values (1),(10),(2),($1)' language sql immutable
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS test_function_exists(int, text, int[])
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=25957 host=/tmp/Y3mX4chFuk dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub4 FOR TABLE ONLY testpub_tbl3
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_conversion
;
----------
[CREATE_PROG_OBJ] -- Regression tests for bugs:

-- Check that arguments that are R/W expanded datums aren't corrupted by
-- multiple uses.  This test knows that array_append() returns a R/W datum
-- and will modify a R/W array input in-place.  We use SETOF to prevent
-- inlining of the SQL function.
CREATE FUNCTION double_append(anyarray, anyelement) RETURNS SETOF anyarray
LANGUAGE SQL IMMUTABLE AS
$$ SELECT array_append($1, $2) || array_append($1, $2) $$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER sub1_tab1_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1
  FOR EACH ROW EXECUTE PROCEDURE sub1_trigger_activity_func()
;
----------
[DROP_TYPE] DROP TYPE test_type_empty
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest14 ALTER id ADD GENERATED BY DEFAULT AS IDENTITY
;
----------
[ALTER_TABLE] ALTER TABLE inh_test INHERIT part_2
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_trgm/pg_trgm--1.3.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Deprecated function
CREATE FUNCTION set_limit(float4)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT VOLATILE PARALLEL UNSAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_ssl AS
    SELECT
            S.pid,
            S.ssl,
            S.sslversion AS version,
            S.sslcipher AS cipher,
            S.sslbits AS bits,
            S.sslcompression AS compression,
            S.ssl_client_dn AS client_dn,
            S.ssl_client_serial AS client_serial,
            S.ssl_issuer_dn AS issuer_dn
    FROM pg_stat_get_activity(NULL) AS S
    WHERE S.client_port IS NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE test_extdep_commands (command text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_cube_compress(internal)
RETURNS internal
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) corruption_test
;
----------
[CREATE_TABLE] create table idxpart2 (col1 int, col2 int, b int, a int)
;
----------
[CREATE_TABLE] -- Make sure a table named excluded is handled properly
create table excluded(key int primary key, data text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab1" (
    "x" integer,
    "y" integer
)
PARTITION BY RANGE ("x")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION intarray_del_elem(_int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE import_source."x 6" (c1 int, c2 int generated always as (c1 * 2) stored)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE jointest AS SELECT generate_series(1,10) AS id, 0 AS data
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ab1 ALTER a SET STATISTICS 0
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.6
 * FOREIGN_DATA_WRAPPERS view
 */
CREATE VIEW foreign_data_wrappers AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           authorization_identifier,
           CAST(NULL AS character_data) AS library_name,
           foreign_data_wrapper_language
    FROM _pg_foreign_data_wrappers w
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER row_before_insupd_trigger BEFORE INSERT OR UPDATE ON child_tbl FOR EACH ROW EXECUTE PROCEDURE row_before_insupd_trigfunc()
;
----------
[CREATE_SCHEMA] CREATE SCHEMA my_schema_1
;
----------
[CREATE_SCHEMA] --
-- Test extension with objects outside the extension's schema.
--
CREATE SCHEMA test_func_dep1
;
