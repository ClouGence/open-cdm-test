DROP TABLE truncate_a
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
CREATE FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int8,
    OUT level int8,
    OUT fastroot int8,
    OUT fastlevel int8,
    OUT oldest_xact xid,
    OUT last_cleanup_num_tuples float8,
    OUT allequalimage boolean)
AS '$libdir/pageinspect', 'bt_metap'
LANGUAGE C STRICT PARALLEL SAFE
;
create table sq_limit (pk int primary key, c1 int, c2 int)
;
-- ok

-- string_agg bytea tests
create table bytea_test_table(v bytea)
;
ALTER TABLE pg_temp_13.pg_temp_26923_2 ADD COLUMN newdata pg_temp_13.pg_temp_26923
;
ALTER TABLE ONLY "public"."mlparted12" DROP COLUMN "........pg.dropped.5........"
;
CREATE TABLE test33 (a int, b char(3) COLLATE case_insensitive) PARTITION BY HASH (b)
;
ALTER DATABASE template_db IS_TEMPLATE = false
;
CREATE TABLE disappear (a int4)
;
ANALYZE test2
;
CREATE VIEW _pg_foreign_table_columns AS
    SELECT n.nspname,
           c.relname,
           a.attname,
           a.attfdwoptions
    FROM pg_foreign_table t, pg_authid u, pg_namespace n, pg_class c,
         pg_attribute a
    WHERE u.oid = c.relowner
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_column_privilege(c.oid, a.attnum, 'SELECT, INSERT, UPDATE, REFERENCES'))
          AND n.oid = c.relnamespace
          AND c.oid = t.ftrelid
          AND c.relkind = 'f'
          AND a.attrelid = c.oid
          AND a.attnum > 0
;
DROP TABLE IF EXISTS d3_listp, d3_listp1, d3_listp2, d3_pid
;
CREATE FUNCTION intset_subtract(_int4, _int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE ONLY "generated_stored_tests"."gtest10a" DROP COLUMN "........pg.dropped.2........"
;
CREATE FUNCTION "public"."distinct_func"("a" integer) RETURNS integer
    LANGUAGE "plpgsql" PARALLEL SAFE
    AS $$
  BEGIN
    RETURN a;
  END;
$$
;
CREATE TYPE split_domain_type_next.address_type AS (street text COLLATE "C", zip integer)
;
create table ��Ƒ�㑻�������(������ text, ��֑����� varchar, ����ע1A char(16))
;
ALTER INDEX pg_toast.pg_toast_30573_index RENAME TO reind_con_toast_idx
;
CREATE TABLE pred_parent (a int)
;
-- test deparsing
CREATE TABLE sf_target(id int, data text, filling int[])
;
DROP TABLE tab_fk CASCADE
;
CREATE FUNCTION read_ordered_named_pair(p ordered_named_pair) RETURNS integer AS $$
begin
return p.i + p.j;
end
$$ LANGUAGE plpgsql
;
CREATE FUNCTION "public"."my_int_eq"(integer, integer) RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT SUPPORT "public"."test_support_func" PARALLEL SAFE
    AS $$int4eq$$
;
ALTER TABLE pg_temp_24.pg_temp_26555_2 ADD COLUMN newdata pg_temp_24.pg_temp_26555
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26215_2 (tid pg_catalog.tid)
;
create table s3.p1 (a int, b int) PARTITION BY list (a)
;
-- same with arrays
CREATE TABLE test22a (a int, b text[] COLLATE case_sensitive) PARTITION BY HASH (b)
;
ALTER TABLE part_5 ATTACH PARTITION part5_p1 FOR VALUES IN ('y')
;
ALTER EVENT TRIGGER regress_reindex_start DISABLE
;
-- Check dependencies are established when creating a new type
CREATE TYPE base_type
;
TRUNCATE ONLY tru_ftable
;
CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
CREATE INDEX ON past_inh_db_parent ((1))
;
CREATE TABLE footab(a varchar)
;
create table alterlock2 (f3 int primary key, f1 int)
;
CREATE TABLE ruletest_tbl2 (a int, b int)
;
/*
 * 6.66
 * TRANSFORMS view
 */

CREATE VIEW transforms AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(l.lanname AS sql_identifier) AS group_name,
           CAST('FROM SQL' AS character_data) AS transform_type
    FROM pg_type t JOIN pg_transform x ON t.oid = x.trftype
         JOIN pg_language l ON x.trflang = l.oid
         JOIN pg_proc p ON x.trffromsql = p.oid
         JOIN pg_namespace nt ON t.typnamespace = nt.oid
         JOIN pg_namespace np ON p.pronamespace = np.oid

  UNION

    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(l.lanname AS sql_identifier) AS group_name,
           CAST('TO SQL' AS character_data) AS transform_type
    FROM pg_type t JOIN pg_transform x ON t.oid = x.trftype
         JOIN pg_language l ON x.trflang = l.oid
         JOIN pg_proc p ON x.trftosql = p.oid
         JOIN pg_namespace nt ON t.typnamespace = nt.oid
         JOIN pg_namespace np ON p.pronamespace = np.oid

  ORDER BY udt_catalog, udt_schema, udt_name, group_name, transform_type  -- some sensible grouping for interactive use
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 SET STATISTICS -1
;
CREATE TABLE concur_reindex_schema.tab (a int)
;
-- Check deferred exclusion constraint

CREATE TABLE deferred_excl (
  f1 int,
  f2 int,
  CONSTRAINT deferred_excl_con EXCLUDE (f1 WITH =) INITIALLY DEFERRED
)
;
CREATE FUNCTION price_key_from_table(price) RETURNS price_key AS $$
    SELECT $1.id
$$ LANGUAGE SQL
;
CREATE INDEX "point_table_a_idx" ON "public"."point_table" USING "gist" ("a")
;
DROP TABLE IF EXISTS lcku_table
;
drop function conflict_test()
;
ALTER TABLE ONLY "public"."d_star" INHERIT "public"."c_star"
;
CREATE TABLE hash_parted (
	a int
) PARTITION BY HASH (a)
;
--
-- Another corner case is that we might add a column to a table below a
-- JOIN USING, and thereby make the USING column name ambiguous
--

create table tt11 (x int, y int)
;
-- something a little more complicated
create table atacc1 ( test int, test2 int, test3 int)
;
CREATE FUNCTION "public"."tg_room_ad"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    delete from WSlot where roomno = old.roomno;
    return old;
end;
$$
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE truncate_a
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int8,
    OUT level int8,
    OUT fastroot int8,
    OUT fastlevel int8,
    OUT oldest_xact xid,
    OUT last_cleanup_num_tuples float8,
    OUT allequalimage boolean)
AS '$libdir/pageinspect', 'bt_metap'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table sq_limit (pk int primary key, c1 int, c2 int)
;
----------
[CREATE_TABLE] -- ok

-- string_agg bytea tests
create table bytea_test_table(v bytea)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26923_2 ADD COLUMN newdata pg_temp_13.pg_temp_26923
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted12" DROP COLUMN "........pg.dropped.5........"
;
----------
[CREATE_TABLE] CREATE TABLE test33 (a int, b char(3) COLLATE case_insensitive) PARTITION BY HASH (b)
;
----------
[ALTER_CATALOG] ALTER DATABASE template_db IS_TEMPLATE = false
;
----------
[CREATE_TABLE] CREATE TABLE disappear (a int4)
;
----------
[ADMIN_TABLE] ANALYZE test2
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW _pg_foreign_table_columns AS
    SELECT n.nspname,
           c.relname,
           a.attname,
           a.attfdwoptions
    FROM pg_foreign_table t, pg_authid u, pg_namespace n, pg_class c,
         pg_attribute a
    WHERE u.oid = c.relowner
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_column_privilege(c.oid, a.attnum, 'SELECT, INSERT, UPDATE, REFERENCES'))
          AND n.oid = c.relnamespace
          AND c.oid = t.ftrelid
          AND c.relkind = 'f'
          AND a.attrelid = c.oid
          AND a.attnum > 0
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS d3_listp, d3_listp1, d3_listp2, d3_pid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION intset_subtract(_int4, _int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_stored_tests"."gtest10a" DROP COLUMN "........pg.dropped.2........"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."distinct_func"("a" integer) RETURNS integer
    LANGUAGE "plpgsql" PARALLEL SAFE
    AS $$
  BEGIN
    RETURN a;
  END;
$$
;
----------
[CREATE_TYPE] CREATE TYPE split_domain_type_next.address_type AS (street text COLLATE "C", zip integer)
;
----------
[CREATE_TABLE] create table ��Ƒ�㑻�������(������ text, ��֑����� varchar, ����ע1A char(16))
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30573_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE pred_parent (a int)
;
----------
[CREATE_TABLE] -- test deparsing
CREATE TABLE sf_target(id int, data text, filling int[])
;
----------
[DROP_TABLE] DROP TABLE tab_fk CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION read_ordered_named_pair(p ordered_named_pair) RETURNS integer AS $$
begin
return p.i + p.j;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."my_int_eq"(integer, integer) RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT SUPPORT "public"."test_support_func" PARALLEL SAFE
    AS $$int4eq$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_24.pg_temp_26555_2 ADD COLUMN newdata pg_temp_24.pg_temp_26555
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26215_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table s3.p1 (a int, b int) PARTITION BY list (a)
;
----------
[CREATE_TABLE] -- same with arrays
CREATE TABLE test22a (a int, b text[] COLLATE case_sensitive) PARTITION BY HASH (b)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part_5 ATTACH PARTITION part5_p1 FOR VALUES IN ('y')
;
----------
[ALTER_TRIGGER] ALTER EVENT TRIGGER regress_reindex_start DISABLE
;
----------
[CREATE_TYPE] -- Check dependencies are established when creating a new type
CREATE TYPE base_type
;
----------
[TRUNCATE_TABLE] TRUNCATE ONLY tru_ftable
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
----------
[ADD_INDEX] CREATE INDEX ON past_inh_db_parent ((1))
;
----------
[CREATE_TABLE] CREATE TABLE footab(a varchar)
;
----------
[CREATE_TABLE] create table alterlock2 (f3 int primary key, f1 int)
;
----------
[CREATE_TABLE] CREATE TABLE ruletest_tbl2 (a int, b int)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.66
 * TRANSFORMS view
 */

CREATE VIEW transforms AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(l.lanname AS sql_identifier) AS group_name,
           CAST('FROM SQL' AS character_data) AS transform_type
    FROM pg_type t JOIN pg_transform x ON t.oid = x.trftype
         JOIN pg_language l ON x.trflang = l.oid
         JOIN pg_proc p ON x.trffromsql = p.oid
         JOIN pg_namespace nt ON t.typnamespace = nt.oid
         JOIN pg_namespace np ON p.pronamespace = np.oid

  UNION

    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(l.lanname AS sql_identifier) AS group_name,
           CAST('TO SQL' AS character_data) AS transform_type
    FROM pg_type t JOIN pg_transform x ON t.oid = x.trftype
         JOIN pg_language l ON x.trflang = l.oid
         JOIN pg_proc p ON x.trftosql = p.oid
         JOIN pg_namespace nt ON t.typnamespace = nt.oid
         JOIN pg_namespace np ON p.pronamespace = np.oid

  ORDER BY udt_catalog, udt_schema, udt_name, group_name, transform_type  -- some sensible grouping for interactive use
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 SET STATISTICS -1
;
----------
[CREATE_TABLE] CREATE TABLE concur_reindex_schema.tab (a int)
;
----------
[CREATE_TABLE] -- Check deferred exclusion constraint

CREATE TABLE deferred_excl (
  f1 int,
  f2 int,
  CONSTRAINT deferred_excl_con EXCLUDE (f1 WITH =) INITIALLY DEFERRED
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION price_key_from_table(price) RETURNS price_key AS $$
    SELECT $1.id
$$ LANGUAGE SQL
;
----------
[ADD_INDEX] CREATE INDEX "point_table_a_idx" ON "public"."point_table" USING "gist" ("a")
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS lcku_table
;
----------
[DROP_PROG_OBJ] drop function conflict_test()
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."d_star" INHERIT "public"."c_star"
;
----------
[CREATE_TABLE] CREATE TABLE hash_parted (
	a int
) PARTITION BY HASH (a)
;
----------
[CREATE_TABLE] --
-- Another corner case is that we might add a column to a table below a
-- JOIN USING, and thereby make the USING column name ambiguous
--

create table tt11 (x int, y int)
;
----------
[CREATE_TABLE] -- something a little more complicated
create table atacc1 ( test int, test2 int, test3 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_room_ad"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    delete from WSlot where roomno = old.roomno;
    return old;
end;
$$
;
