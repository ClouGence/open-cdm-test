ALTER TABLE public.clstr_tst_s_rf_a_seq OWNER TO zyc
;
EXECUTE dumpFunc('21757')
;
LOCK TABLE "public"."rtest_order1" IN ACCESS SHARE MODE
;
-- Need to do this so that regress_rol_op3 is not allowed EXECUTE via PUBLIC
SET ROLE regress_rol_op5
;
CREATE USER regress_seq_user
;
explain (analyze, costs off, summary off, timing off)  execute q1 (1,2,2,0)
;
EXECUTE dumpFunc('30025')
;
EXECUTE dumpBaseType('3614')
;
execute foo(false)
;
COPY public.plt2_p2 (a, b, c) TO stdout
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE public.clstr_tst_s_rf_a_seq OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpFunc('21757')
;
----------
[SESSION_LOCK] LOCK TABLE "public"."rtest_order1" IN ACCESS SHARE MODE
;
----------
[SWITCH_ROLE] -- Need to do this so that regress_rol_op3 is not allowed EXECUTE via PUBLIC
SET ROLE regress_rol_op5
;
----------
[CREATE_USER] CREATE USER regress_seq_user
;
----------
[UNSAFE] explain (analyze, costs off, summary off, timing off)  execute q1 (1,2,2,0)
;
----------
[UNSAFE] EXECUTE dumpFunc('30025')
;
----------
[UNSAFE] EXECUTE dumpBaseType('3614')
;
----------
[UNSAFE] execute foo(false)
;
----------
[DATA_EXPORT] COPY public.plt2_p2 (a, b, c) TO stdout
;
