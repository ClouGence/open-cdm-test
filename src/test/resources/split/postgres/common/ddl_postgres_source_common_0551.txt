CREATE FUNCTION btean13cmp(ean13, isbn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION isneq(issn13, issn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create index patest1i on patest1(id)
;
CREATE OPERATOR <<<-> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_dist_op,
        COMMUTATOR = '<->>>'
)
;
CREATE TABLE pitest3 (f1 date NOT NULL, f2 text, f3 int) PARTITION BY RANGE (f1)
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
ALTER TABLE pg_user_mapping ADD UNIQUE USING INDEX pg_user_mapping_user_server_index
;
VACUUM (SKIP_DATABASE_STATS) "Foo".bar
;
-- Test non-error-throwing input

create domain positiveint int4 check(value > 0)
;
CREATE UNLOGGED TABLE split_cov.unlogged_table (LIKE split_cov.parent_table INCLUDING ALL)
;
CREATE FUNCTION "generated_virtual_tests"."gtest_trigger_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  IF tg_op IN ('DELETE', 'UPDATE') THEN
    RAISE INFO '%: %: old = %', TG_NAME, TG_WHEN, OLD;
  END IF;
  IF tg_op IN ('INSERT', 'UPDATE') THEN
    RAISE INFO '%: %: new = %', TG_NAME, TG_WHEN, NEW;
  END IF;
  IF tg_op = 'DELETE' THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END
$$
;
CREATE COLLATION case_coll ("Lc_Collate" = "POSIX", "Lc_Ctype" = "POSIX")
;
CREATE TABLE gtest_err_7d (a int PRIMARY KEY, b int GENERATED ALWAYS AS (generate_series(1, a)) STORED)
;
CREATE FUNCTION "public"."test_event_trigger"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
    RAISE NOTICE 'test_event_trigger: % %', tg_event, tg_tag;
END
$$
;
CREATE TABLE gtest_err_7a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (avg(a)) STORED)
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.date() < $.datetime("HH:MI TZH"))'))
;
-- arrays of ltree

CREATE FUNCTION _ltree_isparent(_ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FOREIGN TABLE foreign_stats (a int) SERVER server_stats
;
-- Check that null values in an unique index are not treated as equal
CREATE TABLE bttest_unique_nulls (a serial, b int, c int UNIQUE)
;
ANALYZE plt1_e
;
CREATE TABLE notnull_tbl1 (a int, b int)
;
CREATE FUNCTION "public"."tg_pfield_ad"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    delete from PSlot where pfname = old.name;
    return old;
end;
$$
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
drop materialized view mvtest_error
;
/*
 * 3.1 Test ALTER TABLE DROP COLUMN.
 * Included column deletion leads to the index deletion,
 * AS well AS key columns deletion. It's explained in documentation.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box)
;
VACUUM (PARALLEL 2) pg_catalog.pg_auth_members
;
create trigger ttdummy
	before delete or update on alterlock
	for each row
	execute procedure
	ttdummy (1, 1)
;
alter extension postgres_fdw drop aggregate least_agg(variadic items anyarray)
;
alter table alterlock2 add foreign key (f1) references alterlock (f1)
;
ALTER OPERATOR FAMILY gist_bpchar_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
create table inh_child() inherits (inh_parent1, inh_parent2)
;
CREATE FUNCTION "public"."stfp"("anyarray") RETURNS "anyarray"
    LANGUAGE "sql"
    AS $_$select $1$_$
;
DROP TABLE IF EXISTS d_lp_fk, d_lp_fk_1, d_lp_fk_2, d_lp_fk_r
;
CREATE OPERATOR CLASS ismn13_ops DEFAULT
	FOR TYPE ismn13 USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btismn13cmp(ismn13, ismn13)
;
CREATE TRIGGER trigtest_before_row BEFORE INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH ROW
EXECUTE PROCEDURE dummy_trigger()
;
CREATE TABLE prt2_l (a int, b int, c varchar) PARTITION BY RANGE(b)
;
CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING bloom AS
	OPERATOR	1	=(int4, int4),
	FUNCTION	1	hashint4(int4)
;
DROP INDEX parent_index
;
CREATE TABLE "strange aaa
name"(a varchar)
;
DROP TABLE concur_reindex_tab4
;
CREATE TABLE concur_reindex_tab (c1 int)
;
drop table descr_t1
;
CREATE VIEW "public"."mvtest_tv" AS
 SELECT "mvtest_t"."type",
    "sum"("mvtest_t"."amt") AS "totamt"
   FROM "public"."mvtest_t"
  GROUP BY "mvtest_t"."type"
;
alter table parted_uniq_detach_test detach partition parted_uniq_detach_test1
;
CREATE EXTENSION amcheck WITH SCHEMA amcheck_schema VERSION '1.3'
;
CREATE TABLE part00 PARTITION OF range_parted3 FOR VALUES FROM (0, minvalue) TO (0, maxvalue)
;
-- Mix TOAST & heap
DROP TABLE reloptions_test
;
CREATE MATERIALIZED VIEW matview AS SELECT col1 FROM table2
;
CREATE TABLE "public"."nv_parent" (
    "d" "date",
    CONSTRAINT "nv_parent_check" CHECK (false) NO INHERIT
)
;
CREATE DOMAIN constraint_comments_dom AS int CONSTRAINT the_constraint CHECK (value > 0)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION btean13cmp(ean13, isbn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(issn13, issn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] create index patest1i on patest1(id)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <<<-> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_dist_op,
        COMMUTATOR = '<->>>'
)
;
----------
[CREATE_TABLE] CREATE TABLE pitest3 (f1 date NOT NULL, f2 text, f3 int) PARTITION BY RANGE (f1)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_user_mapping ADD UNIQUE USING INDEX pg_user_mapping_user_server_index
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) "Foo".bar
;
----------
[CREATE_TYPE] -- Test non-error-throwing input

create domain positiveint int4 check(value > 0)
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE split_cov.unlogged_table (LIKE split_cov.parent_table INCLUDING ALL)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "generated_virtual_tests"."gtest_trigger_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  IF tg_op IN ('DELETE', 'UPDATE') THEN
    RAISE INFO '%: %: old = %', TG_NAME, TG_WHEN, OLD;
  END IF;
  IF tg_op IN ('INSERT', 'UPDATE') THEN
    RAISE INFO '%: %: new = %', TG_NAME, TG_WHEN, NEW;
  END IF;
  IF tg_op = 'DELETE' THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END
$$
;
----------
[CREATE_POLICY] CREATE COLLATION case_coll ("Lc_Collate" = "POSIX", "Lc_Ctype" = "POSIX")
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_7d (a int PRIMARY KEY, b int GENERATED ALWAYS AS (generate_series(1, a)) STORED)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_event_trigger"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
    RAISE NOTICE 'test_event_trigger: % %', tg_event, tg_tag;
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_7a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (avg(a)) STORED)
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.date() < $.datetime("HH:MI TZH"))'))
;
----------
[CREATE_PROG_OBJ] -- arrays of ltree

CREATE FUNCTION _ltree_isparent(_ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE foreign_stats (a int) SERVER server_stats
;
----------
[CREATE_TABLE] -- Check that null values in an unique index are not treated as equal
CREATE TABLE bttest_unique_nulls (a serial, b int, c int UNIQUE)
;
----------
[ADMIN_TABLE] ANALYZE plt1_e
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1 (a int, b int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_pfield_ad"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    delete from PSlot where pfname = old.name;
    return old;
end;
$$
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
----------
[DROP_VIEW] drop materialized view mvtest_error
;
----------
[CREATE_TABLE] /*
 * 3.1 Test ALTER TABLE DROP COLUMN.
 * Included column deletion leads to the index deletion,
 * AS well AS key columns deletion. It's explained in documentation.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_auth_members
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger ttdummy
	before delete or update on alterlock
	for each row
	execute procedure
	ttdummy (1, 1)
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw drop aggregate least_agg(variadic items anyarray)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table alterlock2 add foreign key (f1) references alterlock (f1)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bpchar_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TABLE] create table inh_child() inherits (inh_parent1, inh_parent2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."stfp"("anyarray") RETURNS "anyarray"
    LANGUAGE "sql"
    AS $_$select $1$_$
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS d_lp_fk, d_lp_fk_1, d_lp_fk_2, d_lp_fk_r
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS ismn13_ops DEFAULT
	FOR TYPE ismn13 USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btismn13cmp(ismn13, ismn13)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trigtest_before_row BEFORE INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH ROW
EXECUTE PROCEDURE dummy_trigger()
;
----------
[CREATE_TABLE] CREATE TABLE prt2_l (a int, b int, c varchar) PARTITION BY RANGE(b)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING bloom AS
	OPERATOR	1	=(int4, int4),
	FUNCTION	1	hashint4(int4)
;
----------
[DROP_INDEX] DROP INDEX parent_index
;
----------
[CREATE_TABLE] CREATE TABLE "strange aaa
name"(a varchar)
;
----------
[DROP_TABLE] DROP TABLE concur_reindex_tab4
;
----------
[CREATE_TABLE] CREATE TABLE concur_reindex_tab (c1 int)
;
----------
[DROP_TABLE] drop table descr_t1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."mvtest_tv" AS
 SELECT "mvtest_t"."type",
    "sum"("mvtest_t"."amt") AS "totamt"
   FROM "public"."mvtest_t"
  GROUP BY "mvtest_t"."type"
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_uniq_detach_test detach partition parted_uniq_detach_test1
;
----------
[CREATE_LIBRARY] CREATE EXTENSION amcheck WITH SCHEMA amcheck_schema VERSION '1.3'
;
----------
[CREATE_TABLE] CREATE TABLE part00 PARTITION OF range_parted3 FOR VALUES FROM (0, minvalue) TO (0, maxvalue)
;
----------
[DROP_TABLE] -- Mix TOAST & heap
DROP TABLE reloptions_test
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW matview AS SELECT col1 FROM table2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."nv_parent" (
    "d" "date",
    CONSTRAINT "nv_parent_check" CHECK (false) NO INHERIT
)
;
----------
[CREATE_TYPE] CREATE DOMAIN constraint_comments_dom AS int CONSTRAINT the_constraint CHECK (value > 0)
;
