ALTER TABLE table_dropped_index_no_pk REPLICA IDENTITY
  USING INDEX table_dropped_index_no_pk_idx
;
create function f1(x anyelement) returns anyarray as $$
begin
  return array[x + 1, x + 2];
end$$ language plpgsql
;
CREATE FUNCTION boo(int) RETURNS int IMMUTABLE STRICT LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'boo: %', $1; RETURN $1; END; $$
;
DROP TABLE pgss_dml_tab
;
CREATE SUBSCRIPTION tap_sub
     CONNECTION 'port=26864 host=/tmp/GR2H9fVooJ dbname=postgres'
     PUBLICATION tap_pub
     WITH (copy_data = off)
;
CREATE TABLE "public"."num_result" (
    "id1" integer,
    "id2" integer,
    "result" numeric(210,10)
)
;
CREATE TABLE originated (id int)
;
CREATE TEMPORARY TABLE bool_test(
  b1 BOOL,
  b2 BOOL,
  b3 BOOL,
  b4 BOOL)
;
VACUUM pg_catalog.pg_db_role_setting
;
DROP FUNCTION ptest1(text)
;
ALTER DATABASE "contrib_regression" SET auto_explain.bogus = 1
;
create foreign table grem1 (
  a int,
  b int generated always as (a * 2) stored)
  server loopback options(table_name 'gloc1')
;
drop table parted_pk_detach_test, parted_pk_detach_test1
;
ALTER PUBLICATION testpub_default ADD TABLE pub_test.testpub_nopk
;
create type testtype1 as (a int, b int)
;
CREATE SUBSCRIPTION regress_sub4 CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION regress_pub4 WITH (failover = true)
;
ALTER FUNCTION gbt_float4_union(internal, internal) PARALLEL SAFE
;
-- mcv with pass-by-ref fixlen types, e.g. uuid
CREATE TABLE mcv_lists_uuid (
    a UUID,
    b UUID,
    c UUID
)
WITH (autovacuum_enabled = off)
;
CREATE VIEW bug18634v AS
  SELECT * FROM bug18634t WHERE EXISTS (SELECT 1 FROM bug18634t)
;
CREATE TABLE multixact_conflict (a int primary key)
;
ALTER EXTENSION test_pg_dump ADD AGGREGATE newavg(int4)
;
create temp table recur2 (f1 int, f2 recur1)
;
ALTER TABLE fd_pt2 ALTER c2 SET NOT NULL
;
ALTER TABLE table_dropped_index_with_pk REPLICA IDENTITY
  USING INDEX table_dropped_index_with_pk_idx
;
VACUUM FULL iamalargetable
;
ALTER TYPE "public"."insenum" ADD VALUE 'i13'
;
CREATE FUNCTION ltree_addltree(ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP EVENT TRIGGER regress_reindex_start
;
ALTER TABLE fk_notpartitioned_fk VALIDATE CONSTRAINT fk_notpartitioned_fk_a_b_fkey
;
ANALYZE part1, part2
;
-- the same with the versioned language name
CREATE FUNCTION test1n(val hstore) RETURNS int
LANGUAGE plpython2u
TRANSFORM FOR TYPE hstore
AS $$
assert isinstance(val, dict)
plpy.info(sorted(val.items()))
return len(val)
$$
;
create table sch1.t1(c1 int)
;
-- check that using any of these functions with a partitioned table or index
-- would fail
create table test_partitioned (a int) partition by range (a)
;
--
-- multirange PK: test with existing rows
--

ALTER TABLE temporal_mltrng DROP CONSTRAINT temporal_mltrng_pk
;
ANALYZE pg_temp_2.pg_temp_27071_2
;
ANALYZE public.nv_child_2009
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "hword" WITH "english_stem"
;
DROP TABLE truncme
;
CREATE FUNCTION lca(ltree,ltree,ltree,ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE index stats_test_idx1 on stats_test_tab1(a)
;
CREATE TABLE fktable (
	id		INT4 PRIMARY KEY,
	fk		INT4 REFERENCES pktable DEFERRABLE INITIALLY DEFERRED
)
;
-- fail

drop function anyctest(anycompatible, anycompatible)
;
create function namedparmcursor_test9(p1 int) returns int4 as $$
declare
  c1 cursor (p1 int, p2 int, debug int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2
      and four = debug;
  p2 int4 := 1006;
  n int4;
begin
  open c1 (p1 := p1, p2 := p2, debug := 2);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
-- Create some more partitions following the above pattern of descending bound
-- order, but let's make the situation a bit more complex by having the
-- attribute numbers of the columns vary from their parent partition.
CREATE TABLE part_c_100_200 (e varchar, c numeric, a text, b bigint, d int) PARTITION BY range (abs(d))
;
CREATE TABLE test_like_4a (LIKE test_like_4)
;
ALTER TABLE pg_temp_9.pg_temp_27112_2 ADD COLUMN newdata pg_temp_9.pg_temp_27112
;
-- SET LOCAL is restricted by a function SET option
create or replace function myfunc(int) returns text as $$
begin
  set local work_mem = '2MB';
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
DROP TRIGGER IF EXISTS foo ON no_such_schema.bar
;
--    P    N        P    N
-- should ERROR: tfnp(anyarray, anyelement) not matched by tfnp(int[],int)
CREATE AGGREGATE myaggp15a(BASETYPE = anyelement, SFUNC = tfnp,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
DROP TABLE gencols
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] ALTER TABLE table_dropped_index_no_pk REPLICA IDENTITY
  USING INDEX table_dropped_index_no_pk_idx
;
----------
[CREATE_PROG_OBJ] create function f1(x anyelement) returns anyarray as $$
begin
  return array[x + 1, x + 2];
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION boo(int) RETURNS int IMMUTABLE STRICT LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'boo: %', $1; RETURN $1; END; $$
;
----------
[DROP_TABLE] DROP TABLE pgss_dml_tab
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
     CONNECTION 'port=26864 host=/tmp/GR2H9fVooJ dbname=postgres'
     PUBLICATION tap_pub
     WITH (copy_data = off)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_result" (
    "id1" integer,
    "id2" integer,
    "result" numeric(210,10)
)
;
----------
[CREATE_TABLE] CREATE TABLE originated (id int)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE bool_test(
  b1 BOOL,
  b2 BOOL,
  b3 BOOL,
  b4 BOOL)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_db_role_setting
;
----------
[DROP_PROG_OBJ] DROP FUNCTION ptest1(text)
;
----------
[ALTER_CATALOG] ALTER DATABASE "contrib_regression" SET auto_explain.bogus = 1
;
----------
[CREATE_TABLE] create foreign table grem1 (
  a int,
  b int generated always as (a * 2) stored)
  server loopback options(table_name 'gloc1')
;
----------
[DROP_TABLE] drop table parted_pk_detach_test, parted_pk_detach_test1
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default ADD TABLE pub_test.testpub_nopk
;
----------
[CREATE_TYPE] create type testtype1 as (a int, b int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub4 CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION regress_pub4 WITH (failover = true)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float4_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] -- mcv with pass-by-ref fixlen types, e.g. uuid
CREATE TABLE mcv_lists_uuid (
    a UUID,
    b UUID,
    c UUID
)
WITH (autovacuum_enabled = off)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW bug18634v AS
  SELECT * FROM bug18634t WHERE EXISTS (SELECT 1 FROM bug18634t)
;
----------
[CREATE_TABLE] CREATE TABLE multixact_conflict (a int primary key)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD AGGREGATE newavg(int4)
;
----------
[CREATE_TABLE] create temp table recur2 (f1 int, f2 recur1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt2 ALTER c2 SET NOT NULL
;
----------
[ALTER_TABLE] ALTER TABLE table_dropped_index_with_pk REPLICA IDENTITY
  USING INDEX table_dropped_index_with_pk_idx
;
----------
[ADMIN_TABLE] VACUUM FULL iamalargetable
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i13'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_addltree(ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER regress_reindex_start
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fk_notpartitioned_fk VALIDATE CONSTRAINT fk_notpartitioned_fk_a_b_fkey
;
----------
[ADMIN_TABLE] ANALYZE part1, part2
;
----------
[CREATE_PROG_OBJ] -- the same with the versioned language name
CREATE FUNCTION test1n(val hstore) RETURNS int
LANGUAGE plpython2u
TRANSFORM FOR TYPE hstore
AS $$
assert isinstance(val, dict)
plpy.info(sorted(val.items()))
return len(val)
$$
;
----------
[CREATE_TABLE] create table sch1.t1(c1 int)
;
----------
[CREATE_TABLE] -- check that using any of these functions with a partitioned table or index
-- would fail
create table test_partitioned (a int) partition by range (a)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] --
-- multirange PK: test with existing rows
--

ALTER TABLE temporal_mltrng DROP CONSTRAINT temporal_mltrng_pk
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_27071_2
;
----------
[ADMIN_TABLE] ANALYZE public.nv_child_2009
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "hword" WITH "english_stem"
;
----------
[DROP_TABLE] DROP TABLE truncme
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lca(ltree,ltree,ltree,ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE index stats_test_idx1 on stats_test_tab1(a)
;
----------
[CREATE_TABLE] CREATE TABLE fktable (
	id		INT4 PRIMARY KEY,
	fk		INT4 REFERENCES pktable DEFERRABLE INITIALLY DEFERRED
)
;
----------
[DROP_PROG_OBJ] -- fail

drop function anyctest(anycompatible, anycompatible)
;
----------
[CREATE_PROG_OBJ] create function namedparmcursor_test9(p1 int) returns int4 as $$
declare
  c1 cursor (p1 int, p2 int, debug int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2
      and four = debug;
  p2 int4 := 1006;
  n int4;
begin
  open c1 (p1 := p1, p2 := p2, debug := 2);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
----------
[CREATE_TABLE] -- Create some more partitions following the above pattern of descending bound
-- order, but let's make the situation a bit more complex by having the
-- attribute numbers of the columns vary from their parent partition.
CREATE TABLE part_c_100_200 (e varchar, c numeric, a text, b bigint, d int) PARTITION BY range (abs(d))
;
----------
[CREATE_TABLE] CREATE TABLE test_like_4a (LIKE test_like_4)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_27112_2 ADD COLUMN newdata pg_temp_9.pg_temp_27112
;
----------
[CREATE_PROG_OBJ] -- SET LOCAL is restricted by a function SET option
create or replace function myfunc(int) returns text as $$
begin
  set local work_mem = '2MB';
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
----------
[DROP_TRIGGER] DROP TRIGGER IF EXISTS foo ON no_such_schema.bar
;
----------
[CREATE_PROG_OBJ] --    P    N        P    N
-- should ERROR: tfnp(anyarray, anyelement) not matched by tfnp(int[],int)
CREATE AGGREGATE myaggp15a(BASETYPE = anyelement, SFUNC = tfnp,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
----------
[DROP_TABLE] DROP TABLE gencols
;
