drop table pp_intrangepart
;
--should fail
create function mr_inoutparam_fail(inout i anyelement, out r anymultirange)
  as $$ select $1, '[1,10]' $$ language sql
;
create function xfloat4in(cstring) returns xfloat4 immutable strict
  language internal as 'int4in'
;
create table inh_child3 () inherits (inh_child1)
;
analyze loct1
;
CREATE TABLE colors (
	id INT GENERATED ALWAYS AS IDENTITY (INCREMENT BY 3 START WITH 7),
	name VARCHAR(10)
)
;
CREATE TABLE partitioned (
	a	int
) PARTITION BY RANGE ((partitioned))
;
CREATE TABLE test1 (a text)
;
create or replace function forc_bad() returns void as $$
declare
  c refcursor;
begin
  for r in c loop
    raise notice '%', r.i;
  end loop;
end;
$$ language plpgsql
;
DROP TRIGGER test_trigger_exists ON no_such_table
;
CREATE INDEX idx_pgvector_items_hnsw_l2 ON split_pgvector_probe.items USING hnsw (embedding vector_l2_ops) WITH (m = 16, ef_construction = 64)
;
CREATE SUBSCRIPTION testsub1 CONNECTION 'port=25079 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION testpub1
;
create table t1 ( id integer , t text , d1 numeric , d2 float8 , c char ( 10 ) )
;
CREATE TABLE "public"."f_star" (
    "class" character(1),
    "aa" integer,
    "cc" "name",
    "ee" smallint,
    "ff" "polygon",
    "f" integer,
    "e" integer,
    "a" "text"
)
;
-- Verify ON UPDATE/DELETE behavior
CREATE SCHEMA fkpart6
;
ALTER TABLE ONLY "public"."prt2_e" ATTACH PARTITION "public"."prt2_e_p2" FOR VALUES FROM (250) TO (500)
;
COMMENT ON CONSTRAINT ctlt1_a_check ON ctlt1 IS 't1_a_check'
;
DROP FUNCTION warn(TEXT)
;
CREATE SCHEMA fkpart7
  CREATE TABLE pkpart (a int) PARTITION BY LIST (a)
  CREATE TABLE pkpart1 PARTITION OF pkpart FOR VALUES IN (1)
;
CREATE POLICY p2 ON dump_test.test_table
						   FOR SELECT TO regress_dump_test_role USING (true)
;
DROP FUNCTION test_ambiguous_funcname(text)
;
CREATE TABLE truncpart_2_d PARTITION OF truncpart_2 DEFAULT
;
-- reassign an already-typed table
ALTER TABLE tt7 NOT OF
;
CREATE TABLE tgt (id int PRIMARY KEY, val int)
;
create table hp1 partition of hp for values with (modulus 4, remainder 1)
;
ALTER TABLE has_volatile ADD col1 int
;
CREATE FUNCTION tfnp(int[],int) RETURNS int[] AS
'select $1 || $2' LANGUAGE SQL
;
CREATE TABLE tab3 ("a'" int PRIMARY KEY, "c'" int)
;
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2
  FOR VALUES IN (1600)
;
CREATE TABLE tab1 (a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
CREATE TABLE FLOAT8_TBL(f1 float8)
;
/*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_subscription_rel
;
CREATE TYPE split_domain_type_next.shell_type
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_statistic
;
CREATE PROCEDURE test_proc8c(INOUT a int, INOUT b int, INOUT c int DEFAULT 11)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %, c: %', a, b, c;
  a := a * 10;
  b := b + 10;
  c := c * -10;
END;
$$
;
CREATE TABLE "public"."rule_merge1" (
    "a" integer,
    "b" "text"
)
;
DROP AGGREGATE testns.agg1(int)
;
CREATE TABLE secondparent (tomorrow date default  now() :: date  +  1)
;
ALTER TABLE ONLY tab1_2_2 ENABLE REPLICA TRIGGER sub1_tab1_2_2_log_op_trigger
;
CREATE TYPE "Colors" AS ENUM ('red', 'green', 'blue')
;
drop function dfunc(varchar, numeric, date)
;
CREATE TABLE "public"."rtest_t9" (
    "a" integer,
    "b" "text"
)
;
drop index fruit_index
;
alter table at_partitioned attach partition at_part_2 for values from (1000) to (2000)
;
create table has_hole(f1 int, f2 int, f3 int)
;
COMMENT ON SCHEMA public IS NULL
;
CREATE TABLE orders (id INTEGER PRIMARY KEY,
                        name varchar,
                        user_id INTEGER REFERENCES users (id))
;
CREATE FOREIGN TABLE ft1 (c1 integer NOT NULL) SERVER s0
;
ALTER TABLE part_b DROP CONSTRAINT check_b
;
------------------------------------------------------------------------------------------
[DROP_TABLE] drop table pp_intrangepart
;
----------
[CREATE_PROG_OBJ] --should fail
create function mr_inoutparam_fail(inout i anyelement, out r anymultirange)
  as $$ select $1, '[1,10]' $$ language sql
;
----------
[CREATE_PROG_OBJ] create function xfloat4in(cstring) returns xfloat4 immutable strict
  language internal as 'int4in'
;
----------
[CREATE_TABLE] create table inh_child3 () inherits (inh_child1)
;
----------
[ADMIN_TABLE] analyze loct1
;
----------
[CREATE_TABLE] CREATE TABLE colors (
	id INT GENERATED ALWAYS AS IDENTITY (INCREMENT BY 3 START WITH 7),
	name VARCHAR(10)
)
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a	int
) PARTITION BY RANGE ((partitioned))
;
----------
[CREATE_TABLE] CREATE TABLE test1 (a text)
;
----------
[CREATE_PROG_OBJ] create or replace function forc_bad() returns void as $$
declare
  c refcursor;
begin
  for r in c loop
    raise notice '%', r.i;
  end loop;
end;
$$ language plpgsql
;
----------
[DROP_TRIGGER] DROP TRIGGER test_trigger_exists ON no_such_table
;
----------
[ADD_INDEX] CREATE INDEX idx_pgvector_items_hnsw_l2 ON split_pgvector_probe.items USING hnsw (embedding vector_l2_ops) WITH (m = 16, ef_construction = 64)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub1 CONNECTION 'port=25079 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION testpub1
;
----------
[CREATE_TABLE] create table t1 ( id integer , t text , d1 numeric , d2 float8 , c char ( 10 ) )
;
----------
[CREATE_TABLE] CREATE TABLE "public"."f_star" (
    "class" character(1),
    "aa" integer,
    "cc" "name",
    "ee" smallint,
    "ff" "polygon",
    "f" integer,
    "e" integer,
    "a" "text"
)
;
----------
[CREATE_SCHEMA] -- Verify ON UPDATE/DELETE behavior
CREATE SCHEMA fkpart6
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_e" ATTACH PARTITION "public"."prt2_e_p2" FOR VALUES FROM (250) TO (500)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT ctlt1_a_check ON ctlt1 IS 't1_a_check'
;
----------
[DROP_PROG_OBJ] DROP FUNCTION warn(TEXT)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart7
  CREATE TABLE pkpart (a int) PARTITION BY LIST (a)
  CREATE TABLE pkpart1 PARTITION OF pkpart FOR VALUES IN (1)
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON dump_test.test_table
						   FOR SELECT TO regress_dump_test_role USING (true)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_ambiguous_funcname(text)
;
----------
[CREATE_TABLE] CREATE TABLE truncpart_2_d PARTITION OF truncpart_2 DEFAULT
;
----------
[ALTER_TABLE] -- reassign an already-typed table
ALTER TABLE tt7 NOT OF
;
----------
[CREATE_TABLE] CREATE TABLE tgt (id int PRIMARY KEY, val int)
;
----------
[CREATE_TABLE] create table hp1 partition of hp for values with (modulus 4, remainder 1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE has_volatile ADD col1 int
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tfnp(int[],int) RETURNS int[] AS
'select $1 || $2' LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE tab3 ("a'" int PRIMARY KEY, "c'" int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2
  FOR VALUES IN (1600)
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE FLOAT8_TBL(f1 float8)
;
----------
[CREATE_POLICY] /*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_subscription_rel
;
----------
[CREATE_TYPE] CREATE TYPE split_domain_type_next.shell_type
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_statistic
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc8c(INOUT a int, INOUT b int, INOUT c int DEFAULT 11)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %, c: %', a, b, c;
  a := a * 10;
  b := b + 10;
  c := c * -10;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rule_merge1" (
    "a" integer,
    "b" "text"
)
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE testns.agg1(int)
;
----------
[CREATE_TABLE] CREATE TABLE secondparent (tomorrow date default  now() :: date  +  1)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY tab1_2_2 ENABLE REPLICA TRIGGER sub1_tab1_2_2_log_op_trigger
;
----------
[CREATE_TYPE] CREATE TYPE "Colors" AS ENUM ('red', 'green', 'blue')
;
----------
[DROP_PROG_OBJ] drop function dfunc(varchar, numeric, date)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_t9" (
    "a" integer,
    "b" "text"
)
;
----------
[DROP_INDEX] drop index fruit_index
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table at_partitioned attach partition at_part_2 for values from (1000) to (2000)
;
----------
[CREATE_TABLE] create table has_hole(f1 int, f2 int, f3 int)
;
----------
[COMMENT_SCHEMA] COMMENT ON SCHEMA public IS NULL
;
----------
[CREATE_TABLE] CREATE TABLE orders (id INTEGER PRIMARY KEY,
                        name varchar,
                        user_id INTEGER REFERENCES users (id))
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft1 (c1 integer NOT NULL) SERVER s0
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE part_b DROP CONSTRAINT check_b
;
