CREATE FUNCTION set_val_in_shmem(val INT) RETURNS VOID
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE UNIQUE INDEX mvtest_mv_v_ii ON mvtest_mv_v (ii)
;
CREATE VIEW ro_view12 AS SELECT * FROM generate_series(1, 10) AS g(a)
;
ANALYZE stats_ext_tbl
;
ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.9........"
;
DROP EVENT TRIGGER regress_event_trigger_report_end
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
-- finally success
CREATE EXTENSION test_ext1 SCHEMA has$dollar CASCADE
;
ALTER FUNCTION gbt_int8_consistent(internal, bigint, smallint, oid, internal) PARALLEL SAFE
;
CREATE TABLE mvtest_boxes (id serial primary key, b box)
;
drop table cnn_uq
;
alter table atacc1 add constraint foo check (test>0) no inherit
;
-- changes name of an attribute recursively
ALTER TABLE fd_pt1 RENAME COLUMN c1 TO f1
;
CREATE TABLE evttrig.part_1_10 PARTITION OF evttrig.parted (id)
  FOR VALUES FROM (1) TO (10)
;
ALTER TABLE parent_tbl DETACH PARTITION foreign_tbl
;
CREATE TABLE pagg_tab_para_p1 PARTITION OF pagg_tab_para FOR VALUES FROM (0) TO (10)
;
drop domain domainvarchar restrict
;
COMMENT ON COLLATION test0
					   IS 'comment on test0 collation'
;
create temp table p2 partition of p for values in (2)
;
VACUUM vactst
;
DROP TABLE constraint_rename_test
;
CREATE TRIGGER test_oat_trigger BEFORE INSERT ON test_oat_schema.test_oat_tab
  FOR EACH STATEMENT EXECUTE FUNCTION test_oat_schema.test_trigger()
;
ALTER OPERATOR FAMILY gist_bpchar_ops USING gist ADD
	OPERATOR	6	<> (bpchar, bpchar) ,
	FUNCTION	9 (bpchar, bpchar) gbt_var_fetch (internal)
;
--OK

ALTER TABLE y1 ENABLE ROW LEVEL SECURITY
;
CREATE TABLE test_inh_check_child() INHERITS(test_inh_check)
;
create trigger parenttrig after insert on child
for each row execute procedure f()
;
ALTER EXTENSION cube DROP function g_cube_compress(pg_catalog.internal)
;
create table child_0_10 partition of parent_tab
  for values from (0) to (10)
;
-- check reference to partitioned table's rowtype in partition descriptor
create table partitioned (a int, b int)
  partition by list ((row(a, b)::partitioned))
;
CREATE TABLE fk_partitioned_fk_2 (b int, c text, a int,
	FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk ON UPDATE CASCADE ON DELETE CASCADE)
;
/*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2016
 *
 * Copyright (c) 2003-2022, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 5.1
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
CREATE SCHEMA pubme2 CREATE TABLE t0 (c int, d int)
;
CREATE TEMP TABLE PKTABLE (ptest1 numeric PRIMARY KEY)
;
ALTER TABLE nonpartitioned INHERIT partitioned
;
CREATE FUNCTION city_delete() RETURNS trigger LANGUAGE plpgsql AS $$
begin
    DELETE FROM city_table WHERE city_id = OLD.city_id;
    if NOT FOUND then RETURN NULL; end if;
    RETURN OLD;
end;
$$
;
create table bool_lp (b bool) partition by list(b)
;
CREATE FUNCTION isneq(ismn13, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER FUNCTION gbt_oid_distance(internal, oid, smallint, oid, internal) PARALLEL SAFE
;
create trigger child1_insert_trig
  after insert on child1 referencing new table as new_table
  for each statement execute procedure dump_insert()
;
create domain array_of_recur1 as recur1[]
;
create aggregate sum2(int8,int8) (
   sfunc = sum3, stype = int8,
   initcond = '0'
)
;
CREATE TABLE ext_stats_test (x text, y int, z int)
;
create function least_accum(anyelement, variadic anyarray)
returns anyelement language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
CREATE FUNCTION gin_compare_prefix_float4(float4, float4, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE attmp3 VALIDATE CONSTRAINT b_le_20
;
ALTER FUNCTION gbt_bytea_union(internal, internal) PARALLEL SAFE
;
DROP OPERATOR ==== (real, boolean)
;
--
-- Test for facilities of security label
--
CREATE EXTENSION dummy_seclabel
;
alter text search configuration alter1.cfg set schema alter2
;
CREATE TEMP SEQUENCE myseq3
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION set_val_in_shmem(val INT) RETURNS VOID
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX mvtest_mv_v_ii ON mvtest_mv_v (ii)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view12 AS SELECT * FROM generate_series(1, 10) AS g(a)
;
----------
[ADMIN_TABLE] ANALYZE stats_ext_tbl
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.9........"
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER regress_event_trigger_report_end
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
----------
[CREATE_LIBRARY] -- finally success
CREATE EXTENSION test_ext1 SCHEMA has$dollar CASCADE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int8_consistent(internal, bigint, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE mvtest_boxes (id serial primary key, b box)
;
----------
[DROP_TABLE] drop table cnn_uq
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint foo check (test>0) no inherit
;
----------
[RENAME_COLUMN] -- changes name of an attribute recursively
ALTER TABLE fd_pt1 RENAME COLUMN c1 TO f1
;
----------
[CREATE_TABLE] CREATE TABLE evttrig.part_1_10 PARTITION OF evttrig.parted (id)
  FOR VALUES FROM (1) TO (10)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE parent_tbl DETACH PARTITION foreign_tbl
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_para_p1 PARTITION OF pagg_tab_para FOR VALUES FROM (0) TO (10)
;
----------
[DROP_TYPE] drop domain domainvarchar restrict
;
----------
[ALTER_POLICY] COMMENT ON COLLATION test0
					   IS 'comment on test0 collation'
;
----------
[CREATE_TABLE] create temp table p2 partition of p for values in (2)
;
----------
[ADMIN_TABLE] VACUUM vactst
;
----------
[DROP_TABLE] DROP TABLE constraint_rename_test
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER test_oat_trigger BEFORE INSERT ON test_oat_schema.test_oat_tab
  FOR EACH STATEMENT EXECUTE FUNCTION test_oat_schema.test_trigger()
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bpchar_ops USING gist ADD
	OPERATOR	6	<> (bpchar, bpchar) ,
	FUNCTION	9 (bpchar, bpchar) gbt_var_fetch (internal)
;
----------
[ALTER_TABLE] --OK

ALTER TABLE y1 ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] CREATE TABLE test_inh_check_child() INHERITS(test_inh_check)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parenttrig after insert on child
for each row execute procedure f()
;
----------
[ALTER_LIBRARY] ALTER EXTENSION cube DROP function g_cube_compress(pg_catalog.internal)
;
----------
[CREATE_TABLE] create table child_0_10 partition of parent_tab
  for values from (0) to (10)
;
----------
[CREATE_TABLE] -- check reference to partitioned table's rowtype in partition descriptor
create table partitioned (a int, b int)
  partition by list ((row(a, b)::partitioned))
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_2 (b int, c text, a int,
	FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk ON UPDATE CASCADE ON DELETE CASCADE)
;
----------
[CREATE_SCHEMA] /*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2016
 *
 * Copyright (c) 2003-2022, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 5.1
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
----------
[CREATE_SCHEMA] CREATE SCHEMA pubme2 CREATE TABLE t0 (c int, d int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE PKTABLE (ptest1 numeric PRIMARY KEY)
;
----------
[ALTER_TABLE] ALTER TABLE nonpartitioned INHERIT partitioned
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION city_delete() RETURNS trigger LANGUAGE plpgsql AS $$
begin
    DELETE FROM city_table WHERE city_id = OLD.city_id;
    if NOT FOUND then RETURN NULL; end if;
    RETURN OLD;
end;
$$
;
----------
[CREATE_TABLE] create table bool_lp (b bool) partition by list(b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ismn13, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_oid_distance(internal, oid, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child1_insert_trig
  after insert on child1 referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[CREATE_TYPE] create domain array_of_recur1 as recur1[]
;
----------
[CREATE_PROG_OBJ] create aggregate sum2(int8,int8) (
   sfunc = sum3, stype = int8,
   initcond = '0'
)
;
----------
[CREATE_TABLE] CREATE TABLE ext_stats_test (x text, y int, z int)
;
----------
[CREATE_PROG_OBJ] create function least_accum(anyelement, variadic anyarray)
returns anyelement language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_float4(float4, float4, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE attmp3 VALIDATE CONSTRAINT b_le_20
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bytea_union(internal, internal) PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ==== (real, boolean)
;
----------
[CREATE_LIBRARY] --
-- Test for facilities of security label
--
CREATE EXTENSION dummy_seclabel
;
----------
[ALTER_POLICY] alter text search configuration alter1.cfg set schema alter2
;
----------
[CREATE_SEQUENCE] CREATE TEMP SEQUENCE myseq3
;
