CREATE FUNCTION gin_extract_query_inet(inet, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE FUNCTION isnne(ismn, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION func_stats_crash_discard1() RETURNS VOID AS 'select 2;' LANGUAGE SQL IMMUTABLE
;
ANALYZE pg_catalog.pg_cast
;
create unique index comp_key_index on insertconflicttest(key, fruit)
;
CREATE VIEW v7 AS SELECT * FROM base_table WHERE NOT EXISTS (SELECT 1 FROM base_table2)
;
CREATE TEMP TABLE pg_temp_95.pg_temp_33804_2 (tid pg_catalog.tid)
;
ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (isbn, ean13),
	OPERATOR 1  < (isbn, isbn13),
	OPERATOR 2  <= (isbn, ean13),
	OPERATOR 2  <= (isbn, isbn13),
	OPERATOR 3  = (isbn, ean13),
	OPERATOR 3  = (isbn, isbn13),
	OPERATOR 4  >= (isbn, ean13),
	OPERATOR 4  >= (isbn, isbn13),
	OPERATOR 5  > (isbn, ean13),
	OPERATOR 5  > (isbn, isbn13),
	FUNCTION 1  btisbncmp(isbn, ean13),
	FUNCTION 1  btisbncmp(isbn, isbn13)
;
ANALYZE public.plt1_p2
;
CREATE TABLE tbspace_reindex_part_0 PARTITION OF tbspace_reindex_part
  FOR VALUES FROM (0) TO (10) PARTITION BY list (c2)
;
/* contrib/pg_stat_statements/pg_stat_statements--1.8--1.9.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--- Define pg_stat_statements_info
CREATE FUNCTION pg_stat_statements_info(
    OUT dealloc bigint,
    OUT stats_reset timestamp with time zone
)
RETURNS record
AS '$libdir/pg_stat_statements'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
CREATE COLLATION testcoll_shifted (provider = icu, locale = '@colAlternate=shifted')
;
CREATE FUNCTION cube_out(cube)
RETURNS cstring
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
drop table test1
;
CREATE VIEW pg_stat_subscription_stats AS
    SELECT
        ss.subid,
        s.subname,
        ss.apply_error_count,
        ss.sync_error_count,
        ss.confl_insert_exists,
        ss.confl_update_origin_differs,
        ss.confl_update_exists,
        ss.confl_update_missing,
        ss.confl_delete_origin_differs,
        ss.confl_delete_missing,
        ss.confl_multiple_unique_conflicts,
        ss.stats_reset
    FROM pg_subscription as s,
         pg_stat_get_subscription_stats(s.oid) as ss
;
-- Clean up
DROP TABLE lotest_stash_values
;
ANALYZE pg_temp_11.pg_temp_26628
;
-- This test checks that proper typmods are assigned in a multi-row VALUES

CREATE VIEW tt1 AS
  SELECT * FROM (
    VALUES
       ('abc'::varchar(3), '0123456789', 42, 'abcd'::varchar(4)),
       ('0123456789', 'abc'::varchar(3), 42.12, 'abc'::varchar(4))
  ) vv(a,b,c,d)
;
CREATE TABLE am_partitioned_6p PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 6) PARTITION BY hash(y)
;
ALTER TABLE tbl1 ALTER COLUMN val2 TYPE float
;
truncate regular_table
;
-- Test data

-- all combinations of four boolean values
create table booleans as
select
  case i%3 when 0 then true when 1 then false else null end as x,
  case (i/3)%3 when 0 then true when 1 then false else null end as y,
  case (i/9)%3 when 0 then true when 1 then false else null end as z,
  case (i/27)%3 when 0 then true when 1 then false else null end as w
from generate_series(0, 3*3*3*3-1) i
;
CREATE TABLE test_seg (s seg)
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26571_2 (tid pg_catalog.tid)
;
CALL sum_one(199)
;
CREATE SUBSCRIPTION sub3 CONNECTION 'port=25930 host=/tmp/1gCCONH3rb dbname=postgres' PUBLICATION pub3
;
ALTER TABLE pg_toast.pg_toast_27579 RENAME TO reind_con_toast
;
DROP TABLE distinct_tbl
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_authid
;
-- interval check

CREATE TABLE intervaltmp (a interval)
;
ALTER TABLE regtest_table SET (fillfactor = 75)
;
CREATE TABLE tab4 (a INT PRIMARY KEY, b test_typ, c int, d text)
;
-- char check

CREATE TABLE vchartmp (a varchar(32))
;
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 25, REMAINDER 3)
;
ALTER INDEX dummy_test_idx SET (option_real = 4)
;
VACUUM (FULL) pg_catalog.pg_conversion
;
create view bpchar_view as
  select
    f1, f1::character(14) as f114, f1::bpchar as f1n,
    f2, f2::character(14) as f214, f2::bpchar as f2n
  from bpchar_tbl
;
ANALYZE public.inet_tbl
;
alter table only trigger_parted_p2 disable trigger aft_row
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.datetime("HH:MI") < $.datetime("YY-MM-DD HH:MI"))'))
;
DROP TABLE tbl_gist
;
DROP PROCEDURE test_proc3
;
ALTER RULE "_RETURN" ON rule_v1 RENAME TO abc
;
CREATE DATABASE template_db IS_TEMPLATE = true
;
-- Test ispell dictionary with hunspell affix with FLAG num in configuration
ALTER TEXT SEARCH CONFIGURATION hunspell_tst ALTER MAPPING
	REPLACE hunspell_long WITH hunspell_num
;
ALTER TABLE pg_temp.z2 ADD CONSTRAINT z2_aa_b_key UNIQUE (aa, b)
;
CREATE FUNCTION f_leak (text)
       RETURNS bool LANGUAGE 'plpgsql' COST 0.0000001
       AS 'BEGIN RAISE NOTICE ''f_leak => %'', $1; RETURN true; END'
;
-- conflict
alter table emp rename column salary to ctid
;
drop table clstr_temp
;
-- Verify that constraint errors across partition root / child are
-- handled correctly (Bug #16293)
CREATE TABLE errtst_parent (
    partid int not null,
    shdata int not null,
    data int NOT NULL DEFAULT 0,
    CONSTRAINT shdata_small CHECK(shdata < 3)
) PARTITION BY RANGE (partid)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_inet(inet, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ismn, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_stats_crash_discard1() RETURNS VOID AS 'select 2;' LANGUAGE SQL IMMUTABLE
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_cast
;
----------
[ADD_INDEX] create unique index comp_key_index on insertconflicttest(key, fruit)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v7 AS SELECT * FROM base_table WHERE NOT EXISTS (SELECT 1 FROM base_table2)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_95.pg_temp_33804_2 (tid pg_catalog.tid)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (isbn, ean13),
	OPERATOR 1  < (isbn, isbn13),
	OPERATOR 2  <= (isbn, ean13),
	OPERATOR 2  <= (isbn, isbn13),
	OPERATOR 3  = (isbn, ean13),
	OPERATOR 3  = (isbn, isbn13),
	OPERATOR 4  >= (isbn, ean13),
	OPERATOR 4  >= (isbn, isbn13),
	OPERATOR 5  > (isbn, ean13),
	OPERATOR 5  > (isbn, isbn13),
	FUNCTION 1  btisbncmp(isbn, ean13),
	FUNCTION 1  btisbncmp(isbn, isbn13)
;
----------
[ADMIN_TABLE] ANALYZE public.plt1_p2
;
----------
[CREATE_TABLE] CREATE TABLE tbspace_reindex_part_0 PARTITION OF tbspace_reindex_part
  FOR VALUES FROM (0) TO (10) PARTITION BY list (c2)
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_stat_statements/pg_stat_statements--1.8--1.9.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--- Define pg_stat_statements_info
CREATE FUNCTION pg_stat_statements_info(
    OUT dealloc bigint,
    OUT stats_reset timestamp with time zone
)
RETURNS record
AS '$libdir/pg_stat_statements'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE COLLATION testcoll_shifted (provider = icu, locale = '@colAlternate=shifted')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_out(cube)
RETURNS cstring
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] drop table test1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_subscription_stats AS
    SELECT
        ss.subid,
        s.subname,
        ss.apply_error_count,
        ss.sync_error_count,
        ss.confl_insert_exists,
        ss.confl_update_origin_differs,
        ss.confl_update_exists,
        ss.confl_update_missing,
        ss.confl_delete_origin_differs,
        ss.confl_delete_missing,
        ss.confl_multiple_unique_conflicts,
        ss.stats_reset
    FROM pg_subscription as s,
         pg_stat_get_subscription_stats(s.oid) as ss
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE lotest_stash_values
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26628
;
----------
[CREATE_VIEW(SELECT)] -- This test checks that proper typmods are assigned in a multi-row VALUES

CREATE VIEW tt1 AS
  SELECT * FROM (
    VALUES
       ('abc'::varchar(3), '0123456789', 42, 'abcd'::varchar(4)),
       ('0123456789', 'abc'::varchar(3), 42.12, 'abc'::varchar(4))
  ) vv(a,b,c,d)
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned_6p PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 6) PARTITION BY hash(y)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl1 ALTER COLUMN val2 TYPE float
;
----------
[TRUNCATE_TABLE] truncate regular_table
;
----------
[CREATE_TABLE|SELECT] -- Test data

-- all combinations of four boolean values
create table booleans as
select
  case i%3 when 0 then true when 1 then false else null end as x,
  case (i/3)%3 when 0 then true when 1 then false else null end as y,
  case (i/9)%3 when 0 then true when 1 then false else null end as z,
  case (i/27)%3 when 0 then true when 1 then false else null end as w
from generate_series(0, 3*3*3*3-1) i
;
----------
[CREATE_TABLE] CREATE TABLE test_seg (s seg)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26571_2 (tid pg_catalog.tid)
;
----------
[CALL_PROG_OBJ] CALL sum_one(199)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub3 CONNECTION 'port=25930 host=/tmp/1gCCONH3rb dbname=postgres' PUBLICATION pub3
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27579 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE distinct_tbl
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_authid
;
----------
[CREATE_TABLE] -- interval check

CREATE TABLE intervaltmp (a interval)
;
----------
[ALTER_TABLE] ALTER TABLE regtest_table SET (fillfactor = 75)
;
----------
[CREATE_TABLE] CREATE TABLE tab4 (a INT PRIMARY KEY, b test_typ, c int, d text)
;
----------
[CREATE_TABLE] -- char check

CREATE TABLE vchartmp (a varchar(32))
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 25, REMAINDER 3)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_real = 4)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_conversion
;
----------
[CREATE_VIEW(SELECT)] create view bpchar_view as
  select
    f1, f1::character(14) as f114, f1::bpchar as f1n,
    f2, f2::character(14) as f214, f2::bpchar as f2n
  from bpchar_tbl
;
----------
[ADMIN_TABLE] ANALYZE public.inet_tbl
;
----------
[ALTER_TABLE] alter table only trigger_parted_p2 disable trigger aft_row
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.datetime("HH:MI") < $.datetime("YY-MM-DD HH:MI"))'))
;
----------
[DROP_TABLE] DROP TABLE tbl_gist
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE test_proc3
;
----------
[ALTER_POLICY] ALTER RULE "_RETURN" ON rule_v1 RENAME TO abc
;
----------
[CREATE_CATALOG] CREATE DATABASE template_db IS_TEMPLATE = true
;
----------
[ALTER_POLICY] -- Test ispell dictionary with hunspell affix with FLAG num in configuration
ALTER TEXT SEARCH CONFIGURATION hunspell_tst ALTER MAPPING
	REPLACE hunspell_long WITH hunspell_num
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_temp.z2 ADD CONSTRAINT z2_aa_b_key UNIQUE (aa, b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f_leak (text)
       RETURNS bool LANGUAGE 'plpgsql' COST 0.0000001
       AS 'BEGIN RAISE NOTICE ''f_leak => %'', $1; RETURN true; END'
;
----------
[RENAME_COLUMN] -- conflict
alter table emp rename column salary to ctid
;
----------
[DROP_TABLE] drop table clstr_temp
;
----------
[CREATE_TABLE] -- Verify that constraint errors across partition root / child are
-- handled correctly (Bug #16293)
CREATE TABLE errtst_parent (
    partid int not null,
    shdata int not null,
    data int NOT NULL DEFAULT 0,
    CONSTRAINT shdata_small CHECK(shdata < 3)
) PARTITION BY RANGE (partid)
;
