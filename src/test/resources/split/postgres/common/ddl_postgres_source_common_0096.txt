DROP OPERATOR FAMILY alt_opf17 USING btree
;
create temp table pendtest (ts tsvector)
;
-- old bug with not dropping COLLATE when coercing to non-collatable type
CREATE VIEW collate_on_int AS
SELECT c1+1 AS c1p FROM
  (SELECT ('4' COLLATE "C")::INT AS c1) ss
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS EXECUTE ctas_ine_query
;
DROP SCHEMA split_trigger_probe CASCADE
;
CREATE SCHEMA "regress_schema_2"
;
create function scope_test() returns int as $$
declare x int := 42;
begin
  declare y int := x + 1;
          x int := x + 2;
  begin
    return x * 100 + y;
  end;
end;
$$ language plpgsql
;
DROP TABLE part_drop_index_locking
;
CREATE DATABASE inprimary
;
CREATE FUNCTION functest_A_1(text, date) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 = ''abcd'' AND $2 > ''2001-01-01'''
;
ANALYZE public.mlparted4
;
COMMENT ON FUNCTION _int_same(_int4, _int4) IS 'same as'
;
CREATE TABLE "regress_indexing"."pk" (
    "a" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
CREATE DOMAIN "public"."mynums" AS numeric(4,2)[]
;
CREATE TABLE temporal_rng2 (LIKE temporal_rng INCLUDING ALL)
;
DROP FOREIGN TABLE IF EXISTS no_such_schema.foo
;
ALTER TABLE ONLY "public"."pagg_tab_m" ATTACH PARTITION "public"."pagg_tab_m_p2" FOR VALUES FROM (10, 10) TO (20, 20)
;
drop table list_parted
;
DROP RULE IF EXISTS account_update_notify_renamed ON split_trigger_probe.account_audit CASCADE
;
ALTER INDEX pg_toast.pg_toast_30423_index RENAME TO reind_con_toast_idx
;
-- Check it for a partitioned table, too
create table at_tab1 (a int, b text) partition by list(a)
;
-- also test spgist index on anyrange expression
create index on test_range_elem using spgist(int4range(i,i+10))
;
/*
 * text search configuration for greek language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * greek and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY greek_stem
	(TEMPLATE = snowball, Language = greek )
;
/* First we have to remove them from the extension */
ALTER EXTENSION pg_stat_statements DROP VIEW pg_stat_statements
;
-- should fail
alter table parted_pk_detach_test detach partition parted_pk_detach_test1
;
CREATE TYPE split_domain_type_next.mood AS ENUM ('sad', 'ok', 'happy')
;
create table part_pa_test_p1 partition of part_pa_test for values from (minvalue) to (0)
;
create table j2 (id1 int, id2 int, primary key(id1,id2))
;
CREATE TEMP TABLE bug6051_2 (i int)
;
VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) test
;
-- fail - user-defined operators are not allowed
CREATE FUNCTION testpub_rf_func1(integer, integer) RETURNS boolean AS $$ SELECT hashint4($1) > $2 $$ LANGUAGE SQL
;
create view wcowrtest_v2 as
    select *
      from wcowrtest r
      where r in (select s from sometable s where r.a = s.a)
with check option
;
DROP TABLE employees
;
ANALYZE pg_temp_10.pg_temp_26642
;
CREATE TEMPORARY TABLE just_to_create_temp_schema()
;
VACUUM ANALYZE vacparted(a,b,a)
;
CREATE TABLE regtest_ptable (a int) PARTITION BY RANGE (a)
;
create domain hstestdom1 as integer not null default 0
;
create trigger failed instead of update on parted_trig
  for each row execute procedure trigger_nothing()
;
CREATE OPERATOR >= (
  LEFTARG = int_custom,
  RIGHTARG = int_custom,
  FUNCTION = int_custom_ge,
  COMMUTATOR = <=,
  NEGATOR = <,
  RESTRICT = scalargesel,
  JOIN = scalargejoinsel
)
;
CREATE OPERATOR <% (
   leftarg = point,
   rightarg = widget,
   procedure = pt_in_widget,
   commutator = >% ,
   negator = >=%
)
;
CREATE FUNCTION isnne(isbn13, isbn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE ONLY "generated_stored_tests"."gtestx" INHERIT "generated_stored_tests"."gtest1"
;
create temp table patest1() inherits (patest0)
;
CREATE FUNCTION gbt_oid_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION float4_dist(float4, float4)
RETURNS float4
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE T ADD COLUMN c_timestamp TIMESTAMP DEFAULT '2016-09-01 12:00:00',
              ADD COLUMN c_timestamp_null TIMESTAMP,
              ALTER COLUMN c_date SET DEFAULT '2010-01-01'
;
CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=11382 host=/tmp/C6o4PTAnCK dbname=postgres' PUBLICATION tap_pub_B WITH (origin = none, copy_data = on)
;
CREATE INDEX tbspace_reindex_part_index_0 ON ONLY tbspace_reindex_part_0 (c1)
;
CREATE TABLESPACE regress_test_stats_tblspc LOCATION ''
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf17 USING btree
;
----------
[CREATE_TABLE] create temp table pendtest (ts tsvector)
;
----------
[CREATE_VIEW(SELECT)] -- old bug with not dropping COLLATE when coercing to non-collatable type
CREATE VIEW collate_on_int AS
SELECT c1+1 AS c1p FROM
  (SELECT ('4' COLLATE "C")::INT AS c1) ss
;
----------
[CREATE_TABLE|UNSAFE] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS EXECUTE ctas_ine_query
;
----------
[DROP_SCHEMA] DROP SCHEMA split_trigger_probe CASCADE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "regress_schema_2"
;
----------
[CREATE_PROG_OBJ] create function scope_test() returns int as $$
declare x int := 42;
begin
  declare y int := x + 1;
          x int := x + 2;
  begin
    return x * 100 + y;
  end;
end;
$$ language plpgsql
;
----------
[DROP_TABLE] DROP TABLE part_drop_index_locking
;
----------
[CREATE_CATALOG] CREATE DATABASE inprimary
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_A_1(text, date) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 = ''abcd'' AND $2 > ''2001-01-01'''
;
----------
[ADMIN_TABLE] ANALYZE public.mlparted4
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION _int_same(_int4, _int4) IS 'same as'
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk" (
    "a" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."mynums" AS numeric(4,2)[]
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng2 (LIKE temporal_rng INCLUDING ALL)
;
----------
[DROP_TABLE] DROP FOREIGN TABLE IF EXISTS no_such_schema.foo
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_m" ATTACH PARTITION "public"."pagg_tab_m_p2" FOR VALUES FROM (10, 10) TO (20, 20)
;
----------
[DROP_TABLE] drop table list_parted
;
----------
[DROP_POLICY] DROP RULE IF EXISTS account_update_notify_renamed ON split_trigger_probe.account_audit CASCADE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30423_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- Check it for a partitioned table, too
create table at_tab1 (a int, b text) partition by list(a)
;
----------
[ADD_INDEX] -- also test spgist index on anyrange expression
create index on test_range_elem using spgist(int4range(i,i+10))
;
----------
[CREATE_POLICY] /*
 * text search configuration for greek language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * greek and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY greek_stem
	(TEMPLATE = snowball, Language = greek )
;
----------
[ALTER_LIBRARY] /* First we have to remove them from the extension */
ALTER EXTENSION pg_stat_statements DROP VIEW pg_stat_statements
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- should fail
alter table parted_pk_detach_test detach partition parted_pk_detach_test1
;
----------
[CREATE_TYPE] CREATE TYPE split_domain_type_next.mood AS ENUM ('sad', 'ok', 'happy')
;
----------
[CREATE_TABLE] create table part_pa_test_p1 partition of part_pa_test for values from (minvalue) to (0)
;
----------
[CREATE_TABLE] create table j2 (id1 int, id2 int, primary key(id1,id2))
;
----------
[CREATE_TABLE] CREATE TEMP TABLE bug6051_2 (i int)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) test
;
----------
[CREATE_PROG_OBJ] -- fail - user-defined operators are not allowed
CREATE FUNCTION testpub_rf_func1(integer, integer) RETURNS boolean AS $$ SELECT hashint4($1) > $2 $$ LANGUAGE SQL
;
----------
[CREATE_VIEW(SELECT)] create view wcowrtest_v2 as
    select *
      from wcowrtest r
      where r in (select s from sometable s where r.a = s.a)
with check option
;
----------
[DROP_TABLE] DROP TABLE employees
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26642
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE just_to_create_temp_schema()
;
----------
[ADMIN_TABLE] VACUUM ANALYZE vacparted(a,b,a)
;
----------
[CREATE_TABLE] CREATE TABLE regtest_ptable (a int) PARTITION BY RANGE (a)
;
----------
[CREATE_TYPE] create domain hstestdom1 as integer not null default 0
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger failed instead of update on parted_trig
  for each row execute procedure trigger_nothing()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
  LEFTARG = int_custom,
  RIGHTARG = int_custom,
  FUNCTION = int_custom_ge,
  COMMUTATOR = <=,
  NEGATOR = <,
  RESTRICT = scalargesel,
  JOIN = scalargejoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <% (
   leftarg = point,
   rightarg = widget,
   procedure = pt_in_widget,
   commutator = >% ,
   negator = >=%
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(isbn13, isbn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_stored_tests"."gtestx" INHERIT "generated_stored_tests"."gtest1"
;
----------
[CREATE_TABLE] create temp table patest1() inherits (patest0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_oid_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION float4_dist(float4, float4)
RETURNS float4
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_timestamp TIMESTAMP DEFAULT '2016-09-01 12:00:00',
              ADD COLUMN c_timestamp_null TIMESTAMP,
              ALTER COLUMN c_date SET DEFAULT '2010-01-01'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=11382 host=/tmp/C6o4PTAnCK dbname=postgres' PUBLICATION tap_pub_B WITH (origin = none, copy_data = on)
;
----------
[ADD_INDEX] CREATE INDEX tbspace_reindex_part_index_0 ON ONLY tbspace_reindex_part_0 (c1)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_test_stats_tblspc LOCATION ''
;
