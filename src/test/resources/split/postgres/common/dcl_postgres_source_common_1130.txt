LOCK TABLE "public"."defaultexpr_tbl" IN ACCESS SHARE MODE
;
REVOKE ALL PRIVILEGES ON FUNCTION testagg8(int2)
	   FROM current_user, public, regress_testrolx
;
ALTER FUNCTION "public"."myfunc"(integer) OWNER TO zyc
;
-- failed
COPY (SELECT (o,p) FROM t1p) TO '/dev/null'
;
EXECUTE dumpFunc('21566')
;
EXECUTE dumpBaseType('1266')
;
SET deadlock_timeout = '10ms'
;
--
-- regression test for bug #13908 (hash join with skew tuples & nbatch increase)
--

set work_mem to '64kB'
;
GRANT ALL ON x1 TO PUBLIC
;
EXECUTE dumpTableAttach('44147')
;
------------------------------------------------------------------------------------------
[SESSION_LOCK] LOCK TABLE "public"."defaultexpr_tbl" IN ACCESS SHARE MODE
;
----------
[REVOKE] REVOKE ALL PRIVILEGES ON FUNCTION testagg8(int2)
	   FROM current_user, public, regress_testrolx
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."myfunc"(integer) OWNER TO zyc
;
----------
[DATA_EXPORT|SELECT] -- failed
COPY (SELECT (o,p) FROM t1p) TO '/dev/null'
;
----------
[UNSAFE] EXECUTE dumpFunc('21566')
;
----------
[UNSAFE] EXECUTE dumpBaseType('1266')
;
----------
[SESSION_SETTING_WRITE] SET deadlock_timeout = '10ms'
;
----------
[SESSION_SETTING_WRITE] --
-- regression test for bug #13908 (hash join with skew tuples & nbatch increase)
--

set work_mem to '64kB'
;
----------
[GRANT] GRANT ALL ON x1 TO PUBLIC
;
----------
[UNSAFE] EXECUTE dumpTableAttach('44147')
;
