CREATE UNLOGGED SEQUENCE seq_unlogged2
;
-- Dummy function used for expression evaluations.
-- Note that this function does not use a SQL-standard function body on
-- purpose, so as external statistics can be loaded from it.
CREATE OR REPLACE FUNCTION func_int_custom (p_value int_custom)
  RETURNS int_custom LANGUAGE plpgsql AS $$
  BEGIN
    RETURN p_value;
  END; $$
;
CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (a, ('unknown'))
;
CREATE VIEW street AS
   SELECT r.name, r.thepath, c.cname AS cname
   FROM ONLY road r, real_city c
   WHERE c.outline ?# r.thepath
;
ALTER TABLE test_io_local SET TABLESPACE regress_tblspace
;
CREATE INDEX ON test3ci (x text_pattern_ops)
;
ALTER TABLE ONLY "fkpart5"."fk1" ATTACH PARTITION "fkpart5"."fk11" FOR VALUES IN (1)
;
--
-- CREATE_OPERATOR
--

CREATE OPERATOR ## (
   leftarg = path,
   rightarg = path,
   function = path_inter,
   commutator = ##
)
;
ALTER TABLE pg_toast.pg_toast_30544 RENAME TO reind_con_toast
;
ALTER TABLE rec1 ENABLE ROW LEVEL SECURITY
;
create view agg_view1 as
  select aggfns(a,b,c)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c)
;
/*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
-- Statistics views

CREATE VIEW pg_stat_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_numscans(C.oid) AS seq_scan,
            pg_stat_get_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_numscans(I.indexrelid))::bigint AS idx_scan,
            sum(pg_stat_get_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_tuples_hot_updated(C.oid) AS n_tup_hot_upd,
            pg_stat_get_live_tuples(C.oid) AS n_live_tup,
            pg_stat_get_dead_tuples(C.oid) AS n_dead_tup,
            pg_stat_get_mod_since_analyze(C.oid) AS n_mod_since_analyze,
            pg_stat_get_ins_since_vacuum(C.oid) AS n_ins_since_vacuum,
            pg_stat_get_last_vacuum_time(C.oid) as last_vacuum,
            pg_stat_get_last_autovacuum_time(C.oid) as last_autovacuum,
            pg_stat_get_last_analyze_time(C.oid) as last_analyze,
            pg_stat_get_last_autoanalyze_time(C.oid) as last_autoanalyze,
            pg_stat_get_vacuum_count(C.oid) AS vacuum_count,
            pg_stat_get_autovacuum_count(C.oid) AS autovacuum_count,
            pg_stat_get_analyze_count(C.oid) AS analyze_count,
            pg_stat_get_autoanalyze_count(C.oid) AS autoanalyze_count
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm', 'p')
    GROUP BY C.oid, N.nspname, C.relname
;
ANALYZE (SKIP_LOCKED) vactst
;
-- nope
drop table inh_parent, inh_child
;
CREATE UNIQUE INDEX ataddindexi0 ON ataddindex(f1)
;
-- Fill a few pages
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
CREATE TABLE rewrite_test(col text)
;
create type testtype5 as (a int)
;
ALTER FUNCTION gbt_numeric_picksplit(internal, internal) PARALLEL SAFE
;
CREATE MATERIALIZED VIEW dump_test.regress_pg_dump_matview_am_2 USING heap AS SELECT 1
;
CREATE TABLE mvtest_foo(a, b) AS VALUES(1, 10)
;
CREATE TABLE "generated_stored_tests"."gtest26" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
create aggregate my_half_sum(int4)
(
   stype = int4,
   sfunc = sum_transfn,
   finalfunc = halfsum_finalfn
)
;
CREATE SCHEMA foreign_schema
;
CREATE TABLE fail_part PARTITION OF range_parted3 FOR VALUES FROM (1, 10) TO (1, 20)
;
CREATE FUNCTION spgist_name_inner_consistent(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE FUNCTION test_opclass_options_func(internal)
    RETURNS void
    AS '/src/src/test/regress/regress.so', 'test_opclass_options_func'
    LANGUAGE C
;
CREATE INDEX i_bmtest_a ON bmscantest(a)
;
ALTER TABLE split_alter_table.base_table REPLICA IDENTITY NOTHING
;
CREATE TABLE pagg_tab5_p2 PARTITION OF pagg_tab5 FOR VALUES IN ('c', 'd')
;
-- these should work
alter table if exists doesnt_exist_tab rename column a to d
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=11487 host=/tmp/HTqiQbMKdk dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
--
-- injection_points_stats_drop()
--
-- Drop all statistics of injection points.
--
CREATE FUNCTION injection_points_stats_drop()
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_stats_drop'
LANGUAGE C STRICT
;
COMMENT ON FUNCTION rboolop(query_int, _int4) IS 'boolean operation with array'
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_opfamily
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.date()'))
;
--
-- Verify that triggers with transition tables are not allowed on
-- views
--

create table my_table (i int)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_shdepend
;
CREATE TRIGGER "tg_pfield_au" AFTER UPDATE ON "public"."pfield" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pfield_au"()
;
CREATE TYPE upc (
	INPUT = upc_in,
	OUTPUT = isn_out,
	LIKE = pg_catalog.int8
)
;
comment on table table_name is 'ss'
;
VACUUM (PARALLEL 0) pg_catalog.pg_constraint
;
CREATE TABLE mcv_lists_multi (
	a INTEGER,
	b INTEGER,
	c INTEGER,
	d INTEGER
)
WITH (autovacuum_enabled = off)
;
CREATE FUNCTION func_parted_mod_b() RETURNS trigger AS $$
BEGIN
   NEW.b = NEW.b + 1;
   return NEW;
END $$ language plpgsql
;
ALTER FUNCTION gbt_macad8_consistent(internal, macaddr8, smallint, oid, internal) PARALLEL SAFE
;
create function f1(x anycompatible, y anycompatible) returns anycompatiblearray as $$
begin
  return array[x, y];
end$$ language plpgsql
;
CREATE EXTENSION test_integerset
;
CREATE FUNCTION heap_page_item_attrs(IN page bytea, IN rel_oid regclass,
    OUT lp smallint,
    OUT lp_off smallint,
    OUT lp_flags smallint,
    OUT lp_len smallint,
    OUT t_xmin xid,
    OUT t_xmax xid,
    OUT t_field3 int4,
    OUT t_ctid tid,
    OUT t_infomask2 integer,
    OUT t_infomask integer,
    OUT t_hoff smallint,
    OUT t_bits text,
    OUT t_oid oid,
    OUT t_attrs bytea[]
    )
RETURNS SETOF record AS $$
SELECT * from heap_page_item_attrs(page, rel_oid, false);
$$ LANGUAGE SQL PARALLEL SAFE
;
-- IMPORT FOREIGN SCHEMA
IMPORT FOREIGN SCHEMA s1 FROM SERVER s9 INTO public
;
------------------------------------------------------------------------------------------
[CREATE_SEQUENCE] CREATE UNLOGGED SEQUENCE seq_unlogged2
;
----------
[CREATE_PROG_OBJ] -- Dummy function used for expression evaluations.
-- Note that this function does not use a SQL-standard function body on
-- purpose, so as external statistics can be loaded from it.
CREATE OR REPLACE FUNCTION func_int_custom (p_value int_custom)
  RETURNS int_custom LANGUAGE plpgsql AS $$
  BEGIN
    RETURN p_value;
  END; $$
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (a, ('unknown'))
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW street AS
   SELECT r.name, r.thepath, c.cname AS cname
   FROM ONLY road r, real_city c
   WHERE c.outline ?# r.thepath
;
----------
[ALTER_TABLE] ALTER TABLE test_io_local SET TABLESPACE regress_tblspace
;
----------
[ADD_INDEX] CREATE INDEX ON test3ci (x text_pattern_ops)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart5"."fk1" ATTACH PARTITION "fkpart5"."fk11" FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] --
-- CREATE_OPERATOR
--

CREATE OPERATOR ## (
   leftarg = path,
   rightarg = path,
   function = path_inter,
   commutator = ##
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30544 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE] ALTER TABLE rec1 ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_VIEW(SELECT)] create view agg_view1 as
  select aggfns(a,b,c)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c)
;
----------
[CREATE_POLICY] /*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
----------
[CREATE_VIEW(SELECT)] -- Statistics views

CREATE VIEW pg_stat_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_numscans(C.oid) AS seq_scan,
            pg_stat_get_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_numscans(I.indexrelid))::bigint AS idx_scan,
            sum(pg_stat_get_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_tuples_hot_updated(C.oid) AS n_tup_hot_upd,
            pg_stat_get_live_tuples(C.oid) AS n_live_tup,
            pg_stat_get_dead_tuples(C.oid) AS n_dead_tup,
            pg_stat_get_mod_since_analyze(C.oid) AS n_mod_since_analyze,
            pg_stat_get_ins_since_vacuum(C.oid) AS n_ins_since_vacuum,
            pg_stat_get_last_vacuum_time(C.oid) as last_vacuum,
            pg_stat_get_last_autovacuum_time(C.oid) as last_autovacuum,
            pg_stat_get_last_analyze_time(C.oid) as last_analyze,
            pg_stat_get_last_autoanalyze_time(C.oid) as last_autoanalyze,
            pg_stat_get_vacuum_count(C.oid) AS vacuum_count,
            pg_stat_get_autovacuum_count(C.oid) AS autovacuum_count,
            pg_stat_get_analyze_count(C.oid) AS analyze_count,
            pg_stat_get_autoanalyze_count(C.oid) AS autoanalyze_count
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm', 'p')
    GROUP BY C.oid, N.nspname, C.relname
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) vactst
;
----------
[DROP_TABLE] -- nope
drop table inh_parent, inh_child
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ataddindexi0 ON ataddindex(f1)
;
----------
[BLOCK] -- Fill a few pages
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE rewrite_test(col text)
;
----------
[CREATE_TYPE] create type testtype5 as (a int)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_numeric_picksplit(internal, internal) PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW dump_test.regress_pg_dump_matview_am_2 USING heap AS SELECT 1
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE mvtest_foo(a, b) AS VALUES(1, 10)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest26" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_PROG_OBJ] create aggregate my_half_sum(int4)
(
   stype = int4,
   sfunc = sum_transfn,
   finalfunc = halfsum_finalfn
)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA foreign_schema
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted3 FOR VALUES FROM (1, 10) TO (1, 20)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION spgist_name_inner_consistent(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_opclass_options_func(internal)
    RETURNS void
    AS '/src/src/test/regress/regress.so', 'test_opclass_options_func'
    LANGUAGE C
;
----------
[ADD_INDEX] CREATE INDEX i_bmtest_a ON bmscantest(a)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table REPLICA IDENTITY NOTHING
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab5_p2 PARTITION OF pagg_tab5 FOR VALUES IN ('c', 'd')
;
----------
[RENAME_COLUMN] -- these should work
alter table if exists doesnt_exist_tab rename column a to d
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=11487 host=/tmp/HTqiQbMKdk dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_PROG_OBJ] --
-- injection_points_stats_drop()
--
-- Drop all statistics of injection points.
--
CREATE FUNCTION injection_points_stats_drop()
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_stats_drop'
LANGUAGE C STRICT
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION rboolop(query_int, _int4) IS 'boolean operation with array'
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_opfamily
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.date()'))
;
----------
[CREATE_TABLE] --
-- Verify that triggers with transition tables are not allowed on
-- views
--

create table my_table (i int)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_shdepend
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_pfield_au" AFTER UPDATE ON "public"."pfield" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pfield_au"()
;
----------
[CREATE_TYPE] CREATE TYPE upc (
	INPUT = upc_in,
	OUTPUT = isn_out,
	LIKE = pg_catalog.int8
)
;
----------
[COMMENT_TABLE] comment on table table_name is 'ss'
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_constraint
;
----------
[CREATE_TABLE] CREATE TABLE mcv_lists_multi (
	a INTEGER,
	b INTEGER,
	c INTEGER,
	d INTEGER
)
WITH (autovacuum_enabled = off)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_parted_mod_b() RETURNS trigger AS $$
BEGIN
   NEW.b = NEW.b + 1;
   return NEW;
END $$ language plpgsql
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad8_consistent(internal, macaddr8, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function f1(x anycompatible, y anycompatible) returns anycompatiblearray as $$
begin
  return array[x, y];
end$$ language plpgsql
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_integerset
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION heap_page_item_attrs(IN page bytea, IN rel_oid regclass,
    OUT lp smallint,
    OUT lp_off smallint,
    OUT lp_flags smallint,
    OUT lp_len smallint,
    OUT t_xmin xid,
    OUT t_xmax xid,
    OUT t_field3 int4,
    OUT t_ctid tid,
    OUT t_infomask2 integer,
    OUT t_infomask integer,
    OUT t_hoff smallint,
    OUT t_bits text,
    OUT t_oid oid,
    OUT t_attrs bytea[]
    )
RETURNS SETOF record AS $$
SELECT * from heap_page_item_attrs(page, rel_oid, false);
$$ LANGUAGE SQL PARALLEL SAFE
;
----------
[CREATE_TABLE] -- IMPORT FOREIGN SCHEMA
IMPORT FOREIGN SCHEMA s1 FROM SERVER s9 INTO public
;
