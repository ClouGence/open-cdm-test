ALTER TABLE ONLY pred_parent ALTER a SET NOT NULL
;
ANALYZE public.test_type_diff_c
;
ALTER FOREIGN TABLE text_csv OPTIONS (SET format 'csv')
;
/* src/test/modules/test_extensions/test_ext7--1.0--2.0.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- drop some tables with serial columns
drop table ext7_table1
;
create domain mytype as text
;
create schema stts_s2
;
drop table copydml_test
;
CREATE FUNCTION interpt_pp(path, path)
    RETURNS point
    AS '/src/src/test/regress/regress.so'
    LANGUAGE C STRICT
;
ANALYZE pg_temp_10.pg_temp_25428_2
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', null '
')
;
ALTER TABLE pg_statistic_ext ADD UNIQUE USING INDEX pg_statistic_ext_name_index
;
-- Test comments
COMMENT ON TRIGGER check_fkeys2_pkey_bad ON fkeys2 IS 'wrong'
;
-- min and max will be adjusted
ALTER SEQUENCE sequence_test14 AS int
;
-- gone

DROP TABLE image
;
DROP TRIGGER show_trigger_data_trig on trigger_test
;
ALTER TABLE ONLY "public"."notnull_tbl4_lk2"
    ADD CONSTRAINT "notnull_tbl4_lk2_pkey" PRIMARY KEY ("a") DEFERRABLE INITIALLY DEFERRED
;
create view part_abc_view as select * from part_abc where b <> 'a' with check option
;
create index idxpart_2_idx on only idxpart ((b + a)) where a > 1
;
DROP TABLE foochild
;
-- no such rule
drop rule nonesuch on noplace
;
CREATE FUNCTION "public"."test_conv"("input" "bytea", "src_encoding" "text", "dst_encoding" "text", OUT "result" "bytea", OUT "errorat" "bytea", OUT "error" "text") RETURNS "record"
    LANGUAGE "plpgsql"
    AS $$
declare
  validlen int;
begin
  -- First try to perform the conversion with noError = false. If that errors out,
  -- capture the error message, and try again with noError = true. The second call
  -- should succeed and return the position of the error, return that too.
  begin
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, false);
    errorat = NULL;
    error := NULL;
  exception when others then
    error := sqlerrm;
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, true);
    errorat = substr(input, validlen + 1);
  end;
  return;
end;
$$
;
CREATE TEXT SEARCH CONFIGURATION split_ts_probe.cfg_copy (COPY = pg_catalog.english)
;
create aggregate my_percentile_disc(float8 ORDER BY anyelement) (
  stype = internal,
  sfunc = ordered_set_transition,
  finalfunc = percentile_disc_final,
  finalfunc_extra = true,
  finalfunc_modify = read_write
)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c9 integer
;
--
-- test substr with toasted text values
--
CREATE TABLE toasttest(f1 text)
;
CREATE AGGREGATE split_agg_probe.sum_int(integer) (
    SFUNC = int4pl,
    STYPE = integer,
    INITCOND = '0',
    MSFUNC = int4pl,
    MINVFUNC = int4mi,
    MSTYPE = integer,
    MINITCOND = '0',
    COMBINEFUNC = int4pl,
    PARALLEL = SAFE
)
;
CREATE UNIQUE INDEX ON order_summary(date, item)
;
ALTER TABLE test_oat_schema.test_oat_tab FORCE ROW LEVEL SECURITY
;
create function tg_hub_a() returns trigger as '
declare
    hname	text;
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
	dummy := tg_hub_adjustslots(new.name, 0, new.nslots);
	return new;
    end if;
    if tg_op = ''UPDATE'' then
	if new.name != old.name then
	    update HSlot set hubname = new.name where hubname = old.name;
	end if;
	dummy := tg_hub_adjustslots(new.name, old.nslots, new.nslots);
	return new;
    end if;
    if tg_op = ''DELETE'' then
	dummy := tg_hub_adjustslots(old.name, old.nslots, 0);
	return old;
    end if;
end;
' language plpgsql
;
create rule qqq as on update to copydml_test do instead nothing
;
CREATE TABLE part_attmp1 PARTITION OF part_attmp FOR VALUES FROM (0) TO (100)
;
-- fires only _trigger
create table event_trigger_fire4 (a int)
;
CREATE EXTENSION pg_buffercache
;
CREATE OPERATOR !~* (
    PROCEDURE = texticregexne,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = ~*,
    RESTRICT  = icregexnesel,
    JOIN      = icregexnejoinsel
)
;
create table coll_pruning_def partition of coll_pruning default
;
alter table atacc1 add constraint atacc_test1 primary key (test)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub2, pub3
;
CREATE TABLE "public"."money_data" (
    "m" "money"
)
;
do $$
begin
  -- basic case
  for i in 1..3 loop
    raise notice '1..3: i = %', i;
  end loop;
  -- with BY, end matches exactly
  for i in 1..10 by 3 loop
    raise notice '1..10 by 3: i = %', i;
  end loop;
  -- with BY, end does not match
  for i in 1..11 by 3 loop
    raise notice '1..11 by 3: i = %', i;
  end loop;
  -- zero iterations
  for i in 1..0 by 3 loop
    raise notice '1..0 by 3: i = %', i;
  end loop;
  -- REVERSE
  for i in reverse 10..0 by 3 loop
    raise notice 'reverse 10..0 by 3: i = %', i;
  end loop;
  -- potential overflow
  for i in 2147483620..2147483647 by 10 loop
    raise notice '2147483620..2147483647 by 10: i = %', i;
  end loop;
  -- potential overflow, reverse direction
  for i in reverse -2147483620..-2147483647 by 10 loop
    raise notice 'reverse -2147483620..-2147483647 by 10: i = %', i;
  end loop;
end$$
;
CREATE EXTENSION IF NOT EXISTS pageinspect
;
ALTER TABLE pg_toast.pg_toast_30375 RENAME TO reind_con_toast
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_type
;
CREATE TABLE "public"."test_range_excl" (
    "room" "int4range",
    "speaker" "int4range",
    "during" "tsrange"
)
;
drop table gtest_cursor
;
create table test_partition partition of test_partitioned for values from (1) to (100)
;
create unlogged table pgbench_history(tid int,bid int,aid    int,delta int,mtime timestamp,filler char(22)) tablespace "pg_default"
;
CREATE TABLE leader (a int PRIMARY KEY, b int)
;
CREATE TEMP TABLE tab_search_same (a int, b int)
;
CREATE TABLE "public"."plt1_e_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE EXTENSION amcheck
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY pred_parent ALTER a SET NOT NULL
;
----------
[ADMIN_TABLE] ANALYZE public.test_type_diff_c
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE text_csv OPTIONS (SET format 'csv')
;
----------
[DROP_TABLE] /* src/test/modules/test_extensions/test_ext7--1.0--2.0.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- drop some tables with serial columns
drop table ext7_table1
;
----------
[CREATE_TYPE] create domain mytype as text
;
----------
[CREATE_SCHEMA] create schema stts_s2
;
----------
[DROP_TABLE] drop table copydml_test
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION interpt_pp(path, path)
    RETURNS point
    AS '/src/src/test/regress/regress.so'
    LANGUAGE C STRICT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_25428_2
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', null '
')
;
----------
[ALTER_TABLE|PERFORMANCE|ADD_CONSTRAINT] ALTER TABLE pg_statistic_ext ADD UNIQUE USING INDEX pg_statistic_ext_name_index
;
----------
[COMMENT_TRIGGER] -- Test comments
COMMENT ON TRIGGER check_fkeys2_pkey_bad ON fkeys2 IS 'wrong'
;
----------
[ALTER_SEQUENCE] -- min and max will be adjusted
ALTER SEQUENCE sequence_test14 AS int
;
----------
[DROP_TABLE] -- gone

DROP TABLE image
;
----------
[DROP_TRIGGER] DROP TRIGGER show_trigger_data_trig on trigger_test
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."notnull_tbl4_lk2"
    ADD CONSTRAINT "notnull_tbl4_lk2_pkey" PRIMARY KEY ("a") DEFERRABLE INITIALLY DEFERRED
;
----------
[CREATE_VIEW(SELECT)] create view part_abc_view as select * from part_abc where b <> 'a' with check option
;
----------
[ADD_INDEX] create index idxpart_2_idx on only idxpart ((b + a)) where a > 1
;
----------
[DROP_TABLE] DROP TABLE foochild
;
----------
[DROP_POLICY] -- no such rule
drop rule nonesuch on noplace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_conv"("input" "bytea", "src_encoding" "text", "dst_encoding" "text", OUT "result" "bytea", OUT "errorat" "bytea", OUT "error" "text") RETURNS "record"
    LANGUAGE "plpgsql"
    AS $$
declare
  validlen int;
begin
  -- First try to perform the conversion with noError = false. If that errors out,
  -- capture the error message, and try again with noError = true. The second call
  -- should succeed and return the position of the error, return that too.
  begin
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, false);
    errorat = NULL;
    error := NULL;
  exception when others then
    error := sqlerrm;
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, true);
    errorat = substr(input, validlen + 1);
  end;
  return;
end;
$$
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION split_ts_probe.cfg_copy (COPY = pg_catalog.english)
;
----------
[CREATE_PROG_OBJ] create aggregate my_percentile_disc(float8 ORDER BY anyelement) (
  stype = internal,
  sfunc = ordered_set_transition,
  finalfunc = percentile_disc_final,
  finalfunc_extra = true,
  finalfunc_modify = read_write
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c9 integer
;
----------
[CREATE_TABLE] --
-- test substr with toasted text values
--
CREATE TABLE toasttest(f1 text)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE split_agg_probe.sum_int(integer) (
    SFUNC = int4pl,
    STYPE = integer,
    INITCOND = '0',
    MSFUNC = int4pl,
    MINVFUNC = int4mi,
    MSTYPE = integer,
    MINITCOND = '0',
    COMBINEFUNC = int4pl,
    PARALLEL = SAFE
)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON order_summary(date, item)
;
----------
[ALTER_TABLE] ALTER TABLE test_oat_schema.test_oat_tab FORCE ROW LEVEL SECURITY
;
----------
[CREATE_PROG_OBJ] create function tg_hub_a() returns trigger as '
declare
    hname	text;
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
	dummy := tg_hub_adjustslots(new.name, 0, new.nslots);
	return new;
    end if;
    if tg_op = ''UPDATE'' then
	if new.name != old.name then
	    update HSlot set hubname = new.name where hubname = old.name;
	end if;
	dummy := tg_hub_adjustslots(new.name, old.nslots, new.nslots);
	return new;
    end if;
    if tg_op = ''DELETE'' then
	dummy := tg_hub_adjustslots(old.name, old.nslots, 0);
	return old;
    end if;
end;
' language plpgsql
;
----------
[CREATE_POLICY] create rule qqq as on update to copydml_test do instead nothing
;
----------
[CREATE_TABLE] CREATE TABLE part_attmp1 PARTITION OF part_attmp FOR VALUES FROM (0) TO (100)
;
----------
[CREATE_TABLE] -- fires only _trigger
create table event_trigger_fire4 (a int)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pg_buffercache
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !~* (
    PROCEDURE = texticregexne,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = ~*,
    RESTRICT  = icregexnesel,
    JOIN      = icregexnejoinsel
)
;
----------
[CREATE_TABLE] create table coll_pruning_def partition of coll_pruning default
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_test1 primary key (test)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub2, pub3
;
----------
[CREATE_TABLE] CREATE TABLE "public"."money_data" (
    "m" "money"
)
;
----------
[BLOCK] do $$
begin
  -- basic case
  for i in 1..3 loop
    raise notice '1..3: i = %', i;
  end loop;
  -- with BY, end matches exactly
  for i in 1..10 by 3 loop
    raise notice '1..10 by 3: i = %', i;
  end loop;
  -- with BY, end does not match
  for i in 1..11 by 3 loop
    raise notice '1..11 by 3: i = %', i;
  end loop;
  -- zero iterations
  for i in 1..0 by 3 loop
    raise notice '1..0 by 3: i = %', i;
  end loop;
  -- REVERSE
  for i in reverse 10..0 by 3 loop
    raise notice 'reverse 10..0 by 3: i = %', i;
  end loop;
  -- potential overflow
  for i in 2147483620..2147483647 by 10 loop
    raise notice '2147483620..2147483647 by 10: i = %', i;
  end loop;
  -- potential overflow, reverse direction
  for i in reverse -2147483620..-2147483647 by 10 loop
    raise notice 'reverse -2147483620..-2147483647 by 10: i = %', i;
  end loop;
end$$
;
----------
[CREATE_LIBRARY] CREATE EXTENSION IF NOT EXISTS pageinspect
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30375 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_type
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_range_excl" (
    "room" "int4range",
    "speaker" "int4range",
    "during" "tsrange"
)
;
----------
[DROP_TABLE] drop table gtest_cursor
;
----------
[CREATE_TABLE] create table test_partition partition of test_partitioned for values from (1) to (100)
;
----------
[CREATE_TABLE] create unlogged table pgbench_history(tid int,bid int,aid    int,delta int,mtime timestamp,filler char(22)) tablespace "pg_default"
;
----------
[CREATE_TABLE] CREATE TABLE leader (a int PRIMARY KEY, b int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tab_search_same (a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt1_e_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION amcheck
;
