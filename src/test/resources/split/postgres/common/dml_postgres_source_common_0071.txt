-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3136', relminmxid = '1'
WHERE oid = '"public"."dupindexcols"'::pg_catalog.regclass
;
INSERT INTO tst_comp_enum_array (a, b) VALUES
		(ROW(1.0, 'a', 1), ARRAY[ROW(1, 'a', 1)::tst_comp_enum_t]),
		(ROW(2.0, 'b', 2), ARRAY[ROW(2, 'b', 2)::tst_comp_enum_t]),
		(ROW(3.0, 'c', 3), ARRAY[ROW(3, 'c', 3)::tst_comp_enum_t]),
		(ROW(4.0, 'd', 3), ARRAY[ROW(3, 'd', 3)::tst_comp_enum_t]),
		(ROW(5.0, 'e', 3), ARRAY[ROW(3, 'e', 3)::tst_comp_enum_t, NULL])
;
UPDATE ONLY rw_view2 SET a = a*10 WHERE a IN (-4, 4)
;
INSERT INTO f_star (class, e, f)
   VALUES ('f', '-11'::int2, '(1111111,3333333),(2222222,4444444)'::polygon)
;
UPDATE atest5 SET one = 1
;
INSERT INTO num_exp_mul VALUES (7,3,'-357852770.35')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."pagg_tab_m_p2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1559', relminmxid = '1'
WHERE oid = '"public"."reservations"'::pg_catalog.regclass
;
insert into insertconflicttest values (1, 'Apple') on conflict (keyy) do update set fruit = excluded.fruit
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '38541'
;
INSERT INTO document VALUES (33, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'Some novel, replaces sci-fi') -- takes UPDATE path
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle
;
INSERT INTO public.tht VALUES ('4', 764)
;
INSERT INTO rw_view15 VALUES (3, 'ROW 3')
;
WITH rand_value AS (SELECT string_agg(fipshash((-i)::text),'') AS val FROM generate_series(1,60) s(i))
INSERT INTO brintest_3
SELECT val, val, val, val FROM rand_value
;
INSERT INTO guid1(guid_field) VALUES('111-11111-1111-1111-1111-111111111111')
;
-- Give good diagnostic message when EXCLUDED.* spuriously referenced from
-- RETURNING:
insert into insertconflicttest values (1, 'Apple') on conflict (key) do update set fruit = excluded.fruit RETURNING excluded.fruit
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11720', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest26"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."itest7"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt1_l_p2_p1"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."clstr_tst_inh"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '762', relminmxid = '1'
WHERE oid = '"public"."onek"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12298', relminmxid = '3'
WHERE oid = '"public"."pline"'::pg_catalog.regclass
;
INSERT INTO num_exp_mul VALUES (2,6,'-3224438592470.18449811926184222')
;
INSERT INTO pub_testpart1.child_parent2 values(1)
;
INSERT INTO public.mvtest_mv SELECT (diff.newdata).* FROM pg_temp_11.pg_temp_26215_2 diff WHERE tid IS NULL
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt2_n_p2"'::pg_catalog.regclass
;
INSERT INTO expr_stats SELECT mod(i,10), md5(mod(i,10)::text), md5(mod(i,10)::text) FROM generate_series(1,1000) s(i)
;
INSERT INTO num_exp_sub VALUES (2,8,'-34413373.215397047')
;
INSERT INTO foo VALUES (847001,'t15','GE1043')
;
INSERT INTO public.tht VALUES ('9', 779)
;
-- B == 0
INSERT INTO LINE_TBL VALUES ('{0,3,0}')
;
insert into hp values (1, 'xxx', 2)
;
insert into bytea_test_table values(decode('aa','hex'))
;
INSERT INTO public.tplain VALUES ('1', 541)
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (9, 1, 49458, -2010, CURRENT_TIMESTAMP)
;
INSERT INTO num_exp_div VALUES (3,2,'-.00000012551512084352')
;
INSERT INTO dump_test.test_compression_method (col1) SELECT string_agg(a::text, '') FROM generate_series(1,4096) a
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.2........', -1, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."tt7"'::pg_catalog.regclass
  AND attname = v.dname
;
UPDATE public.test_numerical SET b = 42, c = NULL
;
DELETE FROM table_dropped_index_no_pk WHERE b = 1
;
INSERT INTO public.ths_p3 VALUES (0, 560)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15806', relminmxid = '3'
WHERE oid = '36503'
;
INSERT INTO public.ths_p3 VALUES (0, 350)
;
INSERT INTO num_exp_sub VALUES (4,5,'7783064.373409')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"fkpart3"."pk52"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."d"'::pg_catalog.regclass
  AND attname IN ('aa', 'bb', 'cc')
;
INSERT INTO POLYGON_TBL(f1) VALUES ('(0.0 0.0')
;
-- let's try removing a row that should fail from pktable
delete from pktable where base1>2
;
INSERT INTO num_exp_sub VALUES (1,0,'0')
;
INSERT INTO sq_target(tid, balance) VALUES (1,100), (2,200), (3,300)
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3136', relminmxid = '1'
WHERE oid = '"public"."dupindexcols"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO tst_comp_enum_array (a, b) VALUES
		(ROW(1.0, 'a', 1), ARRAY[ROW(1, 'a', 1)::tst_comp_enum_t]),
		(ROW(2.0, 'b', 2), ARRAY[ROW(2, 'b', 2)::tst_comp_enum_t]),
		(ROW(3.0, 'c', 3), ARRAY[ROW(3, 'c', 3)::tst_comp_enum_t]),
		(ROW(4.0, 'd', 3), ARRAY[ROW(3, 'd', 3)::tst_comp_enum_t]),
		(ROW(5.0, 'e', 3), ARRAY[ROW(3, 'e', 3)::tst_comp_enum_t, NULL])
;
----------
[UPDATE] UPDATE ONLY rw_view2 SET a = a*10 WHERE a IN (-4, 4)
;
----------
[INSERT|SELECT] INSERT INTO f_star (class, e, f)
   VALUES ('f', '-11'::int2, '(1111111,3333333),(2222222,4444444)'::polygon)
;
----------
[UPDATE] UPDATE atest5 SET one = 1
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (7,3,'-357852770.35')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."pagg_tab_m_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1559', relminmxid = '1'
WHERE oid = '"public"."reservations"'::pg_catalog.regclass
;
----------
[MERGE] insert into insertconflicttest values (1, 'Apple') on conflict (keyy) do update set fruit = excluded.fruit
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '38541'
;
----------
[MERGE|SELECT] INSERT INTO document VALUES (33, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'Some novel, replaces sci-fi') -- takes UPDATE path
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 764)
;
----------
[INSERT] INSERT INTO rw_view15 VALUES (3, 'ROW 3')
;
----------
[INSERT|SELECT] WITH rand_value AS (SELECT string_agg(fipshash((-i)::text),'') AS val FROM generate_series(1,60) s(i))
INSERT INTO brintest_3
SELECT val, val, val, val FROM rand_value
;
----------
[INSERT|SELECT] INSERT INTO guid1(guid_field) VALUES('111-11111-1111-1111-1111-111111111111')
;
----------
[MERGE] -- Give good diagnostic message when EXCLUDED.* spuriously referenced from
-- RETURNING:
insert into insertconflicttest values (1, 'Apple') on conflict (key) do update set fruit = excluded.fruit RETURNING excluded.fruit
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '11720', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest26"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."itest7"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt1_l_p2_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."clstr_tst_inh"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '762', relminmxid = '1'
WHERE oid = '"public"."onek"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12298', relminmxid = '3'
WHERE oid = '"public"."pline"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (2,6,'-3224438592470.18449811926184222')
;
----------
[INSERT] INSERT INTO pub_testpart1.child_parent2 values(1)
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_mv SELECT (diff.newdata).* FROM pg_temp_11.pg_temp_26215_2 diff WHERE tid IS NULL
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt2_n_p2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO expr_stats SELECT mod(i,10), md5(mod(i,10)::text), md5(mod(i,10)::text) FROM generate_series(1,1000) s(i)
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (2,8,'-34413373.215397047')
;
----------
[INSERT] INSERT INTO foo VALUES (847001,'t15','GE1043')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 779)
;
----------
[INSERT] -- B == 0
INSERT INTO LINE_TBL VALUES ('{0,3,0}')
;
----------
[INSERT] insert into hp values (1, 'xxx', 2)
;
----------
[INSERT] insert into bytea_test_table values(decode('aa','hex'))
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 541)
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (9, 1, 49458, -2010, CURRENT_TIMESTAMP)
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (3,2,'-.00000012551512084352')
;
----------
[INSERT|SELECT] INSERT INTO dump_test.test_compression_method (col1) SELECT string_agg(a::text, '') FROM generate_series(1,4096) a
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.2........', -1, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."tt7"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[UPDATE] UPDATE public.test_numerical SET b = 42, c = NULL
;
----------
[DELETE] DELETE FROM table_dropped_index_no_pk WHERE b = 1
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 560)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15806', relminmxid = '3'
WHERE oid = '36503'
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 350)
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (4,5,'7783064.373409')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"fkpart3"."pk52"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."d"'::pg_catalog.regclass
  AND attname IN ('aa', 'bb', 'cc')
;
----------
[INSERT|SELECT] INSERT INTO POLYGON_TBL(f1) VALUES ('(0.0 0.0')
;
----------
[DELETE] -- let's try removing a row that should fail from pktable
delete from pktable where base1>2
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (1,0,'0')
;
----------
[INSERT|SELECT] INSERT INTO sq_target(tid, balance) VALUES (1,100), (2,200), (3,300)
;
