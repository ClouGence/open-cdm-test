VACUUM (ANALYZE) pg_catalog.pg_enum
;
-- Things that shouldn't work:

CREATE FUNCTION test1 (int) RETURNS int LANGUAGE C
    AS 'nosuchfile'
;
ANALYZE public.rtest_admin
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_shseclabel
;
-- test jsonb[] -> python list
-- array of dicts
CREATE FUNCTION test3arr(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, list)
assert(val == [{"a": 1,"b": 2}, {"c": 3,"d": 4}])
return len(val)
$$
;
-- Test that updates to indexed columns are still propagated to the
-- BRIN column.
-- https://postgr.es/m/05ebcb44-f383-86e3-4f31-0a97a55634cf@enterprisedb.com
CREATE TABLE brin_hot_3 (a int, filler text) WITH (fillfactor = 10)
;
create trigger rem2_trig_row_before before insert on rem2
	for each row execute procedure trigger_data(23,'skidoo')
;
DROP TABLE rf_tbl_abcd_pk
;
ALTER TABLE cnn_pk ADD CONSTRAINT cnn_primarykey PRIMARY KEY (b)
;
CREATE TABLE ceil_floor_round (a numeric)
;
CREATE DATABASE "contrib_regression" TEMPLATE=template0
;
create temp table t2a () inherits (t2)
;
CREATE SUBSCRIPTION testsub CONNECTION $$port=16005 host=/tmp/B3ysgEyCZr dbname='d1'$$ PUBLICATION testpub WITH (create_slot=false, slot_name='testslot')
;
ANALYZE public.rtest_view2
;
CREATE COLLATION testcoll_backwards (provider = icu, locale = '@colBackwards=yes')
;
CREATE TABLE part5_def_p1 PARTITION OF part5_def FOR VALUES IN (5)
;
drop domain inotnull
;
ALTER TABLE gtest_parent ATTACH PARTITION gtest_child3 FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
-- views with leaky operator
CREATE VIEW atest12v AS
  SELECT * FROM atest12 WHERE b <<< 5
;
ALTER SUBSCRIPTION tap_sub CONNECTION 'port=18417 host=/tmp/jotseMAL4u dbname=postgres options=''-c log_statement_stats=on'''
;
CREATE TABLE pk1 PARTITION OF pk FOR VALUES FROM (0) TO (1000)
;
ALTER TABLE test_replica_identity3 REPLICA IDENTITY USING INDEX test_replica_identity3_id_key
;
CREATE TABLE "public"."brintest_multi" (
    "int8col" bigint,
    "int2col" smallint,
    "int4col" integer,
    "oidcol" "oid",
    "tidcol" "tid",
    "float4col" real,
    "float8col" double precision,
    "macaddrcol" "macaddr",
    "macaddr8col" "macaddr8",
    "inetcol" "inet",
    "cidrcol" "cidr",
    "datecol" "date",
    "timecol" time without time zone,
    "timestampcol" timestamp without time zone,
    "timestamptzcol" timestamp with time zone,
    "intervalcol" interval,
    "timetzcol" time with time zone,
    "numericcol" numeric,
    "uuidcol" "uuid",
    "lsncol" "pg_lsn"
)
WITH ("fillfactor"='10')
;
CREATE TABLE tableam_tbl_heapx(f1 int)
;
CREATE SCHEMA regress_seg_schema
;
--
-- Test self-referential FK with CASCADE (bug #6268)
--
create temp table selfref (
    a int primary key,
    b int,
    foreign key (b) references selfref (a)
        on update cascade on delete cascade
)
;
CALL test_proc10(_a, c => _c, b => _b)
;
DROP TABLE atest5
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', on_error 'ignore')
;
create table part_pa_test(a int, b int) partition by range(a)
;
ALTER SEQUENCE foreign_schema.ft1_c11_seq SET SCHEMA public
;
CREATE SUBSCRIPTION regress_sub CONNECTION 'port=31163 host=/tmp/7vA5ZgkYXy dbname=postgres' PUBLICATION regress_pub WITH (two_phase = 'true', failover = 'true')
;
--
--
--
-- float8 ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_float8_consistent(internal,float8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE tab6 DROP CONSTRAINT tab6_pkey
;
CREATE INDEX abbrev_abort_uuids__abort_increasing_idx ON abbrev_abort_uuids (abort_increasing)
;
create function mlparted11_trig_fn()
returns trigger AS
$$
begin
  NEW.b := 4;
  return NEW;
end;
$$
language plpgsql
;
CREATE FUNCTION "public"."do_analyze"() RETURNS "void"
    LANGUAGE "sql"
    AS $$ANALYZE pg_am$$
;
CREATE TABLE partitioned (
	a point
) PARTITION BY LIST (a point_ops)
;
ALTER TABLE c1 ALTER COLUMN a TYPE float
;
CREATE OR REPLACE FUNCTION regexp_matches(string citext, pattern citext, flags text) RETURNS SETOF TEXT[] AS $$
    SELECT pg_catalog.regexp_matches( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE ROWS 10
;
ALTER TABLE ONLY "public"."prt3_n" ATTACH PARTITION "public"."prt3_n_p2" FOR VALUES IN ('0001', '0002', '0008', '0010')
;
-- should be able to drop the table
ALTER PUBLICATION testpub_forschema DROP TABLE pub_test.testpub_nopk
;
DROP TABLE regtest_ptable_4 CASCADE
;
DROP TABLE pcachetest
;
VACUUM (PARALLEL 0) pg_catalog.pg_ts_dict
;
-- not supported yet

ALTER TABLE regtest_table ADD COLUMN d float
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
CREATE FUNCTION ffp(anyarray) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
-- second partition has replica identity "b"
CREATE TABLE testpub_tbl8_1 PARTITION OF testpub_tbl8 FOR VALUES WITH (modulus 2, remainder 1)
;
CREATE TYPE "public"."insenum" AS ENUM (
)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_enum
;
----------
[CREATE_PROG_OBJ] -- Things that shouldn't work:

CREATE FUNCTION test1 (int) RETURNS int LANGUAGE C
    AS 'nosuchfile'
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_admin
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_shseclabel
;
----------
[CREATE_PROG_OBJ] -- test jsonb[] -> python list
-- array of dicts
CREATE FUNCTION test3arr(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, list)
assert(val == [{"a": 1,"b": 2}, {"c": 3,"d": 4}])
return len(val)
$$
;
----------
[CREATE_TABLE] -- Test that updates to indexed columns are still propagated to the
-- BRIN column.
-- https://postgr.es/m/05ebcb44-f383-86e3-4f31-0a97a55634cf@enterprisedb.com
CREATE TABLE brin_hot_3 (a int, filler text) WITH (fillfactor = 10)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger rem2_trig_row_before before insert on rem2
	for each row execute procedure trigger_data(23,'skidoo')
;
----------
[DROP_TABLE] DROP TABLE rf_tbl_abcd_pk
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE cnn_pk ADD CONSTRAINT cnn_primarykey PRIMARY KEY (b)
;
----------
[CREATE_TABLE] CREATE TABLE ceil_floor_round (a numeric)
;
----------
[CREATE_CATALOG] CREATE DATABASE "contrib_regression" TEMPLATE=template0
;
----------
[CREATE_TABLE] create temp table t2a () inherits (t2)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub CONNECTION $$port=16005 host=/tmp/B3ysgEyCZr dbname='d1'$$ PUBLICATION testpub WITH (create_slot=false, slot_name='testslot')
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_view2
;
----------
[CREATE_POLICY] CREATE COLLATION testcoll_backwards (provider = icu, locale = '@colBackwards=yes')
;
----------
[CREATE_TABLE] CREATE TABLE part5_def_p1 PARTITION OF part5_def FOR VALUES IN (5)
;
----------
[DROP_TYPE] drop domain inotnull
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE gtest_parent ATTACH PARTITION gtest_child3 FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
----------
[CREATE_VIEW(SELECT)] -- views with leaky operator
CREATE VIEW atest12v AS
  SELECT * FROM atest12 WHERE b <<< 5
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub CONNECTION 'port=18417 host=/tmp/jotseMAL4u dbname=postgres options=''-c log_statement_stats=on'''
;
----------
[CREATE_TABLE] CREATE TABLE pk1 PARTITION OF pk FOR VALUES FROM (0) TO (1000)
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity3 REPLICA IDENTITY USING INDEX test_replica_identity3_id_key
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brintest_multi" (
    "int8col" bigint,
    "int2col" smallint,
    "int4col" integer,
    "oidcol" "oid",
    "tidcol" "tid",
    "float4col" real,
    "float8col" double precision,
    "macaddrcol" "macaddr",
    "macaddr8col" "macaddr8",
    "inetcol" "inet",
    "cidrcol" "cidr",
    "datecol" "date",
    "timecol" time without time zone,
    "timestampcol" timestamp without time zone,
    "timestamptzcol" timestamp with time zone,
    "intervalcol" interval,
    "timetzcol" time with time zone,
    "numericcol" numeric,
    "uuidcol" "uuid",
    "lsncol" "pg_lsn"
)
WITH ("fillfactor"='10')
;
----------
[CREATE_TABLE] CREATE TABLE tableam_tbl_heapx(f1 int)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_seg_schema
;
----------
[CREATE_TABLE] --
-- Test self-referential FK with CASCADE (bug #6268)
--
create temp table selfref (
    a int primary key,
    b int,
    foreign key (b) references selfref (a)
        on update cascade on delete cascade
)
;
----------
[CALL_PROG_OBJ] CALL test_proc10(_a, c => _c, b => _b)
;
----------
[DROP_TABLE] DROP TABLE atest5
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', on_error 'ignore')
;
----------
[CREATE_TABLE] create table part_pa_test(a int, b int) partition by range(a)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE foreign_schema.ft1_c11_seq SET SCHEMA public
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub CONNECTION 'port=31163 host=/tmp/7vA5ZgkYXy dbname=postgres' PUBLICATION regress_pub WITH (two_phase = 'true', failover = 'true')
;
----------
[CREATE_PROG_OBJ] --
--
--
-- float8 ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_float8_consistent(internal,float8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE tab6 DROP CONSTRAINT tab6_pkey
;
----------
[ADD_INDEX] CREATE INDEX abbrev_abort_uuids__abort_increasing_idx ON abbrev_abort_uuids (abort_increasing)
;
----------
[CREATE_PROG_OBJ] create function mlparted11_trig_fn()
returns trigger AS
$$
begin
  NEW.b := 4;
  return NEW;
end;
$$
language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."do_analyze"() RETURNS "void"
    LANGUAGE "sql"
    AS $$ANALYZE pg_am$$
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a point
) PARTITION BY LIST (a point_ops)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE c1 ALTER COLUMN a TYPE float
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_matches(string citext, pattern citext, flags text) RETURNS SETOF TEXT[] AS $$
    SELECT pg_catalog.regexp_matches( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE ROWS 10
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt3_n" ATTACH PARTITION "public"."prt3_n_p2" FOR VALUES IN ('0001', '0002', '0008', '0010')
;
----------
[ALTER_PUB_SUB] -- should be able to drop the table
ALTER PUBLICATION testpub_forschema DROP TABLE pub_test.testpub_nopk
;
----------
[DROP_TABLE] DROP TABLE regtest_ptable_4 CASCADE
;
----------
[DROP_TABLE] DROP TABLE pcachetest
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_ts_dict
;
----------
[ALTER_TABLE|ADD_COLUMN] -- not supported yet

ALTER TABLE regtest_table ADD COLUMN d float
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ffp(anyarray) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
----------
[CREATE_TABLE] -- second partition has replica identity "b"
CREATE TABLE testpub_tbl8_1 PARTITION OF testpub_tbl8 FOR VALUES WITH (modulus 2, remainder 1)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."insenum" AS ENUM (
)
;
