CREATE TABLE "generated_stored_tests"."gtest3a" (
    "a" "text",
    "b" "text" GENERATED ALWAYS AS ((("a" || '+'::"text") || "a")) STORED
)
;
alter table t2 alter column z set not null
;
CREATE TABLE notnull_tbl4 (a INTEGER PRIMARY KEY INITIALLY DEFERRED)
;
-- We don't drop the invalid_check_con* tables, to test dump/reload with

--
-- Test parameterized append plans for inheritance trees
--

create temp table patest0 (id, x) as
  select x, x from generate_series(0,1000) x
;
create table fkeys (fkey1 int4, fkey2 text, fkey3 int)
;
-- same with arrays
CREATE TABLE test23a (a int, b text[] COLLATE case_insensitive) PARTITION BY HASH (b)
;
CREATE TABLE amcheck_schema.pg_tablespace (junk text)
;
-- Normally, CREATE FUNCTION should complain about invalid values in
-- function SET options; but not if check_function_bodies is off,
-- because that creates ordering hazards for pg_dump

create function func_with_bad_set() returns int as $$ select 1 $$
language sql
set default_text_search_config = no_such_config
;
ALTER INDEX pg_toast.pg_toast_33136_index RENAME TO reind_con_toast_idx
;
-- create function to filter unstable results of EXPLAIN
CREATE FUNCTION explain_filter(text) RETURNS setof text
LANGUAGE plpgsql AS
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Remove the path portion of foreign file names
        ln := regexp_replace(ln, 'Foreign File: .*/([a-z.]+)$', 'Foreign File: .../\1');
        return next ln;
    end loop;
end;
$$
;
CREATE TABLE ios_bitmap (a int NOT NULL, b int not null, pad char(1024) default '')
    WITH (AUTOVACUUM_ENABLED = false, FILLFACTOR = 10)
;
-- attached partition
CREATE TABLE pitest1_p2 (f3 bigint, f2 text, f1 date NOT NULL)
;
-- index

DROP INDEX test_index_exists
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_attribute
;
-- no updates here
DROP TABLE test12pk, test12fk
;
create table inh_child1(f1 int not null)
;
/*
 * text search configuration for italian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * italian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY italian_stem
	(TEMPLATE = snowball, Language = italian , StopWords=italian)
;
drop schema stts_s1, stts_s2 cascade
;
CREATE OPERATOR <@ (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_risparent,
        COMMUTATOR = '@>',
        RESTRICT = ltreeparentsel,
	JOIN = contjoinsel
)
;
-- Event triggers on relations.
CREATE OR REPLACE FUNCTION event_trigger_report_dropped()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r record;
BEGIN
    FOR r IN SELECT * from pg_event_trigger_dropped_objects()
    LOOP
    IF NOT r.normal AND NOT r.original THEN
        CONTINUE;
    END IF;
    RAISE NOTICE 'NORMAL: orig=% normal=% istemp=% type=% identity=% schema=% name=% addr=% args=%',
        r.original, r.normal, r.is_temporary, r.object_type,
        r.object_identity, r.schema_name, r.object_name,
        r.address_names, r.address_args;
    END LOOP;
END; $$
;
create temp table forc_test as
  select n as i, n as j from generate_series(1,10) n
;
CREATE FUNCTION f() RETURNS TRIGGER LANGUAGE plpgsql AS 'BEGIN RETURN NULL; END;'
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = issn,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE FOREIGN TABLE foreign_part (a int) SERVER s0
;
CREATE FUNCTION gin_trgm_consistent(internal, int2, text, int4, internal, internal, internal, internal)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ANALYZE public.ramp
;
alter table atacc3 drop test2
;
CREATE TABLE t1 (a int, b text)
;
CREATE TABLE s5.t1 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
create table test2(t text COLLATE "C")
;
CREATE TEXT SEARCH CONFIGURATION turkish
	(PARSER = default)
;
ALTER OPERATOR >= (ean13, ismn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
DROP SCHEMA concur_reindex_schema CASCADE
;
ALTER INDEX pg_toast.pg_toast_27879_index RENAME TO reind_con_toast_idx
;
ALTER TABLE pg_subscription ADD UNIQUE USING INDEX pg_subscription_subname_index
;
create function br_insert_trigfunc() returns trigger as $$
begin
	new.b := new.b || ' triggered !';
	return new;
end
$$ language plpgsql
;
CREATE PUBLICATION regress_pub3
;
-- make sure that column names are handled correctly
CREATE TABLE mvtest_v (i int, j int)
;
ALTER INDEX dummy_test_idx SET (option_bool = 'val4')
;
CREATE PROCEDURE ptest4b(INOUT b int, INOUT a int)
LANGUAGE SQL
AS $$
CALL ptest4a(a, b);  -- error, not supported
$$
;
create index on parent(b)
;
create table bpchar_tbl (f1 character(16) unique, f2 bpchar)
;
alter table toasttest alter column f1 set storage external
;
CREATE TABLE money_data (m money)
;
CREATE OPERATOR - (
	LEFTARG = hstore,
	RIGHTARG = text[],
	PROCEDURE = delete
)
;
ALTER TABLE itest4 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
DROP SCHEMA testpub_rf_schema2
;
CREATE FUNCTION regtest_func_1 (text) RETURNS bool
  AS 'BEGIN RETURN true; END' LANGUAGE 'plpgsql'
;
ALTER TABLE tab_ins REPLICA IDENTITY FULL
;
DROP SCHEMA matview_schema CASCADE
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest3a" (
    "a" "text",
    "b" "text" GENERATED ALWAYS AS ((("a" || '+'::"text") || "a")) STORED
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table t2 alter column z set not null
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl4 (a INTEGER PRIMARY KEY INITIALLY DEFERRED)
;
----------
[CREATE_TABLE|SELECT] -- We don't drop the invalid_check_con* tables, to test dump/reload with

--
-- Test parameterized append plans for inheritance trees
--

create temp table patest0 (id, x) as
  select x, x from generate_series(0,1000) x
;
----------
[CREATE_TABLE] create table fkeys (fkey1 int4, fkey2 text, fkey3 int)
;
----------
[CREATE_TABLE] -- same with arrays
CREATE TABLE test23a (a int, b text[] COLLATE case_insensitive) PARTITION BY HASH (b)
;
----------
[CREATE_TABLE] CREATE TABLE amcheck_schema.pg_tablespace (junk text)
;
----------
[CREATE_PROG_OBJ] -- Normally, CREATE FUNCTION should complain about invalid values in
-- function SET options; but not if check_function_bodies is off,
-- because that creates ordering hazards for pg_dump

create function func_with_bad_set() returns int as $$ select 1 $$
language sql
set default_text_search_config = no_such_config
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33136_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] -- create function to filter unstable results of EXPLAIN
CREATE FUNCTION explain_filter(text) RETURNS setof text
LANGUAGE plpgsql AS
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Remove the path portion of foreign file names
        ln := regexp_replace(ln, 'Foreign File: .*/([a-z.]+)$', 'Foreign File: .../\1');
        return next ln;
    end loop;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE ios_bitmap (a int NOT NULL, b int not null, pad char(1024) default '')
    WITH (AUTOVACUUM_ENABLED = false, FILLFACTOR = 10)
;
----------
[CREATE_TABLE] -- attached partition
CREATE TABLE pitest1_p2 (f3 bigint, f2 text, f1 date NOT NULL)
;
----------
[DROP_INDEX] -- index

DROP INDEX test_index_exists
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_attribute
;
----------
[DROP_TABLE] -- no updates here
DROP TABLE test12pk, test12fk
;
----------
[CREATE_TABLE] create table inh_child1(f1 int not null)
;
----------
[CREATE_POLICY] /*
 * text search configuration for italian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * italian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY italian_stem
	(TEMPLATE = snowball, Language = italian , StopWords=italian)
;
----------
[DROP_SCHEMA] drop schema stts_s1, stts_s2 cascade
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <@ (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_risparent,
        COMMUTATOR = '@>',
        RESTRICT = ltreeparentsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_PROG_OBJ] -- Event triggers on relations.
CREATE OR REPLACE FUNCTION event_trigger_report_dropped()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r record;
BEGIN
    FOR r IN SELECT * from pg_event_trigger_dropped_objects()
    LOOP
    IF NOT r.normal AND NOT r.original THEN
        CONTINUE;
    END IF;
    RAISE NOTICE 'NORMAL: orig=% normal=% istemp=% type=% identity=% schema=% name=% addr=% args=%',
        r.original, r.normal, r.is_temporary, r.object_type,
        r.object_identity, r.schema_name, r.object_name,
        r.address_names, r.address_args;
    END LOOP;
END; $$
;
----------
[CREATE_TABLE|SELECT] create temp table forc_test as
  select n as i, n as j from generate_series(1,10) n
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f() RETURNS TRIGGER LANGUAGE plpgsql AS 'BEGIN RETURN NULL; END;'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = issn,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE foreign_part (a int) SERVER s0
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_trgm_consistent(internal, int2, text, int4, internal, internal, internal, internal)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.ramp
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table atacc3 drop test2
;
----------
[CREATE_TABLE] CREATE TABLE t1 (a int, b text)
;
----------
[CREATE_TABLE] CREATE TABLE s5.t1 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
----------
[CREATE_TABLE] create table test2(t text COLLATE "C")
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION turkish
	(PARSER = default)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ean13, ismn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[DROP_SCHEMA] DROP SCHEMA concur_reindex_schema CASCADE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27879_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_subscription ADD UNIQUE USING INDEX pg_subscription_subname_index
;
----------
[CREATE_PROG_OBJ] create function br_insert_trigfunc() returns trigger as $$
begin
	new.b := new.b || ' triggered !';
	return new;
end
$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION regress_pub3
;
----------
[CREATE_TABLE] -- make sure that column names are handled correctly
CREATE TABLE mvtest_v (i int, j int)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_bool = 'val4')
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest4b(INOUT b int, INOUT a int)
LANGUAGE SQL
AS $$
CALL ptest4a(a, b);  -- error, not supported
$$
;
----------
[ADD_INDEX] create index on parent(b)
;
----------
[CREATE_TABLE] create table bpchar_tbl (f1 character(16) unique, f2 bpchar)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table toasttest alter column f1 set storage external
;
----------
[CREATE_TABLE] CREATE TABLE money_data (m money)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR - (
	LEFTARG = hstore,
	RIGHTARG = text[],
	PROCEDURE = delete
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest4 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[DROP_SCHEMA] DROP SCHEMA testpub_rf_schema2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regtest_func_1 (text) RETURNS bool
  AS 'BEGIN RETURN true; END' LANGUAGE 'plpgsql'
;
----------
[ALTER_TABLE] ALTER TABLE tab_ins REPLICA IDENTITY FULL
;
----------
[DROP_SCHEMA] DROP SCHEMA matview_schema CASCADE
;
