update grem1 set a = 22 where a = 2
;
insert into j3 values(1),(1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1034', relminmxid = '1'
WHERE oid = '"public"."num_input_test"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3839', relminmxid = '1'
WHERE oid = '"public"."main_table"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"fkpart3"."pk1"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."plt1_p3"'::pg_catalog.regclass
;
INSERT INTO replication_example(somedata, testcolumn1) VALUES (6, 4)
;
-- allow an alias to be specified for DELETE's target table
DELETE FROM delete_test AS dt WHERE dt.a > 75
;
INSERT INTO TIMETZ_TBL VALUES ('08:08 EDT')
;
UPDATE city_view SET country_name = 'Japan' WHERE city_name = 'Tokyo' RETURNING *
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."beta_pos_p1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '589', relminmxid = '1'
WHERE oid = '"public"."text_tbl"'::pg_catalog.regclass
;
insert into cc values(13)
;
INSERT INTO test_prepared1 VALUES (20)
;
INSERT INTO public.tht VALUES ('0', 40)
;
INSERT INTO trunc_d VALUES (1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14847', relminmxid = '3'
WHERE oid = '"public"."prt4_n_p2"'::pg_catalog.regclass
;
INSERT INTO other_tbl_parent VALUES (7),(200)
;
INSERT INTO tst_comp_one_enum_array (a, b) VALUES
		(1, ROW(1.0, '{a, b, c}', 1)),
		(2, ROW(2.0, '{a, b, c}', 2)),
		(3, ROW(3.0, '{a, b, c}', 3)),
		(4, ROW(4.0, '{c, b, d}', 4)),
		(5, ROW(5.0, '{NULL, e, NULL}', 5))
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14963', relminmxid = '3'
WHERE oid = '37443'
;
INSERT INTO arraggtest (f1, f2, f3) VALUES
('{}','{{pink,white,blue,red,grey,orange}}','{2.1,1.87,1.4,2.2}')
;
INSERT INTO num_exp_mul VALUES (2,1,'0')
;
INSERT INTO pg_description (objoid, classoid, objsubid, description) VALUES (0, 0, 2, 'foo')
;
INSERT INTO pg_temp_24.pg_temp_26747_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_24.pg_temp_26747 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_24.pg_temp_26747 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO public.mvtest_tm SELECT (diff.newdata).* FROM pg_temp_13.pg_temp_25000_2 diff WHERE tid IS NULL
;
DELETE FROM "S 1"."T 1" r1 USING ("S 1"."T 3" r2 LEFT JOIN "S 1"."T 4" r3 ON (((r2.c1 = r3.c1)))) WHERE ((r1.c2 = r2.c1)) AND ((r1."C 1" > 1200)) AND (((r1."C 1" % 10) = 0))
;
INSERT INTO public.ths_p3 VALUES (5, 335)
;
INSERT INTO caster (citext)        VALUES ('foo'::varchar)
;
-- Check EXPLAIN ANALYZE for a query that scans empty partitions asynchronously
DELETE FROM async_p1
;
-- a PK update shrinking the referenced multirange and changing the id invalidates the whole multirange (error):
UPDATE temporal_mltrng SET id = '[2,3)', valid_at = datemultirange(daterange('2018-01-15', '2018-03-01'))
  WHERE id = '[1,2)' AND valid_at @> '2018-01-15'::date
;
UPDATE foo SET f4 = f4 + f3 WHERE f4 = 99 RETURNING *
;
UPDATE ONLY "pg_temp_94"."defc" SET "f1" = DEFAULT WHERE $1 OPERATOR(pg_catalog.=) "f1"
;
INSERT INTO public.tplain VALUES ('2', 872)
;
INSERT INTO caster (varchar)       VALUES ('foo'::citext)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2059', relminmxid = '1'
WHERE oid = '"public"."tenk2"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (2, 12)
;
DELETE FROM temporal_rng WHERE id = '[5,6)' AND valid_at = daterange('2018-02-01', '2018-03-01')
;
INSERT INTO public.tplain VALUES ('7', 557)
;
INSERT INTO public.tplain VALUES ('6', 386)
;
DELETE FROM public.order_summary mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_4.pg_temp_37962_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
update arrtest1 set i[2] = 22, t[2] = 'twenty-two'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11989', relminmxid = '3'
WHERE oid = '"public"."credit_card"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14031', relminmxid = '3'
WHERE oid = '35077'
;
-- doesn't match PK, should throw error now
INSERT INTO fktable VALUES (0, 20)
;
INSERT INTO TIME_TBL VALUES ('00:00')
;
INSERT into pitest3(f1, f2, f3) VALUES ('2016-07-6', 'from pitest3', 5)
;
-- rule not fired, so fk violation
insert into rule_and_refint_t3 values (1, 13, 11, 'row6')
  on conflict (id3a, id3b, id3c) do update
  set id3b = excluded.id3b
;
INSERT INTO b_star (class, a) VALUES ('b', 4)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3926', relminmxid = '1'
WHERE oid = '"public"."check_tbl"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8804', relminmxid = '1'
WHERE oid = '"public"."rtest_nothn3"'::pg_catalog.regclass
;
------------------------------------------------------------------------------------------
[UPDATE] update grem1 set a = 22 where a = 2
;
----------
[INSERT] insert into j3 values(1),(1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1034', relminmxid = '1'
WHERE oid = '"public"."num_input_test"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3839', relminmxid = '1'
WHERE oid = '"public"."main_table"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"fkpart3"."pk1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."plt1_p3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO replication_example(somedata, testcolumn1) VALUES (6, 4)
;
----------
[DELETE] -- allow an alias to be specified for DELETE's target table
DELETE FROM delete_test AS dt WHERE dt.a > 75
;
----------
[INSERT] INSERT INTO TIMETZ_TBL VALUES ('08:08 EDT')
;
----------
[UPDATE] UPDATE city_view SET country_name = 'Japan' WHERE city_name = 'Tokyo' RETURNING *
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."beta_pos_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '589', relminmxid = '1'
WHERE oid = '"public"."text_tbl"'::pg_catalog.regclass
;
----------
[INSERT] insert into cc values(13)
;
----------
[INSERT] INSERT INTO test_prepared1 VALUES (20)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 40)
;
----------
[INSERT] INSERT INTO trunc_d VALUES (1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14847', relminmxid = '3'
WHERE oid = '"public"."prt4_n_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO other_tbl_parent VALUES (7),(200)
;
----------
[INSERT|SELECT] INSERT INTO tst_comp_one_enum_array (a, b) VALUES
		(1, ROW(1.0, '{a, b, c}', 1)),
		(2, ROW(2.0, '{a, b, c}', 2)),
		(3, ROW(3.0, '{a, b, c}', 3)),
		(4, ROW(4.0, '{c, b, d}', 4)),
		(5, ROW(5.0, '{NULL, e, NULL}', 5))
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14963', relminmxid = '3'
WHERE oid = '37443'
;
----------
[INSERT|SELECT] INSERT INTO arraggtest (f1, f2, f3) VALUES
('{}','{{pink,white,blue,red,grey,orange}}','{2.1,1.87,1.4,2.2}')
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (2,1,'0')
;
----------
[INSERT|SELECT] INSERT INTO pg_description (objoid, classoid, objsubid, description) VALUES (0, 0, 2, 'foo')
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_24.pg_temp_26747_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_24.pg_temp_26747 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_24.pg_temp_26747 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_tm SELECT (diff.newdata).* FROM pg_temp_13.pg_temp_25000_2 diff WHERE tid IS NULL
;
----------
[DELETE] DELETE FROM "S 1"."T 1" r1 USING ("S 1"."T 3" r2 LEFT JOIN "S 1"."T 4" r3 ON (((r2.c1 = r3.c1)))) WHERE ((r1.c2 = r2.c1)) AND ((r1."C 1" > 1200)) AND (((r1."C 1" % 10) = 0))
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 335)
;
----------
[INSERT|SELECT] INSERT INTO caster (citext)        VALUES ('foo'::varchar)
;
----------
[DELETE] -- Check EXPLAIN ANALYZE for a query that scans empty partitions asynchronously
DELETE FROM async_p1
;
----------
[UPDATE] -- a PK update shrinking the referenced multirange and changing the id invalidates the whole multirange (error):
UPDATE temporal_mltrng SET id = '[2,3)', valid_at = datemultirange(daterange('2018-01-15', '2018-03-01'))
  WHERE id = '[1,2)' AND valid_at @> '2018-01-15'::date
;
----------
[UPDATE] UPDATE foo SET f4 = f4 + f3 WHERE f4 = 99 RETURNING *
;
----------
[UPDATE] UPDATE ONLY "pg_temp_94"."defc" SET "f1" = DEFAULT WHERE $1 OPERATOR(pg_catalog.=) "f1"
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 872)
;
----------
[INSERT|SELECT] INSERT INTO caster (varchar)       VALUES ('foo'::citext)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2059', relminmxid = '1'
WHERE oid = '"public"."tenk2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 12)
;
----------
[DELETE] DELETE FROM temporal_rng WHERE id = '[5,6)' AND valid_at = daterange('2018-02-01', '2018-03-01')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 557)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 386)
;
----------
[DELETE|SELECT] DELETE FROM public.order_summary mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_4.pg_temp_37962_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE] update arrtest1 set i[2] = 22, t[2] = 'twenty-two'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11989', relminmxid = '3'
WHERE oid = '"public"."credit_card"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14031', relminmxid = '3'
WHERE oid = '35077'
;
----------
[INSERT] -- doesn't match PK, should throw error now
INSERT INTO fktable VALUES (0, 20)
;
----------
[INSERT] INSERT INTO TIME_TBL VALUES ('00:00')
;
----------
[INSERT|SELECT] INSERT into pitest3(f1, f2, f3) VALUES ('2016-07-6', 'from pitest3', 5)
;
----------
[MERGE] -- rule not fired, so fk violation
insert into rule_and_refint_t3 values (1, 13, 11, 'row6')
  on conflict (id3a, id3b, id3c) do update
  set id3b = excluded.id3b
;
----------
[INSERT|SELECT] INSERT INTO b_star (class, a) VALUES ('b', 4)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3926', relminmxid = '1'
WHERE oid = '"public"."check_tbl"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8804', relminmxid = '1'
WHERE oid = '"public"."rtest_nothn3"'::pg_catalog.regclass
;
