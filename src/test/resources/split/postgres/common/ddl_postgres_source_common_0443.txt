ANALYZE fkpart3.fk3
;
CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_PKgG'
;
-- Test cases where one side has an extra partition
CREATE TABLE prt2_adv_extra PARTITION OF prt2_adv FOR VALUES FROM (500) TO (MAXVALUE)
;
create table check_con_tbl (f1 int)
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should fail due to implicit strict
  insert into foo values(7,8),(9,10) returning * into x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
ALTER TYPE "public"."stoplight" ADD VALUE 'green'
;
ALTER INDEX pg_toast.pg_toast_33201_index RENAME TO reind_con_toast_idx
;
create type int8alias2
;
ALTER EXTENSION test_pg_dump ADD TABLE test_pg_dump_t1
;
CREATE TABLE num_input_test (n1 numeric)
;
CREATE FUNCTION test_ambiguous_funcname(int) returns int as $$ select $1; $$ language sql
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26293_2 (tid pg_catalog.tid)
;
create table toasted_data (f1 int[])
;
alter table bar2 add column b integer not null default 1
;
CREATE TABLE num_exp_sqrt (id int4, expected numeric(1000,800))
;
create table events (event_id int primary key)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 RENAME c1 TO foreign_column_1
;
-- OK more than one equal column and a && column
create table idxpart (a int4range, b int4range, c int4range) partition by range (a, b)
;
alter table alttype_cluster alter a type bigint
;
-- duplicate column is pretty pointless, but we allow it anyway
CREATE INDEX ON tbl_include_reg (c1, c2) INCLUDE (c1, c3)
;
VACUUM (PARALLEL 0) pg_catalog.pg_sequence
;
create view at_view_2 as select *, to_json(v1) as j from at_view_1 v1
;
CREATE TEMP TABLE frozen_witness (x xid)
;
CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=31667 host=/tmp/EP7K8mZqpu dbname=postgres' PUBLICATION tap_pub_B_2 WITH (origin = none, copy_data = on)
;
CREATE TRIGGER c1_insert_trig
  AFTER INSERT ON part_c_1_100 for each statement execute procedure trigfunc()
;
create function lock_share(int,int) returns int language sql as
  'select pg_advisory_xact_lock_shared($1); select 1;' parallel safe
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_collation
;
COMMENT ON LARGE OBJECT 32005 IS 'I Wandered Lonely as a Cloud'
;
CREATE OR REPLACE FUNCTION insert_unique(k integer, v text) RETURNS void
  LANGUAGE SQL AS $$
    INSERT INTO test (key, val) SELECT k, v WHERE NOT EXISTS (SELECT key FROM test WHERE key = k);
  $$
;
ALTER FOREIGN TABLE split_fdw_repl.remote_accounts ALTER COLUMN owner_name OPTIONS (SET column_name 'owner_name_remote')
;
ALTER TABLE fd_pt1 ADD CONSTRAINT fd_pt1chk1 CHECK (c1 > 0) NO INHERIT
;
ALTER FUNCTION gbt_bit_compress(internal) PARALLEL SAFE
;
/*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "numhword" WITH "public"."hunspell_num", "english_stem"
;
CREATE TABLE gtest23x (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED REFERENCES gtest23a (x) ON UPDATE CASCADE)
;
alter type insenum add value 'i2' before 'L2'
;
alter table p1_c1c2 drop constraint f1_pos
;
-- int2 check

CREATE TABLE int2tmp (a int2)
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '---')
;
create foreign table rem2 (f1 int, f2 text) server loopback options(table_name 'loc2')
;
ALTER FOREIGN TABLE agg_csv INHERIT agg
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_shdescription
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_parameter_acl
;
ALTER TABLE test_ex_constraints DROP CONSTRAINT test_ex_constraints_c_excl
;
ANALYZE pht2
;
CREATE TABLE public.tst_hstore (
		a INTEGER PRIMARY KEY,
		b public.hstore
	)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_language
;
CREATE FUNCTION test_custom_rmgrs_insert_wal_record(IN payload TEXT,
    OUT lsn pg_lsn
)
AS '$libdir/test_custom_rmgrs', 'test_custom_rmgrs_insert_wal_record'
LANGUAGE C STRICT PARALLEL UNSAFE
;
ALTER TABLE pg_toast.pg_toast_30974 RENAME TO reind_con_toast
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE fkpart3.fk3
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_PKgG'
;
----------
[CREATE_TABLE] -- Test cases where one side has an extra partition
CREATE TABLE prt2_adv_extra PARTITION OF prt2_adv FOR VALUES FROM (500) TO (MAXVALUE)
;
----------
[CREATE_TABLE] create table check_con_tbl (f1 int)
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should fail due to implicit strict
  insert into foo values(7,8),(9,10) returning * into x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[ALTER_TYPE] ALTER TYPE "public"."stoplight" ADD VALUE 'green'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33201_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TYPE] create type int8alias2
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD TABLE test_pg_dump_t1
;
----------
[CREATE_TABLE] CREATE TABLE num_input_test (n1 numeric)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_ambiguous_funcname(int) returns int as $$ select $1; $$ language sql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26293_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table toasted_data (f1 int[])
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table bar2 add column b integer not null default 1
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_sqrt (id int4, expected numeric(1000,800))
;
----------
[CREATE_TABLE] create table events (event_id int primary key)
;
----------
[RENAME_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 RENAME c1 TO foreign_column_1
;
----------
[CREATE_TABLE] -- OK more than one equal column and a && column
create table idxpart (a int4range, b int4range, c int4range) partition by range (a, b)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table alttype_cluster alter a type bigint
;
----------
[ADD_INDEX] -- duplicate column is pretty pointless, but we allow it anyway
CREATE INDEX ON tbl_include_reg (c1, c2) INCLUDE (c1, c3)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_sequence
;
----------
[CREATE_VIEW(SELECT)] create view at_view_2 as select *, to_json(v1) as j from at_view_1 v1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE frozen_witness (x xid)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=31667 host=/tmp/EP7K8mZqpu dbname=postgres' PUBLICATION tap_pub_B_2 WITH (origin = none, copy_data = on)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER c1_insert_trig
  AFTER INSERT ON part_c_1_100 for each statement execute procedure trigfunc()
;
----------
[CREATE_PROG_OBJ] create function lock_share(int,int) returns int language sql as
  'select pg_advisory_xact_lock_shared($1); select 1;' parallel safe
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_collation
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 32005 IS 'I Wandered Lonely as a Cloud'
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION insert_unique(k integer, v text) RETURNS void
  LANGUAGE SQL AS $$
    INSERT INTO test (key, val) SELECT k, v WHERE NOT EXISTS (SELECT key FROM test WHERE key = k);
  $$
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE split_fdw_repl.remote_accounts ALTER COLUMN owner_name OPTIONS (SET column_name 'owner_name_remote')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fd_pt1 ADD CONSTRAINT fd_pt1chk1 CHECK (c1 > 0) NO INHERIT
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bit_compress(internal) PARALLEL SAFE
;
----------
[CREATE_POLICY] /*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "numhword" WITH "public"."hunspell_num", "english_stem"
;
----------
[CREATE_TABLE] CREATE TABLE gtest23x (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED REFERENCES gtest23a (x) ON UPDATE CASCADE)
;
----------
[ALTER_TYPE] alter type insenum add value 'i2' before 'L2'
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table p1_c1c2 drop constraint f1_pos
;
----------
[CREATE_TABLE] -- int2 check

CREATE TABLE int2tmp (a int2)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '---')
;
----------
[CREATE_TABLE] create foreign table rem2 (f1 int, f2 text) server loopback options(table_name 'loc2')
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE agg_csv INHERIT agg
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_shdescription
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_parameter_acl
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE test_ex_constraints DROP CONSTRAINT test_ex_constraints_c_excl
;
----------
[ADMIN_TABLE] ANALYZE pht2
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_hstore (
		a INTEGER PRIMARY KEY,
		b public.hstore
	)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_language
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_custom_rmgrs_insert_wal_record(IN payload TEXT,
    OUT lsn pg_lsn
)
AS '$libdir/test_custom_rmgrs', 'test_custom_rmgrs_insert_wal_record'
LANGUAGE C STRICT PARALLEL UNSAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30974 RENAME TO reind_con_toast
;
