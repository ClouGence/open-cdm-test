EXECUTE dumpFunc('35634')
;
RESET enable_indexscan
;
LOCK TABLE "public"."found_test_tbl" IN ACCESS SHARE MODE
;
EXECUTE dumpTableAttach('40239')
;
ROLLBACK TO SAVEPOINT b
;
-- failed (no role membership)
ALTER FUNCTION alt_func2(int) OWNER TO regress_alter_generic_user3
;
-- Queries as other users.
-- Should return records based on current user's policies.
SET SESSION AUTHORIZATION regress_rls_bob
;
ALTER TABLE public.nummultirange_test OWNER TO zyc
;
COPY y TO stdout (FORMAT CSV, FORCE_QUOTE (col2), ESCAPE E'\\')
;
set enable_hashjoin=t
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('35634')
;
----------
[SESSION_SETTING_WRITE] RESET enable_indexscan
;
----------
[SESSION_LOCK] LOCK TABLE "public"."found_test_tbl" IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpTableAttach('40239')
;
----------
[TRANSACTION] ROLLBACK TO SAVEPOINT b
;
----------
[TRANSFER_PRIVILEGE] -- failed (no role membership)
ALTER FUNCTION alt_func2(int) OWNER TO regress_alter_generic_user3
;
----------
[SWITCH_USER] -- Queries as other users.
-- Should return records based on current user's policies.
SET SESSION AUTHORIZATION regress_rls_bob
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.nummultirange_test OWNER TO zyc
;
----------
[DATA_EXPORT] COPY y TO stdout (FORMAT CSV, FORCE_QUOTE (col2), ESCAPE E'\\')
;
----------
[SESSION_SETTING_WRITE] set enable_hashjoin=t
;
