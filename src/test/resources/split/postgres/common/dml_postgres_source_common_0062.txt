INSERT INTO plt1_adv SELECT i, i, to_char(i % 5, 'FM0000') FROM generate_series(0, 24) i
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '18687'
;
insert into parted_conflict_test values (3, 'b') on conflict (a) do update set b = excluded.b
;
INSERT INTO public.ths_p3 VALUES (0, 500)
;
INSERT INTO POINT_TBL(f1) VALUES ('(5.1, 34.5)')
;
UPDATE users SET sometime = '1830-10-04' WHERE id = 1
;
INSERT INTO temporal_fk_mltrng2mltrng (id, valid_at, parent_id) VALUES
  ('[1,2)', datemultirange(daterange('2018-01-15', '2018-02-01')), '[1,2)'),
  ('[2,3)', datemultirange(daterange('2018-01-15', '2018-05-01')), '[1,2)')
;
DELETE FROM fk
;
insert into test ( b , c , d , e , f , g , h , i ) values ( 2.446456 , null , 'v' , 'c' , '2003-03-03 12:33:07 PDT' , false , null , null )
;
INSERT INTO tbl2 (val1, val2, val3) VALUES (1, 1, 1)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."plt1_p1"'::pg_catalog.regclass
;
-- largest and smallest values
INSERT INTO INT4_TBL(f1) VALUES ('2147483647')
;
INSERT INTO num_exp_add VALUES (9,8,'-24851923.045047420')
;
DELETE FROM attmp3 WHERE NOT b > 10
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '593', relminmxid = '1'
WHERE oid = '16648'
;
UPDATE ONLY "pg_temp_6"."tasks" SET "checked_by" = NULL WHERE $1 OPERATOR(pg_catalog.=) "checked_by"
;
-- set the multirange part to NULL
UPDATE  temporal_mltrng3
SET     id = '[1,2)',
        valid_at = NULL
WHERE   id IS NULL AND valid_at @> '2020-06-01'::date
;
INSERT INTO itest1 OVERRIDING SYSTEM VALUE VALUES (20, 'xyz')
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted_def2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
insert into t2a values (200, 2001)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."prt1_l_p2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1458', relminmxid = '1'
WHERE oid = '"public"."tab_core_types"'::pg_catalog.regclass
;
insert into cnullchild values(2)
;
INSERT INTO public.tplain VALUES ('8', 638)
;
DELETE FROM foo WHERE f1 = 4 RETURNING old.*,new.*, *
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13357', relminmxid = '3'
WHERE oid = '"public"."euc_jis_2004_inputs"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."itest9"'::pg_catalog.regclass
;
INSERT INTO num_exp_log10 VALUES (2,'7.53578122160797276459')
;
DELETE FROM tst WHERE i = 8
;
-- the start jsonb is an object, first subscript is treated as a key
delete from test_jsonb_subscript
;
INSERT INTO tab_no_col default VALUES
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_para_p2"'::pg_catalog.regclass
  AND attname IN ('x', 'y')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12730', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_2_2"'::pg_catalog.regclass
;
INSERT INTO pxtest1 VALUES ('fff')
;
insert into cnn_part1 values (null)
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"fkpart5"."fk3"'::pg_catalog.regclass
  AND attname = v.dname
;
INSERT INTO public.order_summary SELECT (diff.newdata).* FROM pg_temp_9.pg_temp_37969_2 diff WHERE tid IS NULL
;
INSERT INTO regress_tblspace_test_tbl (num1, num2, t)
  SELECT round(random()*100), random(), 'text'
  FROM generate_series(1, 10) s(i)
;
insert into ppk values (1)
;
-- Insert a value in PKTABLE for default
INSERT INTO PKTABLE VALUES (-1, -2, 'The Default!')
;
update part_def set a = 'a' where a = 'd'
;
insert into pparent values (2,'parent2')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13138', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_2_1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11541', relminmxid = '1'
WHERE oid = '"testxmlschema"."test2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest20"'::pg_catalog.regclass
;
INSERT INTO pg_temp_9.pg_temp_38079_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_38079 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_9.pg_temp_38079 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO LINE_TBL VALUES ('[(1,2),(1,2)]')
;
INSERT INTO FLOAT4_TBL(f1) VALUES ('10e-70')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16234', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_m_p1"'::pg_catalog.regclass
;
-- should fail:
UPDATE temporal_partitioned_rng SET valid_at = daterange('2016-01-01', '2016-02-01')
  WHERE id = '[5,6)' AND valid_at = daterange('2018-01-01', '2018-02-01')
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] INSERT INTO plt1_adv SELECT i, i, to_char(i % 5, 'FM0000') FROM generate_series(0, 24) i
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '18687'
;
----------
[MERGE] insert into parted_conflict_test values (3, 'b') on conflict (a) do update set b = excluded.b
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 500)
;
----------
[INSERT|SELECT] INSERT INTO POINT_TBL(f1) VALUES ('(5.1, 34.5)')
;
----------
[UPDATE] UPDATE users SET sometime = '1830-10-04' WHERE id = 1
;
----------
[INSERT|SELECT] INSERT INTO temporal_fk_mltrng2mltrng (id, valid_at, parent_id) VALUES
  ('[1,2)', datemultirange(daterange('2018-01-15', '2018-02-01')), '[1,2)'),
  ('[2,3)', datemultirange(daterange('2018-01-15', '2018-05-01')), '[1,2)')
;
----------
[DELETE] DELETE FROM fk
;
----------
[INSERT|SELECT] insert into test ( b , c , d , e , f , g , h , i ) values ( 2.446456 , null , 'v' , 'c' , '2003-03-03 12:33:07 PDT' , false , null , null )
;
----------
[INSERT|SELECT] INSERT INTO tbl2 (val1, val2, val3) VALUES (1, 1, 1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."plt1_p1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- largest and smallest values
INSERT INTO INT4_TBL(f1) VALUES ('2147483647')
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (9,8,'-24851923.045047420')
;
----------
[DELETE] DELETE FROM attmp3 WHERE NOT b > 10
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '593', relminmxid = '1'
WHERE oid = '16648'
;
----------
[UPDATE] UPDATE ONLY "pg_temp_6"."tasks" SET "checked_by" = NULL WHERE $1 OPERATOR(pg_catalog.=) "checked_by"
;
----------
[UPDATE] -- set the multirange part to NULL
UPDATE  temporal_mltrng3
SET     id = '[1,2)',
        valid_at = NULL
WHERE   id IS NULL AND valid_at @> '2020-06-01'::date
;
----------
[INSERT|SELECT] INSERT INTO itest1 OVERRIDING SYSTEM VALUE VALUES (20, 'xyz')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted_def2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] insert into t2a values (200, 2001)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."prt1_l_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1458', relminmxid = '1'
WHERE oid = '"public"."tab_core_types"'::pg_catalog.regclass
;
----------
[INSERT] insert into cnullchild values(2)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 638)
;
----------
[DELETE] DELETE FROM foo WHERE f1 = 4 RETURNING old.*,new.*, *
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13357', relminmxid = '3'
WHERE oid = '"public"."euc_jis_2004_inputs"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."itest9"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_log10 VALUES (2,'7.53578122160797276459')
;
----------
[DELETE] DELETE FROM tst WHERE i = 8
;
----------
[DELETE] -- the start jsonb is an object, first subscript is treated as a key
delete from test_jsonb_subscript
;
----------
[INSERT] INSERT INTO tab_no_col default VALUES
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_para_p2"'::pg_catalog.regclass
  AND attname IN ('x', 'y')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12730', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_2_2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO pxtest1 VALUES ('fff')
;
----------
[INSERT] insert into cnn_part1 values (null)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"fkpart5"."fk3"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT|SELECT] INSERT INTO public.order_summary SELECT (diff.newdata).* FROM pg_temp_9.pg_temp_37969_2 diff WHERE tid IS NULL
;
----------
[INSERT|SELECT] INSERT INTO regress_tblspace_test_tbl (num1, num2, t)
  SELECT round(random()*100), random(), 'text'
  FROM generate_series(1, 10) s(i)
;
----------
[INSERT] insert into ppk values (1)
;
----------
[INSERT] -- Insert a value in PKTABLE for default
INSERT INTO PKTABLE VALUES (-1, -2, 'The Default!')
;
----------
[UPDATE] update part_def set a = 'a' where a = 'd'
;
----------
[INSERT] insert into pparent values (2,'parent2')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13138', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_2_1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11541', relminmxid = '1'
WHERE oid = '"testxmlschema"."test2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest20"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_9.pg_temp_38079_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_38079 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_9.pg_temp_38079 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] INSERT INTO LINE_TBL VALUES ('[(1,2),(1,2)]')
;
----------
[INSERT|SELECT] INSERT INTO FLOAT4_TBL(f1) VALUES ('10e-70')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16234', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_m_p1"'::pg_catalog.regclass
;
----------
[UPDATE] -- should fail:
UPDATE temporal_partitioned_rng SET valid_at = daterange('2016-01-01', '2016-02-01')
  WHERE id = '[5,6)' AND valid_at = daterange('2018-01-01', '2018-02-01')
;
