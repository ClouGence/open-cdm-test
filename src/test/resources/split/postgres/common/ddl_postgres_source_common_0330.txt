ALTER TABLE ONLY "generated_stored_tests"."gtestxx_3" INHERIT "generated_stored_tests"."gtest1"
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_opclass
;
-- typed tables (currently not supported)

CREATE TYPE itest_type AS (f1 integer, f2 text, f3 bigint)
;
create table some_tab_child () inherits(some_tab)
;
-- upper/lower

CREATE TABLE collate_test10 (
    a int,
    x text COLLATE "en-x-icu",
    y text COLLATE "tr-x-icu"
)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
CREATE TABLE another_parttbl1 PARTITION OF another_parttbl FOR VALUES IN (1)
;
-- tests for temporary views

CREATE SCHEMA temp_view_test
    CREATE TABLE base_table (a int, id int)
    CREATE TABLE base_table2 (a int, id int)
;
CREATE VIEW "public"."shoelace" AS
 SELECT "s"."sl_name",
    "s"."sl_avail",
    "s"."sl_color",
    "s"."sl_len",
    "s"."sl_unit",
    ("s"."sl_len" * "u"."un_fact") AS "sl_len_cm"
   FROM "public"."shoelace_data" "s",
    "public"."unit" "u"
  WHERE ("s"."sl_unit" = "u"."un_name")
;
CREATE FOREIGN TABLE t2 (
  c1 integer OPTIONS (column_name 'c1') DEFAULT 42,
  c2 character varying OPTIONS (column_name 'c2') COLLATE pg_catalog."default",
  c3 text OPTIONS (column_name 'c3') COLLATE pg_catalog."POSIX"
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't2')
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
alter table uv_pt1 attach partition uv_pt11 for values from (2) to (5)
;
DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19622 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
create rule rtest_v1_del as on delete to rtest_v1 do instead
	delete from rtest_t1 where a = old.a
;
ALTER TABLE generated_cols REPLICA IDENTITY FULL
;
CREATE TEMP TABLE pg_temp_3.pg_temp_29555_2 (tid pg_catalog.tid)
;
ANALYZE pg_catalog.pg_statistic
;
ALTER TABLE pg_toast.pg_toast_33348 RENAME TO reind_con_toast
;
CREATE TEMP TABLE jspoptest (js json)
;
CREATE TEMP TABLE pg_temp_11.pg_temp_41070_2 (tid pg_catalog.tid)
;
create table permtest_grandchild (c text, b text, a int)
;
create function part_hashint4_noop(value int4, seed int8)
    returns int8 as $$
    select value + seed;
    $$ language sql strict immutable parallel safe
;
CREATE VIEW mysecview4 WITH (security_barrier)
       AS SELECT * FROM tbl1 WHERE a <> 0
;
ALTER TABLE pg_toast.pg_toast_30666 RENAME TO reind_con_toast
;
CREATE FUNCTION test_wchars_to_text(text, int[]) RETURNS text
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
CREATE POLICY p2 ON rls_ptbl USING (a < 0)
;
ALTER TABLE tbl_include_pk add PRIMARY KEY (c1, c2) INCLUDE (c3, c4)
;
ANALYZE remt2
;
CREATE DOMAIN constraint_comments_dom AS int
;
ALTER OPERATOR @> (_ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
DROP OPERATOR FAMILY alt_opf16 USING gist
;
ALTER EXTENSION test_pg_dump ADD SERVER s0
;
CREATE FUNCTION subpath(ltree,int4,int4)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
drop table some_tab cascade
;
CREATE FUNCTION recursion_test(int,int) RETURNS text AS '
DECLARE rslt text;
BEGIN
    IF $1 <= 0 THEN
        rslt = CAST($2 AS TEXT);
    ELSE
        rslt = CAST($1 AS TEXT) || '','' || recursion_test($1 - 1, $2);
    END IF;
    RETURN rslt;
END;' LANGUAGE plpgsql
;
COMMENT ON LARGE OBJECT 16568 IS 'comment on large object'
;
CREATE TEMP TABLE pg_temp_3.pg_temp_27379_2 (tid pg_catalog.tid)
;
create trigger iocdu_tt_parted_insert_trig
  after insert on iocdu_tt_parted referencing new table as new_table
  for each statement execute procedure dump_insert()
;
ALTER TABLE pg_cast ADD PRIMARY KEY USING INDEX pg_cast_oid_index
;
/*
 * text search configuration for irish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * irish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
alter foreign table remt1 inherit parent
;
ALTER TABLE ONLY "public"."prt1_e" ATTACH PARTITION "public"."prt1_e_p1" FOR VALUES FROM (0) TO (250)
;
CREATE TABLE fkpart13_t3 (
  a int NOT NULL,
  FOREIGN KEY (a)
    REFERENCES fkpart13_t2
    ON UPDATE CASCADE ON DELETE CASCADE
)
;
create table atacc1 (test_a int, test_b int)
;
CREATE TABLE "public"."rtest_t3" (
    "a" integer,
    "b" integer
)
;
drop schema abc,aac cascade
;
-- test access to exception data
create function zero_divide() returns int as $$
declare v int := 0;
begin
  return 10 / v;
end;
$$ language plpgsql parallel safe
;
ALTER TABLE test_replica_id_full REPLICA IDENTITY FULL
;
CREATE TABLE split_maint.maint_table (
    id integer PRIMARY KEY,
    name text NOT NULL,
    amount numeric CONSTRAINT amount_positive CHECK (amount >= 0)
)
;
CREATE TABLE t1(a int, b int)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] ALTER TABLE ONLY "generated_stored_tests"."gtestxx_3" INHERIT "generated_stored_tests"."gtest1"
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_opclass
;
----------
[CREATE_TYPE] -- typed tables (currently not supported)

CREATE TYPE itest_type AS (f1 integer, f2 text, f3 bigint)
;
----------
[CREATE_TABLE] create table some_tab_child () inherits(some_tab)
;
----------
[CREATE_TABLE] -- upper/lower

CREATE TABLE collate_test10 (
    a int,
    x text COLLATE "en-x-icu",
    y text COLLATE "tr-x-icu"
)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
----------
[CREATE_TABLE] CREATE TABLE another_parttbl1 PARTITION OF another_parttbl FOR VALUES IN (1)
;
----------
[CREATE_SCHEMA] -- tests for temporary views

CREATE SCHEMA temp_view_test
    CREATE TABLE base_table (a int, id int)
    CREATE TABLE base_table2 (a int, id int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."shoelace" AS
 SELECT "s"."sl_name",
    "s"."sl_avail",
    "s"."sl_color",
    "s"."sl_len",
    "s"."sl_unit",
    ("s"."sl_len" * "u"."un_fact") AS "sl_len_cm"
   FROM "public"."shoelace_data" "s",
    "public"."unit" "u"
  WHERE ("s"."sl_unit" = "u"."un_name")
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t2 (
  c1 integer OPTIONS (column_name 'c1') DEFAULT 42,
  c2 character varying OPTIONS (column_name 'c2') COLLATE pg_catalog."default",
  c3 text OPTIONS (column_name 'c3') COLLATE pg_catalog."POSIX"
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't2')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table uv_pt1 attach partition uv_pt11 for values from (2) to (5)
;
----------
[BLOCK] DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19622 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
----------
[CREATE_POLICY] create rule rtest_v1_del as on delete to rtest_v1 do instead
	delete from rtest_t1 where a = old.a
;
----------
[ALTER_TABLE] ALTER TABLE generated_cols REPLICA IDENTITY FULL
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_29555_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_statistic
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33348 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TEMP TABLE jspoptest (js json)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_41070_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table permtest_grandchild (c text, b text, a int)
;
----------
[CREATE_PROG_OBJ] create function part_hashint4_noop(value int4, seed int8)
    returns int8 as $$
    select value + seed;
    $$ language sql strict immutable parallel safe
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mysecview4 WITH (security_barrier)
       AS SELECT * FROM tbl1 WHERE a <> 0
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30666 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_wchars_to_text(text, int[]) RETURNS text
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON rls_ptbl USING (a < 0)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tbl_include_pk add PRIMARY KEY (c1, c2) INCLUDE (c3, c4)
;
----------
[ADMIN_TABLE] ANALYZE remt2
;
----------
[CREATE_TYPE] CREATE DOMAIN constraint_comments_dom AS int
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @> (_ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf16 USING gist
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD SERVER s0
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION subpath(ltree,int4,int4)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] drop table some_tab cascade
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION recursion_test(int,int) RETURNS text AS '
DECLARE rslt text;
BEGIN
    IF $1 <= 0 THEN
        rslt = CAST($2 AS TEXT);
    ELSE
        rslt = CAST($1 AS TEXT) || '','' || recursion_test($1 - 1, $2);
    END IF;
    RETURN rslt;
END;' LANGUAGE plpgsql
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 16568 IS 'comment on large object'
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_27379_2 (tid pg_catalog.tid)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger iocdu_tt_parted_insert_trig
  after insert on iocdu_tt_parted referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_cast ADD PRIMARY KEY USING INDEX pg_cast_oid_index
;
----------
[CREATE_POLICY] /*
 * text search configuration for irish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * irish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
----------
[ALTER_TABLE] alter foreign table remt1 inherit parent
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_e" ATTACH PARTITION "public"."prt1_e_p1" FOR VALUES FROM (0) TO (250)
;
----------
[CREATE_TABLE] CREATE TABLE fkpart13_t3 (
  a int NOT NULL,
  FOREIGN KEY (a)
    REFERENCES fkpart13_t2
    ON UPDATE CASCADE ON DELETE CASCADE
)
;
----------
[CREATE_TABLE] create table atacc1 (test_a int, test_b int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_t3" (
    "a" integer,
    "b" integer
)
;
----------
[DROP_SCHEMA] drop schema abc,aac cascade
;
----------
[CREATE_PROG_OBJ] -- test access to exception data
create function zero_divide() returns int as $$
declare v int := 0;
begin
  return 10 / v;
end;
$$ language plpgsql parallel safe
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_id_full REPLICA IDENTITY FULL
;
----------
[CREATE_TABLE] CREATE TABLE split_maint.maint_table (
    id integer PRIMARY KEY,
    name text NOT NULL,
    amount numeric CONSTRAINT amount_positive CHECK (amount >= 0)
)
;
----------
[CREATE_TABLE] CREATE TABLE t1(a int, b int)
;
