CREATE INDEX i1b ON t1 USING hash (b)
;
ALTER TABLE "public"."fk_partitioned_fk_5"
    ADD CONSTRAINT "fk_partitioned_fk_5_a_b_fkey1" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b") MATCH FULL ON UPDATE CASCADE ON DELETE CASCADE
;
-- create indexes that will be preferred over the PKs to perform the join
create index j1_id1_idx on j1 (id1) where id1 % 1000 = 1
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', escape '---')
;
CREATE FUNCTION crosstab(text,int)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
-- test ON CONFLICT DO UPDATE with arrays
create temp table arr_pk_tbl (pk int4 primary key, f1 int[])
;
ANALYZE pagg_tab4
;
VACUUM (PARALLEL 2) pg_catalog.pg_replication_origin
;
DROP TRIGGER rw_view1_trig ON rw_view1
;
create or replace function returnssetofholes() returns setof has_hole language plpgsql as
$$
begin
  return query select 1, 2.0;  -- fails
end$$
;
DROP DATABASE foobar8
;
-- interesting dict -> jsonb
CREATE FUNCTION test_dict1() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = {"a": 1, None: 2, 33: 3}
return x
$$
;
ALTER TABLE dump_test.test_table_nn_chld2 VALIDATE CONSTRAINT nn
;
COMMENT ON INDEX six IS NULL
;
-- also exercise the expanded-array case
do $$ declare a int[];
begin
  a := '[-2147483648:-2147483647]={1,2}'::int[];
  a[2147483647] := 42;
end $$
;
CREATE TABLE corruption_test
			WITH (autovacuum_enabled = false) AS
			SELECT
				i,
				repeat('a', 10) AS data
			FROM
				generate_series(1, 8192) i
;
create table PField (
    name	text,
    comment	text
)
;
CREATE PUBLICATION pub_dropped_cols FOR TABLE dropped_cols
;
-- but these work:
CREATE TABLE testschema.dflt (a int PRIMARY KEY USING INDEX TABLESPACE regress_tblspace) PARTITION BY LIST (a) TABLESPACE regress_tblspace
;
create table pp_intrangepart12 partition of pp_intrangepart for values in ('[1,2]')
;
-- cleanup
DROP TABLE list_parted, list_parted2, range_parted, list_parted3
;
ALTER FUNCTION gbtreekey16_in(cstring) PARALLEL SAFE
;
ANALYZE async_pt
;
-- fail for lack of + operator

drop function f1(x anyelement)
;
CREATE TABLE sub2_trigger_activity (tgtab text,
  tgop text, tgwhen text, tglevel text, olda int, newa int)
;
create table pagg_test (x int, y int) with (autovacuum_enabled = off)
;
CREATE FUNCTION texticregexne(citext, text)
RETURNS bool AS 'texticregexne'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION isneq(ean13, issn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
drop index test_range_spgist_idx
;
alter table uv_iocu_tab alter column b set default 'table default'
;
CREATE EXTENSION test_ext_evttrig
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_index
;
CREATE TABLE IF NOT EXISTS persons OF person_type
;
CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WITH CHECK OPTION
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter 'a')
;
CREATE TRIGGER gtest1 BEFORE DELETE OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (OLD.b < 0)  -- ok
  EXECUTE PROCEDURE gtest_trigger_func()
;
-- Ensure an Append scan is chosen when the partition order is a subset of
-- the required order.
create table range_parted (a int, b int, c int) partition by range(a, b)
;
drop table t1 cascade
;
CREATE TABLE inprimary_tab (a int)
;
CREATE FUNCTION btismncmp(ismn, ismn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE users_table_part_1 REPLICA IDENTITY FULL
;
DROP TABLE dropcol
;
ALTER INDEX pg_toast.pg_toast_33036_index RENAME TO reind_con_toast_idx
;
ANALYZE pg_temp_3.pg_temp_29541
;
ALTER TABLE testpub_tbl8_1 ADD PRIMARY KEY (b)
;
ALTER OPERATOR FAMILY gist_macaddr8_ops USING gist ADD
	OPERATOR	6	<> (macaddr8, macaddr8) ,
	FUNCTION	9 (macaddr8, macaddr8) gbt_macad8_fetch (internal)
;
ALTER TABLE pg_toast.pg_toast_30817 RENAME TO reind_con_toast
;
ANALYZE public.pagg_tab_m_p3
;
ANALYZE public.pagg_tab_p3
;
drop index gist_tbl_box_index
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX i1b ON t1 USING hash (b)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."fk_partitioned_fk_5"
    ADD CONSTRAINT "fk_partitioned_fk_5_a_b_fkey1" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b") MATCH FULL ON UPDATE CASCADE ON DELETE CASCADE
;
----------
[ADD_INDEX] -- create indexes that will be preferred over the PKs to perform the join
create index j1_id1_idx on j1 (id1) where id1 % 1000 = 1
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', escape '---')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION crosstab(text,int)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
----------
[CREATE_TABLE] -- test ON CONFLICT DO UPDATE with arrays
create temp table arr_pk_tbl (pk int4 primary key, f1 int[])
;
----------
[ADMIN_TABLE] ANALYZE pagg_tab4
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_replication_origin
;
----------
[DROP_TRIGGER] DROP TRIGGER rw_view1_trig ON rw_view1
;
----------
[CREATE_PROG_OBJ] create or replace function returnssetofholes() returns setof has_hole language plpgsql as
$$
begin
  return query select 1, 2.0;  -- fails
end$$
;
----------
[DROP_CATALOG] DROP DATABASE foobar8
;
----------
[CREATE_PROG_OBJ] -- interesting dict -> jsonb
CREATE FUNCTION test_dict1() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = {"a": 1, None: 2, 33: 3}
return x
$$
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE dump_test.test_table_nn_chld2 VALIDATE CONSTRAINT nn
;
----------
[COMMENT_INDEX] COMMENT ON INDEX six IS NULL
;
----------
[BLOCK] -- also exercise the expanded-array case
do $$ declare a int[];
begin
  a := '[-2147483648:-2147483647]={1,2}'::int[];
  a[2147483647] := 42;
end $$
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE corruption_test
			WITH (autovacuum_enabled = false) AS
			SELECT
				i,
				repeat('a', 10) AS data
			FROM
				generate_series(1, 8192) i
;
----------
[CREATE_TABLE] create table PField (
    name	text,
    comment	text
)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_dropped_cols FOR TABLE dropped_cols
;
----------
[CREATE_TABLE] -- but these work:
CREATE TABLE testschema.dflt (a int PRIMARY KEY USING INDEX TABLESPACE regress_tblspace) PARTITION BY LIST (a) TABLESPACE regress_tblspace
;
----------
[CREATE_TABLE] create table pp_intrangepart12 partition of pp_intrangepart for values in ('[1,2]')
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE list_parted, list_parted2, range_parted, list_parted3
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbtreekey16_in(cstring) PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE async_pt
;
----------
[DROP_PROG_OBJ] -- fail for lack of + operator

drop function f1(x anyelement)
;
----------
[CREATE_TABLE] CREATE TABLE sub2_trigger_activity (tgtab text,
  tgop text, tgwhen text, tglevel text, olda int, newa int)
;
----------
[CREATE_TABLE] create table pagg_test (x int, y int) with (autovacuum_enabled = off)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION texticregexne(citext, text)
RETURNS bool AS 'texticregexne'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ean13, issn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_INDEX] drop index test_range_spgist_idx
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table uv_iocu_tab alter column b set default 'table default'
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext_evttrig
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_index
;
----------
[CREATE_TABLE] CREATE TABLE IF NOT EXISTS persons OF person_type
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WITH CHECK OPTION
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter 'a')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest1 BEFORE DELETE OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (OLD.b < 0)  -- ok
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[CREATE_TABLE] -- Ensure an Append scan is chosen when the partition order is a subset of
-- the required order.
create table range_parted (a int, b int, c int) partition by range(a, b)
;
----------
[DROP_TABLE] drop table t1 cascade
;
----------
[CREATE_TABLE] CREATE TABLE inprimary_tab (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btismncmp(ismn, ismn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE users_table_part_1 REPLICA IDENTITY FULL
;
----------
[DROP_TABLE] DROP TABLE dropcol
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33036_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_29541
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testpub_tbl8_1 ADD PRIMARY KEY (b)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_macaddr8_ops USING gist ADD
	OPERATOR	6	<> (macaddr8, macaddr8) ,
	FUNCTION	9 (macaddr8, macaddr8) gbt_macad8_fetch (internal)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30817 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab_m_p3
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab_p3
;
----------
[DROP_INDEX] drop index gist_tbl_box_index
;
