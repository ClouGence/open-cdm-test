/*
 * 6.24
 * CONSTRAINT_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 6.25
 * CONSTRAINT_TABLE_USAGE view
 */

CREATE VIEW constraint_table_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name

    FROM pg_constraint c, pg_namespace nc,
         pg_class r, pg_namespace nr

    WHERE c.connamespace = nc.oid AND r.relnamespace = nr.oid
          AND ( (c.contype = 'f' AND c.confrelid = r.oid)
             OR (c.contype IN ('p', 'u') AND c.conrelid = r.oid) )
          AND r.relkind IN ('r', 'p')
          AND pg_has_role(r.relowner, 'USAGE')
;
CREATE TABLE b (a_id int)
;
CREATE FOREIGN TABLE analyze_ftable (id int, a text, b bigint)
       SERVER loopback OPTIONS (table_name 'analyze_table')
;
-- dropping the schema should reflect the change in publication
DROP SCHEMA pub_test3
;
CREATE FUNCTION functest_E_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 100'
;
DROP TABLE plt2_adv_p2_1
;
CREATE FUNCTION bt_index_check(index regclass)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
create domain ordered_int8s as two_int8s check((value).q1 <= (value).q2)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26389_2 (tid pg_catalog.tid)
;
-- should fail
create function bogus_func(int)
  returns anymultirange as 'select int4multirange(int4range(1,10))' language sql
;
--
-- injection_points_detach()
--
-- Detaches the current action, if any, from the given injection point.
--
CREATE FUNCTION injection_points_detach(IN point_name TEXT)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_detach'
LANGUAGE C STRICT PARALLEL UNSAFE
;
CREATE TYPE "generated_virtual_tests"."gtestdomain1range" AS RANGE (
    subtype = "generated_virtual_tests"."gtestdomain1",
    multirange_type_name = "generated_virtual_tests"."gtestdomain1multirange"
)
;
ALTER TABLE onek ADD CONSTRAINT onek_check_constraint CHECK (unique1 >= 0)
;
ANALYZE pg_temp_12.pg_temp_26654
;
CREATE TABLE credit_usage (
       cid      int references customer(cid),
       ymd      date,
       usage    int
)
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn13,
	RIGHTARG = issn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
create function expensivefunc(int) returns int
language plpgsql immutable strict cost 10000
as $$begin return $1; end$$
;
create table rtest_comp (
	part	text,
	unit	char(4),
	size	float
)
;
create temp table src (f1 text)
;
alter table tenk2 reset (parallel_workers)
;
ALTER TABLE pg_temp_32.pg_temp_28597_2 ADD COLUMN newdata pg_temp_32.pg_temp_28597
;
create index test2_idx_gist on test2 using gist (t gist_trgm_ops)
;
CREATE TABLE test_jsonb_mutability(js jsonb, b int)
;
DROP TABLE committs_test
;
/* src/test/modules/dummy_index_am/dummy_index_am--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION dihandler(internal)
RETURNS index_am_handler
AS '$libdir/dummy_index_am'
LANGUAGE C
;
create function explain_sq_limit() returns setof text language plpgsql as
$$
declare ln text;
begin
    for ln in
        explain (analyze, summary off, timing off, costs off)
        select * from (select pk,c2 from sq_limit order by c1,pk) as x limit 3
    loop
        ln := regexp_replace(ln, 'Memory: \S*',  'Memory: xxx');
        -- this case might occur if force_parallel_mode is on:
        ln := regexp_replace(ln, 'Worker 0:  Sort Method',  'Sort Method');
        return next ln;
    end loop;
end;
$$
;
-- index cleanup option is ignored if VACUUM FULL
VACUUM (INDEX_CLEANUP TRUE, FULL TRUE) no_index_cleanup
;
ALTER OPERATOR FAMILY "public"."part_test_text_ops" USING "hash" ADD
    FUNCTION 2 ("text", "text") "public"."part_hashtext_length"("text",bigint)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_publication
;
drop table phv_part
;
create table gc1() inherits (c1)
;
DROP OPERATOR <<< (record, record)
;
--
-- CREATE_AGGREGATE
--

-- all functions CREATEd
CREATE AGGREGATE newavg (
   sfunc = int4_avg_accum, basetype = int4, stype = _int8,
   finalfunc = int8_avg,
   initcond1 = '{0,0}'
)
;
-- volatile
CREATE DOMAIN domain3 AS text DEFAULT foo(4)
;
CREATE TABLE table_without_key(id serial, data int)
;
create rule irule1 as on insert to inserttest2 do also
  insert into inserttestb (f3.if2[1], f3.if2[2])
  values (new.f1,new.f2)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_authid
;
CALL transaction_test8()
;
ANALYZE (VERBOSE, SKIP_LOCKED) does_not_exist
;
CREATE RULE log_shoelace AS ON UPDATE TO shoelace_data
        WHERE NEW.sl_avail != OLD.sl_avail
        DO INSERT INTO shoelace_log VALUES (
                                        NEW.sl_name,
                                        NEW.sl_avail,
                                        'Al Bundy',
                                        'epoch'
                                    )
;
CREATE FUNCTION isnle(isbn, isbn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create table lp_g partition of lp for values in ('g')
;
DROP TABLE test_oat_schema.test_oat_tab
;
--    P    P        N    P
-- should ERROR: tfp(anyarray, int) not matched by tfp(anyarray, anyelement)
CREATE AGGREGATE myaggp18a(BASETYPE = int, SFUNC = tfp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
ALTER INDEX pg_toast.pg_toast_28170_index RENAME TO reind_con_toast_idx
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29692 host=/tmp/KsgVe3OAnU dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_forall
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "hword_numpart" WITH "simple"
;
ALTER TABLE itest4 ALTER COLUMN a DROP NOT NULL
;
DROP TRIGGER IF EXISTS accounts_check_update_renamed ON split_trigger_probe.accounts
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.24
 * CONSTRAINT_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 6.25
 * CONSTRAINT_TABLE_USAGE view
 */

CREATE VIEW constraint_table_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name

    FROM pg_constraint c, pg_namespace nc,
         pg_class r, pg_namespace nr

    WHERE c.connamespace = nc.oid AND r.relnamespace = nr.oid
          AND ( (c.contype = 'f' AND c.confrelid = r.oid)
             OR (c.contype IN ('p', 'u') AND c.conrelid = r.oid) )
          AND r.relkind IN ('r', 'p')
          AND pg_has_role(r.relowner, 'USAGE')
;
----------
[CREATE_TABLE] CREATE TABLE b (a_id int)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE analyze_ftable (id int, a text, b bigint)
       SERVER loopback OPTIONS (table_name 'analyze_table')
;
----------
[DROP_SCHEMA] -- dropping the schema should reflect the change in publication
DROP SCHEMA pub_test3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_E_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 100'
;
----------
[DROP_TABLE] DROP TABLE plt2_adv_p2_1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_index_check(index regclass)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TYPE] create domain ordered_int8s as two_int8s check((value).q1 <= (value).q2)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26389_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] -- should fail
create function bogus_func(int)
  returns anymultirange as 'select int4multirange(int4range(1,10))' language sql
;
----------
[CREATE_PROG_OBJ] --
-- injection_points_detach()
--
-- Detaches the current action, if any, from the given injection point.
--
CREATE FUNCTION injection_points_detach(IN point_name TEXT)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_detach'
LANGUAGE C STRICT PARALLEL UNSAFE
;
----------
[CREATE_TYPE] CREATE TYPE "generated_virtual_tests"."gtestdomain1range" AS RANGE (
    subtype = "generated_virtual_tests"."gtestdomain1",
    multirange_type_name = "generated_virtual_tests"."gtestdomain1multirange"
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE onek ADD CONSTRAINT onek_check_constraint CHECK (unique1 >= 0)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26654
;
----------
[CREATE_TABLE] CREATE TABLE credit_usage (
       cid      int references customer(cid),
       ymd      date,
       usage    int
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn13,
	RIGHTARG = issn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_PROG_OBJ] create function expensivefunc(int) returns int
language plpgsql immutable strict cost 10000
as $$begin return $1; end$$
;
----------
[CREATE_TABLE] create table rtest_comp (
	part	text,
	unit	char(4),
	size	float
)
;
----------
[CREATE_TABLE] create temp table src (f1 text)
;
----------
[ALTER_TABLE] alter table tenk2 reset (parallel_workers)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_32.pg_temp_28597_2 ADD COLUMN newdata pg_temp_32.pg_temp_28597
;
----------
[ADD_INDEX] create index test2_idx_gist on test2 using gist (t gist_trgm_ops)
;
----------
[CREATE_TABLE] CREATE TABLE test_jsonb_mutability(js jsonb, b int)
;
----------
[DROP_TABLE] DROP TABLE committs_test
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/dummy_index_am/dummy_index_am--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION dihandler(internal)
RETURNS index_am_handler
AS '$libdir/dummy_index_am'
LANGUAGE C
;
----------
[CREATE_PROG_OBJ] create function explain_sq_limit() returns setof text language plpgsql as
$$
declare ln text;
begin
    for ln in
        explain (analyze, summary off, timing off, costs off)
        select * from (select pk,c2 from sq_limit order by c1,pk) as x limit 3
    loop
        ln := regexp_replace(ln, 'Memory: \S*',  'Memory: xxx');
        -- this case might occur if force_parallel_mode is on:
        ln := regexp_replace(ln, 'Worker 0:  Sort Method',  'Sort Method');
        return next ln;
    end loop;
end;
$$
;
----------
[ADMIN_TABLE] -- index cleanup option is ignored if VACUUM FULL
VACUUM (INDEX_CLEANUP TRUE, FULL TRUE) no_index_cleanup
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY "public"."part_test_text_ops" USING "hash" ADD
    FUNCTION 2 ("text", "text") "public"."part_hashtext_length"("text",bigint)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_publication
;
----------
[DROP_TABLE] drop table phv_part
;
----------
[CREATE_TABLE] create table gc1() inherits (c1)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR <<< (record, record)
;
----------
[CREATE_PROG_OBJ] --
-- CREATE_AGGREGATE
--

-- all functions CREATEd
CREATE AGGREGATE newavg (
   sfunc = int4_avg_accum, basetype = int4, stype = _int8,
   finalfunc = int8_avg,
   initcond1 = '{0,0}'
)
;
----------
[CREATE_TYPE] -- volatile
CREATE DOMAIN domain3 AS text DEFAULT foo(4)
;
----------
[CREATE_TABLE] CREATE TABLE table_without_key(id serial, data int)
;
----------
[CREATE_POLICY] create rule irule1 as on insert to inserttest2 do also
  insert into inserttestb (f3.if2[1], f3.if2[2])
  values (new.f1,new.f2)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_authid
;
----------
[CALL_PROG_OBJ] CALL transaction_test8()
;
----------
[ADMIN_TABLE] ANALYZE (VERBOSE, SKIP_LOCKED) does_not_exist
;
----------
[CREATE_POLICY] CREATE RULE log_shoelace AS ON UPDATE TO shoelace_data
        WHERE NEW.sl_avail != OLD.sl_avail
        DO INSERT INTO shoelace_log VALUES (
                                        NEW.sl_name,
                                        NEW.sl_avail,
                                        'Al Bundy',
                                        'epoch'
                                    )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(isbn, isbn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] create table lp_g partition of lp for values in ('g')
;
----------
[DROP_TABLE] DROP TABLE test_oat_schema.test_oat_tab
;
----------
[CREATE_PROG_OBJ] --    P    P        N    P
-- should ERROR: tfp(anyarray, int) not matched by tfp(anyarray, anyelement)
CREATE AGGREGATE myaggp18a(BASETYPE = int, SFUNC = tfp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28170_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29692 host=/tmp/KsgVe3OAnU dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_forall
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "hword_numpart" WITH "simple"
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest4 ALTER COLUMN a DROP NOT NULL
;
----------
[DROP_TRIGGER] DROP TRIGGER IF EXISTS accounts_check_update_renamed ON split_trigger_probe.accounts
;
