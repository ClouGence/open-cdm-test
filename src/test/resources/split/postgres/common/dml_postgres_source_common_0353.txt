update WSlot set slotlink = blname where slotname = myname
;
INSERT INTO public.tht VALUES ('3', 43)
;
UPDATE footrg SET a = a + 1, b = b || ' update1' WHERE b like '%ABC%'
;
-- date matches but no conflict
INSERT INTO temporal_mltrng (id, valid_at) VALUES ('[2,3)', datemultirange(daterange('2005-01-01', '2006-01-01'))) ON CONFLICT DO NOTHING
;
insert into PSlot values ('PS.first.d1', 'PF1_1', '', 'WS.106.1a')
;
-- ok
DELETE FROM t1p_ones WHERE q IS NULL
;
-- should fail
DELETE FROM rw_view3 WHERE s = sin(0.1)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15741', relminmxid = '3'
WHERE oid = '36450'
;
INSERT INTO public.tht VALUES ('3', 373)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_l_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO public.ths_p1 VALUES (6, 996)
;
INSERT INTO num_exp_add VALUES (8,2,'-34263611.215397047')
;
UPDATE pgbench_tellers SET tbalance = tbalance + -1405 WHERE tid = 5
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."alpha_pos_p3"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16396', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_p2"'::pg_catalog.regclass
;
INSERT INTO num_input_test(n1) VALUES (' -inf ')
;
-- throw in some NULL's and different values
INSERT INTO brintest (inetcol, cidrcol, int4rangecol) SELECT
	inet 'fe80::6e40:8ff:fea9:8c46' + tenthous,
	cidr 'fe80::6e40:8ff:fea9:8c46' + tenthous,
	'empty'::int4range
FROM tenk1 ORDER BY thousand, tenthous LIMIT 25
;
INSERT INTO caster (text)          VALUES ('12.42'::float4)
;
--
-- test FK referencing updates
--

-- slide the edge across a referenced transition:
UPDATE temporal_fk_mltrng2mltrng SET valid_at = datemultirange(daterange('2018-01-02', '2018-02-20')) WHERE id = '[1,2)'
;
INSERT INTO public.test_arrays (a, b, c) VALUES
		('{1,2,3}', '{1.1, 1.2, 1.3}', '{"one", "two", "three"}'),
        ('{3,1,2}', '{1.3, 1.1, 1.2}', '{"three", "one", "two"}')
;
UPDATE rw_view1 SET a = a + 5
;
-- update the range part
UPDATE  temporal_rng3
SET     valid_at = '[2020-01-01,2021-01-01)'
WHERE   id = '[11,12)'
AND     valid_at @> '2018-01-15'::date
;
UPDATE pgbench_tellers SET tbalance = tbalance + 2440 WHERE tid = 10
;
INSERT INTO public.ths_p1 VALUES (4, 544)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12352', relminmxid = '3'
WHERE oid = '"public"."iface"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10473', relminmxid = '1'
WHERE oid = '"public"."ndistinct"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14814', relminmxid = '3'
WHERE oid = '"public"."nv_child_2010"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('0', 570)
;
INSERT INTO public.mvtest_boxmv SELECT (diff.newdata).* FROM pg_temp_9.pg_temp_26520_2 diff WHERE tid IS NULL
;
INSERT INTO T VALUES (5), (6)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16407', relminmxid = '3'
WHERE oid = '38109'
;
--
-- INT8
-- Test int8 64-bit integers.
--

-- int8_tbl was already created and filled in test_setup.sql.
-- Here we just try to insert bad values.

INSERT INTO INT8_TBL(q1) VALUES ('      ')
;
INSERT INTO UNIQUE_TBL (t) VALUES ('six')
;
insert into WSlot values ('WS.003.3b', '003', '', '')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3566', relminmxid = '1'
WHERE oid = '"public"."check2_tbl"'::pg_catalog.regclass
;
INSERT INTO pg_temp_4.pg_temp_27293_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_4.pg_temp_27293 AS newdata FROM public.sro_index_mv mv FULL JOIN pg_temp_4.pg_temp_27293 newdata ON (newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10738', relminmxid = '3'
WHERE oid = '"public"."pslot"'::pg_catalog.regclass
;
INSERT INTO tst_dom_constr VALUES (11)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12149', relminmxid = '1'
WHERE oid = '"public"."rtest_view2"'::pg_catalog.regclass
;
insert into atacc1 (test,test2) values (3, NULL)
;
insert into rule_and_refint_t1 values (2, 21)
;
INSERT INTO public.tht VALUES ('0', 810)
;
INSERT INTO delete_test_table SELECT i, 1, 2, 3 FROM generate_series(1,1000) i
;
INSERT INTO connectby_int VALUES(111,11)
;
INSERT INTO public.tplain VALUES ('8', 858)
;
UPDATE pg_catalog.pg_database SET	datminmxid = '1'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '16440'
;
INSERT INTO z2 VALUES (NULL, 'text')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13873', relminmxid = '3'
WHERE oid = '"public"."tt2"'::pg_catalog.regclass
;
insert into t select mod(i,10),mod(i,10),i from generate_series(1,10000) s(i)
;
------------------------------------------------------------------------------------------
[UPDATE] update WSlot set slotlink = blname where slotname = myname
;
----------
[INSERT] INSERT INTO public.tht VALUES ('3', 43)
;
----------
[UPDATE] UPDATE footrg SET a = a + 1, b = b || ' update1' WHERE b like '%ABC%'
;
----------
[MERGE|SELECT] -- date matches but no conflict
INSERT INTO temporal_mltrng (id, valid_at) VALUES ('[2,3)', datemultirange(daterange('2005-01-01', '2006-01-01'))) ON CONFLICT DO NOTHING
;
----------
[INSERT] insert into PSlot values ('PS.first.d1', 'PF1_1', '', 'WS.106.1a')
;
----------
[DELETE] -- ok
DELETE FROM t1p_ones WHERE q IS NULL
;
----------
[DELETE] -- should fail
DELETE FROM rw_view3 WHERE s = sin(0.1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15741', relminmxid = '3'
WHERE oid = '36450'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('3', 373)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_l_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 996)
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (8,2,'-34263611.215397047')
;
----------
[UPDATE] UPDATE pgbench_tellers SET tbalance = tbalance + -1405 WHERE tid = 5
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."alpha_pos_p3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16396', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_p2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO num_input_test(n1) VALUES (' -inf ')
;
----------
[INSERT|SELECT] -- throw in some NULL's and different values
INSERT INTO brintest (inetcol, cidrcol, int4rangecol) SELECT
	inet 'fe80::6e40:8ff:fea9:8c46' + tenthous,
	cidr 'fe80::6e40:8ff:fea9:8c46' + tenthous,
	'empty'::int4range
FROM tenk1 ORDER BY thousand, tenthous LIMIT 25
;
----------
[INSERT|SELECT] INSERT INTO caster (text)          VALUES ('12.42'::float4)
;
----------
[UPDATE] --
-- test FK referencing updates
--

-- slide the edge across a referenced transition:
UPDATE temporal_fk_mltrng2mltrng SET valid_at = datemultirange(daterange('2018-01-02', '2018-02-20')) WHERE id = '[1,2)'
;
----------
[INSERT|SELECT] INSERT INTO public.test_arrays (a, b, c) VALUES
		('{1,2,3}', '{1.1, 1.2, 1.3}', '{"one", "two", "three"}'),
        ('{3,1,2}', '{1.3, 1.1, 1.2}', '{"three", "one", "two"}')
;
----------
[UPDATE] UPDATE rw_view1 SET a = a + 5
;
----------
[UPDATE] -- update the range part
UPDATE  temporal_rng3
SET     valid_at = '[2020-01-01,2021-01-01)'
WHERE   id = '[11,12)'
AND     valid_at @> '2018-01-15'::date
;
----------
[UPDATE] UPDATE pgbench_tellers SET tbalance = tbalance + 2440 WHERE tid = 10
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 544)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12352', relminmxid = '3'
WHERE oid = '"public"."iface"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10473', relminmxid = '1'
WHERE oid = '"public"."ndistinct"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14814', relminmxid = '3'
WHERE oid = '"public"."nv_child_2010"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 570)
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_boxmv SELECT (diff.newdata).* FROM pg_temp_9.pg_temp_26520_2 diff WHERE tid IS NULL
;
----------
[INSERT] INSERT INTO T VALUES (5), (6)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16407', relminmxid = '3'
WHERE oid = '38109'
;
----------
[INSERT|SELECT] --
-- INT8
-- Test int8 64-bit integers.
--

-- int8_tbl was already created and filled in test_setup.sql.
-- Here we just try to insert bad values.

INSERT INTO INT8_TBL(q1) VALUES ('      ')
;
----------
[INSERT|SELECT] INSERT INTO UNIQUE_TBL (t) VALUES ('six')
;
----------
[INSERT] insert into WSlot values ('WS.003.3b', '003', '', '')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3566', relminmxid = '1'
WHERE oid = '"public"."check2_tbl"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_4.pg_temp_27293_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_4.pg_temp_27293 AS newdata FROM public.sro_index_mv mv FULL JOIN pg_temp_4.pg_temp_27293 newdata ON (newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10738', relminmxid = '3'
WHERE oid = '"public"."pslot"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO tst_dom_constr VALUES (11)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '12149', relminmxid = '1'
WHERE oid = '"public"."rtest_view2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into atacc1 (test,test2) values (3, NULL)
;
----------
[INSERT] insert into rule_and_refint_t1 values (2, 21)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 810)
;
----------
[INSERT|SELECT] INSERT INTO delete_test_table SELECT i, 1, 2, 3 FROM generate_series(1,1000) i
;
----------
[INSERT] INSERT INTO connectby_int VALUES(111,11)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 858)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_database SET	datminmxid = '1'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '16440'
;
----------
[INSERT] INSERT INTO z2 VALUES (NULL, 'text')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13873', relminmxid = '3'
WHERE oid = '"public"."tt2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into t select mod(i,10),mod(i,10),i from generate_series(1,10000) s(i)
;
