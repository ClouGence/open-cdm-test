CREATE TRIGGER "tg_slotlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."hslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_slotlink_a"('HS')
;
ALTER TEXT SEARCH PARSER split_ts_probe.simple_parser RENAME TO simple_parser_renamed
;
create trigger failed after update on parted_trig
  referencing old table as old_table
  for each row execute procedure trigger_nothing()
;
-- fail

create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2.1, 3) $$ language sql
;
--
-- CoerceViaIO
--

-- Create some dummy type to force CoerceViaIO
CREATE TYPE casttesttype
;
CREATE TEMP TABLE uctest(f1 int, f2 text)
;
create table mc3p0 partition of mc3p
  for values from (0, 0, 0) to (0, maxvalue, maxvalue)
;
DROP TABLE pitest2_p1
;
create table twoconstraints (f1 int unique, f2 box,
                             exclude using gist(f2 with &&))
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "email" WITH "simple"
;
CREATE DATABASE tbuiltin2 TEMPLATE template0 LOCALE 'C' LOCALE_PROVIDER builtin
;
-- Test the 'publish_generated_columns' parameter as 'none' and 'stored' for
-- different scenarios with/without generated columns in column lists.
CREATE TABLE gencols (a int, gen1 int GENERATED ALWAYS AS (a * 2) STORED)
;
create aggregate my_rank(VARIADIC "any" ORDER BY VARIADIC "any") (
  stype = internal,
  sfunc = ordered_set_transition_multi,
  finalfunc = rank_final,
  finalfunc_extra = true,
  hypothetical
)
;
ALTER TYPE "public"."planets" ADD VALUE 'neptune'
;
ALTER TABLE ONLY "public"."mlparted_def" ATTACH PARTITION "public"."mlparted_def1" FOR VALUES FROM (40) TO (50)
;
ALTER FUNCTION gbt_bit_same(gbtreekey_var, gbtreekey_var, internal) PARALLEL SAFE
;
CREATE TABLE test_like_6 (a int, c text, b text)
;
CREATE FUNCTION gbt_bytea_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE "public"."domain_test" (
    "a" integer,
    "b" integer
)
;
-- Views
CREATE VIEW view_stats AS SELECT 1::int AS a, 2::int AS b
;
-- Remove the original allow-all policy
DROP POLICY p1 ON rls_test_restrictive
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_operator
;
ALTER TABLE fk3 DROP COLUMN b
;
-- currently can't be pushed down

-- Test with statement-level triggers
CREATE TRIGGER trig_stmt_before
	BEFORE DELETE OR INSERT OR UPDATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
CREATE TABLE "public"."pagg_tab_ml_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
DROP TABLE sch1.tab2
;
CREATE SCHEMA IF NOT EXISTS newschema5 AUTHORIZATION regress_testrolx
;
ANALYZE public.prt1_n_p1
;
-- ON DELETE SET NULL column_list
ALTER TABLE fk_partitioned_fk DROP CONSTRAINT fk_partitioned_fk_a_b_fkey
;
CREATE FUNCTION _int_contains_sel(internal, oid, internal, integer)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
---

CREATE SEQUENCE regtest_seq_1
;
CREATE FUNCTION test_assign_ordered_pair_domain(x int, y int, z int) RETURNS ordered_pair_domain AS $$
declare v ordered_pair_domain := array[x, y];
begin
v[2] := z;
return v;
end
$$ LANGUAGE plpgsql
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_server
;
create or replace function dom_check(int) returns di as $$
declare d di;
begin
  d := $1;
  return d;
end
$$ language plpgsql immutable
;
CREATE TABLE test1 (x int, y int[])
;
-- Check UPDATE with multi-level partitioned inherited target
create table mlparted_tab (a int, b char, c text) partition by list (a)
;
/* contrib/ltree/ltree--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION ltree_recv(internal)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- security barrier view based on inheritance set
CREATE TABLE t1 (a int, b float, c text)
;
DROP TEXT SEARCH DICTIONARY IF EXISTS no_such_schema.foo
;
-- Simple create
CREATE TABLE reloptions_test(i INT) WITH (FiLLFaCToR=30,
	autovacuum_enabled = false, autovacuum_analyze_scale_factor = 0.2)
;
alter table users add column junk text
;
ALTER TABLE tt7 NOT OF
;
CREATE TABLE prt2_p1 PARTITION OF prt2 FOR VALUES FROM (0) TO (250)
;
drop view rules_fooview
;
-- Not OK more than one equal column: partition keys are a proper superset of constraint
create table idxpart (a int4range, b int4range, exclude USING GIST (a with = )) partition by range (a, b)
;
CREATE TABLE range_parted2 (
    a int
) PARTITION BY RANGE(a)
;
-- fail, type not exist

-- also, let's leave one around for purposes of pg_dump testing
CREATE TYPE myshell
;
-- fail
drop table trigpart3
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_collation
;
CREATE AGGREGATE myaggp18b(BASETYPE = int, SFUNC = tfp, STYPE = anyarray,
  INITCOND = '{}')
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_slotlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."hslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_slotlink_a"('HS')
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH PARSER split_ts_probe.simple_parser RENAME TO simple_parser_renamed
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger failed after update on parted_trig
  referencing old table as old_table
  for each row execute procedure trigger_nothing()
;
----------
[CREATE_PROG_OBJ] -- fail

create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2.1, 3) $$ language sql
;
----------
[CREATE_TYPE] --
-- CoerceViaIO
--

-- Create some dummy type to force CoerceViaIO
CREATE TYPE casttesttype
;
----------
[CREATE_TABLE] CREATE TEMP TABLE uctest(f1 int, f2 text)
;
----------
[CREATE_TABLE] create table mc3p0 partition of mc3p
  for values from (0, 0, 0) to (0, maxvalue, maxvalue)
;
----------
[DROP_TABLE] DROP TABLE pitest2_p1
;
----------
[CREATE_TABLE] create table twoconstraints (f1 int unique, f2 box,
                             exclude using gist(f2 with &&))
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "email" WITH "simple"
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin2 TEMPLATE template0 LOCALE 'C' LOCALE_PROVIDER builtin
;
----------
[CREATE_TABLE] -- Test the 'publish_generated_columns' parameter as 'none' and 'stored' for
-- different scenarios with/without generated columns in column lists.
CREATE TABLE gencols (a int, gen1 int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_PROG_OBJ] create aggregate my_rank(VARIADIC "any" ORDER BY VARIADIC "any") (
  stype = internal,
  sfunc = ordered_set_transition_multi,
  finalfunc = rank_final,
  finalfunc_extra = true,
  hypothetical
)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."planets" ADD VALUE 'neptune'
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted_def" ATTACH PARTITION "public"."mlparted_def1" FOR VALUES FROM (40) TO (50)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bit_same(gbtreekey_var, gbtreekey_var, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test_like_6 (a int, c text, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bytea_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."domain_test" (
    "a" integer,
    "b" integer
)
;
----------
[CREATE_VIEW(SELECT)] -- Views
CREATE VIEW view_stats AS SELECT 1::int AS a, 2::int AS b
;
----------
[DROP_POLICY] -- Remove the original allow-all policy
DROP POLICY p1 ON rls_test_restrictive
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_operator
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk3 DROP COLUMN b
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- currently can't be pushed down

-- Test with statement-level triggers
CREATE TRIGGER trig_stmt_before
	BEFORE DELETE OR INSERT OR UPDATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_ml_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[DROP_TABLE] DROP TABLE sch1.tab2
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema5 AUTHORIZATION regress_testrolx
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_n_p1
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- ON DELETE SET NULL column_list
ALTER TABLE fk_partitioned_fk DROP CONSTRAINT fk_partitioned_fk_a_b_fkey
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_contains_sel(internal, oid, internal, integer)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CREATE_SEQUENCE] ---

CREATE SEQUENCE regtest_seq_1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_assign_ordered_pair_domain(x int, y int, z int) RETURNS ordered_pair_domain AS $$
declare v ordered_pair_domain := array[x, y];
begin
v[2] := z;
return v;
end
$$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_server
;
----------
[CREATE_PROG_OBJ] create or replace function dom_check(int) returns di as $$
declare d di;
begin
  d := $1;
  return d;
end
$$ language plpgsql immutable
;
----------
[CREATE_TABLE] CREATE TABLE test1 (x int, y int[])
;
----------
[CREATE_TABLE] -- Check UPDATE with multi-level partitioned inherited target
create table mlparted_tab (a int, b char, c text) partition by list (a)
;
----------
[CREATE_PROG_OBJ] /* contrib/ltree/ltree--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION ltree_recv(internal)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- security barrier view based on inheritance set
CREATE TABLE t1 (a int, b float, c text)
;
----------
[DROP_POLICY] DROP TEXT SEARCH DICTIONARY IF EXISTS no_such_schema.foo
;
----------
[CREATE_TABLE] -- Simple create
CREATE TABLE reloptions_test(i INT) WITH (FiLLFaCToR=30,
	autovacuum_enabled = false, autovacuum_analyze_scale_factor = 0.2)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table users add column junk text
;
----------
[ALTER_TABLE] ALTER TABLE tt7 NOT OF
;
----------
[CREATE_TABLE] CREATE TABLE prt2_p1 PARTITION OF prt2 FOR VALUES FROM (0) TO (250)
;
----------
[DROP_VIEW] drop view rules_fooview
;
----------
[CREATE_TABLE] -- Not OK more than one equal column: partition keys are a proper superset of constraint
create table idxpart (a int4range, b int4range, exclude USING GIST (a with = )) partition by range (a, b)
;
----------
[CREATE_TABLE] CREATE TABLE range_parted2 (
    a int
) PARTITION BY RANGE(a)
;
----------
[CREATE_TYPE] -- fail, type not exist

-- also, let's leave one around for purposes of pg_dump testing
CREATE TYPE myshell
;
----------
[DROP_TABLE] -- fail
drop table trigpart3
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_collation
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp18b(BASETYPE = int, SFUNC = tfp, STYPE = anyarray,
  INITCOND = '{}')
;
