CREATE TABLE POINT_TBL(f1 point)
;
VACUUM (FREEZE) pg_catalog.pg_extension
;
drop index cross_match
;
CREATE SEQUENCE sequence_testx INCREMENT BY 1 START -10
;
ALTER TABLE table_with_unique_not_null REPLICA IDENTITY USING INDEX table_with_unique_not_null_id_key
;
-- HAVING not supported
CREATE VIEW ro_view4 AS SELECT count(*) FROM base_tbl
;
CREATE OR REPLACE FUNCTION
  pg_promote(wait boolean DEFAULT true, wait_seconds integer DEFAULT 60)
  RETURNS boolean STRICT VOLATILE LANGUAGE INTERNAL AS 'pg_promote'
  PARALLEL SAFE
;
CREATE TABLE pvactst2 (i INT) WITH (autovacuum_enabled = off)
;
CREATE TABLE tab1_def (a int PRIMARY KEY, b text, c text DEFAULT 'sub2_tab1_def')
;
CREATE TABLE test_2 AS
    SELECT x.id::bigint,
           repeat('a', 1600) AS value
    FROM generate_series(1, 100) AS x(id)
;
-- opclass in public schema
create index pti2 on pt (sdata seg_ops)
;
CREATE FUNCTION strict_word_similarity_commutator_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
CREATE SCHEMA split_ts_probe
;
/*
 * 5.5
 * SQL_IDENTIFIER domain
 */

CREATE DOMAIN sql_identifier AS name
;
CREATE FUNCTION "public"."return_unnamed_refcursor"() RETURNS "refcursor"
    LANGUAGE "plpgsql"
    AS $$
declare
    rc refcursor;
begin
    open rc for select a from rc_test;
    return rc;
end
$$
;
CREATE TABLE t()
;
ALTER RULE rules_parted_table_insert ON rules_parted_table RENAME TO rules_parted_table_insert_redirect
;
CREATE FUNCTION sort(_int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- test cascade when referencing a partitioned table
CREATE TABLE trunc_a (a INT PRIMARY KEY) PARTITION BY RANGE (a)
;
create rule rules_foorule as on insert to rules_foo where f1 < 100
do instead insert into rules_foo2 values (f1)
;
-- forbidden expressions for partition bounds with range partitioned table
CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (somename) TO ('2019-01-01')
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26471_2 (tid pg_catalog.tid)
;
ALTER TABLE fk ATTACH PARTITION fk3 FOR VALUES IN (3)
;
CREATE TABLE test4nfd (a int, b text)
;
create type nested_int8s as (c1 two_int8s, c2 two_int8s)
;
CREATE TABLE part_null PARTITION OF list_parted FOR VALUES IN (null)
;
ALTER TABLE deferred_excl ADD EXCLUDE (f1 WITH =)
;
CREATE OPERATOR @> (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_isparent,
        COMMUTATOR = '<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ANALYZE public.plt1_p1
;
CREATE TEXT SEARCH CONFIGURATION nepali
	(PARSER = default)
;
CREATE VIEW "public"."my_property_secure" WITH ("security_barrier"='false') AS
 SELECT "cid",
    "name",
    "tel",
    "passwd"
   FROM "public"."customer"
  WHERE ("name" = CURRENT_USER)
;
ALTER INDEX pg_toast.pg_toast_27867_index RENAME TO reind_con_toast_idx
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_default_acl
;
CREATE TABLE part1_self_fk (
    id bigint NOT NULL PRIMARY KEY,
    id_abc bigint
)
;
ALTER OPERATOR ^? (_lquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
DROP TABLE sch2.tbl1_part1
;
CREATE RULE rw_view1_del_rule AS ON DELETE TO rw_view1
  DO INSTEAD DELETE FROM base_tbl WHERE a=OLD.a RETURNING OLD.*
;
CREATE VIEW pg_stat_database_conflicts AS
    SELECT
            D.oid AS datid,
            D.datname AS datname,
            pg_stat_get_db_conflict_tablespace(D.oid) AS confl_tablespace,
            pg_stat_get_db_conflict_lock(D.oid) AS confl_lock,
            pg_stat_get_db_conflict_snapshot(D.oid) AS confl_snapshot,
            pg_stat_get_db_conflict_bufferpin(D.oid) AS confl_bufferpin,
            pg_stat_get_db_conflict_startup_deadlock(D.oid) AS confl_deadlock
    FROM pg_database D
;
CREATE TABLE "public"."inh_test" (
    "a" integer,
    "b" character(1) CONSTRAINT "list_parted2_b_not_null" NOT NULL
)
;
-- not STRICT; allows (null::recordtype)

CREATE FUNCTION hstore_to_array(hstore)
RETURNS text[]
AS '$libdir/hstore','hstore_to_array'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- Alter column type that's part of a partitioned index
create table at_partitioned (a int, b text) partition by range (a)
;
create or replace function stricttest() returns void as $$
declare
x record;
p1 int := 2;
p3 text := $a$'Valame Dios!' dijo Sancho; 'no le dije yo a vuestra merced que mirase bien lo que hacia?'$a$;
begin
  -- no rows
  select * from foo where f1 = p1 and f1::text = p3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
DROP TABLE tab_part2
;
CALL ptest5(10, 'Hello', 20)
;
CREATE VIEW "public"."domview" AS
 SELECT ("domtab"."col1")::"public"."dom" AS "col1"
   FROM "public"."domtab"
;
CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION regress_pub1 WITH (enabled = false)
;
ALTER TABLE pg_toast.pg_toast_30857 RENAME TO reind_con_toast
;
create trigger t before insert or update or delete on parted
  for each row execute function parted_trigfunc()
;
CREATE TABLE tab_rowfilter_child_sync PARTITION OF tab_rowfilter_parent_sync FOR VALUES FROM (1) TO (20)
;
VACUUM (ANALYZE) public.vactable(a, b)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE POINT_TBL(f1 point)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_extension
;
----------
[DROP_INDEX] drop index cross_match
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_testx INCREMENT BY 1 START -10
;
----------
[ALTER_TABLE] ALTER TABLE table_with_unique_not_null REPLICA IDENTITY USING INDEX table_with_unique_not_null_id_key
;
----------
[CREATE_VIEW(SELECT)] -- HAVING not supported
CREATE VIEW ro_view4 AS SELECT count(*) FROM base_tbl
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  pg_promote(wait boolean DEFAULT true, wait_seconds integer DEFAULT 60)
  RETURNS boolean STRICT VOLATILE LANGUAGE INTERNAL AS 'pg_promote'
  PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE pvactst2 (i INT) WITH (autovacuum_enabled = off)
;
----------
[CREATE_TABLE] CREATE TABLE tab1_def (a int PRIMARY KEY, b text, c text DEFAULT 'sub2_tab1_def')
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_2 AS
    SELECT x.id::bigint,
           repeat('a', 1600) AS value
    FROM generate_series(1, 100) AS x(id)
;
----------
[ADD_INDEX] -- opclass in public schema
create index pti2 on pt (sdata seg_ops)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION strict_word_similarity_commutator_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_ts_probe
;
----------
[CREATE_TYPE] /*
 * 5.5
 * SQL_IDENTIFIER domain
 */

CREATE DOMAIN sql_identifier AS name
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."return_unnamed_refcursor"() RETURNS "refcursor"
    LANGUAGE "plpgsql"
    AS $$
declare
    rc refcursor;
begin
    open rc for select a from rc_test;
    return rc;
end
$$
;
----------
[CREATE_TABLE] CREATE TABLE t()
;
----------
[ALTER_POLICY] ALTER RULE rules_parted_table_insert ON rules_parted_table RENAME TO rules_parted_table_insert_redirect
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION sort(_int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- test cascade when referencing a partitioned table
CREATE TABLE trunc_a (a INT PRIMARY KEY) PARTITION BY RANGE (a)
;
----------
[CREATE_POLICY] create rule rules_foorule as on insert to rules_foo where f1 < 100
do instead insert into rules_foo2 values (f1)
;
----------
[CREATE_TABLE] -- forbidden expressions for partition bounds with range partitioned table
CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (somename) TO ('2019-01-01')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26471_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk ATTACH PARTITION fk3 FOR VALUES IN (3)
;
----------
[CREATE_TABLE] CREATE TABLE test4nfd (a int, b text)
;
----------
[CREATE_TYPE] create type nested_int8s as (c1 two_int8s, c2 two_int8s)
;
----------
[CREATE_TABLE] CREATE TABLE part_null PARTITION OF list_parted FOR VALUES IN (null)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE deferred_excl ADD EXCLUDE (f1 WITH =)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @> (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_isparent,
        COMMUTATOR = '<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] ANALYZE public.plt1_p1
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION nepali
	(PARSER = default)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."my_property_secure" WITH ("security_barrier"='false') AS
 SELECT "cid",
    "name",
    "tel",
    "passwd"
   FROM "public"."customer"
  WHERE ("name" = CURRENT_USER)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27867_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] CREATE TABLE part1_self_fk (
    id bigint NOT NULL PRIMARY KEY,
    id_abc bigint
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^? (_lquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_TABLE] DROP TABLE sch2.tbl1_part1
;
----------
[CREATE_POLICY] CREATE RULE rw_view1_del_rule AS ON DELETE TO rw_view1
  DO INSTEAD DELETE FROM base_tbl WHERE a=OLD.a RETURNING OLD.*
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_database_conflicts AS
    SELECT
            D.oid AS datid,
            D.datname AS datname,
            pg_stat_get_db_conflict_tablespace(D.oid) AS confl_tablespace,
            pg_stat_get_db_conflict_lock(D.oid) AS confl_lock,
            pg_stat_get_db_conflict_snapshot(D.oid) AS confl_snapshot,
            pg_stat_get_db_conflict_bufferpin(D.oid) AS confl_bufferpin,
            pg_stat_get_db_conflict_startup_deadlock(D.oid) AS confl_deadlock
    FROM pg_database D
;
----------
[CREATE_TABLE] CREATE TABLE "public"."inh_test" (
    "a" integer,
    "b" character(1) CONSTRAINT "list_parted2_b_not_null" NOT NULL
)
;
----------
[CREATE_PROG_OBJ] -- not STRICT; allows (null::recordtype)

CREATE FUNCTION hstore_to_array(hstore)
RETURNS text[]
AS '$libdir/hstore','hstore_to_array'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Alter column type that's part of a partitioned index
create table at_partitioned (a int, b text) partition by range (a)
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare
x record;
p1 int := 2;
p3 text := $a$'Valame Dios!' dijo Sancho; 'no le dije yo a vuestra merced que mirase bien lo que hacia?'$a$;
begin
  -- no rows
  select * from foo where f1 = p1 and f1::text = p3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[DROP_TABLE] DROP TABLE tab_part2
;
----------
[CALL_PROG_OBJ] CALL ptest5(10, 'Hello', 20)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."domview" AS
 SELECT ("domtab"."col1")::"public"."dom" AS "col1"
   FROM "public"."domtab"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION regress_pub1 WITH (enabled = false)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30857 RENAME TO reind_con_toast
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger t before insert or update or delete on parted
  for each row execute function parted_trigfunc()
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_child_sync PARTITION OF tab_rowfilter_parent_sync FOR VALUES FROM (1) TO (20)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) public.vactable(a, b)
;
