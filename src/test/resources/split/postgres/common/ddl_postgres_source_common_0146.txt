CREATE TABLE "public"."regress_notnull1" (
    "a" integer
)
;
-- OK
ALTER OPERATOR FAMILY alt_opf2 USING hash SET SCHEMA alt_nsp2
;
VACUUM (FREEZE) pg_catalog.pg_constraint
;
-- error: not a procedure
CALL sum(1)
;
CREATE SEQUENCE sequence_test2 START WITH 32
;
CREATE SUBSCRIPTION sub2
						 CONNECTION 'dbname=doesnotexist' PUBLICATION pub1
						 WITH (connect = false, origin = none)
;
DO LANGUAGE plpgsql $x$ BEGIN COMMIT; END $x$
;
CREATE FUNCTION int_custom_ge(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
alter table p11 drop a
;
DROP POLICY p3 on s1
;
ALTER INDEX "fkpart5"."pk_pkey" ATTACH PARTITION "fkpart5"."pk2_pkey"
;
CREATE FUNCTION hstore_version_diag(hstore)
RETURNS integer
AS '$libdir/hstore','hstore_version_diag'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table parted_index_col_drop2 partition of parted_index_col_drop
  for values in (2) partition by list (a)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_partitioned_table
;
ALTER OPERATOR FAMILY split_operator_probe.int_hash_family_renamed USING hash SET SCHEMA split_operator_probe_next
;
CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE ((avg(a)))
;
ALTER TABLE pg_toast.pg_toast_30511 RENAME TO reind_con_toast
;
CREATE TABLE ttable1 OF nothing
;
ALTER TABLE foo_parted ATTACH PARTITION foo_part_d1 FOR VALUES IN (3)
;
DROP RULE IF EXISTS test_rule_exists ON test_exists
;
CREATE DATABASE "regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ" WITH TEMPLATE = template0 OID = 16385 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
--
-- regression test for proper handling of outer joins within antijoins
--

create temp table tt4x(c1 int, c2 int, c3 int)
;
ANALYZE pg_catalog.pg_pltemplate
;
create trigger trig_ins_before_3 before insert on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
-- Test that changing the relkind of a relcache entry doesn't cause
-- trouble. Prior instances of where it did:
-- CALDaNm2yXz+zOtv7y5zBd5WKT8O0Ld3YxikuU3dcyCvxF7gypA@mail.gmail.com
-- CALDaNm3oZA-8Wbps2Jd1g5_Gjrr-x3YWrJPek-mF5Asrrvz2Dg@mail.gmail.com
CREATE TABLE tt26(c int)
;
CREATE FUNCTION isnge(ean13, upc)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ANALYZE public.rtest_nothn4
;
-- no such aggregate for type
drop aggregate newcnt (float4)
;
DROP TABLE regtest_table
;
ALTER TABLE ctlt3 ALTER COLUMN c SET STORAGE EXTERNAL
;
ALTER DATABASE "regression" SET "lc_numeric" TO 'C'
;
CREATE SCHEMA testxmlschema
;
VACUUM FULL large
;
-- Verify behavior for collation (mis)matches
create table idxpart (a text) partition by range (a)
;
CREATE INDEX ON brin_interval_test USING brin (a interval_minmax_multi_ops) WITH (pages_per_range=1)
;
ALTER TEXT SEARCH CONFIGURATION arabic ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
ALTER TABLE ONLY "public"."clstr_tst_inh" INHERIT "public"."clstr_tst"
;
drop table idxpart_temp
;
CREATE FUNCTION gbt_int8_distance(internal,int8,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE gb18030_inputs (inbytes bytea, description text)
;
CREATE TABLE trigger_tab_stats (a int, b int)
;
ALTER TABLE pg_toast.pg_toast_33292 RENAME TO reind_con_toast
;
ALTER TABLE ONLY "fkpart5"."pk2"
    ADD CONSTRAINT "pk2_pkey" PRIMARY KEY ("a")
;
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT shared_blk_read_time float8,
    OUT shared_blk_write_time float8,
    OUT local_blk_read_time float8,
    OUT local_blk_write_time float8,
    OUT temp_blk_read_time float8,
    OUT temp_blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric,
    OUT jit_functions int8,
    OUT jit_generation_time float8,
    OUT jit_inlining_count int8,
    OUT jit_inlining_time float8,
    OUT jit_optimization_count int8,
    OUT jit_optimization_time float8,
    OUT jit_emission_count int8,
    OUT jit_emission_time float8,
    OUT jit_deform_count int8,
    OUT jit_deform_time float8,
    OUT stats_since timestamp with time zone,
    OUT minmax_stats_since timestamp with time zone
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_11'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
CREATE TABLE "public"."persons" (
    "id" integer,
    "name" "text"
)
;
CREATE TABLE "public"."alpha_neg_p3" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
create type comptype as (r float8, i float8)
;
CREATE TABLE fail_part PARTITION OF range_parted3 FOR VALUES FROM (0, minvalue) TO (0, 1)
;
CREATE TABLE my_schema_2.ts2 (x int, y text)
;
create table idxpart (a int, exclude (a with = )) partition by range (a)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "public"."regress_notnull1" (
    "a" integer
)
;
----------
[ALTER_PROG_OBJ] -- OK
ALTER OPERATOR FAMILY alt_opf2 USING hash SET SCHEMA alt_nsp2
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_constraint
;
----------
[CALL_PROG_OBJ] -- error: not a procedure
CALL sum(1)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test2 START WITH 32
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2
						 CONNECTION 'dbname=doesnotexist' PUBLICATION pub1
						 WITH (connect = false, origin = none)
;
----------
[BLOCK] DO LANGUAGE plpgsql $x$ BEGIN COMMIT; END $x$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_ge(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table p11 drop a
;
----------
[DROP_POLICY] DROP POLICY p3 on s1
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart5"."pk_pkey" ATTACH PARTITION "fkpart5"."pk2_pkey"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_version_diag(hstore)
RETURNS integer
AS '$libdir/hstore','hstore_version_diag'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table parted_index_col_drop2 partition of parted_index_col_drop
  for values in (2) partition by list (a)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_partitioned_table
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY split_operator_probe.int_hash_family_renamed USING hash SET SCHEMA split_operator_probe_next
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE ((avg(a)))
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30511 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE ttable1 OF nothing
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE foo_parted ATTACH PARTITION foo_part_d1 FOR VALUES IN (3)
;
----------
[DROP_POLICY] DROP RULE IF EXISTS test_rule_exists ON test_exists
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ" WITH TEMPLATE = template0 OID = 16385 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[CREATE_TABLE] --
-- regression test for proper handling of outer joins within antijoins
--

create temp table tt4x(c1 int, c2 int, c3 int)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_pltemplate
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_ins_before_3 before insert on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[CREATE_TABLE] -- Test that changing the relkind of a relcache entry doesn't cause
-- trouble. Prior instances of where it did:
-- CALDaNm2yXz+zOtv7y5zBd5WKT8O0Ld3YxikuU3dcyCvxF7gypA@mail.gmail.com
-- CALDaNm3oZA-8Wbps2Jd1g5_Gjrr-x3YWrJPek-mF5Asrrvz2Dg@mail.gmail.com
CREATE TABLE tt26(c int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ean13, upc)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_nothn4
;
----------
[DROP_PROG_OBJ] -- no such aggregate for type
drop aggregate newcnt (float4)
;
----------
[DROP_TABLE] DROP TABLE regtest_table
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ctlt3 ALTER COLUMN c SET STORAGE EXTERNAL
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET "lc_numeric" TO 'C'
;
----------
[CREATE_SCHEMA] CREATE SCHEMA testxmlschema
;
----------
[ADMIN_TABLE] VACUUM FULL large
;
----------
[CREATE_TABLE] -- Verify behavior for collation (mis)matches
create table idxpart (a text) partition by range (a)
;
----------
[ADD_INDEX] CREATE INDEX ON brin_interval_test USING brin (a interval_minmax_multi_ops) WITH (pages_per_range=1)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION arabic ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."clstr_tst_inh" INHERIT "public"."clstr_tst"
;
----------
[DROP_TABLE] drop table idxpart_temp
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int8_distance(internal,int8,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE gb18030_inputs (inbytes bytea, description text)
;
----------
[CREATE_TABLE] CREATE TABLE trigger_tab_stats (a int, b int)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33292 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart5"."pk2"
    ADD CONSTRAINT "pk2_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT shared_blk_read_time float8,
    OUT shared_blk_write_time float8,
    OUT local_blk_read_time float8,
    OUT local_blk_write_time float8,
    OUT temp_blk_read_time float8,
    OUT temp_blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric,
    OUT jit_functions int8,
    OUT jit_generation_time float8,
    OUT jit_inlining_count int8,
    OUT jit_inlining_time float8,
    OUT jit_optimization_count int8,
    OUT jit_optimization_time float8,
    OUT jit_emission_count int8,
    OUT jit_emission_time float8,
    OUT jit_deform_count int8,
    OUT jit_deform_time float8,
    OUT stats_since timestamp with time zone,
    OUT minmax_stats_since timestamp with time zone
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_11'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."persons" (
    "id" integer,
    "name" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."alpha_neg_p3" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TYPE] create type comptype as (r float8, i float8)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted3 FOR VALUES FROM (0, minvalue) TO (0, 1)
;
----------
[CREATE_TABLE] CREATE TABLE my_schema_2.ts2 (x int, y text)
;
----------
[CREATE_TABLE] create table idxpart (a int, exclude (a with = )) partition by range (a)
;
