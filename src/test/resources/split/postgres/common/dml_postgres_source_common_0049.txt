insert into sq_limit values
    (1, 1, 1),
    (2, 2, 2),
    (3, 3, 3),
    (4, 4, 4),
    (5, 1, 1),
    (6, 2, 2),
    (7, 3, 3),
    (8, 4, 4)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8671', relminmxid = '1'
WHERE oid = '"public"."rtest_unitfact"'::pg_catalog.regclass
;
insert into ac (aa) values (NULL)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11525', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest25"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8419', relminmxid = '1'
WHERE oid = '"public"."rtest_t4"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'class'
  AND attrelid = '"public"."f_star"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."gtest_tableoid"'::pg_catalog.regclass
;
UPDATE pgbench_accounts SET abalance = abalance + 309 WHERE aid = 14159
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"regress_indexing"."pk4"'::pg_catalog.regclass
  AND attname IN ('a')
;
UPDATE public.base_tbl2 SET c = (c || c) WHERE ((b = 0)) RETURNING a, b, c
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18539', relminmxid = '3'
WHERE oid = '"public"."idxpart22"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('9', 479)
;
-- a PK update changing only the id invalidates the whole range (error):
UPDATE temporal_rng SET id = '[2,3)'
  WHERE id = '[1,2)' AND valid_at @> '2018-01-15'::date
;
INSERT INTO stream_test SELECT repeat('a', 10) || g.i FROM generate_series(1, 20) g(i)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p3_p2"'::pg_catalog.regclass
;
-- should fail
UPDATE rw_view14 SET b='ROW 3' WHERE a=3
;
INSERT INTO public.tplain VALUES ('2', 442)
;
DELETE FROM public.sro_index_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_12.pg_temp_29625_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17670', relminmxid = '3'
WHERE oid = '39948'
;
insert into insertconflicttest values (3, 'Kiwi') on conflict (key, fruit) do update set insertconflicttest.fruit = 'Mango'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16508', relminmxid = '3'
WHERE oid = '"public"."tt7"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '35607'
;
INSERT INTO public.tplain VALUES ('8', 798)
;
DELETE FROM main_view WHERE a = 31 RETURNING a, b
;
INSERT INTO public.tru_rtable0(id) VALUES ($1)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt1_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
insert into �t�Ӹ�� values ('�s�y�~', '�]���������q', '��B10��')
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart4"."droppk1"'::pg_catalog.regclass
  AND attname IN ('a')
;
insert into PHone values ('PH.hc003', 'Hicom standard', 'WS.002.2a')
;
INSERT INTO public.ths_p3 VALUES (1, 881)
;
INSERT INTO itest13 VALUES (1), (2), (3)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15479', relminmxid = '3'
WHERE oid = '"public"."prt1_e_p1"'::pg_catalog.regclass
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.2........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."covidxpart3"'::pg_catalog.regclass
  AND attname = v.dname
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10709', relminmxid = '1'
WHERE oid = '29277'
;
-- join UPDATE test
UPDATE city_view v SET population = 599657
    FROM city_table ci, country_table co
    WHERE ci.city_name = 'Washington DC' and co.country_name = 'USA'
    AND v.city_id = ci.city_id AND v.country_name = co.country_name
    RETURNING co.country_id, v.country_name,
              v.city_id, v.city_name, v.population
;
insert into ta values(2,2)
;
INSERT INTO num_exp_log10 VALUES (7,'7.91922711353275546914')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'f1'
  AND attrelid = '"public"."invalid_check_con_child"'::pg_catalog.regclass
;
INSERT INTO c(aa) VALUES('ccccccc')
;
INSERT INTO LINE_TBL VALUES ('{}')
;
UPDATE foo_parted_v SET a = 1, c = c || '->P1' WHERE a = 2 AND c = 'P2'
  RETURNING 'P2:'||old.dummy, 'P1:'||new.dummy
;
UPDATE list_parted t1 set a = 2 FROM non_parted t2 WHERE t1.a = t2.id and a = 1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '749', relminmxid = '1'
WHERE oid = '"public"."onek2"'::pg_catalog.regclass
;
INSERT INTO rw_view1 VALUES (null, null, 1.1, null)
;
INSERT INTO public.tplain VALUES ('9', 459)
;
INSERT INTO rw_view1 (id, idplus1) VALUES (6, 7)
;
-- not allowed
INSERT INTO rw_view1 VALUES ('Row 5', 5.0, 5)
;
UPDATE conf_tab set a=6, b=7, c=8 where a=5
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6507', relminmxid = '1'
WHERE oid = '24904'
;
INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(3501,4000) s(i)
;
------------------------------------------------------------------------------------------
[INSERT] insert into sq_limit values
    (1, 1, 1),
    (2, 2, 2),
    (3, 3, 3),
    (4, 4, 4),
    (5, 1, 1),
    (6, 2, 2),
    (7, 3, 3),
    (8, 4, 4)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8671', relminmxid = '1'
WHERE oid = '"public"."rtest_unitfact"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into ac (aa) values (NULL)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '11525', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest25"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8419', relminmxid = '1'
WHERE oid = '"public"."rtest_t4"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'class'
  AND attrelid = '"public"."f_star"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."gtest_tableoid"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE pgbench_accounts SET abalance = abalance + 309 WHERE aid = 14159
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"regress_indexing"."pk4"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[UPDATE] UPDATE public.base_tbl2 SET c = (c || c) WHERE ((b = 0)) RETURNING a, b, c
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18539', relminmxid = '3'
WHERE oid = '"public"."idxpart22"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 479)
;
----------
[UPDATE] -- a PK update changing only the id invalidates the whole range (error):
UPDATE temporal_rng SET id = '[2,3)'
  WHERE id = '[1,2)' AND valid_at @> '2018-01-15'::date
;
----------
[INSERT|SELECT] INSERT INTO stream_test SELECT repeat('a', 10) || g.i FROM generate_series(1, 20) g(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p3_p2"'::pg_catalog.regclass
;
----------
[UPDATE] -- should fail
UPDATE rw_view14 SET b='ROW 3' WHERE a=3
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 442)
;
----------
[DELETE|SELECT] DELETE FROM public.sro_index_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_12.pg_temp_29625_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17670', relminmxid = '3'
WHERE oid = '39948'
;
----------
[MERGE] insert into insertconflicttest values (3, 'Kiwi') on conflict (key, fruit) do update set insertconflicttest.fruit = 'Mango'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16508', relminmxid = '3'
WHERE oid = '"public"."tt7"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '35607'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 798)
;
----------
[DELETE] DELETE FROM main_view WHERE a = 31 RETURNING a, b
;
----------
[INSERT|SELECT] INSERT INTO public.tru_rtable0(id) VALUES ($1)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt1_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] insert into �t�Ӹ�� values ('�s�y�~', '�]���������q', '��B10��')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart4"."droppk1"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[INSERT] insert into PHone values ('PH.hc003', 'Hicom standard', 'WS.002.2a')
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 881)
;
----------
[INSERT] INSERT INTO itest13 VALUES (1), (2), (3)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15479', relminmxid = '3'
WHERE oid = '"public"."prt1_e_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.2........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."covidxpart3"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10709', relminmxid = '1'
WHERE oid = '29277'
;
----------
[UPDATE] -- join UPDATE test
UPDATE city_view v SET population = 599657
    FROM city_table ci, country_table co
    WHERE ci.city_name = 'Washington DC' and co.country_name = 'USA'
    AND v.city_id = ci.city_id AND v.country_name = co.country_name
    RETURNING co.country_id, v.country_name,
              v.city_id, v.city_name, v.population
;
----------
[INSERT] insert into ta values(2,2)
;
----------
[INSERT] INSERT INTO num_exp_log10 VALUES (7,'7.91922711353275546914')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'f1'
  AND attrelid = '"public"."invalid_check_con_child"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO c(aa) VALUES('ccccccc')
;
----------
[INSERT] INSERT INTO LINE_TBL VALUES ('{}')
;
----------
[UPDATE] UPDATE foo_parted_v SET a = 1, c = c || '->P1' WHERE a = 2 AND c = 'P2'
  RETURNING 'P2:'||old.dummy, 'P1:'||new.dummy
;
----------
[UPDATE] UPDATE list_parted t1 set a = 2 FROM non_parted t2 WHERE t1.a = t2.id and a = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '749', relminmxid = '1'
WHERE oid = '"public"."onek2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO rw_view1 VALUES (null, null, 1.1, null)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 459)
;
----------
[INSERT|SELECT] INSERT INTO rw_view1 (id, idplus1) VALUES (6, 7)
;
----------
[INSERT] -- not allowed
INSERT INTO rw_view1 VALUES ('Row 5', 5.0, 5)
;
----------
[UPDATE] UPDATE conf_tab set a=6, b=7, c=8 where a=5
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6507', relminmxid = '1'
WHERE oid = '24904'
;
----------
[INSERT|SELECT] INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(3501,4000) s(i)
;
