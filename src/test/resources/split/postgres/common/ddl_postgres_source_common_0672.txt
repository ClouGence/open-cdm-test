CREATE TABLE beta_pos_p1 PARTITION OF beta_pos FOR VALUES IN ('0002', '0003')
;
CREATE VIEW xmlview1 AS SELECT xmlcomment('test')
;
create table s3.p1_1 partition of s3.p1 for values in (1, 2, 3)
;
-- Cleanup: list_parted no longer needed.
DROP TABLE list_parted
;
CREATE FOREIGN TABLE header_doesnt_match (a int, foo text) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/list1.csv', delimiter ',', header 'match')
;
COMMENT ON TEXT SEARCH CONFIGURATION armenian IS 'configuration for armenian language'
;
CREATE TABLE mcv_lists_arrays (
    a TEXT[],
    b NUMERIC[],
    c INT[]
)
;
-- Adding identity to an identity column is not allowed
ALTER TABLE pitest1_p2 ALTER COLUMN f3 ADD GENERATED BY DEFAULT AS IDENTITY
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_ts_parser
;
ANALYZE public.rule_and_refint_t2
;
ALTER TABLE ONLY "public"."gtest10" DROP COLUMN "........pg.dropped.3........"
;
CREATE TABLE tst (
	i	int4,
	t	text
)
;
create rule rtest_t4_ins2 as on insert to rtest_t4
		where new.a >= 20 and new.a < 30 do
	insert into rtest_t6 values (new.a, new.b)
;
-- Check that addition or removal of any partition is correctly dealt with by
-- default partition table when it is being used in prepared statement.
create table pc_list_parted (a int) partition by list(a)
;
CREATE PUBLICATION pub2 FOR TABLE tab2 WITH (publish = insert)
;
create temp table people (fn fullname, bd date)
;
CREATE FUNCTION gbt_time_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
drop table comptable
;
alter trigger a on grandparent rename to b
;
CREATE OPERATOR <<@@ ( PROCEDURE = pt_contained_poly,
  LEFTARG = point, RIGHTARG = polygon )
;
CREATE UNIQUE INDEX u0rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  TABLESPACE regress_create_idx_tblspace
  WHERE s.index_row_if(y)
;
CREATE TABLE pagg_tab_ml_p1 PARTITION OF pagg_tab_ml FOR VALUES FROM (0) TO (12)
;
ALTER TEXT SEARCH CONFIGURATION greek ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
CREATE TABLE pagg_tab_m_p2 PARTITION OF pagg_tab_m FOR VALUES FROM (12, 12) TO (22, 22)
;
ANALYZE pg_temp_12.pg_temp_29625
;
CREATE TABLE pk2 PARTITION OF pk FOR VALUES IN (2)
;
--
-- page_checksum()
--
CREATE FUNCTION page_checksum(IN page bytea, IN blkno int4)
RETURNS smallint
AS '$libdir/pageinspect', 'page_checksum'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE SUBSCRIPTION sub3 CONNECTION 'port=18237 host=/tmp/6EQjqv0kuf dbname=postgres' PUBLICATION pub3
;
ALTER TABLE fprt2_p2 SET (autovacuum_enabled = 'false')
;
DROP TABLE tt9
;
ALTER TABLE pg_init_privs ADD PRIMARY KEY USING INDEX pg_init_privs_o_c_o_index
;
drop table test_01
;
CREATE TABLE "public"."utf8_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
--    N    P        N    N
-- should CREATE
CREATE AGGREGATE myaggp09a(BASETYPE = int, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
CREATE TEMP TABLE pg_temp_32.pg_temp_28237_2 (tid pg_catalog.tid)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_amproc
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_extension
;
DROP INDEX CONCURRENTLY "concur_heap_expr_idx"
;
create index hidx on testhstore using btree (h)
;
create index fkeys_i on fkeys (fkey1, fkey2)
;
CREATE FUNCTION isnne(issn, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- notice for odd 'a' values, long 'b' values
drop table parted_trigger
;
create table iocdu_tt_parted1 partition of iocdu_tt_parted for values in (1)
;
COMMENT ON COLUMN ctl_table.b IS 'Column b'
;
CREATE TABLE regtest_ptable_tens PARTITION OF regtest_ptable FOR VALUES FROM ('10') TO ('100')
;
create rule rtest_emp_upd as on update to rtest_emp where new.salary != old.salary do
	insert into rtest_emplog values (new.ename, current_user,
			'honored', new.salary, old.salary)
;
-- Generate enough garbage tuples in index to ensure that even the unique index
-- with deduplication enabled has to check multiple leaf pages during unique
-- checking (at least with a BLCKSZ of 8192 or less)
DO $$
BEGIN
    FOR r IN 1..1350 LOOP
        DELETE FROM dedup_unique_test_table;
        INSERT INTO dedup_unique_test_table SELECT 1;
    END LOOP;
END$$
;
ALTER TABLE rls_tbl ENABLE ROW LEVEL SECURITY
;
VACUUM (ANALYZE) vacowned
;
-- ERROR

DROP TABLE fd_pt1 CASCADE
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE beta_pos_p1 PARTITION OF beta_pos FOR VALUES IN ('0002', '0003')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmlview1 AS SELECT xmlcomment('test')
;
----------
[CREATE_TABLE] create table s3.p1_1 partition of s3.p1 for values in (1, 2, 3)
;
----------
[DROP_TABLE] -- Cleanup: list_parted no longer needed.
DROP TABLE list_parted
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE header_doesnt_match (a int, foo text) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/list1.csv', delimiter ',', header 'match')
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION armenian IS 'configuration for armenian language'
;
----------
[CREATE_TABLE] CREATE TABLE mcv_lists_arrays (
    a TEXT[],
    b NUMERIC[],
    c INT[]
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Adding identity to an identity column is not allowed
ALTER TABLE pitest1_p2 ALTER COLUMN f3 ADD GENERATED BY DEFAULT AS IDENTITY
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_ts_parser
;
----------
[ADMIN_TABLE] ANALYZE public.rule_and_refint_t2
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."gtest10" DROP COLUMN "........pg.dropped.3........"
;
----------
[CREATE_TABLE] CREATE TABLE tst (
	i	int4,
	t	text
)
;
----------
[CREATE_POLICY] create rule rtest_t4_ins2 as on insert to rtest_t4
		where new.a >= 20 and new.a < 30 do
	insert into rtest_t6 values (new.a, new.b)
;
----------
[CREATE_TABLE] -- Check that addition or removal of any partition is correctly dealt with by
-- default partition table when it is being used in prepared statement.
create table pc_list_parted (a int) partition by list(a)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub2 FOR TABLE tab2 WITH (publish = insert)
;
----------
[CREATE_TABLE] create temp table people (fn fullname, bd date)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] drop table comptable
;
----------
[RENAME_TRIGGER] alter trigger a on grandparent rename to b
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <<@@ ( PROCEDURE = pt_contained_poly,
  LEFTARG = point, RIGHTARG = polygon )
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX u0rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  TABLESPACE regress_create_idx_tblspace
  WHERE s.index_row_if(y)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p1 PARTITION OF pagg_tab_ml FOR VALUES FROM (0) TO (12)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION greek ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_m_p2 PARTITION OF pagg_tab_m FOR VALUES FROM (12, 12) TO (22, 22)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_29625
;
----------
[CREATE_TABLE] CREATE TABLE pk2 PARTITION OF pk FOR VALUES IN (2)
;
----------
[CREATE_PROG_OBJ] --
-- page_checksum()
--
CREATE FUNCTION page_checksum(IN page bytea, IN blkno int4)
RETURNS smallint
AS '$libdir/pageinspect', 'page_checksum'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub3 CONNECTION 'port=18237 host=/tmp/6EQjqv0kuf dbname=postgres' PUBLICATION pub3
;
----------
[ALTER_TABLE] ALTER TABLE fprt2_p2 SET (autovacuum_enabled = 'false')
;
----------
[DROP_TABLE] DROP TABLE tt9
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_init_privs ADD PRIMARY KEY USING INDEX pg_init_privs_o_c_o_index
;
----------
[DROP_TABLE] drop table test_01
;
----------
[CREATE_TABLE] CREATE TABLE "public"."utf8_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
----------
[CREATE_PROG_OBJ] --    N    P        N    N
-- should CREATE
CREATE AGGREGATE myaggp09a(BASETYPE = int, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_32.pg_temp_28237_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_amproc
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_extension
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY "concur_heap_expr_idx"
;
----------
[ADD_INDEX] create index hidx on testhstore using btree (h)
;
----------
[ADD_INDEX] create index fkeys_i on fkeys (fkey1, fkey2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(issn, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] -- notice for odd 'a' values, long 'b' values
drop table parted_trigger
;
----------
[CREATE_TABLE] create table iocdu_tt_parted1 partition of iocdu_tt_parted for values in (1)
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN ctl_table.b IS 'Column b'
;
----------
[CREATE_TABLE] CREATE TABLE regtest_ptable_tens PARTITION OF regtest_ptable FOR VALUES FROM ('10') TO ('100')
;
----------
[CREATE_POLICY] create rule rtest_emp_upd as on update to rtest_emp where new.salary != old.salary do
	insert into rtest_emplog values (new.ename, current_user,
			'honored', new.salary, old.salary)
;
----------
[BLOCK] -- Generate enough garbage tuples in index to ensure that even the unique index
-- with deduplication enabled has to check multiple leaf pages during unique
-- checking (at least with a BLCKSZ of 8192 or less)
DO $$
BEGIN
    FOR r IN 1..1350 LOOP
        DELETE FROM dedup_unique_test_table;
        INSERT INTO dedup_unique_test_table SELECT 1;
    END LOOP;
END$$
;
----------
[ALTER_TABLE] ALTER TABLE rls_tbl ENABLE ROW LEVEL SECURITY
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) vacowned
;
----------
[DROP_TABLE] -- ERROR

DROP TABLE fd_pt1 CASCADE
;
