CREATE OPERATOR CLASS macaddr_ops
DEFAULT FOR TYPE macaddr USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       macaddr_cmp(macaddr,macaddr),
    FUNCTION        2       gin_extract_value_macaddr(macaddr, internal),
    FUNCTION        3       gin_extract_query_macaddr(macaddr, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_macaddr(macaddr,macaddr,int2, internal),
STORAGE         macaddr
;
ALTER INDEX pg_toast.pg_toast_30507_index RENAME TO reind_con_toast_idx
;
DROP FUNCTION truncate_in_subxact()
;
CREATE TABLE decoding_test(x integer, y text)
;
DROP PROCEDURE test_proc4
;
ANALYZE bt_f8_heap
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist password=regress_fakepassword' PUBLICATION testpub WITH (connect = false)
;
-- check also that the wholerow vars in RETURNING list are converted as needed
alter table returningwrtest add b text
;
CREATE POLICY p3 ON r2 FOR UPDATE USING (false)
;
drop function f1(x anycompatiblerange, y anycompatiblearray)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
-- Test display of default privileges with \pset null.
CREATE TABLE defprivs (a int)
;
ALTER TABLE itest13 ADD COLUMN b int GENERATED BY DEFAULT AS IDENTITY
;
CREATE VIEW "public"."rtest_vcomp" AS
 SELECT "x"."part",
    ("x"."size" * "y"."factor") AS "size_in_cm"
   FROM "public"."rtest_comp" "x",
    "public"."rtest_unitfact" "y"
  WHERE ("x"."unit" = "y"."unit")
;
ANALYZE public.brin_test
;
CREATE FUNCTION "public"."my_gen_series"(integer, integer) RETURNS SETOF integer
    LANGUAGE "internal" IMMUTABLE STRICT SUPPORT "public"."test_support_func" PARALLEL SAFE
    AS $$generate_series_int4$$
;
ANALYZE pg_temp_4.pg_temp_27293
;
DROP SEQUENCE regtest_seq
;
VACUUM (PARALLEL 2) pg_catalog.pg_foreign_server
;
CREATE INDEX test_ne_idx ON test_ne USING gist (a, b)
;
-- invalid use of subquery
CREATE TABLE default_expr_agg (a int DEFAULT (select 1))
;
-- error, wrong result type
CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int) RETURNS float
AS 'select $1+1' LANGUAGE sql
;
VACUUM tail_tbl
;
-- add a check constraint (fails)
alter table atacc1 add constraint atacc_test1 check (test+test2<test3*4)
;
-- Astronomers may want to change the earth function so that distances will be
-- returned in degrees. To do this comment out the above definition and
-- uncomment the one below. Note that doing this will break the regression
-- tests.
--
-- CREATE FUNCTION earth() RETURNS float8
-- LANGUAGE SQL IMMUTABLE
-- AS 'SELECT 180/pi()';

-- Define domain for locations on the surface of the earth using a cube
-- datatype with constraints. cube provides 3D indexing.
-- The cube is restricted to be a point, no more than 3 dimensions
-- (for less than 3 dimensions 0 is assumed for the missing coordinates)
-- and that the point must be very near the surface of the sphere
-- centered about the origin with the radius of the earth.

CREATE DOMAIN earth AS cube
  CONSTRAINT not_point check(cube_is_point(value))
  CONSTRAINT not_3d check(cube_dim(value) <= 3)
  CONSTRAINT on_surface check(abs(cube_distance(value, '(0)'::cube) /
  earth() - '1'::float8) < '10e-7'::float8)
;
CREATE TABLE bt_i4_heap (
	seqno 		int4,
	random 		int4
)
;
CREATE TABLE "public"."mcv_lists_bool" (
    "a" boolean,
    "b" boolean,
    "c" boolean
)
WITH ("autovacuum_enabled"='off')
;
-- Load some example data and build the index
--
CREATE TABLE test_cube (c cube)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_description
;
alter domain con add constraint t check (VALUE < 1)
;
ALTER FOREIGN TABLE ft1 SET SCHEMA foreign_schema
;
ALTER OPERATOR >= (upc, upc) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
DROP SUBSCRIPTION regress_sub3
;
alter table alter1.v1 set schema alter2
;
CREATE VIEW "public"."mvtest_tvvmv" AS
 SELECT "grandtot"
   FROM "public"."mvtest_tvvm"
;
CREATE SCHEMA "MySchema"
;
VACUUM FREEZE ANALYZE tabx
;
ALTER TABLE test_part_c_2 ADD PRIMARY KEY (b)
;
create or replace function raise_test() returns void as $$
begin
  perform 1/0;
exception
  when sqlstate '22012' then
    raise notice using message = sqlstate;
    raise sqlstate '22012' using message = 'substitute message';
end;
$$ language plpgsql
;
CREATE TABLE test_tbl (id int)
;
ALTER TABLE a SET (autovacuum_enabled = 'false')
;
CREATE OPERATOR || (
        LEFTARG = ltree,
	RIGHTARG = text,
	PROCEDURE = ltree_addtext
)
;
DROP TABLE evtrg_nontemp_table
;
-- fail
create or replace function parted_trigfunc() returns trigger language plpgsql as $$
begin
  new.b = new.b + 1;
  return new;
end;
$$
;
VACUUM (FREEZE) pg_catalog.pg_tablespace
;
create temp view vv as select * from temptable
;
-- error
create temp table temp_part partition of temp_parted default
;
CREATE TABLE tbl (a int)
;
-- fail - user-defined functions are not allowed
CREATE FUNCTION testpub_rf_func2() RETURNS integer AS $$ BEGIN RETURN 123; END; $$ LANGUAGE plpgsql
;
-- fails, as we are attempting to rename first argument
CREATE OR REPLACE FUNCTION dup (inout f2 anyelement, out f3 anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS macaddr_ops
DEFAULT FOR TYPE macaddr USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       macaddr_cmp(macaddr,macaddr),
    FUNCTION        2       gin_extract_value_macaddr(macaddr, internal),
    FUNCTION        3       gin_extract_query_macaddr(macaddr, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_macaddr(macaddr,macaddr,int2, internal),
STORAGE         macaddr
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30507_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] DROP FUNCTION truncate_in_subxact()
;
----------
[CREATE_TABLE] CREATE TABLE decoding_test(x integer, y text)
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE test_proc4
;
----------
[ADMIN_TABLE] ANALYZE bt_f8_heap
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist password=regress_fakepassword' PUBLICATION testpub WITH (connect = false)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- check also that the wholerow vars in RETURNING list are converted as needed
alter table returningwrtest add b text
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON r2 FOR UPDATE USING (false)
;
----------
[DROP_PROG_OBJ] drop function f1(x anycompatiblerange, y anycompatiblearray)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
----------
[CREATE_TABLE] -- Test display of default privileges with \pset null.
CREATE TABLE defprivs (a int)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE itest13 ADD COLUMN b int GENERATED BY DEFAULT AS IDENTITY
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_vcomp" AS
 SELECT "x"."part",
    ("x"."size" * "y"."factor") AS "size_in_cm"
   FROM "public"."rtest_comp" "x",
    "public"."rtest_unitfact" "y"
  WHERE ("x"."unit" = "y"."unit")
;
----------
[ADMIN_TABLE] ANALYZE public.brin_test
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."my_gen_series"(integer, integer) RETURNS SETOF integer
    LANGUAGE "internal" IMMUTABLE STRICT SUPPORT "public"."test_support_func" PARALLEL SAFE
    AS $$generate_series_int4$$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_27293
;
----------
[DROP_SEQUENCE] DROP SEQUENCE regtest_seq
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_foreign_server
;
----------
[ADD_INDEX] CREATE INDEX test_ne_idx ON test_ne USING gist (a, b)
;
----------
[CREATE_TABLE] -- invalid use of subquery
CREATE TABLE default_expr_agg (a int DEFAULT (select 1))
;
----------
[CREATE_PROG_OBJ] -- error, wrong result type
CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int) RETURNS float
AS 'select $1+1' LANGUAGE sql
;
----------
[ADMIN_TABLE] VACUUM tail_tbl
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a check constraint (fails)
alter table atacc1 add constraint atacc_test1 check (test+test2<test3*4)
;
----------
[CREATE_TYPE] -- Astronomers may want to change the earth function so that distances will be
-- returned in degrees. To do this comment out the above definition and
-- uncomment the one below. Note that doing this will break the regression
-- tests.
--
-- CREATE FUNCTION earth() RETURNS float8
-- LANGUAGE SQL IMMUTABLE
-- AS 'SELECT 180/pi()';

-- Define domain for locations on the surface of the earth using a cube
-- datatype with constraints. cube provides 3D indexing.
-- The cube is restricted to be a point, no more than 3 dimensions
-- (for less than 3 dimensions 0 is assumed for the missing coordinates)
-- and that the point must be very near the surface of the sphere
-- centered about the origin with the radius of the earth.

CREATE DOMAIN earth AS cube
  CONSTRAINT not_point check(cube_is_point(value))
  CONSTRAINT not_3d check(cube_dim(value) <= 3)
  CONSTRAINT on_surface check(abs(cube_distance(value, '(0)'::cube) /
  earth() - '1'::float8) < '10e-7'::float8)
;
----------
[CREATE_TABLE] CREATE TABLE bt_i4_heap (
	seqno 		int4,
	random 		int4
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mcv_lists_bool" (
    "a" boolean,
    "b" boolean,
    "c" boolean
)
WITH ("autovacuum_enabled"='off')
;
----------
[CREATE_TABLE] -- Load some example data and build the index
--
CREATE TABLE test_cube (c cube)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_description
;
----------
[ALTER_TYPE] alter domain con add constraint t check (VALUE < 1)
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE ft1 SET SCHEMA foreign_schema
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (upc, upc) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION regress_sub3
;
----------
[ALTER_TABLE] alter table alter1.v1 set schema alter2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."mvtest_tvvmv" AS
 SELECT "grandtot"
   FROM "public"."mvtest_tvvm"
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "MySchema"
;
----------
[ADMIN_TABLE] VACUUM FREEZE ANALYZE tabx
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE test_part_c_2 ADD PRIMARY KEY (b)
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  perform 1/0;
exception
  when sqlstate '22012' then
    raise notice using message = sqlstate;
    raise sqlstate '22012' using message = 'substitute message';
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE test_tbl (id int)
;
----------
[ALTER_TABLE] ALTER TABLE a SET (autovacuum_enabled = 'false')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR || (
        LEFTARG = ltree,
	RIGHTARG = text,
	PROCEDURE = ltree_addtext
)
;
----------
[DROP_TABLE] DROP TABLE evtrg_nontemp_table
;
----------
[CREATE_PROG_OBJ] -- fail
create or replace function parted_trigfunc() returns trigger language plpgsql as $$
begin
  new.b = new.b + 1;
  return new;
end;
$$
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_tablespace
;
----------
[CREATE_VIEW(SELECT)] create temp view vv as select * from temptable
;
----------
[CREATE_TABLE] -- error
create temp table temp_part partition of temp_parted default
;
----------
[CREATE_TABLE] CREATE TABLE tbl (a int)
;
----------
[CREATE_PROG_OBJ] -- fail - user-defined functions are not allowed
CREATE FUNCTION testpub_rf_func2() RETURNS integer AS $$ BEGIN RETURN 123; END; $$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] -- fails, as we are attempting to rename first argument
CREATE OR REPLACE FUNCTION dup (inout f2 anyelement, out f3 anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
