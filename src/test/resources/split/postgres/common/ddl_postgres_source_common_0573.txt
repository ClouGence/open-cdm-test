ALTER TABLE tab_rowfilter_partitioned ATTACH PARTITION tab_rowfilter_less_10k FOR VALUES FROM (MINVALUE) TO (10000)
;
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY, c_int INT DEFAULT LENGTH(foo(6)))
;
alter table atacc1 alter "........pg.dropped.1........" set statistics 0
;
create trigger trigtest_b_row_tg before insert or update or delete on trigtest
for each row execute procedure trigtest()
;
CREATE POLICY p1 ON current_check_2 AS PERMISSIVE
  USING (ctid IN ('(0,1)', '(0,2)', '(4294967295,0)'))
;
ALTER TABLE itest8
  ADD COLUMN f3 int NOT NULL,
  ALTER COLUMN f3 ADD GENERATED ALWAYS AS IDENTITY,
  ALTER COLUMN f3 SET GENERATED BY DEFAULT SET INCREMENT 10
;
DROP VIEW test_view_exists
;
-- operator number should be between 1 and 5
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 1 <
;
create table domdeftest (col1 ddef1)
;
create function myinthash(myint) returns integer strict immutable language
  internal as 'hashint4'
;
create rule "_RETURN" as on select to rules_fooview do instead
  select 1 as x, 'aaa'::text as y
;
-- make sure rewrites don't work
ALTER TABLE replication_metadata ADD COLUMN rewritemeornot int
;
ALTER TABLE ONLY "public"."trigger_parted" ATTACH PARTITION "public"."trigger_parted_p1" FOR VALUES IN (1)
;
CREATE FUNCTION isngt(issn13, issn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE MATERIALIZED VIEW "mvtest_mvschema"."mvtest_tvm" AS
 SELECT "type",
    "totamt"
   FROM "public"."mvtest_tv"
  ORDER BY "type"
  WITH NO DATA
;
CREATE TABLE "fkpart3"."fk1" (
    "a" integer
)
;
ALTER OPERATOR FAMILY gist_time_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE TABLE covering_index_heap (f1 int, f2 int, f3 text)
;
CREATE FUNCTION reindex_end_command_snap() RETURNS EVENT_TRIGGER
    AS $$ BEGIN PERFORM 1; END $$ LANGUAGE plpgsql
;
create table nv_child_2010 () inherits (nv_parent)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_largeobject_metadata
;
-- should fail
create function bogus_func(anyelement)
  returns anyrange as 'select int4range(1,10)' language sql
;
CREATE INDEX ON temptest(bit_length(''))
;
CREATE FUNCTION "public"."mr_polymorphic"("i" "anyrange") RETURNS "anymultirange"
    LANGUAGE "plpgsql"
    AS $_$ begin return multirange($1); end; $_$
;
CREATE TABLE "public"."comment_test" (
    "id" "text" CONSTRAINT "id_notnull_constraint" NOT NULL,
    "positive_col" bigint,
    "indexed_col" "text",
    CONSTRAINT "comment_test_positive_col_check" CHECK (("positive_col" > 0))
)
;
DROP OPERATOR CLASS IF EXISTS no_such_schema.widget_ops USING btree
;
create table inh_parent(f1 int not null)
;
CREATE MATERIALIZED VIEW matview_ine_tab AS SELECT 1 / 0
;
CREATE TABLE am_partitioned_1 PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
DROP TABLE stats_ext_tbl
;
create function constant_refcursor() returns refcursor as $$
declare
    rc constant refcursor;
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
/*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
-- Set ops not supported
CREATE VIEW ro_view7 AS WITH t AS (SELECT a, b FROM base_tbl) SELECT * FROM t
;
create table nv_child_2009 (check (d between '2009-01-01'::date and '2009-12-31'::date)) inherits (nv_parent)
;
CREATE DOMAIN domain3 AS text DEFAULT foo(4)
;
ALTER TABLE ONLY "public"."persons3" OF "public"."person_type"
;
-- test prefetching, if the platform allows it
DO $$
BEGIN
 SET effective_io_concurrency = 50;
EXCEPTION WHEN invalid_parameter_value THEN
END $$
;
CREATE TABLE temporal3 (
  id int4range
)
;
create table failpart partition of idxpart (b primary key) for values from (0) to (100)
;
CREATE FUNCTION int_custom_ne(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
DROP TABLE vactst
;
--
-- test ALTER TABLE ADD CONSTRAINT
--

CREATE TABLE temporal_rng (
  id int4range,
  valid_at daterange
)
;
-- error because its constraint as the default partition would be violated
-- by the row containing 1
alter table defpart_attach_test attach partition defpart_attach_test_d default
;
create table indicator_test ( "id" int primary key , "str" text not null , val int null )
;
DROP TABLE reload_output
;
CREATE VIEW pg_shmem_allocations_numa AS
    SELECT * FROM pg_get_shmem_allocations_numa()
;
CREATE FUNCTION test_resowner_forget_between_phases()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
ALTER TABLE ONLY "fkpart3"."pk51"
    ADD CONSTRAINT "pk51_pkey" PRIMARY KEY ("a")
;
CREATE TABLE target_pa (key int primary key, val text) PARTITION BY LIST (key)
;
CREATE FUNCTION gbt_inet_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab_rowfilter_partitioned ATTACH PARTITION tab_rowfilter_less_10k FOR VALUES FROM (MINVALUE) TO (10000)
;
----------
[CREATE_TABLE] CREATE TABLE T(pk INT NOT NULL PRIMARY KEY, c_int INT DEFAULT LENGTH(foo(6)))
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter "........pg.dropped.1........" set statistics 0
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trigtest_b_row_tg before insert or update or delete on trigtest
for each row execute procedure trigtest()
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON current_check_2 AS PERMISSIVE
  USING (ctid IN ('(0,1)', '(0,2)', '(4294967295,0)'))
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE itest8
  ADD COLUMN f3 int NOT NULL,
  ALTER COLUMN f3 ADD GENERATED ALWAYS AS IDENTITY,
  ALTER COLUMN f3 SET GENERATED BY DEFAULT SET INCREMENT 10
;
----------
[DROP_VIEW] DROP VIEW test_view_exists
;
----------
[ALTER_PROG_OBJ] -- operator number should be between 1 and 5
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 1 <
;
----------
[CREATE_TABLE] create table domdeftest (col1 ddef1)
;
----------
[CREATE_PROG_OBJ] create function myinthash(myint) returns integer strict immutable language
  internal as 'hashint4'
;
----------
[CREATE_POLICY] create rule "_RETURN" as on select to rules_fooview do instead
  select 1 as x, 'aaa'::text as y
;
----------
[ALTER_TABLE|ADD_COLUMN] -- make sure rewrites don't work
ALTER TABLE replication_metadata ADD COLUMN rewritemeornot int
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."trigger_parted" ATTACH PARTITION "public"."trigger_parted_p1" FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(issn13, issn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "mvtest_mvschema"."mvtest_tvm" AS
 SELECT "type",
    "totamt"
   FROM "public"."mvtest_tv"
  ORDER BY "type"
  WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."fk1" (
    "a" integer
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_time_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TABLE] CREATE TABLE covering_index_heap (f1 int, f2 int, f3 text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION reindex_end_command_snap() RETURNS EVENT_TRIGGER
    AS $$ BEGIN PERFORM 1; END $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] create table nv_child_2010 () inherits (nv_parent)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_PROG_OBJ] -- should fail
create function bogus_func(anyelement)
  returns anyrange as 'select int4range(1,10)' language sql
;
----------
[ADD_INDEX] CREATE INDEX ON temptest(bit_length(''))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."mr_polymorphic"("i" "anyrange") RETURNS "anymultirange"
    LANGUAGE "plpgsql"
    AS $_$ begin return multirange($1); end; $_$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."comment_test" (
    "id" "text" CONSTRAINT "id_notnull_constraint" NOT NULL,
    "positive_col" bigint,
    "indexed_col" "text",
    CONSTRAINT "comment_test_positive_col_check" CHECK (("positive_col" > 0))
)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR CLASS IF EXISTS no_such_schema.widget_ops USING btree
;
----------
[CREATE_TABLE] create table inh_parent(f1 int not null)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW matview_ine_tab AS SELECT 1 / 0
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned_1 PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
----------
[DROP_TABLE] DROP TABLE stats_ext_tbl
;
----------
[CREATE_PROG_OBJ] create function constant_refcursor() returns refcursor as $$
declare
    rc constant refcursor;
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
----------
[CREATE_POLICY] /*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
----------
[CREATE_VIEW(SELECT)] -- Set ops not supported
CREATE VIEW ro_view7 AS WITH t AS (SELECT a, b FROM base_tbl) SELECT * FROM t
;
----------
[CREATE_TABLE] create table nv_child_2009 (check (d between '2009-01-01'::date and '2009-12-31'::date)) inherits (nv_parent)
;
----------
[CREATE_TYPE] CREATE DOMAIN domain3 AS text DEFAULT foo(4)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."persons3" OF "public"."person_type"
;
----------
[BLOCK] -- test prefetching, if the platform allows it
DO $$
BEGIN
 SET effective_io_concurrency = 50;
EXCEPTION WHEN invalid_parameter_value THEN
END $$
;
----------
[CREATE_TABLE] CREATE TABLE temporal3 (
  id int4range
)
;
----------
[CREATE_TABLE] create table failpart partition of idxpart (b primary key) for values from (0) to (100)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_ne(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] DROP TABLE vactst
;
----------
[CREATE_TABLE] --
-- test ALTER TABLE ADD CONSTRAINT
--

CREATE TABLE temporal_rng (
  id int4range,
  valid_at daterange
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- error because its constraint as the default partition would be violated
-- by the row containing 1
alter table defpart_attach_test attach partition defpart_attach_test_d default
;
----------
[CREATE_TABLE] create table indicator_test ( "id" int primary key , "str" text not null , val int null )
;
----------
[DROP_TABLE] DROP TABLE reload_output
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_shmem_allocations_numa AS
    SELECT * FROM pg_get_shmem_allocations_numa()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_resowner_forget_between_phases()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart3"."pk51"
    ADD CONSTRAINT "pk51_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE target_pa (key int primary key, val text) PARTITION BY LIST (key)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_inet_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
