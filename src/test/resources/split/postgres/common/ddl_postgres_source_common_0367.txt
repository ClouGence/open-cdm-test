alter table mlparted drop d
;
CREATE PUBLICATION pub FOR TABLE tbl
;
CREATE TABLE num_exp_log10 (id int4, expected numeric(210,10))
;
CREATE TABLE fk_partitioned_fk_2_2 PARTITION OF fk_partitioned_fk_2 FOR VALUES FROM (1000) TO (2000)
;
-- not an incomplete one though
alter table idxpart add primary key (a, b)
;
CREATE TABLE test()
;
ALTER TABLE ONLY "public"."parted_self_fk" ATTACH PARTITION "public"."part1_self_fk" FOR VALUES FROM ('0') TO ('10')
;
CREATE AGGREGATE newsum (
   sfunc1 = int4pl, basetype = int4, stype1 = int4,
   initcond1 = '0'
)
;
create trigger my_trigger after update on my_view referencing old table as old_table
   for each statement execute procedure my_trigger_function()
;
CREATE TABLE t1p (o int, p text) PARTITION BY RANGE (o)
;
-- ===================================================================
-- test generated columns
-- ===================================================================
create table gloc1 (
  a int,
  b int generated always as (a * 2) stored)
;
CREATE TABLE foo(s TEXT)
;
DROP PUBLICATION tap_pub_B
;
create unique index plain on insertconflicttest(key)
;
CREATE TABLE selinto_schema.tbl_withdata3 (a) AS
  EXECUTE data_sel WITH DATA
;
CREATE TABLE "public"."fk_partitioned_fk_4" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
PARTITION BY RANGE ("b", "a")
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_inherits
;
CREATE TABLE transition_table_base (id int PRIMARY KEY, val text)
;
CREATE VIEW pg_publication_tables AS
    SELECT
        P.pubname AS pubname,
        N.nspname AS schemaname,
        C.relname AS tablename,
        ( SELECT array_agg(a.attname ORDER BY a.attnum)
          FROM pg_attribute a
          WHERE a.attrelid = GPT.relid AND
                a.attnum = ANY(GPT.attrs)
        ) AS attnames,
        pg_get_expr(GPT.qual, GPT.relid) AS rowfilter
    FROM pg_publication P,
         LATERAL pg_get_publication_tables(P.pubname) GPT,
         pg_class C JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.oid = GPT.relid
;
CREATE PROCEDURE ptest11(a OUT int, VARIADIC b int[]) LANGUAGE SQL
  AS $$ SELECT b[1] + b[2] $$
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_rewrite
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', quote ':')
;
CREATE TABLE itest13 (a int)
;
create table mlparted5_a partition of mlparted5_ab for values in ('a')
;
ANALYZE pg_temp_26.pg_temp_28293
;
CREATE TABLE dump_test.regress_pg_dump_table_am_parent (id int) PARTITION BY LIST (id)
;
VACUUM pg_catalog.pg_ts_parser
;
DROP TABLE result_tbl
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE INDEX tbspace_reindex_part_index ON ONLY tbspace_reindex_part (c1)
;
create function make_bogus_matview() returns trigger as
$$ begin
  create materialized view transition_test_mv as select * from new_table;
  return new;
end $$
language plpgsql
;
DROP TABLE hash_split_heap
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_shseclabel
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
ALTER TABLE pg_toast.pg_toast_30900 RENAME TO reind_con_toast
;
CREATE TABLE test_replica_id_full (x int)
;
create temp table text_support_test (t text collate "C")
;
-- false

CREATE FUNCTION testns.priv_testfunc(int) RETURNS int AS 'select 3 * $1;' LANGUAGE sql
;
CREATE TABLE tableam_parted_heapx (a text, b int) PARTITION BY list (a)
;
ALTER TABLE ONLY "public"."part_p2" ATTACH PARTITION "public"."part_p2_p1" DEFAULT
;
DROP TABLE part_c_1_c_20
;
-- and Dave isn't allowed to see manga documents
CREATE POLICY p1r ON document AS RESTRICTIVE TO regress_rls_dave
    USING (cid <> 44)
;
CREATE VIEW rw_view1 AS
  SELECT *, 'Const1' AS c1 FROM base_tbl WHERE a>0 OFFSET 0
;
CREATE TEMP TABLE pg_temp_4.pg_temp_37962_2 (tid pg_catalog.tid)
;
alter domain dnotnulltest drop not null
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_amop
;
create temp view zv1 as select *,'dummy'::text AS junk from zt1
;
CREATE TABLE cminh() INHERITS(cmdata, cmdata1)
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (on_error 'ignore', reject_limit '0')
;
create or replace function compos() returns int as $$
begin
  return (1, 'hello')::compostype;
end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] alter table mlparted drop d
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub FOR TABLE tbl
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_log10 (id int4, expected numeric(210,10))
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_2_2 PARTITION OF fk_partitioned_fk_2 FOR VALUES FROM (1000) TO (2000)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- not an incomplete one though
alter table idxpart add primary key (a, b)
;
----------
[CREATE_TABLE] CREATE TABLE test()
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."parted_self_fk" ATTACH PARTITION "public"."part1_self_fk" FOR VALUES FROM ('0') TO ('10')
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE newsum (
   sfunc1 = int4pl, basetype = int4, stype1 = int4,
   initcond1 = '0'
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_trigger after update on my_view referencing old table as old_table
   for each statement execute procedure my_trigger_function()
;
----------
[CREATE_TABLE] CREATE TABLE t1p (o int, p text) PARTITION BY RANGE (o)
;
----------
[CREATE_TABLE] -- ===================================================================
-- test generated columns
-- ===================================================================
create table gloc1 (
  a int,
  b int generated always as (a * 2) stored)
;
----------
[CREATE_TABLE] CREATE TABLE foo(s TEXT)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION tap_pub_B
;
----------
[ADD_INDEX] create unique index plain on insertconflicttest(key)
;
----------
[CREATE_TABLE|UNSAFE] CREATE TABLE selinto_schema.tbl_withdata3 (a) AS
  EXECUTE data_sel WITH DATA
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_4" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
PARTITION BY RANGE ("b", "a")
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_inherits
;
----------
[CREATE_TABLE] CREATE TABLE transition_table_base (id int PRIMARY KEY, val text)
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW pg_publication_tables AS
    SELECT
        P.pubname AS pubname,
        N.nspname AS schemaname,
        C.relname AS tablename,
        ( SELECT array_agg(a.attname ORDER BY a.attnum)
          FROM pg_attribute a
          WHERE a.attrelid = GPT.relid AND
                a.attnum = ANY(GPT.attrs)
        ) AS attnames,
        pg_get_expr(GPT.qual, GPT.relid) AS rowfilter
    FROM pg_publication P,
         LATERAL pg_get_publication_tables(P.pubname) GPT,
         pg_class C JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.oid = GPT.relid
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest11(a OUT int, VARIADIC b int[]) LANGUAGE SQL
  AS $$ SELECT b[1] + b[2] $$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_rewrite
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', quote ':')
;
----------
[CREATE_TABLE] CREATE TABLE itest13 (a int)
;
----------
[CREATE_TABLE] create table mlparted5_a partition of mlparted5_ab for values in ('a')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_26.pg_temp_28293
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.regress_pg_dump_table_am_parent (id int) PARTITION BY LIST (id)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_ts_parser
;
----------
[DROP_TABLE] DROP TABLE result_tbl
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ADD_INDEX] CREATE INDEX tbspace_reindex_part_index ON ONLY tbspace_reindex_part (c1)
;
----------
[CREATE_PROG_OBJ] create function make_bogus_matview() returns trigger as
$$ begin
  create materialized view transition_test_mv as select * from new_table;
  return new;
end $$
language plpgsql
;
----------
[DROP_TABLE] DROP TABLE hash_split_heap
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_shseclabel
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30900 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_id_full (x int)
;
----------
[CREATE_TABLE] create temp table text_support_test (t text collate "C")
;
----------
[CREATE_PROG_OBJ] -- false

CREATE FUNCTION testns.priv_testfunc(int) RETURNS int AS 'select 3 * $1;' LANGUAGE sql
;
----------
[CREATE_TABLE] CREATE TABLE tableam_parted_heapx (a text, b int) PARTITION BY list (a)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."part_p2" ATTACH PARTITION "public"."part_p2_p1" DEFAULT
;
----------
[DROP_TABLE] DROP TABLE part_c_1_c_20
;
----------
[CREATE_POLICY] -- and Dave isn't allowed to see manga documents
CREATE POLICY p1r ON document AS RESTRICTIVE TO regress_rls_dave
    USING (cid <> 44)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS
  SELECT *, 'Const1' AS c1 FROM base_tbl WHERE a>0 OFFSET 0
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_37962_2 (tid pg_catalog.tid)
;
----------
[ALTER_TYPE] alter domain dnotnulltest drop not null
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_amop
;
----------
[CREATE_VIEW(SELECT)] create temp view zv1 as select *,'dummy'::text AS junk from zt1
;
----------
[CREATE_TABLE] CREATE TABLE cminh() INHERITS(cmdata, cmdata1)
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (on_error 'ignore', reject_limit '0')
;
----------
[CREATE_PROG_OBJ] create or replace function compos() returns int as $$
begin
  return (1, 'hello')::compostype;
end;
$$ language plpgsql
;
