create temp table test_table (a text, b text)
;
CREATE TABLE parted_uniq_tbl_1 PARTITION OF parted_uniq_tbl FOR VALUES FROM (0) TO (10)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
CREATE TABLE tenk1 (
	unique1		int4,
	unique2		int4,
	two			int4,
	four		int4,
	ten			int4,
	twenty		int4,
	hundred		int4,
	thousand	int4,
	twothousand	int4,
	fivethous	int4,
	tenthous	int4,
	odd			int4,
	even		int4,
	stringu1	name,
	stringu2	name,
	string4		name
)
;
--
-- pgp_sym_encrypt(data, key)
--
CREATE FUNCTION pgp_sym_encrypt(text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_encrypt_text'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE RULE "shoelace_del" AS
    ON DELETE TO "public"."shoelace" DO INSTEAD  DELETE FROM "public"."shoelace_data"
  WHERE ("shoelace_data"."sl_name" = "old"."sl_name")
;
--
-- tuple_data_split()
--
CREATE FUNCTION tuple_data_split(rel_oid oid,
    t_data bytea,
    t_infomask integer,
    t_infomask2 integer,
    t_bits text)
RETURNS bytea[]
AS '$libdir/pageinspect','tuple_data_split'
LANGUAGE C PARALLEL SAFE
;
-- Valid AFTER statement VIEW triggers
CREATE TRIGGER after_ins_stmt_trig AFTER INSERT ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('after_view_ins_stmt')
;
CREATE OPERATOR CLASS dump_test.op_class_custom
							 FOR TYPE int USING btree AS
							 OPERATOR 3 dump_test.~~
;
CREATE INDEX "reloptions_test_idx3" ON "public"."reloptions_test" USING "btree" ("s") WITH ("fillfactor"='40')
;
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES IN (1000)
;
CREATE TABLE public.bar (t TEXT)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_publication_rel
;
ALTER TABLE gtest27
  DROP COLUMN x,
  ALTER COLUMN a TYPE bigint,
  ALTER COLUMN b TYPE bigint,
  ADD COLUMN x bigint GENERATED ALWAYS AS ((a + b) * 2) STORED
;
CREATE VIEW rw_view1 AS SELECT a FROM base_tbl WHERE a < b
;
ALTER TABLE pg_toast.pg_toast_28057 RENAME TO reind_con_toast
;
DROP AGGREGATE IF EXISTS split_agg_probe_next.sum_int_renamed(integer),
                         split_agg_probe.min_int(integer),
                         split_agg_probe.count_star(*),
                         split_agg_probe.count_any(*),
                         split_agg_probe.sum_variadic(VARIADIC integer[]),
                         split_agg_probe_next.os_sum(ORDER BY integer),
                         split_agg_probe.hypo_sum(integer ORDER BY integer)
CASCADE
;
CREATE PROCEDURE "public"."ptest5"(IN "a" integer, IN "b" "text", IN "c" integer DEFAULT 100)
    LANGUAGE "sql"
    AS $$
INSERT INTO cp_test VALUES(a, b);
INSERT INTO cp_test VALUES(c, b);
$$
;
CREATE INDEX sp_quad_ind ON quad_point_tbl USING spgist (p)
;
CREATE TABLE batch_cp_upd_test2 PARTITION OF batch_cp_upd_test
	FOR VALUES IN (2)
;
ALTER TABLE ONLY "public"."rewritemetoo2" OF "public"."rewritetype"
;
ALTER FUNCTION isn_weak(boolean) VOLATILE PARALLEL UNSAFE
;
CREATE FUNCTION _ltree_compress(internal)
RETURNS internal
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER FUNCTION my_int_eq(int, int) SUPPORT test_support_func
;
CREATE DATABASE beforepromotion
;
CREATE TABLE collate_test_fail (
    a int COLLATE "en_US",
    b text
)
;
create temp table test_jsonb_subscript (
       id text,
       test_json jsonb
)
;
-- test jsonb int -> python int
CREATE FUNCTION test1int(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == 1)
return val
$$
;
explain (analyze,serialize) create temp table explain_temp as select * from int8_tbl i8
;
create or replace function foreach_test(anyarray)
returns void as $$
declare x int; y int;
begin
  foreach x, y in array $1
  loop
    raise notice 'x = %, y = %', x, y;
  end loop;
  end;
$$ language plpgsql
;
ALTER OPERATOR FAMILY gist_macaddr8_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
do $$
declare
  t test_01;
begin
  select 1 into t; -- should fail;
end;
$$
;
CREATE TYPE deptest_enum AS ENUM ('red')
;
CREATE INDEX "iprt2_p2_b" ON "public"."prt2_p2" USING "btree" ("b")
;
DROP TABLE p CASCADE
;
CREATE TABLE tbspace_reindex_part_10 PARTITION OF tbspace_reindex_part
   FOR VALUES FROM (10) TO (20) PARTITION BY list (c2)
;
CREATE TEMP TABLE past_inh_db_other ()
;
-- polymorphic types

CREATE PROCEDURE ptest6(a int, b anyelement)
LANGUAGE SQL
AS $$
SELECT NULL::int;
$$
;
-- succeed, nondeferrable unique constraint over nonnullable cols
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_unique_nondefer
;
create temp table parent2(f1 int, f3 bigint)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_ts_config_map
;
-- try it on an inheritance tree
create table minmaxtest(f1 int)
;
-- fail

drop table test1
;
CREATE TABLE test_timestamptz (
	i timestamptz
)
;
CREATE TABLE t (a int, b int, c int) PARTITION BY RANGE (a)
;
-- fail - disable_on_error must be boolean
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, disable_on_error = foo)
;
CREATE TABLE part_rp100 PARTITION OF range_parted2 (CHECK (a>=123 AND a<133 AND a IS NOT NULL)) FOR VALUES FROM (100) to (200)
;
CREATE TABLE ctas_nodata (ii, jj, kk) AS SELECT i, j FROM ctas_base WITH NO DATA
;
CREATE TABLE check_fk_presence_1 (id int PRIMARY KEY, t text)
;
CREATE DATABASE d1
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create temp table test_table (a text, b text)
;
----------
[CREATE_TABLE] CREATE TABLE parted_uniq_tbl_1 PARTITION OF parted_uniq_tbl FOR VALUES FROM (0) TO (10)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
----------
[CREATE_TABLE] CREATE TABLE tenk1 (
	unique1		int4,
	unique2		int4,
	two			int4,
	four		int4,
	ten			int4,
	twenty		int4,
	hundred		int4,
	thousand	int4,
	twothousand	int4,
	fivethous	int4,
	tenthous	int4,
	odd			int4,
	even		int4,
	stringu1	name,
	stringu2	name,
	string4		name
)
;
----------
[CREATE_PROG_OBJ] --
-- pgp_sym_encrypt(data, key)
--
CREATE FUNCTION pgp_sym_encrypt(text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_encrypt_text'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE RULE "shoelace_del" AS
    ON DELETE TO "public"."shoelace" DO INSTEAD  DELETE FROM "public"."shoelace_data"
  WHERE ("shoelace_data"."sl_name" = "old"."sl_name")
;
----------
[CREATE_PROG_OBJ] --
-- tuple_data_split()
--
CREATE FUNCTION tuple_data_split(rel_oid oid,
    t_data bytea,
    t_infomask integer,
    t_infomask2 integer,
    t_bits text)
RETURNS bytea[]
AS '$libdir/pageinspect','tuple_data_split'
LANGUAGE C PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Valid AFTER statement VIEW triggers
CREATE TRIGGER after_ins_stmt_trig AFTER INSERT ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('after_view_ins_stmt')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS dump_test.op_class_custom
							 FOR TYPE int USING btree AS
							 OPERATOR 3 dump_test.~~
;
----------
[ADD_INDEX] CREATE INDEX "reloptions_test_idx3" ON "public"."reloptions_test" USING "btree" ("s") WITH ("fillfactor"='40')
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES IN (1000)
;
----------
[CREATE_TABLE] CREATE TABLE public.bar (t TEXT)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_publication_rel
;
----------
[ALTER_TABLE|DROP_COLUMN|ALTER_COLUMN|ADD_COLUMN] ALTER TABLE gtest27
  DROP COLUMN x,
  ALTER COLUMN a TYPE bigint,
  ALTER COLUMN b TYPE bigint,
  ADD COLUMN x bigint GENERATED ALWAYS AS ((a + b) * 2) STORED
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT a FROM base_tbl WHERE a < b
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28057 RENAME TO reind_con_toast
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE IF EXISTS split_agg_probe_next.sum_int_renamed(integer),
                         split_agg_probe.min_int(integer),
                         split_agg_probe.count_star(*),
                         split_agg_probe.count_any(*),
                         split_agg_probe.sum_variadic(VARIADIC integer[]),
                         split_agg_probe_next.os_sum(ORDER BY integer),
                         split_agg_probe.hypo_sum(integer ORDER BY integer)
CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest5"(IN "a" integer, IN "b" "text", IN "c" integer DEFAULT 100)
    LANGUAGE "sql"
    AS $$
INSERT INTO cp_test VALUES(a, b);
INSERT INTO cp_test VALUES(c, b);
$$
;
----------
[ADD_INDEX] CREATE INDEX sp_quad_ind ON quad_point_tbl USING spgist (p)
;
----------
[CREATE_TABLE] CREATE TABLE batch_cp_upd_test2 PARTITION OF batch_cp_upd_test
	FOR VALUES IN (2)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."rewritemetoo2" OF "public"."rewritetype"
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION isn_weak(boolean) VOLATILE PARALLEL UNSAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_compress(internal)
RETURNS internal
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION my_int_eq(int, int) SUPPORT test_support_func
;
----------
[CREATE_CATALOG] CREATE DATABASE beforepromotion
;
----------
[CREATE_TABLE] CREATE TABLE collate_test_fail (
    a int COLLATE "en_US",
    b text
)
;
----------
[CREATE_TABLE] create temp table test_jsonb_subscript (
       id text,
       test_json jsonb
)
;
----------
[CREATE_PROG_OBJ] -- test jsonb int -> python int
CREATE FUNCTION test1int(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == 1)
return val
$$
;
----------
[CREATE_TABLE|SELECT] explain (analyze,serialize) create temp table explain_temp as select * from int8_tbl i8
;
----------
[CREATE_PROG_OBJ] create or replace function foreach_test(anyarray)
returns void as $$
declare x int; y int;
begin
  foreach x, y in array $1
  loop
    raise notice 'x = %, y = %', x, y;
  end loop;
  end;
$$ language plpgsql
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_macaddr8_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[BLOCK] do $$
declare
  t test_01;
begin
  select 1 into t; -- should fail;
end;
$$
;
----------
[CREATE_TYPE] CREATE TYPE deptest_enum AS ENUM ('red')
;
----------
[ADD_INDEX] CREATE INDEX "iprt2_p2_b" ON "public"."prt2_p2" USING "btree" ("b")
;
----------
[DROP_TABLE] DROP TABLE p CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE tbspace_reindex_part_10 PARTITION OF tbspace_reindex_part
   FOR VALUES FROM (10) TO (20) PARTITION BY list (c2)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE past_inh_db_other ()
;
----------
[CREATE_PROG_OBJ] -- polymorphic types

CREATE PROCEDURE ptest6(a int, b anyelement)
LANGUAGE SQL
AS $$
SELECT NULL::int;
$$
;
----------
[ALTER_TABLE] -- succeed, nondeferrable unique constraint over nonnullable cols
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_unique_nondefer
;
----------
[CREATE_TABLE] create temp table parent2(f1 int, f3 bigint)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_ts_config_map
;
----------
[CREATE_TABLE] -- try it on an inheritance tree
create table minmaxtest(f1 int)
;
----------
[DROP_TABLE] -- fail

drop table test1
;
----------
[CREATE_TABLE] CREATE TABLE test_timestamptz (
	i timestamptz
)
;
----------
[CREATE_TABLE] CREATE TABLE t (a int, b int, c int) PARTITION BY RANGE (a)
;
----------
[CREATE_PUB_SUB] -- fail - disable_on_error must be boolean
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, disable_on_error = foo)
;
----------
[CREATE_TABLE] CREATE TABLE part_rp100 PARTITION OF range_parted2 (CHECK (a>=123 AND a<133 AND a IS NOT NULL)) FOR VALUES FROM (100) to (200)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_nodata (ii, jj, kk) AS SELECT i, j FROM ctas_base WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TABLE check_fk_presence_1 (id int PRIMARY KEY, t text)
;
----------
[CREATE_CATALOG] CREATE DATABASE d1
;
