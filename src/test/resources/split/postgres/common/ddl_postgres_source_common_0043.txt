ANALYZE public.hash_name_heap
;
ALTER TABLE ONLY "public"."emp1" ALTER COLUMN "id" SET DEFAULT "nextval"('"public"."emp1_id_seq"'::"regclass")
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_constraint
;
-- restore test table state

-- Check inheritance cases
CREATE TEMP TABLE ucchild () inherits (uctest)
;
drop function polyf(x anycompatiblerange, y anycompatible, z anycompatible)
;
DROP TABLE pg_temp_3.pg_temp_26915_2, pg_temp_3.pg_temp_26915
;
create table parted_trigger_3_2 partition of parted_trigger_3 for values from (3) to (5)
;
--DROP TRIGGER t2 ON pg_description;

-- rules
CREATE RULE r1 AS ON INSERT TO pg_description DO INSTEAD NOTHING
;
ALTER EXTENSION test_pg_dump DROP OPERATOR ==== (int, int)
;
create table d4_primary (a int primary key) partition by list (a)
;
ALTER TABLE ONLY "public"."mlparted2" DROP COLUMN "........pg.dropped.6........"
;
--
-- PATH
--

--DROP TABLE PATH_TBL;

CREATE TABLE PATH_TBL (f1 path)
;
CREATE TABLE testpub_tbl_both_filters (a int, b int, c int, PRIMARY KEY (a,c))
;
CREATE TABLE ctlt1 (a text CHECK (length(a) > 2) PRIMARY KEY, b text)
;
SECURITY LABEL ON TABLE t1 IS 'system_u:object_r:sepgsql_table_t:s0'
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic
;
CREATE TABLE "public"."notnull_tbl6_1" (
    "a" integer CONSTRAINT "ann" NOT NULL,
    "b" integer CONSTRAINT "bnn" NOT NULL,
    CONSTRAINT "notnull_tbl6_a_check" CHECK (("a" > 0))
)
;
ALTER TABLE fk_partitioned_fk_2 DROP COLUMN c
;
ALTER OPERATOR @+@(int4, int4) SET SCHEMA alt_nsp2
;
CREATE INDEX "spgist_box_idx" ON "public"."spgist_box_tbl" USING "spgist" ("b")
;
alter extension test_ext9 drop type varbitrange
;
-- doesn't exist

-- verify constraint deferrability
create schema fkpart3
  create table pkey (a int primary key)
  create table fk_part (a int, constraint fkey foreign key (a) references fkpart3.pkey deferrable initially immediate) partition by list (a)
  create table fk_part_1 partition of fkpart3.fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 partition of fkpart3.fk_part_1 for values in (1)
  create table fk_part_2 partition of fkpart3.fk_part for values in (2)
;
ALTER TABLE tab5 DETACH PARTITION tab5_1
;
-- fail, max has to be adjusted
ALTER SEQUENCE sequence_test8 AS smallint MAXVALUE 20000
;
CREATE INDEX test_idx ON test(c1)
;
CREATE INDEX collate_test1_idx2 ON collate_test1 (b COLLATE "C")
;
TRUNCATE TABLE truncpart
;
/*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
create temp table phrase_index_test(fts tsvector)
;
-- failure, schema does not exist
CREATE SCHEMA schema_to_reindex
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_subscription_rel
;
CREATE CONVERSION myconv FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
ALTER FUNCTION gbt_float4_picksplit(internal, internal) PARALLEL SAFE
;
-- Trigger renaming does not recurse on legacy inheritance
create table parent (a int)
;
ALTER TABLE ONLY "public"."gtest27" DROP COLUMN "........pg.dropped.3........"
;
comment on column atacc1."........pg.dropped.1........" is 'testing'
;
DO $$
BEGIN
    -- iterate often enough to see index growth even on larger-than-default page sizes
    FOR i IN 1..100 LOOP
        BEGIN
	    -- perform index scan over all the inserted keys to get them to be seen as dead
            IF EXISTS(SELECT * FROM clean_aborted_self WHERE key > 0 AND key < 100) THEN
	        RAISE data_corrupted USING MESSAGE = 'these rows should not exist';
            END IF;
            INSERT INTO clean_aborted_self SELECT g.i, 'rolling back in a sec' FROM generate_series(1, 100) g(i);
	    -- just some error that's not normally thrown
	    RAISE reading_sql_data_not_permitted USING MESSAGE = 'round and round again';
	EXCEPTION WHEN reading_sql_data_not_permitted THEN END;
    END LOOP;
END;$$
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_amop
;
CREATE EXTENSION adminpack
;
create index gist_tbl_multi_index on gist_tbl using gist (circle(p,1), p)
;
create table j1 (id int primary key)
;
create table gs_data_1 as
select g%1000 as g1000, g%100 as g100, g%10 as g10, g
   from generate_series(0,1999) g
;
CREATE AGGREGATE "public"."myaggn05b"(integer) (
    SFUNC = "public"."tfnp",
    STYPE = integer[],
    INITCOND = '{}'
)
;
ALTER TABLE ONLY constraint_rename_test RENAME CONSTRAINT con2 TO con2foo
;
CREATE TABLE "public"."pagg_tab_ml_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
-- Terminate the remote connection and wait for the termination to complete.
-- (If a cache flush happens, the remote connection might have already been
-- dropped; so code this step in a way that doesn't fail if no connection.)
DO $$ BEGIN
PERFORM pg_terminate_backend(pid, 180000) FROM pg_stat_activity
	WHERE application_name = 'fdw_retry_check';
END $$
;
TRUNCATE spc_commit
;
ALTER TYPE planets ADD VALUE IF NOT EXISTS 'pluto'
;
CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=15683 host=/tmp/7RF1JxeTxg dbname=postgres' PUBLICATION pub5
;
create temp table t_opf (a t_rec not null, b text)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE public.hash_name_heap
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."emp1" ALTER COLUMN "id" SET DEFAULT "nextval"('"public"."emp1_id_seq"'::"regclass")
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_constraint
;
----------
[CREATE_TABLE] -- restore test table state

-- Check inheritance cases
CREATE TEMP TABLE ucchild () inherits (uctest)
;
----------
[DROP_PROG_OBJ] drop function polyf(x anycompatiblerange, y anycompatible, z anycompatible)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_26915_2, pg_temp_3.pg_temp_26915
;
----------
[CREATE_TABLE] create table parted_trigger_3_2 partition of parted_trigger_3 for values from (3) to (5)
;
----------
[CREATE_POLICY] --DROP TRIGGER t2 ON pg_description;

-- rules
CREATE RULE r1 AS ON INSERT TO pg_description DO INSTEAD NOTHING
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP OPERATOR ==== (int, int)
;
----------
[CREATE_TABLE] create table d4_primary (a int primary key) partition by list (a)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted2" DROP COLUMN "........pg.dropped.6........"
;
----------
[CREATE_TABLE] --
-- PATH
--

--DROP TABLE PATH_TBL;

CREATE TABLE PATH_TBL (f1 path)
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl_both_filters (a int, b int, c int, PRIMARY KEY (a,c))
;
----------
[CREATE_TABLE] CREATE TABLE ctlt1 (a text CHECK (length(a) > 2) PRIMARY KEY, b text)
;
----------
[UNKNOWN] SECURITY LABEL ON TABLE t1 IS 'system_u:object_r:sepgsql_table_t:s0'
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl6_1" (
    "a" integer CONSTRAINT "ann" NOT NULL,
    "b" integer CONSTRAINT "bnn" NOT NULL,
    CONSTRAINT "notnull_tbl6_a_check" CHECK (("a" > 0))
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk_partitioned_fk_2 DROP COLUMN c
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @+@(int4, int4) SET SCHEMA alt_nsp2
;
----------
[ADD_INDEX] CREATE INDEX "spgist_box_idx" ON "public"."spgist_box_tbl" USING "spgist" ("b")
;
----------
[ALTER_LIBRARY] alter extension test_ext9 drop type varbitrange
;
----------
[CREATE_SCHEMA] -- doesn't exist

-- verify constraint deferrability
create schema fkpart3
  create table pkey (a int primary key)
  create table fk_part (a int, constraint fkey foreign key (a) references fkpart3.pkey deferrable initially immediate) partition by list (a)
  create table fk_part_1 partition of fkpart3.fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 partition of fkpart3.fk_part_1 for values in (1)
  create table fk_part_2 partition of fkpart3.fk_part for values in (2)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab5 DETACH PARTITION tab5_1
;
----------
[ALTER_SEQUENCE] -- fail, max has to be adjusted
ALTER SEQUENCE sequence_test8 AS smallint MAXVALUE 20000
;
----------
[ADD_INDEX] CREATE INDEX test_idx ON test(c1)
;
----------
[ADD_INDEX] CREATE INDEX collate_test1_idx2 ON collate_test1 (b COLLATE "C")
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE truncpart
;
----------
[CREATE_POLICY] /*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
----------
[CREATE_TABLE] create temp table phrase_index_test(fts tsvector)
;
----------
[CREATE_SCHEMA] -- failure, schema does not exist
CREATE SCHEMA schema_to_reindex
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_subscription_rel
;
----------
[CREATE_POLICY] CREATE CONVERSION myconv FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float4_picksplit(internal, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Trigger renaming does not recurse on legacy inheritance
create table parent (a int)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."gtest27" DROP COLUMN "........pg.dropped.3........"
;
----------
[COMMENT_COLUMN] comment on column atacc1."........pg.dropped.1........" is 'testing'
;
----------
[BLOCK] DO $$
BEGIN
    -- iterate often enough to see index growth even on larger-than-default page sizes
    FOR i IN 1..100 LOOP
        BEGIN
	    -- perform index scan over all the inserted keys to get them to be seen as dead
            IF EXISTS(SELECT * FROM clean_aborted_self WHERE key > 0 AND key < 100) THEN
	        RAISE data_corrupted USING MESSAGE = 'these rows should not exist';
            END IF;
            INSERT INTO clean_aborted_self SELECT g.i, 'rolling back in a sec' FROM generate_series(1, 100) g(i);
	    -- just some error that's not normally thrown
	    RAISE reading_sql_data_not_permitted USING MESSAGE = 'round and round again';
	EXCEPTION WHEN reading_sql_data_not_permitted THEN END;
    END LOOP;
END;$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_amop
;
----------
[CREATE_LIBRARY] CREATE EXTENSION adminpack
;
----------
[ADD_INDEX] create index gist_tbl_multi_index on gist_tbl using gist (circle(p,1), p)
;
----------
[CREATE_TABLE] create table j1 (id int primary key)
;
----------
[CREATE_TABLE|SELECT] create table gs_data_1 as
select g%1000 as g1000, g%100 as g100, g%10 as g10, g
   from generate_series(0,1999) g
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn05b"(integer) (
    SFUNC = "public"."tfnp",
    STYPE = integer[],
    INITCOND = '{}'
)
;
----------
[RENAME_CONSTRAINT] ALTER TABLE ONLY constraint_rename_test RENAME CONSTRAINT con2 TO con2foo
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_ml_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
----------
[BLOCK] -- Terminate the remote connection and wait for the termination to complete.
-- (If a cache flush happens, the remote connection might have already been
-- dropped; so code this step in a way that doesn't fail if no connection.)
DO $$ BEGIN
PERFORM pg_terminate_backend(pid, 180000) FROM pg_stat_activity
	WHERE application_name = 'fdw_retry_check';
END $$
;
----------
[TRUNCATE_TABLE] TRUNCATE spc_commit
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE IF NOT EXISTS 'pluto'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=15683 host=/tmp/7RF1JxeTxg dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_TABLE] create temp table t_opf (a t_rec not null, b text)
;
