/*
 * 6.59
 * SQL_IMPLEMENTATION_INFO table
 */

CREATE TABLE sql_implementation_info (
    implementation_info_id      character_data,
    implementation_info_name    character_data,
    integer_value               cardinal_number,
    character_value             character_data,
    comments                    character_data
)
;
ALTER TABLE pg_toast.pg_toast_30341 RENAME TO reind_con_toast
;
VACUUM pg_catalog.pg_enum
;
drop aggregate newcnt (nonesuch)
;
CREATE TABLE other_tbl_parent (id int)
;
CREATE FUNCTION hash_ltree_extended(ltree, bigint)
RETURNS bigint
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_27970 RENAME TO reind_con_toast
;
-- ok
SECURITY LABEL ON TABLE tpart
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
CREATE OPERATOR <-> (
	LEFTARG = int4,
	RIGHTARG = int4,
	PROCEDURE = int4_dist,
	COMMUTATOR = '<->'
)
;
-- Perform tests on the Memoize node.

-- The cache hits/misses/evictions from the Memoize node can vary between
-- machines.  Let's just replace the number with an 'N'.  In order to allow us
-- to perform validation when the measure was zero, we replace a zero value
-- with "Zero".  All other numbers are replaced with 'N'.
create function explain_memoize(query text, hide_hitmiss bool) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off, buffers off) %s',
            query)
    loop
        if hide_hitmiss = true then
                ln := regexp_replace(ln, 'Hits: 0', 'Hits: Zero');
                ln := regexp_replace(ln, 'Hits: \d+', 'Hits: N');
                ln := regexp_replace(ln, 'Misses: 0', 'Misses: Zero');
                ln := regexp_replace(ln, 'Misses: \d+', 'Misses: N');
        end if;
        ln := regexp_replace(ln, 'Evictions: 0', 'Evictions: Zero');
        ln := regexp_replace(ln, 'Evictions: \d+', 'Evictions: N');
        ln := regexp_replace(ln, 'Memory Usage: \d+', 'Memory Usage: N');
        ln := regexp_replace(ln, 'Heap Fetches: \d+', 'Heap Fetches: N');
        ln := regexp_replace(ln, 'loops=\d+', 'loops=N');
        ln := regexp_replace(ln, 'Index Searches: \d+', 'Index Searches: N');
        return next ln;
    end loop;
end;
$$
;
ALTER TABLE test_part_d_2 REPLICA IDENTITY USING INDEX test_part_d_2_pkey
;
create table parted_inval_tab_1 (a int) partition by range (a)
;
-- without an index
DROP INDEX concur_reindex_ind
;
ALTER INDEX pg_toast.pg_toast_30984_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION "public"."transition_table_level1_ri_parent_upd_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  DECLARE
    x int;
  BEGIN
    WITH p AS (SELECT level1_no, sum(delta) cnt
                 FROM (SELECT level1_no, 1 AS delta FROM i
                       UNION ALL
                       SELECT level1_no, -1 AS delta FROM d) w
                 GROUP BY level1_no
                 HAVING sum(delta) < 0)
    SELECT level1_no
      FROM p JOIN transition_table_level2 c ON c.parent_no = p.level1_no
      INTO x;
    IF FOUND THEN
      RAISE EXCEPTION 'RI error';
    END IF;
    RETURN NULL;
  END;
$$
;
ALTER SEQUENCE IF EXISTS sequence_test2 RESTART WITH 32 START WITH 32
  INCREMENT BY 4 MAXVALUE 36 MINVALUE 5 CYCLE
;
CREATE TABLE like_constraint_rename_cache
  (LIKE constraint_rename_cache INCLUDING ALL)
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn13,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE TEMP TABLE id_alw4 (i int GENERATED ALWAYS AS IDENTITY)
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc1(1)
;
-- Create foreign partitions
CREATE FOREIGN TABLE fpagg_tab_p1 PARTITION OF pagg_tab FOR VALUES FROM (0) TO (10) SERVER loopback OPTIONS (table_name 'pagg_tab_p1')
;
ALTER TABLE ONLY "public"."mlparted_def2" DROP COLUMN "........pg.dropped.4........"
;
DROP FUNCTION f(int)
;
-- This tests the issue described in match_pattern_prefix().  In the
-- absence of that check, the case_insensitive tests below would
-- return no rows where they should logically return one.
CREATE TABLE test4c (x text COLLATE "C")
;
CREATE RULE gtest_rule_upd AS ON UPDATE TO gtest_rule
  DO ALSO INSERT INTO gtest_rule_log VALUES ('UPD', OLD.b, NEW.b)
;
CREATE TABLE func_index_heap (f1 text, f2 text)
;
alter domain dom drop not null
;
DROP TABLE testpub_temptbl
;
-- Don't support column-level INSTEAD OF triggers
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE OF a ON main_view
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
ALTER TABLE replication_example ADD COLUMN testcolumn2 int
;
-- Simple table
VACUUM vacowned
;
DROP TABLE batch_cp_upd_test
;
create table child1 partition of parent for values in ('AAA')
;
CREATE TABLE "public"."prt1_l_p3_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
--    N    P        P    P
-- should ERROR: tfp(int[],anyelement) not matched by tfp(anyarray,anyelement)
CREATE AGGREGATE myaggn12a(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE FUNCTION functest_IS_2(out a int, b int default 1)
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT $1'
;
CREATE FUNCTION gbt_var_decompress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create table depth0()
;
ALTER INDEX pg_toast.pg_toast_30785_index RENAME TO reind_con_toast_idx
;
-- Currently, this doesn't work for composite types, but verify it complains
create type ddtest1 as (f1 posint)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_largeobject
;
DROP TABLE unlogged2
;
CREATE TABLE sql_features (
    feature_id          character_data,
    feature_name        character_data,
    sub_feature_id      character_data,
    sub_feature_name    character_data,
    is_supported        yes_or_no,
    is_verified_by      character_data,
    comments            character_data
)
;
CREATE EXTENSION bool_plperl CASCADE
;
CREATE TABLE "public"."inhf" (
    "xx" "text" DEFAULT 'text'::"text"
)
;
CREATE TABLE part2 (
	a int NOT NULL CHECK (a = 1),
	b int NOT NULL CHECK (b >= 10 AND b < 18)
)
;
CREATE TABLE ref1 PARTITION OF ref1_2 FOR VALUES IN (1)
;
COMMENT ON RULE rule_1 ON datatype_table IS 'RULE test'
;
CREATE TABLE "regress_rls_schema"."rls_tbl_force" (
    "c1" integer
)
;
ALTER TABLE ONLY "public"."pht1_e" ATTACH PARTITION "public"."pht1_e_p2" FOR VALUES WITH (modulus 3, remainder 1)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] /*
 * 6.59
 * SQL_IMPLEMENTATION_INFO table
 */

CREATE TABLE sql_implementation_info (
    implementation_info_id      character_data,
    implementation_info_name    character_data,
    integer_value               cardinal_number,
    character_value             character_data,
    comments                    character_data
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30341 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_enum
;
----------
[DROP_PROG_OBJ] drop aggregate newcnt (nonesuch)
;
----------
[CREATE_TABLE] CREATE TABLE other_tbl_parent (id int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hash_ltree_extended(ltree, bigint)
RETURNS bigint
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27970 RENAME TO reind_con_toast
;
----------
[UNKNOWN] -- ok
SECURITY LABEL ON TABLE tpart
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = int4,
	RIGHTARG = int4,
	PROCEDURE = int4_dist,
	COMMUTATOR = '<->'
)
;
----------
[CREATE_PROG_OBJ] -- Perform tests on the Memoize node.

-- The cache hits/misses/evictions from the Memoize node can vary between
-- machines.  Let's just replace the number with an 'N'.  In order to allow us
-- to perform validation when the measure was zero, we replace a zero value
-- with "Zero".  All other numbers are replaced with 'N'.
create function explain_memoize(query text, hide_hitmiss bool) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off, buffers off) %s',
            query)
    loop
        if hide_hitmiss = true then
                ln := regexp_replace(ln, 'Hits: 0', 'Hits: Zero');
                ln := regexp_replace(ln, 'Hits: \d+', 'Hits: N');
                ln := regexp_replace(ln, 'Misses: 0', 'Misses: Zero');
                ln := regexp_replace(ln, 'Misses: \d+', 'Misses: N');
        end if;
        ln := regexp_replace(ln, 'Evictions: 0', 'Evictions: Zero');
        ln := regexp_replace(ln, 'Evictions: \d+', 'Evictions: N');
        ln := regexp_replace(ln, 'Memory Usage: \d+', 'Memory Usage: N');
        ln := regexp_replace(ln, 'Heap Fetches: \d+', 'Heap Fetches: N');
        ln := regexp_replace(ln, 'loops=\d+', 'loops=N');
        ln := regexp_replace(ln, 'Index Searches: \d+', 'Index Searches: N');
        return next ln;
    end loop;
end;
$$
;
----------
[ALTER_TABLE] ALTER TABLE test_part_d_2 REPLICA IDENTITY USING INDEX test_part_d_2_pkey
;
----------
[CREATE_TABLE] create table parted_inval_tab_1 (a int) partition by range (a)
;
----------
[DROP_INDEX] -- without an index
DROP INDEX concur_reindex_ind
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30984_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."transition_table_level1_ri_parent_upd_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  DECLARE
    x int;
  BEGIN
    WITH p AS (SELECT level1_no, sum(delta) cnt
                 FROM (SELECT level1_no, 1 AS delta FROM i
                       UNION ALL
                       SELECT level1_no, -1 AS delta FROM d) w
                 GROUP BY level1_no
                 HAVING sum(delta) < 0)
    SELECT level1_no
      FROM p JOIN transition_table_level2 c ON c.parent_no = p.level1_no
      INTO x;
    IF FOUND THEN
      RAISE EXCEPTION 'RI error';
    END IF;
    RETURN NULL;
  END;
$$
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE IF EXISTS sequence_test2 RESTART WITH 32 START WITH 32
  INCREMENT BY 4 MAXVALUE 36 MINVALUE 5 CYCLE
;
----------
[CREATE_TABLE] CREATE TABLE like_constraint_rename_cache
  (LIKE constraint_rename_cache INCLUDING ALL)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn13,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] CREATE TEMP TABLE id_alw4 (i int GENERATED ALWAYS AS IDENTITY)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc1(1)
;
----------
[CREATE_TABLE] -- Create foreign partitions
CREATE FOREIGN TABLE fpagg_tab_p1 PARTITION OF pagg_tab FOR VALUES FROM (0) TO (10) SERVER loopback OPTIONS (table_name 'pagg_tab_p1')
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def2" DROP COLUMN "........pg.dropped.4........"
;
----------
[DROP_PROG_OBJ] DROP FUNCTION f(int)
;
----------
[CREATE_TABLE] -- This tests the issue described in match_pattern_prefix().  In the
-- absence of that check, the case_insensitive tests below would
-- return no rows where they should logically return one.
CREATE TABLE test4c (x text COLLATE "C")
;
----------
[CREATE_POLICY] CREATE RULE gtest_rule_upd AS ON UPDATE TO gtest_rule
  DO ALSO INSERT INTO gtest_rule_log VALUES ('UPD', OLD.b, NEW.b)
;
----------
[CREATE_TABLE] CREATE TABLE func_index_heap (f1 text, f2 text)
;
----------
[ALTER_TYPE] alter domain dom drop not null
;
----------
[DROP_TABLE] DROP TABLE testpub_temptbl
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Don't support column-level INSTEAD OF triggers
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE OF a ON main_view
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE replication_example ADD COLUMN testcolumn2 int
;
----------
[ADMIN_TABLE] -- Simple table
VACUUM vacowned
;
----------
[DROP_TABLE] DROP TABLE batch_cp_upd_test
;
----------
[CREATE_TABLE] create table child1 partition of parent for values in ('AAA')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_l_p3_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[CREATE_PROG_OBJ] --    N    P        P    P
-- should ERROR: tfp(int[],anyelement) not matched by tfp(anyarray,anyelement)
CREATE AGGREGATE myaggn12a(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_IS_2(out a int, b int default 1)
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT $1'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_var_decompress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] create table depth0()
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30785_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TYPE] -- Currently, this doesn't work for composite types, but verify it complains
create type ddtest1 as (f1 posint)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_largeobject
;
----------
[DROP_TABLE] DROP TABLE unlogged2
;
----------
[CREATE_TABLE] CREATE TABLE sql_features (
    feature_id          character_data,
    feature_name        character_data,
    sub_feature_id      character_data,
    sub_feature_name    character_data,
    is_supported        yes_or_no,
    is_verified_by      character_data,
    comments            character_data
)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION bool_plperl CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."inhf" (
    "xx" "text" DEFAULT 'text'::"text"
)
;
----------
[CREATE_TABLE] CREATE TABLE part2 (
	a int NOT NULL CHECK (a = 1),
	b int NOT NULL CHECK (b >= 10 AND b < 18)
)
;
----------
[CREATE_TABLE] CREATE TABLE ref1 PARTITION OF ref1_2 FOR VALUES IN (1)
;
----------
[ALTER_POLICY] COMMENT ON RULE rule_1 ON datatype_table IS 'RULE test'
;
----------
[CREATE_TABLE] CREATE TABLE "regress_rls_schema"."rls_tbl_force" (
    "c1" integer
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pht1_e" ATTACH PARTITION "public"."pht1_e_p2" FOR VALUES WITH (modulus 3, remainder 1)
;
