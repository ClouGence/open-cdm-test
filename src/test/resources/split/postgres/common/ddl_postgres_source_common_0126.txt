CREATE INDEX gin_check_text_array_idx on "gin_check_text_array" USING GIN("Column1")
;
-- test views

CREATE TABLE itest10 (a int generated by default as identity, b text)
;
-- Fill a few pages
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize_bloom VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
ALTER TABLE parent FORCE ROW LEVEL SECURITY
;
-- test jsonb -> python dict
-- complex dict with dicts as value
CREATE FUNCTION test1complex(val jsonb) RETURNS int
LANGUAGE plpython2u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {"d": {"d": 1}})
return len(val)
$$
;
CREATE TABLE upsert (key int primary key, val text)
;
ALTER TABLE itest8
  ALTER COLUMN f5 DROP IDENTITY,
  ALTER COLUMN f5 DROP NOT NULL,
  ALTER COLUMN f5 SET DATA TYPE bigint
;
ALTER TABLE ONLY public.ths_p1
    ADD CONSTRAINT ths_p1_mod_data_key UNIQUE (mod, data)
;
ALTER TABLE pg_temp_13.pg_temp_26765_2 ADD COLUMN newdata pg_temp_13.pg_temp_26765
;
create temp table arr_tbl (f1 int[] unique)
;
/*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
-- after validation, the constraint should be used
alter table nv_child_2011 VALIDATE CONSTRAINT nv_child_2011_d_check
;
drop table test2
;
CREATE PROCEDURE ptest6b(a anyelement, out b anyelement, out c anyarray)
LANGUAGE SQL
AS $$
SELECT $1, array[$1];
$$
;
CREATE TABLE gtest1_2 () INHERITS (gtest1, gtesty)
;
ANALYZE public.mcv_lists
;
CREATE TABLE "generated_stored_tests"."gtest20c" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    CONSTRAINT "whole_row_check" CHECK (("gtest20c".* IS NOT NULL))
)
;
ALTER OPERATOR FAMILY alt_opf4 USING btree DROP
  
  OPERATOR 1 (int4, int2) ,
  OPERATOR 2 (int4, int2) ,
  OPERATOR 3 (int4, int2) ,
  OPERATOR 4 (int4, int2) ,
  OPERATOR 5 (int4, int2) ,
  FUNCTION 1 (int4, int2)
;
CREATE RULE sj_del_rule AS ON DELETE TO sj
  DO INSTEAD
    UPDATE sj SET a = 1 WHERE a = old.a
;
do $$ declare c nested_int8s; begin c.x.q1 = 1; end $$
;
drop table mlparted_tab
;
-- also check reverse-listing
create table inserttest2 (f1 bigint, f2 text)
;
CREATE TEXT SEARCH PARSER testparser (
    START    = testprs_start,
    GETTOKEN = testprs_getlexeme,
    END      = testprs_end,
    HEADLINE = pg_catalog.prsd_headline,
    LEXTYPES = testprs_lextype
)
;
CREATE TABLE pub_test1.tbl (id int, data text)
;
create table idxpart31 partition of idxpart3 for values from (1000) to (1200)
;
create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = 'string')
;
alter table wcowrtest add b text
;
/*
 * text search configuration for hungarian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hungarian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
CREATE FUNCTION _lt_q_regex(_ltree,_lquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
alter table tenk2 set (parallel_workers = 1)
;
ALTER TABLE tt5 OF tt_t0
;
CREATE TABLE "public"."partr_def2" (
    "a" integer CONSTRAINT "part1_a_not_null" NOT NULL,
    "b" integer CONSTRAINT "part1_b_not_null" NOT NULL,
    CONSTRAINT "part1_a_check" CHECK (("a" = 1)),
    CONSTRAINT "part1_b_check" CHECK ((("b" >= 1) AND ("b" <= 10)))
)
;
-- should fail, can't have event triggers on event triggers
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('DROP EVENT TRIGGER')
   execute procedure test_event_trigger()
;
CREATE TABLE generated_cols (a int, b_gen int GENERATED ALWAYS AS (5 * a) STORED, c int)
;
-- fail
TRUNCATE TABLE trunc_c,trunc_d
;
VACUUM INT2_TBL
;
ANALYZE public.num_exp_log10
;
--
-- Test sub-select in nested security barrier views, per bug #17972
--
CREATE TABLE t1 (a int)
;
-- no such relation
drop table nonesuch
;
alter table atacc1 rename a to x
;
ALTER FUNCTION alt_func2(int) SET SCHEMA alt_nsp2
;
CLUSTER ptnowner USING ptnowner_i_idx
;
VACUUM (FULL) pg_catalog.pg_shdepend
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=22471 host=/tmp/pXMOhcsv2s dbname=postgres' PUBLICATION tap_pub
;
CREATE OPERATOR <-> (
	LEFTARG = interval,
	RIGHTARG = interval,
	PROCEDURE = interval_dist,
	COMMUTATOR = '<->'
)
;
DROP FUNCTION dogrant_ok()
;
ALTER TABLE pg_toast.pg_toast_28127 RENAME TO reind_con_toast
;
CREATE TABLE intra_grant_inplace (c int)
;
DROP OPERATOR @ (cube, cube)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_language
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX gin_check_text_array_idx on "gin_check_text_array" USING GIN("Column1")
;
----------
[CREATE_TABLE] -- test views

CREATE TABLE itest10 (a int generated by default as identity, b text)
;
----------
[BLOCK] -- Fill a few pages
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize_bloom VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
----------
[ALTER_TABLE] ALTER TABLE parent FORCE ROW LEVEL SECURITY
;
----------
[CREATE_PROG_OBJ] -- test jsonb -> python dict
-- complex dict with dicts as value
CREATE FUNCTION test1complex(val jsonb) RETURNS int
LANGUAGE plpython2u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {"d": {"d": 1}})
return len(val)
$$
;
----------
[CREATE_TABLE] CREATE TABLE upsert (key int primary key, val text)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest8
  ALTER COLUMN f5 DROP IDENTITY,
  ALTER COLUMN f5 DROP NOT NULL,
  ALTER COLUMN f5 SET DATA TYPE bigint
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY public.ths_p1
    ADD CONSTRAINT ths_p1_mod_data_key UNIQUE (mod, data)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26765_2 ADD COLUMN newdata pg_temp_13.pg_temp_26765
;
----------
[CREATE_TABLE] create temp table arr_tbl (f1 int[] unique)
;
----------
[CREATE_POLICY] /*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- after validation, the constraint should be used
alter table nv_child_2011 VALIDATE CONSTRAINT nv_child_2011_d_check
;
----------
[DROP_TABLE] drop table test2
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest6b(a anyelement, out b anyelement, out c anyarray)
LANGUAGE SQL
AS $$
SELECT $1, array[$1];
$$
;
----------
[CREATE_TABLE] CREATE TABLE gtest1_2 () INHERITS (gtest1, gtesty)
;
----------
[ADMIN_TABLE] ANALYZE public.mcv_lists
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest20c" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    CONSTRAINT "whole_row_check" CHECK (("gtest20c".* IS NOT NULL))
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf4 USING btree DROP
  
  OPERATOR 1 (int4, int2) ,
  OPERATOR 2 (int4, int2) ,
  OPERATOR 3 (int4, int2) ,
  OPERATOR 4 (int4, int2) ,
  OPERATOR 5 (int4, int2) ,
  FUNCTION 1 (int4, int2)
;
----------
[CREATE_POLICY] CREATE RULE sj_del_rule AS ON DELETE TO sj
  DO INSTEAD
    UPDATE sj SET a = 1 WHERE a = old.a
;
----------
[BLOCK] do $$ declare c nested_int8s; begin c.x.q1 = 1; end $$
;
----------
[DROP_TABLE] drop table mlparted_tab
;
----------
[CREATE_TABLE] -- also check reverse-listing
create table inserttest2 (f1 bigint, f2 text)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH PARSER testparser (
    START    = testprs_start,
    GETTOKEN = testprs_getlexeme,
    END      = testprs_end,
    HEADLINE = pg_catalog.prsd_headline,
    LEXTYPES = testprs_lextype
)
;
----------
[CREATE_TABLE] CREATE TABLE pub_test1.tbl (id int, data text)
;
----------
[CREATE_TABLE] create table idxpart31 partition of idxpart3 for values from (1000) to (1200)
;
----------
[ADD_INDEX] create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = 'string')
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table wcowrtest add b text
;
----------
[CREATE_POLICY] /*
 * text search configuration for hungarian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hungarian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _lt_q_regex(_ltree,_lquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] alter table tenk2 set (parallel_workers = 1)
;
----------
[ALTER_TABLE] ALTER TABLE tt5 OF tt_t0
;
----------
[CREATE_TABLE] CREATE TABLE "public"."partr_def2" (
    "a" integer CONSTRAINT "part1_a_not_null" NOT NULL,
    "b" integer CONSTRAINT "part1_b_not_null" NOT NULL,
    CONSTRAINT "part1_a_check" CHECK (("a" = 1)),
    CONSTRAINT "part1_b_check" CHECK ((("b" >= 1) AND ("b" <= 10)))
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should fail, can't have event triggers on event triggers
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('DROP EVENT TRIGGER')
   execute procedure test_event_trigger()
;
----------
[CREATE_TABLE] CREATE TABLE generated_cols (a int, b_gen int GENERATED ALWAYS AS (5 * a) STORED, c int)
;
----------
[TRUNCATE_TABLE] -- fail
TRUNCATE TABLE trunc_c,trunc_d
;
----------
[ADMIN_TABLE] VACUUM INT2_TBL
;
----------
[ADMIN_TABLE] ANALYZE public.num_exp_log10
;
----------
[CREATE_TABLE] --
-- Test sub-select in nested security barrier views, per bug #17972
--
CREATE TABLE t1 (a int)
;
----------
[DROP_TABLE] -- no such relation
drop table nonesuch
;
----------
[RENAME_COLUMN] alter table atacc1 rename a to x
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION alt_func2(int) SET SCHEMA alt_nsp2
;
----------
[ADMIN_TABLE] CLUSTER ptnowner USING ptnowner_i_idx
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_shdepend
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=22471 host=/tmp/pXMOhcsv2s dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = interval,
	RIGHTARG = interval,
	PROCEDURE = interval_dist,
	COMMUTATOR = '<->'
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION dogrant_ok()
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28127 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE intra_grant_inplace (c int)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR @ (cube, cube)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_language
;
