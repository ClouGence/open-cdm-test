CREATE TABLE heaptest (a integer, b text)
;
ALTER TABLE pg_toast.pg_toast_30805 RENAME TO reind_con_toast
;
ALTER TABLE ONLY "public"."d" INHERIT "public"."c"
;
create table parted_replica_tab_11 partition of parted_replica_tab_1
  for values from (1) to (5)
;
drop function anyctest(anycompatible, anycompatiblerange)
;
CREATE TEXT SEARCH CONFIGURATION hindi
	(PARSER = default)
;
CREATE VIEW ro_view4 AS SELECT count(*) FROM base_tbl
;
CREATE FUNCTION btean13cmp(ean13, issn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (FULL) pg_catalog.pg_trigger
;
-- ************************************************************
-- * AFTER INSERT or UPDATE or DELETE on slot with backlink
-- *	- Ensure that the opponent correctly points back to us
-- ************************************************************
create function tg_backlink_a() returns trigger as '
declare
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
        if new.backlink != '''' then
	    dummy := tg_backlink_set(new.backlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = ''UPDATE'' then
        if new.backlink != old.backlink then
	    if old.backlink != '''' then
	        dummy := tg_backlink_unset(old.backlink, old.slotname);
	    end if;
	    if new.backlink != '''' then
	        dummy := tg_backlink_set(new.backlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.backlink != '''' then
	        dummy := tg_slotlink_set(new.backlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = ''DELETE'' then
        if old.backlink != '''' then
	    dummy := tg_backlink_unset(old.backlink, old.slotname);
	end if;
	return old;
    end if;
end;
' language plpgsql
;
-- Check that invalid indexes are not selected when attaching a partition.
create table parted_inval_tab (a int) partition by range (a)
;
create table child (b decimal(10,7)) inherits (parent)
;
DROP VIEW rls_view
;
-- check that default_tablespace doesn't affect ALTER TABLE index rebuilds
-- (this time with a partitioned table)
CREATE TABLE testschema.test_default_tab_p(id bigint, val bigint)
    PARTITION BY LIST (id) TABLESPACE regress_tblspace
;
CREATE TABLE fk_partitioned_fk (b int, fdrop1 int, a int) PARTITION BY RANGE (a, b)
;
alter extension postgres_fdw drop function my_op_cmp(a int, b int)
;
-- test deferrable UNIQUE with a partitioned table
CREATE TABLE parted_uniq_tbl (i int UNIQUE DEFERRABLE) partition by range (i)
;
CREATE FUNCTION inj_io_reopen_attach()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
ANALYZE pg_temp_3.pg_temp_16656
;
--
-- connectby
--

-- test connectby with text based hierarchy
CREATE TABLE connectby_text(keyid text, parent_keyid text, pos int)
;
CREATE TABLE partitioned (
	a int,
	b int,
	c text,
	d text
) PARTITION BY RANGE (a oid_ops, plusone(b), c collate "default", d collate "C")
;
-- silently do nothing
DROP TABLE unlogged3
;
CREATE SUBSCRIPTION sub CONNECTION 'port=14459 host=/tmp/LVMff29NBJ dbname=postgres' PUBLICATION pub WITH (disable_on_error = true, streaming = on, two_phase = on)
;
-- success, max will be adjusted
ALTER SEQUENCE sequence_test8 AS smallint
;
CREATE TABLE sch1.tbl1_part3 (a int) PARTITION BY RANGE(a)
;
CREATE TEMP TABLE t2 (a int, b int)
;
CREATE TABLE ctas_stats_2 AS
  SELECT a AS col1, 2::int AS col2
    FROM generate_series(1, 10) AS tab(a) WHERE a < 5 AND a > 2
;
ALTER TABLE foo_parted ATTACH PARTITION foo_part_d2 FOR VALUES IN (4)
;
CREATE SUBSCRIPTION testsub CONNECTION $$port=25799 host=/tmp/REnOPq3M6y dbname='d1'$$ PUBLICATION testpub WITH (create_slot=false, slot_name='testslot')
;
CREATE TEMP TABLE hash_temp_heap (x int, y int)
;
CREATE TABLE dump_test.table_index_stats (
						   col1 int,
						   col2 int,
						   col3 int)
;
CREATE TABLE alpha_pos_p3 PARTITION OF alpha_pos FOR VALUES IN ('0008', '0009')
;
DROP RULE y_rule ON y
;
DROP TABLE distinct_hash_2
;
ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_pkey PRIMARY KEY (f1)
;
DROP TABLE pxtest4
;
CREATE SUBSCRIPTION regress_sub CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION regress_pub
;
CREATE TABLE split_alter_table.typed_probe (id integer, note text)
;
VACUUM ANALYZE num_exp_ln
;
ANALYZE public.gtestxx_4
;
create trigger parted_trig after insert on parted_irreg
  for each row execute procedure trigger_notice_ab()
;
create table atacc1 (test int, test2 int, primary key(test))
;
CREATE FUNCTION do_set_block_offsets(
blkno bigint,
offsets int2[])
RETURNS bigint STRICT PARALLEL UNSAFE
AS 'MODULE_PATHNAME' LANGUAGE C
;
---
--- CREATE_CONVERSION
---

-- Simple test should suffice for this
CREATE CONVERSION myconv FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
ALTER TABLE pg_temp_11.pg_temp_26417_2 ADD COLUMN newdata pg_temp_11.pg_temp_26417
;
CREATE TABLE parted_uniq_tbl (i int UNIQUE DEFERRABLE) partition by range (i)
;
ALTER TABLE ONLY "public"."part2_self_fk"
    ADD CONSTRAINT "part2_self_fk_pkey" PRIMARY KEY ("id")
;
-- index
CREATE INDEX foo_idx on testschema.foo(i) TABLESPACE regress_tblspace
;
CREATE VIEW tv AS SELECT * FROM target
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_class
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE heaptest (a integer, b text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30805 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."d" INHERIT "public"."c"
;
----------
[CREATE_TABLE] create table parted_replica_tab_11 partition of parted_replica_tab_1
  for values from (1) to (5)
;
----------
[DROP_PROG_OBJ] drop function anyctest(anycompatible, anycompatiblerange)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION hindi
	(PARSER = default)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view4 AS SELECT count(*) FROM base_tbl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btean13cmp(ean13, issn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_trigger
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * AFTER INSERT or UPDATE or DELETE on slot with backlink
-- *	- Ensure that the opponent correctly points back to us
-- ************************************************************
create function tg_backlink_a() returns trigger as '
declare
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
        if new.backlink != '''' then
	    dummy := tg_backlink_set(new.backlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = ''UPDATE'' then
        if new.backlink != old.backlink then
	    if old.backlink != '''' then
	        dummy := tg_backlink_unset(old.backlink, old.slotname);
	    end if;
	    if new.backlink != '''' then
	        dummy := tg_backlink_set(new.backlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.backlink != '''' then
	        dummy := tg_slotlink_set(new.backlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = ''DELETE'' then
        if old.backlink != '''' then
	    dummy := tg_backlink_unset(old.backlink, old.slotname);
	end if;
	return old;
    end if;
end;
' language plpgsql
;
----------
[CREATE_TABLE] -- Check that invalid indexes are not selected when attaching a partition.
create table parted_inval_tab (a int) partition by range (a)
;
----------
[CREATE_TABLE] create table child (b decimal(10,7)) inherits (parent)
;
----------
[DROP_VIEW] DROP VIEW rls_view
;
----------
[CREATE_TABLE] -- check that default_tablespace doesn't affect ALTER TABLE index rebuilds
-- (this time with a partitioned table)
CREATE TABLE testschema.test_default_tab_p(id bigint, val bigint)
    PARTITION BY LIST (id) TABLESPACE regress_tblspace
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk (b int, fdrop1 int, a int) PARTITION BY RANGE (a, b)
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw drop function my_op_cmp(a int, b int)
;
----------
[CREATE_TABLE] -- test deferrable UNIQUE with a partitioned table
CREATE TABLE parted_uniq_tbl (i int UNIQUE DEFERRABLE) partition by range (i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION inj_io_reopen_attach()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_16656
;
----------
[CREATE_TABLE] --
-- connectby
--

-- test connectby with text based hierarchy
CREATE TABLE connectby_text(keyid text, parent_keyid text, pos int)
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int,
	b int,
	c text,
	d text
) PARTITION BY RANGE (a oid_ops, plusone(b), c collate "default", d collate "C")
;
----------
[DROP_TABLE] -- silently do nothing
DROP TABLE unlogged3
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub CONNECTION 'port=14459 host=/tmp/LVMff29NBJ dbname=postgres' PUBLICATION pub WITH (disable_on_error = true, streaming = on, two_phase = on)
;
----------
[ALTER_SEQUENCE] -- success, max will be adjusted
ALTER SEQUENCE sequence_test8 AS smallint
;
----------
[CREATE_TABLE] CREATE TABLE sch1.tbl1_part3 (a int) PARTITION BY RANGE(a)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE t2 (a int, b int)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_stats_2 AS
  SELECT a AS col1, 2::int AS col2
    FROM generate_series(1, 10) AS tab(a) WHERE a < 5 AND a > 2
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE foo_parted ATTACH PARTITION foo_part_d2 FOR VALUES IN (4)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub CONNECTION $$port=25799 host=/tmp/REnOPq3M6y dbname='d1'$$ PUBLICATION testpub WITH (create_slot=false, slot_name='testslot')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE hash_temp_heap (x int, y int)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.table_index_stats (
						   col1 int,
						   col2 int,
						   col3 int)
;
----------
[CREATE_TABLE] CREATE TABLE alpha_pos_p3 PARTITION OF alpha_pos FOR VALUES IN ('0008', '0009')
;
----------
[DROP_POLICY] DROP RULE y_rule ON y
;
----------
[DROP_TABLE] DROP TABLE distinct_hash_2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_pkey PRIMARY KEY (f1)
;
----------
[DROP_TABLE] DROP TABLE pxtest4
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION regress_pub
;
----------
[CREATE_TABLE] CREATE TABLE split_alter_table.typed_probe (id integer, note text)
;
----------
[ADMIN_TABLE] VACUUM ANALYZE num_exp_ln
;
----------
[ADMIN_TABLE] ANALYZE public.gtestxx_4
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parted_trig after insert on parted_irreg
  for each row execute procedure trigger_notice_ab()
;
----------
[CREATE_TABLE] create table atacc1 (test int, test2 int, primary key(test))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION do_set_block_offsets(
blkno bigint,
offsets int2[])
RETURNS bigint STRICT PARALLEL UNSAFE
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_POLICY] ---
--- CREATE_CONVERSION
---

-- Simple test should suffice for this
CREATE CONVERSION myconv FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26417_2 ADD COLUMN newdata pg_temp_11.pg_temp_26417
;
----------
[CREATE_TABLE] CREATE TABLE parted_uniq_tbl (i int UNIQUE DEFERRABLE) partition by range (i)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."part2_self_fk"
    ADD CONSTRAINT "part2_self_fk_pkey" PRIMARY KEY ("id")
;
----------
[ADD_INDEX] -- index
CREATE INDEX foo_idx on testschema.foo(i) TABLESPACE regress_tblspace
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW tv AS SELECT * FROM target
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_class
;
