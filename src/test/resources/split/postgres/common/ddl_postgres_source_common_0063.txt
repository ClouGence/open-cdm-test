create table wcowrtest1 partition of wcowrtest for values in (1)
;
ANALYZE public.ruletest_tbl
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', header 'true')
;
CREATE TABLE footrg2 PARTITION OF footrg FOR VALUES IN (2)
;
ANALYZE prt1
;
CREATE TABLE "public"."idxpart1" (
    "a" integer
)
;
/*
 * text search configuration for hindi language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hindi and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (max(somename)) TO ('2019-01-01')
;
--
-- DROP Permission checks (with clean-up)
--

DROP FUNCTION regtest_func(text,int[])
;
TRUNCATE TABLE ONLY public.ths_p1
;
CREATE VIEW sums_1_100 AS
WITH RECURSIVE t(n) AS (
    VALUES (1)
UNION ALL
    SELECT n+1 FROM t WHERE n < 100
)
SELECT sum(n) FROM t
;
--
-- Check that volatile quals aren't pushed down past a DISTINCT:
-- nextval() should not be called more than the nominal number of times
--
create temp sequence ts1
;
ALTER TABLE alter_table_under_transition_tables
  ALTER COLUMN name TYPE int USING name::integer
;
-- should fail
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b::numeric, c, d FROM viewtest_tbl
;
create table rtest_emplog (ename char(20), who name, action char(10), newsal money, oldsal money)
;
CREATE OPERATOR @> (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_contains,
	COMMUTATOR = '<@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
DROP TABLE default_test
;
DROP TABLE public.tab_rf_partition
;
create table parted_trig (a int) partition by list (a)
;
ANALYZE pg_temp_11.pg_temp_26168
;
CREATE VIEW "public"."my_property_secure" WITH ("security_barrier"='false') AS
 SELECT "customer"."cid",
    "customer"."name",
    "customer"."tel",
    "customer"."passwd"
   FROM "public"."customer"
  WHERE ("customer"."name" = CURRENT_USER)
;
CREATE TABLE int2tmp (a int2)
;
-- we want a view based on the table, too, since views present additional challenges
CREATE VIEW mvtest_tv AS SELECT type, sum(amt) AS totamt FROM mvtest_t GROUP BY type
;
CREATE INDEX t2_brin ON s4.t2 USING BRIN (i)
;
-- Move back the function, and the extension can be moved.
ALTER FUNCTION test_func_dep2.dep_req1() SET SCHEMA test_func_dep1
;
ANALYZE public.xacttest
;
VACUUM (FULL) pg_catalog.pg_tablespace
;
--
-- FOREIGN KEY
--

-- MATCH FULL
--
-- First test, check and cascade
--
CREATE TABLE PKTABLE ( ptest1 int PRIMARY KEY, ptest2 text )
;
CREATE INDEX regress_test_table_t_idx ON regress_test_table (t)
;
/*
 * 6.43
 * ROLE_ROUTINE_GRANTS view
 */

CREATE VIEW role_routine_grants AS
    SELECT grantor,
           grantee,
           specific_catalog,
           specific_schema,
           specific_name,
           routine_catalog,
           routine_schema,
           routine_name,
           privilege_type,
           is_grantable
    FROM routine_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
---
--- CREATE_TRIGGER
---

CREATE FUNCTION plpgsql_function_trigger_1()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
BEGIN
  RETURN NEW;
END;
$$
;
CREATE TABLE RANDOM_TBL AS
  SELECT count(*) AS random
  FROM onek WHERE random() < 1.0/10
;
ALTER TABLE ONLY list_parted2 DROP CONSTRAINT check_b
;
-- Test when parent can produce parallel paths but not any (or some) of its children
ALTER TABLE pagg_tab_para_p1 SET (parallel_workers = 0)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
-- check schema-qualification of regconfig constant
CREATE TEXT SEARCH CONFIGURATION public.custom_search
  (COPY = pg_catalog.english)
;
CREATE TABLE pub_test2.tbl1 (id serial primary key, data text)
;
/*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
alter domain con drop constraint t
;
CREATE FUNCTION alter_and_insert() RETURNS VOID LANGUAGE sql AS $$
  alter table ddl_test alter column f1 type numeric;
  insert into ddl_test values (1.2);
$$
;
VACUUM (ANALYZE) pg_catalog.pg_operator
;
DROP TABLE as_select1
;
ALTER PUBLICATION testpub_ins_trunct ADD TABLE pub_test.testpub_nopk, testpub_tbl1
;
ALTER INDEX pg_toast.pg_toast_27601_index RENAME TO reind_con_toast_idx
;
DROP TABLE pg_temp_4.pg_temp_29544_2, pg_temp_4.pg_temp_29544
;
DROP TABLE IF EXISTS ctlt4, ctlt10, ctlt11, ctlt11a, ctlt12
;
CREATE TEMP VIEW id_alw2_view AS SELECT * FROM id_alw2
;
DROP TABLE pg_temp_12.pg_temp_26162_2, pg_temp_12.pg_temp_26162
;
VACUUM (ANALYZE) pg_catalog.pg_ts_template
;
ALTER FUNCTION gbt_float8_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table wcowrtest1 partition of wcowrtest for values in (1)
;
----------
[ADMIN_TABLE] ANALYZE public.ruletest_tbl
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', header 'true')
;
----------
[CREATE_TABLE] CREATE TABLE footrg2 PARTITION OF footrg FOR VALUES IN (2)
;
----------
[ADMIN_TABLE] ANALYZE prt1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart1" (
    "a" integer
)
;
----------
[CREATE_POLICY] /*
 * text search configuration for hindi language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hindi and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (max(somename)) TO ('2019-01-01')
;
----------
[DROP_PROG_OBJ] --
-- DROP Permission checks (with clean-up)
--

DROP FUNCTION regtest_func(text,int[])
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE ONLY public.ths_p1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW sums_1_100 AS
WITH RECURSIVE t(n) AS (
    VALUES (1)
UNION ALL
    SELECT n+1 FROM t WHERE n < 100
)
SELECT sum(n) FROM t
;
----------
[CREATE_SEQUENCE] --
-- Check that volatile quals aren't pushed down past a DISTINCT:
-- nextval() should not be called more than the nominal number of times
--
create temp sequence ts1
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE alter_table_under_transition_tables
  ALTER COLUMN name TYPE int USING name::integer
;
----------
[CREATE_VIEW(SELECT)] -- should fail
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b::numeric, c, d FROM viewtest_tbl
;
----------
[CREATE_TABLE] create table rtest_emplog (ename char(20), who name, action char(10), newsal money, oldsal money)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @> (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_contains,
	COMMUTATOR = '<@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[DROP_TABLE] DROP TABLE default_test
;
----------
[DROP_TABLE] DROP TABLE public.tab_rf_partition
;
----------
[CREATE_TABLE] create table parted_trig (a int) partition by list (a)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26168
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."my_property_secure" WITH ("security_barrier"='false') AS
 SELECT "customer"."cid",
    "customer"."name",
    "customer"."tel",
    "customer"."passwd"
   FROM "public"."customer"
  WHERE ("customer"."name" = CURRENT_USER)
;
----------
[CREATE_TABLE] CREATE TABLE int2tmp (a int2)
;
----------
[CREATE_VIEW(SELECT)] -- we want a view based on the table, too, since views present additional challenges
CREATE VIEW mvtest_tv AS SELECT type, sum(amt) AS totamt FROM mvtest_t GROUP BY type
;
----------
[ADD_INDEX] CREATE INDEX t2_brin ON s4.t2 USING BRIN (i)
;
----------
[ALTER_PROG_OBJ] -- Move back the function, and the extension can be moved.
ALTER FUNCTION test_func_dep2.dep_req1() SET SCHEMA test_func_dep1
;
----------
[ADMIN_TABLE] ANALYZE public.xacttest
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_tablespace
;
----------
[CREATE_TABLE] --
-- FOREIGN KEY
--

-- MATCH FULL
--
-- First test, check and cascade
--
CREATE TABLE PKTABLE ( ptest1 int PRIMARY KEY, ptest2 text )
;
----------
[ADD_INDEX] CREATE INDEX regress_test_table_t_idx ON regress_test_table (t)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.43
 * ROLE_ROUTINE_GRANTS view
 */

CREATE VIEW role_routine_grants AS
    SELECT grantor,
           grantee,
           specific_catalog,
           specific_schema,
           specific_name,
           routine_catalog,
           routine_schema,
           routine_name,
           privilege_type,
           is_grantable
    FROM routine_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[CREATE_PROG_OBJ] ---
--- CREATE_TRIGGER
---

CREATE FUNCTION plpgsql_function_trigger_1()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
BEGIN
  RETURN NEW;
END;
$$
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE RANDOM_TBL AS
  SELECT count(*) AS random
  FROM onek WHERE random() < 1.0/10
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE ONLY list_parted2 DROP CONSTRAINT check_b
;
----------
[ALTER_TABLE] -- Test when parent can produce parallel paths but not any (or some) of its children
ALTER TABLE pagg_tab_para_p1 SET (parallel_workers = 0)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
----------
[CREATE_POLICY] -- check schema-qualification of regconfig constant
CREATE TEXT SEARCH CONFIGURATION public.custom_search
  (COPY = pg_catalog.english)
;
----------
[CREATE_TABLE] CREATE TABLE pub_test2.tbl1 (id serial primary key, data text)
;
----------
[CREATE_POLICY] /*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
----------
[ALTER_TYPE] alter domain con drop constraint t
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION alter_and_insert() RETURNS VOID LANGUAGE sql AS $$
  alter table ddl_test alter column f1 type numeric;
  insert into ddl_test values (1.2);
$$
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_operator
;
----------
[DROP_TABLE] DROP TABLE as_select1
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_ins_trunct ADD TABLE pub_test.testpub_nopk, testpub_tbl1
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27601_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_29544_2, pg_temp_4.pg_temp_29544
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS ctlt4, ctlt10, ctlt11, ctlt11a, ctlt12
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW id_alw2_view AS SELECT * FROM id_alw2
;
----------
[DROP_TABLE] DROP TABLE pg_temp_12.pg_temp_26162_2, pg_temp_12.pg_temp_26162
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_ts_template
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float8_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
