ALTER TABLE pitest1_p1 ALTER COLUMN f3 DROP NOT NULL
;
alter table pc_list_parted detach partition pc_list_part_null
;
create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = '1234F', detail = 'some detail info';
  exception
    when others then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
create table partitioned2
  partition of partitioned for values in ('(2,4)')
;
-- Test with row-level ON DELETE triggers
CREATE TRIGGER trig_row_before_delete
BEFORE DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
CREATE OR REPLACE FUNCTION
  pg_terminate_backend(pid integer, timeout int8 DEFAULT 0)
  RETURNS boolean STRICT VOLATILE LANGUAGE INTERNAL AS 'pg_terminate_backend'
  PARALLEL SAFE
;
create table idxpart(drop_1 int, drop_2 int, col_keep int, drop_3 int) partition by range (col_keep)
;
-- Test that pending inserts are handled properly when needed
CREATE TABLE batch_table (a text, b int)
;
ALTER FOREIGN TABLE ft1 DROP CONSTRAINT ft1_c9_check
;
-- error
CREATE SCHEMA newschema6 AUTHORIZATION "public"
;
CREATE TABLE "generated_stored_tests"."gtest22a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" / 2)) STORED
)
;
-- drop constraints recursively
ALTER TABLE fd_pt1 DROP CONSTRAINT fd_pt1chk1 CASCADE
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29512 host=/tmp/2AUEITRTRR dbname=postgres' PUBLICATION tap_pub
;
CREATE FUNCTION seg_lower(seg)
RETURNS float4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP TRIGGER trigtest_before_stmt ON foreign_schema.foreign_table_1
;
drop table capitals
;
DROP DATABASE foobar1
;
CREATE TABLE some_t (some_col boolean NOT NULL)
;
CREATE PUBLICATION testpub_foralltables FOR ALL TABLES
;
CREATE TABLE test30_1 PARTITION OF test30 FOR VALUES IN ('abc')
;
CREATE FUNCTION gin_btree_consistent(internal, int2, anyelement, int4, internal, internal)
RETURNS bool
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
-- Test domains over domains
create domain vchar4 varchar(4)
;
ALTER FUNCTION gbtreekey4_out(gbtreekey4) PARALLEL SAFE
;
create trigger trigtest_a_row_tg after insert or update or delete on trigtest
for each row execute procedure trigtest()
;
CREATE UNIQUE INDEX ctoast_lock_idx ON ctoast (ctoast_lock_func(key))
;
DROP TRIGGER parent_update_trig ON range_parted
;
create table t2()
;
ALTER DATABASE sepgsql_test_regression SET search_path TO regtest_schema, public
;
alter table my_locks set (security_barrier=off)
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
create trigger upsert_after_trig after insert or update on upsert
  for each row execute procedure upsert_after_func()
;
create unique index hash_f8_index_3 on hash_f8_heap(random) where seqno > 1000
;
ANALYZE fkpart4.droppk2_d
;
create table listp_12_2 partition of listp_12 for values in(2)
;
CREATE INDEX ON jointest(id)
;
-- error
CREATE MATERIALIZED VIEW mvtest_mv_v (ii, jj) AS SELECT i, j FROM mvtest_v
;
ALTER TYPE "public"."insenum" ADD VALUE 'i10'
;
CREATE FUNCTION gin_compare_prefix_anyenum(anyenum, anyenum, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE TABLE "public"."date_tbl" (
    "f1" "date"
)
;
CREATE TABLE persons OF person_type
;
-- Fill a few pages
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize_multi VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
CREATE TABLE fewmore AS SELECT generate_series(1,3) AS data
;
create table lparted_nonullpart (a int, b char) partition by list (b)
;
-- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
DROP INDEX dummy_test_idx
;
-- ALTER TABLE SET ACCESS METHOD
CREATE TABLE heaptable USING heap AS
  SELECT a, repeat(a::text, 100) FROM generate_series(1,9) AS a
;
-- check handling of nulls in SRF results (bug #7808)

create type rngfunc2 as (a integer, b text)
;
CREATE TABLE "public"."pt_part1" (
    "a" integer,
    "b" "text"
)
;
-- column list for partitioned tables has to cover replica identities for
-- all child relations
CREATE TABLE testpub_tbl8 (a int, b text, c text) PARTITION BY HASH (a)
;
-- check restriction with default expressions
-- invalid use of column reference in default expressions
CREATE TABLE default_expr_column (id int DEFAULT (id))
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest1_p1 ALTER COLUMN f3 DROP NOT NULL
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table pc_list_parted detach partition pc_list_part_null
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = '1234F', detail = 'some detail info';
  exception
    when others then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] create table partitioned2
  partition of partitioned for values in ('(2,4)')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Test with row-level ON DELETE triggers
CREATE TRIGGER trig_row_before_delete
BEFORE DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  pg_terminate_backend(pid integer, timeout int8 DEFAULT 0)
  RETURNS boolean STRICT VOLATILE LANGUAGE INTERNAL AS 'pg_terminate_backend'
  PARALLEL SAFE
;
----------
[CREATE_TABLE] create table idxpart(drop_1 int, drop_2 int, col_keep int, drop_3 int) partition by range (col_keep)
;
----------
[CREATE_TABLE] -- Test that pending inserts are handled properly when needed
CREATE TABLE batch_table (a text, b int)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER FOREIGN TABLE ft1 DROP CONSTRAINT ft1_c9_check
;
----------
[CREATE_SCHEMA] -- error
CREATE SCHEMA newschema6 AUTHORIZATION "public"
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest22a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" / 2)) STORED
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- drop constraints recursively
ALTER TABLE fd_pt1 DROP CONSTRAINT fd_pt1chk1 CASCADE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29512 host=/tmp/2AUEITRTRR dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_lower(seg)
RETURNS float4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TRIGGER] DROP TRIGGER trigtest_before_stmt ON foreign_schema.foreign_table_1
;
----------
[DROP_TABLE] drop table capitals
;
----------
[DROP_CATALOG] DROP DATABASE foobar1
;
----------
[CREATE_TABLE] CREATE TABLE some_t (some_col boolean NOT NULL)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_foralltables FOR ALL TABLES
;
----------
[CREATE_TABLE] CREATE TABLE test30_1 PARTITION OF test30 FOR VALUES IN ('abc')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_btree_consistent(internal, int2, anyelement, int4, internal, internal)
RETURNS bool
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TYPE] -- Test domains over domains
create domain vchar4 varchar(4)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbtreekey4_out(gbtreekey4) PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trigtest_a_row_tg after insert or update or delete on trigtest
for each row execute procedure trigtest()
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ctoast_lock_idx ON ctoast (ctoast_lock_func(key))
;
----------
[DROP_TRIGGER] DROP TRIGGER parent_update_trig ON range_parted
;
----------
[CREATE_TABLE] create table t2()
;
----------
[ALTER_CATALOG] ALTER DATABASE sepgsql_test_regression SET search_path TO regtest_schema, public
;
----------
[ALTER_TABLE] alter table my_locks set (security_barrier=off)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger upsert_after_trig after insert or update on upsert
  for each row execute procedure upsert_after_func()
;
----------
[ADD_INDEX] create unique index hash_f8_index_3 on hash_f8_heap(random) where seqno > 1000
;
----------
[ADMIN_TABLE] ANALYZE fkpart4.droppk2_d
;
----------
[CREATE_TABLE] create table listp_12_2 partition of listp_12 for values in(2)
;
----------
[ADD_INDEX] CREATE INDEX ON jointest(id)
;
----------
[CREATE_VIEW(SELECT)] -- error
CREATE MATERIALIZED VIEW mvtest_mv_v (ii, jj) AS SELECT i, j FROM mvtest_v
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i10'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_anyenum(anyenum, anyenum, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."date_tbl" (
    "f1" "date"
)
;
----------
[CREATE_TABLE] CREATE TABLE persons OF person_type
;
----------
[BLOCK] -- Fill a few pages
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize_multi VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE fewmore AS SELECT generate_series(1,3) AS data
;
----------
[CREATE_TABLE] create table lparted_nonullpart (a int, b char) partition by list (b)
;
----------
[CREATE_VIEW] -- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
----------
[DROP_INDEX] DROP INDEX dummy_test_idx
;
----------
[CREATE_TABLE|SELECT] -- ALTER TABLE SET ACCESS METHOD
CREATE TABLE heaptable USING heap AS
  SELECT a, repeat(a::text, 100) FROM generate_series(1,9) AS a
;
----------
[CREATE_TYPE] -- check handling of nulls in SRF results (bug #7808)

create type rngfunc2 as (a integer, b text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pt_part1" (
    "a" integer,
    "b" "text"
)
;
----------
[CREATE_TABLE] -- column list for partitioned tables has to cover replica identities for
-- all child relations
CREATE TABLE testpub_tbl8 (a int, b text, c text) PARTITION BY HASH (a)
;
----------
[CREATE_TABLE] -- check restriction with default expressions
-- invalid use of column reference in default expressions
CREATE TABLE default_expr_column (id int DEFAULT (id))
;
