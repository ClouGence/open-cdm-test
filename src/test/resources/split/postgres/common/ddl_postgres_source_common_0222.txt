/*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
-- Simple querie
CREATE TABLE T (pk INT NOT NULL PRIMARY KEY)
;
--fail, named parameters are not unique
create function testpolym(a int, a int) returns int as $$ select 1;$$ language sql
;
CREATE TEMP TABLE pg_temp_3.pg_temp_29539_2 (tid pg_catalog.tid)
;
create table fkpart0.fk_part_56_5 partition of fkpart0.fk_part_56
    for values in (5)
;
create index on permtest_parent (left(c, 3))
;
ALTER TABLE IF EXISTS tt8 SET SCHEMA alter2
;
CREATE TABLE SYS_COL_CHECK_TBL (city text, state text, is_capital bool,
                  altitude int,
				  CHECK (NOT (is_capital AND ctid::text = 'sys_col_check_tbl')))
;
drop index comp_key_index
;
VACUUM (FULL) pg_catalog.pg_database
;
CREATE FUNCTION dummy_update_func() RETURNS trigger AS $$
BEGIN
  RAISE NOTICE 'dummy_update_func(%) called: action = %, old = %, new = %',
    TG_ARGV[0], TG_OP, OLD, NEW;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql
;
CREATE FUNCTION priv_testfunc1(int) RETURNS int AS 'select 2 * $1;' LANGUAGE sql
;
ANALYZE public.rtest_interface
;
ANALYZE pg_temp_26.pg_temp_28654_2
;
CREATE FUNCTION "public"."is_catalog_text_unique_index_oid"("oid") RETURNS boolean
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'is_catalog_text_unique_index_oid'
;
-- Test for consistent reporting of error context

create function fail() returns int language plpgsql as $$
begin
  return 1/0;
end
$$
;
drop type _comptype
;
alter table header_copytest add column c text
;
ALTER TABLE tbl1 ADD COLUMN id serial primary key
;
CREATE TABLE pagg_tab_ml_p3(b int, c text, a int) PARTITION BY RANGE (b)
;
ALTER INDEX pg_toast.pg_toast_30405_index RENAME TO reind_con_toast_idx
;
-- Should be possible to drop and recreate this extension
drop extension test_ext8
;
CREATE FUNCTION intset_union_elem(_int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP TYPE mytype CASCADE
;
CREATE INDEX part_parent_i ON stats_import.part_parent(i)
;
CREATE EXTENSION bool_plperlu CASCADE
;
DROP TABLE lr_test
;
-- tablespace should not change if no rewrite
ALTER TABLE testschema.test_default_tab ALTER id TYPE int
;
CREATE TABLE numerictmp (a numeric)
;
CREATE TABLE pk11 PARTITION OF pk1 FOR VALUES FROM (1) TO (50)
;
CREATE TEMP TABLE stud_emp_copy (LIKE stud_emp)
;
-- fail
CREATE COLLATION testy (locale = 'en_US.utf8', version = 'foo')
;
-- match failure

create function anycompatiblearray_anycompatiblemultirange_func(a anycompatiblearray, mr anycompatiblemultirange)
  returns anycompatible as 'select $1[1] + lower($2);' language sql
;
ALTER TABLE attmp ADD COLUMN v timestamp
;
alter table alttype_cluster add primary key (a)
;
CREATE FUNCTION handle_release_last()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE FUNCTION dump_test.event_trigger_func()
					   RETURNS event_trigger LANGUAGE plpgsql
					   AS $$ BEGIN RETURN; END;$$
;
do $$
declare x constant int[];
begin
  x[1] := 42;  -- fail
end$$
;
DROP FUNCTION update_checking(int)
;
DROP INDEX enumtest_hash
;
ALTER SEQUENCE sequence_test10 AS smallint MINVALUE -20000
;
CREATE TABLE fk2 (b int, a int)
;
-- ok

COMMENT ON TRANSFORM FOR hstore LANGUAGE plperl IS 'test'
;
ALTER TABLE pg_temp_4.pg_temp_37808_2 ADD COLUMN newdata pg_temp_4.pg_temp_37808
;
create or replace function retc(int) returns two_int8s language plpgsql as
$$ begin return row($1::int8, 1::int8); end $$
;
DROP TABLESPACE IF EXISTS split_ts_space_renamed
;
CREATE TABLE "public"."student" (
    "name" "text",
    "age" integer,
    "location" "point",
    "gpa" double precision
)
;
-- Exercise Memoize in binary mode with a large fixed width type and a
-- varlena type.
CREATE TABLE strtest (n name, t text)
;
CREATE FUNCTION do_analyze() RETURNS VOID VOLATILE LANGUAGE SQL
	AS 'ANALYZE pg_am'
;
-- test that ri_Check_Pk_Match() scans the correct partition for a deferred
-- ON DELETE/UPDATE NO ACTION constraint
CREATE SCHEMA fkpart10
  CREATE TABLE tbl1(f1 int PRIMARY KEY) PARTITION BY RANGE(f1)
  CREATE TABLE tbl1_p1 PARTITION OF tbl1 FOR VALUES FROM (minvalue) TO (1)
  CREATE TABLE tbl1_p2 PARTITION OF tbl1 FOR VALUES FROM (1) TO (maxvalue)
  CREATE TABLE tbl2(f1 int REFERENCES tbl1 DEFERRABLE INITIALLY DEFERRED)
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] /*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
----------
[CREATE_TABLE] -- Simple querie
CREATE TABLE T (pk INT NOT NULL PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] --fail, named parameters are not unique
create function testpolym(a int, a int) returns int as $$ select 1;$$ language sql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_29539_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table fkpart0.fk_part_56_5 partition of fkpart0.fk_part_56
    for values in (5)
;
----------
[ADD_INDEX] create index on permtest_parent (left(c, 3))
;
----------
[ALTER_TABLE] ALTER TABLE IF EXISTS tt8 SET SCHEMA alter2
;
----------
[CREATE_TABLE] CREATE TABLE SYS_COL_CHECK_TBL (city text, state text, is_capital bool,
                  altitude int,
				  CHECK (NOT (is_capital AND ctid::text = 'sys_col_check_tbl')))
;
----------
[DROP_INDEX] drop index comp_key_index
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_database
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dummy_update_func() RETURNS trigger AS $$
BEGIN
  RAISE NOTICE 'dummy_update_func(%) called: action = %, old = %, new = %',
    TG_ARGV[0], TG_OP, OLD, NEW;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION priv_testfunc1(int) RETURNS int AS 'select 2 * $1;' LANGUAGE sql
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_interface
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_26.pg_temp_28654_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."is_catalog_text_unique_index_oid"("oid") RETURNS boolean
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'is_catalog_text_unique_index_oid'
;
----------
[CREATE_PROG_OBJ] -- Test for consistent reporting of error context

create function fail() returns int language plpgsql as $$
begin
  return 1/0;
end
$$
;
----------
[DROP_TYPE] drop type _comptype
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table header_copytest add column c text
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tbl1 ADD COLUMN id serial primary key
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p3(b int, c text, a int) PARTITION BY RANGE (b)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30405_index RENAME TO reind_con_toast_idx
;
----------
[DROP_LIBRARY] -- Should be possible to drop and recreate this extension
drop extension test_ext8
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION intset_union_elem(_int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TYPE] DROP TYPE mytype CASCADE
;
----------
[ADD_INDEX] CREATE INDEX part_parent_i ON stats_import.part_parent(i)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION bool_plperlu CASCADE
;
----------
[DROP_TABLE] DROP TABLE lr_test
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- tablespace should not change if no rewrite
ALTER TABLE testschema.test_default_tab ALTER id TYPE int
;
----------
[CREATE_TABLE] CREATE TABLE numerictmp (a numeric)
;
----------
[CREATE_TABLE] CREATE TABLE pk11 PARTITION OF pk1 FOR VALUES FROM (1) TO (50)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE stud_emp_copy (LIKE stud_emp)
;
----------
[CREATE_POLICY] -- fail
CREATE COLLATION testy (locale = 'en_US.utf8', version = 'foo')
;
----------
[CREATE_PROG_OBJ] -- match failure

create function anycompatiblearray_anycompatiblemultirange_func(a anycompatiblearray, mr anycompatiblemultirange)
  returns anycompatible as 'select $1[1] + lower($2);' language sql
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN v timestamp
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table alttype_cluster add primary key (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_release_last()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dump_test.event_trigger_func()
					   RETURNS event_trigger LANGUAGE plpgsql
					   AS $$ BEGIN RETURN; END;$$
;
----------
[BLOCK] do $$
declare x constant int[];
begin
  x[1] := 42;  -- fail
end$$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION update_checking(int)
;
----------
[DROP_INDEX] DROP INDEX enumtest_hash
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test10 AS smallint MINVALUE -20000
;
----------
[CREATE_TABLE] CREATE TABLE fk2 (b int, a int)
;
----------
[COMMENT_TRANSFORM] -- ok

COMMENT ON TRANSFORM FOR hstore LANGUAGE plperl IS 'test'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_37808_2 ADD COLUMN newdata pg_temp_4.pg_temp_37808
;
----------
[CREATE_PROG_OBJ] create or replace function retc(int) returns two_int8s language plpgsql as
$$ begin return row($1::int8, 1::int8); end $$
;
----------
[DROP_TABLESPACE] DROP TABLESPACE IF EXISTS split_ts_space_renamed
;
----------
[CREATE_TABLE] CREATE TABLE "public"."student" (
    "name" "text",
    "age" integer,
    "location" "point",
    "gpa" double precision
)
;
----------
[CREATE_TABLE] -- Exercise Memoize in binary mode with a large fixed width type and a
-- varlena type.
CREATE TABLE strtest (n name, t text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION do_analyze() RETURNS VOID VOLATILE LANGUAGE SQL
	AS 'ANALYZE pg_am'
;
----------
[CREATE_SCHEMA] -- test that ri_Check_Pk_Match() scans the correct partition for a deferred
-- ON DELETE/UPDATE NO ACTION constraint
CREATE SCHEMA fkpart10
  CREATE TABLE tbl1(f1 int PRIMARY KEY) PARTITION BY RANGE(f1)
  CREATE TABLE tbl1_p1 PARTITION OF tbl1 FOR VALUES FROM (minvalue) TO (1)
  CREATE TABLE tbl1_p2 PARTITION OF tbl1 FOR VALUES FROM (1) TO (maxvalue)
  CREATE TABLE tbl2(f1 int REFERENCES tbl1 DEFERRABLE INITIALLY DEFERRED)
;
