DO $$BEGIN FOR i IN 1..320 LOOP CHECKPOINT; PERFORM pg_switch_wal(); END LOOP; END$$
;
CREATE TABLE selinto_schema.tbl_withdata1 (a)
  AS SELECT generate_series(1,3) WITH DATA
;
CREATE DATABASE foobar8 OWNER role_foobar TEMPLATE foobar2
;
ALTER TABLE persons ALTER COLUMN name TYPE varchar
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
CREATE FUNCTION gbt_int2_union(internal, internal)
RETURNS gbtreekey4
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER OPERATOR @=(real, boolean) SET (COMMUTATOR = ===)
;
alter table "catalog1".schema1."test" rename column a1 to a123456
;
--
-- Test proper snapshot handling in simple expressions
--

create temp table users(login text, id serial)
;
-- view with ORDER BY

CREATE TABLE base_tbl (a int, b int)
;
CREATE TRIGGER "tg_backlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."pline" FOR EACH ROW EXECUTE FUNCTION "public"."tg_backlink_a"('PL')
;
CREATE INDEX brin_test_b_idx ON brin_test USING brin (b) WITH (pages_per_range = 2)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_publication_namespace
;
DROP DOMAIN testns.priv_testdomain1
;
---------------------------
-- UPDATE with row movement
---------------------------

-- When a partitioned table receives an UPDATE to the partitioned key and the
-- new values no longer meet the partition's bound, the row must be moved to
-- the correct partition for the new partition key (if one exists). We must
-- also ensure that updatable views on partitioned tables properly enforce any
-- WITH CHECK OPTION that is defined. The situation with triggers in this case
-- also requires thorough testing as partition key updates causing row
-- movement convert UPDATEs into DELETE+INSERT.

CREATE TABLE range_parted (
	a text,
	b bigint,
	c numeric,
	d int,
	e varchar
) PARTITION BY RANGE (a, b)
;
CREATE TABLE t AS SELECT 42
;
CREATE OPERATOR <@ (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contained,
	COMMUTATOR = '@>',
	RESTRICT = _int_contained_sel,
	JOIN = _int_contained_joinsel
)
;
ANALYZE public.plt1_e_p2
;
CREATE PUBLICATION testpub_xxx WITH (publish_generated_columns)
;
CREATE DATABASE db_will_get_dropped
;
CREATE OR REPLACE FUNCTION rngfunc()
RETURNS TABLE(a int)
AS $$ SELECT a FROM generate_series(1,5) a(a) $$ LANGUAGE sql
;
create table d4_pid (pid int)
;
CREATE VIEW dummy_seclabel_view1 AS SELECT * FROM dummy_seclabel_tbl2
;
CREATE TABLE fail_part(LIKE hash_parted)
;
CREATE EVENT TRIGGER split_event_helper_ddl_end
    ON ddl_command_end
    WHEN TAG IN ('CREATE TABLE', 'ALTER TABLE')
    EXECUTE FUNCTION split_event_helper.capture_ddl_commands()
;
CREATE FUNCTION seg_out(seg)
RETURNS cstring
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TRIGGER accounts_check_update_renamed ON split_trigger_probe.accounts DEPENDS ON EXTENSION plpgsql
;
ALTER TABLE pg_toast.pg_toast_33576 RENAME TO reind_con_toast
;
CREATE TABLE IF NOT EXISTS test_tsvector(
	t text
)
;
CREATE TEMP TABLE pg_temp_11.pg_temp_41207_2 (tid pg_catalog.tid)
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=20208 host=/tmp/uqmrRM8pzR dbname=postgres' PUBLICATION mypub
;
VACUUM (VERBOSE, FREEZE) vac_horizon_floor_table
;
CREATE VIEW mysecview6 WITH (invalid_option)		
       AS SELECT * FROM tbl1 WHERE a < 100
;
ALTER TABLE ONLY "public"."prt2_l_p2" ATTACH PARTITION "public"."prt2_l_p2_p2" FOR VALUES IN ('0002', '0003')
;
CREATE TABLE "public"."rtest_admin" (
    "pname" "text",
    "sysname" "text"
)
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (on_error 'unsupported')
;
create table brtrigpartcon1 partition of brtrigpartcon for values in (1)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_database
;
DROP TABLE past_inh_child
;
create index testtable_apple_index on testtable_apple(logdate)
;
create temp table shipped (
    ttype       char(2),
    ordnum      int4,
    partnum     text,
    value       float8
)
;
CREATE RULE rule_3 AS
  ON DELETE
  TO like_datatype_table
  WHERE id < 100
  DO ALSO NOTHING
;
create index on idxpart1 using hash (a)
;
ANALYZE public.prt2_e_p3
;
--
-- Test PG_C_UTF8
--

CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C_UTF8')
;
-- Failures for non-BRIN index.
CREATE INDEX test1_a_btree ON test1 (a)
;
AlTER EXTENSION pg_stat_statements UPDATE TO '1.12'
;
CREATE TABLE ins_trunc (id serial PRIMARY KEY, id2 int)
;
create table mlparted3 partition of mlparted for values from (1, 20) to (1, 30)
;
------------------------------------------------------------------------------------------
[BLOCK] DO $$BEGIN FOR i IN 1..320 LOOP CHECKPOINT; PERFORM pg_switch_wal(); END LOOP; END$$
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE selinto_schema.tbl_withdata1 (a)
  AS SELECT generate_series(1,3) WITH DATA
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar8 OWNER role_foobar TEMPLATE foobar2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE persons ALTER COLUMN name TYPE varchar
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int2_union(internal, internal)
RETURNS gbtreekey4
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @=(real, boolean) SET (COMMUTATOR = ===)
;
----------
[RENAME_COLUMN] alter table "catalog1".schema1."test" rename column a1 to a123456
;
----------
[CREATE_TABLE] --
-- Test proper snapshot handling in simple expressions
--

create temp table users(login text, id serial)
;
----------
[CREATE_TABLE] -- view with ORDER BY

CREATE TABLE base_tbl (a int, b int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_backlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."pline" FOR EACH ROW EXECUTE FUNCTION "public"."tg_backlink_a"('PL')
;
----------
[ADD_INDEX] CREATE INDEX brin_test_b_idx ON brin_test USING brin (b) WITH (pages_per_range = 2)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_publication_namespace
;
----------
[DROP_TYPE] DROP DOMAIN testns.priv_testdomain1
;
----------
[CREATE_TABLE] ---------------------------
-- UPDATE with row movement
---------------------------

-- When a partitioned table receives an UPDATE to the partitioned key and the
-- new values no longer meet the partition's bound, the row must be moved to
-- the correct partition for the new partition key (if one exists). We must
-- also ensure that updatable views on partitioned tables properly enforce any
-- WITH CHECK OPTION that is defined. The situation with triggers in this case
-- also requires thorough testing as partition key updates causing row
-- movement convert UPDATEs into DELETE+INSERT.

CREATE TABLE range_parted (
	a text,
	b bigint,
	c numeric,
	d int,
	e varchar
) PARTITION BY RANGE (a, b)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE t AS SELECT 42
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <@ (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contained,
	COMMUTATOR = '@>',
	RESTRICT = _int_contained_sel,
	JOIN = _int_contained_joinsel
)
;
----------
[ADMIN_TABLE] ANALYZE public.plt1_e_p2
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_xxx WITH (publish_generated_columns)
;
----------
[CREATE_CATALOG] CREATE DATABASE db_will_get_dropped
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION rngfunc()
RETURNS TABLE(a int)
AS $$ SELECT a FROM generate_series(1,5) a(a) $$ LANGUAGE sql
;
----------
[CREATE_TABLE] create table d4_pid (pid int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW dummy_seclabel_view1 AS SELECT * FROM dummy_seclabel_tbl2
;
----------
[CREATE_TABLE] CREATE TABLE fail_part(LIKE hash_parted)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER split_event_helper_ddl_end
    ON ddl_command_end
    WHEN TAG IN ('CREATE TABLE', 'ALTER TABLE')
    EXECUTE FUNCTION split_event_helper.capture_ddl_commands()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_out(seg)
RETURNS cstring
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TRIGGER] ALTER TRIGGER accounts_check_update_renamed ON split_trigger_probe.accounts DEPENDS ON EXTENSION plpgsql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33576 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE IF NOT EXISTS test_tsvector(
	t text
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_41207_2 (tid pg_catalog.tid)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=20208 host=/tmp/uqmrRM8pzR dbname=postgres' PUBLICATION mypub
;
----------
[ADMIN_TABLE] VACUUM (VERBOSE, FREEZE) vac_horizon_floor_table
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mysecview6 WITH (invalid_option)		
       AS SELECT * FROM tbl1 WHERE a < 100
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_l_p2" ATTACH PARTITION "public"."prt2_l_p2_p2" FOR VALUES IN ('0002', '0003')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_admin" (
    "pname" "text",
    "sysname" "text"
)
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (on_error 'unsupported')
;
----------
[CREATE_TABLE] create table brtrigpartcon1 partition of brtrigpartcon for values in (1)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_database
;
----------
[DROP_TABLE] DROP TABLE past_inh_child
;
----------
[ADD_INDEX] create index testtable_apple_index on testtable_apple(logdate)
;
----------
[CREATE_TABLE] create temp table shipped (
    ttype       char(2),
    ordnum      int4,
    partnum     text,
    value       float8
)
;
----------
[CREATE_POLICY] CREATE RULE rule_3 AS
  ON DELETE
  TO like_datatype_table
  WHERE id < 100
  DO ALSO NOTHING
;
----------
[ADD_INDEX] create index on idxpart1 using hash (a)
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_e_p3
;
----------
[CREATE_POLICY] --
-- Test PG_C_UTF8
--

CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C_UTF8')
;
----------
[ADD_INDEX] -- Failures for non-BRIN index.
CREATE INDEX test1_a_btree ON test1 (a)
;
----------
[ALTER_LIBRARY] AlTER EXTENSION pg_stat_statements UPDATE TO '1.12'
;
----------
[CREATE_TABLE] CREATE TABLE ins_trunc (id serial PRIMARY KEY, id2 int)
;
----------
[CREATE_TABLE] create table mlparted3 partition of mlparted for values from (1, 20) to (1, 30)
;
