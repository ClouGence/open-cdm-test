-- failed (name conflict)
ALTER FUNCTION alt_func1(int) RENAME TO alt_func3
;
-- Make operator class for boxes using gist2
CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	OPERATOR 13	~,
	OPERATOR 14	@,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	-- don't need compress, decompress, or fetch functions
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
DROP TYPE person_type CASCADE
;
ALTER TABLE pg_temp_4.pg_temp_37940_2 ADD COLUMN newdata pg_temp_4.pg_temp_37940
;
DROP TABLE this_table_does_not_exist
;
DROP TABLE prt2_adv
;
-- bool datatype support new in 1.3.
CREATE FUNCTION gin_extract_value_bool(bool, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
-- overload
CREATE FUNCTION functest_B_2(bigint) RETURNS bool LANGUAGE 'sql'
       IMMUTABLE AS 'SELECT $1 > 0'
;
CREATE TABLE t3 (id int not null primary key, c text, b text, a int)
;
DROP TABLE notnull_tbl1, notnull_chld0
;
--
-- partitioned by multiple columns
--
CREATE TABLE prt1_m (a int, b int, c int) PARTITION BY RANGE(a, ((a + b)/2))
;
CREATE TABLE fprt1 (a int, b int, c varchar) PARTITION BY RANGE(a)
;
CREATE TABLE width_bucket_test (operand_num numeric, operand_f8 float8)
;
-- test default partition behavior for range
ALTER TABLE prt1 DETACH PARTITION prt1_p3
;
ALTER DATABASE contrib_regression SET session_authorization = regress_session_user
;
CREATE TABLE will_change_in_ts (a int, b text) TABLESPACE ts1
;
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 text, PRIMARY KEY(ptest1, ptest2) )
;
create table depth1(c text) inherits (depth0)
;
alter table parent_noinh_convalid validate constraint check_a_is_2
;
CREATE TABLE a (aa TEXT)
;
-- can't rename trigger on partition
create trigger c after insert on middle
for each row execute procedure f()
;
create operator class myint_ops
default for type myint using hash as
  operator    1   =  (myint, myint),
  function    1   myinthash(myint)
;
ALTER TABLE tab5_1 DROP COLUMN b
;
COMMENT ON FUNCTION seg_different(seg, seg) IS
'different'
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest2"
    ADD CONSTRAINT "gtest2_pkey" PRIMARY KEY ("a")
;
-- trigger repeated rewrites of a system catalog with a toast table,
-- that previously was buggy: 20180914021046.oi7dm4ra3ot2g2kt@alap3.anarazel.de
VACUUM FULL pg_proc
;
CREATE TABLE itest1 (a int generated by default as identity, b text)
;
CREATE TRIGGER accounts_update_transition
    AFTER UPDATE ON split_trigger_probe.accounts
    REFERENCING OLD TABLE AS old_rows NEW TABLE AS new_rows
    FOR EACH STATEMENT
    EXECUTE FUNCTION split_trigger_probe.audit_statement('after_update_statement')
;
-- only CREATE TYPE AS types may be used

CREATE TYPE tt_enum_type AS ENUM ('a')
;
ALTER FUNCTION gbt_float8_union(internal, internal) PARALLEL SAFE
;
CREATE FUNCTION pgstattuple(IN relname text,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuple'
LANGUAGE C STRICT PARALLEL SAFE
;
create domain testdomain1 as int
;
drop table pagg_test
;
drop function some_tab_stmt_trig_func()
;
ALTER INDEX dummy_test_idx SET (option_bool = 1)
;
ALTER TABLE FKTABLE ALTER COLUMN ftest1 TYPE bigint
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
DROP TABLE copy_rel_to CASCADE
;
ALTER TABLE ONLY "public"."mlparted1" DROP COLUMN "........pg.dropped.4........"
;
ALTER FUNCTION oid_dist(oid, oid) PARALLEL SAFE
;
CREATE TYPE issn13 (
	INPUT = issn13_in,
	OUTPUT = ean13_out,
	LIKE = pg_catalog.int8
)
;
alter table parted_conflict_test drop b, add b char
;
CREATE TABLE tab1_2 (a int PRIMARY KEY, c text DEFAULT 'sub2_tab1_2', b text)
;
CREATE INDEX "nummultirange_test_btree" ON "public"."nummultirange_test" USING "btree" ("nmr")
;
ALTER TABLE stats_plan_test ADD COLUMN x int
;
CREATE EVENT TRIGGER has_volatile_rewrite
                  ON table_rewrite
   EXECUTE PROCEDURE log_rewrite()
;
ALTER INDEX public.ths_mod_data_key ATTACH PARTITION public.ths_p2_mod_data_key
;
CREATE TABLE "public"."copy_tbl" (
    "x" integer,
    "y" "text",
    "z" integer,
    CONSTRAINT "copy_con" CHECK ((("x" > 3) AND ("y" <> 'check failed'::"text") AND ("x" < 7)))
)
;
create table list_part2 partition of list_part for values in (2)
;
CREATE table stats_test_tab1 as select generate_series(1,10) a
;
------------------------------------------------------------------------------------------
[RENAME_PROG_OBJ] -- failed (name conflict)
ALTER FUNCTION alt_func1(int) RENAME TO alt_func3
;
----------
[CREATE_PROG_OBJ] -- Make operator class for boxes using gist2
CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	OPERATOR 13	~,
	OPERATOR 14	@,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	-- don't need compress, decompress, or fetch functions
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
----------
[DROP_TYPE] DROP TYPE person_type CASCADE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_37940_2 ADD COLUMN newdata pg_temp_4.pg_temp_37940
;
----------
[DROP_TABLE] DROP TABLE this_table_does_not_exist
;
----------
[DROP_TABLE] DROP TABLE prt2_adv
;
----------
[CREATE_PROG_OBJ] -- bool datatype support new in 1.3.
CREATE FUNCTION gin_extract_value_bool(bool, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] -- overload
CREATE FUNCTION functest_B_2(bigint) RETURNS bool LANGUAGE 'sql'
       IMMUTABLE AS 'SELECT $1 > 0'
;
----------
[CREATE_TABLE] CREATE TABLE t3 (id int not null primary key, c text, b text, a int)
;
----------
[DROP_TABLE] DROP TABLE notnull_tbl1, notnull_chld0
;
----------
[CREATE_TABLE] --
-- partitioned by multiple columns
--
CREATE TABLE prt1_m (a int, b int, c int) PARTITION BY RANGE(a, ((a + b)/2))
;
----------
[CREATE_TABLE] CREATE TABLE fprt1 (a int, b int, c varchar) PARTITION BY RANGE(a)
;
----------
[CREATE_TABLE] CREATE TABLE width_bucket_test (operand_num numeric, operand_f8 float8)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- test default partition behavior for range
ALTER TABLE prt1 DETACH PARTITION prt1_p3
;
----------
[ALTER_CATALOG] ALTER DATABASE contrib_regression SET session_authorization = regress_session_user
;
----------
[CREATE_TABLE] CREATE TABLE will_change_in_ts (a int, b text) TABLESPACE ts1
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 text, PRIMARY KEY(ptest1, ptest2) )
;
----------
[CREATE_TABLE] create table depth1(c text) inherits (depth0)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] alter table parent_noinh_convalid validate constraint check_a_is_2
;
----------
[CREATE_TABLE] CREATE TABLE a (aa TEXT)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- can't rename trigger on partition
create trigger c after insert on middle
for each row execute procedure f()
;
----------
[CREATE_PROG_OBJ] create operator class myint_ops
default for type myint using hash as
  operator    1   =  (myint, myint),
  function    1   myinthash(myint)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tab5_1 DROP COLUMN b
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_different(seg, seg) IS
'different'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest2"
    ADD CONSTRAINT "gtest2_pkey" PRIMARY KEY ("a")
;
----------
[ADMIN_TABLE] -- trigger repeated rewrites of a system catalog with a toast table,
-- that previously was buggy: 20180914021046.oi7dm4ra3ot2g2kt@alap3.anarazel.de
VACUUM FULL pg_proc
;
----------
[CREATE_TABLE] CREATE TABLE itest1 (a int generated by default as identity, b text)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER accounts_update_transition
    AFTER UPDATE ON split_trigger_probe.accounts
    REFERENCING OLD TABLE AS old_rows NEW TABLE AS new_rows
    FOR EACH STATEMENT
    EXECUTE FUNCTION split_trigger_probe.audit_statement('after_update_statement')
;
----------
[CREATE_TYPE] -- only CREATE TYPE AS types may be used

CREATE TYPE tt_enum_type AS ENUM ('a')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float8_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgstattuple(IN relname text,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuple'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TYPE] create domain testdomain1 as int
;
----------
[DROP_TABLE] drop table pagg_test
;
----------
[DROP_PROG_OBJ] drop function some_tab_stmt_trig_func()
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_bool = 1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE FKTABLE ALTER COLUMN ftest1 TYPE bigint
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
----------
[DROP_TABLE] DROP TABLE copy_rel_to CASCADE
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted1" DROP COLUMN "........pg.dropped.4........"
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION oid_dist(oid, oid) PARALLEL SAFE
;
----------
[CREATE_TYPE] CREATE TYPE issn13 (
	INPUT = issn13_in,
	OUTPUT = ean13_out,
	LIKE = pg_catalog.int8
)
;
----------
[ALTER_TABLE|DROP_COLUMN|ADD_COLUMN] alter table parted_conflict_test drop b, add b char
;
----------
[CREATE_TABLE] CREATE TABLE tab1_2 (a int PRIMARY KEY, c text DEFAULT 'sub2_tab1_2', b text)
;
----------
[ADD_INDEX] CREATE INDEX "nummultirange_test_btree" ON "public"."nummultirange_test" USING "btree" ("nmr")
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE stats_plan_test ADD COLUMN x int
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER has_volatile_rewrite
                  ON table_rewrite
   EXECUTE PROCEDURE log_rewrite()
;
----------
[ALTER_INDEX] ALTER INDEX public.ths_mod_data_key ATTACH PARTITION public.ths_p2_mod_data_key
;
----------
[CREATE_TABLE] CREATE TABLE "public"."copy_tbl" (
    "x" integer,
    "y" "text",
    "z" integer,
    CONSTRAINT "copy_con" CHECK ((("x" > 3) AND ("y" <> 'check failed'::"text") AND ("x" < 7)))
)
;
----------
[CREATE_TABLE] create table list_part2 partition of list_part for values in (2)
;
----------
[CREATE_TABLE|SELECT] CREATE table stats_test_tab1 as select generate_series(1,10) a
;
