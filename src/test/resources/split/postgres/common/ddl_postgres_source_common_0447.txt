-- check that the table being attached has all constraints of the parent
CREATE TABLE fail_part (
	b char(2) COLLATE "C",
	a int NOT NULL
)
;
DROP TABLE deferred_excl
;
ALTER FUNCTION functest_E_1(int) NOT LEAKPROOF
;
DROP TABLE corrupt1
;
create temp table t_append (a int not null, b int)
;
-- ok
CALL ptest1('xy' || 'zzy')
;
CREATE TABLE bar (a int REFERENCES foo_range_parted1(a))
;
-- error
CREATE SCHEMA newschemax AUTHORIZATION PUBLIC
;
CREATE TABLE pagg_tab_p1 PARTITION OF pagg_tab FOR VALUES IN ('0000', '0001', '0002', '0003', '0004')
;
-- succeed unique index over nonnullable cols
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_keyab_key
;
CREATE TEMP TABLE test_stats_temp AS SELECT 17
;
-- nor used in predicates
CREATE INDEX ON syscol_table (a) WHERE ctid >= '(1000,0)'
;
CREATE PUBLICATION testpub_ftbl FOR TABLE ft1
;
ALTER SUBSCRIPTION regress_testsub CONNECTION 'foobar'
;
DROP FOREIGN TABLE ft_part1, ft_part2
;
CREATE FUNCTION gin_extract_value_name(name, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ANALYZE public.tt4
;
COMMENT ON TYPE dump_test.undefined
					   IS 'comment on undefined type'
;
CREATE FUNCTION page_checksum(IN page bytea, IN blkno int8)
RETURNS smallint
AS '$libdir/pageinspect', 'page_checksum_1_9'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE SUBSCRIPTION testsub CONNECTION $$port=14073 host=/tmp/cxWTX1q2yg dbname='d1'$$ PUBLICATION testpub WITH (create_slot=false, slot_name='testslot')
;
-- fail, not a candidate key, nullable column
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_nonkey
;
CREATE TABLE test_float (data float)
;
-- Test ON TRUNCATE triggers

CREATE TABLE trunc_trigger_test (f1 int, f2 text, f3 text)
;
ALTER TABLE regress_constr_partitioned DETACH PARTITION regress_constr_partition1
;
ANALYZE public.test_tsquery
;
--
-- VACUUM
--

CREATE TABLE vactst (i INT)
;
CREATE FUNCTION inj_io_reopen_detach()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
CREATE POLICY pp1r ON part_document AS RESTRICTIVE TO regress_rls_dave
    USING (cid < 55)
;
CREATE TABLE "generated_stored_tests"."gtest23p" (
    "x" integer,
    "y" integer GENERATED ALWAYS AS (("x" * 2)) STORED NOT NULL
)
;
ALTER TABLE "public"."itest1" ALTER COLUMN "a" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."itest1_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
ALTER TABLE ONLY "public"."depth2" INHERIT "public"."depth1"
;
drop function trigger_nothing()
;
CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub1 WITH (enabled = false)
;
-- Even with partitions, relhassubclass should not be set if a partitioned
-- index is created only on the parent.
create index idxpart_idx on only idxpart(a)
;
-- duplicate generated
CREATE TABLE gtest_err_1 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED GENERATED ALWAYS AS (a * 3) STORED)
;
CREATE TEMP TABLE gpolygon_tbl AS
    SELECT polygon(home_base) AS f1 FROM slow_emp4000
;
-- cleanup
DROP TRIGGER trig_row_before ON rem1
;
CREATE TEMP TABLE pg_temp_32.pg_temp_28710_2 (tid pg_catalog.tid)
;
-- clean up
drop table capitals
;
CREATE TABLE "pubme"."t0" (
    "c" integer,
    "d" integer
)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "url_path" WITH "simple"
;
CREATE TABLE "public"."array_index_op_test" (
    "seqno" integer,
    "i" integer[],
    "t" "text"[]
)
;
COMMENT ON COLUMN "public"."comment_test"."id" IS 'Column ''id'' on comment_test'
;
CREATE VIEW enabled_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS role_name
    FROM pg_authid a
    WHERE pg_has_role(a.oid, 'USAGE')
;
CREATE TABLE "public"."persons2" (
    "id" integer NOT NULL,
    "name" "text"
)
;
DROP TABLE update_test
;
alter index idxpart2_a_idx attach partition idxpart21_a_idx
;
-- proximity routines

CREATE FUNCTION cube_distance(cube, cube)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create table inh_p3(f2 int)
;
drop rule qqq on copydml_test
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- check that the table being attached has all constraints of the parent
CREATE TABLE fail_part (
	b char(2) COLLATE "C",
	a int NOT NULL
)
;
----------
[DROP_TABLE] DROP TABLE deferred_excl
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_E_1(int) NOT LEAKPROOF
;
----------
[DROP_TABLE] DROP TABLE corrupt1
;
----------
[CREATE_TABLE] create temp table t_append (a int not null, b int)
;
----------
[CALL_PROG_OBJ] -- ok
CALL ptest1('xy' || 'zzy')
;
----------
[CREATE_TABLE] CREATE TABLE bar (a int REFERENCES foo_range_parted1(a))
;
----------
[CREATE_SCHEMA] -- error
CREATE SCHEMA newschemax AUTHORIZATION PUBLIC
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_p1 PARTITION OF pagg_tab FOR VALUES IN ('0000', '0001', '0002', '0003', '0004')
;
----------
[ALTER_TABLE] -- succeed unique index over nonnullable cols
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_keyab_key
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE test_stats_temp AS SELECT 17
;
----------
[ADD_INDEX] -- nor used in predicates
CREATE INDEX ON syscol_table (a) WHERE ctid >= '(1000,0)'
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_ftbl FOR TABLE ft1
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub CONNECTION 'foobar'
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ft_part1, ft_part2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_name(name, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] ANALYZE public.tt4
;
----------
[COMMENT_TYPE] COMMENT ON TYPE dump_test.undefined
					   IS 'comment on undefined type'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION page_checksum(IN page bytea, IN blkno int8)
RETURNS smallint
AS '$libdir/pageinspect', 'page_checksum_1_9'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub CONNECTION $$port=14073 host=/tmp/cxWTX1q2yg dbname='d1'$$ PUBLICATION testpub WITH (create_slot=false, slot_name='testslot')
;
----------
[ALTER_TABLE] -- fail, not a candidate key, nullable column
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_nonkey
;
----------
[CREATE_TABLE] CREATE TABLE test_float (data float)
;
----------
[CREATE_TABLE] -- Test ON TRUNCATE triggers

CREATE TABLE trunc_trigger_test (f1 int, f2 text, f3 text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE regress_constr_partitioned DETACH PARTITION regress_constr_partition1
;
----------
[ADMIN_TABLE] ANALYZE public.test_tsquery
;
----------
[CREATE_TABLE] --
-- VACUUM
--

CREATE TABLE vactst (i INT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION inj_io_reopen_detach()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_POLICY] CREATE POLICY pp1r ON part_document AS RESTRICTIVE TO regress_rls_dave
    USING (cid < 55)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest23p" (
    "x" integer,
    "y" integer GENERATED ALWAYS AS (("x" * 2)) STORED NOT NULL
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest1" ALTER COLUMN "a" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."itest1_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."depth2" INHERIT "public"."depth1"
;
----------
[DROP_PROG_OBJ] drop function trigger_nothing()
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub1 WITH (enabled = false)
;
----------
[ADD_INDEX] -- Even with partitions, relhassubclass should not be set if a partitioned
-- index is created only on the parent.
create index idxpart_idx on only idxpart(a)
;
----------
[CREATE_TABLE] -- duplicate generated
CREATE TABLE gtest_err_1 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED GENERATED ALWAYS AS (a * 3) STORED)
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE gpolygon_tbl AS
    SELECT polygon(home_base) AS f1 FROM slow_emp4000
;
----------
[DROP_TRIGGER] -- cleanup
DROP TRIGGER trig_row_before ON rem1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_32.pg_temp_28710_2 (tid pg_catalog.tid)
;
----------
[DROP_TABLE] -- clean up
drop table capitals
;
----------
[CREATE_TABLE] CREATE TABLE "pubme"."t0" (
    "c" integer,
    "d" integer
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "url_path" WITH "simple"
;
----------
[CREATE_TABLE] CREATE TABLE "public"."array_index_op_test" (
    "seqno" integer,
    "i" integer[],
    "t" "text"[]
)
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN "public"."comment_test"."id" IS 'Column ''id'' on comment_test'
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW enabled_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS role_name
    FROM pg_authid a
    WHERE pg_has_role(a.oid, 'USAGE')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."persons2" (
    "id" integer NOT NULL,
    "name" "text"
)
;
----------
[DROP_TABLE] DROP TABLE update_test
;
----------
[ALTER_INDEX] alter index idxpart2_a_idx attach partition idxpart21_a_idx
;
----------
[CREATE_PROG_OBJ] -- proximity routines

CREATE FUNCTION cube_distance(cube, cube)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table inh_p3(f2 int)
;
----------
[DROP_POLICY] drop rule qqq on copydml_test
;
