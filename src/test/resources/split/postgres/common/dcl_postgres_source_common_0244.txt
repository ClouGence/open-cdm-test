ALTER TABLE public.mlparted12 OWNER TO zyc
;
ALTER TABLE public.prt1_l_p3_p2 OWNER TO zyc
;
LOCK TABLE pg_catalog.pg_sequence IN ACCESS SHARE MODE
;
EXECUTE dumpFunc('16735')
;
ALTER VIEW public.xmlview1 OWNER TO zyc
;
SET search_path = TESTLIBPQ2
;
ALTER SUBSCRIPTION "regress_sub4" ENABLE
;
-- used in prepare statements
PREPARE pp AS
SELECT  xmltable.*
   FROM (SELECT data FROM xmldata) x,
        LATERAL XMLTABLE('/ROWS/ROW'
                         PASSING data
                         COLUMNS id int PATH '@id',
                                  _id FOR ORDINALITY,
                                  country_name text PATH 'COUNTRY_NAME' NOT NULL,
                                  country_id text PATH 'COUNTRY_ID',
                                  region_id int PATH 'REGION_ID',
                                  size float PATH 'SIZE',
                                  unit text PATH 'SIZE/@unit',
                                  premier_name text PATH 'PREMIER_NAME' DEFAULT 'not specified')
;
COPY generated_stored_tests.gtest3a (a) TO stdout
;
EXECUTE dumpFunc('35769')
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE public.mlparted12 OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.prt1_l_p3_p2 OWNER TO zyc
;
----------
[SESSION_LOCK] LOCK TABLE pg_catalog.pg_sequence IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpFunc('16735')
;
----------
[TRANSFER_PRIVILEGE] ALTER VIEW public.xmlview1 OWNER TO zyc
;
----------
[SESSION_SETTING_WRITE] SET search_path = TESTLIBPQ2
;
----------
[ADMIN_PUB_SUB] ALTER SUBSCRIPTION "regress_sub4" ENABLE
;
----------
[UNSAFE] -- used in prepare statements
PREPARE pp AS
SELECT  xmltable.*
   FROM (SELECT data FROM xmldata) x,
        LATERAL XMLTABLE('/ROWS/ROW'
                         PASSING data
                         COLUMNS id int PATH '@id',
                                  _id FOR ORDINALITY,
                                  country_name text PATH 'COUNTRY_NAME' NOT NULL,
                                  country_id text PATH 'COUNTRY_ID',
                                  region_id int PATH 'REGION_ID',
                                  size float PATH 'SIZE',
                                  unit text PATH 'SIZE/@unit',
                                  premier_name text PATH 'PREMIER_NAME' DEFAULT 'not specified')
;
----------
[DATA_EXPORT] COPY generated_stored_tests.gtest3a (a) TO stdout
;
----------
[UNSAFE] EXECUTE dumpFunc('35769')
;
