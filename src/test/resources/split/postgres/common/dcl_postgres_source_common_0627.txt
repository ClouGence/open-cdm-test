ABORT WORK
;
-- error
SET password_encryption = true
;
GRANT TEMP ON DATABASE isolation_regression TO regress_temp_grantee
;
-- yes

REVOKE EXECUTE ON FUNCTION pg_get_wal_record_info(pg_lsn)
  FROM regress_pg_walinspect
;
-- should pass
ROLLBACK
;
GRANT EXECUTE ON FUNCTION pg_visibility(regclass, bigint) TO pg_stat_scan_tables
;
ALTER SERVER loopback2 OPTIONS (SET application_name 'fdw_wrong')
;
EXECUTE dumpFunc('36316')
;
EXECUTE dumpTableAttach('40242')
;
LOCK TABLE "public"."gtest22a" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[TRANSACTION] ABORT WORK
;
----------
[SESSION_SETTING_WRITE] -- error
SET password_encryption = true
;
----------
[GRANT] GRANT TEMP ON DATABASE isolation_regression TO regress_temp_grantee
;
----------
[REVOKE] -- yes

REVOKE EXECUTE ON FUNCTION pg_get_wal_record_info(pg_lsn)
  FROM regress_pg_walinspect
;
----------
[TRANSACTION] -- should pass
ROLLBACK
;
----------
[GRANT] GRANT EXECUTE ON FUNCTION pg_visibility(regclass, bigint) TO pg_stat_scan_tables
;
----------
[SYSTEM_SETTING_WRITE] ALTER SERVER loopback2 OPTIONS (SET application_name 'fdw_wrong')
;
----------
[UNSAFE] EXECUTE dumpFunc('36316')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('40242')
;
----------
[SESSION_LOCK] LOCK TABLE "public"."gtest22a" IN ACCESS SHARE MODE
;
