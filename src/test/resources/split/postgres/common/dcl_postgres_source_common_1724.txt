-- Should not leak

-- Create plain inheritance parent table with no access permissions
RESET SESSION AUTHORIZATION
;
COPY public.itest17 (a, b, c) TO stdout
;
EXECUTE dumpTableAttach('28918')
;
EXECUTE dumpFunc('39718')
;
EXECUTE dumpFunc('16683')
;
-- test for column lists
SET client_min_messages = 'ERROR'
;
LOCK TABLE public.depth2 IN ACCESS SHARE MODE
;
ALTER TYPE public.rngfunc_rescan_t OWNER TO zyc
;
ALTER FUNCTION "public"."autoinc"() OWNER TO zyc
;
SET LOCAL work_mem FROM CURRENT
;
------------------------------------------------------------------------------------------
[SWITCH_USER] -- Should not leak

-- Create plain inheritance parent table with no access permissions
RESET SESSION AUTHORIZATION
;
----------
[DATA_EXPORT] COPY public.itest17 (a, b, c) TO stdout
;
----------
[UNSAFE] EXECUTE dumpTableAttach('28918')
;
----------
[UNSAFE] EXECUTE dumpFunc('39718')
;
----------
[UNSAFE] EXECUTE dumpFunc('16683')
;
----------
[SESSION_SETTING_WRITE] -- test for column lists
SET client_min_messages = 'ERROR'
;
----------
[SESSION_LOCK] LOCK TABLE public.depth2 IN ACCESS SHARE MODE
;
----------
[TRANSFER_PRIVILEGE] ALTER TYPE public.rngfunc_rescan_t OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."autoinc"() OWNER TO zyc
;
----------
[SESSION_SETTING_WRITE] SET LOCAL work_mem FROM CURRENT
;
