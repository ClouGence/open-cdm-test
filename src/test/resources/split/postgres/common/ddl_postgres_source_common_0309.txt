CREATE RULE "log_shoelace" AS
    ON UPDATE TO "public"."shoelace_data"
   WHERE ("new"."sl_avail" <> "old"."sl_avail") DO  INSERT INTO "public"."shoelace_log" ("sl_name", "sl_avail", "log_who", "log_when")
  VALUES ("new"."sl_name", "new"."sl_avail", 'Al Bundy'::"name", '1970-01-01 00:00:00'::timestamp without time zone)
;
-- fail

-- empty tablespace locations are not usually allowed
CREATE TABLESPACE regress_tblspace LOCATION ''
;
CREATE TABLE onek_with_null AS SELECT unique1, unique2 FROM onek
;
DROP TABLE reind_fdw_parent
;
TRUNCATE TABLE ONLY public.tplain
;
-- integer overflow error
VACUUM (BUFFER_USAGE_LIMIT 10000000000) vac_option_tab
;
alter type insenum add value 'i6' before 'L2'
;
drop trigger child2_update_trig on child2
;
CREATE FUNCTION isngt(issn, ean13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
alter table a_star set (parallel_workers = 0)
;
CREATE FUNCTION bt_page_items(IN relname text, IN blkno int4,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
create function polyf(x anyrange) returns anyarray as $$
  select array[lower(x), upper(x)]
$$ language sql
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_proc
;
alter table idxpart0 add primary key(i)
;
CREATE TABLE concur_reindex_tab3 (c1 int, c2 int4range, EXCLUDE USING gist (c2 WITH &&))
;
create index test_partition_hash_idx on test_partition using hash (a)
;
TRUNCATE TABLE toasttest
;
alter table p1_c1 add constraint inh_check_constraint2 check (f1 < 10)
;
ALTER FUNCTION gbt_macad8_fetch(internal) PARALLEL SAFE
;
-- Tests with various custom types

CREATE EXTENSION test_custom_types
;
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'unclassified'
;
-- fail
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'secret'
;
-- check that AlternativeSubPlan within a pruning expression gets cleaned up

create table asptab (id int primary key) partition by range (id)
;
CREATE TABLE fk12 PARTITION OF fk1 FOR VALUES FROM (10) TO (100)
;
ALTER TABLE prt1 ATTACH PARTITION prt1_p3 DEFAULT
;
CREATE FUNCTION svals(hstore)
RETURNS setof text
AS '$libdir/hstore','hstore_svals'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FUNCTION test_encoding(encoding text, description text, input bytea)
RETURNS VOID LANGUAGE plpgsql AS
$$
DECLARE
	prefix text;
	len int;
	wchars int[];
	round_trip bytea;
	result text;
BEGIN
	prefix := rpad(encoding || ' ' || description || ':', 28);

	-- XXX could also test validation, length functions and include client
	-- only encodings with these test cases

	IF test_valid_server_encoding(encoding) THEN
		wchars := test_text_to_wchars(encoding, test_bytea_to_text(input));
		round_trip = test_text_to_bytea(test_wchars_to_text(encoding, wchars));
		if input = round_trip then
			result := 'OK';
		elsif length(input) > length(round_trip) and round_trip = substr(input, 1, length(round_trip)) then
			result := 'truncated';
		else
			result := 'failed';
		end if;
		RAISE NOTICE '% % -> % -> % = %', prefix, input, wchars, round_trip, result;
	END IF;
END;
$$
;
/*
 * 24.13
 * USER_MAPPING_OPTIONS view
 */
CREATE VIEW user_mapping_options AS
    SELECT authorization_identifier,
           foreign_server_catalog,
           foreign_server_name,
           CAST(opts.option_name AS sql_identifier) AS option_name,
           CAST(CASE WHEN (umuser <> 0 AND authorization_identifier = current_user)
                       OR (umuser = 0 AND pg_has_role(srvowner, 'USAGE'))
                       OR (SELECT rolsuper FROM pg_authid WHERE rolname = current_user)
                     THEN opts.option_value
                     ELSE NULL END AS character_data) AS option_value
    FROM _pg_user_mappings um,
         pg_options_to_table(um.umoptions) opts
;
DROP PUBLICATION dummy_pub
;
create table range_parted2 partition of range_parted for values from (10,10) to (20,20)
;
TRUNCATE public.tru_pk_table CONTINUE IDENTITY CASCADE
;
create type jpop2 as (a int, b json, c int, d int)
;
--
-- Also create some non-built-in range types.
--

create type float8range as range (subtype = float8, subtype_diff = float8mi)
;
create or replace function dump_update_new() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, new table = %', TG_NAME,
                 (select string_agg(new_table::text, ', ' order by a) from new_table);
    return null;
  end;
$$
;
CREATE OPERATOR CLASS hash_hstore_ops
DEFAULT FOR TYPE hstore USING hash
AS
	OPERATOR	1	= ,
	FUNCTION	1	hstore_hash(hstore)
;
ALTER TABLE test_replica_identity2 REPLICA IDENTITY USING INDEX test_replica_identity2_id_key
;
--
-- Test Append's fractional paths
--

CREATE INDEX pht1_c_idx ON pht1(c)
;
CREATE TABLE expr_key (x numeric, t text)
;
create table s5.p2_2 partition of s5.p2 for values in (4, 5, 6)
;
VACUUM (ANALYZE) pg_catalog.pg_namespace
;
CREATE MATERIALIZED VIEW "public"."mvtest_bb" AS
 SELECT "grandtot"
   FROM "public"."mvtest_tvvmv"
  WITH NO DATA
;
CREATE TABLE "public"."gtest22a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" / 2)) STORED
)
;
create table trigtest (
  a integer,
  b bool default true not null,
  c text default 'xyzzy' not null)
;
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS '...invalid label...'
;
CREATE TEMP TABLE attmp (attmptable int)
;
-- can't change to a different kind:
CREATE OR REPLACE AGGREGATE myavg (order by numeric)
(
	stype = numeric,
	sfunc = numeric_add
)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
CREATE VIEW tenk1_vw_sec WITH (security_barrier) AS SELECT * FROM tenk1
;
--
-- Hot Standby tests
--
-- hs_primary_setup.sql
--

drop table if exists hs1
;
CREATE OPERATOR CLASS gist_ltree_ops
    DEFAULT FOR TYPE ltree USING gist AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	OPERATOR	10	@> ,
	OPERATOR	11	<@ ,
	OPERATOR	12	~ (ltree, lquery) ,
	OPERATOR	13	~ (lquery, ltree) ,
	OPERATOR	14	@ (ltree, ltxtquery) ,
	OPERATOR	15	@ (ltxtquery, ltree) ,
	OPERATOR	16	? (ltree, _lquery) ,
	OPERATOR	17	? (_lquery, ltree) ,
	FUNCTION	1	ltree_consistent (internal, ltree, int2, oid, internal),
	FUNCTION	2	ltree_union (internal, internal),
	FUNCTION	3	ltree_compress (internal),
	FUNCTION	4	ltree_decompress (internal),
	FUNCTION	5	ltree_penalty (internal, internal, internal),
	FUNCTION	6	ltree_picksplit (internal, internal),
	FUNCTION	7	ltree_same (ltree_gist, ltree_gist, internal),
	STORAGE		ltree_gist
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] CREATE RULE "log_shoelace" AS
    ON UPDATE TO "public"."shoelace_data"
   WHERE ("new"."sl_avail" <> "old"."sl_avail") DO  INSERT INTO "public"."shoelace_log" ("sl_name", "sl_avail", "log_who", "log_when")
  VALUES ("new"."sl_name", "new"."sl_avail", 'Al Bundy'::"name", '1970-01-01 00:00:00'::timestamp without time zone)
;
----------
[CREATE_TABLESPACE] -- fail

-- empty tablespace locations are not usually allowed
CREATE TABLESPACE regress_tblspace LOCATION ''
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE onek_with_null AS SELECT unique1, unique2 FROM onek
;
----------
[DROP_TABLE] DROP TABLE reind_fdw_parent
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE ONLY public.tplain
;
----------
[ADMIN_TABLE] -- integer overflow error
VACUUM (BUFFER_USAGE_LIMIT 10000000000) vac_option_tab
;
----------
[ALTER_TYPE] alter type insenum add value 'i6' before 'L2'
;
----------
[DROP_TRIGGER] drop trigger child2_update_trig on child2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(issn, ean13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE] alter table a_star set (parallel_workers = 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_page_items(IN relname text, IN blkno int4,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function polyf(x anyrange) returns anyarray as $$
  select array[lower(x), upper(x)]
$$ language sql
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_proc
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart0 add primary key(i)
;
----------
[CREATE_TABLE] CREATE TABLE concur_reindex_tab3 (c1 int, c2 int4range, EXCLUDE USING gist (c2 WITH &&))
;
----------
[ADD_INDEX] create index test_partition_hash_idx on test_partition using hash (a)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE toasttest
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1_c1 add constraint inh_check_constraint2 check (f1 < 10)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad8_fetch(internal) PARALLEL SAFE
;
----------
[CREATE_LIBRARY] -- Tests with various custom types

CREATE EXTENSION test_custom_types
;
----------
[SECURITY_LABEL] SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'unclassified'
;
----------
[SECURITY_LABEL] -- fail
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'secret'
;
----------
[CREATE_TABLE] -- check that AlternativeSubPlan within a pruning expression gets cleaned up

create table asptab (id int primary key) partition by range (id)
;
----------
[CREATE_TABLE] CREATE TABLE fk12 PARTITION OF fk1 FOR VALUES FROM (10) TO (100)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt1 ATTACH PARTITION prt1_p3 DEFAULT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION svals(hstore)
RETURNS setof text
AS '$libdir/hstore','hstore_svals'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_encoding(encoding text, description text, input bytea)
RETURNS VOID LANGUAGE plpgsql AS
$$
DECLARE
	prefix text;
	len int;
	wchars int[];
	round_trip bytea;
	result text;
BEGIN
	prefix := rpad(encoding || ' ' || description || ':', 28);

	-- XXX could also test validation, length functions and include client
	-- only encodings with these test cases

	IF test_valid_server_encoding(encoding) THEN
		wchars := test_text_to_wchars(encoding, test_bytea_to_text(input));
		round_trip = test_text_to_bytea(test_wchars_to_text(encoding, wchars));
		if input = round_trip then
			result := 'OK';
		elsif length(input) > length(round_trip) and round_trip = substr(input, 1, length(round_trip)) then
			result := 'truncated';
		else
			result := 'failed';
		end if;
		RAISE NOTICE '% % -> % -> % = %', prefix, input, wchars, round_trip, result;
	END IF;
END;
$$
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 24.13
 * USER_MAPPING_OPTIONS view
 */
CREATE VIEW user_mapping_options AS
    SELECT authorization_identifier,
           foreign_server_catalog,
           foreign_server_name,
           CAST(opts.option_name AS sql_identifier) AS option_name,
           CAST(CASE WHEN (umuser <> 0 AND authorization_identifier = current_user)
                       OR (umuser = 0 AND pg_has_role(srvowner, 'USAGE'))
                       OR (SELECT rolsuper FROM pg_authid WHERE rolname = current_user)
                     THEN opts.option_value
                     ELSE NULL END AS character_data) AS option_value
    FROM _pg_user_mappings um,
         pg_options_to_table(um.umoptions) opts
;
----------
[DROP_PUB_SUB] DROP PUBLICATION dummy_pub
;
----------
[CREATE_TABLE] create table range_parted2 partition of range_parted for values from (10,10) to (20,20)
;
----------
[TRUNCATE_TABLE] TRUNCATE public.tru_pk_table CONTINUE IDENTITY CASCADE
;
----------
[CREATE_TYPE] create type jpop2 as (a int, b json, c int, d int)
;
----------
[CREATE_TYPE] --
-- Also create some non-built-in range types.
--

create type float8range as range (subtype = float8, subtype_diff = float8mi)
;
----------
[CREATE_PROG_OBJ] create or replace function dump_update_new() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, new table = %', TG_NAME,
                 (select string_agg(new_table::text, ', ' order by a) from new_table);
    return null;
  end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS hash_hstore_ops
DEFAULT FOR TYPE hstore USING hash
AS
	OPERATOR	1	= ,
	FUNCTION	1	hstore_hash(hstore)
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity2 REPLICA IDENTITY USING INDEX test_replica_identity2_id_key
;
----------
[ADD_INDEX] --
-- Test Append's fractional paths
--

CREATE INDEX pht1_c_idx ON pht1(c)
;
----------
[CREATE_TABLE] CREATE TABLE expr_key (x numeric, t text)
;
----------
[CREATE_TABLE] create table s5.p2_2 partition of s5.p2 for values in (4, 5, 6)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_namespace
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_bb" AS
 SELECT "grandtot"
   FROM "public"."mvtest_tvvmv"
  WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest22a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" / 2)) STORED
)
;
----------
[CREATE_TABLE] create table trigtest (
  a integer,
  b bool default true not null,
  c text default 'xyzzy' not null)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS '...invalid label...'
;
----------
[CREATE_TABLE] CREATE TEMP TABLE attmp (attmptable int)
;
----------
[CREATE_PROG_OBJ] -- can't change to a different kind:
CREATE OR REPLACE AGGREGATE myavg (order by numeric)
(
	stype = numeric,
	sfunc = numeric_add
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW tenk1_vw_sec WITH (security_barrier) AS SELECT * FROM tenk1
;
----------
[DROP_TABLE] --
-- Hot Standby tests
--
-- hs_primary_setup.sql
--

drop table if exists hs1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_ltree_ops
    DEFAULT FOR TYPE ltree USING gist AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	OPERATOR	10	@> ,
	OPERATOR	11	<@ ,
	OPERATOR	12	~ (ltree, lquery) ,
	OPERATOR	13	~ (lquery, ltree) ,
	OPERATOR	14	@ (ltree, ltxtquery) ,
	OPERATOR	15	@ (ltxtquery, ltree) ,
	OPERATOR	16	? (ltree, _lquery) ,
	OPERATOR	17	? (_lquery, ltree) ,
	FUNCTION	1	ltree_consistent (internal, ltree, int2, oid, internal),
	FUNCTION	2	ltree_union (internal, internal),
	FUNCTION	3	ltree_compress (internal),
	FUNCTION	4	ltree_decompress (internal),
	FUNCTION	5	ltree_penalty (internal, internal, internal),
	FUNCTION	6	ltree_picksplit (internal, internal),
	FUNCTION	7	ltree_same (ltree_gist, ltree_gist, internal),
	STORAGE		ltree_gist
;
