ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser
    ADD MAPPING FOR asciiword, word WITH split_ts_probe.simple_dict, english_stem
;
DROP PUBLICATION IF EXISTS split_pub18_cols
;
create table defpart_attach_test_d (b int, a int)
;
ANALYZE road
;
CREATE TABLE "public"."constr_parent3" (
    "a" integer NOT NULL
)
;
DROP TABLE spoolme
;
create function sp_test_func() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar $$
language sql stable
;
drop view def_view_test
;
-- alter table / drop column tests
-- try altering system catalogs, should fail
alter table pg_class drop column relname
;
ALTER COLLATION test0 RENAME TO test11
;
DROP TABLE badcompresstbl
;
alter view my_locks reset (security_barrier)
;
ALTER PUBLICATION split_pub18_cols SET (publish = 'insert, update', publish_via_partition_root = true)
;
--
-- Verify prohibition of row triggers with transition triggers on
-- partitions
--
create table parent (a text, b int) partition by list (a)
;
DROP TABLE copy_encoding_tab
;
CREATE FUNCTION ltree_ne(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create function pg_temp.twophase_func() returns void as
  $$ select '2pc_func'::text $$ language sql
;
CREATE PUBLICATION pub_all FOR ALL TABLES
;
CREATE COLLATION ctest_nondet (locale = 'en_US', deterministic = false)
;
CREATE FUNCTION split_trigger_probe.event_logger()
RETURNS event_trigger
LANGUAGE plpgsql
AS $$
BEGIN
    NULL;
END;
$$
;
CREATE OPERATOR <->>> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_dist_commutator_op,
        COMMUTATOR = '<<<->'
)
;
CREATE FUNCTION "public"."aggfns_trans"("public"."aggtype"[], integer, integer, "text") RETURNS "public"."aggtype"[]
    LANGUAGE "sql" IMMUTABLE
    AS $_$select array_append($1,ROW($2,$3,$4)::aggtype)$_$
;
CREATE TABLE mchash1
  PARTITION OF mchash FOR VALUES WITH (MODULUS 4, REMAINDER 0)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
-- Test local triggers
create trigger trig_stmt_before before insert on rem2
	for each statement execute procedure trigger_func()
;
-- check that line comments work correctly within the argument list
-- (this used to require a special hack in the code; it no longer does,
-- but let's keep the test anyway)
create function namedparmcursor_test8() returns int4 as $$
declare
  c1 cursor (p1 int, p2 int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2;
  n int4;
begin
  open c1 (77 -- test
  , 42);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
create function int8alias1lt(int8alias1, int8alias1) returns bool
  strict immutable language internal as 'int8lt'
;
ALTER TABLE regtest_ptable ALTER p SET DEFAULT 'abcd'
;
ALTER TABLE "public"."trigger_parted_p2" DISABLE TRIGGER "aft_row"
;
ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p2" FOR VALUES FROM (12) TO (22)
;
DROP TABLE partkey_t, hash_parted, hash_parted2
;
drop function polyf(x anycompatiblerange, y anycompatiblearray)
;
DROP TABLE viewtest_tbl
;
CREATE FUNCTION test_future_xid_status(xid8)
RETURNS void
LANGUAGE plpgsql
AS
$$
BEGIN
  PERFORM pg_xact_status($1);
  RAISE EXCEPTION 'didn''t ERROR at xid in the future as expected';
EXCEPTION
  WHEN invalid_parameter_value THEN
    RAISE NOTICE 'Got expected error for xid in the future';
END;
$$
;
drop table pitable
;
create materialized view mvtest_error as select 1/0 as x with no data
;
CREATE SCHEMA pub_test2
;
-- page_header() uses int instead of smallint for lower, upper, special and
-- pagesize in pageinspect >= 1.10.
ALTER EXTENSION pageinspect UPDATE TO '1.9'
;
VACUUM FULL vacparted, vactst
;
DROP VIEW gtest1v
;
ALTER SEQUENCE truncate_a_id1 OWNED BY truncate_a.id1
;
CREATE TABLE "public"."gtestxx_3" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
ALTER INDEX dummy_test_idx SET (option_int = 10)
;
-- 0


-- Shifting

CREATE TABLE BIT_SHIFT_TABLE(b BIT(16))
;
CREATE FUNCTION gbt_bpchar_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE radix_text_tbl AS
    SELECT name AS t FROM road WHERE name !~ '^[0-9]'
;
CREATE FUNCTION citext_lt( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- Check invalidation of plpgsql "simple expression"

create temp view v1 as
  select 2+2 as f1
;
ALTER INDEX "public"."idxpart_a_idx" ATTACH PARTITION "public"."idxpart1_a_idx"
;
------------------------------------------------------------------------------------------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser
    ADD MAPPING FOR asciiword, word WITH split_ts_probe.simple_dict, english_stem
;
----------
[DROP_PUB_SUB] DROP PUBLICATION IF EXISTS split_pub18_cols
;
----------
[CREATE_TABLE] create table defpart_attach_test_d (b int, a int)
;
----------
[ADMIN_TABLE] ANALYZE road
;
----------
[CREATE_TABLE] CREATE TABLE "public"."constr_parent3" (
    "a" integer NOT NULL
)
;
----------
[DROP_TABLE] DROP TABLE spoolme
;
----------
[CREATE_PROG_OBJ] create function sp_test_func() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar $$
language sql stable
;
----------
[DROP_VIEW] drop view def_view_test
;
----------
[ALTER_TABLE|DROP_COLUMN] -- alter table / drop column tests
-- try altering system catalogs, should fail
alter table pg_class drop column relname
;
----------
[ALTER_POLICY] ALTER COLLATION test0 RENAME TO test11
;
----------
[DROP_TABLE] DROP TABLE badcompresstbl
;
----------
[ALTER_VIEW] alter view my_locks reset (security_barrier)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION split_pub18_cols SET (publish = 'insert, update', publish_via_partition_root = true)
;
----------
[CREATE_TABLE] --
-- Verify prohibition of row triggers with transition triggers on
-- partitions
--
create table parent (a text, b int) partition by list (a)
;
----------
[DROP_TABLE] DROP TABLE copy_encoding_tab
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_ne(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function pg_temp.twophase_func() returns void as
  $$ select '2pc_func'::text $$ language sql
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_all FOR ALL TABLES
;
----------
[CREATE_POLICY] CREATE COLLATION ctest_nondet (locale = 'en_US', deterministic = false)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_trigger_probe.event_logger()
RETURNS event_trigger
LANGUAGE plpgsql
AS $$
BEGIN
    NULL;
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <->>> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_dist_commutator_op,
        COMMUTATOR = '<<<->'
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."aggfns_trans"("public"."aggtype"[], integer, integer, "text") RETURNS "public"."aggtype"[]
    LANGUAGE "sql" IMMUTABLE
    AS $_$select array_append($1,ROW($2,$3,$4)::aggtype)$_$
;
----------
[CREATE_TABLE] CREATE TABLE mchash1
  PARTITION OF mchash FOR VALUES WITH (MODULUS 4, REMAINDER 0)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Test local triggers
create trigger trig_stmt_before before insert on rem2
	for each statement execute procedure trigger_func()
;
----------
[CREATE_PROG_OBJ] -- check that line comments work correctly within the argument list
-- (this used to require a special hack in the code; it no longer does,
-- but let's keep the test anyway)
create function namedparmcursor_test8() returns int4 as $$
declare
  c1 cursor (p1 int, p2 int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2;
  n int4;
begin
  open c1 (77 -- test
  , 42);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
----------
[CREATE_PROG_OBJ] create function int8alias1lt(int8alias1, int8alias1) returns bool
  strict immutable language internal as 'int8lt'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_ptable ALTER p SET DEFAULT 'abcd'
;
----------
[ALTER_TABLE] ALTER TABLE "public"."trigger_parted_p2" DISABLE TRIGGER "aft_row"
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p2" FOR VALUES FROM (12) TO (22)
;
----------
[DROP_TABLE] DROP TABLE partkey_t, hash_parted, hash_parted2
;
----------
[DROP_PROG_OBJ] drop function polyf(x anycompatiblerange, y anycompatiblearray)
;
----------
[DROP_TABLE] DROP TABLE viewtest_tbl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_future_xid_status(xid8)
RETURNS void
LANGUAGE plpgsql
AS
$$
BEGIN
  PERFORM pg_xact_status($1);
  RAISE EXCEPTION 'didn''t ERROR at xid in the future as expected';
EXCEPTION
  WHEN invalid_parameter_value THEN
    RAISE NOTICE 'Got expected error for xid in the future';
END;
$$
;
----------
[DROP_TABLE] drop table pitable
;
----------
[CREATE_VIEW(SELECT)] create materialized view mvtest_error as select 1/0 as x with no data
;
----------
[CREATE_SCHEMA] CREATE SCHEMA pub_test2
;
----------
[ALTER_LIBRARY] -- page_header() uses int instead of smallint for lower, upper, special and
-- pagesize in pageinspect >= 1.10.
ALTER EXTENSION pageinspect UPDATE TO '1.9'
;
----------
[ADMIN_TABLE] VACUUM FULL vacparted, vactst
;
----------
[DROP_VIEW] DROP VIEW gtest1v
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE truncate_a_id1 OWNED BY truncate_a.id1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtestxx_3" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_int = 10)
;
----------
[CREATE_TABLE] -- 0


-- Shifting

CREATE TABLE BIT_SHIFT_TABLE(b BIT(16))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bpchar_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE radix_text_tbl AS
    SELECT name AS t FROM road WHERE name !~ '^[0-9]'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_lt( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] -- Check invalidation of plpgsql "simple expression"

create temp view v1 as
  select 2+2 as f1
;
----------
[ALTER_INDEX] ALTER INDEX "public"."idxpart_a_idx" ATTACH PARTITION "public"."idxpart1_a_idx"
;
