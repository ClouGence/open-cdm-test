ALTER TABLE t ADD COLUMN j int
;
do $$
declare y int := x + 1;  -- error
        x int := 42;
begin
  raise notice 'x = %, y = %', x, y;
end;
$$
;
CREATE INDEX gtest22c_pred_idx ON gtest22c (a) WHERE b > 0
;
CREATE TABLE "public"."pfield" (
    "name" "text",
    "comment" "text"
)
;
ALTER TABLE tab7 ADD PRIMARY KEY (a, b)
;
CREATE TRIGGER trans_inserttrig
  AFTER INSERT ON range_parted REFERENCING NEW TABLE AS new_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trans_updatetrigfunc()
;
CREATE TEMP TABLE bar (id1 integer, id2 integer)
;
/*
 * A few supporting functions first ...
 */

/* Expand any 1-D array into a set with integers 1..N */
CREATE FUNCTION _pg_expandarray(IN anyarray, OUT x anyelement, OUT n int)
    RETURNS SETOF RECORD
    LANGUAGE sql STRICT IMMUTABLE PARALLEL SAFE
    AS 'select $1[s], s - pg_catalog.array_lower($1,1) + 1
        from pg_catalog.generate_series(pg_catalog.array_lower($1,1),
                                        pg_catalog.array_upper($1,1),
                                        1) as g(s)'
;
ALTER TEXT SEARCH CONFIGURATION tamil ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_publication_rel
;
CREATE TABLE "public"."plt2_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE FUNCTION test_ginpostinglist()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
-- Check with foreign modify
CREATE TABLE base_tbl3 (a int, b int, c text)
;
create table inh_nn4 () inherits (inh_nn1, inh_nn2)
;
-- test with more than one reason to rewrite a single table
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE NOTICE 'Table ''%'' is being rewritten (reason = %)',
               pg_event_trigger_table_rewrite_oid()::regclass,
               pg_event_trigger_table_rewrite_reason();
END;
$$
;
CREATE UNIQUE INDEX enumtest_btree ON enumtest USING btree (col)
;
CREATE FUNCTION isnle(issn13, issn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE OPERATOR | (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	COMMUTATOR = |,
	PROCEDURE = _int_union
)
;
ALTER TABLE ONLY "public"."gtest_tableoid"
    ADD CONSTRAINT "gtest_tableoid_pkey" PRIMARY KEY ("a")
;
DROP FOREIGN TABLE foreign_tbl
;
CREATE MATERIALIZED VIEW split_cov.child_mv AS SELECT parent_id, count(*) AS total FROM split_cov.child_table GROUP BY parent_id WITH NO DATA
;
CREATE FUNCTION regress_test_func (t text) RETURNS text AS $$
	SELECT $1;
$$ LANGUAGE sql
;
ALTER TEXT SEARCH DICTIONARY synonym (CaseSensitive = off)
;
CREATE TABLE tbl_phv(x int, y int PRIMARY KEY)
;
create index on t using spgist(f1 name_ops_old) include(f2, f3)
;
COMMENT ON INDEX "public"."comment_test_pk" IS 'Index backing the PRIMARY KEY of comment_test'
;
drop table testpart_orange
;
--
--
--
-- oid ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_oid_consistent(internal,oid,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE tab3_1 PARTITION OF tab3 FOR VALUES IN (0, 1, 2, 3, 5, 6)
;
CREATE TABLE tenk2 AS SELECT * FROM tenk1
;
-- aggregate combine and serialization functions

-- can't specify just one of serialfunc and deserialfunc
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize
)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_opclass
;
CREATE INDEX ON rescan_bhs (a)
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "url" WITH "simple"
;
CREATE OPERATOR ? (
        LEFTARG = _ltree,
	RIGHTARG = _lquery,
	PROCEDURE = _lt_q_regex,
        COMMUTATOR = '?',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN IF NOT EXISTS c3 integer, 
	ADD COLUMN c4 integer REFERENCES test_add_column
;
COMMENT ON COLUMN ctlt1.a IS 'A'
;
-- Deduplication
TRUNCATE bttest_a
;
CREATE TABLE prt2_l_p3_p2 PARTITION OF prt2_l_p3 FOR VALUES FROM (13) TO (25)
;
DROP FOREIGN TABLE ft_part_1_1, ft_part_1_2
;
ALTER SEQUENCE sequence_test14 AS int
;
ANALYZE pg_temp_3.pg_temp_16652
;
CREATE FUNCTION gbt_int4_same(gbtreekey8, gbtreekey8, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- error: cannot work with deferrable primary keys
CREATE TABLE testpub_tbl5d (a int PRIMARY KEY DEFERRABLE)
;
CREATE MATERIALIZED VIEW pgss_matv AS SELECT * FROM pgss_ctas
;
DROP INDEX CONCURRENTLY IF EXISTS "concur_index2"
;
--lquery type
CREATE FUNCTION lquery_in(cstring)
RETURNS lquery
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- partitioned table child
ALTER TABLE regtest_ptable_1_ones RENAME TO regtest_table_part
;
CREATE FUNCTION gbtreekey32_out(gbtreekey32)
RETURNS cstring
AS '$libdir/btree_gist', 'gbtreekey_out'
LANGUAGE C IMMUTABLE STRICT
;
create table ab_a2_b2 partition of ab_a2 for values in (2)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t ADD COLUMN j int
;
----------
[BLOCK] do $$
declare y int := x + 1;  -- error
        x int := 42;
begin
  raise notice 'x = %, y = %', x, y;
end;
$$
;
----------
[ADD_INDEX] CREATE INDEX gtest22c_pred_idx ON gtest22c (a) WHERE b > 0
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pfield" (
    "name" "text",
    "comment" "text"
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tab7 ADD PRIMARY KEY (a, b)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trans_inserttrig
  AFTER INSERT ON range_parted REFERENCING NEW TABLE AS new_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trans_updatetrigfunc()
;
----------
[CREATE_TABLE] CREATE TEMP TABLE bar (id1 integer, id2 integer)
;
----------
[CREATE_PROG_OBJ] /*
 * A few supporting functions first ...
 */

/* Expand any 1-D array into a set with integers 1..N */
CREATE FUNCTION _pg_expandarray(IN anyarray, OUT x anyelement, OUT n int)
    RETURNS SETOF RECORD
    LANGUAGE sql STRICT IMMUTABLE PARALLEL SAFE
    AS 'select $1[s], s - pg_catalog.array_lower($1,1) + 1
        from pg_catalog.generate_series(pg_catalog.array_lower($1,1),
                                        pg_catalog.array_upper($1,1),
                                        1) as g(s)'
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION tamil ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_publication_rel
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt2_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_ginpostinglist()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] -- Check with foreign modify
CREATE TABLE base_tbl3 (a int, b int, c text)
;
----------
[CREATE_TABLE] create table inh_nn4 () inherits (inh_nn1, inh_nn2)
;
----------
[CREATE_PROG_OBJ] -- test with more than one reason to rewrite a single table
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE NOTICE 'Table ''%'' is being rewritten (reason = %)',
               pg_event_trigger_table_rewrite_oid()::regclass,
               pg_event_trigger_table_rewrite_reason();
END;
$$
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX enumtest_btree ON enumtest USING btree (col)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(issn13, issn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR | (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	COMMUTATOR = |,
	PROCEDURE = _int_union
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest_tableoid"
    ADD CONSTRAINT "gtest_tableoid_pkey" PRIMARY KEY ("a")
;
----------
[DROP_TABLE] DROP FOREIGN TABLE foreign_tbl
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW split_cov.child_mv AS SELECT parent_id, count(*) AS total FROM split_cov.child_table GROUP BY parent_id WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regress_test_func (t text) RETURNS text AS $$
	SELECT $1;
$$ LANGUAGE sql
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY synonym (CaseSensitive = off)
;
----------
[CREATE_TABLE] CREATE TABLE tbl_phv(x int, y int PRIMARY KEY)
;
----------
[ADD_INDEX] create index on t using spgist(f1 name_ops_old) include(f2, f3)
;
----------
[COMMENT_INDEX] COMMENT ON INDEX "public"."comment_test_pk" IS 'Index backing the PRIMARY KEY of comment_test'
;
----------
[DROP_TABLE] drop table testpart_orange
;
----------
[CREATE_PROG_OBJ] --
--
--
-- oid ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_oid_consistent(internal,oid,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE tab3_1 PARTITION OF tab3 FOR VALUES IN (0, 1, 2, 3, 5, 6)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tenk2 AS SELECT * FROM tenk1
;
----------
[CREATE_PROG_OBJ] -- aggregate combine and serialization functions

-- can't specify just one of serialfunc and deserialfunc
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_opclass
;
----------
[ADD_INDEX] CREATE INDEX ON rescan_bhs (a)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "url" WITH "simple"
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ? (
        LEFTARG = _ltree,
	RIGHTARG = _lquery,
	PROCEDURE = _lt_q_regex,
        COMMUTATOR = '?',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN IF NOT EXISTS c3 integer, 
	ADD COLUMN c4 integer REFERENCES test_add_column
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN ctlt1.a IS 'A'
;
----------
[TRUNCATE_TABLE] -- Deduplication
TRUNCATE bttest_a
;
----------
[CREATE_TABLE] CREATE TABLE prt2_l_p3_p2 PARTITION OF prt2_l_p3 FOR VALUES FROM (13) TO (25)
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ft_part_1_1, ft_part_1_2
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test14 AS int
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_16652
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int4_same(gbtreekey8, gbtreekey8, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] -- error: cannot work with deferrable primary keys
CREATE TABLE testpub_tbl5d (a int PRIMARY KEY DEFERRABLE)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW pgss_matv AS SELECT * FROM pgss_ctas
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY IF EXISTS "concur_index2"
;
----------
[CREATE_PROG_OBJ] --lquery type
CREATE FUNCTION lquery_in(cstring)
RETURNS lquery
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[RENAME_TABLE] -- partitioned table child
ALTER TABLE regtest_ptable_1_ones RENAME TO regtest_table_part
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey32_out(gbtreekey32)
RETURNS cstring
AS '$libdir/btree_gist', 'gbtreekey_out'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] create table ab_a2_b2 partition of ab_a2 for values in (2)
;
