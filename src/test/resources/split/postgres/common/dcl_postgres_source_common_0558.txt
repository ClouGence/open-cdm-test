EXECUTE dumpFunc('34517')
;
EXECUTE dumpFunc('34409')
;
EXECUTE dumpFunc('16421')
;
COMMIT PREPARED 'regress_sub2'
;
EXECUTE dumpFunc('23042')
;
set plpgsql.extra_warnings to 'none'
;
PREPARE q8 AS
    UPDATE tenk1 SET stringu1 = $2 WHERE unique1 = $1
;
EXECUTE dumpFunc('35305')
;
COPY generated_virtual_tests.gtestnn_child (f1, f2) TO stdout
;
--
-- Tests for external toast datums
--

-- Other compression algorithms may cause the compressed data to be stored
-- inline.  pglz guarantees that the data is externalized, so stick to it.
SET default_toast_compression = 'pglz'
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('34517')
;
----------
[UNSAFE] EXECUTE dumpFunc('34409')
;
----------
[UNSAFE] EXECUTE dumpFunc('16421')
;
----------
[TRANSACTION] COMMIT PREPARED 'regress_sub2'
;
----------
[UNSAFE] EXECUTE dumpFunc('23042')
;
----------
[SESSION_SETTING_WRITE] set plpgsql.extra_warnings to 'none'
;
----------
[UNSAFE] PREPARE q8 AS
    UPDATE tenk1 SET stringu1 = $2 WHERE unique1 = $1
;
----------
[UNSAFE] EXECUTE dumpFunc('35305')
;
----------
[DATA_EXPORT] COPY generated_virtual_tests.gtestnn_child (f1, f2) TO stdout
;
----------
[SESSION_SETTING_WRITE] --
-- Tests for external toast datums
--

-- Other compression algorithms may cause the compressed data to be stored
-- inline.  pglz guarantees that the data is externalized, so stick to it.
SET default_toast_compression = 'pglz'
;
