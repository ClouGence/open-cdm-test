-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14109', relminmxid = '3'
WHERE oid = '"public"."pht1_e_p3"'::pg_catalog.regclass
;
INSERT INTO prt2_adv_p1 SELECT i % 25, i, to_char(i, 'FM0000') FROM generate_series(100, 149) i
;
UPDATE t1 SET b=t1.b FROM t2
WHERE t1.a = 3 and t2.a = 3 AND f_leak(t1.b) AND f_leak(t2.b)
;
DELETE FROM tst_comp_one_enum_array WHERE 'a' = ANY((b).b)
;
INSERT INTO POINT_TBL(f1) VALUES ('(10.0 10.0)')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14709', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_4_2"'::pg_catalog.regclass
;
insert into arr_pk_tbl(pk, f1[1:2]) values (1, '{6,7,8}') on conflict (pk)
  do update set f1[1] = excluded.f1[1],
    f1[2] = excluded.f1[2],
    f1[3] = excluded.f1[3]
  returning pk, f1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9224', relminmxid = '1'
WHERE oid = '"public"."rules_src"'::pg_catalog.regclass
;
INSERT into pitest2(f1, f2) VALUES ('2016-07-4', 'from pitest2')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15980', relminmxid = '3'
WHERE oid = '"public"."prt1_n_p2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10239', relminmxid = '1'
WHERE oid = '"public"."mcv_lists"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14847', relminmxid = '3'
WHERE oid = '36979'
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt1_e_p1"'::pg_catalog.regclass
;
INSERT INTO PATH_TBL VALUES ('((10,20))')
;
INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_trigger
;
insert into rtest_t3 values (4, 34)
;
-- Small groups of 10 tuples each tested around each mode transition point.
insert into t(a, b) select i / 10, i from generate_series(1, 1000) n(i)
;
INSERT INTO test (a,b) VALUES ('{aaa}','{bbb}')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11984', relminmxid = '3'
WHERE oid = '33046'
;
INSERT INTO macaddr_data VALUES (12, '08:00:2a:01:02:03')
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt4_n_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '38801'
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."alpha_neg_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '20973'
;
UPDATE colors SET is_active = true WHERE key = 1
;
INSERT INTO num_exp_div VALUES (0,7,'0')
;
INSERT INTO TIMESTAMP_TBL VALUES ('epoch')
;
--
-- Test multiple-set-clause syntax
--

INSERT INTO update_test SELECT a,b+1,c FROM update_test
;
-- OVERRIDING tests

-- GENERATED BY DEFAULT

-- This inserts the row as presented:
INSERT INTO itest1 VALUES (10, 'xyz')
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part1_self_fk"'::pg_catalog.regclass
  AND attname IN ('id', 'id_abc')
;
INSERT INTO num_exp_add VALUES (9,6,'-24832902.467417160')
;
INSERT INTO public.ths_p2 VALUES (7, 897)
;
insert into insertconflicttest values (15, 'Cranberry') on conflict (key) do update set fruit = excluded.fruit
;
insert into pendtest values (to_tsvector('Lore ipsum'))
;
INSERT INTO public.tht VALUES ('0', 160)
;
DELETE FROM tststats.priv_test_parent_tbl WHERE a <<< 0 AND b <<< 0
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1098', relminmxid = '1'
WHERE oid = '"public"."num_exp_log10"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'x'
  AND attrelid = '"public"."gtest1_y"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '18476'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15896', relminmxid = '3'
WHERE oid = '"public"."mic_inputs"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('4', 344)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12925', relminmxid = '3'
WHERE oid = '"fkpart5"."fk2"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('5', 395)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12352', relminmxid = '3'
WHERE oid = '32123'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1563', relminmxid = '1'
WHERE oid = '"public"."onek"'::pg_catalog.regclass
;
INSERT INTO rls_test_permissive VALUES ('regress_r4','regress_s4',7)
;
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.2........', -1, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."tt7"'::pg_catalog.regclass
  AND attname = v.dname
;
INSERT INTO tab_copy VALUES (88)
;
-- not allowed
UPDATE rw_view2 SET b = 'bar' WHERE a = 1
;
INSERT INTO public.ths_p2 VALUES (3, 973)
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14109', relminmxid = '3'
WHERE oid = '"public"."pht1_e_p3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO prt2_adv_p1 SELECT i % 25, i, to_char(i, 'FM0000') FROM generate_series(100, 149) i
;
----------
[UPDATE] UPDATE t1 SET b=t1.b FROM t2
WHERE t1.a = 3 and t2.a = 3 AND f_leak(t1.b) AND f_leak(t2.b)
;
----------
[DELETE] DELETE FROM tst_comp_one_enum_array WHERE 'a' = ANY((b).b)
;
----------
[INSERT|SELECT] INSERT INTO POINT_TBL(f1) VALUES ('(10.0 10.0)')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '14709', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_4_2"'::pg_catalog.regclass
;
----------
[MERGE|SELECT] insert into arr_pk_tbl(pk, f1[1:2]) values (1, '{6,7,8}') on conflict (pk)
  do update set f1[1] = excluded.f1[1],
    f1[2] = excluded.f1[2],
    f1[3] = excluded.f1[3]
  returning pk, f1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9224', relminmxid = '1'
WHERE oid = '"public"."rules_src"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT into pitest2(f1, f2) VALUES ('2016-07-4', 'from pitest2')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15980', relminmxid = '3'
WHERE oid = '"public"."prt1_n_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10239', relminmxid = '1'
WHERE oid = '"public"."mcv_lists"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14847', relminmxid = '3'
WHERE oid = '36979'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt1_e_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO PATH_TBL VALUES ('((10,20))')
;
----------
[INSERT|SELECT] INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_trigger
;
----------
[INSERT] insert into rtest_t3 values (4, 34)
;
----------
[INSERT|SELECT] -- Small groups of 10 tuples each tested around each mode transition point.
insert into t(a, b) select i / 10, i from generate_series(1, 1000) n(i)
;
----------
[INSERT|SELECT] INSERT INTO test (a,b) VALUES ('{aaa}','{bbb}')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11984', relminmxid = '3'
WHERE oid = '33046'
;
----------
[INSERT] INSERT INTO macaddr_data VALUES (12, '08:00:2a:01:02:03')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt4_n_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '38801'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."alpha_neg_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '20973'
;
----------
[UPDATE] UPDATE colors SET is_active = true WHERE key = 1
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (0,7,'0')
;
----------
[INSERT] INSERT INTO TIMESTAMP_TBL VALUES ('epoch')
;
----------
[INSERT|SELECT] --
-- Test multiple-set-clause syntax
--

INSERT INTO update_test SELECT a,b+1,c FROM update_test
;
----------
[INSERT] -- OVERRIDING tests

-- GENERATED BY DEFAULT

-- This inserts the row as presented:
INSERT INTO itest1 VALUES (10, 'xyz')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part1_self_fk"'::pg_catalog.regclass
  AND attname IN ('id', 'id_abc')
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (9,6,'-24832902.467417160')
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 897)
;
----------
[MERGE] insert into insertconflicttest values (15, 'Cranberry') on conflict (key) do update set fruit = excluded.fruit
;
----------
[INSERT] insert into pendtest values (to_tsvector('Lore ipsum'))
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 160)
;
----------
[DELETE] DELETE FROM tststats.priv_test_parent_tbl WHERE a <<< 0 AND b <<< 0
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1098', relminmxid = '1'
WHERE oid = '"public"."num_exp_log10"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'x'
  AND attrelid = '"public"."gtest1_y"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '18476'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '15896', relminmxid = '3'
WHERE oid = '"public"."mic_inputs"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 344)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12925', relminmxid = '3'
WHERE oid = '"fkpart5"."fk2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 395)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12352', relminmxid = '3'
WHERE oid = '32123'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1563', relminmxid = '1'
WHERE oid = '"public"."onek"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO rls_test_permissive VALUES ('regress_r4','regress_s4',7)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.2........', -1, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."tt7"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT] INSERT INTO tab_copy VALUES (88)
;
----------
[UPDATE] -- not allowed
UPDATE rw_view2 SET b = 'bar' WHERE a = 1
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 973)
;
