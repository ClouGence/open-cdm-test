CREATE FUNCTION get_val_in_shmem() RETURNS INT
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE SCHEMA regtest_schema_1
;
create table integers as
select
  case i%11 when 10 then null else i%11 end as x,
  case (i/11)%11 when 10 then null else (i/11)%11 end as y
from generate_series(0, 11*11-1) i
;
ANALYZE pg_temp_11.pg_temp_26417_2
;
ALTER TABLE fd_pt1 DROP CONSTRAINT fd_pt1chk1 CASCADE
;
CREATE POLICY p1 ON r1 USING (true)
;
ANALYZE public.gtest28a
;
CREATE INDEX gist_idx ON gist_idx_tbl USING gist (p)
;
ALTER FUNCTION gbt_float8_distance(internal, double precision, smallint, oid, internal) PARALLEL SAFE
;
CREATE TABLE "public"."clstr_tst_inh" (
    "a" integer DEFAULT "nextval"('"public"."clstr_tst_a_seq"'::"regclass") NOT NULL,
    "b" integer,
    "c" "text",
    "d" "text"
)
;
-- cleanup
drop table parted_trig
;
CREATE TABLE "fkpart3"."pk4" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
create index concurrently on idxpart (a)
;
-- picks nonconflicting name
ALTER TABLE tt9 ADD CONSTRAINT foo CHECK(c > 3)
;
CREATE TABLE rule_t1 (a INT)
;
CREATE TABLE transition_table_level1
(
      level1_no serial NOT NULL ,
      level1_node_name varchar(255),
       PRIMARY KEY (level1_no)
) WITHOUT OIDS
;
-- can be pushed down
DROP TRIGGER trig_row_before_update ON rem1
;
CREATE TABLE serialTest1 (f1 text, f2 serial)
;
VACUUM (FREEZE) pg_catalog.pg_replication_origin
;
ALTER TABLE pg_temp_11.pg_temp_26640_2 ADD COLUMN newdata pg_temp_11.pg_temp_26640
;
CREATE FUNCTION test_shm_mq_pipelined(queue_size pg_catalog.int8,
					   message pg_catalog.text,
					   repeat_count pg_catalog.int4 default 1,
					   num_workers pg_catalog.int4 default 1,
					   verify pg_catalog.bool default true)
    RETURNS pg_catalog.void STRICT
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE TABLE test_float8 (
	i float8
)
;
-- Stress run-time partition pruning a bit more, per bug reports
create temp table p (a int, b int, c int) partition by list (a)
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest20"
    ADD CONSTRAINT "gtest20_pkey" PRIMARY KEY ("a")
;
CREATE TYPE dump_test.composite AS (
						   f1 int,
						   f2 dump_test.int42
					   )
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_subscription
;
CREATE TABLE test_root_2 PARTITION OF test_root FOR VALUES FROM (10) TO (20)
;
CREATE TEMP TABLE filenode_mapping AS
SELECT
    oid, mapped_oid, reltablespace, relfilenode, relname
FROM pg_class,
    pg_filenode_relation(reltablespace, pg_relation_filenode(oid)) AS mapped_oid
WHERE relkind IN ('r', 'i', 'S', 't', 'm')
  AND relpersistence != 't'
  AND mapped_oid IS DISTINCT FROM oid
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
CREATE FUNCTION hstore_recv(internal)
RETURNS hstore
AS '$libdir/hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE ONLY "public"."pagg_tab_ml_p2" ATTACH PARTITION "public"."pagg_tab_ml_p2_s1" FOR VALUES IN ('0000', '0001', '0002')
;
create unique index loct1_idx on loct1 (a)
;
-- ERROR
ALTER FOREIGN TABLE ft1 DROP COLUMN IF EXISTS no_column
;
CREATE TYPE hash_test_t2 AS (a varbit, b text)
;
drop function dfunc(int)
;
create type quadarray as (c1 complex[], c2 complex)
;
CREATE FUNCTION lo_oid(lo) RETURNS pg_catalog.oid AS
'SELECT $1::pg_catalog.oid' LANGUAGE SQL STRICT IMMUTABLE PARALLEL SAFE
;
drop view vw_ord
;
drop function trap_foreign_key_2()
;
ALTER TEXT SEARCH CONFIGURATION norwegian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH norwegian_stem
;
COMMENT ON TEXT SEARCH TEMPLATE split_ts_probe.simple_template IS 'template probe'
;
ALTER POLICY p1 ON document USING (true)
;
drop table perm_parted cascade
;
CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int) RETURNS int
AS 'select $1+1' LANGUAGE sql
;
alter table e_star reset (parallel_workers)
;
CREATE TABLE t1(c1 int, c2 int, c3 int GENERATED ALWAYS AS (c1 * 2) STORED)
;
CREATE TABLE itest14 (id serial)
;
CREATE TABLE prt3_n_p3 PARTITION OF prt3_n FOR VALUES IN ('0003', '0005', '0009', '0011')
;
CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS concur_index2 ON concur_heap(f1)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_index
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION get_val_in_shmem() RETURNS INT
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regtest_schema_1
;
----------
[CREATE_TABLE|SELECT] create table integers as
select
  case i%11 when 10 then null else i%11 end as x,
  case (i/11)%11 when 10 then null else (i/11)%11 end as y
from generate_series(0, 11*11-1) i
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26417_2
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE fd_pt1 DROP CONSTRAINT fd_pt1chk1 CASCADE
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON r1 USING (true)
;
----------
[ADMIN_TABLE] ANALYZE public.gtest28a
;
----------
[ADD_INDEX] CREATE INDEX gist_idx ON gist_idx_tbl USING gist (p)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float8_distance(internal, double precision, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."clstr_tst_inh" (
    "a" integer DEFAULT "nextval"('"public"."clstr_tst_a_seq"'::"regclass") NOT NULL,
    "b" integer,
    "c" "text",
    "d" "text"
)
;
----------
[DROP_TABLE] -- cleanup
drop table parted_trig
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk4" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[ADD_INDEX] create index concurrently on idxpart (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- picks nonconflicting name
ALTER TABLE tt9 ADD CONSTRAINT foo CHECK(c > 3)
;
----------
[CREATE_TABLE] CREATE TABLE rule_t1 (a INT)
;
----------
[CREATE_TABLE] CREATE TABLE transition_table_level1
(
      level1_no serial NOT NULL ,
      level1_node_name varchar(255),
       PRIMARY KEY (level1_no)
) WITHOUT OIDS
;
----------
[DROP_TRIGGER] -- can be pushed down
DROP TRIGGER trig_row_before_update ON rem1
;
----------
[CREATE_TABLE] CREATE TABLE serialTest1 (f1 text, f2 serial)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_replication_origin
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26640_2 ADD COLUMN newdata pg_temp_11.pg_temp_26640
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_shm_mq_pipelined(queue_size pg_catalog.int8,
					   message pg_catalog.text,
					   repeat_count pg_catalog.int4 default 1,
					   num_workers pg_catalog.int4 default 1,
					   verify pg_catalog.bool default true)
    RETURNS pg_catalog.void STRICT
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE test_float8 (
	i float8
)
;
----------
[CREATE_TABLE] -- Stress run-time partition pruning a bit more, per bug reports
create temp table p (a int, b int, c int) partition by list (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest20"
    ADD CONSTRAINT "gtest20_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TYPE] CREATE TYPE dump_test.composite AS (
						   f1 int,
						   f2 dump_test.int42
					   )
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] CREATE TABLE test_root_2 PARTITION OF test_root FOR VALUES FROM (10) TO (20)
;
----------
[CREATE_TABLE|SELECT|METADATA] CREATE TEMP TABLE filenode_mapping AS
SELECT
    oid, mapped_oid, reltablespace, relfilenode, relname
FROM pg_class,
    pg_filenode_relation(reltablespace, pg_relation_filenode(oid)) AS mapped_oid
WHERE relkind IN ('r', 'i', 'S', 't', 'm')
  AND relpersistence != 't'
  AND mapped_oid IS DISTINCT FROM oid
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_recv(internal)
RETURNS hstore
AS '$libdir/hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml_p2" ATTACH PARTITION "public"."pagg_tab_ml_p2_s1" FOR VALUES IN ('0000', '0001', '0002')
;
----------
[ADD_INDEX] create unique index loct1_idx on loct1 (a)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- ERROR
ALTER FOREIGN TABLE ft1 DROP COLUMN IF EXISTS no_column
;
----------
[CREATE_TYPE] CREATE TYPE hash_test_t2 AS (a varbit, b text)
;
----------
[DROP_PROG_OBJ] drop function dfunc(int)
;
----------
[CREATE_TYPE] create type quadarray as (c1 complex[], c2 complex)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lo_oid(lo) RETURNS pg_catalog.oid AS
'SELECT $1::pg_catalog.oid' LANGUAGE SQL STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_VIEW] drop view vw_ord
;
----------
[DROP_PROG_OBJ] drop function trap_foreign_key_2()
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION norwegian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH norwegian_stem
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH TEMPLATE split_ts_probe.simple_template IS 'template probe'
;
----------
[ALTER_POLICY] ALTER POLICY p1 ON document USING (true)
;
----------
[DROP_TABLE] drop table perm_parted cascade
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int) RETURNS int
AS 'select $1+1' LANGUAGE sql
;
----------
[ALTER_TABLE] alter table e_star reset (parallel_workers)
;
----------
[CREATE_TABLE] CREATE TABLE t1(c1 int, c2 int, c3 int GENERATED ALWAYS AS (c1 * 2) STORED)
;
----------
[CREATE_TABLE] CREATE TABLE itest14 (id serial)
;
----------
[CREATE_TABLE] CREATE TABLE prt3_n_p3 PARTITION OF prt3_n FOR VALUES IN ('0003', '0005', '0009', '0011')
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS concur_index2 ON concur_heap(f1)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_index
;
