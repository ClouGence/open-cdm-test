-- partition cannot have additional columns
DROP FOREIGN TABLE fd_pt2_1
;
ALTER TABLE pg_namespace ADD CONSTRAINT foo UNIQUE USING INDEX pg_namespace_nspname_index
;
DROP TABLE psql_pipeline
;
ALTER FUNCTION gbt_int2_penalty(internal, internal, internal) PARALLEL SAFE
;
alter table atacc1 add check (a > 3)
;
CREATE TEMP TABLE pg_temp_4.pg_temp_27293_2 (tid pg_catalog.tid)
;
create table lp (a char) partition by list (a)
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '13506'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
-- Assorted error cases
IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest4
;
CREATE TABLE hpart_5 (
	LIKE hash_parted
) PARTITION BY LIST (b)
;
CREATE FUNCTION int42_in(cstring)
   RETURNS int42
   AS 'int4in'
   LANGUAGE internal STRICT IMMUTABLE
;
alter extension postgres_fdw add aggregate least_agg(variadic items anyarray)
;
-- sql, proretset = f, prorettype = record
CREATE FUNCTION getrngfunc6(int) RETURNS RECORD AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER OPERATOR FAMILY gist_timestamp_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE TABLE "generated_stored_tests"."gtest21a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULLIF("a", 0)) STORED NOT NULL
)
;
create table tt27v_tbl (a int)
;
DROP TABLE mcv_lists_uuid
;
CREATE FUNCTION "public"."test_fdw_handler"() RETURNS "fdw_handler"
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'test_fdw_handler'
;
CREATE POLICY p4 ON rls_tbl FOR DELETE USING (c1 <= 3)
;
CREATE TABLE orders (date date, item text, num int)
;
CREATE RULE rw_view16_del_rule AS ON DELETE TO rw_view16
  WHERE OLD.a > 0 DO INSTEAD DELETE FROM base_tbl WHERE a=OLD.a
;
CREATE OPERATOR >>> (procedure = leak2, leftarg = integer, rightarg = integer,
                     restrict = scalargtsel)
;
ALTER TABLE part4 DROP COLUMN extraid
;
ANALYZE pg_temp_3.pg_temp_26915_2
;
ALTER TABLE ONLY "public"."idxpart_another"
    ADD CONSTRAINT "idxpart_another_pkey" PRIMARY KEY ("a", "b")
;
ALTER TABLE ONLY "public"."cc1" INHERIT "public"."pp1"
;
CREATE DATABASE sourcedb
;
CREATE TEMP TABLE temptest(col) ON COMMIT DROP AS SELECT 1
;
create table nantest1 ( id int4 , d float8 )
;
COMMENT ON TYPE seg IS
'floating point interval ''FLOAT .. FLOAT'', ''.. FLOAT'', ''FLOAT ..'' or ''FLOAT'''
;
CREATE UNIQUE INDEX t2_btree_unique ON s4.t2 USING BTREE (i)
;
CREATE MATERIALIZED VIEW mvtest_mv_v_3 (ii, jj, kk) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
drop function unnest1(anyarray)
;
CREATE TABLE "public"."test_inh_check_child" (
    "a" numeric,
    "b" numeric,
    CONSTRAINT "blocal" CHECK ((("b")::double precision < (1000)::double precision)),
    CONSTRAINT "bmerged" CHECK ((("b")::double precision > (1)::double precision))
)
;
COMMENT ON FOREIGN TABLE ft1 IS 'ft1'
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_sequence
;
CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=18253 host=/tmp/Gnlu8IK54H dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
-- ALTER .. NO INHERIT works for invalid constraints
CREATE TABLE notnull_tbl1 (a int)
;
-- these two should fail
alter table dropColumnchild drop column a
;
CREATE INDEX macaddr_data_btree ON macaddr_data USING btree (b)
;
VACUUM (PARALLEL 2) information_schema.sql_parts
;
ALTER TABLE toasted_key ALTER COLUMN toasted_col1 SET STORAGE EXTERNAL
;
AlTER EXTENSION pg_stat_statements UPDATE TO '1.7'
;
CREATE TRIGGER merge_ari AFTER INSERT ON target FOR EACH ROW EXECUTE PROCEDURE merge_trigfunc ()
;
CREATE TABLE "fkpart4"."droppk_d" (
    "a" integer CONSTRAINT "droppk_a_not_null" NOT NULL
)
;
alter table utrtest detach partition remp
;
-- OK

ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg2
;
CREATE TRIGGER ri_parent BEFORE UPDATE OR DELETE ON parent FOR EACH ROW EXECUTE PROCEDURE ri_parent()
;
CREATE INDEX ios_bitmap_a ON ios_bitmap(a)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] -- partition cannot have additional columns
DROP FOREIGN TABLE fd_pt2_1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_namespace ADD CONSTRAINT foo UNIQUE USING INDEX pg_namespace_nspname_index
;
----------
[DROP_TABLE] DROP TABLE psql_pipeline
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int2_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add check (a > 3)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_27293_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table lp (a char) partition by list (a)
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '13506'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
----------
[CREATE_TABLE] -- Assorted error cases
IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest4
;
----------
[CREATE_TABLE] CREATE TABLE hpart_5 (
	LIKE hash_parted
) PARTITION BY LIST (b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int42_in(cstring)
   RETURNS int42
   AS 'int4in'
   LANGUAGE internal STRICT IMMUTABLE
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw add aggregate least_agg(variadic items anyarray)
;
----------
[CREATE_PROG_OBJ] -- sql, proretset = f, prorettype = record
CREATE FUNCTION getrngfunc6(int) RETURNS RECORD AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timestamp_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest21a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULLIF("a", 0)) STORED NOT NULL
)
;
----------
[CREATE_TABLE] create table tt27v_tbl (a int)
;
----------
[DROP_TABLE] DROP TABLE mcv_lists_uuid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_fdw_handler"() RETURNS "fdw_handler"
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'test_fdw_handler'
;
----------
[CREATE_POLICY] CREATE POLICY p4 ON rls_tbl FOR DELETE USING (c1 <= 3)
;
----------
[CREATE_TABLE] CREATE TABLE orders (date date, item text, num int)
;
----------
[CREATE_POLICY] CREATE RULE rw_view16_del_rule AS ON DELETE TO rw_view16
  WHERE OLD.a > 0 DO INSTEAD DELETE FROM base_tbl WHERE a=OLD.a
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >>> (procedure = leak2, leftarg = integer, rightarg = integer,
                     restrict = scalargtsel)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE part4 DROP COLUMN extraid
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_26915_2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."idxpart_another"
    ADD CONSTRAINT "idxpart_another_pkey" PRIMARY KEY ("a", "b")
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."cc1" INHERIT "public"."pp1"
;
----------
[CREATE_CATALOG] CREATE DATABASE sourcedb
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE temptest(col) ON COMMIT DROP AS SELECT 1
;
----------
[CREATE_TABLE] create table nantest1 ( id int4 , d float8 )
;
----------
[COMMENT_TYPE] COMMENT ON TYPE seg IS
'floating point interval ''FLOAT .. FLOAT'', ''.. FLOAT'', ''FLOAT ..'' or ''FLOAT'''
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t2_btree_unique ON s4.t2 USING BTREE (i)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv_v_3 (ii, jj, kk) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
----------
[DROP_PROG_OBJ] drop function unnest1(anyarray)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_inh_check_child" (
    "a" numeric,
    "b" numeric,
    CONSTRAINT "blocal" CHECK ((("b")::double precision < (1000)::double precision)),
    CONSTRAINT "bmerged" CHECK ((("b")::double precision > (1)::double precision))
)
;
----------
[COMMENT_TABLE] COMMENT ON FOREIGN TABLE ft1 IS 'ft1'
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_sequence
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=18253 host=/tmp/Gnlu8IK54H dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[CREATE_TABLE] -- ALTER .. NO INHERIT works for invalid constraints
CREATE TABLE notnull_tbl1 (a int)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- these two should fail
alter table dropColumnchild drop column a
;
----------
[ADD_INDEX] CREATE INDEX macaddr_data_btree ON macaddr_data USING btree (b)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) information_schema.sql_parts
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE toasted_key ALTER COLUMN toasted_col1 SET STORAGE EXTERNAL
;
----------
[ALTER_LIBRARY] AlTER EXTENSION pg_stat_statements UPDATE TO '1.7'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER merge_ari AFTER INSERT ON target FOR EACH ROW EXECUTE PROCEDURE merge_trigfunc ()
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart4"."droppk_d" (
    "a" integer CONSTRAINT "droppk_a_not_null" NOT NULL
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table utrtest detach partition remp
;
----------
[RENAME_PROG_OBJ] -- OK

ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER ri_parent BEFORE UPDATE OR DELETE ON parent FOR EACH ROW EXECUTE PROCEDURE ri_parent()
;
----------
[ADD_INDEX] CREATE INDEX ios_bitmap_a ON ios_bitmap(a)
;
