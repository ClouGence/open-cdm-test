create temp table temp_parted_oncommit_test (a int)
  partition by list (a) on commit drop
;
-- adding a NOT NULL constraint will cause the scan to be skipped
ALTER TABLE list_parted2 DETACH PARTITION part_3_4
;
CREATE TRIGGER t
BEFORE UPDATE ON trigger_test
FOR EACH ROW EXECUTE PROCEDURE mytrigger()
;
ANALYZE pg_temp_18.pg_temp_26820_2
;
CREATE TABLE replayed(val integer)
;
/*
 * 3.0 Test ALTER TABLE DROP COLUMN.
 * Any column deletion leads to index deletion.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 int)
;
do $$
declare x var_record_colnn := null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
ALTER TABLE onek DROP CONSTRAINT onek_unique1_constraint_foo
;
-- not supported by sepgsql
ALTER TABLE regtest_table_part ALTER p SET DEFAULT 'XYZ'
;
CREATE FUNCTION isn_out(isbn)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (FULL) pg_catalog.pg_default_acl
;
CREATE TEMP TABLE y (
	col1 text,
	col2 text
)
;
COMMENT ON INDEX six IS 'add the comment back'
;
create table notnull_tbl5 (a int constraint foo not null constraint foo not null)
;
CREATE VIEW shoe AS
	SELECT sh.shoename,
		   sh.sh_avail,
		   sh.slcolor,
		   sh.slminlen,
		   sh.slminlen * un.un_fact AS slminlen_cm,
		   sh.slmaxlen,
		   sh.slmaxlen * un.un_fact AS slmaxlen_cm,
		   sh.slunit
	  FROM shoe_data sh, unit un
	 WHERE sh.slunit = un.un_name
;
create table perm_part_parent (a int) partition by list (a)
;
COMMENT ON COLUMN ctlt1.b IS 'B'
;
alter table alterlock set (autovacuum_enabled = off, fillfactor = 80)
;
CLUSTER cluster_part_tab USING cluster_part_ind
;
CREATE TABLE fail_part PARTITION OF list_parted2 FOR VALUES IN ('W', 'X', 'Y')
;
ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (ismn, ean13),
	OPERATOR 1  < (ismn, ismn13),
	OPERATOR 2  <= (ismn, ean13),
	OPERATOR 2  <= (ismn, ismn13),
	OPERATOR 3  = (ismn, ean13),
	OPERATOR 3  = (ismn, ismn13),
	OPERATOR 4  >= (ismn, ean13),
	OPERATOR 4  >= (ismn, ismn13),
	OPERATOR 5  > (ismn, ean13),
	OPERATOR 5  > (ismn, ismn13),
	FUNCTION 1  btismncmp(ismn, ean13),
	FUNCTION 1  btismncmp(ismn, ismn13)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."depth2" INHERIT "public"."depth1"
;
CREATE FUNCTION test_text_to_wchars(text, text) RETURNS int[]
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
CREATE FUNCTION trigger_func() RETURNS trigger LANGUAGE plpgsql AS '
BEGIN
	RAISE NOTICE ''trigger_func(%) called: action = %, when = %, level = %'', TG_ARGV[0], TG_OP, TG_WHEN, TG_LEVEL;
	RETURN NULL;
END;'
;
ALTER TABLE sch1.tab3 SET SCHEMA public
;
-- Also, detaching a partition that has the FK itself should work
-- https://postgr.es/m/CAAJ_b97GuPh6wQPbxQS-Zpy16Oh+0aMv-w64QcGrLhCOZZ6p+g@mail.gmail.com
CREATE TABLE ffk (a int, b int REFERENCES pk) PARTITION BY list (a)
;
DROP TABLE IF EXISTS tbl1_part
;
CREATE FUNCTION heap_page_item_attrs(
    IN page bytea,
    IN rel_oid regclass,
    IN do_detoast bool,
    OUT lp smallint,
    OUT lp_off smallint,
    OUT lp_flags smallint,
    OUT lp_len smallint,
    OUT t_xmin xid,
    OUT t_xmax xid,
    OUT t_field3 int4,
    OUT t_ctid tid,
    OUT t_infomask2 integer,
    OUT t_infomask integer,
    OUT t_hoff smallint,
    OUT t_bits text,
    OUT t_oid oid,
    OUT t_attrs bytea[]
    )
RETURNS SETOF record AS $$
SELECT lp,
       lp_off,
       lp_flags,
       lp_len,
       t_xmin,
       t_xmax,
       t_field3,
       t_ctid,
       t_infomask2,
       t_infomask,
       t_hoff,
       t_bits,
       t_oid,
       tuple_data_split(
         rel_oid,
         t_data,
         t_infomask,
         t_infomask2,
         t_bits,
         do_detoast)
         AS t_attrs
  FROM heap_page_items(page);
$$ LANGUAGE SQL PARALLEL SAFE
;
ALTER FOREIGN TABLE ft1 DROP CONSTRAINT no_const
;
-- only owner can change policies
ALTER POLICY pp1 ON part_document USING (true)
;
CREATE FUNCTION gtrgm_decompress(internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION isneq(isbn, isbn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- check in case of multi-level partitioned table
create table part_ee_ff partition of list_parted for values in ('ee', 'ff') partition by range (b)
;
ALTER TABLE regtest_ptable ADD COLUMN q int
;
ALTER FUNCTION gbt_time_compress(internal) PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_33124_index RENAME TO reind_con_toast_idx
;
ALTER TYPE split_domain_type_next.mood RENAME VALUE 'angry' TO 'upset'
;
DROP TABLE pg_temp_11.pg_temp_41099_2, pg_temp_11.pg_temp_41099
;
CLUSTER public.test1
;
create table hpart3 partition of hash_parted for values with (modulus 4, remainder 3)
;
CREATE TABLE "public"."fk_partitioned_fk_4_2" (
    "a" integer,
    "b" integer
)
;
/* contrib/pg_trgm/pg_trgm--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        11       pg_catalog.= (text, text)
;
DROP TEXT SEARCH TEMPLATE IF EXISTS no_such_schema.foo
;
ALTER TABLE upsert_test ATTACH PARTITION upsert_test_2 FOR VALUES IN (2)
;
ALTER TABLE public.test_inh_check ADD CONSTRAINT bmerged CHECK ((b > (1)::double precision))
;
DROP TABLE user_logins
;
-- fail

drop function anyctest(anycompatible, anycompatiblerange)
;
ALTER TABLE ONLY "public"."test_type_diff2_c3" INHERIT "public"."test_type_diff2"
;
-- ERROR
ALTER FOREIGN TABLE fd_pt2_1 ALTER c2 SET NOT NULL
;
ALTER INDEX attmp_idx ALTER COLUMN 2 SET STATISTICS -1
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create temp table temp_parted_oncommit_test (a int)
  partition by list (a) on commit drop
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- adding a NOT NULL constraint will cause the scan to be skipped
ALTER TABLE list_parted2 DETACH PARTITION part_3_4
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER t
BEFORE UPDATE ON trigger_test
FOR EACH ROW EXECUTE PROCEDURE mytrigger()
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_26820_2
;
----------
[CREATE_TABLE] CREATE TABLE replayed(val integer)
;
----------
[CREATE_TABLE] /*
 * 3.0 Test ALTER TABLE DROP COLUMN.
 * Any column deletion leads to index deletion.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 int)
;
----------
[BLOCK] do $$
declare x var_record_colnn := null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE onek DROP CONSTRAINT onek_unique1_constraint_foo
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- not supported by sepgsql
ALTER TABLE regtest_table_part ALTER p SET DEFAULT 'XYZ'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isn_out(isbn)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] CREATE TEMP TABLE y (
	col1 text,
	col2 text
)
;
----------
[COMMENT_INDEX] COMMENT ON INDEX six IS 'add the comment back'
;
----------
[CREATE_TABLE] create table notnull_tbl5 (a int constraint foo not null constraint foo not null)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW shoe AS
	SELECT sh.shoename,
		   sh.sh_avail,
		   sh.slcolor,
		   sh.slminlen,
		   sh.slminlen * un.un_fact AS slminlen_cm,
		   sh.slmaxlen,
		   sh.slmaxlen * un.un_fact AS slmaxlen_cm,
		   sh.slunit
	  FROM shoe_data sh, unit un
	 WHERE sh.slunit = un.un_name
;
----------
[CREATE_TABLE] create table perm_part_parent (a int) partition by list (a)
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN ctlt1.b IS 'B'
;
----------
[ALTER_TABLE] alter table alterlock set (autovacuum_enabled = off, fillfactor = 80)
;
----------
[ADMIN_TABLE] CLUSTER cluster_part_tab USING cluster_part_ind
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF list_parted2 FOR VALUES IN ('W', 'X', 'Y')
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (ismn, ean13),
	OPERATOR 1  < (ismn, ismn13),
	OPERATOR 2  <= (ismn, ean13),
	OPERATOR 2  <= (ismn, ismn13),
	OPERATOR 3  = (ismn, ean13),
	OPERATOR 3  = (ismn, ismn13),
	OPERATOR 4  >= (ismn, ean13),
	OPERATOR 4  >= (ismn, ismn13),
	OPERATOR 5  > (ismn, ean13),
	OPERATOR 5  > (ismn, ismn13),
	FUNCTION 1  btismncmp(ismn, ean13),
	FUNCTION 1  btismncmp(ismn, ismn13)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."depth2" INHERIT "public"."depth1"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_text_to_wchars(text, text) RETURNS int[]
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trigger_func() RETURNS trigger LANGUAGE plpgsql AS '
BEGIN
	RAISE NOTICE ''trigger_func(%) called: action = %, when = %, level = %'', TG_ARGV[0], TG_OP, TG_WHEN, TG_LEVEL;
	RETURN NULL;
END;'
;
----------
[ALTER_TABLE] ALTER TABLE sch1.tab3 SET SCHEMA public
;
----------
[CREATE_TABLE] -- Also, detaching a partition that has the FK itself should work
-- https://postgr.es/m/CAAJ_b97GuPh6wQPbxQS-Zpy16Oh+0aMv-w64QcGrLhCOZZ6p+g@mail.gmail.com
CREATE TABLE ffk (a int, b int REFERENCES pk) PARTITION BY list (a)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS tbl1_part
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION heap_page_item_attrs(
    IN page bytea,
    IN rel_oid regclass,
    IN do_detoast bool,
    OUT lp smallint,
    OUT lp_off smallint,
    OUT lp_flags smallint,
    OUT lp_len smallint,
    OUT t_xmin xid,
    OUT t_xmax xid,
    OUT t_field3 int4,
    OUT t_ctid tid,
    OUT t_infomask2 integer,
    OUT t_infomask integer,
    OUT t_hoff smallint,
    OUT t_bits text,
    OUT t_oid oid,
    OUT t_attrs bytea[]
    )
RETURNS SETOF record AS $$
SELECT lp,
       lp_off,
       lp_flags,
       lp_len,
       t_xmin,
       t_xmax,
       t_field3,
       t_ctid,
       t_infomask2,
       t_infomask,
       t_hoff,
       t_bits,
       t_oid,
       tuple_data_split(
         rel_oid,
         t_data,
         t_infomask,
         t_infomask2,
         t_bits,
         do_detoast)
         AS t_attrs
  FROM heap_page_items(page);
$$ LANGUAGE SQL PARALLEL SAFE
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER FOREIGN TABLE ft1 DROP CONSTRAINT no_const
;
----------
[ALTER_POLICY] -- only owner can change policies
ALTER POLICY pp1 ON part_document USING (true)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_decompress(internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(isbn, isbn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- check in case of multi-level partitioned table
create table part_ee_ff partition of list_parted for values in ('ee', 'ff') partition by range (b)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE regtest_ptable ADD COLUMN q int
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_time_compress(internal) PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33124_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TYPE] ALTER TYPE split_domain_type_next.mood RENAME VALUE 'angry' TO 'upset'
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_41099_2, pg_temp_11.pg_temp_41099
;
----------
[ADMIN_TABLE] CLUSTER public.test1
;
----------
[CREATE_TABLE] create table hpart3 partition of hash_parted for values with (modulus 4, remainder 3)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_4_2" (
    "a" integer,
    "b" integer
)
;
----------
[ALTER_PROG_OBJ] /* contrib/pg_trgm/pg_trgm--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        11       pg_catalog.= (text, text)
;
----------
[DROP_POLICY] DROP TEXT SEARCH TEMPLATE IF EXISTS no_such_schema.foo
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE upsert_test ATTACH PARTITION upsert_test_2 FOR VALUES IN (2)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.test_inh_check ADD CONSTRAINT bmerged CHECK ((b > (1)::double precision))
;
----------
[DROP_TABLE] DROP TABLE user_logins
;
----------
[DROP_PROG_OBJ] -- fail

drop function anyctest(anycompatible, anycompatiblerange)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."test_type_diff2_c3" INHERIT "public"."test_type_diff2"
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ERROR
ALTER FOREIGN TABLE fd_pt2_1 ALTER c2 SET NOT NULL
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX attmp_idx ALTER COLUMN 2 SET STATISTICS -1
;
