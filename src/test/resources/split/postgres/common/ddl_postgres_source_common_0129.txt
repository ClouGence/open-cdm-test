-- currently, must respecify PKEY for each inherited subtable
create table test_1 (id integer primary key) inherits (id)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29634 host=/tmp/ru_HfKRoh1 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_forall
;
create temp table t (a int unique, b int)
;
CREATE DOMAIN "public"."con" AS integer
	CONSTRAINT "con_check" CHECK ((VALUE > 0))
;
CREATE SCHEMA testschema
;
ALTER TABLE T ALTER COLUMN c_bpchar    DROP DEFAULT,
              ALTER COLUMN c_date      DROP DEFAULT,
              ALTER COLUMN c_text      DROP DEFAULT,
              ALTER COLUMN c_timestamp DROP DEFAULT,
              ALTER COLUMN c_array     DROP DEFAULT,
              ALTER COLUMN c_small     DROP DEFAULT,
              ALTER COLUMN c_big       DROP DEFAULT,
              ALTER COLUMN c_num       DROP DEFAULT,
              ALTER COLUMN c_time      DROP DEFAULT,
              ALTER COLUMN c_hugetext  DROP DEFAULT
;
--
-- Test cases involving OUT parameters
--

CREATE FUNCTION rngfunc(in f1 int, out f2 int)
AS 'select $1+1' LANGUAGE sql
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_transform
;
alter type insenum add value 'i20' before 'L2'
;
--
-- test transition tables with MERGE
--
create table merge_target_table (a int primary key, b text)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
create type two_ints as (a int, b int)
;
VACUUM (FREEZE) information_schema.sql_features
;
ALTER FUNCTION gbt_text_same(gbtreekey_var, gbtreekey_var, internal) PARALLEL SAFE
;
CREATE TABLE partkey_t (a int4) PARTITION BY RANGE (a test_int4_ops)
;
CREATE INDEX idx ON tbl(i)
;
ALTER TABLE part ATTACH PARTITION part2 FOR VALUES FROM (101) to (200)
;
truncate test1
;
CREATE TABLE cluster_part_tab (a int) PARTITION BY LIST (a)
;
CREATE FUNCTION "public"."stricttest"() RETURNS "void"
    LANGUAGE "plpgsql"
    AS $$
-- override the global
#print_strict_params on
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- too many rows
  select * from foo where f1 > p1 or f1::text = p3  into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$
;
ALTER SUBSCRIPTION tsub SET (binary = true)
;
-- Test updating of column numbers in statistics expressions (bug #18468)
CREATE TABLE test_like_6 (a int, c text, b text)
;
CREATE TABLE should_not_replay_insertion(a int)
;
CREATE CAST (issn AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
CREATE SUBSCRIPTION "regress_sub5" CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION "regress_pub5" WITH (connect = false, slot_name = 'regress_sub5', streaming = parallel)
;
ALTER TYPE rainbow RENAME VALUE 'blue' TO 'green'
;
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
--
-- check that using the module's functions with unsupported relations will fail
--

-- partitioned tables (the parent ones) don't have visibility maps
create table test_partitioned (a int) partition by list (a)
;
CREATE TEMP TABLE fktable (
    id int primary key,
    fk int references pktable deferrable initially deferred
)
;
ALTER TABLE test_tsquery ADD COLUMN sample tsquery
;
-- this is different grammatically
CREATE INDEX collate_test1_idx4 ON collate_test1 (((b||'foo') COLLATE "POSIX"))
;
-- ===================================================================
-- used in PL/pgSQL function
-- ===================================================================
CREATE OR REPLACE FUNCTION f_test(p_c1 int) RETURNS int AS $$
DECLARE
	v_c1 int;
BEGIN
    SELECT c1 INTO v_c1 FROM ft1 WHERE c1 = p_c1 LIMIT 1;
    PERFORM c1 FROM ft1 WHERE c1 = p_c1 AND p_c1 = v_c1 LIMIT 1;
    RETURN v_c1;
END;
$$ LANGUAGE plpgsql
;
ANALYZE pred_tab
;
DROP FUNCTION IF EXISTS split_agg_probe.sum_variadic_state(integer, VARIADIC integer[])
;
CREATE TABLE tab_rowfilter_inherited (a int)
;
CREATE TABLE dump_test.test_table_nn_chld3 (
							) INHERITS (dump_test.test_table_nn, dump_test.test_table_nn_2)
;
CREATE TYPE enum_test AS ENUM ('foo', 'bar', 'baz')
;
CREATE INDEX idx_timestamp ON test_timestamp USING gin (i)
;
-- It will fail because the attach operation implicitly validates the data.
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2 FOR VALUES FROM (1000,1000) TO (2000,2000)
;
CREATE DATABASE tbuiltin8 TEMPLATE template0 LOCALE 'C' LOCALE_PROVIDER builtin ICU_RULES '""'
;
CREATE INDEX CONCURRENTLY concur_index7 ON concur_heap(f1)
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest10a" DROP COLUMN "........pg.dropped.2........"
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) information_schema.sql_features
;
ALTER FOREIGN TABLE split_fdw_repl.remote_accounts DROP COLUMN note2
;
ANALYZE pg_temp_4.pg_temp_37962
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub1
;
ALTER TABLE part_c_100_200 DROP COLUMN b
;
-- Fall back to full rewrite for volatile expressions
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY)
;
-- should fail
alter table parted_uniq_detach_test detach partition parted_uniq_detach_test1
;
ALTER TABLE pg_temp_2.pg_temp_30214_2 ADD COLUMN newdata pg_temp_2.pg_temp_30214
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- currently, must respecify PKEY for each inherited subtable
create table test_1 (id integer primary key) inherits (id)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29634 host=/tmp/ru_HfKRoh1 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_forall
;
----------
[CREATE_TABLE] create temp table t (a int unique, b int)
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."con" AS integer
	CONSTRAINT "con_check" CHECK ((VALUE > 0))
;
----------
[CREATE_SCHEMA] CREATE SCHEMA testschema
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE T ALTER COLUMN c_bpchar    DROP DEFAULT,
              ALTER COLUMN c_date      DROP DEFAULT,
              ALTER COLUMN c_text      DROP DEFAULT,
              ALTER COLUMN c_timestamp DROP DEFAULT,
              ALTER COLUMN c_array     DROP DEFAULT,
              ALTER COLUMN c_small     DROP DEFAULT,
              ALTER COLUMN c_big       DROP DEFAULT,
              ALTER COLUMN c_num       DROP DEFAULT,
              ALTER COLUMN c_time      DROP DEFAULT,
              ALTER COLUMN c_hugetext  DROP DEFAULT
;
----------
[CREATE_PROG_OBJ] --
-- Test cases involving OUT parameters
--

CREATE FUNCTION rngfunc(in f1 int, out f2 int)
AS 'select $1+1' LANGUAGE sql
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_transform
;
----------
[ALTER_TYPE] alter type insenum add value 'i20' before 'L2'
;
----------
[CREATE_TABLE] --
-- test transition tables with MERGE
--
create table merge_target_table (a int primary key, b text)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
----------
[CREATE_TYPE] create type two_ints as (a int, b int)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) information_schema.sql_features
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_text_same(gbtreekey_var, gbtreekey_var, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE partkey_t (a int4) PARTITION BY RANGE (a test_int4_ops)
;
----------
[ADD_INDEX] CREATE INDEX idx ON tbl(i)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part ATTACH PARTITION part2 FOR VALUES FROM (101) to (200)
;
----------
[TRUNCATE_TABLE] truncate test1
;
----------
[CREATE_TABLE] CREATE TABLE cluster_part_tab (a int) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."stricttest"() RETURNS "void"
    LANGUAGE "plpgsql"
    AS $$
-- override the global
#print_strict_params on
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- too many rows
  select * from foo where f1 > p1 or f1::text = p3  into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tsub SET (binary = true)
;
----------
[CREATE_TABLE] -- Test updating of column numbers in statistics expressions (bug #18468)
CREATE TABLE test_like_6 (a int, c text, b text)
;
----------
[CREATE_TABLE] CREATE TABLE should_not_replay_insertion(a int)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (issn AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION "regress_sub5" CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION "regress_pub5" WITH (connect = false, slot_name = 'regress_sub5', streaming = parallel)
;
----------
[ALTER_TYPE] ALTER TYPE rainbow RENAME VALUE 'blue' TO 'green'
;
----------
[BLOCK] DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
----------
[CREATE_TABLE] --
-- check that using the module's functions with unsupported relations will fail
--

-- partitioned tables (the parent ones) don't have visibility maps
create table test_partitioned (a int) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE fktable (
    id int primary key,
    fk int references pktable deferrable initially deferred
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_tsquery ADD COLUMN sample tsquery
;
----------
[ADD_INDEX] -- this is different grammatically
CREATE INDEX collate_test1_idx4 ON collate_test1 (((b||'foo') COLLATE "POSIX"))
;
----------
[CREATE_PROG_OBJ] -- ===================================================================
-- used in PL/pgSQL function
-- ===================================================================
CREATE OR REPLACE FUNCTION f_test(p_c1 int) RETURNS int AS $$
DECLARE
	v_c1 int;
BEGIN
    SELECT c1 INTO v_c1 FROM ft1 WHERE c1 = p_c1 LIMIT 1;
    PERFORM c1 FROM ft1 WHERE c1 = p_c1 AND p_c1 = v_c1 LIMIT 1;
    RETURN v_c1;
END;
$$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] ANALYZE pred_tab
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS split_agg_probe.sum_variadic_state(integer, VARIADIC integer[])
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_inherited (a int)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_nn_chld3 (
							) INHERITS (dump_test.test_table_nn, dump_test.test_table_nn_2)
;
----------
[CREATE_TYPE] CREATE TYPE enum_test AS ENUM ('foo', 'bar', 'baz')
;
----------
[ADD_INDEX] CREATE INDEX idx_timestamp ON test_timestamp USING gin (i)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- It will fail because the attach operation implicitly validates the data.
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2 FOR VALUES FROM (1000,1000) TO (2000,2000)
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin8 TEMPLATE template0 LOCALE 'C' LOCALE_PROVIDER builtin ICU_RULES '""'
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY concur_index7 ON concur_heap(f1)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_virtual_tests"."gtest10a" DROP COLUMN "........pg.dropped.2........"
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) information_schema.sql_features
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE split_fdw_repl.remote_accounts DROP COLUMN note2
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37962
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub1
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE part_c_100_200 DROP COLUMN b
;
----------
[CREATE_TABLE] -- Fall back to full rewrite for volatile expressions
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- should fail
alter table parted_uniq_detach_test detach partition parted_uniq_detach_test1
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_2.pg_temp_30214_2 ADD COLUMN newdata pg_temp_2.pg_temp_30214
;
