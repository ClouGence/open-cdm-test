CREATE TABLE t5 (e text, f text, g text)
;
DROP TABLE toast_4b_utf8
;
CREATE FUNCTION "public"."shadowtest"("f1" integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
declare f1 int; begin return 1; end $$
;
ALTER OPERATOR FAMILY gist_bit_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE FUNCTION test_create(
shared bool)
RETURNS void STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
CREATE TABLE blog (id integer, author text, post text)
;
create index idxparti3 on idxpart ((a+b)) where d = true
;
ALTER TABLE temp_view_test.tt1 RENAME TO tmp1
;
create table c2(f3 int) inherits(p1,p2)
;
ALTER TABLE pg_default_acl ADD UNIQUE USING INDEX pg_default_acl_role_nsp_obj_index
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) information_schema.sql_sizing
;
create index ab_a2_b2_a_idx on ab_a2_b2 (a)
;
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, 0 AS c FROM viewtest_tbl
;
ALTER TABLE atest6 ADD COLUMN three integer
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_event_trigger
;
CREATE MATERIALIZED VIEW mv_unspecified_types AS
  SELECT 42 as i, 42.5 as num, 'foo' as u, 'foo'::unknown as u2, null as n
;
drop table agg_group_4
;
CREATE VIEW foo AS SELECT 1 INTO int4_tbl
;
-- no such table/domain
COMMENT ON CONSTRAINT the_constraint ON no_comments_tbl IS 'bad comment'
;
create or replace function forc01() returns void as $$
declare
  c cursor(r1 integer, r2 integer)
       for select * from generate_series(r1,r2) i;
  c2 cursor
       for select * from generate_series(41,43) i;
begin
  -- assign portal names to cursors to get stable output
  c := 'c';
  c2 := 'c2';
  for r in c(5,7) loop
    raise notice '% from %', r.i, c;
  end loop;
  -- again, to test if cursor was closed properly
  -- (and while we're at it, test named-parameter notation)
  for r in c(r2 := 10, r1 => 9) loop
    raise notice '% from %', r.i, c;
  end loop;
  -- and test a parameterless cursor
  for r in c2 loop
    raise notice '% from %', r.i, c2;
  end loop;
  -- and try it with a hand-assigned name
  raise notice 'after loop, c2 = %', c2;
  c2 := 'special_name';
  for r in c2 loop
    raise notice '% from %', r.i, c2;
  end loop;
  raise notice 'after loop, c2 = %', c2;
  -- and try it with a generated name
  -- (which we can't show in the output because it's variable)
  c2 := null;
  for r in c2 loop
    raise notice '%', r.i;
  end loop;
  raise notice 'after loop, c2 = %', c2;
  return;
end;
$$ language plpgsql
;
CREATE FUNCTION gbt_time_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TYPE "public"."int8alias1"
;
ALTER SEQUENCE "public"."sj_t3_id_seq" OWNED BY "public"."sj_t3"."id"
;
CREATE TABLE test_like_4b (LIKE test_like_4 INCLUDING DEFAULTS)
;
-- ok
CREATE TRANSFORM FOR hstore LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
CREATE FUNCTION "public"."int4_casttesttype"(integer) RETURNS "public"."casttesttype"
    LANGUAGE "sql"
    AS $_$ SELECT ('foo'::text || $1::text)::casttesttype; $_$
;
CREATE FUNCTION isnle(issn, issn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION plsample_result_void(a1 text[])
RETURNS VOID
AS $$
  Example of source with void result.
$$ LANGUAGE plsample
;
create table test2 (id serial, t text)
;
CREATE TABLE batch_table ( x int )
;
create function tg_pfield_au() returns trigger as '
begin
    if new.name != old.name then
        update PSlot set pfname = new.name where pfname = old.name;
    end if;
    return new;
end;
' language plpgsql
;
CREATE TABLE dump_test.test_table_identity (
						   col1 int generated always as identity primary key,
						   col2 text
					   )
;
TRUNCATE TIMESTAMP_TBL
;
-- Test synonym dictionary in configuration
CREATE TEXT SEARCH CONFIGURATION synonym_tst (
						COPY=english
)
;
CREATE TABLE test_tab2(a int primary key)
;
alter operator family integer_ops using btree add
  operator 3 = (int8, int8alias1)
;
CREATE TRIGGER trig_row_before_insupd
BEFORE INSERT OR UPDATE ON rem1
FOR EACH ROW
WHEN (NEW.f2 like '%update%')
EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
DROP EXTENSION tsm_system_rows
;
CREATE TABLE split_builtin_trigger.messages_by_column (
    id integer PRIMARY KEY,
    text_search_config regconfig NOT NULL DEFAULT 'pg_catalog.english',
    title text,
    body text,
    document tsvector
)
;
CREATE FUNCTION transition_table_level2_ri_child_insupd_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
  BEGIN
    PERFORM FROM i
      LEFT JOIN transition_table_level1 p
        ON p.level1_no IS NOT NULL AND p.level1_no = i.parent_no
      WHERE p.level1_no IS NULL;
    IF FOUND THEN
      RAISE EXCEPTION 'RI error';
    END IF;
    RETURN NULL;
  END;
$$
;
CREATE FUNCTION gin_extract_query_int8(int8, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE FOREIGN TABLE foreign_tbl (a text, b int)
  SERVER loopback OPTIONS (table_name 'local_tbl')
;
create table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) partition by hash (aid) tablespace "regress_pgbench_tap_1_ts"
;
CREATE TYPE "public"."city_budget" (
    INTERNALLENGTH = 16,
    INPUT = "public"."int44in",
    OUTPUT = "public"."int44out",
    SUBSCRIPT = "raw_array_subscript_handler",
    ELEMENT = integer,
    CATEGORY = 'x',
    PREFERRED = true,
    ALIGNMENT = int4,
    STORAGE = plain
)
;
CREATE FUNCTION dmetaphone_alt (text) RETURNS text
AS '$libdir/fuzzystrmatch', 'dmetaphone_alt'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- ERROR
DROP TABLE use_ft1_column_type
;
VACUUM pg_catalog.pg_authid
;
-- test handling of infinite timestamp values
CREATE TABLE brin_timestamp_test(a TIMESTAMP)
;
CREATE OPERATOR && (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_overlap,
	COMMUTATOR = '&&',
	RESTRICT = _int_overlap_sel,
	JOIN = _int_overlap_joinsel
)
;
CREATE TEMP TABLE foo (f1 int, f2 int, f3 int, f4 int)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE t5 (e text, f text, g text)
;
----------
[DROP_TABLE] DROP TABLE toast_4b_utf8
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."shadowtest"("f1" integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
declare f1 int; begin return 1; end $$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bit_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_create(
shared bool)
RETURNS void STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE blog (id integer, author text, post text)
;
----------
[ADD_INDEX] create index idxparti3 on idxpart ((a+b)) where d = true
;
----------
[RENAME_TABLE] ALTER TABLE temp_view_test.tt1 RENAME TO tmp1
;
----------
[CREATE_TABLE] create table c2(f3 int) inherits(p1,p2)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_default_acl ADD UNIQUE USING INDEX pg_default_acl_role_nsp_obj_index
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) information_schema.sql_sizing
;
----------
[ADD_INDEX] create index ab_a2_b2_a_idx on ab_a2_b2 (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, 0 AS c FROM viewtest_tbl
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE atest6 ADD COLUMN three integer
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_event_trigger
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mv_unspecified_types AS
  SELECT 42 as i, 42.5 as num, 'foo' as u, 'foo'::unknown as u2, null as n
;
----------
[DROP_TABLE] drop table agg_group_4
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW foo AS SELECT 1 INTO int4_tbl
;
----------
[COMMENT_CONSTRAINT] -- no such table/domain
COMMENT ON CONSTRAINT the_constraint ON no_comments_tbl IS 'bad comment'
;
----------
[CREATE_PROG_OBJ] create or replace function forc01() returns void as $$
declare
  c cursor(r1 integer, r2 integer)
       for select * from generate_series(r1,r2) i;
  c2 cursor
       for select * from generate_series(41,43) i;
begin
  -- assign portal names to cursors to get stable output
  c := 'c';
  c2 := 'c2';
  for r in c(5,7) loop
    raise notice '% from %', r.i, c;
  end loop;
  -- again, to test if cursor was closed properly
  -- (and while we're at it, test named-parameter notation)
  for r in c(r2 := 10, r1 => 9) loop
    raise notice '% from %', r.i, c;
  end loop;
  -- and test a parameterless cursor
  for r in c2 loop
    raise notice '% from %', r.i, c2;
  end loop;
  -- and try it with a hand-assigned name
  raise notice 'after loop, c2 = %', c2;
  c2 := 'special_name';
  for r in c2 loop
    raise notice '% from %', r.i, c2;
  end loop;
  raise notice 'after loop, c2 = %', c2;
  -- and try it with a generated name
  -- (which we can't show in the output because it's variable)
  c2 := null;
  for r in c2 loop
    raise notice '%', r.i;
  end loop;
  raise notice 'after loop, c2 = %', c2;
  return;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TYPE] CREATE TYPE "public"."int8alias1"
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."sj_t3_id_seq" OWNED BY "public"."sj_t3"."id"
;
----------
[CREATE_TABLE] CREATE TABLE test_like_4b (LIKE test_like_4 INCLUDING DEFAULTS)
;
----------
[CREATE_TRANSFORM] -- ok
CREATE TRANSFORM FOR hstore LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int4_casttesttype"(integer) RETURNS "public"."casttesttype"
    LANGUAGE "sql"
    AS $_$ SELECT ('foo'::text || $1::text)::casttesttype; $_$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(issn, issn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION plsample_result_void(a1 text[])
RETURNS VOID
AS $$
  Example of source with void result.
$$ LANGUAGE plsample
;
----------
[CREATE_TABLE] create table test2 (id serial, t text)
;
----------
[CREATE_TABLE] CREATE TABLE batch_table ( x int )
;
----------
[CREATE_PROG_OBJ] create function tg_pfield_au() returns trigger as '
begin
    if new.name != old.name then
        update PSlot set pfname = new.name where pfname = old.name;
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_identity (
						   col1 int generated always as identity primary key,
						   col2 text
					   )
;
----------
[TRUNCATE_TABLE] TRUNCATE TIMESTAMP_TBL
;
----------
[CREATE_POLICY] -- Test synonym dictionary in configuration
CREATE TEXT SEARCH CONFIGURATION synonym_tst (
						COPY=english
)
;
----------
[CREATE_TABLE] CREATE TABLE test_tab2(a int primary key)
;
----------
[ALTER_PROG_OBJ] alter operator family integer_ops using btree add
  operator 3 = (int8, int8alias1)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_before_insupd
BEFORE INSERT OR UPDATE ON rem1
FOR EACH ROW
WHEN (NEW.f2 like '%update%')
EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[DROP_LIBRARY] DROP EXTENSION tsm_system_rows
;
----------
[CREATE_TABLE] CREATE TABLE split_builtin_trigger.messages_by_column (
    id integer PRIMARY KEY,
    text_search_config regconfig NOT NULL DEFAULT 'pg_catalog.english',
    title text,
    body text,
    document tsvector
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION transition_table_level2_ri_child_insupd_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
  BEGIN
    PERFORM FROM i
      LEFT JOIN transition_table_level1 p
        ON p.level1_no IS NOT NULL AND p.level1_no = i.parent_no
      WHERE p.level1_no IS NULL;
    IF FOUND THEN
      RAISE EXCEPTION 'RI error';
    END IF;
    RETURN NULL;
  END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_int8(int8, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE foreign_tbl (a text, b int)
  SERVER loopback OPTIONS (table_name 'local_tbl')
;
----------
[CREATE_TABLE] create table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) partition by hash (aid) tablespace "regress_pgbench_tap_1_ts"
;
----------
[CREATE_TYPE] CREATE TYPE "public"."city_budget" (
    INTERNALLENGTH = 16,
    INPUT = "public"."int44in",
    OUTPUT = "public"."int44out",
    SUBSCRIPT = "raw_array_subscript_handler",
    ELEMENT = integer,
    CATEGORY = 'x',
    PREFERRED = true,
    ALIGNMENT = int4,
    STORAGE = plain
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dmetaphone_alt (text) RETURNS text
AS '$libdir/fuzzystrmatch', 'dmetaphone_alt'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] -- ERROR
DROP TABLE use_ft1_column_type
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_authid
;
----------
[CREATE_TABLE] -- test handling of infinite timestamp values
CREATE TABLE brin_timestamp_test(a TIMESTAMP)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR && (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_overlap,
	COMMUTATOR = '&&',
	RESTRICT = _int_overlap_sel,
	JOIN = _int_overlap_joinsel
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE foo (f1 int, f2 int, f3 int, f4 int)
;
