CREATE TABLE test_replica_identity_t3 (id serial constraint pk primary key deferrable)
;
create table idxpart1 (col2 int, b int, col1 int, a int)
;
create index on part_abc (a)
;
create table hpart2 partition of hash_parted for values with (modulus 4, remainder 2)
;
CREATE TABLE fract_t0 PARTITION OF fract_t FOR VALUES FROM ('0') TO ('1000')
;
create table pgbench_history(tid int,bid int,aid    int,delta int,mtime timestamp,filler char(22)) tablespace "regress_pgbench_tap_1_ts"
;
ANALYZE public.prt3_n_p3
;
CREATE TEMP VIEW voo AS SELECT f1, f2 FROM foo
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_seclabel
;
DROP TABLE r2
;
-- partitioned table with index
CREATE TABLE vacparted_i (a int primary key, b varchar(100))
  PARTITION BY HASH (a)
;
alter table mlparted add e int, add d int
;
CREATE TABLESPACE regress_tblspace LOCATION '/src/src/test/regress/testtablespace'
;
/*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
-- fail
CREATE TABLE itest15 (id integer GENERATED ALWAYS AS IDENTITY NOT NULL)
;
DROP TABLE a
;
create domain plpgsql_arr_domain as int[] check(plpgsql_arr_domain_check(value))
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=30997 host=/tmp/0D8xBxChJ8 dbname=postgres' PUBLICATION tap_pub
;
alter table only dropColumnChild drop column b
;
--
-- Test case for bug #4290: bogus calculation of subplan param sets
--

create temp table ta (id int primary key, val int)
;
-- fails
DROP EXTENSION test_ext_req_schema3
;
create table IFace (
    slotname	char(20),
    sysname	text,
    ifname	text,
    slotlink	char(20)
)
;
ALTER TABLE part_2 RENAME COLUMN b to c
;
ALTER TABLE gtest21b ALTER COLUMN b DROP NOT NULL
;
CREATE FUNCTION isnge(issn13, issn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
TRUNCATE TABLE trunc_a21 CASCADE
;
CREATE TABLE test (a text[],b text[])
;
ANALYZE regress_indexing.pk52
;
CREATE PUBLICATION "dump_pub_nsp_2ct" WITH (publish = 'insert, update, delete, truncate')
;
create table bool_rp_false_1k partition of bool_rp for values from (false,0) to (false,1000)
;
ALTER TABLE pg_constraint ADD UNIQUE USING INDEX pg_constraint_conrelid_contypid_conname_index
;
--
-- HASH
--
CREATE UNLOGGED TABLE unlogged_hash_table (id int4)
;
--GiST support for ltree[]
CREATE FUNCTION _ltree_consistent(internal,_ltree,int2,oid,internal)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP TABLE toasted_several
;
CREATE FUNCTION texticnlike(citext, text)
RETURNS bool AS 'texticnlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE pg_temp_13.pg_temp_25962_2 ADD COLUMN newdata pg_temp_13.pg_temp_25962
;
CREATE TABLE FKTABLE (ftest1 cidr, ftest2 timestamp, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest1, ptest2))
;
CREATE TRIGGER trig_stmt_before BEFORE DELETE OR INSERT OR UPDATE OR TRUNCATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
CREATE TABLE test12fk (a int, b text COLLATE case_insensitive REFERENCES test12pk (x) ON UPDATE NO ACTION)
;
--
-- GIN functions
--

--
-- gin_metapage_info()
--
CREATE FUNCTION gin_metapage_info(IN page bytea,
    OUT pending_head bigint,
    OUT pending_tail bigint,
    OUT tail_free_size int4,
    OUT n_pending_pages bigint,
    OUT n_pending_tuples bigint,
    OUT n_total_pages bigint,
    OUT n_entry_pages bigint,
    OUT n_data_pages bigint,
    OUT n_entries bigint,
    OUT version int4)
AS '$libdir/pageinspect', 'gin_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=-30.1)
;
CREATE VIEW pg_stat_progress_basebackup AS
    SELECT
        S.pid AS pid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'waiting for checkpoint to finish'
                      WHEN 2 THEN 'estimating backup size'
                      WHEN 3 THEN 'streaming database files'
                      WHEN 4 THEN 'waiting for wal archiving to finish'
                      WHEN 5 THEN 'transferring wal files'
                      END AS phase,
        CASE S.param2 WHEN -1 THEN NULL ELSE S.param2 END AS backup_total,
        S.param3 AS backup_streamed,
        S.param4 AS tablespaces_total,
        S.param5 AS tablespaces_streamed
    FROM pg_stat_get_progress_info('BASEBACKUP') AS S
;
/* Then we can drop them */
DROP VIEW pg_stat_statements
;
CREATE UNIQUE INDEX num_exp_sqrt_idx ON num_exp_sqrt (id)
;
create schema s2
  create table abc (f1 int)
;
---
-- partitioned table parent
ALTER TABLE regtest_ptable ADD COLUMN d float
;
CREATE PUBLICATION tpub FOR ALL TABLES
;
-- ok: attaching first partition works, because (a) is in column list
ALTER TABLE testpub_tbl8 ATTACH PARTITION testpub_tbl8_0 FOR VALUES WITH (modulus 2, remainder 0)
;
CREATE AGGREGATE sum_int_randomrestart (int4)
(
	stype = int4,
	sfunc = int4pl,
	mstype = int4,
	msfunc = int4pl,
	minvfunc = sum_int_randrestart_minvfunc
)
;
ALTER TABLE temporal_pk REPLICA IDENTITY NOTHING
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE test_replica_identity_t3 (id serial constraint pk primary key deferrable)
;
----------
[CREATE_TABLE] create table idxpart1 (col2 int, b int, col1 int, a int)
;
----------
[ADD_INDEX] create index on part_abc (a)
;
----------
[CREATE_TABLE] create table hpart2 partition of hash_parted for values with (modulus 4, remainder 2)
;
----------
[CREATE_TABLE] CREATE TABLE fract_t0 PARTITION OF fract_t FOR VALUES FROM ('0') TO ('1000')
;
----------
[CREATE_TABLE] create table pgbench_history(tid int,bid int,aid    int,delta int,mtime timestamp,filler char(22)) tablespace "regress_pgbench_tap_1_ts"
;
----------
[ADMIN_TABLE] ANALYZE public.prt3_n_p3
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW voo AS SELECT f1, f2 FROM foo
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_seclabel
;
----------
[DROP_TABLE] DROP TABLE r2
;
----------
[CREATE_TABLE] -- partitioned table with index
CREATE TABLE vacparted_i (a int primary key, b varchar(100))
  PARTITION BY HASH (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table mlparted add e int, add d int
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspace LOCATION '/src/src/test/regress/testtablespace'
;
----------
[CREATE_POLICY] /*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
----------
[CREATE_TABLE] -- fail
CREATE TABLE itest15 (id integer GENERATED ALWAYS AS IDENTITY NOT NULL)
;
----------
[DROP_TABLE] DROP TABLE a
;
----------
[CREATE_TYPE] create domain plpgsql_arr_domain as int[] check(plpgsql_arr_domain_check(value))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=30997 host=/tmp/0D8xBxChJ8 dbname=postgres' PUBLICATION tap_pub
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table only dropColumnChild drop column b
;
----------
[CREATE_TABLE] --
-- Test case for bug #4290: bogus calculation of subplan param sets
--

create temp table ta (id int primary key, val int)
;
----------
[DROP_LIBRARY] -- fails
DROP EXTENSION test_ext_req_schema3
;
----------
[CREATE_TABLE] create table IFace (
    slotname	char(20),
    sysname	text,
    ifname	text,
    slotlink	char(20)
)
;
----------
[RENAME_COLUMN] ALTER TABLE part_2 RENAME COLUMN b to c
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest21b ALTER COLUMN b DROP NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(issn13, issn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE trunc_a21 CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE test (a text[],b text[])
;
----------
[ADMIN_TABLE] ANALYZE regress_indexing.pk52
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "dump_pub_nsp_2ct" WITH (publish = 'insert, update, delete, truncate')
;
----------
[CREATE_TABLE] create table bool_rp_false_1k partition of bool_rp for values from (false,0) to (false,1000)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_constraint ADD UNIQUE USING INDEX pg_constraint_conrelid_contypid_conname_index
;
----------
[CREATE_TABLE] --
-- HASH
--
CREATE UNLOGGED TABLE unlogged_hash_table (id int4)
;
----------
[CREATE_PROG_OBJ] --GiST support for ltree[]
CREATE FUNCTION _ltree_consistent(internal,_ltree,int2,oid,internal)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE toasted_several
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION texticnlike(citext, text)
RETURNS bool AS 'texticnlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_25962_2 ADD COLUMN newdata pg_temp_13.pg_temp_25962
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 cidr, ftest2 timestamp, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest1, ptest2))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_stmt_before BEFORE DELETE OR INSERT OR UPDATE OR TRUNCATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
----------
[CREATE_TABLE] CREATE TABLE test12fk (a int, b text COLLATE case_insensitive REFERENCES test12pk (x) ON UPDATE NO ACTION)
;
----------
[CREATE_PROG_OBJ] --
-- GIN functions
--

--
-- gin_metapage_info()
--
CREATE FUNCTION gin_metapage_info(IN page bytea,
    OUT pending_head bigint,
    OUT pending_tail bigint,
    OUT tail_free_size int4,
    OUT n_pending_pages bigint,
    OUT n_pending_tuples bigint,
    OUT n_total_pages bigint,
    OUT n_entry_pages bigint,
    OUT n_data_pages bigint,
    OUT n_entries bigint,
    OUT version int4)
AS '$libdir/pageinspect', 'gin_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=-30.1)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_progress_basebackup AS
    SELECT
        S.pid AS pid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'waiting for checkpoint to finish'
                      WHEN 2 THEN 'estimating backup size'
                      WHEN 3 THEN 'streaming database files'
                      WHEN 4 THEN 'waiting for wal archiving to finish'
                      WHEN 5 THEN 'transferring wal files'
                      END AS phase,
        CASE S.param2 WHEN -1 THEN NULL ELSE S.param2 END AS backup_total,
        S.param3 AS backup_streamed,
        S.param4 AS tablespaces_total,
        S.param5 AS tablespaces_streamed
    FROM pg_stat_get_progress_info('BASEBACKUP') AS S
;
----------
[DROP_VIEW] /* Then we can drop them */
DROP VIEW pg_stat_statements
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX num_exp_sqrt_idx ON num_exp_sqrt (id)
;
----------
[CREATE_SCHEMA] create schema s2
  create table abc (f1 int)
;
----------
[ALTER_TABLE|ADD_COLUMN] ---
-- partitioned table parent
ALTER TABLE regtest_ptable ADD COLUMN d float
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tpub FOR ALL TABLES
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- ok: attaching first partition works, because (a) is in column list
ALTER TABLE testpub_tbl8 ATTACH PARTITION testpub_tbl8_0 FOR VALUES WITH (modulus 2, remainder 0)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE sum_int_randomrestart (int4)
(
	stype = int4,
	sfunc = int4pl,
	mstype = int4,
	msfunc = int4pl,
	minvfunc = sum_int_randrestart_minvfunc
)
;
----------
[ALTER_TABLE] ALTER TABLE temporal_pk REPLICA IDENTITY NOTHING
;
