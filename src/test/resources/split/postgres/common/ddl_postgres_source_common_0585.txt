DROP TABLE inh_parent
;
CREATE FUNCTION "public"."ffnp"(integer[]) RETURNS integer[]
    LANGUAGE "sql"
    AS $_$select $1$_$
;
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_dba_t:s0
CREATE TABLE t4 (m int, n text)
;
create schema fkpart1
  create table pkey (a int primary key)
  create table fk_part (a int) partition by list (a)
  create table fk_part_1 partition of fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 partition of fk_part_1 for values in (1)
;
CREATE TABLE quuux1 PARTITION OF quuux FOR VALUES IN (1)
;
--
-- SELECT_HAVING
--

-- load test data
CREATE TABLE test_having (a int, b int, c char(8), d char)
;
CREATE INDEX t2_gin ON s3.t2 USING GIN (ia)
;
--
-- check set NULL and table constraint on multiple columns
--
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 text, PRIMARY KEY(ptest1, ptest2) )
;
create temp table idxpart_temp (a int) partition by range (a)
;
create trigger p after insert on grandparent for each statement execute function f()
;
ALTER INDEX pg_toast.pg_toast_33313_index RENAME TO reind_con_toast_idx
;
-- same, with a cursor whose portal name doesn't match variable name
create or replace function forc01() returns void as $$
declare
  c refcursor := 'fooled_ya';
  r record;
begin
  open c for select * from forc_test;
  loop
    fetch c into r;
    exit when not found;
    raise notice '%, %', r.i, r.j;
    update forc_test set i = i * 100, j = r.j * 2 where current of c;
  end loop;
end;
$$ language plpgsql
;
CREATE TABLE "public"."rtest_nothn1" (
    "a" integer,
    "b" "text"
)
;
create table parttmp_11_to_20 partition of parttmp for values from (11) to (20)
;
ANALYZE vegetables
;
CREATE FUNCTION batch_end()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
CREATE VIEW data_type_privileges AS
    SELECT CAST(current_database() AS sql_identifier) AS object_catalog,
           CAST(x.objschema AS sql_identifier) AS object_schema,
           CAST(x.objname AS sql_identifier) AS object_name,
           CAST(x.objtype AS character_data) AS object_type,
           CAST(x.objdtdid AS sql_identifier) AS dtd_identifier

    FROM
      (
        SELECT udt_schema, udt_name, 'USER-DEFINED TYPE'::text, dtd_identifier FROM attributes
        UNION ALL
        SELECT table_schema, table_name, 'TABLE'::text, dtd_identifier FROM columns
        UNION ALL
        SELECT domain_schema, domain_name, 'DOMAIN'::text, dtd_identifier FROM domains
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM parameters
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM routines
      ) AS x (objschema, objname, objtype, objdtdid)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_replication_origin
;
CREATE CAST (integer AS "public"."casttesttype") WITH FUNCTION "public"."bar_int4_text"(integer) AS IMPLICIT
;
/* Now redefine */
CREATE FUNCTION pg_stat_statements_reset(IN userid Oid DEFAULT 0,
	IN dbid Oid DEFAULT 0,
	IN queryid bigint DEFAULT 0
)
RETURNS void
AS '$libdir/pg_stat_statements', 'pg_stat_statements_reset_1_7'
LANGUAGE C STRICT PARALLEL SAFE
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
-- fail, disallowed
alter table ac drop constraint ac_aa_check
;
DROP INDEX jidx
;
CREATE TEMP VIEW id_alw3_view AS SELECT * FROM id_alw3
;
CREATE TABLE delete_test_table (a bigint, b bigint, c bigint, d bigint)
;
create table idxpart2 (c text, a int, b int)
;
DROP TABLE errtst_parent
;
CREATE SUBSCRIPTION sub3 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub3
;
CREATE INDEX "clstr_tst_b" ON "public"."clstr_tst" USING "btree" ("b")
;
DROP VIEW ruletest_v1
;
-- be failed
SECURITY LABEL ON COLUMN t2
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
DROP TYPE IF EXISTS split_extension_next.ext_mood
;
-- Adding a PK at the top level of a hierarchy should cause all descendants
-- to be checked for nulls, even past a no-inherit constraint
CREATE TABLE inh_nn_lvl1 (a int)
;
CREATE FUNCTION gin_extract_query_macaddr8(macaddr8, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE EXTENSION test_bloomfilter
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_partitioned_table
;
CREATE SCHEMA split_obj_priv
;
CREATE UNLOGGED SEQUENCE sequence_test_unlogged
;
create function tg_iface_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from IFace where slotname = old.slotname;
	insert into IFace (
		    slotname,
		    sysname,
		    ifname,
		    slotlink
		) values (
		    new.slotname,
		    new.sysname,
		    new.ifname,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
-- the wolf bug - schema mods caused inconsistent row descriptors
CREATE TABLE attmp (
	initial 	int4
)
;
-- And again..
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest2, ptest1))
;
CREATE TABLE am_partitioned(x INT, y INT) PARTITION BY hash (x)
;
CREATE TABLE partkey_t_1 PARTITION OF partkey_t FOR VALUES FROM (0) TO (1000)
;
ALTER TABLE num_variance SET (parallel_workers = 4)
;
DROP TABLE pg_temp_3.pg_temp_27281_2, pg_temp_3.pg_temp_27281
;
-- Test if cube is also a point

CREATE FUNCTION cube_is_point(cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
drop table upsert
;
CREATE AGGREGATE "public"."myaggn06b"(integer) (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}'
)
;
create function tg_room_ad() returns trigger as '
begin
    delete from WSlot where roomno = old.roomno;
    return old;
end;
' language plpgsql
;
DO $$
DECLARE r text[];
BEGIN
  r := parse_ident('Schemax.Tabley');
  RAISE NOTICE '%', format('%I.%I', r[1], r[2]);
  r := parse_ident('"SchemaX"."TableY"');
  RAISE NOTICE '%', format('%I.%I', r[1], r[2]);
END;
$$
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE inh_parent
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ffnp"(integer[]) RETURNS integer[]
    LANGUAGE "sql"
    AS $_$select $1$_$
;
----------
[CREATE_TABLE] -- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_dba_t:s0
CREATE TABLE t4 (m int, n text)
;
----------
[CREATE_SCHEMA] create schema fkpart1
  create table pkey (a int primary key)
  create table fk_part (a int) partition by list (a)
  create table fk_part_1 partition of fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 partition of fk_part_1 for values in (1)
;
----------
[CREATE_TABLE] CREATE TABLE quuux1 PARTITION OF quuux FOR VALUES IN (1)
;
----------
[CREATE_TABLE] --
-- SELECT_HAVING
--

-- load test data
CREATE TABLE test_having (a int, b int, c char(8), d char)
;
----------
[ADD_INDEX] CREATE INDEX t2_gin ON s3.t2 USING GIN (ia)
;
----------
[CREATE_TABLE] --
-- check set NULL and table constraint on multiple columns
--
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 text, PRIMARY KEY(ptest1, ptest2) )
;
----------
[CREATE_TABLE] create temp table idxpart_temp (a int) partition by range (a)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger p after insert on grandparent for each statement execute function f()
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33313_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] -- same, with a cursor whose portal name doesn't match variable name
create or replace function forc01() returns void as $$
declare
  c refcursor := 'fooled_ya';
  r record;
begin
  open c for select * from forc_test;
  loop
    fetch c into r;
    exit when not found;
    raise notice '%, %', r.i, r.j;
    update forc_test set i = i * 100, j = r.j * 2 where current of c;
  end loop;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_nothn1" (
    "a" integer,
    "b" "text"
)
;
----------
[CREATE_TABLE] create table parttmp_11_to_20 partition of parttmp for values from (11) to (20)
;
----------
[ADMIN_TABLE] ANALYZE vegetables
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION batch_end()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW data_type_privileges AS
    SELECT CAST(current_database() AS sql_identifier) AS object_catalog,
           CAST(x.objschema AS sql_identifier) AS object_schema,
           CAST(x.objname AS sql_identifier) AS object_name,
           CAST(x.objtype AS character_data) AS object_type,
           CAST(x.objdtdid AS sql_identifier) AS dtd_identifier

    FROM
      (
        SELECT udt_schema, udt_name, 'USER-DEFINED TYPE'::text, dtd_identifier FROM attributes
        UNION ALL
        SELECT table_schema, table_name, 'TABLE'::text, dtd_identifier FROM columns
        UNION ALL
        SELECT domain_schema, domain_name, 'DOMAIN'::text, dtd_identifier FROM domains
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM parameters
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM routines
      ) AS x (objschema, objname, objtype, objdtdid)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_replication_origin
;
----------
[CREATE_PROG_OBJ] CREATE CAST (integer AS "public"."casttesttype") WITH FUNCTION "public"."bar_int4_text"(integer) AS IMPLICIT
;
----------
[CREATE_PROG_OBJ] /* Now redefine */
CREATE FUNCTION pg_stat_statements_reset(IN userid Oid DEFAULT 0,
	IN dbid Oid DEFAULT 0,
	IN queryid bigint DEFAULT 0
)
RETURNS void
AS '$libdir/pg_stat_statements', 'pg_stat_statements_reset_1_7'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- fail, disallowed
alter table ac drop constraint ac_aa_check
;
----------
[DROP_INDEX] DROP INDEX jidx
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW id_alw3_view AS SELECT * FROM id_alw3
;
----------
[CREATE_TABLE] CREATE TABLE delete_test_table (a bigint, b bigint, c bigint, d bigint)
;
----------
[CREATE_TABLE] create table idxpart2 (c text, a int, b int)
;
----------
[DROP_TABLE] DROP TABLE errtst_parent
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub3 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub3
;
----------
[ADD_INDEX] CREATE INDEX "clstr_tst_b" ON "public"."clstr_tst" USING "btree" ("b")
;
----------
[DROP_VIEW] DROP VIEW ruletest_v1
;
----------
[UNKNOWN] -- be failed
SECURITY LABEL ON COLUMN t2
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS split_extension_next.ext_mood
;
----------
[CREATE_TABLE] -- Adding a PK at the top level of a hierarchy should cause all descendants
-- to be checked for nulls, even past a no-inherit constraint
CREATE TABLE inh_nn_lvl1 (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_macaddr8(macaddr8, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_bloomfilter
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_partitioned_table
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_obj_priv
;
----------
[CREATE_SEQUENCE] CREATE UNLOGGED SEQUENCE sequence_test_unlogged
;
----------
[CREATE_PROG_OBJ] create function tg_iface_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from IFace where slotname = old.slotname;
	insert into IFace (
		    slotname,
		    sysname,
		    ifname,
		    slotlink
		) values (
		    new.slotname,
		    new.sysname,
		    new.ifname,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_TABLE] -- the wolf bug - schema mods caused inconsistent row descriptors
CREATE TABLE attmp (
	initial 	int4
)
;
----------
[CREATE_TABLE] -- And again..
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest2, ptest1))
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned(x INT, y INT) PARTITION BY hash (x)
;
----------
[CREATE_TABLE] CREATE TABLE partkey_t_1 PARTITION OF partkey_t FOR VALUES FROM (0) TO (1000)
;
----------
[ALTER_TABLE] ALTER TABLE num_variance SET (parallel_workers = 4)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_27281_2, pg_temp_3.pg_temp_27281
;
----------
[CREATE_PROG_OBJ] -- Test if cube is also a point

CREATE FUNCTION cube_is_point(cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] drop table upsert
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn06b"(integer) (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}'
)
;
----------
[CREATE_PROG_OBJ] create function tg_room_ad() returns trigger as '
begin
    delete from WSlot where roomno = old.roomno;
    return old;
end;
' language plpgsql
;
----------
[BLOCK] DO $$
DECLARE r text[];
BEGIN
  r := parse_ident('Schemax.Tabley');
  RAISE NOTICE '%', format('%I.%I', r[1], r[2]);
  r := parse_ident('"SchemaX"."TableY"');
  RAISE NOTICE '%', format('%I.%I', r[1], r[2]);
END;
$$
;
