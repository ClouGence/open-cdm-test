-- Check SELECT FOR UPDATE/SHARE with an inherited source table
create table loct1 (f1 int, f2 int, f3 int)
;
CREATE TABLE test_part_d_1 PARTITION OF test_part_d FOR VALUES IN (1,3)
;
CREATE TRIGGER ri_child BEFORE INSERT OR UPDATE ON child FOR EACH ROW EXECUTE PROCEDURE ri_child()
;
CREATE OR REPLACE FUNCTION test_create_multixact() RETURNS xid
  AS '$libdir/test_slru', 'test_create_multixact' LANGUAGE C
;
-- failure: replica identity full can't be used with a column list
ALTER TABLE testpub_tbl8_1 REPLICA IDENTITY FULL
;
CREATE UNIQUE INDEX dedup_unique ON dedup_unique_test_table (a) WITH (deduplicate_items=on)
;
CREATE TRIGGER "transition_table_base_ins_trig" AFTER INSERT ON "public"."transition_table_base" REFERENCING NEW TABLE AS "newtable" FOR EACH STATEMENT EXECUTE FUNCTION "public"."transition_table_base_ins_func"()
;
CREATE FUNCTION isnge(issn, issn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE a_star RENAME COLUMN foo TO aa
;
CREATE FUNCTION gbt_inet_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION slot_changes_wrapper(slot_name name) RETURNS SETOF TEXT AS $$
BEGIN
  RETURN QUERY
    SELECT data FROM pg_logical_slot_peek_changes(slot_name, NULL, NULL, 'include-xids', '0', 'skip-empty-xacts', '1');
END$$ LANGUAGE plpgsql
;
CREATE TRIGGER gtest3 AFTER DELETE OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (OLD.b < 0)  -- ok
  EXECUTE PROCEDURE gtest_trigger_func()
;
ALTER TABLE pg_transform ADD UNIQUE USING INDEX pg_transform_type_lang_index
;
CREATE TABLE itest4 (a int, b text)
;
ANALYZE pg_temp_32.pg_temp_28597
;
-- new owner lacks permission to create this index from scratch
CREATE INDEX k ON testschema.tablespace_acl (c) TABLESPACE regress_tblspace
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc8(1)
;
ALTER TABLE varlena_bug ALTER COLUMN v SET STORAGE extended
;
CREATE INDEX idx_bit ON test_bit USING gin (i)
;
CREATE FOREIGN TABLE p1 partition of pt for values in (1) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/list1.csv', delimiter ',')
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opfamily
;
ALTER FOREIGN TABLE async_p2 OPTIONS (use_remote_estimate 'true')
;
ALTER AGGREGATE alt_func1(int) SET SCHEMA alt_nsp2
;
CREATE TABLE s1 (a int, b text)
;
ALTER TABLE tabx ADD PRIMARY KEY (id)
;
ALTER TABLE ONLY "public"."part_7_a_null" DROP COLUMN "........pg.dropped.1........"
;
CREATE EXTENSION test_copy_callbacks
;
-- check that implicitly named array type _comptype isn't a problem
create type _comptype as enum('fooey')
;
alter extension postgres_fdw add operator public.<^(int, int)
;
ALTER TABLE ONLY "public"."itest7c" INHERIT "public"."itest7b"
;
-- should fail

DROP INDEX text_idx
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_db_role_setting
;
CREATE TABLE "public"."pht1_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
DROP TABLE t1 CASCADE
;
CREATE FUNCTION uuid_multicast_bit(uuid) RETURNS bool AS
$$ SELECT (('x' || substr($1::text, 25, 2))::bit(8) & '00000001') != '00000000' $$
LANGUAGE SQL STRICT IMMUTABLE
;
create function dfunc(a int DEFAULT 1.0, int DEFAULT '-1') returns int as $$
  select $1 + $2;
$$ language sql
;
CREATE TABLE prt1_adv_default PARTITION OF prt1_adv DEFAULT
;
CREATE FUNCTION f5 (text) RETURNS bool
	AS 'SELECT sepgsql_setcon($1)'
    LANGUAGE sql
;
ALTER OPERATOR === (boolean, boolean) SET (JOIN = NONE)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest_tableoid"
    ADD CONSTRAINT "gtest_tableoid_pkey" PRIMARY KEY ("a")
;
ALTER SEQUENCE seq1 START 101
;
CREATE FUNCTION create_temp_tab() RETURNS text
LANGUAGE plpgsql AS $$
BEGIN
  CREATE TEMP TABLE new_table (f1 float8);
  -- case of interest is that we fail while holding an open
  -- relcache reference to new_table
  INSERT INTO new_table SELECT invert(0.0);
  RETURN 'foo';
END $$
;
ALTER TABLE pg_toast.pg_toast_28084 RENAME TO reind_con_toast
;
drop view rw_view5
;
CREATE TABLE "fkpart5"."fk" (
    "a" integer
)
PARTITION BY LIST ("a")
;
CREATE SCHEMA split_pg18
;
-- Test ALTER DOMAIN .. CONSTRAINT ..
create domain con as integer
;
-- dblink_connect_u allows non-superusers to use
-- non-password authenticated connections, but initially
-- privileges are revoked from public
CREATE FUNCTION dblink_connect_u (text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED SECURITY DEFINER
;
CREATE FUNCTION pg_prewarm(regclass,
						   mode text default 'buffer',
						   fork text default 'main',
						   first_block int8 default null,
						   last_block int8 default null)
RETURNS int8
AS '$libdir/pg_prewarm', 'pg_prewarm'
LANGUAGE C PARALLEL SAFE
;
VACUUM pg_catalog.pg_tablespace
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- Check SELECT FOR UPDATE/SHARE with an inherited source table
create table loct1 (f1 int, f2 int, f3 int)
;
----------
[CREATE_TABLE] CREATE TABLE test_part_d_1 PARTITION OF test_part_d FOR VALUES IN (1,3)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER ri_child BEFORE INSERT OR UPDATE ON child FOR EACH ROW EXECUTE PROCEDURE ri_child()
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_create_multixact() RETURNS xid
  AS '$libdir/test_slru', 'test_create_multixact' LANGUAGE C
;
----------
[ALTER_TABLE] -- failure: replica identity full can't be used with a column list
ALTER TABLE testpub_tbl8_1 REPLICA IDENTITY FULL
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX dedup_unique ON dedup_unique_test_table (a) WITH (deduplicate_items=on)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "transition_table_base_ins_trig" AFTER INSERT ON "public"."transition_table_base" REFERENCING NEW TABLE AS "newtable" FOR EACH STATEMENT EXECUTE FUNCTION "public"."transition_table_base_ins_func"()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(issn, issn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_COLUMN] ALTER TABLE a_star RENAME COLUMN foo TO aa
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_inet_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION slot_changes_wrapper(slot_name name) RETURNS SETOF TEXT AS $$
BEGIN
  RETURN QUERY
    SELECT data FROM pg_logical_slot_peek_changes(slot_name, NULL, NULL, 'include-xids', '0', 'skip-empty-xacts', '1');
END$$ LANGUAGE plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest3 AFTER DELETE OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (OLD.b < 0)  -- ok
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_transform ADD UNIQUE USING INDEX pg_transform_type_lang_index
;
----------
[CREATE_TABLE] CREATE TABLE itest4 (a int, b text)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28597
;
----------
[ADD_INDEX] -- new owner lacks permission to create this index from scratch
CREATE INDEX k ON testschema.tablespace_acl (c) TABLESPACE regress_tblspace
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc8(1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE varlena_bug ALTER COLUMN v SET STORAGE extended
;
----------
[ADD_INDEX] CREATE INDEX idx_bit ON test_bit USING gin (i)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE p1 partition of pt for values in (1) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/list1.csv', delimiter ',')
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opfamily
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE async_p2 OPTIONS (use_remote_estimate 'true')
;
----------
[ALTER_PROG_OBJ] ALTER AGGREGATE alt_func1(int) SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] CREATE TABLE s1 (a int, b text)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tabx ADD PRIMARY KEY (id)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."part_7_a_null" DROP COLUMN "........pg.dropped.1........"
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_copy_callbacks
;
----------
[CREATE_TYPE] -- check that implicitly named array type _comptype isn't a problem
create type _comptype as enum('fooey')
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw add operator public.<^(int, int)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."itest7c" INHERIT "public"."itest7b"
;
----------
[DROP_INDEX] -- should fail

DROP INDEX text_idx
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_db_role_setting
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht1_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[DROP_TABLE] DROP TABLE t1 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION uuid_multicast_bit(uuid) RETURNS bool AS
$$ SELECT (('x' || substr($1::text, 25, 2))::bit(8) & '00000001') != '00000000' $$
LANGUAGE SQL STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] create function dfunc(a int DEFAULT 1.0, int DEFAULT '-1') returns int as $$
  select $1 + $2;
$$ language sql
;
----------
[CREATE_TABLE] CREATE TABLE prt1_adv_default PARTITION OF prt1_adv DEFAULT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f5 (text) RETURNS bool
	AS 'SELECT sepgsql_setcon($1)'
    LANGUAGE sql
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, boolean) SET (JOIN = NONE)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest_tableoid"
    ADD CONSTRAINT "gtest_tableoid_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE seq1 START 101
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION create_temp_tab() RETURNS text
LANGUAGE plpgsql AS $$
BEGIN
  CREATE TEMP TABLE new_table (f1 float8);
  -- case of interest is that we fail while holding an open
  -- relcache reference to new_table
  INSERT INTO new_table SELECT invert(0.0);
  RETURN 'foo';
END $$
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28084 RENAME TO reind_con_toast
;
----------
[DROP_VIEW] drop view rw_view5
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."fk" (
    "a" integer
)
PARTITION BY LIST ("a")
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_pg18
;
----------
[CREATE_TYPE] -- Test ALTER DOMAIN .. CONSTRAINT ..
create domain con as integer
;
----------
[CREATE_PROG_OBJ] -- dblink_connect_u allows non-superusers to use
-- non-password authenticated connections, but initially
-- privileges are revoked from public
CREATE FUNCTION dblink_connect_u (text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED SECURITY DEFINER
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_prewarm(regclass,
						   mode text default 'buffer',
						   fork text default 'main',
						   first_block int8 default null,
						   last_block int8 default null)
RETURNS int8
AS '$libdir/pg_prewarm', 'pg_prewarm'
LANGUAGE C PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_tablespace
;
