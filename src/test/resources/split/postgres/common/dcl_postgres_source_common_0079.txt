GRANT ALL ON DATABASE "ecpg2_regression" TO "regress_ecpg_user2"
;
reindex index concurrently test_pg_index_toast_index
;
EXECUTE foo
;
EXECUTE dumpFunc('16936')
;
REVOKE EXECUTE ON FUNCTION pg_relpages(text) FROM PUBLIC
;
CREATE USER regress_rule_user1
;
LOCK TABLE public.inhx IN ACCESS SHARE MODE
;
COPY (SELECT a FROM public.test_root) TO STDOUT
;
EXECUTE dumpFunc('41734')
;
-- Should now have access via the view, but see nothing and leak nothing
SET SESSION AUTHORIZATION regress_stats_user1
;
------------------------------------------------------------------------------------------
[GRANT] GRANT ALL ON DATABASE "ecpg2_regression" TO "regress_ecpg_user2"
;
----------
[ADMIN] reindex index concurrently test_pg_index_toast_index
;
----------
[UNSAFE] EXECUTE foo
;
----------
[UNSAFE] EXECUTE dumpFunc('16936')
;
----------
[REVOKE] REVOKE EXECUTE ON FUNCTION pg_relpages(text) FROM PUBLIC
;
----------
[CREATE_USER] CREATE USER regress_rule_user1
;
----------
[SESSION_LOCK] LOCK TABLE public.inhx IN ACCESS SHARE MODE
;
----------
[DATA_EXPORT|SELECT] COPY (SELECT a FROM public.test_root) TO STDOUT
;
----------
[UNSAFE] EXECUTE dumpFunc('41734')
;
----------
[SWITCH_USER] -- Should now have access via the view, but see nothing and leak nothing
SET SESSION AUTHORIZATION regress_stats_user1
;
