VACUUM (FULL) pg_catalog.pg_largeobject_metadata
;
CREATE INDEX "onek2_u2_prtl" ON "public"."onek2" USING "btree" ("unique2") WHERE ("stringu1" < 'B'::"name")
;
ALTER TABLE rf_tbl_abcd_pk REPLICA IDENTITY NOTHING
;
-- fails
ALTER TABLE ONLY pitest2 ALTER COLUMN f3 SET GENERATED BY DEFAULT SET INCREMENT BY 2 SET START WITH 1000 RESTART
;
VACUUM (PARALLEL 0) pg_catalog.pg_am
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_largeobject
;
CREATE TABLE array_op_test (
	seqno		int4,
	i			int4[],
	t			text[]
)
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', null '
')
;
CREATE TABLE prt2_p3 PARTITION OF prt2 FOR VALUES FROM (500) TO (600)
;
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_superuser_t:s0

--
-- CREATE Objects to be altered (with debug_audit being silent)
--
CREATE DATABASE sepgsql_test_regression_1
;
CREATE TABLE parttbl2 PARTITION OF parttbl FOR VALUES IN (2)
;
CREATE TEXT SEARCH DICTIONARY serbian_stem
	(TEMPLATE = snowball, Language = serbian )
;
ALTER TABLE attmp5 add constraint attmpconstr foreign key(a) references attmp4(a) match full
;
/* contrib/pgstattuple/pgstattuple--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE OR REPLACE FUNCTION pgstattuple(IN relname text,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuple_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE pagg_tab_p3 (LIKE pagg_tab)
;
-- c1.f1 is NOT dropped, but must now be considered non-inherited
alter table c1 drop column f1
;
CREATE INDEX ggcircleind ON gcircle_tbl USING gist (f1)
;
-- DELETE turned into UPDATE by a rule has OLD and NEW
CREATE RULE foo_del_rule AS ON DELETE TO foo DO INSTEAD
  UPDATE foo SET f2 = f2||' (deleted)', f3 = -1, f4 = -1 WHERE f1 = OLD.f1
  RETURNING *
;
CREATE FUNCTION gbt_time_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
-- We need to do this this way to cope with varying names for encodings:
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test0 (provider = icu, locale = ' ||
          quote_literal(current_setting('lc_collate')) || ');';
END
$$
;
-- fail
ALTER TABLE articles_in_category DROP CONSTRAINT articles_in_category_pkey RESTRICT
;
ANALYZE public.tableam_parted_b_heap2
;
create index i_loct1_f1 on loct1(f1)
;
CREATE TABLE "public"."fk_partitioned_fk_3" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
/*
 * 5.49
 * ROUTINE_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 5.50
 * ROUTINE_PRIVILEGES view
 */

CREATE VIEW routine_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(n.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST('EXECUTE' AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, p.proowner, 'USAGE')
                  OR p.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, proname, proowner, pronamespace, (aclexplode(coalesce(proacl, acldefault('f', proowner)))).* FROM pg_proc
         ) p (oid, proname, proowner, pronamespace, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE p.pronamespace = n.oid
          AND grantee.oid = p.grantee
          AND u_grantor.oid = p.grantor
          AND p.prtype IN ('EXECUTE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
-- need DO to protect the -- from psql
do $$
  declare r integer;
  begin
    select dfunc(a=>-- comment
      1) into r;
    raise info 'r = %', r;
  end;
$$
;
CREATE INDEX strtest_t_idx ON strtest (t)
;
/*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
CREATE FUNCTION each(IN hs hstore,
    OUT key text,
    OUT value text)
RETURNS SETOF record
AS '$libdir/hstore','hstore_each'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_operator
;
-- ok
CALL priv_testproc1(6)
;
DROP TABLE pg_temp_3.pg_temp_16649_2, pg_temp_3.pg_temp_16649
;
CREATE OR REPLACE VIEW v1 AS SELECT * FROM t1 WHERE a > 0 WITH CHECK OPTION
;
CREATE FUNCTION isnne(ean13, ismn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- ok, owning it is enough for this stuff
ALTER SUBSCRIPTION regress_testsub SET (slot_name = NONE)
;
ALTER TABLE pg_toast.pg_toast_27621 RENAME TO reind_con_toast
;
CREATE TABLE batch_table_p1 (LIKE batch_table)
;
CREATE FUNCTION "public"."ks_test_uniform_random_numeric_in_range"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 0.999999) r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
;
ALTER TABLE pitest1_p2 ALTER COLUMN f3 SET DEFAULT 10000
;
-- fail due to duplicates and circular inheritance
alter table atacc3 inherit atacc2
;
CREATE INDEX onek_stringu1 ON onek USING btree(stringu1 name_ops)
;
CREATE FUNCTION hashupc(upc)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE SCHEMA newschemax AUTHORIZATION PUBLIC
;
CREATE FUNCTION gbt_date_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE pg_temp_24.pg_temp_25663
;
/* contrib/dict_xsyn/dict_xsyn--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION dxsyn_init(internal)
        RETURNS internal
        AS '$libdir/dict_xsyn'
        LANGUAGE C STRICT
;
-- Test that OR predicated are updated correctly after join removal
CREATE TABLE tab_with_flag ( id INT PRIMARY KEY, is_flag SMALLINT)
;
CREATE INDEX gtest22c_b_idx ON gtest22c (b)
;
TRUNCATE batch_table
;
/*
 * 5.74
 * UDT_PRIVILEGES view
 */

CREATE VIEW udt_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(n.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST('TYPE USAGE' AS character_data) AS privilege_type, -- sic
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, t.typowner, 'USAGE')
                  OR t.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, typname, typnamespace, typtype, typowner, (aclexplode(coalesce(typacl, acldefault('T', typowner)))).* FROM pg_type
         ) AS t (oid, typname, typnamespace, typtype, typowner, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE t.typnamespace = n.oid
          AND t.typtype = 'c'
          AND t.grantee = grantee.oid
          AND t.grantor = u_grantor.oid
          AND t.prtype IN ('USAGE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_largeobject_metadata
;
----------
[ADD_INDEX] CREATE INDEX "onek2_u2_prtl" ON "public"."onek2" USING "btree" ("unique2") WHERE ("stringu1" < 'B'::"name")
;
----------
[ALTER_TABLE] ALTER TABLE rf_tbl_abcd_pk REPLICA IDENTITY NOTHING
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails
ALTER TABLE ONLY pitest2 ALTER COLUMN f3 SET GENERATED BY DEFAULT SET INCREMENT BY 2 SET START WITH 1000 RESTART
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_am
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_largeobject
;
----------
[CREATE_TABLE] CREATE TABLE array_op_test (
	seqno		int4,
	i			int4[],
	t			text[]
)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', null '
')
;
----------
[CREATE_TABLE] CREATE TABLE prt2_p3 PARTITION OF prt2 FOR VALUES FROM (500) TO (600)
;
----------
[CREATE_CATALOG] -- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_superuser_t:s0

--
-- CREATE Objects to be altered (with debug_audit being silent)
--
CREATE DATABASE sepgsql_test_regression_1
;
----------
[CREATE_TABLE] CREATE TABLE parttbl2 PARTITION OF parttbl FOR VALUES IN (2)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY serbian_stem
	(TEMPLATE = snowball, Language = serbian )
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp5 add constraint attmpconstr foreign key(a) references attmp4(a) match full
;
----------
[CREATE_PROG_OBJ] /* contrib/pgstattuple/pgstattuple--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE OR REPLACE FUNCTION pgstattuple(IN relname text,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuple_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_p3 (LIKE pagg_tab)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- c1.f1 is NOT dropped, but must now be considered non-inherited
alter table c1 drop column f1
;
----------
[ADD_INDEX] CREATE INDEX ggcircleind ON gcircle_tbl USING gist (f1)
;
----------
[CREATE_POLICY] -- DELETE turned into UPDATE by a rule has OLD and NEW
CREATE RULE foo_del_rule AS ON DELETE TO foo DO INSTEAD
  UPDATE foo SET f2 = f2||' (deleted)', f3 = -1, f4 = -1 WHERE f1 = OLD.f1
  RETURNING *
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[BLOCK] -- We need to do this this way to cope with varying names for encodings:
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test0 (provider = icu, locale = ' ||
          quote_literal(current_setting('lc_collate')) || ');';
END
$$
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- fail
ALTER TABLE articles_in_category DROP CONSTRAINT articles_in_category_pkey RESTRICT
;
----------
[ADMIN_TABLE] ANALYZE public.tableam_parted_b_heap2
;
----------
[ADD_INDEX] create index i_loct1_f1 on loct1(f1)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_3" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.49
 * ROUTINE_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 5.50
 * ROUTINE_PRIVILEGES view
 */

CREATE VIEW routine_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(n.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST('EXECUTE' AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, p.proowner, 'USAGE')
                  OR p.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, proname, proowner, pronamespace, (aclexplode(coalesce(proacl, acldefault('f', proowner)))).* FROM pg_proc
         ) p (oid, proname, proowner, pronamespace, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE p.pronamespace = n.oid
          AND grantee.oid = p.grantee
          AND u_grantor.oid = p.grantor
          AND p.prtype IN ('EXECUTE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
----------
[BLOCK] -- need DO to protect the -- from psql
do $$
  declare r integer;
  begin
    select dfunc(a=>-- comment
      1) into r;
    raise info 'r = %', r;
  end;
$$
;
----------
[ADD_INDEX] CREATE INDEX strtest_t_idx ON strtest (t)
;
----------
[CREATE_POLICY] /*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION each(IN hs hstore,
    OUT key text,
    OUT value text)
RETURNS SETOF record
AS '$libdir/hstore','hstore_each'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_operator
;
----------
[CALL_PROG_OBJ] -- ok
CALL priv_testproc1(6)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_16649_2, pg_temp_3.pg_temp_16649
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW v1 AS SELECT * FROM t1 WHERE a > 0 WITH CHECK OPTION
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ean13, ismn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_PUB_SUB] -- ok, owning it is enough for this stuff
ALTER SUBSCRIPTION regress_testsub SET (slot_name = NONE)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27621 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE batch_table_p1 (LIKE batch_table)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ks_test_uniform_random_numeric_in_range"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 0.999999) r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest1_p2 ALTER COLUMN f3 SET DEFAULT 10000
;
----------
[ALTER_TABLE] -- fail due to duplicates and circular inheritance
alter table atacc3 inherit atacc2
;
----------
[ADD_INDEX] CREATE INDEX onek_stringu1 ON onek USING btree(stringu1 name_ops)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hashupc(upc)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschemax AUTHORIZATION PUBLIC
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_date_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_25663
;
----------
[CREATE_PROG_OBJ] /* contrib/dict_xsyn/dict_xsyn--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION dxsyn_init(internal)
        RETURNS internal
        AS '$libdir/dict_xsyn'
        LANGUAGE C STRICT
;
----------
[CREATE_TABLE] -- Test that OR predicated are updated correctly after join removal
CREATE TABLE tab_with_flag ( id INT PRIMARY KEY, is_flag SMALLINT)
;
----------
[ADD_INDEX] CREATE INDEX gtest22c_b_idx ON gtest22c (b)
;
----------
[TRUNCATE_TABLE] TRUNCATE batch_table
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.74
 * UDT_PRIVILEGES view
 */

CREATE VIEW udt_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(n.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST('TYPE USAGE' AS character_data) AS privilege_type, -- sic
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, t.typowner, 'USAGE')
                  OR t.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, typname, typnamespace, typtype, typowner, (aclexplode(coalesce(typacl, acldefault('T', typowner)))).* FROM pg_type
         ) AS t (oid, typname, typnamespace, typtype, typowner, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE t.typnamespace = n.oid
          AND t.typtype = 'c'
          AND t.grantee = grantee.oid
          AND t.grantor = u_grantor.oid
          AND t.prtype IN ('USAGE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
