VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_parser
;
-- simple updatable view

CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified')
;
ALTER TABLE pg_temp_10.pg_temp_26514_2 ADD COLUMN newdata pg_temp_10.pg_temp_26514
;
CREATE FUNCTION pgp_sym_decrypt_bytea(bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_decrypt_bytea'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- specifying storage parameters for partitioned tables is not supported
ALTER TABLE partitioned SET (fillfactor=100)
;
drop function dfunc(a variadic int[])
;
-- Create the operator class
CREATE OPERATOR CLASS gist_bool_ops
DEFAULT FOR TYPE bool USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	OPERATOR	6	<> ,
	FUNCTION	1	gbt_bool_consistent (internal, bool, int2, oid, internal),
	FUNCTION	2	gbt_bool_union (internal, internal),
	FUNCTION	3	gbt_bool_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_bool_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bool_picksplit (internal, internal),
	FUNCTION	7	gbt_bool_same (gbtreekey2, gbtreekey2, internal),
	FUNCTION	9   gbt_bool_fetch (internal),
	STORAGE		gbtreekey2
;
CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0
;
CREATE AGGREGATE min(citext)  (
    SFUNC = citext_smaller,
    STYPE = citext,
    SORTOP = <,
    PARALLEL = SAFE,
    COMBINEFUNC = citext_smaller
)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext
;
create table cchild (pid int, descrip text)
;
-- Test non-const jsonpath
CREATE TEMP TABLE jsonpaths (path) AS SELECT '$'
;
ALTER INDEX "public"."covidxpart_a_b_idx" ATTACH PARTITION "public"."covidxpart3_a_b_idx"
;
ANALYZE public.pht1_p1
;
CREATE TRIGGER accounts_insert_stmt
    AFTER INSERT ON split_trigger_probe.accounts
    REFERENCING NEW TABLE AS inserted_rows
    FOR EACH STATEMENT
    EXECUTE PROCEDURE split_trigger_probe.audit_statement('after_insert_statement')
;
create view rtest_vview5 as select a, b, rtest_viewfunc1(a) as refcount
	from rtest_view1
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
DROP POLICY p1 ON rls_test_restrictive
;
-- let's try moving a table from one place to another
CREATE TABLE testschema.atable AS VALUES (1), (2)
;
DROP FUNCTION test1(hstore)
;
drop function polyf(anycompatiblerange)
;
drop table cities
;
CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=12066 host=/tmp/Ia3hZucwmd dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
CREATE INDEX "idxpart1_a_idx" ON "public"."idxpart1" USING "btree" ("a")
;
CREATE UNIQUE INDEX CONCURRENTLY concur_reindex_ind5 ON concur_reindex_tab4 (c1)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=16006 host=/tmp/B3ysgEyCZr dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
ALTER INDEX pg_toast.pg_toast_28176_index RENAME TO reind_con_toast_idx
;
create or replace function shadowtest(in1 int)
	returns void as $$
declare
in1 int;
begin
	declare
	in1 int;
	begin
	end;
end$$ language plpgsql
;
CREATE FUNCTION int_custom_ne(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
comment on domain domaindroptest is 'About to drop this..'
;
DROP TABLE pvactst2
;
-- fail
TRUNCATE TABLE trunc_c
;
ALTER SUBSCRIPTION regress_test_sub CONNECTION 'port=25714 host=/tmp/mUR9A5zNgJ user=regress_test_user dbname=postgres password=secret'
;
ALTER TABLE sts_sch1.tbl ALTER COLUMN a TYPE SMALLINT
;
create or replace function raise_test() returns void as $$
begin
  raise division_by_zero using detail = 'some detail info';
  exception
    when others then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
create type pg_temp.twophase_type as (a int)
;
-- Ensure const bool quals can be properly detected as redundant
create table bool_rp (b bool, a int) partition by range(b,a)
;
ALTER TABLE tab_stats ALTER COLUMN b set default 'a'
;
CREATE AGGREGATE myaggp08a(BASETYPE = anyelement, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
ALTER SUBSCRIPTION regress_testsub_foo SET (synchronous_commit = local)
;
CREATE RULE "rtest_pers_del" AS
    ON DELETE TO "public"."rtest_person" DO  DELETE FROM "public"."rtest_admin"
  WHERE ("rtest_admin"."pname" = "old"."pname")
;
create table rule_and_refint_t2 (
	id2a integer,
	id2c integer,

	primary key (id2a, id2c)
)
;
-- error, requires NOT NULL
ALTER TABLE itest4 ALTER COLUMN a SET NOT NULL
;
-- fail
SECURITY LABEL FOR 'dummy' ON ROLE regress_dummy_seclabel_user2 IS 'unclassified'
;
CREATE TRIGGER "tg_slotlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_slotlink_a"('WS')
;
ANALYZE pg_temp_13.pg_temp_26424
;
CREATE FUNCTION my_trigger_func() RETURNS trigger AS $$
if TD_event == "INSERT"
    return TD_NEW
elseif TD_event == "UPDATE"
    return TD_NEW
else
    return "OK"
end
$$ language plsample
;
CREATE TABLE notnull_tbl4_lk2 (LIKE notnull_tbl4 INCLUDING INDEXES)
;
CREATE INDEX i_temptest ON temptest(col)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_parser
;
----------
[CREATE_TABLE] -- simple updatable view

CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified')
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_26514_2 ADD COLUMN newdata pg_temp_10.pg_temp_26514
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgp_sym_decrypt_bytea(bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_decrypt_bytea'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE] -- specifying storage parameters for partitioned tables is not supported
ALTER TABLE partitioned SET (fillfactor=100)
;
----------
[DROP_PROG_OBJ] drop function dfunc(a variadic int[])
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_bool_ops
DEFAULT FOR TYPE bool USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	OPERATOR	6	<> ,
	FUNCTION	1	gbt_bool_consistent (internal, bool, int2, oid, internal),
	FUNCTION	2	gbt_bool_union (internal, internal),
	FUNCTION	3	gbt_bool_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_bool_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bool_picksplit (internal, internal),
	FUNCTION	7	gbt_bool_same (gbtreekey2, gbtreekey2, internal),
	FUNCTION	9   gbt_bool_fetch (internal),
	STORAGE		gbtreekey2
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE min(citext)  (
    SFUNC = citext_smaller,
    STYPE = citext,
    SORTOP = <,
    PARALLEL = SAFE,
    COMBINEFUNC = citext_smaller
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext
;
----------
[CREATE_TABLE] create table cchild (pid int, descrip text)
;
----------
[CREATE_TABLE|SELECT] -- Test non-const jsonpath
CREATE TEMP TABLE jsonpaths (path) AS SELECT '$'
;
----------
[ALTER_INDEX] ALTER INDEX "public"."covidxpart_a_b_idx" ATTACH PARTITION "public"."covidxpart3_a_b_idx"
;
----------
[ADMIN_TABLE] ANALYZE public.pht1_p1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER accounts_insert_stmt
    AFTER INSERT ON split_trigger_probe.accounts
    REFERENCING NEW TABLE AS inserted_rows
    FOR EACH STATEMENT
    EXECUTE PROCEDURE split_trigger_probe.audit_statement('after_insert_statement')
;
----------
[CREATE_VIEW(SELECT)] create view rtest_vview5 as select a, b, rtest_viewfunc1(a) as refcount
	from rtest_view1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[DROP_POLICY] DROP POLICY p1 ON rls_test_restrictive
;
----------
[CREATE_TABLE|SELECT] -- let's try moving a table from one place to another
CREATE TABLE testschema.atable AS VALUES (1), (2)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test1(hstore)
;
----------
[DROP_PROG_OBJ] drop function polyf(anycompatiblerange)
;
----------
[DROP_TABLE] drop table cities
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=12066 host=/tmp/Ia3hZucwmd dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
----------
[ADD_INDEX] CREATE INDEX "idxpart1_a_idx" ON "public"."idxpart1" USING "btree" ("a")
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX CONCURRENTLY concur_reindex_ind5 ON concur_reindex_tab4 (c1)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=16006 host=/tmp/B3ysgEyCZr dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28176_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] create or replace function shadowtest(in1 int)
	returns void as $$
declare
in1 int;
begin
	declare
	in1 int;
	begin
	end;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_ne(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
----------
[COMMENT_TYPE] comment on domain domaindroptest is 'About to drop this..'
;
----------
[DROP_TABLE] DROP TABLE pvactst2
;
----------
[TRUNCATE_TABLE] -- fail
TRUNCATE TABLE trunc_c
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_test_sub CONNECTION 'port=25714 host=/tmp/mUR9A5zNgJ user=regress_test_user dbname=postgres password=secret'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE sts_sch1.tbl ALTER COLUMN a TYPE SMALLINT
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise division_by_zero using detail = 'some detail info';
  exception
    when others then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
----------
[CREATE_TYPE] create type pg_temp.twophase_type as (a int)
;
----------
[CREATE_TABLE] -- Ensure const bool quals can be properly detected as redundant
create table bool_rp (b bool, a int) partition by range(b,a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tab_stats ALTER COLUMN b set default 'a'
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp08a(BASETYPE = anyelement, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub_foo SET (synchronous_commit = local)
;
----------
[CREATE_POLICY] CREATE RULE "rtest_pers_del" AS
    ON DELETE TO "public"."rtest_person" DO  DELETE FROM "public"."rtest_admin"
  WHERE ("rtest_admin"."pname" = "old"."pname")
;
----------
[CREATE_TABLE] create table rule_and_refint_t2 (
	id2a integer,
	id2c integer,

	primary key (id2a, id2c)
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- error, requires NOT NULL
ALTER TABLE itest4 ALTER COLUMN a SET NOT NULL
;
----------
[UNKNOWN] -- fail
SECURITY LABEL FOR 'dummy' ON ROLE regress_dummy_seclabel_user2 IS 'unclassified'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_slotlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_slotlink_a"('WS')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26424
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION my_trigger_func() RETURNS trigger AS $$
if TD_event == "INSERT"
    return TD_NEW
elseif TD_event == "UPDATE"
    return TD_NEW
else
    return "OK"
end
$$ language plsample
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl4_lk2 (LIKE notnull_tbl4 INCLUDING INDEXES)
;
----------
[ADD_INDEX] CREATE INDEX i_temptest ON temptest(col)
;
