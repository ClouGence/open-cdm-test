comment on constraint at_partitioned_1_id_name_key on at_partitioned_1 is 'child 1 constraint'
;
create index on bool_rp (b,a)
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "url" WITH "simple"
;
ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk2" FOR VALUES FROM (1000) TO (2000)
;
-- Test RENAME TRIGGER
ALTER TRIGGER modified_a ON main_table RENAME TO modified_modified_a
;
ALTER TABLE temporal3 DROP COLUMN valid_at
;
-- test table functions

create function tftest(int) returns table(a int, b int) as $$
begin
  return query select $1, $1+i from generate_series(1,5) g(i);
end;
$$ language plpgsql immutable strict
;
ALTER TABLE ruletest1 ENABLE REPLICA RULE rule1
;
CREATE OR REPLACE FUNCTION
  "normalize"(text, text DEFAULT 'NFC')
RETURNS text
LANGUAGE internal
STRICT IMMUTABLE PARALLEL SAFE
AS 'unicode_normalize_func'
;
DROP TABLE t, tp
;
ALTER TABLE pg_toast.pg_toast_31078 RENAME TO reind_con_toast
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.time() < $.time())'))
;
CREATE TABLE test_timestamp (
	i timestamp
)
;
CREATE TABLE tab_rowfilter_partitioned_2 (a int primary key, b integer) PARTITION BY RANGE(a)
;
-- Test index partition tree
CREATE INDEX ptif_test_index ON ONLY ptif_test (a)
;
ANALYZE public.alpha_pos_p2
;
ALTER EXTENSION hstore DROP DOMAIN split_extension_next.ext_code
;
CREATE TABLE ext_cine_tab3 (z int)
;
CREATE INDEX vcharidx ON vchartmp USING GIST ( text(a) )
;
CREATE FUNCTION build_ordered_named_pairs(i int, j int) RETURNS ordered_named_pair[] AS $$
begin
return array[row(i, j), row(i, j+1)];
end
$$ LANGUAGE plpgsql
;
create function namedparmcursor_test4() returns void as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
begin
    open c1(20, param1 := 21);
end
$$ language plpgsql
;
-- Check with a partitioned table
CREATE TABLE sro_ptab (a int) PARTITION BY RANGE (a)
;
ALTER TABLE ONLY test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "float" WITH "simple"
;
CREATE TABLE test_part_a (a int, b int, c int) PARTITION BY LIST (a)
;
CREATE TABLE "public"."pitest1_p1_like" (
    "f1" "date" CONSTRAINT "pitest1_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" bigint CONSTRAINT "pitest1_f3_not_null" NOT NULL
)
;
create temp table t1_1 (check (a >= 0 and a < 10)) inherits (t1)
;
ALTER FUNCTION gbt_int8_distance(internal, bigint, smallint, oid, internal) PARALLEL SAFE
;
CREATE TABLE prun(id integer, s char(2000)) WITH (fillfactor = 75, user_catalog_table = true)
;
CREATE INDEX textarrayidx ON array_index_op_test USING gin (t)
;
create temp table bar(f1 int, f2 int)
;
create index wowidx on test_tsvector using gist (a)
;
ALTER TABLE evttrig.one ALTER COLUMN col_b DROP DEFAULT
;
CREATE TABLE "public"."rewritemetoo2" (
    "a" "text"
)
;
DROP DOMAIN jsb_int_not_null
;
-- Test some extra relations.
VACUUM (INDEX_CLEANUP FALSE) vaccluster
;
ALTER TYPE "public"."planets" ADD VALUE 'mercury'
;
DROP TABLE IF EXISTS intra_grant_inplace
;
ALTER OPERATOR FAMILY gist_cidr_ops USING gist ADD
	FUNCTION	11  (cidr, cidr) gbt_inet_sortsupport (internal)
;
-- Split plt2_adv_p2 into two partitions so that plt1_adv_p2 matches both
CREATE TABLE plt2_adv_p2_1 PARTITION OF plt2_adv FOR VALUES IN ('0004')
;
CREATE TABLE remember_create_subid (c int)
;
CREATE FUNCTION gbt_cash_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
DROP AGGREGATE IF EXISTS no_such_schema.foo(int)
;
DROP COLLATION testx
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=27783 host=/tmp/WPXzmhPeul dbname=postgres' PUBLICATION pub1
;
ALTER TABLE ONLY "public"."mlparted" ATTACH PARTITION "public"."mlparted1" FOR VALUES FROM (1, 2) TO (1, 10)
;
CREATE INDEX on bttest_unique_nulls (b,c)
;
DROP TRIGGER trig_row_before ON rem1
;
CREATE INDEX cidridx ON cidrtmp USING gist ( a )
;
-- trying to specify range for list partitioned table
CREATE TABLE fail_part PARTITION OF list_parted FOR VALUES FROM (1) TO (2)
;
------------------------------------------------------------------------------------------
[COMMENT_CONSTRAINT] comment on constraint at_partitioned_1_id_name_key on at_partitioned_1 is 'child 1 constraint'
;
----------
[ADD_INDEX] create index on bool_rp (b,a)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "url" WITH "simple"
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk2" FOR VALUES FROM (1000) TO (2000)
;
----------
[RENAME_TRIGGER] -- Test RENAME TRIGGER
ALTER TRIGGER modified_a ON main_table RENAME TO modified_modified_a
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE temporal3 DROP COLUMN valid_at
;
----------
[CREATE_PROG_OBJ] -- test table functions

create function tftest(int) returns table(a int, b int) as $$
begin
  return query select $1, $1+i from generate_series(1,5) g(i);
end;
$$ language plpgsql immutable strict
;
----------
[ALTER_TABLE] ALTER TABLE ruletest1 ENABLE REPLICA RULE rule1
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  "normalize"(text, text DEFAULT 'NFC')
RETURNS text
LANGUAGE internal
STRICT IMMUTABLE PARALLEL SAFE
AS 'unicode_normalize_func'
;
----------
[DROP_TABLE] DROP TABLE t, tp
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_31078 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.time() < $.time())'))
;
----------
[CREATE_TABLE] CREATE TABLE test_timestamp (
	i timestamp
)
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_partitioned_2 (a int primary key, b integer) PARTITION BY RANGE(a)
;
----------
[ADD_INDEX] -- Test index partition tree
CREATE INDEX ptif_test_index ON ONLY ptif_test (a)
;
----------
[ADMIN_TABLE] ANALYZE public.alpha_pos_p2
;
----------
[ALTER_LIBRARY] ALTER EXTENSION hstore DROP DOMAIN split_extension_next.ext_code
;
----------
[CREATE_TABLE] CREATE TABLE ext_cine_tab3 (z int)
;
----------
[ADD_INDEX] CREATE INDEX vcharidx ON vchartmp USING GIST ( text(a) )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION build_ordered_named_pairs(i int, j int) RETURNS ordered_named_pair[] AS $$
begin
return array[row(i, j), row(i, j+1)];
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] create function namedparmcursor_test4() returns void as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
begin
    open c1(20, param1 := 21);
end
$$ language plpgsql
;
----------
[CREATE_TABLE] -- Check with a partitioned table
CREATE TABLE sro_ptab (a int) PARTITION BY RANGE (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE ONLY test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "float" WITH "simple"
;
----------
[CREATE_TABLE] CREATE TABLE test_part_a (a int, b int, c int) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest1_p1_like" (
    "f1" "date" CONSTRAINT "pitest1_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" bigint CONSTRAINT "pitest1_f3_not_null" NOT NULL
)
;
----------
[CREATE_TABLE] create temp table t1_1 (check (a >= 0 and a < 10)) inherits (t1)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int8_distance(internal, bigint, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE prun(id integer, s char(2000)) WITH (fillfactor = 75, user_catalog_table = true)
;
----------
[ADD_INDEX] CREATE INDEX textarrayidx ON array_index_op_test USING gin (t)
;
----------
[CREATE_TABLE] create temp table bar(f1 int, f2 int)
;
----------
[ADD_INDEX] create index wowidx on test_tsvector using gist (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE evttrig.one ALTER COLUMN col_b DROP DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rewritemetoo2" (
    "a" "text"
)
;
----------
[DROP_TYPE] DROP DOMAIN jsb_int_not_null
;
----------
[ADMIN_TABLE] -- Test some extra relations.
VACUUM (INDEX_CLEANUP FALSE) vaccluster
;
----------
[ALTER_TYPE] ALTER TYPE "public"."planets" ADD VALUE 'mercury'
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS intra_grant_inplace
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_cidr_ops USING gist ADD
	FUNCTION	11  (cidr, cidr) gbt_inet_sortsupport (internal)
;
----------
[CREATE_TABLE] -- Split plt2_adv_p2 into two partitions so that plt1_adv_p2 matches both
CREATE TABLE plt2_adv_p2_1 PARTITION OF plt2_adv FOR VALUES IN ('0004')
;
----------
[CREATE_TABLE] CREATE TABLE remember_create_subid (c int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_cash_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE IF EXISTS no_such_schema.foo(int)
;
----------
[DROP_POLICY] DROP COLLATION testx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=27783 host=/tmp/WPXzmhPeul dbname=postgres' PUBLICATION pub1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted" ATTACH PARTITION "public"."mlparted1" FOR VALUES FROM (1, 2) TO (1, 10)
;
----------
[ADD_INDEX] CREATE INDEX on bttest_unique_nulls (b,c)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_before ON rem1
;
----------
[ADD_INDEX] CREATE INDEX cidridx ON cidrtmp USING gist ( a )
;
----------
[CREATE_TABLE] -- trying to specify range for list partitioned table
CREATE TABLE fail_part PARTITION OF list_parted FOR VALUES FROM (1) TO (2)
;
