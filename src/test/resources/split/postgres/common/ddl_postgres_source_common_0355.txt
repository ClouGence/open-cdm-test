create table part_ee_ff3 partition of part_ee_ff for values from (20) to (30) partition by range (b)
;
-- fail, needs "locale"
CREATE COLLATION testx (provider = icu, locale = 'nonsense')
;
CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING dummy_index_am AS
  OPERATOR 1 = (int4, int4),
  FUNCTION 1 hashint4(int4)
;
CREATE TABLE "generated_stored_tests"."gtest24r" (
    "a" integer NOT NULL,
    "b" "generated_stored_tests"."gtestdomain1range" GENERATED ALWAYS AS ("generated_stored_tests"."gtestdomain1range"(("a")::"generated_stored_tests"."gtestdomain1", (("a" + 5))::"generated_stored_tests"."gtestdomain1")) STORED
)
;
-- Parallel Append is not to be used when the subpath depends on the outer param
create table part_pa_test(a int, b int) partition by range(a)
;
create index �׻����Ѹ�index1 on �׻����Ѹ� using btree (�Ѹ�)
;
CREATE TABLE testpub_gencol (a INT, b INT GENERATED ALWAYS AS (a + 1) STORED NOT NULL)
;
drop function depth_b_tf()
;
CREATE FUNCTION gbt_bit_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE temporal_fk_rng2rng
    ALTER CONSTRAINT temporal_fk_rng2rng_fk
    DEFERRABLE INITIALLY DEFERRED
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_publication
;
CREATE OPERATOR <> (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_ne,
       COMMUTATOR = <>,
       NEGATOR = =,
       RESTRICT = neqsel,
       JOIN = neqjoinsel
)
;
create domain dposinta as posint[]
;
CREATE EXTENSION autoinc
;
ALTER TABLE tt6 OF tt_t0
;
create table phv_boolpart_t partition of phv_boolpart for values in (true)
;
CREATE UNLOGGED TABLE identity_dump_unlogged (a INT GENERATED ALWAYS AS IDENTITY)
;
create policy p1 on rls_t for select to regress_rls_alice
  using (c = current_setting('rls_test.blah'))
;
create table join_pt1p2 partition of join_pt1 for values from (100) to (200)
;
CREATE TABLE "pubme"."t1" (
    "c" integer
)
;
/* contrib/pgstattuple/pgstattuple--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION pgstattuple(IN relname text,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuple'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TEMP TABLE t1 (
  f1 serial,
  f2 int DEFAULT nextval('myseq2'),
  f3 int DEFAULT nextval('myseq3'::text)
)
;
ALTER TABLE gtest_tableoid ADD COLUMN
  c regclass GENERATED ALWAYS AS (tableoid) STORED
;
--- test transforming to perl

CREATE FUNCTION bool2perl(bool, bool, bool) RETURNS void
LANGUAGE plperl
TRANSFORM FOR TYPE bool, for type boolean  -- duplicate to test ruleutils
AS $$
my ($x, $y, $z) = @_;

die("NULL mistransformed") if (defined($z));
die("TRUE mistransformed to UNDEF") if (!defined($x));
die("FALSE mistransformed to UNDEF") if (!defined($y));
die("TRUE mistransformed") if (!$x);
die("FALSE mistransformed") if ($y);
$$
;
ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.2........"
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=11979 host=/tmp/kQtpO29Zz8 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
create function testrngfunc() returns record as $$
  insert into rngfunc values (1,2) returning *;
$$ language sql
;
--
-- Operators.
--

CREATE OPERATOR = (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    COMMUTATOR = =,
    NEGATOR    = <>,
    PROCEDURE  = citext_eq,
    RESTRICT   = eqsel,
    JOIN       = eqjoinsel,
    HASHES,
    MERGES
)
;
-- while we're here, verify successful truncation of index with SQL function
CREATE INDEX ON temptest(bit_length(''))
;
-- add constraints recursively
ALTER TABLE fd_pt1 ADD CONSTRAINT fd_pt1chk1 CHECK (c1 > 0) NO INHERIT
;
CREATE FUNCTION wgo_then_no_access() RETURNS int LANGUAGE SQL AS 'SELECT 1'
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
CREATE EXTENSION dummy_index_am
;
CREATE FUNCTION test_null_ordered_named_pair()
  RETURNS ordered_named_pair AS $$
declare v ordered_named_pair;
begin
return v;
end
$$ LANGUAGE plpgsql
;
ALTER TYPE test_type DROP ATTRIBUTE b
;
CREATE POLICY policy_range_parted_subplan on range_parted
    AS RESTRICTIVE for UPDATE USING (true)
    WITH CHECK ((SELECT range_parted.c <= c1 FROM mintab))
;
CREATE VIEW limit_thousand_v_5 AS SELECT thousand FROM onek WHERE thousand < 995
		ORDER BY thousand FETCH FIRST NULL ROWS ONLY
;
ALTER OPERATOR FAMILY gist_trgm_ops USING gist
ADD FUNCTION 10 (text) gtrgm_options (internal)
;
-- Verify the fillfactor and buffering options
create index gist_pointidx2 on gist_point_tbl using gist(p) with (buffering = on, fillfactor=50)
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE OPERATOR @ (
        LEFTARG = ltxtquery,
	RIGHTARG = _ltree,
	PROCEDURE = _ltxtq_rexec,
        COMMUTATOR = '@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE TABLE another_parttbl (a int, b int, c int) PARTITION BY LIST (a)
;
ANALYZE public.test_type_diff2_c1
;
CREATE TABLE t (i int, v text)
;
ANALYZE pg_temp_24.pg_temp_27043
;
truncate table loct1
;
COMMENT ON COLUMN parted_col_comment.a IS 'Partition key'
;
CREATE FUNCTION wrap_do_analyze(c INT) RETURNS INT IMMUTABLE LANGUAGE SQL
	AS 'SELECT $1 FROM public.do_analyze()'
;
create temp table tb (id int primary key, aval int)
;
ALTER TABLE ONLY "public"."part" ATTACH PARTITION "public"."part_p2" DEFAULT
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table part_ee_ff3 partition of part_ee_ff for values from (20) to (30) partition by range (b)
;
----------
[CREATE_POLICY] -- fail, needs "locale"
CREATE COLLATION testx (provider = icu, locale = 'nonsense')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING dummy_index_am AS
  OPERATOR 1 = (int4, int4),
  FUNCTION 1 hashint4(int4)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest24r" (
    "a" integer NOT NULL,
    "b" "generated_stored_tests"."gtestdomain1range" GENERATED ALWAYS AS ("generated_stored_tests"."gtestdomain1range"(("a")::"generated_stored_tests"."gtestdomain1", (("a" + 5))::"generated_stored_tests"."gtestdomain1")) STORED
)
;
----------
[CREATE_TABLE] -- Parallel Append is not to be used when the subpath depends on the outer param
create table part_pa_test(a int, b int) partition by range(a)
;
----------
[ADD_INDEX] create index �׻����Ѹ�index1 on �׻����Ѹ� using btree (�Ѹ�)
;
----------
[CREATE_TABLE] CREATE TABLE testpub_gencol (a INT, b INT GENERATED ALWAYS AS (a + 1) STORED NOT NULL)
;
----------
[DROP_PROG_OBJ] drop function depth_b_tf()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bit_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE temporal_fk_rng2rng
    ALTER CONSTRAINT temporal_fk_rng2rng_fk
    DEFERRABLE INITIALLY DEFERRED
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_publication
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_ne,
       COMMUTATOR = <>,
       NEGATOR = =,
       RESTRICT = neqsel,
       JOIN = neqjoinsel
)
;
----------
[CREATE_TYPE] create domain dposinta as posint[]
;
----------
[CREATE_LIBRARY] CREATE EXTENSION autoinc
;
----------
[ALTER_TABLE] ALTER TABLE tt6 OF tt_t0
;
----------
[CREATE_TABLE] create table phv_boolpart_t partition of phv_boolpart for values in (true)
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE identity_dump_unlogged (a INT GENERATED ALWAYS AS IDENTITY)
;
----------
[CREATE_POLICY] create policy p1 on rls_t for select to regress_rls_alice
  using (c = current_setting('rls_test.blah'))
;
----------
[CREATE_TABLE] create table join_pt1p2 partition of join_pt1 for values from (100) to (200)
;
----------
[CREATE_TABLE] CREATE TABLE "pubme"."t1" (
    "c" integer
)
;
----------
[CREATE_PROG_OBJ] /* contrib/pgstattuple/pgstattuple--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION pgstattuple(IN relname text,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuple'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE t1 (
  f1 serial,
  f2 int DEFAULT nextval('myseq2'),
  f3 int DEFAULT nextval('myseq3'::text)
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest_tableoid ADD COLUMN
  c regclass GENERATED ALWAYS AS (tableoid) STORED
;
----------
[CREATE_PROG_OBJ] --- test transforming to perl

CREATE FUNCTION bool2perl(bool, bool, bool) RETURNS void
LANGUAGE plperl
TRANSFORM FOR TYPE bool, for type boolean  -- duplicate to test ruleutils
AS $$
my ($x, $y, $z) = @_;

die("NULL mistransformed") if (defined($z));
die("TRUE mistransformed to UNDEF") if (!defined($x));
die("FALSE mistransformed to UNDEF") if (!defined($y));
die("TRUE mistransformed") if (!$x);
die("FALSE mistransformed") if ($y);
$$
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.2........"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=11979 host=/tmp/kQtpO29Zz8 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_PROG_OBJ] create function testrngfunc() returns record as $$
  insert into rngfunc values (1,2) returning *;
$$ language sql
;
----------
[CREATE_PROG_OBJ] --
-- Operators.
--

CREATE OPERATOR = (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    COMMUTATOR = =,
    NEGATOR    = <>,
    PROCEDURE  = citext_eq,
    RESTRICT   = eqsel,
    JOIN       = eqjoinsel,
    HASHES,
    MERGES
)
;
----------
[ADD_INDEX] -- while we're here, verify successful truncation of index with SQL function
CREATE INDEX ON temptest(bit_length(''))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add constraints recursively
ALTER TABLE fd_pt1 ADD CONSTRAINT fd_pt1chk1 CHECK (c1 > 0) NO INHERIT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION wgo_then_no_access() RETURNS int LANGUAGE SQL AS 'SELECT 1'
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
----------
[CREATE_LIBRARY] CREATE EXTENSION dummy_index_am
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_null_ordered_named_pair()
  RETURNS ordered_named_pair AS $$
declare v ordered_named_pair;
begin
return v;
end
$$ LANGUAGE plpgsql
;
----------
[ALTER_TYPE] ALTER TYPE test_type DROP ATTRIBUTE b
;
----------
[CREATE_POLICY] CREATE POLICY policy_range_parted_subplan on range_parted
    AS RESTRICTIVE for UPDATE USING (true)
    WITH CHECK ((SELECT range_parted.c <= c1 FROM mintab))
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW limit_thousand_v_5 AS SELECT thousand FROM onek WHERE thousand < 995
		ORDER BY thousand FETCH FIRST NULL ROWS ONLY
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_trgm_ops USING gist
ADD FUNCTION 10 (text) gtrgm_options (internal)
;
----------
[ADD_INDEX] -- Verify the fillfactor and buffering options
create index gist_pointidx2 on gist_point_tbl using gist(p) with (buffering = on, fillfactor=50)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @ (
        LEFTARG = ltxtquery,
	RIGHTARG = _ltree,
	PROCEDURE = _ltxtq_rexec,
        COMMUTATOR = '@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE another_parttbl (a int, b int, c int) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] ANALYZE public.test_type_diff2_c1
;
----------
[CREATE_TABLE] CREATE TABLE t (i int, v text)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_27043
;
----------
[TRUNCATE_TABLE] truncate table loct1
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN parted_col_comment.a IS 'Partition key'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION wrap_do_analyze(c INT) RETURNS INT IMMUTABLE LANGUAGE SQL
	AS 'SELECT $1 FROM public.do_analyze()'
;
----------
[CREATE_TABLE] create temp table tb (id int primary key, aval int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."part" ATTACH PARTITION "public"."part_p2" DEFAULT
;
