create function psql_df_internal (float8)
  returns float8
  language internal immutable parallel safe strict
  as 'dsin'
;
CREATE TRIGGER z_min_update
    BEFORE UPDATE ON split_builtin_trigger.redundant_updates
    FOR EACH ROW
    EXECUTE FUNCTION suppress_redundant_updates_trigger()
;
DROP TABLE ref_b
;
--
-- Test set-returning functions for PL/pgSQL
--

create function test_table_func_rec() returns setof found_test_tbl as '
DECLARE
	rec RECORD;
BEGIN
	FOR rec IN select * from found_test_tbl LOOP
		RETURN NEXT rec;
	END LOOP;
	RETURN;
END;' language plpgsql
;
CREATE TYPE not_existing_type (INPUT = array_in,
    OUTPUT = array_out,
    ELEMENT = int,
    INTERNALLENGTH = 32)
;
CREATE TABLE clstrpart1 PARTITION OF clstrpart FOR VALUES FROM (1) TO (10) PARTITION BY RANGE (a)
;
-- New functions and views for pg_stat_statements in 1.10
AlTER EXTENSION pg_stat_statements UPDATE TO '1.10'
;
CREATE DATABASE "template1" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
CREATE TABLE notnull_tbl1_1 PARTITION OF notnull_tbl1 FOR VALUES IN (1,2)
;
CREATE TABLE tab_fk (bid int PRIMARY KEY)
;
ALTER INDEX pg_toast.pg_toast_30786_index RENAME TO reind_con_toast_idx
;
-- does not trigger
alter table trigtest enable always trigger trigtest_a_stmt_tg
;
--
-- isn_weak(boolean) - Sets the weak input mode.
-- This function is intended for testing use only!
--
CREATE FUNCTION isn_weak(boolean)
	RETURNS boolean
	AS '$libdir/isn', 'accept_weak_input'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL RESTRICTED
;
CREATE POLICY priv_test_tbl_pol ON tststats.priv_test_tbl USING (2 * a < 0)
;
create unique index parted_replica_idx_1
  on only parted_replica_tab_1 using btree (id)
;
CREATE TABLE mcinthash (a int, b int, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_int4_ops)
;
CREATE TABLE plt2_adv_p2_1 PARTITION OF plt2_adv FOR VALUES IN ('0004')
;
alter table stud_emp rename to stud_emp
;
create operator class part_test_int4_ops
for type int4
using hash as
operator 1 =,
function 2 part_hashint4_noop(int4, int8)
;
-- Constraint index remains clustered.
alter table alttype_cluster cluster on alttype_cluster_pkey
;
DROP TABLE testpub_tbl5d
;
DROP FUNCTION pg_get_wal_records_info_till_end_of_wal(pg_lsn)
;
ALTER OPERATOR FAMILY gist_seg_ops USING gist drop function 3 (seg)
;
COMMENT ON TYPE ean13
	IS 'International European Article Number (EAN13)'
;
CREATE TEMP TABLE pg_temp_3.pg_temp_16656_2 (tid pg_catalog.tid)
;
CREATE TABLE testschema.part1 PARTITION OF testschema.part FOR VALUES IN (1)
;
CREATE TEMP TABLE temp_t (id int) ON COMMIT DROP
;
CREATE RULE "rtest_order_r3" AS
    ON INSERT TO "public"."rtest_order1" DO INSTEAD  INSERT INTO "public"."rtest_order2" ("a", "b", "c")
  VALUES ("new"."a", "nextval"('"public"."rtest_seq"'::"regclass"), 'rule 3 - this should run 3rd'::"text")
;
ALTER TABLE fktable ADD CONSTRAINT fk_213_213
FOREIGN KEY (x2,x1,x3) REFERENCES pktable(id2,id1,id3)
;
ALTER TABLE fk_r DETACH PARTITION fk_r_1
;
CREATE TABLE deptest (a serial primary key, b text)
;
ALTER TABLE "public"."itest3" ALTER COLUMN "c" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest3_c_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
ALTER TABLE pg_toast.pg_toast_28073 RENAME TO reind_con_toast
;
-- dropping non-partition key columns should be allowed on the parent table.
ALTER TABLE list_parted DROP COLUMN b
;
DROP TABLE parted_uniq_tbl
;
--
-- check corner case where an entirely-dummy subplan is created by
-- constraint exclusion
--

create temp table t1 (a integer primary key)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_tablespace
;
CREATE TABLE brin_hot (
  id  integer PRIMARY KEY,
  val integer NOT NULL
) WITH (autovacuum_enabled = off, fillfactor = 70)
;
CREATE FUNCTION "public"."tg_backlink_a"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    dummy	integer;
begin
    if tg_op = 'INSERT' then
        if new.backlink != '' then
	    dummy := tg_backlink_set(new.backlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = 'UPDATE' then
        if new.backlink != old.backlink then
	    if old.backlink != '' then
	        dummy := tg_backlink_unset(old.backlink, old.slotname);
	    end if;
	    if new.backlink != '' then
	        dummy := tg_backlink_set(new.backlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.backlink != '' then
	        dummy := tg_slotlink_set(new.backlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = 'DELETE' then
        if old.backlink != '' then
	    dummy := tg_backlink_unset(old.backlink, old.slotname);
	end if;
	return old;
    end if;
end;
$$
;
-- fail

drop function anyctest(a anyelement, b anyarray,
                       c anycompatible, d anycompatible)
;
ANALYZE fprt1_p2
;
alter table abc add constraint ptr foreign key (id) references abc2 (id2)
;
create table mc2p3 partition of mc2p for values from (2, minvalue) to (2, 1)
;
create table part_column_drop (
  useless_1 int,
  id int,
  useless_2 int,
  d int,
  b int,
  useless_3 int
) partition by range (id)
;
CREATE FUNCTION rngfunc(in f1 int, out f2 int)
AS 'select $1+1' LANGUAGE sql
;
CREATE TABLE "public"."gtest23q" (
    "a" integer NOT NULL,
    "b" integer
)
;
-- fail
alter index idxpart_2_idx attach partition idxpart1_2_idx
;
DROP FUNCTION invert(x float8)
;
create table mcrparted1 partition of mcrparted for values from (1, 1, 1) to (10, 5, 10)
;
--
-- Concrete function should override generic one
--
CREATE FUNCTION echo_me(rainbow) RETURNS text AS $$
BEGIN
RETURN $1::text || 'wtf';
END
$$ LANGUAGE plpgsql
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create function psql_df_internal (float8)
  returns float8
  language internal immutable parallel safe strict
  as 'dsin'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER z_min_update
    BEFORE UPDATE ON split_builtin_trigger.redundant_updates
    FOR EACH ROW
    EXECUTE FUNCTION suppress_redundant_updates_trigger()
;
----------
[DROP_TABLE] DROP TABLE ref_b
;
----------
[CREATE_PROG_OBJ] --
-- Test set-returning functions for PL/pgSQL
--

create function test_table_func_rec() returns setof found_test_tbl as '
DECLARE
	rec RECORD;
BEGIN
	FOR rec IN select * from found_test_tbl LOOP
		RETURN NEXT rec;
	END LOOP;
	RETURN;
END;' language plpgsql
;
----------
[CREATE_TYPE] CREATE TYPE not_existing_type (INPUT = array_in,
    OUTPUT = array_out,
    ELEMENT = int,
    INTERNALLENGTH = 32)
;
----------
[CREATE_TABLE] CREATE TABLE clstrpart1 PARTITION OF clstrpart FOR VALUES FROM (1) TO (10) PARTITION BY RANGE (a)
;
----------
[ALTER_LIBRARY] -- New functions and views for pg_stat_statements in 1.10
AlTER EXTENSION pg_stat_statements UPDATE TO '1.10'
;
----------
[CREATE_CATALOG] CREATE DATABASE "template1" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1_1 PARTITION OF notnull_tbl1 FOR VALUES IN (1,2)
;
----------
[CREATE_TABLE] CREATE TABLE tab_fk (bid int PRIMARY KEY)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30786_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE] -- does not trigger
alter table trigtest enable always trigger trigtest_a_stmt_tg
;
----------
[CREATE_PROG_OBJ] --
-- isn_weak(boolean) - Sets the weak input mode.
-- This function is intended for testing use only!
--
CREATE FUNCTION isn_weak(boolean)
	RETURNS boolean
	AS '$libdir/isn', 'accept_weak_input'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL RESTRICTED
;
----------
[CREATE_POLICY] CREATE POLICY priv_test_tbl_pol ON tststats.priv_test_tbl USING (2 * a < 0)
;
----------
[ADD_INDEX] create unique index parted_replica_idx_1
  on only parted_replica_tab_1 using btree (id)
;
----------
[CREATE_TABLE] CREATE TABLE mcinthash (a int, b int, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_int4_ops)
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p2_1 PARTITION OF plt2_adv FOR VALUES IN ('0004')
;
----------
[RENAME_TABLE] alter table stud_emp rename to stud_emp
;
----------
[CREATE_PROG_OBJ] create operator class part_test_int4_ops
for type int4
using hash as
operator 1 =,
function 2 part_hashint4_noop(int4, int8)
;
----------
[ALTER_TABLE] -- Constraint index remains clustered.
alter table alttype_cluster cluster on alttype_cluster_pkey
;
----------
[DROP_TABLE] DROP TABLE testpub_tbl5d
;
----------
[DROP_PROG_OBJ] DROP FUNCTION pg_get_wal_records_info_till_end_of_wal(pg_lsn)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_seg_ops USING gist drop function 3 (seg)
;
----------
[COMMENT_TYPE] COMMENT ON TYPE ean13
	IS 'International European Article Number (EAN13)'
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_16656_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part1 PARTITION OF testschema.part FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temp_t (id int) ON COMMIT DROP
;
----------
[CREATE_POLICY] CREATE RULE "rtest_order_r3" AS
    ON INSERT TO "public"."rtest_order1" DO INSTEAD  INSERT INTO "public"."rtest_order2" ("a", "b", "c")
  VALUES ("new"."a", "nextval"('"public"."rtest_seq"'::"regclass"), 'rule 3 - this should run 3rd'::"text")
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_213_213
FOREIGN KEY (x2,x1,x3) REFERENCES pktable(id2,id1,id3)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_r DETACH PARTITION fk_r_1
;
----------
[CREATE_TABLE] CREATE TABLE deptest (a serial primary key, b text)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest3" ALTER COLUMN "c" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest3_c_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28073 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|DROP_COLUMN] -- dropping non-partition key columns should be allowed on the parent table.
ALTER TABLE list_parted DROP COLUMN b
;
----------
[DROP_TABLE] DROP TABLE parted_uniq_tbl
;
----------
[CREATE_TABLE] --
-- check corner case where an entirely-dummy subplan is created by
-- constraint exclusion
--

create temp table t1 (a integer primary key)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_tablespace
;
----------
[CREATE_TABLE] CREATE TABLE brin_hot (
  id  integer PRIMARY KEY,
  val integer NOT NULL
) WITH (autovacuum_enabled = off, fillfactor = 70)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_backlink_a"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    dummy	integer;
begin
    if tg_op = 'INSERT' then
        if new.backlink != '' then
	    dummy := tg_backlink_set(new.backlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = 'UPDATE' then
        if new.backlink != old.backlink then
	    if old.backlink != '' then
	        dummy := tg_backlink_unset(old.backlink, old.slotname);
	    end if;
	    if new.backlink != '' then
	        dummy := tg_backlink_set(new.backlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.backlink != '' then
	        dummy := tg_slotlink_set(new.backlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = 'DELETE' then
        if old.backlink != '' then
	    dummy := tg_backlink_unset(old.backlink, old.slotname);
	end if;
	return old;
    end if;
end;
$$
;
----------
[DROP_PROG_OBJ] -- fail

drop function anyctest(a anyelement, b anyarray,
                       c anycompatible, d anycompatible)
;
----------
[ADMIN_TABLE] ANALYZE fprt1_p2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table abc add constraint ptr foreign key (id) references abc2 (id2)
;
----------
[CREATE_TABLE] create table mc2p3 partition of mc2p for values from (2, minvalue) to (2, 1)
;
----------
[CREATE_TABLE] create table part_column_drop (
  useless_1 int,
  id int,
  useless_2 int,
  d int,
  b int,
  useless_3 int
) partition by range (id)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rngfunc(in f1 int, out f2 int)
AS 'select $1+1' LANGUAGE sql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest23q" (
    "a" integer NOT NULL,
    "b" integer
)
;
----------
[ALTER_INDEX] -- fail
alter index idxpart_2_idx attach partition idxpart1_2_idx
;
----------
[DROP_PROG_OBJ] DROP FUNCTION invert(x float8)
;
----------
[CREATE_TABLE] create table mcrparted1 partition of mcrparted for values from (1, 1, 1) to (10, 5, 10)
;
----------
[CREATE_PROG_OBJ] --
-- Concrete function should override generic one
--
CREATE FUNCTION echo_me(rainbow) RETURNS text AS $$
BEGIN
RETURN $1::text || 'wtf';
END
$$ LANGUAGE plpgsql
;
