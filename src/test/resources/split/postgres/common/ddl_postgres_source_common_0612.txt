ALTER INDEX pg_toast.pg_toast_28293_index RENAME TO reind_con_toast_idx
;
CREATE TABLE accounts (accountid text PRIMARY KEY, balance numeric not null)
;
ALTER TABLE pg_temp_3.pg_temp_16652_2 ADD COLUMN newdata pg_temp_3.pg_temp_16652
;
CREATE FUNCTION pg_buffercache_evict(IN int)
RETURNS bool
AS '$libdir/pg_buffercache', 'pg_buffercache_evict'
LANGUAGE C PARALLEL SAFE VOLATILE STRICT
;
CREATE FUNCTION "public"."mr_outparam_succeed"("i" "anymultirange", OUT "r" "anymultirange", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select $1, 'foo'::text $_$
;
do $$
  declare
    r record;
  begin
    insert into test1 values (2, repeat('bar', 3000));
    insert into test1 values (3, repeat('baz', 4000));
    for r in select test1.b from test1 loop
      delete from test1;
      commit;
      perform pg_advisory_lock(1);
      raise notice 'length(r) = %', length(r::text);
    end loop;
  end;
$$
;
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest4,
ptest3) REFERENCES pktable)
;
CREATE FUNCTION leaker_2(fail BOOL, OUT error_code INTEGER, OUT new_id INTEGER)
  RETURNS RECORD AS $$
BEGIN
  IF fail THEN
    RAISE EXCEPTION 'fail ...';
  END IF;
  error_code := 1;
  new_id := 1;
  RETURN;
END;
$$ LANGUAGE plpgsql
;
CREATE TABLE stxdinp(i int, a int, b int) PARTITION BY RANGE (i)
;
VACUUM ANALYZE stxdinp
;
create table stmt_trig_on_empty_upd (a int)
;
VACUUM (PARALLEL 0) pg_catalog.pg_language
;
CREATE INDEX "regular_sized_index" ON "public"."parallel_vacuum_table" USING "btree" ("a")
;
ALTER AGGREGATE split_agg_probe.sum_int_renamed(integer) SET SCHEMA split_agg_probe_next
;
-- no remapping in either direction necessary
CREATE TABLE errtst_child_plaindef (
    partid int not null,
    shdata int not null,
    data int NOT NULL DEFAULT 0,
    CONSTRAINT shdata_small CHECK(shdata < 3),
    CHECK(data < 10)
)
;
-- This should also fail for the same reason, but here we
-- give the column name
CREATE TABLE FKTABLE (ftest1 inet REFERENCES pktable(ptest1))
;
VACUUM (ANALYZE) pg_catalog.pg_proc
;
-- btree
drop index hidx
;
CREATE VIEW xmltableview2 AS SELECT * FROM XMLTABLE(XMLNAMESPACES('http://x.y' AS zz),
                      '/zz:rows/zz:row'
                      PASSING '<rows xmlns="http://x.y"><row><a>10</a></row></rows>'
                      COLUMNS a int PATH 'zz:a')
;
ANALYZE public.itest7a
;
CREATE FUNCTION gin_compare_prefix_timetz(timetz, timetz, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE TABLE "public"."prt1_m_p2" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
create rule qqq as on insert to copydml_test do instead nothing
;
-- tests with REPLICA IDENTITY FULL
CREATE TABLE testpub_tbl6 (a int, b text, c text)
;
CREATE DATABASE foobar2 STRATEGY foo
;
DROP FUNCTION pg_stat_statements(boolean)
;
DROP TABLE pg_temp_9.pg_temp_26520_2, pg_temp_9.pg_temp_26520
;
-- ok now
ALTER SEQUENCE sequence_test9 AS smallint
;
CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES FROM ('a') TO ('z', 1)
;
-- these are obsolete/deprecated:
CREATE OPERATOR @ (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_contains,
	COMMUTATOR = '~',
	RESTRICT = contsel, JOIN = contjoinsel
)
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26560_2 (tid pg_catalog.tid)
;
CREATE DOMAIN testdomain AS text
;
CREATE FUNCTION "public"."extractq2_2_opt"("t" "public"."int8_tbl") RETURNS TABLE("ret1" bigint)
    LANGUAGE "sql" IMMUTABLE
    AS $$
  select extractq2(t)
$$
;
create table pt12 partition of pt for values in (1,2)
;
alter table myview drop d
;
ANALYZE pg_temp_19.pg_temp_33778
;
ALTER TABLE pg_toast.pg_toast_30860 RENAME TO reind_con_toast
;
DROP DATABASE IF EXISTS sepgsql_test_regression
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub5
;
drop table T
;
VACUUM pg_catalog.pg_foreign_data_wrapper
;
DROP FUNCTION rngfuncb(in f1 int, inout f2 int)
;
CREATE TEMP TABLE explicitly_temp (a int primary key)
;
ALTER FUNCTION gbt_numeric_compress(internal) PARALLEL SAFE
;
DROP SUBSCRIPTION tap_sub_B
;
CREATE FUNCTION sum3(anyelement,anyelement,anyelement) returns anyelement AS
'select $1+$2+$3' language sql strict
;
CREATE TABLE live_measures_copy (uuid, project_uuid, component_uuid, metric_uuid, value, text_value, variation, measure_data, update_marker, created_at, updated_at) AS (SELECT uuid, project_uuid, component_uuid, CAST (metric_id AS VARCHAR (40)) AS metric_uuid, value, text_value, variation, measure_data, update_marker, created_at, updated_at FROM live_measures)
;
-- check change of unrelated column type does not reset the MCV statistics
ALTER TABLE mcv_lists ALTER COLUMN d TYPE VARCHAR(64)
;
CREATE FUNCTION boolop(_int4, query_int)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table minmaxtest1() inherits (minmaxtest)
;
------------------------------------------------------------------------------------------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28293_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE accounts (accountid text PRIMARY KEY, balance numeric not null)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_16652_2 ADD COLUMN newdata pg_temp_3.pg_temp_16652
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_buffercache_evict(IN int)
RETURNS bool
AS '$libdir/pg_buffercache', 'pg_buffercache_evict'
LANGUAGE C PARALLEL SAFE VOLATILE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."mr_outparam_succeed"("i" "anymultirange", OUT "r" "anymultirange", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select $1, 'foo'::text $_$
;
----------
[BLOCK] do $$
  declare
    r record;
  begin
    insert into test1 values (2, repeat('bar', 3000));
    insert into test1 values (3, repeat('baz', 4000));
    for r in select test1.b from test1 loop
      delete from test1;
      commit;
      perform pg_advisory_lock(1);
      raise notice 'length(r) = %', length(r::text);
    end loop;
  end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest4,
ptest3) REFERENCES pktable)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION leaker_2(fail BOOL, OUT error_code INTEGER, OUT new_id INTEGER)
  RETURNS RECORD AS $$
BEGIN
  IF fail THEN
    RAISE EXCEPTION 'fail ...';
  END IF;
  error_code := 1;
  new_id := 1;
  RETURN;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE stxdinp(i int, a int, b int) PARTITION BY RANGE (i)
;
----------
[ADMIN_TABLE] VACUUM ANALYZE stxdinp
;
----------
[CREATE_TABLE] create table stmt_trig_on_empty_upd (a int)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_language
;
----------
[ADD_INDEX] CREATE INDEX "regular_sized_index" ON "public"."parallel_vacuum_table" USING "btree" ("a")
;
----------
[ALTER_PROG_OBJ] ALTER AGGREGATE split_agg_probe.sum_int_renamed(integer) SET SCHEMA split_agg_probe_next
;
----------
[CREATE_TABLE] -- no remapping in either direction necessary
CREATE TABLE errtst_child_plaindef (
    partid int not null,
    shdata int not null,
    data int NOT NULL DEFAULT 0,
    CONSTRAINT shdata_small CHECK(shdata < 3),
    CHECK(data < 10)
)
;
----------
[CREATE_TABLE] -- This should also fail for the same reason, but here we
-- give the column name
CREATE TABLE FKTABLE (ftest1 inet REFERENCES pktable(ptest1))
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_proc
;
----------
[DROP_INDEX] -- btree
drop index hidx
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmltableview2 AS SELECT * FROM XMLTABLE(XMLNAMESPACES('http://x.y' AS zz),
                      '/zz:rows/zz:row'
                      PASSING '<rows xmlns="http://x.y"><row><a>10</a></row></rows>'
                      COLUMNS a int PATH 'zz:a')
;
----------
[ADMIN_TABLE] ANALYZE public.itest7a
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_timetz(timetz, timetz, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_m_p2" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[CREATE_POLICY] create rule qqq as on insert to copydml_test do instead nothing
;
----------
[CREATE_TABLE] -- tests with REPLICA IDENTITY FULL
CREATE TABLE testpub_tbl6 (a int, b text, c text)
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar2 STRATEGY foo
;
----------
[DROP_PROG_OBJ] DROP FUNCTION pg_stat_statements(boolean)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_26520_2, pg_temp_9.pg_temp_26520
;
----------
[ALTER_SEQUENCE] -- ok now
ALTER SEQUENCE sequence_test9 AS smallint
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES FROM ('a') TO ('z', 1)
;
----------
[CREATE_PROG_OBJ] -- these are obsolete/deprecated:
CREATE OPERATOR @ (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_contains,
	COMMUTATOR = '~',
	RESTRICT = contsel, JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26560_2 (tid pg_catalog.tid)
;
----------
[CREATE_TYPE] CREATE DOMAIN testdomain AS text
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."extractq2_2_opt"("t" "public"."int8_tbl") RETURNS TABLE("ret1" bigint)
    LANGUAGE "sql" IMMUTABLE
    AS $$
  select extractq2(t)
$$
;
----------
[CREATE_TABLE] create table pt12 partition of pt for values in (1,2)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table myview drop d
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_19.pg_temp_33778
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30860 RENAME TO reind_con_toast
;
----------
[DROP_CATALOG] DROP DATABASE IF EXISTS sepgsql_test_regression
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub5
;
----------
[DROP_TABLE] drop table T
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_foreign_data_wrapper
;
----------
[DROP_PROG_OBJ] DROP FUNCTION rngfuncb(in f1 int, inout f2 int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE explicitly_temp (a int primary key)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_numeric_compress(internal) PARALLEL SAFE
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION tap_sub_B
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION sum3(anyelement,anyelement,anyelement) returns anyelement AS
'select $1+$2+$3' language sql strict
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE live_measures_copy (uuid, project_uuid, component_uuid, metric_uuid, value, text_value, variation, measure_data, update_marker, created_at, updated_at) AS (SELECT uuid, project_uuid, component_uuid, CAST (metric_id AS VARCHAR (40)) AS metric_uuid, value, text_value, variation, measure_data, update_marker, created_at, updated_at FROM live_measures)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- check change of unrelated column type does not reset the MCV statistics
ALTER TABLE mcv_lists ALTER COLUMN d TYPE VARCHAR(64)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION boolop(_int4, query_int)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table minmaxtest1() inherits (minmaxtest)
;
