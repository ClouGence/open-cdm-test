-- finalfn polymorphic
CREATE FUNCTION ffp(anyarray) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
/* contrib/seg/seg--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Remove @ and ~
DROP OPERATOR @ (seg, seg)
;
CREATE TABLE "public"."tbl_include_box" (
    "c1" integer NOT NULL,
    "c2" integer NOT NULL,
    "c3" integer,
    "c4" "box"
)
;
CREATE TABLE sub_part2(b int, c int8, a numeric)
;
ALTER SUBSCRIPTION regress_doesnotexist CONNECTION 'dbname=regress_doesnotexist2'
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "uint" WITH "simple"
;
CREATE SCHEMA schema_one authorization regress_evt_user
;
-- conflict on errcode
create or replace function raise_test() returns void as $$
begin
  raise division_by_zero using message = 'custom' || ' message', errcode = '22012';
end;
$$ language plpgsql
;
CREATE TABLE space_tbl (d text) TABLESPACE space_test
;
CREATE TABLE prt2_adv_p2 PARTITION OF prt2_adv FOR VALUES FROM (200) TO (400)
;
-- Now make the equal function return false when given two NULLs
create or replace function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return false;
  else
    return $1::int = $2::int;
  end if;
end;
$$ language plpgsql immutable
;
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test0 (provider = icu, locale = ' ||
          quote_literal((SELECT CASE WHEN datlocprovider='i' THEN daticulocale ELSE datcollate END FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
CREATE TABLE parttab (col1 text, key INTEGER PRIMARY KEY, col2 text) PARTITION BY LIST (key)
;
VACUUM hash_split_heap
;
ALTER TABLE "public"."itest9" ALTER COLUMN "a" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest9_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
create table defcheck (a int, b int) partition by list (b)
;
CREATE FOREIGN TABLE foreign_tbl (b int)
  SERVER loopback OPTIONS (table_name 'base_tbl')
;
CREATE FUNCTION isnge(upc, upc)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- stored
CREATE TABLE testpub_gencol (a INT, b INT GENERATED ALWAYS AS (a + 1) STORED NOT NULL)
;
/*
 * 3.3 Test ALTER TABLE SET STATISTICS
 */
CREATE TABLE tbl (c1 int, c2 int)
;
-- Check basic SPI plan invalidation

create function cache_test(int) returns int as $$
declare total int;
begin
	create temp table t1(f1 int);
	insert into t1 values($1);
	insert into t1 values(11);
	insert into t1 values(12);
	insert into t1 values(13);
	select sum(f1) into total from t1;
	drop table t1;
	return total;
end
$$ language plpgsql
;
CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE SEQUENCE schema_not_existing.seq
;
-- succeed
alter table atacc3 add test2 int
;
-- Cannot set an operator that already has a commutator as the commutator
ALTER OPERATOR @=(real, boolean) SET (COMMUTATOR = ===)
;
VACUUM pg_catalog.pg_statistic_ext_data
;
create table if not exists cta_test1 as select * from cta_test
;
CREATE TABLESPACE other LOCATION '/src/src/test/recovery/tmp_check/t_018_wal_optimize_node_minimal_data/tablespace_other'
;
--
-- Constraint-related indexes
--

-- Verify that it works to add primary key / unique to partitioned tables
create table idxpart (a int primary key, b int) partition by range (a)
;
ANALYZE public.pht1_e_p3
;
CREATE OR REPLACE FUNCTION test_evtrig_dropped_objects() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
    LOOP
        IF obj.object_type = 'table' THEN
                EXECUTE format('DROP TABLE IF EXISTS audit_tbls.%I',
					format('%s_%s', obj.schema_name, obj.object_name));
        END IF;

	INSERT INTO dropped_objects
		(type, schema, object) VALUES
		(obj.object_type, obj.schema_name, obj.object_identity);
    END LOOP;
END
$$
;
CREATE TABLE tststats.priv_test_parent_tbl (a int, b int)
;
-- failed (no role membership)
ALTER OPERATOR FAMILY alt_opf3 USING hash SET SCHEMA alt_nsp2
;
ALTER TABLE ONLY "public"."parted_self_fk" ATTACH PARTITION "public"."part2_self_fk" FOR VALUES FROM ('10') TO ('20')
;
--
-- UPDATE syntax tests
--

CREATE TABLE update_test (
    a   INT DEFAULT 10,
    b   INT,
    c   TEXT
)
;
create trigger samename after delete on trigpart execute function trigger_nothing()
;
ALTER OPERATOR FAMILY split_operator_probe.int_hash_family USING hash RENAME TO int_hash_family_renamed
;
CREATE SEQUENCE "public"."sequence_test4"
    START WITH -1
    INCREMENT BY -1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
-- Should work now

DROP FUNCTION int4_casttesttype(int4) CASCADE
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
COMMENT ON TEXT SEARCH DICTIONARY xsyn IS 'eXtended synonym dictionary'
;
DROP FUNCTION getrngfunc1(int)
;
CREATE FUNCTION seg_contains(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE T ALTER COLUMN c_int DROP DEFAULT,
              ALTER COLUMN c_array
                  SET DEFAULT ('{"This", "is", "' || foo(1) ||
                               '", "fantasy"}')::text[]
;
ALTER SEQUENCE "public"."transition_table_level2_level2_no_seq" OWNED BY "public"."transition_table_level2"."level2_no"
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_opclass
;
--
-- Btree operator class for int_custom
--
-- This is required for the type to be usable in extended statistics objects,
-- for attributes and expressions.
--
CREATE OPERATOR CLASS int_custom_ops
  DEFAULT FOR TYPE int_custom USING btree AS
    OPERATOR    1     <,
    OPERATOR    2     <=,
    OPERATOR    3     =,
    OPERATOR    4     >=,
    OPERATOR    5     >,
    FUNCTION    1     int_custom_cmp(int_custom, int_custom)
;
CREATE FUNCTION drop_schema_test1.drop_func_test1() RETURNS VOID AS 'select 2;' LANGUAGE SQL IMMUTABLE
;
ALTER DATABASE "pl_regression" SET bytea_output TO 'hex'
;
CREATE OR REPLACE FUNCTION regexp_match(string citext, pattern citext, flags text) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
CREATE DOMAIN queryfuncs_char2_chk AS char(2) CHECK (VALUE NOT IN ('12'))
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- finalfn polymorphic
CREATE FUNCTION ffp(anyarray) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
----------
[DROP_PROG_OBJ] /* contrib/seg/seg--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Remove @ and ~
DROP OPERATOR @ (seg, seg)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tbl_include_box" (
    "c1" integer NOT NULL,
    "c2" integer NOT NULL,
    "c3" integer,
    "c4" "box"
)
;
----------
[CREATE_TABLE] CREATE TABLE sub_part2(b int, c int8, a numeric)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_doesnotexist CONNECTION 'dbname=regress_doesnotexist2'
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "uint" WITH "simple"
;
----------
[CREATE_SCHEMA] CREATE SCHEMA schema_one authorization regress_evt_user
;
----------
[CREATE_PROG_OBJ] -- conflict on errcode
create or replace function raise_test() returns void as $$
begin
  raise division_by_zero using message = 'custom' || ' message', errcode = '22012';
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE space_tbl (d text) TABLESPACE space_test
;
----------
[CREATE_TABLE] CREATE TABLE prt2_adv_p2 PARTITION OF prt2_adv FOR VALUES FROM (200) TO (400)
;
----------
[CREATE_PROG_OBJ] -- Now make the equal function return false when given two NULLs
create or replace function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return false;
  else
    return $1::int = $2::int;
  end if;
end;
$$ language plpgsql immutable
;
----------
[BLOCK] do $$
BEGIN
  EXECUTE 'CREATE COLLATION test0 (provider = icu, locale = ' ||
          quote_literal((SELECT CASE WHEN datlocprovider='i' THEN daticulocale ELSE datcollate END FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE parttab (col1 text, key INTEGER PRIMARY KEY, col2 text) PARTITION BY LIST (key)
;
----------
[ADMIN_TABLE] VACUUM hash_split_heap
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest9" ALTER COLUMN "a" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest9_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[CREATE_TABLE] create table defcheck (a int, b int) partition by list (b)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE foreign_tbl (b int)
  SERVER loopback OPTIONS (table_name 'base_tbl')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(upc, upc)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- stored
CREATE TABLE testpub_gencol (a INT, b INT GENERATED ALWAYS AS (a + 1) STORED NOT NULL)
;
----------
[CREATE_TABLE] /*
 * 3.3 Test ALTER TABLE SET STATISTICS
 */
CREATE TABLE tbl (c1 int, c2 int)
;
----------
[CREATE_PROG_OBJ] -- Check basic SPI plan invalidation

create function cache_test(int) returns int as $$
declare total int;
begin
	create temp table t1(f1 int);
	insert into t1 values($1);
	insert into t1 values(11);
	insert into t1 values(12);
	insert into t1 values(13);
	select sum(f1) into total from t1;
	drop table t1;
	return total;
end
$$ language plpgsql
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE SEQUENCE schema_not_existing.seq
;
----------
[ALTER_TABLE|ADD_COLUMN] -- succeed
alter table atacc3 add test2 int
;
----------
[ALTER_PROG_OBJ] -- Cannot set an operator that already has a commutator as the commutator
ALTER OPERATOR @=(real, boolean) SET (COMMUTATOR = ===)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_TABLE|SELECT] create table if not exists cta_test1 as select * from cta_test
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE other LOCATION '/src/src/test/recovery/tmp_check/t_018_wal_optimize_node_minimal_data/tablespace_other'
;
----------
[CREATE_TABLE] --
-- Constraint-related indexes
--

-- Verify that it works to add primary key / unique to partitioned tables
create table idxpart (a int primary key, b int) partition by range (a)
;
----------
[ADMIN_TABLE] ANALYZE public.pht1_e_p3
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_evtrig_dropped_objects() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
    LOOP
        IF obj.object_type = 'table' THEN
                EXECUTE format('DROP TABLE IF EXISTS audit_tbls.%I',
					format('%s_%s', obj.schema_name, obj.object_name));
        END IF;

	INSERT INTO dropped_objects
		(type, schema, object) VALUES
		(obj.object_type, obj.schema_name, obj.object_identity);
    END LOOP;
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE tststats.priv_test_parent_tbl (a int, b int)
;
----------
[ALTER_PROG_OBJ] -- failed (no role membership)
ALTER OPERATOR FAMILY alt_opf3 USING hash SET SCHEMA alt_nsp2
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."parted_self_fk" ATTACH PARTITION "public"."part2_self_fk" FOR VALUES FROM ('10') TO ('20')
;
----------
[CREATE_TABLE] --
-- UPDATE syntax tests
--

CREATE TABLE update_test (
    a   INT DEFAULT 10,
    b   INT,
    c   TEXT
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger samename after delete on trigpart execute function trigger_nothing()
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY split_operator_probe.int_hash_family USING hash RENAME TO int_hash_family_renamed
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test4"
    START WITH -1
    INCREMENT BY -1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[DROP_PROG_OBJ] -- Should work now

DROP FUNCTION int4_casttesttype(int4) CASCADE
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY xsyn IS 'eXtended synonym dictionary'
;
----------
[DROP_PROG_OBJ] DROP FUNCTION getrngfunc1(int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_contains(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE T ALTER COLUMN c_int DROP DEFAULT,
              ALTER COLUMN c_array
                  SET DEFAULT ('{"This", "is", "' || foo(1) ||
                               '", "fantasy"}')::text[]
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."transition_table_level2_level2_no_seq" OWNED BY "public"."transition_table_level2"."level2_no"
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_opclass
;
----------
[CREATE_PROG_OBJ] --
-- Btree operator class for int_custom
--
-- This is required for the type to be usable in extended statistics objects,
-- for attributes and expressions.
--
CREATE OPERATOR CLASS int_custom_ops
  DEFAULT FOR TYPE int_custom USING btree AS
    OPERATOR    1     <,
    OPERATOR    2     <=,
    OPERATOR    3     =,
    OPERATOR    4     >=,
    OPERATOR    5     >,
    FUNCTION    1     int_custom_cmp(int_custom, int_custom)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION drop_schema_test1.drop_func_test1() RETURNS VOID AS 'select 2;' LANGUAGE SQL IMMUTABLE
;
----------
[ALTER_CATALOG] ALTER DATABASE "pl_regression" SET bytea_output TO 'hex'
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_match(string citext, pattern citext, flags text) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TYPE] CREATE DOMAIN queryfuncs_char2_chk AS char(2) CHECK (VALUE NOT IN ('12'))
;
