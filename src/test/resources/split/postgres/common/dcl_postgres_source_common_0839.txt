-- ERROR: no usage on FDW
RESET ROLE
;
LOCK TABLE public.plt1_e_p3 IN ACCESS SHARE MODE
;
-- Don't want this to be available to non-superusers.
REVOKE ALL ON FUNCTION pg_stat_statements_reset(Oid, Oid, bigint) FROM PUBLIC
;
EXECUTE dumpFunc('34367')
;
EXECUTE dumpBaseType('26')
;
PREPARE get_type(name) AS SELECT * FROM pg_type WHERE typname = $1
;
ALTER STATISTICS ab1_a_b_stats RENAME TO ab1_a_b_stats_new
;
EXECUTE dumpDomain('36318')
;
COMMIT PREPARED 'xact_009_16'
;
-- Rename a role
ALTER ROLE regress_passwd2 RENAME TO regress_passwd2_new
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] -- ERROR: no usage on FDW
RESET ROLE
;
----------
[SESSION_LOCK] LOCK TABLE public.plt1_e_p3 IN ACCESS SHARE MODE
;
----------
[REVOKE] -- Don't want this to be available to non-superusers.
REVOKE ALL ON FUNCTION pg_stat_statements_reset(Oid, Oid, bigint) FROM PUBLIC
;
----------
[UNSAFE] EXECUTE dumpFunc('34367')
;
----------
[UNSAFE] EXECUTE dumpBaseType('26')
;
----------
[UNSAFE] PREPARE get_type(name) AS SELECT * FROM pg_type WHERE typname = $1
;
----------
[ADMIN_PERFORMANCE] ALTER STATISTICS ab1_a_b_stats RENAME TO ab1_a_b_stats_new
;
----------
[UNSAFE] EXECUTE dumpDomain('36318')
;
----------
[TRANSACTION] COMMIT PREPARED 'xact_009_16'
;
----------
[RENAME_ROLE] -- Rename a role
ALTER ROLE regress_passwd2 RENAME TO regress_passwd2_new
;
