-- mcv with bool
CREATE TABLE mcv_lists_bool (
    a BOOL,
    b BOOL,
    c BOOL
)
WITH (autovacuum_enabled = off)
;
-- partition must have parent's constraints
ALTER TABLE fd_pt2 DETACH PARTITION fd_pt2_1
;
vacuum verbose test1
;
CREATE FUNCTION citext_pattern_ge( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP TABLE varlena_bug
;
DROP TABLE droppk1
;
TRUNCATE TABLE ndistinct
;
CREATE INDEX "hslot_hubname" ON "public"."hslot" USING "btree" ("hubname")
;
CREATE OR REPLACE VIEW viewtest AS
	SELECT * FROM viewtest_tbl
;
-- error
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
CREATE TABLE "public"."sj_t3" (
    "id" integer NOT NULL,
    "a" integer
)
;
CREATE TABLE defprivs (a int)
;
create unique index fruit_index on insertconflicttest(fruit)
;
CREATE TABLE part_drop_index_locking_subpart_child PARTITION OF part_drop_index_locking_subpart FOR VALUES FROM (1) TO (100)
;
alter table def_test alter column c3 set default 30
;
CREATE TABLE tab3 (a int, gen1 int, gen2 int)
;
CREATE TABLE "public"."idxpart_another_1" (
    "a" integer CONSTRAINT "idxpart_another_a_not_null" NOT NULL,
    "b" integer CONSTRAINT "idxpart_another_b_not_null" NOT NULL
)
;
create table clstr_4 as select * from tenk1
;
ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_2" FOR VALUES IN (1600)
;
-- Ensure subplans which don't have a path with the correct pathkeys get
-- sorted correctly.
drop index mcrparted_a_abs_c_idx
;
-- forbidden expressions for partition bound with list partitioned table
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (somename)
;
create table parted_index_col_drop11 partition of parted_index_col_drop1
  for values in (1)
;
create table idxpart1 partition of idxpart for values from (0) to (1000) partition by range (a)
;
CREATE INDEX abbrev_abort_uuids__noabort_decreasing_idx ON abbrev_abort_uuids (noabort_decreasing)
;
CREATE FUNCTION gbt_inet_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE "public"."nv_child_2009" (
    "d" "date",
    CONSTRAINT "nv_child_2009_d_check" CHECK ((("d" >= '2009-01-01'::"date") AND ("d" <= '2009-12-31'::"date")))
)
;
create index pti3 on pt (tdata collate mycollation)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=30672 host=/tmp/INMnCgU8So dbname=postgres' PUBLICATION pub1
;
CREATE INDEX idx_temporal3_uq ON temporal3 USING gist (id, valid_at)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
ALTER TABLE pg_collation ADD UNIQUE USING INDEX pg_collation_name_enc_nsp_index
;
CREATE TABLE "public"."test_type_diff_c" (
    "f1" integer,
    "extra" smallint,
    "f2" bigint
)
;
DROP TABLE bug18634s
;
create table parted_boolean_greater partition of parted_boolean_col
  for values in ('foo' > 'bar')
;
-- failed (name conflict)
ALTER TEXT SEARCH TEMPLATE alt_ts_temp1 RENAME TO alt_ts_temp3
;
CREATE INDEX idx_timestamptz ON test_timestamptz USING gin (i)
;
-- rewrite table to remove its TOAST table; need a non-constant column default
alter table test_storage add b int default random()::int
;
-- no error
-- ... and it had better match the type of the parameter
CALL ptest9(1./0.)
;
ANALYZE public.test_type_diff2_c2
;
DROP FUNCTION ifun(int8)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_partitioned_table
;
ALTER TABLE ab1 DROP COLUMN a
;
/*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
CREATE TABLESPACE ts1 LOCATION '/tmp/RyTpc9w4ud'
;
--
-- CREATE OR REPLACE VIEW
--

CREATE TABLE viewtest_tbl (a int, b int, c numeric(10,1), d text COLLATE "C")
;
COMMENT ON COLUMN default_test_row.nope IS 'bad comment'
;
CREATE TABLE cmdlog (cmd text)
;
create table tprt_2 partition of tprt for values from (501) to (1001)
;
CREATE TABLE gtestxx_3 (a int NOT NULL, b int GENERATED ALWAYS AS (a * 2) STORED)
;
CREATE INDEX tbspace_reindex_part_index_10 ON ONLY tbspace_reindex_part_10 (c1)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- mcv with bool
CREATE TABLE mcv_lists_bool (
    a BOOL,
    b BOOL,
    c BOOL
)
WITH (autovacuum_enabled = off)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- partition must have parent's constraints
ALTER TABLE fd_pt2 DETACH PARTITION fd_pt2_1
;
----------
[ADMIN_TABLE] vacuum verbose test1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_pattern_ge( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE varlena_bug
;
----------
[DROP_TABLE] DROP TABLE droppk1
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE ndistinct
;
----------
[ADD_INDEX] CREATE INDEX "hslot_hubname" ON "public"."hslot" USING "btree" ("hubname")
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW viewtest AS
	SELECT * FROM viewtest_tbl
;
----------
[CREATE_VIEW] -- error
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TABLE "public"."sj_t3" (
    "id" integer NOT NULL,
    "a" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE defprivs (a int)
;
----------
[ADD_INDEX] create unique index fruit_index on insertconflicttest(fruit)
;
----------
[CREATE_TABLE] CREATE TABLE part_drop_index_locking_subpart_child PARTITION OF part_drop_index_locking_subpart FOR VALUES FROM (1) TO (100)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table def_test alter column c3 set default 30
;
----------
[CREATE_TABLE] CREATE TABLE tab3 (a int, gen1 int, gen2 int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart_another_1" (
    "a" integer CONSTRAINT "idxpart_another_a_not_null" NOT NULL,
    "b" integer CONSTRAINT "idxpart_another_b_not_null" NOT NULL
)
;
----------
[CREATE_TABLE|SELECT] create table clstr_4 as select * from tenk1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_2" FOR VALUES IN (1600)
;
----------
[DROP_INDEX] -- Ensure subplans which don't have a path with the correct pathkeys get
-- sorted correctly.
drop index mcrparted_a_abs_c_idx
;
----------
[CREATE_TABLE] -- forbidden expressions for partition bound with list partitioned table
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (somename)
;
----------
[CREATE_TABLE] create table parted_index_col_drop11 partition of parted_index_col_drop1
  for values in (1)
;
----------
[CREATE_TABLE] create table idxpart1 partition of idxpart for values from (0) to (1000) partition by range (a)
;
----------
[ADD_INDEX] CREATE INDEX abbrev_abort_uuids__noabort_decreasing_idx ON abbrev_abort_uuids (noabort_decreasing)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_inet_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."nv_child_2009" (
    "d" "date",
    CONSTRAINT "nv_child_2009_d_check" CHECK ((("d" >= '2009-01-01'::"date") AND ("d" <= '2009-12-31'::"date")))
)
;
----------
[ADD_INDEX] create index pti3 on pt (tdata collate mycollation)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=30672 host=/tmp/INMnCgU8So dbname=postgres' PUBLICATION pub1
;
----------
[ADD_INDEX] CREATE INDEX idx_temporal3_uq ON temporal3 USING gist (id, valid_at)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_collation ADD UNIQUE USING INDEX pg_collation_name_enc_nsp_index
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_type_diff_c" (
    "f1" integer,
    "extra" smallint,
    "f2" bigint
)
;
----------
[DROP_TABLE] DROP TABLE bug18634s
;
----------
[CREATE_TABLE] create table parted_boolean_greater partition of parted_boolean_col
  for values in ('foo' > 'bar')
;
----------
[ALTER_POLICY] -- failed (name conflict)
ALTER TEXT SEARCH TEMPLATE alt_ts_temp1 RENAME TO alt_ts_temp3
;
----------
[ADD_INDEX] CREATE INDEX idx_timestamptz ON test_timestamptz USING gin (i)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- rewrite table to remove its TOAST table; need a non-constant column default
alter table test_storage add b int default random()::int
;
----------
[CALL_PROG_OBJ] -- no error
-- ... and it had better match the type of the parameter
CALL ptest9(1./0.)
;
----------
[ADMIN_TABLE] ANALYZE public.test_type_diff2_c2
;
----------
[DROP_PROG_OBJ] DROP FUNCTION ifun(int8)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_partitioned_table
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ab1 DROP COLUMN a
;
----------
[CREATE_POLICY] /*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/tmp/RyTpc9w4ud'
;
----------
[CREATE_TABLE] --
-- CREATE OR REPLACE VIEW
--

CREATE TABLE viewtest_tbl (a int, b int, c numeric(10,1), d text COLLATE "C")
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN default_test_row.nope IS 'bad comment'
;
----------
[CREATE_TABLE] CREATE TABLE cmdlog (cmd text)
;
----------
[CREATE_TABLE] create table tprt_2 partition of tprt for values from (501) to (1001)
;
----------
[CREATE_TABLE] CREATE TABLE gtestxx_3 (a int NOT NULL, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[ADD_INDEX] CREATE INDEX tbspace_reindex_part_index_10 ON ONLY tbspace_reindex_part_10 (c1)
;
