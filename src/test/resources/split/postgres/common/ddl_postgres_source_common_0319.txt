CREATE TABLE part_55_66 PARTITION OF list_parted2 FOR VALUES IN (55, 66)
;
TRUNCATE mcv_lists
;
ANALYZE public.phone
;
create aggregate my_avg(int4)
(
   stype = avg_state,
   sfunc = avg_transfn,
   finalfunc = avg_finalfn
)
;
-- shadowing in cursor definitions
create or replace function shadowtest()
	returns void as $$
declare
f1 int;
c1 cursor (f1 int) for select 1;
begin
end$$ language plpgsql
;
CREATE TABLE TIMESTAMPTZ_TBL (d1 timestamp(2) with time zone)
;
CREATE FUNCTION pg_temp.f(group_size BIGINT) RETURNS SETOF integer[]
AS $$
    SELECT array_agg(s) OVER w
      FROM generate_series(1,5) s
    WINDOW w AS (ORDER BY s ROWS BETWEEN CURRENT ROW AND GROUP_SIZE FOLLOWING)
$$ LANGUAGE SQL STABLE
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_amproc
;
DROP TABLE stats_temp
;
-- tests for RETURN QUERY
create function ret_query1(out int, out int) returns setof record as $$
begin
    $1 := -1;
    $2 := -2;
    return next;
    return query select x + 1, x * 10 from generate_series(0, 10) s (x);
    return next;
end;
$$ language plpgsql
;
CREATE FUNCTION get_raw_page(text, text, int8)
RETURNS bytea
AS '$libdir/pageinspect', 'get_raw_page_fork_1_9'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP OPERATOR FAMILY alt_opf13 USING hash
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_depend
;
ALTER TABLE ONLY "public"."test_type_diff2_c1" INHERIT "public"."test_type_diff2"
;
CREATE FUNCTION "public"."large_val"() RETURNS "text"
    LANGUAGE "sql"
    AS $$select array_agg(md5(g::text))::text from generate_series(1, 256) g$$
;
CREATE SCHEMA split_owned_schema AUTHORIZATION split_owned_source_a
;
CREATE TABLE dummy_seclabel_tbl1 (a int, b text)
;
ALTER INDEX pg_toast.pg_toast_33224_index RENAME TO reind_con_toast_idx
;
CREATE SCHEMA split_sequence_next
;
CREATE VIEW toyemp AS
   SELECT name, age, location, 12*salary AS annualsal
   FROM emp
;
drop table other_tbl
;
---------------------------------------------------
-- ISBN:
CREATE FUNCTION btisbncmp(isbn, isbn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TRIGGER trans_deletetrig
  AFTER DELETE ON range_parted REFERENCING OLD TABLE AS old_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trans_updatetrigfunc()
;
alter index idxpart_c attach partition idxpart1_c
;
create database regress_public_owner
;
-- When a table is attached a partition and it already has an index, a
-- duplicate index should not get created, but rather the index becomes
-- attached to the parent's index.
create table idxpart (a int, b int, c text, d bool) partition by range (a)
;
analyze patest1
;
CREATE TABLE "public"."renamecolumn" (
    "d" integer,
    "w" integer
)
;
create table tprt_5 partition of tprt for values from (3001) to (4001)
;
ALTER TYPE enum_test ADD VALUE 'aaa' BEFORE 'foo'
;
IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest3
  OPTIONS (import_collate 'false', import_generated 'false', import_not_null 'false')
;
CREATE TABLE base_tbl_hist(ts timestamptz default now(), a int, b text)
;
ALTER TABLE ONLY "fkpart4"."droppk" ATTACH PARTITION "fkpart4"."droppk_d" DEFAULT
;
create temp table upsert(key int4 primary key, val text)
;
-- fail at initialization assignment

create or replace function doubledecrement(p1 pos_int) returns pos_int as $$
declare v pos_int := 1;
begin
    v := p1 - 1;
    return v - 1;
end$$ language plpgsql
;
ALTER TABLE pg_toast.pg_toast_28374 RENAME TO reind_con_toast
;
CREATE DATABASE foobar6 STRATEGY wal_log TEMPLATE foobar2
;
ANALYZE pg_temp_13.pg_temp_25962_2
;
CREATE FUNCTION "public"."make_tuple_indirect"("record") RETURNS "record"
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'make_tuple_indirect'
;
DROP TABLE ints
;
CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_xcCW'
;
CREATE TABLE accounts_ext (accountid text PRIMARY KEY, balance numeric not null, other text)
;
ANALYZE pg_temp_13.pg_temp_26675
;
ALTER OPERATOR FAMILY gist_vbit_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
alter table child alter a set not null
;
alter table idxpart1 attach partition idxpart11 for values from (0) to (1000)
;
CREATE TABLE INET_TBL (c cidr, i inet)
;
CREATE TABLE pa_target (key integer, val text)
	  PARTITION BY LIST (key)
;
CREATE TABLE two_not_null_constraints (
   col integer NOT NULL,
   CONSTRAINT two_not_null_constraints_col_not_null CHECK (col IS NOT NULL)
)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_publication_rel
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE part_55_66 PARTITION OF list_parted2 FOR VALUES IN (55, 66)
;
----------
[TRUNCATE_TABLE] TRUNCATE mcv_lists
;
----------
[ADMIN_TABLE] ANALYZE public.phone
;
----------
[CREATE_PROG_OBJ] create aggregate my_avg(int4)
(
   stype = avg_state,
   sfunc = avg_transfn,
   finalfunc = avg_finalfn
)
;
----------
[CREATE_PROG_OBJ] -- shadowing in cursor definitions
create or replace function shadowtest()
	returns void as $$
declare
f1 int;
c1 cursor (f1 int) for select 1;
begin
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE TIMESTAMPTZ_TBL (d1 timestamp(2) with time zone)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_temp.f(group_size BIGINT) RETURNS SETOF integer[]
AS $$
    SELECT array_agg(s) OVER w
      FROM generate_series(1,5) s
    WINDOW w AS (ORDER BY s ROWS BETWEEN CURRENT ROW AND GROUP_SIZE FOLLOWING)
$$ LANGUAGE SQL STABLE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_amproc
;
----------
[DROP_TABLE] DROP TABLE stats_temp
;
----------
[CREATE_PROG_OBJ] -- tests for RETURN QUERY
create function ret_query1(out int, out int) returns setof record as $$
begin
    $1 := -1;
    $2 := -2;
    return next;
    return query select x + 1, x * 10 from generate_series(0, 10) s (x);
    return next;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION get_raw_page(text, text, int8)
RETURNS bytea
AS '$libdir/pageinspect', 'get_raw_page_fork_1_9'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf13 USING hash
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_depend
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."test_type_diff2_c1" INHERIT "public"."test_type_diff2"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."large_val"() RETURNS "text"
    LANGUAGE "sql"
    AS $$select array_agg(md5(g::text))::text from generate_series(1, 256) g$$
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_owned_schema AUTHORIZATION split_owned_source_a
;
----------
[CREATE_TABLE] CREATE TABLE dummy_seclabel_tbl1 (a int, b text)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33224_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_sequence_next
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW toyemp AS
   SELECT name, age, location, 12*salary AS annualsal
   FROM emp
;
----------
[DROP_TABLE] drop table other_tbl
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISBN:
CREATE FUNCTION btisbncmp(isbn, isbn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trans_deletetrig
  AFTER DELETE ON range_parted REFERENCING OLD TABLE AS old_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trans_updatetrigfunc()
;
----------
[ALTER_INDEX] alter index idxpart_c attach partition idxpart1_c
;
----------
[CREATE_CATALOG] create database regress_public_owner
;
----------
[CREATE_TABLE] -- When a table is attached a partition and it already has an index, a
-- duplicate index should not get created, but rather the index becomes
-- attached to the parent's index.
create table idxpart (a int, b int, c text, d bool) partition by range (a)
;
----------
[ADMIN_TABLE] analyze patest1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."renamecolumn" (
    "d" integer,
    "w" integer
)
;
----------
[CREATE_TABLE] create table tprt_5 partition of tprt for values from (3001) to (4001)
;
----------
[ALTER_TYPE] ALTER TYPE enum_test ADD VALUE 'aaa' BEFORE 'foo'
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest3
  OPTIONS (import_collate 'false', import_generated 'false', import_not_null 'false')
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl_hist(ts timestamptz default now(), a int, b text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart4"."droppk" ATTACH PARTITION "fkpart4"."droppk_d" DEFAULT
;
----------
[CREATE_TABLE] create temp table upsert(key int4 primary key, val text)
;
----------
[CREATE_PROG_OBJ] -- fail at initialization assignment

create or replace function doubledecrement(p1 pos_int) returns pos_int as $$
declare v pos_int := 1;
begin
    v := p1 - 1;
    return v - 1;
end$$ language plpgsql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28374 RENAME TO reind_con_toast
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar6 STRATEGY wal_log TEMPLATE foobar2
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25962_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."make_tuple_indirect"("record") RETURNS "record"
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'make_tuple_indirect'
;
----------
[DROP_TABLE] DROP TABLE ints
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_xcCW'
;
----------
[CREATE_TABLE] CREATE TABLE accounts_ext (accountid text PRIMARY KEY, balance numeric not null, other text)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26675
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_vbit_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table child alter a set not null
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart1 attach partition idxpart11 for values from (0) to (1000)
;
----------
[CREATE_TABLE] CREATE TABLE INET_TBL (c cidr, i inet)
;
----------
[CREATE_TABLE] CREATE TABLE pa_target (key integer, val text)
	  PARTITION BY LIST (key)
;
----------
[CREATE_TABLE] CREATE TABLE two_not_null_constraints (
   col integer NOT NULL,
   CONSTRAINT two_not_null_constraints_col_not_null CHECK (col IS NOT NULL)
)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_publication_rel
;
