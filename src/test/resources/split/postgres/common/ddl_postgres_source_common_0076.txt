-- check that wholerow vars in the RETURNING list work with partitioned tables
create table returningwrtest (a int) partition by list (a)
;
alter table p1_c1 drop constraint f1_pos
;
ALTER TABLE pg_toast.pg_toast_30365 RENAME TO reind_con_toast
;
CREATE TABLE "public"."part32_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
;
CREATE FUNCTION fn_op5(int8, int8)
RETURNS int8 AS $$
    SELECT NULL::int8;
$$ LANGUAGE sql IMMUTABLE
;
DROP TABLE idxpart
;
VACUUM (PARALLEL 2) pg_catalog.pg_amop
;
ALTER TABLE pg_transform ADD PRIMARY KEY USING INDEX pg_transform_oid_index
;
CREATE VIEW rw_view2 AS
  SELECT * FROM rw_view1 WHERE a > b WITH LOCAL CHECK OPTION
;
-- Likewise for domains within domains over array of composite
create domain ddtest1d as ddtest1[]
;
CREATE OPERATOR CLASS isbn_ops DEFAULT
	FOR TYPE isbn USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btisbncmp(isbn, isbn)
;
DROP TABLE regtest_table_4 CASCADE
;
ALTER TABLE pg_toast.pg_toast_33100 RENAME TO reind_con_toast
;
CREATE TABLE "generated_stored_tests"."gtest_child" (
    "f1" "date" CONSTRAINT "gtest_parent_f1_not_null" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
ALTER TABLE circles ADD EXCLUDE USING gist
  (c1 WITH &&, (c2::circle) WITH &&)
;
CREATE RULE v1_ins AS ON INSERT TO rule_v1
  DO ALSO INSERT INTO rule_t1
  SELECT * FROM (SELECT a + 10 FROM rule_t1 WHERE a = NEW.a) tt
;
ALTER FUNCTION gbt_enum_picksplit(internal, internal) PARALLEL SAFE
;
DROP TABLE some_t
;
CREATE TYPE price_key AS (
    id INTEGER
)
;
CREATE TABLE "public"."pitest1" (
    "f1" "date" NOT NULL,
    "f2" "text",
    "f3" bigint NOT NULL
)
PARTITION BY RANGE ("f1")
;
ALTER TABLE range_parted ATTACH PARTITION range_part1 FOR VALUES FROM (1, 1) TO (1, 10)
;
drop type comptype cascade
;
drop function f1(x anyrange)
;
create table test_mode (a int)
;
DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19643 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
DROP FUNCTION insert_unique(integer, text)
;
create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = '1234F', detail = 'some detail info';
  exception
    when sqlstate '1234F' then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
CREATE FUNCTION levenshtein_less_equal (text,text,int,int,int,int) RETURNS int
AS '$libdir/fuzzystrmatch','levenshtein_less_equal_with_costs'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
alter table another
  alter f1 type text using f2 || ' and ' || f3 || ' more',
  alter f2 type bigint using f1 * 10,
  drop column f3
;
create table bool_rp (b bool, a int) partition by range(b,a)
;
-- Test domains over composites

create type comptype as (r float8, i float8)
;
create type nestedcomposite as (x int8_tbl)
;
CREATE TABLE will_get_vacuumed (a int, b text)
;
-- 2 columns (1 table), mismatched types
create table pktable_base(base1 int not null, base2 int)
;
CREATE TABLE gtest24at (a int PRIMARY KEY)
;
--
-- Comparison functions for int_custom
--
-- These are required to create a btree operator class, which is needed
-- for the type to be usable in extended statistics objects.
--
CREATE FUNCTION int_custom_eq(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
--
-- Check behavior when subtype lacks a hash function
--

create type cashrange as range (subtype = money)
;
ALTER INDEX e DEPENDS ON EXTENSION test_ext5
;
CREATE FUNCTION gbt_enum_consistent(internal,anyenum,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE "public"."renamecolumnanother" (
    "d" integer,
    "a" integer,
    "c" integer,
    "w" integer
)
;
CREATE TABLE alpha_neg PARTITION OF alpha FOR VALUES FROM ('-Infinity') TO (0) PARTITION BY RANGE (b)
;
-- fail due to "does not exist"
drop table trigpart4
;
ALTER TABLE range_parted ATTACH partition part3 FOR VALUES FROM (2, 10) TO (2, 20)
;
create aggregate least_agg(int4) (
  stype = int8, sfunc = least_accum
)
;
create temp table copytest3 (
	c1 int,
	"col with , comma" text,
	"col with "" quote"  int)
;
create index spgist_text_idx on spgist_text_tbl using spgist(t)
;
ALTER TABLE pg_temp_2.pg_temp_28316_2 ADD COLUMN newdata pg_temp_2.pg_temp_28316
;
alter table parent1 alter column f1 type bigint
;
create function continue_test1() returns void as $$
declare _i integer = 0; _r record;
begin
  raise notice '---1---';
  loop
    _i := _i + 1;
    raise notice '%', _i;
    continue when _i < 10;
    exit;
  end loop;

  raise notice '---2---';
  <<lbl>>
  loop
    _i := _i - 1;
    loop
      raise notice '%', _i;
      continue lbl when _i > 0;
      exit lbl;
    end loop;
  end loop;

  raise notice '---3---';
  <<the_loop>>
  while _i < 10 loop
    _i := _i + 1;
    continue the_loop when _i % 2 = 0;
    raise notice '%', _i;
  end loop;

  raise notice '---4---';
  for _i in 1..10 loop
    begin
      -- applies to outer loop, not the nested begin block
      continue when _i < 5;
      raise notice '%', _i;
    end;
  end loop;

  raise notice '---5---';
  for _r in select * from conttesttbl loop
    continue when _r.v <= 20;
    raise notice '%', _r.v;
  end loop;

  raise notice '---6---';
  for _r in execute 'select * from conttesttbl' loop
    continue when _r.v <= 20;
    raise notice '%', _r.v;
  end loop;

  raise notice '---7---';
  <<looplabel>>
  for _i in 1..3 loop
    continue looplabel when _i = 2;
    raise notice '%', _i;
  end loop;

  raise notice '---8---';
  _i := 1;
  while _i <= 3 loop
    raise notice '%', _i;
    _i := _i + 1;
    continue when _i = 3;
  end loop;

  raise notice '---9---';
  for _r in select * from conttesttbl order by v limit 1 loop
    raise notice '%', _r.v;
    continue;
  end loop;

  raise notice '---10---';
  for _r in execute 'select * from conttesttbl order by v limit 1' loop
    raise notice '%', _r.v;
    continue;
  end loop;

  raise notice '---11---';
  <<outerlooplabel>>
  for _i in 1..2 loop
    raise notice 'outer %', _i;
    <<innerlooplabel>>
    for _j in 1..3 loop
      continue outerlooplabel when _j = 2;
      raise notice 'inner %', _j;
    end loop;
  end loop;
end; $$ language plpgsql
;
create table notnull_tbl4 (a int not null constraint foo not null)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- check that wholerow vars in the RETURNING list work with partitioned tables
create table returningwrtest (a int) partition by list (a)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table p1_c1 drop constraint f1_pos
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30365 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part32_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn_op5(int8, int8)
RETURNS int8 AS $$
    SELECT NULL::int8;
$$ LANGUAGE sql IMMUTABLE
;
----------
[DROP_TABLE] DROP TABLE idxpart
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_amop
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_transform ADD PRIMARY KEY USING INDEX pg_transform_oid_index
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS
  SELECT * FROM rw_view1 WHERE a > b WITH LOCAL CHECK OPTION
;
----------
[CREATE_TYPE] -- Likewise for domains within domains over array of composite
create domain ddtest1d as ddtest1[]
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS isbn_ops DEFAULT
	FOR TYPE isbn USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btisbncmp(isbn, isbn)
;
----------
[DROP_TABLE] DROP TABLE regtest_table_4 CASCADE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33100 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest_child" (
    "f1" "date" CONSTRAINT "gtest_parent_f1_not_null" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE circles ADD EXCLUDE USING gist
  (c1 WITH &&, (c2::circle) WITH &&)
;
----------
[CREATE_POLICY] CREATE RULE v1_ins AS ON INSERT TO rule_v1
  DO ALSO INSERT INTO rule_t1
  SELECT * FROM (SELECT a + 10 FROM rule_t1 WHERE a = NEW.a) tt
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_enum_picksplit(internal, internal) PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE some_t
;
----------
[CREATE_TYPE] CREATE TYPE price_key AS (
    id INTEGER
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest1" (
    "f1" "date" NOT NULL,
    "f2" "text",
    "f3" bigint NOT NULL
)
PARTITION BY RANGE ("f1")
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE range_parted ATTACH PARTITION range_part1 FOR VALUES FROM (1, 1) TO (1, 10)
;
----------
[DROP_TYPE] drop type comptype cascade
;
----------
[DROP_PROG_OBJ] drop function f1(x anyrange)
;
----------
[CREATE_TABLE] create table test_mode (a int)
;
----------
[BLOCK] DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19643 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION insert_unique(integer, text)
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = '1234F', detail = 'some detail info';
  exception
    when sqlstate '1234F' then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION levenshtein_less_equal (text,text,int,int,int,int) RETURNS int
AS '$libdir/fuzzystrmatch','levenshtein_less_equal_with_costs'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN|DROP_COLUMN] alter table another
  alter f1 type text using f2 || ' and ' || f3 || ' more',
  alter f2 type bigint using f1 * 10,
  drop column f3
;
----------
[CREATE_TABLE] create table bool_rp (b bool, a int) partition by range(b,a)
;
----------
[CREATE_TYPE] -- Test domains over composites

create type comptype as (r float8, i float8)
;
----------
[CREATE_TYPE] create type nestedcomposite as (x int8_tbl)
;
----------
[CREATE_TABLE] CREATE TABLE will_get_vacuumed (a int, b text)
;
----------
[CREATE_TABLE] -- 2 columns (1 table), mismatched types
create table pktable_base(base1 int not null, base2 int)
;
----------
[CREATE_TABLE] CREATE TABLE gtest24at (a int PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] --
-- Comparison functions for int_custom
--
-- These are required to create a btree operator class, which is needed
-- for the type to be usable in extended statistics objects.
--
CREATE FUNCTION int_custom_eq(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TYPE] --
-- Check behavior when subtype lacks a hash function
--

create type cashrange as range (subtype = money)
;
----------
[ALTER_INDEX] ALTER INDEX e DEPENDS ON EXTENSION test_ext5
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_enum_consistent(internal,anyenum,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."renamecolumnanother" (
    "d" integer,
    "a" integer,
    "c" integer,
    "w" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE alpha_neg PARTITION OF alpha FOR VALUES FROM ('-Infinity') TO (0) PARTITION BY RANGE (b)
;
----------
[DROP_TABLE] -- fail due to "does not exist"
drop table trigpart4
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE range_parted ATTACH partition part3 FOR VALUES FROM (2, 10) TO (2, 20)
;
----------
[CREATE_PROG_OBJ] create aggregate least_agg(int4) (
  stype = int8, sfunc = least_accum
)
;
----------
[CREATE_TABLE] create temp table copytest3 (
	c1 int,
	"col with , comma" text,
	"col with "" quote"  int)
;
----------
[ADD_INDEX] create index spgist_text_idx on spgist_text_tbl using spgist(t)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_2.pg_temp_28316_2 ADD COLUMN newdata pg_temp_2.pg_temp_28316
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table parent1 alter column f1 type bigint
;
----------
[CREATE_PROG_OBJ] create function continue_test1() returns void as $$
declare _i integer = 0; _r record;
begin
  raise notice '---1---';
  loop
    _i := _i + 1;
    raise notice '%', _i;
    continue when _i < 10;
    exit;
  end loop;

  raise notice '---2---';
  <<lbl>>
  loop
    _i := _i - 1;
    loop
      raise notice '%', _i;
      continue lbl when _i > 0;
      exit lbl;
    end loop;
  end loop;

  raise notice '---3---';
  <<the_loop>>
  while _i < 10 loop
    _i := _i + 1;
    continue the_loop when _i % 2 = 0;
    raise notice '%', _i;
  end loop;

  raise notice '---4---';
  for _i in 1..10 loop
    begin
      -- applies to outer loop, not the nested begin block
      continue when _i < 5;
      raise notice '%', _i;
    end;
  end loop;

  raise notice '---5---';
  for _r in select * from conttesttbl loop
    continue when _r.v <= 20;
    raise notice '%', _r.v;
  end loop;

  raise notice '---6---';
  for _r in execute 'select * from conttesttbl' loop
    continue when _r.v <= 20;
    raise notice '%', _r.v;
  end loop;

  raise notice '---7---';
  <<looplabel>>
  for _i in 1..3 loop
    continue looplabel when _i = 2;
    raise notice '%', _i;
  end loop;

  raise notice '---8---';
  _i := 1;
  while _i <= 3 loop
    raise notice '%', _i;
    _i := _i + 1;
    continue when _i = 3;
  end loop;

  raise notice '---9---';
  for _r in select * from conttesttbl order by v limit 1 loop
    raise notice '%', _r.v;
    continue;
  end loop;

  raise notice '---10---';
  for _r in execute 'select * from conttesttbl order by v limit 1' loop
    raise notice '%', _r.v;
    continue;
  end loop;

  raise notice '---11---';
  <<outerlooplabel>>
  for _i in 1..2 loop
    raise notice 'outer %', _i;
    <<innerlooplabel>>
    for _j in 1..3 loop
      continue outerlooplabel when _j = 2;
      raise notice 'inner %', _j;
    end loop;
  end loop;
end; $$ language plpgsql
;
----------
[CREATE_TABLE] create table notnull_tbl4 (a int not null constraint foo not null)
;
