-- streaming test with sub-transaction and PREPARE/COMMIT PREPARED but with
-- filtered gid. gids with '_nodecode' will not be decoded at prepare time.
BEGIN
;
SET search_path to public, test_ns_schema_1
;
COPY public.will_get_vacuumed (a, b) TO stdout
;
-- or put it last for security
set search_path = public, pg_temp
;
EXECUTE dumpFunc('34813')
;
ALTER TABLE public.rule_merge1 OWNER TO zyc
;
ALTER TABLE generated_stored_tests.gtest3a OWNER TO zyc
;
SET CONSTRAINTS split_session_ext.child_parent_fk IMMEDIATE
;
EXECUTE dumpAgg('20577')
;
EXECUTE dumpFunc('28031')
;
------------------------------------------------------------------------------------------
[TRANSACTION] -- streaming test with sub-transaction and PREPARE/COMMIT PREPARED but with
-- filtered gid. gids with '_nodecode' will not be decoded at prepare time.
BEGIN
;
----------
[SESSION_SETTING_WRITE] SET search_path to public, test_ns_schema_1
;
----------
[DATA_EXPORT] COPY public.will_get_vacuumed (a, b) TO stdout
;
----------
[SESSION_SETTING_WRITE] -- or put it last for security
set search_path = public, pg_temp
;
----------
[UNSAFE] EXECUTE dumpFunc('34813')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.rule_merge1 OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE generated_stored_tests.gtest3a OWNER TO zyc
;
----------
[TRANSACTION] SET CONSTRAINTS split_session_ext.child_parent_fk IMMEDIATE
;
----------
[UNSAFE] EXECUTE dumpAgg('20577')
;
----------
[UNSAFE] EXECUTE dumpFunc('28031')
;
