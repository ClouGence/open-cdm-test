ALTER TABLE schema_one.table_one DROP COLUMN a
;
-- Test that trigger on remote table works as expected
CREATE OR REPLACE FUNCTION "S 1".F_BRTRIG() RETURNS trigger AS $$
BEGIN
    NEW.c3 = NEW.c3 || '_trig_update';
    RETURN NEW;
END;
$$ LANGUAGE plpgsql
;
create function execute_text_query_heap(query_sql text)
returns setof text
language plpgsql
as
$$
begin
  set enable_seqscan = on;
  set enable_bitmapscan = off;
  return query execute query_sql;
end;
$$
;
CREATE DATABASE foobar2 LC_COLLATE 'foo''; SELECT ''1'
;
CREATE TEMP SEQUENCE myseq2
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '.')
;
CREATE SUBSCRIPTION tap_sub_sch CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION tap_pub_sch
;
/*
 * Injection point related functions
 */
CREATE FUNCTION inj_io_short_read_attach(result int)
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE FOREIGN TABLE ctl_foreign_table2(LIKE ctl_table INCLUDING ALL) SERVER ctl_s0
;
CREATE INDEX gin_pvactst ON pvactst USING gin (a)
;
CREATE TABLE ffk (a int, b int REFERENCES pk) PARTITION BY list (a)
;
alter domain con drop constraint if exists nonexistent
;
ALTER TABLE ONLY "public"."plt1" ATTACH PARTITION "public"."plt1_p1" FOR VALUES IN ('0000', '0003', '0004', '0010')
;
create unlogged table gist_tbl (b box)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_template
;
do $$
declare
 p_CurData refcursor;
 val int;
begin
 open p_CurData scroll for select 42;
 fetch p_CurData into val;
 raise notice 'val = %', val;
end; $$
;
-- set defaults on views: we need to create a view, add a rule
-- to allow insertions into it, and then alter the view to add
-- a default
create view def_view_test as select * from def_test
;
-- partitioned table child
ALTER TABLE regtest_table_part ALTER p SET DEFAULT 'abcd'
;
CREATE TABLE test_inet (
	i inet
)
;
CREATE FUNCTION _pg_truetypmod(pg_attribute, pg_type) RETURNS int4
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT CASE WHEN $2.typtype = 'd' THEN $2.typtypmod ELSE $1.atttypmod END$$
;
--
-- Verify that you can't create a trigger with transition tables with
-- a column list.
--

create trigger my_table_col_update_trig
  after update of b on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
drop function testoa(x1 int, x2 int, x3 int)
;
CREATE FUNCTION ffnp(int[]) returns int[] as
'select $1' LANGUAGE SQL
;
CREATE FUNCTION gin_extract_value_int8(int8, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE pg_toast.pg_toast_30578 RENAME TO reind_con_toast
;
ALTER FUNCTION regtest_func_1(text) RENAME TO regtest_func
;
alter table idxpart0 add primary key (a)
;
alter table pgbench_branches add primary key (bid) using index tablespace "regress_pgbench_tap_1_ts"
;
drop table inh_nn_parent, inh_nn_child, inh_nn_child2
;
CREATE UNIQUE INDEX sj_temp_idx ON sj(a,b)
;
ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk
;
ALTER DATABASE "contrib_regression" SET lc_monetary TO 'C'
;
create table tt14t (f1 text, f2 text, f3 text, f4 text)
;
VACUUM (ANALYZE) pg_catalog.pg_auth_members
;
create table t1 (id integer, xml_data xml)
;
CREATE FUNCTION subpath(ltree,int4)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE "public"."idxpart22" (
    "a" integer
)
;
-- temp tables
CREATE TEMP TABLE stats_temp(i int)
;
CREATE FUNCTION connectby(text,text,text,text,int)
RETURNS setof record
AS '$libdir/tablefunc','connectby_text'
LANGUAGE C STABLE STRICT
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_statistic_ext
;
ALTER TABLE regtest_table DISABLE TRIGGER regtest_test_trig
;
CREATE VIEW functestv3 AS SELECT * FROM functest3
;
CREATE FUNCTION "public"."whoami"() RETURNS "text"
    LANGUAGE "sql"
    AS $$select 'public'::text$$
;
CREATE FUNCTION gseg_decompress(internal)
RETURNS internal
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
alter table p2 add check (f2>0)
;
CREATE TABLE test_drop_constr_parent (c text CHECK (c IS NOT NULL))
;
CREATE TABLE clstrpart2 PARTITION OF clstrpart FOR VALUES FROM (10) TO (20)
;
DROP FUNCTION f_test(int)
;
ALTER TABLE pg_toast.pg_toast_30554 RENAME TO reind_con_toast
;
-- Try to build index on table that already contains data
CREATE UNIQUE INDEX covering_pkey on covering_index_heap (f1,f2) INCLUDE(f3)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE schema_one.table_one DROP COLUMN a
;
----------
[CREATE_PROG_OBJ] -- Test that trigger on remote table works as expected
CREATE OR REPLACE FUNCTION "S 1".F_BRTRIG() RETURNS trigger AS $$
BEGIN
    NEW.c3 = NEW.c3 || '_trig_update';
    RETURN NEW;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] create function execute_text_query_heap(query_sql text)
returns setof text
language plpgsql
as
$$
begin
  set enable_seqscan = on;
  set enable_bitmapscan = off;
  return query execute query_sql;
end;
$$
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar2 LC_COLLATE 'foo''; SELECT ''1'
;
----------
[CREATE_SEQUENCE] CREATE TEMP SEQUENCE myseq2
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '.')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_sch CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION tap_pub_sch
;
----------
[CREATE_PROG_OBJ] /*
 * Injection point related functions
 */
CREATE FUNCTION inj_io_short_read_attach(result int)
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ctl_foreign_table2(LIKE ctl_table INCLUDING ALL) SERVER ctl_s0
;
----------
[ADD_INDEX] CREATE INDEX gin_pvactst ON pvactst USING gin (a)
;
----------
[CREATE_TABLE] CREATE TABLE ffk (a int, b int REFERENCES pk) PARTITION BY list (a)
;
----------
[ALTER_TYPE] alter domain con drop constraint if exists nonexistent
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."plt1" ATTACH PARTITION "public"."plt1_p1" FOR VALUES IN ('0000', '0003', '0004', '0010')
;
----------
[CREATE_TABLE] create unlogged table gist_tbl (b box)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_template
;
----------
[BLOCK] do $$
declare
 p_CurData refcursor;
 val int;
begin
 open p_CurData scroll for select 42;
 fetch p_CurData into val;
 raise notice 'val = %', val;
end; $$
;
----------
[CREATE_VIEW(SELECT)] -- set defaults on views: we need to create a view, add a rule
-- to allow insertions into it, and then alter the view to add
-- a default
create view def_view_test as select * from def_test
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- partitioned table child
ALTER TABLE regtest_table_part ALTER p SET DEFAULT 'abcd'
;
----------
[CREATE_TABLE] CREATE TABLE test_inet (
	i inet
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _pg_truetypmod(pg_attribute, pg_type) RETURNS int4
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT CASE WHEN $2.typtype = 'd' THEN $2.typtypmod ELSE $1.atttypmod END$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- Verify that you can't create a trigger with transition tables with
-- a column list.
--

create trigger my_table_col_update_trig
  after update of b on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[DROP_PROG_OBJ] drop function testoa(x1 int, x2 int, x3 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ffnp(int[]) returns int[] as
'select $1' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_int8(int8, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30578 RENAME TO reind_con_toast
;
----------
[RENAME_PROG_OBJ] ALTER FUNCTION regtest_func_1(text) RENAME TO regtest_func
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart0 add primary key (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_branches add primary key (bid) using index tablespace "regress_pgbench_tap_1_ts"
;
----------
[DROP_TABLE] drop table inh_nn_parent, inh_nn_child, inh_nn_child2
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX sj_temp_idx ON sj(a,b)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk
;
----------
[ALTER_CATALOG] ALTER DATABASE "contrib_regression" SET lc_monetary TO 'C'
;
----------
[CREATE_TABLE] create table tt14t (f1 text, f2 text, f3 text, f4 text)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_auth_members
;
----------
[CREATE_TABLE] create table t1 (id integer, xml_data xml)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION subpath(ltree,int4)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart22" (
    "a" integer
)
;
----------
[CREATE_TABLE] -- temp tables
CREATE TEMP TABLE stats_temp(i int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION connectby(text,text,text,text,int)
RETURNS setof record
AS '$libdir/tablefunc','connectby_text'
LANGUAGE C STABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_statistic_ext
;
----------
[ALTER_TABLE] ALTER TABLE regtest_table DISABLE TRIGGER regtest_test_trig
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW functestv3 AS SELECT * FROM functest3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."whoami"() RETURNS "text"
    LANGUAGE "sql"
    AS $$select 'public'::text$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gseg_decompress(internal)
RETURNS internal
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p2 add check (f2>0)
;
----------
[CREATE_TABLE] CREATE TABLE test_drop_constr_parent (c text CHECK (c IS NOT NULL))
;
----------
[CREATE_TABLE] CREATE TABLE clstrpart2 PARTITION OF clstrpart FOR VALUES FROM (10) TO (20)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION f_test(int)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30554 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] -- Try to build index on table that already contains data
CREATE UNIQUE INDEX covering_pkey on covering_index_heap (f1,f2) INCLUDE(f3)
;
