CREATE TABLE stxdinh1() INHERITS(stxdinh)
;
CREATE OPERATOR ~>~ (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = ~<=~,
    COMMUTATOR = ~<~,
    PROCEDURE  = citext_pattern_gt,
    RESTRICT   = scalargtsel,
    JOIN       = scalargtjoinsel
)
;
create index on abc (id)
;
CREATE TRIGGER "tg_room_au" AFTER UPDATE ON "public"."room" FOR EACH ROW EXECUTE FUNCTION "public"."tg_room_au"()
;
CREATE TABLE quuux_default1 PARTITION OF quuux_default (
	CONSTRAINT check_1 CHECK (a IS NOT NULL AND a = 1)
) FOR VALUES IN ('b')
;
CREATE TRIGGER sub2_tab1_2_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1_2
  FOR EACH ROW EXECUTE PROCEDURE sub2_trigger_activity_func()
;
DROP FUNCTION mood_to_json
;
ALTER FUNCTION gbt_time_distance(internal, time without time zone, smallint, oid, internal) PARALLEL SAFE
;
CREATE FUNCTION unwanted_grant_nofail(int) RETURNS int
	IMMUTABLE LANGUAGE plpgsql AS $$
BEGIN
	PERFORM unwanted_grant();
	RAISE WARNING 'owned';
	RETURN 1;
EXCEPTION WHEN OTHERS THEN
	RETURN 2;
END$$
;
CREATE FUNCTION ltree_isparent(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_amop
;
ANALYZE past_parted
;
-- ... but be sure that the immutability test is accurate
CREATE TABLE gtest2 (a int, b text GENERATED ALWAYS AS (a || ' sec') STORED)
;
do $$ declare a complex;
begin a.r[1] := 2; raise notice 'a = %', a; end$$
;
-- if a partition has a unique index without a constraint, does not attach
-- automatically; creates a new index instead.
create table idxpart (a int, b int) partition by range (a)
;
/*
 * text search configuration for nepali language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * nepali and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
DROP TABLE PKTABLE
;
--
-- Check behavior when subtype lacks a hash function
--

create type varbitrange as range (subtype = varbit)
;
-- failed (not owner)
ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg4
;
ALTER TABLE pg_toast.pg_toast_28017 RENAME TO reind_con_toast
;
ALTER TABLE fd_pt1 VALIDATE CONSTRAINT fd_pt1chk3
;
CREATE TABLE tab_rule_stats_2 (a int, b int, c int, d int)
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE DATABASE moveme_db TABLESPACE source_ts
;
CREATE INDEX "idxpart31_a_idx" ON "public"."idxpart31" USING "btree" ("a")
;
create or replace function myfunc(int) returns text as $$
begin
  set work_mem = '2MB';
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
ALTER TABLE harvest ADD COLUMN mangos int
;
create table inh_perm_child () inherits (inh_temp_parent)
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26049_2 (tid pg_catalog.tid)
;
create trigger tg_slotlink_a after insert or update or delete
    on HSlot for each row execute procedure tg_slotlink_a('HS')
;
CREATE TEMP TABLE quad_point_tbl_ord_idx2 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM quad_point_tbl WHERE p <@ box '(200,200,1000,1000)'
;
CREATE INDEX CONCURRENTLY on concur_heap((f2||f1))
;
create table inh_multiparent2 (a int not null, f1 int) inherits(inh_p3, inh_multiparent)
;
CREATE OR REPLACE FUNCTION test_slru_page_readonly(bigint) RETURNS text
  AS 'MODULE_PATHNAME', 'test_slru_page_readonly' LANGUAGE C
;
CREATE TEMP TABLE forcetest (
    a INT NOT NULL,
    b TEXT NOT NULL,
    c TEXT,
    d TEXT,
    e TEXT
)
;
ANALYZE public.num_exp_div
;
CREATE FUNCTION gbt_cash_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE TABLE selinto_schema.tbl_nodata4 (a) AS
  EXECUTE data_sel WITH NO DATA
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_amproc
;
CREATE PUBLICATION "Pub2" FOR ALL TABLES
;
ALTER TABLE list_parted2 ATTACH PARTITION part_7 FOR VALUES IN (7)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_conversion
;
CREATE TABLE test1 (a int8, b int4range)
;
CREATE TABLE local_tbl(a int, b int, c text)
;
ALTER TABLE pg_toast.pg_toast_28499 RENAME TO reind_con_toast
;
ALTER TABLE ONLY "public"."alpha_pos" ATTACH PARTITION "public"."alpha_pos_p3" FOR VALUES IN ('0008', '0009')
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_range
;
ALTER INDEX pg_toast.pg_toast_31108_index RENAME TO reind_con_toast_idx
;
CREATE TABLE part3 (key integer, val text)
;
do $$
declare x int not null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE stxdinh1() INHERITS(stxdinh)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~>~ (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = ~<=~,
    COMMUTATOR = ~<~,
    PROCEDURE  = citext_pattern_gt,
    RESTRICT   = scalargtsel,
    JOIN       = scalargtjoinsel
)
;
----------
[ADD_INDEX] create index on abc (id)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_room_au" AFTER UPDATE ON "public"."room" FOR EACH ROW EXECUTE FUNCTION "public"."tg_room_au"()
;
----------
[CREATE_TABLE] CREATE TABLE quuux_default1 PARTITION OF quuux_default (
	CONSTRAINT check_1 CHECK (a IS NOT NULL AND a = 1)
) FOR VALUES IN ('b')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER sub2_tab1_2_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1_2
  FOR EACH ROW EXECUTE PROCEDURE sub2_trigger_activity_func()
;
----------
[DROP_PROG_OBJ] DROP FUNCTION mood_to_json
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_time_distance(internal, time without time zone, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION unwanted_grant_nofail(int) RETURNS int
	IMMUTABLE LANGUAGE plpgsql AS $$
BEGIN
	PERFORM unwanted_grant();
	RAISE WARNING 'owned';
	RETURN 1;
EXCEPTION WHEN OTHERS THEN
	RETURN 2;
END$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_isparent(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_amop
;
----------
[ADMIN_TABLE] ANALYZE past_parted
;
----------
[CREATE_TABLE] -- ... but be sure that the immutability test is accurate
CREATE TABLE gtest2 (a int, b text GENERATED ALWAYS AS (a || ' sec') STORED)
;
----------
[BLOCK] do $$ declare a complex;
begin a.r[1] := 2; raise notice 'a = %', a; end$$
;
----------
[CREATE_TABLE] -- if a partition has a unique index without a constraint, does not attach
-- automatically; creates a new index instead.
create table idxpart (a int, b int) partition by range (a)
;
----------
[CREATE_POLICY] /*
 * text search configuration for nepali language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * nepali and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
----------
[DROP_TABLE] DROP TABLE PKTABLE
;
----------
[CREATE_TYPE] --
-- Check behavior when subtype lacks a hash function
--

create type varbitrange as range (subtype = varbit)
;
----------
[RENAME_PROG_OBJ] -- failed (not owner)
ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg4
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28017 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fd_pt1 VALIDATE CONSTRAINT fd_pt1chk3
;
----------
[CREATE_TABLE] CREATE TABLE tab_rule_stats_2 (a int, b int, c int, d int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_CATALOG] CREATE DATABASE moveme_db TABLESPACE source_ts
;
----------
[ADD_INDEX] CREATE INDEX "idxpart31_a_idx" ON "public"."idxpart31" USING "btree" ("a")
;
----------
[CREATE_PROG_OBJ] create or replace function myfunc(int) returns text as $$
begin
  set work_mem = '2MB';
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE harvest ADD COLUMN mangos int
;
----------
[CREATE_TABLE] create table inh_perm_child () inherits (inh_temp_parent)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26049_2 (tid pg_catalog.tid)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_slotlink_a after insert or update or delete
    on HSlot for each row execute procedure tg_slotlink_a('HS')
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE quad_point_tbl_ord_idx2 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM quad_point_tbl WHERE p <@ box '(200,200,1000,1000)'
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY on concur_heap((f2||f1))
;
----------
[CREATE_TABLE] create table inh_multiparent2 (a int not null, f1 int) inherits(inh_p3, inh_multiparent)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_readonly(bigint) RETURNS text
  AS 'MODULE_PATHNAME', 'test_slru_page_readonly' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TEMP TABLE forcetest (
    a INT NOT NULL,
    b TEXT NOT NULL,
    c TEXT,
    d TEXT,
    e TEXT
)
;
----------
[ADMIN_TABLE] ANALYZE public.num_exp_div
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_cash_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE|UNSAFE] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE TABLE selinto_schema.tbl_nodata4 (a) AS
  EXECUTE data_sel WITH NO DATA
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_amproc
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "Pub2" FOR ALL TABLES
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 ATTACH PARTITION part_7 FOR VALUES IN (7)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_conversion
;
----------
[CREATE_TABLE] CREATE TABLE test1 (a int8, b int4range)
;
----------
[CREATE_TABLE] CREATE TABLE local_tbl(a int, b int, c text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28499 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."alpha_pos" ATTACH PARTITION "public"."alpha_pos_p3" FOR VALUES IN ('0008', '0009')
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_range
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_31108_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE part3 (key integer, val text)
;
----------
[BLOCK] do $$
declare x int not null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
