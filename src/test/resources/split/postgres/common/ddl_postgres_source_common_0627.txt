CREATE SCHEMA generated_virtual_tests
;
create table ab_a1_b1 partition of ab_a1 for values in (1)
;
-- Sequences should get wiped out as well:
DROP TABLE serialTest1, serialTest2
;
DROP TABLE TEMP_INT4
;
COMMENT ON DATABASE split_db_probe IS NULL
;
CREATE INDEX t2_btree ON s1.t2 USING BTREE (i)
;
-- test inlining when function returns composite

create function mki8(bigint, bigint) returns int8_tbl as
$$select row($1,$2)::int8_tbl$$ language sql
;
VACUUM (ANALYZE) pg_catalog.pg_inherits
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub4
;
-- test jsonb[] -> python list
-- array of dicts
CREATE FUNCTION test3arr(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, list)
assert(val == [{"a": 1,"b": 2}, {"c": 3,"d": 4}])
return len(val)
$$
;
CREATE TABLE "public"."pht1_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE FUNCTION set_ttdummy (int4)
        RETURNS int4
        AS '/src/src/test/regress/regress.so'
        LANGUAGE C STRICT
;
CREATE SCHEMA foo
;
-- failure: changing replica identity to FULL for partition fails, because
-- of the column list on the parent
ALTER TABLE testpub_tbl8_0 REPLICA IDENTITY FULL
;
CREATE FUNCTION "public"."tg_iface_bu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotname != old.slotname then
        delete from IFace where slotname = old.slotname;
	insert into IFace (
		    slotname,
		    sysname,
		    ifname,
		    slotlink
		) values (
		    new.slotname,
		    new.sysname,
		    new.ifname,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
$$
;
drop sequence seq
;
create type mycomptype as (id int, v bigint)
;
alter type comptype alter attribute r type varchar
;
CREATE TABLE prt2_m_p1 PARTITION OF prt2_m FOR VALUES FROM (0, 0) TO (250, 250)
;
ANALYZE public.e_star
;
do $$ declare x foo.bar.baz%type; begin end $$
;
DROP TABLE brintest_3
;
-- Check with zero-column foreign table; batch insert will be disabled
alter table loc2 drop column f1
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
CREATE TABLE plt2_adv_p1 PARTITION OF plt2_adv FOR VALUES IN ('0001', '0002')
;
DROP TABLE clstr_4
;
CREATE TABLE notnull_tbl1_chld () INHERITS (notnull_tbl1)
;
-- Create the operator class
CREATE OPERATOR CLASS gist_vbit_ops
DEFAULT FOR TYPE varbit USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bit_consistent (internal, bit, int2, oid, internal),
	FUNCTION	2	gbt_bit_union (internal, internal),
	FUNCTION	3	gbt_bit_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_bit_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bit_picksplit (internal, internal),
	FUNCTION	7	gbt_bit_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
-- a simple function to make arrays opaque to the optimizer
create function opaque_array(int[]) returns int[]
language plpgsql as $$begin return $1; end$$ strict
;
DO $$
DECLARE _a int; _b int; _c text[];
BEGIN
  _a := 10;
  CALL test_proc12(_a, _b, _c);  -- error
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
CREATE TABLE public.ths_p3 (
    mod integer,
    data integer
)
;
-- Same number of attributes, different types
CREATE TABLE pgss_schema_1.tab_search_diff_2 (a int)
;
CREATE TABLE fk_partitioned_fk_full (x int, y int) PARTITION BY RANGE (x)
;
create extension test_ext9
;
CREATE TABLE "public"."bar1" (
    "a" integer,
    "b" integer DEFAULT 1 NOT NULL
)
PARTITION BY RANGE ("a")
;
create trigger before_stmt_trigger
	before update on stmt_trig_on_empty_upd1
	execute procedure update_stmt_notice()
;
ALTER TABLE pg_toast.pg_toast_28099 RENAME TO reind_con_toast
;
create or replace function rls_f() returns setof rls_t
  stable language sql
  as $$ select * from (select array_agg(c) as cs from rls_t) _ group by cs $$
;
ALTER TEXT SEARCH CONFIGURATION armenian ADD MAPPING
    FOR word, hword_part, hword
	WITH armenian_stem
;
ALTER TABLE quuux ATTACH PARTITION quuux1 FOR VALUES IN (1)
;
CREATE FUNCTION gin_extract_query_uuid(uuid, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
-- If a table without index is attached as partition to a table with
-- an index, the index is automatically created
create table idxpart (a int, b int, c text) partition by range (a)
;
-- disallow OLD/NEW reference in CTE
CREATE TEMPORARY TABLE x (n integer)
;
ANALYZE pg_catalog.pg_class
;
ALTER TABLE tab_full_pk REPLICA IDENTITY FULL
;
CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=20571 host=/tmp/o2wey5DTaB dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
/*
 * 1.2. test CREATE INDEX with inserts
 */

CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
/*
 * 6.52
 * ROUTINE_ROUTINE_USAGE view
 */

CREATE VIEW routine_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np1.nspname AS sql_identifier) AS routine_schema,
           CAST(nameconcatoid(p1.proname, p1.oid) AS sql_identifier) AS routine_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_proc p1, pg_namespace np1

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = p1.oid
          AND d.refclassid = 'pg_catalog.pg_proc'::regclass
          AND p1.pronamespace = np1.oid
          AND p.prokind IN ('f', 'p') AND p1.prokind IN ('f', 'p')
          AND pg_has_role(p1.proowner, 'USAGE')
;
-- data-modifying WITH containing INSERT...ON CONFLICT DO UPDATE
CREATE TABLE withz AS SELECT i AS k, (i || ' v')::text v FROM generate_series(1, 16, 3) i
;
CREATE TABLE batch_table (a text, b int)
;
------------------------------------------------------------------------------------------
[CREATE_SCHEMA] CREATE SCHEMA generated_virtual_tests
;
----------
[CREATE_TABLE] create table ab_a1_b1 partition of ab_a1 for values in (1)
;
----------
[DROP_TABLE] -- Sequences should get wiped out as well:
DROP TABLE serialTest1, serialTest2
;
----------
[DROP_TABLE] DROP TABLE TEMP_INT4
;
----------
[COMMENT_CATALOG] COMMENT ON DATABASE split_db_probe IS NULL
;
----------
[ADD_INDEX] CREATE INDEX t2_btree ON s1.t2 USING BTREE (i)
;
----------
[CREATE_PROG_OBJ] -- test inlining when function returns composite

create function mki8(bigint, bigint) returns int8_tbl as
$$select row($1,$2)::int8_tbl$$ language sql
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_inherits
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub4
;
----------
[CREATE_PROG_OBJ] -- test jsonb[] -> python list
-- array of dicts
CREATE FUNCTION test3arr(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, list)
assert(val == [{"a": 1,"b": 2}, {"c": 3,"d": 4}])
return len(val)
$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht1_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION set_ttdummy (int4)
        RETURNS int4
        AS '/src/src/test/regress/regress.so'
        LANGUAGE C STRICT
;
----------
[CREATE_SCHEMA] CREATE SCHEMA foo
;
----------
[ALTER_TABLE] -- failure: changing replica identity to FULL for partition fails, because
-- of the column list on the parent
ALTER TABLE testpub_tbl8_0 REPLICA IDENTITY FULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_iface_bu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotname != old.slotname then
        delete from IFace where slotname = old.slotname;
	insert into IFace (
		    slotname,
		    sysname,
		    ifname,
		    slotlink
		) values (
		    new.slotname,
		    new.sysname,
		    new.ifname,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
$$
;
----------
[DROP_SEQUENCE] drop sequence seq
;
----------
[CREATE_TYPE] create type mycomptype as (id int, v bigint)
;
----------
[ALTER_TYPE] alter type comptype alter attribute r type varchar
;
----------
[CREATE_TABLE] CREATE TABLE prt2_m_p1 PARTITION OF prt2_m FOR VALUES FROM (0, 0) TO (250, 250)
;
----------
[ADMIN_TABLE] ANALYZE public.e_star
;
----------
[BLOCK] do $$ declare x foo.bar.baz%type; begin end $$
;
----------
[DROP_TABLE] DROP TABLE brintest_3
;
----------
[ALTER_TABLE|DROP_COLUMN] -- Check with zero-column foreign table; batch insert will be disabled
alter table loc2 drop column f1
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p1 PARTITION OF plt2_adv FOR VALUES IN ('0001', '0002')
;
----------
[DROP_TABLE] DROP TABLE clstr_4
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1_chld () INHERITS (notnull_tbl1)
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_vbit_ops
DEFAULT FOR TYPE varbit USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bit_consistent (internal, bit, int2, oid, internal),
	FUNCTION	2	gbt_bit_union (internal, internal),
	FUNCTION	3	gbt_bit_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_bit_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bit_picksplit (internal, internal),
	FUNCTION	7	gbt_bit_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
----------
[CREATE_PROG_OBJ] -- a simple function to make arrays opaque to the optimizer
create function opaque_array(int[]) returns int[]
language plpgsql as $$begin return $1; end$$ strict
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int; _c text[];
BEGIN
  _a := 10;
  CALL test_proc12(_a, _b, _c);  -- error
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE public.ths_p3 (
    mod integer,
    data integer
)
;
----------
[CREATE_TABLE] -- Same number of attributes, different types
CREATE TABLE pgss_schema_1.tab_search_diff_2 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_full (x int, y int) PARTITION BY RANGE (x)
;
----------
[CREATE_LIBRARY] create extension test_ext9
;
----------
[CREATE_TABLE] CREATE TABLE "public"."bar1" (
    "a" integer,
    "b" integer DEFAULT 1 NOT NULL
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger before_stmt_trigger
	before update on stmt_trig_on_empty_upd1
	execute procedure update_stmt_notice()
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28099 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] create or replace function rls_f() returns setof rls_t
  stable language sql
  as $$ select * from (select array_agg(c) as cs from rls_t) _ group by cs $$
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION armenian ADD MAPPING
    FOR word, hword_part, hword
	WITH armenian_stem
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE quuux ATTACH PARTITION quuux1 FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_uuid(uuid, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] -- If a table without index is attached as partition to a table with
-- an index, the index is automatically created
create table idxpart (a int, b int, c text) partition by range (a)
;
----------
[CREATE_TABLE] -- disallow OLD/NEW reference in CTE
CREATE TEMPORARY TABLE x (n integer)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_class
;
----------
[ALTER_TABLE] ALTER TABLE tab_full_pk REPLICA IDENTITY FULL
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=20571 host=/tmp/o2wey5DTaB dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[CREATE_TABLE] /*
 * 1.2. test CREATE INDEX with inserts
 */

CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.52
 * ROUTINE_ROUTINE_USAGE view
 */

CREATE VIEW routine_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np1.nspname AS sql_identifier) AS routine_schema,
           CAST(nameconcatoid(p1.proname, p1.oid) AS sql_identifier) AS routine_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_proc p1, pg_namespace np1

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = p1.oid
          AND d.refclassid = 'pg_catalog.pg_proc'::regclass
          AND p1.pronamespace = np1.oid
          AND p.prokind IN ('f', 'p') AND p1.prokind IN ('f', 'p')
          AND pg_has_role(p1.proowner, 'USAGE')
;
----------
[CREATE_TABLE|SELECT] -- data-modifying WITH containing INSERT...ON CONFLICT DO UPDATE
CREATE TABLE withz AS SELECT i AS k, (i || ' v')::text v FROM generate_series(1, 16, 3) i
;
----------
[CREATE_TABLE] CREATE TABLE batch_table (a text, b int)
;
