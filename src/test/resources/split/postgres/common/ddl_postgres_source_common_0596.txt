CREATE TABLE tab_part2_1(a int)
;
create temp table idxpart1_temp partition of idxpart_temp
  for values from (0) to (10)
;
CREATE TABLE tt()
;
CREATE FUNCTION gbt_bool_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE test3cs (x text COLLATE case_sensitive)
;
create type textrange1 as range(subtype=text, multirange_type_name=multitextrange1, collation="C")
;
CREATE TABLE test_missing_target3 AS
SELECT count(x.b)
FROM test_missing_target x, test_missing_target y
	WHERE x.a = y.a
	GROUP BY x.b/2 ORDER BY x.b/2
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest10" DROP COLUMN "........pg.dropped.3........"
;
-- xid8 has btree and hash opclasses
create table xid8_t1 (x xid8)
;
CREATE TABLE "public"."spgist_domain_tbl" (
    "f1" "public"."spgist_text"
)
;
CREATE FUNCTION crosstab_named(text, text)
RETURNS setof my_crosstab_result
AS '$libdir/tablefunc','crosstab_hash'
LANGUAGE C STABLE STRICT
;
CREATE FUNCTION "public"."event_trigger_report_dropped"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE r record;
BEGIN
    FOR r IN SELECT * from pg_event_trigger_dropped_objects()
    LOOP
    IF NOT r.normal AND NOT r.original THEN
        CONTINUE;
    END IF;
    RAISE NOTICE 'NORMAL: orig=% normal=% istemp=% type=% identity=% name=% args=%',
        r.original, r.normal, r.is_temporary, r.object_type,
        r.object_identity, r.address_names, r.address_args;
    END LOOP;
END; $$
;
-- OK
ALTER OPERATOR @-@(int4, int4) SET SCHEMA alt_nsp2
;
DROP VIEW regtest_view
;
drop rule rrule on vview
;
CREATE FUNCTION inline_eq(foodomain, foodomain) returns boolean as
  'SELECT CASE $2::text WHEN $1::text THEN true ELSE false END' language sql
;
CREATE INDEX test_hash_a_btree ON test_hash USING btree (a)
;
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_expr
;
CREATE TABLE BOOLTBL3 (d text, b bool, o int)
;
CREATE TABLE "public"."onek" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
DROP SCHEMA schema_to_reindex CASCADE
;
create table pk51 partition of pk5 for values from (4000) to (4500)
;
CREATE TABLE "public"."brin_test_multi" (
    "a" integer,
    "b" integer
)
;
ANALYZE stud_emp
;
-- unrelated change, no effect
ALTER FUNCTION functest_C_2(int) SECURITY INVOKER
;
ALTER TABLE plt2_adv DETACH PARTITION plt2_adv_p2_ext
;
CREATE TABLE macaddr_data (a int, b macaddr)
;
CREATE TABLE ptif_test11 PARTITION OF ptif_test1 FOR VALUES IN (1)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=15749 host=/tmp/tCppWZnQQK dbname=postgres' PUBLICATION tap_pub
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_init_privs
;
ALTER TABLE gtest31_1 ALTER COLUMN b TYPE varchar
;
create table cho partition of middle for values from (6) to (10)
;
alter table parent disable trigger tg
;
CREATE TABLE bank_account (id TEXT PRIMARY KEY, balance DECIMAL NOT NULL)
;
create temp table nocolumns()
;
-- should fail, sandwich is not a valid command tag
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('sandwich')
   execute procedure test_event_trigger()
;
DROP SCHEMA pub_testpart1 CASCADE
;
DROP TABLE x, y
;
create index on idxpart ((a + b))
;
ALTER INDEX pg_toast.pg_toast_30758_index RENAME TO reind_con_toast_idx
;
drop table table1
;
CREATE FUNCTION overpaid(emp)
   RETURNS bool
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT
;
DROP SCHEMA split_merge18 CASCADE
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_foreign_server
;
CREATE INDEX "comment_test_child_fk" ON "public"."comment_test_child" USING "btree" ("id")
;
CREATE FUNCTION test1(val ltree) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE ltree
AS $$
plpy.info(repr(val))
return len(val)
$$
;
--
-- cleanup
--
DROP EXTENSION isn
;
ALTER TABLE public.atacc1 ADD CONSTRAINT atacc_test1 UNIQUE (test)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_class
;
--
-- FuncExpr
--

-- Verify multiple type representation end up with the same query_id
CREATE TABLE test_float (data float)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE tab_part2_1(a int)
;
----------
[CREATE_TABLE] create temp table idxpart1_temp partition of idxpart_temp
  for values from (0) to (10)
;
----------
[CREATE_TABLE] CREATE TABLE tt()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bool_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE test3cs (x text COLLATE case_sensitive)
;
----------
[CREATE_POLICY] create type textrange1 as range(subtype=text, multirange_type_name=multitextrange1, collation="C")
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_missing_target3 AS
SELECT count(x.b)
FROM test_missing_target x, test_missing_target y
	WHERE x.a = y.a
	GROUP BY x.b/2 ORDER BY x.b/2
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_virtual_tests"."gtest10" DROP COLUMN "........pg.dropped.3........"
;
----------
[CREATE_TABLE] -- xid8 has btree and hash opclasses
create table xid8_t1 (x xid8)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."spgist_domain_tbl" (
    "f1" "public"."spgist_text"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION crosstab_named(text, text)
RETURNS setof my_crosstab_result
AS '$libdir/tablefunc','crosstab_hash'
LANGUAGE C STABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."event_trigger_report_dropped"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE r record;
BEGIN
    FOR r IN SELECT * from pg_event_trigger_dropped_objects()
    LOOP
    IF NOT r.normal AND NOT r.original THEN
        CONTINUE;
    END IF;
    RAISE NOTICE 'NORMAL: orig=% normal=% istemp=% type=% identity=% name=% args=%',
        r.original, r.normal, r.is_temporary, r.object_type,
        r.object_identity, r.address_names, r.address_args;
    END LOOP;
END; $$
;
----------
[ALTER_PROG_OBJ] -- OK
ALTER OPERATOR @-@(int4, int4) SET SCHEMA alt_nsp2
;
----------
[DROP_VIEW] DROP VIEW regtest_view
;
----------
[DROP_POLICY] drop rule rrule on vview
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION inline_eq(foodomain, foodomain) returns boolean as
  'SELECT CASE $2::text WHEN $1::text THEN true ELSE false END' language sql
;
----------
[ADD_INDEX] CREATE INDEX test_hash_a_btree ON test_hash USING btree (a)
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_expr
;
----------
[CREATE_TABLE] CREATE TABLE BOOLTBL3 (d text, b bool, o int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."onek" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
----------
[DROP_SCHEMA] DROP SCHEMA schema_to_reindex CASCADE
;
----------
[CREATE_TABLE] create table pk51 partition of pk5 for values from (4000) to (4500)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brin_test_multi" (
    "a" integer,
    "b" integer
)
;
----------
[ADMIN_TABLE] ANALYZE stud_emp
;
----------
[ALTER_PROG_OBJ] -- unrelated change, no effect
ALTER FUNCTION functest_C_2(int) SECURITY INVOKER
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt2_adv DETACH PARTITION plt2_adv_p2_ext
;
----------
[CREATE_TABLE] CREATE TABLE macaddr_data (a int, b macaddr)
;
----------
[CREATE_TABLE] CREATE TABLE ptif_test11 PARTITION OF ptif_test1 FOR VALUES IN (1)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=15749 host=/tmp/tCppWZnQQK dbname=postgres' PUBLICATION tap_pub
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_init_privs
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest31_1 ALTER COLUMN b TYPE varchar
;
----------
[CREATE_TABLE] create table cho partition of middle for values from (6) to (10)
;
----------
[ALTER_TABLE] alter table parent disable trigger tg
;
----------
[CREATE_TABLE] CREATE TABLE bank_account (id TEXT PRIMARY KEY, balance DECIMAL NOT NULL)
;
----------
[CREATE_TABLE] create temp table nocolumns()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should fail, sandwich is not a valid command tag
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('sandwich')
   execute procedure test_event_trigger()
;
----------
[DROP_SCHEMA] DROP SCHEMA pub_testpart1 CASCADE
;
----------
[DROP_TABLE] DROP TABLE x, y
;
----------
[ADD_INDEX] create index on idxpart ((a + b))
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30758_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] drop table table1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION overpaid(emp)
   RETURNS bool
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT
;
----------
[DROP_SCHEMA] DROP SCHEMA split_merge18 CASCADE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_foreign_server
;
----------
[ADD_INDEX] CREATE INDEX "comment_test_child_fk" ON "public"."comment_test_child" USING "btree" ("id")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1(val ltree) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE ltree
AS $$
plpy.info(repr(val))
return len(val)
$$
;
----------
[DROP_LIBRARY] --
-- cleanup
--
DROP EXTENSION isn
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.atacc1 ADD CONSTRAINT atacc_test1 UNIQUE (test)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_class
;
----------
[CREATE_TABLE] --
-- FuncExpr
--

-- Verify multiple type representation end up with the same query_id
CREATE TABLE test_float (data float)
;
