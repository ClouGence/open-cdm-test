ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p3" FOR VALUES FROM (22) TO (30)
;
alter table mlparted add constraint check_b check (b = 3)
;
DROP EXTENSION amcheck
;
DO $$ -- use DO to protect -- from psql
  declare r boolean;
  begin
    execute $e$ select 2 !=-- comment
      1 $e$ into r;
    raise info 'r = %', r;
  end;
$$
;
/* End test case for bug #16242 */

/* Test case for bug #18970 */

create table attbl(a int)
;
vacuum ͪߩѦ���
;
ALTER INDEX pg_toast.pg_toast_33383_index RENAME TO reind_con_toast_idx
;
--
-- UTF-8
--
-- The description column must be unique.
CREATE TABLE utf8_verification_inputs (inbytes bytea, description text PRIMARY KEY)
;
CREATE TABLE test31 (a int, b char(3) COLLATE case_insensitive) PARTITION BY RANGE (b)
;
ANALYZE pg_temp_4.pg_temp_27609
;
CREATE TABLE s3.t2 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
CREATE TABLE PKTABLE (tid int, id int, PRIMARY KEY (tid, id))
;
do $$
      declare x int = 0;
      begin
        x := 1 / x;
      end;
      $$
;
CREATE TABLE ctlt1 (LIKE ctlt1 INCLUDING ALL)
;
/* Now redefine */
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT shared_blk_read_time float8,
    OUT shared_blk_write_time float8,
    OUT local_blk_read_time float8,
    OUT local_blk_write_time float8,
    OUT temp_blk_read_time float8,
    OUT temp_blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric,
    OUT jit_functions int8,
    OUT jit_generation_time float8,
    OUT jit_inlining_count int8,
    OUT jit_inlining_time float8,
    OUT jit_optimization_count int8,
    OUT jit_optimization_time float8,
    OUT jit_emission_count int8,
    OUT jit_emission_time float8,
    OUT jit_deform_count int8,
    OUT jit_deform_time float8,
    OUT stats_since timestamp with time zone,
    OUT minmax_stats_since timestamp with time zone
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_11'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
VACUUM ANALYZE tenk2
;
CREATE TABLESPACE other LOCATION '/src/src/test/recovery/tmp_check/t_018_wal_optimize_node_replica_data/tablespace_other'
;
CREATE EXTENSION btree_gist
;
CREATE UNLOGGED TABLE xy (x integer, y integer)
;
ALTER TABLE pg_temp_52.pg_temp_29691_2 ADD COLUMN newdata pg_temp_52.pg_temp_29691
;
ALTER TABLE list_parted2 ATTACH PARTITION part_3 FOR VALUES IN (11)
;
CREATE FUNCTION gbt_uuid_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
--
--
-- bit ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_bit_consistent(internal,bit,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
DROP RULE rules_stats ON tab_rule_stats
;
create table part_null partition of list_parted FOR VALUES IN (null)
;
create table s5.p2_1 partition of s5.p2 for values in (1, 2, 3)
;
CREATE FUNCTION isngt(ean13, ismn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
/*
 * text search configuration for irish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * irish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
ALTER TABLE r2 NO FORCE ROW LEVEL SECURITY
;
ALTER TABLE r1 FORCE ROW LEVEL SECURITY
;
-- ok - we don't have row filter for partitioned table.
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=0)
;
-- check that the table being attached is not a typed table
CREATE TYPE mytype AS (a int)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_conversion
;
CREATE PROCEDURE ptest4c(INOUT comp int8_tbl)
LANGUAGE SQL
AS $$
SELECT ROW(1, 2);
$$
;
create type two_texts as (f1 text, f2 text)
;
-- Another reverse-listing example
CREATE VIEW sums_1_100 AS
WITH RECURSIVE t(n) AS (
    VALUES (1)
UNION ALL
    SELECT n+1 FROM t WHERE n < 100
)
SELECT sum(n) FROM t
;
-- test handling of infinite date values
CREATE TABLE brin_date_test(a DATE)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=19422 host=/tmp/9iBCfaPpy5 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_1, tap_pub_2, tap_pub_3, tap_pub_4a, tap_pub_4b, tap_pub_5a, tap_pub_5b, tap_pub_toast, tap_pub_inherits, tap_pub_viaroot_2, tap_pub_viaroot_1
;
CREATE FUNCTION gbt_time_distance(internal,time,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE pg_temp_32.pg_temp_28302_2 ADD COLUMN newdata pg_temp_32.pg_temp_28302
;
VACUUM brintest_multi
;
CREATE POLICY p1 ON t1 TO regress_rls_bob USING ((a % 2) = 0)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=32633 host=/tmp/2uCjl_qJmA dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
--ok
CREATE TABLE notnull_part1_1_upg PARTITION OF notnull_part1_upg FOR VALUES IN (1,2)
;
create index tprt6_idx on tprt_6 (col1)
;
CREATE TYPE collate_dep_test2 AS (x int, y text COLLATE test0)
;
ALTER TABLE parent DISABLE ROW LEVEL SECURITY
;
ALTER TABLE tt1 RENAME TO a2
;
CREATE TYPE "public"."sqljsonb_reca" AS (
	"reca" "public"."sqljsonb_rec"[]
)
;
ALTER TABLE pg_toast.pg_toast_30533 RENAME TO reind_con_toast
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p3" FOR VALUES FROM (22) TO (30)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table mlparted add constraint check_b check (b = 3)
;
----------
[DROP_LIBRARY] DROP EXTENSION amcheck
;
----------
[BLOCK] DO $$ -- use DO to protect -- from psql
  declare r boolean;
  begin
    execute $e$ select 2 !=-- comment
      1 $e$ into r;
    raise info 'r = %', r;
  end;
$$
;
----------
[CREATE_TABLE] /* End test case for bug #16242 */

/* Test case for bug #18970 */

create table attbl(a int)
;
----------
[ADMIN_TABLE] vacuum ͪߩѦ���
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33383_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] --
-- UTF-8
--
-- The description column must be unique.
CREATE TABLE utf8_verification_inputs (inbytes bytea, description text PRIMARY KEY)
;
----------
[CREATE_TABLE] CREATE TABLE test31 (a int, b char(3) COLLATE case_insensitive) PARTITION BY RANGE (b)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_27609
;
----------
[CREATE_TABLE] CREATE TABLE s3.t2 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (tid int, id int, PRIMARY KEY (tid, id))
;
----------
[BLOCK] do $$
      declare x int = 0;
      begin
        x := 1 / x;
      end;
      $$
;
----------
[CREATE_TABLE] CREATE TABLE ctlt1 (LIKE ctlt1 INCLUDING ALL)
;
----------
[CREATE_PROG_OBJ] /* Now redefine */
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT shared_blk_read_time float8,
    OUT shared_blk_write_time float8,
    OUT local_blk_read_time float8,
    OUT local_blk_write_time float8,
    OUT temp_blk_read_time float8,
    OUT temp_blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric,
    OUT jit_functions int8,
    OUT jit_generation_time float8,
    OUT jit_inlining_count int8,
    OUT jit_inlining_time float8,
    OUT jit_optimization_count int8,
    OUT jit_optimization_time float8,
    OUT jit_emission_count int8,
    OUT jit_emission_time float8,
    OUT jit_deform_count int8,
    OUT jit_deform_time float8,
    OUT stats_since timestamp with time zone,
    OUT minmax_stats_since timestamp with time zone
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_11'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM ANALYZE tenk2
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE other LOCATION '/src/src/test/recovery/tmp_check/t_018_wal_optimize_node_replica_data/tablespace_other'
;
----------
[CREATE_LIBRARY] CREATE EXTENSION btree_gist
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE xy (x integer, y integer)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_52.pg_temp_29691_2 ADD COLUMN newdata pg_temp_52.pg_temp_29691
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 ATTACH PARTITION part_3 FOR VALUES IN (11)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_uuid_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] --
--
-- bit ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_bit_consistent(internal,bit,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_POLICY] DROP RULE rules_stats ON tab_rule_stats
;
----------
[CREATE_TABLE] create table part_null partition of list_parted FOR VALUES IN (null)
;
----------
[CREATE_TABLE] create table s5.p2_1 partition of s5.p2 for values in (1, 2, 3)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ean13, ismn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_POLICY] /*
 * text search configuration for irish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * irish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
----------
[ALTER_TABLE] ALTER TABLE r2 NO FORCE ROW LEVEL SECURITY
;
----------
[ALTER_TABLE] ALTER TABLE r1 FORCE ROW LEVEL SECURITY
;
----------
[ALTER_PUB_SUB] -- ok - we don't have row filter for partitioned table.
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=0)
;
----------
[CREATE_TYPE] -- check that the table being attached is not a typed table
CREATE TYPE mytype AS (a int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_conversion
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest4c(INOUT comp int8_tbl)
LANGUAGE SQL
AS $$
SELECT ROW(1, 2);
$$
;
----------
[CREATE_TYPE] create type two_texts as (f1 text, f2 text)
;
----------
[CREATE_VIEW(SELECT)] -- Another reverse-listing example
CREATE VIEW sums_1_100 AS
WITH RECURSIVE t(n) AS (
    VALUES (1)
UNION ALL
    SELECT n+1 FROM t WHERE n < 100
)
SELECT sum(n) FROM t
;
----------
[CREATE_TABLE] -- test handling of infinite date values
CREATE TABLE brin_date_test(a DATE)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=19422 host=/tmp/9iBCfaPpy5 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_1, tap_pub_2, tap_pub_3, tap_pub_4a, tap_pub_4b, tap_pub_5a, tap_pub_5b, tap_pub_toast, tap_pub_inherits, tap_pub_viaroot_2, tap_pub_viaroot_1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_distance(internal,time,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_32.pg_temp_28302_2 ADD COLUMN newdata pg_temp_32.pg_temp_28302
;
----------
[ADMIN_TABLE] VACUUM brintest_multi
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON t1 TO regress_rls_bob USING ((a % 2) = 0)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=32633 host=/tmp/2uCjl_qJmA dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
----------
[CREATE_TABLE] --ok
CREATE TABLE notnull_part1_1_upg PARTITION OF notnull_part1_upg FOR VALUES IN (1,2)
;
----------
[ADD_INDEX] create index tprt6_idx on tprt_6 (col1)
;
----------
[CREATE_TYPE] CREATE TYPE collate_dep_test2 AS (x int, y text COLLATE test0)
;
----------
[ALTER_TABLE] ALTER TABLE parent DISABLE ROW LEVEL SECURITY
;
----------
[RENAME_TABLE] ALTER TABLE tt1 RENAME TO a2
;
----------
[CREATE_TYPE] CREATE TYPE "public"."sqljsonb_reca" AS (
	"reca" "public"."sqljsonb_rec"[]
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30533 RENAME TO reind_con_toast
;
