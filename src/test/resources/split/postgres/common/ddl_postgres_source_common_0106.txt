ALTER TABLE pg_temp_3.pg_temp_27281_2 ADD COLUMN newdata pg_temp_3.pg_temp_27281
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_tablespace
;
CREATE TABLE test23_0 PARTITION OF test23 FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
-- constraints with different enforceability can be merged by marking them as ENFORCED
create table p1_c3() inherits(p1, p1_c1)
;
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean, rootdescend boolean, checkunique boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub5
;
alter table idxpart0 alter column a set not null
;
create table list_part (a int) partition by list (a)
;
ALTER TABLE itest7d ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
CREATE TABLE amcheck_schema.pg_database (junk text)
;
DROP TABLE texttest, byteatest
;
create function conflict_test() returns setof int8_tbl as $$
declare r record;
  q1 bigint := 42;
begin
  for r in select q1,q2 from int8_tbl loop
    return next r;
  end loop;
end;
$$ language plpgsql
;
CREATE SUBSCRIPTION tap_sub_A_B_2
        CONNECTION 'port=29891 host=/tmp/7Wj1Y48wnU dbname=postgres application_name=tap_sub_A_B_2'
        PUBLICATION tap_pub_B
        WITH (origin = none, copy_data = on)
;
CREATE PROCEDURE "public"."ptest6c"(INOUT "a" "anyelement", INOUT "b" "anyelement")
    LANGUAGE "sql"
    AS $_$
SELECT $1, 1;
$_$
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn13,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
alter table anothertab
  add exclude using btree (f4 with =) where (f4 is not null)
;
create or replace function insert_tt2(text,text) returns setof int as
$$ insert into tt(data) values($1),($2) returning f1 $$
language sql
;
CREATE INDEX test_replica_id_full_idx ON test_replica_id_full USING HASH (x)
;
CLUSTER clstr_1
;
ALTER DATABASE "ecpg2_regression" SET bytea_output TO 'hex'
;
DROP TABLE use_ft1_column_type
;
alter table utrtest attach partition utr1 for values in (1)
;
CREATE AGGREGATE myaggn16a(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE VIEW role_column_grants AS
    SELECT grantor,
           grantee,
           table_catalog,
           table_schema,
           table_name,
           column_name,
           privilege_type,
           is_grantable
    FROM column_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
DROP FUNCTION priv_testfunc4(boolean)
;
CREATE TABLE split_lock_next.child (note text) INHERITS (split_lock_next.parent)
;
create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = 'division_by_zero', detail = 'some detail info';
  exception
    when others then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
ALTER TABLE pg_toast.pg_toast_27691 RENAME TO reind_con_toast
;
ALTER TABLE attmp ADD COLUMN g polygon
;
DROP PROCEDURE cp_testfunc1(int)
;
CREATE TABLE hash_parted (
	a int,
	b int
) PARTITION BY HASH (a part_test_int4_ops)
;
CREATE OR REPLACE FUNCTION test_slru_page_read(int, bool DEFAULT true) RETURNS text
  AS 'MODULE_PATHNAME', 'test_slru_page_read' LANGUAGE C
;
drop table notnull_tbl2, notnull_tbl3, notnull_tbl4, notnull_tbl5, notnull_tbl6
;
COMMENT ON TABLESPACE split_ts_space IS 'split tablespace comment'
;
ALTER TABLE testschema.test_tab ALTER b TYPE bigint, ADD UNIQUE (c)
;
ALTER TABLE ONLY "public"."pht1" ATTACH PARTITION "public"."pht1_p1" FOR VALUES WITH (modulus 3, remainder 0)
;
DROP TRIGGER footrg_mod_a ON footrg1
;
CREATE SCHEMA "S 1"
;
CREATE TABLE bit_defaults(
  b1 bit(4) DEFAULT '1001',
  b2 bit(4) DEFAULT B'0101',
  b3 bit varying(5) DEFAULT '1001',
  b4 bit varying(5) DEFAULT B'0101'
)
;
ALTER TABLE pg_toast.pg_toast_30796 RENAME TO reind_con_toast
;
DROP SUBSCRIPTION dummy_sub
;
VACUUM pg_catalog.pg_ts_config
;
ALTER TABLE tenk1 set (parallel_workers = 4)
;
CREATE SEQUENCE "public"."sequence_test6"
    AS smallint
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
/* contrib/fuzzystrmatch/fuzzystrmatch--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION daitch_mokotoff(text) RETURNS text[]
AS '$libdir/fuzzystrmatch', 'daitch_mokotoff'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create or replace function outer_outer_func(int)
returns int as $$
declare
  myresult int;
begin
  raise notice 'calling down into outer_func()';
  myresult := outer_func($1);
  raise notice 'outer_func() done';
  return myresult;
end;
$$ language plpgsql
;
CREATE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'DECLARE c CURSOR WITH HOLD FOR SELECT public.unwanted_grant(); SELECT true'
;
do $$
declare x constant var_record;
begin
  x.f1 := 42;  -- fail
end$$
;
drop view usersview
;
CREATE TABLE tt1 (f1 int, f2 int, f3 text)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_27281_2 ADD COLUMN newdata pg_temp_3.pg_temp_27281
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_tablespace
;
----------
[CREATE_TABLE] CREATE TABLE test23_0 PARTITION OF test23 FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[CREATE_TABLE] -- constraints with different enforceability can be merged by marking them as ENFORCED
create table p1_c3() inherits(p1, p1_c1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean, rootdescend boolean, checkunique boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub5
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table idxpart0 alter column a set not null
;
----------
[CREATE_TABLE] create table list_part (a int) partition by list (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest7d ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[CREATE_TABLE] CREATE TABLE amcheck_schema.pg_database (junk text)
;
----------
[DROP_TABLE] DROP TABLE texttest, byteatest
;
----------
[CREATE_PROG_OBJ] create function conflict_test() returns setof int8_tbl as $$
declare r record;
  q1 bigint := 42;
begin
  for r in select q1,q2 from int8_tbl loop
    return next r;
  end loop;
end;
$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_A_B_2
        CONNECTION 'port=29891 host=/tmp/7Wj1Y48wnU dbname=postgres application_name=tap_sub_A_B_2'
        PUBLICATION tap_pub_B
        WITH (origin = none, copy_data = on)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest6c"(INOUT "a" "anyelement", INOUT "b" "anyelement")
    LANGUAGE "sql"
    AS $_$
SELECT $1, 1;
$_$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn13,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table anothertab
  add exclude using btree (f4 with =) where (f4 is not null)
;
----------
[CREATE_PROG_OBJ] create or replace function insert_tt2(text,text) returns setof int as
$$ insert into tt(data) values($1),($2) returning f1 $$
language sql
;
----------
[ADD_INDEX] CREATE INDEX test_replica_id_full_idx ON test_replica_id_full USING HASH (x)
;
----------
[ADMIN_TABLE] CLUSTER clstr_1
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg2_regression" SET bytea_output TO 'hex'
;
----------
[DROP_TABLE] DROP TABLE use_ft1_column_type
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table utrtest attach partition utr1 for values in (1)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn16a(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW role_column_grants AS
    SELECT grantor,
           grantee,
           table_catalog,
           table_schema,
           table_name,
           column_name,
           privilege_type,
           is_grantable
    FROM column_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION priv_testfunc4(boolean)
;
----------
[CREATE_TABLE] CREATE TABLE split_lock_next.child (note text) INHERITS (split_lock_next.parent)
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = 'division_by_zero', detail = 'some detail info';
  exception
    when others then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27691 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN g polygon
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE cp_testfunc1(int)
;
----------
[CREATE_TABLE] CREATE TABLE hash_parted (
	a int,
	b int
) PARTITION BY HASH (a part_test_int4_ops)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_read(int, bool DEFAULT true) RETURNS text
  AS 'MODULE_PATHNAME', 'test_slru_page_read' LANGUAGE C
;
----------
[DROP_TABLE] drop table notnull_tbl2, notnull_tbl3, notnull_tbl4, notnull_tbl5, notnull_tbl6
;
----------
[COMMENT_TABLESPACE] COMMENT ON TABLESPACE split_ts_space IS 'split tablespace comment'
;
----------
[ALTER_TABLE|ALTER_COLUMN|ADD_CONSTRAINT] ALTER TABLE testschema.test_tab ALTER b TYPE bigint, ADD UNIQUE (c)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pht1" ATTACH PARTITION "public"."pht1_p1" FOR VALUES WITH (modulus 3, remainder 0)
;
----------
[DROP_TRIGGER] DROP TRIGGER footrg_mod_a ON footrg1
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "S 1"
;
----------
[CREATE_TABLE] CREATE TABLE bit_defaults(
  b1 bit(4) DEFAULT '1001',
  b2 bit(4) DEFAULT B'0101',
  b3 bit varying(5) DEFAULT '1001',
  b4 bit varying(5) DEFAULT B'0101'
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30796 RENAME TO reind_con_toast
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION dummy_sub
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_ts_config
;
----------
[ALTER_TABLE] ALTER TABLE tenk1 set (parallel_workers = 4)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test6"
    AS smallint
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_PROG_OBJ] /* contrib/fuzzystrmatch/fuzzystrmatch--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION daitch_mokotoff(text) RETURNS text[]
AS '$libdir/fuzzystrmatch', 'daitch_mokotoff'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function outer_outer_func(int)
returns int as $$
declare
  myresult int;
begin
  raise notice 'calling down into outer_func()';
  myresult := outer_func($1);
  raise notice 'outer_func() done';
  return myresult;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'DECLARE c CURSOR WITH HOLD FOR SELECT public.unwanted_grant(); SELECT true'
;
----------
[BLOCK] do $$
declare x constant var_record;
begin
  x.f1 := 42;  -- fail
end$$
;
----------
[DROP_VIEW] drop view usersview
;
----------
[CREATE_TABLE] CREATE TABLE tt1 (f1 int, f2 int, f3 text)
;
