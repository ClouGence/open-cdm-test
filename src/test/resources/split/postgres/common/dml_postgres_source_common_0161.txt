INSERT INTO public.tplain VALUES ('4', 644)
;
-- not allowed
DELETE FROM rw_view2 WHERE aa=2
;
INSERT INTO f_star (class, a, f)
   VALUES ('f', 26, '(11111,33333),(22222,44444)'::polygon)
;
INSERT INTO circles VALUES('<(10,10), 10>', '<(0,0), 5>')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16299', relminmxid = '3'
WHERE oid = '"public"."pt_part1"'::pg_catalog.regclass
;
INSERT INTO errtst_parent(partid, shdata, data) VALUES ('20', '5', '5')
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (10, 1, 24740, 3689, CURRENT_TIMESTAMP)
;
insert into test_1 (name) values ('Test 1')
;
INSERT INTO pg_temp_9.pg_temp_25621_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_25621 AS newdata FROM public.mvtest_tm mv FULL JOIN pg_temp_9.pg_temp_25621 newdata ON (newdata.type OPERATOR(pg_catalog.=) mv.type AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'aa'
  AND attrelid = '"public"."e_star"'::pg_catalog.regclass
;
INSERT INTO tab7 VALUES (1, 22, 333, 4444)
;
INSERT INTO public.ths_p1 VALUES (8, 748)
;
insert into another values(2, 'two', 'due')
;
INSERT INTO reindex_before
SELECT oid, 'pg_toast_TABLE_index', relfilenode, relkind, reltoastrelid
FROM pg_class where oid in
	(select indexrelid from pg_index where indrelid in
		(select reltoastrelid from reindex_before where reltoastrelid > 0))
;
DELETE FROM users WHERE id = 4
;
UPDATE pgbench_tellers SET tbalance = tbalance + 1345 WHERE tid = 7
;
INSERT INTO public.tplain VALUES ('3', 183)
;
INSERT INTO public.tplain VALUES ('3', 343)
;
-- any of the following three input formats are acceptable
INSERT INTO VARCHAR_TBL (f1) VALUES ('1')
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."pk2"'::pg_catalog.regclass
  AND attname IN ('a')
;
INSERT INTO plt2_adv SELECT i, i, to_char(i % 10, 'FM0000') FROM generate_series(1, 299) i WHERE i % 10 IN (2, 3, 4, 6, 7, 9)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4298', relminmxid = '1'
WHERE oid = '20936'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17908', relminmxid = '3'
WHERE oid = '"public"."pt_part2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15303', relminmxid = '3'
WHERE oid = '"public"."thethings"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (6, 466)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14151', relminmxid = '3'
WHERE oid = '"public"."shiftjis2004_inputs"'::pg_catalog.regclass
;
INSERT INTO origin_tbl(data) VALUES ('no_lsn, commit')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '"public"."prt2_m_p3"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('3', 403)
;
-- fail on f2
insert into twoconstraints values(2, '((0,0),(1,2))')
  on conflict on constraint twoconstraints_f2_excl do nothing
;
INSERT INTO public.tht VALUES ('1', 341)
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.2........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."part_rev"'::pg_catalog.regclass
  AND attname = v.dname
;
INSERT INTO CIRCLE_TBL VALUES (' ( ( 1 , 2 ) , 3 ) ')
;
-- can't be pushed down
  DELETE FROM ft2 WHERE c1 % 10 = 6 AND c1 < 40 RETURNING old.c1, c4
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11990', relminmxid = '1'
WHERE oid = '32915'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13313', relminmxid = '3'
WHERE oid = '"public"."tt7"'::pg_catalog.regclass
;
INSERT INTO pxtest2 VALUES (1)
;
-- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '479', datminmxid = '1'
WHERE datname = 'regression'
;
insert into iocdu_tt_parted values (1, 'AAA'), (2, 'BBB'), (3, 'CCC'), (4, 'DDD')
  on conflict (a) do
  update set b = iocdu_tt_parted.b || ':' || excluded.b
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13652', relminmxid = '3'
WHERE oid = '34264'
;
INSERT INTO public.tplain VALUES ('9', 449)
;
insert into matest3 (name) values ('Test 6')
;
UPDATE current_check SET payload = payload || '_new' WHERE CURRENT OF current_check_cursor RETURNING *
;
UPDATE test SET b = '444' WHERE a = 4
;
UPDATE ctest SET salt = gen_salt('bf', 8)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."fk_partitioned_fk_4_2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
DELETE FROM test_tab1
;
INSERT INTO pg_temp_11.pg_temp_26410_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_26410 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_11.pg_temp_26410 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtestxx_4"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_m_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO public.tplain VALUES ('4', 644)
;
----------
[DELETE] -- not allowed
DELETE FROM rw_view2 WHERE aa=2
;
----------
[INSERT|SELECT] INSERT INTO f_star (class, a, f)
   VALUES ('f', 26, '(11111,33333),(22222,44444)'::polygon)
;
----------
[INSERT] INSERT INTO circles VALUES('<(10,10), 10>', '<(0,0), 5>')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16299', relminmxid = '3'
WHERE oid = '"public"."pt_part1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO errtst_parent(partid, shdata, data) VALUES ('20', '5', '5')
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (10, 1, 24740, 3689, CURRENT_TIMESTAMP)
;
----------
[INSERT|SELECT] insert into test_1 (name) values ('Test 1')
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_9.pg_temp_25621_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_25621 AS newdata FROM public.mvtest_tm mv FULL JOIN pg_temp_9.pg_temp_25621 newdata ON (newdata.type OPERATOR(pg_catalog.=) mv.type AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'aa'
  AND attrelid = '"public"."e_star"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO tab7 VALUES (1, 22, 333, 4444)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 748)
;
----------
[INSERT] insert into another values(2, 'two', 'due')
;
----------
[INSERT|SELECT] INSERT INTO reindex_before
SELECT oid, 'pg_toast_TABLE_index', relfilenode, relkind, reltoastrelid
FROM pg_class where oid in
	(select indexrelid from pg_index where indrelid in
		(select reltoastrelid from reindex_before where reltoastrelid > 0))
;
----------
[DELETE] DELETE FROM users WHERE id = 4
;
----------
[UPDATE] UPDATE pgbench_tellers SET tbalance = tbalance + 1345 WHERE tid = 7
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 183)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 343)
;
----------
[INSERT|SELECT] -- any of the following three input formats are acceptable
INSERT INTO VARCHAR_TBL (f1) VALUES ('1')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."pk2"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[INSERT|SELECT] INSERT INTO plt2_adv SELECT i, i, to_char(i % 10, 'FM0000') FROM generate_series(1, 299) i WHERE i % 10 IN (2, 3, 4, 6, 7, 9)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4298', relminmxid = '1'
WHERE oid = '20936'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '17908', relminmxid = '3'
WHERE oid = '"public"."pt_part2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15303', relminmxid = '3'
WHERE oid = '"public"."thethings"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 466)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14151', relminmxid = '3'
WHERE oid = '"public"."shiftjis2004_inputs"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO origin_tbl(data) VALUES ('no_lsn, commit')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '"public"."prt2_m_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 403)
;
----------
[MERGE] -- fail on f2
insert into twoconstraints values(2, '((0,0),(1,2))')
  on conflict on constraint twoconstraints_f2_excl do nothing
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 341)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.2........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."part_rev"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT] INSERT INTO CIRCLE_TBL VALUES (' ( ( 1 , 2 ) , 3 ) ')
;
----------
[DELETE] -- can't be pushed down
  DELETE FROM ft2 WHERE c1 % 10 = 6 AND c1 < 40 RETURNING old.c1, c4
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '11990', relminmxid = '1'
WHERE oid = '32915'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13313', relminmxid = '3'
WHERE oid = '"public"."tt7"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO pxtest2 VALUES (1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '479', datminmxid = '1'
WHERE datname = 'regression'
;
----------
[MERGE] insert into iocdu_tt_parted values (1, 'AAA'), (2, 'BBB'), (3, 'CCC'), (4, 'DDD')
  on conflict (a) do
  update set b = iocdu_tt_parted.b || ':' || excluded.b
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13652', relminmxid = '3'
WHERE oid = '34264'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 449)
;
----------
[INSERT|SELECT] insert into matest3 (name) values ('Test 6')
;
----------
[UPDATE] UPDATE current_check SET payload = payload || '_new' WHERE CURRENT OF current_check_cursor RETURNING *
;
----------
[UPDATE] UPDATE test SET b = '444' WHERE a = 4
;
----------
[UPDATE] UPDATE ctest SET salt = gen_salt('bf', 8)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."fk_partitioned_fk_4_2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[DELETE] DELETE FROM test_tab1
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_11.pg_temp_26410_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_26410 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_11.pg_temp_26410 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtestxx_4"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_m_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
