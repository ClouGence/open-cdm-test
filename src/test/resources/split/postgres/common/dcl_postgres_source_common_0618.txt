prepare ab_q3 (int, int) as
select a from ab where b between $1 and $2 and a < (select 3)
;
EXECUTE dumpFunc('34993')
;
EXECUTE getDomainConstraints('31022')
;
COPY public.b_star (class, aa, bb, a) TO stdout
;
LOCK TABLE pg_catalog.pg_publication IN ACCESS SHARE MODE
;
-- Test that rollback of a prepared xact is decoded.
BEGIN
;
ALTER FOREIGN DATA WRAPPER file_fdw OPTIONS (ADD force_null '*')
;
ALTER TABLE public.alpha_pos_p2 OWNER TO zyc
;
ROLLBACK
;
ALTER ROLE regress_createdb NOSUPERUSER
;
------------------------------------------------------------------------------------------
[UNSAFE] prepare ab_q3 (int, int) as
select a from ab where b between $1 and $2 and a < (select 3)
;
----------
[UNSAFE] EXECUTE dumpFunc('34993')
;
----------
[UNSAFE] EXECUTE getDomainConstraints('31022')
;
----------
[DATA_EXPORT] COPY public.b_star (class, aa, bb, a) TO stdout
;
----------
[SESSION_LOCK] LOCK TABLE pg_catalog.pg_publication IN ACCESS SHARE MODE
;
----------
[TRANSACTION] -- Test that rollback of a prepared xact is decoded.
BEGIN
;
----------
[SYSTEM_SETTING_WRITE] ALTER FOREIGN DATA WRAPPER file_fdw OPTIONS (ADD force_null '*')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.alpha_pos_p2 OWNER TO zyc
;
----------
[TRANSACTION] ROLLBACK
;
----------
[ALTER_USER] ALTER ROLE regress_createdb NOSUPERUSER
;
