ALTER TABLE regtest_table_part ALTER p SET STATISTICS -1
;
CREATE TRIGGER trig_row_after1
AFTER INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
CREATE POLICY r1 ON rec1 USING (x = (SELECT r.x FROM rec1 r WHERE y = r.y))
;
CREATE TABLE ref2 PARTITION OF ref FOR VALUES in (2)
;
-- ok
TRUNCATE TABLE truncate_a,trunc_e
;
create view rtest_v1 as select * from rtest_t1
;
CREATE OPERATOR CLASS "public"."part_test_int4_ops"
    FOR TYPE integer USING "hash" FAMILY "public"."part_test_int4_ops" AS
    OPERATOR 1 =(integer,integer)
;
create or replace function case_test(bigint) returns text as $$
declare a int = 10;
begin
  case
    when $1 = 1 then
      return 'one';
    when $1 = a + 2 then
      return 'twelve';
    else
      return 'other';
  end case;
end;
$$ language plpgsql immutable
;
CREATE TEXT SEARCH DICTIONARY intdict (
	TEMPLATE = intdict_template
)
;
-- Functions sufficient for making an index column that has the side effect of
-- changing search_path at expression planning time.
CREATE FUNCTION public.setter() RETURNS bool VOLATILE
  LANGUAGE SQL AS $$SET search_path = s; SELECT true$$
;
alter table my_locks reset (autovacuum_enabled)
;
CREATE TABLE testtab3 (a int)
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=19323 host=/tmp/FohDx5iOiZ dbname=postgres' PUBLICATION pub_all
;
CREATE FUNCTION ghstore_decompress(internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP VIEW atestv1
;
CREATE FUNCTION "public"."range_add_bounds"("anyrange") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$select lower($1) + upper($1)$_$
;
--
-- EUC_JIS_2004
--
CREATE TABLE euc_jis_2004_inputs (inbytes bytea, description text)
;
CREATE TABLE test_money (
	i money
)
;
CREATE AGGREGATE myaggn11a(BASETYPE = anyelement, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
alter table p attach partition p1 for values from (1, 2) to (1, 10)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_partitioned_table
;
CREATE FUNCTION gin_consistent_hstore(internal, int2, hstore, int4, internal, internal)
RETURNS bool
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ANALYZE pg_temp_9.pg_temp_25561_2
;
ALTER TABLE tbl2 ADD COLUMN val3 character varying
;
ANALYZE pg_temp_9.pg_temp_38052
;
alter table child2 drop column x
;
CREATE FUNCTION declares_cursor(text)
   RETURNS void
   AS 'DECLARE c CURSOR FOR SELECT stringu1 FROM tenk1 WHERE stringu1 LIKE $1;'
   LANGUAGE SQL
;
-- Should fail. duplicate operator number / function number in ALTER OPERATOR FAMILY ... ADD FUNCTION
CREATE OPERATOR FAMILY alt_opf17 USING btree
;
CREATE TABLE d_listp2 PARTITION OF d_listp FOR VALUES IN (2)
;
alter table abc add primary key (id)
;
create or replace function intercept_insert() returns trigger language plpgsql as
$$
  begin
    new.b = new.b + 1000;
    return new;
  end;
$$
;
TRUNCATE atest1
;
create trigger trig_ins_before_child before insert on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
CREATE TABLE pgss_schema_2.tab_search_same (a int, b int)
;
create table dropColumnAnother (d int) inherits (dropColumnChild)
;
create function range_add_bounds(anyrange)
  returns anyelement as 'select lower($1) + upper($1)' language sql
;
DROP TABLE past_part
;
drop function error2(p_name_table text)
;
-- inheritance related tests
create table atacc1 (test int)
;
alter table test alter column a1 drop default
;
DROP TRIGGER y_trig ON y
;
CREATE TEMP TABLE t1 (a text, b text)
;
-- ERROR

-- DROP FOREIGN TABLE
DROP FOREIGN TABLE no_table
;
CREATE VIEW pg_stat_bgwriter AS
    SELECT
        pg_stat_get_bgwriter_timed_checkpoints() AS checkpoints_timed,
        pg_stat_get_bgwriter_requested_checkpoints() AS checkpoints_req,
        pg_stat_get_checkpoint_write_time() AS checkpoint_write_time,
        pg_stat_get_checkpoint_sync_time() AS checkpoint_sync_time,
        pg_stat_get_bgwriter_buf_written_checkpoints() AS buffers_checkpoint,
        pg_stat_get_bgwriter_buf_written_clean() AS buffers_clean,
        pg_stat_get_bgwriter_maxwritten_clean() AS maxwritten_clean,
        pg_stat_get_buf_written_backend() AS buffers_backend,
        pg_stat_get_buf_fsync_backend() AS buffers_backend_fsync,
        pg_stat_get_buf_alloc() AS buffers_alloc,
        pg_stat_get_bgwriter_stat_reset_time() AS stats_reset
;
CREATE UNLOGGED TABLE tstu (
	i	int4,
	t	text
)
;
create table iocdu_tt_parted4 partition of iocdu_tt_parted for values in (4)
;
ALTER TABLE pg_temp_4.pg_temp_27487_2 ADD COLUMN newdata pg_temp_4.pg_temp_27487
;
CREATE FUNCTION test_argresult_array_domain_check_violation()
  RETURNS ordered_pair_domain AS $$
begin
return array[2,1];
end
$$ LANGUAGE plpgsql
;
CREATE TABLE emp (
	salary 		int4,
	manager 	name
) INHERITS (person)
;
-- NUL byte in text decrypt.  Ciphertext source:
-- printf 'a\x00\xc' | gpg --homedir /nonexistent \
--      --personal-compress-preferences uncompressed --textmode \
--      --personal-cipher-preferences aes --no-emit-version --batch \
--      --symmetric --passphrase key --armor
do $$
begin
  perform pgp_sym_decrypt(dearmor('
-----BEGIN PGP MESSAGE-----

jA0EBwMCXLc8pozB10Fg0jQBVUID59TLvWutJp0j6eh9ZgjqIRzdYaIymFB8y4XH
vu0YlJP5D5BX7yqZ+Pry7TlDmiFO
=rV7z
-----END PGP MESSAGE-----
'), 'key', 'debug=1');
exception when others then
  raise '%',
    regexp_replace(sqlerrm, 'encoding "[^"]*"', 'encoding [REDACTED]');
end
$$
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_table_part ALTER p SET STATISTICS -1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_after1
AFTER INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[CREATE_POLICY] CREATE POLICY r1 ON rec1 USING (x = (SELECT r.x FROM rec1 r WHERE y = r.y))
;
----------
[CREATE_TABLE] CREATE TABLE ref2 PARTITION OF ref FOR VALUES in (2)
;
----------
[TRUNCATE_TABLE] -- ok
TRUNCATE TABLE truncate_a,trunc_e
;
----------
[CREATE_VIEW(SELECT)] create view rtest_v1 as select * from rtest_t1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS "public"."part_test_int4_ops"
    FOR TYPE integer USING "hash" FAMILY "public"."part_test_int4_ops" AS
    OPERATOR 1 =(integer,integer)
;
----------
[CREATE_PROG_OBJ] create or replace function case_test(bigint) returns text as $$
declare a int = 10;
begin
  case
    when $1 = 1 then
      return 'one';
    when $1 = a + 2 then
      return 'twelve';
    else
      return 'other';
  end case;
end;
$$ language plpgsql immutable
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY intdict (
	TEMPLATE = intdict_template
)
;
----------
[CREATE_PROG_OBJ] -- Functions sufficient for making an index column that has the side effect of
-- changing search_path at expression planning time.
CREATE FUNCTION public.setter() RETURNS bool VOLATILE
  LANGUAGE SQL AS $$SET search_path = s; SELECT true$$
;
----------
[ALTER_TABLE] alter table my_locks reset (autovacuum_enabled)
;
----------
[CREATE_TABLE] CREATE TABLE testtab3 (a int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=19323 host=/tmp/FohDx5iOiZ dbname=postgres' PUBLICATION pub_all
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ghstore_decompress(internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_VIEW] DROP VIEW atestv1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."range_add_bounds"("anyrange") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$select lower($1) + upper($1)$_$
;
----------
[CREATE_TABLE] --
-- EUC_JIS_2004
--
CREATE TABLE euc_jis_2004_inputs (inbytes bytea, description text)
;
----------
[CREATE_TABLE] CREATE TABLE test_money (
	i money
)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn11a(BASETYPE = anyelement, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table p attach partition p1 for values from (1, 2) to (1, 10)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_partitioned_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_consistent_hstore(internal, int2, hstore, int4, internal, internal)
RETURNS bool
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_25561_2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tbl2 ADD COLUMN val3 character varying
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38052
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table child2 drop column x
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION declares_cursor(text)
   RETURNS void
   AS 'DECLARE c CURSOR FOR SELECT stringu1 FROM tenk1 WHERE stringu1 LIKE $1;'
   LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] -- Should fail. duplicate operator number / function number in ALTER OPERATOR FAMILY ... ADD FUNCTION
CREATE OPERATOR FAMILY alt_opf17 USING btree
;
----------
[CREATE_TABLE] CREATE TABLE d_listp2 PARTITION OF d_listp FOR VALUES IN (2)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table abc add primary key (id)
;
----------
[CREATE_PROG_OBJ] create or replace function intercept_insert() returns trigger language plpgsql as
$$
  begin
    new.b = new.b + 1000;
    return new;
  end;
$$
;
----------
[TRUNCATE_TABLE] TRUNCATE atest1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_ins_before_child before insert on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
----------
[CREATE_TABLE] CREATE TABLE pgss_schema_2.tab_search_same (a int, b int)
;
----------
[CREATE_TABLE] create table dropColumnAnother (d int) inherits (dropColumnChild)
;
----------
[CREATE_PROG_OBJ] create function range_add_bounds(anyrange)
  returns anyelement as 'select lower($1) + upper($1)' language sql
;
----------
[DROP_TABLE] DROP TABLE past_part
;
----------
[DROP_PROG_OBJ] drop function error2(p_name_table text)
;
----------
[CREATE_TABLE] -- inheritance related tests
create table atacc1 (test int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table test alter column a1 drop default
;
----------
[DROP_TRIGGER] DROP TRIGGER y_trig ON y
;
----------
[CREATE_TABLE] CREATE TEMP TABLE t1 (a text, b text)
;
----------
[DROP_TABLE] -- ERROR

-- DROP FOREIGN TABLE
DROP FOREIGN TABLE no_table
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_bgwriter AS
    SELECT
        pg_stat_get_bgwriter_timed_checkpoints() AS checkpoints_timed,
        pg_stat_get_bgwriter_requested_checkpoints() AS checkpoints_req,
        pg_stat_get_checkpoint_write_time() AS checkpoint_write_time,
        pg_stat_get_checkpoint_sync_time() AS checkpoint_sync_time,
        pg_stat_get_bgwriter_buf_written_checkpoints() AS buffers_checkpoint,
        pg_stat_get_bgwriter_buf_written_clean() AS buffers_clean,
        pg_stat_get_bgwriter_maxwritten_clean() AS maxwritten_clean,
        pg_stat_get_buf_written_backend() AS buffers_backend,
        pg_stat_get_buf_fsync_backend() AS buffers_backend_fsync,
        pg_stat_get_buf_alloc() AS buffers_alloc,
        pg_stat_get_bgwriter_stat_reset_time() AS stats_reset
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE tstu (
	i	int4,
	t	text
)
;
----------
[CREATE_TABLE] create table iocdu_tt_parted4 partition of iocdu_tt_parted for values in (4)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_27487_2 ADD COLUMN newdata pg_temp_4.pg_temp_27487
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_argresult_array_domain_check_violation()
  RETURNS ordered_pair_domain AS $$
begin
return array[2,1];
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE emp (
	salary 		int4,
	manager 	name
) INHERITS (person)
;
----------
[BLOCK] -- NUL byte in text decrypt.  Ciphertext source:
-- printf 'a\x00\xc' | gpg --homedir /nonexistent \
--      --personal-compress-preferences uncompressed --textmode \
--      --personal-cipher-preferences aes --no-emit-version --batch \
--      --symmetric --passphrase key --armor
do $$
begin
  perform pgp_sym_decrypt(dearmor('
-----BEGIN PGP MESSAGE-----

jA0EBwMCXLc8pozB10Fg0jQBVUID59TLvWutJp0j6eh9ZgjqIRzdYaIymFB8y4XH
vu0YlJP5D5BX7yqZ+Pry7TlDmiFO
=rV7z
-----END PGP MESSAGE-----
'), 'key', 'debug=1');
exception when others then
  raise '%',
    regexp_replace(sqlerrm, 'encoding "[^"]*"', 'encoding [REDACTED]');
end
$$
;
