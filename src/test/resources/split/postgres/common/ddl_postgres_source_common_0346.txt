drop table test9v
;
CREATE RULE accounts_audit_update AS
ON UPDATE TO split_prog.accounts
DO ALSO INSERT INTO split_prog.audit_log(account_id, action) VALUES (NEW.id, 'update')
;
ALTER TABLE testpub_tbl8_0 REPLICA IDENTITY FULL
;
/* src/test/modules/test_dsm_registry/test_dsm_registry--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION set_val_in_shmem(val INT) RETURNS VOID
	AS '$libdir/test_dsm_registry' LANGUAGE C
;
CREATE PROCEDURE transaction_test10a(INOUT x int)
LANGUAGE plpgsql
AS $$
BEGIN
  x := x + 1;
  COMMIT;
END;
$$
;
CREATE TYPE tablefunc_crosstab_3 AS
(
	row_name TEXT,
	category_1 TEXT,
	category_2 TEXT,
	category_3 TEXT
)
;
ANALYZE public.itest7b
;
create function lock_excl(bigint) returns int language internal as
  'pg_advisory_xact_lock_int8' strict parallel safe
;
CREATE TABLE tab2 (a int PRIMARY KEY, c text DEFAULT 'sub2_tab2', b text)
;
DROP TABLE brintest_unlogged
;
ALTER TABLE pitest3_p1 ALTER COLUMN f3 SET NOT NULL
;
create index brin_wi_idx on brin_wi using brin (a) with (pages_per_range=1, autosummarize=on)
;
/*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
CREATE FUNCTION gin_extract_hstore(hstore, internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_33024_index RENAME TO reind_con_toast_idx
;
CREATE TRIGGER messages_by_name_tsvector
    BEFORE UPDATE OR INSERT ON split_builtin_trigger.messages_by_name
    FOR EACH ROW
    EXECUTE PROCEDURE tsvector_update_trigger(document, 'pg_catalog.english', title, body)
;
CREATE TABLE "pubme2"."t0" (
    "c" integer,
    "d" integer
)
;
CREATE TRIGGER "gtest12_02" BEFORE INSERT OR UPDATE ON "generated_virtual_tests"."gtest26" FOR EACH ROW EXECUTE FUNCTION "generated_virtual_tests"."gtest_trigger_func4"()
;
CREATE FUNCTION spgist_name_config(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS '$libdir/spgist_name_ops' LANGUAGE C
;
alter table p1 drop constraint f1_pos
;
CREATE VIEW "public"."rtest_vview2" AS
 SELECT "rtest_view1"."a",
    "rtest_view1"."b"
   FROM "public"."rtest_view1"
  WHERE "rtest_view1"."v"
;
CREATE TABLE tab_full2 (x text)
;
create or replace function dummy_hashint4(a int4, seed int8) returns int8 as
$$ begin return (a + seed); end; $$ language 'plpgsql' immutable
;
CREATE EXTENSION test_ext4 SCHEMA pg_temp_3 CASCADE
;
create trigger base_tab_def_view_instrig instead of insert on base_tab_def_view
  for each row execute function base_tab_def_view_instrig_func()
;
CREATE FUNCTION hstore_to_jsonb(hstore)
RETURNS jsonb
AS '$libdir/hstore', 'hstore_to_jsonb'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ANALYZE (SKIP_LOCKED, VERBOSE) does_not_exist
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_enum
;
DROP SCHEMA IF EXISTS split_pgvector_probe CASCADE
;
CREATE RULE rw_view16_ins_rule AS ON INSERT TO rw_view16
  WHERE NEW.a > 0 DO INSTEAD INSERT INTO base_tbl VALUES (NEW.a, NEW.b)
;
ALTER TABLE test_tab ADD COLUMN d INT
;
-- It no longer is possible to issue CREATE TYPE without making a shell first
CREATE TYPE bogus_type (INPUT = array_in,
    OUTPUT = array_out,
    ELEMENT = int,
    INTERNALLENGTH = 32)
;
CREATE TABLE pgss_do_util_tab_1 (a int)
;
CREATE TABLE public.tab1_child1 PARTITION OF sch1.tab1_parent FOR VALUES IN (1, 2, 3)
;
--   Cleanup
DROP TABLE test_missing_target
;
ALTER INDEX pg_toast.pg_toast_30845_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION test_pg_dump(int) RETURNS int AS $$
BEGIN
RETURN abs($1);
END
$$ LANGUAGE plpgsql IMMUTABLE
;
create table dropcol(key int primary key, drop1 int, keep1 text, drop2 numeric, keep2 float)
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
CREATE VIEW "public"."xmltableview1" AS
 SELECT "xmltable"."id",
    "xmltable"."_id",
    "xmltable"."country_name",
    "xmltable"."country_id",
    "xmltable"."region_id",
    "xmltable"."size",
    "xmltable"."unit",
    "xmltable"."premier_name"
   FROM ( SELECT "xmldata"."data"
           FROM "public"."xmldata") "x",
    LATERAL XMLTABLE(('/ROWS/ROW'::"text") PASSING ("x"."data") COLUMNS "id" integer PATH ('@id'::"text"), "_id" FOR ORDINALITY, "country_name" "text" PATH ('COUNTRY_NAME/text()'::"text") NOT NULL, "country_id" "text" PATH ('COUNTRY_ID'::"text"), "region_id" integer PATH ('REGION_ID'::"text"), "size" double precision PATH ('SIZE'::"text"), "unit" "text" PATH ('SIZE/@unit'::"text"), "premier_name" "text" DEFAULT ('not specified'::"text") PATH ('PREMIER_NAME'::"text"))
;
CREATE FUNCTION "public"."non_strict"("text") RETURNS "text"
    LANGUAGE "sql" STRICT
    AS $_$select coalesce($1, 'got passed a null');$_$
;
CREATE FUNCTION spgist_name_compress(name)
RETURNS text IMMUTABLE PARALLEL SAFE STRICT
AS '$libdir/spgist_name_ops' LANGUAGE C
;
create temp table tt1 as select * from int8_tbl limit 2
;
CREATE TABLE "public"."arrtest" (
    "a" smallint[],
    "b" integer[],
    "c" "name"[],
    "d" "text"[],
    "e" double precision[],
    "f" character(5)[],
    "g" character varying(5)[]
)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_shdescription
;
CREATE TABLE "public"."prt2_e_p2" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
-- Attaching a child with a NOT VALID constraint.
CREATE TABLE fk_partitioned_fk_2 (a int, b int)
;
create temp view shipped_view as
    select * from shipped where ttype = 'wt'
;
CREATE TABLE prt1 (a int, b int, c varchar) PARTITION BY RANGE(a)
;
ALTER OPERATOR split_operator_probe.## (integer, integer) SET SCHEMA split_operator_probe_next
;
------------------------------------------------------------------------------------------
[DROP_TABLE] drop table test9v
;
----------
[CREATE_POLICY] CREATE RULE accounts_audit_update AS
ON UPDATE TO split_prog.accounts
DO ALSO INSERT INTO split_prog.audit_log(account_id, action) VALUES (NEW.id, 'update')
;
----------
[ALTER_TABLE] ALTER TABLE testpub_tbl8_0 REPLICA IDENTITY FULL
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_dsm_registry/test_dsm_registry--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION set_val_in_shmem(val INT) RETURNS VOID
	AS '$libdir/test_dsm_registry' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test10a(INOUT x int)
LANGUAGE plpgsql
AS $$
BEGIN
  x := x + 1;
  COMMIT;
END;
$$
;
----------
[CREATE_TYPE] CREATE TYPE tablefunc_crosstab_3 AS
(
	row_name TEXT,
	category_1 TEXT,
	category_2 TEXT,
	category_3 TEXT
)
;
----------
[ADMIN_TABLE] ANALYZE public.itest7b
;
----------
[CREATE_PROG_OBJ] create function lock_excl(bigint) returns int language internal as
  'pg_advisory_xact_lock_int8' strict parallel safe
;
----------
[CREATE_TABLE] CREATE TABLE tab2 (a int PRIMARY KEY, c text DEFAULT 'sub2_tab2', b text)
;
----------
[DROP_TABLE] DROP TABLE brintest_unlogged
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest3_p1 ALTER COLUMN f3 SET NOT NULL
;
----------
[ADD_INDEX] create index brin_wi_idx on brin_wi using brin (a) with (pages_per_range=1, autosummarize=on)
;
----------
[CREATE_POLICY] /*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_hstore(hstore, internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33024_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER messages_by_name_tsvector
    BEFORE UPDATE OR INSERT ON split_builtin_trigger.messages_by_name
    FOR EACH ROW
    EXECUTE PROCEDURE tsvector_update_trigger(document, 'pg_catalog.english', title, body)
;
----------
[CREATE_TABLE] CREATE TABLE "pubme2"."t0" (
    "c" integer,
    "d" integer
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "gtest12_02" BEFORE INSERT OR UPDATE ON "generated_virtual_tests"."gtest26" FOR EACH ROW EXECUTE FUNCTION "generated_virtual_tests"."gtest_trigger_func4"()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION spgist_name_config(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS '$libdir/spgist_name_ops' LANGUAGE C
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table p1 drop constraint f1_pos
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_vview2" AS
 SELECT "rtest_view1"."a",
    "rtest_view1"."b"
   FROM "public"."rtest_view1"
  WHERE "rtest_view1"."v"
;
----------
[CREATE_TABLE] CREATE TABLE tab_full2 (x text)
;
----------
[CREATE_PROG_OBJ] create or replace function dummy_hashint4(a int4, seed int8) returns int8 as
$$ begin return (a + seed); end; $$ language 'plpgsql' immutable
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext4 SCHEMA pg_temp_3 CASCADE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger base_tab_def_view_instrig instead of insert on base_tab_def_view
  for each row execute function base_tab_def_view_instrig_func()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_to_jsonb(hstore)
RETURNS jsonb
AS '$libdir/hstore', 'hstore_to_jsonb'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED, VERBOSE) does_not_exist
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_enum
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_pgvector_probe CASCADE
;
----------
[CREATE_POLICY] CREATE RULE rw_view16_ins_rule AS ON INSERT TO rw_view16
  WHERE NEW.a > 0 DO INSTEAD INSERT INTO base_tbl VALUES (NEW.a, NEW.b)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_tab ADD COLUMN d INT
;
----------
[CREATE_TYPE] -- It no longer is possible to issue CREATE TYPE without making a shell first
CREATE TYPE bogus_type (INPUT = array_in,
    OUTPUT = array_out,
    ELEMENT = int,
    INTERNALLENGTH = 32)
;
----------
[CREATE_TABLE] CREATE TABLE pgss_do_util_tab_1 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE public.tab1_child1 PARTITION OF sch1.tab1_parent FOR VALUES IN (1, 2, 3)
;
----------
[DROP_TABLE] --   Cleanup
DROP TABLE test_missing_target
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30845_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_pg_dump(int) RETURNS int AS $$
BEGIN
RETURN abs($1);
END
$$ LANGUAGE plpgsql IMMUTABLE
;
----------
[CREATE_TABLE] create table dropcol(key int primary key, drop1 int, keep1 text, drop2 numeric, keep2 float)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."xmltableview1" AS
 SELECT "xmltable"."id",
    "xmltable"."_id",
    "xmltable"."country_name",
    "xmltable"."country_id",
    "xmltable"."region_id",
    "xmltable"."size",
    "xmltable"."unit",
    "xmltable"."premier_name"
   FROM ( SELECT "xmldata"."data"
           FROM "public"."xmldata") "x",
    LATERAL XMLTABLE(('/ROWS/ROW'::"text") PASSING ("x"."data") COLUMNS "id" integer PATH ('@id'::"text"), "_id" FOR ORDINALITY, "country_name" "text" PATH ('COUNTRY_NAME/text()'::"text") NOT NULL, "country_id" "text" PATH ('COUNTRY_ID'::"text"), "region_id" integer PATH ('REGION_ID'::"text"), "size" double precision PATH ('SIZE'::"text"), "unit" "text" PATH ('SIZE/@unit'::"text"), "premier_name" "text" DEFAULT ('not specified'::"text") PATH ('PREMIER_NAME'::"text"))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."non_strict"("text") RETURNS "text"
    LANGUAGE "sql" STRICT
    AS $_$select coalesce($1, 'got passed a null');$_$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION spgist_name_compress(name)
RETURNS text IMMUTABLE PARALLEL SAFE STRICT
AS '$libdir/spgist_name_ops' LANGUAGE C
;
----------
[CREATE_TABLE|SELECT] create temp table tt1 as select * from int8_tbl limit 2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."arrtest" (
    "a" smallint[],
    "b" integer[],
    "c" "name"[],
    "d" "text"[],
    "e" double precision[],
    "f" character(5)[],
    "g" character varying(5)[]
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_shdescription
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_e_p2" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[CREATE_TABLE] -- Attaching a child with a NOT VALID constraint.
CREATE TABLE fk_partitioned_fk_2 (a int, b int)
;
----------
[CREATE_VIEW(SELECT)] create temp view shipped_view as
    select * from shipped where ttype = 'wt'
;
----------
[CREATE_TABLE] CREATE TABLE prt1 (a int, b int, c varchar) PARTITION BY RANGE(a)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR split_operator_probe.## (integer, integer) SET SCHEMA split_operator_probe_next
;
