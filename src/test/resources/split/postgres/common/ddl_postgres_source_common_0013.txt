CREATE TABLE "public"."itest7c" (
    "a" integer NOT NULL
)
;
--
-- Tests for the planner's "equivalence class" mechanism
--

-- One thing that's not tested well during normal querying is the logic
-- for handling "broken" ECs.  This is because an EC can only become broken
-- if its underlying btree operator family doesn't include a complete set
-- of cross-type equality operators.  There are not (and should not be)
-- any such families built into Postgres; so we have to hack things up
-- to create one.  We do this by making two alias types that are really
-- int8 (so we need no new C code) and adding only some operators for them
-- into the standard integer_ops opfamily.

create type int8alias1
;
CREATE TABLE kd_point_tbl AS SELECT * FROM quad_point_tbl
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should work
  execute 'insert into foo values(5,6) returning *' into x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
create table ths_p1 partition of ths for values with (modulus 3, remainder 0)
;
CREATE DATABASE foobar1
;
ALTER RULE r1 ON pg_description RENAME TO r2
;
CREATE TABLE plt1_adv_p1 PARTITION OF plt1_adv FOR VALUES IN ('0000', '0001', '0002')
;
ANALYZE public.dropcolumn
;
do $$
declare r ordered_int8s;
begin
  for r in values (1,2),(3,4),(6,5) loop
    raise notice 'r = %', r;
  end loop;
end$$
;
ALTER TABLE regtest_ptable_1 SET SCHEMA regtest_schema_2
;
ALTER FUNCTION functest_F_3(int) CALLED ON NULL INPUT
;
-- additionally, we've seen issues with foreign key validation not being
-- properly delayed until after a table rewrite.  Check that works ok.
create table atacc1 (a int primary key)
;
ALTER INDEX dummy_test_idx SET (option_string_val = 4)
;
CREATE TABLE pred_tab_notnull (a int, b int NOT NULL)
;
ALTER TABLE pg_toast.pg_toast_27611 RENAME TO reind_con_toast
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_policy
;
ALTER TABLE part_5 ADD CONSTRAINT check_a CHECK (a IN (5)),
	 ALTER a SET NOT NULL
;
CREATE FUNCTION gbt_float8_distance(internal,float8,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE t0()
;
DROP TYPE bogus_type
;
ALTER TABLE ONLY "public"."cnn_uq"
    ADD CONSTRAINT "cnn_uq_idx" UNIQUE ("a")
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "asciihword" WITH "public"."synonym", "english_stem"
;
-- GiST support

CREATE TYPE ghstore
;
alter table fullname add column suffix text default null
;
CREATE TYPE "public"."cashrange" AS RANGE (
    subtype = "money"
)
;
CREATE FUNCTION update_using_indirect()
        RETURNS trigger
        LANGUAGE plpgsql AS $$
BEGIN
    NEW := make_tuple_indirect(NEW);
    RETURN NEW;
END$$
;
CREATE UNLOGGED TABLE itest17 (a int NOT NULL, b text)
;
-- but it's okay like this
create or replace function constant_refcursor() returns refcursor as $$
declare
    rc constant refcursor := 'my_cursor_name';
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
alter table parted_irreg_ancestor drop column fd,
  drop column fd2, drop column fd3
;
create table idxpart_another_1 partition of idxpart_another for values from (0) to (100)
;
CREATE TABLE "public"."log_table" (
    "tstamp" timestamp without time zone DEFAULT ("timeofday"())::timestamp without time zone
)
;
-- insert into parent with a before trigger on a child tuple before
-- insertion, and we capture the newly modified row in parent format
create or replace function intercept_insert() returns trigger language plpgsql as
$$
  begin
    new.b = new.b + 1000;
    return new;
  end;
$$
;
CREATE FUNCTION gbt_intv_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE FUNCTION texticregexne(citext, citext)
RETURNS bool AS 'texticregexne'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
--
-- [re]plan counting
--
CREATE TABLE stats_plan_test ()
;
CREATE FUNCTION f_get_x () RETURNS int
AS $$
DECLARE l_result int;
BEGIN
  SELECT x INTO l_result FROM t_test;
  RETURN l_result;
END
$$ LANGUAGE plpgsql STABLE
;
drop table hash_parted
;
ALTER TYPE "public"."insenum" ADD VALUE 'i25'
;
CREATE SEQUENCE sequence_test3
;
-- DML affecting parent sees tuples collected from children even if
-- there is no transition table trigger on the children
drop trigger child1_insert_trig on child1
;
ALTER TABLE ONLY "fkpart5"."fk" ATTACH PARTITION "fkpart5"."fk2" FOR VALUES IN (2)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=26674 host=/tmp/1uFGuomZGN dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
ALTER TABLE ONLY "fkpart5"."fk" ATTACH PARTITION "fkpart5"."fk3" FOR VALUES IN (3)
;
CREATE OPERATOR = (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_eq,
	COMMUTATOR = '=', NEGATOR = '<>',
	RESTRICT = eqsel, JOIN = eqjoinsel,
	MERGES
)
;
CREATE TABLE base_tbl4 (a int, b int, c text)
;
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD
  
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
/*
 * 6.61
 * SQL_SIZING table
 */

CREATE TABLE sql_sizing (
    sizing_id       cardinal_number,
    sizing_name     character_data,
    supported_value cardinal_number,
    comments        character_data
)
;
DROP PUBLICATION split_pub18_schema
;
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 1 <
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "public"."itest7c" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TYPE] --
-- Tests for the planner's "equivalence class" mechanism
--

-- One thing that's not tested well during normal querying is the logic
-- for handling "broken" ECs.  This is because an EC can only become broken
-- if its underlying btree operator family doesn't include a complete set
-- of cross-type equality operators.  There are not (and should not be)
-- any such families built into Postgres; so we have to hack things up
-- to create one.  We do this by making two alias types that are really
-- int8 (so we need no new C code) and adding only some operators for them
-- into the standard integer_ops opfamily.

create type int8alias1
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE kd_point_tbl AS SELECT * FROM quad_point_tbl
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should work
  execute 'insert into foo values(5,6) returning *' into x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[CREATE_TABLE] create table ths_p1 partition of ths for values with (modulus 3, remainder 0)
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar1
;
----------
[ALTER_POLICY] ALTER RULE r1 ON pg_description RENAME TO r2
;
----------
[CREATE_TABLE] CREATE TABLE plt1_adv_p1 PARTITION OF plt1_adv FOR VALUES IN ('0000', '0001', '0002')
;
----------
[ADMIN_TABLE] ANALYZE public.dropcolumn
;
----------
[BLOCK] do $$
declare r ordered_int8s;
begin
  for r in values (1,2),(3,4),(6,5) loop
    raise notice 'r = %', r;
  end loop;
end$$
;
----------
[ALTER_TABLE] ALTER TABLE regtest_ptable_1 SET SCHEMA regtest_schema_2
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_F_3(int) CALLED ON NULL INPUT
;
----------
[CREATE_TABLE] -- additionally, we've seen issues with foreign key validation not being
-- properly delayed until after a table rewrite.  Check that works ok.
create table atacc1 (a int primary key)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_string_val = 4)
;
----------
[CREATE_TABLE] CREATE TABLE pred_tab_notnull (a int, b int NOT NULL)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27611 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_policy
;
----------
[ALTER_TABLE|ADD_CONSTRAINT|ALTER_COLUMN] ALTER TABLE part_5 ADD CONSTRAINT check_a CHECK (a IN (5)),
	 ALTER a SET NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float8_distance(internal,float8,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE t0()
;
----------
[DROP_TYPE] DROP TYPE bogus_type
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."cnn_uq"
    ADD CONSTRAINT "cnn_uq_idx" UNIQUE ("a")
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "asciihword" WITH "public"."synonym", "english_stem"
;
----------
[CREATE_TYPE] -- GiST support

CREATE TYPE ghstore
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table fullname add column suffix text default null
;
----------
[CREATE_TYPE] CREATE TYPE "public"."cashrange" AS RANGE (
    subtype = "money"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION update_using_indirect()
        RETURNS trigger
        LANGUAGE plpgsql AS $$
BEGIN
    NEW := make_tuple_indirect(NEW);
    RETURN NEW;
END$$
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE itest17 (a int NOT NULL, b text)
;
----------
[CREATE_PROG_OBJ] -- but it's okay like this
create or replace function constant_refcursor() returns refcursor as $$
declare
    rc constant refcursor := 'my_cursor_name';
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table parted_irreg_ancestor drop column fd,
  drop column fd2, drop column fd3
;
----------
[CREATE_TABLE] create table idxpart_another_1 partition of idxpart_another for values from (0) to (100)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."log_table" (
    "tstamp" timestamp without time zone DEFAULT ("timeofday"())::timestamp without time zone
)
;
----------
[CREATE_PROG_OBJ] -- insert into parent with a before trigger on a child tuple before
-- insertion, and we capture the newly modified row in parent format
create or replace function intercept_insert() returns trigger language plpgsql as
$$
  begin
    new.b = new.b + 1000;
    return new;
  end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_intv_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION texticregexne(citext, citext)
RETURNS bool AS 'texticregexne'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] --
-- [re]plan counting
--
CREATE TABLE stats_plan_test ()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f_get_x () RETURNS int
AS $$
DECLARE l_result int;
BEGIN
  SELECT x INTO l_result FROM t_test;
  RETURN l_result;
END
$$ LANGUAGE plpgsql STABLE
;
----------
[DROP_TABLE] drop table hash_parted
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i25'
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test3
;
----------
[DROP_TRIGGER] -- DML affecting parent sees tuples collected from children even if
-- there is no transition table trigger on the children
drop trigger child1_insert_trig on child1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart5"."fk" ATTACH PARTITION "fkpart5"."fk2" FOR VALUES IN (2)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=26674 host=/tmp/1uFGuomZGN dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart5"."fk" ATTACH PARTITION "fkpart5"."fk3" FOR VALUES IN (3)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_eq,
	COMMUTATOR = '=', NEGATOR = '<>',
	RESTRICT = eqsel, JOIN = eqjoinsel,
	MERGES
)
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl4 (a int, b int, c text)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf4 USING btree ADD
  
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
----------
[CREATE_TABLE] /*
 * 6.61
 * SQL_SIZING table
 */

CREATE TABLE sql_sizing (
    sizing_id       cardinal_number,
    sizing_name     character_data,
    supported_value cardinal_number,
    comments        character_data
)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION split_pub18_schema
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 1 <
;
