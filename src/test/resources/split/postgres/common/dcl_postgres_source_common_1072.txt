EXECUTE dumpTableAttach('38656')
;
ALTER VIEW public.rtest_vview5 OWNER TO zyc
;
DROP ROLE regress_rls_eve
;
COPY pg_catalog.pg_publication_rel (oid, prpubid, prrelid, prqual, prattrs) TO stdout
;
COPY public.tab1_2 (a, b) TO STDOUT
;
SET SESSION AUTHORIZATION regress_role_limited_admin
;
set work_mem to default
;
LOCK TABLE "public"."mlparted_def2" IN ACCESS SHARE MODE
;
ALTER SERVER loopback OPTIONS (DROP parallel_commit)
;
PREPARE dumpCompositeType(pg_catalog.oid) AS
SELECT a.attname, a.attnum, pg_catalog.format_type(a.atttypid, a.atttypmod) AS atttypdefn, a.attlen, a.attalign, a.attisdropped, CASE WHEN a.attcollation <> at.typcollation THEN a.attcollation ELSE 0 END AS attcollation FROM pg_catalog.pg_type ct JOIN pg_catalog.pg_attribute a ON a.attrelid = ct.typrelid LEFT JOIN pg_catalog.pg_type at ON at.oid = a.atttypid WHERE ct.oid = $1 ORDER BY a.attnum
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpTableAttach('38656')
;
----------
[TRANSFER_PRIVILEGE] ALTER VIEW public.rtest_vview5 OWNER TO zyc
;
----------
[DROP_ROLE] DROP ROLE regress_rls_eve
;
----------
[DATA_EXPORT] COPY pg_catalog.pg_publication_rel (oid, prpubid, prrelid, prqual, prattrs) TO stdout
;
----------
[DATA_EXPORT] COPY public.tab1_2 (a, b) TO STDOUT
;
----------
[SWITCH_USER] SET SESSION AUTHORIZATION regress_role_limited_admin
;
----------
[SESSION_SETTING_WRITE] set work_mem to default
;
----------
[SESSION_LOCK] LOCK TABLE "public"."mlparted_def2" IN ACCESS SHARE MODE
;
----------
[SYSTEM_SETTING_WRITE] ALTER SERVER loopback OPTIONS (DROP parallel_commit)
;
----------
[UNSAFE] PREPARE dumpCompositeType(pg_catalog.oid) AS
SELECT a.attname, a.attnum, pg_catalog.format_type(a.atttypid, a.atttypmod) AS atttypdefn, a.attlen, a.attalign, a.attisdropped, CASE WHEN a.attcollation <> at.typcollation THEN a.attcollation ELSE 0 END AS attcollation FROM pg_catalog.pg_type ct JOIN pg_catalog.pg_attribute a ON a.attrelid = ct.typrelid LEFT JOIN pg_catalog.pg_type at ON at.oid = a.atttypid WHERE ct.oid = $1 ORDER BY a.attnum
;
