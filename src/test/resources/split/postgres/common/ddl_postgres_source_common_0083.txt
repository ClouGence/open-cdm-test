CREATE TYPE rf_bug_status AS ENUM ('new', 'open', 'closed')
;
ANALYZE pg_temp_13.pg_temp_26250_2
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_attrdef
;
-- fail, can't infer type:
create function polyf(x anycompatible) returns anycompatiblerange as $$
  select array[x + 1, x + 2]
$$ language sql
;
-- error
CALL ptest1('a')
;
CREATE FUNCTION gseg_union(internal, internal)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN xmin OPTIONS (ADD p1 'v1')
;
drop table inh_nn1, inh_nn2, inh_nn3
;
create operator = (
    procedure = int8alias1eq,
    leftarg = int8alias1, rightarg = int8alias2,
    restrict = eqsel, join = eqjoinsel,
    merges
)
;
VACUUM (ANALYZE) information_schema.sql_packages
;
DROP PUBLICATION regress_pub1
;
ALTER OPERATOR FAMILY gist_uuid_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
ALTER TABLE "public"."rule_merge1" DISABLE RULE "rule1"
;
CREATE OPERATOR ?<@ (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_extract_risparent
)
;
--
-- Tables and rules for the logging test
--
create table rtest_emp (ename char(20), salary numeric)
;
-- invalid: non-matching result types

CREATE FUNCTION float8mi_int(float8, float8) RETURNS int AS
$$ SELECT CAST($1 - $2 AS INT); $$
LANGUAGE SQL
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.timestamp()'))
;
alter table invalid_check_con_child add constraint inh_check_constraint check(f1 > 0) not valid
;
CREATE TABLE base_tbl (a int, b int)
;
DROP INDEX brinidx_multi
;
ANALYZE public.tt6
;
ANALYZE public.gtest30_1
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_proc
;
-- 5.8 ADMINISTRABLE_ROLE_AUTHORIZATIONS view appears later.


/*
 * 5.9
 * APPLICABLE_ROLES view
 */

CREATE VIEW applicable_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS grantee,
           CAST(b.rolname AS sql_identifier) AS role_name,
           CAST(CASE WHEN m.admin_option THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable
    FROM (SELECT member, roleid, admin_option FROM pg_auth_members
          -- This UNION could be UNION ALL, but UNION works even if we start
          -- to allow explicit pg_database_owner membership.
          UNION
          SELECT datdba, pg_authid.oid, false
          FROM pg_database, pg_authid
          WHERE datname = current_database() AND rolname = 'pg_database_owner'
         )  m
         JOIN pg_authid a ON (m.member = a.oid)
         JOIN pg_authid b ON (m.roleid = b.oid)
    WHERE pg_has_role(a.oid, 'USAGE')
;
CREATE INDEX i_bmtest_b ON bmscantest(b)
;
CREATE FOREIGN TABLE async_p1 PARTITION OF async_pt FOR VALUES FROM (1000) TO (2000)
  SERVER loopback OPTIONS (table_name 'base_tbl1')
;
CREATE TABLE pagg_tab_para_p2 PARTITION OF pagg_tab_para FOR VALUES FROM (10) TO (20)
;
drop function anyctest(anycompatiblenonarray, anycompatiblenonarray)
;
ALTER TABLE pred_child ALTER a SET NOT NULL
;
do $$
declare
  v1 int;
  v2 varchar;
  a1 v1%type[];
  a2 v2%type[];
begin
  v1 := 10;
  v2 := 'Hi';
  a1 := array[v1,v1];
  a2 := array[v2,v2];
  raise notice '% %', a1, a2;
end;
$$
;
ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f3_excl EXCLUDE USING btree (f3 WITH =)
;
do $$ declare a int[];
begin a[1][4] := 4; raise notice 'a = %', a; end$$
;
ALTER TABLE fk ADD FOREIGN KEY (a) REFERENCES pk ON UPDATE SET DEFAULT ON DELETE SET DEFAULT
;
CREATE TABLE "public"."constr_parent2" (
    "a" integer
)
;
TRUNCATE copy1
;
-- A table with with contents that, when sorted, triggers abbreviated
-- key aborts. One easy way to achieve that is to use uuids that all
-- have the same prefix, as abbreviated keys for uuids just use the
-- first sizeof(Datum) bytes.
CREATE TEMP TABLE abbrev_abort_uuids (
    id serial not null,
    abort_increasing uuid,
    abort_decreasing uuid,
    noabort_increasing uuid,
    noabort_decreasing uuid)
;
/* Has ee entry */

CREATE TABLE inhf (LIKE inhx, LIKE inhx)
;
ALTER TEXT SEARCH PARSER alt_ts_prs2 SET SCHEMA alt_nsp2
;
CREATE TABLE part1 (tid integer, balance float, val text)
  WITH (autovacuum_enabled=off)
;
CREATE TABLE test2 AS (SELECT generate_series(1, 1000)::int8 AS col1)
;
-- fail

-- unsupported constraint type for partitioned tables
CREATE TABLE partitioned (
	a int,
	EXCLUDE USING gist (a WITH &&)
) PARTITION BY RANGE (a)
;
ALTER TABLE pg_toast.pg_toast_33271 RENAME TO reind_con_toast
;
CREATE SUBSCRIPTION regress_mysub1 CONNECTION 'port=29713 host=/tmp/jOoI3lrZrW dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub1_slot, failover = true, create_slot = false)
;
-- error
CREATE TABLE gtestx (x int, b int GENERATED ALWAYS AS IDENTITY) INHERITS (gtest1)
;
-- user's security level must be higher than or equal to document's
CREATE POLICY p1 ON document AS PERMISSIVE
    USING (dlevel <= (SELECT seclv FROM uaccount WHERE pguser = current_user))
;
CREATE FUNCTION "public"."func_with_set_params"() RETURNS integer
    LANGUAGE "sql" IMMUTABLE STRICT
    SET "search_path" TO 'pg_catalog'
    SET "extra_float_digits" TO '2'
    SET "work_mem" TO '4MB'
    SET "DateStyle" TO 'iso, mdy'
    SET "local_preload_libraries" TO 'Mixed/Case', 'c:/''a"/path', '', '0123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789'
    AS $$select 1;$$
;
--ALTER TABLE attmp ADD COLUMN o lock;
ALTER TABLE attmp ADD COLUMN p boolean
;
CREATE POLICY p2 ON r1 AS RESTRICTIVE USING ((SELECT c) < 400)
;
CREATE SCHEMA "pubme"
;
CREATE TABLE vacparted (a int, b char) PARTITION BY LIST (a)
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] CREATE TYPE rf_bug_status AS ENUM ('new', 'open', 'closed')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26250_2
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_attrdef
;
----------
[CREATE_PROG_OBJ] -- fail, can't infer type:
create function polyf(x anycompatible) returns anycompatiblerange as $$
  select array[x + 1, x + 2]
$$ language sql
;
----------
[CALL_PROG_OBJ] -- error
CALL ptest1('a')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gseg_union(internal, internal)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN xmin OPTIONS (ADD p1 'v1')
;
----------
[DROP_TABLE] drop table inh_nn1, inh_nn2, inh_nn3
;
----------
[CREATE_PROG_OBJ] create operator = (
    procedure = int8alias1eq,
    leftarg = int8alias1, rightarg = int8alias2,
    restrict = eqsel, join = eqjoinsel,
    merges
)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) information_schema.sql_packages
;
----------
[DROP_PUB_SUB] DROP PUBLICATION regress_pub1
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_uuid_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[ALTER_TABLE] ALTER TABLE "public"."rule_merge1" DISABLE RULE "rule1"
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ?<@ (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_extract_risparent
)
;
----------
[CREATE_TABLE] --
-- Tables and rules for the logging test
--
create table rtest_emp (ename char(20), salary numeric)
;
----------
[CREATE_PROG_OBJ] -- invalid: non-matching result types

CREATE FUNCTION float8mi_int(float8, float8) RETURNS int AS
$$ SELECT CAST($1 - $2 AS INT); $$
LANGUAGE SQL
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.timestamp()'))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table invalid_check_con_child add constraint inh_check_constraint check(f1 > 0) not valid
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl (a int, b int)
;
----------
[DROP_INDEX] DROP INDEX brinidx_multi
;
----------
[ADMIN_TABLE] ANALYZE public.tt6
;
----------
[ADMIN_TABLE] ANALYZE public.gtest30_1
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_proc
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- 5.8 ADMINISTRABLE_ROLE_AUTHORIZATIONS view appears later.


/*
 * 5.9
 * APPLICABLE_ROLES view
 */

CREATE VIEW applicable_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS grantee,
           CAST(b.rolname AS sql_identifier) AS role_name,
           CAST(CASE WHEN m.admin_option THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable
    FROM (SELECT member, roleid, admin_option FROM pg_auth_members
          -- This UNION could be UNION ALL, but UNION works even if we start
          -- to allow explicit pg_database_owner membership.
          UNION
          SELECT datdba, pg_authid.oid, false
          FROM pg_database, pg_authid
          WHERE datname = current_database() AND rolname = 'pg_database_owner'
         )  m
         JOIN pg_authid a ON (m.member = a.oid)
         JOIN pg_authid b ON (m.roleid = b.oid)
    WHERE pg_has_role(a.oid, 'USAGE')
;
----------
[ADD_INDEX] CREATE INDEX i_bmtest_b ON bmscantest(b)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE async_p1 PARTITION OF async_pt FOR VALUES FROM (1000) TO (2000)
  SERVER loopback OPTIONS (table_name 'base_tbl1')
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_para_p2 PARTITION OF pagg_tab_para FOR VALUES FROM (10) TO (20)
;
----------
[DROP_PROG_OBJ] drop function anyctest(anycompatiblenonarray, anycompatiblenonarray)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pred_child ALTER a SET NOT NULL
;
----------
[BLOCK] do $$
declare
  v1 int;
  v2 varchar;
  a1 v1%type[];
  a2 v2%type[];
begin
  v1 := 10;
  v2 := 'Hi';
  a1 := array[v1,v1];
  a2 := array[v2,v2];
  raise notice '% %', a1, a2;
end;
$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f3_excl EXCLUDE USING btree (f3 WITH =)
;
----------
[BLOCK] do $$ declare a int[];
begin a[1][4] := 4; raise notice 'a = %', a; end$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk ADD FOREIGN KEY (a) REFERENCES pk ON UPDATE SET DEFAULT ON DELETE SET DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."constr_parent2" (
    "a" integer
)
;
----------
[TRUNCATE_TABLE] TRUNCATE copy1
;
----------
[CREATE_TABLE] -- A table with with contents that, when sorted, triggers abbreviated
-- key aborts. One easy way to achieve that is to use uuids that all
-- have the same prefix, as abbreviated keys for uuids just use the
-- first sizeof(Datum) bytes.
CREATE TEMP TABLE abbrev_abort_uuids (
    id serial not null,
    abort_increasing uuid,
    abort_decreasing uuid,
    noabort_increasing uuid,
    noabort_decreasing uuid)
;
----------
[CREATE_TABLE] /* Has ee entry */

CREATE TABLE inhf (LIKE inhx, LIKE inhx)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH PARSER alt_ts_prs2 SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] CREATE TABLE part1 (tid integer, balance float, val text)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test2 AS (SELECT generate_series(1, 1000)::int8 AS col1)
;
----------
[CREATE_TABLE] -- fail

-- unsupported constraint type for partitioned tables
CREATE TABLE partitioned (
	a int,
	EXCLUDE USING gist (a WITH &&)
) PARTITION BY RANGE (a)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33271 RENAME TO reind_con_toast
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_mysub1 CONNECTION 'port=29713 host=/tmp/jOoI3lrZrW dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub1_slot, failover = true, create_slot = false)
;
----------
[CREATE_TABLE] -- error
CREATE TABLE gtestx (x int, b int GENERATED ALWAYS AS IDENTITY) INHERITS (gtest1)
;
----------
[CREATE_POLICY] -- user's security level must be higher than or equal to document's
CREATE POLICY p1 ON document AS PERMISSIVE
    USING (dlevel <= (SELECT seclv FROM uaccount WHERE pguser = current_user))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."func_with_set_params"() RETURNS integer
    LANGUAGE "sql" IMMUTABLE STRICT
    SET "search_path" TO 'pg_catalog'
    SET "extra_float_digits" TO '2'
    SET "work_mem" TO '4MB'
    SET "DateStyle" TO 'iso, mdy'
    SET "local_preload_libraries" TO 'Mixed/Case', 'c:/''a"/path', '', '0123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789'
    AS $$select 1;$$
;
----------
[ALTER_TABLE|ADD_COLUMN] --ALTER TABLE attmp ADD COLUMN o lock;
ALTER TABLE attmp ADD COLUMN p boolean
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON r1 AS RESTRICTIVE USING ((SELECT c) < 400)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "pubme"
;
----------
[CREATE_TABLE] CREATE TABLE vacparted (a int, b char) PARTITION BY LIST (a)
;
