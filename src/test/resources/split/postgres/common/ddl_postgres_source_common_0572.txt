DO LANGUAGE plpgsql $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT a FROM test1 FOR UPDATE LOOP
      UPDATE test1 SET b = b || ' ' || b WHERE a = r.a;
      COMMIT;
    END LOOP;
END;
$$
;
-- create a table to use as a basis for views and materialized views in various combinations
CREATE TABLE mvtest_t (id int NOT NULL PRIMARY KEY, type text NOT NULL, amt numeric NOT NULL)
;
CREATE FUNCTION hstore_to_array(hstore)
RETURNS text[]
AS '$libdir/hstore','hstore_to_array'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE plt2_p2 PARTITION OF plt2 FOR VALUES IN ('0001', '0005', '0002', '0009')
;
CREATE OPERATOR CLASS dump_test.op_class
		                 FOR TYPE bigint USING btree FAMILY dump_test.op_family
						 AS STORAGE bigint,
						 OPERATOR 1 <(bigint,bigint),
						 OPERATOR 2 <=(bigint,bigint),
						 OPERATOR 3 =(bigint,bigint),
						 OPERATOR 4 >=(bigint,bigint),
						 OPERATOR 5 >(bigint,bigint),
						 FUNCTION 1 btint8cmp(bigint,bigint),
						 FUNCTION 2 btint8sortsupport(internal),
						 FUNCTION 4 btequalimage(oid)
;
-- multiple array-column updates

CREATE TABLE base_tbl (a int, arr int[])
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 SET SCHEMA foreign_schema
;
DROP TABLE sample_tbl
;
ALTER TABLE parted_self_fk ATTACH PARTITION part2_self_fk FOR VALUES FROM (10) TO (20)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_dict
;
ALTER TABLE split_alter_table.base_table DISABLE RULE base_table_notify
;
CREATE TABLE "public"."beta_pos" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
drop function upsert_before_func()
;
DROP RULE rw_view1_upd_rule ON rw_view1
;
CREATE FUNCTION fetchval(hstore,text)
RETURNS text
AS '$libdir/hstore','hstore_fetchval'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE OR REPLACE FUNCTION
  jsonb_path_match(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                   silent boolean DEFAULT false)
RETURNS boolean
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_match'
;
DROP FUNCTION IF EXISTS foo(no_such_schema.no_such_type)
;
CREATE VIEW classv AS SELECT * FROM pg_class
;
CREATE VIEW pg_cursors AS
    SELECT * FROM pg_cursor() AS C
;
drop table trigtest2
;
-- Check materialized views
CREATE MATERIALIZED VIEW concur_reindex_matview AS SELECT * FROM concur_reindex_tab
;
-- fail, can't infer result type:
create function anyctest(anycompatible)
returns anycompatiblemultirange as $$
  select $1
$$ language sql
;
create table bytea_test_table(v bytea)
;
CREATE INDEX CONCURRENTLY tbl_gist_idx ON tbl_gist using gist (c4) INCLUDE (c1,c2,c3)
;
DO $$
	BEGIN
		REVOKE SELECT ON intra_grant_inplace FROM PUBLIC;
	EXCEPTION WHEN others THEN
		RAISE WARNING 'got: %', regexp_replace(sqlerrm, '[0-9]+', 'REDACTED');
	END
	$$
;
ALTER TABLE pg_toast.pg_toast_30500 RENAME TO reind_con_toast
;
alter table parted_irreg_ancestor attach partition parted_irreg
  for values from ('aaaa') to ('zzzz')
;
/* src/test/modules/test_bloomfilter/test_bloomfilter--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_bloomfilter(power integer,
    nelements bigint,
    seed integer DEFAULT -1,
    tests integer DEFAULT 1)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_bloomfilter' LANGUAGE C
;
CREATE TEMP TABLE temptest(col int) ON COMMIT DELETE ROWS
;
create function testpolym(out a int, inout a int) returns int as $$ select 1;$$ language sql
;
ALTER CONVERSION alt_conv2 SET SCHEMA alt_nsp2
;
CREATE TRIGGER a BEFORE UPDATE ON t FOR EACH ROW EXECUTE PROCEDURE test_trigger()
;
CREATE TABLE "generated_stored_tests"."gtest2" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULL::integer) STORED
)
;
VACUUM (BUFFER_USAGE_LIMIT 10000000000) vac_option_tab
;
CREATE UNIQUE INDEX bttest_multi_idx ON bttest_multi
USING btree (id) INCLUDE (data)
;
ALTER TABLE test_part_d_1 ADD PRIMARY KEY (a)
;
ANALYZE public.inhf
;
CREATE TABLE target_tg (key int primary key, balance integer, status text, val text)
;
DROP TRIGGER trig_row_before_insupd ON rem1
;
ALTER TABLE ONLY "fkpart5"."pk3" ATTACH PARTITION "fkpart5"."pk32" FOR VALUES IN (32)
;
CREATE TRIGGER trig_null
BEFORE INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trig_null()
;
CREATE TABLE "public"."plt2_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
-- As of Postgres 7.1, ALTER implicitly recurses,
-- so this should be same as ALTER a_star*

ALTER TABLE a_star RENAME COLUMN aa TO foo
;
CREATE FUNCTION isnlt(ismn, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE ONLY grandchild ADD CONSTRAINT a_pos CHECK (a > 0)
;
alter table atacc1 alter test_a set not null, alter test_b set not null
;
create table part_gg2 partition of part_gg for values from (1) to (10) partition by range (b)
;
create table domarrtest
           ( testint4arr domainint4arr
           , testchar4arr domainchar4arr
            )
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_type
;
-- test that sequence of half-dropped serial column is properly ignored

CREATE TABLE itest14 (id serial)
;
------------------------------------------------------------------------------------------
[BLOCK] DO LANGUAGE plpgsql $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT a FROM test1 FOR UPDATE LOOP
      UPDATE test1 SET b = b || ' ' || b WHERE a = r.a;
      COMMIT;
    END LOOP;
END;
$$
;
----------
[CREATE_TABLE] -- create a table to use as a basis for views and materialized views in various combinations
CREATE TABLE mvtest_t (id int NOT NULL PRIMARY KEY, type text NOT NULL, amt numeric NOT NULL)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_to_array(hstore)
RETURNS text[]
AS '$libdir/hstore','hstore_to_array'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE plt2_p2 PARTITION OF plt2 FOR VALUES IN ('0001', '0005', '0002', '0009')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS dump_test.op_class
		                 FOR TYPE bigint USING btree FAMILY dump_test.op_family
						 AS STORAGE bigint,
						 OPERATOR 1 <(bigint,bigint),
						 OPERATOR 2 <=(bigint,bigint),
						 OPERATOR 3 =(bigint,bigint),
						 OPERATOR 4 >=(bigint,bigint),
						 OPERATOR 5 >(bigint,bigint),
						 FUNCTION 1 btint8cmp(bigint,bigint),
						 FUNCTION 2 btint8sortsupport(internal),
						 FUNCTION 4 btequalimage(oid)
;
----------
[CREATE_TABLE] -- multiple array-column updates

CREATE TABLE base_tbl (a int, arr int[])
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 SET SCHEMA foreign_schema
;
----------
[DROP_TABLE] DROP TABLE sample_tbl
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE parted_self_fk ATTACH PARTITION part2_self_fk FOR VALUES FROM (10) TO (20)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_dict
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table DISABLE RULE base_table_notify
;
----------
[CREATE_TABLE] CREATE TABLE "public"."beta_pos" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
----------
[DROP_PROG_OBJ] drop function upsert_before_func()
;
----------
[DROP_POLICY] DROP RULE rw_view1_upd_rule ON rw_view1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fetchval(hstore,text)
RETURNS text
AS '$libdir/hstore','hstore_fetchval'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_match(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                   silent boolean DEFAULT false)
RETURNS boolean
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_match'
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS foo(no_such_schema.no_such_type)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW classv AS SELECT * FROM pg_class
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_cursors AS
    SELECT * FROM pg_cursor() AS C
;
----------
[DROP_TABLE] drop table trigtest2
;
----------
[CREATE_VIEW(SELECT)] -- Check materialized views
CREATE MATERIALIZED VIEW concur_reindex_matview AS SELECT * FROM concur_reindex_tab
;
----------
[CREATE_PROG_OBJ] -- fail, can't infer result type:
create function anyctest(anycompatible)
returns anycompatiblemultirange as $$
  select $1
$$ language sql
;
----------
[CREATE_TABLE] create table bytea_test_table(v bytea)
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY tbl_gist_idx ON tbl_gist using gist (c4) INCLUDE (c1,c2,c3)
;
----------
[BLOCK] DO $$
	BEGIN
		REVOKE SELECT ON intra_grant_inplace FROM PUBLIC;
	EXCEPTION WHEN others THEN
		RAISE WARNING 'got: %', regexp_replace(sqlerrm, '[0-9]+', 'REDACTED');
	END
	$$
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30500 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_irreg_ancestor attach partition parted_irreg
  for values from ('aaaa') to ('zzzz')
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_bloomfilter/test_bloomfilter--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_bloomfilter(power integer,
    nelements bigint,
    seed integer DEFAULT -1,
    tests integer DEFAULT 1)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_bloomfilter' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest(col int) ON COMMIT DELETE ROWS
;
----------
[CREATE_PROG_OBJ] create function testpolym(out a int, inout a int) returns int as $$ select 1;$$ language sql
;
----------
[ALTER_POLICY] ALTER CONVERSION alt_conv2 SET SCHEMA alt_nsp2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER a BEFORE UPDATE ON t FOR EACH ROW EXECUTE PROCEDURE test_trigger()
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest2" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULL::integer) STORED
)
;
----------
[ADMIN_TABLE] VACUUM (BUFFER_USAGE_LIMIT 10000000000) vac_option_tab
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX bttest_multi_idx ON bttest_multi
USING btree (id) INCLUDE (data)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE test_part_d_1 ADD PRIMARY KEY (a)
;
----------
[ADMIN_TABLE] ANALYZE public.inhf
;
----------
[CREATE_TABLE] CREATE TABLE target_tg (key int primary key, balance integer, status text, val text)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_before_insupd ON rem1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart5"."pk3" ATTACH PARTITION "fkpart5"."pk32" FOR VALUES IN (32)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_null
BEFORE INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trig_null()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt2_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[RENAME_COLUMN] -- As of Postgres 7.1, ALTER implicitly recurses,
-- so this should be same as ALTER a_star*

ALTER TABLE a_star RENAME COLUMN aa TO foo
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ismn, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY grandchild ADD CONSTRAINT a_pos CHECK (a > 0)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter test_a set not null, alter test_b set not null
;
----------
[CREATE_TABLE] create table part_gg2 partition of part_gg for values from (1) to (10) partition by range (b)
;
----------
[CREATE_TABLE] create table domarrtest
           ( testint4arr domainint4arr
           , testchar4arr domainchar4arr
            )
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_type
;
----------
[CREATE_TABLE] -- test that sequence of half-dropped serial column is properly ignored

CREATE TABLE itest14 (id serial)
;
