DO $$
BEGIN
    DELETE FROM test;
END;
$$ LANGUAGE plpgsql
;
CREATE TABLE split_prog.accounts (
    id integer PRIMARY KEY,
    owner_name text NOT NULL,
    balance numeric DEFAULT 0,
    updated_at timestamptz DEFAULT clock_timestamp()
)
;
CREATE FUNCTION page_header(IN page bytea,
    OUT lsn pg_lsn,
    OUT checksum smallint,
    OUT flags smallint,
    OUT lower int,
    OUT upper int,
    OUT special int,
    OUT pagesize int,
    OUT version smallint,
    OUT prune_xid xid)
AS '$libdir/pageinspect', 'page_header'
LANGUAGE C STRICT PARALLEL SAFE
;
-- fail - unlogged table
CREATE PUBLICATION testpub_forunloggedtbl FOR TABLE testpub_unloggedtbl
;
/*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
--
-- Deferrable unique constraints
--

CREATE TABLE unique_tbl (i int UNIQUE DEFERRABLE, t text)
;
CREATE OPERATOR CLASS evttrigopclass FOR TYPE int USING btree AS STORAGE int
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_ts_config_map
;
ALTER OPERATOR FAMILY gist__ltree_ops USING gist
ADD FUNCTION 10 (_ltree) _ltree_gist_options (internal)
;
ALTER TABLE fk_p_1_2 DROP COLUMN x, DROP COLUMN y
;
CREATE FUNCTION gin_extract_value_int4(int4, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ANALYZE pg_temp_12.pg_temp_26420_2
;
alter table parted_copytest attach partition parted_copytest_a1 for values in(1)
;
CREATE COLLATION builtin_c ( PROVIDER = builtin, LOCALE = "C" )
;
CREATE SCHEMA test_schema
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub6
;
-- ok
CREATE MATERIALIZED VIEW matview_ine_tab AS
  SELECT 1 / 0 WITH NO DATA
;
CREATE DOMAIN jsb_int_array_1d  AS int[]   CHECK(array_length(VALUE, 1) = 3)
;
DROP TABLE test_like_5, test_like_5x, test_like_5c
;
create view aggordview1 as
select ten,
       percentile_disc(0.5) within group (order by thousand) as p50,
       percentile_disc(0.5) within group (order by thousand) filter (where hundred=1) as px,
       rank(5,'AZZZZ',50) within group (order by hundred, string4 desc, hundred)
  from tenk1
 group by ten order by ten
;
-- Exercise the LP_DEAD-bit-set tuple deletion code with a posting list tuple.
-- The implementation prefers deleting existing items to merging any duplicate
-- tuples into a posting list, so we need an explicit test to make sure we get
-- coverage (note that this test also assumes BLCKSZ is 8192 or less):
DROP INDEX plain_unique
;
ALTER TABLE testpub_gencol REPLICA IDENTITY FULL
;
CREATE AGGREGATE "public"."logging_agg_nonstrict"("anyelement") (
    SFUNC = "public"."logging_sfunc_nonstrict",
    STYPE = "text",
    MSFUNC = "public"."logging_msfunc_nonstrict",
    MINVFUNC = "public"."logging_minvfunc_nonstrict",
    MSTYPE = "text"
)
;
-- stored generated columns need a rewrite
ALTER TABLE has_volatile ADD col7 int GENERATED ALWAYS AS (55) stored
;
--
-- test usage of transition tables in AFTER triggers
--

CREATE TABLE transition_table_base (id int PRIMARY KEY, val text)
;
ALTER TABLE ONLY "public"."part3_self_fk" ATTACH PARTITION "public"."part32_self_fk" FOR VALUES FROM ('20') TO ('30')
;
alter index ginidx set (fastupdate = on)
;
CREATE OPERATOR CLASS hash_ltree_ops
DEFAULT FOR TYPE ltree USING hash
AS
	OPERATOR	1	= ,
	FUNCTION	1	hash_ltree(ltree),
	FUNCTION	2	hash_ltree_extended(ltree, bigint)
;
CREATE UNIQUE INDEX ON sj_t2 USING btree (id)
;
COMMENT ON TYPE isbn
	IS 'International Standard Book Number (ISBN)'
;
ALTER TABLE pg_toast.pg_toast_27791 RENAME TO reind_con_toast
;
DROP TABLE hideblocks
;
CREATE TYPE tststats.ty AS (a int, b int, c text)
;
DROP INDEX CONCURRENTLY "concur_index5"
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_type
;
CREATE SUBSCRIPTION tap_sub_schema CONNECTION 'port=17790 host=/tmp/9EHiBg3gR4 dbname=postgres' PUBLICATION tap_pub_schema
;
do $$
declare var text := 'some value';
begin
  assert 1=0, format('assertion failed, var = "%s"', var);
end;
$$
;
CREATE TABLESPACE tblspc2 LOCATION ''
;
alter table nonesuch rename to newnonesuch
;
COMMENT ON TEXT SEARCH DICTIONARY armenian_stem IS 'snowball stemmer for armenian language'
;
-- triggers
CREATE TRIGGER t1 BEFORE INSERT ON pg_description EXECUTE FUNCTION tf1()
;
alter table trigpart detach partition trigpart4
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_statistic_ext_data
;
DROP TABLE functional_dependencies_multi
;
CREATE TEMP TABLE temp_parted (
	a int
) PARTITION BY LIST (a)
;
-- test smallserial / bigserial
CREATE TABLE serialTest2 (f1 text, f2 serial, f3 smallserial, f4 serial2,
  f5 bigserial, f6 serial8)
;
CREATE TABLESPACE ts1 LOCATION '/tmp/1h4zYiPr9x'
;
create event trigger regress_event_trigger_noperms on ddl_command_start
   execute procedure test_event_trigger()
;
CREATE TABLE fprt2_p1 (LIKE fprt2)
;
-- fails
CREATE COLLATION builtin2 ( PROVIDER = builtin, LOCALE = "en_US" )
;
------------------------------------------------------------------------------------------
[BLOCK] DO $$
BEGIN
    DELETE FROM test;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE split_prog.accounts (
    id integer PRIMARY KEY,
    owner_name text NOT NULL,
    balance numeric DEFAULT 0,
    updated_at timestamptz DEFAULT clock_timestamp()
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION page_header(IN page bytea,
    OUT lsn pg_lsn,
    OUT checksum smallint,
    OUT flags smallint,
    OUT lower int,
    OUT upper int,
    OUT special int,
    OUT pagesize int,
    OUT version smallint,
    OUT prune_xid xid)
AS '$libdir/pageinspect', 'page_header'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] -- fail - unlogged table
CREATE PUBLICATION testpub_forunloggedtbl FOR TABLE testpub_unloggedtbl
;
----------
[CREATE_POLICY] /*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
----------
[CREATE_TABLE] --
-- Deferrable unique constraints
--

CREATE TABLE unique_tbl (i int UNIQUE DEFERRABLE, t text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS evttrigopclass FOR TYPE int USING btree AS STORAGE int
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_ts_config_map
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist__ltree_ops USING gist
ADD FUNCTION 10 (_ltree) _ltree_gist_options (internal)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk_p_1_2 DROP COLUMN x, DROP COLUMN y
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_int4(int4, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26420_2
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_copytest attach partition parted_copytest_a1 for values in(1)
;
----------
[CREATE_POLICY] CREATE COLLATION builtin_c ( PROVIDER = builtin, LOCALE = "C" )
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_schema
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub6
;
----------
[CREATE_VIEW(SELECT)] -- ok
CREATE MATERIALIZED VIEW matview_ine_tab AS
  SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_TYPE] CREATE DOMAIN jsb_int_array_1d  AS int[]   CHECK(array_length(VALUE, 1) = 3)
;
----------
[DROP_TABLE] DROP TABLE test_like_5, test_like_5x, test_like_5c
;
----------
[CREATE_VIEW(SELECT)] create view aggordview1 as
select ten,
       percentile_disc(0.5) within group (order by thousand) as p50,
       percentile_disc(0.5) within group (order by thousand) filter (where hundred=1) as px,
       rank(5,'AZZZZ',50) within group (order by hundred, string4 desc, hundred)
  from tenk1
 group by ten order by ten
;
----------
[DROP_INDEX] -- Exercise the LP_DEAD-bit-set tuple deletion code with a posting list tuple.
-- The implementation prefers deleting existing items to merging any duplicate
-- tuples into a posting list, so we need an explicit test to make sure we get
-- coverage (note that this test also assumes BLCKSZ is 8192 or less):
DROP INDEX plain_unique
;
----------
[ALTER_TABLE] ALTER TABLE testpub_gencol REPLICA IDENTITY FULL
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."logging_agg_nonstrict"("anyelement") (
    SFUNC = "public"."logging_sfunc_nonstrict",
    STYPE = "text",
    MSFUNC = "public"."logging_msfunc_nonstrict",
    MINVFUNC = "public"."logging_minvfunc_nonstrict",
    MSTYPE = "text"
)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- stored generated columns need a rewrite
ALTER TABLE has_volatile ADD col7 int GENERATED ALWAYS AS (55) stored
;
----------
[CREATE_TABLE] --
-- test usage of transition tables in AFTER triggers
--

CREATE TABLE transition_table_base (id int PRIMARY KEY, val text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."part3_self_fk" ATTACH PARTITION "public"."part32_self_fk" FOR VALUES FROM ('20') TO ('30')
;
----------
[ALTER_INDEX] alter index ginidx set (fastupdate = on)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS hash_ltree_ops
DEFAULT FOR TYPE ltree USING hash
AS
	OPERATOR	1	= ,
	FUNCTION	1	hash_ltree(ltree),
	FUNCTION	2	hash_ltree_extended(ltree, bigint)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON sj_t2 USING btree (id)
;
----------
[COMMENT_TYPE] COMMENT ON TYPE isbn
	IS 'International Standard Book Number (ISBN)'
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27791 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE hideblocks
;
----------
[CREATE_TYPE] CREATE TYPE tststats.ty AS (a int, b int, c text)
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY "concur_index5"
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_type
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_schema CONNECTION 'port=17790 host=/tmp/9EHiBg3gR4 dbname=postgres' PUBLICATION tap_pub_schema
;
----------
[BLOCK] do $$
declare var text := 'some value';
begin
  assert 1=0, format('assertion failed, var = "%s"', var);
end;
$$
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc2 LOCATION ''
;
----------
[RENAME_TABLE] alter table nonesuch rename to newnonesuch
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY armenian_stem IS 'snowball stemmer for armenian language'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- triggers
CREATE TRIGGER t1 BEFORE INSERT ON pg_description EXECUTE FUNCTION tf1()
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table trigpart detach partition trigpart4
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_statistic_ext_data
;
----------
[DROP_TABLE] DROP TABLE functional_dependencies_multi
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temp_parted (
	a int
) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] -- test smallserial / bigserial
CREATE TABLE serialTest2 (f1 text, f2 serial, f3 smallserial, f4 serial2,
  f5 bigserial, f6 serial8)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/tmp/1h4zYiPr9x'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger_noperms on ddl_command_start
   execute procedure test_event_trigger()
;
----------
[CREATE_TABLE] CREATE TABLE fprt2_p1 (LIKE fprt2)
;
----------
[CREATE_POLICY] -- fails
CREATE COLLATION builtin2 ( PROVIDER = builtin, LOCALE = "en_US" )
;
