drop table cta_test
;
--
-- CREATE SEQUENCE
--

-- various error cases
CREATE SEQUENCE sequence_testx INCREMENT BY 0
;
create view vv4 as
select * from (values(1,2,3,4,5,6,7)) v(a,b,c,x,e,f,g)
union all
select * from
  tt7 full join tt8 using (x),
  tt7 tt7x full join tt8 tt8x using (x) full join tt8 tt8y using (x)
;
ALTER INDEX reloptions_test_idx SET (fillfactor=40)
;
ALTER TEXT SEARCH CONFIGURATION danish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH danish_stem
;
DROP TABLE unlogged_hash_table
;
ANALYZE public.unit
;
ALTER TABLE new_system_table RENAME TO old_system_table
;
create index on parted_copytest (b)
;
DROP TABLE fract_only
;
create function upsert_before_func()
  returns trigger language plpgsql as
$$
begin
  if (TG_OP = 'UPDATE') then
    raise warning 'before update (old): %', old.*::text;
    raise warning 'before update (new): %', new.*::text;
  elsif (TG_OP = 'INSERT') then
    raise warning 'before insert (new): %', new.*::text;
    if new.key % 2 = 0 then
      new.key := new.key + 1;
      new.color := new.color || ' trig modified';
      raise warning 'before insert (new, modified): %', new.*::text;
    end if;
  end if;
  return new;
end;
$$
;
--
-- Test removal of redundant GROUP BY columns
--

create temp table t1 (a int, b int, c int, d int, primary key (a, b))
;
create table old_table1 (col1 serial primary key)
;
create domain jsb_i_not_null as int not null
;
DROP EXTENSION test_ext_req_schema1
;
-- Only toast index is cleaned up.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = off,
    toast.vacuum_index_cleanup = yes)
;
ANALYZE public.prt2_p1
;
CREATE TABLE ints (key int primary key, val text)
;
-- Create a table that holds all the known in-core data types and leave it
-- around so as pg_upgrade is able to test their binary compatibility.
CREATE TABLE tab_core_types AS SELECT
  '(11,12)'::point,
  '(1,1),(2,2)'::line,
  '((11,11),(12,12))'::lseg,
  '((11,11),(13,13))'::box,
  '((11,12),(13,13),(14,14))'::path AS openedpath,
  '[(11,12),(13,13),(14,14)]'::path AS closedpath,
  '((11,12),(13,13),(14,14))'::polygon,
  '1,1,1'::circle,
  'today'::date,
  'now'::time,
  'now'::timestamp,
  'now'::timetz,
  'now'::timestamptz,
  '12 seconds'::interval,
  '{"reason":"because"}'::json,
  '{"when":"now"}'::jsonb,
  '$.a[*] ? (@ > 2)'::jsonpath,
  '127.0.0.1'::inet,
  '127.0.0.0/8'::cidr,
  '00:01:03:86:1c:ba'::macaddr8,
  '00:01:03:86:1c:ba'::macaddr,
  2::int2, 4::int4, 8::int8,
  4::float4, '8'::float8, pi()::numeric,
  'foo'::"char",
  'c'::bpchar,
  'abc'::varchar,
  'name'::name,
  'txt'::text,
  true::bool,
  E'\\xDEADBEEF'::bytea,
  B'10001'::bit,
  B'10001'::varbit AS varbit,
  '12.34'::money,
  'abc'::refcursor,
  '1 2'::int2vector,
  '1 2'::oidvector,
  format('%I=UC/%I', USER, USER)::aclitem AS aclitem,
  'a fat cat sat on a mat and ate a fat rat'::tsvector,
  'fat & rat'::tsquery,
  'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::uuid,
  '11'::xid8,
  'pg_class'::regclass,
  'regtype'::regtype,
  'pg_monitor'::regrole,
  'pg_class'::regclass::oid,
  '(1,1)'::tid, '2'::xid, '3'::cid,
  '10:20:10,14,15'::txid_snapshot,
  '10:20:10,14,15'::pg_snapshot,
  '16/B374D848'::pg_lsn,
  1::information_schema.cardinal_number,
  'l'::information_schema.character_data,
  'n'::information_schema.sql_identifier,
  'now'::information_schema.time_stamp,
  'YES'::information_schema.yes_or_no,
  'venus'::planets,
  'i16'::insenum,
  '(1,2)'::int4range,
  '(3,4)'::int8range,
  '(1,2)'::float8range,
  '(3,4)'::numrange,
  '(a,b)'::textrange,
  '(12.34, 56.78)'::cashrange,
  '(2020-01-02, 2021-02-03)'::daterange,
  '(2020-01-02 03:04:05, 2021-02-03 06:07:08)'::tsrange,
  '(2020-01-02 03:04:05, 2021-02-03 06:07:08)'::tstzrange,
  arrayrange(ARRAY[1,2], ARRAY[2,1])
;
CREATE TABLE "public"."rtest_view1" (
    "a" integer,
    "b" "text",
    "v" boolean
)
;
ALTER TABLE tt4 OF tt_t0
;
-- Empty-table DefineIndex()
CREATE UNIQUE INDEX u0rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  WHERE s.index_row_if(y)
;
DROP TABLE test1
;
CREATE VIEW "public"."shoe_ready" AS
 SELECT "rsh"."shoename",
    "rsh"."sh_avail",
    "rsl"."sl_name",
    "rsl"."sl_avail",
    "int4smaller"("rsh"."sh_avail", "rsl"."sl_avail") AS "total_avail"
   FROM "public"."shoe" "rsh",
    "public"."shoelace" "rsl"
  WHERE (("rsl"."sl_color" = "rsh"."slcolor") AND ("rsl"."sl_len_cm" >= "rsh"."slminlen_cm") AND ("rsl"."sl_len_cm" <= "rsh"."slmaxlen_cm"))
;
alter schema abc rename to aac
;
CREATE TRIGGER gtest2a BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (NEW.b < 0)  
  EXECUTE PROCEDURE gtest_trigger_func()
;
CREATE TRIGGER "transition_table_level2_ri_child_upd_trigger" AFTER UPDATE ON "public"."transition_table_level2" REFERENCING NEW TABLE AS "i" FOR EACH STATEMENT EXECUTE FUNCTION "public"."transition_table_level2_ri_child_insupd_func"()
;
ANALYZE pg_temp_4.pg_temp_29544_2
;
alter table atacc1 alter "........pg.dropped.1........" set default 3
;
-- error
CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION PUBLIC
;
CREATE TEMP TABLE test_json (
       json_type text,
       test_json json
)
;
ALTER OPERATOR ~ (ltree, lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
-- One table on schema but no indexes
DROP INDEX concur_reindex_schema.ind
;
create or replace function compos() returns int as $$
declare
  v compostype;
begin
  v := (1, 'hello');
  return v;
end;
$$ language plpgsql
;
ALTER EXTENSION test_pg_dump DROP TYPE test_pg_dump_e1
;
-- should fail
alter table fkpart1.fk_part detach partition fkpart1.fk_part_1
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_constraint
;
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=110)
;
ANALYZE public.regression_vacuumdb_test
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
DROP RULE gtest_rule_upd ON gtest_rule
;
create table parted_sample_1 partition of parted_sample for values in (1)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest_normal_child2" INHERIT "public"."gtest_normal"
;
-- AUTOCOMMIT

CREATE TABLE ac_test (a int)
;
create table boolrangep_null partition of boolrangep default
;
create domain orderedpair as int[2] check (value[1] < value[2])
;
-- Case 2. REPLICA IDENTITY FULL
ALTER TABLE rf_tbl_abcd_pk REPLICA IDENTITY FULL
;
ALTER TABLE vactst ALTER COLUMN t SET STORAGE EXTERNAL
;
-- Check the case where attnos of the partitioning columns in the table being
-- attached differs from the parent.  It should not affect the constraint-
-- checking logic that allows to skip the scan.
CREATE TABLE part_6 (
	c int,
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 6)
)
;
create operator = (
    procedure = int8alias1eq,
    leftarg = int8alias1, rightarg = int8alias1,
    commutator = =,
    restrict = eqsel, join = eqjoinsel,
    merges
)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] drop table cta_test
;
----------
[CREATE_SEQUENCE] --
-- CREATE SEQUENCE
--

-- various error cases
CREATE SEQUENCE sequence_testx INCREMENT BY 0
;
----------
[CREATE_VIEW(SELECT)] create view vv4 as
select * from (values(1,2,3,4,5,6,7)) v(a,b,c,x,e,f,g)
union all
select * from
  tt7 full join tt8 using (x),
  tt7 tt7x full join tt8 tt8x using (x) full join tt8 tt8y using (x)
;
----------
[ALTER_INDEX] ALTER INDEX reloptions_test_idx SET (fillfactor=40)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION danish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH danish_stem
;
----------
[DROP_TABLE] DROP TABLE unlogged_hash_table
;
----------
[ADMIN_TABLE] ANALYZE public.unit
;
----------
[RENAME_TABLE] ALTER TABLE new_system_table RENAME TO old_system_table
;
----------
[ADD_INDEX] create index on parted_copytest (b)
;
----------
[DROP_TABLE] DROP TABLE fract_only
;
----------
[CREATE_PROG_OBJ] create function upsert_before_func()
  returns trigger language plpgsql as
$$
begin
  if (TG_OP = 'UPDATE') then
    raise warning 'before update (old): %', old.*::text;
    raise warning 'before update (new): %', new.*::text;
  elsif (TG_OP = 'INSERT') then
    raise warning 'before insert (new): %', new.*::text;
    if new.key % 2 = 0 then
      new.key := new.key + 1;
      new.color := new.color || ' trig modified';
      raise warning 'before insert (new, modified): %', new.*::text;
    end if;
  end if;
  return new;
end;
$$
;
----------
[CREATE_TABLE] --
-- Test removal of redundant GROUP BY columns
--

create temp table t1 (a int, b int, c int, d int, primary key (a, b))
;
----------
[CREATE_TABLE] create table old_table1 (col1 serial primary key)
;
----------
[CREATE_TYPE] create domain jsb_i_not_null as int not null
;
----------
[DROP_LIBRARY] DROP EXTENSION test_ext_req_schema1
;
----------
[ALTER_TABLE] -- Only toast index is cleaned up.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = off,
    toast.vacuum_index_cleanup = yes)
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_p1
;
----------
[CREATE_TABLE] CREATE TABLE ints (key int primary key, val text)
;
----------
[CREATE_TABLE|SELECT] -- Create a table that holds all the known in-core data types and leave it
-- around so as pg_upgrade is able to test their binary compatibility.
CREATE TABLE tab_core_types AS SELECT
  '(11,12)'::point,
  '(1,1),(2,2)'::line,
  '((11,11),(12,12))'::lseg,
  '((11,11),(13,13))'::box,
  '((11,12),(13,13),(14,14))'::path AS openedpath,
  '[(11,12),(13,13),(14,14)]'::path AS closedpath,
  '((11,12),(13,13),(14,14))'::polygon,
  '1,1,1'::circle,
  'today'::date,
  'now'::time,
  'now'::timestamp,
  'now'::timetz,
  'now'::timestamptz,
  '12 seconds'::interval,
  '{"reason":"because"}'::json,
  '{"when":"now"}'::jsonb,
  '$.a[*] ? (@ > 2)'::jsonpath,
  '127.0.0.1'::inet,
  '127.0.0.0/8'::cidr,
  '00:01:03:86:1c:ba'::macaddr8,
  '00:01:03:86:1c:ba'::macaddr,
  2::int2, 4::int4, 8::int8,
  4::float4, '8'::float8, pi()::numeric,
  'foo'::"char",
  'c'::bpchar,
  'abc'::varchar,
  'name'::name,
  'txt'::text,
  true::bool,
  E'\\xDEADBEEF'::bytea,
  B'10001'::bit,
  B'10001'::varbit AS varbit,
  '12.34'::money,
  'abc'::refcursor,
  '1 2'::int2vector,
  '1 2'::oidvector,
  format('%I=UC/%I', USER, USER)::aclitem AS aclitem,
  'a fat cat sat on a mat and ate a fat rat'::tsvector,
  'fat & rat'::tsquery,
  'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::uuid,
  '11'::xid8,
  'pg_class'::regclass,
  'regtype'::regtype,
  'pg_monitor'::regrole,
  'pg_class'::regclass::oid,
  '(1,1)'::tid, '2'::xid, '3'::cid,
  '10:20:10,14,15'::txid_snapshot,
  '10:20:10,14,15'::pg_snapshot,
  '16/B374D848'::pg_lsn,
  1::information_schema.cardinal_number,
  'l'::information_schema.character_data,
  'n'::information_schema.sql_identifier,
  'now'::information_schema.time_stamp,
  'YES'::information_schema.yes_or_no,
  'venus'::planets,
  'i16'::insenum,
  '(1,2)'::int4range,
  '(3,4)'::int8range,
  '(1,2)'::float8range,
  '(3,4)'::numrange,
  '(a,b)'::textrange,
  '(12.34, 56.78)'::cashrange,
  '(2020-01-02, 2021-02-03)'::daterange,
  '(2020-01-02 03:04:05, 2021-02-03 06:07:08)'::tsrange,
  '(2020-01-02 03:04:05, 2021-02-03 06:07:08)'::tstzrange,
  arrayrange(ARRAY[1,2], ARRAY[2,1])
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_view1" (
    "a" integer,
    "b" "text",
    "v" boolean
)
;
----------
[ALTER_TABLE] ALTER TABLE tt4 OF tt_t0
;
----------
[ADD_INDEX] -- Empty-table DefineIndex()
CREATE UNIQUE INDEX u0rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  WHERE s.index_row_if(y)
;
----------
[DROP_TABLE] DROP TABLE test1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."shoe_ready" AS
 SELECT "rsh"."shoename",
    "rsh"."sh_avail",
    "rsl"."sl_name",
    "rsl"."sl_avail",
    "int4smaller"("rsh"."sh_avail", "rsl"."sl_avail") AS "total_avail"
   FROM "public"."shoe" "rsh",
    "public"."shoelace" "rsl"
  WHERE (("rsl"."sl_color" = "rsh"."slcolor") AND ("rsl"."sl_len_cm" >= "rsh"."slminlen_cm") AND ("rsl"."sl_len_cm" <= "rsh"."slmaxlen_cm"))
;
----------
[RENAME_SCHEMA] alter schema abc rename to aac
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest2a BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (NEW.b < 0)  
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "transition_table_level2_ri_child_upd_trigger" AFTER UPDATE ON "public"."transition_table_level2" REFERENCING NEW TABLE AS "i" FOR EACH STATEMENT EXECUTE FUNCTION "public"."transition_table_level2_ri_child_insupd_func"()
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_29544_2
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter "........pg.dropped.1........" set default 3
;
----------
[CREATE_SCHEMA] -- error
CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION PUBLIC
;
----------
[CREATE_TABLE] CREATE TEMP TABLE test_json (
       json_type text,
       test_json json
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ~ (ltree, lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_INDEX] -- One table on schema but no indexes
DROP INDEX concur_reindex_schema.ind
;
----------
[CREATE_PROG_OBJ] create or replace function compos() returns int as $$
declare
  v compostype;
begin
  v := (1, 'hello');
  return v;
end;
$$ language plpgsql
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP TYPE test_pg_dump_e1
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- should fail
alter table fkpart1.fk_part detach partition fkpart1.fk_part_1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_constraint
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=110)
;
----------
[ADMIN_TABLE] ANALYZE public.regression_vacuumdb_test
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[DROP_POLICY] DROP RULE gtest_rule_upd ON gtest_rule
;
----------
[CREATE_TABLE] create table parted_sample_1 partition of parted_sample for values in (1)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest_normal_child2" INHERIT "public"."gtest_normal"
;
----------
[CREATE_TABLE] -- AUTOCOMMIT

CREATE TABLE ac_test (a int)
;
----------
[CREATE_TABLE] create table boolrangep_null partition of boolrangep default
;
----------
[CREATE_TYPE] create domain orderedpair as int[2] check (value[1] < value[2])
;
----------
[ALTER_TABLE] -- Case 2. REPLICA IDENTITY FULL
ALTER TABLE rf_tbl_abcd_pk REPLICA IDENTITY FULL
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE vactst ALTER COLUMN t SET STORAGE EXTERNAL
;
----------
[CREATE_TABLE] -- Check the case where attnos of the partitioning columns in the table being
-- attached differs from the parent.  It should not affect the constraint-
-- checking logic that allows to skip the scan.
CREATE TABLE part_6 (
	c int,
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 6)
)
;
----------
[CREATE_PROG_OBJ] create operator = (
    procedure = int8alias1eq,
    leftarg = int8alias1, rightarg = int8alias1,
    commutator = =,
    restrict = eqsel, join = eqjoinsel,
    merges
)
;
