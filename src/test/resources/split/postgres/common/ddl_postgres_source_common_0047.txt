CREATE DOMAIN "public"."spgist_text" AS character varying
;
-- Simple check that ceil(), floor(), and round() work correctly
CREATE TABLE ceil_floor_round (a numeric)
;
ALTER TABLE spc_nest SET TABLESPACE pg_default
;
CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (retset(a))
;
ALTER TABLE test_part_a_1 REPLICA IDENTITY USING INDEX test_part_a_1_pkey
;
create table asptab0 partition of asptab for values from (0) to (1)
;
CREATE POLICY p3 ON y2 USING (a % 4 = 0)
;
CREATE VIEW tid_view_fake_ctid AS SELECT 1 AS ctid, 2 AS a
;
DROP TABLE tbspace_reindex_part
;
-- test child table with inherited columns and
-- with explicitly specified not null constraints
create table inh_parent_1(f1 int)
;
-- let's mix up which columns reference which
create table fktable(ftest1 int, ftest2 inet, foreign key(ftest2, ftest1) references pktable)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."test_type_diff2_c2" INHERIT "public"."test_type_diff2"
;
CREATE TABLE "public"."int2_tbl" (
    "f1" smallint
)
;
CREATE TABLE FKTABLE_FAIL1 (ftest1 int REFERENCES pktable(ptest1))
;
-- Ditto if there is some distance between the PKs (subpartitioning)
create table idxpart (a int, b int, primary key (a)) partition by range (a)
;
--error
ALTER TABLE pp_nn_1 VALIDATE CONSTRAINT nn1
;
DROP TABLE plt2_adv
;
-- Check index behavior for polygons

CREATE INDEX gpolygonind ON polygon_tbl USING gist (f1)
;
CREATE TYPE city_budget (
   internallength = 16,
   input = int44in,
   output = int44out,
   element = int4,
   category = 'x',   
   preferred = true  
)
;
ALTER TABLE regtest_table ALTER b SET STORAGE PLAIN
;
CREATE OR REPLACE TEMP VIEW v_window AS
	SELECT i, sum(i) over (order by i rows between 1 preceding and 1 following
	exclude current row) as sum_rows FROM generate_series(1, 10) i
;
CREATE TABLE temp2 (a int)
;
CREATE TABLE mytable (a int, b text)
;
DROP FUNCTION IF EXISTS PLUS_ONE(INTEGER)
;
-- a child with a different column order
create table child2 (b int, a text)
;
ANALYZE public.tableam_parted_d_heap2
;
ALTER FOREIGN TABLE ft1 ADD c11 integer
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_type
;
CREATE INDEX macaddr8_data_hash ON macaddr8_data USING hash (b)
;
ALTER TABLE has_volatile ADD col3 timestamptz DEFAULT current_timestamp
;
-- OK
SECURITY LABEL ON ROLE regress_dummy_seclabel_user1 IS '...invalid label...'
;
ALTER TEXT SEARCH DICTIONARY intdict (MAXLEN = -214783648)
;
DO $$
BEGIN
 SET effective_io_concurrency = 50;
EXCEPTION WHEN invalid_parameter_value THEN
END $$
;
ALTER TABLE dropped_cols DROP COLUMN b_drop
;
drop table perform_test
;
CREATE FUNCTION leak2(integer,integer) RETURNS boolean
  AS $$begin raise notice 'leak % %', $1, $2; return $1 > $2; end$$
  LANGUAGE plpgsql immutable
;
CREATE TABLE pitest1_p1_like (LIKE pitest1_p1 INCLUDING IDENTITY)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_aggregate
;
COMMENT ON TEXT SEARCH DICTIONARY hungarian_stem IS 'snowball stemmer for hungarian language'
;
CREATE PUBLICATION pub1 FOR TABLE tab_default
;
CREATE DOMAIN gtestdomainnn AS int CHECK (VALUE IS NOT NULL)
;
CREATE FUNCTION gin_compare_prefix_timestamptz(timestamptz, timestamptz, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE TEMP TABLE a (id int PRIMARY KEY, b_id int)
;
create table like_op_noprune2 partition of like_op_noprune for values in ('BCD')
;
-- test Table Rewrite Event Trigger
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE EXCEPTION 'rewrites not allowed';
END;
$$
;
drop table ��ٸ����
;
create table at_tab1 (a int, b text)
;
CREATE UNIQUE INDEX "pline_name" ON "public"."pline" USING "btree" ("slotname")
;
CREATE PROCEDURE addr_nsp.proc(int4) LANGUAGE SQL AS $$ $$
;
CREATE TABLE tab1 (a int)
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] CREATE DOMAIN "public"."spgist_text" AS character varying
;
----------
[CREATE_TABLE] -- Simple check that ceil(), floor(), and round() work correctly
CREATE TABLE ceil_floor_round (a numeric)
;
----------
[ALTER_TABLE] ALTER TABLE spc_nest SET TABLESPACE pg_default
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (retset(a))
;
----------
[ALTER_TABLE] ALTER TABLE test_part_a_1 REPLICA IDENTITY USING INDEX test_part_a_1_pkey
;
----------
[CREATE_TABLE] create table asptab0 partition of asptab for values from (0) to (1)
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON y2 USING (a % 4 = 0)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW tid_view_fake_ctid AS SELECT 1 AS ctid, 2 AS a
;
----------
[DROP_TABLE] DROP TABLE tbspace_reindex_part
;
----------
[CREATE_TABLE] -- test child table with inherited columns and
-- with explicitly specified not null constraints
create table inh_parent_1(f1 int)
;
----------
[CREATE_TABLE] -- let's mix up which columns reference which
create table fktable(ftest1 int, ftest2 inet, foreign key(ftest2, ftest1) references pktable)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."test_type_diff2_c2" INHERIT "public"."test_type_diff2"
;
----------
[CREATE_TABLE] CREATE TABLE "public"."int2_tbl" (
    "f1" smallint
)
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE_FAIL1 (ftest1 int REFERENCES pktable(ptest1))
;
----------
[CREATE_TABLE] -- Ditto if there is some distance between the PKs (subpartitioning)
create table idxpart (a int, b int, primary key (a)) partition by range (a)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] --error
ALTER TABLE pp_nn_1 VALIDATE CONSTRAINT nn1
;
----------
[DROP_TABLE] DROP TABLE plt2_adv
;
----------
[ADD_INDEX] -- Check index behavior for polygons

CREATE INDEX gpolygonind ON polygon_tbl USING gist (f1)
;
----------
[CREATE_TYPE] CREATE TYPE city_budget (
   internallength = 16,
   input = int44in,
   output = int44out,
   element = int4,
   category = 'x',   
   preferred = true  
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_table ALTER b SET STORAGE PLAIN
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE TEMP VIEW v_window AS
	SELECT i, sum(i) over (order by i rows between 1 preceding and 1 following
	exclude current row) as sum_rows FROM generate_series(1, 10) i
;
----------
[CREATE_TABLE] CREATE TABLE temp2 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE mytable (a int, b text)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS PLUS_ONE(INTEGER)
;
----------
[CREATE_TABLE] -- a child with a different column order
create table child2 (b int, a text)
;
----------
[ADMIN_TABLE] ANALYZE public.tableam_parted_d_heap2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD c11 integer
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_type
;
----------
[ADD_INDEX] CREATE INDEX macaddr8_data_hash ON macaddr8_data USING hash (b)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE has_volatile ADD col3 timestamptz DEFAULT current_timestamp
;
----------
[SECURITY_LABEL] -- OK
SECURITY LABEL ON ROLE regress_dummy_seclabel_user1 IS '...invalid label...'
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY intdict (MAXLEN = -214783648)
;
----------
[BLOCK] DO $$
BEGIN
 SET effective_io_concurrency = 50;
EXCEPTION WHEN invalid_parameter_value THEN
END $$
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE dropped_cols DROP COLUMN b_drop
;
----------
[DROP_TABLE] drop table perform_test
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION leak2(integer,integer) RETURNS boolean
  AS $$begin raise notice 'leak % %', $1, $2; return $1 > $2; end$$
  LANGUAGE plpgsql immutable
;
----------
[CREATE_TABLE] CREATE TABLE pitest1_p1_like (LIKE pitest1_p1 INCLUDING IDENTITY)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_aggregate
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY hungarian_stem IS 'snowball stemmer for hungarian language'
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub1 FOR TABLE tab_default
;
----------
[CREATE_TYPE] CREATE DOMAIN gtestdomainnn AS int CHECK (VALUE IS NOT NULL)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_timestamptz(timestamptz, timestamptz, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE a (id int PRIMARY KEY, b_id int)
;
----------
[CREATE_TABLE] create table like_op_noprune2 partition of like_op_noprune for values in ('BCD')
;
----------
[CREATE_PROG_OBJ] -- test Table Rewrite Event Trigger
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE EXCEPTION 'rewrites not allowed';
END;
$$
;
----------
[DROP_TABLE] drop table ��ٸ����
;
----------
[CREATE_TABLE] create table at_tab1 (a int, b text)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "pline_name" ON "public"."pline" USING "btree" ("slotname")
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE addr_nsp.proc(int4) LANGUAGE SQL AS $$ $$
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (a int)
;
