TRUNCATE pg_catalog.pg_largeobject
;
ALTER TABLE pg_temp_22.pg_temp_28002_2 ADD COLUMN newdata pg_temp_22.pg_temp_28002
;
-- MCV lists
CREATE TABLE mcv_lists (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b VARCHAR,
    filler3 DATE,
    c INT,
    d TEXT,
    ia INT[]
)
WITH (autovacuum_enabled = off)
;
-- check it works with OUT parameters, too

CREATE FUNCTION crosstab_out(text,
	OUT rowid text, OUT att1 text, OUT att2 text, OUT att3 text)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
VACUUM (PARALLEL) pvactst
;
CREATE PUBLICATION testpub2 FOR TABLE tab1
;
ALTER TABLE ctl_stats3_parent DROP COLUMN b
;
CREATE FUNCTION y_trigger() RETURNS trigger AS $$
begin
  raise notice 'y_trigger: a = %', new.a;
  return new;
end;
$$ LANGUAGE plpgsql
;
CREATE TABLE copy_t (a integer, b text)
;
CREATE INDEX test1_a_idx ON test1 USING brin (a)
;
ANALYZE public.tableam_tbl_heap2
;
CREATE TABLE btree_test_expr (n int)
;
VACUUM (FULL) pg_catalog.pg_proc
;
-- on the partition key
create table parted_collate_must_match1 partition of parted_collate_must_match
  (a collate "POSIX") for values from ('a') to ('m')
;
CREATE TABLE "public"."testjsonb" (
    "j" "jsonb"
)
;
-- test some errors
CREATE EXTENSION test_ext1
;
ALTER TABLE pg_seclabel ADD PRIMARY KEY USING INDEX pg_seclabel_object_index
;
CREATE TABLE "public"."beta_pos_p3" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
-- information_schema tests

CREATE FUNCTION functest_IS_1(a int, b int default 1, c text default 'foo')
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT $1 + $2'
;
DROP DOMAIN js_int_not_null
;
create table spgist_text_tbl(id int4, t text)
;
VACUUM vac_tab_auto
;
CREATE TABLE IF NOT EXISTS ext_cine_tab1 (x int)
;
CREATE TABLE lr_test(data text)
;
CREATE TABLE tab_part2_1 PARTITION OF tab_part2 FOR VALUES FROM (5) TO (10)
;
DROP TABLE logged1
;
drop function sp_add_user(text)
;
ANALYZE attmp
;
-- constant
CREATE DOMAIN domain2 AS int DEFAULT 10 + (random() * 10)::int
;
CREATE PUBLICATION regress_mypub FOR ALL TABLES
;
drop table testtable_apple
;
alter table atacc1 add constraint atacc_test1 unique (test1)
;
CREATE FOREIGN TABLE fd_pt2_1 PARTITION OF fd_pt2 FOR VALUES IN (1)
  SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
--
-- Redeclare built-in functions that need default values attached to their
-- arguments.  It's impractical to set those up directly in pg_proc.h because
-- of the complexity and platform-dependency of the expression tree
-- representation.  (Note that internal functions still have to have entries
-- in pg_proc.h; we are merely causing their proargnames and proargdefaults
-- to get filled in.)
--

CREATE OR REPLACE FUNCTION
  pg_start_backup(label text, fast boolean DEFAULT false, exclusive boolean DEFAULT true)
  RETURNS pg_lsn STRICT VOLATILE LANGUAGE internal AS 'pg_start_backup'
  PARALLEL RESTRICTED
;
-- ordinary member tables with serial columns
create table ext7_table1 (col1 serial primary key)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
create function anyctest(anycompatible, anycompatiblearray)
returns anycompatiblearray as $$
  select array[$1] || $2
$$ language sql
;
create table itrtest (a int, b text) partition by list (a)
;
-- the same, but return only synonyms
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=false, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=false)
;
DROP TABLE follower
;
create table mcrparted1 partition of mcrparted for values from (2, 1, minvalue) to (10, 5, 10)
;
DROP TABLE unparted
;
CREATE FUNCTION gbt_decompress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
COMMENT ON CONSTRAINT the_constraint ON constraint_comments_tbl IS 'yes, the comment'
;
CREATE RULE "r7" AS
    ON DELETE TO "public"."rules_src" DO INSTEAD  WITH "wins" AS (
         INSERT INTO "public"."int4_tbl" AS "trgt_1" ("f1")
          VALUES (0)
          RETURNING "trgt_1"."f1"
        ), "wupd" AS (
         UPDATE "public"."int4_tbl" "trgt_1" SET "f1" = ("trgt_1"."f1" + 1)
          RETURNING "trgt_1"."f1"
        ), "wdel" AS (
         DELETE FROM "public"."int4_tbl" "trgt_1"
          WHERE ("trgt_1"."f1" = 0)
          RETURNING "trgt_1"."f1"
        )
 INSERT INTO "public"."rules_log" AS "trgt" ("f1", "f2")  SELECT "old"."f1",
            "old"."f2"
           FROM "wins",
            "wupd",
            "wdel"
  RETURNING "trgt"."f1",
    "trgt"."f2"
;
CREATE FUNCTION spgist_name_choose(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS '$libdir/spgist_name_ops' LANGUAGE C
;
TRUNCATE TABLE trunc_c,trunc_d,trunc_e,truncate_a,trunc_b
;
create table idxpart (a int, b int) partition by range (a)
;
--
-- Test updates to rows during firing of BEFORE ROW triggers.
-- As of 9.2, such cases should be rejected (see bug #6123).
--

create temp table parent (
    aid int not null primary key,
    val1 text,
    val2 text,
    val3 text,
    val4 text,
    bcnt int not null default 0)
;
CREATE SUBSCRIPTION tap_sub_B
	CONNECTION 'port=18729 host=/tmp/sK49zD6iE3 dbname=postgres application_name=tap_sub_B'
	PUBLICATION tap_pub_A
	WITH (two_phase = on)
;
------------------------------------------------------------------------------------------
[TRUNCATE_TABLE] TRUNCATE pg_catalog.pg_largeobject
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_22.pg_temp_28002_2 ADD COLUMN newdata pg_temp_22.pg_temp_28002
;
----------
[CREATE_TABLE] -- MCV lists
CREATE TABLE mcv_lists (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b VARCHAR,
    filler3 DATE,
    c INT,
    d TEXT,
    ia INT[]
)
WITH (autovacuum_enabled = off)
;
----------
[CREATE_PROG_OBJ] -- check it works with OUT parameters, too

CREATE FUNCTION crosstab_out(text,
	OUT rowid text, OUT att1 text, OUT att2 text, OUT att3 text)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL) pvactst
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub2 FOR TABLE tab1
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ctl_stats3_parent DROP COLUMN b
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION y_trigger() RETURNS trigger AS $$
begin
  raise notice 'y_trigger: a = %', new.a;
  return new;
end;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE copy_t (a integer, b text)
;
----------
[ADD_INDEX] CREATE INDEX test1_a_idx ON test1 USING brin (a)
;
----------
[ADMIN_TABLE] ANALYZE public.tableam_tbl_heap2
;
----------
[CREATE_TABLE] CREATE TABLE btree_test_expr (n int)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_proc
;
----------
[CREATE_TABLE] -- on the partition key
create table parted_collate_must_match1 partition of parted_collate_must_match
  (a collate "POSIX") for values from ('a') to ('m')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."testjsonb" (
    "j" "jsonb"
)
;
----------
[CREATE_LIBRARY] -- test some errors
CREATE EXTENSION test_ext1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_seclabel ADD PRIMARY KEY USING INDEX pg_seclabel_object_index
;
----------
[CREATE_TABLE] CREATE TABLE "public"."beta_pos_p3" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_PROG_OBJ] -- information_schema tests

CREATE FUNCTION functest_IS_1(a int, b int default 1, c text default 'foo')
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT $1 + $2'
;
----------
[DROP_TYPE] DROP DOMAIN js_int_not_null
;
----------
[CREATE_TABLE] create table spgist_text_tbl(id int4, t text)
;
----------
[ADMIN_TABLE] VACUUM vac_tab_auto
;
----------
[CREATE_TABLE] CREATE TABLE IF NOT EXISTS ext_cine_tab1 (x int)
;
----------
[CREATE_TABLE] CREATE TABLE lr_test(data text)
;
----------
[CREATE_TABLE] CREATE TABLE tab_part2_1 PARTITION OF tab_part2 FOR VALUES FROM (5) TO (10)
;
----------
[DROP_TABLE] DROP TABLE logged1
;
----------
[DROP_PROG_OBJ] drop function sp_add_user(text)
;
----------
[ADMIN_TABLE] ANALYZE attmp
;
----------
[CREATE_TYPE] -- constant
CREATE DOMAIN domain2 AS int DEFAULT 10 + (random() * 10)::int
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION regress_mypub FOR ALL TABLES
;
----------
[DROP_TABLE] drop table testtable_apple
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_test1 unique (test1)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE fd_pt2_1 PARTITION OF fd_pt2 FOR VALUES IN (1)
  SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[CREATE_PROG_OBJ] --
-- Redeclare built-in functions that need default values attached to their
-- arguments.  It's impractical to set those up directly in pg_proc.h because
-- of the complexity and platform-dependency of the expression tree
-- representation.  (Note that internal functions still have to have entries
-- in pg_proc.h; we are merely causing their proargnames and proargdefaults
-- to get filled in.)
--

CREATE OR REPLACE FUNCTION
  pg_start_backup(label text, fast boolean DEFAULT false, exclusive boolean DEFAULT true)
  RETURNS pg_lsn STRICT VOLATILE LANGUAGE internal AS 'pg_start_backup'
  PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] -- ordinary member tables with serial columns
create table ext7_table1 (col1 serial primary key)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
----------
[CREATE_PROG_OBJ] create function anyctest(anycompatible, anycompatiblearray)
returns anycompatiblearray as $$
  select array[$1] || $2
$$ language sql
;
----------
[CREATE_TABLE] create table itrtest (a int, b text) partition by list (a)
;
----------
[ALTER_POLICY] -- the same, but return only synonyms
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=false, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=false)
;
----------
[DROP_TABLE] DROP TABLE follower
;
----------
[CREATE_TABLE] create table mcrparted1 partition of mcrparted for values from (2, 1, minvalue) to (10, 5, 10)
;
----------
[DROP_TABLE] DROP TABLE unparted
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_decompress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT the_constraint ON constraint_comments_tbl IS 'yes, the comment'
;
----------
[CREATE_POLICY] CREATE RULE "r7" AS
    ON DELETE TO "public"."rules_src" DO INSTEAD  WITH "wins" AS (
         INSERT INTO "public"."int4_tbl" AS "trgt_1" ("f1")
          VALUES (0)
          RETURNING "trgt_1"."f1"
        ), "wupd" AS (
         UPDATE "public"."int4_tbl" "trgt_1" SET "f1" = ("trgt_1"."f1" + 1)
          RETURNING "trgt_1"."f1"
        ), "wdel" AS (
         DELETE FROM "public"."int4_tbl" "trgt_1"
          WHERE ("trgt_1"."f1" = 0)
          RETURNING "trgt_1"."f1"
        )
 INSERT INTO "public"."rules_log" AS "trgt" ("f1", "f2")  SELECT "old"."f1",
            "old"."f2"
           FROM "wins",
            "wupd",
            "wdel"
  RETURNING "trgt"."f1",
    "trgt"."f2"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION spgist_name_choose(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS '$libdir/spgist_name_ops' LANGUAGE C
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE trunc_c,trunc_d,trunc_e,truncate_a,trunc_b
;
----------
[CREATE_TABLE] create table idxpart (a int, b int) partition by range (a)
;
----------
[CREATE_TABLE] --
-- Test updates to rows during firing of BEFORE ROW triggers.
-- As of 9.2, such cases should be rejected (see bug #6123).
--

create temp table parent (
    aid int not null primary key,
    val1 text,
    val2 text,
    val3 text,
    val4 text,
    bcnt int not null default 0)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B
	CONNECTION 'port=18729 host=/tmp/sK49zD6iE3 dbname=postgres application_name=tap_sub_B'
	PUBLICATION tap_pub_A
	WITH (two_phase = on)
;
