--
-- Check DO INSTEAD rules with ON CONFLICT
--
CREATE TABLE hats (
	hat_name    char(10) primary key,
	hat_color   char(10)      -- hat color
)
;
alter table wide set (parallel_workers = 2)
;
CREATE MATERIALIZED VIEW mvtest_mv_v (ii, jj, kk) AS SELECT i, j FROM mvtest_v
;
CREATE SCHEMA split_returning18
;
CREATE COLLATION builtin2 ( PROVIDER = builtin, LOCALE = "en_US" )
;
-- "limit" is to prevent this from being inlined
create function sql_recurse(float8) returns float8 as
$$ select recurse($1) limit 1; $$ language sql
;
ALTER POLICY dep_p1 ON dep1 TO regress_rls_bob,regress_rls_carol
;
DROP PUBLICATION testpub1
;
CREATE TABLE gtestm (
  id int PRIMARY KEY,
  f1 int,
  f2 int,
  f3 int GENERATED ALWAYS AS (f1 * 2) STORED,
  f4 int GENERATED ALWAYS AS (f2 * 2) STORED
)
;
VACUUM part1, part2
;
create rule r3 as on insert to rules_src do also
  insert into rules_log values(null, null, '-', default), (new.*, 'new', default)
;
-- failed (no role membership)
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf3 SET SCHEMA alt_nsp2
;
CREATE INDEX timestampidx ON timestamptmp USING gist ( a )
;
CREATE TEMPORARY TABLE test_io_local(a int, b TEXT)
;
CREATE FUNCTION reindex_end_command()
RETURNS event_trigger AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'REINDEX END: command_tag=% type=% identity=%',
	    obj.command_tag, obj.object_type, obj.object_identity;
    END LOOP;
END;
$$ LANGUAGE plpgsql
;
CREATE VIEW "public"."domview" AS
 SELECT ("col1")::"public"."dom" AS "col1"
   FROM "public"."domtab"
;
CREATE TEMP TABLE testeoc (a text)
;
create table ec1 (ff int8 primary key, f1 int8alias1, f2 int8alias2)
;
CREATE FOREIGN TABLE ft_empty (c1 int NOT NULL, c2 text)
  SERVER loopback OPTIONS (table_name 'loct_empty')
;
CREATE FUNCTION "public"."list_partitioned_table"() RETURNS SETOF integer
    LANGUAGE "plpgsql"
    AS $$
DECLARE
    row public.partitioned_table%ROWTYPE;
    a_val public.partitioned_table.a%TYPE;
BEGIN
    FOR row IN SELECT * FROM public.partitioned_table ORDER BY a LOOP
        a_val := row.a;
        RETURN NEXT a_val;
    END LOOP;
    RETURN;
END; $$
;
-- Parallel Append that runs serially
create function sp_test_func() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar $$
language sql stable
;
ANALYZE public.interval_tbl
;
CREATE INDEX idx_postgis_places_geom_gist ON split_postgis.places USING gist (geom)
;
ALTER INDEX dummy_test_idx SET (option_enum = 'three')
;
COMMENT ON CONSTRAINT "constr_parent2_a_not_null" ON "public"."constr_child2" IS 'this constraint is valid'
;
DROP TABLE pg_temp_3.pg_temp_27606_2, pg_temp_3.pg_temp_27606
;
ALTER TABLE T ADD COLUMN c_array TEXT[]
                  DEFAULT ('{"This", "is", "' || foo(4) ||
                           '","the", "real", "world"}')::TEXT[],
              ALTER COLUMN c_timestamp
                  SET DEFAULT '1970-12-31'::DATE + LENGTH(foo(30))
;
ALTER TABLE toasted_several ALTER COLUMN toasted_col1 SET STORAGE EXTERNAL
;
ANALYZE public.gtest23q
;
/*
 * Batchmode related functions
 */
CREATE FUNCTION batch_start()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
ANALYZE (SKIP_LOCKED) information_schema.sql_sizing
;
-- This variant malfunctioned in some releases.
alter table parent disable trigger user
;
DROP PUBLICATION testpub_foralltables, testpub_fortable, testpub_forschema, testpub_for_tbl_schema
;
CREATE OR REPLACE VIEW mysecview7
       AS SELECT * FROM tbl1 WHERE a > 256
;
-- cleanup
DROP PROCEDURE test_ambiguous_procname(int)
;
alter table tt8 add column z2 int
;
CREATE TABLE trunc_stats_test1(id serial, stuff text)
;
CREATE TRIGGER del_trig BEFORE DELETE ON pa_target
  FOR EACH ROW EXECUTE PROCEDURE trig_fn()
;
create trigger self_ref_r_trig
  after delete on self_ref referencing old table as old_table
  for each row execute procedure dump_delete()
;
ALTER TYPE rainbow RENAME VALUE 'red' TO 'crimson'
;
ANALYZE public.quad_poly_tbl
;
-- truncate a partition cascading to a table
CREATE TABLE ref_b (
    b INT PRIMARY KEY,
    a INT REFERENCES trunc_a(a) ON DELETE CASCADE
)
;
create temp table t (a int, b int)
;
DROP INDEX wowidx
;
DROP TABLE mytable
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_dict
;
CREATE TABLE unparted (
	a int
)
;
ALTER TABLE accounts_ext ADD COLUMN newcol int DEFAULT 42
;
CREATE SCHEMA split_stats_next
;
CREATE FUNCTION crosstab4(text)
RETURNS setof tablefunc_crosstab_4
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] --
-- Check DO INSTEAD rules with ON CONFLICT
--
CREATE TABLE hats (
	hat_name    char(10) primary key,
	hat_color   char(10)      -- hat color
)
;
----------
[ALTER_TABLE] alter table wide set (parallel_workers = 2)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv_v (ii, jj, kk) AS SELECT i, j FROM mvtest_v
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_returning18
;
----------
[CREATE_POLICY] CREATE COLLATION builtin2 ( PROVIDER = builtin, LOCALE = "en_US" )
;
----------
[CREATE_PROG_OBJ] -- "limit" is to prevent this from being inlined
create function sql_recurse(float8) returns float8 as
$$ select recurse($1) limit 1; $$ language sql
;
----------
[ALTER_POLICY] ALTER POLICY dep_p1 ON dep1 TO regress_rls_bob,regress_rls_carol
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub1
;
----------
[CREATE_TABLE] CREATE TABLE gtestm (
  id int PRIMARY KEY,
  f1 int,
  f2 int,
  f3 int GENERATED ALWAYS AS (f1 * 2) STORED,
  f4 int GENERATED ALWAYS AS (f2 * 2) STORED
)
;
----------
[ADMIN_TABLE] VACUUM part1, part2
;
----------
[CREATE_POLICY] create rule r3 as on insert to rules_src do also
  insert into rules_log values(null, null, '-', default), (new.*, 'new', default)
;
----------
[ALTER_POLICY] -- failed (no role membership)
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf3 SET SCHEMA alt_nsp2
;
----------
[ADD_INDEX] CREATE INDEX timestampidx ON timestamptmp USING gist ( a )
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE test_io_local(a int, b TEXT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION reindex_end_command()
RETURNS event_trigger AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'REINDEX END: command_tag=% type=% identity=%',
	    obj.command_tag, obj.object_type, obj.object_identity;
    END LOOP;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."domview" AS
 SELECT ("col1")::"public"."dom" AS "col1"
   FROM "public"."domtab"
;
----------
[CREATE_TABLE] CREATE TEMP TABLE testeoc (a text)
;
----------
[CREATE_TABLE] create table ec1 (ff int8 primary key, f1 int8alias1, f2 int8alias2)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft_empty (c1 int NOT NULL, c2 text)
  SERVER loopback OPTIONS (table_name 'loct_empty')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."list_partitioned_table"() RETURNS SETOF integer
    LANGUAGE "plpgsql"
    AS $$
DECLARE
    row public.partitioned_table%ROWTYPE;
    a_val public.partitioned_table.a%TYPE;
BEGIN
    FOR row IN SELECT * FROM public.partitioned_table ORDER BY a LOOP
        a_val := row.a;
        RETURN NEXT a_val;
    END LOOP;
    RETURN;
END; $$
;
----------
[CREATE_PROG_OBJ] -- Parallel Append that runs serially
create function sp_test_func() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar $$
language sql stable
;
----------
[ADMIN_TABLE] ANALYZE public.interval_tbl
;
----------
[ADD_INDEX] CREATE INDEX idx_postgis_places_geom_gist ON split_postgis.places USING gist (geom)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_enum = 'three')
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT "constr_parent2_a_not_null" ON "public"."constr_child2" IS 'this constraint is valid'
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_27606_2, pg_temp_3.pg_temp_27606
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_array TEXT[]
                  DEFAULT ('{"This", "is", "' || foo(4) ||
                           '","the", "real", "world"}')::TEXT[],
              ALTER COLUMN c_timestamp
                  SET DEFAULT '1970-12-31'::DATE + LENGTH(foo(30))
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE toasted_several ALTER COLUMN toasted_col1 SET STORAGE EXTERNAL
;
----------
[ADMIN_TABLE] ANALYZE public.gtest23q
;
----------
[CREATE_PROG_OBJ] /*
 * Batchmode related functions
 */
CREATE FUNCTION batch_start()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) information_schema.sql_sizing
;
----------
[ALTER_TABLE] -- This variant malfunctioned in some releases.
alter table parent disable trigger user
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_foralltables, testpub_fortable, testpub_forschema, testpub_for_tbl_schema
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW mysecview7
       AS SELECT * FROM tbl1 WHERE a > 256
;
----------
[DROP_PROG_OBJ] -- cleanup
DROP PROCEDURE test_ambiguous_procname(int)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tt8 add column z2 int
;
----------
[CREATE_TABLE] CREATE TABLE trunc_stats_test1(id serial, stuff text)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER del_trig BEFORE DELETE ON pa_target
  FOR EACH ROW EXECUTE PROCEDURE trig_fn()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger self_ref_r_trig
  after delete on self_ref referencing old table as old_table
  for each row execute procedure dump_delete()
;
----------
[ALTER_TYPE] ALTER TYPE rainbow RENAME VALUE 'red' TO 'crimson'
;
----------
[ADMIN_TABLE] ANALYZE public.quad_poly_tbl
;
----------
[CREATE_TABLE] -- truncate a partition cascading to a table
CREATE TABLE ref_b (
    b INT PRIMARY KEY,
    a INT REFERENCES trunc_a(a) ON DELETE CASCADE
)
;
----------
[CREATE_TABLE] create temp table t (a int, b int)
;
----------
[DROP_INDEX] DROP INDEX wowidx
;
----------
[DROP_TABLE] DROP TABLE mytable
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_dict
;
----------
[CREATE_TABLE] CREATE TABLE unparted (
	a int
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE accounts_ext ADD COLUMN newcol int DEFAULT 42
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_stats_next
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION crosstab4(text)
RETURNS setof tablefunc_crosstab_4
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
