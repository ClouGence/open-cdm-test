-- named NOT NULL constraint
alter table cc1 add column a2 int constraint nn not null
;
ALTER OPERATOR >= (upc, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
ALTER INDEX pg_toast.pg_toast_30544_index RENAME TO reind_con_toast_idx
;
CREATE SUBSCRIPTION "regress_sub4" CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION "regress_pub4" WITH (connect = false, slot_name = 'regress_sub4', streaming = parallel, failover = true)
;
alter table defaulttest alter column col5 set default null
;
ALTER PUBLICATION tap_pub_ins_only SET (publish = 'insert, delete')
;
-- Test that the INSERT ON CONFLICT command correctly checks REPLICA IDENTITY
-- when the target table is published.
CREATE TABLE testpub_insert_onconfl_no_ri (a int unique, b int)
;
-- Add to plt2_adv the extra NULL partition containing only NULL values as the
-- key values
CREATE TABLE plt2_adv_extra PARTITION OF plt2_adv FOR VALUES IN (NULL)
;
CREATE TABLE query ( expr TEXT )
;
-- ok, but not in view (doesn't fail rw_view2's check)
DROP TABLE base_tbl CASCADE
;
CREATE SCHEMA fkpart3
;
CREATE OPERATOR @> (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contains,
	COMMUTATOR = '<@',
	RESTRICT = _int_contains_sel,
	JOIN = _int_contains_joinsel
)
;
CREATE AGGREGATE myaggp19a(BASETYPE = anyelement, SFUNC = tf1p,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
-- Test ISpell dictionary with hunspell affix file with FLAG long parameter
CREATE TEXT SEARCH DICTIONARY hunspell_long (
                        Template=ispell,
                        DictFile=hunspell_sample_long,
                        AffFile=hunspell_sample_long
)
;
create trigger my_trig
  before insert on my_table
  for each row execute procedure funcB()
;
CREATE TABLE syscol_table (a INT)
;
CREATE TEMP TABLE gcircle_tbl AS
    SELECT circle(home_base) AS f1 FROM slow_emp4000
;
CREATE TABLE "generated_stored_tests"."gtest22c" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 4)) STORED
)
;
create table t8()
;
ALTER TABLE pg_temp_18.pg_temp_27200_2 ADD COLUMN newdata pg_temp_18.pg_temp_27200
;
-- btree and hash index creation test
CREATE INDEX guid1_btree ON guid1 USING BTREE (guid_field)
;
VACUUM (FULL) pg_catalog.pg_statistic
;
ANALYZE pg_catalog.pg_largeobject_metadata
;
CREATE PUBLICATION testpub_rf_no FOR TABLE testpub_rf_tbl1
;
CREATE TABLE guid3
(
	id SERIAL,
	guid_field UUID
)
;
ALTER TABLE test_oat_schema.test_oat_tab ENABLE ROW LEVEL SECURITY
;
CREATE TABLE "generated_stored_tests"."gtest24nn" (
    "a" integer,
    "b" "generated_stored_tests"."gtestdomainnn" GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
-- Move back to the default tablespace.
ALTER INDEX regress_tblspace_test_tbl_idx SET TABLESPACE pg_default
;
create schema authorization aac
    create table test_user1 (id int primary key,name varchar(50))
    create table test_user2 (id int primary key,name varchar(50))
;
create table inh_p4(f1 int not null, f3 text not null)
;
CREATE TEXT SEARCH DICTIONARY synonym (
						Template=synonym,
						Synonyms=synonym_sample
)
;
CREATE TABLE tab4_1 PARTITION OF tab4 FOR VALUES IN (0, 1) PARTITION BY LIST (a)
;
DROP INDEX CONCURRENTLY test_dc_data
;
VACUUM (TRUNCATE) t
;
ALTER SUBSCRIPTION regress_testsub SET (password_required = false)
;
COMMENT ON CONSTRAINT the_constraint ON constraint_comments_tbl IS NULL
;
create table tab_part_attach_1 (a int)
;
-- but this should work:
alter table fullname add column suffix text default null
;
ALTER TABLE pg_toast.pg_toast_28363 RENAME TO reind_con_toast
;
CREATE OR REPLACE FUNCTION
  jsonb_path_query_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                   silent boolean DEFAULT false)
RETURNS SETOF jsonb
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_query_tz'
;
alter trigger p on grandparent rename to q
;
drop domain ddef5 restrict
;
CREATE COLLATION mycoll3 FROM "default"
;
VACUUM ANALYZE brin_test_multi
;
-- Check handling of errors thrown from/into anonymous code blocks.
do $outer$
begin
  for i in 1..10 loop
   begin
    execute $ex$
      do $$
      declare x int = 0;
      begin
        x := 1 / x;
      end;
      $$;
    $ex$;
  exception when division_by_zero then
    raise notice 'caught division by zero';
  end;
  end loop;
end;
$outer$
;
-- ONLY_DATABASE_STATS option
VACUUM (ONLY_DATABASE_STATS)
;
-- error
CREATE SCHEMA newschemax AUTHORIZATION nonexistent
;
create function refcursor_test1(refcursor) returns refcursor as $$
begin
    perform return_refcursor($1);
    return $1;
end
$$ language plpgsql
;
CREATE TABLE "public"."sk" (
    "a" integer,
    "b" integer
)
;
COMMENT ON CONSTRAINT test_table_col1_check
						 ON dump_test.test_table IS 'bounds check'
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] -- named NOT NULL constraint
alter table cc1 add column a2 int constraint nn not null
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (upc, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30544_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION "regress_sub4" CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION "regress_pub4" WITH (connect = false, slot_name = 'regress_sub4', streaming = parallel, failover = true)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table defaulttest alter column col5 set default null
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_ins_only SET (publish = 'insert, delete')
;
----------
[CREATE_TABLE] -- Test that the INSERT ON CONFLICT command correctly checks REPLICA IDENTITY
-- when the target table is published.
CREATE TABLE testpub_insert_onconfl_no_ri (a int unique, b int)
;
----------
[CREATE_TABLE] -- Add to plt2_adv the extra NULL partition containing only NULL values as the
-- key values
CREATE TABLE plt2_adv_extra PARTITION OF plt2_adv FOR VALUES IN (NULL)
;
----------
[CREATE_TABLE] CREATE TABLE query ( expr TEXT )
;
----------
[DROP_TABLE] -- ok, but not in view (doesn't fail rw_view2's check)
DROP TABLE base_tbl CASCADE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart3
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @> (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contains,
	COMMUTATOR = '<@',
	RESTRICT = _int_contains_sel,
	JOIN = _int_contains_joinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp19a(BASETYPE = anyelement, SFUNC = tf1p,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_POLICY] -- Test ISpell dictionary with hunspell affix file with FLAG long parameter
CREATE TEXT SEARCH DICTIONARY hunspell_long (
                        Template=ispell,
                        DictFile=hunspell_sample_long,
                        AffFile=hunspell_sample_long
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_trig
  before insert on my_table
  for each row execute procedure funcB()
;
----------
[CREATE_TABLE] CREATE TABLE syscol_table (a INT)
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE gcircle_tbl AS
    SELECT circle(home_base) AS f1 FROM slow_emp4000
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest22c" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 4)) STORED
)
;
----------
[CREATE_TABLE] create table t8()
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_18.pg_temp_27200_2 ADD COLUMN newdata pg_temp_18.pg_temp_27200
;
----------
[ADD_INDEX] -- btree and hash index creation test
CREATE INDEX guid1_btree ON guid1 USING BTREE (guid_field)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_statistic
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_rf_no FOR TABLE testpub_rf_tbl1
;
----------
[CREATE_TABLE] CREATE TABLE guid3
(
	id SERIAL,
	guid_field UUID
)
;
----------
[ALTER_TABLE] ALTER TABLE test_oat_schema.test_oat_tab ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest24nn" (
    "a" integer,
    "b" "generated_stored_tests"."gtestdomainnn" GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[ALTER_INDEX] -- Move back to the default tablespace.
ALTER INDEX regress_tblspace_test_tbl_idx SET TABLESPACE pg_default
;
----------
[CREATE_SCHEMA] create schema authorization aac
    create table test_user1 (id int primary key,name varchar(50))
    create table test_user2 (id int primary key,name varchar(50))
;
----------
[CREATE_TABLE] create table inh_p4(f1 int not null, f3 text not null)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY synonym (
						Template=synonym,
						Synonyms=synonym_sample
)
;
----------
[CREATE_TABLE] CREATE TABLE tab4_1 PARTITION OF tab4 FOR VALUES IN (0, 1) PARTITION BY LIST (a)
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY test_dc_data
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE) t
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (password_required = false)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT the_constraint ON constraint_comments_tbl IS NULL
;
----------
[CREATE_TABLE] create table tab_part_attach_1 (a int)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- but this should work:
alter table fullname add column suffix text default null
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28363 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_query_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                   silent boolean DEFAULT false)
RETURNS SETOF jsonb
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_query_tz'
;
----------
[RENAME_TRIGGER] alter trigger p on grandparent rename to q
;
----------
[DROP_TYPE] drop domain ddef5 restrict
;
----------
[CREATE_POLICY] CREATE COLLATION mycoll3 FROM "default"
;
----------
[ADMIN_TABLE] VACUUM ANALYZE brin_test_multi
;
----------
[BLOCK] -- Check handling of errors thrown from/into anonymous code blocks.
do $outer$
begin
  for i in 1..10 loop
   begin
    execute $ex$
      do $$
      declare x int = 0;
      begin
        x := 1 / x;
      end;
      $$;
    $ex$;
  exception when division_by_zero then
    raise notice 'caught division by zero';
  end;
  end loop;
end;
$outer$
;
----------
[ADMIN_TABLE] -- ONLY_DATABASE_STATS option
VACUUM (ONLY_DATABASE_STATS)
;
----------
[CREATE_SCHEMA] -- error
CREATE SCHEMA newschemax AUTHORIZATION nonexistent
;
----------
[CREATE_PROG_OBJ] create function refcursor_test1(refcursor) returns refcursor as $$
begin
    perform return_refcursor($1);
    return $1;
end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."sk" (
    "a" integer,
    "b" integer
)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT test_table_col1_check
						 ON dump_test.test_table IS 'bounds check'
;
