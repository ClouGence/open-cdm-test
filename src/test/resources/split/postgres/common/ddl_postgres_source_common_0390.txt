create table list_part4 partition of list_part for values in (4)
;
CREATE FUNCTION gseg_picksplit(internal, internal)
RETURNS internal
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER OPERATOR <= (issn13, issn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE MATERIALIZED VIEW test_pg_dump_mv1 AS SELECT * FROM test_pg_dump_t1
;
create temp table a (i integer)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_am
;
CREATE UNIQUE INDEX idx_replidentity_index_b ON tab_replidentity_index(b)
;
create trigger parted_trigger after update of b on parted_trigger
  for each row execute procedure trigger_notice_ab()
;
DROP TABLE clstr_1
;
ALTER OPERATOR >= (ismn, ismn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
CREATE VIEW "public"."limit_thousand_v_2" AS
 SELECT "onek"."thousand"
   FROM "public"."onek"
  WHERE ("onek"."thousand" < 995)
  ORDER BY "onek"."thousand"
 OFFSET 10
 LIMIT 5
;
CREATE OPERATOR split_operator_probe.!! (
    PROCEDURE = split_operator_probe.int_abs,
    RIGHTARG = integer
)
;
CREATE TABLE reloptions_test(i INT) WITH (FiLLFaCToR=30,
	autovacuum_enabled = false, autovacuum_analyze_scale_factor = 0.2)
;
CREATE TABLE foo_ptbl(o int, p text) PARTITION BY RANGE (o)
;
-- ===================================================================
-- test for ANALYZE sampling
-- ===================================================================

CREATE TABLE analyze_table (id int, a text, b bigint)
;
CREATE FUNCTION int_custom_eq(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE split_alter_table.base_table ADD CONSTRAINT base_table_payload_check CHECK (payload IS NOT NULL) NOT VALID
;
CREATE TABLE part2 (a int)
;
CREATE TABLE fk_partitioned_pk (a int, b int, PRIMARY KEY (a, b)) PARTITION BY RANGE (a, b)
;
CREATE TABLE public.test_mismatching_types (
        a bigint PRIMARY KEY
    )
;
CREATE TABLE notnull_tbl3 (a INTEGER NOT NULL, CHECK (a IS NOT NULL))
;
create table idxpart (a int, b int, c text, primary key  (a, b, c)) partition by range (b, c, a)
;
ALTER TABLE ONLY pitest3
            ALTER COLUMN f3 ADD GENERATED ALWAYS AS IDENTITY (START WITH 3)
;
create table ac (a int constraint check_a check (a <> 0))
;
DROP TEXT SEARCH CONFIGURATION IF EXISTS test_tsconfig_exists
;
-- not enough parameters, should throw an error at parse time
create function namedparmcursor_test6() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77);
end
$$ language plpgsql
;
create temp table dtest(f1 dtop)
;
do $$
begin
  assert 1=0;  -- should fail
end;
$$
;
create index matest1i on matest1 ((1-id))
;
DROP FUNCTION psql_error
;
CREATE DATABASE "regress_
attack"
;
create table lparted_by_int2_1 partition of lparted_by_int2 for values in (1)
;
ALTER TABLE pg_temp_11.pg_temp_41153_2 ADD COLUMN newdata pg_temp_11.pg_temp_41153
;
CREATE FUNCTION int_custom_lt(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE ONLY "public"."plt1_e" ATTACH PARTITION "public"."plt1_e_p3" FOR VALUES IN ('0006', '0007', '0008', '0011')
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (log_verbosity 'unsupported')
;
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, enabled = false, create_slot = true)
;
DROP TABLE temporal_partitioned_fk_mltrng2mltrng
;
ALTER INDEX "public"."idxpart_a_idx" ATTACH PARTITION "public"."idxpart3_a_idx"
;
-- functions
DROP FUNCTION test_function_exists()
;
-- Test that estimation of relation size works with tuples wider than the
-- relation fillfactor. We create a table with wide inline attributes and
-- low fillfactor, insert rows and then see how many rows EXPLAIN shows
-- before running analyze. We disable autovacuum so that it does not
-- interfere with the test.
CREATE TABLE table_fillfactor (
  n char(1000)
) with (fillfactor=10, autovacuum_enabled=off)
;
-- Test handling of expanded array passed to a domain constraint (bug #14472)

create function plpgsql_arr_domain_check(val int[]) returns boolean as $$
begin return val[1] > 0; end
$$ language plpgsql immutable
;
CREATE TEMP TABLE users (
    uid integer NOT NULL default '0',
    name varchar(60) NOT NULL default '',
    pass varchar(32) NOT NULL default '',
    -- snip
    PRIMARY KEY (uid),
    UNIQUE (name)
)
;
-- timetz check

CREATE TABLE timetztmp (a timetz)
;
CREATE TABLE "public"."gtest3" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 3)) STORED
)
;
create or replace function returnssetofholes() returns setof has_hole language plpgsql as
$$
begin
  return next row(1,2,3);  -- fails
end$$
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_extension
;
CREATE TABLE "public"."b" (
    "aa" integer,
    "bb" "text"
)
;
CREATE VIEW rec2v WITH (security_barrier) AS SELECT * FROM rec2
;
CREATE TEXT SEARCH CONFIGURATION arabic
	(PARSER = default)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table list_part4 partition of list_part for values in (4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gseg_picksplit(internal, internal)
RETURNS internal
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (issn13, issn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW test_pg_dump_mv1 AS SELECT * FROM test_pg_dump_t1
;
----------
[CREATE_TABLE] create temp table a (i integer)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_am
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX idx_replidentity_index_b ON tab_replidentity_index(b)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parted_trigger after update of b on parted_trigger
  for each row execute procedure trigger_notice_ab()
;
----------
[DROP_TABLE] DROP TABLE clstr_1
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ismn, ismn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."limit_thousand_v_2" AS
 SELECT "onek"."thousand"
   FROM "public"."onek"
  WHERE ("onek"."thousand" < 995)
  ORDER BY "onek"."thousand"
 OFFSET 10
 LIMIT 5
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR split_operator_probe.!! (
    PROCEDURE = split_operator_probe.int_abs,
    RIGHTARG = integer
)
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test(i INT) WITH (FiLLFaCToR=30,
	autovacuum_enabled = false, autovacuum_analyze_scale_factor = 0.2)
;
----------
[CREATE_TABLE] CREATE TABLE foo_ptbl(o int, p text) PARTITION BY RANGE (o)
;
----------
[CREATE_TABLE] -- ===================================================================
-- test for ANALYZE sampling
-- ===================================================================

CREATE TABLE analyze_table (id int, a text, b bigint)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_eq(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE split_alter_table.base_table ADD CONSTRAINT base_table_payload_check CHECK (payload IS NOT NULL) NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE part2 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_pk (a int, b int, PRIMARY KEY (a, b)) PARTITION BY RANGE (a, b)
;
----------
[CREATE_TABLE] CREATE TABLE public.test_mismatching_types (
        a bigint PRIMARY KEY
    )
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl3 (a INTEGER NOT NULL, CHECK (a IS NOT NULL))
;
----------
[CREATE_TABLE] create table idxpart (a int, b int, c text, primary key  (a, b, c)) partition by range (b, c, a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY pitest3
            ALTER COLUMN f3 ADD GENERATED ALWAYS AS IDENTITY (START WITH 3)
;
----------
[CREATE_TABLE] create table ac (a int constraint check_a check (a <> 0))
;
----------
[DROP_POLICY] DROP TEXT SEARCH CONFIGURATION IF EXISTS test_tsconfig_exists
;
----------
[CREATE_PROG_OBJ] -- not enough parameters, should throw an error at parse time
create function namedparmcursor_test6() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77);
end
$$ language plpgsql
;
----------
[CREATE_TABLE] create temp table dtest(f1 dtop)
;
----------
[BLOCK] do $$
begin
  assert 1=0;  -- should fail
end;
$$
;
----------
[ADD_INDEX] create index matest1i on matest1 ((1-id))
;
----------
[DROP_PROG_OBJ] DROP FUNCTION psql_error
;
----------
[CREATE_CATALOG] CREATE DATABASE "regress_
attack"
;
----------
[CREATE_TABLE] create table lparted_by_int2_1 partition of lparted_by_int2 for values in (1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_41153_2 ADD COLUMN newdata pg_temp_11.pg_temp_41153
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_lt(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."plt1_e" ATTACH PARTITION "public"."plt1_e_p3" FOR VALUES IN ('0006', '0007', '0008', '0011')
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (log_verbosity 'unsupported')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, enabled = false, create_slot = true)
;
----------
[DROP_TABLE] DROP TABLE temporal_partitioned_fk_mltrng2mltrng
;
----------
[ALTER_INDEX] ALTER INDEX "public"."idxpart_a_idx" ATTACH PARTITION "public"."idxpart3_a_idx"
;
----------
[DROP_PROG_OBJ] -- functions
DROP FUNCTION test_function_exists()
;
----------
[CREATE_TABLE] -- Test that estimation of relation size works with tuples wider than the
-- relation fillfactor. We create a table with wide inline attributes and
-- low fillfactor, insert rows and then see how many rows EXPLAIN shows
-- before running analyze. We disable autovacuum so that it does not
-- interfere with the test.
CREATE TABLE table_fillfactor (
  n char(1000)
) with (fillfactor=10, autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] -- Test handling of expanded array passed to a domain constraint (bug #14472)

create function plpgsql_arr_domain_check(val int[]) returns boolean as $$
begin return val[1] > 0; end
$$ language plpgsql immutable
;
----------
[CREATE_TABLE] CREATE TEMP TABLE users (
    uid integer NOT NULL default '0',
    name varchar(60) NOT NULL default '',
    pass varchar(32) NOT NULL default '',
    -- snip
    PRIMARY KEY (uid),
    UNIQUE (name)
)
;
----------
[CREATE_TABLE] -- timetz check

CREATE TABLE timetztmp (a timetz)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest3" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 3)) STORED
)
;
----------
[CREATE_PROG_OBJ] create or replace function returnssetofholes() returns setof has_hole language plpgsql as
$$
begin
  return next row(1,2,3);  -- fails
end$$
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_extension
;
----------
[CREATE_TABLE] CREATE TABLE "public"."b" (
    "aa" integer,
    "bb" "text"
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rec2v WITH (security_barrier) AS SELECT * FROM rec2
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION arabic
	(PARSER = default)
;
