create view view_of_joins_2c as select * from (tbl1 join tbl1a using (a)) as y
;
CREATE TRIGGER "before_ins_stmt_trig" BEFORE INSERT ON "public"."main_table" FOR EACH STATEMENT EXECUTE FUNCTION "public"."trigger_func"('before_ins_stmt')
;
ALTER TABLE ONLY "public"."delete_test_table"
    ADD CONSTRAINT "delete_test_table_pkey" PRIMARY KEY ("a", "b", "c", "d")
;
create temp sequence testseq
;
TRUNCATE TABLE truncate_a,trunc_b,trunc_e
;
CREATE TABLE tbl1_part_p1 PARTITION OF tbl1_part FOR VALUES FROM (0) TO (10)
;
CREATE TYPE unknown_comptype AS (
	u unknown    
)
;
-- should fail

-- Check inherited UPDATE with first child excluded
create table some_tab (f1 int, f2 int, f3 int, check (f1 < 10) no inherit)
;
DROP FUNCTION seclabel_four()
;
--
-- test output (and round-trip safety) of various values.
-- To ensure we're testing what we think we're testing, start with
-- float values specified by bit patterns (as a useful side effect,
-- this means we'll fail on non-IEEE platforms).

create type xfloat8
;
COMMENT ON TEXT SEARCH CONFIGURATION portuguese IS 'configuration for portuguese language'
;
VACUUM (PARALLEL 0) pg_catalog.pg_ts_config
;
COMMENT ON CONSTRAINT nn ON dump_test.test_table_nn_chld2 IS 'nn_chld2 comment is valid'
;
DROP TABLE PG_LSN_TBL
;
-- fails: no self-admin in SECURITY DEFINER
DROP FUNCTION dogrant_fails()
;
CREATE TABLE as_select1 AS EXECUTE select1
;
DROP TABLE batch_table, batch_cp_upd_test, batch_table_p0, batch_table_p1 CASCADE
;
ANALYZE public.road
;
ALTER VIEW split_view_probe.base_view SET (security_barrier = true, security_invoker = false, check_option = cascaded)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_authid
;
alter type insenum add value 'i12' before 'L2'
;
-- Verify that the correct triggers fire for cross-partition updates
create table parted_trig (a int) partition by list (a)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_type
;
CREATE INDEX sro_cluster_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
;
SECURITY LABEL ON SCHEMA my_schema_2
    IS 'system_u:object_r:sepgsql_regtest_invisible_schema_t:s0'
;
DROP FUNCTION update_using_indirect()
;
DROP TABLE sto1
;
CREATE EXTENSION seg VERSION '1.2'
;
create temp table patest0 (id, x) as
  select x, x from generate_series(0,1000) x
;
-- OK
SECURITY LABEL ON COLUMN dummy_seclabel_tbl1.a IS 'unclassified'
;
ALTER INDEX public.tht_en_data_key ATTACH PARTITION public.tht_p1_en_data_key
;
CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=32526 host=/tmp/jRTl7S_quU dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (streaming = on, two_phase = on)
;
CREATE TABLE mcic_one (
	id int
  )
;
DROP COLLATION IF EXISTS split_obj_priv.c_locale
;
ALTER TEXT SEARCH CONFIGURATION norwegian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
CREATE TEMP TABLE pg_temp_81.pg_temp_30251_2 (tid pg_catalog.tid)
;
CREATE PUBLICATION pub1
;
CREATE RULE "rtest_nothn_r3" AS
    ON INSERT TO "public"."rtest_nothn2"
   WHERE ("new"."a" >= 100) DO INSTEAD  INSERT INTO "public"."rtest_nothn3" ("a", "b")
  VALUES ("new"."a", "new"."b")
;
CREATE FUNCTION gbt_numeric_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE AGGREGATE myaggp11a(BASETYPE = anyelement, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
CREATE TABLE CHAR_TBL(f1 char(4))
;
ALTER TABLE fprt1_p1 SET (autovacuum_enabled = 'false')
;
ALTER PUBLICATION testpub_fortable ADD TABLE testpub_tbl5d
;
drop view v1
;
create table gin_tbl(p int4[])
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) information_schema.sql_sizing
;
create temp view gstest1(a,b,v)
  as values (1,1,10),(1,1,11),(1,2,12),(1,2,13),(1,3,14),
            (2,3,15),
            (3,3,16),(3,4,17),
            (4,1,18),(4,1,19)
;
CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btint4cmp(int4,int4),
    FUNCTION        2       gin_extract_value_int4(int4, internal),
    FUNCTION        3       gin_extract_query_int4(int4, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_int4(int4,int4,int2, internal),
STORAGE         int4
;
create trigger tg_pslot_biu before insert or update
    on PSlot for each row execute procedure tg_pslot_biu()
;
ALTER INDEX pg_toast.pg_toast_30658_index RENAME TO reind_con_toast_idx
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] create view view_of_joins_2c as select * from (tbl1 join tbl1a using (a)) as y
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "before_ins_stmt_trig" BEFORE INSERT ON "public"."main_table" FOR EACH STATEMENT EXECUTE FUNCTION "public"."trigger_func"('before_ins_stmt')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."delete_test_table"
    ADD CONSTRAINT "delete_test_table_pkey" PRIMARY KEY ("a", "b", "c", "d")
;
----------
[CREATE_SEQUENCE] create temp sequence testseq
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE truncate_a,trunc_b,trunc_e
;
----------
[CREATE_TABLE] CREATE TABLE tbl1_part_p1 PARTITION OF tbl1_part FOR VALUES FROM (0) TO (10)
;
----------
[CREATE_TYPE] CREATE TYPE unknown_comptype AS (
	u unknown    
)
;
----------
[CREATE_TABLE] -- should fail

-- Check inherited UPDATE with first child excluded
create table some_tab (f1 int, f2 int, f3 int, check (f1 < 10) no inherit)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION seclabel_four()
;
----------
[CREATE_TYPE] --
-- test output (and round-trip safety) of various values.
-- To ensure we're testing what we think we're testing, start with
-- float values specified by bit patterns (as a useful side effect,
-- this means we'll fail on non-IEEE platforms).

create type xfloat8
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION portuguese IS 'configuration for portuguese language'
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_ts_config
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT nn ON dump_test.test_table_nn_chld2 IS 'nn_chld2 comment is valid'
;
----------
[DROP_TABLE] DROP TABLE PG_LSN_TBL
;
----------
[DROP_PROG_OBJ] -- fails: no self-admin in SECURITY DEFINER
DROP FUNCTION dogrant_fails()
;
----------
[CREATE_TABLE|UNSAFE] CREATE TABLE as_select1 AS EXECUTE select1
;
----------
[DROP_TABLE] DROP TABLE batch_table, batch_cp_upd_test, batch_table_p0, batch_table_p1 CASCADE
;
----------
[ADMIN_TABLE] ANALYZE public.road
;
----------
[ALTER_VIEW] ALTER VIEW split_view_probe.base_view SET (security_barrier = true, security_invoker = false, check_option = cascaded)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_authid
;
----------
[ALTER_TYPE] alter type insenum add value 'i12' before 'L2'
;
----------
[CREATE_TABLE] -- Verify that the correct triggers fire for cross-partition updates
create table parted_trig (a int) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_type
;
----------
[ADD_INDEX] CREATE INDEX sro_cluster_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
;
----------
[UNKNOWN] SECURITY LABEL ON SCHEMA my_schema_2
    IS 'system_u:object_r:sepgsql_regtest_invisible_schema_t:s0'
;
----------
[DROP_PROG_OBJ] DROP FUNCTION update_using_indirect()
;
----------
[DROP_TABLE] DROP TABLE sto1
;
----------
[CREATE_LIBRARY] CREATE EXTENSION seg VERSION '1.2'
;
----------
[CREATE_TABLE|SELECT] create temp table patest0 (id, x) as
  select x, x from generate_series(0,1000) x
;
----------
[UNKNOWN] -- OK
SECURITY LABEL ON COLUMN dummy_seclabel_tbl1.a IS 'unclassified'
;
----------
[ALTER_INDEX] ALTER INDEX public.tht_en_data_key ATTACH PARTITION public.tht_p1_en_data_key
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=32526 host=/tmp/jRTl7S_quU dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (streaming = on, two_phase = on)
;
----------
[CREATE_TABLE] CREATE TABLE mcic_one (
	id int
  )
;
----------
[DROP_POLICY] DROP COLLATION IF EXISTS split_obj_priv.c_locale
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION norwegian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_81.pg_temp_30251_2 (tid pg_catalog.tid)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub1
;
----------
[CREATE_POLICY] CREATE RULE "rtest_nothn_r3" AS
    ON INSERT TO "public"."rtest_nothn2"
   WHERE ("new"."a" >= 100) DO INSTEAD  INSERT INTO "public"."rtest_nothn3" ("a", "b")
  VALUES ("new"."a", "new"."b")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_numeric_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp11a(BASETYPE = anyelement, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_TABLE] CREATE TABLE CHAR_TBL(f1 char(4))
;
----------
[ALTER_TABLE] ALTER TABLE fprt1_p1 SET (autovacuum_enabled = 'false')
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_fortable ADD TABLE testpub_tbl5d
;
----------
[DROP_VIEW] drop view v1
;
----------
[CREATE_TABLE] create table gin_tbl(p int4[])
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) information_schema.sql_sizing
;
----------
[CREATE_VIEW(SELECT)] create temp view gstest1(a,b,v)
  as values (1,1,10),(1,1,11),(1,2,12),(1,2,13),(1,3,14),
            (2,3,15),
            (3,3,16),(3,4,17),
            (4,1,18),(4,1,19)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btint4cmp(int4,int4),
    FUNCTION        2       gin_extract_value_int4(int4, internal),
    FUNCTION        3       gin_extract_query_int4(int4, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_int4(int4,int4,int2, internal),
STORAGE         int4
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_pslot_biu before insert or update
    on PSlot for each row execute procedure tg_pslot_biu()
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30658_index RENAME TO reind_con_toast_idx
;
