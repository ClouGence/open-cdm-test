CREATE FUNCTION text_w_default_out(text_w_default)
   RETURNS cstring
   AS 'textout'
   LANGUAGE internal STABLE STRICT
;
DROP TABLE IF EXISTS target
;
VACUUM (PARALLEL 2) pg_catalog.pg_index
;
CREATE TABLE test_like_id_2 (LIKE test_like_id_1)
;
ALTER INDEX pg_toast.pg_toast_30445_index RENAME TO reind_con_toast_idx
;
CREATE DATABASE db1
;
ANALYZE public.pagg_tab_m_p1
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_largeobject
;
CREATE FUNCTION subarray(_int4, int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- make sure that types with unusual equality tests work
CREATE TABLE mvtest_boxes (id serial primary key, b box)
;
ALTER TABLE part_2 ALTER b DROP NOT NULL
;
CREATE TABLE tab_rowfilter_child (b text) INHERITS (tab_rowfilter_inherited)
;
CREATE TYPE "public"."quad" AS (
	"c1" "public"."complex",
	"c2" "public"."complex"
)
;
CREATE INDEX test_1_a_brin_idx ON test1 USING BRIN (a)
;
DROP TABLE pg_temp_9.pg_temp_26996_2, pg_temp_9.pg_temp_26996
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
CREATE TABLE "public"."ec2" (
    "xf" bigint NOT NULL,
    "x1" "public"."int8alias1",
    "x2" "public"."int8alias2"
)
;
alter table recur1 add column f2 recur2
;
ALTER TABLE ONLY public.ths
    ADD CONSTRAINT ths_mod_data_key UNIQUE (mod, data)
;
CREATE INDEX collate_test1_idx4 ON collate_test1 (((b||'foo') COLLATE "POSIX"))
;
--
-- VARCHAR
--

CREATE TABLE VARCHAR_TBL(f1 varchar(1))
;
-- Verify that attaching an invalid index does not mark the parent index valid.
-- On the other hand, attaching a valid index marks not only its direct
-- ancestor valid, but also any indirect ancestor that was only missing the one
-- that was just made valid
create table idxpart (a int, b int) partition by range (a)
;
create table part2 partition of range_parted for values from ('a', 10) to ('a', 20)
;
do $$
declare x int_nn := null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
alter table btree_tall_tbl alter COLUMN t set storage plain
;
CREATE POLICY p1r ON document AS RESTRICTIVE TO regress_rls_dave
    USING (cid <> 44)
;
--
-- In addition run the (slightly modified) queries from the
-- programmers manual section on the rule system.
--
CREATE TABLE shoe_data (
	shoename   char(10),      -- primary key
	sh_avail   integer,       -- available # of pairs
	slcolor    char(10),      -- preferred shoelace color
	slminlen   float,         -- minimum shoelace length
	slmaxlen   float,         -- maximum shoelace length
	slunit     char(8)        -- length unit
)
;
CREATE TABLE inh_nn_lvl1 (a int)
;
VACUUM pg_catalog.pg_database
;
CREATE TABLE pg_temp.implicitly_temp (a int primary key)
;
-- check for partition bound overlap and other invalid specifications for the hash partition
CREATE TABLE hash_parted2 (
	a varchar
) PARTITION BY HASH (a)
;
ALTER TABLE test_replica_identity REPLICA IDENTITY FULL
;
CREATE INDEX "tbl_include_reg_c1_c2_c11_c3_idx" ON "public"."tbl_include_reg" USING "btree" ("c1", "c2") INCLUDE ("c1", "c3")
;
CREATE TABLE "public"."pagg_tab_ml" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY RANGE ("a")
;
CREATE DATABASE "regression" WITH TEMPLATE = template0 OID = 16387 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
CREATE TABLE "public"."prt2_l_p2_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
ALTER TABLE persons INHERIT stuff
;
DROP TABLESPACE tblspc2
;
create index on gexec_test(a)
;
CREATE INDEX reloptions_test_idx ON reloptions_test (s)
	WITH (not_existing_ns.fillfactor=2)
;
--------------

-- define the GiST support methods
CREATE FUNCTION g_int_consistent(internal,_int4,smallint,oid,internal)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER SUBSCRIPTION test_tab2_sub SET (slot_name = NONE)
;
ALTER TABLE pg_toast.pg_toast_30671 RENAME TO reind_con_toast
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING IF EXISTS FOR word, word
;
ALTER INDEX pg_toast.pg_toast_27723_index RENAME TO reind_con_toast_idx
;
ANALYZE information_schema.sql_sizing_profiles
;
ALTER TABLE ONLY "public"."part_7_a_null" DROP COLUMN "........pg.dropped.3........"
;
CREATE FUNCTION isnge(isbn, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- ok
alter table fkpart2.fk_part_1_1 drop constraint my_fkey
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION text_w_default_out(text_w_default)
   RETURNS cstring
   AS 'textout'
   LANGUAGE internal STABLE STRICT
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS target
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_index
;
----------
[CREATE_TABLE] CREATE TABLE test_like_id_2 (LIKE test_like_id_1)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30445_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_CATALOG] CREATE DATABASE db1
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab_m_p1
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_largeobject
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION subarray(_int4, int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- make sure that types with unusual equality tests work
CREATE TABLE mvtest_boxes (id serial primary key, b box)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE part_2 ALTER b DROP NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_child (b text) INHERITS (tab_rowfilter_inherited)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."quad" AS (
	"c1" "public"."complex",
	"c2" "public"."complex"
)
;
----------
[ADD_INDEX] CREATE INDEX test_1_a_brin_idx ON test1 USING BRIN (a)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_26996_2, pg_temp_9.pg_temp_26996
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ec2" (
    "xf" bigint NOT NULL,
    "x1" "public"."int8alias1",
    "x2" "public"."int8alias2"
)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table recur1 add column f2 recur2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY public.ths
    ADD CONSTRAINT ths_mod_data_key UNIQUE (mod, data)
;
----------
[ADD_INDEX] CREATE INDEX collate_test1_idx4 ON collate_test1 (((b||'foo') COLLATE "POSIX"))
;
----------
[CREATE_TABLE] --
-- VARCHAR
--

CREATE TABLE VARCHAR_TBL(f1 varchar(1))
;
----------
[CREATE_TABLE] -- Verify that attaching an invalid index does not mark the parent index valid.
-- On the other hand, attaching a valid index marks not only its direct
-- ancestor valid, but also any indirect ancestor that was only missing the one
-- that was just made valid
create table idxpart (a int, b int) partition by range (a)
;
----------
[CREATE_TABLE] create table part2 partition of range_parted for values from ('a', 10) to ('a', 20)
;
----------
[BLOCK] do $$
declare x int_nn := null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table btree_tall_tbl alter COLUMN t set storage plain
;
----------
[CREATE_POLICY] CREATE POLICY p1r ON document AS RESTRICTIVE TO regress_rls_dave
    USING (cid <> 44)
;
----------
[CREATE_TABLE] --
-- In addition run the (slightly modified) queries from the
-- programmers manual section on the rule system.
--
CREATE TABLE shoe_data (
	shoename   char(10),      -- primary key
	sh_avail   integer,       -- available # of pairs
	slcolor    char(10),      -- preferred shoelace color
	slminlen   float,         -- minimum shoelace length
	slmaxlen   float,         -- maximum shoelace length
	slunit     char(8)        -- length unit
)
;
----------
[CREATE_TABLE] CREATE TABLE inh_nn_lvl1 (a int)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_database
;
----------
[CREATE_TABLE] CREATE TABLE pg_temp.implicitly_temp (a int primary key)
;
----------
[CREATE_TABLE] -- check for partition bound overlap and other invalid specifications for the hash partition
CREATE TABLE hash_parted2 (
	a varchar
) PARTITION BY HASH (a)
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY FULL
;
----------
[ADD_INDEX] CREATE INDEX "tbl_include_reg_c1_c2_c11_c3_idx" ON "public"."tbl_include_reg" USING "btree" ("c1", "c2") INCLUDE ("c1", "c3")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_ml" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression" WITH TEMPLATE = template0 OID = 16387 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_l_p2_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[ALTER_TABLE] ALTER TABLE persons INHERIT stuff
;
----------
[DROP_TABLESPACE] DROP TABLESPACE tblspc2
;
----------
[ADD_INDEX] create index on gexec_test(a)
;
----------
[ADD_INDEX] CREATE INDEX reloptions_test_idx ON reloptions_test (s)
	WITH (not_existing_ns.fillfactor=2)
;
----------
[CREATE_PROG_OBJ] --------------

-- define the GiST support methods
CREATE FUNCTION g_int_consistent(internal,_int4,smallint,oid,internal)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION test_tab2_sub SET (slot_name = NONE)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30671 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING IF EXISTS FOR word, word
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27723_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE information_schema.sql_sizing_profiles
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."part_7_a_null" DROP COLUMN "........pg.dropped.3........"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(isbn, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- ok
alter table fkpart2.fk_part_1_1 drop constraint my_fkey
;
