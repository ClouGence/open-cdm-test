EXECUTE dumpFunc('36805')
;
-- assume a minimum timestamp granularity of 100ms

-- cause one sequential scan
BEGIN
;
ALTER DEFAULT PRIVILEGES FOR ROLE regress_matview_user
  REVOKE INSERT ON TABLES FROM regress_matview_user
;
explain (analyze, costs off, summary off, timing off) execute ab_q5 (33, 44, 55)
;
-- so no fixed xid apears in the outfile
SAVEPOINT a
;
-- ERROR
DROP SERVER s6
;
EXECUTE dumpOpr('522')
;
copy parted_copytest from '/src/src/test/regress/results/parted_copytest.csv' (freeze)
;
EXECUTE dumpOpr('16770')
;
EXECUTE dumpTableAttach('39117')
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('36805')
;
----------
[TRANSACTION] -- assume a minimum timestamp granularity of 100ms

-- cause one sequential scan
BEGIN
;
----------
[REVOKE] ALTER DEFAULT PRIVILEGES FOR ROLE regress_matview_user
  REVOKE INSERT ON TABLES FROM regress_matview_user
;
----------
[UNSAFE] explain (analyze, costs off, summary off, timing off) execute ab_q5 (33, 44, 55)
;
----------
[TRANSACTION] -- so no fixed xid apears in the outfile
SAVEPOINT a
;
----------
[SYSTEM_SETTING_WRITE] -- ERROR
DROP SERVER s6
;
----------
[UNSAFE] EXECUTE dumpOpr('522')
;
----------
[DATA_IMPORT] copy parted_copytest from '/src/src/test/regress/results/parted_copytest.csv' (freeze)
;
----------
[UNSAFE] EXECUTE dumpOpr('16770')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('39117')
;
