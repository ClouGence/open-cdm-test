CREATE VIEW mysecview7 WITH (security_invoker=true)
       AS SELECT * FROM tbl1 WHERE a = 100
;
CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES FROM ('a', 1) TO ('z')
;
ALTER FUNCTION gbt_intv_picksplit(internal, internal) PARALLEL SAFE
;
drop table loc3
;
VACUUM (FULL) pg_catalog.pg_aggregate
;
ALTER TABLE ONLY "public"."depth1" INHERIT "public"."depth0"
;
ANALYZE public.trigger_parted_p2_2
;
CREATE FUNCTION gin_extract_value_anyenum(anyenum, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE ONLY "fkpart4"."droppk"
    ADD CONSTRAINT "droppk_pkey" PRIMARY KEY ("a")
;
-- scan should again be skipped, even though NOT NULL is now a column property
ALTER TABLE part_5 ADD CONSTRAINT check_a CHECK (a IN (5)), ALTER a SET NOT NULL
;
/*
 * 1.test CREATE INDEX
 *
 * Deliberately avoid dropping objects in this section, to get some pg_dump
 * coverage.
 */

-- Regular index with included columns
CREATE TABLE tbl_include_reg (c1 int, c2 int, c3 int, c4 box)
;
do $$
declare
  x int;
  y int;
begin
  select * from test_01 into x, y; -- should be ok
  raise notice 'ok';
  select * from test_01 into x;    -- should to fail
end;
$$
;
-- fail (not owner)
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS 'secret'
;
ALTER TABLE gtestxx_2 INHERIT gtest1
;
CREATE TABLE pg_catalog.new_system_table()
;
ALTER INDEX pg_toast.pg_toast_30890_index RENAME TO reind_con_toast_idx
;
CREATE TABLE "regress_indexing"."pk52" (
    "a" integer NOT NULL
)
;
drop table dest
;
create table parted_validate_test_1 partition of parted_validate_test for values in (0, 1)
;
-- Tests for list-partitioned tables
CREATE TABLE plt1_adv (a int, b int, c text) PARTITION BY LIST (c)
;
CREATE FOREIGN TABLE async_p3 PARTITION OF async_pt FOR VALUES FROM (3000) TO (4000)
  SERVER loopback2 OPTIONS (table_name 'base_tbl3')
;
DROP FUNCTION tgf()
;
create trigger tg_iface_bu before update
    on IFace for each row execute procedure tg_iface_bu()
;
ALTER TABLE pg_temp_12.pg_temp_29625_2 ADD COLUMN newdata pg_temp_12.pg_temp_29625
;
CREATE TEXT SEARCH DICTIONARY catalan_stem
	(TEMPLATE = snowball, Language = catalan )
;
--
-- Ensure an aliased target relation for insert is correctly deparsed.
--
create rule r5 as on insert to rules_src do instead insert into rules_log AS trgt SELECT NEW.* RETURNING trgt.f1, trgt.f2
;
ALTER TABLE sub_parent ATTACH PARTITION child_foreign FOR VALUES FROM (1) TO (10)
;
create unlogged table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=98) tablespace "regress_pgbench_tap_1_ts"
;
-- For testing of pg_dump and pg_upgrade, leave behind some identity
-- sequences whose logged-ness doesn't match their owning table's.
CREATE TABLE identity_dump_logged (a INT GENERATED ALWAYS AS IDENTITY)
;
ALTER TABLE ONLY "public"."fk_partitioned_fk_4" ATTACH PARTITION "public"."fk_partitioned_fk_4_1" FOR VALUES FROM (1, 1) TO (100, 100)
;
CREATE FUNCTION gbt_time_union(internal, internal)
RETURNS gbtreekey16
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
DROP INDEX CONCURRENTLY "concur_index4"
;
ANALYZE pg_temp_18.pg_temp_26820
;
CREATE TABLE FKTABLE ( ftest1 int, ftest2 int, ftest3 int, ftest4 int,  CONSTRAINT constrname3
			FOREIGN KEY(ftest1, ftest2, ftest3) REFERENCES PKTABLE (ptest1, ptest2, ptest3))
;
CREATE FUNCTION pg_visibility(regclass, blkno bigint,
							  all_visible OUT boolean,
							  all_frozen OUT boolean,
							  pd_all_visible OUT boolean)
RETURNS record
AS '$libdir/pg_visibility', 'pg_visibility'
LANGUAGE C STRICT
;
create table idxpart (a int4range, b int4range, exclude USING GIST (a with = )) partition by range (a, b)
;
-- views with updatable and non-updatable columns

CREATE TABLE base_tbl(a float)
;
ALTER FUNCTION gbt_inet_penalty(internal, internal, internal) PARALLEL SAFE
;
create table bug_16784(i int, j int)
;
/*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
CREATE FUNCTION float8mi_int(float8, float8) RETURNS int AS
$$ SELECT CAST($1 - $2 AS INT); $$
LANGUAGE SQL
;
create table idxpart (a int, b int unique) partition by range (a, b)
;
CREATE TABLE test2cs (x text COLLATE case_sensitive)
;
alter table parted_trig attach partition parted_trig3 for values in (3)
;
drop table lp, coll_pruning, rlp, mc3p, mc2p, boolpart, iboolpart, boolrangep, rp, coll_pruning_multi, like_op_noprune, lparted_by_int2, rparted_by_int2
;
ALTER INDEX pg_toast.pg_toast_30536_index RENAME TO reind_con_toast_idx
;
DROP OPERATOR @ (seg, seg)
;
ALTER TABLE range_parted ATTACH PARTITION partr_def2 DEFAULT
;
CREATE TEXT SEARCH CONFIGURATION portuguese
	(PARSER = default)
;
CREATE FUNCTION base_fn_in(cstring) RETURNS opaque AS 'boolin'
    LANGUAGE internal IMMUTABLE STRICT
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW mysecview7 WITH (security_invoker=true)
       AS SELECT * FROM tbl1 WHERE a = 100
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES FROM ('a', 1) TO ('z')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_picksplit(internal, internal) PARALLEL SAFE
;
----------
[DROP_TABLE] drop table loc3
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_aggregate
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."depth1" INHERIT "public"."depth0"
;
----------
[ADMIN_TABLE] ANALYZE public.trigger_parted_p2_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_anyenum(anyenum, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart4"."droppk"
    ADD CONSTRAINT "droppk_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLE|ADD_CONSTRAINT|ALTER_COLUMN] -- scan should again be skipped, even though NOT NULL is now a column property
ALTER TABLE part_5 ADD CONSTRAINT check_a CHECK (a IN (5)), ALTER a SET NOT NULL
;
----------
[CREATE_TABLE] /*
 * 1.test CREATE INDEX
 *
 * Deliberately avoid dropping objects in this section, to get some pg_dump
 * coverage.
 */

-- Regular index with included columns
CREATE TABLE tbl_include_reg (c1 int, c2 int, c3 int, c4 box)
;
----------
[BLOCK] do $$
declare
  x int;
  y int;
begin
  select * from test_01 into x, y; -- should be ok
  raise notice 'ok';
  select * from test_01 into x;    -- should to fail
end;
$$
;
----------
[SECURITY_LABEL] -- fail (not owner)
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS 'secret'
;
----------
[ALTER_TABLE] ALTER TABLE gtestxx_2 INHERIT gtest1
;
----------
[CREATE_TABLE] CREATE TABLE pg_catalog.new_system_table()
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30890_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk52" (
    "a" integer NOT NULL
)
;
----------
[DROP_TABLE] drop table dest
;
----------
[CREATE_TABLE] create table parted_validate_test_1 partition of parted_validate_test for values in (0, 1)
;
----------
[CREATE_TABLE] -- Tests for list-partitioned tables
CREATE TABLE plt1_adv (a int, b int, c text) PARTITION BY LIST (c)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE async_p3 PARTITION OF async_pt FOR VALUES FROM (3000) TO (4000)
  SERVER loopback2 OPTIONS (table_name 'base_tbl3')
;
----------
[DROP_PROG_OBJ] DROP FUNCTION tgf()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_iface_bu before update
    on IFace for each row execute procedure tg_iface_bu()
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_29625_2 ADD COLUMN newdata pg_temp_12.pg_temp_29625
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY catalan_stem
	(TEMPLATE = snowball, Language = catalan )
;
----------
[CREATE_POLICY] --
-- Ensure an aliased target relation for insert is correctly deparsed.
--
create rule r5 as on insert to rules_src do instead insert into rules_log AS trgt SELECT NEW.* RETURNING trgt.f1, trgt.f2
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE sub_parent ATTACH PARTITION child_foreign FOR VALUES FROM (1) TO (10)
;
----------
[CREATE_TABLE] create unlogged table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=98) tablespace "regress_pgbench_tap_1_ts"
;
----------
[CREATE_TABLE] -- For testing of pg_dump and pg_upgrade, leave behind some identity
-- sequences whose logged-ness doesn't match their owning table's.
CREATE TABLE identity_dump_logged (a INT GENERATED ALWAYS AS IDENTITY)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."fk_partitioned_fk_4" ATTACH PARTITION "public"."fk_partitioned_fk_4_1" FOR VALUES FROM (1, 1) TO (100, 100)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_union(internal, internal)
RETURNS gbtreekey16
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY "concur_index4"
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_26820
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE ( ftest1 int, ftest2 int, ftest3 int, ftest4 int,  CONSTRAINT constrname3
			FOREIGN KEY(ftest1, ftest2, ftest3) REFERENCES PKTABLE (ptest1, ptest2, ptest3))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_visibility(regclass, blkno bigint,
							  all_visible OUT boolean,
							  all_frozen OUT boolean,
							  pd_all_visible OUT boolean)
RETURNS record
AS '$libdir/pg_visibility', 'pg_visibility'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range, exclude USING GIST (a with = )) partition by range (a, b)
;
----------
[CREATE_TABLE] -- views with updatable and non-updatable columns

CREATE TABLE base_tbl(a float)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_inet_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] create table bug_16784(i int, j int)
;
----------
[CREATE_POLICY] /*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION float8mi_int(float8, float8) RETURNS int AS
$$ SELECT CAST($1 - $2 AS INT); $$
LANGUAGE SQL
;
----------
[CREATE_TABLE] create table idxpart (a int, b int unique) partition by range (a, b)
;
----------
[CREATE_TABLE] CREATE TABLE test2cs (x text COLLATE case_sensitive)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_trig attach partition parted_trig3 for values in (3)
;
----------
[DROP_TABLE] drop table lp, coll_pruning, rlp, mc3p, mc2p, boolpart, iboolpart, boolrangep, rp, coll_pruning_multi, like_op_noprune, lparted_by_int2, rparted_by_int2
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30536_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] DROP OPERATOR @ (seg, seg)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE range_parted ATTACH PARTITION partr_def2 DEFAULT
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION portuguese
	(PARSER = default)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION base_fn_in(cstring) RETURNS opaque AS 'boolin'
    LANGUAGE internal IMMUTABLE STRICT
;
