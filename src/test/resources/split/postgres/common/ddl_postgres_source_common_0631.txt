ALTER TABLE pg_toast.pg_toast_30507 RENAME TO reind_con_toast
;
ALTER TEXT SEARCH CONFIGURATION swedish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH swedish_stem
;
CREATE FUNCTION gin_metapage_info(IN page bytea,
    OUT pending_head bigint,
    OUT pending_tail bigint,
    OUT tail_free_size int4,
    OUT n_pending_pages bigint,
    OUT n_pending_tuples bigint,
    OUT n_total_pages bigint,
    OUT n_entry_pages bigint,
    OUT n_data_pages bigint,
    OUT n_entries bigint,
    OUT version int4)
AS '$libdir/pageinspect', 'gin_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_range
;
create table event_trigger_fire3 (a int)
;
create table phv_boolpart (a bool, b text) partition by list (a)
;
create function infinite_recurse() returns int as
'select infinite_recurse()' language sql
;
ALTER TABLE ONLY "generated_stored_tests"."gtest10" DROP COLUMN "........pg.dropped.3........"
;
CREATE INDEX brin_test_multi_b_idx ON brin_test_multi USING brin (b) WITH (pages_per_range = 2)
;
CREATE TABLE "public"."prt2_n" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
create function dfunc(a anyelement, b anyelement = null, flag bool = true)
returns anyelement as $$
  select case when $3 then $1 else $2 end;
$$ language sql
;
CREATE FUNCTION buffer_call_start_io(buffer int, for_input bool, nowait bool)
RETURNS pg_catalog.bool STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
--
--
--
-- cash ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_cash_consistent(internal,money,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
DROP TABLE tab_rep_next
;
ALTER INDEX pg_toast.pg_toast_30846_index RENAME TO reind_con_toast_idx
;
create or replace function forc01() returns void as $$
declare
  c refcursor := 'fooled_ya';
  r record;
begin
  open c for select * from forc_test;
  loop
    fetch c into r;
    exit when not found;
    raise notice '%, %', r.i, r.j;
    update forc_test set i = i * 100, j = r.j * 2 where current of c;
  end loop;
end;
$$ language plpgsql
;
create type xfloat8
;
CREATE TRIGGER error_upd_a_a BEFORE UPDATE OF a, a ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('error_upd_a_a')
;
create index on test_range_elem using spgist(int4range(i,i+10))
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_foreign_data_wrapper
;
CREATE TABLE "public"."gtest1_1" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
ALTER TABLE fd_pt1 ADD COLUMN c6 integer
;
ANALYZE real_city
;
ALTER INDEX pg_toast.pg_toast_28108_index RENAME TO reind_con_toast_idx
;
ALTER TABLE part_at2tmp RENAME TO fail
;
CREATE TABLE "public"."temporal_fk2_mltrng2mltrng" (
    "id" "int4range" NOT NULL,
    "valid_at" "datemultirange" NOT NULL,
    "parent_id1" "int4range",
    "parent_id2" "int4range"
)
;
CREATE TABLE notnull_tbl1_3(a int, b int)
;
ANALYZE pg_temp_14.pg_temp_27156
;
ALTER TABLE pg_temp.z ADD CONSTRAINT z_pkey PRIMARY KEY (aa, b)
;
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_view
EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
CREATE DATABASE regression_dest2
;
create table rtest_order2 (a int4, b int4, c text)
;
create table constr_parent (a int)
;
CREATE FUNCTION dblink_get_result(text)
RETURNS SETOF record
AS '$libdir/dblink', 'dblink_get_result'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
DROP INDEX CONCURRENTLY concur_temp_ind
;
create function mr_table_succeed(i anyelement, r anymultirange) returns table(i anyelement, r anymultirange)
  as $$ select $1, $2 $$ language sql
;
-- When reassigning ownership of a composite type, its pg_class entry
-- should match
CREATE TYPE deptest_t AS (a int)
;
alter table atacc1 add constraint atacc_test1 primary key (test, test2)
;
-- failure: one of the partitions has REPLICA IDENTITY FULL
ALTER TABLE testpub_tbl8_1 REPLICA IDENTITY FULL
;
CREATE TABLE toasttable(
       id serial primary key,
       toasted_col1 text,
       rand1 float8 DEFAULT nextval('toasttable_rand_seq'),
       toasted_col2 text,
       rand2 float8 DEFAULT nextval('toasttable_rand_seq')
       )
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
ANALYZE public.rtest_view3
;
ALTER FUNCTION gbt_uuid_compress(internal) PARALLEL SAFE
;
CREATE TYPE test_typ AS ENUM ('blue', 'red')
;
-- fail

DROP TABLE cwi_test
;
CREATE VIEW rw_view2 AS SELECT cc AS ccc, aa AS aaa, bb AS bbb FROM rw_view1
;
alter table idxpart detach partition idxpart1
;
-- check that the message shows the appropriate column description in a
-- situation where the partitioned table is not the primary ModifyTable node
create table inserttest3 (f1 text default 'foo', f2 text default 'bar', f3 int)
;
-- should fail
CREATE SCHEMA test_view_schema
    CREATE TEMP VIEW testview AS SELECT 1
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26444_2 (tid pg_catalog.tid)
;
------------------------------------------------------------------------------------------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30507 RENAME TO reind_con_toast
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION swedish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH swedish_stem
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_metapage_info(IN page bytea,
    OUT pending_head bigint,
    OUT pending_tail bigint,
    OUT tail_free_size int4,
    OUT n_pending_pages bigint,
    OUT n_pending_tuples bigint,
    OUT n_total_pages bigint,
    OUT n_entry_pages bigint,
    OUT n_data_pages bigint,
    OUT n_entries bigint,
    OUT version int4)
AS '$libdir/pageinspect', 'gin_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_range
;
----------
[CREATE_TABLE] create table event_trigger_fire3 (a int)
;
----------
[CREATE_TABLE] create table phv_boolpart (a bool, b text) partition by list (a)
;
----------
[CREATE_PROG_OBJ] create function infinite_recurse() returns int as
'select infinite_recurse()' language sql
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_stored_tests"."gtest10" DROP COLUMN "........pg.dropped.3........"
;
----------
[ADD_INDEX] CREATE INDEX brin_test_multi_b_idx ON brin_test_multi USING brin (b) WITH (pages_per_range = 2)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_n" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
----------
[CREATE_PROG_OBJ] create function dfunc(a anyelement, b anyelement = null, flag bool = true)
returns anyelement as $$
  select case when $3 then $1 else $2 end;
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION buffer_call_start_io(buffer int, for_input bool, nowait bool)
RETURNS pg_catalog.bool STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] --
--
--
-- cash ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_cash_consistent(internal,money,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] DROP TABLE tab_rep_next
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30846_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] create or replace function forc01() returns void as $$
declare
  c refcursor := 'fooled_ya';
  r record;
begin
  open c for select * from forc_test;
  loop
    fetch c into r;
    exit when not found;
    raise notice '%, %', r.i, r.j;
    update forc_test set i = i * 100, j = r.j * 2 where current of c;
  end loop;
end;
$$ language plpgsql
;
----------
[CREATE_TYPE] create type xfloat8
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER error_upd_a_a BEFORE UPDATE OF a, a ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('error_upd_a_a')
;
----------
[ADD_INDEX] create index on test_range_elem using spgist(int4range(i,i+10))
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest1_1" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE fd_pt1 ADD COLUMN c6 integer
;
----------
[ADMIN_TABLE] ANALYZE real_city
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28108_index RENAME TO reind_con_toast_idx
;
----------
[RENAME_TABLE] ALTER TABLE part_at2tmp RENAME TO fail
;
----------
[CREATE_TABLE] CREATE TABLE "public"."temporal_fk2_mltrng2mltrng" (
    "id" "int4range" NOT NULL,
    "valid_at" "datemultirange" NOT NULL,
    "parent_id1" "int4range",
    "parent_id2" "int4range"
)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1_3(a int, b int)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_14.pg_temp_27156
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_temp.z ADD CONSTRAINT z_pkey PRIMARY KEY (aa, b)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_view
EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
----------
[CREATE_CATALOG] CREATE DATABASE regression_dest2
;
----------
[CREATE_TABLE] create table rtest_order2 (a int4, b int4, c text)
;
----------
[CREATE_TABLE] create table constr_parent (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_get_result(text)
RETURNS SETOF record
AS '$libdir/dblink', 'dblink_get_result'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY concur_temp_ind
;
----------
[CREATE_PROG_OBJ] create function mr_table_succeed(i anyelement, r anymultirange) returns table(i anyelement, r anymultirange)
  as $$ select $1, $2 $$ language sql
;
----------
[CREATE_TYPE] -- When reassigning ownership of a composite type, its pg_class entry
-- should match
CREATE TYPE deptest_t AS (a int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_test1 primary key (test, test2)
;
----------
[ALTER_TABLE] -- failure: one of the partitions has REPLICA IDENTITY FULL
ALTER TABLE testpub_tbl8_1 REPLICA IDENTITY FULL
;
----------
[CREATE_TABLE] CREATE TABLE toasttable(
       id serial primary key,
       toasted_col1 text,
       rand1 float8 DEFAULT nextval('toasttable_rand_seq'),
       toasted_col2 text,
       rand2 float8 DEFAULT nextval('toasttable_rand_seq')
       )
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_view3
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_uuid_compress(internal) PARALLEL SAFE
;
----------
[CREATE_TYPE] CREATE TYPE test_typ AS ENUM ('blue', 'red')
;
----------
[DROP_TABLE] -- fail

DROP TABLE cwi_test
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS SELECT cc AS ccc, aa AS aaa, bb AS bbb FROM rw_view1
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart detach partition idxpart1
;
----------
[CREATE_TABLE] -- check that the message shows the appropriate column description in a
-- situation where the partitioned table is not the primary ModifyTable node
create table inserttest3 (f1 text default 'foo', f2 text default 'bar', f3 int)
;
----------
[CREATE_SCHEMA(SELECT)] -- should fail
CREATE SCHEMA test_view_schema
    CREATE TEMP VIEW testview AS SELECT 1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26444_2 (tid pg_catalog.tid)
;
