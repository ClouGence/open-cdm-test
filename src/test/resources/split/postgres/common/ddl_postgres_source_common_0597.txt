create rule rtest_sys_del as on delete to rtest_system do also (
	delete from rtest_interface where sysname = old.sysname;
	delete from rtest_admin where sysname = old.sysname;
	)
;
create temp table p_t1 (
  a int,
  b int,
  c int,
  d int,
  primary key(a,b)
) partition by list(a)
;
/* The next *should* fail, but it's not implemented that way yet. */
CREATE VIEW atestv2 AS SELECT * FROM atest2
;
-- ALTER

ALTER COLLATION "en_US" REFRESH VERSION
;
alter table tabwithcols add column d int
;
create index 計算機用語index2 on 計算機用語 using hash (分類コード)
;
CREATE FUNCTION test_text_to_bytea(text) RETURNS bytea
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
CREATE TABLESPACE regress_tblspacewith LOCATION '/src/src/test/regress/testtablespace' WITH (some_nonexistent_parameter = true)
;
CREATE RULE rule3 AS ON DELETE TO rule_merge1
	DO INSTEAD DELETE FROM rule_merge2 WHERE a = OLD.a
;
ALTER TABLE ONLY "public"."mlparted12" DROP COLUMN "........pg.dropped.6........"
;
ALTER INDEX pg_toast.pg_toast_27713_index RENAME TO reind_con_toast_idx
;
ALTER TABLE bttest_b SET (autovacuum_enabled = false)
;
DROP TABLE base_tbl
;
create table inh_parent2(d int, e int, b int, primary key (d, b))
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, disable_on_error = foo)
;
CREATE TABLE "public"."gtest30_1" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
ALTER TABLE ONLY "public"."pagg_tab1" ATTACH PARTITION "public"."pagg_tab1_p1" FOR VALUES FROM (0) TO (10)
;
alter table p2 drop constraint f1_pos
;
CREATE DOMAIN domain4 AS text[]
  DEFAULT ('{"This", "is", "' || foo(4) || '","the", "real", "world"}')::TEXT[]
;
alter table cwi_test add primary key using index cwi_test_a_idx
;
COMMENT ON FOREIGN DATA WRAPPER dummy IS 'useless'
;
CREATE TYPE "public"."textrange" AS RANGE (
    subtype = "text",
    collation = "pg_catalog"."C"
)
;
DROP TABLE bank_account
;
CREATE FOREIGN TABLE t1 (
  c1 integer OPTIONS (column_name 'c1'),
  c2 character varying OPTIONS (column_name 'c2')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't1')
;
--not indexed
CREATE OPERATOR ^@> (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_isparent,
        COMMUTATOR = '^<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_subscription
;
create type testtype4 as (a int, b point)
;
ALTER PUBLICATION testpub_tbl9 DROP TABLE public.testpub_tbl7
;
-- check for a conflicting COLLATE clause
create table parted_collate_must_match (a text collate "C", b text collate "C")
  partition by range (a)
;
alter domain con add constraint t check (VALUE < 34)
;
CREATE TEMP TABLE concur_temp_tab_1 (c1 int, c2 text)
  ON COMMIT PRESERVE ROWS
;
DROP FUNCTION func_d_1_15()
;
drop table tlu_job
;
create temp table tt4(f1 int)
;
ALTER SUBSCRIPTION regress_testsub RENAME TO regress_testsub_dummy
;
CREATE TRIGGER after_upd_b_row_trig AFTER UPDATE OF b ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('after_upd_b_row')
;
create index tprt2_idx on tprt_2 (col1)
;
ALTER INDEX pg_toast.pg_toast_27579_index RENAME TO reind_con_toast_idx
;
alter domain di add constraint pos check (value > 0)
;
-- This level of partitioning has different column positions than the parent
CREATE TABLE pagg_tab_ml_p3(b int, c text, a int) PARTITION BY RANGE (b)
;
VACUUM (FREEZE) pg_catalog.pg_ts_template
;
ANALYZE public.xmltest
;
CREATE FUNCTION testUVToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
as $$
$val = ~0;
return $val;
$$
;
-- Function scans
CREATE TABLE fs_target (a int, b int, c text)
  WITH (autovacuum_enabled=off)
;
-- test externally stored compressed data
CREATE OR REPLACE FUNCTION large_val() RETURNS TEXT LANGUAGE SQL AS
'select array_agg(fipshash(g::text))::text from generate_series(1, 256) g'
;
CREATE LANGUAGE alt_lang1 HANDLER plpgsql_call_handler
;
ALTER OPERATOR FAMILY gist_vbit_ops USING gist ADD
	OPERATOR	6	<> (varbit, varbit) ,
	FUNCTION	9 (varbit, varbit) gbt_var_fetch (internal)
;
create rule r5 as on insert to rules_src do instead insert into rules_log AS trgt SELECT NEW.* RETURNING trgt.f1, trgt.f2
;
DROP TABLE agg
;
-- error
drop table my_table
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] create rule rtest_sys_del as on delete to rtest_system do also (
	delete from rtest_interface where sysname = old.sysname;
	delete from rtest_admin where sysname = old.sysname;
	)
;
----------
[CREATE_TABLE] create temp table p_t1 (
  a int,
  b int,
  c int,
  d int,
  primary key(a,b)
) partition by list(a)
;
----------
[CREATE_VIEW(SELECT)] /* The next *should* fail, but it's not implemented that way yet. */
CREATE VIEW atestv2 AS SELECT * FROM atest2
;
----------
[ALTER_POLICY] -- ALTER

ALTER COLLATION "en_US" REFRESH VERSION
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tabwithcols add column d int
;
----------
[ADD_INDEX] create index 計算機用語index2 on 計算機用語 using hash (分類コード)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_text_to_bytea(text) RETURNS bytea
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspacewith LOCATION '/src/src/test/regress/testtablespace' WITH (some_nonexistent_parameter = true)
;
----------
[CREATE_POLICY] CREATE RULE rule3 AS ON DELETE TO rule_merge1
	DO INSTEAD DELETE FROM rule_merge2 WHERE a = OLD.a
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted12" DROP COLUMN "........pg.dropped.6........"
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27713_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE] ALTER TABLE bttest_b SET (autovacuum_enabled = false)
;
----------
[DROP_TABLE] DROP TABLE base_tbl
;
----------
[CREATE_TABLE] create table inh_parent2(d int, e int, b int, primary key (d, b))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, disable_on_error = foo)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest30_1" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab1" ATTACH PARTITION "public"."pagg_tab1_p1" FOR VALUES FROM (0) TO (10)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table p2 drop constraint f1_pos
;
----------
[CREATE_TYPE] CREATE DOMAIN domain4 AS text[]
  DEFAULT ('{"This", "is", "' || foo(4) || '","the", "real", "world"}')::TEXT[]
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table cwi_test add primary key using index cwi_test_a_idx
;
----------
[UNKNOWN] COMMENT ON FOREIGN DATA WRAPPER dummy IS 'useless'
;
----------
[CREATE_POLICY] CREATE TYPE "public"."textrange" AS RANGE (
    subtype = "text",
    collation = "pg_catalog"."C"
)
;
----------
[DROP_TABLE] DROP TABLE bank_account
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t1 (
  c1 integer OPTIONS (column_name 'c1'),
  c2 character varying OPTIONS (column_name 'c2')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't1')
;
----------
[CREATE_PROG_OBJ] --not indexed
CREATE OPERATOR ^@> (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_isparent,
        COMMUTATOR = '^<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_subscription
;
----------
[CREATE_TYPE] create type testtype4 as (a int, b point)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_tbl9 DROP TABLE public.testpub_tbl7
;
----------
[CREATE_TABLE] -- check for a conflicting COLLATE clause
create table parted_collate_must_match (a text collate "C", b text collate "C")
  partition by range (a)
;
----------
[ALTER_TYPE] alter domain con add constraint t check (VALUE < 34)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE concur_temp_tab_1 (c1 int, c2 text)
  ON COMMIT PRESERVE ROWS
;
----------
[DROP_PROG_OBJ] DROP FUNCTION func_d_1_15()
;
----------
[DROP_TABLE] drop table tlu_job
;
----------
[CREATE_TABLE] create temp table tt4(f1 int)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub RENAME TO regress_testsub_dummy
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER after_upd_b_row_trig AFTER UPDATE OF b ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('after_upd_b_row')
;
----------
[ADD_INDEX] create index tprt2_idx on tprt_2 (col1)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27579_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TYPE] alter domain di add constraint pos check (value > 0)
;
----------
[CREATE_TABLE] -- This level of partitioning has different column positions than the parent
CREATE TABLE pagg_tab_ml_p3(b int, c text, a int) PARTITION BY RANGE (b)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_ts_template
;
----------
[ADMIN_TABLE] ANALYZE public.xmltest
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testUVToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
as $$
$val = ~0;
return $val;
$$
;
----------
[CREATE_TABLE] -- Function scans
CREATE TABLE fs_target (a int, b int, c text)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] -- test externally stored compressed data
CREATE OR REPLACE FUNCTION large_val() RETURNS TEXT LANGUAGE SQL AS
'select array_agg(fipshash(g::text))::text from generate_series(1, 256) g'
;
----------
[UNKNOWN] CREATE LANGUAGE alt_lang1 HANDLER plpgsql_call_handler
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_vbit_ops USING gist ADD
	OPERATOR	6	<> (varbit, varbit) ,
	FUNCTION	9 (varbit, varbit) gbt_var_fetch (internal)
;
----------
[CREATE_POLICY] create rule r5 as on insert to rules_src do instead insert into rules_log AS trgt SELECT NEW.* RETURNING trgt.f1, trgt.f2
;
----------
[DROP_TABLE] DROP TABLE agg
;
----------
[DROP_TABLE] -- error
drop table my_table
;
