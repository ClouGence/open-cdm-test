EXECUTE dumpFunc('19281')
;
SET search_path = lock_schema1
;
EXECUTE dumpTableAttach('38394')
;
-- all-level tracking.
SET pg_stat_statements.track = 'all'
;
EXECUTE dumpFunc('18254')
;
GRANT regress_priv_user9 TO regress_priv_user8
;
COPY public.clstr_tst (a, b, c, d) TO stdout
;
EXECUTE dumpFunc('19338')
;
-- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
ALTER PROCEDURE "public"."ptest10"(OUT "a" integer, IN "b" integer, IN "c" integer) OWNER TO zyc
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('19281')
;
----------
[SESSION_SETTING_WRITE] SET search_path = lock_schema1
;
----------
[UNSAFE] EXECUTE dumpTableAttach('38394')
;
----------
[SESSION_SETTING_WRITE] -- all-level tracking.
SET pg_stat_statements.track = 'all'
;
----------
[UNSAFE] EXECUTE dumpFunc('18254')
;
----------
[GRANT] GRANT regress_priv_user9 TO regress_priv_user8
;
----------
[DATA_EXPORT] COPY public.clstr_tst (a, b, c, d) TO stdout
;
----------
[UNSAFE] EXECUTE dumpFunc('19338')
;
----------
[SESSION_SETTING_WRITE] -- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
----------
[TRANSFER_PRIVILEGE] ALTER PROCEDURE "public"."ptest10"(OUT "a" integer, IN "b" integer, IN "c" integer) OWNER TO zyc
;
