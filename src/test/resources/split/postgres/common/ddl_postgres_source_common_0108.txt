CREATE FUNCTION gbt_int4_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create table event_trigger_fire5 (a int)
;
drop function shadowtest(int)
;
drop table bytea_test_table
;
-- Should work now

DROP CAST (int4 AS casttesttype)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_description
;
CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION PUBLIC
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest24at"
    ADD CONSTRAINT "gtest24at_pkey" PRIMARY KEY ("a")
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_conversion
;
CREATE VIEW pg_matviews AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS matviewname,
        pg_get_userbyid(C.relowner) AS matviewowner,
        T.spcname AS tablespace,
        C.relhasindex AS hasindexes,
        C.relispopulated AS ispopulated,
        pg_get_viewdef(C.oid) AS definition
    FROM pg_class C LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
         LEFT JOIN pg_tablespace T ON (T.oid = C.reltablespace)
    WHERE C.relkind = 'm'
;
CREATE TABLE testschema.part2 PARTITION OF testschema.part FOR VALUES IN (2)
;
CREATE CAST (ean13 AS ismn13) WITH FUNCTION ismn13(ean13)
;
-- With a Schema
CREATE SCHEMA concur_reindex_schema
;
ALTER TABLE parent SET UNLOGGED
;
CREATE OPERATOR = (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_eq,
        COMMUTATOR = '=',
	NEGATOR = '<>',
        RESTRICT = eqsel,
	JOIN = eqjoinsel,
        SORT1 = '<',
	SORT2 = '<'
)
;
CREATE COLLATION testcoll_error1 (provider = icu, locale = '@colNumeric=lower')
;
CREATE SCHEMA baz
;
CREATE TABLE part_document_nonfiction PARTITION OF part_document FOR VALUES FROM (99) to (100)
;
CREATE TABLE trunc_stats_test4(id serial)
;
DROP DOMAIN queryfuncs_char2, queryfuncs_char2_chk
;
DROP INDEX idx_tab
;
-- Show tupleids of non-frozen tuples if any in all_frozen pages
-- for a relation.
CREATE FUNCTION pg_check_frozen(regclass, t_ctid OUT tid)
RETURNS SETOF tid
AS '$libdir/pg_visibility', 'pg_check_frozen'
LANGUAGE C STRICT
;
CREATE FUNCTION isbn(ean13)
RETURNS isbn
AS '$libdir/isn', 'isbn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION "public"."gstest_data"("v" integer, OUT "a" integer, OUT "b" integer) RETURNS SETOF "record"
    LANGUAGE "plpgsql"
    AS $$
    begin
      return query select v, i from generate_series(1,3) i;
    end;
  $$
;
CREATE FUNCTION hashisbn13(isbn13)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create table constr_parent2 (a int)
;
ANALYZE beta
;
CREATE COLLATION case_sensitive (provider = icu, locale = '')
;
create function outparam_succeed2(i anyrange, out r anyarray, out t text)
  as $$ select ARRAY[upper($1)], 'foo'::text $$ language sql
;
CREATE INDEX iprt2_p2_b on prt2_p2(b)
;
CREATE INDEX "sp_quad_ind" ON "public"."quad_point_tbl" USING "spgist" ("p")
;
-- fail

CREATE TABLE test_u AS SELECT a, b FROM collate_test1 UNION ALL SELECT a, b FROM collate_test3
;
create table System (
    name	text,
    comment	text
)
;
CREATE TABLE not_a_part (a int)
;
-- LIKE ... INCLUDING STATISTICS with dropped columns in the parent,
-- so stxkeys attnums are not contiguous.
CREATE TABLE ctl_stats3_parent (a int, b int, c int)
;
DROP TABLE withoutoid
;
create operator class my_op_class for type int using btree family my_op_family as
 operator 1 public.<^,
 operator 3 public.=^,
 operator 5 public.>^,
 function 1 my_op_cmp(int, int)
;
CREATE VIEW rw_view3 AS
  SELECT s, c, s/c t, ctid
  FROM rw_view1
;
CREATE TABLE test_tablesample (id int, name text)
;
CREATE TABLE fail_part PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 8, REMAINDER 8)
;
-- invalid column type
CREATE TABLE itest_err_1 (a text generated by default as identity)
;
CREATE TABLE "public"."gtestxx_2" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 22)) STORED
)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_namespace
;
CREATE TABLE orders (
    order_id     bigint not null,
    cust_id      bigint not null,
    status       text
) PARTITION BY HASH (order_id)
;
create table idxpart2 partition of idxpart for values from (100) to (1000)
  partition by range (a)
;
CREATE TABLE split_alter_table.base_table (
    id integer NOT NULL,
    code text NOT NULL,
    payload text,
    old_payload varchar(10),
    generated_col integer GENERATED ALWAYS AS (id * 2) STORED,
    identity_col integer GENERATED BY DEFAULT AS IDENTITY,
    parent_key integer,
    CONSTRAINT base_table_pk PRIMARY KEY (id)
)
;
ANALYZE pg_temp_3.pg_temp_27503_2
;
--
-- Check that index expressions and predicates are run as the table's owner
--
TRUNCATE bttest_a
;
--
-- Also check dropping a column that existed when the view was made
--

create table tt9 (x int, xx int, y int)
;
CREATE FUNCTION earth() RETURNS float8
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS 'SELECT ''6378168''::float8'
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int4_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] create table event_trigger_fire5 (a int)
;
----------
[DROP_PROG_OBJ] drop function shadowtest(int)
;
----------
[DROP_TABLE] drop table bytea_test_table
;
----------
[DROP_PROG_OBJ] -- Should work now

DROP CAST (int4 AS casttesttype)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_description
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION PUBLIC
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest24at"
    ADD CONSTRAINT "gtest24at_pkey" PRIMARY KEY ("a")
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_conversion
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW pg_matviews AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS matviewname,
        pg_get_userbyid(C.relowner) AS matviewowner,
        T.spcname AS tablespace,
        C.relhasindex AS hasindexes,
        C.relispopulated AS ispopulated,
        pg_get_viewdef(C.oid) AS definition
    FROM pg_class C LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
         LEFT JOIN pg_tablespace T ON (T.oid = C.reltablespace)
    WHERE C.relkind = 'm'
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part2 PARTITION OF testschema.part FOR VALUES IN (2)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ean13 AS ismn13) WITH FUNCTION ismn13(ean13)
;
----------
[CREATE_SCHEMA] -- With a Schema
CREATE SCHEMA concur_reindex_schema
;
----------
[ALTER_TABLE] ALTER TABLE parent SET UNLOGGED
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_eq,
        COMMUTATOR = '=',
	NEGATOR = '<>',
        RESTRICT = eqsel,
	JOIN = eqjoinsel,
        SORT1 = '<',
	SORT2 = '<'
)
;
----------
[CREATE_POLICY] CREATE COLLATION testcoll_error1 (provider = icu, locale = '@colNumeric=lower')
;
----------
[CREATE_SCHEMA] CREATE SCHEMA baz
;
----------
[CREATE_TABLE] CREATE TABLE part_document_nonfiction PARTITION OF part_document FOR VALUES FROM (99) to (100)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_stats_test4(id serial)
;
----------
[DROP_TYPE] DROP DOMAIN queryfuncs_char2, queryfuncs_char2_chk
;
----------
[DROP_INDEX] DROP INDEX idx_tab
;
----------
[CREATE_PROG_OBJ] -- Show tupleids of non-frozen tuples if any in all_frozen pages
-- for a relation.
CREATE FUNCTION pg_check_frozen(regclass, t_ctid OUT tid)
RETURNS SETOF tid
AS '$libdir/pg_visibility', 'pg_check_frozen'
LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isbn(ean13)
RETURNS isbn
AS '$libdir/isn', 'isbn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."gstest_data"("v" integer, OUT "a" integer, OUT "b" integer) RETURNS SETOF "record"
    LANGUAGE "plpgsql"
    AS $$
    begin
      return query select v, i from generate_series(1,3) i;
    end;
  $$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hashisbn13(isbn13)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] create table constr_parent2 (a int)
;
----------
[ADMIN_TABLE] ANALYZE beta
;
----------
[CREATE_POLICY] CREATE COLLATION case_sensitive (provider = icu, locale = '')
;
----------
[CREATE_PROG_OBJ] create function outparam_succeed2(i anyrange, out r anyarray, out t text)
  as $$ select ARRAY[upper($1)], 'foo'::text $$ language sql
;
----------
[ADD_INDEX] CREATE INDEX iprt2_p2_b on prt2_p2(b)
;
----------
[ADD_INDEX] CREATE INDEX "sp_quad_ind" ON "public"."quad_point_tbl" USING "spgist" ("p")
;
----------
[CREATE_TABLE|SELECT] -- fail

CREATE TABLE test_u AS SELECT a, b FROM collate_test1 UNION ALL SELECT a, b FROM collate_test3
;
----------
[CREATE_TABLE] create table System (
    name	text,
    comment	text
)
;
----------
[CREATE_TABLE] CREATE TABLE not_a_part (a int)
;
----------
[CREATE_TABLE] -- LIKE ... INCLUDING STATISTICS with dropped columns in the parent,
-- so stxkeys attnums are not contiguous.
CREATE TABLE ctl_stats3_parent (a int, b int, c int)
;
----------
[DROP_TABLE] DROP TABLE withoutoid
;
----------
[CREATE_PROG_OBJ] create operator class my_op_class for type int using btree family my_op_family as
 operator 1 public.<^,
 operator 3 public.=^,
 operator 5 public.>^,
 function 1 my_op_cmp(int, int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view3 AS
  SELECT s, c, s/c t, ctid
  FROM rw_view1
;
----------
[CREATE_TABLE] CREATE TABLE test_tablesample (id int, name text)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 8, REMAINDER 8)
;
----------
[CREATE_TABLE] -- invalid column type
CREATE TABLE itest_err_1 (a text generated by default as identity)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtestxx_2" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 22)) STORED
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_namespace
;
----------
[CREATE_TABLE] CREATE TABLE orders (
    order_id     bigint not null,
    cust_id      bigint not null,
    status       text
) PARTITION BY HASH (order_id)
;
----------
[CREATE_TABLE] create table idxpart2 partition of idxpart for values from (100) to (1000)
  partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE split_alter_table.base_table (
    id integer NOT NULL,
    code text NOT NULL,
    payload text,
    old_payload varchar(10),
    generated_col integer GENERATED ALWAYS AS (id * 2) STORED,
    identity_col integer GENERATED BY DEFAULT AS IDENTITY,
    parent_key integer,
    CONSTRAINT base_table_pk PRIMARY KEY (id)
)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27503_2
;
----------
[TRUNCATE_TABLE] --
-- Check that index expressions and predicates are run as the table's owner
--
TRUNCATE bttest_a
;
----------
[CREATE_TABLE] --
-- Also check dropping a column that existed when the view was made
--

create table tt9 (x int, xx int, y int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION earth() RETURNS float8
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS 'SELECT ''6378168''::float8'
;
