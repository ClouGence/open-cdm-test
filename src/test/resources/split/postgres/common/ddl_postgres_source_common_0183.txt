CREATE TABLE tbl2 (val1 integer, val2 integer)
;
DROP TABLE pt_stats
;
CREATE FUNCTION set_limit(float4)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT VOLATILE PARALLEL UNSAFE
;
CREATE TYPE "public"."stoplight" AS ENUM (
)
;
create index gist_pointidx5 on gist_point_tbl using gist(p) with (fillfactor=9)
;
ALTER TABLE pg_toast.pg_toast_27744 RENAME TO reind_con_toast
;
ANALYZE pg_temp_26.pg_temp_28795
;
-- fails
alter table anothertab alter column atcol1 type boolean using atcol1::int
;
CREATE TABLE "public"."nv_child_2011" (
    "d" "date",
    CONSTRAINT "nv_child_2011_d_check" CHECK ((("d" >= '2011-01-01'::"date") AND ("d" <= '2011-12-31'::"date")))
)
;
create function gstest_data(v integer, out a integer, out b integer)
  returns setof record
  as $f$
    begin
      return query select v, i from generate_series(1,3) i;
    end;
  $f$ language plpgsql
;
create function first_el_transfn(anyarray, anyelement) returns anyarray as
'select $1 || $2' language sql immutable
;
CREATE TABLE plt2_p3 PARTITION OF plt2 FOR VALUES IN ('0006', '0007', '0008', '0011')
;
CREATE FUNCTION isnlt(isbn13, isbn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION isnne(upc, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
do $$
declare x bigint[] := array[1,3,5];
begin
  raise notice 'x = %', x;
end$$
;
create index on gexec_test(c)
;
CREATE VIEW timetz_local_view AS
  SELECT f1 AS dat,
       f1 AT TIME ZONE current_setting('TimeZone') AS dat_at_tz,
       f1 AT TIME ZONE INTERVAL '00:00' AS dat_at_int
  FROM TIMETZ_TBL
  ORDER BY f1
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."e_star" INHERIT "public"."c_star"
;
create table rtest_nothn1 (a int4, b text)
;
CREATE FOREIGN TABLE batch_cp_upd_test1_f
	PARTITION OF batch_cp_upd_test
	FOR VALUES IN (1)
	SERVER loopback
	OPTIONS (table_name 'batch_cp_upd_test1', batch_size '10')
;
-- stable because depends on pg_trgm.word_similarity_threshold

CREATE OPERATOR <<% (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_op,
        COMMUTATOR = '%>>',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
CREATE TABLE test_replica_id_full (x int, y text)
;
CREATE TABLE pa_target (tid integer PRIMARY KEY) PARTITION BY LIST (tid)
;
CREATE EXTENSION IF NOT EXISTS postgis
;
alter table cc2 alter column f1 drop not null
;
CREATE TYPE ctlty1 AS (a int, b text)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_ts_parser
;
drop function convslot_trig2()
;
CREATE TABLE plt3_adv_p2 PARTITION OF plt3_adv FOR VALUES IN ('0003', '0004')
;
-- check that default_tablespace is handled correctly by multi-command
-- ALTER TABLE that includes a tablespace-preserving rewrite
CREATE TABLE testschema.test_tab(a int, b int, c int)
;
CREATE OPERATOR || (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_concat
)
;
ALTER SEQUENCE sequence_test2 RESTART
;
ALTER FUNCTION gbt_text_consistent(internal, text, smallint, oid, internal) PARALLEL SAFE
;
create aggregate build_group(int8, integer) (
  SFUNC = add_group,
  STYPE = int2[]
)
;
alter table perm_part_parent attach partition temp_part_child default
;
-- commands that should succeed

CREATE AGGREGATE priv_testagg1b(priv_testdomain1) (sfunc = int4_sum, stype = bigint)
;
ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p3" FOR VALUES IN ('0009', '0010', '0011')
;
alter table join_foo set (parallel_workers = 0)
;
create index �ͪ�ߩ�Ѧ��듾�index1 on �ͪ�ߩ�Ѧ��듾� using btree (��듾�)
;
ALTER TABLE pg_toast.pg_toast_30880 RENAME TO reind_con_toast
;
CREATE FUNCTION gbt_time_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
drop table if exists pgbench_accounts, pgbench_branches, pgbench_history, pgbench_tellers
;
--
--
--
-- text/ bpchar ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_text_consistent(internal,text,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create aggregate build_group(anyelement, integer) (
  SFUNC = add_group,
  STYPE = anyarray
)
;
CREATE FUNCTION "public"."range_add_bounds"("anymultirange") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$select lower($1) + upper($1)$_$
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.3........"
;
VACUUM (FULL) information_schema.sql_sizing
;
-- not valid constraint, should fail
alter table atacc1 add constraint atacc1_constr_invalid check(test_a is not null) not valid
;
COMMENT ON TEXT SEARCH CONFIGURATION english IS 'configuration for english language'
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE tbl2 (val1 integer, val2 integer)
;
----------
[DROP_TABLE] DROP TABLE pt_stats
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION set_limit(float4)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT VOLATILE PARALLEL UNSAFE
;
----------
[CREATE_TYPE] CREATE TYPE "public"."stoplight" AS ENUM (
)
;
----------
[ADD_INDEX] create index gist_pointidx5 on gist_point_tbl using gist(p) with (fillfactor=9)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27744 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_26.pg_temp_28795
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails
alter table anothertab alter column atcol1 type boolean using atcol1::int
;
----------
[CREATE_TABLE] CREATE TABLE "public"."nv_child_2011" (
    "d" "date",
    CONSTRAINT "nv_child_2011_d_check" CHECK ((("d" >= '2011-01-01'::"date") AND ("d" <= '2011-12-31'::"date")))
)
;
----------
[CREATE_PROG_OBJ] create function gstest_data(v integer, out a integer, out b integer)
  returns setof record
  as $f$
    begin
      return query select v, i from generate_series(1,3) i;
    end;
  $f$ language plpgsql
;
----------
[CREATE_PROG_OBJ] create function first_el_transfn(anyarray, anyelement) returns anyarray as
'select $1 || $2' language sql immutable
;
----------
[CREATE_TABLE] CREATE TABLE plt2_p3 PARTITION OF plt2 FOR VALUES IN ('0006', '0007', '0008', '0011')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(isbn13, isbn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(upc, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[BLOCK] do $$
declare x bigint[] := array[1,3,5];
begin
  raise notice 'x = %', x;
end$$
;
----------
[ADD_INDEX] create index on gexec_test(c)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW timetz_local_view AS
  SELECT f1 AS dat,
       f1 AT TIME ZONE current_setting('TimeZone') AS dat_at_tz,
       f1 AT TIME ZONE INTERVAL '00:00' AS dat_at_int
  FROM TIMETZ_TBL
  ORDER BY f1
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."e_star" INHERIT "public"."c_star"
;
----------
[CREATE_TABLE] create table rtest_nothn1 (a int4, b text)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE batch_cp_upd_test1_f
	PARTITION OF batch_cp_upd_test
	FOR VALUES IN (1)
	SERVER loopback
	OPTIONS (table_name 'batch_cp_upd_test1', batch_size '10')
;
----------
[CREATE_PROG_OBJ] -- stable because depends on pg_trgm.word_similarity_threshold

CREATE OPERATOR <<% (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_op,
        COMMUTATOR = '%>>',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_id_full (x int, y text)
;
----------
[CREATE_TABLE] CREATE TABLE pa_target (tid integer PRIMARY KEY) PARTITION BY LIST (tid)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION IF NOT EXISTS postgis
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table cc2 alter column f1 drop not null
;
----------
[CREATE_TYPE] CREATE TYPE ctlty1 AS (a int, b text)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_ts_parser
;
----------
[DROP_PROG_OBJ] drop function convslot_trig2()
;
----------
[CREATE_TABLE] CREATE TABLE plt3_adv_p2 PARTITION OF plt3_adv FOR VALUES IN ('0003', '0004')
;
----------
[CREATE_TABLE] -- check that default_tablespace is handled correctly by multi-command
-- ALTER TABLE that includes a tablespace-preserving rewrite
CREATE TABLE testschema.test_tab(a int, b int, c int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR || (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_concat
)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test2 RESTART
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_text_consistent(internal, text, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create aggregate build_group(int8, integer) (
  SFUNC = add_group,
  STYPE = int2[]
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table perm_part_parent attach partition temp_part_child default
;
----------
[CREATE_PROG_OBJ] -- commands that should succeed

CREATE AGGREGATE priv_testagg1b(priv_testdomain1) (sfunc = int4_sum, stype = bigint)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p3" FOR VALUES IN ('0009', '0010', '0011')
;
----------
[ALTER_TABLE] alter table join_foo set (parallel_workers = 0)
;
----------
[ADD_INDEX] create index �ͪ�ߩ�Ѧ��듾�index1 on �ͪ�ߩ�Ѧ��듾� using btree (��듾�)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30880 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] drop table if exists pgbench_accounts, pgbench_branches, pgbench_history, pgbench_tellers
;
----------
[CREATE_PROG_OBJ] --
--
--
-- text/ bpchar ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_text_consistent(internal,text,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] create aggregate build_group(anyelement, integer) (
  SFUNC = add_group,
  STYPE = anyarray
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."range_add_bounds"("anymultirange") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$select lower($1) + upper($1)$_$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.3........"
;
----------
[ADMIN_TABLE] VACUUM (FULL) information_schema.sql_sizing
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- not valid constraint, should fail
alter table atacc1 add constraint atacc1_constr_invalid check(test_a is not null) not valid
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION english IS 'configuration for english language'
;
