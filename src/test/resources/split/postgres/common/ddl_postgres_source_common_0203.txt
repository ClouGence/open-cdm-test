-- clean up
drop table excluded
;
TRUNCATE truncate_tab
;
drop table attest1
;
ALTER TABLE ONLY "public"."mlparted_defd" DROP COLUMN "........pg.dropped.7........"
;
ALTER OPERATOR FAMILY alt_opf18 USING btree DROP OPERATOR 1 (int4, int4)
;
CREATE OR REPLACE RULE y_rule AS ON INSERT TO y
  DO INSTEAD (NOTIFY foo; NOTIFY bar)
;
CREATE TABLE drop_stats_test_subxact()
;
CREATE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a < 10
  WITH CHECK OPTION
;
ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (issn13, ean13),
	OPERATOR 1  = (issn13, issn)
;
DROP FUNCTION func_footrg_mod_a()
;
CREATE OR REPLACE FUNCTION end_command()
RETURNS event_trigger AS $$
BEGIN
RAISE NOTICE '% - ddl_command_end', tg_tag;
END;
$$ LANGUAGE plpgsql
;
ALTER TABLE pg_description ALTER COLUMN description SET NOT NULL
;
-- 5.25 DATA_TYPE_PRIVILEGES view appears later.


/*
 * 5.26
 * DIRECT_SUPERTABLES view
 */

-- feature not supported


/*
 * 5.27
 * DIRECT_SUPERTYPES view
 */

-- feature not supported


/*
 * 5.28
 * DOMAIN_CONSTRAINTS view
 */

CREATE VIEW domain_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(n.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(CASE WHEN condeferrable THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS is_deferrable,
           CAST(CASE WHEN condeferred THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS initially_deferred
    FROM pg_namespace rs, pg_namespace n, pg_constraint con, pg_type t
    WHERE rs.oid = con.connamespace
          AND n.oid = t.typnamespace
          AND t.oid = con.contypid
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
ALTER INDEX pg_toast.pg_toast_28509_index RENAME TO reind_con_toast_idx
;
CREATE TABLE project (project_no int NOT NULL PRIMARY KEY, description text NOT NULL, project_manager int NOT NULL)
;
CREATE TABLE "fkpart3"."pk4" (
    "a" integer NOT NULL
)
;
ANALYZE pubme.t0
;
ALTER TABLE pg_toast.pg_toast_33530 RENAME TO reind_con_toast
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_index
;
CREATE TABLE split_alter_table.inherit_parent (id integer, code text)
;
ALTER TABLE split_alter_table.base_table VALIDATE CONSTRAINT base_table_payload_check
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
DROP FUNCTION priv_testfunc1(int)
;
CREATE TABLE hats (
	hat_name    char(10) primary key,
	hat_color   char(10)      -- hat color
)
;
create table trigger_ddl_table (
   col1 integer,
   col2 integer
)
;
-- check that renaming to an existent value fails
ALTER TYPE rainbow RENAME VALUE 'blue' TO 'green'
;
TRUNCATE pa_target
;
ALTER TABLE ONLY notnull_tbl6 DROP CONSTRAINT ann
;
ALTER TEXT SEARCH CONFIGURATION finnish ADD MAPPING
    FOR word, hword_part, hword
	WITH finnish_stem
;
ALTER SEQUENCE sequence_test2 RESTART WITH -24
  NO CYCLE
;
create function f_immutable_int4(i integer) returns integer as
$$ begin return i; end; $$ language plpgsql immutable
;
COMMENT ON COLUMN dump_test.test_table.col1
					   IS 'comment on column'
;
create table agg_group_3 as
select (g/2)::numeric as c1, sum(7::int4) as c2, count(*) as c3
  from agg_data_2k group by g/2
;
CREATE VIEW pg_stat_activity AS
    SELECT
            S.datid AS datid,
            D.datname AS datname,
            S.pid,
            S.leader_pid,
            S.usesysid,
            U.rolname AS usename,
            S.application_name,
            S.client_addr,
            S.client_hostname,
            S.client_port,
            S.backend_start,
            S.xact_start,
            S.query_start,
            S.state_change,
            S.wait_event_type,
            S.wait_event,
            S.state,
            S.backend_xid,
            s.backend_xmin,
            S.query_id,
            S.query,
            S.backend_type
    FROM pg_stat_get_activity(NULL) AS S
        LEFT JOIN pg_database AS D ON (S.datid = D.oid)
        LEFT JOIN pg_authid AS U ON (S.usesysid = U.oid)
;
vacuum tenk1
;
ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg2
;
COMMENT ON TRIGGER check_fkeys2_pkey_exist ON fkeys2 IS 'right'
;
ANALYZE pg_temp_95.pg_temp_33804_2
;
CREATE FUNCTION alt_func2(int) RETURNS int LANGUAGE sql
  AS 'SELECT $1 - 1'
;
CREATE TABLE "public"."gtest28a" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2)) STORED
)
;
CREATE OPERATOR CLASS gist_macaddr8_ops
DEFAULT FOR TYPE macaddr8 USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_macad8_consistent (internal, macaddr8, int2, oid, internal),
	FUNCTION	2	gbt_macad8_union (internal, internal),
	FUNCTION	3	gbt_macad8_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_macad8_penalty (internal, internal, internal),
	FUNCTION	6	gbt_macad8_picksplit (internal, internal),
	FUNCTION	7	gbt_macad8_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
ALTER INDEX pg_toast.pg_toast_33438_index RENAME TO reind_con_toast_idx
;
alter table at_partitioned alter column b type numeric using b::numeric
;
CREATE TABLE trunc_fa (col2a text) INHERITS (trunc_f)
;
CREATE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a<10
;
CREATE FUNCTION get_val_in_shmem() RETURNS INT
	AS '$libdir/test_dsm_registry' LANGUAGE C
;
CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_MWBU'
;
CREATE TABLE tbl_include_unique2 (c1 int, c2 int, c3 int, c4 box)
;
CREATE TABLE prt2_p2 PARTITION OF prt2 FOR VALUES FROM (250) TO (500)
;
CREATE TEMP TABLE temp_table (
    id INT PRIMARY KEY
)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] -- clean up
drop table excluded
;
----------
[TRUNCATE_TABLE] TRUNCATE truncate_tab
;
----------
[DROP_TABLE] drop table attest1
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_defd" DROP COLUMN "........pg.dropped.7........"
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf18 USING btree DROP OPERATOR 1 (int4, int4)
;
----------
[CREATE_POLICY] CREATE OR REPLACE RULE y_rule AS ON INSERT TO y
  DO INSTEAD (NOTIFY foo; NOTIFY bar)
;
----------
[CREATE_TABLE] CREATE TABLE drop_stats_test_subxact()
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a < 10
  WITH CHECK OPTION
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (issn13, ean13),
	OPERATOR 1  = (issn13, issn)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION func_footrg_mod_a()
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION end_command()
RETURNS event_trigger AS $$
BEGIN
RAISE NOTICE '% - ddl_command_end', tg_tag;
END;
$$ LANGUAGE plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pg_description ALTER COLUMN description SET NOT NULL
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- 5.25 DATA_TYPE_PRIVILEGES view appears later.


/*
 * 5.26
 * DIRECT_SUPERTABLES view
 */

-- feature not supported


/*
 * 5.27
 * DIRECT_SUPERTYPES view
 */

-- feature not supported


/*
 * 5.28
 * DOMAIN_CONSTRAINTS view
 */

CREATE VIEW domain_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(n.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(CASE WHEN condeferrable THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS is_deferrable,
           CAST(CASE WHEN condeferred THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS initially_deferred
    FROM pg_namespace rs, pg_namespace n, pg_constraint con, pg_type t
    WHERE rs.oid = con.connamespace
          AND n.oid = t.typnamespace
          AND t.oid = con.contypid
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28509_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE project (project_no int NOT NULL PRIMARY KEY, description text NOT NULL, project_manager int NOT NULL)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk4" (
    "a" integer NOT NULL
)
;
----------
[ADMIN_TABLE] ANALYZE pubme.t0
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33530 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_index
;
----------
[CREATE_TABLE] CREATE TABLE split_alter_table.inherit_parent (id integer, code text)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE split_alter_table.base_table VALIDATE CONSTRAINT base_table_payload_check
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
----------
[DROP_PROG_OBJ] DROP FUNCTION priv_testfunc1(int)
;
----------
[CREATE_TABLE] CREATE TABLE hats (
	hat_name    char(10) primary key,
	hat_color   char(10)      -- hat color
)
;
----------
[CREATE_TABLE] create table trigger_ddl_table (
   col1 integer,
   col2 integer
)
;
----------
[ALTER_TYPE] -- check that renaming to an existent value fails
ALTER TYPE rainbow RENAME VALUE 'blue' TO 'green'
;
----------
[TRUNCATE_TABLE] TRUNCATE pa_target
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE ONLY notnull_tbl6 DROP CONSTRAINT ann
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION finnish ADD MAPPING
    FOR word, hword_part, hword
	WITH finnish_stem
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test2 RESTART WITH -24
  NO CYCLE
;
----------
[CREATE_PROG_OBJ] create function f_immutable_int4(i integer) returns integer as
$$ begin return i; end; $$ language plpgsql immutable
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN dump_test.test_table.col1
					   IS 'comment on column'
;
----------
[CREATE_TABLE|SELECT] create table agg_group_3 as
select (g/2)::numeric as c1, sum(7::int4) as c2, count(*) as c3
  from agg_data_2k group by g/2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_activity AS
    SELECT
            S.datid AS datid,
            D.datname AS datname,
            S.pid,
            S.leader_pid,
            S.usesysid,
            U.rolname AS usename,
            S.application_name,
            S.client_addr,
            S.client_hostname,
            S.client_port,
            S.backend_start,
            S.xact_start,
            S.query_start,
            S.state_change,
            S.wait_event_type,
            S.wait_event,
            S.state,
            S.backend_xid,
            s.backend_xmin,
            S.query_id,
            S.query,
            S.backend_type
    FROM pg_stat_get_activity(NULL) AS S
        LEFT JOIN pg_database AS D ON (S.datid = D.oid)
        LEFT JOIN pg_authid AS U ON (S.usesysid = U.oid)
;
----------
[ADMIN_TABLE] vacuum tenk1
;
----------
[RENAME_PROG_OBJ] ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg2
;
----------
[COMMENT_TRIGGER] COMMENT ON TRIGGER check_fkeys2_pkey_exist ON fkeys2 IS 'right'
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_95.pg_temp_33804_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION alt_func2(int) RETURNS int LANGUAGE sql
  AS 'SELECT $1 - 1'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest28a" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2)) STORED
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_macaddr8_ops
DEFAULT FOR TYPE macaddr8 USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_macad8_consistent (internal, macaddr8, int2, oid, internal),
	FUNCTION	2	gbt_macad8_union (internal, internal),
	FUNCTION	3	gbt_macad8_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_macad8_penalty (internal, internal, internal),
	FUNCTION	6	gbt_macad8_picksplit (internal, internal),
	FUNCTION	7	gbt_macad8_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33438_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table at_partitioned alter column b type numeric using b::numeric
;
----------
[CREATE_TABLE] CREATE TABLE trunc_fa (col2a text) INHERITS (trunc_f)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a<10
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION get_val_in_shmem() RETURNS INT
	AS '$libdir/test_dsm_registry' LANGUAGE C
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_MWBU'
;
----------
[CREATE_TABLE] CREATE TABLE tbl_include_unique2 (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_TABLE] CREATE TABLE prt2_p2 PARTITION OF prt2 FOR VALUES FROM (250) TO (500)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temp_table (
    id INT PRIMARY KEY
)
;
