CALL in_out(1, NULL, 1)
;
-- test compression with inheritance
CREATE TABLE cminh() INHERITS(cmdata, cmdata1)
;
CREATE TABLE tab_copy (a int PRIMARY KEY)
;
DROP TABLE test
;
CREATE TABLE will_shrink (a int, b text)
;
DROP TABLE text_hashp
;
/*
 * 5.59
 * SQL_PARTS table
 */

CREATE TABLE sql_parts (
    feature_id      character_data,
    feature_name    character_data,
    is_supported    yes_or_no,
    is_verified_by  character_data,
    comments        character_data
)
;
VACUUM (PROCESS_TOAST FALSE) information_schema.sql_sizing
;
ALTER TABLE ONLY "public"."rewritemetoo1" OF "public"."rewritetype"
;
CREATE PROCEDURE regress_zeropriv_proc() LANGUAGE sql AS ''
;
ALTER INDEX pg_toast.pg_toast_28352_index RENAME TO reind_con_toast_idx
;
ALTER FUNCTION gbt_float4_consistent(internal, real, smallint, oid, internal) PARALLEL SAFE
;
ALTER TABLE ATACC2 INHERIT ATACC1
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_event_trigger
;
-- test that inhcount is updated correctly through multiple inheritance
create table inh_pp1 (f1 int)
;
CREATE FUNCTION alt_func2(int) RETURNS int LANGUAGE sql
  AS 'SELECT $1 - 2'
;
CREATE SCHEMA split_postgis
;
ALTER TABLE ONLY "public"."beta_pos" ATTACH PARTITION "public"."beta_pos_p1" FOR VALUES IN ('0002', '0003')
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '38354'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
CREATE PROCEDURE test_proc7c(x int, INOUT a int, INOUT b numeric)
LANGUAGE plpgsql
AS $$
BEGIN
  a := x / 10;
  b := x / 2;
  COMMIT;
END;
$$
;
CREATE SCHEMA split_builtin_trigger
;
ALTER TABLE test_like_6 DROP COLUMN c
;
CREATE UNIQUE INDEX "covidxpart2_a_b_idx" ON "public"."covidxpart2" USING "btree" ("a") INCLUDE ("b")
;
DROP DATABASE conflict_db
;
--
-- bt_page_items(text, int4)
--
DROP FUNCTION bt_page_items(text, int4)
;
create table part_abc (a int, b text, c bool) partition by list (a)
;
-- Test suitability of affix and dict files
CREATE TEXT SEARCH DICTIONARY hunspell_err (
						Template=ispell,
						DictFile=ispell_sample,
						AffFile=hunspell_sample_long
)
;
CREATE TABLE gtest24r (a int PRIMARY KEY, b gtestdomain1range GENERATED ALWAYS AS (gtestdomain1range(a, a + 5)) STORED)
;
-- DISTINCT not supported
CREATE VIEW ro_view2 AS SELECT a, b FROM base_tbl GROUP BY a, b
;
CREATE TEMPORARY TABLE x (n integer)
;
ANALYZE pg_temp_3.pg_temp_29555_2
;
ALTER TABLE ONLY "public"."mlparted_defd" DROP COLUMN "........pg.dropped.6........"
;
CREATE TABLE "public"."shoelace_ok" (
    "ok_name" character(10),
    "ok_quant" integer
)
;
CREATE TABLE "fkpart3"."pk2" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer NOT NULL
)
;
create materialized view transition_test_mv as select * from new_table
;
-- test parallel nestloop join path with materialization of the inner path
alter table tenk2 set (parallel_workers = 0)
;
CREATE DATABASE test
;
ALTER INDEX pg_toast.pg_toast_30777_index RENAME TO reind_con_toast_idx
;
-- check that wholerow vars appearing in WITH CHECK OPTION constraint expressions
-- work fine with partitioned tables
create table wcowrtest (a int) partition by list (a)
;
ALTER TABLE pg_temp_12.pg_temp_26358_2 ADD COLUMN newdata pg_temp_12.pg_temp_26358
;
DROP TABLE r1
;
create type int8alias1 (
    input = int8alias1in,
    output = int8alias1out,
    like = int8
)
;
CREATE EXTENSION IF NOT EXISTS "pgrowlocks"
;
ALTER PUBLICATION testpub_foralltables SET (publish = 'insert, update')
;
DROP FUNCTION functest1(a int)
;
ALTER TABLE ONLY "public"."trigger_parted_p1_1"
    ADD CONSTRAINT "trigger_parted_p1_1_pkey" PRIMARY KEY ("a")
;
CREATE TABLE "public"."gtest_parent" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
PARTITION BY RANGE ("f1")
;
CREATE UNIQUE INDEX "num_exp_ln_idx" ON "public"."num_exp_ln" USING "btree" ("id")
;
-- Test creation and destruction of shell types
CREATE TYPE shell
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=27783 host=/tmp/WPXzmhPeul dbname=postgres' PUBLICATION pub2
;
------------------------------------------------------------------------------------------
[CALL_PROG_OBJ] CALL in_out(1, NULL, 1)
;
----------
[CREATE_TABLE] -- test compression with inheritance
CREATE TABLE cminh() INHERITS(cmdata, cmdata1)
;
----------
[CREATE_TABLE] CREATE TABLE tab_copy (a int PRIMARY KEY)
;
----------
[DROP_TABLE] DROP TABLE test
;
----------
[CREATE_TABLE] CREATE TABLE will_shrink (a int, b text)
;
----------
[DROP_TABLE] DROP TABLE text_hashp
;
----------
[CREATE_TABLE] /*
 * 5.59
 * SQL_PARTS table
 */

CREATE TABLE sql_parts (
    feature_id      character_data,
    feature_name    character_data,
    is_supported    yes_or_no,
    is_verified_by  character_data,
    comments        character_data
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) information_schema.sql_sizing
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."rewritemetoo1" OF "public"."rewritetype"
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE regress_zeropriv_proc() LANGUAGE sql AS ''
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28352_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float4_consistent(internal, real, smallint, oid, internal) PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE ATACC2 INHERIT ATACC1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_event_trigger
;
----------
[CREATE_TABLE] -- test that inhcount is updated correctly through multiple inheritance
create table inh_pp1 (f1 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION alt_func2(int) RETURNS int LANGUAGE sql
  AS 'SELECT $1 - 2'
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_postgis
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."beta_pos" ATTACH PARTITION "public"."beta_pos_p1" FOR VALUES IN ('0002', '0003')
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '38354'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc7c(x int, INOUT a int, INOUT b numeric)
LANGUAGE plpgsql
AS $$
BEGIN
  a := x / 10;
  b := x / 2;
  COMMIT;
END;
$$
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_builtin_trigger
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE test_like_6 DROP COLUMN c
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "covidxpart2_a_b_idx" ON "public"."covidxpart2" USING "btree" ("a") INCLUDE ("b")
;
----------
[DROP_CATALOG] DROP DATABASE conflict_db
;
----------
[DROP_PROG_OBJ] --
-- bt_page_items(text, int4)
--
DROP FUNCTION bt_page_items(text, int4)
;
----------
[CREATE_TABLE] create table part_abc (a int, b text, c bool) partition by list (a)
;
----------
[CREATE_POLICY] -- Test suitability of affix and dict files
CREATE TEXT SEARCH DICTIONARY hunspell_err (
						Template=ispell,
						DictFile=ispell_sample,
						AffFile=hunspell_sample_long
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest24r (a int PRIMARY KEY, b gtestdomain1range GENERATED ALWAYS AS (gtestdomain1range(a, a + 5)) STORED)
;
----------
[CREATE_VIEW(SELECT)] -- DISTINCT not supported
CREATE VIEW ro_view2 AS SELECT a, b FROM base_tbl GROUP BY a, b
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE x (n integer)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_29555_2
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_defd" DROP COLUMN "........pg.dropped.6........"
;
----------
[CREATE_TABLE] CREATE TABLE "public"."shoelace_ok" (
    "ok_name" character(10),
    "ok_quant" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk2" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer NOT NULL
)
;
----------
[CREATE_VIEW(SELECT)] create materialized view transition_test_mv as select * from new_table
;
----------
[ALTER_TABLE] -- test parallel nestloop join path with materialization of the inner path
alter table tenk2 set (parallel_workers = 0)
;
----------
[CREATE_CATALOG] CREATE DATABASE test
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30777_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- check that wholerow vars appearing in WITH CHECK OPTION constraint expressions
-- work fine with partitioned tables
create table wcowrtest (a int) partition by list (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26358_2 ADD COLUMN newdata pg_temp_12.pg_temp_26358
;
----------
[DROP_TABLE] DROP TABLE r1
;
----------
[CREATE_TYPE] create type int8alias1 (
    input = int8alias1in,
    output = int8alias1out,
    like = int8
)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION IF NOT EXISTS "pgrowlocks"
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_foralltables SET (publish = 'insert, update')
;
----------
[DROP_PROG_OBJ] DROP FUNCTION functest1(a int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."trigger_parted_p1_1"
    ADD CONSTRAINT "trigger_parted_p1_1_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest_parent" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
PARTITION BY RANGE ("f1")
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "num_exp_ln_idx" ON "public"."num_exp_ln" USING "btree" ("id")
;
----------
[CREATE_TYPE] -- Test creation and destruction of shell types
CREATE TYPE shell
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=27783 host=/tmp/WPXzmhPeul dbname=postgres' PUBLICATION pub2
;
