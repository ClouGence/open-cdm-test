SET SESSION AUTHORIZATION regress_rule_user1
;
ROLLBACK TO SAVEPOINT a
;
ALTER TABLE public.partr_def2 OWNER TO zyc
;
COPY pg_catalog.pg_sequence (seqrelid, seqtypid, seqstart, seqincrement, seqmax, seqmin, seqcache, seqcycle) TO stdout
;
rollback to savepoint s1
;
GRANT SELECT ON my_credit_card_usage_normal TO public
;
-- this should be ok since only s.a is accessed
BEGIN
;
SAVEPOINT sp
;
-- test GRANTED BY with DROP OWNED and REASSIGN OWNED
GRANT regress_priv_user1 TO regress_priv_user2 WITH ADMIN OPTION
;
REVOKE ALL PRIVILEGES ON FUNCTION testagg2(int2) FROM CURRENT_USER
;
------------------------------------------------------------------------------------------
[SWITCH_USER] SET SESSION AUTHORIZATION regress_rule_user1
;
----------
[TRANSACTION] ROLLBACK TO SAVEPOINT a
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.partr_def2 OWNER TO zyc
;
----------
[DATA_EXPORT] COPY pg_catalog.pg_sequence (seqrelid, seqtypid, seqstart, seqincrement, seqmax, seqmin, seqcache, seqcycle) TO stdout
;
----------
[TRANSACTION] rollback to savepoint s1
;
----------
[GRANT] GRANT SELECT ON my_credit_card_usage_normal TO public
;
----------
[TRANSACTION] -- this should be ok since only s.a is accessed
BEGIN
;
----------
[TRANSACTION] SAVEPOINT sp
;
----------
[GRANT] -- test GRANTED BY with DROP OWNED and REASSIGN OWNED
GRANT regress_priv_user1 TO regress_priv_user2 WITH ADMIN OPTION
;
----------
[REVOKE] REVOKE ALL PRIVILEGES ON FUNCTION testagg2(int2) FROM CURRENT_USER
;
