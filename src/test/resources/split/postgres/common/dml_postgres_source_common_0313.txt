INSERT INTO gtest26 (a) VALUES (-2), (0), (3)
;
INSERT INTO public.order_summary SELECT (diff.newdata).* FROM pg_temp_9.pg_temp_38106_2 diff WHERE tid IS NULL
;
INSERT INTO "S 1"."T 1"
	SELECT id,
	       id % 10,
	       to_char(id, 'FM00000'),
	       '1970-01-01'::timestamptz + ((id % 100) || ' days')::interval,
	       '1970-01-01'::timestamp + ((id % 100) || ' days')::interval,
	       id % 10,
	       id % 10,
	       'foo'::user_enum
	FROM generate_series(1, 1000) id
;
DELETE FROM ro_view1
;
-- an empty sub element

delete from test_jsonb_subscript
;
INSERT INTO public.ths_p1 VALUES (4, 834)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11961', relminmxid = '1'
WHERE oid = '"testxmlschema"."test1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15674', relminmxid = '3'
WHERE oid = '"public"."test_type_diff2"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."fk_partitioned_fk_2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12898', relminmxid = '3'
WHERE oid = '"fkpart4"."droppk21"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9023', relminmxid = '1'
WHERE oid = '"public"."shoelace_data"'::pg_catalog.regclass
;
-- no partition exists, nor does mcrparted3 accept it
insert into mcrparted values (11, 1, -1)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'name'
  AND attrelid = '"public"."emp"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12323', relminmxid = '1'
WHERE oid = '"public"."ruletest_tbl2"'::pg_catalog.regclass
;
INSERT INTO fk_partitioned_fk_3 (a,b) VALUES (2500, 2502)
;
-- with a conflict
INSERT INTO temporal_mltrng3 (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2005-01-01', '2006-01-01'))) ON CONFLICT ON CONSTRAINT temporal_mltrng3_uq DO NOTHING
;
-- fail (even the parent's, ie, part_ee_ff's partition constraint applies)
insert into part_ee_ff1 values ('cc', 1)
;
insert into boolpart values (true), (false), (null)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."pagg_tab_p3"'::pg_catalog.regclass
;
INSERT INTO sql_sizing VALUES (97,    'MAXIMUM COLUMNS IN GROUP BY', 0, NULL)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13657', relminmxid = '3'
WHERE oid = '"public"."iso8859_5_inputs"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17558', relminmxid = '3'
WHERE oid = '39882'
;
-- and again with duplicate source rows
INSERT INTO source VALUES (5, 50)
;
INSERT INTO public.mvtest_mv_v SELECT (diff.newdata).* FROM pg_temp_9.pg_temp_26761_2 diff WHERE tid IS NULL
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1591', relminmxid = '1'
WHERE oid = '17493'
;
-- horizontal
INSERT INTO LSEG_TBL VALUES ('[(NaN,1),(NaN,90)]')
;
INSERT INTO pg_temp_13.pg_temp_26049_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_13.pg_temp_26049 AS newdata FROM public.mvtest_boxmv mv FULL JOIN pg_temp_13.pg_temp_26049 newdata ON (newdata.id OPERATOR(pg_catalog.=) mv.id AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16969', relminmxid = '3'
WHERE oid = '"regress_indexing"."pk3"'::pg_catalog.regclass
;
INSERT INTO tr_sub(path) VALUES ('1-top-2-1-#2')
;
INSERT INTO tab_1 SELECT generate_series(1,10)
;
INSERT INTO query VALUES ('/menu/beers/cost[text() = ''lots'']')
;
INSERT INTO serialTest2 (f1, f5)
  VALUES ('bogus', -9223372036854775809)
;
INSERT INTO num_exp_ln VALUES (2,'17.35177750493897715514')
;
INSERT INTO s4.t1 (i, b, ia, ir, t)
				(SELECT gs::INTEGER AS i,
						box(point(gs,gs+5),point(gs*2,gs*3)) AS b,
						array[gs, gs + 1]::int4[] AS ia,
						int4range(gs, gs+100) AS ir,
						repeat('foo', gs) AS t
					 FROM generate_series(1,10000,3000) AS gs)
;
INSERT INTO test_having VALUES (6, 4, 'cccc', 'g')
;
INSERT INTO public.tplain VALUES ('1', 691)
;
INSERT INTO lr_test VALUES('lr_superuser_init')
;
INSERT INTO replication_example(somedata, testcolumn1, testcolumn3) VALUES (8, 6, 1)
;
INSERT INTO public.ths_p3 VALUES (5, 515)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11157', relminmxid = '3'
WHERE oid = '"public"."room"'::pg_catalog.regclass
;
DELETE FROM tst_hstore WHERE a = 1
;
INSERT INTO public.ths_p2 VALUES (7, 557)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16535', relminmxid = '3'
WHERE oid = '"public"."prt2_m_p2"'::pg_catalog.regclass
;
INSERT INTO alive VALUES($$committed-before-sigkill$$) RETURNING status
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."fk3"'::pg_catalog.regclass
  AND attname IN ('a')
;
INSERT INTO gtest23q VALUES (1, 2)
;
-- test create as and select into
insert into atacc1 values (21, 22, 23)
;
INSERT INTO test1 VALUES (1), (2), (3)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8438', relminmxid = '1'
WHERE oid = '"public"."rtest_order2"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('0', 680)
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] INSERT INTO gtest26 (a) VALUES (-2), (0), (3)
;
----------
[INSERT|SELECT] INSERT INTO public.order_summary SELECT (diff.newdata).* FROM pg_temp_9.pg_temp_38106_2 diff WHERE tid IS NULL
;
----------
[INSERT|SELECT] INSERT INTO "S 1"."T 1"
	SELECT id,
	       id % 10,
	       to_char(id, 'FM00000'),
	       '1970-01-01'::timestamptz + ((id % 100) || ' days')::interval,
	       '1970-01-01'::timestamp + ((id % 100) || ' days')::interval,
	       id % 10,
	       id % 10,
	       'foo'::user_enum
	FROM generate_series(1, 1000) id
;
----------
[DELETE] DELETE FROM ro_view1
;
----------
[DELETE] -- an empty sub element

delete from test_jsonb_subscript
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 834)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11961', relminmxid = '1'
WHERE oid = '"testxmlschema"."test1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15674', relminmxid = '3'
WHERE oid = '"public"."test_type_diff2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."fk_partitioned_fk_2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12898', relminmxid = '3'
WHERE oid = '"fkpart4"."droppk21"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9023', relminmxid = '1'
WHERE oid = '"public"."shoelace_data"'::pg_catalog.regclass
;
----------
[INSERT] -- no partition exists, nor does mcrparted3 accept it
insert into mcrparted values (11, 1, -1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'name'
  AND attrelid = '"public"."emp"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '12323', relminmxid = '1'
WHERE oid = '"public"."ruletest_tbl2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO fk_partitioned_fk_3 (a,b) VALUES (2500, 2502)
;
----------
[MERGE|SELECT] -- with a conflict
INSERT INTO temporal_mltrng3 (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2005-01-01', '2006-01-01'))) ON CONFLICT ON CONSTRAINT temporal_mltrng3_uq DO NOTHING
;
----------
[INSERT] -- fail (even the parent's, ie, part_ee_ff's partition constraint applies)
insert into part_ee_ff1 values ('cc', 1)
;
----------
[INSERT] insert into boolpart values (true), (false), (null)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."pagg_tab_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO sql_sizing VALUES (97,    'MAXIMUM COLUMNS IN GROUP BY', 0, NULL)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13657', relminmxid = '3'
WHERE oid = '"public"."iso8859_5_inputs"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17558', relminmxid = '3'
WHERE oid = '39882'
;
----------
[INSERT] -- and again with duplicate source rows
INSERT INTO source VALUES (5, 50)
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_mv_v SELECT (diff.newdata).* FROM pg_temp_9.pg_temp_26761_2 diff WHERE tid IS NULL
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1591', relminmxid = '1'
WHERE oid = '17493'
;
----------
[INSERT] -- horizontal
INSERT INTO LSEG_TBL VALUES ('[(NaN,1),(NaN,90)]')
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_13.pg_temp_26049_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_13.pg_temp_26049 AS newdata FROM public.mvtest_boxmv mv FULL JOIN pg_temp_13.pg_temp_26049 newdata ON (newdata.id OPERATOR(pg_catalog.=) mv.id AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16969', relminmxid = '3'
WHERE oid = '"regress_indexing"."pk3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO tr_sub(path) VALUES ('1-top-2-1-#2')
;
----------
[INSERT|SELECT] INSERT INTO tab_1 SELECT generate_series(1,10)
;
----------
[INSERT] INSERT INTO query VALUES ('/menu/beers/cost[text() = ''lots'']')
;
----------
[INSERT|SELECT] INSERT INTO serialTest2 (f1, f5)
  VALUES ('bogus', -9223372036854775809)
;
----------
[INSERT] INSERT INTO num_exp_ln VALUES (2,'17.35177750493897715514')
;
----------
[INSERT|SELECT] INSERT INTO s4.t1 (i, b, ia, ir, t)
				(SELECT gs::INTEGER AS i,
						box(point(gs,gs+5),point(gs*2,gs*3)) AS b,
						array[gs, gs + 1]::int4[] AS ia,
						int4range(gs, gs+100) AS ir,
						repeat('foo', gs) AS t
					 FROM generate_series(1,10000,3000) AS gs)
;
----------
[INSERT] INSERT INTO test_having VALUES (6, 4, 'cccc', 'g')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 691)
;
----------
[INSERT] INSERT INTO lr_test VALUES('lr_superuser_init')
;
----------
[INSERT|SELECT] INSERT INTO replication_example(somedata, testcolumn1, testcolumn3) VALUES (8, 6, 1)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 515)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11157', relminmxid = '3'
WHERE oid = '"public"."room"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM tst_hstore WHERE a = 1
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 557)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16535', relminmxid = '3'
WHERE oid = '"public"."prt2_m_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO alive VALUES($$committed-before-sigkill$$) RETURNING status
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."fk3"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[INSERT] INSERT INTO gtest23q VALUES (1, 2)
;
----------
[INSERT] -- test create as and select into
insert into atacc1 values (21, 22, 23)
;
----------
[INSERT] INSERT INTO test1 VALUES (1), (2), (3)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8438', relminmxid = '1'
WHERE oid = '"public"."rtest_order2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 680)
;
