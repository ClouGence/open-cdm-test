-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16506', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10856', relminmxid = '3'
WHERE oid = '"fkpart3"."pk4"'::pg_catalog.regclass
;
INSERT INTO t_test VALUES (0)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3649', relminmxid = '1'
WHERE oid = '20801'
;
INSERT INTO public.tplain VALUES ('2', 702)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17282', relminmxid = '3'
WHERE oid = '39590'
;
INSERT INTO tab_upgraded1 VALUES (generate_series(1,50))
;
insert into insertconflicttest values (16, 'Melon') on conflict (key, key, key) do update set fruit = excluded.fruit
;
INSERT INTO public.tplain VALUES ('3', 853)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO public.ths_p3 VALUES (1, 221)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18646', relminmxid = '3'
WHERE oid = '"public"."cmmove2"'::pg_catalog.regclass
;
delete from spgist_point_tbl where id < 5
;
insert into pgbench_tellers(tid,bid,tbalance) values (6,1,0)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9995', relminmxid = '1'
WHERE oid = '"public"."rtest_t1"'::pg_catalog.regclass
;
UPDATE temporal_partitioned_fk_rng2rng SET id = '[4,5)' WHERE id = '[1,2)'
;
DELETE FROM ft2
  USING ft4 LEFT JOIN ft5 ON (ft4.c1 = ft5.c1)
  WHERE ft2.c1 > 1200 AND ft2.c1 % 10 = 0 AND ft2.c2 = ft4.c1
  RETURNING 100
;
INSERT INTO public.tht VALUES ('6', 166)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '20575', relminmxid = '3'
WHERE oid = '46762'
;
INSERT INTO num_exp_power_10_ln VALUES (1,'NaN')
;
INSERT INTO public.tht VALUES ('9', 759)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pagg_tab_ml_p2"'::pg_catalog.regclass
;
insert into bar2 values(7,77,77)
;
INSERT INTO public.ths_p2 VALUES (3, 323)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt3_n_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10848', relminmxid = '1'
WHERE oid = '"public"."mcv_lists_arrays"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18719', relminmxid = '3'
WHERE oid = '41861'
;
INSERT INTO pg_temp_9.pg_temp_38052_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_38052 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_9.pg_temp_38052 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3067', relminmxid = '1'
WHERE oid = '19392'
;
INSERT INTO test_stat_tab(key, value) VALUES('k1', 1), ('k2', 1), ('k3', 1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15805', relminmxid = '3'
WHERE oid = '"public"."tt0"'::pg_catalog.regclass
;
-- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.6........'
  AND attrelid = '"public"."mlparted3"'::pg_catalog.regclass
;
UPDATE rw_view1 SET aa=aa
;
insert into part1 values ('a', 1)
;
insert into PSlot values ('PS.base.a5', 'PF0_1', '', '')
;
-- Nothing should have changed.
INSERT INTO rem1 VALUES (2, 'test2')
;
-- This makes the previous failure go away, so the index can become valid.
DELETE FROM concur_reindex_tab4 WHERE c1 = 1
;
INSERT INTO public.tplain VALUES ('1', 101)
;
INSERT INTO itestv10 VALUES (10, 'xyz')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
;
insert into customers values ( 'Jane Doe' , '67890' )
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '17063'
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."c_star"'::pg_catalog.regclass
  AND attname IN ('class', 'aa', 'a')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12215', relminmxid = '1'
WHERE oid = '"public"."shoelace_data"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('7', 537)
;
INSERT INTO public.tht VALUES ('2', 482)
;
INSERT INTO brin_timestamp_test VALUES ('-infinity'), ('infinity')
;
INSERT INTO public.tht VALUES ('0', 210)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2570', relminmxid = '1'
WHERE oid = '"public"."d_star"'::pg_catalog.regclass
;
INSERT INTO beta_neg SELECT -1.0, i, to_char(i % 10, 'FM0000') FROM generate_series(100, 149) i WHERE i % 10 IN (2, 3, 4, 6, 7, 9)
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16506', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10856', relminmxid = '3'
WHERE oid = '"fkpart3"."pk4"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO t_test VALUES (0)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3649', relminmxid = '1'
WHERE oid = '20801'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 702)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17282', relminmxid = '3'
WHERE oid = '39590'
;
----------
[INSERT] INSERT INTO tab_upgraded1 VALUES (generate_series(1,50))
;
----------
[MERGE] insert into insertconflicttest values (16, 'Melon') on conflict (key, key, key) do update set fruit = excluded.fruit
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 853)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 221)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18646', relminmxid = '3'
WHERE oid = '"public"."cmmove2"'::pg_catalog.regclass
;
----------
[DELETE] delete from spgist_point_tbl where id < 5
;
----------
[INSERT|SELECT] insert into pgbench_tellers(tid,bid,tbalance) values (6,1,0)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9995', relminmxid = '1'
WHERE oid = '"public"."rtest_t1"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE temporal_partitioned_fk_rng2rng SET id = '[4,5)' WHERE id = '[1,2)'
;
----------
[DELETE] DELETE FROM ft2
  USING ft4 LEFT JOIN ft5 ON (ft4.c1 = ft5.c1)
  WHERE ft2.c1 > 1200 AND ft2.c1 % 10 = 0 AND ft2.c2 = ft4.c1
  RETURNING 100
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 166)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '20575', relminmxid = '3'
WHERE oid = '46762'
;
----------
[INSERT] INSERT INTO num_exp_power_10_ln VALUES (1,'NaN')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 759)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pagg_tab_ml_p2"'::pg_catalog.regclass
;
----------
[INSERT] insert into bar2 values(7,77,77)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 323)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt3_n_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10848', relminmxid = '1'
WHERE oid = '"public"."mcv_lists_arrays"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18719', relminmxid = '3'
WHERE oid = '41861'
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_9.pg_temp_38052_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_38052 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_9.pg_temp_38052 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3067', relminmxid = '1'
WHERE oid = '19392'
;
----------
[INSERT|SELECT] INSERT INTO test_stat_tab(key, value) VALUES('k1', 1), ('k2', 1), ('k3', 1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15805', relminmxid = '3'
WHERE oid = '"public"."tt0"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.6........'
  AND attrelid = '"public"."mlparted3"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE rw_view1 SET aa=aa
;
----------
[INSERT] insert into part1 values ('a', 1)
;
----------
[INSERT] insert into PSlot values ('PS.base.a5', 'PF0_1', '', '')
;
----------
[INSERT] -- Nothing should have changed.
INSERT INTO rem1 VALUES (2, 'test2')
;
----------
[DELETE] -- This makes the previous failure go away, so the index can become valid.
DELETE FROM concur_reindex_tab4 WHERE c1 = 1
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 101)
;
----------
[INSERT] INSERT INTO itestv10 VALUES (10, 'xyz')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
;
----------
[INSERT] insert into customers values ( 'Jane Doe' , '67890' )
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '17063'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."c_star"'::pg_catalog.regclass
  AND attname IN ('class', 'aa', 'a')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '12215', relminmxid = '1'
WHERE oid = '"public"."shoelace_data"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('7', 537)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('2', 482)
;
----------
[INSERT] INSERT INTO brin_timestamp_test VALUES ('-infinity'), ('infinity')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 210)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2570', relminmxid = '1'
WHERE oid = '"public"."d_star"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO beta_neg SELECT -1.0, i, to_char(i % 10, 'FM0000') FROM generate_series(100, 149) i WHERE i % 10 IN (2, 3, 4, 6, 7, 9)
;
