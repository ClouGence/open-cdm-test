drop table utrtest
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_init_privs
;
CREATE INDEX brin_test_multi_a_idx ON brin_test_multi USING brin (a) WITH (pages_per_range = 2)
;
alter table inh_pp1 alter column f1 drop not null
;
ALTER TABLE withz ADD UNIQUE (k)
;
vacuum t_gin_test_tbl
;
create table pk4 (like pk)
;
CREATE UNIQUE INDEX t_a_idx ON ONLY t (a)
;
ALTER TABLE pg_toast.pg_toast_2615 SET (fillfactor = '90')
;
CREATE TRIGGER invalid_trig AFTER TRUNCATE ON main_view
EXECUTE PROCEDURE trigger_func('before_tru_row')
;
CREATE TABLE r1 (a int)
;
drop table depth_a, depth_b, depth_c
;
CREATE TABLE plt2_adv_p4 PARTITION OF plt2_adv FOR VALUES IN ('0004', '0005')
;
create table match_source as
  select x as id, x*10 as data, x/10 as ten from generate_series(1,100) x
;
ALTER OPERATOR === (boolean, real) SET (NEGATOR = ===)
;
CREATE TABLE alter_table_under_transition_tables
(
  id int PRIMARY KEY,
  name text
)
;
CREATE PUBLICATION tap_pub_temp1 FOR TABLE temp1 WITH (publish = insert)
;
ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (upc, ean13),
	OPERATOR 2  <= (upc, ean13),
	OPERATOR 3  = (upc, ean13),
	OPERATOR 4  >= (upc, ean13),
	OPERATOR 5  > (upc, ean13),
	FUNCTION 1  btupccmp(upc, ean13)
;
DROP SCHEMA IF EXISTS split_ts_next CASCADE
;
CREATE TABLE alter_idx_rename_test_parted (a INT) PARTITION BY LIST (a)
;
ALTER INDEX public.ths_mod_data_key ATTACH PARTITION public.ths_p1_mod_data_key
;
CREATE INDEX guid1_hash  ON guid1 USING HASH  (guid_field)
;
DO LANGUAGE plpgsql $$
BEGIN
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
    SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
END;
$$
;
DROP TABLE test_replica_identity3
;
-- Test for appropriate cleanup of non-simple expression evaluations
-- (bug in all versions prior to August 2010)

CREATE FUNCTION nonsimple_expr_test() RETURNS text[] AS $$
DECLARE
  arr text[];
  lr text;
  i integer;
BEGIN
  arr := array[array['foo','bar'], array['baz', 'quux']];
  lr := 'fool';
  i := 1;
  -- use sub-SELECTs to make expressions non-simple
  arr[(SELECT i)][(SELECT i+1)] := (SELECT lr);
  RETURN arr;
END;
$$ LANGUAGE plpgsql
;
CREATE INDEX "tenk1_unique2" ON "public"."tenk1" USING "btree" ("unique2")
;
-- cleanup
DROP TABLE fract_t
;
ALTER INDEX IF EXISTS __onek_unique1 RENAME TO attmp_onek_unique1
;
CREATE FUNCTION worker_spi_launch(pg_catalog.int4)
RETURNS pg_catalog.int4 STRICT
AS 'MODULE_PATHNAME'
LANGUAGE C
;
CREATE DATABASE dropme_db2 WITH TABLESPACE dropme_ts2
;
CREATE EVENT TRIGGER always_drop ON sql_drop
EXECUTE PROCEDURE event_trigger_test()
;
CREATE POLICY P ON tbl1 TO regress_rls_eve, regress_rls_frank USING (true)
;
drop table test_multirange_gist
;
CREATE TEXT SEARCH TEMPLATE snowball
	(INIT = dsnowball_init,
	LEXIZE = dsnowball_lexize)
;
CREATE FUNCTION isnne(isbn, isbn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION bt_index_parent_check(index regclass)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
ANALYZE public.itest10
;
CREATE FUNCTION hobby_construct(text, text)
   RETURNS hobbies_r
   AS 'select $1 as name, $2 as hobby'
   LANGUAGE SQL
;
CREATE TABLE test_exists (a int, b text)
;
SECURITY LABEL ON TABLE seclabel_tbl1 IS '...invalid label...'
;
-- To hide the output of do_set_block_offsets()
CREATE TEMP TABLE hideblocks(blockno bigint)
;
do $$ declare a real;
begin a[1] := 2; raise notice 'a = %', a; end$$
;
drop publication pub1
;
alter table header_copytest drop column c
;
-- some complex joins on the source side

CREATE TABLE cj_target (tid integer, balance float, val text)
  WITH (autovacuum_enabled=off)
;
CREATE AGGREGATE case_agg ( -- old syntax
	"Sfunc1" = int4pl,
	"Basetype" = int4,
	"Stype1" = int4,
	"Initcond1" = '0',
	"Parallel" = safe
)
;
CLUSTER VERBOSE split_maint.maint_table USING maint_name_idx
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_shdepend
;
CREATE FUNCTION on_login_proc() RETURNS EVENT_TRIGGER AS $$
BEGIN
  IF NOT pg_is_in_recovery() THEN
    INSERT INTO user_logins (who) VALUES (session_user);
  END IF;
  IF session_user = 'regress_hacker' THEN
    RAISE EXCEPTION 'You are not welcome!';
  END IF;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER
;
CREATE INDEX bloomidx2 ON tst USING bloom (i, t) WITH (length=0)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] drop table utrtest
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_init_privs
;
----------
[ADD_INDEX] CREATE INDEX brin_test_multi_a_idx ON brin_test_multi USING brin (a) WITH (pages_per_range = 2)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table inh_pp1 alter column f1 drop not null
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE withz ADD UNIQUE (k)
;
----------
[ADMIN_TABLE] vacuum t_gin_test_tbl
;
----------
[CREATE_TABLE] create table pk4 (like pk)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t_a_idx ON ONLY t (a)
;
----------
[ALTER_TABLE] ALTER TABLE pg_toast.pg_toast_2615 SET (fillfactor = '90')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig AFTER TRUNCATE ON main_view
EXECUTE PROCEDURE trigger_func('before_tru_row')
;
----------
[CREATE_TABLE] CREATE TABLE r1 (a int)
;
----------
[DROP_TABLE] drop table depth_a, depth_b, depth_c
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p4 PARTITION OF plt2_adv FOR VALUES IN ('0004', '0005')
;
----------
[CREATE_TABLE|SELECT] create table match_source as
  select x as id, x*10 as data, x/10 as ten from generate_series(1,100) x
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, real) SET (NEGATOR = ===)
;
----------
[CREATE_TABLE] CREATE TABLE alter_table_under_transition_tables
(
  id int PRIMARY KEY,
  name text
)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_temp1 FOR TABLE temp1 WITH (publish = insert)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (upc, ean13),
	OPERATOR 2  <= (upc, ean13),
	OPERATOR 3  = (upc, ean13),
	OPERATOR 4  >= (upc, ean13),
	OPERATOR 5  > (upc, ean13),
	FUNCTION 1  btupccmp(upc, ean13)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_ts_next CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE alter_idx_rename_test_parted (a INT) PARTITION BY LIST (a)
;
----------
[ALTER_INDEX] ALTER INDEX public.ths_mod_data_key ATTACH PARTITION public.ths_p1_mod_data_key
;
----------
[ADD_INDEX] CREATE INDEX guid1_hash  ON guid1 USING HASH  (guid_field)
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
    SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
END;
$$
;
----------
[DROP_TABLE] DROP TABLE test_replica_identity3
;
----------
[CREATE_PROG_OBJ] -- Test for appropriate cleanup of non-simple expression evaluations
-- (bug in all versions prior to August 2010)

CREATE FUNCTION nonsimple_expr_test() RETURNS text[] AS $$
DECLARE
  arr text[];
  lr text;
  i integer;
BEGIN
  arr := array[array['foo','bar'], array['baz', 'quux']];
  lr := 'fool';
  i := 1;
  -- use sub-SELECTs to make expressions non-simple
  arr[(SELECT i)][(SELECT i+1)] := (SELECT lr);
  RETURN arr;
END;
$$ LANGUAGE plpgsql
;
----------
[ADD_INDEX] CREATE INDEX "tenk1_unique2" ON "public"."tenk1" USING "btree" ("unique2")
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE fract_t
;
----------
[RENAME_INDEX] ALTER INDEX IF EXISTS __onek_unique1 RENAME TO attmp_onek_unique1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION worker_spi_launch(pg_catalog.int4)
RETURNS pg_catalog.int4 STRICT
AS 'MODULE_PATHNAME'
LANGUAGE C
;
----------
[CREATE_CATALOG] CREATE DATABASE dropme_db2 WITH TABLESPACE dropme_ts2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER always_drop ON sql_drop
EXECUTE PROCEDURE event_trigger_test()
;
----------
[CREATE_POLICY] CREATE POLICY P ON tbl1 TO regress_rls_eve, regress_rls_frank USING (true)
;
----------
[DROP_TABLE] drop table test_multirange_gist
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH TEMPLATE snowball
	(INIT = dsnowball_init,
	LEXIZE = dsnowball_lexize)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(isbn, isbn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_index_parent_check(index regclass)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ADMIN_TABLE] ANALYZE public.itest10
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hobby_construct(text, text)
   RETURNS hobbies_r
   AS 'select $1 as name, $2 as hobby'
   LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE test_exists (a int, b text)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE seclabel_tbl1 IS '...invalid label...'
;
----------
[CREATE_TABLE] -- To hide the output of do_set_block_offsets()
CREATE TEMP TABLE hideblocks(blockno bigint)
;
----------
[BLOCK] do $$ declare a real;
begin a[1] := 2; raise notice 'a = %', a; end$$
;
----------
[DROP_PUB_SUB] drop publication pub1
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table header_copytest drop column c
;
----------
[CREATE_TABLE] -- some complex joins on the source side

CREATE TABLE cj_target (tid integer, balance float, val text)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE case_agg ( -- old syntax
	"Sfunc1" = int4pl,
	"Basetype" = int4,
	"Stype1" = int4,
	"Initcond1" = '0',
	"Parallel" = safe
)
;
----------
[ADMIN_TABLE] CLUSTER VERBOSE split_maint.maint_table USING maint_name_idx
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_shdepend
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION on_login_proc() RETURNS EVENT_TRIGGER AS $$
BEGIN
  IF NOT pg_is_in_recovery() THEN
    INSERT INTO user_logins (who) VALUES (session_user);
  END IF;
  IF session_user = 'regress_hacker' THEN
    RAISE EXCEPTION 'You are not welcome!';
  END IF;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER
;
----------
[ADD_INDEX] CREATE INDEX bloomidx2 ON tst USING bloom (i, t) WITH (length=0)
;
