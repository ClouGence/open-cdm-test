insert into my_table values (3, 'CCC'), (4, 'DDD')
  on conflict (a) do
  update set b = my_table.b || ':' || excluded.b
;
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"fkpart5"."fk3"'::pg_catalog.regclass
  AND attname = v.dname
;
INSERT INTO public.ths_p3 VALUES (0, 980)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt1_e_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
insert into test values('ESPA��OL')
;
UPDATE brintest_bloom SET int8col = int8col * int4col
;
UPDATE inh_parent SET a = 13 WHERE a IN (3, 5)
;
insert into rtest_view1 values (1, 'item 1', 't')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15479', relminmxid = '3'
WHERE oid = '35913'
;
WITH t AS (UPDATE remote_tbl SET c = c || c RETURNING *)
INSERT INTO join_tbl SELECT * FROM async_pt LEFT JOIN t ON (async_pt.a = t.a AND async_pt.b = t.b) WHERE async_pt.b === 505
;
INSERT INTO public.tht VALUES ('1', 31)
;
INSERT INTO public.tht VALUES ('8', 88)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14834', relminmxid = '3'
WHERE oid = '"fkpart3"."pk2"'::pg_catalog.regclass
;
INSERT INTO temporal_partitioned_rng (id, valid_at) VALUES ('[5,6)', daterange('2018-02-01', '2018-03-01'))
;
INSERT INTO public.parallel_mat_view SELECT (diff.newdata).* FROM pg_temp_4.pg_temp_29544_2 diff WHERE tid IS NULL
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17282', relminmxid = '3'
WHERE oid = '39544'
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."covidxpart4"'::pg_catalog.regclass
;
INSERT INTO num_exp_power_10_ln VALUES (8,'167361463828.07491320069016125952')
;
UPDATE pg_catalog.pg_database
SET datfrozenxid = '744', datminmxid = '1'
WHERE datname = 'regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ'
;
INSERT INTO num_exp_mul VALUES (1,0,'0')
;
INSERT INTO public.ths_p2 VALUES (3, 233)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18643', relminmxid = '3'
WHERE oid = '"public"."cmmove1"'::pg_catalog.regclass
;
INSERT INTO test_tablesample SELECT i, repeat(i::text, 1000)
  FROM generate_series(0, 30) s(i)
;
INSERT INTO public.tht VALUES ('5', 455)
;
UPDATE room_reservation SET start_time = TIMESTAMP WITH TIME ZONE '2010-04-01 13:30', end_time = TIMESTAMP WITH TIME ZONE '2010-04-01 14:30' WHERE room_id = '101' AND start_time = TIMESTAMP WITH TIME ZONE '2010-04-01 10:00'
;
INSERT INTO public.tht VALUES ('9', 899)
;
INSERT INTO xacttest (a, b) VALUES (777, 777.777)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15474', relminmxid = '3'
WHERE oid = '"public"."prt1_p2"'::pg_catalog.regclass
;
INSERT INTO tab5 VALUES (2, 22, 222, 2222)
;
INSERT INTO pg_temp_9.pg_temp_38133_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_38133 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_9.pg_temp_38133 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p2_p2"'::pg_catalog.regclass
;
INSERT INTO ceil_floor_round VALUES ('9.4999999')
;
INSERT INTO public.mvtest_tm SELECT (diff.newdata).* FROM pg_temp_10.pg_temp_25340_2 diff WHERE tid IS NULL
;
INSERT INTO public.tplain VALUES ('7', 217)
;
INSERT INTO booltmp VALUES (false), (true)
;
UPDATE test_tab SET b = md5(b)
;
DELETE FROM tst WHERE i = 9
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt1_l_p3"'::pg_catalog.regclass
;
UPDATE pg_class SET reltuples = reltuples * 2
	WHERE oid = 'accounts'::regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20721'
;
DELETE FROM inh_parent WHERE a IN (4, 6)
;
INSERT INTO public.tplain VALUES ('1', 71)
;
insert into part_ee_ff1 values ('ff', 1)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_ml_p3"'::pg_catalog.regclass
  AND attname IN ('b', 'c', 'a')
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (4, 1, 7545, -4313, CURRENT_TIMESTAMP)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17991', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo2"'::pg_catalog.regclass
;
insert into bytea_test_table values(decode('ff','hex'))
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."covidxpart4"'::pg_catalog.regclass
  AND attname IN ('b', 'a')
;
INSERT INTO public.tht VALUES ('0', 710)
;
INSERT INTO public.tht VALUES ('5', 965)
;
------------------------------------------------------------------------------------------
[MERGE] insert into my_table values (3, 'CCC'), (4, 'DDD')
  on conflict (a) do
  update set b = my_table.b || ':' || excluded.b
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"fkpart5"."fk3"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 980)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt1_e_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] insert into test values('ESPA��OL')
;
----------
[UPDATE] UPDATE brintest_bloom SET int8col = int8col * int4col
;
----------
[UPDATE] UPDATE inh_parent SET a = 13 WHERE a IN (3, 5)
;
----------
[INSERT] insert into rtest_view1 values (1, 'item 1', 't')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15479', relminmxid = '3'
WHERE oid = '35913'
;
----------
[INSERT|UPDATE|SELECT] WITH t AS (UPDATE remote_tbl SET c = c || c RETURNING *)
INSERT INTO join_tbl SELECT * FROM async_pt LEFT JOIN t ON (async_pt.a = t.a AND async_pt.b = t.b) WHERE async_pt.b === 505
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 31)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 88)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '14834', relminmxid = '3'
WHERE oid = '"fkpart3"."pk2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO temporal_partitioned_rng (id, valid_at) VALUES ('[5,6)', daterange('2018-02-01', '2018-03-01'))
;
----------
[INSERT|SELECT] INSERT INTO public.parallel_mat_view SELECT (diff.newdata).* FROM pg_temp_4.pg_temp_29544_2 diff WHERE tid IS NULL
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17282', relminmxid = '3'
WHERE oid = '39544'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."covidxpart4"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_power_10_ln VALUES (8,'167361463828.07491320069016125952')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_database
SET datfrozenxid = '744', datminmxid = '1'
WHERE datname = 'regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ'
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (1,0,'0')
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 233)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18643', relminmxid = '3'
WHERE oid = '"public"."cmmove1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO test_tablesample SELECT i, repeat(i::text, 1000)
  FROM generate_series(0, 30) s(i)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 455)
;
----------
[UPDATE] UPDATE room_reservation SET start_time = TIMESTAMP WITH TIME ZONE '2010-04-01 13:30', end_time = TIMESTAMP WITH TIME ZONE '2010-04-01 14:30' WHERE room_id = '101' AND start_time = TIMESTAMP WITH TIME ZONE '2010-04-01 10:00'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 899)
;
----------
[INSERT|SELECT] INSERT INTO xacttest (a, b) VALUES (777, 777.777)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15474', relminmxid = '3'
WHERE oid = '"public"."prt1_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO tab5 VALUES (2, 22, 222, 2222)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_9.pg_temp_38133_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_38133 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_9.pg_temp_38133 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p2_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO ceil_floor_round VALUES ('9.4999999')
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_tm SELECT (diff.newdata).* FROM pg_temp_10.pg_temp_25340_2 diff WHERE tid IS NULL
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 217)
;
----------
[INSERT] INSERT INTO booltmp VALUES (false), (true)
;
----------
[UPDATE] UPDATE test_tab SET b = md5(b)
;
----------
[DELETE] DELETE FROM tst WHERE i = 9
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt1_l_p3"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE pg_class SET reltuples = reltuples * 2
	WHERE oid = 'accounts'::regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20721'
;
----------
[DELETE] DELETE FROM inh_parent WHERE a IN (4, 6)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 71)
;
----------
[INSERT] insert into part_ee_ff1 values ('ff', 1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_ml_p3"'::pg_catalog.regclass
  AND attname IN ('b', 'c', 'a')
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (4, 1, 7545, -4313, CURRENT_TIMESTAMP)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17991', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo2"'::pg_catalog.regclass
;
----------
[INSERT] insert into bytea_test_table values(decode('ff','hex'))
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."covidxpart4"'::pg_catalog.regclass
  AND attname IN ('b', 'a')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 710)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 965)
;
