COPY trunc_copy FROM '/src/src/test/recovery/tmp_check/t_018_wal_optimize_node_replica_data/copy_data.txt' DELIMITER ','
;
COPY public.test_tab2 (a) TO STDOUT
;
-- Test ALTER LARGE OBJECT OWNER, GRANT, COMMENT
CREATE ROLE regress_lo_user
;
-- Produce results with hash aggregation.

set enable_hashagg = true
;
-- Check that pg_monitor is allowed to query view / function
SET ROLE pg_monitor
;
EXECUTE dumpOpr('30187')
;
EXECUTE dumpFunc('27709')
;
EXECUTE dumpTableAttach('40230')
;
ALTER FUNCTION "public"."explain_memoize"("query" "text", "hide_hitmiss" boolean) OWNER TO zyc
;
-- pg_stat_have_stats returns false for rolled back index creation
BEGIN
;
------------------------------------------------------------------------------------------
[DATA_IMPORT] COPY trunc_copy FROM '/src/src/test/recovery/tmp_check/t_018_wal_optimize_node_replica_data/copy_data.txt' DELIMITER ','
;
----------
[DATA_EXPORT] COPY public.test_tab2 (a) TO STDOUT
;
----------
[CREATE_ROLE] -- Test ALTER LARGE OBJECT OWNER, GRANT, COMMENT
CREATE ROLE regress_lo_user
;
----------
[SESSION_SETTING_WRITE] -- Produce results with hash aggregation.

set enable_hashagg = true
;
----------
[SWITCH_ROLE] -- Check that pg_monitor is allowed to query view / function
SET ROLE pg_monitor
;
----------
[UNSAFE] EXECUTE dumpOpr('30187')
;
----------
[UNSAFE] EXECUTE dumpFunc('27709')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('40230')
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."explain_memoize"("query" "text", "hide_hitmiss" boolean) OWNER TO zyc
;
----------
[TRANSACTION] -- pg_stat_have_stats returns false for rolled back index creation
BEGIN
;
