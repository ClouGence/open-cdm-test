set parallel_setup_cost = 0
;
LOCK TABLE public.transition_table_level1 IN ACCESS SHARE MODE
;
COPY public.table_three_one (a) TO stdout
;
CREATE STATISTICS mcv_lists_stats (mcv) ON a, b, d FROM mcv_lists
;
EXECUTE dumpDomain('35598')
;
ALTER MATERIALIZED VIEW mvtest_mvschema.mvtest_tvm OWNER TO zyc
;
-- Execution of pg_stat_statements_reset() is granted only to
-- superusers in 1.4, so this fails.
SET SESSION AUTHORIZATION pg_read_all_stats
;
EXECUTE dumpBaseType('3769')
;
EXECUTE dumpAgg('16724')
;
COPY public.defaultexpr_tbl (i1, i2) TO stdout
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] set parallel_setup_cost = 0
;
----------
[SESSION_LOCK] LOCK TABLE public.transition_table_level1 IN ACCESS SHARE MODE
;
----------
[DATA_EXPORT] COPY public.table_three_one (a) TO stdout
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS mcv_lists_stats (mcv) ON a, b, d FROM mcv_lists
;
----------
[UNSAFE] EXECUTE dumpDomain('35598')
;
----------
[TRANSFER_PRIVILEGE] ALTER MATERIALIZED VIEW mvtest_mvschema.mvtest_tvm OWNER TO zyc
;
----------
[SWITCH_USER] -- Execution of pg_stat_statements_reset() is granted only to
-- superusers in 1.4, so this fails.
SET SESSION AUTHORIZATION pg_read_all_stats
;
----------
[UNSAFE] EXECUTE dumpBaseType('3769')
;
----------
[UNSAFE] EXECUTE dumpAgg('16724')
;
----------
[DATA_EXPORT] COPY public.defaultexpr_tbl (i1, i2) TO stdout
;
