alter type insenum add value 'i8' before 'L2'
;
IMPORT FOREIGN SCHEMA s1 LIMIT TO (t1) FROM SERVER s9 INTO public
;
CREATE TABLE "public"."pht1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY HASH ("c")
;
CREATE FUNCTION crosstab2(text)
RETURNS setof tablefunc_crosstab_2
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
CREATE TEMP TABLE pg_temp_3.pg_temp_29533_2 (tid pg_catalog.tid)
;
VACUUM (PARALLEL 0) pg_catalog.pg_class
;
ALTER FUNCTION gbt_date_consistent(internal, date, smallint, oid, internal) PARALLEL SAFE
;
CREATE TABLE trunc_e (a int REFERENCES truncate_a, b int REFERENCES trunc_c)
;
-- ===================================================================
-- test for TRUNCATE
-- ===================================================================
CREATE TABLE tru_rtable0 (id int primary key)
;
create index matest3i on matest3 ((1-id))
;
-- add a check constraint (fails)
alter table atacc1 add constraint atacc_test1 check (test1>3)
;
-- fail - name already exists
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false)
;
CREATE VIEW pg_stat_subscription_stats AS
    SELECT
        ss.subid,
        s.subname,
        ss.apply_error_count,
        ss.sync_error_count,
        ss.stats_reset
    FROM pg_subscription as s,
         pg_stat_get_subscription_stats(s.oid) as ss
;
create table fkest (x integer, x10 integer, x10b integer, x100 integer)
;
--
-- GB18030
--
CREATE TABLE gb18030_inputs (inbytes bytea, description text)
;
DROP TABLE parted, list_parted, range_parted, list_parted2, range_parted2, range_parted3
;
CREATE TABLE merge_actions(action text, abbrev text)
;
CREATE TABLE "public"."float4_tbl" (
    "f1" real
)
;
CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS 'not even SQL'
;
create table Hub (
    name	char(14),
    comment	text,
    nslots	integer
)
;
VACUUM (PARALLEL 2) pvactst2
;
ALTER TABLE pg_toast.pg_toast_28405 RENAME TO reind_con_toast
;
CREATE TABLE truncate_a (col1 integer primary key)
;
alter index idxpart_a_b_idx attach partition idxpart_a_b_idx
;
CREATE FUNCTION grow_rel(rel regclass, nblocks int)
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
DROP function make_record(n int)
;
CREATE TABLE PKTABLE (ptest1 int PRIMARY KEY)
;
ALTER FUNCTION gbt_oid_same(gbtreekey8, gbtreekey8, internal) PARALLEL SAFE
;
CREATE TABLE part_3 (LIKE list_parted2)
;
ALTER TABLE target ADD PRIMARY KEY (tid)
;
CREATE OR REPLACE VIEW ext_cor_view AS
  SELECT 'ext_cor_view: from extension'::text AS col
;
CREATE POLICY "p4" ON "regress_rls_schema"."rls_tbl_force" FOR DELETE USING (("c1" = 8))
;
ALTER OPERATOR <= (ean13, issn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
ALTER TABLE atest6 DROP COLUMN two
;
-- ok
DROP TABLE reind_fdw_parent
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_aggregate
;
create temp table p (a int, b int, c int) partition by list (a)
;
ALTER TABLE test SET (autovacuum_enabled=false)
;
CREATE TABLE dropfk (a int REFERENCES droppk)
;
-- attach both; parent stays invalid
alter index idxpart_expr_idx attach partition idxpart1_expr_idx
;
--should fail
create function table_fail(i anyelement) returns table(i anyelement, r anyrange)
  as $$ select $1, '[1,10]' $$ language sql
;
CREATE MATERIALIZED VIEW "public"."mvtest_tvvm" AS
 SELECT "grandtot"
   FROM "public"."mvtest_tvv"
  WITH NO DATA
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn13,
	RIGHTARG = issn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
CREATE TYPE "public"."city_budget"
;
-- test RETURN QUERY with dropped columns

create table tabwithcols(a int, b int, c int, d int)
;
create trigger parent_update_trig
  after update on parent referencing old table as old_table new table as new_table
  for each statement execute procedure dump_update()
;
CREATE TABLE lock_tbl2 (b BIGINT) INHERITS (lock_tbl1)
;
DROP TABLE test_chunk_id
;
VACUUM (ANALYZE) pg_catalog.pg_statistic_ext_data
;
DROP TRIGGER trig_local_before ON loc1
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] alter type insenum add value 'i8' before 'L2'
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA s1 LIMIT TO (t1) FROM SERVER s9 INTO public
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY HASH ("c")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION crosstab2(text)
RETURNS setof tablefunc_crosstab_2
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_29533_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_class
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_date_consistent(internal, date, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE trunc_e (a int REFERENCES truncate_a, b int REFERENCES trunc_c)
;
----------
[CREATE_TABLE] -- ===================================================================
-- test for TRUNCATE
-- ===================================================================
CREATE TABLE tru_rtable0 (id int primary key)
;
----------
[ADD_INDEX] create index matest3i on matest3 ((1-id))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a check constraint (fails)
alter table atacc1 add constraint atacc_test1 check (test1>3)
;
----------
[CREATE_PUB_SUB] -- fail - name already exists
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_subscription_stats AS
    SELECT
        ss.subid,
        s.subname,
        ss.apply_error_count,
        ss.sync_error_count,
        ss.stats_reset
    FROM pg_subscription as s,
         pg_stat_get_subscription_stats(s.oid) as ss
;
----------
[CREATE_TABLE] create table fkest (x integer, x10 integer, x10b integer, x100 integer)
;
----------
[CREATE_TABLE] --
-- GB18030
--
CREATE TABLE gb18030_inputs (inbytes bytea, description text)
;
----------
[DROP_TABLE] DROP TABLE parted, list_parted, range_parted, list_parted2, range_parted2, range_parted3
;
----------
[CREATE_TABLE] CREATE TABLE merge_actions(action text, abbrev text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."float4_tbl" (
    "f1" real
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS 'not even SQL'
;
----------
[CREATE_TABLE] create table Hub (
    name	char(14),
    comment	text,
    nslots	integer
)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pvactst2
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28405 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE truncate_a (col1 integer primary key)
;
----------
[ALTER_INDEX] alter index idxpart_a_b_idx attach partition idxpart_a_b_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION grow_rel(rel regclass, nblocks int)
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[DROP_PROG_OBJ] DROP function make_record(n int)
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 int PRIMARY KEY)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_oid_same(gbtreekey8, gbtreekey8, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE part_3 (LIKE list_parted2)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE target ADD PRIMARY KEY (tid)
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW ext_cor_view AS
  SELECT 'ext_cor_view: from extension'::text AS col
;
----------
[CREATE_POLICY] CREATE POLICY "p4" ON "regress_rls_schema"."rls_tbl_force" FOR DELETE USING (("c1" = 8))
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ean13, issn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE atest6 DROP COLUMN two
;
----------
[DROP_TABLE] -- ok
DROP TABLE reind_fdw_parent
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_aggregate
;
----------
[CREATE_TABLE] create temp table p (a int, b int, c int) partition by list (a)
;
----------
[ALTER_TABLE] ALTER TABLE test SET (autovacuum_enabled=false)
;
----------
[CREATE_TABLE] CREATE TABLE dropfk (a int REFERENCES droppk)
;
----------
[ALTER_INDEX] -- attach both; parent stays invalid
alter index idxpart_expr_idx attach partition idxpart1_expr_idx
;
----------
[CREATE_PROG_OBJ] --should fail
create function table_fail(i anyelement) returns table(i anyelement, r anyrange)
  as $$ select $1, '[1,10]' $$ language sql
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_tvvm" AS
 SELECT "grandtot"
   FROM "public"."mvtest_tvv"
  WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn13,
	RIGHTARG = issn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."city_budget"
;
----------
[CREATE_TABLE] -- test RETURN QUERY with dropped columns

create table tabwithcols(a int, b int, c int, d int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parent_update_trig
  after update on parent referencing old table as old_table new table as new_table
  for each statement execute procedure dump_update()
;
----------
[CREATE_TABLE] CREATE TABLE lock_tbl2 (b BIGINT) INHERITS (lock_tbl1)
;
----------
[DROP_TABLE] DROP TABLE test_chunk_id
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_statistic_ext_data
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_local_before ON loc1
;
