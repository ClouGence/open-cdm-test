CALL test_proc11(_a, _b, _c)
;
DROP SUBSCRIPTION regress_testsub
;
CREATE TABLE upsert_test (
    a   INT PRIMARY KEY,
    b   TEXT
) PARTITION BY LIST (a)
;
-- Nested composite

create type quad as (c1 complex, c2 complex)
;
ALTER TABLE pg_toast.pg_toast_33180 RENAME TO reind_con_toast
;
/*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2016
 *
 * Copyright (c) 2003-2021, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 5.1
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
ANALYZE pg_temp_13.pg_temp_26049_2
;
CREATE TEMP TABLE pg_temp_3.pg_temp_27487_2 (tid pg_catalog.tid)
;
CREATE FUNCTION gc_to_sec(float8)
RETURNS float8
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
AS 'SELECT CASE WHEN $1 < 0 THEN 0::float8 WHEN $1/earth() > pi() THEN 2*earth() ELSE 2*earth()*sin($1/(2*earth())) END'
;
DROP TABLE cmdlog
;
DROP TEXT SEARCH CONFIGURATION IF EXISTS split_ts_next.cfg_parser_renamed CASCADE
;
create table my_table (a int)
;
DROP SUBSCRIPTION "regress_sub_dummy"
;
alter table defpart_attach_test_d drop b
;
create table merge_target_table (a int primary key, b text)
;
CREATE TABLE "generated_stored_tests"."gtest27" (
    "a" bigint,
    "b" bigint,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "x" bigint GENERATED ALWAYS AS ((("a" + "b") * 2)) STORED
)
;
/*
 * text search configuration for russian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * russian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
ALTER TABLE pg_temp_10.pg_temp_26364_2 ADD COLUMN newdata pg_temp_10.pg_temp_26364
;
alter function tattle(x int, y int) stable
;
create function raise_exprs() returns void as $$
declare
    a integer[] = '{10,20,30}';
    c varchar = 'xyz';
    i integer;
begin
    i := 2;
    raise notice '%; %; %; %; %; %', a, a[i], c, (select c || 'abc'), row(10,'aaa',NULL,30), NULL;
end;$$ language plpgsql
;
CREATE DOMAIN rgb AS rainbow CHECK (VALUE IN ('red', 'green', 'blue'))
;
CREATE FUNCTION gin_extract_query_timetz(timetz, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE OPERATOR FAMILY "public"."custom_opclass" USING "hash"
;
-- test minvalue/maxvalue restrictions
create table mcrparted (a int, b int, c int) partition by range (a, abs(b), c)
;
-- Test display of temporary objects
create temp table t1(f1 float8)
;
-- not supported by sepgsql
ALTER TABLE regtest_ptable ALTER p DROP DEFAULT
;
DROP SCHEMA split_typeconv CASCADE
;
-- Verify that expression indexes inherit correctly
create table idxpart (a int, b int) partition by range (a)
;
/* src/test/modules/test_copy_callbacks/test_copy_callbacks--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_copy_to_callback(pg_catalog.regclass)
	RETURNS pg_catalog.void
	AS '$libdir/test_copy_callbacks' LANGUAGE C
;
CREATE FUNCTION gbt_bool_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE fkpart3.fk1
;
create table mcrparted4 partition of mcrparted for values from (21, minvalue, minvalue) to (30, 20, maxvalue)
;
-- dropping it should still work
drop extension test_ext8
;
-- fail
ALTER TABLESPACE regress_tblspace RESET (random_page_cost = 2.0)
;
create table idxpart (a text) partition by range (a)
;
CREATE TABLE sample_tbl(col1 int, col2 int)
;
VACUUM (PROCESS_TOAST FALSE) vactst
;
--
-- Try some concurrent index builds
--
-- Unfortunately this only tests about half the code paths because there are
-- no concurrent updates happening to the table at the same time.

CREATE TABLE concur_heap (f1 text, f2 text)
;
create unique index partial_key_index on insertconflicttest(key) where fruit like '%berry'
;
create trigger my_trig
  after insert on parted_trig_1
  for each row execute procedure funcA()
;
--
-- The hash indexing operator class.
--

CREATE OPERATOR CLASS citext_ops
DEFAULT FOR TYPE citext USING hash AS
    OPERATOR    1   =  (citext, citext),
    FUNCTION    1   citext_hash(citext)
;
CREATE TABLE savepoints (a int)
;
create function tg_hslot_bu() returns trigger as '
begin
    if new.slotname != old.slotname or new.hubname != old.hubname then
        delete from HSlot where slotname = old.slotname;
	insert into HSlot (
		    slotname,
		    hubname,
		    slotno,
		    slotlink
		) values (
		    new.slotname,
		    new.hubname,
		    new.slotno,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
CREATE TABLE t_009_tbl5 (id int, msg text)
;
-- Change plt1_adv_p1 to the NULL partition
CREATE TABLE plt1_adv_p1_null PARTITION OF plt1_adv FOR VALUES IN (NULL, '0001', '0003')
;
CREATE TRIGGER trg_x_before BEFORE INSERT ON x
FOR EACH ROW EXECUTE PROCEDURE fn_x_before()
;
CREATE TABLE prt2_l_p2 PARTITION OF prt2_l FOR VALUES FROM (250) TO (500) PARTITION BY LIST (c)
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE selinto_schema.tbl_withdata2 (a) AS
  SELECT generate_series(1,3) WITH DATA
;
-- char check

CREATE TABLE chartmp (a char(32))
;
CREATE FUNCTION functest_C_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 0'
;
------------------------------------------------------------------------------------------
[CALL_PROG_OBJ] CALL test_proc11(_a, _b, _c)
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION regress_testsub
;
----------
[CREATE_TABLE] CREATE TABLE upsert_test (
    a   INT PRIMARY KEY,
    b   TEXT
) PARTITION BY LIST (a)
;
----------
[CREATE_TYPE] -- Nested composite

create type quad as (c1 complex, c2 complex)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33180 RENAME TO reind_con_toast
;
----------
[CREATE_SCHEMA] /*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2016
 *
 * Copyright (c) 2003-2021, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 5.1
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26049_2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_27487_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gc_to_sec(float8)
RETURNS float8
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
AS 'SELECT CASE WHEN $1 < 0 THEN 0::float8 WHEN $1/earth() > pi() THEN 2*earth() ELSE 2*earth()*sin($1/(2*earth())) END'
;
----------
[DROP_TABLE] DROP TABLE cmdlog
;
----------
[DROP_POLICY] DROP TEXT SEARCH CONFIGURATION IF EXISTS split_ts_next.cfg_parser_renamed CASCADE
;
----------
[CREATE_TABLE] create table my_table (a int)
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION "regress_sub_dummy"
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table defpart_attach_test_d drop b
;
----------
[CREATE_TABLE] create table merge_target_table (a int primary key, b text)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest27" (
    "a" bigint,
    "b" bigint,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "x" bigint GENERATED ALWAYS AS ((("a" + "b") * 2)) STORED
)
;
----------
[CREATE_POLICY] /*
 * text search configuration for russian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * russian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_26364_2 ADD COLUMN newdata pg_temp_10.pg_temp_26364
;
----------
[ALTER_PROG_OBJ] alter function tattle(x int, y int) stable
;
----------
[CREATE_PROG_OBJ] create function raise_exprs() returns void as $$
declare
    a integer[] = '{10,20,30}';
    c varchar = 'xyz';
    i integer;
begin
    i := 2;
    raise notice '%; %; %; %; %; %', a, a[i], c, (select c || 'abc'), row(10,'aaa',NULL,30), NULL;
end;$$ language plpgsql
;
----------
[CREATE_TYPE] CREATE DOMAIN rgb AS rainbow CHECK (VALUE IN ('red', 'green', 'blue'))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_timetz(timetz, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY "public"."custom_opclass" USING "hash"
;
----------
[CREATE_TABLE] -- test minvalue/maxvalue restrictions
create table mcrparted (a int, b int, c int) partition by range (a, abs(b), c)
;
----------
[CREATE_TABLE] -- Test display of temporary objects
create temp table t1(f1 float8)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- not supported by sepgsql
ALTER TABLE regtest_ptable ALTER p DROP DEFAULT
;
----------
[DROP_SCHEMA] DROP SCHEMA split_typeconv CASCADE
;
----------
[CREATE_TABLE] -- Verify that expression indexes inherit correctly
create table idxpart (a int, b int) partition by range (a)
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_copy_callbacks/test_copy_callbacks--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_copy_to_callback(pg_catalog.regclass)
	RETURNS pg_catalog.void
	AS '$libdir/test_copy_callbacks' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bool_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE fkpart3.fk1
;
----------
[CREATE_TABLE] create table mcrparted4 partition of mcrparted for values from (21, minvalue, minvalue) to (30, 20, maxvalue)
;
----------
[DROP_LIBRARY] -- dropping it should still work
drop extension test_ext8
;
----------
[ALTER_TABLESPACE] -- fail
ALTER TABLESPACE regress_tblspace RESET (random_page_cost = 2.0)
;
----------
[CREATE_TABLE] create table idxpart (a text) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE sample_tbl(col1 int, col2 int)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) vactst
;
----------
[CREATE_TABLE] --
-- Try some concurrent index builds
--
-- Unfortunately this only tests about half the code paths because there are
-- no concurrent updates happening to the table at the same time.

CREATE TABLE concur_heap (f1 text, f2 text)
;
----------
[ADD_INDEX] create unique index partial_key_index on insertconflicttest(key) where fruit like '%berry'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_trig
  after insert on parted_trig_1
  for each row execute procedure funcA()
;
----------
[CREATE_PROG_OBJ] --
-- The hash indexing operator class.
--

CREATE OPERATOR CLASS citext_ops
DEFAULT FOR TYPE citext USING hash AS
    OPERATOR    1   =  (citext, citext),
    FUNCTION    1   citext_hash(citext)
;
----------
[CREATE_TABLE] CREATE TABLE savepoints (a int)
;
----------
[CREATE_PROG_OBJ] create function tg_hslot_bu() returns trigger as '
begin
    if new.slotname != old.slotname or new.hubname != old.hubname then
        delete from HSlot where slotname = old.slotname;
	insert into HSlot (
		    slotname,
		    hubname,
		    slotno,
		    slotlink
		) values (
		    new.slotname,
		    new.hubname,
		    new.slotno,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE t_009_tbl5 (id int, msg text)
;
----------
[CREATE_TABLE] -- Change plt1_adv_p1 to the NULL partition
CREATE TABLE plt1_adv_p1_null PARTITION OF plt1_adv FOR VALUES IN (NULL, '0001', '0003')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trg_x_before BEFORE INSERT ON x
FOR EACH ROW EXECUTE PROCEDURE fn_x_before()
;
----------
[CREATE_TABLE] CREATE TABLE prt2_l_p2 PARTITION OF prt2_l FOR VALUES FROM (250) TO (500) PARTITION BY LIST (c)
;
----------
[CREATE_TABLE|SELECT] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE selinto_schema.tbl_withdata2 (a) AS
  SELECT generate_series(1,3) WITH DATA
;
----------
[CREATE_TABLE] -- char check

CREATE TABLE chartmp (a char(32))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_C_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 0'
;
