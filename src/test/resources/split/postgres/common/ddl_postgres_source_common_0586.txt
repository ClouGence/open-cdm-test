-- mismatches
create type testtype3 as (a int, b text)
;
-- succeeds

alter table anothertab alter column atcol1 type boolean
        using case when atcol1 % 2 = 0 then true else false end
;
create table cities (
	name		text,
	population	float8,
	altitude	int		-- (in ft)
)
;
create table parted_minmax1 partition of parted_minmax for values from (1) to (10)
;
create table ab_a1_b3 partition of ab_a1 for values in (3)
;
CREATE TABLE fk_partitioned_fk_6 (a int REFERENCES fk_partitioned_pk_6) PARTITION BY LIST (a)
;
-- View based on a sequence
CREATE VIEW ro_view20 AS SELECT a, b, generate_series(1, a) g FROM base_tbl
;
/*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
COMMENT ON PUBLICATION testpub_default IS 'test publication'
;
ALTER TABLE ONLY "generated_stored_tests"."gtest30_1" INHERIT "generated_stored_tests"."gtest30"
;
CREATE PROCEDURE "public"."ptest10"(OUT "a" integer, IN "b" integer, IN "c" integer)
    LANGUAGE "sql"
    AS $$ SELECT b - c $$
;
CREATE FUNCTION gin_extract_query_text(text, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE VIEW regtest_pview_2 AS SELECT * FROM regtest_ptable_3 WHERE o < p
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) information_schema.sql_features
;
CREATE SCHEMA fkpart13
;
CREATE INDEX botharrayidx ON array_index_op_test USING gin (i, t)
;
ALTER TABLE ONLY "fkpart5"."pk" ATTACH PARTITION "fkpart5"."pk3" FOR VALUES IN (31, 32)
;
CREATE FUNCTION ltree_penalty(internal,internal,internal)
RETURNS internal as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
TRUNCATE tab_int
;
ALTER FUNCTION gbt_bpchar_compress(internal) PARALLEL SAFE
;
alter table returningwrtest add b text
;
DROP TRIGGER parted_mod_b ON sub_part1
;
CREATE TABLE "fkpart3"."pk1" (
    "a" integer NOT NULL
)
;
TRUNCATE brin_test_multi_2
;
CREATE TABLE "public"."pitest1_p1" (
    "f1" "date" CONSTRAINT "pitest1_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" bigint CONSTRAINT "pitest1_f3_not_null" NOT NULL
)
;
CREATE AGGREGATE "public"."logging_agg_strict_initcond"("anyelement") (
    SFUNC = "public"."logging_sfunc_strict",
    STYPE = "text",
    INITCOND = 'I',
    MSFUNC = "public"."logging_msfunc_strict",
    MINVFUNC = "public"."logging_minvfunc_strict",
    MSTYPE = "text",
    MINITCOND = 'MI'
)
;
CREATE TABLE fract_t1 PARTITION OF fract_t FOR VALUES FROM ('1000') TO ('2000')
;
CREATE DOMAIN nnint AS int NOT NULL
;
drop table testpart_apple
;
CREATE TABLE test_ext3_table (col_old INT)
;
CREATE TABLE pht2_p2 PARTITION OF pht2 FOR VALUES WITH (MODULUS 3, REMAINDER 1)
;
CREATE FUNCTION gbt_tstz_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
DROP FUNCTION test_function_exists(int, text, int[])
;
-- Test a DO ALSO INSERT ... SELECT rule
drop rule base_tab_def_view_ins_rule on base_tab_def_view
;
CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE TABLE regress_create_schema_role.tab (id int)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub1
;
CREATE INDEX regression_vacuumdb_test_idx ON regression_vacuumdb_test (mod(a, 2))
;
VACUUM (PARALLEL 2) pg_catalog.pg_policy
;
CREATE TABLE test1 (i int4, t text, b bytea)
;
/*
 * A few supporting functions first ...
 */

/* Expand any 1-D array into a set with integers 1..N */
CREATE FUNCTION _pg_expandarray(IN anyarray, OUT x anyelement, OUT n int)
    RETURNS SETOF RECORD
    LANGUAGE sql STRICT IMMUTABLE PARALLEL SAFE
    ROWS 100 SUPPORT pg_catalog.array_unnest_support
    AS 'SELECT * FROM pg_catalog.unnest($1) WITH ORDINALITY'
;
-- these should work
alter table renameColumn rename column a to d
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub_mix_5, pub_mix_6
;
CREATE TEMP TABLE foochild (fc int) INHERITS (foo)
;
ALTER INDEX dummy_test_idx SET (option_string_val = 'val2')
;
ALTER TABLE notnull_chld0 DROP CONSTRAINT nn_chld0
;
--
  -- Setup to test an error from ExecLockRows
  --
  CREATE TABLE foo_range_parted (a int, b text) PARTITION BY RANGE(a)
;
CREATE SUBSCRIPTION sub3
						 CONNECTION 'dbname=doesnotexist' PUBLICATION pub1
						 WITH (connect = false, origin = any, streaming = on)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_data_wrapper
;
ALTER FOREIGN TABLE ft1 DROP COLUMN IF EXISTS no_column
;
CREATE INDEX test1_a_brin ON test1 USING brin(a)
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] -- mismatches
create type testtype3 as (a int, b text)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- succeeds

alter table anothertab alter column atcol1 type boolean
        using case when atcol1 % 2 = 0 then true else false end
;
----------
[CREATE_TABLE] create table cities (
	name		text,
	population	float8,
	altitude	int		-- (in ft)
)
;
----------
[CREATE_TABLE] create table parted_minmax1 partition of parted_minmax for values from (1) to (10)
;
----------
[CREATE_TABLE] create table ab_a1_b3 partition of ab_a1 for values in (3)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_6 (a int REFERENCES fk_partitioned_pk_6) PARTITION BY LIST (a)
;
----------
[CREATE_VIEW(SELECT)] -- View based on a sequence
CREATE VIEW ro_view20 AS SELECT a, b, generate_series(1, a) g FROM base_tbl
;
----------
[CREATE_POLICY] /*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
----------
[ALTER_PUB_SUB] COMMENT ON PUBLICATION testpub_default IS 'test publication'
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_stored_tests"."gtest30_1" INHERIT "generated_stored_tests"."gtest30"
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest10"(OUT "a" integer, IN "b" integer, IN "c" integer)
    LANGUAGE "sql"
    AS $$ SELECT b - c $$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_text(text, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW regtest_pview_2 AS SELECT * FROM regtest_ptable_3 WHERE o < p
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) information_schema.sql_features
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart13
;
----------
[ADD_INDEX] CREATE INDEX botharrayidx ON array_index_op_test USING gin (i, t)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart5"."pk" ATTACH PARTITION "fkpart5"."pk3" FOR VALUES IN (31, 32)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_penalty(internal,internal,internal)
RETURNS internal as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[TRUNCATE_TABLE] TRUNCATE tab_int
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bpchar_compress(internal) PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table returningwrtest add b text
;
----------
[DROP_TRIGGER] DROP TRIGGER parted_mod_b ON sub_part1
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk1" (
    "a" integer NOT NULL
)
;
----------
[TRUNCATE_TABLE] TRUNCATE brin_test_multi_2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest1_p1" (
    "f1" "date" CONSTRAINT "pitest1_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" bigint CONSTRAINT "pitest1_f3_not_null" NOT NULL
)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."logging_agg_strict_initcond"("anyelement") (
    SFUNC = "public"."logging_sfunc_strict",
    STYPE = "text",
    INITCOND = 'I',
    MSFUNC = "public"."logging_msfunc_strict",
    MINVFUNC = "public"."logging_minvfunc_strict",
    MSTYPE = "text",
    MINITCOND = 'MI'
)
;
----------
[CREATE_TABLE] CREATE TABLE fract_t1 PARTITION OF fract_t FOR VALUES FROM ('1000') TO ('2000')
;
----------
[CREATE_TYPE] CREATE DOMAIN nnint AS int NOT NULL
;
----------
[DROP_TABLE] drop table testpart_apple
;
----------
[CREATE_TABLE] CREATE TABLE test_ext3_table (col_old INT)
;
----------
[CREATE_TABLE] CREATE TABLE pht2_p2 PARTITION OF pht2 FOR VALUES WITH (MODULUS 3, REMAINDER 1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_tstz_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_function_exists(int, text, int[])
;
----------
[DROP_POLICY] -- Test a DO ALSO INSERT ... SELECT rule
drop rule base_tab_def_view_ins_rule on base_tab_def_view
;
----------
[CREATE_SCHEMA] CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE TABLE regress_create_schema_role.tab (id int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub1
;
----------
[ADD_INDEX] CREATE INDEX regression_vacuumdb_test_idx ON regression_vacuumdb_test (mod(a, 2))
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_policy
;
----------
[CREATE_TABLE] CREATE TABLE test1 (i int4, t text, b bytea)
;
----------
[CREATE_PROG_OBJ] /*
 * A few supporting functions first ...
 */

/* Expand any 1-D array into a set with integers 1..N */
CREATE FUNCTION _pg_expandarray(IN anyarray, OUT x anyelement, OUT n int)
    RETURNS SETOF RECORD
    LANGUAGE sql STRICT IMMUTABLE PARALLEL SAFE
    ROWS 100 SUPPORT pg_catalog.array_unnest_support
    AS 'SELECT * FROM pg_catalog.unnest($1) WITH ORDINALITY'
;
----------
[RENAME_COLUMN] -- these should work
alter table renameColumn rename column a to d
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub_mix_5, pub_mix_6
;
----------
[CREATE_TABLE] CREATE TEMP TABLE foochild (fc int) INHERITS (foo)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_string_val = 'val2')
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE notnull_chld0 DROP CONSTRAINT nn_chld0
;
----------
[CREATE_TABLE] --
  -- Setup to test an error from ExecLockRows
  --
  CREATE TABLE foo_range_parted (a int, b text) PARTITION BY RANGE(a)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub3
						 CONNECTION 'dbname=doesnotexist' PUBLICATION pub1
						 WITH (connect = false, origin = any, streaming = on)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_data_wrapper
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE ft1 DROP COLUMN IF EXISTS no_column
;
----------
[ADD_INDEX] CREATE INDEX test1_a_brin ON test1 USING brin(a)
;
