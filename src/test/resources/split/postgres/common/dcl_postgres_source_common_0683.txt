/* If objects don't exist, raise errors. */

DROP ROLE regress_regrole_test
;
REVOKE ALL ON pg_aios FROM PUBLIC
;
EXECUTE dumpTableAttach('28827')
;
-- error if the column is excluded
copy oversized_column_default (col2) from stdin
;
DROP ROLE regress_lr_normal
;
-- Cleanup
reset enable_hashagg
;
ALTER TABLE public.timetz_tbl OWNER TO zyc
;
SET xmlbinary TO base64
;
DROP OWNED BY regress_test_role2 CASCADE
;
-- for the moment we only want to look at nestloop plans
set enable_hashjoin = off
;
------------------------------------------------------------------------------------------
[DROP_ROLE] /* If objects don't exist, raise errors. */

DROP ROLE regress_regrole_test
;
----------
[REVOKE] REVOKE ALL ON pg_aios FROM PUBLIC
;
----------
[UNSAFE] EXECUTE dumpTableAttach('28827')
;
----------
[DATA_IMPORT] -- error if the column is excluded
copy oversized_column_default (col2) from stdin
;
----------
[DROP_ROLE] DROP ROLE regress_lr_normal
;
----------
[SESSION_SETTING_WRITE] -- Cleanup
reset enable_hashagg
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.timetz_tbl OWNER TO zyc
;
----------
[SESSION_SETTING_WRITE] SET xmlbinary TO base64
;
----------
[REVOKE] DROP OWNED BY regress_test_role2 CASCADE
;
----------
[SESSION_SETTING_WRITE] -- for the moment we only want to look at nestloop plans
set enable_hashjoin = off
;
