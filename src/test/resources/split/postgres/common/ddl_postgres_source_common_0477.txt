ALTER INDEX pg_toast.pg_toast_30860_index RENAME TO reind_con_toast_idx
;
create type rainbow as enum ('r','o','y','g','b','i','v')
;
/*
 * 5.56
 * SEQUENCES view
 */

CREATE VIEW sequences AS
    SELECT CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(nc.nspname AS sql_identifier) AS sequence_schema,
           CAST(c.relname AS sql_identifier) AS sequence_name,
           CAST(format_type(s.seqtypid, null) AS character_data) AS data_type,
           CAST(_pg_numeric_precision(s.seqtypid, -1) AS cardinal_number) AS numeric_precision,
           CAST(2 AS cardinal_number) AS numeric_precision_radix,
           CAST(0 AS cardinal_number) AS numeric_scale,
           CAST(s.seqstart AS character_data) AS start_value,
           CAST(s.seqmin AS character_data) AS minimum_value,
           CAST(s.seqmax AS character_data) AS maximum_value,
           CAST(s.seqincrement AS character_data) AS increment,
           CAST(CASE WHEN s.seqcycle THEN 'YES' ELSE 'NO' END AS yes_or_no) AS cycle_option
    FROM pg_namespace nc, pg_class c, pg_sequence s
    WHERE c.relnamespace = nc.oid
          AND c.relkind = 'S'
          AND NOT EXISTS (SELECT 1 FROM pg_depend WHERE classid = 'pg_class'::regclass AND objid = c.oid AND deptype = 'i')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND c.oid = s.seqrelid
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_sequence_privilege(c.oid, 'SELECT, UPDATE, USAGE') )
;
CREATE TABLE pagg_tab_ml_p3_s2 PARTITION OF pagg_tab_ml_p3 FOR VALUES FROM (5) TO (10)
;
-- check response to attempt to modify table with same name as a CTE (perhaps
-- surprisingly it works, because CTEs don't hide tables from data-modifying
-- statements)
create temp table with_test (i int)
;
CREATE INDEX on tbl USING gist(c3) INCLUDE (c1, c4)
;
create unique index on only parted_conflict_1 (a)
;
ANALYZE public.int8_tbl
;
create conversion alter1.latin1_to_utf8 for 'latin1' to 'utf8' from iso8859_1_to_utf8
;
DROP SCHEMA IF EXISTS regress_rls_schema CASCADE
;
-- Clean up
DROP FUNCTION test_pglz_compress
;
CREATE TABLE txn0(id int4 PRIMARY KEY, val TEXT)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
ALTER TABLE ONLY "public"."prt1_l" ATTACH PARTITION "public"."prt1_l_p2" FOR VALUES FROM (250) TO (500)
;
CREATE TABLE "public"."iportaltest" (
    "i" integer,
    "d" real,
    "p" "polygon"
)
;
create index concurrently parted_isvalid_idx_11 on parted_isvalid_tab_11 ((a/b))
;
CREATE TABLE tab_int (a int PRIMARY KEY)
;
ANALYZE "S 1"."T 3"
;
-- test basic arithmetic
CREATE FUNCTION eval(expr text)
RETURNS text AS
$$
DECLARE
  result text;
BEGIN
  EXECUTE 'select '||expr INTO result;
  RETURN result;
EXCEPTION WHEN OTHERS THEN
  RETURN SQLERRM;
END
$$
LANGUAGE plpgsql
;
CREATE TABLE partitioned (
	a point
) PARTITION BY RANGE (a)
;
CREATE POLICY p1 ON t1 USING (a % 2 = 0)
;
CREATE TRIGGER trig BEFORE INSERT ON part_c_1_c_20 FOR EACH ROW EXECUTE FUNCTION trigfunc()
;
create table mc2p_default partition of mc2p default
;
CREATE PROCEDURE test_proc10(IN a int, OUT b int, IN c int DEFAULT 11)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %, c: %', a, b, c;
  b := a - c;
END;
$$
;
DROP FUNCTION explain_merge(text)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_event_trigger
;
CREATE TABLE hpart_4 PARTITION OF hash_parted FOR VALUES WITH (MODULUS 10, REMAINDER 3)
;
ANALYZE pg_temp_12.pg_temp_26637
;
CREATE FUNCTION pg_buffercache_evict(
    IN int,
    OUT buffer_evicted boolean,
    OUT buffer_flushed boolean)
AS '$libdir/pg_buffercache', 'pg_buffercache_evict'
LANGUAGE C PARALLEL SAFE VOLATILE STRICT
;
create function f1(in i int, out j int) returns setof int as $$
begin
  j := i+1;
  return next;
  j := i+2;
  return next;
  return;
end$$ language plpgsql
;
CREATE TABLE "public"."found_test_tbl" (
    "a" integer
)
;
ALTER OPERATOR FAMILY gist_ltree_ops USING gist
ADD FUNCTION 10 (ltree) ltree_gist_options (internal)
;
CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (somename) TO ('2019-01-01')
;
ALTER FUNCTION gbt_uuid_picksplit(internal, internal) PARALLEL SAFE
;
create function parent_del_func()
  returns trigger language plpgsql as
$$
begin
  delete from child where aid = old.aid;
  return old;
end;
$$
;
CREATE TYPE jsrec AS (
	i	int,
	ia	_int4,
	ia1	int[],
	ia2	int[][],
	ia3	int[][][],
	ia1d	js_int_array_1d,
	ia2d	js_int_array_2d,
	t	text,
	ta	text[],
	c	char(10),
	ca	char(10)[],
	ts	timestamp,
	js	json,
	jsb	jsonb,
	jsa	json[],
	rec	jpop,
	reca	jpop[]
)
;
CREATE SEQUENCE IF NOT EXISTS split_sequence_next.seq_default NO MINVALUE NO MAXVALUE OWNED BY NONE
;
CREATE TABLE my_schema_1.pts1_ones PARTITION OF my_schema_1.pts1 FOR VALUES FROM ('0') to ('10')
;
create domain d_fail int4 collate "C"
;
CREATE TABLE am_partitioned_3 PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 3)
;
CREATE TABLE batch_cp_upd_test1 (LIKE batch_cp_upd_test)
;
CREATE INDEX hash_cleanup_index on hash_cleanup_heap USING HASH (keycol)
;
CREATE OPERATOR @#@ (
   rightarg = int8,		-- prefix
   procedure = factorial
)
;
CREATE RECURSIVE VIEW nums (n) AS
    VALUES (1)
UNION ALL
    SELECT n+1 FROM nums WHERE n < 5
;
create table trg_clone1 partition of trg_clone for values from (0) to (1000)
;
CREATE TABLE bar (i int) TABLESPACE regress_nosuchspace
;
CREATE SCHEMA "schema11" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
CREATE TABLE part_2 (LIKE list_parted2)
;
CREATE TABLE ex_msource (a int, b int)
  WITH (autovacuum_enabled=off)
;
ANALYZE public.array_index_op_test
;
------------------------------------------------------------------------------------------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30860_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TYPE] create type rainbow as enum ('r','o','y','g','b','i','v')
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.56
 * SEQUENCES view
 */

CREATE VIEW sequences AS
    SELECT CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(nc.nspname AS sql_identifier) AS sequence_schema,
           CAST(c.relname AS sql_identifier) AS sequence_name,
           CAST(format_type(s.seqtypid, null) AS character_data) AS data_type,
           CAST(_pg_numeric_precision(s.seqtypid, -1) AS cardinal_number) AS numeric_precision,
           CAST(2 AS cardinal_number) AS numeric_precision_radix,
           CAST(0 AS cardinal_number) AS numeric_scale,
           CAST(s.seqstart AS character_data) AS start_value,
           CAST(s.seqmin AS character_data) AS minimum_value,
           CAST(s.seqmax AS character_data) AS maximum_value,
           CAST(s.seqincrement AS character_data) AS increment,
           CAST(CASE WHEN s.seqcycle THEN 'YES' ELSE 'NO' END AS yes_or_no) AS cycle_option
    FROM pg_namespace nc, pg_class c, pg_sequence s
    WHERE c.relnamespace = nc.oid
          AND c.relkind = 'S'
          AND NOT EXISTS (SELECT 1 FROM pg_depend WHERE classid = 'pg_class'::regclass AND objid = c.oid AND deptype = 'i')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND c.oid = s.seqrelid
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_sequence_privilege(c.oid, 'SELECT, UPDATE, USAGE') )
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p3_s2 PARTITION OF pagg_tab_ml_p3 FOR VALUES FROM (5) TO (10)
;
----------
[CREATE_TABLE] -- check response to attempt to modify table with same name as a CTE (perhaps
-- surprisingly it works, because CTEs don't hide tables from data-modifying
-- statements)
create temp table with_test (i int)
;
----------
[ADD_INDEX] CREATE INDEX on tbl USING gist(c3) INCLUDE (c1, c4)
;
----------
[ADD_INDEX] create unique index on only parted_conflict_1 (a)
;
----------
[ADMIN_TABLE] ANALYZE public.int8_tbl
;
----------
[CREATE_POLICY] create conversion alter1.latin1_to_utf8 for 'latin1' to 'utf8' from iso8859_1_to_utf8
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS regress_rls_schema CASCADE
;
----------
[DROP_PROG_OBJ] -- Clean up
DROP FUNCTION test_pglz_compress
;
----------
[CREATE_TABLE] CREATE TABLE txn0(id int4 PRIMARY KEY, val TEXT)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_l" ATTACH PARTITION "public"."prt1_l_p2" FOR VALUES FROM (250) TO (500)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."iportaltest" (
    "i" integer,
    "d" real,
    "p" "polygon"
)
;
----------
[ADD_INDEX] create index concurrently parted_isvalid_idx_11 on parted_isvalid_tab_11 ((a/b))
;
----------
[CREATE_TABLE] CREATE TABLE tab_int (a int PRIMARY KEY)
;
----------
[ADMIN_TABLE] ANALYZE "S 1"."T 3"
;
----------
[CREATE_PROG_OBJ] -- test basic arithmetic
CREATE FUNCTION eval(expr text)
RETURNS text AS
$$
DECLARE
  result text;
BEGIN
  EXECUTE 'select '||expr INTO result;
  RETURN result;
EXCEPTION WHEN OTHERS THEN
  RETURN SQLERRM;
END
$$
LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a point
) PARTITION BY RANGE (a)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON t1 USING (a % 2 = 0)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig BEFORE INSERT ON part_c_1_c_20 FOR EACH ROW EXECUTE FUNCTION trigfunc()
;
----------
[CREATE_TABLE] create table mc2p_default partition of mc2p default
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc10(IN a int, OUT b int, IN c int DEFAULT 11)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %, c: %', a, b, c;
  b := a - c;
END;
$$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION explain_merge(text)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_event_trigger
;
----------
[CREATE_TABLE] CREATE TABLE hpart_4 PARTITION OF hash_parted FOR VALUES WITH (MODULUS 10, REMAINDER 3)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26637
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_buffercache_evict(
    IN int,
    OUT buffer_evicted boolean,
    OUT buffer_flushed boolean)
AS '$libdir/pg_buffercache', 'pg_buffercache_evict'
LANGUAGE C PARALLEL SAFE VOLATILE STRICT
;
----------
[CREATE_PROG_OBJ] create function f1(in i int, out j int) returns setof int as $$
begin
  j := i+1;
  return next;
  j := i+2;
  return next;
  return;
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."found_test_tbl" (
    "a" integer
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_ltree_ops USING gist
ADD FUNCTION 10 (ltree) ltree_gist_options (internal)
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (somename) TO ('2019-01-01')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_uuid_picksplit(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function parent_del_func()
  returns trigger language plpgsql as
$$
begin
  delete from child where aid = old.aid;
  return old;
end;
$$
;
----------
[CREATE_TYPE] CREATE TYPE jsrec AS (
	i	int,
	ia	_int4,
	ia1	int[],
	ia2	int[][],
	ia3	int[][][],
	ia1d	js_int_array_1d,
	ia2d	js_int_array_2d,
	t	text,
	ta	text[],
	c	char(10),
	ca	char(10)[],
	ts	timestamp,
	js	json,
	jsb	jsonb,
	jsa	json[],
	rec	jpop,
	reca	jpop[]
)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE IF NOT EXISTS split_sequence_next.seq_default NO MINVALUE NO MAXVALUE OWNED BY NONE
;
----------
[CREATE_TABLE] CREATE TABLE my_schema_1.pts1_ones PARTITION OF my_schema_1.pts1 FOR VALUES FROM ('0') to ('10')
;
----------
[CREATE_TYPE] create domain d_fail int4 collate "C"
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned_3 PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 3)
;
----------
[CREATE_TABLE] CREATE TABLE batch_cp_upd_test1 (LIKE batch_cp_upd_test)
;
----------
[ADD_INDEX] CREATE INDEX hash_cleanup_index on hash_cleanup_heap USING HASH (keycol)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @#@ (
   rightarg = int8,		-- prefix
   procedure = factorial
)
;
----------
[CREATE_VIEW(SELECT)] CREATE RECURSIVE VIEW nums (n) AS
    VALUES (1)
UNION ALL
    SELECT n+1 FROM nums WHERE n < 5
;
----------
[CREATE_TABLE] create table trg_clone1 partition of trg_clone for values from (0) to (1000)
;
----------
[CREATE_TABLE] CREATE TABLE bar (i int) TABLESPACE regress_nosuchspace
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "schema11" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
----------
[CREATE_TABLE] CREATE TABLE part_2 (LIKE list_parted2)
;
----------
[CREATE_TABLE] CREATE TABLE ex_msource (a int, b int)
  WITH (autovacuum_enabled=off)
;
----------
[ADMIN_TABLE] ANALYZE public.array_index_op_test
;
