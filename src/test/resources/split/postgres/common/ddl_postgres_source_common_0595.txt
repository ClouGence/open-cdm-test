CREATE TABLE clstrpart (a int) PARTITION BY RANGE (a)
;
-- create some changes
CREATE TABLE somechange(id serial primary key)
;
ALTER TABLE ONLY pitest2 ALTER COLUMN f3 SET GENERATED BY DEFAULT SET INCREMENT BY 2 SET START WITH 1000 RESTART
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_proc
;
CREATE FUNCTION pg_stat_statements_reset()
RETURNS void
AS '$libdir/pg_stat_statements'
LANGUAGE C PARALLEL SAFE
;
ANALYZE ctv_data
;
-- Creating a derived table using LIKE gets the constraint, but it's valid
CREATE TABLE notnull_tbl1_copy (LIKE notnull_tbl1)
;
CREATE FUNCTION citext_pattern_gt( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE droppk (a int PRIMARY KEY) PARTITION BY RANGE (a)
;
create or replace function return_dquery()
returns setof int as $$
begin
  return query execute 'select * from (values(10),(20)) f';
  return query execute 'select * from (values($1),($2)) f' using 40,50;
end;
$$ language plpgsql
;
ALTER INDEX pg_toast.pg_toast_27803_index RENAME TO reind_con_toast_idx
;
--
-- IMMUTABLE | STABLE | VOLATILE
--
CREATE FUNCTION functest_B_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 0'
;
CREATE TABLE atnnpart1 (col1 int, id int)
;
create temp table tt2 () inherits(tt1)
;
create index on idxpart2 (a collate "C")
;
alter table anothertab alter column atcol1 drop default
;
DROP TABLE num_exp_div
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_class
;
ALTER TABLE temporal_rng3 DROP CONSTRAINT temporal_rng3_uq
;
DROP TABLE temporal_partitioned_fk_rng2rng
;
CREATE TABLE array_index_op_test (
	seqno		int4,
	i			int4[],
	t			text[]
)
;
CREATE DATABASE "regression" WITH TEMPLATE = template0 OID = 16387 ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
ALTER TABLE range_parted ATTACH PARTITION part_b_10_b_20 FOR VALUES FROM ('b', 10) TO ('b', 20)
;
create table idxpart32 partition of idxpart3 for values from (1200) to (1400)
;
CREATE TABLE "public"."query" (
    "expr" "text"
)
;
drop table at_test_sql_partop
;
ALTER INDEX pg_toast.pg_toast_28143_index RENAME TO reind_con_toast_idx
;
CREATE TABLE test_part_2_2 PARTITION OF test_part_2_1 FOR VALUES IN (7,8,9,10)
;
CREATE TABLE "Foo".bar(id int)
;
CREATE INDEX boolidx ON booltmp USING gist ( a )
;
alter extension test_ext7 update to '2.0'
;
ALTER TABLE pg_temp_11.pg_temp_41234_2 ADD COLUMN newdata pg_temp_11.pg_temp_41234
;
-- we want to work with a point_tbl that includes a null
CREATE TEMP TABLE point_tbl AS SELECT * FROM public.point_tbl
;
CREATE TEMP TABLE quad_poly_tbl_ord_idx2 AS
SELECT rank() OVER (ORDER BY p <-> point '123,456') n, p <-> point '123,456' dist, id
FROM quad_poly_tbl WHERE p <@ polygon '((300,300),(400,600),(600,500),(700,200))'
;
analyze tt4
;
CREATE TABLE other_tbl_child () INHERITS (other_tbl_parent)
;
CREATE TABLE "public"."notnull_tbl4" (
    "a" integer NOT NULL
)
;
create table test_1 (id integer primary key) inherits (id)
;
CREATE TRIGGER merge_asu AFTER UPDATE ON target FOR EACH STATEMENT EXECUTE PROCEDURE merge_trigfunc ()
;
CREATE PUBLICATION testpub_forparted
;
alter table rule_v1 rename column column2 to q2
;
CREATE TABLE test_squash_bigint (id int, data bigint)
;
ALTER FUNCTION gbtreekey_var_out(gbtreekey_var) PARALLEL SAFE
;
CREATE TABLE split_select_lock.child (
    id integer PRIMARY KEY,
    parent_id integer REFERENCES split_select_lock.parent(id),
    note text
)
;
CREATE TABLE dump_test.test_inheritance_parent (
						   col1 int NOT NULL,
						   col2 int CHECK (col2 >= 42)
						 )
;
ALTER TYPE "public"."insenum" ADD VALUE 'i11'
;
CREATE FUNCTION isngt(ismn13, ismn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
CREATE TABLE idxpart1 (LIKE idxpart)
;
create function ret_query1(out int, out int) returns setof record as $$
begin
    $1 := -1;
    $2 := -2;
    return next;
    return query select x + 1, x * 10 from generate_series(0, 10) s (x);
    return next;
end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE clstrpart (a int) PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE] -- create some changes
CREATE TABLE somechange(id serial primary key)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY pitest2 ALTER COLUMN f3 SET GENERATED BY DEFAULT SET INCREMENT BY 2 SET START WITH 1000 RESTART
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_proc
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_stat_statements_reset()
RETURNS void
AS '$libdir/pg_stat_statements'
LANGUAGE C PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE ctv_data
;
----------
[CREATE_TABLE] -- Creating a derived table using LIKE gets the constraint, but it's valid
CREATE TABLE notnull_tbl1_copy (LIKE notnull_tbl1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_pattern_gt( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE droppk (a int PRIMARY KEY) PARTITION BY RANGE (a)
;
----------
[CREATE_PROG_OBJ] create or replace function return_dquery()
returns setof int as $$
begin
  return query execute 'select * from (values(10),(20)) f';
  return query execute 'select * from (values($1),($2)) f' using 40,50;
end;
$$ language plpgsql
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27803_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] --
-- IMMUTABLE | STABLE | VOLATILE
--
CREATE FUNCTION functest_B_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 0'
;
----------
[CREATE_TABLE] CREATE TABLE atnnpart1 (col1 int, id int)
;
----------
[CREATE_TABLE] create temp table tt2 () inherits(tt1)
;
----------
[ADD_INDEX] create index on idxpart2 (a collate "C")
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table anothertab alter column atcol1 drop default
;
----------
[DROP_TABLE] DROP TABLE num_exp_div
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_class
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE temporal_rng3 DROP CONSTRAINT temporal_rng3_uq
;
----------
[DROP_TABLE] DROP TABLE temporal_partitioned_fk_rng2rng
;
----------
[CREATE_TABLE] CREATE TABLE array_index_op_test (
	seqno		int4,
	i			int4[],
	t			text[]
)
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression" WITH TEMPLATE = template0 OID = 16387 ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE range_parted ATTACH PARTITION part_b_10_b_20 FOR VALUES FROM ('b', 10) TO ('b', 20)
;
----------
[CREATE_TABLE] create table idxpart32 partition of idxpart3 for values from (1200) to (1400)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."query" (
    "expr" "text"
)
;
----------
[DROP_TABLE] drop table at_test_sql_partop
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28143_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE test_part_2_2 PARTITION OF test_part_2_1 FOR VALUES IN (7,8,9,10)
;
----------
[CREATE_TABLE] CREATE TABLE "Foo".bar(id int)
;
----------
[ADD_INDEX] CREATE INDEX boolidx ON booltmp USING gist ( a )
;
----------
[ALTER_LIBRARY] alter extension test_ext7 update to '2.0'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_41234_2 ADD COLUMN newdata pg_temp_11.pg_temp_41234
;
----------
[CREATE_TABLE|SELECT] -- we want to work with a point_tbl that includes a null
CREATE TEMP TABLE point_tbl AS SELECT * FROM public.point_tbl
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE quad_poly_tbl_ord_idx2 AS
SELECT rank() OVER (ORDER BY p <-> point '123,456') n, p <-> point '123,456' dist, id
FROM quad_poly_tbl WHERE p <@ polygon '((300,300),(400,600),(600,500),(700,200))'
;
----------
[ADMIN_TABLE] analyze tt4
;
----------
[CREATE_TABLE] CREATE TABLE other_tbl_child () INHERITS (other_tbl_parent)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl4" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TABLE] create table test_1 (id integer primary key) inherits (id)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER merge_asu AFTER UPDATE ON target FOR EACH STATEMENT EXECUTE PROCEDURE merge_trigfunc ()
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_forparted
;
----------
[RENAME_COLUMN] alter table rule_v1 rename column column2 to q2
;
----------
[CREATE_TABLE] CREATE TABLE test_squash_bigint (id int, data bigint)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbtreekey_var_out(gbtreekey_var) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE split_select_lock.child (
    id integer PRIMARY KEY,
    parent_id integer REFERENCES split_select_lock.parent(id),
    note text
)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_inheritance_parent (
						   col1 int NOT NULL,
						   col2 int CHECK (col2 >= 42)
						 )
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i11'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ismn13, ismn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
----------
[CREATE_TABLE] CREATE TABLE idxpart1 (LIKE idxpart)
;
----------
[CREATE_PROG_OBJ] create function ret_query1(out int, out int) returns setof record as $$
begin
    $1 := -1;
    $2 := -2;
    return next;
    return query select x + 1, x * 10 from generate_series(0, 10) s (x);
    return next;
end;
$$ language plpgsql
;
