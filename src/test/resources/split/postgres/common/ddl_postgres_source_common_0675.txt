CREATE AGGREGATE rwagg(anyarray) (
    STYPE = anyarray,
    SFUNC = rwagg_sfunc,
    FINALFUNC = rwagg_finalfunc
)
;
CREATE VIEW role_table_grants AS
    SELECT grantor,
           grantee,
           table_catalog,
           table_schema,
           table_name,
           privilege_type,
           is_grantable,
           with_hierarchy
    FROM table_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
ANALYZE pg_temp_13.pg_temp_27032
;
-- LIKE

CREATE TABLE like_datatype_table (
  LIKE datatype_table
  EXCLUDING ALL
)
;
-- create parent indexes
create index on only idxpart ((a/b))
;
alter table if exists user_table add column if not exists address varchar(255)
;
CREATE TABLE test_like_id_3 (LIKE test_like_id_1 INCLUDING IDENTITY)
;
ALTER INDEX pg_toast.pg_toast_30454_index RENAME TO reind_con_toast_idx
;
CREATE TABLE evttrig.part_15_20 PARTITION OF evttrig.part_10_20 (id)
  FOR VALUES FROM (15) TO (20)
;
VACUUM (FREEZE) pg_catalog.pg_opfamily
;
ANALYZE public.hash_i4_heap
;
-- fail

drop function f1(a anyelement, b anyarray,
                 c anycompatible, d anycompatible)
;
ANALYZE pg_catalog.pg_seclabel
;
CREATE VIEW pg_replication_slots AS
    SELECT
            L.slot_name,
            L.plugin,
            L.slot_type,
            L.datoid,
            D.datname AS database,
            L.temporary,
            L.active,
            L.active_pid,
            L.xmin,
            L.catalog_xmin,
            L.restart_lsn,
            L.confirmed_flush_lsn
    FROM pg_get_replication_slots() AS L
            LEFT JOIN pg_database D ON (L.datoid = D.oid)
;
CREATE TABLE "fkpart3"."pk51" (
    "a" integer NOT NULL
)
;
/* Base view for foreign-data wrappers */
CREATE VIEW _pg_foreign_data_wrappers AS
    SELECT w.oid,
           w.fdwowner,
           w.fdwoptions,
           CAST(current_database() AS sql_identifier) AS foreign_data_wrapper_catalog,
           CAST(fdwname AS sql_identifier) AS foreign_data_wrapper_name,
           CAST(u.rolname AS sql_identifier) AS authorization_identifier,
           CAST('c' AS character_data) AS foreign_data_wrapper_language
    FROM pg_foreign_data_wrapper w, pg_authid u
    WHERE u.oid = w.fdwowner
          AND (pg_has_role(fdwowner, 'USAGE')
               OR has_foreign_data_wrapper_privilege(w.oid, 'USAGE'))
;
-- fail
SECURITY LABEL FOR 'dummy' ON TABLE dummy_seclabel_tbl1 IS 'unclassified'
;
create trigger trig_part_attach before insert on tab_part_attach
  for each statement execute procedure func_part_attach()
;
CREATE FUNCTION g_intbig_compress(internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION gbt_intv_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE pg_temp_37.pg_temp_29701
;
CREATE FUNCTION functest_B_2(int) RETURNS bool LANGUAGE 'sql'
       IMMUTABLE AS 'SELECT $1 > 0'
;
DROP SCHEMA IF EXISTS split_agg_probe CASCADE
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=26512 host=/tmp/gUd_xhlenG dbname=postgres' PUBLICATION mypub
;
CREATE TABLE testpub_tbl7 (a int primary key, b text, c text)
;
-- We filter relation OIDs out of the test output in order to avoid
-- test instability. This is currently only needed for EXPLAIN (DEBUG), not
-- EXPLAIN (RANGE_TABLE). Also suppress actual row counts, which are not
-- stable (e.g. 1/8 is 0.12 on some buildfarm machines and 0.13 on others).
CREATE FUNCTION explain_filter(text) RETURNS SETOF text
LANGUAGE plpgsql AS
$$
DECLARE
    ln text;
BEGIN
    FOR ln IN EXECUTE $1
	LOOP
		ln := regexp_replace(ln, 'Relation OIDs:( \m\d+\M)+',
								 'Relation OIDs: NNN...', 'g');
		ln := regexp_replace(ln, '<Relation-OIDs>( ?\m\d+\M)+</Relation-OIDs>',
								 '<Relation-OIDs>NNN...</Relation-OIDs>', 'g');
		ln := regexp_replace(ln, 'actual rows=\d+\.\d+',
								 'actual rows=N.NN', 'g');
		RETURN NEXT ln;
	END LOOP;
END;
$$
;
VACUUM pg_catalog.pg_language
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
CREATE TABLE "public"."mlparted_def" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
PARTITION BY RANGE ("a")
;
ALTER EXTENSION test_pg_dump ADD TABLE regress_pg_dump_table_added
;
-- error
CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0
;
DROP TABLE pg_temp_46.pg_temp_28044_2, pg_temp_46.pg_temp_28044
;
create table part_ee_ff partition of list_parted for values in ('ee', 'ff') partition by range (b)
;
CREATE VIEW rw_view1 AS SELECT a AS aa, b AS bb FROM base_tbl
;
ANALYZE pg_temp_22.pg_temp_28002_2
;
CREATE FUNCTION perl2undef() RETURNS bool
LANGUAGE plperl
TRANSFORM FOR TYPE bool
AS $$
return undef;
$$
;
VACUUM (PARALLEL 0) pg_catalog.pg_namespace
;
drop trigger trig_stmt_after on rem2
;
VACUUM pg_catalog.pg_operator
;
ALTER TABLE ONLY "public"."pagg_tab_ml_p2" ATTACH PARTITION "public"."pagg_tab_ml_p2_s2" FOR VALUES IN ('0002', '0003')
;
CREATE TABLE "public"."timestamp_tbl" (
    "d1" timestamp(2) without time zone
)
;
ALTER OPERATOR FAMILY gist_interval_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
create index on btree_bpchar(f1 bpchar_ops)
;
drop table t3
;
create function excpt_test4() returns text as $$
begin
	begin perform 1/0;
	exception when others then return sqlerrm; end;
end; $$ language plpgsql
;
CREATE TABLE test_partitioned (a int, b text default repeat('x', 5000))
			 PARTITION BY list (a)
;
ALTER TABLE pg_temp_12.pg_temp_26295_2 ADD COLUMN newdata pg_temp_12.pg_temp_26295
;
ALTER INDEX pg_toast.pg_toast_30721_index RENAME TO reind_con_toast_idx
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "asciihword" WITH "public"."ispell", "english_stem"
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE AGGREGATE rwagg(anyarray) (
    STYPE = anyarray,
    SFUNC = rwagg_sfunc,
    FINALFUNC = rwagg_finalfunc
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW role_table_grants AS
    SELECT grantor,
           grantee,
           table_catalog,
           table_schema,
           table_name,
           privilege_type,
           is_grantable,
           with_hierarchy
    FROM table_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_27032
;
----------
[CREATE_TABLE] -- LIKE

CREATE TABLE like_datatype_table (
  LIKE datatype_table
  EXCLUDING ALL
)
;
----------
[ADD_INDEX] -- create parent indexes
create index on only idxpart ((a/b))
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table if exists user_table add column if not exists address varchar(255)
;
----------
[CREATE_TABLE] CREATE TABLE test_like_id_3 (LIKE test_like_id_1 INCLUDING IDENTITY)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30454_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE evttrig.part_15_20 PARTITION OF evttrig.part_10_20 (id)
  FOR VALUES FROM (15) TO (20)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_opfamily
;
----------
[ADMIN_TABLE] ANALYZE public.hash_i4_heap
;
----------
[DROP_PROG_OBJ] -- fail

drop function f1(a anyelement, b anyarray,
                 c anycompatible, d anycompatible)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_seclabel
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_replication_slots AS
    SELECT
            L.slot_name,
            L.plugin,
            L.slot_type,
            L.datoid,
            D.datname AS database,
            L.temporary,
            L.active,
            L.active_pid,
            L.xmin,
            L.catalog_xmin,
            L.restart_lsn,
            L.confirmed_flush_lsn
    FROM pg_get_replication_slots() AS L
            LEFT JOIN pg_database D ON (L.datoid = D.oid)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk51" (
    "a" integer NOT NULL
)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /* Base view for foreign-data wrappers */
CREATE VIEW _pg_foreign_data_wrappers AS
    SELECT w.oid,
           w.fdwowner,
           w.fdwoptions,
           CAST(current_database() AS sql_identifier) AS foreign_data_wrapper_catalog,
           CAST(fdwname AS sql_identifier) AS foreign_data_wrapper_name,
           CAST(u.rolname AS sql_identifier) AS authorization_identifier,
           CAST('c' AS character_data) AS foreign_data_wrapper_language
    FROM pg_foreign_data_wrapper w, pg_authid u
    WHERE u.oid = w.fdwowner
          AND (pg_has_role(fdwowner, 'USAGE')
               OR has_foreign_data_wrapper_privilege(w.oid, 'USAGE'))
;
----------
[SECURITY_LABEL] -- fail
SECURITY LABEL FOR 'dummy' ON TABLE dummy_seclabel_tbl1 IS 'unclassified'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_part_attach before insert on tab_part_attach
  for each statement execute procedure func_part_attach()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_intbig_compress(internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_intv_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_37.pg_temp_29701
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_B_2(int) RETURNS bool LANGUAGE 'sql'
       IMMUTABLE AS 'SELECT $1 > 0'
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_agg_probe CASCADE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=26512 host=/tmp/gUd_xhlenG dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl7 (a int primary key, b text, c text)
;
----------
[CREATE_PROG_OBJ] -- We filter relation OIDs out of the test output in order to avoid
-- test instability. This is currently only needed for EXPLAIN (DEBUG), not
-- EXPLAIN (RANGE_TABLE). Also suppress actual row counts, which are not
-- stable (e.g. 1/8 is 0.12 on some buildfarm machines and 0.13 on others).
CREATE FUNCTION explain_filter(text) RETURNS SETOF text
LANGUAGE plpgsql AS
$$
DECLARE
    ln text;
BEGIN
    FOR ln IN EXECUTE $1
	LOOP
		ln := regexp_replace(ln, 'Relation OIDs:( \m\d+\M)+',
								 'Relation OIDs: NNN...', 'g');
		ln := regexp_replace(ln, '<Relation-OIDs>( ?\m\d+\M)+</Relation-OIDs>',
								 '<Relation-OIDs>NNN...</Relation-OIDs>', 'g');
		ln := regexp_replace(ln, 'actual rows=\d+\.\d+',
								 'actual rows=N.NN', 'g');
		RETURN NEXT ln;
	END LOOP;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_language
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted_def" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
PARTITION BY RANGE ("a")
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD TABLE regress_pg_dump_table_added
;
----------
[CREATE_TABLE|SELECT] -- error
CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0
;
----------
[DROP_TABLE] DROP TABLE pg_temp_46.pg_temp_28044_2, pg_temp_46.pg_temp_28044
;
----------
[CREATE_TABLE] create table part_ee_ff partition of list_parted for values in ('ee', 'ff') partition by range (b)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT a AS aa, b AS bb FROM base_tbl
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_22.pg_temp_28002_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION perl2undef() RETURNS bool
LANGUAGE plperl
TRANSFORM FOR TYPE bool
AS $$
return undef;
$$
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_namespace
;
----------
[DROP_TRIGGER] drop trigger trig_stmt_after on rem2
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_operator
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml_p2" ATTACH PARTITION "public"."pagg_tab_ml_p2_s2" FOR VALUES IN ('0002', '0003')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."timestamp_tbl" (
    "d1" timestamp(2) without time zone
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_interval_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[ADD_INDEX] create index on btree_bpchar(f1 bpchar_ops)
;
----------
[DROP_TABLE] drop table t3
;
----------
[CREATE_PROG_OBJ] create function excpt_test4() returns text as $$
begin
	begin perform 1/0;
	exception when others then return sqlerrm; end;
end; $$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE test_partitioned (a int, b text default repeat('x', 5000))
			 PARTITION BY list (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26295_2 ADD COLUMN newdata pg_temp_12.pg_temp_26295
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30721_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "asciihword" WITH "public"."ispell", "english_stem"
;
