CREATE PUBLICATION pub3
;
CREATE DATABASE dump_test
;
CREATE OPERATOR CLASS gist_int4_ops
DEFAULT FOR TYPE int4 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_int4_consistent (internal, int4, int2, oid, internal),
	FUNCTION	2	gbt_int4_union (internal, internal),
	FUNCTION	3	gbt_int4_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_int4_penalty (internal, internal, internal),
	FUNCTION	6	gbt_int4_picksplit (internal, internal),
	FUNCTION	7	gbt_int4_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_default_acl
;
-- this will now succeed:
alter table tt14t alter column f4 type integer using f4::integer
;
-- failed (policy violation)

--
-- Clean up
--
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_superuser_t:s0-s0:c0.c255
DROP TABLE IF EXISTS t1 CASCADE
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_publication_namespace
;
CREATE TABLE target_pa2 PARTITION OF target_pa FOR VALUES IN (2)
;
CREATE EXTENSION test_shm_mq
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=30672 host=/tmp/INMnCgU8So dbname=postgres' PUBLICATION pub_all
;
CREATE INDEX hash_tuplesort_idx ON tenk1 USING hash (stringu1 name_ops) WITH (fillfactor = 10)
;
-- fires only _trigger
create table event_trigger_fire3 (a int)
;
-- test thesaurus in configuration
-- see thesaurus_sample.ths to understand 'odd' resulting tsvector
CREATE TEXT SEARCH CONFIGURATION thesaurus_tst (
						COPY=synonym_tst
)
;
-- ************************************************************
-- * BEFORE UPDATE on PHone
-- *	- do delete/insert instead of update if name changes
-- ************************************************************
create function tg_phone_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from PHone where slotname = old.slotname;
	insert into PHone (
		    slotname,
		    comment,
		    slotlink
		) values (
		    new.slotname,
		    new.comment,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
ANALYZE public.rtest_nothn1
;
CREATE INDEX concur_reindex_part_index_10 ON ONLY concur_reindex_part_10 (c1)
;
CREATE TEXT SEARCH CONFIGURATION addr_ts_conf (copy=english)
;
drop type comptype
;
drop function dump_update_new, dump_update_old
;
CREATE TABLE pp_enumpart1 PARTITION OF pp_enumpart
							FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
CREATE TABLE test_maint(i INT)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_rewrite
;
CREATE FUNCTION g_int_decompress(internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE T ADD COLUMN c_timestamp TIMESTAMP
                  DEFAULT '2016-09-01'::DATE + LENGTH(foo(10)),
              ALTER COLUMN c_date
                  SET DEFAULT '2010-01-01'::DATE - LENGTH(foo(4))
;
-- Try EXPLAIN ANALYZE SELECT INTO and EXPLAIN ANALYZE CREATE TABLE AS
-- WITH NO DATA, but hide the outputs since they won't be stable.
DO $$
BEGIN
	EXECUTE 'EXPLAIN ANALYZE SELECT * INTO TABLE easi FROM int8_tbl';
	EXECUTE 'EXPLAIN ANALYZE CREATE TABLE easi2 AS SELECT * FROM int8_tbl WITH NO DATA';
END$$
;
ALTER TABLE part_c_1_c_20 DROP a, DROP b, ADD a text, ADD b bigint
;
create table event_trigger_fire6 (a int)
;
CREATE OR REPLACE FUNCTION pg_relpages(IN relname text)
RETURNS BIGINT
AS '$libdir/pgstattuple', 'pg_relpages_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
-- link some existing serial-owning table to the extension
alter extension test_ext7 add table old_table1
;
VACUUM hash_cleanup_heap
;
create table rparted_by_int2_maxvalue partition of rparted_by_int2 for values from (16384) to (maxvalue)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
VACUUM pg_catalog.pg_amproc
;
CREATE OPERATOR CLASS inet_ops
DEFAULT FOR TYPE inet USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       network_cmp(inet,inet),
    FUNCTION        2       gin_extract_value_inet(inet, internal),
    FUNCTION        3       gin_extract_query_inet(inet, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_inet(inet,inet,int2, internal),
STORAGE         inet
;
ALTER TABLE tab_stats ADD CONSTRAINT a_nonzero CHECK (a <> 0)
;
CREATE TABLE test_slru_stats(slru TEXT, stat TEXT, value INT)
;
CREATE TEXT SEARCH DICTIONARY addr_ts_dict (template=simple)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_shdepend
;
CREATE TYPE split_domain_type_next.int4_demo_range AS RANGE (subtype = integer, multirange_type_name = split_domain_type_next.int4_demo_multirange)
;
CREATE TABLE "public"."utf8_verification_inputs" (
    "inbytes" "bytea",
    "description" "text" NOT NULL
)
;
COMMENT ON FUNCTION seg_gt(seg, seg) IS
'greater than'
;
ALTER TABLE attmp ADD COLUMN t box
;
--
-- ALTER xxx RENAME TO
--
ALTER DATABASE sepgsql_test_regression_1 RENAME TO sepgsql_test_regression
;
DROP TABLE range_parted
;
ANALYZE prt1_l
;
CREATE TABLE "public"."xy_1" (
    "x" integer,
    "y" integer
)
;
alter table trigtest disable trigger trigtest_b_row_tg
;
-- error
CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
  SELECT 1 / 0
;
VACUUM (SKIP_DATABASE_STATS) vactst
;
ALTER TABLE droppk DETACH PARTITION droppk_d
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE PUBLICATION pub3
;
----------
[CREATE_CATALOG] CREATE DATABASE dump_test
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_int4_ops
DEFAULT FOR TYPE int4 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_int4_consistent (internal, int4, int2, oid, internal),
	FUNCTION	2	gbt_int4_union (internal, internal),
	FUNCTION	3	gbt_int4_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_int4_penalty (internal, internal, internal),
	FUNCTION	6	gbt_int4_picksplit (internal, internal),
	FUNCTION	7	gbt_int4_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_default_acl
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- this will now succeed:
alter table tt14t alter column f4 type integer using f4::integer
;
----------
[DROP_TABLE] -- failed (policy violation)

--
-- Clean up
--
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_superuser_t:s0-s0:c0.c255
DROP TABLE IF EXISTS t1 CASCADE
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_publication_namespace
;
----------
[CREATE_TABLE] CREATE TABLE target_pa2 PARTITION OF target_pa FOR VALUES IN (2)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_shm_mq
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=30672 host=/tmp/INMnCgU8So dbname=postgres' PUBLICATION pub_all
;
----------
[ADD_INDEX] CREATE INDEX hash_tuplesort_idx ON tenk1 USING hash (stringu1 name_ops) WITH (fillfactor = 10)
;
----------
[CREATE_TABLE] -- fires only _trigger
create table event_trigger_fire3 (a int)
;
----------
[CREATE_POLICY] -- test thesaurus in configuration
-- see thesaurus_sample.ths to understand 'odd' resulting tsvector
CREATE TEXT SEARCH CONFIGURATION thesaurus_tst (
						COPY=synonym_tst
)
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * BEFORE UPDATE on PHone
-- *	- do delete/insert instead of update if name changes
-- ************************************************************
create function tg_phone_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from PHone where slotname = old.slotname;
	insert into PHone (
		    slotname,
		    comment,
		    slotlink
		) values (
		    new.slotname,
		    new.comment,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_nothn1
;
----------
[ADD_INDEX] CREATE INDEX concur_reindex_part_index_10 ON ONLY concur_reindex_part_10 (c1)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION addr_ts_conf (copy=english)
;
----------
[DROP_TYPE] drop type comptype
;
----------
[DROP_PROG_OBJ] drop function dump_update_new, dump_update_old
;
----------
[CREATE_TABLE] CREATE TABLE pp_enumpart1 PARTITION OF pp_enumpart
							FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[CREATE_TABLE] CREATE TABLE test_maint(i INT)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_rewrite
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_int_decompress(internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_timestamp TIMESTAMP
                  DEFAULT '2016-09-01'::DATE + LENGTH(foo(10)),
              ALTER COLUMN c_date
                  SET DEFAULT '2010-01-01'::DATE - LENGTH(foo(4))
;
----------
[BLOCK] -- Try EXPLAIN ANALYZE SELECT INTO and EXPLAIN ANALYZE CREATE TABLE AS
-- WITH NO DATA, but hide the outputs since they won't be stable.
DO $$
BEGIN
	EXECUTE 'EXPLAIN ANALYZE SELECT * INTO TABLE easi FROM int8_tbl';
	EXECUTE 'EXPLAIN ANALYZE CREATE TABLE easi2 AS SELECT * FROM int8_tbl WITH NO DATA';
END$$
;
----------
[ALTER_TABLE|DROP_COLUMN|ADD_COLUMN] ALTER TABLE part_c_1_c_20 DROP a, DROP b, ADD a text, ADD b bigint
;
----------
[CREATE_TABLE] create table event_trigger_fire6 (a int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_relpages(IN relname text)
RETURNS BIGINT
AS '$libdir/pgstattuple', 'pg_relpages_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_LIBRARY] -- link some existing serial-owning table to the extension
alter extension test_ext7 add table old_table1
;
----------
[ADMIN_TABLE] VACUUM hash_cleanup_heap
;
----------
[CREATE_TABLE] create table rparted_by_int2_maxvalue partition of rparted_by_int2 for values from (16384) to (maxvalue)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_amproc
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS inet_ops
DEFAULT FOR TYPE inet USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       network_cmp(inet,inet),
    FUNCTION        2       gin_extract_value_inet(inet, internal),
    FUNCTION        3       gin_extract_query_inet(inet, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_inet(inet,inet,int2, internal),
STORAGE         inet
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tab_stats ADD CONSTRAINT a_nonzero CHECK (a <> 0)
;
----------
[CREATE_TABLE] CREATE TABLE test_slru_stats(slru TEXT, stat TEXT, value INT)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY addr_ts_dict (template=simple)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_shdepend
;
----------
[CREATE_TYPE] CREATE TYPE split_domain_type_next.int4_demo_range AS RANGE (subtype = integer, multirange_type_name = split_domain_type_next.int4_demo_multirange)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."utf8_verification_inputs" (
    "inbytes" "bytea",
    "description" "text" NOT NULL
)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_gt(seg, seg) IS
'greater than'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN t box
;
----------
[RENAME_CATALOG] --
-- ALTER xxx RENAME TO
--
ALTER DATABASE sepgsql_test_regression_1 RENAME TO sepgsql_test_regression
;
----------
[DROP_TABLE] DROP TABLE range_parted
;
----------
[ADMIN_TABLE] ANALYZE prt1_l
;
----------
[CREATE_TABLE] CREATE TABLE "public"."xy_1" (
    "x" integer,
    "y" integer
)
;
----------
[ALTER_TABLE] alter table trigtest disable trigger trigtest_b_row_tg
;
----------
[CREATE_VIEW(SELECT)] -- error
CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
  SELECT 1 / 0
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) vactst
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE droppk DETACH PARTITION droppk_d
;
