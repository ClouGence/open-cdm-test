vacuum verbose testtab
;
-- and after clustering on clstr_expression_minus_a
CLUSTER clstr_expression USING clstr_expression_minus_a
;
CREATE TABLE "MySchema"."Foo"(f1 int, f2 text, f3 text[], primary key (f1,f2))
;
CREATE TABLE pt1 PARTITION OF pt1_2 FOR VALUES IN (1)
;
CREATE SEQUENCE sequence_testx OWNED BY sequence_test_table.b
;
drop function polyf(x anyrange)
;
CREATE TABLE split_cov.part_table (id int, created_on date) PARTITION BY RANGE (created_on)
;
-- WITH DATA, passes.
CREATE TABLE selinto_schema.tbl_withdata1 (a)
  AS SELECT generate_series(1,3) WITH DATA
;
VACUUM (FREEZE) pg_catalog.pg_attrdef
;
ALTER INDEX pg_toast.pg_toast_30341_index RENAME TO reind_con_toast_idx
;
-- columns in regular and LIKE inheritance should be marked not-nullable
-- for primary keys, even if those are deferred
CREATE TABLE notnull_tbl4 (a INTEGER PRIMARY KEY INITIALLY DEFERRED)
;
DROP function gseg_compress(pg_catalog.internal)
;
ANALYZE public.num_exp_add
;
ALTER FUNCTION tstz_dist(timestamp with time zone, timestamp with time zone) PARALLEL SAFE
;
ANALYZE public.rtest_t2
;
CREATE VIEW rw_view1 AS SELECT b AS bb, a AS aa FROM base_tbl
;
ALTER TABLE tr_unique RENAME TO tr_pkey
;
CREATE TRIGGER alter_table_under_transition_tables_upd_trigger
  AFTER TRUNCATE OR UPDATE ON alter_table_under_transition_tables
  REFERENCING OLD TABLE AS d NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    alter_table_under_transition_tables_upd_func()
;
CREATE FUNCTION hstore_subscript_handler(internal)
RETURNS internal
AS '$libdir/hstore', 'hstore_subscript_handler'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP TABLE pa_targetp
;
ANALYZE public.num_exp_sub
;
CREATE VIEW "public"."limit_thousand_v_2" AS
 SELECT "thousand"
   FROM "public"."onek"
  WHERE ("thousand" < 995)
  ORDER BY "thousand"
 OFFSET 10
 LIMIT 5
;
alter table has_hole drop column f2
;
ALTER TABLE pg_temp_4.pg_temp_29539_2 ADD COLUMN newdata pg_temp_4.pg_temp_29539
;
-- fail
CREATE PUBLICATION testpub3
;
CREATE TABLE TBL1 (i int4)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_class
;
CREATE TABLE z1 (a int, b text)
;
create table s1.p2_1 partition of s1.p2 for values in (1, 2, 3)
;
ALTER OPERATOR >= (ean13, issn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
-- not supported by sepgsql
ALTER TABLE regtest_table_part ENABLE  TRIGGER regtest_part_test_trig
;
CREATE FUNCTION gbt_int2_distance(internal,int2,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION "public"."gtest_trigger_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  IF tg_op IN ('DELETE', 'UPDATE') THEN
    RAISE INFO '%: %: old = %', TG_NAME, TG_WHEN, OLD;
  END IF;
  IF tg_op IN ('INSERT', 'UPDATE') THEN
    RAISE INFO '%: %: new = %', TG_NAME, TG_WHEN, NEW;
  END IF;
  IF tg_op = 'DELETE' THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END
$$
;
CREATE OPERATOR CLASS gist_cube_ops
    DEFAULT FOR TYPE cube USING gist AS
	OPERATOR	3	&& ,
	OPERATOR	6	= ,
	OPERATOR	7	@> ,
	OPERATOR	8	<@ ,
	OPERATOR	13	@ ,
	OPERATOR	14	~ ,
	OPERATOR	15	~> (cube, int) FOR ORDER BY float_ops,
	OPERATOR	16	<#> (cube, cube) FOR ORDER BY float_ops,
	OPERATOR	17	<-> (cube, cube) FOR ORDER BY float_ops,
	OPERATOR	18	<=> (cube, cube) FOR ORDER BY float_ops,

	FUNCTION	1	g_cube_consistent (internal, cube, smallint, oid, internal),
	FUNCTION	2	g_cube_union (internal, internal),
	FUNCTION	3	g_cube_compress (internal),
	FUNCTION	4	g_cube_decompress (internal),
	FUNCTION	5	g_cube_penalty (internal, internal, internal),
	FUNCTION	6	g_cube_picksplit (internal, internal),
	FUNCTION	7	g_cube_same (cube, cube, internal),
	FUNCTION	8	g_cube_distance (internal, cube, smallint, oid, internal)
;
DROP TABLE rules_parted_table
;
DO $$
DECLARE
    objtype text;
BEGIN
    FOR objtype IN VALUES ('toast table'), ('index column'), ('sequence column'),
        ('toast table column'), ('view column'), ('materialized view column')
    LOOP
        BEGIN
            PERFORM pg_get_object_address(objtype, '{one}', '{}');
        EXCEPTION WHEN invalid_parameter_value THEN
            RAISE WARNING 'error for %: %', objtype, sqlerrm;
        END;
    END LOOP;
END;
$$
;
VACUUM (FULL) pg_catalog.pg_auth_members
;
alter table fkpart0.fk_part_23_2 drop constraint fk_part_23_a_fkey
;
create table lp_null partition of lp for values in (null)
;
-- and a simple strict function that's opaque to the optimizer
create function strictf(bool, bool) returns bool
language plpgsql as $$begin return $1 and not $2; end$$ strict
;
create domain dnotnulltest integer
;
CREATE TABLE "public"."gtestxx_4" (
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    "a" integer NOT NULL
)
;
DROP TABLE test_like_gen_1, test_like_gen_2, test_like_gen_3
;
create type jb_unordered_pair as (x int, y int)
;
CREATE TABLE list_default PARTITION OF list_parted default
;
-- ************************************************************
-- * BEFORE INSERT or UPDATE on WSlot
-- *	- Check that room exists
-- ************************************************************
create function tg_wslot_biu() returns trigger as $$
begin
    if count(*) = 0 from Room where roomno = new.roomno then
        raise exception 'Room % does not exist', new.roomno;
    end if;
    return new;
end;
$$ language plpgsql
;
CREATE TABLE "public"."credit_usage" (
    "cid" integer,
    "ymd" "date",
    "usage" integer
)
;
ALTER TABLE fprt2_p1 SET (autovacuum_enabled = 'false')
;
CREATE EXTENSION test_ext_req_schema1 SCHEMA test_s_dep
;
create function fail() returns int language plpgsql as $$
begin
  return 1/0;
end
$$
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] vacuum verbose testtab
;
----------
[ADMIN_TABLE] -- and after clustering on clstr_expression_minus_a
CLUSTER clstr_expression USING clstr_expression_minus_a
;
----------
[CREATE_TABLE] CREATE TABLE "MySchema"."Foo"(f1 int, f2 text, f3 text[], primary key (f1,f2))
;
----------
[CREATE_TABLE] CREATE TABLE pt1 PARTITION OF pt1_2 FOR VALUES IN (1)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_testx OWNED BY sequence_test_table.b
;
----------
[DROP_PROG_OBJ] drop function polyf(x anyrange)
;
----------
[CREATE_TABLE] CREATE TABLE split_cov.part_table (id int, created_on date) PARTITION BY RANGE (created_on)
;
----------
[CREATE_TABLE|SELECT] -- WITH DATA, passes.
CREATE TABLE selinto_schema.tbl_withdata1 (a)
  AS SELECT generate_series(1,3) WITH DATA
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_attrdef
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30341_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- columns in regular and LIKE inheritance should be marked not-nullable
-- for primary keys, even if those are deferred
CREATE TABLE notnull_tbl4 (a INTEGER PRIMARY KEY INITIALLY DEFERRED)
;
----------
[DROP_PROG_OBJ] DROP function gseg_compress(pg_catalog.internal)
;
----------
[ADMIN_TABLE] ANALYZE public.num_exp_add
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION tstz_dist(timestamp with time zone, timestamp with time zone) PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_t2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT b AS bb, a AS aa FROM base_tbl
;
----------
[RENAME_TABLE] ALTER TABLE tr_unique RENAME TO tr_pkey
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER alter_table_under_transition_tables_upd_trigger
  AFTER TRUNCATE OR UPDATE ON alter_table_under_transition_tables
  REFERENCING OLD TABLE AS d NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    alter_table_under_transition_tables_upd_func()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_subscript_handler(internal)
RETURNS internal
AS '$libdir/hstore', 'hstore_subscript_handler'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE pa_targetp
;
----------
[ADMIN_TABLE] ANALYZE public.num_exp_sub
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."limit_thousand_v_2" AS
 SELECT "thousand"
   FROM "public"."onek"
  WHERE ("thousand" < 995)
  ORDER BY "thousand"
 OFFSET 10
 LIMIT 5
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table has_hole drop column f2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_29539_2 ADD COLUMN newdata pg_temp_4.pg_temp_29539
;
----------
[CREATE_PUB_SUB] -- fail
CREATE PUBLICATION testpub3
;
----------
[CREATE_TABLE] CREATE TABLE TBL1 (i int4)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_class
;
----------
[CREATE_TABLE] CREATE TABLE z1 (a int, b text)
;
----------
[CREATE_TABLE] create table s1.p2_1 partition of s1.p2 for values in (1, 2, 3)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ean13, issn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ALTER_TABLE] -- not supported by sepgsql
ALTER TABLE regtest_table_part ENABLE  TRIGGER regtest_part_test_trig
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int2_distance(internal,int2,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."gtest_trigger_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  IF tg_op IN ('DELETE', 'UPDATE') THEN
    RAISE INFO '%: %: old = %', TG_NAME, TG_WHEN, OLD;
  END IF;
  IF tg_op IN ('INSERT', 'UPDATE') THEN
    RAISE INFO '%: %: new = %', TG_NAME, TG_WHEN, NEW;
  END IF;
  IF tg_op = 'DELETE' THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END
$$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_cube_ops
    DEFAULT FOR TYPE cube USING gist AS
	OPERATOR	3	&& ,
	OPERATOR	6	= ,
	OPERATOR	7	@> ,
	OPERATOR	8	<@ ,
	OPERATOR	13	@ ,
	OPERATOR	14	~ ,
	OPERATOR	15	~> (cube, int) FOR ORDER BY float_ops,
	OPERATOR	16	<#> (cube, cube) FOR ORDER BY float_ops,
	OPERATOR	17	<-> (cube, cube) FOR ORDER BY float_ops,
	OPERATOR	18	<=> (cube, cube) FOR ORDER BY float_ops,

	FUNCTION	1	g_cube_consistent (internal, cube, smallint, oid, internal),
	FUNCTION	2	g_cube_union (internal, internal),
	FUNCTION	3	g_cube_compress (internal),
	FUNCTION	4	g_cube_decompress (internal),
	FUNCTION	5	g_cube_penalty (internal, internal, internal),
	FUNCTION	6	g_cube_picksplit (internal, internal),
	FUNCTION	7	g_cube_same (cube, cube, internal),
	FUNCTION	8	g_cube_distance (internal, cube, smallint, oid, internal)
;
----------
[DROP_TABLE] DROP TABLE rules_parted_table
;
----------
[BLOCK] DO $$
DECLARE
    objtype text;
BEGIN
    FOR objtype IN VALUES ('toast table'), ('index column'), ('sequence column'),
        ('toast table column'), ('view column'), ('materialized view column')
    LOOP
        BEGIN
            PERFORM pg_get_object_address(objtype, '{one}', '{}');
        EXCEPTION WHEN invalid_parameter_value THEN
            RAISE WARNING 'error for %: %', objtype, sqlerrm;
        END;
    END LOOP;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_auth_members
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table fkpart0.fk_part_23_2 drop constraint fk_part_23_a_fkey
;
----------
[CREATE_TABLE] create table lp_null partition of lp for values in (null)
;
----------
[CREATE_PROG_OBJ] -- and a simple strict function that's opaque to the optimizer
create function strictf(bool, bool) returns bool
language plpgsql as $$begin return $1 and not $2; end$$ strict
;
----------
[CREATE_TYPE] create domain dnotnulltest integer
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtestxx_4" (
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    "a" integer NOT NULL
)
;
----------
[DROP_TABLE] DROP TABLE test_like_gen_1, test_like_gen_2, test_like_gen_3
;
----------
[CREATE_TYPE] create type jb_unordered_pair as (x int, y int)
;
----------
[CREATE_TABLE] CREATE TABLE list_default PARTITION OF list_parted default
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * BEFORE INSERT or UPDATE on WSlot
-- *	- Check that room exists
-- ************************************************************
create function tg_wslot_biu() returns trigger as $$
begin
    if count(*) = 0 from Room where roomno = new.roomno then
        raise exception 'Room % does not exist', new.roomno;
    end if;
    return new;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."credit_usage" (
    "cid" integer,
    "ymd" "date",
    "usage" integer
)
;
----------
[ALTER_TABLE] ALTER TABLE fprt2_p1 SET (autovacuum_enabled = 'false')
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext_req_schema1 SCHEMA test_s_dep
;
----------
[CREATE_PROG_OBJ] create function fail() returns int language plpgsql as $$
begin
  return 1/0;
end
$$
;
