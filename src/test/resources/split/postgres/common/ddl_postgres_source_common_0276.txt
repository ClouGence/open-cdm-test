CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_deserialize,
	deserialfunc = numeric_avg_deserialize
)
;
create rule r6 as on update to rules_src do instead UPDATE rules_log AS trgt SET tag = 'updated' WHERE trgt.f1 = new.f1
;
VACUUM information_schema.sql_sizing
;
-- LOCALE conflicts with LC_CTYPE
CREATE COLLATION coll_dup_chk (LC_CTYPE = "POSIX", LOCALE = '')
;
CREATE TABLE part_a_1_a_10 PARTITION OF range_parted FOR VALUES FROM ('a', 1) TO ('a', 10)
;
CREATE FUNCTION namelen(person_type) RETURNS int LANGUAGE SQL AS $$ SELECT length($1.name) $$
;
-- multiple primary key on child should fail
create table failpart partition of idxpart (b primary key) for values from (0) to (100)
;
--
-- Similar test case using text that verifies that collation
-- information is passed through by execTuplesEqual() in nodeSubplan.c
-- (otherwise it would error in texteq())
--

create temp table outer_text (f1 text, f2 text)
;
drop table test
;
ANALYZE fkpart3.pk52
;
DROP TABLE expr_stats_incompatible_test
;
CREATE TABLE "public"."thethings" (
    "stuff" "public"."things"
)
;
DROP TYPE stats_type
;
-- test perl -> hstore[]
CREATE FUNCTION test2arr() RETURNS hstore[]
LANGUAGE plperl
TRANSFORM FOR TYPE hstore
AS $$
$val = [{a => 1, b => 'boo', c => undef}, {d => 2}];
return $val;
$$
;
ALTER TABLE fd_pt1 RENAME COLUMN c3 TO f3
;
create view uv_ptv as select * from uv_pt
;
create function tg_wslot_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from WSlot where slotname = old.slotname;
	insert into WSlot (
		    slotname,
		    roomno,
		    slotlink,
		    backlink
		) values (
		    new.slotname,
		    new.roomno,
		    new.slotlink,
		    new.backlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
create schema testpart
;
CREATE EXTENSION plpgsql
;
-- RangeVarCallbackOwnsRelation()
CREATE INDEX pg_descripton_test_index ON pg_description (description)
;
ANALYZE pg_temp_9.pg_temp_26996_2
;
CREATE TABLE pub_testpart1.child_parent2 partition of pub_testpart2.parent2 for values in (1)
;
CREATE OPERATOR CLASS alt_opc2 FOR TYPE macaddr USING hash AS STORAGE macaddr
;
DROP CAST (text AS text)
;
create function explain_sq_limit() returns setof text language plpgsql as
$$
declare ln text;
begin
    for ln in
        explain (analyze, summary off, timing off, costs off)
        select * from (select pk,c2 from sq_limit order by c1,pk) as x limit 3
    loop
        ln := regexp_replace(ln, 'Memory: \S*',  'Memory: xxx');
        return next ln;
    end loop;
end;
$$
;
drop function explain_analyze(text)
;
--
-- tests for hash partitioned tables.
--
CREATE TABLE pht1 (a int, b int, c text) PARTITION BY HASH(c)
;
ALTER SEQUENCE seq1 RESTART WITH 5
;
-- assorted cases with multiple ALTER TABLE steps
CREATE TABLE ataddindex(f1 INT)
;
create table part_cc_dd partition of list_parted FOR VALUES IN ('cc', 'dd')
;
ALTER TABLE spc_nest SET TABLESPACE other
;
-- Test tables using domains
create table basictest
           ( testint4 domainint4
           , testtext domaintext
           , testvarchar domainvarchar
           , testnumeric domainnumeric
           )
;
CREATE TABLE dump_test.measurement (
						city_id serial not null,
						logdate date not null,
						peaktemp int CHECK (peaktemp >= -460),
						unitsales int
					   ) PARTITION BY RANGE (logdate)
;
-- Can't easily use a unique index, since dead tuples can be found
-- independent of the kill_prior_tuples optimization.
CREATE TABLE clean_aborted_self(key int, data text)
;
-- can't drop NOT NULL from under an invalid PK
CREATE TABLE list_parted3 (a int NOT NULL) PARTITION BY LIST (a)
;
create table at_partitioned_1 partition of at_partitioned
  for values with (modulus 2, remainder 1)
;
DROP FUNCTION echo_me(rainbow)
;
drop table j1
;
ALTER TABLE itest1 ALTER COLUMN a SET DEFAULT 1
;
CREATE SCHEMA regress_pg_dump_schema
;
VACUUM (PARALLEL 0) pg_catalog.pg_ts_parser
;
-- This next should fail, because int=inet does not exist
CREATE TABLE FKTABLE (ftest1 inet REFERENCES pktable)
;
create table parted_trigger_3_2 partition of parted_trigger_3 for values from (4) to (8)
;
ALTER TABLE pg_toast.pg_toast_30768 RENAME TO reind_con_toast
;
DROP TABLE unique_tbl
;
DROP OPERATOR FAMILY alt_opf15 USING hash
;
CREATE OPERATOR @@>> ( PROCEDURE = poly_contain_pt,
  LEFTARG = polygon, RIGHTARG = point,
  COMMUTATOR = <<@@ )
;
CREATE EXTENSION injection_points
;
CREATE TEXT SEARCH DICTIONARY hunspell_err (
						Template=ispell,
						DictFile=ispell_sample,
						AffFile=hunspell_sample_long
)
;
-- not OK
CREATE TABLE pg_temp.implicitly_temp (a int primary key)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_deserialize,
	deserialfunc = numeric_avg_deserialize
)
;
----------
[CREATE_POLICY] create rule r6 as on update to rules_src do instead UPDATE rules_log AS trgt SET tag = 'updated' WHERE trgt.f1 = new.f1
;
----------
[ADMIN_TABLE] VACUUM information_schema.sql_sizing
;
----------
[CREATE_POLICY] -- LOCALE conflicts with LC_CTYPE
CREATE COLLATION coll_dup_chk (LC_CTYPE = "POSIX", LOCALE = '')
;
----------
[CREATE_TABLE] CREATE TABLE part_a_1_a_10 PARTITION OF range_parted FOR VALUES FROM ('a', 1) TO ('a', 10)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION namelen(person_type) RETURNS int LANGUAGE SQL AS $$ SELECT length($1.name) $$
;
----------
[CREATE_TABLE] -- multiple primary key on child should fail
create table failpart partition of idxpart (b primary key) for values from (0) to (100)
;
----------
[CREATE_TABLE] --
-- Similar test case using text that verifies that collation
-- information is passed through by execTuplesEqual() in nodeSubplan.c
-- (otherwise it would error in texteq())
--

create temp table outer_text (f1 text, f2 text)
;
----------
[DROP_TABLE] drop table test
;
----------
[ADMIN_TABLE] ANALYZE fkpart3.pk52
;
----------
[DROP_TABLE] DROP TABLE expr_stats_incompatible_test
;
----------
[CREATE_TABLE] CREATE TABLE "public"."thethings" (
    "stuff" "public"."things"
)
;
----------
[DROP_TYPE] DROP TYPE stats_type
;
----------
[CREATE_PROG_OBJ] -- test perl -> hstore[]
CREATE FUNCTION test2arr() RETURNS hstore[]
LANGUAGE plperl
TRANSFORM FOR TYPE hstore
AS $$
$val = [{a => 1, b => 'boo', c => undef}, {d => 2}];
return $val;
$$
;
----------
[RENAME_COLUMN] ALTER TABLE fd_pt1 RENAME COLUMN c3 TO f3
;
----------
[CREATE_VIEW(SELECT)] create view uv_ptv as select * from uv_pt
;
----------
[CREATE_PROG_OBJ] create function tg_wslot_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from WSlot where slotname = old.slotname;
	insert into WSlot (
		    slotname,
		    roomno,
		    slotlink,
		    backlink
		) values (
		    new.slotname,
		    new.roomno,
		    new.slotlink,
		    new.backlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_SCHEMA] create schema testpart
;
----------
[CREATE_LIBRARY] CREATE EXTENSION plpgsql
;
----------
[ADD_INDEX] -- RangeVarCallbackOwnsRelation()
CREATE INDEX pg_descripton_test_index ON pg_description (description)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26996_2
;
----------
[CREATE_TABLE] CREATE TABLE pub_testpart1.child_parent2 partition of pub_testpart2.parent2 for values in (1)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS alt_opc2 FOR TYPE macaddr USING hash AS STORAGE macaddr
;
----------
[DROP_PROG_OBJ] DROP CAST (text AS text)
;
----------
[CREATE_PROG_OBJ] create function explain_sq_limit() returns setof text language plpgsql as
$$
declare ln text;
begin
    for ln in
        explain (analyze, summary off, timing off, costs off)
        select * from (select pk,c2 from sq_limit order by c1,pk) as x limit 3
    loop
        ln := regexp_replace(ln, 'Memory: \S*',  'Memory: xxx');
        return next ln;
    end loop;
end;
$$
;
----------
[DROP_PROG_OBJ] drop function explain_analyze(text)
;
----------
[CREATE_TABLE] --
-- tests for hash partitioned tables.
--
CREATE TABLE pht1 (a int, b int, c text) PARTITION BY HASH(c)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE seq1 RESTART WITH 5
;
----------
[CREATE_TABLE] -- assorted cases with multiple ALTER TABLE steps
CREATE TABLE ataddindex(f1 INT)
;
----------
[CREATE_TABLE] create table part_cc_dd partition of list_parted FOR VALUES IN ('cc', 'dd')
;
----------
[ALTER_TABLE] ALTER TABLE spc_nest SET TABLESPACE other
;
----------
[CREATE_TABLE] -- Test tables using domains
create table basictest
           ( testint4 domainint4
           , testtext domaintext
           , testvarchar domainvarchar
           , testnumeric domainnumeric
           )
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.measurement (
						city_id serial not null,
						logdate date not null,
						peaktemp int CHECK (peaktemp >= -460),
						unitsales int
					   ) PARTITION BY RANGE (logdate)
;
----------
[CREATE_TABLE] -- Can't easily use a unique index, since dead tuples can be found
-- independent of the kill_prior_tuples optimization.
CREATE TABLE clean_aborted_self(key int, data text)
;
----------
[CREATE_TABLE] -- can't drop NOT NULL from under an invalid PK
CREATE TABLE list_parted3 (a int NOT NULL) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] create table at_partitioned_1 partition of at_partitioned
  for values with (modulus 2, remainder 1)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION echo_me(rainbow)
;
----------
[DROP_TABLE] drop table j1
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest1 ALTER COLUMN a SET DEFAULT 1
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_pg_dump_schema
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_ts_parser
;
----------
[CREATE_TABLE] -- This next should fail, because int=inet does not exist
CREATE TABLE FKTABLE (ftest1 inet REFERENCES pktable)
;
----------
[CREATE_TABLE] create table parted_trigger_3_2 partition of parted_trigger_3 for values from (4) to (8)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30768 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE unique_tbl
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf15 USING hash
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @@>> ( PROCEDURE = poly_contain_pt,
  LEFTARG = polygon, RIGHTARG = point,
  COMMUTATOR = <<@@ )
;
----------
[CREATE_LIBRARY] CREATE EXTENSION injection_points
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hunspell_err (
						Template=ispell,
						DictFile=ispell_sample,
						AffFile=hunspell_sample_long
)
;
----------
[CREATE_TABLE] -- not OK
CREATE TABLE pg_temp.implicitly_temp (a int primary key)
;
