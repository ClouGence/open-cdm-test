CREATE DEFAULT CONVERSION public.mydef FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
ALTER TABLE ONLY "generated_stored_tests"."gtest21b"
    ADD CONSTRAINT "gtest21b_pkey" PRIMARY KEY ("a")
;
ALTER SEQUENCE "public"."rules_log_id_seq" OWNED BY "public"."rules_log"."id"
;
ALTER INDEX pg_toast.pg_toast_30599_index RENAME TO reind_con_toast_idx
;
CALL test_proc8a(_a, _b)
;
CREATE SCHEMA drop_schema_test1
;
ALTER TYPE "public"."insenum" ADD VALUE 'i17'
;
ALTER TABLE toasttest set (toast_tuple_target = 4080)
;
CREATE FUNCTION func() RETURNS VOID AS
$$ ALTER SUBSCRIPTION regress_testsub SET PUBLICATION mypub WITH (refresh = true) $$ LANGUAGE SQL
;
create table pp_arrpart1 partition of pp_arrpart for values in ('{1}')
;
drop table xid8_t1
;
-- verify relation cache invalidation when a primary key is added using
-- an existing index
CREATE TABLE pub_test.testpub_addpk (id int not null, data int)
;
--
-- Check that pruning with composite range partitioning works correctly when
-- it must ignore clauses for trailing keys once it has seen a clause with
-- non-inclusive operator for an earlier key
--
create table mc3p (a int, b int, c int) partition by range (a, abs(b), c)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_publication_namespace
;
create table s5.p1_2 partition of s5.p1 for values in (4, 5, 6)
;
CREATE RULE "rtest_order_r1" AS
    ON INSERT TO "public"."rtest_order1" DO INSTEAD  INSERT INTO "public"."rtest_order2" ("a", "b", "c")
  VALUES ("new"."a", "nextval"('"public"."rtest_seq"'::"regclass"), 'rule 1 - this should run 1st'::"text")
;
-- Partitioned table with no partitions
create table idxpart2 partition of idxpart for values from (1000) to (2000) partition by range (a)
;
CREATE PUBLICATION "dump_pub_qual_2ct" WITH (publish = 'insert, update, delete, truncate')
;
CREATE FUNCTION gbt_bytea_consistent(internal,bytea,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- And dropping it from parted should leave no trace of them on part_b, unlike
-- traditional inheritance where they will be left behind, because they would
-- be local constraints.
ALTER TABLE parted DROP CONSTRAINT check_a, DROP CONSTRAINT check_b
;
drop table loct1
;
CREATE TYPE hstore (
        INTERNALLENGTH = -1,
        INPUT = hstore_in,
        OUTPUT = hstore_out,
        RECEIVE = hstore_recv,
        SEND = hstore_send,
        STORAGE = extended
)
;
CALL sum_two(1,1)
;
ALTER TABLE test_tab2 ADD COLUMN b serial PRIMARY KEY
;
CREATE TABLE "public"."cc2" (
    "f1" integer CONSTRAINT "pp1_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" integer,
    "f4" double precision,
    "a2" integer
)
;
do $$
declare r1 record;
begin
  raise notice 'r1 = %', r1;
  raise notice 'r1.f1 = %', r1.f1;
  raise notice 'r1.f2 = %', r1.f2;
  raise notice 'r1 = %', r1;
end$$
;
CREATE INDEX tbspace_reindex_part_index_0_2 ON ONLY tbspace_reindex_part_0_2 (c1)
;
create or replace temp view v1 as
  select 2+2+4 as f1
;
ANALYZE pg_temp_2.pg_temp_30214_2
;
drop function polyf(anymultirange)
;
CREATE FUNCTION ltree_picksplit(internal, internal)
RETURNS internal as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP DATABASE otherdb
;
-- this should fail because stype isn't compatible with arg
create aggregate build_group(int8, integer) (
  SFUNC = add_group,
  STYPE = int2[]
)
;
CREATE TABLE notnull_tbl6_1 PARTITION OF notnull_tbl6 FOR VALUES IN (1)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest30_1" INHERIT "public"."gtest30"
;
--
-- CASE
-- Test the case statement
--

CREATE TABLE CASE_TBL (
  i integer,
  f double precision
)
;
ALTER PUBLICATION testpib_ins_trunct ADD TABLE pub_test.testpub_nopk, testpub_tbl1
;
/*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
-- PL/pgSQL function
CREATE FUNCTION PLUS_TWO(i INTEGER) RETURNS INTEGER AS $$
DECLARE
  r INTEGER;
BEGIN
  SELECT (i + 1 + 1.0)::INTEGER INTO r;
  RETURN r;
END; $$ LANGUAGE plpgsql
;
CREATE TABLE "public"."rtest_t4" (
    "a" integer,
    "b" "text"
)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "version" WITH "simple"
;
CREATE OPERATOR @= (
    LEFTARG = real,
    RIGHTARG = boolean,
    PROCEDURE = alter_op_test_fn_real_bool
)
;
ALTER PUBLICATION "dump_pub_1ct" ADD TABLE ONLY "pubme"."t0"
;
DROP TEXT SEARCH CONFIGURATION test_tsconfig_exists
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should fail, too many rows
  execute 'select * from foo where f1 > 3' into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
COMMENT ON LARGE OBJECT 16605 IS 'comment on large object'
;
ALTER OPERATOR <= (ismn13, ismn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
ALTER INDEX pg_toast.pg_toast_30954_index RENAME TO reind_con_toast_idx
;
--
-- Test built-in conversion functions.
--

-- Helper function to test a conversion. Uses the test_enc_conversion function
-- that was created in the create_function_0 test.
create or replace function test_conv(
  input IN bytea,
  src_encoding IN text,
  dst_encoding IN text,

  result OUT bytea,
  errorat OUT bytea,
  error OUT text)
language plpgsql as
$$
declare
  validlen int;
begin
  -- First try to perform the conversion with noError = false. If that errors out,
  -- capture the error message, and try again with noError = true. The second call
  -- should succeed and return the position of the error, return that too.
  begin
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, false);
    errorat = NULL;
    error := NULL;
  exception when others then
    error := sqlerrm;
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, true);
    errorat = substr(input, validlen + 1);
  end;
  return;
end;
$$
;
alter table target_parted detach partition attach_parted
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] CREATE DEFAULT CONVERSION public.mydef FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest21b"
    ADD CONSTRAINT "gtest21b_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."rules_log_id_seq" OWNED BY "public"."rules_log"."id"
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30599_index RENAME TO reind_con_toast_idx
;
----------
[CALL_PROG_OBJ] CALL test_proc8a(_a, _b)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA drop_schema_test1
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i17'
;
----------
[ALTER_TABLE] ALTER TABLE toasttest set (toast_tuple_target = 4080)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func() RETURNS VOID AS
$$ ALTER SUBSCRIPTION regress_testsub SET PUBLICATION mypub WITH (refresh = true) $$ LANGUAGE SQL
;
----------
[CREATE_TABLE] create table pp_arrpart1 partition of pp_arrpart for values in ('{1}')
;
----------
[DROP_TABLE] drop table xid8_t1
;
----------
[CREATE_TABLE] -- verify relation cache invalidation when a primary key is added using
-- an existing index
CREATE TABLE pub_test.testpub_addpk (id int not null, data int)
;
----------
[CREATE_TABLE] --
-- Check that pruning with composite range partitioning works correctly when
-- it must ignore clauses for trailing keys once it has seen a clause with
-- non-inclusive operator for an earlier key
--
create table mc3p (a int, b int, c int) partition by range (a, abs(b), c)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_publication_namespace
;
----------
[CREATE_TABLE] create table s5.p1_2 partition of s5.p1 for values in (4, 5, 6)
;
----------
[CREATE_POLICY] CREATE RULE "rtest_order_r1" AS
    ON INSERT TO "public"."rtest_order1" DO INSTEAD  INSERT INTO "public"."rtest_order2" ("a", "b", "c")
  VALUES ("new"."a", "nextval"('"public"."rtest_seq"'::"regclass"), 'rule 1 - this should run 1st'::"text")
;
----------
[CREATE_TABLE] -- Partitioned table with no partitions
create table idxpart2 partition of idxpart for values from (1000) to (2000) partition by range (a)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "dump_pub_qual_2ct" WITH (publish = 'insert, update, delete, truncate')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bytea_consistent(internal,bytea,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- And dropping it from parted should leave no trace of them on part_b, unlike
-- traditional inheritance where they will be left behind, because they would
-- be local constraints.
ALTER TABLE parted DROP CONSTRAINT check_a, DROP CONSTRAINT check_b
;
----------
[DROP_TABLE] drop table loct1
;
----------
[CREATE_TYPE] CREATE TYPE hstore (
        INTERNALLENGTH = -1,
        INPUT = hstore_in,
        OUTPUT = hstore_out,
        RECEIVE = hstore_recv,
        SEND = hstore_send,
        STORAGE = extended
)
;
----------
[CALL_PROG_OBJ] CALL sum_two(1,1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_tab2 ADD COLUMN b serial PRIMARY KEY
;
----------
[CREATE_TABLE] CREATE TABLE "public"."cc2" (
    "f1" integer CONSTRAINT "pp1_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" integer,
    "f4" double precision,
    "a2" integer
)
;
----------
[BLOCK] do $$
declare r1 record;
begin
  raise notice 'r1 = %', r1;
  raise notice 'r1.f1 = %', r1.f1;
  raise notice 'r1.f2 = %', r1.f2;
  raise notice 'r1 = %', r1;
end$$
;
----------
[ADD_INDEX] CREATE INDEX tbspace_reindex_part_index_0_2 ON ONLY tbspace_reindex_part_0_2 (c1)
;
----------
[CREATE_VIEW(SELECT)] create or replace temp view v1 as
  select 2+2+4 as f1
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_30214_2
;
----------
[DROP_PROG_OBJ] drop function polyf(anymultirange)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_picksplit(internal, internal)
RETURNS internal as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_CATALOG] DROP DATABASE otherdb
;
----------
[CREATE_PROG_OBJ] -- this should fail because stype isn't compatible with arg
create aggregate build_group(int8, integer) (
  SFUNC = add_group,
  STYPE = int2[]
)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl6_1 PARTITION OF notnull_tbl6 FOR VALUES IN (1)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest30_1" INHERIT "public"."gtest30"
;
----------
[CREATE_TABLE] --
-- CASE
-- Test the case statement
--

CREATE TABLE CASE_TBL (
  i integer,
  f double precision
)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpib_ins_trunct ADD TABLE pub_test.testpub_nopk, testpub_tbl1
;
----------
[CREATE_POLICY] /*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
----------
[CREATE_PROG_OBJ] -- PL/pgSQL function
CREATE FUNCTION PLUS_TWO(i INTEGER) RETURNS INTEGER AS $$
DECLARE
  r INTEGER;
BEGIN
  SELECT (i + 1 + 1.0)::INTEGER INTO r;
  RETURN r;
END; $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_t4" (
    "a" integer,
    "b" "text"
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "version" WITH "simple"
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @= (
    LEFTARG = real,
    RIGHTARG = boolean,
    PROCEDURE = alter_op_test_fn_real_bool
)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION "dump_pub_1ct" ADD TABLE ONLY "pubme"."t0"
;
----------
[DROP_POLICY] DROP TEXT SEARCH CONFIGURATION test_tsconfig_exists
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should fail, too many rows
  execute 'select * from foo where f1 > 3' into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 16605 IS 'comment on large object'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ismn13, ismn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30954_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] --
-- Test built-in conversion functions.
--

-- Helper function to test a conversion. Uses the test_enc_conversion function
-- that was created in the create_function_0 test.
create or replace function test_conv(
  input IN bytea,
  src_encoding IN text,
  dst_encoding IN text,

  result OUT bytea,
  errorat OUT bytea,
  error OUT text)
language plpgsql as
$$
declare
  validlen int;
begin
  -- First try to perform the conversion with noError = false. If that errors out,
  -- capture the error message, and try again with noError = true. The second call
  -- should succeed and return the position of the error, return that too.
  begin
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, false);
    errorat = NULL;
    error := NULL;
  exception when others then
    error := sqlerrm;
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, true);
    errorat = substr(input, validlen + 1);
  end;
  return;
end;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table target_parted detach partition attach_parted
;
