ALTER FUNCTION gbtreekey8_out(gbtreekey8) PARALLEL SAFE
;
CREATE FUNCTION dblink_open (text, text, text, boolean)
RETURNS text
AS '$libdir/dblink','dblink_open'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
drop trigger child_row_trig on child
;
CREATE FUNCTION errfunc() RETURNS int LANGUAGE SQL AS 'SELECT 1'
SET transaction_read_only = on
;
CREATE FUNCTION texticlike(citext, text)
RETURNS bool AS 'texticlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION const_func () RETURNS int AS $$ SELECT 1; $$ LANGUAGE SQL IMMUTABLE
;
VACUUM (PARALLEL 2) pg_catalog.pg_init_privs
;
-- Create unique indexes.  Due to a general limitation of inheritance,
-- uniqueness is only enforced per-relation.  Unique index inference
-- specification will do the right thing, though.
create unique index cities_names_unique on cities (name)
;
-- should fail: column does not exist
alter table gc1 drop column name
;
ALTER FUNCTION gbt_intv_same(gbtreekey32, gbtreekey32, internal) PARALLEL SAFE
;
CREATE FUNCTION citextrecv(internal)
RETURNS citext
AS 'textrecv'
LANGUAGE internal STABLE STRICT PARALLEL SAFE
;
VACUUM (PARALLEL 0) pg_catalog.pg_proc
;
ANALYZE pagg_tab3
;
create table �ͪ�ߩ�Ѧ��듾� (��듾� text, ��׾��ړ�� varchar, ����1A��󓱸 char(16))
;
ALTER TABLE ONLY "public"."beta_neg" ATTACH PARTITION "public"."beta_neg_p3" FOR VALUES FROM (350) TO (500)
;
--
--
--
-- int8 ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_int8_consistent(internal,int8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=2)
;
VACUUM (SKIP_DATABASE_STATS) public."need""q(uot"
;
drop table rules_base
;
ALTER INDEX pg_toast.pg_toast_27926_index RENAME TO reind_con_toast_idx
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
CREATE TRIGGER joinview_upd_trig INSTEAD OF UPDATE ON joinview
  FOR EACH ROW EXECUTE FUNCTION joinview_upd_trig_fn()
;
CREATE PROCEDURE test_proc8a(INOUT a int, INOUT b int)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %', a, b;
  a := a * 10;
  b := b + 10;
END;
$$
;
ANALYZE (SKIP_LOCKED) information_schema.sql_implementation_info
;
drop table inh_temp_parent cascade
;
CREATE TABLE "public"."pitest1_p1_like" (
    "f1" "date" NOT NULL,
    "f2" "text",
    "f3" bigint NOT NULL
)
;
CREATE VIEW v8_temp AS SELECT * FROM base_table WHERE EXISTS (SELECT 1 FROM temp_table)
;
-- Check variable scoping -- a var is not available in its own or prior
-- default expressions.

create function scope_test() returns int as $$
declare x int := 42;
begin
  declare y int := x + 1;
          x int := x + 2;
  begin
    return x * 100 + y;
  end;
end;
$$ language plpgsql
;
CREATE TABLE split_prog.audit_log (account_id integer, action text, changed_at timestamptz DEFAULT clock_timestamp())
;
ALTER TABLE base_tbl SET (autovacuum_enabled = 'false')
;
ALTER FUNCTION gbt_float8_consistent(internal, double precision, smallint, oid, internal) PARALLEL SAFE
;
--
-- Test that expression-index constraints can be pushed down through
-- UNION or UNION ALL
--

CREATE TEMP TABLE t1 (a text, b text)
;
CREATE TABLE collate_test22 (f2 text COLLATE "POSIX")
;
ALTER OPERATOR FAMILY alt_opf10 USING btree ADD OPERATOR 1 < (int4, int4) FOR ORDER BY float_ops
;
CREATE TABLESPACE regress_tblspacewith LOCATION '/src/src/test/regress/testtablespace' WITH (random_page_cost = 3.0)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_policy
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc9(1)
;
CREATE FUNCTION gin_extract_query_varbit(varbit, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
DROP VIEW view_unnamed_ss_locking
;
-- fail

drop function anyctest(anycompatible, anycompatiblearray)
;
ALTER TABLE pg_toast.pg_toast_30691 RENAME TO reind_con_toast
;
ALTER TABLE regtest_table ALTER b DROP NOT NULL
;
-- ************************************************************
-- * AFTER UPDATE on System
-- *	- If system name changes let interfaces follow
-- ************************************************************
create function tg_system_au() returns trigger as '
begin
    if new.name != old.name then
        update IFace set sysname = new.name where sysname = old.name;
    end if;
    return new;
end;
' language plpgsql
;
CREATE TABLE test (a integer, b text)
;
create table loc1 (f1 serial, f2 text)
;
CREATE INDEX CONCURRENTLY concur_index8 ON concur_heap (f1)
  WHERE predicate_stable()
;
CREATE FUNCTION testprs_end(internal)
RETURNS void
AS '$libdir/test_parser'
LANGUAGE C STRICT
;
CREATE TABLE gtest23b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED REFERENCES gtest23a (x))
;
CREATE TEMPORARY TABLE yy (a INTEGER)
;
DROP FUNCTION test_bytea_to_text
;
------------------------------------------------------------------------------------------
[ALTER_PROG_OBJ] ALTER FUNCTION gbtreekey8_out(gbtreekey8) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_open (text, text, text, boolean)
RETURNS text
AS '$libdir/dblink','dblink_open'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[DROP_TRIGGER] drop trigger child_row_trig on child
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION errfunc() RETURNS int LANGUAGE SQL AS 'SELECT 1'
SET transaction_read_only = on
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION texticlike(citext, text)
RETURNS bool AS 'texticlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION const_func () RETURNS int AS $$ SELECT 1; $$ LANGUAGE SQL IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_init_privs
;
----------
[ADD_INDEX] -- Create unique indexes.  Due to a general limitation of inheritance,
-- uniqueness is only enforced per-relation.  Unique index inference
-- specification will do the right thing, though.
create unique index cities_names_unique on cities (name)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- should fail: column does not exist
alter table gc1 drop column name
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_same(gbtreekey32, gbtreekey32, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citextrecv(internal)
RETURNS citext
AS 'textrecv'
LANGUAGE internal STABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_proc
;
----------
[ADMIN_TABLE] ANALYZE pagg_tab3
;
----------
[CREATE_TABLE] create table �ͪ�ߩ�Ѧ��듾� (��듾� text, ��׾��ړ�� varchar, ����1A��󓱸 char(16))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."beta_neg" ATTACH PARTITION "public"."beta_neg_p3" FOR VALUES FROM (350) TO (500)
;
----------
[CREATE_PROG_OBJ] --
--
--
-- int8 ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_int8_consistent(internal,int8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=2)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) public."need""q(uot"
;
----------
[DROP_TABLE] drop table rules_base
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27926_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER joinview_upd_trig INSTEAD OF UPDATE ON joinview
  FOR EACH ROW EXECUTE FUNCTION joinview_upd_trig_fn()
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc8a(INOUT a int, INOUT b int)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %', a, b;
  a := a * 10;
  b := b + 10;
END;
$$
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) information_schema.sql_implementation_info
;
----------
[DROP_TABLE] drop table inh_temp_parent cascade
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest1_p1_like" (
    "f1" "date" NOT NULL,
    "f2" "text",
    "f3" bigint NOT NULL
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v8_temp AS SELECT * FROM base_table WHERE EXISTS (SELECT 1 FROM temp_table)
;
----------
[CREATE_PROG_OBJ] -- Check variable scoping -- a var is not available in its own or prior
-- default expressions.

create function scope_test() returns int as $$
declare x int := 42;
begin
  declare y int := x + 1;
          x int := x + 2;
  begin
    return x * 100 + y;
  end;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE split_prog.audit_log (account_id integer, action text, changed_at timestamptz DEFAULT clock_timestamp())
;
----------
[ALTER_TABLE] ALTER TABLE base_tbl SET (autovacuum_enabled = 'false')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float8_consistent(internal, double precision, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] --
-- Test that expression-index constraints can be pushed down through
-- UNION or UNION ALL
--

CREATE TEMP TABLE t1 (a text, b text)
;
----------
[CREATE_TABLE] CREATE TABLE collate_test22 (f2 text COLLATE "POSIX")
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf10 USING btree ADD OPERATOR 1 < (int4, int4) FOR ORDER BY float_ops
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspacewith LOCATION '/src/src/test/regress/testtablespace' WITH (random_page_cost = 3.0)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_policy
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc9(1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_varbit(varbit, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[DROP_VIEW] DROP VIEW view_unnamed_ss_locking
;
----------
[DROP_PROG_OBJ] -- fail

drop function anyctest(anycompatible, anycompatiblearray)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30691 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_table ALTER b DROP NOT NULL
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * AFTER UPDATE on System
-- *	- If system name changes let interfaces follow
-- ************************************************************
create function tg_system_au() returns trigger as '
begin
    if new.name != old.name then
        update IFace set sysname = new.name where sysname = old.name;
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE test (a integer, b text)
;
----------
[CREATE_TABLE] create table loc1 (f1 serial, f2 text)
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY concur_index8 ON concur_heap (f1)
  WHERE predicate_stable()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testprs_end(internal)
RETURNS void
AS '$libdir/test_parser'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] CREATE TABLE gtest23b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED REFERENCES gtest23a (x))
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE yy (a INTEGER)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_bytea_to_text
;
