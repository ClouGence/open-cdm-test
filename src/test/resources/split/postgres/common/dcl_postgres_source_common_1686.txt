ALTER TABLE public.gtest0 OWNER TO zyc
;
-- ERROR
GRANT USAGE ON FOREIGN SERVER s9 TO regress_test_role
;
PREPARE dumpAgg(pg_catalog.oid) AS
SELECT aggtransfn,
aggfinalfn,
aggtranstype::pg_catalog.regtype,
agginitval,
aggsortop,
pg_catalog.pg_get_function_arguments(p.oid) AS funcargs,
pg_catalog.pg_get_function_identity_arguments(p.oid) AS funciargs,
aggkind,
aggmtransfn,
aggminvtransfn,
aggmfinalfn,
aggmtranstype::pg_catalog.regtype,
aggfinalextra,
aggmfinalextra,
aggtransspace,
aggmtransspace,
aggminitval,
aggcombinefn,
aggserialfn,
aggdeserialfn,
proparallel,
aggfinalmodify,
aggmfinalmodify
FROM pg_catalog.pg_aggregate a, pg_catalog.pg_proc p WHERE a.aggfnoid = p.oid AND p.oid = $1
;
ALTER TABLE public.limit_thousand_v_1 OWNER TO zyc
;
COPY public.transition_table_level2 (level2_no, parent_no, level1_node_name) TO stdout
;
PREPARE TRANSACTION 'fdw_tpc'
;
-- custom plan should be chosen initially
PREPARE st4(int) AS SELECT * FROM ft1 t1 WHERE t1.c1 = $1
;
COPY public.tht_p2 (en, data) TO stdout
;
ALTER TABLE regtest_ptable_1_ones OWNER TO regress_sepgsql_test_user
;
ALTER TABLE public.dropped_objects OWNER TO zyc
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE public.gtest0 OWNER TO zyc
;
----------
[GRANT] -- ERROR
GRANT USAGE ON FOREIGN SERVER s9 TO regress_test_role
;
----------
[UNSAFE] PREPARE dumpAgg(pg_catalog.oid) AS
SELECT aggtransfn,
aggfinalfn,
aggtranstype::pg_catalog.regtype,
agginitval,
aggsortop,
pg_catalog.pg_get_function_arguments(p.oid) AS funcargs,
pg_catalog.pg_get_function_identity_arguments(p.oid) AS funciargs,
aggkind,
aggmtransfn,
aggminvtransfn,
aggmfinalfn,
aggmtranstype::pg_catalog.regtype,
aggfinalextra,
aggmfinalextra,
aggtransspace,
aggmtransspace,
aggminitval,
aggcombinefn,
aggserialfn,
aggdeserialfn,
proparallel,
aggfinalmodify,
aggmfinalmodify
FROM pg_catalog.pg_aggregate a, pg_catalog.pg_proc p WHERE a.aggfnoid = p.oid AND p.oid = $1
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.limit_thousand_v_1 OWNER TO zyc
;
----------
[DATA_EXPORT] COPY public.transition_table_level2 (level2_no, parent_no, level1_node_name) TO stdout
;
----------
[TRANSACTION] PREPARE TRANSACTION 'fdw_tpc'
;
----------
[UNSAFE] -- custom plan should be chosen initially
PREPARE st4(int) AS SELECT * FROM ft1 t1 WHERE t1.c1 = $1
;
----------
[DATA_EXPORT] COPY public.tht_p2 (en, data) TO stdout
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE regtest_ptable_1_ones OWNER TO regress_sepgsql_test_user
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.dropped_objects OWNER TO zyc
;
