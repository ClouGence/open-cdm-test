-- rollback a truncate: this should count 2 inserts and produce 2 dead tuples
BEGIN
;
GRANT USAGE ON FOREIGN SERVER s9 TO regress_test_role
;
EXECUTE dumpOpr('262')
;
-- Check that temp tables with existing cursors can't be dropped.
BEGIN
;
COPY public.circle_tbl (f1) TO stdout
;
DROP ROLE regress_createrole
;
SET LOCAL TIME ZONE -8
;
ALTER FUNCTION "public"."constant_refcursor"() OWNER TO zyc
;
COPY public.test_tablesample (id, name) TO stdout
;
LOCK TABLE "public"."brintest" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[TRANSACTION] -- rollback a truncate: this should count 2 inserts and produce 2 dead tuples
BEGIN
;
----------
[GRANT] GRANT USAGE ON FOREIGN SERVER s9 TO regress_test_role
;
----------
[UNSAFE] EXECUTE dumpOpr('262')
;
----------
[TRANSACTION] -- Check that temp tables with existing cursors can't be dropped.
BEGIN
;
----------
[DATA_EXPORT] COPY public.circle_tbl (f1) TO stdout
;
----------
[DROP_ROLE] DROP ROLE regress_createrole
;
----------
[SESSION_SETTING_WRITE] SET LOCAL TIME ZONE -8
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."constant_refcursor"() OWNER TO zyc
;
----------
[DATA_EXPORT] COPY public.test_tablesample (id, name) TO stdout
;
----------
[SESSION_LOCK] LOCK TABLE "public"."brintest" IN ACCESS SHARE MODE
;
