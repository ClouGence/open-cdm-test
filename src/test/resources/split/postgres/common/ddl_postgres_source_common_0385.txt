ALTER TABLE ONLY "public"."pht2" ATTACH PARTITION "public"."pht2_p1" FOR VALUES WITH (modulus 3, remainder 0)
;
ALTER TABLE leader ADD c int
;
CREATE TABLE user_cat (val1 integer) WITH (user_catalog_table = true)
;
DROP OPERATOR @!= (boolean, real)
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_schema.mv_withdata2 (a) AS
  SELECT generate_series(1, 10) WITH DATA
;
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (minvalue) TO (2)
;
CREATE OPERATOR @#@ (
   rightarg = int8,		
   procedure = factorial
)
;
ALTER TABLE ONLY test_replica_identity4
  ATTACH PARTITION test_replica_identity4_1 FOR VALUES IN (1)
;
ANALYZE "S 1"."T 2"
;
CREATE TABLE "public"."pitest2" (
    "f1" "date" NOT NULL,
    "f2" "text",
    "f3" integer NOT NULL
)
PARTITION BY RANGE ("f1")
;
ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (issn, ean13),
	OPERATOR 1  < (issn, issn13),
	OPERATOR 2  <= (issn, ean13),
	OPERATOR 2  <= (issn, issn13),
	OPERATOR 3  = (issn, ean13),
	OPERATOR 3  = (issn, issn13),
	OPERATOR 4  >= (issn, ean13),
	OPERATOR 4  >= (issn, issn13),
	OPERATOR 5  > (issn, ean13),
	OPERATOR 5  > (issn, issn13),
	FUNCTION 1  btissncmp(issn, ean13),
	FUNCTION 1  btissncmp(issn, issn13)
;
CREATE OR REPLACE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'INSERT INTO public.sro_trojan_table DEFAULT VALUES; SELECT true'
;
alter function non_strict(text) returns null on null input
;
ALTER DATABASE split_db_probe SET search_path FROM CURRENT
;
CREATE INDEX t1_gist ON s2.t1 USING GIST (b)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_cast
;
CREATE FUNCTION "public"."part_hashint4_noop"("value" integer, "seed" bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE
    AS $$
select value + seed;
$$
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_sequence
;
ALTER TABLE pg_toast.pg_toast_27926 RENAME TO reind_con_toast
;
/*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
CREATE TABLE "public"."beta" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
PARTITION BY RANGE ("a")
;
CREATE TABLE INTERVAL_MULDIV_TBL (span interval)
;
/* contrib/tcn/tcn--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION triggered_change_notification()
RETURNS pg_catalog.trigger
AS '$libdir/tcn'
LANGUAGE C
;
create table atacc1 ( test int, test2 int)
;
-- Creating a foreign key with ONLY on a partitioned table referencing
-- a non-partitioned table fails.
ALTER TABLE ONLY fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk
;
ALTER TYPE test_type RENAME ATTRIBUTE a TO aa
;
ALTER PUBLICATION pub1 ADD TABLE dump_test.test_second_table
;
-- test that attributes not handled by icu_set_collation_attributes()
-- (handled by ucol_open() directly) also work
CREATE COLLATION testcoll_de_phonebook (provider = icu, locale = 'de@collation=phonebook')
;
-- Should succeed
DROP TABLESPACE regress_tblspace_renamed
;
-- text search configuration
DROP TEXT SEARCH CONFIGURATION test_tsconfig_exists
;
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_table
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
CREATE TABLE loct2 (c1 int, c2 text)
;
drop table self_ref
;
create table ths_p2 partition of ths for values with (modulus 3, remainder 1)
;
-- operator 1 requested again in separate statement
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
create table listp_12_1 partition of listp_12 for values in(1)
;
CREATE TABLE fk_partitioned_fk_2 PARTITION OF fk_partitioned_fk FOR VALUES IN (1500,1502)
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26669_2 (tid pg_catalog.tid)
;
CREATE TABLE test_tab (a int primary key, b text, c timestamptz DEFAULT now(), d bigint DEFAULT 999, e int GENERATED BY DEFAULT AS IDENTITY)
;
-- dependencies

CREATE COLLATION test0 FROM "C"
;
-- should fail, return type mismatch
create event trigger regress_event_trigger
   on ddl_command_start
   execute procedure pg_backend_pid()
;
create function trap_foreign_key(int) returns int as $$
begin
	begin	-- start a subtransaction
		insert into slave values($1);
	exception
		when foreign_key_violation then
			raise notice 'caught foreign_key_violation';
			return 0;
	end;
	return 1;
end$$ language plpgsql
;
create temp table t1_2 (check (a >= 10 and a < 20)) inherits (t1)
;
DROP FUNCTION test3()
;
CREATE FUNCTION sub1_trigger_activity_func() RETURNS TRIGGER AS $$
BEGIN
  IF (TG_OP = 'INSERT') THEN
    INSERT INTO public.sub1_trigger_activity
      SELECT TG_RELNAME, TG_OP, TG_WHEN, TG_LEVEL, NULL, NEW.a;
  ELSIF (TG_OP = 'UPDATE') THEN
    INSERT INTO public.sub1_trigger_activity
      SELECT TG_RELNAME, TG_OP, TG_WHEN, TG_LEVEL, OLD.a, NEW.a;
  END IF;
  RETURN NULL;
END;
$$ LANGUAGE plpgsql
;
-- test of uuidv4() alias
TRUNCATE guid1
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_seclabel
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
CREATE SUBSCRIPTION tsub CONNECTION 'port=20984 host=/tmp/giy7KClrPp dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
create function end_label2() returns void as $$
begin
  for _i in 1 .. 10 loop
    exit;
  end loop flbl1;
end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pht2" ATTACH PARTITION "public"."pht2_p1" FOR VALUES WITH (modulus 3, remainder 0)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE leader ADD c int
;
----------
[CREATE_TABLE] CREATE TABLE user_cat (val1 integer) WITH (user_catalog_table = true)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR @!= (boolean, real)
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_schema.mv_withdata2 (a) AS
  SELECT generate_series(1, 10) WITH DATA
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (minvalue) TO (2)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @#@ (
   rightarg = int8,		
   procedure = factorial
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY test_replica_identity4
  ATTACH PARTITION test_replica_identity4_1 FOR VALUES IN (1)
;
----------
[ADMIN_TABLE] ANALYZE "S 1"."T 2"
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest2" (
    "f1" "date" NOT NULL,
    "f2" "text",
    "f3" integer NOT NULL
)
PARTITION BY RANGE ("f1")
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (issn, ean13),
	OPERATOR 1  < (issn, issn13),
	OPERATOR 2  <= (issn, ean13),
	OPERATOR 2  <= (issn, issn13),
	OPERATOR 3  = (issn, ean13),
	OPERATOR 3  = (issn, issn13),
	OPERATOR 4  >= (issn, ean13),
	OPERATOR 4  >= (issn, issn13),
	OPERATOR 5  > (issn, ean13),
	OPERATOR 5  > (issn, issn13),
	FUNCTION 1  btissncmp(issn, ean13),
	FUNCTION 1  btissncmp(issn, issn13)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'INSERT INTO public.sro_trojan_table DEFAULT VALUES; SELECT true'
;
----------
[ALTER_PROG_OBJ] alter function non_strict(text) returns null on null input
;
----------
[ALTER_CATALOG] ALTER DATABASE split_db_probe SET search_path FROM CURRENT
;
----------
[ADD_INDEX] CREATE INDEX t1_gist ON s2.t1 USING GIST (b)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_cast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."part_hashint4_noop"("value" integer, "seed" bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE
    AS $$
select value + seed;
$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_sequence
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27926 RENAME TO reind_con_toast
;
----------
[CREATE_POLICY] /*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."beta" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_TABLE] CREATE TABLE INTERVAL_MULDIV_TBL (span interval)
;
----------
[CREATE_PROG_OBJ] /* contrib/tcn/tcn--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION triggered_change_notification()
RETURNS pg_catalog.trigger
AS '$libdir/tcn'
LANGUAGE C
;
----------
[CREATE_TABLE] create table atacc1 ( test int, test2 int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Creating a foreign key with ONLY on a partitioned table referencing
-- a non-partitioned table fails.
ALTER TABLE ONLY fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk
;
----------
[ALTER_TYPE] ALTER TYPE test_type RENAME ATTRIBUTE a TO aa
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub1 ADD TABLE dump_test.test_second_table
;
----------
[CREATE_POLICY] -- test that attributes not handled by icu_set_collation_attributes()
-- (handled by ucol_open() directly) also work
CREATE COLLATION testcoll_de_phonebook (provider = icu, locale = 'de@collation=phonebook')
;
----------
[DROP_TABLESPACE] -- Should succeed
DROP TABLESPACE regress_tblspace_renamed
;
----------
[DROP_POLICY] -- text search configuration
DROP TEXT SEARCH CONFIGURATION test_tsconfig_exists
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_table
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
----------
[CREATE_TABLE] CREATE TABLE loct2 (c1 int, c2 text)
;
----------
[DROP_TABLE] drop table self_ref
;
----------
[CREATE_TABLE] create table ths_p2 partition of ths for values with (modulus 3, remainder 1)
;
----------
[ALTER_PROG_OBJ] -- operator 1 requested again in separate statement
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
----------
[CREATE_TABLE] create table listp_12_1 partition of listp_12 for values in(1)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_2 PARTITION OF fk_partitioned_fk FOR VALUES IN (1500,1502)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26669_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE test_tab (a int primary key, b text, c timestamptz DEFAULT now(), d bigint DEFAULT 999, e int GENERATED BY DEFAULT AS IDENTITY)
;
----------
[CREATE_POLICY] -- dependencies

CREATE COLLATION test0 FROM "C"
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should fail, return type mismatch
create event trigger regress_event_trigger
   on ddl_command_start
   execute procedure pg_backend_pid()
;
----------
[CREATE_PROG_OBJ] create function trap_foreign_key(int) returns int as $$
begin
	begin	-- start a subtransaction
		insert into slave values($1);
	exception
		when foreign_key_violation then
			raise notice 'caught foreign_key_violation';
			return 0;
	end;
	return 1;
end$$ language plpgsql
;
----------
[CREATE_TABLE] create temp table t1_2 (check (a >= 10 and a < 20)) inherits (t1)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test3()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION sub1_trigger_activity_func() RETURNS TRIGGER AS $$
BEGIN
  IF (TG_OP = 'INSERT') THEN
    INSERT INTO public.sub1_trigger_activity
      SELECT TG_RELNAME, TG_OP, TG_WHEN, TG_LEVEL, NULL, NEW.a;
  ELSIF (TG_OP = 'UPDATE') THEN
    INSERT INTO public.sub1_trigger_activity
      SELECT TG_RELNAME, TG_OP, TG_WHEN, TG_LEVEL, OLD.a, NEW.a;
  END IF;
  RETURN NULL;
END;
$$ LANGUAGE plpgsql
;
----------
[TRUNCATE_TABLE] -- test of uuidv4() alias
TRUNCATE guid1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_seclabel
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tsub CONNECTION 'port=20984 host=/tmp/giy7KClrPp dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
----------
[CREATE_PROG_OBJ] create function end_label2() returns void as $$
begin
  for _i in 1 .. 10 loop
    exit;
  end loop flbl1;
end;
$$ language plpgsql
;
