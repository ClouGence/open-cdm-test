CREATE POLICY p2 ON y1 FOR SELECT USING (a > 2)
;
ALTER INDEX pg_toast.pg_toast_30365_index RENAME TO reind_con_toast_idx
;
-- LOCALE conflicts with LC_COLLATE and LC_CTYPE
CREATE COLLATION coll_dup_chk (LC_COLLATE = "POSIX", LC_CTYPE = "POSIX", LOCALE = '')
;
create table atacc1 (a int primary key)
;
--
-- Tests for VACUUM ONLY / ANALYZE ONLY on inheritance tables
--
CREATE TABLE only_inh_parent (a int primary key, b TEXT)
;
DROP DOMAIN priv_testdomain2b
;
create table c3 (f4 int) inherits(c1,c2)
;
-- Create a partitioned table.
CREATE TABLE vegetables (id serial, name text, genus text)
PARTITION BY LIST (genus)
;
create table test ( "index" numeric ( 3 ) primary key , "payload" int4 not null )
;
drop table ��ג�������ђ��
;
ALTER OPERATOR @-@(int4, int4) SET SCHEMA alt_nsp2
;
CREATE FUNCTION consume_xids_until(targetxid xid8)
RETURNS xid8 VOLATILE PARALLEL UNSAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
DROP TABLE parted_no_parts
;
drop foreign table rem3
;
--
-- CREATE_INDEX
-- Create ancillary data structures (i.e. indices)
--

--
-- BTREE
--
CREATE INDEX onek_unique1 ON onek USING btree(unique1 int4_ops)
;
-- leave a table in this state for the pg_upgrade test

-- test inheritance
create table parent (a int)
;
-- Other weird invalid cases that cause problems
CREATE FOREIGN TABLE fp PARTITION OF tableam_parted_a_heap2 DEFAULT SERVER x
;
DROP TABLE pg_temp_13.pg_temp_25962_2, pg_temp_13.pg_temp_25962
;
-- error

-- check named-parameter matching
CREATE PROCEDURE ptest10(OUT a int, IN b int, IN c int)
LANGUAGE SQL AS $$ SELECT b - c $$
;
CREATE PUBLICATION test_tab1_pub FOR TABLE test_tab1
;
CREATE TYPE public.tst_enum_t AS ENUM ('a', 'b', 'c', 'd', 'e')
;
CREATE FUNCTION "public"."explain_query_json"("query_sql" "text") RETURNS TABLE("explain_line" "json")
    LANGUAGE "plpgsql"
    AS $$
begin
  set enable_seqscan = off;
  set enable_bitmapscan = on;
  return query execute 'EXPLAIN (ANALYZE, FORMAT json) ' || query_sql;
end;
$$
;
COMMENT ON CONSTRAINT ctlt2_c_not_null ON ctlt2 IS 't2_c_not_null'
;
CREATE DOMAIN priv_testdomain3a AS int
;
ANALYZE pg_temp_2.pg_temp_28230
;
CREATE DATABASE "regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ"
;
CREATE TEMP TABLE temptest (col text CHECK (col < '1:0.13402913890371382|2:0.767961418108855|3:0.23862428458353024|4:0.4706082835429144|5:0.07329965346895717|6:0.712540926522722|7:0.9649746208658345|8:0.5784512023018522|9:0.14292862257351735|10:0.22965317909984773|11:0.44932288322631564|12:0.8788343857150263|13:0.25337530775704686|14:0.5092681264428587|15:0.9619399329042106|16:0.25102773861561434|17:0.21421987881123106|18:0.6236216182646468|19:0.25559451159768143|20:0.4780281351800302|21:0.6503702889914373|22:0.03401512725123457|23:0.7239764844296528|24:0.07241979074358817|25:0.5859053048554193|26:0.7804822888535288|27:0.8793629763536275|28:0.1928986646639581|29:0.7307689495132514|30:0.3560655128888275|31:0.5590267801063882|32:0.538897384957773|33:0.457431900445183|34:0.03220824503109476|35:0.9804843987110299|36:0.447493369588295|37:0.16096786991756606|38:0.8810232011006902|39:0.38745288439675263|40:0.3257812029099727|41:0.5674195467747747|42:0.35648794113995996|43:0.6585449511270032|44:0.17384540125664172|45:0.039524616588502814|46:0.4534885944658893|47:0.9144804024716409|48:0.13543715998653472|49:0.3908719984218356|50:0.19797613723660246|51:0.5437703128334199|52:0.22744593392770707|53:0.8707092684782285|54:0.10235638011424308|55:0.9913122904648448|56:0.03641860344779424|57:0.863109375168815|58:0.6505341661141422|59:0.45607392478827324|60:0.4864129008320659|61:0.5402861456029306|62:0.6433299727035702|63:0.4801700817482202|64:0.09818190319669773|65:0.27009193821655453|66:0.41001021246957503|67:0.5482936119978445|68:0.6084409372396415|69:0.7694416970539626|70:0.8569930080279597|71:0.7190324464563798|72:0.8027835022571235|73:0.9935866338302746|74:0.007753468237294836|75:0.24365730647342843|76:0.10782387027405327|77:0.5851901365272547|78:0.9013216885592725|79:0.5696286029775075|80:0.4701273669343533|81:0.3164687528353929|82:0.7996631778888448|83:0.9139486564180712|84:0.6648270109129772|85:0.7135906223560629|86:0.23411937064087995|87:0.6992229494152142|88:0.2727026140737814|89:0.24600986368506805|90:0.618042716132627|91:0.952755812232599|92:0.5200625767503873|93:0.7724980564820145|94:0.8061558737554146|95:0.9332980790306578|96:0.1715919226657059|97:0.6400948153290029|98:0.19331447084555076|99:0.5434199668577782|100:0.9854824540868321')) ON COMMIT DROP
;
CREATE FUNCTION "public"."pt_in_widget"("point", "public"."widget") RETURNS boolean
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'pt_in_widget'
;
create or replace function testrngfunc() returns setof rngfunc_type as $$
  select 1, 2 union select 3, 4 order by 1;
$$ language sql immutable
;
ANALYZE public.num_result
;
ANALYZE public.truncate_b
;
create table listp_2_1 partition of listp_2 for values in(2)
;
create trigger my_trig
  after insert on parted_trig
  for each row execute procedure funcB()
;
ALTER TABLE pt_stats ATTACH PARTITION pt_stats1 FOR VALUES FROM (0) TO (100)
;
CREATE FUNCTION strict_word_similarity(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- implicit cast during assignment is a separate code path, test that too

create or replace function dom_check(int) returns di as $$
declare d di;
begin
  d := $1;
  return d;
end
$$ language plpgsql immutable
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, two_phase = foo)
;
do $$
declare r1 record[];
begin
end$$
;
CREATE TABLE "public"."main_table" (
    "a" integer,
    "b" integer
)
;
CREATE FUNCTION "public"."ks_test_uniform_random_int_in_range"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 999999) / 1000000.0 r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
;
--
-- bt_index_parent_check()
--
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE MATERIALIZED VIEW dump_test.regress_pg_dump_matview_am_1
				USING regress_table_am AS SELECT count(*) FROM pg_class
;
CREATE TABLE parted (a INT) PARTITION BY LIST (a)
;
create extension test_ext7
;
ALTER TABLE pg_toast.pg_toast_30681 RENAME TO reind_con_toast
;
create table inh_cc2(f4 float) inherits(inh_pp1,inh_cc1)
;
CREATE AGGREGATE newavg (
   sfunc = int4_avg_accum, basetype = int4, stype = _int8,
   finalfunc = int8_avg,
   initcond1 = '{0,0}'
)
;
/*
 * 5.30
 * ELEMENT_TYPES view
 */

CREATE VIEW element_types AS
    SELECT CAST(current_database() AS sql_identifier) AS object_catalog,
           CAST(n.nspname AS sql_identifier) AS object_schema,
           CAST(x.objname AS sql_identifier) AS object_name,
           CAST(x.objtype AS character_data) AS object_type,
           CAST(x.objdtdid AS sql_identifier) AS collection_type_identifier,
           CAST(
             CASE WHEN nbt.nspname = 'pg_catalog' THEN format_type(bt.oid, null)
                  ELSE 'USER-DEFINED' END AS character_data) AS data_type,

           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(CASE WHEN nco.nspname IS NOT NULL THEN current_database() END AS sql_identifier) AS collation_catalog,
           CAST(nco.nspname AS sql_identifier) AS collation_schema,
           CAST(co.collname AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,

           CAST(null AS character_data) AS domain_default, -- XXX maybe a bug in the standard

           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nbt.nspname AS sql_identifier) AS udt_schema,
           CAST(bt.typname AS sql_identifier) AS udt_name,

           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,

           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST('a' || CAST(x.objdtdid AS text) AS sql_identifier) AS dtd_identifier

    FROM pg_namespace n, pg_type at, pg_namespace nbt, pg_type bt,
         (
           /* columns, attributes */
           SELECT c.relnamespace, CAST(c.relname AS sql_identifier),
                  CASE WHEN c.relkind = 'c' THEN 'USER-DEFINED TYPE'::text ELSE 'TABLE'::text END,
                  a.attnum, a.atttypid, a.attcollation
           FROM pg_class c, pg_attribute a
           WHERE c.oid = a.attrelid
                 AND c.relkind IN ('r', 'v', 'f', 'c', 'p')
                 AND attnum > 0 AND NOT attisdropped

           UNION ALL

           /* domains */
           SELECT t.typnamespace, CAST(t.typname AS sql_identifier),
                  'DOMAIN'::text, 1, t.typbasetype, t.typcollation
           FROM pg_type t
           WHERE t.typtype = 'd'

           UNION ALL

           /* parameters */
           SELECT pronamespace,
                  CAST(nameconcatoid(proname, oid) AS sql_identifier),
                  'ROUTINE'::text, (ss.x).n, (ss.x).x, 0
           FROM (SELECT p.pronamespace, p.proname, p.oid,
                        _pg_expandarray(coalesce(p.proallargtypes, p.proargtypes::oid[])) AS x
                 FROM pg_proc p) AS ss

           UNION ALL

           /* result types */
           SELECT p.pronamespace,
                  CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier),
                  'ROUTINE'::text, 0, p.prorettype, 0
           FROM pg_proc p

         ) AS x (objschema, objname, objtype, objdtdid, objtypeid, objcollation)
         LEFT JOIN (pg_collation co JOIN pg_namespace nco ON (co.collnamespace = nco.oid))
           ON x.objcollation = co.oid AND (nco.nspname, co.collname) <> ('pg_catalog', 'default')

    WHERE n.oid = x.objschema
          AND at.oid = x.objtypeid
          AND (at.typelem <> 0 AND at.typlen = -1)
          AND at.typelem = bt.oid
          AND nbt.oid = bt.typnamespace

          AND (n.nspname, x.objname, x.objtype, CAST(x.objdtdid AS sql_identifier)) IN
              ( SELECT object_schema, object_name, object_type, dtd_identifier
                    FROM data_type_privileges )
;
-- create a shell operator for ===!!! by referencing it as a commutator
CREATE OPERATOR === (
    leftarg = integer,
    rightarg = integer,
    procedure = int4eq,
    commutator = ===!!!
)
;
CREATE OR REPLACE FUNCTION
  parse_ident(str text, strict boolean DEFAULT true)
RETURNS text[]
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'parse_ident'
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] CREATE POLICY p2 ON y1 FOR SELECT USING (a > 2)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30365_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_POLICY] -- LOCALE conflicts with LC_COLLATE and LC_CTYPE
CREATE COLLATION coll_dup_chk (LC_COLLATE = "POSIX", LC_CTYPE = "POSIX", LOCALE = '')
;
----------
[CREATE_TABLE] create table atacc1 (a int primary key)
;
----------
[CREATE_TABLE] --
-- Tests for VACUUM ONLY / ANALYZE ONLY on inheritance tables
--
CREATE TABLE only_inh_parent (a int primary key, b TEXT)
;
----------
[DROP_TYPE] DROP DOMAIN priv_testdomain2b
;
----------
[CREATE_TABLE] create table c3 (f4 int) inherits(c1,c2)
;
----------
[CREATE_TABLE] -- Create a partitioned table.
CREATE TABLE vegetables (id serial, name text, genus text)
PARTITION BY LIST (genus)
;
----------
[CREATE_TABLE] create table test ( "index" numeric ( 3 ) primary key , "payload" int4 not null )
;
----------
[DROP_TABLE] drop table ��ג�������ђ��
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @-@(int4, int4) SET SCHEMA alt_nsp2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION consume_xids_until(targetxid xid8)
RETURNS xid8 VOLATILE PARALLEL UNSAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[DROP_TABLE] DROP TABLE parted_no_parts
;
----------
[DROP_TABLE] drop foreign table rem3
;
----------
[ADD_INDEX] --
-- CREATE_INDEX
-- Create ancillary data structures (i.e. indices)
--

--
-- BTREE
--
CREATE INDEX onek_unique1 ON onek USING btree(unique1 int4_ops)
;
----------
[CREATE_TABLE] -- leave a table in this state for the pg_upgrade test

-- test inheritance
create table parent (a int)
;
----------
[CREATE_TABLE] -- Other weird invalid cases that cause problems
CREATE FOREIGN TABLE fp PARTITION OF tableam_parted_a_heap2 DEFAULT SERVER x
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_25962_2, pg_temp_13.pg_temp_25962
;
----------
[CREATE_PROG_OBJ] -- error

-- check named-parameter matching
CREATE PROCEDURE ptest10(OUT a int, IN b int, IN c int)
LANGUAGE SQL AS $$ SELECT b - c $$
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION test_tab1_pub FOR TABLE test_tab1
;
----------
[CREATE_TYPE] CREATE TYPE public.tst_enum_t AS ENUM ('a', 'b', 'c', 'd', 'e')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_query_json"("query_sql" "text") RETURNS TABLE("explain_line" "json")
    LANGUAGE "plpgsql"
    AS $$
begin
  set enable_seqscan = off;
  set enable_bitmapscan = on;
  return query execute 'EXPLAIN (ANALYZE, FORMAT json) ' || query_sql;
end;
$$
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT ctlt2_c_not_null ON ctlt2 IS 't2_c_not_null'
;
----------
[CREATE_TYPE] CREATE DOMAIN priv_testdomain3a AS int
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_28230
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ"
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest (col text CHECK (col < '1:0.13402913890371382|2:0.767961418108855|3:0.23862428458353024|4:0.4706082835429144|5:0.07329965346895717|6:0.712540926522722|7:0.9649746208658345|8:0.5784512023018522|9:0.14292862257351735|10:0.22965317909984773|11:0.44932288322631564|12:0.8788343857150263|13:0.25337530775704686|14:0.5092681264428587|15:0.9619399329042106|16:0.25102773861561434|17:0.21421987881123106|18:0.6236216182646468|19:0.25559451159768143|20:0.4780281351800302|21:0.6503702889914373|22:0.03401512725123457|23:0.7239764844296528|24:0.07241979074358817|25:0.5859053048554193|26:0.7804822888535288|27:0.8793629763536275|28:0.1928986646639581|29:0.7307689495132514|30:0.3560655128888275|31:0.5590267801063882|32:0.538897384957773|33:0.457431900445183|34:0.03220824503109476|35:0.9804843987110299|36:0.447493369588295|37:0.16096786991756606|38:0.8810232011006902|39:0.38745288439675263|40:0.3257812029099727|41:0.5674195467747747|42:0.35648794113995996|43:0.6585449511270032|44:0.17384540125664172|45:0.039524616588502814|46:0.4534885944658893|47:0.9144804024716409|48:0.13543715998653472|49:0.3908719984218356|50:0.19797613723660246|51:0.5437703128334199|52:0.22744593392770707|53:0.8707092684782285|54:0.10235638011424308|55:0.9913122904648448|56:0.03641860344779424|57:0.863109375168815|58:0.6505341661141422|59:0.45607392478827324|60:0.4864129008320659|61:0.5402861456029306|62:0.6433299727035702|63:0.4801700817482202|64:0.09818190319669773|65:0.27009193821655453|66:0.41001021246957503|67:0.5482936119978445|68:0.6084409372396415|69:0.7694416970539626|70:0.8569930080279597|71:0.7190324464563798|72:0.8027835022571235|73:0.9935866338302746|74:0.007753468237294836|75:0.24365730647342843|76:0.10782387027405327|77:0.5851901365272547|78:0.9013216885592725|79:0.5696286029775075|80:0.4701273669343533|81:0.3164687528353929|82:0.7996631778888448|83:0.9139486564180712|84:0.6648270109129772|85:0.7135906223560629|86:0.23411937064087995|87:0.6992229494152142|88:0.2727026140737814|89:0.24600986368506805|90:0.618042716132627|91:0.952755812232599|92:0.5200625767503873|93:0.7724980564820145|94:0.8061558737554146|95:0.9332980790306578|96:0.1715919226657059|97:0.6400948153290029|98:0.19331447084555076|99:0.5434199668577782|100:0.9854824540868321')) ON COMMIT DROP
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."pt_in_widget"("point", "public"."widget") RETURNS boolean
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'pt_in_widget'
;
----------
[CREATE_PROG_OBJ] create or replace function testrngfunc() returns setof rngfunc_type as $$
  select 1, 2 union select 3, 4 order by 1;
$$ language sql immutable
;
----------
[ADMIN_TABLE] ANALYZE public.num_result
;
----------
[ADMIN_TABLE] ANALYZE public.truncate_b
;
----------
[CREATE_TABLE] create table listp_2_1 partition of listp_2 for values in(2)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_trig
  after insert on parted_trig
  for each row execute procedure funcB()
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pt_stats ATTACH PARTITION pt_stats1 FOR VALUES FROM (0) TO (100)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION strict_word_similarity(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- implicit cast during assignment is a separate code path, test that too

create or replace function dom_check(int) returns di as $$
declare d di;
begin
  d := $1;
  return d;
end
$$ language plpgsql immutable
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, two_phase = foo)
;
----------
[BLOCK] do $$
declare r1 record[];
begin
end$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."main_table" (
    "a" integer,
    "b" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ks_test_uniform_random_int_in_range"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 999999) / 1000000.0 r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
;
----------
[CREATE_PROG_OBJ] --
-- bt_index_parent_check()
--
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW dump_test.regress_pg_dump_matview_am_1
				USING regress_table_am AS SELECT count(*) FROM pg_class
;
----------
[CREATE_TABLE] CREATE TABLE parted (a INT) PARTITION BY LIST (a)
;
----------
[CREATE_LIBRARY] create extension test_ext7
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30681 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] create table inh_cc2(f4 float) inherits(inh_pp1,inh_cc1)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE newavg (
   sfunc = int4_avg_accum, basetype = int4, stype = _int8,
   finalfunc = int8_avg,
   initcond1 = '{0,0}'
)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.30
 * ELEMENT_TYPES view
 */

CREATE VIEW element_types AS
    SELECT CAST(current_database() AS sql_identifier) AS object_catalog,
           CAST(n.nspname AS sql_identifier) AS object_schema,
           CAST(x.objname AS sql_identifier) AS object_name,
           CAST(x.objtype AS character_data) AS object_type,
           CAST(x.objdtdid AS sql_identifier) AS collection_type_identifier,
           CAST(
             CASE WHEN nbt.nspname = 'pg_catalog' THEN format_type(bt.oid, null)
                  ELSE 'USER-DEFINED' END AS character_data) AS data_type,

           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(CASE WHEN nco.nspname IS NOT NULL THEN current_database() END AS sql_identifier) AS collation_catalog,
           CAST(nco.nspname AS sql_identifier) AS collation_schema,
           CAST(co.collname AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,

           CAST(null AS character_data) AS domain_default, -- XXX maybe a bug in the standard

           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nbt.nspname AS sql_identifier) AS udt_schema,
           CAST(bt.typname AS sql_identifier) AS udt_name,

           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,

           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST('a' || CAST(x.objdtdid AS text) AS sql_identifier) AS dtd_identifier

    FROM pg_namespace n, pg_type at, pg_namespace nbt, pg_type bt,
         (
           /* columns, attributes */
           SELECT c.relnamespace, CAST(c.relname AS sql_identifier),
                  CASE WHEN c.relkind = 'c' THEN 'USER-DEFINED TYPE'::text ELSE 'TABLE'::text END,
                  a.attnum, a.atttypid, a.attcollation
           FROM pg_class c, pg_attribute a
           WHERE c.oid = a.attrelid
                 AND c.relkind IN ('r', 'v', 'f', 'c', 'p')
                 AND attnum > 0 AND NOT attisdropped

           UNION ALL

           /* domains */
           SELECT t.typnamespace, CAST(t.typname AS sql_identifier),
                  'DOMAIN'::text, 1, t.typbasetype, t.typcollation
           FROM pg_type t
           WHERE t.typtype = 'd'

           UNION ALL

           /* parameters */
           SELECT pronamespace,
                  CAST(nameconcatoid(proname, oid) AS sql_identifier),
                  'ROUTINE'::text, (ss.x).n, (ss.x).x, 0
           FROM (SELECT p.pronamespace, p.proname, p.oid,
                        _pg_expandarray(coalesce(p.proallargtypes, p.proargtypes::oid[])) AS x
                 FROM pg_proc p) AS ss

           UNION ALL

           /* result types */
           SELECT p.pronamespace,
                  CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier),
                  'ROUTINE'::text, 0, p.prorettype, 0
           FROM pg_proc p

         ) AS x (objschema, objname, objtype, objdtdid, objtypeid, objcollation)
         LEFT JOIN (pg_collation co JOIN pg_namespace nco ON (co.collnamespace = nco.oid))
           ON x.objcollation = co.oid AND (nco.nspname, co.collname) <> ('pg_catalog', 'default')

    WHERE n.oid = x.objschema
          AND at.oid = x.objtypeid
          AND (at.typelem <> 0 AND at.typlen = -1)
          AND at.typelem = bt.oid
          AND nbt.oid = bt.typnamespace

          AND (n.nspname, x.objname, x.objtype, CAST(x.objdtdid AS sql_identifier)) IN
              ( SELECT object_schema, object_name, object_type, dtd_identifier
                    FROM data_type_privileges )
;
----------
[CREATE_PROG_OBJ] -- create a shell operator for ===!!! by referencing it as a commutator
CREATE OPERATOR === (
    leftarg = integer,
    rightarg = integer,
    procedure = int4eq,
    commutator = ===!!!
)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  parse_ident(str text, strict boolean DEFAULT true)
RETURNS text[]
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'parse_ident'
;
