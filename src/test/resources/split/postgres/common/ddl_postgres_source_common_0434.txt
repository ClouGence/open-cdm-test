CREATE DATABASE "postgres" WITH TEMPLATE = template0 OID = 5 STRATEGY = FILE_COPY ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
CREATE CAST (issn AS issn13) WITHOUT FUNCTION AS ASSIGNMENT
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_user_mapping
;
ALTER TABLE inhta ADD COLUMN i int
;
create table idxpart2 partition of idxpart for values from (0) to (1000) partition by range (b)
;
ALTER TABLE alice.hashpart_b REPLICA IDENTITY FULL
;
CREATE TRIGGER d1_update_trig
  AFTER UPDATE ON part_d_1_15 for each statement execute procedure trigfunc()
;
drop table ext7_table1
;
ALTER FUNCTION gbt_int8_union(internal, internal) PARALLEL SAFE
;
create or replace function rls_f() returns setof rls_t
  stable language sql
  as $$ with cte as (select * from rls_t) select * from cte $$
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should work
  select * from foo where f1 = 3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
CREATE TABLE s1.t2 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
ALTER TABLE pg_ts_template ADD PRIMARY KEY USING INDEX pg_ts_template_oid_index
;
--
-- BTREE ascending/descending cases
--
-- we load int4/text from pure descending data (each key is a new
-- low key) and name/f8 from pure ascending data (each key is a new
-- high key).  we had a bug where new low keys would sometimes be
-- "lost".
--
CREATE INDEX bt_i4_index ON bt_i4_heap USING btree (seqno int4_ops)
;
ANALYZE public.rewritemetoo2
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER TABLE fk2 DROP COLUMN b
;
create temp table t2 (a integer primary key, b integer references t1)
;
ALTER TABLE pg_temp_10.pg_temp_26444_2 ADD COLUMN newdata pg_temp_10.pg_temp_26444
;
CREATE TABLE itest7 (a int GENERATED ALWAYS AS IDENTITY)
;
DROP SCHEMA pub_test3
;
DROP TABLE test_pg_unicode_fast
;
CREATE TABLE truncme(i int) WITH (fillfactor = 50)
;
TRUNCATE twophase
;
-- ERROR
ALTER FOREIGN TABLE ft1 DROP CONSTRAINT ft1_c9_check
;
CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION "Public"
;
CREATE TEMP TABLE public.temp_to_perm (a int primary key)
;
/* contrib/bloom/bloom--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION blhandler(internal)
RETURNS index_am_handler
AS '$libdir/bloom'
LANGUAGE C
;
CREATE INDEX gpointind ON point_tbl USING gist (f1)
;
DROP TABLE IF EXISTS temporal_unique
;
CREATE TABLE test1 (a int, b int)
;
drop table dtest
;
create unlogged table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) partition by range (aid)
;
ALTER TABLE pg_toast.pg_toast_28229 RENAME TO reind_con_toast
;
COMMENT ON TEXT SEARCH DICTIONARY basque_stem IS 'snowball stemmer for basque language'
;
ANALYZE (nonexistentarg) does_not_exit
;
create function trap_timeout() returns void as $$
begin
  declare x int;
  begin
    -- we assume this will take longer than 1 second:
    select count(*) into x from generate_series(1, 1000000000000);
  exception
    when others then
      raise notice 'caught others?';
    when query_canceled then
      raise notice 'nyeah nyeah, can''t stop me';
  end;
  -- Abort transaction to abandon the statement_timeout setting.  Otherwise,
  -- the next top-level statement would be vulnerable to the timeout.
  raise exception 'end of function';
end$$ language plpgsql
;
-- subqueries
CREATE VIEW v4 AS SELECT * FROM base_table WHERE id IN (SELECT id FROM base_table2)
;
-- skip self-referencing foreign key
ALTER TABLE logged2 SET UNLOGGED
;
CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=25082 host=/tmp/sxNfZEUQ8y dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
create table s2.p1_2 partition of s2.p1 for values in (4, 5, 6)
;
DROP TRIGGER before_upd_a_row_trig ON main_table
;
CALL ptest9(1./0.)
;
create trigger depth_c_tr before insert on depth_c
  for each row execute procedure depth_c_tf()
;
create view tt22v as
select * from tt5 natural left join tt6
;
-- deserialfunc must have correct parameters
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_serialize
)
;
create table test ( name char ( 8 ) , amount int , letter char ( 1 ) )
;
DO $$
DECLARE
  xml_declaration text := '<?xml version="1.0" encoding="ISO-8859-1"?>';
  degree_symbol text;
  res xml[];
BEGIN
  -- Per the documentation, except when the server encoding is UTF8, xpath()
  -- may not work on non-ASCII data.  The untranslatable_character and
  -- undefined_function traps below, currently dead code, will become relevant
  -- if we remove this limitation.
  IF current_setting('server_encoding') <> 'UTF8' THEN
    RAISE LOG 'skip: encoding % unsupported for xpath',
      current_setting('server_encoding');
    RETURN;
  END IF;

  degree_symbol := convert_from('\xc2b0', 'UTF8');
  res := xpath('text()', (xml_declaration ||
    '<x>' || degree_symbol || '</x>')::xml);
  IF degree_symbol <> res[1]::text THEN
    RAISE 'expected % (%), got % (%)',
      degree_symbol, convert_to(degree_symbol, 'UTF8'),
      res[1], convert_to(res[1]::text, 'UTF8');
  END IF;
EXCEPTION
  -- character with byte sequence 0xc2 0xb0 in encoding "UTF8" has no equivalent in encoding "LATIN8"
  WHEN untranslatable_character
  -- default conversion function for encoding "UTF8" to "MULE_INTERNAL" does not exist
  OR undefined_function
  -- unsupported XML feature
  OR feature_not_supported THEN
    RAISE LOG 'skip: %', SQLERRM;
END
$$
;
CREATE INDEX t1_btree ON s2.t1 USING BTREE (i)
;
CREATE OPERATOR CLASS gist_bytea_ops
DEFAULT FOR TYPE bytea USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bytea_consistent (internal, bytea, int2, oid, internal),
	FUNCTION	2	gbt_bytea_union (internal, internal),
	FUNCTION	3	gbt_bytea_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_bytea_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bytea_picksplit (internal, internal),
	FUNCTION	7	gbt_bytea_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
------------------------------------------------------------------------------------------
[CREATE_CATALOG] CREATE DATABASE "postgres" WITH TEMPLATE = template0 OID = 5 STRATEGY = FILE_COPY ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[CREATE_PROG_OBJ] CREATE CAST (issn AS issn13) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_user_mapping
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE inhta ADD COLUMN i int
;
----------
[CREATE_TABLE] create table idxpart2 partition of idxpart for values from (0) to (1000) partition by range (b)
;
----------
[ALTER_TABLE] ALTER TABLE alice.hashpart_b REPLICA IDENTITY FULL
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER d1_update_trig
  AFTER UPDATE ON part_d_1_15 for each statement execute procedure trigfunc()
;
----------
[DROP_TABLE] drop table ext7_table1
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int8_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function rls_f() returns setof rls_t
  stable language sql
  as $$ with cte as (select * from rls_t) select * from cte $$
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should work
  select * from foo where f1 = 3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE s1.t2 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_template ADD PRIMARY KEY USING INDEX pg_ts_template_oid_index
;
----------
[ADD_INDEX] --
-- BTREE ascending/descending cases
--
-- we load int4/text from pure descending data (each key is a new
-- low key) and name/f8 from pure ascending data (each key is a new
-- high key).  we had a bug where new low keys would sometimes be
-- "lost".
--
CREATE INDEX bt_i4_index ON bt_i4_heap USING btree (seqno int4_ops)
;
----------
[ADMIN_TABLE] ANALYZE public.rewritemetoo2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk2 DROP COLUMN b
;
----------
[CREATE_TABLE] create temp table t2 (a integer primary key, b integer references t1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_26444_2 ADD COLUMN newdata pg_temp_10.pg_temp_26444
;
----------
[CREATE_TABLE] CREATE TABLE itest7 (a int GENERATED ALWAYS AS IDENTITY)
;
----------
[DROP_SCHEMA] DROP SCHEMA pub_test3
;
----------
[DROP_TABLE] DROP TABLE test_pg_unicode_fast
;
----------
[CREATE_TABLE] CREATE TABLE truncme(i int) WITH (fillfactor = 50)
;
----------
[TRUNCATE_TABLE] TRUNCATE twophase
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- ERROR
ALTER FOREIGN TABLE ft1 DROP CONSTRAINT ft1_c9_check
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION "Public"
;
----------
[CREATE_TABLE] CREATE TEMP TABLE public.temp_to_perm (a int primary key)
;
----------
[CREATE_PROG_OBJ] /* contrib/bloom/bloom--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION blhandler(internal)
RETURNS index_am_handler
AS '$libdir/bloom'
LANGUAGE C
;
----------
[ADD_INDEX] CREATE INDEX gpointind ON point_tbl USING gist (f1)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS temporal_unique
;
----------
[CREATE_TABLE] CREATE TABLE test1 (a int, b int)
;
----------
[DROP_TABLE] drop table dtest
;
----------
[CREATE_TABLE] create unlogged table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) partition by range (aid)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28229 RENAME TO reind_con_toast
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY basque_stem IS 'snowball stemmer for basque language'
;
----------
[ADMIN_TABLE] ANALYZE (nonexistentarg) does_not_exit
;
----------
[CREATE_PROG_OBJ] create function trap_timeout() returns void as $$
begin
  declare x int;
  begin
    -- we assume this will take longer than 1 second:
    select count(*) into x from generate_series(1, 1000000000000);
  exception
    when others then
      raise notice 'caught others?';
    when query_canceled then
      raise notice 'nyeah nyeah, can''t stop me';
  end;
  -- Abort transaction to abandon the statement_timeout setting.  Otherwise,
  -- the next top-level statement would be vulnerable to the timeout.
  raise exception 'end of function';
end$$ language plpgsql
;
----------
[CREATE_VIEW(SELECT)] -- subqueries
CREATE VIEW v4 AS SELECT * FROM base_table WHERE id IN (SELECT id FROM base_table2)
;
----------
[ALTER_TABLE] -- skip self-referencing foreign key
ALTER TABLE logged2 SET UNLOGGED
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=25082 host=/tmp/sxNfZEUQ8y dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
----------
[CREATE_TABLE] create table s2.p1_2 partition of s2.p1 for values in (4, 5, 6)
;
----------
[DROP_TRIGGER] DROP TRIGGER before_upd_a_row_trig ON main_table
;
----------
[CALL_PROG_OBJ] CALL ptest9(1./0.)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger depth_c_tr before insert on depth_c
  for each row execute procedure depth_c_tf()
;
----------
[CREATE_VIEW(SELECT)] create view tt22v as
select * from tt5 natural left join tt6
;
----------
[CREATE_PROG_OBJ] -- deserialfunc must have correct parameters
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_serialize
)
;
----------
[CREATE_TABLE] create table test ( name char ( 8 ) , amount int , letter char ( 1 ) )
;
----------
[BLOCK] DO $$
DECLARE
  xml_declaration text := '<?xml version="1.0" encoding="ISO-8859-1"?>';
  degree_symbol text;
  res xml[];
BEGIN
  -- Per the documentation, except when the server encoding is UTF8, xpath()
  -- may not work on non-ASCII data.  The untranslatable_character and
  -- undefined_function traps below, currently dead code, will become relevant
  -- if we remove this limitation.
  IF current_setting('server_encoding') <> 'UTF8' THEN
    RAISE LOG 'skip: encoding % unsupported for xpath',
      current_setting('server_encoding');
    RETURN;
  END IF;

  degree_symbol := convert_from('\xc2b0', 'UTF8');
  res := xpath('text()', (xml_declaration ||
    '<x>' || degree_symbol || '</x>')::xml);
  IF degree_symbol <> res[1]::text THEN
    RAISE 'expected % (%), got % (%)',
      degree_symbol, convert_to(degree_symbol, 'UTF8'),
      res[1], convert_to(res[1]::text, 'UTF8');
  END IF;
EXCEPTION
  -- character with byte sequence 0xc2 0xb0 in encoding "UTF8" has no equivalent in encoding "LATIN8"
  WHEN untranslatable_character
  -- default conversion function for encoding "UTF8" to "MULE_INTERNAL" does not exist
  OR undefined_function
  -- unsupported XML feature
  OR feature_not_supported THEN
    RAISE LOG 'skip: %', SQLERRM;
END
$$
;
----------
[ADD_INDEX] CREATE INDEX t1_btree ON s2.t1 USING BTREE (i)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_bytea_ops
DEFAULT FOR TYPE bytea USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bytea_consistent (internal, bytea, int2, oid, internal),
	FUNCTION	2	gbt_bytea_union (internal, internal),
	FUNCTION	3	gbt_bytea_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_bytea_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bytea_picksplit (internal, internal),
	FUNCTION	7	gbt_bytea_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
