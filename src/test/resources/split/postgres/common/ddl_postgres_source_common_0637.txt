CREATE TEMP TABLE pg_temp_9.pg_temp_26520_2 (tid pg_catalog.tid)
;
CREATE TABLE trans_foo (a int)
;
CREATE SCHEMA pubme CREATE TABLE t0 (c int, d int) CREATE TABLE t1 (c int)
;
DROP EXTENSION pg_logicalinspect
;
CREATE TABLE regress_notnull1 (a integer)
;
ALTER INDEX "public"."trigger_parted_p1_pkey" ATTACH PARTITION "public"."trigger_parted_p1_1_pkey"
;
-- ok

-- Test non-inheritable parent constraints
create table p1(ff1 int)
;
/* src/test/modules/test_radixtree/test_radixtree--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_radixtree()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_radixtree' LANGUAGE C
;
-- ok - drop table
ALTER PUBLICATION testpub_tbl9 DROP TABLE public.testpub_tbl7
;
-- to exercise dropped-col cases
CREATE VIEW test_pg_dump_v1 AS SELECT * FROM test_pg_dump_t1
;
ALTER TABLE pg_toast.pg_toast_33148 RENAME TO reind_con_toast
;
drop index idxpart_gin
;
-- test that we can't use part of an existing commutator or negator pair
-- as a commutator or negator
CREATE OPERATOR === (
    leftarg = integer,
    rightarg = integer,
    procedure = int4eq,
    commutator = =
)
;
-- should fail

ALTER TABLE fk_r ATTACH PARTITION fk_r_1 FOR VALUES IN (1)
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (somename.somename)
;
ALTER TABLE parent ALTER COLUMN a SET STATISTICS 100
;
CREATE VIEW "public"."limit_thousand_v_5" AS
 SELECT "onek"."thousand"
   FROM "public"."onek"
  WHERE ("onek"."thousand" < 995)
  ORDER BY "onek"."thousand"
 LIMIT ALL
;
CREATE TYPE "public"."sqljsonb_rec" AS (
	"a" integer,
	"t" "text",
	"js" json,
	"jb" "jsonb",
	"jsa" json[]
)
;
-- test an unlogged table, mostly to get coverage of gistbuildempty
create unlogged table gist_tbl (b box)
;
-- Test that child does not inherit NO INHERIT constraints
create table c1 () inherits (p1)
;
alter type e add value '1' after '0'
;
CREATE TEXT SEARCH DICTIONARY danish_stem
	(TEMPLATE = snowball, Language = danish , StopWords=danish)
;
do $$
begin
  assert 1=0, 'unhandled assertion';
exception when others then
  null; -- do nothing
end;
$$
;
CREATE TABLE spill_test(data text)
;
-- disinheriting it first works
alter table child no inherit parent
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16814 host=/tmp/I9Nd_BkCYQ dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
-- division by zero runtime error, the context given in the error message
-- should be sensible
create function namedparmcursor_test7() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77, p1 := 42/0);
end $$ language plpgsql
;
ALTER TABLE testschema.test_default_tab_p ALTER val TYPE int
;
DROP DATABASE "regression	 !""x""#$%&'()*+-./0123456"
;
ALTER TABLE ONLY "public"."persons2" OF "public"."person_type"
;
ALTER INDEX dummy_test_idx SET (option_int = 3.3)
;
create table parent (a int primary key, f int references parent)
  partition by list (a)
;
DROP TYPE pg_temp.e
;
DROP TABLE testschema.dflt, testschema.dflt2
;
create type textrange2 as range(subtype=text, multirange_type_name=_textrange1, collation="C")
;
-- previous modulus 50 is factor of 150 but this modulus is not factor of next modulus 200.
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 150, REMAINDER 3)
;
ANALYZE plt3_adv
;
DROP TABLE brin_hot_3
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "file" WITH "simple"
;
-- left out basetype
create aggregate newcnt1 (sfunc = int4inc,
			  stype = int4,
			  initcond = '0')
;
CREATE OR REPLACE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'INSERT INTO sro_trojan_table DEFAULT VALUES; SELECT true'
;
-- check that specifying incompatible collations for partition bound
-- expressions fails promptly

create table test_part_coll_posix (a text) partition by range (a collate "POSIX")
;
CREATE OR REPLACE TEMP VIEW v_window AS
	SELECT i, sum(i) over (order by i groups between 1 preceding and 1 following) as sum_rows FROM generate_series(1, 10) i
;
alter table tpart_default attach partition tpart_default_default default
;
-- Test cases where one side has the default partition while the other side
-- has the NULL partition
DROP TABLE plt2_adv_p1
;
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = true)
;
CREATE TABLE "public"."prt2_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
VACUUM (ANALYZE) pg_catalog.pg_collation
;
CREATE OR REPLACE FUNCTION create_extension_with_temp_schema()
  RETURNS VOID AS $$
  DECLARE
    tmpschema text;
    query text;
  BEGIN
    SELECT INTO tmpschema pg_my_temp_schema()::regnamespace;
    query := 'CREATE EXTENSION test_ext4 SCHEMA ' || tmpschema || ' CASCADE;';
    RAISE NOTICE 'query %', query;
    EXECUTE query;
  END; $$ LANGUAGE plpgsql
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_attribute
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_26520_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE trans_foo (a int)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA pubme CREATE TABLE t0 (c int, d int) CREATE TABLE t1 (c int)
;
----------
[DROP_LIBRARY] DROP EXTENSION pg_logicalinspect
;
----------
[CREATE_TABLE] CREATE TABLE regress_notnull1 (a integer)
;
----------
[ALTER_INDEX] ALTER INDEX "public"."trigger_parted_p1_pkey" ATTACH PARTITION "public"."trigger_parted_p1_1_pkey"
;
----------
[CREATE_TABLE] -- ok

-- Test non-inheritable parent constraints
create table p1(ff1 int)
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_radixtree/test_radixtree--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_radixtree()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_radixtree' LANGUAGE C
;
----------
[ALTER_PUB_SUB] -- ok - drop table
ALTER PUBLICATION testpub_tbl9 DROP TABLE public.testpub_tbl7
;
----------
[CREATE_VIEW(SELECT)] -- to exercise dropped-col cases
CREATE VIEW test_pg_dump_v1 AS SELECT * FROM test_pg_dump_t1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33148 RENAME TO reind_con_toast
;
----------
[DROP_INDEX] drop index idxpart_gin
;
----------
[CREATE_PROG_OBJ] -- test that we can't use part of an existing commutator or negator pair
-- as a commutator or negator
CREATE OPERATOR === (
    leftarg = integer,
    rightarg = integer,
    procedure = int4eq,
    commutator = =
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- should fail

ALTER TABLE fk_r ATTACH PARTITION fk_r_1 FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (somename.somename)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE parent ALTER COLUMN a SET STATISTICS 100
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."limit_thousand_v_5" AS
 SELECT "onek"."thousand"
   FROM "public"."onek"
  WHERE ("onek"."thousand" < 995)
  ORDER BY "onek"."thousand"
 LIMIT ALL
;
----------
[CREATE_TYPE] CREATE TYPE "public"."sqljsonb_rec" AS (
	"a" integer,
	"t" "text",
	"js" json,
	"jb" "jsonb",
	"jsa" json[]
)
;
----------
[CREATE_TABLE] -- test an unlogged table, mostly to get coverage of gistbuildempty
create unlogged table gist_tbl (b box)
;
----------
[CREATE_TABLE] -- Test that child does not inherit NO INHERIT constraints
create table c1 () inherits (p1)
;
----------
[ALTER_TYPE] alter type e add value '1' after '0'
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY danish_stem
	(TEMPLATE = snowball, Language = danish , StopWords=danish)
;
----------
[BLOCK] do $$
begin
  assert 1=0, 'unhandled assertion';
exception when others then
  null; -- do nothing
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE spill_test(data text)
;
----------
[ALTER_TABLE] -- disinheriting it first works
alter table child no inherit parent
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16814 host=/tmp/I9Nd_BkCYQ dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_PROG_OBJ] -- division by zero runtime error, the context given in the error message
-- should be sensible
create function namedparmcursor_test7() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77, p1 := 42/0);
end $$ language plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE testschema.test_default_tab_p ALTER val TYPE int
;
----------
[DROP_CATALOG] DROP DATABASE "regression	 !""x""#$%&'()*+-./0123456"
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."persons2" OF "public"."person_type"
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_int = 3.3)
;
----------
[CREATE_TABLE] create table parent (a int primary key, f int references parent)
  partition by list (a)
;
----------
[DROP_TYPE] DROP TYPE pg_temp.e
;
----------
[DROP_TABLE] DROP TABLE testschema.dflt, testschema.dflt2
;
----------
[CREATE_POLICY] create type textrange2 as range(subtype=text, multirange_type_name=_textrange1, collation="C")
;
----------
[CREATE_TABLE] -- previous modulus 50 is factor of 150 but this modulus is not factor of next modulus 200.
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 150, REMAINDER 3)
;
----------
[ADMIN_TABLE] ANALYZE plt3_adv
;
----------
[DROP_TABLE] DROP TABLE brin_hot_3
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "file" WITH "simple"
;
----------
[CREATE_PROG_OBJ] -- left out basetype
create aggregate newcnt1 (sfunc = int4inc,
			  stype = int4,
			  initcond = '0')
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'INSERT INTO sro_trojan_table DEFAULT VALUES; SELECT true'
;
----------
[CREATE_TABLE] -- check that specifying incompatible collations for partition bound
-- expressions fails promptly

create table test_part_coll_posix (a text) partition by range (a collate "POSIX")
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE TEMP VIEW v_window AS
	SELECT i, sum(i) over (order by i groups between 1 preceding and 1 following) as sum_rows FROM generate_series(1, 10) i
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table tpart_default attach partition tpart_default_default default
;
----------
[DROP_TABLE] -- Test cases where one side has the default partition while the other side
-- has the NULL partition
DROP TABLE plt2_adv_p1
;
----------
[ALTER_TABLE] ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = true)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_collation
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION create_extension_with_temp_schema()
  RETURNS VOID AS $$
  DECLARE
    tmpschema text;
    query text;
  BEGIN
    SELECT INTO tmpschema pg_my_temp_schema()::regnamespace;
    query := 'CREATE EXTENSION test_ext4 SCHEMA ' || tmpschema || ' CASCADE;';
    RAISE NOTICE 'query %', query;
    EXECUTE query;
  END; $$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_attribute
;
