ALTER DATABASE "pl_regression" SET timezone_abbreviations TO 'Default'
;
-- fails
create temp table recur2 (f1 int, f2 recur1)
;
-- shouldn't trigger a table_rewrite event
alter table rewriteme alter column foo type numeric(12,4)
;
CREATE FUNCTION split_alter_table.touch_payload() RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
    NEW.payload := COALESCE(NEW.payload, 'touched');
    RETURN NEW;
END
$$
;
create domain insert_test_domain as insert_test_type
  check ((value).if2[1] is not null)
;
ALTER CONVERSION alt_conv3 SET SCHEMA alt_nsp2
;
ANALYZE public.prt2_l_p2_p2
;
CREATE FUNCTION "public"."check_con_function"("public"."check_con_tbl") RETURNS boolean
    LANGUAGE "plpgsql" IMMUTABLE
    AS $_$
begin
  raise notice 'input = %', row_to_json($1);
  return $1.f1 > 0;
end $_$
;
-- Check that rewrite rules splitting one INSERT into multiple
-- conditional statements does not disable FK checking.
create table rule_and_refint_t1 (
	id1a integer,
	id1b integer,

	primary key (id1a, id1b)
)
;
drop table range_list_parted
;
COMMENT ON FUNCTION cube_overlap(cube, cube) IS 'overlaps'
;
CREATE FUNCTION file_fdw_validator(text[], oid)
RETURNS void
AS '$libdir/file_fdw'
LANGUAGE C STRICT
;
/*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
DROP TABLE pg_temp_9.pg_temp_38025_2, pg_temp_9.pg_temp_38025
;
ALTER SUBSCRIPTION regress_mysub1 CONNECTION 'port=15037 host=/tmp/2MdPFByj7i dbname=postgres'
;
ALTER TABLE pg_toast.pg_toast_28539 RENAME TO reind_con_toast
;
ALTER TABLE ONLY list_parted3 ADD PRIMARY KEY (a)
;
CREATE TABLE sch1.tbl1_part2 PARTITION OF sch1.tbl1 FOR VALUES FROM (10) to (20)
;
CREATE FUNCTION pt_in_widget(point, widget)
   RETURNS bool
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT
;
create collation mycollation from "POSIX"
;
-- try adding a second primary key - should fail
alter table atacc1 add constraint atacc_test2 primary key (test)
;
TRUNCATE fd_pt2_1
;
DROP TABLE PRIMARY_TBL
;
alter type insenum add value 'i9' before 'L2'
;
CREATE TEXT SEARCH CONFIGURATION indonesian
	(PARSER = default)
;
ALTER TEXT SEARCH CONFIGURATION dutch ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH dutch_stem
;
CREATE TABLE fail_part (
	b char(2) COLLATE "C",
	a int NOT NULL
)
;
CREATE TABLE heaptable USING heap AS
  SELECT a, repeat(a::text, 100) FROM generate_series(1,9) AS a
;
CREATE FUNCTION casttestfunc(casttesttype) RETURNS int4 LANGUAGE SQL AS
$$ SELECT 1; $$
;
create table parted_trig1 partition of parted_trig for values in (1)
;
VACUUM (PARALLEL 0) information_schema.sql_sizing
;
CREATE VIEW xmltableview1 AS SELECT  xmltable.*
   FROM (SELECT data FROM xmldata) x,
        LATERAL XMLTABLE('/ROWS/ROW'
                         PASSING data
                         COLUMNS id int PATH '@id',
                                  _id FOR ORDINALITY,
                                  country_name text PATH 'COUNTRY_NAME/text()' NOT NULL,
                                  country_id text PATH 'COUNTRY_ID',
                                  region_id int PATH 'REGION_ID',
                                  size float PATH 'SIZE',
                                  unit text PATH 'SIZE/@unit',
                                  premier_name text PATH 'PREMIER_NAME' DEFAULT 'not specified')
;
drop table c1
;
ALTER INDEX pg_toast.pg_toast_28117_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION transition_table_level2_bad_usage_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
  BEGIN
    INSERT INTO dx VALUES (1000000, 1000000, 'x');
    RETURN NULL;
  END;
$$
;
-- test jsonb[] -> python dict
-- dict with array as value
CREATE FUNCTION test1arr(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {"d": [12, 1]})
return len(val)
$$
;
-- check that a detached partition is not dropped on dropping a partitioned table
CREATE TABLE range_parted2 (
    a int
) PARTITION BY RANGE(a)
;
CREATE TABLE "public"."test_storage" (
    "a" "text",
    "b" integer DEFAULT ("random"())::integer
)
;
CREATE TABLE temporal_mltrng ( id int4range, valid_at datemultirange)
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', escape ':')
;
CREATE TABLE spc_abort (id serial PRIMARY KEY, id2 int)
;
create or replace function explain_analyze_inc_sort_nodes(query text)
returns jsonb language plpgsql
as
$$
declare
  elements jsonb;
  element jsonb;
  matching_nodes jsonb := '[]'::jsonb;
begin
  execute 'explain (analyze, costs off, summary off, timing off, format ''json'') ' || query into strict elements;
  while jsonb_array_length(elements) > 0 loop
    element := elements->0;
    elements := elements - 0;
    case jsonb_typeof(element)
    when 'array' then
      if jsonb_array_length(element) > 0 then
        elements := elements || element;
      end if;
    when 'object' then
      if element ? 'Plan' then
        elements := elements || jsonb_build_array(element->'Plan');
        element := element - 'Plan';
      else
        if element ? 'Plans' then
          elements := elements || jsonb_build_array(element->'Plans');
          element := element - 'Plans';
        end if;
        if (element->>'Node Type')::text = 'Incremental Sort' then
          matching_nodes := matching_nodes || element;
        end if;
      end if;
    end case;
  end loop;
  return matching_nodes;
end;
$$
;
CREATE INDEX t2_gist ON s5.t2 USING GIST (b)
;
alter table child no inherit parent
;
CREATE FUNCTION voidtest1(a int) RETURNS VOID LANGUAGE SQL AS
$$ SELECT a + 1 $$
;
CREATE TABLE droppk21 PARTITION OF droppk2 FOR VALUES FROM (1000) TO (1400)
;
drop trigger trig_null on rem2
;
-- Tidy up
DROP OPERATOR <<< (int, int)
;
create type myint
;
ALTER TABLE pg_temp_13.pg_temp_25481_2 ADD COLUMN newdata pg_temp_13.pg_temp_25481
;
------------------------------------------------------------------------------------------
[ALTER_CATALOG] ALTER DATABASE "pl_regression" SET timezone_abbreviations TO 'Default'
;
----------
[CREATE_TABLE] -- fails
create temp table recur2 (f1 int, f2 recur1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- shouldn't trigger a table_rewrite event
alter table rewriteme alter column foo type numeric(12,4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_alter_table.touch_payload() RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
    NEW.payload := COALESCE(NEW.payload, 'touched');
    RETURN NEW;
END
$$
;
----------
[CREATE_TYPE] create domain insert_test_domain as insert_test_type
  check ((value).if2[1] is not null)
;
----------
[ALTER_POLICY] ALTER CONVERSION alt_conv3 SET SCHEMA alt_nsp2
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_l_p2_p2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."check_con_function"("public"."check_con_tbl") RETURNS boolean
    LANGUAGE "plpgsql" IMMUTABLE
    AS $_$
begin
  raise notice 'input = %', row_to_json($1);
  return $1.f1 > 0;
end $_$
;
----------
[CREATE_TABLE] -- Check that rewrite rules splitting one INSERT into multiple
-- conditional statements does not disable FK checking.
create table rule_and_refint_t1 (
	id1a integer,
	id1b integer,

	primary key (id1a, id1b)
)
;
----------
[DROP_TABLE] drop table range_list_parted
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_overlap(cube, cube) IS 'overlaps'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION file_fdw_validator(text[], oid)
RETURNS void
AS '$libdir/file_fdw'
LANGUAGE C STRICT
;
----------
[CREATE_POLICY] /*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_38025_2, pg_temp_9.pg_temp_38025
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_mysub1 CONNECTION 'port=15037 host=/tmp/2MdPFByj7i dbname=postgres'
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28539 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY list_parted3 ADD PRIMARY KEY (a)
;
----------
[CREATE_TABLE] CREATE TABLE sch1.tbl1_part2 PARTITION OF sch1.tbl1 FOR VALUES FROM (10) to (20)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pt_in_widget(point, widget)
   RETURNS bool
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT
;
----------
[CREATE_POLICY] create collation mycollation from "POSIX"
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- try adding a second primary key - should fail
alter table atacc1 add constraint atacc_test2 primary key (test)
;
----------
[TRUNCATE_TABLE] TRUNCATE fd_pt2_1
;
----------
[DROP_TABLE] DROP TABLE PRIMARY_TBL
;
----------
[ALTER_TYPE] alter type insenum add value 'i9' before 'L2'
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION indonesian
	(PARSER = default)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION dutch ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH dutch_stem
;
----------
[CREATE_TABLE] CREATE TABLE fail_part (
	b char(2) COLLATE "C",
	a int NOT NULL
)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE heaptable USING heap AS
  SELECT a, repeat(a::text, 100) FROM generate_series(1,9) AS a
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION casttestfunc(casttesttype) RETURNS int4 LANGUAGE SQL AS
$$ SELECT 1; $$
;
----------
[CREATE_TABLE] create table parted_trig1 partition of parted_trig for values in (1)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) information_schema.sql_sizing
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmltableview1 AS SELECT  xmltable.*
   FROM (SELECT data FROM xmldata) x,
        LATERAL XMLTABLE('/ROWS/ROW'
                         PASSING data
                         COLUMNS id int PATH '@id',
                                  _id FOR ORDINALITY,
                                  country_name text PATH 'COUNTRY_NAME/text()' NOT NULL,
                                  country_id text PATH 'COUNTRY_ID',
                                  region_id int PATH 'REGION_ID',
                                  size float PATH 'SIZE',
                                  unit text PATH 'SIZE/@unit',
                                  premier_name text PATH 'PREMIER_NAME' DEFAULT 'not specified')
;
----------
[DROP_TABLE] drop table c1
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28117_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION transition_table_level2_bad_usage_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
  BEGIN
    INSERT INTO dx VALUES (1000000, 1000000, 'x');
    RETURN NULL;
  END;
$$
;
----------
[CREATE_PROG_OBJ] -- test jsonb[] -> python dict
-- dict with array as value
CREATE FUNCTION test1arr(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {"d": [12, 1]})
return len(val)
$$
;
----------
[CREATE_TABLE] -- check that a detached partition is not dropped on dropping a partitioned table
CREATE TABLE range_parted2 (
    a int
) PARTITION BY RANGE(a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_storage" (
    "a" "text",
    "b" integer DEFAULT ("random"())::integer
)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_mltrng ( id int4range, valid_at datemultirange)
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', escape ':')
;
----------
[CREATE_TABLE] CREATE TABLE spc_abort (id serial PRIMARY KEY, id2 int)
;
----------
[CREATE_PROG_OBJ] create or replace function explain_analyze_inc_sort_nodes(query text)
returns jsonb language plpgsql
as
$$
declare
  elements jsonb;
  element jsonb;
  matching_nodes jsonb := '[]'::jsonb;
begin
  execute 'explain (analyze, costs off, summary off, timing off, format ''json'') ' || query into strict elements;
  while jsonb_array_length(elements) > 0 loop
    element := elements->0;
    elements := elements - 0;
    case jsonb_typeof(element)
    when 'array' then
      if jsonb_array_length(element) > 0 then
        elements := elements || element;
      end if;
    when 'object' then
      if element ? 'Plan' then
        elements := elements || jsonb_build_array(element->'Plan');
        element := element - 'Plan';
      else
        if element ? 'Plans' then
          elements := elements || jsonb_build_array(element->'Plans');
          element := element - 'Plans';
        end if;
        if (element->>'Node Type')::text = 'Incremental Sort' then
          matching_nodes := matching_nodes || element;
        end if;
      end if;
    end case;
  end loop;
  return matching_nodes;
end;
$$
;
----------
[ADD_INDEX] CREATE INDEX t2_gist ON s5.t2 USING GIST (b)
;
----------
[ALTER_TABLE] alter table child no inherit parent
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION voidtest1(a int) RETURNS VOID LANGUAGE SQL AS
$$ SELECT a + 1 $$
;
----------
[CREATE_TABLE] CREATE TABLE droppk21 PARTITION OF droppk2 FOR VALUES FROM (1000) TO (1400)
;
----------
[DROP_TRIGGER] drop trigger trig_null on rem2
;
----------
[DROP_PROG_OBJ] -- Tidy up
DROP OPERATOR <<< (int, int)
;
----------
[CREATE_TYPE] create type myint
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_25481_2 ADD COLUMN newdata pg_temp_13.pg_temp_25481
;
