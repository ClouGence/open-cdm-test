-- not spilling subxact, followed by spilling subxact
BEGIN
;
LOCK TABLE public.invalid_check_con_child IN ACCESS SHARE MODE
;
LOCK TABLE "public"."aggtest" IN ACCESS SHARE MODE
;
-- failed (not owner)
ALTER STATISTICS alt_stat2 SET SCHEMA alt_nsp2
;
COPY public.tst_arrays TO STDOUT
;
LOCK TABLE public.pagg_tab_para IN ACCESS SHARE MODE
;
-- successful usage of DEFAULT option in COPY
copy copy_default from stdin with (default '\D')
;
-- Should not leak

-- privilege checks for pg_stats_ext and pg_stats_ext_exprs
RESET SESSION AUTHORIZATION
;
--
-- UPDATABLE VIEWS
--

-- avoid bit-exact output here because operations may not be bit-exact.
SET extra_float_digits = 0
;
COPY public.rtest_view2 (a) TO stdout
;
------------------------------------------------------------------------------------------
[TRANSACTION] -- not spilling subxact, followed by spilling subxact
BEGIN
;
----------
[SESSION_LOCK] LOCK TABLE public.invalid_check_con_child IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."aggtest" IN ACCESS SHARE MODE
;
----------
[ADMIN_PERFORMANCE] -- failed (not owner)
ALTER STATISTICS alt_stat2 SET SCHEMA alt_nsp2
;
----------
[DATA_EXPORT] COPY public.tst_arrays TO STDOUT
;
----------
[SESSION_LOCK] LOCK TABLE public.pagg_tab_para IN ACCESS SHARE MODE
;
----------
[DATA_IMPORT] -- successful usage of DEFAULT option in COPY
copy copy_default from stdin with (default '\D')
;
----------
[SWITCH_USER] -- Should not leak

-- privilege checks for pg_stats_ext and pg_stats_ext_exprs
RESET SESSION AUTHORIZATION
;
----------
[SESSION_SETTING_WRITE] --
-- UPDATABLE VIEWS
--

-- avoid bit-exact output here because operations may not be bit-exact.
SET extra_float_digits = 0
;
----------
[DATA_EXPORT] COPY public.rtest_view2 (a) TO stdout
;
