EXECUTE dumpTableAttach('40231')
;
-- When GROUP BY clause does not match; partial aggregation is performed for each partition.
-- Also test GroupAggregate paths by disabling hash aggregates.
SET enable_hashagg TO false
;
LOCK TABLE "regress_indexing"."pk5" IN ACCESS SHARE MODE
;
-- non-superuser with bypass privilege can bypass RLS policy when disabled
SET SESSION AUTHORIZATION regress_rls_exempt_user
;
ALTER ROLE regress_bypassrls BYPASSRLS
;
EXECUTE dumpDomain('36448')
;
-- ERROR
CREATE FOREIGN DATA WRAPPER foo OPTIONS (testing '1', another '2')
;
EXECUTE getDomainConstraints('41573')
;
ALTER TABLE public.sequence_test5 OWNER TO zyc
;
REINDEX TABLE CONCURRENTLY psql_pipeline
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpTableAttach('40231')
;
----------
[SESSION_SETTING_WRITE] -- When GROUP BY clause does not match; partial aggregation is performed for each partition.
-- Also test GroupAggregate paths by disabling hash aggregates.
SET enable_hashagg TO false
;
----------
[SESSION_LOCK] LOCK TABLE "regress_indexing"."pk5" IN ACCESS SHARE MODE
;
----------
[SWITCH_USER] -- non-superuser with bypass privilege can bypass RLS policy when disabled
SET SESSION AUTHORIZATION regress_rls_exempt_user
;
----------
[ALTER_USER] ALTER ROLE regress_bypassrls BYPASSRLS
;
----------
[UNSAFE] EXECUTE dumpDomain('36448')
;
----------
[SYSTEM_SETTING_WRITE] -- ERROR
CREATE FOREIGN DATA WRAPPER foo OPTIONS (testing '1', another '2')
;
----------
[UNSAFE] EXECUTE getDomainConstraints('41573')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.sequence_test5 OWNER TO zyc
;
----------
[ADMIN] REINDEX TABLE CONCURRENTLY psql_pipeline
;
