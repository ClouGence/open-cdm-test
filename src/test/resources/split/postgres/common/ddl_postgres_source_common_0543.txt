create table boolpart_f partition of boolpart for values in ('false')
;
CREATE TEXT SEARCH CONFIGURATION english
	(PARSER = default)
;
create trigger parenttrig after insert on parent
for each row execute procedure f()
;
create table mlparted_tab_part1 partition of mlparted_tab for values in (1)
;
create table defpart_attach_test1 partition of defpart_attach_test for values in (1)
;
drop table ppk, pfk, pfk1
;
DROP TABLE test_missing_target3
;
create trigger tg_chkslotname before insert
    on WSlot for each row execute procedure tg_chkslotname('WS')
;
-- ===================================================================
-- test COPY FROM
-- ===================================================================

create table loc2 (f1 int, f2 text)
;
CREATE DOMAIN split_extension_next.ext_code AS text CHECK (VALUE <> '')
;
DROP TABLE unlogged1
;
CREATE DOMAIN "public"."plpgsql_domain" AS integer
	CONSTRAINT "plpgsql_domain_check" CHECK ("public"."plpgsql_domain_check"(VALUE))
;
DROP SCHEMA sts_sch1 CASCADE
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_shseclabel
;
-- No mapping for token supported by the configuration's parser, fails.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING FOR word
;
ALTER TABLE notnull_tbl1 ATTACH PARTITION notnull_tbl1_2 FOR VALUES IN (3,4)
;
create trigger trig_part_create before insert on tab_part_create
  for each statement execute procedure func_part_create()
;
CREATE TABLE tableam_parted_c_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('c') USING heap
;
create database abc
;
ALTER TABLE ONLY "public"."constr_child3" INHERIT "public"."constr_parent3"
;
CALL transaction_test9b(report_count())
;
CREATE SEQUENCE s3.seq2
;
CREATE TABLE distinct_tbl (x int, y int)
;
ALTER TABLE pg_toast.pg_toast_30476 RENAME TO reind_con_toast
;
ALTER TABLE ONLY "generated_stored_tests"."gtest20b"
    ADD CONSTRAINT "gtest20b_pkey" PRIMARY KEY ("a")
;
CREATE EXTENSION jsonb_plperl CASCADE
;
CREATE TRIGGER invalid_trig AFTER UPDATE ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_upd_row')
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=26046 host=/tmp/PwAQRKpWqD dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
create table idxpart1 (col_keep int)
;
ALTER INDEX pg_toast.pg_toast_30547_index RENAME TO reind_con_toast_idx
;
CREATE TABLE fk1 PARTITION OF fk FOR VALUES FROM (1) TO (100) PARTITION BY RANGE (a)
;
CREATE FUNCTION truncate_in_subxact() RETURNS VOID AS
$$
BEGIN
	TRUNCATE vistest;
EXCEPTION
  WHEN OTHERS THEN
	INSERT INTO vistest VALUES ('subxact failure');
END;
$$ language plpgsql
;
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_9'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
COMMENT ON COLUMN ctlt2.c IS 'C'
;
CREATE TABLE pitest1 (f1 date NOT NULL, f2 text, f3 bigint generated always as identity) PARTITION BY RANGE (f1)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
CREATE TABLE reindex_verbose(id integer primary key)
;
create index on tbl_timestamp0 using brin (d1) with (pages_per_range = 1, autosummarize=false)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub9
;
CREATE INDEX bt_f8_index ON bt_f8_heap USING btree (seqno float8_ops)
;
CREATE INDEX hash_txt_index ON hash_txt_heap USING hash (random text_ops)
;
drop extension test_ext8
;
create function polyf(x anycompatible, y anycompatible) returns anycompatiblearray as $$
  select array[x, y]
$$ language sql
;
-- For binary upgrade, set up typed tables this way.
ALTER TABLE ONLY "public"."rewritemetoo1" OF "public"."rewritetype"
;
CREATE TABLE "public"."trigger_parted_p1" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
create temp table htab (a int)
;
create function bogus_func(anyelement)
  returns anyrange as 'select int4range(1,10)' language sql
;
CREATE TABLE "public"."pht1_e_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE TABLE atest2 (col1 varchar(10), col2 boolean)
;
create function polyf(x anycompatiblerange, y anycompatible, z anycompatible) returns anycompatiblearray as $$
  select array[lower(x), upper(x), y, z]
$$ language sql
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table boolpart_f partition of boolpart for values in ('false')
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION english
	(PARSER = default)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parenttrig after insert on parent
for each row execute procedure f()
;
----------
[CREATE_TABLE] create table mlparted_tab_part1 partition of mlparted_tab for values in (1)
;
----------
[CREATE_TABLE] create table defpart_attach_test1 partition of defpart_attach_test for values in (1)
;
----------
[DROP_TABLE] drop table ppk, pfk, pfk1
;
----------
[DROP_TABLE] DROP TABLE test_missing_target3
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_chkslotname before insert
    on WSlot for each row execute procedure tg_chkslotname('WS')
;
----------
[CREATE_TABLE] -- ===================================================================
-- test COPY FROM
-- ===================================================================

create table loc2 (f1 int, f2 text)
;
----------
[CREATE_TYPE] CREATE DOMAIN split_extension_next.ext_code AS text CHECK (VALUE <> '')
;
----------
[DROP_TABLE] DROP TABLE unlogged1
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."plpgsql_domain" AS integer
	CONSTRAINT "plpgsql_domain_check" CHECK ("public"."plpgsql_domain_check"(VALUE))
;
----------
[DROP_SCHEMA] DROP SCHEMA sts_sch1 CASCADE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_shseclabel
;
----------
[ALTER_POLICY] -- No mapping for token supported by the configuration's parser, fails.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING FOR word
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE notnull_tbl1 ATTACH PARTITION notnull_tbl1_2 FOR VALUES IN (3,4)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_part_create before insert on tab_part_create
  for each statement execute procedure func_part_create()
;
----------
[CREATE_TABLE] CREATE TABLE tableam_parted_c_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('c') USING heap
;
----------
[CREATE_CATALOG] create database abc
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."constr_child3" INHERIT "public"."constr_parent3"
;
----------
[CALL_PROG_OBJ] CALL transaction_test9b(report_count())
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE s3.seq2
;
----------
[CREATE_TABLE] CREATE TABLE distinct_tbl (x int, y int)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30476 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest20b"
    ADD CONSTRAINT "gtest20b_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_LIBRARY] CREATE EXTENSION jsonb_plperl CASCADE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig AFTER UPDATE ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_upd_row')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=26046 host=/tmp/PwAQRKpWqD dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_TABLE] create table idxpart1 (col_keep int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30547_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE fk1 PARTITION OF fk FOR VALUES FROM (1) TO (100) PARTITION BY RANGE (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION truncate_in_subxact() RETURNS VOID AS
$$
BEGIN
	TRUNCATE vistest;
EXCEPTION
  WHEN OTHERS THEN
	INSERT INTO vistest VALUES ('subxact failure');
END;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_9'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN ctlt2.c IS 'C'
;
----------
[CREATE_TABLE] CREATE TABLE pitest1 (f1 date NOT NULL, f2 text, f3 bigint generated always as identity) PARTITION BY RANGE (f1)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
----------
[CREATE_TABLE] CREATE TABLE reindex_verbose(id integer primary key)
;
----------
[ADD_INDEX] create index on tbl_timestamp0 using brin (d1) with (pages_per_range = 1, autosummarize=false)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub9
;
----------
[ADD_INDEX] CREATE INDEX bt_f8_index ON bt_f8_heap USING btree (seqno float8_ops)
;
----------
[ADD_INDEX] CREATE INDEX hash_txt_index ON hash_txt_heap USING hash (random text_ops)
;
----------
[DROP_LIBRARY] drop extension test_ext8
;
----------
[CREATE_PROG_OBJ] create function polyf(x anycompatible, y anycompatible) returns anycompatiblearray as $$
  select array[x, y]
$$ language sql
;
----------
[ALTER_TABLE] -- For binary upgrade, set up typed tables this way.
ALTER TABLE ONLY "public"."rewritemetoo1" OF "public"."rewritetype"
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted_p1" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
----------
[CREATE_TABLE] create temp table htab (a int)
;
----------
[CREATE_PROG_OBJ] create function bogus_func(anyelement)
  returns anyrange as 'select int4range(1,10)' language sql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht1_e_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE atest2 (col1 varchar(10), col2 boolean)
;
----------
[CREATE_PROG_OBJ] create function polyf(x anycompatiblerange, y anycompatible, z anycompatible) returns anycompatiblearray as $$
  select array[lower(x), upper(x), y, z]
$$ language sql
;
