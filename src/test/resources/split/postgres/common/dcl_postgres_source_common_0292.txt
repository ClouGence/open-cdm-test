ALTER TABLE public.inhf OWNER TO zyc
;
EXECUTE dumpFunc('35662')
;
DROP SERVER fdtest
;
-- We should not see the portal that is created internally to
-- implement EXECUTE in pg_cursors
PREPARE cprep AS
  SELECT name, statement, is_holdable, is_binary, is_scrollable FROM pg_cursors
;
LOCK TABLE public.domain_test IN ACCESS SHARE MODE
;
ALTER TABLE public.utf8_verification_inputs OWNER TO zyc
;
ALTER OPERATOR FAMILY alt_opf2 USING hash OWNER TO regress_alter_generic_user1
;
ALTER TABLE public.rtest_t1 OWNER TO zyc
;
COPY public.sj_t3 (id, a) TO stdout
;
EXECUTE dumpBaseType('30825')
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE public.inhf OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpFunc('35662')
;
----------
[SYSTEM_SETTING_WRITE] DROP SERVER fdtest
;
----------
[UNSAFE] -- We should not see the portal that is created internally to
-- implement EXECUTE in pg_cursors
PREPARE cprep AS
  SELECT name, statement, is_holdable, is_binary, is_scrollable FROM pg_cursors
;
----------
[SESSION_LOCK] LOCK TABLE public.domain_test IN ACCESS SHARE MODE
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.utf8_verification_inputs OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER OPERATOR FAMILY alt_opf2 USING hash OWNER TO regress_alter_generic_user1
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.rtest_t1 OWNER TO zyc
;
----------
[DATA_EXPORT] COPY public.sj_t3 (id, a) TO stdout
;
----------
[UNSAFE] EXECUTE dumpBaseType('30825')
;
