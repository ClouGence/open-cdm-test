CREATE TABLE pgss_schema_2.tab_search_diff_2 (a text)
;
create table mclparted_0_null partition of mclparted for values in(0,null)
;
CREATE TYPE gtest_type AS (f1 integer, f2 text, f3 bigint)
;
CREATE TABLE tab4 (x int PRIMARY KEY, y int REFERENCES tab3)
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "word" WITH "english_stem"
;
CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=19323 host=/tmp/FohDx5iOiZ dbname=postgres' PUBLICATION pub_viaroot
;
-- be even number
CREATE POLICY p2 ON t2 FOR ALL TO PUBLIC USING (a % 2 = 1)
;
CREATE DATABASE tbuiltin6 ENCODING 'LATIN1' TEMPLATE template0 BUILTIN_LOCALE 'C.UTF-8' LC_COLLATE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
ALTER SUBSCRIPTION regress_testsub SET (slot_name = '')
;
VACUUM
;
CREATE TABLE tab_nothing (a int)
;
ALTER TABLE tt9 ADD CONSTRAINT foo UNIQUE(c)
;
CREATE TABLE moneyp_12 PARTITION OF moneyp FOR VALUES IN (to_char(12, '99')::int)
;
ALTER FUNCTION gbt_time_penalty(internal, internal, internal) PARALLEL SAFE
;
CALL test_proc10(_a, _b, c => _c)
;
ALTER TABLE pg_toast.pg_toast_28571 RENAME TO reind_con_toast
;
DROP DATABASE regress_db
;
-- test jsonb -> python dict
CREATE FUNCTION test1(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {'a': 1, 'c': 'NULL'})
return len(val)
$$
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
CREATE TYPE query_int (
	INTERNALLENGTH = -1,
	INPUT = bqarr_in,
	OUTPUT = bqarr_out
)
;
CREATE TABLE ctas_stats_1 AS SELECT 1 AS a
;
--
-- lock levels
--
drop type lockmodes
;
ALTER TABLE list_parted2_def ADD CONSTRAINT check_a CHECK (a IN (5, 6))
;
ALTER TABLE plt1 ATTACH PARTITION plt1_p3 DEFAULT
;
CREATE FUNCTION priv_testfunc2(int) RETURNS int AS 'select 3 * $1;' LANGUAGE sql
;
-- fail
SECURITY LABEL ON TABLE dummy_seclabel_tbl2 IS 'classified'
;
ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        5       pg_catalog.~ (text, text),
        OPERATOR        6       pg_catalog.~* (text, text)
;
CREATE SUBSCRIPTION sub CONNECTION 'port=23417 host=/tmp/k4PcfWEKjj dbname=postgres' PUBLICATION pub WITH (disable_on_error = true, streaming = on, two_phase = on)
;
CREATE FUNCTION isnne(ean13, issn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- test custom message
do $$
declare var text := 'some value';
begin
  assert 1=0, format('assertion failed, var = "%s"', var);
end;
$$
;
DROP DOMAIN js_int_array_1d
;
ALTER TABLE fd_pt1 ALTER COLUMN c8 SET DATA TYPE text
;
CREATE TABLE alpha_neg_p1 PARTITION OF alpha_neg FOR VALUES FROM (100) TO (200)
;
--------------- geo_distance as operator <@>

CREATE OPERATOR <@> (
  LEFTARG = point,
  RIGHTARG = point,
  PROCEDURE = geo_distance,
  COMMUTATOR = <@>
)
;
create rule rtest_nothn_r4 as on insert to rtest_nothn2
	do instead nothing
;
ANALYZE mcv_lists_bool
;
CREATE TABLE "public"."ihighway" (
    "name" "text",
    "thepath" "path"
)
;
CREATE AGGREGATE "public"."myaggn05a"(integer) (
    SFUNC = "public"."tfnp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
create table event_trigger_fire1 (a int)
;
CREATE TABLE tru_fk_table(fkey int references tru_pk_table(id))
;
CREATE OR REPLACE FUNCTION drop_sql_command()
RETURNS event_trigger AS $$
BEGIN
RAISE NOTICE '% - sql_drop', tg_tag;
END;
$$ LANGUAGE plpgsql
;
CREATE VIEW "public"."rtest_v1" AS
 SELECT "rtest_t1"."a",
    "rtest_t1"."b"
   FROM "public"."rtest_t1"
;
CREATE TABLE plt2_adv_p3_null PARTITION OF plt2_adv FOR VALUES IN (NULL, '0007', '0009')
;
CREATE EXTENSION test_ginpostinglist
;
ALTER INDEX pg_toast.pg_toast_30554_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION lca(_ltree)
RETURNS ltree
AS '$libdir/ltree','_lca'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create or replace function testrngfunc() returns setof rngfunc_type as $$
  select 7.136178319899999964, 7.136178319899999964;
$$ language sql volatile
;
drop table float8multirange_test
;
-- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0
;
--
-- test inlining of immutable functions
--
create function f_immutable_int4(i integer) returns integer as
$$ begin return i; end; $$ language plpgsql immutable
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE pgss_schema_2.tab_search_diff_2 (a text)
;
----------
[CREATE_TABLE] create table mclparted_0_null partition of mclparted for values in(0,null)
;
----------
[CREATE_TYPE] CREATE TYPE gtest_type AS (f1 integer, f2 text, f3 bigint)
;
----------
[CREATE_TABLE] CREATE TABLE tab4 (x int PRIMARY KEY, y int REFERENCES tab3)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "word" WITH "english_stem"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=19323 host=/tmp/FohDx5iOiZ dbname=postgres' PUBLICATION pub_viaroot
;
----------
[CREATE_POLICY] -- be even number
CREATE POLICY p2 ON t2 FOR ALL TO PUBLIC USING (a % 2 = 1)
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin6 ENCODING 'LATIN1' TEMPLATE template0 BUILTIN_LOCALE 'C.UTF-8' LC_COLLATE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (slot_name = '')
;
----------
[ADMIN_TABLE] VACUUM
;
----------
[CREATE_TABLE] CREATE TABLE tab_nothing (a int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD CONSTRAINT foo UNIQUE(c)
;
----------
[CREATE_TABLE] CREATE TABLE moneyp_12 PARTITION OF moneyp FOR VALUES IN (to_char(12, '99')::int)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_time_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[CALL_PROG_OBJ] CALL test_proc10(_a, _b, c => _c)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28571 RENAME TO reind_con_toast
;
----------
[DROP_CATALOG] DROP DATABASE regress_db
;
----------
[CREATE_PROG_OBJ] -- test jsonb -> python dict
CREATE FUNCTION test1(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {'a': 1, 'c': 'NULL'})
return len(val)
$$
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
----------
[CREATE_TYPE] CREATE TYPE query_int (
	INTERNALLENGTH = -1,
	INPUT = bqarr_in,
	OUTPUT = bqarr_out
)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_stats_1 AS SELECT 1 AS a
;
----------
[DROP_TYPE] --
-- lock levels
--
drop type lockmodes
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE list_parted2_def ADD CONSTRAINT check_a CHECK (a IN (5, 6))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt1 ATTACH PARTITION plt1_p3 DEFAULT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION priv_testfunc2(int) RETURNS int AS 'select 3 * $1;' LANGUAGE sql
;
----------
[SECURITY_LABEL] -- fail
SECURITY LABEL ON TABLE dummy_seclabel_tbl2 IS 'classified'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        5       pg_catalog.~ (text, text),
        OPERATOR        6       pg_catalog.~* (text, text)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub CONNECTION 'port=23417 host=/tmp/k4PcfWEKjj dbname=postgres' PUBLICATION pub WITH (disable_on_error = true, streaming = on, two_phase = on)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ean13, issn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[BLOCK] -- test custom message
do $$
declare var text := 'some value';
begin
  assert 1=0, format('assertion failed, var = "%s"', var);
end;
$$
;
----------
[DROP_TYPE] DROP DOMAIN js_int_array_1d
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c8 SET DATA TYPE text
;
----------
[CREATE_TABLE] CREATE TABLE alpha_neg_p1 PARTITION OF alpha_neg FOR VALUES FROM (100) TO (200)
;
----------
[CREATE_PROG_OBJ] --------------- geo_distance as operator <@>

CREATE OPERATOR <@> (
  LEFTARG = point,
  RIGHTARG = point,
  PROCEDURE = geo_distance,
  COMMUTATOR = <@>
)
;
----------
[CREATE_POLICY] create rule rtest_nothn_r4 as on insert to rtest_nothn2
	do instead nothing
;
----------
[ADMIN_TABLE] ANALYZE mcv_lists_bool
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ihighway" (
    "name" "text",
    "thepath" "path"
)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn05a"(integer) (
    SFUNC = "public"."tfnp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
----------
[CREATE_TABLE] create table event_trigger_fire1 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE tru_fk_table(fkey int references tru_pk_table(id))
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION drop_sql_command()
RETURNS event_trigger AS $$
BEGIN
RAISE NOTICE '% - sql_drop', tg_tag;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_v1" AS
 SELECT "rtest_t1"."a",
    "rtest_t1"."b"
   FROM "public"."rtest_t1"
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p3_null PARTITION OF plt2_adv FOR VALUES IN (NULL, '0007', '0009')
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ginpostinglist
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30554_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lca(_ltree)
RETURNS ltree
AS '$libdir/ltree','_lca'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function testrngfunc() returns setof rngfunc_type as $$
  select 7.136178319899999964, 7.136178319899999964;
$$ language sql volatile
;
----------
[DROP_TABLE] drop table float8multirange_test
;
----------
[CREATE_TABLE|SELECT] -- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0
;
----------
[CREATE_PROG_OBJ] --
-- test inlining of immutable functions
--
create function f_immutable_int4(i integer) returns integer as
$$ begin return i; end; $$ language plpgsql immutable
;
