-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."itest13"'::pg_catalog.regclass
;
INSERT INTO num_exp_sub VALUES (1,4,'-7799461.4119')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17551', relminmxid = '3'
WHERE oid = '"public"."alpha_neg_p1"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('0', 960)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8741', relminmxid = '1'
WHERE oid = '"public"."rtest_person"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15980', relminmxid = '3'
WHERE oid = '36887'
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (9, 1, 14159, 309, CURRENT_TIMESTAMP)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt2_m_p3"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."gtestxx_1"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."trigger_parted_p1"'::pg_catalog.regclass
;
UPDATE pgbench_branches SET bbalance = bbalance + -4313 WHERE bid = 1
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtest23p"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (1, 291)
;
INSERT INTO tab7 VALUES (4, 77, 888, 9999)
;
DELETE FROM tbl_gist WHERE c1 = 5 OR c3 = 12
;
INSERT INTO public.tplain VALUES ('6', 16)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1444', relminmxid = '1'
WHERE oid = '"public"."box_tbl"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (0, 800)
;
DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_11.pg_temp_26330_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
INSERT INTO public.tht VALUES ('5', 535)
;
INSERT INTO prt1_adv SELECT i, i % 25, to_char(i, 'FM0000') FROM generate_series(100, 399) i
;
INSERT INTO joinme VALUES('more', 12345)
;
INSERT INTO public.ths_p2 VALUES (3, 993)
;
INSERT INTO msource VALUES (1, 'source1'), (2, 'source2'), (3, 'source3')
;
INSERT INTO public.tplain VALUES ('2', 902)
;
INSERT INTO public.ths_p3 VALUES (1, 641)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart3"."pk52"'::pg_catalog.regclass
  AND attname IN ('a')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18508', relminmxid = '3'
WHERE oid = '"public"."test_io_vac_strategy"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16128', relminmxid = '3'
WHERE oid = '"public"."dropcolumn"'::pg_catalog.regclass
;
INSERT INTO upserttest(key, data) VALUES('k1', 'inserted s2') ON CONFLICT (blurt_and_lock(key)) DO UPDATE SET data = upserttest.data || ' with conflict update s2'
;
INSERT INTO pagg_tab_ml SELECT i % 30, i % 10, to_char(i % 4, 'FM0000') FROM generate_series(0, 29999) i
;
INSERT INTO table_one VALUES('*** TABLE ONE ***')
;
-- fails (SELECT hjtest_1.b * 5) = (SELECT hjtest_2.c*5)

INSERT INTO hjtest_2(a, id, b, c) VALUES (true, 1, 'another', 2)
;
INSERT INTO public.tplain VALUES ('1', 731)
;
-- the start jsonb is an array
delete from test_jsonb_subscript
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1283', relminmxid = '1'
WHERE oid = '"public"."test_range_excl"'::pg_catalog.regclass
;
INSERT INTO tab1_2 VALUES (5)
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted12"'::pg_catalog.regclass
  AND attname = v.dname
;
UPDATE pktab SET data = DEFAULT WHERE pg_advisory_lock_shared(571428) IS NOT NULL
;
insert into iocdu_tt_parted values (1, 'AAA'), (2, 'BBB')
  on conflict (a) do
  update set b = iocdu_tt_parted.b || ':' || excluded.b
;
INSERT INTO public.tht VALUES ('4', 64)
;
-- OK

insert into my_table values (2)
;
INSERT INTO basictest values ('88', 'haha', 'short', '123.1212')
;
INSERT INTO parttab (key, col1, col2) VALUES (1, 'a', 'b') ON CONFLICT (key) DO UPDATE SET key=1
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '19836', relminmxid = '3'
WHERE oid = '"public"."beta_neg_p3"'::pg_catalog.regclass
;
INSERT INTO dedup_unique_test_table SELECT 1
;
-- Insert Foreign Key values
INSERT INTO FKTABLE VALUES (1, 2, 3, 1)
;
INSERT INTO loct_empty
  SELECT id, 'AAA' || to_char(id, 'FM000') FROM generate_series(1, 100) id
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20227'
;
INSERT INTO public.tht VALUES ('2', 212)
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."itest13"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (1,4,'-7799461.4119')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17551', relminmxid = '3'
WHERE oid = '"public"."alpha_neg_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 960)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8741', relminmxid = '1'
WHERE oid = '"public"."rtest_person"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15980', relminmxid = '3'
WHERE oid = '36887'
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (9, 1, 14159, 309, CURRENT_TIMESTAMP)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt2_m_p3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."gtestxx_1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."trigger_parted_p1"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE pgbench_branches SET bbalance = bbalance + -4313 WHERE bid = 1
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtest23p"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 291)
;
----------
[INSERT] INSERT INTO tab7 VALUES (4, 77, 888, 9999)
;
----------
[DELETE] DELETE FROM tbl_gist WHERE c1 = 5 OR c3 = 12
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 16)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1444', relminmxid = '1'
WHERE oid = '"public"."box_tbl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 800)
;
----------
[DELETE|SELECT] DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_11.pg_temp_26330_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 535)
;
----------
[INSERT|SELECT] INSERT INTO prt1_adv SELECT i, i % 25, to_char(i, 'FM0000') FROM generate_series(100, 399) i
;
----------
[INSERT] INSERT INTO joinme VALUES('more', 12345)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 993)
;
----------
[INSERT] INSERT INTO msource VALUES (1, 'source1'), (2, 'source2'), (3, 'source3')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 902)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 641)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart3"."pk52"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18508', relminmxid = '3'
WHERE oid = '"public"."test_io_vac_strategy"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16128', relminmxid = '3'
WHERE oid = '"public"."dropcolumn"'::pg_catalog.regclass
;
----------
[MERGE|SELECT] INSERT INTO upserttest(key, data) VALUES('k1', 'inserted s2') ON CONFLICT (blurt_and_lock(key)) DO UPDATE SET data = upserttest.data || ' with conflict update s2'
;
----------
[INSERT|SELECT] INSERT INTO pagg_tab_ml SELECT i % 30, i % 10, to_char(i % 4, 'FM0000') FROM generate_series(0, 29999) i
;
----------
[INSERT] INSERT INTO table_one VALUES('*** TABLE ONE ***')
;
----------
[INSERT|SELECT] -- fails (SELECT hjtest_1.b * 5) = (SELECT hjtest_2.c*5)

INSERT INTO hjtest_2(a, id, b, c) VALUES (true, 1, 'another', 2)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 731)
;
----------
[DELETE] -- the start jsonb is an array
delete from test_jsonb_subscript
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1283', relminmxid = '1'
WHERE oid = '"public"."test_range_excl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO tab1_2 VALUES (5)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted12"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[UPDATE|SESSION_LOCK] UPDATE pktab SET data = DEFAULT WHERE pg_advisory_lock_shared(571428) IS NOT NULL
;
----------
[MERGE] insert into iocdu_tt_parted values (1, 'AAA'), (2, 'BBB')
  on conflict (a) do
  update set b = iocdu_tt_parted.b || ':' || excluded.b
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 64)
;
----------
[INSERT] -- OK

insert into my_table values (2)
;
----------
[INSERT] INSERT INTO basictest values ('88', 'haha', 'short', '123.1212')
;
----------
[MERGE|SELECT] INSERT INTO parttab (key, col1, col2) VALUES (1, 'a', 'b') ON CONFLICT (key) DO UPDATE SET key=1
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '19836', relminmxid = '3'
WHERE oid = '"public"."beta_neg_p3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO dedup_unique_test_table SELECT 1
;
----------
[INSERT] -- Insert Foreign Key values
INSERT INTO FKTABLE VALUES (1, 2, 3, 1)
;
----------
[INSERT|SELECT] INSERT INTO loct_empty
  SELECT id, 'AAA' || to_char(id, 'FM000') FROM generate_series(1, 100) id
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20227'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('2', 212)
;
