-- At least one permissive policy must exist, otherwise
-- the default deny policy will be applied.  For
-- testing the only-restrictive-policies from the hook,
-- create a simple 'allow all' policy.
CREATE POLICY p1 ON rls_test_restrictive USING (true)
;
CREATE FUNCTION gbt_cash_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- Try cast with a function

CREATE FUNCTION int4_casttesttype(int4) RETURNS casttesttype LANGUAGE SQL AS
$$ SELECT ('foo'::text || $1::text)::casttesttype; $$
;
CREATE POLICY p2 ON x1 FOR INSERT WITH CHECK (a % 2 = 1)
;
CREATE SCHEMA information_schema
;
CREATE TABLE "public"."agg_data_2k" (
    "g" integer
)
;
create domain dtop dinter check (substring(VALUE, 2, 1) = '1')
;
ALTER DOMAIN dcomptype ADD CONSTRAINT c1 check ((value).r > 0)
;
--    P    N        P    P
-- should ERROR: tf2p(anyarray, anyelement) not matched by tf2p(int[],anyelement)
CREATE AGGREGATE myaggp16a(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
VACUUM (DISABLE_PAGE_SKIPPING) vac_truncate_test
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=30938 host=/tmp/aWxT0CKw58 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
drop domain dcheck restrict
;
DROP EVENT TRIGGER IF EXISTS split_event_probe_proc
;
ALTER TABLE regtest_table_part ALTER p SET (n_distinct = 999)
;
-- bool check

CREATE TABLE booltmp (a bool)
;
CREATE UNIQUE INDEX "mvtest_tm_type" ON "public"."mvtest_tm" USING "btree" ("type")
;
CREATE TABLE "public"."equipment_r" (
    "name" "text",
    "hobby" "text"
)
;
-- error - generated column "b" must be published explicitly as it is
-- part of the REPLICA IDENTITY index.
CREATE PUBLICATION pub_gencol FOR TABLE testpub_gencol
;
ALTER TABLE pg_toast.pg_toast_31088 RENAME TO reind_con_toast
;
ALTER TABLE large_tuple_test ALTER COLUMN b SET STORAGE plain
;
-- error
DROP FUNCTION base_fn_out(base_type)
;
create table pktable_base(base1 int not null, base2 int)
;
CREATE TABLE t_test (x int)
;
-- UPC vs other types:
CREATE FUNCTION btupccmp(upc, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- Test a combination of local and remote triggers
CREATE TRIGGER trig_row_before
BEFORE INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
ALTER TABLE ONLY "public"."idxpart" ATTACH PARTITION "public"."idxpart2" FOR VALUES FROM (100) TO (1000)
;
create function anyctest(anycompatiblemultirange, anycompatiblemultirange)
returns anycompatible as $$
  select lower($1) + upper($2)
$$ language sql
;
CREATE FUNCTION dup (f1 anyelement, f2 out anyelement, f3 out anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
-- failed (not owner)
ALTER FUNCTION alt_func1(int) RENAME TO alt_func4
;
DROP FUNCTION rw_view1_trig_fn()
;
COMMENT ON AGGREGATE newcnt (*) IS 'an agg(*) comment'
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_operator
;
-- fail
alter table p2 add check (f2>0)
;
ALTER INDEX pg_toast.pg_toast_30343_index RENAME TO reind_con_toast_idx
;
DROP TABLE test_like_id_1, test_like_id_2, test_like_id_3
;
DROP FOREIGN TABLE ft1
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_constraint
;
ALTER INDEX pg_toast.pg_toast_27835_index RENAME TO reind_con_toast_idx
;
CREATE DOMAIN domain_stats AS int CHECK (VALUE > 0)
;
CREATE TABLE withz AS SELECT i AS k, (i || ' v')::text v FROM generate_series(1, 16, 3) i
;
create trigger ttdummy
	before delete or update on tttest
	for each row
	execute procedure
	ttdummy (price_on, price_off)
;
create trigger make_bogus_matview
  after insert on my_table
  referencing new table as new_table
  for each statement execute function make_bogus_matview()
;
drop materialized view matview_visibility_test
;
ALTER TABLE accounts_ext ADD COLUMN newcol2 text DEFAULT NULL
;
-- int2

CREATE TABLE TEMP_INT2 (f1 INT2)
;
COMMENT ON TEXT SEARCH CONFIGURATION dump_test.alt_ts_conf1
					   IS 'comment on text search configuration'
;
ALTER TABLE fk_partitioned_fk VALIDATE CONSTRAINT fk_partitioned_fk_a_b_fkey
;
-- Disable Parallel Append
alter table a_star reset (parallel_workers)
;
CREATE TEXT SEARCH DICTIONARY test_tsdict_exists (
        Template=ispell,
        DictFile=ispell_sample,
        AffFile=ispell_sample
)
;
/* contrib/isn/isn--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Example:
--   create table test ( id isbn );
--   insert into test values('978-0-393-04002-9');
--
--   select isbn('978-0-393-04002-9');
--   select isbn13('0-901690-54-6');
--

--
--	Input and output functions and data types:
--
---------------------------------------------------
CREATE FUNCTION ean13_in(cstring)
	RETURNS ean13
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] -- At least one permissive policy must exist, otherwise
-- the default deny policy will be applied.  For
-- testing the only-restrictive-policies from the hook,
-- create a simple 'allow all' policy.
CREATE POLICY p1 ON rls_test_restrictive USING (true)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_cash_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] -- Try cast with a function

CREATE FUNCTION int4_casttesttype(int4) RETURNS casttesttype LANGUAGE SQL AS
$$ SELECT ('foo'::text || $1::text)::casttesttype; $$
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON x1 FOR INSERT WITH CHECK (a % 2 = 1)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA information_schema
;
----------
[CREATE_TABLE] CREATE TABLE "public"."agg_data_2k" (
    "g" integer
)
;
----------
[CREATE_TYPE] create domain dtop dinter check (substring(VALUE, 2, 1) = '1')
;
----------
[ALTER_TYPE] ALTER DOMAIN dcomptype ADD CONSTRAINT c1 check ((value).r > 0)
;
----------
[CREATE_PROG_OBJ] --    P    N        P    P
-- should ERROR: tf2p(anyarray, anyelement) not matched by tf2p(int[],anyelement)
CREATE AGGREGATE myaggp16a(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) vac_truncate_test
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=30938 host=/tmp/aWxT0CKw58 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[DROP_TYPE] drop domain dcheck restrict
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER IF EXISTS split_event_probe_proc
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_table_part ALTER p SET (n_distinct = 999)
;
----------
[CREATE_TABLE] -- bool check

CREATE TABLE booltmp (a bool)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "mvtest_tm_type" ON "public"."mvtest_tm" USING "btree" ("type")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."equipment_r" (
    "name" "text",
    "hobby" "text"
)
;
----------
[CREATE_PUB_SUB] -- error - generated column "b" must be published explicitly as it is
-- part of the REPLICA IDENTITY index.
CREATE PUBLICATION pub_gencol FOR TABLE testpub_gencol
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_31088 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE large_tuple_test ALTER COLUMN b SET STORAGE plain
;
----------
[DROP_PROG_OBJ] -- error
DROP FUNCTION base_fn_out(base_type)
;
----------
[CREATE_TABLE] create table pktable_base(base1 int not null, base2 int)
;
----------
[CREATE_TABLE] CREATE TABLE t_test (x int)
;
----------
[CREATE_PROG_OBJ] -- UPC vs other types:
CREATE FUNCTION btupccmp(upc, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Test a combination of local and remote triggers
CREATE TRIGGER trig_row_before
BEFORE INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."idxpart" ATTACH PARTITION "public"."idxpart2" FOR VALUES FROM (100) TO (1000)
;
----------
[CREATE_PROG_OBJ] create function anyctest(anycompatiblemultirange, anycompatiblemultirange)
returns anycompatible as $$
  select lower($1) + upper($2)
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dup (f1 anyelement, f2 out anyelement, f3 out anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
----------
[RENAME_PROG_OBJ] -- failed (not owner)
ALTER FUNCTION alt_func1(int) RENAME TO alt_func4
;
----------
[DROP_PROG_OBJ] DROP FUNCTION rw_view1_trig_fn()
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE newcnt (*) IS 'an agg(*) comment'
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_operator
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- fail
alter table p2 add check (f2>0)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30343_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] DROP TABLE test_like_id_1, test_like_id_2, test_like_id_3
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ft1
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_constraint
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27835_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TYPE] CREATE DOMAIN domain_stats AS int CHECK (VALUE > 0)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE withz AS SELECT i AS k, (i || ' v')::text v FROM generate_series(1, 16, 3) i
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger ttdummy
	before delete or update on tttest
	for each row
	execute procedure
	ttdummy (price_on, price_off)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger make_bogus_matview
  after insert on my_table
  referencing new table as new_table
  for each statement execute function make_bogus_matview()
;
----------
[DROP_VIEW] drop materialized view matview_visibility_test
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE accounts_ext ADD COLUMN newcol2 text DEFAULT NULL
;
----------
[CREATE_TABLE] -- int2

CREATE TABLE TEMP_INT2 (f1 INT2)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION dump_test.alt_ts_conf1
					   IS 'comment on text search configuration'
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fk_partitioned_fk VALIDATE CONSTRAINT fk_partitioned_fk_a_b_fkey
;
----------
[ALTER_TABLE] -- Disable Parallel Append
alter table a_star reset (parallel_workers)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY test_tsdict_exists (
        Template=ispell,
        DictFile=ispell_sample,
        AffFile=ispell_sample
)
;
----------
[CREATE_PROG_OBJ] /* contrib/isn/isn--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Example:
--   create table test ( id isbn );
--   insert into test values('978-0-393-04002-9');
--
--   select isbn('978-0-393-04002-9');
--   select isbn13('0-901690-54-6');
--

--
--	Input and output functions and data types:
--
---------------------------------------------------
CREATE FUNCTION ean13_in(cstring)
	RETURNS ean13
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
