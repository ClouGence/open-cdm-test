CREATE FUNCTION test_resowner_leak()
	RETURNS pg_catalog.void
	AS '$libdir/test_resowner' LANGUAGE C
;
create table pktable1 (a int primary key)
;
/* contrib/pg_stat_statements/pg_stat_statements--1.7--1.8.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* First we have to remove them from the extension */
ALTER EXTENSION pg_stat_statements DROP VIEW pg_stat_statements
;
-- Test cases where a partition on one side matches multiple partitions on
-- the other side; we currently can't do partitioned join in such cases
ALTER TABLE prt2_adv DETACH PARTITION prt2_adv_p3
;
ALTER SEQUENCE sequence_test2 RESTART WITH -24 START WITH -24
  INCREMENT BY -4 MINVALUE -36 MAXVALUE -5 CYCLE
;
CREATE OR REPLACE FUNCTION mvtest_drop_the_index()
  RETURNS bool AS $$
BEGIN
  EXECUTE 'DROP INDEX IF EXISTS mvtest_drop_idx';
  RETURN true;
END;
$$ LANGUAGE plpgsql
;
-- an actual index of a partitioned table should work though
create index test_partition_idx on test_partition(a)
;
ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f4_excl1 EXCLUDE USING btree (f4 WITH =) WHERE ((f5 > 0))
;
-- should fail
alter table domain_test add column d str_domain2
;
-- Forbid ALTER TABLE when attaching or detaching an index to a partition.
create index idxpart_c on only idxpart (c)
;
DROP PUBLICATION "test_pub1"
;
CREATE INDEX reloptions_test_idx2 ON reloptions_test (s) WITH (fillfactor=1)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_index
;
-- cleanup.
drop view vw
;
ALTER INDEX pg_toast.pg_toast_28068_index RENAME TO reind_con_toast_idx
;
CREATE DATABASE "regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ" WITH TEMPLATE = template0 OID = 16385 STRATEGY = FILE_COPY ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
ANALYZE plt2
;
CREATE TABLESPACE regress_tblspacewith LOCATION '/src/src/bin/pg_upgrade/tmp_check/regress/testtablespace' WITH (some_nonexistent_parameter = true)
;
ALTER INDEX pg_toast.pg_toast_30817_index RENAME TO reind_con_toast_idx
;
CREATE TABLE collate_test2 (
    a int,
    b text COLLATE "POSIX"
)
;
DROP TABLE somechange
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_index
;
CREATE TYPE tablefunc_crosstab_4 AS
(
	row_name TEXT,
	category_1 TEXT,
	category_2 TEXT,
	category_3 TEXT,
	category_4 TEXT
)
;
ANALYZE public.prt4_n_p3
;
ALTER TABLE plt1_adv DETACH PARTITION plt1_adv_p1
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
CREATE VIEW rec1v WITH (security_barrier) AS SELECT * FROM rec1
;
alter table pgbench_tellers add constraint pgbench_tellers_bid_fkey foreign key (bid) references pgbench_branches
;
drop table pktable1, pktable2, fktable2
;
create or replace function part_hashint4_noop(value int4, seed int8)
returns int8 as $$
select value + seed;
$$ language sql immutable
;
DROP INDEX CONCURRENTLY IF EXISTS "concur_index3"
;
ALTER TABLE pg_toast.pg_toast_33024 RENAME TO reind_con_toast
;
ALTER TEXT SEARCH CONFIGURATION italian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH italian_stem
;
CREATE FUNCTION gbt_text_union(internal, internal)
RETURNS gbtreekey_var
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE INDEX jidx ON testjsonb USING gin (j jsonb_path_ops)
;
CREATE TABLE notnull_tbl1 (a int)
;
CREATE FUNCTION hstore_cmp(hstore,hstore)
RETURNS integer
AS '$libdir/hstore','hstore_cmp'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP TABLE regress_roleoption.t3
;
CREATE FUNCTION mylt_plpgsql (text, text) RETURNS boolean LANGUAGE plpgsql
    AS $$ begin return $1 < $2; end $$
;
ALTER OPERATOR FAMILY gist_timetz_ops USING gist ADD
	OPERATOR	6	<> (timetz, timetz)
;
CREATE OPERATOR <@ (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_contained,
	COMMUTATOR = '@>',
	RESTRICT = contsel, JOIN = contjoinsel
)
;
create table boolvalues (value bool not null)
;
ALTER TABLE pg_database ADD UNIQUE USING INDEX pg_database_datname_index
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_cast
;
create function returnssetofholes() returns setof has_hole language plpgsql as
$$
declare r record;
  h has_hole;
begin
  return next h;
  r := (1,2);
  h := (3,4);
  return next r;
  return next h;
  return next row(5,6);
  return next row(7,8)::has_hole;
end$$
;
CREATE PUBLICATION pub_viaroot FOR TABLE tab2, tab2_1, tab3_1 WITH (publish_via_partition_root = true)
;
-- skip self-referencing foreign key
ALTER TABLE unlogged2 SET LOGGED
;
--- adding tables
CREATE SCHEMA pub_test
;
ALTER TABLE pg_inherits ADD PRIMARY KEY USING INDEX pg_inherits_relid_seqno_index
;
-- adding constraints that describe the desired partition constraint
-- (or more restrictive) will help skip the validation scan
CREATE TABLE part2 (
	a int NOT NULL CHECK (a = 1),
	b int NOT NULL CHECK (b >= 10 AND b < 18)
)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION test_resowner_leak()
	RETURNS pg_catalog.void
	AS '$libdir/test_resowner' LANGUAGE C
;
----------
[CREATE_TABLE] create table pktable1 (a int primary key)
;
----------
[ALTER_LIBRARY] /* contrib/pg_stat_statements/pg_stat_statements--1.7--1.8.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* First we have to remove them from the extension */
ALTER EXTENSION pg_stat_statements DROP VIEW pg_stat_statements
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Test cases where a partition on one side matches multiple partitions on
-- the other side; we currently can't do partitioned join in such cases
ALTER TABLE prt2_adv DETACH PARTITION prt2_adv_p3
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test2 RESTART WITH -24 START WITH -24
  INCREMENT BY -4 MINVALUE -36 MAXVALUE -5 CYCLE
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION mvtest_drop_the_index()
  RETURNS bool AS $$
BEGIN
  EXECUTE 'DROP INDEX IF EXISTS mvtest_drop_idx';
  RETURN true;
END;
$$ LANGUAGE plpgsql
;
----------
[ADD_INDEX] -- an actual index of a partitioned table should work though
create index test_partition_idx on test_partition(a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f4_excl1 EXCLUDE USING btree (f4 WITH =) WHERE ((f5 > 0))
;
----------
[ALTER_TABLE|ADD_COLUMN] -- should fail
alter table domain_test add column d str_domain2
;
----------
[ADD_INDEX] -- Forbid ALTER TABLE when attaching or detaching an index to a partition.
create index idxpart_c on only idxpart (c)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION "test_pub1"
;
----------
[ADD_INDEX] CREATE INDEX reloptions_test_idx2 ON reloptions_test (s) WITH (fillfactor=1)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_index
;
----------
[DROP_VIEW] -- cleanup.
drop view vw
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28068_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ" WITH TEMPLATE = template0 OID = 16385 STRATEGY = FILE_COPY ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[ADMIN_TABLE] ANALYZE plt2
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspacewith LOCATION '/src/src/bin/pg_upgrade/tmp_check/regress/testtablespace' WITH (some_nonexistent_parameter = true)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30817_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE collate_test2 (
    a int,
    b text COLLATE "POSIX"
)
;
----------
[DROP_TABLE] DROP TABLE somechange
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_index
;
----------
[CREATE_TYPE] CREATE TYPE tablefunc_crosstab_4 AS
(
	row_name TEXT,
	category_1 TEXT,
	category_2 TEXT,
	category_3 TEXT,
	category_4 TEXT
)
;
----------
[ADMIN_TABLE] ANALYZE public.prt4_n_p3
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt1_adv DETACH PARTITION plt1_adv_p1
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rec1v WITH (security_barrier) AS SELECT * FROM rec1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_tellers add constraint pgbench_tellers_bid_fkey foreign key (bid) references pgbench_branches
;
----------
[DROP_TABLE] drop table pktable1, pktable2, fktable2
;
----------
[CREATE_PROG_OBJ] create or replace function part_hashint4_noop(value int4, seed int8)
returns int8 as $$
select value + seed;
$$ language sql immutable
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY IF EXISTS "concur_index3"
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33024 RENAME TO reind_con_toast
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION italian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH italian_stem
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_text_union(internal, internal)
RETURNS gbtreekey_var
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADD_INDEX] CREATE INDEX jidx ON testjsonb USING gin (j jsonb_path_ops)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1 (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_cmp(hstore,hstore)
RETURNS integer
AS '$libdir/hstore','hstore_cmp'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE regress_roleoption.t3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION mylt_plpgsql (text, text) RETURNS boolean LANGUAGE plpgsql
    AS $$ begin return $1 < $2; end $$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timetz_ops USING gist ADD
	OPERATOR	6	<> (timetz, timetz)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <@ (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_contained,
	COMMUTATOR = '@>',
	RESTRICT = contsel, JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] create table boolvalues (value bool not null)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_database ADD UNIQUE USING INDEX pg_database_datname_index
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_cast
;
----------
[CREATE_PROG_OBJ] create function returnssetofholes() returns setof has_hole language plpgsql as
$$
declare r record;
  h has_hole;
begin
  return next h;
  r := (1,2);
  h := (3,4);
  return next r;
  return next h;
  return next row(5,6);
  return next row(7,8)::has_hole;
end$$
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_viaroot FOR TABLE tab2, tab2_1, tab3_1 WITH (publish_via_partition_root = true)
;
----------
[ALTER_TABLE] -- skip self-referencing foreign key
ALTER TABLE unlogged2 SET LOGGED
;
----------
[CREATE_SCHEMA] --- adding tables
CREATE SCHEMA pub_test
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_inherits ADD PRIMARY KEY USING INDEX pg_inherits_relid_seqno_index
;
----------
[CREATE_TABLE] -- adding constraints that describe the desired partition constraint
-- (or more restrictive) will help skip the validation scan
CREATE TABLE part2 (
	a int NOT NULL CHECK (a = 1),
	b int NOT NULL CHECK (b >= 10 AND b < 18)
)
;
