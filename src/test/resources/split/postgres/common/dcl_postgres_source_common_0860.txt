COPY generated_virtual_tests.gtest0 (a) TO stdout
;
EXECUTE dumpFunc('16467')
;
CREATE USER regress_cp_user1
;
-- ===================================================================
-- test for postgres_fdw_get_connections function with check_conn = true
-- ===================================================================

-- Disable debug_discard_caches in order to manage remote connections
SET debug_discard_caches TO '0'
;
ALTER TABLE public.spgist_box_tbl OWNER TO zyc
;
/* contrib/adminpack/adminpack--1.0--1.1.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


REVOKE EXECUTE ON FUNCTION pg_catalog.pg_logfile_rotate() FROM PUBLIC
;
LOCK TABLE public.rtest_t6 IN ACCESS SHARE MODE
;
EXECUTE dumpTableAttach('40481')
;
--
-- Clean up
--

DROP USER regress_alter_op_user
;
LOCK TABLE public.tbl_include_box IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[DATA_EXPORT] COPY generated_virtual_tests.gtest0 (a) TO stdout
;
----------
[UNSAFE] EXECUTE dumpFunc('16467')
;
----------
[CREATE_USER] CREATE USER regress_cp_user1
;
----------
[SESSION_SETTING_WRITE] -- ===================================================================
-- test for postgres_fdw_get_connections function with check_conn = true
-- ===================================================================

-- Disable debug_discard_caches in order to manage remote connections
SET debug_discard_caches TO '0'
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.spgist_box_tbl OWNER TO zyc
;
----------
[REVOKE] /* contrib/adminpack/adminpack--1.0--1.1.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


REVOKE EXECUTE ON FUNCTION pg_catalog.pg_logfile_rotate() FROM PUBLIC
;
----------
[SESSION_LOCK] LOCK TABLE public.rtest_t6 IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpTableAttach('40481')
;
----------
[DROP_USER] --
-- Clean up
--

DROP USER regress_alter_op_user
;
----------
[SESSION_LOCK] LOCK TABLE public.tbl_include_box IN ACCESS SHARE MODE
;
