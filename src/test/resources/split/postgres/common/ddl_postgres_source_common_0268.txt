ALTER TABLE ref ALTER CONSTRAINT ref_f1_f2_fkey_1
  DEFERRABLE INITIALLY DEFERRED
;
CREATE FUNCTION gbt_bool_same(gbtreekey2, gbtreekey2, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
truncate test2
;
CREATE FUNCTION gin_trgm_triconsistent(internal, int2, text, int4, internal, internal, internal)
RETURNS "char"
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- check that using any of these functions with unsupported relations will fail
create table test_partitioned (a int) partition by range (a)
;
-- Do an aggressive vacuum to prevent page-skipping.
VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) reloptions_test
;
-- Index 50 translations of the word "Mathematics"
CREATE TEMP TABLE mb (s text)
;
DROP TABLE pgss_parallel_tab
;
--test that we read consecutive LFs properly

CREATE TEMP TABLE testnl (a int, b text, c int)
;
CREATE TABLE quad_point_tbl AS
    SELECT point(unique1,unique2) AS p FROM tenk1
;
--
-- Cleanup
--
DROP TABLE enumtest_child
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_index
;
-- test EXECUTE USING
create function exc_using(int, text) returns int as $$
declare i int;
begin
  for i in execute 'select * from generate_series(1,$1)' using $1+1 loop
    raise notice '%', i;
  end loop;
  execute 'select $2 + $2*3 + length($1)' into i using $2,$1;
  return i;
end
$$ language plpgsql
;
CREATE TABLE "public"."renamecolumnchild" (
    "d" integer,
    "a" integer,
    "w" integer
)
;
ALTER TEXT SEARCH DICTIONARY intdict (REJECTLONG = 1)
;
ALTER INDEX pg_toast.pg_toast_28395_index RENAME TO reind_con_toast_idx
;
CREATE TABLE regress_pg_dump_schema.extdependtab (col1 integer primary key, col2 int)
;
CREATE FUNCTION cube_ge(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER OPERATOR CLASS alt_opc3 USING hash SET SCHEMA alt_nsp2
;
CREATE OR REPLACE FUNCTION test_ddl_deparse()
  RETURNS event_trigger LANGUAGE plpgsql AS
$$
DECLARE
	r record;
	r2 record;
	cmdtype text;
	objtype text;
	tag text;
BEGIN
	FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
	LOOP
		-- verify that tags match
		tag = public.get_command_tag(r.command);
		IF tag <> r.command_tag THEN
			RAISE NOTICE 'tag % doesn''t match %', tag, r.command_tag;
		END IF;

		-- log the operation
		cmdtype = public.get_command_type(r.command);
		IF cmdtype <> 'grant' THEN
			RAISE NOTICE 'DDL test: type %, tag %', cmdtype, tag;
		ELSE
			RAISE NOTICE 'DDL test: type %, object type %', cmdtype, r.object_type;
		END IF;

		-- if alter table, log more
		IF cmdtype = 'alter table' THEN
			FOR r2 IN SELECT *
						FROM public.get_altertable_subcmdinfo(r.command)
			LOOP
				RAISE NOTICE '  subcommand: type % desc %', r2.cmdtype, r2.objdesc;
			END LOOP;
		END IF;
	END LOOP;
END;
$$
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=11135 host=/tmp/Y6nGyQJdp_ dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
/* contrib/seg/seg--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR <= (seg, seg) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel
)
;
ALTER TABLE pg_temp_11.pg_temp_26722_2 ADD COLUMN newdata pg_temp_11.pg_temp_26722
;
-- ERROR
CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') PRIMARY KEY,
	c2 text OPTIONS (param2 'val2', param3 'val3'),
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
CREATE VIEW "public"."my_credit_card_secure" WITH ("security_barrier"='true') AS
 SELECT "l"."cid",
    "l"."name",
    "l"."tel",
    "l"."passwd",
    "r"."cnum",
    "r"."climit"
   FROM ("public"."customer" "l"
     JOIN "public"."credit_card" "r" USING ("cid"))
  WHERE ("l"."name" = CURRENT_USER)
;
CREATE AGGREGATE mysum2(anyelement,anyelement) (SFUNC = sum3,
  STYPE = anyelement, INITCOND = '0')
;
-- failed (no role membership)
ALTER FUNCTION alt_func3(int) SET SCHEMA alt_nsp2
;
--
-- Check handling of a constant-null CHECK constraint
--
create table cnullparent (f1 int)
;
ALTER OPERATOR FAMILY gist_bytea_ops USING gist ADD
	OPERATOR	6	<> (bytea, bytea) ,
	FUNCTION	9 (bytea, bytea) gbt_var_fetch (internal)
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
create table stable_qual_pruning (a timestamp) partition by range (a)
;
CREATE TABLE "public"."rule_merge2" (
    "a" integer,
    "b" "text"
)
;
DROP FUNCTION stats_test_func1()
;
create table pp1 (f1 int)
;
-- cannot combine serial and identity
CREATE TABLE itest_err_4 (a serial generated by default as identity)
;
-- must reject
alter index idxpart_a_idx attach partition idxpart2_a_idx
;
-- OK

ALTER TABLE tt0 OF tt_t0
;
ALTER TABLE atest6 DROP COLUMN three
;
CREATE PUBLICATION pub2 FOR ALL TABLES WITH (publish_generated_columns = none)
;
alter table parted_trigger_2 drop column drp
;
create table ab_a3 partition of ab for values in(3) partition by list (b)
;
CREATE AGGREGATE myaggn10a(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE INDEX people_names_partial ON people(firstname) WHERE (firstname = 'first_name_1')
;
CREATE SCHEMA fkpart9
  CREATE TABLE pk (a INT PRIMARY KEY) PARTITION BY RANGE (a)
  CREATE TABLE fk (
    fk_a INT REFERENCES pk(a) ON DELETE CASCADE
  )
  CREATE TABLE pk1 PARTITION OF pk FOR VALUES FROM (30) TO (50) PARTITION BY RANGE (a)
  CREATE TABLE pk11 PARTITION OF pk1 FOR VALUES FROM (30) TO (40)
;
CREATE TABLE mcv_lists_uuid (
    a UUID,
    b UUID,
    c UUID
)
WITH (autovacuum_enabled = off)
;
-- fail

drop function dfunc(varchar, numeric, date)
;
CREATE TABLE cminh() INHERITS (cmdata, cmdata3)
;
CREATE INDEX ON s2.t2(id)
;
ALTER TABLE pg_type ADD PRIMARY KEY USING INDEX pg_type_oid_index
;
--
--  Implicit and assignment type casts.
--

CREATE CAST (citext AS text)    WITHOUT FUNCTION AS IMPLICIT
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE ref ALTER CONSTRAINT ref_f1_f2_fkey_1
  DEFERRABLE INITIALLY DEFERRED
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bool_same(gbtreekey2, gbtreekey2, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[TRUNCATE_TABLE] truncate test2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_trgm_triconsistent(internal, int2, text, int4, internal, internal, internal)
RETURNS "char"
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] -- check that using any of these functions with unsupported relations will fail
create table test_partitioned (a int) partition by range (a)
;
----------
[ADMIN_TABLE] -- Do an aggressive vacuum to prevent page-skipping.
VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) reloptions_test
;
----------
[CREATE_TABLE] -- Index 50 translations of the word "Mathematics"
CREATE TEMP TABLE mb (s text)
;
----------
[DROP_TABLE] DROP TABLE pgss_parallel_tab
;
----------
[CREATE_TABLE] --test that we read consecutive LFs properly

CREATE TEMP TABLE testnl (a int, b text, c int)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE quad_point_tbl AS
    SELECT point(unique1,unique2) AS p FROM tenk1
;
----------
[DROP_TABLE] --
-- Cleanup
--
DROP TABLE enumtest_child
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_index
;
----------
[CREATE_PROG_OBJ] -- test EXECUTE USING
create function exc_using(int, text) returns int as $$
declare i int;
begin
  for i in execute 'select * from generate_series(1,$1)' using $1+1 loop
    raise notice '%', i;
  end loop;
  execute 'select $2 + $2*3 + length($1)' into i using $2,$1;
  return i;
end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."renamecolumnchild" (
    "d" integer,
    "a" integer,
    "w" integer
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY intdict (REJECTLONG = 1)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28395_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE regress_pg_dump_schema.extdependtab (col1 integer primary key, col2 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_ge(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR CLASS alt_opc3 USING hash SET SCHEMA alt_nsp2
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_ddl_deparse()
  RETURNS event_trigger LANGUAGE plpgsql AS
$$
DECLARE
	r record;
	r2 record;
	cmdtype text;
	objtype text;
	tag text;
BEGIN
	FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
	LOOP
		-- verify that tags match
		tag = public.get_command_tag(r.command);
		IF tag <> r.command_tag THEN
			RAISE NOTICE 'tag % doesn''t match %', tag, r.command_tag;
		END IF;

		-- log the operation
		cmdtype = public.get_command_type(r.command);
		IF cmdtype <> 'grant' THEN
			RAISE NOTICE 'DDL test: type %, tag %', cmdtype, tag;
		ELSE
			RAISE NOTICE 'DDL test: type %, object type %', cmdtype, r.object_type;
		END IF;

		-- if alter table, log more
		IF cmdtype = 'alter table' THEN
			FOR r2 IN SELECT *
						FROM public.get_altertable_subcmdinfo(r.command)
			LOOP
				RAISE NOTICE '  subcommand: type % desc %', r2.cmdtype, r2.objdesc;
			END LOOP;
		END IF;
	END LOOP;
END;
$$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=11135 host=/tmp/Y6nGyQJdp_ dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
----------
[ALTER_PROG_OBJ] /* contrib/seg/seg--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR <= (seg, seg) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26722_2 ADD COLUMN newdata pg_temp_11.pg_temp_26722
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') PRIMARY KEY,
	c2 text OPTIONS (param2 'val2', param3 'val3'),
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."my_credit_card_secure" WITH ("security_barrier"='true') AS
 SELECT "l"."cid",
    "l"."name",
    "l"."tel",
    "l"."passwd",
    "r"."cnum",
    "r"."climit"
   FROM ("public"."customer" "l"
     JOIN "public"."credit_card" "r" USING ("cid"))
  WHERE ("l"."name" = CURRENT_USER)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE mysum2(anyelement,anyelement) (SFUNC = sum3,
  STYPE = anyelement, INITCOND = '0')
;
----------
[ALTER_PROG_OBJ] -- failed (no role membership)
ALTER FUNCTION alt_func3(int) SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] --
-- Check handling of a constant-null CHECK constraint
--
create table cnullparent (f1 int)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bytea_ops USING gist ADD
	OPERATOR	6	<> (bytea, bytea) ,
	FUNCTION	9 (bytea, bytea) gbt_var_fetch (internal)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] create table stable_qual_pruning (a timestamp) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rule_merge2" (
    "a" integer,
    "b" "text"
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION stats_test_func1()
;
----------
[CREATE_TABLE] create table pp1 (f1 int)
;
----------
[CREATE_TABLE] -- cannot combine serial and identity
CREATE TABLE itest_err_4 (a serial generated by default as identity)
;
----------
[ALTER_INDEX] -- must reject
alter index idxpart_a_idx attach partition idxpart2_a_idx
;
----------
[ALTER_TABLE] -- OK

ALTER TABLE tt0 OF tt_t0
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE atest6 DROP COLUMN three
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub2 FOR ALL TABLES WITH (publish_generated_columns = none)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table parted_trigger_2 drop column drp
;
----------
[CREATE_TABLE] create table ab_a3 partition of ab for values in(3) partition by list (b)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn10a(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ADD_INDEX] CREATE INDEX people_names_partial ON people(firstname) WHERE (firstname = 'first_name_1')
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart9
  CREATE TABLE pk (a INT PRIMARY KEY) PARTITION BY RANGE (a)
  CREATE TABLE fk (
    fk_a INT REFERENCES pk(a) ON DELETE CASCADE
  )
  CREATE TABLE pk1 PARTITION OF pk FOR VALUES FROM (30) TO (50) PARTITION BY RANGE (a)
  CREATE TABLE pk11 PARTITION OF pk1 FOR VALUES FROM (30) TO (40)
;
----------
[CREATE_TABLE] CREATE TABLE mcv_lists_uuid (
    a UUID,
    b UUID,
    c UUID
)
WITH (autovacuum_enabled = off)
;
----------
[DROP_PROG_OBJ] -- fail

drop function dfunc(varchar, numeric, date)
;
----------
[CREATE_TABLE] CREATE TABLE cminh() INHERITS (cmdata, cmdata3)
;
----------
[ADD_INDEX] CREATE INDEX ON s2.t2(id)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_type ADD PRIMARY KEY USING INDEX pg_type_oid_index
;
----------
[CREATE_PROG_OBJ] --
--  Implicit and assignment type casts.
--

CREATE CAST (citext AS text)    WITHOUT FUNCTION AS IMPLICIT
;
