VACUUM (PARALLEL 0) pg_catalog.pg_publication_rel
;
ALTER TABLE list_parted2 DETACH PARTITION not_a_part
;
CREATE TABLE atest4 (a int)
;
DROP TABLE pg_temp_12.pg_temp_26420_2, pg_temp_12.pg_temp_26420
;
--
-- for internal portal (cursor) tests
--
CREATE TABLE iportaltest (
	i		int4,
	d		float4,
	p		polygon
)
;
CREATE PROCEDURE testns.priv_testproc(int) AS 'select 3' LANGUAGE sql
;
do $$
<<outerblock>>
begin
  <<innerblock>>
  begin
    <<moreinnerblock>>
    begin
      begin  -- unlabeled block
        exit innerblock;
        raise notice 'should not get here';
      end;
      raise notice 'should not get here, either';
    end;
    raise notice 'nor here';
  end;
  raise notice 'should get here';
end$$
;
ANALYZE public.gtest20
;
CREATE OR REPLACE FUNCTION pg_logical_slot_peek_changes(
    IN slot_name name, IN upto_lsn pg_lsn, IN upto_nchanges int, VARIADIC options text[] DEFAULT '{}',
    OUT lsn pg_lsn, OUT xid xid, OUT data text)
RETURNS SETOF RECORD
LANGUAGE INTERNAL
VOLATILE ROWS 1000 COST 1000
AS 'pg_logical_slot_peek_changes'
;
/* contrib/btree_gist/btree_gist--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
--
--
-- enum ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_enum_consistent(internal,anyenum,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE pg_temp_11.pg_temp_41153_2
;
CREATE OPERATOR CLASS alt_opc1 FOR TYPE uuid USING hash AS STORAGE uuid
;
DROP VIEW tid_view_fake_ctid
;
CREATE TRIGGER after_upd_b_stmt_trig AFTER UPDATE OF b ON main_table
FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func('after_upd_b_stmt')
;
alter table trigtest add column d integer default 42 not null
;
-- XMLPATH tests
CREATE TABLE xmldata(data xml)
;
--
-- Test unique null behavior
--
CREATE TABLE unique_tbl (i int, t text)
;
-- not supported
ALTER TABLE regtest_table_3 ENABLE RULE regtest_test_rule
;
--
-- Check matching of boolean index columns to WHERE conditions and sort keys
--

create temp table boolindex (b bool, i int, unique(b, i), junk float)
;
create temp table inh_temp_parent (a1 int)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_parameter_acl
;
TRUNCATE test_stat_tab
;
-- Make operator class for boxes using gist2
CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	-- don't need compress, decompress, or fetch functions
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
ALTER TABLE fk_notpartitioned_fk ADD CONSTRAINT fk_notpartitioned_fk_a_b_fkey
	FOREIGN KEY (a, b) REFERENCES fk_partitioned_pk NOT VALID
;
-- ************************************************************
-- * Describe the front of a patchfield slot
-- ************************************************************
create function pslot_slotlink_view(bpchar)
returns text as '
declare
    psrec	record;
    sltype	char(2);
    retval	text;
begin
    select into psrec * from PSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if psrec.slotlink = '''' then
        return ''-'';
    end if;
    sltype := substr(psrec.slotlink, 1, 2);
    if sltype = ''PS'' then
	retval := trim(psrec.slotlink) || '' -> '';
	return retval || pslot_backlink_view(psrec.slotlink);
    end if;
    if sltype = ''HS'' then
        retval := comment from Hub H, HSlot HS
			where HS.slotname = psrec.slotlink
			  and H.name = HS.hubname;
        retval := retval || '' slot '';
	retval := retval || slotno::text from HSlot
			where slotname = psrec.slotlink;
	return retval;
    end if;
    return psrec.slotlink;
end;
' language plpgsql
;
CREATE OR REPLACE VIEW split_view_probe.base_view (id, qty, note, doubled_qty) WITH (security_barrier = false) AS SELECT id, qty, note, qty * 2 FROM split_view_probe.base_table WHERE qty > 0 WITH CASCADED CHECK OPTION
;
ANALYZE pg_temp_4.pg_temp_27487_2
;
DROP TABLE statute, offense
;
CREATE PROCEDURE "public"."ptest3"(IN "y" "text")
    LANGUAGE "sql"
    AS $_$
CALL ptest1(y);
CALL ptest1($1);
$_$
;
create table idxpart (a int4range, exclude USING GIST (a with = )) partition by range (a)
;
CREATE OPERATOR FAMILY alt_opf2 USING hash
;
CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C.UTF-8')
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_attribute
;
CREATE TEMP TABLE a_temp_tbl ()
;
/*
 * 8. Update, delete values in indexed table.
 */
CREATE TABLE tbl (c1 int, c2 int, c3 int, c4 box)
;
ANALYZE pg_catalog.pg_proc
;
create table table_with_oids(a int)
;
drop table dcomptable
;
CREATE VIEW collation_character_set_applicability AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
drop function dfunc(text)
;
-- anyarray column

analyze rows
;
-- ===================================================================
-- create foreign tables
-- ===================================================================
CREATE FOREIGN TABLE ft1 (
	c0 int,
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10) default 'ft1',
	c8 user_enum
) SERVER loopback
;
CREATE SUBSCRIPTION tap_sub_B_A
	CONNECTION 'port=29890 host=/tmp/7Wj1Y48wnU dbname=postgres application_name=tap_sub_B_A'
	PUBLICATION tap_pub_A
	WITH (origin = none)
;
VACUUM (PARALLEL 0) information_schema.sql_features
;
drop trigger trigger_alpha on trigtest
;
CREATE FOREIGN TABLE tststats.f (a int, b int, c text) SERVER extstats_dummy_srv
;
DROP VIEW itestv10, itestv11
;
CREATE INDEX t2_brin ON s3.t2 USING BRIN (i)
;
CREATE TABLE tru_ptable (id int) PARTITION BY HASH(id)
;
drop table inserttestb
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_publication_rel
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 DETACH PARTITION not_a_part
;
----------
[CREATE_TABLE] CREATE TABLE atest4 (a int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_12.pg_temp_26420_2, pg_temp_12.pg_temp_26420
;
----------
[CREATE_TABLE] --
-- for internal portal (cursor) tests
--
CREATE TABLE iportaltest (
	i		int4,
	d		float4,
	p		polygon
)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE testns.priv_testproc(int) AS 'select 3' LANGUAGE sql
;
----------
[BLOCK] do $$
<<outerblock>>
begin
  <<innerblock>>
  begin
    <<moreinnerblock>>
    begin
      begin  -- unlabeled block
        exit innerblock;
        raise notice 'should not get here';
      end;
      raise notice 'should not get here, either';
    end;
    raise notice 'nor here';
  end;
  raise notice 'should get here';
end$$
;
----------
[ADMIN_TABLE] ANALYZE public.gtest20
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_logical_slot_peek_changes(
    IN slot_name name, IN upto_lsn pg_lsn, IN upto_nchanges int, VARIADIC options text[] DEFAULT '{}',
    OUT lsn pg_lsn, OUT xid xid, OUT data text)
RETURNS SETOF RECORD
LANGUAGE INTERNAL
VOLATILE ROWS 1000 COST 1000
AS 'pg_logical_slot_peek_changes'
;
----------
[CREATE_PROG_OBJ] /* contrib/btree_gist/btree_gist--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
--
--
-- enum ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_enum_consistent(internal,anyenum,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41153_2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS alt_opc1 FOR TYPE uuid USING hash AS STORAGE uuid
;
----------
[DROP_VIEW] DROP VIEW tid_view_fake_ctid
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER after_upd_b_stmt_trig AFTER UPDATE OF b ON main_table
FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func('after_upd_b_stmt')
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table trigtest add column d integer default 42 not null
;
----------
[CREATE_TABLE] -- XMLPATH tests
CREATE TABLE xmldata(data xml)
;
----------
[CREATE_TABLE] --
-- Test unique null behavior
--
CREATE TABLE unique_tbl (i int, t text)
;
----------
[ALTER_TABLE] -- not supported
ALTER TABLE regtest_table_3 ENABLE RULE regtest_test_rule
;
----------
[CREATE_TABLE] --
-- Check matching of boolean index columns to WHERE conditions and sort keys
--

create temp table boolindex (b bool, i int, unique(b, i), junk float)
;
----------
[CREATE_TABLE] create temp table inh_temp_parent (a1 int)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_parameter_acl
;
----------
[TRUNCATE_TABLE] TRUNCATE test_stat_tab
;
----------
[CREATE_PROG_OBJ] -- Make operator class for boxes using gist2
CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	-- don't need compress, decompress, or fetch functions
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_notpartitioned_fk ADD CONSTRAINT fk_notpartitioned_fk_a_b_fkey
	FOREIGN KEY (a, b) REFERENCES fk_partitioned_pk NOT VALID
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * Describe the front of a patchfield slot
-- ************************************************************
create function pslot_slotlink_view(bpchar)
returns text as '
declare
    psrec	record;
    sltype	char(2);
    retval	text;
begin
    select into psrec * from PSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if psrec.slotlink = '''' then
        return ''-'';
    end if;
    sltype := substr(psrec.slotlink, 1, 2);
    if sltype = ''PS'' then
	retval := trim(psrec.slotlink) || '' -> '';
	return retval || pslot_backlink_view(psrec.slotlink);
    end if;
    if sltype = ''HS'' then
        retval := comment from Hub H, HSlot HS
			where HS.slotname = psrec.slotlink
			  and H.name = HS.hubname;
        retval := retval || '' slot '';
	retval := retval || slotno::text from HSlot
			where slotname = psrec.slotlink;
	return retval;
    end if;
    return psrec.slotlink;
end;
' language plpgsql
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW split_view_probe.base_view (id, qty, note, doubled_qty) WITH (security_barrier = false) AS SELECT id, qty, note, qty * 2 FROM split_view_probe.base_table WHERE qty > 0 WITH CASCADED CHECK OPTION
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_27487_2
;
----------
[DROP_TABLE] DROP TABLE statute, offense
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest3"(IN "y" "text")
    LANGUAGE "sql"
    AS $_$
CALL ptest1(y);
CALL ptest1($1);
$_$
;
----------
[CREATE_TABLE] create table idxpart (a int4range, exclude USING GIST (a with = )) partition by range (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf2 USING hash
;
----------
[CREATE_POLICY] CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C.UTF-8')
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_attribute
;
----------
[CREATE_TABLE] CREATE TEMP TABLE a_temp_tbl ()
;
----------
[CREATE_TABLE] /*
 * 8. Update, delete values in indexed table.
 */
CREATE TABLE tbl (c1 int, c2 int, c3 int, c4 box)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_proc
;
----------
[CREATE_TABLE] create table table_with_oids(a int)
;
----------
[DROP_TABLE] drop table dcomptable
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW collation_character_set_applicability AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
----------
[DROP_PROG_OBJ] drop function dfunc(text)
;
----------
[ADMIN_TABLE] -- anyarray column

analyze rows
;
----------
[CREATE_TABLE] -- ===================================================================
-- create foreign tables
-- ===================================================================
CREATE FOREIGN TABLE ft1 (
	c0 int,
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10) default 'ft1',
	c8 user_enum
) SERVER loopback
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_A
	CONNECTION 'port=29890 host=/tmp/7Wj1Y48wnU dbname=postgres application_name=tap_sub_B_A'
	PUBLICATION tap_pub_A
	WITH (origin = none)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) information_schema.sql_features
;
----------
[DROP_TRIGGER] drop trigger trigger_alpha on trigtest
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tststats.f (a int, b int, c text) SERVER extstats_dummy_srv
;
----------
[DROP_VIEW] DROP VIEW itestv10, itestv11
;
----------
[ADD_INDEX] CREATE INDEX t2_brin ON s3.t2 USING BRIN (i)
;
----------
[CREATE_TABLE] CREATE TABLE tru_ptable (id int) PARTITION BY HASH(id)
;
----------
[DROP_TABLE] drop table inserttestb
;
