UPDATE transition_table_level1 SET level1_no = -1
  WHERE level1_no = 30
;
UPDATE rw_view1 SET aa=aa, bb=bb
;
insert into HSlot (
		    slotname,
		    hubname,
		    slotno,
		    slotlink
		) values (
		    new.slotname,
		    new.hubname,
		    new.slotno,
		    new.slotlink
		)
;
INSERT INTO d(aa) VALUES('ddddddd')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '5447', relminmxid = '1'
WHERE oid = '"public"."invalid_check_con"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (2, 132)
;
INSERT INTO public.tplain VALUES ('2', 272)
;
INSERT INTO rls_test_both VALUES ('regress_r1','regress_s1',10)
;
UPDATE ONLY "pg_temp_14"."tasks" SET "checked_by" = NULL WHERE $1 OPERATOR(pg_catalog.=) "checked_by"
;
-- now we want to change pric_id in ALL tuples
-- this gets us not what we need
update tttest set price_id = 5 where price_id = 3
;
INSERT INTO trunc_stats_test DEFAULT VALUES
;
UPDATE pgbench_accounts SET abalance = abalance + -3761 WHERE aid = 70674
;
INSERT INTO ctv_data SELECT 1, x, '*' || x FROM generate_series(1,10) x
;
INSERT INTO result_tbl
(SELECT a, b, 'AAA' || c FROM async_p1 ORDER BY a LIMIT 10)
UNION ALL
(SELECT a, b, 'AAA' || c FROM async_p2 WHERE b < 10)
;
UPDATE pgbench_accounts SET abalance = abalance + 3123 WHERE aid = 9113
;
insert into at_partitioned values(1, 'foo')
;
INSERT INTO caster (circle)        VALUES ('((0,0),2)'::text)
;
INSERT INTO inhg (xx, yy, x) VALUES ('test', 5, 10)
;
-- Fill the gaps logic
delete from test_jsonb_subscript
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."renamecolumnchild"'::pg_catalog.regclass
  AND attname IN ('d', 'w')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."mlparted2"'::pg_catalog.regclass
;
delete from utrtest
  returning *, tableoid::regclass, xmax = pg_current_xact_id()::xid as xmax_ok
;
INSERT INTO public.tplain VALUES ('4', 474)
;
INSERT INTO public.tplain VALUES ('4', 214)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14565', relminmxid = '3'
WHERE oid = '"public"."truncate_b"'::pg_catalog.regclass
;
-- fail
insert into dcomptable (d1[1].r) values(99)
;
insert into insertconflict values (1, 2)
on conflict (coalesce(a, 0)) do nothing
;
INSERT INTO t values(0)
;
update trigtest set a = 2 where a = 1 returning *
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3071', relminmxid = '1'
WHERE oid = '"public"."tbl_include_reg"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17386', relminmxid = '3'
WHERE oid = '39677'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11548', relminmxid = '3'
WHERE oid = '30209'
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."covidxpart2"'::pg_catalog.regclass
;
INSERT INTO INT2_TBL(f1) VALUES ('asdf')
;
UPDATE tab2 SET b = 'xyz' where a = 2
;
insert into anothertab (atcol1, atcol2) values (45, null)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."mlparted4"'::pg_catalog.regclass
;
INSERT INTO CIRCLE_TBL VALUES ('<(3,5),0>')
;
insert into rtest_t3 values (1, 31)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1038', relminmxid = '1'
WHERE oid = '"public"."polygon_tbl"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."beta_neg"'::pg_catalog.regclass
;
delete from rtest_nothn3
;
INSERT INTO TIMESTAMP_TBL VALUES ('19970210 173201 -0800')
;
UPDATE rows SET x = NULL WHERE x = 1
;
INSERT INTO public.ploc2(f1, f2) VALUES ($1, $2)
;
INSERT INTO covering_index_heap VALUES(1,2,'BBB')
;
INSERT INTO trunc_e VALUES (1,1)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13847', relminmxid = '3'
WHERE oid = '34808'
;
INSERT INTO caster (date)          VALUES ('1999-01-08'::text)
;
INSERT INTO public.ths_p1 VALUES (2, 62)
;
------------------------------------------------------------------------------------------
[UPDATE] UPDATE transition_table_level1 SET level1_no = -1
  WHERE level1_no = 30
;
----------
[UPDATE] UPDATE rw_view1 SET aa=aa, bb=bb
;
----------
[INSERT|SELECT] insert into HSlot (
		    slotname,
		    hubname,
		    slotno,
		    slotlink
		) values (
		    new.slotname,
		    new.hubname,
		    new.slotno,
		    new.slotlink
		)
;
----------
[INSERT|SELECT] INSERT INTO d(aa) VALUES('ddddddd')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '5447', relminmxid = '1'
WHERE oid = '"public"."invalid_check_con"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 132)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 272)
;
----------
[INSERT] INSERT INTO rls_test_both VALUES ('regress_r1','regress_s1',10)
;
----------
[UPDATE] UPDATE ONLY "pg_temp_14"."tasks" SET "checked_by" = NULL WHERE $1 OPERATOR(pg_catalog.=) "checked_by"
;
----------
[UPDATE] -- now we want to change pric_id in ALL tuples
-- this gets us not what we need
update tttest set price_id = 5 where price_id = 3
;
----------
[INSERT] INSERT INTO trunc_stats_test DEFAULT VALUES
;
----------
[UPDATE] UPDATE pgbench_accounts SET abalance = abalance + -3761 WHERE aid = 70674
;
----------
[INSERT|SELECT] INSERT INTO ctv_data SELECT 1, x, '*' || x FROM generate_series(1,10) x
;
----------
[INSERT|SELECT] INSERT INTO result_tbl
(SELECT a, b, 'AAA' || c FROM async_p1 ORDER BY a LIMIT 10)
UNION ALL
(SELECT a, b, 'AAA' || c FROM async_p2 WHERE b < 10)
;
----------
[UPDATE] UPDATE pgbench_accounts SET abalance = abalance + 3123 WHERE aid = 9113
;
----------
[INSERT] insert into at_partitioned values(1, 'foo')
;
----------
[INSERT|SELECT] INSERT INTO caster (circle)        VALUES ('((0,0),2)'::text)
;
----------
[INSERT|SELECT] INSERT INTO inhg (xx, yy, x) VALUES ('test', 5, 10)
;
----------
[DELETE] -- Fill the gaps logic
delete from test_jsonb_subscript
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."renamecolumnchild"'::pg_catalog.regclass
  AND attname IN ('d', 'w')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."mlparted2"'::pg_catalog.regclass
;
----------
[DELETE] delete from utrtest
  returning *, tableoid::regclass, xmax = pg_current_xact_id()::xid as xmax_ok
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 474)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 214)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14565', relminmxid = '3'
WHERE oid = '"public"."truncate_b"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- fail
insert into dcomptable (d1[1].r) values(99)
;
----------
[MERGE] insert into insertconflict values (1, 2)
on conflict (coalesce(a, 0)) do nothing
;
----------
[INSERT] INSERT INTO t values(0)
;
----------
[UPDATE] update trigtest set a = 2 where a = 1 returning *
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3071', relminmxid = '1'
WHERE oid = '"public"."tbl_include_reg"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17386', relminmxid = '3'
WHERE oid = '39677'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11548', relminmxid = '3'
WHERE oid = '30209'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."covidxpart2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO INT2_TBL(f1) VALUES ('asdf')
;
----------
[UPDATE] UPDATE tab2 SET b = 'xyz' where a = 2
;
----------
[INSERT|SELECT] insert into anothertab (atcol1, atcol2) values (45, null)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."mlparted4"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO CIRCLE_TBL VALUES ('<(3,5),0>')
;
----------
[INSERT] insert into rtest_t3 values (1, 31)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1038', relminmxid = '1'
WHERE oid = '"public"."polygon_tbl"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."beta_neg"'::pg_catalog.regclass
;
----------
[DELETE] delete from rtest_nothn3
;
----------
[INSERT] INSERT INTO TIMESTAMP_TBL VALUES ('19970210 173201 -0800')
;
----------
[UPDATE] UPDATE rows SET x = NULL WHERE x = 1
;
----------
[INSERT|SELECT] INSERT INTO public.ploc2(f1, f2) VALUES ($1, $2)
;
----------
[INSERT] INSERT INTO covering_index_heap VALUES(1,2,'BBB')
;
----------
[INSERT] INSERT INTO trunc_e VALUES (1,1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13847', relminmxid = '3'
WHERE oid = '34808'
;
----------
[INSERT|SELECT] INSERT INTO caster (date)          VALUES ('1999-01-08'::text)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 62)
;
