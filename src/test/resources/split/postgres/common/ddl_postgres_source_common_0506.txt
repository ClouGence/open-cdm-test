CREATE AGGREGATE testagg2(int2) (SFUNC = int2_sum, STYPE = int8)
;
ALTER TABLE ONLY "public"."rules_log" ALTER COLUMN "id" SET DEFAULT "nextval"('"public"."rules_log_id_seq"'::"regclass")
;
create rule v1_upd_rule as on update to v1
  do also insert into log values (new.*)
;
CALL ptest2()
;
create index ��ג�������ђ��index1 on ��ג�������ђ�� using btree (��ђ��)
;
CREATE OPERATOR CLASS dump_test.op_class
		                 FOR TYPE bigint USING btree FAMILY dump_test.op_family
						 AS STORAGE bigint,
						 OPERATOR 1 <(bigint,bigint),
						 OPERATOR 2 <=(bigint,bigint),
						 OPERATOR 3 =(bigint,bigint),
						 OPERATOR 4 >=(bigint,bigint),
						 OPERATOR 5 >(bigint,bigint),
						 FUNCTION 1 btint8cmp(bigint,bigint),
						 FUNCTION 2 btint8sortsupport(internal)
;
CREATE TABLE "fkpart6"."pk1" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
PARTITION BY RANGE ("a")
;
ALTER TABLE regtest_table_part ALTER p DROP NOT NULL
;
ALTER TABLE ONLY "public"."pagg_tab_ml" ATTACH PARTITION "public"."pagg_tab_ml_p2" FOR VALUES FROM (10) TO (20)
;
ALTER INDEX pg_toast.pg_toast_28187_index RENAME TO reind_con_toast_idx
;
CREATE TABLE test_tab3 (a int)
;
DROP TRIGGER row_before_insupd_trigger ON child_tbl
;
CREATE EXTENSION test_ext_extschema SCHEMA has$dollar
;
VACUUM (ANALYZE) pg_catalog.pg_tablespace
;
CALL ptest6c(1.1, null)
;
-- 2 columns (2 tables), mismatched types
create table pktable_base(base1 int not null)
;
ALTER TABLE tbl ALTER c1 TYPE bigint
;
CREATE AGGREGATE "public"."myaggn03a"(*) (
    SFUNC = "public"."stfp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
CREATE TRIGGER invalid_trig INSTEAD OF INSERT ON main_table
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_ins')
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=27859 host=/tmp/RNRzVZCEDw dbname=postgres' PUBLICATION mypub
;
ALTER TABLE ONLY "public"."nv_child_2009" INHERIT "public"."nv_parent"
;
-- check that the partition being detached is actually a partition of the parent
CREATE TABLE not_a_part (a int)
;
alter type comptype drop attribute i
;
alter table fkest1
  add constraint fkest1_a_b_fkey foreign key (a,b) references fkest
;
CREATE TABLE "generated_stored_tests"."gtest0" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (55) STORED
)
;
CREATE OPERATOR "public".|@| (
    FUNCTION = "unnest",
    RIGHTARG = "anyarray"
)
;
-- not supported by sepgsql
ALTER TABLE regtest_ptable DROP CONSTRAINT test_ck
;
CREATE TABLE fk_partitioned_fk_2_1 PARTITION OF fk_partitioned_fk_2 FOR VALUES FROM (0) TO (1000)
;
CREATE FUNCTION _int_different(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table other_partitioned_fk_1 partition of other_partitioned_fk
  for values in (2048)
;
CREATE VIEW "public"."mvtest_tvv" AS
 SELECT "sum"("totamt") AS "grandtot"
   FROM "public"."mvtest_tv"
;
create table convslot_test_part (val int, id int not null)
;
-- should fail, can't have event triggers on global objects
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('CREATE ROLE')
   execute procedure test_event_trigger()
;
DROP TABLE pg_temp_12.pg_temp_25379_2, pg_temp_12.pg_temp_25379
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_description
;
CREATE TABLE gtest20b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED)
;
create function recurse(float8) returns float8 as
$$
begin
  if ($1 > 0) then
    return sql_recurse($1 - 1);
  else
    return $1;
  end if;
end;
$$ language plpgsql
;
DROP TABLE CHAR_TBL
;
create view tt24v as
with cte as materialized (select r from (values(1,2),(3,4)) r)
select (r).column2 as col_a, (rr).column2 as col_b from
  cte join (select rr from (values(1,7),(3,8)) rr limit 2) ss
  on (r).column1 = (rr).column1
;
CREATE FUNCTION "public"."tg_hslot_bd"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    hubrec	record;
begin
    select into hubrec * from Hub where name = old.hubname;
    if not found then
        return old;
    end if;
    if old.slotno > hubrec.nslots then
        return old;
    end if;
    raise exception 'no manual manipulation of HSlot';
end;
$$
;
CREATE MATERIALIZED VIEW ptif_test_matview AS SELECT 1
;
DROP TABLE target_pa
;
-- check handling of types as extension members
create extension test_ext9
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext
;
CREATE TABLE owned_by_me (
	a int
) PARTITION BY LIST (a)
;
CREATE TABLE "public"."real_city" (
    "pop" integer,
    "cname" "text",
    "outline" "path"
)
;
ALTER TABLE atnotnull1
  ADD COLUMN c INT,
  ADD PRIMARY KEY (c)
;
DROP TABLE inh_temp_child_s2
;
ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg4
;
CREATE TABLE pgss_test (a int, b char(20))
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE AGGREGATE testagg2(int2) (SFUNC = int2_sum, STYPE = int8)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."rules_log" ALTER COLUMN "id" SET DEFAULT "nextval"('"public"."rules_log_id_seq"'::"regclass")
;
----------
[CREATE_POLICY] create rule v1_upd_rule as on update to v1
  do also insert into log values (new.*)
;
----------
[CALL_PROG_OBJ] CALL ptest2()
;
----------
[ADD_INDEX] create index ��ג�������ђ��index1 on ��ג�������ђ�� using btree (��ђ��)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS dump_test.op_class
		                 FOR TYPE bigint USING btree FAMILY dump_test.op_family
						 AS STORAGE bigint,
						 OPERATOR 1 <(bigint,bigint),
						 OPERATOR 2 <=(bigint,bigint),
						 OPERATOR 3 =(bigint,bigint),
						 OPERATOR 4 >=(bigint,bigint),
						 OPERATOR 5 >(bigint,bigint),
						 FUNCTION 1 btint8cmp(bigint,bigint),
						 FUNCTION 2 btint8sortsupport(internal)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart6"."pk1" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
PARTITION BY RANGE ("a")
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_table_part ALTER p DROP NOT NULL
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml" ATTACH PARTITION "public"."pagg_tab_ml_p2" FOR VALUES FROM (10) TO (20)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28187_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE test_tab3 (a int)
;
----------
[DROP_TRIGGER] DROP TRIGGER row_before_insupd_trigger ON child_tbl
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext_extschema SCHEMA has$dollar
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_tablespace
;
----------
[CALL_PROG_OBJ] CALL ptest6c(1.1, null)
;
----------
[CREATE_TABLE] -- 2 columns (2 tables), mismatched types
create table pktable_base(base1 int not null)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl ALTER c1 TYPE bigint
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn03a"(*) (
    SFUNC = "public"."stfp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig INSTEAD OF INSERT ON main_table
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_ins')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=27859 host=/tmp/RNRzVZCEDw dbname=postgres' PUBLICATION mypub
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."nv_child_2009" INHERIT "public"."nv_parent"
;
----------
[CREATE_TABLE] -- check that the partition being detached is actually a partition of the parent
CREATE TABLE not_a_part (a int)
;
----------
[ALTER_TYPE] alter type comptype drop attribute i
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table fkest1
  add constraint fkest1_a_b_fkey foreign key (a,b) references fkest
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest0" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (55) STORED
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".|@| (
    FUNCTION = "unnest",
    RIGHTARG = "anyarray"
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- not supported by sepgsql
ALTER TABLE regtest_ptable DROP CONSTRAINT test_ck
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_2_1 PARTITION OF fk_partitioned_fk_2 FOR VALUES FROM (0) TO (1000)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_different(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table other_partitioned_fk_1 partition of other_partitioned_fk
  for values in (2048)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."mvtest_tvv" AS
 SELECT "sum"("totamt") AS "grandtot"
   FROM "public"."mvtest_tv"
;
----------
[CREATE_TABLE] create table convslot_test_part (val int, id int not null)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should fail, can't have event triggers on global objects
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('CREATE ROLE')
   execute procedure test_event_trigger()
;
----------
[DROP_TABLE] DROP TABLE pg_temp_12.pg_temp_25379_2, pg_temp_12.pg_temp_25379
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_description
;
----------
[CREATE_TABLE] CREATE TABLE gtest20b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_PROG_OBJ] create function recurse(float8) returns float8 as
$$
begin
  if ($1 > 0) then
    return sql_recurse($1 - 1);
  else
    return $1;
  end if;
end;
$$ language plpgsql
;
----------
[DROP_TABLE] DROP TABLE CHAR_TBL
;
----------
[CREATE_VIEW(SELECT)] create view tt24v as
with cte as materialized (select r from (values(1,2),(3,4)) r)
select (r).column2 as col_a, (rr).column2 as col_b from
  cte join (select rr from (values(1,7),(3,8)) rr limit 2) ss
  on (r).column1 = (rr).column1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_hslot_bd"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    hubrec	record;
begin
    select into hubrec * from Hub where name = old.hubname;
    if not found then
        return old;
    end if;
    if old.slotno > hubrec.nslots then
        return old;
    end if;
    raise exception 'no manual manipulation of HSlot';
end;
$$
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW ptif_test_matview AS SELECT 1
;
----------
[DROP_TABLE] DROP TABLE target_pa
;
----------
[CREATE_LIBRARY] -- check handling of types as extension members
create extension test_ext9
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext
;
----------
[CREATE_TABLE] CREATE TABLE owned_by_me (
	a int
) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."real_city" (
    "pop" integer,
    "cname" "text",
    "outline" "path"
)
;
----------
[ALTER_TABLE|ADD_COLUMN|ADD_CONSTRAINT] ALTER TABLE atnotnull1
  ADD COLUMN c INT,
  ADD PRIMARY KEY (c)
;
----------
[DROP_TABLE] DROP TABLE inh_temp_child_s2
;
----------
[RENAME_PROG_OBJ] ALTER AGGREGATE alt_agg1(int) RENAME TO alt_agg4
;
----------
[CREATE_TABLE] CREATE TABLE pgss_test (a int, b char(20))
;
