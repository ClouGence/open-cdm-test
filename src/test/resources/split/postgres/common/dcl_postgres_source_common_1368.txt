-- test that INT_MIN number is formatted properly
SET IntervalStyle to postgres
;
EXECUTE dumpFunc('35947')
;
--
-- Tests for things affected by allow_system_table_mods
--
-- We run the same set of commands once with allow_system_table_mods
-- off and then again with on.
--
-- The "on" tests should where possible be wrapped in BEGIN/ROLLBACK
-- blocks so as to not leave a mess around.

CREATE USER regress_user_ast
;
CREATE ROLE regress_publication_user2
;
create role regress_other_partitioned_fk_owner
;
DROP SERVER s4
;
LOCK TABLE public.part_p2 IN ACCESS SHARE MODE
;
EXECUTE dumpDomain('30395')
;
ALTER TABLE public.stats_test_tab1 OWNER TO zyc
;
EXECUTE dumpFunc('35042')
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] -- test that INT_MIN number is formatted properly
SET IntervalStyle to postgres
;
----------
[UNSAFE] EXECUTE dumpFunc('35947')
;
----------
[CREATE_USER] --
-- Tests for things affected by allow_system_table_mods
--
-- We run the same set of commands once with allow_system_table_mods
-- off and then again with on.
--
-- The "on" tests should where possible be wrapped in BEGIN/ROLLBACK
-- blocks so as to not leave a mess around.

CREATE USER regress_user_ast
;
----------
[CREATE_ROLE] CREATE ROLE regress_publication_user2
;
----------
[CREATE_ROLE] create role regress_other_partitioned_fk_owner
;
----------
[SYSTEM_SETTING_WRITE] DROP SERVER s4
;
----------
[SESSION_LOCK] LOCK TABLE public.part_p2 IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpDomain('30395')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.stats_test_tab1 OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpFunc('35042')
;
