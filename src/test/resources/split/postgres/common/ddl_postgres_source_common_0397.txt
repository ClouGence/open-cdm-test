CREATE CONSTRAINT TRIGGER accounts_balance_constraint
    AFTER INSERT OR UPDATE ON split_trigger_probe.accounts
    DEFERRABLE INITIALLY DEFERRED
    FOR EACH ROW
    WHEN (NEW.balance >= 0)
    EXECUTE FUNCTION split_trigger_probe.audit_row('constraint_balance')
;
-- drop system table
DROP TABLE pg_description
;
ALTER TABLE part_2 DROP CONSTRAINT check_a2
;
ALTER TABLE T ADD COLUMN c_date DATE DEFAULT '2016-06-02',
              ALTER COLUMN c_text SET DEFAULT 'cat'
;
CREATE TABLE temporal_mltrng3 (
  id int4range,
  valid_at datemultirange
)
;
CREATE TRIGGER gtest12_02 BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func4()
;
CREATE TEMP TABLE pg_temp_4.pg_temp_27487_2 (tid pg_catalog.tid)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_extension
;
CREATE INDEX oididx ON oidtmp USING gist ( oid )
;
create table parted_trigger (a int, b text) partition by range (a)
;
CREATE TABLE ref_table (id integer PRIMARY KEY)
;
CREATE TABLE list_part1(a numeric, b int, c int8)
;
CREATE TEMP TABLE pg_temp_19.pg_temp_33778_2 (tid pg_catalog.tid)
;
CREATE VIEW pg_stat_checkpointer AS
    SELECT
        pg_stat_get_checkpointer_num_timed() AS num_timed,
        pg_stat_get_checkpointer_num_requested() AS num_requested,
        pg_stat_get_checkpointer_restartpoints_timed() AS restartpoints_timed,
        pg_stat_get_checkpointer_restartpoints_requested() AS restartpoints_req,
        pg_stat_get_checkpointer_restartpoints_performed() AS restartpoints_done,
        pg_stat_get_checkpointer_write_time() AS write_time,
        pg_stat_get_checkpointer_sync_time() AS sync_time,
        pg_stat_get_checkpointer_buffers_written() AS buffers_written,
        pg_stat_get_checkpointer_stat_reset_time() AS stats_reset
;
alter table recur1 add column f2 int
;
CREATE FUNCTION "public"."reverse_name"("name") RETURNS "name"
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'reverse_name'
;
CREATE TABLE test_type_diff2_c2 (int_eight int8, int_two int2, int_four int4)
;
/*
 * text search configuration for russian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * russian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
CREATE EVENT TRIGGER regress_event_trigger_report_end ON ddl_command_end
  EXECUTE PROCEDURE event_trigger_report_end()
;
CREATE TABLE "public"."gtest1" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
-- Test proper handling of parameterized appendrel paths when the
-- potential join qual is expensive
create function expensivefunc(int) returns int
language plpgsql immutable strict cost 10000
as $$begin return $1; end$$
;
-- Test single- and multi-row inserts with table and view defaults.
-- Table defaults should be used, unless overridden by view defaults.
create table base_tab_def (a int, b text default 'Table default',
                           c text default 'Table default', d text, e text)
;
create table rules_fooview_child () inherits (rules_fooview)
;
CREATE TABLE test_tab (a int primary key, b text, c INT, d INT, e INT)
;
CREATE TABLE prt_p1 PARTITION OF prt FOR VALUES FROM (0) TO (10)
;
ALTER TABLE regtest_table ALTER b SET (n_distinct = 999)
;
DO LANGUAGE plpgsql $$
BEGIN
  EXECUTE 'COPY copy1 TO stdout';
END;
$$
;
-- error
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS EXECUTE ctas_ine_query
;
CREATE SCHEMA split_select_lock
;
ANALYZE public.renamecolumnchild
;
CREATE TABLE fk (id int, a int DEFAULT 50) PARTITION BY RANGE (a)
;
create table dcomptable (d1 dcomptypea unique)
;
DROP TABLE rls_test_both
;
CREATE DATABASE tbuiltin4 TEMPLATE template0 LOCALE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
CREATE TABLE stats_import.test_clone ( LIKE stats_import.test )
    WITH (autovacuum_enabled = false)
;
CREATE TYPE bogus_type
;
ALTER TABLE testpub_tbl8_0 ADD PRIMARY KEY (a)
;
CREATE TABLE alter_idx_rename_test (a INT)
;
CREATE TABLE itest15 (id integer NULL GENERATED ALWAYS AS IDENTITY)
;
create or replace view agg_view1 as
  select aggfns(distinct a,b,c order by a,c using ~<~,b)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c),
         generate_series(1,2) i
;
create function dfunc(int = 1, int = 2, int = 3, int = 4) returns int as $$
  select 4;
$$ language sql
;
CREATE PUBLICATION pub_lower_level FOR TABLE tab4_1 WITH (publish_via_partition_root = true)
;
/* End test case for bug #16242 */

-- Test that ALTER TABLE rewrite preserves a clustered index
-- for normal indexes and indexes on constraints.
create table alttype_cluster (a int)
;
CREATE TYPE split_extension_next.ext_mood AS ENUM ('on', 'off')
;
CREATE TABLE part3 (LIKE range_parted)
;
drop function rngfunc1(n integer)
;
DROP TABLE plt2_adv_extra
;
CREATE AGGREGATE testagg7(int2) (SFUNC = int2_sum, STYPE = int8)
;
-- Triggers on root partition
CREATE TRIGGER parent_delete_trig
  AFTER DELETE ON range_parted for each statement execute procedure trigfunc()
;
-- reserved schema name
CREATE SCHEMA pg_foo
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE CONSTRAINT TRIGGER accounts_balance_constraint
    AFTER INSERT OR UPDATE ON split_trigger_probe.accounts
    DEFERRABLE INITIALLY DEFERRED
    FOR EACH ROW
    WHEN (NEW.balance >= 0)
    EXECUTE FUNCTION split_trigger_probe.audit_row('constraint_balance')
;
----------
[DROP_TABLE] -- drop system table
DROP TABLE pg_description
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE part_2 DROP CONSTRAINT check_a2
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_date DATE DEFAULT '2016-06-02',
              ALTER COLUMN c_text SET DEFAULT 'cat'
;
----------
[CREATE_TABLE] CREATE TABLE temporal_mltrng3 (
  id int4range,
  valid_at datemultirange
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest12_02 BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func4()
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_27487_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_extension
;
----------
[ADD_INDEX] CREATE INDEX oididx ON oidtmp USING gist ( oid )
;
----------
[CREATE_TABLE] create table parted_trigger (a int, b text) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE ref_table (id integer PRIMARY KEY)
;
----------
[CREATE_TABLE] CREATE TABLE list_part1(a numeric, b int, c int8)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_19.pg_temp_33778_2 (tid pg_catalog.tid)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_checkpointer AS
    SELECT
        pg_stat_get_checkpointer_num_timed() AS num_timed,
        pg_stat_get_checkpointer_num_requested() AS num_requested,
        pg_stat_get_checkpointer_restartpoints_timed() AS restartpoints_timed,
        pg_stat_get_checkpointer_restartpoints_requested() AS restartpoints_req,
        pg_stat_get_checkpointer_restartpoints_performed() AS restartpoints_done,
        pg_stat_get_checkpointer_write_time() AS write_time,
        pg_stat_get_checkpointer_sync_time() AS sync_time,
        pg_stat_get_checkpointer_buffers_written() AS buffers_written,
        pg_stat_get_checkpointer_stat_reset_time() AS stats_reset
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table recur1 add column f2 int
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."reverse_name"("name") RETURNS "name"
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'reverse_name'
;
----------
[CREATE_TABLE] CREATE TABLE test_type_diff2_c2 (int_eight int8, int_two int2, int_four int4)
;
----------
[CREATE_POLICY] /*
 * text search configuration for russian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * russian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER regress_event_trigger_report_end ON ddl_command_end
  EXECUTE PROCEDURE event_trigger_report_end()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest1" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_PROG_OBJ] -- Test proper handling of parameterized appendrel paths when the
-- potential join qual is expensive
create function expensivefunc(int) returns int
language plpgsql immutable strict cost 10000
as $$begin return $1; end$$
;
----------
[CREATE_TABLE] -- Test single- and multi-row inserts with table and view defaults.
-- Table defaults should be used, unless overridden by view defaults.
create table base_tab_def (a int, b text default 'Table default',
                           c text default 'Table default', d text, e text)
;
----------
[CREATE_TABLE] create table rules_fooview_child () inherits (rules_fooview)
;
----------
[CREATE_TABLE] CREATE TABLE test_tab (a int primary key, b text, c INT, d INT, e INT)
;
----------
[CREATE_TABLE] CREATE TABLE prt_p1 PARTITION OF prt FOR VALUES FROM (0) TO (10)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_table ALTER b SET (n_distinct = 999)
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
  EXECUTE 'COPY copy1 TO stdout';
END;
$$
;
----------
[CREATE_TABLE|UNSAFE] -- error
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS EXECUTE ctas_ine_query
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_select_lock
;
----------
[ADMIN_TABLE] ANALYZE public.renamecolumnchild
;
----------
[CREATE_TABLE] CREATE TABLE fk (id int, a int DEFAULT 50) PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE] create table dcomptable (d1 dcomptypea unique)
;
----------
[DROP_TABLE] DROP TABLE rls_test_both
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin4 TEMPLATE template0 LOCALE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
----------
[CREATE_TABLE] CREATE TABLE stats_import.test_clone ( LIKE stats_import.test )
    WITH (autovacuum_enabled = false)
;
----------
[CREATE_TYPE] CREATE TYPE bogus_type
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testpub_tbl8_0 ADD PRIMARY KEY (a)
;
----------
[CREATE_TABLE] CREATE TABLE alter_idx_rename_test (a INT)
;
----------
[CREATE_TABLE] CREATE TABLE itest15 (id integer NULL GENERATED ALWAYS AS IDENTITY)
;
----------
[CREATE_VIEW(SELECT)] create or replace view agg_view1 as
  select aggfns(distinct a,b,c order by a,c using ~<~,b)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c),
         generate_series(1,2) i
;
----------
[CREATE_PROG_OBJ] create function dfunc(int = 1, int = 2, int = 3, int = 4) returns int as $$
  select 4;
$$ language sql
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_lower_level FOR TABLE tab4_1 WITH (publish_via_partition_root = true)
;
----------
[CREATE_TABLE] /* End test case for bug #16242 */

-- Test that ALTER TABLE rewrite preserves a clustered index
-- for normal indexes and indexes on constraints.
create table alttype_cluster (a int)
;
----------
[CREATE_TYPE] CREATE TYPE split_extension_next.ext_mood AS ENUM ('on', 'off')
;
----------
[CREATE_TABLE] CREATE TABLE part3 (LIKE range_parted)
;
----------
[DROP_PROG_OBJ] drop function rngfunc1(n integer)
;
----------
[DROP_TABLE] DROP TABLE plt2_adv_extra
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testagg7(int2) (SFUNC = int2_sum, STYPE = int8)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Triggers on root partition
CREATE TRIGGER parent_delete_trig
  AFTER DELETE ON range_parted for each statement execute procedure trigfunc()
;
----------
[CREATE_SCHEMA] -- reserved schema name
CREATE SCHEMA pg_foo
;
