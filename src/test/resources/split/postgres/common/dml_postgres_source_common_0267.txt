UPDATE pgbench_branches SET bbalance = bbalance + -1149 WHERE bid = 1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10056', relminmxid = '1'
WHERE oid = '"public"."rtest_t6"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."tableam_parted_a_heap2"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'ee'
  AND attrelid = '"public"."f_star"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14685', relminmxid = '3'
WHERE oid = '"public"."part_p1"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15666', relminmxid = '3'
WHERE oid = '"public"."euc_jis_2004_inputs"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2085', relminmxid = '1'
WHERE oid = '"public"."bt_txt_heap"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17514', relminmxid = '3'
WHERE oid = '39916'
;
insert into trig_table values (1)
;
INSERT INTO public.ths_p3 VALUES (0, 990)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17906', relminmxid = '3'
WHERE oid = '"public"."pt_part1"'::pg_catalog.regclass
;
INSERT INTO macaddr_data VALUES (2, '08-00-2b-01-02-03')
;
-- This entry is needed to test that TOASTED values are copied correctly.
INSERT INTO clstr_tst (b, c, d) VALUES (6, 'seis', repeat('xyzzy', 100000))
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt2_p3"'::pg_catalog.regclass
;
UPDATE temporal_mltrng SET valid_at = datemultirange(daterange('2018-01-15', '2018-02-15'))
  WHERE id = '[2,3)'
;
INSERT INTO public.ths_p3 VALUES (1, 851)
;
-- remove all tuples after the 10th tuple on each page.  Trying to ensure
-- we get the same layout with all CPU architectures and smaller than standard
-- page sizes.
DELETE FROM tidrangescan
WHERE substring(ctid::text FROM ',(\d+)\)')::integer > 10 OR substring(ctid::text FROM '\((\d+),')::integer > 2
;
DELETE FROM tst WHERE i = 3
;
INSERT INTO public.ths_p1 VALUES (4, 814)
;
INSERT INTO test_part VALUES (8, 'bcd', '2021-07-03 11:12:13')
;
INSERT INTO OID_TBL(f1) VALUES ('99asdfasd')
;
INSERT INTO FKTABLE VALUES (2, 4, 8)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '996', relminmxid = '1'
WHERE oid = '"public"."textrange_test"'::pg_catalog.regclass
;
INSERT INTO rw_view2 VALUES (-200), (200)
;
INSERT INTO public.ths_p3 VALUES (9, 439)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15799', relminmxid = '3'
WHERE oid = '36929'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"mvtest_mvschema"."mvtest_tvm"'::pg_catalog.regclass
;
INSERT INTO caster (numeric)       VALUES ('12.42'::text)
;
UPDATE ONLY "pg_temp_2"."selfref" SET "b" = $1 WHERE $2 OPERATOR(pg_catalog.=) "b"
;
UPDATE testpub_tbl5 SET a = 1
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1566', relminmxid = '1'
WHERE oid = '17379'
;
UPDATE large SET datab = 7
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."gtest_normal_child2"'::pg_catalog.regclass
;
-- fail - "c" is not part of the PK
UPDATE rf_tbl_abcd_pk SET a = 1
;
insert into test1 values (3, repeat('baz', 4000))
;
DELETE FROM t1 WHERE b = 'one'
;
INSERT INTO public.ths_p3 VALUES (1, 761)
;
INSERT INTO unique_tbl VALUES (0, 'one')
;
-- invalid

INSERT INTO macaddr8_data VALUES (10, '08:00:2b:01:02:04')
;
INSERT INTO public.ths_p1 VALUES (4, 284)
;
-- should succeed
UPDATE FKTABLE SET ftest1 = ftest1 + 1
;
INSERT INTO public.tht VALUES ('3', 973)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_m_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
update PSlot set slotlink = 'PS.base.ta2' where slotname = 'PS.base.a3'
;
INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(    3,  500) s(i)
;
INSERT INTO public.tht VALUES ('1', 841)
;
INSERT INTO caster (citext)        VALUES ('((0,0),2)'::circle)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1580', relminmxid = '1'
WHERE oid = '"public"."c_star"'::pg_catalog.regclass
;
insert into arrtest_i values(9,'cat2',30)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pht2_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
------------------------------------------------------------------------------------------
[UPDATE] UPDATE pgbench_branches SET bbalance = bbalance + -1149 WHERE bid = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10056', relminmxid = '1'
WHERE oid = '"public"."rtest_t6"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."tableam_parted_a_heap2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'ee'
  AND attrelid = '"public"."f_star"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14685', relminmxid = '3'
WHERE oid = '"public"."part_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '15666', relminmxid = '3'
WHERE oid = '"public"."euc_jis_2004_inputs"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2085', relminmxid = '1'
WHERE oid = '"public"."bt_txt_heap"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17514', relminmxid = '3'
WHERE oid = '39916'
;
----------
[INSERT] insert into trig_table values (1)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 990)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '17906', relminmxid = '3'
WHERE oid = '"public"."pt_part1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO macaddr_data VALUES (2, '08-00-2b-01-02-03')
;
----------
[INSERT|SELECT] -- This entry is needed to test that TOASTED values are copied correctly.
INSERT INTO clstr_tst (b, c, d) VALUES (6, 'seis', repeat('xyzzy', 100000))
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt2_p3"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE temporal_mltrng SET valid_at = datemultirange(daterange('2018-01-15', '2018-02-15'))
  WHERE id = '[2,3)'
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 851)
;
----------
[DELETE] -- remove all tuples after the 10th tuple on each page.  Trying to ensure
-- we get the same layout with all CPU architectures and smaller than standard
-- page sizes.
DELETE FROM tidrangescan
WHERE substring(ctid::text FROM ',(\d+)\)')::integer > 10 OR substring(ctid::text FROM '\((\d+),')::integer > 2
;
----------
[DELETE] DELETE FROM tst WHERE i = 3
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 814)
;
----------
[INSERT] INSERT INTO test_part VALUES (8, 'bcd', '2021-07-03 11:12:13')
;
----------
[INSERT|SELECT] INSERT INTO OID_TBL(f1) VALUES ('99asdfasd')
;
----------
[INSERT] INSERT INTO FKTABLE VALUES (2, 4, 8)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '996', relminmxid = '1'
WHERE oid = '"public"."textrange_test"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO rw_view2 VALUES (-200), (200)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 439)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15799', relminmxid = '3'
WHERE oid = '36929'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"mvtest_mvschema"."mvtest_tvm"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO caster (numeric)       VALUES ('12.42'::text)
;
----------
[UPDATE] UPDATE ONLY "pg_temp_2"."selfref" SET "b" = $1 WHERE $2 OPERATOR(pg_catalog.=) "b"
;
----------
[UPDATE] UPDATE testpub_tbl5 SET a = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1566', relminmxid = '1'
WHERE oid = '17379'
;
----------
[UPDATE] UPDATE large SET datab = 7
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."gtest_normal_child2"'::pg_catalog.regclass
;
----------
[UPDATE] -- fail - "c" is not part of the PK
UPDATE rf_tbl_abcd_pk SET a = 1
;
----------
[INSERT] insert into test1 values (3, repeat('baz', 4000))
;
----------
[DELETE] DELETE FROM t1 WHERE b = 'one'
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 761)
;
----------
[INSERT] INSERT INTO unique_tbl VALUES (0, 'one')
;
----------
[INSERT] -- invalid

INSERT INTO macaddr8_data VALUES (10, '08:00:2b:01:02:04')
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 284)
;
----------
[UPDATE] -- should succeed
UPDATE FKTABLE SET ftest1 = ftest1 + 1
;
----------
[INSERT] INSERT INTO public.tht VALUES ('3', 973)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_m_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE] update PSlot set slotlink = 'PS.base.ta2' where slotname = 'PS.base.a3'
;
----------
[INSERT|SELECT] INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(    3,  500) s(i)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 841)
;
----------
[INSERT|SELECT] INSERT INTO caster (citext)        VALUES ('((0,0),2)'::circle)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1580', relminmxid = '1'
WHERE oid = '"public"."c_star"'::pg_catalog.regclass
;
----------
[INSERT] insert into arrtest_i values(9,'cat2',30)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pht2_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
