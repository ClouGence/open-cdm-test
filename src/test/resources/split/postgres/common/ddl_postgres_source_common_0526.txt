VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
ANALYZE public.part_p2_p1
;
ANALYZE pg_temp_32.pg_temp_28710
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest25"
    ADD CONSTRAINT "gtest25_pkey" PRIMARY KEY ("a")
;
DROP TEXT SEARCH PARSER IF EXISTS no_such_schema.foo
;
COMMENT ON CONVERSION dump_test.test_conversion
					   IS 'comment on test conversion'
;
drop table atacc2
;
ALTER DATABASE "pl_regression" SET lc_monetary TO 'C'
;
CALL test_proc3(55)
;
CREATE FUNCTION isnge(isbn13, isbn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ANALYZE pg_temp_4.pg_temp_37896_2
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=29079 host=/tmp/jRp_hpVVG9 dbname=postgres' PUBLICATION mypub
;
-- check altering and dropping columns used by domain constraints
create type comptype as (r float8, i float8)
;
CREATE SCHEMA testns4
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_range
;
create table tht_p1 partition of tht for values with (modulus 3, remainder 0)
;
CALL random()
;
CREATE OPERATOR === (
    leftarg = integer,
    rightarg = integer,
    procedure = int4eq,
    negator = <>
)
;
CREATE FUNCTION plsample_result_text(a1 numeric, a2 text, a3 integer[])
RETURNS TEXT
AS $$
  Example of source with text result.
$$ LANGUAGE plsample
;
DROP TABLE pp_nn
;
DROP FOREIGN TABLE insert_tbl
;
create table testpart_orange(logdate date) partition by range(logdate)
;
ALTER TABLE covering_index_heap ADD CONSTRAINT covering_pkey PRIMARY KEY USING INDEX
covering_pkey
;
CREATE TABLE tab3 (a int, gen1 int GENERATED ALWAYS AS (a * 2) STORED, gen2 int GENERATED ALWAYS AS (a * 2) STORED)
;
CREATE FUNCTION test_ambiguous_funcname(text) returns text as $$ select $1; $$ language sql
;
create function f_field_select(t onek) returns int4 as
$$ select t.unique2; $$ language sql immutable
;
analyze test_multirange_gist
;
create table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) with (fillfactor=100)
;
CREATE TABLE regtest_table_3 (x int primary key, y text)
;
--
-- Expression index tests
--
create unique index expr_key_index on insertconflicttest(lower(fruit))
;
CREATE TABLE foo_part_d1 (c text, a int, b float8)
;
ALTER TABLE dump_test.test_table ALTER COLUMN col4 SET (n_distinct = 10)
;
drop table if exists hs2
;
DROP TABLE INTERVAL_TBL_OF
;
CREATE TABLE FKTABLE_FAIL1 ( ftest1 int, CONSTRAINT fkfail1 FOREIGN KEY (ftest2) REFERENCES PKTABLE)
;
--
-- Clean up
-- Many tables are retained by the regression test, but these do not seem
--  particularly useful so just get rid of them for now.
--  - thomas 1997-11-30
--

DROP TABLE  BOOLTBL1
;
/*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
ALTER TABLE pitest1 ALTER COLUMN f3 SET DATA TYPE bigint
;
ALTER FUNCTION gbt_float4_same(gbtreekey8, gbtreekey8, internal) PARALLEL SAFE
;
CREATE INDEX reloptions_test_idx2 ON reloptions_test (s) WITH (fillfactor=130)
;
drop view uv_iocu_view
;
SECURITY LABEL ON TABLE t1p IS 'system_u:object_r:sepgsql_table_t:s0'
;
CREATE FUNCTION int_custom_lt(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
create function namedparmcursor_test5() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77, p2 := 42);
end
$$ language plpgsql
;
create temp table exists_tbl (c1 int, c2 int, c3 int) partition by list (c1)
;
--
-- tests wherein clause value requires a cross-type comparison function
--
create table lparted_by_int2 (a smallint) partition by list (a)
;
-- unrelated change, no effect
ALTER FUNCTION functest_F_2(int) STRICT
;
ALTER INDEX pg_toast.pg_toast_30835_index RENAME TO reind_con_toast_idx
;
create table capitals (
	state		char(2)
) inherits (cities)
;
CREATE INDEX btree_tall_idx2 ON btree_tall_tbl (id)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
----------
[ADMIN_TABLE] ANALYZE public.part_p2_p1
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28710
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest25"
    ADD CONSTRAINT "gtest25_pkey" PRIMARY KEY ("a")
;
----------
[DROP_POLICY] DROP TEXT SEARCH PARSER IF EXISTS no_such_schema.foo
;
----------
[ALTER_POLICY] COMMENT ON CONVERSION dump_test.test_conversion
					   IS 'comment on test conversion'
;
----------
[DROP_TABLE] drop table atacc2
;
----------
[ALTER_CATALOG] ALTER DATABASE "pl_regression" SET lc_monetary TO 'C'
;
----------
[CALL_PROG_OBJ] CALL test_proc3(55)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(isbn13, isbn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37896_2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=29079 host=/tmp/jRp_hpVVG9 dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_TYPE] -- check altering and dropping columns used by domain constraints
create type comptype as (r float8, i float8)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA testns4
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_range
;
----------
[CREATE_TABLE] create table tht_p1 partition of tht for values with (modulus 3, remainder 0)
;
----------
[CALL_PROG_OBJ] CALL random()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR === (
    leftarg = integer,
    rightarg = integer,
    procedure = int4eq,
    negator = <>
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION plsample_result_text(a1 numeric, a2 text, a3 integer[])
RETURNS TEXT
AS $$
  Example of source with text result.
$$ LANGUAGE plsample
;
----------
[DROP_TABLE] DROP TABLE pp_nn
;
----------
[DROP_TABLE] DROP FOREIGN TABLE insert_tbl
;
----------
[CREATE_TABLE] create table testpart_orange(logdate date) partition by range(logdate)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE covering_index_heap ADD CONSTRAINT covering_pkey PRIMARY KEY USING INDEX
covering_pkey
;
----------
[CREATE_TABLE] CREATE TABLE tab3 (a int, gen1 int GENERATED ALWAYS AS (a * 2) STORED, gen2 int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_ambiguous_funcname(text) returns text as $$ select $1; $$ language sql
;
----------
[CREATE_PROG_OBJ] create function f_field_select(t onek) returns int4 as
$$ select t.unique2; $$ language sql immutable
;
----------
[ADMIN_TABLE] analyze test_multirange_gist
;
----------
[CREATE_TABLE] create table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) with (fillfactor=100)
;
----------
[CREATE_TABLE] CREATE TABLE regtest_table_3 (x int primary key, y text)
;
----------
[ADD_INDEX] --
-- Expression index tests
--
create unique index expr_key_index on insertconflicttest(lower(fruit))
;
----------
[CREATE_TABLE] CREATE TABLE foo_part_d1 (c text, a int, b float8)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE dump_test.test_table ALTER COLUMN col4 SET (n_distinct = 10)
;
----------
[DROP_TABLE] drop table if exists hs2
;
----------
[DROP_TABLE] DROP TABLE INTERVAL_TBL_OF
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE_FAIL1 ( ftest1 int, CONSTRAINT fkfail1 FOREIGN KEY (ftest2) REFERENCES PKTABLE)
;
----------
[DROP_TABLE] --
-- Clean up
-- Many tables are retained by the regression test, but these do not seem
--  particularly useful so just get rid of them for now.
--  - thomas 1997-11-30
--

DROP TABLE  BOOLTBL1
;
----------
[CREATE_POLICY] /*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest1 ALTER COLUMN f3 SET DATA TYPE bigint
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float4_same(gbtreekey8, gbtreekey8, internal) PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX reloptions_test_idx2 ON reloptions_test (s) WITH (fillfactor=130)
;
----------
[DROP_VIEW] drop view uv_iocu_view
;
----------
[UNKNOWN] SECURITY LABEL ON TABLE t1p IS 'system_u:object_r:sepgsql_table_t:s0'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_lt(int_custom, int_custom)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] create function namedparmcursor_test5() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77, p2 := 42);
end
$$ language plpgsql
;
----------
[CREATE_TABLE] create temp table exists_tbl (c1 int, c2 int, c3 int) partition by list (c1)
;
----------
[CREATE_TABLE] --
-- tests wherein clause value requires a cross-type comparison function
--
create table lparted_by_int2 (a smallint) partition by list (a)
;
----------
[ALTER_PROG_OBJ] -- unrelated change, no effect
ALTER FUNCTION functest_F_2(int) STRICT
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30835_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table capitals (
	state		char(2)
) inherits (cities)
;
----------
[ADD_INDEX] CREATE INDEX btree_tall_idx2 ON btree_tall_tbl (id)
;
