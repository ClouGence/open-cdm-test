ALTER TABLE fkpart6.pk1 OWNER TO zyc
;
-- same with a constant qual
BEGIN
;
DROP STATISTICS ab1_a_b_stats
;
ALTER SERVER split_dummy_server OPTIONS (DROP dbname)
;
--other user, fail
ROLLBACK
;
REVOKE ALL ON FUNCTION s.citext_eq FROM PUBLIC
;
COPY public.pt_part1 (a, b) TO stdout
;
GRANT regress_current_user TO regress_authenticated_user_sr
;
-- SQL SRF with MERGE ... RETURNING
BEGIN
;
GRANT SELECT (oid, subdbid, subskiplsn, subname, subowner, subenabled,
              subbinary, substream, subtwophasestate, subdisableonerr,
			  subpasswordrequired, subrunasowner, subfailover,
              subslotname, subsynccommit, subpublications, suborigin)
    ON pg_subscription TO public
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE fkpart6.pk1 OWNER TO zyc
;
----------
[TRANSACTION] -- same with a constant qual
BEGIN
;
----------
[ADMIN_PERFORMANCE] DROP STATISTICS ab1_a_b_stats
;
----------
[SYSTEM_SETTING_WRITE] ALTER SERVER split_dummy_server OPTIONS (DROP dbname)
;
----------
[TRANSACTION] --other user, fail
ROLLBACK
;
----------
[REVOKE] REVOKE ALL ON FUNCTION s.citext_eq FROM PUBLIC
;
----------
[DATA_EXPORT] COPY public.pt_part1 (a, b) TO stdout
;
----------
[GRANT] GRANT regress_current_user TO regress_authenticated_user_sr
;
----------
[TRANSACTION] -- SQL SRF with MERGE ... RETURNING
BEGIN
;
----------
[GRANT] GRANT SELECT (oid, subdbid, subskiplsn, subname, subowner, subenabled,
              subbinary, substream, subtwophasestate, subdisableonerr,
			  subpasswordrequired, subrunasowner, subfailover,
              subslotname, subsynccommit, subpublications, suborigin)
    ON pg_subscription TO public
;
