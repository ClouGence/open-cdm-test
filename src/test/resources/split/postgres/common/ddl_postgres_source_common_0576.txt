CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', escape ':')
;
CREATE TABLE table_dropped_index_with_pk (a int PRIMARY KEY, b int, c int)
;
ALTER TABLE pg_toast.pg_toast_33112 RENAME TO reind_con_toast
;
create temp table op (f1 orderedpair)
;
do $$ <<b>> declare c nested_int8s; begin b.c.x = 1; end $$
;
DROP FUNCTION test_ambiguous_funcname
;
/*
 * 6.3
 * INFORMATION_SCHEMA_CATALOG_NAME view
 */

CREATE VIEW information_schema_catalog_name AS
    SELECT CAST(current_database() AS sql_identifier) AS catalog_name
;
CREATE FUNCTION dblink_exec (text, text)
RETURNS text
AS '$libdir/dblink','dblink_exec'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
ALTER TABLE e_star* ADD COLUMN e int4
;
CREATE TABLE part_7 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
	) PARTITION BY LIST (b)
;
-- fail
SECURITY LABEL ON TABLE seclabel_tbl1 IS '...invalid label...'
;
CREATE TABLE "public"."trigger_parted_p1_1" (
    "a" integer CONSTRAINT "trigger_parted_a_not_null" NOT NULL
)
;
CREATE TABLE split_event_helper.rewrite_log (
    relation_oid oid,
    reason integer
)
;
ALTER SEQUENCE seq1 MAXVALUE 10
;
--
-- CREATE IF NOT EXISTS is an entirely unsound thing for an extension
-- to be doing, but let's at least plug the major security hole in it.
--

CREATE COLLATION IF NOT EXISTS ext_cine_coll
  ( LC_COLLATE = "POSIX", LC_CTYPE = "POSIX" )
;
-- Partitioned table and its partitions, nothing owned by other user.
-- Relations are not listed in a single command to test ownership
-- independently.
VACUUM vacowned_parted
;
CREATE INDEX collate_test1_idx5 ON collate_test1 (a COLLATE "C")
;
CREATE UNIQUE INDEX "mvtest_tvmm_expr" ON "public"."mvtest_tvmm" USING "btree" ((("grandtot" > (0)::numeric)))
;
create index on btree_bpchar(f1 bpchar_ops) WITH (deduplicate_items=on)
;
--
-- Test behavior of volatile and set-returning functions in conjunction
-- with ORDER BY and LIMIT.
--

create temp sequence testseq
;
DROP TABLE pg_temp_10.pg_temp_26213_2, pg_temp_10.pg_temp_26213
;
create table parted_copytest_a1 (c text, b int, a int)
;
CREATE PUBLICATION pub_tab FOR TABLE conf_tab, conf_tab_2
;
create table t as select (i % 4)::text::e from generate_series(0, 100000) as i
;
DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN SELECT * FROM test2 ORDER BY x LOOP
        INSERT INTO test1 (a) VALUES (r.x);
        ROLLBACK;
    END LOOP;
END;
$$
;
analyze fkest1
;
-- triggers
create function qqq_trig() returns trigger as $$
begin
if tg_op in ('INSERT', 'UPDATE') then
    raise notice '% %', tg_op, new.id;
    return new;
else
    raise notice '% %', tg_op, old.id;
    return old;
end if;
end
$$ language plpgsql
;
VACUUM vacuum_tab
;
VACUUM ANALYZE num_exp_log10
;
CREATE TEMP TABLE pg_temp_18.pg_temp_27200_2 (tid pg_catalog.tid)
;
CREATE TABLE "public"."gin_test_tbl" (
    "i" integer[]
)
WITH ("autovacuum_enabled"='off')
;
CREATE TABLE test_tab2 (a int)
;
ALTER TABLE pitest1 ALTER COLUMN f3 SET DEFAULT 10000
;
vacuum analyze pgbench_tellers
;
drop trigger self_ref_r_trig on self_ref
;
TRUNCATE trunc_stats_test1
;
--
-- RI triggers on enum types
--
CREATE TABLE enumtest_parent (id rainbow PRIMARY KEY)
;
create temp table patest2() inherits (patest0)
;
create table if not exists test ( data1 bytea , data2 bytea )
;
CREATE TEMP TABLE a_temp_tbl (f1 int primary key, f2 int default 42)
;
CREATE TABLE use_ft1_column_type (x ft1)
;
create temp table datetimes(
    id int,
    f_time time,
    f_timetz timetz,
    f_interval interval,
    f_timestamptz timestamptz,
    f_timestamp timestamp
)
;
DROP TABLE delete_test_table
;
create unique index hat_data_unique_idx
  on hat_data (hat_name COLLATE "C" bpchar_pattern_ops)
;
-- And this,
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest3,
ptest4) REFERENCES pktable)
;
ANALYZE pg_temp_32.pg_temp_27252
;
DROP TABLE test_slru_stats
;
CREATE TABLE itest15 (id integer GENERATED ALWAYS AS IDENTITY NULL)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=19422 host=/tmp/9iBCfaPpy5 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_forall
;
CREATE INDEX tenk1_unique2 ON tenk1 USING btree(unique2 int4_ops)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', escape ':')
;
----------
[CREATE_TABLE] CREATE TABLE table_dropped_index_with_pk (a int PRIMARY KEY, b int, c int)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33112 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] create temp table op (f1 orderedpair)
;
----------
[BLOCK] do $$ <<b>> declare c nested_int8s; begin b.c.x = 1; end $$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_ambiguous_funcname
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.3
 * INFORMATION_SCHEMA_CATALOG_NAME view
 */

CREATE VIEW information_schema_catalog_name AS
    SELECT CAST(current_database() AS sql_identifier) AS catalog_name
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_exec (text, text)
RETURNS text
AS '$libdir/dblink','dblink_exec'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE e_star* ADD COLUMN e int4
;
----------
[CREATE_TABLE] CREATE TABLE part_7 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
	) PARTITION BY LIST (b)
;
----------
[SECURITY_LABEL] -- fail
SECURITY LABEL ON TABLE seclabel_tbl1 IS '...invalid label...'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted_p1_1" (
    "a" integer CONSTRAINT "trigger_parted_a_not_null" NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE split_event_helper.rewrite_log (
    relation_oid oid,
    reason integer
)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE seq1 MAXVALUE 10
;
----------
[CREATE_POLICY] --
-- CREATE IF NOT EXISTS is an entirely unsound thing for an extension
-- to be doing, but let's at least plug the major security hole in it.
--

CREATE COLLATION IF NOT EXISTS ext_cine_coll
  ( LC_COLLATE = "POSIX", LC_CTYPE = "POSIX" )
;
----------
[ADMIN_TABLE] -- Partitioned table and its partitions, nothing owned by other user.
-- Relations are not listed in a single command to test ownership
-- independently.
VACUUM vacowned_parted
;
----------
[ADD_INDEX] CREATE INDEX collate_test1_idx5 ON collate_test1 (a COLLATE "C")
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "mvtest_tvmm_expr" ON "public"."mvtest_tvmm" USING "btree" ((("grandtot" > (0)::numeric)))
;
----------
[ADD_INDEX] create index on btree_bpchar(f1 bpchar_ops) WITH (deduplicate_items=on)
;
----------
[CREATE_SEQUENCE] --
-- Test behavior of volatile and set-returning functions in conjunction
-- with ORDER BY and LIMIT.
--

create temp sequence testseq
;
----------
[DROP_TABLE] DROP TABLE pg_temp_10.pg_temp_26213_2, pg_temp_10.pg_temp_26213
;
----------
[CREATE_TABLE] create table parted_copytest_a1 (c text, b int, a int)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_tab FOR TABLE conf_tab, conf_tab_2
;
----------
[CREATE_TABLE|SELECT] create table t as select (i % 4)::text::e from generate_series(0, 100000) as i
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN SELECT * FROM test2 ORDER BY x LOOP
        INSERT INTO test1 (a) VALUES (r.x);
        ROLLBACK;
    END LOOP;
END;
$$
;
----------
[ADMIN_TABLE] analyze fkest1
;
----------
[CREATE_PROG_OBJ] -- triggers
create function qqq_trig() returns trigger as $$
begin
if tg_op in ('INSERT', 'UPDATE') then
    raise notice '% %', tg_op, new.id;
    return new;
else
    raise notice '% %', tg_op, old.id;
    return old;
end if;
end
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM vacuum_tab
;
----------
[ADMIN_TABLE] VACUUM ANALYZE num_exp_log10
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_18.pg_temp_27200_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gin_test_tbl" (
    "i" integer[]
)
WITH ("autovacuum_enabled"='off')
;
----------
[CREATE_TABLE] CREATE TABLE test_tab2 (a int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest1 ALTER COLUMN f3 SET DEFAULT 10000
;
----------
[ADMIN_TABLE] vacuum analyze pgbench_tellers
;
----------
[DROP_TRIGGER] drop trigger self_ref_r_trig on self_ref
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_stats_test1
;
----------
[CREATE_TABLE] --
-- RI triggers on enum types
--
CREATE TABLE enumtest_parent (id rainbow PRIMARY KEY)
;
----------
[CREATE_TABLE] create temp table patest2() inherits (patest0)
;
----------
[CREATE_TABLE] create table if not exists test ( data1 bytea , data2 bytea )
;
----------
[CREATE_TABLE] CREATE TEMP TABLE a_temp_tbl (f1 int primary key, f2 int default 42)
;
----------
[CREATE_TABLE] CREATE TABLE use_ft1_column_type (x ft1)
;
----------
[CREATE_TABLE] create temp table datetimes(
    id int,
    f_time time,
    f_timetz timetz,
    f_interval interval,
    f_timestamptz timestamptz,
    f_timestamp timestamp
)
;
----------
[DROP_TABLE] DROP TABLE delete_test_table
;
----------
[ADD_INDEX] create unique index hat_data_unique_idx
  on hat_data (hat_name COLLATE "C" bpchar_pattern_ops)
;
----------
[CREATE_TABLE] -- And this,
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest3,
ptest4) REFERENCES pktable)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_27252
;
----------
[DROP_TABLE] DROP TABLE test_slru_stats
;
----------
[CREATE_TABLE] CREATE TABLE itest15 (id integer GENERATED ALWAYS AS IDENTITY NULL)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=19422 host=/tmp/9iBCfaPpy5 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_forall
;
----------
[ADD_INDEX] CREATE INDEX tenk1_unique2 ON tenk1 USING btree(unique2 int4_ops)
;
