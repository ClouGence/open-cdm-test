CREATE TABLE ATACC1 (TEST INT, TEST2 INT
	CHECK (TEST > 0), CHECK (TEST2 > 10) NO INHERIT)
;
CREATE EXTENSION test_custom_types
;
CREATE FOREIGN TABLE dump_test.foreign_table (c1 int options (column_name 'col1'))
						SERVER s1 OPTIONS (schema_name 'x1')
;
CREATE TABLE m(id OID)
;
CREATE INDEX pt_stats_index ON ONLY pt_stats (a)
;
--
-- Check dependencies of serial and ordinary sequences
--
CREATE TEMP SEQUENCE myseq2
;
CREATE FUNCTION page_checksum(IN page bytea, IN blkno int4)
RETURNS smallint
AS '$libdir/pageinspect', 'page_checksum'
LANGUAGE C STRICT PARALLEL SAFE
;
create function mlparted5abrtrig_func() returns trigger as $$ begin new.c = 'b'; return new; end; $$ language plpgsql
;
create table listp_12 partition of listp for values in(1,2) partition by list(a)
;
-- type

DROP TYPE test_type_exists
;
-- note that the range '[1, 1)' has no elements
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (1) TO (1)
;
create function sql_to_date(integer) returns date as $$
select $1::text::date
$$ language sql immutable strict
;
ALTER MATERIALIZED VIEW split_view_probe.base_mv_renamed SET (fillfactor = 90)
;
CREATE TABLE testpub_rf_tbl1 (a integer, b text)
;
create function polyf(x anyelement) returns anyarray as $$
  select array[x + 1, x + 2]
$$ language sql
;
-- tuple -> jsonb
CREATE FUNCTION testTuple() RETURNS jsonb
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
x = (1, 'String', None)
return x
$$
;
create index on t using gin (e)
;
alter table part_bac attach partition part_cab for values in(2)
;
COMMENT ON CONSTRAINT "constr_parent2_a_not_null" ON "public"."constr_parent2" IS 'this constraint is invalid'
;
ALTER INDEX pg_toast.pg_toast_30487_index RENAME TO reind_con_toast_idx
;
CREATE OPERATOR #*# (
   leftarg = int8,
   rightarg = int8,
   procedure = fn_op5
)
;
create domain alter1.posint integer check (value > 0)
;
CREATE VIEW pg_views AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS viewname,
        pg_get_userbyid(C.relowner) AS viewowner,
        pg_get_viewdef(C.oid) AS definition
    FROM pg_class C LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind = 'v'
;
CREATE TABLE pagg_tab3_p2 PARTITION OF pagg_tab3 FOR VALUES IN ('B', 'A')
;
ALTER FUNCTION gbt_int2_same(gbtreekey4, gbtreekey4, internal) PARALLEL SAFE
;
CREATE TABLE conf_tab_2 (a int PRIMARY KEY, b int, c int, unique(a,b)) PARTITION BY RANGE (a)
;
ALTER TYPE test_type ALTER ATTRIBUTE b SET DATA TYPE integer
;
ALTER TABLE itest8
  ALTER COLUMN f4 SET NOT NULL,
  ALTER COLUMN f4 ADD GENERATED ALWAYS AS IDENTITY,
  ALTER COLUMN f4 SET DATA TYPE bigint
;
ANALYZE public.iportaltest
;
CREATE POLICY p1 ON dump_test.test_table
						   USING (true)
						   WITH CHECK (true)
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = foo)
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
/* contrib/hstore/hstore--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE TYPE hstore
;
-- now check for multi-column range partition key
CREATE TABLE range_parted3 (
	a int,
	b int
) PARTITION BY RANGE (a, (b+1))
;
ANALYZE public.plt1_e_p3
;
CREATE FUNCTION ltree_textadd(text,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(_a, b => _b);  -- fail, no output argument for c
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "hword" WITH "public"."hunspell_num", "english_stem"
;
alter domain con drop constraint nonexistent
;
--
-- Test the SP-GiST index on the larger volume of data
--
CREATE TABLE quad_box_tbl (id int, b box)
;
ALTER TABLE pg_temp_11.pg_temp_26410_2 ADD COLUMN newdata pg_temp_11.pg_temp_26410
;
COMMENT ON DATABASE "regression" IS NULL
;
-- Create some partitioned indexes
CREATE INDEX tbspace_reindex_part_index ON ONLY tbspace_reindex_part (c1)
;
CREATE FUNCTION _ltree_r_isparent(ltree,_ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create function base_tab_def_view_instrig_func() returns trigger
as
$$
begin
  insert into base_tab_def values (new.a, new.b, new.c, new.d, new.e);
  return new;
end;
$$
language plpgsql
;
ANALYZE pg_temp_22.pg_temp_28002
;
CREATE VIEW pg_rules AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS tablename,
        R.rulename AS rulename,
        pg_get_ruledef(R.oid) AS definition
    FROM (pg_rewrite R JOIN pg_class C ON (C.oid = R.ev_class))
        LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE R.rulename != '_RETURN'
;
CREATE INDEX comment_test_index ON comment_test(indexed_col)
;
CREATE TABLE flush_wal()
;
CREATE VIEW pg_group AS
    SELECT
        rolname AS groname,
        oid AS grosysid,
        ARRAY(SELECT member FROM pg_auth_members WHERE roleid = pg_authid.oid) AS grolist
    FROM pg_authid
    WHERE NOT rolcanlogin
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE ATACC1 (TEST INT, TEST2 INT
	CHECK (TEST > 0), CHECK (TEST2 > 10) NO INHERIT)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_custom_types
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE dump_test.foreign_table (c1 int options (column_name 'col1'))
						SERVER s1 OPTIONS (schema_name 'x1')
;
----------
[CREATE_TABLE] CREATE TABLE m(id OID)
;
----------
[ADD_INDEX] CREATE INDEX pt_stats_index ON ONLY pt_stats (a)
;
----------
[CREATE_SEQUENCE] --
-- Check dependencies of serial and ordinary sequences
--
CREATE TEMP SEQUENCE myseq2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION page_checksum(IN page bytea, IN blkno int4)
RETURNS smallint
AS '$libdir/pageinspect', 'page_checksum'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function mlparted5abrtrig_func() returns trigger as $$ begin new.c = 'b'; return new; end; $$ language plpgsql
;
----------
[CREATE_TABLE] create table listp_12 partition of listp for values in(1,2) partition by list(a)
;
----------
[DROP_TYPE] -- type

DROP TYPE test_type_exists
;
----------
[CREATE_TABLE] -- note that the range '[1, 1)' has no elements
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (1) TO (1)
;
----------
[CREATE_PROG_OBJ] create function sql_to_date(integer) returns date as $$
select $1::text::date
$$ language sql immutable strict
;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW split_view_probe.base_mv_renamed SET (fillfactor = 90)
;
----------
[CREATE_TABLE] CREATE TABLE testpub_rf_tbl1 (a integer, b text)
;
----------
[CREATE_PROG_OBJ] create function polyf(x anyelement) returns anyarray as $$
  select array[x + 1, x + 2]
$$ language sql
;
----------
[CREATE_PROG_OBJ] -- tuple -> jsonb
CREATE FUNCTION testTuple() RETURNS jsonb
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
x = (1, 'String', None)
return x
$$
;
----------
[ADD_INDEX] create index on t using gin (e)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table part_bac attach partition part_cab for values in(2)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT "constr_parent2_a_not_null" ON "public"."constr_parent2" IS 'this constraint is invalid'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30487_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #*# (
   leftarg = int8,
   rightarg = int8,
   procedure = fn_op5
)
;
----------
[CREATE_TYPE] create domain alter1.posint integer check (value > 0)
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW pg_views AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS viewname,
        pg_get_userbyid(C.relowner) AS viewowner,
        pg_get_viewdef(C.oid) AS definition
    FROM pg_class C LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind = 'v'
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab3_p2 PARTITION OF pagg_tab3 FOR VALUES IN ('B', 'A')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int2_same(gbtreekey4, gbtreekey4, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE conf_tab_2 (a int PRIMARY KEY, b int, c int, unique(a,b)) PARTITION BY RANGE (a)
;
----------
[ALTER_TYPE] ALTER TYPE test_type ALTER ATTRIBUTE b SET DATA TYPE integer
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest8
  ALTER COLUMN f4 SET NOT NULL,
  ALTER COLUMN f4 ADD GENERATED ALWAYS AS IDENTITY,
  ALTER COLUMN f4 SET DATA TYPE bigint
;
----------
[ADMIN_TABLE] ANALYZE public.iportaltest
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON dump_test.test_table
						   USING (true)
						   WITH CHECK (true)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = foo)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TYPE] /* contrib/hstore/hstore--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE TYPE hstore
;
----------
[CREATE_TABLE] -- now check for multi-column range partition key
CREATE TABLE range_parted3 (
	a int,
	b int
) PARTITION BY RANGE (a, (b+1))
;
----------
[ADMIN_TABLE] ANALYZE public.plt1_e_p3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_textadd(text,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(_a, b => _b);  -- fail, no output argument for c
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "hword" WITH "public"."hunspell_num", "english_stem"
;
----------
[ALTER_TYPE] alter domain con drop constraint nonexistent
;
----------
[CREATE_TABLE] --
-- Test the SP-GiST index on the larger volume of data
--
CREATE TABLE quad_box_tbl (id int, b box)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26410_2 ADD COLUMN newdata pg_temp_11.pg_temp_26410
;
----------
[COMMENT_CATALOG] COMMENT ON DATABASE "regression" IS NULL
;
----------
[ADD_INDEX] -- Create some partitioned indexes
CREATE INDEX tbspace_reindex_part_index ON ONLY tbspace_reindex_part (c1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_r_isparent(ltree,_ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function base_tab_def_view_instrig_func() returns trigger
as
$$
begin
  insert into base_tab_def values (new.a, new.b, new.c, new.d, new.e);
  return new;
end;
$$
language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_22.pg_temp_28002
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW pg_rules AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS tablename,
        R.rulename AS rulename,
        pg_get_ruledef(R.oid) AS definition
    FROM (pg_rewrite R JOIN pg_class C ON (C.oid = R.ev_class))
        LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE R.rulename != '_RETURN'
;
----------
[ADD_INDEX] CREATE INDEX comment_test_index ON comment_test(indexed_col)
;
----------
[CREATE_TABLE] CREATE TABLE flush_wal()
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_group AS
    SELECT
        rolname AS groname,
        oid AS grosysid,
        ARRAY(SELECT member FROM pg_auth_members WHERE roleid = pg_authid.oid) AS grolist
    FROM pg_authid
    WHERE NOT rolcanlogin
;
