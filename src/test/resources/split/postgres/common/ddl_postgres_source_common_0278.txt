CREATE TABLE dropped_cols (a int, b_drop int, c int)
;
ALTER TABLE fk ATTACH PARTITION fk2 FOR VALUES FROM (750) TO (3500)
;
CREATE TABLE bootab() inherits (footab)
;
do $$
declare v_test plpgsql_arr_domain;
begin
  v_test := array[1];
  v_test := v_test || 2;
end;
$$
;
CREATE TABLE category (
    cid        int primary key,
    cname      text
)
;
DROP FUNCTION create_temp_tab()
;
DO LANGUAGE plpgsql $$
BEGIN
    EXECUTE 'COMMIT';
END;
$$
;
SECURITY LABEL ON ROLE regress_dummy_seclabel_user2 IS 'unclassified'
;
--
-- Test domains over range types
--

create domain restrictedrange as int4range check (upper(value) < 10)
;
create table tt8a (x timestamptz, z int)
;
-- Now we'll do one all in 1 table with 2 columns of matching types
create table pktable_base(base1 int not null, base2 int)
;
CREATE FUNCTION func_footrg() RETURNS TRIGGER AS $$
  BEGIN
	 OLD.b = OLD.b || ' trigger';

	 -- This will verify that the trigger is not run *before* the row is
	 -- refetched by EvalPlanQual. The OLD row should contain the changes made
	 -- by the concurrent session.
     INSERT INTO triglog select OLD.*;

     RETURN OLD;
  END $$ LANGUAGE PLPGSQL
;
ALTER TABLE gtest20a ADD COLUMN c float8 DEFAULT random() CHECK (b < 61)
;
ALTER OPERATOR >= (ismn, ismn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
DROP TABLE notnull_tbl1
;
ANALYZE pg_temp_13.pg_temp_25980
;
DROP TABLE collate_dep_test1, collate_dep_test4t
;
-- Rules and views

CREATE TEMP VIEW voo AS SELECT f1, f2 FROM foo
;
CREATE TABLE "public"."onek2" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
alter table parent attach partition child2 for values in ('BBB')
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_user_mapping
;
CREATE FUNCTION dblink_build_sql_delete (text, int2vector, int, _text)
RETURNS text
AS '$libdir/dblink','dblink_build_sql_delete'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE temporal_rng (id int4range, valid_at daterange)
;
DROP OPERATOR FAMILY test_operator_family USING no_such_am
;
create table parent (a int) partition by list (a)
;
/*
 * 5.63
 * TABLES view
 */

CREATE VIEW tables AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,

           CAST(
             CASE WHEN nc.oid = pg_my_temp_schema() THEN 'LOCAL TEMPORARY'
                  WHEN c.relkind IN ('r', 'p') THEN 'BASE TABLE'
                  WHEN c.relkind = 'v' THEN 'VIEW'
                  WHEN c.relkind = 'f' THEN 'FOREIGN'
                  ELSE null END
             AS character_data) AS table_type,

           CAST(null AS sql_identifier) AS self_referencing_column_name,
           CAST(null AS character_data) AS reference_generation,

           CAST(CASE WHEN t.typname IS NOT NULL THEN current_database() ELSE null END AS sql_identifier) AS user_defined_type_catalog,
           CAST(nt.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(t.typname AS sql_identifier) AS user_defined_type_name,

           CAST(CASE WHEN c.relkind IN ('r', 'p') OR
                          (c.relkind IN ('v', 'f') AND
                           -- 1 << CMD_INSERT
                           pg_relation_is_updatable(c.oid, false) & 8 = 8)
                THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_insertable_into,

           CAST(CASE WHEN t.typname IS NOT NULL THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_typed,
           CAST(null AS character_data) AS commit_action

    FROM pg_namespace nc JOIN pg_class c ON (nc.oid = c.relnamespace)
           LEFT JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON (c.reloftype = t.oid)

    WHERE c.relkind IN ('r', 'v', 'f', 'p')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_table_privilege(c.oid, 'SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(c.oid, 'SELECT, INSERT, UPDATE, REFERENCES') )
;
ALTER TABLE ONLY "public"."idxpart2" ATTACH PARTITION "public"."idxpart21" FOR VALUES FROM (100) TO (200)
;
-- Alter column type when no table rewrite is required
-- Also check that comments are preserved
create table at_partitioned(id int, name varchar(64), unique (id, name))
  partition by hash(id)
;
DROP TEXT SEARCH DICTIONARY IF EXISTS test_tsdict_exists
;
ALTER TABLE pg_rewrite ADD PRIMARY KEY USING INDEX pg_rewrite_oid_index
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_user_mapping
;
drop table trigpart2
;
create domain plpgsql_domain as integer check(plpgsql_domain_check(value))
;
DO LANGUAGE plpgsql $$
BEGIN
    ROLLBACK;
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
    FOR i IN 0..3 LOOP
        RAISE INFO 'transaction_isolation = %', current_setting('transaction_isolation');
        INSERT INTO test1 (a) VALUES (i);
        IF i % 2 = 0 THEN
            COMMIT AND CHAIN;
        ELSE
            ROLLBACK AND CHAIN;
        END IF;
    END LOOP;
END
$$
;
DROP TABLE varbit_table
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_authid
;
CREATE VIEW pg_stat_subscription AS
    SELECT
            su.oid AS subid,
            su.subname,
            st.worker_type,
            st.pid,
            st.leader_pid,
            st.relid,
            st.received_lsn,
            st.last_msg_send_time,
            st.last_msg_receipt_time,
            st.latest_end_lsn,
            st.latest_end_time
    FROM pg_subscription su
            LEFT JOIN pg_stat_get_subscription(NULL) st
                      ON (st.subid = su.oid)
;
/* contrib/isn/isn--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR <= (ean13, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE COLLATION test0 FROM "C"
;
CREATE INDEX id_ft1_c2 ON ft1 (c2)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_ts_dict
;
VACUUM FULL tst
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."invalid_check_con_child" INHERIT "public"."invalid_check_con"
;
-- relative tablespace locations are not allowed
CREATE TABLESPACE regress_tblspace LOCATION 'relative'
;
CREATE TEMP TABLE pg_temp_11.pg_temp_41099_2 (tid pg_catalog.tid)
;
CREATE OPERATOR #@# (
   leftarg = int8,		
   procedure = factorial
)
;
CREATE TABLE fk_partitioned_fk_3_1 PARTITION OF fk_partitioned_fk_3 FOR VALUES WITH (MODULUS 5, REMAINDER 1)
;
ANALYZE pg_temp_9.pg_temp_26480
;
ALTER DATABASE "template1" IS_TEMPLATE = true
;
ALTER TABLE test_inh_check ADD CONSTRAINT bmerged CHECK (b > 1)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE dropped_cols (a int, b_drop int, c int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk ATTACH PARTITION fk2 FOR VALUES FROM (750) TO (3500)
;
----------
[CREATE_TABLE] CREATE TABLE bootab() inherits (footab)
;
----------
[BLOCK] do $$
declare v_test plpgsql_arr_domain;
begin
  v_test := array[1];
  v_test := v_test || 2;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE category (
    cid        int primary key,
    cname      text
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION create_temp_tab()
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    EXECUTE 'COMMIT';
END;
$$
;
----------
[UNKNOWN] SECURITY LABEL ON ROLE regress_dummy_seclabel_user2 IS 'unclassified'
;
----------
[CREATE_TYPE] --
-- Test domains over range types
--

create domain restrictedrange as int4range check (upper(value) < 10)
;
----------
[CREATE_TABLE] create table tt8a (x timestamptz, z int)
;
----------
[CREATE_TABLE] -- Now we'll do one all in 1 table with 2 columns of matching types
create table pktable_base(base1 int not null, base2 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_footrg() RETURNS TRIGGER AS $$
  BEGIN
	 OLD.b = OLD.b || ' trigger';

	 -- This will verify that the trigger is not run *before* the row is
	 -- refetched by EvalPlanQual. The OLD row should contain the changes made
	 -- by the concurrent session.
     INSERT INTO triglog select OLD.*;

     RETURN OLD;
  END $$ LANGUAGE PLPGSQL
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest20a ADD COLUMN c float8 DEFAULT random() CHECK (b < 61)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ismn, ismn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[DROP_TABLE] DROP TABLE notnull_tbl1
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25980
;
----------
[DROP_TABLE] DROP TABLE collate_dep_test1, collate_dep_test4t
;
----------
[CREATE_VIEW(SELECT)] -- Rules and views

CREATE TEMP VIEW voo AS SELECT f1, f2 FROM foo
;
----------
[CREATE_TABLE] CREATE TABLE "public"."onek2" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parent attach partition child2 for values in ('BBB')
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_user_mapping
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_build_sql_delete (text, int2vector, int, _text)
RETURNS text
AS '$libdir/dblink','dblink_build_sql_delete'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng (id int4range, valid_at daterange)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY test_operator_family USING no_such_am
;
----------
[CREATE_TABLE] create table parent (a int) partition by list (a)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.63
 * TABLES view
 */

CREATE VIEW tables AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,

           CAST(
             CASE WHEN nc.oid = pg_my_temp_schema() THEN 'LOCAL TEMPORARY'
                  WHEN c.relkind IN ('r', 'p') THEN 'BASE TABLE'
                  WHEN c.relkind = 'v' THEN 'VIEW'
                  WHEN c.relkind = 'f' THEN 'FOREIGN'
                  ELSE null END
             AS character_data) AS table_type,

           CAST(null AS sql_identifier) AS self_referencing_column_name,
           CAST(null AS character_data) AS reference_generation,

           CAST(CASE WHEN t.typname IS NOT NULL THEN current_database() ELSE null END AS sql_identifier) AS user_defined_type_catalog,
           CAST(nt.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(t.typname AS sql_identifier) AS user_defined_type_name,

           CAST(CASE WHEN c.relkind IN ('r', 'p') OR
                          (c.relkind IN ('v', 'f') AND
                           -- 1 << CMD_INSERT
                           pg_relation_is_updatable(c.oid, false) & 8 = 8)
                THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_insertable_into,

           CAST(CASE WHEN t.typname IS NOT NULL THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_typed,
           CAST(null AS character_data) AS commit_action

    FROM pg_namespace nc JOIN pg_class c ON (nc.oid = c.relnamespace)
           LEFT JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON (c.reloftype = t.oid)

    WHERE c.relkind IN ('r', 'v', 'f', 'p')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_table_privilege(c.oid, 'SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(c.oid, 'SELECT, INSERT, UPDATE, REFERENCES') )
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."idxpart2" ATTACH PARTITION "public"."idxpart21" FOR VALUES FROM (100) TO (200)
;
----------
[CREATE_TABLE] -- Alter column type when no table rewrite is required
-- Also check that comments are preserved
create table at_partitioned(id int, name varchar(64), unique (id, name))
  partition by hash(id)
;
----------
[DROP_POLICY] DROP TEXT SEARCH DICTIONARY IF EXISTS test_tsdict_exists
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_rewrite ADD PRIMARY KEY USING INDEX pg_rewrite_oid_index
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_user_mapping
;
----------
[DROP_TABLE] drop table trigpart2
;
----------
[CREATE_TYPE] create domain plpgsql_domain as integer check(plpgsql_domain_check(value))
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    ROLLBACK;
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
    FOR i IN 0..3 LOOP
        RAISE INFO 'transaction_isolation = %', current_setting('transaction_isolation');
        INSERT INTO test1 (a) VALUES (i);
        IF i % 2 = 0 THEN
            COMMIT AND CHAIN;
        ELSE
            ROLLBACK AND CHAIN;
        END IF;
    END LOOP;
END
$$
;
----------
[DROP_TABLE] DROP TABLE varbit_table
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_authid
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_subscription AS
    SELECT
            su.oid AS subid,
            su.subname,
            st.worker_type,
            st.pid,
            st.leader_pid,
            st.relid,
            st.received_lsn,
            st.last_msg_send_time,
            st.last_msg_receipt_time,
            st.latest_end_lsn,
            st.latest_end_time
    FROM pg_subscription su
            LEFT JOIN pg_stat_get_subscription(NULL) st
                      ON (st.subid = su.oid)
;
----------
[ALTER_PROG_OBJ] /* contrib/isn/isn--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR <= (ean13, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_POLICY] CREATE COLLATION test0 FROM "C"
;
----------
[ADD_INDEX] CREATE INDEX id_ft1_c2 ON ft1 (c2)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_ts_dict
;
----------
[ADMIN_TABLE] VACUUM FULL tst
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."invalid_check_con_child" INHERIT "public"."invalid_check_con"
;
----------
[CREATE_TABLESPACE] -- relative tablespace locations are not allowed
CREATE TABLESPACE regress_tblspace LOCATION 'relative'
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_41099_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #@# (
   leftarg = int8,		
   procedure = factorial
)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_3_1 PARTITION OF fk_partitioned_fk_3 FOR VALUES WITH (MODULUS 5, REMAINDER 1)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26480
;
----------
[ALTER_CATALOG] ALTER DATABASE "template1" IS_TEMPLATE = true
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE test_inh_check ADD CONSTRAINT bmerged CHECK (b > 1)
;
