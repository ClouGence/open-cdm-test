DROP TABLE moneyp
;
CREATE FUNCTION connectby(text,text,text,text,text,int)
RETURNS setof record
AS '$libdir/tablefunc','connectby_text_serial'
LANGUAGE C STABLE STRICT
;
SECURITY LABEL ON COLUMN t1p_ones.q IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
CREATE TABLE "public"."domnotnull" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "........pg.dropped.2........" INTEGER /* dummy */
)
;
-- Test for adding a column to a parent table with complex inheritance
CREATE TABLE inhta ()
;
CREATE TABLE range_parted2 (
	a int
) PARTITION BY RANGE(a)
;
-- Join
create table tbl1(col1 int)
;
CREATE FUNCTION isnle(ismn, ismn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE INDEX ON idxpart (col1)
;
CREATE TABLE rls_ptbl (a int) PARTITION BY RANGE (a)
;
drop table p
;
CREATE TABLE gtest12 (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (gf1(b)) STORED)
;
ANALYZE pg_temp_10.pg_temp_26754
;
--    N    P        N    P
-- should CREATE
CREATE AGGREGATE myaggp10a(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
drop table toasted_data
;
ALTER SUBSCRIPTION regress_testsub SET PUBLICATION mypub WITH (refresh = true)
;
ALTER TABLE pg_temp_2.pg_temp_27071_2 ADD COLUMN newdata pg_temp_2.pg_temp_27071
;
create index on idxpart2 (a)
;
drop table stable_qual_pruning
;
DO 'DECLARE loid oid; BEGIN SELECT tbl.loid INTO loid FROM lotest_stash_values tbl; PERFORM lo_export(loid, ''/src/src/test/recovery/tmp_check/results/invalid/path''); EXCEPTION WHEN UNDEFINED_FILE THEN RAISE NOTICE ''could not open file, as expected''; END'
;
ALTER TABLE pp_nn_1 VALIDATE CONSTRAINT nn1
;
CREATE TABLE tx3 (c integer, d integer)
;
--
-- COPY FROM ... DEFAULT
--

create temp table copy_default (
	id integer primary key,
	text_value text not null default 'test',
	ts_value timestamp without time zone not null default '2022-07-05'
)
;
create table coercepart (a varchar) partition by list (a)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_ts_config
;
CREATE POLICY p1 ON dob_t2 TO regress_rls_dob_role1,regress_rls_dob_role2 USING (true)
;
CREATE TABLE multi_test (i int)
;
create table mc3p2 partition of mc3p for values from (10, 5, 10) to (10, 10, 10)
;
CREATE INDEX maint_name_idx ON split_maint.maint_table (name)
;
CREATE TABLE tbl_include_box_pk (c1 int, c2 int, c3 int, c4 box)
;
-- Tests for VACUUM

CREATE EXTENSION injection_points
;
TRUNCATE test2
;
CREATE TABLE brin_hot_2 (a int, b int)
;
DO
LANGUAGE plpgsql
$$
DECLARE
    x int := 3;
    y int := 4;
BEGIN
    CALL test_proc6(2, x, y);
    RAISE INFO 'x = %, y = %', x, y;
    CALL test_proc6(2, c => y, b => x);
    RAISE INFO 'x = %, y = %', x, y;
END;
$$
;
-- Should fail. At least leftarg or rightarg should be mandatorily specified
CREATE OPERATOR #@%# (
   procedure = factorial
)
;
-- fail, dup name
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key2 CHECK(c > 6)
;
CREATE TABLE "public"."hash_txt_heap" (
    "seqno" integer,
    "random" "text"
)
;
CREATE TRIGGER a_temp_trig
  BEFORE INSERT ON a_temp_tbl
  EXECUTE FUNCTION event_trigger_dummy_trigger()
;
drop view vw
;
--
-- range_agg function
--
create table reservations ( room_id integer not null, booked_during daterange )
;
DROP TABLE withz
;
CREATE TABLE "public"."rtest_nothn4" (
    "a" integer,
    "b" "text"
)
;
-- Adding an INSTEAD OF trigger should cause NULLs to be inserted instead of
-- table defaults, where there are no view defaults.
create function base_tab_def_view_instrig_func() returns trigger
as
$$
begin
  insert into base_tab_def values (new.a, new.b, new.c, new.d, new.e);
  return new;
end;
$$
language plpgsql
;
CREATE TABLE persons3 OF person_type (
    PRIMARY KEY (id),
    name NOT NULL DEFAULT ''
)
;
CREATE TABLE trunc_trigger_test (f1 int, f2 text, f3 text)
;
ALTER MATERIALIZED VIEW ALL IN TABLESPACE regress_tblspace_renamed SET TABLESPACE pg_default
;
CREATE TABLE "public"."testjsonb" (
    "j" "jsonb"
)
WITH ("parallel_workers"='2')
;
-- Triggers
CREATE TABLE trigger_tab_stats (a int, b int)
;
DROP TABLE mcic_one, mcic_two
;
-- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE moneyp
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION connectby(text,text,text,text,text,int)
RETURNS setof record
AS '$libdir/tablefunc','connectby_text_serial'
LANGUAGE C STABLE STRICT
;
----------
[UNKNOWN] SECURITY LABEL ON COLUMN t1p_ones.q IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."domnotnull" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "........pg.dropped.2........" INTEGER /* dummy */
)
;
----------
[CREATE_TABLE] -- Test for adding a column to a parent table with complex inheritance
CREATE TABLE inhta ()
;
----------
[CREATE_TABLE] CREATE TABLE range_parted2 (
	a int
) PARTITION BY RANGE(a)
;
----------
[CREATE_TABLE] -- Join
create table tbl1(col1 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ismn, ismn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX ON idxpart (col1)
;
----------
[CREATE_TABLE] CREATE TABLE rls_ptbl (a int) PARTITION BY RANGE (a)
;
----------
[DROP_TABLE] drop table p
;
----------
[CREATE_TABLE] CREATE TABLE gtest12 (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (gf1(b)) STORED)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26754
;
----------
[CREATE_PROG_OBJ] --    N    P        N    P
-- should CREATE
CREATE AGGREGATE myaggp10a(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[DROP_TABLE] drop table toasted_data
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET PUBLICATION mypub WITH (refresh = true)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_2.pg_temp_27071_2 ADD COLUMN newdata pg_temp_2.pg_temp_27071
;
----------
[ADD_INDEX] create index on idxpart2 (a)
;
----------
[DROP_TABLE] drop table stable_qual_pruning
;
----------
[BLOCK] DO 'DECLARE loid oid; BEGIN SELECT tbl.loid INTO loid FROM lotest_stash_values tbl; PERFORM lo_export(loid, ''/src/src/test/recovery/tmp_check/results/invalid/path''); EXCEPTION WHEN UNDEFINED_FILE THEN RAISE NOTICE ''could not open file, as expected''; END'
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE pp_nn_1 VALIDATE CONSTRAINT nn1
;
----------
[CREATE_TABLE] CREATE TABLE tx3 (c integer, d integer)
;
----------
[CREATE_TABLE] --
-- COPY FROM ... DEFAULT
--

create temp table copy_default (
	id integer primary key,
	text_value text not null default 'test',
	ts_value timestamp without time zone not null default '2022-07-05'
)
;
----------
[CREATE_TABLE] create table coercepart (a varchar) partition by list (a)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_ts_config
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON dob_t2 TO regress_rls_dob_role1,regress_rls_dob_role2 USING (true)
;
----------
[CREATE_TABLE] CREATE TABLE multi_test (i int)
;
----------
[CREATE_TABLE] create table mc3p2 partition of mc3p for values from (10, 5, 10) to (10, 10, 10)
;
----------
[ADD_INDEX] CREATE INDEX maint_name_idx ON split_maint.maint_table (name)
;
----------
[CREATE_TABLE] CREATE TABLE tbl_include_box_pk (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_LIBRARY] -- Tests for VACUUM

CREATE EXTENSION injection_points
;
----------
[TRUNCATE_TABLE] TRUNCATE test2
;
----------
[CREATE_TABLE] CREATE TABLE brin_hot_2 (a int, b int)
;
----------
[BLOCK] DO
LANGUAGE plpgsql
$$
DECLARE
    x int := 3;
    y int := 4;
BEGIN
    CALL test_proc6(2, x, y);
    RAISE INFO 'x = %, y = %', x, y;
    CALL test_proc6(2, c => y, b => x);
    RAISE INFO 'x = %, y = %', x, y;
END;
$$
;
----------
[CREATE_PROG_OBJ] -- Should fail. At least leftarg or rightarg should be mandatorily specified
CREATE OPERATOR #@%# (
   procedure = factorial
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- fail, dup name
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key2 CHECK(c > 6)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."hash_txt_heap" (
    "seqno" integer,
    "random" "text"
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER a_temp_trig
  BEFORE INSERT ON a_temp_tbl
  EXECUTE FUNCTION event_trigger_dummy_trigger()
;
----------
[DROP_VIEW] drop view vw
;
----------
[CREATE_TABLE] --
-- range_agg function
--
create table reservations ( room_id integer not null, booked_during daterange )
;
----------
[DROP_TABLE] DROP TABLE withz
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_nothn4" (
    "a" integer,
    "b" "text"
)
;
----------
[CREATE_PROG_OBJ] -- Adding an INSTEAD OF trigger should cause NULLs to be inserted instead of
-- table defaults, where there are no view defaults.
create function base_tab_def_view_instrig_func() returns trigger
as
$$
begin
  insert into base_tab_def values (new.a, new.b, new.c, new.d, new.e);
  return new;
end;
$$
language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE persons3 OF person_type (
    PRIMARY KEY (id),
    name NOT NULL DEFAULT ''
)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_trigger_test (f1 int, f2 text, f3 text)
;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW ALL IN TABLESPACE regress_tblspace_renamed SET TABLESPACE pg_default
;
----------
[CREATE_TABLE] CREATE TABLE "public"."testjsonb" (
    "j" "jsonb"
)
WITH ("parallel_workers"='2')
;
----------
[CREATE_TABLE] -- Triggers
CREATE TABLE trigger_tab_stats (a int, b int)
;
----------
[DROP_TABLE] DROP TABLE mcic_one, mcic_two
;
----------
[CREATE_TABLE|SELECT] -- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
