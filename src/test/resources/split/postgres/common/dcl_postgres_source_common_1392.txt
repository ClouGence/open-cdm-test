RESET autovacuum_freeze_max_age
;
ALTER FUNCTION "public"."first_el_transfn"("anyarray", "anyelement") OWNER TO zyc
;
COPY public.notnull_tbl5_child (a, b) TO stdout
;
COPY public.generated_cols (a, c) TO STDOUT
;
drop user regress_view_user2
;
COPY public.tab1_1 TO STDOUT
;
EXECUTE dumpFunc('16724')
;
SET search_path TO tableam_display
;
ALTER ROLE split_role_member WITH NOCREATEDB NOCREATEROLE NOREPLICATION NOBYPASSRLS CONNECTION LIMIT -1 PASSWORD NULL VALID UNTIL 'infinity'
;
-- Test for DROP OWNED BY with shared dependencies.  This is done in a
-- separate, rollbacked, transaction to avoid any trouble with other
-- regression sessions.
BEGIN
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] RESET autovacuum_freeze_max_age
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."first_el_transfn"("anyarray", "anyelement") OWNER TO zyc
;
----------
[DATA_EXPORT] COPY public.notnull_tbl5_child (a, b) TO stdout
;
----------
[DATA_EXPORT] COPY public.generated_cols (a, c) TO STDOUT
;
----------
[DROP_USER] drop user regress_view_user2
;
----------
[DATA_EXPORT] COPY public.tab1_1 TO STDOUT
;
----------
[UNSAFE] EXECUTE dumpFunc('16724')
;
----------
[SESSION_SETTING_WRITE] SET search_path TO tableam_display
;
----------
[ALTER_USER] ALTER ROLE split_role_member WITH NOCREATEDB NOCREATEROLE NOREPLICATION NOBYPASSRLS CONNECTION LIMIT -1 PASSWORD NULL VALID UNTIL 'infinity'
;
----------
[TRANSACTION] -- Test for DROP OWNED BY with shared dependencies.  This is done in a
-- separate, rollbacked, transaction to avoid any trouble with other
-- regression sessions.
BEGIN
;
