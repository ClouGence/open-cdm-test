ALTER FUNCTION "public"."myleast"(VARIADIC "anyarray") OWNER TO zyc
;
DEALLOCATE pgsql_fdw_prep_65
;
-- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
COPY public.gtest21a (a) TO stdout
;
GRANT CREATE ON DATABASE regression TO regress_createrole WITH GRANT OPTION
;
DROP ROLE regress_rls_copy_user
;
EXECUTE getDomainConstraints('40766')
;
-- bug 5255: this is not optimizable by join removal
begin
;
EXECUTE dumpTableAttach('38534')
;
REINDEX INDEX CONCURRENTLY pg_toast.reind_con_toast_idx
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."myleast"(VARIADIC "anyarray") OWNER TO zyc
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_65
;
----------
[SESSION_SETTING_WRITE] -- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
----------
[DATA_EXPORT] COPY public.gtest21a (a) TO stdout
;
----------
[GRANT] GRANT CREATE ON DATABASE regression TO regress_createrole WITH GRANT OPTION
;
----------
[DROP_ROLE] DROP ROLE regress_rls_copy_user
;
----------
[UNSAFE] EXECUTE getDomainConstraints('40766')
;
----------
[TRANSACTION] -- bug 5255: this is not optimizable by join removal
begin
;
----------
[UNSAFE] EXECUTE dumpTableAttach('38534')
;
----------
[ADMIN] REINDEX INDEX CONCURRENTLY pg_toast.reind_con_toast_idx
;
