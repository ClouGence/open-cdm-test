CREATE VIEW limit_thousand_v_2 AS SELECT thousand FROM onek WHERE thousand < 995
		ORDER BY thousand OFFSET 10 FETCH FIRST 5 ROWS ONLY
;
ALTER TABLE smalltbl SET (autovacuum_enabled = off)
;
CREATE DATABASE "regression" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LC_COLLATE = 'C' LC_CTYPE = 'C'
;
ALTER INDEX "public"."parted_self_fk_pkey" ATTACH PARTITION "public"."part2_self_fk_pkey"
;
CREATE TABLE ctas_nodata_2 (ii, jj) AS SELECT i, j FROM ctas_base WITH NO DATA
;
CREATE TYPE int4_d_range as range (subtype = int4_d)
;
DROP FUNCTION castfunc(int) CASCADE
;
-- Create a table owned by regress_sro_user
CREATE TABLE sro_tab (a int)
;
CREATE TYPE named_pair AS (
    i integer,
    j integer
)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c10 integer OPTIONS (p1 'v1')
;
COMMENT ON FUNCTION seg_right(seg, seg) IS
'is right of'
;
CREATE TABLE a3 ()
;
CREATE INDEX test_tab_b_idx ON testschema.test_tab (b)
;
CREATE OPERATOR CLASS gist__int_ops
DEFAULT FOR TYPE _int4 USING gist AS
	OPERATOR	3	&&,
	OPERATOR	6	= (anyarray, anyarray),
	OPERATOR	7	@>,
	OPERATOR	8	<@,
	OPERATOR	13	@,
	OPERATOR	14	~,
	OPERATOR	20	@@ (_int4, query_int),
	FUNCTION	1	g_int_consistent (internal, _int4, smallint, oid, internal),
	FUNCTION	2	g_int_union (internal, internal),
	FUNCTION	3	g_int_compress (internal),
	FUNCTION	4	g_int_decompress (internal),
	FUNCTION	5	g_int_penalty (internal, internal, internal),
	FUNCTION	6	g_int_picksplit (internal, internal),
	FUNCTION	7	g_int_same (_int4, _int4, internal)
;
CREATE TEMP TABLE pg_temp_3.pg_temp_27503_2 (tid pg_catalog.tid)
;
drop schema if exists abc
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29758 host=/tmp/UzfSZ54FlS dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
alter table donothingbrtrig_test attach partition donothingbrtrig_test2 for values in (2)
;
CREATE TABLE public.tst_comp_mix_array (
		a public.tst_comp_mix_t PRIMARY KEY,
		b public.tst_comp_mix_t[]
		)
;
DROP TABLE expr_key
;
ALTER TABLE ONLY "public"."clstr_tst"
    ADD CONSTRAINT "clstr_tst_pkey" PRIMARY KEY ("a")
;
-- Same example, but check this time that the constraint correctly detects
-- violating rows
ALTER TABLE list_parted2 DETACH PARTITION part_7
;
ALTER TABLE part3_self_fk ATTACH PARTITION part33_self_fk FOR VALUES FROM (30) TO (40)
;
ALTER TABLE has_volatile ADD col7 int GENERATED ALWAYS AS (55) stored
;
DROP VIEW json_arrayagg_view
;
drop type pp_rectype
;
CREATE TEMP TABLE FKTABLE (ftest1 int)
;
CREATE AGGREGATE myaggp12b(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  INITCOND = '{}')
;
-- SQL function --- use LIMIT to keep it from being inlined
CREATE FUNCTION PLUS_ONE(i INTEGER) RETURNS INTEGER AS
$$ SELECT (i + 1.0)::INTEGER LIMIT 1 $$ LANGUAGE SQL
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_cast
;
CREATE TABLE tab4 (a INT PRIMARY KEY, b test_typ, d text)
;
CREATE TABLE fail_part (LIKE hpart_1)
;
create operator pg_temp.@@ (leftarg = int4, rightarg = int4, procedure = int4mi)
;
CREATE DOMAIN constraint_enforced_dom AS int
;
CREATE TABLE vacparted_i2 PARTITION OF vacparted_i
  FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
-- test out parameters with named params
create function dfunc(a varchar = 'def a', out _a varchar, c numeric = NULL, out _c numeric)
returns record as $$
  select $1, $2;
$$ language sql
;
create function end_label1() returns void as $$
<<blbl>>
begin
  <<flbl1>>
  for i in 1 .. 10 loop
    raise notice 'i = %', i;
    exit flbl1;
  end loop flbl1;
  <<flbl2>>
  for j in 1 .. 10 loop
    raise notice 'j = %', j;
    exit flbl2;
  end loop;
end blbl;
$$ language plpgsql
;
CREATE OPERATOR ?@> (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_extract_isparent
)
;
ANALYZE pg_catalog.pg_depend
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
-- however, if a list partition does not accept nulls, there should be
-- an explicit NOT NULL constraint on the partition key column for the
-- validation scan to be skipped;
ALTER TABLE list_parted2 ATTACH PARTITION part_3_4 FOR VALUES IN (3, 4)
;
create index ��ٸ����index1 on ��ٸ���� using btree (����ɱ)
;
alter table emp rename column salary to manager
;
CREATE FUNCTION ghstore_same(ghstore, ghstore, internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE past_parted (i int) PARTITION BY LIST(i)
;
drop function tattle(x int, y int)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
CREATE OPERATOR CLASS gin_hstore_ops
DEFAULT FOR TYPE hstore USING gin
AS
	OPERATOR        7       @>,
	OPERATOR        9       ?(hstore,text),
	OPERATOR        10      ?|(hstore,text[]),
	OPERATOR        11      ?&(hstore,text[]),
	FUNCTION        1       bttextcmp(text,text),
	FUNCTION        2       gin_extract_hstore(hstore, internal),
	FUNCTION        3       gin_extract_hstore_query(hstore, internal, int2, internal, internal),
	FUNCTION        4       gin_consistent_hstore(internal, int2, hstore, int4, internal, internal),
	STORAGE         text
;
ALTER TABLE droppk DETACH PARTITION droppk2
;
drop table test_range_elem
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW limit_thousand_v_2 AS SELECT thousand FROM onek WHERE thousand < 995
		ORDER BY thousand OFFSET 10 FETCH FIRST 5 ROWS ONLY
;
----------
[ALTER_TABLE] ALTER TABLE smalltbl SET (autovacuum_enabled = off)
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LC_COLLATE = 'C' LC_CTYPE = 'C'
;
----------
[ALTER_INDEX] ALTER INDEX "public"."parted_self_fk_pkey" ATTACH PARTITION "public"."part2_self_fk_pkey"
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_nodata_2 (ii, jj) AS SELECT i, j FROM ctas_base WITH NO DATA
;
----------
[CREATE_TYPE] CREATE TYPE int4_d_range as range (subtype = int4_d)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION castfunc(int) CASCADE
;
----------
[CREATE_TABLE] -- Create a table owned by regress_sro_user
CREATE TABLE sro_tab (a int)
;
----------
[CREATE_TYPE] CREATE TYPE named_pair AS (
    i integer,
    j integer
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c10 integer OPTIONS (p1 'v1')
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_right(seg, seg) IS
'is right of'
;
----------
[CREATE_TABLE] CREATE TABLE a3 ()
;
----------
[ADD_INDEX] CREATE INDEX test_tab_b_idx ON testschema.test_tab (b)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist__int_ops
DEFAULT FOR TYPE _int4 USING gist AS
	OPERATOR	3	&&,
	OPERATOR	6	= (anyarray, anyarray),
	OPERATOR	7	@>,
	OPERATOR	8	<@,
	OPERATOR	13	@,
	OPERATOR	14	~,
	OPERATOR	20	@@ (_int4, query_int),
	FUNCTION	1	g_int_consistent (internal, _int4, smallint, oid, internal),
	FUNCTION	2	g_int_union (internal, internal),
	FUNCTION	3	g_int_compress (internal),
	FUNCTION	4	g_int_decompress (internal),
	FUNCTION	5	g_int_penalty (internal, internal, internal),
	FUNCTION	6	g_int_picksplit (internal, internal),
	FUNCTION	7	g_int_same (_int4, _int4, internal)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_27503_2 (tid pg_catalog.tid)
;
----------
[DROP_SCHEMA] drop schema if exists abc
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29758 host=/tmp/UzfSZ54FlS dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table donothingbrtrig_test attach partition donothingbrtrig_test2 for values in (2)
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_comp_mix_array (
		a public.tst_comp_mix_t PRIMARY KEY,
		b public.tst_comp_mix_t[]
		)
;
----------
[DROP_TABLE] DROP TABLE expr_key
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."clstr_tst"
    ADD CONSTRAINT "clstr_tst_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Same example, but check this time that the constraint correctly detects
-- violating rows
ALTER TABLE list_parted2 DETACH PARTITION part_7
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part3_self_fk ATTACH PARTITION part33_self_fk FOR VALUES FROM (30) TO (40)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE has_volatile ADD col7 int GENERATED ALWAYS AS (55) stored
;
----------
[DROP_VIEW] DROP VIEW json_arrayagg_view
;
----------
[DROP_TYPE] drop type pp_rectype
;
----------
[CREATE_TABLE] CREATE TEMP TABLE FKTABLE (ftest1 int)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp12b(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] -- SQL function --- use LIMIT to keep it from being inlined
CREATE FUNCTION PLUS_ONE(i INTEGER) RETURNS INTEGER AS
$$ SELECT (i + 1.0)::INTEGER LIMIT 1 $$ LANGUAGE SQL
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_cast
;
----------
[CREATE_TABLE] CREATE TABLE tab4 (a INT PRIMARY KEY, b test_typ, d text)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part (LIKE hpart_1)
;
----------
[CREATE_PROG_OBJ] create operator pg_temp.@@ (leftarg = int4, rightarg = int4, procedure = int4mi)
;
----------
[CREATE_TYPE] CREATE DOMAIN constraint_enforced_dom AS int
;
----------
[CREATE_TABLE] CREATE TABLE vacparted_i2 PARTITION OF vacparted_i
  FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[CREATE_PROG_OBJ] -- test out parameters with named params
create function dfunc(a varchar = 'def a', out _a varchar, c numeric = NULL, out _c numeric)
returns record as $$
  select $1, $2;
$$ language sql
;
----------
[CREATE_PROG_OBJ] create function end_label1() returns void as $$
<<blbl>>
begin
  <<flbl1>>
  for i in 1 .. 10 loop
    raise notice 'i = %', i;
    exit flbl1;
  end loop flbl1;
  <<flbl2>>
  for j in 1 .. 10 loop
    raise notice 'j = %', j;
    exit flbl2;
  end loop;
end blbl;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ?@> (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_extract_isparent
)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_depend
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- however, if a list partition does not accept nulls, there should be
-- an explicit NOT NULL constraint on the partition key column for the
-- validation scan to be skipped;
ALTER TABLE list_parted2 ATTACH PARTITION part_3_4 FOR VALUES IN (3, 4)
;
----------
[ADD_INDEX] create index ��ٸ����index1 on ��ٸ���� using btree (����ɱ)
;
----------
[RENAME_COLUMN] alter table emp rename column salary to manager
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ghstore_same(ghstore, ghstore, internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE past_parted (i int) PARTITION BY LIST(i)
;
----------
[DROP_PROG_OBJ] drop function tattle(x int, y int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gin_hstore_ops
DEFAULT FOR TYPE hstore USING gin
AS
	OPERATOR        7       @>,
	OPERATOR        9       ?(hstore,text),
	OPERATOR        10      ?|(hstore,text[]),
	OPERATOR        11      ?&(hstore,text[]),
	FUNCTION        1       bttextcmp(text,text),
	FUNCTION        2       gin_extract_hstore(hstore, internal),
	FUNCTION        3       gin_extract_hstore_query(hstore, internal, int2, internal, internal),
	FUNCTION        4       gin_consistent_hstore(internal, int2, hstore, int4, internal, internal),
	STORAGE         text
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE droppk DETACH PARTITION droppk2
;
----------
[DROP_TABLE] drop table test_range_elem
;
