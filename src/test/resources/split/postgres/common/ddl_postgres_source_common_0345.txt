CREATE INDEX freespace_hash ON freespace_tab USING hash (c1)
;
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1, ftest2)
     references pktable(ptest1, ptest2)
;
create table mcrparted2_b partition of mcrparted for values from ('b', minvalue) to ('c', minvalue)
;
drop table pp_enumpart
;
-- Create partitions for document categories
CREATE TABLE part_document_fiction PARTITION OF part_document FOR VALUES FROM (11) to (12)
;
ALTER TABLE pg_toast.pg_toast_27779 RENAME TO reind_con_toast
;
CREATE OPERATOR = (
  LEFTARG = int_custom,
  RIGHTARG = int_custom,
  FUNCTION = int_custom_eq,
  COMMUTATOR = =,
  NEGATOR = <>,
  RESTRICT = eqsel,
  JOIN = eqjoinsel,
  HASHES,
  MERGES
)
;
--
-- test conversion of table to view (needed to load some pg_dump files)
--

create table rules_fooview (x int, y text)
;
CREATE TABLE pht1_p3 PARTITION OF pht1 FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
create domain restrictedmultirange as int4multirange check (upper(value) < 10)
;
drop table pktable_base
;
alter table idxpart attach partition idxpart2 for values from (100000) to (1000000)
;
CREATE VIEW split_lock_next.parent_view AS SELECT id FROM split_lock_next.parent
;
-- Both parent relation and toast are cleaned up.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = true)
;
DROP FUNCTION op_leak(record, record)
;
ALTER TABLE unique_tbl ADD CONSTRAINT unique_tbl_i_key
	UNIQUE (i) DEFERRABLE INITIALLY DEFERRED
;
ANALYZE public.gtest22b
;
ALTER TABLE ONLY "public"."notnull_tbl5_child" INHERIT "public"."notnull_tbl5"
;
alter table idxpart attach partition idxpart1 for values from (0) to (1000)
;
CREATE INDEX brintest_expr_idx ON brintest_expr USING brin (brintest_func())
;
-- Verify that attaching indexes maps attribute numbers correctly
create table idxpart (col1 int, a int, col2 int, b int) partition by range (a)
;
CREATE TABLE trgfire5 (LIKE trgfire) PARTITION BY LIST (i)
;
create table domcontest (col1 con)
;
CREATE VIEW test_view AS SELECT 1
;
CREATE DOMAIN "public"."int4range_d" AS "int4range"
	CONSTRAINT "int4range_d_check" CHECK ((VALUE <> '[10,11)'::"int4range"))
;
alter table c_mutable drop column f1
;
ALTER TABLE public.comment_test ADD CONSTRAINT comment_test_positive_col_check CHECK ((positive_col > 0))
;
CREATE TABLE "fkpart4"."droppk2_d" (
    "a" integer CONSTRAINT "droppk_a_not_null" NOT NULL
)
;
CREATE INDEX ON tbl_include_reg (c1, c2) INCLUDE (c1, c3)
;
-- macaddr check

CREATE TABLE macaddr8tmp (a macaddr8)
;
create table idxpart2 partition of idxpart for values from (1000) to (2000) partition by range (a)
;
ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc4
;
ALTER TABLE t2 ADD COLUMN e domain2
;
CREATE TABLE tbl_include_unique1 (c1 int, c2 int, c3 int, c4 box)
;
DROP TABLE users_table_part
;
drop index both_index_expr_key
;
CREATE TABLE "public"."gist_point_tbl" (
    "id" integer,
    "p" "point"
)
;
-- check that "do nothing" BR triggers work with tuple-routing
create table donothingbrtrig_test (a int, b text) partition by list (a)
;
create index on xid8_t1 using btree(x)
;
DROP TABLE ref_tbl
;
CREATE TABLE s2.t1 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
DROP function gseg_decompress(pg_catalog.internal)
;
ALTER TABLE split_alter_table.base_table ALTER COLUMN note SET DEFAULT 'y', ALTER COLUMN old_payload SET DATA TYPE text USING old_payload::text
;
do $$ declare x public.misc_table.zed%type; begin end $$
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) information_schema.sql_parts
;
drop table gist_tbl
;
do $$ declare x foo.bar.baz%rowtype; begin end $$
;
CREATE POLICY pp3 ON part_document AS RESTRICTIVE
    USING ((SELECT dlevel <= seclv FROM uaccount WHERE pguser = current_user))
;
CREATE TABLE "public"."rule_and_refint_t3" (
    "id3a" integer NOT NULL,
    "id3b" integer NOT NULL,
    "id3c" integer NOT NULL,
    "data" "text"
)
;
CREATE TABLE testpub_tbl5 (a int PRIMARY KEY, b text, c text,
	d int generated always as (a + length(b)) stored)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX freespace_hash ON freespace_tab USING hash (c1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1, ftest2)
     references pktable(ptest1, ptest2)
;
----------
[CREATE_TABLE] create table mcrparted2_b partition of mcrparted for values from ('b', minvalue) to ('c', minvalue)
;
----------
[DROP_TABLE] drop table pp_enumpart
;
----------
[CREATE_TABLE] -- Create partitions for document categories
CREATE TABLE part_document_fiction PARTITION OF part_document FOR VALUES FROM (11) to (12)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27779 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
  LEFTARG = int_custom,
  RIGHTARG = int_custom,
  FUNCTION = int_custom_eq,
  COMMUTATOR = =,
  NEGATOR = <>,
  RESTRICT = eqsel,
  JOIN = eqjoinsel,
  HASHES,
  MERGES
)
;
----------
[CREATE_TABLE] --
-- test conversion of table to view (needed to load some pg_dump files)
--

create table rules_fooview (x int, y text)
;
----------
[CREATE_TABLE] CREATE TABLE pht1_p3 PARTITION OF pht1 FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
----------
[CREATE_TYPE] create domain restrictedmultirange as int4multirange check (upper(value) < 10)
;
----------
[DROP_TABLE] drop table pktable_base
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart2 for values from (100000) to (1000000)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW split_lock_next.parent_view AS SELECT id FROM split_lock_next.parent
;
----------
[ALTER_TABLE] -- Both parent relation and toast are cleaned up.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = true)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION op_leak(record, record)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE unique_tbl ADD CONSTRAINT unique_tbl_i_key
	UNIQUE (i) DEFERRABLE INITIALLY DEFERRED
;
----------
[ADMIN_TABLE] ANALYZE public.gtest22b
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."notnull_tbl5_child" INHERIT "public"."notnull_tbl5"
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart1 for values from (0) to (1000)
;
----------
[ADD_INDEX] CREATE INDEX brintest_expr_idx ON brintest_expr USING brin (brintest_func())
;
----------
[CREATE_TABLE] -- Verify that attaching indexes maps attribute numbers correctly
create table idxpart (col1 int, a int, col2 int, b int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE trgfire5 (LIKE trgfire) PARTITION BY LIST (i)
;
----------
[CREATE_TABLE] create table domcontest (col1 con)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW test_view AS SELECT 1
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."int4range_d" AS "int4range"
	CONSTRAINT "int4range_d_check" CHECK ((VALUE <> '[10,11)'::"int4range"))
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table c_mutable drop column f1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.comment_test ADD CONSTRAINT comment_test_positive_col_check CHECK ((positive_col > 0))
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart4"."droppk2_d" (
    "a" integer CONSTRAINT "droppk_a_not_null" NOT NULL
)
;
----------
[ADD_INDEX] CREATE INDEX ON tbl_include_reg (c1, c2) INCLUDE (c1, c3)
;
----------
[CREATE_TABLE] -- macaddr check

CREATE TABLE macaddr8tmp (a macaddr8)
;
----------
[CREATE_TABLE] create table idxpart2 partition of idxpart for values from (1000) to (2000) partition by range (a)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc4
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t2 ADD COLUMN e domain2
;
----------
[CREATE_TABLE] CREATE TABLE tbl_include_unique1 (c1 int, c2 int, c3 int, c4 box)
;
----------
[DROP_TABLE] DROP TABLE users_table_part
;
----------
[DROP_INDEX] drop index both_index_expr_key
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gist_point_tbl" (
    "id" integer,
    "p" "point"
)
;
----------
[CREATE_TABLE] -- check that "do nothing" BR triggers work with tuple-routing
create table donothingbrtrig_test (a int, b text) partition by list (a)
;
----------
[ADD_INDEX] create index on xid8_t1 using btree(x)
;
----------
[DROP_TABLE] DROP TABLE ref_tbl
;
----------
[CREATE_TABLE] CREATE TABLE s2.t1 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
----------
[DROP_PROG_OBJ] DROP function gseg_decompress(pg_catalog.internal)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE split_alter_table.base_table ALTER COLUMN note SET DEFAULT 'y', ALTER COLUMN old_payload SET DATA TYPE text USING old_payload::text
;
----------
[BLOCK] do $$ declare x public.misc_table.zed%type; begin end $$
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) information_schema.sql_parts
;
----------
[DROP_TABLE] drop table gist_tbl
;
----------
[BLOCK] do $$ declare x foo.bar.baz%rowtype; begin end $$
;
----------
[CREATE_POLICY] CREATE POLICY pp3 ON part_document AS RESTRICTIVE
    USING ((SELECT dlevel <= seclv FROM uaccount WHERE pguser = current_user))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rule_and_refint_t3" (
    "id3a" integer NOT NULL,
    "id3b" integer NOT NULL,
    "id3c" integer NOT NULL,
    "data" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl5 (a int PRIMARY KEY, b text, c text,
	d int generated always as (a + length(b)) stored)
;
