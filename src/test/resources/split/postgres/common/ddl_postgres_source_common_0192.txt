CREATE FUNCTION "public"."part_ins_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
  return new;
end;
$$
;
-- Fail when option and namespace do not exist
CREATE INDEX reloptions_test_idx ON reloptions_test (s)
	WITH (not_existing_option=2)
;
create trigger child_ins_trig after insert on child
  for each row execute procedure child_ins_func()
;
CREATE TRIGGER alter_table_under_transition_tables_upd_trigger
  AFTER UPDATE ON alter_table_under_transition_tables
  REFERENCING OLD TABLE AS d NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    alter_table_under_transition_tables_upd_func()
;
CREATE FUNCTION testSVToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
$val = 1;
return $val;
$$
;
ALTER INDEX onek_unique1_constraint RENAME TO onek_unique1_constraint_foo
;
CREATE FUNCTION sro_ifun(int) RETURNS int AS $$
BEGIN
	-- Below we set the table's owner to regress_sro_user
	ASSERT current_user = 'regress_sro_user',
		format('sro_ifun(%s) called by %s', $1, current_user);
	RETURN $1;
END;
$$ LANGUAGE plpgsql IMMUTABLE
;
CREATE TABLE "generated_virtual_tests"."gtestxx_1" (
    "a" integer NOT NULL,
    "b" integer
)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_authid
;
CREATE FUNCTION test2dom(fn text) RETURNS hstore_foo
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
return {'a': 1, fn: 'boo', 'c': None}
$$
;
CREATE UNIQUE INDEX bttest_unique_idx3
						ON bttest_unique3
						USING btree (i int4_custom_ops3)
						WITH (deduplicate_items = on)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
drop table trigger_ddl_table
;
CREATE VIEW rw_view3 AS SELECT * FROM rw_view2 WITH CHECK OPTION
;
CREATE TEMP TABLE pg_temp_9.pg_temp_38025_2 (tid pg_catalog.tid)
;
CREATE INDEX "spgist_point_idx" ON "public"."spgist_point_tbl" USING "spgist" ("p") WITH ("fillfactor"='90')
;
VACUUM pg_catalog.pg_policy
;
CREATE FUNCTION "public"."test_enc_setup"() RETURNS "void"
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'test_enc_setup'
;
CREATE TABLE TIME_TBL (f1 time(2))
;
ALTER TABLE attmp3 VALIDATE CONSTRAINT b_greater_than_ten_not_enforced
;
ANALYZE pg_temp_9.pg_temp_38160
;
create trigger parted_trig_before_row before insert or update or delete on parted_trig
   for each row execute procedure trigger_notice()
;
/*
 * 6.65
 * TABLES view
 */

CREATE VIEW tables AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,

           CAST(
             CASE WHEN nc.oid = pg_my_temp_schema() THEN 'LOCAL TEMPORARY'
                  WHEN c.relkind IN ('r', 'p') THEN 'BASE TABLE'
                  WHEN c.relkind = 'v' THEN 'VIEW'
                  WHEN c.relkind = 'f' THEN 'FOREIGN'
                  ELSE null END
             AS character_data) AS table_type,

           CAST(null AS sql_identifier) AS self_referencing_column_name,
           CAST(null AS character_data) AS reference_generation,

           CAST(CASE WHEN t.typname IS NOT NULL THEN current_database() ELSE null END AS sql_identifier) AS user_defined_type_catalog,
           CAST(nt.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(t.typname AS sql_identifier) AS user_defined_type_name,

           CAST(CASE WHEN c.relkind IN ('r', 'p') OR
                          (c.relkind IN ('v', 'f') AND
                           -- 1 << CMD_INSERT
                           pg_relation_is_updatable(c.oid, false) & 8 = 8)
                THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_insertable_into,

           CAST(CASE WHEN t.typname IS NOT NULL THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_typed,
           CAST(null AS character_data) AS commit_action

    FROM pg_namespace nc JOIN pg_class c ON (nc.oid = c.relnamespace)
           LEFT JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON (c.reloftype = t.oid)

    WHERE c.relkind IN ('r', 'v', 'f', 'p')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_table_privilege(c.oid, 'SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(c.oid, 'SELECT, INSERT, UPDATE, REFERENCES') )
;
DROP FUNCTION y_trigger()
;
drop schema fkpart0, fkpart1, fkpart2, fkpart3 cascade
;
DROP TABLE pg_temp_4.pg_temp_27609_2, pg_temp_4.pg_temp_27609
;
CREATE FUNCTION setenv(text, text)
   RETURNS void
   AS '/src/src/test/regress/regress.so', 'regress_setenv'
   LANGUAGE C STRICT
;
CREATE FUNCTION "public"."namedparmcursor_test8"() RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
declare
  c1 cursor (p1 int, p2 int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2;
  n int4;
begin
  open c1 (77 -- test
  , 42);
  fetch c1 into n;
  return n;
end $$
;
ALTER TABLE ONLY "fkpart4"."dropfk"
    ADD CONSTRAINT "dropfk_a_fkey" FOREIGN KEY ("a") REFERENCES "fkpart4"."droppk"("a")
;
CREATE TABLE badcompresstbl (a text)
;
CREATE TABLE testpub_tbl6 (a int, b text, c text)
;
ANALYZE public.beta_neg_p2
;
CREATE INDEX "idxpart3_a_idx" ON ONLY "public"."idxpart3" USING "btree" ("a")
;
-- these functions encapsulate knowledge about the encoding of typmod:

CREATE FUNCTION _pg_char_max_length(typid oid, typmod int4) RETURNS integer
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT
  CASE WHEN $2 = -1 /* default typmod */
       THEN null
       WHEN $1 IN (1042, 1043) /* char, varchar */
       THEN $2 - 4
       WHEN $1 IN (1560, 1562) /* bit, varbit */
       THEN $2
       ELSE null
  END$$
;
ANALYZE pg_temp_4.pg_temp_29539
;
CREATE TABLE test_gist AS SELECT point(i,i) p, i::text t FROM
    generate_series(1,1000) i
;
create view vw as select 1
;
-- ok
create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "POSIX") to ('g')
;
VACUUM (FULL) pg_catalog.pg_description
;
CREATE FUNCTION make_tuple_indirect (record)
        RETURNS record
        AS '/src/src/test/regress/regress.so'
        LANGUAGE C STRICT
;
ALTER TABLE pg_toast.pg_toast_30646 RENAME TO reind_con_toast
;
alter table alter1.t1 set schema alter1
;
create rule r2 as on update to rules_src do also
  values(old.*, 'old'), (new.*, 'new')
;
drop schema abc,aac
;
CREATE SEQUENCE seq1
;
-- test perl -> hstore
CREATE FUNCTION test2() RETURNS hstore
LANGUAGE plperl
TRANSFORM FOR TYPE hstore
AS $$
$val = {a => 1, b => 'boo', c => undef};
return $val;
$$
;
-- ON SELECT rule cannot be renamed

DROP VIEW rule_v1
;
CREATE TABLE num_exp_add (id1 int4, id2 int4, expected numeric(1000,800))
;
create table parted_isvalid_tab_12 partition of parted_isvalid_tab_1
  for values from (5) to (10)
;
create foreign table ft3 (f1 text collate "C", f2 text, f3 varchar(10))
  server loopback options (table_name 'loct3', use_remote_estimate 'true')
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."part_ins_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
  return new;
end;
$$
;
----------
[ADD_INDEX] -- Fail when option and namespace do not exist
CREATE INDEX reloptions_test_idx ON reloptions_test (s)
	WITH (not_existing_option=2)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child_ins_trig after insert on child
  for each row execute procedure child_ins_func()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER alter_table_under_transition_tables_upd_trigger
  AFTER UPDATE ON alter_table_under_transition_tables
  REFERENCING OLD TABLE AS d NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    alter_table_under_transition_tables_upd_func()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testSVToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
$val = 1;
return $val;
$$
;
----------
[RENAME_INDEX] ALTER INDEX onek_unique1_constraint RENAME TO onek_unique1_constraint_foo
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION sro_ifun(int) RETURNS int AS $$
BEGIN
	-- Below we set the table's owner to regress_sro_user
	ASSERT current_user = 'regress_sro_user',
		format('sro_ifun(%s) called by %s', $1, current_user);
	RETURN $1;
END;
$$ LANGUAGE plpgsql IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtestxx_1" (
    "a" integer NOT NULL,
    "b" integer
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_authid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test2dom(fn text) RETURNS hstore_foo
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
return {'a': 1, fn: 'boo', 'c': None}
$$
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX bttest_unique_idx3
						ON bttest_unique3
						USING btree (i int4_custom_ops3)
						WITH (deduplicate_items = on)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
----------
[DROP_TABLE] drop table trigger_ddl_table
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view3 AS SELECT * FROM rw_view2 WITH CHECK OPTION
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_38025_2 (tid pg_catalog.tid)
;
----------
[ADD_INDEX] CREATE INDEX "spgist_point_idx" ON "public"."spgist_point_tbl" USING "spgist" ("p") WITH ("fillfactor"='90')
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_policy
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_enc_setup"() RETURNS "void"
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'test_enc_setup'
;
----------
[CREATE_TABLE] CREATE TABLE TIME_TBL (f1 time(2))
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE attmp3 VALIDATE CONSTRAINT b_greater_than_ten_not_enforced
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38160
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parted_trig_before_row before insert or update or delete on parted_trig
   for each row execute procedure trigger_notice()
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.65
 * TABLES view
 */

CREATE VIEW tables AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,

           CAST(
             CASE WHEN nc.oid = pg_my_temp_schema() THEN 'LOCAL TEMPORARY'
                  WHEN c.relkind IN ('r', 'p') THEN 'BASE TABLE'
                  WHEN c.relkind = 'v' THEN 'VIEW'
                  WHEN c.relkind = 'f' THEN 'FOREIGN'
                  ELSE null END
             AS character_data) AS table_type,

           CAST(null AS sql_identifier) AS self_referencing_column_name,
           CAST(null AS character_data) AS reference_generation,

           CAST(CASE WHEN t.typname IS NOT NULL THEN current_database() ELSE null END AS sql_identifier) AS user_defined_type_catalog,
           CAST(nt.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(t.typname AS sql_identifier) AS user_defined_type_name,

           CAST(CASE WHEN c.relkind IN ('r', 'p') OR
                          (c.relkind IN ('v', 'f') AND
                           -- 1 << CMD_INSERT
                           pg_relation_is_updatable(c.oid, false) & 8 = 8)
                THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_insertable_into,

           CAST(CASE WHEN t.typname IS NOT NULL THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_typed,
           CAST(null AS character_data) AS commit_action

    FROM pg_namespace nc JOIN pg_class c ON (nc.oid = c.relnamespace)
           LEFT JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON (c.reloftype = t.oid)

    WHERE c.relkind IN ('r', 'v', 'f', 'p')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_table_privilege(c.oid, 'SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(c.oid, 'SELECT, INSERT, UPDATE, REFERENCES') )
;
----------
[DROP_PROG_OBJ] DROP FUNCTION y_trigger()
;
----------
[DROP_SCHEMA] drop schema fkpart0, fkpart1, fkpart2, fkpart3 cascade
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_27609_2, pg_temp_4.pg_temp_27609
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION setenv(text, text)
   RETURNS void
   AS '/src/src/test/regress/regress.so', 'regress_setenv'
   LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."namedparmcursor_test8"() RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
declare
  c1 cursor (p1 int, p2 int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2;
  n int4;
begin
  open c1 (77 -- test
  , 42);
  fetch c1 into n;
  return n;
end $$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart4"."dropfk"
    ADD CONSTRAINT "dropfk_a_fkey" FOREIGN KEY ("a") REFERENCES "fkpart4"."droppk"("a")
;
----------
[CREATE_TABLE] CREATE TABLE badcompresstbl (a text)
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl6 (a int, b text, c text)
;
----------
[ADMIN_TABLE] ANALYZE public.beta_neg_p2
;
----------
[ADD_INDEX] CREATE INDEX "idxpart3_a_idx" ON ONLY "public"."idxpart3" USING "btree" ("a")
;
----------
[CREATE_PROG_OBJ] -- these functions encapsulate knowledge about the encoding of typmod:

CREATE FUNCTION _pg_char_max_length(typid oid, typmod int4) RETURNS integer
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT
  CASE WHEN $2 = -1 /* default typmod */
       THEN null
       WHEN $1 IN (1042, 1043) /* char, varchar */
       THEN $2 - 4
       WHEN $1 IN (1560, 1562) /* bit, varbit */
       THEN $2
       ELSE null
  END$$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_29539
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_gist AS SELECT point(i,i) p, i::text t FROM
    generate_series(1,1000) i
;
----------
[CREATE_VIEW(SELECT)] create view vw as select 1
;
----------
[CREATE_TABLE] -- ok
create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "POSIX") to ('g')
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_description
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_tuple_indirect (record)
        RETURNS record
        AS '/src/src/test/regress/regress.so'
        LANGUAGE C STRICT
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30646 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE] alter table alter1.t1 set schema alter1
;
----------
[CREATE_POLICY] create rule r2 as on update to rules_src do also
  values(old.*, 'old'), (new.*, 'new')
;
----------
[DROP_SCHEMA] drop schema abc,aac
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE seq1
;
----------
[CREATE_PROG_OBJ] -- test perl -> hstore
CREATE FUNCTION test2() RETURNS hstore
LANGUAGE plperl
TRANSFORM FOR TYPE hstore
AS $$
$val = {a => 1, b => 'boo', c => undef};
return $val;
$$
;
----------
[DROP_VIEW] -- ON SELECT rule cannot be renamed

DROP VIEW rule_v1
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_add (id1 int4, id2 int4, expected numeric(1000,800))
;
----------
[CREATE_TABLE] create table parted_isvalid_tab_12 partition of parted_isvalid_tab_1
  for values from (5) to (10)
;
----------
[CREATE_TABLE] create foreign table ft3 (f1 text collate "C", f2 text, f3 varchar(10))
  server loopback options (table_name 'loct3', use_remote_estimate 'true')
;
