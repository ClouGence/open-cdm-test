CREATE OPERATOR CLASS ismn_ops DEFAULT
	FOR TYPE ismn USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btismncmp(ismn, ismn)
;
CREATE type test_type_exists as (a int, b text)
;
-- set default update / set null delete
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, PRIMARY KEY(ptest1, ptest2, ptest3) )
;
ALTER TABLE temporal_unique REPLICA IDENTITY NOTHING
;
--
-- check that an upper-level qual is not pushed down if it references a grouped
-- Var whose underlying expression contains volatile functions
--
alter function tattle(x int, y int) volatile
;
create table My_Table ( Item1 int , Item2 text )
;
DROP FUNCTION IF EXISTS f5(text) CASCADE
;
ANALYZE public.prt2_p3
;
CREATE FUNCTION test_rb_tree(size INTEGER)
	RETURNS pg_catalog.void STRICT
	AS '$libdir/test_rbtree' LANGUAGE C
;
CREATE TABLE "public"."mlparted4" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */,
    "........pg.dropped.8........" INTEGER /* dummy */
)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
CREATE RULE rules_parted_table_insert AS ON INSERT to rules_parted_table
    DO INSTEAD INSERT INTO rules_parted_table_1 VALUES (NEW.*)
;
CREATE TABLE tbl_inh(id integer)
;
create function anyctest(anycompatiblenonarray, anycompatiblenonarray)
returns anycompatiblearray as $$
  select array[$1, $2]
$$ language sql
;
analyze agg_data_2k
;
ALTER TYPE "public"."insenum" ADD VALUE 'i29'
;
TRUNCATE trunc_stats_test4
;
CREATE FUNCTION myvarcharrecv(internal, oid, integer) RETURNS myvarchar
        LANGUAGE internal STABLE PARALLEL SAFE STRICT AS 'varcharrecv'
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=32392 host=/tmp/SInD19yGs4 dbname=postgres' PUBLICATION pub1
;
-- Create policy on parent
-- user's security level must be higher than or equal to document's
CREATE POLICY pp1 ON part_document AS PERMISSIVE
    USING (dlevel <= (SELECT seclv FROM uaccount WHERE pguser = current_user))
;
ANALYZE public.invalid_check_con
;
CREATE MATERIALIZED VIEW s3.t2_mv AS SELECT * FROM s3.t2
;
CREATE FUNCTION gin_extract_value_macaddr(macaddr, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ANALYZE pg_temp_3.pg_temp_27487_2
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest1"
    ADD CONSTRAINT "gtest1_pkey" PRIMARY KEY ("a")
;
CREATE TABLE dropped_objects (
	object_type text,
	schema_name text,
	object_name text,
	object_identity text,
	address_names text[],
	address_args text[],
	is_temporary bool,
	original bool,
	normal bool
)
;
create table test8 (f1 int, f2 int, f3 int, f4 int, f5 int, f6 int, f7 int, f8 int)
;
ALTER TABLE alter_idx_rename_test_idx_2 RENAME TO alter_idx_rename_test_idx_3
;
COMMENT ON TEXT SEARCH CONFIGURATION spanish IS 'configuration for spanish language'
;
ALTER TABLE tbl1 ALTER COLUMN val2 TYPE boolean
;
alter table depth0 add c text
;
CREATE EXTENSION btree_gin
;
CREATE TRIGGER tcntrig AFTER INSERT OR UPDATE OR DELETE ON mytable
    FOR EACH ROW EXECUTE FUNCTION triggered_change_notification(mychannel)
;
CREATE TRIGGER transition_table_level2_ri_child_upd_trigger
  AFTER UPDATE ON transition_table_level2
  REFERENCING NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level2_ri_child_insupd_func()
;
CREATE TABLE t1 (name TEXT, n INTEGER)
;
CREATE VIEW rw_view1 AS SELECT * FROM base_tbl ORDER BY a+b
;
ANALYZE pg_temp_11.pg_temp_26230
;
CREATE   VIEW  pubview AS SELECT * FROM tbl1 WHERE tbl1.a
BETWEEN (SELECT d FROM tbl2 WHERE c = 1) AND (SELECT e FROM tbl3 WHERE f = 2)
AND EXISTS (SELECT g FROM tbl4 LEFT JOIN tbl3 ON tbl4.h = tbl3.f)
;
ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk3_pkey"
;
CREATE FUNCTION regexp_matches( citext, citext, text ) RETURNS SETOF TEXT[] AS $$
    SELECT pg_catalog.regexp_matches( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE ROWS 10
;
CREATE FUNCTION int_custom_cmp(int_custom, int_custom)
RETURNS integer
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE ONLY "public"."idxpart3" DROP COLUMN "........pg.dropped.2........"
;
ALTER OPERATOR FAMILY citext_ops USING hash ADD
    FUNCTION    2   citext_hash_extended(citext, int8)
;
-- one may only delete documents in 'manga' category
CREATE POLICY p4 ON document FOR DELETE
  USING (cid = (SELECT cid from category WHERE cname = 'manga'))
;
--
-- BIT types
--

--
-- Build tables for testing
--

CREATE TABLE BIT_TABLE(b BIT(11))
;
ALTER TABLE FKTABLE ADD CONSTRAINT fknd FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION NOT DEFERRABLE
;
CREATE TRIGGER gtest12_03 BEFORE UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func()
;
COMMENT ON VIEW noview IS 'no view'
;
CREATE FUNCTION "public"."wrap_do_analyze"("c" integer) RETURNS integer
    LANGUAGE "sql" IMMUTABLE
    AS $_$SELECT $1 FROM public.do_analyze()$_$
;
ALTER TABLE prt2 DETACH PARTITION prt2_p3
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS ismn_ops DEFAULT
	FOR TYPE ismn USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btismncmp(ismn, ismn)
;
----------
[CREATE_TYPE] CREATE type test_type_exists as (a int, b text)
;
----------
[CREATE_TABLE] -- set default update / set null delete
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, PRIMARY KEY(ptest1, ptest2, ptest3) )
;
----------
[ALTER_TABLE] ALTER TABLE temporal_unique REPLICA IDENTITY NOTHING
;
----------
[ALTER_PROG_OBJ] --
-- check that an upper-level qual is not pushed down if it references a grouped
-- Var whose underlying expression contains volatile functions
--
alter function tattle(x int, y int) volatile
;
----------
[CREATE_TABLE] create table My_Table ( Item1 int , Item2 text )
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS f5(text) CASCADE
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_p3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_rb_tree(size INTEGER)
	RETURNS pg_catalog.void STRICT
	AS '$libdir/test_rbtree' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted4" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */,
    "........pg.dropped.8........" INTEGER /* dummy */
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
----------
[CREATE_POLICY] CREATE RULE rules_parted_table_insert AS ON INSERT to rules_parted_table
    DO INSTEAD INSERT INTO rules_parted_table_1 VALUES (NEW.*)
;
----------
[CREATE_TABLE] CREATE TABLE tbl_inh(id integer)
;
----------
[CREATE_PROG_OBJ] create function anyctest(anycompatiblenonarray, anycompatiblenonarray)
returns anycompatiblearray as $$
  select array[$1, $2]
$$ language sql
;
----------
[ADMIN_TABLE] analyze agg_data_2k
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i29'
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_stats_test4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION myvarcharrecv(internal, oid, integer) RETURNS myvarchar
        LANGUAGE internal STABLE PARALLEL SAFE STRICT AS 'varcharrecv'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=32392 host=/tmp/SInD19yGs4 dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_POLICY] -- Create policy on parent
-- user's security level must be higher than or equal to document's
CREATE POLICY pp1 ON part_document AS PERMISSIVE
    USING (dlevel <= (SELECT seclv FROM uaccount WHERE pguser = current_user))
;
----------
[ADMIN_TABLE] ANALYZE public.invalid_check_con
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW s3.t2_mv AS SELECT * FROM s3.t2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_macaddr(macaddr, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27487_2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest1"
    ADD CONSTRAINT "gtest1_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE dropped_objects (
	object_type text,
	schema_name text,
	object_name text,
	object_identity text,
	address_names text[],
	address_args text[],
	is_temporary bool,
	original bool,
	normal bool
)
;
----------
[CREATE_TABLE] create table test8 (f1 int, f2 int, f3 int, f4 int, f5 int, f6 int, f7 int, f8 int)
;
----------
[RENAME_TABLE] ALTER TABLE alter_idx_rename_test_idx_2 RENAME TO alter_idx_rename_test_idx_3
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION spanish IS 'configuration for spanish language'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl1 ALTER COLUMN val2 TYPE boolean
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table depth0 add c text
;
----------
[CREATE_LIBRARY] CREATE EXTENSION btree_gin
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER tcntrig AFTER INSERT OR UPDATE OR DELETE ON mytable
    FOR EACH ROW EXECUTE FUNCTION triggered_change_notification(mychannel)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER transition_table_level2_ri_child_upd_trigger
  AFTER UPDATE ON transition_table_level2
  REFERENCING NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level2_ri_child_insupd_func()
;
----------
[CREATE_TABLE] CREATE TABLE t1 (name TEXT, n INTEGER)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT * FROM base_tbl ORDER BY a+b
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26230
;
----------
[CREATE_VIEW(SELECT)] CREATE   VIEW  pubview AS SELECT * FROM tbl1 WHERE tbl1.a
BETWEEN (SELECT d FROM tbl2 WHERE c = 1) AND (SELECT e FROM tbl3 WHERE f = 2)
AND EXISTS (SELECT g FROM tbl4 LEFT JOIN tbl3 ON tbl4.h = tbl3.f)
;
----------
[ALTER_INDEX] ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk3_pkey"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regexp_matches( citext, citext, text ) RETURNS SETOF TEXT[] AS $$
    SELECT pg_catalog.regexp_matches( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE ROWS 10
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_cmp(int_custom, int_custom)
RETURNS integer
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."idxpart3" DROP COLUMN "........pg.dropped.2........"
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY citext_ops USING hash ADD
    FUNCTION    2   citext_hash_extended(citext, int8)
;
----------
[CREATE_POLICY] -- one may only delete documents in 'manga' category
CREATE POLICY p4 ON document FOR DELETE
  USING (cid = (SELECT cid from category WHERE cname = 'manga'))
;
----------
[CREATE_TABLE] --
-- BIT types
--

--
-- Build tables for testing
--

CREATE TABLE BIT_TABLE(b BIT(11))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD CONSTRAINT fknd FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION NOT DEFERRABLE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest12_03 BEFORE UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[COMMENT_VIEW] COMMENT ON VIEW noview IS 'no view'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."wrap_do_analyze"("c" integer) RETURNS integer
    LANGUAGE "sql" IMMUTABLE
    AS $_$SELECT $1 FROM public.do_analyze()$_$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt2 DETACH PARTITION prt2_p3
;
