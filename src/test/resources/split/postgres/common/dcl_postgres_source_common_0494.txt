EXECUTE dumpOpr('3693')
;
DEALLOCATE pgsql_fdw_prep_151
;
-- no

-- placeholder for test with duplicated schema and role names
ALTER DEFAULT PRIVILEGES IN SCHEMA testns,testns GRANT SELECT ON TABLES TO public,public
;
EXECUTE dumpTableAttach('36634')
;
COPY public.prt1_p1 (a, b, c) TO stdout
;
CREATE STATISTICS mcv_lists_multi_2 (mcv) ON c, d FROM mcv_lists_multi
;
-- records
set enable_hashagg to on
;
EXECUTE dumpBaseType('18458')
;
ALTER AGGREGATE "public"."aggfstr"(integer, integer, "text") OWNER TO zyc
;
LOCK TABLE "public"."part_p1" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpOpr('3693')
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_151
;
----------
[GRANT] -- no

-- placeholder for test with duplicated schema and role names
ALTER DEFAULT PRIVILEGES IN SCHEMA testns,testns GRANT SELECT ON TABLES TO public,public
;
----------
[UNSAFE] EXECUTE dumpTableAttach('36634')
;
----------
[DATA_EXPORT] COPY public.prt1_p1 (a, b, c) TO stdout
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS mcv_lists_multi_2 (mcv) ON c, d FROM mcv_lists_multi
;
----------
[SESSION_SETTING_WRITE] -- records
set enable_hashagg to on
;
----------
[UNSAFE] EXECUTE dumpBaseType('18458')
;
----------
[TRANSFER_PRIVILEGE] ALTER AGGREGATE "public"."aggfstr"(integer, integer, "text") OWNER TO zyc
;
----------
[SESSION_LOCK] LOCK TABLE "public"."part_p1" IN ACCESS SHARE MODE
;
