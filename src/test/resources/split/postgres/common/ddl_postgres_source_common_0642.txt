ALTER TABLE test_replica_identity3 ALTER COLUMN id TYPE bigint
;
VACUUM (FREEZE) vacparted, vaccluster, vactst
;
drop function polyf(x anyarray)
;
drop table part_abc
;
drop function update_stmt_notice()
;
ANALYZE public.rtest_comp
;
ALTER TABLE pg_toast.pg_toast_28159 RENAME TO reind_con_toast
;
--
-- add attribute
--

CREATE TABLE attmp (initial int4)
;
CREATE FUNCTION citext_le( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CALL test_proc7c(_x, _a, _b)
;
CREATE TABLE t_2 PARTITION OF t_1 FOR VALUES FROM (1) TO (10)
;
alter table nosuchtable drop column bar
;
DROP SCHEMA tableam_display CASCADE
;
CREATE FUNCTION gbt_intv_decompress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
TRUNCATE tab1_2
;
ANALYZE public.c_star
;
create table inh_parent1(a int, b int, c int, primary key (a, b))
;
CREATE TABLE test_replica_identity2 (id int UNIQUE NOT NULL)
;
CREATE TEMPORARY TABLE inh_temp_child_s2 () INHERITS (inh_parent)
;
ALTER TABLE pg_opfamily ADD PRIMARY KEY USING INDEX pg_opfamily_oid_index
;
CREATE TABLE functional_dependencies_multi (
	a INTEGER,
	b INTEGER,
	c INTEGER,
	d INTEGER
)
WITH (autovacuum_enabled = off)
;
ALTER TABLE tab5 ADD COLUMN b INT
;
CREATE TABLE brin_test (a INT, b INT)
;
ANALYZE public.gtest27
;
CREATE TEMP VIEW foo_v AS SELECT * FROM foo OFFSET 0
;
-- fail

-- fail - binary must be boolean
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, binary = foo)
;
create table boolspart_t partition of boolspart for values in (true)
;
VACUUM (PARALLEL 0) pg_catalog.pg_description
;
ALTER SUBSCRIPTION regress_test_sub CONNECTION 'port=16822 host=/tmp/ekP6Yh8edH user=regress_test_user dbname=postgres password=secret'
;
CREATE VIEW pg_stat_activity AS
    SELECT
            S.datid AS datid,
            D.datname AS datname,
            S.pid,
            S.leader_pid,
            S.usesysid,
            U.rolname AS usename,
            S.application_name,
            S.client_addr,
            S.client_hostname,
            S.client_port,
            S.backend_start,
            S.xact_start,
            S.query_start,
            S.state_change,
            S.wait_event_type,
            S.wait_event,
            S.state,
            S.backend_xid,
            s.backend_xmin,
            S.query,
            S.backend_type
    FROM pg_stat_get_activity(NULL) AS S
        LEFT JOIN pg_database AS D ON (S.datid = D.oid)
        LEFT JOIN pg_authid AS U ON (S.usesysid = U.oid)
;
ALTER TYPE "public"."insenum" ADD VALUE 'i12'
;
CREATE TABLE gtestx (x int, b int GENERATED ALWAYS AS IDENTITY) INHERITS (gtest1)
;
CREATE INDEX t2_hash ON s2.t2 USING HASH (i)
;
CREATE TYPE test7a AS (a int, b priv_testdomain1)
;
CREATE FUNCTION dblink_exec (text,boolean)
RETURNS text
AS '$libdir/dblink','dblink_exec'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE test_time (
	i time
)
;
create or replace function retc(int) returns two_int8s language plpgsql as
$$ declare r two_int8s; begin r := row($1::int8, 1::int8, 42); return r; end $$
;
CREATE TABLE "public"."hub" (
    "name" character(14),
    "comment" "text",
    "nslots" integer
)
;
CREATE FUNCTION "public"."mr_outparam_succeed4"("i" "anyrange", OUT "r" "anymultirange", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select multirange($1), 'foo'::text $_$
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
DROP OPERATOR ~ (_int4, _int4)
;
create temp sequence seq
;
-- default values are not allowed for output parameters
create function dfunc(out int = 20) returns int as $$
  select 1;
$$ language sql
;
-- something a little more complicated
create table atacc1 ( test int, test2 int)
;
CREATE UNIQUE INDEX covering_index_index on covering_index_heap (f1,f2) INCLUDE(f3)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shseclabel
;
ALTER FUNCTION gbt_int8_picksplit(internal, internal) PARALLEL SAFE
;
CREATE FUNCTION pg_buffercache_evict_relation(
    IN regclass,
    OUT buffers_evicted int4,
    OUT buffers_flushed int4,
    OUT buffers_skipped int4)
AS '$libdir/pg_buffercache', 'pg_buffercache_evict_relation'
LANGUAGE C PARALLEL SAFE VOLATILE STRICT
;
VACUUM (FREEZE)
;
DO $$
DECLARE _a int; _b int;
BEGIN
  _a := 10; _b := 30;
  CALL test_proc8a(_a, _b);
  RAISE NOTICE '_a: %, _b: %', _a, _b;
  CALL test_proc8a(b => _b, a => _a);
  RAISE NOTICE '_a: %, _b: %', _a, _b;
END
$$
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_replica_identity3 ALTER COLUMN id TYPE bigint
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) vacparted, vaccluster, vactst
;
----------
[DROP_PROG_OBJ] drop function polyf(x anyarray)
;
----------
[DROP_TABLE] drop table part_abc
;
----------
[DROP_PROG_OBJ] drop function update_stmt_notice()
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_comp
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28159 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] --
-- add attribute
--

CREATE TABLE attmp (initial int4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_le( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CALL_PROG_OBJ] CALL test_proc7c(_x, _a, _b)
;
----------
[CREATE_TABLE] CREATE TABLE t_2 PARTITION OF t_1 FOR VALUES FROM (1) TO (10)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table nosuchtable drop column bar
;
----------
[DROP_SCHEMA] DROP SCHEMA tableam_display CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_intv_decompress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[TRUNCATE_TABLE] TRUNCATE tab1_2
;
----------
[ADMIN_TABLE] ANALYZE public.c_star
;
----------
[CREATE_TABLE] create table inh_parent1(a int, b int, c int, primary key (a, b))
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_identity2 (id int UNIQUE NOT NULL)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE inh_temp_child_s2 () INHERITS (inh_parent)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_opfamily ADD PRIMARY KEY USING INDEX pg_opfamily_oid_index
;
----------
[CREATE_TABLE] CREATE TABLE functional_dependencies_multi (
	a INTEGER,
	b INTEGER,
	c INTEGER,
	d INTEGER
)
WITH (autovacuum_enabled = off)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tab5 ADD COLUMN b INT
;
----------
[CREATE_TABLE] CREATE TABLE brin_test (a INT, b INT)
;
----------
[ADMIN_TABLE] ANALYZE public.gtest27
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW foo_v AS SELECT * FROM foo OFFSET 0
;
----------
[CREATE_PUB_SUB] -- fail

-- fail - binary must be boolean
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, binary = foo)
;
----------
[CREATE_TABLE] create table boolspart_t partition of boolspart for values in (true)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_description
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_test_sub CONNECTION 'port=16822 host=/tmp/ekP6Yh8edH user=regress_test_user dbname=postgres password=secret'
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_activity AS
    SELECT
            S.datid AS datid,
            D.datname AS datname,
            S.pid,
            S.leader_pid,
            S.usesysid,
            U.rolname AS usename,
            S.application_name,
            S.client_addr,
            S.client_hostname,
            S.client_port,
            S.backend_start,
            S.xact_start,
            S.query_start,
            S.state_change,
            S.wait_event_type,
            S.wait_event,
            S.state,
            S.backend_xid,
            s.backend_xmin,
            S.query,
            S.backend_type
    FROM pg_stat_get_activity(NULL) AS S
        LEFT JOIN pg_database AS D ON (S.datid = D.oid)
        LEFT JOIN pg_authid AS U ON (S.usesysid = U.oid)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i12'
;
----------
[CREATE_TABLE] CREATE TABLE gtestx (x int, b int GENERATED ALWAYS AS IDENTITY) INHERITS (gtest1)
;
----------
[ADD_INDEX] CREATE INDEX t2_hash ON s2.t2 USING HASH (i)
;
----------
[CREATE_TYPE] CREATE TYPE test7a AS (a int, b priv_testdomain1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_exec (text,boolean)
RETURNS text
AS '$libdir/dblink','dblink_exec'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE test_time (
	i time
)
;
----------
[CREATE_PROG_OBJ] create or replace function retc(int) returns two_int8s language plpgsql as
$$ declare r two_int8s; begin r := row($1::int8, 1::int8, 42); return r; end $$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."hub" (
    "name" character(14),
    "comment" "text",
    "nslots" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."mr_outparam_succeed4"("i" "anyrange", OUT "r" "anymultirange", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select multirange($1), 'foo'::text $_$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ~ (_int4, _int4)
;
----------
[CREATE_SEQUENCE] create temp sequence seq
;
----------
[CREATE_PROG_OBJ] -- default values are not allowed for output parameters
create function dfunc(out int = 20) returns int as $$
  select 1;
$$ language sql
;
----------
[CREATE_TABLE] -- something a little more complicated
create table atacc1 ( test int, test2 int)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX covering_index_index on covering_index_heap (f1,f2) INCLUDE(f3)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shseclabel
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int8_picksplit(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_buffercache_evict_relation(
    IN regclass,
    OUT buffers_evicted int4,
    OUT buffers_flushed int4,
    OUT buffers_skipped int4)
AS '$libdir/pg_buffercache', 'pg_buffercache_evict_relation'
LANGUAGE C PARALLEL SAFE VOLATILE STRICT
;
----------
[ADMIN_TABLE] VACUUM (FREEZE)
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int;
BEGIN
  _a := 10; _b := 30;
  CALL test_proc8a(_a, _b);
  RAISE NOTICE '_a: %, _b: %', _a, _b;
  CALL test_proc8a(b => _b, a => _a);
  RAISE NOTICE '_a: %, _b: %', _a, _b;
END
$$
;
