ALTER TABLE ONLY "public"."mlparted4" DROP COLUMN "........pg.dropped.7........"
;
ALTER INDEX pg_toast.pg_toast_28613_index RENAME TO reind_con_toast_idx
;
ALTER INDEX pg_toast.pg_toast_28095_index RENAME TO reind_con_toast_idx
;
CREATE TRIGGER invalid_trig AFTER INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_ins_row')
;
CREATE TABLE tt6 () INHERITS (tt0)
;
drop schema mr_sch
;
create type textrange1 as range(subtype=text, multirange_type_name=multirange_of_text, collation="C")
;
CREATE TABLE multixact_conflict (a int PRIMARY KEY, b int)
;
drop table family
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '23538'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
CREATE INDEX fdeps_abc_idx ON public.functional_dependencies USING btree (a, b, c)
;
-- Disabling a trigger in the parent table should disable children triggers too
alter table parted_stmt_trig disable trigger trig_ins_after_parent
;
CREATE UNIQUE INDEX "ec1_expr2" ON "public"."ec1" USING "btree" (((("ff" + 2) + 1)))
;
ALTER INDEX pg_toast.pg_toast_30403_index RENAME TO reind_con_toast_idx
;
DROP SEQUENCE ctlseq1
;
-- test passing expanded-value representations to workers
CREATE FUNCTION make_some_array(int,int) returns int[] as
$$declare x int[];
  begin
    x[1] := $1;
    x[2] := $2;
    return x;
  end$$ language plpgsql parallel safe
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest1_y" INHERIT "public"."gtest1"
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_namespace
;
CREATE OPERATOR ~<=~ (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = ~>~,
    COMMUTATOR = ~>=~,
    PROCEDURE  = citext_pattern_le,
    RESTRICT   = scalarltsel,
    JOIN       = scalarltjoinsel
)
;
CREATE TABLE t4 (m int, junk int, n text)
;
DROP SCHEMA testpub_rf_schema1
;
create table idxpart (a int primary key, b int) partition by range (a)
;
CALL test_proc12(_a, _b, _c)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
CREATE UNIQUE INDEX cnn_uq ON cnn_pk (b)
;
create index idxpart1_1_idx on idxpart1 (b, a)
;
VACUUM (PARALLEL 0) pg_catalog.pg_shdescription
;
create table parted2_stmt_trig (a int) partition by list (a)
;
CREATE OPERATOR FAMILY "public"."test_int4_ops" USING "btree"
;
create function my_op_cmp(a int, b int) returns int as
  $$begin return btint4cmp(a, b); end $$ language plpgsql
;
-- This trick creates an invalid index.
CREATE UNIQUE INDEX CONCURRENTLY concur_reindex_ind5 ON concur_reindex_tab4 (c1)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."dropcolumnanother" INHERIT "public"."dropcolumnchild"
;
CREATE FUNCTION "public"."tfnp"(integer[], integer) RETURNS integer[]
    LANGUAGE "sql"
    AS $_$select $1 || $2$_$
;
CREATE INDEX t1_hash ON s1.t1 USING HASH (i)
;
CREATE FUNCTION make_valid(upc)
	RETURNS upc
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
drop table inh_parent, inh_child, inh_child2, inh_child3
;
ALTER TABLE alice.hashpart_a REPLICA IDENTITY FULL
;
-- Clean up
DROP TABLE test_ext4_tab
;
drop function fail()
;
ANALYZE public.fk_partitioned_fk_4_1
;
ALTER FUNCTION functest_C_1(int) IMMUTABLE
;
CREATE TABLE idxpart (col1 INT) PARTITION BY RANGE (col1)
;
ALTER TABLE dump_test_second_schema.measurement_y2006m5 ENABLE ALWAYS TRIGGER test_trigger
;
DROP AGGREGATE IF EXISTS split_maint.sum_amount(numeric)
;
CREATE TABLE trunc_stats_test(id serial)
;
CREATE TRIGGER parent_delete_trig
  AFTER DELETE ON range_parted for each statement execute procedure trigfunc()
;
-- check it works with OUT parameters

CREATE FUNCTION crosstab_out(text, text,
  OUT rowid text, OUT rowdt timestamp,
  OUT temperature int4, OUT test_result text,
  OUT test_startdate timestamp, OUT volts float8)
RETURNS setof record
AS '$libdir/tablefunc','crosstab_hash'
LANGUAGE C STABLE STRICT
;
-- should fail, can't have event triggers on global objects
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('CREATE TABLESPACE')
   execute procedure test_event_trigger()
;
CREATE OPERATOR "public".= (
    FUNCTION = "public"."int8alias1eq",
    LEFTARG = "public"."int8alias1",
    RIGHTARG = "public"."int8alias1",
    COMMUTATOR = OPERATOR("public".=),
    MERGES,
    RESTRICT = "eqsel",
    JOIN = "eqjoinsel"
)
;
CREATE TABLE "public"."temporal_mltrng" (
    "id" "int4range" NOT NULL,
    "valid_at" "datemultirange" NOT NULL
)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted4" DROP COLUMN "........pg.dropped.7........"
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28613_index RENAME TO reind_con_toast_idx
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28095_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig AFTER INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_ins_row')
;
----------
[CREATE_TABLE] CREATE TABLE tt6 () INHERITS (tt0)
;
----------
[DROP_SCHEMA] drop schema mr_sch
;
----------
[CREATE_POLICY] create type textrange1 as range(subtype=text, multirange_type_name=multirange_of_text, collation="C")
;
----------
[CREATE_TABLE] CREATE TABLE multixact_conflict (a int PRIMARY KEY, b int)
;
----------
[DROP_TABLE] drop table family
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '23538'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
----------
[ADD_INDEX] CREATE INDEX fdeps_abc_idx ON public.functional_dependencies USING btree (a, b, c)
;
----------
[ALTER_TABLE] -- Disabling a trigger in the parent table should disable children triggers too
alter table parted_stmt_trig disable trigger trig_ins_after_parent
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "ec1_expr2" ON "public"."ec1" USING "btree" (((("ff" + 2) + 1)))
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30403_index RENAME TO reind_con_toast_idx
;
----------
[DROP_SEQUENCE] DROP SEQUENCE ctlseq1
;
----------
[CREATE_PROG_OBJ] -- test passing expanded-value representations to workers
CREATE FUNCTION make_some_array(int,int) returns int[] as
$$declare x int[];
  begin
    x[1] := $1;
    x[2] := $2;
    return x;
  end$$ language plpgsql parallel safe
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest1_y" INHERIT "public"."gtest1"
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_namespace
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~<=~ (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = ~>~,
    COMMUTATOR = ~>=~,
    PROCEDURE  = citext_pattern_le,
    RESTRICT   = scalarltsel,
    JOIN       = scalarltjoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE t4 (m int, junk int, n text)
;
----------
[DROP_SCHEMA] DROP SCHEMA testpub_rf_schema1
;
----------
[CREATE_TABLE] create table idxpart (a int primary key, b int) partition by range (a)
;
----------
[CALL_PROG_OBJ] CALL test_proc12(_a, _b, _c)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX cnn_uq ON cnn_pk (b)
;
----------
[ADD_INDEX] create index idxpart1_1_idx on idxpart1 (b, a)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_shdescription
;
----------
[CREATE_TABLE] create table parted2_stmt_trig (a int) partition by list (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY "public"."test_int4_ops" USING "btree"
;
----------
[CREATE_PROG_OBJ] create function my_op_cmp(a int, b int) returns int as
  $$begin return btint4cmp(a, b); end $$ language plpgsql
;
----------
[ADD_INDEX] -- This trick creates an invalid index.
CREATE UNIQUE INDEX CONCURRENTLY concur_reindex_ind5 ON concur_reindex_tab4 (c1)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."dropcolumnanother" INHERIT "public"."dropcolumnchild"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tfnp"(integer[], integer) RETURNS integer[]
    LANGUAGE "sql"
    AS $_$select $1 || $2$_$
;
----------
[ADD_INDEX] CREATE INDEX t1_hash ON s1.t1 USING HASH (i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_valid(upc)
	RETURNS upc
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] drop table inh_parent, inh_child, inh_child2, inh_child3
;
----------
[ALTER_TABLE] ALTER TABLE alice.hashpart_a REPLICA IDENTITY FULL
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE test_ext4_tab
;
----------
[DROP_PROG_OBJ] drop function fail()
;
----------
[ADMIN_TABLE] ANALYZE public.fk_partitioned_fk_4_1
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_C_1(int) IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE idxpart (col1 INT) PARTITION BY RANGE (col1)
;
----------
[ALTER_TABLE] ALTER TABLE dump_test_second_schema.measurement_y2006m5 ENABLE ALWAYS TRIGGER test_trigger
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE IF EXISTS split_maint.sum_amount(numeric)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_stats_test(id serial)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER parent_delete_trig
  AFTER DELETE ON range_parted for each statement execute procedure trigfunc()
;
----------
[CREATE_PROG_OBJ] -- check it works with OUT parameters

CREATE FUNCTION crosstab_out(text, text,
  OUT rowid text, OUT rowdt timestamp,
  OUT temperature int4, OUT test_result text,
  OUT test_startdate timestamp, OUT volts float8)
RETURNS setof record
AS '$libdir/tablefunc','crosstab_hash'
LANGUAGE C STABLE STRICT
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should fail, can't have event triggers on global objects
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('CREATE TABLESPACE')
   execute procedure test_event_trigger()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".= (
    FUNCTION = "public"."int8alias1eq",
    LEFTARG = "public"."int8alias1",
    RIGHTARG = "public"."int8alias1",
    COMMUTATOR = OPERATOR("public".=),
    MERGES,
    RESTRICT = "eqsel",
    JOIN = "eqjoinsel"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."temporal_mltrng" (
    "id" "int4range" NOT NULL,
    "valid_at" "datemultirange" NOT NULL
)
;
