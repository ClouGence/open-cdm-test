CREATE VIEW routine_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(n.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST('EXECUTE' AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, p.proowner, 'USAGE')
                  OR p.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, proname, proowner, pronamespace, (aclexplode(coalesce(proacl, acldefault('f', proowner)))).* FROM pg_proc
         ) p (oid, proname, proowner, pronamespace, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE p.pronamespace = n.oid
          AND grantee.oid = p.grantee
          AND u_grantor.oid = p.grantor
          AND p.prtype IN ('EXECUTE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
CREATE OPERATOR !! (PROCEDURE = priv_testfunc5b, RIGHTARG = priv_testdomain1)
;
--
-- BTREE partial indices
--
CREATE INDEX onek2_u1_prtl ON onek2 USING btree(unique1 int4_ops)
	where unique1 < 20 or unique1 > 980
;
create table donothingbrtrig_test1 (b text, a int)
;
-- Test a MergeAppend plan where one child requires a sort
create table matest0(a int primary key)
;
-- Enum
ALTER INDEX dummy_test_idx SET (option_enum = 'one')
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
-- try dropping all columns
alter table atacc1 drop c
;
create function inner_func(int)
returns int as $$
declare _context text;
begin
  get diagnostics _context = pg_context;
  raise notice '***%***', _context;
  -- lets do it again, just for fun..
  get diagnostics _context = pg_context;
  raise notice '***%***', _context;
  raise notice 'lets make sure we didnt break anything';
  return 2 * $1;
end;
$$ language plpgsql
;
create trigger donothingbrtrig1 before insert on donothingbrtrig_test1 for each row execute procedure donothingbrtrig_func()
;
ALTER INDEX pg_toast.pg_toast_30775_index RENAME TO reind_con_toast_idx
;
ALTER INDEX dummy_test_idx RESET (option_real)
;
CREATE TABLE public.tht (
    en public.digit,
    data integer
)
PARTITION BY HASH (en)
;
create index patest0i on patest0(id)
;
create unique index on covidxpart4 (a) include (b)
;
ALTER TABLE pg_toast.pg_toast_33360 RENAME TO reind_con_toast
;
CREATE INDEX iprt1_p2_a on prt1_p2(a)
;
ALTER TABLE ONLY pitest3 ALTER COLUMN f3 DROP IDENTITY
;
CREATE MATERIALIZED VIEW mvtest_mv_v (ii, jj) AS SELECT i, j FROM mvtest_v
;
DROP TABLE hash_parted
;
CREATE CONVERSION alt_conv2 FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
CREATE TABLE remember_node_subid (c int)
;
DROP TABLE enumtest_child
;
create operator === (
   leftarg = int4,
   rightarg = int4,
   procedure = int4eq,
   commutator = ===,
   hashes
)
;
ALTER OPERATOR <= (ean13, ismn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
create index on only idxpart ((a/b))
;
DO LANGUAGE plpgsql $$
BEGIN
    BEGIN
        INSERT INTO test1 (a) VALUES (1);
        COMMIT;
        INSERT INTO test1 (a) VALUES (1/0);
        COMMIT;
    EXCEPTION
        WHEN division_by_zero THEN
            RAISE NOTICE 'caught division_by_zero';
    END;
END;
$$
;
-- make sure we handle the non-evenly-numbered oid case for enums
create type e as enum ('0', '2', '3')
;
CREATE TYPE "public"."test_type3" AS (
	"........pg.dropped.1........" INTEGER /* dummy */,
	"b" integer
)
;
CREATE FUNCTION priv_testfunc3(int) RETURNS int AS 'select 2 * $1;' LANGUAGE sql
;
-- test that operations with a dropped column do not try to reference
-- its datatype

create domain mytype as text
;
do $$
declare r some_table;
begin
  r := (23, 'skidoo');
  for i in 1 .. 10 loop
    r.id := r.id + i;
    r.data := r.data || ' ' || i;
  end loop;
  raise notice 'r = %', r;
end$$
;
CREATE TABLE droppk2 PARTITION OF droppk FOR VALUES FROM (1000) TO (2000)
  PARTITION BY RANGE (a)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_attribute
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', quote ':')
;
drop type ddtest1
;
CREATE POLICY p3 ON document FOR UPDATE
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
  WITH CHECK (dlevel > 0)
;
alter table test rename column a1 to a123456
;
alter table itrtest attach partition remp2 for values in (2)
;
-- Two scalar columns
DROP TABLE temporal_mltrng2
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_rewrite
;
CREATE FUNCTION gin_extract_value_timestamp(timestamp, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
analyze t3
;
drop schema s1 cascade
;
CREATE DOMAIN booltrue AS bool CHECK (VALUE IS TRUE OR VALUE IS NULL)
;
-- upper/lower

CREATE TABLE collate_test10 (
    a int,
    x text COLLATE "en_US",
    y text COLLATE "tr_TR"
)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=18237 host=/tmp/0Ui4Oufbqt dbname=postgres' PUBLICATION tap_pub
;
do $$
declare r two_int8s;
begin
  for r in select i, i+1 from generate_series(1,4) i
  loop
    raise notice 'r = %', r;
  end loop;
end$$
;
alter table fkpart1.fk_part add foreign key (a) references fkpart1.pkey
;
-- interesting dict -> jsonb
CREATE FUNCTION test_dict1() RETURNS jsonb
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
x = {"a": 1, None: 2, 33: 3}
return x
$$
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW routine_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(n.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST('EXECUTE' AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, p.proowner, 'USAGE')
                  OR p.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, proname, proowner, pronamespace, (aclexplode(coalesce(proacl, acldefault('f', proowner)))).* FROM pg_proc
         ) p (oid, proname, proowner, pronamespace, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE p.pronamespace = n.oid
          AND grantee.oid = p.grantee
          AND u_grantor.oid = p.grantor
          AND p.prtype IN ('EXECUTE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !! (PROCEDURE = priv_testfunc5b, RIGHTARG = priv_testdomain1)
;
----------
[ADD_INDEX] --
-- BTREE partial indices
--
CREATE INDEX onek2_u1_prtl ON onek2 USING btree(unique1 int4_ops)
	where unique1 < 20 or unique1 > 980
;
----------
[CREATE_TABLE] create table donothingbrtrig_test1 (b text, a int)
;
----------
[CREATE_TABLE] -- Test a MergeAppend plan where one child requires a sort
create table matest0(a int primary key)
;
----------
[ALTER_INDEX] -- Enum
ALTER INDEX dummy_test_idx SET (option_enum = 'one')
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
----------
[ALTER_TABLE|DROP_COLUMN] -- try dropping all columns
alter table atacc1 drop c
;
----------
[CREATE_PROG_OBJ] create function inner_func(int)
returns int as $$
declare _context text;
begin
  get diagnostics _context = pg_context;
  raise notice '***%***', _context;
  -- lets do it again, just for fun..
  get diagnostics _context = pg_context;
  raise notice '***%***', _context;
  raise notice 'lets make sure we didnt break anything';
  return 2 * $1;
end;
$$ language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger donothingbrtrig1 before insert on donothingbrtrig_test1 for each row execute procedure donothingbrtrig_func()
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30775_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx RESET (option_real)
;
----------
[CREATE_TABLE] CREATE TABLE public.tht (
    en public.digit,
    data integer
)
PARTITION BY HASH (en)
;
----------
[ADD_INDEX] create index patest0i on patest0(id)
;
----------
[ADD_INDEX] create unique index on covidxpart4 (a) include (b)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33360 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] CREATE INDEX iprt1_p2_a on prt1_p2(a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY pitest3 ALTER COLUMN f3 DROP IDENTITY
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv_v (ii, jj) AS SELECT i, j FROM mvtest_v
;
----------
[DROP_TABLE] DROP TABLE hash_parted
;
----------
[CREATE_POLICY] CREATE CONVERSION alt_conv2 FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[CREATE_TABLE] CREATE TABLE remember_node_subid (c int)
;
----------
[DROP_TABLE] DROP TABLE enumtest_child
;
----------
[CREATE_PROG_OBJ] create operator === (
   leftarg = int4,
   rightarg = int4,
   procedure = int4eq,
   commutator = ===,
   hashes
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ean13, ismn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[ADD_INDEX] create index on only idxpart ((a/b))
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    BEGIN
        INSERT INTO test1 (a) VALUES (1);
        COMMIT;
        INSERT INTO test1 (a) VALUES (1/0);
        COMMIT;
    EXCEPTION
        WHEN division_by_zero THEN
            RAISE NOTICE 'caught division_by_zero';
    END;
END;
$$
;
----------
[CREATE_TYPE] -- make sure we handle the non-evenly-numbered oid case for enums
create type e as enum ('0', '2', '3')
;
----------
[CREATE_TYPE] CREATE TYPE "public"."test_type3" AS (
	"........pg.dropped.1........" INTEGER /* dummy */,
	"b" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION priv_testfunc3(int) RETURNS int AS 'select 2 * $1;' LANGUAGE sql
;
----------
[CREATE_TYPE] -- test that operations with a dropped column do not try to reference
-- its datatype

create domain mytype as text
;
----------
[BLOCK] do $$
declare r some_table;
begin
  r := (23, 'skidoo');
  for i in 1 .. 10 loop
    r.id := r.id + i;
    r.data := r.data || ' ' || i;
  end loop;
  raise notice 'r = %', r;
end$$
;
----------
[CREATE_TABLE] CREATE TABLE droppk2 PARTITION OF droppk FOR VALUES FROM (1000) TO (2000)
  PARTITION BY RANGE (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_attribute
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', quote ':')
;
----------
[DROP_TYPE] drop type ddtest1
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON document FOR UPDATE
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
  WITH CHECK (dlevel > 0)
;
----------
[RENAME_COLUMN] alter table test rename column a1 to a123456
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table itrtest attach partition remp2 for values in (2)
;
----------
[DROP_TABLE] -- Two scalar columns
DROP TABLE temporal_mltrng2
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_rewrite
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_timestamp(timestamp, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] analyze t3
;
----------
[DROP_SCHEMA] drop schema s1 cascade
;
----------
[CREATE_TYPE] CREATE DOMAIN booltrue AS bool CHECK (VALUE IS TRUE OR VALUE IS NULL)
;
----------
[CREATE_TABLE] -- upper/lower

CREATE TABLE collate_test10 (
    a int,
    x text COLLATE "en_US",
    y text COLLATE "tr_TR"
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=18237 host=/tmp/0Ui4Oufbqt dbname=postgres' PUBLICATION tap_pub
;
----------
[BLOCK] do $$
declare r two_int8s;
begin
  for r in select i, i+1 from generate_series(1,4) i
  loop
    raise notice 'r = %', r;
  end loop;
end$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table fkpart1.fk_part add foreign key (a) references fkpart1.pkey
;
----------
[CREATE_PROG_OBJ] -- interesting dict -> jsonb
CREATE FUNCTION test_dict1() RETURNS jsonb
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
x = {"a": 1, None: 2, 33: 3}
return x
$$
;
