-- one may insert documents only authored by them
CREATE POLICY p2 ON document FOR INSERT WITH CHECK (dauthor = current_user)
;
ALTER SEQUENCE split_sequence_next.seq_full SET UNLOGGED
;
ALTER TABLE pg_temp_3.pg_temp_27606_2 ADD COLUMN newdata pg_temp_3.pg_temp_27606
;
CREATE FUNCTION isngt(ean13, isbn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION explain_filter(text) RETURNS setof text
LANGUAGE plpgsql AS
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Remove the path portion of foreign file names
        ln := regexp_replace(ln, 'Foreign File: .*/([a-z.]+)$', 'Foreign File: .../\1');
        return next ln;
    end loop;
end;
$$
;
CREATE TRANSFORM FOR integer LANGUAGE "sql" (FROM SQL WITH FUNCTION "pg_catalog"."prsd_lextype"("internal"), TO SQL WITH FUNCTION "pg_catalog"."int4recv"("internal"))
;
CREATE SCHEMA stats_import
;
-- Test NULL checks execution in domain types
CREATE DOMAIN sqljsonb_int_not_null AS int NOT NULL
;
-- range type doesn't fit

drop function polyf(x anycompatiblemultirange, y anycompatible, z anycompatible)
;
ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p2" FOR VALUES FROM (10) TO (20)
;
DROP TABLE pg_temp_11.pg_temp_26628_2, pg_temp_11.pg_temp_26628
;
CREATE FUNCTION isnne(ean13, ismn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE tab3 (a int PRIMARY KEY)
;
-- check some degenerate cases
create temp table t1 (f1 int, f2 int8)
;
alter table foo alter f1 TYPE varchar(10)
;
DROP TABLE few
;
alter table d_star set (parallel_workers = 0)
;
CREATE TEMP TABLE pg_temp_12.pg_temp_25379_2 (tid pg_catalog.tid)
;
DROP TABLE num_exp_ln
;
-- Add operators that are new in 9.1.  We do it like this, leaving them
-- "loose" in the operator family rather than bound into the opclass, because
-- that's the only state that can be reproduced during an upgrade from 9.0.
ALTER OPERATOR FAMILY gist_oid_ops USING gist ADD
	OPERATOR	6	<> (oid, oid) ,
	OPERATOR	15	<-> (oid, oid) FOR ORDER BY pg_catalog.oid_ops ,
	FUNCTION	8 (oid, oid) gbt_oid_distance (internal, oid, int2, oid, internal) ,
	-- Also add support function for index-only-scans, added in 9.5.
	FUNCTION	9 (oid, oid) gbt_oid_fetch (internal)
;
DROP TABLE upserttest
;
CREATE FUNCTION functest_IS_5(x int DEFAULT nextval('functest1'))
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT x'
;
DROP TABLE test_drop_constr_parent CASCADE
;
ALTER PUBLICATION testpub ADD TABLE t2
;
create unlogged table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=98) tablespace "regress_pgbench_tap_1_ts"
;
-- Deprecated function
CREATE FUNCTION show_limit()
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
CREATE TABLE test_char (
	i "char"
)
;
CREATE TEXT SEARCH CONFIGURATION dutch
	(PARSER = default)
;
-- For binary upgrade, set up typed tables this way.
ALTER TABLE ONLY "public"."tt0" OF "public"."tt_t0"
;
ALTER TABLE tbl2 ADD COLUMN val3 TEXT
;
-- ok

--
-- Test RETURNING OLD/NEW.
--
-- Start with new data, to ensure predictable TIDs.
--
TRUNCATE foo
;
CREATE FUNCTION spgist_name_config(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
/*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
CREATE FUNCTION cube_contained(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DO $$BEGIN EXECUTE format(
	'ALTER DATABASE %I OWNER TO regress_priv_group2', current_catalog); END$$
;
/* src/test/modules/test_extensions/test_ext_cine--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- CREATE IF NOT EXISTS is an entirely unsound thing for an extension
-- to be doing, but let's at least plug the major security hole in it.
--

CREATE COLLATION IF NOT EXISTS ext_cine_coll
  ( LC_COLLATE = "POSIX", LC_CTYPE = "POSIX" )
;
CREATE MATERIALIZED VIEW matview_stats_1 AS
  SELECT a AS col1, 4::int AS col2
    FROM generate_series(1, 5) AS tab(a) WHERE a < 4 AND a > 3
;
DROP FUNCTION IF EXISTS split_maint.add_amount(numeric, numeric)
;
DROP EVENT TRIGGER sql_drop_command
;
-- security barrier view on top of security barrier view

CREATE VIEW rw_view2 WITH (security_barrier = true) AS
  SELECT * FROM rw_view1 WHERE snoop(person)
;
-- OK
SECURITY LABEL ON COLUMN dummy_seclabel_tbl1 IS 'unclassified'
;
DROP POLICY pguc ON event_trigger_test
;
drop table graph0 cascade
;
CREATE TABLE public.test (a BIGINT, b TEXT, c TEXT)
;
DROP TABLE pg_temp_32.pg_temp_27252_2, pg_temp_32.pg_temp_27252
;
-- Computation of the stats fails, some data generated.
ANALYZE test_table
;
-- composite function with row expansion
create table test_composite(x integer)
;
CREATE SCHEMA test_ns_schema_2
       CREATE VIEW abc_view AS SELECT c FROM abc
;
do $$
declare
  fn_oid oid;
begin
  get diagnostics fn_oid = pg_routine_oid;
  raise notice 'pg_routine_oid = %', fn_oid;
end;
$$
;
CREATE INDEX rix ON road USING btree (name text_ops)
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] -- one may insert documents only authored by them
CREATE POLICY p2 ON document FOR INSERT WITH CHECK (dauthor = current_user)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE split_sequence_next.seq_full SET UNLOGGED
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_27606_2 ADD COLUMN newdata pg_temp_3.pg_temp_27606
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ean13, isbn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION explain_filter(text) RETURNS setof text
LANGUAGE plpgsql AS
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Remove the path portion of foreign file names
        ln := regexp_replace(ln, 'Foreign File: .*/([a-z.]+)$', 'Foreign File: .../\1');
        return next ln;
    end loop;
end;
$$
;
----------
[UNKNOWN] CREATE TRANSFORM FOR integer LANGUAGE "sql" (FROM SQL WITH FUNCTION "pg_catalog"."prsd_lextype"("internal"), TO SQL WITH FUNCTION "pg_catalog"."int4recv"("internal"))
;
----------
[CREATE_SCHEMA] CREATE SCHEMA stats_import
;
----------
[CREATE_TYPE] -- Test NULL checks execution in domain types
CREATE DOMAIN sqljsonb_int_not_null AS int NOT NULL
;
----------
[DROP_PROG_OBJ] -- range type doesn't fit

drop function polyf(x anycompatiblemultirange, y anycompatible, z anycompatible)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p2" FOR VALUES FROM (10) TO (20)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_26628_2, pg_temp_11.pg_temp_26628
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ean13, ismn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE tab3 (a int PRIMARY KEY)
;
----------
[CREATE_TABLE] -- check some degenerate cases
create temp table t1 (f1 int, f2 int8)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table foo alter f1 TYPE varchar(10)
;
----------
[DROP_TABLE] DROP TABLE few
;
----------
[ALTER_TABLE] alter table d_star set (parallel_workers = 0)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_25379_2 (tid pg_catalog.tid)
;
----------
[DROP_TABLE] DROP TABLE num_exp_ln
;
----------
[ALTER_PROG_OBJ] -- Add operators that are new in 9.1.  We do it like this, leaving them
-- "loose" in the operator family rather than bound into the opclass, because
-- that's the only state that can be reproduced during an upgrade from 9.0.
ALTER OPERATOR FAMILY gist_oid_ops USING gist ADD
	OPERATOR	6	<> (oid, oid) ,
	OPERATOR	15	<-> (oid, oid) FOR ORDER BY pg_catalog.oid_ops ,
	FUNCTION	8 (oid, oid) gbt_oid_distance (internal, oid, int2, oid, internal) ,
	-- Also add support function for index-only-scans, added in 9.5.
	FUNCTION	9 (oid, oid) gbt_oid_fetch (internal)
;
----------
[DROP_TABLE] DROP TABLE upserttest
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_IS_5(x int DEFAULT nextval('functest1'))
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT x'
;
----------
[DROP_TABLE] DROP TABLE test_drop_constr_parent CASCADE
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub ADD TABLE t2
;
----------
[CREATE_TABLE] create unlogged table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=98) tablespace "regress_pgbench_tap_1_ts"
;
----------
[CREATE_PROG_OBJ] -- Deprecated function
CREATE FUNCTION show_limit()
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test_char (
	i "char"
)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION dutch
	(PARSER = default)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up typed tables this way.
ALTER TABLE ONLY "public"."tt0" OF "public"."tt_t0"
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tbl2 ADD COLUMN val3 TEXT
;
----------
[TRUNCATE_TABLE] -- ok

--
-- Test RETURNING OLD/NEW.
--
-- Start with new data, to ensure predictable TIDs.
--
TRUNCATE foo
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION spgist_name_config(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_POLICY] /*
 * text search configuration for english language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * english and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_contained(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[BLOCK] DO $$BEGIN EXECUTE format(
	'ALTER DATABASE %I OWNER TO regress_priv_group2', current_catalog); END$$
;
----------
[CREATE_POLICY] /* src/test/modules/test_extensions/test_ext_cine--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- CREATE IF NOT EXISTS is an entirely unsound thing for an extension
-- to be doing, but let's at least plug the major security hole in it.
--

CREATE COLLATION IF NOT EXISTS ext_cine_coll
  ( LC_COLLATE = "POSIX", LC_CTYPE = "POSIX" )
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW matview_stats_1 AS
  SELECT a AS col1, 4::int AS col2
    FROM generate_series(1, 5) AS tab(a) WHERE a < 4 AND a > 3
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS split_maint.add_amount(numeric, numeric)
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER sql_drop_command
;
----------
[CREATE_VIEW(SELECT)] -- security barrier view on top of security barrier view

CREATE VIEW rw_view2 WITH (security_barrier = true) AS
  SELECT * FROM rw_view1 WHERE snoop(person)
;
----------
[UNKNOWN] -- OK
SECURITY LABEL ON COLUMN dummy_seclabel_tbl1 IS 'unclassified'
;
----------
[DROP_POLICY] DROP POLICY pguc ON event_trigger_test
;
----------
[DROP_TABLE] drop table graph0 cascade
;
----------
[CREATE_TABLE] CREATE TABLE public.test (a BIGINT, b TEXT, c TEXT)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_32.pg_temp_27252_2, pg_temp_32.pg_temp_27252
;
----------
[ADMIN_TABLE] -- Computation of the stats fails, some data generated.
ANALYZE test_table
;
----------
[CREATE_TABLE] -- composite function with row expansion
create table test_composite(x integer)
;
----------
[CREATE_SCHEMA(SELECT)] CREATE SCHEMA test_ns_schema_2
       CREATE VIEW abc_view AS SELECT c FROM abc
;
----------
[BLOCK] do $$
declare
  fn_oid oid;
begin
  get diagnostics fn_oid = pg_routine_oid;
  raise notice 'pg_routine_oid = %', fn_oid;
end;
$$
;
----------
[ADD_INDEX] CREATE INDEX rix ON road USING btree (name text_ops)
;
