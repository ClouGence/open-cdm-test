-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (force_not_null '*')
;
-- These views are left around mainly to exercise special cases in pg_dump.

CREATE TABLE view_base_table (key int PRIMARY KEY, data varchar(20))
;
alter table cc3 no inherit cc1
;
--
-- LSEG
-- Line segments
--

--DROP TABLE LSEG_TBL;
CREATE TABLE LSEG_TBL (s lseg)
;
drop table tabwithcols
;
ALTER OPERATOR <@ (ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
/*
 * text search configuration for serbian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * serbian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY serbian_stem
	(TEMPLATE = snowball, Language = serbian )
;
-- For binary upgrade, set up inherited constraints.
ALTER TABLE ONLY "public"."test_inh_check_child" ADD CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "asciihword" WITH "public"."hunspell_num", "english_stem"
;
DROP TABLE tt
;
/*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
ALTER TABLE tab_mixed DROP COLUMN b
;
-- infer anyelement from anyrange
create function inoutparam_succeed(out i anyelement, inout r anyrange)
  as $$ select upper($1), $1 $$ language sql
;
-- Don't remove this DROP, it exposes bug #15672
drop table at_partitioned
;
ALTER TABLE evttrig.two DROP COLUMN col_c
;
CREATE TABLE trunc_a21 PARTITION OF trunc_a2 FOR VALUES FROM (10) TO (12)
;
--
-- ISBN operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE RULE shoelace_ok_ins AS ON INSERT TO shoelace_ok
        DO INSTEAD
        UPDATE shoelace SET
               sl_avail = sl_avail + NEW.ok_quant
         WHERE sl_name = NEW.ok_name
;
create index parted_inval_idx on parted_inval_tab (a)
;
-- ok
ALTER INDEX dummy_test_idx SET (option_int = true)
;
ANALYZE pg_temp_14.pg_temp_25613_2
;
-- Check that indisclustered updates are preserved
CREATE TABLE concur_clustered(i int)
;
create function show_result_type(text) returns text language plpgsql as
$$
    declare
        r record;
        t text;
    begin
        execute $1 into r;
        select pg_typeof(r.a) into t;
        return format('type %s value %s', t, r.a::text);
    end;
$$
;
VACUUM (PARALLEL 2) pg_catalog.pg_user_mapping
;
-- substring fetches a slice of a toasted value; unused tail of that slice is
-- an incomplete char (bug #19406)
CREATE TABLE toast_3b_utf8 (c text)
;
-- Check that unknown literals are converted to "text" in CREATE MATVIEW,
-- so that we don't end up with unknown-type columns.
CREATE MATERIALIZED VIEW mv_unspecified_types AS
  SELECT 42 as i, 42.5 as num, 'foo' as u, 'foo'::unknown as u2, null as n
;
--
-- Test the SP-GiST index
--

CREATE TEMPORARY TABLE box_temp (f1 box)
;
CREATE VIEW xmlview5 AS SELECT xmlparse(content '<abc>x</abc>')
;
CREATE TRIGGER transition_table_level1_ri_parent_upd_trigger
  AFTER UPDATE ON transition_table_level1
  REFERENCING OLD TABLE AS d NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level1_ri_parent_upd_func()
;
VACUUM (FREEZE) pg_catalog.pg_partitioned_table
;
ALTER TABLE ONLY "public"."beta" ATTACH PARTITION "public"."beta_neg" FOR VALUES FROM ('-10') TO ('0')
;
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=1)
;
DROP OPERATOR CLASS test_operator_class USING btree
;
-- fail, no good with null

-- we want to work with a point_tbl that includes a null
CREATE TEMP TABLE point_tbl AS SELECT * FROM public.point_tbl
;
-- error, disallowed
ALTER TABLE itest4 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
ANALYZE public.d
;
create temp table t1 as select * from int8_tbl
;
alter table mlparted5_ab attach partition mlparted5_b for values in ('b')
;
DROP SCHEMA regress_rls_schema CASCADE
;
ALTER TABLE pg_toast.pg_toast_28613 RENAME TO reind_con_toast
;
CREATE FUNCTION isnle(ean13, isbn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER FUNCTION regtest_func_1(text) SET SCHEMA regtest_schema_2
;
DROP VIEW rule_v1
;
DROP VIEW timestamp_local_view
;
CREATE TABLE tab_part2(a int)
;
TRUNCATE TABLE trunc_c,trunc_d,trunc_e
;
-- Leave this table around, for pg_upgrade/pg_dump tests


-- Test creating a constraint at the parent that already exists in partitions.
-- There should be no duplicated constraints, and attempts to drop the
-- constraint in partitions should raise appropriate errors.
create schema fkpart0
  create table pkey (a int primary key)
  create table fk_part (a int) partition by list (a)
  create table fk_part_1 partition of fk_part
      (foreign key (a) references fkpart0.pkey) for values in (1)
  create table fk_part_23 partition of fk_part
      (foreign key (a) references fkpart0.pkey) for values in (2, 3)
      partition by list (a)
  create table fk_part_23_2 partition of fk_part_23 for values in (2)
;
CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE TABLE schema_not_existing.tab (id int)
;
CREATE TABLE vactst (i INT)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (force_not_null '*')
;
----------
[CREATE_TABLE] -- These views are left around mainly to exercise special cases in pg_dump.

CREATE TABLE view_base_table (key int PRIMARY KEY, data varchar(20))
;
----------
[ALTER_TABLE] alter table cc3 no inherit cc1
;
----------
[CREATE_TABLE] --
-- LSEG
-- Line segments
--

--DROP TABLE LSEG_TBL;
CREATE TABLE LSEG_TBL (s lseg)
;
----------
[DROP_TABLE] drop table tabwithcols
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <@ (ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_POLICY] /*
 * text search configuration for serbian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * serbian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY serbian_stem
	(TEMPLATE = snowball, Language = serbian )
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- For binary upgrade, set up inherited constraints.
ALTER TABLE ONLY "public"."test_inh_check_child" ADD CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "asciihword" WITH "public"."hunspell_num", "english_stem"
;
----------
[DROP_TABLE] DROP TABLE tt
;
----------
[CREATE_POLICY] /*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tab_mixed DROP COLUMN b
;
----------
[CREATE_PROG_OBJ] -- infer anyelement from anyrange
create function inoutparam_succeed(out i anyelement, inout r anyrange)
  as $$ select upper($1), $1 $$ language sql
;
----------
[DROP_TABLE] -- Don't remove this DROP, it exposes bug #15672
drop table at_partitioned
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE evttrig.two DROP COLUMN col_c
;
----------
[CREATE_TABLE] CREATE TABLE trunc_a21 PARTITION OF trunc_a2 FOR VALUES FROM (10) TO (12)
;
----------
[CREATE_PROG_OBJ] --
-- ISBN operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_POLICY] CREATE RULE shoelace_ok_ins AS ON INSERT TO shoelace_ok
        DO INSTEAD
        UPDATE shoelace SET
               sl_avail = sl_avail + NEW.ok_quant
         WHERE sl_name = NEW.ok_name
;
----------
[ADD_INDEX] create index parted_inval_idx on parted_inval_tab (a)
;
----------
[ALTER_INDEX] -- ok
ALTER INDEX dummy_test_idx SET (option_int = true)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_14.pg_temp_25613_2
;
----------
[CREATE_TABLE] -- Check that indisclustered updates are preserved
CREATE TABLE concur_clustered(i int)
;
----------
[CREATE_PROG_OBJ] create function show_result_type(text) returns text language plpgsql as
$$
    declare
        r record;
        t text;
    begin
        execute $1 into r;
        select pg_typeof(r.a) into t;
        return format('type %s value %s', t, r.a::text);
    end;
$$
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_user_mapping
;
----------
[CREATE_TABLE] -- substring fetches a slice of a toasted value; unused tail of that slice is
-- an incomplete char (bug #19406)
CREATE TABLE toast_3b_utf8 (c text)
;
----------
[CREATE_VIEW(SELECT)] -- Check that unknown literals are converted to "text" in CREATE MATVIEW,
-- so that we don't end up with unknown-type columns.
CREATE MATERIALIZED VIEW mv_unspecified_types AS
  SELECT 42 as i, 42.5 as num, 'foo' as u, 'foo'::unknown as u2, null as n
;
----------
[CREATE_TABLE] --
-- Test the SP-GiST index
--

CREATE TEMPORARY TABLE box_temp (f1 box)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmlview5 AS SELECT xmlparse(content '<abc>x</abc>')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER transition_table_level1_ri_parent_upd_trigger
  AFTER UPDATE ON transition_table_level1
  REFERENCING OLD TABLE AS d NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level1_ri_parent_upd_func()
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_partitioned_table
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."beta" ATTACH PARTITION "public"."beta_neg" FOR VALUES FROM ('-10') TO ('0')
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=1)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR CLASS test_operator_class USING btree
;
----------
[CREATE_TABLE|SELECT] -- fail, no good with null

-- we want to work with a point_tbl that includes a null
CREATE TEMP TABLE point_tbl AS SELECT * FROM public.point_tbl
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- error, disallowed
ALTER TABLE itest4 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
----------
[ADMIN_TABLE] ANALYZE public.d
;
----------
[CREATE_TABLE|SELECT] create temp table t1 as select * from int8_tbl
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table mlparted5_ab attach partition mlparted5_b for values in ('b')
;
----------
[DROP_SCHEMA] DROP SCHEMA regress_rls_schema CASCADE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28613 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ean13, isbn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION regtest_func_1(text) SET SCHEMA regtest_schema_2
;
----------
[DROP_VIEW] DROP VIEW rule_v1
;
----------
[DROP_VIEW] DROP VIEW timestamp_local_view
;
----------
[CREATE_TABLE] CREATE TABLE tab_part2(a int)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE trunc_c,trunc_d,trunc_e
;
----------
[CREATE_SCHEMA] -- Leave this table around, for pg_upgrade/pg_dump tests


-- Test creating a constraint at the parent that already exists in partitions.
-- There should be no duplicated constraints, and attempts to drop the
-- constraint in partitions should raise appropriate errors.
create schema fkpart0
  create table pkey (a int primary key)
  create table fk_part (a int) partition by list (a)
  create table fk_part_1 partition of fk_part
      (foreign key (a) references fkpart0.pkey) for values in (1)
  create table fk_part_23 partition of fk_part
      (foreign key (a) references fkpart0.pkey) for values in (2, 3)
      partition by list (a)
  create table fk_part_23_2 partition of fk_part_23 for values in (2)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE TABLE schema_not_existing.tab (id int)
;
----------
[CREATE_TABLE] CREATE TABLE vactst (i INT)
;
