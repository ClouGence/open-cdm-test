ANALYZE pg_temp_13.pg_temp_25481_2
;
DROP FUNCTION gf1(int)
;
CREATE INDEX intervalidx ON intervaltmp USING gist ( a )
;
create table defpart_attach_test_2 (like defpart_attach_test_d)
;
DROP SCHEMA IF EXISTS split_postgis CASCADE
;
-- RangeVarCallbackOwnsRelation()
CREATE INDEX pg_description_test_index ON pg_description (description)
;
DROP FOREIGN TABLE foreign_tbl CASCADE
;
ALTER PUBLICATION testpub_fortbl ADD TABLE testpub_tbl1
;
CALL test_proc10(_a, _b, _c)
;
create table c2 (constraint p2chk check (ff1 > 10) no inherit) inherits (p1)
;
ANALYZE pg_temp_3.pg_temp_27374_2
;
create or replace view my_locks as
select case when c.relname like 'pg_toast%' then 'pg_toast' else c.relname end, max(mode::lockmodes) as max_lockmode
from pg_locks l join pg_class c on l.relation = c.oid
where virtualtransaction = (
        select virtualtransaction
        from pg_locks
        where transactionid = pg_current_xact_id()::xid)
and locktype = 'relation'
and relnamespace != (select oid from pg_namespace where nspname = 'pg_catalog')
and c.relname != 'my_locks'
group by c.relname
;
CREATE FUNCTION fun_instead_of_insert_tbl() RETURNS trigger AS $$
BEGIN
  INSERT INTO instead_of_insert_tbl (name) VALUES (NEW.str);
  RETURN NULL;
END;
$$ LANGUAGE plpgsql
;
CREATE TABLE num_result (id1 int4, id2 int4, result numeric(1000,800))
;
ALTER TEXT SEARCH CONFIGURATION turkish ADD MAPPING
    FOR word, hword_part, hword
	WITH turkish_stem
;
CREATE OPERATOR CLASS varbit_ops
DEFAULT FOR TYPE varbit USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       varbitcmp(varbit,varbit),
    FUNCTION        2       gin_extract_value_varbit(varbit, internal),
    FUNCTION        3       gin_extract_query_varbit(varbit, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_varbit(varbit,varbit,int2, internal),
STORAGE         varbit
;
CREATE TRIGGER footrg_ondel BEFORE DELETE ON footrg1
   FOR EACH ROW EXECUTE PROCEDURE func_footrg()
;
ALTER TABLE pg_temp_3.pg_temp_29533_2 ADD COLUMN newdata pg_temp_3.pg_temp_29533
;
-- partial analyze doesn't build stats either
ANALYZE ab1 (a)
;
CREATE FUNCTION test_argresult_nnint(x nnint, y int) RETURNS nnint AS $$
begin
return y;
end
$$ LANGUAGE plpgsql
;
CREATE VIEW tableam_view_heapx AS SELECT * FROM tableam_tbl_heapx
;
CREATE TABLE "public"."alpha_neg_p1" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
VACUUM (ANALYZE) pg_catalog.pg_depend
;
-- all triggers should fire
drop table parted_trigger
;
CREATE TABLE "public"."itest11" (
    "a" integer NOT NULL,
    "b" "text"
)
;
truncate table parted_copytest
;
CREATE FUNCTION "public"."ret_query2"("lim" integer) RETURNS SETOF "public"."record_type"
    LANGUAGE "plpgsql"
    AS $$
begin
    return query select md5(s.x::text), s.x, s.x > 0
                 from generate_series(-8, lim) s (x) where s.x % 2 = 0;
end;
$$
;
-- ======================================================
-- More row filter tests for validating column references
CREATE TABLE rf_tbl_abcd_nopk(a int, b int, c int, d int)
;
drop table inserttest3
;
alter table join_bar set (parallel_workers = 2)
;
DROP POLICY p ON t
;
ALTER TABLE split_alter_table.base_table ENABLE TRIGGER USER
;
create temp table prtx1_3 partition of prtx1 for values from (21) to (31)
;
CREATE VIEW mysecview6 WITH (invalid_option)		-- Error
       AS SELECT * FROM tbl1 WHERE a < 100
;
/* These functions are now in the backend and callers should update to use those */

DROP FUNCTION pg_file_read(text, bigint, bigint)
;
VACUUM ddl_target
;
ALTER TEXT SEARCH CONFIGURATION lithuanian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
create function returnsrecord(int) returns record language plpgsql as
$$ begin return row($1,$1+1); end $$
;
drop table history
;
ALTER TABLE ONLY "public"."pitest1" ATTACH PARTITION "public"."pitest1_p2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
alter table constr_child2 validate constraint constr_parent2_a_not_null
;
create table mc3p0 partition of mc3p for values from (minvalue, minvalue, minvalue) to (1, 1, 1)
;
ALTER TABLE test_foreign_constraints DROP CONSTRAINT test_foreign_constraints_id1_fkey
;
CREATE TABLE test_io_vac_strategy(a int, b int) WITH (autovacuum_enabled = 'false')
;
create table base_tab (a serial, b int[], c text, d text default 'Table default')
;
create table pc_list_part_1 partition of pc_list_parted for values in (1)
;
create type testtype3 as (a int, b text)
;
CREATE SCHEMA TESTLIBPQ2
;
/* src/test/modules/test_parser/test_parser--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION testprs_start(internal, int4)
RETURNS internal
AS '$libdir/test_parser'
LANGUAGE C STRICT
;
-- fail
DROP COLLATION IF EXISTS test0
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25481_2
;
----------
[DROP_PROG_OBJ] DROP FUNCTION gf1(int)
;
----------
[ADD_INDEX] CREATE INDEX intervalidx ON intervaltmp USING gist ( a )
;
----------
[CREATE_TABLE] create table defpart_attach_test_2 (like defpart_attach_test_d)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_postgis CASCADE
;
----------
[ADD_INDEX] -- RangeVarCallbackOwnsRelation()
CREATE INDEX pg_description_test_index ON pg_description (description)
;
----------
[DROP_TABLE] DROP FOREIGN TABLE foreign_tbl CASCADE
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_fortbl ADD TABLE testpub_tbl1
;
----------
[CALL_PROG_OBJ] CALL test_proc10(_a, _b, _c)
;
----------
[CREATE_TABLE] create table c2 (constraint p2chk check (ff1 > 10) no inherit) inherits (p1)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27374_2
;
----------
[CREATE_VIEW(SELECT)] create or replace view my_locks as
select case when c.relname like 'pg_toast%' then 'pg_toast' else c.relname end, max(mode::lockmodes) as max_lockmode
from pg_locks l join pg_class c on l.relation = c.oid
where virtualtransaction = (
        select virtualtransaction
        from pg_locks
        where transactionid = pg_current_xact_id()::xid)
and locktype = 'relation'
and relnamespace != (select oid from pg_namespace where nspname = 'pg_catalog')
and c.relname != 'my_locks'
group by c.relname
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fun_instead_of_insert_tbl() RETURNS trigger AS $$
BEGIN
  INSERT INTO instead_of_insert_tbl (name) VALUES (NEW.str);
  RETURN NULL;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE num_result (id1 int4, id2 int4, result numeric(1000,800))
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION turkish ADD MAPPING
    FOR word, hword_part, hword
	WITH turkish_stem
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS varbit_ops
DEFAULT FOR TYPE varbit USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       varbitcmp(varbit,varbit),
    FUNCTION        2       gin_extract_value_varbit(varbit, internal),
    FUNCTION        3       gin_extract_query_varbit(varbit, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_varbit(varbit,varbit,int2, internal),
STORAGE         varbit
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER footrg_ondel BEFORE DELETE ON footrg1
   FOR EACH ROW EXECUTE PROCEDURE func_footrg()
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_29533_2 ADD COLUMN newdata pg_temp_3.pg_temp_29533
;
----------
[ADMIN_TABLE] -- partial analyze doesn't build stats either
ANALYZE ab1 (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_argresult_nnint(x nnint, y int) RETURNS nnint AS $$
begin
return y;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW tableam_view_heapx AS SELECT * FROM tableam_tbl_heapx
;
----------
[CREATE_TABLE] CREATE TABLE "public"."alpha_neg_p1" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_depend
;
----------
[DROP_TABLE] -- all triggers should fire
drop table parted_trigger
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest11" (
    "a" integer NOT NULL,
    "b" "text"
)
;
----------
[TRUNCATE_TABLE] truncate table parted_copytest
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ret_query2"("lim" integer) RETURNS SETOF "public"."record_type"
    LANGUAGE "plpgsql"
    AS $$
begin
    return query select md5(s.x::text), s.x, s.x > 0
                 from generate_series(-8, lim) s (x) where s.x % 2 = 0;
end;
$$
;
----------
[CREATE_TABLE] -- ======================================================
-- More row filter tests for validating column references
CREATE TABLE rf_tbl_abcd_nopk(a int, b int, c int, d int)
;
----------
[DROP_TABLE] drop table inserttest3
;
----------
[ALTER_TABLE] alter table join_bar set (parallel_workers = 2)
;
----------
[DROP_POLICY] DROP POLICY p ON t
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table ENABLE TRIGGER USER
;
----------
[CREATE_TABLE] create temp table prtx1_3 partition of prtx1 for values from (21) to (31)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mysecview6 WITH (invalid_option)		-- Error
       AS SELECT * FROM tbl1 WHERE a < 100
;
----------
[DROP_PROG_OBJ] /* These functions are now in the backend and callers should update to use those */

DROP FUNCTION pg_file_read(text, bigint, bigint)
;
----------
[ADMIN_TABLE] VACUUM ddl_target
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION lithuanian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_PROG_OBJ] create function returnsrecord(int) returns record language plpgsql as
$$ begin return row($1,$1+1); end $$
;
----------
[DROP_TABLE] drop table history
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pitest1" ATTACH PARTITION "public"."pitest1_p2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] alter table constr_child2 validate constraint constr_parent2_a_not_null
;
----------
[CREATE_TABLE] create table mc3p0 partition of mc3p for values from (minvalue, minvalue, minvalue) to (1, 1, 1)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE test_foreign_constraints DROP CONSTRAINT test_foreign_constraints_id1_fkey
;
----------
[CREATE_TABLE] CREATE TABLE test_io_vac_strategy(a int, b int) WITH (autovacuum_enabled = 'false')
;
----------
[CREATE_TABLE] create table base_tab (a serial, b int[], c text, d text default 'Table default')
;
----------
[CREATE_TABLE] create table pc_list_part_1 partition of pc_list_parted for values in (1)
;
----------
[CREATE_TYPE] create type testtype3 as (a int, b text)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA TESTLIBPQ2
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_parser/test_parser--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION testprs_start(internal, int4)
RETURNS internal
AS '$libdir/test_parser'
LANGUAGE C STRICT
;
----------
[DROP_POLICY] -- fail
DROP COLLATION IF EXISTS test0
;
