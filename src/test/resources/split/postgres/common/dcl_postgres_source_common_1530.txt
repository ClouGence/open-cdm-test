REASSIGN OWNED BY regress_dep_user0 TO regress_dep_user1
;
-- Extended statistics with an expression that cannot be analyzed.
CREATE STATISTICS test_stats ON func_int_custom(data), (id) FROM test_table
;
EXECUTE dumpDomain('31027')
;
DROP USER MAPPING IF EXISTS FOR CURRENT_USER SERVER split_dummy_server_renamed
;
ALTER TABLE public.alpha_neg OWNER TO zyc
;
-- Connections are not closed at the end of the alter and drop statements.
-- That's because the connections are in midst of this xact,
-- they are just marked as invalid in pgfdw_inval_callback.
ALTER SERVER loopback OPTIONS (ADD use_remote_estimate 'off')
;
EXECUTE dumpFunc('35617')
;
ALTER TABLE public.test_type_diff2_c1 OWNER TO zyc
;
ALTER FOREIGN DATA WRAPPER foo OWNER TO regress_unprivileged_role
;
EXECUTE dumpTableAttach('41788')
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] REASSIGN OWNED BY regress_dep_user0 TO regress_dep_user1
;
----------
[ADMIN_PERFORMANCE] -- Extended statistics with an expression that cannot be analyzed.
CREATE STATISTICS test_stats ON func_int_custom(data), (id) FROM test_table
;
----------
[UNSAFE] EXECUTE dumpDomain('31027')
;
----------
[REVOKE] DROP USER MAPPING IF EXISTS FOR CURRENT_USER SERVER split_dummy_server_renamed
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.alpha_neg OWNER TO zyc
;
----------
[SYSTEM_SETTING_WRITE] -- Connections are not closed at the end of the alter and drop statements.
-- That's because the connections are in midst of this xact,
-- they are just marked as invalid in pgfdw_inval_callback.
ALTER SERVER loopback OPTIONS (ADD use_remote_estimate 'off')
;
----------
[UNSAFE] EXECUTE dumpFunc('35617')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.test_type_diff2_c1 OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER FOREIGN DATA WRAPPER foo OWNER TO regress_unprivileged_role
;
----------
[UNSAFE] EXECUTE dumpTableAttach('41788')
;
