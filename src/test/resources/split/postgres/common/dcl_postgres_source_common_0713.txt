REINDEX TABLE concur_reindex_part_index
;
EXECUTE dumpOpr('18281')
;
EXECUTE dumpFunc('39127')
;
set time zone '+00'
;
LOCK TABLE "public"."num_exp_div" IN ACCESS SHARE MODE
;
LOCK TABLE fkpart4.droppk IN ACCESS SHARE MODE
;
EXECUTE dumpTableAttach('38708')
;
-- The "ugly" case: increasing the number of batches during execution
-- doesn't help, so stop trying to fit in work_mem and hope for the
-- best; in this case we plan for 1 batch, increases just once and
-- then stop increasing because that didn't help at all, so we blow
-- right through the work_mem budget and hope for the best...

-- non-parallel
savepoint settings
;
EXECUTE dumpOpr('3521')
;
ALTER TABLE public.mvtest_tvv OWNER TO zyc
;
------------------------------------------------------------------------------------------
[ADMIN] REINDEX TABLE concur_reindex_part_index
;
----------
[UNSAFE] EXECUTE dumpOpr('18281')
;
----------
[UNSAFE] EXECUTE dumpFunc('39127')
;
----------
[SESSION_SETTING_WRITE] set time zone '+00'
;
----------
[SESSION_LOCK] LOCK TABLE "public"."num_exp_div" IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE fkpart4.droppk IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpTableAttach('38708')
;
----------
[TRANSACTION] -- The "ugly" case: increasing the number of batches during execution
-- doesn't help, so stop trying to fit in work_mem and hope for the
-- best; in this case we plan for 1 batch, increases just once and
-- then stop increasing because that didn't help at all, so we blow
-- right through the work_mem budget and hope for the best...

-- non-parallel
savepoint settings
;
----------
[UNSAFE] EXECUTE dumpOpr('3521')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.mvtest_tvv OWNER TO zyc
;
