LOCK TABLE public.brinopers IN ACCESS SHARE MODE
;
CREATE STATISTICS sts_sch2.pass1 ON a, b FROM sts_sch1.tbl
;
COMMIT PREPARED 'test_prepared_tab_full'
;
COPY public.conf_tab_2 (a, b, c) TO STDOUT
;
-- Test that decoding happens at PREPARE time when two-phase-commit is enabled.
-- Decoding after COMMIT PREPARED must have all the commands in the transaction.
BEGIN
;
DROP ROLE regress_nosuch_replication_bypassrls
;
COPY testxmlschema.test1 (a, b) TO stdout
;
CREATE ROLE split_priv_role
;
EXECUTE dumpDomain('41573')
;
LOCK TABLE public.point_tbl IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[SESSION_LOCK] LOCK TABLE public.brinopers IN ACCESS SHARE MODE
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS sts_sch2.pass1 ON a, b FROM sts_sch1.tbl
;
----------
[TRANSACTION] COMMIT PREPARED 'test_prepared_tab_full'
;
----------
[DATA_EXPORT] COPY public.conf_tab_2 (a, b, c) TO STDOUT
;
----------
[TRANSACTION] -- Test that decoding happens at PREPARE time when two-phase-commit is enabled.
-- Decoding after COMMIT PREPARED must have all the commands in the transaction.
BEGIN
;
----------
[DROP_ROLE] DROP ROLE regress_nosuch_replication_bypassrls
;
----------
[DATA_EXPORT] COPY testxmlschema.test1 (a, b) TO stdout
;
----------
[CREATE_ROLE] CREATE ROLE split_priv_role
;
----------
[UNSAFE] EXECUTE dumpDomain('41573')
;
----------
[SESSION_LOCK] LOCK TABLE public.point_tbl IN ACCESS SHARE MODE
;
