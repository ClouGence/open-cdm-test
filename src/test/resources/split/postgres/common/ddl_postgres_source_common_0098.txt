VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_rewrite
;
-- update itself moves tuple to new partition; trigger still works
truncate table parted
;
--
  -- Setup to test concurrent handling of GetTupleForTrigger().
  --
  CREATE TABLE footrg (a int, b text) PARTITION BY LIST(a)
;
-- Doing it twice doesn't create a redundant constraint
ALTER TABLE notnull_tbl1 ALTER a SET NOT NULL
;
create index idxpart1_tst2 on idxpart1 using hash (a)
;
ALTER FUNCTION functest_E_2(int) STABLE
;
TRUNCATE ndistinct
;
CREATE SUBSCRIPTION sub3 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub3
;
-- fail
CREATE TABLESPACE regress_tblspacewith LOCATION '' WITH (random_page_cost = 3.0)
;
ANALYZE public.brintest
;
CREATE TABLE prt2 (a int, b int, c varchar) PARTITION BY RANGE(b)
;
-- WITH CHECK OPTION with BEFORE trigger on base table

CREATE TABLE base_tbl (a int, b int)
;
ANALYZE pg_catalog.pg_database
;
CREATE TABLE hash_parted2 (
	a varchar
) PARTITION BY HASH (a)
;
CREATE UNIQUE INDEX concur_replident_i_idx ON concur_replident(i)
;
create table idxpart1 partition of idxpart for values from (0) to (10)
;
DROP TABLE gtest2
;
create table mcrparted2 partition of mcrparted for values from (10, 6, minvalue) to (10, maxvalue, maxvalue)
;
VACUUM (PARALLEL 0) pg_catalog.pg_authid
;
CREATE TABLE sch2.tbl1_part1 PARTITION OF sch1.tbl1 FOR VALUES FROM (1) to (10)
;
ALTER TABLE fd_pt1 ALTER COLUMN c5 DROP DEFAULT
;
CREATE TEMPORARY TABLE unlogged2 (a int primary key)
;
drop table minmaxtest cascade
;
--
-- Foreign keys and partitioned tables
--

-- Creation of a partitioned hierarchy with irregular definitions
CREATE TABLE fk_notpartitioned_pk (fdrop1 int, a int, fdrop2 int, b int,
  PRIMARY KEY (a, b))
;
CREATE TABLE gtest20c (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
create operator class part_test_text_ops for type text using hash as
    operator 1 =,
    function 2 part_hashtext_length(text, int8)
;
DROP FOREIGN TABLE ctl_foreign_table1
;
CREATE SUBSCRIPTION tap_sub_B_C
	CONNECTION 'port=31668 host=/tmp/EP7K8mZqpu dbname=postgres application_name=tap_sub_B_C'
	PUBLICATION tap_pub_C
	WITH (origin = none)
;
CREATE POLICY p1 ON r1 FOR SELECT USING (a < 20)
;
CREATE TABLE pub_test1.tbl1 (id serial primary key, data text)
;
create index gist_pointidx3 on gist_point_tbl using gist(p) with (buffering = off)
;
CREATE FUNCTION isneq(ean13, isbn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- fail - system table
CREATE PUBLICATION testpub_forsystemtbl FOR TABLE pg_publication
;
CREATE PROCEDURE test_proc7(x int, INOUT a int, INOUT b numeric)
LANGUAGE plpgsql
AS $$
BEGIN
IF x > 1 THEN
    a := x / 10;
    b := x / 2;
    CALL test_proc7(b::int, a, b);
END IF;
END;
$$
;
create trigger my_table_col_update_trig
  after update of b on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
CREATE RULE account_delete_multi AS ON DELETE TO split_trigger_probe.account_audit
    DO ALSO (
        NOTIFY split_trigger_probe_channel;
        INSERT INTO split_trigger_probe.account_audit(id, action) VALUES (OLD.id, 'deleted')
    )
;
CREATE FUNCTION "public"."tg_chkslotname"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if substr(new.slotname, 1, 2) != tg_argv[0] then
        raise exception 'slotname must begin with %', tg_argv[0];
    end if;
    return new;
end;
$$
;
DROP TABLE IF EXISTS temporal_no_key
;
create table parted_1 partition of parted for values in (1)
  partition by list (b)
;
ALTER TABLE regtest_ptable ALTER p SET STATISTICS -1
;
drop function least_accum(anyelement, variadic anyarray)
;
ALTER OPERATOR FAMILY gist__intbig_ops USING gist
DROP OPERATOR 8 (_int4, _int4)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext
;
CREATE TABLE dump_test.test_sixth_table (
						   col1 int,
						   col2 text,
						   col3 bytea
					   )
;
CREATE TABLE list_part1  PARTITION OF list_parted for VALUES in ('a', 'b')
;
create table mc2p5 partition of mc2p for values from (2, maxvalue) to (maxvalue, maxvalue)
;
alter table mlparted drop constraint check_b
;
--
-- Check that statement triggers work correctly even with all children excluded
--

create table stmt_trig_on_empty_upd (a int)
;
CREATE TABLE UNIQUE_TBL (i int UNIQUE, t text)
;
/*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_rewrite
;
----------
[TRUNCATE_TABLE] -- update itself moves tuple to new partition; trigger still works
truncate table parted
;
----------
[CREATE_TABLE] --
  -- Setup to test concurrent handling of GetTupleForTrigger().
  --
  CREATE TABLE footrg (a int, b text) PARTITION BY LIST(a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Doing it twice doesn't create a redundant constraint
ALTER TABLE notnull_tbl1 ALTER a SET NOT NULL
;
----------
[ADD_INDEX] create index idxpart1_tst2 on idxpart1 using hash (a)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_E_2(int) STABLE
;
----------
[TRUNCATE_TABLE] TRUNCATE ndistinct
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub3 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub3
;
----------
[CREATE_TABLESPACE] -- fail
CREATE TABLESPACE regress_tblspacewith LOCATION '' WITH (random_page_cost = 3.0)
;
----------
[ADMIN_TABLE] ANALYZE public.brintest
;
----------
[CREATE_TABLE] CREATE TABLE prt2 (a int, b int, c varchar) PARTITION BY RANGE(b)
;
----------
[CREATE_TABLE] -- WITH CHECK OPTION with BEFORE trigger on base table

CREATE TABLE base_tbl (a int, b int)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_database
;
----------
[CREATE_TABLE] CREATE TABLE hash_parted2 (
	a varchar
) PARTITION BY HASH (a)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX concur_replident_i_idx ON concur_replident(i)
;
----------
[CREATE_TABLE] create table idxpart1 partition of idxpart for values from (0) to (10)
;
----------
[DROP_TABLE] DROP TABLE gtest2
;
----------
[CREATE_TABLE] create table mcrparted2 partition of mcrparted for values from (10, 6, minvalue) to (10, maxvalue, maxvalue)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_authid
;
----------
[CREATE_TABLE] CREATE TABLE sch2.tbl1_part1 PARTITION OF sch1.tbl1 FOR VALUES FROM (1) to (10)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c5 DROP DEFAULT
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE unlogged2 (a int primary key)
;
----------
[DROP_TABLE] drop table minmaxtest cascade
;
----------
[CREATE_TABLE] --
-- Foreign keys and partitioned tables
--

-- Creation of a partitioned hierarchy with irregular definitions
CREATE TABLE fk_notpartitioned_pk (fdrop1 int, a int, fdrop2 int, b int,
  PRIMARY KEY (a, b))
;
----------
[CREATE_TABLE] CREATE TABLE gtest20c (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_PROG_OBJ] create operator class part_test_text_ops for type text using hash as
    operator 1 =,
    function 2 part_hashtext_length(text, int8)
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ctl_foreign_table1
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C
	CONNECTION 'port=31668 host=/tmp/EP7K8mZqpu dbname=postgres application_name=tap_sub_B_C'
	PUBLICATION tap_pub_C
	WITH (origin = none)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON r1 FOR SELECT USING (a < 20)
;
----------
[CREATE_TABLE] CREATE TABLE pub_test1.tbl1 (id serial primary key, data text)
;
----------
[ADD_INDEX] create index gist_pointidx3 on gist_point_tbl using gist(p) with (buffering = off)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ean13, isbn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] -- fail - system table
CREATE PUBLICATION testpub_forsystemtbl FOR TABLE pg_publication
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc7(x int, INOUT a int, INOUT b numeric)
LANGUAGE plpgsql
AS $$
BEGIN
IF x > 1 THEN
    a := x / 10;
    b := x / 2;
    CALL test_proc7(b::int, a, b);
END IF;
END;
$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_table_col_update_trig
  after update of b on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[CREATE_POLICY] CREATE RULE account_delete_multi AS ON DELETE TO split_trigger_probe.account_audit
    DO ALSO (
        NOTIFY split_trigger_probe_channel;
        INSERT INTO split_trigger_probe.account_audit(id, action) VALUES (OLD.id, 'deleted')
    )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_chkslotname"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if substr(new.slotname, 1, 2) != tg_argv[0] then
        raise exception 'slotname must begin with %', tg_argv[0];
    end if;
    return new;
end;
$$
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS temporal_no_key
;
----------
[CREATE_TABLE] create table parted_1 partition of parted for values in (1)
  partition by list (b)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_ptable ALTER p SET STATISTICS -1
;
----------
[DROP_PROG_OBJ] drop function least_accum(anyelement, variadic anyarray)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist__intbig_ops USING gist
DROP OPERATOR 8 (_int4, _int4)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_sixth_table (
						   col1 int,
						   col2 text,
						   col3 bytea
					   )
;
----------
[CREATE_TABLE] CREATE TABLE list_part1  PARTITION OF list_parted for VALUES in ('a', 'b')
;
----------
[CREATE_TABLE] create table mc2p5 partition of mc2p for values from (2, maxvalue) to (maxvalue, maxvalue)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table mlparted drop constraint check_b
;
----------
[CREATE_TABLE] --
-- Check that statement triggers work correctly even with all children excluded
--

create table stmt_trig_on_empty_upd (a int)
;
----------
[CREATE_TABLE] CREATE TABLE UNIQUE_TBL (i int UNIQUE, t text)
;
----------
[CREATE_POLICY] /*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
