CREATE UNIQUE INDEX num_exp_div_idx ON num_exp_div (id1, id2)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=27835 host=/tmp/rQ9EvTuOLI dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
DROP FUNCTION foo(INT)
;
do $$
declare r1 two_int8s; r2 two_int8s; r3 record; r4 record;
begin
  r1 := row(1,2);
  raise notice 'r1 = %', r1;
  r1 := r1;  -- shouldn't do anything
  raise notice 'r1 = %', r1;
  r2 := r1;
  raise notice 'r1 = %', r1;
  raise notice 'r2 = %', r2;
  r2.q2 = r1.q1 + 3;  -- check that r2 has distinct storage
  raise notice 'r1 = %', r1;
  raise notice 'r2 = %', r2;
  r1 := null;
  raise notice 'r1 = %', r1;
  raise notice 'r2 = %', r2;
  r1 := row(7,11)::two_int8s;
  r2 := r1;
  raise notice 'r1 = %', r1;
  raise notice 'r2 = %', r2;
  r3 := row(1,2);
  r4 := r3;
  raise notice 'r3 = %', r3;
  raise notice 'r4 = %', r4;
  r4.f1 := r4.f1 + 3;  -- check that r4 has distinct storage
  raise notice 'r3 = %', r3;
  raise notice 'r4 = %', r4;
  r1 := r3;
  raise notice 'r1 = %', r1;
  r4 := r1;
  raise notice 'r4 = %', r4;
  r4.q2 := r4.q2 + 1;  -- r4's field names have changed
  raise notice 'r4 = %', r4;
end$$
;
-- successes
DROP INDEX CONCURRENTLY IF EXISTS "concur_index3"
;
CREATE FUNCTION test_radixtree()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE TRIGGER before_del_stmt_trig BEFORE DELETE ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('before_view_del_stmt')
;
TRUNCATE trunc_stats_test3
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_amproc
;
ALTER TABLE testschema.test_default_tab_p ADD CONSTRAINT test_index4 UNIQUE (id) USING INDEX TABLESPACE regress_tblspace
;
/* contrib/btree_gist/btree_gist--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- This upgrade script marks all btree_gist functions as parallel safe.

-- Input/output functions for GiST key types (gbtreekey*)
ALTER FUNCTION gbtreekey4_in(cstring) PARALLEL SAFE
;
create table part_def partition of range_parted default
;
truncate trunc_trigger_log
;
ALTER TABLE pg_temp_3.pg_temp_29539_2 ADD COLUMN newdata pg_temp_3.pg_temp_29539
;
create temp view overview as
  select f1::information_schema.sql_identifier as sqli, f2 from undername
;
-- test search for "not equals"

CREATE TABLE test_ne (
   a  TIMESTAMP,
   b  NUMERIC
)
;
-- error
alter table temp_part_parent attach partition temp_part_child default
;
CREATE TABLE "public"."bt_f8_heap" (
    "seqno" double precision,
    "random" integer
)
;
create table inhcld(f2 name, f1 int)
;
ANALYZE pg_temp_14.pg_temp_27052_2
;
VACUUM (PARALLEL -1) pvactst
;
create table base_tab_def (a int, b text default 'Table default',
                           c text default 'Table default', d text, e text)
;
CREATE FUNCTION functest_srf0() RETURNS SETOF int
LANGUAGE SQL
AS $$ SELECT i FROM generate_series(1, 100) i $$
;
CREATE INDEX hash_pvactst ON pvactst USING hash (i)
;
ALTER TABLE regtest_table DROP COLUMN y
;
CREATE TABLESPACE ts1 LOCATION '/tmp/xcFVXkeJJZ'
;
VACUUM hs2
;
ANALYZE public.pagg_tab_p2
;
VACUUM (FREEZE) pg_catalog.pg_user_mapping
;
alter table cc no inherit bc
;
ANALYZE public.dedup_unique_test_table
;
-- fails
ALTER TYPE test_type2 ADD ATTRIBUTE c text CASCADE
;
/*
 * 5.14
 * CHECK_CONSTRAINTS view
 */

CREATE VIEW check_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(substring(pg_get_constraintdef(con.oid) from 7) AS character_data)
             AS check_clause
    FROM pg_constraint con
           LEFT OUTER JOIN pg_namespace rs ON (rs.oid = con.connamespace)
           LEFT OUTER JOIN pg_class c ON (c.oid = con.conrelid)
           LEFT OUTER JOIN pg_type t ON (t.oid = con.contypid)
    WHERE pg_has_role(coalesce(c.relowner, t.typowner), 'USAGE')
      AND con.contype = 'c'

    UNION
    -- not-null constraints

    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(n.nspname AS sql_identifier) AS constraint_schema,
           CAST(CAST(n.oid AS text) || '_' || CAST(r.oid AS text) || '_' || CAST(a.attnum AS text) || '_not_null' AS sql_identifier) AS constraint_name, -- XXX
           CAST(a.attname || ' IS NOT NULL' AS character_data)
             AS check_clause
    FROM pg_namespace n, pg_class r, pg_attribute a
    WHERE n.oid = r.relnamespace
      AND r.oid = a.attrelid
      AND a.attnum > 0
      AND NOT a.attisdropped
      AND a.attnotnull
      AND r.relkind IN ('r', 'p')
      AND pg_has_role(r.relowner, 'USAGE')
;
ALTER OPERATOR FAMILY gist_interval_ops USING gist ADD
	OPERATOR	6	<> (interval, interval) ,
	OPERATOR	15	<-> (interval, interval) FOR ORDER BY pg_catalog.interval_ops ,
	FUNCTION	8 (interval, interval) gbt_intv_distance (internal, interval, int2, oid, internal) ,
	FUNCTION	9 (interval, interval) gbt_intv_fetch (internal)
;
ALTER FUNCTION gbt_uuid_fetch(internal) PARALLEL SAFE
;
CREATE FUNCTION isngt(ismn, ismn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- Test that partial indexes have all pages, including empty ones.
CREATE TABLE test2 (a int)
;
drop index idxpart0_pkey
;
create trigger loct1_br_insert_trigger before insert on loct1
	for each row execute procedure br_insert_trigfunc()
;
DROP TABLE pg_temp_13.pg_temp_26669_2, pg_temp_13.pg_temp_26669
;
CREATE TABLE itest12 OF itest_type (f1 WITH OPTIONS GENERATED ALWAYS AS IDENTITY)
;
-- Check variable scoping -- a var is not available in its own or prior
-- default expressions, but it is available in later ones.

do $$
declare x int := x + 1;  -- error
begin
  raise notice 'x = %', x;
end;
$$
;
create table inh_parent(f1 int)
;
CREATE FUNCTION gtrgm_picksplit(internal, internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
VACUUM (PARALLEL 2) pg_catalog.pg_transform
;
create table ab_a2_b1 partition of ab_a2 for values in (1)
;
create table defcheck_1 partition of defcheck for values in (1, null)
;
-- not same schema
CREATE TABLE sequence_test_table (a int)
;
-- Again, with a schema name and a role specification.
CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE SEQUENCE schema_not_existing.seq
;
CREATE FUNCTION "public"."mr_table_succeed"("i" "anyelement", "r" "anymultirange") RETURNS TABLE("i" "anyelement", "r" "anymultirange")
    LANGUAGE "sql"
    AS $_$ select $1, $2 $_$
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE UNIQUE INDEX num_exp_div_idx ON num_exp_div (id1, id2)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=27835 host=/tmp/rQ9EvTuOLI dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION foo(INT)
;
----------
[BLOCK] do $$
declare r1 two_int8s; r2 two_int8s; r3 record; r4 record;
begin
  r1 := row(1,2);
  raise notice 'r1 = %', r1;
  r1 := r1;  -- shouldn't do anything
  raise notice 'r1 = %', r1;
  r2 := r1;
  raise notice 'r1 = %', r1;
  raise notice 'r2 = %', r2;
  r2.q2 = r1.q1 + 3;  -- check that r2 has distinct storage
  raise notice 'r1 = %', r1;
  raise notice 'r2 = %', r2;
  r1 := null;
  raise notice 'r1 = %', r1;
  raise notice 'r2 = %', r2;
  r1 := row(7,11)::two_int8s;
  r2 := r1;
  raise notice 'r1 = %', r1;
  raise notice 'r2 = %', r2;
  r3 := row(1,2);
  r4 := r3;
  raise notice 'r3 = %', r3;
  raise notice 'r4 = %', r4;
  r4.f1 := r4.f1 + 3;  -- check that r4 has distinct storage
  raise notice 'r3 = %', r3;
  raise notice 'r4 = %', r4;
  r1 := r3;
  raise notice 'r1 = %', r1;
  r4 := r1;
  raise notice 'r4 = %', r4;
  r4.q2 := r4.q2 + 1;  -- r4's field names have changed
  raise notice 'r4 = %', r4;
end$$
;
----------
[DROP_INDEX] -- successes
DROP INDEX CONCURRENTLY IF EXISTS "concur_index3"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_radixtree()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER before_del_stmt_trig BEFORE DELETE ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('before_view_del_stmt')
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_stats_test3
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_amproc
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testschema.test_default_tab_p ADD CONSTRAINT test_index4 UNIQUE (id) USING INDEX TABLESPACE regress_tblspace
;
----------
[ALTER_PROG_OBJ] /* contrib/btree_gist/btree_gist--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- This upgrade script marks all btree_gist functions as parallel safe.

-- Input/output functions for GiST key types (gbtreekey*)
ALTER FUNCTION gbtreekey4_in(cstring) PARALLEL SAFE
;
----------
[CREATE_TABLE] create table part_def partition of range_parted default
;
----------
[TRUNCATE_TABLE] truncate trunc_trigger_log
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_29539_2 ADD COLUMN newdata pg_temp_3.pg_temp_29539
;
----------
[CREATE_VIEW(SELECT)] create temp view overview as
  select f1::information_schema.sql_identifier as sqli, f2 from undername
;
----------
[CREATE_TABLE] -- test search for "not equals"

CREATE TABLE test_ne (
   a  TIMESTAMP,
   b  NUMERIC
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- error
alter table temp_part_parent attach partition temp_part_child default
;
----------
[CREATE_TABLE] CREATE TABLE "public"."bt_f8_heap" (
    "seqno" double precision,
    "random" integer
)
;
----------
[CREATE_TABLE] create table inhcld(f2 name, f1 int)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_14.pg_temp_27052_2
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL -1) pvactst
;
----------
[CREATE_TABLE] create table base_tab_def (a int, b text default 'Table default',
                           c text default 'Table default', d text, e text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_srf0() RETURNS SETOF int
LANGUAGE SQL
AS $$ SELECT i FROM generate_series(1, 100) i $$
;
----------
[ADD_INDEX] CREATE INDEX hash_pvactst ON pvactst USING hash (i)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE regtest_table DROP COLUMN y
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/tmp/xcFVXkeJJZ'
;
----------
[ADMIN_TABLE] VACUUM hs2
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab_p2
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_user_mapping
;
----------
[ALTER_TABLE] alter table cc no inherit bc
;
----------
[ADMIN_TABLE] ANALYZE public.dedup_unique_test_table
;
----------
[ALTER_TYPE] -- fails
ALTER TYPE test_type2 ADD ATTRIBUTE c text CASCADE
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.14
 * CHECK_CONSTRAINTS view
 */

CREATE VIEW check_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(substring(pg_get_constraintdef(con.oid) from 7) AS character_data)
             AS check_clause
    FROM pg_constraint con
           LEFT OUTER JOIN pg_namespace rs ON (rs.oid = con.connamespace)
           LEFT OUTER JOIN pg_class c ON (c.oid = con.conrelid)
           LEFT OUTER JOIN pg_type t ON (t.oid = con.contypid)
    WHERE pg_has_role(coalesce(c.relowner, t.typowner), 'USAGE')
      AND con.contype = 'c'

    UNION
    -- not-null constraints

    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(n.nspname AS sql_identifier) AS constraint_schema,
           CAST(CAST(n.oid AS text) || '_' || CAST(r.oid AS text) || '_' || CAST(a.attnum AS text) || '_not_null' AS sql_identifier) AS constraint_name, -- XXX
           CAST(a.attname || ' IS NOT NULL' AS character_data)
             AS check_clause
    FROM pg_namespace n, pg_class r, pg_attribute a
    WHERE n.oid = r.relnamespace
      AND r.oid = a.attrelid
      AND a.attnum > 0
      AND NOT a.attisdropped
      AND a.attnotnull
      AND r.relkind IN ('r', 'p')
      AND pg_has_role(r.relowner, 'USAGE')
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_interval_ops USING gist ADD
	OPERATOR	6	<> (interval, interval) ,
	OPERATOR	15	<-> (interval, interval) FOR ORDER BY pg_catalog.interval_ops ,
	FUNCTION	8 (interval, interval) gbt_intv_distance (internal, interval, int2, oid, internal) ,
	FUNCTION	9 (interval, interval) gbt_intv_fetch (internal)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_uuid_fetch(internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ismn, ismn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Test that partial indexes have all pages, including empty ones.
CREATE TABLE test2 (a int)
;
----------
[DROP_INDEX] drop index idxpart0_pkey
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger loct1_br_insert_trigger before insert on loct1
	for each row execute procedure br_insert_trigfunc()
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26669_2, pg_temp_13.pg_temp_26669
;
----------
[CREATE_TABLE] CREATE TABLE itest12 OF itest_type (f1 WITH OPTIONS GENERATED ALWAYS AS IDENTITY)
;
----------
[BLOCK] -- Check variable scoping -- a var is not available in its own or prior
-- default expressions, but it is available in later ones.

do $$
declare x int := x + 1;  -- error
begin
  raise notice 'x = %', x;
end;
$$
;
----------
[CREATE_TABLE] create table inh_parent(f1 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_picksplit(internal, internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_transform
;
----------
[CREATE_TABLE] create table ab_a2_b1 partition of ab_a2 for values in (1)
;
----------
[CREATE_TABLE] create table defcheck_1 partition of defcheck for values in (1, null)
;
----------
[CREATE_TABLE] -- not same schema
CREATE TABLE sequence_test_table (a int)
;
----------
[CREATE_SCHEMA] -- Again, with a schema name and a role specification.
CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE SEQUENCE schema_not_existing.seq
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."mr_table_succeed"("i" "anyelement", "r" "anymultirange") RETURNS TABLE("i" "anyelement", "r" "anymultirange")
    LANGUAGE "sql"
    AS $_$ select $1, $2 $_$
;
