CREATE OR REPLACE FUNCTION regexp_split_to_array(string citext, pattern citext, flags text) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_split_to_array( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE test_oat_schema.test_oat_tab (c1 int, c2 text)
;
create table attbl (p1 int constraint pk_attbl primary key)
;
---
-- partitioned table parent
CREATE TABLE t1p (o int, p text, q text) PARTITION BY RANGE (o)
;
CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=12942 host=/tmp/OOswabqf8K dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (two_phase = on)
;
CREATE TRIGGER transition_table_base_ins_trig
  AFTER INSERT ON transition_table_base
  REFERENCING OLD TABLE AS oldtable NEW TABLE AS newtable
  FOR EACH STATEMENT
  EXECUTE PROCEDURE transition_table_base_ins_func()
;
-- following tests should all respect the default AM
CREATE TABLE tableam_tbl_heapx(f1 int)
;
DO $d$
    BEGIN
        EXECUTE $$ALTER SERVER loopback
            OPTIONS (SET dbname '$$||current_database()||$$')$$;
    END;
$d$
;
create index �׻����Ѹ�index2 on �׻����Ѹ� using hash (ʬ�ॳ����)
;
CREATE OPERATOR ? (
	LEFTARG = hstore,
	RIGHTARG = text,
	PROCEDURE = exist,
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE OPERATOR <@> (
  LEFTARG = point,
  RIGHTARG = point,
  PROCEDURE = geo_distance,
  COMMUTATOR = <@>
)
;
DROP VIEW my_backend_pid
;
create table idxpart1 (b int, col1 int, col2 int, col3 int, a int)
;
CREATE FUNCTION isnne(isbn13, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE DATABASE db1 WITH TABLESPACE ts1 STRATEGY=FILE_COPY
;
VACUUM (FREEZE) information_schema.sql_sizing_profiles
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
--
-- Test the SP-GiST index
--

CREATE TABLE quad_poly_tbl (id int, p polygon)
;
CREATE TABLE "public"."regress_constr_partition1" (
    "a" integer NOT NULL
)
;
CREATE TABLE split_postgis.places (
    id bigserial PRIMARY KEY,
    name text,
    geom geometry(Point, 4326),
    area geometry(Polygon, 4326),
    route geometry(LineString, 4326),
    geog geography(Point, 4326)
)
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub2
;
CREATE PUBLICATION tap_pub_rep_full FOR TABLE users_table_part
;
DROP TABLE vacowned_parted
;
DROP TABLE IF EXISTS lcu_table
;
CREATE FUNCTION btean13cmp(ean13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE UNIQUE INDEX "covidxpart_a_b_idx" ON ONLY "public"."covidxpart" USING "btree" ("a") INCLUDE ("b")
;
create table pktable(ptest1 int, ptest2 int, primary key(base1, ptest1), foreign key(base2, ptest2) references
                                             pktable(base1, ptest1)) inherits (pktable_base)
;
ALTER TABLE pg_toast.pg_toast_31108 RENAME TO reind_con_toast
;
-- test GiST index that's been built incrementally
create table test_range_gist(ir int4range)
;
create or replace function simplecaller() returns int language plpgsql
as $$
declare
  sum int := 0;
begin
  for n in 1..10 loop
    sum := sum + simpletarget(n);
    if n = 5 then
      set local search_path = 'simple1';
    end if;
  end loop;
  return sum;
end$$
;
CREATE TABLE e_star (
	e 			int2
) INHERITS (c_star)
;
DROP DATABASE dropme_db1
;
--
-- CITEXT pattern matching.
--

CREATE FUNCTION texticlike(citext, citext)
RETURNS bool AS 'texticlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
create unique index on j3(unique1, tenthous)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest2"
    ADD CONSTRAINT "gtest2_pkey" PRIMARY KEY ("a")
;
-- 55
TRUNCATE tru_pk_ftable
;
drop table my_table
;
DROP SEQUENCE rngfunc_rescan_seq2
;
CREATE FUNCTION hashismn13(ismn13)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE DATABASE "regression" WITH TEMPLATE = template0 OID = 16387 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
DROP EVENT TRIGGER regress_reindex_end_snap
;
-- fail, hash indexes cannot do uniqueness
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_hash
;
CREATE TABLE pagg_tab_m_p1 PARTITION OF pagg_tab_m FOR VALUES FROM (0, 0) TO (12, 12)
;
DROP TABLE IF EXISTS temporal_pk
;
ALTER TABLE inht1 RENAME aaa TO aaaa
;
CREATE TABLE child_tbl (a int, b int)
;
CREATE TABLE test_enum (
   i rainbow
)
;
CREATE INDEX t2_hash ON s3.t2 USING HASH (i)
;
ALTER OPERATOR ^@ (ltree, ltxtquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
-- fail, type mismatch

drop function formarray(anyelement, variadic anyarray)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_split_to_array(string citext, pattern citext, flags text) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_split_to_array( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test_oat_schema.test_oat_tab (c1 int, c2 text)
;
----------
[CREATE_TABLE] create table attbl (p1 int constraint pk_attbl primary key)
;
----------
[CREATE_TABLE] ---
-- partitioned table parent
CREATE TABLE t1p (o int, p text, q text) PARTITION BY RANGE (o)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=12942 host=/tmp/OOswabqf8K dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (two_phase = on)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER transition_table_base_ins_trig
  AFTER INSERT ON transition_table_base
  REFERENCING OLD TABLE AS oldtable NEW TABLE AS newtable
  FOR EACH STATEMENT
  EXECUTE PROCEDURE transition_table_base_ins_func()
;
----------
[CREATE_TABLE] -- following tests should all respect the default AM
CREATE TABLE tableam_tbl_heapx(f1 int)
;
----------
[BLOCK] DO $d$
    BEGIN
        EXECUTE $$ALTER SERVER loopback
            OPTIONS (SET dbname '$$||current_database()||$$')$$;
    END;
$d$
;
----------
[ADD_INDEX] create index �׻����Ѹ�index2 on �׻����Ѹ� using hash (ʬ�ॳ����)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ? (
	LEFTARG = hstore,
	RIGHTARG = text,
	PROCEDURE = exist,
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <@> (
  LEFTARG = point,
  RIGHTARG = point,
  PROCEDURE = geo_distance,
  COMMUTATOR = <@>
)
;
----------
[DROP_VIEW] DROP VIEW my_backend_pid
;
----------
[CREATE_TABLE] create table idxpart1 (b int, col1 int, col2 int, col3 int, a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(isbn13, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_CATALOG] CREATE DATABASE db1 WITH TABLESPACE ts1 STRATEGY=FILE_COPY
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) information_schema.sql_sizing_profiles
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] --
-- Test the SP-GiST index
--

CREATE TABLE quad_poly_tbl (id int, p polygon)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."regress_constr_partition1" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE split_postgis.places (
    id bigserial PRIMARY KEY,
    name text,
    geom geometry(Point, 4326),
    area geometry(Polygon, 4326),
    route geometry(LineString, 4326),
    geog geography(Point, 4326)
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub2
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_rep_full FOR TABLE users_table_part
;
----------
[DROP_TABLE] DROP TABLE vacowned_parted
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS lcu_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btean13cmp(ean13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "covidxpart_a_b_idx" ON ONLY "public"."covidxpart" USING "btree" ("a") INCLUDE ("b")
;
----------
[CREATE_TABLE] create table pktable(ptest1 int, ptest2 int, primary key(base1, ptest1), foreign key(base2, ptest2) references
                                             pktable(base1, ptest1)) inherits (pktable_base)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_31108 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- test GiST index that's been built incrementally
create table test_range_gist(ir int4range)
;
----------
[CREATE_PROG_OBJ] create or replace function simplecaller() returns int language plpgsql
as $$
declare
  sum int := 0;
begin
  for n in 1..10 loop
    sum := sum + simpletarget(n);
    if n = 5 then
      set local search_path = 'simple1';
    end if;
  end loop;
  return sum;
end$$
;
----------
[CREATE_TABLE] CREATE TABLE e_star (
	e 			int2
) INHERITS (c_star)
;
----------
[DROP_CATALOG] DROP DATABASE dropme_db1
;
----------
[CREATE_PROG_OBJ] --
-- CITEXT pattern matching.
--

CREATE FUNCTION texticlike(citext, citext)
RETURNS bool AS 'texticlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] create unique index on j3(unique1, tenthous)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest2"
    ADD CONSTRAINT "gtest2_pkey" PRIMARY KEY ("a")
;
----------
[TRUNCATE_TABLE] -- 55
TRUNCATE tru_pk_ftable
;
----------
[DROP_TABLE] drop table my_table
;
----------
[DROP_SEQUENCE] DROP SEQUENCE rngfunc_rescan_seq2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hashismn13(ismn13)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression" WITH TEMPLATE = template0 OID = 16387 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER regress_reindex_end_snap
;
----------
[ALTER_TABLE] -- fail, hash indexes cannot do uniqueness
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_hash
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_m_p1 PARTITION OF pagg_tab_m FOR VALUES FROM (0, 0) TO (12, 12)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS temporal_pk
;
----------
[RENAME_COLUMN] ALTER TABLE inht1 RENAME aaa TO aaaa
;
----------
[CREATE_TABLE] CREATE TABLE child_tbl (a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE test_enum (
   i rainbow
)
;
----------
[ADD_INDEX] CREATE INDEX t2_hash ON s3.t2 USING HASH (i)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^@ (ltree, ltxtquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_PROG_OBJ] -- fail, type mismatch

drop function formarray(anyelement, variadic anyarray)
;
