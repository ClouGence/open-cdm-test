-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."part_p2"'::pg_catalog.regclass
;
INSERT INTO num_exp_sub VALUES (2,7,'48689992.784602953')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2504', relminmxid = '1'
WHERE oid = '"public"."ramp"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('5', 865)
;
UPDATE pgbench_branches SET bbalance = bbalance + 4898 WHERE bid = 1
;
-- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '726', datminmxid = '1'
WHERE datname = 'regression\"\	 !"#$%&''()*+,-\\"\\\'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13856', relminmxid = '3'
WHERE oid = '"public"."pagg_tab1_p1"'::pg_catalog.regclass
;
-- not allowed
INSERT INTO rw_view1 VALUES ('Row 3', 3.0, 3)
;
--
-- Now test longer arrays of char
--
-- This varchar_tbl was already created and filled in test_setup.sql.
-- Here we just try to insert bad values.
--

INSERT INTO VARCHAR_TBL (f1) VALUES ('abcde')
;
INSERT INTO public.tht VALUES ('8', 358)
;
INSERT INTO concur_reindex_tab VALUES  (1, 'a')
;
-- Cannot cast to bit on assignment.
INSERT INTO caster (bit)           VALUES ('101'::text)
;
DELETE FROM image
;
insert into arr_tbl values ('{1,2,10}')
;
INSERT INTO circles VALUES('<(0,0), 5>', '<(0,0), 4>')
;
UPDATE gtest26 SET a = 11 WHERE a = 10
;
INSERT INTO public.tht VALUES ('8', 598)
;
INSERT INTO t2 VALUES ( 'bb', 12 )
;
insert into revalidate_bug values (inverse(0))
;
INSERT into fk VALUES (1)
;
UPDATE range_parted set a = 'b' WHERE a = 'bd'
;
INSERT INTO pg_temp_10.pg_temp_26514_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_10.pg_temp_26514 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_10.pg_temp_26514 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '18032'
;
-- works now
INSERT INTO voo VALUES(14,'zoo2') RETURNING *
;
INSERT INTO foo VALUES (2, 'ADD2')
;
INSERT INTO arraggtest (f1, f2, f3) VALUES
('{4,2,6,7,8,1}','{{red},{black},{purple},{blue},{blue}}',NULL)
;
-- ok
DELETE FROM base_tbl WHERE a=1
;
INSERT INTO counter VALUES (0)
;
INSERT INTO public.mvtest_mv_foo SELECT (diff.newdata).* FROM pg_temp_12.pg_temp_26654_2 diff WHERE tid IS NULL
;
INSERT INTO truncpart VALUES (1), (100), (150)
;
-- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.1........'
  AND attrelid = '"fkpart3"."pk5"'::pg_catalog.regclass
;
INSERT INTO new_measurement VALUES (1, '2007-01-17', NULL, NULL)
;
INSERT INTO gtest1 VALUES (3, DEFAULT), (4, 44)
;
INSERT INTO public.tplain VALUES ('7', 727)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9258', relminmxid = '1'
WHERE oid = '"public"."brinopers_multi"'::pg_catalog.regclass
;
insert into table_with_oids values (1)
;
insert into comptable
  values (row(1,'foo'), array[row(2,'bar')::comptype, row(3,'baz')::comptype])
;
UPDATE hash_txt_heap
   SET random = '0123456789abcdefghijklmnop'::text
   WHERE hash_txt_heap.seqno = 4002
;
UPDATE FLOAT8_TBL
   SET f1 = FLOAT8_TBL.f1 * '-1'
   WHERE FLOAT8_TBL.f1 > '0.0'
;
INSERT INTO test20 VALUES (2, 'ABC')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17043', relminmxid = '3'
WHERE oid = '"public"."cmmove1"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('6', 136)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8213', relminmxid = '1'
WHERE oid = '27156'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16806', relminmxid = '3'
WHERE oid = '38061'
;
insert into zt2 values(53)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."b"'::pg_catalog.regclass
  AND attname IN ('aa')
;
UPDATE itest2 SET a = DEFAULT WHERE a = 2
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10709', relminmxid = '1'
WHERE oid = '"public"."rule_merge1"'::pg_catalog.regclass
;
-- fail
UPDATE atest5 SET three = 5, one = 2
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14574', relminmxid = '3'
WHERE oid = '"public"."plt1_p1"'::pg_catalog.regclass
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."part_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (2,7,'48689992.784602953')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2504', relminmxid = '1'
WHERE oid = '"public"."ramp"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 865)
;
----------
[UPDATE] UPDATE pgbench_branches SET bbalance = bbalance + 4898 WHERE bid = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '726', datminmxid = '1'
WHERE datname = 'regression\"\	 !"#$%&''()*+,-\\"\\\'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13856', relminmxid = '3'
WHERE oid = '"public"."pagg_tab1_p1"'::pg_catalog.regclass
;
----------
[INSERT] -- not allowed
INSERT INTO rw_view1 VALUES ('Row 3', 3.0, 3)
;
----------
[INSERT|SELECT] --
-- Now test longer arrays of char
--
-- This varchar_tbl was already created and filled in test_setup.sql.
-- Here we just try to insert bad values.
--

INSERT INTO VARCHAR_TBL (f1) VALUES ('abcde')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 358)
;
----------
[INSERT] INSERT INTO concur_reindex_tab VALUES  (1, 'a')
;
----------
[INSERT|SELECT] -- Cannot cast to bit on assignment.
INSERT INTO caster (bit)           VALUES ('101'::text)
;
----------
[DELETE] DELETE FROM image
;
----------
[INSERT] insert into arr_tbl values ('{1,2,10}')
;
----------
[INSERT] INSERT INTO circles VALUES('<(0,0), 5>', '<(0,0), 4>')
;
----------
[UPDATE] UPDATE gtest26 SET a = 11 WHERE a = 10
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 598)
;
----------
[INSERT] INSERT INTO t2 VALUES ( 'bb', 12 )
;
----------
[INSERT] insert into revalidate_bug values (inverse(0))
;
----------
[INSERT] INSERT into fk VALUES (1)
;
----------
[UPDATE] UPDATE range_parted set a = 'b' WHERE a = 'bd'
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_10.pg_temp_26514_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_10.pg_temp_26514 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_10.pg_temp_26514 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '18032'
;
----------
[INSERT] -- works now
INSERT INTO voo VALUES(14,'zoo2') RETURNING *
;
----------
[INSERT] INSERT INTO foo VALUES (2, 'ADD2')
;
----------
[INSERT|SELECT] INSERT INTO arraggtest (f1, f2, f3) VALUES
('{4,2,6,7,8,1}','{{red},{black},{purple},{blue},{blue}}',NULL)
;
----------
[DELETE] -- ok
DELETE FROM base_tbl WHERE a=1
;
----------
[INSERT] INSERT INTO counter VALUES (0)
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_mv_foo SELECT (diff.newdata).* FROM pg_temp_12.pg_temp_26654_2 diff WHERE tid IS NULL
;
----------
[INSERT] INSERT INTO truncpart VALUES (1), (100), (150)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.1........'
  AND attrelid = '"fkpart3"."pk5"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO new_measurement VALUES (1, '2007-01-17', NULL, NULL)
;
----------
[INSERT] INSERT INTO gtest1 VALUES (3, DEFAULT), (4, 44)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 727)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9258', relminmxid = '1'
WHERE oid = '"public"."brinopers_multi"'::pg_catalog.regclass
;
----------
[INSERT] insert into table_with_oids values (1)
;
----------
[INSERT] insert into comptable
  values (row(1,'foo'), array[row(2,'bar')::comptype, row(3,'baz')::comptype])
;
----------
[UPDATE] UPDATE hash_txt_heap
   SET random = '0123456789abcdefghijklmnop'::text
   WHERE hash_txt_heap.seqno = 4002
;
----------
[UPDATE] UPDATE FLOAT8_TBL
   SET f1 = FLOAT8_TBL.f1 * '-1'
   WHERE FLOAT8_TBL.f1 > '0.0'
;
----------
[INSERT] INSERT INTO test20 VALUES (2, 'ABC')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17043', relminmxid = '3'
WHERE oid = '"public"."cmmove1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 136)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8213', relminmxid = '1'
WHERE oid = '27156'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16806', relminmxid = '3'
WHERE oid = '38061'
;
----------
[INSERT] insert into zt2 values(53)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."b"'::pg_catalog.regclass
  AND attname IN ('aa')
;
----------
[UPDATE] UPDATE itest2 SET a = DEFAULT WHERE a = 2
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10709', relminmxid = '1'
WHERE oid = '"public"."rule_merge1"'::pg_catalog.regclass
;
----------
[UPDATE] -- fail
UPDATE atest5 SET three = 5, one = 2
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14574', relminmxid = '3'
WHERE oid = '"public"."plt1_p1"'::pg_catalog.regclass
;
