-- Other compression algorithms may cause the compressed data to be stored
-- inline.  Use pglz to ensure consistent results.
set default_toast_compression = 'pglz'
;
EXECUTE getDomainConstraints('21798')
;
CREATE USER MAPPING IF NOT EXISTS FOR CURRENT_USER SERVER split_dummy_server_renamed OPTIONS (user 'postgres', password 'secret')
;
EXECUTE dumpBaseType('2203')
;
COPY dump_test.test_second_table (col1, col2) TO stdout
;
LOCK TABLE public.thethings IN ACCESS SHARE MODE
;
CREATE ACCESS METHOD split_heap_probe TYPE TABLE HANDLER heap_tableam_handler
;
---
--- Cleanup resources
---
DROP FOREIGN DATA WRAPPER addr_fdw CASCADE
;
EXECUTE dumpTableAttach('46249')
;
DROP USER regress_seclabel_user1
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] -- Other compression algorithms may cause the compressed data to be stored
-- inline.  Use pglz to ensure consistent results.
set default_toast_compression = 'pglz'
;
----------
[UNSAFE] EXECUTE getDomainConstraints('21798')
;
----------
[GRANT] CREATE USER MAPPING IF NOT EXISTS FOR CURRENT_USER SERVER split_dummy_server_renamed OPTIONS (user 'postgres', password 'secret')
;
----------
[UNSAFE] EXECUTE dumpBaseType('2203')
;
----------
[DATA_EXPORT] COPY dump_test.test_second_table (col1, col2) TO stdout
;
----------
[SESSION_LOCK] LOCK TABLE public.thethings IN ACCESS SHARE MODE
;
----------
[SYSTEM_SETTING_WRITE] CREATE ACCESS METHOD split_heap_probe TYPE TABLE HANDLER heap_tableam_handler
;
----------
[SYSTEM_SETTING_WRITE] ---
--- Cleanup resources
---
DROP FOREIGN DATA WRAPPER addr_fdw CASCADE
;
----------
[UNSAFE] EXECUTE dumpTableAttach('46249')
;
----------
[DROP_USER] DROP USER regress_seclabel_user1
;
