CREATE AGGREGATE myaggn12a(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE INDEX tenant_idx ON tenant_table(i)
;
DROP TABLE IF EXISTS pq_pipeline_demo
;
-- invalid column definition
CREATE TABLE default_expr_non_column (a int DEFAULT (avg(non_existent)))
;
create trigger mutable_trig before update on mutable for each row
execute procedure sillytrig()
;
CREATE TRIGGER after_ins_stmt_trig AFTER INSERT ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('after_view_ins_stmt')
;
DO LANGUAGE plpgsql $$
declare
  c test1 := row(42, 'hello');
  r bool;
begin
  for i in 1..3 loop
    r := c is not null;
    raise notice 'r = %', r;
    commit;
  end loop;
  for i in 1..3 loop
    r := c is null;
    raise notice 'r = %', r;
    rollback;
  end loop;
end
$$
;
CREATE TEXT SEARCH DICTIONARY split_ts_probe.simple_dict (
    TEMPLATE = simple,
    StopWords = english,
    Accept = true
)
;
DROP COLLATION test0, test_schema.test11, test5
;
VACUUM (FREEZE) pg_catalog.pg_publication_namespace
;
CREATE DOMAIN "public"."dom" AS integer
;
CREATE TABLE "public"."trigger_parted_p1" (
    "a" integer CONSTRAINT "trigger_parted_a_not_null" NOT NULL
)
PARTITION BY LIST ("a")
;
create view composite_v as
with cte(c) as materialized (select row(1, 2)),
     cte2(c) as (select * from cte)
select 1 as one from cte2 as t
where (select * from (select c as c1) s
       where (select (c1).f1 > 0)) is not null
;
alter database abc rename to abc2
;
create type textrange2 as range(subtype=text, collation="C")
;
-- Exercise rescans.  We'll turn off parallel_leader_participation so
-- that we can check that instrumentation comes back correctly.

create table join_foo as select generate_series(1, 3) as id, 'xxxxx'::text as t
;
create table inh_fk_2 (x int primary key, y int references inh_fk_1 on delete cascade)
;
create table tlu_job (id integer primary key, name text)
;
alter table idxpart_c detach partition idxpart1_c
;
CREATE TABLE agg_sort_order (c1 int PRIMARY KEY, c2 int)
;
-- fail

ALTER TEXT SEARCH DICTIONARY synonym (CaseSensitive = off)
;
CREATE TEMP TABLE pg_temp_4.pg_temp_26914_2 (tid pg_catalog.tid)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_event_trigger
;
-- now check that DO UPDATE works correctly for target partition with
-- different attribute numbers
create table parted_conflict_test_2 (b char, a int unique)
;
ALTER TABLE pg_toast.pg_toast_33438 RENAME TO reind_con_toast
;
CREATE SUBSCRIPTION tsub CONNECTION 'port=20325 host=/tmp/DIV3IFaJWh dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
CREATE MATERIALIZED VIEW mvtest_bb AS SELECT * FROM mvtest_tvvmv
;
DROP TABLE test_like_4, test_like_4a, test_like_4b, test_like_4c, test_like_4d
;
create or replace view my_locks as
select case when c.relname like 'pg_toast%' then 'pg_toast' else c.relname end, max(mode::lockmodes) as max_lockmode
from pg_locks l join pg_class c on l.relation = c.oid
where virtualtransaction = (
        select virtualtransaction
        from pg_locks
        where transactionid = pg_current_xact_id()::xid)
and locktype = 'relation'
and relnamespace != (select oid from pg_namespace where nspname = 'pg_catalog')
and c.relname = 'my_locks'
group by c.relname
;
CREATE TEXT SEARCH CONFIGURATION romanian
	(PARSER = default)
;
create trigger tg_slotlink_a after insert or update or delete
    on PHone for each row execute procedure tg_slotlink_a('PH')
;
create function depth_a_tf() returns trigger
  language plpgsql as $$
begin
  raise notice '%: depth = %', tg_name, pg_trigger_depth();
  insert into depth_b values (new.id);
  raise notice '%: depth = %', tg_name, pg_trigger_depth();
  return new;
end;
$$
;
create trigger oid_unchanged_trig after update on table_with_oids
	for each row
	when (new.tableoid = old.tableoid AND new.tableoid <> 0)
	execute procedure trigger_func('after_upd_oid_unchanged')
;
ANALYZE public.skip_wal_skip_rewrite_index
;
DROP TABLE IF EXISTS customer CASCADE
;
ANALYZE public.itest7e
;
create domain d_fail as int4 constraint cc check (values > 1) deferrable
;
DROP VIEW IF EXISTS split_view_next.base_view_renamed CASCADE
;
CREATE TABLE "public"."pitest1_p2" (
    "f3" bigint CONSTRAINT "pitest1_f3_not_null" NOT NULL,
    "f2" "text",
    "f1" "date" NOT NULL
)
;
-- multi-argument aggregates sensitive to distinct/order, strict/nonstrict
create type aggtype as (a integer, b integer, c text)
;
-- should fail with a good error message:
create table parttmp (id int, valid_at daterange, exclude using gist (id with <>, valid_at with &&)) partition by range (id)
;
-- drop the restrictive SELECT policy so that we can look at the
-- final state of the table
DROP POLICY p1 ON document
;
CREATE INDEX t2_brin ON s5.t2 USING BRIN (i)
;
drop table ctrtest
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '1')
;
CREATE TABLE test2bpci (x char(3) COLLATE case_insensitive)
;
--
-- Const squashing functionality
--
CREATE EXTENSION pg_stat_statements
;
CREATE TRIGGER "xtrig" AFTER UPDATE ON "public"."bar1" REFERENCING OLD TABLE AS "old" FOR EACH STATEMENT EXECUTE FUNCTION "public"."xtrig"()
;
create temp table pp (f1 int primary key)
;
-- fails, changing only a partition not allowed
ALTER TABLE pitest3_p1
            ALTER COLUMN f3 SET NOT NULL,
            ALTER COLUMN f3 ADD GENERATED ALWAYS AS IDENTITY (START WITH 3)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn12a(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ADD_INDEX] CREATE INDEX tenant_idx ON tenant_table(i)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS pq_pipeline_demo
;
----------
[CREATE_TABLE] -- invalid column definition
CREATE TABLE default_expr_non_column (a int DEFAULT (avg(non_existent)))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger mutable_trig before update on mutable for each row
execute procedure sillytrig()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER after_ins_stmt_trig AFTER INSERT ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('after_view_ins_stmt')
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
declare
  c test1 := row(42, 'hello');
  r bool;
begin
  for i in 1..3 loop
    r := c is not null;
    raise notice 'r = %', r;
    commit;
  end loop;
  for i in 1..3 loop
    r := c is null;
    raise notice 'r = %', r;
    rollback;
  end loop;
end
$$
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY split_ts_probe.simple_dict (
    TEMPLATE = simple,
    StopWords = english,
    Accept = true
)
;
----------
[DROP_POLICY] DROP COLLATION test0, test_schema.test11, test5
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_publication_namespace
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."dom" AS integer
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted_p1" (
    "a" integer CONSTRAINT "trigger_parted_a_not_null" NOT NULL
)
PARTITION BY LIST ("a")
;
----------
[CREATE_VIEW(SELECT)] create view composite_v as
with cte(c) as materialized (select row(1, 2)),
     cte2(c) as (select * from cte)
select 1 as one from cte2 as t
where (select * from (select c as c1) s
       where (select (c1).f1 > 0)) is not null
;
----------
[RENAME_CATALOG] alter database abc rename to abc2
;
----------
[CREATE_POLICY] create type textrange2 as range(subtype=text, collation="C")
;
----------
[CREATE_TABLE|SELECT] -- Exercise rescans.  We'll turn off parallel_leader_participation so
-- that we can check that instrumentation comes back correctly.

create table join_foo as select generate_series(1, 3) as id, 'xxxxx'::text as t
;
----------
[CREATE_TABLE] create table inh_fk_2 (x int primary key, y int references inh_fk_1 on delete cascade)
;
----------
[CREATE_TABLE] create table tlu_job (id integer primary key, name text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart_c detach partition idxpart1_c
;
----------
[CREATE_TABLE] CREATE TABLE agg_sort_order (c1 int PRIMARY KEY, c2 int)
;
----------
[ALTER_POLICY] -- fail

ALTER TEXT SEARCH DICTIONARY synonym (CaseSensitive = off)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_26914_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_event_trigger
;
----------
[CREATE_TABLE] -- now check that DO UPDATE works correctly for target partition with
-- different attribute numbers
create table parted_conflict_test_2 (b char, a int unique)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33438 RENAME TO reind_con_toast
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tsub CONNECTION 'port=20325 host=/tmp/DIV3IFaJWh dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_bb AS SELECT * FROM mvtest_tvvmv
;
----------
[DROP_TABLE] DROP TABLE test_like_4, test_like_4a, test_like_4b, test_like_4c, test_like_4d
;
----------
[CREATE_VIEW(SELECT)] create or replace view my_locks as
select case when c.relname like 'pg_toast%' then 'pg_toast' else c.relname end, max(mode::lockmodes) as max_lockmode
from pg_locks l join pg_class c on l.relation = c.oid
where virtualtransaction = (
        select virtualtransaction
        from pg_locks
        where transactionid = pg_current_xact_id()::xid)
and locktype = 'relation'
and relnamespace != (select oid from pg_namespace where nspname = 'pg_catalog')
and c.relname = 'my_locks'
group by c.relname
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION romanian
	(PARSER = default)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_slotlink_a after insert or update or delete
    on PHone for each row execute procedure tg_slotlink_a('PH')
;
----------
[CREATE_PROG_OBJ] create function depth_a_tf() returns trigger
  language plpgsql as $$
begin
  raise notice '%: depth = %', tg_name, pg_trigger_depth();
  insert into depth_b values (new.id);
  raise notice '%: depth = %', tg_name, pg_trigger_depth();
  return new;
end;
$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger oid_unchanged_trig after update on table_with_oids
	for each row
	when (new.tableoid = old.tableoid AND new.tableoid <> 0)
	execute procedure trigger_func('after_upd_oid_unchanged')
;
----------
[ADMIN_TABLE] ANALYZE public.skip_wal_skip_rewrite_index
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS customer CASCADE
;
----------
[ADMIN_TABLE] ANALYZE public.itest7e
;
----------
[CREATE_TYPE] create domain d_fail as int4 constraint cc check (values > 1) deferrable
;
----------
[DROP_VIEW] DROP VIEW IF EXISTS split_view_next.base_view_renamed CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest1_p2" (
    "f3" bigint CONSTRAINT "pitest1_f3_not_null" NOT NULL,
    "f2" "text",
    "f1" "date" NOT NULL
)
;
----------
[CREATE_TYPE] -- multi-argument aggregates sensitive to distinct/order, strict/nonstrict
create type aggtype as (a integer, b integer, c text)
;
----------
[CREATE_TABLE] -- should fail with a good error message:
create table parttmp (id int, valid_at daterange, exclude using gist (id with <>, valid_at with &&)) partition by range (id)
;
----------
[DROP_POLICY] -- drop the restrictive SELECT policy so that we can look at the
-- final state of the table
DROP POLICY p1 ON document
;
----------
[ADD_INDEX] CREATE INDEX t2_brin ON s5.t2 USING BRIN (i)
;
----------
[DROP_TABLE] drop table ctrtest
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '1')
;
----------
[CREATE_TABLE] CREATE TABLE test2bpci (x char(3) COLLATE case_insensitive)
;
----------
[CREATE_LIBRARY] --
-- Const squashing functionality
--
CREATE EXTENSION pg_stat_statements
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "xtrig" AFTER UPDATE ON "public"."bar1" REFERENCING OLD TABLE AS "old" FOR EACH STATEMENT EXECUTE FUNCTION "public"."xtrig"()
;
----------
[CREATE_TABLE] create temp table pp (f1 int primary key)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails, changing only a partition not allowed
ALTER TABLE pitest3_p1
            ALTER COLUMN f3 SET NOT NULL,
            ALTER COLUMN f3 ADD GENERATED ALWAYS AS IDENTITY (START WITH 3)
;
