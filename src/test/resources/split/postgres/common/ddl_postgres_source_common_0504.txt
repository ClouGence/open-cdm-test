-- cleanup before next test
-- these are all OK; the second one should emit a NOTICE
drop event trigger if exists regress_event_trigger2
;
create table mlparted1 (b int not null, a int not null) partition by range ((b+0))
;
--
-- check alter rename rule
--
CREATE TABLE rule_t1 (a INT)
;
CREATE TABLE gtest_child3 (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 33) STORED)
;
-- not-null constraints
create domain connotnull integer
;
-- create before insert row trigger on parted_copytest_a2
create function part_ins_func() returns trigger language plpgsql as $$
begin
  return new;
end;
$$
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
DROP TABLE num_exp_log10
;
CREATE TABLE sro_part PARTITION OF sro_ptab FOR VALUES FROM (1) TO (10)
;
-- good, but it'll prevent the attach below
ALTER TABLE parted_self_fk ATTACH PARTITION part2_self_fk FOR VALUES FROM (10) TO (20)
;
DROP TYPE IF EXISTS split_domain_type_next.address_record CASCADE
;
CREATE TABLE pagg_tab_ml_p1 PARTITION OF pagg_tab_ml FOR VALUES FROM (0) TO (10)
;
ALTER TABLE prt2 ATTACH PARTITION prt2_p3 FOR VALUES FROM (500) TO (600)
;
-- should log old key
ALTER TABLE table_with_unique_not_null REPLICA IDENTITY USING INDEX table_with_unique_not_null_id_key
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_db_role_setting
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_foreign_table
;
drop table inh_parent, inh_child1, inh_child2, inh_child3
;
alter table T alter column Item2 set data type smallint
;
CREATE TABLE J2_TBL (
  i integer,
  k integer
)
;
DROP TABLE t3
;
CREATE PUBLICATION "dump_pub_2ct" WITH (publish = 'insert, update, delete, truncate')
;
CREATE TABLE rf_tbl_abcd_pk(a int, b int, c int, d int, PRIMARY KEY(a,b))
;
--
--  Test earthdistance extension
--
-- In this file we also do some testing of extension create/drop scenarios.
-- That's really exercising the core database's dependency logic, so ideally
-- we'd do it in the core regression tests, but we can't for lack of suitable
-- guaranteed-available extensions.  earthdistance is a good test case because
-- it has a dependency on the cube extension.
--

CREATE EXTENSION earthdistance
;
CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE (f1)
;
CREATE TABLE part_p2 PARTITION OF part DEFAULT PARTITION BY RANGE(a)
;
alter table part_column_drop drop column useless_3
;
-- System columns cannot be indexed
CREATE INDEX ON syscol_table (ctid)
;
create table upsert (key int4 primary key, color text)
;
--    N    N        N    P
-- should CREATE
CREATE AGGREGATE myaggp06a(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
DROP RULE hat_nosert ON hats
;
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize
)
;
CREATE OPERATOR >= (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_ge,
        COMMUTATOR = '<=',
	NEGATOR = '<',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ALTER TABLE users_table_part REPLICA IDENTITY FULL
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest1_y" INHERIT "generated_virtual_tests"."gtest1"
;
CREATE TABLE DATE_TBL (f1 date)
;
CREATE TRIGGER sub2_tab1_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1
  FOR EACH ROW EXECUTE PROCEDURE sub2_trigger_activity_func()
;
ALTER TABLE pg_publication ADD UNIQUE USING INDEX pg_publication_pubname_index
;
-- error
CREATE MATERIALIZED VIEW mvtest_mv_v_3 (ii, jj) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
create table test_01(a int, b int, c int)
;
drop function raise_exprs()
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_publication
;
CREATE TRIGGER "tg_hub_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."hub" FOR EACH ROW EXECUTE FUNCTION "public"."tg_hub_a"()
;
-- remove constraint from cc1, should succeed
alter table cc1 alter column a2 drop not null
;
DROP TRIGGER transition_table_level2_bad_usage_trigger
  ON transition_table_level2
;
CREATE TEMP TABLE pg_temp_9.pg_temp_26570_2 (tid pg_catalog.tid)
;
alter table if exists doesnt_exist_tab rename column b to a
;
-- this should cascade to drop atestv4
DROP VIEW atestv3 CASCADE
;
create temp table test1(f1 multitextrange1[])
;
ALTER TABLE pg_temp_9.pg_temp_38160_2 ADD COLUMN newdata pg_temp_9.pg_temp_38160
;
create table nv_parent (d date, check (false) no inherit not valid)
;
------------------------------------------------------------------------------------------
[DROP_TRIGGER] -- cleanup before next test
-- these are all OK; the second one should emit a NOTICE
drop event trigger if exists regress_event_trigger2
;
----------
[CREATE_TABLE] create table mlparted1 (b int not null, a int not null) partition by range ((b+0))
;
----------
[CREATE_TABLE] --
-- check alter rename rule
--
CREATE TABLE rule_t1 (a INT)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child3 (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 33) STORED)
;
----------
[CREATE_TYPE] -- not-null constraints
create domain connotnull integer
;
----------
[CREATE_PROG_OBJ] -- create before insert row trigger on parted_copytest_a2
create function part_ins_func() returns trigger language plpgsql as $$
begin
  return new;
end;
$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
----------
[DROP_TABLE] DROP TABLE num_exp_log10
;
----------
[CREATE_TABLE] CREATE TABLE sro_part PARTITION OF sro_ptab FOR VALUES FROM (1) TO (10)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- good, but it'll prevent the attach below
ALTER TABLE parted_self_fk ATTACH PARTITION part2_self_fk FOR VALUES FROM (10) TO (20)
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS split_domain_type_next.address_record CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p1 PARTITION OF pagg_tab_ml FOR VALUES FROM (0) TO (10)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt2 ATTACH PARTITION prt2_p3 FOR VALUES FROM (500) TO (600)
;
----------
[ALTER_TABLE] -- should log old key
ALTER TABLE table_with_unique_not_null REPLICA IDENTITY USING INDEX table_with_unique_not_null_id_key
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_db_role_setting
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_foreign_table
;
----------
[DROP_TABLE] drop table inh_parent, inh_child1, inh_child2, inh_child3
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table T alter column Item2 set data type smallint
;
----------
[CREATE_TABLE] CREATE TABLE J2_TBL (
  i integer,
  k integer
)
;
----------
[DROP_TABLE] DROP TABLE t3
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "dump_pub_2ct" WITH (publish = 'insert, update, delete, truncate')
;
----------
[CREATE_TABLE] CREATE TABLE rf_tbl_abcd_pk(a int, b int, c int, d int, PRIMARY KEY(a,b))
;
----------
[CREATE_LIBRARY] --
--  Test earthdistance extension
--
-- In this file we also do some testing of extension create/drop scenarios.
-- That's really exercising the core database's dependency logic, so ideally
-- we'd do it in the core regression tests, but we can't for lack of suitable
-- guaranteed-available extensions.  earthdistance is a good test case because
-- it has a dependency on the cube extension.
--

CREATE EXTENSION earthdistance
;
----------
[CREATE_TABLE] CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE (f1)
;
----------
[CREATE_TABLE] CREATE TABLE part_p2 PARTITION OF part DEFAULT PARTITION BY RANGE(a)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table part_column_drop drop column useless_3
;
----------
[ADD_INDEX] -- System columns cannot be indexed
CREATE INDEX ON syscol_table (ctid)
;
----------
[CREATE_TABLE] create table upsert (key int4 primary key, color text)
;
----------
[CREATE_PROG_OBJ] --    N    N        N    P
-- should CREATE
CREATE AGGREGATE myaggp06a(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[DROP_POLICY] DROP RULE hat_nosert ON hats
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_ge,
        COMMUTATOR = '<=',
	NEGATOR = '<',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ALTER_TABLE] ALTER TABLE users_table_part REPLICA IDENTITY FULL
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_virtual_tests"."gtest1_y" INHERIT "generated_virtual_tests"."gtest1"
;
----------
[CREATE_TABLE] CREATE TABLE DATE_TBL (f1 date)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER sub2_tab1_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1
  FOR EACH ROW EXECUTE PROCEDURE sub2_trigger_activity_func()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_publication ADD UNIQUE USING INDEX pg_publication_pubname_index
;
----------
[CREATE_VIEW(SELECT)] -- error
CREATE MATERIALIZED VIEW mvtest_mv_v_3 (ii, jj) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
----------
[CREATE_TABLE] create table test_01(a int, b int, c int)
;
----------
[DROP_PROG_OBJ] drop function raise_exprs()
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_publication
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_hub_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."hub" FOR EACH ROW EXECUTE FUNCTION "public"."tg_hub_a"()
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- remove constraint from cc1, should succeed
alter table cc1 alter column a2 drop not null
;
----------
[DROP_TRIGGER] DROP TRIGGER transition_table_level2_bad_usage_trigger
  ON transition_table_level2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_26570_2 (tid pg_catalog.tid)
;
----------
[RENAME_COLUMN] alter table if exists doesnt_exist_tab rename column b to a
;
----------
[DROP_VIEW] -- this should cascade to drop atestv4
DROP VIEW atestv3 CASCADE
;
----------
[CREATE_TABLE] create temp table test1(f1 multitextrange1[])
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_38160_2 ADD COLUMN newdata pg_temp_9.pg_temp_38160
;
----------
[CREATE_TABLE] create table nv_parent (d date, check (false) no inherit not valid)
;
