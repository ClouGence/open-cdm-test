ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "email" WITH "simple"
;
CREATE INDEX macaddr_data_hash ON macaddr_data USING hash (b)
;
create function void_return_expr() returns void as $$
begin
    perform 2+2;
end;$$ language plpgsql
;
-- INSERT/DELETE on zero column table
CREATE TABLE zerocol()
;
CREATE FUNCTION gin_extract_value_date(date, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER FUNCTION gbt_timetz_compress(internal) PARALLEL SAFE
;
CREATE FUNCTION perl2text(text) RETURNS bool
LANGUAGE plperlu
TRANSFORM FOR TYPE bool
AS $$
return shift;
$$
;
DROP TABLE ctas_stats_1
;
-- Test handling of index predicates - updating attributes in precicates
-- should not block HOT when summarizing indexes are involved. We update
-- a row that was not indexed due to the index predicate, and becomes
-- indexable - the HOT-updated tuple is forwarded to the BRIN index.
CREATE TABLE brin_hot_2 (a int, b int)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_shdescription
;
-- populate_record
create type jpop as (a text, b int, c timestamp)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_trigger
;
ANALYZE pg_temp_3.pg_temp_28840
;
alter table alterlock alter column f2 set statistics 150
;
drop table permtest_parent
;
ALTER FUNCTION gbtreekey8_in(cstring) PARALLEL SAFE
;
CREATE SUBSCRIPTION regress_testsub5 CONNECTION 'i_dont_exist=param' PUBLICATION testpub
;
-- check error reporting with column privs on a partitioned table
CREATE TABLE errtst(a text, b text NOT NULL, c text, secret1 text, secret2 text) PARTITION BY LIST (a)
;
VACUUM FULL pg_index
;
drop function zero_divide()
;
CREATE TABLE prevstats AS
SELECT t.seq_scan, t.seq_tup_read, t.idx_scan, t.idx_tup_fetch,
       (b.heap_blks_read + b.heap_blks_hit) AS heap_blks,
       (b.idx_blks_read + b.idx_blks_hit) AS idx_blks,
       pg_stat_get_snapshot_timestamp() as snap_ts
  FROM pg_catalog.pg_stat_user_tables AS t,
       pg_catalog.pg_statio_user_tables AS b
 WHERE t.relname='tenk2' AND b.relname='tenk2'
;
CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
TRUNCATE inh_parent
;
ANALYZE pg_temp_12.pg_temp_26389_2
;
/*
 * 6.48
 * ROLE_UDT_GRANTS view
 */

CREATE VIEW role_udt_grants AS
    SELECT grantor,
           grantee,
           udt_catalog,
           udt_schema,
           udt_name,
           privilege_type,
           is_grantable
    FROM udt_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
CREATE TEMP TABLE test (a int, b char(20))
;
DROP TABLE pg_temp_3.pg_temp_28840_2, pg_temp_3.pg_temp_28840
;
ALTER INDEX pg_toast.pg_toast_33450_index RENAME TO reind_con_toast_idx
;
CREATE OR REPLACE VIEW "public"."key_dependent_view_no_cols" AS
 SELECT
   FROM "public"."view_base_table"
  GROUP BY "key"
 HAVING ("length"(("data")::"text") > 0)
;
CREATE TEMP TABLE pg_temp_9.pg_temp_38079_2 (tid pg_catalog.tid)
;
CREATE TABLE pagg_tab_ml_p2 PARTITION OF pagg_tab_ml FOR VALUES FROM (10) TO (20) PARTITION BY LIST (c)
;
-- text search dictionary
DROP TEXT SEARCH DICTIONARY test_tsdict_exists
;
CREATE TABLE brassica PARTITION OF vegetables FOR VALUES IN ('brassica')
;
-- Test subscripting in plpgsql
do $$ declare h hstore;
begin h['a'] := 'b'; raise notice 'h = %, h[a] = %', h, h['a']; end $$
;
create table parted_irreg_ancestor (fd text, b text, fd2 int, fd3 int, a int)
  partition by range (b)
;
alter table trigtest disable trigger all
;
CREATE TABLE temptest(col) ON COMMIT DELETE ROWS AS SELECT 1
;
-- set not null verified by constraints
create table atacc1 (test_a int, test_b int)
;
CREATE UNLOGGED TABLE brintest_unlogged (n numrange)
;
CREATE TABLE base_tbl(a float)
;
DROP TABLE gtest_child3
;
do $$
declare relnm text;
  reloid oid;
  shared bool;
  lowoid oid;
  pinned bool;
begin
for relnm, reloid, shared in
  select relname, oid, relisshared from pg_class
  where EXISTS(
      SELECT * FROM pg_attribute
      WHERE attrelid = pg_class.oid AND attname = 'oid')
    and relkind = 'r' and oid < 16384 order by 1
loop
  execute 'select min(oid) from ' || relnm into lowoid;
  continue when lowoid is null or lowoid >= 16384;
  if shared then
    pinned := exists(select 1 from pg_shdepend
                     where refclassid = reloid and refobjid = lowoid
                     and deptype = 'p');
  else
    pinned := exists(select 1 from pg_depend
                     where refclassid = reloid and refobjid = lowoid
                     and deptype = 'p');
  end if;
  if not pinned then
    raise notice '% contains unpinned initdb-created object(s)', relnm;
  end if;
end loop;
end$$
;
ALTER OPERATOR FAMILY gist_int8_ops USING gist ADD
	OPERATOR	6	<> (int8, int8) ,
	OPERATOR	15	<-> (int8, int8) FOR ORDER BY pg_catalog.integer_ops ,
	FUNCTION	8 (int8, int8) gbt_int8_distance (internal, int8, int2, oid, internal) ,
	FUNCTION	9 (int8, int8) gbt_int8_fetch (internal)
;
DROP TABLE pg_temp_4.pg_temp_37896_2, pg_temp_4.pg_temp_37896
;
ALTER TABLE comment_test ALTER COLUMN indexed_col SET DATA TYPE int
;
CREATE TEMP TABLE quad_poly_tbl_ord_seq2 AS
SELECT rank() OVER (ORDER BY p <-> point '123,456') n, p <-> point '123,456' dist, id
FROM quad_poly_tbl WHERE p <@ polygon '((300,300),(400,600),(600,500),(700,200))'
;
ALTER INDEX pg_toast.pg_toast_30363_index RENAME TO reind_con_toast_idx
;
VACUUM (TRUNCATE FALSE) information_schema.sql_parts
;
DROP TABLE btg
;
CREATE TABLE main_table (a int unique, b int)
;
------------------------------------------------------------------------------------------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "email" WITH "simple"
;
----------
[ADD_INDEX] CREATE INDEX macaddr_data_hash ON macaddr_data USING hash (b)
;
----------
[CREATE_PROG_OBJ] create function void_return_expr() returns void as $$
begin
    perform 2+2;
end;$$ language plpgsql
;
----------
[CREATE_TABLE] -- INSERT/DELETE on zero column table
CREATE TABLE zerocol()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_date(date, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_timetz_compress(internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION perl2text(text) RETURNS bool
LANGUAGE plperlu
TRANSFORM FOR TYPE bool
AS $$
return shift;
$$
;
----------
[DROP_TABLE] DROP TABLE ctas_stats_1
;
----------
[CREATE_TABLE] -- Test handling of index predicates - updating attributes in precicates
-- should not block HOT when summarizing indexes are involved. We update
-- a row that was not indexed due to the index predicate, and becomes
-- indexable - the HOT-updated tuple is forwarded to the BRIN index.
CREATE TABLE brin_hot_2 (a int, b int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_shdescription
;
----------
[CREATE_TYPE] -- populate_record
create type jpop as (a text, b int, c timestamp)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_trigger
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_28840
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table alterlock alter column f2 set statistics 150
;
----------
[DROP_TABLE] drop table permtest_parent
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbtreekey8_in(cstring) PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub5 CONNECTION 'i_dont_exist=param' PUBLICATION testpub
;
----------
[CREATE_TABLE] -- check error reporting with column privs on a partitioned table
CREATE TABLE errtst(a text, b text NOT NULL, c text, secret1 text, secret2 text) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM FULL pg_index
;
----------
[DROP_PROG_OBJ] drop function zero_divide()
;
----------
[CREATE_TABLE|METADATA] CREATE TABLE prevstats AS
SELECT t.seq_scan, t.seq_tup_read, t.idx_scan, t.idx_tup_fetch,
       (b.heap_blks_read + b.heap_blks_hit) AS heap_blks,
       (b.idx_blks_read + b.idx_blks_hit) AS idx_blks,
       pg_stat_get_snapshot_timestamp() as snap_ts
  FROM pg_catalog.pg_stat_user_tables AS t,
       pg_catalog.pg_statio_user_tables AS b
 WHERE t.relname='tenk2' AND b.relname='tenk2'
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
----------
[TRUNCATE_TABLE] TRUNCATE inh_parent
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26389_2
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.48
 * ROLE_UDT_GRANTS view
 */

CREATE VIEW role_udt_grants AS
    SELECT grantor,
           grantee,
           udt_catalog,
           udt_schema,
           udt_name,
           privilege_type,
           is_grantable
    FROM udt_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE test (a int, b char(20))
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_28840_2, pg_temp_3.pg_temp_28840
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33450_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW "public"."key_dependent_view_no_cols" AS
 SELECT
   FROM "public"."view_base_table"
  GROUP BY "key"
 HAVING ("length"(("data")::"text") > 0)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_38079_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p2 PARTITION OF pagg_tab_ml FOR VALUES FROM (10) TO (20) PARTITION BY LIST (c)
;
----------
[DROP_POLICY] -- text search dictionary
DROP TEXT SEARCH DICTIONARY test_tsdict_exists
;
----------
[CREATE_TABLE] CREATE TABLE brassica PARTITION OF vegetables FOR VALUES IN ('brassica')
;
----------
[BLOCK] -- Test subscripting in plpgsql
do $$ declare h hstore;
begin h['a'] := 'b'; raise notice 'h = %, h[a] = %', h, h['a']; end $$
;
----------
[CREATE_TABLE] create table parted_irreg_ancestor (fd text, b text, fd2 int, fd3 int, a int)
  partition by range (b)
;
----------
[ALTER_TABLE] alter table trigtest disable trigger all
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE temptest(col) ON COMMIT DELETE ROWS AS SELECT 1
;
----------
[CREATE_TABLE] -- set not null verified by constraints
create table atacc1 (test_a int, test_b int)
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE brintest_unlogged (n numrange)
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl(a float)
;
----------
[DROP_TABLE] DROP TABLE gtest_child3
;
----------
[BLOCK] do $$
declare relnm text;
  reloid oid;
  shared bool;
  lowoid oid;
  pinned bool;
begin
for relnm, reloid, shared in
  select relname, oid, relisshared from pg_class
  where EXISTS(
      SELECT * FROM pg_attribute
      WHERE attrelid = pg_class.oid AND attname = 'oid')
    and relkind = 'r' and oid < 16384 order by 1
loop
  execute 'select min(oid) from ' || relnm into lowoid;
  continue when lowoid is null or lowoid >= 16384;
  if shared then
    pinned := exists(select 1 from pg_shdepend
                     where refclassid = reloid and refobjid = lowoid
                     and deptype = 'p');
  else
    pinned := exists(select 1 from pg_depend
                     where refclassid = reloid and refobjid = lowoid
                     and deptype = 'p');
  end if;
  if not pinned then
    raise notice '% contains unpinned initdb-created object(s)', relnm;
  end if;
end loop;
end$$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_int8_ops USING gist ADD
	OPERATOR	6	<> (int8, int8) ,
	OPERATOR	15	<-> (int8, int8) FOR ORDER BY pg_catalog.integer_ops ,
	FUNCTION	8 (int8, int8) gbt_int8_distance (internal, int8, int2, oid, internal) ,
	FUNCTION	9 (int8, int8) gbt_int8_fetch (internal)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_37896_2, pg_temp_4.pg_temp_37896
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN indexed_col SET DATA TYPE int
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE quad_poly_tbl_ord_seq2 AS
SELECT rank() OVER (ORDER BY p <-> point '123,456') n, p <-> point '123,456' dist, id
FROM quad_poly_tbl WHERE p <@ polygon '((300,300),(400,600),(600,500),(700,200))'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30363_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) information_schema.sql_parts
;
----------
[DROP_TABLE] DROP TABLE btg
;
----------
[CREATE_TABLE] CREATE TABLE main_table (a int unique, b int)
;
