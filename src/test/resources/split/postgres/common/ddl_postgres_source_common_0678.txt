ANALYZE (SKIP_LOCKED) pg_catalog.pg_statistic_ext_data
;
CREATE TABLE plt2_adv_p1_null PARTITION OF plt2_adv FOR VALUES IN (NULL)
;
drop table mcrparted5_def
;
DROP TABLE pxtest2
;
alter function whoami() security definer
;
CREATE TABLE "public"."itest5" (
    "a" integer NOT NULL,
    "b" "text"
)
;
drop table emp
;
CREATE TEMP TABLE pg_temp_9.pg_temp_26894_2 (tid pg_catalog.tid)
;
VACUUM (TRUNCATE FALSE) information_schema.sql_sizing
;
CREATE TYPE person_type AS (id int, name text)
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer
;
ALTER TABLE no_index_cleanup ALTER COLUMN t SET STORAGE EXTERNAL
;
-- Create partitions intentionally in descending bound order, so as to test
-- that update-row-movement works with the leaf partitions not in bound order.
CREATE TABLE part_b_20_b_30 (e varchar, c numeric, a text, b bigint, d int)
;
CREATE TABLE flt (f float)
;
CREATE TABLE t3 (name TEXT, n INTEGER)
;
CREATE TABLE notnull_tbl4_cld () INHERITS (notnull_tbl4)
;
CREATE TABLE c_star (
	c 			name
) INHERITS (a_star)
;
-- Not a token supported by the configuration's parser, fails.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ADD MAPPING FOR not_a_token WITH ispell
;
create index tprt5_idx on tprt_5 (col1)
;
COMMENT ON TEXT SEARCH DICTIONARY french_stem IS 'snowball stemmer for french language'
;
alter table test_storage alter a set storage plain
;
CREATE MATERIALIZED VIEW
						   dump_test.matview_third (col1) AS
						   SELECT * FROM dump_test.matview_second WITH NO DATA
;
ALTER TABLE ONLY "public"."idxpart2" ATTACH PARTITION "public"."idxpart22" FOR VALUES FROM (200) TO (300)
;
CREATE FUNCTION lca(ltree,ltree,ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ANALYZE pg_catalog.pg_publication_rel
;
CREATE FUNCTION test_event_trigger_guc() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
	obj record;
BEGIN
	FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
	LOOP
		RAISE NOTICE '% dropped %', tg_tag, obj.object_type;
	END LOOP;
END;
$$
;
ALTER VIEW rw_view1 SET (check_option=here)
;
create table parted_trig_1_1 partition of parted_trig_1 for values from (0) to (100)
;
ALTER TABLE plt1_adv ATTACH PARTITION plt1_adv_p1 DEFAULT
;
CREATE OPERATOR "public".#%# (
    FUNCTION = "factorial",
    LEFTARG = bigint
)
;
-- add column to populated table
ALTER TABLE itest13 ADD COLUMN c int GENERATED BY DEFAULT AS IDENTITY
;
CREATE TABLE regression_vacuumdb_test AS select generate_series(1, 10) a, generate_series(2, 11) b
;
DROP EXTENSION test_ext_evttrig
;
ALTER AGGREGATE alt_agg2(int) SET SCHEMA alt_nsp2
;
--
-- Check we don't misoptimize a NOT IN where the subquery returns no rows.
--
create temp table notinouter (a int)
;
DROP TABLE pg_temp_13.pg_temp_25481_2, pg_temp_13.pg_temp_25481
;
CREATE DOMAIN testdomain_i AS int COLLATE "sv-x-icu"
;
ALTER TABLE test_part_d_1 REPLICA IDENTITY USING INDEX test_part_d_1_pkey
;
ALTER FUNCTION functest_C_3(int) SECURITY DEFINER
;
create temp table users (userid text, seq int, email text, todrop bool, moredrop int, enabled bool)
;
DROP SCHEMA IF EXISTS split_builtin_trigger CASCADE
;
--
-- Test case for bug #5514 (mishandling of whole-row Vars in subselects)
--

create temp table table_a(id integer)
;
CREATE TEMP TABLE test_dropped
(
	col1 INT NOT NULL DEFAULT 111,
	id SERIAL PRIMARY KEY,
	col2 INT NOT NULL DEFAULT 112,
	col2b INT NOT NULL DEFAULT 113
)
;
CREATE INDEX on tbl USING gin(c1, c2) INCLUDE (c3, c4)
;
ALTER SEQUENCE fkey_table_seq_renamed
  SET SCHEMA foo
;
-- Domains
CREATE DOMAIN domain_stats AS int CHECK (VALUE > 0)
;
CREATE VIEW my_backend_pid (pid) AS SELECT pg_backend_pid()
;
-- No base relations
CREATE VIEW ro_view11 AS SELECT b1.a, b2.b FROM base_tbl b1, base_tbl b2
;
ALTER TABLE pg_toast.pg_toast_30949 RENAME TO reind_con_toast
;
CREATE TABLE p2 partition of pt for values in (2)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p1_null PARTITION OF plt2_adv FOR VALUES IN (NULL)
;
----------
[DROP_TABLE] drop table mcrparted5_def
;
----------
[DROP_TABLE] DROP TABLE pxtest2
;
----------
[ALTER_PROG_OBJ] alter function whoami() security definer
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest5" (
    "a" integer NOT NULL,
    "b" "text"
)
;
----------
[DROP_TABLE] drop table emp
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_26894_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) information_schema.sql_sizing
;
----------
[CREATE_TYPE] CREATE TYPE person_type AS (id int, name text)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE no_index_cleanup ALTER COLUMN t SET STORAGE EXTERNAL
;
----------
[CREATE_TABLE] -- Create partitions intentionally in descending bound order, so as to test
-- that update-row-movement works with the leaf partitions not in bound order.
CREATE TABLE part_b_20_b_30 (e varchar, c numeric, a text, b bigint, d int)
;
----------
[CREATE_TABLE] CREATE TABLE flt (f float)
;
----------
[CREATE_TABLE] CREATE TABLE t3 (name TEXT, n INTEGER)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl4_cld () INHERITS (notnull_tbl4)
;
----------
[CREATE_TABLE] CREATE TABLE c_star (
	c 			name
) INHERITS (a_star)
;
----------
[ALTER_POLICY] -- Not a token supported by the configuration's parser, fails.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ADD MAPPING FOR not_a_token WITH ispell
;
----------
[ADD_INDEX] create index tprt5_idx on tprt_5 (col1)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY french_stem IS 'snowball stemmer for french language'
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table test_storage alter a set storage plain
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW
						   dump_test.matview_third (col1) AS
						   SELECT * FROM dump_test.matview_second WITH NO DATA
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."idxpart2" ATTACH PARTITION "public"."idxpart22" FOR VALUES FROM (200) TO (300)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lca(ltree,ltree,ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_publication_rel
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_event_trigger_guc() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
	obj record;
BEGIN
	FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
	LOOP
		RAISE NOTICE '% dropped %', tg_tag, obj.object_type;
	END LOOP;
END;
$$
;
----------
[ALTER_VIEW] ALTER VIEW rw_view1 SET (check_option=here)
;
----------
[CREATE_TABLE] create table parted_trig_1_1 partition of parted_trig_1 for values from (0) to (100)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt1_adv ATTACH PARTITION plt1_adv_p1 DEFAULT
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".#%# (
    FUNCTION = "factorial",
    LEFTARG = bigint
)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- add column to populated table
ALTER TABLE itest13 ADD COLUMN c int GENERATED BY DEFAULT AS IDENTITY
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE regression_vacuumdb_test AS select generate_series(1, 10) a, generate_series(2, 11) b
;
----------
[DROP_LIBRARY] DROP EXTENSION test_ext_evttrig
;
----------
[ALTER_PROG_OBJ] ALTER AGGREGATE alt_agg2(int) SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] --
-- Check we don't misoptimize a NOT IN where the subquery returns no rows.
--
create temp table notinouter (a int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_25481_2, pg_temp_13.pg_temp_25481
;
----------
[CREATE_TYPE] CREATE DOMAIN testdomain_i AS int COLLATE "sv-x-icu"
;
----------
[ALTER_TABLE] ALTER TABLE test_part_d_1 REPLICA IDENTITY USING INDEX test_part_d_1_pkey
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_C_3(int) SECURITY DEFINER
;
----------
[CREATE_TABLE] create temp table users (userid text, seq int, email text, todrop bool, moredrop int, enabled bool)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_builtin_trigger CASCADE
;
----------
[CREATE_TABLE] --
-- Test case for bug #5514 (mishandling of whole-row Vars in subselects)
--

create temp table table_a(id integer)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE test_dropped
(
	col1 INT NOT NULL DEFAULT 111,
	id SERIAL PRIMARY KEY,
	col2 INT NOT NULL DEFAULT 112,
	col2b INT NOT NULL DEFAULT 113
)
;
----------
[ADD_INDEX] CREATE INDEX on tbl USING gin(c1, c2) INCLUDE (c3, c4)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE fkey_table_seq_renamed
  SET SCHEMA foo
;
----------
[CREATE_TYPE] -- Domains
CREATE DOMAIN domain_stats AS int CHECK (VALUE > 0)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW my_backend_pid (pid) AS SELECT pg_backend_pid()
;
----------
[CREATE_VIEW(SELECT)] -- No base relations
CREATE VIEW ro_view11 AS SELECT b1.a, b2.b FROM base_tbl b1, base_tbl b2
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30949 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE p2 partition of pt for values in (2)
;
