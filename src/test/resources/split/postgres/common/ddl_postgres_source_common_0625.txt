CREATE AGGREGATE "public"."myaggn10a"(integer) (
    SFUNC = "public"."tfp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
-- error
DROP TYPE base_type
;
-- this creates new indexes for all the partitions of parted_inval_tab_1,
-- discarding the invalid index created previously as what is chosen.
alter table parted_inval_tab attach partition parted_inval_tab_1
  for values from (1) to (100)
;
ALTER OPERATOR ^~ (lquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE FUNCTION cube_enlarge(cube, float8, int4)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create or replace function get_users() returns setof users as
$$ SELECT * FROM users ORDER BY userid; $$
language sql stable
;
ALTER INDEX dummy_test_idx SET (option_bool = 3.4)
;
alter table def_test alter column c1 set default 'wrong_datatype'
;
alter table mlparted attach partition mlparted2 for values from (1, 10) to (1, 20)
;
-- check the ability to use multiple functional dependencies
CREATE TABLE functional_dependencies_multi (
	a INTEGER,
	b INTEGER,
	c INTEGER,
	d INTEGER
)
WITH (autovacuum_enabled = off)
;
DROP TABLE INTERVAL_MULDIV_TBL
;
create index on at_partitioned (a)
;
-- this creates an invalid index.
create index parted_inval_ixd_1 on only parted_inval_tab_1 (a)
;
CREATE DATABASE "regression	 !""x""#$%&'()*+-./0123456" WITH TEMPLATE = template0 ENCODING = 'LATIN1' LC_COLLATE = 'C' LC_CTYPE = 'C'
;
ALTER SUBSCRIPTION regress_mysub1 CONNECTION 'port=29715 host=/tmp/jOoI3lrZrW dbname=postgres'
;
CREATE RULE r2 AS ON INSERT TO pg_description DO INSTEAD NOTHING
;
-- set unlogged
CREATE TABLE logged1(f1 SERIAL PRIMARY KEY, f2 TEXT)
;
CREATE EXTENSION test_ext_req_schema1 SCHEMA test_func_dep1
;
ALTER TABLE split_alter_table.base_table DISABLE ROW LEVEL SECURITY
;
-- Rebuild the index using a different fillfactor
ALTER INDEX hash_split_index SET (fillfactor = 10)
;
CREATE PUBLICATION testpub3
;
CREATE TABLE inhtc () INHERITS (inhtb)
;
-- OK

CREATE TEXT SEARCH PARSER alt_ts_prs2
    (start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
CREATE OPERATOR <->> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = word_similarity_dist_commutator_op,
        COMMUTATOR = '<<->'
)
;
drop table tab_batch_sharded
;
drop table selfconflict
;
CREATE OR REPLACE FUNCTION pg_logical_emit_message(
    transactional boolean,
    prefix text,
    message bytea,
    flush boolean DEFAULT false)
RETURNS pg_lsn
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_logical_emit_message_bytea'
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_opfamily
;
-- test unique constraint adding

create table atacc1 ( test int )
;
ALTER TABLE pg_toast.pg_toast_30599 RENAME TO reind_con_toast
;
-- Partitioned index
CREATE TABLE btree_part (id int4) PARTITION BY RANGE (id)
;
--
-- CREATE_SEQUENCE
--

CREATE SEQUENCE fkey_table_seq
  INCREMENT BY 1
  MINVALUE 0
  MAXVALUE 1000000
  START 10
  CACHE 10
  CYCLE
;
do $$
declare r1 two_int8s;
begin
  raise notice 'r1 = %', r1;
  raise notice 'r1.q1 = %', r1.q1;
  raise notice 'r1.q2 = %', r1.q2;
  raise notice 'r1 = %', r1;
end$$
;
ALTER TABLE ONLY "regress_indexing"."pk5" ATTACH PARTITION "regress_indexing"."pk51" FOR VALUES FROM (4000) TO (4500)
;
create table j3 as select unique1, tenthous from onek
;
-- test that parallel_restricted function doesn't run in worker
alter table tenk1 set (parallel_workers = 4)
;
create table invalid_check_con(f1 int)
;
DROP TABLE tab1
;
-- test extraction of FieldSelect field names (get_name_for_var_field)

create view tt24v as
with cte as materialized (select r from (values(1,2),(3,4)) r)
select (r).column2 as col_a, (rr).column2 as col_b from
  cte join (select rr from (values(1,7),(3,8)) rr limit 2) ss
  on (r).column1 = (rr).column1
;
CREATE TABLE split_table_idx.child_inherit (
    child_note text DEFAULT 'child'
) INHERITS (split_table_idx.base_inherit)
;
ANALYZE fkpart5.fk11
;
CREATE FOREIGN TABLE ft_part2 (a INT NOT NULL) SERVER s0
;
--
-- INT8
-- Test int8 64-bit integers.
--
CREATE TABLE INT8_TBL(q1 int8, q2 int8)
;
-- The above bug requires function removal to happen after a catalog
    -- invalidation. dependency.c sorts objects in descending oid order so
    -- that newer objects are deleted before older objects, so create a
    -- table after.
    CREATE TEMPORARY TABLE invalidate_catalog_cache()
;
CREATE AGGREGATE regtest_agg (
           sfunc1 = int4pl, basetype = int4, stype1 = int4, initcond1 = '0'
)
;
ANALYZE pg_temp_11.pg_temp_26628_2
;
-- test overflows during CREATE INDEX with extreme timestamp values
CREATE TABLE brin_timestamp_test(a TIMESTAMPTZ)
;
do $$
declare x var_record not null := null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
-- failed (not owner)
ALTER TEXT SEARCH DICTIONARY alt_ts_dict1 RENAME TO alt_ts_dict4
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub4
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn10a"(integer) (
    SFUNC = "public"."tfp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
----------
[DROP_TYPE] -- error
DROP TYPE base_type
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- this creates new indexes for all the partitions of parted_inval_tab_1,
-- discarding the invalid index created previously as what is chosen.
alter table parted_inval_tab attach partition parted_inval_tab_1
  for values from (1) to (100)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^~ (lquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_enlarge(cube, float8, int4)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function get_users() returns setof users as
$$ SELECT * FROM users ORDER BY userid; $$
language sql stable
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_bool = 3.4)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table def_test alter column c1 set default 'wrong_datatype'
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table mlparted attach partition mlparted2 for values from (1, 10) to (1, 20)
;
----------
[CREATE_TABLE] -- check the ability to use multiple functional dependencies
CREATE TABLE functional_dependencies_multi (
	a INTEGER,
	b INTEGER,
	c INTEGER,
	d INTEGER
)
WITH (autovacuum_enabled = off)
;
----------
[DROP_TABLE] DROP TABLE INTERVAL_MULDIV_TBL
;
----------
[ADD_INDEX] create index on at_partitioned (a)
;
----------
[ADD_INDEX] -- this creates an invalid index.
create index parted_inval_ixd_1 on only parted_inval_tab_1 (a)
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression	 !""x""#$%&'()*+-./0123456" WITH TEMPLATE = template0 ENCODING = 'LATIN1' LC_COLLATE = 'C' LC_CTYPE = 'C'
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_mysub1 CONNECTION 'port=29715 host=/tmp/jOoI3lrZrW dbname=postgres'
;
----------
[CREATE_POLICY] CREATE RULE r2 AS ON INSERT TO pg_description DO INSTEAD NOTHING
;
----------
[CREATE_TABLE] -- set unlogged
CREATE TABLE logged1(f1 SERIAL PRIMARY KEY, f2 TEXT)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext_req_schema1 SCHEMA test_func_dep1
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table DISABLE ROW LEVEL SECURITY
;
----------
[ALTER_INDEX] -- Rebuild the index using a different fillfactor
ALTER INDEX hash_split_index SET (fillfactor = 10)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub3
;
----------
[CREATE_TABLE] CREATE TABLE inhtc () INHERITS (inhtb)
;
----------
[CREATE_POLICY] -- OK

CREATE TEXT SEARCH PARSER alt_ts_prs2
    (start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <->> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = word_similarity_dist_commutator_op,
        COMMUTATOR = '<<->'
)
;
----------
[DROP_TABLE] drop table tab_batch_sharded
;
----------
[DROP_TABLE] drop table selfconflict
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_logical_emit_message(
    transactional boolean,
    prefix text,
    message bytea,
    flush boolean DEFAULT false)
RETURNS pg_lsn
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_logical_emit_message_bytea'
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_opfamily
;
----------
[CREATE_TABLE] -- test unique constraint adding

create table atacc1 ( test int )
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30599 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- Partitioned index
CREATE TABLE btree_part (id int4) PARTITION BY RANGE (id)
;
----------
[CREATE_SEQUENCE] --
-- CREATE_SEQUENCE
--

CREATE SEQUENCE fkey_table_seq
  INCREMENT BY 1
  MINVALUE 0
  MAXVALUE 1000000
  START 10
  CACHE 10
  CYCLE
;
----------
[BLOCK] do $$
declare r1 two_int8s;
begin
  raise notice 'r1 = %', r1;
  raise notice 'r1.q1 = %', r1.q1;
  raise notice 'r1.q2 = %', r1.q2;
  raise notice 'r1 = %', r1;
end$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "regress_indexing"."pk5" ATTACH PARTITION "regress_indexing"."pk51" FOR VALUES FROM (4000) TO (4500)
;
----------
[CREATE_TABLE|SELECT] create table j3 as select unique1, tenthous from onek
;
----------
[ALTER_TABLE] -- test that parallel_restricted function doesn't run in worker
alter table tenk1 set (parallel_workers = 4)
;
----------
[CREATE_TABLE] create table invalid_check_con(f1 int)
;
----------
[DROP_TABLE] DROP TABLE tab1
;
----------
[CREATE_VIEW(SELECT)] -- test extraction of FieldSelect field names (get_name_for_var_field)

create view tt24v as
with cte as materialized (select r from (values(1,2),(3,4)) r)
select (r).column2 as col_a, (rr).column2 as col_b from
  cte join (select rr from (values(1,7),(3,8)) rr limit 2) ss
  on (r).column1 = (rr).column1
;
----------
[CREATE_TABLE] CREATE TABLE split_table_idx.child_inherit (
    child_note text DEFAULT 'child'
) INHERITS (split_table_idx.base_inherit)
;
----------
[ADMIN_TABLE] ANALYZE fkpart5.fk11
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft_part2 (a INT NOT NULL) SERVER s0
;
----------
[CREATE_TABLE] --
-- INT8
-- Test int8 64-bit integers.
--
CREATE TABLE INT8_TBL(q1 int8, q2 int8)
;
----------
[CREATE_TABLE] -- The above bug requires function removal to happen after a catalog
    -- invalidation. dependency.c sorts objects in descending oid order so
    -- that newer objects are deleted before older objects, so create a
    -- table after.
    CREATE TEMPORARY TABLE invalidate_catalog_cache()
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE regtest_agg (
           sfunc1 = int4pl, basetype = int4, stype1 = int4, initcond1 = '0'
)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26628_2
;
----------
[CREATE_TABLE] -- test overflows during CREATE INDEX with extreme timestamp values
CREATE TABLE brin_timestamp_test(a TIMESTAMPTZ)
;
----------
[BLOCK] do $$
declare x var_record not null := null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[ALTER_POLICY] -- failed (not owner)
ALTER TEXT SEARCH DICTIONARY alt_ts_dict1 RENAME TO alt_ts_dict4
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub4
;
