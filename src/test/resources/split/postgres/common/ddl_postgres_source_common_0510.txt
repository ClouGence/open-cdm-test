drop function donothingbrtrig_func()
;
CREATE POLICY p0 ON r1 USING (b * 10 = c)
;
create type quad as (c1 complex, c2 complex)
;
ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser
    DROP MAPPING IF EXISTS FOR asciiword, word
;
ALTER TABLE pg_toast.pg_toast_30835 RENAME TO reind_con_toast
;
/*
 * 6.11
 * ASSERTIONS view
 */

-- feature not supported


/*
 * 6.12
 * ATTRIBUTES view
 */

CREATE VIEW attributes AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nc.nspname AS sql_identifier) AS udt_schema,
           CAST(c.relname AS sql_identifier) AS udt_name,
           CAST(a.attname AS sql_identifier) AS attribute_name,
           CAST(a.attnum AS cardinal_number) AS ordinal_position,
           CAST(pg_get_expr(ad.adbin, ad.adrelid) AS character_data) AS attribute_default,
           CAST(CASE WHEN a.attnotnull OR (t.typtype = 'd' AND t.typnotnull) THEN 'NO' ELSE 'YES' END
             AS yes_or_no)
             AS is_nullable, -- This column was apparently removed between SQL:2003 and SQL:2008.

           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nt.nspname = 'pg_catalog' THEN format_type(a.atttypid, null)
                  ELSE 'USER-DEFINED' END
             AS character_data)
             AS data_type,

           CAST(
             _pg_char_max_length(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS character_maximum_length,

           CAST(
             _pg_char_octet_length(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS character_octet_length,

           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,

           CAST(CASE WHEN nco.nspname IS NOT NULL THEN current_database() END AS sql_identifier) AS collation_catalog,
           CAST(nco.nspname AS sql_identifier) AS collation_schema,
           CAST(co.collname AS sql_identifier) AS collation_name,

           CAST(
             _pg_numeric_precision(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_precision,

           CAST(
             _pg_numeric_precision_radix(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_precision_radix,

           CAST(
             _pg_numeric_scale(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_scale,

           CAST(
             _pg_datetime_precision(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS datetime_precision,

           CAST(
             _pg_interval_type(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS character_data)
             AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,

           CAST(current_database() AS sql_identifier) AS attribute_udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS attribute_udt_schema,
           CAST(t.typname AS sql_identifier) AS attribute_udt_name,

           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,

           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST(a.attnum AS sql_identifier) AS dtd_identifier,
           CAST('NO' AS yes_or_no) AS is_derived_reference_attribute

    FROM (pg_attribute a LEFT JOIN pg_attrdef ad ON attrelid = adrelid AND attnum = adnum)
         JOIN (pg_class c JOIN pg_namespace nc ON (c.relnamespace = nc.oid)) ON a.attrelid = c.oid
         JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON a.atttypid = t.oid
         LEFT JOIN (pg_collation co JOIN pg_namespace nco ON (co.collnamespace = nco.oid))
           ON a.attcollation = co.oid AND (nco.nspname, co.collname) <> ('pg_catalog', 'default')

    WHERE a.attnum > 0 AND NOT a.attisdropped
          AND c.relkind IN ('c')
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_type_privilege(c.reltype, 'USAGE'))
;
create table hp_contradict_test_p1 partition of hp_contradict_test for values with (modulus 2, remainder 0)
;
ALTER FUNCTION heap_page_item_attrs(bytea, regclass, bool) PARALLEL RESTRICTED
;
DROP TABLE pg_temp_9.pg_temp_37969_2, pg_temp_9.pg_temp_37969
;
CREATE EVENT TRIGGER "test_event_trigger_guc" ON "sql_drop"
         WHEN TAG IN ('DROP POLICY')
   EXECUTE FUNCTION "public"."test_event_trigger_guc"()
;
create or replace function foreach_test(anyarray)
returns void as $$
declare x int[];
begin
  foreach x slice 1 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
create table parted_index_col_drop(a int, b int, c int)
  partition by list (a)
;
VACUUM (FULL) pg_catalog.pg_ts_dict
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_event_trigger
;
ALTER DOMAIN things VALIDATE CONSTRAINT meow
;
CREATE CAST (citext AS text)    WITHOUT FUNCTION AS IMPLICIT
;
ALTER TABLE rule_merge1 DISABLE RULE rule3
;
ALTER SEQUENCE "public"."itest14_id_seq" OWNED BY "public"."itest14"."id"
;
-- fail, duplicate name
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (provider = icu, lc_collate = ' ||
          quote_literal(current_setting('lc_collate')) ||
          ', lc_ctype = ' ||
          quote_literal(current_setting('lc_ctype')) || ');';
END
$$
;
ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        5       pg_catalog.~ (text, text),
        OPERATOR        6       pg_catalog.~* (text, text)
;
CREATE TABLE "public"."functional_dependencies" (
    "filler1" "text",
    "filler2" numeric,
    "a" integer,
    "b" "text",
    "filler3" "date",
    "c" numeric,
    "d" "text"
)
WITH ("autovacuum_enabled"='off')
;
--
-- Test cases for COPY (INSERT/UPDATE/DELETE) TO
--
create table copydml_test (id serial, t text)
;
create table inh_child2() inherits(inh_parent)
;
alter table other_partitioned_fk add foreign key (a, b)
  references fk_notpartitioned_pk(a, b)
;
ALTER TABLE pg_temp_81.pg_temp_30251_2 ADD COLUMN newdata pg_temp_81.pg_temp_30251
;
drop function scope_test()
;
drop table p2
;
ALTER PUBLICATION testpub_default ADD TABLE testpub_tbl1
;
CREATE OR REPLACE FUNCTION pg_logical_slot_get_changes(
    IN slot_name name, IN upto_lsn pg_lsn, IN upto_nchanges int, VARIADIC options text[] DEFAULT '{}',
    OUT lsn pg_lsn, OUT xid xid, OUT data text)
RETURNS SETOF RECORD
LANGUAGE INTERNAL
VOLATILE ROWS 1000 COST 1000
AS 'pg_logical_slot_get_changes'
;
-- test jsonb[] -> python dict
-- dict with array as value
CREATE FUNCTION test1arr(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {"d": [12, 1]})
return len(val)
$$
;
DROP FUNCTION on_login_proc()
;
CREATE OR REPLACE FUNCTION pgstatindex(IN relname regclass,
    OUT version INT,
    OUT tree_level INT,
    OUT index_size BIGINT,
    OUT root_block_no BIGINT,
    OUT internal_pages BIGINT,
    OUT leaf_pages BIGINT,
    OUT empty_pages BIGINT,
    OUT deleted_pages BIGINT,
    OUT avg_leaf_density FLOAT8,
    OUT leaf_fragmentation FLOAT8)
AS '$libdir/pgstattuple', 'pgstatindexbyid_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE EXTENSION test_ext4 SCHEMA pg_temp_4 CASCADE
;
ANALYZE pg_temp_12.pg_temp_29625_2
;
TRUNCATE truncate_a
;
DROP OPERATOR IF EXISTS no_such_schema.+ (int, int)
;
CREATE FUNCTION test1(val ltree) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE ltree
AS $$
plpy.info(repr(val))
return len(val)
$$
;
/* contrib/adminpack/adminpack--2.0--2.1.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* ***********************************************
 * Administrative functions for PostgreSQL
 * *********************************************** */

/* generic file access functions */

CREATE OR REPLACE FUNCTION pg_catalog.pg_file_sync(text)
RETURNS void
AS '$libdir/adminpack', 'pg_file_sync'
LANGUAGE C VOLATILE STRICT
;
create index hidx on testhstore using gin (h)
;
CREATE INDEX t2_gist ON s4.t2 USING GIST (b)
;
COMMENT ON AGGREGATE split_agg_probe.hypo_sum(integer ORDER BY integer) IS 'hypothetical set aggregate'
;
-- Tests for REINDEX CONCURRENTLY
CREATE EXTENSION injection_points
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_authid
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
drop trigger trig_row_after on rem2
;
ALTER TYPE bogus ADD VALUE 'new'
;
CREATE FUNCTION bt_page_stats(IN relname text, IN blkno int4,
    OUT blkno int4,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int4,
    OUT btpo_next int4,
    OUT btpo int4,
    OUT btpo_flags int4)
AS '$libdir/pageinspect', 'bt_page_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TRIGGER t BEFORE INSERT ON addr_nsp.gentable FOR EACH ROW EXECUTE PROCEDURE addr_nsp.trig()
;
DROP TABLE lotest_stash_values
;
CREATE TABLE t3 AS SELECT * FROM t1
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] drop function donothingbrtrig_func()
;
----------
[CREATE_POLICY] CREATE POLICY p0 ON r1 USING (b * 10 = c)
;
----------
[CREATE_TYPE] create type quad as (c1 complex, c2 complex)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser
    DROP MAPPING IF EXISTS FOR asciiword, word
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30835 RENAME TO reind_con_toast
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.11
 * ASSERTIONS view
 */

-- feature not supported


/*
 * 6.12
 * ATTRIBUTES view
 */

CREATE VIEW attributes AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nc.nspname AS sql_identifier) AS udt_schema,
           CAST(c.relname AS sql_identifier) AS udt_name,
           CAST(a.attname AS sql_identifier) AS attribute_name,
           CAST(a.attnum AS cardinal_number) AS ordinal_position,
           CAST(pg_get_expr(ad.adbin, ad.adrelid) AS character_data) AS attribute_default,
           CAST(CASE WHEN a.attnotnull OR (t.typtype = 'd' AND t.typnotnull) THEN 'NO' ELSE 'YES' END
             AS yes_or_no)
             AS is_nullable, -- This column was apparently removed between SQL:2003 and SQL:2008.

           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nt.nspname = 'pg_catalog' THEN format_type(a.atttypid, null)
                  ELSE 'USER-DEFINED' END
             AS character_data)
             AS data_type,

           CAST(
             _pg_char_max_length(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS character_maximum_length,

           CAST(
             _pg_char_octet_length(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS character_octet_length,

           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,

           CAST(CASE WHEN nco.nspname IS NOT NULL THEN current_database() END AS sql_identifier) AS collation_catalog,
           CAST(nco.nspname AS sql_identifier) AS collation_schema,
           CAST(co.collname AS sql_identifier) AS collation_name,

           CAST(
             _pg_numeric_precision(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_precision,

           CAST(
             _pg_numeric_precision_radix(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_precision_radix,

           CAST(
             _pg_numeric_scale(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS numeric_scale,

           CAST(
             _pg_datetime_precision(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS cardinal_number)
             AS datetime_precision,

           CAST(
             _pg_interval_type(_pg_truetypid(a, t), _pg_truetypmod(a, t))
             AS character_data)
             AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,

           CAST(current_database() AS sql_identifier) AS attribute_udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS attribute_udt_schema,
           CAST(t.typname AS sql_identifier) AS attribute_udt_name,

           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,

           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST(a.attnum AS sql_identifier) AS dtd_identifier,
           CAST('NO' AS yes_or_no) AS is_derived_reference_attribute

    FROM (pg_attribute a LEFT JOIN pg_attrdef ad ON attrelid = adrelid AND attnum = adnum)
         JOIN (pg_class c JOIN pg_namespace nc ON (c.relnamespace = nc.oid)) ON a.attrelid = c.oid
         JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON a.atttypid = t.oid
         LEFT JOIN (pg_collation co JOIN pg_namespace nco ON (co.collnamespace = nco.oid))
           ON a.attcollation = co.oid AND (nco.nspname, co.collname) <> ('pg_catalog', 'default')

    WHERE a.attnum > 0 AND NOT a.attisdropped
          AND c.relkind IN ('c')
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_type_privilege(c.reltype, 'USAGE'))
;
----------
[CREATE_TABLE] create table hp_contradict_test_p1 partition of hp_contradict_test for values with (modulus 2, remainder 0)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION heap_page_item_attrs(bytea, regclass, bool) PARALLEL RESTRICTED
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_37969_2, pg_temp_9.pg_temp_37969
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER "test_event_trigger_guc" ON "sql_drop"
         WHEN TAG IN ('DROP POLICY')
   EXECUTE FUNCTION "public"."test_event_trigger_guc"()
;
----------
[CREATE_PROG_OBJ] create or replace function foreach_test(anyarray)
returns void as $$
declare x int[];
begin
  foreach x slice 1 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
----------
[CREATE_TABLE] create table parted_index_col_drop(a int, b int, c int)
  partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_ts_dict
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_event_trigger
;
----------
[ALTER_TYPE] ALTER DOMAIN things VALIDATE CONSTRAINT meow
;
----------
[CREATE_PROG_OBJ] CREATE CAST (citext AS text)    WITHOUT FUNCTION AS IMPLICIT
;
----------
[ALTER_TABLE] ALTER TABLE rule_merge1 DISABLE RULE rule3
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."itest14_id_seq" OWNED BY "public"."itest14"."id"
;
----------
[BLOCK] -- fail, duplicate name
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (provider = icu, lc_collate = ' ||
          quote_literal(current_setting('lc_collate')) ||
          ', lc_ctype = ' ||
          quote_literal(current_setting('lc_ctype')) || ');';
END
$$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        5       pg_catalog.~ (text, text),
        OPERATOR        6       pg_catalog.~* (text, text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."functional_dependencies" (
    "filler1" "text",
    "filler2" numeric,
    "a" integer,
    "b" "text",
    "filler3" "date",
    "c" numeric,
    "d" "text"
)
WITH ("autovacuum_enabled"='off')
;
----------
[CREATE_TABLE] --
-- Test cases for COPY (INSERT/UPDATE/DELETE) TO
--
create table copydml_test (id serial, t text)
;
----------
[CREATE_TABLE] create table inh_child2() inherits(inh_parent)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table other_partitioned_fk add foreign key (a, b)
  references fk_notpartitioned_pk(a, b)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_81.pg_temp_30251_2 ADD COLUMN newdata pg_temp_81.pg_temp_30251
;
----------
[DROP_PROG_OBJ] drop function scope_test()
;
----------
[DROP_TABLE] drop table p2
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default ADD TABLE testpub_tbl1
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_logical_slot_get_changes(
    IN slot_name name, IN upto_lsn pg_lsn, IN upto_nchanges int, VARIADIC options text[] DEFAULT '{}',
    OUT lsn pg_lsn, OUT xid xid, OUT data text)
RETURNS SETOF RECORD
LANGUAGE INTERNAL
VOLATILE ROWS 1000 COST 1000
AS 'pg_logical_slot_get_changes'
;
----------
[CREATE_PROG_OBJ] -- test jsonb[] -> python dict
-- dict with array as value
CREATE FUNCTION test1arr(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {"d": [12, 1]})
return len(val)
$$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION on_login_proc()
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pgstatindex(IN relname regclass,
    OUT version INT,
    OUT tree_level INT,
    OUT index_size BIGINT,
    OUT root_block_no BIGINT,
    OUT internal_pages BIGINT,
    OUT leaf_pages BIGINT,
    OUT empty_pages BIGINT,
    OUT deleted_pages BIGINT,
    OUT avg_leaf_density FLOAT8,
    OUT leaf_fragmentation FLOAT8)
AS '$libdir/pgstattuple', 'pgstatindexbyid_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext4 SCHEMA pg_temp_4 CASCADE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_29625_2
;
----------
[TRUNCATE_TABLE] TRUNCATE truncate_a
;
----------
[DROP_PROG_OBJ] DROP OPERATOR IF EXISTS no_such_schema.+ (int, int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1(val ltree) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE ltree
AS $$
plpy.info(repr(val))
return len(val)
$$
;
----------
[CREATE_PROG_OBJ] /* contrib/adminpack/adminpack--2.0--2.1.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* ***********************************************
 * Administrative functions for PostgreSQL
 * *********************************************** */

/* generic file access functions */

CREATE OR REPLACE FUNCTION pg_catalog.pg_file_sync(text)
RETURNS void
AS '$libdir/adminpack', 'pg_file_sync'
LANGUAGE C VOLATILE STRICT
;
----------
[ADD_INDEX] create index hidx on testhstore using gin (h)
;
----------
[ADD_INDEX] CREATE INDEX t2_gist ON s4.t2 USING GIST (b)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE split_agg_probe.hypo_sum(integer ORDER BY integer) IS 'hypothetical set aggregate'
;
----------
[CREATE_LIBRARY] -- Tests for REINDEX CONCURRENTLY
CREATE EXTENSION injection_points
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_authid
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[DROP_TRIGGER] drop trigger trig_row_after on rem2
;
----------
[ALTER_TYPE] ALTER TYPE bogus ADD VALUE 'new'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_page_stats(IN relname text, IN blkno int4,
    OUT blkno int4,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int4,
    OUT btpo_next int4,
    OUT btpo int4,
    OUT btpo_flags int4)
AS '$libdir/pageinspect', 'bt_page_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER t BEFORE INSERT ON addr_nsp.gentable FOR EACH ROW EXECUTE PROCEDURE addr_nsp.trig()
;
----------
[DROP_TABLE] DROP TABLE lotest_stash_values
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE t3 AS SELECT * FROM t1
;
