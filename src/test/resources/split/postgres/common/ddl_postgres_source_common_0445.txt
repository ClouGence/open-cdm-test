CREATE TABLE gtest31_1 (a int, b text GENERATED ALWAYS AS ('hello') STORED, c text) PARTITION BY LIST (a)
;
DROP OPERATOR IF EXISTS split_maint.## (numeric, numeric)
;
ALTER VIEW split_view_probe.base_view_renamed SET SCHEMA split_view_next
;
create or replace function sum_transfn(state int4, n int4) returns int4 as
$$
declare new_state int4;
begin
	raise notice 'sum_transfn called with %', n;
	if state is null then
		if n is not null then
			new_state := n;
			return new_state;
		end if;
		return null;
	elsif n is not null then
		state := state + n;
		return state;
	end if;

	return null;
end
$$ language plpgsql
;
ALTER TABLE new_system_table SET SCHEMA public
;
-- by using a low fillfactor and a wide tuple we can get multiple blocks
    -- with just few rows
    CREATE TABLE ios_bitmap (a int NOT NULL, b int not null, pad char(1024) default '')
    WITH (AUTOVACUUM_ENABLED = false, FILLFACTOR = 10)
;
-- infer anymultirange from anyrange
create function mr_outparam_succeed4(i anyrange, out r anymultirange, out t text)
  as $$ select multirange($1), 'foo'::text $$ language sql
;
COMMENT ON LARGE OBJECT 34021 IS 'I Wandered Lonely as a Cloud'
;
create function f1(x anyarray) returns anyarray as $$
begin
  return x;
end$$ language plpgsql
;
DO $$
BEGIN
	EXECUTE 'EXPLAIN ANALYZE SELECT * INTO TABLE easi FROM int8_tbl';
	EXECUTE 'EXPLAIN ANALYZE CREATE TABLE easi2 AS SELECT * FROM int8_tbl WITH NO DATA';
END$$
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_authid
;
CREATE INDEX t2_gist ON s1.t2 USING GIST (b)
;
CREATE TABLE loct1 (c1 int)
;
CREATE DATABASE tbuiltin9 TEMPLATE template1 LOCALE 'C' LOCALE_PROVIDER builtin
;
CREATE OPERATOR @ (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_contains,
	COMMUTATOR = '~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE POLICY p1 ON r1 AS RESTRICTIVE USING (b > 10)
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '-', quote '-')
;
CREATE TABLE fprt1_p1 (LIKE fprt1)
;
ALTER TABLE T ADD COLUMN c_text TEXT DEFAULT 'hello'
;
-- create a new partition without the dropped column
CREATE TABLE fkpart13_t2_p2 PARTITION OF fkpart13_t2 FOR VALUES IN (2)
;
-- Try (and succeed)
ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full
;
DROP TYPE IF EXISTS split_domain_type_next.shell_type
;
ALTER OPERATOR <@ (_ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE FUNCTION g_cube_picksplit(internal, internal)
RETURNS internal
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER FUNCTION gbt_macad_fetch(internal) PARALLEL SAFE
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) information_schema.sql_parts
;
alter table covidxpart attach partition covidxpart4 for values in (4)
;
CREATE FUNCTION trigfunc() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = % fired on table % during %',
                 TG_NAME, TG_TABLE_NAME, TG_OP;
    return null;
  end;
$$
;
CREATE TABLE tbl1 (a INT, b TEXT)
;
CREATE VIEW "public"."my_credit_card_usage_secure" WITH ("security_barrier"='true') AS
 SELECT "l"."cid",
    "l"."name",
    "l"."tel",
    "l"."passwd",
    "l"."cnum",
    "l"."climit",
    "r"."ymd",
    "r"."usage"
   FROM ("public"."my_credit_card_secure" "l"
     JOIN "public"."credit_usage" "r" USING ("cid"))
;
CREATE UNIQUE INDEX ON test1cs (x)
;
-- ERROR
DROP TABLE lt1
;
CREATE OR REPLACE FUNCTION
  json_populate_recordset(base anyelement, from_json json, use_json_as_text boolean DEFAULT false)
  RETURNS SETOF anyelement LANGUAGE internal STABLE ROWS 100  AS 'json_populate_recordset' PARALLEL SAFE
;
drop table �׻����Ѹ�
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
ANALYZE public.float4_tbl
;
CREATE INDEX gtest22c_pred_idx ON generated_stored_tests.gtest22c USING btree (a) WHERE (b > 0)
;
CREATE TABLE t (c int)
;
drop type intmultirange
;
DROP TABLE droppedtest
;
-- fail, duplicate name
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (provider = icu, locale = ' ||
          quote_literal((SELECT CASE WHEN datlocprovider='i' THEN daticulocale ELSE datcollate END FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
ALTER INDEX concur_reindex_part_index_0 ATTACH PARTITION concur_reindex_part_index_0_1
;
ALTER TABLE pg_temp_18.pg_temp_26864_2 ADD COLUMN newdata pg_temp_18.pg_temp_26864
;
ALTER INDEX "fkpart3"."pk_pkey" ATTACH PARTITION "fkpart3"."pk3_pkey"
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_subscription_rel
;
create index "index_name" on "test"."abc" ("id")
;
CREATE TABLE cidrtmp AS
  SELECT cidr(a) AS a FROM inettmp
;
create table tpart_1(like tpart)
;
CREATE FUNCTION f(int) RETURNS int AS $$ SELECT $1 + 1 $$ LANGUAGE sql
;
CREATE INDEX part_drop_index_locking_idx ON part_drop_index_locking(id)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE gtest31_1 (a int, b text GENERATED ALWAYS AS ('hello') STORED, c text) PARTITION BY LIST (a)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR IF EXISTS split_maint.## (numeric, numeric)
;
----------
[ALTER_VIEW] ALTER VIEW split_view_probe.base_view_renamed SET SCHEMA split_view_next
;
----------
[CREATE_PROG_OBJ] create or replace function sum_transfn(state int4, n int4) returns int4 as
$$
declare new_state int4;
begin
	raise notice 'sum_transfn called with %', n;
	if state is null then
		if n is not null then
			new_state := n;
			return new_state;
		end if;
		return null;
	elsif n is not null then
		state := state + n;
		return state;
	end if;

	return null;
end
$$ language plpgsql
;
----------
[ALTER_TABLE] ALTER TABLE new_system_table SET SCHEMA public
;
----------
[CREATE_TABLE] -- by using a low fillfactor and a wide tuple we can get multiple blocks
    -- with just few rows
    CREATE TABLE ios_bitmap (a int NOT NULL, b int not null, pad char(1024) default '')
    WITH (AUTOVACUUM_ENABLED = false, FILLFACTOR = 10)
;
----------
[CREATE_PROG_OBJ] -- infer anymultirange from anyrange
create function mr_outparam_succeed4(i anyrange, out r anymultirange, out t text)
  as $$ select multirange($1), 'foo'::text $$ language sql
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 34021 IS 'I Wandered Lonely as a Cloud'
;
----------
[CREATE_PROG_OBJ] create function f1(x anyarray) returns anyarray as $$
begin
  return x;
end$$ language plpgsql
;
----------
[BLOCK] DO $$
BEGIN
	EXECUTE 'EXPLAIN ANALYZE SELECT * INTO TABLE easi FROM int8_tbl';
	EXECUTE 'EXPLAIN ANALYZE CREATE TABLE easi2 AS SELECT * FROM int8_tbl WITH NO DATA';
END$$
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_authid
;
----------
[ADD_INDEX] CREATE INDEX t2_gist ON s1.t2 USING GIST (b)
;
----------
[CREATE_TABLE] CREATE TABLE loct1 (c1 int)
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin9 TEMPLATE template1 LOCALE 'C' LOCALE_PROVIDER builtin
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @ (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_contains,
	COMMUTATOR = '~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON r1 AS RESTRICTIVE USING (b > 10)
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '-', quote '-')
;
----------
[CREATE_TABLE] CREATE TABLE fprt1_p1 (LIKE fprt1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE T ADD COLUMN c_text TEXT DEFAULT 'hello'
;
----------
[CREATE_TABLE] -- create a new partition without the dropped column
CREATE TABLE fkpart13_t2_p2 PARTITION OF fkpart13_t2 FOR VALUES IN (2)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Try (and succeed)
ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS split_domain_type_next.shell_type
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <@ (_ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_cube_picksplit(internal, internal)
RETURNS internal
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad_fetch(internal) PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) information_schema.sql_parts
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table covidxpart attach partition covidxpart4 for values in (4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trigfunc() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = % fired on table % during %',
                 TG_NAME, TG_TABLE_NAME, TG_OP;
    return null;
  end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE tbl1 (a INT, b TEXT)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."my_credit_card_usage_secure" WITH ("security_barrier"='true') AS
 SELECT "l"."cid",
    "l"."name",
    "l"."tel",
    "l"."passwd",
    "l"."cnum",
    "l"."climit",
    "r"."ymd",
    "r"."usage"
   FROM ("public"."my_credit_card_secure" "l"
     JOIN "public"."credit_usage" "r" USING ("cid"))
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON test1cs (x)
;
----------
[DROP_TABLE] -- ERROR
DROP TABLE lt1
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  json_populate_recordset(base anyelement, from_json json, use_json_as_text boolean DEFAULT false)
  RETURNS SETOF anyelement LANGUAGE internal STABLE ROWS 100  AS 'json_populate_recordset' PARALLEL SAFE
;
----------
[DROP_TABLE] drop table �׻����Ѹ�
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
----------
[ADMIN_TABLE] ANALYZE public.float4_tbl
;
----------
[ADD_INDEX] CREATE INDEX gtest22c_pred_idx ON generated_stored_tests.gtest22c USING btree (a) WHERE (b > 0)
;
----------
[CREATE_TABLE] CREATE TABLE t (c int)
;
----------
[DROP_TYPE] drop type intmultirange
;
----------
[DROP_TABLE] DROP TABLE droppedtest
;
----------
[BLOCK] -- fail, duplicate name
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (provider = icu, locale = ' ||
          quote_literal((SELECT CASE WHEN datlocprovider='i' THEN daticulocale ELSE datcollate END FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
----------
[ALTER_INDEX] ALTER INDEX concur_reindex_part_index_0 ATTACH PARTITION concur_reindex_part_index_0_1
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_18.pg_temp_26864_2 ADD COLUMN newdata pg_temp_18.pg_temp_26864
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart3"."pk_pkey" ATTACH PARTITION "fkpart3"."pk3_pkey"
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_subscription_rel
;
----------
[ADD_INDEX] create index "index_name" on "test"."abc" ("id")
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE cidrtmp AS
  SELECT cidr(a) AS a FROM inettmp
;
----------
[CREATE_TABLE] create table tpart_1(like tpart)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f(int) RETURNS int AS $$ SELECT $1 + 1 $$ LANGUAGE sql
;
----------
[ADD_INDEX] CREATE INDEX part_drop_index_locking_idx ON part_drop_index_locking(id)
;
