ALTER TABLE ONLY "public"."idxpart_another" ATTACH PARTITION "public"."idxpart_another_1" FOR VALUES FROM (0) TO (100)
;
DROP TABLE pg_temp_26.pg_temp_28654_2, pg_temp_26.pg_temp_28654
;
create temp table temp_inh_oncommit_test1 ()
  inherits(temp_inh_oncommit_test) on commit drop
;
-- tablespace should not change if no rewrite
ALTER TABLE testschema.test_default_tab_p ALTER val TYPE int
;
CREATE UNIQUE INDEX t1_btree_unique ON s3.t1 USING BTREE (i)
;
CREATE TABLE testschema.asselect TABLESPACE regress_tblspace AS SELECT 1
;
CREATE FUNCTION get_altertable_subcmdinfo(IN cmd pg_ddl_command,
    OUT cmdtype text,
    OUT objdesc text)
  RETURNS SETOF record IMMUTABLE STRICT
  AS '$libdir/test_ddl_deparse' LANGUAGE C
;
-- EXPLAIN SubPlans and InitPlans
CREATE TABLE src (a int, b int, c int, d int)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_conversion
;
CREATE SEQUENCE "public"."sj_t1_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES IN ('a')
;
drop function convslot_trig4()
;
CREATE PROCEDURE test_proc5(INOUT a text)
LANGUAGE plpgsql
AS $$
BEGIN
    a := a || '+' || a;
END;
$$
;
ALTER TABLE mcv_lists ALTER COLUMN d TYPE VARCHAR(64)
;
CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub5
;
CREATE TABLE "public"."insert_tbl" (
    "x" integer DEFAULT "nextval"('"public"."insert_seq"'::"regclass"),
    "y" "text" DEFAULT '-NULL-'::"text",
    "z" integer DEFAULT ('-1'::integer * "currval"('"public"."insert_seq"'::"regclass")),
    CONSTRAINT "insert_tbl_check" CHECK ((("x" + "z") = 0)),
    CONSTRAINT "insert_tbl_con" CHECK ((("x" >= 3) AND ("y" <> 'check failed'::"text") AND ("x" < 8)))
)
;
CREATE EXTENSION test_resowner
;
DROP FUNCTION test_ambiguous_funcname(int)
;
-- let's do one where the primary key constraint fails when added
create table atacc1 ( test int )
;
CREATE TABLE "public"."part3_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
PARTITION BY RANGE ("id")
;
CREATE TYPE test_typex AS (a int, b text)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_db_role_setting
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_policy
;
alter table idxpart add primary key (a, b)
;
CREATE UNLOGGED SEQUENCE "public"."spgist_unlogged_tbl_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
-- this should fail for lack of CASCADE
DROP TABLE PKTABLE
;
--
-- test ON_ERROR_ROLLBACK and combined queries
--
CREATE FUNCTION psql_error(msg TEXT) RETURNS BOOLEAN AS $$
  BEGIN
    RAISE EXCEPTION 'error %', msg;
  END;
$$ LANGUAGE plpgsql
;
-- This partitioned index will have no partitions.
CREATE INDEX concur_reindex_part_index_10 ON ONLY concur_reindex_part_10 (c1)
;
create index idxpart_1_idx on only idxpart (b, a)
;
create materialized view matview_visibility_test as select * from regular_table
;
CREATE TYPE ean13 (
	INPUT = ean13_in,
	OUTPUT = ean13_out,
	LIKE = pg_catalog.int8
)
;
CREATE INDEX ON restaurants USING gist(city gist_trgm_ops)
;
drop domain domainnumeric restrict
;
CREATE SCHEMA test_ns_schema_1
       CREATE UNIQUE INDEX abc_a_idx ON abc (a)

       CREATE VIEW abc_view AS
              SELECT a+1 AS a, b+1 AS b FROM abc

       CREATE TABLE abc (
              a serial,
              b int UNIQUE
       )
;
-- Functions
CREATE FUNCTION func_stats(a text DEFAULT 'a_data', b text DEFAULT lower('b_data'))
  RETURNS text AS $$ SELECT $1::text || '_' || $2::text; $$ LANGUAGE SQL
  SET work_mem = '256kB'
;
CREATE OPERATOR FAMILY alt_opf12 USING btree
;
ALTER EXTENSION test_pg_dump DROP MATERIALIZED VIEW test_pg_dump_mv1
;
alter table mlparted11 add a int not null
;
DROP TABLE pg_temp_9.pg_temp_25621_2, pg_temp_9.pg_temp_25621
;
COMMENT ON SEQUENCE asdf IS 'won''t work'
;
CREATE TABLE "public"."d" (
    "aa" integer,
    "bb" "text",
    "cc" "text",
    "dd" "text"
)
;
CREATE TABLE trunc_tbl (d text)
;
CREATE FUNCTION cube_coord_llur(cube, int4)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
alter table parent enable always trigger tg
;
create table vc4table (f1 vc4[])
;
CREATE INDEX parent_index ON parent(a)
;
CREATE INDEX timeidx ON timetmp USING gist ( a )
;
VACUUM FREEZE public.junk
;
--
-- Verify cases that are unsupported with partitioned tables
--
create table parted_trig (a int) partition by list (a)
;
ALTER SEQUENCE IF EXISTS split_sequence_next.seq_full AS bigint INCREMENT BY 3 NO MINVALUE NO MAXVALUE START WITH 7 RESTART WITH 11 CACHE 2 NO CYCLE OWNED BY NONE
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."idxpart_another" ATTACH PARTITION "public"."idxpart_another_1" FOR VALUES FROM (0) TO (100)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_26.pg_temp_28654_2, pg_temp_26.pg_temp_28654
;
----------
[CREATE_TABLE] create temp table temp_inh_oncommit_test1 ()
  inherits(temp_inh_oncommit_test) on commit drop
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- tablespace should not change if no rewrite
ALTER TABLE testschema.test_default_tab_p ALTER val TYPE int
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t1_btree_unique ON s3.t1 USING BTREE (i)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE testschema.asselect TABLESPACE regress_tblspace AS SELECT 1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION get_altertable_subcmdinfo(IN cmd pg_ddl_command,
    OUT cmdtype text,
    OUT objdesc text)
  RETURNS SETOF record IMMUTABLE STRICT
  AS '$libdir/test_ddl_deparse' LANGUAGE C
;
----------
[CREATE_TABLE] -- EXPLAIN SubPlans and InitPlans
CREATE TABLE src (a int, b int, c int, d int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_conversion
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sj_t1_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES IN ('a')
;
----------
[DROP_PROG_OBJ] drop function convslot_trig4()
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc5(INOUT a text)
LANGUAGE plpgsql
AS $$
BEGIN
    a := a || '+' || a;
END;
$$
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE mcv_lists ALTER COLUMN d TYPE VARCHAR(64)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_TABLE] CREATE TABLE "public"."insert_tbl" (
    "x" integer DEFAULT "nextval"('"public"."insert_seq"'::"regclass"),
    "y" "text" DEFAULT '-NULL-'::"text",
    "z" integer DEFAULT ('-1'::integer * "currval"('"public"."insert_seq"'::"regclass")),
    CONSTRAINT "insert_tbl_check" CHECK ((("x" + "z") = 0)),
    CONSTRAINT "insert_tbl_con" CHECK ((("x" >= 3) AND ("y" <> 'check failed'::"text") AND ("x" < 8)))
)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_resowner
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_ambiguous_funcname(int)
;
----------
[CREATE_TABLE] -- let's do one where the primary key constraint fails when added
create table atacc1 ( test int )
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part3_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
PARTITION BY RANGE ("id")
;
----------
[CREATE_TYPE] CREATE TYPE test_typex AS (a int, b text)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_db_role_setting
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_policy
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add primary key (a, b)
;
----------
[CREATE_SEQUENCE] CREATE UNLOGGED SEQUENCE "public"."spgist_unlogged_tbl_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[DROP_TABLE] -- this should fail for lack of CASCADE
DROP TABLE PKTABLE
;
----------
[CREATE_PROG_OBJ] --
-- test ON_ERROR_ROLLBACK and combined queries
--
CREATE FUNCTION psql_error(msg TEXT) RETURNS BOOLEAN AS $$
  BEGIN
    RAISE EXCEPTION 'error %', msg;
  END;
$$ LANGUAGE plpgsql
;
----------
[ADD_INDEX] -- This partitioned index will have no partitions.
CREATE INDEX concur_reindex_part_index_10 ON ONLY concur_reindex_part_10 (c1)
;
----------
[ADD_INDEX] create index idxpart_1_idx on only idxpart (b, a)
;
----------
[CREATE_VIEW(SELECT)] create materialized view matview_visibility_test as select * from regular_table
;
----------
[CREATE_TYPE] CREATE TYPE ean13 (
	INPUT = ean13_in,
	OUTPUT = ean13_out,
	LIKE = pg_catalog.int8
)
;
----------
[ADD_INDEX] CREATE INDEX ON restaurants USING gist(city gist_trgm_ops)
;
----------
[DROP_TYPE] drop domain domainnumeric restrict
;
----------
[CREATE_SCHEMA(SELECT)] CREATE SCHEMA test_ns_schema_1
       CREATE UNIQUE INDEX abc_a_idx ON abc (a)

       CREATE VIEW abc_view AS
              SELECT a+1 AS a, b+1 AS b FROM abc

       CREATE TABLE abc (
              a serial,
              b int UNIQUE
       )
;
----------
[CREATE_PROG_OBJ] -- Functions
CREATE FUNCTION func_stats(a text DEFAULT 'a_data', b text DEFAULT lower('b_data'))
  RETURNS text AS $$ SELECT $1::text || '_' || $2::text; $$ LANGUAGE SQL
  SET work_mem = '256kB'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf12 USING btree
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP MATERIALIZED VIEW test_pg_dump_mv1
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table mlparted11 add a int not null
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_25621_2, pg_temp_9.pg_temp_25621
;
----------
[COMMENT_SEQUENCE] COMMENT ON SEQUENCE asdf IS 'won''t work'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."d" (
    "aa" integer,
    "bb" "text",
    "cc" "text",
    "dd" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_tbl (d text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_coord_llur(cube, int4)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE] alter table parent enable always trigger tg
;
----------
[CREATE_TABLE] create table vc4table (f1 vc4[])
;
----------
[ADD_INDEX] CREATE INDEX parent_index ON parent(a)
;
----------
[ADD_INDEX] CREATE INDEX timeidx ON timetmp USING gist ( a )
;
----------
[ADMIN_TABLE] VACUUM FREEZE public.junk
;
----------
[CREATE_TABLE] --
-- Verify cases that are unsupported with partitioned tables
--
create table parted_trig (a int) partition by list (a)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE IF EXISTS split_sequence_next.seq_full AS bigint INCREMENT BY 3 NO MINVALUE NO MAXVALUE START WITH 7 RESTART WITH 11 CACHE 2 NO CYCLE OWNED BY NONE
;
