ANALYZE pg_temp_18.pg_temp_25732_2
;
create table alter1.t1 (a int)
;
DROP VIEW rw_view
;
CREATE TABLE test (i integer PRIMARY KEY)
;
-- accepted
ALTER FOREIGN TABLE tru_ftable OPTIONS (SET truncatable 'false')
;
CREATE MATERIALIZED VIEW pgss_materialized_view_2 AS
  SELECT * FROM generate_series(1, 5) as id
;
CREATE UNIQUE INDEX "pfield_name" ON "public"."pfield" USING "btree" ("name")
;
ALTER TEXT SEARCH CONFIGURATION yiddish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
-- with start trigger disabled.
ALTER EVENT TRIGGER regress_reindex_start DISABLE
;
--    P    N        N    P
-- should ERROR: tf2p(anyarray, int) not matched by tf2p(int[],anyelement)
CREATE AGGREGATE myaggn14a(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
--
-- SP-GiST index tests
--

CREATE TABLE quad_point_tbl AS
    SELECT point(unique1,unique2) AS p FROM tenk1
;
drop function polyf(x anycompatible, y anycompatible)
;
ALTER INDEX pg_toast.pg_toast_33000_index RENAME TO reind_con_toast_idx
;
create trigger tg_chkslotname before insert
    on PHone for each row execute procedure tg_chkslotname('PH')
;
alter type insenum add value 'i17' before 'L2'
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
-- should fail

ALTER VIEW rw_view2 RESET (check_option)
;
CREATE FUNCTION warn(msg TEXT) RETURNS BOOLEAN LANGUAGE plpgsql
AS $$
  BEGIN RAISE NOTICE 'warn %', msg ; RETURN TRUE ; END
$$
;
CREATE TRIGGER gtest3 AFTER DELETE OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (OLD.b < 0)  
  EXECUTE PROCEDURE gtest_trigger_func()
;
DROP FUNCTION getrngfunc4(int)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_statistic
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
-- ERROR

drop table utrtest
;
CREATE DATABASE regress_regression_group
;
/*
 * text search configuration for armenian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * armenian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY armenian_stem
	(TEMPLATE = snowball, Language = armenian )
;
-- Define the GiST support methods
CREATE FUNCTION gbt_bool_consistent(internal,bool,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create function polyf(anymultirange) returns anyrange
as 'select range_merge($1);' language sql
;
CALL test_proc6(i, x, y)
;
CREATE TABLE "public"."boolvalues" (
    "value" boolean NOT NULL
)
;
CREATE VIEW "public"."nums" AS
 WITH RECURSIVE "nums"("n") AS (
         VALUES (1)
        UNION ALL
         SELECT ("nums_1"."n" + 1)
           FROM "nums" "nums_1"
          WHERE ("nums_1"."n" < 6)
        )
 SELECT "nums"."n"
   FROM "nums"
;
VACUUM (FULL) pg_catalog.pg_range
;
CREATE FUNCTION "public"."noticetrigger"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
  raise notice 'noticetrigger % %', new.f1, new.data;
  return null;
end $$
;
ALTER TABLE pg_temp_14.pg_temp_26565_2 ADD COLUMN newdata pg_temp_14.pg_temp_26565
;
CREATE TABLE "public"."attach_parted" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("b")
;
ALTER TABLE vac_truncate_test RESET (vacuum_truncate)
;
CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub5
;
drop table hp3
;
CREATE SCHEMA "generated_virtual_tests"
;
CREATE FUNCTION split_operator_probe.int_same(left_value integer, right_value integer)
RETURNS boolean
LANGUAGE SQL
IMMUTABLE
AS $$ SELECT left_value = right_value $$
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=27372 host=/tmp/XKM0FyCJ6G dbname=postgres' PUBLICATION mypub
;
TRUNCATE TABLE trunc_c,trunc_d
;
ALTER TABLE ONLY public.ths_p3
    ADD CONSTRAINT ths_p3_mod_data_key UNIQUE (mod, data)
;
-- fail


-- test as part of prepare/execute
CREATE FUNCTION test3() RETURNS void
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
rv = plpy.execute("SELECT 'aa=>bb, cc=>NULL'::hstore AS col1")
assert(rv[0]["col1"] == {'aa': 'bb', 'cc': None})

val = {'a': 1, 'b': 'boo', 'c': None}
plan = plpy.prepare("SELECT $1::text AS col1", ["hstore"])
rv = plpy.execute(plan, [val])
assert(rv[0]["col1"] == '"a"=>"1", "b"=>"boo", "c"=>NULL')
$$
;
VACUUM  verbose pg_authid
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_opclass
;
ALTER TYPE planets ADD VALUE 'jupiter' AFTER 'mars'
;
CREATE EXTENSION file_fdw
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_authid
;
CREATE TABLE collate_test1 (
    a int,
    b text COLLATE "en-x-icu" NOT NULL
)
;
ALTER TABLE table_without_key REPLICA IDENTITY FULL
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_25732_2
;
----------
[CREATE_TABLE] create table alter1.t1 (a int)
;
----------
[DROP_VIEW] DROP VIEW rw_view
;
----------
[CREATE_TABLE] CREATE TABLE test (i integer PRIMARY KEY)
;
----------
[ALTER_TABLE] -- accepted
ALTER FOREIGN TABLE tru_ftable OPTIONS (SET truncatable 'false')
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW pgss_materialized_view_2 AS
  SELECT * FROM generate_series(1, 5) as id
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "pfield_name" ON "public"."pfield" USING "btree" ("name")
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION yiddish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ALTER_TRIGGER] -- with start trigger disabled.
ALTER EVENT TRIGGER regress_reindex_start DISABLE
;
----------
[CREATE_PROG_OBJ] --    P    N        N    P
-- should ERROR: tf2p(anyarray, int) not matched by tf2p(int[],anyelement)
CREATE AGGREGATE myaggn14a(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_TABLE|SELECT] --
-- SP-GiST index tests
--

CREATE TABLE quad_point_tbl AS
    SELECT point(unique1,unique2) AS p FROM tenk1
;
----------
[DROP_PROG_OBJ] drop function polyf(x anycompatible, y anycompatible)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33000_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_chkslotname before insert
    on PHone for each row execute procedure tg_chkslotname('PH')
;
----------
[ALTER_TYPE] alter type insenum add value 'i17' before 'L2'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[ALTER_VIEW] -- should fail

ALTER VIEW rw_view2 RESET (check_option)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION warn(msg TEXT) RETURNS BOOLEAN LANGUAGE plpgsql
AS $$
  BEGIN RAISE NOTICE 'warn %', msg ; RETURN TRUE ; END
$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest3 AFTER DELETE OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (OLD.b < 0)  
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[DROP_PROG_OBJ] DROP FUNCTION getrngfunc4(int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_statistic
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[DROP_TABLE] -- ERROR

drop table utrtest
;
----------
[CREATE_CATALOG] CREATE DATABASE regress_regression_group
;
----------
[CREATE_POLICY] /*
 * text search configuration for armenian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * armenian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY armenian_stem
	(TEMPLATE = snowball, Language = armenian )
;
----------
[CREATE_PROG_OBJ] -- Define the GiST support methods
CREATE FUNCTION gbt_bool_consistent(internal,bool,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] create function polyf(anymultirange) returns anyrange
as 'select range_merge($1);' language sql
;
----------
[CALL_PROG_OBJ] CALL test_proc6(i, x, y)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."boolvalues" (
    "value" boolean NOT NULL
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."nums" AS
 WITH RECURSIVE "nums"("n") AS (
         VALUES (1)
        UNION ALL
         SELECT ("nums_1"."n" + 1)
           FROM "nums" "nums_1"
          WHERE ("nums_1"."n" < 6)
        )
 SELECT "nums"."n"
   FROM "nums"
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_range
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."noticetrigger"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
  raise notice 'noticetrigger % %', new.f1, new.data;
  return null;
end $$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_14.pg_temp_26565_2 ADD COLUMN newdata pg_temp_14.pg_temp_26565
;
----------
[CREATE_TABLE] CREATE TABLE "public"."attach_parted" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("b")
;
----------
[ALTER_TABLE] ALTER TABLE vac_truncate_test RESET (vacuum_truncate)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub5
;
----------
[DROP_TABLE] drop table hp3
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "generated_virtual_tests"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_operator_probe.int_same(left_value integer, right_value integer)
RETURNS boolean
LANGUAGE SQL
IMMUTABLE
AS $$ SELECT left_value = right_value $$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=27372 host=/tmp/XKM0FyCJ6G dbname=postgres' PUBLICATION mypub
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE trunc_c,trunc_d
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY public.ths_p3
    ADD CONSTRAINT ths_p3_mod_data_key UNIQUE (mod, data)
;
----------
[CREATE_PROG_OBJ] -- fail


-- test as part of prepare/execute
CREATE FUNCTION test3() RETURNS void
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
rv = plpy.execute("SELECT 'aa=>bb, cc=>NULL'::hstore AS col1")
assert(rv[0]["col1"] == {'aa': 'bb', 'cc': None})

val = {'a': 1, 'b': 'boo', 'c': None}
plan = plpy.prepare("SELECT $1::text AS col1", ["hstore"])
rv = plpy.execute(plan, [val])
assert(rv[0]["col1"] == '"a"=>"1", "b"=>"boo", "c"=>NULL')
$$
;
----------
[ADMIN_TABLE] VACUUM  verbose pg_authid
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_opclass
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE 'jupiter' AFTER 'mars'
;
----------
[CREATE_LIBRARY] CREATE EXTENSION file_fdw
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_authid
;
----------
[CREATE_TABLE] CREATE TABLE collate_test1 (
    a int,
    b text COLLATE "en-x-icu" NOT NULL
)
;
----------
[ALTER_TABLE] ALTER TABLE table_without_key REPLICA IDENTITY FULL
;
