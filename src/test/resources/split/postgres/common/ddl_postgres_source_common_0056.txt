create function array_to_set(anyarray) returns setof record as $$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$$ language sql strict immutable
;
CREATE TABLE quad_box_tbl_ord_seq1 AS
SELECT rank() OVER (ORDER BY b <-> point '123,456') n, b <-> point '123,456' dist, id
FROM quad_box_tbl
;
create table parent (a float8, b numeric(10,4), c text collate "C")
;
DROP VIEW IF EXISTS split_view_probe.counting_view RESTRICT
;
ALTER FUNCTION gbt_float8_picksplit(internal, internal) PARALLEL SAFE
;
--
-- check transactional behaviour of ALTER TYPE ... ADD VALUE
--
CREATE TYPE bogus AS ENUM('good')
;
CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_analyze_scale_factor = 110.0)
;
CREATE TABLE part_7_a_null (
	c int,
	d int,
	e int,
	LIKE list_parted2,  -- a will have attnum = 4
	CONSTRAINT check_b CHECK (b IS NULL OR b = 'a'),
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
	)
;
CREATE TEMP VIEW pcacheview AS
  SELECT * FROM pcachetest
;
CREATE UNLOGGED TABLE unlogged_table (
    id INT PRIMARY KEY
)
;
CREATE TRIGGER tt
AFTER TRUNCATE ON trunc_trigger_test
FOR EACH STATEMENT
EXECUTE PROCEDURE trunctrigger('after trigger truncate')
;
ALTER TABLE fd_pt1 RENAME CONSTRAINT fd_pt1chk3 TO f2_check
;
CREATE FUNCTION word_similarity_dist_commutator_op(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE OR REPLACE FUNCTION
  jsonb_path_query(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                   silent boolean DEFAULT false)
RETURNS SETOF jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_query'
;
DROP FUNCTION functest_IS_4a CASCADE
;
drop function polyf(x anycompatiblemultirange, y anycompatible, z anycompatible)
;
create index spgist_point_idx2 on spgist_point_tbl using spgist(p) with (fillfactor = 9)
;
CREATE TABLE "public"."ndistinct" (
    "filler1" "text",
    "filler2" numeric,
    "a" integer,
    "b" integer,
    "filler3" "date",
    "c" integer,
    "d" integer
)
WITH ("autovacuum_enabled"='off')
;
COMMENT ON TEXT SEARCH DICTIONARY danish_stem IS 'snowball stemmer for danish language'
;
CREATE FUNCTION gbt_int8_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE schema_rf_x.tab_rf_x (x int primary key)
;
drop function explain_sq_limit()
;
VACUUM (FULL) pg_catalog.pg_class
;
CREATE TABLE list_parted3 (a int NOT NULL) PARTITION BY LIST (a)
;
CREATE VIEW tenant_view AS SELECT * FROM pg_catalog.pg_class
;
DROP FOREIGN TABLE async_p3
;
DROP INDEX tenant_idx
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_proc
;
-- check that adding partitions to a table while it is being used is prevented
create table tab_part_create (a int) partition by list (a)
;
ALTER TABLE evttrig.id ALTER COLUMN col_d DROP IDENTITY,
  ALTER COLUMN col_d SET DATA TYPE int
;
CREATE TABLE "public"."func_index_heap" (
    "f1" "text",
    "f2" "text"
)
;
ALTER TABLE notnull_tbl4_lk3 RENAME CONSTRAINT notnull_tbl4_a_not_null TO a_nn
;
ALTER FUNCTION gbt_tstz_distance(internal, timestamp with time zone, smallint, oid, internal) PARALLEL SAFE
;
CALL ptest6a(1, null)
;
CREATE TEMP TABLE test_jsonb (
       json_type text,
       test_json jsonb
)
;
-- fails
ALTER TYPE test_type2 DROP ATTRIBUTE b CASCADE
;
create table mc2p2 partition of mc2p for values from (1, 1) to (2, minvalue)
;
drop table cc3
;
DROP TRIGGER trig_row_before_delete ON rem1
;
ALTER TABLE list_parted2 ATTACH PARTITION part_6 FOR VALUES IN (6)
;
-- Test TRUNCATE with inheritance

CREATE TABLE trunc_f (col1 integer primary key)
;
ALTER TABLE pg_toast.pg_toast_28148 RENAME TO reind_con_toast
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_init_privs
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) information_schema.sql_sizing
;
create table inh_child(f1 int not null, f2 text not null) inherits(inh_parent_1, inh_parent_2)
;
create table parted_pk_detach_test (a int primary key) partition by list (a)
;
CREATE PROCEDURE ptest10(OUT a int, IN b int, IN c int)
LANGUAGE SQL AS $$ SELECT b - c $$
;
ALTER FUNCTION gbt_int4_union(internal, internal) PARALLEL SAFE
;
DROP TABLE test_constraints_inh
;
create schema stts_s1
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create function array_to_set(anyarray) returns setof record as $$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$$ language sql strict immutable
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE quad_box_tbl_ord_seq1 AS
SELECT rank() OVER (ORDER BY b <-> point '123,456') n, b <-> point '123,456' dist, id
FROM quad_box_tbl
;
----------
[CREATE_TABLE] create table parent (a float8, b numeric(10,4), c text collate "C")
;
----------
[DROP_VIEW] DROP VIEW IF EXISTS split_view_probe.counting_view RESTRICT
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float8_picksplit(internal, internal) PARALLEL SAFE
;
----------
[CREATE_TYPE] --
-- check transactional behaviour of ALTER TYPE ... ADD VALUE
--
CREATE TYPE bogus AS ENUM('good')
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_analyze_scale_factor = 110.0)
;
----------
[CREATE_TABLE] CREATE TABLE part_7_a_null (
	c int,
	d int,
	e int,
	LIKE list_parted2,  -- a will have attnum = 4
	CONSTRAINT check_b CHECK (b IS NULL OR b = 'a'),
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
	)
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW pcacheview AS
  SELECT * FROM pcachetest
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE unlogged_table (
    id INT PRIMARY KEY
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER tt
AFTER TRUNCATE ON trunc_trigger_test
FOR EACH STATEMENT
EXECUTE PROCEDURE trunctrigger('after trigger truncate')
;
----------
[RENAME_CONSTRAINT] ALTER TABLE fd_pt1 RENAME CONSTRAINT fd_pt1chk3 TO f2_check
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION word_similarity_dist_commutator_op(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_query(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                   silent boolean DEFAULT false)
RETURNS SETOF jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_query'
;
----------
[DROP_PROG_OBJ] DROP FUNCTION functest_IS_4a CASCADE
;
----------
[DROP_PROG_OBJ] drop function polyf(x anycompatiblemultirange, y anycompatible, z anycompatible)
;
----------
[ADD_INDEX] create index spgist_point_idx2 on spgist_point_tbl using spgist(p) with (fillfactor = 9)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ndistinct" (
    "filler1" "text",
    "filler2" numeric,
    "a" integer,
    "b" integer,
    "filler3" "date",
    "c" integer,
    "d" integer
)
WITH ("autovacuum_enabled"='off')
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY danish_stem IS 'snowball stemmer for danish language'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int8_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE schema_rf_x.tab_rf_x (x int primary key)
;
----------
[DROP_PROG_OBJ] drop function explain_sq_limit()
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_class
;
----------
[CREATE_TABLE] CREATE TABLE list_parted3 (a int NOT NULL) PARTITION BY LIST (a)
;
----------
[CREATE_VIEW(METADATA)] CREATE VIEW tenant_view AS SELECT * FROM pg_catalog.pg_class
;
----------
[DROP_TABLE] DROP FOREIGN TABLE async_p3
;
----------
[DROP_INDEX] DROP INDEX tenant_idx
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_proc
;
----------
[CREATE_TABLE] -- check that adding partitions to a table while it is being used is prevented
create table tab_part_create (a int) partition by list (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE evttrig.id ALTER COLUMN col_d DROP IDENTITY,
  ALTER COLUMN col_d SET DATA TYPE int
;
----------
[CREATE_TABLE] CREATE TABLE "public"."func_index_heap" (
    "f1" "text",
    "f2" "text"
)
;
----------
[RENAME_CONSTRAINT] ALTER TABLE notnull_tbl4_lk3 RENAME CONSTRAINT notnull_tbl4_a_not_null TO a_nn
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_tstz_distance(internal, timestamp with time zone, smallint, oid, internal) PARALLEL SAFE
;
----------
[CALL_PROG_OBJ] CALL ptest6a(1, null)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE test_jsonb (
       json_type text,
       test_json jsonb
)
;
----------
[ALTER_TYPE] -- fails
ALTER TYPE test_type2 DROP ATTRIBUTE b CASCADE
;
----------
[CREATE_TABLE] create table mc2p2 partition of mc2p for values from (1, 1) to (2, minvalue)
;
----------
[DROP_TABLE] drop table cc3
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_before_delete ON rem1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 ATTACH PARTITION part_6 FOR VALUES IN (6)
;
----------
[CREATE_TABLE] -- Test TRUNCATE with inheritance

CREATE TABLE trunc_f (col1 integer primary key)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28148 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_init_privs
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) information_schema.sql_sizing
;
----------
[CREATE_TABLE] create table inh_child(f1 int not null, f2 text not null) inherits(inh_parent_1, inh_parent_2)
;
----------
[CREATE_TABLE] create table parted_pk_detach_test (a int primary key) partition by list (a)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest10(OUT a int, IN b int, IN c int)
LANGUAGE SQL AS $$ SELECT b - c $$
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int4_union(internal, internal) PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE test_constraints_inh
;
----------
[CREATE_SCHEMA] create schema stts_s1
;
