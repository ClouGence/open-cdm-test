-- Test for renaming in diamond inheritance
CREATE TABLE inht2 (x int) INHERITS (inht1)
;
CREATE VIEW tststats.v AS SELECT * FROM tststats.t
;
--table rewrite won't validate invalid constraint
ALTER TABLE notnull_tbl1 ADD column d float8 default random()
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "email" WITH "simple"
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub5
;
create temp table btree_bpchar (f1 text collate "C")
;
ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p3" FOR VALUES IN ('0008', '0009', '0010', '0011')
;
CREATE INDEX val_brin ON brin_hot using brin(val)
;
TRUNCATE fk_partitioned_fk_2
;
ALTER TABLE test_inh_check ALTER COLUMN a TYPE numeric
;
create foreign table test_foreign_table () server dummy_server
;
create table mutable2(f0 text, f1 int, f2 text)
;
-- ok
ALTER TABLE itest4 ALTER COLUMN a DROP NOT NULL
;
create temp table t1 (a integer primary key)
;
-- Some casting expression are simplified to Const
CREATE TABLE test_squash_jsonb (id int, data jsonb)
;
-- commands that should fail

CREATE AGGREGATE priv_testagg1a(priv_testdomain1) (sfunc = int4_sum, stype = bigint)
;
ANALYZE public.default_tbl
;
CREATE TABLE will_get_dropped_in_ts (a int, b text)
;
CREATE TABLE "public"."fk_partitioned_fk_4_1" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
drop index ec1_expr3
;
CREATE FUNCTION oid_dist(oid, oid)
RETURNS oid
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- invalid relkinds for statistics
CREATE SEQUENCE stats_import.testseq
;
DROP EXTENSION plperl CASCADE
;
ANALYZE public.depth2
;
create temp table foo(f1 int, f2 int)
;
CREATE OR REPLACE FUNCTION
  pg_backup_start(label text, fast boolean DEFAULT false)
  RETURNS pg_lsn STRICT VOLATILE LANGUAGE internal AS 'pg_backup_start'
  PARALLEL RESTRICTED
;
drop operator class at_test_sql_partop using btree
;
CREATE OPERATOR ^? (
        LEFTARG = _lquery,
	RIGHTARG = ltree,
	PROCEDURE = lt_q_rregex,
	COMMUTATOR = '^?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
-- Bug #17607: variant of above in which trigger function raises an error;
-- we don't see any ill effects unless trigger tuple requires mapping

create table convslot_test_parent (id int primary key, val int)
partition by range (id)
;
CREATE EXTENSION isn
;
COMMENT ON FUNCTION seg_ge(seg, seg) IS
'greater than or equal'
;
CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=26577 host=/tmp/Lnf6IHniY8 dbname=postgres' PUBLICATION pub_viaroot
;
CREATE TABLE collate_test_fail (
    a int,
    b text COLLATE "foo"
)
;
CREATE INDEX onek2_stu1_prtl ON onek2 USING btree(stringu1 name_ops)
	where onek2.stringu1 >= 'J' and onek2.stringu1 < 'K'
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = true, two_phase = true)
;
DO $$
DECLARE _a int; _b int; _c int[];
BEGIN
  _a := 10;
  CALL test_proc12(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
ALTER TABLE test4 ALTER COLUMN f1 SET STORAGE EXTERNAL
;
alter table p1 add constraint p2chk check (ff1 > 10)
;
--
-- Reset and set params
--

ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = NONE)
;
ALTER TABLE constraint_rename_test RENAME CONSTRAINT con1 TO con1foo
;
CREATE FUNCTION levenshtein (text,text) RETURNS int
AS '$libdir/fuzzystrmatch','levenshtein'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
drop table revalidate_bug
;
ALTER INDEX tbspace_reindex_part_index ATTACH PARTITION tbspace_reindex_part_index_0
;
ALTER DATABASE "contrib_regression" SET lc_numeric TO 'C'
;
DROP SCHEMA alt_nsp1 CASCADE
;
-- SQL/MED views; these use section numbers from part 9 of the standard.

/* Base view for foreign table columns */
CREATE VIEW _pg_foreign_table_columns AS
    SELECT n.nspname,
           c.relname,
           a.attname,
           a.attfdwoptions
    FROM pg_foreign_table t, pg_authid u, pg_namespace n, pg_class c,
         pg_attribute a
    WHERE u.oid = c.relowner
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_column_privilege(c.oid, a.attnum, 'SELECT, INSERT, UPDATE, REFERENCES'))
          AND n.oid = c.relnamespace
          AND c.oid = t.ftrelid
          AND c.relkind = 'f'
          AND a.attrelid = c.oid
          AND a.attnum > 0
;
ALTER INDEX dummy_test_idx SET (option_real = 3.2)
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', header 'true')
;
VACUUM (PARALLEL 2) pg_catalog.pg_aggregate
;
-- When (sub)partitions are created, they also contain the constraint
create table idxpart (a int, b int, primary key (a, b)) partition by range (a, b)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- Test for renaming in diamond inheritance
CREATE TABLE inht2 (x int) INHERITS (inht1)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW tststats.v AS SELECT * FROM tststats.t
;
----------
[ALTER_TABLE|ADD_COLUMN] --table rewrite won't validate invalid constraint
ALTER TABLE notnull_tbl1 ADD column d float8 default random()
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "email" WITH "simple"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_TABLE] create temp table btree_bpchar (f1 text collate "C")
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p3" FOR VALUES IN ('0008', '0009', '0010', '0011')
;
----------
[ADD_INDEX] CREATE INDEX val_brin ON brin_hot using brin(val)
;
----------
[TRUNCATE_TABLE] TRUNCATE fk_partitioned_fk_2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_inh_check ALTER COLUMN a TYPE numeric
;
----------
[CREATE_TABLE] create foreign table test_foreign_table () server dummy_server
;
----------
[CREATE_TABLE] create table mutable2(f0 text, f1 int, f2 text)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ok
ALTER TABLE itest4 ALTER COLUMN a DROP NOT NULL
;
----------
[CREATE_TABLE] create temp table t1 (a integer primary key)
;
----------
[CREATE_TABLE] -- Some casting expression are simplified to Const
CREATE TABLE test_squash_jsonb (id int, data jsonb)
;
----------
[CREATE_PROG_OBJ] -- commands that should fail

CREATE AGGREGATE priv_testagg1a(priv_testdomain1) (sfunc = int4_sum, stype = bigint)
;
----------
[ADMIN_TABLE] ANALYZE public.default_tbl
;
----------
[CREATE_TABLE] CREATE TABLE will_get_dropped_in_ts (a int, b text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_4_1" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
----------
[DROP_INDEX] drop index ec1_expr3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION oid_dist(oid, oid)
RETURNS oid
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_SEQUENCE] -- invalid relkinds for statistics
CREATE SEQUENCE stats_import.testseq
;
----------
[DROP_LIBRARY] DROP EXTENSION plperl CASCADE
;
----------
[ADMIN_TABLE] ANALYZE public.depth2
;
----------
[CREATE_TABLE] create temp table foo(f1 int, f2 int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  pg_backup_start(label text, fast boolean DEFAULT false)
  RETURNS pg_lsn STRICT VOLATILE LANGUAGE internal AS 'pg_backup_start'
  PARALLEL RESTRICTED
;
----------
[DROP_PROG_OBJ] drop operator class at_test_sql_partop using btree
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^? (
        LEFTARG = _lquery,
	RIGHTARG = ltree,
	PROCEDURE = lt_q_rregex,
	COMMUTATOR = '^?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] -- Bug #17607: variant of above in which trigger function raises an error;
-- we don't see any ill effects unless trigger tuple requires mapping

create table convslot_test_parent (id int primary key, val int)
partition by range (id)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION isn
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_ge(seg, seg) IS
'greater than or equal'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=26577 host=/tmp/Lnf6IHniY8 dbname=postgres' PUBLICATION pub_viaroot
;
----------
[CREATE_TABLE] CREATE TABLE collate_test_fail (
    a int,
    b text COLLATE "foo"
)
;
----------
[ADD_INDEX] CREATE INDEX onek2_stu1_prtl ON onek2 USING btree(stringu1 name_ops)
	where onek2.stringu1 >= 'J' and onek2.stringu1 < 'K'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = true, two_phase = true)
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int; _c int[];
BEGIN
  _a := 10;
  CALL test_proc12(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test4 ALTER COLUMN f1 SET STORAGE EXTERNAL
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1 add constraint p2chk check (ff1 > 10)
;
----------
[ALTER_PROG_OBJ] --
-- Reset and set params
--

ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = NONE)
;
----------
[RENAME_CONSTRAINT] ALTER TABLE constraint_rename_test RENAME CONSTRAINT con1 TO con1foo
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION levenshtein (text,text) RETURNS int
AS '$libdir/fuzzystrmatch','levenshtein'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] drop table revalidate_bug
;
----------
[ALTER_INDEX] ALTER INDEX tbspace_reindex_part_index ATTACH PARTITION tbspace_reindex_part_index_0
;
----------
[ALTER_CATALOG] ALTER DATABASE "contrib_regression" SET lc_numeric TO 'C'
;
----------
[DROP_SCHEMA] DROP SCHEMA alt_nsp1 CASCADE
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- SQL/MED views; these use section numbers from part 9 of the standard.

/* Base view for foreign table columns */
CREATE VIEW _pg_foreign_table_columns AS
    SELECT n.nspname,
           c.relname,
           a.attname,
           a.attfdwoptions
    FROM pg_foreign_table t, pg_authid u, pg_namespace n, pg_class c,
         pg_attribute a
    WHERE u.oid = c.relowner
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_column_privilege(c.oid, a.attnum, 'SELECT, INSERT, UPDATE, REFERENCES'))
          AND n.oid = c.relnamespace
          AND c.oid = t.ftrelid
          AND c.relkind = 'f'
          AND a.attrelid = c.oid
          AND a.attnum > 0
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_real = 3.2)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', header 'true')
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_aggregate
;
----------
[CREATE_TABLE] -- When (sub)partitions are created, they also contain the constraint
create table idxpart (a int, b int, primary key (a, b)) partition by range (a, b)
;
