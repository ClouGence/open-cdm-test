insert into Test ( name , amount , letter ) values ( 'false' , 1 , 'f' )
;
insert into hash_tbl (id, p)
			  select g, 50 from generate_series(41, 50) g
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11375', relminmxid = '1'
WHERE oid = '"pubme2"."t0"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10721', relminmxid = '1'
WHERE oid = '"testxmlschema"."test3"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('1', 481)
;
delete from rtest_nothn2
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."gtestxx_1"'::pg_catalog.regclass
;
INSERT INTO document VALUES (78, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'some technology novel')
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle, cid = 33 RETURNING *
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20079'
;
-- these should succeed because they don't match the index predicate
INSERT INTO circles VALUES('<(0,0), 5>', '<(0,0), 5>')
;
UPDATE tab1 SET "B" = 2 * "B" where a = 1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '589', relminmxid = '1'
WHERE oid = '"public"."money_data"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_l_p3_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO caster (timestamp)     VALUES ('1999-01-08 04:05:06'::citext)
;
-- flush the fastupdate buffers

insert into gin_test_tbl select array[3, 1, g] from generate_series(1, 1000) g
;
UPDATE pgbench_branches SET bbalance = bbalance + 230 WHERE bid = 1
;
insert into test values('ENGLISH FRAN��AIS ESPA��OL ��SLENSKA')
;
INSERT INTO num_input_test(n1) VALUES ('555.50')
;
INSERT INTO pg_temp_3.pg_temp_27606_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_3.pg_temp_27606 AS newdata FROM public.sro_index_mv mv FULL JOIN pg_temp_3.pg_temp_27606 newdata ON (newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
UPDATE pgbench_accounts SET abalance = abalance + 2400 WHERE aid = 89957
;
UPDATE footrg SET b = b || ' update2' WHERE a = 1
;
INSERT INTO upsert_test VALUES(1, 'Boo'), (2, 'Zoo')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10693', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_4_1"'::pg_catalog.regclass
;
INSERT INTO mcv_lists_partial (a, b, c)
     SELECT
         i,
         i,
         i
     FROM generate_series(0,3999) s(i)
;
INSERT INTO ftable SELECT * FROM generate_series(11, 31) i
;
INSERT INTO t1 (SELECT x, md5(x::text) FROM generate_series(1,100) x)
;
INSERT INTO POINT_TBL(f1) VALUES ('(10.0,10.0')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8460', relminmxid = '1'
WHERE oid = '"public"."rtest_nothn3"'::pg_catalog.regclass
;
INSERT INTO num_input_test(n1) VALUES ('')
;
INSERT INTO d_star (class, c, d)
   VALUES ('d', 'hi ginger'::name, '100000.000001'::float8)
;
DELETE FROM fk_partitioned_fk
;
-- uncorrelated sub-select:
WITH aaa AS (SELECT 1 AS a, 'Foo' AS b) INSERT INTO upsert_test
  VALUES (1, 'Bar') ON CONFLICT(a)
  DO UPDATE SET (b, a) = (SELECT b, a FROM aaa) RETURNING *
;
-- fails (due to UPDATE)
INSERT INTO atest5(three) VALUES (4) ON CONFLICT (two) DO UPDATE set three = 10
;
-- Delete most entries, and vacuum, deleting internal pages and creating "fast
-- root"
DELETE FROM delete_test_table WHERE a < 79990
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15866', relminmxid = '3'
WHERE oid = '"public"."pht2_p2"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"fkpart3"."pk2"'::pg_catalog.regclass
  AND attname = v.dname
;
INSERT INTO public.ths_p1 VALUES (2, 872)
;
-- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '722', datminmxid = '1'
WHERE datname = 'regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~'
;
INSERT INTO tab_full VALUES (14)
;
INSERT INTO rw_view1 VALUES(3,4)
;
INSERT INTO public.tplain VALUES ('9', 849)
;
INSERT INTO public.tht VALUES ('5', 755)
;
DELETE FROM tab1
;
INSERT INTO FLOAT4_TBL(f1) VALUES ('-10e70')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10979', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest2"'::pg_catalog.regclass
;
DELETE FROM clstr_2
;
INSERT INTO atest5(two) VALUES (6) ON CONFLICT (two) DO UPDATE set three = 10 RETURNING atest5.three
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11989', relminmxid = '3'
WHERE oid = '31180'
;
INSERT INTO public.tht VALUES ('0', 560)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13443', relminmxid = '3'
WHERE oid = '34166'
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] insert into Test ( name , amount , letter ) values ( 'false' , 1 , 'f' )
;
----------
[INSERT|SELECT] insert into hash_tbl (id, p)
			  select g, 50 from generate_series(41, 50) g
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11375', relminmxid = '1'
WHERE oid = '"pubme2"."t0"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10721', relminmxid = '1'
WHERE oid = '"testxmlschema"."test3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 481)
;
----------
[DELETE] delete from rtest_nothn2
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."gtestxx_1"'::pg_catalog.regclass
;
----------
[MERGE|SELECT] INSERT INTO document VALUES (78, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'some technology novel')
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle, cid = 33 RETURNING *
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20079'
;
----------
[INSERT] -- these should succeed because they don't match the index predicate
INSERT INTO circles VALUES('<(0,0), 5>', '<(0,0), 5>')
;
----------
[UPDATE] UPDATE tab1 SET "B" = 2 * "B" where a = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '589', relminmxid = '1'
WHERE oid = '"public"."money_data"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_l_p3_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT|SELECT] INSERT INTO caster (timestamp)     VALUES ('1999-01-08 04:05:06'::citext)
;
----------
[INSERT|SELECT] -- flush the fastupdate buffers

insert into gin_test_tbl select array[3, 1, g] from generate_series(1, 1000) g
;
----------
[UPDATE] UPDATE pgbench_branches SET bbalance = bbalance + 230 WHERE bid = 1
;
----------
[INSERT] insert into test values('ENGLISH FRAN��AIS ESPA��OL ��SLENSKA')
;
----------
[INSERT|SELECT] INSERT INTO num_input_test(n1) VALUES ('555.50')
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_3.pg_temp_27606_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_3.pg_temp_27606 AS newdata FROM public.sro_index_mv mv FULL JOIN pg_temp_3.pg_temp_27606 newdata ON (newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE] UPDATE pgbench_accounts SET abalance = abalance + 2400 WHERE aid = 89957
;
----------
[UPDATE] UPDATE footrg SET b = b || ' update2' WHERE a = 1
;
----------
[INSERT] INSERT INTO upsert_test VALUES(1, 'Boo'), (2, 'Zoo')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10693', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_4_1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO mcv_lists_partial (a, b, c)
     SELECT
         i,
         i,
         i
     FROM generate_series(0,3999) s(i)
;
----------
[INSERT|SELECT] INSERT INTO ftable SELECT * FROM generate_series(11, 31) i
;
----------
[INSERT|SELECT] INSERT INTO t1 (SELECT x, md5(x::text) FROM generate_series(1,100) x)
;
----------
[INSERT|SELECT] INSERT INTO POINT_TBL(f1) VALUES ('(10.0,10.0')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8460', relminmxid = '1'
WHERE oid = '"public"."rtest_nothn3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO num_input_test(n1) VALUES ('')
;
----------
[INSERT|SELECT] INSERT INTO d_star (class, c, d)
   VALUES ('d', 'hi ginger'::name, '100000.000001'::float8)
;
----------
[DELETE] DELETE FROM fk_partitioned_fk
;
----------
[MERGE|SELECT] -- uncorrelated sub-select:
WITH aaa AS (SELECT 1 AS a, 'Foo' AS b) INSERT INTO upsert_test
  VALUES (1, 'Bar') ON CONFLICT(a)
  DO UPDATE SET (b, a) = (SELECT b, a FROM aaa) RETURNING *
;
----------
[MERGE|SELECT] -- fails (due to UPDATE)
INSERT INTO atest5(three) VALUES (4) ON CONFLICT (two) DO UPDATE set three = 10
;
----------
[DELETE] -- Delete most entries, and vacuum, deleting internal pages and creating "fast
-- root"
DELETE FROM delete_test_table WHERE a < 79990
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15866', relminmxid = '3'
WHERE oid = '"public"."pht2_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"fkpart3"."pk2"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 872)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '722', datminmxid = '1'
WHERE datname = 'regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~'
;
----------
[INSERT] INSERT INTO tab_full VALUES (14)
;
----------
[INSERT] INSERT INTO rw_view1 VALUES(3,4)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 849)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 755)
;
----------
[DELETE] DELETE FROM tab1
;
----------
[INSERT|SELECT] INSERT INTO FLOAT4_TBL(f1) VALUES ('-10e70')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '10979', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest2"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM clstr_2
;
----------
[MERGE|SELECT] INSERT INTO atest5(two) VALUES (6) ON CONFLICT (two) DO UPDATE set three = 10 RETURNING atest5.three
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11989', relminmxid = '3'
WHERE oid = '31180'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 560)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13443', relminmxid = '3'
WHERE oid = '34166'
;
