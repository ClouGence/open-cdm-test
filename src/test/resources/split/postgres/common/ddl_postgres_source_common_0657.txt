CREATE RULE "rtest_emp_ins" AS
    ON INSERT TO "public"."rtest_emp" DO  INSERT INTO "public"."rtest_emplog" ("ename", "who", "action", "newsal", "oldsal")
  VALUES ("new"."ename", CURRENT_USER, 'hired'::"bpchar", "new"."salary", 0.00)
;
CREATE TABLE tab_rowfilter_greater_10k (LIKE tab_rowfilter_partitioned)
;
/*
 * 1.2. test CREATE INDEX with inserts
 */
-- Regular index with included columns
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
create temp table p_t1_2 partition of p_t1 for values in(2)
;
ALTER TEXT SEARCH CONFIGURATION lithuanian ADD MAPPING
    FOR word, hword_part, hword
	WITH lithuanian_stem
;
DO $$
DECLARE
	relname text := reltoastrelid::regclass FROM pg_class WHERE oid = 'tststats.t'::regclass;
BEGIN
	EXECUTE 'CREATE STATISTICS tststats.s10 ON a, b FROM ' || relname;
EXCEPTION WHEN wrong_object_type THEN
	RAISE NOTICE 'stats on toast table not created';
END;
$$
;
COMMENT ON INDEX ctlt1_pkey IS 'index pkey'
;
ALTER TABLE ONLY "public"."notnull_tbl4_cld3"
    ADD CONSTRAINT "notnull_tbl4_cld3_pkey" PRIMARY KEY ("a") DEFERRABLE
;
AlTER EXTENSION pg_stat_statements UPDATE TO '1.8'
;
CREATE TYPE widget (
   internallength = 24,
   input = widget_in,
   output = widget_out,
   typmod_in = numerictypmodin,
   typmod_out = numerictypmodout,
   alignment = double
)
;
CREATE FUNCTION dummy_seclabel_four() RETURNS integer AS $$SELECT 4$$ language sql
;
DROP SCHEMA split_event_helper CASCADE
;
CREATE TABLE people (firstname text, lastname text)
;
create type intr as range(subtype=int)
;
CREATE FUNCTION gbt_int2_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- failed by FK reference
TRUNCATE tru_pk_ftable CASCADE
;
ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE text
;
CREATE RULE shoelace_del AS ON DELETE TO shoelace
        DO INSTEAD
        DELETE FROM shoelace_data
         WHERE sl_name = OLD.sl_name
;
-- Add a trigger to catch and print the contents of the catalog view
-- pg_stat_progress_copy during data insertion.  This allows to test
-- the validation of some progress reports for COPY FROM where the trigger
-- would fire.
create function notice_after_tab_progress_reporting() returns trigger AS
$$
declare report record;
begin
  -- The fields ignored here are the ones that may not remain
  -- consistent across multiple runs.  The sizes reported may differ
  -- across platforms, so just check if these are strictly positive.
  with progress_data as (
    select
       relid::regclass::text as relname,
       command,
       type,
       bytes_processed > 0 as has_bytes_processed,
       bytes_total > 0 as has_bytes_total,
       tuples_processed,
       tuples_excluded,
       tuples_skipped
      from pg_stat_progress_copy
      where pid = pg_backend_pid())
  select into report (to_jsonb(r)) as value
    from progress_data r;

  raise info 'progress: %', report.value::text;
  return new;
end;
$$ language plpgsql
;
ALTER TABLE testpub_tbl5 DROP COLUMN c
;
alter table atacc1 add constraint atacc_test1 unique (test)
;
CREATE SUBSCRIPTION sub1
						 CONNECTION 'dbname=doesnotexist' PUBLICATION pub1
						 WITH (connect = false)
;
DROP VIEW attmp_view_new
;
CREATE TABLE "public"."hobbies_r" (
    "name" "text",
    "person" "text"
)
;
-- error
DROP FUNCTION base_fn_out(opaque)
;
DROP POLICY p2 ON document
;
-- conflict
alter table stud_emp rename to student
;
CREATE UNIQUE INDEX num_exp_log10_idx ON num_exp_log10 (id)
;
DROP DOMAIN seclabel_domain
;
create function dfunc(a varchar, b numeric, c date = current_date)
  returns table (a varchar, b numeric, c date) as $$
  select $1, $2, $3;
$$ language sql
;
ALTER OPERATOR ? (ltree, _lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
create function bark(text) returns bool language plpgsql immutable
  as $$ begin raise notice '% <- woof!', $1; return true; end; $$
;
-- ok
CREATE OR REPLACE TRANSFORM FOR hstore LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal))
;
/*
 * 6.9
 * ADMINISTRABLE_ROLE_AUTHORIZATIONS view
 */

CREATE VIEW administrable_role_authorizations AS
    SELECT *
    FROM applicable_roles
    WHERE is_grantable = 'YES'
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '13633'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_config_map
;
CREATE TABLE cluster_tab (a int)
;
CREATE TABLE "public"."rtest_empmass" (
    "ename" character(20),
    "salary" numeric
)
;
ANALYZE pg_temp_3.pg_temp_16649
;
create operator < (
    procedure = int8alias1lt,
    leftarg = int8alias1, rightarg = int8alias1
)
;
CREATE FUNCTION tab1_trigger_func() RETURNS trigger
LANGUAGE plpgsql AS $$
BEGIN
  NEW.c := NEW.a + 10;
  RETURN NEW;
END $$
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_class
;
ALTER TEXT SEARCH CONFIGURATION indonesian ADD MAPPING
    FOR word, hword_part, hword
	WITH indonesian_stem
;
ALTER TABLE pg_temp_4.pg_temp_26914_2 ADD COLUMN newdata pg_temp_4.pg_temp_26914
;
-- operator class
DROP OPERATOR CLASS test_operator_class USING btree
;
CREATE TABLE "
t
t
"(a int)
;
create table coll_pruning_multi1 partition of coll_pruning_multi for values from ('a', 'a') to ('a', 'e')
;
create table listp_1_1 partition of listp_1 for values in(1)
;
DROP TABLE pub_test.testpub_addpk
;
CREATE FUNCTION invert(x float8) RETURNS float8 LANGUAGE plpgsql AS
$$ begin return 1/x; end $$
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] CREATE RULE "rtest_emp_ins" AS
    ON INSERT TO "public"."rtest_emp" DO  INSERT INTO "public"."rtest_emplog" ("ename", "who", "action", "newsal", "oldsal")
  VALUES ("new"."ename", CURRENT_USER, 'hired'::"bpchar", "new"."salary", 0.00)
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_greater_10k (LIKE tab_rowfilter_partitioned)
;
----------
[CREATE_TABLE] /*
 * 1.2. test CREATE INDEX with inserts
 */
-- Regular index with included columns
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_TABLE] create temp table p_t1_2 partition of p_t1 for values in(2)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION lithuanian ADD MAPPING
    FOR word, hword_part, hword
	WITH lithuanian_stem
;
----------
[BLOCK] DO $$
DECLARE
	relname text := reltoastrelid::regclass FROM pg_class WHERE oid = 'tststats.t'::regclass;
BEGIN
	EXECUTE 'CREATE STATISTICS tststats.s10 ON a, b FROM ' || relname;
EXCEPTION WHEN wrong_object_type THEN
	RAISE NOTICE 'stats on toast table not created';
END;
$$
;
----------
[COMMENT_INDEX] COMMENT ON INDEX ctlt1_pkey IS 'index pkey'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."notnull_tbl4_cld3"
    ADD CONSTRAINT "notnull_tbl4_cld3_pkey" PRIMARY KEY ("a") DEFERRABLE
;
----------
[ALTER_LIBRARY] AlTER EXTENSION pg_stat_statements UPDATE TO '1.8'
;
----------
[CREATE_TYPE] CREATE TYPE widget (
   internallength = 24,
   input = widget_in,
   output = widget_out,
   typmod_in = numerictypmodin,
   typmod_out = numerictypmodout,
   alignment = double
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dummy_seclabel_four() RETURNS integer AS $$SELECT 4$$ language sql
;
----------
[DROP_SCHEMA] DROP SCHEMA split_event_helper CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE people (firstname text, lastname text)
;
----------
[CREATE_TYPE] create type intr as range(subtype=int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int2_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[TRUNCATE_TABLE] -- failed by FK reference
TRUNCATE tru_pk_ftable CASCADE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE text
;
----------
[CREATE_POLICY] CREATE RULE shoelace_del AS ON DELETE TO shoelace
        DO INSTEAD
        DELETE FROM shoelace_data
         WHERE sl_name = OLD.sl_name
;
----------
[CREATE_PROG_OBJ] -- Add a trigger to catch and print the contents of the catalog view
-- pg_stat_progress_copy during data insertion.  This allows to test
-- the validation of some progress reports for COPY FROM where the trigger
-- would fire.
create function notice_after_tab_progress_reporting() returns trigger AS
$$
declare report record;
begin
  -- The fields ignored here are the ones that may not remain
  -- consistent across multiple runs.  The sizes reported may differ
  -- across platforms, so just check if these are strictly positive.
  with progress_data as (
    select
       relid::regclass::text as relname,
       command,
       type,
       bytes_processed > 0 as has_bytes_processed,
       bytes_total > 0 as has_bytes_total,
       tuples_processed,
       tuples_excluded,
       tuples_skipped
      from pg_stat_progress_copy
      where pid = pg_backend_pid())
  select into report (to_jsonb(r)) as value
    from progress_data r;

  raise info 'progress: %', report.value::text;
  return new;
end;
$$ language plpgsql
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE testpub_tbl5 DROP COLUMN c
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_test1 unique (test)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1
						 CONNECTION 'dbname=doesnotexist' PUBLICATION pub1
						 WITH (connect = false)
;
----------
[DROP_VIEW] DROP VIEW attmp_view_new
;
----------
[CREATE_TABLE] CREATE TABLE "public"."hobbies_r" (
    "name" "text",
    "person" "text"
)
;
----------
[DROP_PROG_OBJ] -- error
DROP FUNCTION base_fn_out(opaque)
;
----------
[DROP_POLICY] DROP POLICY p2 ON document
;
----------
[RENAME_TABLE] -- conflict
alter table stud_emp rename to student
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX num_exp_log10_idx ON num_exp_log10 (id)
;
----------
[DROP_TYPE] DROP DOMAIN seclabel_domain
;
----------
[CREATE_PROG_OBJ] create function dfunc(a varchar, b numeric, c date = current_date)
  returns table (a varchar, b numeric, c date) as $$
  select $1, $2, $3;
$$ language sql
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ? (ltree, _lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_PROG_OBJ] create function bark(text) returns bool language plpgsql immutable
  as $$ begin raise notice '% <- woof!', $1; return true; end; $$
;
----------
[CREATE_TRANSFORM] -- ok
CREATE OR REPLACE TRANSFORM FOR hstore LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal))
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.9
 * ADMINISTRABLE_ROLE_AUTHORIZATIONS view
 */

CREATE VIEW administrable_role_authorizations AS
    SELECT *
    FROM applicable_roles
    WHERE is_grantable = 'YES'
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '13633'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_config_map
;
----------
[CREATE_TABLE] CREATE TABLE cluster_tab (a int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_empmass" (
    "ename" character(20),
    "salary" numeric
)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_16649
;
----------
[CREATE_PROG_OBJ] create operator < (
    procedure = int8alias1lt,
    leftarg = int8alias1, rightarg = int8alias1
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tab1_trigger_func() RETURNS trigger
LANGUAGE plpgsql AS $$
BEGIN
  NEW.c := NEW.a + 10;
  RETURN NEW;
END $$
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_class
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION indonesian ADD MAPPING
    FOR word, hword_part, hword
	WITH indonesian_stem
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_26914_2 ADD COLUMN newdata pg_temp_4.pg_temp_26914
;
----------
[DROP_PROG_OBJ] -- operator class
DROP OPERATOR CLASS test_operator_class USING btree
;
----------
[CREATE_TABLE] CREATE TABLE "
t
t
"(a int)
;
----------
[CREATE_TABLE] create table coll_pruning_multi1 partition of coll_pruning_multi for values from ('a', 'a') to ('a', 'e')
;
----------
[CREATE_TABLE] create table listp_1_1 partition of listp_1 for values in(1)
;
----------
[DROP_TABLE] DROP TABLE pub_test.testpub_addpk
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION invert(x float8) RETURNS float8 LANGUAGE plpgsql AS
$$ begin return 1/x; end $$
;
