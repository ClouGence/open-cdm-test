ALTER INDEX pg_toast.pg_toast_28017_index RENAME TO reind_con_toast_idx
;
DROP TABLE pg_temp_3.pg_temp_16651_2, pg_temp_3.pg_temp_16651
;
CREATE TABLE tbl4 (g int, h int)
;
CREATE TABLE public.tht_p3 (
    en public.digit,
    data integer
)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
-- ALTER column SET NOT NULL validates an invalid constraint (but this fails
-- because of rows with null values)
ALTER TABLE notnull_tbl1 ALTER a SET NOT NULL
;
CREATE OR REPLACE VIEW pg_sequences AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS sequencename,
        pg_get_userbyid(C.relowner) AS sequenceowner,
        S.seqtypid::regtype AS data_type,
        S.seqstart AS start_value,
        S.seqmin AS min_value,
        S.seqmax AS max_value,
        S.seqincrement AS increment_by,
        S.seqcycle AS cycle,
        S.seqcache AS cache_size,
        CASE
            WHEN has_sequence_privilege(C.oid, 'SELECT,USAGE'::text)
                THEN pg_sequence_last_value(C.oid)
            ELSE NULL
        END AS last_value
    FROM pg_sequence S JOIN pg_class C ON (C.oid = S.seqrelid)
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE NOT pg_is_other_temp_schema(N.oid)
          AND relkind = 'S'
;
create trigger but_trigger after update on convslot_test_child
referencing new table as new_table
for each statement execute function convslot_trig2()
;
CREATE TABLE prt2_adv_p2 PARTITION OF prt2_adv FOR VALUES FROM (200) TO (300)
;
create view id_ordered as select * from id order by id
;
-- bpchar datatype support new in 1.3.
CREATE FUNCTION gin_extract_value_bpchar(bpchar, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
drop function trap_foreign_key(int)
;
create table idxpart1 (a int not null, b int) partition by range (a)
;
create index idxpart_idx on idxpart (a)
;
VACUUM (INDEX_CLEANUP FALSE) information_schema.sql_sizing
;
/*
 * text search configuration for basque language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * basque and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY basque_stem
	(TEMPLATE = snowball, Language = basque )
;
DROP TABLE t1, t11, t12, t111 CASCADE
;
ANALYZE public.tableam_tblas_heap2
;
CREATE VIEW "public"."rtest_vview4" AS
 SELECT "x"."a",
    "x"."b",
    "count"("y"."a") AS "refcount"
   FROM "public"."rtest_view1" "x",
    "public"."rtest_view2" "y"
  WHERE ("x"."a" = "y"."a")
  GROUP BY "x"."a", "x"."b"
;
create type _comptype as enum('fooey')
;
CREATE TABLE pk32 (b int, a int NOT NULL)
;
create function tg_chkbacklink() returns trigger as '
begin
    if new.backlink isnull then
        new.backlink := '''';
    end if;
    return new;
end;
' language plpgsql
;
CREATE TABLE "public"."spgist_point_tbl" (
    "id" integer,
    "p" "point"
)
;
CREATE TABLE "public"."tbl_include_reg" (
    "c1" integer,
    "c2" integer,
    "c3" integer,
    "c4" "box"
)
;
create table iboolpart_null partition of iboolpart for values in (null)
;
create or replace function raise_test() returns void as $$
begin
  raise;
end;
$$ language plpgsql
;
CREATE FUNCTION gbt_oid_same(gbtreekey8, gbtreekey8, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create index on xid8_t1 using hash(x)
;
CREATE TABLE itest11 (a int generated always as identity, b text)
;
CREATE OPERATOR split_operator_probe.=== (
    FUNCTION = split_operator_probe.int_same,
    LEFTARG = integer,
    RIGHTARG = integer,
    COMMUTATOR = OPERATOR(split_operator_probe.===),
    RESTRICT = eqsel,
    JOIN = eqjoinsel,
    HASHES,
    MERGES
)
;
CREATE TABLE "public"."atnotnull1" (
    "a" integer NOT NULL,
    "c" integer NOT NULL
)
;
DROP SEQUENCE sequence_test_unlogged
;
CREATE TABLE psql_pipeline()
;
create table idxpart11 (a int not null, b int primary key)
;
ALTER TABLE notnull_tbl1 DROP CONSTRAINT notnull_tbl1_b_not_null
;
-- bpchar
CREATE TABLE test1bpci (x char(3) COLLATE case_insensitive)
;
-- try again to exercise internal caching

create domain orderedarray as int[2]
  constraint sorted check (value[1] < value[2])
;
alter type comptype alter attribute r type bigint
;
CREATE INDEX idx_no_replidentity_index ON tab_no_replidentity_index(c1)
;
CREATE INDEX t2_gin ON s2.t2 USING GIN (ia)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_replication_origin
;
ANALYZE public.renamecolumnanother
;
CREATE FUNCTION decrypt(bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_decrypt'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=16262 host=/tmp/Z2NRyt87xn dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
-- this perhaps should be rejected, but it isn't:
alter table tt14t drop column f3
;
CREATE TABLE tbl (c1 int, c2 int, c3 int, c4 box)
;
/* contrib/hstore/hstore--1.6--1.7.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION ghstore_options(internal)
RETURNS void
AS '$libdir/hstore', 'ghstore_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
create table mc3p5 partition of mc3p for values from (11, 1, 1) to (20, 10, 10)
;
DROP TABLE base_tbl_parent, base_tbl_child CASCADE
;
CREATE INDEX test_index ON test_partition (a)
;
------------------------------------------------------------------------------------------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28017_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_16651_2, pg_temp_3.pg_temp_16651
;
----------
[CREATE_TABLE] CREATE TABLE tbl4 (g int, h int)
;
----------
[CREATE_TABLE] CREATE TABLE public.tht_p3 (
    en public.digit,
    data integer
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ALTER column SET NOT NULL validates an invalid constraint (but this fails
-- because of rows with null values)
ALTER TABLE notnull_tbl1 ALTER a SET NOT NULL
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE OR REPLACE VIEW pg_sequences AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS sequencename,
        pg_get_userbyid(C.relowner) AS sequenceowner,
        S.seqtypid::regtype AS data_type,
        S.seqstart AS start_value,
        S.seqmin AS min_value,
        S.seqmax AS max_value,
        S.seqincrement AS increment_by,
        S.seqcycle AS cycle,
        S.seqcache AS cache_size,
        CASE
            WHEN has_sequence_privilege(C.oid, 'SELECT,USAGE'::text)
                THEN pg_sequence_last_value(C.oid)
            ELSE NULL
        END AS last_value
    FROM pg_sequence S JOIN pg_class C ON (C.oid = S.seqrelid)
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE NOT pg_is_other_temp_schema(N.oid)
          AND relkind = 'S'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger but_trigger after update on convslot_test_child
referencing new table as new_table
for each statement execute function convslot_trig2()
;
----------
[CREATE_TABLE] CREATE TABLE prt2_adv_p2 PARTITION OF prt2_adv FOR VALUES FROM (200) TO (300)
;
----------
[CREATE_VIEW(SELECT)] create view id_ordered as select * from id order by id
;
----------
[CREATE_PROG_OBJ] -- bpchar datatype support new in 1.3.
CREATE FUNCTION gin_extract_value_bpchar(bpchar, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[DROP_PROG_OBJ] drop function trap_foreign_key(int)
;
----------
[CREATE_TABLE] create table idxpart1 (a int not null, b int) partition by range (a)
;
----------
[ADD_INDEX] create index idxpart_idx on idxpart (a)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) information_schema.sql_sizing
;
----------
[CREATE_POLICY] /*
 * text search configuration for basque language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * basque and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY basque_stem
	(TEMPLATE = snowball, Language = basque )
;
----------
[DROP_TABLE] DROP TABLE t1, t11, t12, t111 CASCADE
;
----------
[ADMIN_TABLE] ANALYZE public.tableam_tblas_heap2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_vview4" AS
 SELECT "x"."a",
    "x"."b",
    "count"("y"."a") AS "refcount"
   FROM "public"."rtest_view1" "x",
    "public"."rtest_view2" "y"
  WHERE ("x"."a" = "y"."a")
  GROUP BY "x"."a", "x"."b"
;
----------
[CREATE_TYPE] create type _comptype as enum('fooey')
;
----------
[CREATE_TABLE] CREATE TABLE pk32 (b int, a int NOT NULL)
;
----------
[CREATE_PROG_OBJ] create function tg_chkbacklink() returns trigger as '
begin
    if new.backlink isnull then
        new.backlink := '''';
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."spgist_point_tbl" (
    "id" integer,
    "p" "point"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tbl_include_reg" (
    "c1" integer,
    "c2" integer,
    "c3" integer,
    "c4" "box"
)
;
----------
[CREATE_TABLE] create table iboolpart_null partition of iboolpart for values in (null)
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_oid_same(gbtreekey8, gbtreekey8, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADD_INDEX] create index on xid8_t1 using hash(x)
;
----------
[CREATE_TABLE] CREATE TABLE itest11 (a int generated always as identity, b text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR split_operator_probe.=== (
    FUNCTION = split_operator_probe.int_same,
    LEFTARG = integer,
    RIGHTARG = integer,
    COMMUTATOR = OPERATOR(split_operator_probe.===),
    RESTRICT = eqsel,
    JOIN = eqjoinsel,
    HASHES,
    MERGES
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."atnotnull1" (
    "a" integer NOT NULL,
    "c" integer NOT NULL
)
;
----------
[DROP_SEQUENCE] DROP SEQUENCE sequence_test_unlogged
;
----------
[CREATE_TABLE] CREATE TABLE psql_pipeline()
;
----------
[CREATE_TABLE] create table idxpart11 (a int not null, b int primary key)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE notnull_tbl1 DROP CONSTRAINT notnull_tbl1_b_not_null
;
----------
[CREATE_TABLE] -- bpchar
CREATE TABLE test1bpci (x char(3) COLLATE case_insensitive)
;
----------
[CREATE_TYPE] -- try again to exercise internal caching

create domain orderedarray as int[2]
  constraint sorted check (value[1] < value[2])
;
----------
[ALTER_TYPE] alter type comptype alter attribute r type bigint
;
----------
[ADD_INDEX] CREATE INDEX idx_no_replidentity_index ON tab_no_replidentity_index(c1)
;
----------
[ADD_INDEX] CREATE INDEX t2_gin ON s2.t2 USING GIN (ia)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_replication_origin
;
----------
[ADMIN_TABLE] ANALYZE public.renamecolumnanother
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION decrypt(bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_decrypt'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub1 CONNECTION 'port=16262 host=/tmp/Z2NRyt87xn dbname=regress_db' PUBLICATION regress_pub WITH (connect=false)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- this perhaps should be rejected, but it isn't:
alter table tt14t drop column f3
;
----------
[CREATE_TABLE] CREATE TABLE tbl (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_PROG_OBJ] /* contrib/hstore/hstore--1.6--1.7.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION ghstore_options(internal)
RETURNS void
AS '$libdir/hstore', 'ghstore_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table mc3p5 partition of mc3p for values from (11, 1, 1) to (20, 10, 10)
;
----------
[DROP_TABLE] DROP TABLE base_tbl_parent, base_tbl_child CASCADE
;
----------
[ADD_INDEX] CREATE INDEX test_index ON test_partition (a)
;
