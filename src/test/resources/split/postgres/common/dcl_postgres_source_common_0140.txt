CREATE USER MAPPING FOR regress_view_owner SERVER loopback
;
DEALLOCATE pgsql_fdw_prep_182
;
COPY public.sk (a, b) TO stdout
;
deallocate "normal_name"
;
--
-- synchronous_commit=off delays when hint bits may be set. Some plans change
-- depending on the number of all-visible pages, which in turn can be
-- influenced by the delayed hint bits. Force synchronous_commit=on to avoid
-- that source of variability.
--
SET synchronous_commit = on
;
LOCK TABLE fkpart5.pk11 IN ACCESS SHARE MODE
;
LOCK TABLE "public"."transition_table_level2" IN ACCESS SHARE MODE
;
LOCK TABLE "public"."test_type_diff" IN ACCESS SHARE MODE
;
DEALLOCATE pgsql_fdw_prep_184
;
GRANT UPDATE ON TABLE lock_view8 TO regress_rol_lock1
;
------------------------------------------------------------------------------------------
[GRANT] CREATE USER MAPPING FOR regress_view_owner SERVER loopback
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_182
;
----------
[DATA_EXPORT] COPY public.sk (a, b) TO stdout
;
----------
[UNSAFE] deallocate "normal_name"
;
----------
[SESSION_SETTING_WRITE] --
-- synchronous_commit=off delays when hint bits may be set. Some plans change
-- depending on the number of all-visible pages, which in turn can be
-- influenced by the delayed hint bits. Force synchronous_commit=on to avoid
-- that source of variability.
--
SET synchronous_commit = on
;
----------
[SESSION_LOCK] LOCK TABLE fkpart5.pk11 IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."transition_table_level2" IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."test_type_diff" IN ACCESS SHARE MODE
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_184
;
----------
[GRANT] GRANT UPDATE ON TABLE lock_view8 TO regress_rol_lock1
;
