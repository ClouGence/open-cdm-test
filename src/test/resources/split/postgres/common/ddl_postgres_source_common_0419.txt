ALTER TABLE ONLY "generated_virtual_tests"."gtest28a" DROP COLUMN "........pg.dropped.1........"
;
ANALYZE public.itest7d
;
create domain ddef5 numeric(8,2) NOT NULL DEFAULT '12.12'
;
ALTER TABLE leader DROP c
;
-- two_phase and streaming are compatible.
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = true, two_phase = true)
;
CREATE TABLE itest_err_1 (a text generated by default as identity)
;
DROP TABLE pg_temp_11.pg_temp_41126_2, pg_temp_11.pg_temp_41126
;
create index on t (a)
;
CREATE TABLE persons4 OF person_type (
    name WITH OPTIONS NOT NULL,
    name WITH OPTIONS DEFAULT ''  
)
;
-- nonsense word
CREATE SEQUENCE sequence_testx OWNED BY pg_class_oid_index.oid
;
ALTER TABLE pg_toast.pg_toast_30943 RENAME TO reind_con_toast
;
DROP TRIGGER d15_delete_trig ON part_d_15_20
;
create table boolrangep_ff2 partition of boolrangep for values from ('false', 'false', 50) to ('false', 'false', 100)
;
-- ===================================================================
-- test tuple routing for foreign-table partitions
-- ===================================================================

-- Test insert tuple routing
create table itrtest (a int, b text) partition by list (a)
;
CREATE FUNCTION pg_get_wal_records_info(IN start_lsn pg_lsn,
    IN end_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_records_info'
LANGUAGE C STRICT PARALLEL SAFE
;
-- Test for varlena datatype with expr evaluation
CREATE TABLE expr_key (x numeric, t text)
;
do $$
  declare
    r test2;
  begin
    select * into r from test1;
    delete from test1;
    commit;
    perform pg_advisory_lock(1);
    raise notice 'length(r) = %', length(r::text);
  end;
$$
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c1 SET (n_distinct = 100)
;
create table part_10_20 partition of range_list_parted for values from (10) to (20) partition by list (b)
;
CREATE VIEW v1 WITH (security_barrier = true) AS
  SELECT * FROM t1 WHERE (a > 0)
  WITH CHECK OPTION
;
CREATE INDEX idx_int2 ON test_int2 USING gin (i)
;
CREATE TABLE testschema.tablespace_acl (c int)
;
ALTER TABLE pg_temp_3.pg_temp_27503_2 ADD COLUMN newdata pg_temp_3.pg_temp_27503
;
CREATE TABLE "public"."test_is_json" (
    "js" "text"
)
;
ALTER INDEX attmp_onek_unique1 RENAME TO onek_unique1
;
-- ok
DROP DOMAIN priv_testdomain1
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "asciiword" WITH "public"."synonym", "english_stem"
;
ALTER TABLE tbl2 ALTER COLUMN val2 TYPE character varying
;
VACUUM (ANALYZE) pg_catalog.pg_largeobject_metadata
;
ALTER TABLE part_at2tmp1 RENAME TO fail
;
alter table inh_cc2 no inherit inh_cc1
;
create table test ( id int , c char ( 10 ) , s smallint , i int , b bool , f float , l bigint , dbl double precision , dec decimal , dat date , tmp timestamptz )
;
CREATE FUNCTION brin_metapage_info(IN page bytea, OUT magic text,
	OUT version integer, OUT pagesperrange integer, OUT lastrevmappage bigint)
AS '$libdir/pageinspect', 'brin_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER TABLE part_2 ADD COLUMN c text
;
CREATE FUNCTION "public"."tg_hub_a"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    hname	text;
    dummy	integer;
begin
    if tg_op = 'INSERT' then
	dummy := tg_hub_adjustslots(new.name, 0, new.nslots);
	return new;
    end if;
    if tg_op = 'UPDATE' then
	if new.name != old.name then
	    update HSlot set hubname = new.name where hubname = old.name;
	end if;
	dummy := tg_hub_adjustslots(new.name, old.nslots, new.nslots);
	return new;
    end if;
    if tg_op = 'DELETE' then
	dummy := tg_hub_adjustslots(old.name, old.nslots, 0);
	return old;
    end if;
end;
$$
;
do $$
declare x int := 42;
begin
  declare y int := x + 1;
          x int := x + 2;
          z int := x * 10;
  begin
    raise notice 'x = %, y = %, z = %', x, y, z;
  end;
end;
$$
;
ALTER OPERATOR <<% (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
DROP EXTENSION test_ext6
;
-- make sure that dependencies are reported properly when they block the drop
DROP TABLE mvtest_t
;
/*
 * 5.58
 * SQL_IMPLEMENTATION_INFO table
 */

-- Note: Implementation information items are defined in ISO/IEC 9075-3:2008,
-- clause 9.1.

CREATE TABLE sql_implementation_info (
    implementation_info_id      character_data,
    implementation_info_name    character_data,
    integer_value               cardinal_number,
    character_value             character_data,
    comments                    character_data
)
;
CREATE FUNCTION ks_test_normal_random()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random_normal() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs((1+erf(r/sqrt(2)))/2 - i/n)) < c / sqrt(n)
    FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
-- ok
CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
--
-- test FK referenced deletes NO ACTION
--

TRUNCATE temporal_rng, temporal_fk_rng2rng
;
CREATE INDEX iprt2_p1_b on prt2_p1(b)
;
CREATE TABLE pagg_tab_ml_p2 PARTITION OF pagg_tab_ml FOR VALUES FROM (12) TO (20) PARTITION BY LIST (c)
;
create table foo (f1 c.cube, f2 int)
;
create index on idxpart11 (a)
;
CREATE TABLE unit (
	un_name    char(8),       -- the primary key
	un_fact    float          -- factor to transform to cm
)
;
CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=31799 host=/tmp/Cx8DC1CF0C dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
ALTER INDEX pg_toast.pg_toast_27621_index RENAME TO reind_con_toast_idx
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_virtual_tests"."gtest28a" DROP COLUMN "........pg.dropped.1........"
;
----------
[ADMIN_TABLE] ANALYZE public.itest7d
;
----------
[CREATE_TYPE] create domain ddef5 numeric(8,2) NOT NULL DEFAULT '12.12'
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE leader DROP c
;
----------
[CREATE_PUB_SUB] -- two_phase and streaming are compatible.
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = true, two_phase = true)
;
----------
[CREATE_TABLE] CREATE TABLE itest_err_1 (a text generated by default as identity)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_41126_2, pg_temp_11.pg_temp_41126
;
----------
[ADD_INDEX] create index on t (a)
;
----------
[CREATE_TABLE] CREATE TABLE persons4 OF person_type (
    name WITH OPTIONS NOT NULL,
    name WITH OPTIONS DEFAULT ''  
)
;
----------
[CREATE_SEQUENCE] -- nonsense word
CREATE SEQUENCE sequence_testx OWNED BY pg_class_oid_index.oid
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30943 RENAME TO reind_con_toast
;
----------
[DROP_TRIGGER] DROP TRIGGER d15_delete_trig ON part_d_15_20
;
----------
[CREATE_TABLE] create table boolrangep_ff2 partition of boolrangep for values from ('false', 'false', 50) to ('false', 'false', 100)
;
----------
[CREATE_TABLE] -- ===================================================================
-- test tuple routing for foreign-table partitions
-- ===================================================================

-- Test insert tuple routing
create table itrtest (a int, b text) partition by list (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_get_wal_records_info(IN start_lsn pg_lsn,
    IN end_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_records_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Test for varlena datatype with expr evaluation
CREATE TABLE expr_key (x numeric, t text)
;
----------
[BLOCK] do $$
  declare
    r test2;
  begin
    select * into r from test1;
    delete from test1;
    commit;
    perform pg_advisory_lock(1);
    raise notice 'length(r) = %', length(r::text);
  end;
$$
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c1 SET (n_distinct = 100)
;
----------
[CREATE_TABLE] create table part_10_20 partition of range_list_parted for values from (10) to (20) partition by list (b)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v1 WITH (security_barrier = true) AS
  SELECT * FROM t1 WHERE (a > 0)
  WITH CHECK OPTION
;
----------
[ADD_INDEX] CREATE INDEX idx_int2 ON test_int2 USING gin (i)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.tablespace_acl (c int)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_27503_2 ADD COLUMN newdata pg_temp_3.pg_temp_27503
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_is_json" (
    "js" "text"
)
;
----------
[RENAME_INDEX] ALTER INDEX attmp_onek_unique1 RENAME TO onek_unique1
;
----------
[DROP_TYPE] -- ok
DROP DOMAIN priv_testdomain1
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "asciiword" WITH "public"."synonym", "english_stem"
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl2 ALTER COLUMN val2 TYPE character varying
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_largeobject_metadata
;
----------
[RENAME_TABLE] ALTER TABLE part_at2tmp1 RENAME TO fail
;
----------
[ALTER_TABLE] alter table inh_cc2 no inherit inh_cc1
;
----------
[CREATE_TABLE] create table test ( id int , c char ( 10 ) , s smallint , i int , b bool , f float , l bigint , dbl double precision , dec decimal , dat date , tmp timestamptz )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION brin_metapage_info(IN page bytea, OUT magic text,
	OUT version integer, OUT pagesperrange integer, OUT lastrevmappage bigint)
AS '$libdir/pageinspect', 'brin_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE part_2 ADD COLUMN c text
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_hub_a"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    hname	text;
    dummy	integer;
begin
    if tg_op = 'INSERT' then
	dummy := tg_hub_adjustslots(new.name, 0, new.nslots);
	return new;
    end if;
    if tg_op = 'UPDATE' then
	if new.name != old.name then
	    update HSlot set hubname = new.name where hubname = old.name;
	end if;
	dummy := tg_hub_adjustslots(new.name, old.nslots, new.nslots);
	return new;
    end if;
    if tg_op = 'DELETE' then
	dummy := tg_hub_adjustslots(old.name, old.nslots, 0);
	return old;
    end if;
end;
$$
;
----------
[BLOCK] do $$
declare x int := 42;
begin
  declare y int := x + 1;
          x int := x + 2;
          z int := x * 10;
  begin
    raise notice 'x = %, y = %, z = %', x, y, z;
  end;
end;
$$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <<% (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_LIBRARY] DROP EXTENSION test_ext6
;
----------
[DROP_TABLE] -- make sure that dependencies are reported properly when they block the drop
DROP TABLE mvtest_t
;
----------
[CREATE_TABLE] /*
 * 5.58
 * SQL_IMPLEMENTATION_INFO table
 */

-- Note: Implementation information items are defined in ISO/IEC 9075-3:2008,
-- clause 9.1.

CREATE TABLE sql_implementation_info (
    implementation_info_id      character_data,
    implementation_info_name    character_data,
    integer_value               cardinal_number,
    character_value             character_data,
    comments                    character_data
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ks_test_normal_random()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random_normal() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs((1+erf(r/sqrt(2)))/2 - i/n)) < c / sqrt(n)
    FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
----------
[CREATE_TABLE|SELECT] -- ok
CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
----------
[TRUNCATE_TABLE] --
-- test FK referenced deletes NO ACTION
--

TRUNCATE temporal_rng, temporal_fk_rng2rng
;
----------
[ADD_INDEX] CREATE INDEX iprt2_p1_b on prt2_p1(b)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p2 PARTITION OF pagg_tab_ml FOR VALUES FROM (12) TO (20) PARTITION BY LIST (c)
;
----------
[CREATE_TABLE] create table foo (f1 c.cube, f2 int)
;
----------
[ADD_INDEX] create index on idxpart11 (a)
;
----------
[CREATE_TABLE] CREATE TABLE unit (
	un_name    char(8),       -- the primary key
	un_fact    float          -- factor to transform to cm
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=31799 host=/tmp/Cx8DC1CF0C dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27621_index RENAME TO reind_con_toast_idx
;
