CREATE INDEX moneyidx ON moneytmp USING gist ( a )
;
CREATE VIEW foreign_tables AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_foreign_tables
;
alter table cc2 alter column a2 drop not null
;
ANALYZE pg_temp_11.pg_temp_25449
;
ALTER TABLE ONLY "public"."ec0"
    ADD CONSTRAINT "ec0_pkey" PRIMARY KEY ("ff")
;
CREATE DATABASE dropme_db2 WITH TABLESPACE dropme_ts2 STRATEGY=WAL_LOG
;
CREATE FUNCTION blhandler(internal)
RETURNS index_am_handler
AS '$libdir/bloom'
LANGUAGE C
;
CREATE FUNCTION gbt_oid_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "float" WITH "simple"
;
CREATE TABLE int8tmp (a int8)
;
/* src/test/modules/test_extensions/test_ext_req_schema3--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- This formulation cannot handle relocation of the required extension.
CREATE FUNCTION dep_req3() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT test_s_dep.dep_req1() || ' req3' $$
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (force_null '*')
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26708_2 (tid pg_catalog.tid)
;
create view tt19v as
select 'foo'::text = any(array['abc','def','foo']::text[]) c1,
       'foo'::text = any((select array['abc','def','foo']::text[])::text[]) c2
;
COMMENT ON CONVERSION myconv_bad IS 'foo'
;
--
-- Tests for 8.4's new RAISE features
--

create or replace function raise_test() returns void as $$
begin
  raise notice '% % %', 1, 2, 3
     using errcode = '55001', detail = 'some detail info', hint = 'some hint';
  raise '% % %', 1, 2, 3
     using errcode = 'division_by_zero', detail = 'some detail info';
end;
$$ language plpgsql
;
CREATE TABLE collate_test1 (
    a int,
    b text COLLATE "en_US" NOT NULL
)
;
CREATE TABLE shared_pg_buffercache()
;
drop type nonesuch
;
ALTER TABLE ONLY "public"."partitioned_table" ATTACH PARTITION "public"."pt_part1" FOR VALUES IN (1)
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=21437 host=/tmp/4yqECsv5gm dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
-- Unique index and unique constraint. Both must fail.
CREATE TABLE tbl_include_unique2 (c1 int, c2 int, c3 int, c4 box)
;
DROP INDEX table_dropped_index_with_pk_idx
;
create table PHone (
    slotname	char(20),
    comment	text,
    slotlink	char(20)
)
;
-- idxpart1_a_idx is not valid, so idxpart_a_idx should not become valid:
alter index idxpart_a_idx attach partition idxpart1_a_idx
;
CREATE OPERATOR "public".= (
    FUNCTION = "public"."int8alias1eq",
    LEFTARG = bigint,
    RIGHTARG = "public"."int8alias1",
    MERGES,
    RESTRICT = "eqsel",
    JOIN = "eqjoinsel"
)
;
DROP INDEX i_temptest
;
CREATE TABLE clstrpart11 PARTITION OF clstrpart1 FOR VALUES FROM (1) TO (5)
;
/*
 * text search configuration for russian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * russian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
CREATE TABLE prt2_adv_default PARTITION OF prt2_adv DEFAULT
;
create table tt3 (ax int8, b int2, c numeric)
;
CREATE TABLE regtest_ptable_ones PARTITION OF regtest_ptable FOR VALUES FROM ('0') TO ('10')
;
--
-- Some examples with a tree
--
-- department structure represented here is as follows:
--
-- ROOT-+->A-+->B-+->C
--      |         |
--      |         +->D-+->F
--      +->E-+->G

CREATE TEMP TABLE department (
	id INTEGER PRIMARY KEY,  -- department ID
	parent_department INTEGER REFERENCES department, -- upper department ID
	name TEXT -- department name
)
;
CREATE TABLE expr_stats (a int, b name, c text)
;
create table pktable2 (a float8, b float8, primary key (a, b))
;
ALTER TABLE dump_test.test_table CLUSTER ON test_table_pkey
;
ALTER TABLE part_document_satire ENABLE ROW LEVEL SECURITY
;
CREATE TABLE test22a_0 PARTITION OF test22a FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
CREATE OPERATOR !=- (
   rightarg = int8,
   procedure = factorial
)
;
CREATE RULE voo_i AS ON INSERT TO voo DO INSTEAD
  INSERT INTO foo VALUES(new.*, 57)
;
CREATE TEMP TABLE pg_temp_9.pg_temp_25561_2 (tid pg_catalog.tid)
;
alter table atacc1 add check ("........pg.dropped.1........" > 3)
;
ALTER TABLE ONLY "public"."mlparted3" DROP COLUMN "........pg.dropped.5........"
;
CREATE FUNCTION functest_E_2(int) RETURNS bool LANGUAGE 'sql'
       LEAKPROOF AS 'SELECT $1 > 100'
;
CREATE TABLE conf_tab_2_p1 PARTITION OF conf_tab_2 FOR VALUES FROM (MINVALUE) TO (100)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_trigger
;
DROP TABLE IF EXISTS split_table_idx.list_part
;
ALTER PUBLICATION testpub_foralltables ADD TABLE testpub_tbl2
;
-- Inserts to the table until an insert fails. Like insert_n(), returns the
-- new value for :next_i.
create function insert_until_fail(next_i int, step int default 1) returns int language plpgsql as $$
declare
  i integer;
begin
  -- Insert arrays with 'step' elements each, until an error occurs.
  i := 0;
  loop
    begin
      select insert_n(next_i, step) into next_i;
    exception when others then
      raise notice 'failed with: %', sqlerrm;
      exit;
    end;

    -- The caller is expected to set an injection point that eventually
    -- causes an error. But bail out if still no error after 10000
    -- attempts, so that we don't get stuck in an infinite loop.
    i := i + 1;
    if i = 10000 then
      raise 'no error on inserts after % iterations', i;
    end if;
  end loop;

  return next_i;
end;
$$
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX moneyidx ON moneytmp USING gist ( a )
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW foreign_tables AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_foreign_tables
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table cc2 alter column a2 drop not null
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_25449
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."ec0"
    ADD CONSTRAINT "ec0_pkey" PRIMARY KEY ("ff")
;
----------
[CREATE_CATALOG] CREATE DATABASE dropme_db2 WITH TABLESPACE dropme_ts2 STRATEGY=WAL_LOG
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION blhandler(internal)
RETURNS index_am_handler
AS '$libdir/bloom'
LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_oid_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "float" WITH "simple"
;
----------
[CREATE_TABLE] CREATE TABLE int8tmp (a int8)
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_extensions/test_ext_req_schema3--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- This formulation cannot handle relocation of the required extension.
CREATE FUNCTION dep_req3() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT test_s_dep.dep_req1() || ' req3' $$
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (force_null '*')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26708_2 (tid pg_catalog.tid)
;
----------
[CREATE_VIEW(SELECT)] create view tt19v as
select 'foo'::text = any(array['abc','def','foo']::text[]) c1,
       'foo'::text = any((select array['abc','def','foo']::text[])::text[]) c2
;
----------
[ALTER_POLICY] COMMENT ON CONVERSION myconv_bad IS 'foo'
;
----------
[CREATE_PROG_OBJ] --
-- Tests for 8.4's new RAISE features
--

create or replace function raise_test() returns void as $$
begin
  raise notice '% % %', 1, 2, 3
     using errcode = '55001', detail = 'some detail info', hint = 'some hint';
  raise '% % %', 1, 2, 3
     using errcode = 'division_by_zero', detail = 'some detail info';
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE collate_test1 (
    a int,
    b text COLLATE "en_US" NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE shared_pg_buffercache()
;
----------
[DROP_TYPE] drop type nonesuch
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."partitioned_table" ATTACH PARTITION "public"."pt_part1" FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=21437 host=/tmp/4yqECsv5gm dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[CREATE_TABLE] -- Unique index and unique constraint. Both must fail.
CREATE TABLE tbl_include_unique2 (c1 int, c2 int, c3 int, c4 box)
;
----------
[DROP_INDEX] DROP INDEX table_dropped_index_with_pk_idx
;
----------
[CREATE_TABLE] create table PHone (
    slotname	char(20),
    comment	text,
    slotlink	char(20)
)
;
----------
[ALTER_INDEX] -- idxpart1_a_idx is not valid, so idxpart_a_idx should not become valid:
alter index idxpart_a_idx attach partition idxpart1_a_idx
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".= (
    FUNCTION = "public"."int8alias1eq",
    LEFTARG = bigint,
    RIGHTARG = "public"."int8alias1",
    MERGES,
    RESTRICT = "eqsel",
    JOIN = "eqjoinsel"
)
;
----------
[DROP_INDEX] DROP INDEX i_temptest
;
----------
[CREATE_TABLE] CREATE TABLE clstrpart11 PARTITION OF clstrpart1 FOR VALUES FROM (1) TO (5)
;
----------
[CREATE_POLICY] /*
 * text search configuration for russian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * russian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
----------
[CREATE_TABLE] CREATE TABLE prt2_adv_default PARTITION OF prt2_adv DEFAULT
;
----------
[CREATE_TABLE] create table tt3 (ax int8, b int2, c numeric)
;
----------
[CREATE_TABLE] CREATE TABLE regtest_ptable_ones PARTITION OF regtest_ptable FOR VALUES FROM ('0') TO ('10')
;
----------
[CREATE_TABLE] --
-- Some examples with a tree
--
-- department structure represented here is as follows:
--
-- ROOT-+->A-+->B-+->C
--      |         |
--      |         +->D-+->F
--      +->E-+->G

CREATE TEMP TABLE department (
	id INTEGER PRIMARY KEY,  -- department ID
	parent_department INTEGER REFERENCES department, -- upper department ID
	name TEXT -- department name
)
;
----------
[CREATE_TABLE] CREATE TABLE expr_stats (a int, b name, c text)
;
----------
[CREATE_TABLE] create table pktable2 (a float8, b float8, primary key (a, b))
;
----------
[ALTER_TABLE] ALTER TABLE dump_test.test_table CLUSTER ON test_table_pkey
;
----------
[ALTER_TABLE] ALTER TABLE part_document_satire ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] CREATE TABLE test22a_0 PARTITION OF test22a FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !=- (
   rightarg = int8,
   procedure = factorial
)
;
----------
[CREATE_POLICY] CREATE RULE voo_i AS ON INSERT TO voo DO INSTEAD
  INSERT INTO foo VALUES(new.*, 57)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_25561_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add check ("........pg.dropped.1........" > 3)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted3" DROP COLUMN "........pg.dropped.5........"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_E_2(int) RETURNS bool LANGUAGE 'sql'
       LEAKPROOF AS 'SELECT $1 > 100'
;
----------
[CREATE_TABLE] CREATE TABLE conf_tab_2_p1 PARTITION OF conf_tab_2 FOR VALUES FROM (MINVALUE) TO (100)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_trigger
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS split_table_idx.list_part
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_foralltables ADD TABLE testpub_tbl2
;
----------
[CREATE_PROG_OBJ] -- Inserts to the table until an insert fails. Like insert_n(), returns the
-- new value for :next_i.
create function insert_until_fail(next_i int, step int default 1) returns int language plpgsql as $$
declare
  i integer;
begin
  -- Insert arrays with 'step' elements each, until an error occurs.
  i := 0;
  loop
    begin
      select insert_n(next_i, step) into next_i;
    exception when others then
      raise notice 'failed with: %', sqlerrm;
      exit;
    end;

    -- The caller is expected to set an injection point that eventually
    -- causes an error. But bail out if still no error after 10000
    -- attempts, so that we don't get stuck in an infinite loop.
    i := i + 1;
    if i = 10000 then
      raise 'no error on inserts after % iterations', i;
    end if;
  end loop;

  return next_i;
end;
$$
;
