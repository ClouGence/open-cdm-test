CREATE EVENT TRIGGER split_event_helper_table_rewrite
    ON table_rewrite
    WHEN TAG IN ('ALTER TABLE')
    EXECUTE FUNCTION split_event_helper.capture_table_rewrite()
;
CREATE FUNCTION test_resowner_priorities(nkinds pg_catalog.int4, nresources pg_catalog.int4)
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
ALTER TEXT SEARCH CONFIGURATION irish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH irish_stem
;
ALTER TABLE itest7 ALTER COLUMN a DROP IDENTITY
;
ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser
    ALTER MAPPING REPLACE simple WITH split_ts_probe.simple_dict_two
;
CREATE UNIQUE INDEX "num_exp_div_idx" ON "public"."num_exp_div" USING "btree" ("id1", "id2")
;
-- Re-add mapping, with duplicated tokens supported by the parser.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ADD MAPPING FOR word, word WITH ispell
;
-- not STRICT; needs to allow (key,NULL)

CREATE FUNCTION hstore(text[],text[])
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_arrays'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
ALTER TABLE parent SET (fillfactor = 50)
;
create table s1.p1_1 partition of s1.p1 for values in (1, 2, 3)
;
call test_catalog.test.test_procedure(1,test_catalog.test.test_function(3),(select id from test_catalog.test.table1))
;
ALTER TABLE ONLY "public"."prt2_e" ATTACH PARTITION "public"."prt2_e_p3" FOR VALUES FROM (500) TO (600)
;
ALTER INDEX onek_unique1 RENAME TO attmp_onek_unique1
;
create function test_found()
  returns boolean as '
  declare
  begin
  insert into found_test_tbl values (1);
  if FOUND then
     insert into found_test_tbl values (2);
  end if;

  update found_test_tbl set a = 100 where a = 1;
  if FOUND then
    insert into found_test_tbl values (3);
  end if;

  delete from found_test_tbl where a = 9999; -- matches no rows
  if not FOUND then
    insert into found_test_tbl values (4);
  end if;

  for i in 1 .. 10 loop
    -- no need to do anything
  end loop;
  if FOUND then
    insert into found_test_tbl values (5);
  end if;

  -- never executes the loop
  for i in 2 .. 1 loop
    -- no need to do anything
  end loop;
  if not FOUND then
    insert into found_test_tbl values (6);
  end if;
  return true;
  end;' language plpgsql
;
CREATE FUNCTION gtrgm_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP FUNCTION func_parted_mod_b()
;
--
-- Check that attaching or detaching a partitioned partition correctly leads
-- to its partitions' constraint being updated to reflect the parent's
-- newly added/removed constraint
create table target_parted (a int, b int) partition by list (a)
;
CREATE TABLE default_test (f1 text_w_default, f2 int42)
;
DROP TRIGGER trig_skip_delete ON sub_part2
;
create trigger trig_ins_before before insert on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
CREATE UNIQUE INDEX idx_abcd_nopk_c ON rf_tbl_abcd_nopk(c)
;
ANALYZE public.reloptions_test
;
CREATE TABLE pgss_do_util_tab_2 (a int)
;
alter table dropcol drop column drop1, drop column drop2
;
CREATE FUNCTION dump_test.int42_out(dump_test.int42)
					   RETURNS cstring AS 'int4out'
					   LANGUAGE internal STRICT IMMUTABLE
;
-- cannot add NOT NULL or check constraints to *only* the parent, when
-- partitions exist
ALTER TABLE ONLY list_parted2 ALTER b SET NOT NULL
;
create table idxpart1 (a int not null, b int)
;
-- This next should fail, because int=inet does not exist
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1) references pktable
;
create table def_test (
	c1	int4 default 5,
	c2	text default 'initial_default'
)
;
CREATE OPERATOR FAMILY "public"."part_test_int4_ops" USING "hash"
;
CREATE SEQUENCE test_seq
			INCREMENT BY 13
			MINVALUE 17
			START WITH 23
;
-- Add functions that are new in 9.6 (pg_trgm 1.2).

CREATE FUNCTION gin_trgm_triconsistent(internal, int2, text, int4, internal, internal, internal)
RETURNS "char"
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- delete a few rows inside
ANALYZE vactst
;
-- nested queries

CREATE TEMP VIEW fdv3 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
UNION
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
;
-- We need to do this this way to cope with varying names for encodings:
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test0 (locale = ' ||
          quote_literal(current_setting('lc_collate')) || ');';
END
$$
;
CREATE FUNCTION citext_gt( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
do $$ declare a int[];
begin a[1:2] := array[3,4]; raise notice 'a = %', a; end$$
;
ALTER INDEX "public"."trigger_parted_p2_pkey" ATTACH PARTITION "public"."trigger_parted_p2_2_pkey"
;
CREATE TABLE sq_target_merge_log (tid integer NOT NULL, last_change text)
;
/* contrib/pg_walinspect/pg_walinspect--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- pg_get_wal_record_info()
--
CREATE FUNCTION pg_get_wal_record_info(IN in_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
AS '$libdir/pg_walinspect', 'pg_get_wal_record_info'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE FUNCTION gist_page_opaque_info(IN page bytea,
    OUT lsn pg_lsn,
    OUT nsn pg_lsn,
    OUT rightlink bigint,
    OUT flags text[])
AS '$libdir/pageinspect', 'gist_page_opaque_info'
LANGUAGE C STRICT PARALLEL SAFE
;
drop table if exists tpart
;
-- anyarray column
CREATE TABLE t1x (a int, b anyarray)
;
-- not supported by sepgsql

CREATE TRIGGER regtest_part_test_trig BEFORE UPDATE ON regtest_table_part
    FOR EACH ROW EXECUTE PROCEDURE suppress_redundant_updates_trigger()
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_subscription_rel
;
CREATE INDEX test_tab_a_idx ON testschema.test_tab (a)
;
CREATE OPERATOR CLASS gist_date_ops
DEFAULT FOR TYPE date USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_date_consistent (internal, date, int2, oid, internal),
	FUNCTION	2	gbt_date_union (internal, internal),
	FUNCTION	3	gbt_date_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_date_penalty (internal, internal, internal),
	FUNCTION	6	gbt_date_picksplit (internal, internal),
	FUNCTION	7	gbt_date_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
CREATE TRANSFORM FOR int LANGUAGE SQL (
	FROM SQL WITH FUNCTION prsd_lextype(internal),
	TO SQL WITH FUNCTION int4recv(internal))
;
drop table ���������
;
CREATE TABLE hat_data (
	hat_name    char(10),
	hat_color   char(10)      -- hat color
)
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER split_event_helper_table_rewrite
    ON table_rewrite
    WHEN TAG IN ('ALTER TABLE')
    EXECUTE FUNCTION split_event_helper.capture_table_rewrite()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_resowner_priorities(nkinds pg_catalog.int4, nresources pg_catalog.int4)
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION irish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH irish_stem
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest7 ALTER COLUMN a DROP IDENTITY
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser
    ALTER MAPPING REPLACE simple WITH split_ts_probe.simple_dict_two
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "num_exp_div_idx" ON "public"."num_exp_div" USING "btree" ("id1", "id2")
;
----------
[ALTER_POLICY] -- Re-add mapping, with duplicated tokens supported by the parser.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ADD MAPPING FOR word, word WITH ispell
;
----------
[CREATE_PROG_OBJ] -- not STRICT; needs to allow (key,NULL)

CREATE FUNCTION hstore(text[],text[])
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_arrays'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE parent SET (fillfactor = 50)
;
----------
[CREATE_TABLE] create table s1.p1_1 partition of s1.p1 for values in (1, 2, 3)
;
----------
[CALL_PROG_OBJ] call test_catalog.test.test_procedure(1,test_catalog.test.test_function(3),(select id from test_catalog.test.table1))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_e" ATTACH PARTITION "public"."prt2_e_p3" FOR VALUES FROM (500) TO (600)
;
----------
[RENAME_INDEX] ALTER INDEX onek_unique1 RENAME TO attmp_onek_unique1
;
----------
[CREATE_PROG_OBJ] create function test_found()
  returns boolean as '
  declare
  begin
  insert into found_test_tbl values (1);
  if FOUND then
     insert into found_test_tbl values (2);
  end if;

  update found_test_tbl set a = 100 where a = 1;
  if FOUND then
    insert into found_test_tbl values (3);
  end if;

  delete from found_test_tbl where a = 9999; -- matches no rows
  if not FOUND then
    insert into found_test_tbl values (4);
  end if;

  for i in 1 .. 10 loop
    -- no need to do anything
  end loop;
  if FOUND then
    insert into found_test_tbl values (5);
  end if;

  -- never executes the loop
  for i in 2 .. 1 loop
    -- no need to do anything
  end loop;
  if not FOUND then
    insert into found_test_tbl values (6);
  end if;
  return true;
  end;' language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP FUNCTION func_parted_mod_b()
;
----------
[CREATE_TABLE] --
-- Check that attaching or detaching a partitioned partition correctly leads
-- to its partitions' constraint being updated to reflect the parent's
-- newly added/removed constraint
create table target_parted (a int, b int) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE default_test (f1 text_w_default, f2 int42)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_skip_delete ON sub_part2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_ins_before before insert on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX idx_abcd_nopk_c ON rf_tbl_abcd_nopk(c)
;
----------
[ADMIN_TABLE] ANALYZE public.reloptions_test
;
----------
[CREATE_TABLE] CREATE TABLE pgss_do_util_tab_2 (a int)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table dropcol drop column drop1, drop column drop2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dump_test.int42_out(dump_test.int42)
					   RETURNS cstring AS 'int4out'
					   LANGUAGE internal STRICT IMMUTABLE
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- cannot add NOT NULL or check constraints to *only* the parent, when
-- partitions exist
ALTER TABLE ONLY list_parted2 ALTER b SET NOT NULL
;
----------
[CREATE_TABLE] create table idxpart1 (a int not null, b int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- This next should fail, because int=inet does not exist
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1) references pktable
;
----------
[CREATE_TABLE] create table def_test (
	c1	int4 default 5,
	c2	text default 'initial_default'
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY "public"."part_test_int4_ops" USING "hash"
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE test_seq
			INCREMENT BY 13
			MINVALUE 17
			START WITH 23
;
----------
[CREATE_PROG_OBJ] -- Add functions that are new in 9.6 (pg_trgm 1.2).

CREATE FUNCTION gin_trgm_triconsistent(internal, int2, text, int4, internal, internal, internal)
RETURNS "char"
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] -- delete a few rows inside
ANALYZE vactst
;
----------
[CREATE_VIEW(SELECT)] -- nested queries

CREATE TEMP VIEW fdv3 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
UNION
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
;
----------
[BLOCK] -- We need to do this this way to cope with varying names for encodings:
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test0 (locale = ' ||
          quote_literal(current_setting('lc_collate')) || ');';
END
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_gt( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[BLOCK] do $$ declare a int[];
begin a[1:2] := array[3,4]; raise notice 'a = %', a; end$$
;
----------
[ALTER_INDEX] ALTER INDEX "public"."trigger_parted_p2_pkey" ATTACH PARTITION "public"."trigger_parted_p2_2_pkey"
;
----------
[CREATE_TABLE] CREATE TABLE sq_target_merge_log (tid integer NOT NULL, last_change text)
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_walinspect/pg_walinspect--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- pg_get_wal_record_info()
--
CREATE FUNCTION pg_get_wal_record_info(IN in_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
AS '$libdir/pg_walinspect', 'pg_get_wal_record_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gist_page_opaque_info(IN page bytea,
    OUT lsn pg_lsn,
    OUT nsn pg_lsn,
    OUT rightlink bigint,
    OUT flags text[])
AS '$libdir/pageinspect', 'gist_page_opaque_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] drop table if exists tpart
;
----------
[CREATE_TABLE] -- anyarray column
CREATE TABLE t1x (a int, b anyarray)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- not supported by sepgsql

CREATE TRIGGER regtest_part_test_trig BEFORE UPDATE ON regtest_table_part
    FOR EACH ROW EXECUTE PROCEDURE suppress_redundant_updates_trigger()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_subscription_rel
;
----------
[ADD_INDEX] CREATE INDEX test_tab_a_idx ON testschema.test_tab (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_date_ops
DEFAULT FOR TYPE date USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_date_consistent (internal, date, int2, oid, internal),
	FUNCTION	2	gbt_date_union (internal, internal),
	FUNCTION	3	gbt_date_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_date_penalty (internal, internal, internal),
	FUNCTION	6	gbt_date_picksplit (internal, internal),
	FUNCTION	7	gbt_date_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
----------
[UNKNOWN] CREATE TRANSFORM FOR int LANGUAGE SQL (
	FROM SQL WITH FUNCTION prsd_lextype(internal),
	TO SQL WITH FUNCTION int4recv(internal))
;
----------
[DROP_TABLE] drop table ���������
;
----------
[CREATE_TABLE] CREATE TABLE hat_data (
	hat_name    char(10),
	hat_color   char(10)      -- hat color
)
;
