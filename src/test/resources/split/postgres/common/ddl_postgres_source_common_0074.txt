CREATE INDEX ON atest12 (a)
;
CREATE EVENT TRIGGER split_event_helper_sql_drop
    ON sql_drop
    WHEN TAG IN ('DROP TABLE')
    EXECUTE FUNCTION split_event_helper.capture_dropped_objects()
;
DROP TRIGGER rw_view1_ins_trig ON rw_view1
;
drop function parted_conflict_update_func()
;
DROP FUNCTION foo_f()
;
CREATE TABLE xact_test(data text)
;
CREATE OR REPLACE FUNCTION
  jsonb_set_lax(jsonb_in jsonb, path text[] , replacement jsonb,
            create_if_missing boolean DEFAULT true,
            null_value_treatment text DEFAULT 'use_json_null')
RETURNS jsonb
LANGUAGE INTERNAL
CALLED ON NULL INPUT IMMUTABLE PARALLEL SAFE
AS 'jsonb_set_lax'
;
CREATE TABLE schema_one."table two"(a int)
;
-- check that we preserve join alias in GROUP BY expressions
create temp view v1 as
select f1::int from t1 left join t2 using (f1) group by f1
;
drop table parent
;
ALTER TABLE pg_toast.pg_toast_30731 RENAME TO reind_con_toast
;
ALTER OPERATOR >= (ean13, issn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
create table test_part_coll_cast partition of test_part_coll_posix for values from (name 'm' collate "C") to ('s')
;
VACUUM (PARALLEL 2) pg_catalog.pg_namespace
;
CREATE PROCEDURE ptest4c(INOUT comp int8_tbl)
LANGUAGE SQL
AS $$
SELECT ROW(1, 2)::int8_tbl;
$$
;
create table p1p2_c2 (f1 int constraint f1_pos CHECK (f1 > 0)) inherits (p1, p2)
;
-- we used to get confused by a single output argument that is composite
CREATE PROCEDURE ptest4c(INOUT comp int8_tbl)
LANGUAGE SQL
AS $$
SELECT ROW(1, 2)::int8_tbl;
$$
;
-- PK in inheriting table:
CREATE TABLE temporal_rng (
  id int4range,
  valid_at daterange
)
;
CREATE TABLE tab2 (a int PRIMARY KEY, b varchar)
;
drop table alterlock2
;
ALTER FUNCTION gbt_ts_picksplit(internal, internal) PARALLEL SAFE
;
CREATE FUNCTION chkrolattr()
 RETURNS TABLE ("role" name, rolekeyword text, canlogin bool, replication bool)
 AS $$
SELECT r.rolname, v.keyword, r.rolcanlogin, r.rolreplication
 FROM pg_roles r
 JOIN (VALUES(CURRENT_ROLE, 'current_role'),
             (CURRENT_USER, 'current_user'),
             (SESSION_USER, 'session_user'),
             ('current_role', '-'),
             ('current_user', '-'),
             ('session_user', '-'),
             ('Public', '-'),
             ('None', '-'))
      AS v(uname, keyword)
      ON (r.rolname = v.uname)
 ORDER BY 1, 2;
$$ LANGUAGE SQL
;
ALTER TABLE pg_amop ADD UNIQUE USING INDEX pg_amop_fam_strat_index
;
CREATE TABLE "public"."pagg_tab2_p2" (
    "x" integer,
    "y" integer
)
;
create index mcrparted_a_abs_c_idx on mcrparted (a, abs(b), c)
;
CREATE TABLE "public"."tab_upgraded" (
    "id" integer
)
;
CREATE AGGREGATE myaggn13b(BASETYPE = int, SFUNC = tfnp, STYPE = anyarray,
  INITCOND = '{}')
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_inherits
;
DROP DOMAIN priv_testdomain1
;
ALTER TABLE ONLY "public"."mlparted_def" ATTACH PARTITION "public"."mlparted_def2" FOR VALUES FROM (50) TO (60)
;
CREATE TABLE dump_test.test_table_fk_1
							PARTITION OF dump_test.test_table_fk
							FOR VALUES FROM (0) TO (10)
;
-- also test generated column with a "forward" reference (bug #16342)
CREATE TABLE test_like_4 (b int DEFAULT 42,
  c int GENERATED ALWAYS AS (a * 2) STORED,
  a int CHECK (a > 0))
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) information_schema.sql_features
;
-- test compression with materialized view
CREATE MATERIALIZED VIEW compressmv(x) AS SELECT * FROM cmdata1
;
CREATE FUNCTION "public"."tg_iface_biu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $_$
declare
    sname	text;
    sysrec	record;
begin
    select into sysrec * from system where name = new.sysname;
    if not found then
        raise exception $q$system "%" does not exist$q$, new.sysname;
    end if;
    sname := 'IF.' || new.sysname;
    sname := sname || '.';
    sname := sname || new.ifname;
    if length(sname) > 20 then
        raise exception 'IFace slotname "%" too long (20 char max)', sname;
    end if;
    new.slotname := sname;
    return new;
end;
$_$
;
CREATE TABLE inht2 (x int) INHERITS (inht1)
;
ALTER TABLE replication_example ADD COLUMN bar int
;
ALTER TEXT SEARCH CONFIGURATION tamil ADD MAPPING
    FOR word, hword_part, hword
	WITH tamil_stem
;
CREATE INDEX idx_postgis_places_geog_gist ON split_postgis.places USING gist (geog)
;
ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.6........"
;
DROP TABLE concur_exprs_tab
;
CREATE INDEX btg_x_y_idx ON btg(x, y)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_statistic
;
ALTER TABLE fk_partitioned_fk_6 DETACH PARTITION fk_partitioned_fk_6_1
;
alter table fkpart0.fk_part_1 drop constraint fk_part_1_a_fkey
;
CREATE TABLE public.tst_range (
		a INTEGER PRIMARY KEY,
		b int4range
	)
;
CREATE EVENT TRIGGER test_event_trigger
					   ON ddl_command_start
					   EXECUTE FUNCTION dump_test.event_trigger_func()
;
--distance operators

CREATE FUNCTION cash_dist(money, money)
RETURNS money
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create or replace function dump_delete() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, old table = %',
                 TG_NAME,
                 (select string_agg(old_table::text, ', ' order by a) from old_table);
    return null;
  end;
$$
;
alter table pfk attach partition pfk1 for values in (1)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX ON atest12 (a)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER split_event_helper_sql_drop
    ON sql_drop
    WHEN TAG IN ('DROP TABLE')
    EXECUTE FUNCTION split_event_helper.capture_dropped_objects()
;
----------
[DROP_TRIGGER] DROP TRIGGER rw_view1_ins_trig ON rw_view1
;
----------
[DROP_PROG_OBJ] drop function parted_conflict_update_func()
;
----------
[DROP_PROG_OBJ] DROP FUNCTION foo_f()
;
----------
[CREATE_TABLE] CREATE TABLE xact_test(data text)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_set_lax(jsonb_in jsonb, path text[] , replacement jsonb,
            create_if_missing boolean DEFAULT true,
            null_value_treatment text DEFAULT 'use_json_null')
RETURNS jsonb
LANGUAGE INTERNAL
CALLED ON NULL INPUT IMMUTABLE PARALLEL SAFE
AS 'jsonb_set_lax'
;
----------
[CREATE_TABLE] CREATE TABLE schema_one."table two"(a int)
;
----------
[CREATE_VIEW(SELECT)] -- check that we preserve join alias in GROUP BY expressions
create temp view v1 as
select f1::int from t1 left join t2 using (f1) group by f1
;
----------
[DROP_TABLE] drop table parent
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30731 RENAME TO reind_con_toast
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ean13, issn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[CREATE_TABLE] create table test_part_coll_cast partition of test_part_coll_posix for values from (name 'm' collate "C") to ('s')
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_namespace
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest4c(INOUT comp int8_tbl)
LANGUAGE SQL
AS $$
SELECT ROW(1, 2)::int8_tbl;
$$
;
----------
[CREATE_TABLE] create table p1p2_c2 (f1 int constraint f1_pos CHECK (f1 > 0)) inherits (p1, p2)
;
----------
[CREATE_PROG_OBJ] -- we used to get confused by a single output argument that is composite
CREATE PROCEDURE ptest4c(INOUT comp int8_tbl)
LANGUAGE SQL
AS $$
SELECT ROW(1, 2)::int8_tbl;
$$
;
----------
[CREATE_TABLE] -- PK in inheriting table:
CREATE TABLE temporal_rng (
  id int4range,
  valid_at daterange
)
;
----------
[CREATE_TABLE] CREATE TABLE tab2 (a int PRIMARY KEY, b varchar)
;
----------
[DROP_TABLE] drop table alterlock2
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_ts_picksplit(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION chkrolattr()
 RETURNS TABLE ("role" name, rolekeyword text, canlogin bool, replication bool)
 AS $$
SELECT r.rolname, v.keyword, r.rolcanlogin, r.rolreplication
 FROM pg_roles r
 JOIN (VALUES(CURRENT_ROLE, 'current_role'),
             (CURRENT_USER, 'current_user'),
             (SESSION_USER, 'session_user'),
             ('current_role', '-'),
             ('current_user', '-'),
             ('session_user', '-'),
             ('Public', '-'),
             ('None', '-'))
      AS v(uname, keyword)
      ON (r.rolname = v.uname)
 ORDER BY 1, 2;
$$ LANGUAGE SQL
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_amop ADD UNIQUE USING INDEX pg_amop_fam_strat_index
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab2_p2" (
    "x" integer,
    "y" integer
)
;
----------
[ADD_INDEX] create index mcrparted_a_abs_c_idx on mcrparted (a, abs(b), c)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tab_upgraded" (
    "id" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn13b(BASETYPE = int, SFUNC = tfnp, STYPE = anyarray,
  INITCOND = '{}')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_inherits
;
----------
[DROP_TYPE] DROP DOMAIN priv_testdomain1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted_def" ATTACH PARTITION "public"."mlparted_def2" FOR VALUES FROM (50) TO (60)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_fk_1
							PARTITION OF dump_test.test_table_fk
							FOR VALUES FROM (0) TO (10)
;
----------
[CREATE_TABLE] -- also test generated column with a "forward" reference (bug #16342)
CREATE TABLE test_like_4 (b int DEFAULT 42,
  c int GENERATED ALWAYS AS (a * 2) STORED,
  a int CHECK (a > 0))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) information_schema.sql_features
;
----------
[CREATE_VIEW(SELECT)] -- test compression with materialized view
CREATE MATERIALIZED VIEW compressmv(x) AS SELECT * FROM cmdata1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_iface_biu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $_$
declare
    sname	text;
    sysrec	record;
begin
    select into sysrec * from system where name = new.sysname;
    if not found then
        raise exception $q$system "%" does not exist$q$, new.sysname;
    end if;
    sname := 'IF.' || new.sysname;
    sname := sname || '.';
    sname := sname || new.ifname;
    if length(sname) > 20 then
        raise exception 'IFace slotname "%" too long (20 char max)', sname;
    end if;
    new.slotname := sname;
    return new;
end;
$_$
;
----------
[CREATE_TABLE] CREATE TABLE inht2 (x int) INHERITS (inht1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE replication_example ADD COLUMN bar int
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION tamil ADD MAPPING
    FOR word, hword_part, hword
	WITH tamil_stem
;
----------
[ADD_INDEX] CREATE INDEX idx_postgis_places_geog_gist ON split_postgis.places USING gist (geog)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.6........"
;
----------
[DROP_TABLE] DROP TABLE concur_exprs_tab
;
----------
[ADD_INDEX] CREATE INDEX btg_x_y_idx ON btg(x, y)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_statistic
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk_6 DETACH PARTITION fk_partitioned_fk_6_1
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table fkpart0.fk_part_1 drop constraint fk_part_1_a_fkey
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_range (
		a INTEGER PRIMARY KEY,
		b int4range
	)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER test_event_trigger
					   ON ddl_command_start
					   EXECUTE FUNCTION dump_test.event_trigger_func()
;
----------
[CREATE_PROG_OBJ] --distance operators

CREATE FUNCTION cash_dist(money, money)
RETURNS money
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] create or replace function dump_delete() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, old table = %',
                 TG_NAME,
                 (select string_agg(old_table::text, ', ' order by a) from old_table);
    return null;
  end;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table pfk attach partition pfk1 for values in (1)
;
