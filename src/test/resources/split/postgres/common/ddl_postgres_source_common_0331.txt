DROP TABLE TEMP_FLOAT
;
-- force use of parallelism
ALTER TABLE tenk1 set (parallel_workers = 4)
;
CREATE TABLE alpha_neg_p2 PARTITION OF alpha_neg FOR VALUES FROM (200) TO (300)
;
VACUUM FULL pg_proc
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "url_path" WITH "simple"
;
ANALYZE test__int
;
create table insertconflicttest(key int4, fruit text)
;
create table parted_trigger_2 (drp int, a int, b text)
;
CREATE OPERATOR CLASS gist__ltree_ops
    DEFAULT FOR TYPE _ltree USING gist AS
	OPERATOR	10	<@ (_ltree, ltree),
	OPERATOR	11	@> (ltree, _ltree),
	OPERATOR	12	~ (_ltree, lquery),
	OPERATOR	13	~ (lquery, _ltree),
	OPERATOR	14	@ (_ltree, ltxtquery),
	OPERATOR	15	@ (ltxtquery, _ltree),
	OPERATOR	16	? (_ltree, _lquery),
	OPERATOR	17	? (_lquery, _ltree),
	FUNCTION	1	_ltree_consistent (internal, _ltree, int2, oid, internal),
	FUNCTION	2	_ltree_union (internal, internal),
	FUNCTION	3	_ltree_compress (internal),
	FUNCTION	4	ltree_decompress (internal),
	FUNCTION	5	_ltree_penalty (internal, internal, internal),
	FUNCTION	6	_ltree_picksplit (internal, internal),
	FUNCTION	7	_ltree_same (ltree_gist, ltree_gist, internal),
	STORAGE		ltree_gist
;
-- ok

-- test multiple inheritance mismatches
CREATE TABLE gtesty (x int, b int DEFAULT 55)
;
CREATE RULE "rtest_pers_upd" AS
    ON UPDATE TO "public"."rtest_person" DO  UPDATE "public"."rtest_admin" SET "pname" = "new"."pname"
  WHERE ("rtest_admin"."pname" = "old"."pname")
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub8
;
create table fktable (ftest1 int references pktable(base1))
;
TRUNCATE tbl_created_outside_xact
;
-- 6.3 INFORMATION_SCHEMA_CATALOG_NAME view appears later.


/*
 * 6.4
 * CARDINAL_NUMBER domain
 */

CREATE DOMAIN cardinal_number AS integer
    CONSTRAINT cardinal_number_domain_check CHECK (value >= 0)
;
COMMENT ON TEXT SEARCH CONFIGURATION tamil IS 'configuration for tamil language'
;
CREATE INDEX t2_hash ON s5.t2 USING HASH (i)
;
CREATE TABLE plt2_adv_p3 PARTITION OF plt2_adv FOR VALUES IN ('0007', '0009')
;
-- Different number of attributes, mapping types
CREATE TABLE pgss_schema_1.tab_search_diff_1 (a int)
;
create table trg_clone_3_3 partition of trg_clone3 for values from (2000) to (2100)
;
COMMENT ON CONSTRAINT the_constraint ON DOMAIN constraint_comments_dom IS NULL
;
-- Check UPDATE with *partitioned* inherited target and an appendrel subquery
create table some_tab (a int)
;
ALTER TABLE pg_amproc ADD UNIQUE USING INDEX pg_amproc_fam_proc_index
;
create cast (float4 as xfloat4) without function
;
--
-- Test builtin "C"
--
CREATE COLLATION regress_builtin_c (
  provider = builtin, locale = 'C')
;
DROP MATERIALIZED VIEW order_summary
;
create table c1(f3 int) inherits(p1,p2)
;
-- whole-row references are disallowed
ALTER TABLE test_type_diff2 ALTER COLUMN int_four TYPE int4 USING (pg_column_size(test_type_diff2))
;
ALTER TABLE ONLY "generated_virtual_tests"."gtestx" INHERIT "generated_virtual_tests"."gtest1"
;
create table fkpart0.fk_part_4 partition of fkpart0.fk_part for values in (4)
;
CREATE DATABASE postgres
;
create index part_column_drop_d_pred on part_column_drop(d) where d = 2
;
alter table recur1 add column f2 recur1
;
ALTER TABLE list_parted2 DROP COLUMN b
;
-- UNIQUE with a custom range type:
CREATE TYPE textrange2 AS range (subtype=text, collation="C")
;
create table pgbench_history(tid int,bid int,aid    int,delta int,mtime timestamp,filler char(22))
;
-- check index compatibility handling when opclass option is specified
alter table test_trgm alter column t type varchar(768)
;
-- VALIDATE CONSTRAINT scans the table
ALTER TABLE notnull_tbl1 VALIDATE CONSTRAINT nn
;
CREATE FUNCTION hstore_gt(hstore,hstore)
RETURNS boolean
AS '$libdir/hstore','hstore_gt'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table range_parted1 partition of range_parted for values from (0,0) to (10,10)
;
/*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2016
 *
 * Copyright (c) 2003-2020, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 5.1
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
CREATE FUNCTION g_intbig_union(internal, internal)
RETURNS intbig_gkey
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE SUBSCRIPTION "regress_sub5" CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION "regress_pub5" WITH (connect = false, slot_name = 'regress_sub5')
;
ANALYZE public.tenk2
;
-- rename back to keep the rest simple
ALTER SUBSCRIPTION regress_testsub_foo RENAME TO regress_testsub
;
CREATE TABLE concur_reindex_tab4 (c1 int)
;
CREATE TABLE "public"."pagg_tab2" (
    "x" integer,
    "y" integer
)
PARTITION BY RANGE ("y")
;
-- error
ALTER INDEX dummy_test_idx SET (option_bool = 1)
;
DROP MATERIALIZED VIEW drop_idx_matview
;
ALTER INDEX pg_toast.pg_toast_28478_index RENAME TO reind_con_toast_idx
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE TEMP_FLOAT
;
----------
[ALTER_TABLE] -- force use of parallelism
ALTER TABLE tenk1 set (parallel_workers = 4)
;
----------
[CREATE_TABLE] CREATE TABLE alpha_neg_p2 PARTITION OF alpha_neg FOR VALUES FROM (200) TO (300)
;
----------
[ADMIN_TABLE] VACUUM FULL pg_proc
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "url_path" WITH "simple"
;
----------
[ADMIN_TABLE] ANALYZE test__int
;
----------
[CREATE_TABLE] create table insertconflicttest(key int4, fruit text)
;
----------
[CREATE_TABLE] create table parted_trigger_2 (drp int, a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist__ltree_ops
    DEFAULT FOR TYPE _ltree USING gist AS
	OPERATOR	10	<@ (_ltree, ltree),
	OPERATOR	11	@> (ltree, _ltree),
	OPERATOR	12	~ (_ltree, lquery),
	OPERATOR	13	~ (lquery, _ltree),
	OPERATOR	14	@ (_ltree, ltxtquery),
	OPERATOR	15	@ (ltxtquery, _ltree),
	OPERATOR	16	? (_ltree, _lquery),
	OPERATOR	17	? (_lquery, _ltree),
	FUNCTION	1	_ltree_consistent (internal, _ltree, int2, oid, internal),
	FUNCTION	2	_ltree_union (internal, internal),
	FUNCTION	3	_ltree_compress (internal),
	FUNCTION	4	ltree_decompress (internal),
	FUNCTION	5	_ltree_penalty (internal, internal, internal),
	FUNCTION	6	_ltree_picksplit (internal, internal),
	FUNCTION	7	_ltree_same (ltree_gist, ltree_gist, internal),
	STORAGE		ltree_gist
;
----------
[CREATE_TABLE] -- ok

-- test multiple inheritance mismatches
CREATE TABLE gtesty (x int, b int DEFAULT 55)
;
----------
[CREATE_POLICY] CREATE RULE "rtest_pers_upd" AS
    ON UPDATE TO "public"."rtest_person" DO  UPDATE "public"."rtest_admin" SET "pname" = "new"."pname"
  WHERE ("rtest_admin"."pname" = "old"."pname")
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub8
;
----------
[CREATE_TABLE] create table fktable (ftest1 int references pktable(base1))
;
----------
[TRUNCATE_TABLE] TRUNCATE tbl_created_outside_xact
;
----------
[CREATE_TYPE] -- 6.3 INFORMATION_SCHEMA_CATALOG_NAME view appears later.


/*
 * 6.4
 * CARDINAL_NUMBER domain
 */

CREATE DOMAIN cardinal_number AS integer
    CONSTRAINT cardinal_number_domain_check CHECK (value >= 0)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION tamil IS 'configuration for tamil language'
;
----------
[ADD_INDEX] CREATE INDEX t2_hash ON s5.t2 USING HASH (i)
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p3 PARTITION OF plt2_adv FOR VALUES IN ('0007', '0009')
;
----------
[CREATE_TABLE] -- Different number of attributes, mapping types
CREATE TABLE pgss_schema_1.tab_search_diff_1 (a int)
;
----------
[CREATE_TABLE] create table trg_clone_3_3 partition of trg_clone3 for values from (2000) to (2100)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT the_constraint ON DOMAIN constraint_comments_dom IS NULL
;
----------
[CREATE_TABLE] -- Check UPDATE with *partitioned* inherited target and an appendrel subquery
create table some_tab (a int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_amproc ADD UNIQUE USING INDEX pg_amproc_fam_proc_index
;
----------
[CREATE_PROG_OBJ] create cast (float4 as xfloat4) without function
;
----------
[CREATE_POLICY] --
-- Test builtin "C"
--
CREATE COLLATION regress_builtin_c (
  provider = builtin, locale = 'C')
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW order_summary
;
----------
[CREATE_TABLE] create table c1(f3 int) inherits(p1,p2)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- whole-row references are disallowed
ALTER TABLE test_type_diff2 ALTER COLUMN int_four TYPE int4 USING (pg_column_size(test_type_diff2))
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_virtual_tests"."gtestx" INHERIT "generated_virtual_tests"."gtest1"
;
----------
[CREATE_TABLE] create table fkpart0.fk_part_4 partition of fkpart0.fk_part for values in (4)
;
----------
[CREATE_CATALOG] CREATE DATABASE postgres
;
----------
[ADD_INDEX] create index part_column_drop_d_pred on part_column_drop(d) where d = 2
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table recur1 add column f2 recur1
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE list_parted2 DROP COLUMN b
;
----------
[CREATE_POLICY] -- UNIQUE with a custom range type:
CREATE TYPE textrange2 AS range (subtype=text, collation="C")
;
----------
[CREATE_TABLE] create table pgbench_history(tid int,bid int,aid    int,delta int,mtime timestamp,filler char(22))
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- check index compatibility handling when opclass option is specified
alter table test_trgm alter column t type varchar(768)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- VALIDATE CONSTRAINT scans the table
ALTER TABLE notnull_tbl1 VALIDATE CONSTRAINT nn
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_gt(hstore,hstore)
RETURNS boolean
AS '$libdir/hstore','hstore_gt'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table range_parted1 partition of range_parted for values from (0,0) to (10,10)
;
----------
[CREATE_SCHEMA] /*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2016
 *
 * Copyright (c) 2003-2020, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 5.1
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_intbig_union(internal, internal)
RETURNS intbig_gkey
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION "regress_sub5" CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION "regress_pub5" WITH (connect = false, slot_name = 'regress_sub5')
;
----------
[ADMIN_TABLE] ANALYZE public.tenk2
;
----------
[ALTER_PUB_SUB] -- rename back to keep the rest simple
ALTER SUBSCRIPTION regress_testsub_foo RENAME TO regress_testsub
;
----------
[CREATE_TABLE] CREATE TABLE concur_reindex_tab4 (c1 int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab2" (
    "x" integer,
    "y" integer
)
PARTITION BY RANGE ("y")
;
----------
[ALTER_INDEX] -- error
ALTER INDEX dummy_test_idx SET (option_bool = 1)
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW drop_idx_matview
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28478_index RENAME TO reind_con_toast_idx
;
