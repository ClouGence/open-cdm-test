CREATE FUNCTION double_append(anyarray, anyelement) RETURNS SETOF anyarray
LANGUAGE SQL IMMUTABLE AS
$$ SELECT array_append($1, $2) || array_append($1, $2) $$
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_statistic
;
CREATE OPERATOR !~* (
    PROCEDURE = texticregexne,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = ~*,
    RESTRICT  = icregexnesel,
    JOIN      = icregexnejoinsel
)
;
ALTER TABLE ONLY "regress_indexing"."pk" ATTACH PARTITION "regress_indexing"."pk1" FOR VALUES FROM (0) TO (1000)
;
CREATE TABLE gtest27 (
    a int,
    b int,
    x int GENERATED ALWAYS AS ((a + b) * 2) STORED
)
;
ANALYZE public.pht2_p3
;
create type textrange_en_us as range(subtype=text, collation="en_US")
;
ALTER TABLE pg_toast.pg_toast_33552 RENAME TO reind_con_toast
;
drop table pc_list_parted, pc_list_part_null
;
-- Token supported by the configuration's parser, cannot be found,
-- succeeds with IF EXISTS.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING IF EXISTS FOR word, word
;
VACUUM (FULL) pg_catalog.pg_init_privs
;
CREATE TABLE gtest22a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a / 2) STORED UNIQUE)
;
-- ok
SECURITY LABEL ON TABLE tpart
    IS 'invalid security context'
;
DROP TABLE test_io_shared
;
-- fails

DROP TABLE test_tbl1
;
CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS 'SELECT 1, 2, 3;'
;
ALTER TABLE ONLY "public"."notnull_tbl4"
    ADD CONSTRAINT "notnull_tbl4_pkey" PRIMARY KEY ("a") DEFERRABLE INITIALLY DEFERRED
;
-- test scalar-to-array operators
create table coercepart (a varchar) partition by list (a)
;
-- simple WITH CHECK OPTION

CREATE TABLE base_tbl (a int, b int DEFAULT 10)
;
CREATE DOMAIN earth AS public.cube
  CONSTRAINT not_point CHECK(public.cube_is_point(VALUE))
  CONSTRAINT not_3d CHECK(public.cube_dim(VALUE) <= 3)
  CONSTRAINT on_surface CHECK(abs(public.cube_distance(VALUE, '(0)'::public.cube) /
  earth() - '1'::float8) < '10e-7'::float8)
;
-- Tests for REINDEX
CREATE OR REPLACE FUNCTION reindex_start_command()
RETURNS event_trigger AS $$
BEGIN
    RAISE NOTICE 'REINDEX START: % %', tg_event, tg_tag;
END;
$$ LANGUAGE plpgsql
;
--
-- test the "star" operators a bit more thoroughly -- this time,
-- throw in lots of NULL fields...
--
-- a is the type root
-- b and c inherit from a (one-level single inheritance)
-- d inherits from b and c (two-level multiple inheritance)
-- e inherits from c (two-level single inheritance)
-- f inherits from e (three-level single inheritance)
--
CREATE TABLE a_star (
	class		char,
	a 			int4
)
;
DROP POLICY p1 ON r2
;
CREATE TABLE IF NOT EXISTS fkey_table (
    id           INT NOT NULL DEFAULT nextval('fkey_table_seq'::REGCLASS),
    datatype_id  INT NOT NULL REFERENCES datatype_table(id),
    big_id       BIGINT NOT NULL,
    sometext     TEXT COLLATE "POSIX",
    check_col_1  INT NOT NULL CHECK(check_col_1 < 10),
    check_col_2  INT NOT NULL,
    PRIMARY KEY  (id),
    CONSTRAINT fkey_big_id
      FOREIGN KEY (big_id)
      REFERENCES datatype_table(id_big),
    EXCLUDE USING btree (check_col_2 WITH =)
)
;
create table tpart_2(like tpart)
;
CREATE OPERATOR CLASS issn13_ops DEFAULT
	FOR TYPE issn13 USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashissn13(issn13)
;
ANALYZE pg_temp_12.pg_temp_26358_2
;
CREATE FUNCTION spgist_name_leaf_consistent(internal, internal)
RETURNS boolean IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE TYPE public.tst_comp_enum_array_t AS (a FLOAT, b public.tst_enum_t[], c INTEGER)
;
ALTER POLICY pp1 ON part_document USING (dauthor = current_user)
;
ALTER TABLE batch_table_p1 ADD CONSTRAINT p1_pkey PRIMARY KEY (x)
;
create temp table parent (
    aid int not null primary key,
    val1 text,
    val2 text,
    val3 text,
    val4 text,
    bcnt int not null default 0)
;
CREATE TABLE pitest3_p1 PARTITION OF pitest3 FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
ALTER TABLE pg_toast.pg_toast_28593 RENAME TO reind_con_toast
;
CREATE INDEX brinidx_multi ON brintest_multi USING brin (
	int8col int8_minmax_multi_ops,
	int2col int2_minmax_multi_ops,
	int4col int4_minmax_multi_ops,
	oidcol oid_minmax_multi_ops,
	tidcol tid_minmax_multi_ops,
	float4col float4_minmax_multi_ops,
	float8col float8_minmax_multi_ops,
	macaddrcol macaddr_minmax_multi_ops,
	macaddr8col macaddr8_minmax_multi_ops,
	inetcol inet_minmax_multi_ops,
	cidrcol inet_minmax_multi_ops,
	datecol date_minmax_multi_ops,
	timecol time_minmax_multi_ops,
	timestampcol timestamp_minmax_multi_ops,
	timestamptzcol timestamptz_minmax_multi_ops,
	intervalcol interval_minmax_multi_ops,
	timetzcol timetz_minmax_multi_ops,
	numericcol numeric_minmax_multi_ops,
	uuidcol uuid_minmax_multi_ops,
	lsncol pg_lsn_minmax_multi_ops
)
;
drop table base_tab_def
;
ALTER TABLE dump_test.test_table
					   ENABLE ROW LEVEL SECURITY
;
-- force_not_null and force_null can be used together on the same column
ALTER FOREIGN TABLE text_csv ALTER COLUMN word1 OPTIONS (force_null 'true')
;
alter table pgbench_tellers add primary key (tid) using index tablespace "regress_pgbench_tap_1_ts"
;
CREATE TRIGGER "tg_chkslotlink" BEFORE INSERT OR UPDATE ON "public"."hslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotlink"()
;
ALTER INDEX pg_toast.pg_toast_27691_index RENAME TO reind_con_toast_idx
;
CREATE TABLE tbl1(val1 integer, val2 integer)
;
--
-- Test domains over multirange types
--

create domain restrictedmultirange as int4multirange check (upper(value) < 10)
;
-- Parallel Append with only non-partial subplans
alter table a_star set (parallel_workers = 0)
;
/*
 * 2. CREATE INDEX CONCURRENTLY
 */
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
drop function lock_excl(int,int)
;
create index parted_minmax1i on parted_minmax1 (a, b)
;
DROP CAST (text AS casttesttype)
;
CREATE FUNCTION daitch_mokotoff(text) RETURNS text[]
AS '$libdir/fuzzystrmatch', 'daitch_mokotoff'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- various error cases

CALL version()
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION double_append(anyarray, anyelement) RETURNS SETOF anyarray
LANGUAGE SQL IMMUTABLE AS
$$ SELECT array_append($1, $2) || array_append($1, $2) $$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_statistic
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !~* (
    PROCEDURE = texticregexne,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = ~*,
    RESTRICT  = icregexnesel,
    JOIN      = icregexnejoinsel
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "regress_indexing"."pk" ATTACH PARTITION "regress_indexing"."pk1" FOR VALUES FROM (0) TO (1000)
;
----------
[CREATE_TABLE] CREATE TABLE gtest27 (
    a int,
    b int,
    x int GENERATED ALWAYS AS ((a + b) * 2) STORED
)
;
----------
[ADMIN_TABLE] ANALYZE public.pht2_p3
;
----------
[CREATE_POLICY] create type textrange_en_us as range(subtype=text, collation="en_US")
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33552 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] drop table pc_list_parted, pc_list_part_null
;
----------
[ALTER_POLICY] -- Token supported by the configuration's parser, cannot be found,
-- succeeds with IF EXISTS.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING IF EXISTS FOR word, word
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_init_privs
;
----------
[CREATE_TABLE] CREATE TABLE gtest22a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a / 2) STORED UNIQUE)
;
----------
[UNKNOWN] -- ok
SECURITY LABEL ON TABLE tpart
    IS 'invalid security context'
;
----------
[DROP_TABLE] DROP TABLE test_io_shared
;
----------
[DROP_TABLE] -- fails

DROP TABLE test_tbl1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS 'SELECT 1, 2, 3;'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."notnull_tbl4"
    ADD CONSTRAINT "notnull_tbl4_pkey" PRIMARY KEY ("a") DEFERRABLE INITIALLY DEFERRED
;
----------
[CREATE_TABLE] -- test scalar-to-array operators
create table coercepart (a varchar) partition by list (a)
;
----------
[CREATE_TABLE] -- simple WITH CHECK OPTION

CREATE TABLE base_tbl (a int, b int DEFAULT 10)
;
----------
[CREATE_TYPE] CREATE DOMAIN earth AS public.cube
  CONSTRAINT not_point CHECK(public.cube_is_point(VALUE))
  CONSTRAINT not_3d CHECK(public.cube_dim(VALUE) <= 3)
  CONSTRAINT on_surface CHECK(abs(public.cube_distance(VALUE, '(0)'::public.cube) /
  earth() - '1'::float8) < '10e-7'::float8)
;
----------
[CREATE_PROG_OBJ] -- Tests for REINDEX
CREATE OR REPLACE FUNCTION reindex_start_command()
RETURNS event_trigger AS $$
BEGIN
    RAISE NOTICE 'REINDEX START: % %', tg_event, tg_tag;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] --
-- test the "star" operators a bit more thoroughly -- this time,
-- throw in lots of NULL fields...
--
-- a is the type root
-- b and c inherit from a (one-level single inheritance)
-- d inherits from b and c (two-level multiple inheritance)
-- e inherits from c (two-level single inheritance)
-- f inherits from e (three-level single inheritance)
--
CREATE TABLE a_star (
	class		char,
	a 			int4
)
;
----------
[DROP_POLICY] DROP POLICY p1 ON r2
;
----------
[CREATE_TABLE] CREATE TABLE IF NOT EXISTS fkey_table (
    id           INT NOT NULL DEFAULT nextval('fkey_table_seq'::REGCLASS),
    datatype_id  INT NOT NULL REFERENCES datatype_table(id),
    big_id       BIGINT NOT NULL,
    sometext     TEXT COLLATE "POSIX",
    check_col_1  INT NOT NULL CHECK(check_col_1 < 10),
    check_col_2  INT NOT NULL,
    PRIMARY KEY  (id),
    CONSTRAINT fkey_big_id
      FOREIGN KEY (big_id)
      REFERENCES datatype_table(id_big),
    EXCLUDE USING btree (check_col_2 WITH =)
)
;
----------
[CREATE_TABLE] create table tpart_2(like tpart)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS issn13_ops DEFAULT
	FOR TYPE issn13 USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashissn13(issn13)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26358_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION spgist_name_leaf_consistent(internal, internal)
RETURNS boolean IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TYPE] CREATE TYPE public.tst_comp_enum_array_t AS (a FLOAT, b public.tst_enum_t[], c INTEGER)
;
----------
[ALTER_POLICY] ALTER POLICY pp1 ON part_document USING (dauthor = current_user)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE batch_table_p1 ADD CONSTRAINT p1_pkey PRIMARY KEY (x)
;
----------
[CREATE_TABLE] create temp table parent (
    aid int not null primary key,
    val1 text,
    val2 text,
    val3 text,
    val4 text,
    bcnt int not null default 0)
;
----------
[CREATE_TABLE] CREATE TABLE pitest3_p1 PARTITION OF pitest3 FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28593 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] CREATE INDEX brinidx_multi ON brintest_multi USING brin (
	int8col int8_minmax_multi_ops,
	int2col int2_minmax_multi_ops,
	int4col int4_minmax_multi_ops,
	oidcol oid_minmax_multi_ops,
	tidcol tid_minmax_multi_ops,
	float4col float4_minmax_multi_ops,
	float8col float8_minmax_multi_ops,
	macaddrcol macaddr_minmax_multi_ops,
	macaddr8col macaddr8_minmax_multi_ops,
	inetcol inet_minmax_multi_ops,
	cidrcol inet_minmax_multi_ops,
	datecol date_minmax_multi_ops,
	timecol time_minmax_multi_ops,
	timestampcol timestamp_minmax_multi_ops,
	timestamptzcol timestamptz_minmax_multi_ops,
	intervalcol interval_minmax_multi_ops,
	timetzcol timetz_minmax_multi_ops,
	numericcol numeric_minmax_multi_ops,
	uuidcol uuid_minmax_multi_ops,
	lsncol pg_lsn_minmax_multi_ops
)
;
----------
[DROP_TABLE] drop table base_tab_def
;
----------
[ALTER_TABLE] ALTER TABLE dump_test.test_table
					   ENABLE ROW LEVEL SECURITY
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- force_not_null and force_null can be used together on the same column
ALTER FOREIGN TABLE text_csv ALTER COLUMN word1 OPTIONS (force_null 'true')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_tellers add primary key (tid) using index tablespace "regress_pgbench_tap_1_ts"
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkslotlink" BEFORE INSERT OR UPDATE ON "public"."hslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotlink"()
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27691_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE tbl1(val1 integer, val2 integer)
;
----------
[CREATE_TYPE] --
-- Test domains over multirange types
--

create domain restrictedmultirange as int4multirange check (upper(value) < 10)
;
----------
[ALTER_TABLE] -- Parallel Append with only non-partial subplans
alter table a_star set (parallel_workers = 0)
;
----------
[CREATE_TABLE] /*
 * 2. CREATE INDEX CONCURRENTLY
 */
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[DROP_PROG_OBJ] drop function lock_excl(int,int)
;
----------
[ADD_INDEX] create index parted_minmax1i on parted_minmax1 (a, b)
;
----------
[DROP_PROG_OBJ] DROP CAST (text AS casttesttype)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION daitch_mokotoff(text) RETURNS text[]
AS '$libdir/fuzzystrmatch', 'daitch_mokotoff'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CALL_PROG_OBJ] -- various error cases

CALL version()
;
