CREATE FUNCTION gbt_ts_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
-- OK with equals and &&, and equals is the partition key
create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with &&)) partition by range (a)
;
create function excpt_test3() returns void as $$
begin
    begin
        raise exception 'user exception';
    exception when others then
	    raise notice 'caught exception % %', sqlstate, sqlerrm;
	    begin
	        raise notice '% %', sqlstate, sqlerrm;
	        perform 10/0;
        exception
            when substring_error then
                -- this exception handler shouldn't be invoked
                raise notice 'unexpected exception: % %', sqlstate, sqlerrm;
	        when division_by_zero then
	            raise notice 'caught exception % %', sqlstate, sqlerrm;
	    end;
	    raise notice '% %', sqlstate, sqlerrm;
    end;
end; $$ language plpgsql
;
CLUSTER cluster_tab USING cluster_ind
;
CREATE SEQUENCE sequence_testx OWNED BY pg_class.relname
;
create view v3 as select * from tt2 join tt3 using (b,c) full join tt4 using (b)
;
create index ab_a3_b1_a_idx on ab_a3_b1 (a)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_type
;
-- fails

alter domain con add constraint t check (VALUE < 34)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
create table cc2 (f4 float) inherits (pp1,cc1)
;
DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19411 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
ANALYZE t12
;
ALTER TABLE fd_pt1 ALTER COLUMN c1 SET (n_distinct = 100)
;
CREATE TABLE "public"."itest1" (
    "a" integer NOT NULL,
    "b" "text"
)
;
drop type two_ints cascade
;
CREATE TABLE r2 (a int)
;
create function funcB() returns trigger as $$
begin
  raise notice 'hello from funcB';
  return null;
end; $$ language plpgsql
;
CREATE FUNCTION transaction_test4() RETURNS int
LANGUAGE plpgsql
AS $$
BEGIN
    EXECUTE 'DO LANGUAGE plpgsql $x$ BEGIN COMMIT; END $x$';
    RETURN 1;
END;
$$
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_largeobject_metadata
;
ANALYZE public.tableam_parted_a_heap2
;
CREATE OPERATOR ^@> (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_isparent,
        COMMUTATOR = '^<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
alter table idxpart detach partition idxpart2
;
CREATE FUNCTION g_intbig_picksplit(internal, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE "public"."skip_wal_skip_rewrite_index" (
    "c" character varying(20) NOT NULL
)
;
CREATE TABLE test3ci (x text COLLATE case_insensitive)
;
create table list_part1 partition of list_part for values in (1)
;
-- cannot drop column that is part of the partition key
ALTER TABLE partitioned DROP COLUMN a
;
ALTER TYPE "public"."insenum" ADD VALUE 'i22'
;
ALTER TABLE pg_temp_13.pg_temp_26293_2 ADD COLUMN newdata pg_temp_13.pg_temp_26293
;
CREATE FUNCTION dep_req3() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT test_s_dep.dep_req1() || ' req3' $$
;
DROP PUBLICATION testpub_foralltables
;
CREATE TABLE "generated_virtual_tests"."gtest23a" (
    "x" integer NOT NULL,
    "y" integer
)
;
ALTER TABLE regtest_table_1 RENAME TO regtest_table
;
CREATE TABLE comment_test (
  id int,
  positive_col int CHECK (positive_col > 0),
  indexed_col int,
  CONSTRAINT comment_test_pk PRIMARY KEY (id))
;
CREATE TEXT SEARCH PARSER dump_test.alt_ts_prs1
		(start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
ALTER TABLE split_alter_table.inherit_child NO INHERIT split_alter_table.inherit_parent
;
-- check display of function arguments in sub-SELECT
CREATE TABLE functest1 (i int)
;
DROP COLLATION regress_builtin_c
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest_parent" ATTACH PARTITION "generated_virtual_tests"."gtest_child2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
-- Test update tuple routing
create table utrtest (a int, b text) partition by list (a)
;
CREATE TABLE fk_partitioned_fk_6 (a int REFERENCES fk_partitioned_pk_61) PARTITION BY LIST (a)
;
CREATE TABLE "public"."part2_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
;
CREATE MATERIALIZED VIEW s4.t1_mv AS SELECT * FROM s4.t1
;
CREATE FUNCTION cube_overlap(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
VACUUM (BUFFER_USAGE_LIMIT '512 kB', FULL) vac_option_tab
;
COMMENT ON LARGE OBJECT 16561 IS 'comment on large object'
;
CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint) PARTITION BY RANGE (f1)
;
create table p2(id2 int, name text, height int)
;
alter table recur1 add column f2 recur1[]
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_ts_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_TABLE] -- OK with equals and &&, and equals is the partition key
create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with &&)) partition by range (a)
;
----------
[CREATE_PROG_OBJ] create function excpt_test3() returns void as $$
begin
    begin
        raise exception 'user exception';
    exception when others then
	    raise notice 'caught exception % %', sqlstate, sqlerrm;
	    begin
	        raise notice '% %', sqlstate, sqlerrm;
	        perform 10/0;
        exception
            when substring_error then
                -- this exception handler shouldn't be invoked
                raise notice 'unexpected exception: % %', sqlstate, sqlerrm;
	        when division_by_zero then
	            raise notice 'caught exception % %', sqlstate, sqlerrm;
	    end;
	    raise notice '% %', sqlstate, sqlerrm;
    end;
end; $$ language plpgsql
;
----------
[ADMIN_TABLE] CLUSTER cluster_tab USING cluster_ind
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_testx OWNED BY pg_class.relname
;
----------
[CREATE_VIEW(SELECT)] create view v3 as select * from tt2 join tt3 using (b,c) full join tt4 using (b)
;
----------
[ADD_INDEX] create index ab_a3_b1_a_idx on ab_a3_b1 (a)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_type
;
----------
[ALTER_TYPE] -- fails

alter domain con add constraint t check (VALUE < 34)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_TABLE] create table cc2 (f4 float) inherits (pp1,cc1)
;
----------
[BLOCK] DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19411 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
----------
[ADMIN_TABLE] ANALYZE t12
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c1 SET (n_distinct = 100)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest1" (
    "a" integer NOT NULL,
    "b" "text"
)
;
----------
[DROP_TYPE] drop type two_ints cascade
;
----------
[CREATE_TABLE] CREATE TABLE r2 (a int)
;
----------
[CREATE_PROG_OBJ] create function funcB() returns trigger as $$
begin
  raise notice 'hello from funcB';
  return null;
end; $$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION transaction_test4() RETURNS int
LANGUAGE plpgsql
AS $$
BEGIN
    EXECUTE 'DO LANGUAGE plpgsql $x$ BEGIN COMMIT; END $x$';
    RETURN 1;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_largeobject_metadata
;
----------
[ADMIN_TABLE] ANALYZE public.tableam_parted_a_heap2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^@> (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_isparent,
        COMMUTATOR = '^<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart detach partition idxpart2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_intbig_picksplit(internal, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."skip_wal_skip_rewrite_index" (
    "c" character varying(20) NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE test3ci (x text COLLATE case_insensitive)
;
----------
[CREATE_TABLE] create table list_part1 partition of list_part for values in (1)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- cannot drop column that is part of the partition key
ALTER TABLE partitioned DROP COLUMN a
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i22'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26293_2 ADD COLUMN newdata pg_temp_13.pg_temp_26293
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dep_req3() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT test_s_dep.dep_req1() || ' req3' $$
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_foralltables
;
----------
[CREATE_TABLE] CREATE TABLE "generated_virtual_tests"."gtest23a" (
    "x" integer NOT NULL,
    "y" integer
)
;
----------
[RENAME_TABLE] ALTER TABLE regtest_table_1 RENAME TO regtest_table
;
----------
[CREATE_TABLE] CREATE TABLE comment_test (
  id int,
  positive_col int CHECK (positive_col > 0),
  indexed_col int,
  CONSTRAINT comment_test_pk PRIMARY KEY (id))
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH PARSER dump_test.alt_ts_prs1
		(start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.inherit_child NO INHERIT split_alter_table.inherit_parent
;
----------
[CREATE_TABLE] -- check display of function arguments in sub-SELECT
CREATE TABLE functest1 (i int)
;
----------
[DROP_POLICY] DROP COLLATION regress_builtin_c
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "generated_virtual_tests"."gtest_parent" ATTACH PARTITION "generated_virtual_tests"."gtest_child2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
----------
[CREATE_TABLE] -- Test update tuple routing
create table utrtest (a int, b text) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_6 (a int REFERENCES fk_partitioned_pk_61) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part2_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW s4.t1_mv AS SELECT * FROM s4.t1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_overlap(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (BUFFER_USAGE_LIMIT '512 kB', FULL) vac_option_tab
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 16561 IS 'comment on large object'
;
----------
[CREATE_TABLE] CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint) PARTITION BY RANGE (f1)
;
----------
[CREATE_TABLE] create table p2(id2 int, name text, height int)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table recur1 add column f2 recur1[]
;
