COPY public.textrange_test (tr) TO stdout
;
-- Test shared invalidation
BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE
;
ALTER TABLE public.transition_table_level2_level2_no_seq OWNER TO zyc
;
SET default_table_access_method = 'btree'
;
GRANT USAGE ON FOREIGN SERVER s6 TO regress_test_role
;
ALTER VIEW public.xmlview5 OWNER TO zyc
;
-- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
ALTER DOMAIN generated_stored_tests.gtestdomain1 OWNER TO zyc
;
CREATE ROLE regress_hasprivs CREATEROLE LOGIN INHERIT CONNECTION LIMIT 5
;
LOCK TABLE fkpart6.pk IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[DATA_EXPORT] COPY public.textrange_test (tr) TO stdout
;
----------
[TRANSACTION] -- Test shared invalidation
BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.transition_table_level2_level2_no_seq OWNER TO zyc
;
----------
[SESSION_SETTING_WRITE] SET default_table_access_method = 'btree'
;
----------
[GRANT] GRANT USAGE ON FOREIGN SERVER s6 TO regress_test_role
;
----------
[TRANSFER_PRIVILEGE] ALTER VIEW public.xmlview5 OWNER TO zyc
;
----------
[SESSION_SETTING_WRITE] -- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
----------
[TRANSFER_PRIVILEGE] ALTER DOMAIN generated_stored_tests.gtestdomain1 OWNER TO zyc
;
----------
[CREATE_ROLE] CREATE ROLE regress_hasprivs CREATEROLE LOGIN INHERIT CONNECTION LIMIT 5
;
----------
[SESSION_LOCK] LOCK TABLE fkpart6.pk IN ACCESS SHARE MODE
;
