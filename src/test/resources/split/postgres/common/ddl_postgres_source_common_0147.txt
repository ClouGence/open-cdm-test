EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE TABLE selinto_schema.tbl_nodata2 (a) AS
  SELECT generate_series(1,3) WITH NO DATA
;
COMMENT ON FUNCTION cube_contained(cube, cube) IS 'contained in'
;
-- range -> jsonb
CREATE FUNCTION testRange() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = range(3)
return x
$$
;
CREATE FUNCTION ltxtq_rexec(ltxtquery, ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE pgss_schema_2.tab_search_diff_1 (a int, b int)
;
CREATE TABLE "public"."gtest22b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" / 2)) STORED NOT NULL
)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=31944 host=/tmp/vpc6RwIfhv dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
CREATE TABLE test_io_shared(a int)
;
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest4,
ptest3) REFERENCES pktable(ptest1, ptest2))
;
CREATE INDEX alter_idx_rename_test_parted_idx ON alter_idx_rename_test_parted (a)
;
CREATE INDEX no_index_cleanup_idx ON no_index_cleanup(t)
;
-- OK

-- Verify constraint deferrability when changed by ALTER
-- Partitioned table at referencing end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
;
CREATE PUBLICATION testpub_xxx WITH (publish_via_partition_root = 'true', publish_via_partition_root = '0')
;
-- defaults to text

drop function anyctest(anycompatible, anycompatible)
;
VACUUM (VERBOSE false, ANALYZE true, DISABLE_PAGE_SKIPPING true, SKIP_LOCKED true, INDEX_CLEANUP auto, PROCESS_MAIN true, PROCESS_TOAST true, TRUNCATE false, PARALLEL 0, BUFFER_USAGE_LIMIT '128 kB') split_system_admin.maint_table (payload, qty)
;
CREATE EXTENSION bloom
;
CREATE TEMP TABLE pg_temp_9.pg_temp_26996_2 (tid pg_catalog.tid)
;
CREATE FUNCTION s.index_row_if(s.citext) RETURNS bool IMMUTABLE
  LANGUAGE SQL AS $$SELECT $1 IS NOT NULL$$
;
-- infer anyrange from anymultirange
create function mr_outparam_succeed3(i anymultirange, out r anyrange, out t text)
  as $$ select range_merge($1), 'foo'::text $$ language sql
;
-- uuid check

CREATE TABLE uuidtmp (a uuid)
;
DROP TABLE ctas_ine_tbl
;
--
--
-- bytea ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_bytea_consistent(internal,bytea,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION gbt_macaddr_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE FUNCTION citext_pattern_lt( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE VIEW pg_file_settings AS
   SELECT * FROM pg_show_all_file_settings() AS A
;
-- Test altering table having publication
create schema alter1
;
CREATE PROCEDURE ptestx(a VARIADIC int[], b OUT int) LANGUAGE SQL
  AS $$ SELECT a[1] $$
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=20868 host=/tmp/fzEe_a6AjU dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
CREATE TABLE gtest_child PARTITION OF gtest_parent FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
CREATE TABLE b (i int)
;
---------------------------------------------------
-- ISSN:
CREATE FUNCTION isnlt(issn, issn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- new partition
create table pt34 partition of pt for values in (3,4)
;
alter table if exists doesnt_exist_tab rename column a to d
;
create table base (i integer)
;
-- New functions and views for pg_stat_statements in 1.11
AlTER EXTENSION pg_stat_statements UPDATE TO '1.11'
;
ALTER TABLE ONLY "public"."attach_parted" ATTACH PARTITION "public"."attach_parted_part1" FOR VALUES IN (1)
;
-- want to check updating of a domain over the target type, too
CREATE DOMAIN myvarchardom AS myvarchar
;
CREATE FUNCTION cube_send(cube)
RETURNS bytea
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create or replace function unnest1(anyarray)
returns setof anyelement as $$
select $1[s] from generate_subscripts($1,1) g(s);
$$ language sql immutable
;
/*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
CREATE TABLE test_tbl1 (x int, y test_type1)
;
CREATE TABLE foo (LIKE nonexistent)
;
CREATE POLICY measurement_p ON measurement USING (peaktemp IS NOT NULL)
;
-- to see whether it's there
ALTER TABLE onek RENAME CONSTRAINT onek_unique1_constraint TO onek_unique1_constraint_foo
;
/* contrib/btree_gin/btree_gin--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
--
--
-- enum ops
--
--


CREATE FUNCTION gin_extract_value_anyenum(anyenum, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub_mix_5, pub_mix_6
;
CREATE CAST (bigint AS "public"."int8alias1") WITHOUT FUNCTION
;
create function table_fail(i anyelement) returns table(i anyelement, r anyrange)
  as $$ select $1, '[1,10]' $$ language sql
;
ALTER TABLE pa_target FORCE ROW LEVEL SECURITY
;
-- cleanup
drop table alterlock2
;
------------------------------------------------------------------------------------------
[CREATE_TABLE|SELECT] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE TABLE selinto_schema.tbl_nodata2 (a) AS
  SELECT generate_series(1,3) WITH NO DATA
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_contained(cube, cube) IS 'contained in'
;
----------
[CREATE_PROG_OBJ] -- range -> jsonb
CREATE FUNCTION testRange() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = range(3)
return x
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltxtq_rexec(ltxtquery, ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE pgss_schema_2.tab_search_diff_1 (a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest22b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" / 2)) STORED NOT NULL
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=31944 host=/tmp/vpc6RwIfhv dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
----------
[CREATE_TABLE] CREATE TABLE test_io_shared(a int)
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest4,
ptest3) REFERENCES pktable(ptest1, ptest2))
;
----------
[ADD_INDEX] CREATE INDEX alter_idx_rename_test_parted_idx ON alter_idx_rename_test_parted (a)
;
----------
[ADD_INDEX] CREATE INDEX no_index_cleanup_idx ON no_index_cleanup(t)
;
----------
[CREATE_TABLE] -- OK

-- Verify constraint deferrability when changed by ALTER
-- Partitioned table at referencing end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_xxx WITH (publish_via_partition_root = 'true', publish_via_partition_root = '0')
;
----------
[DROP_PROG_OBJ] -- defaults to text

drop function anyctest(anycompatible, anycompatible)
;
----------
[ADMIN_TABLE] VACUUM (VERBOSE false, ANALYZE true, DISABLE_PAGE_SKIPPING true, SKIP_LOCKED true, INDEX_CLEANUP auto, PROCESS_MAIN true, PROCESS_TOAST true, TRUNCATE false, PARALLEL 0, BUFFER_USAGE_LIMIT '128 kB') split_system_admin.maint_table (payload, qty)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION bloom
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_26996_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION s.index_row_if(s.citext) RETURNS bool IMMUTABLE
  LANGUAGE SQL AS $$SELECT $1 IS NOT NULL$$
;
----------
[CREATE_PROG_OBJ] -- infer anyrange from anymultirange
create function mr_outparam_succeed3(i anymultirange, out r anyrange, out t text)
  as $$ select range_merge($1), 'foo'::text $$ language sql
;
----------
[CREATE_TABLE] -- uuid check

CREATE TABLE uuidtmp (a uuid)
;
----------
[DROP_TABLE] DROP TABLE ctas_ine_tbl
;
----------
[CREATE_PROG_OBJ] --
--
-- bytea ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_bytea_consistent(internal,bytea,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macaddr_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_pattern_lt( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_file_settings AS
   SELECT * FROM pg_show_all_file_settings() AS A
;
----------
[CREATE_SCHEMA] -- Test altering table having publication
create schema alter1
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptestx(a VARIADIC int[], b OUT int) LANGUAGE SQL
  AS $$ SELECT a[1] $$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=20868 host=/tmp/fzEe_a6AjU dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child PARTITION OF gtest_parent FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[CREATE_TABLE] CREATE TABLE b (i int)
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISSN:
CREATE FUNCTION isnlt(issn, issn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- new partition
create table pt34 partition of pt for values in (3,4)
;
----------
[RENAME_COLUMN] alter table if exists doesnt_exist_tab rename column a to d
;
----------
[CREATE_TABLE] create table base (i integer)
;
----------
[ALTER_LIBRARY] -- New functions and views for pg_stat_statements in 1.11
AlTER EXTENSION pg_stat_statements UPDATE TO '1.11'
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."attach_parted" ATTACH PARTITION "public"."attach_parted_part1" FOR VALUES IN (1)
;
----------
[CREATE_TYPE] -- want to check updating of a domain over the target type, too
CREATE DOMAIN myvarchardom AS myvarchar
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_send(cube)
RETURNS bytea
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function unnest1(anyarray)
returns setof anyelement as $$
select $1[s] from generate_subscripts($1,1) g(s);
$$ language sql immutable
;
----------
[CREATE_POLICY] /*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
----------
[CREATE_TABLE] CREATE TABLE test_tbl1 (x int, y test_type1)
;
----------
[CREATE_TABLE] CREATE TABLE foo (LIKE nonexistent)
;
----------
[CREATE_POLICY] CREATE POLICY measurement_p ON measurement USING (peaktemp IS NOT NULL)
;
----------
[RENAME_CONSTRAINT] -- to see whether it's there
ALTER TABLE onek RENAME CONSTRAINT onek_unique1_constraint TO onek_unique1_constraint_foo
;
----------
[CREATE_PROG_OBJ] /* contrib/btree_gin/btree_gin--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
--
--
-- enum ops
--
--


CREATE FUNCTION gin_extract_value_anyenum(anyenum, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub_mix_5, pub_mix_6
;
----------
[CREATE_PROG_OBJ] CREATE CAST (bigint AS "public"."int8alias1") WITHOUT FUNCTION
;
----------
[CREATE_PROG_OBJ] create function table_fail(i anyelement) returns table(i anyelement, r anyrange)
  as $$ select $1, '[1,10]' $$ language sql
;
----------
[ALTER_TABLE] ALTER TABLE pa_target FORCE ROW LEVEL SECURITY
;
----------
[DROP_TABLE] -- cleanup
drop table alterlock2
;
