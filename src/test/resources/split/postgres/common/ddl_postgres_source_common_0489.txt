CREATE TABLE num_exp_log10 (id int4, expected numeric(1000,800))
;
-- Create the operator class
CREATE OPERATOR CLASS gist_macaddr_ops
DEFAULT FOR TYPE macaddr USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_macad_consistent (internal, macaddr, int2, oid, internal),
	FUNCTION	2	gbt_macad_union (internal, internal),
	FUNCTION	3	gbt_macad_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_macad_penalty (internal, internal, internal),
	FUNCTION	6	gbt_macad_picksplit (internal, internal),
	FUNCTION	7	gbt_macad_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
-- Try (and succeed) and repeat to show it works on already valid constraint
ALTER TABLE attmp3 validate constraint attmpconstr
;
DROP TABLE testpub_tbl8
;
ALTER TABLE testpub_tbl8 ATTACH PARTITION testpub_tbl8_0 FOR VALUES WITH (modulus 2, remainder 0)
;
ALTER INDEX "public"."idxpart3_a_idx" ATTACH PARTITION "public"."idxpart31_a_idx"
;
CREATE FUNCTION "public"."namelen"("public"."person_type") RETURNS integer
    LANGUAGE "sql"
    AS $_$ SELECT length($1.name) $_$
;
-- Implicit coercions in a JOIN USING create issues similar to FULL JOIN

create table tt7a (x date, xx int, y int)
;
CREATE TABLE split_builtin_trigger.messages_by_name (
    id integer PRIMARY KEY,
    title text,
    body text,
    document tsvector
)
;
CREATE OPERATOR <| (
        PROCEDURE = int8lt,
        LEFTARG = bigint,
        RIGHTARG = bigint
)
;
-- fail

DROP SCHEMA ext_cine_schema
;
create table j1 (id1 int, id2 int, primary key(id1,id2))
;
DROP TABLE ref_table
;
CREATE FUNCTION dblink_error_message(text)
RETURNS text
AS '$libdir/dblink', 'dblink_error_message'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
create table alter1.t1(f1 serial primary key, f2 int check (f2 > 0))
;
CREATE OPERATOR <-> (
	LEFTARG = time,
	RIGHTARG = time,
	PROCEDURE = time_dist,
	COMMUTATOR = '<->'
)
;
create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with =)) partition by range (a, b)
;
/* Base view for user mappings */
CREATE VIEW _pg_user_mappings AS
    SELECT um.oid,
           um.umoptions,
           um.umuser,
           CAST(COALESCE(u.rolname,'PUBLIC') AS sql_identifier ) AS authorization_identifier,
           s.foreign_server_catalog,
           s.foreign_server_name,
           s.authorization_identifier AS srvowner
    FROM pg_user_mapping um LEFT JOIN pg_authid u ON (u.oid = um.umuser),
         _pg_foreign_servers s
    WHERE s.oid = um.umserver
;
ALTER OPERATOR FAMILY gist_oid_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
alter table defaulttest alter column col5 drop default
;
-- Ensure we don't use a partial index as proof of functional dependency
drop index t2_z_uidx
;
CREATE PUBLICATION tap_pub FOR ALL TABLES
;
ALTER INDEX "fkpart3"."pk5_pkey" ATTACH PARTITION "fkpart3"."pk52_pkey"
;
CALL ptest3('b')
;
CREATE TABLE part_c PARTITION OF parted (b WITH OPTIONS NOT NULL DEFAULT 0) FOR VALUES IN ('c') PARTITION BY RANGE ((b))
;
ANALYZE public.gtestxx_3
;
VACUUM (BUFFER_USAGE_LIMIT 120) vac_option_tab
;
CREATE TABLE test6 (a int, b text)
;
discard temp
;
CREATE INDEX reloptions_test_idx ON reloptions_test (s)
	WITH (not_existing_option=2)
;
CREATE TABLE testtab7 (a int)
;
CREATE FUNCTION isnge(ean13, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP INDEX inet_idx3
;
CREATE TABLE "public"."mlparted12" (
    "b" integer NOT NULL,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
CREATE PUBLICATION tap_pub_B FOR TABLE "tab'le"
;
CREATE FOREIGN TABLE t1 (a int) SERVER s1
;
create table t (a int, b int, c int)
;
DROP TABLE pg_temp_52.pg_temp_29691_2, pg_temp_52.pg_temp_29691
;
CREATE TEMP TABLE pg_temp_88.pg_temp_30263_2 (tid pg_catalog.tid)
;
ALTER TEXT SEARCH CONFIGURATION russian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH english_stem
;
ALTER TABLE ONLY "fkpart5"."fk" ATTACH PARTITION "fkpart5"."fk1" FOR VALUES IN (1)
;
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE OR REPLACE FUNCTION test_slru_page_write(int, text) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_write' LANGUAGE C
;
-- failed (name conflict)

-- invalid: non-lowercase quoted identifiers
CREATE TEXT SEARCH PARSER tspars_case ("Start" = start_function)
;
ANALYZE public.tbl_include_reg
;
ALTER TABLE tmp1 RENAME TO tx1
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13501 host=/tmp/W3c8xszmEE dbname=postgres' PUBLICATION tap_pub
;
create table constr_child2 () inherits (constr_parent2)
;
CREATE OPERATOR CLASS gist_float4_ops
DEFAULT FOR TYPE float4 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_float4_consistent (internal, float4, int2, oid, internal),
	FUNCTION	2	gbt_float4_union (internal, internal),
	FUNCTION	3	gbt_float4_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_float4_penalty (internal, internal, internal),
	FUNCTION	6	gbt_float4_picksplit (internal, internal),
	FUNCTION	7	gbt_float4_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
CREATE RULE european_city_insert_rule AS ON INSERT TO european_city_view
DO INSTEAD INSERT INTO city_view
VALUES (NEW.city_id, NEW.city_name, NEW.population, NEW.country_name, NEW.continent)
RETURNING *
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE num_exp_log10 (id int4, expected numeric(1000,800))
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_macaddr_ops
DEFAULT FOR TYPE macaddr USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_macad_consistent (internal, macaddr, int2, oid, internal),
	FUNCTION	2	gbt_macad_union (internal, internal),
	FUNCTION	3	gbt_macad_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_macad_penalty (internal, internal, internal),
	FUNCTION	6	gbt_macad_picksplit (internal, internal),
	FUNCTION	7	gbt_macad_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- Try (and succeed) and repeat to show it works on already valid constraint
ALTER TABLE attmp3 validate constraint attmpconstr
;
----------
[DROP_TABLE] DROP TABLE testpub_tbl8
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE testpub_tbl8 ATTACH PARTITION testpub_tbl8_0 FOR VALUES WITH (modulus 2, remainder 0)
;
----------
[ALTER_INDEX] ALTER INDEX "public"."idxpart3_a_idx" ATTACH PARTITION "public"."idxpart31_a_idx"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."namelen"("public"."person_type") RETURNS integer
    LANGUAGE "sql"
    AS $_$ SELECT length($1.name) $_$
;
----------
[CREATE_TABLE] -- Implicit coercions in a JOIN USING create issues similar to FULL JOIN

create table tt7a (x date, xx int, y int)
;
----------
[CREATE_TABLE] CREATE TABLE split_builtin_trigger.messages_by_name (
    id integer PRIMARY KEY,
    title text,
    body text,
    document tsvector
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <| (
        PROCEDURE = int8lt,
        LEFTARG = bigint,
        RIGHTARG = bigint
)
;
----------
[DROP_SCHEMA] -- fail

DROP SCHEMA ext_cine_schema
;
----------
[CREATE_TABLE] create table j1 (id1 int, id2 int, primary key(id1,id2))
;
----------
[DROP_TABLE] DROP TABLE ref_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_error_message(text)
RETURNS text
AS '$libdir/dblink', 'dblink_error_message'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] create table alter1.t1(f1 serial primary key, f2 int check (f2 > 0))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = time,
	RIGHTARG = time,
	PROCEDURE = time_dist,
	COMMUTATOR = '<->'
)
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with =)) partition by range (a, b)
;
----------
[CREATE_VIEW(SELECT)] /* Base view for user mappings */
CREATE VIEW _pg_user_mappings AS
    SELECT um.oid,
           um.umoptions,
           um.umuser,
           CAST(COALESCE(u.rolname,'PUBLIC') AS sql_identifier ) AS authorization_identifier,
           s.foreign_server_catalog,
           s.foreign_server_name,
           s.authorization_identifier AS srvowner
    FROM pg_user_mapping um LEFT JOIN pg_authid u ON (u.oid = um.umuser),
         _pg_foreign_servers s
    WHERE s.oid = um.umserver
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_oid_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table defaulttest alter column col5 drop default
;
----------
[DROP_INDEX] -- Ensure we don't use a partial index as proof of functional dependency
drop index t2_z_uidx
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub FOR ALL TABLES
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart3"."pk5_pkey" ATTACH PARTITION "fkpart3"."pk52_pkey"
;
----------
[CALL_PROG_OBJ] CALL ptest3('b')
;
----------
[CREATE_TABLE] CREATE TABLE part_c PARTITION OF parted (b WITH OPTIONS NOT NULL DEFAULT 0) FOR VALUES IN ('c') PARTITION BY RANGE ((b))
;
----------
[ADMIN_TABLE] ANALYZE public.gtestxx_3
;
----------
[ADMIN_TABLE] VACUUM (BUFFER_USAGE_LIMIT 120) vac_option_tab
;
----------
[CREATE_TABLE] CREATE TABLE test6 (a int, b text)
;
----------
[DROP_TABLE] discard temp
;
----------
[ADD_INDEX] CREATE INDEX reloptions_test_idx ON reloptions_test (s)
	WITH (not_existing_option=2)
;
----------
[CREATE_TABLE] CREATE TABLE testtab7 (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ean13, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_INDEX] DROP INDEX inet_idx3
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted12" (
    "b" integer NOT NULL,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_B FOR TABLE "tab'le"
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t1 (a int) SERVER s1
;
----------
[CREATE_TABLE] create table t (a int, b int, c int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_52.pg_temp_29691_2, pg_temp_52.pg_temp_29691
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_88.pg_temp_30263_2 (tid pg_catalog.tid)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION russian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH english_stem
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart5"."fk" ATTACH PARTITION "fkpart5"."fk1" FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] -- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE OR REPLACE FUNCTION test_slru_page_write(int, text) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_write' LANGUAGE C
;
----------
[CREATE_POLICY] -- failed (name conflict)

-- invalid: non-lowercase quoted identifiers
CREATE TEXT SEARCH PARSER tspars_case ("Start" = start_function)
;
----------
[ADMIN_TABLE] ANALYZE public.tbl_include_reg
;
----------
[RENAME_TABLE] ALTER TABLE tmp1 RENAME TO tx1
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13501 host=/tmp/W3c8xszmEE dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_TABLE] create table constr_child2 () inherits (constr_parent2)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_float4_ops
DEFAULT FOR TYPE float4 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_float4_consistent (internal, float4, int2, oid, internal),
	FUNCTION	2	gbt_float4_union (internal, internal),
	FUNCTION	3	gbt_float4_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_float4_penalty (internal, internal, internal),
	FUNCTION	6	gbt_float4_picksplit (internal, internal),
	FUNCTION	7	gbt_float4_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
----------
[CREATE_POLICY] CREATE RULE european_city_insert_rule AS ON INSERT TO european_city_view
DO INSTEAD INSERT INTO city_view
VALUES (NEW.city_id, NEW.city_name, NEW.population, NEW.country_name, NEW.continent)
RETURNING *
;
