create function testpolym(a int) returns table(a int) as $$ select $1;$$ language sql
;
-- should fail
create function constant_refcursor() returns refcursor as $$
declare
    rc constant refcursor;
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26410_2 (tid pg_catalog.tid)
;
-- check that an existing table can be attached as a default partition
CREATE TABLE def_part (LIKE list_parted INCLUDING CONSTRAINTS)
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "hword_asciipart" WITH "public"."hunspell_num", "english_stem"
;
create type lockmodes as enum (
 'SIReadLock'
,'AccessShareLock'
,'RowShareLock'
,'RowExclusiveLock'
,'ShareUpdateExclusiveLock'
,'ShareLock'
,'ShareRowExclusiveLock'
,'ExclusiveLock'
,'AccessExclusiveLock'
)
;
CREATE SEQUENCE "public"."sequence_test9"
    AS smallint
    START WITH -1
    INCREMENT BY -1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
-- regression test: verify that multiple uses of same plpgsql datum within
-- a SQL command all get mapped to the same $n parameter.  The return value
-- of the SELECT is not important, we only care that it doesn't fail with
-- a complaint about an ungrouped column reference.
create function multi_datum_use(p1 int) returns bool as $$
declare
  x int;
  y int;
begin
  select into x,y unique1/p1, unique1/$1 from tenk1 group by unique1/p1;
  return x = y;
end$$ language plpgsql
;
-- some quick and dirty field extraction functions

-- this is actually timestamp concatenated with clock sequence, per RFC 4122
CREATE FUNCTION uuid_timestamp_bits(uuid) RETURNS varbit AS
$$ SELECT ('x' || substr($1::text, 15, 4) || substr($1::text, 10, 4) ||
           substr($1::text, 1, 8) || substr($1::text, 20, 4))::bit(80)
          & x'0FFFFFFFFFFFFFFF3FFF' $$
LANGUAGE SQL STRICT IMMUTABLE
;
ALTER INDEX dummy_test_idx SET (option_bool = true)
;
create unique index on covidxpart (b) include (a)
;
CREATE TYPE gbtreekey_var (
	INTERNALLENGTH = VARIABLE,
	INPUT  = gbtreekey_var_in,
	OUTPUT = gbtreekey_var_out,
	STORAGE = EXTENDED
)
;
create temp table temp_parted_oncommit (a int)
  partition by list (a) on commit delete rows
;
ALTER TABLE ONLY test_add_column
	ADD COLUMN c2 integer
;
CREATE TABLE gtestnn_childdef PARTITION OF gtestnn_parent default
;
CREATE TABLE bt_txt_heap (
	seqno 		text,
	random 		int4
)
;
CREATE TABLE quuux1 (a int, b text)
;
ALTER TABLE T ADD COLUMN c_hugetext TEXT DEFAULT repeat('abcdefg',1000),
              ALTER COLUMN c_interval SET DEFAULT '3 hours'
;
CREATE FUNCTION gbt_intv_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
SECURITY LABEL ON FUNCTION f2()
    IS 'system_u:object_r:sepgsql_trusted_proc_exec_t:s0'
;
CREATE FUNCTION gbt_macad_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE test1 (a int) TABLESPACE tblspc1
;
create trigger trans_tab_parent_insert_trig
  after insert on trans_tab_parent referencing new table as new_table
  for each statement execute procedure dump_insert()
;
create table utrtest (a int, b text) partition by list (a)
;
ALTER TABLE vtype ADD COLUMN c BOOLEAN DEFAULT true
;
-- drop the tablespace so we can re-use the location
DROP TABLESPACE regress_tblspacewith
;
CREATE TABLE replication_example(id SERIAL PRIMARY KEY, somedata int, text varchar(120))
;
CREATE FUNCTION "public"."trigger_return_old"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'trigger_return_old'
;
-- not supported yet
ALTER TABLE regtest_table ALTER b SET NOT NULL
;
-- index cleanup option is ignored if no indexes
VACUUM (INDEX_CLEANUP FALSE, FREEZE TRUE) vaccluster
;
create table rtest_empmass (ename char(20), salary numeric)
;
CREATE TABLE part_b_1_b_10 PARTITION OF range_parted FOR VALUES FROM ('b', 1) TO ('b', 10)
;
ALTER TABLE pg_temp_13.pg_temp_26383_2 ADD COLUMN newdata pg_temp_13.pg_temp_26383
;
/* contrib/ltree/ltree--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION ltree_in(cstring)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
--
-- Test self-referencing foreign key with partition.
-- This should create only one fk constraint per partition
--
CREATE TABLE parted_self_fk (
    id bigint NOT NULL PRIMARY KEY,
    id_abc bigint,
    FOREIGN KEY (id_abc) REFERENCES parted_self_fk(id)
)
PARTITION BY RANGE (id)
;
ALTER TABLE prt2_adv ATTACH PARTITION prt2_adv_p3 FOR VALUES FROM (350) TO (500)
;
DROP TABLE toasttable
;
ALTER OPERATOR ?| (hstore, text[])
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
create view v_test1
as select 'v_'||t from test1
;
CREATE TEMP TABLE sales (product_id int, units int)
;
create function void_return_expr() returns void as $$
begin
    return 5;
end;$$ language plpgsql
;
/*
 * text search configuration for italian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * italian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY italian_stem
	(TEMPLATE = snowball, Language = italian , StopWords=italian)
;
VACUUM (BUFFER_USAGE_LIMIT 16777220) vac_option_tab
;
drop table j2
;
-- Test table partition trees
CREATE TABLE ptif_test (a int, b int) PARTITION BY range (a)
;
CREATE VIEW collview1 AS SELECT * FROM collate_test1 WHERE b COLLATE "C" >= 'bbc'
;
create table ma_test_p1 partition of ma_test for values from (0) to (10)
;
CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((gtest_parent))
;
CREATE TABLE "public"."rtest_person" (
    "pname" "text",
    "pdesc" "text"
)
;
CREATE TABLE schema_one.table_three(a int)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create function testpolym(a int) returns table(a int) as $$ select $1;$$ language sql
;
----------
[CREATE_PROG_OBJ] -- should fail
create function constant_refcursor() returns refcursor as $$
declare
    rc constant refcursor;
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26410_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] -- check that an existing table can be attached as a default partition
CREATE TABLE def_part (LIKE list_parted INCLUDING CONSTRAINTS)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "hword_asciipart" WITH "public"."hunspell_num", "english_stem"
;
----------
[CREATE_TYPE] create type lockmodes as enum (
 'SIReadLock'
,'AccessShareLock'
,'RowShareLock'
,'RowExclusiveLock'
,'ShareUpdateExclusiveLock'
,'ShareLock'
,'ShareRowExclusiveLock'
,'ExclusiveLock'
,'AccessExclusiveLock'
)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test9"
    AS smallint
    START WITH -1
    INCREMENT BY -1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_PROG_OBJ] -- regression test: verify that multiple uses of same plpgsql datum within
-- a SQL command all get mapped to the same $n parameter.  The return value
-- of the SELECT is not important, we only care that it doesn't fail with
-- a complaint about an ungrouped column reference.
create function multi_datum_use(p1 int) returns bool as $$
declare
  x int;
  y int;
begin
  select into x,y unique1/p1, unique1/$1 from tenk1 group by unique1/p1;
  return x = y;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] -- some quick and dirty field extraction functions

-- this is actually timestamp concatenated with clock sequence, per RFC 4122
CREATE FUNCTION uuid_timestamp_bits(uuid) RETURNS varbit AS
$$ SELECT ('x' || substr($1::text, 15, 4) || substr($1::text, 10, 4) ||
           substr($1::text, 1, 8) || substr($1::text, 20, 4))::bit(80)
          & x'0FFFFFFFFFFFFFFF3FFF' $$
LANGUAGE SQL STRICT IMMUTABLE
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_bool = true)
;
----------
[ADD_INDEX] create unique index on covidxpart (b) include (a)
;
----------
[CREATE_TYPE] CREATE TYPE gbtreekey_var (
	INTERNALLENGTH = VARIABLE,
	INPUT  = gbtreekey_var_in,
	OUTPUT = gbtreekey_var_out,
	STORAGE = EXTENDED
)
;
----------
[CREATE_TABLE] create temp table temp_parted_oncommit (a int)
  partition by list (a) on commit delete rows
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE ONLY test_add_column
	ADD COLUMN c2 integer
;
----------
[CREATE_TABLE] CREATE TABLE gtestnn_childdef PARTITION OF gtestnn_parent default
;
----------
[CREATE_TABLE] CREATE TABLE bt_txt_heap (
	seqno 		text,
	random 		int4
)
;
----------
[CREATE_TABLE] CREATE TABLE quuux1 (a int, b text)
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_hugetext TEXT DEFAULT repeat('abcdefg',1000),
              ALTER COLUMN c_interval SET DEFAULT '3 hours'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_intv_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[SECURITY_LABEL] SECURITY LABEL ON FUNCTION f2()
    IS 'system_u:object_r:sepgsql_trusted_proc_exec_t:s0'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE test1 (a int) TABLESPACE tblspc1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trans_tab_parent_insert_trig
  after insert on trans_tab_parent referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[CREATE_TABLE] create table utrtest (a int, b text) partition by list (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE vtype ADD COLUMN c BOOLEAN DEFAULT true
;
----------
[DROP_TABLESPACE] -- drop the tablespace so we can re-use the location
DROP TABLESPACE regress_tblspacewith
;
----------
[CREATE_TABLE] CREATE TABLE replication_example(id SERIAL PRIMARY KEY, somedata int, text varchar(120))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."trigger_return_old"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'trigger_return_old'
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- not supported yet
ALTER TABLE regtest_table ALTER b SET NOT NULL
;
----------
[ADMIN_TABLE] -- index cleanup option is ignored if no indexes
VACUUM (INDEX_CLEANUP FALSE, FREEZE TRUE) vaccluster
;
----------
[CREATE_TABLE] create table rtest_empmass (ename char(20), salary numeric)
;
----------
[CREATE_TABLE] CREATE TABLE part_b_1_b_10 PARTITION OF range_parted FOR VALUES FROM ('b', 1) TO ('b', 10)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26383_2 ADD COLUMN newdata pg_temp_13.pg_temp_26383
;
----------
[CREATE_PROG_OBJ] /* contrib/ltree/ltree--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION ltree_in(cstring)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] --
-- Test self-referencing foreign key with partition.
-- This should create only one fk constraint per partition
--
CREATE TABLE parted_self_fk (
    id bigint NOT NULL PRIMARY KEY,
    id_abc bigint,
    FOREIGN KEY (id_abc) REFERENCES parted_self_fk(id)
)
PARTITION BY RANGE (id)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt2_adv ATTACH PARTITION prt2_adv_p3 FOR VALUES FROM (350) TO (500)
;
----------
[DROP_TABLE] DROP TABLE toasttable
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ?| (hstore, text[])
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_VIEW(SELECT)] create view v_test1
as select 'v_'||t from test1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE sales (product_id int, units int)
;
----------
[CREATE_PROG_OBJ] create function void_return_expr() returns void as $$
begin
    return 5;
end;$$ language plpgsql
;
----------
[CREATE_POLICY] /*
 * text search configuration for italian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * italian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY italian_stem
	(TEMPLATE = snowball, Language = italian , StopWords=italian)
;
----------
[ADMIN_TABLE] VACUUM (BUFFER_USAGE_LIMIT 16777220) vac_option_tab
;
----------
[DROP_TABLE] drop table j2
;
----------
[CREATE_TABLE] -- Test table partition trees
CREATE TABLE ptif_test (a int, b int) PARTITION BY range (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW collview1 AS SELECT * FROM collate_test1 WHERE b COLLATE "C" >= 'bbc'
;
----------
[CREATE_TABLE] create table ma_test_p1 partition of ma_test for values from (0) to (10)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((gtest_parent))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_person" (
    "pname" "text",
    "pdesc" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE schema_one.table_three(a int)
;
