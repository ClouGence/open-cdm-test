UPDATE temporal_partitioned_rng SET valid_at = daterange('2016-01-01', '2016-02-01')
  WHERE id = '[5,6)' AND valid_at = daterange('2018-01-01', '2018-02-01')
;
INSERT INTO test (a) VALUES (('{' || 'vvvvvvvvvv' || random_string(1900) ||'}')::text[])
;
INSERT INTO public.ths_p1 VALUES (6, 486)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15682', relminmxid = '3'
WHERE oid = '"regress_indexing"."pk4"'::pg_catalog.regclass
;
INSERT INTO t2 VALUES (1, 'xxx'), (2, 'yyy'), (3, 'zzz')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1075', relminmxid = '1'
WHERE oid = '"public"."test_range_gist"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('2', 152)
;
INSERT INTO savepoints VALUES (8)
;
DELETE FROM ft2 WHERE c1 = 1200 RETURNING tableoid::regclass
;
UPDATE tbl_gist SET c1 = 1 WHERE c1 = 3
;
INSERT INTO CIRCLE_TBL VALUES ('(3,(1,2),3)')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt2_l_p2"'::pg_catalog.regclass
;
UPDATE "public"."fk_partitioned_fk" SET "a" = $1, "b" = $2 WHERE $3 OPERATOR(pg_catalog.=) "a" AND $4 OPERATOR(pg_catalog.=) "b"
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '21303'
;
delete from ppk1 where a = 1
;
update part_def set a = 'd' where a = 'c'
;
INSERT INTO macaddr8_data VALUES (16, '08-00-2b-01-02-03-04-05')
;
insert into insertconflicttest values (6, 'Passionfruit') on conflict (lower(fruit)) do update set fruit = excluded.fruit
;
INSERT INTO pg_temp_11.pg_temp_41126_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_41126 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_11.pg_temp_41126 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO fk_partitioned_fk_1 (a,b) VALUES (500, 501)
;
INSERT INTO parted_self_fk VALUES (1, NULL), (2, NULL), (3, NULL)
;
WITH deleted AS (DELETE FROM schema12.counted WHERE type = 'delta' RETURNING value), total AS (SELECT coalesce(sum(value), 0) as sum FROM deleted) UPDATE schema12.counted SET value = counted.value + total.sum FROM total WHERE type = 'total' RETURNING counted.value
;
------------------------------------------------------------------------------------------
[UPDATE] UPDATE temporal_partitioned_rng SET valid_at = daterange('2016-01-01', '2016-02-01')
  WHERE id = '[5,6)' AND valid_at = daterange('2018-01-01', '2018-02-01')
;
----------
[INSERT|SELECT] INSERT INTO test (a) VALUES (('{' || 'vvvvvvvvvv' || random_string(1900) ||'}')::text[])
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 486)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15682', relminmxid = '3'
WHERE oid = '"regress_indexing"."pk4"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO t2 VALUES (1, 'xxx'), (2, 'yyy'), (3, 'zzz')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1075', relminmxid = '1'
WHERE oid = '"public"."test_range_gist"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 152)
;
----------
[INSERT] INSERT INTO savepoints VALUES (8)
;
----------
[DELETE] DELETE FROM ft2 WHERE c1 = 1200 RETURNING tableoid::regclass
;
----------
[UPDATE] UPDATE tbl_gist SET c1 = 1 WHERE c1 = 3
;
----------
[INSERT] INSERT INTO CIRCLE_TBL VALUES ('(3,(1,2),3)')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt2_l_p2"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE "public"."fk_partitioned_fk" SET "a" = $1, "b" = $2 WHERE $3 OPERATOR(pg_catalog.=) "a" AND $4 OPERATOR(pg_catalog.=) "b"
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '21303'
;
----------
[DELETE] delete from ppk1 where a = 1
;
----------
[UPDATE] update part_def set a = 'd' where a = 'c'
;
----------
[INSERT] INSERT INTO macaddr8_data VALUES (16, '08-00-2b-01-02-03-04-05')
;
----------
[MERGE] insert into insertconflicttest values (6, 'Passionfruit') on conflict (lower(fruit)) do update set fruit = excluded.fruit
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_11.pg_temp_41126_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_41126 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_11.pg_temp_41126 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT|SELECT] INSERT INTO fk_partitioned_fk_1 (a,b) VALUES (500, 501)
;
----------
[INSERT] INSERT INTO parted_self_fk VALUES (1, NULL), (2, NULL), (3, NULL)
;
----------
[UPDATE|DELETE|SELECT] WITH deleted AS (DELETE FROM schema12.counted WHERE type = 'delta' RETURNING value), total AS (SELECT coalesce(sum(value), 0) as sum FROM deleted) UPDATE schema12.counted SET value = counted.value + total.sum FROM total WHERE type = 'total' RETURNING counted.value
;
