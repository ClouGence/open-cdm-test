LOCK TABLE "public"."itest7c" IN ACCESS SHARE MODE
;
CREATE SERVER loopback FOREIGN DATA WRAPPER postgres_fdw
                OPTIONS (dbname 'isolation_regression',
                         port '64487',
                         use_remote_estimate 'true'
                )
;
-- OK
ALTER CONVERSION alt_conv2 OWNER TO regress_alter_generic_user2
;
-- Check when the hooks fire relative to dependency based abort of a drop
DROP ROLE regress_role_joe
;
REVOKE EXECUTE ON FUNCTION pg_ls_replslotdir(text) FROM PUBLIC
;
-- Try with no matching partitions.
explain (analyze, costs off, summary off, timing off)  execute q1 (0,0)
;
SET DateStyle TO 'European,ISO'
;
-- DROP test table
SET pg_stat_statements.track_utility = TRUE
;
-- Produce results with sorting.
SET enable_hashagg TO OFF
;
EXECUTE dumpCompositeType('33765')
;
------------------------------------------------------------------------------------------
[SESSION_LOCK] LOCK TABLE "public"."itest7c" IN ACCESS SHARE MODE
;
----------
[SYSTEM_SETTING_WRITE] CREATE SERVER loopback FOREIGN DATA WRAPPER postgres_fdw
                OPTIONS (dbname 'isolation_regression',
                         port '64487',
                         use_remote_estimate 'true'
                )
;
----------
[TRANSFER_PRIVILEGE] -- OK
ALTER CONVERSION alt_conv2 OWNER TO regress_alter_generic_user2
;
----------
[DROP_ROLE] -- Check when the hooks fire relative to dependency based abort of a drop
DROP ROLE regress_role_joe
;
----------
[REVOKE] REVOKE EXECUTE ON FUNCTION pg_ls_replslotdir(text) FROM PUBLIC
;
----------
[UNSAFE] -- Try with no matching partitions.
explain (analyze, costs off, summary off, timing off)  execute q1 (0,0)
;
----------
[SESSION_SETTING_WRITE] SET DateStyle TO 'European,ISO'
;
----------
[SESSION_SETTING_WRITE] -- DROP test table
SET pg_stat_statements.track_utility = TRUE
;
----------
[SESSION_SETTING_WRITE] -- Produce results with sorting.
SET enable_hashagg TO OFF
;
----------
[UNSAFE] EXECUTE dumpCompositeType('33765')
;
