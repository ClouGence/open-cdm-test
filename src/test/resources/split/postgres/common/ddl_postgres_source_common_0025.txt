VACUUM (ANALYZE) information_schema.sql_features
;
CREATE FUNCTION hashean13(ean13)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal' IMMUTABLE STRICT
	PARALLEL SAFE
;
-- check reverse-listing of named-arg calls
CREATE VIEW dfview AS
   SELECT q1, q2,
     dfunc(q1,q2, flag := q1>q2) as c3,
     dfunc(q1, flag := q1<q2, b := q2) as c4
     FROM int8_tbl
;
create table lp_default partition of lp default
;
--fail
DROP POLICY p1 ON document
;
CREATE EXTENSION hstore_plpython2u CASCADE
;
create table hp_prefix_test_p2 partition of hp_prefix_test for values with (modulus 8, remainder 2)
;
TRUNCATE temporal_mltrng3
;
CREATE TABLE test6a OF priv_testtype1
;
CREATE TABLE test_squash_jsonb (id int, data jsonb)
;
create rule rtest_v1_upd as on update to rtest_v1 do instead
	update rtest_t1 set a = new.a, b = new.b
	where a = old.a
;
CREATE CAST (int4 AS casttesttype) WITH FUNCTION bar_int4_text(int4) AS IMPLICIT
;
ALTER INDEX pg_toast.pg_toast_28046_index RENAME TO reind_con_toast_idx
;
ALTER TABLE s.x ADD CONSTRAINT e2rows EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll WITH s.=)
  WHERE (s.index_row_if(y))
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) public.vactable(c)
;
create domain domainchar4arr varchar(4)[2][3]
;
CREATE FUNCTION seg_inter(seg, seg)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
VACUUM (BUFFER_USAGE_LIMIT '512 kB') vac_option_tab
;
DROP TABLE part_at2tmp
;
ALTER TABLE regress_tblspace_test_tbl SET TABLESPACE pg_default
;
CREATE TABLE prt1_l_p3 PARTITION OF prt1_l FOR VALUES FROM (500) TO (600) PARTITION BY RANGE (b)
;
CREATE PUBLICATION pub
;
create table partitioned (
	a intdom1,
	b text
) partition by range (plusone(a))
;
ALTER TABLE ONLY "public"."transition_table_level2"
    ADD CONSTRAINT "transition_table_level2_pkey" PRIMARY KEY ("level2_no")
;
-- and rules work
create temp table tt_log(f1 int, data text)
;
CREATE TABLE part5_p1 (LIKE part_5)
;
CREATE OPERATOR #@%# (
   leftarg = int8,		
   procedure = factorial,
   invalid_att = int8
)
;
ALTER FOREIGN TABLE agg_bad OPTIONS (ADD reject_limit '1')
;
-- test use of SQL functions returning record
-- this is supported in some cases where the query doesn't specify
-- the actual record type ...

create function array_to_set(anyarray) returns setof record as $$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$$ language sql strict immutable
;
-- no table rewrite
ALTER TABLE t2 ADD COLUMN b domain1 default 3
;
drop function set_and_report_role()
;
create function exit_error2() returns void as $$
begin
    begin
        loop
            exit no_such_label;
        end loop;
    end;
end;
$$ language plpgsql
;
ALTER FUNCTION gbt_int8_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
CREATE FUNCTION chksetconfig()
 RETURNS TABLE (db name, "role" name, rolkeyword text, setconfig text[])
 AS $$
SELECT COALESCE(d.datname, 'ALL'), COALESCE(r.rolname, 'ALL'),
       COALESCE(v.keyword, '-'), s.setconfig
 FROM pg_db_role_setting s
 LEFT JOIN pg_roles r ON (r.oid = s.setrole)
 LEFT JOIN pg_database d ON (d.oid = s.setdatabase)
 LEFT JOIN (VALUES(CURRENT_ROLE, 'current_role'),
                  (CURRENT_USER, 'current_user'),
                  (SESSION_USER, 'session_user'))
      AS v(uname, keyword)
      ON (r.rolname = v.uname)
   WHERE (r.rolname) IN ('Public', 'current_user', 'regress_testrol1', 'regress_testrol2')
ORDER BY 1, 2, 3;
$$ LANGUAGE SQL
;
VACUUM (SKIP_LOCKED) information_schema.sql_languages
;
CREATE TABLE s.x (y s.citext)
;
-- another kind of polymorphic aggregate

create function add_group(grp anyarray, ad anyelement, size integer)
  returns anyarray
  as $$
begin
  if grp is null then
    return array[ad];
  end if;
  if array_upper(grp, 1) < size then
    return grp || ad;
  end if;
  return grp;
end;
$$
  language plpgsql immutable
;
CREATE PUBLICATION tap_pub_copy FOR TABLE tab_copy
;
create table part_40_inf_cd partition of part_40_inf for values in ('cd')
;
ALTER TABLE pg_ts_dict ADD PRIMARY KEY USING INDEX pg_ts_dict_oid_index
;
CREATE TABLE "public"."alpha_pos_p1" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
ANALYZE public.bar2
;
CREATE TABLE sql_parts (
    feature_id      character_data,
    feature_name    character_data,
    is_supported    yes_or_no,
    is_verified_by  character_data,
    comments        character_data
)
;
/* contrib/file_fdw/file_fdw--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION file_fdw_handler()
RETURNS fdw_handler
AS '$libdir/file_fdw'
LANGUAGE C STRICT
;
create temp table inner_text (c1 text, c2 text)
;
ALTER TABLE pg_auth_members ADD PRIMARY KEY USING INDEX pg_auth_members_role_member_index
;
ALTER TABLE owned_by_me ATTACH PARTITION not_owned_by_me FOR VALUES IN (1)
;
CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=29891 host=/tmp/7Wj1Y48wnU dbname=postgres' PUBLICATION tap_pub_B_2 WITH (origin = none, copy_data = on)
;
CREATE PROCEDURE transaction_test6(c text)
LANGUAGE plpgsql
AS $$
BEGIN
    CALL transaction_test1(9, c);
END;
$$
;
ALTER TABLE pg_toast.pg_toast_30609 RENAME TO reind_con_toast
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (ANALYZE) information_schema.sql_features
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hashean13(ean13)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal' IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] -- check reverse-listing of named-arg calls
CREATE VIEW dfview AS
   SELECT q1, q2,
     dfunc(q1,q2, flag := q1>q2) as c3,
     dfunc(q1, flag := q1<q2, b := q2) as c4
     FROM int8_tbl
;
----------
[CREATE_TABLE] create table lp_default partition of lp default
;
----------
[DROP_POLICY] --fail
DROP POLICY p1 ON document
;
----------
[CREATE_LIBRARY] CREATE EXTENSION hstore_plpython2u CASCADE
;
----------
[CREATE_TABLE] create table hp_prefix_test_p2 partition of hp_prefix_test for values with (modulus 8, remainder 2)
;
----------
[TRUNCATE_TABLE] TRUNCATE temporal_mltrng3
;
----------
[CREATE_TABLE] CREATE TABLE test6a OF priv_testtype1
;
----------
[CREATE_TABLE] CREATE TABLE test_squash_jsonb (id int, data jsonb)
;
----------
[CREATE_POLICY] create rule rtest_v1_upd as on update to rtest_v1 do instead
	update rtest_t1 set a = new.a, b = new.b
	where a = old.a
;
----------
[CREATE_PROG_OBJ] CREATE CAST (int4 AS casttesttype) WITH FUNCTION bar_int4_text(int4) AS IMPLICIT
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28046_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE s.x ADD CONSTRAINT e2rows EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll WITH s.=)
  WHERE (s.index_row_if(y))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) public.vactable(c)
;
----------
[CREATE_TYPE] create domain domainchar4arr varchar(4)[2][3]
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_inter(seg, seg)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (BUFFER_USAGE_LIMIT '512 kB') vac_option_tab
;
----------
[DROP_TABLE] DROP TABLE part_at2tmp
;
----------
[ALTER_TABLE] ALTER TABLE regress_tblspace_test_tbl SET TABLESPACE pg_default
;
----------
[CREATE_TABLE] CREATE TABLE prt1_l_p3 PARTITION OF prt1_l FOR VALUES FROM (500) TO (600) PARTITION BY RANGE (b)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub
;
----------
[CREATE_TABLE] create table partitioned (
	a intdom1,
	b text
) partition by range (plusone(a))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."transition_table_level2"
    ADD CONSTRAINT "transition_table_level2_pkey" PRIMARY KEY ("level2_no")
;
----------
[CREATE_TABLE] -- and rules work
create temp table tt_log(f1 int, data text)
;
----------
[CREATE_TABLE] CREATE TABLE part5_p1 (LIKE part_5)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #@%# (
   leftarg = int8,		
   procedure = factorial,
   invalid_att = int8
)
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE agg_bad OPTIONS (ADD reject_limit '1')
;
----------
[CREATE_PROG_OBJ] -- test use of SQL functions returning record
-- this is supported in some cases where the query doesn't specify
-- the actual record type ...

create function array_to_set(anyarray) returns setof record as $$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$$ language sql strict immutable
;
----------
[ALTER_TABLE|ADD_COLUMN] -- no table rewrite
ALTER TABLE t2 ADD COLUMN b domain1 default 3
;
----------
[DROP_PROG_OBJ] drop function set_and_report_role()
;
----------
[CREATE_PROG_OBJ] create function exit_error2() returns void as $$
begin
    begin
        loop
            exit no_such_label;
        end loop;
    end;
end;
$$ language plpgsql
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int8_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION chksetconfig()
 RETURNS TABLE (db name, "role" name, rolkeyword text, setconfig text[])
 AS $$
SELECT COALESCE(d.datname, 'ALL'), COALESCE(r.rolname, 'ALL'),
       COALESCE(v.keyword, '-'), s.setconfig
 FROM pg_db_role_setting s
 LEFT JOIN pg_roles r ON (r.oid = s.setrole)
 LEFT JOIN pg_database d ON (d.oid = s.setdatabase)
 LEFT JOIN (VALUES(CURRENT_ROLE, 'current_role'),
                  (CURRENT_USER, 'current_user'),
                  (SESSION_USER, 'session_user'))
      AS v(uname, keyword)
      ON (r.rolname = v.uname)
   WHERE (r.rolname) IN ('Public', 'current_user', 'regress_testrol1', 'regress_testrol2')
ORDER BY 1, 2, 3;
$$ LANGUAGE SQL
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) information_schema.sql_languages
;
----------
[CREATE_TABLE] CREATE TABLE s.x (y s.citext)
;
----------
[CREATE_PROG_OBJ] -- another kind of polymorphic aggregate

create function add_group(grp anyarray, ad anyelement, size integer)
  returns anyarray
  as $$
begin
  if grp is null then
    return array[ad];
  end if;
  if array_upper(grp, 1) < size then
    return grp || ad;
  end if;
  return grp;
end;
$$
  language plpgsql immutable
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_copy FOR TABLE tab_copy
;
----------
[CREATE_TABLE] create table part_40_inf_cd partition of part_40_inf for values in ('cd')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_dict ADD PRIMARY KEY USING INDEX pg_ts_dict_oid_index
;
----------
[CREATE_TABLE] CREATE TABLE "public"."alpha_pos_p1" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[ADMIN_TABLE] ANALYZE public.bar2
;
----------
[CREATE_TABLE] CREATE TABLE sql_parts (
    feature_id      character_data,
    feature_name    character_data,
    is_supported    yes_or_no,
    is_verified_by  character_data,
    comments        character_data
)
;
----------
[CREATE_PROG_OBJ] /* contrib/file_fdw/file_fdw--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION file_fdw_handler()
RETURNS fdw_handler
AS '$libdir/file_fdw'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] create temp table inner_text (c1 text, c2 text)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_auth_members ADD PRIMARY KEY USING INDEX pg_auth_members_role_member_index
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE owned_by_me ATTACH PARTITION not_owned_by_me FOR VALUES IN (1)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=29891 host=/tmp/7Wj1Y48wnU dbname=postgres' PUBLICATION tap_pub_B_2 WITH (origin = none, copy_data = on)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test6(c text)
LANGUAGE plpgsql
AS $$
BEGIN
    CALL transaction_test1(9, c);
END;
$$
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30609 RENAME TO reind_con_toast
;
