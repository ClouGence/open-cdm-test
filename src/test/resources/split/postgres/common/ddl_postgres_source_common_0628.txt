CREATE INDEX macaddr8_data_btree ON macaddr8_data USING btree (b)
;
ANALYZE public.fk_partitioned_fk_5_1
;
CREATE VIEW bv1 WITH (security_barrier) AS SELECT * FROM b1 WHERE a > 0 WITH CHECK OPTION
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '26882'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
CREATE FUNCTION "public"."f_leak"("text") RETURNS boolean
    LANGUAGE "plpgsql" COST 1e-07
    AS $_$BEGIN RAISE NOTICE 'f_leak => %', $1; RETURN true; END$_$
;
ALTER TEXT SEARCH CONFIGURATION hungarian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
create function test_event_trigger_sql() returns event_trigger as $$
SELECT 1 $$ language sql
;
ALTER TABLE pg_temp_18.pg_temp_25732_2 ADD COLUMN newdata pg_temp_18.pg_temp_25732
;
alter table trigger_parted_p2_2 enable always trigger aft_row
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_description
;
-- examples of building custom type-specific crosstab functions:
CREATE TYPE tablefunc_crosstab_2 AS
(
	row_name TEXT,
	category_1 TEXT,
	category_2 TEXT
)
;
ANALYZE pg_temp_3.pg_temp_16652_2
;
DROP PUBLICATION testpub_forparted, testpub_forparted1
;
-- but we can rename the subscription we just created
ALTER SUBSCRIPTION regress_testsub RENAME TO regress_testsub2
;
drop table table2
;
ALTER INDEX pg_toast.pg_toast_30741_index RENAME TO reind_con_toast_idx
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_constraint
;
DROP CAST IF EXISTS (no_such_schema.foo AS INTEGER)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD c11 integer
;
CREATE TABLE country_table (
    country_id        serial primary key,
    country_name    text unique not null,
    continent        text not null
)
;
ANALYZE prt2_m
;
VACUUM (FREEZE) pg_catalog.pg_statistic_ext_data
;
CREATE TABLE itest5 (a int generated always as identity, b text)
;
ALTER TABLE pg_toast.pg_toast_28117 RENAME TO reind_con_toast
;
-- Test index-only scan with point opclass
create index gist_tbl_point_index on gist_tbl using gist (p)
;
-- fail, expression index
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_expr
;
ALTER INDEX pg_toast.pg_toast_30558_index RENAME TO reind_con_toast_idx
;
CREATE PROCEDURE cursor_fail_during_commit()
 LANGUAGE plpgsql
AS $$
  DECLARE id int;
  BEGIN
    FOR id IN SELECT 1/(x-1000) FROM generate_series(1,1000) x LOOP
        INSERT INTO test1 VALUES(id);
        COMMIT;
    END LOOP;
  END;
$$
;
CREATE INDEX idx_date ON test_date USING gin (i)
;
CREATE FUNCTION test_dsa_allocate(int, int, int)
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE FUNCTION gbt_text_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create function my_trigger_function() returns trigger as $$ begin end; $$ language plpgsql
;
ANALYZE public.rtest_t4
;
CREATE FUNCTION ismn13(ean13)
RETURNS ismn13
AS '$libdir/isn', 'ismn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE UNIQUE INDEX u2rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  WHERE s.index_row_if(y)
;
CREATE TABLE vacowned_part1 PARTITION OF vacowned_parted FOR VALUES IN (1)
;
/* contrib/btree_gist/btree_gist--1.6--1.7.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- This upgrade scripts add support for bool.
CREATE FUNCTION gbtreekey2_in(cstring)
RETURNS gbtreekey2
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
--
-- Assorted tests for array subscript assignment
--

create temp table rtype (id int, ar text[])
;
CREATE MATERIALIZED VIEW compressmv(x) AS SELECT * FROM cmdata1
;
VACUUM (ANALYZE) pg_catalog.pg_rewrite
;
ALTER OPERATOR FAMILY gist_cash_ops USING gist ADD
	OPERATOR	6	<> (money, money) ,
	OPERATOR	15	<-> (money, money) FOR ORDER BY pg_catalog.money_ops ,
	FUNCTION	8 (money, money) gbt_cash_distance (internal, money, int2, oid, internal) ,
	FUNCTION	9 (money, money) gbt_cash_fetch (internal)
;
CREATE FUNCTION tp_ins_data() RETURNS void LANGUAGE plpgsql AS $$
  BEGIN
	INSERT INTO truncprim VALUES (1), (100), (150);
	INSERT INTO truncpart VALUES (1), (100), (150);
  END
$$
;
ALTER OPERATOR >= (isbn13, isbn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
CREATE TABLE fkpart7.fk (a int REFERENCES fkpart7.pkpart)
;
TRUNCATE trunc_trig
;
ALTER TABLE ONLY "public"."dropcolumnanother" DROP COLUMN "........pg.dropped.4........"
;
-- Restore plt2_adv_p2
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p2 FOR VALUES IN ('0004', '0006')
;
CREATE TABLE tab_rule_stats (a int, b int)
;
-- Check handling of indexes with expressions and predicates.  The
-- definitions of the rebuilt indexes should match the original
-- definitions.
CREATE TABLE concur_exprs_tab (c1 int , c2 boolean)
;
DO $$
DECLARE
  v_Text text;
  v_cnt  integer := 42;
BEGIN
  CALL p1(v_cnt := v_cnt);  -- error, must supply something for v_Text
  RAISE NOTICE '%', v_Text;
END;
$$
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX macaddr8_data_btree ON macaddr8_data USING btree (b)
;
----------
[ADMIN_TABLE] ANALYZE public.fk_partitioned_fk_5_1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW bv1 WITH (security_barrier) AS SELECT * FROM b1 WHERE a > 0 WITH CHECK OPTION
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '26882'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."f_leak"("text") RETURNS boolean
    LANGUAGE "plpgsql" COST 1e-07
    AS $_$BEGIN RAISE NOTICE 'f_leak => %', $1; RETURN true; END$_$
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION hungarian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_PROG_OBJ] create function test_event_trigger_sql() returns event_trigger as $$
SELECT 1 $$ language sql
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_18.pg_temp_25732_2 ADD COLUMN newdata pg_temp_18.pg_temp_25732
;
----------
[ALTER_TABLE] alter table trigger_parted_p2_2 enable always trigger aft_row
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_description
;
----------
[CREATE_TYPE] -- examples of building custom type-specific crosstab functions:
CREATE TYPE tablefunc_crosstab_2 AS
(
	row_name TEXT,
	category_1 TEXT,
	category_2 TEXT
)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_16652_2
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_forparted, testpub_forparted1
;
----------
[ALTER_PUB_SUB] -- but we can rename the subscription we just created
ALTER SUBSCRIPTION regress_testsub RENAME TO regress_testsub2
;
----------
[DROP_TABLE] drop table table2
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30741_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_constraint
;
----------
[DROP_PROG_OBJ] DROP CAST IF EXISTS (no_such_schema.foo AS INTEGER)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD c11 integer
;
----------
[CREATE_TABLE] CREATE TABLE country_table (
    country_id        serial primary key,
    country_name    text unique not null,
    continent        text not null
)
;
----------
[ADMIN_TABLE] ANALYZE prt2_m
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_TABLE] CREATE TABLE itest5 (a int generated always as identity, b text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28117 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] -- Test index-only scan with point opclass
create index gist_tbl_point_index on gist_tbl using gist (p)
;
----------
[ALTER_TABLE] -- fail, expression index
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_expr
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30558_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE cursor_fail_during_commit()
 LANGUAGE plpgsql
AS $$
  DECLARE id int;
  BEGIN
    FOR id IN SELECT 1/(x-1000) FROM generate_series(1,1000) x LOOP
        INSERT INTO test1 VALUES(id);
        COMMIT;
    END LOOP;
  END;
$$
;
----------
[ADD_INDEX] CREATE INDEX idx_date ON test_date USING gin (i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_dsa_allocate(int, int, int)
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_text_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] create function my_trigger_function() returns trigger as $$ begin end; $$ language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_t4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ismn13(ean13)
RETURNS ismn13
AS '$libdir/isn', 'ismn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX u2rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  WHERE s.index_row_if(y)
;
----------
[CREATE_TABLE] CREATE TABLE vacowned_part1 PARTITION OF vacowned_parted FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] /* contrib/btree_gist/btree_gist--1.6--1.7.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- This upgrade scripts add support for bool.
CREATE FUNCTION gbtreekey2_in(cstring)
RETURNS gbtreekey2
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] --
-- Assorted tests for array subscript assignment
--

create temp table rtype (id int, ar text[])
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW compressmv(x) AS SELECT * FROM cmdata1
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_rewrite
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_cash_ops USING gist ADD
	OPERATOR	6	<> (money, money) ,
	OPERATOR	15	<-> (money, money) FOR ORDER BY pg_catalog.money_ops ,
	FUNCTION	8 (money, money) gbt_cash_distance (internal, money, int2, oid, internal) ,
	FUNCTION	9 (money, money) gbt_cash_fetch (internal)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tp_ins_data() RETURNS void LANGUAGE plpgsql AS $$
  BEGIN
	INSERT INTO truncprim VALUES (1), (100), (150);
	INSERT INTO truncpart VALUES (1), (100), (150);
  END
$$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (isbn13, isbn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE fkpart7.fk (a int REFERENCES fkpart7.pkpart)
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_trig
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."dropcolumnanother" DROP COLUMN "........pg.dropped.4........"
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Restore plt2_adv_p2
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p2 FOR VALUES IN ('0004', '0006')
;
----------
[CREATE_TABLE] CREATE TABLE tab_rule_stats (a int, b int)
;
----------
[CREATE_TABLE] -- Check handling of indexes with expressions and predicates.  The
-- definitions of the rebuilt indexes should match the original
-- definitions.
CREATE TABLE concur_exprs_tab (c1 int , c2 boolean)
;
----------
[BLOCK] DO $$
DECLARE
  v_Text text;
  v_cnt  integer := 42;
BEGIN
  CALL p1(v_cnt := v_cnt);  -- error, must supply something for v_Text
  RAISE NOTICE '%', v_Text;
END;
$$
;
