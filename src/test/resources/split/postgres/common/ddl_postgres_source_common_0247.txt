create table attach_parted_part1 partition of attach_parted for values in (1)
;
-- fail
ALTER COLLATION test1 RENAME TO test22
;
-- fail - cannot set PUBLISH_VIA_PARTITION_ROOT to false if any column list is
-- used for partitioned table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=0)
;
CREATE FUNCTION uuid_reserved_bits(uuid) RETURNS varbit AS
$$ SELECT ('x' || substr($1::text, 20, 2))::bit(8) & '11000000' $$
LANGUAGE SQL STRICT IMMUTABLE
;
CREATE SCHEMA sch1
;
CREATE TABLE "public"."xmltest" (
    "id" integer,
    "data" "xml"
)
;
CREATE INDEX ON foo(a)
;
-- multi-argument aggregate
create function sum3(int8,int8,int8) returns int8 as
'select $1 + $2 + $3' language sql strict immutable
;
-- fails
alter table recur1 add column f2 int
;
drop foreign table remp
;
CREATE TABLE pk1 PARTITION OF pk FOR VALUES FROM (1) TO (100) PARTITION BY RANGE (a)
;
create trigger trg1 after insert on trigpart for each row execute procedure trigger_nothing()
;
CREATE OR REPLACE FUNCTION
  jsonb_set(jsonb_in jsonb, path text[] , replacement jsonb,
            create_if_missing boolean DEFAULT true)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_set'
;
CREATE TABLE tbl(i int, j jsonb)
;
CREATE TABLE "public"."gtest2" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULL::integer) STORED
)
;
-- 1 2 3

create table test3 (c int)
;
ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = NONE)
;
CREATE TABLE itest3 (a smallint generated by default as identity (start with 7 increment by 5), b text)
;
ALTER SUBSCRIPTION sub2 SET PUBLICATION pub_all, pub_lower_level
;
-- Multiple filters
CREATE UNIQUE INDEX sj_temp_idx1 ON sj(a,b,c)
;
-- should fail here.

--
-- test FK referenced updates NO ACTION
--

TRUNCATE temporal_rng, temporal_fk_rng2rng
;
--
-- Test GIN indexes.
--
-- There are other tests to test different GIN opclasses. This is for testing
-- GIN itself.

-- Create and populate a test table with a GIN index.
create table gin_test_tbl(i int4[]) with (autovacuum_enabled = off)
;
CREATE TABLE orderstest (
    approver_ref integer,
    po_ref integer,
    ordercanceled boolean
)
;
ALTER INDEX pg_toast.pg_toast_33078_index RENAME TO reind_con_toast_idx
;
CREATE FOREIGN TABLE table30000 ( x int ) SERVER fetch101 OPTIONS ( fetch_size '30000' )
;
drop domain mytext cascade
;
drop function anycompatiblearray_anycompatiblemultirange_func(anycompatiblearray, anycompatiblemultirange)
;
CREATE TEMP TABLE pg_temp_9.pg_temp_26555_2 (tid pg_catalog.tid)
;
--- test transforming from perl

CREATE FUNCTION perl2int(int) RETURNS bool
LANGUAGE plperlu
TRANSFORM FOR TYPE bool
AS $$
return shift;
$$
;
CREATE TABLE guid2
(
	guid_field UUID,
	text_field TEXT DEFAULT(now())
)
;
CREATE TABLE tab4_1_1 PARTITION OF tab4_1 FOR VALUES IN (-1, 0, 1)
;
--
-- Test that we can drop a not-yet-fired deferred trigger
--

create table refd_table (id int primary key)
;
CREATE TRIGGER base_table_bi BEFORE INSERT ON split_alter_table.base_table FOR EACH ROW EXECUTE FUNCTION split_alter_table.touch_payload()
;
/*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
CREATE DATABASE db2 WITH TABLESPACE ts1
;
create index j2_id1_idx on j2 (id1) where id1 % 1000 = 1
;
DROP FUNCTION test_text_to_bytea
;
CREATE TABLE inh_error2 (LIKE ctlt4 INCLUDING STORAGE) INHERITS (ctlt1)
;
CREATE FUNCTION isngt(ean13, issn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE VIEW "public"."nums" AS
 WITH RECURSIVE "nums"("n") AS (
         VALUES (1)
        UNION ALL
         SELECT ("nums_1"."n" + 1)
           FROM "nums" "nums_1"
          WHERE ("nums_1"."n" < 6)
        )
 SELECT "n"
   FROM "nums"
;
CREATE PROCEDURE p1(v_cnt int, v_Text inout text = NULL)
AS $$
BEGIN
  v_Text := 'v_cnt = ' || v_cnt;
END
$$ LANGUAGE plpgsql
;
-- test handling of infinite interval values
CREATE TABLE brin_interval_test(a INTERVAL)
;
ALTER TABLE measurement ENABLE ROW LEVEL SECURITY
;
CREATE SEQUENCE s1.s1
;
CREATE TABLE tab_upgraded2(id int)
;
create index on idxpart1 ((a + 0))
;
-- Also check LIKE optimization with binary-compatible cases

create temp table btree_bpchar (f1 text collate "C")
;
ALTER TABLE temporal_fk_mltrng2mltrng
    ALTER CONSTRAINT temporal_fk_mltrng2mltrng_fk
    DEFERRABLE INITIALLY DEFERRED
;
CREATE TABLESPACE regress_ts1 LOCATION '/tmp/F0LBN5oH3w'
;
/* src/test/modules/test_custom_types/test_custom_types--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- Input/output functions for int_custom type
--
CREATE FUNCTION int_custom_in(cstring)
RETURNS int_custom
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table attach_parted_part1 partition of attach_parted for values in (1)
;
----------
[ALTER_POLICY] -- fail
ALTER COLLATION test1 RENAME TO test22
;
----------
[ALTER_PUB_SUB] -- fail - cannot set PUBLISH_VIA_PARTITION_ROOT to false if any column list is
-- used for partitioned table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION uuid_reserved_bits(uuid) RETURNS varbit AS
$$ SELECT ('x' || substr($1::text, 20, 2))::bit(8) & '11000000' $$
LANGUAGE SQL STRICT IMMUTABLE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA sch1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."xmltest" (
    "id" integer,
    "data" "xml"
)
;
----------
[ADD_INDEX] CREATE INDEX ON foo(a)
;
----------
[CREATE_PROG_OBJ] -- multi-argument aggregate
create function sum3(int8,int8,int8) returns int8 as
'select $1 + $2 + $3' language sql strict immutable
;
----------
[ALTER_TABLE|ADD_COLUMN] -- fails
alter table recur1 add column f2 int
;
----------
[DROP_TABLE] drop foreign table remp
;
----------
[CREATE_TABLE] CREATE TABLE pk1 PARTITION OF pk FOR VALUES FROM (1) TO (100) PARTITION BY RANGE (a)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trg1 after insert on trigpart for each row execute procedure trigger_nothing()
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_set(jsonb_in jsonb, path text[] , replacement jsonb,
            create_if_missing boolean DEFAULT true)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_set'
;
----------
[CREATE_TABLE] CREATE TABLE tbl(i int, j jsonb)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest2" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULL::integer) STORED
)
;
----------
[CREATE_TABLE] -- 1 2 3

create table test3 (c int)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = NONE)
;
----------
[CREATE_TABLE] CREATE TABLE itest3 (a smallint generated by default as identity (start with 7 increment by 5), b text)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION sub2 SET PUBLICATION pub_all, pub_lower_level
;
----------
[ADD_INDEX] -- Multiple filters
CREATE UNIQUE INDEX sj_temp_idx1 ON sj(a,b,c)
;
----------
[TRUNCATE_TABLE] -- should fail here.

--
-- test FK referenced updates NO ACTION
--

TRUNCATE temporal_rng, temporal_fk_rng2rng
;
----------
[CREATE_TABLE] --
-- Test GIN indexes.
--
-- There are other tests to test different GIN opclasses. This is for testing
-- GIN itself.

-- Create and populate a test table with a GIN index.
create table gin_test_tbl(i int4[]) with (autovacuum_enabled = off)
;
----------
[CREATE_TABLE] CREATE TABLE orderstest (
    approver_ref integer,
    po_ref integer,
    ordercanceled boolean
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33078_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE table30000 ( x int ) SERVER fetch101 OPTIONS ( fetch_size '30000' )
;
----------
[DROP_TYPE] drop domain mytext cascade
;
----------
[DROP_PROG_OBJ] drop function anycompatiblearray_anycompatiblemultirange_func(anycompatiblearray, anycompatiblemultirange)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_26555_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] --- test transforming from perl

CREATE FUNCTION perl2int(int) RETURNS bool
LANGUAGE plperlu
TRANSFORM FOR TYPE bool
AS $$
return shift;
$$
;
----------
[CREATE_TABLE] CREATE TABLE guid2
(
	guid_field UUID,
	text_field TEXT DEFAULT(now())
)
;
----------
[CREATE_TABLE] CREATE TABLE tab4_1_1 PARTITION OF tab4_1 FOR VALUES IN (-1, 0, 1)
;
----------
[CREATE_TABLE] --
-- Test that we can drop a not-yet-fired deferred trigger
--

create table refd_table (id int primary key)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER base_table_bi BEFORE INSERT ON split_alter_table.base_table FOR EACH ROW EXECUTE FUNCTION split_alter_table.touch_payload()
;
----------
[CREATE_POLICY] /*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
----------
[CREATE_CATALOG] CREATE DATABASE db2 WITH TABLESPACE ts1
;
----------
[ADD_INDEX] create index j2_id1_idx on j2 (id1) where id1 % 1000 = 1
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_text_to_bytea
;
----------
[CREATE_TABLE] CREATE TABLE inh_error2 (LIKE ctlt4 INCLUDING STORAGE) INHERITS (ctlt1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ean13, issn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."nums" AS
 WITH RECURSIVE "nums"("n") AS (
         VALUES (1)
        UNION ALL
         SELECT ("nums_1"."n" + 1)
           FROM "nums" "nums_1"
          WHERE ("nums_1"."n" < 6)
        )
 SELECT "n"
   FROM "nums"
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE p1(v_cnt int, v_Text inout text = NULL)
AS $$
BEGIN
  v_Text := 'v_cnt = ' || v_cnt;
END
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] -- test handling of infinite interval values
CREATE TABLE brin_interval_test(a INTERVAL)
;
----------
[ALTER_TABLE] ALTER TABLE measurement ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE s1.s1
;
----------
[CREATE_TABLE] CREATE TABLE tab_upgraded2(id int)
;
----------
[ADD_INDEX] create index on idxpart1 ((a + 0))
;
----------
[CREATE_TABLE] -- Also check LIKE optimization with binary-compatible cases

create temp table btree_bpchar (f1 text collate "C")
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE temporal_fk_mltrng2mltrng
    ALTER CONSTRAINT temporal_fk_mltrng2mltrng_fk
    DEFERRABLE INITIALLY DEFERRED
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_ts1 LOCATION '/tmp/F0LBN5oH3w'
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_custom_types/test_custom_types--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- Input/output functions for int_custom type
--
CREATE FUNCTION int_custom_in(cstring)
RETURNS int_custom
AS '$libdir/test_custom_types'
LANGUAGE C IMMUTABLE STRICT
;
