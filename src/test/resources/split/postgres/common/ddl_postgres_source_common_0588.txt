CREATE SUBSCRIPTION tap_sub CONNECTION 'port=25797 host=/tmp/REnOPq3M6y dbname=postgres' PUBLICATION tap_pub
;
alter table inh_child3 inherit inh_parent
;
CREATE SCHEMA test_s_dep2
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=17017 host=/tmp/VJnliAaCVw dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
create index patest2i on patest2(id)
;
--
-- PGP Public Key Encryption
--

-- As most of the low-level stuff is tested in symmetric key
-- tests, here's only public-key specific tests

create table keytbl (
	id int4,
	name text,
	pubkey text,
	seckey text
)
;
comment on column table_name.column1 is 'ss'
;
DROP TABLE droppk_d
;
CREATE TEXT SEARCH DICTIONARY hunspell_invalid_2 (
						Template=ispell,
						DictFile=hunspell_sample_long,
						AffFile=hunspell_sample_num
)
;
COMMENT ON FUNCTION cube_le(cube, cube) IS 'lower than or equal to'
;
CREATE TABLE beforepromotion_tab (a int)
;
CREATE SEQUENCE functest1
;
DROP SUBSCRIPTION tap_sub_temp1
;
CREATE FUNCTION fn_opf13  (int4) RETURNS BIGINT AS 'SELECT NULL::BIGINT;' LANGUAGE SQL
;
CREATE TABLE concur_reindex_part (c1 int, c2 int) PARTITION BY RANGE (c1)
;
CREATE TABLE split_typeconv.mytable (
    val split_typeconv.mytext
)
;
-- useful in some tests below
create temp table onerow()
;
DROP VIEW atestv4
;
CREATE FUNCTION "public"."sro_ifun"(integer) RETURNS integer
    LANGUAGE "plpgsql" IMMUTABLE
    AS $_$
BEGIN
	-- Below we set the table's owner to regress_sro_user
	ASSERT current_user = 'regress_sro_user',
		format('sro_ifun(%s) called by %s', $1, current_user);
	RETURN $1;
END;
$_$
;
ALTER TYPE basket DROP ATTRIBUTE mangos
;
create function sp_add_user(a_login text) returns int as $$
declare my_id_user int;
begin
  my_id_user = sp_id_user( a_login );
  IF  my_id_user > 0 THEN
    RETURN -1;  -- error code for existing user
  END IF;
  INSERT INTO users ( login ) VALUES ( a_login );
  my_id_user = sp_id_user( a_login );
  IF  my_id_user = 0 THEN
    RETURN -2;  -- error code for insertion failure
  END IF;
  RETURN my_id_user;
end$$ language plpgsql
;
CREATE FUNCTION "public"."plpgsql_arr_domain_check"("val" integer[]) RETURNS boolean
    LANGUAGE "plpgsql" IMMUTABLE
    AS $$
begin return val[1] > 0; end
$$
;
CREATE FUNCTION gin_extract_query_cidr(cidr, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
create function raise_test1(int) returns int as $$
begin
    raise notice 'This message has too many parameters!', $1;
    return $1;
end;
$$ language plpgsql
;
CREATE TABLE "public"."rewritemetoo1" (
    "a" "text"
)
;
--
-- Test inheritance features
--
CREATE TABLE a (aa TEXT)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_auth_members
;
CREATE TABLE t111 () INHERITS (t11, t12)
;
ALTER FUNCTION gbt_enum_consistent(internal, anyenum, smallint, oid, internal) PARALLEL SAFE
;
CREATE TABLE "public"."tab_upgraded1" (
    "id" integer
)
;
CREATE OPERATOR < (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = >=,
    COMMUTATOR = >,
    PROCEDURE  = citext_lt,
    RESTRICT   = scalarltsel,
    JOIN       = scalarltjoinsel
)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_shseclabel
;
CREATE TYPE myvarchar
;
ALTER TABLE plt2 DETACH PARTITION plt2_p3
;
alter table returningwrtest attach partition returningwrtest2 for values in (2)
;
DROP TYPE test_type1
;
ALTER TABLE pg_toast.pg_toast_28303 RENAME TO reind_con_toast
;
CREATE TABLE tab_rowfilter_viaroot_part_1 (a int)
;
DROP TRIGGER IF EXISTS accounts_insert_stmt ON split_trigger_probe.accounts CASCADE
;
ALTER INDEX pg_toast.pg_toast_28435_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION tconvert(text,text)
RETURNS hstore
AS '$libdir/hstore','hstore_from_text'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
create table permtest_child (b text, c text, a int) partition by list (b)
;
create table idxpart (a int4range, b int4range) partition by range (a)
;
CREATE VIEW ro_view5 AS SELECT a, rank() OVER() FROM base_tbl
;
CREATE TABLE itest_err_4 (a serial generated by default as identity)
;
DROP TEXT SEARCH DICTIONARY test_tsdict_exists
;
-- Check use of a whole-row variable for an un-flattenable view
CREATE TEMP VIEW foo_v AS SELECT * FROM foo OFFSET 0
;
ALTER RULE account_update_notify ON split_trigger_probe.account_audit RENAME TO account_update_notify_renamed
;
ANALYZE public.rules_log
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=25797 host=/tmp/REnOPq3M6y dbname=postgres' PUBLICATION tap_pub
;
----------
[ALTER_TABLE] alter table inh_child3 inherit inh_parent
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_s_dep2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=17017 host=/tmp/VJnliAaCVw dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[ADD_INDEX] create index patest2i on patest2(id)
;
----------
[CREATE_TABLE] --
-- PGP Public Key Encryption
--

-- As most of the low-level stuff is tested in symmetric key
-- tests, here's only public-key specific tests

create table keytbl (
	id int4,
	name text,
	pubkey text,
	seckey text
)
;
----------
[COMMENT_COLUMN] comment on column table_name.column1 is 'ss'
;
----------
[DROP_TABLE] DROP TABLE droppk_d
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hunspell_invalid_2 (
						Template=ispell,
						DictFile=hunspell_sample_long,
						AffFile=hunspell_sample_num
)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_le(cube, cube) IS 'lower than or equal to'
;
----------
[CREATE_TABLE] CREATE TABLE beforepromotion_tab (a int)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE functest1
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION tap_sub_temp1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn_opf13  (int4) RETURNS BIGINT AS 'SELECT NULL::BIGINT;' LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE concur_reindex_part (c1 int, c2 int) PARTITION BY RANGE (c1)
;
----------
[CREATE_TABLE] CREATE TABLE split_typeconv.mytable (
    val split_typeconv.mytext
)
;
----------
[CREATE_TABLE] -- useful in some tests below
create temp table onerow()
;
----------
[DROP_VIEW] DROP VIEW atestv4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."sro_ifun"(integer) RETURNS integer
    LANGUAGE "plpgsql" IMMUTABLE
    AS $_$
BEGIN
	-- Below we set the table's owner to regress_sro_user
	ASSERT current_user = 'regress_sro_user',
		format('sro_ifun(%s) called by %s', $1, current_user);
	RETURN $1;
END;
$_$
;
----------
[ALTER_TYPE] ALTER TYPE basket DROP ATTRIBUTE mangos
;
----------
[CREATE_PROG_OBJ] create function sp_add_user(a_login text) returns int as $$
declare my_id_user int;
begin
  my_id_user = sp_id_user( a_login );
  IF  my_id_user > 0 THEN
    RETURN -1;  -- error code for existing user
  END IF;
  INSERT INTO users ( login ) VALUES ( a_login );
  my_id_user = sp_id_user( a_login );
  IF  my_id_user = 0 THEN
    RETURN -2;  -- error code for insertion failure
  END IF;
  RETURN my_id_user;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."plpgsql_arr_domain_check"("val" integer[]) RETURNS boolean
    LANGUAGE "plpgsql" IMMUTABLE
    AS $$
begin return val[1] > 0; end
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_cidr(cidr, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] create function raise_test1(int) returns int as $$
begin
    raise notice 'This message has too many parameters!', $1;
    return $1;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rewritemetoo1" (
    "a" "text"
)
;
----------
[CREATE_TABLE] --
-- Test inheritance features
--
CREATE TABLE a (aa TEXT)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_auth_members
;
----------
[CREATE_TABLE] CREATE TABLE t111 () INHERITS (t11, t12)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_enum_consistent(internal, anyenum, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tab_upgraded1" (
    "id" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = >=,
    COMMUTATOR = >,
    PROCEDURE  = citext_lt,
    RESTRICT   = scalarltsel,
    JOIN       = scalarltjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_shseclabel
;
----------
[CREATE_TYPE] CREATE TYPE myvarchar
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt2 DETACH PARTITION plt2_p3
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table returningwrtest attach partition returningwrtest2 for values in (2)
;
----------
[DROP_TYPE] DROP TYPE test_type1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28303 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_viaroot_part_1 (a int)
;
----------
[DROP_TRIGGER] DROP TRIGGER IF EXISTS accounts_insert_stmt ON split_trigger_probe.accounts CASCADE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28435_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tconvert(text,text)
RETURNS hstore
AS '$libdir/hstore','hstore_from_text'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table permtest_child (b text, c text, a int) partition by list (b)
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range) partition by range (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view5 AS SELECT a, rank() OVER() FROM base_tbl
;
----------
[CREATE_TABLE] CREATE TABLE itest_err_4 (a serial generated by default as identity)
;
----------
[DROP_POLICY] DROP TEXT SEARCH DICTIONARY test_tsdict_exists
;
----------
[CREATE_VIEW(SELECT)] -- Check use of a whole-row variable for an un-flattenable view
CREATE TEMP VIEW foo_v AS SELECT * FROM foo OFFSET 0
;
----------
[ALTER_POLICY] ALTER RULE account_update_notify ON split_trigger_probe.account_audit RENAME TO account_update_notify_renamed
;
----------
[ADMIN_TABLE] ANALYZE public.rules_log
;
