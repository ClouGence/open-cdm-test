SET enable_indexscan=on
;
CREATE ROLE regress_passwd_sha_len0 PASSWORD 'SCRAM-SHA-256$4096:A6xHKoH/494E941doaPOYg==$Ky+A30sewHIH3VHQLRN9vYsuzlgNyGNKCh37dy96Rqw=:COPdlNiIkrsacU5QoxydEuOH6e/KfiipeETb/bPw8ZI='
;
LOCK TABLE "public"."interval_tbl" IN ACCESS SHARE MODE
;
-- Test ROLLBACK PREPARED
ROLLBACK PREPARED 'regress_foo1'
;
DROP ROLE regress_passwd8
;
-- can't drop the user if we revoke the privileges partially
REVOKE SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, MAINTAIN ON deptest FROM regress_dep_user
;
ALTER TABLE public.test_type_diff2_c2 OWNER TO zyc
;
ALTER USER regress_authenticated_user_ssa superuser
;
--
-- test handling of merge/hash clauses that do not have valid commutators
--

-- There are not (and should not be) any such operators built into Postgres
-- that are mergejoinable or hashable but have no commutators; so we leverage
-- the alias type 'int8alias1' created in this file to conduct the tests.
-- That's why this test is included here rather than in join.sql.

begin
;
-- should succeed for now
COMMIT
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] SET enable_indexscan=on
;
----------
[CREATE_ROLE] CREATE ROLE regress_passwd_sha_len0 PASSWORD 'SCRAM-SHA-256$4096:A6xHKoH/494E941doaPOYg==$Ky+A30sewHIH3VHQLRN9vYsuzlgNyGNKCh37dy96Rqw=:COPdlNiIkrsacU5QoxydEuOH6e/KfiipeETb/bPw8ZI='
;
----------
[SESSION_LOCK] LOCK TABLE "public"."interval_tbl" IN ACCESS SHARE MODE
;
----------
[TRANSACTION] -- Test ROLLBACK PREPARED
ROLLBACK PREPARED 'regress_foo1'
;
----------
[DROP_ROLE] DROP ROLE regress_passwd8
;
----------
[REVOKE] -- can't drop the user if we revoke the privileges partially
REVOKE SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, MAINTAIN ON deptest FROM regress_dep_user
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.test_type_diff2_c2 OWNER TO zyc
;
----------
[ALTER_USER] ALTER USER regress_authenticated_user_ssa superuser
;
----------
[TRANSACTION] --
-- test handling of merge/hash clauses that do not have valid commutators
--

-- There are not (and should not be) any such operators built into Postgres
-- that are mergejoinable or hashable but have no commutators; so we leverage
-- the alias type 'int8alias1' created in this file to conduct the tests.
-- That's why this test is included here rather than in join.sql.

begin
;
----------
[TRANSACTION] -- should succeed for now
COMMIT
;
