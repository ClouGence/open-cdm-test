-- ok
-- partitioned table children
COPY t1p_ones FROM '/dev/null'
;
--fail - permission denied

-- Check COPY relation TO; keep it just one row to avoid reordering issues
RESET SESSION AUTHORIZATION
;
EXECUTE dumpDomain('34492')
;
-- ===================================================================
-- parameterized queries
-- ===================================================================
-- simple join
PREPARE st1(int, int) AS SELECT t1.c3, t2.c3 FROM ft1 t1, ft2 t2 WHERE t1.c1 = $1 AND t2.c1 = $2
;
REVOKE GRANT OPTION FOR UPDATE ON SEQUENCE
							wgo_then_regular FROM pg_signal_backend
;
RESET parallel_setup_cost
;
PREPARE dumpDomain(pg_catalog.oid) AS
SELECT t.typnotnull, pg_catalog.format_type(t.typbasetype, t.typtypmod) AS typdefn, pg_catalog.pg_get_expr(t.typdefaultbin, 'pg_catalog.pg_type'::pg_catalog.regclass) AS typdefaultbin, t.typdefault, CASE WHEN t.typcollation <> u.typcollation THEN t.typcollation ELSE 0 END AS typcollation FROM pg_catalog.pg_type t LEFT JOIN pg_catalog.pg_type u ON (t.typbasetype = u.oid) WHERE t.oid = $1
;
RELEASE SAVEPOINT p1
;
EXECUTE dumpFunc('35799')
;
EXECUTE dumpCompositeType('33815')
;
------------------------------------------------------------------------------------------
[DATA_IMPORT] -- ok
-- partitioned table children
COPY t1p_ones FROM '/dev/null'
;
----------
[SWITCH_USER] --fail - permission denied

-- Check COPY relation TO; keep it just one row to avoid reordering issues
RESET SESSION AUTHORIZATION
;
----------
[UNSAFE] EXECUTE dumpDomain('34492')
;
----------
[UNSAFE] -- ===================================================================
-- parameterized queries
-- ===================================================================
-- simple join
PREPARE st1(int, int) AS SELECT t1.c3, t2.c3 FROM ft1 t1, ft2 t2 WHERE t1.c1 = $1 AND t2.c1 = $2
;
----------
[REVOKE] REVOKE GRANT OPTION FOR UPDATE ON SEQUENCE
							wgo_then_regular FROM pg_signal_backend
;
----------
[SESSION_SETTING_WRITE] RESET parallel_setup_cost
;
----------
[UNSAFE] PREPARE dumpDomain(pg_catalog.oid) AS
SELECT t.typnotnull, pg_catalog.format_type(t.typbasetype, t.typtypmod) AS typdefn, pg_catalog.pg_get_expr(t.typdefaultbin, 'pg_catalog.pg_type'::pg_catalog.regclass) AS typdefaultbin, t.typdefault, CASE WHEN t.typcollation <> u.typcollation THEN t.typcollation ELSE 0 END AS typcollation FROM pg_catalog.pg_type t LEFT JOIN pg_catalog.pg_type u ON (t.typbasetype = u.oid) WHERE t.oid = $1
;
----------
[TRANSACTION] RELEASE SAVEPOINT p1
;
----------
[UNSAFE] EXECUTE dumpFunc('35799')
;
----------
[UNSAFE] EXECUTE dumpCompositeType('33815')
;
