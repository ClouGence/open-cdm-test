VACUUM (SKIP_DATABASE_STATS) information_schema.sql_sizing
;
DROP TABLE sq_target, sq_target_merge_log, sq_source CASCADE
;
CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (((a, b)))
;
ANALYZE public.part32_self_fk
;
DROP TABLE IF EXISTS audit_tbls.audit_tbls_schema_one_table_two
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
-- Change plt2_adv_p3 to the NULL partition
CREATE TABLE plt2_adv_p3_null PARTITION OF plt2_adv FOR VALUES IN (NULL, '0007', '0009')
;
COMMENT ON CONVERSION split_conversion_probe_next.latin1_to_utf8_renamed IS 'split conversion comment'
;
create function test_table_func_rec() returns setof found_test_tbl as '
DECLARE
	rec RECORD;
BEGIN
	FOR rec IN select * from found_test_tbl LOOP
		RETURN NEXT rec;
	END LOOP;
	RETURN;
END;' language plpgsql
;
create table btree_tall_tbl(id int4, t text)
;
drop table cc
;
DROP TABLE ptif_test
;
DROP TYPE ctlty1
;
CREATE PUBLICATION pub1 FOR TABLE tab1
;
VACUUM (PARALLEL 0) pg_catalog.pg_statistic
;
CREATE TABLE copy_rel_to_child () INHERITS (copy_rel_to)
;
CALL ptest4a(NULL, NULL)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_range
;
-- For binary upgrade, set up typed tables this way.
ALTER TABLE ONLY "public"."persons2" OF "public"."person_type"
;
ANALYZE pg_temp_10.pg_temp_26350
;
CREATE FUNCTION heap_force_kill(reloid regclass, tids tid[])
RETURNS VOID
AS '$libdir/pg_surgery', 'heap_force_kill'
LANGUAGE C STRICT
;
--
-- test_custom_rmgrs_insert_wal_record()
--
-- Writes a simple message into WAL with the help of custom WAL
-- resource manager.
--
CREATE FUNCTION test_custom_rmgrs_insert_wal_record(IN payload TEXT,
    OUT lsn pg_lsn
)
AS 'MODULE_PATHNAME', 'test_custom_rmgrs_insert_wal_record'
LANGUAGE C STRICT PARALLEL UNSAFE
;
ALTER TABLE pg_toast.pg_toast_27755 RENAME TO reind_con_toast
;
CREATE VIEW collview3 AS SELECT a, lower((x || x) COLLATE "POSIX") FROM collate_test10
;
ALTER INDEX pg_toast.pg_toast_30828_index RENAME TO reind_con_toast_idx
;
CREATE TABLE dropped_objects (
	type text,
	schema text,
	object text
)
;
COMMENT ON AGGREGATE split_agg_probe.sum_int(integer) IS 'integer aggregate with moving support'
;
-- let's do one where the check fails because the column doesn't exist
create table atacc1 ( test int )
;
-- test COPY with overlong column defaults
create temp table oversized_column_default (
    col1 varchar(5) DEFAULT 'more than 5 chars',
    col2 varchar(5))
;
--
-- Test interaction of foreign-key optimization with rules (bug #14219)
--
create temp table t1 (a integer primary key, b text)
;
DROP TABLE seclabel_tbl1
;
ALTER TABLE pg_temp_13.pg_temp_26115_2 ADD COLUMN newdata pg_temp_13.pg_temp_26115
;
CREATE FUNCTION ltree_decompress(internal)
RETURNS internal as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
drop function convslot_trig3()
;
--    P    N        N    N
-- should ERROR: tfnp(anyarray, int) not matched by tfnp(int[],int)
CREATE AGGREGATE myaggp13a(BASETYPE = int, SFUNC = tfnp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
CREATE INDEX ON brin_timestamp_test USING brin (a timestamptz_minmax_multi_ops) WITH (pages_per_range=1)
;
COMMENT ON TEXT SEARCH DICTIONARY indonesian_stem IS 'snowball stemmer for indonesian language'
;
CREATE OPERATOR < (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_lt,
        COMMUTATOR = '>',
	NEGATOR = '>=',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
-- prepare some test data
CREATE TABLE aggtest (
	a 			int2,
	b			float4
)
;
create trigger child3_delete_trig
  after delete on child3 referencing old table as old_table
  for each statement execute procedure dump_delete()
;
DROP OPERATOR FAMILY alt_opf10 USING btree
;
-- Try (and fail) to create constraint from attmp5(a) to attmp4(a) - unique constraint on
-- attmp4 is a,b

ALTER TABLE attmp5 add constraint attmpconstr foreign key(a) references attmp4(a) match full
;
-- should work
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, c, d, 0 AS e FROM viewtest_tbl
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_largeobject
;
CREATE TABLE "public"."gtest3a" (
    "a" "text",
    "b" "text" GENERATED ALWAYS AS ((("a" || '+'::"text") || "a")) STORED
)
;
-- Operators can return sets too
CREATE OPERATOR |@| (PROCEDURE = unnest, RIGHTARG = ANYARRAY)
;
-- Create the operator classes for indexing

CREATE OPERATOR CLASS seg_ops
    DEFAULT FOR TYPE seg USING btree AS
        OPERATOR        1       < ,
        OPERATOR        2       <= ,
        OPERATOR        3       = ,
        OPERATOR        4       >= ,
        OPERATOR        5       > ,
        FUNCTION        1       seg_cmp(seg, seg)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
create index spgist_point_idx2 on spgist_point_tbl using spgist(p) with (fillfactor = 101)
;
CREATE FUNCTION handle_get_and_error()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) information_schema.sql_sizing
;
----------
[DROP_TABLE] DROP TABLE sq_target, sq_target_merge_log, sq_source CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (((a, b)))
;
----------
[ADMIN_TABLE] ANALYZE public.part32_self_fk
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS audit_tbls.audit_tbls_schema_one_table_two
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_TABLE] -- Change plt2_adv_p3 to the NULL partition
CREATE TABLE plt2_adv_p3_null PARTITION OF plt2_adv FOR VALUES IN (NULL, '0007', '0009')
;
----------
[ALTER_POLICY] COMMENT ON CONVERSION split_conversion_probe_next.latin1_to_utf8_renamed IS 'split conversion comment'
;
----------
[CREATE_PROG_OBJ] create function test_table_func_rec() returns setof found_test_tbl as '
DECLARE
	rec RECORD;
BEGIN
	FOR rec IN select * from found_test_tbl LOOP
		RETURN NEXT rec;
	END LOOP;
	RETURN;
END;' language plpgsql
;
----------
[CREATE_TABLE] create table btree_tall_tbl(id int4, t text)
;
----------
[DROP_TABLE] drop table cc
;
----------
[DROP_TABLE] DROP TABLE ptif_test
;
----------
[DROP_TYPE] DROP TYPE ctlty1
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub1 FOR TABLE tab1
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_statistic
;
----------
[CREATE_TABLE] CREATE TABLE copy_rel_to_child () INHERITS (copy_rel_to)
;
----------
[CALL_PROG_OBJ] CALL ptest4a(NULL, NULL)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_range
;
----------
[ALTER_TABLE] -- For binary upgrade, set up typed tables this way.
ALTER TABLE ONLY "public"."persons2" OF "public"."person_type"
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26350
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION heap_force_kill(reloid regclass, tids tid[])
RETURNS VOID
AS '$libdir/pg_surgery', 'heap_force_kill'
LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] --
-- test_custom_rmgrs_insert_wal_record()
--
-- Writes a simple message into WAL with the help of custom WAL
-- resource manager.
--
CREATE FUNCTION test_custom_rmgrs_insert_wal_record(IN payload TEXT,
    OUT lsn pg_lsn
)
AS 'MODULE_PATHNAME', 'test_custom_rmgrs_insert_wal_record'
LANGUAGE C STRICT PARALLEL UNSAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27755 RENAME TO reind_con_toast
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW collview3 AS SELECT a, lower((x || x) COLLATE "POSIX") FROM collate_test10
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30828_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE dropped_objects (
	type text,
	schema text,
	object text
)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE split_agg_probe.sum_int(integer) IS 'integer aggregate with moving support'
;
----------
[CREATE_TABLE] -- let's do one where the check fails because the column doesn't exist
create table atacc1 ( test int )
;
----------
[CREATE_TABLE] -- test COPY with overlong column defaults
create temp table oversized_column_default (
    col1 varchar(5) DEFAULT 'more than 5 chars',
    col2 varchar(5))
;
----------
[CREATE_TABLE] --
-- Test interaction of foreign-key optimization with rules (bug #14219)
--
create temp table t1 (a integer primary key, b text)
;
----------
[DROP_TABLE] DROP TABLE seclabel_tbl1
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26115_2 ADD COLUMN newdata pg_temp_13.pg_temp_26115
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_decompress(internal)
RETURNS internal as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] drop function convslot_trig3()
;
----------
[CREATE_PROG_OBJ] --    P    N        N    N
-- should ERROR: tfnp(anyarray, int) not matched by tfnp(int[],int)
CREATE AGGREGATE myaggp13a(BASETYPE = int, SFUNC = tfnp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ADD_INDEX] CREATE INDEX ON brin_timestamp_test USING brin (a timestamptz_minmax_multi_ops) WITH (pages_per_range=1)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY indonesian_stem IS 'snowball stemmer for indonesian language'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_lt,
        COMMUTATOR = '>',
	NEGATOR = '>=',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] -- prepare some test data
CREATE TABLE aggtest (
	a 			int2,
	b			float4
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child3_delete_trig
  after delete on child3 referencing old table as old_table
  for each statement execute procedure dump_delete()
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf10 USING btree
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Try (and fail) to create constraint from attmp5(a) to attmp4(a) - unique constraint on
-- attmp4 is a,b

ALTER TABLE attmp5 add constraint attmpconstr foreign key(a) references attmp4(a) match full
;
----------
[CREATE_VIEW(SELECT)] -- should work
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, c, d, 0 AS e FROM viewtest_tbl
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_largeobject
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest3a" (
    "a" "text",
    "b" "text" GENERATED ALWAYS AS ((("a" || '+'::"text") || "a")) STORED
)
;
----------
[CREATE_PROG_OBJ] -- Operators can return sets too
CREATE OPERATOR |@| (PROCEDURE = unnest, RIGHTARG = ANYARRAY)
;
----------
[CREATE_PROG_OBJ] -- Create the operator classes for indexing

CREATE OPERATOR CLASS seg_ops
    DEFAULT FOR TYPE seg USING btree AS
        OPERATOR        1       < ,
        OPERATOR        2       <= ,
        OPERATOR        3       = ,
        OPERATOR        4       >= ,
        OPERATOR        5       > ,
        FUNCTION        1       seg_cmp(seg, seg)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ADD_INDEX] create index spgist_point_idx2 on spgist_point_tbl using spgist(p) with (fillfactor = 101)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_get_and_error()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
