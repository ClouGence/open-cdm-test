CREATE ROLE regress_rls_dob_role1
;
-- Clean up
reset enable_seqscan
;
ALTER ROLE regress_passwd2 PASSWORD 'foo'
;
EXECUTE dumpFunc('30923')
;
COPY public.nummultirange_test (nmr) TO stdout
;
-- normal users can't drop existing slots
SET ROLE regress_lr_superuser
;
-- error

--
-- test behavior of escape_string_warning and standard_conforming_strings options
--
set escape_string_warning = off
;
GRANT SELECT ON my_property_normal TO public
;
-- check it's really aborted
COMMIT AND CHAIN
;
EXECUTE dumpOpr('693')
;
------------------------------------------------------------------------------------------
[CREATE_ROLE] CREATE ROLE regress_rls_dob_role1
;
----------
[SESSION_SETTING_WRITE] -- Clean up
reset enable_seqscan
;
----------
[ALTER_USER] ALTER ROLE regress_passwd2 PASSWORD 'foo'
;
----------
[UNSAFE] EXECUTE dumpFunc('30923')
;
----------
[DATA_EXPORT] COPY public.nummultirange_test (nmr) TO stdout
;
----------
[SWITCH_ROLE] -- normal users can't drop existing slots
SET ROLE regress_lr_superuser
;
----------
[SESSION_SETTING_WRITE] -- error

--
-- test behavior of escape_string_warning and standard_conforming_strings options
--
set escape_string_warning = off
;
----------
[GRANT] GRANT SELECT ON my_property_normal TO public
;
----------
[TRANSACTION] -- check it's really aborted
COMMIT AND CHAIN
;
----------
[UNSAFE] EXECUTE dumpOpr('693')
;
