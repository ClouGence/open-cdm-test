EXECUTE dumpOpr('1111')
;
LOCK TABLE public.rtest_t4 IN ACCESS SHARE MODE
;
EXECUTE dumpFunc('30940')
;
CREATE STATISTICS tst ON (x || 'x'), (x || 'x'), y FROM ext_stats_test
;
COPY public.fast_emp4000 (home_base) TO stdout
;
EXECUTE dumpAgg('34574')
;
-- test warnings and errors
set plpgsql.extra_warnings to 'all'
;
-- ===================================================================
-- reestablish new connection
-- ===================================================================

-- Change application_name of remote connection to special one
-- so that we can easily terminate the connection later.
ALTER SERVER loopback OPTIONS (application_name 'fdw_retry_check')
;
ALTER TABLE public.rtest_view4 OWNER TO zyc
;
ALTER VIEW public.xmltableview1 OWNER TO zyc
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpOpr('1111')
;
----------
[SESSION_LOCK] LOCK TABLE public.rtest_t4 IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpFunc('30940')
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS tst ON (x || 'x'), (x || 'x'), y FROM ext_stats_test
;
----------
[DATA_EXPORT] COPY public.fast_emp4000 (home_base) TO stdout
;
----------
[UNSAFE] EXECUTE dumpAgg('34574')
;
----------
[SESSION_SETTING_WRITE] -- test warnings and errors
set plpgsql.extra_warnings to 'all'
;
----------
[SYSTEM_SETTING_WRITE] -- ===================================================================
-- reestablish new connection
-- ===================================================================

-- Change application_name of remote connection to special one
-- so that we can easily terminate the connection later.
ALTER SERVER loopback OPTIONS (application_name 'fdw_retry_check')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.rtest_view4 OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER VIEW public.xmltableview1 OWNER TO zyc
;
