create unique index on covidxpart (a) include (b)
;
ALTER INDEX pg_toast.pg_toast_33012_index RENAME TO reind_con_toast_idx
;
CREATE TABLE spc_nest (id serial PRIMARY KEY, id2 int)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_shdepend
;
drop function polyf(anyrange)
;
ALTER TABLE "public"."fk_partitioned_fk"
    ADD CONSTRAINT "fk_partitioned_fk_a_b_fkey" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b") ON UPDATE CASCADE ON DELETE CASCADE
;
CREATE TABLE test22 (a int, b text COLLATE case_sensitive) PARTITION BY HASH (b)
;
-- fails because a foreign key to an unlogged table exists
ALTER TABLE unlogged1 SET LOGGED
;
ALTER TABLE tab_full REPLICA IDENTITY FULL
;
CREATE VIEW ro_view6 AS SELECT a, b FROM base_tbl UNION SELECT -a, b FROM base_tbl
;
ALTER TABLE ONLY "public"."prt1_l" ATTACH PARTITION "public"."prt1_l_p3" FOR VALUES FROM (500) TO (600)
;
--
-- Test domains.
--

-- Test Comment / Drop
create domain domaindroptest int4
;
CREATE FUNCTION handle_release_last()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
ALTER INDEX "fkpart5"."pk_pkey" ATTACH PARTITION "fkpart5"."pk1_pkey"
;
CREATE VIEW rw_view16 AS SELECT a, b, a AS aa FROM base_tbl
;
CREATE CONSTRAINT TRIGGER trigtest_constraint AFTER INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH ROW
EXECUTE PROCEDURE dummy_trigger()
;
CREATE FUNCTION "public"."sql_recurse"(double precision) RETURNS double precision
    LANGUAGE "sql"
    AS $_$ select recurse($1) limit 1; $_$
;
DROP TEXT SEARCH TEMPLATE test_tstemplate_exists
;
CREATE DATABASE regression_invalid
;
CREATE FUNCTION gseg_consistent(internal,seg,smallint,oid,internal)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE OPERATOR <#> (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = distance_taxicab,
	COMMUTATOR = '<#>'
)
;
CREATE FUNCTION _int_matchsel(internal, oid, internal, integer)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_largeobject_metadata
;
CREATE SEQUENCE "public"."emp1_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
alter table cc3 no inherit cc2
;
ALTER TABLE ONLY "generated_virtual_tests"."gtestxx_4" INHERIT "generated_virtual_tests"."gtest1"
;
--
-- create some test data and test the operators
--

CREATE TABLE numrange_test (nr NUMRANGE)
;
CREATE INDEX idx_method_lower_include ON split_table_idx.method_table (lower(payload) text_pattern_ops) INCLUDE (id) WITH (fillfactor = 70)
;
CREATE DATABASE dropme_db1 WITH TABLESPACE dropme_ts1 STRATEGY=WAL_LOG
;
VACUUM (FREEZE) pg_catalog.pg_largeobject_metadata
;
-- excluding physical slots

CREATE VIEW pg_stat_database AS
    SELECT
            D.oid AS datid,
            D.datname AS datname,
                CASE
                    WHEN (D.oid = (0)::oid) THEN 0
                    ELSE pg_stat_get_db_numbackends(D.oid)
                END AS numbackends,
            pg_stat_get_db_xact_commit(D.oid) AS xact_commit,
            pg_stat_get_db_xact_rollback(D.oid) AS xact_rollback,
            pg_stat_get_db_blocks_fetched(D.oid) -
                    pg_stat_get_db_blocks_hit(D.oid) AS blks_read,
            pg_stat_get_db_blocks_hit(D.oid) AS blks_hit,
            pg_stat_get_db_tuples_returned(D.oid) AS tup_returned,
            pg_stat_get_db_tuples_fetched(D.oid) AS tup_fetched,
            pg_stat_get_db_tuples_inserted(D.oid) AS tup_inserted,
            pg_stat_get_db_tuples_updated(D.oid) AS tup_updated,
            pg_stat_get_db_tuples_deleted(D.oid) AS tup_deleted,
            pg_stat_get_db_conflict_all(D.oid) AS conflicts,
            pg_stat_get_db_temp_files(D.oid) AS temp_files,
            pg_stat_get_db_temp_bytes(D.oid) AS temp_bytes,
            pg_stat_get_db_deadlocks(D.oid) AS deadlocks,
            pg_stat_get_db_checksum_failures(D.oid) AS checksum_failures,
            pg_stat_get_db_checksum_last_failure(D.oid) AS checksum_last_failure,
            pg_stat_get_db_blk_read_time(D.oid) AS blk_read_time,
            pg_stat_get_db_blk_write_time(D.oid) AS blk_write_time,
            pg_stat_get_db_session_time(D.oid) AS session_time,
            pg_stat_get_db_active_time(D.oid) AS active_time,
            pg_stat_get_db_idle_in_transaction_time(D.oid) AS idle_in_transaction_time,
            pg_stat_get_db_sessions(D.oid) AS sessions,
            pg_stat_get_db_sessions_abandoned(D.oid) AS sessions_abandoned,
            pg_stat_get_db_sessions_fatal(D.oid) AS sessions_fatal,
            pg_stat_get_db_sessions_killed(D.oid) AS sessions_killed,
            pg_stat_get_db_stat_reset_time(D.oid) AS stats_reset
    FROM (
        SELECT 0 AS oid, NULL::name AS datname
        UNION ALL
        SELECT oid, datname FROM pg_database
    ) D
;
CREATE EXTENSION dblink
;
ALTER TABLE gtest20a ADD COLUMN c float8 DEFAULT random() CHECK (b < 50)
;
DROP TABLE ext_stats_test1
;
CREATE OR REPLACE FUNCTION f_leak(text) RETURNS bool
    COST 0.0000001 LANGUAGE plpgsql
    AS 'BEGIN RAISE NOTICE ''f_leak => %'', $1; RETURN true; END'
;
-- fail

-- clean up objects
DROP FUNCTION seclabel_four()
;
ALTER TABLE inhx ADD PRIMARY KEY (xx)
;
CREATE TEXT SEARCH TEMPLATE tstemp_case ("Init" = init_function)
;
CREATE VIEW atestv1 AS SELECT * FROM atest1
;
SECURITY LABEL ON TABLE seclabel_tbl3 IS 'unclassified'
;
CREATE OR REPLACE VIEW viewtest AS
	SELECT a FROM viewtest_tbl WHERE a <> 20
;
DROP SCHEMA split_ts_next
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
ANALYZE pg_temp_4.pg_temp_27293_2
;
ALTER INDEX pg_toast.pg_toast_28623_index RENAME TO reind_con_toast_idx
;
CREATE TABLE tid_part (a int) PARTITION BY RANGE (a)
;
CREATE TABLE fk3 (b int, a int)
;
CREATE OR REPLACE FUNCTION test_read_multixact(xid) RETURNS VOID
  AS '$libdir/test_slru', 'test_read_multixact'LANGUAGE C
;
-- fails
ALTER TYPE test_type2 RENAME ATTRIBUTE a TO aa CASCADE
;
CREATE TABLE tab_rowfilter_2 (c int primary key)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] create unique index on covidxpart (a) include (b)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33012_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE spc_nest (id serial PRIMARY KEY, id2 int)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_shdepend
;
----------
[DROP_PROG_OBJ] drop function polyf(anyrange)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."fk_partitioned_fk"
    ADD CONSTRAINT "fk_partitioned_fk_a_b_fkey" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b") ON UPDATE CASCADE ON DELETE CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE test22 (a int, b text COLLATE case_sensitive) PARTITION BY HASH (b)
;
----------
[ALTER_TABLE] -- fails because a foreign key to an unlogged table exists
ALTER TABLE unlogged1 SET LOGGED
;
----------
[ALTER_TABLE] ALTER TABLE tab_full REPLICA IDENTITY FULL
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view6 AS SELECT a, b FROM base_tbl UNION SELECT -a, b FROM base_tbl
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_l" ATTACH PARTITION "public"."prt1_l_p3" FOR VALUES FROM (500) TO (600)
;
----------
[CREATE_TYPE] --
-- Test domains.
--

-- Test Comment / Drop
create domain domaindroptest int4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_release_last()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart5"."pk_pkey" ATTACH PARTITION "fkpart5"."pk1_pkey"
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view16 AS SELECT a, b, a AS aa FROM base_tbl
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE CONSTRAINT TRIGGER trigtest_constraint AFTER INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH ROW
EXECUTE PROCEDURE dummy_trigger()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."sql_recurse"(double precision) RETURNS double precision
    LANGUAGE "sql"
    AS $_$ select recurse($1) limit 1; $_$
;
----------
[DROP_POLICY] DROP TEXT SEARCH TEMPLATE test_tstemplate_exists
;
----------
[CREATE_CATALOG] CREATE DATABASE regression_invalid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gseg_consistent(internal,seg,smallint,oid,internal)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <#> (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = distance_taxicab,
	COMMUTATOR = '<#>'
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_matchsel(internal, oid, internal, integer)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."emp1_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ALTER_TABLE] alter table cc3 no inherit cc2
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_virtual_tests"."gtestxx_4" INHERIT "generated_virtual_tests"."gtest1"
;
----------
[CREATE_TABLE] --
-- create some test data and test the operators
--

CREATE TABLE numrange_test (nr NUMRANGE)
;
----------
[ADD_INDEX] CREATE INDEX idx_method_lower_include ON split_table_idx.method_table (lower(payload) text_pattern_ops) INCLUDE (id) WITH (fillfactor = 70)
;
----------
[CREATE_CATALOG] CREATE DATABASE dropme_db1 WITH TABLESPACE dropme_ts1 STRATEGY=WAL_LOG
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_VIEW(SELECT)] -- excluding physical slots

CREATE VIEW pg_stat_database AS
    SELECT
            D.oid AS datid,
            D.datname AS datname,
                CASE
                    WHEN (D.oid = (0)::oid) THEN 0
                    ELSE pg_stat_get_db_numbackends(D.oid)
                END AS numbackends,
            pg_stat_get_db_xact_commit(D.oid) AS xact_commit,
            pg_stat_get_db_xact_rollback(D.oid) AS xact_rollback,
            pg_stat_get_db_blocks_fetched(D.oid) -
                    pg_stat_get_db_blocks_hit(D.oid) AS blks_read,
            pg_stat_get_db_blocks_hit(D.oid) AS blks_hit,
            pg_stat_get_db_tuples_returned(D.oid) AS tup_returned,
            pg_stat_get_db_tuples_fetched(D.oid) AS tup_fetched,
            pg_stat_get_db_tuples_inserted(D.oid) AS tup_inserted,
            pg_stat_get_db_tuples_updated(D.oid) AS tup_updated,
            pg_stat_get_db_tuples_deleted(D.oid) AS tup_deleted,
            pg_stat_get_db_conflict_all(D.oid) AS conflicts,
            pg_stat_get_db_temp_files(D.oid) AS temp_files,
            pg_stat_get_db_temp_bytes(D.oid) AS temp_bytes,
            pg_stat_get_db_deadlocks(D.oid) AS deadlocks,
            pg_stat_get_db_checksum_failures(D.oid) AS checksum_failures,
            pg_stat_get_db_checksum_last_failure(D.oid) AS checksum_last_failure,
            pg_stat_get_db_blk_read_time(D.oid) AS blk_read_time,
            pg_stat_get_db_blk_write_time(D.oid) AS blk_write_time,
            pg_stat_get_db_session_time(D.oid) AS session_time,
            pg_stat_get_db_active_time(D.oid) AS active_time,
            pg_stat_get_db_idle_in_transaction_time(D.oid) AS idle_in_transaction_time,
            pg_stat_get_db_sessions(D.oid) AS sessions,
            pg_stat_get_db_sessions_abandoned(D.oid) AS sessions_abandoned,
            pg_stat_get_db_sessions_fatal(D.oid) AS sessions_fatal,
            pg_stat_get_db_sessions_killed(D.oid) AS sessions_killed,
            pg_stat_get_db_stat_reset_time(D.oid) AS stats_reset
    FROM (
        SELECT 0 AS oid, NULL::name AS datname
        UNION ALL
        SELECT oid, datname FROM pg_database
    ) D
;
----------
[CREATE_LIBRARY] CREATE EXTENSION dblink
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest20a ADD COLUMN c float8 DEFAULT random() CHECK (b < 50)
;
----------
[DROP_TABLE] DROP TABLE ext_stats_test1
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION f_leak(text) RETURNS bool
    COST 0.0000001 LANGUAGE plpgsql
    AS 'BEGIN RAISE NOTICE ''f_leak => %'', $1; RETURN true; END'
;
----------
[DROP_PROG_OBJ] -- fail

-- clean up objects
DROP FUNCTION seclabel_four()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE inhx ADD PRIMARY KEY (xx)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH TEMPLATE tstemp_case ("Init" = init_function)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW atestv1 AS SELECT * FROM atest1
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE seclabel_tbl3 IS 'unclassified'
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW viewtest AS
	SELECT a FROM viewtest_tbl WHERE a <> 20
;
----------
[DROP_SCHEMA] DROP SCHEMA split_ts_next
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_27293_2
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28623_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE tid_part (a int) PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE] CREATE TABLE fk3 (b int, a int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_read_multixact(xid) RETURNS VOID
  AS '$libdir/test_slru', 'test_read_multixact'LANGUAGE C
;
----------
[ALTER_TYPE] -- fails
ALTER TYPE test_type2 RENAME ATTRIBUTE a TO aa CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_2 (c int primary key)
;
