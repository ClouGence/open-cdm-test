UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"generated_virtual_tests"."gtest30_1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
INSERT INTO arraggtest (f1, f2, f3) VALUES
('{1,2,3}','{{grey,red},{grey,blue}}','{1.6}')
;
INSERT INTO pg_temp_11.pg_temp_26330_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_26330 AS newdata FROM public.mvtest_boxmv mv FULL JOIN pg_temp_11.pg_temp_26330 newdata ON (newdata.id OPERATOR(pg_catalog.=) mv.id AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6507', relminmxid = '1'
WHERE oid = '"public"."test_tablesample"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('8', 318)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3363', relminmxid = '1'
WHERE oid = '"public"."check_tbl"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (8, 88)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'id'
  AND attrelid = '"public"."part33_self_fk"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('3', 163)
;
insert into rtest_view1 values (5, 'item 5', 't')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14740', relminmxid = '3'
WHERE oid = '"public"."covidxpart1"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18643', relminmxid = '3'
WHERE oid = '41722'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14329', relminmxid = '3'
WHERE oid = '35935'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12579', relminmxid = '1'
WHERE oid = '33502'
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."idxpart31"'::pg_catalog.regclass
  AND attname IN ('a')
;
INSERT INTO gtest1 VALUES (3, 33)
;
INSERT INTO plt2_adv SELECT i, i, to_char(i % 10, 'FM0000') FROM generate_series(1, 299) i WHERE i % 10 IN (1, 2, 3, 4, 5)
;
INSERT INTO vctable VALUES ('foo' COLLATE "C")
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2900', relminmxid = '1'
WHERE oid = '"public"."mlparted3"'::pg_catalog.regclass
;
insert into parent values ('CCC', 42)
;
INSERT INTO public.tht VALUES ('3', 63)
;
-- set using existing table
insert into dropcol(key, keep1, keep2) values(1, '5', 5) on conflict(key)
    do update set keep1 = dropcol.keep1, keep2 = dropcol.keep2
    returning *
;
INSERT INTO public.tht VALUES ('1', 251)
;
INSERT INTO public.tplain VALUES ('4', 164)
;
insert into insertconflicttest values (10, 'Blueberry') on conflict (key, key, key) do update set fruit = excluded.fruit
;
update PLine set backlink = blname where slotname = myname
;
-- fail, wrong data type:
UPDATE update_test SET a = v.* FROM (VALUES(100, 20)) AS v(i, j)
  WHERE update_test.b = v.j
;
INSERT INTO test2 VALUES (2)
;
-- this always works
INSERT INTO fk_partitioned_fk (a,b) VALUES (NULL, NULL)
;
INSERT INTO public.tplain VALUES ('3', 433)
;
INSERT INTO pvactst SELECT i, array[1,2,3], point(i, i+1) FROM generate_series(1,1000) i
;
INSERT INTO TEMP_GROUP
  SELECT 2, i.f1, f.f1
  FROM INT4_TBL i, FLOAT8_TBL f
;
INSERT INTO test1 (a, b) VALUES (1, 'one'), (2, 'two')
;
INSERT INTO public.tht VALUES ('5', 795)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9202', relminmxid = '1'
WHERE oid = '"public"."brintest_bloom"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10522', relminmxid = '1'
WHERE oid = '29379'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '948', relminmxid = '1'
WHERE oid = '"public"."num_exp_div"'::pg_catalog.regclass
;
update t1 set a = 4 where a = 5
;
UPDATE public.loct1 r4 SET b = (r4.b || r2.b) FROM public.loct2 r2 WHERE ((r4.a = r2.a)) RETURNING r4.a, r4.b, r2.a, r2.b
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4030', relminmxid = '1'
WHERE oid = '"public"."tableam_tbl_heap2"'::pg_catalog.regclass
;
insert into inserttestb (f3.if1, f3.if2) select 3, '{null,quux}'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11278', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest_child2"'::pg_catalog.regclass
;
INSERT INTO b VALUES (1)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14172', relminmxid = '3'
WHERE oid = '35922'
;
INSERT INTO restaurants SELECT 'Szczecin' FROM generate_series(1, 10000)
;
INSERT INTO temporal_mltrng (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2018-03-03', '2018-04-04')))
;
UPDATE temporal_rng
  SET valid_at = CASE WHEN lower(valid_at) = '2018-01-01' THEN daterange('2018-01-01', '2018-01-05')
                      WHEN lower(valid_at) = '2018-02-01' THEN daterange('2018-01-05', '2018-03-01') END
  WHERE id = '[6,7)'
;
INSERT INTO num_exp_mul VALUES (8,5,'1227826639.244571')
;
-- error
INSERT INTO gtest1v VALUES (6, DEFAULT), (7, DEFAULT)
;
INSERT INTO public.ths_p1 VALUES (4, 4)
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"generated_virtual_tests"."gtest30_1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[INSERT|SELECT] INSERT INTO arraggtest (f1, f2, f3) VALUES
('{1,2,3}','{{grey,red},{grey,blue}}','{1.6}')
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_11.pg_temp_26330_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_26330 AS newdata FROM public.mvtest_boxmv mv FULL JOIN pg_temp_11.pg_temp_26330 newdata ON (newdata.id OPERATOR(pg_catalog.=) mv.id AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6507', relminmxid = '1'
WHERE oid = '"public"."test_tablesample"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 318)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3363', relminmxid = '1'
WHERE oid = '"public"."check_tbl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 88)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'id'
  AND attrelid = '"public"."part33_self_fk"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('3', 163)
;
----------
[INSERT] insert into rtest_view1 values (5, 'item 5', 't')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14740', relminmxid = '3'
WHERE oid = '"public"."covidxpart1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18643', relminmxid = '3'
WHERE oid = '41722'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14329', relminmxid = '3'
WHERE oid = '35935'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '12579', relminmxid = '1'
WHERE oid = '33502'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."idxpart31"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[INSERT] INSERT INTO gtest1 VALUES (3, 33)
;
----------
[INSERT|SELECT] INSERT INTO plt2_adv SELECT i, i, to_char(i % 10, 'FM0000') FROM generate_series(1, 299) i WHERE i % 10 IN (1, 2, 3, 4, 5)
;
----------
[INSERT] INSERT INTO vctable VALUES ('foo' COLLATE "C")
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2900', relminmxid = '1'
WHERE oid = '"public"."mlparted3"'::pg_catalog.regclass
;
----------
[INSERT] insert into parent values ('CCC', 42)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('3', 63)
;
----------
[MERGE|SELECT] -- set using existing table
insert into dropcol(key, keep1, keep2) values(1, '5', 5) on conflict(key)
    do update set keep1 = dropcol.keep1, keep2 = dropcol.keep2
    returning *
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 251)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 164)
;
----------
[MERGE] insert into insertconflicttest values (10, 'Blueberry') on conflict (key, key, key) do update set fruit = excluded.fruit
;
----------
[UPDATE] update PLine set backlink = blname where slotname = myname
;
----------
[UPDATE|SELECT] -- fail, wrong data type:
UPDATE update_test SET a = v.* FROM (VALUES(100, 20)) AS v(i, j)
  WHERE update_test.b = v.j
;
----------
[INSERT] INSERT INTO test2 VALUES (2)
;
----------
[INSERT|SELECT] -- this always works
INSERT INTO fk_partitioned_fk (a,b) VALUES (NULL, NULL)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 433)
;
----------
[INSERT|SELECT] INSERT INTO pvactst SELECT i, array[1,2,3], point(i, i+1) FROM generate_series(1,1000) i
;
----------
[INSERT|SELECT] INSERT INTO TEMP_GROUP
  SELECT 2, i.f1, f.f1
  FROM INT4_TBL i, FLOAT8_TBL f
;
----------
[INSERT|SELECT] INSERT INTO test1 (a, b) VALUES (1, 'one'), (2, 'two')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 795)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9202', relminmxid = '1'
WHERE oid = '"public"."brintest_bloom"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10522', relminmxid = '1'
WHERE oid = '29379'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '948', relminmxid = '1'
WHERE oid = '"public"."num_exp_div"'::pg_catalog.regclass
;
----------
[UPDATE] update t1 set a = 4 where a = 5
;
----------
[UPDATE] UPDATE public.loct1 r4 SET b = (r4.b || r2.b) FROM public.loct2 r2 WHERE ((r4.a = r2.a)) RETURNING r4.a, r4.b, r2.a, r2.b
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4030', relminmxid = '1'
WHERE oid = '"public"."tableam_tbl_heap2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into inserttestb (f3.if1, f3.if2) select 3, '{null,quux}'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '11278', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest_child2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO b VALUES (1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14172', relminmxid = '3'
WHERE oid = '35922'
;
----------
[INSERT|SELECT] INSERT INTO restaurants SELECT 'Szczecin' FROM generate_series(1, 10000)
;
----------
[INSERT|SELECT] INSERT INTO temporal_mltrng (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2018-03-03', '2018-04-04')))
;
----------
[UPDATE] UPDATE temporal_rng
  SET valid_at = CASE WHEN lower(valid_at) = '2018-01-01' THEN daterange('2018-01-01', '2018-01-05')
                      WHEN lower(valid_at) = '2018-02-01' THEN daterange('2018-01-05', '2018-03-01') END
  WHERE id = '[6,7)'
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (8,5,'1227826639.244571')
;
----------
[INSERT] -- error
INSERT INTO gtest1v VALUES (6, DEFAULT), (7, DEFAULT)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 4)
;
