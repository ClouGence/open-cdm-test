--
-- Ensure that some values are uncompressed, to test the faster substring
-- operation used in that case
--
alter table toasttest alter column f1 set storage external
;
ANALYZE public.thethings
;
-- Nor should this... (same reason, we have 4,3 referencing 1,2 which mismatches types
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest4,
ptest3) REFERENCES pktable(ptest1, ptest2))
;
ALTER TABLE split_alter_table.base_table DISABLE TRIGGER base_table_bi
;
alter domain ddef1 set default '42'
;
alter table test_trgm alter column t type varchar(768)
;
/* contrib/seg/seg--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Create the user-defined type for 1-D floating point intervals (seg)

CREATE FUNCTION seg_in(cstring)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE tab_upgraded1(id int)
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c7 OPTIONS (ADD p1 'v1', ADD p2 'v2'),
                        ALTER COLUMN c8 OPTIONS (ADD p1 'v1', ADD p2 'v2')
;
create or replace function dump_update() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, old table = %, new table = %',
                 TG_NAME,
                 (select string_agg(old_table::text, ', ' order by a) from old_table),
                 (select string_agg(new_table::text, ', ' order by a) from new_table);
    return null;
  end;
$$
;
CREATE TABLE "public"."prt2_m_p2" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
create type insenum as enum ('L1', 'L2')
;
VACUUM (PROCESS_TOAST FALSE, FULL) vac_option_tab
;
create table part_cab (c int not null, a int not null, b int not null) partition by list (c)
;
create table coercepart_cd partition of coercepart for values in ('cd')
;
CREATE TRIGGER some_trig_afterb AFTER UPDATE ON some_t FOR EACH ROW
  WHEN (NOT NEW.some_col)
  EXECUTE PROCEDURE dummy_update_func('afterb')
;
ALTER TABLE ONLY "public"."ec2"
    ADD CONSTRAINT "ec2_pkey" PRIMARY KEY ("xf")
;
DROP COLLATION IF EXISTS test_collation_exists
;
CREATE FUNCTION test_shm_mq(queue_size pg_catalog.int8,
					   message pg_catalog.text,
					   repeat_count pg_catalog.int4 default 1,
					   num_workers pg_catalog.int4 default 1)
    RETURNS pg_catalog.void STRICT
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE COLLATION regress_builtin_c (
  provider = builtin, locale = 'C')
;
DROP PROCEDURE priv_testproc1(int)
;
ANALYZE public.box_tbl
;
CREATE TABLE "fkpart3"."pk3" (
    "a" integer NOT NULL
)
;
CREATE TABLE split_cursor_next.items (id integer PRIMARY KEY, note text)
;
-- failed (not owner)
ALTER OPERATOR FAMILY alt_opf2 USING hash SET SCHEMA alt_nsp2
;
ALTER TABLE itest14 ALTER id DROP DEFAULT
;
CREATE TABLE fk_partitioned_fk_1 PARTITION OF fk_partitioned_fk FOR VALUES IN (NULL,500,501,502)
;
CREATE INDEX idx_varchar ON test_varchar USING gin (i)
;
CREATE OR REPLACE FUNCTION rngfuncr(in f1 int, out f2 int, out text)
AS $$select $1-1, $1::text || 'z'$$ LANGUAGE sql
;
DROP DOMAIN priv_testdomain3b
;
ALTER CONVERSION split_conversion_probe.latin1_to_utf8_renamed SET SCHEMA split_conversion_probe_next
;
DROP TRIGGER IF EXISTS test_trigger_exists ON no_such_schema.no_such_table
;
DROP TABLE public.pg_attrdef
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc7(1) AS
(rngfuncid int, rngfuncsubid int, rngfuncname text)
;
drop table parted_index_col_drop
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0
;
drop table agg_group_2
;
DROP SCHEMA testschema CASCADE
;
DROP TABLE testpub_merge_pk
;
alter table cnn_uq add unique (a)
;
/*
 * 5.32
 * FIELDS view
 */

-- feature not supported


/*
 * 5.33
 * KEY_COLUMN_USAGE view
 */

CREATE VIEW key_column_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc_nspname AS sql_identifier) AS constraint_schema,
           CAST(conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr_nspname AS sql_identifier) AS table_schema,
           CAST(relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(CASE WHEN contype = 'f' THEN
                       _pg_index_position(ss.conindid, ss.confkey[(ss.x).n])
                     ELSE NULL
                END AS cardinal_number)
             AS position_in_unique_constraint
    FROM pg_attribute a,
         (SELECT r.oid AS roid, r.relname, r.relowner,
                 nc.nspname AS nc_nspname, nr.nspname AS nr_nspname,
                 c.oid AS coid, c.conname, c.contype, c.conindid,
                 c.confkey, c.confrelid,
                 _pg_expandarray(c.conkey) AS x
          FROM pg_namespace nr, pg_class r, pg_namespace nc,
               pg_constraint c
          WHERE nr.oid = r.relnamespace
                AND r.oid = c.conrelid
                AND nc.oid = c.connamespace
                AND c.contype IN ('p', 'u', 'f')
                AND r.relkind IN ('r', 'p')
                AND (NOT pg_is_other_temp_schema(nr.oid)) ) AS ss
    WHERE ss.roid = a.attrelid
          AND a.attnum = (ss.x).x
          AND NOT a.attisdropped
          AND (pg_has_role(relowner, 'USAGE')
               OR has_column_privilege(roid, a.attnum,
                                       'SELECT, INSERT, UPDATE, REFERENCES'))
;
DROP TABLE replication_example
;
ALTER SUBSCRIPTION tap_sub CONNECTION 'port=12066 host=/tmp/Ia3hZucwmd dbname=postgres sslmode=disable'
;
ALTER EXTENSION hstore ADD DOMAIN split_extension_next.ext_code
;
CREATE INDEX t1_btree ON s5.t1 USING BTREE (i)
;
CREATE SUBSCRIPTION sub3 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub3
;
CREATE INDEX t1_btree ON s1.t1 USING BTREE (i)
;
CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION nonexistent
;
analyze j2
;
alter table atacc2 inherit atacc2
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] --
-- Ensure that some values are uncompressed, to test the faster substring
-- operation used in that case
--
alter table toasttest alter column f1 set storage external
;
----------
[ADMIN_TABLE] ANALYZE public.thethings
;
----------
[CREATE_TABLE] -- Nor should this... (same reason, we have 4,3 referencing 1,2 which mismatches types
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest4,
ptest3) REFERENCES pktable(ptest1, ptest2))
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table DISABLE TRIGGER base_table_bi
;
----------
[ALTER_TYPE] alter domain ddef1 set default '42'
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table test_trgm alter column t type varchar(768)
;
----------
[CREATE_PROG_OBJ] /* contrib/seg/seg--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Create the user-defined type for 1-D floating point intervals (seg)

CREATE FUNCTION seg_in(cstring)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE tab_upgraded1(id int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c7 OPTIONS (ADD p1 'v1', ADD p2 'v2'),
                        ALTER COLUMN c8 OPTIONS (ADD p1 'v1', ADD p2 'v2')
;
----------
[CREATE_PROG_OBJ] create or replace function dump_update() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, old table = %, new table = %',
                 TG_NAME,
                 (select string_agg(old_table::text, ', ' order by a) from old_table),
                 (select string_agg(new_table::text, ', ' order by a) from new_table);
    return null;
  end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_m_p2" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[CREATE_TYPE] create type insenum as enum ('L1', 'L2')
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, FULL) vac_option_tab
;
----------
[CREATE_TABLE] create table part_cab (c int not null, a int not null, b int not null) partition by list (c)
;
----------
[CREATE_TABLE] create table coercepart_cd partition of coercepart for values in ('cd')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER some_trig_afterb AFTER UPDATE ON some_t FOR EACH ROW
  WHEN (NOT NEW.some_col)
  EXECUTE PROCEDURE dummy_update_func('afterb')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."ec2"
    ADD CONSTRAINT "ec2_pkey" PRIMARY KEY ("xf")
;
----------
[DROP_POLICY] DROP COLLATION IF EXISTS test_collation_exists
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_shm_mq(queue_size pg_catalog.int8,
					   message pg_catalog.text,
					   repeat_count pg_catalog.int4 default 1,
					   num_workers pg_catalog.int4 default 1)
    RETURNS pg_catalog.void STRICT
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_POLICY] CREATE COLLATION regress_builtin_c (
  provider = builtin, locale = 'C')
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE priv_testproc1(int)
;
----------
[ADMIN_TABLE] ANALYZE public.box_tbl
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk3" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE split_cursor_next.items (id integer PRIMARY KEY, note text)
;
----------
[ALTER_PROG_OBJ] -- failed (not owner)
ALTER OPERATOR FAMILY alt_opf2 USING hash SET SCHEMA alt_nsp2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest14 ALTER id DROP DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_1 PARTITION OF fk_partitioned_fk FOR VALUES IN (NULL,500,501,502)
;
----------
[ADD_INDEX] CREATE INDEX idx_varchar ON test_varchar USING gin (i)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION rngfuncr(in f1 int, out f2 int, out text)
AS $$select $1-1, $1::text || 'z'$$ LANGUAGE sql
;
----------
[DROP_TYPE] DROP DOMAIN priv_testdomain3b
;
----------
[ALTER_POLICY] ALTER CONVERSION split_conversion_probe.latin1_to_utf8_renamed SET SCHEMA split_conversion_probe_next
;
----------
[DROP_TRIGGER] DROP TRIGGER IF EXISTS test_trigger_exists ON no_such_schema.no_such_table
;
----------
[DROP_TABLE] DROP TABLE public.pg_attrdef
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc7(1) AS
(rngfuncid int, rngfuncsubid int, rngfuncname text)
;
----------
[DROP_TABLE] drop table parted_index_col_drop
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0
;
----------
[DROP_TABLE] drop table agg_group_2
;
----------
[DROP_SCHEMA] DROP SCHEMA testschema CASCADE
;
----------
[DROP_TABLE] DROP TABLE testpub_merge_pk
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table cnn_uq add unique (a)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.32
 * FIELDS view
 */

-- feature not supported


/*
 * 5.33
 * KEY_COLUMN_USAGE view
 */

CREATE VIEW key_column_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc_nspname AS sql_identifier) AS constraint_schema,
           CAST(conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr_nspname AS sql_identifier) AS table_schema,
           CAST(relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(CASE WHEN contype = 'f' THEN
                       _pg_index_position(ss.conindid, ss.confkey[(ss.x).n])
                     ELSE NULL
                END AS cardinal_number)
             AS position_in_unique_constraint
    FROM pg_attribute a,
         (SELECT r.oid AS roid, r.relname, r.relowner,
                 nc.nspname AS nc_nspname, nr.nspname AS nr_nspname,
                 c.oid AS coid, c.conname, c.contype, c.conindid,
                 c.confkey, c.confrelid,
                 _pg_expandarray(c.conkey) AS x
          FROM pg_namespace nr, pg_class r, pg_namespace nc,
               pg_constraint c
          WHERE nr.oid = r.relnamespace
                AND r.oid = c.conrelid
                AND nc.oid = c.connamespace
                AND c.contype IN ('p', 'u', 'f')
                AND r.relkind IN ('r', 'p')
                AND (NOT pg_is_other_temp_schema(nr.oid)) ) AS ss
    WHERE ss.roid = a.attrelid
          AND a.attnum = (ss.x).x
          AND NOT a.attisdropped
          AND (pg_has_role(relowner, 'USAGE')
               OR has_column_privilege(roid, a.attnum,
                                       'SELECT, INSERT, UPDATE, REFERENCES'))
;
----------
[DROP_TABLE] DROP TABLE replication_example
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub CONNECTION 'port=12066 host=/tmp/Ia3hZucwmd dbname=postgres sslmode=disable'
;
----------
[ALTER_LIBRARY] ALTER EXTENSION hstore ADD DOMAIN split_extension_next.ext_code
;
----------
[ADD_INDEX] CREATE INDEX t1_btree ON s5.t1 USING BTREE (i)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub3 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub3
;
----------
[ADD_INDEX] CREATE INDEX t1_btree ON s1.t1 USING BTREE (i)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION nonexistent
;
----------
[ADMIN_TABLE] analyze j2
;
----------
[ALTER_TABLE] alter table atacc2 inherit atacc2
;
