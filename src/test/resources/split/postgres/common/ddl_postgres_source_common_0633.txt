create trigger trig_del_after_parent after delete on parted_stmt_trig
  for each row execute procedure trigger_notice()
;
ALTER TABLE pg_extension ADD UNIQUE USING INDEX pg_extension_name_index
;
SECURITY LABEL ON COLUMN dummy_seclabel_tbl1.a IS 'unclassified'
;
ANALYZE pagg_tab1
;
CREATE OPERATOR FAMILY alt_nsp6.alt_opf6 USING btree
;
-- test irregular partitions (i.e., different column definitions),
-- including that the WHEN clause works
create function bark(text) returns bool language plpgsql immutable
  as $$ begin raise notice '% <- woof!', $1; return true; end; $$
;
-- rule
DROP RULE test_rule_exists ON test_exists
;
ALTER TABLE ONLY "public"."plt1" ATTACH PARTITION "public"."plt1_p3" DEFAULT
;
CREATE FUNCTION isneq(isbn13, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create function return_refcursor(rc refcursor) returns refcursor as $$
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
DROP EXTENSION pg_stat_statements
;
ALTER TABLE fk_partitioned_fk_1 DROP COLUMN fdrop1, DROP COLUMN fdrop2, DROP COLUMN fdrop3
;
ALTER TYPE tt_t0 DROP ATTRIBUTE z
;
create table parted_tab (a int, b char) partition by list (a)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
create or replace function raise_test() returns void as $$
begin
  raise division_by_zero using message = 'custom' || ' message', errcode = '22012';
end;
$$ language plpgsql
;
ALTER TABLE pg_toast.pg_toast_33462 RENAME TO reind_con_toast
;
VACUUM (FULL) pg_catalog.pg_publication_namespace
;
-- Ensure that we do not select the aggregate pathkeys instead of the grouping
-- pathkeys
CREATE TABLE group_agg_pk AS SELECT
  i % 10 AS x,
  i % 2 AS y,
  i % 2 AS z,
  2 AS w,
  i % 10 AS f
FROM generate_series(1,100) AS i
;
-- check that attaching a partition correctly reports any rows in the default
-- partition that should not be there for the new partition to be attached
-- successfully
create table defpart_attach_test_2 (like defpart_attach_test_d)
;
CREATE FUNCTION "public"."event_trigger_report_end"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'END: command_tag=% type=% identity=%',
            r.command_tag, r.object_type, r.object_identity;
    END LOOP;
END; $$
;
DO $d$
    BEGIN
        EXECUTE $$ALTER SERVER loopback_nopw OPTIONS (ADD password 'dummypw')$$;
    END;
$d$
;
DROP INDEX sj_fn_idx, sj_temp_idx1, sj_temp_idx
;
ALTER PUBLICATION pub_for_all_tables RENAME TO tap_pub_tmp
;
DROP TABLE drop_tbl
;
/*
 * text search configuration for hungarian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hungarian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
CREATE TABLE enumtest_parent (id rainbow PRIMARY KEY)
;
ALTER TABLE ONLY "public"."f_star" INHERIT "public"."e_star"
;
CREATE TABLE pt_part1 PARTITION OF partitioned_table FOR VALUES IN (1)
;
CREATE FUNCTION citext_hash(citext)
RETURNS int4
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE pg_temp_3.pg_temp_16649_2 ADD COLUMN newdata pg_temp_3.pg_temp_16649
;
CREATE OR REPLACE RULE y_rule AS ON INSERT TO y DO ALSO NOTIFY foo
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_class
;
--
-- CREATE AGGREGATE

-- sfunc/finalfunc type disagreement
create aggregate newavg2 (sfunc = int4pl,
			  basetype = int4,
			  stype = int4,
			  finalfunc = int2um,
			  initcond = '0')
;
drop function f1(x anyelement)
;
-- fails because a foreign key from a permanent table exists
ALTER TABLE logged3 SET UNLOGGED
;
comment on constraint at_partitioned_id_name_key on at_partitioned is 'parent constraint'
;
alter table tpart attach partition tpart_default default
;
-- Verify that we can lock a table with inheritance children.
CREATE TABLE lock_tbl2 (b BIGINT) INHERITS (lock_tbl1)
;
create or replace function conflict_test() returns setof int8_tbl as $$
#variable_conflict use_column
declare r record;
  q1 bigint := 42;
begin
  for r in select q1,q2 from int8_tbl loop
    return next r;
  end loop;
end;
$$ language plpgsql
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_ts_config_map
;
create function arrayassign1() returns text[] language plpgsql as $$
declare
 r record;
begin
  r := row(12, '{foo,bar,baz}')::rtype;
  r.ar[2] := 'replace';
  return r.ar;
end$$
;
create view v2a as select * from (tt2 join tt3 using (b,c) join tt4 using (b)) j
;
CREATE FUNCTION pg_buffercache_summary(
    OUT buffers_used int4,
    OUT buffers_unused int4,
    OUT buffers_dirty int4,
    OUT buffers_pinned int4,
    OUT usagecount_avg float8)
AS '$libdir/pg_buffercache', 'pg_buffercache_summary'
LANGUAGE C PARALLEL SAFE
;
-- fail
DROP COLLATION test0 CASCADE
;
TRUNCATE atest3
;
create table gs_group_1 as
select g100, g10, sum(g::numeric), count(*), max(g::text)
from gs_data_1 group by cube (g1000, g100,g10)
;
CREATE INDEX comment_test_index ON public.comment_test USING btree (indexed_col)
;
-- fail, need lc_ctype
CREATE COLLATION testx (provider = icu, locale = 'nonsense')
;
DROP TABLE pg_temp_13.pg_temp_26616_2, pg_temp_13.pg_temp_26616
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_del_after_parent after delete on parted_stmt_trig
  for each row execute procedure trigger_notice()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_extension ADD UNIQUE USING INDEX pg_extension_name_index
;
----------
[SECURITY_LABEL] SECURITY LABEL ON COLUMN dummy_seclabel_tbl1.a IS 'unclassified'
;
----------
[ADMIN_TABLE] ANALYZE pagg_tab1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_nsp6.alt_opf6 USING btree
;
----------
[CREATE_PROG_OBJ] -- test irregular partitions (i.e., different column definitions),
-- including that the WHEN clause works
create function bark(text) returns bool language plpgsql immutable
  as $$ begin raise notice '% <- woof!', $1; return true; end; $$
;
----------
[DROP_POLICY] -- rule
DROP RULE test_rule_exists ON test_exists
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."plt1" ATTACH PARTITION "public"."plt1_p3" DEFAULT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(isbn13, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function return_refcursor(rc refcursor) returns refcursor as $$
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
----------
[DROP_LIBRARY] DROP EXTENSION pg_stat_statements
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk_partitioned_fk_1 DROP COLUMN fdrop1, DROP COLUMN fdrop2, DROP COLUMN fdrop3
;
----------
[ALTER_TYPE] ALTER TYPE tt_t0 DROP ATTRIBUTE z
;
----------
[CREATE_TABLE] create table parted_tab (a int, b char) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise division_by_zero using message = 'custom' || ' message', errcode = '22012';
end;
$$ language plpgsql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33462 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_publication_namespace
;
----------
[CREATE_TABLE|SELECT] -- Ensure that we do not select the aggregate pathkeys instead of the grouping
-- pathkeys
CREATE TABLE group_agg_pk AS SELECT
  i % 10 AS x,
  i % 2 AS y,
  i % 2 AS z,
  2 AS w,
  i % 10 AS f
FROM generate_series(1,100) AS i
;
----------
[CREATE_TABLE] -- check that attaching a partition correctly reports any rows in the default
-- partition that should not be there for the new partition to be attached
-- successfully
create table defpart_attach_test_2 (like defpart_attach_test_d)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."event_trigger_report_end"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'END: command_tag=% type=% identity=%',
            r.command_tag, r.object_type, r.object_identity;
    END LOOP;
END; $$
;
----------
[BLOCK] DO $d$
    BEGIN
        EXECUTE $$ALTER SERVER loopback_nopw OPTIONS (ADD password 'dummypw')$$;
    END;
$d$
;
----------
[DROP_INDEX] DROP INDEX sj_fn_idx, sj_temp_idx1, sj_temp_idx
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub_for_all_tables RENAME TO tap_pub_tmp
;
----------
[DROP_TABLE] DROP TABLE drop_tbl
;
----------
[CREATE_POLICY] /*
 * text search configuration for hungarian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hungarian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
----------
[CREATE_TABLE] CREATE TABLE enumtest_parent (id rainbow PRIMARY KEY)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."f_star" INHERIT "public"."e_star"
;
----------
[CREATE_TABLE] CREATE TABLE pt_part1 PARTITION OF partitioned_table FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_hash(citext)
RETURNS int4
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_16649_2 ADD COLUMN newdata pg_temp_3.pg_temp_16649
;
----------
[CREATE_POLICY] CREATE OR REPLACE RULE y_rule AS ON INSERT TO y DO ALSO NOTIFY foo
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_class
;
----------
[CREATE_PROG_OBJ] --
-- CREATE AGGREGATE

-- sfunc/finalfunc type disagreement
create aggregate newavg2 (sfunc = int4pl,
			  basetype = int4,
			  stype = int4,
			  finalfunc = int2um,
			  initcond = '0')
;
----------
[DROP_PROG_OBJ] drop function f1(x anyelement)
;
----------
[ALTER_TABLE] -- fails because a foreign key from a permanent table exists
ALTER TABLE logged3 SET UNLOGGED
;
----------
[COMMENT_CONSTRAINT] comment on constraint at_partitioned_id_name_key on at_partitioned is 'parent constraint'
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table tpart attach partition tpart_default default
;
----------
[CREATE_TABLE] -- Verify that we can lock a table with inheritance children.
CREATE TABLE lock_tbl2 (b BIGINT) INHERITS (lock_tbl1)
;
----------
[CREATE_PROG_OBJ] create or replace function conflict_test() returns setof int8_tbl as $$
#variable_conflict use_column
declare r record;
  q1 bigint := 42;
begin
  for r in select q1,q2 from int8_tbl loop
    return next r;
  end loop;
end;
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_ts_config_map
;
----------
[CREATE_PROG_OBJ] create function arrayassign1() returns text[] language plpgsql as $$
declare
 r record;
begin
  r := row(12, '{foo,bar,baz}')::rtype;
  r.ar[2] := 'replace';
  return r.ar;
end$$
;
----------
[CREATE_VIEW(SELECT)] create view v2a as select * from (tt2 join tt3 using (b,c) join tt4 using (b)) j
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_buffercache_summary(
    OUT buffers_used int4,
    OUT buffers_unused int4,
    OUT buffers_dirty int4,
    OUT buffers_pinned int4,
    OUT usagecount_avg float8)
AS '$libdir/pg_buffercache', 'pg_buffercache_summary'
LANGUAGE C PARALLEL SAFE
;
----------
[DROP_POLICY] -- fail
DROP COLLATION test0 CASCADE
;
----------
[TRUNCATE_TABLE] TRUNCATE atest3
;
----------
[CREATE_TABLE|SELECT] create table gs_group_1 as
select g100, g10, sum(g::numeric), count(*), max(g::text)
from gs_data_1 group by cube (g1000, g100,g10)
;
----------
[ADD_INDEX] CREATE INDEX comment_test_index ON public.comment_test USING btree (indexed_col)
;
----------
[CREATE_POLICY] -- fail, need lc_ctype
CREATE COLLATION testx (provider = icu, locale = 'nonsense')
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26616_2, pg_temp_13.pg_temp_26616
;
