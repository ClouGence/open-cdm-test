CREATE UNIQUE INDEX anindex ON testschema.atable(column1)
;
ALTER SUBSCRIPTION split_sub CONNECTION 'dbname=postgres'
;
DROP TABLE pg_temp_10.pg_temp_25428_2, pg_temp_10.pg_temp_25428
;
-- The target table is partitioned in the same way, but this time by attaching
-- partitions which have columns in different order, dropped columns etc.
CREATE TABLE pa_target (tid integer, balance float, val text)
	PARTITION BY LIST (tid)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_policy
;
create function test_table_func_row() returns setof found_test_tbl as '
DECLARE
	row found_test_tbl%ROWTYPE;
BEGIN
	FOR row IN select * from found_test_tbl LOOP
		RETURN NEXT row;
	END LOOP;
	RETURN;
END;' language plpgsql
;
-- commands order has no importance
alter table atacc1 alter test_b set not null, alter test_a set not null
;
CREATE FUNCTION myvarcharin(cstring, oid, integer) RETURNS myvarchar
LANGUAGE internal IMMUTABLE PARALLEL SAFE STRICT AS 'varcharin'
;
--
-- COMMENT_ON
--

COMMENT ON SCHEMA foo IS 'This is schema foo'
;
DROP TABLE tab_primary_key
;
DROP TABLE pa_source
;
ANALYZE public.brin_summarize
;
ALTER INDEX pg_toast.pg_toast_30352_index RENAME TO reind_con_toast_idx
;
ALTER TABLE pg_temp_4.pg_temp_29544_2 ADD COLUMN newdata pg_temp_4.pg_temp_29544
;
ALTER TABLE test_tsquery ADD COLUMN keyword tsquery
;
ALTER INDEX "fkpart5"."pk3_pkey" ATTACH PARTITION "fkpart5"."pk31_pkey"
;
CREATE TRIGGER after_upd_a_b_row_trig AFTER UPDATE OF a, b ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('after_upd_a_b_row')
;
ALTER TABLE ONLY tab1_2 ENABLE REPLICA TRIGGER sub2_tab1_2_log_op_trigger
;
CREATE VIEW split_view_probe.base_view (id, qty, note) WITH (security_barrier = true, security_invoker = true) AS SELECT id, qty, note FROM split_view_probe.base_table WHERE qty > 0 WITH LOCAL CHECK OPTION
;
ALTER FOREIGN TABLE fd_pt2_1 ADD CONSTRAINT fd_pt2chk1 CHECK (c1 > 0)
;
CREATE TABLESPACE ts1 LOCATION '/tmp/rU53qTmPvY/ts'
;
CREATE OR REPLACE FUNCTION regexp_matches(string citext, pattern citext) RETURNS SETOF TEXT[] AS $$
    SELECT pg_catalog.regexp_matches( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE ROWS 1
;
CREATE FUNCTION double(x int) RETURNS int IMMUTABLE LANGUAGE SQL AS 'select x * 2'
;
-- Test that main query's relation's permissions are checked before
-- the rule action's relation's.
CREATE TABLE ruletest_t3 (x int)
;
-- Foreign partition DDL stuff
CREATE TABLE fd_pt2 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
) PARTITION BY LIST (c1)
;
CREATE TABLE "public"."gtest20" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    CONSTRAINT "gtest20_b_check" CHECK (("b" < 50))
)
;
CREATE INDEX test_index2 on testschema.test_default_tab_p (val) TABLESPACE regress_tblspace
;
ALTER TABLE ONLY "fkpart4"."droppk2" ATTACH PARTITION "fkpart4"."droppk2_d" DEFAULT
;
-- test that user trigger can be overwritten by one defined at upper level
create trigger my_trig
  after insert on parted_trig_1
  for each row execute procedure funcA()
;
-- ok
/* Empty view is a corner case that failed in 9.2. */
CREATE VIEW atestv0 AS SELECT 0 as x WHERE false
;
create table renameColumnAnother (c int) inherits (renameColumnChild)
;
CREATE TABLE "public"."depth1" (
    "c" "text"
)
;
--
-- regression test for problems of the sort depicted in bug #3588
--

create temp table xx (pkxx int)
;
CREATE TEXT SEARCH CONFIGURATION danish
	(PARSER = default)
;
CREATE TABLE part_p3 PARTITION OF list_parted FOR VALUES IN ((2+1))
;
CREATE FUNCTION testpub_rf_func2() RETURNS integer IMMUTABLE AS $$ BEGIN RETURN 123; END; $$ LANGUAGE plpgsql
;
drop operator = (int4, nonesuch)
;
ALTER TABLE ONLY "public"."prt1_l_p3" ATTACH PARTITION "public"."prt1_l_p3_p1" FOR VALUES FROM (0) TO (13)
;
-- Add the NULL partition that contains only NULL values as the key values
CREATE TABLE plt2_adv_p1_null PARTITION OF plt2_adv FOR VALUES IN (NULL)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_constraint
;
create table mcrparted0 partition of mcrparted for values from (minvalue, 0, 0) to (1, maxvalue, maxvalue)
;
alter table listp_12_1 set (parallel_workers = 0)
;
-- Test use of plpgsql in a domain check constraint (cf. bug #14414)

create function plpgsql_domain_check(val int) returns boolean as $$
begin return val > 0; end
$$ language plpgsql immutable
;
DROP TABLE loct2
;
ANALYZE public.mlparted12
;
-- fails
DROP VIEW tid_view_fake_ctid
;
CREATE TABLE "public"."constr_child2" (
    "a" integer CONSTRAINT "constr_parent2_a_not_null" NOT NULL
)
;
CREATE FUNCTION pg_get_wal_records_info_till_end_of_wal(IN start_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_records_info_till_end_of_wal'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER TABLE temporal_pk REPLICA IDENTITY FULL
;
drop function stricttest()
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE UNIQUE INDEX anindex ON testschema.atable(column1)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION split_sub CONNECTION 'dbname=postgres'
;
----------
[DROP_TABLE] DROP TABLE pg_temp_10.pg_temp_25428_2, pg_temp_10.pg_temp_25428
;
----------
[CREATE_TABLE] -- The target table is partitioned in the same way, but this time by attaching
-- partitions which have columns in different order, dropped columns etc.
CREATE TABLE pa_target (tid integer, balance float, val text)
	PARTITION BY LIST (tid)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_policy
;
----------
[CREATE_PROG_OBJ] create function test_table_func_row() returns setof found_test_tbl as '
DECLARE
	row found_test_tbl%ROWTYPE;
BEGIN
	FOR row IN select * from found_test_tbl LOOP
		RETURN NEXT row;
	END LOOP;
	RETURN;
END;' language plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- commands order has no importance
alter table atacc1 alter test_b set not null, alter test_a set not null
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION myvarcharin(cstring, oid, integer) RETURNS myvarchar
LANGUAGE internal IMMUTABLE PARALLEL SAFE STRICT AS 'varcharin'
;
----------
[COMMENT_SCHEMA] --
-- COMMENT_ON
--

COMMENT ON SCHEMA foo IS 'This is schema foo'
;
----------
[DROP_TABLE] DROP TABLE tab_primary_key
;
----------
[DROP_TABLE] DROP TABLE pa_source
;
----------
[ADMIN_TABLE] ANALYZE public.brin_summarize
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30352_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_29544_2 ADD COLUMN newdata pg_temp_4.pg_temp_29544
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_tsquery ADD COLUMN keyword tsquery
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart5"."pk3_pkey" ATTACH PARTITION "fkpart5"."pk31_pkey"
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER after_upd_a_b_row_trig AFTER UPDATE OF a, b ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('after_upd_a_b_row')
;
----------
[ALTER_TABLE] ALTER TABLE ONLY tab1_2 ENABLE REPLICA TRIGGER sub2_tab1_2_log_op_trigger
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW split_view_probe.base_view (id, qty, note) WITH (security_barrier = true, security_invoker = true) AS SELECT id, qty, note FROM split_view_probe.base_table WHERE qty > 0 WITH LOCAL CHECK OPTION
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER FOREIGN TABLE fd_pt2_1 ADD CONSTRAINT fd_pt2chk1 CHECK (c1 > 0)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/tmp/rU53qTmPvY/ts'
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_matches(string citext, pattern citext) RETURNS SETOF TEXT[] AS $$
    SELECT pg_catalog.regexp_matches( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE ROWS 1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION double(x int) RETURNS int IMMUTABLE LANGUAGE SQL AS 'select x * 2'
;
----------
[CREATE_TABLE] -- Test that main query's relation's permissions are checked before
-- the rule action's relation's.
CREATE TABLE ruletest_t3 (x int)
;
----------
[CREATE_TABLE] -- Foreign partition DDL stuff
CREATE TABLE fd_pt2 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
) PARTITION BY LIST (c1)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest20" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    CONSTRAINT "gtest20_b_check" CHECK (("b" < 50))
)
;
----------
[ADD_INDEX] CREATE INDEX test_index2 on testschema.test_default_tab_p (val) TABLESPACE regress_tblspace
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart4"."droppk2" ATTACH PARTITION "fkpart4"."droppk2_d" DEFAULT
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- test that user trigger can be overwritten by one defined at upper level
create trigger my_trig
  after insert on parted_trig_1
  for each row execute procedure funcA()
;
----------
[CREATE_VIEW(SELECT)] -- ok
/* Empty view is a corner case that failed in 9.2. */
CREATE VIEW atestv0 AS SELECT 0 as x WHERE false
;
----------
[CREATE_TABLE] create table renameColumnAnother (c int) inherits (renameColumnChild)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."depth1" (
    "c" "text"
)
;
----------
[CREATE_TABLE] --
-- regression test for problems of the sort depicted in bug #3588
--

create temp table xx (pkxx int)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION danish
	(PARSER = default)
;
----------
[CREATE_TABLE] CREATE TABLE part_p3 PARTITION OF list_parted FOR VALUES IN ((2+1))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testpub_rf_func2() RETURNS integer IMMUTABLE AS $$ BEGIN RETURN 123; END; $$ LANGUAGE plpgsql
;
----------
[DROP_PROG_OBJ] drop operator = (int4, nonesuch)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_l_p3" ATTACH PARTITION "public"."prt1_l_p3_p1" FOR VALUES FROM (0) TO (13)
;
----------
[CREATE_TABLE] -- Add the NULL partition that contains only NULL values as the key values
CREATE TABLE plt2_adv_p1_null PARTITION OF plt2_adv FOR VALUES IN (NULL)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_constraint
;
----------
[CREATE_TABLE] create table mcrparted0 partition of mcrparted for values from (minvalue, 0, 0) to (1, maxvalue, maxvalue)
;
----------
[ALTER_TABLE] alter table listp_12_1 set (parallel_workers = 0)
;
----------
[CREATE_PROG_OBJ] -- Test use of plpgsql in a domain check constraint (cf. bug #14414)

create function plpgsql_domain_check(val int) returns boolean as $$
begin return val > 0; end
$$ language plpgsql immutable
;
----------
[DROP_TABLE] DROP TABLE loct2
;
----------
[ADMIN_TABLE] ANALYZE public.mlparted12
;
----------
[DROP_VIEW] -- fails
DROP VIEW tid_view_fake_ctid
;
----------
[CREATE_TABLE] CREATE TABLE "public"."constr_child2" (
    "a" integer CONSTRAINT "constr_parent2_a_not_null" NOT NULL
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_get_wal_records_info_till_end_of_wal(IN start_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_records_info_till_end_of_wal'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE temporal_pk REPLICA IDENTITY FULL
;
----------
[DROP_PROG_OBJ] drop function stricttest()
;
