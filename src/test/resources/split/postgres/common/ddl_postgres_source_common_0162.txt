EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_schema.mv_withdata2 (a) AS
  SELECT generate_series(1, 10) WITH DATA
;
CREATE TABLE upd (id serial PRIMARY KEY, id2 int)
;
ALTER DATABASE "ecpg2_regression" SET lc_numeric TO 'C'
;
CREATE TABLE do_write(id serial primary key)
;
create database abc template = template1
;
-- concurrent detach
CREATE TABLE range_parted2 (
	a int
) PARTITION BY RANGE(a)
;
ALTER INDEX dummy_test_idx SET (option_int = true)
;
-- duplicate named parameter, should throw an error at parse time
create function namedparmcursor_test5() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77, p2 := 42);
end
$$ language plpgsql
;
VACUUM (ANALYZE) pg_catalog.pg_index
;
CREATE OPERATOR CLASS enum_ops
DEFAULT FOR TYPE anyenum USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       gin_enum_cmp(anyenum,anyenum),
    FUNCTION        2       gin_extract_value_anyenum(anyenum, internal),
    FUNCTION        3       gin_extract_query_anyenum(anyenum, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_anyenum(anyenum,anyenum,int2, internal),
STORAGE         anyenum
;
CREATE VIEW pg_stat_xact_user_tables AS
    SELECT * FROM pg_stat_xact_all_tables
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema') AND
          schemaname !~ '^pg_toast'
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_ts_config
;
-- Before row triggers aren't allowed on views
CREATE TRIGGER invalid_trig BEFORE INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_ins_row')
;
ALTER TABLE testjsonb SET (parallel_workers = 2)
;
CREATE TEXT SEARCH DICTIONARY split_ts_probe.simple_dict_two (TEMPLATE = simple)
;
CREATE TABLE TEMP_TIMESTAMP (f1 timestamp with time zone)
;
drop view base_tab_def_view
;
/* contrib/pageinspect/pageinspect--1.11--1.12.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- bt_multi_page_stats()
--
CREATE FUNCTION bt_multi_page_stats(IN relname text, IN blkno int8, IN blk_count int8,
    OUT blkno int8,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int8,
    OUT btpo_next int8,
    OUT btpo_level int8,
    OUT btpo_flags int4)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_multi_page_stats'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
drop trigger My_Table_Check_Trigger on My_Table
;
-- Attaching a child table with the same valid foreign key constraint.
CREATE TABLE fk_partitioned_fk_1 (a int, b int)
;
ALTER TABLE cj_source2 RENAME COLUMN sid2 TO sid
;
CREATE TYPE public.tst_comp_mix_t AS (
		a public.tst_comp_basic_t,
		b public.tst_comp_basic_t[],
		c public.tst_enum_t,
		d public.tst_enum_t[]
		)
;
CREATE FUNCTION rw_view1_aa(x rw_view1)
  RETURNS int AS $$ SELECT x.aa $$ LANGUAGE sql
;
do $$
declare v_test plpgsql_domain := 1;
begin
  v_test := 0;  -- fail
end;
$$
;
create trigger parent_upd_trig before update on parent
  for each row execute procedure parent_upd_func()
;
CREATE TABLE test_tab2(a int)
;
create rule shipped_view_update as on update to shipped_view do instead
    update shipped set partnum = new.partnum, value = new.value
        where ttype = new.ttype and ordnum = new.ordnum
;
-- only parent is listed as being in publication, not the partition
ALTER PUBLICATION testpub_forparted ADD TABLE testpub_parted
;
ALTER TEXT SEARCH CONFIGURATION nepali ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH nepali_stem
;
create table mcrparted (a int, b int, c int) partition by range (a, abs(b), c)
;
create table misc_table(f1 int)
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', on_error 'ignore')
;
CREATE OPERATOR <> (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_ne,
        COMMUTATOR = '<>',
	NEGATOR = '=',
        RESTRICT = neqsel,
	JOIN = neqjoinsel
)
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26722_2 (tid pg_catalog.tid)
;
do $$
declare x var_record_colnn := row(1,2);
begin
  raise notice 'x = %', x;
  x := null;  -- fail
end$$
;
CREATE PUBLICATION regress_pub2_gen_to_nogen FOR TABLE tab_gen_to_nogen WITH (publish_generated_columns = stored)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
VACUUM FREEZE ANALYZE taby
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_type
;
drop function pl_qual_names(int)
;
ALTER TABLE pg_toast.pg_toast_30374 RENAME TO reind_con_toast
;
DROP TABLE ctoast
;
DROP TABLE owned_by_me, not_owned_by_me
;
CREATE TABLE "public"."tab_upgraded2" (
    "id" integer
)
;
ALTER TABLE split_alter_table.part_parent DETACH PARTITION split_alter_table.part_eu
;
-- fail

drop function testrngfunc()
;
-- example from documentation

CREATE TEMP TABLE products (product_id int, name text, price numeric)
;
ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (ean13, isbn13),
	OPERATOR 1  < (ean13, ismn13),
	OPERATOR 1  < (ean13, issn13),
	OPERATOR 1  < (ean13, isbn),
	OPERATOR 1  < (ean13, ismn),
	OPERATOR 1  < (ean13, issn),
	OPERATOR 1  < (ean13, upc),
	OPERATOR 2  <= (ean13, isbn13),
	OPERATOR 2  <= (ean13, ismn13),
	OPERATOR 2  <= (ean13, issn13),
	OPERATOR 2  <= (ean13, isbn),
	OPERATOR 2  <= (ean13, ismn),
	OPERATOR 2  <= (ean13, issn),
	OPERATOR 2  <= (ean13, upc),
	OPERATOR 3  = (ean13, isbn13),
	OPERATOR 3  = (ean13, ismn13),
	OPERATOR 3  = (ean13, issn13),
	OPERATOR 3  = (ean13, isbn),
	OPERATOR 3  = (ean13, ismn),
	OPERATOR 3  = (ean13, issn),
	OPERATOR 3  = (ean13, upc),
	OPERATOR 4  >= (ean13, isbn13),
	OPERATOR 4  >= (ean13, ismn13),
	OPERATOR 4  >= (ean13, issn13),
	OPERATOR 4  >= (ean13, isbn),
	OPERATOR 4  >= (ean13, ismn),
	OPERATOR 4  >= (ean13, issn),
	OPERATOR 4  >= (ean13, upc),
	OPERATOR 5  > (ean13, isbn13),
	OPERATOR 5  > (ean13, ismn13),
	OPERATOR 5  > (ean13, issn13),
	OPERATOR 5  > (ean13, isbn),
	OPERATOR 5  > (ean13, ismn),
	OPERATOR 5  > (ean13, issn),
	OPERATOR 5  > (ean13, upc),
	FUNCTION 1  btean13cmp(ean13, isbn13),
	FUNCTION 1  btean13cmp(ean13, ismn13),
	FUNCTION 1  btean13cmp(ean13, issn13),
	FUNCTION 1  btean13cmp(ean13, isbn),
	FUNCTION 1  btean13cmp(ean13, ismn),
	FUNCTION 1  btean13cmp(ean13, issn),
	FUNCTION 1  btean13cmp(ean13, upc)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_amop
;
-- changing an identity column to a non-identity column in a partitioned table
ALTER TABLE pitest3_p1 ALTER COLUMN f3 DROP IDENTITY
;
------------------------------------------------------------------------------------------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_schema.mv_withdata2 (a) AS
  SELECT generate_series(1, 10) WITH DATA
;
----------
[CREATE_TABLE] CREATE TABLE upd (id serial PRIMARY KEY, id2 int)
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg2_regression" SET lc_numeric TO 'C'
;
----------
[CREATE_TABLE] CREATE TABLE do_write(id serial primary key)
;
----------
[CREATE_CATALOG] create database abc template = template1
;
----------
[CREATE_TABLE] -- concurrent detach
CREATE TABLE range_parted2 (
	a int
) PARTITION BY RANGE(a)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_int = true)
;
----------
[CREATE_PROG_OBJ] -- duplicate named parameter, should throw an error at parse time
create function namedparmcursor_test5() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77, p2 := 42);
end
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_index
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS enum_ops
DEFAULT FOR TYPE anyenum USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       gin_enum_cmp(anyenum,anyenum),
    FUNCTION        2       gin_extract_value_anyenum(anyenum, internal),
    FUNCTION        3       gin_extract_query_anyenum(anyenum, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_anyenum(anyenum,anyenum,int2, internal),
STORAGE         anyenum
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_xact_user_tables AS
    SELECT * FROM pg_stat_xact_all_tables
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema') AND
          schemaname !~ '^pg_toast'
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_ts_config
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Before row triggers aren't allowed on views
CREATE TRIGGER invalid_trig BEFORE INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_ins_row')
;
----------
[ALTER_TABLE] ALTER TABLE testjsonb SET (parallel_workers = 2)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY split_ts_probe.simple_dict_two (TEMPLATE = simple)
;
----------
[CREATE_TABLE] CREATE TABLE TEMP_TIMESTAMP (f1 timestamp with time zone)
;
----------
[DROP_VIEW] drop view base_tab_def_view
;
----------
[CREATE_PROG_OBJ] /* contrib/pageinspect/pageinspect--1.11--1.12.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- bt_multi_page_stats()
--
CREATE FUNCTION bt_multi_page_stats(IN relname text, IN blkno int8, IN blk_count int8,
    OUT blkno int8,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int8,
    OUT btpo_next int8,
    OUT btpo_level int8,
    OUT btpo_flags int4)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_multi_page_stats'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[DROP_TRIGGER] drop trigger My_Table_Check_Trigger on My_Table
;
----------
[CREATE_TABLE] -- Attaching a child table with the same valid foreign key constraint.
CREATE TABLE fk_partitioned_fk_1 (a int, b int)
;
----------
[RENAME_COLUMN] ALTER TABLE cj_source2 RENAME COLUMN sid2 TO sid
;
----------
[CREATE_TYPE] CREATE TYPE public.tst_comp_mix_t AS (
		a public.tst_comp_basic_t,
		b public.tst_comp_basic_t[],
		c public.tst_enum_t,
		d public.tst_enum_t[]
		)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rw_view1_aa(x rw_view1)
  RETURNS int AS $$ SELECT x.aa $$ LANGUAGE sql
;
----------
[BLOCK] do $$
declare v_test plpgsql_domain := 1;
begin
  v_test := 0;  -- fail
end;
$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parent_upd_trig before update on parent
  for each row execute procedure parent_upd_func()
;
----------
[CREATE_TABLE] CREATE TABLE test_tab2(a int)
;
----------
[CREATE_POLICY] create rule shipped_view_update as on update to shipped_view do instead
    update shipped set partnum = new.partnum, value = new.value
        where ttype = new.ttype and ordnum = new.ordnum
;
----------
[ALTER_PUB_SUB] -- only parent is listed as being in publication, not the partition
ALTER PUBLICATION testpub_forparted ADD TABLE testpub_parted
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION nepali ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH nepali_stem
;
----------
[CREATE_TABLE] create table mcrparted (a int, b int, c int) partition by range (a, abs(b), c)
;
----------
[CREATE_TABLE] create table misc_table(f1 int)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', on_error 'ignore')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_ne,
        COMMUTATOR = '<>',
	NEGATOR = '=',
        RESTRICT = neqsel,
	JOIN = neqjoinsel
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26722_2 (tid pg_catalog.tid)
;
----------
[BLOCK] do $$
declare x var_record_colnn := row(1,2);
begin
  raise notice 'x = %', x;
  x := null;  -- fail
end$$
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION regress_pub2_gen_to_nogen FOR TABLE tab_gen_to_nogen WITH (publish_generated_columns = stored)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ADMIN_TABLE] VACUUM FREEZE ANALYZE taby
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_type
;
----------
[DROP_PROG_OBJ] drop function pl_qual_names(int)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30374 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE ctoast
;
----------
[DROP_TABLE] DROP TABLE owned_by_me, not_owned_by_me
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tab_upgraded2" (
    "id" integer
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE split_alter_table.part_parent DETACH PARTITION split_alter_table.part_eu
;
----------
[DROP_PROG_OBJ] -- fail

drop function testrngfunc()
;
----------
[CREATE_TABLE] -- example from documentation

CREATE TEMP TABLE products (product_id int, name text, price numeric)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (ean13, isbn13),
	OPERATOR 1  < (ean13, ismn13),
	OPERATOR 1  < (ean13, issn13),
	OPERATOR 1  < (ean13, isbn),
	OPERATOR 1  < (ean13, ismn),
	OPERATOR 1  < (ean13, issn),
	OPERATOR 1  < (ean13, upc),
	OPERATOR 2  <= (ean13, isbn13),
	OPERATOR 2  <= (ean13, ismn13),
	OPERATOR 2  <= (ean13, issn13),
	OPERATOR 2  <= (ean13, isbn),
	OPERATOR 2  <= (ean13, ismn),
	OPERATOR 2  <= (ean13, issn),
	OPERATOR 2  <= (ean13, upc),
	OPERATOR 3  = (ean13, isbn13),
	OPERATOR 3  = (ean13, ismn13),
	OPERATOR 3  = (ean13, issn13),
	OPERATOR 3  = (ean13, isbn),
	OPERATOR 3  = (ean13, ismn),
	OPERATOR 3  = (ean13, issn),
	OPERATOR 3  = (ean13, upc),
	OPERATOR 4  >= (ean13, isbn13),
	OPERATOR 4  >= (ean13, ismn13),
	OPERATOR 4  >= (ean13, issn13),
	OPERATOR 4  >= (ean13, isbn),
	OPERATOR 4  >= (ean13, ismn),
	OPERATOR 4  >= (ean13, issn),
	OPERATOR 4  >= (ean13, upc),
	OPERATOR 5  > (ean13, isbn13),
	OPERATOR 5  > (ean13, ismn13),
	OPERATOR 5  > (ean13, issn13),
	OPERATOR 5  > (ean13, isbn),
	OPERATOR 5  > (ean13, ismn),
	OPERATOR 5  > (ean13, issn),
	OPERATOR 5  > (ean13, upc),
	FUNCTION 1  btean13cmp(ean13, isbn13),
	FUNCTION 1  btean13cmp(ean13, ismn13),
	FUNCTION 1  btean13cmp(ean13, issn13),
	FUNCTION 1  btean13cmp(ean13, isbn),
	FUNCTION 1  btean13cmp(ean13, ismn),
	FUNCTION 1  btean13cmp(ean13, issn),
	FUNCTION 1  btean13cmp(ean13, upc)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_amop
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- changing an identity column to a non-identity column in a partitioned table
ALTER TABLE pitest3_p1 ALTER COLUMN f3 DROP IDENTITY
;
