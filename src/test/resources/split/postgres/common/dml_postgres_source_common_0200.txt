INSERT INTO public.ths_p1 VALUES (6, 716)
;
INSERT INTO public.ths_p3 VALUES (1, 591)
;
INSERT INTO alive VALUES($$before-orderly-restart$$) RETURNING status
;
UPDATE sql_sizing
    SET supported_value = (SELECT typlen-1 FROM pg_catalog.pg_type WHERE typname = 'name'),
        comments = 'Might be less, depending on character set.'
    WHERE supported_value = 63
;
DELETE FROM table_with_unique_not_null WHERE data = 1
;
-- error

--
-- Check the equivalence of functional and column notation
--
insert into fullname values ('Joe', 'Blow')
;
insert into inserttest (f3.if1, f3.if2) values (1,'{foo}'), (2,'{bar}')
;
INSERT INTO TIMESTAMPTZ_TBL VALUES ('Feb 10 17:32:01 1997 -0800')
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"fkpart6"."pk12"'::pg_catalog.regclass AND
conname IN ('pk_a_not_null')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtesty"'::pg_catalog.regclass
;
UPDATE person SET is_project_manager = false WHERE person_id = 1
;
-- updates aren't supported
UPDATE agg SET a = 1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14675', relminmxid = '3'
WHERE oid = '"public"."transition_table_level1"'::pg_catalog.regclass
;
insert into insertconflicttest values (27, 'Prune') on conflict (key, upper(fruit)) do update set fruit = excluded.fruit
;
INSERT INTO tru_pk_ftable (SELECT x FROM generate_series(3,10) x)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14717', relminmxid = '3'
WHERE oid = '"public"."prt2_l_p3_p2"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (9, 329)
;
INSERT INTO public.ths_p3 VALUES (9, 109)
;
INSERT INTO arrtest2 VALUES(
  ARRAY[[[113,142],[1,147]]],
  ARRAY[1.1,1.2,1.3]::float8[],
  ARRAY[1.1,1.2,1.3],
  ARRAY[[['aaa','aab'],['aba','abb'],['aca','acb']],[['baa','bab'],['bba','bbb'],['bca','bcb']]],
  ARRAY['19620326','19931223','19970117']::timestamp[]
)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_m_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO public.parallel_mat_view SELECT (diff.newdata).* FROM pg_temp_3.pg_temp_29541_2 diff WHERE tid IS NULL
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17611', relminmxid = '3'
WHERE oid = '40007'
;
INSERT INTO itest1 OVERRIDING USER VALUE VALUES (10, 'xyz')
;
DELETE FROM onek
;
INSERT INTO public.ths_p3 VALUES (9, 499)
;
INSERT INTO brin_iso VALUES (1000)
;
INSERT INTO public.ths_p1 VALUES (2, 812)
;
insert into spgist_point_tbl (id, p)
select g, point(g*10, g*10) from generate_series(1, 10) g
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13490', relminmxid = '3'
WHERE oid = '34682'
;
insert into itrtest values (1, 'bar') on conflict (a) do update set b = excluded.b
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10512', relminmxid = '1'
WHERE oid = '"public"."rtest_order2"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '18073'
;
INSERT INTO temporal_partitioned (id, valid_at, name) VALUES
  ('[1,2)', daterange('2000-01-01', '2000-02-01'), 'one'),
  ('[1,2)', daterange('2000-02-01', '2000-03-01'), 'one'),
  ('[3,4)', daterange('2000-01-01', '2010-01-01'), 'three')
;
update D1 set id = id + 1
;
INSERT INTO savepoints VALUES (13)
;
DELETE FROM uctest WHERE f1 = 10
;
insert into rngfunc values (1,2), (3,4) returning *
;
insert into twoconstraints values(1, '((0,0),(1,1))')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18165', relminmxid = '3'
WHERE oid = '"public"."tt4"'::pg_catalog.regclass
;
UPDATE rw_view1 SET a=5 WHERE a=4
;
WITH rand_value AS (SELECT string_agg(fipshash(i::text),'') AS val FROM generate_series(1,60) s(i))
INSERT INTO brintest_3
SELECT val, val, val, val FROM rand_value
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13701', relminmxid = '3'
WHERE oid = '"public"."mic_inputs"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('4', 94)
;
INSERT INTO delete_test (a) VALUES (10)
;
DELETE FROM foo WHERE f1 = 5
  RETURNING (SELECT max(old.f4 + x) FROM generate_series(1, 10) x) old_max,
            (SELECT max(new.f4 + x) FROM generate_series(1, 10) x) new_max
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '19830', relminmxid = '3'
WHERE oid = '45244'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '38744'
;
INSERT INTO VARBIT_SHIFT_TABLE SELECT CAST(v || B'0000' AS BIT VARYING(12)) >>4 FROM VARBIT_SHIFT_TABLE
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1100', relminmxid = '1'
WHERE oid = '"public"."num_exp_power_10_ln"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (6, 16)
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 716)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 591)
;
----------
[INSERT] INSERT INTO alive VALUES($$before-orderly-restart$$) RETURNING status
;
----------
[UPDATE|SELECT|METADATA] UPDATE sql_sizing
    SET supported_value = (SELECT typlen-1 FROM pg_catalog.pg_type WHERE typname = 'name'),
        comments = 'Might be less, depending on character set.'
    WHERE supported_value = 63
;
----------
[DELETE] DELETE FROM table_with_unique_not_null WHERE data = 1
;
----------
[INSERT] -- error

--
-- Check the equivalence of functional and column notation
--
insert into fullname values ('Joe', 'Blow')
;
----------
[INSERT|SELECT] insert into inserttest (f3.if1, f3.if2) values (1,'{foo}'), (2,'{bar}')
;
----------
[INSERT] INSERT INTO TIMESTAMPTZ_TBL VALUES ('Feb 10 17:32:01 1997 -0800')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"fkpart6"."pk12"'::pg_catalog.regclass AND
conname IN ('pk_a_not_null')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtesty"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE person SET is_project_manager = false WHERE person_id = 1
;
----------
[UPDATE] -- updates aren't supported
UPDATE agg SET a = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14675', relminmxid = '3'
WHERE oid = '"public"."transition_table_level1"'::pg_catalog.regclass
;
----------
[MERGE] insert into insertconflicttest values (27, 'Prune') on conflict (key, upper(fruit)) do update set fruit = excluded.fruit
;
----------
[INSERT|SELECT] INSERT INTO tru_pk_ftable (SELECT x FROM generate_series(3,10) x)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14717', relminmxid = '3'
WHERE oid = '"public"."prt2_l_p3_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 329)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 109)
;
----------
[INSERT] INSERT INTO arrtest2 VALUES(
  ARRAY[[[113,142],[1,147]]],
  ARRAY[1.1,1.2,1.3]::float8[],
  ARRAY[1.1,1.2,1.3],
  ARRAY[[['aaa','aab'],['aba','abb'],['aca','acb']],[['baa','bab'],['bba','bbb'],['bca','bcb']]],
  ARRAY['19620326','19931223','19970117']::timestamp[]
)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_m_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT|SELECT] INSERT INTO public.parallel_mat_view SELECT (diff.newdata).* FROM pg_temp_3.pg_temp_29541_2 diff WHERE tid IS NULL
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17611', relminmxid = '3'
WHERE oid = '40007'
;
----------
[INSERT|SELECT] INSERT INTO itest1 OVERRIDING USER VALUE VALUES (10, 'xyz')
;
----------
[DELETE] DELETE FROM onek
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 499)
;
----------
[INSERT] INSERT INTO brin_iso VALUES (1000)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 812)
;
----------
[INSERT|SELECT] insert into spgist_point_tbl (id, p)
select g, point(g*10, g*10) from generate_series(1, 10) g
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '13490', relminmxid = '3'
WHERE oid = '34682'
;
----------
[MERGE] insert into itrtest values (1, 'bar') on conflict (a) do update set b = excluded.b
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10512', relminmxid = '1'
WHERE oid = '"public"."rtest_order2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '18073'
;
----------
[INSERT|SELECT] INSERT INTO temporal_partitioned (id, valid_at, name) VALUES
  ('[1,2)', daterange('2000-01-01', '2000-02-01'), 'one'),
  ('[1,2)', daterange('2000-02-01', '2000-03-01'), 'one'),
  ('[3,4)', daterange('2000-01-01', '2010-01-01'), 'three')
;
----------
[UPDATE] update D1 set id = id + 1
;
----------
[INSERT] INSERT INTO savepoints VALUES (13)
;
----------
[DELETE] DELETE FROM uctest WHERE f1 = 10
;
----------
[INSERT] insert into rngfunc values (1,2), (3,4) returning *
;
----------
[INSERT] insert into twoconstraints values(1, '((0,0),(1,1))')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '18165', relminmxid = '3'
WHERE oid = '"public"."tt4"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE rw_view1 SET a=5 WHERE a=4
;
----------
[INSERT|SELECT] WITH rand_value AS (SELECT string_agg(fipshash(i::text),'') AS val FROM generate_series(1,60) s(i))
INSERT INTO brintest_3
SELECT val, val, val, val FROM rand_value
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13701', relminmxid = '3'
WHERE oid = '"public"."mic_inputs"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 94)
;
----------
[INSERT|SELECT] INSERT INTO delete_test (a) VALUES (10)
;
----------
[DELETE|SELECT] DELETE FROM foo WHERE f1 = 5
  RETURNING (SELECT max(old.f4 + x) FROM generate_series(1, 10) x) old_max,
            (SELECT max(new.f4 + x) FROM generate_series(1, 10) x) new_max
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '19830', relminmxid = '3'
WHERE oid = '45244'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '38744'
;
----------
[INSERT|SELECT] INSERT INTO VARBIT_SHIFT_TABLE SELECT CAST(v || B'0000' AS BIT VARYING(12)) >>4 FROM VARBIT_SHIFT_TABLE
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1100', relminmxid = '1'
WHERE oid = '"public"."num_exp_power_10_ln"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 16)
;
