LOCK TABLE public.pagg_tab_m_p2 IN ACCESS SHARE MODE
;
-- fail, cannot be changed
ALTER SYSTEM RESET config_file
;
ALTER TABLE public.domnotnull OWNER TO zyc
;
ALTER TABLE public.gtest1_1 OWNER TO zyc
;
-- The INSERT changes are large enough to be spilled but will not be, because
-- the transaction is aborted. The logical decoding skips collecting further
-- changes too. The transaction is prepared to make sure the decoding processes
-- the aborted transaction.
BEGIN
;
set datestyle to mdy
;
SAVEPOINT second_sp
;
-- check create publication on CURRENT_SCHEMA where search_path is not set
SET SEARCH_PATH=''
;
--
-- GEOMETRY
--

-- Back off displayed precision a little bit to reduce platform-to-platform
-- variation in results.
SET extra_float_digits TO -3
;
EXECUTE dumpTableAttach('36579')
;
------------------------------------------------------------------------------------------
[SESSION_LOCK] LOCK TABLE public.pagg_tab_m_p2 IN ACCESS SHARE MODE
;
----------
[SYSTEM_SETTING_WRITE] -- fail, cannot be changed
ALTER SYSTEM RESET config_file
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.domnotnull OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.gtest1_1 OWNER TO zyc
;
----------
[TRANSACTION] -- The INSERT changes are large enough to be spilled but will not be, because
-- the transaction is aborted. The logical decoding skips collecting further
-- changes too. The transaction is prepared to make sure the decoding processes
-- the aborted transaction.
BEGIN
;
----------
[SESSION_SETTING_WRITE] set datestyle to mdy
;
----------
[TRANSACTION] SAVEPOINT second_sp
;
----------
[SESSION_SETTING_WRITE] -- check create publication on CURRENT_SCHEMA where search_path is not set
SET SEARCH_PATH=''
;
----------
[SESSION_SETTING_WRITE] --
-- GEOMETRY
--

-- Back off displayed precision a little bit to reduce platform-to-platform
-- variation in results.
SET extra_float_digits TO -3
;
----------
[UNSAFE] EXECUTE dumpTableAttach('36579')
;
