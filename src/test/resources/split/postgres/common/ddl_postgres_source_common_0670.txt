-- Starting in v11, the notational form does matter if there's ambiguity
alter table fullname add column longname text
;
create temp table tbl_ra(a int unique, b int)
;
CREATE TABLE sch1.tab3(a int)
;
DROP TABLE pg_temp_4.pg_temp_26914_2, pg_temp_4.pg_temp_26914
;
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2 FOR VALUES IN (1500,1502)
;
alter table atacc1 add constraint atacc_test1 unique (test, test2)
;
CREATE FUNCTION logging_minvfunc_strict(text, anyelement) RETURNS text AS
$$ SELECT $1 || '-' || quote_nullable($2) $$
LANGUAGE SQL STRICT IMMUTABLE
;
ALTER INDEX pg_toast.pg_toast_28519_index RENAME TO reind_con_toast_idx
;
/* Base view for foreign servers */
CREATE VIEW _pg_foreign_servers AS
    SELECT s.oid,
           s.srvoptions,
           CAST(current_database() AS sql_identifier) AS foreign_server_catalog,
           CAST(srvname AS sql_identifier) AS foreign_server_name,
           CAST(current_database() AS sql_identifier) AS foreign_data_wrapper_catalog,
           CAST(w.fdwname AS sql_identifier) AS foreign_data_wrapper_name,
           CAST(srvtype AS character_data) AS foreign_server_type,
           CAST(srvversion AS character_data) AS foreign_server_version,
           CAST(u.rolname AS sql_identifier) AS authorization_identifier
    FROM pg_foreign_server s, pg_foreign_data_wrapper w, pg_authid u
    WHERE w.oid = s.srvfdw
          AND u.oid = s.srvowner
          AND (pg_has_role(s.srvowner, 'USAGE')
               OR has_server_privilege(s.oid, 'USAGE'))
;
ALTER INDEX bloomidx SET (length=80)
;
CREATE TABLE "public"."parallel_vacuum_table" (
    "a" integer
)
WITH ("autovacuum_enabled"='off')
;
ANALYZE public.mvtest_tvvm
;
CREATE OPERATOR CLASS cidr_ops
DEFAULT FOR TYPE cidr USING gin
AS
    OPERATOR        1       <(inet,inet),
    OPERATOR        2       <=(inet,inet),
    OPERATOR        3       =(inet,inet),
    OPERATOR        4       >=(inet,inet),
    OPERATOR        5       >(inet,inet),
    FUNCTION        1       network_cmp(inet,inet),
    FUNCTION        2       gin_extract_value_cidr(cidr, internal),
    FUNCTION        3       gin_extract_query_cidr(cidr, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_cidr(cidr,cidr,int2, internal),
STORAGE         cidr
;
CREATE PROCEDURE "public"."ptest11"(OUT "a" integer, VARIADIC "b" integer[])
    LANGUAGE "sql"
    AS $$ SELECT b[1] + b[2] $$
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_type
;
CREATE TABLE split_merge18.inventory (
    sku text PRIMARY KEY,
    qty integer NOT NULL DEFAULT 0,
    note text
)
;
ALTER TABLE ONLY "public"."part3_self_fk"
    ADD CONSTRAINT "part3_self_fk_pkey" PRIMARY KEY ("id")
;
CREATE TABLE auth_tbl(uname text, credential text, label text)
;
-- Function drop
create function pg_temp.twophase_func() returns void as
  $$ select '2pc_func'::text $$ language sql
;
DROP SCHEMA testns2 CASCADE
;
ANALYZE ab1 (a)
;
CREATE TABLE toasted_copy (
    id int primary key, -- no default, copy didn't use to handle that with multi inserts
    data text
)
;
create index on mcrparted1 (a, abs(b), c)
;
CREATE VIEW pg_wait_events AS
    SELECT * FROM pg_get_wait_events()
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_aggregate
;
ALTER TEXT SEARCH CONFIGURATION yiddish ADD MAPPING
    FOR word, hword_part, hword
	WITH yiddish_stem
;
CREATE TABLE bug18634s (a int, b int, c text)
;
ALTER TABLE pg_toast.pg_toast_30598 RENAME TO reind_con_toast
;
do $$ declare x text[]; begin x := '{1.23, 4.56}'::numeric[]; end $$
;
CREATE INDEX brin_pvactst ON pvactst USING brin (i)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_template
;
-- Test GIN over jsonb
CREATE TABLE "gin_check_jsonb"("j" jsonb)
;
drop table rls_t, test_t
;
CREATE FUNCTION getrngfunc5(int) RETURNS setof rngfunc AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
VACUUM (ANALYZE) pg_catalog.pg_class
;
ALTER TABLE regtest_ptable ADD CONSTRAINT test_ck CHECK (p like '%abc%') NOT VALID
;
CREATE FUNCTION pg_buffercache_evict_all(
    OUT buffers_evicted int4,
    OUT buffers_flushed int4,
    OUT buffers_skipped int4)
AS '$libdir/pg_buffercache', 'pg_buffercache_evict_all'
LANGUAGE C PARALLEL SAFE VOLATILE
;
alter database abc set datestyle = default
;
create table rewritemetoo2 of rewritetype
;
-- ... this will fail
drop table at_tab1, at_tab2
;
CREATE INDEX idx_enum ON test_enum USING gin (i)
;
create temp table htab2(a int)
;
CREATE TABLE "public"."trigger_parted_p2" (
    "a" integer CONSTRAINT "trigger_parted_a_not_null" NOT NULL
)
PARTITION BY LIST ("a")
;
CREATE OPERATOR ~ (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contained,
	COMMUTATOR = '@',
	RESTRICT = _int_contained_sel,
	JOIN = _int_contained_joinsel
)
;
drop table src
;
create table at_test_sql_partop (a int) partition by range (a at_test_sql_partop)
;
--
-- page_header()
--
CREATE FUNCTION page_header(IN page bytea,
    OUT lsn pg_lsn,
    OUT checksum smallint,
    OUT flags smallint,
    OUT lower smallint,
    OUT upper smallint,
    OUT special smallint,
    OUT pagesize smallint,
    OUT version smallint,
    OUT prune_xid xid)
AS '$libdir/pageinspect', 'page_header'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP TABLE pg_temp_11.pg_temp_41234_2, pg_temp_11.pg_temp_41234
;
CREATE UNIQUE INDEX ON sro_index_mv (c) WHERE unwanted_grant_nofail(1) > 0
;
ALTER INDEX pg_toast.pg_toast_30613_index RENAME TO reind_con_toast_idx
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] -- Starting in v11, the notational form does matter if there's ambiguity
alter table fullname add column longname text
;
----------
[CREATE_TABLE] create temp table tbl_ra(a int unique, b int)
;
----------
[CREATE_TABLE] CREATE TABLE sch1.tab3(a int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_26914_2, pg_temp_4.pg_temp_26914
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2 FOR VALUES IN (1500,1502)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_test1 unique (test, test2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION logging_minvfunc_strict(text, anyelement) RETURNS text AS
$$ SELECT $1 || '-' || quote_nullable($2) $$
LANGUAGE SQL STRICT IMMUTABLE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28519_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT,METADATA)] /* Base view for foreign servers */
CREATE VIEW _pg_foreign_servers AS
    SELECT s.oid,
           s.srvoptions,
           CAST(current_database() AS sql_identifier) AS foreign_server_catalog,
           CAST(srvname AS sql_identifier) AS foreign_server_name,
           CAST(current_database() AS sql_identifier) AS foreign_data_wrapper_catalog,
           CAST(w.fdwname AS sql_identifier) AS foreign_data_wrapper_name,
           CAST(srvtype AS character_data) AS foreign_server_type,
           CAST(srvversion AS character_data) AS foreign_server_version,
           CAST(u.rolname AS sql_identifier) AS authorization_identifier
    FROM pg_foreign_server s, pg_foreign_data_wrapper w, pg_authid u
    WHERE w.oid = s.srvfdw
          AND u.oid = s.srvowner
          AND (pg_has_role(s.srvowner, 'USAGE')
               OR has_server_privilege(s.oid, 'USAGE'))
;
----------
[ALTER_INDEX] ALTER INDEX bloomidx SET (length=80)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."parallel_vacuum_table" (
    "a" integer
)
WITH ("autovacuum_enabled"='off')
;
----------
[ADMIN_TABLE] ANALYZE public.mvtest_tvvm
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS cidr_ops
DEFAULT FOR TYPE cidr USING gin
AS
    OPERATOR        1       <(inet,inet),
    OPERATOR        2       <=(inet,inet),
    OPERATOR        3       =(inet,inet),
    OPERATOR        4       >=(inet,inet),
    OPERATOR        5       >(inet,inet),
    FUNCTION        1       network_cmp(inet,inet),
    FUNCTION        2       gin_extract_value_cidr(cidr, internal),
    FUNCTION        3       gin_extract_query_cidr(cidr, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_cidr(cidr,cidr,int2, internal),
STORAGE         cidr
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest11"(OUT "a" integer, VARIADIC "b" integer[])
    LANGUAGE "sql"
    AS $$ SELECT b[1] + b[2] $$
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_type
;
----------
[CREATE_TABLE] CREATE TABLE split_merge18.inventory (
    sku text PRIMARY KEY,
    qty integer NOT NULL DEFAULT 0,
    note text
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."part3_self_fk"
    ADD CONSTRAINT "part3_self_fk_pkey" PRIMARY KEY ("id")
;
----------
[CREATE_TABLE] CREATE TABLE auth_tbl(uname text, credential text, label text)
;
----------
[CREATE_PROG_OBJ] -- Function drop
create function pg_temp.twophase_func() returns void as
  $$ select '2pc_func'::text $$ language sql
;
----------
[DROP_SCHEMA] DROP SCHEMA testns2 CASCADE
;
----------
[ADMIN_TABLE] ANALYZE ab1 (a)
;
----------
[CREATE_TABLE] CREATE TABLE toasted_copy (
    id int primary key, -- no default, copy didn't use to handle that with multi inserts
    data text
)
;
----------
[ADD_INDEX] create index on mcrparted1 (a, abs(b), c)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_wait_events AS
    SELECT * FROM pg_get_wait_events()
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_aggregate
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION yiddish ADD MAPPING
    FOR word, hword_part, hword
	WITH yiddish_stem
;
----------
[CREATE_TABLE] CREATE TABLE bug18634s (a int, b int, c text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30598 RENAME TO reind_con_toast
;
----------
[BLOCK] do $$ declare x text[]; begin x := '{1.23, 4.56}'::numeric[]; end $$
;
----------
[ADD_INDEX] CREATE INDEX brin_pvactst ON pvactst USING brin (i)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_template
;
----------
[CREATE_TABLE] -- Test GIN over jsonb
CREATE TABLE "gin_check_jsonb"("j" jsonb)
;
----------
[DROP_TABLE] drop table rls_t, test_t
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION getrngfunc5(int) RETURNS setof rngfunc AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_class
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE regtest_ptable ADD CONSTRAINT test_ck CHECK (p like '%abc%') NOT VALID
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_buffercache_evict_all(
    OUT buffers_evicted int4,
    OUT buffers_flushed int4,
    OUT buffers_skipped int4)
AS '$libdir/pg_buffercache', 'pg_buffercache_evict_all'
LANGUAGE C PARALLEL SAFE VOLATILE
;
----------
[ALTER_CATALOG] alter database abc set datestyle = default
;
----------
[CREATE_TABLE] create table rewritemetoo2 of rewritetype
;
----------
[DROP_TABLE] -- ... this will fail
drop table at_tab1, at_tab2
;
----------
[ADD_INDEX] CREATE INDEX idx_enum ON test_enum USING gin (i)
;
----------
[CREATE_TABLE] create temp table htab2(a int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted_p2" (
    "a" integer CONSTRAINT "trigger_parted_a_not_null" NOT NULL
)
PARTITION BY LIST ("a")
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~ (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contained,
	COMMUTATOR = '@',
	RESTRICT = _int_contained_sel,
	JOIN = _int_contained_joinsel
)
;
----------
[DROP_TABLE] drop table src
;
----------
[CREATE_TABLE] create table at_test_sql_partop (a int) partition by range (a at_test_sql_partop)
;
----------
[CREATE_PROG_OBJ] --
-- page_header()
--
CREATE FUNCTION page_header(IN page bytea,
    OUT lsn pg_lsn,
    OUT checksum smallint,
    OUT flags smallint,
    OUT lower smallint,
    OUT upper smallint,
    OUT special smallint,
    OUT pagesize smallint,
    OUT version smallint,
    OUT prune_xid xid)
AS '$libdir/pageinspect', 'page_header'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_41234_2, pg_temp_11.pg_temp_41234
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON sro_index_mv (c) WHERE unwanted_grant_nofail(1) > 0
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30613_index RENAME TO reind_con_toast_idx
;
