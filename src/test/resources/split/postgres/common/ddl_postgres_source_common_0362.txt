DROP TABLE covering_index_heap
;
DROP FUNCTION foolme(timestamptz)
;
--
-- Test ISN extension
--

CREATE EXTENSION isn
;
ANALYZE public.rtest_t8
;
CREATE INDEX gtest22c_b_idx ON generated_stored_tests.gtest22c USING btree (b)
;
create view tt26v as
select x + y + z as c1,
       (x * y) + z as c2,
       x + (y * z) as c3,
       (x + y) * z as c4,
       x * (y + z) as c5,
       x + (y + z) as c6,
       x + (y # z) as c7,
       (x > y) AND (y > z OR x > z) as c8,
       (x > y) OR (y > z AND NOT (x > z)) as c9,
       (x,y) <> ALL (values(1,2),(3,4)) as c10,
       (x,y) <= ANY (values(1,2),(3,4)) as c11
from (values(1,2,3)) v(x,y,z)
;
DROP VIEW pg_stat_statements
;
alter table anothertab alter column atcol1 type boolean using atcol1::int
;
CREATE OPERATOR CLASS text_ops
DEFAULT FOR TYPE text USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       bttextcmp(text,text),
    FUNCTION        2       gin_extract_value_text(text, internal),
    FUNCTION        3       gin_extract_query_text(text, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_text(text,text,int2, internal),
STORAGE         text
;
CREATE SUBSCRIPTION admin_sub CONNECTION 'port=25712 host=/tmp/mUR9A5zNgJ dbname=postgres' PUBLICATION alice WITH (password_required=false)
;
create function end_label3() returns void as $$
<<outer_label>>
begin
  <<inner_label>>
  for _i in 1 .. 10 loop
    exit;
  end loop outer_label;
end;
$$ language plpgsql
;
ALTER FOREIGN TABLE async_p1 OPTIONS (DROP use_remote_estimate)
;
CREATE FUNCTION g_intbig_consistent(internal,_int4,smallint,oid,internal)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE ONLY "public"."clstr_tst_s"
    ADD CONSTRAINT "clstr_tst_s_pkey" PRIMARY KEY ("rf_a")
;
CREATE AGGREGATE "public"."first_el_agg_any"("anyelement") (
    SFUNC = "public"."first_el_transfn",
    STYPE = "anyarray",
    FINALFUNC = "public"."first_el"
)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_attribute
;
ALTER SUBSCRIPTION tap_sub_copy SET (two_phase = true, failover = true)
;
-- Identity columns must be NOT NULL (cf bug #16913)

CREATE TABLE itest15 (id integer GENERATED ALWAYS AS IDENTITY NULL)
;
create trigger My_Table_Check_Trigger before insert on My_Table for each row execute procedure My_Table_Check ( )
;
CREATE TABLE inettmp (a inet)
;
CREATE TYPE issn (
	INPUT = issn_in,
	OUTPUT = isn_out,
	LIKE = pg_catalog.int8
)
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26417_2 (tid pg_catalog.tid)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
CREATE INDEX tenk2_hundred ON tenk2 USING btree(hundred int4_ops)
;
CREATE TABLE p (a int, b int, c int)
;
-- This should fail, because we'd have to cast numeric to int which is
-- not an implicit coercion (or use numeric=numeric, but that's not part
-- of the integer opfamily)
CREATE TABLE FKTABLE (ftest1 numeric REFERENCES pktable)
;
CREATE TABLE my_schema_2.pts2 (o int, p text) PARTITION BY RANGE (o)
;
ALTER TABLE IF EXISTS split_alter_table.base_table ADD COLUMN IF NOT EXISTS note text COLLATE "C" DEFAULT 'x' CHECK (length(note) > 0)
;
CREATE PUBLICATION "dump_pub_qual_1ct" WITH (publish = 'insert, update, delete, truncate')
;
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf1 RENAME TO alt_ts_conf4
;
CREATE FUNCTION "public"."logging_minvfunc_nonstrict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE
    AS $_$ SELECT $1 || '-' || quote_nullable($2) $_$
;
ALTER POLICY dep_p1 ON dep1 USING (true)
;
CREATE TABLE test_tsquery (txtkeyword TEXT, txtsample TEXT)
;
CREATE TABLE pagg_tab_ml_p2_s1 PARTITION OF pagg_tab_ml_p2 FOR VALUES IN ('0000', '0001', '0002')
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = upc,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE TABLE list_parted2 (
	a varchar
) PARTITION BY LIST (a)
;
-- ok
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false)
;
DROP DATABASE d1
;
CREATE TABLE "public"."itest4" (
    "a" integer,
    "b" "text" DEFAULT ''::"text"
)
;
ANALYZE public.bt_txt_heap
;
--
-- For fkeys2:
-- 	ON DELETE/UPDATE (pkey23) RESTRICT:
-- 		fkeys (fkey3)
--
create trigger check_fkeys2_fkey_restrict
	after delete or update on fkeys2
	for each row
	execute procedure check_foreign_key (1, 'restrict', 'pkey23', 'fkeys', 'fkey3')
;
COMMENT ON FUNCTION seg_overlap(seg, seg) IS
'overlaps'
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_collation
;
CREATE TABLE sj_t3 (id serial, a int)
;
DROP TABLE schema_rf_x.tab_rf_partitioned
;
CREATE INDEX tbl_phv_idx ON tbl_phv(x)
;
-- typed tables are rewritten when their type changes.  Don't emit table
-- name, because firing order is not stable.
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE NOTICE 'Table is being rewritten (reason = %)',
               pg_event_trigger_table_rewrite_reason();
END;
$$
;
ANALYZE pg_temp_3.pg_temp_16651
;
CREATE TEXT SEARCH CONFIGURATION ispell_tst (
						COPY=english
)
;
ALTER TABLE ONLY "public"."beta_neg" ATTACH PARTITION "public"."beta_neg_p1" FOR VALUES FROM (100) TO (150)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE covering_index_heap
;
----------
[DROP_PROG_OBJ] DROP FUNCTION foolme(timestamptz)
;
----------
[CREATE_LIBRARY] --
-- Test ISN extension
--

CREATE EXTENSION isn
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_t8
;
----------
[ADD_INDEX] CREATE INDEX gtest22c_b_idx ON generated_stored_tests.gtest22c USING btree (b)
;
----------
[CREATE_VIEW(SELECT)] create view tt26v as
select x + y + z as c1,
       (x * y) + z as c2,
       x + (y * z) as c3,
       (x + y) * z as c4,
       x * (y + z) as c5,
       x + (y + z) as c6,
       x + (y # z) as c7,
       (x > y) AND (y > z OR x > z) as c8,
       (x > y) OR (y > z AND NOT (x > z)) as c9,
       (x,y) <> ALL (values(1,2),(3,4)) as c10,
       (x,y) <= ANY (values(1,2),(3,4)) as c11
from (values(1,2,3)) v(x,y,z)
;
----------
[DROP_VIEW] DROP VIEW pg_stat_statements
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table anothertab alter column atcol1 type boolean using atcol1::int
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS text_ops
DEFAULT FOR TYPE text USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       bttextcmp(text,text),
    FUNCTION        2       gin_extract_value_text(text, internal),
    FUNCTION        3       gin_extract_query_text(text, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_text(text,text,int2, internal),
STORAGE         text
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION admin_sub CONNECTION 'port=25712 host=/tmp/mUR9A5zNgJ dbname=postgres' PUBLICATION alice WITH (password_required=false)
;
----------
[CREATE_PROG_OBJ] create function end_label3() returns void as $$
<<outer_label>>
begin
  <<inner_label>>
  for _i in 1 .. 10 loop
    exit;
  end loop outer_label;
end;
$$ language plpgsql
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE async_p1 OPTIONS (DROP use_remote_estimate)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_intbig_consistent(internal,_int4,smallint,oid,internal)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."clstr_tst_s"
    ADD CONSTRAINT "clstr_tst_s_pkey" PRIMARY KEY ("rf_a")
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."first_el_agg_any"("anyelement") (
    SFUNC = "public"."first_el_transfn",
    STYPE = "anyarray",
    FINALFUNC = "public"."first_el"
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_attribute
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub_copy SET (two_phase = true, failover = true)
;
----------
[CREATE_TABLE] -- Identity columns must be NOT NULL (cf bug #16913)

CREATE TABLE itest15 (id integer GENERATED ALWAYS AS IDENTITY NULL)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger My_Table_Check_Trigger before insert on My_Table for each row execute procedure My_Table_Check ( )
;
----------
[CREATE_TABLE] CREATE TABLE inettmp (a inet)
;
----------
[CREATE_TYPE] CREATE TYPE issn (
	INPUT = issn_in,
	OUTPUT = isn_out,
	LIKE = pg_catalog.int8
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26417_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
----------
[ADD_INDEX] CREATE INDEX tenk2_hundred ON tenk2 USING btree(hundred int4_ops)
;
----------
[CREATE_TABLE] CREATE TABLE p (a int, b int, c int)
;
----------
[CREATE_TABLE] -- This should fail, because we'd have to cast numeric to int which is
-- not an implicit coercion (or use numeric=numeric, but that's not part
-- of the integer opfamily)
CREATE TABLE FKTABLE (ftest1 numeric REFERENCES pktable)
;
----------
[CREATE_TABLE] CREATE TABLE my_schema_2.pts2 (o int, p text) PARTITION BY RANGE (o)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE IF EXISTS split_alter_table.base_table ADD COLUMN IF NOT EXISTS note text COLLATE "C" DEFAULT 'x' CHECK (length(note) > 0)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "dump_pub_qual_1ct" WITH (publish = 'insert, update, delete, truncate')
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION alt_ts_conf1 RENAME TO alt_ts_conf4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."logging_minvfunc_nonstrict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE
    AS $_$ SELECT $1 || '-' || quote_nullable($2) $_$
;
----------
[ALTER_POLICY] ALTER POLICY dep_p1 ON dep1 USING (true)
;
----------
[CREATE_TABLE] CREATE TABLE test_tsquery (txtkeyword TEXT, txtsample TEXT)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p2_s1 PARTITION OF pagg_tab_ml_p2 FOR VALUES IN ('0000', '0001', '0002')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = upc,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE list_parted2 (
	a varchar
) PARTITION BY LIST (a)
;
----------
[CREATE_PUB_SUB] -- ok
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false)
;
----------
[DROP_CATALOG] DROP DATABASE d1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest4" (
    "a" integer,
    "b" "text" DEFAULT ''::"text"
)
;
----------
[ADMIN_TABLE] ANALYZE public.bt_txt_heap
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- For fkeys2:
-- 	ON DELETE/UPDATE (pkey23) RESTRICT:
-- 		fkeys (fkey3)
--
create trigger check_fkeys2_fkey_restrict
	after delete or update on fkeys2
	for each row
	execute procedure check_foreign_key (1, 'restrict', 'pkey23', 'fkeys', 'fkey3')
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_overlap(seg, seg) IS
'overlaps'
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_collation
;
----------
[CREATE_TABLE] CREATE TABLE sj_t3 (id serial, a int)
;
----------
[DROP_TABLE] DROP TABLE schema_rf_x.tab_rf_partitioned
;
----------
[ADD_INDEX] CREATE INDEX tbl_phv_idx ON tbl_phv(x)
;
----------
[CREATE_PROG_OBJ] -- typed tables are rewritten when their type changes.  Don't emit table
-- name, because firing order is not stable.
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE NOTICE 'Table is being rewritten (reason = %)',
               pg_event_trigger_table_rewrite_reason();
END;
$$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_16651
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION ispell_tst (
						COPY=english
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."beta_neg" ATTACH PARTITION "public"."beta_neg_p1" FOR VALUES FROM (100) TO (150)
;
