create or replace function testrngfunc() returns rngfunc_type as $$
  select 7.136178319899999964, 7.136178319899999964;
$$ language sql volatile
;
create table s2.p2 (a int, b int) PARTITION BY list (a)
;
-- Test the interaction between ALTER TABLE .. DISABLE TRIGGER and
-- both kinds of inheritance.  Historically, legacy inheritance has
-- not recursed to children, so that behavior is preserved.
create table parent (a int)
;
ALTER TABLE cj_source1 RENAME COLUMN sid1 TO sid
;
ALTER TABLE ONLY "public"."test_range_excl"
    ADD CONSTRAINT "test_range_excl_speaker_during_excl" EXCLUDE USING "gist" ("speaker" WITH =, "during" WITH &&)
;
COMMENT ON DATABASE split_db_probe IS 'split database comment'
;
DROP TABLE constraint_comments_tbl
;
VACUUM (PARALLEL 2) pg_catalog.pg_depend
;
DROP EVENT TRIGGER IF EXISTS split_event_probe
;
CREATE TABLE partitioned (
	a int
) INHERITS (some_table) PARTITION BY LIST (a)
;
CREATE OPERATOR CLASS btree_hstore_ops
DEFAULT FOR TYPE hstore USING btree
AS
	OPERATOR	1	#<# ,
	OPERATOR	2	#<=# ,
	OPERATOR	3	= ,
	OPERATOR	4	#>=# ,
	OPERATOR	5	#># ,
	FUNCTION	1	hstore_cmp(hstore,hstore)
;
-- this should fail
alter table other_partitioned_fk add foreign key (a, b)
  references fk_notpartitioned_pk(a, b)
;
-- fail -- cannot merge constraints with different enforceability.
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2 FOR VALUES IN (1500,1502)
;
DROP TABLE fk_partitioned_fk_2
;
CREATE TEMP TABLE pg_temp_3.pg_temp_27374_2 (tid pg_catalog.tid)
;
CREATE FUNCTION PLUS_ONE(i INTEGER) RETURNS INTEGER AS
$$ SELECT (i + 1.0)::INTEGER LIMIT 1 $$ LANGUAGE SQL
;
CREATE FUNCTION gin_numeric_cmp(numeric, numeric)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE INDEX t111_a_idx ON t111(a)
;
CREATE TYPE textrange2 AS range (subtype=text, collation="C")
;
do $$
declare x int := 42;
        y int := x + 1;
begin
  raise notice 'x = %, y = %', x, y;
end;
$$
;
ANALYZE vacparted(a,b,b)
;
ALTER INDEX pg_toast.pg_toast_33622_index RENAME TO reind_con_toast_idx
;
create trigger aiserial
	before insert or update on aitest
	for each row
	execute procedure
	autoinc (price_on, aitest_seq)
;
create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2.1, 3) $$ language sql
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_seclabel
;
-- Neither local nor cascaded check options work with INSTEAD rules

DROP TRIGGER rw_view1_trig ON rw_view1
;
VACUUM (PARALLEL 2) pg_catalog.pg_foreign_data_wrapper
;
CREATE CONSTRAINT TRIGGER t AFTER INSERT ON sro_trojan_table
    INITIALLY DEFERRED FOR EACH ROW EXECUTE PROCEDURE sro_trojan()
;
ALTER TABLE fk_partitioned_fk_6 ATTACH PARTITION fk_partitioned_pk_6 FOR VALUES IN (1)
;
ANALYZE public.mvtest_tvmm
;
DROP FUNCTION page_checksum(IN page bytea, IN blkno int4)
;
-- works

ALTER TABLE fk_r ATTACH PARTITION fk_r_1 FOR VALUES IN (1)
;
-- Test CREATE TABLE AS ... IF NOT EXISTS
CREATE TABLE ctas_ine_tbl AS SELECT 1
;
CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
--
--
--
-- int4 ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_int4_consistent(internal,int4,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE VIEW mvtest_tv AS SELECT type, sum(amt) AS totamt FROM mvtest_t GROUP BY type
;
CREATE TABLE part_b_10_b_20 (e varchar, c numeric, a text, b bigint, d int) PARTITION BY RANGE (c)
;
ALTER TABLE do_write ADD column ts timestamptz
;
CREATE TABLE evttrig.parted (
    id int PRIMARY KEY)
    PARTITION BY RANGE (id)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest_parent" ATTACH PARTITION "generated_stored_tests"."gtest_child" FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
CREATE FUNCTION gin_extract_query_bool(bool, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
-- Add operators that are new in 9.3.

ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        5       pg_catalog.~ (text, text),
        OPERATOR        6       pg_catalog.~* (text, text)
;
CREATE VIEW lock_view1 AS SELECT * FROM lock_tbl1
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c7 DROP NOT NULL
;
CREATE TABLE ataddindex(id int, ref_id int)
;
CREATE FUNCTION postgres_fdw_validator(text[], oid)
RETURNS void
AS '$libdir/postgres_fdw'
LANGUAGE C STRICT
;
create function polyf(x anycompatiblemultirange, y anycompatible, z anycompatible) returns anycompatiblearray as $$
  select array[lower(x), upper(x), y, z]
$$ language sql
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_replication_origin
;
CREATE TABLE itest16 (a int GENERATED BY DEFAULT AS IDENTITY, b text)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create or replace function testrngfunc() returns rngfunc_type as $$
  select 7.136178319899999964, 7.136178319899999964;
$$ language sql volatile
;
----------
[CREATE_TABLE] create table s2.p2 (a int, b int) PARTITION BY list (a)
;
----------
[CREATE_TABLE] -- Test the interaction between ALTER TABLE .. DISABLE TRIGGER and
-- both kinds of inheritance.  Historically, legacy inheritance has
-- not recursed to children, so that behavior is preserved.
create table parent (a int)
;
----------
[RENAME_COLUMN] ALTER TABLE cj_source1 RENAME COLUMN sid1 TO sid
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."test_range_excl"
    ADD CONSTRAINT "test_range_excl_speaker_during_excl" EXCLUDE USING "gist" ("speaker" WITH =, "during" WITH &&)
;
----------
[COMMENT_CATALOG] COMMENT ON DATABASE split_db_probe IS 'split database comment'
;
----------
[DROP_TABLE] DROP TABLE constraint_comments_tbl
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_depend
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER IF EXISTS split_event_probe
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) INHERITS (some_table) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS btree_hstore_ops
DEFAULT FOR TYPE hstore USING btree
AS
	OPERATOR	1	#<# ,
	OPERATOR	2	#<=# ,
	OPERATOR	3	= ,
	OPERATOR	4	#>=# ,
	OPERATOR	5	#># ,
	FUNCTION	1	hstore_cmp(hstore,hstore)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- this should fail
alter table other_partitioned_fk add foreign key (a, b)
  references fk_notpartitioned_pk(a, b)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- fail -- cannot merge constraints with different enforceability.
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2 FOR VALUES IN (1500,1502)
;
----------
[DROP_TABLE] DROP TABLE fk_partitioned_fk_2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_27374_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION PLUS_ONE(i INTEGER) RETURNS INTEGER AS
$$ SELECT (i + 1.0)::INTEGER LIMIT 1 $$ LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_numeric_cmp(numeric, numeric)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADD_INDEX] CREATE INDEX t111_a_idx ON t111(a)
;
----------
[CREATE_POLICY] CREATE TYPE textrange2 AS range (subtype=text, collation="C")
;
----------
[BLOCK] do $$
declare x int := 42;
        y int := x + 1;
begin
  raise notice 'x = %, y = %', x, y;
end;
$$
;
----------
[ADMIN_TABLE] ANALYZE vacparted(a,b,b)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33622_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger aiserial
	before insert or update on aitest
	for each row
	execute procedure
	autoinc (price_on, aitest_seq)
;
----------
[CREATE_PROG_OBJ] create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2.1, 3) $$ language sql
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_seclabel
;
----------
[DROP_TRIGGER] -- Neither local nor cascaded check options work with INSTEAD rules

DROP TRIGGER rw_view1_trig ON rw_view1
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE CONSTRAINT TRIGGER t AFTER INSERT ON sro_trojan_table
    INITIALLY DEFERRED FOR EACH ROW EXECUTE PROCEDURE sro_trojan()
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk_6 ATTACH PARTITION fk_partitioned_pk_6 FOR VALUES IN (1)
;
----------
[ADMIN_TABLE] ANALYZE public.mvtest_tvmm
;
----------
[DROP_PROG_OBJ] DROP FUNCTION page_checksum(IN page bytea, IN blkno int4)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- works

ALTER TABLE fk_r ATTACH PARTITION fk_r_1 FOR VALUES IN (1)
;
----------
[CREATE_TABLE|SELECT] -- Test CREATE TABLE AS ... IF NOT EXISTS
CREATE TABLE ctas_ine_tbl AS SELECT 1
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
----------
[CREATE_PROG_OBJ] --
--
--
-- int4 ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_int4_consistent(internal,int4,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mvtest_tv AS SELECT type, sum(amt) AS totamt FROM mvtest_t GROUP BY type
;
----------
[CREATE_TABLE] CREATE TABLE part_b_10_b_20 (e varchar, c numeric, a text, b bigint, d int) PARTITION BY RANGE (c)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE do_write ADD column ts timestamptz
;
----------
[CREATE_TABLE] CREATE TABLE evttrig.parted (
    id int PRIMARY KEY)
    PARTITION BY RANGE (id)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "generated_stored_tests"."gtest_parent" ATTACH PARTITION "generated_stored_tests"."gtest_child" FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_bool(bool, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
----------
[ALTER_PROG_OBJ] -- Add operators that are new in 9.3.

ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        5       pg_catalog.~ (text, text),
        OPERATOR        6       pg_catalog.~* (text, text)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW lock_view1 AS SELECT * FROM lock_tbl1
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c7 DROP NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE ataddindex(id int, ref_id int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION postgres_fdw_validator(text[], oid)
RETURNS void
AS '$libdir/postgres_fdw'
LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] create function polyf(x anycompatiblemultirange, y anycompatible, z anycompatible) returns anycompatiblearray as $$
  select array[lower(x), upper(x), y, z]
$$ language sql
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE] CREATE TABLE itest16 (a int GENERATED BY DEFAULT AS IDENTITY, b text)
;
