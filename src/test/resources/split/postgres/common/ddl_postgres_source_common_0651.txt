CREATE TABLE caster (
    citext      citext,
    text        text,
    varchar     varchar,
    bpchar      bpchar,
    char        char,
    chr         "char",
    name        name,
    bytea       bytea,
    boolean     boolean,
    float4      float4,
    float8      float8,
    numeric     numeric,
    int8        int8,
    int4        int4,
    int2        int2,
    cidr        cidr,
    inet        inet,
    macaddr     macaddr,
    money       money,
    timestamp   timestamp,
    timestamptz timestamptz,
    interval    interval,
    date        date,
    time        time,
    timetz      timetz,
    point       point,
    lseg        lseg,
    box         box,
    path        path,
    polygon     polygon,
    circle      circle,
    bit         bit,
    bitv        bit varying,
    tsvector    tsvector,
    tsquery     tsquery,
    uuid        uuid
)
;
DROP TABLE rewrite_test
;
CREATE TRIGGER t1 BEFORE INSERT ON test1 EXECUTE PROCEDURE triggerfunc1()
;
CREATE TABLE "public"."rtest_t8" (
    "a" integer,
    "b" "text"
)
;
CREATE TABLE "public"."prt3_n_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_index
;
CREATE FUNCTION split_event_helper.capture_dropped_objects()
RETURNS event_trigger
LANGUAGE plpgsql
AS $event$
BEGIN
    INSERT INTO split_event_helper.drop_log (
        classid, objid, objsubid, original, normal, is_temporary,
        object_type, schema_name, object_name, object_identity,
        address_names, address_args
    )
    SELECT
        classid, objid, objsubid, original, normal, is_temporary,
        object_type, schema_name, object_name, object_identity,
        address_names, address_args
    FROM pg_event_trigger_dropped_objects();
END;
$event$
;
ANALYZE pg_temp_11.pg_temp_41153
;
alter table atacc1 add constraint atacc_test1 primary key (test1)
;
ANALYZE public.view_base_table
;
CREATE VIEW pg_stat_sys_tables AS
    SELECT * FROM pg_stat_all_tables
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
-- populate_array()
create type jsb_ia as (a int[])
;
ALTER INDEX testschema.part_a_idx SET TABLESPACE regress_tblspace
;
/*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
DROP INDEX enumtest_btree
;
alter event trigger regress_event_trigger enable always
;
ALTER EXTENSION pageinspect UPDATE TO '1.11'
;
CREATE SUBSCRIPTION admin_sub CONNECTION 'port=10246 host=/tmp/jZENFq3HZU dbname=postgres' PUBLICATION alice WITH (password_required=false)
;
ALTER TABLE fkpart7.pkpart1 ADD PRIMARY KEY (a)
;
/*
 * text search configuration for norwegian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * norwegian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_depend
;
CREATE VIEW vv AS
  SELECT * FROM test_tablesample TABLESAMPLE system_time (20)
;
ALTER INDEX pg_toast.pg_toast_33089_index RENAME TO reind_con_toast_idx
;
-- OK
ALTER TEXT SEARCH DICTIONARY alt_ts_dict2 SET SCHEMA alt_nsp2
;
DROP TRIGGER trig_row_after2 ON rem1
;
alter table t2 alter column z drop not null
;
DROP TABLE sequence_test_table
;
CREATE VIEW "public"."my_credit_card_usage_normal" AS
 SELECT "l"."cid",
    "l"."name",
    "l"."tel",
    "l"."passwd",
    "l"."cnum",
    "l"."climit",
    "r"."ymd",
    "r"."usage"
   FROM ("public"."my_credit_card_secure" "l"
     JOIN "public"."credit_usage" "r" USING ("cid"))
;
CREATE SUBSCRIPTION regression_sub CONNECTION 'port=16820 host=/tmp/ekP6Yh8edH dbname=postgres' PUBLICATION alice
;
CREATE FUNCTION alter_op_test_fn_real_bool(real, boolean)
RETURNS boolean AS $$ SELECT NULL::BOOLEAN; $$ LANGUAGE sql IMMUTABLE
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 RENAME TO foreign_table_1
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_cast
;
DROP FUNCTION test_wchars_to_text
;
CREATE TEMPORARY TABLE pgss_ctas_1 AS SELECT 1
;
ALTER TEXT SEARCH DICTIONARY alt_ts_dict1 RENAME TO alt_ts_dict2
;
ALTER TYPE test_type2 DROP ATTRIBUTE b
;
DROP FUNCTION dup(f1 anycompatible, f2 anycompatiblearray)
;
ALTER TABLE "public"."rule_merge1" DISABLE RULE "rule2"
;
CREATE TEXT SEARCH PARSER addr_ts_prs
    (start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
-- Test ON CONFLICT DO UPDATE with partitioned table and non-identical children

CREATE TABLE upsert_test (
    a   INT PRIMARY KEY,
    b   TEXT
) PARTITION BY LIST (a)
;
create table hp2 partition of hp for values with (modulus 4, remainder 2)
;
DROP SCHEMA pub_test1 CASCADE
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=19323 host=/tmp/FohDx5iOiZ dbname=postgres' PUBLICATION pub1
;
CREATE SEQUENCE seq
;
CREATE TABLE rngfunc (rngfuncid int, rngfuncsubid int, rngfuncname text, primary key(rngfuncid,rngfuncsubid))
;
CREATE OPERATOR # (
	LEFTARG = _int4,
	RIGHTARG = int4,
	PROCEDURE = idx
)
;
create temp table temp_part_child (a int)
;
CREATE INDEX "tenk1_thous_tenthous" ON "public"."tenk1" USING "btree" ("thousand", "tenthous")
;
VACUUM (ONLY_DATABASE_STATS true, VERBOSE true)
;
VACUUM (ANALYZE) pg_catalog.pg_publication_namespace
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE caster (
    citext      citext,
    text        text,
    varchar     varchar,
    bpchar      bpchar,
    char        char,
    chr         "char",
    name        name,
    bytea       bytea,
    boolean     boolean,
    float4      float4,
    float8      float8,
    numeric     numeric,
    int8        int8,
    int4        int4,
    int2        int2,
    cidr        cidr,
    inet        inet,
    macaddr     macaddr,
    money       money,
    timestamp   timestamp,
    timestamptz timestamptz,
    interval    interval,
    date        date,
    time        time,
    timetz      timetz,
    point       point,
    lseg        lseg,
    box         box,
    path        path,
    polygon     polygon,
    circle      circle,
    bit         bit,
    bitv        bit varying,
    tsvector    tsvector,
    tsquery     tsquery,
    uuid        uuid
)
;
----------
[DROP_TABLE] DROP TABLE rewrite_test
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER t1 BEFORE INSERT ON test1 EXECUTE PROCEDURE triggerfunc1()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_t8" (
    "a" integer,
    "b" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt3_n_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_index
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_event_helper.capture_dropped_objects()
RETURNS event_trigger
LANGUAGE plpgsql
AS $event$
BEGIN
    INSERT INTO split_event_helper.drop_log (
        classid, objid, objsubid, original, normal, is_temporary,
        object_type, schema_name, object_name, object_identity,
        address_names, address_args
    )
    SELECT
        classid, objid, objsubid, original, normal, is_temporary,
        object_type, schema_name, object_name, object_identity,
        address_names, address_args
    FROM pg_event_trigger_dropped_objects();
END;
$event$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41153
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_test1 primary key (test1)
;
----------
[ADMIN_TABLE] ANALYZE public.view_base_table
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_sys_tables AS
    SELECT * FROM pg_stat_all_tables
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
----------
[CREATE_TYPE] -- populate_array()
create type jsb_ia as (a int[])
;
----------
[ALTER_INDEX] ALTER INDEX testschema.part_a_idx SET TABLESPACE regress_tblspace
;
----------
[CREATE_POLICY] /*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
----------
[DROP_INDEX] DROP INDEX enumtest_btree
;
----------
[ALTER_TRIGGER] alter event trigger regress_event_trigger enable always
;
----------
[ALTER_LIBRARY] ALTER EXTENSION pageinspect UPDATE TO '1.11'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION admin_sub CONNECTION 'port=10246 host=/tmp/jZENFq3HZU dbname=postgres' PUBLICATION alice WITH (password_required=false)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fkpart7.pkpart1 ADD PRIMARY KEY (a)
;
----------
[CREATE_POLICY] /*
 * text search configuration for norwegian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * norwegian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_depend
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vv AS
  SELECT * FROM test_tablesample TABLESAMPLE system_time (20)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33089_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_POLICY] -- OK
ALTER TEXT SEARCH DICTIONARY alt_ts_dict2 SET SCHEMA alt_nsp2
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_after2 ON rem1
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table t2 alter column z drop not null
;
----------
[DROP_TABLE] DROP TABLE sequence_test_table
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."my_credit_card_usage_normal" AS
 SELECT "l"."cid",
    "l"."name",
    "l"."tel",
    "l"."passwd",
    "l"."cnum",
    "l"."climit",
    "r"."ymd",
    "r"."usage"
   FROM ("public"."my_credit_card_secure" "l"
     JOIN "public"."credit_usage" "r" USING ("cid"))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regression_sub CONNECTION 'port=16820 host=/tmp/ekP6Yh8edH dbname=postgres' PUBLICATION alice
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION alter_op_test_fn_real_bool(real, boolean)
RETURNS boolean AS $$ SELECT NULL::BOOLEAN; $$ LANGUAGE sql IMMUTABLE
;
----------
[RENAME_TABLE] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 RENAME TO foreign_table_1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_cast
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_wchars_to_text
;
----------
[CREATE_TABLE|SELECT] CREATE TEMPORARY TABLE pgss_ctas_1 AS SELECT 1
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY alt_ts_dict1 RENAME TO alt_ts_dict2
;
----------
[ALTER_TYPE] ALTER TYPE test_type2 DROP ATTRIBUTE b
;
----------
[DROP_PROG_OBJ] DROP FUNCTION dup(f1 anycompatible, f2 anycompatiblearray)
;
----------
[ALTER_TABLE] ALTER TABLE "public"."rule_merge1" DISABLE RULE "rule2"
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH PARSER addr_ts_prs
    (start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
----------
[CREATE_TABLE] -- Test ON CONFLICT DO UPDATE with partitioned table and non-identical children

CREATE TABLE upsert_test (
    a   INT PRIMARY KEY,
    b   TEXT
) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] create table hp2 partition of hp for values with (modulus 4, remainder 2)
;
----------
[DROP_SCHEMA] DROP SCHEMA pub_test1 CASCADE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=19323 host=/tmp/FohDx5iOiZ dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE seq
;
----------
[CREATE_TABLE] CREATE TABLE rngfunc (rngfuncid int, rngfuncsubid int, rngfuncname text, primary key(rngfuncid,rngfuncsubid))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR # (
	LEFTARG = _int4,
	RIGHTARG = int4,
	PROCEDURE = idx
)
;
----------
[CREATE_TABLE] create temp table temp_part_child (a int)
;
----------
[ADD_INDEX] CREATE INDEX "tenk1_thous_tenthous" ON "public"."tenk1" USING "btree" ("thousand", "tenthous")
;
----------
[ADMIN_TABLE] VACUUM (ONLY_DATABASE_STATS true, VERBOSE true)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_publication_namespace
;
