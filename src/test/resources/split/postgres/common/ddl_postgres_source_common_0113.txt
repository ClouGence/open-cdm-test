VACUUM pg_catalog.pg_rewrite
;
CREATE VIEW shoelace_obsolete AS
	SELECT * FROM shoelace WHERE NOT EXISTS
	    (SELECT shoename FROM shoe WHERE slcolor = sl_color)
;
CREATE TABLE selinto_schema.tbl_nodata3 (a) AS
  EXECUTE data_sel WITH NO DATA
;
CREATE PROCEDURE transaction_test10b(INOUT x int)
LANGUAGE plpgsql
AS $$
BEGIN
  x := x - 1;
  ROLLBACK;
END;
$$
;
-- Change prt1_adv_p1 to the default partition
ALTER TABLE prt1_adv ATTACH PARTITION prt1_adv_p1 DEFAULT
;
create domain ddef3 text DEFAULT 5
;
ALTER PUBLICATION tap_pub_3 SET (publish_via_partition_root = true)
;
-- Test comments
COMMENT ON OPERATOR ###### (int4, NONE) IS 'bad right unary'
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_foreign_server
;
-- test ordered-set aggs using built-in support functions
create aggregate my_percentile_disc(float8 ORDER BY anyelement) (
  stype = internal,
  sfunc = ordered_set_transition,
  finalfunc = percentile_disc_final,
  finalfunc_extra = true,
  finalfunc_modify = read_write
)
;
alter table p11 add a int not null
;
create unlogged table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=100)
;
CREATE TABLE sf_target(id int, data text, filling int[])
;
CREATE TRIGGER trig_row_after
AFTER INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
DROP TABLE clstrpart
;
COMMENT ON TEXT SEARCH DICTIONARY greek_stem IS 'snowball stemmer for greek language'
;
ANALYZE public.clstr_tst
;
CREATE TABLE foo_range_parted (a int, b text) PARTITION BY RANGE(a)
;
DROP EVENT TRIGGER has_volatile_rewrite
;
create table tt7 (x int, xx int, y int)
;
CALL ptest6a(1.1, null)
;
CREATE FUNCTION gbt_cash_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION _int_overlap(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ANALYZE VERBOSE split_maint.maint_table
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) information_schema.sql_sizing
;
ANALYZE fkpart3.pk1
;
--
-- Test that LIMIT can be pushed to SORT through a subquery that just projects
-- columns.  We check for that having happened by looking to see if EXPLAIN
-- ANALYZE shows that a top-N sort was used.  We must suppress or filter away
-- all the non-invariant parts of the EXPLAIN ANALYZE output.
--
create table sq_limit (pk int primary key, c1 int, c2 int)
;
-- insert/update/delete row-level triggers on the first partition
create trigger trig_ins_before_child before insert on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
create trigger qqqaf after insert or update or delete on copydml_test
    for each row execute procedure qqq_trig()
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_description
;
--
-- Test adding a support function to a subject function
--

CREATE FUNCTION my_int_eq(int, int) RETURNS bool
  LANGUAGE internal STRICT IMMUTABLE PARALLEL SAFE
  AS $$int4eq$$
;
DROP SCHEMA foreign_schema CASCADE
;
ANALYZE public.beta_neg_p3
;
CREATE FUNCTION gin_extract_value_inet(inet, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
VACUUM (FREEZE) pg_catalog.pg_cast
;
CREATE VIEW user_defined_types AS
    SELECT CAST(current_database() AS sql_identifier) AS user_defined_type_catalog,
           CAST(n.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(c.relname AS sql_identifier) AS user_defined_type_name,
           CAST('STRUCTURED' AS character_data) AS user_defined_type_category,
           CAST('YES' AS yes_or_no) AS is_instantiable,
           CAST(null AS yes_or_no) AS is_final,
           CAST(null AS character_data) AS ordering_form,
           CAST(null AS character_data) AS ordering_category,
           CAST(null AS sql_identifier) AS ordering_routine_catalog,
           CAST(null AS sql_identifier) AS ordering_routine_schema,
           CAST(null AS sql_identifier) AS ordering_routine_name,
           CAST(null AS character_data) AS reference_type,
           CAST(null AS character_data) AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(null AS sql_identifier) AS source_dtd_identifier,
           CAST(null AS sql_identifier) AS ref_dtd_identifier

    FROM pg_namespace n, pg_class c, pg_type t

    WHERE n.oid = c.relnamespace
          AND t.typrelid = c.oid
          AND c.relkind = 'c'
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
alter table pgbench_accounts add primary key (aid) using index tablespace "pg_default"
;
CLUSTER clstrpart USING clstrpart_idx
;
-- add identity column
CREATE TABLE pitest2 (f1 date NOT NULL, f2 text) PARTITION BY RANGE (f1)
;
CREATE FUNCTION issn13(ean13)
RETURNS issn13
AS '$libdir/isn', 'issn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE ONLY "public"."gtest22a"
    ADD CONSTRAINT "gtest22a_pkey" PRIMARY KEY ("a")
;
CREATE TABLE perm_part (a int)
;
CREATE TABLE distinct_on_tbl (x int, y int, z int)
;
alter type rewritetype alter attribute a type varchar cascade
;
CREATE RECURSIVE VIEW split_view_probe.counting_view(n) AS SELECT 1 UNION ALL SELECT n + 1 FROM split_view_probe.counting_view WHERE n < 3
;
CREATE AGGREGATE "public"."myaggp06a"(integer) (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
VACUUM (FULL) pg_catalog.pg_replication_origin
;
CREATE TABLE tab_rowfilter_less_10k (LIKE tab_rowfilter_partitioned)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "numhword" WITH "public"."ispell", "english_stem"
;
create table join_pt1p1p1 partition of join_pt1p1 for values from (0) to (100)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM pg_catalog.pg_rewrite
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW shoelace_obsolete AS
	SELECT * FROM shoelace WHERE NOT EXISTS
	    (SELECT shoename FROM shoe WHERE slcolor = sl_color)
;
----------
[CREATE_TABLE|UNSAFE] CREATE TABLE selinto_schema.tbl_nodata3 (a) AS
  EXECUTE data_sel WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test10b(INOUT x int)
LANGUAGE plpgsql
AS $$
BEGIN
  x := x - 1;
  ROLLBACK;
END;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Change prt1_adv_p1 to the default partition
ALTER TABLE prt1_adv ATTACH PARTITION prt1_adv_p1 DEFAULT
;
----------
[CREATE_TYPE] create domain ddef3 text DEFAULT 5
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_3 SET (publish_via_partition_root = true)
;
----------
[COMMENT_PROG_OBJ] -- Test comments
COMMENT ON OPERATOR ###### (int4, NONE) IS 'bad right unary'
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_foreign_server
;
----------
[CREATE_PROG_OBJ] -- test ordered-set aggs using built-in support functions
create aggregate my_percentile_disc(float8 ORDER BY anyelement) (
  stype = internal,
  sfunc = ordered_set_transition,
  finalfunc = percentile_disc_final,
  finalfunc_extra = true,
  finalfunc_modify = read_write
)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table p11 add a int not null
;
----------
[CREATE_TABLE] create unlogged table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=100)
;
----------
[CREATE_TABLE] CREATE TABLE sf_target(id int, data text, filling int[])
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_after
AFTER INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[DROP_TABLE] DROP TABLE clstrpart
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY greek_stem IS 'snowball stemmer for greek language'
;
----------
[ADMIN_TABLE] ANALYZE public.clstr_tst
;
----------
[CREATE_TABLE] CREATE TABLE foo_range_parted (a int, b text) PARTITION BY RANGE(a)
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER has_volatile_rewrite
;
----------
[CREATE_TABLE] create table tt7 (x int, xx int, y int)
;
----------
[CALL_PROG_OBJ] CALL ptest6a(1.1, null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_cash_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_overlap(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE VERBOSE split_maint.maint_table
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) information_schema.sql_sizing
;
----------
[ADMIN_TABLE] ANALYZE fkpart3.pk1
;
----------
[CREATE_TABLE] --
-- Test that LIMIT can be pushed to SORT through a subquery that just projects
-- columns.  We check for that having happened by looking to see if EXPLAIN
-- ANALYZE shows that a top-N sort was used.  We must suppress or filter away
-- all the non-invariant parts of the EXPLAIN ANALYZE output.
--
create table sq_limit (pk int primary key, c1 int, c2 int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- insert/update/delete row-level triggers on the first partition
create trigger trig_ins_before_child before insert on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger qqqaf after insert or update or delete on copydml_test
    for each row execute procedure qqq_trig()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_description
;
----------
[CREATE_PROG_OBJ] --
-- Test adding a support function to a subject function
--

CREATE FUNCTION my_int_eq(int, int) RETURNS bool
  LANGUAGE internal STRICT IMMUTABLE PARALLEL SAFE
  AS $$int4eq$$
;
----------
[DROP_SCHEMA] DROP SCHEMA foreign_schema CASCADE
;
----------
[ADMIN_TABLE] ANALYZE public.beta_neg_p3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_inet(inet, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_cast
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW user_defined_types AS
    SELECT CAST(current_database() AS sql_identifier) AS user_defined_type_catalog,
           CAST(n.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(c.relname AS sql_identifier) AS user_defined_type_name,
           CAST('STRUCTURED' AS character_data) AS user_defined_type_category,
           CAST('YES' AS yes_or_no) AS is_instantiable,
           CAST(null AS yes_or_no) AS is_final,
           CAST(null AS character_data) AS ordering_form,
           CAST(null AS character_data) AS ordering_category,
           CAST(null AS sql_identifier) AS ordering_routine_catalog,
           CAST(null AS sql_identifier) AS ordering_routine_schema,
           CAST(null AS sql_identifier) AS ordering_routine_name,
           CAST(null AS character_data) AS reference_type,
           CAST(null AS character_data) AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(null AS sql_identifier) AS source_dtd_identifier,
           CAST(null AS sql_identifier) AS ref_dtd_identifier

    FROM pg_namespace n, pg_class c, pg_type t

    WHERE n.oid = c.relnamespace
          AND t.typrelid = c.oid
          AND c.relkind = 'c'
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_accounts add primary key (aid) using index tablespace "pg_default"
;
----------
[ADMIN_TABLE] CLUSTER clstrpart USING clstrpart_idx
;
----------
[CREATE_TABLE] -- add identity column
CREATE TABLE pitest2 (f1 date NOT NULL, f2 text) PARTITION BY RANGE (f1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION issn13(ean13)
RETURNS issn13
AS '$libdir/isn', 'issn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest22a"
    ADD CONSTRAINT "gtest22a_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE perm_part (a int)
;
----------
[CREATE_TABLE] CREATE TABLE distinct_on_tbl (x int, y int, z int)
;
----------
[ALTER_TYPE] alter type rewritetype alter attribute a type varchar cascade
;
----------
[CREATE_VIEW(SELECT)] CREATE RECURSIVE VIEW split_view_probe.counting_view(n) AS SELECT 1 UNION ALL SELECT n + 1 FROM split_view_probe.counting_view WHERE n < 3
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp06a"(integer) (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_less_10k (LIKE tab_rowfilter_partitioned)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "numhword" WITH "public"."ispell", "english_stem"
;
----------
[CREATE_TABLE] create table join_pt1p1p1 partition of join_pt1p1 for values from (0) to (100)
;
