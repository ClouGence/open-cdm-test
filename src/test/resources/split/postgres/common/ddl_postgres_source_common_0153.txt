create unique index tricky_expr_comp_key_index on insertconflicttest(key, lower(fruit), upper(fruit))
;
CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val))
;
CREATE SUBSCRIPTION test_tab1_sub CONNECTION 'port=22821 host=/tmp/Ajgausiysd dbname=postgres' PUBLICATION test_tab1_pub
;
create table returningwrtest2 (b text, c int, a int)
;
--
-- Ranges of arrays
--

create type arrayrange as range (subtype=int4[])
;
ALTER TABLE temporal3
  ADD CONSTRAINT temporal3_pk
  PRIMARY KEY USING INDEX idx_temporal3_uq
;
CREATE FUNCTION uses_a_temp_type(just_give_me_a_type) RETURNS int LANGUAGE sql AS $$SELECT 1;$$
;
-- We are allowed to create transition-table triggers on both kinds of
-- inheritance even if they contain foreign tables as children, but currently
-- collecting transition tuples from such foreign tables is not supported.

CREATE TABLE local_tbl (a text, b int)
;
ALTER TABLE errtst ATTACH PARTITION errtst_part_1 FOR VALUES IN ('aaa')
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub1, pub2
;
CREATE TABLE reloptions_test2(i INT) WITH (not_existing_option=2)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest24r"
    ADD CONSTRAINT "gtest24r_pkey" PRIMARY KEY ("a")
;
ANALYZE pg_temp_4.pg_temp_26914
;
create or replace function returnssetofholes() returns setof has_hole language plpgsql as
$$
declare r record;
begin
  return next r;  -- fails, not assigned yet
end$$
;
CREATE FUNCTION regtest_func(text,int[]) RETURNS bool LANGUAGE plpgsql
	   AS 'BEGIN RAISE NOTICE ''regtest_func => %'', $1; RETURN true; END'
;
COMMENT ON LARGE OBJECT 34070 IS 'I Wandered Lonely as a Cloud'
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_foreign_server
;
CREATE TRIGGER trigger_tab_stats
    AFTER UPDATE ON trigger_tab_stats
    FOR EACH ROW WHEN (OLD.a < 0 AND OLD.b < 1 AND true)
    EXECUTE FUNCTION trigger_func_stats()
;
CREATE TABLE gtest20 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED CHECK (b < 50))
;
drop domain ddtest1d
;
CREATE TABLE bla(s NO_SUCH_TYPE)
;
CREATE SUBSCRIPTION tap_sub_A_B_2
        CONNECTION 'port=11382 host=/tmp/C6o4PTAnCK dbname=postgres application_name=tap_sub_A_B_2'
        PUBLICATION tap_pub_B
        WITH (origin = none, copy_data = on)
;
CREATE VIEW "public"."my_credit_card_normal" AS
 SELECT "l"."cid",
    "l"."name",
    "l"."tel",
    "l"."passwd",
    "r"."cnum",
    "r"."climit"
   FROM ("public"."customer" "l"
     JOIN "public"."credit_card" "r" USING ("cid"))
  WHERE ("l"."name" = CURRENT_USER)
;
CREATE FUNCTION pgp_sym_encrypt_bytea(bytea, text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_encrypt_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE reind_con_wide(id int primary key, data text)
;
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 100, REMAINDER 3)
;
create table test_part_coll2 partition of test_part_coll_posix for values from ('g') to ('m')
;
create temp table inh_temp_child () inherits (inh_perm_parent)
;
CREATE TABLE public.ths_p2 (
    mod integer,
    data integer
)
;
ALTER TABLE ONLY "public"."utf8_verification_inputs"
    ADD CONSTRAINT "utf8_verification_inputs_pkey" PRIMARY KEY ("description")
;
VACUUM ANALYZE inettmp
;
ALTER EXTENSION seg DROP function gseg_compress(pg_catalog.internal)
;
-- Check dependency transfer of opaque functions when creating a new type
CREATE FUNCTION base_fn_in(cstring) RETURNS opaque AS 'boolin'
    LANGUAGE internal IMMUTABLE STRICT
;
create table t1 as select 1 as i from generate_series(1, 10000)
;
DROP TABLE lock_tbl1
;
COMMENT ON TEXT SEARCH CONFIGURATION greek IS 'configuration for greek language'
;
ALTER OPERATOR >= (ean13, upc) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
CREATE FOREIGN TABLE header_match ("1" int, foo text) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/list1.csv', delimiter ',', header 'match')
;
ANALYZE public.plt1_e_p1
;
-- try the unnamed-constraint case
alter table ac add check (aa is not null)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_proc
;
CREATE TABLE tab_ins (a int)
;
vacuum freeze pg_catalog.pg_inherits
;
CREATE INDEX on tbl USING brin(c1, c2) INCLUDE (c3, c4)
;
CREATE FUNCTION trig_null() RETURNS TRIGGER AS $$
  BEGIN
    RETURN NULL;
  END
$$ language plpgsql
;
CREATE TEMP TABLE pg_temp_26.pg_temp_28654_2 (tid pg_catalog.tid)
;
-- gist key
CREATE FUNCTION gtrgm_in(cstring)
RETURNS gtrgm
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_30921_index RENAME TO reind_con_toast_idx
;
CREATE TABLE test_add_column(c1 integer)
;
CREATE TYPE base_type
;
------------------------------------------------------------------------------------------
[ADD_INDEX] create unique index tricky_expr_comp_key_index on insertconflicttest(key, lower(fruit), upper(fruit))
;
----------
[CREATE_TABLE] CREATE TABLE ints (key int, val text, PRIMARY KEY (key) INCLUDE (val))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION test_tab1_sub CONNECTION 'port=22821 host=/tmp/Ajgausiysd dbname=postgres' PUBLICATION test_tab1_pub
;
----------
[CREATE_TABLE] create table returningwrtest2 (b text, c int, a int)
;
----------
[CREATE_TYPE] --
-- Ranges of arrays
--

create type arrayrange as range (subtype=int4[])
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE temporal3
  ADD CONSTRAINT temporal3_pk
  PRIMARY KEY USING INDEX idx_temporal3_uq
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION uses_a_temp_type(just_give_me_a_type) RETURNS int LANGUAGE sql AS $$SELECT 1;$$
;
----------
[CREATE_TABLE] -- We are allowed to create transition-table triggers on both kinds of
-- inheritance even if they contain foreign tables as children, but currently
-- collecting transition tuples from such foreign tables is not supported.

CREATE TABLE local_tbl (a text, b int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE errtst ATTACH PARTITION errtst_part_1 FOR VALUES IN ('aaa')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub1, pub2
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (not_existing_option=2)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest24r"
    ADD CONSTRAINT "gtest24r_pkey" PRIMARY KEY ("a")
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_26914
;
----------
[CREATE_PROG_OBJ] create or replace function returnssetofholes() returns setof has_hole language plpgsql as
$$
declare r record;
begin
  return next r;  -- fails, not assigned yet
end$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regtest_func(text,int[]) RETURNS bool LANGUAGE plpgsql
	   AS 'BEGIN RAISE NOTICE ''regtest_func => %'', $1; RETURN true; END'
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 34070 IS 'I Wandered Lonely as a Cloud'
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_foreign_server
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trigger_tab_stats
    AFTER UPDATE ON trigger_tab_stats
    FOR EACH ROW WHEN (OLD.a < 0 AND OLD.b < 1 AND true)
    EXECUTE FUNCTION trigger_func_stats()
;
----------
[CREATE_TABLE] CREATE TABLE gtest20 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED CHECK (b < 50))
;
----------
[DROP_TYPE] drop domain ddtest1d
;
----------
[CREATE_TABLE] CREATE TABLE bla(s NO_SUCH_TYPE)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_A_B_2
        CONNECTION 'port=11382 host=/tmp/C6o4PTAnCK dbname=postgres application_name=tap_sub_A_B_2'
        PUBLICATION tap_pub_B
        WITH (origin = none, copy_data = on)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."my_credit_card_normal" AS
 SELECT "l"."cid",
    "l"."name",
    "l"."tel",
    "l"."passwd",
    "r"."cnum",
    "r"."climit"
   FROM ("public"."customer" "l"
     JOIN "public"."credit_card" "r" USING ("cid"))
  WHERE ("l"."name" = CURRENT_USER)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgp_sym_encrypt_bytea(bytea, text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_encrypt_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE reind_con_wide(id int primary key, data text)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 100, REMAINDER 3)
;
----------
[CREATE_TABLE] create table test_part_coll2 partition of test_part_coll_posix for values from ('g') to ('m')
;
----------
[CREATE_TABLE] create temp table inh_temp_child () inherits (inh_perm_parent)
;
----------
[CREATE_TABLE] CREATE TABLE public.ths_p2 (
    mod integer,
    data integer
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."utf8_verification_inputs"
    ADD CONSTRAINT "utf8_verification_inputs_pkey" PRIMARY KEY ("description")
;
----------
[ADMIN_TABLE] VACUUM ANALYZE inettmp
;
----------
[ALTER_LIBRARY] ALTER EXTENSION seg DROP function gseg_compress(pg_catalog.internal)
;
----------
[CREATE_PROG_OBJ] -- Check dependency transfer of opaque functions when creating a new type
CREATE FUNCTION base_fn_in(cstring) RETURNS opaque AS 'boolin'
    LANGUAGE internal IMMUTABLE STRICT
;
----------
[CREATE_TABLE|SELECT] create table t1 as select 1 as i from generate_series(1, 10000)
;
----------
[DROP_TABLE] DROP TABLE lock_tbl1
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION greek IS 'configuration for greek language'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ean13, upc) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE header_match ("1" int, foo text) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/list1.csv', delimiter ',', header 'match')
;
----------
[ADMIN_TABLE] ANALYZE public.plt1_e_p1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- try the unnamed-constraint case
alter table ac add check (aa is not null)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_proc
;
----------
[CREATE_TABLE] CREATE TABLE tab_ins (a int)
;
----------
[ADMIN_TABLE] vacuum freeze pg_catalog.pg_inherits
;
----------
[ADD_INDEX] CREATE INDEX on tbl USING brin(c1, c2) INCLUDE (c3, c4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trig_null() RETURNS TRIGGER AS $$
  BEGIN
    RETURN NULL;
  END
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_26.pg_temp_28654_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] -- gist key
CREATE FUNCTION gtrgm_in(cstring)
RETURNS gtrgm
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30921_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE test_add_column(c1 integer)
;
----------
[CREATE_TYPE] CREATE TYPE base_type
;
