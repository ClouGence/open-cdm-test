create table mcrparted5 partition of mcrparted for values from (20, 20, 20) to (maxvalue, maxvalue, maxvalue)
;
CREATE TRIGGER trig_skip_delete before delete on sub_part2
   for each row execute procedure func_parted_mod_b()
;
--
-- Test ALL policies with ON CONFLICT DO UPDATE (much the same as existing UPDATE
-- tests)
--
CREATE POLICY p3_with_all ON document FOR ALL
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
  WITH CHECK (dauthor = current_user)
;
--
-- rename - check on both non-temp and temp tables
--
CREATE TABLE attmp (regtable int)
;
CREATE TABLE "public"."partr_def2" (
    "a" integer NOT NULL,
    "b" integer NOT NULL,
    CONSTRAINT "part1_a_check" CHECK (("a" = 1)),
    CONSTRAINT "part1_b_check" CHECK ((("b" >= 1) AND ("b" <= 10)))
)
;
ALTER TABLE ONLY "regress_indexing"."pk1"
    ADD CONSTRAINT "pk1_pkey" PRIMARY KEY ("a")
;
CREATE INDEX iprt2_p3_b on prt2_p3(b)
;
ANALYZE public.domcontest
;
DROP VIEW view_unnamed_ss
;
alter table part_abc add d int
;
DROP TABLE unbounded_range_part
;
COMMENT ON OPERATOR FAMILY split_operator_probe.int_hash_family USING hash IS 'split hash family'
;
drop function parted_trigfunc()
;
--
-- PLPGSQL
--
-- Scenario:
--
--     A building with a modern TP cable installation where any
--     of the wall connectors can be used to plug in phones,
--     ethernet interfaces or local office hubs. The backside
--     of the wall connectors is wired to one of several patch-
--     fields in the building.
--
--     In the patchfields, there are hubs and all the slots
--     representing the wall connectors. In addition there are
--     slots that can represent a phone line from the central
--     phone system.
--
--     Triggers ensure consistency of the patching information.
--
--     Functions are used to build up powerful views that let
--     you look behind the wall when looking at a patchfield
--     or into a room.
--


create table Room (
    roomno	char(8),
    comment	text
)
;
-- check handling of types as extension members
create type varbitrange as range (subtype = varbit)
;
/* Throw error */
CREATE TABLE inhf (LIKE inhx INCLUDING DEFAULTS INCLUDING CONSTRAINTS)
;
-- Nothing is processed.
VACUUM (PROCESS_MAIN FALSE, PROCESS_TOAST FALSE) vac_option_tab
;
alter table "abc" drop column if exists address CASCADE
;
alter table permtest_parent attach partition permtest_child for values in (1)
;
create rule rtest_order_r1 as on insert to rtest_order1 do instead
	insert into rtest_order2 values (new.a, nextval('rtest_seq'),
		'rule 1 - this should run 1st')
;
CREATE AGGREGATE myaggp09a(BASETYPE = int, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_largeobject
;
DROP TABLE pagg_tab6
;
CREATE TYPE test2 AS (a bigint, b text)
;
ALTER OPERATOR >= (ismn13, ismn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
ALTER TEXT SEARCH CONFIGURATION spanish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH spanish_stem
;
create view Pfield_v1 as select PF.pfname, PF.slotname,
	pslot_backlink_view(PF.slotname) as backside,
	pslot_slotlink_view(PF.slotname) as patch
    from PSlot PF
;
create trigger child_del_trig after delete on child
  for each row execute procedure child_del_func()
;
ALTER OPERATOR FAMILY gist__int_ops USING gist
ADD FUNCTION 10 (_int4) g_int_options (internal)
;
create trigger check_pkeys_fkey_cascade
	before delete or update on pkeys
	for each row
	execute procedure
	check_foreign_key (2, 'cascade', 'pkey1', 'pkey2',
	'fkeys', 'fkey1', 'fkey2', 'fkeys2', 'fkey21', 'fkey22')
;
CREATE VIEW pg_user_mappings AS
    SELECT
        U.oid       AS umid,
        S.oid       AS srvid,
        S.srvname   AS srvname,
        U.umuser    AS umuser,
        CASE WHEN U.umuser = 0 THEN
            'public'
        ELSE
            A.rolname
        END AS usename,
        CASE WHEN (U.umuser <> 0 AND A.rolname = current_user
                     AND (pg_has_role(S.srvowner, 'USAGE')
                          OR has_server_privilege(S.oid, 'USAGE')))
                    OR (U.umuser = 0 AND pg_has_role(S.srvowner, 'USAGE'))
                    OR (SELECT rolsuper FROM pg_authid WHERE rolname = current_user)
                    THEN U.umoptions
                 ELSE NULL END AS umoptions
    FROM pg_user_mapping U
        JOIN pg_foreign_server S ON (U.umserver = S.oid)
        LEFT JOIN pg_authid A ON (A.oid = U.umuser)
;
CREATE TABLE tbl3 (e int, f int)
;
CREATE TABLE plt1_e_p3 PARTITION OF plt1_e FOR VALUES IN ('0006', '0007', '0008', '0011')
;
-- Change prt2_adv_p3 to the default partition
ALTER TABLE prt2_adv ATTACH PARTITION prt2_adv_p3 DEFAULT
;
ALTER FUNCTION gbt_inet_picksplit(internal, internal) PARALLEL SAFE
;
-- System columns may be part of an updatable view
CREATE VIEW rw_view15 AS SELECT a, upper(b) FROM base_tbl
;
ANALYZE pg_temp_26.pg_temp_28654
;
ANALYZE pg_temp_14.pg_temp_26781
;
create function some_tab_stmt_trig_func() returns trigger as
$$begin raise notice 'updating some_tab'; return NULL; end;$$
language plpgsql
;
-- a regconfig constant referring to this text search configuration
-- is initially unshippable
CREATE TEXT SEARCH CONFIGURATION public.custom_search
  (COPY = pg_catalog.english)
;
CREATE PROCEDURE test_proc11(a OUT int, VARIADIC b int[])
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %', a, b;
  a := b[1] + b[2];
END;
$$
;
create table array_test_table(a int, b varchar)
;
CREATE UNIQUE INDEX func_index_index on func_index_heap (textcat(f1,f2))
;
-- error
CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION "public"
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '-', quote '-')
;
VACUUM (ANALYZE) public.vactable(c)
;
CREATE TABLE prt2_adv (a int, b int, c varchar) PARTITION BY RANGE (b)
;
CREATE TYPE test_type1 AS (a int, b text)
;
create domain ddef2 oid DEFAULT '12'
;
ANALYZE public.numrange_test
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table mcrparted5 partition of mcrparted for values from (20, 20, 20) to (maxvalue, maxvalue, maxvalue)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_skip_delete before delete on sub_part2
   for each row execute procedure func_parted_mod_b()
;
----------
[CREATE_POLICY] --
-- Test ALL policies with ON CONFLICT DO UPDATE (much the same as existing UPDATE
-- tests)
--
CREATE POLICY p3_with_all ON document FOR ALL
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
  WITH CHECK (dauthor = current_user)
;
----------
[CREATE_TABLE] --
-- rename - check on both non-temp and temp tables
--
CREATE TABLE attmp (regtable int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."partr_def2" (
    "a" integer NOT NULL,
    "b" integer NOT NULL,
    CONSTRAINT "part1_a_check" CHECK (("a" = 1)),
    CONSTRAINT "part1_b_check" CHECK ((("b" >= 1) AND ("b" <= 10)))
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "regress_indexing"."pk1"
    ADD CONSTRAINT "pk1_pkey" PRIMARY KEY ("a")
;
----------
[ADD_INDEX] CREATE INDEX iprt2_p3_b on prt2_p3(b)
;
----------
[ADMIN_TABLE] ANALYZE public.domcontest
;
----------
[DROP_VIEW] DROP VIEW view_unnamed_ss
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table part_abc add d int
;
----------
[DROP_TABLE] DROP TABLE unbounded_range_part
;
----------
[COMMENT_PROG_OBJ] COMMENT ON OPERATOR FAMILY split_operator_probe.int_hash_family USING hash IS 'split hash family'
;
----------
[DROP_PROG_OBJ] drop function parted_trigfunc()
;
----------
[CREATE_TABLE] --
-- PLPGSQL
--
-- Scenario:
--
--     A building with a modern TP cable installation where any
--     of the wall connectors can be used to plug in phones,
--     ethernet interfaces or local office hubs. The backside
--     of the wall connectors is wired to one of several patch-
--     fields in the building.
--
--     In the patchfields, there are hubs and all the slots
--     representing the wall connectors. In addition there are
--     slots that can represent a phone line from the central
--     phone system.
--
--     Triggers ensure consistency of the patching information.
--
--     Functions are used to build up powerful views that let
--     you look behind the wall when looking at a patchfield
--     or into a room.
--


create table Room (
    roomno	char(8),
    comment	text
)
;
----------
[CREATE_TYPE] -- check handling of types as extension members
create type varbitrange as range (subtype = varbit)
;
----------
[CREATE_TABLE] /* Throw error */
CREATE TABLE inhf (LIKE inhx INCLUDING DEFAULTS INCLUDING CONSTRAINTS)
;
----------
[ADMIN_TABLE] -- Nothing is processed.
VACUUM (PROCESS_MAIN FALSE, PROCESS_TOAST FALSE) vac_option_tab
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table "abc" drop column if exists address CASCADE
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table permtest_parent attach partition permtest_child for values in (1)
;
----------
[CREATE_POLICY] create rule rtest_order_r1 as on insert to rtest_order1 do instead
	insert into rtest_order2 values (new.a, nextval('rtest_seq'),
		'rule 1 - this should run 1st')
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp09a(BASETYPE = int, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_largeobject
;
----------
[DROP_TABLE] DROP TABLE pagg_tab6
;
----------
[CREATE_TYPE] CREATE TYPE test2 AS (a bigint, b text)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ismn13, ismn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION spanish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH spanish_stem
;
----------
[CREATE_VIEW(SELECT)] create view Pfield_v1 as select PF.pfname, PF.slotname,
	pslot_backlink_view(PF.slotname) as backside,
	pslot_slotlink_view(PF.slotname) as patch
    from PSlot PF
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child_del_trig after delete on child
  for each row execute procedure child_del_func()
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist__int_ops USING gist
ADD FUNCTION 10 (_int4) g_int_options (internal)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger check_pkeys_fkey_cascade
	before delete or update on pkeys
	for each row
	execute procedure
	check_foreign_key (2, 'cascade', 'pkey1', 'pkey2',
	'fkeys', 'fkey1', 'fkey2', 'fkeys2', 'fkey21', 'fkey22')
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW pg_user_mappings AS
    SELECT
        U.oid       AS umid,
        S.oid       AS srvid,
        S.srvname   AS srvname,
        U.umuser    AS umuser,
        CASE WHEN U.umuser = 0 THEN
            'public'
        ELSE
            A.rolname
        END AS usename,
        CASE WHEN (U.umuser <> 0 AND A.rolname = current_user
                     AND (pg_has_role(S.srvowner, 'USAGE')
                          OR has_server_privilege(S.oid, 'USAGE')))
                    OR (U.umuser = 0 AND pg_has_role(S.srvowner, 'USAGE'))
                    OR (SELECT rolsuper FROM pg_authid WHERE rolname = current_user)
                    THEN U.umoptions
                 ELSE NULL END AS umoptions
    FROM pg_user_mapping U
        JOIN pg_foreign_server S ON (U.umserver = S.oid)
        LEFT JOIN pg_authid A ON (A.oid = U.umuser)
;
----------
[CREATE_TABLE] CREATE TABLE tbl3 (e int, f int)
;
----------
[CREATE_TABLE] CREATE TABLE plt1_e_p3 PARTITION OF plt1_e FOR VALUES IN ('0006', '0007', '0008', '0011')
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Change prt2_adv_p3 to the default partition
ALTER TABLE prt2_adv ATTACH PARTITION prt2_adv_p3 DEFAULT
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_inet_picksplit(internal, internal) PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] -- System columns may be part of an updatable view
CREATE VIEW rw_view15 AS SELECT a, upper(b) FROM base_tbl
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_26.pg_temp_28654
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_14.pg_temp_26781
;
----------
[CREATE_PROG_OBJ] create function some_tab_stmt_trig_func() returns trigger as
$$begin raise notice 'updating some_tab'; return NULL; end;$$
language plpgsql
;
----------
[CREATE_POLICY] -- a regconfig constant referring to this text search configuration
-- is initially unshippable
CREATE TEXT SEARCH CONFIGURATION public.custom_search
  (COPY = pg_catalog.english)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc11(a OUT int, VARIADIC b int[])
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %', a, b;
  a := b[1] + b[2];
END;
$$
;
----------
[CREATE_TABLE] create table array_test_table(a int, b varchar)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX func_index_index on func_index_heap (textcat(f1,f2))
;
----------
[CREATE_SCHEMA] -- error
CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION "public"
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '-', quote '-')
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) public.vactable(c)
;
----------
[CREATE_TABLE] CREATE TABLE prt2_adv (a int, b int, c varchar) PARTITION BY RANGE (b)
;
----------
[CREATE_TYPE] CREATE TYPE test_type1 AS (a int, b text)
;
----------
[CREATE_TYPE] create domain ddef2 oid DEFAULT '12'
;
----------
[ADMIN_TABLE] ANALYZE public.numrange_test
;
