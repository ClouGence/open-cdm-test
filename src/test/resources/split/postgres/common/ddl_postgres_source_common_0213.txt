CREATE SCHEMA split_stats_move
;
-- substring needing last byte of its slice_size
ALTER TABLE toast_3b_utf8 RENAME TO toast_4b_utf8
;
ALTER TABLE itest7d ADD COLUMN b int GENERATED ALWAYS AS IDENTITY
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = off,
    toast.vacuum_index_cleanup = yes)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_aggregate
;
alter extension test_ext9 drop table sometable
;
analyze atacc1("........pg.dropped.1........")
;
DROP TABLE prt2_adv_p3
;
drop table nulltest
;
DROP OPERATOR <<< (int, int)
;
CREATE TABLE "public"."notnull_tbl4_lk3" (
    "a" integer CONSTRAINT "a_nn" NOT NULL
)
;
CREATE TEMP TABLE INTERVAL_TBL_OF (f1 interval)
;
CREATE SCHEMA test_ns_schema_1
       CREATE UNIQUE INDEX abc_a_idx ON abc (a)
       CREATE VIEW abc_view AS
              SELECT a+1 AS a, b+1 AS b FROM abc
       CREATE TABLE abc (
              a serial,
              b int UNIQUE
       )
;
CREATE FUNCTION isnlt(issn13, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP TRIGGER modified_any ON main_table
;
CREATE SCHEMA newschemax AUTHORIZATION nonexistent
;
create trigger tg_wslot_bu before update
    on WSlot for each row execute procedure tg_Wslot_bu()
;
-- Test that remote triggers work with update tuple routing
create trigger loct_br_insert_trigger before insert on loct
	for each row execute procedure br_insert_trigfunc()
;
create table history ( customerid integer , timestamp timestamp without time zone , action_taken char ( 5 ) , narrative varchar ( 100 ) )
;
create materialized view parallel_mat_view as
    select length(stringu1) from tenk1 group by length(stringu1)
;
create operator <> (
  leftarg    = myint,
  rightarg   = myint,
  commutator = <>,
  negator    = =,
  procedure  = myintne,
  restrict   = eqsel,
  join       = eqjoinsel,
  merges
)
;
ALTER TABLE testpub_parted ATTACH PARTITION testpub_parted2 FOR VALUES IN (2)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_ts_config
;
vacuum ��ג�������ђ��
;
CREATE TABLE stream_test(data text)
;
SECURITY LABEL ON COLUMN t1p.o IS 'system_u:object_r:sepgsql_table_t:s0'
;
ALTER TABLE regtest_table ENABLE  TRIGGER regtest_test_trig
;
CREATE VIEW static_view AS
  SELECT 'foo'::TEXT AS col
;
CREATE SUBSCRIPTION tap_sub_rep_full CONNECTION 'port=20465 host=/tmp/vlW5Cd5sJm dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_rep_full
;
CREATE INDEX i2 ON s2.t2(id)
;
CREATE VIEW column_options AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(c.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.attname AS sql_identifier) AS column_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_table_columns c
;
ALTER SEQUENCE "public"."emp1_id_seq" OWNED BY "public"."emp1"."id"
;
DROP TABLE oer_test
;
create table mlparted_defd partition of mlparted_def default
;
CREATE INDEX grect2ind ON fast_emp4000 USING gist (home_base)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_inherits
;
ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (ean13, isbn13),
	OPERATOR 1  = (ean13, ismn13),
	OPERATOR 1  = (ean13, issn13),
	OPERATOR 1  = (ean13, isbn),
	OPERATOR 1  = (ean13, ismn),
	OPERATOR 1  = (ean13, issn),
	OPERATOR 1  = (ean13, upc)
;
CREATE OR REPLACE FUNCTION
  jsonb_insert(jsonb_in jsonb, path text[] , replacement jsonb,
            insert_after boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_insert'
;
create trigger trig_del_after_3 after delete on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
-- Create a set of objects that are part of the schema created by
-- this extension.
CREATE TABLE regress_pg_dump_schema.test_table (
	col1 int,
	col2 int check (col2 > 0)
)
;
DROP SUBSCRIPTION tap_sub_copy
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
DROP TABLE pg_temp_13.pg_temp_26675_2, pg_temp_13.pg_temp_26675
;
-- previous modulus 50 is factor of 150 but this modulus is not a factor of next modulus 200.
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 150, REMAINDER 3)
;
comment on constraint c1 on domain dcomptype is 'random commentary'
;
-- check that partition validation scan correctly detects violating rows
alter table p attach partition p1 for values from (1, 2) to (1, 10)
;
CREATE INDEX t2_btree ON s3.t2 USING BTREE (i)
;
CREATE FUNCTION btisbncmp(isbn, isbn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
drop rule rules_foorule on rules_foo
;
------------------------------------------------------------------------------------------
[CREATE_SCHEMA] CREATE SCHEMA split_stats_move
;
----------
[RENAME_TABLE] -- substring needing last byte of its slice_size
ALTER TABLE toast_3b_utf8 RENAME TO toast_4b_utf8
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE itest7d ADD COLUMN b int GENERATED ALWAYS AS IDENTITY
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
----------
[ALTER_TABLE] ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = off,
    toast.vacuum_index_cleanup = yes)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_aggregate
;
----------
[ALTER_LIBRARY] alter extension test_ext9 drop table sometable
;
----------
[ADMIN_TABLE] analyze atacc1("........pg.dropped.1........")
;
----------
[DROP_TABLE] DROP TABLE prt2_adv_p3
;
----------
[DROP_TABLE] drop table nulltest
;
----------
[DROP_PROG_OBJ] DROP OPERATOR <<< (int, int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl4_lk3" (
    "a" integer CONSTRAINT "a_nn" NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE INTERVAL_TBL_OF (f1 interval)
;
----------
[CREATE_SCHEMA(SELECT)] CREATE SCHEMA test_ns_schema_1
       CREATE UNIQUE INDEX abc_a_idx ON abc (a)
       CREATE VIEW abc_view AS
              SELECT a+1 AS a, b+1 AS b FROM abc
       CREATE TABLE abc (
              a serial,
              b int UNIQUE
       )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(issn13, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TRIGGER] DROP TRIGGER modified_any ON main_table
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschemax AUTHORIZATION nonexistent
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_wslot_bu before update
    on WSlot for each row execute procedure tg_Wslot_bu()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Test that remote triggers work with update tuple routing
create trigger loct_br_insert_trigger before insert on loct
	for each row execute procedure br_insert_trigfunc()
;
----------
[CREATE_TABLE] create table history ( customerid integer , timestamp timestamp without time zone , action_taken char ( 5 ) , narrative varchar ( 100 ) )
;
----------
[CREATE_VIEW(SELECT)] create materialized view parallel_mat_view as
    select length(stringu1) from tenk1 group by length(stringu1)
;
----------
[CREATE_PROG_OBJ] create operator <> (
  leftarg    = myint,
  rightarg   = myint,
  commutator = <>,
  negator    = =,
  procedure  = myintne,
  restrict   = eqsel,
  join       = eqjoinsel,
  merges
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE testpub_parted ATTACH PARTITION testpub_parted2 FOR VALUES IN (2)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_ts_config
;
----------
[ADMIN_TABLE] vacuum ��ג�������ђ��
;
----------
[CREATE_TABLE] CREATE TABLE stream_test(data text)
;
----------
[UNKNOWN] SECURITY LABEL ON COLUMN t1p.o IS 'system_u:object_r:sepgsql_table_t:s0'
;
----------
[ALTER_TABLE] ALTER TABLE regtest_table ENABLE  TRIGGER regtest_test_trig
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW static_view AS
  SELECT 'foo'::TEXT AS col
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_rep_full CONNECTION 'port=20465 host=/tmp/vlW5Cd5sJm dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_rep_full
;
----------
[ADD_INDEX] CREATE INDEX i2 ON s2.t2(id)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW column_options AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(c.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.attname AS sql_identifier) AS column_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_table_columns c
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."emp1_id_seq" OWNED BY "public"."emp1"."id"
;
----------
[DROP_TABLE] DROP TABLE oer_test
;
----------
[CREATE_TABLE] create table mlparted_defd partition of mlparted_def default
;
----------
[ADD_INDEX] CREATE INDEX grect2ind ON fast_emp4000 USING gist (home_base)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_inherits
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (ean13, isbn13),
	OPERATOR 1  = (ean13, ismn13),
	OPERATOR 1  = (ean13, issn13),
	OPERATOR 1  = (ean13, isbn),
	OPERATOR 1  = (ean13, ismn),
	OPERATOR 1  = (ean13, issn),
	OPERATOR 1  = (ean13, upc)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_insert(jsonb_in jsonb, path text[] , replacement jsonb,
            insert_after boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_insert'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_del_after_3 after delete on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[CREATE_TABLE] -- Create a set of objects that are part of the schema created by
-- this extension.
CREATE TABLE regress_pg_dump_schema.test_table (
	col1 int,
	col2 int check (col2 > 0)
)
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION tap_sub_copy
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26675_2, pg_temp_13.pg_temp_26675
;
----------
[CREATE_TABLE] -- previous modulus 50 is factor of 150 but this modulus is not a factor of next modulus 200.
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 150, REMAINDER 3)
;
----------
[COMMENT_CONSTRAINT] comment on constraint c1 on domain dcomptype is 'random commentary'
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- check that partition validation scan correctly detects violating rows
alter table p attach partition p1 for values from (1, 2) to (1, 10)
;
----------
[ADD_INDEX] CREATE INDEX t2_btree ON s3.t2 USING BTREE (i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btisbncmp(isbn, isbn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_POLICY] drop rule rules_foorule on rules_foo
;
