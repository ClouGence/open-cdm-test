ALTER TABLE public.beta_neg OWNER TO zyc
;
EXECUTE dumpTableAttach('38647')
;
EXECUTE dumpAgg('16758')
;
-- SET statements.
-- These use two different strings, still they count as one entry.
CREATE ROLE regress_stat_set_1
;
SAVEPOINT b
;
EXECUTE dumpTableAttach('41504')
;
ALTER USER MAPPING FOR current_user SERVER s8 OPTIONS (username 'test')
;
GRANT ALL PRIVILEGES ON FUNCTION testagg1(int2) TO PUBLIC
;
-- clean up

-- make sure that create WITH NO DATA works via SPI
BEGIN
;
RELEASE SAVEPOINT subtop
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE public.beta_neg OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpTableAttach('38647')
;
----------
[UNSAFE] EXECUTE dumpAgg('16758')
;
----------
[CREATE_ROLE] -- SET statements.
-- These use two different strings, still they count as one entry.
CREATE ROLE regress_stat_set_1
;
----------
[TRANSACTION] SAVEPOINT b
;
----------
[UNSAFE] EXECUTE dumpTableAttach('41504')
;
----------
[GRANT] ALTER USER MAPPING FOR current_user SERVER s8 OPTIONS (username 'test')
;
----------
[GRANT] GRANT ALL PRIVILEGES ON FUNCTION testagg1(int2) TO PUBLIC
;
----------
[TRANSACTION] -- clean up

-- make sure that create WITH NO DATA works via SPI
BEGIN
;
----------
[TRANSACTION] RELEASE SAVEPOINT subtop
;
