ALTER TABLE test_tab ADD COLUMN c INT
;
create view tt16v as select * from int8_tbl i, lateral(values(i)) ss
;
create temp table gstest2 (a integer, b integer, c integer, d integer,
                           e integer, f integer, g integer, h integer)
;
DROP COLLATION IF EXISTS split_collation_next.c_from_c_renamed RESTRICT
;
create table rparted_by_int2 (a smallint) partition by range (a)
;
CREATE TABLE viewtest_tbl (a int, b int)
;
create trigger tg_pfield_ad after delete
    on PField for each row execute procedure tg_pfield_ad()
;
CREATE DATABASE foobar2 ENCODING 'LATIN1' TEMPLATE template0 LC_COLLATE 'C' LC_CTYPE 'C'
;
create table atacc1 ( id serial, test int)
;
ALTER SEQUENCE IF EXISTS split_sequence_next.seq_renamed SET SCHEMA public
;
/*
 * 5.17
 * COLUMN_COLUMN_USAGE view
 */

CREATE VIEW column_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(n.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(ac.attname AS sql_identifier) AS column_name,
           CAST(ad.attname AS sql_identifier) AS dependent_column

    FROM pg_namespace n, pg_class c, pg_depend d,
         pg_attribute ac, pg_attribute ad

    WHERE n.oid = c.relnamespace
          AND c.oid = ac.attrelid
          AND c.oid = ad.attrelid
          AND d.classid = 'pg_catalog.pg_class'::regclass
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND d.objid = d.refobjid
          AND c.oid = d.objid
          AND d.objsubid = ad.attnum
          AND d.refobjsubid = ac.attnum
          AND ad.attgenerated <> ''
          AND pg_has_role(c.relowner, 'USAGE')
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_enum
;
CREATE TABLE trunc_fb (col2b int) INHERITS (trunc_f)
;
DROP TABLE pg_temp_6.pg_temp_33826_2, pg_temp_6.pg_temp_33826
;
CREATE PUBLICATION "dump_pub_all" WITH (publish = 'insert, update, delete, truncate', publish_via_partition_root = true)
;
ALTER TABLE public.test_inh_check_child ADD CONSTRAINT bmerged CHECK ((b > (1)::double precision))
;
create or replace function array_elem_check(int) returns int as $$
declare
  x orderedpair := '{1,2}';
begin
  x[2] := $1;
  return x[2];
end$$ language plpgsql
;
CREATE PUBLICATION pub_dropped_cols FOR TABLE dropped_cols, generated_cols
;
CREATE TEXT SEARCH CONFIGURATION greek
	(PARSER = default)
;
CREATE TABLE truncprim (a int PRIMARY KEY)
;
ALTER TABLE pg_toast.pg_toast_30870 RENAME TO reind_con_toast
;
CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=10962 host=/tmp/u67lV1GsQY dbname=postgres' PUBLICATION pub_viaroot
;
CREATE TABLE part_p2_p1 PARTITION OF part_p2 DEFAULT
;
ALTER INDEX dummy_test_idx SET (option_string_val = true)
;
CREATE TEMP TABLE arrtest_s (
  a       int2[],
  b       int2[][]
)
;
ALTER TABLE "public"."itest13" ALTER COLUMN "b" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."itest13_b_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
DROP TABLE TEMP_INT2
;
DROP TABLE brin_hot
;
ALTER TABLE parted_self_fk DETACH PARTITION part2_self_fk
;
drop table mclparted_null
;
create function longname(fullname) returns text language sql
as $$select $1.first || ' ' || $1.last$$
;
ALTER TABLE fk_partitioned_fk DETACH PARTITION fk_partitioned_fk_4
;
DROP TABLE xact_test
;
DROP TABLE cj_source2, cj_source1, cj_target
;
ALTER INDEX pg_toast.pg_toast_33336_index RENAME TO reind_con_toast_idx
;
TRUNCATE TABLE truncate_a,trunc_b
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_publication_rel
;
do $$ declare x foo.bar%type; begin end $$
;
DROP SEQUENCE test_seq1
;
-- check the attributes and constraints after partition is attached
CREATE TABLE part_1 (
	a int NOT NULL,
	b char(2) COLLATE "C",
	CONSTRAINT check_a CHECK (a > 0)
)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP COLUMN c9
;
CREATE FUNCTION my_erroneous_func(int) RETURNS int LANGUAGE SQL
AS $$ SELECT $1 + , 1 $$
;
--
-- Test SP-GiST indexes.
--
-- There are other tests to test different SP-GiST opclasses. This is for
-- testing SP-GiST code itself.

create table spgist_point_tbl(id int4, p point)
;
ANALYZE public.prt1_p2
;
CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (immut_func(a))
;
DROP TABLE pg_temp_26.pg_temp_28381_2, pg_temp_26.pg_temp_28381
;
ALTER OPERATOR ~ (lquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE TABLE public.junk AS SELECT 'junk'::TEXT AS junk_column
;
CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_tab ADD COLUMN c INT
;
----------
[CREATE_VIEW(SELECT)] create view tt16v as select * from int8_tbl i, lateral(values(i)) ss
;
----------
[CREATE_TABLE] create temp table gstest2 (a integer, b integer, c integer, d integer,
                           e integer, f integer, g integer, h integer)
;
----------
[DROP_POLICY] DROP COLLATION IF EXISTS split_collation_next.c_from_c_renamed RESTRICT
;
----------
[CREATE_TABLE] create table rparted_by_int2 (a smallint) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE viewtest_tbl (a int, b int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_pfield_ad after delete
    on PField for each row execute procedure tg_pfield_ad()
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar2 ENCODING 'LATIN1' TEMPLATE template0 LC_COLLATE 'C' LC_CTYPE 'C'
;
----------
[CREATE_TABLE] create table atacc1 ( id serial, test int)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE IF EXISTS split_sequence_next.seq_renamed SET SCHEMA public
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.17
 * COLUMN_COLUMN_USAGE view
 */

CREATE VIEW column_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(n.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(ac.attname AS sql_identifier) AS column_name,
           CAST(ad.attname AS sql_identifier) AS dependent_column

    FROM pg_namespace n, pg_class c, pg_depend d,
         pg_attribute ac, pg_attribute ad

    WHERE n.oid = c.relnamespace
          AND c.oid = ac.attrelid
          AND c.oid = ad.attrelid
          AND d.classid = 'pg_catalog.pg_class'::regclass
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND d.objid = d.refobjid
          AND c.oid = d.objid
          AND d.objsubid = ad.attnum
          AND d.refobjsubid = ac.attnum
          AND ad.attgenerated <> ''
          AND pg_has_role(c.relowner, 'USAGE')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_enum
;
----------
[CREATE_TABLE] CREATE TABLE trunc_fb (col2b int) INHERITS (trunc_f)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_6.pg_temp_33826_2, pg_temp_6.pg_temp_33826
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "dump_pub_all" WITH (publish = 'insert, update, delete, truncate', publish_via_partition_root = true)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.test_inh_check_child ADD CONSTRAINT bmerged CHECK ((b > (1)::double precision))
;
----------
[CREATE_PROG_OBJ] create or replace function array_elem_check(int) returns int as $$
declare
  x orderedpair := '{1,2}';
begin
  x[2] := $1;
  return x[2];
end$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_dropped_cols FOR TABLE dropped_cols, generated_cols
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION greek
	(PARSER = default)
;
----------
[CREATE_TABLE] CREATE TABLE truncprim (a int PRIMARY KEY)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30870 RENAME TO reind_con_toast
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=10962 host=/tmp/u67lV1GsQY dbname=postgres' PUBLICATION pub_viaroot
;
----------
[CREATE_TABLE] CREATE TABLE part_p2_p1 PARTITION OF part_p2 DEFAULT
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_string_val = true)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE arrtest_s (
  a       int2[],
  b       int2[][]
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest13" ALTER COLUMN "b" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."itest13_b_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
----------
[DROP_TABLE] DROP TABLE TEMP_INT2
;
----------
[DROP_TABLE] DROP TABLE brin_hot
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE parted_self_fk DETACH PARTITION part2_self_fk
;
----------
[DROP_TABLE] drop table mclparted_null
;
----------
[CREATE_PROG_OBJ] create function longname(fullname) returns text language sql
as $$select $1.first || ' ' || $1.last$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk DETACH PARTITION fk_partitioned_fk_4
;
----------
[DROP_TABLE] DROP TABLE xact_test
;
----------
[DROP_TABLE] DROP TABLE cj_source2, cj_source1, cj_target
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33336_index RENAME TO reind_con_toast_idx
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE truncate_a,trunc_b
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_publication_rel
;
----------
[BLOCK] do $$ declare x foo.bar%type; begin end $$
;
----------
[DROP_SEQUENCE] DROP SEQUENCE test_seq1
;
----------
[CREATE_TABLE] -- check the attributes and constraints after partition is attached
CREATE TABLE part_1 (
	a int NOT NULL,
	b char(2) COLLATE "C",
	CONSTRAINT check_a CHECK (a > 0)
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP COLUMN c9
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION my_erroneous_func(int) RETURNS int LANGUAGE SQL
AS $$ SELECT $1 + , 1 $$
;
----------
[CREATE_TABLE] --
-- Test SP-GiST indexes.
--
-- There are other tests to test different SP-GiST opclasses. This is for
-- testing SP-GiST code itself.

create table spgist_point_tbl(id int4, p point)
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_p2
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (immut_func(a))
;
----------
[DROP_TABLE] DROP TABLE pg_temp_26.pg_temp_28381_2, pg_temp_26.pg_temp_28381
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ~ (lquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE public.junk AS SELECT 'junk'::TEXT AS junk_column
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
