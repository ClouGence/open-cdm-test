DROP TABLE IF EXISTS split_lock_next.child
;
drop table derived
;
-- ok
ALTER TABLE constraint_rename_test RENAME CONSTRAINT con2foo TO con2bar
;
--
-- bt_index_parent_check()
--
CREATE FUNCTION bt_index_parent_check(index regclass)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
create or replace function raise_test() returns void as $$
begin
  raise notice 'some message' using message = 'custom' || ' message', errcode = '22012';
end;
$$ language plpgsql
;
CREATE FUNCTION echo_me(rainbow) RETURNS text AS $$
BEGIN
RETURN $1::text || 'wtf';
END
$$ LANGUAGE plpgsql
;
DROP TABLE pktab
;
CREATE OR REPLACE FUNCTION tgf() RETURNS trigger LANGUAGE plpgsql
  AS $$ begin raise exception 'except'; end $$
;
create table lp_ad partition of lp for values in ('a', 'd')
;
create table hs3 (col1 integer primary key)
;
create view rtest_vcomp as
	select X.part, (X.size * Y.factor) as size_in_cm
			from rtest_comp X, rtest_unitfact Y
			where X.unit = Y.unit
;
-- operator
DROP OPERATOR @#@ (int, int)
;
-- If a partition already has an index, don't create a duplicative one
create table idxpart (a int, b int) partition by range (a, b)
;
CREATE TABLE test22a_1 PARTITION OF test22a FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
ALTER TABLE pg_conversion ADD UNIQUE USING INDEX pg_conversion_default_index
;
CREATE FUNCTION logging_minvfunc_nonstrict(text, anyelement) RETURNS text AS
$$ SELECT $1 || '-' || quote_nullable($2) $$
LANGUAGE SQL IMMUTABLE
;
CREATE OPERATOR CLASS gist_hstore_ops
DEFAULT FOR TYPE hstore USING gist
AS
	OPERATOR        7       @> ,
	OPERATOR        9       ?(hstore,text) ,
	OPERATOR        10      ?|(hstore,text[]) ,
	OPERATOR        11      ?&(hstore,text[]) ,
        --OPERATOR        8       <@ ,
        OPERATOR        13      @ ,
        --OPERATOR        14      ~ ,
        FUNCTION        1       ghstore_consistent (internal, hstore, smallint, oid, internal),
        FUNCTION        2       ghstore_union (internal, internal),
        FUNCTION        3       ghstore_compress (internal),
        FUNCTION        4       ghstore_decompress (internal),
        FUNCTION        5       ghstore_penalty (internal, internal, internal),
        FUNCTION        6       ghstore_picksplit (internal, internal),
        FUNCTION        7       ghstore_same (ghstore, ghstore, internal),
        STORAGE         ghstore
;
CREATE FUNCTION isneq(isbn13, isbn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- Try (and fail) to add constraint due to invalid data
ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full
;
drop domain domaindroptest
;
ALTER TABLE pg_temp_3.pg_temp_27487_2 ADD COLUMN newdata pg_temp_3.pg_temp_27487
;
DROP INDEX clstrpart_only_idx
;
CREATE TABLE base_tbl_parent (a int)
;
DROP PUBLICATION IF EXISTS split_pub18_schema
;
CREATE OPERATOR ^@> (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_isparent,
        COMMUTATOR = '^<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE TABLE tab4 (a int PRIMARY KEY)
;
/*
 * 2. Test CREATE TABLE with constraint
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box,
				CONSTRAINT covering UNIQUE(c1,c2) INCLUDE(c3,c4))
;
-- fail

CREATE TABLE test_u AS SELECT a, b FROM collate_test1 UNION ALL SELECT a, b FROM collate_test2
;
ALTER TABLE ONLY "public"."part_7_a_null" DROP COLUMN "........pg.dropped.2........"
;
--
-- Check that operands wrapped in PlaceHolderVars are matched to partition
-- keys, allowing partition pruning to occur.  PlaceHolderVars can be
-- introduced when a subquery's output is used with grouping sets.
--
create table phv_part (a int, b text) partition by list (a)
;
/*
 * PostgreSQL System Views
 *
 * Copyright (c) 1996-2023, PostgreSQL Global Development Group
 *
 * src/backend/catalog/system_views.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
SECURITY LABEL ON TABLE t5 IS 'system_u:object_r:sepgsql_table_t:s0'
;
CREATE TABLE trunc_stats_test2(id serial)
;
CREATE VIEW rec1v AS SELECT * FROM rec1
;
-- check column addition within a view (bug #14876)
create table at_base_table(id int, stuff text)
;
-- now it works
alter table idxpart0 alter column a drop not null
;
CREATE UNIQUE INDEX parallel_hang_idx
					ON parallel_hang
					USING btree (i int4_custom_ops)
;
COMMENT ON TEXT SEARCH DICTIONARY finnish_stem IS 'snowball stemmer for finnish language'
;
CREATE TRIGGER transition_table_base_ins_trig
  AFTER INSERT ON transition_table_base
  REFERENCING NEW TABLE AS newtable
  FOR EACH STATEMENT
  EXECUTE PROCEDURE transition_table_base_ins_func()
;
CREATE OPERATOR !~ (
    PROCEDURE = texticregexne,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = ~,
    RESTRICT  = icregexnesel,
    JOIN      = icregexnejoinsel
)
;
create function testtr_trigger() returns trigger language plpgsql as
$$begin
  raise notice 'tg_op = %', tg_op;
  raise notice 'old(%) = %', old.a, row(old.*);
  raise notice 'new(%) = %', new.a, row(new.*);
  if (tg_op = 'DELETE') then
    return old;
  else
    return new;
  end if;
end$$
;
alter table idxpart1 drop column col1, drop column col2
;
DROP TABLE a1, a2, a3, a4, a5, a6, a7, a8
;
create function f1(x anycompatiblerange, y anycompatible, z anycompatible) returns anycompatiblearray as $$
begin
  return array[lower(x), upper(x), y, z];
end$$ language plpgsql
;
CREATE INDEX int4idx ON int4tmp USING gist ( a )
;
--
-- test FOREIGN KEY, multirange references multirange
--

-- test table setup
DROP TABLE temporal_mltrng
;
ALTER OPERATOR FAMILY gist_interval_ops USING gist ADD
	FUNCTION	11  (interval, interval) gbt_intv_sortsupport (internal)
;
COMMENT ON COLUMN ft1.c1 IS 'foreign column'
;
CREATE INDEX tenk2_unique1 ON tenk2 USING btree(unique1 int4_ops)
;
create table has_fast_default(f1 int)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE IF EXISTS split_lock_next.child
;
----------
[DROP_TABLE] drop table derived
;
----------
[RENAME_CONSTRAINT] -- ok
ALTER TABLE constraint_rename_test RENAME CONSTRAINT con2foo TO con2bar
;
----------
[CREATE_PROG_OBJ] --
-- bt_index_parent_check()
--
CREATE FUNCTION bt_index_parent_check(index regclass)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise notice 'some message' using message = 'custom' || ' message', errcode = '22012';
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION echo_me(rainbow) RETURNS text AS $$
BEGIN
RETURN $1::text || 'wtf';
END
$$ LANGUAGE plpgsql
;
----------
[DROP_TABLE] DROP TABLE pktab
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION tgf() RETURNS trigger LANGUAGE plpgsql
  AS $$ begin raise exception 'except'; end $$
;
----------
[CREATE_TABLE] create table lp_ad partition of lp for values in ('a', 'd')
;
----------
[CREATE_TABLE] create table hs3 (col1 integer primary key)
;
----------
[CREATE_VIEW(SELECT)] create view rtest_vcomp as
	select X.part, (X.size * Y.factor) as size_in_cm
			from rtest_comp X, rtest_unitfact Y
			where X.unit = Y.unit
;
----------
[DROP_PROG_OBJ] -- operator
DROP OPERATOR @#@ (int, int)
;
----------
[CREATE_TABLE] -- If a partition already has an index, don't create a duplicative one
create table idxpart (a int, b int) partition by range (a, b)
;
----------
[CREATE_TABLE] CREATE TABLE test22a_1 PARTITION OF test22a FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_conversion ADD UNIQUE USING INDEX pg_conversion_default_index
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION logging_minvfunc_nonstrict(text, anyelement) RETURNS text AS
$$ SELECT $1 || '-' || quote_nullable($2) $$
LANGUAGE SQL IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_hstore_ops
DEFAULT FOR TYPE hstore USING gist
AS
	OPERATOR        7       @> ,
	OPERATOR        9       ?(hstore,text) ,
	OPERATOR        10      ?|(hstore,text[]) ,
	OPERATOR        11      ?&(hstore,text[]) ,
        --OPERATOR        8       <@ ,
        OPERATOR        13      @ ,
        --OPERATOR        14      ~ ,
        FUNCTION        1       ghstore_consistent (internal, hstore, smallint, oid, internal),
        FUNCTION        2       ghstore_union (internal, internal),
        FUNCTION        3       ghstore_compress (internal),
        FUNCTION        4       ghstore_decompress (internal),
        FUNCTION        5       ghstore_penalty (internal, internal, internal),
        FUNCTION        6       ghstore_picksplit (internal, internal),
        FUNCTION        7       ghstore_same (ghstore, ghstore, internal),
        STORAGE         ghstore
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(isbn13, isbn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Try (and fail) to add constraint due to invalid data
ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full
;
----------
[DROP_TYPE] drop domain domaindroptest
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_27487_2 ADD COLUMN newdata pg_temp_3.pg_temp_27487
;
----------
[DROP_INDEX] DROP INDEX clstrpart_only_idx
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl_parent (a int)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION IF EXISTS split_pub18_schema
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^@> (
        LEFTARG = _ltree,
	RIGHTARG = ltree,
	PROCEDURE = _ltree_isparent,
        COMMUTATOR = '^<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE tab4 (a int PRIMARY KEY)
;
----------
[CREATE_TABLE] /*
 * 2. Test CREATE TABLE with constraint
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box,
				CONSTRAINT covering UNIQUE(c1,c2) INCLUDE(c3,c4))
;
----------
[CREATE_TABLE|SELECT] -- fail

CREATE TABLE test_u AS SELECT a, b FROM collate_test1 UNION ALL SELECT a, b FROM collate_test2
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."part_7_a_null" DROP COLUMN "........pg.dropped.2........"
;
----------
[CREATE_TABLE] --
-- Check that operands wrapped in PlaceHolderVars are matched to partition
-- keys, allowing partition pruning to occur.  PlaceHolderVars can be
-- introduced when a subquery's output is used with grouping sets.
--
create table phv_part (a int, b text) partition by list (a)
;
----------
[CREATE_VIEW(SELECT)] /*
 * PostgreSQL System Views
 *
 * Copyright (c) 1996-2023, PostgreSQL Global Development Group
 *
 * src/backend/catalog/system_views.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
----------
[UNKNOWN] SECURITY LABEL ON TABLE t5 IS 'system_u:object_r:sepgsql_table_t:s0'
;
----------
[CREATE_TABLE] CREATE TABLE trunc_stats_test2(id serial)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rec1v AS SELECT * FROM rec1
;
----------
[CREATE_TABLE] -- check column addition within a view (bug #14876)
create table at_base_table(id int, stuff text)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- now it works
alter table idxpart0 alter column a drop not null
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX parallel_hang_idx
					ON parallel_hang
					USING btree (i int4_custom_ops)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY finnish_stem IS 'snowball stemmer for finnish language'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER transition_table_base_ins_trig
  AFTER INSERT ON transition_table_base
  REFERENCING NEW TABLE AS newtable
  FOR EACH STATEMENT
  EXECUTE PROCEDURE transition_table_base_ins_func()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !~ (
    PROCEDURE = texticregexne,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = ~,
    RESTRICT  = icregexnesel,
    JOIN      = icregexnejoinsel
)
;
----------
[CREATE_PROG_OBJ] create function testtr_trigger() returns trigger language plpgsql as
$$begin
  raise notice 'tg_op = %', tg_op;
  raise notice 'old(%) = %', old.a, row(old.*);
  raise notice 'new(%) = %', new.a, row(new.*);
  if (tg_op = 'DELETE') then
    return old;
  else
    return new;
  end if;
end$$
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart1 drop column col1, drop column col2
;
----------
[DROP_TABLE] DROP TABLE a1, a2, a3, a4, a5, a6, a7, a8
;
----------
[CREATE_PROG_OBJ] create function f1(x anycompatiblerange, y anycompatible, z anycompatible) returns anycompatiblearray as $$
begin
  return array[lower(x), upper(x), y, z];
end$$ language plpgsql
;
----------
[ADD_INDEX] CREATE INDEX int4idx ON int4tmp USING gist ( a )
;
----------
[DROP_TABLE] --
-- test FOREIGN KEY, multirange references multirange
--

-- test table setup
DROP TABLE temporal_mltrng
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_interval_ops USING gist ADD
	FUNCTION	11  (interval, interval) gbt_intv_sortsupport (internal)
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN ft1.c1 IS 'foreign column'
;
----------
[ADD_INDEX] CREATE INDEX tenk2_unique1 ON tenk2 USING btree(unique1 int4_ops)
;
----------
[CREATE_TABLE] create table has_fast_default(f1 int)
;
