COPY public.room (roomno, comment) TO stdout
;
EXECUTE dumpRangeType('16472')
;
EXECUTE dumpOpr('1753')
;
ALTER TABLE public.bt_i4_heap OWNER TO zyc
;
COPY dump_test.fk_reference_test_table (col1) TO stdout
;
SET debug_parallel_query = on
;
ALTER TABLE public.spgist_text_tbl OWNER TO zyc
;
-- Connection is not closed at the end of the alter statement in
-- pgfdw_inval_callback. That's because the connection is in midst of this
-- xact, it is just marked as invalid.
ALTER SERVER loopback OPTIONS (ADD use_remote_estimate 'off')
;
SET LOCAL enable_material = off
;
ALTER TABLE public.sj_t4 OWNER TO zyc
;
------------------------------------------------------------------------------------------
[DATA_EXPORT] COPY public.room (roomno, comment) TO stdout
;
----------
[UNSAFE] EXECUTE dumpRangeType('16472')
;
----------
[UNSAFE] EXECUTE dumpOpr('1753')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.bt_i4_heap OWNER TO zyc
;
----------
[DATA_EXPORT] COPY dump_test.fk_reference_test_table (col1) TO stdout
;
----------
[SESSION_SETTING_WRITE] SET debug_parallel_query = on
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.spgist_text_tbl OWNER TO zyc
;
----------
[SYSTEM_SETTING_WRITE] -- Connection is not closed at the end of the alter statement in
-- pgfdw_inval_callback. That's because the connection is in midst of this
-- xact, it is just marked as invalid.
ALTER SERVER loopback OPTIONS (ADD use_remote_estimate 'off')
;
----------
[SESSION_SETTING_WRITE] SET LOCAL enable_material = off
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.sj_t4 OWNER TO zyc
;
