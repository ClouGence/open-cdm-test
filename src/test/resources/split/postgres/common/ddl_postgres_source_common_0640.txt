alter domain dcomptype add constraint c1 check ((value).r <= (value).i)
;
create table inh_multiparent() inherits(inh_p1, inh_p2, inh_p3, inh_p4)
;
create table float8multirange_test(f8mr float8multirange, i int)
;
ANALYZE pg_temp_11.pg_temp_26571
;
create function lock_excl(int,int) returns int language sql as
  'select 1 - lock_excl($1)' parallel safe
;
-- ok
drop table perm_part_parent cascade
;
CREATE FUNCTION getrngfunc7(int) RETURNS setof record AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
CREATE TABLE psql_pipeline(a INTEGER PRIMARY KEY, s TEXT)
;
ALTER TABLE test_type_diff2 ALTER COLUMN int_four TYPE int8 USING int_four::int8
;
DROP TRIGGER trig_row_after_insert ON rem1
;
create domain jb_ordered_pair as jb_unordered_pair check((value).x <= (value).y)
;
ALTER TABLE tab5_1 REPLICA IDENTITY USING INDEX tab5_1_a_idx
;
CREATE FUNCTION gbt_int4_distance(internal,int4,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE CAST (ean13 AS upc) WITH FUNCTION upc(ean13)
;
create aggregate least_agg(int8) (
  stype = int8, sfunc = least_accum
)
;
ALTER OPERATOR FAMILY gist_enum_ops USING gist ADD
	FUNCTION	11  (anyenum, anyenum) gbt_enum_sortsupport (internal)
;
create domain mytext_child_1 as mytext
;
CREATE FUNCTION gbt_int8_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER FUNCTION brin_page_items(bytea, regclass) PARALLEL RESTRICTED
;
ALTER TABLE pg_temp_11.pg_temp_41261_2 ADD COLUMN newdata pg_temp_11.pg_temp_41261
;
DROP SUBSCRIPTION tap_sub_renamed
;
-- GROUP BY optimization by reordering GROUP BY clauses
CREATE TABLE btg AS SELECT
  i % 10 AS x,
  i % 10 AS y,
  'abc' || i % 10 AS z,
  i AS w
FROM generate_series(1, 100) AS i
;
-- try adding a primary key on oid (should succeed)
alter table atacc1 add constraint atacc_oid1 primary key(id)
;
drop function f_field_select(t onek)
;
create temp table parts (
    partnum     text,
    cost        float8
)
;
alter table test_storage alter a set storage extended
;
-- INSERT to this table will queue a GRANT at end of transaction
CREATE TABLE sro_trojan_table ()
;
CREATE OPERATOR >= (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = <,
    COMMUTATOR = <=,
    PROCEDURE  = citext_ge,
    RESTRICT   = scalargtsel,
    JOIN       = scalargtjoinsel
)
;
CREATE TABLE sub_part1(b int, c int8, a numeric)
;
CREATE TABLE "public"."num_exp_log10" (
    "id" integer,
    "expected" numeric(210,10)
)
;
CREATE OPERATOR ~* (
    PROCEDURE = texticregexeq,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = !~*,
    RESTRICT  = icregexeqsel,
    JOIN      = icregexeqjoinsel
)
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp() < $.datetime("HH:MI"))'))
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_database
;
CREATE TABLE "public"."tbl_include_pk" (
    "c1" integer NOT NULL,
    "c2" integer NOT NULL,
    "c3" integer,
    "c4" "box"
)
;
alter table tt3 add column c int
;
create view v_pagg_test AS
select
	y,
	min(t) AS tmin,max(t) AS tmax,count(distinct t) AS tndistinct,
	min(b) AS bmin,max(b) AS bmax,count(distinct b) AS bndistinct,
	min(a) AS amin,max(a) AS amax,count(distinct a) AS andistinct,
	min(aa) AS aamin,max(aa) AS aamax,count(distinct aa) AS aandistinct
from (
	select
		y,
		unnest(regexp_split_to_array(a1.t, ','))::int AS t,
		unnest(regexp_split_to_array(a1.b::text, ',')) AS b,
		unnest(a1.a) AS a,
		unnest(a1.aa) AS aa
	from (
		select
			y,
			string_agg(x::text, ',') AS t,
			string_agg(x::text::bytea, ',') AS b,
			array_agg(x) AS a,
			array_agg(ARRAY[x]) AS aa
		from pagg_test
		group by y
	) a1
) a2
group by y
;
DROP INDEX brin_test_parallel_idx
;
CREATE FUNCTION isngt(issn, issn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- picks nonconflicting name
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key UNIQUE(c)
;
CREATE OPERATOR =#> (PROCEDURE = testpub_rf_func1, LEFTARG = integer, RIGHTARG = integer)
;
-- Check usage of typmod with a user-defined type
-- (we have borrowed numeric's typmod functions)

CREATE TEMP TABLE mytab (foo widget(42,13,7))
;
/* contrib/pg_prewarm/pg_prewarm--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION autoprewarm_start_worker()
RETURNS VOID STRICT
AS '$libdir/pg_prewarm', 'autoprewarm_start_worker'
LANGUAGE C
;
ALTER TYPE "public"."rainbow" ADD VALUE 'blue'
;
CREATE INDEX atref_idx ON public.atref USING btree (((b).a))
;
CREATE EXTENSION test_parser
;
CREATE AGGREGATE "public"."first_el_agg_f8"(double precision) (
    SFUNC = "array_append",
    STYPE = double precision[],
    FINALFUNC = "public"."first_el"
)
;
CREATE DATABASE regression_invalid_interrupt
;
CREATE FUNCTION gbt_bytea_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE VIEW attmp_view (unique1) AS SELECT unique1 FROM tenk1
;
create temp table json_tab (a int)
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] alter domain dcomptype add constraint c1 check ((value).r <= (value).i)
;
----------
[CREATE_TABLE] create table inh_multiparent() inherits(inh_p1, inh_p2, inh_p3, inh_p4)
;
----------
[CREATE_TABLE] create table float8multirange_test(f8mr float8multirange, i int)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26571
;
----------
[CREATE_PROG_OBJ] create function lock_excl(int,int) returns int language sql as
  'select 1 - lock_excl($1)' parallel safe
;
----------
[DROP_TABLE] -- ok
drop table perm_part_parent cascade
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION getrngfunc7(int) RETURNS setof record AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE psql_pipeline(a INTEGER PRIMARY KEY, s TEXT)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_type_diff2 ALTER COLUMN int_four TYPE int8 USING int_four::int8
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_after_insert ON rem1
;
----------
[CREATE_TYPE] create domain jb_ordered_pair as jb_unordered_pair check((value).x <= (value).y)
;
----------
[ALTER_TABLE] ALTER TABLE tab5_1 REPLICA IDENTITY USING INDEX tab5_1_a_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int4_distance(internal,int4,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ean13 AS upc) WITH FUNCTION upc(ean13)
;
----------
[CREATE_PROG_OBJ] create aggregate least_agg(int8) (
  stype = int8, sfunc = least_accum
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_enum_ops USING gist ADD
	FUNCTION	11  (anyenum, anyenum) gbt_enum_sortsupport (internal)
;
----------
[CREATE_TYPE] create domain mytext_child_1 as mytext
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int8_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION brin_page_items(bytea, regclass) PARALLEL RESTRICTED
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_41261_2 ADD COLUMN newdata pg_temp_11.pg_temp_41261
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION tap_sub_renamed
;
----------
[CREATE_TABLE|SELECT] -- GROUP BY optimization by reordering GROUP BY clauses
CREATE TABLE btg AS SELECT
  i % 10 AS x,
  i % 10 AS y,
  'abc' || i % 10 AS z,
  i AS w
FROM generate_series(1, 100) AS i
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- try adding a primary key on oid (should succeed)
alter table atacc1 add constraint atacc_oid1 primary key(id)
;
----------
[DROP_PROG_OBJ] drop function f_field_select(t onek)
;
----------
[CREATE_TABLE] create temp table parts (
    partnum     text,
    cost        float8
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table test_storage alter a set storage extended
;
----------
[CREATE_TABLE] -- INSERT to this table will queue a GRANT at end of transaction
CREATE TABLE sro_trojan_table ()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = <,
    COMMUTATOR = <=,
    PROCEDURE  = citext_ge,
    RESTRICT   = scalargtsel,
    JOIN       = scalargtjoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE sub_part1(b int, c int8, a numeric)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_exp_log10" (
    "id" integer,
    "expected" numeric(210,10)
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~* (
    PROCEDURE = texticregexeq,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = !~*,
    RESTRICT  = icregexeqsel,
    JOIN      = icregexeqjoinsel
)
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp() < $.datetime("HH:MI"))'))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_database
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tbl_include_pk" (
    "c1" integer NOT NULL,
    "c2" integer NOT NULL,
    "c3" integer,
    "c4" "box"
)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tt3 add column c int
;
----------
[CREATE_VIEW(SELECT)] create view v_pagg_test AS
select
	y,
	min(t) AS tmin,max(t) AS tmax,count(distinct t) AS tndistinct,
	min(b) AS bmin,max(b) AS bmax,count(distinct b) AS bndistinct,
	min(a) AS amin,max(a) AS amax,count(distinct a) AS andistinct,
	min(aa) AS aamin,max(aa) AS aamax,count(distinct aa) AS aandistinct
from (
	select
		y,
		unnest(regexp_split_to_array(a1.t, ','))::int AS t,
		unnest(regexp_split_to_array(a1.b::text, ',')) AS b,
		unnest(a1.a) AS a,
		unnest(a1.aa) AS aa
	from (
		select
			y,
			string_agg(x::text, ',') AS t,
			string_agg(x::text::bytea, ',') AS b,
			array_agg(x) AS a,
			array_agg(ARRAY[x]) AS aa
		from pagg_test
		group by y
	) a1
) a2
group by y
;
----------
[DROP_INDEX] DROP INDEX brin_test_parallel_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(issn, issn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- picks nonconflicting name
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key UNIQUE(c)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR =#> (PROCEDURE = testpub_rf_func1, LEFTARG = integer, RIGHTARG = integer)
;
----------
[CREATE_TABLE] -- Check usage of typmod with a user-defined type
-- (we have borrowed numeric's typmod functions)

CREATE TEMP TABLE mytab (foo widget(42,13,7))
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_prewarm/pg_prewarm--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION autoprewarm_start_worker()
RETURNS VOID STRICT
AS '$libdir/pg_prewarm', 'autoprewarm_start_worker'
LANGUAGE C
;
----------
[ALTER_TYPE] ALTER TYPE "public"."rainbow" ADD VALUE 'blue'
;
----------
[ADD_INDEX] CREATE INDEX atref_idx ON public.atref USING btree (((b).a))
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_parser
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."first_el_agg_f8"(double precision) (
    SFUNC = "array_append",
    STYPE = double precision[],
    FINALFUNC = "public"."first_el"
)
;
----------
[CREATE_CATALOG] CREATE DATABASE regression_invalid_interrupt
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bytea_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW attmp_view (unique1) AS SELECT unique1 FROM tenk1
;
----------
[CREATE_TABLE] create temp table json_tab (a int)
;
