create table part_abc_3_2 partition of part_abc_3 for values from (1) to (100)
;
create or replace function raise_test() returns void as $$
begin
  raise sqlstate '1234F';
end;
$$ language plpgsql
;
create unique index Hub_name on Hub using btree (name bpchar_ops)
;
ALTER PUBLICATION testpub_default SET TABLE testpub_tbl1
;
CREATE CAST (text AS casttesttype) WITHOUT FUNCTION AS IMPLICIT
;
ALTER FUNCTION gbt_numeric_union(internal, internal) PARALLEL SAFE
;
ALTER FUNCTION test_func_dep1.dep_req1() SET SCHEMA test_func_dep2
;
CREATE MATERIALIZED VIEW "public"."mvtest_tvmm" AS
 SELECT "sum"("mvtest_tvm"."totamt") AS "grandtot"
   FROM "mvtest_mvschema"."mvtest_tvm"
  WITH NO DATA
;
-- Functions, which implement distance operators (<->)
ALTER FUNCTION cash_dist(money, money) PARALLEL SAFE
;
CREATE TABLE fail_part (
	b char(3),
	a int NOT NULL
)
;
alter table abc drop constraint abc_pk
;
alter table a alter column aa type integer using bit_length(aa)
;
-- test: return a row expr as record.
create or replace function composrec() returns record as $$
declare
  v record;
begin
  v := (1, 'hello');
  return v;
end;
$$ language plpgsql
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_foreign_table
;
CREATE FUNCTION gbt_oid_distance(internal,oid,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER SUBSCRIPTION tap_sub SET(streaming = parallel)
;
CREATE INDEX idx_float8 ON test_float8 USING gin (i)
;
DO $$BEGIN
	EXECUTE format(
		'ALTER DATABASE %I RESET session_authorization', current_catalog);
	EXECUTE format(
		'ALTER DATABASE %I SET role = regress_current_user', current_catalog);
END$$
;
alter table temp_part_parent attach partition perm_part_child default
;
CREATE FUNCTION gbt_intv_union(internal, internal)
RETURNS gbtreekey32
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE check_fk_presence_2 (id int REFERENCES check_fk_presence_1, t text)
;
create index pti1 on pt ((mydouble(category) + 1))
;
-- not ok
CREATE TABLE otherchild (tomorrow date default now())
  INHERITS (firstparent, thirdparent)
;
CREATE FOREIGN TABLE foreign_tbl (a int, b int)
  SERVER loopback OPTIONS (table_name 'child_tbl')
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=18037 host=/tmp/U3Vg1MdoVx dbname=postgres' PUBLICATION pub1
;
DROP TABLE circles
;
ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full NOT VALID
;
CREATE DOMAIN dint4_0 AS int CHECK (VALUE <> 0 )
;
alter table fkpart2.fk_part_1 attach partition fkpart2.fk_part_1_1 for values in (1)
;
ALTER TABLE test ALTER b SET STORAGE external
;
DROP FOREIGN TABLE reindex_foreign
;
CREATE TABLE tab_rowfilter_viaroot_part (a int)
;
ANALYZE public.copy_tbl
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_parameter_acl
;
CREATE FUNCTION gbt_ts_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
COMMENT ON LARGE OBJECT 34924 IS 'I Wandered Lonely as a Cloud'
;
CREATE FUNCTION earth_box(earth, float8)
RETURNS cube
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
AS 'SELECT cube_enlarge($1, gc_to_sec($2), 3)'
;
CREATE TABLE ref(f1 int, f2 int, f3 int)
;
create table rp (a int) partition by range (a)
;
CREATE TEMP TABLE pgss_extend_temp_tab (a int, b text)
;
ALTER FUNCTION gbt_int4_same(gbtreekey8, gbtreekey8, internal) PARALLEL SAFE
;
ALTER TEXT SEARCH CONFIGURATION irish ADD MAPPING
    FOR word, hword_part, hword
	WITH irish_stem
;
CREATE TABLE tab_2 (a int)
;
do $$
declare r1 two_int8s;
begin
  raise notice 'r1.q1 = %', r1.q1;
  raise notice 'r1.q2 = %', r1.q2;
  raise notice 'r1 = %', r1;
  raise notice 'r1.nosuchfield = %', r1.nosuchfield;
end$$
;
CREATE TABLE gtest_err_2a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (b * 2) STORED)
;
CREATE TABLE notnull_tbl5 (a int CONSTRAINT ann NOT NULL,
	b int CONSTRAINT bnn NOT NULL)
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "version" WITH "simple"
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_index
;
CREATE TABLE regress_pg_dump_table (
	col1 serial,
	col2 int check (col2 > 0)
)
;
-- fail, not identity


-- prohibited direct modification of sequence

ALTER SEQUENCE itest6_a_seq OWNED BY NONE
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table part_abc_3_2 partition of part_abc_3 for values from (1) to (100)
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise sqlstate '1234F';
end;
$$ language plpgsql
;
----------
[ADD_INDEX] create unique index Hub_name on Hub using btree (name bpchar_ops)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default SET TABLE testpub_tbl1
;
----------
[CREATE_PROG_OBJ] CREATE CAST (text AS casttesttype) WITHOUT FUNCTION AS IMPLICIT
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_numeric_union(internal, internal) PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION test_func_dep1.dep_req1() SET SCHEMA test_func_dep2
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_tvmm" AS
 SELECT "sum"("mvtest_tvm"."totamt") AS "grandtot"
   FROM "mvtest_mvschema"."mvtest_tvm"
  WITH NO DATA
;
----------
[ALTER_PROG_OBJ] -- Functions, which implement distance operators (<->)
ALTER FUNCTION cash_dist(money, money) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE fail_part (
	b char(3),
	a int NOT NULL
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table abc drop constraint abc_pk
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table a alter column aa type integer using bit_length(aa)
;
----------
[CREATE_PROG_OBJ] -- test: return a row expr as record.
create or replace function composrec() returns record as $$
declare
  v record;
begin
  v := (1, 'hello');
  return v;
end;
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_foreign_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_oid_distance(internal,oid,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub SET(streaming = parallel)
;
----------
[ADD_INDEX] CREATE INDEX idx_float8 ON test_float8 USING gin (i)
;
----------
[BLOCK] DO $$BEGIN
	EXECUTE format(
		'ALTER DATABASE %I RESET session_authorization', current_catalog);
	EXECUTE format(
		'ALTER DATABASE %I SET role = regress_current_user', current_catalog);
END$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table temp_part_parent attach partition perm_part_child default
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_intv_union(internal, internal)
RETURNS gbtreekey32
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE check_fk_presence_2 (id int REFERENCES check_fk_presence_1, t text)
;
----------
[ADD_INDEX] create index pti1 on pt ((mydouble(category) + 1))
;
----------
[CREATE_TABLE] -- not ok
CREATE TABLE otherchild (tomorrow date default now())
  INHERITS (firstparent, thirdparent)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE foreign_tbl (a int, b int)
  SERVER loopback OPTIONS (table_name 'child_tbl')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=18037 host=/tmp/U3Vg1MdoVx dbname=postgres' PUBLICATION pub1
;
----------
[DROP_TABLE] DROP TABLE circles
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full NOT VALID
;
----------
[CREATE_TYPE] CREATE DOMAIN dint4_0 AS int CHECK (VALUE <> 0 )
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table fkpart2.fk_part_1 attach partition fkpart2.fk_part_1_1 for values in (1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test ALTER b SET STORAGE external
;
----------
[DROP_TABLE] DROP FOREIGN TABLE reindex_foreign
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_viaroot_part (a int)
;
----------
[ADMIN_TABLE] ANALYZE public.copy_tbl
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_parameter_acl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_ts_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 34924 IS 'I Wandered Lonely as a Cloud'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION earth_box(earth, float8)
RETURNS cube
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
AS 'SELECT cube_enlarge($1, gc_to_sec($2), 3)'
;
----------
[CREATE_TABLE] CREATE TABLE ref(f1 int, f2 int, f3 int)
;
----------
[CREATE_TABLE] create table rp (a int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pgss_extend_temp_tab (a int, b text)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int4_same(gbtreekey8, gbtreekey8, internal) PARALLEL SAFE
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION irish ADD MAPPING
    FOR word, hword_part, hword
	WITH irish_stem
;
----------
[CREATE_TABLE] CREATE TABLE tab_2 (a int)
;
----------
[BLOCK] do $$
declare r1 two_int8s;
begin
  raise notice 'r1.q1 = %', r1.q1;
  raise notice 'r1.q2 = %', r1.q2;
  raise notice 'r1 = %', r1;
  raise notice 'r1.nosuchfield = %', r1.nosuchfield;
end$$
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_2a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (b * 2) STORED)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl5 (a int CONSTRAINT ann NOT NULL,
	b int CONSTRAINT bnn NOT NULL)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "version" WITH "simple"
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_index
;
----------
[CREATE_TABLE] CREATE TABLE regress_pg_dump_table (
	col1 serial,
	col2 int check (col2 > 0)
)
;
----------
[ALTER_SEQUENCE] -- fail, not identity


-- prohibited direct modification of sequence

ALTER SEQUENCE itest6_a_seq OWNED BY NONE
;
