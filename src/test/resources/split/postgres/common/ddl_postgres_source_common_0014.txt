VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) information_schema.sql_sizing
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
ALTER TABLE ONLY "public"."pagg_tab_m" ATTACH PARTITION "public"."pagg_tab_m_p1" FOR VALUES FROM (0, 0) TO (10, 10)
;
do $$
declare x int := x + 1;  -- error
begin
  raise notice 'x = %', x;
end;
$$
;
CREATE DATABASE db1 WITH TABLESPACE ts1
;
COMMENT ON OPERATOR ###### (NONE, int4) IS 'bad prefix'
;
drop event trigger regress_event_trigger3
;
ALTER OPERATOR FAMILY alt_opf3 USING hash RENAME TO alt_opf4
;
CREATE FUNCTION functest_B_2(bigint) RETURNS bool LANGUAGE 'sql'
       IMMUTABLE AS 'SELECT $1 > 0'
;
DROP TABLE dependee
;
DROP TRIGGER rw_view1_del_trig ON rw_view1
;
DROP EXTENSION isn
;
CREATE TABLE tab_phys_slot (a int)
;
DROP LANGUAGE test_language_exists
;
drop function anyctest(anycompatible, anycompatible)
;
CREATE TEMP TABLE vac_truncate_test(i INT NOT NULL, j text)
	WITH (vacuum_truncate=true, autovacuum_enabled=false)
;
-- for comparison purposes
create table parted_trigger (a int, b text) partition by range (a)
;
CREATE SUBSCRIPTION regress_testsub4 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false, origin = foo)
;
-- Test for renaming in simple multiple inheritance
CREATE TABLE inht1 (a int, b int)
;
DROP FUNCTION reindex_end_command_snap()
;
create trigger child1_delete_trig
  after delete on child1 referencing old table as old_table
  for each statement execute procedure dump_delete()
;
CREATE TABLE "public"."varchar_tbl" (
    "f1" character varying(4)
)
;
ALTER TABLE pg_toast.pg_toast_30638 RENAME TO reind_con_toast
;
TRUNCATE TABLE ONLY public.ths_p2
;
-- Related to the above scenario: ADD PRIMARY KEY on the parent mustn't
-- automatically propagate NOT NULL to child columns.
create table idxpart (a int) partition by range (a)
;
-- try a view
CREATE VIEW v AS SELECT * FROM sq_source WHERE sid < 2
;
CREATE TABLE otherchild () INHERITS (firstparent, thirdparent)
;
CREATE TABLE part2 (balance float, tid integer, val text)
  WITH (autovacuum_enabled=off)
;
drop table gtest33
;
-- Test renumbering of Vars when combining LIKE with inheritance
CREATE TABLE test_like_5 (x point, y point, z point)
;
-- CREATE TABLE ..  PARTITION BY doesn't not support USING
CREATE TABLE tableam_parted_heap2 (a text, b int) PARTITION BY list (a) USING heap2
;
-- has priv but no user mapping

-- Install file_fdw
CREATE EXTENSION file_fdw
;
create unique index parted_replica_idx_11 on parted_replica_tab_11 USING btree (id)
;
create schema abc authorization aac
;
-- Temporary tables with concurrent builds and on-commit actions
-- CONCURRENTLY used with CREATE INDEX and DROP INDEX is ignored.
-- PRESERVE ROWS, the default.
CREATE TEMP TABLE concur_temp (f1 int, f2 text)
  ON COMMIT PRESERVE ROWS
;
CREATE TABLE split_trigger_probe.account_audit (
    id integer,
    action text,
    old_balance integer,
    new_balance integer
)
;
CREATE FUNCTION "public"."perform_simple_func"(integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
BEGIN
	IF $1 < 20 THEN
		INSERT INTO perform_test VALUES ($1, $1 + 10);
		RETURN TRUE;
	ELSE
		RETURN FALSE;
	END IF;
END;$_$
;
CREATE OR REPLACE FUNCTION
 random_normal(mean float8 DEFAULT 0, stddev float8 DEFAULT 1)
 RETURNS float8
 LANGUAGE internal
 VOLATILE PARALLEL RESTRICTED STRICT COST 1
AS 'drandom_normal'
;
CREATE TABLE base_tbl (a int)
;
create table foo2child (f3 int) inherits (foo2)
;
DROP AGGREGATE IF EXISTS test_aggregate_exists(*)
;
ALTER OPERATOR ^@ (_ltree, ltxtquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE OPERATOR CLASS issn_ops DEFAULT
	FOR TYPE issn USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btissncmp(issn, issn)
;
CREATE INDEX uuididx ON uuidtmp USING gist ( a )
;
comment on table catalog1.schema1.table_name is 'ss'
;
CREATE TABLE ref(f1 int, f2 int, f3 int)
  PARTITION BY list(f1)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_db_role_setting
;
ALTER TABLE pg_namespace ADD UNIQUE USING INDEX pg_namespace_oid_index
;
alter extension postgres_fdw drop operator public.>^(int, int)
;
CREATE TABLE tbspace_reindex_part_0_1 PARTITION OF tbspace_reindex_part_0
  FOR VALUES IN (1)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) information_schema.sql_sizing
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_m" ATTACH PARTITION "public"."pagg_tab_m_p1" FOR VALUES FROM (0, 0) TO (10, 10)
;
----------
[BLOCK] do $$
declare x int := x + 1;  -- error
begin
  raise notice 'x = %', x;
end;
$$
;
----------
[CREATE_CATALOG] CREATE DATABASE db1 WITH TABLESPACE ts1
;
----------
[COMMENT_PROG_OBJ] COMMENT ON OPERATOR ###### (NONE, int4) IS 'bad prefix'
;
----------
[DROP_TRIGGER] drop event trigger regress_event_trigger3
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf3 USING hash RENAME TO alt_opf4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_B_2(bigint) RETURNS bool LANGUAGE 'sql'
       IMMUTABLE AS 'SELECT $1 > 0'
;
----------
[DROP_TABLE] DROP TABLE dependee
;
----------
[DROP_TRIGGER] DROP TRIGGER rw_view1_del_trig ON rw_view1
;
----------
[DROP_LIBRARY] DROP EXTENSION isn
;
----------
[CREATE_TABLE] CREATE TABLE tab_phys_slot (a int)
;
----------
[UNKNOWN] DROP LANGUAGE test_language_exists
;
----------
[DROP_PROG_OBJ] drop function anyctest(anycompatible, anycompatible)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE vac_truncate_test(i INT NOT NULL, j text)
	WITH (vacuum_truncate=true, autovacuum_enabled=false)
;
----------
[CREATE_TABLE] -- for comparison purposes
create table parted_trigger (a int, b text) partition by range (a)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub4 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false, origin = foo)
;
----------
[CREATE_TABLE] -- Test for renaming in simple multiple inheritance
CREATE TABLE inht1 (a int, b int)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION reindex_end_command_snap()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child1_delete_trig
  after delete on child1 referencing old table as old_table
  for each statement execute procedure dump_delete()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."varchar_tbl" (
    "f1" character varying(4)
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30638 RENAME TO reind_con_toast
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE ONLY public.ths_p2
;
----------
[CREATE_TABLE] -- Related to the above scenario: ADD PRIMARY KEY on the parent mustn't
-- automatically propagate NOT NULL to child columns.
create table idxpart (a int) partition by range (a)
;
----------
[CREATE_VIEW(SELECT)] -- try a view
CREATE VIEW v AS SELECT * FROM sq_source WHERE sid < 2
;
----------
[CREATE_TABLE] CREATE TABLE otherchild () INHERITS (firstparent, thirdparent)
;
----------
[CREATE_TABLE] CREATE TABLE part2 (balance float, tid integer, val text)
  WITH (autovacuum_enabled=off)
;
----------
[DROP_TABLE] drop table gtest33
;
----------
[CREATE_TABLE] -- Test renumbering of Vars when combining LIKE with inheritance
CREATE TABLE test_like_5 (x point, y point, z point)
;
----------
[CREATE_TABLE] -- CREATE TABLE ..  PARTITION BY doesn't not support USING
CREATE TABLE tableam_parted_heap2 (a text, b int) PARTITION BY list (a) USING heap2
;
----------
[CREATE_LIBRARY] -- has priv but no user mapping

-- Install file_fdw
CREATE EXTENSION file_fdw
;
----------
[ADD_INDEX] create unique index parted_replica_idx_11 on parted_replica_tab_11 USING btree (id)
;
----------
[CREATE_SCHEMA] create schema abc authorization aac
;
----------
[CREATE_TABLE] -- Temporary tables with concurrent builds and on-commit actions
-- CONCURRENTLY used with CREATE INDEX and DROP INDEX is ignored.
-- PRESERVE ROWS, the default.
CREATE TEMP TABLE concur_temp (f1 int, f2 text)
  ON COMMIT PRESERVE ROWS
;
----------
[CREATE_TABLE] CREATE TABLE split_trigger_probe.account_audit (
    id integer,
    action text,
    old_balance integer,
    new_balance integer
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."perform_simple_func"(integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
BEGIN
	IF $1 < 20 THEN
		INSERT INTO perform_test VALUES ($1, $1 + 10);
		RETURN TRUE;
	ELSE
		RETURN FALSE;
	END IF;
END;$_$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
 random_normal(mean float8 DEFAULT 0, stddev float8 DEFAULT 1)
 RETURNS float8
 LANGUAGE internal
 VOLATILE PARALLEL RESTRICTED STRICT COST 1
AS 'drandom_normal'
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl (a int)
;
----------
[CREATE_TABLE] create table foo2child (f3 int) inherits (foo2)
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE IF EXISTS test_aggregate_exists(*)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^@ (_ltree, ltxtquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS issn_ops DEFAULT
	FOR TYPE issn USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btissncmp(issn, issn)
;
----------
[ADD_INDEX] CREATE INDEX uuididx ON uuidtmp USING gist ( a )
;
----------
[COMMENT_TABLE] comment on table catalog1.schema1.table_name is 'ss'
;
----------
[CREATE_TABLE] CREATE TABLE ref(f1 int, f2 int, f3 int)
  PARTITION BY list(f1)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_db_role_setting
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_namespace ADD UNIQUE USING INDEX pg_namespace_oid_index
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw drop operator public.>^(int, int)
;
----------
[CREATE_TABLE] CREATE TABLE tbspace_reindex_part_0_1 PARTITION OF tbspace_reindex_part_0
  FOR VALUES IN (1)
;
