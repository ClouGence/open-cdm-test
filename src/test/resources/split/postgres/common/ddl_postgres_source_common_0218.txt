-- quiet

-- reject dupe
create index idxpart1_2_a_b on idxpart1 (a, b)
;
ALTER TABLE test_tab ADD COLUMN e INT
;
VACUUM (ANALYZE) pg_catalog.pg_language
;
do $$ declare a int[];
begin a := f1 from onecol limit 1; raise notice 'a = %', a; end$$
;
DROP VIEW rec1v, rec2v CASCADE
;
ALTER TABLE pg_toast.pg_toast_30939 RENAME TO reind_con_toast
;
CREATE TEMP TABLE t (a integer, b integer) PARTITION BY HASH (a, b)
;
DROP TABLE gtest4
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_rewrite
;
CREATE TABLE vaccluster (i INT PRIMARY KEY)
;
create table inserttestb (f1 insert_test_domain)
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "sfloat" WITH "simple"
;
CREATE OR REPLACE RULE y_rule AS ON INSERT TO y DO INSTEAD NOTHING
;
VACUUM (ANALYZE) pg_catalog.pg_foreign_server
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_foreign_data_wrapper
;
-- 1 2 3 4

create table test3 (c int)
;
CREATE TEMP TABLE z (b TEXT, PRIMARY KEY(aa, b)) inherits (a)
;
CREATE TABLE "public"."pagg_tab_ml_p2_s2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
COMMENT ON COLUMN comment_test_child.id IS 'Column ''id'' on comment_test_child'
;
ALTER TABLE testpub_tbl8_0 REPLICA IDENTITY USING INDEX testpub_tbl8_0_pkey
;
create table pktable(ptest1 inet, ptest2 inet, primary key(base1, ptest1), foreign key(ptest2, base2) references
                                             pktable(base1, ptest1)) inherits (pktable_base)
;
-- test with views
CREATE TABLE functest3 (a int)
;
-- a => b, a => c, b => c
TRUNCATE functional_dependencies
;
CREATE TYPE lquery (
	INTERNALLENGTH = -1,
	INPUT = lquery_in,
	OUTPUT = lquery_out,
	STORAGE = extended
)
;
CREATE UNIQUE INDEX mvtest_drop_idx ON drop_idx_matview (i)
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "numword" WITH "simple"
;
ALTER OPERATOR FAMILY alt_opf5 USING btree ADD OPERATOR 1 < (int4, int2), FUNCTION 1 btint42cmp(int4, int2)
;
CREATE FUNCTION base_fn_out(opaque) RETURNS opaque AS 'boolout'
    LANGUAGE internal IMMUTABLE STRICT
;
DROP TABLE truncprim, truncpart
;
CREATE INDEX bt_i4_index ON bt_i4_heap USING btree (seqno int4_ops)
;
CREATE TABLE "public"."ne_check_tbl" (
    "x" integer
)
;
-- Test anonymous code blocks.

DO $$
DECLARE r record;
BEGIN
    FOR r IN SELECT rtrim(roomno) AS roomno, comment FROM Room ORDER BY roomno
    LOOP
        RAISE NOTICE '%, %', r.roomno, r.comment;
    END LOOP;
END$$
;
DROP TABLE VARCHAR_TBL
;
-- fail
alter table idxpart0 drop constraint idxpart0_pkey
;
vacuum analyze atacc1("........pg.dropped.1........")
;
-- ok
create table test_part_coll2 partition of test_part_coll_posix for values from ('g') to ('m')
;
DROP FUNCTION composite_f
;
-- Catalog
VACUUM pg_catalog.pg_class
;
CREATE TABLE plt1_adv_p2 PARTITION OF plt1_adv FOR VALUES IN ('0004', '0006')
;
ALTER TABLE split_alter_table.base_table ALTER COLUMN payload SET (n_distinct = -0.5, n_distinct_inherited = -0.25)
;
ALTER TABLE pg_toast.pg_toast_28529 RENAME TO reind_con_toast
;
/*
 * 5.17
 * COLUMN_COLUMN_USAGE view
 */

CREATE VIEW column_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(n.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(ac.attname AS sql_identifier) AS column_name,
           CAST(ad.attname AS sql_identifier) AS dependent_column

    FROM pg_namespace n, pg_class c, pg_depend d,
         pg_attribute ac, pg_attribute ad, pg_attrdef atd

    WHERE n.oid = c.relnamespace
          AND c.oid = ac.attrelid
          AND c.oid = ad.attrelid
          AND ac.attnum <> ad.attnum
          AND ad.attrelid = atd.adrelid
          AND ad.attnum = atd.adnum
          AND d.classid = 'pg_catalog.pg_attrdef'::regclass
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND d.objid = atd.oid
          AND d.refobjid = ac.attrelid
          AND d.refobjsubid = ac.attnum
          AND ad.attgenerated <> ''
          AND pg_has_role(c.relowner, 'USAGE')
;
VACUUM (PARALLEL 0) pg_catalog.pg_extension
;
-- both trigger and update change the partition
create or replace function parted_trigfunc2() returns trigger language plpgsql as $$
begin
  new.a = new.a + 1;
  return new;
end;
$$
;
CREATE TABLE tbl (val1 integer)
;
ALTER TEXT SEARCH CONFIGURATION english ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
-- we leave nv_parent and children around to help test pg_dump logic

-- Foreign key adding test with mixed types

-- Note: these tables are TEMP to avoid name conflicts when this test
-- is run in parallel with foreign_key.sql.

CREATE TEMP TABLE PKTABLE (ptest1 int PRIMARY KEY)
;
DROP TABLE pg_temp_3.pg_temp_29555_2, pg_temp_3.pg_temp_29555
;
ALTER INDEX pg_toast.pg_toast_27981_index RENAME TO reind_con_toast_idx
;
DROP TRIGGER after_upd_a_b_row_trig ON main_table
;
------------------------------------------------------------------------------------------
[ADD_INDEX] -- quiet

-- reject dupe
create index idxpart1_2_a_b on idxpart1 (a, b)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_tab ADD COLUMN e INT
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_language
;
----------
[BLOCK] do $$ declare a int[];
begin a := f1 from onecol limit 1; raise notice 'a = %', a; end$$
;
----------
[DROP_VIEW] DROP VIEW rec1v, rec2v CASCADE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30939 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TEMP TABLE t (a integer, b integer) PARTITION BY HASH (a, b)
;
----------
[DROP_TABLE] DROP TABLE gtest4
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_rewrite
;
----------
[CREATE_TABLE] CREATE TABLE vaccluster (i INT PRIMARY KEY)
;
----------
[CREATE_TABLE] create table inserttestb (f1 insert_test_domain)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "sfloat" WITH "simple"
;
----------
[CREATE_POLICY] CREATE OR REPLACE RULE y_rule AS ON INSERT TO y DO INSTEAD NOTHING
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_foreign_server
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_TABLE] -- 1 2 3 4

create table test3 (c int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE z (b TEXT, PRIMARY KEY(aa, b)) inherits (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_ml_p2_s2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN comment_test_child.id IS 'Column ''id'' on comment_test_child'
;
----------
[ALTER_TABLE] ALTER TABLE testpub_tbl8_0 REPLICA IDENTITY USING INDEX testpub_tbl8_0_pkey
;
----------
[CREATE_TABLE] create table pktable(ptest1 inet, ptest2 inet, primary key(base1, ptest1), foreign key(ptest2, base2) references
                                             pktable(base1, ptest1)) inherits (pktable_base)
;
----------
[CREATE_TABLE] -- test with views
CREATE TABLE functest3 (a int)
;
----------
[TRUNCATE_TABLE] -- a => b, a => c, b => c
TRUNCATE functional_dependencies
;
----------
[CREATE_TYPE] CREATE TYPE lquery (
	INTERNALLENGTH = -1,
	INPUT = lquery_in,
	OUTPUT = lquery_out,
	STORAGE = extended
)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX mvtest_drop_idx ON drop_idx_matview (i)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "numword" WITH "simple"
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf5 USING btree ADD OPERATOR 1 < (int4, int2), FUNCTION 1 btint42cmp(int4, int2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION base_fn_out(opaque) RETURNS opaque AS 'boolout'
    LANGUAGE internal IMMUTABLE STRICT
;
----------
[DROP_TABLE] DROP TABLE truncprim, truncpart
;
----------
[ADD_INDEX] CREATE INDEX bt_i4_index ON bt_i4_heap USING btree (seqno int4_ops)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ne_check_tbl" (
    "x" integer
)
;
----------
[BLOCK] -- Test anonymous code blocks.

DO $$
DECLARE r record;
BEGIN
    FOR r IN SELECT rtrim(roomno) AS roomno, comment FROM Room ORDER BY roomno
    LOOP
        RAISE NOTICE '%, %', r.roomno, r.comment;
    END LOOP;
END$$
;
----------
[DROP_TABLE] DROP TABLE VARCHAR_TBL
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- fail
alter table idxpart0 drop constraint idxpart0_pkey
;
----------
[ADMIN_TABLE] vacuum analyze atacc1("........pg.dropped.1........")
;
----------
[CREATE_TABLE] -- ok
create table test_part_coll2 partition of test_part_coll_posix for values from ('g') to ('m')
;
----------
[DROP_PROG_OBJ] DROP FUNCTION composite_f
;
----------
[ADMIN_TABLE] -- Catalog
VACUUM pg_catalog.pg_class
;
----------
[CREATE_TABLE] CREATE TABLE plt1_adv_p2 PARTITION OF plt1_adv FOR VALUES IN ('0004', '0006')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE split_alter_table.base_table ALTER COLUMN payload SET (n_distinct = -0.5, n_distinct_inherited = -0.25)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28529 RENAME TO reind_con_toast
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.17
 * COLUMN_COLUMN_USAGE view
 */

CREATE VIEW column_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(n.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(ac.attname AS sql_identifier) AS column_name,
           CAST(ad.attname AS sql_identifier) AS dependent_column

    FROM pg_namespace n, pg_class c, pg_depend d,
         pg_attribute ac, pg_attribute ad, pg_attrdef atd

    WHERE n.oid = c.relnamespace
          AND c.oid = ac.attrelid
          AND c.oid = ad.attrelid
          AND ac.attnum <> ad.attnum
          AND ad.attrelid = atd.adrelid
          AND ad.attnum = atd.adnum
          AND d.classid = 'pg_catalog.pg_attrdef'::regclass
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND d.objid = atd.oid
          AND d.refobjid = ac.attrelid
          AND d.refobjsubid = ac.attnum
          AND ad.attgenerated <> ''
          AND pg_has_role(c.relowner, 'USAGE')
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_extension
;
----------
[CREATE_PROG_OBJ] -- both trigger and update change the partition
create or replace function parted_trigfunc2() returns trigger language plpgsql as $$
begin
  new.a = new.a + 1;
  return new;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE tbl (val1 integer)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION english ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_TABLE] -- we leave nv_parent and children around to help test pg_dump logic

-- Foreign key adding test with mixed types

-- Note: these tables are TEMP to avoid name conflicts when this test
-- is run in parallel with foreign_key.sql.

CREATE TEMP TABLE PKTABLE (ptest1 int PRIMARY KEY)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_29555_2, pg_temp_3.pg_temp_29555
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27981_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TRIGGER] DROP TRIGGER after_upd_a_b_row_trig ON main_table
;
