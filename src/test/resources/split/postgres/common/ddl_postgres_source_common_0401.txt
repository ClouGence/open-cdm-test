ALTER TEXT SEARCH DICTIONARY synonym (CaseSensitive = 2)
;
ALTER FUNCTION gbt_int8_penalty(internal, internal, internal) PARALLEL SAFE
;
CREATE VIEW v9_temp AS SELECT * FROM base_table WHERE NOT EXISTS (SELECT 1 FROM temp_table)
;
ALTER TEXT SEARCH CONFIGURATION spanish ADD MAPPING
    FOR word, hword_part, hword
	WITH spanish_stem
;
-- Again, so should this...
CREATE TABLE FKTABLE (ftest1 cidr, ftest2 timestamp, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest1, ptest2))
;
CREATE TRIGGER d15_delete_trig
  AFTER DELETE ON part_d_15_20 for each statement execute procedure trigfunc()
;
alter table my_locks reset (security_barrier)
;
--
-- Verify access of transition tables with UPDATE triggers and tuples
-- moved across partitions.
--
create or replace function dump_update_new() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, new table = %', TG_NAME,
                 (select string_agg(new_table::text, ', ' order by a) from new_table);
    return null;
  end;
$$
;
ALTER TABLE ONLY "fkpart3"."fk" ATTACH PARTITION "fkpart3"."fk1" FOR VALUES FROM (0) TO (750)
;
ALTER OPERATOR FAMILY gist_bit_ops USING gist ADD
	OPERATOR	6	<> (bit, bit) ,
	FUNCTION	9 (bit, bit) gbt_var_fetch (internal)
;
/*
 * text search configuration for dutch language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * dutch and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
drop trigger trans_tab_parent_update_trig on trans_tab_parent
;
-- Try an unlogged table too

CREATE UNLOGGED TABLE tstu (
	i	int4,
	t	text
)
;
-- xid8 has btree and hash opclasses
create index on xid8_t1 using btree(x)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attribute
;
ALTER TABLE pg_toast.pg_toast_33383 RENAME TO reind_con_toast
;
create table rc_test (a int, b int)
;
ANALYZE sb_2
;
CREATE TABLE c (i int)
;
CREATE FUNCTION btissn13cmp(issn13, issn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ANALYZE pg_temp_3.pg_temp_28840_2
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_aggregate
;
CREATE TABLE fail_default_part PARTITION OF list_parted DEFAULT
;
ALTER TYPE test_type DROP ATTRIBUTE a, ADD ATTRIBUTE d boolean
;
CREATE FUNCTION "public"."test_atomic_ops"() RETURNS boolean
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'test_atomic_ops'
;
-- Test that no-op set succeeds
ALTER OPERATOR === (boolean, real) SET (NEGATOR = !====)
;
-- cleanup
DROP FOREIGN TABLE analyze_ftable
;
TRUNCATE tab1, tab1 RESTART IDENTITY CASCADE
;
CREATE TABLE dependent (x integer, y integer)
;
CREATE TABLE "public"."test_io_vac_strategy" (
    "a" integer,
    "b" integer
)
WITH ("autovacuum_enabled"='false')
;
ANALYZE public.pagg_tab_ml_p2_s2
;
/* src/test/modules/test_rbtree/test_rbtree--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_rb_tree(size INTEGER)
	RETURNS pg_catalog.void STRICT
	AS '$libdir/test_rbtree' LANGUAGE C
;
-- fail - invalid option combinations
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, copy_data = true)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_database
;
-- Show visibility map and page-level visibility information for each block.
CREATE FUNCTION pg_visibility(regclass, blkno OUT bigint,
							  all_visible OUT boolean,
							  all_frozen OUT boolean,
							  pd_all_visible OUT boolean)
RETURNS SETOF record
AS '$libdir/pg_visibility', 'pg_visibility_rel'
LANGUAGE C STRICT
;
create table inh_lp2 (a int, value int, check(a = 2)) inherits (inh_lp)
;
CREATE SCHEMA selinto_schema
;
ANALYZE fkpart3.pk2
;
ALTER VIEW attmp_view_new RENAME TO fail
;
ALTER TABLE schema_rf_x.tab_rf_partitioned ATTACH PARTITION public.tab_rf_partition DEFAULT
;
DROP TRIGGER trig_row_after1 ON rem1
;
DROP TABLE plt1_adv_extra
;
VACUUM pg_catalog.pg_shdescription
;
create extension test_ext8
;
ALTER FUNCTION gbt_float4_fetch(internal) PARALLEL SAFE
;
DROP DOMAIN dcheck_ign_err2
;
ALTER TABLE split_alter_table.base_table RESET (fillfactor, autovacuum_enabled)
;
ALTER INDEX pg_toast.pg_toast_28181_index RENAME TO reind_con_toast_idx
;
ANALYZE public.real_city
;
DROP TABLE IF EXISTS toasted_several
;
------------------------------------------------------------------------------------------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY synonym (CaseSensitive = 2)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int8_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v9_temp AS SELECT * FROM base_table WHERE NOT EXISTS (SELECT 1 FROM temp_table)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION spanish ADD MAPPING
    FOR word, hword_part, hword
	WITH spanish_stem
;
----------
[CREATE_TABLE] -- Again, so should this...
CREATE TABLE FKTABLE (ftest1 cidr, ftest2 timestamp, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest1, ptest2))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER d15_delete_trig
  AFTER DELETE ON part_d_15_20 for each statement execute procedure trigfunc()
;
----------
[ALTER_TABLE] alter table my_locks reset (security_barrier)
;
----------
[CREATE_PROG_OBJ] --
-- Verify access of transition tables with UPDATE triggers and tuples
-- moved across partitions.
--
create or replace function dump_update_new() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, new table = %', TG_NAME,
                 (select string_agg(new_table::text, ', ' order by a) from new_table);
    return null;
  end;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."fk" ATTACH PARTITION "fkpart3"."fk1" FOR VALUES FROM (0) TO (750)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bit_ops USING gist ADD
	OPERATOR	6	<> (bit, bit) ,
	FUNCTION	9 (bit, bit) gbt_var_fetch (internal)
;
----------
[CREATE_POLICY] /*
 * text search configuration for dutch language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * dutch and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
----------
[DROP_TRIGGER] drop trigger trans_tab_parent_update_trig on trans_tab_parent
;
----------
[CREATE_TABLE] -- Try an unlogged table too

CREATE UNLOGGED TABLE tstu (
	i	int4,
	t	text
)
;
----------
[ADD_INDEX] -- xid8 has btree and hash opclasses
create index on xid8_t1 using btree(x)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attribute
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33383 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] create table rc_test (a int, b int)
;
----------
[ADMIN_TABLE] ANALYZE sb_2
;
----------
[CREATE_TABLE] CREATE TABLE c (i int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btissn13cmp(issn13, issn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_28840_2
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_aggregate
;
----------
[CREATE_TABLE] CREATE TABLE fail_default_part PARTITION OF list_parted DEFAULT
;
----------
[ALTER_TYPE] ALTER TYPE test_type DROP ATTRIBUTE a, ADD ATTRIBUTE d boolean
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_atomic_ops"() RETURNS boolean
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'test_atomic_ops'
;
----------
[ALTER_PROG_OBJ] -- Test that no-op set succeeds
ALTER OPERATOR === (boolean, real) SET (NEGATOR = !====)
;
----------
[DROP_TABLE] -- cleanup
DROP FOREIGN TABLE analyze_ftable
;
----------
[TRUNCATE_TABLE] TRUNCATE tab1, tab1 RESTART IDENTITY CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE dependent (x integer, y integer)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_io_vac_strategy" (
    "a" integer,
    "b" integer
)
WITH ("autovacuum_enabled"='false')
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab_ml_p2_s2
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_rbtree/test_rbtree--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_rb_tree(size INTEGER)
	RETURNS pg_catalog.void STRICT
	AS '$libdir/test_rbtree' LANGUAGE C
;
----------
[CREATE_PUB_SUB] -- fail - invalid option combinations
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, copy_data = true)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_database
;
----------
[CREATE_PROG_OBJ] -- Show visibility map and page-level visibility information for each block.
CREATE FUNCTION pg_visibility(regclass, blkno OUT bigint,
							  all_visible OUT boolean,
							  all_frozen OUT boolean,
							  pd_all_visible OUT boolean)
RETURNS SETOF record
AS '$libdir/pg_visibility', 'pg_visibility_rel'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] create table inh_lp2 (a int, value int, check(a = 2)) inherits (inh_lp)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA selinto_schema
;
----------
[ADMIN_TABLE] ANALYZE fkpart3.pk2
;
----------
[RENAME_VIEW] ALTER VIEW attmp_view_new RENAME TO fail
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE schema_rf_x.tab_rf_partitioned ATTACH PARTITION public.tab_rf_partition DEFAULT
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_after1 ON rem1
;
----------
[DROP_TABLE] DROP TABLE plt1_adv_extra
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_shdescription
;
----------
[CREATE_LIBRARY] create extension test_ext8
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float4_fetch(internal) PARALLEL SAFE
;
----------
[DROP_TYPE] DROP DOMAIN dcheck_ign_err2
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table RESET (fillfactor, autovacuum_enabled)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28181_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE public.real_city
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS toasted_several
;
