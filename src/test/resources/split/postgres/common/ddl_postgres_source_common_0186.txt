CREATE INDEX k ON testschema.tablespace_acl (c) TABLESPACE regress_tblspace
;
DROP PUBLICATION testpub_default
;
CREATE SEQUENCE fkey_table_seq
  INCREMENT BY 1
  MINVALUE 0
  MAXVALUE 1000000
  START 10
  CACHE 10
  CYCLE
;
CREATE TABLE regtest_table_2 (c text) inherits (regtest_table_1)
;
CREATE FUNCTION unlck() RETURNS bool IMMUTABLE LANGUAGE plpgsql AS $$
     BEGIN PERFORM pg_advisory_unlock_all(); RETURN true; END;
  $$
;
--
-- Check deparse disambiguation of INSERT/UPDATE/DELETE targets.
--
create rule r7 as on delete to rules_src do instead
  with wins as (insert into int4_tbl as trgt values (0) returning *),
       wupd as (update int4_tbl trgt set f1 = f1+1 returning *),
       wdel as (delete from int4_tbl trgt where f1 = 0 returning *)
  insert into rules_log AS trgt select old.* from wins, wupd, wdel
  returning trgt.f1, trgt.f2
;
drop table atacc1
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
CREATE FUNCTION "public"."int8alias1out"("public"."int8alias1") RETURNS "cstring"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8out$$
;
ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = NONE, JOIN = NONE)
;
ANALYZE public.pht1_e_p2
;
VACUUM (FULL) pg_catalog.pg_depend
;
alter index spgist_point_idx set (fillfactor = 90)
;
create table gen_part_1_2
  partition of gen_part_1 for values from (2) to (3)
;
CREATE INDEX hash_i4_partial_index ON hash_i4_heap USING hash (seqno)
  WHERE seqno = 9999
;
do $$
begin
  for i in 10000..11000 loop
    -- use an exception block so that each iteration eats an XID
    begin
      insert into tab_int values (i);
    exception
      when division_by_zero then null;
    end;
  end loop;
end$$
;
create or replace function execute_into_test(varchar) returns record as $$
declare
    _r record;
    _rt eifoo%rowtype;
    _v eitype;
    i int;
    j int;
    k int;
begin
    execute 'insert into '||$1||' values(10,15)';
    execute 'select (row).* from (select row(10,1)::eifoo) s' into _r;
    raise notice '% %', _r.i, _r.y;
    execute 'select * from '||$1||' limit 1' into _rt;
    raise notice '% %', _rt.i, _rt.y;
    execute 'select *, 20 from '||$1||' limit 1' into i, j, k;
    raise notice '% % %', i, j, k;
    execute 'select 1,2' into _v;
    return _v;
end; $$ language plpgsql
;
drop foreign table remt1
;
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_3
  FOR VALUES FROM (2000,2000) TO (3000,3000)
;
CREATE TABLE prt1_l (a int, b int, c varchar) PARTITION BY RANGE(a)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
CREATE TABLE bools (
	a bool
) PARTITION BY LIST (a)
;
CREATE FUNCTION test_resowner_many(
   nkinds       pg_catalog.int4,
   nremember_bl pg_catalog.int4,
   nforget_bl   pg_catalog.int4,
   nremember_al pg_catalog.int4,
   nforget_al   pg_catalog.int4
)
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE FUNCTION buffer_call_terminate_io(buffer int, for_input bool, succeed bool, io_error bool, release_aio bool)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
drop function count_tt1_s()
;
CREATE INDEX t1_spgist ON s1.t1 USING SPGIST (ir)
;
alter table inh_child2 inherit inh_child1
;
CREATE TABLE num_exp_power_10_ln (id int4, expected numeric(210,10))
;
ALTER TABLE ONLY public.tht ATTACH PARTITION public.tht_p2 FOR VALUES WITH (modulus 3, remainder 1)
;
ALTER OPERATOR FAMILY alt_opf4 USING invalid_index_method ADD  OPERATOR 1 < (int4, int2)
;
ANALYZE pg_temp_11.pg_temp_26345_2
;
create type two_int8s as (q1 int8, q2 int8)
;
-- ok
create table test_part_coll_cast partition of test_part_coll_posix for values from (name 'm' collate "POSIX") to ('s')
;
CREATE FUNCTION isnge(ean13, ismn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_sequence
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_extension
;
CREATE PUBLICATION tap_pub2 FOR TABLE skip_wal
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_default_acl
;
CREATE TABLE test3 (y int UNIQUE DEFERRABLE INITIALLY DEFERRED)
;
DROP SUBSCRIPTION test_tab1_sub
;
drop table parent, child1
;
create function getq1(two_int8s) returns int8 language plpgsql as $$
declare r two_int8s; begin r := $1; return r.q1; end $$
;
ANALYZE information_schema.sql_parts
;
DROP VIEW lock_view1
;
drop table ddtest2
;
ALTER INDEX testschema.anindex SET TABLESPACE regress_tblspace
;
ALTER SEQUENCE "public"."spgist_unlogged_tbl_id_seq" OWNED BY "public"."spgist_unlogged_tbl"."id"
;
CREATE FUNCTION cube(cube, float8) RETURNS cube
AS '$libdir/cube', 'cube_c_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
--
-- Verify behavior of statement triggers on (non-partition)
-- inheritance hierarchy with transition tables; similar to the
-- partition case, except there is no rerouting on insertion and child
-- tables can have extra columns
--

-- set up inheritance hierarchy with different TupleDescriptors
create table parent (a text, b int)
;
-- Test setting commutator
ALTER OPERATOR === (boolean, real) SET (COMMUTATOR = ====)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX k ON testschema.tablespace_acl (c) TABLESPACE regress_tblspace
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_default
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE fkey_table_seq
  INCREMENT BY 1
  MINVALUE 0
  MAXVALUE 1000000
  START 10
  CACHE 10
  CYCLE
;
----------
[CREATE_TABLE] CREATE TABLE regtest_table_2 (c text) inherits (regtest_table_1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION unlck() RETURNS bool IMMUTABLE LANGUAGE plpgsql AS $$
     BEGIN PERFORM pg_advisory_unlock_all(); RETURN true; END;
  $$
;
----------
[CREATE_POLICY] --
-- Check deparse disambiguation of INSERT/UPDATE/DELETE targets.
--
create rule r7 as on delete to rules_src do instead
  with wins as (insert into int4_tbl as trgt values (0) returning *),
       wupd as (update int4_tbl trgt set f1 = f1+1 returning *),
       wdel as (delete from int4_tbl trgt where f1 = 0 returning *)
  insert into rules_log AS trgt select old.* from wins, wupd, wdel
  returning trgt.f1, trgt.f2
;
----------
[DROP_TABLE] drop table atacc1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int8alias1out"("public"."int8alias1") RETURNS "cstring"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8out$$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = NONE, JOIN = NONE)
;
----------
[ADMIN_TABLE] ANALYZE public.pht1_e_p2
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_depend
;
----------
[ALTER_INDEX] alter index spgist_point_idx set (fillfactor = 90)
;
----------
[CREATE_TABLE] create table gen_part_1_2
  partition of gen_part_1 for values from (2) to (3)
;
----------
[ADD_INDEX] CREATE INDEX hash_i4_partial_index ON hash_i4_heap USING hash (seqno)
  WHERE seqno = 9999
;
----------
[BLOCK] do $$
begin
  for i in 10000..11000 loop
    -- use an exception block so that each iteration eats an XID
    begin
      insert into tab_int values (i);
    exception
      when division_by_zero then null;
    end;
  end loop;
end$$
;
----------
[CREATE_PROG_OBJ] create or replace function execute_into_test(varchar) returns record as $$
declare
    _r record;
    _rt eifoo%rowtype;
    _v eitype;
    i int;
    j int;
    k int;
begin
    execute 'insert into '||$1||' values(10,15)';
    execute 'select (row).* from (select row(10,1)::eifoo) s' into _r;
    raise notice '% %', _r.i, _r.y;
    execute 'select * from '||$1||' limit 1' into _rt;
    raise notice '% %', _rt.i, _rt.y;
    execute 'select *, 20 from '||$1||' limit 1' into i, j, k;
    raise notice '% % %', i, j, k;
    execute 'select 1,2' into _v;
    return _v;
end; $$ language plpgsql
;
----------
[DROP_TABLE] drop foreign table remt1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_3
  FOR VALUES FROM (2000,2000) TO (3000,3000)
;
----------
[CREATE_TABLE] CREATE TABLE prt1_l (a int, b int, c varchar) PARTITION BY RANGE(a)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] CREATE TABLE bools (
	a bool
) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_resowner_many(
   nkinds       pg_catalog.int4,
   nremember_bl pg_catalog.int4,
   nforget_bl   pg_catalog.int4,
   nremember_al pg_catalog.int4,
   nforget_al   pg_catalog.int4
)
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION buffer_call_terminate_io(buffer int, for_input bool, succeed bool, io_error bool, release_aio bool)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[DROP_PROG_OBJ] drop function count_tt1_s()
;
----------
[ADD_INDEX] CREATE INDEX t1_spgist ON s1.t1 USING SPGIST (ir)
;
----------
[ALTER_TABLE] alter table inh_child2 inherit inh_child1
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_power_10_ln (id int4, expected numeric(210,10))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY public.tht ATTACH PARTITION public.tht_p2 FOR VALUES WITH (modulus 3, remainder 1)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf4 USING invalid_index_method ADD  OPERATOR 1 < (int4, int2)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26345_2
;
----------
[CREATE_TYPE] create type two_int8s as (q1 int8, q2 int8)
;
----------
[CREATE_TABLE] -- ok
create table test_part_coll_cast partition of test_part_coll_posix for values from (name 'm' collate "POSIX") to ('s')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ean13, ismn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_sequence
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_extension
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub2 FOR TABLE skip_wal
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] CREATE TABLE test3 (y int UNIQUE DEFERRABLE INITIALLY DEFERRED)
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION test_tab1_sub
;
----------
[DROP_TABLE] drop table parent, child1
;
----------
[CREATE_PROG_OBJ] create function getq1(two_int8s) returns int8 language plpgsql as $$
declare r two_int8s; begin r := $1; return r.q1; end $$
;
----------
[ADMIN_TABLE] ANALYZE information_schema.sql_parts
;
----------
[DROP_VIEW] DROP VIEW lock_view1
;
----------
[DROP_TABLE] drop table ddtest2
;
----------
[ALTER_INDEX] ALTER INDEX testschema.anindex SET TABLESPACE regress_tblspace
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."spgist_unlogged_tbl_id_seq" OWNED BY "public"."spgist_unlogged_tbl"."id"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube(cube, float8) RETURNS cube
AS '$libdir/cube', 'cube_c_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] --
-- Verify behavior of statement triggers on (non-partition)
-- inheritance hierarchy with transition tables; similar to the
-- partition case, except there is no rerouting on insertion and child
-- tables can have extra columns
--

-- set up inheritance hierarchy with different TupleDescriptors
create table parent (a text, b int)
;
----------
[ALTER_PROG_OBJ] -- Test setting commutator
ALTER OPERATOR === (boolean, real) SET (COMMUTATOR = ====)
;
