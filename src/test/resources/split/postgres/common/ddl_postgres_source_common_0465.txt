-- also OK
CREATE UNLOGGED TABLE pg_temp.unlogged3 (a int primary key)
;
ALTER OPERATOR ^? (_ltree, _lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE TABLE cp_test (a int, b text)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_range
;
create table child (d varchar(255)) inherits (parent)
;
CREATE FUNCTION pg_check_visible(regclass, t_ctid OUT tid)
RETURNS SETOF tid
AS '$libdir/pg_visibility', 'pg_check_visible'
LANGUAGE C STRICT
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_shseclabel
;
-- fail

DROP TABLE ext_cine_tab2
;
--
-- Check type parsing and record fetching from partitioned tables
--

CREATE TABLE partitioned_table (a int, b text) PARTITION BY LIST (a)
;
-- fail, conflict w/parent2
alter table parent1 alter column f2 type bigint
;
CREATE TABLE cnn_pk (a int, b int)
;
CREATE TABLE "S 1"."T 3" (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	CONSTRAINT t3_pkey PRIMARY KEY (c1)
)
;
CREATE INDEX idx_varbit ON test_varbit USING gin (i)
;
ALTER TEXT SEARCH CONFIGURATION evttrig_tscfg
    DROP MAPPING FOR word
;
CREATE TABLE base_tbl_child (CHECK (a > 0)) INHERITS (base_tbl_parent)
;
ALTER TYPE test_type2 ALTER ATTRIBUTE b TYPE varchar
;
CREATE TEMP TABLE pg_temp_2.pg_temp_28105_2 (tid pg_catalog.tid)
;
ALTER TABLE tab_fk_ref ENABLE REPLICA TRIGGER filter_basic_dml_trg
;
alter type testdomain2 rename to testdomain3
;
--
-- IF EXISTS tests
--

-- table (will be really dropped at the end)

DROP TABLE test_exists
;
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = numeric_avg_combine,
	finalfunc_modify = shareable  
)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn13,
	RIGHTARG = issn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
ANALYZE agg_bad
;
CREATE TYPE sqljsonb_rec AS (a int, t text, js json, jb jsonb, jsa json[])
;
create extension test_regex
;
CREATE TABLE pk (a int PRIMARY KEY) PARTITION BY RANGE (a)
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "asciihword" WITH "public"."synonym", "public"."thesaurus", "english_stem"
;
CREATE VIEW "public"."street" AS
 SELECT "r"."name",
    "r"."thepath",
    "c"."cname"
   FROM ONLY "public"."road" "r",
    "public"."real_city" "c"
  WHERE ("c"."outline" ?# "r"."thepath")
;
ALTER FUNCTION gbt_int8_fetch(internal) PARALLEL SAFE
;
DO $$
DECLARE
  o int;
  a int[] := ARRAY[1,2,3,2,3,1,2];
BEGIN
  o := array_position(a, 2);
  WHILE o IS NOT NULL
  LOOP
    RAISE NOTICE '%', o;
    o := array_position(a, 2, o + 1);
  END LOOP;
END
$$ LANGUAGE plpgsql
;
ALTER PUBLICATION split_pub_cols DROP TABLE split_fdw_repl.accounts_extra
;
ALTER TABLE split_alter_table.base_table REPLICA IDENTITY DEFAULT
;
CREATE UNIQUE INDEX concur_reindex_ind3 ON concur_reindex_tab(abs(c1))
;
CREATE TABLE "public"."pitest1_p2" (
    "f3" bigint NOT NULL,
    "f2" "text",
    "f1" "date" NOT NULL
)
;
-- Ensure LIST partitions allow an Append to be used instead of a MergeAppend
create table bool_lp (b bool) partition by list(b)
;
create table uv_pt1 (b int not null, v varchar, a int not null) partition by range (b)
;
CREATE POLICY p1 ON z1 TO regress_rls_group1 USING (a % 2 = 0)
;
-- results below depend on having quite accurate stats for atest12, so...
ALTER TABLE atest12 SET (autovacuum_enabled = off)
;
ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p1" FOR VALUES FROM (0) TO (12)
;
DROP TABLE pg_temp_13.pg_temp_26366_2, pg_temp_13.pg_temp_26366
;
create temp table gstest4(id integer, v integer,
                          unhashable_col bit(4), unsortable_col xid)
;
ANALYZE transition_table_level2
;
CREATE OPERATOR <-> (
	LEFTARG = float4,
	RIGHTARG = float4,
	PROCEDURE = float4_dist,
	COMMUTATOR = '<->'
)
;
DROP TABLE BIT_SHIFT_TABLE
;
ALTER DATABASE "isolation_regression" SET timezone_abbreviations TO 'Default'
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c4 SET DEFAULT 0
;
CREATE TABLE datetmp (a date)
;
CREATE TABLE "tab'le" (a int PRIMARY KEY)
;
CREATE TABLE "public"."gtest_child2" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
CREATE TABLE fk_d PARTITION OF fk DEFAULT
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- also OK
CREATE UNLOGGED TABLE pg_temp.unlogged3 (a int primary key)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^? (_ltree, _lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE cp_test (a int, b text)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_range
;
----------
[CREATE_TABLE] create table child (d varchar(255)) inherits (parent)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_check_visible(regclass, t_ctid OUT tid)
RETURNS SETOF tid
AS '$libdir/pg_visibility', 'pg_check_visible'
LANGUAGE C STRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_shseclabel
;
----------
[DROP_TABLE] -- fail

DROP TABLE ext_cine_tab2
;
----------
[CREATE_TABLE] --
-- Check type parsing and record fetching from partitioned tables
--

CREATE TABLE partitioned_table (a int, b text) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fail, conflict w/parent2
alter table parent1 alter column f2 type bigint
;
----------
[CREATE_TABLE] CREATE TABLE cnn_pk (a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE "S 1"."T 3" (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	CONSTRAINT t3_pkey PRIMARY KEY (c1)
)
;
----------
[ADD_INDEX] CREATE INDEX idx_varbit ON test_varbit USING gin (i)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION evttrig_tscfg
    DROP MAPPING FOR word
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl_child (CHECK (a > 0)) INHERITS (base_tbl_parent)
;
----------
[ALTER_TYPE] ALTER TYPE test_type2 ALTER ATTRIBUTE b TYPE varchar
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_2.pg_temp_28105_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE] ALTER TABLE tab_fk_ref ENABLE REPLICA TRIGGER filter_basic_dml_trg
;
----------
[RENAME_TYPE] alter type testdomain2 rename to testdomain3
;
----------
[DROP_TABLE] --
-- IF EXISTS tests
--

-- table (will be really dropped at the end)

DROP TABLE test_exists
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = numeric_avg_combine,
	finalfunc_modify = shareable  
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn13,
	RIGHTARG = issn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ADMIN_TABLE] ANALYZE agg_bad
;
----------
[CREATE_TYPE] CREATE TYPE sqljsonb_rec AS (a int, t text, js json, jb jsonb, jsa json[])
;
----------
[CREATE_LIBRARY] create extension test_regex
;
----------
[CREATE_TABLE] CREATE TABLE pk (a int PRIMARY KEY) PARTITION BY RANGE (a)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "asciihword" WITH "public"."synonym", "public"."thesaurus", "english_stem"
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."street" AS
 SELECT "r"."name",
    "r"."thepath",
    "c"."cname"
   FROM ONLY "public"."road" "r",
    "public"."real_city" "c"
  WHERE ("c"."outline" ?# "r"."thepath")
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int8_fetch(internal) PARALLEL SAFE
;
----------
[BLOCK] DO $$
DECLARE
  o int;
  a int[] := ARRAY[1,2,3,2,3,1,2];
BEGIN
  o := array_position(a, 2);
  WHILE o IS NOT NULL
  LOOP
    RAISE NOTICE '%', o;
    o := array_position(a, 2, o + 1);
  END LOOP;
END
$$ LANGUAGE plpgsql
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION split_pub_cols DROP TABLE split_fdw_repl.accounts_extra
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table REPLICA IDENTITY DEFAULT
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX concur_reindex_ind3 ON concur_reindex_tab(abs(c1))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest1_p2" (
    "f3" bigint NOT NULL,
    "f2" "text",
    "f1" "date" NOT NULL
)
;
----------
[CREATE_TABLE] -- Ensure LIST partitions allow an Append to be used instead of a MergeAppend
create table bool_lp (b bool) partition by list(b)
;
----------
[CREATE_TABLE] create table uv_pt1 (b int not null, v varchar, a int not null) partition by range (b)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON z1 TO regress_rls_group1 USING (a % 2 = 0)
;
----------
[ALTER_TABLE] -- results below depend on having quite accurate stats for atest12, so...
ALTER TABLE atest12 SET (autovacuum_enabled = off)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p1" FOR VALUES FROM (0) TO (12)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26366_2, pg_temp_13.pg_temp_26366
;
----------
[CREATE_TABLE] create temp table gstest4(id integer, v integer,
                          unhashable_col bit(4), unsortable_col xid)
;
----------
[ADMIN_TABLE] ANALYZE transition_table_level2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = float4,
	RIGHTARG = float4,
	PROCEDURE = float4_dist,
	COMMUTATOR = '<->'
)
;
----------
[DROP_TABLE] DROP TABLE BIT_SHIFT_TABLE
;
----------
[ALTER_CATALOG] ALTER DATABASE "isolation_regression" SET timezone_abbreviations TO 'Default'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c4 SET DEFAULT 0
;
----------
[CREATE_TABLE] CREATE TABLE datetmp (a date)
;
----------
[CREATE_TABLE] CREATE TABLE "tab'le" (a int PRIMARY KEY)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest_child2" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
----------
[CREATE_TABLE] CREATE TABLE fk_d PARTITION OF fk DEFAULT
;
