REASSIGN OWNED BY CURRENT_USER TO split_owned_receiver
;
COPY public.test_tab (a, b, c, d) TO STDOUT
;
EXECUTE dumpTableAttach('37624')
;
LOCK TABLE public.bug_16784 IN ACCESS SHARE MODE
;
PREPARE getrow_seqscan AS SELECT * FROM test_dc WHERE data = 34 ORDER BY id,data
;
EXECUTE dumpAgg('16778')
;
EXECUTE st1(101, 101)
;
-- ok

-- test column privileges with MERGE
SET SESSION AUTHORIZATION regress_priv_user1
;
LOCK TABLE "public"."customer" IN ACCESS SHARE MODE
;
COPY pg_catalog.pg_class (oid, relname, relnamespace, reltype, reloftype, relowner, relam, relfilenode, reltablespace, relpages, reltuples, relallvisible, relallfrozen, reltoastrelid, relhasindex, relisshared, relpersistence, relkind, relnatts, relchecks, relhasrules, relhastriggers, relhassubclass, relrowsecurity, relforcerowsecurity, relispopulated, relreplident, relispartition, relrewrite, relfrozenxid, relminmxid, relacl, reloptions, relpartbound) TO stdout
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] REASSIGN OWNED BY CURRENT_USER TO split_owned_receiver
;
----------
[DATA_EXPORT] COPY public.test_tab (a, b, c, d) TO STDOUT
;
----------
[UNSAFE] EXECUTE dumpTableAttach('37624')
;
----------
[SESSION_LOCK] LOCK TABLE public.bug_16784 IN ACCESS SHARE MODE
;
----------
[UNSAFE] PREPARE getrow_seqscan AS SELECT * FROM test_dc WHERE data = 34 ORDER BY id,data
;
----------
[UNSAFE] EXECUTE dumpAgg('16778')
;
----------
[UNSAFE] EXECUTE st1(101, 101)
;
----------
[SWITCH_USER] -- ok

-- test column privileges with MERGE
SET SESSION AUTHORIZATION regress_priv_user1
;
----------
[SESSION_LOCK] LOCK TABLE "public"."customer" IN ACCESS SHARE MODE
;
----------
[DATA_EXPORT] COPY pg_catalog.pg_class (oid, relname, relnamespace, reltype, reloftype, relowner, relam, relfilenode, reltablespace, relpages, reltuples, relallvisible, relallfrozen, reltoastrelid, relhasindex, relisshared, relpersistence, relkind, relnatts, relchecks, relhasrules, relhastriggers, relhassubclass, relrowsecurity, relforcerowsecurity, relispopulated, relreplident, relispartition, relrewrite, relfrozenxid, relminmxid, relacl, reloptions, relpartbound) TO stdout
;
