CREATE TABLE gtest20a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED)
;
-- NOT VALID foreign keys on partitioned table
CREATE TABLE fk_notpartitioned_pk (a int, b int, PRIMARY KEY (a, b))
;
DROP TABLE attmp_array
;
CREATE FUNCTION btean13cmp(ean13, upc)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE SCHEMA audit_tbls authorization regress_evt_user
;
-- error
CREATE TABLE cmdata3(f1 text)
;
create foreign table bar2 (f3 int) inherits (bar)
  server loopback options (table_name 'loct2')
;
ALTER TABLE pg_toast.pg_toast_30784 RENAME TO reind_con_toast
;
ALTER OPERATOR FAMILY gist_uuid_ops USING gist ADD
	OPERATOR	6	<>  (uuid, uuid) ,
	FUNCTION	9 (uuid, uuid) gbt_uuid_fetch (internal)
;
ALTER TEXT SEARCH CONFIGURATION danish ADD MAPPING
    FOR word, hword_part, hword
	WITH danish_stem
;
-- test support for dropped objects
CREATE SCHEMA schema_one authorization regress_evt_user
;
alter function whoami() security invoker
;
CREATE FUNCTION isnle(ismn13, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE test_int8 (
	i int8
)
;
ALTER FOREIGN TABLE ft1 DROP c11
;
DROP MATERIALIZED VIEW matview_ine_tab
;
ALTER TABLE bttest_multi SET (autovacuum_enabled = false)
;
CREATE TABLE ruletest_t2 (x int)
;
ALTER INDEX pg_toast.pg_toast_28079_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION hstore_to_matrix(hstore)
RETURNS text[]
AS '$libdir/hstore','hstore_to_matrix'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE UNLOGGED TABLE first_client_table (value integer)
;
create or replace function parted_trigfunc() returns trigger language plpgsql as $$
begin
  new.c = new.c || ' did '|| TG_OP;
  return new;
end;
$$
;
CREATE FUNCTION citext_ge( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
--
-- Ranges of composites
--

create type two_ints as (a int, b int)
;
CREATE TEMP TABLE t3 (x int, y int)
;
CREATE TABLE truncate_b (id int GENERATED ALWAYS AS IDENTITY (START WITH 44))
;
-- Test with row-level ON INSERT triggers
CREATE TRIGGER trig_row_before_insert
BEFORE INSERT ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
-- ************************************************************
-- * AFTER DELETE on PField
-- *	- Remove all slots of this patchfield
-- ************************************************************
create function tg_pfield_ad() returns trigger as '
begin
    delete from PSlot where pfname = old.name;
    return old;
end;
' language plpgsql
;
--
-- And the same for partitioned cases
--
create table inhpar(f1 int primary key, f2 name) partition by range (f1)
;
DROP VIEW view_stats
;
CREATE FUNCTION gin_extract_value_time(time, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
--
-- ATTACH PARTITION
--

-- check that target table is partitioned
CREATE TABLE unparted (
	a int
)
;
VACUUM (FULL) pg_catalog.pg_publication_rel
;
-- a simpler multi-column keys case
create table mc2p (a int, b int) partition by range (a, b)
;
ALTER TABLE ONLY "public"."prt2_l" ATTACH PARTITION "public"."prt2_l_p1" FOR VALUES FROM (0) TO (250)
;
CREATE TABLE s (js jsonb)
;
CREATE FUNCTION gin_extract_value_bytea(bytea, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE ONLY "public"."tt7" DROP COLUMN "........pg.dropped.2........"
;
CREATE FUNCTION "public"."int8alias1eq"("public"."int8alias1", "public"."int8alias1") RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8eq$$
;
CREATE TABLE split_owned_schema.source_table (id integer)
;
CREATE TABLE hpart_1 PARTITION OF hash_parted FOR VALUES WITH (MODULUS 10, REMAINDER 0)
;
CREATE INDEX srt_name ON srt (name)
;
CREATE TABLE test_missing_target (a int, b int, c char(8), d char)
;
create index on idxpart ((b + 1))
;
ALTER DATABASE "regression" SET "lc_time" TO 'C'
;
ALTER TABLE fd_pt1 ALTER COLUMN c8 TYPE char(10) USING '0'
;
CREATE FUNCTION "public"."equipment_named_ambiguous_1c"("hobby" "public"."hobbies_r") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where hobby = hobby.name$$
;
-- ALTER INDEX .. SET
ALTER INDEX dummy_test_idx SET (option_int = 10)
;
CREATE TABLE "public"."trigger_parted" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE gtest20a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_TABLE] -- NOT VALID foreign keys on partitioned table
CREATE TABLE fk_notpartitioned_pk (a int, b int, PRIMARY KEY (a, b))
;
----------
[DROP_TABLE] DROP TABLE attmp_array
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btean13cmp(ean13, upc)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA audit_tbls authorization regress_evt_user
;
----------
[CREATE_TABLE] -- error
CREATE TABLE cmdata3(f1 text)
;
----------
[CREATE_TABLE] create foreign table bar2 (f3 int) inherits (bar)
  server loopback options (table_name 'loct2')
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30784 RENAME TO reind_con_toast
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_uuid_ops USING gist ADD
	OPERATOR	6	<>  (uuid, uuid) ,
	FUNCTION	9 (uuid, uuid) gbt_uuid_fetch (internal)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION danish ADD MAPPING
    FOR word, hword_part, hword
	WITH danish_stem
;
----------
[CREATE_SCHEMA] -- test support for dropped objects
CREATE SCHEMA schema_one authorization regress_evt_user
;
----------
[ALTER_PROG_OBJ] alter function whoami() security invoker
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ismn13, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test_int8 (
	i int8
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE ft1 DROP c11
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW matview_ine_tab
;
----------
[ALTER_TABLE] ALTER TABLE bttest_multi SET (autovacuum_enabled = false)
;
----------
[CREATE_TABLE] CREATE TABLE ruletest_t2 (x int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28079_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_to_matrix(hstore)
RETURNS text[]
AS '$libdir/hstore','hstore_to_matrix'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE first_client_table (value integer)
;
----------
[CREATE_PROG_OBJ] create or replace function parted_trigfunc() returns trigger language plpgsql as $$
begin
  new.c = new.c || ' did '|| TG_OP;
  return new;
end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_ge( citext, citext )
RETURNS bool
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TYPE] --
-- Ranges of composites
--

create type two_ints as (a int, b int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE t3 (x int, y int)
;
----------
[CREATE_TABLE] CREATE TABLE truncate_b (id int GENERATED ALWAYS AS IDENTITY (START WITH 44))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Test with row-level ON INSERT triggers
CREATE TRIGGER trig_row_before_insert
BEFORE INSERT ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * AFTER DELETE on PField
-- *	- Remove all slots of this patchfield
-- ************************************************************
create function tg_pfield_ad() returns trigger as '
begin
    delete from PSlot where pfname = old.name;
    return old;
end;
' language plpgsql
;
----------
[CREATE_TABLE] --
-- And the same for partitioned cases
--
create table inhpar(f1 int primary key, f2 name) partition by range (f1)
;
----------
[DROP_VIEW] DROP VIEW view_stats
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_time(time, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
----------
[CREATE_TABLE] --
-- ATTACH PARTITION
--

-- check that target table is partitioned
CREATE TABLE unparted (
	a int
)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_publication_rel
;
----------
[CREATE_TABLE] -- a simpler multi-column keys case
create table mc2p (a int, b int) partition by range (a, b)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_l" ATTACH PARTITION "public"."prt2_l_p1" FOR VALUES FROM (0) TO (250)
;
----------
[CREATE_TABLE] CREATE TABLE s (js jsonb)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_bytea(bytea, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."tt7" DROP COLUMN "........pg.dropped.2........"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int8alias1eq"("public"."int8alias1", "public"."int8alias1") RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8eq$$
;
----------
[CREATE_TABLE] CREATE TABLE split_owned_schema.source_table (id integer)
;
----------
[CREATE_TABLE] CREATE TABLE hpart_1 PARTITION OF hash_parted FOR VALUES WITH (MODULUS 10, REMAINDER 0)
;
----------
[ADD_INDEX] CREATE INDEX srt_name ON srt (name)
;
----------
[CREATE_TABLE] CREATE TABLE test_missing_target (a int, b int, c char(8), d char)
;
----------
[ADD_INDEX] create index on idxpart ((b + 1))
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET "lc_time" TO 'C'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c8 TYPE char(10) USING '0'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."equipment_named_ambiguous_1c"("hobby" "public"."hobbies_r") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where hobby = hobby.name$$
;
----------
[ALTER_INDEX] -- ALTER INDEX .. SET
ALTER INDEX dummy_test_idx SET (option_int = 10)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
