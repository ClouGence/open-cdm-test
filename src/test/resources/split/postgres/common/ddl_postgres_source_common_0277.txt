ANALYZE public.prt2_n_p2
;
ALTER TABLE gtest21b ALTER COLUMN b SET NOT NULL
;
CREATE FUNCTION gbt_var_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION time_dist(time, time)
RETURNS interval
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create table idxpart1 (a int not null, b int, unique (a, b))
  partition by range (a, b)
;
-- add a unique constraint
alter table atacc1 add constraint atacc_test1 unique (test, test2)
;
CREATE RULE rule2 AS ON UPDATE TO rule_merge1
	DO INSTEAD UPDATE rule_merge2 SET a = NEW.a, b = NEW.b
	WHERE a = OLD.a
;
ANALYZE public.bt_i4_heap
;
CREATE FUNCTION "public"."parted_trigfunc2"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
  new.a = new.a + 1;
  return new;
end;
$$
;
COMMENT ON RULE rtest_v1_del ON rtest_v1 IS 'delete rule'
;
--
-- pg_get_wal_stats_till_end_of_wal()
--
CREATE FUNCTION pg_get_wal_stats_till_end_of_wal(IN start_lsn pg_lsn,
    IN  per_record boolean DEFAULT false,
    OUT "resource_manager/record_type" text,
    OUT count int8,
    OUT count_percentage float8,
    OUT record_size int8,
    OUT record_size_percentage float8,
    OUT fpi_size int8,
    OUT fpi_size_percentage float8,
    OUT combined_size int8,
    OUT combined_size_percentage float8
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_stats_till_end_of_wal'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP TRIGGER instead_of_insert_trig ON main_view
;
ALTER TABLE fkpart11.fk ADD CONSTRAINT fkey FOREIGN KEY (a) REFERENCES fkpart11.pk1 (a) ON UPDATE CASCADE ON DELETE CASCADE
;
ALTER TABLE ONLY "public"."gtest20b"
    ADD CONSTRAINT "gtest20b_pkey" PRIMARY KEY ("a")
;
CREATE INDEX concur_clustered_i_idx ON concur_clustered(i)
;
-- Negative scenarios for alter constraint .. inherit.
create table inh_nn1 (f1 int check(f1 > 5) primary key references inh_nn1, f2 int not null)
;
CREATE TABLE trigger_test (f1 int, f2 text, f3 text)
;
CREATE OR REPLACE FUNCTION trigger_data()  RETURNS trigger
LANGUAGE plpgsql AS $$

declare

	argstr text;
	relid text;

begin

	relid := TG_relid::regclass;

	-- plpgsql can't discover its trigger data in a hash like perl and python
	-- can, or by a sort of reflection like tcl can,
	-- so we have to hard code the names.
	raise NOTICE 'TG_NAME: %', TG_name;
	raise NOTICE 'TG_WHEN: %', TG_when;
	raise NOTICE 'TG_LEVEL: %', TG_level;
	raise NOTICE 'TG_OP: %', TG_op;
	raise NOTICE 'TG_RELID::regclass: %', relid;
	raise NOTICE 'TG_RELNAME: %', TG_relname;
	raise NOTICE 'TG_TABLE_NAME: %', TG_table_name;
	raise NOTICE 'TG_TABLE_SCHEMA: %', TG_table_schema;
	raise NOTICE 'TG_NARGS: %', TG_nargs;

	argstr := '[';
	for i in 0 .. TG_nargs - 1 loop
		if i > 0 then
			argstr := argstr || ', ';
		end if;
		argstr := argstr || TG_argv[i];
	end loop;
	argstr := argstr || ']';
	raise NOTICE 'TG_ARGV: %', argstr;

	if TG_OP != 'INSERT' then
		raise NOTICE 'OLD: %', OLD;
	end if;

	if TG_OP != 'DELETE' then
		raise NOTICE 'NEW: %', NEW;
	end if;

	if TG_OP = 'DELETE' then
		return OLD;
	else
		return NEW;
	end if;

end;
$$
;
CREATE DATABASE targetdb
;
CREATE INDEX CONCURRENTLY on cic_test(a)
;
CREATE TABLE collate_test_fail (
    a int,
    b text COLLATE "foo-x-icu"
)
;
drop table mcrparted
;
DROP VIEW jsonb_table_view5
;
-- should be able to add a table of the same schema to the schema publication
ALTER PUBLICATION testpub_forschema ADD TABLE pub_test.testpub_nopk
;
create temp table t_test_trgm(t text COLLATE "C")
;
create table parted_conflict_1 partition of parted_conflict for values from (0) to (1000) partition by range (a)
;
VACUUM (ANALYZE) maintain_test
;
VACUUM (FREEZE) pg_catalog.pg_class
;
ANALYZE pg_temp_10.pg_temp_26444_2
;
ALTER TABLE pg_toast.pg_toast_28385 RENAME TO reind_con_toast
;
-- Remove function and aggregate from extension
alter extension postgres_fdw drop function least_accum(anyelement, variadic anyarray)
;
CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=18417 host=/tmp/jotseMAL4u dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
CREATE TEMPORARY TABLE pgss_ctas_4 AS EXECUTE test_prepare_pgss
;
CREATE TRIGGER c BEFORE INSERT ON a FOR EACH ROW EXECUTE PROCEDURE b()
;
CREATE TABLE pgss_schema_1.tab_search_same (a int, b int)
;
ALTER TABLE pg_toast.pg_toast_31025 RENAME TO reind_con_toast
;
-- test python -> hstore
CREATE FUNCTION test2(a int, b text) RETURNS hstore
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
val = {'a': a, 'b': b, 'c': None}
return val
$$
;
DROP OPERATOR CLASS IF EXISTS split_operator_probe_next.int_hash_ops_renamed USING hash RESTRICT
;
drop table vc4table
;
DROP TABLE pg_temp_3.pg_temp_27379_2, pg_temp_3.pg_temp_27379
;
ALTER TABLE not_leftmost ALTER c SET STORAGE PLAIN
;
CREATE FOREIGN TABLE tru_ftable (id int)
       SERVER loopback OPTIONS (table_name 'tru_rtable0')
;
ALTER FOREIGN TABLE ft1 ADD COLUMN c8 integer
;
-- dump trigger data
CREATE TABLE trigger_test (
        i int,
        v varchar
)
;
CREATE TABLE "public"."prt2_n_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
VACUUM (FULL) pg_catalog.pg_enum
;
COMMENT ON TEXT SEARCH CONFIGURATION lithuanian IS 'configuration for lithuanian language'
;
-- Event triggers on relations.
CREATE OR REPLACE FUNCTION event_trigger_report_dropped()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r record;
BEGIN
    FOR r IN SELECT * from pg_event_trigger_dropped_objects()
    LOOP
    IF NOT r.normal AND NOT r.original THEN
        CONTINUE;
    END IF;
    RAISE NOTICE 'NORMAL: orig=% normal=% istemp=% type=% identity=% name=% args=%',
        r.original, r.normal, r.is_temporary, r.object_type,
        r.object_identity, r.address_names, r.address_args;
    END LOOP;
END; $$
;
CREATE INDEX clstr_tst_c_b ON clstr_tst (c,b)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE public.prt2_n_p2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest21b ALTER COLUMN b SET NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_var_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION time_dist(time, time)
RETURNS interval
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] create table idxpart1 (a int not null, b int, unique (a, b))
  partition by range (a, b)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a unique constraint
alter table atacc1 add constraint atacc_test1 unique (test, test2)
;
----------
[CREATE_POLICY] CREATE RULE rule2 AS ON UPDATE TO rule_merge1
	DO INSTEAD UPDATE rule_merge2 SET a = NEW.a, b = NEW.b
	WHERE a = OLD.a
;
----------
[ADMIN_TABLE] ANALYZE public.bt_i4_heap
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."parted_trigfunc2"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
  new.a = new.a + 1;
  return new;
end;
$$
;
----------
[ALTER_POLICY] COMMENT ON RULE rtest_v1_del ON rtest_v1 IS 'delete rule'
;
----------
[CREATE_PROG_OBJ] --
-- pg_get_wal_stats_till_end_of_wal()
--
CREATE FUNCTION pg_get_wal_stats_till_end_of_wal(IN start_lsn pg_lsn,
    IN  per_record boolean DEFAULT false,
    OUT "resource_manager/record_type" text,
    OUT count int8,
    OUT count_percentage float8,
    OUT record_size int8,
    OUT record_size_percentage float8,
    OUT fpi_size int8,
    OUT fpi_size_percentage float8,
    OUT combined_size int8,
    OUT combined_size_percentage float8
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_stats_till_end_of_wal'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_TRIGGER] DROP TRIGGER instead_of_insert_trig ON main_view
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fkpart11.fk ADD CONSTRAINT fkey FOREIGN KEY (a) REFERENCES fkpart11.pk1 (a) ON UPDATE CASCADE ON DELETE CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest20b"
    ADD CONSTRAINT "gtest20b_pkey" PRIMARY KEY ("a")
;
----------
[ADD_INDEX] CREATE INDEX concur_clustered_i_idx ON concur_clustered(i)
;
----------
[CREATE_TABLE] -- Negative scenarios for alter constraint .. inherit.
create table inh_nn1 (f1 int check(f1 > 5) primary key references inh_nn1, f2 int not null)
;
----------
[CREATE_TABLE] CREATE TABLE trigger_test (f1 int, f2 text, f3 text)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION trigger_data()  RETURNS trigger
LANGUAGE plpgsql AS $$

declare

	argstr text;
	relid text;

begin

	relid := TG_relid::regclass;

	-- plpgsql can't discover its trigger data in a hash like perl and python
	-- can, or by a sort of reflection like tcl can,
	-- so we have to hard code the names.
	raise NOTICE 'TG_NAME: %', TG_name;
	raise NOTICE 'TG_WHEN: %', TG_when;
	raise NOTICE 'TG_LEVEL: %', TG_level;
	raise NOTICE 'TG_OP: %', TG_op;
	raise NOTICE 'TG_RELID::regclass: %', relid;
	raise NOTICE 'TG_RELNAME: %', TG_relname;
	raise NOTICE 'TG_TABLE_NAME: %', TG_table_name;
	raise NOTICE 'TG_TABLE_SCHEMA: %', TG_table_schema;
	raise NOTICE 'TG_NARGS: %', TG_nargs;

	argstr := '[';
	for i in 0 .. TG_nargs - 1 loop
		if i > 0 then
			argstr := argstr || ', ';
		end if;
		argstr := argstr || TG_argv[i];
	end loop;
	argstr := argstr || ']';
	raise NOTICE 'TG_ARGV: %', argstr;

	if TG_OP != 'INSERT' then
		raise NOTICE 'OLD: %', OLD;
	end if;

	if TG_OP != 'DELETE' then
		raise NOTICE 'NEW: %', NEW;
	end if;

	if TG_OP = 'DELETE' then
		return OLD;
	else
		return NEW;
	end if;

end;
$$
;
----------
[CREATE_CATALOG] CREATE DATABASE targetdb
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY on cic_test(a)
;
----------
[CREATE_TABLE] CREATE TABLE collate_test_fail (
    a int,
    b text COLLATE "foo-x-icu"
)
;
----------
[DROP_TABLE] drop table mcrparted
;
----------
[DROP_VIEW] DROP VIEW jsonb_table_view5
;
----------
[ALTER_PUB_SUB] -- should be able to add a table of the same schema to the schema publication
ALTER PUBLICATION testpub_forschema ADD TABLE pub_test.testpub_nopk
;
----------
[CREATE_TABLE] create temp table t_test_trgm(t text COLLATE "C")
;
----------
[CREATE_TABLE] create table parted_conflict_1 partition of parted_conflict for values from (0) to (1000) partition by range (a)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) maintain_test
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_class
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26444_2
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28385 RENAME TO reind_con_toast
;
----------
[ALTER_LIBRARY] -- Remove function and aggregate from extension
alter extension postgres_fdw drop function least_accum(anyelement, variadic anyarray)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=18417 host=/tmp/jotseMAL4u dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
----------
[CREATE_TABLE|UNSAFE] CREATE TEMPORARY TABLE pgss_ctas_4 AS EXECUTE test_prepare_pgss
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER c BEFORE INSERT ON a FOR EACH ROW EXECUTE PROCEDURE b()
;
----------
[CREATE_TABLE] CREATE TABLE pgss_schema_1.tab_search_same (a int, b int)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_31025 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] -- test python -> hstore
CREATE FUNCTION test2(a int, b text) RETURNS hstore
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
val = {'a': a, 'b': b, 'c': None}
return val
$$
;
----------
[DROP_PROG_OBJ] DROP OPERATOR CLASS IF EXISTS split_operator_probe_next.int_hash_ops_renamed USING hash RESTRICT
;
----------
[DROP_TABLE] drop table vc4table
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_27379_2, pg_temp_3.pg_temp_27379
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE not_leftmost ALTER c SET STORAGE PLAIN
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tru_ftable (id int)
       SERVER loopback OPTIONS (table_name 'tru_rtable0')
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD COLUMN c8 integer
;
----------
[CREATE_TABLE] -- dump trigger data
CREATE TABLE trigger_test (
        i int,
        v varchar
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_n_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_enum
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION lithuanian IS 'configuration for lithuanian language'
;
----------
[CREATE_PROG_OBJ] -- Event triggers on relations.
CREATE OR REPLACE FUNCTION event_trigger_report_dropped()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r record;
BEGIN
    FOR r IN SELECT * from pg_event_trigger_dropped_objects()
    LOOP
    IF NOT r.normal AND NOT r.original THEN
        CONTINUE;
    END IF;
    RAISE NOTICE 'NORMAL: orig=% normal=% istemp=% type=% identity=% name=% args=%',
        r.original, r.normal, r.is_temporary, r.object_type,
        r.object_identity, r.address_names, r.address_args;
    END LOOP;
END; $$
;
----------
[ADD_INDEX] CREATE INDEX clstr_tst_c_b ON clstr_tst (c,b)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
