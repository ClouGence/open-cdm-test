DROP DOMAIN rgb
;
CREATE TABLE test2 (a int, b text)
;
ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f2_key UNIQUE (f2)
;
CREATE FUNCTION alter_op_test_fn_bool_real(boolean, real)
RETURNS boolean AS $$ SELECT NULL::BOOLEAN; $$ LANGUAGE sql IMMUTABLE
;
ALTER TABLE pitest2 ADD COLUMN f3 int GENERATED ALWAYS AS IDENTITY
;
DROP TABLE hash_temp_heap CASCADE
;
DROP SUBSCRIPTION testsub2
;
ALTER FUNCTION alt_func2(int) SET SCHEMA alt_nsp1
;
alter table idxpart attach partition idxpart2 for values from (1000) to (2000)
;
CREATE TABLE parted_no_parts (a int) PARTITION BY LIST (a)
;
create function explain_sq_limit() returns setof text language plpgsql as
$$
declare ln text;
begin
    for ln in
        explain (analyze, summary off, timing off, costs off, buffers off)
        select * from (select pk,c2 from sq_limit order by c1,pk) as x limit 3
    loop
        ln := regexp_replace(ln, 'Memory: \S*',  'Memory: xxx');
        return next ln;
    end loop;
end;
$$
;
CREATE TABLE inh_test () INHERITS (part_2)
;
CREATE VIEW pg_stat_xact_sys_tables AS
    SELECT * FROM pg_stat_xact_all_tables
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
-- Test ON COMMIT DELETE ROWS

CREATE TEMP TABLE temptest(col int) ON COMMIT DELETE ROWS
;
-- works
DROP INDEX CONCURRENTLY IF EXISTS "concur_index2"
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
CREATE INDEX flt_f_idx ON flt (f)
;
CREATE SCHEMA split_agg_probe
;
CREATE TABLE beta_neg_p2 PARTITION OF beta_neg FOR VALUES FROM (200) TO (300)
;
create rule rules_foorule as on insert to rules_foo where f1 < 100
do instead insert into rules_foo2 values (new.f1)
;
ALTER INDEX pg_toast.pg_toast_33588_index RENAME TO reind_con_toast_idx
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_am
;
ALTER TABLE pg_toast.pg_toast_28478 RENAME TO reind_con_toast
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
-- fail
DROP TYPE myvarchar
;
CREATE PUBLICATION tap_pub
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub_mix_3, pub_mix_4
;
ALTER FUNCTION gbt_int2_consistent(internal, smallint, smallint, oid, internal) PARALLEL SAFE
;
CREATE SCHEMA IF NOT EXISTS newschema3 AUTHORIZATION SESSION_USER
;
CREATE TEMP TABLE pg_temp_26.pg_temp_28795_2 (tid pg_catalog.tid)
;
CREATE AGGREGATE myaggp14a(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
-- but it's OK when the identity is FULL
ALTER TABLE test_replica_identity3 REPLICA IDENTITY FULL
;
CREATE TRIGGER "transition_table_level1_ri_parent_del_trigger" AFTER DELETE ON "public"."transition_table_level1" REFERENCING OLD TABLE AS "p" FOR EACH STATEMENT EXECUTE FUNCTION "public"."transition_table_level1_ri_parent_del_func"()
;
ALTER OPERATOR ^@> (ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
ALTER FUNCTION gbt_ts_union(internal, internal) PARALLEL SAFE
;
CREATE TABLE gtest31_2 (x int, y gtest31_1)
;
DROP TABLE s1_temp_schema
;
create table pp_enumpart_green partition of pp_enumpart for values in ('green')
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_event_trigger
;
--    P    N        P    P
-- should ERROR: tf2p(anyarray, anyelement) not matched by tf2p(int[],anyelement)
CREATE AGGREGATE myaggn16a(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
-- a RETURN expression is mandatory, except for void-returning
-- functions, where it is not allowed
create function missing_return_expr() returns int as $$
begin
    return ;
end;$$ language plpgsql
;
create table phv_part_2 partition of phv_part for values in (2)
;
DROP INDEX regtest_index_tbl4_y
;
TRUNCATE temporal_rng, temporal_fk_rng2rng
;
create index on only idxpart2 (a)
;
create index idxpart1_tst3 on idxpart1 (a, b) where a > 10
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_shdescription
;
ALTER OPERATOR FAMILY gist_macaddr_ops USING gist ADD
	OPERATOR	6	<> (macaddr, macaddr) ,
	FUNCTION	9 (macaddr, macaddr) gbt_macad_fetch (internal)
;
CREATE TABLE tenant_table (i integer)
;
------------------------------------------------------------------------------------------
[DROP_TYPE] DROP DOMAIN rgb
;
----------
[CREATE_TABLE] CREATE TABLE test2 (a int, b text)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f2_key UNIQUE (f2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION alter_op_test_fn_bool_real(boolean, real)
RETURNS boolean AS $$ SELECT NULL::BOOLEAN; $$ LANGUAGE sql IMMUTABLE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pitest2 ADD COLUMN f3 int GENERATED ALWAYS AS IDENTITY
;
----------
[DROP_TABLE] DROP TABLE hash_temp_heap CASCADE
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION testsub2
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION alt_func2(int) SET SCHEMA alt_nsp1
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart2 for values from (1000) to (2000)
;
----------
[CREATE_TABLE] CREATE TABLE parted_no_parts (a int) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] create function explain_sq_limit() returns setof text language plpgsql as
$$
declare ln text;
begin
    for ln in
        explain (analyze, summary off, timing off, costs off, buffers off)
        select * from (select pk,c2 from sq_limit order by c1,pk) as x limit 3
    loop
        ln := regexp_replace(ln, 'Memory: \S*',  'Memory: xxx');
        return next ln;
    end loop;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE inh_test () INHERITS (part_2)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_xact_sys_tables AS
    SELECT * FROM pg_stat_xact_all_tables
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
----------
[CREATE_TABLE] -- Test ON COMMIT DELETE ROWS

CREATE TEMP TABLE temptest(col int) ON COMMIT DELETE ROWS
;
----------
[DROP_INDEX] -- works
DROP INDEX CONCURRENTLY IF EXISTS "concur_index2"
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
----------
[ADD_INDEX] CREATE INDEX flt_f_idx ON flt (f)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_agg_probe
;
----------
[CREATE_TABLE] CREATE TABLE beta_neg_p2 PARTITION OF beta_neg FOR VALUES FROM (200) TO (300)
;
----------
[CREATE_POLICY] create rule rules_foorule as on insert to rules_foo where f1 < 100
do instead insert into rules_foo2 values (new.f1)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33588_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_am
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28478 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
----------
[DROP_TYPE] -- fail
DROP TYPE myvarchar
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub_mix_3, pub_mix_4
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int2_consistent(internal, smallint, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema3 AUTHORIZATION SESSION_USER
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_26.pg_temp_28795_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp14a(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ALTER_TABLE] -- but it's OK when the identity is FULL
ALTER TABLE test_replica_identity3 REPLICA IDENTITY FULL
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "transition_table_level1_ri_parent_del_trigger" AFTER DELETE ON "public"."transition_table_level1" REFERENCING OLD TABLE AS "p" FOR EACH STATEMENT EXECUTE FUNCTION "public"."transition_table_level1_ri_parent_del_func"()
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^@> (ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_ts_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE gtest31_2 (x int, y gtest31_1)
;
----------
[DROP_TABLE] DROP TABLE s1_temp_schema
;
----------
[CREATE_TABLE] create table pp_enumpart_green partition of pp_enumpart for values in ('green')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_event_trigger
;
----------
[CREATE_PROG_OBJ] --    P    N        P    P
-- should ERROR: tf2p(anyarray, anyelement) not matched by tf2p(int[],anyelement)
CREATE AGGREGATE myaggn16a(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] -- a RETURN expression is mandatory, except for void-returning
-- functions, where it is not allowed
create function missing_return_expr() returns int as $$
begin
    return ;
end;$$ language plpgsql
;
----------
[CREATE_TABLE] create table phv_part_2 partition of phv_part for values in (2)
;
----------
[DROP_INDEX] DROP INDEX regtest_index_tbl4_y
;
----------
[TRUNCATE_TABLE] TRUNCATE temporal_rng, temporal_fk_rng2rng
;
----------
[ADD_INDEX] create index on only idxpart2 (a)
;
----------
[ADD_INDEX] create index idxpart1_tst3 on idxpart1 (a, b) where a > 10
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_shdescription
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_macaddr_ops USING gist ADD
	OPERATOR	6	<> (macaddr, macaddr) ,
	FUNCTION	9 (macaddr, macaddr) gbt_macad_fetch (internal)
;
----------
[CREATE_TABLE] CREATE TABLE tenant_table (i integer)
;
