CREATE TABLE "S 1"."T 2" (
	c1 int NOT NULL,
	c2 text,
	CONSTRAINT t2_pkey PRIMARY KEY (c1)
)
;
ALTER EXTENSION test_pg_dump DROP TABLE test_pg_dump_t1
;
ALTER CONVERSION split_conversion_probe.latin1_to_utf8_probe RENAME TO latin1_to_utf8_renamed
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
-- Try a join case

CREATE TEMP TABLE joinme (f2j text, other int)
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest20b"
    ADD CONSTRAINT "gtest20b_pkey" PRIMARY KEY ("a")
;
CREATE POLICY p1 ON rls_tbl USING (c1 > 5)
;
-- Runtime pruning on UPDATE using WITH CHECK OPTIONS and RETURNING
create table part_abc (a int, b text, c bool) partition by list (a)
;
ALTER DOMAIN split_domain_type_next.email_domain RENAME CONSTRAINT email_length_chk TO email_len_chk
;
CREATE TEMP TABLE test1 (a int, b int)
;
CREATE TYPE split_alter_table.typed_row AS (id integer, note text)
;
-- Permission tests. Users should not be able to see specific data values in
-- the extended statistics, if they lack permission to see those values in
-- the underlying table.
--
-- Currently this is only relevant for MCV stats.
CREATE SCHEMA tststats
;
ALTER TABLE ONLY "generated_stored_tests"."gtest22a"
    ADD CONSTRAINT "gtest22a_pkey" PRIMARY KEY ("a")
;
-- test building an index with many values, to force compaction of the buffer
CREATE TABLE brin_large_range (a int4)
;
CREATE TABLE gtest_normal_child (a int, b int GENERATED ALWAYS AS (a * 2) STORED) INHERITS (gtest_normal)
;
CREATE OPERATOR CLASS bool_ops
DEFAULT FOR TYPE bool USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btboolcmp(bool,bool),
    FUNCTION        2       gin_extract_value_bool(bool, internal),
    FUNCTION        3       gin_extract_query_bool(bool, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_bool(bool,bool,int2, internal),
STORAGE         bool
;
DROP FUNCTION PLUS_THREE(INTEGER)
;
CREATE TABLE "public"."gtest_child2" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 22)) STORED
)
;
DROP SEQUENCE uv_seq CASCADE
;
create table hp_prefix_test_p3 partition of hp_prefix_test for values with (modulus 8, remainder 3)
;
-- 2 distinct combinations with NULL values, all in the MCV list
TRUNCATE mcv_lists
;
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (-1) TO (1)
;
drop domain insert_pos_ints
;
DROP INDEX intarrayidx, textarrayidx
;
CREATE TABLE committs_test(id serial, ts timestamptz default now())
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_inherits
;
-- check if default partition scan skipped
ALTER TABLE list_parted2_def ADD CONSTRAINT check_a CHECK (a IN (5, 6))
;
CREATE INDEX bttest_b_idx ON bttest_b USING btree (id)
;
create index on func_index_heap ((f1 || f2), (row(f1, f2)))
;
create temp table temp_inh_oncommit_test (a int) on commit drop
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
drop index key_index
;
alter type insenum add value 'i14' before 'L2'
;
-- column defaults

CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified', c serial)
;
CREATE FUNCTION isnne(ean13, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP TYPE rainbow
;
CREATE MATERIALIZED VIEW mvtest_mv_v_3 (ii, jj) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
create table rtest_unitfact (
	unit	char(4),
	factor	float
)
;
alter table alterlock alter column f2 set (n_distinct = 1)
;
ALTER TEXT SEARCH CONFIGURATION serbian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
create index on idxpart (a text_pattern_ops)
;
create table trigger_parted_p1_1 partition of trigger_parted_p1 for values in (1)
;
-- if we pretend it's stable, we get different results:
alter function tattle(x int, y int) stable
;
DROP POLICY p1r ON document
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) information_schema.sql_parts
;
-- Since we can't actually see the thrown SQLSTATE in default psql output,
-- test it like this; this also tests re-RAISE

create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = 'division_by_zero', detail = 'some detail info';
  exception
    when others then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
/* Now redefine */
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_9'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
CREATE FUNCTION op_leak(int, int) RETURNS bool
    AS 'BEGIN RAISE NOTICE ''op_leak => %, %'', $1, $2; RETURN $1 < $2; END'
    LANGUAGE plpgsql
;
CREATE TABLE trunc_d (a int REFERENCES trunc_c)
;
-- fail - streaming must be boolean or 'parallel'
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = foo)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "S 1"."T 2" (
	c1 int NOT NULL,
	c2 text,
	CONSTRAINT t2_pkey PRIMARY KEY (c1)
)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP TABLE test_pg_dump_t1
;
----------
[ALTER_POLICY] ALTER CONVERSION split_conversion_probe.latin1_to_utf8_probe RENAME TO latin1_to_utf8_renamed
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
----------
[CREATE_TABLE] -- Try a join case

CREATE TEMP TABLE joinme (f2j text, other int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest20b"
    ADD CONSTRAINT "gtest20b_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON rls_tbl USING (c1 > 5)
;
----------
[CREATE_TABLE] -- Runtime pruning on UPDATE using WITH CHECK OPTIONS and RETURNING
create table part_abc (a int, b text, c bool) partition by list (a)
;
----------
[RENAME_CONSTRAINT] ALTER DOMAIN split_domain_type_next.email_domain RENAME CONSTRAINT email_length_chk TO email_len_chk
;
----------
[CREATE_TABLE] CREATE TEMP TABLE test1 (a int, b int)
;
----------
[CREATE_TYPE] CREATE TYPE split_alter_table.typed_row AS (id integer, note text)
;
----------
[CREATE_SCHEMA] -- Permission tests. Users should not be able to see specific data values in
-- the extended statistics, if they lack permission to see those values in
-- the underlying table.
--
-- Currently this is only relevant for MCV stats.
CREATE SCHEMA tststats
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest22a"
    ADD CONSTRAINT "gtest22a_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] -- test building an index with many values, to force compaction of the buffer
CREATE TABLE brin_large_range (a int4)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_normal_child (a int, b int GENERATED ALWAYS AS (a * 2) STORED) INHERITS (gtest_normal)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS bool_ops
DEFAULT FOR TYPE bool USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btboolcmp(bool,bool),
    FUNCTION        2       gin_extract_value_bool(bool, internal),
    FUNCTION        3       gin_extract_query_bool(bool, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_bool(bool,bool,int2, internal),
STORAGE         bool
;
----------
[DROP_PROG_OBJ] DROP FUNCTION PLUS_THREE(INTEGER)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest_child2" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 22)) STORED
)
;
----------
[DROP_SEQUENCE] DROP SEQUENCE uv_seq CASCADE
;
----------
[CREATE_TABLE] create table hp_prefix_test_p3 partition of hp_prefix_test for values with (modulus 8, remainder 3)
;
----------
[TRUNCATE_TABLE] -- 2 distinct combinations with NULL values, all in the MCV list
TRUNCATE mcv_lists
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (-1) TO (1)
;
----------
[DROP_TYPE] drop domain insert_pos_ints
;
----------
[DROP_INDEX] DROP INDEX intarrayidx, textarrayidx
;
----------
[CREATE_TABLE] CREATE TABLE committs_test(id serial, ts timestamptz default now())
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_inherits
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- check if default partition scan skipped
ALTER TABLE list_parted2_def ADD CONSTRAINT check_a CHECK (a IN (5, 6))
;
----------
[ADD_INDEX] CREATE INDEX bttest_b_idx ON bttest_b USING btree (id)
;
----------
[ADD_INDEX] create index on func_index_heap ((f1 || f2), (row(f1, f2)))
;
----------
[CREATE_TABLE] create temp table temp_inh_oncommit_test (a int) on commit drop
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
----------
[DROP_INDEX] drop index key_index
;
----------
[ALTER_TYPE] alter type insenum add value 'i14' before 'L2'
;
----------
[CREATE_TABLE] -- column defaults

CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified', c serial)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ean13, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TYPE] DROP TYPE rainbow
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv_v_3 (ii, jj) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
----------
[CREATE_TABLE] create table rtest_unitfact (
	unit	char(4),
	factor	float
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table alterlock alter column f2 set (n_distinct = 1)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION serbian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ADD_INDEX] create index on idxpart (a text_pattern_ops)
;
----------
[CREATE_TABLE] create table trigger_parted_p1_1 partition of trigger_parted_p1 for values in (1)
;
----------
[ALTER_PROG_OBJ] -- if we pretend it's stable, we get different results:
alter function tattle(x int, y int) stable
;
----------
[DROP_POLICY] DROP POLICY p1r ON document
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) information_schema.sql_parts
;
----------
[CREATE_PROG_OBJ] -- Since we can't actually see the thrown SQLSTATE in default psql output,
-- test it like this; this also tests re-RAISE

create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = 'division_by_zero', detail = 'some detail info';
  exception
    when others then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] /* Now redefine */
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_9'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION op_leak(int, int) RETURNS bool
    AS 'BEGIN RAISE NOTICE ''op_leak => %, %'', $1, $2; RETURN $1 < $2; END'
    LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE trunc_d (a int REFERENCES trunc_c)
;
----------
[CREATE_PUB_SUB] -- fail - streaming must be boolean or 'parallel'
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = foo)
;
