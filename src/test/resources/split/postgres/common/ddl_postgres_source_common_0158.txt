alter table tt7a drop column xx
;
CREATE TABLE ATACC3 (a int) INHERITS (ATACC2)
;
create or replace function returnsrecord(int) returns record language plpgsql as
$$ declare r record; begin r := row($1,$1+1)::has_hole; return r; end $$
;
CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE TABLE schema_not_existing.tab (id int)
;
DROP TABLE gtestm
;
alter table pg_class drop column relname
;
CREATE UNIQUE INDEX cwi_uniq4_idx ON cwi_test(b collate "POSIX")
;
ALTER TABLE pg_toast.pg_toast_28191 RENAME TO reind_con_toast
;
-- ******************************
-- * Test suite for the Postgres NUMERIC data type
-- ******************************

-- Must drop tables created by short numeric test.
DROP TABLE num_data
;
-- Check processing of foreign key triggers
create table parent (a int primary key, f int references parent)
  partition by list (a)
;
ALTER INDEX pg_toast.pg_toast_30796_index RENAME TO reind_con_toast_idx
;
ALTER OPERATOR FAMILY gist_timestamp_ops USING gist ADD
	FUNCTION	11  (timestamp, timestamp) gbt_ts_sortsupport (internal)
;
COMMENT ON TEXT SEARCH DICTIONARY tamil_stem IS 'snowball stemmer for tamil language'
;
DROP TRIGGER test_trigger_exists ON no_such_schema.no_such_table
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_policy
;
ALTER TABLE ONLY "fkpart3"."pk5" ATTACH PARTITION "fkpart3"."pk52" FOR VALUES FROM (4500) TO (5000)
;
ALTER TABLE ONLY "fkpart4"."droppk21"
    ADD CONSTRAINT "droppk21_pkey" PRIMARY KEY ("a")
;
-- leave this partition without children.
create table parted_index_col_drop2 partition of parted_index_col_drop
  for values in (2) partition by list (a)
;
DROP TABLE droppk21
;
create domain dnull    varchar(15)
;
ALTER FUNCTION gbt_inet_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
CREATE TABLE test_tab (a int primary key, b bytea, c INT, d INT, e INT)
;
-- tablespace should not change even if there is an index rewrite
ALTER TABLE testschema.test_default_tab ALTER id TYPE int
;
-- ROUTINE syntax

ALTER ROUTINE cp_testfunc1(int) RENAME TO cp_testfunc1a
;
CREATE TABLE tab_full_pk (a int primary key, b text)
;
create table t10()
;
create function tt14f() returns setof tt14t as
$$
declare
    rec1 record;
begin
    for rec1 in select * from tt14t
    loop
        return next rec1;
    end loop;
end;
$$
language plpgsql
;
ALTER TABLE pg_temp_26.pg_temp_28654_2 ADD COLUMN newdata pg_temp_26.pg_temp_28654
;
CREATE TABLE "fkpart3"."fk" (
    "a" integer
)
PARTITION BY RANGE ("a")
;
DROP INDEX onek_nulltest
;
-- accents
CREATE COLLATION ignore_accents (provider = icu, locale = '@colStrength=primary;colCaseLevel=yes', deterministic = false)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest23q"
    ADD CONSTRAINT "gtest23q_pkey" PRIMARY KEY ("a")
;
CREATE TABLE TEMP_FLOAT (f1 FLOAT8)
;
ALTER DATABASE "regression" SET bytea_output TO 'hex'
;
ALTER TABLE pg_toast.pg_toast_27856 RENAME TO reind_con_toast
;
CREATE FUNCTION isneq(ismn13, ismn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE test23_1 PARTITION OF test23 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
ALTER OPERATOR FAMILY gist_timestamptz_ops USING gist ADD
	OPERATOR	6	<> (timestamptz, timestamptz) ,
	OPERATOR	15	<-> (timestamptz, timestamptz) FOR ORDER BY pg_catalog.interval_ops ,
	FUNCTION	8 (timestamptz, timestamptz) gbt_tstz_distance (internal, timestamptz, int2, oid, internal) ,
	FUNCTION	9 (timestamptz, timestamptz) gbt_ts_fetch (internal)
;
drop function dom_check(int)
;
ANALYZE dupindexcols
;
CREATE VIEW pg_statio_user_tables AS
    SELECT * FROM pg_statio_all_tables
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema') AND
          schemaname !~ '^pg_toast'
;
CREATE TABLE public.tst_range_array (
		a INTEGER PRIMARY KEY,
		b TSTZRANGE,
		c int8range[]
	)
;
CREATE FUNCTION "public"."casttesttype_out"("public"."casttesttype") RETURNS "cstring"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$textout$$
;
-- cleanup
drop table list_parted
;
ALTER INDEX pg_toast.pg_toast_30394_index RENAME TO reind_con_toast_idx
;
-- create a tablespace using WITH clause
CREATE TABLESPACE regress_tblspacewith LOCATION '' WITH (some_nonexistent_parameter = true)
;
create or replace function func_part_create() returns trigger
  language plpgsql as $$
  begin
    execute 'create table tab_part_create_1 partition of tab_part_create for values in (1)';
    return null;
  end $$
;
CREATE TABLE POLYGON_TBL(f1 polygon)
;
-- fails, no way to deduce outputs
CREATE FUNCTION bad (f1 anyarray, out f2 anycompatible, out f3 anycompatiblearray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
--
-- INT4
--

CREATE TABLE INT4_TBL(f1 int4)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] alter table tt7a drop column xx
;
----------
[CREATE_TABLE] CREATE TABLE ATACC3 (a int) INHERITS (ATACC2)
;
----------
[CREATE_PROG_OBJ] create or replace function returnsrecord(int) returns record language plpgsql as
$$ declare r record; begin r := row($1,$1+1)::has_hole; return r; end $$
;
----------
[CREATE_SCHEMA] CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE TABLE schema_not_existing.tab (id int)
;
----------
[DROP_TABLE] DROP TABLE gtestm
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table pg_class drop column relname
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX cwi_uniq4_idx ON cwi_test(b collate "POSIX")
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28191 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] -- ******************************
-- * Test suite for the Postgres NUMERIC data type
-- ******************************

-- Must drop tables created by short numeric test.
DROP TABLE num_data
;
----------
[CREATE_TABLE] -- Check processing of foreign key triggers
create table parent (a int primary key, f int references parent)
  partition by list (a)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30796_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timestamp_ops USING gist ADD
	FUNCTION	11  (timestamp, timestamp) gbt_ts_sortsupport (internal)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY tamil_stem IS 'snowball stemmer for tamil language'
;
----------
[DROP_TRIGGER] DROP TRIGGER test_trigger_exists ON no_such_schema.no_such_table
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_policy
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."pk5" ATTACH PARTITION "fkpart3"."pk52" FOR VALUES FROM (4500) TO (5000)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart4"."droppk21"
    ADD CONSTRAINT "droppk21_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] -- leave this partition without children.
create table parted_index_col_drop2 partition of parted_index_col_drop
  for values in (2) partition by list (a)
;
----------
[DROP_TABLE] DROP TABLE droppk21
;
----------
[CREATE_TYPE] create domain dnull    varchar(15)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_inet_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test_tab (a int primary key, b bytea, c INT, d INT, e INT)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- tablespace should not change even if there is an index rewrite
ALTER TABLE testschema.test_default_tab ALTER id TYPE int
;
----------
[RENAME_PROG_OBJ] -- ROUTINE syntax

ALTER ROUTINE cp_testfunc1(int) RENAME TO cp_testfunc1a
;
----------
[CREATE_TABLE] CREATE TABLE tab_full_pk (a int primary key, b text)
;
----------
[CREATE_TABLE] create table t10()
;
----------
[CREATE_PROG_OBJ] create function tt14f() returns setof tt14t as
$$
declare
    rec1 record;
begin
    for rec1 in select * from tt14t
    loop
        return next rec1;
    end loop;
end;
$$
language plpgsql
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_26.pg_temp_28654_2 ADD COLUMN newdata pg_temp_26.pg_temp_28654
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."fk" (
    "a" integer
)
PARTITION BY RANGE ("a")
;
----------
[DROP_INDEX] DROP INDEX onek_nulltest
;
----------
[CREATE_POLICY] -- accents
CREATE COLLATION ignore_accents (provider = icu, locale = '@colStrength=primary;colCaseLevel=yes', deterministic = false)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest23q"
    ADD CONSTRAINT "gtest23q_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE TEMP_FLOAT (f1 FLOAT8)
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET bytea_output TO 'hex'
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27856 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ismn13, ismn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test23_1 PARTITION OF test23 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timestamptz_ops USING gist ADD
	OPERATOR	6	<> (timestamptz, timestamptz) ,
	OPERATOR	15	<-> (timestamptz, timestamptz) FOR ORDER BY pg_catalog.interval_ops ,
	FUNCTION	8 (timestamptz, timestamptz) gbt_tstz_distance (internal, timestamptz, int2, oid, internal) ,
	FUNCTION	9 (timestamptz, timestamptz) gbt_ts_fetch (internal)
;
----------
[DROP_PROG_OBJ] drop function dom_check(int)
;
----------
[ADMIN_TABLE] ANALYZE dupindexcols
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_statio_user_tables AS
    SELECT * FROM pg_statio_all_tables
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema') AND
          schemaname !~ '^pg_toast'
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_range_array (
		a INTEGER PRIMARY KEY,
		b TSTZRANGE,
		c int8range[]
	)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."casttesttype_out"("public"."casttesttype") RETURNS "cstring"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$textout$$
;
----------
[DROP_TABLE] -- cleanup
drop table list_parted
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30394_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLESPACE] -- create a tablespace using WITH clause
CREATE TABLESPACE regress_tblspacewith LOCATION '' WITH (some_nonexistent_parameter = true)
;
----------
[CREATE_PROG_OBJ] create or replace function func_part_create() returns trigger
  language plpgsql as $$
  begin
    execute 'create table tab_part_create_1 partition of tab_part_create for values in (1)';
    return null;
  end $$
;
----------
[CREATE_TABLE] CREATE TABLE POLYGON_TBL(f1 polygon)
;
----------
[CREATE_PROG_OBJ] -- fails, no way to deduce outputs
CREATE FUNCTION bad (f1 anyarray, out f2 anycompatible, out f3 anycompatiblearray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
----------
[CREATE_TABLE] --
-- INT4
--

CREATE TABLE INT4_TBL(f1 int4)
;
