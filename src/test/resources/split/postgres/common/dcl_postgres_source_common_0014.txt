EXECUTE dumpTableAttach('38710')
;
CREATE ROLE regress_test_def_replication
;
EXECUTE dumpOpr('2975')
;
COPY public.test_replica_id_full (x) TO STDOUT
;
CREATE USER MAPPING FOR regress_addr_user SERVER "integer"
;
EXECUTE dumpOpr('513')
;
-- in regress testing FirstNormalTransactionId will always be behind oldestXmin

COMMIT
;
DROP ROLE regress_passwd_sha_len2
;
SET datestyle = 'German'
;
-- Check that LIKE isn't confused when new table masks the old, either
BEGIN
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpTableAttach('38710')
;
----------
[CREATE_ROLE] CREATE ROLE regress_test_def_replication
;
----------
[UNSAFE] EXECUTE dumpOpr('2975')
;
----------
[DATA_EXPORT] COPY public.test_replica_id_full (x) TO STDOUT
;
----------
[GRANT] CREATE USER MAPPING FOR regress_addr_user SERVER "integer"
;
----------
[UNSAFE] EXECUTE dumpOpr('513')
;
----------
[TRANSACTION] -- in regress testing FirstNormalTransactionId will always be behind oldestXmin

COMMIT
;
----------
[DROP_ROLE] DROP ROLE regress_passwd_sha_len2
;
----------
[SESSION_SETTING_WRITE] SET datestyle = 'German'
;
----------
[TRANSACTION] -- Check that LIKE isn't confused when new table masks the old, either
BEGIN
;
