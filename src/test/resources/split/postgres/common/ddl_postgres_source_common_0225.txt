DROP TABLE pg_temp_11.pg_temp_41070_2, pg_temp_11.pg_temp_41070
;
drop function f1(a anyelement, b anyarray,
                 c anycompatible, d anycompatible)
;
VACUUM pg_catalog.pg_transform
;
CREATE FUNCTION dep_req3b() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT @extschema:test_ext_req_schema2@.dep_req2() || ' req3b' $$
;
-- test temp table deletion

CREATE TEMP TABLE temptest(col int)
;
create view uv_iocu_view as
   select b, b+1 as c, a, '2.0'::text as two from uv_iocu_tab
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
DO $$
DECLARE
    wal_segsize int := setting::int FROM pg_settings WHERE name = 'wal_segment_size';
    remain int;
    iters  int := 0;
BEGIN
    LOOP
        INSERT into filler
        select g, repeat(encode(sha256(g::text::bytea), 'hex'), (random() * 15 + 1)::int)
        from generate_series(1, 10) g;

        remain := wal_segsize - (pg_current_wal_insert_lsn() - '0/0') % wal_segsize;
        IF remain < 2 * setting::int from pg_settings where name = 'block_size' THEN
            RAISE log 'exiting after % iterations, % bytes to end of WAL segment', iters, remain;
            EXIT;
        END IF;
        iters := iters + 1;
    END LOOP;
END
$$
;
create table Room (
    roomno	char(8),
    comment	text
)
;
-- Test ISpell dictionary with hunspell affix file
CREATE TEXT SEARCH DICTIONARY hunspell (
                        Template=ispell,
                        DictFile=ispell_sample,
                        AffFile=hunspell_sample
)
;
CREATE TEMP TABLE pg_temp_3.pg_temp_29541_2 (tid pg_catalog.tid)
;
CREATE TABLE "public"."parted_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
PARTITION BY RANGE ("id")
;
CREATE FUNCTION ltree_same(ltree_gist, ltree_gist, internal)
RETURNS internal as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP TRANSFORM FOR hstore LANGUAGE foo
;
ALTER INDEX pg_toast.pg_toast_30675_index RENAME TO reind_con_toast_idx
;
DROP SEQUENCE tid_seq
;
-- cleanup
DROP TABLE ctest
;
CREATE FUNCTION isnne(isbn, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_30413 RENAME TO reind_con_toast
;
CREATE VIEW rw_view2 AS SELECT * FROM ONLY base_tbl_parent
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '42009'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_serialize
)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_replication_origin
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_amop
;
create view v1 as select * from tt2 natural join tt3
;
CREATE OR REPLACE FUNCTION f_test(p_c1 int) RETURNS int AS $$
DECLARE
	v_c1 int;
BEGIN
    SELECT c1 INTO v_c1 FROM ft1 WHERE c1 = p_c1 LIMIT 1;
    PERFORM c1 FROM ft1 WHERE c1 = p_c1 AND p_c1 = v_c1 LIMIT 1;
    RETURN v_c1;
END;
$$ LANGUAGE plpgsql
;
ANALYZE pg_temp_3.pg_temp_29533_2
;
-- Make a relation with a couple of enormous tuples.
create table wide as select generate_series(1, 2) as id, rpad('', 320000, 'x') as t
;
--
-- tuple larger than fillfactor
--
CREATE TABLE large_tuple_test (a int, b text) WITH (fillfactor = 10)
;
CREATE TABLE "public"."stats_test_tab1" (
    "a" integer
)
;
/*
 * 6.8
 * YES_OR_NO domain
 */

CREATE DOMAIN yes_or_no AS character varying(3) COLLATE "C"
    CONSTRAINT yes_or_no_check CHECK (value IN ('YES', 'NO'))
;
CREATE OR REPLACE FUNCTION view_trigger() RETURNS trigger
LANGUAGE plpgsql AS $$
declare
    argstr text := '';
begin
    for i in 0 .. TG_nargs - 1 loop
        if i > 0 then
            argstr := argstr || ', ';
        end if;
        argstr := argstr || TG_argv[i];
    end loop;

    raise notice '% % % % (%)', TG_TABLE_NAME, TG_WHEN, TG_OP, TG_LEVEL, argstr;

    if TG_LEVEL = 'ROW' then
        if TG_OP = 'INSERT' then
            raise NOTICE 'NEW: %', NEW;
            INSERT INTO main_table VALUES (NEW.a, NEW.b);
            RETURN NEW;
        end if;

        if TG_OP = 'UPDATE' then
            raise NOTICE 'OLD: %, NEW: %', OLD, NEW;
            UPDATE main_table SET a = NEW.a, b = NEW.b WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN NEW;
        end if;

        if TG_OP = 'DELETE' then
            raise NOTICE 'OLD: %', OLD;
            DELETE FROM main_table WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN OLD;
        end if;
    end if;

    RETURN NULL;
end;
$$
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_database
;
CREATE FUNCTION reraise_test() RETURNS void AS $$
BEGIN
   BEGIN
       RAISE syntax_error;
   EXCEPTION
       WHEN syntax_error THEN
           BEGIN
               raise notice 'exception % thrown in inner block, reraising', sqlerrm;
               RAISE;
           EXCEPTION
               WHEN OTHERS THEN
                   raise notice 'RIGHT - exception % caught in inner block', sqlerrm;
           END;
   END;
EXCEPTION
   WHEN OTHERS THEN
       raise notice 'WRONG - exception % caught in outer block', sqlerrm;
END;
$$ LANGUAGE plpgsql
;
ANALYZE pg_temp_9.pg_temp_25561
;
-- WITH TIES
CREATE TABLE limitoption AS SELECT 0 AS val FROM generate_series(1, 10)
;
create domain j_ordered_pair as j_unordered_pair check((value).x <= (value).y)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=15299 host=/tmp/MhdLJauTm4 dbname=postgres' PUBLICATION tap_pub
;
--
-- now using a partitioned table
--

create table iocdu_tt_parted (a int primary key, b text) partition by list (a)
;
ANALYZE grouping_unique
;
CREATE TABLE gtestnn_child PARTITION OF gtestnn_parent FOR VALUES FROM (1) TO (5)
;
CREATE OPERATOR CLASS issn13_ops DEFAULT
	FOR TYPE issn13 USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btissn13cmp(issn13, issn13)
;
--
-- partitioned FK referenced updates NO ACTION
--

TRUNCATE temporal_partitioned_mltrng, temporal_partitioned_fk_mltrng2mltrng
;
ALTER TABLE ONLY "public"."rule_and_refint_t3"
    ADD CONSTRAINT "rule_and_refint_t3_id3a_id3b_fkey" FOREIGN KEY ("id3a", "id3b") REFERENCES "public"."rule_and_refint_t1"("id1a", "id1b")
;
create temp table t1 (f1 int, f2 int8)
;
create table cta_test2 as select * from cta_test with no data
;
DROP ROUTINE IF EXISTS test_ambiguous_procname
;
CREATE TABLE fract_t (id BIGINT, PRIMARY KEY (id)) PARTITION BY RANGE (id)
;
CREATE CAST ("public"."int8alias1" AS bigint) WITHOUT FUNCTION
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_41070_2, pg_temp_11.pg_temp_41070
;
----------
[DROP_PROG_OBJ] drop function f1(a anyelement, b anyarray,
                 c anycompatible, d anycompatible)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_transform
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dep_req3b() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT @extschema:test_ext_req_schema2@.dep_req2() || ' req3b' $$
;
----------
[CREATE_TABLE] -- test temp table deletion

CREATE TEMP TABLE temptest(col int)
;
----------
[CREATE_VIEW(SELECT)] create view uv_iocu_view as
   select b, b+1 as c, a, '2.0'::text as two from uv_iocu_tab
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[BLOCK] DO $$
DECLARE
    wal_segsize int := setting::int FROM pg_settings WHERE name = 'wal_segment_size';
    remain int;
    iters  int := 0;
BEGIN
    LOOP
        INSERT into filler
        select g, repeat(encode(sha256(g::text::bytea), 'hex'), (random() * 15 + 1)::int)
        from generate_series(1, 10) g;

        remain := wal_segsize - (pg_current_wal_insert_lsn() - '0/0') % wal_segsize;
        IF remain < 2 * setting::int from pg_settings where name = 'block_size' THEN
            RAISE log 'exiting after % iterations, % bytes to end of WAL segment', iters, remain;
            EXIT;
        END IF;
        iters := iters + 1;
    END LOOP;
END
$$
;
----------
[CREATE_TABLE] create table Room (
    roomno	char(8),
    comment	text
)
;
----------
[CREATE_POLICY] -- Test ISpell dictionary with hunspell affix file
CREATE TEXT SEARCH DICTIONARY hunspell (
                        Template=ispell,
                        DictFile=ispell_sample,
                        AffFile=hunspell_sample
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_29541_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."parted_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
PARTITION BY RANGE ("id")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_same(ltree_gist, ltree_gist, internal)
RETURNS internal as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[UNKNOWN] DROP TRANSFORM FOR hstore LANGUAGE foo
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30675_index RENAME TO reind_con_toast_idx
;
----------
[DROP_SEQUENCE] DROP SEQUENCE tid_seq
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE ctest
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(isbn, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30413 RENAME TO reind_con_toast
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS SELECT * FROM ONLY base_tbl_parent
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '42009'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_serialize
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_replication_origin
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_amop
;
----------
[CREATE_VIEW(SELECT)] create view v1 as select * from tt2 natural join tt3
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION f_test(p_c1 int) RETURNS int AS $$
DECLARE
	v_c1 int;
BEGIN
    SELECT c1 INTO v_c1 FROM ft1 WHERE c1 = p_c1 LIMIT 1;
    PERFORM c1 FROM ft1 WHERE c1 = p_c1 AND p_c1 = v_c1 LIMIT 1;
    RETURN v_c1;
END;
$$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_29533_2
;
----------
[CREATE_TABLE|SELECT] -- Make a relation with a couple of enormous tuples.
create table wide as select generate_series(1, 2) as id, rpad('', 320000, 'x') as t
;
----------
[CREATE_TABLE] --
-- tuple larger than fillfactor
--
CREATE TABLE large_tuple_test (a int, b text) WITH (fillfactor = 10)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."stats_test_tab1" (
    "a" integer
)
;
----------
[CREATE_TYPE] /*
 * 6.8
 * YES_OR_NO domain
 */

CREATE DOMAIN yes_or_no AS character varying(3) COLLATE "C"
    CONSTRAINT yes_or_no_check CHECK (value IN ('YES', 'NO'))
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION view_trigger() RETURNS trigger
LANGUAGE plpgsql AS $$
declare
    argstr text := '';
begin
    for i in 0 .. TG_nargs - 1 loop
        if i > 0 then
            argstr := argstr || ', ';
        end if;
        argstr := argstr || TG_argv[i];
    end loop;

    raise notice '% % % % (%)', TG_TABLE_NAME, TG_WHEN, TG_OP, TG_LEVEL, argstr;

    if TG_LEVEL = 'ROW' then
        if TG_OP = 'INSERT' then
            raise NOTICE 'NEW: %', NEW;
            INSERT INTO main_table VALUES (NEW.a, NEW.b);
            RETURN NEW;
        end if;

        if TG_OP = 'UPDATE' then
            raise NOTICE 'OLD: %, NEW: %', OLD, NEW;
            UPDATE main_table SET a = NEW.a, b = NEW.b WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN NEW;
        end if;

        if TG_OP = 'DELETE' then
            raise NOTICE 'OLD: %', OLD;
            DELETE FROM main_table WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN OLD;
        end if;
    end if;

    RETURN NULL;
end;
$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_database
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION reraise_test() RETURNS void AS $$
BEGIN
   BEGIN
       RAISE syntax_error;
   EXCEPTION
       WHEN syntax_error THEN
           BEGIN
               raise notice 'exception % thrown in inner block, reraising', sqlerrm;
               RAISE;
           EXCEPTION
               WHEN OTHERS THEN
                   raise notice 'RIGHT - exception % caught in inner block', sqlerrm;
           END;
   END;
EXCEPTION
   WHEN OTHERS THEN
       raise notice 'WRONG - exception % caught in outer block', sqlerrm;
END;
$$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_25561
;
----------
[CREATE_TABLE|SELECT] -- WITH TIES
CREATE TABLE limitoption AS SELECT 0 AS val FROM generate_series(1, 10)
;
----------
[CREATE_TYPE] create domain j_ordered_pair as j_unordered_pair check((value).x <= (value).y)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=15299 host=/tmp/MhdLJauTm4 dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_TABLE] --
-- now using a partitioned table
--

create table iocdu_tt_parted (a int primary key, b text) partition by list (a)
;
----------
[ADMIN_TABLE] ANALYZE grouping_unique
;
----------
[CREATE_TABLE] CREATE TABLE gtestnn_child PARTITION OF gtestnn_parent FOR VALUES FROM (1) TO (5)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS issn13_ops DEFAULT
	FOR TYPE issn13 USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btissn13cmp(issn13, issn13)
;
----------
[TRUNCATE_TABLE] --
-- partitioned FK referenced updates NO ACTION
--

TRUNCATE temporal_partitioned_mltrng, temporal_partitioned_fk_mltrng2mltrng
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."rule_and_refint_t3"
    ADD CONSTRAINT "rule_and_refint_t3_id3a_id3b_fkey" FOREIGN KEY ("id3a", "id3b") REFERENCES "public"."rule_and_refint_t1"("id1a", "id1b")
;
----------
[CREATE_TABLE] create temp table t1 (f1 int, f2 int8)
;
----------
[CREATE_TABLE|SELECT] create table cta_test2 as select * from cta_test with no data
;
----------
[DROP_PROG_OBJ] DROP ROUTINE IF EXISTS test_ambiguous_procname
;
----------
[CREATE_TABLE] CREATE TABLE fract_t (id BIGINT, PRIMARY KEY (id)) PARTITION BY RANGE (id)
;
----------
[CREATE_PROG_OBJ] CREATE CAST ("public"."int8alias1" AS bigint) WITHOUT FUNCTION
;
