EXECUTE dumpFunc('34553')
;
-- fail
GRANT SELECT ON atestv1, atestv3 TO regress_priv_user4
;
LOCK TABLE public.city IN ACCESS SHARE MODE
;
LOCK TABLE public.tht_p2 IN ACCESS SHARE MODE NOWAIT
;
SET allow_system_table_mods = on
;
--
-- Verify that index scans encountering dead rows produced by an
-- aborted subtransaction of the current transaction can utilize the
-- kill_prior_tuple optimization
--
-- NB: The table size is currently *not* expected to stay the same, we
-- don't have logic to trigger opportunistic pruning in cases like
-- this.
BEGIN
;
EXECUTE dumpTableAttach('24350')
;
EXECUTE dumpCompositeType('38608')
;
ALTER TABLE public.idxpart21 OWNER TO zyc
;
EXECUTE dumpOpr('2636')
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('34553')
;
----------
[GRANT] -- fail
GRANT SELECT ON atestv1, atestv3 TO regress_priv_user4
;
----------
[SESSION_LOCK] LOCK TABLE public.city IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE public.tht_p2 IN ACCESS SHARE MODE NOWAIT
;
----------
[SESSION_SETTING_WRITE] SET allow_system_table_mods = on
;
----------
[TRANSACTION] --
-- Verify that index scans encountering dead rows produced by an
-- aborted subtransaction of the current transaction can utilize the
-- kill_prior_tuple optimization
--
-- NB: The table size is currently *not* expected to stay the same, we
-- don't have logic to trigger opportunistic pruning in cases like
-- this.
BEGIN
;
----------
[UNSAFE] EXECUTE dumpTableAttach('24350')
;
----------
[UNSAFE] EXECUTE dumpCompositeType('38608')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.idxpart21 OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpOpr('2636')
;
