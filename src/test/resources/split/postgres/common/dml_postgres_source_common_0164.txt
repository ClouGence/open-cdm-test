UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."attach_parted_part1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3615', relminmxid = '1'
WHERE oid = '"public"."tableam_parted_d_heap2"'::pg_catalog.regclass
;
insert into My_Table values ( 1 , 'text1' )
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '652', relminmxid = '1'
WHERE oid = '"public"."num_exp_sub"'::pg_catalog.regclass
;
INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_attrdef
;
INSERT INTO VARBIT_SHIFT_TABLE SELECT CAST(v || B'0' AS BIT VARYING(6)) >>1 FROM VARBIT_SHIFT_TABLE
;
-- this update fails because there is no referenced row
UPDATE fk_partitioned_fk SET a = a + 1 WHERE a = 2501
;
INSERT INTO test_tab VALUES (4, md5(4::text))
;
INSERT INTO num_input_test(n1) VALUES ('12__34')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10768', relminmxid = '1'
WHERE oid = '29551'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtestxx_3"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '966', relminmxid = '1'
WHERE oid = '"public"."test_range_excl"'::pg_catalog.regclass
;
DELETE FROM tidrangescan
WHERE substring(ctid::text FROM ',(\d+)\)')::integer > 10 OR substring(ctid::text FROM '\((\d+),')::integer > 2
;
-- not allowed
UPDATE rw_view2 SET ccc=ccc
;
-- Table trigger will prevent updates
UPDATE main_view SET b = 31 WHERE a = 20
;
insert into parted1_irreg values ('aardwolf', 2)
;
insert into arrtest_i values(2,'cat1',24)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."itest14"'::pg_catalog.regclass
;
-- fail, row movement happens only within the partition subtree.
UPDATE part_c_100_200 set c = c - 20, d = c WHERE c = 105
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '"public"."prt2_l_p1"'::pg_catalog.regclass
;
INSERT INTO atest5 VALUES (1,2,3)
;
INSERT INTO public.tplain VALUES ('6', 676)
;
INSERT INTO public.tplain VALUES ('8', 148)
;
WITH rcte AS ( SELECT sum(id) AS totalid FROM parent )
UPDATE parent SET id = id + totalid FROM rcte
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pt_part1"'::pg_catalog.regclass
;
-- ensure we still insert, even when all triggers are disabled
insert into trigtest default values
;
INSERT INTO test_tab VALUES (2002, md5(2002::text), -2002, 2*2002, -3*2002)
;
UPDATE	pg_catalog.pg_class SET	relfrozenxid = '16166' WHERE	relkind IN ('r', 'm', 't')
;
INSERT INTO tbl1 VALUES ('in master, after promotion')
;
INSERT INTO quad_box_tbl
  SELECT i, '((200, 300),(210, 310))'
  FROM generate_series(10001, 11000) AS i
;
DELETE FROM people WHERE lastname = 'last_name_18'
;
INSERT INTO public.tht VALUES ('2', 892)
;
INSERT INTO public.ths_p2 VALUES (3, 703)
;
INSERT INTO public.tht VALUES ('9', 889)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."bug_16784"'::pg_catalog.regclass
;
UPDATE errtst_parent SET data = data + 1 WHERE partid = 10
;
UPDATE test2 SET x = x * 2 RETURNING x
;
INSERT INTO public.tht VALUES ('2', 522)
;
delete from inh_nn2
;
delete from tt1
;
INSERT INTO public.ths_p1 VALUES (4, 784)
;
INSERT INTO INT4_TBL(f1) VALUES ('   0  ')
;
INSERT INTO hideblocks (blockno)
SELECT do_set_block_offsets(blk, ARRAY[1,31,32,63,64,200]::int2[])
  FROM generate_series(1000, 2000, 1) blk
;
INSERT INTO test_jsonb_constraints VALUES ('', 1)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '695', relminmxid = '1'
WHERE oid = '16902'
;
INSERT INTO POINT_TBL(f1) VALUES
  ('(0.0,0.0)'),
  ('(-10.0,0.0)'),
  ('(-3.0,4.0)'),
  ('(5.1, 34.5)'),
  ('(-5.0,-12.0)'),
  ('(1e-300,-1e-300)'),  -- To underflow
  ('(1e+300,Inf)'),  -- To overflow
  ('(Inf,1e+300)'),  -- Transposed
  (' ( Nan , NaN ) '),
  ('10.0,10.0')
;
insert into test (name, amount, letter) select name, amount+$1, letter from test
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10527', relminmxid = '1'
WHERE oid = '29390'
;
INSERT INTO public.tplain VALUES ('0', 120)
;
INSERT INTO public.mvtest_tm SELECT (diff.newdata).* FROM pg_temp_14.pg_temp_25613_2 diff WHERE tid IS NULL
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."attach_parted_part1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3615', relminmxid = '1'
WHERE oid = '"public"."tableam_parted_d_heap2"'::pg_catalog.regclass
;
----------
[INSERT] insert into My_Table values ( 1 , 'text1' )
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '652', relminmxid = '1'
WHERE oid = '"public"."num_exp_sub"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_attrdef
;
----------
[INSERT|SELECT] INSERT INTO VARBIT_SHIFT_TABLE SELECT CAST(v || B'0' AS BIT VARYING(6)) >>1 FROM VARBIT_SHIFT_TABLE
;
----------
[UPDATE] -- this update fails because there is no referenced row
UPDATE fk_partitioned_fk SET a = a + 1 WHERE a = 2501
;
----------
[INSERT] INSERT INTO test_tab VALUES (4, md5(4::text))
;
----------
[INSERT|SELECT] INSERT INTO num_input_test(n1) VALUES ('12__34')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10768', relminmxid = '1'
WHERE oid = '29551'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtestxx_3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '966', relminmxid = '1'
WHERE oid = '"public"."test_range_excl"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM tidrangescan
WHERE substring(ctid::text FROM ',(\d+)\)')::integer > 10 OR substring(ctid::text FROM '\((\d+),')::integer > 2
;
----------
[UPDATE] -- not allowed
UPDATE rw_view2 SET ccc=ccc
;
----------
[UPDATE] -- Table trigger will prevent updates
UPDATE main_view SET b = 31 WHERE a = 20
;
----------
[INSERT] insert into parted1_irreg values ('aardwolf', 2)
;
----------
[INSERT] insert into arrtest_i values(2,'cat1',24)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."itest14"'::pg_catalog.regclass
;
----------
[UPDATE] -- fail, row movement happens only within the partition subtree.
UPDATE part_c_100_200 set c = c - 20, d = c WHERE c = 105
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '"public"."prt2_l_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO atest5 VALUES (1,2,3)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 676)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 148)
;
----------
[UPDATE|SELECT] WITH rcte AS ( SELECT sum(id) AS totalid FROM parent )
UPDATE parent SET id = id + totalid FROM rcte
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pt_part1"'::pg_catalog.regclass
;
----------
[INSERT] -- ensure we still insert, even when all triggers are disabled
insert into trigtest default values
;
----------
[INSERT] INSERT INTO test_tab VALUES (2002, md5(2002::text), -2002, 2*2002, -3*2002)
;
----------
[UPDATE|METADATA] UPDATE	pg_catalog.pg_class SET	relfrozenxid = '16166' WHERE	relkind IN ('r', 'm', 't')
;
----------
[INSERT] INSERT INTO tbl1 VALUES ('in master, after promotion')
;
----------
[INSERT|SELECT] INSERT INTO quad_box_tbl
  SELECT i, '((200, 300),(210, 310))'
  FROM generate_series(10001, 11000) AS i
;
----------
[DELETE] DELETE FROM people WHERE lastname = 'last_name_18'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('2', 892)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 703)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 889)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."bug_16784"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE errtst_parent SET data = data + 1 WHERE partid = 10
;
----------
[UPDATE] UPDATE test2 SET x = x * 2 RETURNING x
;
----------
[INSERT] INSERT INTO public.tht VALUES ('2', 522)
;
----------
[DELETE] delete from inh_nn2
;
----------
[DELETE] delete from tt1
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 784)
;
----------
[INSERT|SELECT] INSERT INTO INT4_TBL(f1) VALUES ('   0  ')
;
----------
[INSERT|SELECT] INSERT INTO hideblocks (blockno)
SELECT do_set_block_offsets(blk, ARRAY[1,31,32,63,64,200]::int2[])
  FROM generate_series(1000, 2000, 1) blk
;
----------
[INSERT] INSERT INTO test_jsonb_constraints VALUES ('', 1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '695', relminmxid = '1'
WHERE oid = '16902'
;
----------
[INSERT|SELECT] INSERT INTO POINT_TBL(f1) VALUES
  ('(0.0,0.0)'),
  ('(-10.0,0.0)'),
  ('(-3.0,4.0)'),
  ('(5.1, 34.5)'),
  ('(-5.0,-12.0)'),
  ('(1e-300,-1e-300)'),  -- To underflow
  ('(1e+300,Inf)'),  -- To overflow
  ('(Inf,1e+300)'),  -- Transposed
  (' ( Nan , NaN ) '),
  ('10.0,10.0')
;
----------
[INSERT|SELECT] insert into test (name, amount, letter) select name, amount+$1, letter from test
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10527', relminmxid = '1'
WHERE oid = '29390'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 120)
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_tm SELECT (diff.newdata).* FROM pg_temp_14.pg_temp_25613_2 diff WHERE tid IS NULL
;
