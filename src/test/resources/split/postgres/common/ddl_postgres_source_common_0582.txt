ALTER TABLE ONLY "fkpart3"."pk5" ATTACH PARTITION "fkpart3"."pk51" FOR VALUES FROM (4000) TO (4500)
;
--
-- User functions, can't test perl/python etc here since may not be compiled.
--
CREATE FUNCTION echo_me(anyenum) RETURNS text AS $$
BEGIN
RETURN $1::text || 'omg';
END
$$ LANGUAGE plpgsql
;
CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS ''
;
CREATE FUNCTION my_int4_sort(int4,int4) RETURNS int LANGUAGE sql
  AS $$ SELECT CASE WHEN $1 = $2 THEN 0 WHEN $1 > $2 THEN 1 ELSE -1 END; $$
;
CREATE TABLE beta_neg_p1 PARTITION OF beta_neg FOR VALUES FROM (100) TO (150)
;
ALTER FUNCTION gbt_ts_penalty(internal, internal, internal) PARALLEL SAFE
;
VACUUM (PARALLEL 2) pg_catalog.pg_extension
;
create table rules_fooview (x int, y text)
;
DROP RULE sj_del_rule ON sj CASCADE
;
--
-- Test cases for COPY (select) TO
--
create table test1 (id serial, t text)
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=27912 host=/tmp/KgwChXZUr_ dbname=postgres' PUBLICATION mypub
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_attribute
;
-- We disallow changing table's row type if it's used for storage
create table at_tab1 (a int, b text)
;
-- The alias functions themselves.  Really these return "void", but
-- the implementation is such that we can declare them to return "int",
-- and we will get a zero result.
  create function lock_share(bigint) returns int language internal as
  'pg_advisory_xact_lock_shared_int8' strict parallel safe
;
TRUNCATE pg_description
;
CREATE TABLE prt1_l_p2_p1 PARTITION OF prt1_l_p2 FOR VALUES IN ('0000', '0001')
;
CREATE FUNCTION ins_trig_after_row_trig() RETURNS trigger
		  LANGUAGE plpgsql as $$
		  BEGIN
			IF new.id2 NOT LIKE 'triggered%' THEN
			  INSERT INTO ins_trig
				VALUES (DEFAULT, 'triggered row after' || NEW.id2);
			END IF;
			RETURN NEW;
		  END; $$
;
-- operations WITH ltxtquery

CREATE FUNCTION ltxtq_exec(ltree, ltxtquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TEXT SEARCH CONFIGURATION lithuanian
	(PARSER = default)
;
create table parted_trig (a int) partition by range (a)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
CREATE FUNCTION hstore_hash(hstore)
RETURNS integer
AS '$libdir/hstore','hstore_hash'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- Test that ALTER CONSTRAINT updates trigger deferrability properly

CREATE TEMP TABLE PKTABLE (ptest1 int primary key)
;
ALTER FUNCTION gbtreekey16_out(gbtreekey16) PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_33405_index RENAME TO reind_con_toast_idx
;
DROP PROCEDURE test_ambiguous_procname
;
CREATE AGGREGATE myaggn06b(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  INITCOND = '{}')
;
CREATE   VIEW  mytempview AS SELECT * FROM tbl1 WHERE tbl1.a
BETWEEN (SELECT d FROM tbl2 WHERE c = 1) AND (SELECT e FROM tbl3 WHERE f = 2)
AND EXISTS (SELECT g FROM tbl4 LEFT JOIN tbl3 ON tbl4.h = tbl3.f)
AND NOT EXISTS (SELECT g FROM tbl4 LEFT JOIN tmptbl ON tbl4.h = tmptbl.j)
;
/*
 * PostgreSQL System Views
 *
 * Copyright (c) 1996-2021, PostgreSQL Global Development Group
 *
 * src/backend/catalog/system_views.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
CREATE FUNCTION consume_xids(nxids bigint)
RETURNS xid8 IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
ANALYZE public.dropcolumnanother
;
CREATE INDEX collate_test1_idx2 ON collate_test1 (b COLLATE "POSIX")
;
-- should fail
alter table domain_test add column c str_domain
;
DROP FUNCTION brin_page_items(IN page bytea, IN index_oid regclass)
;
CREATE TEMP TABLE temptest(tcol int)
;
CREATE SCHEMA test_ext
;
create trigger tg_chkslotlink before insert or update
    on IFace for each row execute procedure tg_chkslotlink()
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_operator
;
-- should fail
CREATE OR REPLACE VIEW viewtest AS
	SELECT 1, * FROM viewtest_tbl
;
DO LANGUAGE plpgsql $$
BEGIN
MERGE INTO target t
USING source AS s
ON t.tid = s.sid
WHEN MATCHED AND t.balance > s.delta THEN
	UPDATE SET balance = t.balance - s.delta;
END;
$$
;
-- ===================================================================
-- test postgres_fdw.application_name GUC
-- ===================================================================
-- To avoid race conditions in checking the remote session's application_name,
-- use this view to make the remote session itself read its application_name.
CREATE VIEW my_application_name AS
  SELECT application_name FROM pg_stat_activity WHERE pid = pg_backend_pid()
;
--
-- Check that gen_partprune_steps() detects self-contradiction from clauses
-- regardless of the order of the clauses (Here we use a custom operator to
-- prevent the equivclass.c machinery from reordering the clauses)
--

create operator === (
   leftarg = int4,
   rightarg = int4,
   procedure = int4eq,
   commutator = ===,
   hashes
)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
DROP TABLE agg_sort_order CASCADE
;
DROP TABLE IF EXISTS test_nowait
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_statistic
;
CREATE FUNCTION gbtreekey_var_in(cstring)
RETURNS gbtreekey_var
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
/*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
-- should fail

-- Test intervals that are large enough to overflow 64 bits in comparisons
CREATE TEMP TABLE INTERVAL_TBL_OF (f1 interval)
;
create index idxparti on idxpart (a)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."pk5" ATTACH PARTITION "fkpart3"."pk51" FOR VALUES FROM (4000) TO (4500)
;
----------
[CREATE_PROG_OBJ] --
-- User functions, can't test perl/python etc here since may not be compiled.
--
CREATE FUNCTION echo_me(anyenum) RETURNS text AS $$
BEGIN
RETURN $1::text || 'omg';
END
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS ''
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION my_int4_sort(int4,int4) RETURNS int LANGUAGE sql
  AS $$ SELECT CASE WHEN $1 = $2 THEN 0 WHEN $1 > $2 THEN 1 ELSE -1 END; $$
;
----------
[CREATE_TABLE] CREATE TABLE beta_neg_p1 PARTITION OF beta_neg FOR VALUES FROM (100) TO (150)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_ts_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_extension
;
----------
[CREATE_TABLE] create table rules_fooview (x int, y text)
;
----------
[DROP_POLICY] DROP RULE sj_del_rule ON sj CASCADE
;
----------
[CREATE_TABLE] --
-- Test cases for COPY (select) TO
--
create table test1 (id serial, t text)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=27912 host=/tmp/KgwChXZUr_ dbname=postgres' PUBLICATION mypub
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_attribute
;
----------
[CREATE_TABLE] -- We disallow changing table's row type if it's used for storage
create table at_tab1 (a int, b text)
;
----------
[CREATE_PROG_OBJ] -- The alias functions themselves.  Really these return "void", but
-- the implementation is such that we can declare them to return "int",
-- and we will get a zero result.
  create function lock_share(bigint) returns int language internal as
  'pg_advisory_xact_lock_shared_int8' strict parallel safe
;
----------
[TRUNCATE_TABLE] TRUNCATE pg_description
;
----------
[CREATE_TABLE] CREATE TABLE prt1_l_p2_p1 PARTITION OF prt1_l_p2 FOR VALUES IN ('0000', '0001')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ins_trig_after_row_trig() RETURNS trigger
		  LANGUAGE plpgsql as $$
		  BEGIN
			IF new.id2 NOT LIKE 'triggered%' THEN
			  INSERT INTO ins_trig
				VALUES (DEFAULT, 'triggered row after' || NEW.id2);
			END IF;
			RETURN NEW;
		  END; $$
;
----------
[CREATE_PROG_OBJ] -- operations WITH ltxtquery

CREATE FUNCTION ltxtq_exec(ltree, ltxtquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION lithuanian
	(PARSER = default)
;
----------
[CREATE_TABLE] create table parted_trig (a int) partition by range (a)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_hash(hstore)
RETURNS integer
AS '$libdir/hstore','hstore_hash'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Test that ALTER CONSTRAINT updates trigger deferrability properly

CREATE TEMP TABLE PKTABLE (ptest1 int primary key)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbtreekey16_out(gbtreekey16) PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33405_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE test_ambiguous_procname
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn06b(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  INITCOND = '{}')
;
----------
[CREATE_VIEW(SELECT)] CREATE   VIEW  mytempview AS SELECT * FROM tbl1 WHERE tbl1.a
BETWEEN (SELECT d FROM tbl2 WHERE c = 1) AND (SELECT e FROM tbl3 WHERE f = 2)
AND EXISTS (SELECT g FROM tbl4 LEFT JOIN tbl3 ON tbl4.h = tbl3.f)
AND NOT EXISTS (SELECT g FROM tbl4 LEFT JOIN tmptbl ON tbl4.h = tmptbl.j)
;
----------
[CREATE_VIEW(SELECT)] /*
 * PostgreSQL System Views
 *
 * Copyright (c) 1996-2021, PostgreSQL Global Development Group
 *
 * src/backend/catalog/system_views.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION consume_xids(nxids bigint)
RETURNS xid8 IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ADMIN_TABLE] ANALYZE public.dropcolumnanother
;
----------
[ADD_INDEX] CREATE INDEX collate_test1_idx2 ON collate_test1 (b COLLATE "POSIX")
;
----------
[ALTER_TABLE|ADD_COLUMN] -- should fail
alter table domain_test add column c str_domain
;
----------
[DROP_PROG_OBJ] DROP FUNCTION brin_page_items(IN page bytea, IN index_oid regclass)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest(tcol int)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_ext
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_chkslotlink before insert or update
    on IFace for each row execute procedure tg_chkslotlink()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_operator
;
----------
[CREATE_VIEW(SELECT)] -- should fail
CREATE OR REPLACE VIEW viewtest AS
	SELECT 1, * FROM viewtest_tbl
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
MERGE INTO target t
USING source AS s
ON t.tid = s.sid
WHEN MATCHED AND t.balance > s.delta THEN
	UPDATE SET balance = t.balance - s.delta;
END;
$$
;
----------
[CREATE_VIEW(SELECT)] -- ===================================================================
-- test postgres_fdw.application_name GUC
-- ===================================================================
-- To avoid race conditions in checking the remote session's application_name,
-- use this view to make the remote session itself read its application_name.
CREATE VIEW my_application_name AS
  SELECT application_name FROM pg_stat_activity WHERE pid = pg_backend_pid()
;
----------
[CREATE_PROG_OBJ] --
-- Check that gen_partprune_steps() detects self-contradiction from clauses
-- regardless of the order of the clauses (Here we use a custom operator to
-- prevent the equivclass.c machinery from reordering the clauses)
--

create operator === (
   leftarg = int4,
   rightarg = int4,
   procedure = int4eq,
   commutator = ===,
   hashes
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
----------
[DROP_TABLE] DROP TABLE agg_sort_order CASCADE
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS test_nowait
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_statistic
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey_var_in(cstring)
RETURNS gbtreekey_var
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_POLICY] /*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
----------
[CREATE_TABLE] -- should fail

-- Test intervals that are large enough to overflow 64 bits in comparisons
CREATE TEMP TABLE INTERVAL_TBL_OF (f1 interval)
;
----------
[ADD_INDEX] create index idxparti on idxpart (a)
;
