BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE READ ONLY
;
reset plpgsql.print_strict_params
;
CREATE ROLE regress_rol_op6
;
COPY public.gtest20 (a) TO stdout
;
refresh materialized view v1
;
ALTER TABLE public.part_rev OWNER TO zyc
;
-- works
copy header_copytest (c, a) from stdin with (header match)
;
-- ======================================================

-- fail - duplicate tables are not allowed if that table has any column lists
SET client_min_messages = 'ERROR'
;
ALTER AGGREGATE testagg4(int2) OWNER TO regress_testrolx
;
EXECUTE dumpFunc('21012')
;
------------------------------------------------------------------------------------------
[TRANSACTION] BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE READ ONLY
;
----------
[SESSION_SETTING_WRITE] reset plpgsql.print_strict_params
;
----------
[CREATE_ROLE] CREATE ROLE regress_rol_op6
;
----------
[DATA_EXPORT] COPY public.gtest20 (a) TO stdout
;
----------
[ADMIN] refresh materialized view v1
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.part_rev OWNER TO zyc
;
----------
[DATA_IMPORT] -- works
copy header_copytest (c, a) from stdin with (header match)
;
----------
[SESSION_SETTING_WRITE] -- ======================================================

-- fail - duplicate tables are not allowed if that table has any column lists
SET client_min_messages = 'ERROR'
;
----------
[TRANSFER_PRIVILEGE] ALTER AGGREGATE testagg4(int2) OWNER TO regress_testrolx
;
----------
[UNSAFE] EXECUTE dumpFunc('21012')
;
