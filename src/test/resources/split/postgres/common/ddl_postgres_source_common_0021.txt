-- ctid fetched directly from the source table.
CREATE VIEW tid_view_with_ctid AS SELECT ctid, a FROM tid_tab
;
create index _tstidx on _ltreetest using gist (t)
;
ALTER OPERATOR FAMILY gist__int_ops USING gist
DROP OPERATOR 8 (_int4, _int4)
;
CREATE UNIQUE INDEX ind_expr_pred ON reindex_inj.tbl(abs(i)) WHERE mod(i, 2) = 0
;
--
-- CREATE_MISC
--

--
-- a is the type root
-- b and c inherit from a (one-level single inheritance)
-- d inherits from b and c (two-level multiple inheritance)
-- e inherits from c (two-level single inheritance)
-- f inherits from e (three-level single inheritance)
--
CREATE TABLE a_star (
	class		char,
	a 			int4
)
;
create index on nestjsonb using gin(j jsonb_path_ops)
;
-- Check that clustering sets new relfilenodes:
CREATE TEMP TABLE old_cluster_info AS SELECT relname, level, relfilenode, relkind FROM pg_partition_tree('clstrpart'::regclass) AS tree JOIN pg_class c ON c.oid=tree.relid
;
CALL transaction_test11()
;
drop table regular_table
;
CREATE TABLE fk (a int DEFAULT 50) PARTITION BY RANGE (a)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_replication_origin
;
drop table parted_conflict
;
alter index idxpart2_a_idx attach partition idxpart22_a_idx
;
CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=19285 host=/tmp/ZBWsOYky2b dbname=postgres' PUBLICATION pub_viaroot
;
ALTER TABLE split_alter_table.inherit_child INHERIT split_alter_table.inherit_parent
;
CREATE DATABASE "postgres" WITH TEMPLATE = template0 OID = 5 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
CREATE TABLE foo_1(f1 int, f2 text, f3 text[], primary key (f1,f2) include (f3))
;
alter domain ddef1 drop default
;
CREATE TABLE stats_test(data text)
;
create cast (int8 as int8alias2) without function
;
alter table tt11 add column z int
;
/* contrib/pg_surgery/pg_surgery--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION heap_force_kill(reloid regclass, tids tid[])
RETURNS VOID
AS '$libdir/pg_surgery', 'heap_force_kill'
LANGUAGE C STRICT
;
CREATE EXTENSION hstore_plperl
;
ALTER TABLE regtest_ptable ALTER p SET STORAGE PLAIN
;
ALTER TABLE ONLY "public"."prt4_n" ATTACH PARTITION "public"."prt4_n_p3" FOR VALUES FROM (500) TO (600)
;
DROP TABLE plt2_adv_p3_null
;
CREATE DOMAIN sqljson_char2 AS char(2) CHECK (VALUE NOT IN ('12'))
;
create function dfunc(a int = 1, out sum int, b int = 2) as $$
  select $1 + $2;
$$ language sql
;
ALTER DATABASE "regression" SET "lc_monetary" TO 'C'
;
ALTER TABLE pg_toast.pg_toast_30352 RENAME TO reind_con_toast
;
alter table idxpart add exclude (a with =)
;
ALTER TABLE pg_temp_13.pg_temp_26250_2 ADD COLUMN newdata pg_temp_13.pg_temp_26250
;
create trigger trans_tab_parent_delete_trig
  after delete on trans_tab_parent referencing old table as old_table
  for each statement execute procedure dump_delete()
;
ALTER TABLE t1 DROP COLUMN junk
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_auth_members
;
CREATE TABLE rec1 (x integer, y integer)
;
CREATE TABLE lock_tbl1 (a BIGINT)
;
create trigger tg_hslot_bu before update
    on HSlot for each row execute procedure tg_hslot_bu()
;
DROP TABLE test_mix_2
;
drop table attest2
;
CREATE TABLE "public"."sl" (
    "a" integer,
    "b" integer,
    "c" integer,
    "bool_col" boolean
)
;
VACUUM (PARALLEL 2) pg_catalog.pg_enum
;
ALTER TABLE ONLY "public"."part33_self_fk"
    ADD CONSTRAINT "part33_self_fk_pkey" PRIMARY KEY ("id")
;
TRUNCATE trunc_stats_test
;
CREATE OPERATOR ~<~ (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = ~>=~,
    COMMUTATOR = ~>~,
    PROCEDURE  = citext_pattern_lt,
    RESTRICT   = scalarltsel,
    JOIN       = scalarltjoinsel
)
;
ALTER TABLE pg_toast.pg_toast_30825 RENAME TO reind_con_toast
;
CREATE TABLE addr_nsp.parttable (
    a int PRIMARY KEY
) PARTITION BY RANGE (a)
;
CREATE FUNCTION test_canonicalize_path(text)
   RETURNS text
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT IMMUTABLE
;
CREATE TYPE myshell
;
ALTER TABLE pg_toast.pg_toast_33497 RENAME TO reind_con_toast
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] -- ctid fetched directly from the source table.
CREATE VIEW tid_view_with_ctid AS SELECT ctid, a FROM tid_tab
;
----------
[ADD_INDEX] create index _tstidx on _ltreetest using gist (t)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist__int_ops USING gist
DROP OPERATOR 8 (_int4, _int4)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ind_expr_pred ON reindex_inj.tbl(abs(i)) WHERE mod(i, 2) = 0
;
----------
[CREATE_TABLE] --
-- CREATE_MISC
--

--
-- a is the type root
-- b and c inherit from a (one-level single inheritance)
-- d inherits from b and c (two-level multiple inheritance)
-- e inherits from c (two-level single inheritance)
-- f inherits from e (three-level single inheritance)
--
CREATE TABLE a_star (
	class		char,
	a 			int4
)
;
----------
[ADD_INDEX] create index on nestjsonb using gin(j jsonb_path_ops)
;
----------
[CREATE_TABLE|SELECT|METADATA] -- Check that clustering sets new relfilenodes:
CREATE TEMP TABLE old_cluster_info AS SELECT relname, level, relfilenode, relkind FROM pg_partition_tree('clstrpart'::regclass) AS tree JOIN pg_class c ON c.oid=tree.relid
;
----------
[CALL_PROG_OBJ] CALL transaction_test11()
;
----------
[DROP_TABLE] drop table regular_table
;
----------
[CREATE_TABLE] CREATE TABLE fk (a int DEFAULT 50) PARTITION BY RANGE (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_replication_origin
;
----------
[DROP_TABLE] drop table parted_conflict
;
----------
[ALTER_INDEX] alter index idxpart2_a_idx attach partition idxpart22_a_idx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=19285 host=/tmp/ZBWsOYky2b dbname=postgres' PUBLICATION pub_viaroot
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.inherit_child INHERIT split_alter_table.inherit_parent
;
----------
[CREATE_CATALOG] CREATE DATABASE "postgres" WITH TEMPLATE = template0 OID = 5 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[CREATE_TABLE] CREATE TABLE foo_1(f1 int, f2 text, f3 text[], primary key (f1,f2) include (f3))
;
----------
[ALTER_TYPE] alter domain ddef1 drop default
;
----------
[CREATE_TABLE] CREATE TABLE stats_test(data text)
;
----------
[CREATE_PROG_OBJ] create cast (int8 as int8alias2) without function
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tt11 add column z int
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_surgery/pg_surgery--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION heap_force_kill(reloid regclass, tids tid[])
RETURNS VOID
AS '$libdir/pg_surgery', 'heap_force_kill'
LANGUAGE C STRICT
;
----------
[CREATE_LIBRARY] CREATE EXTENSION hstore_plperl
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_ptable ALTER p SET STORAGE PLAIN
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt4_n" ATTACH PARTITION "public"."prt4_n_p3" FOR VALUES FROM (500) TO (600)
;
----------
[DROP_TABLE] DROP TABLE plt2_adv_p3_null
;
----------
[CREATE_TYPE] CREATE DOMAIN sqljson_char2 AS char(2) CHECK (VALUE NOT IN ('12'))
;
----------
[CREATE_PROG_OBJ] create function dfunc(a int = 1, out sum int, b int = 2) as $$
  select $1 + $2;
$$ language sql
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET "lc_monetary" TO 'C'
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30352 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add exclude (a with =)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26250_2 ADD COLUMN newdata pg_temp_13.pg_temp_26250
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trans_tab_parent_delete_trig
  after delete on trans_tab_parent referencing old table as old_table
  for each statement execute procedure dump_delete()
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE t1 DROP COLUMN junk
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_auth_members
;
----------
[CREATE_TABLE] CREATE TABLE rec1 (x integer, y integer)
;
----------
[CREATE_TABLE] CREATE TABLE lock_tbl1 (a BIGINT)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_hslot_bu before update
    on HSlot for each row execute procedure tg_hslot_bu()
;
----------
[DROP_TABLE] DROP TABLE test_mix_2
;
----------
[DROP_TABLE] drop table attest2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."sl" (
    "a" integer,
    "b" integer,
    "c" integer,
    "bool_col" boolean
)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_enum
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."part33_self_fk"
    ADD CONSTRAINT "part33_self_fk_pkey" PRIMARY KEY ("id")
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_stats_test
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~<~ (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = ~>=~,
    COMMUTATOR = ~>~,
    PROCEDURE  = citext_pattern_lt,
    RESTRICT   = scalarltsel,
    JOIN       = scalarltjoinsel
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30825 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE addr_nsp.parttable (
    a int PRIMARY KEY
) PARTITION BY RANGE (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_canonicalize_path(text)
   RETURNS text
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TYPE] CREATE TYPE myshell
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33497 RENAME TO reind_con_toast
;
