INSERT INTO num_data VALUES (2, '-34338492.215397047')
;
-- we want to correct some "date"
update tttest set price_on = -1 where price_id = 1
;
UPDATE brin_idx_tbl SET col2 = 'updated' WHERE col1 BETWEEN 1 AND 5000
;
UPDATE ONLY "pg_temp_5"."tasks" SET "checked_by" = NULL WHERE $1 OPERATOR(pg_catalog.=) "checked_by"
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12666', relminmxid = '3'
WHERE oid = '"public"."part33_self_fk"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('5', 735)
;
INSERT INTO INT8_TBL VALUES('+4567890123456789','-4567890123456789')
;
INSERT INTO public.tplain VALUES ('2', 422)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8863', relminmxid = '1'
WHERE oid = '"public"."rules_src"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13501', relminmxid = '3'
WHERE oid = '34465'
;
INSERT INTO caster (text)          VALUES ('the fat cat'::tsvector)
;
-- OK
insert into rw_view1 values ('zzz',2.0,1)
  on conflict (aa) do update set cc = 3.0
;
INSERT INTO public.ths_p1 VALUES (2, 242)
;
-- force a summarization cycle in brinidx

UPDATE brintest SET int8col = int8col * int4col
;
-- a PK update shrinking the referenced range but still valid:
-- There are two references: one fulfilled by the first pk row,
-- the other fulfilled by both pk rows combined.
INSERT INTO temporal_rng (id, valid_at) VALUES
  ('[1,2)', daterange('2018-01-01', '2018-03-01')),
  ('[1,2)', daterange('2018-03-01', '2018-06-01'))
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13241', relminmxid = '3'
WHERE oid = '33148'
;
INSERT INTO temporal_rng3 (id, valid_at) VALUES (NULL, daterange('2018-01-01', '2018-01-05'))
;
-- rejects empty:
UPDATE  temporal_rng
SET     id = '[1,2)',
        valid_at = 'empty'
WHERE   id = '[21,22)'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9540', relminmxid = '1'
WHERE oid = '"public"."clstr_tst_s"'::pg_catalog.regclass
;
INSERT INTO DATE_TBL VALUES ('2039-04-09')
;
INSERT INTO macaddr_data VALUES (10, '08:00:2b:01:02:04')
;
INSERT INTO "S 1"."T 1"("C 1", c2, c3, c4, c5, c6, c7, c8) VALUES ($1, $2, $3, $4, $5, $6, $7, $8) RETURNING "C 1", c2, c3, c4, c5, c6, c7, c8
;
INSERT INTO public.tht VALUES ('4', 794)
;
INSERT INTO J1_TBL VALUES (NULL, NULL, 'null')
;
INSERT INTO public.tht VALUES ('6', 116)
;
INSERT INTO public.tplain VALUES ('9', 779)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_para_p3"'::pg_catalog.regclass
  AND attname IN ('x', 'y')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '19870'
;
INSERT INTO public.ths_p2 VALUES (7, 677)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest24"'::pg_catalog.regclass
;
DELETE FROM target t WHERE t.key = 1
;
insert into parted values (0, 1, 'zero win')
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"fkpart3"."pk1"'::pg_catalog.regclass AND
conname IN ('pk_a_not_null')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17866', relminmxid = '3'
WHERE oid = '"public"."cmpart2"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (9, 669)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1547', relminmxid = '1'
WHERE oid = '"public"."quad_poly_tbl"'::pg_catalog.regclass
;
insert into test_t (select generate_series(0, 10000), 'a', 2)
;
INSERT INTO public.ths_p3 VALUES (9, 389)
;
INSERT INTO tab3 VALUES (3), (4)
;
INSERT INTO CHAR_TBL (f1) VALUES ('')
;
INSERT INTO public.tht VALUES ('4', 944)
;
UPDATE tab_rowfilter_toast SET b = '1'
;
INSERT INTO public.test (a, b, c)
		VALUES (
			x'DEADF9F9DEADF9F9'::bigint,
			'abcdefg',
			repeat('w', 10000)
		)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10443', relminmxid = '1'
WHERE oid = '"public"."rtest_t1"'::pg_catalog.regclass
;
-- Inserting into the foreign key table will not result in an error, even if
-- there is no matching key in the referenced table.
INSERT INTO FKTABLE VALUES (1, 2)
;
INSERT INTO sql_sizing VALUES (20002, 'MAXIMUM STATEMENT OCTETS SCHEMA', 0, NULL)
;
DELETE FROM european_city_view WHERE city_name = 'Cambridge' RETURNING *
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9129', relminmxid = '1'
WHERE oid = '"public"."ruletest_tbl"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '19942', relminmxid = '3'
WHERE oid = '"public"."test_io_vac_strategy"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('8', 748)
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO num_data VALUES (2, '-34338492.215397047')
;
----------
[UPDATE] -- we want to correct some "date"
update tttest set price_on = -1 where price_id = 1
;
----------
[UPDATE] UPDATE brin_idx_tbl SET col2 = 'updated' WHERE col1 BETWEEN 1 AND 5000
;
----------
[UPDATE] UPDATE ONLY "pg_temp_5"."tasks" SET "checked_by" = NULL WHERE $1 OPERATOR(pg_catalog.=) "checked_by"
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12666', relminmxid = '3'
WHERE oid = '"public"."part33_self_fk"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('5', 735)
;
----------
[INSERT] INSERT INTO INT8_TBL VALUES('+4567890123456789','-4567890123456789')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 422)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8863', relminmxid = '1'
WHERE oid = '"public"."rules_src"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13501', relminmxid = '3'
WHERE oid = '34465'
;
----------
[INSERT|SELECT] INSERT INTO caster (text)          VALUES ('the fat cat'::tsvector)
;
----------
[MERGE] -- OK
insert into rw_view1 values ('zzz',2.0,1)
  on conflict (aa) do update set cc = 3.0
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 242)
;
----------
[UPDATE] -- force a summarization cycle in brinidx

UPDATE brintest SET int8col = int8col * int4col
;
----------
[INSERT|SELECT] -- a PK update shrinking the referenced range but still valid:
-- There are two references: one fulfilled by the first pk row,
-- the other fulfilled by both pk rows combined.
INSERT INTO temporal_rng (id, valid_at) VALUES
  ('[1,2)', daterange('2018-01-01', '2018-03-01')),
  ('[1,2)', daterange('2018-03-01', '2018-06-01'))
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13241', relminmxid = '3'
WHERE oid = '33148'
;
----------
[INSERT|SELECT] INSERT INTO temporal_rng3 (id, valid_at) VALUES (NULL, daterange('2018-01-01', '2018-01-05'))
;
----------
[UPDATE] -- rejects empty:
UPDATE  temporal_rng
SET     id = '[1,2)',
        valid_at = 'empty'
WHERE   id = '[21,22)'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9540', relminmxid = '1'
WHERE oid = '"public"."clstr_tst_s"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO DATE_TBL VALUES ('2039-04-09')
;
----------
[INSERT] INSERT INTO macaddr_data VALUES (10, '08:00:2b:01:02:04')
;
----------
[INSERT|SELECT] INSERT INTO "S 1"."T 1"("C 1", c2, c3, c4, c5, c6, c7, c8) VALUES ($1, $2, $3, $4, $5, $6, $7, $8) RETURNING "C 1", c2, c3, c4, c5, c6, c7, c8
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 794)
;
----------
[INSERT] INSERT INTO J1_TBL VALUES (NULL, NULL, 'null')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 116)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 779)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_para_p3"'::pg_catalog.regclass
  AND attname IN ('x', 'y')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '19870'
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 677)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest24"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM target t WHERE t.key = 1
;
----------
[INSERT] insert into parted values (0, 1, 'zero win')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"fkpart3"."pk1"'::pg_catalog.regclass AND
conname IN ('pk_a_not_null')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17866', relminmxid = '3'
WHERE oid = '"public"."cmpart2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 669)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1547', relminmxid = '1'
WHERE oid = '"public"."quad_poly_tbl"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into test_t (select generate_series(0, 10000), 'a', 2)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 389)
;
----------
[INSERT] INSERT INTO tab3 VALUES (3), (4)
;
----------
[INSERT|SELECT] INSERT INTO CHAR_TBL (f1) VALUES ('')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 944)
;
----------
[UPDATE] UPDATE tab_rowfilter_toast SET b = '1'
;
----------
[INSERT|SELECT] INSERT INTO public.test (a, b, c)
		VALUES (
			x'DEADF9F9DEADF9F9'::bigint,
			'abcdefg',
			repeat('w', 10000)
		)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10443', relminmxid = '1'
WHERE oid = '"public"."rtest_t1"'::pg_catalog.regclass
;
----------
[INSERT] -- Inserting into the foreign key table will not result in an error, even if
-- there is no matching key in the referenced table.
INSERT INTO FKTABLE VALUES (1, 2)
;
----------
[INSERT] INSERT INTO sql_sizing VALUES (20002, 'MAXIMUM STATEMENT OCTETS SCHEMA', 0, NULL)
;
----------
[DELETE] DELETE FROM european_city_view WHERE city_name = 'Cambridge' RETURNING *
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9129', relminmxid = '1'
WHERE oid = '"public"."ruletest_tbl"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '19942', relminmxid = '3'
WHERE oid = '"public"."test_io_vac_strategy"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 748)
;
