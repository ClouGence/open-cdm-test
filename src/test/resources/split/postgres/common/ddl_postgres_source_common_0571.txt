-- functional dependencies tests
CREATE TABLE functional_dependencies (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b TEXT,
    filler3 DATE,
    c INT,
    d TEXT
)
WITH (autovacuum_enabled = off)
;
DROP EVENT TRIGGER IF EXISTS split_event_helper_table_rewrite
;
CREATE TABLE storage (
    id INT
) WITH (
    fillfactor = 10,
    autovacuum_enabled = FALSE
)
;
CREATE TABLE fk_partitioned_fk_3 (a int, b int)
;
DROP FUNCTION test_text_to_wchars
;
CREATE TABLE "public"."sj_t4" (
    "id" integer NOT NULL,
    "a" integer
)
;
create temp table t1 (f1 int8_tbl[])
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', quote ':')
;
-- can't have generated column that is a child of normal column
CREATE TABLE gtest_normal (a int, b int)
;
CREATE EVENT TRIGGER always_end ON ddl_command_end
EXECUTE PROCEDURE event_trigger_test()
;
create temp table tt6(f1 int, f2 int)
;
ALTER TABLE "public"."pitest1_p1_like" ALTER COLUMN "f3" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."pitest1_p1_like_f3_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
CREATE OPERATOR === (
    LEFTARG = boolean,
    RIGHTARG = boolean,
    PROCEDURE = alter_op_test_fn,
    COMMUTATOR = ===,
    NEGATOR = !==,
    RESTRICT = customcontsel,
    JOIN = contjoinsel,
    HASHES, MERGES
)
;
DROP SCHEMA IF EXISTS test_schema_exists
;
create table tbl1a (a int, c int)
;
DROP TABLE testpub_rf_schema1.testpub_rf_tbl5
;
create or replace function dfunc(a varchar = 'def a', out _a varchar, x numeric = NULL, out _c numeric)
returns record as $$
  select $1, $2;
$$ language sql
;
DROP TABLE attmp7
;
-- ok, as true
ALTER INDEX dummy_test_idx SET (option_bool = 3.4)
;
-- Create the operator class
CREATE OPERATOR CLASS gist_int8_ops
DEFAULT FOR TYPE int8 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_int8_consistent (internal, int8, int2, oid, internal),
	FUNCTION	2	gbt_int8_union (internal, internal),
	FUNCTION	3	gbt_int8_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_int8_penalty (internal, internal, internal),
	FUNCTION	6	gbt_int8_picksplit (internal, internal),
	FUNCTION	7	gbt_int8_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
-- error, references aliased away name

drop index key_index
;
CREATE SCHEMA import_dest2
;
CREATE OPERATOR CLASS money_ops
DEFAULT FOR TYPE money USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       cash_cmp(money,money),
    FUNCTION        2       gin_extract_value_money(money, internal),
    FUNCTION        3       gin_extract_query_money(money, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_money(money,money,int2, internal),
STORAGE         money
;
COMMENT ON LARGE OBJECT 16549 IS 'comment on large object'
;
ANALYZE pg_catalog.pg_largeobject
;
ANALYZE regress_indexing.pk1
;
-- partitioned table cannot participate in regular inheritance
CREATE TABLE partitioned2 (
	a int,
	b text
) PARTITION BY RANGE ((a+1), substr(b, 1, 5))
;
-- UPDATE on view with INSTEAD OF trigger
CREATE FUNCTION joinview_upd_trig_fn() RETURNS trigger
LANGUAGE plpgsql AS
$$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING new.f1, new.f4 INTO new.f1, new.f4;  -- should fail
  RETURN NEW;
END;
$$
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_amop
;
DROP TEXT SEARCH TEMPLATE IF EXISTS test_tstemplate_exists
;
DROP SCHEMA sch2 cascade
;
create function pslot_backlink_view(bpchar)
returns text as '
<<outer>>
declare
    rec		record;
    bltype	char(2);
    retval	text;
begin
    select into rec * from PSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if rec.backlink = '''' then
        return ''-'';
    end if;
    bltype := substr(rec.backlink, 1, 2);
    if bltype = ''PL'' then
        declare
	    rec		record;
	begin
	    select into rec * from PLine where slotname = "outer".rec.backlink;
	    retval := ''Phone line '' || trim(rec.phonenumber);
	    if rec.comment != '''' then
	        retval := retval || '' ('';
		retval := retval || rec.comment;
		retval := retval || '')'';
	    end if;
	    return retval;
	end;
    end if;
    if bltype = ''WS'' then
        select into rec * from WSlot where slotname = rec.backlink;
	retval := trim(rec.slotname) || '' in room '';
	retval := retval || trim(rec.roomno);
	retval := retval || '' -> '';
	return retval || wslot_slotlink_view(rec.slotname);
    end if;
    return rec.backlink;
end;
' language plpgsql
;
alter table atacc1 add foreign key (a) references atacc2(id)
;
ALTER TABLE pg_toast.pg_toast_28198 RENAME TO reind_con_toast
;
CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub5
;
ALTER TABLE FKTABLE ALTER CONSTRAINT fkdi2 DEFERRABLE INITIALLY IMMEDIATE
;
CREATE TYPE "public"."rainbow" AS ENUM (
)
;
ALTER TABLE t1 ADD COLUMN c text
;
CREATE EXTENSION pg_visibility
;
CREATE FUNCTION gtrgm_options(internal)
RETURNS void
AS '$libdir/pg_trgm', 'gtrgm_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
ALTER INDEX dummy_test_idx SET (option_enum = 'one')
;
CREATE RULE "shoelace_upd" AS
    ON UPDATE TO "public"."shoelace" DO INSTEAD  UPDATE "public"."shoelace_data" SET "sl_name" = "new"."sl_name", "sl_avail" = "new"."sl_avail", "sl_color" = "new"."sl_color", "sl_len" = "new"."sl_len", "sl_unit" = "new"."sl_unit"
  WHERE ("shoelace_data"."sl_name" = "old"."sl_name")
;
CREATE TABLE "public"."rules_src" (
    "f1" integer,
    "f2" integer DEFAULT 0
)
;
ALTER SEQUENCE serialTest1 CYCLE
;
CREATE TABLE "public"."inh_test" (
    "a" integer,
    "b" character(1) NOT NULL
)
;
CREATE VIEW domain_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(n.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(CASE WHEN condeferrable THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS is_deferrable,
           CAST(CASE WHEN condeferred THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS initially_deferred
    FROM pg_namespace rs, pg_namespace n, pg_constraint con, pg_type t
    WHERE rs.oid = con.connamespace
          AND n.oid = t.typnamespace
          AND t.oid = con.contypid
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
drop index op_index_key
;
DROP TRIGGER modified_modified_a ON main_table
;
CREATE FUNCTION int8_dist(int8, int8)
RETURNS int8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- On the other hand, this should work because int implicitly promotes to
-- numeric, and we allow promotion on the FK side
CREATE TEMP TABLE PKTABLE (ptest1 numeric PRIMARY KEY)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- functional dependencies tests
CREATE TABLE functional_dependencies (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b TEXT,
    filler3 DATE,
    c INT,
    d TEXT
)
WITH (autovacuum_enabled = off)
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER IF EXISTS split_event_helper_table_rewrite
;
----------
[CREATE_TABLE] CREATE TABLE storage (
    id INT
) WITH (
    fillfactor = 10,
    autovacuum_enabled = FALSE
)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_3 (a int, b int)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_text_to_wchars
;
----------
[CREATE_TABLE] CREATE TABLE "public"."sj_t4" (
    "id" integer NOT NULL,
    "a" integer
)
;
----------
[CREATE_TABLE] create temp table t1 (f1 int8_tbl[])
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', quote ':')
;
----------
[CREATE_TABLE] -- can't have generated column that is a child of normal column
CREATE TABLE gtest_normal (a int, b int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER always_end ON ddl_command_end
EXECUTE PROCEDURE event_trigger_test()
;
----------
[CREATE_TABLE] create temp table tt6(f1 int, f2 int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."pitest1_p1_like" ALTER COLUMN "f3" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."pitest1_p1_like_f3_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR === (
    LEFTARG = boolean,
    RIGHTARG = boolean,
    PROCEDURE = alter_op_test_fn,
    COMMUTATOR = ===,
    NEGATOR = !==,
    RESTRICT = customcontsel,
    JOIN = contjoinsel,
    HASHES, MERGES
)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS test_schema_exists
;
----------
[CREATE_TABLE] create table tbl1a (a int, c int)
;
----------
[DROP_TABLE] DROP TABLE testpub_rf_schema1.testpub_rf_tbl5
;
----------
[CREATE_PROG_OBJ] create or replace function dfunc(a varchar = 'def a', out _a varchar, x numeric = NULL, out _c numeric)
returns record as $$
  select $1, $2;
$$ language sql
;
----------
[DROP_TABLE] DROP TABLE attmp7
;
----------
[ALTER_INDEX] -- ok, as true
ALTER INDEX dummy_test_idx SET (option_bool = 3.4)
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_int8_ops
DEFAULT FOR TYPE int8 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_int8_consistent (internal, int8, int2, oid, internal),
	FUNCTION	2	gbt_int8_union (internal, internal),
	FUNCTION	3	gbt_int8_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_int8_penalty (internal, internal, internal),
	FUNCTION	6	gbt_int8_picksplit (internal, internal),
	FUNCTION	7	gbt_int8_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[DROP_INDEX] -- error, references aliased away name

drop index key_index
;
----------
[CREATE_SCHEMA] CREATE SCHEMA import_dest2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS money_ops
DEFAULT FOR TYPE money USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       cash_cmp(money,money),
    FUNCTION        2       gin_extract_value_money(money, internal),
    FUNCTION        3       gin_extract_query_money(money, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_money(money,money,int2, internal),
STORAGE         money
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 16549 IS 'comment on large object'
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_largeobject
;
----------
[ADMIN_TABLE] ANALYZE regress_indexing.pk1
;
----------
[CREATE_TABLE] -- partitioned table cannot participate in regular inheritance
CREATE TABLE partitioned2 (
	a int,
	b text
) PARTITION BY RANGE ((a+1), substr(b, 1, 5))
;
----------
[CREATE_PROG_OBJ] -- UPDATE on view with INSTEAD OF trigger
CREATE FUNCTION joinview_upd_trig_fn() RETURNS trigger
LANGUAGE plpgsql AS
$$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING new.f1, new.f4 INTO new.f1, new.f4;  -- should fail
  RETURN NEW;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_amop
;
----------
[DROP_POLICY] DROP TEXT SEARCH TEMPLATE IF EXISTS test_tstemplate_exists
;
----------
[DROP_SCHEMA] DROP SCHEMA sch2 cascade
;
----------
[CREATE_PROG_OBJ] create function pslot_backlink_view(bpchar)
returns text as '
<<outer>>
declare
    rec		record;
    bltype	char(2);
    retval	text;
begin
    select into rec * from PSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if rec.backlink = '''' then
        return ''-'';
    end if;
    bltype := substr(rec.backlink, 1, 2);
    if bltype = ''PL'' then
        declare
	    rec		record;
	begin
	    select into rec * from PLine where slotname = "outer".rec.backlink;
	    retval := ''Phone line '' || trim(rec.phonenumber);
	    if rec.comment != '''' then
	        retval := retval || '' ('';
		retval := retval || rec.comment;
		retval := retval || '')'';
	    end if;
	    return retval;
	end;
    end if;
    if bltype = ''WS'' then
        select into rec * from WSlot where slotname = rec.backlink;
	retval := trim(rec.slotname) || '' in room '';
	retval := retval || trim(rec.roomno);
	retval := retval || '' -> '';
	return retval || wslot_slotlink_view(rec.slotname);
    end if;
    return rec.backlink;
end;
' language plpgsql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add foreign key (a) references atacc2(id)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28198 RENAME TO reind_con_toast
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub5
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE FKTABLE ALTER CONSTRAINT fkdi2 DEFERRABLE INITIALLY IMMEDIATE
;
----------
[CREATE_TYPE] CREATE TYPE "public"."rainbow" AS ENUM (
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t1 ADD COLUMN c text
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pg_visibility
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_options(internal)
RETURNS void
AS '$libdir/pg_trgm', 'gtrgm_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_enum = 'one')
;
----------
[CREATE_POLICY] CREATE RULE "shoelace_upd" AS
    ON UPDATE TO "public"."shoelace" DO INSTEAD  UPDATE "public"."shoelace_data" SET "sl_name" = "new"."sl_name", "sl_avail" = "new"."sl_avail", "sl_color" = "new"."sl_color", "sl_len" = "new"."sl_len", "sl_unit" = "new"."sl_unit"
  WHERE ("shoelace_data"."sl_name" = "old"."sl_name")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rules_src" (
    "f1" integer,
    "f2" integer DEFAULT 0
)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE serialTest1 CYCLE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."inh_test" (
    "a" integer,
    "b" character(1) NOT NULL
)
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW domain_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(n.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(CASE WHEN condeferrable THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS is_deferrable,
           CAST(CASE WHEN condeferred THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS initially_deferred
    FROM pg_namespace rs, pg_namespace n, pg_constraint con, pg_type t
    WHERE rs.oid = con.connamespace
          AND n.oid = t.typnamespace
          AND t.oid = con.contypid
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
----------
[DROP_INDEX] drop index op_index_key
;
----------
[DROP_TRIGGER] DROP TRIGGER modified_modified_a ON main_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int8_dist(int8, int8)
RETURNS int8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] -- On the other hand, this should work because int implicitly promotes to
-- numeric, and we allow promotion on the FK side
CREATE TEMP TABLE PKTABLE (ptest1 numeric PRIMARY KEY)
;
