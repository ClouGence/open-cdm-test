create table idxpart2 partition of idxpart
for values from (0) to (1000) partition by range (b)
;
-- check behavior with type coercion required for a set-op

create or replace function rngfuncbar() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar ; $$
language sql stable
;
ANALYZE public.rtest_unitfact
;
DROP TABLE toasttest
;
CREATE TABLE pk4 (LIKE pk)
;
-- OFFSET not supported
CREATE VIEW ro_view9 AS SELECT a, b FROM base_tbl ORDER BY a LIMIT 1
;
create table part1 partition of range_parted for values from ('a', 1) to ('a', 10)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_conversion
;
do $$
declare i int := 0;
begin
  <<outermostwhile>>
  while i < 2 loop
    raise notice 'outermostwhile, i = %', i;
    i := i + 1;
    <<outerwhile>>
    while 1 > 0 loop
      <<innerwhile>>
      while 1 > 0 loop
        continue outermostwhile;
        raise notice 'should not get here';
      end loop;
      raise notice 'should not get here, either';
    end loop;
    raise notice 'nor here';
  end loop;
  raise notice 'out of outermostwhile, i = %', i;
end$$
;
CREATE AGGREGATE myaggn02a(*) (SFUNC = stfnp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13160 host=/tmp/HlInQjKngU dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
CREATE INDEX idx_float4 ON test_float4 USING gin (i)
;
create table mcrparted4 partition of mcrparted for values from (20, 10, 10) to (20, 20, 20)
;
--fails

drop table p1 cascade
;
CREATE INDEX "gpolygonind" ON "public"."polygon_tbl" USING "gist" ("f1")
;
alter table alterlock alter column f2 set default 'x'
;
CREATE FUNCTION isnlt(ismn13, ismn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE PROCEDURE transaction_test5()
LANGUAGE plpgsql
SET work_mem = 555
AS $$
BEGIN
    COMMIT;
END;
$$
;
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD STORAGE invalid_storage
;
alter table target drop CONSTRAINT target_pkey
;
CREATE TEMPORARY TABLE result AS
(SELECT * from dblink_get_result('dtest1') as t1(f1 int, f2 text, f3 text[]))
UNION
(SELECT * from dblink_get_result('dtest2') as t2(f1 int, f2 text, f3 text[]))
UNION
(SELECT * from dblink_get_result('dtest3') as t3(f1 int, f2 text, f3 text[]))
ORDER by f1
;
alter table defpart_attach_test attach partition defpart_attach_test_2 for values in (2)
;
DROP FUNCTION getrngfunc2(int)
;
create or replace function dfunc(a variadic int[] default array[]::int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
-- check routing error through a list partitioned table when the key is null
create table lparted_nonullpart (a int, b char) partition by list (b)
;
CREATE TABLE testschema.test_tab(a int, b int, c int)
;
-- fail

DROP MATERIALIZED VIEW ext_cine_mv
;
CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=29891 host=/tmp/7Wj1Y48wnU dbname=postgres' PUBLICATION tap_pub_B WITH (origin = none, copy_data = on)
;
ALTER INDEX alter_idx_rename_test RENAME TO alter_idx_rename_test_2
;
create temp table log (q1 int8, q2 int8)
;
CREATE TABLE accounts (accountid text NOT NULL PRIMARY KEY, balance numeric not null)
;
CREATE FUNCTION logging_msfunc_strict(text, anyelement) RETURNS text AS
$$ SELECT $1 || '+' || quote_nullable($2) $$
LANGUAGE SQL STRICT IMMUTABLE
;
CREATE OPERATOR ? (
        LEFTARG = ltree,
	RIGHTARG = _lquery,
	PROCEDURE = lt_q_regex,
	COMMUTATOR = '?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ALTER TABLE atnotnull1
  ADD COLUMN a INT,
  ALTER a SET NOT NULL
;
CREATE FUNCTION ltq_regex(ltree,lquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create trigger upsert_before_trig before insert or update on upsert
  for each row execute procedure upsert_before_func()
;
CREATE VIEW pg_statio_sys_tables AS
    SELECT * FROM pg_statio_all_tables
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
-- and check a related issue where we miscompute required relids for
-- a PHV that's been translated to a child rel
create temp table parttbl (a integer primary key) partition by range (a)
;
DROP TRIGGER c1_delete_trig ON part_c_1_100
;
-- Should fail. In gist throw an error when giving different data types for function argument
-- without defining left / right type in ALTER OPERATOR FAMILY ... ADD FUNCTION
CREATE OPERATOR FAMILY alt_opf16 USING gist
;
ALTER TABLE testpub_tbl5d REPLICA IDENTITY FULL
;
CREATE TRIGGER trigtest_after_row AFTER INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH ROW
EXECUTE PROCEDURE dummy_trigger()
;
CREATE TABLE foo4 PARTITION OF foo FOR VALUES IN (4)
;
ANALYZE public.room
;
create table idxpart (a int, b int) partition by range (a, b)
;
-- fails

CREATE COLLATION mycoll1 FROM "C"
;
create type textrange as range (subtype=text, collation="C")
;
CREATE UNIQUE INDEX cwi_uniq2_idx ON cwi_test(b , a)
;
CREATE VIEW "public"."xmlview5" AS
 SELECT XMLPARSE(CONTENT '<abc>x</abc>'::"text" STRIP WHITESPACE) AS "xmlparse"
;
drop table bool_rp
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table idxpart2 partition of idxpart
for values from (0) to (1000) partition by range (b)
;
----------
[CREATE_PROG_OBJ] -- check behavior with type coercion required for a set-op

create or replace function rngfuncbar() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar ; $$
language sql stable
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_unitfact
;
----------
[DROP_TABLE] DROP TABLE toasttest
;
----------
[CREATE_TABLE] CREATE TABLE pk4 (LIKE pk)
;
----------
[CREATE_VIEW(SELECT)] -- OFFSET not supported
CREATE VIEW ro_view9 AS SELECT a, b FROM base_tbl ORDER BY a LIMIT 1
;
----------
[CREATE_TABLE] create table part1 partition of range_parted for values from ('a', 1) to ('a', 10)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_conversion
;
----------
[BLOCK] do $$
declare i int := 0;
begin
  <<outermostwhile>>
  while i < 2 loop
    raise notice 'outermostwhile, i = %', i;
    i := i + 1;
    <<outerwhile>>
    while 1 > 0 loop
      <<innerwhile>>
      while 1 > 0 loop
        continue outermostwhile;
        raise notice 'should not get here';
      end loop;
      raise notice 'should not get here, either';
    end loop;
    raise notice 'nor here';
  end loop;
  raise notice 'out of outermostwhile, i = %', i;
end$$
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn02a(*) (SFUNC = stfnp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13160 host=/tmp/HlInQjKngU dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
----------
[ADD_INDEX] CREATE INDEX idx_float4 ON test_float4 USING gin (i)
;
----------
[CREATE_TABLE] create table mcrparted4 partition of mcrparted for values from (20, 10, 10) to (20, 20, 20)
;
----------
[DROP_TABLE] --fails

drop table p1 cascade
;
----------
[ADD_INDEX] CREATE INDEX "gpolygonind" ON "public"."polygon_tbl" USING "gist" ("f1")
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table alterlock alter column f2 set default 'x'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ismn13, ismn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test5()
LANGUAGE plpgsql
SET work_mem = 555
AS $$
BEGIN
    COMMIT;
END;
$$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf4 USING btree ADD STORAGE invalid_storage
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table target drop CONSTRAINT target_pkey
;
----------
[CREATE_TABLE|SELECT] CREATE TEMPORARY TABLE result AS
(SELECT * from dblink_get_result('dtest1') as t1(f1 int, f2 text, f3 text[]))
UNION
(SELECT * from dblink_get_result('dtest2') as t2(f1 int, f2 text, f3 text[]))
UNION
(SELECT * from dblink_get_result('dtest3') as t3(f1 int, f2 text, f3 text[]))
ORDER by f1
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table defpart_attach_test attach partition defpart_attach_test_2 for values in (2)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION getrngfunc2(int)
;
----------
[CREATE_PROG_OBJ] create or replace function dfunc(a variadic int[] default array[]::int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
----------
[CREATE_TABLE] -- check routing error through a list partitioned table when the key is null
create table lparted_nonullpart (a int, b char) partition by list (b)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.test_tab(a int, b int, c int)
;
----------
[DROP_VIEW] -- fail

DROP MATERIALIZED VIEW ext_cine_mv
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=29891 host=/tmp/7Wj1Y48wnU dbname=postgres' PUBLICATION tap_pub_B WITH (origin = none, copy_data = on)
;
----------
[RENAME_INDEX] ALTER INDEX alter_idx_rename_test RENAME TO alter_idx_rename_test_2
;
----------
[CREATE_TABLE] create temp table log (q1 int8, q2 int8)
;
----------
[CREATE_TABLE] CREATE TABLE accounts (accountid text NOT NULL PRIMARY KEY, balance numeric not null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION logging_msfunc_strict(text, anyelement) RETURNS text AS
$$ SELECT $1 || '+' || quote_nullable($2) $$
LANGUAGE SQL STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ? (
        LEFTARG = ltree,
	RIGHTARG = _lquery,
	PROCEDURE = lt_q_regex,
	COMMUTATOR = '?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE atnotnull1
  ADD COLUMN a INT,
  ALTER a SET NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltq_regex(ltree,lquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger upsert_before_trig before insert or update on upsert
  for each row execute procedure upsert_before_func()
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_statio_sys_tables AS
    SELECT * FROM pg_statio_all_tables
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
----------
[CREATE_TABLE] -- and check a related issue where we miscompute required relids for
-- a PHV that's been translated to a child rel
create temp table parttbl (a integer primary key) partition by range (a)
;
----------
[DROP_TRIGGER] DROP TRIGGER c1_delete_trig ON part_c_1_100
;
----------
[CREATE_PROG_OBJ] -- Should fail. In gist throw an error when giving different data types for function argument
-- without defining left / right type in ALTER OPERATOR FAMILY ... ADD FUNCTION
CREATE OPERATOR FAMILY alt_opf16 USING gist
;
----------
[ALTER_TABLE] ALTER TABLE testpub_tbl5d REPLICA IDENTITY FULL
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trigtest_after_row AFTER INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH ROW
EXECUTE PROCEDURE dummy_trigger()
;
----------
[CREATE_TABLE] CREATE TABLE foo4 PARTITION OF foo FOR VALUES IN (4)
;
----------
[ADMIN_TABLE] ANALYZE public.room
;
----------
[CREATE_TABLE] create table idxpart (a int, b int) partition by range (a, b)
;
----------
[CREATE_POLICY] -- fails

CREATE COLLATION mycoll1 FROM "C"
;
----------
[CREATE_POLICY] create type textrange as range (subtype=text, collation="C")
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX cwi_uniq2_idx ON cwi_test(b , a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."xmlview5" AS
 SELECT XMLPARSE(CONTENT '<abc>x</abc>'::"text" STRIP WHITESPACE) AS "xmlparse"
;
----------
[DROP_TABLE] drop table bool_rp
;
