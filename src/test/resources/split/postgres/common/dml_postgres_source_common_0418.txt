INSERT INTO tab_no_replidentity_index VALUES(1)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pitest2_p2"'::pg_catalog.regclass
  AND attname IN ('f1', 'f2', 'f3')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4660', relminmxid = '1'
WHERE oid = '"public"."c"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."mlparted_def1"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."atnnpart1"'::pg_catalog.regclass
  AND attname IN ('col1', 'id')
;
INSERT INTO public.tplain VALUES ('6', 6)
;
-- failure
INSERT INTO rls_test_permissive VALUES ('regress_r4','regress_s4',7)
;
INSERT INTO TIMESTAMPTZ_TBL VALUES ('Jan 01 17:32:01 2001')
;
INSERT INTO num_exp_add VALUES (3,0,'4.31')
;
-- fail
insert into ddtest2 values(2)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14546', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p1"'::pg_catalog.regclass
;
insert into tab_part_attach values (1)
;
INSERT INTO num_exp_sub VALUES (2,2,'0')
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."gtest_child2"'::pg_catalog.regclass
  AND attname IN ('f1', 'f2', 'f3')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest27"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest1_y"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."gtest10a"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (5, 85)
;
INSERT INTO public.ths_p1 VALUES (6, 766)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3035', relminmxid = '1'
WHERE oid = '"public"."onek2"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('2', 652)
;
INSERT INTO public.tplain VALUES ('0', 390)
;
INSERT INTO "tab'le" VALUES (33)
;
insert into rtest_nothn2 values (10, 'too small')
;
insert into at_partitioned values (512, '0.123')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1536', relminmxid = '1'
WHERE oid = '"public"."c_star"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14229', relminmxid = '3'
WHERE oid = '"public"."prt1_n_p2"'::pg_catalog.regclass
;
-- set using existing table
insert into dropcol(key, drop1, keep1, drop2, keep2) values(1, 3, '3', '3', 3) on conflict(key)
    do update set drop1 = dropcol.drop1, keep1 = dropcol.keep1, drop2 = dropcol.drop2, keep2 = dropcol.keep2
    returning *
;
INSERT INTO PKTABLE VALUES (4, 8, 'Test4')
;
INSERT INTO public.tplain VALUES ('8', 258)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2065', relminmxid = '1'
WHERE oid = '"public"."dept"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."itest9"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '"public"."log_table"'::pg_catalog.regclass
;
INSERT INTO t3 VALUES ( 'dd', 33 )
;
INSERT INTO BIT_SHIFT_TABLE SELECT b>>2 FROM BIT_SHIFT_TABLE
;
-- insert a few rows in the target table (odd numbered tid)
INSERT INTO pa_target SELECT id, id * 100, 'initial' FROM generate_series(1,14,2) AS id
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (5, 1, 33199, 3718, CURRENT_TIMESTAMP)
;
INSERT INTO tab_rowfilter_partitioned_2 (a, b) VALUES(1, 1),(20, 20)
;
insert into WSlot values ('WS.001.1b', '001', '', '')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2900', relminmxid = '1'
WHERE oid = '19008'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."mvtest_tvmm"'::pg_catalog.regclass
;
INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(3,500) s(i)
;
INSERT INTO mcv_lists_partial (a, b, c)
     SELECT
         mod(i,10),
         mod(i,10),
         mod(i,10)
     FROM generate_series(0,999) s(i)
;
-- zero-length char
INSERT INTO VARCHAR_TBL (f1) VALUES ('')
;
INSERT INTO num_exp_add VALUES (8,6,'8496986223.68757431572672621257436634648368772473081887846765003074279255322456188404621827857612554765910678041003765241409149793494330798800')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2076', relminmxid = '1'
WHERE oid = '"public"."e_star"'::pg_catalog.regclass
;
WITH data(hat_name, hat_color) AS MATERIALIZED (
    VALUES ('h8', 'green'),
        ('h9', 'blue'),
        ('h7', 'forbidden')
)
INSERT INTO hats
    SELECT * FROM data
RETURNING *
;
INSERT INTO public.tplain VALUES ('0', 70)
;
UPDATE pgbench_branches SET bbalance = bbalance + 4821 WHERE bid = 1
;
DELETE FROM rw_view2
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO tab_no_replidentity_index VALUES(1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pitest2_p2"'::pg_catalog.regclass
  AND attname IN ('f1', 'f2', 'f3')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4660', relminmxid = '1'
WHERE oid = '"public"."c"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."mlparted_def1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."atnnpart1"'::pg_catalog.regclass
  AND attname IN ('col1', 'id')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 6)
;
----------
[INSERT] -- failure
INSERT INTO rls_test_permissive VALUES ('regress_r4','regress_s4',7)
;
----------
[INSERT] INSERT INTO TIMESTAMPTZ_TBL VALUES ('Jan 01 17:32:01 2001')
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (3,0,'4.31')
;
----------
[INSERT] -- fail
insert into ddtest2 values(2)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14546', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p1"'::pg_catalog.regclass
;
----------
[INSERT] insert into tab_part_attach values (1)
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (2,2,'0')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."gtest_child2"'::pg_catalog.regclass
  AND attname IN ('f1', 'f2', 'f3')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest27"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest1_y"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."gtest10a"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 85)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 766)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3035', relminmxid = '1'
WHERE oid = '"public"."onek2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 652)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 390)
;
----------
[INSERT] INSERT INTO "tab'le" VALUES (33)
;
----------
[INSERT] insert into rtest_nothn2 values (10, 'too small')
;
----------
[INSERT] insert into at_partitioned values (512, '0.123')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1536', relminmxid = '1'
WHERE oid = '"public"."c_star"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14229', relminmxid = '3'
WHERE oid = '"public"."prt1_n_p2"'::pg_catalog.regclass
;
----------
[MERGE|SELECT] -- set using existing table
insert into dropcol(key, drop1, keep1, drop2, keep2) values(1, 3, '3', '3', 3) on conflict(key)
    do update set drop1 = dropcol.drop1, keep1 = dropcol.keep1, drop2 = dropcol.drop2, keep2 = dropcol.keep2
    returning *
;
----------
[INSERT] INSERT INTO PKTABLE VALUES (4, 8, 'Test4')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 258)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2065', relminmxid = '1'
WHERE oid = '"public"."dept"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."itest9"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '"public"."log_table"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO t3 VALUES ( 'dd', 33 )
;
----------
[INSERT|SELECT] INSERT INTO BIT_SHIFT_TABLE SELECT b>>2 FROM BIT_SHIFT_TABLE
;
----------
[INSERT|SELECT] -- insert a few rows in the target table (odd numbered tid)
INSERT INTO pa_target SELECT id, id * 100, 'initial' FROM generate_series(1,14,2) AS id
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (5, 1, 33199, 3718, CURRENT_TIMESTAMP)
;
----------
[INSERT|SELECT] INSERT INTO tab_rowfilter_partitioned_2 (a, b) VALUES(1, 1),(20, 20)
;
----------
[INSERT] insert into WSlot values ('WS.001.1b', '001', '', '')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2900', relminmxid = '1'
WHERE oid = '19008'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."mvtest_tvmm"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(3,500) s(i)
;
----------
[INSERT|SELECT] INSERT INTO mcv_lists_partial (a, b, c)
     SELECT
         mod(i,10),
         mod(i,10),
         mod(i,10)
     FROM generate_series(0,999) s(i)
;
----------
[INSERT|SELECT] -- zero-length char
INSERT INTO VARCHAR_TBL (f1) VALUES ('')
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (8,6,'8496986223.68757431572672621257436634648368772473081887846765003074279255322456188404621827857612554765910678041003765241409149793494330798800')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2076', relminmxid = '1'
WHERE oid = '"public"."e_star"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] WITH data(hat_name, hat_color) AS MATERIALIZED (
    VALUES ('h8', 'green'),
        ('h9', 'blue'),
        ('h7', 'forbidden')
)
INSERT INTO hats
    SELECT * FROM data
RETURNING *
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 70)
;
----------
[UPDATE] UPDATE pgbench_branches SET bbalance = bbalance + 4821 WHERE bid = 1
;
----------
[DELETE] DELETE FROM rw_view2
;
