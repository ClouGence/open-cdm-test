ALTER TABLE "S 1"."T 1" RENAME TO "T 0"
;
alter table only parent enable always trigger tg
;
create table bool_lp_true partition of bool_lp for values in(true)
;
alter table part_cab attach partition part_abc_p1 for values in(3)
;
CREATE TABLE public.tst_arrays (
		a INTEGER[] PRIMARY KEY,
		b TEXT[],
		c FLOAT[],
		d INTERVAL[]
		)
;
CREATE RULE "r3" AS
    ON INSERT TO "public"."rules_src" DO  INSERT INTO "public"."rules_log" ("f1", "f2", "tag", "id") VALUES (NULL::integer,NULL::integer,'-'::"text",DEFAULT), ("new"."f1","new"."f2",'new'::"text",DEFAULT)
;
-- Add an index
CREATE INDEX i ON T(c_int, c_text)
;
CREATE TABLE inhe (ee text, LIKE inhx) inherits (ctlb)
;
CREATE TABLE tableam_parted_heap2 (a text, b int) PARTITION BY list (a) USING heap2
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_subscription
;
CREATE OPERATOR CLASS cube_ops
    DEFAULT FOR TYPE cube USING btree AS
        OPERATOR        1       < ,
        OPERATOR        2       <= ,
        OPERATOR        3       = ,
        OPERATOR        4       >= ,
        OPERATOR        5       > ,
        FUNCTION        1       cube_cmp(cube, cube)
;
CREATE COLLATION "public"."regress_pg_c_utf8" (provider = builtin, locale = 'C.UTF-8', version = '1')
;
CREATE TABLE hash_cleanup_heap(keycol INT)
;
CREATE FUNCTION isnlt(ismn13, ismn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE INDEX concur_reindex_partidx ON concur_reindex_part (id)
;
CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~" WITH TEMPLATE = template0 OID = 16386 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
-- Fail while setting improper values
create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = -10.0)
;
DROP TABLE test_composite
;
CREATE PROCEDURE "public"."ptest5"("a" integer, "b" "text", "c" integer DEFAULT 100)
    LANGUAGE "sql"
    AS $$
INSERT INTO cp_test VALUES(a, b);
INSERT INTO cp_test VALUES(c, b);
$$
;
ALTER SEQUENCE identity_dump_unlogged_a_seq SET LOGGED
;
ALTER TYPE "public"."planets" ADD VALUE 'uranus'
;
DROP TABLE image
;
VACUUM (PARALLEL 0, BUFFER_USAGE_LIMIT 128) test_io_vac_strategy
;
ANALYZE pg_temp_13.pg_temp_25980_2
;
DROP PUBLICATION testpub2_forschema
;
-- ERROR
TRUNCATE fd_pt1
;
CREATE FOREIGN TABLE ft_part_1_2 (a INT) SERVER s0
;
ANALYZE (SKIP_LOCKED) information_schema.sql_packages
;
CREATE TABLE "public"."part" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("a")
;
CREATE SCHEMA test_schema
CREATE TABLE t(i int) PARTITION BY RANGE (i)
CREATE TABLE p1 PARTITION OF t FOR VALUES FROM (1) TO (regress_seg_schema.exfun(2))
;
ALTER TABLE droppk2 DETACH PARTITION droppk21
;
create function current_function(text)
returns regprocedure as $$
declare
  fn_oid regprocedure;
begin
  get diagnostics fn_oid = pg_routine_oid;
  return fn_oid;
end;
$$ language plpgsql
;
--
-- Clean up
--
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_superuser_t:s0-s0:c0.c255
DROP TABLE IF EXISTS t1 CASCADE
;
do $$ declare a int[];
begin a := from onecol; raise notice 'a = %', a; end$$
;
CREATE TABLE plt2 (a int, b int, c text) PARTITION BY LIST(c)
;
VACUUM FULL test_maint_search_path.test_maint
;
ALTER DATABASE split_db_probe RENAME TO split_db_renamed
;
ALTER TABLE pg_toast.pg_toast_30405 RENAME TO reind_con_toast
;
CREATE SUBSCRIPTION sub_tab
	 CONNECTION 'port=19134 host=/tmp/v9rn_BUJzu dbname=postgres application_name=sub_tab'
	 PUBLICATION pub_tab
;
--
--  Test cube datatype
--

CREATE EXTENSION cube
;
VACUUM vacowned_part1
;
-- Verify that re-attaching does not validate the parent when another
-- child index is still invalid.
create table idxpart (a int, b int) partition by range (a)
;
DROP TABLE temp_parted, perm_part
;
ANALYZE pg_temp_13.pg_temp_25000_2
;
CREATE POLICY "p2" ON "regress_rls_schema"."rls_tbl_force" FOR SELECT USING (("c1" = 8))
;
-- fail


-- test as part of prepare/execute
CREATE FUNCTION test3() RETURNS void
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
rv = plpy.execute("SELECT 'aa=>bb, cc=>NULL'::hstore AS col1")
assert(rv[0]["col1"] == {'aa': 'bb', 'cc': None})

val = {'a': 1, 'b': 'boo', 'c': None}
plan = plpy.prepare("SELECT $1::text AS col1", ["hstore"])
rv = plpy.execute(plan, [val])
assert(rv[0]["col1"] == '"a"=>"1", "b"=>"boo", "c"=>NULL')
$$
;
ALTER TABLE pg_temp_26.pg_temp_27076_2 ADD COLUMN newdata pg_temp_26.pg_temp_27076
;
CREATE TABLE test_dc(id serial primary key, data int)
;
CREATE DOMAIN testdomain_sv AS text COLLATE "sv-x-icu"
;
DROP TABLE ltable
;
------------------------------------------------------------------------------------------
[RENAME_TABLE] ALTER TABLE "S 1"."T 1" RENAME TO "T 0"
;
----------
[ALTER_TABLE] alter table only parent enable always trigger tg
;
----------
[CREATE_TABLE] create table bool_lp_true partition of bool_lp for values in(true)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table part_cab attach partition part_abc_p1 for values in(3)
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_arrays (
		a INTEGER[] PRIMARY KEY,
		b TEXT[],
		c FLOAT[],
		d INTERVAL[]
		)
;
----------
[CREATE_POLICY] CREATE RULE "r3" AS
    ON INSERT TO "public"."rules_src" DO  INSERT INTO "public"."rules_log" ("f1", "f2", "tag", "id") VALUES (NULL::integer,NULL::integer,'-'::"text",DEFAULT), ("new"."f1","new"."f2",'new'::"text",DEFAULT)
;
----------
[ADD_INDEX] -- Add an index
CREATE INDEX i ON T(c_int, c_text)
;
----------
[CREATE_TABLE] CREATE TABLE inhe (ee text, LIKE inhx) inherits (ctlb)
;
----------
[CREATE_TABLE] CREATE TABLE tableam_parted_heap2 (a text, b int) PARTITION BY list (a) USING heap2
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_subscription
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS cube_ops
    DEFAULT FOR TYPE cube USING btree AS
        OPERATOR        1       < ,
        OPERATOR        2       <= ,
        OPERATOR        3       = ,
        OPERATOR        4       >= ,
        OPERATOR        5       > ,
        FUNCTION        1       cube_cmp(cube, cube)
;
----------
[CREATE_POLICY] CREATE COLLATION "public"."regress_pg_c_utf8" (provider = builtin, locale = 'C.UTF-8', version = '1')
;
----------
[CREATE_TABLE] CREATE TABLE hash_cleanup_heap(keycol INT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ismn13, ismn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX concur_reindex_partidx ON concur_reindex_part (id)
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~" WITH TEMPLATE = template0 OID = 16386 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[ADD_INDEX] -- Fail while setting improper values
create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = -10.0)
;
----------
[DROP_TABLE] DROP TABLE test_composite
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest5"("a" integer, "b" "text", "c" integer DEFAULT 100)
    LANGUAGE "sql"
    AS $$
INSERT INTO cp_test VALUES(a, b);
INSERT INTO cp_test VALUES(c, b);
$$
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE identity_dump_unlogged_a_seq SET LOGGED
;
----------
[ALTER_TYPE] ALTER TYPE "public"."planets" ADD VALUE 'uranus'
;
----------
[DROP_TABLE] DROP TABLE image
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0, BUFFER_USAGE_LIMIT 128) test_io_vac_strategy
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25980_2
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub2_forschema
;
----------
[TRUNCATE_TABLE] -- ERROR
TRUNCATE fd_pt1
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft_part_1_2 (a INT) SERVER s0
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) information_schema.sql_packages
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("a")
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_schema
CREATE TABLE t(i int) PARTITION BY RANGE (i)
CREATE TABLE p1 PARTITION OF t FOR VALUES FROM (1) TO (regress_seg_schema.exfun(2))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE droppk2 DETACH PARTITION droppk21
;
----------
[CREATE_PROG_OBJ] create function current_function(text)
returns regprocedure as $$
declare
  fn_oid regprocedure;
begin
  get diagnostics fn_oid = pg_routine_oid;
  return fn_oid;
end;
$$ language plpgsql
;
----------
[DROP_TABLE] --
-- Clean up
--
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_superuser_t:s0-s0:c0.c255
DROP TABLE IF EXISTS t1 CASCADE
;
----------
[BLOCK] do $$ declare a int[];
begin a := from onecol; raise notice 'a = %', a; end$$
;
----------
[CREATE_TABLE] CREATE TABLE plt2 (a int, b int, c text) PARTITION BY LIST(c)
;
----------
[ADMIN_TABLE] VACUUM FULL test_maint_search_path.test_maint
;
----------
[RENAME_CATALOG] ALTER DATABASE split_db_probe RENAME TO split_db_renamed
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30405 RENAME TO reind_con_toast
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_tab
	 CONNECTION 'port=19134 host=/tmp/v9rn_BUJzu dbname=postgres application_name=sub_tab'
	 PUBLICATION pub_tab
;
----------
[CREATE_LIBRARY] --
--  Test cube datatype
--

CREATE EXTENSION cube
;
----------
[ADMIN_TABLE] VACUUM vacowned_part1
;
----------
[CREATE_TABLE] -- Verify that re-attaching does not validate the parent when another
-- child index is still invalid.
create table idxpart (a int, b int) partition by range (a)
;
----------
[DROP_TABLE] DROP TABLE temp_parted, perm_part
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25000_2
;
----------
[CREATE_POLICY] CREATE POLICY "p2" ON "regress_rls_schema"."rls_tbl_force" FOR SELECT USING (("c1" = 8))
;
----------
[CREATE_PROG_OBJ] -- fail


-- test as part of prepare/execute
CREATE FUNCTION test3() RETURNS void
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
rv = plpy.execute("SELECT 'aa=>bb, cc=>NULL'::hstore AS col1")
assert(rv[0]["col1"] == {'aa': 'bb', 'cc': None})

val = {'a': 1, 'b': 'boo', 'c': None}
plan = plpy.prepare("SELECT $1::text AS col1", ["hstore"])
rv = plpy.execute(plan, [val])
assert(rv[0]["col1"] == '"a"=>"1", "b"=>"boo", "c"=>NULL')
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_26.pg_temp_27076_2 ADD COLUMN newdata pg_temp_26.pg_temp_27076
;
----------
[CREATE_TABLE] CREATE TABLE test_dc(id serial primary key, data int)
;
----------
[CREATE_TYPE] CREATE DOMAIN testdomain_sv AS text COLLATE "sv-x-icu"
;
----------
[DROP_TABLE] DROP TABLE ltable
;
