create trigger xtrig
  after update on bar1
  referencing old table as old
  for each statement execute procedure xtrig()
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.date() < $.time())'))
;
CREATE TABLE ruletest_t1 (x int)
;
CREATE SUBSCRIPTION tap_sub_B_A
	CONNECTION 'port=31666 host=/tmp/EP7K8mZqpu dbname=postgres application_name=tap_sub_B_A'
	PUBLICATION tap_pub_A
	WITH (origin = none)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_transform
;
--
-- INET
--

-- prepare the table...

DROP TABLE INET_TBL
;
-- fail - already added
CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_tbl1
;
CREATE INDEX index_name9 ON "abc"."table_name8" using btree(SUBSTRING( column_name_1 from 1 for 11 ))
;
CREATE TRIGGER t1_br_insert BEFORE INSERT OR UPDATE
    ON "S 1"."T 1" FOR EACH ROW EXECUTE PROCEDURE "S 1".F_BRTRIG()
;
-- foreign key referencing catalog
CREATE TABLE foo (a oid, b oid, c int, FOREIGN KEY (a, b, c) REFERENCES pg_description)
;
DROP TABLE pgss_test
;
ALTER TABLE ONLY "public"."pagg_tab_ml_p3" ATTACH PARTITION "public"."pagg_tab_ml_p3_s1" FOR VALUES FROM (0) TO (7)
;
ALTER TABLE pg_toast.pg_toast_28293 RENAME TO reind_con_toast
;
ANALYZE fkpart5.fk2
;
ALTER TABLE ONLY "generated_stored_tests"."gtest_parent" ATTACH PARTITION "generated_stored_tests"."gtest_child2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
CREATE POLICY p3_with_all ON document FOR ALL
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
  WITH CHECK (dauthor = current_user)
;
CREATE FUNCTION gbt_enum_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE testpub_tbl5d (a int PRIMARY KEY DEFERRABLE)
;
create function mr_inoutparam_succeed(out i anyelement, inout r anymultirange)
  as $$ select upper($1), $1 $$ language sql
;
DROP TABLESPACE regress_ts2
;
alter table renameColumn add column w int
;
-- Basic table
create table tprt (col1 int) partition by range (col1)
;
CREATE TABLE public.test_arrays (
		a INTEGER[] PRIMARY KEY,
		b NUMERIC[],
		c TEXT[]
		)
;
-- This one ignores castcontext, so it will allow cases where an explicit
-- (but still binary) cast would be required to convert the input type.
-- We don't currently use this for any tests in this file, but it is a
-- reasonable alternative definition for some scenarios.
create function explicitly_binary_coercible(oid, oid) returns bool as $$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$$ language plpgsql strict stable
;
CREATE EXTENSION hstore_plperlu CASCADE
;
DROP DOMAIN j_ordered_pair
;
DO LANGUAGE plpgsql $$
BEGIN
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
    RESET TRANSACTION ISOLATION LEVEL;
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
END;
$$
;
CREATE TABLE foo_ptbl_ones PARTITION OF foo_ptbl FOR VALUES FROM ('0') TO ('10')
;
ANALYZE (VERBOSE true, SKIP_LOCKED true, BUFFER_USAGE_LIMIT '128 kB') split_system_admin.maint_table (payload, qty)
;
alter table atacc1 alter a set default 3
;
COMMENT ON TYPE ismn13
	IS 'International Standard Music Number 13 (ISMN13)'
;
DROP TABLE pg_temp_3.pg_temp_27598_2, pg_temp_3.pg_temp_27598
;
DROP TABLE trunc_a, ref_c
;
ALTER PUBLICATION pub ADD TABLE tbl1
;
CREATE TABLE brinopers_multi (colname name, typ text,
	op text[], value text[], matches int[],
	check (cardinality(op) = cardinality(value)),
	check (cardinality(op) = cardinality(matches)))
;
ALTER OPERATOR FAMILY gist_inet_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE SUBSCRIPTION tap_sub_copy
	CONNECTION 'port=22425 host=/tmp/lyw0fq4545 dbname=postgres application_name=appname_copy'
	PUBLICATION tap_pub_copy
	WITH (two_phase=on, copy_data=false)
;
CREATE SUBSCRIPTION tap_sub_copy
	CONNECTION 'port=11528 host=/tmp/qAw2OODPba dbname=postgres application_name=appname_copy'
	PUBLICATION tap_pub_copy
	WITH (two_phase=on, copy_data=false)
;
CREATE VIEW pg_stat_wal_receiver AS
    SELECT
            s.pid,
            s.status,
            s.receive_start_lsn,
            s.receive_start_tli,
            s.written_lsn,
            s.flushed_lsn,
            s.received_tli,
            s.last_msg_send_time,
            s.last_msg_receipt_time,
            s.latest_end_lsn,
            s.latest_end_time,
            s.slot_name,
            s.sender_host,
            s.sender_port,
            s.conninfo
    FROM pg_stat_get_wal_receiver() s
    WHERE s.pid IS NOT NULL
;
CREATE SCHEMA "generated_stored_tests"
;
ALTER TABLE pg_toast.pg_toast_31014 RENAME TO reind_con_toast
;
CREATE AGGREGATE myaggp03a(*) (SFUNC = stfp, STYPE = int4[],
  FINALFUNC = ffp, INITCOND = '{}')
;
alter index idxpart_pkey attach partition idxpart0_pkey
;
CREATE FUNCTION predicate_stable() RETURNS bool IMMUTABLE
LANGUAGE plpgsql AS $$
BEGIN
  EXECUTE 'SELECT txid_current()';
  RETURN true;
END; $$
;
CREATE TEMP TABLE pg_temp_12.pg_temp_25367_2 (tid pg_catalog.tid)
;
drop table patest0 cascade
;
ALTER TABLE public.skip_wal_skip_rewrite_index ADD CONSTRAINT skip_wal_skip_rewrite_index_pkey PRIMARY KEY (c)
;
CREATE FUNCTION gbt_bit_consistent(internal,bit,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- cleanup
DROP FUNCTION test_ambiguous_funcname(int)
;
create table pitable (f1 posint[])
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger xtrig
  after update on bar1
  referencing old table as old
  for each statement execute procedure xtrig()
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.date() < $.time())'))
;
----------
[CREATE_TABLE] CREATE TABLE ruletest_t1 (x int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_A
	CONNECTION 'port=31666 host=/tmp/EP7K8mZqpu dbname=postgres application_name=tap_sub_B_A'
	PUBLICATION tap_pub_A
	WITH (origin = none)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_transform
;
----------
[DROP_TABLE] --
-- INET
--

-- prepare the table...

DROP TABLE INET_TBL
;
----------
[CREATE_PUB_SUB] -- fail - already added
CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_tbl1
;
----------
[ADD_INDEX] CREATE INDEX index_name9 ON "abc"."table_name8" using btree(SUBSTRING( column_name_1 from 1 for 11 ))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER t1_br_insert BEFORE INSERT OR UPDATE
    ON "S 1"."T 1" FOR EACH ROW EXECUTE PROCEDURE "S 1".F_BRTRIG()
;
----------
[CREATE_TABLE] -- foreign key referencing catalog
CREATE TABLE foo (a oid, b oid, c int, FOREIGN KEY (a, b, c) REFERENCES pg_description)
;
----------
[DROP_TABLE] DROP TABLE pgss_test
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml_p3" ATTACH PARTITION "public"."pagg_tab_ml_p3_s1" FOR VALUES FROM (0) TO (7)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28293 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] ANALYZE fkpart5.fk2
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "generated_stored_tests"."gtest_parent" ATTACH PARTITION "generated_stored_tests"."gtest_child2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
----------
[CREATE_POLICY] CREATE POLICY p3_with_all ON document FOR ALL
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
  WITH CHECK (dauthor = current_user)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_enum_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl5d (a int PRIMARY KEY DEFERRABLE)
;
----------
[CREATE_PROG_OBJ] create function mr_inoutparam_succeed(out i anyelement, inout r anymultirange)
  as $$ select upper($1), $1 $$ language sql
;
----------
[DROP_TABLESPACE] DROP TABLESPACE regress_ts2
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table renameColumn add column w int
;
----------
[CREATE_TABLE] -- Basic table
create table tprt (col1 int) partition by range (col1)
;
----------
[CREATE_TABLE] CREATE TABLE public.test_arrays (
		a INTEGER[] PRIMARY KEY,
		b NUMERIC[],
		c TEXT[]
		)
;
----------
[CREATE_PROG_OBJ] -- This one ignores castcontext, so it will allow cases where an explicit
-- (but still binary) cast would be required to convert the input type.
-- We don't currently use this for any tests in this file, but it is a
-- reasonable alternative definition for some scenarios.
create function explicitly_binary_coercible(oid, oid) returns bool as $$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$$ language plpgsql strict stable
;
----------
[CREATE_LIBRARY] CREATE EXTENSION hstore_plperlu CASCADE
;
----------
[DROP_TYPE] DROP DOMAIN j_ordered_pair
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
    RESET TRANSACTION ISOLATION LEVEL;
    PERFORM 1;
    RAISE INFO '%', current_setting('transaction_isolation');
    COMMIT;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE foo_ptbl_ones PARTITION OF foo_ptbl FOR VALUES FROM ('0') TO ('10')
;
----------
[ADMIN_TABLE] ANALYZE (VERBOSE true, SKIP_LOCKED true, BUFFER_USAGE_LIMIT '128 kB') split_system_admin.maint_table (payload, qty)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter a set default 3
;
----------
[COMMENT_TYPE] COMMENT ON TYPE ismn13
	IS 'International Standard Music Number 13 (ISMN13)'
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_27598_2, pg_temp_3.pg_temp_27598
;
----------
[DROP_TABLE] DROP TABLE trunc_a, ref_c
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub ADD TABLE tbl1
;
----------
[CREATE_TABLE] CREATE TABLE brinopers_multi (colname name, typ text,
	op text[], value text[], matches int[],
	check (cardinality(op) = cardinality(value)),
	check (cardinality(op) = cardinality(matches)))
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_inet_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_copy
	CONNECTION 'port=22425 host=/tmp/lyw0fq4545 dbname=postgres application_name=appname_copy'
	PUBLICATION tap_pub_copy
	WITH (two_phase=on, copy_data=false)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_copy
	CONNECTION 'port=11528 host=/tmp/qAw2OODPba dbname=postgres application_name=appname_copy'
	PUBLICATION tap_pub_copy
	WITH (two_phase=on, copy_data=false)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_wal_receiver AS
    SELECT
            s.pid,
            s.status,
            s.receive_start_lsn,
            s.receive_start_tli,
            s.written_lsn,
            s.flushed_lsn,
            s.received_tli,
            s.last_msg_send_time,
            s.last_msg_receipt_time,
            s.latest_end_lsn,
            s.latest_end_time,
            s.slot_name,
            s.sender_host,
            s.sender_port,
            s.conninfo
    FROM pg_stat_get_wal_receiver() s
    WHERE s.pid IS NOT NULL
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "generated_stored_tests"
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_31014 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp03a(*) (SFUNC = stfp, STYPE = int4[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ALTER_INDEX] alter index idxpart_pkey attach partition idxpart0_pkey
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION predicate_stable() RETURNS bool IMMUTABLE
LANGUAGE plpgsql AS $$
BEGIN
  EXECUTE 'SELECT txid_current()';
  RETURN true;
END; $$
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_25367_2 (tid pg_catalog.tid)
;
----------
[DROP_TABLE] drop table patest0 cascade
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.skip_wal_skip_rewrite_index ADD CONSTRAINT skip_wal_skip_rewrite_index_pkey PRIMARY KEY (c)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bit_consistent(internal,bit,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_PROG_OBJ] -- cleanup
DROP FUNCTION test_ambiguous_funcname(int)
;
----------
[CREATE_TABLE] create table pitable (f1 posint[])
;
