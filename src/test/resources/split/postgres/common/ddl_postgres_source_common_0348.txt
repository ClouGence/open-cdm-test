ANALYZE pred_tab_notnull
;
DROP OPERATOR ###### (NONE, int4)
;
CREATE TABLE itest8 (f1 int)
;
ALTER TABLE intra_grant_inplace ADD PRIMARY KEY (c)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_shseclabel
;
-- fails due to overlap:
CREATE TABLE bigintp_10_2 PARTITION OF bigintp FOR VALUES IN ('10')
;
CREATE FUNCTION isnlt(isbn, isbn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
alter table atacc3 add constraint foo check (test2>0)
;
CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
DROP TABLE gtest11s, gtest12s
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_foreign_table
;
-- Create default partition
CREATE TABLE partr_def1 PARTITION OF range_parted DEFAULT
;
create table rlp_default partition of rlp default partition by list (a)
;
alter table atacc1 add constraint "atacc1_pkey" primary key (test)
;
COMMENT ON INDEX split_maint.maint_name_idx IS 'name lookup index'
;
TRUNCATE cp_test
;
-- This works...
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest2, ftest1) REFERENCES pktable(ptest2, ptest1))
;
-- After row triggers aren't allowed on views
CREATE TRIGGER invalid_trig AFTER INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_ins_row')
;
ALTER INDEX pg_toast.pg_toast_28073_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION gin_compare_prefix_date(date, date, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CLUSTER clstr_2
;
ALTER TABLE pg_ts_config_map ADD PRIMARY KEY USING INDEX pg_ts_config_map_index
;
-- invalid: non-lowercase quoted reloptions identifiers
CREATE TABLE tas_case WITH ("Fillfactor" = 10) AS SELECT 1 a
;
ALTER TABLE ONLY "public"."gtest_parent" ATTACH PARTITION "public"."gtest_child2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
CREATE FUNCTION public.drop_func_test1() RETURNS VOID AS 'select 2;' LANGUAGE SQL IMMUTABLE
;
create table gen_part_2
  partition of gen_part for values in (2)
;
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key UNIQUE(c)
;
CREATE FUNCTION "public"."namedparmcursor_test7"() RETURNS "void"
    LANGUAGE "plpgsql"
    AS $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77, p1 := 42/0);
end $$
;
ALTER TABLE part_b_10_b_20 ATTACH PARTITION part_c_100_200 FOR VALUES FROM (100) TO (200)
;
-- collation in public schema
create index pti3 on pt (tdata collate mycollation)
;
ANALYZE public.prt1_n_p2
;
-- use ALTER TABLE to add a unique constraint
create table idxpart (a int, b int) partition by range (a, b)
;
COMMENT ON DATABASE postgres IS 'default administrative connection database'
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_opfamily
;
DROP VIEW v1
;
CREATE CAST (int4 AS casttesttype) WITH FUNCTION int4_casttesttype(int4) AS IMPLICIT
;
-- test overflows during CREATE INDEX with extreme date values
CREATE TABLE brin_date_test(a DATE)
;
create function trap_foreign_key_2() returns int as $$
begin
	begin	-- start a subtransaction
		set constraints all immediate;
	exception
		when foreign_key_violation then
			raise notice 'caught foreign_key_violation';
			return 0;
	end;
	return 1;
end$$ language plpgsql
;
DROP TABLE gtest_parent
;
CREATE FUNCTION "public"."multi_datum_use"("p1" integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
declare
  x int;
  y int;
begin
  select into x,y unique1/p1, unique1/$1 from tenk1 group by unique1/p1;
  return x = y;
end$_$
;
DROP FUNCTION base_fn_out(base_type)
;
/*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
ALTER OPERATOR #>=# (hstore, hstore) SET (
       RESTRICT = scalargesel,
       JOIN = scalargejoinsel
)
;
CREATE FUNCTION equipment_named_ambiguous_1b(hobby hobbies_r)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where equipment_r.hobby = hobby.name'
   LANGUAGE SQL
;
-- test scrollable cursor support

create function sc_test() returns setof integer as $$
declare
  c scroll cursor for select f1 from int4_tbl;
  x integer;
begin
  open c;
  fetch last from c into x;
  while found loop
    return next x;
    fetch prior from c into x;
  end loop;
  close c;
end;
$$ language plpgsql
;
CREATE TRIGGER "gtest12_01" BEFORE UPDATE ON "public"."gtest26" FOR EACH ROW EXECUTE FUNCTION "public"."gtest_trigger_func"()
;
alter table donothingbrtrig_test attach partition donothingbrtrig_test1 for values in (1)
;
CREATE TABLE "public"."gtest20a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
create table stts_t2 (a int, b int, c int)
;
-- fail

DROP OPERATOR <<@@ (point, polygon)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pred_tab_notnull
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ###### (NONE, int4)
;
----------
[CREATE_TABLE] CREATE TABLE itest8 (f1 int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE intra_grant_inplace ADD PRIMARY KEY (c)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_shseclabel
;
----------
[CREATE_TABLE] -- fails due to overlap:
CREATE TABLE bigintp_10_2 PARTITION OF bigintp FOR VALUES IN ('10')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(isbn, isbn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc3 add constraint foo check (test2>0)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
----------
[DROP_TABLE] DROP TABLE gtest11s, gtest12s
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_foreign_table
;
----------
[CREATE_TABLE] -- Create default partition
CREATE TABLE partr_def1 PARTITION OF range_parted DEFAULT
;
----------
[CREATE_TABLE] create table rlp_default partition of rlp default partition by list (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint "atacc1_pkey" primary key (test)
;
----------
[COMMENT_INDEX] COMMENT ON INDEX split_maint.maint_name_idx IS 'name lookup index'
;
----------
[TRUNCATE_TABLE] TRUNCATE cp_test
;
----------
[CREATE_TABLE] -- This works...
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest2, ftest1) REFERENCES pktable(ptest2, ptest1))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- After row triggers aren't allowed on views
CREATE TRIGGER invalid_trig AFTER INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_ins_row')
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28073_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_date(date, date, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] CLUSTER clstr_2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_config_map ADD PRIMARY KEY USING INDEX pg_ts_config_map_index
;
----------
[CREATE_TABLE|SELECT] -- invalid: non-lowercase quoted reloptions identifiers
CREATE TABLE tas_case WITH ("Fillfactor" = 10) AS SELECT 1 a
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."gtest_parent" ATTACH PARTITION "public"."gtest_child2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION public.drop_func_test1() RETURNS VOID AS 'select 2;' LANGUAGE SQL IMMUTABLE
;
----------
[CREATE_TABLE] create table gen_part_2
  partition of gen_part for values in (2)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key UNIQUE(c)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."namedparmcursor_test7"() RETURNS "void"
    LANGUAGE "plpgsql"
    AS $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77, p1 := 42/0);
end $$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part_b_10_b_20 ATTACH PARTITION part_c_100_200 FOR VALUES FROM (100) TO (200)
;
----------
[ADD_INDEX] -- collation in public schema
create index pti3 on pt (tdata collate mycollation)
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_n_p2
;
----------
[CREATE_TABLE] -- use ALTER TABLE to add a unique constraint
create table idxpart (a int, b int) partition by range (a, b)
;
----------
[COMMENT_CATALOG] COMMENT ON DATABASE postgres IS 'default administrative connection database'
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_opfamily
;
----------
[DROP_VIEW] DROP VIEW v1
;
----------
[CREATE_PROG_OBJ] CREATE CAST (int4 AS casttesttype) WITH FUNCTION int4_casttesttype(int4) AS IMPLICIT
;
----------
[CREATE_TABLE] -- test overflows during CREATE INDEX with extreme date values
CREATE TABLE brin_date_test(a DATE)
;
----------
[CREATE_PROG_OBJ] create function trap_foreign_key_2() returns int as $$
begin
	begin	-- start a subtransaction
		set constraints all immediate;
	exception
		when foreign_key_violation then
			raise notice 'caught foreign_key_violation';
			return 0;
	end;
	return 1;
end$$ language plpgsql
;
----------
[DROP_TABLE] DROP TABLE gtest_parent
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."multi_datum_use"("p1" integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
declare
  x int;
  y int;
begin
  select into x,y unique1/p1, unique1/$1 from tenk1 group by unique1/p1;
  return x = y;
end$_$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION base_fn_out(base_type)
;
----------
[CREATE_POLICY] /*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR #>=# (hstore, hstore) SET (
       RESTRICT = scalargesel,
       JOIN = scalargejoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION equipment_named_ambiguous_1b(hobby hobbies_r)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where equipment_r.hobby = hobby.name'
   LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] -- test scrollable cursor support

create function sc_test() returns setof integer as $$
declare
  c scroll cursor for select f1 from int4_tbl;
  x integer;
begin
  open c;
  fetch last from c into x;
  while found loop
    return next x;
    fetch prior from c into x;
  end loop;
  close c;
end;
$$ language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "gtest12_01" BEFORE UPDATE ON "public"."gtest26" FOR EACH ROW EXECUTE FUNCTION "public"."gtest_trigger_func"()
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table donothingbrtrig_test attach partition donothingbrtrig_test1 for values in (1)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest20a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_TABLE] create table stts_t2 (a int, b int, c int)
;
----------
[DROP_PROG_OBJ] -- fail

DROP OPERATOR <<@@ (point, polygon)
;
