-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."regress_notnull2" INHERIT "public"."regress_notnull1"
;
-- error
ALTER FOREIGN TABLE tru_ftable OPTIONS (ADD truncatable 'true')
;
CREATE TABLE "fkpart3"."pk52" (
    "a" integer CONSTRAINT "pk5_a_not_null" NOT NULL
)
;
CREATE DATABASE newdb TEMPLATE template_db
;
CREATE SEQUENCE sequence_test13 AS smallint MINVALUE -32768
;
alter event trigger regress_event_trigger enable replica
;
-- test a self-referential FK
ALTER TABLE fk_partitioned_pk ADD CONSTRAINT selffk FOREIGN KEY (a, b) REFERENCES fk_partitioned_pk NOT VALID
;
CREATE TABLE employees (
    id INT NOT NULL,
    name VARCHAR(100),
    position VARCHAR(100),
    salary DECIMAL(10,2),
    PRIMARY KEY (id)
)
;
CREATE INDEX "iprt2_p3_b" ON "public"."prt2_p3" USING "btree" ("b")
;
create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = -10.0)
;
ALTER TABLE pg_toast.pg_toast_30984 RENAME TO reind_con_toast
;
CREATE TABLE tab_int AS SELECT generate_series(1, 10) AS a
;
create index idxpart_idx on only idxpart(a)
;
CREATE TABLE pagg_tab4_p2 PARTITION OF pagg_tab4 FOR VALUES IN ('B', 'A')
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_parameter_acl
;
CLUSTER abbrev_abort_uuids USING abbrev_abort_uuids__abort_increasing_idx
;
alter foreign table rem2 drop constraint rem2_f1positive
;
CREATE OR REPLACE FUNCTION blurt_and_lock_4(text) RETURNS text IMMUTABLE LANGUAGE plpgsql AS $$
    BEGIN
        RAISE NOTICE 'blurt_and_lock_4() called for % in session %', $1, current_setting('spec.session')::int;
        RAISE NOTICE 'acquiring advisory lock on 4';
        PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 4);
    RETURN $1;
    END;$$
;
DROP FUNCTION rngfunc()
;
CREATE TABLE inhf (LIKE inhx INCLUDING DEFAULTS INCLUDING CONSTRAINTS)
;
drop type ct1
;
CREATE FUNCTION gin_compare_prefix_cidr(cidr, cidr, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
drop table idxpart, idxpart2
;
ANALYZE public.tbl_include_box_pk
;
CREATE TABLE gtest25 (a int PRIMARY KEY)
;
CREATE PROCEDURE "public"."ptest6"("a" integer, "b" "anyelement")
    LANGUAGE "sql"
    AS $$
SELECT NULL::int;
$$
;
ALTER TABLE pg_temp_3.pg_temp_16656_2 ADD COLUMN newdata pg_temp_3.pg_temp_16656
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_default_acl
;
CREATE TABLE foo_tbl(a int, b text)
;
-- Shuffle self-joined relations. Only in the case of iterative deletion
-- attempts explains of these queries will be identical.
CREATE UNIQUE INDEX ON emp1((id*id))
;
CREATE TABLE tp2 PARTITION OF temporal_partitioned FOR VALUES IN ('[3,4)', '[4,5)')
;
DROP TABLE pg_temp_9.pg_temp_25561_2, pg_temp_9.pg_temp_25561
;
CREATE FUNCTION dblink_get_notify(
    conname TEXT,
    OUT notify_name TEXT,
    OUT be_pid INT4,
    OUT extra TEXT
)
RETURNS setof record
AS '$libdir/dblink', 'dblink_get_notify'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
create rule qqq as on insert to copydml_test do also delete from copydml_test
;
CREATE TABLE fkpart10.tbl5(f1 int REFERENCES fkpart10.tbl3)
;
create table WSlot (
    slotname	char(20),
    roomno	char(8),
    slotlink	char(20),
    backlink	char(20)
)
;
CREATE INDEX freespace_btree ON freespace_tab USING btree (c1)
;
ANALYZE pg_temp_14.pg_temp_26781_2
;
create table two_int8s_tab (f1 two_int8s)
;
CREATE TABLE fkpart13_t1 (a int PRIMARY KEY)
;
-- An inheritance child table gets the constraint, but it's valid
CREATE TABLE notnull_tbl1_child (a int, b int) INHERITS (notnull_tbl1)
;
create view rule_v1 as values(1,2)
;
DROP TABLE rngfunc
;
CREATE SEQUENCE toasttable_rand_seq START 79 INCREMENT 1499
;
drop procedure ptest10(in int, int, int)
;
ALTER TABLE ONLY "public"."prt2_n" ATTACH PARTITION "public"."prt2_n_p1" FOR VALUES IN ('0000', '0003', '0004', '0010', '0006', '0007')
;
-- relation ownership, WARNING logs generated as all are skipped.
CREATE TABLE vacowned (a int)
;
CREATE TEXT SEARCH PARSER alt_ts_prs1
    (start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
DROP TABLE pg_temp_12.pg_temp_26358_2, pg_temp_12.pg_temp_26358
;
-- ok
ALTER TABLE unlogged1 SET LOGGED
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."regress_notnull2" INHERIT "public"."regress_notnull1"
;
----------
[ALTER_TABLE] -- error
ALTER FOREIGN TABLE tru_ftable OPTIONS (ADD truncatable 'true')
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk52" (
    "a" integer CONSTRAINT "pk5_a_not_null" NOT NULL
)
;
----------
[CREATE_CATALOG] CREATE DATABASE newdb TEMPLATE template_db
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test13 AS smallint MINVALUE -32768
;
----------
[ALTER_TRIGGER] alter event trigger regress_event_trigger enable replica
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- test a self-referential FK
ALTER TABLE fk_partitioned_pk ADD CONSTRAINT selffk FOREIGN KEY (a, b) REFERENCES fk_partitioned_pk NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE employees (
    id INT NOT NULL,
    name VARCHAR(100),
    position VARCHAR(100),
    salary DECIMAL(10,2),
    PRIMARY KEY (id)
)
;
----------
[ADD_INDEX] CREATE INDEX "iprt2_p3_b" ON "public"."prt2_p3" USING "btree" ("b")
;
----------
[ADD_INDEX] create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = -10.0)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30984 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tab_int AS SELECT generate_series(1, 10) AS a
;
----------
[ADD_INDEX] create index idxpart_idx on only idxpart(a)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab4_p2 PARTITION OF pagg_tab4 FOR VALUES IN ('B', 'A')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_parameter_acl
;
----------
[ADMIN_TABLE] CLUSTER abbrev_abort_uuids USING abbrev_abort_uuids__abort_increasing_idx
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter foreign table rem2 drop constraint rem2_f1positive
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION blurt_and_lock_4(text) RETURNS text IMMUTABLE LANGUAGE plpgsql AS $$
    BEGIN
        RAISE NOTICE 'blurt_and_lock_4() called for % in session %', $1, current_setting('spec.session')::int;
        RAISE NOTICE 'acquiring advisory lock on 4';
        PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 4);
    RETURN $1;
    END;$$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION rngfunc()
;
----------
[CREATE_TABLE] CREATE TABLE inhf (LIKE inhx INCLUDING DEFAULTS INCLUDING CONSTRAINTS)
;
----------
[DROP_TYPE] drop type ct1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_cidr(cidr, cidr, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[DROP_TABLE] drop table idxpart, idxpart2
;
----------
[ADMIN_TABLE] ANALYZE public.tbl_include_box_pk
;
----------
[CREATE_TABLE] CREATE TABLE gtest25 (a int PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest6"("a" integer, "b" "anyelement")
    LANGUAGE "sql"
    AS $$
SELECT NULL::int;
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_16656_2 ADD COLUMN newdata pg_temp_3.pg_temp_16656
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] CREATE TABLE foo_tbl(a int, b text)
;
----------
[ADD_INDEX] -- Shuffle self-joined relations. Only in the case of iterative deletion
-- attempts explains of these queries will be identical.
CREATE UNIQUE INDEX ON emp1((id*id))
;
----------
[CREATE_TABLE] CREATE TABLE tp2 PARTITION OF temporal_partitioned FOR VALUES IN ('[3,4)', '[4,5)')
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_25561_2, pg_temp_9.pg_temp_25561
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_get_notify(
    conname TEXT,
    OUT notify_name TEXT,
    OUT be_pid INT4,
    OUT extra TEXT
)
RETURNS setof record
AS '$libdir/dblink', 'dblink_get_notify'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_POLICY] create rule qqq as on insert to copydml_test do also delete from copydml_test
;
----------
[CREATE_TABLE] CREATE TABLE fkpart10.tbl5(f1 int REFERENCES fkpart10.tbl3)
;
----------
[CREATE_TABLE] create table WSlot (
    slotname	char(20),
    roomno	char(8),
    slotlink	char(20),
    backlink	char(20)
)
;
----------
[ADD_INDEX] CREATE INDEX freespace_btree ON freespace_tab USING btree (c1)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_14.pg_temp_26781_2
;
----------
[CREATE_TABLE] create table two_int8s_tab (f1 two_int8s)
;
----------
[CREATE_TABLE] CREATE TABLE fkpart13_t1 (a int PRIMARY KEY)
;
----------
[CREATE_TABLE] -- An inheritance child table gets the constraint, but it's valid
CREATE TABLE notnull_tbl1_child (a int, b int) INHERITS (notnull_tbl1)
;
----------
[CREATE_VIEW(SELECT)] create view rule_v1 as values(1,2)
;
----------
[DROP_TABLE] DROP TABLE rngfunc
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE toasttable_rand_seq START 79 INCREMENT 1499
;
----------
[DROP_PROG_OBJ] drop procedure ptest10(in int, int, int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_n" ATTACH PARTITION "public"."prt2_n_p1" FOR VALUES IN ('0000', '0003', '0004', '0010', '0006', '0007')
;
----------
[CREATE_TABLE] -- relation ownership, WARNING logs generated as all are skipped.
CREATE TABLE vacowned (a int)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH PARSER alt_ts_prs1
    (start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_12.pg_temp_26358_2, pg_temp_12.pg_temp_26358
;
----------
[ALTER_TABLE] -- ok
ALTER TABLE unlogged1 SET LOGGED
;
