ALTER TABLE inhta ADD COLUMN i int, ADD COLUMN j bigint DEFAULT 1
;
CREATE TABLE "public"."idxpart2" (
    "a" integer
)
PARTITION BY RANGE ("a")
;
-- specified literal can't be cast to the partition column data type
CREATE TABLE bools (
	a bool
) PARTITION BY LIST (a)
;
VACUUM vacparted, does_not_exist
;
ALTER TABLE current_check ENABLE ROW LEVEL SECURITY
;
ALTER INDEX dummy_test_idx SET (option_enum = 0)
;
-- invalid indexing_method
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 6 < (int4, int2)
;
CREATE FUNCTION tuple_data_split(rel_oid oid,
    t_data bytea,
    t_infomask integer,
    t_infomask2 integer,
    t_bits text)
RETURNS bytea[]
AS '$libdir/pageinspect','tuple_data_split'
LANGUAGE C PARALLEL SAFE
;
CREATE OPERATOR "public".<% (
    FUNCTION = "public"."pt_in_widget",
    LEFTARG = "point",
    RIGHTARG = "public"."widget",
    COMMUTATOR = OPERATOR("public".>%),
    NEGATOR = OPERATOR("public".>=%)
)
;
create extension spgist_name_ops
;
CREATE MATERIALIZED VIEW "public"."mvtest_tmm" AS
 SELECT "sum"("totamt") AS "grandtot"
   FROM "public"."mvtest_tm"
  WITH NO DATA
;
CREATE RULE ucrule AS ON DELETE TO ucview DO INSTEAD
  DELETE FROM uctest WHERE f1 = OLD.f1
;
CREATE TABLE test_macaddr (
	i macaddr
)
;
CREATE TYPE test_type2 AS (a int, b text)
;
CREATE OPERATOR @> (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_contains,
	COMMUTATOR = '<@',
	RESTRICT = contsel, JOIN = contjoinsel
)
;
CREATE INDEX jidx_array ON testjsonb USING gin((j->'array'))
;
CREATE OR REPLACE FUNCTION joinview_upd_trig_fn() RETURNS trigger
LANGUAGE plpgsql AS
$$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING WITH (new AS n) new.f1, n.f4 INTO new.f1, new.f4;  -- now ok
  RETURN NEW;
END;
$$
;
CREATE INDEX "brin_summarize_idx" ON "public"."brin_summarize" USING "brin" ("value") WITH ("pages_per_range"='2')
;
CREATE TABLE event_trigger_test (a integer, b text)
;
CREATE INDEX "brin_test_b_idx" ON "public"."brin_test" USING "brin" ("b") WITH ("pages_per_range"='2')
;
create domain d_fail as anyelement
;
CREATE TABLE bug18634t (a int, b int, c text)
;
CREATE TABLE "public"."dupindexcols" (
    "id" integer,
    "f1" "text" COLLATE "pg_catalog"."C"
)
;
ALTER TABLE ONLY "public"."trigger_parted_p2"
    ADD CONSTRAINT "trigger_parted_p2_pkey" PRIMARY KEY ("a")
;
create or replace function stricttest() returns void as $$
-- override the global
#print_strict_params on
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- too many rows
  select * from foo where f1 > p1 or f1::text = p3  into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
CREATE INDEX test_gin_idx ON test USING gin (a,b)
;
create function explicitly_binary_coercible(oid, oid) returns bool as $$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$$ language plpgsql strict stable
;
ALTER DATABASE "contrib_regression" RESET auto_explain.bogus
;
ALTER TABLE "public"."fk_partitioned_fk_5"
    ADD CONSTRAINT "fk_partitioned_fk_5_a_b_fkey" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b") ON UPDATE CASCADE ON DELETE CASCADE DEFERRABLE
;
drop domain restrictedrange
;
CREATE FUNCTION btismncmp(ismn, ismn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create operator class part_test_text_ops
for type text
using hash as
operator 1 =,
function 2 part_hashtext_length(text, int8)
;
CREATE TABLE part_drop_index_locking (id int) PARTITION BY RANGE(id)
;
ALTER OPERATOR CLASS alt_opc2 USING hash SET SCHEMA alt_nsp2
;
CREATE MATERIALIZED VIEW mvtest_tmm AS SELECT sum(totamt) AS grandtot FROM mvtest_tm
;
CREATE TABLE prt2_e_p2 PARTITION OF prt2_e FOR VALUES FROM (250) TO (500)
;
CREATE INDEX idx_child_doc_gin ON split_cov.child_table USING gin (doc jsonb_path_ops) WHERE doc ? 'kind'
;
CREATE FUNCTION pg_truncate_visibility_map(regclass)
RETURNS void
AS '$libdir/pg_visibility', 'pg_truncate_visibility_map'
LANGUAGE C STRICT
PARALLEL UNSAFE
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp_tz() < $.datetime("HH:MI TZH"))'))
;
CREATE TABLE ATACC1 (a int)
;
VACUUM (FULL) pg_catalog.pg_collation
;
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE)
;
-- Validate we do not reduce a clone clause to a constant true or false
CREATE TABLE pred_tab (a int, b int)
;
--
-- partitioned by a single column
--
CREATE TABLE prt1 (a int, b int, c varchar) PARTITION BY RANGE(a)
;
-- fail

drop domain intdom1
;
/*
 * text search configuration for catalan language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * catalan and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY catalan_stem
	(TEMPLATE = snowball, Language = catalan )
;
create table brtrigpartcon (a int, b text) partition by list (a)
;
ALTER TABLE ONLY "public"."notnull_tbl4_lk3"
    ADD CONSTRAINT "notnull_tbl4_lk3_pkey" PRIMARY KEY ("a") DEFERRABLE INITIALLY DEFERRED
;
-- table inheritance tests
CREATE TABLE agg (a int2, b float4)
;
VACUUM (PARALLEL 2) pg_catalog.pg_parameter_acl
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE inhta ADD COLUMN i int, ADD COLUMN j bigint DEFAULT 1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart2" (
    "a" integer
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_TABLE] -- specified literal can't be cast to the partition column data type
CREATE TABLE bools (
	a bool
) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM vacparted, does_not_exist
;
----------
[ALTER_TABLE] ALTER TABLE current_check ENABLE ROW LEVEL SECURITY
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_enum = 0)
;
----------
[ALTER_PROG_OBJ] -- invalid indexing_method
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 6 < (int4, int2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tuple_data_split(rel_oid oid,
    t_data bytea,
    t_infomask integer,
    t_infomask2 integer,
    t_bits text)
RETURNS bytea[]
AS '$libdir/pageinspect','tuple_data_split'
LANGUAGE C PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".<% (
    FUNCTION = "public"."pt_in_widget",
    LEFTARG = "point",
    RIGHTARG = "public"."widget",
    COMMUTATOR = OPERATOR("public".>%),
    NEGATOR = OPERATOR("public".>=%)
)
;
----------
[CREATE_LIBRARY] create extension spgist_name_ops
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_tmm" AS
 SELECT "sum"("totamt") AS "grandtot"
   FROM "public"."mvtest_tm"
  WITH NO DATA
;
----------
[CREATE_POLICY] CREATE RULE ucrule AS ON DELETE TO ucview DO INSTEAD
  DELETE FROM uctest WHERE f1 = OLD.f1
;
----------
[CREATE_TABLE] CREATE TABLE test_macaddr (
	i macaddr
)
;
----------
[CREATE_TYPE] CREATE TYPE test_type2 AS (a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @> (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_contains,
	COMMUTATOR = '<@',
	RESTRICT = contsel, JOIN = contjoinsel
)
;
----------
[ADD_INDEX] CREATE INDEX jidx_array ON testjsonb USING gin((j->'array'))
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION joinview_upd_trig_fn() RETURNS trigger
LANGUAGE plpgsql AS
$$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING WITH (new AS n) new.f1, n.f4 INTO new.f1, new.f4;  -- now ok
  RETURN NEW;
END;
$$
;
----------
[ADD_INDEX] CREATE INDEX "brin_summarize_idx" ON "public"."brin_summarize" USING "brin" ("value") WITH ("pages_per_range"='2')
;
----------
[CREATE_TABLE] CREATE TABLE event_trigger_test (a integer, b text)
;
----------
[ADD_INDEX] CREATE INDEX "brin_test_b_idx" ON "public"."brin_test" USING "brin" ("b") WITH ("pages_per_range"='2')
;
----------
[CREATE_TYPE] create domain d_fail as anyelement
;
----------
[CREATE_TABLE] CREATE TABLE bug18634t (a int, b int, c text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."dupindexcols" (
    "id" integer,
    "f1" "text" COLLATE "pg_catalog"."C"
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."trigger_parted_p2"
    ADD CONSTRAINT "trigger_parted_p2_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
-- override the global
#print_strict_params on
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- too many rows
  select * from foo where f1 > p1 or f1::text = p3  into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[ADD_INDEX] CREATE INDEX test_gin_idx ON test USING gin (a,b)
;
----------
[CREATE_PROG_OBJ] create function explicitly_binary_coercible(oid, oid) returns bool as $$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$$ language plpgsql strict stable
;
----------
[ALTER_CATALOG] ALTER DATABASE "contrib_regression" RESET auto_explain.bogus
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."fk_partitioned_fk_5"
    ADD CONSTRAINT "fk_partitioned_fk_5_a_b_fkey" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b") ON UPDATE CASCADE ON DELETE CASCADE DEFERRABLE
;
----------
[DROP_TYPE] drop domain restrictedrange
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btismncmp(ismn, ismn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create operator class part_test_text_ops
for type text
using hash as
operator 1 =,
function 2 part_hashtext_length(text, int8)
;
----------
[CREATE_TABLE] CREATE TABLE part_drop_index_locking (id int) PARTITION BY RANGE(id)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR CLASS alt_opc2 USING hash SET SCHEMA alt_nsp2
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_tmm AS SELECT sum(totamt) AS grandtot FROM mvtest_tm
;
----------
[CREATE_TABLE] CREATE TABLE prt2_e_p2 PARTITION OF prt2_e FOR VALUES FROM (250) TO (500)
;
----------
[ADD_INDEX] CREATE INDEX idx_child_doc_gin ON split_cov.child_table USING gin (doc jsonb_path_ops) WHERE doc ? 'kind'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_truncate_visibility_map(regclass)
RETURNS void
AS '$libdir/pg_visibility', 'pg_truncate_visibility_map'
LANGUAGE C STRICT
PARALLEL UNSAFE
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp_tz() < $.datetime("HH:MI TZH"))'))
;
----------
[CREATE_TABLE] CREATE TABLE ATACC1 (a int)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_collation
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE)
;
----------
[CREATE_TABLE] -- Validate we do not reduce a clone clause to a constant true or false
CREATE TABLE pred_tab (a int, b int)
;
----------
[CREATE_TABLE] --
-- partitioned by a single column
--
CREATE TABLE prt1 (a int, b int, c varchar) PARTITION BY RANGE(a)
;
----------
[DROP_TYPE] -- fail

drop domain intdom1
;
----------
[CREATE_POLICY] /*
 * text search configuration for catalan language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * catalan and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY catalan_stem
	(TEMPLATE = snowball, Language = catalan )
;
----------
[CREATE_TABLE] create table brtrigpartcon (a int, b text) partition by list (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."notnull_tbl4_lk3"
    ADD CONSTRAINT "notnull_tbl4_lk3_pkey" PRIMARY KEY ("a") DEFERRABLE INITIALLY DEFERRED
;
----------
[CREATE_TABLE] -- table inheritance tests
CREATE TABLE agg (a int2, b float4)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_parameter_acl
;
