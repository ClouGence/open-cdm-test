-- language
DROP LANGUAGE test_language_exists
;
-- Show summary of visibility map bits for a relation.
CREATE FUNCTION pg_visibility_map_summary(regclass,
    OUT all_visible bigint, OUT all_frozen bigint)
RETURNS record
AS '$libdir/pg_visibility', 'pg_visibility_map_summary'
LANGUAGE C STRICT
;
CREATE TABLE notnull_tbl1_child2 (c int, b int, a int)
;
CREATE INDEX t1_gist ON s1.t1 USING GIST (b)
;
CREATE TABLE trunc_c (a serial PRIMARY KEY)
;
create constraint trigger parted_trigger after update on parted_trigger
  from parted_referenced
  for each row execute procedure trigger_notice_ab()
;
ANALYZE pg_temp_12.pg_temp_26389
;
ALTER OPERATOR FAMILY "public"."part_test_int4_ops2" USING "hash" ADD
    FUNCTION 2 (integer, integer) "public"."part_hashint4_noop"(integer,bigint)
;
CREATE TEMP TABLE pg_temp_11.pg_temp_41261_2 (tid pg_catalog.tid)
;
create domain mytext as text
;
ALTER TABLE ONLY "public"."test_inh_check_child" ADD CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
;
ANALYZE pg_temp_11.pg_temp_26330
;
-- from http://www.depesz.com/index.php/2010/04/19/getting-unique-elements/

CREATE TEMP TABLE articles (
    id int CONSTRAINT articles_pkey PRIMARY KEY,
    keywords text,
    title text UNIQUE NOT NULL,
    body text UNIQUE,
    created date
)
;
CREATE TABLE "public"."gtest21b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULLIF("a", 0)) STORED
)
;
create table parttmp (id int, valid_at daterange, exclude using gist (id with <>, valid_at with &&)) partition by range (id)
;
CREATE DATABASE "contrib_regression" TEMPLATE=template0 ENCODING='UTF8' LOCALE='C'
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
CREATE TABLE iamalargetable AS SELECT iamalongfunction() longfunctionoutput
;
ALTER TABLE fd_pt1 ALTER COLUMN c6 SET NOT NULL
;
DROP TABLE tab_replidentity_index
;
drop table idxpart1
;
-- Partitions
CREATE TABLE pt_stats (a int, b int) PARTITION BY range (a)
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc5(1)
;
DROP SEQUENCE rngfunc_rescan_seq1
;
VACUUM (FULL) pg_catalog.pg_namespace
;
create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with =)) partition by range (a)
;
CALL ptest1s('b')
;
-- general regression test for transforms

DROP EXTENSION IF EXISTS hstore CASCADE
;
ANALYZE pg_catalog.pg_index
;
CREATE SCHEMA regtest_schema
;
ANALYZE pg_temp_9.pg_temp_26388
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
CREATE SCHEMA dummy_seclabel_test
;
CREATE TABLE test_mix_4 (a int PRIMARY KEY, b int, c int, d int GENERATED ALWAYS AS (a + 1) STORED)
;
-- Put it into an operator class so that FmgrInfo will be cached in relcache.
CREATE OPERATOR CLASS part_test_int4_ops_bad FOR TYPE int4 USING hash AS
  FUNCTION 2 part_hashint4_error(int4, int8)
;
VACUUM FULL pg_shdescription
;
--
-- Make sanity checks on the pg_proc entries created by CREATE FUNCTION
--

--
-- ARGUMENT and RETURN TYPES
--
CREATE FUNCTION functest_A_1(text, date) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 = ''abcd'' AND $2 > ''2001-01-01'''
;
CREATE TEXT SEARCH DICTIONARY "public"."hunspell_invalid_3" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_num', afffile = 'ispell_sample' )
;
ANALYZE pg_temp_46.pg_temp_28044_2
;
CREATE FUNCTION seg_in(cstring)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE constraint_rename_test RENAME CONSTRAINT con3 TO con3foo
;
ALTER TABLE tx1 RENAME TO a1
;
-- invalid: bad parallel-safety marking
CREATE AGGREGATE mysum (int)
(
	stype = int,
	sfunc = int4pl,
	parallel = pear
)
;
drop function array_elem_check(int)
;
CREATE TYPE "public"."float8range" AS RANGE (
    subtype = double precision,
    subtype_diff = "float8mi"
)
;
SECURITY LABEL ON ROLE regress_dummy_seclabel_user1 IS 'secret'
;
CREATE INDEX test_replica_id_full_idx ON test_replica_id_full(x,y)
;
alter type insenum add value 'i27' before 'L2'
;
CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_enabled='string')
;
ALTER TABLE parted_self_fk ATTACH PARTITION part3_self_fk FOR VALUES FROM (20) TO (40)
;
------------------------------------------------------------------------------------------
[DROP_LANGUAGE] -- language
DROP LANGUAGE test_language_exists
;
----------
[CREATE_PROG_OBJ] -- Show summary of visibility map bits for a relation.
CREATE FUNCTION pg_visibility_map_summary(regclass,
    OUT all_visible bigint, OUT all_frozen bigint)
RETURNS record
AS '$libdir/pg_visibility', 'pg_visibility_map_summary'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1_child2 (c int, b int, a int)
;
----------
[ADD_INDEX] CREATE INDEX t1_gist ON s1.t1 USING GIST (b)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_c (a serial PRIMARY KEY)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create constraint trigger parted_trigger after update on parted_trigger
  from parted_referenced
  for each row execute procedure trigger_notice_ab()
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26389
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY "public"."part_test_int4_ops2" USING "hash" ADD
    FUNCTION 2 (integer, integer) "public"."part_hashint4_noop"(integer,bigint)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_41261_2 (tid pg_catalog.tid)
;
----------
[CREATE_TYPE] create domain mytext as text
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."test_inh_check_child" ADD CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26330
;
----------
[CREATE_TABLE] -- from http://www.depesz.com/index.php/2010/04/19/getting-unique-elements/

CREATE TEMP TABLE articles (
    id int CONSTRAINT articles_pkey PRIMARY KEY,
    keywords text,
    title text UNIQUE NOT NULL,
    body text UNIQUE,
    created date
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest21b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULLIF("a", 0)) STORED
)
;
----------
[CREATE_TABLE] create table parttmp (id int, valid_at daterange, exclude using gist (id with <>, valid_at with &&)) partition by range (id)
;
----------
[CREATE_CATALOG] CREATE DATABASE "contrib_regression" TEMPLATE=template0 ENCODING='UTF8' LOCALE='C'
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE iamalargetable AS SELECT iamalongfunction() longfunctionoutput
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c6 SET NOT NULL
;
----------
[DROP_TABLE] DROP TABLE tab_replidentity_index
;
----------
[DROP_TABLE] drop table idxpart1
;
----------
[CREATE_TABLE] -- Partitions
CREATE TABLE pt_stats (a int, b int) PARTITION BY range (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc5(1)
;
----------
[DROP_SEQUENCE] DROP SEQUENCE rngfunc_rescan_seq1
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_namespace
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with =)) partition by range (a)
;
----------
[CALL_PROG_OBJ] CALL ptest1s('b')
;
----------
[DROP_LIBRARY] -- general regression test for transforms

DROP EXTENSION IF EXISTS hstore CASCADE
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_index
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regtest_schema
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26388
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
----------
[CREATE_SCHEMA] CREATE SCHEMA dummy_seclabel_test
;
----------
[CREATE_TABLE] CREATE TABLE test_mix_4 (a int PRIMARY KEY, b int, c int, d int GENERATED ALWAYS AS (a + 1) STORED)
;
----------
[CREATE_PROG_OBJ] -- Put it into an operator class so that FmgrInfo will be cached in relcache.
CREATE OPERATOR CLASS part_test_int4_ops_bad FOR TYPE int4 USING hash AS
  FUNCTION 2 part_hashint4_error(int4, int8)
;
----------
[ADMIN_TABLE] VACUUM FULL pg_shdescription
;
----------
[CREATE_PROG_OBJ] --
-- Make sanity checks on the pg_proc entries created by CREATE FUNCTION
--

--
-- ARGUMENT and RETURN TYPES
--
CREATE FUNCTION functest_A_1(text, date) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 = ''abcd'' AND $2 > ''2001-01-01'''
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."hunspell_invalid_3" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_num', afffile = 'ispell_sample' )
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_46.pg_temp_28044_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_in(cstring)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[RENAME_CONSTRAINT] ALTER TABLE constraint_rename_test RENAME CONSTRAINT con3 TO con3foo
;
----------
[RENAME_TABLE] ALTER TABLE tx1 RENAME TO a1
;
----------
[CREATE_PROG_OBJ] -- invalid: bad parallel-safety marking
CREATE AGGREGATE mysum (int)
(
	stype = int,
	sfunc = int4pl,
	parallel = pear
)
;
----------
[DROP_PROG_OBJ] drop function array_elem_check(int)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."float8range" AS RANGE (
    subtype = double precision,
    subtype_diff = "float8mi"
)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON ROLE regress_dummy_seclabel_user1 IS 'secret'
;
----------
[ADD_INDEX] CREATE INDEX test_replica_id_full_idx ON test_replica_id_full(x,y)
;
----------
[ALTER_TYPE] alter type insenum add value 'i27' before 'L2'
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_enabled='string')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE parted_self_fk ATTACH PARTITION part3_self_fk FOR VALUES FROM (20) TO (40)
;
