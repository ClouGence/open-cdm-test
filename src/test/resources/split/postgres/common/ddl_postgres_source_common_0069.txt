-- Invalid fetch_size option
CREATE FOREIGN TABLE inv_fsz (c1 int )
	SERVER loopback OPTIONS (fetch_size '100$%$#$#')
;
ALTER TABLE pg_toast.pg_toast_27632 RENAME TO reind_con_toast
;
DROP TABLE trgfire
;
create function anyctest(anycompatible)
returns anycompatiblemultirange as $$
  select $1
$$ language sql
;
CREATE FUNCTION cube_distance(cube, cube)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP TABLE pg_temp_11.pg_temp_41180_2, pg_temp_11.pg_temp_41180
;
CREATE TRIGGER "tg_wslot_biu" BEFORE INSERT OR UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_wslot_biu"()
;
-- fail, cannot use stacked diagnostics statement outside handler
create or replace function stacked_diagnostics_test() returns void as $$
declare _detail text;
        _hint text;
        _message text;
begin
  get stacked diagnostics
        _message = message_text,
        _detail = pg_exception_detail,
        _hint = pg_exception_hint;
  raise notice 'message: %, detail: %, hint: %', _message, _detail, _hint;
end;
$$ language plpgsql
;
ALTER TEXT SEARCH TEMPLATE split_ts_probe.simple_template_renamed SET SCHEMA split_ts_next
;
--
-- tests for list partitioned tables.
--
CREATE TABLE plt1 (a int, b int, c text) PARTITION BY LIST(c)
;
CREATE INDEX hash_f8_index2 ON hash_f8_heap USING hash (random float8_ops)
	WITH (fillfactor=9)
;
DROP TABLE test_recovery_conflict_table1
;
-- test alter column options
CREATE TABLE attmp(i integer)
;
ALTER TABLE tab_default REPLICA IDENTITY FULL
;
ALTER TABLE ctlt4 ALTER COLUMN c SET STORAGE EXTERNAL
;
CREATE TEXT SEARCH CONFIGURATION tamil
	(PARSER = default)
;
CREATE TABLE itest6 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
CREATE OPERATOR ~> (
	LEFTARG = cube, RIGHTARG = int, PROCEDURE = cube_coord_llur
)
;
ALTER VIEW rw_view1 SET (check_option=local)
;
-- Case 3. REPLICA IDENTITY NOTHING
ALTER TABLE rf_tbl_abcd_pk REPLICA IDENTITY NOTHING
;
create index pti2 on pt (sdata seg_ops)
;
CREATE DATABASE foobar6s STRATEGY "WAL_LOG" TEMPLATE foobar2
;
CREATE FUNCTION dup (f1 anycompatible, f2 anycompatiblearray, f3 out anycompatible, f4 out anycompatiblearray)
AS 'select $1, $2' LANGUAGE sql
;
CREATE TABLE public.tplain (
    en public.digit,
    data integer
)
;
ALTER TEXT SEARCH CONFIGURATION finnish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
ALTER TABLE alice.unpartitioned ENABLE ROW LEVEL SECURITY
;
ALTER TABLE replication_example ALTER COLUMN somenum TYPE int4 USING (somenum::int4)
;
CREATE INDEX t1_brin ON s4.t1 USING BRIN (i)
;
DROP EVENT TRIGGER always_start, always_end, always_drop, always_rewrite
;
VACUUM (FREEZE) pg_catalog.pg_init_privs
;
CREATE TRIGGER error_del_when BEFORE DELETE OR UPDATE ON main_table
FOR EACH ROW WHEN (OLD.a <> NEW.a)
EXECUTE PROCEDURE trigger_func('error_del_new')
;
ALTER TABLE fk_partitioned_fk DROP CONSTRAINT fk_partitioned_fk_a_b_fkey
;
CREATE SCHEMA alt_nsp6
;
CREATE TEMP TABLE FKTABLE (ftest1 numeric)
;
create trigger my_table_multievent_trig
  after insert or update on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
create temp table childtab(f4 int) inherits(parent1, parent2)
;
CREATE TABLE pt1 PARTITION OF pt FOR VALUES IN (1)
;
CREATE TABLE regress_zeropriv_tbl (a int)
;
CREATE CAST (issn13 AS issn) WITHOUT FUNCTION AS ASSIGNMENT
;
alter table bar1 attach partition bar2 default
;
create table rlp3efgh partition of rlp3 for values in ('ef', 'gh')
;
DROP TABLE lcku_table
;
ALTER INDEX pg_toast.pg_toast_30715_index RENAME TO reind_con_toast_idx
;
drop table hp_prefix_test
;
CREATE TABLE users_table_part(user_id bigint, value_1 int, value_2 int) PARTITION BY RANGE (value_1)
;
--test expressions in targetlist are pushed down for gather merge
create function sp_simple_func(var1 integer) returns integer
as $$
begin
        return var1 + 10;
end;
$$ language plpgsql PARALLEL SAFE
;
-- ON UPDATE/DELETE SET DEFAULT
ALTER TABLE fk_partitioned_fk DROP CONSTRAINT fk_partitioned_fk_a_b_fkey
;
create table rp1 partition of rp for values from (1) to (2)
;
ANALYZE hash_txt_heap
;
DROP TABLE regr_test
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- Invalid fetch_size option
CREATE FOREIGN TABLE inv_fsz (c1 int )
	SERVER loopback OPTIONS (fetch_size '100$%$#$#')
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27632 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE trgfire
;
----------
[CREATE_PROG_OBJ] create function anyctest(anycompatible)
returns anycompatiblemultirange as $$
  select $1
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_distance(cube, cube)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_41180_2, pg_temp_11.pg_temp_41180
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_wslot_biu" BEFORE INSERT OR UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_wslot_biu"()
;
----------
[CREATE_PROG_OBJ] -- fail, cannot use stacked diagnostics statement outside handler
create or replace function stacked_diagnostics_test() returns void as $$
declare _detail text;
        _hint text;
        _message text;
begin
  get stacked diagnostics
        _message = message_text,
        _detail = pg_exception_detail,
        _hint = pg_exception_hint;
  raise notice 'message: %, detail: %, hint: %', _message, _detail, _hint;
end;
$$ language plpgsql
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH TEMPLATE split_ts_probe.simple_template_renamed SET SCHEMA split_ts_next
;
----------
[CREATE_TABLE] --
-- tests for list partitioned tables.
--
CREATE TABLE plt1 (a int, b int, c text) PARTITION BY LIST(c)
;
----------
[ADD_INDEX] CREATE INDEX hash_f8_index2 ON hash_f8_heap USING hash (random float8_ops)
	WITH (fillfactor=9)
;
----------
[DROP_TABLE] DROP TABLE test_recovery_conflict_table1
;
----------
[CREATE_TABLE] -- test alter column options
CREATE TABLE attmp(i integer)
;
----------
[ALTER_TABLE] ALTER TABLE tab_default REPLICA IDENTITY FULL
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ctlt4 ALTER COLUMN c SET STORAGE EXTERNAL
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION tamil
	(PARSER = default)
;
----------
[CREATE_TABLE] CREATE TABLE itest6 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~> (
	LEFTARG = cube, RIGHTARG = int, PROCEDURE = cube_coord_llur
)
;
----------
[ALTER_VIEW] ALTER VIEW rw_view1 SET (check_option=local)
;
----------
[ALTER_TABLE] -- Case 3. REPLICA IDENTITY NOTHING
ALTER TABLE rf_tbl_abcd_pk REPLICA IDENTITY NOTHING
;
----------
[ADD_INDEX] create index pti2 on pt (sdata seg_ops)
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar6s STRATEGY "WAL_LOG" TEMPLATE foobar2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dup (f1 anycompatible, f2 anycompatiblearray, f3 out anycompatible, f4 out anycompatiblearray)
AS 'select $1, $2' LANGUAGE sql
;
----------
[CREATE_TABLE] CREATE TABLE public.tplain (
    en public.digit,
    data integer
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION finnish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ALTER_TABLE] ALTER TABLE alice.unpartitioned ENABLE ROW LEVEL SECURITY
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE replication_example ALTER COLUMN somenum TYPE int4 USING (somenum::int4)
;
----------
[ADD_INDEX] CREATE INDEX t1_brin ON s4.t1 USING BRIN (i)
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER always_start, always_end, always_drop, always_rewrite
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_init_privs
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER error_del_when BEFORE DELETE OR UPDATE ON main_table
FOR EACH ROW WHEN (OLD.a <> NEW.a)
EXECUTE PROCEDURE trigger_func('error_del_new')
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE fk_partitioned_fk DROP CONSTRAINT fk_partitioned_fk_a_b_fkey
;
----------
[CREATE_SCHEMA] CREATE SCHEMA alt_nsp6
;
----------
[CREATE_TABLE] CREATE TEMP TABLE FKTABLE (ftest1 numeric)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_table_multievent_trig
  after insert or update on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[CREATE_TABLE] create temp table childtab(f4 int) inherits(parent1, parent2)
;
----------
[CREATE_TABLE] CREATE TABLE pt1 PARTITION OF pt FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE regress_zeropriv_tbl (a int)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (issn13 AS issn) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table bar1 attach partition bar2 default
;
----------
[CREATE_TABLE] create table rlp3efgh partition of rlp3 for values in ('ef', 'gh')
;
----------
[DROP_TABLE] DROP TABLE lcku_table
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30715_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] drop table hp_prefix_test
;
----------
[CREATE_TABLE] CREATE TABLE users_table_part(user_id bigint, value_1 int, value_2 int) PARTITION BY RANGE (value_1)
;
----------
[CREATE_PROG_OBJ] --test expressions in targetlist are pushed down for gather merge
create function sp_simple_func(var1 integer) returns integer
as $$
begin
        return var1 + 10;
end;
$$ language plpgsql PARALLEL SAFE
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- ON UPDATE/DELETE SET DEFAULT
ALTER TABLE fk_partitioned_fk DROP CONSTRAINT fk_partitioned_fk_a_b_fkey
;
----------
[CREATE_TABLE] create table rp1 partition of rp for values from (1) to (2)
;
----------
[ADMIN_TABLE] ANALYZE hash_txt_heap
;
----------
[DROP_TABLE] DROP TABLE regr_test
;
