/* fails */
CREATE TABLE inhz (xx text DEFAULT 'text', yy int UNIQUE)
;
DROP TABLE temporal3
;
CREATE TABLE lcku_table (id INTEGER, value TEXT, PRIMARY KEY (id) INCLUDE (value))
;
VACUUM (SKIP_DATABASE_STATS) information_schema.sql_features
;
DROP OPERATOR <<@@ (point, polygon)
;
DROP TRIGGER trans_inserttrig ON range_parted
;
CREATE TABLE dedup_unique_test_table (a int) WITH (autovacuum_enabled=false)
;
-- Exercise the column mapping code with foreign keys.  In this test we'll
-- create a partitioned table which has a partition with a dropped column and
-- check to ensure that an UPDATE cascades the changes correctly to the
-- partitioned table.
CREATE SCHEMA fkpart13
;
-- Reindex concurrently of exclusion constraint currently not supported
CREATE TABLE concur_reindex_tab3 (c1 int, c2 int4range, EXCLUDE USING gist (c2 WITH &&))
;
CREATE OR REPLACE VIEW viewtest AS
	SELECT 1, * FROM viewtest_tbl
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."cminh" INHERIT "public"."cmdata"
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_statistic_ext
;
DROP PUBLICATION tap_pub_temp2
;
CREATE SCHEMA ctl_schema
;
ALTER TABLE ONLY "public"."mlparted_def2" DROP COLUMN "........pg.dropped.7........"
;
ANALYZE public.gtest_normal_child2
;
alter table idxpart1 drop column drop_2
;
--
-- Test multiple reference to a transition table
--

CREATE TABLE multi_test (i int)
;
CREATE TABLE gtest24nn (a int, b gtestdomainnn GENERATED ALWAYS AS (a * 2) STORED)
;
CREATE VIEW tt1 AS
  SELECT * FROM (
    VALUES
       ('abc'::varchar(3), '0123456789', 42, 'abcd'::varchar(4)),
       ('0123456789', 'abc'::varchar(3), 42.12, 'abc'::varchar(4))
  ) vv(a,b,c,d)
;
CREATE FUNCTION "public"."part_hashtext_length"("value" "text", "seed" bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE
    AS $$
select length(coalesce(value, ''))::int8
$$
;
ALTER TABLE f_star ADD COLUMN f int4
;
-- Create gist2 index on fast_emp4000
CREATE INDEX grect2ind2 ON fast_emp4000 USING gist2 (home_base)
;
DROP TABLESPACE source_ts
;
TRUNCATE TABLE trunc_c
;
CREATE TABLE notnull_child_upg () INHERITS (notnull_parent_upg)
;
CREATE FUNCTION functest_A_2(text[]) RETURNS int LANGUAGE 'sql'
       AS 'SELECT $1[1]::int'
;
create domain str_domain as text not null
;
DROP SCHEMA regress_tenant2_schema
;
ALTER TABLE ONLY "public"."mlparted12" DROP COLUMN "........pg.dropped.4........"
;
-- VALIDATE CONSTRAINT need do nothing on foreign tables
ALTER TABLE fd_pt1 VALIDATE CONSTRAINT fd_pt1chk3
;
ALTER TABLE ONLY "public"."dropcolumnanother" DROP COLUMN "........pg.dropped.1........"
;
vacuum analyze gist_tbl
;
create table hpart3 partition of hash_parted for values with (modulus 8, remainder 0)
;
--
-- test for bitwise integer aggregates
--
CREATE TEMPORARY TABLE bitwise_test(
  i2 INT2,
  i4 INT4,
  i8 INT8,
  i INTEGER,
  x INT2,
  y BIT(4)
)
;
create function polyf(anycompatiblemultirange) returns anycompatiblerange
as 'select range_merge($1);' language sql
;
CREATE FOREIGN TABLE tru_ftable_parent (id int)
       SERVER loopback OPTIONS (table_name 'tru_rtable_parent')
;
ALTER TABLE pg_toast.pg_toast_27643 RENAME TO reind_con_toast
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."test_inh_check_child" INHERIT "public"."test_inh_check"
;
CREATE TABLE pagg_tab_m_p1 PARTITION OF pagg_tab_m FOR VALUES FROM (0, 0) TO (10, 10)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
/* src/test/modules/test_dsa/test_dsa--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_dsa_basic()
	RETURNS pg_catalog.void
	AS '$libdir/test_dsa' LANGUAGE C
;
-- default configuration - match first word and return it among with all synonyms
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=true, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=false)
;
ANALYZE pg_temp_3.pg_temp_27606
;
create function trigger_ddl_func() returns trigger as $$
begin
  alter table trigger_ddl_table add primary key (col1);
  return new;
end$$ language plpgsql
;
--
-- Test inserting so many values that we have to renumber
--

create type insenum as enum ('L1', 'L2')
;
CREATE EVENT TRIGGER split_event_probe
    ON ddl_command_end
    WHEN TAG IN ('CREATE TABLE', 'ALTER TABLE')
    EXECUTE FUNCTION split_trigger_probe.event_logger()
;
CREATE POLICY p2 ON category
    USING (CASE WHEN current_user = 'regress_rls_bob' THEN cid IN (11, 33)
           WHEN current_user = 'regress_rls_carol' THEN cid IN (22, 44)
           ELSE false END)
;
CREATE TYPE "public"."text_w_default" (
    INTERNALLENGTH = variable,
    INPUT = "public"."text_w_default_in",
    OUTPUT = "public"."text_w_default_out",
    DEFAULT = 'zippo',
    ALIGNMENT = int4,
    STORAGE = plain
)
;
-- dropping partitions is disallowed
DROP TABLE droppk_d
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] /* fails */
CREATE TABLE inhz (xx text DEFAULT 'text', yy int UNIQUE)
;
----------
[DROP_TABLE] DROP TABLE temporal3
;
----------
[CREATE_TABLE] CREATE TABLE lcku_table (id INTEGER, value TEXT, PRIMARY KEY (id) INCLUDE (value))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) information_schema.sql_features
;
----------
[DROP_PROG_OBJ] DROP OPERATOR <<@@ (point, polygon)
;
----------
[DROP_TRIGGER] DROP TRIGGER trans_inserttrig ON range_parted
;
----------
[CREATE_TABLE] CREATE TABLE dedup_unique_test_table (a int) WITH (autovacuum_enabled=false)
;
----------
[CREATE_SCHEMA] -- Exercise the column mapping code with foreign keys.  In this test we'll
-- create a partitioned table which has a partition with a dropped column and
-- check to ensure that an UPDATE cascades the changes correctly to the
-- partitioned table.
CREATE SCHEMA fkpart13
;
----------
[CREATE_TABLE] -- Reindex concurrently of exclusion constraint currently not supported
CREATE TABLE concur_reindex_tab3 (c1 int, c2 int4range, EXCLUDE USING gist (c2 WITH &&))
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW viewtest AS
	SELECT 1, * FROM viewtest_tbl
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."cminh" INHERIT "public"."cmdata"
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_statistic_ext
;
----------
[DROP_PUB_SUB] DROP PUBLICATION tap_pub_temp2
;
----------
[CREATE_SCHEMA] CREATE SCHEMA ctl_schema
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def2" DROP COLUMN "........pg.dropped.7........"
;
----------
[ADMIN_TABLE] ANALYZE public.gtest_normal_child2
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart1 drop column drop_2
;
----------
[CREATE_TABLE] --
-- Test multiple reference to a transition table
--

CREATE TABLE multi_test (i int)
;
----------
[CREATE_TABLE] CREATE TABLE gtest24nn (a int, b gtestdomainnn GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW tt1 AS
  SELECT * FROM (
    VALUES
       ('abc'::varchar(3), '0123456789', 42, 'abcd'::varchar(4)),
       ('0123456789', 'abc'::varchar(3), 42.12, 'abc'::varchar(4))
  ) vv(a,b,c,d)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."part_hashtext_length"("value" "text", "seed" bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE
    AS $$
select length(coalesce(value, ''))::int8
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE f_star ADD COLUMN f int4
;
----------
[ADD_INDEX] -- Create gist2 index on fast_emp4000
CREATE INDEX grect2ind2 ON fast_emp4000 USING gist2 (home_base)
;
----------
[DROP_TABLESPACE] DROP TABLESPACE source_ts
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE trunc_c
;
----------
[CREATE_TABLE] CREATE TABLE notnull_child_upg () INHERITS (notnull_parent_upg)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_A_2(text[]) RETURNS int LANGUAGE 'sql'
       AS 'SELECT $1[1]::int'
;
----------
[CREATE_TYPE] create domain str_domain as text not null
;
----------
[DROP_SCHEMA] DROP SCHEMA regress_tenant2_schema
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted12" DROP COLUMN "........pg.dropped.4........"
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- VALIDATE CONSTRAINT need do nothing on foreign tables
ALTER TABLE fd_pt1 VALIDATE CONSTRAINT fd_pt1chk3
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."dropcolumnanother" DROP COLUMN "........pg.dropped.1........"
;
----------
[ADMIN_TABLE] vacuum analyze gist_tbl
;
----------
[CREATE_TABLE] create table hpart3 partition of hash_parted for values with (modulus 8, remainder 0)
;
----------
[CREATE_TABLE] --
-- test for bitwise integer aggregates
--
CREATE TEMPORARY TABLE bitwise_test(
  i2 INT2,
  i4 INT4,
  i8 INT8,
  i INTEGER,
  x INT2,
  y BIT(4)
)
;
----------
[CREATE_PROG_OBJ] create function polyf(anycompatiblemultirange) returns anycompatiblerange
as 'select range_merge($1);' language sql
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tru_ftable_parent (id int)
       SERVER loopback OPTIONS (table_name 'tru_rtable_parent')
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27643 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."test_inh_check_child" INHERIT "public"."test_inh_check"
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_m_p1 PARTITION OF pagg_tab_m FOR VALUES FROM (0, 0) TO (10, 10)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_dsa/test_dsa--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_dsa_basic()
	RETURNS pg_catalog.void
	AS '$libdir/test_dsa' LANGUAGE C
;
----------
[ALTER_POLICY] -- default configuration - match first word and return it among with all synonyms
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=true, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=false)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27606
;
----------
[CREATE_PROG_OBJ] create function trigger_ddl_func() returns trigger as $$
begin
  alter table trigger_ddl_table add primary key (col1);
  return new;
end$$ language plpgsql
;
----------
[CREATE_TYPE] --
-- Test inserting so many values that we have to renumber
--

create type insenum as enum ('L1', 'L2')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER split_event_probe
    ON ddl_command_end
    WHEN TAG IN ('CREATE TABLE', 'ALTER TABLE')
    EXECUTE FUNCTION split_trigger_probe.event_logger()
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON category
    USING (CASE WHEN current_user = 'regress_rls_bob' THEN cid IN (11, 33)
           WHEN current_user = 'regress_rls_carol' THEN cid IN (22, 44)
           ELSE false END)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."text_w_default" (
    INTERNALLENGTH = variable,
    INPUT = "public"."text_w_default_in",
    OUTPUT = "public"."text_w_default_out",
    DEFAULT = 'zippo',
    ALIGNMENT = int4,
    STORAGE = plain
)
;
----------
[DROP_TABLE] -- dropping partitions is disallowed
DROP TABLE droppk_d
;
