--
-- Test the planner's ability to reorder the distinctClause Pathkeys to match
-- the input path's ordering
--

CREATE TABLE distinct_on_tbl (x int, y int, z int)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub1, pub2
;
create table hs_extreme (col1 integer)
;
SECURITY LABEL ON SCHEMA dummy_seclabel_test IS 'unclassified'
;
VACUUM (ANALYZE) skip_fetch
;
CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a>0
;
ALTER FUNCTION gbt_bytea_consistent(internal, bytea, smallint, oid, internal) PARALLEL SAFE
;
ANALYZE public.fast_emp4000
;
CREATE RULE rule1 AS ON INSERT TO ruletest1
    DO INSTEAD INSERT INTO ruletest2 VALUES (NEW.*)
;
do $$ declare a int[];
begin a[1] := 23::text; raise notice 'a = %', a; end$$
;
CREATE TABLE pa_source (sid integer, delta float)
  WITH (autovacuum_enabled=off)
;
create cast (int8alias1 as int8) without function
;
create table tab_part_attach (a int) partition by list (a)
;
CREATE FUNCTION "public"."event_trigger_report_end"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'END: command_tag=% type=% identity=%',
            r.command_tag, r.object_type, r.object_identity;
    END LOOP;
EXCEPTION WHEN SQLSTATE 'XX000' THEN
	RAISE NOTICE 'END: got internal exception';
END; $$
;
ANALYZE information_schema.sql_packages
;
do $$ declare c two_int8s; begin c.x = 1; end $$
;
DROP TABLE temporal_fk_rng2rng
;
CREATE MATERIALIZED VIEW order_summary AS
    SELECT date, item, sum(num) FROM orders GROUP BY date, item
;
create or replace rule r1 as on update to rules_base do instead
  select * from rules_base where f1 = 11 for update of old
;
CREATE OPERATOR CLASS gist_numeric_ops
DEFAULT FOR TYPE numeric USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_numeric_consistent (internal, numeric, int2, oid, internal),
	FUNCTION	2	gbt_numeric_union (internal, internal),
	FUNCTION	3	gbt_numeric_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_numeric_penalty (internal, internal, internal),
	FUNCTION	6	gbt_numeric_picksplit (internal, internal),
	FUNCTION	7	gbt_numeric_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc3
;
-- test brin cost estimates behave sanely based on correlation of values
CREATE TABLE brin_test (a INT, b INT)
;
CREATE TEMP TABLE abbrev_abort_uuids (
    id serial not null,
    abort_increasing uuid,
    abort_decreasing uuid,
    noabort_increasing uuid,
    noabort_decreasing uuid)
;
alter table bc drop constraint ac_check
;
DROP TRIGGER insert_measurement_trigger ON measurement
;
-- Primary keys cause not-null constraints to be created.
CREATE TABLE cnn_pk (a int, b int)
;
CREATE FUNCTION ks_test_uniform_random_numeric_in_range()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 0.999999) r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
CREATE FUNCTION dblink_is_busy(text)
RETURNS int4
AS '$libdir/dblink', 'dblink_is_busy'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_parameter_acl
;
create trigger child_row_trig
  after insert on child referencing new table as new_table
  for each row execute procedure dump_insert()
;
-- Add operators that are new in 9.6 (pg_trgm 1.2).

ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        7       %> (text, text),
        OPERATOR        8       <->> (text, text) FOR ORDER BY pg_catalog.float_ops
;
ANALYZE pg_temp_11.pg_temp_26410
;
CREATE VIEW temporal4 AS SELECT * FROM t1 LEFT JOIN tt ON t1.num = tt.num2 AND tt.value = 'xxx'
;
create index on idxpart1 (a, b)
;
ALTER TABLE tbl2 ALTER COLUMN val2 TYPE float
;
CREATE TABLE trigtest(key text primary key, data text)
;
drop schema abc restrict
;
CREATE INDEX float8idx ON float8tmp USING gist ( a )
;
-- error
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
CREATE OR REPLACE VIEW lock_view2 AS SELECT * from lock_view3
;
create or replace function array_elem_check(numeric) returns numeric as $$
declare
  x mynums2;
begin
  x[1] := $1;
  return x[1];
end$$ language plpgsql
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub2
;
CREATE TABLE persons5 OF stuff
;
DROP TABLE temporal_rng CASCADE
;
alter table pgbench_history add constraint pgbench_history_tid_fkey foreign key (tid) references pgbench_tellers
;
CREATE UNIQUE INDEX "covidxpart4_a_b_idx" ON "public"."covidxpart4" USING "btree" ("a") INCLUDE ("b")
;
create table xy_1 (x int, y int)
;
create table bigt as select x from generate_series(1, 10000) x
;
CREATE TABLE collate_test21 (f2 text COLLATE "POSIX" REFERENCES collate_test20)
;
-- Track the original tablespace, relfilenode and OID of each index
-- in the tree.
CREATE TEMP TABLE reindex_temp_before AS
  SELECT oid, relname, relfilenode, reltablespace
  FROM pg_class
    WHERE relname ~ 'tbspace_reindex_part_index'
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] --
-- Test the planner's ability to reorder the distinctClause Pathkeys to match
-- the input path's ordering
--

CREATE TABLE distinct_on_tbl (x int, y int, z int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub1, pub2
;
----------
[CREATE_TABLE] create table hs_extreme (col1 integer)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON SCHEMA dummy_seclabel_test IS 'unclassified'
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) skip_fetch
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a>0
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bytea_consistent(internal, bytea, smallint, oid, internal) PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.fast_emp4000
;
----------
[CREATE_POLICY] CREATE RULE rule1 AS ON INSERT TO ruletest1
    DO INSTEAD INSERT INTO ruletest2 VALUES (NEW.*)
;
----------
[BLOCK] do $$ declare a int[];
begin a[1] := 23::text; raise notice 'a = %', a; end$$
;
----------
[CREATE_TABLE] CREATE TABLE pa_source (sid integer, delta float)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] create cast (int8alias1 as int8) without function
;
----------
[CREATE_TABLE] create table tab_part_attach (a int) partition by list (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."event_trigger_report_end"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'END: command_tag=% type=% identity=%',
            r.command_tag, r.object_type, r.object_identity;
    END LOOP;
EXCEPTION WHEN SQLSTATE 'XX000' THEN
	RAISE NOTICE 'END: got internal exception';
END; $$
;
----------
[ADMIN_TABLE] ANALYZE information_schema.sql_packages
;
----------
[BLOCK] do $$ declare c two_int8s; begin c.x = 1; end $$
;
----------
[DROP_TABLE] DROP TABLE temporal_fk_rng2rng
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW order_summary AS
    SELECT date, item, sum(num) FROM orders GROUP BY date, item
;
----------
[CREATE_POLICY] create or replace rule r1 as on update to rules_base do instead
  select * from rules_base where f1 = 11 for update of old
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_numeric_ops
DEFAULT FOR TYPE numeric USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_numeric_consistent (internal, numeric, int2, oid, internal),
	FUNCTION	2	gbt_numeric_union (internal, internal),
	FUNCTION	3	gbt_numeric_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_numeric_penalty (internal, internal, internal),
	FUNCTION	6	gbt_numeric_picksplit (internal, internal),
	FUNCTION	7	gbt_numeric_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc3
;
----------
[CREATE_TABLE] -- test brin cost estimates behave sanely based on correlation of values
CREATE TABLE brin_test (a INT, b INT)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE abbrev_abort_uuids (
    id serial not null,
    abort_increasing uuid,
    abort_decreasing uuid,
    noabort_increasing uuid,
    noabort_decreasing uuid)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table bc drop constraint ac_check
;
----------
[DROP_TRIGGER] DROP TRIGGER insert_measurement_trigger ON measurement
;
----------
[CREATE_TABLE] -- Primary keys cause not-null constraints to be created.
CREATE TABLE cnn_pk (a int, b int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ks_test_uniform_random_numeric_in_range()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 0.999999) r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_is_busy(text)
RETURNS int4
AS '$libdir/dblink', 'dblink_is_busy'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_parameter_acl
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child_row_trig
  after insert on child referencing new table as new_table
  for each row execute procedure dump_insert()
;
----------
[ALTER_PROG_OBJ] -- Add operators that are new in 9.6 (pg_trgm 1.2).

ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        7       %> (text, text),
        OPERATOR        8       <->> (text, text) FOR ORDER BY pg_catalog.float_ops
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26410
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW temporal4 AS SELECT * FROM t1 LEFT JOIN tt ON t1.num = tt.num2 AND tt.value = 'xxx'
;
----------
[ADD_INDEX] create index on idxpart1 (a, b)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl2 ALTER COLUMN val2 TYPE float
;
----------
[CREATE_TABLE] CREATE TABLE trigtest(key text primary key, data text)
;
----------
[DROP_SCHEMA] drop schema abc restrict
;
----------
[ADD_INDEX] CREATE INDEX float8idx ON float8tmp USING gist ( a )
;
----------
[CREATE_TABLE|SELECT] -- error
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW lock_view2 AS SELECT * from lock_view3
;
----------
[CREATE_PROG_OBJ] create or replace function array_elem_check(numeric) returns numeric as $$
declare
  x mynums2;
begin
  x[1] := $1;
  return x[1];
end$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub2
;
----------
[CREATE_TABLE] CREATE TABLE persons5 OF stuff
;
----------
[DROP_TABLE] DROP TABLE temporal_rng CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_history add constraint pgbench_history_tid_fkey foreign key (tid) references pgbench_tellers
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "covidxpart4_a_b_idx" ON "public"."covidxpart4" USING "btree" ("a") INCLUDE ("b")
;
----------
[CREATE_TABLE] create table xy_1 (x int, y int)
;
----------
[CREATE_TABLE|SELECT] create table bigt as select x from generate_series(1, 10000) x
;
----------
[CREATE_TABLE] CREATE TABLE collate_test21 (f2 text COLLATE "POSIX" REFERENCES collate_test20)
;
----------
[CREATE_TABLE|SELECT] -- Track the original tablespace, relfilenode and OID of each index
-- in the tree.
CREATE TEMP TABLE reindex_temp_before AS
  SELECT oid, relname, relfilenode, reltablespace
  FROM pg_class
    WHERE relname ~ 'tbspace_reindex_part_index'
;
