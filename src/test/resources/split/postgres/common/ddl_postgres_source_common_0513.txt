CREATE TABLE drop_stats_test_xact()
;
--
-- Multiranges don't have their own ownership or permissions.
--
create type textrange1 as range(subtype=text, multirange_type_name=multitextrange1, collation="C")
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_authid
;
ALTER TABLE pg_index ADD PRIMARY KEY USING INDEX pg_index_indexrelid_index
;
CREATE INDEX "tenk1_unique1" ON "public"."tenk1" USING "btree" ("unique1")
;
-- Change plt2_adv_p2 to contain '0005' in addition to '0004' and '0006' as
-- the key values
CREATE TABLE plt2_adv_p2_ext PARTITION OF plt2_adv FOR VALUES IN ('0004', '0005', '0006')
;
CREATE FUNCTION isnle(upc, upc)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- fails
ALTER TYPE test_type2 ALTER ATTRIBUTE b TYPE varchar CASCADE
;
CREATE TABLE "public"."prt1_n" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("c")
;
DROP TABLE errtst
;
-- value is less than min size error
VACUUM (BUFFER_USAGE_LIMIT 120) vac_option_tab
;
-- Create table for check on foreign key dependence switch with indexes swapped
ALTER TABLE concur_reindex_tab ADD PRIMARY KEY USING INDEX concur_reindex_ind1
;
DROP POLICY policy_range_parted_wholerow ON range_parted
;
alter table atacc1 add constraint atacc1_constr_invalid check(test_a is not null) not valid
;
CREATE FUNCTION lt_q_rregex(_lquery,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- The object here is to ensure that toasted references inside
-- composite values don't cause problems.  The large f1 value will
-- be toasted inside pp, it must still work after being copied to people.

create temp table pp (f1 text)
;
ALTER TABLE table_with_unique_not_null ALTER COLUMN id SET NOT NULL
;
-- this should fail because already gone
drop domain domaindroptest cascade
;
create table mclparted (a int) partition by list(a)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_foreign_data_wrapper
;
DROP TABLE nametbl
;
CREATE TABLE "public"."rtest_emp" (
    "ename" character(20),
    "salary" "money"
)
;
CREATE FUNCTION lt_q_regex(ltree,_lquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER SEQUENCE "public"."itest5_a_seq" OWNED BY "public"."itest5"."a"
;
create database abc owner zyc
;
CREATE TABLE foo_range_parted1 PARTITION OF foo_range_parted FOR VALUES FROM (1) TO (10)
;
create schema fkpart2
  create table pkey (a int primary key)
  create table fk_part (a int, constraint fkey foreign key (a) references fkpart2.pkey) partition by list (a)
  create table fk_part_1 partition of fkpart2.fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 (a int, constraint my_fkey foreign key (a) references fkpart2.pkey)
;
ALTER TABLE ONLY "public"."alpha" ATTACH PARTITION "public"."alpha_neg" FOR VALUES FROM ('-Infinity') TO ('0')
;
ANALYZE vacuum_tab
;
drop function make_bogus_matview()
;
CREATE temporary TABLE temporary_transactional (id int not null, data text not null) WITH (AUTOVACUUM_ENABLED = false)
;
CREATE TABLE ac_test (a int)
;
DROP TABLE tx3
;
alter index gin_test_idx set (fastupdate = off)
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "file" WITH "simple"
;
drop index tstidx
;
CREATE UNIQUE INDEX agg_sort_order_c2_idx ON agg_sort_order(c2)
;
create view test_view as select 1
;
-- Drop the trigger. Now the row should be moved.
DROP TRIGGER trig_skip_delete ON sub_part2
;
ANALYZE plt2_adv
;
DROP TABLE test_ext.a CASCADE
;
create table parted_conflict_test_3 partition of parted_conflict_test for values in (4)
;
--    N    N        P    N
-- should ERROR: tfnp(int[], anyelement) not matched by tfnp(int[], int)
CREATE AGGREGATE myaggn07a(BASETYPE = anyelement, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
ALTER TABLE rewrite_test
        ADD COLUMN empty5 text,
        ADD COLUMN notempty5_norewrite int default 42,
        ADD COLUMN notempty5_rewrite serial
;
CREATE TABLE dept (
	dname		name,
	mgrname 	text
)
;
DROP TABLE enumtest
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
ALTER TYPE test8b ADD ATTRIBUTE c priv_testdomain1
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
DROP schema sampledb
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE drop_stats_test_xact()
;
----------
[CREATE_POLICY] --
-- Multiranges don't have their own ownership or permissions.
--
create type textrange1 as range(subtype=text, multirange_type_name=multitextrange1, collation="C")
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_authid
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_index ADD PRIMARY KEY USING INDEX pg_index_indexrelid_index
;
----------
[ADD_INDEX] CREATE INDEX "tenk1_unique1" ON "public"."tenk1" USING "btree" ("unique1")
;
----------
[CREATE_TABLE] -- Change plt2_adv_p2 to contain '0005' in addition to '0004' and '0006' as
-- the key values
CREATE TABLE plt2_adv_p2_ext PARTITION OF plt2_adv FOR VALUES IN ('0004', '0005', '0006')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(upc, upc)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TYPE] -- fails
ALTER TYPE test_type2 ALTER ATTRIBUTE b TYPE varchar CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_n" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("c")
;
----------
[DROP_TABLE] DROP TABLE errtst
;
----------
[ADMIN_TABLE] -- value is less than min size error
VACUUM (BUFFER_USAGE_LIMIT 120) vac_option_tab
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Create table for check on foreign key dependence switch with indexes swapped
ALTER TABLE concur_reindex_tab ADD PRIMARY KEY USING INDEX concur_reindex_ind1
;
----------
[DROP_POLICY] DROP POLICY policy_range_parted_wholerow ON range_parted
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc1_constr_invalid check(test_a is not null) not valid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lt_q_rregex(_lquery,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- The object here is to ensure that toasted references inside
-- composite values don't cause problems.  The large f1 value will
-- be toasted inside pp, it must still work after being copied to people.

create temp table pp (f1 text)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE table_with_unique_not_null ALTER COLUMN id SET NOT NULL
;
----------
[DROP_TYPE] -- this should fail because already gone
drop domain domaindroptest cascade
;
----------
[CREATE_TABLE] create table mclparted (a int) partition by list(a)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_foreign_data_wrapper
;
----------
[DROP_TABLE] DROP TABLE nametbl
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_emp" (
    "ename" character(20),
    "salary" "money"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lt_q_regex(ltree,_lquery)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."itest5_a_seq" OWNED BY "public"."itest5"."a"
;
----------
[CREATE_CATALOG] create database abc owner zyc
;
----------
[CREATE_TABLE] CREATE TABLE foo_range_parted1 PARTITION OF foo_range_parted FOR VALUES FROM (1) TO (10)
;
----------
[CREATE_SCHEMA] create schema fkpart2
  create table pkey (a int primary key)
  create table fk_part (a int, constraint fkey foreign key (a) references fkpart2.pkey) partition by list (a)
  create table fk_part_1 partition of fkpart2.fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 (a int, constraint my_fkey foreign key (a) references fkpart2.pkey)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."alpha" ATTACH PARTITION "public"."alpha_neg" FOR VALUES FROM ('-Infinity') TO ('0')
;
----------
[ADMIN_TABLE] ANALYZE vacuum_tab
;
----------
[DROP_PROG_OBJ] drop function make_bogus_matview()
;
----------
[CREATE_TABLE] CREATE temporary TABLE temporary_transactional (id int not null, data text not null) WITH (AUTOVACUUM_ENABLED = false)
;
----------
[CREATE_TABLE] CREATE TABLE ac_test (a int)
;
----------
[DROP_TABLE] DROP TABLE tx3
;
----------
[ALTER_INDEX] alter index gin_test_idx set (fastupdate = off)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "file" WITH "simple"
;
----------
[DROP_INDEX] drop index tstidx
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX agg_sort_order_c2_idx ON agg_sort_order(c2)
;
----------
[CREATE_VIEW(SELECT)] create view test_view as select 1
;
----------
[DROP_TRIGGER] -- Drop the trigger. Now the row should be moved.
DROP TRIGGER trig_skip_delete ON sub_part2
;
----------
[ADMIN_TABLE] ANALYZE plt2_adv
;
----------
[DROP_TABLE] DROP TABLE test_ext.a CASCADE
;
----------
[CREATE_TABLE] create table parted_conflict_test_3 partition of parted_conflict_test for values in (4)
;
----------
[CREATE_PROG_OBJ] --    N    N        P    N
-- should ERROR: tfnp(int[], anyelement) not matched by tfnp(int[], int)
CREATE AGGREGATE myaggn07a(BASETYPE = anyelement, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE rewrite_test
        ADD COLUMN empty5 text,
        ADD COLUMN notempty5_norewrite int default 42,
        ADD COLUMN notempty5_rewrite serial
;
----------
[CREATE_TABLE] CREATE TABLE dept (
	dname		name,
	mgrname 	text
)
;
----------
[DROP_TABLE] DROP TABLE enumtest
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
----------
[ALTER_TYPE] ALTER TYPE test8b ADD ATTRIBUTE c priv_testdomain1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
----------
[DROP_SCHEMA] DROP schema sampledb
;
