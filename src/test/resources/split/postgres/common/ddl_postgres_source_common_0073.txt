ALTER TABLE "regress_rls_schema"."rls_tbl" ENABLE ROW LEVEL SECURITY
;
alter table mutable add column f3 int
;
CREATE TABLE "public"."itest7" (
    "a" integer NOT NULL
)
;
CREATE DATABASE dropme_db1 WITH TABLESPACE dropme_ts1 STRATEGY=FILE_COPY
;
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (1) TO (0)
;
create or replace function ret_query2(lim int) returns setof record_type as $$
begin
    return query select md5(s.x::text), s.x, s.x > 0
                 from generate_series(-8, lim) s (x) where s.x % 2 = 0;
end;
$$ language plpgsql
;
DROP FUNCTION gen_subxids(integer)
;
--
-- Test that an IN implemented using a UniquePath does unique-ification
-- with the right semantics, as per bug #4113.  (Unfortunately we have
-- no simple way to ensure that this test case actually chooses that type
-- of plan, but it does in releases 7.4-8.3.  Note that an ordering difference
-- here might mean that some other plan type is being used, rendering the test
-- pointless.)
--

create temp table numeric_table (num_col numeric)
;
CREATE FUNCTION hstore(text[],text[])
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_arrays'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
create function sp_parallel_restricted(int) returns int as
  $$begin return $1; end$$ language plpgsql parallel restricted
;
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest2, ftest1) REFERENCES pktable(ptest2, ptest1))
;
ALTER INDEX pg_toast.pg_toast_30742_index RENAME TO reind_con_toast_idx
;
-- Ensure a MergeAppend is used when a partition exists with interleaved
-- datums in the partition bound.
create table mclparted3_5 partition of mclparted for values in(3,5)
;
CREATE FUNCTION tp_chk_data(OUT pktb regclass, OUT pkval int, OUT fktb regclass, OUT fkval int)
  RETURNS SETOF record LANGUAGE plpgsql AS $$
  BEGIN
    RETURN QUERY SELECT
      pk.tableoid::regclass, pk.a, fk.tableoid::regclass, fk.a
    FROM truncprim pk FULL JOIN truncpart fk USING (a)
    ORDER BY 2, 4;
  END
$$
;
create temp table mytable(f1 int, f2 int, f3 int)
;
alter table atacc1 drop constraint atacc1_constr_invalid
;
CREATE DOMAIN regress_zeropriv_domain AS int
;
CREATE EXTENSION pg_stat_statements
;
CREATE UNIQUE INDEX "phone_name" ON "public"."phone" USING "btree" ("slotname")
;
DROP TABLE test_ext4_tab
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_sequence
;
ALTER TABLE pg_toast.pg_toast_28046 RENAME TO reind_con_toast
;
VACUUM brintest_3
;
ALTER DATABASE "ecpg1_regression" SET bytea_output TO 'hex'
;
CREATE TABLE current_check_2 (a int, b text)
;
-- not OK
DROP TABLE unlogged1, public.unlogged2
;
ALTER TABLE testpub_tbl5 ALTER b SET NOT NULL, ALTER c SET NOT NULL
;
VACUUM (PARALLEL 2, FULL TRUE) pvactst
;
-- 2 new columns, second has default
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE TRIGGER schema_trig BEFORE INSERT ON schema_not_existing.tab
  EXECUTE FUNCTION schema_trig.no_func()
;
do $$
  declare
    r record;
  begin
    select * into r from test1;
    r.b := (select test1.b from test1);
    delete from test1;
    commit;
    perform pg_advisory_lock(1);
    raise notice 'length(r) = %', length(r::text);
  end;
$$
;
create function insert_tt(text) returns int as
$$ insert into tt(data) values($1) returning f1 $$
language sql
;
TRUNCATE tab1 RESTART IDENTITY
;
CREATE TABLE test_name (
	i name
)
;
CREATE FUNCTION dblink_fetch (text, int)
RETURNS setof record
AS '$libdir/dblink','dblink_fetch'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
DROP TABLE pg_temp_13.pg_temp_26115_2, pg_temp_13.pg_temp_26115
;
CREATE SUBSCRIPTION regress_mysub1 CONNECTION 'port=15035 host=/tmp/2MdPFByj7i dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub1_slot, failover = true, create_slot = false)
;
create trigger parent_del_trig before delete on parent
  for each row execute procedure parent_del_func()
;
-- Also test non-default rowcount estimate
CREATE FUNCTION my_gen_series(int, int) RETURNS SETOF integer
  LANGUAGE internal STRICT IMMUTABLE PARALLEL SAFE
  AS $$generate_series_int4$$
  SUPPORT test_support_func
;
--
-- Single key tests
--
create unique index key_index on insertconflicttest(key)
;
CREATE TABLE fail_part PARTITION OF unparted FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
ALTER INDEX pg_toast.pg_toast_31088_index RENAME TO reind_con_toast_idx
;
ANALYZE fract_t
;
DROP TABLE pg_temp_24.pg_temp_25663_2, pg_temp_24.pg_temp_25663
;
create table s3.p1_2 partition of s3.p1 for values in (4, 5, 6)
;
drop function excpt_test2()
;
CREATE INDEX t2_hash ON s1.t2 USING HASH (i)
;
ALTER TABLE pg_toast.pg_toast_33302 RENAME TO reind_con_toast
;
-- test jsonb null -> python None
CREATE FUNCTION test1null(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == None)
return 1
$$
;
ALTER TABLE pagg_tab_ml ATTACH PARTITION pagg_tab_ml_p3 FOR VALUES FROM (20) TO (30)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] ALTER TABLE "regress_rls_schema"."rls_tbl" ENABLE ROW LEVEL SECURITY
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table mutable add column f3 int
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest7" (
    "a" integer NOT NULL
)
;
----------
[CREATE_CATALOG] CREATE DATABASE dropme_db1 WITH TABLESPACE dropme_ts1 STRATEGY=FILE_COPY
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (1) TO (0)
;
----------
[CREATE_PROG_OBJ] create or replace function ret_query2(lim int) returns setof record_type as $$
begin
    return query select md5(s.x::text), s.x, s.x > 0
                 from generate_series(-8, lim) s (x) where s.x % 2 = 0;
end;
$$ language plpgsql
;
----------
[DROP_PROG_OBJ] DROP FUNCTION gen_subxids(integer)
;
----------
[CREATE_TABLE] --
-- Test that an IN implemented using a UniquePath does unique-ification
-- with the right semantics, as per bug #4113.  (Unfortunately we have
-- no simple way to ensure that this test case actually chooses that type
-- of plan, but it does in releases 7.4-8.3.  Note that an ordering difference
-- here might mean that some other plan type is being used, rendering the test
-- pointless.)
--

create temp table numeric_table (num_col numeric)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore(text[],text[])
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_arrays'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function sp_parallel_restricted(int) returns int as
  $$begin return $1; end$$ language plpgsql parallel restricted
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest2, ftest1) REFERENCES pktable(ptest2, ptest1))
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30742_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- Ensure a MergeAppend is used when a partition exists with interleaved
-- datums in the partition bound.
create table mclparted3_5 partition of mclparted for values in(3,5)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tp_chk_data(OUT pktb regclass, OUT pkval int, OUT fktb regclass, OUT fkval int)
  RETURNS SETOF record LANGUAGE plpgsql AS $$
  BEGIN
    RETURN QUERY SELECT
      pk.tableoid::regclass, pk.a, fk.tableoid::regclass, fk.a
    FROM truncprim pk FULL JOIN truncpart fk USING (a)
    ORDER BY 2, 4;
  END
$$
;
----------
[CREATE_TABLE] create temp table mytable(f1 int, f2 int, f3 int)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table atacc1 drop constraint atacc1_constr_invalid
;
----------
[CREATE_TYPE] CREATE DOMAIN regress_zeropriv_domain AS int
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pg_stat_statements
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "phone_name" ON "public"."phone" USING "btree" ("slotname")
;
----------
[DROP_TABLE] DROP TABLE test_ext4_tab
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_sequence
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28046 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM brintest_3
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg1_regression" SET bytea_output TO 'hex'
;
----------
[CREATE_TABLE] CREATE TABLE current_check_2 (a int, b text)
;
----------
[DROP_TABLE] -- not OK
DROP TABLE unlogged1, public.unlogged2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE testpub_tbl5 ALTER b SET NOT NULL, ALTER c SET NOT NULL
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2, FULL TRUE) pvactst
;
----------
[CREATE_TABLE] -- 2 new columns, second has default
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
----------
[CREATE_SCHEMA(CALL_PROG_OBJ)] CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE TRIGGER schema_trig BEFORE INSERT ON schema_not_existing.tab
  EXECUTE FUNCTION schema_trig.no_func()
;
----------
[BLOCK] do $$
  declare
    r record;
  begin
    select * into r from test1;
    r.b := (select test1.b from test1);
    delete from test1;
    commit;
    perform pg_advisory_lock(1);
    raise notice 'length(r) = %', length(r::text);
  end;
$$
;
----------
[CREATE_PROG_OBJ] create function insert_tt(text) returns int as
$$ insert into tt(data) values($1) returning f1 $$
language sql
;
----------
[TRUNCATE_TABLE] TRUNCATE tab1 RESTART IDENTITY
;
----------
[CREATE_TABLE] CREATE TABLE test_name (
	i name
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_fetch (text, int)
RETURNS setof record
AS '$libdir/dblink','dblink_fetch'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26115_2, pg_temp_13.pg_temp_26115
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_mysub1 CONNECTION 'port=15035 host=/tmp/2MdPFByj7i dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub1_slot, failover = true, create_slot = false)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parent_del_trig before delete on parent
  for each row execute procedure parent_del_func()
;
----------
[CREATE_PROG_OBJ] -- Also test non-default rowcount estimate
CREATE FUNCTION my_gen_series(int, int) RETURNS SETOF integer
  LANGUAGE internal STRICT IMMUTABLE PARALLEL SAFE
  AS $$generate_series_int4$$
  SUPPORT test_support_func
;
----------
[ADD_INDEX] --
-- Single key tests
--
create unique index key_index on insertconflicttest(key)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF unparted FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_31088_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE fract_t
;
----------
[DROP_TABLE] DROP TABLE pg_temp_24.pg_temp_25663_2, pg_temp_24.pg_temp_25663
;
----------
[CREATE_TABLE] create table s3.p1_2 partition of s3.p1 for values in (4, 5, 6)
;
----------
[DROP_PROG_OBJ] drop function excpt_test2()
;
----------
[ADD_INDEX] CREATE INDEX t2_hash ON s1.t2 USING HASH (i)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33302 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] -- test jsonb null -> python None
CREATE FUNCTION test1null(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == None)
return 1
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pagg_tab_ml ATTACH PARTITION pagg_tab_ml_p3 FOR VALUES FROM (20) TO (30)
;
