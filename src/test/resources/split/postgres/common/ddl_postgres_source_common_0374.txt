/* New stuff in 1.1 begins here */

CREATE FUNCTION pgstatginindex(IN relname regclass,
    OUT version INT4,
    OUT pending_pages INT4,
    OUT pending_tuples BIGINT)
AS '$libdir/pgstattuple', 'pgstatginindex'
LANGUAGE C STRICT PARALLEL SAFE
;
-- Verify that toast tables are clusterable
CLUSTER pg_toast.pg_toast_826 USING pg_toast_826_index
;
ALTER TABLE tt7 OF tt_t0
;
create table unparted_trigger (a int, b text)
;
CREATE RULE r1 AS ON INSERT TO pg_description DO INSTEAD NOTHING
;
CREATE TRIGGER gtest2 BEFORE INSERT ON gtest26
  FOR EACH ROW
  WHEN (NEW.a < 0)
  EXECUTE PROCEDURE gtest_trigger_func()
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
VACUUM (PARALLEL 2) pg_catalog.pg_range
;
ANALYZE public.itest4
;
CREATE TRIGGER parent_tbl_delete_trig
  AFTER DELETE ON parent_tbl REFERENCING OLD TABLE AS old_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
CREATE SCHEMA sts_sch1 CREATE TABLE sts_sch1.tbl (a INT, b INT)
;
CREATE schema sampledb
;
VACUUM (PARALLEL 2) pg_catalog.pg_attrdef
;
CREATE TABLE "public"."gb18030_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
ALTER TABLE regtest_table_4
      ADD CONSTRAINT regtest_tbl4_con EXCLUDE USING btree (z WITH =)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29121 host=/tmp/t7ulhX_Bso dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
CREATE TABLE part_b_20_b_30 (e varchar, c numeric, a text, b bigint, d int)
;
CREATE PROCEDURE consume_xid(cnt int)
AS $$
DECLARE
    i int;
    BEGIN
        FOR i in 1..cnt LOOP
            EXECUTE 'SELECT txid_current()';
            COMMIT;
        END LOOP;
    END;
$$
LANGUAGE plpgsql
;
CREATE FUNCTION dintdict_init(internal)
        RETURNS internal
        AS '$libdir/dict_int'
        LANGUAGE C STRICT
;
alter extension postgres_fdw add function least_accum(anyelement, variadic anyarray)
;
CREATE RULE "_RETURN" AS ON SELECT TO tt28 DO INSTEAD SELECT * FROM tt26
;
create trigger tg_room_au after update
    on Room for each row execute procedure tg_room_au()
;
CREATE UNIQUE INDEX mvtest_tm_type ON mvtest_tm (type)
;
-- BUG: https://postgr.es/m/CADXhmgTJtJZK9A3Na_ry%2BXrq-ghjcejBRhcRMzWZvbd__QdgJA%40mail.gmail.com
-- datum_to_jsonb_internal() didn't catch keys that are casts instead of a simple scalar
CREATE TYPE mood AS ENUM ('happy', 'sad', 'neutral')
;
DROP OPERATOR FAMILY alt_opf11 USING gist
;
-- fail, duplicate name
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (provider = icu, locale = ' ||
          quote_literal((SELECT CASE WHEN datlocprovider='i' THEN datlocale ELSE datcollate END FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
ALTER TYPE "public"."insenum" ADD VALUE 'i26'
;
drop table inserttest
;
CREATE TABLE testpub_insert_onconfl_no_ri (a int unique, b int)
;
CREATE INDEX test_gin_idx ON test USING gin (a)
;
CREATE INDEX clstr_tst_c ON clstr_tst (c)
;
CREATE FUNCTION testprs_lextype(internal)
RETURNS internal
AS 'MODULE_PATHNAME'
LANGUAGE C STRICT
;
create table rlp5_default partition of rlp5 default
;
--
-- ALTER xxx SET SCHEMA
--
ALTER TABLE regtest_table_1 SET SCHEMA regtest_schema_2
;
ALTER FOREIGN TABLE ft1 DROP COLUMN c0
;
create table hp_contradict_test (a int, b int) partition by hash (a part_test_int4_ops2, b part_test_int4_ops2)
;
CREATE TABLE nummultirange_test (nmr NUMMULTIRANGE)
;
ALTER INDEX pg_toast.pg_toast_30850_index RENAME TO reind_con_toast_idx
;
ANALYZE pht1_e
;
/*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
CREATE INDEX t2_spgist ON s1.t2 USING SPGIST (ir)
;
ANALYZE public.test_inh_check
;
ALTER TABLE ONLY "fkpart3"."pk5"
    ADD CONSTRAINT "pk5_pkey" PRIMARY KEY ("a")
;
-- Test a more complex case where the NULL partition allows some other value
drop table mclparted_null
;
-- Subselect in rangetable
CREATE VIEW rw_view14 AS SELECT ctid, a, b FROM base_tbl
;
CREATE TEXT SEARCH CONFIGURATION dump_test.alt_ts_conf1 (copy=english)
;
CREATE FUNCTION isnle(ean13, ismn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TRIGGER gtest12_01 BEFORE UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func()
;
-- fails

create aggregate least_agg(int8) (
  stype = int8, sfunc = least_accum
)
;
DROP SCHEMA s1 CASCADE
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] /* New stuff in 1.1 begins here */

CREATE FUNCTION pgstatginindex(IN relname regclass,
    OUT version INT4,
    OUT pending_pages INT4,
    OUT pending_tuples BIGINT)
AS '$libdir/pgstattuple', 'pgstatginindex'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] -- Verify that toast tables are clusterable
CLUSTER pg_toast.pg_toast_826 USING pg_toast_826_index
;
----------
[ALTER_TABLE] ALTER TABLE tt7 OF tt_t0
;
----------
[CREATE_TABLE] create table unparted_trigger (a int, b text)
;
----------
[CREATE_POLICY] CREATE RULE r1 AS ON INSERT TO pg_description DO INSTEAD NOTHING
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest2 BEFORE INSERT ON gtest26
  FOR EACH ROW
  WHEN (NEW.a < 0)
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_range
;
----------
[ADMIN_TABLE] ANALYZE public.itest4
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER parent_tbl_delete_trig
  AFTER DELETE ON parent_tbl REFERENCING OLD TABLE AS old_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
----------
[CREATE_SCHEMA] CREATE SCHEMA sts_sch1 CREATE TABLE sts_sch1.tbl (a INT, b INT)
;
----------
[CREATE_SCHEMA] CREATE schema sampledb
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_attrdef
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gb18030_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE regtest_table_4
      ADD CONSTRAINT regtest_tbl4_con EXCLUDE USING btree (z WITH =)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=29121 host=/tmp/t7ulhX_Bso dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_TABLE] CREATE TABLE part_b_20_b_30 (e varchar, c numeric, a text, b bigint, d int)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE consume_xid(cnt int)
AS $$
DECLARE
    i int;
    BEGIN
        FOR i in 1..cnt LOOP
            EXECUTE 'SELECT txid_current()';
            COMMIT;
        END LOOP;
    END;
$$
LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dintdict_init(internal)
        RETURNS internal
        AS '$libdir/dict_int'
        LANGUAGE C STRICT
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw add function least_accum(anyelement, variadic anyarray)
;
----------
[CREATE_POLICY] CREATE RULE "_RETURN" AS ON SELECT TO tt28 DO INSTEAD SELECT * FROM tt26
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_room_au after update
    on Room for each row execute procedure tg_room_au()
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX mvtest_tm_type ON mvtest_tm (type)
;
----------
[CREATE_TYPE] -- BUG: https://postgr.es/m/CADXhmgTJtJZK9A3Na_ry%2BXrq-ghjcejBRhcRMzWZvbd__QdgJA%40mail.gmail.com
-- datum_to_jsonb_internal() didn't catch keys that are casts instead of a simple scalar
CREATE TYPE mood AS ENUM ('happy', 'sad', 'neutral')
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf11 USING gist
;
----------
[BLOCK] -- fail, duplicate name
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (provider = icu, locale = ' ||
          quote_literal((SELECT CASE WHEN datlocprovider='i' THEN datlocale ELSE datcollate END FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i26'
;
----------
[DROP_TABLE] drop table inserttest
;
----------
[CREATE_TABLE] CREATE TABLE testpub_insert_onconfl_no_ri (a int unique, b int)
;
----------
[ADD_INDEX] CREATE INDEX test_gin_idx ON test USING gin (a)
;
----------
[ADD_INDEX] CREATE INDEX clstr_tst_c ON clstr_tst (c)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testprs_lextype(internal)
RETURNS internal
AS 'MODULE_PATHNAME'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] create table rlp5_default partition of rlp5 default
;
----------
[ALTER_TABLE] --
-- ALTER xxx SET SCHEMA
--
ALTER TABLE regtest_table_1 SET SCHEMA regtest_schema_2
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE ft1 DROP COLUMN c0
;
----------
[CREATE_TABLE] create table hp_contradict_test (a int, b int) partition by hash (a part_test_int4_ops2, b part_test_int4_ops2)
;
----------
[CREATE_TABLE] CREATE TABLE nummultirange_test (nmr NUMMULTIRANGE)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30850_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE pht1_e
;
----------
[CREATE_POLICY] /*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
----------
[ADD_INDEX] CREATE INDEX t2_spgist ON s1.t2 USING SPGIST (ir)
;
----------
[ADMIN_TABLE] ANALYZE public.test_inh_check
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart3"."pk5"
    ADD CONSTRAINT "pk5_pkey" PRIMARY KEY ("a")
;
----------
[DROP_TABLE] -- Test a more complex case where the NULL partition allows some other value
drop table mclparted_null
;
----------
[CREATE_VIEW(SELECT)] -- Subselect in rangetable
CREATE VIEW rw_view14 AS SELECT ctid, a, b FROM base_tbl
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION dump_test.alt_ts_conf1 (copy=english)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ean13, ismn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest12_01 BEFORE UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[CREATE_PROG_OBJ] -- fails

create aggregate least_agg(int8) (
  stype = int8, sfunc = least_accum
)
;
----------
[DROP_SCHEMA] DROP SCHEMA s1 CASCADE
;
