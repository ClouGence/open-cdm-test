ALTER INDEX pg_toast.pg_toast_28239_index RENAME TO reind_con_toast_idx
;
CREATE FOREIGN TABLE ft_part_1_1
  PARTITION OF lt1_part1 FOR VALUES FROM (0) TO (100) SERVER s0
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN c3 integer primary key
;
CREATE TYPE mood AS ENUM ('happy', 'sad', 'neutral')
;
CREATE AGGREGATE testagg1(int2) (SFUNC = int2_sum, STYPE = int8)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_parameter_acl
;
CREATE AGGREGATE "public"."myaggp09a"(integer) (
    SFUNC = "public"."tf1p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
ALTER TABLE ctl_table ALTER COLUMN b SET STORAGE MAIN
;
--    P    P        P    P
-- should ERROR: ffnp(anyarray) not matched by ffnp(int[])
CREATE AGGREGATE myaggn20a(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE CONVERSION test_conversion_exists
    FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_opclass
;
CREATE TRIGGER "after_ins_stmt_trig" AFTER INSERT ON "public"."main_table" FOR EACH STATEMENT EXECUTE FUNCTION "public"."trigger_func"('after_ins_stmt')
;
CREATE PROCEDURE test_proc4(y int)
LANGUAGE plpgsql
AS $$
BEGIN
    CALL test_proc3(y);
    CALL test_proc3($1);
END;
$$
;
DROP EXTENSION plperlu CASCADE
;
DROP TRIGGER gtest2 ON gtest26
;
--
-- Btree_gist is not included by default, so to test exclusion
-- constraints with range types, use singleton int ranges for the "="
-- portion of the constraint.
--

create table test_range_excl(
  room int4range,
  speaker int4range,
  during tsrange,
  exclude using gist (room with =, during with &&),
  exclude using gist (speaker with =, during with &&)
)
;
CREATE TABLE "public"."clstr_tst_s" (
    "rf_a" integer NOT NULL,
    "b" integer
)
;
ALTER TEXT SEARCH CONFIGURATION basque ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH basque_stem
;
CREATE TABLE shoelace_log (
        sl_name    char(10),      -- shoelace changed
        sl_avail   integer,       -- new available value
        log_who    name,          -- who did it
        log_when   timestamp      -- when
    )
;
/*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
CREATE INDEX grect2ind2 ON fast_emp4000 USING gist2 (home_base)
;
-- test pruning when not all the partitions exist
drop table hp1
;
ALTER INDEX "fkpart5"."pk3_pkey" ATTACH PARTITION "fkpart5"."pk32_pkey"
;
CREATE FUNCTION buffer_call_terminate_io(buffer int, for_input bool, succeed bool, io_error bool, release_aio bool)
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE TABLE tgt (a int, b int, c int, d int)
;
CREATE FUNCTION btismn13cmp(ismn13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP TABLE gtest31_1, gtest31_2
;
CREATE UNLOGGED TABLE "public"."itest17" (
    "a" integer NOT NULL,
    "b" "text",
    "c" integer NOT NULL
)
;
ALTER INDEX pg_toast.pg_toast_28165_index RENAME TO reind_con_toast_idx
;
CREATE TABLE gtest1_y () INHERITS (gtest1, gtesty)
;
DROP TABLE test_tbl1
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_foreign_data_wrapper
;
ANALYZE public.itest6
;
CREATE UNIQUE INDEX "num_exp_mul_idx" ON "public"."num_exp_mul" USING "btree" ("id1", "id2")
;
DROP TABLE numrange_test2
;
CREATE TABLE split_owned_schema.session_table (id integer)
;
CREATE TABLE "public"."pitest2_p2" (
    "f1" "date" CONSTRAINT "pitest2_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" integer CONSTRAINT "pitest2_f3_not_null" NOT NULL
)
;
ALTER TABLE split_alter_table.base_table ALTER COLUMN identity_col SET GENERATED ALWAYS SET INCREMENT BY 2 SET START WITH 10 RESTART WITH 10
;
CREATE TYPE hash_test_t1 AS (a int, b text)
;
VACUUM (ANALYZE) pg_catalog.pg_ts_config
;
CREATE VIEW "public"."rtest_vview3" AS
 SELECT "a",
    "b"
   FROM "public"."rtest_vview2" "x"
  WHERE (0 < ( SELECT "count"(*) AS "count"
           FROM "public"."rtest_view2" "y"
          WHERE ("y"."a" = "x"."a")))
;
-- add a unique constraint
alter table atacc1 add constraint atacc_test1 unique (test)
;
CREATE TRIGGER rep_a_i AFTER INSERT ON trigtest FOR EACH ROW EXECUTE PROCEDURE trig_report()
;
drop table nonesuch
;
CREATE TRIGGER trans_updatetrig
  AFTER UPDATE ON range_parted REFERENCING OLD TABLE AS old_table NEW TABLE AS new_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trans_updatetrigfunc()
;
/*
 * 5.61
 * TABLE_CONSTRAINTS view
 */

CREATE VIEW table_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST(
             CASE c.contype WHEN 'c' THEN 'CHECK'
                            WHEN 'f' THEN 'FOREIGN KEY'
                            WHEN 'p' THEN 'PRIMARY KEY'
                            WHEN 'u' THEN 'UNIQUE' END
             AS character_data) AS constraint_type,
           CAST(CASE WHEN c.condeferrable THEN 'YES' ELSE 'NO' END AS yes_or_no)
             AS is_deferrable,
           CAST(CASE WHEN c.condeferred THEN 'YES' ELSE 'NO' END AS yes_or_no)
             AS initially_deferred,
           CAST('YES' AS yes_or_no) AS enforced,
           CAST(CASE WHEN c.contype = 'u'
                     THEN CASE WHEN (SELECT NOT indnullsnotdistinct FROM pg_index WHERE indexrelid = conindid) THEN 'YES' ELSE 'NO' END
                     END
                AS yes_or_no) AS nulls_distinct

    FROM pg_namespace nc,
         pg_namespace nr,
         pg_constraint c,
         pg_class r

    WHERE nc.oid = c.connamespace AND nr.oid = r.relnamespace
          AND c.conrelid = r.oid
          AND c.contype NOT IN ('t', 'x')  -- ignore nonstandard constraints
          AND r.relkind IN ('r', 'p')
          AND (NOT pg_is_other_temp_schema(nr.oid))
          AND (pg_has_role(r.relowner, 'USAGE')
               -- SELECT privilege omitted, per SQL standard
               OR has_table_privilege(r.oid, 'INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(r.oid, 'INSERT, UPDATE, REFERENCES') )

    UNION ALL

    -- not-null constraints

    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nr.nspname AS sql_identifier) AS constraint_schema,
           CAST(CAST(nr.oid AS text) || '_' || CAST(r.oid AS text) || '_' || CAST(a.attnum AS text) || '_not_null' AS sql_identifier) AS constraint_name, -- XXX
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST('CHECK' AS character_data) AS constraint_type,
           CAST('NO' AS yes_or_no) AS is_deferrable,
           CAST('NO' AS yes_or_no) AS initially_deferred,
           CAST('YES' AS yes_or_no) AS enforced,
           CAST(NULL AS yes_or_no) AS nulls_distinct

    FROM pg_namespace nr,
         pg_class r,
         pg_attribute a

    WHERE nr.oid = r.relnamespace
          AND r.oid = a.attrelid
          AND a.attnotnull
          AND a.attnum > 0
          AND NOT a.attisdropped
          AND r.relkind IN ('r', 'p')
          AND (NOT pg_is_other_temp_schema(nr.oid))
          AND (pg_has_role(r.relowner, 'USAGE')
               -- SELECT privilege omitted, per SQL standard
               OR has_table_privilege(r.oid, 'INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(r.oid, 'INSERT, UPDATE, REFERENCES') )
;
CREATE TABLE fkpart11.pk12 (b text, c int, a int NOT NULL)
;
ALTER TABLE ONLY "public"."prt1_l_p2" ATTACH PARTITION "public"."prt1_l_p2_p2" FOR VALUES IN ('0002', '0003')
;
ALTER PUBLICATION "dump_pub_2ct" ADD TABLE ONLY "pubme"."t1"
;
CREATE UNIQUE INDEX sj_temp_idx1 ON sj(a,b,c)
;
------------------------------------------------------------------------------------------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28239_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft_part_1_1
  PARTITION OF lt1_part1 FOR VALUES FROM (0) TO (100) SERVER s0
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN c3 integer primary key
;
----------
[CREATE_TYPE] CREATE TYPE mood AS ENUM ('happy', 'sad', 'neutral')
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testagg1(int2) (SFUNC = int2_sum, STYPE = int8)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_parameter_acl
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp09a"(integer) (
    SFUNC = "public"."tf1p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ctl_table ALTER COLUMN b SET STORAGE MAIN
;
----------
[CREATE_PROG_OBJ] --    P    P        P    P
-- should ERROR: ffnp(anyarray) not matched by ffnp(int[])
CREATE AGGREGATE myaggn20a(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_POLICY] CREATE CONVERSION test_conversion_exists
    FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_opclass
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "after_ins_stmt_trig" AFTER INSERT ON "public"."main_table" FOR EACH STATEMENT EXECUTE FUNCTION "public"."trigger_func"('after_ins_stmt')
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc4(y int)
LANGUAGE plpgsql
AS $$
BEGIN
    CALL test_proc3(y);
    CALL test_proc3($1);
END;
$$
;
----------
[DROP_LIBRARY] DROP EXTENSION plperlu CASCADE
;
----------
[DROP_TRIGGER] DROP TRIGGER gtest2 ON gtest26
;
----------
[CREATE_TABLE] --
-- Btree_gist is not included by default, so to test exclusion
-- constraints with range types, use singleton int ranges for the "="
-- portion of the constraint.
--

create table test_range_excl(
  room int4range,
  speaker int4range,
  during tsrange,
  exclude using gist (room with =, during with &&),
  exclude using gist (speaker with =, during with &&)
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."clstr_tst_s" (
    "rf_a" integer NOT NULL,
    "b" integer
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION basque ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH basque_stem
;
----------
[CREATE_TABLE] CREATE TABLE shoelace_log (
        sl_name    char(10),      -- shoelace changed
        sl_avail   integer,       -- new available value
        log_who    name,          -- who did it
        log_when   timestamp      -- when
    )
;
----------
[CREATE_POLICY] /*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
----------
[ADD_INDEX] CREATE INDEX grect2ind2 ON fast_emp4000 USING gist2 (home_base)
;
----------
[DROP_TABLE] -- test pruning when not all the partitions exist
drop table hp1
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart5"."pk3_pkey" ATTACH PARTITION "fkpart5"."pk32_pkey"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION buffer_call_terminate_io(buffer int, for_input bool, succeed bool, io_error bool, release_aio bool)
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE tgt (a int, b int, c int, d int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btismn13cmp(ismn13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE gtest31_1, gtest31_2
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE "public"."itest17" (
    "a" integer NOT NULL,
    "b" "text",
    "c" integer NOT NULL
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28165_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE gtest1_y () INHERITS (gtest1, gtesty)
;
----------
[DROP_TABLE] DROP TABLE test_tbl1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_foreign_data_wrapper
;
----------
[ADMIN_TABLE] ANALYZE public.itest6
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "num_exp_mul_idx" ON "public"."num_exp_mul" USING "btree" ("id1", "id2")
;
----------
[DROP_TABLE] DROP TABLE numrange_test2
;
----------
[CREATE_TABLE] CREATE TABLE split_owned_schema.session_table (id integer)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest2_p2" (
    "f1" "date" CONSTRAINT "pitest2_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" integer CONSTRAINT "pitest2_f3_not_null" NOT NULL
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE split_alter_table.base_table ALTER COLUMN identity_col SET GENERATED ALWAYS SET INCREMENT BY 2 SET START WITH 10 RESTART WITH 10
;
----------
[CREATE_TYPE] CREATE TYPE hash_test_t1 AS (a int, b text)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_ts_config
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_vview3" AS
 SELECT "a",
    "b"
   FROM "public"."rtest_vview2" "x"
  WHERE (0 < ( SELECT "count"(*) AS "count"
           FROM "public"."rtest_view2" "y"
          WHERE ("y"."a" = "x"."a")))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a unique constraint
alter table atacc1 add constraint atacc_test1 unique (test)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER rep_a_i AFTER INSERT ON trigtest FOR EACH ROW EXECUTE PROCEDURE trig_report()
;
----------
[DROP_TABLE] drop table nonesuch
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trans_updatetrig
  AFTER UPDATE ON range_parted REFERENCING OLD TABLE AS old_table NEW TABLE AS new_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trans_updatetrigfunc()
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.61
 * TABLE_CONSTRAINTS view
 */

CREATE VIEW table_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST(
             CASE c.contype WHEN 'c' THEN 'CHECK'
                            WHEN 'f' THEN 'FOREIGN KEY'
                            WHEN 'p' THEN 'PRIMARY KEY'
                            WHEN 'u' THEN 'UNIQUE' END
             AS character_data) AS constraint_type,
           CAST(CASE WHEN c.condeferrable THEN 'YES' ELSE 'NO' END AS yes_or_no)
             AS is_deferrable,
           CAST(CASE WHEN c.condeferred THEN 'YES' ELSE 'NO' END AS yes_or_no)
             AS initially_deferred,
           CAST('YES' AS yes_or_no) AS enforced,
           CAST(CASE WHEN c.contype = 'u'
                     THEN CASE WHEN (SELECT NOT indnullsnotdistinct FROM pg_index WHERE indexrelid = conindid) THEN 'YES' ELSE 'NO' END
                     END
                AS yes_or_no) AS nulls_distinct

    FROM pg_namespace nc,
         pg_namespace nr,
         pg_constraint c,
         pg_class r

    WHERE nc.oid = c.connamespace AND nr.oid = r.relnamespace
          AND c.conrelid = r.oid
          AND c.contype NOT IN ('t', 'x')  -- ignore nonstandard constraints
          AND r.relkind IN ('r', 'p')
          AND (NOT pg_is_other_temp_schema(nr.oid))
          AND (pg_has_role(r.relowner, 'USAGE')
               -- SELECT privilege omitted, per SQL standard
               OR has_table_privilege(r.oid, 'INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(r.oid, 'INSERT, UPDATE, REFERENCES') )

    UNION ALL

    -- not-null constraints

    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nr.nspname AS sql_identifier) AS constraint_schema,
           CAST(CAST(nr.oid AS text) || '_' || CAST(r.oid AS text) || '_' || CAST(a.attnum AS text) || '_not_null' AS sql_identifier) AS constraint_name, -- XXX
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST('CHECK' AS character_data) AS constraint_type,
           CAST('NO' AS yes_or_no) AS is_deferrable,
           CAST('NO' AS yes_or_no) AS initially_deferred,
           CAST('YES' AS yes_or_no) AS enforced,
           CAST(NULL AS yes_or_no) AS nulls_distinct

    FROM pg_namespace nr,
         pg_class r,
         pg_attribute a

    WHERE nr.oid = r.relnamespace
          AND r.oid = a.attrelid
          AND a.attnotnull
          AND a.attnum > 0
          AND NOT a.attisdropped
          AND r.relkind IN ('r', 'p')
          AND (NOT pg_is_other_temp_schema(nr.oid))
          AND (pg_has_role(r.relowner, 'USAGE')
               -- SELECT privilege omitted, per SQL standard
               OR has_table_privilege(r.oid, 'INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(r.oid, 'INSERT, UPDATE, REFERENCES') )
;
----------
[CREATE_TABLE] CREATE TABLE fkpart11.pk12 (b text, c int, a int NOT NULL)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_l_p2" ATTACH PARTITION "public"."prt1_l_p2_p2" FOR VALUES IN ('0002', '0003')
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION "dump_pub_2ct" ADD TABLE ONLY "pubme"."t1"
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX sj_temp_idx1 ON sj(a,b,c)
;
