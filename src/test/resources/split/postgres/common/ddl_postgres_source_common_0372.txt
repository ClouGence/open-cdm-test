CREATE TABLE test_tablesample (id int, name text) WITH (fillfactor=10)
;
--
-- insert...on conflict do unique index inference
--
create table insertconflicttest(key int4, fruit text)
;
create table point_table (a point, b int)
;
-- check validation when attaching list partitions
CREATE TABLE list_parted2 (
	a int,
	b char
) PARTITION BY LIST (a)
;
create table loc2 (f1 int, f2 text)
;
CREATE CAST ("public"."int8alias2" AS bigint) WITHOUT FUNCTION
;
VACUUM pg_catalog.pg_opclass
;
CREATE INDEX btree_part_idx ON btree_part(id)
;
CREATE TABLE bttest_unique1 (i int4)
;
DROP TABLE test_stat_tab
;
ALTER SCHEMA pub_test1_renamed RENAME to pub_test1
;
create table inttest (a myint)
;
ALTER SUBSCRIPTION regress_test_sub CONNECTION 'port=10248 host=/tmp/jZENFq3HZU user=regress_test_user dbname=postgres password=secret'
;
CREATE INDEX reloptions_test_idx3 ON reloptions_test (s)
;
DROP SCHEMA IF EXISTS split_event_helper CASCADE
;
drop table old_table1
;
create function outparam_fail(i anyelement, out r anyrange, out t text)
  as $$ select '[1,10]', 'foo' $$ language sql
;
create conversion alter1.ascii_to_utf8 for 'sql_ascii' to 'utf8' from ascii_to_utf8
;
alter table idxpart add unique (b, a)
;
CREATE FUNCTION gbtreekey4_in(cstring)
RETURNS gbtreekey4
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp_tz() < $.datetime("HH:MI"))'))
;
CREATE UNIQUE INDEX "covidxpart4_a_idx" ON "public"."covidxpart4" USING "btree" ("a")
;
ALTER PUBLICATION testpub_forparted ADD TABLE testpub_parted, testpub_parted2
;
CREATE OPERATOR "public".!=- (
    FUNCTION = "factorial",
    LEFTARG = bigint
)
;
-- fail

CREATE DOMAIN priv_testdomain1 AS int
;
--
-- Verify system catalog foreign key relationships
--
DO $doblock$
declare
  fk record;
  nkeys integer;
  cmd text;
  err record;
begin
  for fk in select * from pg_get_catalog_foreign_keys()
  loop
    raise notice 'checking % % => % %',
      fk.fktable, fk.fkcols, fk.pktable, fk.pkcols;
    nkeys := array_length(fk.fkcols, 1);
    cmd := 'SELECT ctid';
    for i in 1 .. nkeys loop
      cmd := cmd || ', ' || quote_ident(fk.fkcols[i]);
    end loop;
    if fk.is_array then
      cmd := cmd || ' FROM (SELECT ctid';
      for i in 1 .. nkeys-1 loop
        cmd := cmd || ', ' || quote_ident(fk.fkcols[i]);
      end loop;
      cmd := cmd || ', unnest(' || quote_ident(fk.fkcols[nkeys]);
      cmd := cmd || ') as ' || quote_ident(fk.fkcols[nkeys]);
      cmd := cmd || ' FROM ' || fk.fktable::text || ') fk WHERE ';
    else
      cmd := cmd || ' FROM ' || fk.fktable::text || ' fk WHERE ';
    end if;
    if fk.is_opt then
      for i in 1 .. nkeys loop
        cmd := cmd || quote_ident(fk.fkcols[i]) || ' != 0 AND ';
      end loop;
    end if;
    cmd := cmd || 'NOT EXISTS(SELECT 1 FROM ' || fk.pktable::text || ' pk WHERE ';
    for i in 1 .. nkeys loop
      if i > 1 then cmd := cmd || ' AND '; end if;
      cmd := cmd || 'pk.' || quote_ident(fk.pkcols[i]);
      cmd := cmd || ' = fk.' || quote_ident(fk.fkcols[i]);
    end loop;
    cmd := cmd || ')';
    -- raise notice 'cmd = %', cmd;
    for err in execute cmd loop
      raise warning 'FK VIOLATION IN %(%): %', fk.fktable, fk.fkcols, err;
    end loop;
  end loop;
end
$doblock$
;
-- vacuum the table, to discard TOAST data
VACUUM brintest_3
;
create table cnn_uq (a int)
;
-- cascade update/delete
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, PRIMARY KEY(ptest1, ptest2, ptest3) )
;
CREATE TEMP TABLE pg_temp_3.pg_temp_16651_2 (tid pg_catalog.tid)
;
CREATE TABLE schema_two.table_two(a int)
;
--
-- Tests for predicate handling
--

--
-- Test that restrictions that are always true are ignored, and that are always
-- false are replaced with constant-FALSE
--
-- Currently we only check for NullTest quals and OR clauses that include
-- NullTest quals.  We may extend it in the future.
--
CREATE TABLE pred_tab (a int NOT NULL, b int, c int NOT NULL)
;
CREATE TYPE double_int as (a int, b int)
;
CREATE EVENT TRIGGER split_event_probe_proc
    ON sql_drop
    EXECUTE PROCEDURE split_trigger_probe.event_logger()
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
DROP TABLE people
;
create or replace function test_pg_index_toast_func (a int, b int[])
  returns bool as $$ select true $$ language sql immutable
;
-- OK
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS '...invalid label...'
;
DROP TABLE pg_temp_3.pg_temp_27487_2, pg_temp_3.pg_temp_27487
;
CREATE TABLE "public"."xmltest2" (
    "x" "xml",
    "_path" "text"
)
;
CREATE TABLESPACE tblspc2 LOCATION '/tmp/5tJCEz0j6a/tempdir/tbl=spc2'
;
ALTER PUBLICATION testpub_foralltables DROP TABLE testpub_tbl2
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
ALTER DATABASE "pl_regression" SET lc_messages TO 'C'
;
ALTER TABLE fktable ADD CONSTRAINT fk_253_213
FOREIGN KEY (x2,x5,x3) REFERENCES pktable(id2,id1,id3)
;
create foreign table remt2 (a int, b text)
  server loopback options (table_name 'loct2')
;
CREATE TABLE s4.t2 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
create table rewritemetoo3 (a rewritetype)
;
ALTER TABLE ONLY "public"."domnotnull" DROP COLUMN "........pg.dropped.1........"
;
ALTER TABLE attmp3 VALIDATE CONSTRAINT b_greater_than_ten
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE test_tablesample (id int, name text) WITH (fillfactor=10)
;
----------
[CREATE_TABLE] --
-- insert...on conflict do unique index inference
--
create table insertconflicttest(key int4, fruit text)
;
----------
[CREATE_TABLE] create table point_table (a point, b int)
;
----------
[CREATE_TABLE] -- check validation when attaching list partitions
CREATE TABLE list_parted2 (
	a int,
	b char
) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] create table loc2 (f1 int, f2 text)
;
----------
[CREATE_PROG_OBJ] CREATE CAST ("public"."int8alias2" AS bigint) WITHOUT FUNCTION
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_opclass
;
----------
[ADD_INDEX] CREATE INDEX btree_part_idx ON btree_part(id)
;
----------
[CREATE_TABLE] CREATE TABLE bttest_unique1 (i int4)
;
----------
[DROP_TABLE] DROP TABLE test_stat_tab
;
----------
[RENAME_SCHEMA] ALTER SCHEMA pub_test1_renamed RENAME to pub_test1
;
----------
[CREATE_TABLE] create table inttest (a myint)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_test_sub CONNECTION 'port=10248 host=/tmp/jZENFq3HZU user=regress_test_user dbname=postgres password=secret'
;
----------
[ADD_INDEX] CREATE INDEX reloptions_test_idx3 ON reloptions_test (s)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_event_helper CASCADE
;
----------
[DROP_TABLE] drop table old_table1
;
----------
[CREATE_PROG_OBJ] create function outparam_fail(i anyelement, out r anyrange, out t text)
  as $$ select '[1,10]', 'foo' $$ language sql
;
----------
[CREATE_POLICY] create conversion alter1.ascii_to_utf8 for 'sql_ascii' to 'utf8' from ascii_to_utf8
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add unique (b, a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey4_in(cstring)
RETURNS gbtreekey4
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp_tz() < $.datetime("HH:MI"))'))
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "covidxpart4_a_idx" ON "public"."covidxpart4" USING "btree" ("a")
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_forparted ADD TABLE testpub_parted, testpub_parted2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".!=- (
    FUNCTION = "factorial",
    LEFTARG = bigint
)
;
----------
[CREATE_TYPE] -- fail

CREATE DOMAIN priv_testdomain1 AS int
;
----------
[BLOCK] --
-- Verify system catalog foreign key relationships
--
DO $doblock$
declare
  fk record;
  nkeys integer;
  cmd text;
  err record;
begin
  for fk in select * from pg_get_catalog_foreign_keys()
  loop
    raise notice 'checking % % => % %',
      fk.fktable, fk.fkcols, fk.pktable, fk.pkcols;
    nkeys := array_length(fk.fkcols, 1);
    cmd := 'SELECT ctid';
    for i in 1 .. nkeys loop
      cmd := cmd || ', ' || quote_ident(fk.fkcols[i]);
    end loop;
    if fk.is_array then
      cmd := cmd || ' FROM (SELECT ctid';
      for i in 1 .. nkeys-1 loop
        cmd := cmd || ', ' || quote_ident(fk.fkcols[i]);
      end loop;
      cmd := cmd || ', unnest(' || quote_ident(fk.fkcols[nkeys]);
      cmd := cmd || ') as ' || quote_ident(fk.fkcols[nkeys]);
      cmd := cmd || ' FROM ' || fk.fktable::text || ') fk WHERE ';
    else
      cmd := cmd || ' FROM ' || fk.fktable::text || ' fk WHERE ';
    end if;
    if fk.is_opt then
      for i in 1 .. nkeys loop
        cmd := cmd || quote_ident(fk.fkcols[i]) || ' != 0 AND ';
      end loop;
    end if;
    cmd := cmd || 'NOT EXISTS(SELECT 1 FROM ' || fk.pktable::text || ' pk WHERE ';
    for i in 1 .. nkeys loop
      if i > 1 then cmd := cmd || ' AND '; end if;
      cmd := cmd || 'pk.' || quote_ident(fk.pkcols[i]);
      cmd := cmd || ' = fk.' || quote_ident(fk.fkcols[i]);
    end loop;
    cmd := cmd || ')';
    -- raise notice 'cmd = %', cmd;
    for err in execute cmd loop
      raise warning 'FK VIOLATION IN %(%): %', fk.fktable, fk.fkcols, err;
    end loop;
  end loop;
end
$doblock$
;
----------
[ADMIN_TABLE] -- vacuum the table, to discard TOAST data
VACUUM brintest_3
;
----------
[CREATE_TABLE] create table cnn_uq (a int)
;
----------
[CREATE_TABLE] -- cascade update/delete
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, PRIMARY KEY(ptest1, ptest2, ptest3) )
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_16651_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE schema_two.table_two(a int)
;
----------
[CREATE_TABLE] --
-- Tests for predicate handling
--

--
-- Test that restrictions that are always true are ignored, and that are always
-- false are replaced with constant-FALSE
--
-- Currently we only check for NullTest quals and OR clauses that include
-- NullTest quals.  We may extend it in the future.
--
CREATE TABLE pred_tab (a int NOT NULL, b int, c int NOT NULL)
;
----------
[CREATE_TYPE] CREATE TYPE double_int as (a int, b int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER split_event_probe_proc
    ON sql_drop
    EXECUTE PROCEDURE split_trigger_probe.event_logger()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[DROP_TABLE] DROP TABLE people
;
----------
[CREATE_PROG_OBJ] create or replace function test_pg_index_toast_func (a int, b int[])
  returns bool as $$ select true $$ language sql immutable
;
----------
[SECURITY_LABEL] -- OK
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS '...invalid label...'
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_27487_2, pg_temp_3.pg_temp_27487
;
----------
[CREATE_TABLE] CREATE TABLE "public"."xmltest2" (
    "x" "xml",
    "_path" "text"
)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc2 LOCATION '/tmp/5tJCEz0j6a/tempdir/tbl=spc2'
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_foralltables DROP TABLE testpub_tbl2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ALTER_CATALOG] ALTER DATABASE "pl_regression" SET lc_messages TO 'C'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_253_213
FOREIGN KEY (x2,x5,x3) REFERENCES pktable(id2,id1,id3)
;
----------
[CREATE_TABLE] create foreign table remt2 (a int, b text)
  server loopback options (table_name 'loct2')
;
----------
[CREATE_TABLE] CREATE TABLE s4.t2 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
----------
[CREATE_TABLE] create table rewritemetoo3 (a rewritetype)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."domnotnull" DROP COLUMN "........pg.dropped.1........"
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE attmp3 VALIDATE CONSTRAINT b_greater_than_ten
;
