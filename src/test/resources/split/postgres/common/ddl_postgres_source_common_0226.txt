create table part_pa_test_p2 partition of part_pa_test for values from (0) to (maxvalue)
;
CREATE FUNCTION hash_page_stats(IN page bytea,
    OUT live_items int4,
    OUT dead_items int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT hasho_prevblkno int8,
    OUT hasho_nextblkno int8,
    OUT hasho_bucket int8,
	OUT hasho_flag int4,
	OUT hasho_page_id int4)
AS '$libdir/pageinspect', 'hash_page_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
--    Case2 (R = P) && ((B = P) || (B = N))
--    -------------------------------------
--    S    tf1      B    tf2
--    -----------------------
--    N    N        N    N
-- should CREATE
CREATE AGGREGATE myaggp05a(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
-- cleanup
DROP TABLE empsalary
;
CREATE OPERATOR CLASS bpchar_ops
DEFAULT FOR TYPE bpchar USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       bpcharcmp(bpchar, bpchar),
    FUNCTION        2       gin_extract_value_bpchar(bpchar, internal),
    FUNCTION        3       gin_extract_query_bpchar(bpchar, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_bpchar(bpchar,bpchar,int2, internal),
STORAGE         bpchar
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_inherits
;
VACUUM pg_catalog.pg_ts_dict
;
CREATE FUNCTION seg_different(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE ONLY "generated_stored_tests"."gtest1_y" INHERIT "generated_stored_tests"."gtesty"
;
CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
/*
 * text search configuration for italian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * italian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY italian_stem
	(TEMPLATE = snowball, Language = italian , StopWords=italian)
;
-- add a unique constraint (fails)
alter table atacc1 add constraint atacc_test1 unique (test)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_user_mapping
;
--
-- Test rules containing INSERT ... SELECT, which is a very ugly special
-- case as of 7.1.  Example is based on bug report from Joel Burton.
--
create table pparent (pid int, txt text)
;
-- fail

CREATE DOMAIN testdomain_p AS text COLLATE "POSIX"
;
CREATE OPERATOR @ (
        LEFTARG = ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = ltxtq_exec,
	COMMUTATOR = '@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
VACUUM (PARALLEL 2) pg_catalog.pg_collation
;
create table simple as
  select generate_series(1, 20000) AS id, 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'
;
-- Now add some indexes.
CREATE INDEX concur_reindex_partidx ON concur_reindex_part (id)
;
create table ext7_table1 (col1 serial primary key)
;
CREATE FUNCTION dump_test.func_with_support() RETURNS int LANGUAGE sql AS $$ SELECT 1 $$ SUPPORT varchar_support
;
VACUUM (PROCESS_TOAST FALSE) information_schema.sql_features
;
CREATE TABLE utf8_verification_inputs (inbytes bytea, description text PRIMARY KEY)
;
CREATE PROCEDURE test_proc9(IN a int, OUT b int)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %', a, b;
  b := a * 2;
END;
$$
;
CREATE TEMP VIEW fdv1 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY body
;
create table test_multirange_gist(mr int4multirange)
;
CREATE OR REPLACE FUNCTION _evt_table_rewrite_fnct()
RETURNS EVENT_TRIGGER LANGUAGE plpgsql AS
$$
  BEGIN
  END;
$$
;
CREATE FUNCTION regress_pg_dump_schema.test_func () RETURNS int
AS 'SELECT 1;' LANGUAGE SQL
;
create table rlp3 (b varchar, a int) partition by list (b varchar_ops)
;
DROP TABLE test_squash_cast CASCADE
;
CREATE TABLE "public"."spgist_box_tbl" (
    "id" integer NOT NULL,
    "b" "box"
)
;
ALTER INDEX pg_toast.pg_toast_28331_index RENAME TO reind_con_toast_idx
;
-- Truncate triggers aren't allowed on views
CREATE TRIGGER invalid_trig BEFORE TRUNCATE ON main_view
EXECUTE PROCEDURE trigger_func('before_tru_row')
;
CREATE TABLE justthis (
	value	int
  )
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest10" DROP COLUMN "........pg.dropped.2........"
;
CREATE FUNCTION querytree(query_int)
RETURNS text
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_28456 RENAME TO reind_con_toast
;
ALTER SUBSCRIPTION regress_testsub SET (run_as_owner = false)
;
-- no

DROP TABLE testns.acltest1
;
-- ERROR
ALTER TABLE fd_pt1 ALTER COLUMN c8 TYPE char(10)
;
CREATE OPERATOR = (LEFTARG = oid, RIGHTARG = regclass, PROCEDURE = regress_seg_schema.compare)
;
CREATE SEQUENCE CHECK_SEQ
;
CREATE RULE rule2 AS ON UPDATE TO ruletest_t1
    DO INSTEAD INSERT INTO ruletest_t2 VALUES (OLD.*)
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
-- Nor rename, alter type
ALTER TABLE part_2 RENAME COLUMN b to c
;
-- Create the operator class
CREATE OPERATOR CLASS gist_text_ops
DEFAULT FOR TYPE text USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_text_consistent (internal, text, int2, oid, internal),
	FUNCTION	2	gbt_text_union (internal, internal),
	FUNCTION	3	gbt_text_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_text_penalty (internal, internal, internal),
	FUNCTION	6	gbt_text_picksplit (internal, internal),
	FUNCTION	7	gbt_text_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
CREATE SCHEMA split_cursor_next
;
ALTER TABLE ONLY "public"."gtest21b"
    ADD CONSTRAINT "gtest21b_pkey" PRIMARY KEY ("a")
;
create domain spgist_text as varchar
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_authid
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table part_pa_test_p2 partition of part_pa_test for values from (0) to (maxvalue)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hash_page_stats(IN page bytea,
    OUT live_items int4,
    OUT dead_items int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT hasho_prevblkno int8,
    OUT hasho_nextblkno int8,
    OUT hasho_bucket int8,
	OUT hasho_flag int4,
	OUT hasho_page_id int4)
AS '$libdir/pageinspect', 'hash_page_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] --    Case2 (R = P) && ((B = P) || (B = N))
--    -------------------------------------
--    S    tf1      B    tf2
--    -----------------------
--    N    N        N    N
-- should CREATE
CREATE AGGREGATE myaggp05a(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE empsalary
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS bpchar_ops
DEFAULT FOR TYPE bpchar USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       bpcharcmp(bpchar, bpchar),
    FUNCTION        2       gin_extract_value_bpchar(bpchar, internal),
    FUNCTION        3       gin_extract_query_bpchar(bpchar, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_bpchar(bpchar,bpchar,int2, internal),
STORAGE         bpchar
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_inherits
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_ts_dict
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_different(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_stored_tests"."gtest1_y" INHERIT "generated_stored_tests"."gtesty"
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
----------
[CREATE_POLICY] /*
 * text search configuration for italian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * italian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY italian_stem
	(TEMPLATE = snowball, Language = italian , StopWords=italian)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a unique constraint (fails)
alter table atacc1 add constraint atacc_test1 unique (test)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_user_mapping
;
----------
[CREATE_TABLE] --
-- Test rules containing INSERT ... SELECT, which is a very ugly special
-- case as of 7.1.  Example is based on bug report from Joel Burton.
--
create table pparent (pid int, txt text)
;
----------
[CREATE_TYPE] -- fail

CREATE DOMAIN testdomain_p AS text COLLATE "POSIX"
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @ (
        LEFTARG = ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = ltxtq_exec,
	COMMUTATOR = '@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_collation
;
----------
[CREATE_TABLE|SELECT] create table simple as
  select generate_series(1, 20000) AS id, 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'
;
----------
[ADD_INDEX] -- Now add some indexes.
CREATE INDEX concur_reindex_partidx ON concur_reindex_part (id)
;
----------
[CREATE_TABLE] create table ext7_table1 (col1 serial primary key)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dump_test.func_with_support() RETURNS int LANGUAGE sql AS $$ SELECT 1 $$ SUPPORT varchar_support
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) information_schema.sql_features
;
----------
[CREATE_TABLE] CREATE TABLE utf8_verification_inputs (inbytes bytea, description text PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc9(IN a int, OUT b int)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %, b: %', a, b;
  b := a * 2;
END;
$$
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW fdv1 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY body
;
----------
[CREATE_TABLE] create table test_multirange_gist(mr int4multirange)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION _evt_table_rewrite_fnct()
RETURNS EVENT_TRIGGER LANGUAGE plpgsql AS
$$
  BEGIN
  END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regress_pg_dump_schema.test_func () RETURNS int
AS 'SELECT 1;' LANGUAGE SQL
;
----------
[CREATE_TABLE] create table rlp3 (b varchar, a int) partition by list (b varchar_ops)
;
----------
[DROP_TABLE] DROP TABLE test_squash_cast CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."spgist_box_tbl" (
    "id" integer NOT NULL,
    "b" "box"
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28331_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Truncate triggers aren't allowed on views
CREATE TRIGGER invalid_trig BEFORE TRUNCATE ON main_view
EXECUTE PROCEDURE trigger_func('before_tru_row')
;
----------
[CREATE_TABLE] CREATE TABLE justthis (
	value	int
  )
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_virtual_tests"."gtest10" DROP COLUMN "........pg.dropped.2........"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION querytree(query_int)
RETURNS text
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28456 RENAME TO reind_con_toast
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (run_as_owner = false)
;
----------
[DROP_TABLE] -- no

DROP TABLE testns.acltest1
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ERROR
ALTER TABLE fd_pt1 ALTER COLUMN c8 TYPE char(10)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (LEFTARG = oid, RIGHTARG = regclass, PROCEDURE = regress_seg_schema.compare)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE CHECK_SEQ
;
----------
[CREATE_POLICY] CREATE RULE rule2 AS ON UPDATE TO ruletest_t1
    DO INSTEAD INSERT INTO ruletest_t2 VALUES (OLD.*)
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
----------
[RENAME_COLUMN] -- Nor rename, alter type
ALTER TABLE part_2 RENAME COLUMN b to c
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_text_ops
DEFAULT FOR TYPE text USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_text_consistent (internal, text, int2, oid, internal),
	FUNCTION	2	gbt_text_union (internal, internal),
	FUNCTION	3	gbt_text_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_text_penalty (internal, internal, internal),
	FUNCTION	6	gbt_text_picksplit (internal, internal),
	FUNCTION	7	gbt_text_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_cursor_next
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest21b"
    ADD CONSTRAINT "gtest21b_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TYPE] create domain spgist_text as varchar
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_authid
;
