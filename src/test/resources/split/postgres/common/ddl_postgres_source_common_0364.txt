create rule qqq as on update to copydml_test do instead (delete from copydml_test; delete from copydml_test)
;
DROP TYPE double_int
;
create table part_ee_ff1 partition of part_ee_ff for values from (1) to (10)
;
ALTER FUNCTION gbt_time_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
CLUSTER clstr_expression USING clstr_expression_upper_b
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=17469 host=/tmp/uPEqeV_Qpf dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=32593 host=/tmp/leS0vXQqx0 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
create table hp_prefix_test_p1 partition of hp_prefix_test for values with (modulus 8, remainder 1)
;
VACUUM (PARALLEL 0) pg_catalog.pg_seclabel
;
-- Both drops should fail, but with different error messages:
DROP SEQUENCE t1_f1_seq
;
ALTER TABLE fk_partitioned_fk_5 DETACH PARTITION fk_partitioned_fk_5_1
;
-- Verify that firing state propagates correctly on creation, too
CREATE TABLE trgfire (i int) PARTITION BY RANGE (i)
;
CREATE SEQUENCE split_cov.seq_test AS bigint START WITH 10 INCREMENT BY 5
;
DROP TEXT SEARCH DICTIONARY IF EXISTS split_ts_next.simple_dict_renamed CASCADE
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_enum
;
create type xfloat4 (input = xfloat4in, output = xfloat4out, like = float4)
;
CREATE TABLE sch1.tab1 AS SELECT generate_series(1,10) AS a
;
DROP VIEW lock_view7
;
-- test pretty-print parenthesization rules, and SubLink deparsing

create view tt26v as
select x + y + z as c1,
       (x * y) + z as c2,
       x + (y * z) as c3,
       (x + y) * z as c4,
       x * (y + z) as c5,
       x + (y + z) as c6,
       x + (y # z) as c7,
       (x > y) AND (y > z OR x > z) as c8,
       (x > y) OR (y > z AND NOT (x > z)) as c9,
       (x,y) <> ALL (values(1,2),(3,4)) as c10,
       (x,y) <= ANY (values(1,2),(3,4)) as c11
from (values(1,2,3)) v(x,y,z)
;
ALTER TABLE fd_pt1 ADD CONSTRAINT fd_pt1chk3 CHECK (c2 <> '') NOT VALID
;
ANALYZE public.prt2_e_p2
;
-- ERROR; cast not pushed down
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE user_enum
;
CREATE AGGREGATE priv_testagg1b(priv_testdomain1) (sfunc = int4_sum, stype = bigint)
;
ALTER EXTENSION test_ext_req_schema1 SET SCHEMA test_s_dep2
;
DROP TABLE concur_appclass_tab
;
ALTER INDEX "public"."covidxpart_a_b_idx" ATTACH PARTITION "public"."covidxpart2_a_b_idx"
;
TRUNCATE bttest_a
;
alter table wcowrtest attach partition wcowrtest2 for values in (2)
;
CREATE OPERATOR CLASS gist_cash_ops
DEFAULT FOR TYPE money USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_cash_consistent (internal, money, int2, oid, internal),
	FUNCTION	2	gbt_cash_union (internal, internal),
	FUNCTION	3	gbt_cash_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_cash_penalty (internal, internal, internal),
	FUNCTION	6	gbt_cash_picksplit (internal, internal),
	FUNCTION	7	gbt_cash_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
ALTER TABLE people REPLICA IDENTITY FULL
;
create view pg_temp.twophase_view as select 1
;
CREATE TABLE "public"."prt1_l" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("a")
;
ALTER TABLE pa_target ATTACH PARTITION part3 FOR VALUES IN (3,8,9)
;
CREATE TABLE bittmp (a bit(33))
;
ALTER INDEX "fkpart3"."pk5_pkey" ATTACH PARTITION "fkpart3"."pk51_pkey"
;
create index idxparti2 on idxpart (b, c)
;
CREATE TABLE foo(i int)
;
ALTER FUNCTION gbt_bit_consistent(internal, bit, smallint, oid, internal) PARALLEL SAFE
;
CREATE FUNCTION test_argresult_uint2(x uint2, y int) RETURNS uint2 AS $$
begin
return y;
end
$$ LANGUAGE plpgsql
;
CREATE OR REPLACE FUNCTION test_slru_page_exists(int) RETURNS bool
  AS 'MODULE_PATHNAME', 'test_slru_page_exists' LANGUAGE C
;
-- ************************************************************
-- * BEFORE UPDATE on HSlot
-- *	- do delete/insert instead of update if name changes
-- ************************************************************
create function tg_hslot_bu() returns trigger as '
begin
    if new.slotname != old.slotname or new.hubname != old.hubname then
        delete from HSlot where slotname = old.slotname;
	insert into HSlot (
		    slotname,
		    hubname,
		    slotno,
		    slotlink
		) values (
		    new.slotname,
		    new.hubname,
		    new.slotno,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
CREATE INDEX "fdeps_ab_idx" ON "public"."functional_dependencies" USING "btree" ("a", "b")
;
-- fail

CREATE TRANSFORM FOR hstore LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
DROP PUBLICATION testpubpart_forschema
;
CREATE INDEX "quad_box_tbl_idx" ON "public"."quad_box_tbl" USING "spgist" ("b")
;
-- renaming serial sequences
ALTER TABLE serialtest1_f2_seq RENAME TO serialtest1_f2_foo
;
/*
 * SQL_PACKAGES table
 * removed in SQL:2011
 */

CREATE TABLE sql_packages (
    feature_id      character_data,
    feature_name    character_data,
    is_supported    yes_or_no,
    is_verified_by  character_data,
    comments        character_data
)
;
-- views
CREATE VIEW gtest1v AS SELECT * FROM gtest1
;
CREATE OR REPLACE FUNCTION test_slru_page_exists(int) RETURNS bool
  AS '$libdir/test_slru', 'test_slru_page_exists' LANGUAGE C
;
-- Test ReScan code path that recreates the cursor even when no parameters
-- change (bug #17889)
CREATE TABLE loct1 (c1 int)
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] create rule qqq as on update to copydml_test do instead (delete from copydml_test; delete from copydml_test)
;
----------
[DROP_TYPE] DROP TYPE double_int
;
----------
[CREATE_TABLE] create table part_ee_ff1 partition of part_ee_ff for values from (1) to (10)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_time_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
----------
[ADMIN_TABLE] CLUSTER clstr_expression USING clstr_expression_upper_b
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=17469 host=/tmp/uPEqeV_Qpf dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=32593 host=/tmp/leS0vXQqx0 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_TABLE] create table hp_prefix_test_p1 partition of hp_prefix_test for values with (modulus 8, remainder 1)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_seclabel
;
----------
[DROP_SEQUENCE] -- Both drops should fail, but with different error messages:
DROP SEQUENCE t1_f1_seq
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk_5 DETACH PARTITION fk_partitioned_fk_5_1
;
----------
[CREATE_TABLE] -- Verify that firing state propagates correctly on creation, too
CREATE TABLE trgfire (i int) PARTITION BY RANGE (i)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE split_cov.seq_test AS bigint START WITH 10 INCREMENT BY 5
;
----------
[DROP_POLICY] DROP TEXT SEARCH DICTIONARY IF EXISTS split_ts_next.simple_dict_renamed CASCADE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_enum
;
----------
[CREATE_TYPE] create type xfloat4 (input = xfloat4in, output = xfloat4out, like = float4)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE sch1.tab1 AS SELECT generate_series(1,10) AS a
;
----------
[DROP_VIEW] DROP VIEW lock_view7
;
----------
[CREATE_VIEW(SELECT)] -- test pretty-print parenthesization rules, and SubLink deparsing

create view tt26v as
select x + y + z as c1,
       (x * y) + z as c2,
       x + (y * z) as c3,
       (x + y) * z as c4,
       x * (y + z) as c5,
       x + (y + z) as c6,
       x + (y # z) as c7,
       (x > y) AND (y > z OR x > z) as c8,
       (x > y) OR (y > z AND NOT (x > z)) as c9,
       (x,y) <> ALL (values(1,2),(3,4)) as c10,
       (x,y) <= ANY (values(1,2),(3,4)) as c11
from (values(1,2,3)) v(x,y,z)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fd_pt1 ADD CONSTRAINT fd_pt1chk3 CHECK (c2 <> '') NOT VALID
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_e_p2
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ERROR; cast not pushed down
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE user_enum
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE priv_testagg1b(priv_testdomain1) (sfunc = int4_sum, stype = bigint)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_ext_req_schema1 SET SCHEMA test_s_dep2
;
----------
[DROP_TABLE] DROP TABLE concur_appclass_tab
;
----------
[ALTER_INDEX] ALTER INDEX "public"."covidxpart_a_b_idx" ATTACH PARTITION "public"."covidxpart2_a_b_idx"
;
----------
[TRUNCATE_TABLE] TRUNCATE bttest_a
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table wcowrtest attach partition wcowrtest2 for values in (2)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_cash_ops
DEFAULT FOR TYPE money USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_cash_consistent (internal, money, int2, oid, internal),
	FUNCTION	2	gbt_cash_union (internal, internal),
	FUNCTION	3	gbt_cash_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_cash_penalty (internal, internal, internal),
	FUNCTION	6	gbt_cash_picksplit (internal, internal),
	FUNCTION	7	gbt_cash_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[ALTER_TABLE] ALTER TABLE people REPLICA IDENTITY FULL
;
----------
[CREATE_VIEW(SELECT)] create view pg_temp.twophase_view as select 1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_l" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("a")
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pa_target ATTACH PARTITION part3 FOR VALUES IN (3,8,9)
;
----------
[CREATE_TABLE] CREATE TABLE bittmp (a bit(33))
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart3"."pk5_pkey" ATTACH PARTITION "fkpart3"."pk51_pkey"
;
----------
[ADD_INDEX] create index idxparti2 on idxpart (b, c)
;
----------
[CREATE_TABLE] CREATE TABLE foo(i int)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bit_consistent(internal, bit, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_argresult_uint2(x uint2, y int) RETURNS uint2 AS $$
begin
return y;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_exists(int) RETURNS bool
  AS 'MODULE_PATHNAME', 'test_slru_page_exists' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * BEFORE UPDATE on HSlot
-- *	- do delete/insert instead of update if name changes
-- ************************************************************
create function tg_hslot_bu() returns trigger as '
begin
    if new.slotname != old.slotname or new.hubname != old.hubname then
        delete from HSlot where slotname = old.slotname;
	insert into HSlot (
		    slotname,
		    hubname,
		    slotno,
		    slotlink
		) values (
		    new.slotname,
		    new.hubname,
		    new.slotno,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
----------
[ADD_INDEX] CREATE INDEX "fdeps_ab_idx" ON "public"."functional_dependencies" USING "btree" ("a", "b")
;
----------
[UNKNOWN] -- fail

CREATE TRANSFORM FOR hstore LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpubpart_forschema
;
----------
[ADD_INDEX] CREATE INDEX "quad_box_tbl_idx" ON "public"."quad_box_tbl" USING "spgist" ("b")
;
----------
[RENAME_TABLE] -- renaming serial sequences
ALTER TABLE serialtest1_f2_seq RENAME TO serialtest1_f2_foo
;
----------
[CREATE_TABLE] /*
 * SQL_PACKAGES table
 * removed in SQL:2011
 */

CREATE TABLE sql_packages (
    feature_id      character_data,
    feature_name    character_data,
    is_supported    yes_or_no,
    is_verified_by  character_data,
    comments        character_data
)
;
----------
[CREATE_VIEW(SELECT)] -- views
CREATE VIEW gtest1v AS SELECT * FROM gtest1
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_exists(int) RETURNS bool
  AS '$libdir/test_slru', 'test_slru_page_exists' LANGUAGE C
;
----------
[CREATE_TABLE] -- Test ReScan code path that recreates the cursor even when no parameters
-- change (bug #17889)
CREATE TABLE loct1 (c1 int)
;
