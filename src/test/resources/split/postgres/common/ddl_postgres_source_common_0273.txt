ALTER TABLE pg_temp_24.pg_temp_27148_2 ADD COLUMN newdata pg_temp_24.pg_temp_27148
;
CREATE FUNCTION hobbies_by_name(hobbies_r.name%TYPE)
   RETURNS hobbies_r.person%TYPE
   AS 'select person from hobbies_r where name = $1'
   LANGUAGE SQL
;
ALTER TABLE ONLY public.ths_p2
    ADD CONSTRAINT ths_p2_mod_data_key UNIQUE (mod, data)
;
CREATE FUNCTION pg_stat_statements_reset(IN userid Oid DEFAULT 0,
	IN dbid Oid DEFAULT 0,
	IN queryid bigint DEFAULT 0
)
RETURNS void
AS '$libdir/pg_stat_statements', 'pg_stat_statements_reset_1_7'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER TABLE fd_pt1 ALTER COLUMN c8 TYPE char(10)
;
CREATE TABLE "public"."atnnparted" (
    "id" integer,
    "col1" integer
)
PARTITION BY LIST ("id")
;
create table source ( f1 integer , f2 integer , f3 varchar ( 20 ) )
;
--
-- Progress reporting for COPY
--
create table tab_progress_reporting (
	name text,
	age int4,
	location point,
	salary int4,
	manager name
)
;
VACUUM (INDEX_CLEANUP FALSE) vactst
;
truncate copy_default
;
-- not supported

ALTER TABLE regtest_table SET (fillfactor = 75)
;
do $$
begin
  for i in reverse 1..3 by -1 loop
    raise notice 'reverse 1..3 by -1: i = %', i;
  end loop;
end$$
;
ANALYZE pg_temp_3.pg_temp_29533
;
VACUUM (PARALLEL 0) pg_catalog.pg_partitioned_table
;
ANALYZE pg_temp_4.pg_temp_29539_2
;
-- fail

create temp table rngfunc(f1 int8, f2 int8)
;
CREATE TABLE test_tblx (x int, y test_typex check ((y).a > 0))
;
CREATE FUNCTION tf2p(int[],anyelement) RETURNS int[] AS
'select $1' LANGUAGE SQL
;
-- test passing column_name, constraint_name, datatype_name, table_name
-- and schema_name error fields

create or replace function stacked_diagnostics_test() returns void as $$
declare _column_name text;
        _constraint_name text;
        _datatype_name text;
        _table_name text;
        _schema_name text;
begin
  raise exception using
    column = '>>some column name<<',
    constraint = '>>some constraint name<<',
    datatype = '>>some datatype name<<',
    table = '>>some table name<<',
    schema = '>>some schema name<<';
exception when others then
  get stacked diagnostics
        _column_name = column_name,
        _constraint_name = constraint_name,
        _datatype_name = pg_datatype_name,
        _table_name = table_name,
        _schema_name = schema_name;
  raise notice 'column %, constraint %, type %, table %, schema %',
    _column_name, _constraint_name, _datatype_name, _table_name, _schema_name;
end;
$$ language plpgsql
;
DROP OPERATOR =#>(integer, integer)
;
create table fktable (ftest1 int, ftest2 int, foreign key(ftest1, ftest2) references pktable(base1, ptest1))
;
VACUUM (SKIP_DATABASE_STATS, FULL) information_schema.sql_parts
;
CREATE VIEW pg_stat_sys_indexes AS
    SELECT * FROM pg_stat_all_indexes
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
CREATE TABLE loct (aa TEXT, bb TEXT)
;
CREATE TYPE test_pg_dump_e1 AS ENUM ('abc', 'def')
;
drop table trg_clone
;
create table idxpart (a int, b int, c text) partition by range (a)
;
CREATE VIEW rw_view1 AS
  SELECT b AS bb, a AS aa, 'Const1' AS c FROM base_tbl WHERE a>0
;
CREATE COLLATION coll_dup_chk (LC_COLLATE = "POSIX", LOCALE = '')
;
/*
 * 6.19
 * COLUMN_DOMAIN_USAGE view
 */

CREATE VIEW column_domain_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(nt.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_type t, pg_namespace nt, pg_class c, pg_namespace nc,
         pg_attribute a

    WHERE t.typnamespace = nt.oid
          AND c.relnamespace = nc.oid
          AND a.attrelid = c.oid
          AND a.atttypid = t.oid
          AND t.typtype = 'd'
          AND c.relkind IN ('r', 'v', 'f', 'p')
          AND a.attnum > 0
          AND NOT a.attisdropped
          AND pg_has_role(t.typowner, 'USAGE')
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
create table mlparted_tab_part2 partition of mlparted_tab for values in (2) partition by list (b)
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26754_2 (tid pg_catalog.tid)
;
create table idxpart (a int primary key, b int) partition by range ((b + a))
;
CREATE FUNCTION isnne(ismn, ismn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- fail
drop table idxpart
;
DROP PUBLICATION mypub
;
--
-- The following tests are unrelated to the scenario outlined above;
-- they merely exercise specific parts of PL/pgSQL
--

--
-- Test recursion, per bug report 7-Sep-01
--
CREATE FUNCTION recursion_test(int,int) RETURNS text AS '
DECLARE rslt text;
BEGIN
    IF $1 <= 0 THEN
        rslt = CAST($2 AS TEXT);
    ELSE
        rslt = CAST($1 AS TEXT) || '','' || recursion_test($1 - 1, $2);
    END IF;
    RETURN rslt;
END;' LANGUAGE plpgsql
;
truncate copytest2
;
DROP FUNCTION rngfunct(int)
;
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('create table') and tag in ('CREATE FUNCTION')
   execute procedure test_event_trigger()
;
CREATE FUNCTION regexp_match( citext, citext, text ) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
-- New functions and views for pg_stat_statements in 1.12
AlTER EXTENSION pg_stat_statements UPDATE TO '1.12'
;
CREATE TABLE parent_tbl (a text, b int)
;
ALTER TABLE replication_metadata SET (user_catalog_table = true)
;
--
-- Clean up
--
DROP VIEW lock_view7
;
create table inh_perm_parent (a1 int)
;
DROP TABLE brin_hot_2
;
ALTER TABLE pg_temp_46.pg_temp_28044_2 ADD COLUMN newdata pg_temp_46.pg_temp_28044
;
CREATE INDEX inet_idx3 ON inet_tbl using spgist (i)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_24.pg_temp_27148_2 ADD COLUMN newdata pg_temp_24.pg_temp_27148
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hobbies_by_name(hobbies_r.name%TYPE)
   RETURNS hobbies_r.person%TYPE
   AS 'select person from hobbies_r where name = $1'
   LANGUAGE SQL
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY public.ths_p2
    ADD CONSTRAINT ths_p2_mod_data_key UNIQUE (mod, data)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_stat_statements_reset(IN userid Oid DEFAULT 0,
	IN dbid Oid DEFAULT 0,
	IN queryid bigint DEFAULT 0
)
RETURNS void
AS '$libdir/pg_stat_statements', 'pg_stat_statements_reset_1_7'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c8 TYPE char(10)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."atnnparted" (
    "id" integer,
    "col1" integer
)
PARTITION BY LIST ("id")
;
----------
[CREATE_TABLE] create table source ( f1 integer , f2 integer , f3 varchar ( 20 ) )
;
----------
[CREATE_TABLE] --
-- Progress reporting for COPY
--
create table tab_progress_reporting (
	name text,
	age int4,
	location point,
	salary int4,
	manager name
)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) vactst
;
----------
[TRUNCATE_TABLE] truncate copy_default
;
----------
[ALTER_TABLE] -- not supported

ALTER TABLE regtest_table SET (fillfactor = 75)
;
----------
[BLOCK] do $$
begin
  for i in reverse 1..3 by -1 loop
    raise notice 'reverse 1..3 by -1: i = %', i;
  end loop;
end$$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_29533
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_partitioned_table
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_29539_2
;
----------
[CREATE_TABLE] -- fail

create temp table rngfunc(f1 int8, f2 int8)
;
----------
[CREATE_TABLE] CREATE TABLE test_tblx (x int, y test_typex check ((y).a > 0))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tf2p(int[],anyelement) RETURNS int[] AS
'select $1' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] -- test passing column_name, constraint_name, datatype_name, table_name
-- and schema_name error fields

create or replace function stacked_diagnostics_test() returns void as $$
declare _column_name text;
        _constraint_name text;
        _datatype_name text;
        _table_name text;
        _schema_name text;
begin
  raise exception using
    column = '>>some column name<<',
    constraint = '>>some constraint name<<',
    datatype = '>>some datatype name<<',
    table = '>>some table name<<',
    schema = '>>some schema name<<';
exception when others then
  get stacked diagnostics
        _column_name = column_name,
        _constraint_name = constraint_name,
        _datatype_name = pg_datatype_name,
        _table_name = table_name,
        _schema_name = schema_name;
  raise notice 'column %, constraint %, type %, table %, schema %',
    _column_name, _constraint_name, _datatype_name, _table_name, _schema_name;
end;
$$ language plpgsql
;
----------
[DROP_PROG_OBJ] DROP OPERATOR =#>(integer, integer)
;
----------
[CREATE_TABLE] create table fktable (ftest1 int, ftest2 int, foreign key(ftest1, ftest2) references pktable(base1, ptest1))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) information_schema.sql_parts
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_sys_indexes AS
    SELECT * FROM pg_stat_all_indexes
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
----------
[CREATE_TABLE] CREATE TABLE loct (aa TEXT, bb TEXT)
;
----------
[CREATE_TYPE] CREATE TYPE test_pg_dump_e1 AS ENUM ('abc', 'def')
;
----------
[DROP_TABLE] drop table trg_clone
;
----------
[CREATE_TABLE] create table idxpart (a int, b int, c text) partition by range (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS
  SELECT b AS bb, a AS aa, 'Const1' AS c FROM base_tbl WHERE a>0
;
----------
[CREATE_POLICY] CREATE COLLATION coll_dup_chk (LC_COLLATE = "POSIX", LOCALE = '')
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.19
 * COLUMN_DOMAIN_USAGE view
 */

CREATE VIEW column_domain_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(nt.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_type t, pg_namespace nt, pg_class c, pg_namespace nc,
         pg_attribute a

    WHERE t.typnamespace = nt.oid
          AND c.relnamespace = nc.oid
          AND a.attrelid = c.oid
          AND a.atttypid = t.oid
          AND t.typtype = 'd'
          AND c.relkind IN ('r', 'v', 'f', 'p')
          AND a.attnum > 0
          AND NOT a.attisdropped
          AND pg_has_role(t.typowner, 'USAGE')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] create table mlparted_tab_part2 partition of mlparted_tab for values in (2) partition by list (b)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26754_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table idxpart (a int primary key, b int) partition by range ((b + a))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ismn, ismn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] -- fail
drop table idxpart
;
----------
[DROP_PUB_SUB] DROP PUBLICATION mypub
;
----------
[CREATE_PROG_OBJ] --
-- The following tests are unrelated to the scenario outlined above;
-- they merely exercise specific parts of PL/pgSQL
--

--
-- Test recursion, per bug report 7-Sep-01
--
CREATE FUNCTION recursion_test(int,int) RETURNS text AS '
DECLARE rslt text;
BEGIN
    IF $1 <= 0 THEN
        rslt = CAST($2 AS TEXT);
    ELSE
        rslt = CAST($1 AS TEXT) || '','' || recursion_test($1 - 1, $2);
    END IF;
    RETURN rslt;
END;' LANGUAGE plpgsql
;
----------
[TRUNCATE_TABLE] truncate copytest2
;
----------
[DROP_PROG_OBJ] DROP FUNCTION rngfunct(int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('create table') and tag in ('CREATE FUNCTION')
   execute procedure test_event_trigger()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regexp_match( citext, citext, text ) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_LIBRARY] -- New functions and views for pg_stat_statements in 1.12
AlTER EXTENSION pg_stat_statements UPDATE TO '1.12'
;
----------
[CREATE_TABLE] CREATE TABLE parent_tbl (a text, b int)
;
----------
[ALTER_TABLE] ALTER TABLE replication_metadata SET (user_catalog_table = true)
;
----------
[DROP_VIEW] --
-- Clean up
--
DROP VIEW lock_view7
;
----------
[CREATE_TABLE] create table inh_perm_parent (a1 int)
;
----------
[DROP_TABLE] DROP TABLE brin_hot_2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_46.pg_temp_28044_2 ADD COLUMN newdata pg_temp_46.pg_temp_28044
;
----------
[ADD_INDEX] CREATE INDEX inet_idx3 ON inet_tbl using spgist (i)
;
