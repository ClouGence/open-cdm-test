create index nummultirange_test2_hash_idx on nummultirange_test2 using hash (nmr)
;
CREATE TABLE "public"."fk_notpartitioned_pk" (
    "a" integer NOT NULL,
    "b" integer NOT NULL
)
;
DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8b(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
  CALL test_proc8b(_a, c => _c, b => _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
-- New function pg_stat_statement_info, and new function
-- and view for pg_stat_statements introduced in 1.9
AlTER EXTENSION pg_stat_statements UPDATE TO '1.9'
;
alter table parted1_irreg drop column fd
;
do $$ declare x public.foo.bar%type; begin end $$
;
DROP POLICY policy_range_parted ON range_parted
;
-- Test ALTER DOMAIN .. NOT NULL
create domain dnotnulltest integer
;
ANALYZE public.pt_part2
;
-- Test header matching feature
create table header_copytest (
	a int,
	b int,
	c text
)
;
ALTER OPERATOR ? (_lquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
create type textrange1 as range(subtype=text, collation="C")
;
alter table users alter column seq type numeric
;
CREATE FUNCTION "public"."trans_updatetrigfunc"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  begin
    raise notice 'trigger = %, old table = %, new table = %',
                 TG_NAME,
                 (select string_agg(old_table::text, ', ' ORDER BY a) FROM old_table),
                 (select string_agg(new_table::text, ', ' ORDER BY a) FROM new_table);
    return null;
  end;
$$
;
CREATE FUNCTION "public"."outparam_succeed"("i" "anyrange", OUT "r" "anyrange", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select $1, 'foo'::text $_$
;
drop function f1(int)
;
--trigger
CREATE TRIGGER tsvectorupdate
BEFORE UPDATE OR INSERT ON test_tsvector
FOR EACH ROW EXECUTE PROCEDURE tsvector_update_trigger(a, 'pg_catalog.english', t)
;
CREATE TABLE test_hash_part (a int, b int) PARTITION BY RANGE (a)
;
--
-- Apply a subset of the above tests on a collatable type, too
--

CREATE TABLE textrange_test (tr textrange)
;
ANALYZE pg_temp_9.pg_temp_37969
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN ((select 1))
;
CREATE FUNCTION pgp_sym_encrypt_bytea(bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_encrypt_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
create table child3 (c text) inherits (parent)
;
-- test TRUNCATE ... RESTART IDENTITY
CREATE SEQUENCE truncate_a_id1 START WITH 33
;
-- error about partitioned index

-- a regular table which is a member of a partition set should work though
create table test_part1 partition of test_partitioned for values from ( 1 ) to (100)
;
---
--- CREATE_EXTENSION
---

CREATE EXTENSION pg_stat_statements
;
VACUUM (FULL) pg_catalog.pg_statistic_ext_data
;
CREATE TABLE pitest1_p1 PARTITION OF pitest1 FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
CREATE TRANSFORM FOR int LANGUAGE SQL (
    FROM SQL WITH FUNCTION prsd_lextype(internal),
    TO SQL WITH FUNCTION int4recv(internal))
;
-- Try swapping to another format to ensure the GUCs are tracked
-- properly through a change.
CREATE TEMPORARY TABLE result (t timestamptz)
;
CREATE UNLOGGED TABLE unlogged3(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES unlogged3)
;
-- 9.1 bug with useless COLLATE in an expression subject to length coercion

CREATE TEMP TABLE vctable (f1 varchar(25))
;
CREATE TRIGGER "tg_pline_bu" BEFORE UPDATE ON "public"."pline" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pline_bu"()
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_inherits
;
ALTER OPERATOR FAMILY alt_opf18 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
DROP FUNCTION int4_casttesttype(int4) CASCADE
;
CREATE FUNCTION "public"."xtrig"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  declare
    r record;
  begin
    for r in select * from old loop
      raise info 'a=%, b=%', r.a, r.b;
    end loop;
    return NULL;
  end;
$$
;
ALTER TABLE part_7_a_null DROP c, DROP d, DROP e
;
-- fail because c2 already exists
ALTER TABLE ONLY test_add_column
	ADD COLUMN c2 integer
;
DROP SCHEMA reindex_test
;
CREATE AGGREGATE "public"."array_larger_accum"("anyarray") (
    SFUNC = "array_larger",
    STYPE = "anyarray",
    INITCOND = '{}'
)
;
CREATE TABLE testschema.part_2 PARTITION OF testschema.part FOR VALUES IN (2)
;
-- No such tablespace
CREATE TABLE bar (i int) TABLESPACE regress_nosuchspace
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_am
;
--
-- test cycle detection
--
create temp table graph( f int, t int, label text )
;
ALTER EXTENSION hstore ADD TYPE split_extension_next.ext_mood
;
analyze j1
;
-- fail, min has to be adjusted
ALTER SEQUENCE sequence_test10 AS smallint MINVALUE -20000
;
VACUUM ANALYZE vactst (does_not_exist), vacparted (b)
;
-- int8 check

CREATE TABLE int8tmp (a int8)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] create index nummultirange_test2_hash_idx on nummultirange_test2 using hash (nmr)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_notpartitioned_pk" (
    "a" integer NOT NULL,
    "b" integer NOT NULL
)
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8b(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
  CALL test_proc8b(_a, c => _c, b => _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
----------
[ALTER_LIBRARY] -- New function pg_stat_statement_info, and new function
-- and view for pg_stat_statements introduced in 1.9
AlTER EXTENSION pg_stat_statements UPDATE TO '1.9'
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table parted1_irreg drop column fd
;
----------
[BLOCK] do $$ declare x public.foo.bar%type; begin end $$
;
----------
[DROP_POLICY] DROP POLICY policy_range_parted ON range_parted
;
----------
[CREATE_TYPE] -- Test ALTER DOMAIN .. NOT NULL
create domain dnotnulltest integer
;
----------
[ADMIN_TABLE] ANALYZE public.pt_part2
;
----------
[CREATE_TABLE] -- Test header matching feature
create table header_copytest (
	a int,
	b int,
	c text
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ? (_lquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_POLICY] create type textrange1 as range(subtype=text, collation="C")
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table users alter column seq type numeric
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."trans_updatetrigfunc"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  begin
    raise notice 'trigger = %, old table = %, new table = %',
                 TG_NAME,
                 (select string_agg(old_table::text, ', ' ORDER BY a) FROM old_table),
                 (select string_agg(new_table::text, ', ' ORDER BY a) FROM new_table);
    return null;
  end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."outparam_succeed"("i" "anyrange", OUT "r" "anyrange", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select $1, 'foo'::text $_$
;
----------
[DROP_PROG_OBJ] drop function f1(int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --trigger
CREATE TRIGGER tsvectorupdate
BEFORE UPDATE OR INSERT ON test_tsvector
FOR EACH ROW EXECUTE PROCEDURE tsvector_update_trigger(a, 'pg_catalog.english', t)
;
----------
[CREATE_TABLE] CREATE TABLE test_hash_part (a int, b int) PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE] --
-- Apply a subset of the above tests on a collatable type, too
--

CREATE TABLE textrange_test (tr textrange)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_37969
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN ((select 1))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgp_sym_encrypt_bytea(bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_encrypt_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table child3 (c text) inherits (parent)
;
----------
[CREATE_SEQUENCE] -- test TRUNCATE ... RESTART IDENTITY
CREATE SEQUENCE truncate_a_id1 START WITH 33
;
----------
[CREATE_TABLE] -- error about partitioned index

-- a regular table which is a member of a partition set should work though
create table test_part1 partition of test_partitioned for values from ( 1 ) to (100)
;
----------
[CREATE_LIBRARY] ---
--- CREATE_EXTENSION
---

CREATE EXTENSION pg_stat_statements
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_TABLE] CREATE TABLE pitest1_p1 PARTITION OF pitest1 FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[UNKNOWN] CREATE TRANSFORM FOR int LANGUAGE SQL (
    FROM SQL WITH FUNCTION prsd_lextype(internal),
    TO SQL WITH FUNCTION int4recv(internal))
;
----------
[CREATE_TABLE] -- Try swapping to another format to ensure the GUCs are tracked
-- properly through a change.
CREATE TEMPORARY TABLE result (t timestamptz)
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE unlogged3(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES unlogged3)
;
----------
[CREATE_TABLE] -- 9.1 bug with useless COLLATE in an expression subject to length coercion

CREATE TEMP TABLE vctable (f1 varchar(25))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_pline_bu" BEFORE UPDATE ON "public"."pline" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pline_bu"()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_inherits
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf18 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION int4_casttesttype(int4) CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."xtrig"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  declare
    r record;
  begin
    for r in select * from old loop
      raise info 'a=%, b=%', r.a, r.b;
    end loop;
    return NULL;
  end;
$$
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE part_7_a_null DROP c, DROP d, DROP e
;
----------
[ALTER_TABLE|ADD_COLUMN] -- fail because c2 already exists
ALTER TABLE ONLY test_add_column
	ADD COLUMN c2 integer
;
----------
[DROP_SCHEMA] DROP SCHEMA reindex_test
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."array_larger_accum"("anyarray") (
    SFUNC = "array_larger",
    STYPE = "anyarray",
    INITCOND = '{}'
)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part_2 PARTITION OF testschema.part FOR VALUES IN (2)
;
----------
[CREATE_TABLE] -- No such tablespace
CREATE TABLE bar (i int) TABLESPACE regress_nosuchspace
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_am
;
----------
[CREATE_TABLE] --
-- test cycle detection
--
create temp table graph( f int, t int, label text )
;
----------
[ALTER_LIBRARY] ALTER EXTENSION hstore ADD TYPE split_extension_next.ext_mood
;
----------
[ADMIN_TABLE] analyze j1
;
----------
[ALTER_SEQUENCE] -- fail, min has to be adjusted
ALTER SEQUENCE sequence_test10 AS smallint MINVALUE -20000
;
----------
[ADMIN_TABLE] VACUUM ANALYZE vactst (does_not_exist), vacparted (b)
;
----------
[CREATE_TABLE] -- int8 check

CREATE TABLE int8tmp (a int8)
;
