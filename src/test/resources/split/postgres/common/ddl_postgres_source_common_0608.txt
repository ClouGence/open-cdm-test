COMMENT ON TABLESPACE split_ts_space IS NULL
;
ALTER DATABASE contrib_regression RESET session_authorization
;
CREATE TABLE t1 (a int, b text, c int)
;
drop function f1(x anyarray)
;
CREATE TABLE ref_c2 PARTITION OF ref_c FOR VALUES FROM (200) TO (300)
;
CREATE FUNCTION snoop(anyelement)
RETURNS boolean AS
$$
BEGIN
  RAISE NOTICE 'snooped value: %', $1;
  RETURN true;
END;
$$
LANGUAGE plpgsql COST 0.000001
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_rewrite
;
CREATE TABLE d_lp_fk (a int PRIMARY KEY) PARTITION BY LIST(a)
;
ANALYZE pg_temp_4.pg_temp_37940_2
;
ALTER TABLESPACE regress_tblspace SET (random_page_cost = 1.0, seq_page_cost = 1.1)
;
CREATE FUNCTION isngt(isbn, ean13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
SECURITY LABEL ON COLUMN t5.f IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
/*
 * Test double inheritance
 *
 * Ensure that defaults are NOT included unless
 * INCLUDING DEFAULTS is specified
 */
CREATE TABLE ctla (aa TEXT)
;
-- old-style spelling of same (except without parallel-safe; that's too new)
CREATE AGGREGATE oldcnt (
   sfunc = int8inc, basetype = 'ANY', stype = int8,
   initcond = '0'
)
;
-- Test cross-partition updates and attribute mapping
CREATE TABLE foo_parted (a int, b float8, c text) PARTITION BY LIST (a)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_config_map
;
CREATE UNIQUE INDEX base_mv_id_idx ON split_view_probe.base_mv (id)
;
create schema c
;
-- Create the operator class
CREATE OPERATOR CLASS gist_bytea_ops
DEFAULT FOR TYPE bytea USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bytea_consistent (internal, bytea, int2, oid, internal),
	FUNCTION	2	gbt_bytea_union (internal, internal),
	FUNCTION	3	gbt_bytea_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_bytea_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bytea_picksplit (internal, internal),
	FUNCTION	7	gbt_bytea_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
create extension pageinspect
;
--
-- Build a table for testing
--

CREATE TABLE CHAR_TBL(f1 char)
;
CREATE FUNCTION dblink (text)
RETURNS setof record
AS '$libdir/dblink','dblink_record'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE collate_test20 (f1 text COLLATE "C" PRIMARY KEY)
;
alter table s1.abc add column f2 float8
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_db_role_setting
;
CREATE TABLE fk (a int) PARTITION BY RANGE (a)
;
DROP INDEX table_dropped_index_no_pk_idx
;
CREATE TRIGGER gtest2a BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (NEW.b < 0)  -- error
  EXECUTE PROCEDURE gtest_trigger_func()
;
CREATE CAST (ismn13 AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
CREATE TABLE parent_tbl (a int, b int) PARTITION BY RANGE(a)
;
CREATE OPERATOR myschema.= (
	  leftarg    = bigint,
	  rightarg   = int,
	  procedure  = myschema.myintne)
;
CREATE UNIQUE INDEX "hub_name" ON "public"."hub" USING "btree" ("name")
;
CREATE INDEX idx_interval ON test_interval USING gin (i)
;
DROP FUNCTION drop_func_test1()
;
CREATE FUNCTION "public"."equipment_named_ambiguous_2a"("hobby" "text") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where hobby = equipment_named_ambiguous_2a.hobby$$
;
ALTER COLLATION split_collation_probe.c_from_c RENAME TO c_from_c_renamed
;
ALTER TABLE ONLY parted_no_parts ALTER a DROP NOT NULL
;
-- CREATE TABLE ..  PARTITION BY supports USING.
CREATE TABLE tableam_parted_heap2 (a text, b int) PARTITION BY list (a) USING heap2
;
CREATE FUNCTION "public"."equipment_named_ambiguous_1b"("hobby" "public"."hobbies_r") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where equipment_r.hobby = hobby.name$$
;
do $$
declare x int := 1/0;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
-- now check same queries using a bulk-loaded index
drop index test_range_gist_idx
;
CREATE TEMP TABLE pg_temp_13.pg_temp_25047_2 (tid pg_catalog.tid)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_subscription
;
CREATE INDEX people_names_expr_only ON people ((firstname || ' ' || lastname))
;
CREATE TABLE test5a (a int, b priv_testdomain1)
;
DROP TABLE test_replica_identity5
;
create temp table exists_tbl_null partition of exists_tbl for values in (null)
;
drop function explain_parallel_sort_stats()
;
alter table parted_validate_test validate constraint parted_validate_test_chka
;
CREATE TABLE "public"."prt3_n_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
------------------------------------------------------------------------------------------
[COMMENT_TABLESPACE] COMMENT ON TABLESPACE split_ts_space IS NULL
;
----------
[ALTER_CATALOG] ALTER DATABASE contrib_regression RESET session_authorization
;
----------
[CREATE_TABLE] CREATE TABLE t1 (a int, b text, c int)
;
----------
[DROP_PROG_OBJ] drop function f1(x anyarray)
;
----------
[CREATE_TABLE] CREATE TABLE ref_c2 PARTITION OF ref_c FOR VALUES FROM (200) TO (300)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION snoop(anyelement)
RETURNS boolean AS
$$
BEGIN
  RAISE NOTICE 'snooped value: %', $1;
  RETURN true;
END;
$$
LANGUAGE plpgsql COST 0.000001
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_rewrite
;
----------
[CREATE_TABLE] CREATE TABLE d_lp_fk (a int PRIMARY KEY) PARTITION BY LIST(a)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37940_2
;
----------
[ALTER_TABLESPACE] ALTER TABLESPACE regress_tblspace SET (random_page_cost = 1.0, seq_page_cost = 1.1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(isbn, ean13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[UNKNOWN] SECURITY LABEL ON COLUMN t5.f IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
----------
[CREATE_TABLE] /*
 * Test double inheritance
 *
 * Ensure that defaults are NOT included unless
 * INCLUDING DEFAULTS is specified
 */
CREATE TABLE ctla (aa TEXT)
;
----------
[CREATE_PROG_OBJ] -- old-style spelling of same (except without parallel-safe; that's too new)
CREATE AGGREGATE oldcnt (
   sfunc = int8inc, basetype = 'ANY', stype = int8,
   initcond = '0'
)
;
----------
[CREATE_TABLE] -- Test cross-partition updates and attribute mapping
CREATE TABLE foo_parted (a int, b float8, c text) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_config_map
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX base_mv_id_idx ON split_view_probe.base_mv (id)
;
----------
[CREATE_SCHEMA] create schema c
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_bytea_ops
DEFAULT FOR TYPE bytea USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bytea_consistent (internal, bytea, int2, oid, internal),
	FUNCTION	2	gbt_bytea_union (internal, internal),
	FUNCTION	3	gbt_bytea_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_bytea_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bytea_picksplit (internal, internal),
	FUNCTION	7	gbt_bytea_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
----------
[CREATE_LIBRARY] create extension pageinspect
;
----------
[CREATE_TABLE] --
-- Build a table for testing
--

CREATE TABLE CHAR_TBL(f1 char)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink (text)
RETURNS setof record
AS '$libdir/dblink','dblink_record'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE collate_test20 (f1 text COLLATE "C" PRIMARY KEY)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table s1.abc add column f2 float8
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_db_role_setting
;
----------
[CREATE_TABLE] CREATE TABLE fk (a int) PARTITION BY RANGE (a)
;
----------
[DROP_INDEX] DROP INDEX table_dropped_index_no_pk_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest2a BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (NEW.b < 0)  -- error
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ismn13 AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[CREATE_TABLE] CREATE TABLE parent_tbl (a int, b int) PARTITION BY RANGE(a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR myschema.= (
	  leftarg    = bigint,
	  rightarg   = int,
	  procedure  = myschema.myintne)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "hub_name" ON "public"."hub" USING "btree" ("name")
;
----------
[ADD_INDEX] CREATE INDEX idx_interval ON test_interval USING gin (i)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION drop_func_test1()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."equipment_named_ambiguous_2a"("hobby" "text") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where hobby = equipment_named_ambiguous_2a.hobby$$
;
----------
[ALTER_POLICY] ALTER COLLATION split_collation_probe.c_from_c RENAME TO c_from_c_renamed
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY parted_no_parts ALTER a DROP NOT NULL
;
----------
[CREATE_TABLE] -- CREATE TABLE ..  PARTITION BY supports USING.
CREATE TABLE tableam_parted_heap2 (a text, b int) PARTITION BY list (a) USING heap2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."equipment_named_ambiguous_1b"("hobby" "public"."hobbies_r") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where equipment_r.hobby = hobby.name$$
;
----------
[BLOCK] do $$
declare x int := 1/0;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[DROP_INDEX] -- now check same queries using a bulk-loaded index
drop index test_range_gist_idx
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_25047_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_subscription
;
----------
[ADD_INDEX] CREATE INDEX people_names_expr_only ON people ((firstname || ' ' || lastname))
;
----------
[CREATE_TABLE] CREATE TABLE test5a (a int, b priv_testdomain1)
;
----------
[DROP_TABLE] DROP TABLE test_replica_identity5
;
----------
[CREATE_TABLE] create temp table exists_tbl_null partition of exists_tbl for values in (null)
;
----------
[DROP_PROG_OBJ] drop function explain_parallel_sort_stats()
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] alter table parted_validate_test validate constraint parted_validate_test_chka
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt3_n_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
