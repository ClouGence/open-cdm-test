ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP COLUMN IF EXISTS no_column
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
CREATE INDEX t2_btree ON s5.t2 USING BTREE (i)
;
CREATE TABLE regression_vacuumdb_parted (a INT) PARTITION BY LIST (a)
;
CREATE VIEW pg_timezone_names AS
    SELECT * FROM pg_timezone_names()
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
-- Fail when option and namespace do not exist
CREATE TABLE reloptions_test2(i INT) WITH (not_existing_option=2)
;
CREATE TYPE jsrec_i_not_null AS (
	i	js_int_not_null
)
;
ALTER TABLE ONLY "public"."covidxpart3" DROP COLUMN "........pg.dropped.2........"
;
DROP TABLE test_nowait
;
TRUNCATE TABLE pk
;
create table child2 (b int, a text)
;
CREATE TABLE testjsonb (
       j jsonb
)
;
ALTER OPERATOR FAMILY alt_opf19 USING btree ADD FUNCTION 5 test_opclass_options_func(internal, text[], bool)
;
CREATE VIEW pg_stat_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_numscans(C.oid) AS seq_scan,
            pg_stat_get_lastscan(C.oid) AS last_seq_scan,
            pg_stat_get_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_numscans(I.indexrelid))::bigint AS idx_scan,
            max(pg_stat_get_lastscan(I.indexrelid)) AS last_idx_scan,
            sum(pg_stat_get_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_tuples_hot_updated(C.oid) AS n_tup_hot_upd,
            pg_stat_get_tuples_newpage_updated(C.oid) AS n_tup_newpage_upd,
            pg_stat_get_live_tuples(C.oid) AS n_live_tup,
            pg_stat_get_dead_tuples(C.oid) AS n_dead_tup,
            pg_stat_get_mod_since_analyze(C.oid) AS n_mod_since_analyze,
            pg_stat_get_ins_since_vacuum(C.oid) AS n_ins_since_vacuum,
            pg_stat_get_last_vacuum_time(C.oid) as last_vacuum,
            pg_stat_get_last_autovacuum_time(C.oid) as last_autovacuum,
            pg_stat_get_last_analyze_time(C.oid) as last_analyze,
            pg_stat_get_last_autoanalyze_time(C.oid) as last_autoanalyze,
            pg_stat_get_vacuum_count(C.oid) AS vacuum_count,
            pg_stat_get_autovacuum_count(C.oid) AS autovacuum_count,
            pg_stat_get_analyze_count(C.oid) AS analyze_count,
            pg_stat_get_autoanalyze_count(C.oid) AS autoanalyze_count,
            pg_stat_get_total_vacuum_time(C.oid) AS total_vacuum_time,
            pg_stat_get_total_autovacuum_time(C.oid) AS total_autovacuum_time,
            pg_stat_get_total_analyze_time(C.oid) AS total_analyze_time,
            pg_stat_get_total_autoanalyze_time(C.oid) AS total_autoanalyze_time
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm', 'p')
    GROUP BY C.oid, N.nspname, C.relname
;
drop table test9s
;
drop table defpart_attach_test
;
COMMENT ON TEXT SEARCH DICTIONARY romanian_stem IS 'snowball stemmer for romanian language'
;
-- create test data for polymorphic aggregates
create temp table t(f1 int, f2 int[], f3 text)
;
ALTER FUNCTION gbt_cash_compress(internal) PARALLEL SAFE
;
create or replace function test_conv(
  input IN bytea,
  src_encoding IN text,
  dst_encoding IN text,
  result OUT bytea,
  errorat OUT bytea,
  error OUT text)
language plpgsql as
$$
declare
  validlen int;
begin
  -- First try to perform the conversion with noError = false. If that errors out,
  -- capture the error message, and try again with noError = true. The second call
  -- should succeed and return the position of the error, return that too.
  begin
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, false);
    errorat = NULL;
    error := NULL;
  exception when others then
    error := sqlerrm;
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, true);
    errorat = substr(input, validlen + 1);
  end;
  return;
end;
$$
;
ALTER TABLE fk_notpartitioned_pk DROP COLUMN fdrop1, DROP COLUMN fdrop2
;
alter table check_con_tbl add check (check_con_function(check_con_tbl.*))
;
VACUUM (FREEZE) information_schema.sql_parts
;
CREATE FUNCTION gin_compare_prefix_oid(oid, oid, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE TABLE test_varbit (
	i varbit
)
;
analyze tbl_ra
;
CREATE OPERATOR > (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = <=,
    COMMUTATOR = <,
    PROCEDURE  = citext_gt,
    RESTRICT   = scalargtsel,
    JOIN       = scalargtjoinsel
)
;
CREATE FUNCTION "public"."city_delete"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    DELETE FROM city_table WHERE city_id = OLD.city_id;
    if NOT FOUND then RETURN NULL; end if;
    RETURN OLD;
end;
$$
;
CREATE INDEX ON pvactst2 (i)
;
DROP INDEX IF EXISTS split_maint.maint_name_idx
;
COMMENT ON FUNCTION seg_le(seg, seg) IS
'less than or equal'
;
CREATE TYPE "public"."city_budget" (
    INTERNALLENGTH = 16,
    INPUT = "public"."int44in",
    OUTPUT = "public"."int44out",
    ELEMENT = integer,
    CATEGORY = 'x',
    PREFERRED = true,
    ALIGNMENT = int4,
    STORAGE = plain
)
;
ALTER TABLE pg_toast.pg_toast_27713 RENAME TO reind_con_toast
;
-- foreign key
CREATE UNLOGGED TABLE unlogged3(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES unlogged3)
;
CREATE INDEX is_odd_clone ON stats_import.test_clone(((comp).a % 2 = 1))
;
create schema not_there_initially
;
ALTER INDEX pg_toast.pg_toast_27970_index RENAME TO reind_con_toast_idx
;
drop index _tstidx
;
ALTER TABLE rls_tbl_force ENABLE ROW LEVEL SECURITY
;
ALTER TEXT SEARCH CONFIGURATION catalan ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH catalan_stem
;
drop function stacked_diagnostics_test()
;
--
-- Expression index tests (with regular column)
--
create unique index expr_comp_key_index on insertconflicttest(key, lower(fruit))
;
--
-- Test inheritance of NOT NULL constraints
--
create table pp1 (f1 int)
;
CREATE TABLE vtype2 (a int)
;
drop view insertconflictv
;
CREATE TEMP TABLE tab_search_diff_2 (a bigint)
;
DROP TYPE jsbrec
;
SECURITY LABEL ON PUBLICATION dummy_pub IS 'classified'
;
CREATE PUBLICATION testpub3_forschema
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP COLUMN IF EXISTS no_column
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
----------
[ADD_INDEX] CREATE INDEX t2_btree ON s5.t2 USING BTREE (i)
;
----------
[CREATE_TABLE] CREATE TABLE regression_vacuumdb_parted (a INT) PARTITION BY LIST (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_timezone_names AS
    SELECT * FROM pg_timezone_names()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_operator
;
----------
[CREATE_TABLE] -- Fail when option and namespace do not exist
CREATE TABLE reloptions_test2(i INT) WITH (not_existing_option=2)
;
----------
[CREATE_TYPE] CREATE TYPE jsrec_i_not_null AS (
	i	js_int_not_null
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."covidxpart3" DROP COLUMN "........pg.dropped.2........"
;
----------
[DROP_TABLE] DROP TABLE test_nowait
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE pk
;
----------
[CREATE_TABLE] create table child2 (b int, a text)
;
----------
[CREATE_TABLE] CREATE TABLE testjsonb (
       j jsonb
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf19 USING btree ADD FUNCTION 5 test_opclass_options_func(internal, text[], bool)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_numscans(C.oid) AS seq_scan,
            pg_stat_get_lastscan(C.oid) AS last_seq_scan,
            pg_stat_get_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_numscans(I.indexrelid))::bigint AS idx_scan,
            max(pg_stat_get_lastscan(I.indexrelid)) AS last_idx_scan,
            sum(pg_stat_get_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_tuples_hot_updated(C.oid) AS n_tup_hot_upd,
            pg_stat_get_tuples_newpage_updated(C.oid) AS n_tup_newpage_upd,
            pg_stat_get_live_tuples(C.oid) AS n_live_tup,
            pg_stat_get_dead_tuples(C.oid) AS n_dead_tup,
            pg_stat_get_mod_since_analyze(C.oid) AS n_mod_since_analyze,
            pg_stat_get_ins_since_vacuum(C.oid) AS n_ins_since_vacuum,
            pg_stat_get_last_vacuum_time(C.oid) as last_vacuum,
            pg_stat_get_last_autovacuum_time(C.oid) as last_autovacuum,
            pg_stat_get_last_analyze_time(C.oid) as last_analyze,
            pg_stat_get_last_autoanalyze_time(C.oid) as last_autoanalyze,
            pg_stat_get_vacuum_count(C.oid) AS vacuum_count,
            pg_stat_get_autovacuum_count(C.oid) AS autovacuum_count,
            pg_stat_get_analyze_count(C.oid) AS analyze_count,
            pg_stat_get_autoanalyze_count(C.oid) AS autoanalyze_count,
            pg_stat_get_total_vacuum_time(C.oid) AS total_vacuum_time,
            pg_stat_get_total_autovacuum_time(C.oid) AS total_autovacuum_time,
            pg_stat_get_total_analyze_time(C.oid) AS total_analyze_time,
            pg_stat_get_total_autoanalyze_time(C.oid) AS total_autoanalyze_time
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm', 'p')
    GROUP BY C.oid, N.nspname, C.relname
;
----------
[DROP_TABLE] drop table test9s
;
----------
[DROP_TABLE] drop table defpart_attach_test
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY romanian_stem IS 'snowball stemmer for romanian language'
;
----------
[CREATE_TABLE] -- create test data for polymorphic aggregates
create temp table t(f1 int, f2 int[], f3 text)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_cash_compress(internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function test_conv(
  input IN bytea,
  src_encoding IN text,
  dst_encoding IN text,
  result OUT bytea,
  errorat OUT bytea,
  error OUT text)
language plpgsql as
$$
declare
  validlen int;
begin
  -- First try to perform the conversion with noError = false. If that errors out,
  -- capture the error message, and try again with noError = true. The second call
  -- should succeed and return the position of the error, return that too.
  begin
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, false);
    errorat = NULL;
    error := NULL;
  exception when others then
    error := sqlerrm;
    select * into validlen, result from test_enc_conversion(input, src_encoding, dst_encoding, true);
    errorat = substr(input, validlen + 1);
  end;
  return;
end;
$$
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk_notpartitioned_pk DROP COLUMN fdrop1, DROP COLUMN fdrop2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table check_con_tbl add check (check_con_function(check_con_tbl.*))
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) information_schema.sql_parts
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_oid(oid, oid, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE test_varbit (
	i varbit
)
;
----------
[ADMIN_TABLE] analyze tbl_ra
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = <=,
    COMMUTATOR = <,
    PROCEDURE  = citext_gt,
    RESTRICT   = scalargtsel,
    JOIN       = scalargtjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."city_delete"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    DELETE FROM city_table WHERE city_id = OLD.city_id;
    if NOT FOUND then RETURN NULL; end if;
    RETURN OLD;
end;
$$
;
----------
[ADD_INDEX] CREATE INDEX ON pvactst2 (i)
;
----------
[DROP_INDEX] DROP INDEX IF EXISTS split_maint.maint_name_idx
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_le(seg, seg) IS
'less than or equal'
;
----------
[CREATE_TYPE] CREATE TYPE "public"."city_budget" (
    INTERNALLENGTH = 16,
    INPUT = "public"."int44in",
    OUTPUT = "public"."int44out",
    ELEMENT = integer,
    CATEGORY = 'x',
    PREFERRED = true,
    ALIGNMENT = int4,
    STORAGE = plain
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27713 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- foreign key
CREATE UNLOGGED TABLE unlogged3(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES unlogged3)
;
----------
[ADD_INDEX] CREATE INDEX is_odd_clone ON stats_import.test_clone(((comp).a % 2 = 1))
;
----------
[CREATE_SCHEMA] create schema not_there_initially
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27970_index RENAME TO reind_con_toast_idx
;
----------
[DROP_INDEX] drop index _tstidx
;
----------
[ALTER_TABLE] ALTER TABLE rls_tbl_force ENABLE ROW LEVEL SECURITY
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION catalan ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH catalan_stem
;
----------
[DROP_PROG_OBJ] drop function stacked_diagnostics_test()
;
----------
[ADD_INDEX] --
-- Expression index tests (with regular column)
--
create unique index expr_comp_key_index on insertconflicttest(key, lower(fruit))
;
----------
[CREATE_TABLE] --
-- Test inheritance of NOT NULL constraints
--
create table pp1 (f1 int)
;
----------
[CREATE_TABLE] CREATE TABLE vtype2 (a int)
;
----------
[DROP_VIEW] drop view insertconflictv
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tab_search_diff_2 (a bigint)
;
----------
[DROP_TYPE] DROP TYPE jsbrec
;
----------
[SECURITY_LABEL] SECURITY LABEL ON PUBLICATION dummy_pub IS 'classified'
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub3_forschema
;
