VACUUM (ANALYZE) pg_catalog.pg_range
;
alter table atacc1 add column test2 int default 0 primary key
;
VACUUM pg_catalog.pg_amop
;
CREATE INDEX "brinidx" ON "public"."brintest" USING "brin" ("byteacol", "charcol", "namecol", "int8col", "int2col", "int4col", "textcol", "oidcol", "tidcol", "float4col", "float8col", "macaddrcol", "inetcol", "inetcol" "inet_minmax_ops", "cidrcol", "cidrcol" "inet_minmax_ops", "bpcharcol", "datecol", "timecol", "timestampcol", "timestamptzcol", "intervalcol", "timetzcol", "bitcol", "varbitcol", "numericcol", "uuidcol", "int4rangecol", "lsncol", "boxcol") WITH ("pages_per_range"='1')
;
CREATE TABLE test_float4 (
	i float4
)
;
VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) reloptions_test
;
CREATE TABLE bttest_multi(id int8, data int8)
;
VACUUM (PARALLEL 0) pg_catalog.pg_auth_members
;
CREATE FUNCTION gbt_bit_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER FOREIGN TABLE foreign_stats ADD COLUMN b integer DEFAULT 1
;
VACUUM FREEZE
;
COMMENT ON CONSTRAINT "inv_ck" ON DOMAIN "public"."constraint_comments_dom" IS 'comment on invalid constraint'
;
/*
 * 6.77
 * USER_DEFINED_TYPES view
 */

CREATE VIEW user_defined_types AS
    SELECT CAST(current_database() AS sql_identifier) AS user_defined_type_catalog,
           CAST(n.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(c.relname AS sql_identifier) AS user_defined_type_name,
           CAST('STRUCTURED' AS character_data) AS user_defined_type_category,
           CAST('YES' AS yes_or_no) AS is_instantiable,
           CAST(null AS yes_or_no) AS is_final,
           CAST(null AS character_data) AS ordering_form,
           CAST(null AS character_data) AS ordering_category,
           CAST(null AS sql_identifier) AS ordering_routine_catalog,
           CAST(null AS sql_identifier) AS ordering_routine_schema,
           CAST(null AS sql_identifier) AS ordering_routine_name,
           CAST(null AS character_data) AS reference_type,
           CAST(null AS character_data) AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(null AS sql_identifier) AS source_dtd_identifier,
           CAST(null AS sql_identifier) AS ref_dtd_identifier

    FROM pg_namespace n, pg_class c, pg_type t

    WHERE n.oid = c.relnamespace
          AND t.typrelid = c.oid
          AND c.relkind = 'c'
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
-- fail
create table child (a double precision, b decimal(10,4)) inherits (parent)
;
ALTER PUBLICATION tap_pub_A ADD TABLE tab_new
;
CREATE TABLE quebec(i int primary key)
;
create unique index insertconflicti2 on insertconflict(b)
  where coalesce(a, 1) > 0
;
DROP SCHEMA sch1 cascade
;
CREATE RULE hat_nosert AS ON INSERT TO hats
    DO INSTEAD
    INSERT INTO hat_data VALUES (
           NEW.hat_name,
           NEW.hat_color)
        ON CONFLICT (hat_name COLLATE "C" bpchar_pattern_ops) WHERE hat_color = 'green'
        DO NOTHING
        RETURNING *
;
ALTER TABLE parttab ATTACH PARTITION parttab2 FOR VALUES IN (2)
;
VACUUM (ANALYZE) pg_catalog.pg_user_mapping
;
CREATE UNIQUE INDEX CONCURRENTLY on tbl (c1, c2) INCLUDE (c3, c4)
;
-- enum type list partition key
create type pp_colors as enum ('green', 'blue', 'black')
;
create table PLine (
    slotname	char(20),
    phonenumber	char(20),
    comment	text,
    backlink	char(20)
)
;
CREATE EXTENSION dict_xsyn
;
CREATE INDEX idx_money ON test_money USING gin (i)
;
ANALYZE pg_temp_26.pg_temp_27076
;
create aggregate least_agg(variadic items anyarray) (
  stype = anyelement, sfunc = least_accum
)
;
CREATE SCHEMA split_alter18
;
TRUNCATE truncme
;
CREATE FUNCTION isnle(ean13, ismn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- The first-level connection's backend will crash on exit given OpenLDAP
-- [2.4.24, 2.4.31].  We won't see evidence of any crash until the victim
-- process terminates and the postmaster responds.  If process termination
-- entails writing a core dump, that can take awhile.  Wait for the process to
-- vanish.  At that point, the postmaster has called waitpid() on the crashed
-- process, and it will accept no new connections until it has reinitialized
-- the cluster.  (We can't exploit pg_stat_activity, because the crash happens
-- after the backend updates shared memory to reflect its impending exit.)
DO $pl$
DECLARE
	detail text;
BEGIN
	PERFORM wait_pid(crash_pid)
	FROM dblink(connection_parameters(), $$
		SELECT pg_backend_pid() FROM dblink(
			'service=test_ldap '||connection_parameters(),
			-- This string concatenation is a hack to shoehorn a
			-- set_pgservicefile call into the SQL statement.
			'SELECT 1' || set_pgservicefile('pg_service.conf')
		) t(c int)
	$$) AS t(crash_pid int);
EXCEPTION WHEN OTHERS THEN
	GET STACKED DIAGNOSTICS detail = PG_EXCEPTION_DETAIL;
	-- Expected error in a non-LDAP build.
	IF NOT detail LIKE 'syntax error in service file%' THEN RAISE; END IF;
END
$pl$
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
VACUUM (ANALYZE) pg_catalog.pg_trigger
;
CREATE TABLE plt2_adv_p2 PARTITION OF plt2_adv FOR VALUES IN (NULL)
;
CREATE TABLE test_stat_tab(key text not null, value int)
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a[0]'))
;
-- error
drop table rules_base
;
CREATE AGGREGATE "public"."least_agg"(VARIADIC "items" "anyarray") (
    SFUNC = "public"."least_accum",
    STYPE = "anyelement"
)
;
-- Check for uniform distribution using the Kolmogorov-Smirnov test.

CREATE FUNCTION ks_test_uniform_random_int_in_range()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 999999) / 1000000.0 r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
CREATE TRIGGER sub1_tab1_2_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1_2
  FOR EACH ROW EXECUTE PROCEDURE sub1_trigger_activity_func()
;
DROP TABLE logged3
;
ALTER TABLE pg_toast.pg_toast_28633 RENAME TO reind_con_toast
;
ANALYZE pg_catalog.pg_sequence
;
CREATE TRIGGER merge_bru BEFORE UPDATE ON target FOR EACH ROW EXECUTE PROCEDURE merge_trigfunc ()
;
CREATE TABLE "public"."notnull_part1_1_upg" (
    "a" integer CONSTRAINT "notnull_con" NOT NULL,
    "b" integer
)
;
CREATE TABLE "public"."prt2_p3" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
ALTER TABLE pg_temp_12.pg_temp_26654_2 ADD COLUMN newdata pg_temp_12.pg_temp_26654
;
-- Insert/update on a column that is array of composite

create temp table t1 (f1 int8_tbl[])
;
CREATE TEXT SEARCH CONFIGURATION yiddish
	(PARSER = default)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_range
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table atacc1 add column test2 int default 0 primary key
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_amop
;
----------
[ADD_INDEX] CREATE INDEX "brinidx" ON "public"."brintest" USING "brin" ("byteacol", "charcol", "namecol", "int8col", "int2col", "int4col", "textcol", "oidcol", "tidcol", "float4col", "float8col", "macaddrcol", "inetcol", "inetcol" "inet_minmax_ops", "cidrcol", "cidrcol" "inet_minmax_ops", "bpcharcol", "datecol", "timecol", "timestampcol", "timestamptzcol", "intervalcol", "timetzcol", "bitcol", "varbitcol", "numericcol", "uuidcol", "int4rangecol", "lsncol", "boxcol") WITH ("pages_per_range"='1')
;
----------
[CREATE_TABLE] CREATE TABLE test_float4 (
	i float4
)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) reloptions_test
;
----------
[CREATE_TABLE] CREATE TABLE bttest_multi(id int8, data int8)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_auth_members
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bit_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE foreign_stats ADD COLUMN b integer DEFAULT 1
;
----------
[ADMIN_TABLE] VACUUM FREEZE
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT "inv_ck" ON DOMAIN "public"."constraint_comments_dom" IS 'comment on invalid constraint'
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.77
 * USER_DEFINED_TYPES view
 */

CREATE VIEW user_defined_types AS
    SELECT CAST(current_database() AS sql_identifier) AS user_defined_type_catalog,
           CAST(n.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(c.relname AS sql_identifier) AS user_defined_type_name,
           CAST('STRUCTURED' AS character_data) AS user_defined_type_category,
           CAST('YES' AS yes_or_no) AS is_instantiable,
           CAST(null AS yes_or_no) AS is_final,
           CAST(null AS character_data) AS ordering_form,
           CAST(null AS character_data) AS ordering_category,
           CAST(null AS sql_identifier) AS ordering_routine_catalog,
           CAST(null AS sql_identifier) AS ordering_routine_schema,
           CAST(null AS sql_identifier) AS ordering_routine_name,
           CAST(null AS character_data) AS reference_type,
           CAST(null AS character_data) AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(null AS sql_identifier) AS source_dtd_identifier,
           CAST(null AS sql_identifier) AS ref_dtd_identifier

    FROM pg_namespace n, pg_class c, pg_type t

    WHERE n.oid = c.relnamespace
          AND t.typrelid = c.oid
          AND c.relkind = 'c'
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
----------
[CREATE_TABLE] -- fail
create table child (a double precision, b decimal(10,4)) inherits (parent)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_A ADD TABLE tab_new
;
----------
[CREATE_TABLE] CREATE TABLE quebec(i int primary key)
;
----------
[ADD_INDEX] create unique index insertconflicti2 on insertconflict(b)
  where coalesce(a, 1) > 0
;
----------
[DROP_SCHEMA] DROP SCHEMA sch1 cascade
;
----------
[CREATE_POLICY] CREATE RULE hat_nosert AS ON INSERT TO hats
    DO INSTEAD
    INSERT INTO hat_data VALUES (
           NEW.hat_name,
           NEW.hat_color)
        ON CONFLICT (hat_name COLLATE "C" bpchar_pattern_ops) WHERE hat_color = 'green'
        DO NOTHING
        RETURNING *
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE parttab ATTACH PARTITION parttab2 FOR VALUES IN (2)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_user_mapping
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX CONCURRENTLY on tbl (c1, c2) INCLUDE (c3, c4)
;
----------
[CREATE_TYPE] -- enum type list partition key
create type pp_colors as enum ('green', 'blue', 'black')
;
----------
[CREATE_TABLE] create table PLine (
    slotname	char(20),
    phonenumber	char(20),
    comment	text,
    backlink	char(20)
)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION dict_xsyn
;
----------
[ADD_INDEX] CREATE INDEX idx_money ON test_money USING gin (i)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_26.pg_temp_27076
;
----------
[CREATE_PROG_OBJ] create aggregate least_agg(variadic items anyarray) (
  stype = anyelement, sfunc = least_accum
)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_alter18
;
----------
[TRUNCATE_TABLE] TRUNCATE truncme
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ean13, ismn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[BLOCK] -- The first-level connection's backend will crash on exit given OpenLDAP
-- [2.4.24, 2.4.31].  We won't see evidence of any crash until the victim
-- process terminates and the postmaster responds.  If process termination
-- entails writing a core dump, that can take awhile.  Wait for the process to
-- vanish.  At that point, the postmaster has called waitpid() on the crashed
-- process, and it will accept no new connections until it has reinitialized
-- the cluster.  (We can't exploit pg_stat_activity, because the crash happens
-- after the backend updates shared memory to reflect its impending exit.)
DO $pl$
DECLARE
	detail text;
BEGIN
	PERFORM wait_pid(crash_pid)
	FROM dblink(connection_parameters(), $$
		SELECT pg_backend_pid() FROM dblink(
			'service=test_ldap '||connection_parameters(),
			-- This string concatenation is a hack to shoehorn a
			-- set_pgservicefile call into the SQL statement.
			'SELECT 1' || set_pgservicefile('pg_service.conf')
		) t(c int)
	$$) AS t(crash_pid int);
EXCEPTION WHEN OTHERS THEN
	GET STACKED DIAGNOSTICS detail = PG_EXCEPTION_DETAIL;
	-- Expected error in a non-LDAP build.
	IF NOT detail LIKE 'syntax error in service file%' THEN RAISE; END IF;
END
$pl$
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_trigger
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p2 PARTITION OF plt2_adv FOR VALUES IN (NULL)
;
----------
[CREATE_TABLE] CREATE TABLE test_stat_tab(key text not null, value int)
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a[0]'))
;
----------
[DROP_TABLE] -- error
drop table rules_base
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."least_agg"(VARIADIC "items" "anyarray") (
    SFUNC = "public"."least_accum",
    STYPE = "anyelement"
)
;
----------
[CREATE_PROG_OBJ] -- Check for uniform distribution using the Kolmogorov-Smirnov test.

CREATE FUNCTION ks_test_uniform_random_int_in_range()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 999999) / 1000000.0 r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER sub1_tab1_2_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1_2
  FOR EACH ROW EXECUTE PROCEDURE sub1_trigger_activity_func()
;
----------
[DROP_TABLE] DROP TABLE logged3
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28633 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_sequence
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER merge_bru BEFORE UPDATE ON target FOR EACH ROW EXECUTE PROCEDURE merge_trigfunc ()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_part1_1_upg" (
    "a" integer CONSTRAINT "notnull_con" NOT NULL,
    "b" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_p3" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26654_2 ADD COLUMN newdata pg_temp_12.pg_temp_26654
;
----------
[CREATE_TABLE] -- Insert/update on a column that is array of composite

create temp table t1 (f1 int8_tbl[])
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION yiddish
	(PARSER = default)
;
