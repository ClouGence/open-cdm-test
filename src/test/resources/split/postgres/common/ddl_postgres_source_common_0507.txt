CREATE TABLE xmltest2(x xml, _path text)
;
IMPORT FOREIGN SCHEMA import_source EXCEPT (t1, "x 4", nonesuch)
  FROM SERVER loopback INTO import_dest4
;
do $$
begin
  assert 1=0;  -- won't be tested
end;
$$
;
ALTER TABLE ONLY "fkpart3"."pk2" DROP COLUMN "........pg.dropped.1........"
;
IMPORT FOREIGN SCHEMA split_import_src LIMIT TO (import_source) FROM SERVER split_pg_import INTO split_import_dst OPTIONS (import_collate 'false', import_default 'false')
;
ALTER TABLE pg_toast.pg_toast_30635 RENAME TO reind_con_toast
;
CREATE SCHEMA IF NOT EXISTS bar
;
--
-- create default conversion with qualified name
--
CREATE DEFAULT CONVERSION public.mydef FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
CREATE INDEX "btree_idx1" ON "public"."btree_test" USING "btree" ("a") WITH ("vacuum_cleanup_index_scale_factor"='70.0')
;
DROP FUNCTION leaker_1(bool)
;
ALTER TYPE "public"."rainbow" ADD VALUE 'yellow'
;
create domain posint as int check (value > 0)
;
-- 0

-- truncate with ONLY clause
-- Since ONLY is specified, the table tru_ftable_child that inherits
-- tru_ftable_parent locally is not truncated.
TRUNCATE ONLY tru_ftable_parent
;
DROP EVENT TRIGGER IF EXISTS split_event_probe_renamed
;
DROP TABLE vacparted_i
;
CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=31170 host=/tmp/eViv7QVsoS dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (streaming = on, two_phase = on)
;
drop table parted_copytest
;
/* contrib/btree_gist/btree_gist--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Add support for indexing UUID columns

-- define the GiST support methods
CREATE FUNCTION gbt_uuid_consistent(internal,uuid,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE spc_commit (id serial PRIMARY KEY, id2 int)
;
ALTER TEXT SEARCH DICTIONARY intdict (ABSVAL = true)
;
DROP TABLE pt, ref
;
alter table utr1 drop column q
;
-- check some of the allowed relkinds
create table regular_table (a int, b text)
;
alter table idxpart attach partition idxpart1 for values from (1) to (2)
;
-- check that a BR constraint can't make partition contain violating rows
create table brtrigpartcon (a int, b text) partition by list (a)
;
CREATE VIEW nontemp2 AS SELECT * FROM t1 INNER JOIN t2 ON t1.num = t2.num2
;
CREATE TABLE clstrpart33 PARTITION OF clstrpart3 DEFAULT
;
/*
 * 1.test CREATE INDEX
 *
 * Deliberately avoid dropping objects in this section, to get some pg_dump
 * coverage.
 */

CREATE TABLE tbl_include_reg (c1 int, c2 int, c3 int, c4 box)
;
CREATE SUBSCRIPTION tap_sub_C
	CONNECTION 'port=14114 host=/tmp/vJYivPRhPX dbname=postgres application_name=tap_sub_C'
	PUBLICATION tap_pub_B
	WITH (two_phase = on)
;
DROP FUNCTION retset(int)
;
COMMENT ON RULE rtest_v1_bad ON rtest_v1 IS 'bad rule'
;
VACUUM (ANALYZE) pg_catalog.pg_aggregate
;
CREATE OPERATOR === (
    LEFTARG = int,
    RIGHTARG = int,
    PROCEDURE = int4eq,
    COMMUTATOR = ===
)
;
create function pl_qual_names (param1 int) returns void as $$
<<outerblock>>
declare
  param1 int := 1;
begin
  <<innerblock>>
  declare
    param1 int := 2;
  begin
    raise notice 'param1 = %', param1;
    raise notice 'pl_qual_names.param1 = %', pl_qual_names.param1;
    raise notice 'outerblock.param1 = %', outerblock.param1;
    raise notice 'innerblock.param1 = %', innerblock.param1;
  end;
end;
$$ language plpgsql
;
create or replace function whoami() returns trigger language plpgsql
as $$
begin
  raise notice 'I am %', current_user;
  perform 1 / 0;
  return null;
end;
$$
;
-- check that the table being attached exists
ALTER TABLE list_parted ATTACH PARTITION nonexistent FOR VALUES IN (1)
;
CREATE TABLE split_alter18.parent_table (id integer PRIMARY KEY)
;
CREATE FUNCTION cube_ne(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE pg_foreign_table ADD PRIMARY KEY USING INDEX pg_foreign_table_relid_index
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_opfamily
;
CREATE FUNCTION "public"."consumes_rw_array"(integer[]) RETURNS integer
    LANGUAGE "plpgsql" STABLE
    AS $_$
  begin return $1[1]; end;
$_$
;
create function stable_one() returns int as $$ begin return 1; end; $$ language plpgsql stable
;
DROP TABLE reindex_inj.tbl
;
CREATE TABLE public.tab1_child2 PARTITION OF sch1.tab1_parent FOR VALUES IN (4, 5, 6)
;
-- register the opclass for indexing (not as default)

CREATE OPERATOR CLASS gist__intbig_ops
FOR TYPE _int4 USING gist
AS
	OPERATOR	3	&&,
	OPERATOR	6	= (anyarray, anyarray),
	OPERATOR	7	@>,
	OPERATOR	8	<@,
	OPERATOR	13	@,
	OPERATOR	14	~,
	OPERATOR	20	@@ (_int4, query_int),
	FUNCTION	1	g_intbig_consistent (internal, _int4, smallint, oid, internal),
	FUNCTION	2	g_intbig_union (internal, internal),
	FUNCTION	3	g_intbig_compress (internal),
	FUNCTION	4	g_intbig_decompress (internal),
	FUNCTION	5	g_intbig_penalty (internal, internal, internal),
	FUNCTION	6	g_intbig_picksplit (internal, internal),
	FUNCTION	7	g_intbig_same (intbig_gkey, intbig_gkey, internal),
	STORAGE		intbig_gkey
;
--
-- CREATE_TRANSFORM
--

-- Create a dummy transform
-- The function FROM SQL should have internal as single argument as well
-- as return type. The function TO SQL should have as single argument
-- internal and as return argument the datatype of the transform done.
-- We choose some random built-in functions that have the right signature.
-- This won't actually be used, because the SQL function language
-- doesn't implement transforms (there would be no point).
CREATE TRANSFORM FOR int LANGUAGE SQL (
    FROM SQL WITH FUNCTION prsd_lextype(internal),
    TO SQL WITH FUNCTION int4recv(internal))
;
alter type alter1.ctype set schema alter2
;
create unlogged table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) partition by hash (aid) tablespace "regress_pgbench_tap_1_ts"
;
alter table atacc1 alter a set storage plain
;
CREATE FUNCTION ok_cmp (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT
			CASE WHEN $1 < $2 THEN -1
				 WHEN $1 > $2 THEN  1
				 ELSE 0
			END;
	$$
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE xmltest2(x xml, _path text)
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA import_source EXCEPT (t1, "x 4", nonesuch)
  FROM SERVER loopback INTO import_dest4
;
----------
[BLOCK] do $$
begin
  assert 1=0;  -- won't be tested
end;
$$
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "fkpart3"."pk2" DROP COLUMN "........pg.dropped.1........"
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA split_import_src LIMIT TO (import_source) FROM SERVER split_pg_import INTO split_import_dst OPTIONS (import_collate 'false', import_default 'false')
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30635 RENAME TO reind_con_toast
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS bar
;
----------
[CREATE_POLICY] --
-- create default conversion with qualified name
--
CREATE DEFAULT CONVERSION public.mydef FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[ADD_INDEX] CREATE INDEX "btree_idx1" ON "public"."btree_test" USING "btree" ("a") WITH ("vacuum_cleanup_index_scale_factor"='70.0')
;
----------
[DROP_PROG_OBJ] DROP FUNCTION leaker_1(bool)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."rainbow" ADD VALUE 'yellow'
;
----------
[CREATE_TYPE] create domain posint as int check (value > 0)
;
----------
[TRUNCATE_TABLE] -- 0

-- truncate with ONLY clause
-- Since ONLY is specified, the table tru_ftable_child that inherits
-- tru_ftable_parent locally is not truncated.
TRUNCATE ONLY tru_ftable_parent
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER IF EXISTS split_event_probe_renamed
;
----------
[DROP_TABLE] DROP TABLE vacparted_i
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=31170 host=/tmp/eViv7QVsoS dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (streaming = on, two_phase = on)
;
----------
[DROP_TABLE] drop table parted_copytest
;
----------
[CREATE_PROG_OBJ] /* contrib/btree_gist/btree_gist--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Add support for indexing UUID columns

-- define the GiST support methods
CREATE FUNCTION gbt_uuid_consistent(internal,uuid,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE spc_commit (id serial PRIMARY KEY, id2 int)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY intdict (ABSVAL = true)
;
----------
[DROP_TABLE] DROP TABLE pt, ref
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table utr1 drop column q
;
----------
[CREATE_TABLE] -- check some of the allowed relkinds
create table regular_table (a int, b text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart1 for values from (1) to (2)
;
----------
[CREATE_TABLE] -- check that a BR constraint can't make partition contain violating rows
create table brtrigpartcon (a int, b text) partition by list (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW nontemp2 AS SELECT * FROM t1 INNER JOIN t2 ON t1.num = t2.num2
;
----------
[CREATE_TABLE] CREATE TABLE clstrpart33 PARTITION OF clstrpart3 DEFAULT
;
----------
[CREATE_TABLE] /*
 * 1.test CREATE INDEX
 *
 * Deliberately avoid dropping objects in this section, to get some pg_dump
 * coverage.
 */

CREATE TABLE tbl_include_reg (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_C
	CONNECTION 'port=14114 host=/tmp/vJYivPRhPX dbname=postgres application_name=tap_sub_C'
	PUBLICATION tap_pub_B
	WITH (two_phase = on)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION retset(int)
;
----------
[ALTER_POLICY] COMMENT ON RULE rtest_v1_bad ON rtest_v1 IS 'bad rule'
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_aggregate
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR === (
    LEFTARG = int,
    RIGHTARG = int,
    PROCEDURE = int4eq,
    COMMUTATOR = ===
)
;
----------
[CREATE_PROG_OBJ] create function pl_qual_names (param1 int) returns void as $$
<<outerblock>>
declare
  param1 int := 1;
begin
  <<innerblock>>
  declare
    param1 int := 2;
  begin
    raise notice 'param1 = %', param1;
    raise notice 'pl_qual_names.param1 = %', pl_qual_names.param1;
    raise notice 'outerblock.param1 = %', outerblock.param1;
    raise notice 'innerblock.param1 = %', innerblock.param1;
  end;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] create or replace function whoami() returns trigger language plpgsql
as $$
begin
  raise notice 'I am %', current_user;
  perform 1 / 0;
  return null;
end;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- check that the table being attached exists
ALTER TABLE list_parted ATTACH PARTITION nonexistent FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE split_alter18.parent_table (id integer PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_ne(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_foreign_table ADD PRIMARY KEY USING INDEX pg_foreign_table_relid_index
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_opfamily
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."consumes_rw_array"(integer[]) RETURNS integer
    LANGUAGE "plpgsql" STABLE
    AS $_$
  begin return $1[1]; end;
$_$
;
----------
[CREATE_PROG_OBJ] create function stable_one() returns int as $$ begin return 1; end; $$ language plpgsql stable
;
----------
[DROP_TABLE] DROP TABLE reindex_inj.tbl
;
----------
[CREATE_TABLE] CREATE TABLE public.tab1_child2 PARTITION OF sch1.tab1_parent FOR VALUES IN (4, 5, 6)
;
----------
[CREATE_PROG_OBJ] -- register the opclass for indexing (not as default)

CREATE OPERATOR CLASS gist__intbig_ops
FOR TYPE _int4 USING gist
AS
	OPERATOR	3	&&,
	OPERATOR	6	= (anyarray, anyarray),
	OPERATOR	7	@>,
	OPERATOR	8	<@,
	OPERATOR	13	@,
	OPERATOR	14	~,
	OPERATOR	20	@@ (_int4, query_int),
	FUNCTION	1	g_intbig_consistent (internal, _int4, smallint, oid, internal),
	FUNCTION	2	g_intbig_union (internal, internal),
	FUNCTION	3	g_intbig_compress (internal),
	FUNCTION	4	g_intbig_decompress (internal),
	FUNCTION	5	g_intbig_penalty (internal, internal, internal),
	FUNCTION	6	g_intbig_picksplit (internal, internal),
	FUNCTION	7	g_intbig_same (intbig_gkey, intbig_gkey, internal),
	STORAGE		intbig_gkey
;
----------
[CREATE_TRANSFORM] --
-- CREATE_TRANSFORM
--

-- Create a dummy transform
-- The function FROM SQL should have internal as single argument as well
-- as return type. The function TO SQL should have as single argument
-- internal and as return argument the datatype of the transform done.
-- We choose some random built-in functions that have the right signature.
-- This won't actually be used, because the SQL function language
-- doesn't implement transforms (there would be no point).
CREATE TRANSFORM FOR int LANGUAGE SQL (
    FROM SQL WITH FUNCTION prsd_lextype(internal),
    TO SQL WITH FUNCTION int4recv(internal))
;
----------
[ALTER_TYPE] alter type alter1.ctype set schema alter2
;
----------
[CREATE_TABLE] create unlogged table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) partition by hash (aid) tablespace "regress_pgbench_tap_1_ts"
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter a set storage plain
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ok_cmp (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT
			CASE WHEN $1 < $2 THEN -1
				 WHEN $1 > $2 THEN  1
				 ELSE 0
			END;
	$$
;
