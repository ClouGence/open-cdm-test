--
-- Test triggers on a join view
--
CREATE TABLE country_table (
    country_id        serial primary key,
    country_name    text unique not null,
    continent        text not null
)
;
ALTER TABLE pg_toast.pg_toast_30403 RENAME TO reind_con_toast
;
ALTER TABLE ONLY "public"."gtest_parent" ATTACH PARTITION "public"."gtest_child" FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
-- test schema behavior
CREATE SCHEMA mvtest_mvschema
;
CREATE FOREIGN TABLE foreign_tbl2 () INHERITS (foreign_tbl)
  SERVER loopback OPTIONS (table_name 'base_tbl')
;
create function anyarray_anyrange_func(a anyarray, r anyrange)
  returns anyelement as 'select $1[1] + lower($2);' language sql
;
DROP FOREIGN TABLE fd_pt2_1
;
ALTER TABLE pg_toast.pg_toast_28509 RENAME TO reind_con_toast
;
CREATE TABLE test3bpci (x char(3) COLLATE case_insensitive)
;
-- in text mode, \. must be alone on its line
truncate copytest2
;
DROP TABLESPACE test_tablespace
;
CREATE TABLE toasted_several (
    id serial unique not null,
    toasted_key text primary key,
    toasted_col1 text,
    toasted_col2 text
)
;
CREATE TABLE ploc1 (f1 int, f2 text)
;
drop function test_evtrig_no_rewrite()
;
CREATE TABLE notnull_part1_3_upg (a int, b int)
;
create table loct3 (f1 text collate "C" unique, f2 text, f3 varchar(10) unique)
;
CREATE TABLE tab6 (a int PRIMARY KEY, b int, c int, d int)
;
CREATE FUNCTION f3 () RETURNS text
    AS 'BEGIN
          RAISE EXCEPTION ''an exception from f3()'';
          RETURN NULL;
        END;' LANGUAGE plpgsql
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_range
;
ALTER SEQUENCE "public"."clstr_tst_s_rf_a_seq" OWNED BY "public"."clstr_tst_s"."rf_a"
;
create temp table master(f1 int primary key)
;
CREATE FUNCTION mylt_noninline (text, text) RETURNS boolean LANGUAGE sql
    AS $$ select $1 < $2 limit 1 $$
;
ALTER OPERATOR FAMILY gist_inet_ops USING gist ADD
	FUNCTION	11  (inet, inet) gbt_inet_sortsupport (internal)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
ALTER TABLE pg_toast.pg_toast_30443 RENAME TO reind_con_toast
;
CREATE TABLE itest18 (a int NOT NULL, b text)
;
ANALYZE pg_temp_95.pg_temp_33804
;
VACUUM maintain_test
;
ALTER INDEX pg_toast.pg_toast_30910_index RENAME TO reind_con_toast_idx
;
ALTER FUNCTION float4_dist(real, real) PARALLEL SAFE
;
CREATE TABLE a4 ()
;
CREATE POLICY p3 ON current_check FOR UPDATE USING (currentid = 4) WITH CHECK (rlsuser = current_user)
;
create table parted_isvalid_tab_2 partition of parted_isvalid_tab
  for values from (10) to (20) partition by range (a)
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=29288 host=/tmp/9b_KYkJ1Ka dbname=postgres' PUBLICATION mypub
;
CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION "public"
;
truncate table src
;
-- Test stand-alone composite type

CREATE TYPE default_test_row AS (f1 text_w_default, f2 int42)
;
-- plpgsql, proretset = f, prorettype = b
CREATE FUNCTION getrngfunc8(int) RETURNS int AS 'DECLARE rngfuncint int; BEGIN SELECT rngfuncid into rngfuncint FROM rngfunc WHERE rngfuncid = $1; RETURN rngfuncint; END;' LANGUAGE plpgsql
;
-- trigger
DROP TRIGGER test_trigger_exists ON test_exists
;
DROP TABLE testcomment
;
-- Test rescan paths (cf. bug #15378)
-- use box and && rather than point, so that rescan happens when the
-- traverse stack is non-empty

create table spgist_box_tbl(id serial, b box)
;
create index ab_a3_b2_a_idx on ab_a3_b2 (a)
;
CREATE TABLE "public"."brin_summarize" (
    "value" integer
)
WITH ("fillfactor"='10', "autovacuum_enabled"='false')
;
-- Synonym dictionary
CREATE TEXT SEARCH DICTIONARY synonym (
						Template=synonym,
						Synonyms=synonym_sample
)
;
create table idxpart_another (a int, b int, primary key (a, b)) partition by range (a)
;
/*
 * 5.23
 * CONSTRAINT_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 5.24
 * CONSTRAINT_TABLE_USAGE view
 */

CREATE VIEW constraint_table_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name

    FROM pg_constraint c, pg_namespace nc,
         pg_class r, pg_namespace nr

    WHERE c.connamespace = nc.oid AND r.relnamespace = nr.oid
          AND ( (c.contype = 'f' AND c.confrelid = r.oid)
             OR (c.contype IN ('p', 'u') AND c.conrelid = r.oid) )
          AND r.relkind IN ('r', 'p')
          AND pg_has_role(r.relowner, 'USAGE')
;
CREATE TABLE am_partitioned_6p1 PARTITION OF am_partitioned_6p
  FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
CREATE CAST ("text" AS "public"."casttesttype") WITHOUT FUNCTION AS IMPLICIT
;
CREATE SUBSCRIPTION tap_sub_rep_full CONNECTION 'port=17075 host=/tmp/5nD52R1liU dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_rep_full
;
create trigger tg_slotlink_a after insert or update or delete
    on PSlot for each row execute procedure tg_slotlink_a('PS')
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] --
-- Test triggers on a join view
--
CREATE TABLE country_table (
    country_id        serial primary key,
    country_name    text unique not null,
    continent        text not null
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30403 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."gtest_parent" ATTACH PARTITION "public"."gtest_child" FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[CREATE_SCHEMA] -- test schema behavior
CREATE SCHEMA mvtest_mvschema
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE foreign_tbl2 () INHERITS (foreign_tbl)
  SERVER loopback OPTIONS (table_name 'base_tbl')
;
----------
[CREATE_PROG_OBJ] create function anyarray_anyrange_func(a anyarray, r anyrange)
  returns anyelement as 'select $1[1] + lower($2);' language sql
;
----------
[DROP_TABLE] DROP FOREIGN TABLE fd_pt2_1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28509 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE test3bpci (x char(3) COLLATE case_insensitive)
;
----------
[TRUNCATE_TABLE] -- in text mode, \. must be alone on its line
truncate copytest2
;
----------
[DROP_TABLESPACE] DROP TABLESPACE test_tablespace
;
----------
[CREATE_TABLE] CREATE TABLE toasted_several (
    id serial unique not null,
    toasted_key text primary key,
    toasted_col1 text,
    toasted_col2 text
)
;
----------
[CREATE_TABLE] CREATE TABLE ploc1 (f1 int, f2 text)
;
----------
[DROP_PROG_OBJ] drop function test_evtrig_no_rewrite()
;
----------
[CREATE_TABLE] CREATE TABLE notnull_part1_3_upg (a int, b int)
;
----------
[CREATE_TABLE] create table loct3 (f1 text collate "C" unique, f2 text, f3 varchar(10) unique)
;
----------
[CREATE_TABLE] CREATE TABLE tab6 (a int PRIMARY KEY, b int, c int, d int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f3 () RETURNS text
    AS 'BEGIN
          RAISE EXCEPTION ''an exception from f3()'';
          RETURN NULL;
        END;' LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_range
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."clstr_tst_s_rf_a_seq" OWNED BY "public"."clstr_tst_s"."rf_a"
;
----------
[CREATE_TABLE] create temp table master(f1 int primary key)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION mylt_noninline (text, text) RETURNS boolean LANGUAGE sql
    AS $$ select $1 < $2 limit 1 $$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_inet_ops USING gist ADD
	FUNCTION	11  (inet, inet) gbt_inet_sortsupport (internal)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30443 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE itest18 (a int NOT NULL, b text)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_95.pg_temp_33804
;
----------
[ADMIN_TABLE] VACUUM maintain_test
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30910_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION float4_dist(real, real) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE a4 ()
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON current_check FOR UPDATE USING (currentid = 4) WITH CHECK (rlsuser = current_user)
;
----------
[CREATE_TABLE] create table parted_isvalid_tab_2 partition of parted_isvalid_tab
  for values from (10) to (20) partition by range (a)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=29288 host=/tmp/9b_KYkJ1Ka dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION "public"
;
----------
[TRUNCATE_TABLE] truncate table src
;
----------
[CREATE_TYPE] -- Test stand-alone composite type

CREATE TYPE default_test_row AS (f1 text_w_default, f2 int42)
;
----------
[CREATE_PROG_OBJ] -- plpgsql, proretset = f, prorettype = b
CREATE FUNCTION getrngfunc8(int) RETURNS int AS 'DECLARE rngfuncint int; BEGIN SELECT rngfuncid into rngfuncint FROM rngfunc WHERE rngfuncid = $1; RETURN rngfuncint; END;' LANGUAGE plpgsql
;
----------
[DROP_TRIGGER] -- trigger
DROP TRIGGER test_trigger_exists ON test_exists
;
----------
[DROP_TABLE] DROP TABLE testcomment
;
----------
[CREATE_TABLE] -- Test rescan paths (cf. bug #15378)
-- use box and && rather than point, so that rescan happens when the
-- traverse stack is non-empty

create table spgist_box_tbl(id serial, b box)
;
----------
[ADD_INDEX] create index ab_a3_b2_a_idx on ab_a3_b2 (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brin_summarize" (
    "value" integer
)
WITH ("fillfactor"='10', "autovacuum_enabled"='false')
;
----------
[CREATE_POLICY] -- Synonym dictionary
CREATE TEXT SEARCH DICTIONARY synonym (
						Template=synonym,
						Synonyms=synonym_sample
)
;
----------
[CREATE_TABLE] create table idxpart_another (a int, b int, primary key (a, b)) partition by range (a)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.23
 * CONSTRAINT_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 5.24
 * CONSTRAINT_TABLE_USAGE view
 */

CREATE VIEW constraint_table_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name

    FROM pg_constraint c, pg_namespace nc,
         pg_class r, pg_namespace nr

    WHERE c.connamespace = nc.oid AND r.relnamespace = nr.oid
          AND ( (c.contype = 'f' AND c.confrelid = r.oid)
             OR (c.contype IN ('p', 'u') AND c.conrelid = r.oid) )
          AND r.relkind IN ('r', 'p')
          AND pg_has_role(r.relowner, 'USAGE')
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned_6p1 PARTITION OF am_partitioned_6p
  FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
----------
[CREATE_PROG_OBJ] CREATE CAST ("text" AS "public"."casttesttype") WITHOUT FUNCTION AS IMPLICIT
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_rep_full CONNECTION 'port=17075 host=/tmp/5nD52R1liU dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_rep_full
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_slotlink_a after insert or update or delete
    on PSlot for each row execute procedure tg_slotlink_a('PS')
;
