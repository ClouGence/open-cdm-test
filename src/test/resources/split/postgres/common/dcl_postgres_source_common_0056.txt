--
-- Cursors
--

-- These tests require track_utility to be enabled.
SET pg_stat_statements.track_utility = TRUE
;
EXECUTE dumpTableAttach('37724')
;
CREATE ROLE regress_dblink_user
;
REVOKE ALL ON TABLE tbl1 FROM regress_rls_eve
;
/*
 * This test is for Linux/glibc systems and others that implement proper
 * locale classification of Unicode characters with high code values.
 * It must be run in a database with UTF8 encoding and a Unicode-aware locale.
 */

SET client_encoding TO UTF8
;
REVOKE USAGE ON SCHEMA schema_op1 FROM regress_rol_op1
;
EXECUTE dumpOpr('1502')
;
ALTER TABLE public.sequence_test9 OWNER TO zyc
;
COPY pg_catalog.pg_operator (oid, oprname, oprnamespace, oprowner, oprkind, oprcanmerge, oprcanhash, oprleft, oprright, oprresult, oprcom, oprnegate, oprcode, oprrest, oprjoin) TO stdout
;
SET ROLE regress_priv_user3
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] --
-- Cursors
--

-- These tests require track_utility to be enabled.
SET pg_stat_statements.track_utility = TRUE
;
----------
[UNSAFE] EXECUTE dumpTableAttach('37724')
;
----------
[CREATE_ROLE] CREATE ROLE regress_dblink_user
;
----------
[REVOKE] REVOKE ALL ON TABLE tbl1 FROM regress_rls_eve
;
----------
[SESSION_SETTING_WRITE] /*
 * This test is for Linux/glibc systems and others that implement proper
 * locale classification of Unicode characters with high code values.
 * It must be run in a database with UTF8 encoding and a Unicode-aware locale.
 */

SET client_encoding TO UTF8
;
----------
[REVOKE] REVOKE USAGE ON SCHEMA schema_op1 FROM regress_rol_op1
;
----------
[UNSAFE] EXECUTE dumpOpr('1502')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.sequence_test9 OWNER TO zyc
;
----------
[DATA_EXPORT] COPY pg_catalog.pg_operator (oid, oprname, oprnamespace, oprowner, oprkind, oprcanmerge, oprcanhash, oprleft, oprright, oprresult, oprcom, oprnegate, oprcode, oprrest, oprjoin) TO stdout
;
----------
[SWITCH_ROLE] SET ROLE regress_priv_user3
;
