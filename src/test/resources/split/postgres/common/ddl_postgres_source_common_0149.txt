-- User defined function for user defined aggregate, VARIADIC
create function least_accum(anyelement, variadic anyarray)
returns anyelement language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
CALL ptest9(1/0)
;
DROP TABLE IF EXISTS stream_test1
;
create unique index on parallel_mat_view(length)
;
-- Create and simple test thesaurus dictionary
-- More tests in configuration checks because ts_lexize()
-- cannot pass more than one word to thesaurus.
CREATE TEXT SEARCH DICTIONARY thesaurus (
                        Template=thesaurus,
						DictFile=thesaurus_sample,
						Dictionary=english_stem
)
;
CREATE INDEX iprt1_p1_a on prt1_p1(a)
;
DROP FUNCTION postgres_fdw_get_connections ()
;
alter table at_tab1 alter column b type varchar
;
do $$ declare x foo%rowtype; begin end $$
;
drop table parted_tab
;
CREATE TABLE "public"."identity_dump_logged" (
    "a" integer NOT NULL
)
;
create table pp_lp (a int, value int) partition by list (a)
;
COMMENT ON TEXT SEARCH DICTIONARY intdict IS 'dictionary for integers'
;
ALTER TABLE generated_stored_tests.gtest23b ADD CONSTRAINT gtest23b_b_fkey FOREIGN KEY (b) REFERENCES gtest23a(x)
;
CREATE TABLE t11 (d text) INHERITS (t1)
;
CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
  SELECT 1 / 0
;
create function dfunc(a variadic int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
COMMENT ON FUNCTION seg_over_left(seg, seg) IS
'overlaps or is left of'
;
drop function dfunc(int, int, int, int)
;
CREATE TABLE part_5_a PARTITION OF part_5 FOR VALUES IN ('a')
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_default_acl
;
create table test_part_coll_cast partition of test_part_coll_posix for values from (name 'm' collate "POSIX") to ('s')
;
-- Add range column and UNIQUE constraint at the same time
CREATE TABLE temporal3 (
  id int4range
)
;
ALTER TABLE pg_parameter_acl ADD UNIQUE USING INDEX pg_parameter_acl_parname_index
;
CREATE TABLE testcomment (i int)
;
DROP SCHEMA stats_import CASCADE
;
VACUUM (ANALYZE) pg_catalog.pg_database
;
ALTER PUBLICATION testpub_forparted SET (publish_via_partition_root = true)
;
create function bogus_int8_text_eq(int8, text) returns boolean
language sql as 'select $1::text = $2'
;
CREATE TABLE split_pub18_b.audit_log (
    id integer PRIMARY KEY,
    action text NOT NULL
)
;
-- This should succeed, even though they are different types,
-- because int=int8 exists and is a member of the integer opfamily
CREATE TABLE FKTABLE (ftest1 int8 REFERENCES pktable)
;
CREATE TABLE rls_test_both (
    username        name,
    supervisor      name,
    data            integer
)
;
CREATE TEMP TABLE pg_temp_10.pg_temp_25340_2 (tid pg_catalog.tid)
;
CREATE TABLE alpha_pos PARTITION OF alpha FOR VALUES FROM (0) TO (10.0) PARTITION BY LIST (c)
;
CREATE FUNCTION gbt_macad_union(internal, internal)
RETURNS gbtreekey16
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE prt2
;
CREATE FUNCTION make_valid(issn13)
	RETURNS issn13
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE big5_inputs (inbytes bytea, description text)
;
CREATE TABLE testxmlschema.test1 (a int, b text)
;
--
-- OpFamily and OpClass
--
CREATE OPERATOR FAMILY alt_opf1 USING hash
;
ALTER INDEX pg_toast.pg_toast_30635_index RENAME TO reind_con_toast_idx
;
create table p (a int, b int) partition by range (a, b)
;
-- WITH attached to inherited UPDATE or DELETE

CREATE TEMP TABLE parent ( id int, val text )
;
ALTER FUNCTION gbt_date_union(internal, internal) PARALLEL SAFE
;
create function retc(int) returns two_int8s language plpgsql as
$$ begin return row($1,1)::two_int8s; end $$
;
-- JOIN query

CREATE TABLE pagg_tab1(x int, y int) PARTITION BY RANGE(x)
;
DROP EXTENSION plperl
;
COMMENT ON LARGE OBJECT 39105 IS 'I Wandered Lonely as a Cloud'
;
ALTER TABLE pg_temp_26.pg_temp_28381_2 ADD COLUMN newdata pg_temp_26.pg_temp_28381
;
ALTER TABLE view_base_table DROP CONSTRAINT view_base_table_pkey
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- User defined function for user defined aggregate, VARIADIC
create function least_accum(anyelement, variadic anyarray)
returns anyelement language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
----------
[CALL_PROG_OBJ] CALL ptest9(1/0)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS stream_test1
;
----------
[ADD_INDEX] create unique index on parallel_mat_view(length)
;
----------
[CREATE_POLICY] -- Create and simple test thesaurus dictionary
-- More tests in configuration checks because ts_lexize()
-- cannot pass more than one word to thesaurus.
CREATE TEXT SEARCH DICTIONARY thesaurus (
                        Template=thesaurus,
						DictFile=thesaurus_sample,
						Dictionary=english_stem
)
;
----------
[ADD_INDEX] CREATE INDEX iprt1_p1_a on prt1_p1(a)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION postgres_fdw_get_connections ()
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table at_tab1 alter column b type varchar
;
----------
[BLOCK] do $$ declare x foo%rowtype; begin end $$
;
----------
[DROP_TABLE] drop table parted_tab
;
----------
[CREATE_TABLE] CREATE TABLE "public"."identity_dump_logged" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TABLE] create table pp_lp (a int, value int) partition by list (a)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY intdict IS 'dictionary for integers'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE generated_stored_tests.gtest23b ADD CONSTRAINT gtest23b_b_fkey FOREIGN KEY (b) REFERENCES gtest23a(x)
;
----------
[CREATE_TABLE] CREATE TABLE t11 (d text) INHERITS (t1)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
  SELECT 1 / 0
;
----------
[CREATE_PROG_OBJ] create function dfunc(a variadic int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_over_left(seg, seg) IS
'overlaps or is left of'
;
----------
[DROP_PROG_OBJ] drop function dfunc(int, int, int, int)
;
----------
[CREATE_TABLE] CREATE TABLE part_5_a PARTITION OF part_5 FOR VALUES IN ('a')
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] create table test_part_coll_cast partition of test_part_coll_posix for values from (name 'm' collate "POSIX") to ('s')
;
----------
[CREATE_TABLE] -- Add range column and UNIQUE constraint at the same time
CREATE TABLE temporal3 (
  id int4range
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_parameter_acl ADD UNIQUE USING INDEX pg_parameter_acl_parname_index
;
----------
[CREATE_TABLE] CREATE TABLE testcomment (i int)
;
----------
[DROP_SCHEMA] DROP SCHEMA stats_import CASCADE
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_database
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_forparted SET (publish_via_partition_root = true)
;
----------
[CREATE_PROG_OBJ] create function bogus_int8_text_eq(int8, text) returns boolean
language sql as 'select $1::text = $2'
;
----------
[CREATE_TABLE] CREATE TABLE split_pub18_b.audit_log (
    id integer PRIMARY KEY,
    action text NOT NULL
)
;
----------
[CREATE_TABLE] -- This should succeed, even though they are different types,
-- because int=int8 exists and is a member of the integer opfamily
CREATE TABLE FKTABLE (ftest1 int8 REFERENCES pktable)
;
----------
[CREATE_TABLE] CREATE TABLE rls_test_both (
    username        name,
    supervisor      name,
    data            integer
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_25340_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE alpha_pos PARTITION OF alpha FOR VALUES FROM (0) TO (10.0) PARTITION BY LIST (c)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad_union(internal, internal)
RETURNS gbtreekey16
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE prt2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_valid(issn13)
	RETURNS issn13
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE big5_inputs (inbytes bytea, description text)
;
----------
[CREATE_TABLE] CREATE TABLE testxmlschema.test1 (a int, b text)
;
----------
[CREATE_PROG_OBJ] --
-- OpFamily and OpClass
--
CREATE OPERATOR FAMILY alt_opf1 USING hash
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30635_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table p (a int, b int) partition by range (a, b)
;
----------
[CREATE_TABLE] -- WITH attached to inherited UPDATE or DELETE

CREATE TEMP TABLE parent ( id int, val text )
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_date_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function retc(int) returns two_int8s language plpgsql as
$$ begin return row($1,1)::two_int8s; end $$
;
----------
[CREATE_TABLE] -- JOIN query

CREATE TABLE pagg_tab1(x int, y int) PARTITION BY RANGE(x)
;
----------
[DROP_LIBRARY] DROP EXTENSION plperl
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 39105 IS 'I Wandered Lonely as a Cloud'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_26.pg_temp_28381_2 ADD COLUMN newdata pg_temp_26.pg_temp_28381
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE view_base_table DROP CONSTRAINT view_base_table_pkey
;
