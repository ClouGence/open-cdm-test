--fail
alter table dropColumnExists drop column if exists non_existing
;
CREATE TABLE fk_partitioned_fk_4_1 PARTITION OF fk_partitioned_fk_4 FOR VALUES FROM (1,1) TO (100,100)
;
VACUUM (PARALLEL 2) information_schema.sql_sizing
;
-- fails, no way to deduce outputs
CREATE FUNCTION bad (f1 int, out f2 anyelement, out f3 anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
CREATE FUNCTION gbt_cash_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_statistic
;
drop function parallel_safe_volatile(int)
;
-- Storage parameters

CREATE TABLE storage (
    id INT
) WITH (
    fillfactor = 10,
    autovacuum_enabled = FALSE
)
;
CREATE MATERIALIZED VIEW "public"."mvtest_tvvm" AS
 SELECT "mvtest_tvv"."grandtot"
   FROM "public"."mvtest_tvv"
  WITH NO DATA
;
-- fail, cube requires it

drop extension cube cascade
;
ALTER TABLE pg_toast.pg_toast_30547 RENAME TO reind_con_toast
;
drop view my_locks
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_publication_namespace
;
CREATE INDEX t1_hash ON s5.t1 USING HASH (i)
;
DROP TABLE pg_temp_95.pg_temp_33804_2, pg_temp_95.pg_temp_33804
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest0"
    ADD CONSTRAINT "gtest0_pkey" PRIMARY KEY ("a")
;
ALTER TABLE public.fk_notpartitioned_pk ADD CONSTRAINT fk_notpartitioned_pk_pkey PRIMARY KEY (a)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_subscription_rel
;
do $$
declare d ordered_texts;
begin
  for d in select a, b from sometable loop
    raise notice 'succeeded at "%"', d.f1;
  end loop;
end$$
;
CREATE VIEW "public"."iexit" AS
 SELECT "ih"."name",
    "ih"."thepath",
    "public"."interpt_pp"("ih"."thepath", "r"."thepath") AS "exit"
   FROM "public"."ihighway" "ih",
    "public"."ramp" "r"
  WHERE ("ih"."thepath" OPERATOR("public".##) "r"."thepath")
;
-- failed

--
-- Permissions to execute functions
--
CREATE TABLE t1 (x int, y text)
;
-- match failure

create function anycompatiblearray_anycompatiblerange_func(a anycompatiblearray, r anycompatiblerange)
  returns anycompatible as 'select $1[1] + lower($2);' language sql
;
create function error_trap_test() returns text as $$
begin
  perform zero_divide();
  return 'no error detected!';
exception when division_by_zero then
  return 'division_by_zero detected';
end;
$$ language plpgsql parallel safe
;
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_3 FOR VALUES IN (2500,2501,2502,2503)
;
ANALYZE public.customer
;
-- matview rewrite when changing access method
CREATE MATERIALIZED VIEW heapmv USING heap AS SELECT 1 AS a
;
CREATE TABLE test_tab3 (a int, b int, c int)
;
CREATE TABLE sro_tab (a int)
;
CREATE FUNCTION pg_check_frozen(regclass, t_ctid OUT tid)
RETURNS SETOF tid
AS '$libdir/pg_visibility', 'pg_check_frozen'
LANGUAGE C STRICT
;
--
-- ON CONFLICT: ranges
--

TRUNCATE temporal_rng
;
CREATE FUNCTION cube_recv(internal)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- DDL

create temporary table hstemp1 (col1 integer)
;
CREATE TABLE notnull_tbl2 (a INTEGER PRIMARY KEY)
;
CREATE INDEX t2_gin ON s1.t2 USING GIN (ia)
;
CREATE TABLE target (tid integer, balance integer)
  WITH (autovacuum_enabled=off)
;
CREATE FUNCTION isnlt(ean13, isbn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- check display of whole-row variables in some corner cases

create type nestedcomposite as (x int8_tbl)
;
-- sql, proretset = t, prorettype = b
CREATE FUNCTION getrngfunc2(int) RETURNS setof int AS 'SELECT rngfuncid FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
CREATE FUNCTION pg_get_wal_block_info(IN start_lsn pg_lsn,
	IN end_lsn pg_lsn,
	IN show_data boolean DEFAULT true,
	OUT start_lsn pg_lsn,
	OUT end_lsn pg_lsn,
	OUT prev_lsn pg_lsn,
	OUT block_id int2,
	OUT reltablespace oid,
	OUT reldatabase oid,
	OUT relfilenode oid,
	OUT relforknumber int2,
	OUT relblocknumber int8,
	OUT xid xid,
	OUT resource_manager text,
	OUT record_type text,
	OUT record_length int4,
	OUT main_data_length int4,
	OUT block_data_length int4,
	OUT block_fpi_length int4,
	OUT block_fpi_info text[],
	OUT description text,
	OUT block_data bytea,
	OUT block_fpi_data bytea
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_block_info'
LANGUAGE C STRICT PARALLEL SAFE
;
-- cleanup
DROP TABLE gin_check_jsonb
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest20a"
    ADD CONSTRAINT "gtest20a_pkey" PRIMARY KEY ("a")
;
CREATE VIEW pg_stat_progress_analyze AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        CAST(S.relid AS oid) AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'acquiring sample rows'
                      WHEN 2 THEN 'acquiring inherited sample rows'
                      WHEN 3 THEN 'computing statistics'
                      WHEN 4 THEN 'computing extended statistics'
                      WHEN 5 THEN 'finalizing analyze'
                      END AS phase,
        S.param2 AS sample_blks_total,
        S.param3 AS sample_blks_scanned,
        S.param4 AS ext_stats_total,
        S.param5 AS ext_stats_computed,
        S.param6 AS child_tables_total,
        S.param7 AS child_tables_done,
        CAST(S.param8 AS oid) AS current_child_table_relid
    FROM pg_stat_get_progress_info('ANALYZE') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
-- fail

DROP VIEW fdv4
;
drop function forc01()
;
CREATE OR REPLACE FUNCTION func_parted_mod_b() returns trigger as $$
BEGIN
   raise notice 'Trigger: Got OLD row %, but returning NULL', OLD;
   return NULL;
END $$ LANGUAGE plpgsql
;
create table test_range_spgist(ir int4range)
;
create table lp_ef partition of lp for values in ('e', 'f')
;
CREATE TABLE t1 AS
SELECT 1::int AS a , 2::int AS b
FROM generate_series(1,20) q
;
CREATE TEMP TABLE temptest2(col int REFERENCES temptest1)
  ON COMMIT DELETE ROWS
;
drop table loct2
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] --fail
alter table dropColumnExists drop column if exists non_existing
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_4_1 PARTITION OF fk_partitioned_fk_4 FOR VALUES FROM (1,1) TO (100,100)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) information_schema.sql_sizing
;
----------
[CREATE_PROG_OBJ] -- fails, no way to deduce outputs
CREATE FUNCTION bad (f1 int, out f2 anyelement, out f3 anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_cash_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_statistic
;
----------
[DROP_PROG_OBJ] drop function parallel_safe_volatile(int)
;
----------
[CREATE_TABLE] -- Storage parameters

CREATE TABLE storage (
    id INT
) WITH (
    fillfactor = 10,
    autovacuum_enabled = FALSE
)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_tvvm" AS
 SELECT "mvtest_tvv"."grandtot"
   FROM "public"."mvtest_tvv"
  WITH NO DATA
;
----------
[DROP_LIBRARY] -- fail, cube requires it

drop extension cube cascade
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30547 RENAME TO reind_con_toast
;
----------
[DROP_VIEW] drop view my_locks
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_publication_namespace
;
----------
[ADD_INDEX] CREATE INDEX t1_hash ON s5.t1 USING HASH (i)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_95.pg_temp_33804_2, pg_temp_95.pg_temp_33804
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest0"
    ADD CONSTRAINT "gtest0_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.fk_notpartitioned_pk ADD CONSTRAINT fk_notpartitioned_pk_pkey PRIMARY KEY (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_subscription_rel
;
----------
[BLOCK] do $$
declare d ordered_texts;
begin
  for d in select a, b from sometable loop
    raise notice 'succeeded at "%"', d.f1;
  end loop;
end$$
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."iexit" AS
 SELECT "ih"."name",
    "ih"."thepath",
    "public"."interpt_pp"("ih"."thepath", "r"."thepath") AS "exit"
   FROM "public"."ihighway" "ih",
    "public"."ramp" "r"
  WHERE ("ih"."thepath" OPERATOR("public".##) "r"."thepath")
;
----------
[CREATE_TABLE] -- failed

--
-- Permissions to execute functions
--
CREATE TABLE t1 (x int, y text)
;
----------
[CREATE_PROG_OBJ] -- match failure

create function anycompatiblearray_anycompatiblerange_func(a anycompatiblearray, r anycompatiblerange)
  returns anycompatible as 'select $1[1] + lower($2);' language sql
;
----------
[CREATE_PROG_OBJ] create function error_trap_test() returns text as $$
begin
  perform zero_divide();
  return 'no error detected!';
exception when division_by_zero then
  return 'division_by_zero detected';
end;
$$ language plpgsql parallel safe
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_3 FOR VALUES IN (2500,2501,2502,2503)
;
----------
[ADMIN_TABLE] ANALYZE public.customer
;
----------
[CREATE_VIEW(SELECT)] -- matview rewrite when changing access method
CREATE MATERIALIZED VIEW heapmv USING heap AS SELECT 1 AS a
;
----------
[CREATE_TABLE] CREATE TABLE test_tab3 (a int, b int, c int)
;
----------
[CREATE_TABLE] CREATE TABLE sro_tab (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_check_frozen(regclass, t_ctid OUT tid)
RETURNS SETOF tid
AS '$libdir/pg_visibility', 'pg_check_frozen'
LANGUAGE C STRICT
;
----------
[TRUNCATE_TABLE] --
-- ON CONFLICT: ranges
--

TRUNCATE temporal_rng
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_recv(internal)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] -- DDL

create temporary table hstemp1 (col1 integer)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl2 (a INTEGER PRIMARY KEY)
;
----------
[ADD_INDEX] CREATE INDEX t2_gin ON s1.t2 USING GIN (ia)
;
----------
[CREATE_TABLE] CREATE TABLE target (tid integer, balance integer)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ean13, isbn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TYPE] -- check display of whole-row variables in some corner cases

create type nestedcomposite as (x int8_tbl)
;
----------
[CREATE_PROG_OBJ] -- sql, proretset = t, prorettype = b
CREATE FUNCTION getrngfunc2(int) RETURNS setof int AS 'SELECT rngfuncid FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_get_wal_block_info(IN start_lsn pg_lsn,
	IN end_lsn pg_lsn,
	IN show_data boolean DEFAULT true,
	OUT start_lsn pg_lsn,
	OUT end_lsn pg_lsn,
	OUT prev_lsn pg_lsn,
	OUT block_id int2,
	OUT reltablespace oid,
	OUT reldatabase oid,
	OUT relfilenode oid,
	OUT relforknumber int2,
	OUT relblocknumber int8,
	OUT xid xid,
	OUT resource_manager text,
	OUT record_type text,
	OUT record_length int4,
	OUT main_data_length int4,
	OUT block_data_length int4,
	OUT block_fpi_length int4,
	OUT block_fpi_info text[],
	OUT description text,
	OUT block_data bytea,
	OUT block_fpi_data bytea
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_block_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE gin_check_jsonb
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest20a"
    ADD CONSTRAINT "gtest20a_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_progress_analyze AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        CAST(S.relid AS oid) AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'acquiring sample rows'
                      WHEN 2 THEN 'acquiring inherited sample rows'
                      WHEN 3 THEN 'computing statistics'
                      WHEN 4 THEN 'computing extended statistics'
                      WHEN 5 THEN 'finalizing analyze'
                      END AS phase,
        S.param2 AS sample_blks_total,
        S.param3 AS sample_blks_scanned,
        S.param4 AS ext_stats_total,
        S.param5 AS ext_stats_computed,
        S.param6 AS child_tables_total,
        S.param7 AS child_tables_done,
        CAST(S.param8 AS oid) AS current_child_table_relid
    FROM pg_stat_get_progress_info('ANALYZE') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
----------
[DROP_VIEW] -- fail

DROP VIEW fdv4
;
----------
[DROP_PROG_OBJ] drop function forc01()
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION func_parted_mod_b() returns trigger as $$
BEGIN
   raise notice 'Trigger: Got OLD row %, but returning NULL', OLD;
   return NULL;
END $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] create table test_range_spgist(ir int4range)
;
----------
[CREATE_TABLE] create table lp_ef partition of lp for values in ('e', 'f')
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE t1 AS
SELECT 1::int AS a , 2::int AS b
FROM generate_series(1,20) q
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest2(col int REFERENCES temptest1)
  ON COMMIT DELETE ROWS
;
----------
[DROP_TABLE] drop table loct2
;
