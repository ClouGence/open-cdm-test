EXECUTE dumpFunc('35287')
;
CREATE ROLE regress_sro_user
;
COPY public.num_input_test (n1) TO stdout
;
EXECUTE dumpAgg('20464')
;
copy header_copytest (c, b, a) from stdin with (header match)
;
LOCK TABLE a1 IN EXCLUSIVE MODE
;
EXECUTE dumpEnumType('16863')
;
-- The language object, but not the functions, can be owned by a non-superuser.
ALTER LANGUAGE plpgsql OWNER TO boot
;
SET synchronize_seqscans TO off
;
COPY (SELECT a FROM public.tab4) TO STDOUT
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('35287')
;
----------
[CREATE_ROLE] CREATE ROLE regress_sro_user
;
----------
[DATA_EXPORT] COPY public.num_input_test (n1) TO stdout
;
----------
[UNSAFE] EXECUTE dumpAgg('20464')
;
----------
[DATA_IMPORT] copy header_copytest (c, b, a) from stdin with (header match)
;
----------
[SESSION_LOCK] LOCK TABLE a1 IN EXCLUSIVE MODE
;
----------
[UNSAFE] EXECUTE dumpEnumType('16863')
;
----------
[TRANSFER_PRIVILEGE] -- The language object, but not the functions, can be owned by a non-superuser.
ALTER LANGUAGE plpgsql OWNER TO boot
;
----------
[SESSION_SETTING_WRITE] SET synchronize_seqscans TO off
;
----------
[DATA_EXPORT|SELECT] COPY (SELECT a FROM public.tab4) TO STDOUT
;
