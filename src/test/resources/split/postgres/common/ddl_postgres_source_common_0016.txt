alter operator family integer_ops using hash add
  function 1 hashint8alias1(int8alias1)
;
ALTER TABLE T ADD COLUMN c_big BIGINT DEFAULT 180000000000018,
              ALTER COLUMN c_small SET DEFAULT 9,
              ALTER COLUMN c_small_null SET DEFAULT 13
;
CREATE TABLE "generated_stored_tests"."gtest29" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer
)
;
create table boolpart_t partition of boolpart for values in ('true')
;
create temp table tt4x(c1 int, c2 int, c3 int)
;
ALTER OPERATOR FAMILY gist_bytea_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
-- test IF NOT EXISTS cases
CREATE SCHEMA test_ns_schema_renamed
;
CREATE TABLE mintab(c1 int)
;
ALTER INDEX pg_toast.pg_toast_33180_index RENAME TO reind_con_toast_idx
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opfamily
;
comment on constraint at_partitioned_0_id_name_key on at_partitioned_0 is 'child 0 constraint'
;
--
-- EXCLUDE constraints
--

CREATE TABLE circles (
  c1 CIRCLE,
  c2 TEXT,
  EXCLUDE USING gist
    (c1 WITH &&, (c2::circle) WITH &&)
    WHERE (circle_center(c1) <> '(0,0)')
)
;
CREATE INDEX brin_test_serial_idx ON brin_parallel_test
 USING brin (a int4_minmax_ops, a int4_bloom_ops, b, c int8_minmax_multi_ops)
  WITH (pages_per_range=7)
 WHERE NOT (a BETWEEN 1000 and 1500)
;
-- Check inheritance cases
CREATE TEMP TABLE ucchild () inherits (uctest)
;
ALTER EXTENSION pg_stat_statements DROP FUNCTION pg_stat_statements(boolean)
;
-- check some error cases, too

create or replace function rttest()
returns setof int as $$
begin
  return query select 10 into no_such_table;
end;
$$ language plpgsql
;
VACUUM parted
;
-- ok, collation is implicitly coerced
create table test_part_coll_cast partition of test_part_coll_posix for values from (name 'm' collate "C") to ('s')
;
ALTER TABLE corrupt2 SET (autovacuum_enabled=false)
;
do $$
declare cnt int := 0;
  c1 cursor for select * from forc_test;
begin
  for r1 in c1 loop
    declare c1 cursor for select * from forc_test;
    begin
      for r2 in c1 loop
        cnt := cnt + 1;
      end loop;
    end;
  end loop;
  raise notice 'cnt = %', cnt;
end $$
;
CREATE INDEX "gtest22c_pred_idx" ON "generated_stored_tests"."gtest22c" USING "btree" ("a") WHERE ("b" > 0)
;
ALTER TABLE ONLY "public"."part_7" ATTACH PARTITION "public"."part_7_a_null" FOR VALUES IN ('a', NULL)
;
VACUUM (PARALLEL 0) pg_catalog.pg_foreign_table
;
create table partitioned1
  partition of partitioned for values in ('(1,2)')
;
CREATE FUNCTION gbt_date_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
ALTER TEXT SEARCH CONFIGURATION arabic ADD MAPPING
    FOR word, hword_part, hword
	WITH arabic_stem
;
CREATE TABLE "public"."perm_part_child" (
    "a" integer
)
;
create table test_partitioned (a int) partition by range (a)
;
CREATE TABLE ihighway () INHERITS (road)
;
/* Single entry with value 'text' */
ALTER TABLE inhx add constraint foo CHECK (xx = 'text')
;
ALTER VIEW my_property_normal SET (security_barrier=true)
;
CREATE TABLE error_tbl (b1 bool DEFAULT (1 IN (1, 2)))
;
DROP TABLE stream_test
;
DROP AGGREGATE IF EXISTS foo(no_such_schema.no_such_type)
;
DROP TABLE VARBIT_SHIFT_TABLE
;
CREATE TABLESPACE source_ts  LOCATION ''
;
drop view rule_v1
;
CREATE INDEX base_table_payload_idx ON split_alter_table.base_table (payload)
;
CREATE TYPE case_int42 (
	"Internallength" = 4,
	"Input" = int42_in,
	"Output" = int42_out,
	"Alignment" = int4,
	"Default" = 42,
	"Passedbyvalue"
)
;
-- test diemv when the mv does not exist
DROP MATERIALIZED VIEW IF EXISTS no_such_mv
;
CREATE EVENT TRIGGER regress_reindex_end_snap ON ddl_command_end
    EXECUTE FUNCTION reindex_end_command_snap()
;
DROP FUNCTION bt_page_items(bytea)
;
CREATE TABLE split_event_helper.rewrite_probe (
    payload integer
)
;
ALTER TABLE b_star* RENAME COLUMN b TO bb
;
CREATE FUNCTION rboolop(query_int, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
VACUUM only_inh_parent
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
create table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=100)
;
-- Test join against inheritance tree

create temp table t2a () inherits (t2)
;
------------------------------------------------------------------------------------------
[ALTER_PROG_OBJ] alter operator family integer_ops using hash add
  function 1 hashint8alias1(int8alias1)
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_big BIGINT DEFAULT 180000000000018,
              ALTER COLUMN c_small SET DEFAULT 9,
              ALTER COLUMN c_small_null SET DEFAULT 13
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest29" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer
)
;
----------
[CREATE_TABLE] create table boolpart_t partition of boolpart for values in ('true')
;
----------
[CREATE_TABLE] create temp table tt4x(c1 int, c2 int, c3 int)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bytea_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_SCHEMA] -- test IF NOT EXISTS cases
CREATE SCHEMA test_ns_schema_renamed
;
----------
[CREATE_TABLE] CREATE TABLE mintab(c1 int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33180_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opfamily
;
----------
[COMMENT_CONSTRAINT] comment on constraint at_partitioned_0_id_name_key on at_partitioned_0 is 'child 0 constraint'
;
----------
[CREATE_TABLE] --
-- EXCLUDE constraints
--

CREATE TABLE circles (
  c1 CIRCLE,
  c2 TEXT,
  EXCLUDE USING gist
    (c1 WITH &&, (c2::circle) WITH &&)
    WHERE (circle_center(c1) <> '(0,0)')
)
;
----------
[ADD_INDEX] CREATE INDEX brin_test_serial_idx ON brin_parallel_test
 USING brin (a int4_minmax_ops, a int4_bloom_ops, b, c int8_minmax_multi_ops)
  WITH (pages_per_range=7)
 WHERE NOT (a BETWEEN 1000 and 1500)
;
----------
[CREATE_TABLE] -- Check inheritance cases
CREATE TEMP TABLE ucchild () inherits (uctest)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION pg_stat_statements DROP FUNCTION pg_stat_statements(boolean)
;
----------
[CREATE_PROG_OBJ] -- check some error cases, too

create or replace function rttest()
returns setof int as $$
begin
  return query select 10 into no_such_table;
end;
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM parted
;
----------
[CREATE_TABLE] -- ok, collation is implicitly coerced
create table test_part_coll_cast partition of test_part_coll_posix for values from (name 'm' collate "C") to ('s')
;
----------
[ALTER_TABLE] ALTER TABLE corrupt2 SET (autovacuum_enabled=false)
;
----------
[BLOCK] do $$
declare cnt int := 0;
  c1 cursor for select * from forc_test;
begin
  for r1 in c1 loop
    declare c1 cursor for select * from forc_test;
    begin
      for r2 in c1 loop
        cnt := cnt + 1;
      end loop;
    end;
  end loop;
  raise notice 'cnt = %', cnt;
end $$
;
----------
[ADD_INDEX] CREATE INDEX "gtest22c_pred_idx" ON "generated_stored_tests"."gtest22c" USING "btree" ("a") WHERE ("b" > 0)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."part_7" ATTACH PARTITION "public"."part_7_a_null" FOR VALUES IN ('a', NULL)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_foreign_table
;
----------
[CREATE_TABLE] create table partitioned1
  partition of partitioned for values in ('(1,2)')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_date_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION arabic ADD MAPPING
    FOR word, hword_part, hword
	WITH arabic_stem
;
----------
[CREATE_TABLE] CREATE TABLE "public"."perm_part_child" (
    "a" integer
)
;
----------
[CREATE_TABLE] create table test_partitioned (a int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE ihighway () INHERITS (road)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] /* Single entry with value 'text' */
ALTER TABLE inhx add constraint foo CHECK (xx = 'text')
;
----------
[ALTER_VIEW] ALTER VIEW my_property_normal SET (security_barrier=true)
;
----------
[CREATE_TABLE] CREATE TABLE error_tbl (b1 bool DEFAULT (1 IN (1, 2)))
;
----------
[DROP_TABLE] DROP TABLE stream_test
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE IF EXISTS foo(no_such_schema.no_such_type)
;
----------
[DROP_TABLE] DROP TABLE VARBIT_SHIFT_TABLE
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE source_ts  LOCATION ''
;
----------
[DROP_VIEW] drop view rule_v1
;
----------
[ADD_INDEX] CREATE INDEX base_table_payload_idx ON split_alter_table.base_table (payload)
;
----------
[CREATE_TYPE] CREATE TYPE case_int42 (
	"Internallength" = 4,
	"Input" = int42_in,
	"Output" = int42_out,
	"Alignment" = int4,
	"Default" = 42,
	"Passedbyvalue"
)
;
----------
[DROP_VIEW] -- test diemv when the mv does not exist
DROP MATERIALIZED VIEW IF EXISTS no_such_mv
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER regress_reindex_end_snap ON ddl_command_end
    EXECUTE FUNCTION reindex_end_command_snap()
;
----------
[DROP_PROG_OBJ] DROP FUNCTION bt_page_items(bytea)
;
----------
[CREATE_TABLE] CREATE TABLE split_event_helper.rewrite_probe (
    payload integer
)
;
----------
[RENAME_COLUMN] ALTER TABLE b_star* RENAME COLUMN b TO bb
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rboolop(query_int, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM only_inh_parent
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
----------
[CREATE_TABLE] create table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=100)
;
----------
[CREATE_TABLE] -- Test join against inheritance tree

create temp table t2a () inherits (t2)
;
