TRUNCATE ctest
;
CREATE TABLE alt_regress_1 (a INTEGER, b INTEGER)
;
DROP FUNCTION IF EXISTS split_obj_priv.mood_rank(split_obj_priv.cast_mood)
;
drop function get_users()
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_sequence
;
CLUSTER pg_toast.pg_toast_826 USING pg_toast_826_index
;
-- scans mcrparted3, mcrparte4, mcrparte5, mcrparted_def

-- check that partitioned table Appends cope with being referenced in
-- subplans
create table parted_minmax (a int, b varchar(16)) partition by range (a)
;
-- cannot add a column to partition or drop an inherited one
ALTER TABLE part_2 ADD COLUMN c text
;
ALTER TABLE itest17 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
-- test order of firing of FK triggers when several RI-induced changes need to
-- be made to the same row.  This was broken by subtransaction-related
-- changes in 8.0.

CREATE TEMP TABLE users (
  id INT PRIMARY KEY,
  name VARCHAR NOT NULL
)
;
-- fail - view
CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_view
;
-- ok: the same thing, but try plain ADD TABLE
ALTER PUBLICATION testpub_col_list DROP TABLE testpub_tbl8
;
CREATE TEMP TABLE temp_cov_table (id integer) ON COMMIT DROP
;
do $$ declare a quadarray;
begin a.c1[1].i := 11; raise notice 'a = %, a.c1[1].i = %', a, a.c1[1].i; end$$
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_sequence
;
DROP INDEX IF EXISTS split_table_idx.idx_method_payload_unique
;
-- OK if you use them in some other order
create table idxpart (a int, b int, c text, primary key  (a, b, c)) partition by range (b, c, a)
;
-- support routines for indexing

CREATE FUNCTION cube_union(cube, cube)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TEXT SEARCH PARSER split_ts_probe.simple_parser (
    START = prsd_start,
    GETTOKEN = prsd_nexttoken,
    END = prsd_end,
    LEXTYPES = prsd_lextype,
    HEADLINE = prsd_headline
)
;
ALTER TABLE notnull_tbl1 ALTER a DROP NOT NULL
;
CREATE SEQUENCE regtest_seq
;
CREATE VIEW v1 AS SELECT * FROM base_table
;
ALTER TABLE pcachetest DROP COLUMN q3
;
-- Do not forget to replace relid in bare Var join clause (bug #19435)
ALTER TABLE sl ADD COLUMN bool_col boolean
;
CREATE PUBLICATION tap_pub_A FOR TABLE "tab'le"
;
-- CREATE VIEW
CREATE VIEW view_stats_1 AS
  SELECT a AS col1, 2::int AS col2
    FROM generate_series(1, 10) AS tab(a) WHERE a < 5 AND a > 2
;
CREATE TABLE fk_partitioned_fk_3_1 (a int, b int)
;
CREATE FUNCTION f0(int) RETURNS int LANGUAGE SQL AS 'SELECT $1 * $1'
;
ANALYZE pg_temp_11.pg_temp_26345
;
ALTER TABLE errtst ATTACH PARTITION errtst_part_2 FOR VALUES IN ('aaaa')
;
DROP SUBSCRIPTION IF EXISTS split_sub_renamed
;
CREATE FUNCTION pgp_armor_headers(text, key OUT text, value OUT text)
RETURNS SETOF record
AS '$libdir/pgcrypto', 'pgp_armor_headers'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE POLICY p1 ON dob_t1 TO regress_rls_dob_role1 USING (true)
;
ALTER FOREIGN TABLE tru_ftable OPTIONS (SET truncatable 'true')
;
DROP TABLE unlogged_transactional
;
create or replace function shadowtest()
	returns void as $$
declare
f1 int;
c1 cursor (f1 int) for select 1;
begin
end$$ language plpgsql
;
DROP FUNCTION priv_testfunc5b(a priv_testdomain1)
;
alter table parent detach partition child1
;
CREATE SCHEMA s
;
CALL overload(1)
;
CREATE TABLE prt1_adv_p2 PARTITION OF prt1_adv FOR VALUES FROM (200) TO (300)
;
CREATE SUBSCRIPTION regress_test_sub CONNECTION 'port=16822 host=/tmp/ekP6Yh8edH user=regress_test_user dbname=postgres' PUBLICATION regress_test_pub
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_tablespace
;
CREATE FUNCTION "public"."trigger_parted_trigfunc"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$ begin end; $$
;
ALTER TABLE pg_toast.pg_toast_27803 RENAME TO reind_con_toast
;
ALTER TABLE onek RENAME CONSTRAINT onek_unique1_constraint TO onek_unique1_constraint_foo
;
create or replace function simplecaller() returns int language plpgsql
as $$
declare x int;
begin
  x := simplesql();
  return x;
end$$
;
create table coll_pruning_b partition of coll_pruning for values in ('b')
;
CREATE FUNCTION "public"."gtest_trigger_func3"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'OK';
  RETURN NEW;
END
$$
;
CREATE POLICY p4 ON document FOR DELETE
  USING (cid = (SELECT cid from category WHERE cname = 'manga'))
;
------------------------------------------------------------------------------------------
[TRUNCATE_TABLE] TRUNCATE ctest
;
----------
[CREATE_TABLE] CREATE TABLE alt_regress_1 (a INTEGER, b INTEGER)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS split_obj_priv.mood_rank(split_obj_priv.cast_mood)
;
----------
[DROP_PROG_OBJ] drop function get_users()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_sequence
;
----------
[ADMIN_TABLE] CLUSTER pg_toast.pg_toast_826 USING pg_toast_826_index
;
----------
[CREATE_TABLE] -- scans mcrparted3, mcrparte4, mcrparte5, mcrparted_def

-- check that partitioned table Appends cope with being referenced in
-- subplans
create table parted_minmax (a int, b varchar(16)) partition by range (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- cannot add a column to partition or drop an inherited one
ALTER TABLE part_2 ADD COLUMN c text
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest17 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[CREATE_TABLE] -- test order of firing of FK triggers when several RI-induced changes need to
-- be made to the same row.  This was broken by subtransaction-related
-- changes in 8.0.

CREATE TEMP TABLE users (
  id INT PRIMARY KEY,
  name VARCHAR NOT NULL
)
;
----------
[CREATE_PUB_SUB] -- fail - view
CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_view
;
----------
[ALTER_PUB_SUB] -- ok: the same thing, but try plain ADD TABLE
ALTER PUBLICATION testpub_col_list DROP TABLE testpub_tbl8
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temp_cov_table (id integer) ON COMMIT DROP
;
----------
[BLOCK] do $$ declare a quadarray;
begin a.c1[1].i := 11; raise notice 'a = %, a.c1[1].i = %', a, a.c1[1].i; end$$
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_sequence
;
----------
[DROP_INDEX] DROP INDEX IF EXISTS split_table_idx.idx_method_payload_unique
;
----------
[CREATE_TABLE] -- OK if you use them in some other order
create table idxpart (a int, b int, c text, primary key  (a, b, c)) partition by range (b, c, a)
;
----------
[CREATE_PROG_OBJ] -- support routines for indexing

CREATE FUNCTION cube_union(cube, cube)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH PARSER split_ts_probe.simple_parser (
    START = prsd_start,
    GETTOKEN = prsd_nexttoken,
    END = prsd_end,
    LEXTYPES = prsd_lextype,
    HEADLINE = prsd_headline
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE notnull_tbl1 ALTER a DROP NOT NULL
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE regtest_seq
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v1 AS SELECT * FROM base_table
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE pcachetest DROP COLUMN q3
;
----------
[ALTER_TABLE|ADD_COLUMN] -- Do not forget to replace relid in bare Var join clause (bug #19435)
ALTER TABLE sl ADD COLUMN bool_col boolean
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_A FOR TABLE "tab'le"
;
----------
[CREATE_VIEW(SELECT)] -- CREATE VIEW
CREATE VIEW view_stats_1 AS
  SELECT a AS col1, 2::int AS col2
    FROM generate_series(1, 10) AS tab(a) WHERE a < 5 AND a > 2
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_3_1 (a int, b int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f0(int) RETURNS int LANGUAGE SQL AS 'SELECT $1 * $1'
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26345
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE errtst ATTACH PARTITION errtst_part_2 FOR VALUES IN ('aaaa')
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION IF EXISTS split_sub_renamed
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgp_armor_headers(text, key OUT text, value OUT text)
RETURNS SETOF record
AS '$libdir/pgcrypto', 'pgp_armor_headers'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON dob_t1 TO regress_rls_dob_role1 USING (true)
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE tru_ftable OPTIONS (SET truncatable 'true')
;
----------
[DROP_TABLE] DROP TABLE unlogged_transactional
;
----------
[CREATE_PROG_OBJ] create or replace function shadowtest()
	returns void as $$
declare
f1 int;
c1 cursor (f1 int) for select 1;
begin
end$$ language plpgsql
;
----------
[DROP_PROG_OBJ] DROP FUNCTION priv_testfunc5b(a priv_testdomain1)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parent detach partition child1
;
----------
[CREATE_SCHEMA] CREATE SCHEMA s
;
----------
[CALL_PROG_OBJ] CALL overload(1)
;
----------
[CREATE_TABLE] CREATE TABLE prt1_adv_p2 PARTITION OF prt1_adv FOR VALUES FROM (200) TO (300)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_test_sub CONNECTION 'port=16822 host=/tmp/ekP6Yh8edH user=regress_test_user dbname=postgres' PUBLICATION regress_test_pub
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_tablespace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."trigger_parted_trigfunc"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$ begin end; $$
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27803 RENAME TO reind_con_toast
;
----------
[RENAME_CONSTRAINT] ALTER TABLE onek RENAME CONSTRAINT onek_unique1_constraint TO onek_unique1_constraint_foo
;
----------
[CREATE_PROG_OBJ] create or replace function simplecaller() returns int language plpgsql
as $$
declare x int;
begin
  x := simplesql();
  return x;
end$$
;
----------
[CREATE_TABLE] create table coll_pruning_b partition of coll_pruning for values in ('b')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."gtest_trigger_func3"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'OK';
  RETURN NEW;
END
$$
;
----------
[CREATE_POLICY] CREATE POLICY p4 ON document FOR DELETE
  USING (cid = (SELECT cid from category WHERE cname = 'manga'))
;
