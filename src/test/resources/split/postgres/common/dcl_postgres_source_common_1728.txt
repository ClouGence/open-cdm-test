EXECUTE dumpFunc('27666')
;
CREATE ROLE regress_alter_function_role
;
ALTER TABLE generated_virtual_tests.gtest2 OWNER TO zyc
;
COPY psql_comics FROM STDIN
;
EXECUTE dumpOpr('2360')
;
-- ok, can create a role without any special attributes
CREATE ROLE regress_role_limited
;
ALTER TABLE public.sequence_test13 OWNER TO zyc
;
-- should fail
DEALLOCATE PREPARE ins
;
ALTER TABLE public.prt2_l_p3 OWNER TO zyc
;
-- value of $1 should not be sent to remote
PREPARE st5(user_enum,int) AS SELECT * FROM ft1 t1 WHERE c8 = $1 and c1 = $2
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('27666')
;
----------
[CREATE_ROLE] CREATE ROLE regress_alter_function_role
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE generated_virtual_tests.gtest2 OWNER TO zyc
;
----------
[DATA_IMPORT] COPY psql_comics FROM STDIN
;
----------
[UNSAFE] EXECUTE dumpOpr('2360')
;
----------
[CREATE_ROLE] -- ok, can create a role without any special attributes
CREATE ROLE regress_role_limited
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.sequence_test13 OWNER TO zyc
;
----------
[UNSAFE] -- should fail
DEALLOCATE PREPARE ins
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.prt2_l_p3 OWNER TO zyc
;
----------
[UNSAFE] -- value of $1 should not be sent to remote
PREPARE st5(user_enum,int) AS SELECT * FROM ft1 t1 WHERE c8 = $1 and c1 = $2
;
