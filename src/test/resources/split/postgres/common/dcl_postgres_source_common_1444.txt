COPY public.euc_jis_2004_inputs (inbytes, description) TO stdout
;
EXECUTE dumpOpr('909')
;
EXECUTE dumpFunc('20316')
;
-- Produce results with sorting.

set enable_hashagg = false
;
-- load half the rows via regular COPY and rest via COPY FREEZE. The pages
-- which are touched by regular COPY must not be set all-visible/all-frozen. On
-- the other hand, pages allocated by COPY FREEZE should be marked
-- all-frozen/all-visible.
begin
;
LOCK TABLE "fkpart6"."pk" IN ACCESS SHARE MODE
;
SET track_activities = off
;
-- For Parallel Append
SET max_parallel_workers_per_gather TO 2
;
ALTER TABLE public.prt3_n_p1 OWNER TO zyc
;
CREATE SERVER sv2 FOREIGN DATA WRAPPER test_wrapper
;
------------------------------------------------------------------------------------------
[DATA_EXPORT] COPY public.euc_jis_2004_inputs (inbytes, description) TO stdout
;
----------
[UNSAFE] EXECUTE dumpOpr('909')
;
----------
[UNSAFE] EXECUTE dumpFunc('20316')
;
----------
[SESSION_SETTING_WRITE] -- Produce results with sorting.

set enable_hashagg = false
;
----------
[TRANSACTION] -- load half the rows via regular COPY and rest via COPY FREEZE. The pages
-- which are touched by regular COPY must not be set all-visible/all-frozen. On
-- the other hand, pages allocated by COPY FREEZE should be marked
-- all-frozen/all-visible.
begin
;
----------
[SESSION_LOCK] LOCK TABLE "fkpart6"."pk" IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] SET track_activities = off
;
----------
[SESSION_SETTING_WRITE] -- For Parallel Append
SET max_parallel_workers_per_gather TO 2
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.prt3_n_p1 OWNER TO zyc
;
----------
[SYSTEM_SETTING_WRITE] CREATE SERVER sv2 FOREIGN DATA WRAPPER test_wrapper
;
