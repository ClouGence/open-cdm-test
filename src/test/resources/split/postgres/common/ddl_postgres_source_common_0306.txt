CLUSTER test_prepared1 USING test_prepared1_pkey
;
create table parent_noinh_convalid (a int)
;
CREATE TABLE PATH_TBL (f1 path)
;
CREATE DATABASE foobar3 TEMPLATE foobar2 LC_COLLATE 'C' LC_CTYPE 'C'
;
COMMENT ON CONSTRAINT comment_test_child_fk ON comment_test_child IS 'FOREIGN KEY constraint of comment_test_child'
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."nv_child_2011" INHERIT "public"."nv_parent"
;
ALTER FUNCTION gbt_uuid_union(internal, internal) PARALLEL SAFE
;
/*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
CREATE TEMPORARY TABLE testpub_temptbl(a int)
;
CREATE FUNCTION gseg_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE "public"."gtest20b"
    ADD CONSTRAINT "chk" CHECK (("b" < 50)) NOT VALID
;
ALTER TABLE public.fk_partitioned_fk ADD CONSTRAINT fk_partitioned_fk_a_fkey FOREIGN KEY (a) REFERENCES fk_notpartitioned_pk(a)
;
alter table atacc1 drop constraint atacc1_constr_or
;
ALTER SEQUENCE sequence_test4 RESTART WITH 40
;
CREATE TABLE concur_reindex_part_0 PARTITION OF concur_reindex_part
  FOR VALUES FROM (0) TO (10) PARTITION BY list (c2)
;
ALTER TABLE lt1 ATTACH PARTITION ft_part2 FOR VALUES FROM (1000) TO (2000)
;
-- test CREATE OR REPLACE TRIGGER on partition table
create table parted_trig (a int) partition by range (a)
;
CREATE TABLE "public"."pht1_e_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
-- REINDEX SCHEMA processes all temporary relations
CREATE TABLE reindex_temp_before AS
SELECT oid, relname, relfilenode, relkind, reltoastrelid
  FROM pg_class
  WHERE relname IN ('concur_temp_ind_1', 'concur_temp_ind_2')
;
CREATE TABLE "public"."test_type_diff2_c3" (
    "int_two" smallint,
    "int_four" bigint,
    "int_eight" bigint
)
;
create database abc with template = template1
;
CREATE FUNCTION make_valid(ismn13)
	RETURNS ismn13
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER FOREIGN TABLE agg_bad OPTIONS (ADD on_error 'ignore')
;
--
-- Test cases involving recursion and error recovery in simple expressions
-- (bugs in all versions before October 2010).  The problems are most
-- easily exposed by mutual recursion between plpgsql and sql functions.
--

create function recurse(float8) returns float8 as
$$
begin
  if ($1 > 0) then
    return sql_recurse($1 - 1);
  else
    return $1;
  end if;
end;
$$ language plpgsql
;
do $$ declare c4 two_int4s; c8 two_int8s;
begin
  c8 := row(1,2);
  c4 := c8;
  c8 := c4;
  raise notice 'c4 = %', c4;
  raise notice 'c8 = %', c8;
end$$
;
CREATE OPERATOR CLASS gist_bpchar_ops
DEFAULT FOR TYPE bpchar USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bpchar_consistent (internal, bpchar , int2, oid, internal),
	FUNCTION	2	gbt_text_union (internal, internal),
	FUNCTION	3	gbt_bpchar_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_text_penalty (internal, internal, internal),
	FUNCTION	6	gbt_text_picksplit (internal, internal),
	FUNCTION	7	gbt_text_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
-- Scalar comparison methods

CREATE FUNCTION seg_lt(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- Stabilize tests
ALTER TABLE bttest_a SET (autovacuum_enabled = false)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_attribute
;
--
-- Test enabling/disabling
--
CREATE TABLE ruletest1 (a int)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub2, pub3
;
CREATE DATABASE "regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
CREATE TYPE split_obj_priv.cast_mood AS ENUM ('sad', 'ok', 'happy')
;
CREATE TRIGGER ins_trig_after_row_insert
		  AFTER INSERT ON ins_trig
		  FOR EACH ROW EXECUTE PROCEDURE ins_trig_after_row_trig()
;
-- Stabalize tests
ALTER TABLE bttest_a SET (autovacuum_enabled = false)
;
create table idxpart (a int, b int primary key) partition by range (b, a)
;
-- dual polymorphic transfn
CREATE FUNCTION tfp(anyarray,anyelement) RETURNS anyarray AS
'select $1 || $2' LANGUAGE SQL
;
--
-- hash_page_stats()
--
CREATE FUNCTION hash_page_stats(IN page bytea,
    OUT live_items int4,
    OUT dead_items int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT hasho_prevblkno int8,
    OUT hasho_nextblkno int8,
    OUT hasho_bucket int8,
	OUT hasho_flag int4,
	OUT hasho_page_id int4)
AS '$libdir/pageinspect', 'hash_page_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
alter domain dom add constraint domchkgt6 check(value > 6)
;
CREATE VIEW v11_temp AS SELECT t1.id, t2.a FROM base_table t1, v10_temp t2
;
ALTER TABLE tab_rowfilter_toast REPLICA IDENTITY USING INDEX tab_rowfilter_toast_ri_index
;
COMMENT ON FUNCTION seg_contains(seg, seg) IS
'contains'
;
-- Ensure enable_partition_prune does not affect non-partitioned tables.

create table inh_lp (a int, value int)
;
drop table temp_part_parent cascade
;
CREATE TABLE tab_crash (a integer UNIQUE)
;
CREATE TABLE rls_tbl (c1 int)
;
ALTER TABLE ONLY "public"."itest7a" INHERIT "public"."itest7"
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_inherits
;
CREATE TEMP VIEW fdv3 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
UNION
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
;
SECURITY LABEL ON COLUMN t5.e IS 'system_u:object_r:sepgsql_table_t:s0'
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] CLUSTER test_prepared1 USING test_prepared1_pkey
;
----------
[CREATE_TABLE] create table parent_noinh_convalid (a int)
;
----------
[CREATE_TABLE] CREATE TABLE PATH_TBL (f1 path)
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar3 TEMPLATE foobar2 LC_COLLATE 'C' LC_CTYPE 'C'
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT comment_test_child_fk ON comment_test_child IS 'FOREIGN KEY constraint of comment_test_child'
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."nv_child_2011" INHERIT "public"."nv_parent"
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_uuid_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_POLICY] /*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE testpub_temptbl(a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gseg_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."gtest20b"
    ADD CONSTRAINT "chk" CHECK (("b" < 50)) NOT VALID
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.fk_partitioned_fk ADD CONSTRAINT fk_partitioned_fk_a_fkey FOREIGN KEY (a) REFERENCES fk_notpartitioned_pk(a)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table atacc1 drop constraint atacc1_constr_or
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test4 RESTART WITH 40
;
----------
[CREATE_TABLE] CREATE TABLE concur_reindex_part_0 PARTITION OF concur_reindex_part
  FOR VALUES FROM (0) TO (10) PARTITION BY list (c2)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE lt1 ATTACH PARTITION ft_part2 FOR VALUES FROM (1000) TO (2000)
;
----------
[CREATE_TABLE] -- test CREATE OR REPLACE TRIGGER on partition table
create table parted_trig (a int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht1_e_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TABLE|SELECT] -- REINDEX SCHEMA processes all temporary relations
CREATE TABLE reindex_temp_before AS
SELECT oid, relname, relfilenode, relkind, reltoastrelid
  FROM pg_class
  WHERE relname IN ('concur_temp_ind_1', 'concur_temp_ind_2')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_type_diff2_c3" (
    "int_two" smallint,
    "int_four" bigint,
    "int_eight" bigint
)
;
----------
[CREATE_CATALOG] create database abc with template = template1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_valid(ismn13)
	RETURNS ismn13
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE agg_bad OPTIONS (ADD on_error 'ignore')
;
----------
[CREATE_PROG_OBJ] --
-- Test cases involving recursion and error recovery in simple expressions
-- (bugs in all versions before October 2010).  The problems are most
-- easily exposed by mutual recursion between plpgsql and sql functions.
--

create function recurse(float8) returns float8 as
$$
begin
  if ($1 > 0) then
    return sql_recurse($1 - 1);
  else
    return $1;
  end if;
end;
$$ language plpgsql
;
----------
[BLOCK] do $$ declare c4 two_int4s; c8 two_int8s;
begin
  c8 := row(1,2);
  c4 := c8;
  c8 := c4;
  raise notice 'c4 = %', c4;
  raise notice 'c8 = %', c8;
end$$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_bpchar_ops
DEFAULT FOR TYPE bpchar USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bpchar_consistent (internal, bpchar , int2, oid, internal),
	FUNCTION	2	gbt_text_union (internal, internal),
	FUNCTION	3	gbt_bpchar_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_text_penalty (internal, internal, internal),
	FUNCTION	6	gbt_text_picksplit (internal, internal),
	FUNCTION	7	gbt_text_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
----------
[CREATE_PROG_OBJ] -- Scalar comparison methods

CREATE FUNCTION seg_lt(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] -- Stabilize tests
ALTER TABLE bttest_a SET (autovacuum_enabled = false)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_attribute
;
----------
[CREATE_TABLE] --
-- Test enabling/disabling
--
CREATE TABLE ruletest1 (a int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub2, pub3
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
----------
[CREATE_TYPE] CREATE TYPE split_obj_priv.cast_mood AS ENUM ('sad', 'ok', 'happy')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER ins_trig_after_row_insert
		  AFTER INSERT ON ins_trig
		  FOR EACH ROW EXECUTE PROCEDURE ins_trig_after_row_trig()
;
----------
[ALTER_TABLE] -- Stabalize tests
ALTER TABLE bttest_a SET (autovacuum_enabled = false)
;
----------
[CREATE_TABLE] create table idxpart (a int, b int primary key) partition by range (b, a)
;
----------
[CREATE_PROG_OBJ] -- dual polymorphic transfn
CREATE FUNCTION tfp(anyarray,anyelement) RETURNS anyarray AS
'select $1 || $2' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] --
-- hash_page_stats()
--
CREATE FUNCTION hash_page_stats(IN page bytea,
    OUT live_items int4,
    OUT dead_items int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT hasho_prevblkno int8,
    OUT hasho_nextblkno int8,
    OUT hasho_bucket int8,
	OUT hasho_flag int4,
	OUT hasho_page_id int4)
AS '$libdir/pageinspect', 'hash_page_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TYPE] alter domain dom add constraint domchkgt6 check(value > 6)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v11_temp AS SELECT t1.id, t2.a FROM base_table t1, v10_temp t2
;
----------
[ALTER_TABLE] ALTER TABLE tab_rowfilter_toast REPLICA IDENTITY USING INDEX tab_rowfilter_toast_ri_index
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_contains(seg, seg) IS
'contains'
;
----------
[CREATE_TABLE] -- Ensure enable_partition_prune does not affect non-partitioned tables.

create table inh_lp (a int, value int)
;
----------
[DROP_TABLE] drop table temp_part_parent cascade
;
----------
[CREATE_TABLE] CREATE TABLE tab_crash (a integer UNIQUE)
;
----------
[CREATE_TABLE] CREATE TABLE rls_tbl (c1 int)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."itest7a" INHERIT "public"."itest7"
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_inherits
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW fdv3 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
UNION
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
;
----------
[UNKNOWN] SECURITY LABEL ON COLUMN t5.e IS 'system_u:object_r:sepgsql_table_t:s0'
;
