create temp table compos (f1 int, f2 text)
;
ALTER OPERATOR <@ (ltree, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
ALTER OPERATOR <= (ean13, isbn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE SEQUENCE "public"."spgist_box_tbl_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
ALTER FUNCTION b() DEPENDS ON EXTENSION test_ext5
;
-- Verify that triggers fire in alphabetical order
create table parted_trig (a int) partition by range (a)
;
create table mlparted_def2 partition of mlparted_def for values from (50) to (60)
;
CREATE INDEX "gpointind" ON "public"."point_tbl" USING "gist" ("f1")
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_parser
;
CREATE INDEX ctlt1_b_key ON ctlt1 (b)
;
CREATE FUNCTION gtest_trigger_func3() RETURNS trigger
  LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'OK';
  RETURN NEW;
END
$$
;
CREATE VIEW rls_sbv WITH (security_barrier) AS
    SELECT * FROM y1 WHERE f_leak(b)
;
CREATE TABLE ext_cine_tab2 AS SELECT 42 AS y
;
VACUUM (PARALLEL 0) pg_catalog.pg_parameter_acl
;
CREATE FOREIGN TABLE test_foreign_table () SERVER dummy_server
;
ALTER TABLE lt1 ATTACH PARTITION ft_part2
  FOR VALUES FROM (1000) TO (2000)
;
CREATE TABLE alice.hashpart_b PARTITION OF alice.hashpart
    FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
create index alttype_cluster_ind on alttype_cluster (a)
;
CREATE TABLE t_009_tbl2 (id int, msg text)
;
ALTER TABLE pg_toast.pg_toast_27959 RENAME TO reind_con_toast
;
CREATE SEQUENCE IF NOT EXISTS sequence_test
;
create table phv_part_null partition of phv_part for values in (null)
;
CREATE FUNCTION cube_subset(cube, int4[])
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
VACUUM (PARALLEL 0) pvactst
;
create cast (xfloat4 as integer) without function
;
create table inh_parent_2(f2 text)
;
ALTER TYPE planets ADD VALUE IF NOT EXISTS 'mercury'
;
-- ok, constant-folded arg
CALL ptest1(substring(random()::numeric(20,15)::text, 1, 1))
;
create function build_ordered_int8s_2(i int8, j int8) returns ordered_int8s as $$
declare r record; begin r := row(i,j); return r; end
$$ language plpgsql
;
create table parted_replica_tab_1 partition of parted_replica_tab
  for values from (1) to (10) partition by range (id)
;
ANALYZE public.persons2
;
alter table anothertab drop constraint IF EXISTS anothertab_chk
;
create index test_partition_idx on test_partition(a)
;
/*
 * 5.60
 * SQL_SIZING table
 */

-- Note: Sizing items are defined in ISO/IEC 9075-3:2008, clause 9.2.

CREATE TABLE sql_sizing (
    sizing_id       cardinal_number,
    sizing_name     character_data,
    supported_value cardinal_number,
    comments        character_data
)
;
ALTER EXTENSION test_pg_dump DROP VIEW test_pg_dump_v1
;
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (10) TO (50)
;
CREATE AGGREGATE logging_agg_strict_initcond (anyelement)
(
	stype = text,
	sfunc = logging_sfunc_strict,
	mstype = text,
	msfunc = logging_msfunc_strict,
	minvfunc = logging_minvfunc_strict,
	initcond = 'I',
	minitcond = 'MI'
)
;
ALTER TABLE temporal_rng DROP CONSTRAINT temporal_rng_pk
;
CREATE INDEX index_with_stats
						  ON dump_test.table_index_stats
						  ((col1 + 1), col1, (col2 + 1), (col3 + 1))
;
CREATE FOREIGN table tableam_fdw_heapx () SERVER fs_heap2
;
--
-- alter function
--
create function test_strict(text) returns text as
    'select coalesce($1, ''got passed a null'');'
    language sql returns null on null input
;
ANALYZE pg_temp_13.pg_temp_26413
;
create table idxpart21 partition of idxpart2 for values from (0) to (100)
;
DROP FUNCTION func_stats
;
create table pfk1 partition of pfk for values in (1)
;
CREATE TABLE test_part_c (a int, b int, c int) PARTITION BY LIST (a)
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "hword_asciipart" WITH "public"."synonym", "public"."thesaurus", "english_stem"
;
DROP FUNCTION on_login_proc
;
CREATE TABLE fk_partitioned_pk_3 PARTITION OF fk_partitioned_pk FOR VALUES FROM (2000,2000) TO (3000,3000)
  PARTITION BY RANGE (a)
;
drop function my_trigger_function()
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create temp table compos (f1 int, f2 text)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <@ (ltree, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ean13, isbn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."spgist_box_tbl_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION b() DEPENDS ON EXTENSION test_ext5
;
----------
[CREATE_TABLE] -- Verify that triggers fire in alphabetical order
create table parted_trig (a int) partition by range (a)
;
----------
[CREATE_TABLE] create table mlparted_def2 partition of mlparted_def for values from (50) to (60)
;
----------
[ADD_INDEX] CREATE INDEX "gpointind" ON "public"."point_tbl" USING "gist" ("f1")
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_parser
;
----------
[ADD_INDEX] CREATE INDEX ctlt1_b_key ON ctlt1 (b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtest_trigger_func3() RETURNS trigger
  LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'OK';
  RETURN NEW;
END
$$
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rls_sbv WITH (security_barrier) AS
    SELECT * FROM y1 WHERE f_leak(b)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ext_cine_tab2 AS SELECT 42 AS y
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_parameter_acl
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE test_foreign_table () SERVER dummy_server
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE lt1 ATTACH PARTITION ft_part2
  FOR VALUES FROM (1000) TO (2000)
;
----------
[CREATE_TABLE] CREATE TABLE alice.hashpart_b PARTITION OF alice.hashpart
    FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[ADD_INDEX] create index alttype_cluster_ind on alttype_cluster (a)
;
----------
[CREATE_TABLE] CREATE TABLE t_009_tbl2 (id int, msg text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27959 RENAME TO reind_con_toast
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE IF NOT EXISTS sequence_test
;
----------
[CREATE_TABLE] create table phv_part_null partition of phv_part for values in (null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_subset(cube, int4[])
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pvactst
;
----------
[CREATE_PROG_OBJ] create cast (xfloat4 as integer) without function
;
----------
[CREATE_TABLE] create table inh_parent_2(f2 text)
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE IF NOT EXISTS 'mercury'
;
----------
[CALL_PROG_OBJ] -- ok, constant-folded arg
CALL ptest1(substring(random()::numeric(20,15)::text, 1, 1))
;
----------
[CREATE_PROG_OBJ] create function build_ordered_int8s_2(i int8, j int8) returns ordered_int8s as $$
declare r record; begin r := row(i,j); return r; end
$$ language plpgsql
;
----------
[CREATE_TABLE] create table parted_replica_tab_1 partition of parted_replica_tab
  for values from (1) to (10) partition by range (id)
;
----------
[ADMIN_TABLE] ANALYZE public.persons2
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table anothertab drop constraint IF EXISTS anothertab_chk
;
----------
[ADD_INDEX] create index test_partition_idx on test_partition(a)
;
----------
[CREATE_TABLE] /*
 * 5.60
 * SQL_SIZING table
 */

-- Note: Sizing items are defined in ISO/IEC 9075-3:2008, clause 9.2.

CREATE TABLE sql_sizing (
    sizing_id       cardinal_number,
    sizing_name     character_data,
    supported_value cardinal_number,
    comments        character_data
)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP VIEW test_pg_dump_v1
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (10) TO (50)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE logging_agg_strict_initcond (anyelement)
(
	stype = text,
	sfunc = logging_sfunc_strict,
	mstype = text,
	msfunc = logging_msfunc_strict,
	minvfunc = logging_minvfunc_strict,
	initcond = 'I',
	minitcond = 'MI'
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE temporal_rng DROP CONSTRAINT temporal_rng_pk
;
----------
[ADD_INDEX] CREATE INDEX index_with_stats
						  ON dump_test.table_index_stats
						  ((col1 + 1), col1, (col2 + 1), (col3 + 1))
;
----------
[CREATE_TABLE] CREATE FOREIGN table tableam_fdw_heapx () SERVER fs_heap2
;
----------
[CREATE_PROG_OBJ] --
-- alter function
--
create function test_strict(text) returns text as
    'select coalesce($1, ''got passed a null'');'
    language sql returns null on null input
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26413
;
----------
[CREATE_TABLE] create table idxpart21 partition of idxpart2 for values from (0) to (100)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION func_stats
;
----------
[CREATE_TABLE] create table pfk1 partition of pfk for values in (1)
;
----------
[CREATE_TABLE] CREATE TABLE test_part_c (a int, b int, c int) PARTITION BY LIST (a)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "hword_asciipart" WITH "public"."synonym", "public"."thesaurus", "english_stem"
;
----------
[DROP_PROG_OBJ] DROP FUNCTION on_login_proc
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_pk_3 PARTITION OF fk_partitioned_pk FOR VALUES FROM (2000,2000) TO (3000,3000)
  PARTITION BY RANGE (a)
;
----------
[DROP_PROG_OBJ] drop function my_trigger_function()
;
