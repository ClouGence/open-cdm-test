ANALYZE pg_temp_11.pg_temp_26230_2
;
CREATE FUNCTION isnge(ean13, isbn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER OPERATOR <= (ismn, ismn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE TABLE test2 (a int)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_cast
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."b_star" INHERIT "public"."a_star"
;
CREATE FUNCTION "public"."transition_table_level2_bad_usage_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  BEGIN
    INSERT INTO dx VALUES (1000000, 1000000, 'x');
    RETURN NULL;
  END;
$$
;
create temp table temp_parted_oncommit_1
  partition of temp_parted_oncommit
  for values in (1) on commit delete rows
;
alter table invalid_check_con_child add constraint inh_check_constraint check(f1 > 0)
;
CREATE FUNCTION isnlt(ean13, issn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create unlogged table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=98) tablespace "pg_default"
;
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf1 RENAME TO alt_ts_conf3
;
DROP INDEX inetidx
;
DROP TABLE pg_temp_13.pg_temp_26049_2, pg_temp_13.pg_temp_26049
;
ALTER TABLE ONLY "public"."beta_pos" ATTACH PARTITION "public"."beta_pos_p3" FOR VALUES IN ('0007', '0009')
;
CREATE TABLE "public"."idxpart_another_1" (
    "a" integer NOT NULL,
    "b" integer NOT NULL
)
;
CREATE FUNCTION "public"."tf1p"("anyarray", integer) RETURNS "anyarray"
    LANGUAGE "sql"
    AS $_$select $1$_$
;
--
  -- Setup to test concurrent handling of ExecDelete().
  --
  CREATE TABLE foo (a int, b text) PARTITION BY LIST(a)
;
CREATE TABLE pagg_tab1_p1 PARTITION OF pagg_tab1 FOR VALUES FROM (0) TO (10)
;
CREATE TABLE "public"."ec0" (
    "ff" bigint NOT NULL,
    "f1" bigint,
    "f2" bigint
)
;
VACUUM (FULL) pg_catalog.pg_language
;
CREATE TABLE INSERT_CHILD (cx INT default 42,
	cy INT CHECK (cy > x))
	INHERITS (INSERT_TBL)
;
CREATE INDEX on tbl USING hash(c1, c2) INCLUDE (c3, c4)
;
VACUUM FULL pg_database
;
CREATE FUNCTION dblink_fetch (text, text, int, boolean)
RETURNS setof record
AS '$libdir/dblink','dblink_fetch'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
alter table atacc1 add constraint atacc_oid1 primary key(id)
;
alter table tabwithcols drop column d
;
DROP SCHEMA reindex_inj
;
-- fail - nonexistent
ALTER PUBLICATION testpub_default DROP TABLE pub_test.testpub_nopk
;
ALTER DATABASE "contrib_regression" SET lc_time TO 'C'
;
CREATE TABLE "public"."prt2_l_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY LIST ("c")
;
-- specified literal can be cast, and the cast might not be immutable
CREATE TABLE moneyp (
	a money
) PARTITION BY LIST (a)
;
ANALYZE pg_catalog.pg_subscription
;
create table another (f1 int, f2 text)
;
drop view aggordview1
;
ANALYZE async_p3
;
CREATE TABLE tenk2 (
	unique1 	int4,
	unique2 	int4,
	two 	 	int4,
	four 		int4,
	ten			int4,
	twenty 		int4,
	hundred 	int4,
	thousand 	int4,
	twothousand int4,
	fivethous 	int4,
	tenthous	int4,
	odd			int4,
	even		int4,
	stringu1	name,
	stringu2	name,
	string4		name
)
;
CREATE FUNCTION "public"."start_command"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
RAISE NOTICE '% - ddl_command_start', tg_tag;
END;
$$
;
/*
 * text search configuration for yiddish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * yiddish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY yiddish_stem
	(TEMPLATE = snowball, Language = yiddish )
;
DROP TABLE toast_bug
;
CREATE TABLE ref_b (
    b INT PRIMARY KEY,
    a INT REFERENCES trunc_a(a) ON DELETE CASCADE
)
;
-- Operator classes
CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING dummy_index_am AS
  OPERATOR 1 = (int4, int4),
  FUNCTION 1 hashint4(int4)
;
create table tab_progress_reporting (
	name text,
	age int4,
	location point,
	salary int4,
	manager name
)
;
CREATE TABLE prt (a int) PARTITION BY RANGE(a)
;
ALTER INDEX pg_toast.pg_toast_30646_index RENAME TO reind_con_toast_idx
;
CREATE TRIGGER "tg_pfield_ad" AFTER DELETE ON "public"."pfield" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pfield_ad"()
;
CREATE TABLE master_promotion_tab (a int)
;
DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN SELECT * FROM test2 ORDER BY x LOOP
        INSERT INTO test1 (a) VALUES (12/(r.x-2));
        COMMIT;
    END LOOP;
END;
$$
;
drop table strdbase
;
CREATE FUNCTION gseg_same(seg, seg, internal)
RETURNS internal
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26230_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ean13, isbn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ismn, ismn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE test2 (a int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_cast
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."b_star" INHERIT "public"."a_star"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."transition_table_level2_bad_usage_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  BEGIN
    INSERT INTO dx VALUES (1000000, 1000000, 'x');
    RETURN NULL;
  END;
$$
;
----------
[CREATE_TABLE] create temp table temp_parted_oncommit_1
  partition of temp_parted_oncommit
  for values in (1) on commit delete rows
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table invalid_check_con_child add constraint inh_check_constraint check(f1 > 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ean13, issn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] create unlogged table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=98) tablespace "pg_default"
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION alt_ts_conf1 RENAME TO alt_ts_conf3
;
----------
[DROP_INDEX] DROP INDEX inetidx
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26049_2, pg_temp_13.pg_temp_26049
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."beta_pos" ATTACH PARTITION "public"."beta_pos_p3" FOR VALUES IN ('0007', '0009')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart_another_1" (
    "a" integer NOT NULL,
    "b" integer NOT NULL
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tf1p"("anyarray", integer) RETURNS "anyarray"
    LANGUAGE "sql"
    AS $_$select $1$_$
;
----------
[CREATE_TABLE] --
  -- Setup to test concurrent handling of ExecDelete().
  --
  CREATE TABLE foo (a int, b text) PARTITION BY LIST(a)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab1_p1 PARTITION OF pagg_tab1 FOR VALUES FROM (0) TO (10)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ec0" (
    "ff" bigint NOT NULL,
    "f1" bigint,
    "f2" bigint
)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_language
;
----------
[CREATE_TABLE] CREATE TABLE INSERT_CHILD (cx INT default 42,
	cy INT CHECK (cy > x))
	INHERITS (INSERT_TBL)
;
----------
[ADD_INDEX] CREATE INDEX on tbl USING hash(c1, c2) INCLUDE (c3, c4)
;
----------
[ADMIN_TABLE] VACUUM FULL pg_database
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_fetch (text, text, int, boolean)
RETURNS setof record
AS '$libdir/dblink','dblink_fetch'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_oid1 primary key(id)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table tabwithcols drop column d
;
----------
[DROP_SCHEMA] DROP SCHEMA reindex_inj
;
----------
[ALTER_PUB_SUB] -- fail - nonexistent
ALTER PUBLICATION testpub_default DROP TABLE pub_test.testpub_nopk
;
----------
[ALTER_CATALOG] ALTER DATABASE "contrib_regression" SET lc_time TO 'C'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_l_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY LIST ("c")
;
----------
[CREATE_TABLE] -- specified literal can be cast, and the cast might not be immutable
CREATE TABLE moneyp (
	a money
) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] create table another (f1 int, f2 text)
;
----------
[DROP_VIEW] drop view aggordview1
;
----------
[ADMIN_TABLE] ANALYZE async_p3
;
----------
[CREATE_TABLE] CREATE TABLE tenk2 (
	unique1 	int4,
	unique2 	int4,
	two 	 	int4,
	four 		int4,
	ten			int4,
	twenty 		int4,
	hundred 	int4,
	thousand 	int4,
	twothousand int4,
	fivethous 	int4,
	tenthous	int4,
	odd			int4,
	even		int4,
	stringu1	name,
	stringu2	name,
	string4		name
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."start_command"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
RAISE NOTICE '% - ddl_command_start', tg_tag;
END;
$$
;
----------
[CREATE_POLICY] /*
 * text search configuration for yiddish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * yiddish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY yiddish_stem
	(TEMPLATE = snowball, Language = yiddish )
;
----------
[DROP_TABLE] DROP TABLE toast_bug
;
----------
[CREATE_TABLE] CREATE TABLE ref_b (
    b INT PRIMARY KEY,
    a INT REFERENCES trunc_a(a) ON DELETE CASCADE
)
;
----------
[CREATE_PROG_OBJ] -- Operator classes
CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING dummy_index_am AS
  OPERATOR 1 = (int4, int4),
  FUNCTION 1 hashint4(int4)
;
----------
[CREATE_TABLE] create table tab_progress_reporting (
	name text,
	age int4,
	location point,
	salary int4,
	manager name
)
;
----------
[CREATE_TABLE] CREATE TABLE prt (a int) PARTITION BY RANGE(a)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30646_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_pfield_ad" AFTER DELETE ON "public"."pfield" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pfield_ad"()
;
----------
[CREATE_TABLE] CREATE TABLE master_promotion_tab (a int)
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN SELECT * FROM test2 ORDER BY x LOOP
        INSERT INTO test1 (a) VALUES (12/(r.x-2));
        COMMIT;
    END LOOP;
END;
$$
;
----------
[DROP_TABLE] drop table strdbase
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gseg_same(seg, seg, internal)
RETURNS internal
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
