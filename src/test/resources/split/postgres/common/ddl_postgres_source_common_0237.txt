ALTER TABLE regtest_table_part RESET (fillfactor)
;
CREATE TABLE "public"."truncate_b" (
    "id" integer NOT NULL
)
;
-- fail, partial index
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_partial
;
CREATE SCHEMA fkpart10
  CREATE TABLE tbl1(f1 int PRIMARY KEY) PARTITION BY RANGE(f1)
  CREATE TABLE tbl1_p1 PARTITION OF tbl1 FOR VALUES FROM (minvalue) TO (1)
  CREATE TABLE tbl1_p2 PARTITION OF tbl1 FOR VALUES FROM (1) TO (maxvalue)
  CREATE TABLE tbl2(f1 int REFERENCES tbl1 DEFERRABLE INITIALLY DEFERRED)
;
CREATE TABLE "generated_stored_tests"."gtest_child2" (
    "f1" "date" CONSTRAINT "gtest_parent_f1_not_null" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
ALTER INDEX tbspace_reindex_part_index_0 ATTACH PARTITION tbspace_reindex_part_index_0_1
;
-- Disable fastupdate, and do more insertions. With fastupdate enabled, most
-- insertions (by flushing the list pages) cause page splits. Without
-- fastupdate, we get more churn in the GIN data leaf pages, and exercise the
-- recompression codepaths.
alter index gin_test_idx set (fastupdate = off)
;
CREATE TABLE deferred_excl (
  f1 int,
  f2 int,
  CONSTRAINT deferred_excl_con EXCLUDE (f1 WITH =) INITIALLY DEFERRED
)
;
-- Also check what happens when such a trigger runs before or after others
create function f1_times_10() returns trigger as
$$ begin new.f1 := new.f1 * 10; return new; end $$ language plpgsql
;
CREATE OPERATOR CLASS issn_ops DEFAULT
	FOR TYPE issn USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashissn(issn)
;
create table parted_trig2 partition of parted_trig for values in (2)
;
DROP TABLE defprivs
;
-- test statistics and selectivity estimation as well
--
-- We don't check the accuracy of selectivity estimation, but at least check
-- it doesn't fall.
analyze test_range_gist
;
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest1, ptest2))
;
drop function pleast(numeric)
;
ALTER TABLE fk_r_1 DROP CONSTRAINT fk_r_p_id_p_jd_fkey
;
ANALYZE pg_temp_9.pg_temp_38106
;
CREATE EVENT TRIGGER test_event_trigger_guc
	ON sql_drop
	WHEN TAG IN ('DROP POLICY') EXECUTE FUNCTION test_event_trigger_guc()
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_largeobject_metadata
;
CREATE FUNCTION dblink_get_connections()
RETURNS text[]
AS '$libdir/dblink', 'dblink_get_connections'
LANGUAGE C PARALLEL RESTRICTED
;
create rule t1_upd_2 as on update to t1
	where old.a >= 10 and old.a < 20
	do instead
	update t1_2 set a = new.a where a = old.a
;
--Compare function for ltree
CREATE FUNCTION ltree_cmp(ltree,ltree)
RETURNS int4
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
CREATE INDEX concur_reindex_ind4 ON concur_reindex_tab(c1, c1, c2)
;
CREATE TRIGGER trg2 BEFORE INSERT ON t1 EXECUTE FUNCTION foo_trg()
;
ALTER TABLE notnull_tbl1 ADD PRIMARY KEY (a)
;
CREATE TABLE parted_fk_naming_1 (
    id bigint NOT NULL default 1,
    id_abc bigint,
    PRIMARY KEY (id),
    CONSTRAINT dummy_constr CHECK (true)
)
;
ALTER TABLE test_inh_check ADD CONSTRAINT bnoinherit CHECK (b > 100) NO INHERIT
;
CREATE TABLE INTERVAL_TBL (f1 interval)
;
ALTER DATABASE "ecpg1_regression" SET lc_monetary TO 'C'
;
-- cleanup
DROP TRIGGER trig_row_after1 ON rem1
;
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p2_ext DEFAULT
;
CREATE TABLE tx2 (b integer)
;
CREATE FUNCTION _ltree_risparent(_ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE ext_cine_tab1 (x int)
;
CREATE TABLE "fkpart5"."fk4" (
    "a" integer
)
;
CREATE TABLE batch_table ( x int ) PARTITION BY HASH (x)
;
-- Add the NULL partition to plt2_adv
CREATE TABLE plt2_adv_p1_null PARTITION OF plt2_adv FOR VALUES IN (NULL, '0001', '0003')
;
DO $$
declare x text;
begin
  for i in 1..3 loop
    x := data_source(i);
    commit;
  end loop;
  raise notice 'length(x) = %', length(x);
end $$
;
ALTER FUNCTION gbt_date_same(gbtreekey8, gbtreekey8, internal) PARALLEL SAFE
;
CREATE TABLE attmp3 (a int, b int)
;
CREATE INDEX gtest31_2_y_idx ON gtest31_2(((y).b))
;
DROP TYPE IF EXISTS split_maint.status_enum
;
ALTER TABLE ONLY "public"."pitest1" ATTACH PARTITION "public"."pitest1_p1" FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
drop schema c
;
drop table at_tab1, at_tab2
;
CREATE TABLE "public"."prt1_l_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
CREATE FUNCTION word_similarity_commutator_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_pkey
;
create or replace function sc_test() returns setof integer as $$
declare
  c no scroll cursor for select f1 from int4_tbl;
  x integer;
begin
  open c;
  fetch last from c into x;
  while found loop
    return next x;
    fetch prior from c into x;
  end loop;
  close c;
end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] ALTER TABLE regtest_table_part RESET (fillfactor)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."truncate_b" (
    "id" integer NOT NULL
)
;
----------
[ALTER_TABLE] -- fail, partial index
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_partial
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart10
  CREATE TABLE tbl1(f1 int PRIMARY KEY) PARTITION BY RANGE(f1)
  CREATE TABLE tbl1_p1 PARTITION OF tbl1 FOR VALUES FROM (minvalue) TO (1)
  CREATE TABLE tbl1_p2 PARTITION OF tbl1 FOR VALUES FROM (1) TO (maxvalue)
  CREATE TABLE tbl2(f1 int REFERENCES tbl1 DEFERRABLE INITIALLY DEFERRED)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest_child2" (
    "f1" "date" CONSTRAINT "gtest_parent_f1_not_null" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
----------
[ALTER_INDEX] ALTER INDEX tbspace_reindex_part_index_0 ATTACH PARTITION tbspace_reindex_part_index_0_1
;
----------
[ALTER_INDEX] -- Disable fastupdate, and do more insertions. With fastupdate enabled, most
-- insertions (by flushing the list pages) cause page splits. Without
-- fastupdate, we get more churn in the GIN data leaf pages, and exercise the
-- recompression codepaths.
alter index gin_test_idx set (fastupdate = off)
;
----------
[CREATE_TABLE] CREATE TABLE deferred_excl (
  f1 int,
  f2 int,
  CONSTRAINT deferred_excl_con EXCLUDE (f1 WITH =) INITIALLY DEFERRED
)
;
----------
[CREATE_PROG_OBJ] -- Also check what happens when such a trigger runs before or after others
create function f1_times_10() returns trigger as
$$ begin new.f1 := new.f1 * 10; return new; end $$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS issn_ops DEFAULT
	FOR TYPE issn USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashissn(issn)
;
----------
[CREATE_TABLE] create table parted_trig2 partition of parted_trig for values in (2)
;
----------
[DROP_TABLE] DROP TABLE defprivs
;
----------
[ADMIN_TABLE] -- test statistics and selectivity estimation as well
--
-- We don't check the accuracy of selectivity estimation, but at least check
-- it doesn't fall.
analyze test_range_gist
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest1, ptest2))
;
----------
[DROP_PROG_OBJ] drop function pleast(numeric)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE fk_r_1 DROP CONSTRAINT fk_r_p_id_p_jd_fkey
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38106
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER test_event_trigger_guc
	ON sql_drop
	WHEN TAG IN ('DROP POLICY') EXECUTE FUNCTION test_event_trigger_guc()
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_get_connections()
RETURNS text[]
AS '$libdir/dblink', 'dblink_get_connections'
LANGUAGE C PARALLEL RESTRICTED
;
----------
[CREATE_POLICY] create rule t1_upd_2 as on update to t1
	where old.a >= 10 and old.a < 20
	do instead
	update t1_2 set a = new.a where a = old.a
;
----------
[CREATE_PROG_OBJ] --Compare function for ltree
CREATE FUNCTION ltree_cmp(ltree,ltree)
RETURNS int4
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
----------
[ADD_INDEX] CREATE INDEX concur_reindex_ind4 ON concur_reindex_tab(c1, c1, c2)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trg2 BEFORE INSERT ON t1 EXECUTE FUNCTION foo_trg()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE notnull_tbl1 ADD PRIMARY KEY (a)
;
----------
[CREATE_TABLE] CREATE TABLE parted_fk_naming_1 (
    id bigint NOT NULL default 1,
    id_abc bigint,
    PRIMARY KEY (id),
    CONSTRAINT dummy_constr CHECK (true)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE test_inh_check ADD CONSTRAINT bnoinherit CHECK (b > 100) NO INHERIT
;
----------
[CREATE_TABLE] CREATE TABLE INTERVAL_TBL (f1 interval)
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg1_regression" SET lc_monetary TO 'C'
;
----------
[DROP_TRIGGER] -- cleanup
DROP TRIGGER trig_row_after1 ON rem1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p2_ext DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE tx2 (b integer)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_risparent(_ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE ext_cine_tab1 (x int)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."fk4" (
    "a" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE batch_table ( x int ) PARTITION BY HASH (x)
;
----------
[CREATE_TABLE] -- Add the NULL partition to plt2_adv
CREATE TABLE plt2_adv_p1_null PARTITION OF plt2_adv FOR VALUES IN (NULL, '0001', '0003')
;
----------
[BLOCK] DO $$
declare x text;
begin
  for i in 1..3 loop
    x := data_source(i);
    commit;
  end loop;
  raise notice 'length(x) = %', length(x);
end $$
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_date_same(gbtreekey8, gbtreekey8, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE attmp3 (a int, b int)
;
----------
[ADD_INDEX] CREATE INDEX gtest31_2_y_idx ON gtest31_2(((y).b))
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS split_maint.status_enum
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pitest1" ATTACH PARTITION "public"."pitest1_p1" FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[DROP_SCHEMA] drop schema c
;
----------
[DROP_TABLE] drop table at_tab1, at_tab2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_l_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION word_similarity_commutator_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_pkey
;
----------
[CREATE_PROG_OBJ] create or replace function sc_test() returns setof integer as $$
declare
  c no scroll cursor for select f1 from int4_tbl;
  x integer;
begin
  open c;
  fetch last from c into x;
  while found loop
    return next x;
    fetch prior from c into x;
  end loop;
  close c;
end;
$$ language plpgsql
;
