-- Partition table test, for pg_upgrade
CREATE TABLE notnull_part1_upg (a int, b int) PARTITION BY LIST (a)
;
ANALYZE public.persons3
;
alter table idxpart attach partition idxpart2 for values from (10) to (20)
;
CREATE TABLE "public"."gtest_child" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
ANALYZE public.alter_table_under_transition_tables
;
alter table tt2 add column e int
;
ALTER TABLE pg_toast.pg_toast_30921 RENAME TO reind_con_toast
;
TRUNCATE truncate_b RESTART IDENTITY
;
CREATE VIEW temporal3 AS SELECT * FROM t1 LEFT JOIN tt ON t1.num = tt.num2
;
create table parted_tab_part2 partition of parted_tab for values in (2)
;
CREATE TABLE conf_tab (a int PRIMARY key, b int UNIQUE, c int UNIQUE)
;
-- error, NaN disallowed

-- Test in_range for other datetime datatypes

create temp table datetimes(
    id int,
    f_time time,
    f_timetz timetz,
    f_interval interval,
    f_timestamptz timestamptz,
    f_timestamp timestamp
)
;
CREATE TABLE UNIQUE_TBL (i int, t text,
	UNIQUE(i,t))
;
CREATE TABLE arrlp12 PARTITION OF arrlp FOR VALUES IN ('{1}', '{2}')
;
DROP TABLE created_table
;
ALTER TABLE cwi_test ADD UNIQUE USING INDEX cwi_uniq3_idx
;
SECURITY LABEL ON TABLE t4 IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
DROP DOMAIN IF EXISTS split_extension_next.ext_code
;
DROP SUBSCRIPTION mysub
;
create table gs_hash_1 as
select g100, g10, sum(g::numeric), count(*), max(g::text)
from gs_data_1 group by cube (g1000, g100,g10)
;
CREATE TABLE "public"."renamecolumn" (
    "d" integer,
    "w" integer,
    "x" integer
)
;
-- Test invalid not null on inheritance table.
CREATE TABLE notnull_inhparent (i int)
;
create table my_table (i int)
;
CREATE TABLE itest15 (id integer NOT NULL GENERATED ALWAYS AS IDENTITY)
;
CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	-- don't need compress, decompress, or fetch functions
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
create index ͪߩѦ���index1 on ͪߩѦ��� using btree (���)
;
create trigger trig_stmt_after after insert on rem2
	for each statement execute procedure trigger_func()
;
COMMENT ON TYPE dump_test.textrange
					   IS 'comment on range type'
;
-- Test domain input.

-- Note: the point of checking both INSERT and COPY FROM is that INSERT
-- exercises CoerceToDomain while COPY exercises domain_in.

create domain domainvarchar varchar(5)
;
TRUNCATE TABLE reload_output
;
create table hp_prefix_test_p5 partition of hp_prefix_test for values with (modulus 8, remainder 5)
;
CREATE SCHEMA fkpart6
;
create table event_trigger_fire7 (a int)
;
DROP POLICY p3_with_all ON document
;
create function avg_finalfn(state avg_state) returns int4 as
$$
begin
	if state is null then
		return NULL;
	else
		return state.total / state.count;
	end if;
end
$$ language plpgsql
;
-- truncate of system table
TRUNCATE pg_description
;
ANALYZE public.hobbies_r
;
-- test that the insert optimization works if no rows end up inserted
CREATE TABLE brin_insert_optimization (a int)
;
SECURITY LABEL ON FUNCTION auth_func(text,text)
    IS 'system_u:object_r:sepgsql_regtest_trusted_proc_exec_t:s0'
;
CREATE TABLE test_hash (a int, b text)
;
ANALYZE public.gtest28b
;
DROP TABLE list_parted
;
VACUUM ANALYZE tbl_phv
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 OPTIONS (DROP delimiter, SET quote '~', ADD escape '@')
;
ALTER TABLE pg_toast.pg_toast_30680 RENAME TO reind_con_toast
;
create domain ordered_texts as two_texts check((value).f1 <= (value).f2)
;
CALL in_out(2, 1, 2)
;
-- Check behavior of VOID-returning SQL functions

CREATE FUNCTION voidtest1(a int) RETURNS VOID LANGUAGE SQL AS
$$ SELECT a + 1 $$
;
DROP TABLE pg_temp_12.pg_temp_26234_2, pg_temp_12.pg_temp_26234
;
-- Test domains over arrays of composite

create type comptype as (r float8, i float8)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- Partition table test, for pg_upgrade
CREATE TABLE notnull_part1_upg (a int, b int) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] ANALYZE public.persons3
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart2 for values from (10) to (20)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest_child" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
----------
[ADMIN_TABLE] ANALYZE public.alter_table_under_transition_tables
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tt2 add column e int
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30921 RENAME TO reind_con_toast
;
----------
[TRUNCATE_TABLE] TRUNCATE truncate_b RESTART IDENTITY
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW temporal3 AS SELECT * FROM t1 LEFT JOIN tt ON t1.num = tt.num2
;
----------
[CREATE_TABLE] create table parted_tab_part2 partition of parted_tab for values in (2)
;
----------
[CREATE_TABLE] CREATE TABLE conf_tab (a int PRIMARY key, b int UNIQUE, c int UNIQUE)
;
----------
[CREATE_TABLE] -- error, NaN disallowed

-- Test in_range for other datetime datatypes

create temp table datetimes(
    id int,
    f_time time,
    f_timetz timetz,
    f_interval interval,
    f_timestamptz timestamptz,
    f_timestamp timestamp
)
;
----------
[CREATE_TABLE] CREATE TABLE UNIQUE_TBL (i int, t text,
	UNIQUE(i,t))
;
----------
[CREATE_TABLE] CREATE TABLE arrlp12 PARTITION OF arrlp FOR VALUES IN ('{1}', '{2}')
;
----------
[DROP_TABLE] DROP TABLE created_table
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE cwi_test ADD UNIQUE USING INDEX cwi_uniq3_idx
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE t4 IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
----------
[DROP_TYPE] DROP DOMAIN IF EXISTS split_extension_next.ext_code
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION mysub
;
----------
[CREATE_TABLE|SELECT] create table gs_hash_1 as
select g100, g10, sum(g::numeric), count(*), max(g::text)
from gs_data_1 group by cube (g1000, g100,g10)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."renamecolumn" (
    "d" integer,
    "w" integer,
    "x" integer
)
;
----------
[CREATE_TABLE] -- Test invalid not null on inheritance table.
CREATE TABLE notnull_inhparent (i int)
;
----------
[CREATE_TABLE] create table my_table (i int)
;
----------
[CREATE_TABLE] CREATE TABLE itest15 (id integer NOT NULL GENERATED ALWAYS AS IDENTITY)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	-- don't need compress, decompress, or fetch functions
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
----------
[ADD_INDEX] create index ͪߩѦ���index1 on ͪߩѦ��� using btree (���)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_stmt_after after insert on rem2
	for each statement execute procedure trigger_func()
;
----------
[COMMENT_TYPE] COMMENT ON TYPE dump_test.textrange
					   IS 'comment on range type'
;
----------
[CREATE_TYPE] -- Test domain input.

-- Note: the point of checking both INSERT and COPY FROM is that INSERT
-- exercises CoerceToDomain while COPY exercises domain_in.

create domain domainvarchar varchar(5)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE reload_output
;
----------
[CREATE_TABLE] create table hp_prefix_test_p5 partition of hp_prefix_test for values with (modulus 8, remainder 5)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart6
;
----------
[CREATE_TABLE] create table event_trigger_fire7 (a int)
;
----------
[DROP_POLICY] DROP POLICY p3_with_all ON document
;
----------
[CREATE_PROG_OBJ] create function avg_finalfn(state avg_state) returns int4 as
$$
begin
	if state is null then
		return NULL;
	else
		return state.total / state.count;
	end if;
end
$$ language plpgsql
;
----------
[TRUNCATE_TABLE] -- truncate of system table
TRUNCATE pg_description
;
----------
[ADMIN_TABLE] ANALYZE public.hobbies_r
;
----------
[CREATE_TABLE] -- test that the insert optimization works if no rows end up inserted
CREATE TABLE brin_insert_optimization (a int)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON FUNCTION auth_func(text,text)
    IS 'system_u:object_r:sepgsql_regtest_trusted_proc_exec_t:s0'
;
----------
[CREATE_TABLE] CREATE TABLE test_hash (a int, b text)
;
----------
[ADMIN_TABLE] ANALYZE public.gtest28b
;
----------
[DROP_TABLE] DROP TABLE list_parted
;
----------
[ADMIN_TABLE] VACUUM ANALYZE tbl_phv
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 OPTIONS (DROP delimiter, SET quote '~', ADD escape '@')
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30680 RENAME TO reind_con_toast
;
----------
[CREATE_TYPE] create domain ordered_texts as two_texts check((value).f1 <= (value).f2)
;
----------
[CALL_PROG_OBJ] CALL in_out(2, 1, 2)
;
----------
[CREATE_PROG_OBJ] -- Check behavior of VOID-returning SQL functions

CREATE FUNCTION voidtest1(a int) RETURNS VOID LANGUAGE SQL AS
$$ SELECT a + 1 $$
;
----------
[DROP_TABLE] DROP TABLE pg_temp_12.pg_temp_26234_2, pg_temp_12.pg_temp_26234
;
----------
[CREATE_TYPE] -- Test domains over arrays of composite

create type comptype as (r float8, i float8)
;
