CREATE TRANSFORM FOR foo LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
CREATE AGGREGATE testagg4(int2) (SFUNC = int2_sum, STYPE = int8)
;
create schema simple1
;
CREATE INDEX ON dump_test.measurement (city_id, logdate)
;
ALTER TABLE ONLY "public"."mvtest_t"
    ADD CONSTRAINT "mvtest_t_pkey" PRIMARY KEY ("id")
;
ALTER TYPE "public"."rainbow" ADD VALUE 'green'
;
ALTER TABLE ONLY regress_notnull2 ALTER COLUMN a SET NOT NULL
;
-- Test some other exotic foreign key features: MATCH SIMPLE, ON UPDATE/DELETE
-- actions
CREATE TABLE fk_notpartitioned_pk (a int, b int, primary key (a, b))
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_auth_members
;
COMMENT ON RULE rtest_v1_del ON rtest_v1 IS NULL
;
-- error

CREATE TABLE BOOLTBL1 (f1 bool)
;
CREATE TRIGGER instead_of_insert_trig INSTEAD OF INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_ins')
;
-- Check case where the partitioned table has local/remote partitions
CREATE TABLE async_p3 PARTITION OF async_pt FOR VALUES FROM (3000) TO (4000)
;
CREATE SCHEMA temp_view_test
    CREATE TABLE base_table (a int, id int)
    CREATE TABLE base_table2 (a int, id int)
;
/*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
do $$ <<b>> declare c nested_int8s; begin b.c.c2.x = 1; end $$
;
CREATE TABLE fk_partitioned_fk_full_1 PARTITION OF fk_partitioned_fk_full DEFAULT
;
-- error
DROP TABLE gtest_normal, gtest_normal_child
;
ALTER TABLE pitest3 ALTER COLUMN f3 DROP IDENTITY
;
alter table emp rename column salary to ctid
;
alter function alter1.same(alter1.ctype, alter1.ctype) set schema alter2
;
CREATE FUNCTION data_source(i int) RETURNS TEXT LANGUAGE sql
AS 'select f1 from test4' IMMUTABLE
;
CREATE POLICY p1 ON b1 USING (a % 2 = 0)
;
ALTER TABLE pg_toast.pg_toast_30589 RENAME TO reind_con_toast
;
DROP VIEW tid_view_no_ctid
;
CREATE OPERATOR FAMILY alt_opf13 USING hash
;
ALTER TABLE gtest25 ADD COLUMN c int DEFAULT 42,
  ADD COLUMN x int GENERATED ALWAYS AS (c * 4) STORED
;
CREATE CAST (ean13 AS isbn) WITH FUNCTION isbn(ean13)
;
-- error
ALTER SEQUENCE sequence_test4 RESTART WITH 40
;
ALTER TABLE ONLY "public"."gtest22a"
    ADD CONSTRAINT "gtest22a_b_key" UNIQUE ("b")
;
-- check display of function argments in sub-SELECT
CREATE TABLE functest1 (i int)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_publication_namespace
;
ALTER INDEX pg_toast.pg_toast_30579_index RENAME TO reind_con_toast_idx
;
CREATE DOMAIN domain1 AS int DEFAULT 11
;
-- Check that we map relation oids to filenodes and back correctly.  Only
-- display bad mappings so the test output doesn't change all the time.  A
-- filenode function call can return NULL for a relation dropped concurrently
-- with the call's surrounding query, so ignore a NULL mapped_oid for
-- relations that no longer exist after all calls finish.
-- Temporary relations are ignored, as not supported by pg_filenode_relation().
CREATE TEMP TABLE filenode_mapping AS
SELECT
    oid, mapped_oid, reltablespace, relfilenode, relname
FROM pg_class,
    pg_filenode_relation(reltablespace, pg_relation_filenode(oid)) AS mapped_oid
WHERE relkind IN ('r', 'i', 'S', 't', 'm')
  AND relpersistence != 't'
  AND mapped_oid IS DISTINCT FROM oid
;
create function dfunc(int = 1, int = 2) returns int as $$
  select 2;
$$ language sql
;
CREATE FUNCTION gbt_bool_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- fail, but should complain about cantcompare

--
-- Tests for record_{eq,cmp}
--

create type testtype1 as (a int, b int)
;
CREATE PROCEDURE test_proc12(a anyelement, OUT b anyelement, OUT c anyarray)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %', a;
  b := a;
  c := array[a];
END;
$$
;
ANALYZE pg_temp_11.pg_temp_26410_2
;
DROP TYPE bogus
;
-- inh_child1 should have not null constraint
alter table inh_child1 inherit inh_parent
;
CREATE FOREIGN TABLE t1 (
  c1 integer OPTIONS (column_name 'c1'),
  c2 character varying OPTIONS (column_name 'c2') COLLATE pg_catalog."default" NOT NULL
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't1')
;
alter type rewritetype alter attribute a type text cascade
;
DROP TABLE pxtest3
;
CREATE INDEX sro_pidx ON sro_ptab ((sro_ifun(a) + sro_ifun(0)))
	WHERE sro_ifun(a + 10) > sro_ifun(10)
;
CREATE TEMP TABLE PKTABLE (ptest1 int, ptest2 inet,
                           PRIMARY KEY(ptest1, ptest2))
;
CREATE OPERATOR <-> (
	LEFTARG = float8,
	RIGHTARG = float8,
	PROCEDURE = float8_dist,
	COMMUTATOR = '<->'
)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP c11
;
VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_languages
;
------------------------------------------------------------------------------------------
[CREATE_TRANSFORM] CREATE TRANSFORM FOR foo LANGUAGE plperl (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testagg4(int2) (SFUNC = int2_sum, STYPE = int8)
;
----------
[CREATE_SCHEMA] create schema simple1
;
----------
[ADD_INDEX] CREATE INDEX ON dump_test.measurement (city_id, logdate)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."mvtest_t"
    ADD CONSTRAINT "mvtest_t_pkey" PRIMARY KEY ("id")
;
----------
[ALTER_TYPE] ALTER TYPE "public"."rainbow" ADD VALUE 'green'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY regress_notnull2 ALTER COLUMN a SET NOT NULL
;
----------
[CREATE_TABLE] -- Test some other exotic foreign key features: MATCH SIMPLE, ON UPDATE/DELETE
-- actions
CREATE TABLE fk_notpartitioned_pk (a int, b int, primary key (a, b))
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_auth_members
;
----------
[ALTER_POLICY] COMMENT ON RULE rtest_v1_del ON rtest_v1 IS NULL
;
----------
[CREATE_TABLE] -- error

CREATE TABLE BOOLTBL1 (f1 bool)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER instead_of_insert_trig INSTEAD OF INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_ins')
;
----------
[CREATE_TABLE] -- Check case where the partitioned table has local/remote partitions
CREATE TABLE async_p3 PARTITION OF async_pt FOR VALUES FROM (3000) TO (4000)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA temp_view_test
    CREATE TABLE base_table (a int, id int)
    CREATE TABLE base_table2 (a int, id int)
;
----------
[CREATE_POLICY] /*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
----------
[BLOCK] do $$ <<b>> declare c nested_int8s; begin b.c.c2.x = 1; end $$
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_full_1 PARTITION OF fk_partitioned_fk_full DEFAULT
;
----------
[DROP_TABLE] -- error
DROP TABLE gtest_normal, gtest_normal_child
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest3 ALTER COLUMN f3 DROP IDENTITY
;
----------
[RENAME_COLUMN] alter table emp rename column salary to ctid
;
----------
[ALTER_PROG_OBJ] alter function alter1.same(alter1.ctype, alter1.ctype) set schema alter2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION data_source(i int) RETURNS TEXT LANGUAGE sql
AS 'select f1 from test4' IMMUTABLE
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON b1 USING (a % 2 = 0)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30589 RENAME TO reind_con_toast
;
----------
[DROP_VIEW] DROP VIEW tid_view_no_ctid
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf13 USING hash
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest25 ADD COLUMN c int DEFAULT 42,
  ADD COLUMN x int GENERATED ALWAYS AS (c * 4) STORED
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ean13 AS isbn) WITH FUNCTION isbn(ean13)
;
----------
[ALTER_SEQUENCE] -- error
ALTER SEQUENCE sequence_test4 RESTART WITH 40
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest22a"
    ADD CONSTRAINT "gtest22a_b_key" UNIQUE ("b")
;
----------
[CREATE_TABLE] -- check display of function argments in sub-SELECT
CREATE TABLE functest1 (i int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_publication_namespace
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30579_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TYPE] CREATE DOMAIN domain1 AS int DEFAULT 11
;
----------
[CREATE_TABLE|SELECT|METADATA] -- Check that we map relation oids to filenodes and back correctly.  Only
-- display bad mappings so the test output doesn't change all the time.  A
-- filenode function call can return NULL for a relation dropped concurrently
-- with the call's surrounding query, so ignore a NULL mapped_oid for
-- relations that no longer exist after all calls finish.
-- Temporary relations are ignored, as not supported by pg_filenode_relation().
CREATE TEMP TABLE filenode_mapping AS
SELECT
    oid, mapped_oid, reltablespace, relfilenode, relname
FROM pg_class,
    pg_filenode_relation(reltablespace, pg_relation_filenode(oid)) AS mapped_oid
WHERE relkind IN ('r', 'i', 'S', 't', 'm')
  AND relpersistence != 't'
  AND mapped_oid IS DISTINCT FROM oid
;
----------
[CREATE_PROG_OBJ] create function dfunc(int = 1, int = 2) returns int as $$
  select 2;
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bool_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TYPE] -- fail, but should complain about cantcompare

--
-- Tests for record_{eq,cmp}
--

create type testtype1 as (a int, b int)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc12(a anyelement, OUT b anyelement, OUT c anyarray)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'a: %', a;
  b := a;
  c := array[a];
END;
$$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26410_2
;
----------
[DROP_TYPE] DROP TYPE bogus
;
----------
[ALTER_TABLE] -- inh_child1 should have not null constraint
alter table inh_child1 inherit inh_parent
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t1 (
  c1 integer OPTIONS (column_name 'c1'),
  c2 character varying OPTIONS (column_name 'c2') COLLATE pg_catalog."default" NOT NULL
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't1')
;
----------
[ALTER_TYPE] alter type rewritetype alter attribute a type text cascade
;
----------
[DROP_TABLE] DROP TABLE pxtest3
;
----------
[ADD_INDEX] CREATE INDEX sro_pidx ON sro_ptab ((sro_ifun(a) + sro_ifun(0)))
	WHERE sro_ifun(a + 10) > sro_ifun(10)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE PKTABLE (ptest1 int, ptest2 inet,
                           PRIMARY KEY(ptest1, ptest2))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = float8,
	RIGHTARG = float8,
	PROCEDURE = float8_dist,
	COMMUTATOR = '<->'
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP c11
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_languages
;
