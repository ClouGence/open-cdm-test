create table cta_test ( id int )
;
ALTER TABLE regtest_table_3 ENABLE RULE regtest_test_rule
;
ALTER INDEX pg_toast.pg_toast_33302_index RENAME TO reind_con_toast_idx
;
-- to help create a long function
    CREATE FUNCTION exec(p_foo text) RETURNS void LANGUAGE plpgsql AS $$BEGIN EXECUTE p_foo; END;$$
;
--
-- ISSN operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
--
-- Tests for some likely failure cases with combo cmin/cmax mechanism
--
CREATE TEMP TABLE combocidtest (foobar int)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter 'a')
;
DROP TABLE IF EXISTS c1, c2, p
;
CREATE TEMP TABLE parted_b1 partition of parted_b for values from (0) to (10)
;
DROP TRIGGER gtest11 ON gtest26
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "int" WITH "simple"
;
ANALYZE public.pht1_p3
;
-- Verify constraint deferrability
CREATE SCHEMA fkpart9
;
CREATE TABLE t (i int)
;
DROP TABLE SYS_COL_CHECK_TBL
;
-- adding constraints that describe the desired partition constraint
-- (or more restrictive) will help skip the validation scan
CREATE TABLE part_3_4 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IN (3))
)
;
CREATE TABLE timestamptmp (a timestamp)
;
CREATE VIEW ro_view17 AS SELECT * FROM ro_view1
;
ALTER INDEX "public"."idxpart_another_pkey" ATTACH PARTITION "public"."idxpart_another_1_pkey"
;
CREATE FUNCTION "public"."explain_analyze_inc_sort_nodes"("query" "text") RETURNS "jsonb"
    LANGUAGE "plpgsql"
    AS $$
declare
  elements jsonb;
  element jsonb;
  matching_nodes jsonb := '[]'::jsonb;
begin
  execute 'explain (analyze, costs off, summary off, timing off, format ''json'') ' || query into strict elements;
  while jsonb_array_length(elements) > 0 loop
    element := elements->0;
    elements := elements - 0;
    case jsonb_typeof(element)
    when 'array' then
      if jsonb_array_length(element) > 0 then
        elements := elements || element;
      end if;
    when 'object' then
      if element ? 'Plan' then
        elements := elements || jsonb_build_array(element->'Plan');
        element := element - 'Plan';
      else
        if element ? 'Plans' then
          elements := elements || jsonb_build_array(element->'Plans');
          element := element - 'Plans';
        end if;
        if (element->>'Node Type')::text = 'Incremental Sort' then
          matching_nodes := matching_nodes || element;
        end if;
      end if;
    end case;
  end loop;
  return matching_nodes;
end;
$$
;
-- try creating a view and altering that, should fail
create view myview as select * from atacc1
;
ALTER FOREIGN TABLE fd_pt2_1 ALTER c2 SET NOT NULL
;
CREATE FUNCTION _int_contains_joinsel(internal, oid, internal, smallint, internal)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
DROP TABLE c
;
CREATE TABLE agg (a int2, b float4)
;
create trigger iocdu_tt_parted_update_trig
  after update on iocdu_tt_parted referencing old table as old_table new table as new_table
  for each statement execute procedure dump_update()
;
ALTER TEXT SEARCH CONFIGURATION dutch ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
CREATE INDEX gin_check_idx on "gin_check" USING GIN("Column1")
;
create table tt15v_log(o tt15v, n tt15v, incr bool)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=18253 host=/tmp/Gnlu8IK54H dbname=postgres' PUBLICATION tap_pub
;
CREATE SCHEMA regress_roleoption
;
CREATE DOMAIN "public"."sqljsonb_int_not_null" AS integer NOT NULL
;
ALTER TABLE cwi_test DROP CONSTRAINT cwi_uniq_idx,
	ADD CONSTRAINT cwi_replaced_pkey PRIMARY KEY
		USING INDEX cwi_uniq2_idx
;
CREATE INDEX old_system_table__othercol ON old_system_table (othercol)
;
CREATE FUNCTION my_cmp (int4, int4)
RETURNS int LANGUAGE sql AS
$$
	SELECT
		CASE WHEN $1 < $2 THEN -1
				WHEN $1 > $2 THEN  1
				ELSE 0
		END;
$$
;
DROP TABLE foo_parted CASCADE
;
-- Test view decompilation in the face of relation renaming conflicts

CREATE TABLE tt1 (f1 int, f2 int, f3 text)
;
ALTER TABLE pg_toast.pg_toast_30573 RENAME TO reind_con_toast
;
CREATE TABLE "public"."plt1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
/*
 * text search configuration for norwegian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * norwegian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
create unique index key_index on insertconflicttest(key)
;
ALTER INDEX gin_check_idx SET (fastupdate = false)
;
CREATE FUNCTION transaction_test3() RETURNS int
LANGUAGE plpgsql
AS $$
BEGIN
    CALL transaction_test1(9, 'error');
    RETURN 1;
END;
$$
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_amop
;
CREATE TABLE gtest23a (x int PRIMARY KEY, y int)
;
alter table pgbench_history add constraint pgbench_history_aid_fkey foreign key (aid) references pgbench_accounts
;
CREATE SCHEMA element_test
  CREATE TABLE foo (id int)
  CREATE VIEW bar AS SELECT * FROM foo
;
CREATE SCHEMA split_trigger_probe
;
DROP OPERATOR CLASS test_operator_class USING no_such_am
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table cta_test ( id int )
;
----------
[ALTER_TABLE] ALTER TABLE regtest_table_3 ENABLE RULE regtest_test_rule
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33302_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] -- to help create a long function
    CREATE FUNCTION exec(p_foo text) RETURNS void LANGUAGE plpgsql AS $$BEGIN EXECUTE p_foo; END;$$
;
----------
[CREATE_PROG_OBJ] --
-- ISSN operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] --
-- Tests for some likely failure cases with combo cmin/cmax mechanism
--
CREATE TEMP TABLE combocidtest (foobar int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter 'a')
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS c1, c2, p
;
----------
[CREATE_TABLE] CREATE TEMP TABLE parted_b1 partition of parted_b for values from (0) to (10)
;
----------
[DROP_TRIGGER] DROP TRIGGER gtest11 ON gtest26
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "int" WITH "simple"
;
----------
[ADMIN_TABLE] ANALYZE public.pht1_p3
;
----------
[CREATE_SCHEMA] -- Verify constraint deferrability
CREATE SCHEMA fkpart9
;
----------
[CREATE_TABLE] CREATE TABLE t (i int)
;
----------
[DROP_TABLE] DROP TABLE SYS_COL_CHECK_TBL
;
----------
[CREATE_TABLE] -- adding constraints that describe the desired partition constraint
-- (or more restrictive) will help skip the validation scan
CREATE TABLE part_3_4 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IN (3))
)
;
----------
[CREATE_TABLE] CREATE TABLE timestamptmp (a timestamp)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view17 AS SELECT * FROM ro_view1
;
----------
[ALTER_INDEX] ALTER INDEX "public"."idxpart_another_pkey" ATTACH PARTITION "public"."idxpart_another_1_pkey"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_analyze_inc_sort_nodes"("query" "text") RETURNS "jsonb"
    LANGUAGE "plpgsql"
    AS $$
declare
  elements jsonb;
  element jsonb;
  matching_nodes jsonb := '[]'::jsonb;
begin
  execute 'explain (analyze, costs off, summary off, timing off, format ''json'') ' || query into strict elements;
  while jsonb_array_length(elements) > 0 loop
    element := elements->0;
    elements := elements - 0;
    case jsonb_typeof(element)
    when 'array' then
      if jsonb_array_length(element) > 0 then
        elements := elements || element;
      end if;
    when 'object' then
      if element ? 'Plan' then
        elements := elements || jsonb_build_array(element->'Plan');
        element := element - 'Plan';
      else
        if element ? 'Plans' then
          elements := elements || jsonb_build_array(element->'Plans');
          element := element - 'Plans';
        end if;
        if (element->>'Node Type')::text = 'Incremental Sort' then
          matching_nodes := matching_nodes || element;
        end if;
      end if;
    end case;
  end loop;
  return matching_nodes;
end;
$$
;
----------
[CREATE_VIEW(SELECT)] -- try creating a view and altering that, should fail
create view myview as select * from atacc1
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE fd_pt2_1 ALTER c2 SET NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_contains_joinsel(internal, oid, internal, smallint, internal)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE c
;
----------
[CREATE_TABLE] CREATE TABLE agg (a int2, b float4)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger iocdu_tt_parted_update_trig
  after update on iocdu_tt_parted referencing old table as old_table new table as new_table
  for each statement execute procedure dump_update()
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION dutch ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ADD_INDEX] CREATE INDEX gin_check_idx on "gin_check" USING GIN("Column1")
;
----------
[CREATE_TABLE] create table tt15v_log(o tt15v, n tt15v, incr bool)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=18253 host=/tmp/Gnlu8IK54H dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_roleoption
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."sqljsonb_int_not_null" AS integer NOT NULL
;
----------
[ALTER_TABLE|DROP_CONSTRAINT|ADD_CONSTRAINT] ALTER TABLE cwi_test DROP CONSTRAINT cwi_uniq_idx,
	ADD CONSTRAINT cwi_replaced_pkey PRIMARY KEY
		USING INDEX cwi_uniq2_idx
;
----------
[ADD_INDEX] CREATE INDEX old_system_table__othercol ON old_system_table (othercol)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION my_cmp (int4, int4)
RETURNS int LANGUAGE sql AS
$$
	SELECT
		CASE WHEN $1 < $2 THEN -1
				WHEN $1 > $2 THEN  1
				ELSE 0
		END;
$$
;
----------
[DROP_TABLE] DROP TABLE foo_parted CASCADE
;
----------
[CREATE_TABLE] -- Test view decompilation in the face of relation renaming conflicts

CREATE TABLE tt1 (f1 int, f2 int, f3 text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30573 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
----------
[CREATE_POLICY] /*
 * text search configuration for norwegian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * norwegian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
----------
[ADD_INDEX] create unique index key_index on insertconflicttest(key)
;
----------
[ALTER_INDEX] ALTER INDEX gin_check_idx SET (fastupdate = false)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION transaction_test3() RETURNS int
LANGUAGE plpgsql
AS $$
BEGIN
    CALL transaction_test1(9, 'error');
    RETURN 1;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_amop
;
----------
[CREATE_TABLE] CREATE TABLE gtest23a (x int PRIMARY KEY, y int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_history add constraint pgbench_history_aid_fkey foreign key (aid) references pgbench_accounts
;
----------
[CREATE_SCHEMA(SELECT)] CREATE SCHEMA element_test
  CREATE TABLE foo (id int)
  CREATE VIEW bar AS SELECT * FROM foo
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_trigger_probe
;
----------
[DROP_PROG_OBJ] DROP OPERATOR CLASS test_operator_class USING no_such_am
;
