ALTER TABLE public.pagg_tab_p3 OWNER TO zyc
;
ALTER TABLE generated_stored_tests.gtest3 OWNER TO zyc
;
COPY public.test_mix_2 (a, b, c) TO STDOUT
;
COPY public.test_type_diff_c (f1, extra, f2) TO stdout
;
COPY public.ne_check_tbl (x) TO stdout
;
-- is a superuser
CREATE ROLE regress_file_fdw_user LOGIN
;
SET client_encoding TO EUC_JP
;
DROP USER regress_conversion_user
;
LOCK TABLE public.rc_test IN ACCESS SHARE MODE
;
-- SET statements.
-- These use two different strings, still they count as one entry.
SET work_mem = '1MB'
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE public.pagg_tab_p3 OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE generated_stored_tests.gtest3 OWNER TO zyc
;
----------
[DATA_EXPORT] COPY public.test_mix_2 (a, b, c) TO STDOUT
;
----------
[DATA_EXPORT] COPY public.test_type_diff_c (f1, extra, f2) TO stdout
;
----------
[DATA_EXPORT] COPY public.ne_check_tbl (x) TO stdout
;
----------
[CREATE_ROLE] -- is a superuser
CREATE ROLE regress_file_fdw_user LOGIN
;
----------
[SESSION_SETTING_WRITE] SET client_encoding TO EUC_JP
;
----------
[DROP_USER] DROP USER regress_conversion_user
;
----------
[SESSION_LOCK] LOCK TABLE public.rc_test IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] -- SET statements.
-- These use two different strings, still they count as one entry.
SET work_mem = '1MB'
;
