CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."ihighway" INHERIT "public"."road"
;
create table bar (f1 int, f2 int)
;
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, create_slot = true)
;
-- multi-arg polymorphic
CREATE FUNCTION sum3(anyelement,anyelement,anyelement) returns anyelement AS
'select $1+$2+$3' language sql strict
;
-- cannot create a permanent rel as partition of a temp rel
CREATE TEMP TABLE temp_parted (
	a int
) PARTITION BY LIST (a)
;
VACUUM information_schema.sql_implementation_info
;
create temp table prtx1 (a integer, b integer, c integer)
  partition by range (a)
;
CREATE OPERATOR CLASS timestamptz_ops
DEFAULT FOR TYPE timestamptz USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       timestamptz_cmp(timestamptz,timestamptz),
    FUNCTION        2       gin_extract_value_timestamptz(timestamptz, internal),
    FUNCTION        3       gin_extract_query_timestamptz(timestamptz, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_timestamptz(timestamptz,timestamptz,int2, internal),
STORAGE         timestamptz
;
create table boolpart (a bool) partition by list (a)
;
ANALYZE pg_temp_26.pg_temp_27076_2
;
-- error
ALTER INDEX dummy_test_idx SET (option_real = 'val5')
;
-- fail, because of the row in parent
alter table parent_noinh_convalid validate constraint check_a_is_2
;
COMMENT ON DATABASE template0 IS 'unmodifiable empty database'
;
CREATE TEMP TABLE pg_temp_9.pg_temp_38160_2 (tid pg_catalog.tid)
;
ALTER TABLE pg_temp_18.pg_temp_26711_2 ADD COLUMN newdata pg_temp_18.pg_temp_26711
;
-- Type mixing, function returns int8
create domain ddef3 text DEFAULT 5
;
CREATE INDEX CONCURRENTLY jidx ON testjsonb USING gin (j jsonb_path_ops)
;
-- test: row expr in RETURN NEXT statement.
create or replace function compos() returns setof compostype as $$
begin
  for i in 1..3
  loop
    return next (1, 'hello'::varchar);
  end loop;
  return next null::compostype;
  return next (2, 'goodbye')::compostype;
end;
$$ language plpgsql
;
/* contrib/amcheck/amcheck--1.0--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- In order to avoid issues with dependencies when updating amcheck to 1.1,
-- create new, overloaded versions of the 1.0 functions

--
-- bt_index_check()
--
CREATE FUNCTION bt_index_check(index regclass,
    heapallindexed boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE "public"."pht2_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
create index numrange_test2_hash_idx on numrange_test2 using hash (nr)
;
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS '...invalid label...'
;
ANALYZE vactst, vactst
;
CREATE VIEW pg_stat_subscription AS
    SELECT
            su.oid AS subid,
            su.subname,
            st.pid,
            st.leader_pid,
            st.relid,
            st.received_lsn,
            st.last_msg_send_time,
            st.last_msg_receipt_time,
            st.latest_end_lsn,
            st.latest_end_time
    FROM pg_subscription su
            LEFT JOIN pg_stat_get_subscription(NULL) st
                      ON (st.subid = su.oid)
;
CREATE TABLE notnull_tbl4_lk (LIKE notnull_tbl4)
;
-- Test the case where the number of incoming subtree path keys is more than
-- the number of grouping keys.
CREATE INDEX btg_y_x_w_idx ON btg(y, x, w)
;
drop function f()
;
CREATE FUNCTION issn(ean13)
RETURNS issn
AS '$libdir/isn', 'issn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
--reset the value of workers for each table as it was before this test.
alter table tenk1 set (parallel_workers = 4)
;
-- In 1.6, it got restricted back to superusers.
AlTER EXTENSION pg_stat_statements UPDATE TO '1.6'
;
ALTER TABLE itest5 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
ANALYZE information_schema.sql_implementation_info
;
create function return_from_while() returns int language plpgsql as $$
declare i int := 0;
begin
  while i < 10 loop
    if i > 2 then
      return i;
    end if;
    i := i + 1;
  end loop;
  return null;
end$$
;
alter database abc with allow_connections true
;
ANALYZE prt2_e
;
-- cannot specify null values in range bounds
CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES FROM (null) TO (maxvalue)
;
CREATE TABLE "public"."tableam_parted_b_heap2" (
    "a" "text",
    "b" integer
)
;
CREATE INDEX test_replica_identity_keyab ON test_replica_identity (keya, keyb)
;
CREATE FOREIGN TABLE fpagg_tab_p2 PARTITION OF pagg_tab FOR VALUES FROM (10) TO (20) SERVER loopback OPTIONS (table_name 'pagg_tab_p2')
;
ALTER TABLE gtest27 ALTER COLUMN x TYPE numeric
;
ALTER TABLE gtest25 ADD COLUMN x int GENERATED ALWAYS AS (z * 4) STORED
;
/*
 * 5. REINDEX
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
-- Remove table trigger to allow updates
DROP TRIGGER before_upd_a_row_trig ON main_table
;
alter table trigtest drop column b
;
ALTER TABLE pg_temp_24.pg_temp_27043_2 ADD COLUMN newdata pg_temp_24.pg_temp_27043
;
-- FOREIGN KEY CONSTRAINT adding TEST

CREATE TABLE attmp2 (a int primary key)
;
-- WITH referenced by MERGE statement
CREATE TABLE m AS SELECT i AS k, (i || ' v')::text v FROM generate_series(1, 16, 3) i
;
-- pxtest3 was already dropped
DROP TABLE pxtest4
;
CREATE TABLE test_1 AS
    SELECT x.id::bigint,
           repeat('a', 1600) AS value
    FROM generate_series(1, 100) AS x(id)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."ihighway" INHERIT "public"."road"
;
----------
[CREATE_TABLE] create table bar (f1 int, f2 int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, create_slot = true)
;
----------
[CREATE_PROG_OBJ] -- multi-arg polymorphic
CREATE FUNCTION sum3(anyelement,anyelement,anyelement) returns anyelement AS
'select $1+$2+$3' language sql strict
;
----------
[CREATE_TABLE] -- cannot create a permanent rel as partition of a temp rel
CREATE TEMP TABLE temp_parted (
	a int
) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM information_schema.sql_implementation_info
;
----------
[CREATE_TABLE] create temp table prtx1 (a integer, b integer, c integer)
  partition by range (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS timestamptz_ops
DEFAULT FOR TYPE timestamptz USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       timestamptz_cmp(timestamptz,timestamptz),
    FUNCTION        2       gin_extract_value_timestamptz(timestamptz, internal),
    FUNCTION        3       gin_extract_query_timestamptz(timestamptz, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_timestamptz(timestamptz,timestamptz,int2, internal),
STORAGE         timestamptz
;
----------
[CREATE_TABLE] create table boolpart (a bool) partition by list (a)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_26.pg_temp_27076_2
;
----------
[ALTER_INDEX] -- error
ALTER INDEX dummy_test_idx SET (option_real = 'val5')
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- fail, because of the row in parent
alter table parent_noinh_convalid validate constraint check_a_is_2
;
----------
[COMMENT_CATALOG] COMMENT ON DATABASE template0 IS 'unmodifiable empty database'
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_38160_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_18.pg_temp_26711_2 ADD COLUMN newdata pg_temp_18.pg_temp_26711
;
----------
[CREATE_TYPE] -- Type mixing, function returns int8
create domain ddef3 text DEFAULT 5
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY jidx ON testjsonb USING gin (j jsonb_path_ops)
;
----------
[CREATE_PROG_OBJ] -- test: row expr in RETURN NEXT statement.
create or replace function compos() returns setof compostype as $$
begin
  for i in 1..3
  loop
    return next (1, 'hello'::varchar);
  end loop;
  return next null::compostype;
  return next (2, 'goodbye')::compostype;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] /* contrib/amcheck/amcheck--1.0--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- In order to avoid issues with dependencies when updating amcheck to 1.1,
-- create new, overloaded versions of the 1.0 functions

--
-- bt_index_check()
--
CREATE FUNCTION bt_index_check(index regclass,
    heapallindexed boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht2_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ADD_INDEX] create index numrange_test2_hash_idx on numrange_test2 using hash (nr)
;
----------
[UNKNOWN] SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS '...invalid label...'
;
----------
[ADMIN_TABLE] ANALYZE vactst, vactst
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_subscription AS
    SELECT
            su.oid AS subid,
            su.subname,
            st.pid,
            st.leader_pid,
            st.relid,
            st.received_lsn,
            st.last_msg_send_time,
            st.last_msg_receipt_time,
            st.latest_end_lsn,
            st.latest_end_time
    FROM pg_subscription su
            LEFT JOIN pg_stat_get_subscription(NULL) st
                      ON (st.subid = su.oid)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl4_lk (LIKE notnull_tbl4)
;
----------
[ADD_INDEX] -- Test the case where the number of incoming subtree path keys is more than
-- the number of grouping keys.
CREATE INDEX btg_y_x_w_idx ON btg(y, x, w)
;
----------
[DROP_PROG_OBJ] drop function f()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION issn(ean13)
RETURNS issn
AS '$libdir/isn', 'issn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE] --reset the value of workers for each table as it was before this test.
alter table tenk1 set (parallel_workers = 4)
;
----------
[ALTER_LIBRARY] -- In 1.6, it got restricted back to superusers.
AlTER EXTENSION pg_stat_statements UPDATE TO '1.6'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest5 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[ADMIN_TABLE] ANALYZE information_schema.sql_implementation_info
;
----------
[CREATE_PROG_OBJ] create function return_from_while() returns int language plpgsql as $$
declare i int := 0;
begin
  while i < 10 loop
    if i > 2 then
      return i;
    end if;
    i := i + 1;
  end loop;
  return null;
end$$
;
----------
[ALTER_CATALOG] alter database abc with allow_connections true
;
----------
[ADMIN_TABLE] ANALYZE prt2_e
;
----------
[CREATE_TABLE] -- cannot specify null values in range bounds
CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES FROM (null) TO (maxvalue)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tableam_parted_b_heap2" (
    "a" "text",
    "b" integer
)
;
----------
[ADD_INDEX] CREATE INDEX test_replica_identity_keyab ON test_replica_identity (keya, keyb)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE fpagg_tab_p2 PARTITION OF pagg_tab FOR VALUES FROM (10) TO (20) SERVER loopback OPTIONS (table_name 'pagg_tab_p2')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest27 ALTER COLUMN x TYPE numeric
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest25 ADD COLUMN x int GENERATED ALWAYS AS (z * 4) STORED
;
----------
[CREATE_TABLE] /*
 * 5. REINDEX
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
----------
[DROP_TRIGGER] -- Remove table trigger to allow updates
DROP TRIGGER before_upd_a_row_trig ON main_table
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table trigtest drop column b
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_24.pg_temp_27043_2 ADD COLUMN newdata pg_temp_24.pg_temp_27043
;
----------
[CREATE_TABLE] -- FOREIGN KEY CONSTRAINT adding TEST

CREATE TABLE attmp2 (a int primary key)
;
----------
[CREATE_TABLE|SELECT] -- WITH referenced by MERGE statement
CREATE TABLE m AS SELECT i AS k, (i || ' v')::text v FROM generate_series(1, 16, 3) i
;
----------
[DROP_TABLE] -- pxtest3 was already dropped
DROP TABLE pxtest4
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_1 AS
    SELECT x.id::bigint,
           repeat('a', 1600) AS value
    FROM generate_series(1, 100) AS x(id)
;
