insert into range_parted values ('b', 20)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '16410'
;
INSERT INTO unique_tbl (t) VALUES ('six')
;
INSERT INTO fkpart11.fk_another VALUES (1), (3)
;
INSERT INTO gtest29 (a, b) VALUES (6, 66)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest_tableoid"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
;
-- ok
DELETE FROM t1p WHERE q IS NULL
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (6, 1, 15241, -1725, CURRENT_TIMESTAMP)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1480', relminmxid = '1'
WHERE oid = '"public"."nummultirange_test"'::pg_catalog.regclass
;
DELETE FROM tab_replidentity_index WHERE a = 2
;
INSERT INTO public.ths_p2 VALUES (3, 603)
;
-- Works (does upsert)
INSERT INTO hats VALUES ('h8', 'black') RETURNING *
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."fk_partitioned_fk_2_1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
INSERT INTO upsert VALUES (1, 11, 111)
                  ON CONFLICT (i) DO UPDATE SET k = EXCLUDED.k
;
INSERT INTO BOX_TBL (f1) VALUES ('(2.5, 2.5, 2.5,3.5)')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."itest5"'::pg_catalog.regclass
;
UPDATE pgbench_accounts SET abalance = abalance + 2768 WHERE aid = 3850
;
-- ******************************
-- * Square root check
-- ******************************
DELETE FROM num_result
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15866', relminmxid = '3'
WHERE oid = '36726'
;
-- no error here
INSERT INTO fktable VALUES (100, 200)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt4_n_p1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14963', relminmxid = '3'
WHERE oid = '"public"."event_trigger_test"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14187', relminmxid = '3'
WHERE oid = '"public"."query"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."cc1"'::pg_catalog.regclass
;
UPDATE public.loct SET a = 1 WHERE ((b = 'foo'))
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8132', relminmxid = '1'
WHERE oid = '27174'
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
insert into parent values (1, 'AAA')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '967', relminmxid = '1'
WHERE oid = '"public"."num_exp_ln"'::pg_catalog.regclass
;
insert into ��ג�������ђ�� values('������Ԓ�咡������ǒ�������ג�쒥�','���A01���')
;
INSERT INTO public.ths_p1 VALUES (8, 808)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8755', relminmxid = '1'
WHERE oid = '"public"."rtest_emplog"'::pg_catalog.regclass
;
update rtest_t1 set a = a + 10 where b isnull
;
update PSlot set slotlink = 'XX.illegal' where slotname = 'PS.base.a1'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18972', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9131', relminmxid = '1'
WHERE oid = '"public"."brin_test_multi"'::pg_catalog.regclass
;
INSERT INTO public.mvtest_mv SELECT (diff.newdata).* FROM pg_temp_10.pg_temp_26213_2 diff WHERE tid IS NULL
;
INSERT INTO public.tht VALUES ('9', 319)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4206', relminmxid = '1'
WHERE oid = '"public"."ne_check_tbl"'::pg_catalog.regclass
;
INSERT INTO pg_temp_4.pg_temp_37830_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_4.pg_temp_37830 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_4.pg_temp_37830 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
insert into rtest_nothn4 values (10, 'don''t want this')
;
INSERT INTO test_dropped VALUES(default)
;
-- doesn't match PK, but no error yet
INSERT INTO fktable VALUES (2, 20)
;
DELETE FROM rw_view1 WHERE aa=3
;
INSERT INTO num_exp_sub VALUES (3,3,'0')
;
insert into mcrparted values ('aaa', 0), ('b', 0), ('bz', 10), ('c', -10),
    ('comm', -10), ('common', -10), ('common', 0), ('common', 10),
    ('commons', 0), ('d', -10), ('e', 0)
;
-- fail
update dcomptable set d1.r = (d1).r - 1, d1.i = (d1).i + 1 where (d1).i > 0
;
INSERT INTO undroppable_objs VALUES
('table', 'schema_one.table_three'),
('table', 'audit_tbls.schema_two_table_three')
;
INSERT INTO public.tht VALUES ('4', 914)
;
------------------------------------------------------------------------------------------
[INSERT] insert into range_parted values ('b', 20)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '16410'
;
----------
[INSERT|SELECT] INSERT INTO unique_tbl (t) VALUES ('six')
;
----------
[INSERT] INSERT INTO fkpart11.fk_another VALUES (1), (3)
;
----------
[INSERT|SELECT] INSERT INTO gtest29 (a, b) VALUES (6, 66)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest_tableoid"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
;
----------
[DELETE] -- ok
DELETE FROM t1p WHERE q IS NULL
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (6, 1, 15241, -1725, CURRENT_TIMESTAMP)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1480', relminmxid = '1'
WHERE oid = '"public"."nummultirange_test"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM tab_replidentity_index WHERE a = 2
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 603)
;
----------
[INSERT] -- Works (does upsert)
INSERT INTO hats VALUES ('h8', 'black') RETURNING *
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."fk_partitioned_fk_2_1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[MERGE] INSERT INTO upsert VALUES (1, 11, 111)
                  ON CONFLICT (i) DO UPDATE SET k = EXCLUDED.k
;
----------
[INSERT|SELECT] INSERT INTO BOX_TBL (f1) VALUES ('(2.5, 2.5, 2.5,3.5)')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."itest5"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE pgbench_accounts SET abalance = abalance + 2768 WHERE aid = 3850
;
----------
[DELETE] -- ******************************
-- * Square root check
-- ******************************
DELETE FROM num_result
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15866', relminmxid = '3'
WHERE oid = '36726'
;
----------
[INSERT] -- no error here
INSERT INTO fktable VALUES (100, 200)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt4_n_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14963', relminmxid = '3'
WHERE oid = '"public"."event_trigger_test"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14187', relminmxid = '3'
WHERE oid = '"public"."query"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."cc1"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE public.loct SET a = 1 WHERE ((b = 'foo'))
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8132', relminmxid = '1'
WHERE oid = '27174'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] insert into parent values (1, 'AAA')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '967', relminmxid = '1'
WHERE oid = '"public"."num_exp_ln"'::pg_catalog.regclass
;
----------
[INSERT] insert into ��ג�������ђ�� values('������Ԓ�咡������ǒ�������ג�쒥�','���A01���')
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 808)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8755', relminmxid = '1'
WHERE oid = '"public"."rtest_emplog"'::pg_catalog.regclass
;
----------
[UPDATE] update rtest_t1 set a = a + 10 where b isnull
;
----------
[UPDATE] update PSlot set slotlink = 'XX.illegal' where slotname = 'PS.base.a1'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '18972', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9131', relminmxid = '1'
WHERE oid = '"public"."brin_test_multi"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_mv SELECT (diff.newdata).* FROM pg_temp_10.pg_temp_26213_2 diff WHERE tid IS NULL
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 319)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '4206', relminmxid = '1'
WHERE oid = '"public"."ne_check_tbl"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_4.pg_temp_37830_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_4.pg_temp_37830 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_4.pg_temp_37830 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] insert into rtest_nothn4 values (10, 'don''t want this')
;
----------
[INSERT] INSERT INTO test_dropped VALUES(default)
;
----------
[INSERT] -- doesn't match PK, but no error yet
INSERT INTO fktable VALUES (2, 20)
;
----------
[DELETE] DELETE FROM rw_view1 WHERE aa=3
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (3,3,'0')
;
----------
[INSERT] insert into mcrparted values ('aaa', 0), ('b', 0), ('bz', 10), ('c', -10),
    ('comm', -10), ('common', -10), ('common', 0), ('common', 10),
    ('commons', 0), ('d', -10), ('e', 0)
;
----------
[UPDATE] -- fail
update dcomptable set d1.r = (d1).r - 1, d1.i = (d1).i + 1 where (d1).i > 0
;
----------
[INSERT] INSERT INTO undroppable_objs VALUES
('table', 'schema_one.table_three'),
('table', 'audit_tbls.schema_two_table_three')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 914)
;
