create table s3.p2_1 partition of s3.p2 for values in (1, 2, 3)
;
-- we can alter streaming when two_phase enabled
ALTER SUBSCRIPTION regress_testsub SET (streaming = true)
;
DO 'DECLARE loid oid; BEGIN SELECT tbl.loid INTO loid FROM lotest_stash_values tbl; PERFORM lo_export(loid, ''/src/src/test/regress/results/invalid/path''); EXCEPTION WHEN UNDEFINED_FILE THEN RAISE NOTICE ''could not open file, as expected''; END'
;
create trigger my_trig
  after insert on my_table
  for each row execute procedure funcA()
;
CREATE FUNCTION "public"."sum3"(bigint, bigint, bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$select $1 + $2 + $3$_$
;
ALTER FUNCTION gbt_int2_compress(internal) PARALLEL SAFE
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "hword_part" WITH "public"."ispell", "english_stem"
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26115_2 (tid pg_catalog.tid)
;
CREATE FUNCTION gin_extract_value_char("char", internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE TABLE collate_dep_test4t (a int, b text)
;
ALTER FUNCTION bt_page_items(text, int8) PARALLEL RESTRICTED
;
/*
 * text search configuration for serbian language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * serbian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY serbian_stem
	(TEMPLATE = snowball, Language = serbian )
;
drop domain intdom1
;
CREATE SUBSCRIPTION regress_sub5 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub5
;
CREATE OR REPLACE FUNCTION get_from_partitioned_table(partitioned_table.a%type)
RETURNS partitioned_table AS $$
DECLARE
    a_val partitioned_table.a%TYPE;
    result partitioned_table%ROWTYPE;
BEGIN
    a_val := $1;
    SELECT * INTO result FROM partitioned_table WHERE a = a_val;
    RETURN result;
END; $$ LANGUAGE plpgsql
;
CREATE OPERATOR ~ (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contained,
	COMMUTATOR = '@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ALTER TABLE pg_class ADD PRIMARY KEY USING INDEX pg_class_oid_index
;
drop table pp_arrpart
;
CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=18237 host=/tmp/6EQjqv0kuf dbname=postgres' PUBLICATION pub5
;
CREATE FUNCTION "public"."tg_hub_adjustslots"("hname" character, "oldnslots" integer, "newnslots" integer) RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
begin
    if newnslots = oldnslots then
        return 0;
    end if;
    if newnslots < oldnslots then
        delete from HSlot where hubname = hname and slotno > newnslots;
	return 0;
    end if;
    for i in oldnslots + 1 .. newnslots loop
        insert into HSlot (slotname, hubname, slotno, slotlink)
		values ('HS.dummy', hname, i, '');
    end loop;
    return 0;
end
$$
;
CREATE DOMAIN queryfuncs_test_domain AS text CHECK (value <> 'foo')
;
ALTER TABLE ONLY "public"."prt2_l" ATTACH PARTITION "public"."prt2_l_p3" FOR VALUES FROM (500) TO (600)
;
CREATE INDEX t1_gist ON s4.t1 USING GIST (b)
;
ALTER TABLE has_volatile ALTER COLUMN col5 TYPE float8
;
-- Load a file
CREATE TABLE lotest_stash_values (loid oid, fd integer)
;
CREATE VIEW "public"."street" AS
 SELECT "r"."name",
    "r"."thepath",
    "c"."cname"
   FROM ONLY "public"."road" "r",
    "public"."real_city" "c"
  WHERE ("c"."outline" OPERATOR("public".##) "r"."thepath")
;
CREATE FUNCTION delete(hstore,hstore)
RETURNS hstore
AS '$libdir/hstore','hstore_delete_hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE DATABASE "foobarX" TEMPLATE template0 LOCALE_PROVIDER xyz
;
create table idxpart2 partition of idxpart for values from (500) to (1000)
;
DROP INDEX onek_unique1_constraint
;
CREATE INDEX "vacuum_in_leader_small_index" ON "public"."parallel_vacuum_table" USING "btree" ((1))
;
CREATE TABLE hpart_1 PARTITION OF hash_parted FOR VALUES WITH (MODULUS 4, REMAINDER 0)
;
CREATE SUBSCRIPTION tap_sub_C
	CONNECTION 'port=13200 host=/tmp/MRHxhpCYrn dbname=postgres application_name=tap_sub_C'
	PUBLICATION tap_pub_B
	WITH (two_phase = on, streaming = off)
;
alter table parted_irreg drop column fd, drop column fd2
;
CREATE TABLE "public"."reloptions_test" (
    "s" character varying
)
WITH ("autovacuum_vacuum_cost_delay"='24', "fillfactor"='40', toast."autovacuum_vacuum_cost_delay"='23')
;
CREATE FUNCTION gbt_cash_distance(internal,money,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE cmdata2 (f1 int)
;
drop function mki4(int)
;
COMMENT ON FUNCTION tg_hub_adjustslots_wrong(bpchar, integer, integer) IS 'function with args'
;
drop domain connotnull cascade
;
ANALYZE pg_temp_13.pg_temp_26306_2
;
CREATE TABLE "public"."city" (
    "name" "name",
    "location" "box",
    "budget" "public"."city_budget"
)
;
CREATE TRIGGER test_trigger
		   AFTER INSERT ON dump_test.measurement
		   FOR EACH ROW EXECUTE PROCEDURE dump_test.trigger_func()
;
CREATE OPERATOR ^@ (
        LEFTARG = ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = ltxtq_exec,
	COMMUTATOR = '^@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
-- VIEW trigger function
CREATE OR REPLACE FUNCTION view_trigger() RETURNS trigger
LANGUAGE plpgsql AS $$
declare
    argstr text := '';
begin
    for i in 0 .. TG_nargs - 1 loop
        if i > 0 then
            argstr := argstr || ', ';
        end if;
        argstr := argstr || TG_argv[i];
    end loop;

    raise notice '% % % % (%)', TG_RELNAME, TG_WHEN, TG_OP, TG_LEVEL, argstr;

    if TG_LEVEL = 'ROW' then
        if TG_OP = 'INSERT' then
            raise NOTICE 'NEW: %', NEW;
            INSERT INTO main_table VALUES (NEW.a, NEW.b);
            RETURN NEW;
        end if;

        if TG_OP = 'UPDATE' then
            raise NOTICE 'OLD: %, NEW: %', OLD, NEW;
            UPDATE main_table SET a = NEW.a, b = NEW.b WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN NEW;
        end if;

        if TG_OP = 'DELETE' then
            raise NOTICE 'OLD: %', OLD;
            DELETE FROM main_table WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN OLD;
        end if;
    end if;

    RETURN NULL;
end;
$$
;
CREATE TABLE sch2.t1 (c1 int)
;
CREATE TYPE text_w_default
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_description
;
do $$
declare x var_record_nn := null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
ALTER TABLE pg_foreign_server ADD PRIMARY KEY USING INDEX pg_foreign_server_oid_index
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table s3.p2_1 partition of s3.p2 for values in (1, 2, 3)
;
----------
[ALTER_PUB_SUB] -- we can alter streaming when two_phase enabled
ALTER SUBSCRIPTION regress_testsub SET (streaming = true)
;
----------
[BLOCK] DO 'DECLARE loid oid; BEGIN SELECT tbl.loid INTO loid FROM lotest_stash_values tbl; PERFORM lo_export(loid, ''/src/src/test/regress/results/invalid/path''); EXCEPTION WHEN UNDEFINED_FILE THEN RAISE NOTICE ''could not open file, as expected''; END'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_trig
  after insert on my_table
  for each row execute procedure funcA()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."sum3"(bigint, bigint, bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$select $1 + $2 + $3$_$
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int2_compress(internal) PARALLEL SAFE
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "hword_part" WITH "public"."ispell", "english_stem"
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26115_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_char("char", internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE collate_dep_test4t (a int, b text)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION bt_page_items(text, int8) PARALLEL RESTRICTED
;
----------
[CREATE_POLICY] /*
 * text search configuration for serbian language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * serbian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY serbian_stem
	(TEMPLATE = snowball, Language = serbian )
;
----------
[DROP_TYPE] drop domain intdom1
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub5 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub5
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION get_from_partitioned_table(partitioned_table.a%type)
RETURNS partitioned_table AS $$
DECLARE
    a_val partitioned_table.a%TYPE;
    result partitioned_table%ROWTYPE;
BEGIN
    a_val := $1;
    SELECT * INTO result FROM partitioned_table WHERE a = a_val;
    RETURN result;
END; $$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~ (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contained,
	COMMUTATOR = '@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_class ADD PRIMARY KEY USING INDEX pg_class_oid_index
;
----------
[DROP_TABLE] drop table pp_arrpart
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=18237 host=/tmp/6EQjqv0kuf dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_hub_adjustslots"("hname" character, "oldnslots" integer, "newnslots" integer) RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
begin
    if newnslots = oldnslots then
        return 0;
    end if;
    if newnslots < oldnslots then
        delete from HSlot where hubname = hname and slotno > newnslots;
	return 0;
    end if;
    for i in oldnslots + 1 .. newnslots loop
        insert into HSlot (slotname, hubname, slotno, slotlink)
		values ('HS.dummy', hname, i, '');
    end loop;
    return 0;
end
$$
;
----------
[CREATE_TYPE] CREATE DOMAIN queryfuncs_test_domain AS text CHECK (value <> 'foo')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_l" ATTACH PARTITION "public"."prt2_l_p3" FOR VALUES FROM (500) TO (600)
;
----------
[ADD_INDEX] CREATE INDEX t1_gist ON s4.t1 USING GIST (b)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE has_volatile ALTER COLUMN col5 TYPE float8
;
----------
[CREATE_TABLE] -- Load a file
CREATE TABLE lotest_stash_values (loid oid, fd integer)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."street" AS
 SELECT "r"."name",
    "r"."thepath",
    "c"."cname"
   FROM ONLY "public"."road" "r",
    "public"."real_city" "c"
  WHERE ("c"."outline" OPERATOR("public".##) "r"."thepath")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION delete(hstore,hstore)
RETURNS hstore
AS '$libdir/hstore','hstore_delete_hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_CATALOG] CREATE DATABASE "foobarX" TEMPLATE template0 LOCALE_PROVIDER xyz
;
----------
[CREATE_TABLE] create table idxpart2 partition of idxpart for values from (500) to (1000)
;
----------
[DROP_INDEX] DROP INDEX onek_unique1_constraint
;
----------
[ADD_INDEX] CREATE INDEX "vacuum_in_leader_small_index" ON "public"."parallel_vacuum_table" USING "btree" ((1))
;
----------
[CREATE_TABLE] CREATE TABLE hpart_1 PARTITION OF hash_parted FOR VALUES WITH (MODULUS 4, REMAINDER 0)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_C
	CONNECTION 'port=13200 host=/tmp/MRHxhpCYrn dbname=postgres application_name=tap_sub_C'
	PUBLICATION tap_pub_B
	WITH (two_phase = on, streaming = off)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table parted_irreg drop column fd, drop column fd2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."reloptions_test" (
    "s" character varying
)
WITH ("autovacuum_vacuum_cost_delay"='24', "fillfactor"='40', toast."autovacuum_vacuum_cost_delay"='23')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_cash_distance(internal,money,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE cmdata2 (f1 int)
;
----------
[DROP_PROG_OBJ] drop function mki4(int)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION tg_hub_adjustslots_wrong(bpchar, integer, integer) IS 'function with args'
;
----------
[DROP_TYPE] drop domain connotnull cascade
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26306_2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."city" (
    "name" "name",
    "location" "box",
    "budget" "public"."city_budget"
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER test_trigger
		   AFTER INSERT ON dump_test.measurement
		   FOR EACH ROW EXECUTE PROCEDURE dump_test.trigger_func()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^@ (
        LEFTARG = ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = ltxtq_exec,
	COMMUTATOR = '^@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_PROG_OBJ] -- VIEW trigger function
CREATE OR REPLACE FUNCTION view_trigger() RETURNS trigger
LANGUAGE plpgsql AS $$
declare
    argstr text := '';
begin
    for i in 0 .. TG_nargs - 1 loop
        if i > 0 then
            argstr := argstr || ', ';
        end if;
        argstr := argstr || TG_argv[i];
    end loop;

    raise notice '% % % % (%)', TG_RELNAME, TG_WHEN, TG_OP, TG_LEVEL, argstr;

    if TG_LEVEL = 'ROW' then
        if TG_OP = 'INSERT' then
            raise NOTICE 'NEW: %', NEW;
            INSERT INTO main_table VALUES (NEW.a, NEW.b);
            RETURN NEW;
        end if;

        if TG_OP = 'UPDATE' then
            raise NOTICE 'OLD: %, NEW: %', OLD, NEW;
            UPDATE main_table SET a = NEW.a, b = NEW.b WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN NEW;
        end if;

        if TG_OP = 'DELETE' then
            raise NOTICE 'OLD: %', OLD;
            DELETE FROM main_table WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN OLD;
        end if;
    end if;

    RETURN NULL;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE sch2.t1 (c1 int)
;
----------
[CREATE_TYPE] CREATE TYPE text_w_default
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_description
;
----------
[BLOCK] do $$
declare x var_record_nn := null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_foreign_server ADD PRIMARY KEY USING INDEX pg_foreign_server_oid_index
;
