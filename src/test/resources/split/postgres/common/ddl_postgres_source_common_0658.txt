-- test old extension version entry points

DROP EXTENSION pageinspect
;
DROP FUNCTION reindex_end_command()
;
--only for debug
CREATE FUNCTION querytree(query_int)
RETURNS text
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
VACUUM (SKIP_LOCKED) parted
;
-- Test extension upgrade with event trigger.
ALTER EVENT TRIGGER table_rewrite_trg DISABLE
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_amop
;
CREATE TABLE pht1_p1 PARTITION OF pht1 FOR VALUES WITH (MODULUS 3, REMAINDER 0)
;
ALTER TYPE planets ADD VALUE 'mercury' BEFORE 'venus'
;
CREATE FUNCTION invalid_fdw_handler() RETURNS int LANGUAGE SQL AS 'SELECT 1;'
;
ALTER TRIGGER c ON a DEPENDS ON EXTENSION test_ext5
;
ALTER FUNCTION gbt_macad8_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
ALTER OPERATOR <= (isbn, isbn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE TRIGGER trig_row_before_insupd
BEFORE INSERT OR UPDATE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trig_row_before_insupdate()
;
ANALYZE bt_name_heap
;
CREATE TABLE tab_rep_next (a int)
;
CALL nonexistent()
;
DROP SUBSCRIPTION tap_sub_C
;
CREATE TABLE pagg_tab (a int, b int, c text, d int) PARTITION BY LIST(c)
;
create schema authorization aac create table test_user (id int primary key,name varchar(50))
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest_normal_child" INHERIT "public"."gtest_normal"
;
CREATE UNIQUE INDEX "room_rno" ON "public"."room" USING "btree" ("roomno")
;
DROP FUNCTION alter_op_test_fn_bool_real(boolean, real)
;
ANALYZE pg_temp_2.pg_temp_28316_2
;
create unlogged table pgbench_accounts_3
  partition of pgbench_accounts
  for values from (66669) to (maxvalue) with (fillfactor=100)
;
CREATE OPERATOR # (
	RIGHTARG = _int4,
	PROCEDURE = icount
)
;
CREATE DATABASE tbuiltin7 TEMPLATE template0 LOCALE 'C' LOCALE_PROVIDER builtin ICU_LOCALE 'en'
;
/*
 * 6.17
 * COLLATION_CHARACTER_SET_APPLICABILITY view
 */

CREATE VIEW collation_character_set_applicability AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
-- don't interfere with other types of constraints
create table inh_parent (a int primary key)
;
ALTER OPERATOR FAMILY alt_opf18 USING btree
  ADD FUNCTION 6 (int4, int2) btint4skipsupport(internal)
;
CREATE TABLE "fkpart4"."droppk1" (
    "a" integer NOT NULL
)
;
CREATE TRIGGER "tg_system_au" AFTER UPDATE ON "public"."system" FOR EACH ROW EXECUTE FUNCTION "public"."tg_system_au"()
;
CREATE FUNCTION isnlt(isbn13, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP FUNCTION test_pglz_compress
;
CREATE TEMP TABLE pg_temp_13.pg_temp_25980_2 (tid pg_catalog.tid)
;
CREATE DOMAIN priv_testdomain1 AS int
;
DROP TABLE concur_temp
;
CREATE TABLE "regress_indexing"."pk5" (
    "a" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
CREATE INDEX t1_spgist ON s4.t1 USING SPGIST (ir)
;
create table rule_t1(f1 int, f2 int)
;
create or replace function trigger_ddl_func() returns trigger as $$
begin
  create index on trigger_ddl_table (col2);
  return new;
end$$ language plpgsql
;
CREATE VIEW schemata AS
    SELECT CAST(current_database() AS sql_identifier) AS catalog_name,
           CAST(n.nspname AS sql_identifier) AS schema_name,
           CAST(u.rolname AS sql_identifier) AS schema_owner,
           CAST(null AS sql_identifier) AS default_character_set_catalog,
           CAST(null AS sql_identifier) AS default_character_set_schema,
           CAST(null AS sql_identifier) AS default_character_set_name,
           CAST(null AS character_data) AS sql_path
    FROM pg_namespace n, pg_authid u
    WHERE n.nspowner = u.oid
          AND (pg_has_role(n.nspowner, 'USAGE')
               OR has_schema_privilege(n.oid, 'CREATE, USAGE'))
;
VACUUM (FREEZE) bmscantest
;
--
-- PREPARED TRANSACTIONS (two-phase commit)
--
-- We can't readily test persistence of prepared xacts within the
-- regression script framework, unfortunately.  Note that a crash
-- isn't really needed ... stopping and starting the postmaster would
-- be enough, but we can't even do that here.


-- create a simple table that we'll use in the tests
CREATE TABLE pxtest1 (foobar VARCHAR(10))
;
create function zero_divide() returns int as $$
declare v int := 0;
begin
  return 10 / v;
end;
$$ language plpgsql
;
drop type jsb_ia, jsb_ia2, jsb_char2, jsb_i_not_null_rec, jsb_i_gt_1_rec
;
alter table nv_child_2011 VALIDATE CONSTRAINT nv_child_2011_d_check
;
CREATE POLICY accounts_owner_policy ON split_prog.accounts
AS PERMISSIVE FOR SELECT TO PUBLIC
USING (owner_name = current_user)
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=true, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=true)
;
CREATE VIEW foreign_table_options AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           CAST((pg_options_to_table(t.ftoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(t.ftoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_tables t
;
------------------------------------------------------------------------------------------
[DROP_LIBRARY] -- test old extension version entry points

DROP EXTENSION pageinspect
;
----------
[DROP_PROG_OBJ] DROP FUNCTION reindex_end_command()
;
----------
[CREATE_PROG_OBJ] --only for debug
CREATE FUNCTION querytree(query_int)
RETURNS text
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) parted
;
----------
[ALTER_TRIGGER] -- Test extension upgrade with event trigger.
ALTER EVENT TRIGGER table_rewrite_trg DISABLE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_amop
;
----------
[CREATE_TABLE] CREATE TABLE pht1_p1 PARTITION OF pht1 FOR VALUES WITH (MODULUS 3, REMAINDER 0)
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE 'mercury' BEFORE 'venus'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION invalid_fdw_handler() RETURNS int LANGUAGE SQL AS 'SELECT 1;'
;
----------
[ALTER_TRIGGER] ALTER TRIGGER c ON a DEPENDS ON EXTENSION test_ext5
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad8_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (isbn, isbn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_before_insupd
BEFORE INSERT OR UPDATE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trig_row_before_insupdate()
;
----------
[ADMIN_TABLE] ANALYZE bt_name_heap
;
----------
[CREATE_TABLE] CREATE TABLE tab_rep_next (a int)
;
----------
[CALL_PROG_OBJ] CALL nonexistent()
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION tap_sub_C
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab (a int, b int, c text, d int) PARTITION BY LIST(c)
;
----------
[CREATE_SCHEMA] create schema authorization aac create table test_user (id int primary key,name varchar(50))
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest_normal_child" INHERIT "public"."gtest_normal"
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "room_rno" ON "public"."room" USING "btree" ("roomno")
;
----------
[DROP_PROG_OBJ] DROP FUNCTION alter_op_test_fn_bool_real(boolean, real)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_28316_2
;
----------
[CREATE_TABLE] create unlogged table pgbench_accounts_3
  partition of pgbench_accounts
  for values from (66669) to (maxvalue) with (fillfactor=100)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR # (
	RIGHTARG = _int4,
	PROCEDURE = icount
)
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin7 TEMPLATE template0 LOCALE 'C' LOCALE_PROVIDER builtin ICU_LOCALE 'en'
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.17
 * COLLATION_CHARACTER_SET_APPLICABILITY view
 */

CREATE VIEW collation_character_set_applicability AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
----------
[CREATE_TABLE] -- don't interfere with other types of constraints
create table inh_parent (a int primary key)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf18 USING btree
  ADD FUNCTION 6 (int4, int2) btint4skipsupport(internal)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart4"."droppk1" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_system_au" AFTER UPDATE ON "public"."system" FOR EACH ROW EXECUTE FUNCTION "public"."tg_system_au"()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(isbn13, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_pglz_compress
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_25980_2 (tid pg_catalog.tid)
;
----------
[CREATE_TYPE] CREATE DOMAIN priv_testdomain1 AS int
;
----------
[DROP_TABLE] DROP TABLE concur_temp
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk5" (
    "a" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
----------
[ADD_INDEX] CREATE INDEX t1_spgist ON s4.t1 USING SPGIST (ir)
;
----------
[CREATE_TABLE] create table rule_t1(f1 int, f2 int)
;
----------
[CREATE_PROG_OBJ] create or replace function trigger_ddl_func() returns trigger as $$
begin
  create index on trigger_ddl_table (col2);
  return new;
end$$ language plpgsql
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW schemata AS
    SELECT CAST(current_database() AS sql_identifier) AS catalog_name,
           CAST(n.nspname AS sql_identifier) AS schema_name,
           CAST(u.rolname AS sql_identifier) AS schema_owner,
           CAST(null AS sql_identifier) AS default_character_set_catalog,
           CAST(null AS sql_identifier) AS default_character_set_schema,
           CAST(null AS sql_identifier) AS default_character_set_name,
           CAST(null AS character_data) AS sql_path
    FROM pg_namespace n, pg_authid u
    WHERE n.nspowner = u.oid
          AND (pg_has_role(n.nspowner, 'USAGE')
               OR has_schema_privilege(n.oid, 'CREATE, USAGE'))
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) bmscantest
;
----------
[CREATE_TABLE] --
-- PREPARED TRANSACTIONS (two-phase commit)
--
-- We can't readily test persistence of prepared xacts within the
-- regression script framework, unfortunately.  Note that a crash
-- isn't really needed ... stopping and starting the postmaster would
-- be enough, but we can't even do that here.


-- create a simple table that we'll use in the tests
CREATE TABLE pxtest1 (foobar VARCHAR(10))
;
----------
[CREATE_PROG_OBJ] create function zero_divide() returns int as $$
declare v int := 0;
begin
  return 10 / v;
end;
$$ language plpgsql
;
----------
[DROP_TYPE] drop type jsb_ia, jsb_ia2, jsb_char2, jsb_i_not_null_rec, jsb_i_gt_1_rec
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] alter table nv_child_2011 VALIDATE CONSTRAINT nv_child_2011_d_check
;
----------
[CREATE_POLICY] CREATE POLICY accounts_owner_policy ON split_prog.accounts
AS PERMISSIVE FOR SELECT TO PUBLIC
USING (owner_name = current_user)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=true, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=true)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW foreign_table_options AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           CAST((pg_options_to_table(t.ftoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(t.ftoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_tables t
;
