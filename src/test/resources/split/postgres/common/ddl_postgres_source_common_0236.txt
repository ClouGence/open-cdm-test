create temp table q1 as select 1 as q1
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest26"
    ADD CONSTRAINT "gtest26_pkey" PRIMARY KEY ("a")
;
CREATE VIEW v7_temp AS SELECT t1.id, t2.a FROM base_table t1, (SELECT * FROM temp_table) t2
;
drop table testtable_orange
;
CREATE UNIQUE INDEX "num_exp_add_idx" ON "public"."num_exp_add" USING "btree" ("id1", "id2")
;
CREATE TABLE split_table_idx.hash_part_0 PARTITION OF split_table_idx.hash_part FOR VALUES WITH (MODULUS 4, REMAINDER 0)
;
DROP TABLE test10b
;
create table rangep (a int, b int) partition by range (a)
;
alter database abc reset all
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should work
  execute 'select * from foo where f1 = 3' into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
-- Using the "table constraint" syntax also works
ALTER TABLE notnull_tbl1 ALTER a DROP NOT NULL
;
ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk1" FOR VALUES FROM (0) TO (1000)
;
DROP TABLE hash_parted2
;
--
-- ISMN13 operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE TABLE itest_err_3 (a int default 5 generated by default as identity)
;
CREATE VIEW s3.t2_view AS (
				SELECT i*2, t FROM s3.t2
			)
;
ALTER TABLE T ADD COLUMN c1 TIMESTAMP DEFAULT now()
;
CREATE INDEX gtest22c_expr_idx ON gtest22c ((b * 3))
;
CREATE INDEX "iprt1_e_p1_ab2" ON "public"."prt1_e_p1" USING "btree" (((("a" + "b") / 2)))
;
CREATE TABLE SYS_COL_CHECK_TBL (city text, state text, is_capital bool,
                  altitude int,
                  CHECK (NOT (is_capital AND tableoid::regclass::text = 'sys_col_check_tbl')))
;
drop type rngfunc2
;
ANALYZE pg_temp_13.pg_temp_25903
;
-- multi-column keys
create table mc3p (a int, b int, c int) partition by range (a, abs(b), c)
;
ALTER TABLE split_alter_table.part_parent ATTACH PARTITION split_alter_table.part_eu FOR VALUES IN ('eu')
;
DROP MATERIALIZED VIEW pgss_matv
;
DROP OPERATOR FAMILY alt_opf12 USING btree
;
create temp table onecol as select array[1,2] as f1
;
ALTER TABLE list_parted ATTACH PARTITION nonexistent FOR VALUES IN (1)
;
CREATE TABLE test_repl_stat(col1 int)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_shseclabel
;
ALTER TABLE test ADD COLUMN x int
;
ALTER TABLE pg_temp_13.pg_temp_26366_2 ADD COLUMN newdata pg_temp_13.pg_temp_26366
;
alter domain posint add constraint c1 check(value >= 0)
;
CREATE TABLE dump_test.test_table_nn (
							col1 int)
;
DROP TABLE functest2 CASCADE
;
ALTER DATABASE regression_utf8 RESET TABLESPACE
;
ALTER FUNCTION gbt_inet_compress(internal) PARALLEL SAFE
;
create trigger t before insert or update on parted
  for each row execute function parted_trigfunc()
;
CREATE FUNCTION testprs_start(internal, int4)
RETURNS internal
AS 'MODULE_PATHNAME'
LANGUAGE C STRICT
;
DROP TABLE sb_1, sb_2 CASCADE
;
-- Make the column NOT NULL and ensure we remove the redundant column
alter table t2 alter column z set not null
;
CREATE TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser (PARSER = pg_catalog.default)
;
CREATE DOMAIN "generated_stored_tests"."gtestdomain1" AS integer
	CONSTRAINT "gtestdomain1_check" CHECK ((VALUE < 10))
;
ALTER TABLE test_part_b_2 REPLICA IDENTITY USING INDEX test_part_b_2_pkey
;
CREATE OR REPLACE FUNCTION regexp_split_to_table(string citext, pattern citext, flags text) RETURNS SETOF TEXT AS $$
    SELECT pg_catalog.regexp_split_to_table( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
-- Check the number of vacuums done on table vac_option_tab and on its
-- toast relation, to check that PROCESS_TOAST and PROCESS_MAIN work on
-- what they should.
CREATE VIEW vac_option_tab_counts AS
  SELECT CASE WHEN c.relname IS NULL
    THEN 'main' ELSE 'toast' END as rel,
  s.vacuum_count
  FROM pg_stat_all_tables s
  LEFT JOIN pg_class c ON s.relid = c.reltoastrelid
  WHERE c.relname = 'vac_option_tab' OR s.relname = 'vac_option_tab'
  ORDER BY rel
;
CREATE TABLE employees OF employee_type (
    PRIMARY KEY (name),
    salary WITH OPTIONS DEFAULT 1000
)
;
ALTER OPERATOR <= (citext, citext) SET (
    RESTRICT   = scalarlesel,
    JOIN       = scalarlejoinsel
)
;
CREATE FUNCTION test_read_uint2_array(x uint2[]) RETURNS uint2 AS $$
begin
return x[1];
end
$$ LANGUAGE plpgsql
;
create unlogged table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) with (fillfactor=98) tablespace "pg_default"
;
------------------------------------------------------------------------------------------
[CREATE_TABLE|SELECT] create temp table q1 as select 1 as q1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest26"
    ADD CONSTRAINT "gtest26_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v7_temp AS SELECT t1.id, t2.a FROM base_table t1, (SELECT * FROM temp_table) t2
;
----------
[DROP_TABLE] drop table testtable_orange
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "num_exp_add_idx" ON "public"."num_exp_add" USING "btree" ("id1", "id2")
;
----------
[CREATE_TABLE] CREATE TABLE split_table_idx.hash_part_0 PARTITION OF split_table_idx.hash_part FOR VALUES WITH (MODULUS 4, REMAINDER 0)
;
----------
[DROP_TABLE] DROP TABLE test10b
;
----------
[CREATE_TABLE] create table rangep (a int, b int) partition by range (a)
;
----------
[ALTER_CATALOG] alter database abc reset all
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should work
  execute 'select * from foo where f1 = 3' into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Using the "table constraint" syntax also works
ALTER TABLE notnull_tbl1 ALTER a DROP NOT NULL
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk1" FOR VALUES FROM (0) TO (1000)
;
----------
[DROP_TABLE] DROP TABLE hash_parted2
;
----------
[CREATE_PROG_OBJ] --
-- ISMN13 operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE itest_err_3 (a int default 5 generated by default as identity)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW s3.t2_view AS (
				SELECT i*2, t FROM s3.t2
			)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE T ADD COLUMN c1 TIMESTAMP DEFAULT now()
;
----------
[ADD_INDEX] CREATE INDEX gtest22c_expr_idx ON gtest22c ((b * 3))
;
----------
[ADD_INDEX] CREATE INDEX "iprt1_e_p1_ab2" ON "public"."prt1_e_p1" USING "btree" (((("a" + "b") / 2)))
;
----------
[CREATE_TABLE] CREATE TABLE SYS_COL_CHECK_TBL (city text, state text, is_capital bool,
                  altitude int,
                  CHECK (NOT (is_capital AND tableoid::regclass::text = 'sys_col_check_tbl')))
;
----------
[DROP_TYPE] drop type rngfunc2
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25903
;
----------
[CREATE_TABLE] -- multi-column keys
create table mc3p (a int, b int, c int) partition by range (a, abs(b), c)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE split_alter_table.part_parent ATTACH PARTITION split_alter_table.part_eu FOR VALUES IN ('eu')
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW pgss_matv
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf12 USING btree
;
----------
[CREATE_TABLE|SELECT] create temp table onecol as select array[1,2] as f1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted ATTACH PARTITION nonexistent FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE test_repl_stat(col1 int)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_shseclabel
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test ADD COLUMN x int
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26366_2 ADD COLUMN newdata pg_temp_13.pg_temp_26366
;
----------
[ALTER_TYPE] alter domain posint add constraint c1 check(value >= 0)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_nn (
							col1 int)
;
----------
[DROP_TABLE] DROP TABLE functest2 CASCADE
;
----------
[ALTER_CATALOG] ALTER DATABASE regression_utf8 RESET TABLESPACE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_inet_compress(internal) PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger t before insert or update on parted
  for each row execute function parted_trigfunc()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testprs_start(internal, int4)
RETURNS internal
AS 'MODULE_PATHNAME'
LANGUAGE C STRICT
;
----------
[DROP_TABLE] DROP TABLE sb_1, sb_2 CASCADE
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Make the column NOT NULL and ensure we remove the redundant column
alter table t2 alter column z set not null
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser (PARSER = pg_catalog.default)
;
----------
[CREATE_TYPE] CREATE DOMAIN "generated_stored_tests"."gtestdomain1" AS integer
	CONSTRAINT "gtestdomain1_check" CHECK ((VALUE < 10))
;
----------
[ALTER_TABLE] ALTER TABLE test_part_b_2 REPLICA IDENTITY USING INDEX test_part_b_2_pkey
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_split_to_table(string citext, pattern citext, flags text) RETURNS SETOF TEXT AS $$
    SELECT pg_catalog.regexp_split_to_table( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] -- Check the number of vacuums done on table vac_option_tab and on its
-- toast relation, to check that PROCESS_TOAST and PROCESS_MAIN work on
-- what they should.
CREATE VIEW vac_option_tab_counts AS
  SELECT CASE WHEN c.relname IS NULL
    THEN 'main' ELSE 'toast' END as rel,
  s.vacuum_count
  FROM pg_stat_all_tables s
  LEFT JOIN pg_class c ON s.relid = c.reltoastrelid
  WHERE c.relname = 'vac_option_tab' OR s.relname = 'vac_option_tab'
  ORDER BY rel
;
----------
[CREATE_TABLE] CREATE TABLE employees OF employee_type (
    PRIMARY KEY (name),
    salary WITH OPTIONS DEFAULT 1000
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (citext, citext) SET (
    RESTRICT   = scalarlesel,
    JOIN       = scalarlejoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_read_uint2_array(x uint2[]) RETURNS uint2 AS $$
begin
return x[1];
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] create unlogged table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) with (fillfactor=98) tablespace "pg_default"
;
