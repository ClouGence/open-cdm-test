LOCK TABLE "public"."rtest_interface" IN ACCESS SHARE MODE
;
PREPARE TRANSACTION 'c0'
;
/* contrib/pg_stat_statements/pg_stat_statements--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


GRANT EXECUTE ON FUNCTION pg_stat_statements_reset() TO pg_read_all_stats
;
COPY public.beta_neg_p3 (a, b, c) TO stdout
;
EXECUTE dumpTableAttach('39920')
;
ALTER ROLE regress_tenant NOINHERIT NOLOGIN CONNECTION LIMIT 7
;
reset role
;
EXECUTE dumpFunc('16674')
;
EXECUTE dumpFunc('40210')
;
ALTER TABLE public.prt2_l_p2 OWNER TO zyc
;
------------------------------------------------------------------------------------------
[SESSION_LOCK] LOCK TABLE "public"."rtest_interface" IN ACCESS SHARE MODE
;
----------
[TRANSACTION] PREPARE TRANSACTION 'c0'
;
----------
[GRANT] /* contrib/pg_stat_statements/pg_stat_statements--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


GRANT EXECUTE ON FUNCTION pg_stat_statements_reset() TO pg_read_all_stats
;
----------
[DATA_EXPORT] COPY public.beta_neg_p3 (a, b, c) TO stdout
;
----------
[UNSAFE] EXECUTE dumpTableAttach('39920')
;
----------
[ALTER_USER] ALTER ROLE regress_tenant NOINHERIT NOLOGIN CONNECTION LIMIT 7
;
----------
[SESSION_SETTING_WRITE] reset role
;
----------
[UNSAFE] EXECUTE dumpFunc('16674')
;
----------
[UNSAFE] EXECUTE dumpFunc('40210')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.prt2_l_p2 OWNER TO zyc
;
