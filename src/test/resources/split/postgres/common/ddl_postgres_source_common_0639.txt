ALTER FUNCTION gbt_intv_decompress(internal) PARALLEL SAFE
;
-- cleanup
DROP TRIGGER trig_row_before_insupd ON rem1
;
ALTER TABLE ONLY "public"."idxpart3" ATTACH PARTITION "public"."idxpart31" FOR VALUES FROM (1000) TO (1200)
;
ALTER TABLE pg_temp_13.pg_temp_26306_2 ADD COLUMN newdata pg_temp_13.pg_temp_26306
;
ALTER TEXT SEARCH DICTIONARY intdict (MAXLEN = 6.7)
;
CREATE VIEW pg_config AS
    SELECT * FROM pg_config()
;
DROP PUBLICATION testpub6
;
-- Should fail. Invalid opclass options function (#5) specifications.
CREATE OPERATOR FAMILY alt_opf19 USING btree
;
CREATE FOREIGN TABLE tbl () SERVER file_server
;
CREATE DOMAIN dcomptype AS comptype
;
CREATE TRIGGER ftable_rowcount_trigger
BEFORE INSERT OR UPDATE OR DELETE ON ltable
FOR EACH ROW EXECUTE PROCEDURE ftable_rowcount_trigf()
;
/*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
DROP FUNCTION getrngfunc8(int)
;
ANALYZE current_check
;
CREATE TABLE tststats.pt (a int, b int, c text) PARTITION BY RANGE (a, b)
;
CREATE FUNCTION split_trigger_probe.audit_statement()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
    INSERT INTO split_trigger_probe.account_audit(id, action)
    VALUES (0, TG_ARGV[0]);
    RETURN NULL;
END;
$$
;
CREATE FUNCTION leaker_1(fail BOOL) RETURNS INTEGER AS $$
DECLARE
  v_var INTEGER;
BEGIN
  BEGIN
    v_var := (leaker_2(fail)).error_code;
  EXCEPTION
    WHEN others THEN RETURN 0;
  END;
  RETURN 1;
END;
$$ LANGUAGE plpgsql
;
--
-- Test merge-append plans for inheritance trees
--

create table matest0 (id serial primary key, name text)
;
create table refd_table (id int primary key)
;
CREATE FUNCTION gbt_text_same(gbtreekey_var, gbtreekey_var, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE DATABASE regression_dest1
;
CREATE TABLE collate_test2 (
    a int,
    b text COLLATE "sv_SE"
)
;
CREATE VIEW "public"."rtest_vview2" AS
 SELECT "a",
    "b"
   FROM "public"."rtest_view1"
  WHERE "v"
;
CREATE TABLE dob_t2 (c1 int) PARTITION BY RANGE (c1)
;
DROP DATABASE IF EXISTS "regression"
;
DROP MATERIALIZED VIEW ext_cine_mv
;
--
-- Test handling of expanded arrays
--

create function returns_rw_array(int) returns int[]
language plpgsql as $$
  declare r int[];
  begin r := array[$1, $1]; return r; end;
$$ stable
;
CREATE TABLE test11fk (x text COLLATE case_sensitive REFERENCES test11pk (x) ON UPDATE CASCADE ON DELETE CASCADE)
;
create function simpletarget(int) returns int language plpgsql
as $$begin return $1 + 100; end$$
;
create table s1.p2 (a int, b int) PARTITION BY list (a)
;
CLUSTER abbrev_abort_uuids USING abbrev_abort_uuids__abort_decreasing_idx
;
-- OK
comment on event trigger regress_event_trigger is 'test comment'
;
-- LOCALE
CREATE COLLATION case_sensitive (LOCALE = '', LOCALE = "NONSENSE")
;
CREATE TABLE gtest21b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (nullif(a, 0)) STORED)
;
-- not STRICT; needs to allow (key,NULL)

CREATE FUNCTION hstore(text,text)
RETURNS hstore
AS '$libdir/hstore','hstore_from_text'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
ALTER TABLE ONLY "public"."tt6" INHERIT "public"."tt0"
;
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (10) TO (30)
;
create rule rtest_nothn_r2 as on insert to rtest_nothn1
	where new.a >= 30 and new.a < 40 do instead nothing
;
COMMENT ON INDEX "public"."comment_test_index" IS 'Simple index on comment_test'
;
ALTER DOMAIN domain_stats ADD CONSTRAINT higher_than_one CHECK (VALUE > 1)
;
ALTER TABLE pg_temp_13.pg_temp_26616_2 ADD COLUMN newdata pg_temp_13.pg_temp_26616
;
ANALYZE ft5
;
ALTER OPERATOR @!=(boolean, real) SET (NEGATOR = ===)
;
CREATE INDEX on vac_horizon_floor_table(col1)
;
DROP TABLE ruletest_t1
;
-- table without primary key
CREATE TABLE table_dropped_index_no_pk (a int NOT NULL, b int, c int)
;
DROP PUBLICATION testpub_both_filters
;
VACUUM ANALYZE onek
;
CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE SEQUENCE schema_not_existing.seq
;
CREATE TRIGGER trigger_1
  BEFORE INSERT OR UPDATE
  ON datatype_table
  FOR EACH ROW
  EXECUTE PROCEDURE plpgsql_function_trigger_1()
;
------------------------------------------------------------------------------------------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_decompress(internal) PARALLEL SAFE
;
----------
[DROP_TRIGGER] -- cleanup
DROP TRIGGER trig_row_before_insupd ON rem1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."idxpart3" ATTACH PARTITION "public"."idxpart31" FOR VALUES FROM (1000) TO (1200)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26306_2 ADD COLUMN newdata pg_temp_13.pg_temp_26306
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY intdict (MAXLEN = 6.7)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_config AS
    SELECT * FROM pg_config()
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub6
;
----------
[CREATE_PROG_OBJ] -- Should fail. Invalid opclass options function (#5) specifications.
CREATE OPERATOR FAMILY alt_opf19 USING btree
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server
;
----------
[CREATE_TYPE] CREATE DOMAIN dcomptype AS comptype
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER ftable_rowcount_trigger
BEFORE INSERT OR UPDATE OR DELETE ON ltable
FOR EACH ROW EXECUTE PROCEDURE ftable_rowcount_trigf()
;
----------
[CREATE_POLICY] /*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
----------
[DROP_PROG_OBJ] DROP FUNCTION getrngfunc8(int)
;
----------
[ADMIN_TABLE] ANALYZE current_check
;
----------
[CREATE_TABLE] CREATE TABLE tststats.pt (a int, b int, c text) PARTITION BY RANGE (a, b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_trigger_probe.audit_statement()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
    INSERT INTO split_trigger_probe.account_audit(id, action)
    VALUES (0, TG_ARGV[0]);
    RETURN NULL;
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION leaker_1(fail BOOL) RETURNS INTEGER AS $$
DECLARE
  v_var INTEGER;
BEGIN
  BEGIN
    v_var := (leaker_2(fail)).error_code;
  EXCEPTION
    WHEN others THEN RETURN 0;
  END;
  RETURN 1;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] --
-- Test merge-append plans for inheritance trees
--

create table matest0 (id serial primary key, name text)
;
----------
[CREATE_TABLE] create table refd_table (id int primary key)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_text_same(gbtreekey_var, gbtreekey_var, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_CATALOG] CREATE DATABASE regression_dest1
;
----------
[CREATE_TABLE] CREATE TABLE collate_test2 (
    a int,
    b text COLLATE "sv_SE"
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_vview2" AS
 SELECT "a",
    "b"
   FROM "public"."rtest_view1"
  WHERE "v"
;
----------
[CREATE_TABLE] CREATE TABLE dob_t2 (c1 int) PARTITION BY RANGE (c1)
;
----------
[DROP_CATALOG] DROP DATABASE IF EXISTS "regression"
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW ext_cine_mv
;
----------
[CREATE_PROG_OBJ] --
-- Test handling of expanded arrays
--

create function returns_rw_array(int) returns int[]
language plpgsql as $$
  declare r int[];
  begin r := array[$1, $1]; return r; end;
$$ stable
;
----------
[CREATE_TABLE] CREATE TABLE test11fk (x text COLLATE case_sensitive REFERENCES test11pk (x) ON UPDATE CASCADE ON DELETE CASCADE)
;
----------
[CREATE_PROG_OBJ] create function simpletarget(int) returns int language plpgsql
as $$begin return $1 + 100; end$$
;
----------
[CREATE_TABLE] create table s1.p2 (a int, b int) PARTITION BY list (a)
;
----------
[ADMIN_TABLE] CLUSTER abbrev_abort_uuids USING abbrev_abort_uuids__abort_decreasing_idx
;
----------
[COMMENT_TRIGGER] -- OK
comment on event trigger regress_event_trigger is 'test comment'
;
----------
[CREATE_POLICY] -- LOCALE
CREATE COLLATION case_sensitive (LOCALE = '', LOCALE = "NONSENSE")
;
----------
[CREATE_TABLE] CREATE TABLE gtest21b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (nullif(a, 0)) STORED)
;
----------
[CREATE_PROG_OBJ] -- not STRICT; needs to allow (key,NULL)

CREATE FUNCTION hstore(text,text)
RETURNS hstore
AS '$libdir/hstore','hstore_from_text'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."tt6" INHERIT "public"."tt0"
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (10) TO (30)
;
----------
[CREATE_POLICY] create rule rtest_nothn_r2 as on insert to rtest_nothn1
	where new.a >= 30 and new.a < 40 do instead nothing
;
----------
[COMMENT_INDEX] COMMENT ON INDEX "public"."comment_test_index" IS 'Simple index on comment_test'
;
----------
[ALTER_TYPE] ALTER DOMAIN domain_stats ADD CONSTRAINT higher_than_one CHECK (VALUE > 1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26616_2 ADD COLUMN newdata pg_temp_13.pg_temp_26616
;
----------
[ADMIN_TABLE] ANALYZE ft5
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @!=(boolean, real) SET (NEGATOR = ===)
;
----------
[ADD_INDEX] CREATE INDEX on vac_horizon_floor_table(col1)
;
----------
[DROP_TABLE] DROP TABLE ruletest_t1
;
----------
[CREATE_TABLE] -- table without primary key
CREATE TABLE table_dropped_index_no_pk (a int NOT NULL, b int, c int)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_both_filters
;
----------
[ADMIN_TABLE] VACUUM ANALYZE onek
;
----------
[CREATE_SCHEMA] CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE SEQUENCE schema_not_existing.seq
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trigger_1
  BEFORE INSERT OR UPDATE
  ON datatype_table
  FOR EACH ROW
  EXECUTE PROCEDURE plpgsql_function_trigger_1()
;
