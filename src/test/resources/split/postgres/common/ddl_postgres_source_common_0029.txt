TRUNCATE TABLE truncprim
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_trigger
;
ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser_renamed SET SCHEMA split_ts_next
;
-- Confirm PRIMARY KEY adds NOT NULL constraint to child table
CREATE TEMP TABLE z (b TEXT, PRIMARY KEY(aa, b)) inherits (a)
;
CREATE TABLE gtest_child PARTITION OF gtest_parent (
    f3 WITH OPTIONS GENERATED ALWAYS AS (f2 * 2) STORED
) FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_seclabel
;
ALTER OPERATOR %>> (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
DROP FUNCTION hstore_to_plperl(val internal)
;
ALTER SUBSCRIPTION tap_sub CONNECTION 'port=12333 host=/tmp/XhTqPN5Kw1 dbname=postgres sslmode=disable'
;
CREATE TABLE part12 PARTITION OF range_parted3 FOR VALUES FROM (1, 10) TO (1, maxvalue)
;
call test_catalog.test.test_procedure(1,2)
;
create domain pg_temp.nonempty as text check (value <> '')
;
ALTER TABLE ONLY "fkpart4"."droppk" ATTACH PARTITION "fkpart4"."droppk2" FOR VALUES FROM (1000) TO (2000)
;
CREATE TRIGGER "tg_chkslotlink" BEFORE INSERT OR UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotlink"()
;
create table rp_prefix_test3_p1 partition of rp_prefix_test3 for values from (1, 1, 1, 0) to (1, 1, 1, 10)
;
CREATE TRIGGER trig_row_before_insupd2
BEFORE INSERT OR UPDATE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trig_row_before_insupdate()
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_publication
;
CALL transaction_test1(9, c)
;
CREATE TABLE dump_test_second_schema.measurement_y2006m2
						PARTITION OF dump_test.measurement (
							unitsales DEFAULT 0 CHECK (unitsales >= 0)
						)
						FOR VALUES FROM ('2006-02-01') TO ('2006-03-01')
;
ALTER INDEX public.tht_en_data_key ATTACH PARTITION public.tht_p3_en_data_key
;
CREATE SCHEMA split_conversion_probe_next
;
create function test_event_trigger() returns event_trigger as $$
BEGIN
    RAISE NOTICE 'test_event_trigger: % %', tg_event, tg_tag;
END
$$ language plpgsql
;
CREATE UNIQUE INDEX test_replica_identity3_id_key ON test_replica_identity3 (id)
;
ALTER PUBLICATION tap_pub_ins_only DROP TABLE tab_ins
;
DO $$
DECLARE
  v_Text text;
  v_cnt  integer;
BEGIN
  CALL p1(v_Text := v_Text, v_cnt := v_cnt);
  RAISE NOTICE '%', v_Text;
END;
$$
;
ALTER TABLE pg_partitioned_table ADD PRIMARY KEY USING INDEX pg_partitioned_table_partrelid_index
;
CREATE AGGREGATE myaggp20a(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = upc,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
create table rangep_0_to_100 partition of rangep for values from (0) to (100) partition by list (b)
;
CREATE FUNCTION test1n(val ltree) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE ltree
AS $$
plpy.info(repr(val))
return len(val)
$$
;
CREATE FUNCTION test_dsa_allocate(int, int, int)
	RETURNS pg_catalog.void
	AS '$libdir/test_dsa' LANGUAGE C
;
create view vv3 as
select * from (values(1,2,3,4,5,6)) v(a,b,c,x,e,f)
union all
select * from
  tt7 full join tt8 using (x),
  tt7 tt7x full join tt8 tt8x using (x)
;
drop TABLE trans_foobar
;
CREATE TRIGGER "tg_phone_bu" BEFORE UPDATE ON "public"."phone" FOR EACH ROW EXECUTE FUNCTION "public"."tg_phone_bu"()
;
create sequence hsseq
;
CREATE TRIGGER "tg_iface_biu" BEFORE INSERT OR UPDATE ON "public"."iface" FOR EACH ROW EXECUTE FUNCTION "public"."tg_iface_biu"()
;
SECURITY LABEL ON COLUMN t1p_tens.p IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
CREATE TABLE flavians (name text, birth_date date)
;
do $$
begin
  execute format($cmd$
    CREATE TEMP TABLE temptest (col text CHECK (col < %L)) ON COMMIT DROP
  $cmd$,
    (SELECT string_agg(g.i::text || ':' || random()::text, '|')
     FROM generate_series(1, 100) g(i)));
end$$
;
CREATE UNLOGGED TABLE unlogged2(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES unlogged1)
;
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_1 FOR VALUES FROM (0,0) TO (1000,1000)
;
CREATE TABLE public.tst_comp_enum_array (
		a public.tst_comp_enum_t PRIMARY KEY,
		b public.tst_comp_enum_t[]
		)
;
--
-- partitioned FK referenced deletes NO ACTION
--

TRUNCATE temporal_partitioned_rng, temporal_partitioned_fk_rng2rng
;
-- should fail

CREATE OR REPLACE FUNCTION joinview_upd_trig_fn() RETURNS trigger
LANGUAGE plpgsql AS
$$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING WITH (new AS n) new.f1, n.f4 INTO new.f1, new.f4;  -- now ok
  RETURN NEW;
END;
$$
;
CREATE TABLE test_table(x integer PRIMARY KEY)
;
create trigger trigtest_a_stmt_tg after insert or update or delete on trigtest
for each statement execute procedure trigtest()
;
ALTER POLICY accounts_owner_policy ON split_prog.accounts RENAME TO accounts_select_policy
;
DROP PUBLICATION regress_pub3
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_foreign_server
;
ALTER TABLE t2 ADD COLUMN b domain1 default 3
;
------------------------------------------------------------------------------------------
[TRUNCATE_TABLE] TRUNCATE TABLE truncprim
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_trigger
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser_renamed SET SCHEMA split_ts_next
;
----------
[CREATE_TABLE] -- Confirm PRIMARY KEY adds NOT NULL constraint to child table
CREATE TEMP TABLE z (b TEXT, PRIMARY KEY(aa, b)) inherits (a)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child PARTITION OF gtest_parent (
    f3 WITH OPTIONS GENERATED ALWAYS AS (f2 * 2) STORED
) FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_seclabel
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR %>> (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION hstore_to_plperl(val internal)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub CONNECTION 'port=12333 host=/tmp/XhTqPN5Kw1 dbname=postgres sslmode=disable'
;
----------
[CREATE_TABLE] CREATE TABLE part12 PARTITION OF range_parted3 FOR VALUES FROM (1, 10) TO (1, maxvalue)
;
----------
[CALL_PROG_OBJ] call test_catalog.test.test_procedure(1,2)
;
----------
[CREATE_TYPE] create domain pg_temp.nonempty as text check (value <> '')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart4"."droppk" ATTACH PARTITION "fkpart4"."droppk2" FOR VALUES FROM (1000) TO (2000)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkslotlink" BEFORE INSERT OR UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotlink"()
;
----------
[CREATE_TABLE] create table rp_prefix_test3_p1 partition of rp_prefix_test3 for values from (1, 1, 1, 0) to (1, 1, 1, 10)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_before_insupd2
BEFORE INSERT OR UPDATE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trig_row_before_insupdate()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_publication
;
----------
[CALL_PROG_OBJ] CALL transaction_test1(9, c)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test_second_schema.measurement_y2006m2
						PARTITION OF dump_test.measurement (
							unitsales DEFAULT 0 CHECK (unitsales >= 0)
						)
						FOR VALUES FROM ('2006-02-01') TO ('2006-03-01')
;
----------
[ALTER_INDEX] ALTER INDEX public.tht_en_data_key ATTACH PARTITION public.tht_p3_en_data_key
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_conversion_probe_next
;
----------
[CREATE_PROG_OBJ] create function test_event_trigger() returns event_trigger as $$
BEGIN
    RAISE NOTICE 'test_event_trigger: % %', tg_event, tg_tag;
END
$$ language plpgsql
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX test_replica_identity3_id_key ON test_replica_identity3 (id)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_ins_only DROP TABLE tab_ins
;
----------
[BLOCK] DO $$
DECLARE
  v_Text text;
  v_cnt  integer;
BEGIN
  CALL p1(v_Text := v_Text, v_cnt := v_cnt);
  RAISE NOTICE '%', v_Text;
END;
$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_partitioned_table ADD PRIMARY KEY USING INDEX pg_partitioned_table_partrelid_index
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp20a(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = upc,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_TABLE] create table rangep_0_to_100 partition of rangep for values from (0) to (100) partition by list (b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1n(val ltree) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE ltree
AS $$
plpy.info(repr(val))
return len(val)
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_dsa_allocate(int, int, int)
	RETURNS pg_catalog.void
	AS '$libdir/test_dsa' LANGUAGE C
;
----------
[CREATE_VIEW(SELECT)] create view vv3 as
select * from (values(1,2,3,4,5,6)) v(a,b,c,x,e,f)
union all
select * from
  tt7 full join tt8 using (x),
  tt7 tt7x full join tt8 tt8x using (x)
;
----------
[DROP_TABLE] drop TABLE trans_foobar
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_phone_bu" BEFORE UPDATE ON "public"."phone" FOR EACH ROW EXECUTE FUNCTION "public"."tg_phone_bu"()
;
----------
[CREATE_SEQUENCE] create sequence hsseq
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_iface_biu" BEFORE INSERT OR UPDATE ON "public"."iface" FOR EACH ROW EXECUTE FUNCTION "public"."tg_iface_biu"()
;
----------
[UNKNOWN] SECURITY LABEL ON COLUMN t1p_tens.p IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
----------
[CREATE_TABLE] CREATE TABLE flavians (name text, birth_date date)
;
----------
[BLOCK] do $$
begin
  execute format($cmd$
    CREATE TEMP TABLE temptest (col text CHECK (col < %L)) ON COMMIT DROP
  $cmd$,
    (SELECT string_agg(g.i::text || ':' || random()::text, '|')
     FROM generate_series(1, 100) g(i)));
end$$
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE unlogged2(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES unlogged1)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_1 FOR VALUES FROM (0,0) TO (1000,1000)
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_comp_enum_array (
		a public.tst_comp_enum_t PRIMARY KEY,
		b public.tst_comp_enum_t[]
		)
;
----------
[TRUNCATE_TABLE] --
-- partitioned FK referenced deletes NO ACTION
--

TRUNCATE temporal_partitioned_rng, temporal_partitioned_fk_rng2rng
;
----------
[CREATE_PROG_OBJ] -- should fail

CREATE OR REPLACE FUNCTION joinview_upd_trig_fn() RETURNS trigger
LANGUAGE plpgsql AS
$$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING WITH (new AS n) new.f1, n.f4 INTO new.f1, new.f4;  -- now ok
  RETURN NEW;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE test_table(x integer PRIMARY KEY)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trigtest_a_stmt_tg after insert or update or delete on trigtest
for each statement execute procedure trigtest()
;
----------
[ALTER_POLICY] ALTER POLICY accounts_owner_policy ON split_prog.accounts RENAME TO accounts_select_policy
;
----------
[DROP_PUB_SUB] DROP PUBLICATION regress_pub3
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_foreign_server
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t2 ADD COLUMN b domain1 default 3
;
