COPY public.rules_src (f1, f2) TO stdout
;
DEALLOCATE pgsql_fdw_prep_104
;
ALTER FUNCTION "public"."explicitly_binary_coercible"("oid", "oid") OWNER TO zyc
;
ALTER TABLE generated_virtual_tests.gtest30 OWNER TO zyc
;
-- REINDEX is a no-op for partitioned tables
REINDEX TABLE concur_reindex_part_10
;
LOCK TABLE "public"."gtest2" IN ACCESS SHARE MODE
;
EXECUTE dumpBaseType('4601')
;
LOCK TABLE public.will_change, public.will_grow, public.will_shrink, public.will_get_vacuumed, public.will_get_rewritten, public.will_not_change_in_ts, public.will_change_in_ts, public.newly_created, public.newly_created_in_ts IN ACCESS SHARE MODE
;
--
-- statement timestamps
--

-- planning time is needed during tests
SET pg_stat_statements.track_planning = TRUE
;
--
-- AGGREGATES
--

-- avoid bit-exact output here because operations may not be bit-exact.
SET extra_float_digits = 0
;
------------------------------------------------------------------------------------------
[DATA_EXPORT] COPY public.rules_src (f1, f2) TO stdout
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_104
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."explicitly_binary_coercible"("oid", "oid") OWNER TO zyc
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE generated_virtual_tests.gtest30 OWNER TO zyc
;
----------
[ADMIN] -- REINDEX is a no-op for partitioned tables
REINDEX TABLE concur_reindex_part_10
;
----------
[SESSION_LOCK] LOCK TABLE "public"."gtest2" IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpBaseType('4601')
;
----------
[SESSION_LOCK] LOCK TABLE public.will_change, public.will_grow, public.will_shrink, public.will_get_vacuumed, public.will_get_rewritten, public.will_not_change_in_ts, public.will_change_in_ts, public.newly_created, public.newly_created_in_ts IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] --
-- statement timestamps
--

-- planning time is needed during tests
SET pg_stat_statements.track_planning = TRUE
;
----------
[SESSION_SETTING_WRITE] --
-- AGGREGATES
--

-- avoid bit-exact output here because operations may not be bit-exact.
SET extra_float_digits = 0
;
