drop index gist_tbl_point_index
;
CREATE TABLE tableam_parted_b_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('b')
;
-- check ON CONFLICT handling with partitioned tables
create table parted_conflict_test (a int unique, b char) partition by list (a)
;
ALTER FUNCTION gbtreekey32_in(cstring) PARALLEL SAFE
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_largeobject
;
/* contrib/amcheck/amcheck--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- verify_heapam()
--
CREATE FUNCTION verify_heapam(relation regclass,
							  on_error_stop boolean default false,
							  check_toast boolean default false,
							  skip text default 'none',
							  startblock bigint default null,
							  endblock bigint default null,
							  blkno OUT bigint,
							  offnum OUT integer,
							  attnum OUT integer,
							  msg OUT text)
RETURNS SETOF record
AS '$libdir/amcheck', 'verify_heapam'
LANGUAGE C
;
create table mc3p_default partition of mc3p default
;
CREATE VIEW aliased_view_1 AS
  select * from tt1
    where exists (select 1 from tx1 where tt1.f1 = tx1.x1)
;
create temp table temp_parted_oncommit_test (a int)
  partition by list (a) on commit delete rows
;
CREATE FUNCTION "public"."int8alias1eq"("public"."int8alias1", "public"."int8alias2") RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8eq$$
;
DROP TABLE testpub_merge_no_ri
;
CREATE VIEW pg_stat_ssl AS
    SELECT
            S.pid,
            S.ssl,
            S.sslversion AS version,
            S.sslcipher AS cipher,
            S.sslbits AS bits,
            S.sslcompression AS compression,
            S.ssl_client_dn AS client_dn,
            S.ssl_client_serial AS client_serial,
            S.ssl_issuer_dn AS issuer_dn
    FROM pg_stat_get_activity(NULL) AS S
;
CREATE FUNCTION dblink_send_query(text, text)
RETURNS int4
AS '$libdir/dblink', 'dblink_send_query'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
-- test: use of variable of record type in return statement
create or replace function compos() returns compostype as $$
declare
  v record;
begin
  v := (1, 'hello'::varchar);
  return v;
end;
$$ language plpgsql
;
CREATE OPERATOR @ (
        LEFTARG = _ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = _ltxtq_exec,
        COMMUTATOR = '@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE TABLE m AS SELECT i AS k, (i || ' v')::text v FROM generate_series(1, 16, 3) i
;
CREATE RULE "_RETURN" AS ON SELECT TO tt27 DO INSTEAD SELECT * FROM tt26
;
create table fktable2 (
  a int,
  b int,
  very_very_long_column_name_to_exceed_63_characters int,
  foreign key (very_very_long_column_name_to_exceed_63_characters) references pktable1,
  foreign key (a, very_very_long_column_name_to_exceed_63_characters) references pktable2,
  foreign key (a, very_very_long_column_name_to_exceed_63_characters) references pktable2
)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_publication
;
CREATE TABLE test6b OF priv_testtype1
;
-- Test retrieval of system columns with non-consistent partition row types.
-- This is only partially supported, as seen in the results.

create table utrtest (a int, b text) partition by list (a)
;
CREATE TABLE gtest23p (x int, y int GENERATED ALWAYS AS (x * 2) STORED, PRIMARY KEY (y))
;
CREATE TEMP TABLE pg_temp_12.pg_temp_29625_2 (tid pg_catalog.tid)
;
ALTER INDEX pg_toast.pg_toast_30618_index RENAME TO reind_con_toast_idx
;
-- Before triggers and partitions
create table parted (a int, b int, c text) partition by list (a)
;
DROP TYPE base_type
;
drop domain ddef4 restrict
;
CREATE DATABASE db2 WITH TABLESPACE ts1 STRATEGY=FILE_COPY
;
CREATE FUNCTION gin_extract_query_time(time, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER FUNCTION gbt_ts_consistent(internal, timestamp without time zone, smallint, oid, internal) PARALLEL SAFE
;
CREATE FUNCTION "public"."int8alias1cmp"(bigint, "public"."int8alias1") RETURNS integer
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$btint8cmp$$
;
CREATE COLLATION builtin2 ( PROVIDER = builtin )
;
CREATE FUNCTION autoinc ()
	RETURNS trigger
	AS '/src/src/test/regress/autoinc.so'
	LANGUAGE C
;
--
-- Test multirange types over domains
--

create domain mydomain as int4
;
--     P    N
-- should ERROR: stfnp(anyarray) not matched by stfnp(int[])
CREATE AGGREGATE myaggp02a(*) (SFUNC = stfnp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
-- test that a non-mapping result is correctly rejected
CREATE FUNCTION test1bad() RETURNS hstore
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
return "foo"
$$
;
CREATE TABLE parted_si_p_odd PARTITION OF parted_si FOR VALUES IN (1)
;
create trigger parted_trig_after_stmt after insert or update or delete on parted_trig
   for each statement execute procedure trigger_notice()
;
CREATE TABLE base_tbl (id int, idplus1 int GENERATED ALWAYS AS (id + 1) STORED)
;
CREATE INDEX CONCURRENTLY concur_index5 on concur_heap(f2) WHERE f1='x'
;
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize_bloom VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
alter table loc2 drop column f2
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_template
;
ANALYZE group_agg_pk
;
ALTER TABLE pg_temp_4.pg_temp_27293_2 ADD COLUMN newdata pg_temp_4.pg_temp_27293
;
ALTER TABLE fk_partitioned_pk VALIDATE CONSTRAINT selffk
;
CREATE UNIQUE INDEX "ec1_expr1" ON "public"."ec1" USING "btree" ((("ff" + 1)))
;
CREATE TABLE "public"."test_inh_check" (
    "a" numeric,
    "b" numeric,
    CONSTRAINT "bmerged" CHECK ((("b")::double precision > (1)::double precision)),
    CONSTRAINT "bnoinherit" CHECK ((("b")::double precision > (100)::double precision)) NO INHERIT,
    CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
)
;
create trigger child3_insert_trig
  after insert on child3 referencing new table as new_table
  for each statement execute procedure dump_insert()
;
ALTER TABLE comment_test ALTER COLUMN indexed_col SET DATA TYPE text
;
------------------------------------------------------------------------------------------
[DROP_INDEX] drop index gist_tbl_point_index
;
----------
[CREATE_TABLE] CREATE TABLE tableam_parted_b_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('b')
;
----------
[CREATE_TABLE] -- check ON CONFLICT handling with partitioned tables
create table parted_conflict_test (a int unique, b char) partition by list (a)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbtreekey32_in(cstring) PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_largeobject
;
----------
[CREATE_PROG_OBJ] /* contrib/amcheck/amcheck--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- verify_heapam()
--
CREATE FUNCTION verify_heapam(relation regclass,
							  on_error_stop boolean default false,
							  check_toast boolean default false,
							  skip text default 'none',
							  startblock bigint default null,
							  endblock bigint default null,
							  blkno OUT bigint,
							  offnum OUT integer,
							  attnum OUT integer,
							  msg OUT text)
RETURNS SETOF record
AS '$libdir/amcheck', 'verify_heapam'
LANGUAGE C
;
----------
[CREATE_TABLE] create table mc3p_default partition of mc3p default
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW aliased_view_1 AS
  select * from tt1
    where exists (select 1 from tx1 where tt1.f1 = tx1.x1)
;
----------
[CREATE_TABLE] create temp table temp_parted_oncommit_test (a int)
  partition by list (a) on commit delete rows
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int8alias1eq"("public"."int8alias1", "public"."int8alias2") RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8eq$$
;
----------
[DROP_TABLE] DROP TABLE testpub_merge_no_ri
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_ssl AS
    SELECT
            S.pid,
            S.ssl,
            S.sslversion AS version,
            S.sslcipher AS cipher,
            S.sslbits AS bits,
            S.sslcompression AS compression,
            S.ssl_client_dn AS client_dn,
            S.ssl_client_serial AS client_serial,
            S.ssl_issuer_dn AS issuer_dn
    FROM pg_stat_get_activity(NULL) AS S
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_send_query(text, text)
RETURNS int4
AS '$libdir/dblink', 'dblink_send_query'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] -- test: use of variable of record type in return statement
create or replace function compos() returns compostype as $$
declare
  v record;
begin
  v := (1, 'hello'::varchar);
  return v;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @ (
        LEFTARG = _ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = _ltxtq_exec,
        COMMUTATOR = '@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE m AS SELECT i AS k, (i || ' v')::text v FROM generate_series(1, 16, 3) i
;
----------
[CREATE_POLICY] CREATE RULE "_RETURN" AS ON SELECT TO tt27 DO INSTEAD SELECT * FROM tt26
;
----------
[CREATE_TABLE] create table fktable2 (
  a int,
  b int,
  very_very_long_column_name_to_exceed_63_characters int,
  foreign key (very_very_long_column_name_to_exceed_63_characters) references pktable1,
  foreign key (a, very_very_long_column_name_to_exceed_63_characters) references pktable2,
  foreign key (a, very_very_long_column_name_to_exceed_63_characters) references pktable2
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_publication
;
----------
[CREATE_TABLE] CREATE TABLE test6b OF priv_testtype1
;
----------
[CREATE_TABLE] -- Test retrieval of system columns with non-consistent partition row types.
-- This is only partially supported, as seen in the results.

create table utrtest (a int, b text) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE gtest23p (x int, y int GENERATED ALWAYS AS (x * 2) STORED, PRIMARY KEY (y))
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_29625_2 (tid pg_catalog.tid)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30618_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- Before triggers and partitions
create table parted (a int, b int, c text) partition by list (a)
;
----------
[DROP_TYPE] DROP TYPE base_type
;
----------
[DROP_TYPE] drop domain ddef4 restrict
;
----------
[CREATE_CATALOG] CREATE DATABASE db2 WITH TABLESPACE ts1 STRATEGY=FILE_COPY
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_time(time, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_ts_consistent(internal, timestamp without time zone, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int8alias1cmp"(bigint, "public"."int8alias1") RETURNS integer
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$btint8cmp$$
;
----------
[CREATE_POLICY] CREATE COLLATION builtin2 ( PROVIDER = builtin )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION autoinc ()
	RETURNS trigger
	AS '/src/src/test/regress/autoinc.so'
	LANGUAGE C
;
----------
[CREATE_TYPE] --
-- Test multirange types over domains
--

create domain mydomain as int4
;
----------
[CREATE_PROG_OBJ] --     P    N
-- should ERROR: stfnp(anyarray) not matched by stfnp(int[])
CREATE AGGREGATE myaggp02a(*) (SFUNC = stfnp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] -- test that a non-mapping result is correctly rejected
CREATE FUNCTION test1bad() RETURNS hstore
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
return "foo"
$$
;
----------
[CREATE_TABLE] CREATE TABLE parted_si_p_odd PARTITION OF parted_si FOR VALUES IN (1)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parted_trig_after_stmt after insert or update or delete on parted_trig
   for each statement execute procedure trigger_notice()
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl (id int, idplus1 int GENERATED ALWAYS AS (id + 1) STORED)
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY concur_index5 on concur_heap(f2) WHERE f1='x'
;
----------
[BLOCK] DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize_bloom VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table loc2 drop column f2
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_template
;
----------
[ADMIN_TABLE] ANALYZE group_agg_pk
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_27293_2 ADD COLUMN newdata pg_temp_4.pg_temp_27293
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fk_partitioned_pk VALIDATE CONSTRAINT selffk
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "ec1_expr1" ON "public"."ec1" USING "btree" ((("ff" + 1)))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_inh_check" (
    "a" numeric,
    "b" numeric,
    CONSTRAINT "bmerged" CHECK ((("b")::double precision > (1)::double precision)),
    CONSTRAINT "bnoinherit" CHECK ((("b")::double precision > (100)::double precision)) NO INHERIT,
    CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child3_insert_trig
  after insert on child3 referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN indexed_col SET DATA TYPE text
;
