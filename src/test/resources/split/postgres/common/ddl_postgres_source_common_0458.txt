do $outer$
begin
  for i in 1..10 loop
   begin
    execute $ex$
      do $$
      declare x int = 0;
      begin
        x := 1 / x;
      end;
      $$;
    $ex$;
  exception when division_by_zero then
    raise notice 'caught division by zero';
  end;
  end loop;
end;
$outer$
;
DROP TABLE base_tbl_hist
;
CREATE AGGREGATE myaggn14b(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  INITCOND = '{}')
;
/*
 * 1.2. test CREATE INDEX with inserts
 */

-- Regular index with included columns
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
DROP TABLE check_ign_err2
;
drop function formarray(anyelement, variadic anyarray)
;
CREATE TABLE audit_tbls.schema_two_table_three(the_value text)
;
CREATE DOMAIN dump_test.us_postal_code AS TEXT
		               COLLATE "C"
					   DEFAULT '10014'
					   CONSTRAINT nn NOT NULL
					   CHECK(VALUE ~ '^\d{5}$' OR
							 VALUE ~ '^\d{5}-\d{4}$')
;
DROP TRIGGER trig_row_after ON rem1
;
analyze bigger_than_it_looks
;
create function error1(text) returns text language sql as
$$ SELECT relname::text FROM pg_class c WHERE c.oid = $1::regclass $$
;
DROP SCHEMA test_schema CASCADE
;
-- not supported by sepgsql
ALTER TABLE regtest_table_part VALIDATE CONSTRAINT test_ck
;
-- ok, skipped
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (lc_collate = ' ||
          quote_literal(current_setting('lc_collate')) ||
          ', lc_ctype = ' ||
          quote_literal(current_setting('lc_ctype')) || ');';
END
$$
;
-- check that leaf partitions are scanned when attaching a partitioned
-- table
CREATE TABLE hpart_5 (
	LIKE hash_parted
) PARTITION BY LIST (b)
;
alter table only p1 drop column f1
;
DROP DATABASE "postgres"
;
--
-- pgp_sym_decrypt(data, key, args)
--
CREATE FUNCTION pgp_sym_decrypt(bytea, text, text)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_sym_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
COMMENT ON TEXT SEARCH CONFIGURATION yiddish IS 'configuration for yiddish language'
;
analyze lprt_a
;
COMMENT ON FOREIGN TABLE ft1 IS 'foreign table'
;
-- First create tables employing the new AM using USING

-- plain CREATE TABLE
CREATE TABLE tableam_tbl_heap2(f1 int) USING heap2
;
CREATE FUNCTION ltq_rregex(lquery,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE INDEX t1_gin ON s4.t1 USING GIN (ia)
;
-- failed (not owner)
ALTER CONVERSION alt_conv2 SET SCHEMA alt_nsp2
;
-- fires nothing
create table event_trigger_fire2 (a int)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) information_schema.sql_parts
;
ALTER FUNCTION functest_E_2(int) LEAKPROOF
;
CREATE UNIQUE INDEX ON regress_constr_partitioned (a)
;
create table defcheck_0 partition of defcheck for values in (0)
;
create table rtest_person (pname text, pdesc text)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
create view base_tab_view as select c, a, b from base_tab
;
ANALYZE pg_temp_24.pg_temp_26747
;
CREATE TYPE int2range AS RANGE (
  SUBTYPE = int2
)
;
CREATE TABLE expr_stats_incompatible_test (
    c0 double precision,
    c1 boolean NOT NULL
)
;
DROP TABLE truncate_tab
;
CREATE TABLE tr_unique(id2 serial unique NOT NULL, data int)
;
CREATE OR REPLACE FUNCTION chkumapping()
 RETURNS TABLE (umname name, umserver name, umoptions text[])
 AS $$
SELECT r.rolname, s.srvname, m.umoptions
 FROM pg_user_mapping m
 LEFT JOIN pg_roles r ON (r.oid = m.umuser)
 JOIN pg_foreign_server s ON (s.oid = m.umserver)
 ORDER BY 2;
$$ LANGUAGE SQL
;
CREATE OR REPLACE FUNCTION
  pg_stat_reset_shared(target text DEFAULT NULL)
RETURNS void
LANGUAGE INTERNAL
CALLED ON NULL INPUT VOLATILE PARALLEL SAFE
AS 'pg_stat_reset_shared'
;
CREATE TABLE var_ptbl(q int, r text) PARTITION BY RANGE (q)
;
-- tablespace should not change even if there is an index rewrite
ALTER TABLE testschema.test_default_tab ALTER id TYPE bigint
;
CREATE FUNCTION cube(float8[], float8[]) RETURNS cube
AS '$libdir/cube', 'cube_a_f8_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE "public"."shoe_data" (
    "shoename" character(10),
    "sh_avail" integer,
    "slcolor" character(10),
    "slminlen" double precision,
    "slmaxlen" double precision,
    "slunit" character(8)
)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_cast
;
--
-- isn_weak() - Gets the weak input mode status
--
CREATE FUNCTION isn_weak()
	RETURNS boolean
	AS '$libdir/isn', 'weak_input_status'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL RESTRICTED
;
CREATE AGGREGATE myaggp20b(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, INITCOND = '{}')
;
-- user-defined operator class in partition key
CREATE FUNCTION my_int4_sort(int4,int4) RETURNS int LANGUAGE sql
  AS $$ SELECT CASE WHEN $1 = $2 THEN 0 WHEN $1 > $2 THEN 1 ELSE -1 END; $$
;
ALTER INDEX pg_toast.pg_toast_30321_index RENAME TO reind_con_toast_idx
;
ALTER TEXT SEARCH CONFIGURATION italian ADD MAPPING
    FOR word, hword_part, hword
	WITH italian_stem
;
------------------------------------------------------------------------------------------
[BLOCK] do $outer$
begin
  for i in 1..10 loop
   begin
    execute $ex$
      do $$
      declare x int = 0;
      begin
        x := 1 / x;
      end;
      $$;
    $ex$;
  exception when division_by_zero then
    raise notice 'caught division by zero';
  end;
  end loop;
end;
$outer$
;
----------
[DROP_TABLE] DROP TABLE base_tbl_hist
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn14b(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  INITCOND = '{}')
;
----------
[CREATE_TABLE] /*
 * 1.2. test CREATE INDEX with inserts
 */

-- Regular index with included columns
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[DROP_TABLE] DROP TABLE check_ign_err2
;
----------
[DROP_PROG_OBJ] drop function formarray(anyelement, variadic anyarray)
;
----------
[CREATE_TABLE] CREATE TABLE audit_tbls.schema_two_table_three(the_value text)
;
----------
[CREATE_TYPE] CREATE DOMAIN dump_test.us_postal_code AS TEXT
		               COLLATE "C"
					   DEFAULT '10014'
					   CONSTRAINT nn NOT NULL
					   CHECK(VALUE ~ '^\d{5}$' OR
							 VALUE ~ '^\d{5}-\d{4}$')
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_after ON rem1
;
----------
[ADMIN_TABLE] analyze bigger_than_it_looks
;
----------
[CREATE_PROG_OBJ] create function error1(text) returns text language sql as
$$ SELECT relname::text FROM pg_class c WHERE c.oid = $1::regclass $$
;
----------
[DROP_SCHEMA] DROP SCHEMA test_schema CASCADE
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- not supported by sepgsql
ALTER TABLE regtest_table_part VALIDATE CONSTRAINT test_ck
;
----------
[BLOCK] -- ok, skipped
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (lc_collate = ' ||
          quote_literal(current_setting('lc_collate')) ||
          ', lc_ctype = ' ||
          quote_literal(current_setting('lc_ctype')) || ');';
END
$$
;
----------
[CREATE_TABLE] -- check that leaf partitions are scanned when attaching a partitioned
-- table
CREATE TABLE hpart_5 (
	LIKE hash_parted
) PARTITION BY LIST (b)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table only p1 drop column f1
;
----------
[DROP_CATALOG] DROP DATABASE "postgres"
;
----------
[CREATE_PROG_OBJ] --
-- pgp_sym_decrypt(data, key, args)
--
CREATE FUNCTION pgp_sym_decrypt(bytea, text, text)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_sym_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION yiddish IS 'configuration for yiddish language'
;
----------
[ADMIN_TABLE] analyze lprt_a
;
----------
[COMMENT_TABLE] COMMENT ON FOREIGN TABLE ft1 IS 'foreign table'
;
----------
[CREATE_TABLE] -- First create tables employing the new AM using USING

-- plain CREATE TABLE
CREATE TABLE tableam_tbl_heap2(f1 int) USING heap2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltq_rregex(lquery,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX t1_gin ON s4.t1 USING GIN (ia)
;
----------
[ALTER_POLICY] -- failed (not owner)
ALTER CONVERSION alt_conv2 SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] -- fires nothing
create table event_trigger_fire2 (a int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) information_schema.sql_parts
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_E_2(int) LEAKPROOF
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON regress_constr_partitioned (a)
;
----------
[CREATE_TABLE] create table defcheck_0 partition of defcheck for values in (0)
;
----------
[CREATE_TABLE] create table rtest_person (pname text, pdesc text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_VIEW(SELECT)] create view base_tab_view as select c, a, b from base_tab
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_26747
;
----------
[CREATE_TYPE] CREATE TYPE int2range AS RANGE (
  SUBTYPE = int2
)
;
----------
[CREATE_TABLE] CREATE TABLE expr_stats_incompatible_test (
    c0 double precision,
    c1 boolean NOT NULL
)
;
----------
[DROP_TABLE] DROP TABLE truncate_tab
;
----------
[CREATE_TABLE] CREATE TABLE tr_unique(id2 serial unique NOT NULL, data int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION chkumapping()
 RETURNS TABLE (umname name, umserver name, umoptions text[])
 AS $$
SELECT r.rolname, s.srvname, m.umoptions
 FROM pg_user_mapping m
 LEFT JOIN pg_roles r ON (r.oid = m.umuser)
 JOIN pg_foreign_server s ON (s.oid = m.umserver)
 ORDER BY 2;
$$ LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  pg_stat_reset_shared(target text DEFAULT NULL)
RETURNS void
LANGUAGE INTERNAL
CALLED ON NULL INPUT VOLATILE PARALLEL SAFE
AS 'pg_stat_reset_shared'
;
----------
[CREATE_TABLE] CREATE TABLE var_ptbl(q int, r text) PARTITION BY RANGE (q)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- tablespace should not change even if there is an index rewrite
ALTER TABLE testschema.test_default_tab ALTER id TYPE bigint
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube(float8[], float8[]) RETURNS cube
AS '$libdir/cube', 'cube_a_f8_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."shoe_data" (
    "shoename" character(10),
    "sh_avail" integer,
    "slcolor" character(10),
    "slminlen" double precision,
    "slmaxlen" double precision,
    "slunit" character(8)
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_cast
;
----------
[CREATE_PROG_OBJ] --
-- isn_weak() - Gets the weak input mode status
--
CREATE FUNCTION isn_weak()
	RETURNS boolean
	AS '$libdir/isn', 'weak_input_status'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp20b(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] -- user-defined operator class in partition key
CREATE FUNCTION my_int4_sort(int4,int4) RETURNS int LANGUAGE sql
  AS $$ SELECT CASE WHEN $1 = $2 THEN 0 WHEN $1 > $2 THEN 1 ELSE -1 END; $$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30321_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION italian ADD MAPPING
    FOR word, hword_part, hword
	WITH italian_stem
;
