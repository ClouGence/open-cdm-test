--should succeed
create type float8range as range (subtype=float8, subtype_diff=float8mi)
;
DROP TABLE split_event_helper.rewrite_probe
;
CREATE FUNCTION "public"."bar_int4_text"(integer) RETURNS "text"
    LANGUAGE "sql"
    AS $_$ SELECT ('bar'::text || $1::text); $_$
;
CREATE TABLE prt1_p2 PARTITION OF prt1 FOR VALUES FROM (250) TO (500)
;
ALTER TABLE tbl2 ALTER COLUMN val2 TYPE text
;
ANALYZE public.gtest_normal
;
COMMENT ON CONSTRAINT comment_test_pk ON comment_test IS 'PRIMARY KEY constraint of comment_test'
;
--
-- We used to allow converting a table to a view by creating a "_RETURN"
-- rule for it, but no more.
--

create table rules_fooview (x int, y text)
;
CREATE FUNCTION dearmor(text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_dearmor'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create table bc (b int constraint check_b check (b <> 0))
;
CREATE TYPE gbtreekey8 (
	INTERNALLENGTH = 8,
	INPUT  = gbtreekey8_in,
	OUTPUT = gbtreekey8_out
)
;
-- calls test_stat_func, but hides error if it doesn't exist
    CREATE FUNCTION test_stat_func_ifexists() RETURNS VOID LANGUAGE plpgsql AS $$
    BEGIN
        PERFORM test_stat_func();
    EXCEPTION WHEN undefined_function THEN
    END;$$
;
CALL test_proc8b(_a, _b, _c)
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest10a"
    ADD CONSTRAINT "gtest10a_pkey" PRIMARY KEY ("a")
;
DROP SCHEMA IF EXISTS split_pub18_a CASCADE
;
COMMENT ON TEXT SEARCH CONFIGURATION swedish IS 'configuration for swedish language'
;
COMMENT ON AGGREGATE newcnt ("any") IS 'an agg(any) comment'
;
COMMENT ON TYPE issn
	IS 'International Standard Serial Number (ISSN)'
;
ALTER TABLE pg_temp_9.pg_temp_37998_2 ADD COLUMN newdata pg_temp_9.pg_temp_37998
;
ALTER OPERATOR <= (ean13, upc) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE DOMAIN yes_or_no AS character varying(3) COLLATE "C"
    CONSTRAINT yes_or_no_check CHECK (value IN ('YES', 'NO'))
;
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_hash
;
CREATE FUNCTION gin_index_check(index regclass)
RETURNS VOID
AS '$libdir/amcheck', 'gin_index_check'
LANGUAGE C STRICT
;
ALTER TABLE atest12 SET (autovacuum_enabled = off)
;
/*
 * 6.33
 * FIELDS view
 */

-- feature not supported


/*
 * 6.34
 * KEY_COLUMN_USAGE view
 */

CREATE VIEW key_column_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc_nspname AS sql_identifier) AS constraint_schema,
           CAST(conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr_nspname AS sql_identifier) AS table_schema,
           CAST(relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(CASE WHEN contype = 'f' THEN
                       _pg_index_position(ss.conindid, ss.confkey[(ss.x).n])
                     ELSE NULL
                END AS cardinal_number)
             AS position_in_unique_constraint
    FROM pg_attribute a,
         (SELECT r.oid AS roid, r.relname, r.relowner,
                 nc.nspname AS nc_nspname, nr.nspname AS nr_nspname,
                 c.oid AS coid, c.conname, c.contype, c.conindid,
                 c.confkey, c.confrelid,
                 _pg_expandarray(c.conkey) AS x
          FROM pg_namespace nr, pg_class r, pg_namespace nc,
               pg_constraint c
          WHERE nr.oid = r.relnamespace
                AND r.oid = c.conrelid
                AND nc.oid = c.connamespace
                AND c.contype IN ('p', 'u', 'f')
                AND r.relkind IN ('r', 'p')
                AND (NOT pg_is_other_temp_schema(nr.oid)) ) AS ss
    WHERE ss.roid = a.attrelid
          AND a.attnum = (ss.x).x
          AND NOT a.attisdropped
          AND (pg_has_role(relowner, 'USAGE')
               OR has_column_privilege(roid, a.attnum,
                                       'SELECT, INSERT, UPDATE, REFERENCES'))
;
ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE int
;
alter table alterlock2 validate constraint alterlock2nv
;
create function foreach_test(anyarray)
returns void as $$
declare x int;
begin
  foreach x in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
DROP TABLE ctas_base
;
create function nochange(int) returns int
  as 'select $1 limit 1' language sql stable
;
DROP PUBLICATION tap_pub_A
;
alter table my_locks set (autovacuum_enabled = false)
;
/*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
CREATE TABLE IF NOT EXISTS as_select1 AS EXECUTE select1
;
CREATE FUNCTION ltree_eq(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FUNCTION gbt_time_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE public.system
;
CREATE INDEX iprt1_e_p3_ab2 on prt1_e_p3(((a+b)/2))
;
DROP TABLE part_fail
;
-- Check UPDATE with inherited target and an inherited source table
create temp table foo(f1 int, f2 int)
;
create or replace function sc_test() returns setof integer as $$
declare
  c refcursor;
  x integer;
begin
  open c scroll for select f1 from int4_tbl;
  fetch last from c into x;
  while found loop
    return next x;
    fetch prior from c into x;
  end loop;
  close c;
end;
$$ language plpgsql
;
CREATE TEXT SEARCH DICTIONARY "public"."hunspell_invalid_2" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_long', afffile = 'hunspell_sample_num' )
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_conversion
;
--     P    P
-- should ERROR: ffnp(anyarray) not matched by ffnp(int[])
CREATE AGGREGATE myaggn04a(*) (SFUNC = stfp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
COMMENT ON TEXT SEARCH DICTIONARY turkish_stem IS 'snowball stemmer for turkish language'
;
CREATE DATABASE split_db_probe WITH OWNER = postgres TEMPLATE = template0 ENCODING = 'UTF8' STRATEGY = WAL_LOG LOCALE = 'C.UTF-8' LOCALE_PROVIDER = libc TABLESPACE = split_ts_space_renamed ALLOW_CONNECTIONS = true CONNECTION LIMIT = 5 IS_TEMPLATE = false
;
-- Test aliasing of joins

create view view_of_joins as
select * from
  (select * from (tbl1 cross join tbl2) same) ss,
  (tbl3 cross join tbl4) same
;
CREATE INDEX cluster_part_ind ON cluster_part_tab(a)
;
create function explain_memoize(query text, hide_hitmiss bool) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off) %s',
            query)
    loop
        if hide_hitmiss = true then
                ln := regexp_replace(ln, 'Hits: 0', 'Hits: Zero');
                ln := regexp_replace(ln, 'Hits: \d+', 'Hits: N');
                ln := regexp_replace(ln, 'Misses: 0', 'Misses: Zero');
                ln := regexp_replace(ln, 'Misses: \d+', 'Misses: N');
        end if;
        ln := regexp_replace(ln, 'Evictions: 0', 'Evictions: Zero');
        ln := regexp_replace(ln, 'Evictions: \d+', 'Evictions: N');
        ln := regexp_replace(ln, 'Memory Usage: \d+', 'Memory Usage: N');
	ln := regexp_replace(ln, 'Heap Fetches: \d+', 'Heap Fetches: N');
	ln := regexp_replace(ln, 'loops=\d+', 'loops=N');
        return next ln;
    end loop;
end;
$$
;
CREATE TYPE ghstore
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] --should succeed
create type float8range as range (subtype=float8, subtype_diff=float8mi)
;
----------
[DROP_TABLE] DROP TABLE split_event_helper.rewrite_probe
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."bar_int4_text"(integer) RETURNS "text"
    LANGUAGE "sql"
    AS $_$ SELECT ('bar'::text || $1::text); $_$
;
----------
[CREATE_TABLE] CREATE TABLE prt1_p2 PARTITION OF prt1 FOR VALUES FROM (250) TO (500)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl2 ALTER COLUMN val2 TYPE text
;
----------
[ADMIN_TABLE] ANALYZE public.gtest_normal
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT comment_test_pk ON comment_test IS 'PRIMARY KEY constraint of comment_test'
;
----------
[CREATE_TABLE] --
-- We used to allow converting a table to a view by creating a "_RETURN"
-- rule for it, but no more.
--

create table rules_fooview (x int, y text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dearmor(text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_dearmor'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table bc (b int constraint check_b check (b <> 0))
;
----------
[CREATE_TYPE] CREATE TYPE gbtreekey8 (
	INTERNALLENGTH = 8,
	INPUT  = gbtreekey8_in,
	OUTPUT = gbtreekey8_out
)
;
----------
[CREATE_PROG_OBJ] -- calls test_stat_func, but hides error if it doesn't exist
    CREATE FUNCTION test_stat_func_ifexists() RETURNS VOID LANGUAGE plpgsql AS $$
    BEGIN
        PERFORM test_stat_func();
    EXCEPTION WHEN undefined_function THEN
    END;$$
;
----------
[CALL_PROG_OBJ] CALL test_proc8b(_a, _b, _c)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest10a"
    ADD CONSTRAINT "gtest10a_pkey" PRIMARY KEY ("a")
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_pub18_a CASCADE
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION swedish IS 'configuration for swedish language'
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE newcnt ("any") IS 'an agg(any) comment'
;
----------
[COMMENT_TYPE] COMMENT ON TYPE issn
	IS 'International Standard Serial Number (ISSN)'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_37998_2 ADD COLUMN newdata pg_temp_9.pg_temp_37998
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ean13, upc) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_TYPE] CREATE DOMAIN yes_or_no AS character varying(3) COLLATE "C"
    CONSTRAINT yes_or_no_check CHECK (value IN ('YES', 'NO'))
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_hash
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_index_check(index regclass)
RETURNS VOID
AS '$libdir/amcheck', 'gin_index_check'
LANGUAGE C STRICT
;
----------
[ALTER_TABLE] ALTER TABLE atest12 SET (autovacuum_enabled = off)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.33
 * FIELDS view
 */

-- feature not supported


/*
 * 6.34
 * KEY_COLUMN_USAGE view
 */

CREATE VIEW key_column_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc_nspname AS sql_identifier) AS constraint_schema,
           CAST(conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr_nspname AS sql_identifier) AS table_schema,
           CAST(relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(CASE WHEN contype = 'f' THEN
                       _pg_index_position(ss.conindid, ss.confkey[(ss.x).n])
                     ELSE NULL
                END AS cardinal_number)
             AS position_in_unique_constraint
    FROM pg_attribute a,
         (SELECT r.oid AS roid, r.relname, r.relowner,
                 nc.nspname AS nc_nspname, nr.nspname AS nr_nspname,
                 c.oid AS coid, c.conname, c.contype, c.conindid,
                 c.confkey, c.confrelid,
                 _pg_expandarray(c.conkey) AS x
          FROM pg_namespace nr, pg_class r, pg_namespace nc,
               pg_constraint c
          WHERE nr.oid = r.relnamespace
                AND r.oid = c.conrelid
                AND nc.oid = c.connamespace
                AND c.contype IN ('p', 'u', 'f')
                AND r.relkind IN ('r', 'p')
                AND (NOT pg_is_other_temp_schema(nr.oid)) ) AS ss
    WHERE ss.roid = a.attrelid
          AND a.attnum = (ss.x).x
          AND NOT a.attisdropped
          AND (pg_has_role(relowner, 'USAGE')
               OR has_column_privilege(roid, a.attnum,
                                       'SELECT, INSERT, UPDATE, REFERENCES'))
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE int
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] alter table alterlock2 validate constraint alterlock2nv
;
----------
[CREATE_PROG_OBJ] create function foreach_test(anyarray)
returns void as $$
declare x int;
begin
  foreach x in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
----------
[DROP_TABLE] DROP TABLE ctas_base
;
----------
[CREATE_PROG_OBJ] create function nochange(int) returns int
  as 'select $1 limit 1' language sql stable
;
----------
[DROP_PUB_SUB] DROP PUBLICATION tap_pub_A
;
----------
[ALTER_TABLE] alter table my_locks set (autovacuum_enabled = false)
;
----------
[CREATE_POLICY] /*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
----------
[CREATE_TABLE|UNSAFE] CREATE TABLE IF NOT EXISTS as_select1 AS EXECUTE select1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_eq(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE public.system
;
----------
[ADD_INDEX] CREATE INDEX iprt1_e_p3_ab2 on prt1_e_p3(((a+b)/2))
;
----------
[DROP_TABLE] DROP TABLE part_fail
;
----------
[CREATE_TABLE] -- Check UPDATE with inherited target and an inherited source table
create temp table foo(f1 int, f2 int)
;
----------
[CREATE_PROG_OBJ] create or replace function sc_test() returns setof integer as $$
declare
  c refcursor;
  x integer;
begin
  open c scroll for select f1 from int4_tbl;
  fetch last from c into x;
  while found loop
    return next x;
    fetch prior from c into x;
  end loop;
  close c;
end;
$$ language plpgsql
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."hunspell_invalid_2" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_long', afffile = 'hunspell_sample_num' )
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_conversion
;
----------
[CREATE_PROG_OBJ] --     P    P
-- should ERROR: ffnp(anyarray) not matched by ffnp(int[])
CREATE AGGREGATE myaggn04a(*) (SFUNC = stfp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY turkish_stem IS 'snowball stemmer for turkish language'
;
----------
[CREATE_CATALOG] CREATE DATABASE split_db_probe WITH OWNER = postgres TEMPLATE = template0 ENCODING = 'UTF8' STRATEGY = WAL_LOG LOCALE = 'C.UTF-8' LOCALE_PROVIDER = libc TABLESPACE = split_ts_space_renamed ALLOW_CONNECTIONS = true CONNECTION LIMIT = 5 IS_TEMPLATE = false
;
----------
[CREATE_VIEW(SELECT)] -- Test aliasing of joins

create view view_of_joins as
select * from
  (select * from (tbl1 cross join tbl2) same) ss,
  (tbl3 cross join tbl4) same
;
----------
[ADD_INDEX] CREATE INDEX cluster_part_ind ON cluster_part_tab(a)
;
----------
[CREATE_PROG_OBJ] create function explain_memoize(query text, hide_hitmiss bool) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off) %s',
            query)
    loop
        if hide_hitmiss = true then
                ln := regexp_replace(ln, 'Hits: 0', 'Hits: Zero');
                ln := regexp_replace(ln, 'Hits: \d+', 'Hits: N');
                ln := regexp_replace(ln, 'Misses: 0', 'Misses: Zero');
                ln := regexp_replace(ln, 'Misses: \d+', 'Misses: N');
        end if;
        ln := regexp_replace(ln, 'Evictions: 0', 'Evictions: Zero');
        ln := regexp_replace(ln, 'Evictions: \d+', 'Evictions: N');
        ln := regexp_replace(ln, 'Memory Usage: \d+', 'Memory Usage: N');
	ln := regexp_replace(ln, 'Heap Fetches: \d+', 'Heap Fetches: N');
	ln := regexp_replace(ln, 'loops=\d+', 'loops=N');
        return next ln;
    end loop;
end;
$$
;
----------
[CREATE_TYPE] CREATE TYPE ghstore
;
