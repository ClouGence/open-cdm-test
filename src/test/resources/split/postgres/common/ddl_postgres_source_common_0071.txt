create table mcrparted0 partition of mcrparted for values from (minvalue, minvalue, minvalue) to (1, 1, 1)
;
CREATE TABLE nobarf(id serial primary key, data text)
;
-- fail because of dependent type
drop domain domaindroptest
;
drop table public.whereami
;
DROP TABLE plt2_adv_p2_2
;
CREATE EXTENSION test_ext1 SCHEMA test_ext1
;
drop table temp_parted_oncommit_test
;
drop table child, parent
;
CREATE INDEX iprt_p1_a ON prt_p1 (a)
;
ALTER TYPE split_domain_type_next.mood ADD VALUE IF NOT EXISTS 'calm' AFTER 'ok'
;
CREATE FUNCTION subarray(_int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE "public"."stud_emp" (
    "name" "text",
    "age" integer,
    "location" "point",
    "salary" integer,
    "manager" "name",
    "gpa" double precision,
    "percent" integer
)
;
CREATE FUNCTION gin_extract_value_oid(oid, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
ALTER TABLE fk_r DETACH PARTITION fk_r_2
;
ALTER TABLE ONLY "public"."plt1_e" ATTACH PARTITION "public"."plt1_e_p2" FOR VALUES IN ('0001', '0005', '0002', '0009')
;
DROP PUBLICATION "pub2"
;
-- UPDATE hash_ovfl_heap
--    SET x = 1000
--   WHERE x = 90;

-- this vacuums the index as well
-- VACUUM hash_ovfl_heap;

-- SELECT count(*) AS i0 FROM hash_ovfl_heap
--   WHERE x = 90;

-- SELECT count(*) AS i988 FROM hash_ovfl_heap
--  WHERE x = 1000;

--
-- Cause some overflow insert and splits.
--
CREATE TABLE hash_split_heap (keycol INT)
;
create table hpart4 partition of hash_parted for values with (modulus 8, remainder 4)
;
CREATE FUNCTION texticnlike(citext, citext)
RETURNS bool AS 'texticnlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE toast_bug ALTER COLUMN buggy SET STORAGE extended
;
--
-- Test partial unique index inference
--
create unique index partial_key_index on insertconflicttest(key) where fruit like '%berry'
;
ALTER INDEX pg_toast.pg_toast_27702_index RENAME TO reind_con_toast_idx
;
DROP CONVERSION IF EXISTS split_conversion_probe_next.latin1_to_utf8_renamed RESTRICT
;
-- Test multiple parents with overlapping primary keys
create table inh_parent1(a int, b int, c int, primary key (a, b))
;
CREATE MATERIALIZED VIEW ddl_deparse_mv AS
  SELECT * FROM datatype_table LIMIT 1 WITH NO DATA
;
ANALYZE public.tt1
;
DROP TRIGGER trig_c1_100 ON part_c_1_100
;
ALTER COLLATION test1 RENAME TO test11
;
DROP DOMAIN queryfuncs_test_domain
;
create trigger tg_chkbacklink before insert or update
    on PLine for each row execute procedure tg_chkbacklink()
;
CREATE EXTENSION pg_freespacemap
;
CREATE FOREIGN TABLE batch_table_p1f
	PARTITION OF batch_table
	FOR VALUES WITH (MODULUS 3, REMAINDER 1)
	SERVER loopback
	OPTIONS (table_name 'batch_table_p1', batch_size '1')
;
ALTER INDEX pg_toast.pg_toast_28385_index RENAME TO reind_con_toast_idx
;
create function ext8_even (posint) returns bool as
  'select ($1 % 2) = 0' language sql
;
CREATE TABLE replace_sb(data text)
;
CREATE OPERATOR === (
        PROCEDURE = int8eq,
        LEFTARG = bigint,
        RIGHTARG = bigint,
        COMMUTATOR = ===
)
;
-- gone

-- likewise for columns used in partition expressions
create domain intdom1 as int
;
CREATE TABLE test_root (a int PRIMARY KEY, b int, c int) PARTITION BY RANGE (a)
;
CREATE TABLE ctas_stats_2 AS
  SELECT a AS col1, 4::int AS col2
    FROM generate_series(1, 5) AS tab(a) WHERE a < 4 AND a > 1
;
ALTER TABLE pg_toast.pg_toast_28219 RENAME TO reind_con_toast
;
CREATE FUNCTION f4 () RETURNS text
    AS 'SELECT sepgsql_getcon()'
    LANGUAGE sql
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_transform
;
DROP TRIGGER row_before_insupd_trigger ON base_tbl
;
CREATE TABLE tbl_ok(data int not null) WITH (AUTOVACUUM_ENABLED = false)
;
CREATE TABLE pagg_tab_ml_p3_s2 PARTITION OF pagg_tab_ml_p3 FOR VALUES FROM (7) TO (10)
;
-- no such relation
alter table nonesuch rename to stud_emp
;
CREATE TABLE t12 (e int[]) INHERITS (t1)
;
create index on idxpart (a) where b > 1000
;
TRUNCATE notnull_tbl1
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table mcrparted0 partition of mcrparted for values from (minvalue, minvalue, minvalue) to (1, 1, 1)
;
----------
[CREATE_TABLE] CREATE TABLE nobarf(id serial primary key, data text)
;
----------
[DROP_TYPE] -- fail because of dependent type
drop domain domaindroptest
;
----------
[DROP_TABLE] drop table public.whereami
;
----------
[DROP_TABLE] DROP TABLE plt2_adv_p2_2
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext1 SCHEMA test_ext1
;
----------
[DROP_TABLE] drop table temp_parted_oncommit_test
;
----------
[DROP_TABLE] drop table child, parent
;
----------
[ADD_INDEX] CREATE INDEX iprt_p1_a ON prt_p1 (a)
;
----------
[ALTER_TYPE] ALTER TYPE split_domain_type_next.mood ADD VALUE IF NOT EXISTS 'calm' AFTER 'ok'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION subarray(_int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."stud_emp" (
    "name" "text",
    "age" integer,
    "location" "point",
    "salary" integer,
    "manager" "name",
    "gpa" double precision,
    "percent" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_oid(oid, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_r DETACH PARTITION fk_r_2
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."plt1_e" ATTACH PARTITION "public"."plt1_e_p2" FOR VALUES IN ('0001', '0005', '0002', '0009')
;
----------
[DROP_PUB_SUB] DROP PUBLICATION "pub2"
;
----------
[CREATE_TABLE] -- UPDATE hash_ovfl_heap
--    SET x = 1000
--   WHERE x = 90;

-- this vacuums the index as well
-- VACUUM hash_ovfl_heap;

-- SELECT count(*) AS i0 FROM hash_ovfl_heap
--   WHERE x = 90;

-- SELECT count(*) AS i988 FROM hash_ovfl_heap
--  WHERE x = 1000;

--
-- Cause some overflow insert and splits.
--
CREATE TABLE hash_split_heap (keycol INT)
;
----------
[CREATE_TABLE] create table hpart4 partition of hash_parted for values with (modulus 8, remainder 4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION texticnlike(citext, citext)
RETURNS bool AS 'texticnlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE toast_bug ALTER COLUMN buggy SET STORAGE extended
;
----------
[ADD_INDEX] --
-- Test partial unique index inference
--
create unique index partial_key_index on insertconflicttest(key) where fruit like '%berry'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27702_index RENAME TO reind_con_toast_idx
;
----------
[DROP_POLICY] DROP CONVERSION IF EXISTS split_conversion_probe_next.latin1_to_utf8_renamed RESTRICT
;
----------
[CREATE_TABLE] -- Test multiple parents with overlapping primary keys
create table inh_parent1(a int, b int, c int, primary key (a, b))
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW ddl_deparse_mv AS
  SELECT * FROM datatype_table LIMIT 1 WITH NO DATA
;
----------
[ADMIN_TABLE] ANALYZE public.tt1
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_c1_100 ON part_c_1_100
;
----------
[ALTER_POLICY] ALTER COLLATION test1 RENAME TO test11
;
----------
[DROP_TYPE] DROP DOMAIN queryfuncs_test_domain
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_chkbacklink before insert or update
    on PLine for each row execute procedure tg_chkbacklink()
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pg_freespacemap
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE batch_table_p1f
	PARTITION OF batch_table
	FOR VALUES WITH (MODULUS 3, REMAINDER 1)
	SERVER loopback
	OPTIONS (table_name 'batch_table_p1', batch_size '1')
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28385_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] create function ext8_even (posint) returns bool as
  'select ($1 % 2) = 0' language sql
;
----------
[CREATE_TABLE] CREATE TABLE replace_sb(data text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR === (
        PROCEDURE = int8eq,
        LEFTARG = bigint,
        RIGHTARG = bigint,
        COMMUTATOR = ===
)
;
----------
[CREATE_TYPE] -- gone

-- likewise for columns used in partition expressions
create domain intdom1 as int
;
----------
[CREATE_TABLE] CREATE TABLE test_root (a int PRIMARY KEY, b int, c int) PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_stats_2 AS
  SELECT a AS col1, 4::int AS col2
    FROM generate_series(1, 5) AS tab(a) WHERE a < 4 AND a > 1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28219 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f4 () RETURNS text
    AS 'SELECT sepgsql_getcon()'
    LANGUAGE sql
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_transform
;
----------
[DROP_TRIGGER] DROP TRIGGER row_before_insupd_trigger ON base_tbl
;
----------
[CREATE_TABLE] CREATE TABLE tbl_ok(data int not null) WITH (AUTOVACUUM_ENABLED = false)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p3_s2 PARTITION OF pagg_tab_ml_p3 FOR VALUES FROM (7) TO (10)
;
----------
[RENAME_TABLE] -- no such relation
alter table nonesuch rename to stud_emp
;
----------
[CREATE_TABLE] CREATE TABLE t12 (e int[]) INHERITS (t1)
;
----------
[ADD_INDEX] create index on idxpart (a) where b > 1000
;
----------
[TRUNCATE_TABLE] TRUNCATE notnull_tbl1
;
