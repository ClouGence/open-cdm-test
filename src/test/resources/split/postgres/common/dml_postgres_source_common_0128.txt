-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."itest6"'::pg_catalog.regclass
;
INSERT INTO numrange_test2 VALUES('[, 5)')
;
INSERT INTO fd_pt1 VALUES (1, 'fd_pt1'::text, '1994-01-01'::date)
;
UPDATE pt set a = 1 where a = 2
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10074', relminmxid = '1'
WHERE oid = '28718'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2667', relminmxid = '1'
WHERE oid = '"public"."tbl_include_unique2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15806', relminmxid = '3'
WHERE oid = '"public"."plt2_p1"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('6', 176)
;
INSERT INTO fk_notpartitioned_fk VALUES(100,100), (1000,1000)
;
-- failed
INSERT INTO t1p (o) VALUES (9)
;
INSERT INTO unit VALUES ('m', 100.0)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c', 'd')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2347', relminmxid = '1'
WHERE oid = '"public"."mlparted11"'::pg_catalog.regclass
;
INSERT INTO num_exp_add VALUES (2,8,'-34263611.215397047')
;
INSERT INTO caster (citext)        VALUES ('192.168.100.128/25'::cidr)
;
-- id matches but no conflict
INSERT INTO temporal_mltrng3 (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2010-01-01', '2020-01-01'))) ON CONFLICT (id, valid_at) DO NOTHING
;
INSERT INTO test31 VALUES (2, 'ABC')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3966', relminmxid = '1'
WHERE oid = '21244'
;
UPDATE accounts SET balance = balance - 1200
    WHERE noisy_oper('upid', accountid, '=', 'checking')
    AND noisy_oper('up', balance, '>', 200.0)
    AND EXISTS (
        SELECT accountid
        FROM accounts_ext ae
        WHERE noisy_oper('lock_id', ae.accountid, '=', accounts.accountid)
            AND noisy_oper('lock_bal', ae.balance, '>', 200.0)
        FOR UPDATE
    )
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18606', relminmxid = '3'
WHERE oid = '"public"."pagg_tab1_p1"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('1', 631)
;
INSERT INTO gtest21ax (a) VALUES (0)
;
INSERT INTO test_having VALUES (2, 2, 'AAAA', 'c')
;
UPDATE sub_parted set a = 2 WHERE c = 10
;
insert into fkeys values (60, '6', 4)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8863', relminmxid = '1'
WHERE oid = '28061'
;
-- fail - column list "a" does not cover the REPLICA IDENTITY INDEX on "c"
UPDATE rf_tbl_abcd_nopk SET a = 1
;
UPDATE update_test SET (c,b) = ('car', a+b), a = a + 1 WHERE a = 10
;
UPDATE public.loct SET a = 1 RETURNING a, b
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."itest7e"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3782', relminmxid = '1'
WHERE oid = '"public"."default_tbl"'::pg_catalog.regclass
;
-- fail - "a" is not in REPLICA IDENTITY INDEX
UPDATE rf_tbl_abcd_nopk SET a = 1
;
DELETE FROM table_without_key WHERE data = 3
;
update test_jsonb_subscript set test_json['a']['b']['c'][2] = '1'
;
DELETE FROM result_tbl
;
insert into history ( customerid , timestamp , action_taken , narrative ) values ( $1  , $2  , 'test' , 'test' )
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt4_n_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO public.ths_p2 VALUES (3, 693)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part2_self_fk"'::pg_catalog.regclass
  AND attname IN ('id', 'id_abc')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest22c"'::pg_catalog.regclass
;
-- Cannot cast to box on assignment.
INSERT INTO caster (box)           VALUES ('(0,0),(1,1)'::text)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15729', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p3_s1"'::pg_catalog.regclass
;
-- insert 16 rows, one row for each test to perform.
insert into hp_prefix_test
select
  case a when 0 then null else 1 end,
  case b when 0 then null else 2 end,
  case c when 0 then null else 3 end,
  case d when 0 then null else 4 end
from
  generate_series(0,1) a,
  generate_series(0,1) b,
  generate_Series(0,1) c,
  generate_Series(0,1) d
;
INSERT INTO fktable VALUES (5, 10)
;
INSERT INTO num_exp_sub VALUES (6,8,'19020.57763026')
;
INSERT INTO test_cube VALUES ('(1,1)'), ('(100000)'), ('(0, 100000)')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '18027'
;
INSERT INTO num_exp_mul VALUES (7,4,'-647577464846017.9715')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17260', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '752', relminmxid = '1'
WHERE oid = '"public"."tenk1"'::pg_catalog.regclass
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."itest6"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO numrange_test2 VALUES('[, 5)')
;
----------
[INSERT] INSERT INTO fd_pt1 VALUES (1, 'fd_pt1'::text, '1994-01-01'::date)
;
----------
[UPDATE] UPDATE pt set a = 1 where a = 2
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10074', relminmxid = '1'
WHERE oid = '28718'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2667', relminmxid = '1'
WHERE oid = '"public"."tbl_include_unique2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15806', relminmxid = '3'
WHERE oid = '"public"."plt2_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 176)
;
----------
[INSERT] INSERT INTO fk_notpartitioned_fk VALUES(100,100), (1000,1000)
;
----------
[INSERT|SELECT] -- failed
INSERT INTO t1p (o) VALUES (9)
;
----------
[INSERT] INSERT INTO unit VALUES ('m', 100.0)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c', 'd')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2347', relminmxid = '1'
WHERE oid = '"public"."mlparted11"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (2,8,'-34263611.215397047')
;
----------
[INSERT|SELECT] INSERT INTO caster (citext)        VALUES ('192.168.100.128/25'::cidr)
;
----------
[MERGE|SELECT] -- id matches but no conflict
INSERT INTO temporal_mltrng3 (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2010-01-01', '2020-01-01'))) ON CONFLICT (id, valid_at) DO NOTHING
;
----------
[INSERT] INSERT INTO test31 VALUES (2, 'ABC')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3966', relminmxid = '1'
WHERE oid = '21244'
;
----------
[UPDATE|SELECT|QUERY_LOCK] UPDATE accounts SET balance = balance - 1200
    WHERE noisy_oper('upid', accountid, '=', 'checking')
    AND noisy_oper('up', balance, '>', 200.0)
    AND EXISTS (
        SELECT accountid
        FROM accounts_ext ae
        WHERE noisy_oper('lock_id', ae.accountid, '=', accounts.accountid)
            AND noisy_oper('lock_bal', ae.balance, '>', 200.0)
        FOR UPDATE
    )
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '18606', relminmxid = '3'
WHERE oid = '"public"."pagg_tab1_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 631)
;
----------
[INSERT|SELECT] INSERT INTO gtest21ax (a) VALUES (0)
;
----------
[INSERT] INSERT INTO test_having VALUES (2, 2, 'AAAA', 'c')
;
----------
[UPDATE] UPDATE sub_parted set a = 2 WHERE c = 10
;
----------
[INSERT] insert into fkeys values (60, '6', 4)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8863', relminmxid = '1'
WHERE oid = '28061'
;
----------
[UPDATE] -- fail - column list "a" does not cover the REPLICA IDENTITY INDEX on "c"
UPDATE rf_tbl_abcd_nopk SET a = 1
;
----------
[UPDATE] UPDATE update_test SET (c,b) = ('car', a+b), a = a + 1 WHERE a = 10
;
----------
[UPDATE] UPDATE public.loct SET a = 1 RETURNING a, b
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."itest7e"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3782', relminmxid = '1'
WHERE oid = '"public"."default_tbl"'::pg_catalog.regclass
;
----------
[UPDATE] -- fail - "a" is not in REPLICA IDENTITY INDEX
UPDATE rf_tbl_abcd_nopk SET a = 1
;
----------
[DELETE] DELETE FROM table_without_key WHERE data = 3
;
----------
[UPDATE] update test_jsonb_subscript set test_json['a']['b']['c'][2] = '1'
;
----------
[DELETE] DELETE FROM result_tbl
;
----------
[INSERT|SELECT] insert into history ( customerid , timestamp , action_taken , narrative ) values ( $1  , $2  , 'test' , 'test' )
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt4_n_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 693)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part2_self_fk"'::pg_catalog.regclass
  AND attname IN ('id', 'id_abc')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest22c"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- Cannot cast to box on assignment.
INSERT INTO caster (box)           VALUES ('(0,0),(1,1)'::text)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15729', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p3_s1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- insert 16 rows, one row for each test to perform.
insert into hp_prefix_test
select
  case a when 0 then null else 1 end,
  case b when 0 then null else 2 end,
  case c when 0 then null else 3 end,
  case d when 0 then null else 4 end
from
  generate_series(0,1) a,
  generate_series(0,1) b,
  generate_Series(0,1) c,
  generate_Series(0,1) d
;
----------
[INSERT] INSERT INTO fktable VALUES (5, 10)
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (6,8,'19020.57763026')
;
----------
[INSERT] INSERT INTO test_cube VALUES ('(1,1)'), ('(100000)'), ('(0, 100000)')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '18027'
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (7,4,'-647577464846017.9715')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17260', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '752', relminmxid = '1'
WHERE oid = '"public"."tenk1"'::pg_catalog.regclass
;
