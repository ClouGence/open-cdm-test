ALTER TABLE tab1 ENABLE REPLICA TRIGGER test1
;
create table parted_notnull_inh_test1 partition of parted_notnull_inh_test (a not null, b default 1) for values in (1)
;
CREATE TABLE part_7_a_null (
	c int,
	d int,
	e int,
	LIKE list_parted2,  
	CONSTRAINT check_b CHECK (b IS NULL OR b = 'a'),
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
	)
;
-- Check that an unreserved keyword can be used as a variable name

create function unreserved_test() returns int as $$
declare
  forward int := 21;
begin
  forward := forward * 2;
  return forward;
end
$$ language plpgsql
;
/*
 * 5.2
 * INFORMATION_SCHEMA_CATALOG_NAME view
 */

CREATE VIEW information_schema_catalog_name AS
    SELECT CAST(current_database() AS sql_identifier) AS catalog_name
;
--
-- \crosstabview
--

CREATE TABLE ctv_data (v, h, c, i, d) AS
VALUES
   ('v1','h2','foo', 3, '2015-04-01'::date),
   ('v2','h1','bar', 3, '2015-01-02'),
   ('v1','h0','baz', NULL, '2015-07-12'),
   ('v0','h4','qux', 4, '2015-07-15'),
   ('v0','h4','dbl', -3, '2014-12-15'),
   ('v0',NULL,'qux', 5, '2014-07-15'),
   ('v1','h2','quux',7, '2015-04-04')
;
CREATE TABLE inh_parent ()
;
CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub5
;
CREATE AGGREGATE myaggp15a(BASETYPE = anyelement, SFUNC = tfnp,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
CLUSTER vaccluster
;
CREATE TABLE testschema.test_tab(id int) TABLESPACE regress_tblspace
;
DROP SUBSCRIPTION regress_sub2
;
-- Functional index
CREATE UNIQUE INDEX sj_fn_idx ON sj((a * a))
;
alter type insenum add value 'i4' before 'L2'
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_replication_origin
;
CREATE RULE y_rule AS ON INSERT TO y WHERE a=0 DO INSTEAD DELETE FROM y
;
-- Check setup of indisvalid across a complex partition tree on index
-- creation.  If one index in a partition index is invalid, so should its
-- partitioned index.
create table parted_isvalid_tab (a int, b int) partition by range (a)
;
--
-- Test WITH attached to a data-modifying statement
--

CREATE TEMPORARY TABLE y (a INTEGER)
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE user_enum
;
alter table schema1.abc drop address
;
ANALYZE pg_temp_12.pg_temp_26560
;
DROP FUNCTION pg_get_wal_stats_till_end_of_wal(pg_lsn, boolean)
;
VACUUM pg_catalog.pg_statistic
;
ALTER OPERATOR === (boolean, boolean) SET (JOIN = contjoinsel)
;
-- Clean up
DROP TABLE pxtest2
;
create table event_trigger_fire4 (a int)
;
drop type textrange1
;
drop type cube
;
CREATE TABLE regular_table (a int)
;
--jsonb_agg

CREATE TEMP TABLE rows AS
SELECT x, 'txt' || x as y
FROM generate_series(1,3) AS x
;
CREATE TABLE tidrangescan(id integer, data text)
;
create domain insert_pos_ints as int[] check (value[1] > 0)
;
SECURITY LABEL ON COLUMN dummy_seclabel_tbl1 IS 'unclassified'
;
CREATE FUNCTION "public"."tg_pline_bu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotname != old.slotname then
        delete from PLine where slotname = old.slotname;
	insert into PLine (
		    slotname,
		    phonenumber,
		    comment,
		    backlink
		) values (
		    new.slotname,
		    new.phonenumber,
		    new.comment,
		    new.backlink
		);
        return null;
    end if;
    return new;
end;
$$
;
CREATE CAST (varchar AS citext) WITHOUT FUNCTION AS ASSIGNMENT
;
ANALYZE pg_temp_9.pg_temp_26686_2
;
alter table parted_stmt_trig enable trigger trig_ins_after_parent
;
ALTER FUNCTION gbt_float8_compress(internal) PARALLEL SAFE
;
CREATE TABLE itest7b (a int)
;
CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = numeric_avg_combine,
	finalfunc_modify = shareable  
)
;
CREATE TABLE testhstore (h hstore)
;
CREATE TABLE "public"."person" (
    "name" "text",
    "age" integer,
    "location" "point"
)
;
CREATE TABLE part_c_1_10 PARTITION OF part_c FOR VALUES FROM (1) TO (10)
;
CREATE FUNCTION trig_fn() RETURNS trigger LANGUAGE plpgsql AS
  $$ BEGIN RETURN NULL; END; $$
;
-- Test that an error recovery subtransaction is parallel safe

create function error_trap_test() returns text as $$
begin
  perform zero_divide();
  return 'no error detected!';
exception when division_by_zero then
  return 'division_by_zero detected';
end;
$$ language plpgsql parallel safe
;
create or replace function compos() returns compostype as $$
begin
  return 1 + 1;
end;
$$ language plpgsql
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_am
;
-- kinda silly to change property in the same command, but it should work
ALTER TABLE itest3
  ADD COLUMN c int GENERATED BY DEFAULT AS IDENTITY,
  ALTER COLUMN c SET GENERATED ALWAYS
;
CREATE TEXT SEARCH CONFIGURATION swedish
	(PARSER = default)
;
--
-- Test case for subselect within UPDATE of INSERT...ON CONFLICT DO UPDATE
--
create temp table upsert(key int4 primary key, val text)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] ALTER TABLE tab1 ENABLE REPLICA TRIGGER test1
;
----------
[CREATE_TABLE] create table parted_notnull_inh_test1 partition of parted_notnull_inh_test (a not null, b default 1) for values in (1)
;
----------
[CREATE_TABLE] CREATE TABLE part_7_a_null (
	c int,
	d int,
	e int,
	LIKE list_parted2,  
	CONSTRAINT check_b CHECK (b IS NULL OR b = 'a'),
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
	)
;
----------
[CREATE_PROG_OBJ] -- Check that an unreserved keyword can be used as a variable name

create function unreserved_test() returns int as $$
declare
  forward int := 21;
begin
  forward := forward * 2;
  return forward;
end
$$ language plpgsql
;
----------
[CREATE_VIEW(SELECT)] /*
 * 5.2
 * INFORMATION_SCHEMA_CATALOG_NAME view
 */

CREATE VIEW information_schema_catalog_name AS
    SELECT CAST(current_database() AS sql_identifier) AS catalog_name
;
----------
[CREATE_TABLE|SELECT] --
-- \crosstabview
--

CREATE TABLE ctv_data (v, h, c, i, d) AS
VALUES
   ('v1','h2','foo', 3, '2015-04-01'::date),
   ('v2','h1','bar', 3, '2015-01-02'),
   ('v1','h0','baz', NULL, '2015-07-12'),
   ('v0','h4','qux', 4, '2015-07-15'),
   ('v0','h4','dbl', -3, '2014-12-15'),
   ('v0',NULL,'qux', 5, '2014-07-15'),
   ('v1','h2','quux',7, '2015-04-04')
;
----------
[CREATE_TABLE] CREATE TABLE inh_parent ()
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp15a(BASETYPE = anyelement, SFUNC = tfnp,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ADMIN_TABLE] CLUSTER vaccluster
;
----------
[CREATE_TABLE] CREATE TABLE testschema.test_tab(id int) TABLESPACE regress_tblspace
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION regress_sub2
;
----------
[ADD_INDEX] -- Functional index
CREATE UNIQUE INDEX sj_fn_idx ON sj((a * a))
;
----------
[ALTER_TYPE] alter type insenum add value 'i4' before 'L2'
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_replication_origin
;
----------
[CREATE_POLICY] CREATE RULE y_rule AS ON INSERT TO y WHERE a=0 DO INSTEAD DELETE FROM y
;
----------
[CREATE_TABLE] -- Check setup of indisvalid across a complex partition tree on index
-- creation.  If one index in a partition index is invalid, so should its
-- partitioned index.
create table parted_isvalid_tab (a int, b int) partition by range (a)
;
----------
[CREATE_TABLE] --
-- Test WITH attached to a data-modifying statement
--

CREATE TEMPORARY TABLE y (a INTEGER)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE user_enum
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table schema1.abc drop address
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26560
;
----------
[DROP_PROG_OBJ] DROP FUNCTION pg_get_wal_stats_till_end_of_wal(pg_lsn, boolean)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_statistic
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, boolean) SET (JOIN = contjoinsel)
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE pxtest2
;
----------
[CREATE_TABLE] create table event_trigger_fire4 (a int)
;
----------
[DROP_TYPE] drop type textrange1
;
----------
[DROP_TYPE] drop type cube
;
----------
[CREATE_TABLE] CREATE TABLE regular_table (a int)
;
----------
[CREATE_TABLE|SELECT] --jsonb_agg

CREATE TEMP TABLE rows AS
SELECT x, 'txt' || x as y
FROM generate_series(1,3) AS x
;
----------
[CREATE_TABLE] CREATE TABLE tidrangescan(id integer, data text)
;
----------
[CREATE_TYPE] create domain insert_pos_ints as int[] check (value[1] > 0)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON COLUMN dummy_seclabel_tbl1 IS 'unclassified'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_pline_bu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotname != old.slotname then
        delete from PLine where slotname = old.slotname;
	insert into PLine (
		    slotname,
		    phonenumber,
		    comment,
		    backlink
		) values (
		    new.slotname,
		    new.phonenumber,
		    new.comment,
		    new.backlink
		);
        return null;
    end if;
    return new;
end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE CAST (varchar AS citext) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26686_2
;
----------
[ALTER_TABLE] alter table parted_stmt_trig enable trigger trig_ins_after_parent
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float8_compress(internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE itest7b (a int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = numeric_avg_combine,
	finalfunc_modify = shareable  
)
;
----------
[CREATE_TABLE] CREATE TABLE testhstore (h hstore)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."person" (
    "name" "text",
    "age" integer,
    "location" "point"
)
;
----------
[CREATE_TABLE] CREATE TABLE part_c_1_10 PARTITION OF part_c FOR VALUES FROM (1) TO (10)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trig_fn() RETURNS trigger LANGUAGE plpgsql AS
  $$ BEGIN RETURN NULL; END; $$
;
----------
[CREATE_PROG_OBJ] -- Test that an error recovery subtransaction is parallel safe

create function error_trap_test() returns text as $$
begin
  perform zero_divide();
  return 'no error detected!';
exception when division_by_zero then
  return 'division_by_zero detected';
end;
$$ language plpgsql parallel safe
;
----------
[CREATE_PROG_OBJ] create or replace function compos() returns compostype as $$
begin
  return 1 + 1;
end;
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_am
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] -- kinda silly to change property in the same command, but it should work
ALTER TABLE itest3
  ADD COLUMN c int GENERATED BY DEFAULT AS IDENTITY,
  ALTER COLUMN c SET GENERATED ALWAYS
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION swedish
	(PARSER = default)
;
----------
[CREATE_TABLE] --
-- Test case for subselect within UPDATE of INSERT...ON CONFLICT DO UPDATE
--
create temp table upsert(key int4 primary key, val text)
;
