ALTER TABLE public.rule_merge2 OWNER TO zyc
;
EXECUTE dumpFunc('36291')
;
LOCK TABLE public.random_tbl IN ACCESS SHARE MODE
;
CREATE STATISTICS test_stats ON data, id FROM test_table
;
COPY public.t2 (f) TO STDOUT
;
EXECUTE dumpAgg('39352')
;
-- pg_read_all_settings
GRANT pg_read_all_settings TO regress_role_haspriv
;
ALTER TABLE generated_stored_tests.gtest1_y OWNER TO zyc
;
COPY pg_catalog.pg_auth_members (roleid, member, grantor, admin_option) TO stdout
;
-- But we can add valid ones fine
ALTER USER MAPPING FOR public SERVER testserver1
	OPTIONS (ADD sslpassword 'dummy')
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE public.rule_merge2 OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpFunc('36291')
;
----------
[SESSION_LOCK] LOCK TABLE public.random_tbl IN ACCESS SHARE MODE
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS test_stats ON data, id FROM test_table
;
----------
[DATA_EXPORT] COPY public.t2 (f) TO STDOUT
;
----------
[UNSAFE] EXECUTE dumpAgg('39352')
;
----------
[GRANT] -- pg_read_all_settings
GRANT pg_read_all_settings TO regress_role_haspriv
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE generated_stored_tests.gtest1_y OWNER TO zyc
;
----------
[DATA_EXPORT] COPY pg_catalog.pg_auth_members (roleid, member, grantor, admin_option) TO stdout
;
----------
[GRANT] -- But we can add valid ones fine
ALTER USER MAPPING FOR public SERVER testserver1
	OPTIONS (ADD sslpassword 'dummy')
;
