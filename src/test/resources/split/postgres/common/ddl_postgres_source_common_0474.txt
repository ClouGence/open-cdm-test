ALTER DATABASE conflict_db SET TABLESPACE pg_default
;
CREATE COLLATION testcoll_numeric (provider = icu, locale = '@colNumeric=yes')
;
/*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
-- error

CREATE TABLE persons2 OF person_type (
    id WITH OPTIONS PRIMARY KEY,
    UNIQUE (name)
)
;
alter table nv_child_2011 add check (d between '2011-01-01'::date and '2011-12-31'::date) not valid
;
CREATE INDEX bloomidx ON tst USING bloom (i, t) WITH (length=7, col1=4)
;
-- test renumbering of child-table columns in inherited operations

create table p1 (f1 int)
;
CREATE FOREIGN TABLE t3 (
  c1 timestamp with time zone OPTIONS (column_name 'c1'),
  c2 public.typ1 OPTIONS (column_name 'c2')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't3')
;
-- indexes

CREATE INDEX collate_test1_idx1 ON collate_test1 (b)
;
ALTER TABLE test_type_diff ADD COLUMN f2 int
;
CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION pub_dropped_cols
;
CREATE DOMAIN "public"."orderedpair" AS integer[]
	CONSTRAINT "orderedpair_check" CHECK (((VALUE)[1] < (VALUE)[2]))
;
-- should fail
create function mr_outparam_fail(i anyelement, out r anymultirange, out t text)
  as $$ select '[1,10]', 'foo' $$ language sql
;
DO $$
BEGIN
  DELETE FROM stats_track_tab;
END; $$
;
CREATE TYPE "public"."record_type" AS (
	"x" "text",
	"y" integer,
	"z" boolean
)
;
CREATE UNIQUE INDEX testpub_gencol_idx ON testpub_gencol (b)
;
create index on idxpart1 (col_keep)
;
ANALYZE pg_catalog.pg_rewrite
;
CREATE OR REPLACE FUNCTION multi_test_trig() RETURNS trigger
LANGUAGE plpgsql AS $$
BEGIN
    RAISE NOTICE 'count = %', (SELECT COUNT(*) FROM new_test);
    RAISE NOTICE 'count union = %',
      (SELECT COUNT(*)
       FROM (SELECT * FROM new_test UNION ALL SELECT * FROM new_test) ss);
    RETURN NULL;
END$$
;
/* Throw error */

CREATE TABLE inhf (LIKE inhx INCLUDING DEFAULTS INCLUDING CONSTRAINTS)
;
CREATE TABLE import_source.t1 (c1 int, c2 varchar NOT NULL)
;
ANALYZE maintain_test
;
-- verify transition table conversion slot's lifetime
-- https://postgr.es/m/39a71864-b120-5a5c-8cc5-c632b6f16761@amazon.com
create table convslot_test_parent (col1 text primary key)
;
drop function return_dquery()
;
COMMENT ON FUNCTION cube_eq(cube, cube) IS 'same as'
;
/*
 * 24.8
 * FOREIGN_TABLE_OPTIONS view
 */
CREATE VIEW foreign_table_options AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           CAST((pg_options_to_table(t.ftoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(t.ftoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_tables t
;
ALTER SEQUENCE sequence_test5 AS smallint
;
CREATE DOMAIN testdatexmldomain AS date
;
CREATE FUNCTION hstore(record)
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_record'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
ALTER TABLE part_c_100_200 ADD COLUMN c numeric, ADD COLUMN e varchar, ADD COLUMN a text
;
CREATE PUBLICATION tap_pub FOR TABLE test_tab
;
create table mcrparted5_common_0_to_10 partition of mcrparted for values from ('common', 0) to ('common', 10)
;
CREATE TABLE dump_test_second_schema.measurement_y2006m4
						PARTITION OF dump_test.measurement
						FOR VALUES FROM ('2006-04-01') TO ('2006-05-01')
;
alter table atacc1
  add column b float8 not null default random(),
  add primary key(a)
;
CREATE FUNCTION crypt(text, text)
RETURNS text
AS '$libdir/pgcrypto', 'pg_crypt'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- fail, as partitioned tables don't allow NO INHERIT constraints
CREATE TABLE noinh_con_copy1_parted (LIKE noinh_con_copy INCLUDING ALL)
  PARTITION BY LIST (a)
;
ALTER TABLE pg_namespace ADD UNIQUE USING INDEX pg_namespace_nspname_index
;
ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk4_pkey"
;
CREATE TABLE shoe_data (
	shoename   char(10),      -- primary key
	sh_avail   integer,       -- available # of pairs
	slcolor    char(10),      -- preferred shoelace color
	slminlen   float,         -- minimum shoelace length
	slmaxlen   float,         -- maximum shoelace length
	slunit     char(8)        -- length unit
)
;
CREATE TABLE ref_c (
    c INT PRIMARY KEY,
    a INT REFERENCES trunc_a(a) ON DELETE CASCADE
) PARTITION BY RANGE (c)
;
create function mki4(int) returns int4_tbl as
$$select row($1)::int4_tbl$$ language sql
;
ALTER DOMAIN split_domain_type_next.email_domain DROP NOT NULL
;
ALTER TABLE alice.unpartitioned NO FORCE ROW LEVEL SECURITY
;
-- fails
DROP TABLE badcompresstbl
;
create unlogged table t_gin_test_tbl(i int4[], j int4[])
;
DROP TABLE parent, child
;
ALTER TABLE gtest25 ADD COLUMN d int DEFAULT 101
;
CREATE FUNCTION "public"."cache_test"(integer) RETURNS integer
    LANGUAGE "plpgsql"
    AS $_$
declare total int;
begin
	create temp table t1(f1 int);
	insert into t1 values($1);
	insert into t1 values(11);
	insert into t1 values(12);
	insert into t1 values(13);
	select sum(f1) into total from t1;
	drop table t1;
	return total;
end
$_$
;
DROP TABLE gtestp CASCADE
;
create table mc3p6 partition of mc3p for values from (20, 10, 10) to (20, 20, 20)
;
------------------------------------------------------------------------------------------
[ALTER_CATALOG] ALTER DATABASE conflict_db SET TABLESPACE pg_default
;
----------
[CREATE_POLICY] CREATE COLLATION testcoll_numeric (provider = icu, locale = '@colNumeric=yes')
;
----------
[CREATE_POLICY] /*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
----------
[CREATE_TABLE] -- error

CREATE TABLE persons2 OF person_type (
    id WITH OPTIONS PRIMARY KEY,
    UNIQUE (name)
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table nv_child_2011 add check (d between '2011-01-01'::date and '2011-12-31'::date) not valid
;
----------
[ADD_INDEX] CREATE INDEX bloomidx ON tst USING bloom (i, t) WITH (length=7, col1=4)
;
----------
[CREATE_TABLE] -- test renumbering of child-table columns in inherited operations

create table p1 (f1 int)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t3 (
  c1 timestamp with time zone OPTIONS (column_name 'c1'),
  c2 public.typ1 OPTIONS (column_name 'c2')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't3')
;
----------
[ADD_INDEX] -- indexes

CREATE INDEX collate_test1_idx1 ON collate_test1 (b)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_type_diff ADD COLUMN f2 int
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION pub_dropped_cols
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."orderedpair" AS integer[]
	CONSTRAINT "orderedpair_check" CHECK (((VALUE)[1] < (VALUE)[2]))
;
----------
[CREATE_PROG_OBJ] -- should fail
create function mr_outparam_fail(i anyelement, out r anymultirange, out t text)
  as $$ select '[1,10]', 'foo' $$ language sql
;
----------
[BLOCK] DO $$
BEGIN
  DELETE FROM stats_track_tab;
END; $$
;
----------
[CREATE_TYPE] CREATE TYPE "public"."record_type" AS (
	"x" "text",
	"y" integer,
	"z" boolean
)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX testpub_gencol_idx ON testpub_gencol (b)
;
----------
[ADD_INDEX] create index on idxpart1 (col_keep)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_rewrite
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION multi_test_trig() RETURNS trigger
LANGUAGE plpgsql AS $$
BEGIN
    RAISE NOTICE 'count = %', (SELECT COUNT(*) FROM new_test);
    RAISE NOTICE 'count union = %',
      (SELECT COUNT(*)
       FROM (SELECT * FROM new_test UNION ALL SELECT * FROM new_test) ss);
    RETURN NULL;
END$$
;
----------
[CREATE_TABLE] /* Throw error */

CREATE TABLE inhf (LIKE inhx INCLUDING DEFAULTS INCLUDING CONSTRAINTS)
;
----------
[CREATE_TABLE] CREATE TABLE import_source.t1 (c1 int, c2 varchar NOT NULL)
;
----------
[ADMIN_TABLE] ANALYZE maintain_test
;
----------
[CREATE_TABLE] -- verify transition table conversion slot's lifetime
-- https://postgr.es/m/39a71864-b120-5a5c-8cc5-c632b6f16761@amazon.com
create table convslot_test_parent (col1 text primary key)
;
----------
[DROP_PROG_OBJ] drop function return_dquery()
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_eq(cube, cube) IS 'same as'
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.8
 * FOREIGN_TABLE_OPTIONS view
 */
CREATE VIEW foreign_table_options AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           CAST((pg_options_to_table(t.ftoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(t.ftoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_tables t
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test5 AS smallint
;
----------
[CREATE_TYPE] CREATE DOMAIN testdatexmldomain AS date
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore(record)
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_record'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE part_c_100_200 ADD COLUMN c numeric, ADD COLUMN e varchar, ADD COLUMN a text
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub FOR TABLE test_tab
;
----------
[CREATE_TABLE] create table mcrparted5_common_0_to_10 partition of mcrparted for values from ('common', 0) to ('common', 10)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test_second_schema.measurement_y2006m4
						PARTITION OF dump_test.measurement
						FOR VALUES FROM ('2006-04-01') TO ('2006-05-01')
;
----------
[ALTER_TABLE|ADD_COLUMN|ADD_CONSTRAINT] alter table atacc1
  add column b float8 not null default random(),
  add primary key(a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION crypt(text, text)
RETURNS text
AS '$libdir/pgcrypto', 'pg_crypt'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] -- fail, as partitioned tables don't allow NO INHERIT constraints
CREATE TABLE noinh_con_copy1_parted (LIKE noinh_con_copy INCLUDING ALL)
  PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_namespace ADD UNIQUE USING INDEX pg_namespace_nspname_index
;
----------
[ALTER_INDEX] ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk4_pkey"
;
----------
[CREATE_TABLE] CREATE TABLE shoe_data (
	shoename   char(10),      -- primary key
	sh_avail   integer,       -- available # of pairs
	slcolor    char(10),      -- preferred shoelace color
	slminlen   float,         -- minimum shoelace length
	slmaxlen   float,         -- maximum shoelace length
	slunit     char(8)        -- length unit
)
;
----------
[CREATE_TABLE] CREATE TABLE ref_c (
    c INT PRIMARY KEY,
    a INT REFERENCES trunc_a(a) ON DELETE CASCADE
) PARTITION BY RANGE (c)
;
----------
[CREATE_PROG_OBJ] create function mki4(int) returns int4_tbl as
$$select row($1)::int4_tbl$$ language sql
;
----------
[ALTER_TYPE] ALTER DOMAIN split_domain_type_next.email_domain DROP NOT NULL
;
----------
[ALTER_TABLE] ALTER TABLE alice.unpartitioned NO FORCE ROW LEVEL SECURITY
;
----------
[DROP_TABLE] -- fails
DROP TABLE badcompresstbl
;
----------
[CREATE_TABLE] create unlogged table t_gin_test_tbl(i int4[], j int4[])
;
----------
[DROP_TABLE] DROP TABLE parent, child
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest25 ADD COLUMN d int DEFAULT 101
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."cache_test"(integer) RETURNS integer
    LANGUAGE "plpgsql"
    AS $_$
declare total int;
begin
	create temp table t1(f1 int);
	insert into t1 values($1);
	insert into t1 values(11);
	insert into t1 values(12);
	insert into t1 values(13);
	select sum(f1) into total from t1;
	drop table t1;
	return total;
end
$_$
;
----------
[DROP_TABLE] DROP TABLE gtestp CASCADE
;
----------
[CREATE_TABLE] create table mc3p6 partition of mc3p for values from (20, 10, 10) to (20, 20, 20)
;
