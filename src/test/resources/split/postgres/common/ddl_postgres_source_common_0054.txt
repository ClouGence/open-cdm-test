ANALYZE pagg_tab6
;
CREATE TEMPORARY TABLE empsalary (
    depname varchar,
    empno bigint,
    salary int,
    enroll_date date
)
;
-- fail

drop function dfunc()
;
create table asptab1 partition of asptab for values from (1) to (2)
;
DROP TABLE test_foreign_constraints
;
-- ===================================================================
-- test inheritance features
-- ===================================================================

CREATE TABLE a (aa TEXT)
;
DROP TABLE test_replica_id_full
;
-- combination of multiple ndistinct statistics, with/without expressions
TRUNCATE ndistinct
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
DROP TABLE sf_target
;
-- Test expressions in the defaults
CREATE OR REPLACE FUNCTION foo(a INT) RETURNS TEXT AS $$
DECLARE res TEXT := '';
        i INT;
BEGIN
  i := 0;
  WHILE (i < a) LOOP
    res := res || chr(ascii('a') + i);
    i := i + 1;
  END LOOP;
  RETURN res;
END; $$ LANGUAGE PLPGSQL STABLE
;
CREATE TABLE part_document (
    did         int,
    cid         int,
    dlevel      int not null,
    dauthor     name,
    dtitle      text
) PARTITION BY RANGE (cid)
;
-- but explicitly not adding oids is still supported
CREATE TEMP TABLE withoutoid() WITHOUT OIDS
;
-- Partitioned tables with a partitioned index
CREATE TABLE evttrig.parted (
    id int PRIMARY KEY)
    PARTITION BY RANGE (id)
;
truncate copyfreeze
;
create index i_foo_f1 on foo(f1)
;
alter table loct drop constraint loct_a_check
;
ANALYZE only_parted
;
ALTER TABLE tab2 ATTACH PARTITION tab2_1 FOR VALUES FROM (0) TO (10)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_inherits
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=26249 host=/tmp/OCc4XFvOJI dbname=postgres' PUBLICATION tap_pub
;
CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at daterange
)
;
--
-- cleanup
--
DROP TABLE test_squash
;
DROP TABLE part_rpd
;
CREATE TABLE ref2 PARTITION OF ref1_2 FOR VALUES IN (2) PARTITION BY list (f2)
;
CREATE FUNCTION crosstab_out(text, text,
  OUT rowid text, OUT rowdt timestamp,
  OUT temperature int4, OUT test_result text,
  OUT test_startdate timestamp, OUT volts float8)
RETURNS setof record
AS '$libdir/tablefunc','crosstab_hash'
LANGUAGE C STABLE STRICT
;
CREATE TABLE schema_rf_x.tab_rf_partitioned (x int primary key) PARTITION BY RANGE(x)
;
drop table part_default
;
ALTER TABLE pg_toast.pg_toast_28095 RENAME TO reind_con_toast
;
ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (ismn, ean13),
	OPERATOR 1  = (ismn, ismn13)
;
DROP TABLE IF EXISTS tbl2
;
CREATE RULE bug6051_ins AS ON INSERT TO bug6051 DO INSTEAD
 INSERT INTO bug6051_2
 VALUES(NEW.i)
;
DROP TABLE test_prepared2
;
ALTER INDEX pg_toast.pg_toast_30535_index RENAME TO reind_con_toast_idx
;
ANALYZE fkpart4.dropfk
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_shdepend
;
DROP INDEX bloomidx
;
-- Test intervals that are large enough to overflow 64 bits in comparisons
CREATE TEMP TABLE INTERVAL_TBL_OF (f1 interval)
;
alter table at_test_sql_partop attach partition at_test_sql_partop_1 for values from (0) to (10)
;
CREATE UNIQUE INDEX ON mvtest_mv (b)
;
DROP OPERATOR ~ (hstore, hstore)
;
CREATE FUNCTION rw_view1_trig_fn()
RETURNS trigger AS
$$
BEGIN
  IF TG_OP = 'INSERT' THEN
    INSERT INTO base_tbl VALUES (NEW.a, 10);
    RETURN NEW;
  ELSIF TG_OP = 'UPDATE' THEN
    UPDATE base_tbl SET a=NEW.a WHERE a=OLD.a;
    RETURN NEW;
  ELSIF TG_OP = 'DELETE' THEN
    DELETE FROM base_tbl WHERE a=OLD.a;
    RETURN OLD;
  END IF;
END;
$$
LANGUAGE plpgsql
;
DROP TABLE gtest_rule
;
alter table test_storage alter column a set storage external
;
CREATE TABLE testpub_rf_tbl2 (c text, d integer)
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "version" WITH "simple"
;
vacuum 計算機用語
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
-- now add a primary key column with a default (succeeds).
alter table atacc1 add column test2 int default 0 primary key
;
create or replace function find_hash(node json)
returns json language plpgsql
as
$$
declare
  x json;
  child json;
begin
  if node->>'Node Type' = 'Hash' then
    return node;
  else
    for child in select json_array_elements(node->'Plans')
    loop
      x := find_hash(child);
      if x is not null then
        return x;
      end if;
    end loop;
    return null;
  end if;
end;
$$
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pagg_tab6
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE empsalary (
    depname varchar,
    empno bigint,
    salary int,
    enroll_date date
)
;
----------
[DROP_PROG_OBJ] -- fail

drop function dfunc()
;
----------
[CREATE_TABLE] create table asptab1 partition of asptab for values from (1) to (2)
;
----------
[DROP_TABLE] DROP TABLE test_foreign_constraints
;
----------
[CREATE_TABLE] -- ===================================================================
-- test inheritance features
-- ===================================================================

CREATE TABLE a (aa TEXT)
;
----------
[DROP_TABLE] DROP TABLE test_replica_id_full
;
----------
[TRUNCATE_TABLE] -- combination of multiple ndistinct statistics, with/without expressions
TRUNCATE ndistinct
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[DROP_TABLE] DROP TABLE sf_target
;
----------
[CREATE_PROG_OBJ] -- Test expressions in the defaults
CREATE OR REPLACE FUNCTION foo(a INT) RETURNS TEXT AS $$
DECLARE res TEXT := '';
        i INT;
BEGIN
  i := 0;
  WHILE (i < a) LOOP
    res := res || chr(ascii('a') + i);
    i := i + 1;
  END LOOP;
  RETURN res;
END; $$ LANGUAGE PLPGSQL STABLE
;
----------
[CREATE_TABLE] CREATE TABLE part_document (
    did         int,
    cid         int,
    dlevel      int not null,
    dauthor     name,
    dtitle      text
) PARTITION BY RANGE (cid)
;
----------
[CREATE_TABLE] -- but explicitly not adding oids is still supported
CREATE TEMP TABLE withoutoid() WITHOUT OIDS
;
----------
[CREATE_TABLE] -- Partitioned tables with a partitioned index
CREATE TABLE evttrig.parted (
    id int PRIMARY KEY)
    PARTITION BY RANGE (id)
;
----------
[TRUNCATE_TABLE] truncate copyfreeze
;
----------
[ADD_INDEX] create index i_foo_f1 on foo(f1)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table loct drop constraint loct_a_check
;
----------
[ADMIN_TABLE] ANALYZE only_parted
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab2 ATTACH PARTITION tab2_1 FOR VALUES FROM (0) TO (10)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_inherits
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=26249 host=/tmp/OCc4XFvOJI dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at daterange
)
;
----------
[DROP_TABLE] --
-- cleanup
--
DROP TABLE test_squash
;
----------
[DROP_TABLE] DROP TABLE part_rpd
;
----------
[CREATE_TABLE] CREATE TABLE ref2 PARTITION OF ref1_2 FOR VALUES IN (2) PARTITION BY list (f2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION crosstab_out(text, text,
  OUT rowid text, OUT rowdt timestamp,
  OUT temperature int4, OUT test_result text,
  OUT test_startdate timestamp, OUT volts float8)
RETURNS setof record
AS '$libdir/tablefunc','crosstab_hash'
LANGUAGE C STABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE schema_rf_x.tab_rf_partitioned (x int primary key) PARTITION BY RANGE(x)
;
----------
[DROP_TABLE] drop table part_default
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28095 RENAME TO reind_con_toast
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (ismn, ean13),
	OPERATOR 1  = (ismn, ismn13)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS tbl2
;
----------
[CREATE_POLICY] CREATE RULE bug6051_ins AS ON INSERT TO bug6051 DO INSTEAD
 INSERT INTO bug6051_2
 VALUES(NEW.i)
;
----------
[DROP_TABLE] DROP TABLE test_prepared2
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30535_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE fkpart4.dropfk
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_shdepend
;
----------
[DROP_INDEX] DROP INDEX bloomidx
;
----------
[CREATE_TABLE] -- Test intervals that are large enough to overflow 64 bits in comparisons
CREATE TEMP TABLE INTERVAL_TBL_OF (f1 interval)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table at_test_sql_partop attach partition at_test_sql_partop_1 for values from (0) to (10)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON mvtest_mv (b)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ~ (hstore, hstore)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rw_view1_trig_fn()
RETURNS trigger AS
$$
BEGIN
  IF TG_OP = 'INSERT' THEN
    INSERT INTO base_tbl VALUES (NEW.a, 10);
    RETURN NEW;
  ELSIF TG_OP = 'UPDATE' THEN
    UPDATE base_tbl SET a=NEW.a WHERE a=OLD.a;
    RETURN NEW;
  ELSIF TG_OP = 'DELETE' THEN
    DELETE FROM base_tbl WHERE a=OLD.a;
    RETURN OLD;
  END IF;
END;
$$
LANGUAGE plpgsql
;
----------
[DROP_TABLE] DROP TABLE gtest_rule
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table test_storage alter column a set storage external
;
----------
[CREATE_TABLE] CREATE TABLE testpub_rf_tbl2 (c text, d integer)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "version" WITH "simple"
;
----------
[ADMIN_TABLE] vacuum 計算機用語
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- now add a primary key column with a default (succeeds).
alter table atacc1 add column test2 int default 0 primary key
;
----------
[CREATE_PROG_OBJ] create or replace function find_hash(node json)
returns json language plpgsql
as
$$
declare
  x json;
  child json;
begin
  if node->>'Node Type' = 'Hash' then
    return node;
  else
    for child in select json_array_elements(node->'Plans')
    loop
      x := find_hash(child);
      if x is not null then
        return x;
      end if;
    end loop;
    return null;
  end if;
end;
$$
;
