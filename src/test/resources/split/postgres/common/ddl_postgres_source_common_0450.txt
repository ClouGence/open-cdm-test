alter domain connotnull drop constraint constr1
;
CREATE TRIGGER t_raster BEFORE UPDATE OR DELETE ON image
    FOR EACH ROW EXECUTE PROCEDURE lo_manage(raster)
;
CREATE POLICY pa_target_pol ON pa_target USING (tid != 0)
;
CREATE TEMP TABLE pg_temp_4.pg_temp_37896_2 (tid pg_catalog.tid)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_init_privs
;
CREATE FUNCTION float8mi_n(float8, float8) RETURNS float8 AS
$$ SELECT $1 - $2; $$
LANGUAGE SQL
;
create table partitioned1
  partition of partitioned for values in ('(1,2)'::partitioned)
;
DROP TABLE vacuum_tab
;
ALTER TABLE ONLY "public"."cc2" INHERIT "public"."cc1"
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '\')
;
alter table inh_child no inherit inh_parent
;
alter table child1 inherit parent
;
create table idxpart1 partition of idxpart (i) for values with (modulus 2, remainder 1)
;
DROP TABLE l
;
DROP FOREIGN TABLE ft_part_1_2
;
CREATE TEMP TABLE e (id1 int, id2 int, PRIMARY KEY(id1, id2))
;
create table pph_arrpart (a int[]) partition by hash (a)
;
CREATE AGGREGATE addr_nsp.genaggr(int4) (sfunc = int4pl, stype = int4)
;
CREATE TABLE "public"."test1" (
    "generate_series" integer
)
;
create rule "_RETURN" as on select to rules_fooview_child do instead
  select 1 as x, 'aaa'::text as y
;
create table pt34 partition of pt for values in (3,4)
;
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest3,
ptest4) REFERENCES pktable(ptest2, ptest1))
;
-- ok - with slot_name = NONE
CREATE SUBSCRIPTION regress_testsub3 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false)
;
DROP TABLE only_inh_parent CASCADE
;
CREATE FUNCTION on_login_proc() RETURNS event_trigger AS $$
BEGIN
  INSERT INTO user_logins (who) VALUES (SESSION_USER);
  RAISE NOTICE 'You are welcome!';
END;
$$ LANGUAGE plpgsql
;
CREATE TABLE delete_test (
    id SERIAL PRIMARY KEY,
    a INT,
    b text
)
;
ALTER TYPE split_domain_type_next.address_record SET SCHEMA public
;
DROP VIEW instead_of_insert_tbl_view
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_type
;
CREATE FUNCTION "public"."transition_table_base_ins_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
  t text;
  l text;
BEGIN
  t = '';
  FOR l IN EXECUTE
           $q$
             EXPLAIN (TIMING off, COSTS off, VERBOSE on)
             SELECT * FROM newtable
           $q$ LOOP
    t = t || l || E'\n';
  END LOOP;

  RAISE INFO '%', t;
  RETURN new;
END;
$_$
;
COMMENT ON LARGE OBJECT 31313 IS 'I Wandered Lonely as a Cloud'
;
DROP TABLE IF EXISTS test
;
ANALYZE pg_temp_32.pg_temp_28302_2
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16262 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION tap_pub
;
CREATE UNLOGGED TABLE t2 (x int)
;
CREATE EXTENSION pg_prewarm
;
ALTER TABLE pg_toast.pg_toast_30715 RENAME TO reind_con_toast
;
CREATE FUNCTION serializable_update_trig() RETURNS trigger LANGUAGE plpgsql AS
$$
declare
	rec record;
begin
	new.description = 'updated in trigger';
	return new;
end;
$$
;
ALTER FOREIGN TABLE ft1 ADD COLUMN IF NOT EXISTS c6 integer
;
-- this will now succeed:
alter table tt14t drop column f3
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_db_role_setting
;
ANALYZE pg_temp_11.pg_temp_41234
;
CREATE TABLE "CURRENT_SCHEMA"."CURRENT_SCHEMA"(id int)
;
/*
 * This test is intended to pass on all platforms supported by Postgres.
 * We can therefore only assume that the default, C, and POSIX collations
 * are available --- and since the regression tests are often run in a
 * C-locale database, these may well all have the same behavior.  But
 * fortunately, the system doesn't know that and will treat them as
 * incompatible collations.  It is therefore at least possible to test
 * parser behaviors such as collation conflict resolution.  This test will,
 * however, be more revealing when run in a database with non-C locale,
 * since any departure from C sorting behavior will show as a failure.
 */

CREATE SCHEMA collate_tests
;
ANALYZE public.float8_tbl
;
drop rule nonesuch on noplace
;
CREATE POLICY p3 ON x1 FOR UPDATE USING (a % 2 = 0)
;
create table uv_pt11 (like uv_pt1)
;
VACUUM (SKIP_DATABASE_STATS) public.vactable
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] alter domain connotnull drop constraint constr1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER t_raster BEFORE UPDATE OR DELETE ON image
    FOR EACH ROW EXECUTE PROCEDURE lo_manage(raster)
;
----------
[CREATE_POLICY] CREATE POLICY pa_target_pol ON pa_target USING (tid != 0)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_37896_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_init_privs
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION float8mi_n(float8, float8) RETURNS float8 AS
$$ SELECT $1 - $2; $$
LANGUAGE SQL
;
----------
[CREATE_TABLE] create table partitioned1
  partition of partitioned for values in ('(1,2)'::partitioned)
;
----------
[DROP_TABLE] DROP TABLE vacuum_tab
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."cc2" INHERIT "public"."cc1"
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '\')
;
----------
[ALTER_TABLE] alter table inh_child no inherit inh_parent
;
----------
[ALTER_TABLE] alter table child1 inherit parent
;
----------
[CREATE_TABLE] create table idxpart1 partition of idxpart (i) for values with (modulus 2, remainder 1)
;
----------
[DROP_TABLE] DROP TABLE l
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ft_part_1_2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE e (id1 int, id2 int, PRIMARY KEY(id1, id2))
;
----------
[CREATE_TABLE] create table pph_arrpart (a int[]) partition by hash (a)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE addr_nsp.genaggr(int4) (sfunc = int4pl, stype = int4)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test1" (
    "generate_series" integer
)
;
----------
[CREATE_POLICY] create rule "_RETURN" as on select to rules_fooview_child do instead
  select 1 as x, 'aaa'::text as y
;
----------
[CREATE_TABLE] create table pt34 partition of pt for values in (3,4)
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest3,
ptest4) REFERENCES pktable(ptest2, ptest1))
;
----------
[CREATE_PUB_SUB] -- ok - with slot_name = NONE
CREATE SUBSCRIPTION regress_testsub3 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false)
;
----------
[DROP_TABLE] DROP TABLE only_inh_parent CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION on_login_proc() RETURNS event_trigger AS $$
BEGIN
  INSERT INTO user_logins (who) VALUES (SESSION_USER);
  RAISE NOTICE 'You are welcome!';
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE delete_test (
    id SERIAL PRIMARY KEY,
    a INT,
    b text
)
;
----------
[ALTER_TYPE] ALTER TYPE split_domain_type_next.address_record SET SCHEMA public
;
----------
[DROP_VIEW] DROP VIEW instead_of_insert_tbl_view
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_type
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."transition_table_base_ins_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
  t text;
  l text;
BEGIN
  t = '';
  FOR l IN EXECUTE
           $q$
             EXPLAIN (TIMING off, COSTS off, VERBOSE on)
             SELECT * FROM newtable
           $q$ LOOP
    t = t || l || E'\n';
  END LOOP;

  RAISE INFO '%', t;
  RETURN new;
END;
$_$
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 31313 IS 'I Wandered Lonely as a Cloud'
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS test
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28302_2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16262 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE t2 (x int)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pg_prewarm
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30715 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION serializable_update_trig() RETURNS trigger LANGUAGE plpgsql AS
$$
declare
	rec record;
begin
	new.description = 'updated in trigger';
	return new;
end;
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD COLUMN IF NOT EXISTS c6 integer
;
----------
[ALTER_TABLE|DROP_COLUMN] -- this will now succeed:
alter table tt14t drop column f3
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_db_role_setting
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41234
;
----------
[CREATE_TABLE] CREATE TABLE "CURRENT_SCHEMA"."CURRENT_SCHEMA"(id int)
;
----------
[CREATE_SCHEMA] /*
 * This test is intended to pass on all platforms supported by Postgres.
 * We can therefore only assume that the default, C, and POSIX collations
 * are available --- and since the regression tests are often run in a
 * C-locale database, these may well all have the same behavior.  But
 * fortunately, the system doesn't know that and will treat them as
 * incompatible collations.  It is therefore at least possible to test
 * parser behaviors such as collation conflict resolution.  This test will,
 * however, be more revealing when run in a database with non-C locale,
 * since any departure from C sorting behavior will show as a failure.
 */

CREATE SCHEMA collate_tests
;
----------
[ADMIN_TABLE] ANALYZE public.float8_tbl
;
----------
[DROP_POLICY] drop rule nonesuch on noplace
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON x1 FOR UPDATE USING (a % 2 = 0)
;
----------
[CREATE_TABLE] create table uv_pt11 (like uv_pt1)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) public.vactable
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
