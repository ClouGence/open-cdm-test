VACUUM (FULL) information_schema.sql_implementation_info
;
alter conversion alter1.ascii_to_utf8 set schema alter2
;
CREATE TABLE prt3_adv_p1 PARTITION OF prt3_adv FOR VALUES FROM (200) TO (300)
;
ANALYZE pg_temp_24.pg_temp_26683_2
;
COMMENT ON TYPE dump_test.int42
					   IS 'comment on regular type'
;
--
-- pgp_sym_encrypt(data, key, args)
--
CREATE FUNCTION pgp_sym_encrypt(text, text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_encrypt_text'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE FUNCTION isnne(ismn13, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
/* contrib/citext/citext--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION citext_hash_extended(citext, int8)
RETURNS int8
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP FUNCTION set(name)
;
drop database if exists "abc"
;
-- Test that GiST indexes provide same behavior as sequential scan
CREATE TEMP TABLE point_gist_tbl(f1 point)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_inherits
;
create table test ( text char ( 5 ) , num numeric ( 14 , 7 ) )
;
ANALYZE pg_temp_37.pg_temp_29701_2
;
CREATE INDEX ON skip_fetch(a)
;
ALTER TABLE pa_target ATTACH PARTITION part1 FOR VALUES IN (1,4)
;
CREATE OPERATOR ^@> (
        LEFTARG = ltree,
	RIGHTARG = _ltree,
	PROCEDURE = _ltree_r_risparent,
        COMMUTATOR = '^<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
do $$
declare
  v pg_node_tree;
  v1 v%type[];
begin
end;
$$
;
-- fail, not allowed, already has a default
CREATE TABLE itest5 (a serial, b text)
;
-- have a BR trigger modify the row such that the check_b is violated
create function mlparted11_trig_fn()
returns trigger AS
$$
begin
  NEW.b := 4;
  return NEW;
end;
$$
language plpgsql
;
-- wrong base type
CREATE TABLE tt2 (x int, y numeric(9,2))
;
drop function inner_func(int)
;
CREATE OR REPLACE FUNCTION test_slru_page_exists(bigint) RETURNS bool
  AS '$libdir/test_slru', 'test_slru_page_exists' LANGUAGE C
;
CREATE TEMPORARY TABLE q5_prep_nodata AS EXECUTE q5(200, 'DTAAAA')
    WITH NO DATA
;
-- cleanup
drop table foo cascade
;
--
-- Check that use of an index with an extraneous column doesn't produce
-- a plan with extraneous sorting
--

create table matest0 (a int, b int, c int, d int)
;
drop function func_part_attach()
;
CREATE PUBLICATION tap_pub FOR TABLE tab_test
;
-- also check the case that doesn't rewrite the table

CREATE TABLE vtype2 (a int)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_statistic_ext
;
DROP SUBSCRIPTION regress_testsub3
;
CREATE VIEW pg_stat_xact_user_functions AS
    SELECT
            P.oid AS funcid,
            N.nspname AS schemaname,
            P.proname AS funcname,
            pg_stat_get_xact_function_calls(P.oid) AS calls,
            pg_stat_get_xact_function_total_time(P.oid) AS total_time,
            pg_stat_get_xact_function_self_time(P.oid) AS self_time
    FROM pg_proc P LEFT JOIN pg_namespace N ON (N.oid = P.pronamespace)
    WHERE P.prolang != 12  -- fast check to eliminate built-in functions
          AND pg_stat_get_xact_function_calls(P.oid) IS NOT NULL
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_authid
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
create table pp_lp1 partition of pp_lp for values in(1)
;
comment on function unreserved_test() is 'this is a test'
;
ALTER INDEX pg_toast.pg_toast_28062_index RENAME TO reind_con_toast_idx
;
DROP TRIGGER ftable_rowcount_trigger ON ltable
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=11276 host=/tmp/H11VjIA0o9 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
CREATE AGGREGATE myaggp17b(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  INITCOND = '{}')
;
--
-- TIMETZ
--

CREATE TABLE TIMETZ_TBL (f1 time(2) with time zone)
;
CREATE POLICY p1 ON r1 FOR SELECT USING (false)
;
CREATE TABLE tab_foobar (id int)
;
alter domain posint add constraint c2 check(value > 0)
;
CREATE FUNCTION gbt_macad_consistent(internal,macaddr,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE pg_temp_3.pg_temp_16656_2
;
CREATE OPERATOR ^~ (
        LEFTARG = lquery,
	RIGHTARG = _ltree,
	PROCEDURE = _ltq_rregex,
        COMMUTATOR = '^~',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE OPERATOR dump_test.~~ (
							 PROCEDURE = int4eq,
							 LEFTARG = int,
							 RIGHTARG = int)
;
CREATE TABLE plt2_adv_p3 PARTITION OF plt2_adv FOR VALUES IN ('0003')
;
CREATE FUNCTION get_columns_length(oid[])
    RETURNS int
    AS '/src/src/test/regress/regress.so'
    LANGUAGE C STRICT STABLE PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (FULL) information_schema.sql_implementation_info
;
----------
[ALTER_POLICY] alter conversion alter1.ascii_to_utf8 set schema alter2
;
----------
[CREATE_TABLE] CREATE TABLE prt3_adv_p1 PARTITION OF prt3_adv FOR VALUES FROM (200) TO (300)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_26683_2
;
----------
[COMMENT_TYPE] COMMENT ON TYPE dump_test.int42
					   IS 'comment on regular type'
;
----------
[CREATE_PROG_OBJ] --
-- pgp_sym_encrypt(data, key, args)
--
CREATE FUNCTION pgp_sym_encrypt(text, text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_sym_encrypt_text'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ismn13, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] /* contrib/citext/citext--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION citext_hash_extended(citext, int8)
RETURNS int8
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP FUNCTION set(name)
;
----------
[DROP_CATALOG] drop database if exists "abc"
;
----------
[CREATE_TABLE] -- Test that GiST indexes provide same behavior as sequential scan
CREATE TEMP TABLE point_gist_tbl(f1 point)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_inherits
;
----------
[CREATE_TABLE] create table test ( text char ( 5 ) , num numeric ( 14 , 7 ) )
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_37.pg_temp_29701_2
;
----------
[ADD_INDEX] CREATE INDEX ON skip_fetch(a)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pa_target ATTACH PARTITION part1 FOR VALUES IN (1,4)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^@> (
        LEFTARG = ltree,
	RIGHTARG = _ltree,
	PROCEDURE = _ltree_r_risparent,
        COMMUTATOR = '^<@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[BLOCK] do $$
declare
  v pg_node_tree;
  v1 v%type[];
begin
end;
$$
;
----------
[CREATE_TABLE] -- fail, not allowed, already has a default
CREATE TABLE itest5 (a serial, b text)
;
----------
[CREATE_PROG_OBJ] -- have a BR trigger modify the row such that the check_b is violated
create function mlparted11_trig_fn()
returns trigger AS
$$
begin
  NEW.b := 4;
  return NEW;
end;
$$
language plpgsql
;
----------
[CREATE_TABLE] -- wrong base type
CREATE TABLE tt2 (x int, y numeric(9,2))
;
----------
[DROP_PROG_OBJ] drop function inner_func(int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_exists(bigint) RETURNS bool
  AS '$libdir/test_slru', 'test_slru_page_exists' LANGUAGE C
;
----------
[CREATE_TABLE|UNSAFE] CREATE TEMPORARY TABLE q5_prep_nodata AS EXECUTE q5(200, 'DTAAAA')
    WITH NO DATA
;
----------
[DROP_TABLE] -- cleanup
drop table foo cascade
;
----------
[CREATE_TABLE] --
-- Check that use of an index with an extraneous column doesn't produce
-- a plan with extraneous sorting
--

create table matest0 (a int, b int, c int, d int)
;
----------
[DROP_PROG_OBJ] drop function func_part_attach()
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub FOR TABLE tab_test
;
----------
[CREATE_TABLE] -- also check the case that doesn't rewrite the table

CREATE TABLE vtype2 (a int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_statistic_ext
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION regress_testsub3
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_xact_user_functions AS
    SELECT
            P.oid AS funcid,
            N.nspname AS schemaname,
            P.proname AS funcname,
            pg_stat_get_xact_function_calls(P.oid) AS calls,
            pg_stat_get_xact_function_total_time(P.oid) AS total_time,
            pg_stat_get_xact_function_self_time(P.oid) AS self_time
    FROM pg_proc P LEFT JOIN pg_namespace N ON (N.oid = P.pronamespace)
    WHERE P.prolang != 12  -- fast check to eliminate built-in functions
          AND pg_stat_get_xact_function_calls(P.oid) IS NOT NULL
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_authid
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
----------
[CREATE_TABLE] create table pp_lp1 partition of pp_lp for values in(1)
;
----------
[COMMENT_PROG_OBJ] comment on function unreserved_test() is 'this is a test'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28062_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TRIGGER] DROP TRIGGER ftable_rowcount_trigger ON ltable
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=11276 host=/tmp/H11VjIA0o9 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp17b(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  INITCOND = '{}')
;
----------
[CREATE_TABLE] --
-- TIMETZ
--

CREATE TABLE TIMETZ_TBL (f1 time(2) with time zone)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON r1 FOR SELECT USING (false)
;
----------
[CREATE_TABLE] CREATE TABLE tab_foobar (id int)
;
----------
[ALTER_TYPE] alter domain posint add constraint c2 check(value > 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad_consistent(internal,macaddr,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_16656_2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^~ (
        LEFTARG = lquery,
	RIGHTARG = _ltree,
	PROCEDURE = _ltq_rregex,
        COMMUTATOR = '^~',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR dump_test.~~ (
							 PROCEDURE = int4eq,
							 LEFTARG = int,
							 RIGHTARG = int)
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p3 PARTITION OF plt2_adv FOR VALUES IN ('0003')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION get_columns_length(oid[])
    RETURNS int
    AS '/src/src/test/regress/regress.so'
    LANGUAGE C STRICT STABLE PARALLEL SAFE
;
