EXECUTE dumpTableAttach('46221')
;
EXECUTE dumpTableAttach('27253')
;
ALTER TABLE public.trigger_parted_p1 OWNER TO zyc
;
GRANT SELECT (one), INSERT (two), UPDATE (three) ON atest5 TO regress_priv_user4
;
-- ok, privileges have been granted
ALTER SYSTEM SET max_stack_depth = '100kB'
;
LOCK TABLE public.skip_wal_skip_rewrite_index IN ACCESS SHARE MODE
;
EXECUTE dumpFunc('36174')
;
-- subquery using stable function (can't be sent to remote)
PREPARE st2(int) AS SELECT * FROM ft1 t1 WHERE t1.c1 < $2 AND t1.c3 IN (SELECT c3 FROM ft2 t2 WHERE c1 > $1 AND date(c4) = '1970-01-17'::date) ORDER BY c1
;
EXECUTE dumpFunc('35878')
;
-- true by default

REVOKE ALL ON ALL FUNCTIONS IN SCHEMA testns FROM PUBLIC
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpTableAttach('46221')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('27253')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.trigger_parted_p1 OWNER TO zyc
;
----------
[GRANT] GRANT SELECT (one), INSERT (two), UPDATE (three) ON atest5 TO regress_priv_user4
;
----------
[SYSTEM_SETTING_WRITE] -- ok, privileges have been granted
ALTER SYSTEM SET max_stack_depth = '100kB'
;
----------
[SESSION_LOCK] LOCK TABLE public.skip_wal_skip_rewrite_index IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpFunc('36174')
;
----------
[UNSAFE] -- subquery using stable function (can't be sent to remote)
PREPARE st2(int) AS SELECT * FROM ft1 t1 WHERE t1.c1 < $2 AND t1.c3 IN (SELECT c3 FROM ft2 t2 WHERE c1 > $1 AND date(c4) = '1970-01-17'::date) ORDER BY c1
;
----------
[UNSAFE] EXECUTE dumpFunc('35878')
;
----------
[REVOKE] -- true by default

REVOKE ALL ON ALL FUNCTIONS IN SCHEMA testns FROM PUBLIC
;
