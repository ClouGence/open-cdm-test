DROP FUNCTION rngfuncr(in f2 int, out f1 int, out text)
;
CREATE TABLE public.t0 (
)
;
ALTER TABLE ONLY "public"."persons" OF "public"."person_type"
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=10480 host=/tmp/9XATbFFIzc dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 OPTIONS (SET p2 'V2', DROP p1)
;
create foreign table rem3 (f1 int, f2 text)
	server loopback options(table_name 'loc3')
;
DROP TABLE tmp_pg_shdescription
;
create unique index HSlot_name on HSlot using btree (slotname bpchar_ops)
;
-- fail
drop function dfunc(int)
;
CREATE TABLE PG_LSN_TBL (f1 pg_lsn)
;
-- If a partitioned table has a unique/PK constraint, then it's not possible
-- to drop the corresponding constraint in the children; nor it's possible
-- to drop the indexes individually.  Dropping the constraint in the parent
-- gets rid of the lot.
create table idxpart (i int) partition by hash (i)
;
-- inheritance

CREATE TABLE itest7 (a int GENERATED ALWAYS AS IDENTITY)
;
IMPORT FOREIGN SCHEMA import_source EXCEPT (t1, "x 4", nonesuch, t4_part)
  FROM SERVER loopback INTO import_dest4
;
CREATE TABLE "public"."num_exp_sub" (
    "id1" integer,
    "id2" integer,
    "expected" numeric(210,10)
)
;
CREATE INDEX bttest_a_expr_idx ON bttest_a ((ifun(id) + ifun(0)))
	WHERE ifun(id + 10) > ifun(10)
;
--
-- These tables have traditionally been referenced by many tests,
-- so create and populate them.  Insert only non-error values here.
-- (Some subsequent tests try to insert erroneous values.  That's okay
-- because the table won't actually change.  Do not change the contents
-- of these tables in later tests, as it may affect other tests.)
--

CREATE TABLE CHAR_TBL(f1 char(4))
;
-- valid
create function testpolym(a int, out a int) returns int as $$ select $1;$$ language sql
;
/*
 * This test is intended to pass on all platforms supported by Postgres.
 * We can therefore only assume that the default, C, and POSIX collations
 * are available --- and since the regression tests are often run in a
 * C-locale database, these may well all have the same behavior.  But
 * fortunately, the system doesn't know that and will treat them as
 * incompatible collations.  It is therefore at least possible to test
 * parser behaviors such as collation conflict resolution.  This test will,
 * however, be more revealing when run in a database with non-C locale,
 * since any departure from C sorting behavior will show as a failure.
 */
CREATE SCHEMA collate_tests
;
--
-- check set default and table constraint on multiple columns
--
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 text, PRIMARY KEY(ptest1, ptest2) )
;
--
-- Tests for updatable cursors
--

CREATE TEMP TABLE uctest(f1 int, f2 text)
;
ANALYZE vacowned_part1
;
CREATE FUNCTION "public"."ttdummy"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'ttdummy'
;
CREATE OPERATOR CLASS int4_fickle_ops FOR TYPE int4 USING btree AS
	    OPERATOR 1 < (int4, int4), OPERATOR 2 <= (int4, int4),
	    OPERATOR 3 = (int4, int4), OPERATOR 4 >= (int4, int4),
	    OPERATOR 5 > (int4, int4), FUNCTION 1 int4_asc_cmp(int4, int4)
;
/*
 * 24.2
 * COLUMN_OPTIONS view
 */
CREATE VIEW column_options AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(c.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.attname AS sql_identifier) AS column_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_table_columns c
;
CREATE TEMPORARY TABLE result AS
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
UNION ALL
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
UNION ALL
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
;
CREATE TABLESPACE regress_tblspace LOCATION 'relative'
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
CREATE TRIGGER "tg_backlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_backlink_a"('PS')
;
alter table fkest add constraint fk
  foreign key (x, x10b, x100) references fkest (x, x10, x100)
;
-- fail
ALTER TABLE constraint_rename_test RENAME CONSTRAINT con1 TO con1foo
;
create temp table i_table (f1 int)
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=26280 host=/tmp/kQdz8V4YN4 dbname=postgres' PUBLICATION mypub
;
CREATE RULE "rtest_sys_upd" AS
    ON UPDATE TO "public"."rtest_system" DO ( UPDATE "public"."rtest_interface" SET "sysname" = "new"."sysname"
  WHERE ("rtest_interface"."sysname" = "old"."sysname");
 UPDATE "public"."rtest_admin" SET "sysname" = "new"."sysname"
  WHERE ("rtest_admin"."sysname" = "old"."sysname");
)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_operator
;
create or replace function catch() returns void as $$
begin
  raise notice '%', case_test(6);
exception
  when case_not_found then
    raise notice 'caught case_not_found % %', SQLSTATE, SQLERRM;
end
$$ language plpgsql
;
-- test python -> domain over hstore
CREATE DOMAIN hstore_foo AS hstore CHECK(VALUE ? 'foo')
;
create rule rtest_emp_del as on delete to rtest_emp do
	insert into rtest_emplog values (old.ename, current_user,
			'fired', '0.00', old.salary)
;
ANALYZE public.clstr_tst_s
;
ALTER PUBLICATION testpub_default SET (publish = 'insert, update, delete')
;
CREATE FUNCTION "public"."gtest_trigger_func4"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  NEW.a = 10;
  NEW.b = 300;
  RETURN NEW;
END;
$$
;
drop function anycompatiblearray_anycompatiblerange_func(anycompatiblearray, anycompatiblerange)
;
create table part_gg partition of list_parted for values in ('gg') partition by range (b)
;
ANALYZE pg_temp_2.pg_temp_28694
;
CREATE FUNCTION gin_extract_query_char("char", internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
DROP PUBLICATION IF EXISTS split_pub_schema
;
ANALYZE pg_temp_3.pg_temp_27379_2
;
drop function My_Table_Check ( )
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_rewrite
;
CREATE TABLE droppk_d PARTITION OF droppk DEFAULT
;
CREATE TABLE FKTABLE_FAIL4 ( ftest1 oid, CONSTRAINT fkfail1 FOREIGN KEY (ftest1) REFERENCES PKTABLE(tableoid))
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] DROP FUNCTION rngfuncr(in f2 int, out f1 int, out text)
;
----------
[CREATE_TABLE] CREATE TABLE public.t0 (
)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."persons" OF "public"."person_type"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=10480 host=/tmp/9XATbFFIzc dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 OPTIONS (SET p2 'V2', DROP p1)
;
----------
[CREATE_TABLE] create foreign table rem3 (f1 int, f2 text)
	server loopback options(table_name 'loc3')
;
----------
[DROP_TABLE] DROP TABLE tmp_pg_shdescription
;
----------
[ADD_INDEX] create unique index HSlot_name on HSlot using btree (slotname bpchar_ops)
;
----------
[DROP_PROG_OBJ] -- fail
drop function dfunc(int)
;
----------
[CREATE_TABLE] CREATE TABLE PG_LSN_TBL (f1 pg_lsn)
;
----------
[CREATE_TABLE] -- If a partitioned table has a unique/PK constraint, then it's not possible
-- to drop the corresponding constraint in the children; nor it's possible
-- to drop the indexes individually.  Dropping the constraint in the parent
-- gets rid of the lot.
create table idxpart (i int) partition by hash (i)
;
----------
[CREATE_TABLE] -- inheritance

CREATE TABLE itest7 (a int GENERATED ALWAYS AS IDENTITY)
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA import_source EXCEPT (t1, "x 4", nonesuch, t4_part)
  FROM SERVER loopback INTO import_dest4
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_exp_sub" (
    "id1" integer,
    "id2" integer,
    "expected" numeric(210,10)
)
;
----------
[ADD_INDEX] CREATE INDEX bttest_a_expr_idx ON bttest_a ((ifun(id) + ifun(0)))
	WHERE ifun(id + 10) > ifun(10)
;
----------
[CREATE_TABLE] --
-- These tables have traditionally been referenced by many tests,
-- so create and populate them.  Insert only non-error values here.
-- (Some subsequent tests try to insert erroneous values.  That's okay
-- because the table won't actually change.  Do not change the contents
-- of these tables in later tests, as it may affect other tests.)
--

CREATE TABLE CHAR_TBL(f1 char(4))
;
----------
[CREATE_PROG_OBJ] -- valid
create function testpolym(a int, out a int) returns int as $$ select $1;$$ language sql
;
----------
[CREATE_SCHEMA] /*
 * This test is intended to pass on all platforms supported by Postgres.
 * We can therefore only assume that the default, C, and POSIX collations
 * are available --- and since the regression tests are often run in a
 * C-locale database, these may well all have the same behavior.  But
 * fortunately, the system doesn't know that and will treat them as
 * incompatible collations.  It is therefore at least possible to test
 * parser behaviors such as collation conflict resolution.  This test will,
 * however, be more revealing when run in a database with non-C locale,
 * since any departure from C sorting behavior will show as a failure.
 */
CREATE SCHEMA collate_tests
;
----------
[CREATE_TABLE] --
-- check set default and table constraint on multiple columns
--
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 text, PRIMARY KEY(ptest1, ptest2) )
;
----------
[CREATE_TABLE] --
-- Tests for updatable cursors
--

CREATE TEMP TABLE uctest(f1 int, f2 text)
;
----------
[ADMIN_TABLE] ANALYZE vacowned_part1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ttdummy"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'ttdummy'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS int4_fickle_ops FOR TYPE int4 USING btree AS
	    OPERATOR 1 < (int4, int4), OPERATOR 2 <= (int4, int4),
	    OPERATOR 3 = (int4, int4), OPERATOR 4 >= (int4, int4),
	    OPERATOR 5 > (int4, int4), FUNCTION 1 int4_asc_cmp(int4, int4)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.2
 * COLUMN_OPTIONS view
 */
CREATE VIEW column_options AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(c.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.attname AS sql_identifier) AS column_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_table_columns c
;
----------
[CREATE_TABLE|SELECT] CREATE TEMPORARY TABLE result AS
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
UNION ALL
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
UNION ALL
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspace LOCATION 'relative'
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_backlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_backlink_a"('PS')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table fkest add constraint fk
  foreign key (x, x10b, x100) references fkest (x, x10, x100)
;
----------
[RENAME_CONSTRAINT] -- fail
ALTER TABLE constraint_rename_test RENAME CONSTRAINT con1 TO con1foo
;
----------
[CREATE_TABLE] create temp table i_table (f1 int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=26280 host=/tmp/kQdz8V4YN4 dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_POLICY] CREATE RULE "rtest_sys_upd" AS
    ON UPDATE TO "public"."rtest_system" DO ( UPDATE "public"."rtest_interface" SET "sysname" = "new"."sysname"
  WHERE ("rtest_interface"."sysname" = "old"."sysname");
 UPDATE "public"."rtest_admin" SET "sysname" = "new"."sysname"
  WHERE ("rtest_admin"."sysname" = "old"."sysname");
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_operator
;
----------
[CREATE_PROG_OBJ] create or replace function catch() returns void as $$
begin
  raise notice '%', case_test(6);
exception
  when case_not_found then
    raise notice 'caught case_not_found % %', SQLSTATE, SQLERRM;
end
$$ language plpgsql
;
----------
[CREATE_TYPE] -- test python -> domain over hstore
CREATE DOMAIN hstore_foo AS hstore CHECK(VALUE ? 'foo')
;
----------
[CREATE_POLICY] create rule rtest_emp_del as on delete to rtest_emp do
	insert into rtest_emplog values (old.ename, current_user,
			'fired', '0.00', old.salary)
;
----------
[ADMIN_TABLE] ANALYZE public.clstr_tst_s
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default SET (publish = 'insert, update, delete')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."gtest_trigger_func4"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  NEW.a = 10;
  NEW.b = 300;
  RETURN NEW;
END;
$$
;
----------
[DROP_PROG_OBJ] drop function anycompatiblearray_anycompatiblerange_func(anycompatiblearray, anycompatiblerange)
;
----------
[CREATE_TABLE] create table part_gg partition of list_parted for values in ('gg') partition by range (b)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_28694
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_char("char", internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[DROP_PUB_SUB] DROP PUBLICATION IF EXISTS split_pub_schema
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27379_2
;
----------
[DROP_PROG_OBJ] drop function My_Table_Check ( )
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_rewrite
;
----------
[CREATE_TABLE] CREATE TABLE droppk_d PARTITION OF droppk DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE_FAIL4 ( ftest1 oid, CONSTRAINT fkfail1 FOREIGN KEY (ftest1) REFERENCES PKTABLE(tableoid))
;
