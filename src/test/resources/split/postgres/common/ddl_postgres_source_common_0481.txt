CREATE TABLE sq_target (tid integer NOT NULL, balance integer)
  WITH (autovacuum_enabled=off)
;
ANALYZE public.city
;
-- cast is immutable
CREATE TABLE bigintp (
	a bigint
) PARTITION BY LIST (a)
;
ALTER TABLE split_alter_table.base_table ENABLE ROW LEVEL SECURITY
;
create index on test_table (a,b)
;
-- test an unlogged table, mostly to get coverage of spgistbuildempty
create unlogged table spgist_unlogged_tbl(id serial, b box)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn13,
	RIGHTARG = issn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
create function least_accum(anycompatible, anycompatible)
returns anycompatible language sql as
  'select least($1, $2)'
;
-- failed (name conflict)

-- invalid: non-lowercase quoted identifiers
CREATE TEXT SEARCH TEMPLATE tstemp_case ("Init" = init_function)
;
CREATE INDEX std_index on concur_heap(f2)
;
create index matest0i on matest0 (b, c)
;
ALTER TABLE trgfire ATTACH PARTITION trgfire5 FOR VALUES FROM (40) TO (50)
;
CREATE TEMPORARY TABLE q5_prep_results AS EXECUTE q5(200, 'DTAAAA')
;
CREATE VIEW pg_stat_replication AS
    SELECT
            S.pid,
            S.usesysid,
            U.rolname AS usename,
            S.application_name,
            S.client_addr,
            S.client_hostname,
            S.client_port,
            S.backend_start,
            S.backend_xmin,
            W.state,
            W.sent_lsn,
            W.write_lsn,
            W.flush_lsn,
            W.replay_lsn,
            W.write_lag,
            W.flush_lag,
            W.replay_lag,
            W.sync_priority,
            W.sync_state,
            W.reply_time
    FROM pg_stat_get_activity(NULL) AS S
        JOIN pg_stat_get_wal_senders() AS W ON (S.pid = W.pid)
        LEFT JOIN pg_authid AS U ON (S.usesysid = U.oid)
;
-- cleanup
DROP TABLE gin_check_multicolumn
;
ALTER TABLE rf_tbl_abcd_nopk REPLICA IDENTITY FULL
;
CREATE FUNCTION func_footrg_mod_a() RETURNS TRIGGER AS $$
    BEGIN
	  NEW.a = 2; -- This is changing partition key column.
   RETURN NEW;
  END $$ LANGUAGE PLPGSQL
;
CREATE FUNCTION gbt_bit_union(internal, internal)
RETURNS gbtreekey_var
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE VIEW iexit AS
   SELECT ih.name, ih.thepath,
	interpt_pp(ih.thepath, r.thepath) AS exit
   FROM ihighway ih, ramp r
   WHERE ih.thepath ?# r.thepath
;
DROP TABLE pg_temp_37.pg_temp_29701_2, pg_temp_37.pg_temp_29701
;
CREATE DOMAIN queryfuncs_char2 AS char(2)
;
call "test_catalog".test.test_procedure(1,test.test_function(3))
;
DROP TABLE cnn_pk, cnn_pk_child
;
DROP TABLE vac_truncate_test
;
-- check for event trigger
CREATE FUNCTION event_trigger_test()
RETURNS event_trigger AS $$
  BEGIN RAISE NOTICE 'event %: %', TG_EVENT, TG_TAG; END;
$$ LANGUAGE plpgsql
;
create type int8alias2 (
    input = int8alias2in,
    output = int8alias2out,
    like = int8
)
;
ALTER TABLE vtype2 ADD COLUMN b varchar(10) DEFAULT 'xxx'
;
ALTER TABLE tr_sub_ddl ALTER COLUMN data TYPE bigint
;
create extension pg_surgery
;
drop table psql_serial_tab
;
CREATE TABLE base_tbl (person text, visibility text)
;
CREATE OR REPLACE FUNCTION
  pg_start_backup(label text, fast boolean DEFAULT false, exclusive boolean DEFAULT true)
  RETURNS pg_lsn STRICT VOLATILE LANGUAGE internal AS 'pg_start_backup'
  PARALLEL RESTRICTED
;
-- to be failed
ALTER TABLE inhts RENAME d TO dd
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub3
;
TRUNCATE foo
;
-- null bound is okay
DROP TABLE temporal_rng4
;
create unique index ec1_expr1 on ec1((ff + 1))
;
-- 2 new columns, both have defaults
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
COMMENT ON TEXT SEARCH PARSER dump_test.alt_ts_prs1
					   IS 'comment on text search parser'
;
drop function f_immutable_int4(int)
;
CREATE TABLE maintain_test (a INT)
;
ALTER TABLE ONLY "public"."e_star" INHERIT "public"."c_star"
;
-- test more complex permutations of unique joins

create table j1 (id1 int, id2 int, primary key(id1,id2))
;
create table s5.p1_1 partition of s5.p1 for values in (1, 2, 3)
;
ALTER TABLE pg_toast.pg_toast_30497 RENAME TO reind_con_toast
;
ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p2" FOR VALUES IN ('0005', '0006', '0007', '0008')
;
CREATE TABLE prt2_l_p1 PARTITION OF prt2_l FOR VALUES FROM (0) TO (250)
;
-- ERROR
DROP FOREIGN TABLE ft2 CASCADE
;
CREATE FUNCTION isngt(isbn13, isbn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER CONVERSION alt_conv1 RENAME TO alt_conv3
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE sq_target (tid integer NOT NULL, balance integer)
  WITH (autovacuum_enabled=off)
;
----------
[ADMIN_TABLE] ANALYZE public.city
;
----------
[CREATE_TABLE] -- cast is immutable
CREATE TABLE bigintp (
	a bigint
) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table ENABLE ROW LEVEL SECURITY
;
----------
[ADD_INDEX] create index on test_table (a,b)
;
----------
[CREATE_TABLE] -- test an unlogged table, mostly to get coverage of spgistbuildempty
create unlogged table spgist_unlogged_tbl(id serial, b box)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn13,
	RIGHTARG = issn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_PROG_OBJ] create function least_accum(anycompatible, anycompatible)
returns anycompatible language sql as
  'select least($1, $2)'
;
----------
[CREATE_POLICY] -- failed (name conflict)

-- invalid: non-lowercase quoted identifiers
CREATE TEXT SEARCH TEMPLATE tstemp_case ("Init" = init_function)
;
----------
[ADD_INDEX] CREATE INDEX std_index on concur_heap(f2)
;
----------
[ADD_INDEX] create index matest0i on matest0 (b, c)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE trgfire ATTACH PARTITION trgfire5 FOR VALUES FROM (40) TO (50)
;
----------
[CREATE_TABLE|UNSAFE] CREATE TEMPORARY TABLE q5_prep_results AS EXECUTE q5(200, 'DTAAAA')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_replication AS
    SELECT
            S.pid,
            S.usesysid,
            U.rolname AS usename,
            S.application_name,
            S.client_addr,
            S.client_hostname,
            S.client_port,
            S.backend_start,
            S.backend_xmin,
            W.state,
            W.sent_lsn,
            W.write_lsn,
            W.flush_lsn,
            W.replay_lsn,
            W.write_lag,
            W.flush_lag,
            W.replay_lag,
            W.sync_priority,
            W.sync_state,
            W.reply_time
    FROM pg_stat_get_activity(NULL) AS S
        JOIN pg_stat_get_wal_senders() AS W ON (S.pid = W.pid)
        LEFT JOIN pg_authid AS U ON (S.usesysid = U.oid)
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE gin_check_multicolumn
;
----------
[ALTER_TABLE] ALTER TABLE rf_tbl_abcd_nopk REPLICA IDENTITY FULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_footrg_mod_a() RETURNS TRIGGER AS $$
    BEGIN
	  NEW.a = 2; -- This is changing partition key column.
   RETURN NEW;
  END $$ LANGUAGE PLPGSQL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bit_union(internal, internal)
RETURNS gbtreekey_var
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW iexit AS
   SELECT ih.name, ih.thepath,
	interpt_pp(ih.thepath, r.thepath) AS exit
   FROM ihighway ih, ramp r
   WHERE ih.thepath ?# r.thepath
;
----------
[DROP_TABLE] DROP TABLE pg_temp_37.pg_temp_29701_2, pg_temp_37.pg_temp_29701
;
----------
[CREATE_TYPE] CREATE DOMAIN queryfuncs_char2 AS char(2)
;
----------
[CALL_PROG_OBJ] call "test_catalog".test.test_procedure(1,test.test_function(3))
;
----------
[DROP_TABLE] DROP TABLE cnn_pk, cnn_pk_child
;
----------
[DROP_TABLE] DROP TABLE vac_truncate_test
;
----------
[CREATE_PROG_OBJ] -- check for event trigger
CREATE FUNCTION event_trigger_test()
RETURNS event_trigger AS $$
  BEGIN RAISE NOTICE 'event %: %', TG_EVENT, TG_TAG; END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TYPE] create type int8alias2 (
    input = int8alias2in,
    output = int8alias2out,
    like = int8
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE vtype2 ADD COLUMN b varchar(10) DEFAULT 'xxx'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tr_sub_ddl ALTER COLUMN data TYPE bigint
;
----------
[CREATE_LIBRARY] create extension pg_surgery
;
----------
[DROP_TABLE] drop table psql_serial_tab
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl (person text, visibility text)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  pg_start_backup(label text, fast boolean DEFAULT false, exclusive boolean DEFAULT true)
  RETURNS pg_lsn STRICT VOLATILE LANGUAGE internal AS 'pg_start_backup'
  PARALLEL RESTRICTED
;
----------
[RENAME_COLUMN] -- to be failed
ALTER TABLE inhts RENAME d TO dd
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub3
;
----------
[TRUNCATE_TABLE] TRUNCATE foo
;
----------
[DROP_TABLE] -- null bound is okay
DROP TABLE temporal_rng4
;
----------
[ADD_INDEX] create unique index ec1_expr1 on ec1((ff + 1))
;
----------
[CREATE_TABLE] -- 2 new columns, both have defaults
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH PARSER dump_test.alt_ts_prs1
					   IS 'comment on text search parser'
;
----------
[DROP_PROG_OBJ] drop function f_immutable_int4(int)
;
----------
[CREATE_TABLE] CREATE TABLE maintain_test (a INT)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."e_star" INHERIT "public"."c_star"
;
----------
[CREATE_TABLE] -- test more complex permutations of unique joins

create table j1 (id1 int, id2 int, primary key(id1,id2))
;
----------
[CREATE_TABLE] create table s5.p1_1 partition of s5.p1 for values in (1, 2, 3)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30497 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p2" FOR VALUES IN ('0005', '0006', '0007', '0008')
;
----------
[CREATE_TABLE] CREATE TABLE prt2_l_p1 PARTITION OF prt2_l FOR VALUES FROM (0) TO (250)
;
----------
[DROP_TABLE] -- ERROR
DROP FOREIGN TABLE ft2 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(isbn13, isbn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_POLICY] ALTER CONVERSION alt_conv1 RENAME TO alt_conv3
;
