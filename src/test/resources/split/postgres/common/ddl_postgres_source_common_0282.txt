drop table smalltbl
;
CREATE FUNCTION isnle(ean13, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE SEQUENCE "public"."rules_log_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
create table s4.p1 (a int, b int) PARTITION BY list (a)
;
CREATE OR REPLACE VIEW mysecview3 WITH (security_barrier=true)
       AS SELECT * FROM tbl1 WHERE a < 256
;
create function testrngfunc() returns setof record as $$
  insert into rngfunc values (1,2), (3,4) returning *;
$$ language sql
;
create index gist_tbl_multi_index on gist_tbl using gist (p, c)
;
DROP TEXT SEARCH CONFIGURATION dummy_tst
;
CREATE DATABASE mydb
;
CREATE TABLE "public"."cmmove1" (
    "f1" "text"
)
;
CREATE FUNCTION seg_upper(seg)
RETURNS float4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create trigger mlparted5abrtrig before insert on mlparted5a for each row execute procedure mlparted5abrtrig_func()
;
CREATE FUNCTION trunc_trig_after_stat_trig() RETURNS trigger
		  LANGUAGE plpgsql as $$
		  BEGIN
			INSERT INTO trunc_trig VALUES (DEFAULT, 'triggered stat before');
			RETURN NULL;
		  END; $$
;
DROP TABLE clstr_expression
;
CREATE INDEX "gtest22c_b_idx" ON "generated_stored_tests"."gtest22c" USING "btree" ("b")
;
-- should be created in temp_views schema
CREATE VIEW temp_view_test.v2 AS SELECT * FROM base_table
;
ANALYZE pg_am
;
CREATE TABLE truncate_a (id serial,
                         id1 integer default nextval('truncate_a_id1'))
;
VACUUM (FREEZE) pg_catalog.pg_foreign_server
;
-- Test default partitions
ALTER TABLE prt1_adv DETACH PARTITION prt1_adv_p1
;
CREATE TABLE pk1 PARTITION OF pk FOR VALUES IN (1, 2) PARTITION BY LIST (a)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_inherits
;
-- expression references object in public schema
create index pti1 on pt ((mydouble(category) + 1))
;
DROP SUBSCRIPTION mysub1
;
CREATE TABLE "public"."interval_tbl" (
    "f1" interval
)
;
ALTER TABLE list_parted DROP COLUMN b
;
create view uv_iocu_view as
    select b as bb, a as aa, uv_iocu_tab::text as cc from uv_iocu_tab
;
ANALYZE ndistinct
;
ALTER VIEW rw_view2 RESET (check_option)
;
CREATE TABLE "public"."bar2" (
    "a" integer,
    "b" integer DEFAULT 1 NOT NULL
)
;
DROP CONVERSION IF EXISTS split_conversion_probe.default_latin1_to_utf8_probe CASCADE
;
alter table "catalog1".schema1."test" alter column "a1" drop not null
;
CREATE RULE pg_settings_u AS
    ON UPDATE TO pg_settings
    WHERE new.name = old.name DO
    SELECT set_config(old.name, new.setting, 'f')
;
DROP TABLE fs_target
;
ALTER TABLE dump_test_second_schema.measurement_y2006m4 ENABLE REPLICA TRIGGER test_trigger
;
--
-- regress_injection.c functions
--
CREATE FUNCTION removable_cutoff(rel regclass)
RETURNS xid8
AS 'MODULE_PATHNAME'
LANGUAGE C CALLED ON NULL INPUT
;
/*
 * 6.50
 * ROUTINE_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 6.51
 * ROUTINE_PRIVILEGES view
 */

CREATE VIEW routine_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(n.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST('EXECUTE' AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, p.proowner, 'USAGE')
                  OR p.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, proname, proowner, pronamespace, (aclexplode(coalesce(proacl, acldefault('f', proowner)))).* FROM pg_proc
         ) p (oid, proname, proowner, pronamespace, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE p.pronamespace = n.oid
          AND grantee.oid = p.grantee
          AND u_grantor.oid = p.grantor
          AND p.prtype IN ('EXECUTE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
--
-- bt_index_check()
--
CREATE FUNCTION bt_index_check(index regclass,
    heapallindexed boolean, checkunique boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
VACUUM (ANALYZE) pg_catalog.pg_replication_origin
;
create table ctrtest (a int, b text) partition by list (a)
;
alter table atacc3 add test2 bool
;
CREATE INDEX text_idx on test__int using gist ( a gist__intbig_ops )
;
CREATE VIEW at_v1 AS SELECT 1 as a
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_statistic_ext_data
;
CREATE FUNCTION "generated_stored_tests"."gtest_trigger_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  IF tg_op IN ('DELETE', 'UPDATE') THEN
    RAISE INFO '%: %: old = %', TG_NAME, TG_WHEN, OLD;
  END IF;
  IF tg_op IN ('INSERT', 'UPDATE') THEN
    RAISE INFO '%: %: new = %', TG_NAME, TG_WHEN, NEW;
  END IF;
  IF tg_op = 'DELETE' THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END
$$
;
CREATE TABLE "public"."fk_partitioned_fk_5" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
PARTITION BY RANGE ("a")
;
drop table hash_tbl
;
create or replace function raise_test() returns void as $$
begin
  raise division_by_zero using message = 'custom' || ' message';
end;
$$ language plpgsql
;
ALTER TABLE new_system_table SET SCHEMA pg_catalog
;
drop table parted_inval_tab
;
------------------------------------------------------------------------------------------
[DROP_TABLE] drop table smalltbl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ean13, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."rules_log_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_TABLE] create table s4.p1 (a int, b int) PARTITION BY list (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW mysecview3 WITH (security_barrier=true)
       AS SELECT * FROM tbl1 WHERE a < 256
;
----------
[CREATE_PROG_OBJ] create function testrngfunc() returns setof record as $$
  insert into rngfunc values (1,2), (3,4) returning *;
$$ language sql
;
----------
[ADD_INDEX] create index gist_tbl_multi_index on gist_tbl using gist (p, c)
;
----------
[DROP_POLICY] DROP TEXT SEARCH CONFIGURATION dummy_tst
;
----------
[CREATE_CATALOG] CREATE DATABASE mydb
;
----------
[CREATE_TABLE] CREATE TABLE "public"."cmmove1" (
    "f1" "text"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_upper(seg)
RETURNS float4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger mlparted5abrtrig before insert on mlparted5a for each row execute procedure mlparted5abrtrig_func()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trunc_trig_after_stat_trig() RETURNS trigger
		  LANGUAGE plpgsql as $$
		  BEGIN
			INSERT INTO trunc_trig VALUES (DEFAULT, 'triggered stat before');
			RETURN NULL;
		  END; $$
;
----------
[DROP_TABLE] DROP TABLE clstr_expression
;
----------
[ADD_INDEX] CREATE INDEX "gtest22c_b_idx" ON "generated_stored_tests"."gtest22c" USING "btree" ("b")
;
----------
[CREATE_VIEW(SELECT)] -- should be created in temp_views schema
CREATE VIEW temp_view_test.v2 AS SELECT * FROM base_table
;
----------
[ADMIN_TABLE] ANALYZE pg_am
;
----------
[CREATE_TABLE] CREATE TABLE truncate_a (id serial,
                         id1 integer default nextval('truncate_a_id1'))
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_foreign_server
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Test default partitions
ALTER TABLE prt1_adv DETACH PARTITION prt1_adv_p1
;
----------
[CREATE_TABLE] CREATE TABLE pk1 PARTITION OF pk FOR VALUES IN (1, 2) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_inherits
;
----------
[ADD_INDEX] -- expression references object in public schema
create index pti1 on pt ((mydouble(category) + 1))
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION mysub1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."interval_tbl" (
    "f1" interval
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE list_parted DROP COLUMN b
;
----------
[CREATE_VIEW(SELECT)] create view uv_iocu_view as
    select b as bb, a as aa, uv_iocu_tab::text as cc from uv_iocu_tab
;
----------
[ADMIN_TABLE] ANALYZE ndistinct
;
----------
[ALTER_VIEW] ALTER VIEW rw_view2 RESET (check_option)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."bar2" (
    "a" integer,
    "b" integer DEFAULT 1 NOT NULL
)
;
----------
[DROP_POLICY] DROP CONVERSION IF EXISTS split_conversion_probe.default_latin1_to_utf8_probe CASCADE
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table "catalog1".schema1."test" alter column "a1" drop not null
;
----------
[CREATE_POLICY] CREATE RULE pg_settings_u AS
    ON UPDATE TO pg_settings
    WHERE new.name = old.name DO
    SELECT set_config(old.name, new.setting, 'f')
;
----------
[DROP_TABLE] DROP TABLE fs_target
;
----------
[ALTER_TABLE] ALTER TABLE dump_test_second_schema.measurement_y2006m4 ENABLE REPLICA TRIGGER test_trigger
;
----------
[CREATE_PROG_OBJ] --
-- regress_injection.c functions
--
CREATE FUNCTION removable_cutoff(rel regclass)
RETURNS xid8
AS 'MODULE_PATHNAME'
LANGUAGE C CALLED ON NULL INPUT
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.50
 * ROUTINE_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 6.51
 * ROUTINE_PRIVILEGES view
 */

CREATE VIEW routine_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(n.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST('EXECUTE' AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, p.proowner, 'USAGE')
                  OR p.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, proname, proowner, pronamespace, (aclexplode(coalesce(proacl, acldefault('f', proowner)))).* FROM pg_proc
         ) p (oid, proname, proowner, pronamespace, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE p.pronamespace = n.oid
          AND grantee.oid = p.grantee
          AND u_grantor.oid = p.grantor
          AND p.prtype IN ('EXECUTE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
----------
[CREATE_PROG_OBJ] --
-- bt_index_check()
--
CREATE FUNCTION bt_index_check(index regclass,
    heapallindexed boolean, checkunique boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE] create table ctrtest (a int, b text) partition by list (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table atacc3 add test2 bool
;
----------
[ADD_INDEX] CREATE INDEX text_idx on test__int using gist ( a gist__intbig_ops )
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW at_v1 AS SELECT 1 as a
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "generated_stored_tests"."gtest_trigger_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  IF tg_op IN ('DELETE', 'UPDATE') THEN
    RAISE INFO '%: %: old = %', TG_NAME, TG_WHEN, OLD;
  END IF;
  IF tg_op IN ('INSERT', 'UPDATE') THEN
    RAISE INFO '%: %: new = %', TG_NAME, TG_WHEN, NEW;
  END IF;
  IF tg_op = 'DELETE' THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_5" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
PARTITION BY RANGE ("a")
;
----------
[DROP_TABLE] drop table hash_tbl
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise division_by_zero using message = 'custom' || ' message';
end;
$$ language plpgsql
;
----------
[ALTER_TABLE] ALTER TABLE new_system_table SET SCHEMA pg_catalog
;
----------
[DROP_TABLE] drop table parted_inval_tab
;
