alter table atacc1 add unique(a)
;
--not-indexed
CREATE OPERATOR ^? (
        LEFTARG = ltree,
	RIGHTARG = _lquery,
	PROCEDURE = lt_q_regex,
	COMMUTATOR = '^?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
TRUNCATE TABLE truncate_a RESTRICT
;
CREATE TEMP TABLE withoutoid() WITHOUT OIDS
;
ALTER TABLE pg_toast.pg_toast_30786 RENAME TO reind_con_toast
;
ANALYZE pg_temp_4.pg_temp_37918_2
;
CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=31667 host=/tmp/EP7K8mZqpu dbname=postgres' PUBLICATION tap_pub_B WITH (origin = none, copy_data = on)
;
CREATE TEMPORARY TABLE just_give_me_a_type(id serial primary key)
;
-- ADD CONSTRAINT USING INDEX is forbidden on partitioned tables
CREATE TABLE cwi_test(a int) PARTITION BY hash (a)
;
TRUNCATE table stream_test
;
CREATE OPERATOR !~~* (
    PROCEDURE = texticnlike,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = ~~*,
    RESTRICT  = icnlikesel,
    JOIN      = icnlikejoinsel
)
;
-- normal operation
CREATE TABLE width_bucket_test (operand_num numeric, operand_f8 float8)
;
ALTER TABLE regtest_table_3 DISABLE RULE regtest_test_rule
;
create domain domainint4 int4
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE TABLE test (a INT)
;
-- ERROR
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE char(10)
;
CREATE PUBLICATION testpub1 FOR TABLE tab1
;
DROP TABLE atest1
;
DROP INDEX IF EXISTS mvtest_drop_idx
;
--
-- reject function definitions that contain malformed SQL queries at
-- compile-time, where possible
--
create function bad_sql1() returns int as $$
declare a int;
begin
    a := 5;
    Johnny Yuma;
    a := 10;
    return a;
end$$ language plpgsql
;
alter table temp_part_parent attach partition temp_part_child default
;
create view v1a as select * from (tt2 natural join tt3) j
;
-- Create the operator class
CREATE OPERATOR CLASS gist_inet_ops
DEFAULT FOR TYPE inet USING gist
AS
	OPERATOR	1	<   ,
	OPERATOR	2	<=  ,
	OPERATOR	3	=   ,
	OPERATOR	4	>=  ,
	OPERATOR	5	>   ,
	FUNCTION	1	gbt_inet_consistent (internal, inet, int2, oid, internal),
	FUNCTION	2	gbt_inet_union (internal, internal),
	FUNCTION	3	gbt_inet_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_inet_penalty (internal, internal, internal),
	FUNCTION	6	gbt_inet_picksplit (internal, internal),
	FUNCTION	7	gbt_inet_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
CREATE TABLE tr_oddlength (id text primary key, data text)
;
CREATE TABLE ctlt4 (a text, c text)
;
ALTER TEXT SEARCH CONFIGURATION estonian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
ALTER TABLE prt1_adv ATTACH PARTITION prt1_adv_p1 DEFAULT
;
DROP SCHEMA test_schema_exists
;
-- Bug #16784
create table bug_16784(i int, j int)
;
CREATE TYPE "public"."cashrange" AS RANGE (
    subtype = "money",
    multirange_type_name = "public"."cashmultirange"
)
;
CREATE TABLE conf_tab_2 (a int PRIMARY KEY, b int UNIQUE, c int UNIQUE)
;
CREATE INDEX idx_time ON test_time USING gin (i)
;
CREATE POLICY priv_test_parent_tbl_pol ON tststats.priv_test_parent_tbl USING (2 * a < 0)
;
CREATE TABLE "public"."sj" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
drop table returningwrtest
;
-- Move to new version 1.1.
ALTER EXTENSION pg_walinspect UPDATE TO '1.1'
;
create function whoami() returns trigger language plpgsql
as $$
begin
  raise notice 'I am %', current_user;
  return null;
end;
$$
;
CREATE OPERATOR CLASS uuid_ops
DEFAULT FOR TYPE uuid USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       uuid_cmp(uuid,uuid),
    FUNCTION        2       gin_extract_value_uuid(uuid, internal),
    FUNCTION        3       gin_extract_query_uuid(uuid, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_uuid(uuid,uuid,int2, internal),
STORAGE         uuid
;
create table ddtest2(f1 posint2)
;
CREATE TABLE pub_testpart2.parent2 (a int) partition by list (a)
;
--
-- Test setting commutator, negator, merges, and hashes attributes,
-- which can only be set if not already set
--

CREATE FUNCTION alter_op_test_fn_bool_real(boolean, real)
RETURNS boolean AS $$ SELECT NULL::BOOLEAN; $$ LANGUAGE sql IMMUTABLE
;
create function aggfns_trans(aggtype[],integer,integer,text) returns aggtype[]
as 'select array_append($1,ROW($2,$3,$4)::aggtype)'
language sql immutable
;
CREATE FUNCTION leak(integer,integer) RETURNS boolean
  AS $$begin return $1 < $2; end$$
  LANGUAGE plpgsql immutable
;
DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize_multi VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
create function dfunc(a int = 1, int = 2) returns int as $$
  select $1 + $2;
$$ language sql
;
-- NOT NULL NO INHERIT
create table inh_nn_parent(a int)
;
create index ab_a1_b1_a_idx on ab_a1_b1 (a)
;
DROP SCHEMA IF EXISTS split_extension_next CASCADE
;
CREATE INDEX ctl_table_a_key ON ctl_table(a)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add unique(a)
;
----------
[CREATE_PROG_OBJ] --not-indexed
CREATE OPERATOR ^? (
        LEFTARG = ltree,
	RIGHTARG = _lquery,
	PROCEDURE = lt_q_regex,
	COMMUTATOR = '^?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE truncate_a RESTRICT
;
----------
[CREATE_TABLE] CREATE TEMP TABLE withoutoid() WITHOUT OIDS
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30786 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37918_2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=31667 host=/tmp/EP7K8mZqpu dbname=postgres' PUBLICATION tap_pub_B WITH (origin = none, copy_data = on)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE just_give_me_a_type(id serial primary key)
;
----------
[CREATE_TABLE] -- ADD CONSTRAINT USING INDEX is forbidden on partitioned tables
CREATE TABLE cwi_test(a int) PARTITION BY hash (a)
;
----------
[TRUNCATE_TABLE] TRUNCATE table stream_test
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !~~* (
    PROCEDURE = texticnlike,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = ~~*,
    RESTRICT  = icnlikesel,
    JOIN      = icnlikejoinsel
)
;
----------
[CREATE_TABLE] -- normal operation
CREATE TABLE width_bucket_test (operand_num numeric, operand_f8 float8)
;
----------
[ALTER_TABLE] ALTER TABLE regtest_table_3 DISABLE RULE regtest_test_rule
;
----------
[CREATE_TYPE] create domain domainint4 int4
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] CREATE TABLE test (a INT)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ERROR
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE char(10)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub1 FOR TABLE tab1
;
----------
[DROP_TABLE] DROP TABLE atest1
;
----------
[DROP_INDEX] DROP INDEX IF EXISTS mvtest_drop_idx
;
----------
[CREATE_PROG_OBJ] --
-- reject function definitions that contain malformed SQL queries at
-- compile-time, where possible
--
create function bad_sql1() returns int as $$
declare a int;
begin
    a := 5;
    Johnny Yuma;
    a := 10;
    return a;
end$$ language plpgsql
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table temp_part_parent attach partition temp_part_child default
;
----------
[CREATE_VIEW(SELECT)] create view v1a as select * from (tt2 natural join tt3) j
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_inet_ops
DEFAULT FOR TYPE inet USING gist
AS
	OPERATOR	1	<   ,
	OPERATOR	2	<=  ,
	OPERATOR	3	=   ,
	OPERATOR	4	>=  ,
	OPERATOR	5	>   ,
	FUNCTION	1	gbt_inet_consistent (internal, inet, int2, oid, internal),
	FUNCTION	2	gbt_inet_union (internal, internal),
	FUNCTION	3	gbt_inet_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_inet_penalty (internal, internal, internal),
	FUNCTION	6	gbt_inet_picksplit (internal, internal),
	FUNCTION	7	gbt_inet_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[CREATE_TABLE] CREATE TABLE tr_oddlength (id text primary key, data text)
;
----------
[CREATE_TABLE] CREATE TABLE ctlt4 (a text, c text)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION estonian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt1_adv ATTACH PARTITION prt1_adv_p1 DEFAULT
;
----------
[DROP_SCHEMA] DROP SCHEMA test_schema_exists
;
----------
[CREATE_TABLE] -- Bug #16784
create table bug_16784(i int, j int)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."cashrange" AS RANGE (
    subtype = "money",
    multirange_type_name = "public"."cashmultirange"
)
;
----------
[CREATE_TABLE] CREATE TABLE conf_tab_2 (a int PRIMARY KEY, b int UNIQUE, c int UNIQUE)
;
----------
[ADD_INDEX] CREATE INDEX idx_time ON test_time USING gin (i)
;
----------
[CREATE_POLICY] CREATE POLICY priv_test_parent_tbl_pol ON tststats.priv_test_parent_tbl USING (2 * a < 0)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."sj" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[DROP_TABLE] drop table returningwrtest
;
----------
[ALTER_LIBRARY] -- Move to new version 1.1.
ALTER EXTENSION pg_walinspect UPDATE TO '1.1'
;
----------
[CREATE_PROG_OBJ] create function whoami() returns trigger language plpgsql
as $$
begin
  raise notice 'I am %', current_user;
  return null;
end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS uuid_ops
DEFAULT FOR TYPE uuid USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       uuid_cmp(uuid,uuid),
    FUNCTION        2       gin_extract_value_uuid(uuid, internal),
    FUNCTION        3       gin_extract_query_uuid(uuid, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_uuid(uuid,uuid,int2, internal),
STORAGE         uuid
;
----------
[CREATE_TABLE] create table ddtest2(f1 posint2)
;
----------
[CREATE_TABLE] CREATE TABLE pub_testpart2.parent2 (a int) partition by list (a)
;
----------
[CREATE_PROG_OBJ] --
-- Test setting commutator, negator, merges, and hashes attributes,
-- which can only be set if not already set
--

CREATE FUNCTION alter_op_test_fn_bool_real(boolean, real)
RETURNS boolean AS $$ SELECT NULL::BOOLEAN; $$ LANGUAGE sql IMMUTABLE
;
----------
[CREATE_PROG_OBJ] create function aggfns_trans(aggtype[],integer,integer,text) returns aggtype[]
as 'select array_append($1,ROW($2,$3,$4)::aggtype)'
language sql immutable
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION leak(integer,integer) RETURNS boolean
  AS $$begin return $1 < $2; end$$
  LANGUAGE plpgsql immutable
;
----------
[BLOCK] DO $$
DECLARE curtid tid;
BEGIN
  LOOP
    INSERT INTO brin_summarize_multi VALUES (1) RETURNING ctid INTO curtid;
    EXIT WHEN curtid > tid '(2, 0)';
  END LOOP;
END;
$$
;
----------
[CREATE_PROG_OBJ] create function dfunc(a int = 1, int = 2) returns int as $$
  select $1 + $2;
$$ language sql
;
----------
[CREATE_TABLE] -- NOT NULL NO INHERIT
create table inh_nn_parent(a int)
;
----------
[ADD_INDEX] create index ab_a1_b1_a_idx on ab_a1_b1 (a)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_extension_next CASCADE
;
----------
[ADD_INDEX] CREATE INDEX ctl_table_a_key ON ctl_table(a)
;
