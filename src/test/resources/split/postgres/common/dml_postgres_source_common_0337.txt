INSERT INTO caster (text)          VALUES ('04:05:06'::time)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15643', relminmxid = '3'
WHERE oid = '36409'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14574', relminmxid = '3'
WHERE oid = '36521'
;
insert into shiftjis2004_inputs  values
  ('\x666f6f',		'valid, pure ASCII'),
  ('\x666f6f8fdb',	'valid'),
  ('\x666f6f81c0',	'valid, no translation to UTF-8'),
  ('\x666f6f82f5',	'valid, translates to two UTF-8 chars '),
  ('\x666f6f8fdb8f',	'incomplete char '),
  ('\x666f6f820a',	'incomplete char, followed by newline '),
  ('\x666f6f008fdb',	'invalid, NUL byte'),
  ('\x666f6f8f00db',	'invalid, NUL byte'),
  ('\x666f6f8fdb00',	'invalid, NUL byte')
;
DELETE FROM "S 1"."T 1" WHERE (("C 1" = 1200))
;
INSERT INTO ctv_data VALUES (1, 10, '*')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3915', relminmxid = '1'
WHERE oid = '20516'
;
INSERT INTO test_type_diff2_c1 VALUES (1, 2, 3)
;
insert into inserttest (f3.if2[1], f3.if2[2]) values ('foo', 'bar'), ('baz', 'quux')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."trigger_parted_p1_1"'::pg_catalog.regclass
;
-- basic before trigger
INSERT INTO trunc_trigger_test VALUES(1, 'foo', 'bar'), (2, 'baz', 'quux')
;
UPDATE generated_cols SET a = 100
;
-- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '726', datminmxid = '1'
WHERE datname = 'regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ'
;
explain (analyze, costs off, summary off, timing off)
update ab_a1 set b = 3 from ab_a2 where ab_a2.b = (select 1)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."beta_neg_p2"'::pg_catalog.regclass
;
DELETE FROM onek WHERE CURRENT OF c1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."gtest_normal_child2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15741', relminmxid = '3'
WHERE oid = '"public"."plt1_p2"'::pg_catalog.regclass
;
INSERT INTO gtest21b (a) VALUES (NULL)
;
INSERT INTO sql_parts VALUES ('11', 'Information and Definition Schema (SQL/Schemata)', 'NO', NULL, '')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt1_m_p1"'::pg_catalog.regclass
;
INSERT INTO FKTABLE VALUES (NULL, 2, 7, 4)
;
insert into t1 ( id , t ) values ( default , 'e' )
;
INSERT INTO pg_temp_13.pg_temp_26413_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_13.pg_temp_26413 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_13.pg_temp_26413 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO FKTABLE VALUES (NULL, 2, 4)
;
INSERT INTO num_exp_div VALUES (1,6,'0')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2610', relminmxid = '1'
WHERE oid = '"public"."tbl_include_box"'::pg_catalog.regclass
;
INSERT INTO fk_partitioned_fk_2 VALUES(1000, 1000)
;
insert into parted_trig values (50), (1500)
;
-- Insert enough data to create a tree that's a couple of levels deep.
insert into gist_point_tbl (id, p)
select g,        point(g*10, g*10) from generate_series(1, 10000) g
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '618', relminmxid = '1'
WHERE oid = '"public"."money_data"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14103', relminmxid = '3'
WHERE oid = '"public"."pht1_e_p1"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (2, 692)
;
-- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.7........'
  AND attrelid = '"public"."mlparted_def2"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('7', 857)
;
INSERT INTO public.tht VALUES ('9', 29)
;
INSERT INTO public.ths_p3 VALUES (9, 679)
;
INSERT INTO pg_init_privs   (objoid, classoid, objsubid, initprivs, privtype)    SELECT        oid,        (SELECT oid FROM pg_class WHERE relname = 'pg_type'),        0,        typacl,        'i'    FROM        pg_type    WHERE        typacl IS NOT NULL
;
INSERT INTO public.tht VALUES ('1', 361)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3100', relminmxid = '1'
WHERE oid = '"public"."tbl_include_pk"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('1', 601)
;
insert into WSlot values ('WS.106.3b', '106', '', '')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt2_p1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16727', relminmxid = '3'
WHERE oid = '"public"."prt1_m_p1"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '19086', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p2_p1"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'aa'
  AND attrelid = '"public"."b"'::pg_catalog.regclass
;
UPDATE ONLY "pg_temp_19"."tasks" SET "worker" = $1 WHERE $2 OPERATOR(pg_catalog.=) "worker"
;
WITH aa AS (SELECT 'a' a, 'b' b UNION ALL SELECT 'a' a, 'b' b)
INSERT INTO withz VALUES(1, 'insert')
ON CONFLICT (k) DO UPDATE SET v = (SELECT b || ' update' FROM aa WHERE a = 'a' LIMIT 1)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '16947'
;
INSERT INTO split_cov.parent_table (id, code)
VALUES (1, 'a'), (2, 'b')
ON CONFLICT (id) DO UPDATE SET code = excluded.code
RETURNING id, code
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] INSERT INTO caster (text)          VALUES ('04:05:06'::time)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15643', relminmxid = '3'
WHERE oid = '36409'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14574', relminmxid = '3'
WHERE oid = '36521'
;
----------
[INSERT] insert into shiftjis2004_inputs  values
  ('\x666f6f',		'valid, pure ASCII'),
  ('\x666f6f8fdb',	'valid'),
  ('\x666f6f81c0',	'valid, no translation to UTF-8'),
  ('\x666f6f82f5',	'valid, translates to two UTF-8 chars '),
  ('\x666f6f8fdb8f',	'incomplete char '),
  ('\x666f6f820a',	'incomplete char, followed by newline '),
  ('\x666f6f008fdb',	'invalid, NUL byte'),
  ('\x666f6f8f00db',	'invalid, NUL byte'),
  ('\x666f6f8fdb00',	'invalid, NUL byte')
;
----------
[DELETE] DELETE FROM "S 1"."T 1" WHERE (("C 1" = 1200))
;
----------
[INSERT] INSERT INTO ctv_data VALUES (1, 10, '*')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3915', relminmxid = '1'
WHERE oid = '20516'
;
----------
[INSERT] INSERT INTO test_type_diff2_c1 VALUES (1, 2, 3)
;
----------
[INSERT|SELECT] insert into inserttest (f3.if2[1], f3.if2[2]) values ('foo', 'bar'), ('baz', 'quux')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."trigger_parted_p1_1"'::pg_catalog.regclass
;
----------
[INSERT] -- basic before trigger
INSERT INTO trunc_trigger_test VALUES(1, 'foo', 'bar'), (2, 'baz', 'quux')
;
----------
[UPDATE] UPDATE generated_cols SET a = 100
;
----------
[UPDATE|METADATA] -- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '726', datminmxid = '1'
WHERE datname = 'regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ'
;
----------
[UPDATE|SELECT] explain (analyze, costs off, summary off, timing off)
update ab_a1 set b = 3 from ab_a2 where ab_a2.b = (select 1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."beta_neg_p2"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM onek WHERE CURRENT OF c1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6553', relminmxid = '1'
WHERE oid = '"public"."gtest_normal_child2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15741', relminmxid = '3'
WHERE oid = '"public"."plt1_p2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO gtest21b (a) VALUES (NULL)
;
----------
[INSERT] INSERT INTO sql_parts VALUES ('11', 'Information and Definition Schema (SQL/Schemata)', 'NO', NULL, '')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt1_m_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO FKTABLE VALUES (NULL, 2, 7, 4)
;
----------
[INSERT|SELECT] insert into t1 ( id , t ) values ( default , 'e' )
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_13.pg_temp_26413_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_13.pg_temp_26413 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_13.pg_temp_26413 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] INSERT INTO FKTABLE VALUES (NULL, 2, 4)
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (1,6,'0')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2610', relminmxid = '1'
WHERE oid = '"public"."tbl_include_box"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO fk_partitioned_fk_2 VALUES(1000, 1000)
;
----------
[INSERT] insert into parted_trig values (50), (1500)
;
----------
[INSERT|SELECT] -- Insert enough data to create a tree that's a couple of levels deep.
insert into gist_point_tbl (id, p)
select g,        point(g*10, g*10) from generate_series(1, 10000) g
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '618', relminmxid = '1'
WHERE oid = '"public"."money_data"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14103', relminmxid = '3'
WHERE oid = '"public"."pht1_e_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 692)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.7........'
  AND attrelid = '"public"."mlparted_def2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('7', 857)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 29)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 679)
;
----------
[INSERT|SELECT] INSERT INTO pg_init_privs   (objoid, classoid, objsubid, initprivs, privtype)    SELECT        oid,        (SELECT oid FROM pg_class WHERE relname = 'pg_type'),        0,        typacl,        'i'    FROM        pg_type    WHERE        typacl IS NOT NULL
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 361)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3100', relminmxid = '1'
WHERE oid = '"public"."tbl_include_pk"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 601)
;
----------
[INSERT] insert into WSlot values ('WS.106.3b', '106', '', '')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt2_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16727', relminmxid = '3'
WHERE oid = '"public"."prt1_m_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '19086', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p2_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'aa'
  AND attrelid = '"public"."b"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE ONLY "pg_temp_19"."tasks" SET "worker" = $1 WHERE $2 OPERATOR(pg_catalog.=) "worker"
;
----------
[MERGE|SELECT] WITH aa AS (SELECT 'a' a, 'b' b UNION ALL SELECT 'a' a, 'b' b)
INSERT INTO withz VALUES(1, 'insert')
ON CONFLICT (k) DO UPDATE SET v = (SELECT b || ' update' FROM aa WHERE a = 'a' LIMIT 1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '16947'
;
----------
[MERGE|SELECT] INSERT INTO split_cov.parent_table (id, code)
VALUES (1, 'a'), (2, 'b')
ON CONFLICT (id) DO UPDATE SET code = excluded.code
RETURNING id, code
;
