CREATE TABLE "fkpart5"."fk1" (
    "a" integer
)
PARTITION BY LIST ("a")
;
/* New stuff in 1.2 begins here */

CREATE FUNCTION pgstattuple(IN reloid regclass,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuplebyid'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE FUNCTION brin_page_type(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'brin_page_type'
LANGUAGE C STRICT PARALLEL SAFE
;
VACUUM pg_depend
;
alter table cc3 no inherit pp1
;
-- 0xff -> jsonb
CREATE FUNCTION testDecimal() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = 0xff
return x
$$
;
ALTER INDEX pg_toast.pg_toast_30888_index RENAME TO reind_con_toast_idx
;
do $$
declare r record; d ordered_texts;
begin
  for r in select * from sometable loop
    raise notice 'processing row %', r.id;
    d := row(r.a, r.b);
  end loop;
end$$
;
create or replace function func_part_attach() returns trigger
  language plpgsql as $$
  begin
    execute 'create table tab_part_attach_1 (a int)';
    execute 'alter table tab_part_attach attach partition tab_part_attach_1 for values in (1)';
    return null;
  end $$
;
create type stoplight as enum ('red', 'yellow', 'green')
;
ALTER FUNCTION bt_metap(text) PARALLEL RESTRICTED
;
CREATE FUNCTION hash_page_items(IN page bytea,
	OUT itemoffset int4,
	OUT ctid tid,
	OUT data int8)
RETURNS SETOF record
AS '$libdir/pageinspect', 'hash_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
create table mc3p3 partition of mc3p for values from (10, 10, 10) to (10, 10, 20)
;
ALTER TABLE alice.unpartitioned FORCE ROW LEVEL SECURITY
;
CREATE OR REPLACE FUNCTION distinct_func(a INT) RETURNS INT AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL UNSAFE
;
CREATE TABLE "public"."dropped_objects" (
    "object_type" "text",
    "schema_name" "text",
    "object_name" "text",
    "object_identity" "text",
    "address_names" "text"[],
    "address_args" "text"[],
    "is_temporary" boolean,
    "original" boolean,
    "normal" boolean
)
;
CREATE FUNCTION gbt_int8_union(internal, internal)
RETURNS gbtreekey16
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLESPACE tblspc1 LOCATION '/tmp/7jY_B3_coT/tempdir/tblspc1'
;
CREATE TABLE CIRCLE_TBL (f1 circle)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_sequence
;
-- but without, it can be:

create or replace function array_to_set(anyarray) returns setof record as $$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$$ language sql immutable
;
CREATE FOREIGN TABLE reindex_foreign (c1 int, c2 int)
  SERVER loopback2 OPTIONS (table_name 'reindex_local')
;
drop domain intdom1 cascade
;
ALTER FUNCTION foo.plpgsql_function_trigger_1()
  COST 10
;
-- boolean partitioning
create table boolpart (a bool) partition by list (a)
;
ALTER TABLE cmdata2 ALTER COLUMN f1 TYPE int USING f1::integer
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_init_privs
;
ALTER OPERATOR <% (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
ALTER TABLE ONLY "public"."gtest22b"
    ADD CONSTRAINT "gtest22b_pkey" PRIMARY KEY ("a", "b")
;
DROP FUNCTION functest_b_2
;
-- test stored update
CREATE TABLE gtest3 (a int, b int GENERATED ALWAYS AS (a * 3) STORED)
;
CREATE TABLE gtest1 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED)
;
alter table atacc1 add constraint atacc1_constr_b_valid check(test_b is not null)
;
CREATE TABLE "public"."mlparted1" (
    "b" integer NOT NULL,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
PARTITION BY RANGE ((("b" + 0)))
;
CREATE OPERATOR >> (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_right,
	COMMUTATOR = '<<',
	RESTRICT = positionsel,
	JOIN = positionjoinsel
)
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub5
;
ALTER TABLE hash_parted ATTACH PARTITION fail_part FOR VALUES WITH (MODULUS 8, REMAINDER 0)
;
-- index on system catalog
ALTER TABLE pg_namespace ADD UNIQUE USING INDEX pg_namespace_oid_index
;
create table tprt_1 partition of tprt for values from (1) to (501)
;
ALTER TABLE tbl DROP COLUMN c3
;
-- Test for proper cleanup after a failure in a cursor portal
-- that was created in an outer subtransaction
CREATE FUNCTION invert(x float8) RETURNS float8 LANGUAGE plpgsql AS
$$ begin return 1/x; end $$
;
create or replace function stricttest() returns void as $$
-- override the global
#print_strict_params off
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- too many rows
  select * from foo where f1 > p1 or f1::text = p3  into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
CREATE TABLE tab_full (a int)
;
create index ab_a2_b1_a_idx on ab_a2_b1 (a)
;
CREATE SCHEMA regress_tenant_schema AUTHORIZATION regress_tenant
;
CREATE VIEW foreign_data_wrapper_options AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_data_wrappers w
;
CALL ptest6c(1, null)
;
ALTER TABLE range_parted ATTACH PARTITION part_c_1_c_20 FOR VALUES FROM ('c', 1) TO ('c', 20)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13915 host=/tmp/H6QOc033E6 dbname=postgres' PUBLICATION tap_pub
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "fkpart5"."fk1" (
    "a" integer
)
PARTITION BY LIST ("a")
;
----------
[CREATE_PROG_OBJ] /* New stuff in 1.2 begins here */

CREATE FUNCTION pgstattuple(IN reloid regclass,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuplebyid'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION brin_page_type(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'brin_page_type'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM pg_depend
;
----------
[ALTER_TABLE] alter table cc3 no inherit pp1
;
----------
[CREATE_PROG_OBJ] -- 0xff -> jsonb
CREATE FUNCTION testDecimal() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = 0xff
return x
$$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30888_index RENAME TO reind_con_toast_idx
;
----------
[BLOCK] do $$
declare r record; d ordered_texts;
begin
  for r in select * from sometable loop
    raise notice 'processing row %', r.id;
    d := row(r.a, r.b);
  end loop;
end$$
;
----------
[CREATE_PROG_OBJ] create or replace function func_part_attach() returns trigger
  language plpgsql as $$
  begin
    execute 'create table tab_part_attach_1 (a int)';
    execute 'alter table tab_part_attach attach partition tab_part_attach_1 for values in (1)';
    return null;
  end $$
;
----------
[CREATE_TYPE] create type stoplight as enum ('red', 'yellow', 'green')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION bt_metap(text) PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hash_page_items(IN page bytea,
	OUT itemoffset int4,
	OUT ctid tid,
	OUT data int8)
RETURNS SETOF record
AS '$libdir/pageinspect', 'hash_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table mc3p3 partition of mc3p for values from (10, 10, 10) to (10, 10, 20)
;
----------
[ALTER_TABLE] ALTER TABLE alice.unpartitioned FORCE ROW LEVEL SECURITY
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION distinct_func(a INT) RETURNS INT AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL UNSAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."dropped_objects" (
    "object_type" "text",
    "schema_name" "text",
    "object_name" "text",
    "object_identity" "text",
    "address_names" "text"[],
    "address_args" "text"[],
    "is_temporary" boolean,
    "original" boolean,
    "normal" boolean
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int8_union(internal, internal)
RETURNS gbtreekey16
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc1 LOCATION '/tmp/7jY_B3_coT/tempdir/tblspc1'
;
----------
[CREATE_TABLE] CREATE TABLE CIRCLE_TBL (f1 circle)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_sequence
;
----------
[CREATE_PROG_OBJ] -- but without, it can be:

create or replace function array_to_set(anyarray) returns setof record as $$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$$ language sql immutable
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE reindex_foreign (c1 int, c2 int)
  SERVER loopback2 OPTIONS (table_name 'reindex_local')
;
----------
[DROP_TYPE] drop domain intdom1 cascade
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION foo.plpgsql_function_trigger_1()
  COST 10
;
----------
[CREATE_TABLE] -- boolean partitioning
create table boolpart (a bool) partition by list (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE cmdata2 ALTER COLUMN f1 TYPE int USING f1::integer
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_init_privs
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <% (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest22b"
    ADD CONSTRAINT "gtest22b_pkey" PRIMARY KEY ("a", "b")
;
----------
[DROP_PROG_OBJ] DROP FUNCTION functest_b_2
;
----------
[CREATE_TABLE] -- test stored update
CREATE TABLE gtest3 (a int, b int GENERATED ALWAYS AS (a * 3) STORED)
;
----------
[CREATE_TABLE] CREATE TABLE gtest1 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc1_constr_b_valid check(test_b is not null)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted1" (
    "b" integer NOT NULL,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
PARTITION BY RANGE ((("b" + 0)))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >> (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_right,
	COMMUTATOR = '<<',
	RESTRICT = positionsel,
	JOIN = positionjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub5
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE hash_parted ATTACH PARTITION fail_part FOR VALUES WITH (MODULUS 8, REMAINDER 0)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- index on system catalog
ALTER TABLE pg_namespace ADD UNIQUE USING INDEX pg_namespace_oid_index
;
----------
[CREATE_TABLE] create table tprt_1 partition of tprt for values from (1) to (501)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tbl DROP COLUMN c3
;
----------
[CREATE_PROG_OBJ] -- Test for proper cleanup after a failure in a cursor portal
-- that was created in an outer subtransaction
CREATE FUNCTION invert(x float8) RETURNS float8 LANGUAGE plpgsql AS
$$ begin return 1/x; end $$
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
-- override the global
#print_strict_params off
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- too many rows
  select * from foo where f1 > p1 or f1::text = p3  into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE tab_full (a int)
;
----------
[ADD_INDEX] create index ab_a2_b1_a_idx on ab_a2_b1 (a)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_tenant_schema AUTHORIZATION regress_tenant
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW foreign_data_wrapper_options AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_data_wrappers w
;
----------
[CALL_PROG_OBJ] CALL ptest6c(1, null)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE range_parted ATTACH PARTITION part_c_1_c_20 FOR VALUES FROM ('c', 1) TO ('c', 20)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13915 host=/tmp/H6QOc033E6 dbname=postgres' PUBLICATION tap_pub
;
