-- Make dummy I/O routines using the existing internal support for int4, text
CREATE FUNCTION int42_in(cstring)
   RETURNS int42
   AS 'int4in'
   LANGUAGE internal STRICT IMMUTABLE
;
-- lets do some naming tests
create table atacc1 (test int, test2 int, unique(test))
;
ANALYZE pagg_tab5
;
-- ERROR
TRUNCATE ft1
;
create function binary_coercible(oid, oid) returns bool as $$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b' and castcontext = 'i')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$$ language plpgsql strict stable
;
CREATE FUNCTION gbtreekey2_out(gbtreekey2)
RETURNS cstring
AS '$libdir/btree_gist', 'gbtreekey_out'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE test2ci (x text COLLATE case_insensitive)
;
CREATE AGGREGATE schema_two.newton
  (BASETYPE = int, SFUNC = schema_two.add, STYPE = int)
;
CREATE FUNCTION "public"."test_table_func_rec"() RETURNS SETOF "public"."found_test_tbl"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
	rec RECORD;
BEGIN
	FOR rec IN select * from found_test_tbl LOOP
		RETURN NEXT rec;
	END LOOP;
	RETURN;
END;$$
;
ALTER TABLE hash_parted ATTACH PARTITION fail_part FOR VALUES WITH (MODULUS 8, REMAINDER 8)
;
CREATE VIEW "public"."rtest_vview5" AS
 SELECT "a",
    "b",
    "public"."rtest_viewfunc1"("a") AS "refcount"
   FROM "public"."rtest_view1"
;
CREATE TABLE fail_part PARTITION OF list_parted FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
create trigger trigger_return_old
	before insert or delete or update on trigtest
	for each row execute procedure trigger_return_old()
;
CREATE TRIGGER merge_ard AFTER DELETE ON target FOR EACH ROW EXECUTE PROCEDURE merge_trigfunc ()
;
CREATE FUNCTION "mvtest_mvschema"."mvtest_drop_the_index"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  EXECUTE 'DROP INDEX IF EXISTS mvtest_mvschema.mvtest_drop_idx';
  RETURN true;
END;
$$
;
ALTER TABLE pg_temp_10.pg_temp_25428_2 ADD COLUMN newdata pg_temp_10.pg_temp_25428
;
create table s2.p2_2 partition of s2.p2 for values in (4, 5, 6)
;
ALTER DOMAIN split_domain_type_next.email_domain ADD CONSTRAINT email_length_chk CHECK (char_length(VALUE) > 3) NOT VALID
;
CREATE TABLE notnull_tbl1_child (a int, b int) INHERITS (notnull_tbl1)
;
CREATE DATABASE test_log_connections
;
CREATE TABLE "public"."prt4_n_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE TABLE test_tab1(a int)
;
CREATE FUNCTION update_checking(int) RETURNS bool LANGUAGE sql AS $$
     UPDATE accounts SET balance = balance + 1 WHERE accountid = 'checking'; SELECT true;$$
;
create database abc with template zyc
;
ALTER TABLE ONLY "public"."rule_and_refint_t2"
    ADD CONSTRAINT "rule_and_refint_t2_pkey" PRIMARY KEY ("id2a", "id2c")
;
CREATE FUNCTION wait_pid(int)
   RETURNS void
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT
;
DROP TABLE xpath_test
;
ALTER TABLE pg_toast.pg_toast_28312 RENAME TO reind_con_toast
;
-- Create a table with the int_custom type
CREATE TABLE test_table (
    id int,
    data int_custom
)
;
ALTER PUBLICATION testpub_forparted ADD TABLE testpub_parted
;
CREATE OPERATOR CLASS ltree_ops
    DEFAULT FOR TYPE ltree USING btree AS
        OPERATOR        1       < ,
        OPERATOR        2       <= ,
        OPERATOR        3       = ,
        OPERATOR        4       >= ,
        OPERATOR        5       > ,
        FUNCTION        1       ltree_cmp(ltree, ltree)
;
drop type t_rec
;
create function dfunc(a text DEFAULT 'Hello', b text DEFAULT 'World') returns text as $$
  select $1 || ', ' || $2;
$$ language sql
;
CREATE TABLE pagg_tab_m_p3 PARTITION OF pagg_tab_m FOR VALUES FROM (22, 22) TO (30, 30)
;
VACUUM horizons_tst
;
CREATE TABLE testpub_tbl2 (id serial primary key, data text)
;
CREATE TABLE ffk1 PARTITION OF ffk FOR VALUES IN (1)
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'xml')
;
CREATE TABLE audit_tbls.schema_one_table_two(the_value text)
;
-- Create the operator class
CREATE OPERATOR CLASS gist_time_ops
DEFAULT FOR TYPE time USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_time_consistent (internal, time, int2, oid, internal),
	FUNCTION	2	gbt_time_union (internal, internal),
	FUNCTION	3	gbt_time_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_time_penalty (internal, internal, internal),
	FUNCTION	6	gbt_time_picksplit (internal, internal),
	FUNCTION	7	gbt_time_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=23259 host=/tmp/x3EXkWV6Fo dbname=postgres' PUBLICATION pub_all
;
ALTER TABLE pg_temp_9.pg_temp_25621_2 ADD COLUMN newdata pg_temp_9.pg_temp_25621
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_shseclabel
;
CREATE TABLE partrem (a int, b text) PARTITION BY LIST(a)
;
ALTER TABLE ONLY "public"."rule_and_refint_t3"
    ADD CONSTRAINT "rule_and_refint_t3_id3a_id3c_fkey" FOREIGN KEY ("id3a", "id3c") REFERENCES "public"."rule_and_refint_t2"("id2a", "id2c")
;
ANALYZE pg_temp_13.pg_temp_26471_2
;
CREATE INDEX ON INTERVAL_TBL_OF USING btree (f1)
;
CREATE TABLE tt3 (y numeric(8,2), x int)
;
CREATE TABLE smalltbl AS SELECT i AS id, 't'::name AS t FROM generate_series(1,20) i
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- Make dummy I/O routines using the existing internal support for int4, text
CREATE FUNCTION int42_in(cstring)
   RETURNS int42
   AS 'int4in'
   LANGUAGE internal STRICT IMMUTABLE
;
----------
[CREATE_TABLE] -- lets do some naming tests
create table atacc1 (test int, test2 int, unique(test))
;
----------
[ADMIN_TABLE] ANALYZE pagg_tab5
;
----------
[TRUNCATE_TABLE] -- ERROR
TRUNCATE ft1
;
----------
[CREATE_PROG_OBJ] create function binary_coercible(oid, oid) returns bool as $$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b' and castcontext = 'i')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$$ language plpgsql strict stable
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey2_out(gbtreekey2)
RETURNS cstring
AS '$libdir/btree_gist', 'gbtreekey_out'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test2ci (x text COLLATE case_insensitive)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE schema_two.newton
  (BASETYPE = int, SFUNC = schema_two.add, STYPE = int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_table_func_rec"() RETURNS SETOF "public"."found_test_tbl"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
	rec RECORD;
BEGIN
	FOR rec IN select * from found_test_tbl LOOP
		RETURN NEXT rec;
	END LOOP;
	RETURN;
END;$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE hash_parted ATTACH PARTITION fail_part FOR VALUES WITH (MODULUS 8, REMAINDER 8)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_vview5" AS
 SELECT "a",
    "b",
    "public"."rtest_viewfunc1"("a") AS "refcount"
   FROM "public"."rtest_view1"
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF list_parted FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trigger_return_old
	before insert or delete or update on trigtest
	for each row execute procedure trigger_return_old()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER merge_ard AFTER DELETE ON target FOR EACH ROW EXECUTE PROCEDURE merge_trigfunc ()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "mvtest_mvschema"."mvtest_drop_the_index"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  EXECUTE 'DROP INDEX IF EXISTS mvtest_mvschema.mvtest_drop_idx';
  RETURN true;
END;
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_25428_2 ADD COLUMN newdata pg_temp_10.pg_temp_25428
;
----------
[CREATE_TABLE] create table s2.p2_2 partition of s2.p2 for values in (4, 5, 6)
;
----------
[ALTER_TYPE] ALTER DOMAIN split_domain_type_next.email_domain ADD CONSTRAINT email_length_chk CHECK (char_length(VALUE) > 3) NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1_child (a int, b int) INHERITS (notnull_tbl1)
;
----------
[CREATE_CATALOG] CREATE DATABASE test_log_connections
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt4_n_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE test_tab1(a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION update_checking(int) RETURNS bool LANGUAGE sql AS $$
     UPDATE accounts SET balance = balance + 1 WHERE accountid = 'checking'; SELECT true;$$
;
----------
[CREATE_CATALOG] create database abc with template zyc
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."rule_and_refint_t2"
    ADD CONSTRAINT "rule_and_refint_t2_pkey" PRIMARY KEY ("id2a", "id2c")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION wait_pid(int)
   RETURNS void
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT
;
----------
[DROP_TABLE] DROP TABLE xpath_test
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28312 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- Create a table with the int_custom type
CREATE TABLE test_table (
    id int,
    data int_custom
)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_forparted ADD TABLE testpub_parted
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS ltree_ops
    DEFAULT FOR TYPE ltree USING btree AS
        OPERATOR        1       < ,
        OPERATOR        2       <= ,
        OPERATOR        3       = ,
        OPERATOR        4       >= ,
        OPERATOR        5       > ,
        FUNCTION        1       ltree_cmp(ltree, ltree)
;
----------
[DROP_TYPE] drop type t_rec
;
----------
[CREATE_PROG_OBJ] create function dfunc(a text DEFAULT 'Hello', b text DEFAULT 'World') returns text as $$
  select $1 || ', ' || $2;
$$ language sql
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_m_p3 PARTITION OF pagg_tab_m FOR VALUES FROM (22, 22) TO (30, 30)
;
----------
[ADMIN_TABLE] VACUUM horizons_tst
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl2 (id serial primary key, data text)
;
----------
[CREATE_TABLE] CREATE TABLE ffk1 PARTITION OF ffk FOR VALUES IN (1)
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'xml')
;
----------
[CREATE_TABLE] CREATE TABLE audit_tbls.schema_one_table_two(the_value text)
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_time_ops
DEFAULT FOR TYPE time USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_time_consistent (internal, time, int2, oid, internal),
	FUNCTION	2	gbt_time_union (internal, internal),
	FUNCTION	3	gbt_time_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_time_penalty (internal, internal, internal),
	FUNCTION	6	gbt_time_picksplit (internal, internal),
	FUNCTION	7	gbt_time_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn,
	RIGHTARG = isbn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=23259 host=/tmp/x3EXkWV6Fo dbname=postgres' PUBLICATION pub_all
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_25621_2 ADD COLUMN newdata pg_temp_9.pg_temp_25621
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_shseclabel
;
----------
[CREATE_TABLE] CREATE TABLE partrem (a int, b text) PARTITION BY LIST(a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."rule_and_refint_t3"
    ADD CONSTRAINT "rule_and_refint_t3_id3a_id3c_fkey" FOREIGN KEY ("id3a", "id3c") REFERENCES "public"."rule_and_refint_t2"("id2a", "id2c")
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26471_2
;
----------
[ADD_INDEX] CREATE INDEX ON INTERVAL_TBL_OF USING btree (f1)
;
----------
[CREATE_TABLE] CREATE TABLE tt3 (y numeric(8,2), x int)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE smalltbl AS SELECT i AS id, 't'::name AS t FROM generate_series(1,20) i
;
