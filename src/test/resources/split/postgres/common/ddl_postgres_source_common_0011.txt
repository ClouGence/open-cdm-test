VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_range
;
CREATE SCHEMA mvtest_mvschema
;
CREATE VIEW "public"."classv" AS
 SELECT "oid",
    "relname",
    "relnamespace",
    "reltype",
    "reloftype",
    "relowner",
    "relam",
    "relfilenode",
    "reltablespace",
    "relpages",
    "reltuples",
    "relallvisible",
    "reltoastrelid",
    "relhasindex",
    "relisshared",
    "relpersistence",
    "relkind",
    "relnatts",
    "relchecks",
    "relhasrules",
    "relhastriggers",
    "relhassubclass",
    "relrowsecurity",
    "relforcerowsecurity",
    "relispopulated",
    "relreplident",
    "relispartition",
    "relrewrite",
    "relfrozenxid",
    "relminmxid",
    "relacl",
    "reloptions",
    "relpartbound"
   FROM "pg_class"
;
ALTER TABLE ONLY "public"."mlparted_def" DROP COLUMN "........pg.dropped.7........"
;
ALTER INDEX pg_toast.pg_toast_30681_index RENAME TO reind_con_toast_idx
;
CREATE OPERATOR ? (
        LEFTARG = _lquery,
	RIGHTARG = ltree,
	PROCEDURE = lt_q_rregex,
	COMMUTATOR = '?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
-- ok

DROP TRIGGER rw_view1_ins_trig ON rw_view1
;
alter table p1 add constraint inh_check_constraint2 check (f1 < 10)
;
CREATE FUNCTION test_assign_booltrue(x bool, y bool) RETURNS booltrue AS $$
declare v booltrue := x;
begin
v := y;
return v;
end
$$ LANGUAGE plpgsql
;
CREATE VIEW pg_replication_slots AS
    SELECT
            L.slot_name,
            L.plugin,
            L.slot_type,
            L.datoid,
            D.datname AS database,
            L.temporary,
            L.active,
            L.active_pid,
            L.xmin,
            L.catalog_xmin,
            L.restart_lsn,
            L.confirmed_flush_lsn,
            L.wal_status,
            L.safe_wal_size
    FROM pg_get_replication_slots() AS L
            LEFT JOIN pg_database D ON (L.datoid = D.oid)
;
CREATE SCHEMA split_conversion_probe
;
CREATE INDEX ptif_test_index ON ONLY ptif_test (a)
;
CREATE AGGREGATE alt_agg2 (
  sfunc1 = int4mi, basetype = int4, stype1 = int4, initcond = 0
)
;
CREATE INDEX "test_storage_idx" ON "public"."test_storage" USING "btree" ("b", "a")
;
-- Generate statistics on table with data
ANALYZE stats_import.test
;
create table journal (d timestamp) with (fillfactor = 10)
;
create trigger tg_pslot_bu before update
    on PSlot for each row execute procedure tg_pslot_bu()
;
create or replace function retc(int) returns two_int8s language plpgsql as
$$ begin return row($1::int8, 1::int8, 42); end $$
;
DROP TABLE IF EXISTS subxids
;
CREATE FUNCTION f(int) RETURNS int AS $$ SELECT $1 + 2 $$ LANGUAGE sql
;
CREATE INDEX sro_brin ON sro_tab USING brin ((sro_ifun(a) + sro_ifun(0)))
;
CREATE EXTENSION test_ext_cor
;
CREATE TABLE import_source.t5 (c1 int, c2 text collate "C", "Col" "Colors")
;
vacuum pgbench_tellers
;
alter table trigpart attach partition trigpart3 FOR VALUES FROM (2000) to (3000)
;
DROP SCHEMA IF EXISTS split_domain_type_next CASCADE
;
ALTER EXTENSION test_pg_dump DROP FOREIGN TABLE ft1
;
create table mlparted12 partition of mlparted1 for values from (5) to (10)
;
DROP OPERATOR ###### (int4, NONE)
;
CREATE FUNCTION gin_compare_prefix_text(text, text, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_tbl1
;
DROP FUNCTION log_rewrite
;
-- nondeterministic collations
-- (not supported with libc provider)

CREATE COLLATION ctest_det (locale = 'en_US', deterministic = true)
;
CREATE TABLE "public"."part_p2" (
    "a" integer,
    "b" integer
)
PARTITION BY RANGE ("a")
;
ALTER INDEX pg_toast.pg_toast_27531_index RENAME TO reind_con_toast_idx
;
CREATE EXTENSION refint
;
ALTER INDEX "public"."parted_self_fk_pkey" ATTACH PARTITION "public"."part3_self_fk_pkey"
;
CREATE FUNCTION issn13_in(cstring)
	RETURNS issn13
	AS '$libdir/isn', 'issn_in'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_30888 RENAME TO reind_con_toast
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_aggregate
;
ANALYZE pg_temp_12.pg_temp_26182
;
CREATE UNIQUE INDEX "emp1_expr_idx" ON "public"."emp1" USING "btree" ((("id" * "id")))
;
alter table rewriteme alter column bar type timestamptz
;
CREATE FUNCTION functest_B_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 0'
;
alter table anothertab
  add exclude using btree (f4 with =) where (f5 > 0)
;
ALTER INDEX pg_toast.pg_toast_30784_index RENAME TO reind_con_toast_idx
;
CREATE TABLE base_tbl(a int, b text, c float)
;
CREATE TEMP TABLE pg_temp_18.pg_temp_26711_2 (tid pg_catalog.tid)
;
alter table idxpart1 drop column col1, drop column col2, drop column col3, drop column col4
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_range
;
----------
[CREATE_SCHEMA] CREATE SCHEMA mvtest_mvschema
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."classv" AS
 SELECT "oid",
    "relname",
    "relnamespace",
    "reltype",
    "reloftype",
    "relowner",
    "relam",
    "relfilenode",
    "reltablespace",
    "relpages",
    "reltuples",
    "relallvisible",
    "reltoastrelid",
    "relhasindex",
    "relisshared",
    "relpersistence",
    "relkind",
    "relnatts",
    "relchecks",
    "relhasrules",
    "relhastriggers",
    "relhassubclass",
    "relrowsecurity",
    "relforcerowsecurity",
    "relispopulated",
    "relreplident",
    "relispartition",
    "relrewrite",
    "relfrozenxid",
    "relminmxid",
    "relacl",
    "reloptions",
    "relpartbound"
   FROM "pg_class"
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def" DROP COLUMN "........pg.dropped.7........"
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30681_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ? (
        LEFTARG = _lquery,
	RIGHTARG = ltree,
	PROCEDURE = lt_q_rregex,
	COMMUTATOR = '?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[DROP_TRIGGER] -- ok

DROP TRIGGER rw_view1_ins_trig ON rw_view1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1 add constraint inh_check_constraint2 check (f1 < 10)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_assign_booltrue(x bool, y bool) RETURNS booltrue AS $$
declare v booltrue := x;
begin
v := y;
return v;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_replication_slots AS
    SELECT
            L.slot_name,
            L.plugin,
            L.slot_type,
            L.datoid,
            D.datname AS database,
            L.temporary,
            L.active,
            L.active_pid,
            L.xmin,
            L.catalog_xmin,
            L.restart_lsn,
            L.confirmed_flush_lsn,
            L.wal_status,
            L.safe_wal_size
    FROM pg_get_replication_slots() AS L
            LEFT JOIN pg_database D ON (L.datoid = D.oid)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_conversion_probe
;
----------
[ADD_INDEX] CREATE INDEX ptif_test_index ON ONLY ptif_test (a)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE alt_agg2 (
  sfunc1 = int4mi, basetype = int4, stype1 = int4, initcond = 0
)
;
----------
[ADD_INDEX] CREATE INDEX "test_storage_idx" ON "public"."test_storage" USING "btree" ("b", "a")
;
----------
[ADMIN_TABLE] -- Generate statistics on table with data
ANALYZE stats_import.test
;
----------
[CREATE_TABLE] create table journal (d timestamp) with (fillfactor = 10)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_pslot_bu before update
    on PSlot for each row execute procedure tg_pslot_bu()
;
----------
[CREATE_PROG_OBJ] create or replace function retc(int) returns two_int8s language plpgsql as
$$ begin return row($1::int8, 1::int8, 42); end $$
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS subxids
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f(int) RETURNS int AS $$ SELECT $1 + 2 $$ LANGUAGE sql
;
----------
[ADD_INDEX] CREATE INDEX sro_brin ON sro_tab USING brin ((sro_ifun(a) + sro_ifun(0)))
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext_cor
;
----------
[CREATE_TABLE] CREATE TABLE import_source.t5 (c1 int, c2 text collate "C", "Col" "Colors")
;
----------
[ADMIN_TABLE] vacuum pgbench_tellers
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table trigpart attach partition trigpart3 FOR VALUES FROM (2000) to (3000)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_domain_type_next CASCADE
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP FOREIGN TABLE ft1
;
----------
[CREATE_TABLE] create table mlparted12 partition of mlparted1 for values from (5) to (10)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ###### (int4, NONE)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_text(text, text, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_tbl1
;
----------
[DROP_PROG_OBJ] DROP FUNCTION log_rewrite
;
----------
[CREATE_POLICY] -- nondeterministic collations
-- (not supported with libc provider)

CREATE COLLATION ctest_det (locale = 'en_US', deterministic = true)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part_p2" (
    "a" integer,
    "b" integer
)
PARTITION BY RANGE ("a")
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27531_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_LIBRARY] CREATE EXTENSION refint
;
----------
[ALTER_INDEX] ALTER INDEX "public"."parted_self_fk_pkey" ATTACH PARTITION "public"."part3_self_fk_pkey"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION issn13_in(cstring)
	RETURNS issn13
	AS '$libdir/isn', 'issn_in'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30888 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_aggregate
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26182
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "emp1_expr_idx" ON "public"."emp1" USING "btree" ((("id" * "id")))
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table rewriteme alter column bar type timestamptz
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_B_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 0'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table anothertab
  add exclude using btree (f4 with =) where (f5 > 0)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30784_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl(a int, b text, c float)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_18.pg_temp_26711_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart1 drop column col1, drop column col2, drop column col3, drop column col4
;
