CREATE TABLE table_with_pkey(id serial primary key, data int)
;
drop table test8
;
create function trigtest() returns trigger as $$
begin
	raise notice '% % % %', TG_TABLE_NAME, TG_OP, TG_WHEN, TG_LEVEL;
	return new;
end;$$ language plpgsql
;
ANALYZE public.part_p1
;
ANALYZE pg_temp_12.pg_temp_26295
;
CREATE OR REPLACE VIEW mysecview9 WITH (security_invoker=false, security_barrier=true)
       AS SELECT * FROM tbl1 WHERE a <> 256
;
ALTER TABLE pg_toast.pg_toast_30889 RENAME TO reind_con_toast
;
-- Verify that initial constraint creation and cloning behave correctly
CREATE SCHEMA fkpart5
;
ALTER FUNCTION gbt_bytea_compress(internal) PARALLEL SAFE
;
DROP EVENT TRIGGER split_event_helper_table_rewrite
;
DROP VIEW at_v1
;
/* contrib/citext/citext--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
--  PostgreSQL code for CITEXT.
--
-- Most I/O functions, and a few others, piggyback on the "text" type
-- functions via the implicit cast to text.
--

--
-- Shell type to keep things a bit quieter.
--

CREATE TYPE citext
;
CREATE OPERATOR CLASS upc_ops DEFAULT
	FOR TYPE upc USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btupccmp(upc, upc)
;
CREATE TABLE "public"."prt3_n" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
-- Now OK:
DROP SEQUENCE myseq2
;
CREATE FOREIGN TABLE ft1 (
        c1 integer OPTIONS ("param 1" 'val1') NOT NULL,
        c2 text OPTIONS (param2 'val2', param3 'val3') CHECK (c2 <> ''),
        c3 date,
        CHECK (c3 BETWEEN '1994-01-01'::date AND '1994-01-31'::date)
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
ALTER TABLE ONLY "fkpart5"."pk11"
    ADD CONSTRAINT "pk11_pkey" PRIMARY KEY ("a")
;
DROP DOMAIN priv_testdomain3a
;
DROP TABLE wq_target, wq_source
;
drop table parent_noinh_convalid, child_noinh_convalid
;
ANALYZE pg_temp_24.pg_temp_27148
;
DROP FUNCTION explain_mask_costs(text, bool, bool, bool, bool)
;
-- fails, col5 defaults to null
alter table defaulttest alter column col5 drop default
;
CREATE FUNCTION "public"."float8mi_int"(double precision, double precision) RETURNS integer
    LANGUAGE "sql"
    AS $_$ SELECT CAST($1 - $2 AS INT); $_$
;
-- view

DROP VIEW test_view_exists
;
CREATE FUNCTION equipment_named_ambiguous_2b(hobby text)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where equipment_r.hobby = hobby'
   LANGUAGE SQL
;
CREATE TABLE split_alter_table.part_eu (region text NOT NULL, amount integer)
;
drop table inh_fk_1, inh_fk_2, inh_fk_2_child
;
-- fail, can't infer element type

drop function f1(x anyarray)
;
create sequence rtest_seq
;
CREATE RULE joinview_u AS ON UPDATE TO joinview DO INSTEAD
  UPDATE foo SET f1 = new.f1, f3 = new.f3
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING foo.*, other
;
ALTER TABLE constraint_rename_cache
  RENAME CONSTRAINT constraint_rename_cache_pkey TO constraint_rename_pkey_new
;
CREATE FUNCTION isnlt(issn, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION test_bytea_to_text(bytea) RETURNS text
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
ALTER TABLE ONLY "public"."covidxpart" ATTACH PARTITION "public"."covidxpart4" FOR VALUES IN (4)
;
DROP TABLE inhts
;
--
-- Track the total number of rows retrieved or affected by the utility
-- commands of COPY, FETCH, CREATE TABLE AS, CREATE MATERIALIZED VIEW,
-- REFRESH MATERIALIZED VIEW and SELECT INTO
--
CREATE TABLE pgss_ctas AS SELECT a, 'ctas' b FROM generate_series(1, 10) a
;
CREATE TEXT SEARCH CONFIGURATION russian
	(PARSER = default)
;
alter table idxpart drop column drop_2
;
CREATE VIEW datatype_view AS
  SELECT * FROM datatype_table
;
DROP TRIGGER IF EXISTS accounts_update_transition ON split_trigger_probe.accounts RESTRICT
;
-- Constraint, no recursion
ALTER TABLE ONLY grandchild ADD CONSTRAINT a_pos CHECK (a > 0)
;
-- JSON_TABLE: basic functionality
CREATE DOMAIN jsonb_test_domain AS text CHECK (value <> 'foo')
;
-- Test schema-qualified table name in RETURNING list
CREATE TABLE public.tt(a int, b int)
;
--
-- Also check multiassignment deparsing.
--
create table rule_t1(f1 int, f2 int)
;
DROP SCHEMA IF EXISTS split_conversion_probe_next CASCADE
;
alter table cc1 alter column a2 drop not null
;
do $$
begin
  <<outerwhile>>
  while 1 > 0 loop
    while 1 > 0 loop
      exit outerwhile;
      raise notice 'should not get here';
    end loop;
    raise notice 'should not get here, either';
  end loop;
  raise notice 'should get here';
end$$
;
CREATE TEXT SEARCH DICTIONARY "public"."hunspell" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'ispell_sample', afffile = 'hunspell_sample' )
;
CREATE VIEW rw_view2 AS SELECT aa AS aaa, bb AS bbb FROM rw_view1 WHERE aa<10
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE table_with_pkey(id serial primary key, data int)
;
----------
[DROP_TABLE] drop table test8
;
----------
[CREATE_PROG_OBJ] create function trigtest() returns trigger as $$
begin
	raise notice '% % % %', TG_TABLE_NAME, TG_OP, TG_WHEN, TG_LEVEL;
	return new;
end;$$ language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE public.part_p1
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26295
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW mysecview9 WITH (security_invoker=false, security_barrier=true)
       AS SELECT * FROM tbl1 WHERE a <> 256
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30889 RENAME TO reind_con_toast
;
----------
[CREATE_SCHEMA] -- Verify that initial constraint creation and cloning behave correctly
CREATE SCHEMA fkpart5
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bytea_compress(internal) PARALLEL SAFE
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER split_event_helper_table_rewrite
;
----------
[DROP_VIEW] DROP VIEW at_v1
;
----------
[CREATE_TYPE] /* contrib/citext/citext--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
--  PostgreSQL code for CITEXT.
--
-- Most I/O functions, and a few others, piggyback on the "text" type
-- functions via the implicit cast to text.
--

--
-- Shell type to keep things a bit quieter.
--

CREATE TYPE citext
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS upc_ops DEFAULT
	FOR TYPE upc USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btupccmp(upc, upc)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt3_n" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("c")
;
----------
[DROP_SEQUENCE] -- Now OK:
DROP SEQUENCE myseq2
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft1 (
        c1 integer OPTIONS ("param 1" 'val1') NOT NULL,
        c2 text OPTIONS (param2 'val2', param3 'val3') CHECK (c2 <> ''),
        c3 date,
        CHECK (c3 BETWEEN '1994-01-01'::date AND '1994-01-31'::date)
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart5"."pk11"
    ADD CONSTRAINT "pk11_pkey" PRIMARY KEY ("a")
;
----------
[DROP_TYPE] DROP DOMAIN priv_testdomain3a
;
----------
[DROP_TABLE] DROP TABLE wq_target, wq_source
;
----------
[DROP_TABLE] drop table parent_noinh_convalid, child_noinh_convalid
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_27148
;
----------
[DROP_PROG_OBJ] DROP FUNCTION explain_mask_costs(text, bool, bool, bool, bool)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails, col5 defaults to null
alter table defaulttest alter column col5 drop default
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."float8mi_int"(double precision, double precision) RETURNS integer
    LANGUAGE "sql"
    AS $_$ SELECT CAST($1 - $2 AS INT); $_$
;
----------
[DROP_VIEW] -- view

DROP VIEW test_view_exists
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION equipment_named_ambiguous_2b(hobby text)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where equipment_r.hobby = hobby'
   LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE split_alter_table.part_eu (region text NOT NULL, amount integer)
;
----------
[DROP_TABLE] drop table inh_fk_1, inh_fk_2, inh_fk_2_child
;
----------
[DROP_PROG_OBJ] -- fail, can't infer element type

drop function f1(x anyarray)
;
----------
[CREATE_SEQUENCE] create sequence rtest_seq
;
----------
[CREATE_POLICY] CREATE RULE joinview_u AS ON UPDATE TO joinview DO INSTEAD
  UPDATE foo SET f1 = new.f1, f3 = new.f3
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING foo.*, other
;
----------
[RENAME_CONSTRAINT] ALTER TABLE constraint_rename_cache
  RENAME CONSTRAINT constraint_rename_cache_pkey TO constraint_rename_pkey_new
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(issn, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_bytea_to_text(bytea) RETURNS text
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."covidxpart" ATTACH PARTITION "public"."covidxpart4" FOR VALUES IN (4)
;
----------
[DROP_TABLE] DROP TABLE inhts
;
----------
[CREATE_TABLE|SELECT] --
-- Track the total number of rows retrieved or affected by the utility
-- commands of COPY, FETCH, CREATE TABLE AS, CREATE MATERIALIZED VIEW,
-- REFRESH MATERIALIZED VIEW and SELECT INTO
--
CREATE TABLE pgss_ctas AS SELECT a, 'ctas' b FROM generate_series(1, 10) a
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION russian
	(PARSER = default)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart drop column drop_2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW datatype_view AS
  SELECT * FROM datatype_table
;
----------
[DROP_TRIGGER] DROP TRIGGER IF EXISTS accounts_update_transition ON split_trigger_probe.accounts RESTRICT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Constraint, no recursion
ALTER TABLE ONLY grandchild ADD CONSTRAINT a_pos CHECK (a > 0)
;
----------
[CREATE_TYPE] -- JSON_TABLE: basic functionality
CREATE DOMAIN jsonb_test_domain AS text CHECK (value <> 'foo')
;
----------
[CREATE_TABLE] -- Test schema-qualified table name in RETURNING list
CREATE TABLE public.tt(a int, b int)
;
----------
[CREATE_TABLE] --
-- Also check multiassignment deparsing.
--
create table rule_t1(f1 int, f2 int)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_conversion_probe_next CASCADE
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table cc1 alter column a2 drop not null
;
----------
[BLOCK] do $$
begin
  <<outerwhile>>
  while 1 > 0 loop
    while 1 > 0 loop
      exit outerwhile;
      raise notice 'should not get here';
    end loop;
    raise notice 'should not get here, either';
  end loop;
  raise notice 'should get here';
end$$
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."hunspell" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'ispell_sample', afffile = 'hunspell_sample' )
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS SELECT aa AS aaa, bb AS bbb FROM rw_view1 WHERE aa<10
;
