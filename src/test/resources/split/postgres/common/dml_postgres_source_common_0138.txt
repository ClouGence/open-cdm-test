insert into rtest_t2 values (1, 21)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."tt6"'::pg_catalog.regclass
  AND attname IN ('x', 'y')
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i'),
             ('........pg.dropped.2........', 4, 'i'),
             ('........pg.dropped.3........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."part_7_a_null"'::pg_catalog.regclass
  AND attname = v.dname
;
INSERT INTO public.tht VALUES ('9', 219)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13376', relminmxid = '3'
WHERE oid = '"public"."hub"'::pg_catalog.regclass
;
insert into hs3 values (114)
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"regress_indexing"."pk3"'::pg_catalog.regclass AND
conname IN ('pk_a_not_null')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9023', relminmxid = '1'
WHERE oid = '"public"."rtest_comp"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17992', relminmxid = '3'
WHERE oid = '"public"."covidxpart4"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '774', relminmxid = '1'
WHERE oid = '"public"."varchar_tbl"'::pg_catalog.regclass
;
INSERT INTO num_typemod_test VALUES (123456789, 123456, 123.456, 0.123456, 0.000123456)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."money_data"'::pg_catalog.regclass
;
INSERT INTO num_exp_sub VALUES (9,0,'-24926804.045047420')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3966', relminmxid = '1'
WHERE oid = '"public"."b"'::pg_catalog.regclass
;
insert into pitable values(array[42])
;
INSERT INTO INSERT_TBL SELECT * FROM tmp WHERE yd = 'try again'
;
INSERT INTO num_exp_log10 VALUES (9,'7.39666659961986567059')
;
INSERT INTO public.tht VALUES ('5', 685)
;
INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_ts_dict
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10517', relminmxid = '1'
WHERE oid = '"pubme"."t0"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3599', relminmxid = '1'
WHERE oid = '"public"."tableam_tbl_heap2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13956', relminmxid = '3'
WHERE oid = '"public"."iface"'::pg_catalog.regclass
;
UPDATE clustertest SET key = 80 WHERE key = 70
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7360', relminmxid = '1'
WHERE oid = '"public"."mvtest_t"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."log_table"'::pg_catalog.regclass
;
INSERT INTO will_get_vacuumed VALUES (1, 'initial test row')
;
INSERT INTO public.ths_p1 VALUES (2, 542)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16613', relminmxid = '3'
WHERE oid = '38434'
;
-- fail - with REPLICA IDENTITY FULL no column list is allowed
UPDATE rf_tbl_abcd_pk SET a = 1
;
INSERT INTO foo AS bar DEFAULT VALUES RETURNING bar.f3
;
-- ON CONFLICT using system attributes in RETURNING, testing both the
-- inserting and updating paths. See bug report at:
-- https://www.postgresql.org/message-id/73436355-6432-49B1-92ED-1FE4F7E7E100%40finefun.com.au
INSERT INTO upsert_test VALUES (2, 'Beeble') ON CONFLICT(a)
  DO UPDATE SET (b, a) = (SELECT b || ', Excluded', a from upsert_test i WHERE i.a = excluded.a)
  RETURNING tableoid::regclass, xmin = pg_current_xact_id()::xid AS xmin_correct, xmax = 0 AS xmax_correct
;
INSERT INTO LINE_TBL VALUES ('[-1e6,2e2,3e5, -4e1]')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3022', relminmxid = '1'
WHERE oid = '"public"."real_city"'::pg_catalog.regclass
;
UPDATE errtst_parent SET data = data + 1 WHERE partid = 20
;
INSERT INTO public.tplain VALUES ('8', 98)
;
update some_tab set a = a + 1 where false returning b, a
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12727', relminmxid = '1'
WHERE oid = '"public"."mcv_lists_arrays"'::pg_catalog.regclass
;
-- For binary upgrade, set pg_largeobject relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = 2613
;
UPDATE "S 1"."T 1" r1 SET c2 = (r1.c2 + 500), c3 = (r1.c3 || '_update9'::text), c7 = 'ft2       '::character(10) FROM "S 1"."T 1" r2 WHERE ((r1.c2 = r2."C 1")) AND (((r2."C 1" % 10) = 9))
;
-- update both at once
UPDATE  temporal_rng
SET     id = '[21,22)',
        valid_at = '[2018-01-02,2018-02-03)'
WHERE   id = '[11,12)'
AND     valid_at @> '2020-01-15'::date
;
INSERT INTO public.tht VALUES ('5', 655)
;
INSERT INTO b(aa) VALUES('bbbbb')
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pht2_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO brintest_2 VALUES (numrange(0, 2^1000::numeric))
;
UPDATE gtest3 SET a = 22 WHERE a = 2
;
INSERT INTO public.tplain VALUES ('0', 340)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6338', relminmxid = '1'
WHERE oid = '"public"."bt_name_heap"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest22b"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pht2_p3"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('5', 915)
;
------------------------------------------------------------------------------------------
[INSERT] insert into rtest_t2 values (1, 21)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."tt6"'::pg_catalog.regclass
  AND attname IN ('x', 'y')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i'),
             ('........pg.dropped.2........', 4, 'i'),
             ('........pg.dropped.3........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."part_7_a_null"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 219)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13376', relminmxid = '3'
WHERE oid = '"public"."hub"'::pg_catalog.regclass
;
----------
[INSERT] insert into hs3 values (114)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"regress_indexing"."pk3"'::pg_catalog.regclass AND
conname IN ('pk_a_not_null')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9023', relminmxid = '1'
WHERE oid = '"public"."rtest_comp"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17992', relminmxid = '3'
WHERE oid = '"public"."covidxpart4"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '774', relminmxid = '1'
WHERE oid = '"public"."varchar_tbl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_typemod_test VALUES (123456789, 123456, 123.456, 0.123456, 0.000123456)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."money_data"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (9,0,'-24926804.045047420')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3966', relminmxid = '1'
WHERE oid = '"public"."b"'::pg_catalog.regclass
;
----------
[INSERT] insert into pitable values(array[42])
;
----------
[INSERT|SELECT] INSERT INTO INSERT_TBL SELECT * FROM tmp WHERE yd = 'try again'
;
----------
[INSERT] INSERT INTO num_exp_log10 VALUES (9,'7.39666659961986567059')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 685)
;
----------
[INSERT|SELECT] INSERT INTO pg_depend SELECT 0,0,0, tableoid,oid,0, 'p'  FROM pg_ts_dict
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10517', relminmxid = '1'
WHERE oid = '"pubme"."t0"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3599', relminmxid = '1'
WHERE oid = '"public"."tableam_tbl_heap2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13956', relminmxid = '3'
WHERE oid = '"public"."iface"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE clustertest SET key = 80 WHERE key = 70
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7360', relminmxid = '1'
WHERE oid = '"public"."mvtest_t"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."log_table"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO will_get_vacuumed VALUES (1, 'initial test row')
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 542)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16613', relminmxid = '3'
WHERE oid = '38434'
;
----------
[UPDATE] -- fail - with REPLICA IDENTITY FULL no column list is allowed
UPDATE rf_tbl_abcd_pk SET a = 1
;
----------
[INSERT] INSERT INTO foo AS bar DEFAULT VALUES RETURNING bar.f3
;
----------
[MERGE|SELECT] -- ON CONFLICT using system attributes in RETURNING, testing both the
-- inserting and updating paths. See bug report at:
-- https://www.postgresql.org/message-id/73436355-6432-49B1-92ED-1FE4F7E7E100%40finefun.com.au
INSERT INTO upsert_test VALUES (2, 'Beeble') ON CONFLICT(a)
  DO UPDATE SET (b, a) = (SELECT b || ', Excluded', a from upsert_test i WHERE i.a = excluded.a)
  RETURNING tableoid::regclass, xmin = pg_current_xact_id()::xid AS xmin_correct, xmax = 0 AS xmax_correct
;
----------
[INSERT] INSERT INTO LINE_TBL VALUES ('[-1e6,2e2,3e5, -4e1]')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3022', relminmxid = '1'
WHERE oid = '"public"."real_city"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE errtst_parent SET data = data + 1 WHERE partid = 20
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 98)
;
----------
[UPDATE] update some_tab set a = a + 1 where false returning b, a
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '12727', relminmxid = '1'
WHERE oid = '"public"."mcv_lists_arrays"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set pg_largeobject relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = 2613
;
----------
[UPDATE] UPDATE "S 1"."T 1" r1 SET c2 = (r1.c2 + 500), c3 = (r1.c3 || '_update9'::text), c7 = 'ft2       '::character(10) FROM "S 1"."T 1" r2 WHERE ((r1.c2 = r2."C 1")) AND (((r2."C 1" % 10) = 9))
;
----------
[UPDATE] -- update both at once
UPDATE  temporal_rng
SET     id = '[21,22)',
        valid_at = '[2018-01-02,2018-02-03)'
WHERE   id = '[11,12)'
AND     valid_at @> '2020-01-15'::date
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 655)
;
----------
[INSERT|SELECT] INSERT INTO b(aa) VALUES('bbbbb')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pht2_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] INSERT INTO brintest_2 VALUES (numrange(0, 2^1000::numeric))
;
----------
[UPDATE] UPDATE gtest3 SET a = 22 WHERE a = 2
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 340)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6338', relminmxid = '1'
WHERE oid = '"public"."bt_name_heap"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest22b"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pht2_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 915)
;
