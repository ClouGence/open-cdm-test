-- check constraint is enforced on the remote side, not locally
copy rem2 from stdin
;
ALTER TABLE public.notnull_tbl1_upg OWNER TO zyc
;
EXECUTE dumpCompositeType('38830')
;
COPY sch1.t1 (c1) TO STDOUT
;
LOCK TABLE public.fk_partitioned_fk_3 IN ACCESS SHARE MODE
;
EXECUTE dumpFunc('41613')
;
DROP ROLE regress_tablespace_user1
;
LOCK TABLE lock_view8 IN ACCESS EXCLUSIVE MODE
;
ALTER SERVER loopback OPTIONS (DROP batch_size)
;
COPY pg_catalog.pg_replication_origin (roident, roname) TO stdout
;
------------------------------------------------------------------------------------------
[DATA_IMPORT] -- check constraint is enforced on the remote side, not locally
copy rem2 from stdin
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.notnull_tbl1_upg OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpCompositeType('38830')
;
----------
[DATA_EXPORT] COPY sch1.t1 (c1) TO STDOUT
;
----------
[SESSION_LOCK] LOCK TABLE public.fk_partitioned_fk_3 IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpFunc('41613')
;
----------
[DROP_ROLE] DROP ROLE regress_tablespace_user1
;
----------
[SESSION_LOCK] LOCK TABLE lock_view8 IN ACCESS EXCLUSIVE MODE
;
----------
[SYSTEM_SETTING_WRITE] ALTER SERVER loopback OPTIONS (DROP batch_size)
;
----------
[DATA_EXPORT] COPY pg_catalog.pg_replication_origin (roident, roname) TO stdout
;
