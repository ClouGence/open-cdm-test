ALTER TABLE t ADD COLUMN y int NOT NULL DEFAULT 5
;
IMPORT FOREIGN SCHEMA nonesuch FROM SERVER loopback INTO import_dest4
;
-- test connectby with int based hierarchy
CREATE TABLE connectby_int(keyid int, parent_keyid int)
;
CREATE TABLE alpha_pos_p2 PARTITION OF alpha_pos FOR VALUES IN ('0004', '0006')
;
alter table inhcld inherit inhpar
;
DROP TABLE group_tbl
;
ALTER INDEX pg_toast.pg_toast_30592_index RENAME TO reind_con_toast_idx
;
-- ERROR
ANALYZE ft1
;
CREATE INDEX gtest22c_pred_idx ON public.gtest22c USING btree (a) WHERE (b > 0)
;
CREATE FUNCTION test_radixtree()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_radixtree' LANGUAGE C
;
CREATE TABLE fk_notpartitioned_fk (b int, a int)
;
CREATE TEMP TABLE pg_temp_18.pg_temp_26820_2 (tid pg_catalog.tid)
;
CREATE TABLE t1p_tens PARTITION OF t1p FOR VALUES FROM ('10') TO ('100')
;
--
-- drop user defined conversion
--
DROP CONVERSION myconv
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_language
;
CREATE VIEW gtest1v AS SELECT * FROM gtest1
;
CREATE VIEW column_udt_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(coalesce(nbt.nspname, nt.nspname) AS sql_identifier) AS udt_schema,
           CAST(coalesce(bt.typname, t.typname) AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_attribute a, pg_class c, pg_namespace nc,
         (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid))
           LEFT JOIN (pg_type bt JOIN pg_namespace nbt ON (bt.typnamespace = nbt.oid))
           ON (t.typtype = 'd' AND t.typbasetype = bt.oid)

    WHERE a.attrelid = c.oid
          AND a.atttypid = t.oid
          AND nc.oid = c.relnamespace
          AND a.attnum > 0 AND NOT a.attisdropped
          AND c.relkind in ('r', 'v', 'f', 'p')
          AND pg_has_role(coalesce(bt.typowner, t.typowner), 'USAGE')
;
alter table recur1 add column f2 array_of_recur1
;
DROP TABLE regular_table
;
-- fail
DROP PROCEDURE priv_testproc1(int)
;
ALTER TABLE public.fktable ADD CONSTRAINT constrname FOREIGN KEY (ftest1, ftest2) REFERENCES pktable(ptest1, ptest2) MATCH FULL ON UPDATE SET NULL ON DELETE SET NULL
;
CREATE TABLE "public"."notnull_tbl3" (
    "a" integer NOT NULL,
    "b" integer NOT NULL,
    CONSTRAINT "notnull_tbl3_a_check" CHECK (("a" IS NOT NULL))
)
;
create function getf3(x mutable) returns int language plpgsql as
$$ begin return x.f3; end $$
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_class
;
--
-- Test GROUP BY matching of join columns that are type-coerced due to USING
--

create temp table t1(f1 int, f2 int)
;
create table t6()
;
alter table cnn_uq add unique using index cnn_uq_idx
;
create table matest0 (id serial primary key, name text)
;
-- Now, REFRESH will issue such an INSERT, queueing the GRANT
CREATE OR REPLACE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'INSERT INTO public.sro_trojan_table DEFAULT VALUES; SELECT true'
;
-- these are to check syntax error reporting
DO LANGUAGE plpgsql $$begin return 1; end$$
;
/*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
CREATE FUNCTION gbt_int2_consistent(internal,int2,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER OPERATOR >= (isbn, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
CREATE TEMP TABLE quad_point_tbl_ord_seq1 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM quad_point_tbl
;
CREATE DATABASE split_db18_icu WITH TEMPLATE template0 STRATEGY FILE_COPY LOCALE_PROVIDER icu ICU_LOCALE 'und-x-icu' ICU_RULES '&V << w <<< W'
;
CREATE POLICY p1 ON document AS PERMISSIVE
    USING (dlevel <= (SELECT seclv FROM uaccount WHERE pguser = current_user))
;
drop domain testdomain3
;
CREATE DATABASE "regression" WITH TEMPLATE = template0 OID = 16387 STRATEGY = FILE_COPY ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
DO $$BEGIN EXECUTE format(
	'ALTER DATABASE %I RESET role', current_catalog); END$$
;
-- update partition key using updatable view.
CREATE VIEW upview AS SELECT * FROM range_parted WHERE (select c > c1 FROM mintab) WITH CHECK OPTION
;
-- fails
ALTER EXTENSION test_ext_req_schema2 SET SCHEMA test_s_dep
;
ALTER INDEX alter_idx_rename_test_idx RENAME TO alter_idx_rename_test_idx_2
;
alter foreign table rem2 add constraint rem2_f1positive check (f1 >= 0)
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should work
  insert into foo values(5,6) returning * into x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
CREATE TABLE A1(
   a_id  integer  NOT NULL ,
   b_id  integer  NOT NULL ,
   c_id  integer  NOT NULL ,
   sales_amount  numeric(10, 2)  NOT NULL ,
   order_date  date  NOT NULL ,
   q1  varchar(255) COLLATE "pg_catalog"."default" NULL ,
   q2  varchar(255) COLLATE "pg_catalog"."default" NULL ,
 CONSTRAINT "A1_pkey" PRIMARY KEY ( a_id ) NOT DEFERRABLE )TABLESPACE pg_default
;
ALTER TABLE pg_toast.pg_toast_30516 RENAME TO reind_con_toast
;
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b::numeric, c, d FROM viewtest_tbl
;
create table nulltest
           ( col1 dnotnull
           , col2 dnotnull NULL  
           , col3 dnull    NOT NULL
           , col4 dnull
           , col5 dcheck CHECK (col5 IN ('c', 'd'))
           )
;
CREATE SCHEMA IF NOT EXISTS baz
;
ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p1" FOR VALUES FROM (0) TO (10)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t ADD COLUMN y int NOT NULL DEFAULT 5
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA nonesuch FROM SERVER loopback INTO import_dest4
;
----------
[CREATE_TABLE] -- test connectby with int based hierarchy
CREATE TABLE connectby_int(keyid int, parent_keyid int)
;
----------
[CREATE_TABLE] CREATE TABLE alpha_pos_p2 PARTITION OF alpha_pos FOR VALUES IN ('0004', '0006')
;
----------
[ALTER_TABLE] alter table inhcld inherit inhpar
;
----------
[DROP_TABLE] DROP TABLE group_tbl
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30592_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] -- ERROR
ANALYZE ft1
;
----------
[ADD_INDEX] CREATE INDEX gtest22c_pred_idx ON public.gtest22c USING btree (a) WHERE (b > 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_radixtree()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_radixtree' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE fk_notpartitioned_fk (b int, a int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_18.pg_temp_26820_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE t1p_tens PARTITION OF t1p FOR VALUES FROM ('10') TO ('100')
;
----------
[DROP_POLICY] --
-- drop user defined conversion
--
DROP CONVERSION myconv
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_language
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW gtest1v AS SELECT * FROM gtest1
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW column_udt_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(coalesce(nbt.nspname, nt.nspname) AS sql_identifier) AS udt_schema,
           CAST(coalesce(bt.typname, t.typname) AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_attribute a, pg_class c, pg_namespace nc,
         (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid))
           LEFT JOIN (pg_type bt JOIN pg_namespace nbt ON (bt.typnamespace = nbt.oid))
           ON (t.typtype = 'd' AND t.typbasetype = bt.oid)

    WHERE a.attrelid = c.oid
          AND a.atttypid = t.oid
          AND nc.oid = c.relnamespace
          AND a.attnum > 0 AND NOT a.attisdropped
          AND c.relkind in ('r', 'v', 'f', 'p')
          AND pg_has_role(coalesce(bt.typowner, t.typowner), 'USAGE')
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table recur1 add column f2 array_of_recur1
;
----------
[DROP_TABLE] DROP TABLE regular_table
;
----------
[DROP_PROG_OBJ] -- fail
DROP PROCEDURE priv_testproc1(int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.fktable ADD CONSTRAINT constrname FOREIGN KEY (ftest1, ftest2) REFERENCES pktable(ptest1, ptest2) MATCH FULL ON UPDATE SET NULL ON DELETE SET NULL
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl3" (
    "a" integer NOT NULL,
    "b" integer NOT NULL,
    CONSTRAINT "notnull_tbl3_a_check" CHECK (("a" IS NOT NULL))
)
;
----------
[CREATE_PROG_OBJ] create function getf3(x mutable) returns int language plpgsql as
$$ begin return x.f3; end $$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_class
;
----------
[CREATE_TABLE] --
-- Test GROUP BY matching of join columns that are type-coerced due to USING
--

create temp table t1(f1 int, f2 int)
;
----------
[CREATE_TABLE] create table t6()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table cnn_uq add unique using index cnn_uq_idx
;
----------
[CREATE_TABLE] create table matest0 (id serial primary key, name text)
;
----------
[CREATE_PROG_OBJ] -- Now, REFRESH will issue such an INSERT, queueing the GRANT
CREATE OR REPLACE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'INSERT INTO public.sro_trojan_table DEFAULT VALUES; SELECT true'
;
----------
[BLOCK] -- these are to check syntax error reporting
DO LANGUAGE plpgsql $$begin return 1; end$$
;
----------
[CREATE_POLICY] /*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int2_consistent(internal,int2,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (isbn, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE quad_point_tbl_ord_seq1 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM quad_point_tbl
;
----------
[CREATE_CATALOG] CREATE DATABASE split_db18_icu WITH TEMPLATE template0 STRATEGY FILE_COPY LOCALE_PROVIDER icu ICU_LOCALE 'und-x-icu' ICU_RULES '&V << w <<< W'
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON document AS PERMISSIVE
    USING (dlevel <= (SELECT seclv FROM uaccount WHERE pguser = current_user))
;
----------
[DROP_TYPE] drop domain testdomain3
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression" WITH TEMPLATE = template0 OID = 16387 STRATEGY = FILE_COPY ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[BLOCK] DO $$BEGIN EXECUTE format(
	'ALTER DATABASE %I RESET role', current_catalog); END$$
;
----------
[CREATE_VIEW(SELECT)] -- update partition key using updatable view.
CREATE VIEW upview AS SELECT * FROM range_parted WHERE (select c > c1 FROM mintab) WITH CHECK OPTION
;
----------
[ALTER_LIBRARY] -- fails
ALTER EXTENSION test_ext_req_schema2 SET SCHEMA test_s_dep
;
----------
[RENAME_INDEX] ALTER INDEX alter_idx_rename_test_idx RENAME TO alter_idx_rename_test_idx_2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter foreign table rem2 add constraint rem2_f1positive check (f1 >= 0)
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should work
  insert into foo values(5,6) returning * into x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE A1(
   a_id  integer  NOT NULL ,
   b_id  integer  NOT NULL ,
   c_id  integer  NOT NULL ,
   sales_amount  numeric(10, 2)  NOT NULL ,
   order_date  date  NOT NULL ,
   q1  varchar(255) COLLATE "pg_catalog"."default" NULL ,
   q2  varchar(255) COLLATE "pg_catalog"."default" NULL ,
 CONSTRAINT "A1_pkey" PRIMARY KEY ( a_id ) NOT DEFERRABLE )TABLESPACE pg_default
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30516 RENAME TO reind_con_toast
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b::numeric, c, d FROM viewtest_tbl
;
----------
[CREATE_TABLE] create table nulltest
           ( col1 dnotnull
           , col2 dnotnull NULL  
           , col3 dnull    NOT NULL
           , col4 dnull
           , col5 dcheck CHECK (col5 IN ('c', 'd'))
           )
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS baz
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_para" ATTACH PARTITION "public"."pagg_tab_para_p1" FOR VALUES FROM (0) TO (10)
;
