alter extension test_ext9 add type varbitrange
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
ALTER TABLE pg_toast.pg_toast_30751 RENAME TO reind_con_toast
;
-- a child with a dropped column
create table child2 (x int, a text, b int)
;
ANALYZE public.tableam_tblmv_heap2
;
CREATE VIEW my_application_name AS
  SELECT application_name FROM pg_stat_activity WHERE pid = pg_backend_pid()
;
CREATE FUNCTION gbt_oid_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
drop trigger base_tab_def_view_instrig on base_tab_def_view
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_policy
;
-- test handling of overflow for interval values
CREATE TABLE brin_interval_test(a INTERVAL)
;
CREATE TABLE foo_part_s2 PARTITION OF foo_parted FOR VALUES IN (2)
;
CREATE INDEX gpolygonind ON polygon_tbl USING gist (f1)
;
create view tt18v as
  select * from int8_tbl xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxy
  union all
  select * from int8_tbl xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxz
;
CREATE FUNCTION dogrant_fails() RETURNS void LANGUAGE sql SECURITY DEFINER AS
	'GRANT regress_priv_group2 TO regress_priv_user5'
;
-- Check INSERT .. ON CONFLICT DO UPDATE works correctly when the view's
-- columns are named and ordered differently than the underlying table's.
create table uv_iocu_tab (a text unique, b float)
;
ANALYZE information_schema.sql_sizing
;
create table tabwithcols(a int, b int, c int, d int)
;
ALTER SEQUENCE regtest_seq_1 SET SCHEMA regtest_schema_2
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_type
;
ALTER FOREIGN TABLE foreign_schema.foreign_table_1
	ENABLE TRIGGER trigtest_before_stmt
;
--
-- Tests for VARIANCE()
--
CREATE TABLE num_variance (a numeric)
;
DROP TABLE IF EXISTS stream_test
;
ANALYZE rls_child_tbl
;
create index ͪߩѦ���index2 on ͪߩѦ��� using hash (��׾�ڵ�)
;
CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
ALTER TYPE test8b ALTER ATTRIBUTE b TYPE priv_testdomain1
;
ALTER TABLE ONLY "public"."beta_pos" ATTACH PARTITION "public"."beta_pos_p2" FOR VALUES IN ('0004', '0006')
;
CREATE FUNCTION "public"."execute_text_query_heap"("query_sql" "text") RETURNS SETOF "text"
    LANGUAGE "plpgsql"
    AS $$
begin
  set enable_seqscan = on;
  set enable_bitmapscan = off;
  return query execute query_sql;
end;
$$
;
CREATE VIEW domains AS
    SELECT CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(nt.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,

           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nbt.nspname = 'pg_catalog' THEN format_type(t.typbasetype, null)
                  ELSE 'USER-DEFINED' END
             AS character_data)
             AS data_type,

           CAST(
             _pg_char_max_length(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS character_maximum_length,

           CAST(
             _pg_char_octet_length(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS character_octet_length,

           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,

           CAST(CASE WHEN nco.nspname IS NOT NULL THEN current_database() END AS sql_identifier) AS collation_catalog,
           CAST(nco.nspname AS sql_identifier) AS collation_schema,
           CAST(co.collname AS sql_identifier) AS collation_name,

           CAST(
             _pg_numeric_precision(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS numeric_precision,

           CAST(
             _pg_numeric_precision_radix(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS numeric_precision_radix,

           CAST(
             _pg_numeric_scale(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS numeric_scale,

           CAST(
             _pg_datetime_precision(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS datetime_precision,

           CAST(
             _pg_interval_type(t.typbasetype, t.typtypmod)
             AS character_data)
             AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,

           CAST(t.typdefault AS character_data) AS domain_default,

           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nbt.nspname AS sql_identifier) AS udt_schema,
           CAST(bt.typname AS sql_identifier) AS udt_name,

           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,

           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST(1 AS sql_identifier) AS dtd_identifier

    FROM (pg_type t JOIN pg_namespace nt ON t.typnamespace = nt.oid)
         JOIN (pg_type bt JOIN pg_namespace nbt ON bt.typnamespace = nbt.oid)
           ON (t.typbasetype = bt.oid AND t.typtype = 'd')
         LEFT JOIN (pg_collation co JOIN pg_namespace nco ON (co.collnamespace = nco.oid))
           ON t.typcollation = co.oid AND (nco.nspname, co.collname) <> ('pg_catalog', 'default')

    WHERE (pg_has_role(t.typowner, 'USAGE')
           OR has_type_privilege(t.oid, 'USAGE'))
;
CREATE FUNCTION PLUS_TWO(i INTEGER) RETURNS INTEGER AS $$
DECLARE
  r INTEGER;
BEGIN
  SELECT (i + 1 + 1.0)::INTEGER INTO r;
  RETURN r;
END; $$ LANGUAGE plpgsql
;
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD OPERATOR 1 < (int4, int4), OPERATOR 1 < (int4, int4)
;
CREATE INDEX t1_ab_idx on t1 ((a || b))
;
CREATE TEMP TABLE joinme (f2j text, other int)
;
CREATE INDEX dateidx ON datetmp USING gist ( a )
;
CREATE TABLE "public"."test_tsvector" (
    "t" "text",
    "a" "tsvector"
)
;
-- clean up
DROP TABLE bmscantest
;
create or replace function max_xacttest() returns smallint language sql as
'select max(a) from xacttest' volatile
;
ALTER TABLE pg_toast.pg_toast_30932 RENAME TO reind_con_toast
;
create index pendtest_idx on pendtest using gin(ts)
;
create table idxpart21 partition of idxpart2 for values from (100) to (200)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION tap_pub
;
CREATE TABLE "public"."brinopers_bloom" (
    "colname" "name",
    "typ" "text",
    "op" "text"[],
    "value" "text"[],
    "matches" integer[],
    CONSTRAINT "brinopers_bloom_check" CHECK (("cardinality"("op") = "cardinality"("value"))),
    CONSTRAINT "brinopers_bloom_check1" CHECK (("cardinality"("op") = "cardinality"("matches")))
)
;
ALTER CONVERSION alt_conv1 RENAME TO alt_conv4
;
ALTER TABLE ONLY "public"."pagg_tab_ml" ATTACH PARTITION "public"."pagg_tab_ml_p2" FOR VALUES FROM (12) TO (20)
;
CREATE TYPE "public"."cantcompare" AS (
	"p" "point",
	"r" double precision
)
;
DROP TABLE distinct_group_2
;
CREATE PUBLICATION pub4
;
----
-- Basic tests for track_functions
---
CREATE FUNCTION stats_test_func1() RETURNS VOID LANGUAGE plpgsql AS $$BEGIN END;$$
;
ALTER TABLE fkpart11.pk1 ATTACH PARTITION fkpart11.pk11 FOR VALUES IN (1)
;
CREATE FUNCTION handle_exception_trigger()
RETURNS TRIGGER AS $$
BEGIN
	BEGIN
		-- Raise an exception
		RAISE EXCEPTION 'This is a test exception';
	EXCEPTION
		WHEN OTHERS THEN
			RETURN NEW;
	END;

	RETURN NEW;
END;
$$ LANGUAGE plpgsql
;
------------------------------------------------------------------------------------------
[ALTER_LIBRARY] alter extension test_ext9 add type varbitrange
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30751 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- a child with a dropped column
create table child2 (x int, a text, b int)
;
----------
[ADMIN_TABLE] ANALYZE public.tableam_tblmv_heap2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW my_application_name AS
  SELECT application_name FROM pg_stat_activity WHERE pid = pg_backend_pid()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_oid_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TRIGGER] drop trigger base_tab_def_view_instrig on base_tab_def_view
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_policy
;
----------
[CREATE_TABLE] -- test handling of overflow for interval values
CREATE TABLE brin_interval_test(a INTERVAL)
;
----------
[CREATE_TABLE] CREATE TABLE foo_part_s2 PARTITION OF foo_parted FOR VALUES IN (2)
;
----------
[ADD_INDEX] CREATE INDEX gpolygonind ON polygon_tbl USING gist (f1)
;
----------
[CREATE_VIEW(SELECT)] create view tt18v as
  select * from int8_tbl xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxy
  union all
  select * from int8_tbl xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxz
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dogrant_fails() RETURNS void LANGUAGE sql SECURITY DEFINER AS
	'GRANT regress_priv_group2 TO regress_priv_user5'
;
----------
[CREATE_TABLE] -- Check INSERT .. ON CONFLICT DO UPDATE works correctly when the view's
-- columns are named and ordered differently than the underlying table's.
create table uv_iocu_tab (a text unique, b float)
;
----------
[ADMIN_TABLE] ANALYZE information_schema.sql_sizing
;
----------
[CREATE_TABLE] create table tabwithcols(a int, b int, c int, d int)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE regtest_seq_1 SET SCHEMA regtest_schema_2
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_type
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE foreign_schema.foreign_table_1
	ENABLE TRIGGER trigtest_before_stmt
;
----------
[CREATE_TABLE] --
-- Tests for VARIANCE()
--
CREATE TABLE num_variance (a numeric)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS stream_test
;
----------
[ADMIN_TABLE] ANALYZE rls_child_tbl
;
----------
[ADD_INDEX] create index ͪߩѦ���index2 on ͪߩѦ��� using hash (��׾�ڵ�)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
----------
[ALTER_TYPE] ALTER TYPE test8b ALTER ATTRIBUTE b TYPE priv_testdomain1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."beta_pos" ATTACH PARTITION "public"."beta_pos_p2" FOR VALUES IN ('0004', '0006')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."execute_text_query_heap"("query_sql" "text") RETURNS SETOF "text"
    LANGUAGE "plpgsql"
    AS $$
begin
  set enable_seqscan = on;
  set enable_bitmapscan = off;
  return query execute query_sql;
end;
$$
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW domains AS
    SELECT CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(nt.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,

           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nbt.nspname = 'pg_catalog' THEN format_type(t.typbasetype, null)
                  ELSE 'USER-DEFINED' END
             AS character_data)
             AS data_type,

           CAST(
             _pg_char_max_length(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS character_maximum_length,

           CAST(
             _pg_char_octet_length(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS character_octet_length,

           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,

           CAST(CASE WHEN nco.nspname IS NOT NULL THEN current_database() END AS sql_identifier) AS collation_catalog,
           CAST(nco.nspname AS sql_identifier) AS collation_schema,
           CAST(co.collname AS sql_identifier) AS collation_name,

           CAST(
             _pg_numeric_precision(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS numeric_precision,

           CAST(
             _pg_numeric_precision_radix(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS numeric_precision_radix,

           CAST(
             _pg_numeric_scale(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS numeric_scale,

           CAST(
             _pg_datetime_precision(t.typbasetype, t.typtypmod)
             AS cardinal_number)
             AS datetime_precision,

           CAST(
             _pg_interval_type(t.typbasetype, t.typtypmod)
             AS character_data)
             AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,

           CAST(t.typdefault AS character_data) AS domain_default,

           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nbt.nspname AS sql_identifier) AS udt_schema,
           CAST(bt.typname AS sql_identifier) AS udt_name,

           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,

           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST(1 AS sql_identifier) AS dtd_identifier

    FROM (pg_type t JOIN pg_namespace nt ON t.typnamespace = nt.oid)
         JOIN (pg_type bt JOIN pg_namespace nbt ON bt.typnamespace = nbt.oid)
           ON (t.typbasetype = bt.oid AND t.typtype = 'd')
         LEFT JOIN (pg_collation co JOIN pg_namespace nco ON (co.collnamespace = nco.oid))
           ON t.typcollation = co.oid AND (nco.nspname, co.collname) <> ('pg_catalog', 'default')

    WHERE (pg_has_role(t.typowner, 'USAGE')
           OR has_type_privilege(t.oid, 'USAGE'))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION PLUS_TWO(i INTEGER) RETURNS INTEGER AS $$
DECLARE
  r INTEGER;
BEGIN
  SELECT (i + 1 + 1.0)::INTEGER INTO r;
  RETURN r;
END; $$ LANGUAGE plpgsql
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf17 USING btree ADD OPERATOR 1 < (int4, int4), OPERATOR 1 < (int4, int4)
;
----------
[ADD_INDEX] CREATE INDEX t1_ab_idx on t1 ((a || b))
;
----------
[CREATE_TABLE] CREATE TEMP TABLE joinme (f2j text, other int)
;
----------
[ADD_INDEX] CREATE INDEX dateidx ON datetmp USING gist ( a )
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_tsvector" (
    "t" "text",
    "a" "tsvector"
)
;
----------
[DROP_TABLE] -- clean up
DROP TABLE bmscantest
;
----------
[CREATE_PROG_OBJ] create or replace function max_xacttest() returns smallint language sql as
'select max(a) from xacttest' volatile
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30932 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] create index pendtest_idx on pendtest using gin(ts)
;
----------
[CREATE_TABLE] create table idxpart21 partition of idxpart2 for values from (100) to (200)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brinopers_bloom" (
    "colname" "name",
    "typ" "text",
    "op" "text"[],
    "value" "text"[],
    "matches" integer[],
    CONSTRAINT "brinopers_bloom_check" CHECK (("cardinality"("op") = "cardinality"("value"))),
    CONSTRAINT "brinopers_bloom_check1" CHECK (("cardinality"("op") = "cardinality"("matches")))
)
;
----------
[ALTER_POLICY] ALTER CONVERSION alt_conv1 RENAME TO alt_conv4
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml" ATTACH PARTITION "public"."pagg_tab_ml_p2" FOR VALUES FROM (12) TO (20)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."cantcompare" AS (
	"p" "point",
	"r" double precision
)
;
----------
[DROP_TABLE] DROP TABLE distinct_group_2
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub4
;
----------
[CREATE_PROG_OBJ] ----
-- Basic tests for track_functions
---
CREATE FUNCTION stats_test_func1() RETURNS VOID LANGUAGE plpgsql AS $$BEGIN END;$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fkpart11.pk1 ATTACH PARTITION fkpart11.pk11 FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_exception_trigger()
RETURNS TRIGGER AS $$
BEGIN
	BEGIN
		-- Raise an exception
		RAISE EXCEPTION 'This is a test exception';
	EXCEPTION
		WHEN OTHERS THEN
			RETURN NEW;
	END;

	RETURN NEW;
END;
$$ LANGUAGE plpgsql
;
