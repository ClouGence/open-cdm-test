-- should fail
INSERT INTO rw_view2 VALUES (30)
;
INSERT INTO trans_abc VALUES (3)
;
INSERT INTO TIMESTAMPTZ_TBL VALUES ('Dec 31 17:32:01 2000')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9611', relminmxid = '1'
WHERE oid = '"public"."rtest_order1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15479', relminmxid = '3'
WHERE oid = '"public"."prt1_e_p3"'::pg_catalog.regclass
;
INSERT INTO serialTest2 (f1, f3)
  VALUES ('bogus', -32769)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2667', relminmxid = '1'
WHERE oid = '"public"."tbl_include_box_pk"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('4', 564)
;
INSERT INTO rem1 VALUES (2, 'test2')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11852', relminmxid = '3'
WHERE oid = '"fkpart3"."fk3"'::pg_catalog.regclass
;
INSERT INTO basictest values ('88', 'haha', 'short text', '123.12')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."mlparted12"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."pht1_e_p3"'::pg_catalog.regclass
;
INSERT INTO INT8_TBL VALUES('  123   ','  456')
;
INSERT INTO public.tplain VALUES ('5', 585)
;
-- Cannot cast to boolean on assignment.
INSERT INTO caster (boolean)       VALUES ('t'::text)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7005', relminmxid = '1'
WHERE oid = '"public"."bt_f8_heap"'::pg_catalog.regclass
;
INSERT INTO temporal_mltrng (id, valid_at) VALUES
  ('[5,6)', datemultirange(daterange('2018-01-01', '2018-02-01'))),
  ('[5,6)', datemultirange(daterange('2018-02-01', '2018-03-01')))
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13820', relminmxid = '3'
WHERE oid = '34779'
;
INSERT INTO public.tplain VALUES ('7', 877)
;
INSERT INTO quad_poly_tbl
	SELECT (x - 1) * 100 + y, polygon(circle(point(x * 10, y * 10), 1 + (x + y) % 10))
	FROM generate_series(1, 100) x,
		 generate_series(1, 100) y
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."alpha_neg_p2"'::pg_catalog.regclass
;
insert into test1 select generate_series(1,1000)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18220', relminmxid = '3'
WHERE oid = '"public"."beta_neg_p1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."itest10"'::pg_catalog.regclass
;
INSERT INTO sch1.tab1 VALUES(21)
;
INSERT INTO pg_temp_14.pg_temp_25613_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_14.pg_temp_25613 AS newdata FROM public.mvtest_tm mv FULL JOIN pg_temp_14.pg_temp_25613 newdata ON (newdata.type OPERATOR(pg_catalog.=) mv.type AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO public.tplain VALUES ('8', 158)
;
INSERT INTO pg_temp_14.pg_temp_27052_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_14.pg_temp_27052 AS newdata FROM public.mvtest_mv_foo mv FULL JOIN pg_temp_14.pg_temp_27052 newdata ON (newdata.i OPERATOR(pg_catalog.=) mv.i AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
insert into dom_table values (1)
;
-- fail;
insert into hpart0 values(11)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9163', relminmxid = '1'
WHERE oid = '28435'
;
INSERT INTO public.tplain VALUES ('8', 618)
;
UPDATE temporal_mltrng SET id = '[7,8)'
  WHERE id = '[5,6)' AND valid_at = datemultirange(daterange('2018-01-01', '2018-02-01'))
;
-- fails id join condition
INSERT INTO hjtest_2(a, id, b, c) VALUES (true, 1, 'another', 90)
;
INSERT INTO test_cidr VALUES
	( '1.2.3.4' ),
	( '1.2.4.4' ),
	( '1.2.5.4' ),
	( '1.2.6.4' ),
	( '1.2.7.4' ),
	( '1.2.8.4' )
;
INSERT INTO pa_target SELECT id, id * 100, 'initial' FROM generate_series(1,14,2) AS id
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart4"."droppk2"'::pg_catalog.regclass
  AND attname IN ('a')
;
INSERT INTO num_exp_add VALUES (5,2,'-34322095.176906047')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16503', relminmxid = '3'
WHERE oid = '"public"."tt2"'::pg_catalog.regclass
;
INSERT INTO pg_temp_11.pg_temp_41261_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_41261 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_11.pg_temp_41261 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
DELETE FROM a
;
insert into fktable(ftest1, ftest2) values (3, 1)
;
INSERT INTO public.loc1(f1, f2) VALUES ($1, $2) RETURNING f1, f2, ctid
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11240', relminmxid = '1'
WHERE oid = '30068'
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."beta_neg_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13925', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_m_p3"'::pg_catalog.regclass
;
-- direct leaf partition update, with partition id violation
UPDATE errtst_child_fastdef SET partid = partid + 10 WHERE partid = 0
;
insert into insertconflicttest values (2, 'Orange') on conflict (key, key, key) do update set fruit = excluded.fruit
;
INSERT INTO public.ths_p1 VALUES (6, 6)
;
------------------------------------------------------------------------------------------
[INSERT] -- should fail
INSERT INTO rw_view2 VALUES (30)
;
----------
[INSERT] INSERT INTO trans_abc VALUES (3)
;
----------
[INSERT] INSERT INTO TIMESTAMPTZ_TBL VALUES ('Dec 31 17:32:01 2000')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9611', relminmxid = '1'
WHERE oid = '"public"."rtest_order1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15479', relminmxid = '3'
WHERE oid = '"public"."prt1_e_p3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO serialTest2 (f1, f3)
  VALUES ('bogus', -32769)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2667', relminmxid = '1'
WHERE oid = '"public"."tbl_include_box_pk"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 564)
;
----------
[INSERT] INSERT INTO rem1 VALUES (2, 'test2')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11852', relminmxid = '3'
WHERE oid = '"fkpart3"."fk3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO basictest values ('88', 'haha', 'short text', '123.12')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."mlparted12"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."pht1_e_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO INT8_TBL VALUES('  123   ','  456')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('5', 585)
;
----------
[INSERT|SELECT] -- Cannot cast to boolean on assignment.
INSERT INTO caster (boolean)       VALUES ('t'::text)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '7005', relminmxid = '1'
WHERE oid = '"public"."bt_f8_heap"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO temporal_mltrng (id, valid_at) VALUES
  ('[5,6)', datemultirange(daterange('2018-01-01', '2018-02-01'))),
  ('[5,6)', datemultirange(daterange('2018-02-01', '2018-03-01')))
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13820', relminmxid = '3'
WHERE oid = '34779'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 877)
;
----------
[INSERT|SELECT] INSERT INTO quad_poly_tbl
	SELECT (x - 1) * 100 + y, polygon(circle(point(x * 10, y * 10), 1 + (x + y) % 10))
	FROM generate_series(1, 100) x,
		 generate_series(1, 100) y
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."alpha_neg_p2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into test1 select generate_series(1,1000)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18220', relminmxid = '3'
WHERE oid = '"public"."beta_neg_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."itest10"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO sch1.tab1 VALUES(21)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_14.pg_temp_25613_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_14.pg_temp_25613 AS newdata FROM public.mvtest_tm mv FULL JOIN pg_temp_14.pg_temp_25613 newdata ON (newdata.type OPERATOR(pg_catalog.=) mv.type AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 158)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_14.pg_temp_27052_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_14.pg_temp_27052 AS newdata FROM public.mvtest_mv_foo mv FULL JOIN pg_temp_14.pg_temp_27052 newdata ON (newdata.i OPERATOR(pg_catalog.=) mv.i AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] insert into dom_table values (1)
;
----------
[INSERT] -- fail;
insert into hpart0 values(11)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9163', relminmxid = '1'
WHERE oid = '28435'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 618)
;
----------
[UPDATE] UPDATE temporal_mltrng SET id = '[7,8)'
  WHERE id = '[5,6)' AND valid_at = datemultirange(daterange('2018-01-01', '2018-02-01'))
;
----------
[INSERT|SELECT] -- fails id join condition
INSERT INTO hjtest_2(a, id, b, c) VALUES (true, 1, 'another', 90)
;
----------
[INSERT] INSERT INTO test_cidr VALUES
	( '1.2.3.4' ),
	( '1.2.4.4' ),
	( '1.2.5.4' ),
	( '1.2.6.4' ),
	( '1.2.7.4' ),
	( '1.2.8.4' )
;
----------
[INSERT|SELECT] INSERT INTO pa_target SELECT id, id * 100, 'initial' FROM generate_series(1,14,2) AS id
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart4"."droppk2"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (5,2,'-34322095.176906047')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16503', relminmxid = '3'
WHERE oid = '"public"."tt2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_11.pg_temp_41261_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_41261 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_11.pg_temp_41261 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[DELETE] DELETE FROM a
;
----------
[INSERT|SELECT] insert into fktable(ftest1, ftest2) values (3, 1)
;
----------
[INSERT|SELECT] INSERT INTO public.loc1(f1, f2) VALUES ($1, $2) RETURNING f1, f2, ctid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11240', relminmxid = '1'
WHERE oid = '30068'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."beta_neg_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13925', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_m_p3"'::pg_catalog.regclass
;
----------
[UPDATE] -- direct leaf partition update, with partition id violation
UPDATE errtst_child_fastdef SET partid = partid + 10 WHERE partid = 0
;
----------
[MERGE] insert into insertconflicttest values (2, 'Orange') on conflict (key, key, key) do update set fruit = excluded.fruit
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 6)
;
