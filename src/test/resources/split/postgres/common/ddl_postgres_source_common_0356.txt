CREATE TYPE regress_zeropriv_type AS (a int)
;
do $$
declare x constant var_record; y int;
begin
  for x.f1, y in select 1, 2 loop  -- fail
  end loop;
end$$
;
CREATE TRIGGER d1_insert_trig
  AFTER INSERT ON part_d_1_15 for each statement execute procedure trigfunc()
;
CREATE TABLE batch_cp_upd_test3 (LIKE batch_cp_upd_test)
;
create unique index PField_name on PField using btree (name text_ops)
;
-- ok, collation is implicitly coerced
create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "C") to ('g')
;
VACUUM (FREEZE) information_schema.sql_sizing
;
-- Test that parallel index build produces the same BRIN index as serial build.
CREATE TABLE brin_parallel_test (a int, b text, c bigint) WITH (fillfactor=40)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_ts_parser
;
create function xtrig()
  returns trigger language plpgsql
as $$
  declare
    r record;
  begin
    for r in select * from old loop
      raise info 'a=%, b=%', r.a, r.b;
    end loop;
    return NULL;
  end;
$$
;
-- anyarray column

CREATE TEMP TABLE rows AS
SELECT x, 'txt' || x as y
FROM generate_series(1,3) AS x
;
ALTER TABLE a1 RENAME TO tt1
;
CREATE OPERATOR CLASS name_ops
DEFAULT FOR TYPE name USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btnamecmp(name,name),
    FUNCTION        2       gin_extract_value_name(name, internal),
    FUNCTION        3       gin_extract_query_name(name, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_name(name,name,int2, internal),
STORAGE         name
;
ANALYZE regression_vacuumdb_part1
;
CREATE TABLE "public"."quad_box_tbl" (
    "b" "box"
)
;
create table ddtest2(f1 ddtest1)
;
ALTER TABLE ONLY "public"."mlparted_def1" DROP COLUMN "........pg.dropped.6........"
;
--
--
--
-- timestamp ops
--
--
--

CREATE FUNCTION gbt_ts_consistent(internal,timestamp,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE pg_temp_13.pg_temp_26616_2
;
ANALYZE pg_temp_19.pg_temp_33778_2
;
CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (a) TO ('2019-01-01')
;
CREATE TEMP TABLE test4(f1 text)
;
DROP COLLATION test0 RESTRICT
;
CREATE TABLE tab_default (a int, b bool)
;
CREATE DOMAIN qf_jsonb_domain AS jsonb
;
CREATE PUBLICATION regress_pub2 FOR TABLE tab_primary_key
;
CREATE FOREIGN TABLE copy_default (
	id integer,
	text_value text not null default 'test',
	ts_value timestamp without time zone not null default '2022-07-05'
) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/copy_default.csv', default '\D')
;
COMMENT ON CONSTRAINT the_constraint ON constraint_comments_tbl IS 'no, the comment'
;
CREATE EXTENSION pg_walinspect WITH VERSION '1.0'
;
CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int, out f3 text) RETURNS int
AS 'select $1+1' LANGUAGE sql
;
CREATE FUNCTION "public"."binary_coercible"("oid", "oid") RETURNS boolean
    LANGUAGE "plpgsql" STABLE STRICT
    AS $_$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b' and castcontext = 'i')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$_$
;
-- Verify failures
CREATE TABLE ext_stats_test (x int, y int, z int)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_statistic_ext
;
ALTER INDEX pg_toast.pg_toast_27948_index RENAME TO reind_con_toast_idx
;
CREATE TABLE "public"."depth2" (
    "c" "text"
)
;
CREATE TABLE "fkpart5"."pk2" (
    "a" integer NOT NULL
)
;
CREATE UNIQUE INDEX guid1_unique_BTREE ON guid1 USING BTREE (guid_field)
;
ALTER TABLE foo DROP COLUMN f3 CASCADE
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=23944 host=/tmp/9CKEVhNMNr dbname=postgres' PUBLICATION mypub
;
CREATE UNIQUE INDEX tab_rowfilter_toast_ri_index on tab_rowfilter_toast (a, b)
;
DROP SCHEMA pgss_schema_1 CASCADE
;
CREATE FUNCTION regexp_split_to_table( citext, citext, text ) RETURNS SETOF TEXT AS $$
    SELECT pg_catalog.regexp_split_to_table( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
CREATE VIEW mysecview2 WITH (security_barrier=true)
       AS SELECT * FROM tbl1 WHERE a > 0
;
CALL transaction_test1(10, $x$baz$x$)
;
-- ************************************************************
-- * BEFORE INSERT or UPDATE on all slots with backlink
-- *	- Set backlink to empty string if NULL value given
-- ************************************************************
create function tg_chkbacklink() returns trigger as '
begin
    if new.backlink isnull then
        new.backlink := '''';
    end if;
    return new;
end;
' language plpgsql
;
create function namedparmcursor_test1(int, int) returns boolean as $$
declare
    c1 cursor (param1 int, param12 int) for select * from rc_test where a > param1 and b > param12;
    nonsense record;
begin
    open c1(param12 := $2, param1 := $1);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$$ language plpgsql
;
-- ok

-- test multiple inheritance mismatches
CREATE TABLE gtesty (x int, b int)
;
ALTER TABLE pg_toast.pg_toast_30866 RENAME TO reind_con_toast
;
CREATE INDEX t2_spgist ON s2.t2 USING SPGIST (ir)
;
CREATE VIEW rw_view1 AS
  SELECT * FROM base_tbl b
  WHERE EXISTS(SELECT 1 FROM ref_tbl r WHERE r.a = b.a)
  WITH CHECK OPTION
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] CREATE TYPE regress_zeropriv_type AS (a int)
;
----------
[BLOCK] do $$
declare x constant var_record; y int;
begin
  for x.f1, y in select 1, 2 loop  -- fail
  end loop;
end$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER d1_insert_trig
  AFTER INSERT ON part_d_1_15 for each statement execute procedure trigfunc()
;
----------
[CREATE_TABLE] CREATE TABLE batch_cp_upd_test3 (LIKE batch_cp_upd_test)
;
----------
[ADD_INDEX] create unique index PField_name on PField using btree (name text_ops)
;
----------
[CREATE_TABLE] -- ok, collation is implicitly coerced
create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "C") to ('g')
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) information_schema.sql_sizing
;
----------
[CREATE_TABLE] -- Test that parallel index build produces the same BRIN index as serial build.
CREATE TABLE brin_parallel_test (a int, b text, c bigint) WITH (fillfactor=40)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_ts_parser
;
----------
[CREATE_PROG_OBJ] create function xtrig()
  returns trigger language plpgsql
as $$
  declare
    r record;
  begin
    for r in select * from old loop
      raise info 'a=%, b=%', r.a, r.b;
    end loop;
    return NULL;
  end;
$$
;
----------
[CREATE_TABLE|SELECT] -- anyarray column

CREATE TEMP TABLE rows AS
SELECT x, 'txt' || x as y
FROM generate_series(1,3) AS x
;
----------
[RENAME_TABLE] ALTER TABLE a1 RENAME TO tt1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS name_ops
DEFAULT FOR TYPE name USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btnamecmp(name,name),
    FUNCTION        2       gin_extract_value_name(name, internal),
    FUNCTION        3       gin_extract_query_name(name, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_name(name,name,int2, internal),
STORAGE         name
;
----------
[ADMIN_TABLE] ANALYZE regression_vacuumdb_part1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."quad_box_tbl" (
    "b" "box"
)
;
----------
[CREATE_TABLE] create table ddtest2(f1 ddtest1)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def1" DROP COLUMN "........pg.dropped.6........"
;
----------
[CREATE_PROG_OBJ] --
--
--
-- timestamp ops
--
--
--

CREATE FUNCTION gbt_ts_consistent(internal,timestamp,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26616_2
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_19.pg_temp_33778_2
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (a) TO ('2019-01-01')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE test4(f1 text)
;
----------
[DROP_POLICY] DROP COLLATION test0 RESTRICT
;
----------
[CREATE_TABLE] CREATE TABLE tab_default (a int, b bool)
;
----------
[CREATE_TYPE] CREATE DOMAIN qf_jsonb_domain AS jsonb
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION regress_pub2 FOR TABLE tab_primary_key
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE copy_default (
	id integer,
	text_value text not null default 'test',
	ts_value timestamp without time zone not null default '2022-07-05'
) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/copy_default.csv', default '\D')
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT the_constraint ON constraint_comments_tbl IS 'no, the comment'
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pg_walinspect WITH VERSION '1.0'
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int, out f3 text) RETURNS int
AS 'select $1+1' LANGUAGE sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."binary_coercible"("oid", "oid") RETURNS boolean
    LANGUAGE "plpgsql" STABLE STRICT
    AS $_$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b' and castcontext = 'i')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$_$
;
----------
[CREATE_TABLE] -- Verify failures
CREATE TABLE ext_stats_test (x int, y int, z int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_statistic_ext
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27948_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE "public"."depth2" (
    "c" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."pk2" (
    "a" integer NOT NULL
)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX guid1_unique_BTREE ON guid1 USING BTREE (guid_field)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE foo DROP COLUMN f3 CASCADE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=23944 host=/tmp/9CKEVhNMNr dbname=postgres' PUBLICATION mypub
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX tab_rowfilter_toast_ri_index on tab_rowfilter_toast (a, b)
;
----------
[DROP_SCHEMA] DROP SCHEMA pgss_schema_1 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regexp_split_to_table( citext, citext, text ) RETURNS SETOF TEXT AS $$
    SELECT pg_catalog.regexp_split_to_table( $1::pg_catalog.text, $2::pg_catalog.text, CASE WHEN pg_catalog.strpos($3, 'c') = 0 THEN  $3 || 'i' ELSE $3 END );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mysecview2 WITH (security_barrier=true)
       AS SELECT * FROM tbl1 WHERE a > 0
;
----------
[CALL_PROG_OBJ] CALL transaction_test1(10, $x$baz$x$)
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * BEFORE INSERT or UPDATE on all slots with backlink
-- *	- Set backlink to empty string if NULL value given
-- ************************************************************
create function tg_chkbacklink() returns trigger as '
begin
    if new.backlink isnull then
        new.backlink := '''';
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_PROG_OBJ] create function namedparmcursor_test1(int, int) returns boolean as $$
declare
    c1 cursor (param1 int, param12 int) for select * from rc_test where a > param1 and b > param12;
    nonsense record;
begin
    open c1(param12 := $2, param1 := $1);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$$ language plpgsql
;
----------
[CREATE_TABLE] -- ok

-- test multiple inheritance mismatches
CREATE TABLE gtesty (x int, b int)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30866 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] CREATE INDEX t2_spgist ON s2.t2 USING SPGIST (ir)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS
  SELECT * FROM base_tbl b
  WHERE EXISTS(SELECT 1 FROM ref_tbl r WHERE r.a = b.a)
  WITH CHECK OPTION
;
