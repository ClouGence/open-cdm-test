REASSIGN OWNED BY split_owned_source_a, split_owned_source_b TO split_owned_receiver
;
revoke usage on type textrange1 from regress_multirange_owner
;
DROP USER regress_rls_dob_role1
;
EXECUTE dumpOpr('16808')
;
ALTER DOMAIN public.testboolxmldomain OWNER TO zyc
;
EXECUTE dumpFunc('16563')
;
CREATE STATISTICS tst ON x, x, y FROM ext_stats_test
;
REVOKE USAGE ON FOREIGN DATA WRAPPER foo FROM regress_test_role
;
--
-- Compare results between plans using sorting and plans using hash
-- aggregation. Force spilling in both cases by setting work_mem low.
--

SET work_mem='64kB'
;
ALTER FUNCTION "public"."mr_inoutparam_succeed"(OUT "i" "anyelement", INOUT "r" "anymultirange") OWNER TO zyc
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] REASSIGN OWNED BY split_owned_source_a, split_owned_source_b TO split_owned_receiver
;
----------
[REVOKE] revoke usage on type textrange1 from regress_multirange_owner
;
----------
[DROP_USER] DROP USER regress_rls_dob_role1
;
----------
[UNSAFE] EXECUTE dumpOpr('16808')
;
----------
[TRANSFER_PRIVILEGE] ALTER DOMAIN public.testboolxmldomain OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpFunc('16563')
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS tst ON x, x, y FROM ext_stats_test
;
----------
[REVOKE] REVOKE USAGE ON FOREIGN DATA WRAPPER foo FROM regress_test_role
;
----------
[SESSION_SETTING_WRITE] --
-- Compare results between plans using sorting and plans using hash
-- aggregation. Force spilling in both cases by setting work_mem low.
--

SET work_mem='64kB'
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."mr_inoutparam_succeed"(OUT "i" "anyelement", INOUT "r" "anymultirange") OWNER TO zyc
;
