EXECUTE dumpOpr('3898')
;
LOCK TABLE public.itest9 IN ACCESS SHARE MODE
;
DROP OWNED BY regress_dep_user1
;
ALTER TYPE public.test_type3 OWNER TO zyc
;
COPY public.tableam_tblas_heap2 (f1) TO stdout
;
ALTER USER "current_user" RESET application_name
;
REINDEX TABLE CONCURRENTLY public.test1
;
EXECUTE dumpCompositeType('37081')
;
DROP OWNED BY regress_view_owner_another
;
--
-- The default permissions for functions mean that anyone can execute them.
-- A number of functions shouldn't be executable by just anyone, but rather
-- than use explicit 'superuser()' checks in those functions, we use the GRANT
-- system to REVOKE access to those functions at initdb time.  Administrators
-- can later change who can access these functions, or leave them as only
-- available to superuser / cluster owner, if they choose.
--
REVOKE EXECUTE ON FUNCTION pg_start_backup(text, boolean, boolean) FROM public
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpOpr('3898')
;
----------
[SESSION_LOCK] LOCK TABLE public.itest9 IN ACCESS SHARE MODE
;
----------
[REVOKE] DROP OWNED BY regress_dep_user1
;
----------
[TRANSFER_PRIVILEGE] ALTER TYPE public.test_type3 OWNER TO zyc
;
----------
[DATA_EXPORT] COPY public.tableam_tblas_heap2 (f1) TO stdout
;
----------
[ALTER_USER] ALTER USER "current_user" RESET application_name
;
----------
[ADMIN] REINDEX TABLE CONCURRENTLY public.test1
;
----------
[UNSAFE] EXECUTE dumpCompositeType('37081')
;
----------
[REVOKE] DROP OWNED BY regress_view_owner_another
;
----------
[REVOKE] --
-- The default permissions for functions mean that anyone can execute them.
-- A number of functions shouldn't be executable by just anyone, but rather
-- than use explicit 'superuser()' checks in those functions, we use the GRANT
-- system to REVOKE access to those functions at initdb time.  Administrators
-- can later change who can access these functions, or leave them as only
-- available to superuser / cluster owner, if they choose.
--
REVOKE EXECUTE ON FUNCTION pg_start_backup(text, boolean, boolean) FROM public
;
