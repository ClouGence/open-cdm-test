-- should work
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, 0 AS c FROM viewtest_tbl
;
-- RETURN variable is a different code path ...
create or replace function compos() returns compostype as $$
declare x int := 42;
begin
  return x;
end;
$$ language plpgsql
;
CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 OID = 16384 STRATEGY = FILE_COPY ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
CREATE OPERATOR !~~ (
    PROCEDURE = texticnlike,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = ~~,
    RESTRICT  = icnlikesel,
    JOIN      = icnlikejoinsel
)
;
CREATE VIEW foreign_servers AS
    SELECT foreign_server_catalog,
           foreign_server_name,
           foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           foreign_server_type,
           foreign_server_version,
           authorization_identifier
    FROM _pg_foreign_servers
;
-- at the moment this will not work due to ALTER TABLE inadequacy:
alter table fullname add column suffix text default ''
;
CREATE TABLE "public"."int4_tbl" (
    "f1" integer
)
;
CREATE TABLE fk_partitioned_pk_2 PARTITION OF fk_partitioned_pk FOR VALUES FROM (1000,1000) TO (2000,2000)
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub2
;
CREATE FUNCTION test_lfind32()
	RETURNS pg_catalog.void
	AS '$libdir/test_lfind' LANGUAGE C
;
CREATE TABLE t1 (c1 int, c2 int, c3 int check (c3 < 5), primary key (c1, c2))
;
DROP TEXT SEARCH CONFIGURATION IF EXISTS no_such_schema.foo
;
ANALYZE public.gtest22a
;
ALTER TABLE rf_tbl_abcd_nopk ALTER COLUMN c SET NOT NULL
;
ALTER TABLE a_star RENAME COLUMN aa TO foo
;
CREATE TABLE current_check (currentid int, payload text, rlsuser text)
;
CREATE SEQUENCE tableam_seq_heapx
;
CREATE FUNCTION testUVToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
as $$
$val = ~0;
return $val;
$$
;
CREATE DOMAIN priv_testdomain2b AS priv_testdomain1
;
CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
ALTER TABLE itest8
  ADD COLUMN f22 int NOT NULL,
  ALTER COLUMN f22 ADD GENERATED ALWAYS AS IDENTITY
;
DROP AGGREGATE IF EXISTS test_aggregate_exists(int)
;
DROP TYPE "Colors" CASCADE
;
CREATE TABLE t (id text)
;
create or replace function myfunc(int) returns text as $$
begin
  set work_mem = '2MB';
  perform 1/$1;
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
DROP TYPE person_type RESTRICT
;
CREATE FUNCTION sro_trojan() RETURNS trigger LANGUAGE plpgsql AS
	'BEGIN PERFORM unwanted_grant(); RETURN NULL; END'
;
DROP TABLE TIMESTAMPTZ_TST
;
ANALYZE pg_temp_10.pg_temp_26708
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_statistic_ext_data
;
DROP TABLE tab_main
;
CREATE TABLESPACE tblspc3 LOCATION '/tmp/5tJCEz0j6a/tempdir/superlongname_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
;
CREATE TABLE f_star (
	f 			polygon
) INHERITS (e_star)
;
drop operator class my_op_class using btree
;
CREATE INDEX brin_test_toast_idx ON brintest_3 USING brin (b, c)
;
CREATE TABLE FKTABLE ( ftest1 int DEFAULT 0, ftest2 int, ftest3 int, ftest4 int,  CONSTRAINT constrname3
			FOREIGN KEY(ftest1, ftest2, ftest3) REFERENCES PKTABLE
			ON DELETE SET DEFAULT ON UPDATE SET NULL)
;
drop table float8range_test
;
-- Function for index predicate.
CREATE FUNCTION s.index_row_if(s.citext) RETURNS bool IMMUTABLE
  LANGUAGE SQL AS $$SELECT $1 IS NOT NULL$$
;
VACUUM (PARALLEL 2) pg_catalog.pg_conversion
;
CREATE SCHEMA IF NOT EXISTS newschema1 AUTHORIZATION CURRENT_USER
;
-- Test trigger renaming on partitioned tables
create table grandparent (id int, primary key (id)) partition by range (id)
;
create table atacc2 (id int4 unique)
;
create function multi_datum_use(p1 int) returns bool as $$
declare
  x int;
  y int;
begin
  select into x,y unique1/p1, unique1/$1 from tenk1 group by unique1/p1;
  return x = y;
end$$ language plpgsql
;
CREATE VIEW pg_stat_database AS
    SELECT
            D.oid AS datid,
            D.datname AS datname,
                CASE
                    WHEN (D.oid = (0)::oid) THEN 0
                    ELSE pg_stat_get_db_numbackends(D.oid)
                END AS numbackends,
            pg_stat_get_db_xact_commit(D.oid) AS xact_commit,
            pg_stat_get_db_xact_rollback(D.oid) AS xact_rollback,
            pg_stat_get_db_blocks_fetched(D.oid) -
                    pg_stat_get_db_blocks_hit(D.oid) AS blks_read,
            pg_stat_get_db_blocks_hit(D.oid) AS blks_hit,
            pg_stat_get_db_tuples_returned(D.oid) AS tup_returned,
            pg_stat_get_db_tuples_fetched(D.oid) AS tup_fetched,
            pg_stat_get_db_tuples_inserted(D.oid) AS tup_inserted,
            pg_stat_get_db_tuples_updated(D.oid) AS tup_updated,
            pg_stat_get_db_tuples_deleted(D.oid) AS tup_deleted,
            pg_stat_get_db_conflict_all(D.oid) AS conflicts,
            pg_stat_get_db_temp_files(D.oid) AS temp_files,
            pg_stat_get_db_temp_bytes(D.oid) AS temp_bytes,
            pg_stat_get_db_deadlocks(D.oid) AS deadlocks,
            pg_stat_get_db_checksum_failures(D.oid) AS checksum_failures,
            pg_stat_get_db_checksum_last_failure(D.oid) AS checksum_last_failure,
            pg_stat_get_db_blk_read_time(D.oid) AS blk_read_time,
            pg_stat_get_db_blk_write_time(D.oid) AS blk_write_time,
            pg_stat_get_db_stat_reset_time(D.oid) AS stats_reset
    FROM (
        SELECT 0 AS oid, NULL::name AS datname
        UNION ALL
        SELECT oid, datname FROM pg_database
    ) D
;
ALTER TABLE rewrite_test
        ADD COLUMN notempty4_norewrite int default 42,
        ADD COLUMN empty4 text
;
ALTER TABLE pg_toast.pg_toast_28201 RENAME TO reind_con_toast
;
drop function base_tab_def_view_instrig_func
;
-- size is chosen to exceed page size and trigger actual truncation
CREATE INDEX tbl_gist_idx ON tbl_gist using gist (c4) INCLUDE (c1,c2,c3)
;
CREATE TABLE gtest0 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (55) STORED)
;
ALTER TABLE pg_attrdef ADD PRIMARY KEY USING INDEX pg_attrdef_oid_index
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] -- should work
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, 0 AS c FROM viewtest_tbl
;
----------
[CREATE_PROG_OBJ] -- RETURN variable is a different code path ...
create or replace function compos() returns compostype as $$
declare x int := 42;
begin
  return x;
end;
$$ language plpgsql
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 OID = 16384 STRATEGY = FILE_COPY ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !~~ (
    PROCEDURE = texticnlike,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = ~~,
    RESTRICT  = icnlikesel,
    JOIN      = icnlikejoinsel
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW foreign_servers AS
    SELECT foreign_server_catalog,
           foreign_server_name,
           foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           foreign_server_type,
           foreign_server_version,
           authorization_identifier
    FROM _pg_foreign_servers
;
----------
[ALTER_TABLE|ADD_COLUMN] -- at the moment this will not work due to ALTER TABLE inadequacy:
alter table fullname add column suffix text default ''
;
----------
[CREATE_TABLE] CREATE TABLE "public"."int4_tbl" (
    "f1" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_pk_2 PARTITION OF fk_partitioned_pk FOR VALUES FROM (1000,1000) TO (2000,2000)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=24103 host=/tmp/zpLsC8q0Dk dbname=postgres' PUBLICATION pub2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_lfind32()
	RETURNS pg_catalog.void
	AS '$libdir/test_lfind' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE t1 (c1 int, c2 int, c3 int check (c3 < 5), primary key (c1, c2))
;
----------
[DROP_POLICY] DROP TEXT SEARCH CONFIGURATION IF EXISTS no_such_schema.foo
;
----------
[ADMIN_TABLE] ANALYZE public.gtest22a
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE rf_tbl_abcd_nopk ALTER COLUMN c SET NOT NULL
;
----------
[RENAME_COLUMN] ALTER TABLE a_star RENAME COLUMN aa TO foo
;
----------
[CREATE_TABLE] CREATE TABLE current_check (currentid int, payload text, rlsuser text)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE tableam_seq_heapx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testUVToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
as $$
$val = ~0;
return $val;
$$
;
----------
[CREATE_TYPE] CREATE DOMAIN priv_testdomain2b AS priv_testdomain1
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE itest8
  ADD COLUMN f22 int NOT NULL,
  ALTER COLUMN f22 ADD GENERATED ALWAYS AS IDENTITY
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE IF EXISTS test_aggregate_exists(int)
;
----------
[DROP_TYPE] DROP TYPE "Colors" CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE t (id text)
;
----------
[CREATE_PROG_OBJ] create or replace function myfunc(int) returns text as $$
begin
  set work_mem = '2MB';
  perform 1/$1;
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
----------
[DROP_TYPE] DROP TYPE person_type RESTRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION sro_trojan() RETURNS trigger LANGUAGE plpgsql AS
	'BEGIN PERFORM unwanted_grant(); RETURN NULL; END'
;
----------
[DROP_TABLE] DROP TABLE TIMESTAMPTZ_TST
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26708
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_statistic_ext_data
;
----------
[DROP_TABLE] DROP TABLE tab_main
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc3 LOCATION '/tmp/5tJCEz0j6a/tempdir/superlongname_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
;
----------
[CREATE_TABLE] CREATE TABLE f_star (
	f 			polygon
) INHERITS (e_star)
;
----------
[DROP_PROG_OBJ] drop operator class my_op_class using btree
;
----------
[ADD_INDEX] CREATE INDEX brin_test_toast_idx ON brintest_3 USING brin (b, c)
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE ( ftest1 int DEFAULT 0, ftest2 int, ftest3 int, ftest4 int,  CONSTRAINT constrname3
			FOREIGN KEY(ftest1, ftest2, ftest3) REFERENCES PKTABLE
			ON DELETE SET DEFAULT ON UPDATE SET NULL)
;
----------
[DROP_TABLE] drop table float8range_test
;
----------
[CREATE_PROG_OBJ] -- Function for index predicate.
CREATE FUNCTION s.index_row_if(s.citext) RETURNS bool IMMUTABLE
  LANGUAGE SQL AS $$SELECT $1 IS NOT NULL$$
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_conversion
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema1 AUTHORIZATION CURRENT_USER
;
----------
[CREATE_TABLE] -- Test trigger renaming on partitioned tables
create table grandparent (id int, primary key (id)) partition by range (id)
;
----------
[CREATE_TABLE] create table atacc2 (id int4 unique)
;
----------
[CREATE_PROG_OBJ] create function multi_datum_use(p1 int) returns bool as $$
declare
  x int;
  y int;
begin
  select into x,y unique1/p1, unique1/$1 from tenk1 group by unique1/p1;
  return x = y;
end$$ language plpgsql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_database AS
    SELECT
            D.oid AS datid,
            D.datname AS datname,
                CASE
                    WHEN (D.oid = (0)::oid) THEN 0
                    ELSE pg_stat_get_db_numbackends(D.oid)
                END AS numbackends,
            pg_stat_get_db_xact_commit(D.oid) AS xact_commit,
            pg_stat_get_db_xact_rollback(D.oid) AS xact_rollback,
            pg_stat_get_db_blocks_fetched(D.oid) -
                    pg_stat_get_db_blocks_hit(D.oid) AS blks_read,
            pg_stat_get_db_blocks_hit(D.oid) AS blks_hit,
            pg_stat_get_db_tuples_returned(D.oid) AS tup_returned,
            pg_stat_get_db_tuples_fetched(D.oid) AS tup_fetched,
            pg_stat_get_db_tuples_inserted(D.oid) AS tup_inserted,
            pg_stat_get_db_tuples_updated(D.oid) AS tup_updated,
            pg_stat_get_db_tuples_deleted(D.oid) AS tup_deleted,
            pg_stat_get_db_conflict_all(D.oid) AS conflicts,
            pg_stat_get_db_temp_files(D.oid) AS temp_files,
            pg_stat_get_db_temp_bytes(D.oid) AS temp_bytes,
            pg_stat_get_db_deadlocks(D.oid) AS deadlocks,
            pg_stat_get_db_checksum_failures(D.oid) AS checksum_failures,
            pg_stat_get_db_checksum_last_failure(D.oid) AS checksum_last_failure,
            pg_stat_get_db_blk_read_time(D.oid) AS blk_read_time,
            pg_stat_get_db_blk_write_time(D.oid) AS blk_write_time,
            pg_stat_get_db_stat_reset_time(D.oid) AS stats_reset
    FROM (
        SELECT 0 AS oid, NULL::name AS datname
        UNION ALL
        SELECT oid, datname FROM pg_database
    ) D
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE rewrite_test
        ADD COLUMN notempty4_norewrite int default 42,
        ADD COLUMN empty4 text
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28201 RENAME TO reind_con_toast
;
----------
[DROP_PROG_OBJ] drop function base_tab_def_view_instrig_func
;
----------
[ADD_INDEX] -- size is chosen to exceed page size and trigger actual truncation
CREATE INDEX tbl_gist_idx ON tbl_gist using gist (c4) INCLUDE (c1,c2,c3)
;
----------
[CREATE_TABLE] CREATE TABLE gtest0 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (55) STORED)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_attrdef ADD PRIMARY KEY USING INDEX pg_attrdef_oid_index
;
