-- ERROR
ALTER TABLE lt1 ADD PRIMARY KEY (a)
;
CREATE OPERATOR === (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
ALTER FUNCTION ts_dist(timestamp without time zone, timestamp without time zone) PARALLEL SAFE
;
/*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
-- able to specify column default, column constraint, and table constraint

-- first check the "column specified more than once" error
CREATE TABLE part_b PARTITION OF parted (
	b NOT NULL,
	b DEFAULT 1,
	b CHECK (b >= 0),
	CONSTRAINT check_a CHECK (length(a) > 0)
) FOR VALUES IN ('b')
;
CREATE TABLE "public"."part_p2_p1" (
    "a" integer,
    "b" integer
)
;
CREATE INDEX tid_ind ON tid_tab(a)
;
CREATE TABLE testns.t2 (f1 int)
;
TRUNCATE truncate_a RESTART IDENTITY
;
CREATE CAST (ean13 AS issn13) WITH FUNCTION issn13(ean13)
;
CREATE FOREIGN TABLE tru_pk_ftable (id int)
       SERVER loopback OPTIONS (table_name 'tru_pk_table')
;
CREATE TABLE foo AS SELECT generate_series(1, 10)::int a
;
ALTER TABLE pg_toast.pg_toast_33000 RENAME TO reind_con_toast
;
CREATE TABLE t (f int)
;
CREATE domain test_domain_exists as int not null check (value > 0)
;
CREATE FUNCTION test_destroy()
RETURNS void STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
-- Create the operator class
CREATE OPERATOR CLASS gist_oid_ops
DEFAULT FOR TYPE oid USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_oid_consistent (internal, oid, int2, oid, internal),
	FUNCTION	2	gbt_oid_union (internal, internal),
	FUNCTION	3	gbt_oid_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_oid_penalty (internal, internal, internal),
	FUNCTION	6	gbt_oid_picksplit (internal, internal),
	FUNCTION	7	gbt_oid_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
CREATE AGGREGATE split_agg_probe.count_any (
    SFUNC = int8inc,
    BASETYPE = 'ANY',
    STYPE = bigint,
    INITCOND = '0'
)
;
CREATE FUNCTION texticregexeq(citext, citext)
RETURNS bool AS 'texticregexeq'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE num_typemod_test (
  millions numeric(3, -6),
  thousands numeric(3, -3),
  units numeric(3, 0),
  thousandths numeric(3, 3),
  millionths numeric(3, 6)
)
;
DROP MATERIALIZED VIEW copytest_mv
;
CREATE TABLE gtest11 (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (b * 2) STORED)
;
-- fail, connection string parses, but doesn't work (and does so without
-- connecting, so this is reliable and safe)
CREATE SUBSCRIPTION regress_testsub5 CONNECTION 'port=-1' PUBLICATION testpub
;
alter domain dom drop constraint domchkgt6 restrict
;
create table j2 (id int primary key)
;
-- doesn't exist in referenced table
ALTER TABLE fk_partitioned_fk_2 ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk NOT VALID
;
CREATE TABLE a1 ()
;
--
-- bt_page_stats()
--
CREATE FUNCTION bt_page_stats(IN relname text, IN blkno int4,
    OUT blkno int4,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int4,
    OUT btpo_next int4,
    OUT btpo int4,
    OUT btpo_flags int4)
AS '$libdir/pageinspect', 'bt_page_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE inhz (x text REFERENCES inhz, LIKE inhx INCLUDING INDEXES)
;
COMMENT ON TYPE dump_test.planets
					   IS 'comment on enum type'
;
CREATE TABLE plt2_adv (a int, b int, c text) PARTITION BY LIST (c)
;
DROP FUNCTION stats_test_func2()
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_shdescription
;
truncate table parted
;
ALTER TABLE ref22 ALTER CONSTRAINT ref_f1_f2_fkey
  DEFERRABLE INITIALLY IMMEDIATE
;
CREATE TABLE tableam_parted_1_heapx PARTITION OF tableam_parted_heapx FOR VALUES IN ('a', 'b')
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', quote ':')
;
-- error: reference remains

ALTER TABLE parted_self_fk ATTACH PARTITION part2_self_fk FOR VALUES FROM (10) TO (20)
;
DROP TYPE myvarchar
;
DROP TABLE do_write
;
drop table excluded
;
alter table parted_trigger attach partition parted_trigger_3 for values from (2000) to (3000)
;
-- remove constraint from cc2: no dice, it's inherited
alter table cc2 alter column a2 drop not null
;
ALTER TABLE partitioned SET (fillfactor=100)
;
CREATE TABLE notnull_tbl5_child () INHERITS (notnull_tbl5)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13286 host=/tmp/KJJDhDEf2n dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '30055'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
--
-- Tables and rules for the rule fire order test
--
-- As of PG 7.3, the rules should fire in order by name, regardless
-- of INSTEAD attributes or creation order.
--
create table rtest_order1 (a int4)
;
drop index j1_id2_idx
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] -- ERROR
ALTER TABLE lt1 ADD PRIMARY KEY (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR === (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION ts_dist(timestamp without time zone, timestamp without time zone) PARALLEL SAFE
;
----------
[CREATE_POLICY] /*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
----------
[CREATE_TABLE] -- able to specify column default, column constraint, and table constraint

-- first check the "column specified more than once" error
CREATE TABLE part_b PARTITION OF parted (
	b NOT NULL,
	b DEFAULT 1,
	b CHECK (b >= 0),
	CONSTRAINT check_a CHECK (length(a) > 0)
) FOR VALUES IN ('b')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part_p2_p1" (
    "a" integer,
    "b" integer
)
;
----------
[ADD_INDEX] CREATE INDEX tid_ind ON tid_tab(a)
;
----------
[CREATE_TABLE] CREATE TABLE testns.t2 (f1 int)
;
----------
[TRUNCATE_TABLE] TRUNCATE truncate_a RESTART IDENTITY
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ean13 AS issn13) WITH FUNCTION issn13(ean13)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tru_pk_ftable (id int)
       SERVER loopback OPTIONS (table_name 'tru_pk_table')
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE foo AS SELECT generate_series(1, 10)::int a
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33000 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE t (f int)
;
----------
[CREATE_TYPE] CREATE domain test_domain_exists as int not null check (value > 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_destroy()
RETURNS void STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_oid_ops
DEFAULT FOR TYPE oid USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_oid_consistent (internal, oid, int2, oid, internal),
	FUNCTION	2	gbt_oid_union (internal, internal),
	FUNCTION	3	gbt_oid_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_oid_penalty (internal, internal, internal),
	FUNCTION	6	gbt_oid_picksplit (internal, internal),
	FUNCTION	7	gbt_oid_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE split_agg_probe.count_any (
    SFUNC = int8inc,
    BASETYPE = 'ANY',
    STYPE = bigint,
    INITCOND = '0'
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION texticregexeq(citext, citext)
RETURNS bool AS 'texticregexeq'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE num_typemod_test (
  millions numeric(3, -6),
  thousands numeric(3, -3),
  units numeric(3, 0),
  thousandths numeric(3, 3),
  millionths numeric(3, 6)
)
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW copytest_mv
;
----------
[CREATE_TABLE] CREATE TABLE gtest11 (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (b * 2) STORED)
;
----------
[CREATE_PUB_SUB] -- fail, connection string parses, but doesn't work (and does so without
-- connecting, so this is reliable and safe)
CREATE SUBSCRIPTION regress_testsub5 CONNECTION 'port=-1' PUBLICATION testpub
;
----------
[ALTER_TYPE] alter domain dom drop constraint domchkgt6 restrict
;
----------
[CREATE_TABLE] create table j2 (id int primary key)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- doesn't exist in referenced table
ALTER TABLE fk_partitioned_fk_2 ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE a1 ()
;
----------
[CREATE_PROG_OBJ] --
-- bt_page_stats()
--
CREATE FUNCTION bt_page_stats(IN relname text, IN blkno int4,
    OUT blkno int4,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int4,
    OUT btpo_next int4,
    OUT btpo int4,
    OUT btpo_flags int4)
AS '$libdir/pageinspect', 'bt_page_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE inhz (x text REFERENCES inhz, LIKE inhx INCLUDING INDEXES)
;
----------
[COMMENT_TYPE] COMMENT ON TYPE dump_test.planets
					   IS 'comment on enum type'
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv (a int, b int, c text) PARTITION BY LIST (c)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION stats_test_func2()
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_shdescription
;
----------
[TRUNCATE_TABLE] truncate table parted
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE ref22 ALTER CONSTRAINT ref_f1_f2_fkey
  DEFERRABLE INITIALLY IMMEDIATE
;
----------
[CREATE_TABLE] CREATE TABLE tableam_parted_1_heapx PARTITION OF tableam_parted_heapx FOR VALUES IN ('a', 'b')
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', quote ':')
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- error: reference remains

ALTER TABLE parted_self_fk ATTACH PARTITION part2_self_fk FOR VALUES FROM (10) TO (20)
;
----------
[DROP_TYPE] DROP TYPE myvarchar
;
----------
[DROP_TABLE] DROP TABLE do_write
;
----------
[DROP_TABLE] drop table excluded
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_trigger attach partition parted_trigger_3 for values from (2000) to (3000)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- remove constraint from cc2: no dice, it's inherited
alter table cc2 alter column a2 drop not null
;
----------
[ALTER_TABLE] ALTER TABLE partitioned SET (fillfactor=100)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl5_child () INHERITS (notnull_tbl5)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13286 host=/tmp/KJJDhDEf2n dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '30055'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
----------
[CREATE_TABLE] --
-- Tables and rules for the rule fire order test
--
-- As of PG 7.3, the rules should fire in order by name, regardless
-- of INSTEAD attributes or creation order.
--
create table rtest_order1 (a int4)
;
----------
[DROP_INDEX] drop index j1_id2_idx
;
