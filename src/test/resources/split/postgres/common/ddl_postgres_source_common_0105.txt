CREATE TABLE "public"."gtest_normal" (
    "a" integer,
    "b" integer
)
;
CREATE TABLE invoice (
    year int,
    invoice_number int,
    PRIMARY KEY (year, invoice_number)
  )
;
-- OK
ALTER TEXT SEARCH PARSER alt_ts_prs2 SET SCHEMA alt_nsp2
;
CREATE INDEX "rix" ON "public"."road" USING "btree" ("name")
;
CREATE TABLE s3.t1 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
DROP PUBLICATION testpub8
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE char(10)
;
ALTER INDEX dummy_test_idx SET (option_real = true)
;
DROP INDEX IF EXISTS test_index_exists
;
-- ALTER

ALTER COLLATION "en-x-icu" REFRESH VERSION
;
ALTER TABLE pg_toast.pg_toast_30618 RENAME TO reind_con_toast
;
ALTER MATERIALIZED VIEW mvtest_tvm SET SCHEMA mvtest_mvschema
;
-- WITH CHECK OPTION with no local view qual

CREATE TABLE base_tbl (a int)
;
CREATE PROCEDURE testns.bar() AS 'select 1' LANGUAGE sql
;
ALTER TABLE document ADD COLUMN dnotes text DEFAULT ''
;
CREATE POLICY policy_range_parted ON range_parted for UPDATE USING (true) WITH CHECK (c % 2 = 0)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
ALTER TABLE ONLY "public"."pagg_tab_ml_p3" ATTACH PARTITION "public"."pagg_tab_ml_p3_s2" FOR VALUES FROM (7) TO (10)
;
CREATE INDEX idx_uuid ON test_uuid USING gin (i)
;
-- changing a regular column to identity column in a partitioned table
CREATE TABLE pitest3 (f1 date NOT NULL, f2 text, f3 int) PARTITION BY RANGE (f1)
;
ALTER DATABASE "template0" ALLOW_CONNECTIONS = true
;
CREATE FUNCTION "public"."mlparted5abrtrig_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$ begin new.c = 'b'; return new; end; $$
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN c3 integer
;
ALTER OPERATOR FAMILY alt_opf11 USING gist ADD OPERATOR 1 < (int4, int4) FOR ORDER BY float_ops
;
CREATE TABLE prt2_l_p3 PARTITION OF prt2_l FOR VALUES FROM (500) TO (600) PARTITION BY RANGE (a)
;
CREATE OR REPLACE FUNCTION
  jsonb_strip_nulls(target jsonb, strip_in_arrays boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_strip_nulls'
;
-- basic sequence operations using both text and oid references
CREATE SEQUENCE sequence_test
;
drop trigger child2_insert_trig on child2
;
CREATE TABLE pagg_tab_p3 PARTITION OF pagg_tab FOR VALUES IN ('0009', '0010', '0011')
;
CREATE FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int8,
    OUT level int8,
    OUT fastroot int8,
    OUT fastlevel int8,
    OUT last_cleanup_num_delpages int8,
    OUT last_cleanup_num_tuples float8,
    OUT allequalimage boolean)
AS '$libdir/pageinspect', 'bt_metap'
LANGUAGE C STRICT PARALLEL SAFE
;
create table agg_hash_4 as
select (g/2)::numeric as c1, array_agg(g::numeric) as c2, count(*) as c3
  from agg_data_2k group by g/2
;
create function anyctest(anycompatible)
returns anycompatiblerange as $$
  select $1
$$ language sql
;
--changing column storage should not impact the compression method
--but the data should not be compressed
ALTER TABLE cmdata2 ALTER COLUMN f1 TYPE varchar
;
CREATE TABLESPACE ts1 LOCATION '/tmp/nSf4M2sQOO/ts'
;
CREATE TABLE test_part_b_1 PARTITION OF test_part_b FOR VALUES IN (1,2,3,4,5)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_enum
;
DROP TRIGGER d1_insert_trig ON part_d_1_15
;
/*
 * 5.13
 * CHECK_CONSTRAINT_ROUTINE_USAGE view
 */

CREATE VIEW check_constraint_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name
    FROM pg_namespace nc, pg_constraint c, pg_depend d, pg_proc p, pg_namespace np
    WHERE nc.oid = c.connamespace
      AND c.contype = 'c'
      AND c.oid = d.objid
      AND d.classid = 'pg_catalog.pg_constraint'::regclass
      AND d.refobjid = p.oid
      AND d.refclassid = 'pg_catalog.pg_proc'::regclass
      AND p.pronamespace = np.oid
      AND pg_has_role(p.proowner, 'USAGE')
;
ALTER TEXT SEARCH CONFIGURATION hunspell_tst ALTER MAPPING
	REPLACE hunspell_long WITH hunspell_num
;
ALTER INDEX pg_toast.pg_toast_27590_index RENAME TO reind_con_toast_idx
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "hword_asciipart" WITH "public"."synonym", "english_stem"
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_ts_parser
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_partitioned_table
;
--
-- Test implicit type conversions
-- This fails for Postgres v6.1 (and earlier?)
--  so let's try explicit conversions for now - tgl 97/05/07
--

CREATE TABLE TEMP_FLOAT (f1 FLOAT8)
;
create table idxpart (a int4range, b int4range, c int4range, exclude USING GIST (b with =, c with &&)) partition by range (a)
;
CREATE MATERIALIZED VIEW
						   dump_test.matview_second (col1) AS
						   SELECT * FROM dump_test.matview
;
CREATE TABLE users (id INTEGER PRIMARY KEY,
                       name varchar,
                       sometime timestamp)
;
CREATE PROCEDURE "public"."ptest7"(IN "a" "text", IN "b" "text")
    LANGUAGE "sql"
    AS $$
SELECT a = b;
$$
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "public"."gtest_normal" (
    "a" integer,
    "b" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE invoice (
    year int,
    invoice_number int,
    PRIMARY KEY (year, invoice_number)
  )
;
----------
[ALTER_POLICY] -- OK
ALTER TEXT SEARCH PARSER alt_ts_prs2 SET SCHEMA alt_nsp2
;
----------
[ADD_INDEX] CREATE INDEX "rix" ON "public"."road" USING "btree" ("name")
;
----------
[CREATE_TABLE] CREATE TABLE s3.t1 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub8
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE char(10)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_real = true)
;
----------
[DROP_INDEX] DROP INDEX IF EXISTS test_index_exists
;
----------
[ALTER_POLICY] -- ALTER

ALTER COLLATION "en-x-icu" REFRESH VERSION
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30618 RENAME TO reind_con_toast
;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW mvtest_tvm SET SCHEMA mvtest_mvschema
;
----------
[CREATE_TABLE] -- WITH CHECK OPTION with no local view qual

CREATE TABLE base_tbl (a int)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE testns.bar() AS 'select 1' LANGUAGE sql
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE document ADD COLUMN dnotes text DEFAULT ''
;
----------
[CREATE_POLICY] CREATE POLICY policy_range_parted ON range_parted for UPDATE USING (true) WITH CHECK (c % 2 = 0)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml_p3" ATTACH PARTITION "public"."pagg_tab_ml_p3_s2" FOR VALUES FROM (7) TO (10)
;
----------
[ADD_INDEX] CREATE INDEX idx_uuid ON test_uuid USING gin (i)
;
----------
[CREATE_TABLE] -- changing a regular column to identity column in a partitioned table
CREATE TABLE pitest3 (f1 date NOT NULL, f2 text, f3 int) PARTITION BY RANGE (f1)
;
----------
[ALTER_CATALOG] ALTER DATABASE "template0" ALLOW_CONNECTIONS = true
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."mlparted5abrtrig_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$ begin new.c = 'b'; return new; end; $$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN c3 integer
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf11 USING gist ADD OPERATOR 1 < (int4, int4) FOR ORDER BY float_ops
;
----------
[CREATE_TABLE] CREATE TABLE prt2_l_p3 PARTITION OF prt2_l FOR VALUES FROM (500) TO (600) PARTITION BY RANGE (a)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_strip_nulls(target jsonb, strip_in_arrays boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_strip_nulls'
;
----------
[CREATE_SEQUENCE] -- basic sequence operations using both text and oid references
CREATE SEQUENCE sequence_test
;
----------
[DROP_TRIGGER] drop trigger child2_insert_trig on child2
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_p3 PARTITION OF pagg_tab FOR VALUES IN ('0009', '0010', '0011')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int8,
    OUT level int8,
    OUT fastroot int8,
    OUT fastlevel int8,
    OUT last_cleanup_num_delpages int8,
    OUT last_cleanup_num_tuples float8,
    OUT allequalimage boolean)
AS '$libdir/pageinspect', 'bt_metap'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT] create table agg_hash_4 as
select (g/2)::numeric as c1, array_agg(g::numeric) as c2, count(*) as c3
  from agg_data_2k group by g/2
;
----------
[CREATE_PROG_OBJ] create function anyctest(anycompatible)
returns anycompatiblerange as $$
  select $1
$$ language sql
;
----------
[ALTER_TABLE|ALTER_COLUMN] --changing column storage should not impact the compression method
--but the data should not be compressed
ALTER TABLE cmdata2 ALTER COLUMN f1 TYPE varchar
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/tmp/nSf4M2sQOO/ts'
;
----------
[CREATE_TABLE] CREATE TABLE test_part_b_1 PARTITION OF test_part_b FOR VALUES IN (1,2,3,4,5)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_enum
;
----------
[DROP_TRIGGER] DROP TRIGGER d1_insert_trig ON part_d_1_15
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.13
 * CHECK_CONSTRAINT_ROUTINE_USAGE view
 */

CREATE VIEW check_constraint_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name
    FROM pg_namespace nc, pg_constraint c, pg_depend d, pg_proc p, pg_namespace np
    WHERE nc.oid = c.connamespace
      AND c.contype = 'c'
      AND c.oid = d.objid
      AND d.classid = 'pg_catalog.pg_constraint'::regclass
      AND d.refobjid = p.oid
      AND d.refclassid = 'pg_catalog.pg_proc'::regclass
      AND p.pronamespace = np.oid
      AND pg_has_role(p.proowner, 'USAGE')
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION hunspell_tst ALTER MAPPING
	REPLACE hunspell_long WITH hunspell_num
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27590_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "hword_asciipart" WITH "public"."synonym", "english_stem"
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_ts_parser
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_partitioned_table
;
----------
[CREATE_TABLE] --
-- Test implicit type conversions
-- This fails for Postgres v6.1 (and earlier?)
--  so let's try explicit conversions for now - tgl 97/05/07
--

CREATE TABLE TEMP_FLOAT (f1 FLOAT8)
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range, c int4range, exclude USING GIST (b with =, c with &&)) partition by range (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW
						   dump_test.matview_second (col1) AS
						   SELECT * FROM dump_test.matview
;
----------
[CREATE_TABLE] CREATE TABLE users (id INTEGER PRIMARY KEY,
                       name varchar,
                       sometime timestamp)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest7"(IN "a" "text", IN "b" "text")
    LANGUAGE "sql"
    AS $$
SELECT a = b;
$$
;
