DROP TABLE check_ign_err
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
create temp table copytest (
	style	text,
	test 	text,
	filler	int)
;
create table gloc1 (
  a int,
  b int generated always as (a * 2) stored,
  c int
)
;
DROP FOREIGN TABLE ctl_foreign_table2
;
CREATE TABLE tab_rowfilter_1 (a int primary key, b text)
;
ALTER EXTENSION test_ext_set_schema SET SCHEMA s2
;
TRUNCATE spc_abort
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=14663 host=/tmp/FtJqmHpacG dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
CREATE TYPE "public"."jbpop" AS (
	"a" "text",
	"b" integer,
	"c" timestamp without time zone
)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
create table idxpart22 partition of idxpart2 for values from (15) to (20)
;
CREATE TABLE gtest_part_key (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((gtest_part_key))
;
TRUNCATE test1
;
DROP TABLE pg_temp_18.pg_temp_26820_2, pg_temp_18.pg_temp_26820
;
ALTER TABLE ONLY "regress_indexing"."pk2"
    ADD CONSTRAINT "pk2_pkey" PRIMARY KEY ("a")
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) information_schema.sql_sizing
;
ANALYZE public.gtest2
;
COMMENT ON INDEX comment_test_index IS 'Simple index on comment_test'
;
VACUUM (FREEZE) pg_catalog.pg_foreign_data_wrapper
;
-- Check that batched mode also works for some inserts made during
-- cross-partition updates
CREATE TABLE batch_cp_upd_test (a int) PARTITION BY LIST (a)
;
DROP DOMAIN constraint_comments_dom
;
CLUSTER clstrpart USING clstrpart_only_idx
;
CREATE TABLE uuidtmp (a uuid)
;
CREATE TABLE "public"."int8_tbl" (
    "q1" bigint,
    "q2" bigint
)
;
create table rp_prefix_test3_p2 partition of rp_prefix_test3 for values from (2, 2, 2, 0) to (2, 2, 2, 10)
;
CREATE CAST (boolean AS citext) WITH FUNCTION citext(boolean) AS ASSIGNMENT
;
-- index on system catalog
ALTER TABLE pg_namespace ADD CONSTRAINT foo UNIQUE USING INDEX pg_namespace_nspname_index
;
CREATE FUNCTION btismn13cmp(ismn13, ismn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
--
-- Clean up
--

DROP TABLE t1
;
do $$ <<b>> declare c nested_int8s; begin b.c.x.q1 = 1; end $$
;
CREATE EXTENSION hstore_plpython3u CASCADE
;
DROP TABLE remember_create_subid
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should fail, too many rows
  select * from foo where f1 > 3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26640_2 (tid pg_catalog.tid)
;
create index parted_isvalid_idx on parted_isvalid_tab ((a/b))
;
ALTER TABLE ONLY "fkpart3"."pk3"
    ADD CONSTRAINT "pk3_pkey" PRIMARY KEY ("a")
;
DROP TABLE t2
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_attribute
;
CREATE TABLE part1 (key integer, val text)
;
create index phrase_index_test_idx on phrase_index_test using gin(fts)
;
CREATE TABLE tbl1 (c) AS VALUES ('bar'::text)
;
CREATE AGGREGATE myaggn01b(*) (SFUNC = stfnp, STYPE = int4[],
  INITCOND = '{}')
;
ANALYZE regression_vacuumdb_child
;
DROP TABLE rf_tbl_abcd_part_pk
;
DROP FUNCTION unbounded
;
ALTER TYPE "public"."insenum" ADD VALUE 'i9'
;
CREATE TYPE jsbrec_i_not_null AS (
	i	jsb_int_not_null
)
;
-- Confirm ALTER DOMAIN with RULES.
create table domtab (col1 integer)
;
CREATE TABLE pagg_tab1_p3 PARTITION OF pagg_tab1 FOR VALUES FROM (20) TO (30)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE check_ign_err
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_TABLE] create temp table copytest (
	style	text,
	test 	text,
	filler	int)
;
----------
[CREATE_TABLE] create table gloc1 (
  a int,
  b int generated always as (a * 2) stored,
  c int
)
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ctl_foreign_table2
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_1 (a int primary key, b text)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_ext_set_schema SET SCHEMA s2
;
----------
[TRUNCATE_TABLE] TRUNCATE spc_abort
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=14663 host=/tmp/FtJqmHpacG dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."jbpop" AS (
	"a" "text",
	"b" integer,
	"c" timestamp without time zone
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_TABLE] create table idxpart22 partition of idxpart2 for values from (15) to (20)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_part_key (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((gtest_part_key))
;
----------
[TRUNCATE_TABLE] TRUNCATE test1
;
----------
[DROP_TABLE] DROP TABLE pg_temp_18.pg_temp_26820_2, pg_temp_18.pg_temp_26820
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "regress_indexing"."pk2"
    ADD CONSTRAINT "pk2_pkey" PRIMARY KEY ("a")
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) information_schema.sql_sizing
;
----------
[ADMIN_TABLE] ANALYZE public.gtest2
;
----------
[COMMENT_INDEX] COMMENT ON INDEX comment_test_index IS 'Simple index on comment_test'
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_TABLE] -- Check that batched mode also works for some inserts made during
-- cross-partition updates
CREATE TABLE batch_cp_upd_test (a int) PARTITION BY LIST (a)
;
----------
[DROP_TYPE] DROP DOMAIN constraint_comments_dom
;
----------
[ADMIN_TABLE] CLUSTER clstrpart USING clstrpart_only_idx
;
----------
[CREATE_TABLE] CREATE TABLE uuidtmp (a uuid)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."int8_tbl" (
    "q1" bigint,
    "q2" bigint
)
;
----------
[CREATE_TABLE] create table rp_prefix_test3_p2 partition of rp_prefix_test3 for values from (2, 2, 2, 0) to (2, 2, 2, 10)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (boolean AS citext) WITH FUNCTION citext(boolean) AS ASSIGNMENT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- index on system catalog
ALTER TABLE pg_namespace ADD CONSTRAINT foo UNIQUE USING INDEX pg_namespace_nspname_index
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btismn13cmp(ismn13, ismn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] --
-- Clean up
--

DROP TABLE t1
;
----------
[BLOCK] do $$ <<b>> declare c nested_int8s; begin b.c.x.q1 = 1; end $$
;
----------
[CREATE_LIBRARY] CREATE EXTENSION hstore_plpython3u CASCADE
;
----------
[DROP_TABLE] DROP TABLE remember_create_subid
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should fail, too many rows
  select * from foo where f1 > 3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26640_2 (tid pg_catalog.tid)
;
----------
[ADD_INDEX] create index parted_isvalid_idx on parted_isvalid_tab ((a/b))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart3"."pk3"
    ADD CONSTRAINT "pk3_pkey" PRIMARY KEY ("a")
;
----------
[DROP_TABLE] DROP TABLE t2
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_attribute
;
----------
[CREATE_TABLE] CREATE TABLE part1 (key integer, val text)
;
----------
[ADD_INDEX] create index phrase_index_test_idx on phrase_index_test using gin(fts)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tbl1 (c) AS VALUES ('bar'::text)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn01b(*) (SFUNC = stfnp, STYPE = int4[],
  INITCOND = '{}')
;
----------
[ADMIN_TABLE] ANALYZE regression_vacuumdb_child
;
----------
[DROP_TABLE] DROP TABLE rf_tbl_abcd_part_pk
;
----------
[DROP_PROG_OBJ] DROP FUNCTION unbounded
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i9'
;
----------
[CREATE_TYPE] CREATE TYPE jsbrec_i_not_null AS (
	i	jsb_int_not_null
)
;
----------
[CREATE_TABLE] -- Confirm ALTER DOMAIN with RULES.
create table domtab (col1 integer)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab1_p3 PARTITION OF pagg_tab1 FOR VALUES FROM (20) TO (30)
;
