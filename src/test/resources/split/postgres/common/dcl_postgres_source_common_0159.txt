COPY public.alpha_neg_p1 (a, b, c) TO stdout
;
COMMIT PREPARED 'xact'
;
EXECUTE getDomainConstraints('27181')
;
-- with a single expression, we only enable expression statistics
CREATE STATISTICS ab1_exprstat_2 ON (a+b) FROM ab1
;
EXECUTE dumpAgg('36287')
;
ALTER TABLE regress_roleoption.t1 OWNER TO regress_roleoption_donor
;
ALTER TABLE public.mvtest_tv OWNER TO zyc
;
-- test pg_current_xact_id_if_assigned
BEGIN
;
ALTER TABLE public.mic_inputs OWNER TO zyc
;
EXECUTE dumpOpr('1503')
;
------------------------------------------------------------------------------------------
[DATA_EXPORT] COPY public.alpha_neg_p1 (a, b, c) TO stdout
;
----------
[TRANSACTION] COMMIT PREPARED 'xact'
;
----------
[UNSAFE] EXECUTE getDomainConstraints('27181')
;
----------
[ADMIN_PERFORMANCE] -- with a single expression, we only enable expression statistics
CREATE STATISTICS ab1_exprstat_2 ON (a+b) FROM ab1
;
----------
[UNSAFE] EXECUTE dumpAgg('36287')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE regress_roleoption.t1 OWNER TO regress_roleoption_donor
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.mvtest_tv OWNER TO zyc
;
----------
[TRANSACTION] -- test pg_current_xact_id_if_assigned
BEGIN
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.mic_inputs OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpOpr('1503')
;
