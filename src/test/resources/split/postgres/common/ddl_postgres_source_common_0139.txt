create domain con as integer
;
create table sometable (f1 real, f2 real)
;
drop domain mytype cascade
;
create table rtest_t4 (a int4, b text)
;
ALTER SUBSCRIPTION tap_sub CONNECTION 'port=28775 host=/tmp/A2VbQelPDP dbname=postgres options=''-c log_statement_stats=on'''
;
DROP TABLE IF EXISTS inh_nn1, inh_nn2, inh_nn3, inh_nn4
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
DROP TYPE myvarchar CASCADE
;
ALTER TEXT SEARCH CONFIGURATION tamil ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH tamil_stem
;
CREATE TABLE "public"."plt1_e" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("ltrim"("c", 'A'::"text"))
;
CREATE INDEX t1_btree ON s3.t1 USING BTREE (i)
;
CREATE TABLE z1_blacklist (a int)
;
create table mlparted5_b (d int, b int, c text, a int)
;
ANALYZE public.fk_partitioned_fk_4_2
;
CREATE OR REPLACE FUNCTION test_slru_page_read(bigint, bool DEFAULT true) RETURNS text
  AS '$libdir/test_slru', 'test_slru_page_read' LANGUAGE C
;
CREATE VIEW dump_test.test_view
		                   WITH (check_option = 'local', security_barrier = true) AS
		                   SELECT col1 FROM dump_test.test_table
;
ALTER TABLE pg_event_trigger ADD UNIQUE USING INDEX pg_event_trigger_evtname_index
;
CREATE SUBSCRIPTION tap_sub_B
	CONNECTION 'port=13199 host=/tmp/MRHxhpCYrn dbname=postgres application_name=tap_sub_B'
	PUBLICATION tap_pub_A
	WITH (two_phase = on, streaming = off)
;
CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE VIEW schema_not_existing.view AS SELECT 1
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_tablespace
;
CREATE TABLE tablespace_table (i int) TABLESPACE regress_tblspace
;
DROP TABLE fail_part
;
CREATE FUNCTION gin_extract_hstore_query(hstore, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP TABLE testpub_rf_tbl7
;
alter operator family integer_ops using btree add
  operator 3 = (int8alias2, int8alias2)
;
drop database abc
;
CREATE TABLE "generated_stored_tests"."gtest20a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    "c" double precision DEFAULT "random"(),
    CONSTRAINT "gtest20a_b_check" CHECK (("b" < 61))
)
;
-- Check that LIKE isn't confused by a system catalog of the same name
CREATE TABLE pg_attrdef (LIKE ctlt1 INCLUDING ALL)
;
create function tg_hslot_biu() returns trigger as '
declare
    sname	text;
    xname	HSlot.slotname%TYPE;
    hubrec	record;
begin
    select into hubrec * from Hub where name = new.hubname;
    if not found then
        raise exception ''no manual manipulation of HSlot'';
    end if;
    if new.slotno < 1 or new.slotno > hubrec.nslots then
        raise exception ''no manual manipulation of HSlot'';
    end if;
    if tg_op = ''UPDATE'' and new.hubname != old.hubname then
	if count(*) > 0 from Hub where name = old.hubname then
	    raise exception ''no manual manipulation of HSlot'';
	end if;
    end if;
    sname := ''HS.'' || trim(new.hubname);
    sname := sname || ''.'';
    sname := sname || new.slotno::text;
    if length(sname) > 20 then
        raise exception ''HSlot slotname "%" too long (20 char max)'', sname;
    end if;
    new.slotname := sname;
    return new;
end;
' language plpgsql
;
CREATE TRIGGER "after_upd_b_row_trig" AFTER UPDATE OF "b" ON "public"."main_table" FOR EACH ROW EXECUTE FUNCTION "public"."trigger_func"('after_upd_b_row')
;
-- WITH not supported
CREATE VIEW ro_view8 AS SELECT a, b FROM base_tbl ORDER BY a OFFSET 1
;
-- primary key on child is okay if there's no PK in the parent, though
create table idxpart (a int) partition by range (a)
;
CREATE TABLE test_tab (a int primary key, b bytea)
;
CREATE TABLE "public"."emp1" (
    "id" integer NOT NULL,
    "code" integer
)
;
CREATE MATERIALIZED VIEW "public"."mvtest_tmm" AS
 SELECT "sum"("mvtest_tm"."totamt") AS "grandtot"
   FROM "public"."mvtest_tm"
  WITH NO DATA
;
CREATE TRIGGER "tg_chkbacklink" BEFORE INSERT OR UPDATE ON "public"."pline" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkbacklink"()
;
DROP TABLE gtest11, gtest12
;
ANALYZE public.hash_txt_heap
;
-- test: invalid use of composite variable in scalar-returning function
create or replace function compos() returns int as $$
declare
  v compostype;
begin
  v := (1, 'hello');
  return v;
end;
$$ language plpgsql
;
-- CREATE TABLE AS
-- SELECT queries are normalized, creating matching query IDs.
CREATE TABLE ctas_stats_1 AS SELECT 1 AS a
;
VACUUM (PARALLEL 2) pg_catalog.pg_rewrite
;
CREATE TABLE tab_no_col()
;
-- same thing, but this time try to use an index that's created not in the
-- partition
create table parted_conflict (a int, b text) partition by range (a)
;
-- Verify failures
CREATE TABLE ext_stats_test (x text, y int, z int)
;
ALTER OPERATOR FAMILY gist_timetz_ops USING gist ADD
	FUNCTION	11  (timetz, timetz) gbt_time_sortsupport (internal)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
TRUNCATE will_shrink
;
create foreign table copytest_foreign_table (a int) server copytest_server
;
alter table atacc1 add constraint atacc1_fkey foreign key (a) references atacc1 (a) not valid
;
VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_implementation_info
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] create domain con as integer
;
----------
[CREATE_TABLE] create table sometable (f1 real, f2 real)
;
----------
[DROP_TYPE] drop domain mytype cascade
;
----------
[CREATE_TABLE] create table rtest_t4 (a int4, b text)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub CONNECTION 'port=28775 host=/tmp/A2VbQelPDP dbname=postgres options=''-c log_statement_stats=on'''
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS inh_nn1, inh_nn2, inh_nn3, inh_nn4
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
----------
[DROP_TYPE] DROP TYPE myvarchar CASCADE
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION tamil ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH tamil_stem
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt1_e" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY LIST ("ltrim"("c", 'A'::"text"))
;
----------
[ADD_INDEX] CREATE INDEX t1_btree ON s3.t1 USING BTREE (i)
;
----------
[CREATE_TABLE] CREATE TABLE z1_blacklist (a int)
;
----------
[CREATE_TABLE] create table mlparted5_b (d int, b int, c text, a int)
;
----------
[ADMIN_TABLE] ANALYZE public.fk_partitioned_fk_4_2
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_read(bigint, bool DEFAULT true) RETURNS text
  AS '$libdir/test_slru', 'test_slru_page_read' LANGUAGE C
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW dump_test.test_view
		                   WITH (check_option = 'local', security_barrier = true) AS
		                   SELECT col1 FROM dump_test.test_table
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_event_trigger ADD UNIQUE USING INDEX pg_event_trigger_evtname_index
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B
	CONNECTION 'port=13199 host=/tmp/MRHxhpCYrn dbname=postgres application_name=tap_sub_B'
	PUBLICATION tap_pub_A
	WITH (two_phase = on, streaming = off)
;
----------
[CREATE_SCHEMA(SELECT)] CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE VIEW schema_not_existing.view AS SELECT 1
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_tablespace
;
----------
[CREATE_TABLE] CREATE TABLE tablespace_table (i int) TABLESPACE regress_tblspace
;
----------
[DROP_TABLE] DROP TABLE fail_part
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_hstore_query(hstore, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE testpub_rf_tbl7
;
----------
[ALTER_PROG_OBJ] alter operator family integer_ops using btree add
  operator 3 = (int8alias2, int8alias2)
;
----------
[DROP_CATALOG] drop database abc
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest20a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    "c" double precision DEFAULT "random"(),
    CONSTRAINT "gtest20a_b_check" CHECK (("b" < 61))
)
;
----------
[CREATE_TABLE] -- Check that LIKE isn't confused by a system catalog of the same name
CREATE TABLE pg_attrdef (LIKE ctlt1 INCLUDING ALL)
;
----------
[CREATE_PROG_OBJ] create function tg_hslot_biu() returns trigger as '
declare
    sname	text;
    xname	HSlot.slotname%TYPE;
    hubrec	record;
begin
    select into hubrec * from Hub where name = new.hubname;
    if not found then
        raise exception ''no manual manipulation of HSlot'';
    end if;
    if new.slotno < 1 or new.slotno > hubrec.nslots then
        raise exception ''no manual manipulation of HSlot'';
    end if;
    if tg_op = ''UPDATE'' and new.hubname != old.hubname then
	if count(*) > 0 from Hub where name = old.hubname then
	    raise exception ''no manual manipulation of HSlot'';
	end if;
    end if;
    sname := ''HS.'' || trim(new.hubname);
    sname := sname || ''.'';
    sname := sname || new.slotno::text;
    if length(sname) > 20 then
        raise exception ''HSlot slotname "%" too long (20 char max)'', sname;
    end if;
    new.slotname := sname;
    return new;
end;
' language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "after_upd_b_row_trig" AFTER UPDATE OF "b" ON "public"."main_table" FOR EACH ROW EXECUTE FUNCTION "public"."trigger_func"('after_upd_b_row')
;
----------
[CREATE_VIEW(SELECT)] -- WITH not supported
CREATE VIEW ro_view8 AS SELECT a, b FROM base_tbl ORDER BY a OFFSET 1
;
----------
[CREATE_TABLE] -- primary key on child is okay if there's no PK in the parent, though
create table idxpart (a int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE test_tab (a int primary key, b bytea)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."emp1" (
    "id" integer NOT NULL,
    "code" integer
)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_tmm" AS
 SELECT "sum"("mvtest_tm"."totamt") AS "grandtot"
   FROM "public"."mvtest_tm"
  WITH NO DATA
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkbacklink" BEFORE INSERT OR UPDATE ON "public"."pline" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkbacklink"()
;
----------
[DROP_TABLE] DROP TABLE gtest11, gtest12
;
----------
[ADMIN_TABLE] ANALYZE public.hash_txt_heap
;
----------
[CREATE_PROG_OBJ] -- test: invalid use of composite variable in scalar-returning function
create or replace function compos() returns int as $$
declare
  v compostype;
begin
  v := (1, 'hello');
  return v;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE|SELECT] -- CREATE TABLE AS
-- SELECT queries are normalized, creating matching query IDs.
CREATE TABLE ctas_stats_1 AS SELECT 1 AS a
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_rewrite
;
----------
[CREATE_TABLE] CREATE TABLE tab_no_col()
;
----------
[CREATE_TABLE] -- same thing, but this time try to use an index that's created not in the
-- partition
create table parted_conflict (a int, b text) partition by range (a)
;
----------
[CREATE_TABLE] -- Verify failures
CREATE TABLE ext_stats_test (x text, y int, z int)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timetz_ops USING gist ADD
	FUNCTION	11  (timetz, timetz) gbt_time_sortsupport (internal)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
----------
[TRUNCATE_TABLE] TRUNCATE will_shrink
;
----------
[CREATE_TABLE] create foreign table copytest_foreign_table (a int) server copytest_server
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc1_fkey foreign key (a) references atacc1 (a) not valid
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_implementation_info
;
