ALTER SEQUENCE public.sequence_test10 OWNER TO zyc
;
--
-- test handling of merge clauses using record_ops
--
begin
;
EXECUTE dumpOpr('1615')
;
-- The "bad" case: during execution we need to increase number of
-- batches; in this case we plan for 1 batch, and increase at least a
-- couple of times, and peak memory usage stays within our work_mem
-- budget

-- non-parallel
savepoint settings
;
ALTER TABLE public.identity_dump_logged OWNER TO zyc
;
GRANT pg_read_all_stats TO regress_role_haspriv
;
EXECUTE dumpTableAttach('38389')
;
EXECUTE dumpFunc('20663')
;
-- inferred grantor must be a role with ADMIN OPTION
GRANT regress_priv_user1 TO regress_priv_user2 WITH ADMIN OPTION
;
GRANT pg_read_all_stats TO regress_readallstats
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER SEQUENCE public.sequence_test10 OWNER TO zyc
;
----------
[TRANSACTION] --
-- test handling of merge clauses using record_ops
--
begin
;
----------
[UNSAFE] EXECUTE dumpOpr('1615')
;
----------
[TRANSACTION] -- The "bad" case: during execution we need to increase number of
-- batches; in this case we plan for 1 batch, and increase at least a
-- couple of times, and peak memory usage stays within our work_mem
-- budget

-- non-parallel
savepoint settings
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.identity_dump_logged OWNER TO zyc
;
----------
[GRANT] GRANT pg_read_all_stats TO regress_role_haspriv
;
----------
[UNSAFE] EXECUTE dumpTableAttach('38389')
;
----------
[UNSAFE] EXECUTE dumpFunc('20663')
;
----------
[GRANT] -- inferred grantor must be a role with ADMIN OPTION
GRANT regress_priv_user1 TO regress_priv_user2 WITH ADMIN OPTION
;
----------
[GRANT] GRANT pg_read_all_stats TO regress_readallstats
;
