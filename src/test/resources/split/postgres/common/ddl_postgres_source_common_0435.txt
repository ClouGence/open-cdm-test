drop function anycompatiblerange_anycompatiblemultirange_func(anycompatiblerange, anycompatiblemultirange)
;
CREATE TEMPORARY TABLE inh_temp_child_s1 () INHERITS (inh_parent)
;
ALTER TABLE pg_toast.pg_toast_28143 RENAME TO reind_con_toast
;
-- Permission denied

-- Attempt to gain access using a leaky operator
CREATE FUNCTION op_leak(int, int) RETURNS bool
    AS 'BEGIN RAISE NOTICE ''op_leak => %, %'', $1, $2; RETURN $1 < $2; END'
    LANGUAGE plpgsql
;
ALTER TABLE public.test SET (autovacuum_enabled=false)
;
CREATE TABLE city (
	name		name,
	location 	box,
	budget 		city_budget
)
;
create table rlp4_2 partition of rlp4 for values from (25) to (29)
;
CREATE TABLE t1 (id int not null primary key, a int, junk1 text, b text)
;
COMMENT ON AGGREGATE split_agg_probe.os_sum(ORDER BY integer) IS 'ordered set aggregate'
;
ALTER TABLE fktable ADD CONSTRAINT fk_4_2
FOREIGN KEY (x4) REFERENCES pktable(id2)
;
CREATE TABLE testschema.dflt2 (a int PRIMARY KEY) PARTITION BY LIST (a)
;
DROP PUBLICATION testpub_ins_trunct
;
ALTER TABLE pg_ts_parser ADD UNIQUE USING INDEX pg_ts_parser_prsname_index
;
CREATE PUBLICATION pub1 FOR ALL TABLES
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) information_schema.sql_implementation_info
;
CREATE UNIQUE INDEX ON sj_t3 USING btree (a,id)
;
create table fktable(ftest1 cidr, ftest2 int[], foreign key (ftest1, ftest2) references pktable)
;
create table minmaxtest3() inherits (minmaxtest)
;
CREATE DATABASE tbuiltin3 TEMPLATE template0 LOCALE 'C' LC_COLLATE 'C' LOCALE_PROVIDER builtin
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
create table parted_isvalid_tab_11 partition of parted_isvalid_tab_1
  for values from (1) to (5)
;
ALTER TABLE replication_example ADD COLUMN testcolumn1 int
;
--
-- Tests for list partitioned tables.
--
CREATE TABLE pagg_tab (a int, b int, c text, d int) PARTITION BY LIST(c)
;
CREATE TABLE tas_case WITH ("Fillfactor" = 10) AS SELECT 1 a
;
CREATE TABLE part0 PARTITION OF range_parted2 FOR VALUES FROM (minvalue) TO (1)
;
CREATE DATABASE postgres OID = 5 STRATEGY = file_copy
;
create function testrngfunc() returns setof rngfunc_type as $$
  select 7.136178319899999964, 7.136178319899999964;
$$ language sql immutable
;
-- fails before v13


-- collation assignment

CREATE PROCEDURE ptest7(a text, b text)
LANGUAGE SQL
AS $$
SELECT a = b;
$$
;
CREATE TABLE rec2 (a integer, b integer)
;
create trigger trg1 after insert on trigpart3 for each row execute procedure trigger_nothing()
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_rewrite
;
DISCARD TEMPORARY
;
DROP TABLE like_constraint_rename_cache
;
DROP FUNCTION skip_merge_op()
;
drop table domdeftest
;
CREATE VIEW "public"."test_tablesample_v1" AS
 SELECT "test_tablesample"."id"
   FROM "public"."test_tablesample" TABLESAMPLE "system" ((10 * 2)) REPEATABLE (2)
;
CREATE FUNCTION isn_weak(boolean)
	RETURNS boolean
	AS '$libdir/isn', 'accept_weak_input'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL RESTRICTED
;
CREATE FUNCTION "public"."refcursor_test2"(integer, integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
    nonsense record;
begin
    open c1($1, $2);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$_$
;
ALTER INDEX pg_toast.pg_toast_30711_index RENAME TO reind_con_toast_idx
;
CREATE CAST (hstore AS json)
  WITH FUNCTION hstore_to_json(hstore)
;
ALTER OPERATOR === (boolean, boolean) SET (COMMUTATOR = ====)
;
ANALYZE public.test_tsvector
;
ALTER FUNCTION gbt_cash_penalty(internal, internal, internal) PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_27825 RENAME TO reind_con_toast
;
--
-- table functions
--

CREATE OR REPLACE FUNCTION rngfunc()
RETURNS TABLE(a int)
AS $$ SELECT a FROM generate_series(1,5) a(a) $$ LANGUAGE sql
;
CREATE TEMP TABLE FKTABLE (ftest1 int8)
;
ANALYZE public.prt4_n_p2
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26642_2 (tid pg_catalog.tid)
;
CREATE TYPE "public"."int42" (
    INTERNALLENGTH = 4,
    INPUT = "public"."int42_in",
    OUTPUT = "public"."int42_out",
    DEFAULT = '42',
    ALIGNMENT = int4,
    STORAGE = plain,
    PASSEDBYVALUE
)
;
ALTER TABLE ATACC2 DROP CONSTRAINT a_is_not_null
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] drop function anycompatiblerange_anycompatiblemultirange_func(anycompatiblerange, anycompatiblemultirange)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE inh_temp_child_s1 () INHERITS (inh_parent)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28143 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] -- Permission denied

-- Attempt to gain access using a leaky operator
CREATE FUNCTION op_leak(int, int) RETURNS bool
    AS 'BEGIN RAISE NOTICE ''op_leak => %, %'', $1, $2; RETURN $1 < $2; END'
    LANGUAGE plpgsql
;
----------
[ALTER_TABLE] ALTER TABLE public.test SET (autovacuum_enabled=false)
;
----------
[CREATE_TABLE] CREATE TABLE city (
	name		name,
	location 	box,
	budget 		city_budget
)
;
----------
[CREATE_TABLE] create table rlp4_2 partition of rlp4 for values from (25) to (29)
;
----------
[CREATE_TABLE] CREATE TABLE t1 (id int not null primary key, a int, junk1 text, b text)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE split_agg_probe.os_sum(ORDER BY integer) IS 'ordered set aggregate'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_4_2
FOREIGN KEY (x4) REFERENCES pktable(id2)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.dflt2 (a int PRIMARY KEY) PARTITION BY LIST (a)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_ins_trunct
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_parser ADD UNIQUE USING INDEX pg_ts_parser_prsname_index
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub1 FOR ALL TABLES
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) information_schema.sql_implementation_info
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON sj_t3 USING btree (a,id)
;
----------
[CREATE_TABLE] create table fktable(ftest1 cidr, ftest2 int[], foreign key (ftest1, ftest2) references pktable)
;
----------
[CREATE_TABLE] create table minmaxtest3() inherits (minmaxtest)
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin3 TEMPLATE template0 LOCALE 'C' LC_COLLATE 'C' LOCALE_PROVIDER builtin
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
----------
[CREATE_TABLE] create table parted_isvalid_tab_11 partition of parted_isvalid_tab_1
  for values from (1) to (5)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE replication_example ADD COLUMN testcolumn1 int
;
----------
[CREATE_TABLE] --
-- Tests for list partitioned tables.
--
CREATE TABLE pagg_tab (a int, b int, c text, d int) PARTITION BY LIST(c)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tas_case WITH ("Fillfactor" = 10) AS SELECT 1 a
;
----------
[CREATE_TABLE] CREATE TABLE part0 PARTITION OF range_parted2 FOR VALUES FROM (minvalue) TO (1)
;
----------
[CREATE_CATALOG] CREATE DATABASE postgres OID = 5 STRATEGY = file_copy
;
----------
[CREATE_PROG_OBJ] create function testrngfunc() returns setof rngfunc_type as $$
  select 7.136178319899999964, 7.136178319899999964;
$$ language sql immutable
;
----------
[CREATE_PROG_OBJ] -- fails before v13


-- collation assignment

CREATE PROCEDURE ptest7(a text, b text)
LANGUAGE SQL
AS $$
SELECT a = b;
$$
;
----------
[CREATE_TABLE] CREATE TABLE rec2 (a integer, b integer)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trg1 after insert on trigpart3 for each row execute procedure trigger_nothing()
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_rewrite
;
----------
[DROP_TABLE] DISCARD TEMPORARY
;
----------
[DROP_TABLE] DROP TABLE like_constraint_rename_cache
;
----------
[DROP_PROG_OBJ] DROP FUNCTION skip_merge_op()
;
----------
[DROP_TABLE] drop table domdeftest
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."test_tablesample_v1" AS
 SELECT "test_tablesample"."id"
   FROM "public"."test_tablesample" TABLESAMPLE "system" ((10 * 2)) REPEATABLE (2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isn_weak(boolean)
	RETURNS boolean
	AS '$libdir/isn', 'accept_weak_input'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."refcursor_test2"(integer, integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
    nonsense record;
begin
    open c1($1, $2);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$_$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30711_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE CAST (hstore AS json)
  WITH FUNCTION hstore_to_json(hstore)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, boolean) SET (COMMUTATOR = ====)
;
----------
[ADMIN_TABLE] ANALYZE public.test_tsvector
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_cash_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27825 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] --
-- table functions
--

CREATE OR REPLACE FUNCTION rngfunc()
RETURNS TABLE(a int)
AS $$ SELECT a FROM generate_series(1,5) a(a) $$ LANGUAGE sql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE FKTABLE (ftest1 int8)
;
----------
[ADMIN_TABLE] ANALYZE public.prt4_n_p2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26642_2 (tid pg_catalog.tid)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."int42" (
    INTERNALLENGTH = 4,
    INPUT = "public"."int42_in",
    OUTPUT = "public"."int42_out",
    DEFAULT = '42',
    ALIGNMENT = int4,
    STORAGE = plain,
    PASSEDBYVALUE
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE ATACC2 DROP CONSTRAINT a_is_not_null
;
