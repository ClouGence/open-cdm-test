VACUUM (PARALLEL 0) pg_catalog.pg_range
;
DROP TABLE test_float
;
CREATE TABLE ref22 PARTITION OF ref2 FOR VALUES IN (2)
;
ALTER FUNCTION gbt_cash_distance(internal, money, smallint, oid, internal) PARALLEL SAFE
;
DROP TRIGGER IF EXISTS test_trigger_exists ON test_exists
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "int" WITH "simple"
;
CREATE TEMPORARY TABLE result (t timestamptz)
;
CREATE FUNCTION "public"."end_command"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
RAISE NOTICE '% - ddl_command_end', tg_tag;
END;
$$
;
CREATE FUNCTION isnne(issn13, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_statistic_ext
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) information_schema.sql_parts
;
DROP TABLE min_updates_test
;
CREATE TABLE mvtest_foo(a, b, c) AS VALUES(1, 2, 3)
;
CREATE TRIGGER merge_bsu BEFORE UPDATE ON target FOR EACH STATEMENT EXECUTE PROCEDURE merge_trigfunc ()
;
CREATE FUNCTION "public"."hobbies_by_name"("text") RETURNS "text"
    LANGUAGE "sql"
    AS $_$select person from hobbies_r where name = $1$_$
;
CREATE TYPE "public"."textrange" AS RANGE (
    subtype = "text",
    multirange_type_name = "public"."textmultirange",
    collation = "pg_catalog"."C"
)
;
alter table uv_pt11 add a int
;
-- error

-- PROCESS_MAIN option
-- Only the toast table is processed.
VACUUM (PROCESS_MAIN FALSE) vac_option_tab
;
drop function returnqueryf()
;
--
-- CREATE IF NOT EXISTS is an entirely unsound thing for an extension
-- to be doing, but let's at least plug the major security hole in it.
--

CREATE COLLATION ext_cine_coll
  ( LC_COLLATE = "C", LC_CTYPE = "C" )
;
DROP SCHEMA schema_one, schema_two CASCADE
;
CREATE FUNCTION testns.priv_testfunc(int) RETURNS int AS 'select 3 * $1;' LANGUAGE sql
;
CREATE TABLE test1 (a int8, b text)
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc3(1) WITH ORDINALITY AS t1(v,o)
;
create function simplesql(int) returns int language sql
as 'select $1'
;
VACUUM smalltbl
;
VACUUM (FREEZE) pg_catalog.pg_foreign_table
;
ALTER TABLE ONLY "public"."gtest26"
    ADD CONSTRAINT "gtest26_pkey" PRIMARY KEY ("a")
;
create table depth_a (id int not null primary key)
;
-- renaming sequences
CREATE SEQUENCE foo_seq
;
-- failed (not owner)
ALTER AGGREGATE alt_agg2(int) SET SCHEMA alt_nsp2
;
CREATE TABLE current_check_2 () INHERITS (current_check)
;
-- fails, incompatible list:
CREATE OR REPLACE RULE voo_i AS ON INSERT TO voo DO INSTEAD
  INSERT INTO foo VALUES(new.*, 57) RETURNING *
;
-- 100 distinct combinations, all in the MCV list, but with expressions
TRUNCATE mcv_lists
;
DROP TABLE no_index_cleanup
;
CREATE MATERIALIZED VIEW addr_nsp.genmatview AS SELECT * FROM addr_nsp.gentable
;
ALTER SUBSCRIPTION sub2 SET PUBLICATION pub_lower_level, pub_all
;
TRUNCATE public.tru_rtable_parent, public.tru_rtable_child CONTINUE IDENTITY RESTRICT
;
-- Check type create with input/output incompatibility
CREATE TYPE not_existing_type (INPUT = array_in,
    OUTPUT = array_out,
    ELEMENT = int,
    INTERNALLENGTH = 32)
;
-- make column identity in child table
CREATE TABLE itest7b (a int)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest1"
    ADD CONSTRAINT "gtest1_pkey" PRIMARY KEY ("a")
;
ALTER FUNCTION gbt_ts_distance(internal, timestamp without time zone, smallint, oid, internal) PARALLEL SAFE
;
VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_sizing
;
-- Decoding works in transaction that issues DDL
--
-- We had issues handling relcache invalidations with these, see
-- https://www.postgresql.org/message-id/e56be7d9-14b1-664d-0bfc-00ce9772721c@gmail.com
CREATE TABLE tbl_created_outside_xact(id SERIAL PRIMARY KEY)
;
ALTER TABLE parent ADD CONSTRAINT check_a CHECK (a > 0)
;
ALTER SEQUENCE sequence_test11 AS int
;
-- This should fail, because we'd have to cast numeric to int which is
-- not an implicit coercion (or use numeric=numeric, but that's not part
-- of the integer opfamily)
CREATE TEMP TABLE FKTABLE (ftest1 numeric)
;
ALTER TABLE atnnparted VALIDATE CONSTRAINT dummy_constr
;
DROP OPERATOR IF EXISTS # (NONE, no_such_schema.no_such_type)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_range
;
----------
[DROP_TABLE] DROP TABLE test_float
;
----------
[CREATE_TABLE] CREATE TABLE ref22 PARTITION OF ref2 FOR VALUES IN (2)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_cash_distance(internal, money, smallint, oid, internal) PARALLEL SAFE
;
----------
[DROP_TRIGGER] DROP TRIGGER IF EXISTS test_trigger_exists ON test_exists
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "int" WITH "simple"
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE result (t timestamptz)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."end_command"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
RAISE NOTICE '% - ddl_command_end', tg_tag;
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(issn13, ean13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_statistic_ext
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) information_schema.sql_parts
;
----------
[DROP_TABLE] DROP TABLE min_updates_test
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE mvtest_foo(a, b, c) AS VALUES(1, 2, 3)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER merge_bsu BEFORE UPDATE ON target FOR EACH STATEMENT EXECUTE PROCEDURE merge_trigfunc ()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."hobbies_by_name"("text") RETURNS "text"
    LANGUAGE "sql"
    AS $_$select person from hobbies_r where name = $1$_$
;
----------
[CREATE_POLICY] CREATE TYPE "public"."textrange" AS RANGE (
    subtype = "text",
    multirange_type_name = "public"."textmultirange",
    collation = "pg_catalog"."C"
)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table uv_pt11 add a int
;
----------
[ADMIN_TABLE] -- error

-- PROCESS_MAIN option
-- Only the toast table is processed.
VACUUM (PROCESS_MAIN FALSE) vac_option_tab
;
----------
[DROP_PROG_OBJ] drop function returnqueryf()
;
----------
[CREATE_POLICY] --
-- CREATE IF NOT EXISTS is an entirely unsound thing for an extension
-- to be doing, but let's at least plug the major security hole in it.
--

CREATE COLLATION ext_cine_coll
  ( LC_COLLATE = "C", LC_CTYPE = "C" )
;
----------
[DROP_SCHEMA] DROP SCHEMA schema_one, schema_two CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testns.priv_testfunc(int) RETURNS int AS 'select 3 * $1;' LANGUAGE sql
;
----------
[CREATE_TABLE] CREATE TABLE test1 (a int8, b text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc3(1) WITH ORDINALITY AS t1(v,o)
;
----------
[CREATE_PROG_OBJ] create function simplesql(int) returns int language sql
as 'select $1'
;
----------
[ADMIN_TABLE] VACUUM smalltbl
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_foreign_table
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest26"
    ADD CONSTRAINT "gtest26_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] create table depth_a (id int not null primary key)
;
----------
[CREATE_SEQUENCE] -- renaming sequences
CREATE SEQUENCE foo_seq
;
----------
[ALTER_PROG_OBJ] -- failed (not owner)
ALTER AGGREGATE alt_agg2(int) SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] CREATE TABLE current_check_2 () INHERITS (current_check)
;
----------
[CREATE_POLICY] -- fails, incompatible list:
CREATE OR REPLACE RULE voo_i AS ON INSERT TO voo DO INSTEAD
  INSERT INTO foo VALUES(new.*, 57) RETURNING *
;
----------
[TRUNCATE_TABLE] -- 100 distinct combinations, all in the MCV list, but with expressions
TRUNCATE mcv_lists
;
----------
[DROP_TABLE] DROP TABLE no_index_cleanup
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW addr_nsp.genmatview AS SELECT * FROM addr_nsp.gentable
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION sub2 SET PUBLICATION pub_lower_level, pub_all
;
----------
[TRUNCATE_TABLE] TRUNCATE public.tru_rtable_parent, public.tru_rtable_child CONTINUE IDENTITY RESTRICT
;
----------
[CREATE_TYPE] -- Check type create with input/output incompatibility
CREATE TYPE not_existing_type (INPUT = array_in,
    OUTPUT = array_out,
    ELEMENT = int,
    INTERNALLENGTH = 32)
;
----------
[CREATE_TABLE] -- make column identity in child table
CREATE TABLE itest7b (a int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest1"
    ADD CONSTRAINT "gtest1_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_ts_distance(internal, timestamp without time zone, smallint, oid, internal) PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_sizing
;
----------
[CREATE_TABLE] -- Decoding works in transaction that issues DDL
--
-- We had issues handling relcache invalidations with these, see
-- https://www.postgresql.org/message-id/e56be7d9-14b1-664d-0bfc-00ce9772721c@gmail.com
CREATE TABLE tbl_created_outside_xact(id SERIAL PRIMARY KEY)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE parent ADD CONSTRAINT check_a CHECK (a > 0)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test11 AS int
;
----------
[CREATE_TABLE] -- This should fail, because we'd have to cast numeric to int which is
-- not an implicit coercion (or use numeric=numeric, but that's not part
-- of the integer opfamily)
CREATE TEMP TABLE FKTABLE (ftest1 numeric)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE atnnparted VALIDATE CONSTRAINT dummy_constr
;
----------
[DROP_PROG_OBJ] DROP OPERATOR IF EXISTS # (NONE, no_such_schema.no_such_type)
;
