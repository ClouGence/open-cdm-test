insert into rtest_t4 values (28, 'Record should go to rtest_t4 and t8')
;
insert into insertconflict values (1, 2)
on conflict (b) where coalesce(a, 1) > 1 do nothing
;
INSERT INTO c2 SELECT 1, a / 3, a % 3 FROM generate_series(0, 9) a
;
insert into tb values(4,2)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15914', relminmxid = '3'
WHERE oid = '38834'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '17607'
;
INSERT INTO join_tbl SELECT * FROM async_pt t1, async_pt t2 WHERE t1.a = t2.a AND t1.b = t2.b AND t1.b % 100 = 0
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18786', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo1"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"generated_stored_tests"."gtest1_1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
UPDATE FKTABLE SET ftest1 = 1 WHERE ftest1 = 1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9563', relminmxid = '1'
WHERE oid = '"public"."rtest_interface"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part_rev"'::pg_catalog.regclass
  AND attname IN ('b', 'a')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt1_l_p2"'::pg_catalog.regclass
;
INSERT INTO num_exp_add VALUES (2,2,'-1989755052005613745508684297498482.08706046903733180774109918349472259002621360561646766662015292612487081906999481230493166798592668478219872672892569606041287164205736495714018988279070019145481242576461480779090962790')
;
insert into inserttesta (f2[1], f2[2]) values (1,default)
;
INSERT INTO TIMESTAMP_TBL VALUES ('Dec 30 17:32:01 1996')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18436', relminmxid = '3'
WHERE oid = '"public"."part_rp"'::pg_catalog.regclass
;
INSERT INTO num_exp_sub VALUES (3,0,'4.31')
;
UPDATE r1 SET a = 30 RETURNING *
;
INSERT INTO PRIMARY_TBL (t) VALUES ('six')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15658', relminmxid = '3'
WHERE oid = '36675'
;
INSERT INTO public.ths_p1 VALUES (8, 428)
;
-- routed to mcparted1
insert into mcrparted values (9, 1000, 1)
;
insert into insertconflicttest as i values (23, 'Jackfruit') on conflict (key) do update set fruit = excluded.fruit
  where i.* = excluded.* returning *
;
-- set column with a default 1 to NULL, due to a bug that wasn't
-- noticed has heap_getattr buggily returned NULL for default columns
UPDATE t SET a = NULL
;
-- Fine (we INSERT, so "cid = 33" ("technology") isn't evaluated):
INSERT INTO document VALUES (78, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'some technology novel')
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle, cid = 33 RETURNING *
;
INSERT INTO public.tplain VALUES ('3', 253)
;
INSERT INTO BOOLTBL3 (d, b, o) VALUES ('null', null, 3)
;
INSERT INTO test_io_vac_strategy SELECT i, i from generate_series(1, 4500)i
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."part_p1"'::pg_catalog.regclass
;
INSERT into non_parted VALUES (1), (1), (1), (2), (2), (2), (3), (3), (3)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4815', relminmxid = '1'
WHERE oid = '"public"."invalid_check_con"'::pg_catalog.regclass
;
INSERT INTO public.bar VALUES ('in both')
;
DELETE FROM public.mvtest_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_10.pg_temp_26213_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."gtest10"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10574', relminmxid = '1'
WHERE oid = '"public"."ec0"'::pg_catalog.regclass
;
-- compressed external toast data
INSERT INTO xpto (toasted_col2) SELECT repeat(string_agg(to_char(g.i, 'FM0000'), ''), 50) FROM generate_series(1, 500) g(i)
;
UPDATE pgbench_branches SET bbalance = bbalance + -611 WHERE bid = 1
;
INSERT INTO pht1_e SELECT i, i, 'A' || to_char(i/50, 'FM0000') FROM generate_series(0, 299, 2) i
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10394', relminmxid = '3'
WHERE oid = '"fkpart3"."fk1"'::pg_catalog.regclass
;
INSERT INTO t1 VALUES ( 'bb', 11 )
;
-- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '730', datminmxid = '1'
WHERE datname = 'template1'
;
insert into PSlot values ('PS.first.ta5', 'PF1_2', '', '')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."beta_neg"'::pg_catalog.regclass
;
-- inference fails:
insert into insertconflicttest values (9, 'Banana') on conflict (key) do update set fruit = excluded.fruit
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14835', relminmxid = '3'
WHERE oid = '"public"."test_type_diff2_c3"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '26062'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12729', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_2_1"'::pg_catalog.regclass
;
update arrtest1 set i[-5:-3] = array[-15,-14,-13], t[-5:-3] = array['m15','m14','m13']
;
INSERT INTO rls_tbl
  SELECT * FROM (SELECT b, c FROM rls_tbl ORDER BY a) ss
;
------------------------------------------------------------------------------------------
[INSERT] insert into rtest_t4 values (28, 'Record should go to rtest_t4 and t8')
;
----------
[MERGE] insert into insertconflict values (1, 2)
on conflict (b) where coalesce(a, 1) > 1 do nothing
;
----------
[INSERT|SELECT] INSERT INTO c2 SELECT 1, a / 3, a % 3 FROM generate_series(0, 9) a
;
----------
[INSERT] insert into tb values(4,2)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15914', relminmxid = '3'
WHERE oid = '38834'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '17607'
;
----------
[INSERT|SELECT] INSERT INTO join_tbl SELECT * FROM async_pt t1, async_pt t2 WHERE t1.a = t2.a AND t1.b = t2.b AND t1.b % 100 = 0
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18786', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"generated_stored_tests"."gtest1_1"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[UPDATE] UPDATE FKTABLE SET ftest1 = 1 WHERE ftest1 = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9563', relminmxid = '1'
WHERE oid = '"public"."rtest_interface"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part_rev"'::pg_catalog.regclass
  AND attname IN ('b', 'a')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt1_l_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (2,2,'-1989755052005613745508684297498482.08706046903733180774109918349472259002621360561646766662015292612487081906999481230493166798592668478219872672892569606041287164205736495714018988279070019145481242576461480779090962790')
;
----------
[INSERT|SELECT] insert into inserttesta (f2[1], f2[2]) values (1,default)
;
----------
[INSERT] INSERT INTO TIMESTAMP_TBL VALUES ('Dec 30 17:32:01 1996')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '18436', relminmxid = '3'
WHERE oid = '"public"."part_rp"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (3,0,'4.31')
;
----------
[UPDATE] UPDATE r1 SET a = 30 RETURNING *
;
----------
[INSERT|SELECT] INSERT INTO PRIMARY_TBL (t) VALUES ('six')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15658', relminmxid = '3'
WHERE oid = '36675'
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 428)
;
----------
[INSERT] -- routed to mcparted1
insert into mcrparted values (9, 1000, 1)
;
----------
[MERGE] insert into insertconflicttest as i values (23, 'Jackfruit') on conflict (key) do update set fruit = excluded.fruit
  where i.* = excluded.* returning *
;
----------
[UPDATE] -- set column with a default 1 to NULL, due to a bug that wasn't
-- noticed has heap_getattr buggily returned NULL for default columns
UPDATE t SET a = NULL
;
----------
[MERGE|SELECT] -- Fine (we INSERT, so "cid = 33" ("technology") isn't evaluated):
INSERT INTO document VALUES (78, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'some technology novel')
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle, cid = 33 RETURNING *
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 253)
;
----------
[INSERT|SELECT] INSERT INTO BOOLTBL3 (d, b, o) VALUES ('null', null, 3)
;
----------
[INSERT|SELECT] INSERT INTO test_io_vac_strategy SELECT i, i from generate_series(1, 4500)i
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."part_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT into non_parted VALUES (1), (1), (1), (2), (2), (2), (3), (3), (3)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4815', relminmxid = '1'
WHERE oid = '"public"."invalid_check_con"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.bar VALUES ('in both')
;
----------
[DELETE|SELECT] DELETE FROM public.mvtest_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_10.pg_temp_26213_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."gtest10"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10574', relminmxid = '1'
WHERE oid = '"public"."ec0"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- compressed external toast data
INSERT INTO xpto (toasted_col2) SELECT repeat(string_agg(to_char(g.i, 'FM0000'), ''), 50) FROM generate_series(1, 500) g(i)
;
----------
[UPDATE] UPDATE pgbench_branches SET bbalance = bbalance + -611 WHERE bid = 1
;
----------
[INSERT|SELECT] INSERT INTO pht1_e SELECT i, i, 'A' || to_char(i/50, 'FM0000') FROM generate_series(0, 299, 2) i
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10394', relminmxid = '3'
WHERE oid = '"fkpart3"."fk1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO t1 VALUES ( 'bb', 11 )
;
----------
[UPDATE|METADATA] -- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '730', datminmxid = '1'
WHERE datname = 'template1'
;
----------
[INSERT] insert into PSlot values ('PS.first.ta5', 'PF1_2', '', '')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."beta_neg"'::pg_catalog.regclass
;
----------
[MERGE] -- inference fails:
insert into insertconflicttest values (9, 'Banana') on conflict (key) do update set fruit = excluded.fruit
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14835', relminmxid = '3'
WHERE oid = '"public"."test_type_diff2_c3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '26062'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12729', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_2_1"'::pg_catalog.regclass
;
----------
[UPDATE] update arrtest1 set i[-5:-3] = array[-15,-14,-13], t[-5:-3] = array['m15','m14','m13']
;
----------
[INSERT|SELECT] INSERT INTO rls_tbl
  SELECT * FROM (SELECT b, c FROM rls_tbl ORDER BY a) ss
;
