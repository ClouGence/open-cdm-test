CREATE TABLE "public"."fk_partitioned_fk_1" (
    "a" integer DEFAULT 2501,
    "b" integer DEFAULT 142857
)
;
CREATE FUNCTION isnle(isbn13, isbn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE OR REPLACE FUNCTION test_slru_page_write(bigint, text) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_write' LANGUAGE C
;
-- It's possible for the same EXISTS to get resolved both ways
create temp table exists_tbl (c1 int, c2 int, c3 int) partition by list (c1)
;
CREATE TRIGGER "after_upd_b_stmt_trig" AFTER UPDATE OF "b" ON "public"."main_table" FOR EACH STATEMENT EXECUTE FUNCTION "public"."trigger_func"('after_upd_b_stmt')
;
-- The following views have dependencies that force them to appear out of order.

/*
 * 5.25
 * DATA_TYPE_PRIVILEGES view
 */

CREATE VIEW data_type_privileges AS
    SELECT CAST(current_database() AS sql_identifier) AS object_catalog,
           CAST(x.objschema AS sql_identifier) AS object_schema,
           CAST(x.objname AS sql_identifier) AS object_name,
           CAST(x.objtype AS character_data) AS object_type,
           CAST(x.objdtdid AS sql_identifier) AS dtd_identifier

    FROM
      (
        SELECT udt_schema, udt_name, 'USER-DEFINED TYPE'::text, dtd_identifier FROM attributes
        UNION ALL
        SELECT table_schema, table_name, 'TABLE'::text, dtd_identifier FROM columns
        UNION ALL
        SELECT domain_schema, domain_name, 'DOMAIN'::text, dtd_identifier FROM domains
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM parameters
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM routines
      ) AS x (objschema, objname, objtype, objdtdid)
;
ALTER TEXT SEARCH CONFIGURATION hunspell_tst ALTER MAPPING
	REPLACE hunspell WITH hunspell_long
;
ALTER SUBSCRIPTION regress_testsub SET (two_phase = false)
;
-- simple queries
CREATE TABLE result_tbl (a int, b int, c text)
;
--
-- Test basic SET SCHEMA handling.
--
CREATE SCHEMA s1
;
create table some_tab (a int, b int)
;
create table pc_list_parted (a int) partition by list(a)
;
CREATE OPERATOR !~~* (
    PROCEDURE = texticnlike,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = ~~*,
    RESTRICT  = icnlikesel,
    JOIN      = icnlikejoinsel
)
;
create function bogus_func(anyelement)
  returns anymultirange as 'select int4multirange(int4range(1,10))' language sql
;
DROP TABLE nummultirange_test2
;
CREATE FUNCTION modify_rel_block(rel regclass, blockno int,
  zero bool DEFAULT false,
  corrupt_header bool DEFAULT false,
  corrupt_checksum bool DEFAULT false)
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE DOMAIN domain2 AS int DEFAULT random(min=>10, max=>100)
;
ALTER VIEW dump_test.test_view ALTER COLUMN col1 SET DEFAULT 1
;
CREATE TABLE functest2 (a int, b int)
;
CREATE VIEW unspecified_types AS
  SELECT 42 as i, 42.5 as num, 'foo' as u, 'foo'::unknown as u2, null as n
;
CREATE FUNCTION g_intbig_options(internal)
RETURNS void
AS '$libdir/_int', 'g_intbig_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
IMPORT FOREIGN SCHEMA nonesuch FROM SERVER loopback INTO notthere
;
CREATE OPERATOR = (
    leftarg = casttesttype,
    rightarg = casttesttype,
    procedure = casttesttype_eq,
    commutator = =)
;
--
-- pg_get_wal_stats()
--
CREATE FUNCTION pg_get_wal_stats(IN start_lsn pg_lsn,
    IN end_lsn pg_lsn,
    IN  per_record boolean DEFAULT false,
    OUT "resource_manager/record_type" text,
    OUT count int8,
    OUT count_percentage float8,
    OUT record_size int8,
    OUT record_size_percentage float8,
    OUT fpi_size int8,
    OUT fpi_size_percentage float8,
    OUT combined_size int8,
    OUT combined_size_percentage float8
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE "fkpart5"."pk31" (
    "a" integer NOT NULL
)
;
create or replace function stricttest() returns void as $$
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- no rows
  select * from foo where f1 = p1 and f1::text = p3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
CREATE FUNCTION cube_lt(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE tail_tbl (id integer, d text)
;
-- failed (name conflicts)

ALTER AGGREGATE alt_agg3(int) RENAME TO alt_agg4
;
ANALYZE public.test_storage
;
ALTER OPERATOR >= (ismn13, ismn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
VACUUM pg_catalog.pg_publication
;
CREATE TABLE split_event_helper.drop_log (
    classid oid,
    objid oid,
    objsubid integer,
    original boolean,
    normal boolean,
    is_temporary boolean,
    object_type text,
    schema_name text,
    object_name text,
    object_identity text,
    address_names text[],
    address_args text[]
)
;
create table copyfreeze (a int, b char(1500))
;
ALTER TABLE testschema.test_tab ADD CONSTRAINT test_tab_pkey PRIMARY KEY (id)
;
create function polyf(x anyarray) returns anyarray as $$
  select x
$$ language sql
;
create table gin_test_tbl(i int4[]) with (autovacuum_enabled = off)
;
alter table alttype_cluster alter a type int
;
DROP TABLE distinct_group_1
;
DROP TABLE test_pg_c_utf8
;
CREATE TABLE prt2_m_p3 PARTITION OF prt2_m FOR VALUES FROM (500, 500) TO (600, 600)
;
create function dfunc(a int = 1, b int) returns int as $$
  select $1 + $2;
$$ language sql
;
VACUUM pg_catalog.pg_depend
;
CREATE SCHEMA newschema4 AUTHORIZATION regress_testrolx
;
DROP TRIGGER trig_row_before ON ftable
;
DROP TABLE list_parted, list_parted2, range_parted
;
create function trap_foreign_key(int) returns int as $$
begin
	begin	-- start a subtransaction
		insert into leaf values($1);
	exception
		when foreign_key_violation then
			raise notice 'caught foreign_key_violation';
			return 0;
	end;
	return 1;
end$$ language plpgsql
;
create trigger rem2_trig_row_after after insert on rem2
	for each row execute procedure trigger_data(23,'skidoo')
;
ALTER TEXT SEARCH CONFIGURATION estonian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH estonian_stem
;
-- Simple tests for alter table column type
alter table foo alter f1 TYPE integer
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_1" (
    "a" integer DEFAULT 2501,
    "b" integer DEFAULT 142857
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(isbn13, isbn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE OR REPLACE FUNCTION test_slru_page_write(bigint, text) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_write' LANGUAGE C
;
----------
[CREATE_TABLE] -- It's possible for the same EXISTS to get resolved both ways
create temp table exists_tbl (c1 int, c2 int, c3 int) partition by list (c1)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "after_upd_b_stmt_trig" AFTER UPDATE OF "b" ON "public"."main_table" FOR EACH STATEMENT EXECUTE FUNCTION "public"."trigger_func"('after_upd_b_stmt')
;
----------
[CREATE_VIEW(SELECT)] -- The following views have dependencies that force them to appear out of order.

/*
 * 5.25
 * DATA_TYPE_PRIVILEGES view
 */

CREATE VIEW data_type_privileges AS
    SELECT CAST(current_database() AS sql_identifier) AS object_catalog,
           CAST(x.objschema AS sql_identifier) AS object_schema,
           CAST(x.objname AS sql_identifier) AS object_name,
           CAST(x.objtype AS character_data) AS object_type,
           CAST(x.objdtdid AS sql_identifier) AS dtd_identifier

    FROM
      (
        SELECT udt_schema, udt_name, 'USER-DEFINED TYPE'::text, dtd_identifier FROM attributes
        UNION ALL
        SELECT table_schema, table_name, 'TABLE'::text, dtd_identifier FROM columns
        UNION ALL
        SELECT domain_schema, domain_name, 'DOMAIN'::text, dtd_identifier FROM domains
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM parameters
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM routines
      ) AS x (objschema, objname, objtype, objdtdid)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION hunspell_tst ALTER MAPPING
	REPLACE hunspell WITH hunspell_long
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (two_phase = false)
;
----------
[CREATE_TABLE] -- simple queries
CREATE TABLE result_tbl (a int, b int, c text)
;
----------
[CREATE_SCHEMA] --
-- Test basic SET SCHEMA handling.
--
CREATE SCHEMA s1
;
----------
[CREATE_TABLE] create table some_tab (a int, b int)
;
----------
[CREATE_TABLE] create table pc_list_parted (a int) partition by list(a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !~~* (
    PROCEDURE = texticnlike,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = ~~*,
    RESTRICT  = icnlikesel,
    JOIN      = icnlikejoinsel
)
;
----------
[CREATE_PROG_OBJ] create function bogus_func(anyelement)
  returns anymultirange as 'select int4multirange(int4range(1,10))' language sql
;
----------
[DROP_TABLE] DROP TABLE nummultirange_test2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION modify_rel_block(rel regclass, blockno int,
  zero bool DEFAULT false,
  corrupt_header bool DEFAULT false,
  corrupt_checksum bool DEFAULT false)
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TYPE] CREATE DOMAIN domain2 AS int DEFAULT random(min=>10, max=>100)
;
----------
[ALTER_VIEW|ALTER_COLUMN] ALTER VIEW dump_test.test_view ALTER COLUMN col1 SET DEFAULT 1
;
----------
[CREATE_TABLE] CREATE TABLE functest2 (a int, b int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW unspecified_types AS
  SELECT 42 as i, 42.5 as num, 'foo' as u, 'foo'::unknown as u2, null as n
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_intbig_options(internal)
RETURNS void
AS '$libdir/_int', 'g_intbig_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA nonesuch FROM SERVER loopback INTO notthere
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
    leftarg = casttesttype,
    rightarg = casttesttype,
    procedure = casttesttype_eq,
    commutator = =)
;
----------
[CREATE_PROG_OBJ] --
-- pg_get_wal_stats()
--
CREATE FUNCTION pg_get_wal_stats(IN start_lsn pg_lsn,
    IN end_lsn pg_lsn,
    IN  per_record boolean DEFAULT false,
    OUT "resource_manager/record_type" text,
    OUT count int8,
    OUT count_percentage float8,
    OUT record_size int8,
    OUT record_size_percentage float8,
    OUT fpi_size int8,
    OUT fpi_size_percentage float8,
    OUT combined_size int8,
    OUT combined_size_percentage float8
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."pk31" (
    "a" integer NOT NULL
)
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- no rows
  select * from foo where f1 = p1 and f1::text = p3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_lt(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE tail_tbl (id integer, d text)
;
----------
[RENAME_PROG_OBJ] -- failed (name conflicts)

ALTER AGGREGATE alt_agg3(int) RENAME TO alt_agg4
;
----------
[ADMIN_TABLE] ANALYZE public.test_storage
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ismn13, ismn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_publication
;
----------
[CREATE_TABLE] CREATE TABLE split_event_helper.drop_log (
    classid oid,
    objid oid,
    objsubid integer,
    original boolean,
    normal boolean,
    is_temporary boolean,
    object_type text,
    schema_name text,
    object_name text,
    object_identity text,
    address_names text[],
    address_args text[]
)
;
----------
[CREATE_TABLE] create table copyfreeze (a int, b char(1500))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testschema.test_tab ADD CONSTRAINT test_tab_pkey PRIMARY KEY (id)
;
----------
[CREATE_PROG_OBJ] create function polyf(x anyarray) returns anyarray as $$
  select x
$$ language sql
;
----------
[CREATE_TABLE] create table gin_test_tbl(i int4[]) with (autovacuum_enabled = off)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table alttype_cluster alter a type int
;
----------
[DROP_TABLE] DROP TABLE distinct_group_1
;
----------
[DROP_TABLE] DROP TABLE test_pg_c_utf8
;
----------
[CREATE_TABLE] CREATE TABLE prt2_m_p3 PARTITION OF prt2_m FOR VALUES FROM (500, 500) TO (600, 600)
;
----------
[CREATE_PROG_OBJ] create function dfunc(a int = 1, b int) returns int as $$
  select $1 + $2;
$$ language sql
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_depend
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema4 AUTHORIZATION regress_testrolx
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_before ON ftable
;
----------
[DROP_TABLE] DROP TABLE list_parted, list_parted2, range_parted
;
----------
[CREATE_PROG_OBJ] create function trap_foreign_key(int) returns int as $$
begin
	begin	-- start a subtransaction
		insert into leaf values($1);
	exception
		when foreign_key_violation then
			raise notice 'caught foreign_key_violation';
			return 0;
	end;
	return 1;
end$$ language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger rem2_trig_row_after after insert on rem2
	for each row execute procedure trigger_data(23,'skidoo')
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION estonian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH estonian_stem
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Simple tests for alter table column type
alter table foo alter f1 TYPE integer
;
