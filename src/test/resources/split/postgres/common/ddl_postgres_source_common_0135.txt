CREATE TEMPORARY TABLE result AS
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
UNION ALL
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
;
ANALYZE pg_temp_4.pg_temp_37852_2
;
DROP TABLE brin_interval_test
;
CREATE FOREIGN TABLE inv_fsz (c1 int )
	SERVER loopback OPTIONS (fetch_size '100$%$#$#')
;
-- ALTER COLUMN TYPE with a check constraint and a child table (bug #13779)
CREATE TABLE test_inh_check (a float check (a > 10.2), b float)
;
CREATE VIEW pg_stat_subscription AS
    SELECT
            su.oid AS subid,
            su.subname,
            st.pid,
            st.relid,
            st.received_lsn,
            st.last_msg_send_time,
            st.last_msg_receipt_time,
            st.latest_end_lsn,
            st.latest_end_time
    FROM pg_subscription su
            LEFT JOIN pg_stat_get_subscription(NULL) st
                      ON (st.subid = su.oid)
;
create temp table b (x integer, y integer)
;
DROP TABLE a1
;
ALTER OPERATOR FAMILY gist_bit_ops USING gist ADD
	FUNCTION	11  (bit, bit) gbt_bit_sortsupport (internal)
;
create temp table uniquetbl (f1 text unique)
;
DROP SCHEMA tststats CASCADE
;
drop view myview
;
-- OK
create function test_event_trigger() returns event_trigger as $$
BEGIN
    RAISE NOTICE 'test_event_trigger: % %', tg_event, tg_tag;
END
$$ language plpgsql
;
VACUUM will_get_vacuumed
;
CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
drop view wcowrtest_v, wcowrtest_v2
;
CREATE TABLE regtest_table_3 (x int, y serial)
;
ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p1" FOR VALUES IN ('0000', '0001', '0002', '0003')
;
CREATE TABLE "regress_indexing"."pk4" (
    "a" integer NOT NULL
)
;
CREATE TABLESPACE dropme_ts2 LOCATION ''
;
VACUUM (FULL) test_io_vac_strategy
;
CREATE VIEW pg_stat_progress_vacuum AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        S.relid AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'scanning heap'
                      WHEN 2 THEN 'vacuuming indexes'
                      WHEN 3 THEN 'vacuuming heap'
                      WHEN 4 THEN 'cleaning up indexes'
                      WHEN 5 THEN 'truncating heap'
                      WHEN 6 THEN 'performing final cleanup'
                      END AS phase,
        S.param2 AS heap_blks_total, S.param3 AS heap_blks_scanned,
        S.param4 AS heap_blks_vacuumed, S.param5 AS index_vacuum_count,
        S.param6 AS max_dead_tuple_bytes, S.param7 AS dead_tuple_bytes,
        S.param8 AS num_dead_item_ids, S.param9 AS indexes_total,
        S.param10 AS indexes_processed,
        S.param11 / 1000000::double precision AS delay_time
    FROM pg_stat_get_progress_info('VACUUM') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
create table t5()
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
create table s1.p1_2 partition of s1.p1 for values in (4, 5, 6)
;
CREATE TRIGGER trig_row_after2
AFTER INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
ALTER TYPE bogon ADD VALUE 'ugly'
;
create or replace function parent_del_func()
  returns trigger language plpgsql as
$$
begin
  delete from child where aid = old.aid;
  if found then
    delete from parent where aid = old.aid;
    return null; -- cancel outer deletion
  end if;
  return old;
end;
$$
;
ALTER TABLE test_part_c_1 REPLICA IDENTITY USING INDEX test_part_c_1_pkey
;
ALTER TABLE test9a ADD COLUMN c priv_testdomain1
;
CREATE FUNCTION public.setter() RETURNS bool VOLATILE
  LANGUAGE SQL AS $$SET search_path = s; SELECT true$$
;
CREATE TABLE atestc (fz int) INHERITS (atestp1, atestp2)
;
create trigger qqq after insert on parted_trig_1_1 for each row execute procedure trigger_notice()
;
ALTER TABLE replication_metadata ALTER COLUMN rewritemeornot TYPE text
;
CREATE DATABASE regress_nosuch_db
;
CREATE SUBSCRIPTION admin_sub CONNECTION 'port=15895 host=/tmp/eTSpS1gcF3 dbname=postgres' PUBLICATION alice
WITH (run_as_owner = false, password_required = false, copy_data = true, enabled = false)
;
do $$ declare a int[];
begin
  a := '[-2147483648:-2147483647]={1,2}'::int[];
  a[2147483647] := 42;
end $$
;
CREATE TABLE gtestxx_1 (a int NOT NULL, b int)
;
CREATE FUNCTION test_valid_server_encoding(text) RETURNS boolean
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
-- be failed
SECURITY LABEL ON COLUMN t2.b
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
ALTER TABLE rewrite_test
        ADD COLUMN notempty6_rewrite serial,
        ADD COLUMN empty6 text,
        ADD COLUMN notempty6_norewrite int default 42
;
VACUUM (ANALYZE) information_schema.sql_sizing_profiles
;
CREATE TABLE "public"."brin_summarize_bloom" (
    "value" integer
)
WITH ("fillfactor"='10', "autovacuum_enabled"='false')
;
CREATE TABLE new_measurement (LIKE measurement) WITH (autovacuum_enabled=off)
;
-- ok
ALTER INDEX dummy_test_idx SET (option_enum = 0)
;
CREATE SCHEMA alt_nsp1
;
DROP TABLE pg_temp_12.pg_temp_26389_2, pg_temp_12.pg_temp_26389
;
-- try a constraint trigger, also
create table parted_referenced (a int)
;
CREATE VIEW my_credit_card_usage_normal AS
       SELECT * FROM my_credit_card_secure l NATURAL JOIN credit_usage r
;
CREATE TABLE tab_rowfilter_partition (LIKE tab_rowfilter_partitioned_2)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE|SELECT] CREATE TEMPORARY TABLE result AS
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
UNION ALL
(SELECT * from dblink_get_result('myconn') as t(t timestamptz))
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37852_2
;
----------
[DROP_TABLE] DROP TABLE brin_interval_test
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE inv_fsz (c1 int )
	SERVER loopback OPTIONS (fetch_size '100$%$#$#')
;
----------
[CREATE_TABLE] -- ALTER COLUMN TYPE with a check constraint and a child table (bug #13779)
CREATE TABLE test_inh_check (a float check (a > 10.2), b float)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_subscription AS
    SELECT
            su.oid AS subid,
            su.subname,
            st.pid,
            st.relid,
            st.received_lsn,
            st.last_msg_send_time,
            st.last_msg_receipt_time,
            st.latest_end_lsn,
            st.latest_end_time
    FROM pg_subscription su
            LEFT JOIN pg_stat_get_subscription(NULL) st
                      ON (st.subid = su.oid)
;
----------
[CREATE_TABLE] create temp table b (x integer, y integer)
;
----------
[DROP_TABLE] DROP TABLE a1
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bit_ops USING gist ADD
	FUNCTION	11  (bit, bit) gbt_bit_sortsupport (internal)
;
----------
[CREATE_TABLE] create temp table uniquetbl (f1 text unique)
;
----------
[DROP_SCHEMA] DROP SCHEMA tststats CASCADE
;
----------
[DROP_VIEW] drop view myview
;
----------
[CREATE_PROG_OBJ] -- OK
create function test_event_trigger() returns event_trigger as $$
BEGIN
    RAISE NOTICE 'test_event_trigger: % %', tg_event, tg_tag;
END
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM will_get_vacuumed
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
----------
[DROP_VIEW] drop view wcowrtest_v, wcowrtest_v2
;
----------
[CREATE_TABLE] CREATE TABLE regtest_table_3 (x int, y serial)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab" ATTACH PARTITION "public"."pagg_tab_p1" FOR VALUES IN ('0000', '0001', '0002', '0003')
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk4" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE dropme_ts2 LOCATION ''
;
----------
[ADMIN_TABLE] VACUUM (FULL) test_io_vac_strategy
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_progress_vacuum AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        S.relid AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'scanning heap'
                      WHEN 2 THEN 'vacuuming indexes'
                      WHEN 3 THEN 'vacuuming heap'
                      WHEN 4 THEN 'cleaning up indexes'
                      WHEN 5 THEN 'truncating heap'
                      WHEN 6 THEN 'performing final cleanup'
                      END AS phase,
        S.param2 AS heap_blks_total, S.param3 AS heap_blks_scanned,
        S.param4 AS heap_blks_vacuumed, S.param5 AS index_vacuum_count,
        S.param6 AS max_dead_tuple_bytes, S.param7 AS dead_tuple_bytes,
        S.param8 AS num_dead_item_ids, S.param9 AS indexes_total,
        S.param10 AS indexes_processed,
        S.param11 / 1000000::double precision AS delay_time
    FROM pg_stat_get_progress_info('VACUUM') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
----------
[CREATE_TABLE] create table t5()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_TABLE] create table s1.p1_2 partition of s1.p1 for values in (4, 5, 6)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_after2
AFTER INSERT OR UPDATE OR DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[ALTER_TYPE] ALTER TYPE bogon ADD VALUE 'ugly'
;
----------
[CREATE_PROG_OBJ] create or replace function parent_del_func()
  returns trigger language plpgsql as
$$
begin
  delete from child where aid = old.aid;
  if found then
    delete from parent where aid = old.aid;
    return null; -- cancel outer deletion
  end if;
  return old;
end;
$$
;
----------
[ALTER_TABLE] ALTER TABLE test_part_c_1 REPLICA IDENTITY USING INDEX test_part_c_1_pkey
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test9a ADD COLUMN c priv_testdomain1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION public.setter() RETURNS bool VOLATILE
  LANGUAGE SQL AS $$SET search_path = s; SELECT true$$
;
----------
[CREATE_TABLE] CREATE TABLE atestc (fz int) INHERITS (atestp1, atestp2)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger qqq after insert on parted_trig_1_1 for each row execute procedure trigger_notice()
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE replication_metadata ALTER COLUMN rewritemeornot TYPE text
;
----------
[CREATE_CATALOG] CREATE DATABASE regress_nosuch_db
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION admin_sub CONNECTION 'port=15895 host=/tmp/eTSpS1gcF3 dbname=postgres' PUBLICATION alice
WITH (run_as_owner = false, password_required = false, copy_data = true, enabled = false)
;
----------
[BLOCK] do $$ declare a int[];
begin
  a := '[-2147483648:-2147483647]={1,2}'::int[];
  a[2147483647] := 42;
end $$
;
----------
[CREATE_TABLE] CREATE TABLE gtestxx_1 (a int NOT NULL, b int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_valid_server_encoding(text) RETURNS boolean
    AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
----------
[SECURITY_LABEL] -- be failed
SECURITY LABEL ON COLUMN t2.b
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE rewrite_test
        ADD COLUMN notempty6_rewrite serial,
        ADD COLUMN empty6 text,
        ADD COLUMN notempty6_norewrite int default 42
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) information_schema.sql_sizing_profiles
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brin_summarize_bloom" (
    "value" integer
)
WITH ("fillfactor"='10', "autovacuum_enabled"='false')
;
----------
[CREATE_TABLE] CREATE TABLE new_measurement (LIKE measurement) WITH (autovacuum_enabled=off)
;
----------
[ALTER_INDEX] -- ok
ALTER INDEX dummy_test_idx SET (option_enum = 0)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA alt_nsp1
;
----------
[DROP_TABLE] DROP TABLE pg_temp_12.pg_temp_26389_2, pg_temp_12.pg_temp_26389
;
----------
[CREATE_TABLE] -- try a constraint trigger, also
create table parted_referenced (a int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW my_credit_card_usage_normal AS
       SELECT * FROM my_credit_card_secure l NATURAL JOIN credit_usage r
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_partition (LIKE tab_rowfilter_partitioned_2)
;
