DROP SCHEMA split_agg_probe
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_class
;
CREATE TABLE sql_implementation_info (
    implementation_info_id      character_data,
    implementation_info_name    character_data,
    integer_value               cardinal_number,
    character_value             character_data,
    comments                    character_data
)
;
CREATE TABLE part1 PARTITION OF range_parted2 FOR VALUES FROM (1) TO (10)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_publication_rel
;
ALTER DATABASE "ecpg1_regression" SET lc_numeric TO 'C'
;
create schema regress_indexing
;
CREATE TABLE ltreetest (t ltree)
;
CREATE FUNCTION hash_ltree(ltree)
RETURNS integer
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
VACUUM (PARALLEL 2) pg_catalog.pg_seclabel
;
CREATE FUNCTION volatile_one() RETURNS int AS $$ BEGIN RETURN 1; END; $$ LANGUAGE plpgsql VOLATILE
;
-- ************************************************************
-- * AFTER INSERT or UPDATE or DELETE on slot with slotlink
-- *	- Ensure that the opponent correctly points back to us
-- ************************************************************
create function tg_slotlink_a() returns trigger as '
declare
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
        if new.slotlink != '''' then
	    dummy := tg_slotlink_set(new.slotlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = ''UPDATE'' then
        if new.slotlink != old.slotlink then
	    if old.slotlink != '''' then
	        dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	    end if;
	    if new.slotlink != '''' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.slotlink != '''' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = ''DELETE'' then
        if old.slotlink != '''' then
	    dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	end if;
	return old;
    end if;
end;
' language plpgsql
;
CREATE TRIGGER "tg_chkslotlink" BEFORE INSERT OR UPDATE ON "public"."phone" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotlink"()
;
-- Load some example data and build the index
--
CREATE TABLE test_seg (s seg)
;
ALTER TABLE test_replica_identity REPLICA IDENTITY DEFAULT
;
ALTER OPERATOR ^@> (ltree, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
-- Test GIN multicolumn index
CREATE TABLE "gin_check_multicolumn"(a text[], b text[])
;
ANALYZE public.shoelace_arrive
;
ANALYZE pg_catalog.pg_ts_template
;
CREATE UNLOGGED SEQUENCE sequence_testx
;
CREATE OR REPLACE FUNCTION pg_catalog.pg_file_unlink(text)
RETURNS bool
AS '$libdir/adminpack', 'pg_file_unlink_v1_1'
LANGUAGE C VOLATILE STRICT
;
-- operator number should be between 1 and 5
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 0 < (int4, int2)
;
create unique index tstidx on ltreetest (t)
;
CREATE VIEW v2 WITH (security_barrier = true) AS
  SELECT * FROM v1 WHERE EXISTS (SELECT 1)
;
-- test BRIN index doesn't block HOT update
CREATE TABLE brin_hot (
  id  integer PRIMARY KEY,
  val integer NOT NULL
) WITH (autovacuum_enabled = off, fillfactor = 70)
;
COMMENT ON TEXT SEARCH DICTIONARY yiddish_stem IS 'snowball stemmer for yiddish language'
;
DROP TABLE IF EXISTS parttab
;
CREATE SEQUENCE sequence_test5 AS integer
;
-- here we also check that you can default the index name
CREATE INDEX CONCURRENTLY on concur_heap((f2||f1))
;
CREATE INDEX text_idx on test__int using gin ( a gin__int_ops )
;
-- trying to specify list value for the hash partitioned table
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES IN (1000)
;
/* src/test/modules/test_regex/test_regex--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_regex(pattern text, string text, flags text)
RETURNS SETOF text[]
STRICT
AS '$libdir/test_regex' LANGUAGE C
;
CREATE TEMP TABLE pg_temp_9.pg_temp_37969_2 (tid pg_catalog.tid)
;
alter type alter1.ctype set schema alter1
;
ALTER OPERATOR ^~ (_ltree, lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN IF NOT EXISTS c3 integer, -- skipping because c3 already exists
	ADD COLUMN c4 integer REFERENCES test_add_column
;
create trigger some_tab_stmt_trig
  before update on some_tab execute function some_tab_stmt_trig_func()
;
create function polyf(x anyarray) returns anyelement as $$
  select x[1]
$$ language sql
;
CREATE INDEX brinidx ON brintest USING brin (
	byteacol,
	charcol,
	namecol,
	int8col,
	int2col,
	int4col,
	textcol,
	oidcol,
	tidcol,
	float4col,
	float8col,
	macaddrcol,
	inetcol inet_inclusion_ops,
	inetcol inet_minmax_ops,
	cidrcol inet_inclusion_ops,
	cidrcol inet_minmax_ops,
	bpcharcol,
	datecol,
	timecol,
	timestampcol,
	timestamptzcol,
	intervalcol,
	timetzcol,
	bitcol,
	varbitcol,
	numericcol,
	uuidcol,
	int4rangecol,
	lsncol,
	boxcol
) with (pages_per_range = 1)
;
CREATE TABLE t2(a int, b int)
;
-- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0
;
CREATE FUNCTION testAVToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
$val = [{a => 1, b => 'boo', c => undef}, {d => 2}];
return $val;
$$
;
create table ths (mod int, data int, unique(mod, data)) partition by hash(mod)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=23291 host=/tmp/U59idoMTG8 dbname=postgres' PUBLICATION tap_pub
;
CREATE EXTENSION citext
;
DROP TABLE func_index_heap
;
--
-- test FK referenced updates RESTRICT
--

TRUNCATE temporal_rng, temporal_fk_rng2rng
;
CREATE VIEW v1_temp AS SELECT * FROM temp_table
;
ALTER TABLE tt2 OF tt_t0
;
-- fail due to mismatched data type
alter table atacc3 add test2 bool
;
------------------------------------------------------------------------------------------
[DROP_SCHEMA] DROP SCHEMA split_agg_probe
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_class
;
----------
[CREATE_TABLE] CREATE TABLE sql_implementation_info (
    implementation_info_id      character_data,
    implementation_info_name    character_data,
    integer_value               cardinal_number,
    character_value             character_data,
    comments                    character_data
)
;
----------
[CREATE_TABLE] CREATE TABLE part1 PARTITION OF range_parted2 FOR VALUES FROM (1) TO (10)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_publication_rel
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg1_regression" SET lc_numeric TO 'C'
;
----------
[CREATE_SCHEMA] create schema regress_indexing
;
----------
[CREATE_TABLE] CREATE TABLE ltreetest (t ltree)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hash_ltree(ltree)
RETURNS integer
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_seclabel
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION volatile_one() RETURNS int AS $$ BEGIN RETURN 1; END; $$ LANGUAGE plpgsql VOLATILE
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * AFTER INSERT or UPDATE or DELETE on slot with slotlink
-- *	- Ensure that the opponent correctly points back to us
-- ************************************************************
create function tg_slotlink_a() returns trigger as '
declare
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
        if new.slotlink != '''' then
	    dummy := tg_slotlink_set(new.slotlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = ''UPDATE'' then
        if new.slotlink != old.slotlink then
	    if old.slotlink != '''' then
	        dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	    end if;
	    if new.slotlink != '''' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.slotlink != '''' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = ''DELETE'' then
        if old.slotlink != '''' then
	    dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	end if;
	return old;
    end if;
end;
' language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkslotlink" BEFORE INSERT OR UPDATE ON "public"."phone" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotlink"()
;
----------
[CREATE_TABLE] -- Load some example data and build the index
--
CREATE TABLE test_seg (s seg)
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY DEFAULT
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^@> (ltree, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_TABLE] -- Test GIN multicolumn index
CREATE TABLE "gin_check_multicolumn"(a text[], b text[])
;
----------
[ADMIN_TABLE] ANALYZE public.shoelace_arrive
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_ts_template
;
----------
[CREATE_SEQUENCE] CREATE UNLOGGED SEQUENCE sequence_testx
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_catalog.pg_file_unlink(text)
RETURNS bool
AS '$libdir/adminpack', 'pg_file_unlink_v1_1'
LANGUAGE C VOLATILE STRICT
;
----------
[ALTER_PROG_OBJ] -- operator number should be between 1 and 5
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 0 < (int4, int2)
;
----------
[ADD_INDEX] create unique index tstidx on ltreetest (t)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v2 WITH (security_barrier = true) AS
  SELECT * FROM v1 WHERE EXISTS (SELECT 1)
;
----------
[CREATE_TABLE] -- test BRIN index doesn't block HOT update
CREATE TABLE brin_hot (
  id  integer PRIMARY KEY,
  val integer NOT NULL
) WITH (autovacuum_enabled = off, fillfactor = 70)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY yiddish_stem IS 'snowball stemmer for yiddish language'
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS parttab
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test5 AS integer
;
----------
[ADD_INDEX] -- here we also check that you can default the index name
CREATE INDEX CONCURRENTLY on concur_heap((f2||f1))
;
----------
[ADD_INDEX] CREATE INDEX text_idx on test__int using gin ( a gin__int_ops )
;
----------
[CREATE_TABLE] -- trying to specify list value for the hash partitioned table
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES IN (1000)
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_regex/test_regex--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_regex(pattern text, string text, flags text)
RETURNS SETOF text[]
STRICT
AS '$libdir/test_regex' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_37969_2 (tid pg_catalog.tid)
;
----------
[ALTER_TYPE] alter type alter1.ctype set schema alter1
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^~ (_ltree, lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN IF NOT EXISTS c3 integer, -- skipping because c3 already exists
	ADD COLUMN c4 integer REFERENCES test_add_column
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger some_tab_stmt_trig
  before update on some_tab execute function some_tab_stmt_trig_func()
;
----------
[CREATE_PROG_OBJ] create function polyf(x anyarray) returns anyelement as $$
  select x[1]
$$ language sql
;
----------
[ADD_INDEX] CREATE INDEX brinidx ON brintest USING brin (
	byteacol,
	charcol,
	namecol,
	int8col,
	int2col,
	int4col,
	textcol,
	oidcol,
	tidcol,
	float4col,
	float8col,
	macaddrcol,
	inetcol inet_inclusion_ops,
	inetcol inet_minmax_ops,
	cidrcol inet_inclusion_ops,
	cidrcol inet_minmax_ops,
	bpcharcol,
	datecol,
	timecol,
	timestampcol,
	timestamptzcol,
	intervalcol,
	timetzcol,
	bitcol,
	varbitcol,
	numericcol,
	uuidcol,
	int4rangecol,
	lsncol,
	boxcol
) with (pages_per_range = 1)
;
----------
[CREATE_TABLE] CREATE TABLE t2(a int, b int)
;
----------
[CREATE_VIEW] -- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testAVToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
$val = [{a => 1, b => 'boo', c => undef}, {d => 2}];
return $val;
$$
;
----------
[CREATE_TABLE] create table ths (mod int, data int, unique(mod, data)) partition by hash(mod)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=23291 host=/tmp/U59idoMTG8 dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_LIBRARY] CREATE EXTENSION citext
;
----------
[DROP_TABLE] DROP TABLE func_index_heap
;
----------
[TRUNCATE_TABLE] --
-- test FK referenced updates RESTRICT
--

TRUNCATE temporal_rng, temporal_fk_rng2rng
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v1_temp AS SELECT * FROM temp_table
;
----------
[ALTER_TABLE] ALTER TABLE tt2 OF tt_t0
;
----------
[ALTER_TABLE|ADD_COLUMN] -- fail due to mismatched data type
alter table atacc3 add test2 bool
;
