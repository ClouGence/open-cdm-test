-- regular indexes have special case locking rules
BEGIN
;
-- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
EXECUTE dumpFunc('28028')
;
GRANT SELECT ON Table regress_test_table TO public
;
ALTER TABLE public.pslot OWNER TO zyc
;
execute q1 (1,2)
;
COPY public.inh_test (a, b) TO stdout
;
EXECUTE dumpFunc('35977')
;
CREATE STATISTICS tststats.s4 ON a, b FROM tststats.v
;
LOCK TABLE pg_catalog.pg_statistic_ext_data IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[TRANSACTION] -- regular indexes have special case locking rules
BEGIN
;
----------
[SESSION_SETTING_WRITE] -- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
----------
[UNSAFE] EXECUTE dumpFunc('28028')
;
----------
[GRANT] GRANT SELECT ON Table regress_test_table TO public
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.pslot OWNER TO zyc
;
----------
[UNSAFE] execute q1 (1,2)
;
----------
[DATA_EXPORT] COPY public.inh_test (a, b) TO stdout
;
----------
[UNSAFE] EXECUTE dumpFunc('35977')
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS tststats.s4 ON a, b FROM tststats.v
;
----------
[SESSION_LOCK] LOCK TABLE pg_catalog.pg_statistic_ext_data IN ACCESS SHARE MODE
;
