-- remove from cc1, should fail again
alter table cc1 alter column f1 drop not null
;
CREATE TABLESPACE tblspc3 LOCATION '/src/src/bin/pg_basebackup/tmp_check/tmp_test_r_pR/superlongname_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
;
CREATE TABLE tbl (i INT, t TEXT)
;
CREATE TABLE ltable (a text, b int)
;
-- but we're nice guys and will let you undo your mistake
ALTER TABLE pcachetest DROP COLUMN q3
;
ALTER SUBSCRIPTION split_sub RENAME TO split_sub_renamed
;
CREATE TYPE public.tst_comp_basic_t AS (a FLOAT, b TEXT, c INTEGER)
;
DROP TABLE trunc_trigger_test
;
--
-- Test implicit LATERAL references to old/new in rules
--
CREATE TABLE rule_t1(a int, b text DEFAULT 'xxx', c int)
;
create type cashrange as range (subtype = money)
;
DROP FUNCTION getrngfunc7(int)
;
drop operator public.<^(int, int)
;
ALTER TABLE t SET tablespace regress_ts2
;
ALTER INDEX pg_toast.pg_toast_28415_index RENAME TO reind_con_toast_idx
;
CREATE AGGREGATE "public"."myaggn06a"(integer) (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
-- check that attaching partitions to a table while it is being used is
-- prevented
create table tab_part_attach (a int) partition by list (a)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
CREATE TABLE gtest_child3 PARTITION OF gtest_parent (
    f3 WITH OPTIONS GENERATED ALWAYS AS IDENTITY  -- error
) FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
DROP EXTENSION test_ext_req_schema3
;
ANALYZE pg_temp_11.pg_temp_26330_2
;
create table parent (a int, b int, c int)
;
alter table constr_child validate constraint constr_parent_a_not_null
;
CREATE FUNCTION pgp_pub_encrypt_bytea(bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_pub_encrypt_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_user_mapping
;
CREATE INDEX "iprt1_p3_a" ON "public"."prt1_p3" USING "btree" ("a")
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_publication_rel
;
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY)
;
CREATE POLICY p3 ON s1 FOR INSERT WITH CHECK (a = (SELECT a FROM s1))
;
CREATE FUNCTION plsample_call_handler() RETURNS language_handler
  AS '$libdir/plsample' LANGUAGE C
;
ANALYZE pg_catalog.pg_opfamily
;
create table gist_tbl (b box, p point, c circle)
;
CREATE MATERIALIZED VIEW heapmv USING heap AS SELECT 1 AS a
;
CREATE OPERATOR FAMILY alt_opf7 USING btree
;
create function xfloat4out(xfloat4) returns cstring immutable strict
  language internal as 'int4out'
;
CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=27783 host=/tmp/WPXzmhPeul dbname=postgres' PUBLICATION pub5
;
CREATE TABLE reindex_inj.tbl(i int primary key, updated_at timestamp)
;
CREATE FUNCTION log_stmt() RETURNS TRIGGER LANGUAGE plpgsql AS $$
	BEGIN INSERT INTO public.cmdlog VALUES (TG_OP || ' on ' || TG_RELNAME); RETURN NULL; END;
$$
;
--
-- insert with DEFAULT in the target_list
--
create table inserttest (col1 int4, col2 int4 NOT NULL, col3 text default 'testing')
;
create temp table t_gin_test_tbl(i int4[], j int4[])
;
DROP TABLE d3_listp
;
DROP TABLE pg_temp_18.pg_temp_27200_2, pg_temp_18.pg_temp_27200
;
CREATE TABLE noinh_con_copy1 (LIKE noinh_con_copy INCLUDING CONSTRAINTS INCLUDING COMMENTS)
;
CREATE INDEX "test_range_spgist_idx" ON "public"."test_range_spgist" USING "spgist" ("ir")
;
CREATE VIEW pg_publication_tables AS
    SELECT
        P.pubname AS pubname,
        N.nspname AS schemaname,
        C.relname AS tablename
    FROM pg_publication P,
         LATERAL pg_get_publication_tables(P.pubname) GPT,
         pg_class C JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.oid = GPT.relid
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."emp" INHERIT "public"."person"
;
create function trap_zero_divide(int) returns int as $$
declare x int;
	sx smallint;
begin
	begin	-- start a subtransaction
		raise notice 'should see this';
		x := 100 / $1;
		raise notice 'should see this only if % <> 0', $1;
		sx := $1;
		raise notice 'should see this only if % fits in smallint', $1;
		if $1 < 0 then
			raise exception '% is less than zero', $1;
		end if;
	exception
		when division_by_zero then
			raise notice 'caught division_by_zero';
			x := -1;
		when NUMERIC_VALUE_OUT_OF_RANGE then
			raise notice 'caught numeric_value_out_of_range';
			x := -2;
	end;
	return x;
end$$ language plpgsql
;
CREATE EXTENSION ltree_plpython2u CASCADE
;
-- implicit casting

CREATE TYPE person_type AS (id int, name text)
;
CREATE VIEW collations AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST('NO PAD' AS character_data) AS pad_attribute
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
CALL transaction_test10b(10)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] -- remove from cc1, should fail again
alter table cc1 alter column f1 drop not null
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc3 LOCATION '/src/src/bin/pg_basebackup/tmp_check/tmp_test_r_pR/superlongname_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
;
----------
[CREATE_TABLE] CREATE TABLE tbl (i INT, t TEXT)
;
----------
[CREATE_TABLE] CREATE TABLE ltable (a text, b int)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- but we're nice guys and will let you undo your mistake
ALTER TABLE pcachetest DROP COLUMN q3
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION split_sub RENAME TO split_sub_renamed
;
----------
[CREATE_TYPE] CREATE TYPE public.tst_comp_basic_t AS (a FLOAT, b TEXT, c INTEGER)
;
----------
[DROP_TABLE] DROP TABLE trunc_trigger_test
;
----------
[CREATE_TABLE] --
-- Test implicit LATERAL references to old/new in rules
--
CREATE TABLE rule_t1(a int, b text DEFAULT 'xxx', c int)
;
----------
[CREATE_TYPE] create type cashrange as range (subtype = money)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION getrngfunc7(int)
;
----------
[DROP_PROG_OBJ] drop operator public.<^(int, int)
;
----------
[ALTER_TABLE] ALTER TABLE t SET tablespace regress_ts2
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28415_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn06a"(integer) (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
----------
[CREATE_TABLE] -- check that attaching partitions to a table while it is being used is
-- prevented
create table tab_part_attach (a int) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child3 PARTITION OF gtest_parent (
    f3 WITH OPTIONS GENERATED ALWAYS AS IDENTITY  -- error
) FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
----------
[DROP_LIBRARY] DROP EXTENSION test_ext_req_schema3
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26330_2
;
----------
[CREATE_TABLE] create table parent (a int, b int, c int)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] alter table constr_child validate constraint constr_parent_a_not_null
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgp_pub_encrypt_bytea(bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_pub_encrypt_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_user_mapping
;
----------
[ADD_INDEX] CREATE INDEX "iprt1_p3_a" ON "public"."prt1_p3" USING "btree" ("a")
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_publication_rel
;
----------
[CREATE_TABLE] CREATE TABLE T(pk INT NOT NULL PRIMARY KEY)
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON s1 FOR INSERT WITH CHECK (a = (SELECT a FROM s1))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION plsample_call_handler() RETURNS language_handler
  AS '$libdir/plsample' LANGUAGE C
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_opfamily
;
----------
[CREATE_TABLE] create table gist_tbl (b box, p point, c circle)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW heapmv USING heap AS SELECT 1 AS a
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf7 USING btree
;
----------
[CREATE_PROG_OBJ] create function xfloat4out(xfloat4) returns cstring immutable strict
  language internal as 'int4out'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=27783 host=/tmp/WPXzmhPeul dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_TABLE] CREATE TABLE reindex_inj.tbl(i int primary key, updated_at timestamp)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION log_stmt() RETURNS TRIGGER LANGUAGE plpgsql AS $$
	BEGIN INSERT INTO public.cmdlog VALUES (TG_OP || ' on ' || TG_RELNAME); RETURN NULL; END;
$$
;
----------
[CREATE_TABLE] --
-- insert with DEFAULT in the target_list
--
create table inserttest (col1 int4, col2 int4 NOT NULL, col3 text default 'testing')
;
----------
[CREATE_TABLE] create temp table t_gin_test_tbl(i int4[], j int4[])
;
----------
[DROP_TABLE] DROP TABLE d3_listp
;
----------
[DROP_TABLE] DROP TABLE pg_temp_18.pg_temp_27200_2, pg_temp_18.pg_temp_27200
;
----------
[CREATE_TABLE] CREATE TABLE noinh_con_copy1 (LIKE noinh_con_copy INCLUDING CONSTRAINTS INCLUDING COMMENTS)
;
----------
[ADD_INDEX] CREATE INDEX "test_range_spgist_idx" ON "public"."test_range_spgist" USING "spgist" ("ir")
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_publication_tables AS
    SELECT
        P.pubname AS pubname,
        N.nspname AS schemaname,
        C.relname AS tablename
    FROM pg_publication P,
         LATERAL pg_get_publication_tables(P.pubname) GPT,
         pg_class C JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.oid = GPT.relid
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."emp" INHERIT "public"."person"
;
----------
[CREATE_PROG_OBJ] create function trap_zero_divide(int) returns int as $$
declare x int;
	sx smallint;
begin
	begin	-- start a subtransaction
		raise notice 'should see this';
		x := 100 / $1;
		raise notice 'should see this only if % <> 0', $1;
		sx := $1;
		raise notice 'should see this only if % fits in smallint', $1;
		if $1 < 0 then
			raise exception '% is less than zero', $1;
		end if;
	exception
		when division_by_zero then
			raise notice 'caught division_by_zero';
			x := -1;
		when NUMERIC_VALUE_OUT_OF_RANGE then
			raise notice 'caught numeric_value_out_of_range';
			x := -2;
	end;
	return x;
end$$ language plpgsql
;
----------
[CREATE_LIBRARY] CREATE EXTENSION ltree_plpython2u CASCADE
;
----------
[CREATE_TYPE] -- implicit casting

CREATE TYPE person_type AS (id int, name text)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW collations AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST('NO PAD' AS character_data) AS pad_attribute
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
----------
[CALL_PROG_OBJ] CALL transaction_test10b(10)
;
