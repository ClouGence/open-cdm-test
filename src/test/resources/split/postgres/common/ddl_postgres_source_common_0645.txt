CREATE TABLE "public"."mlparted" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
PARTITION BY RANGE ("a", "b")
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_shdepend
;
CREATE FOREIGN TABLE remote_backend_pid (pid int)
  SERVER loopback OPTIONS (table_name 'my_backend_pid')
;
drop trigger trg1 on trigpart41
;
CREATE POLICY "p1" ON "regress_rls_schema"."rls_tbl_force" USING (("c1" = 5)) WITH CHECK (("c1" < 5))
;
CREATE FUNCTION dblink_fdw_validator(
    options text[],
    catalog oid
)
RETURNS void
AS '$libdir/dblink', 'dblink_fdw_validator'
LANGUAGE C STRICT PARALLEL SAFE
;
-- not STRICT; allows (null::rectype,hstore)

CREATE OPERATOR #= (
	LEFTARG = anyelement,
	RIGHTARG = hstore,
	PROCEDURE = populate_record
)
;
CREATE VIEW ro_view13 AS SELECT a, b FROM (SELECT * FROM base_tbl) AS t
;
CREATE TABLE ctv_data (x int, y int, v text)
;
CREATE OR REPLACE VIEW mysecview4 WITH (security_barrier=false)
       AS SELECT * FROM tbl1 WHERE a <> 256
;
ALTER TABLE rls_tbl FORCE ROW LEVEL SECURITY
;
create function myleast(variadic anyarray) returns anyelement as $$
  select min($1[i]) from generate_subscripts($1,1) g(i)
$$ language sql immutable strict
;
CREATE FUNCTION crosstab(text)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_depend
;
DROP SUBSCRIPTION tap_sub_A_B
;
SECURITY LABEL ON TABLE flavians IS 'system_u:object_r:sepgsql_table_t:s0'
;
-- should fail

DROP TYPE default_test_row CASCADE
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_amop
;
DROP VIEW lock_view6
;
drop table hs2
;
create table idxpart (a int4range, b int4range, c int4range, exclude USING GIST (a with =, b with =, c with &&)) partition by range (a, b)
;
CREATE INDEX regtest_pindex_tbl4_y ON regtest_ptable_4_ones(y)
;
-- a partition of a partitioned table should work though
create table test_partition partition of test_partitioned for values from (1) to (100)
;
ALTER SUBSCRIPTION mysub1 SET PUBLICATION non_existent_pub
;
create type compostype as (x int, y varchar)
;
create table T ( i int )
;
CREATE MATERIALIZED VIEW mv AS SELECT * FROM target
;
-- ERROR
CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') NOT NULL,
	c2 text OPTIONS (param2 'val2', param3 'val3') CHECK (c2 <> ''),
	c3 date,
	CHECK (c3 BETWEEN '1994-01-01'::date AND '1994-01-31'::date)
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
alter table mlparted1 attach partition mlparted11 for values from (2) to (5)
;
-- These are "loose" in the opfamily for consistency with the rest of btree_gist
ALTER OPERATOR FAMILY gist_uuid_ops USING gist ADD
	OPERATOR	6	<>  (uuid, uuid) ,
	FUNCTION	9 (uuid, uuid) gbt_uuid_fetch (internal)
;
CREATE TABLE plt1_adv_p3 PARTITION OF plt1_adv FOR VALUES IN ('0008', '0009')
;
CREATE TABLE stream_test(data bytea)
;
ANALYZE public.extra_wide_table
;
ALTER TABLE part_b DROP CONSTRAINT check_a
;
VACUUM (FREEZE) pg_catalog.pg_amop
;
create table part_xx_yy_p1 partition of part_xx_yy for values in ('xx')
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_largeobject_metadata
;
CREATE OPERATOR CLASS gin__int_ops
FOR TYPE _int4 USING gin
AS
	OPERATOR	3	&&,
	OPERATOR	6	= (anyarray, anyarray),
	OPERATOR	7	@>,
	OPERATOR	8	<@,
	OPERATOR	13	@,
	OPERATOR	14	~,
	OPERATOR	20	@@ (_int4, query_int),
	FUNCTION	1	btint4cmp (int4, int4),
	FUNCTION	2	ginarrayextract (anyarray, internal, internal),
	FUNCTION	3	ginint4_queryextract (_int4, internal, int2, internal, internal, internal, internal),
	FUNCTION	4	ginint4_consistent (internal, int2, _int4, int4, internal, internal, internal, internal),
	STORAGE		int4
;
ALTER FUNCTION get_raw_page(text, text, int8) PARALLEL RESTRICTED
;
--
-- Verify that search_path is set to a safe value during maintenance
-- commands.
--

CREATE SCHEMA test_maint_search_path
;
--fail
CREATE POLICY p1 ON y2 FOR ALL USING (a % 2 = 0)
;
CREATE TRIGGER rep_a_u AFTER UPDATE ON trigtest FOR EACH ROW EXECUTE PROCEDURE trig_report()
;
CREATE OR REPLACE PROCEDURE proc_with_utility_stmt()
LANGUAGE SQL
AS $$
  SHOW pg_stat_statements.track;
  show pg_stat_statements.track;
  SHOW pg_stat_statements.track_utility;
$$
;
create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2.1) $$ language sql
;
ALTER TABLE pg_toast.pg_toast_30385 RENAME TO reind_con_toast
;
CREATE EXTENSION test_slru
;
create schema if not exists abc authorization aac
;
drop table cnn2_parted
;
CREATE INDEX ON tab1 (b) WHERE a > double(1)
;
CREATE TABLE atnotnull1 ()
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "public"."mlparted" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
PARTITION BY RANGE ("a", "b")
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_shdepend
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE remote_backend_pid (pid int)
  SERVER loopback OPTIONS (table_name 'my_backend_pid')
;
----------
[DROP_TRIGGER] drop trigger trg1 on trigpart41
;
----------
[CREATE_POLICY] CREATE POLICY "p1" ON "regress_rls_schema"."rls_tbl_force" USING (("c1" = 5)) WITH CHECK (("c1" < 5))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_fdw_validator(
    options text[],
    catalog oid
)
RETURNS void
AS '$libdir/dblink', 'dblink_fdw_validator'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- not STRICT; allows (null::rectype,hstore)

CREATE OPERATOR #= (
	LEFTARG = anyelement,
	RIGHTARG = hstore,
	PROCEDURE = populate_record
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view13 AS SELECT a, b FROM (SELECT * FROM base_tbl) AS t
;
----------
[CREATE_TABLE] CREATE TABLE ctv_data (x int, y int, v text)
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW mysecview4 WITH (security_barrier=false)
       AS SELECT * FROM tbl1 WHERE a <> 256
;
----------
[ALTER_TABLE] ALTER TABLE rls_tbl FORCE ROW LEVEL SECURITY
;
----------
[CREATE_PROG_OBJ] create function myleast(variadic anyarray) returns anyelement as $$
  select min($1[i]) from generate_subscripts($1,1) g(i)
$$ language sql immutable strict
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION crosstab(text)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_depend
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION tap_sub_A_B
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE flavians IS 'system_u:object_r:sepgsql_table_t:s0'
;
----------
[DROP_TYPE] -- should fail

DROP TYPE default_test_row CASCADE
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_amop
;
----------
[DROP_VIEW] DROP VIEW lock_view6
;
----------
[DROP_TABLE] drop table hs2
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range, c int4range, exclude USING GIST (a with =, b with =, c with &&)) partition by range (a, b)
;
----------
[ADD_INDEX] CREATE INDEX regtest_pindex_tbl4_y ON regtest_ptable_4_ones(y)
;
----------
[CREATE_TABLE] -- a partition of a partitioned table should work though
create table test_partition partition of test_partitioned for values from (1) to (100)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION mysub1 SET PUBLICATION non_existent_pub
;
----------
[CREATE_TYPE] create type compostype as (x int, y varchar)
;
----------
[CREATE_TABLE] create table T ( i int )
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mv AS SELECT * FROM target
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') NOT NULL,
	c2 text OPTIONS (param2 'val2', param3 'val3') CHECK (c2 <> ''),
	c3 date,
	CHECK (c3 BETWEEN '1994-01-01'::date AND '1994-01-31'::date)
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table mlparted1 attach partition mlparted11 for values from (2) to (5)
;
----------
[ALTER_PROG_OBJ] -- These are "loose" in the opfamily for consistency with the rest of btree_gist
ALTER OPERATOR FAMILY gist_uuid_ops USING gist ADD
	OPERATOR	6	<>  (uuid, uuid) ,
	FUNCTION	9 (uuid, uuid) gbt_uuid_fetch (internal)
;
----------
[CREATE_TABLE] CREATE TABLE plt1_adv_p3 PARTITION OF plt1_adv FOR VALUES IN ('0008', '0009')
;
----------
[CREATE_TABLE] CREATE TABLE stream_test(data bytea)
;
----------
[ADMIN_TABLE] ANALYZE public.extra_wide_table
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE part_b DROP CONSTRAINT check_a
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_amop
;
----------
[CREATE_TABLE] create table part_xx_yy_p1 partition of part_xx_yy for values in ('xx')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gin__int_ops
FOR TYPE _int4 USING gin
AS
	OPERATOR	3	&&,
	OPERATOR	6	= (anyarray, anyarray),
	OPERATOR	7	@>,
	OPERATOR	8	<@,
	OPERATOR	13	@,
	OPERATOR	14	~,
	OPERATOR	20	@@ (_int4, query_int),
	FUNCTION	1	btint4cmp (int4, int4),
	FUNCTION	2	ginarrayextract (anyarray, internal, internal),
	FUNCTION	3	ginint4_queryextract (_int4, internal, int2, internal, internal, internal, internal),
	FUNCTION	4	ginint4_consistent (internal, int2, _int4, int4, internal, internal, internal, internal),
	STORAGE		int4
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION get_raw_page(text, text, int8) PARALLEL RESTRICTED
;
----------
[CREATE_SCHEMA] --
-- Verify that search_path is set to a safe value during maintenance
-- commands.
--

CREATE SCHEMA test_maint_search_path
;
----------
[CREATE_POLICY] --fail
CREATE POLICY p1 ON y2 FOR ALL USING (a % 2 = 0)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER rep_a_u AFTER UPDATE ON trigtest FOR EACH ROW EXECUTE PROCEDURE trig_report()
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE PROCEDURE proc_with_utility_stmt()
LANGUAGE SQL
AS $$
  SHOW pg_stat_statements.track;
  show pg_stat_statements.track;
  SHOW pg_stat_statements.track_utility;
$$
;
----------
[CREATE_PROG_OBJ] create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2.1) $$ language sql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30385 RENAME TO reind_con_toast
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_slru
;
----------
[CREATE_SCHEMA] create schema if not exists abc authorization aac
;
----------
[DROP_TABLE] drop table cnn2_parted
;
----------
[ADD_INDEX] CREATE INDEX ON tab1 (b) WHERE a > double(1)
;
----------
[CREATE_TABLE] CREATE TABLE atnotnull1 ()
;
