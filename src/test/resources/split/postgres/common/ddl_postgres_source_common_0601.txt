ALTER TABLE ffk DETACH PARTITION ffk1
;
-- Types
CREATE TYPE stats_type as (f1 numeric(35, 6), f2 numeric(35, 2))
;
CREATE OPERATOR CLASS gist_inet_ops
DEFAULT FOR TYPE inet USING gist
AS
	OPERATOR	1	<   ,
	OPERATOR	2	<=  ,
	OPERATOR	3	=   ,
	OPERATOR	4	>=  ,
	OPERATOR	5	>   ,
	FUNCTION	1	gbt_inet_consistent (internal, inet, int2, oid, internal),
	FUNCTION	2	gbt_inet_union (internal, internal),
	FUNCTION	3	gbt_inet_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_inet_penalty (internal, internal, internal),
	FUNCTION	6	gbt_inet_picksplit (internal, internal),
	FUNCTION	7	gbt_inet_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
create temp table parent (k int primary key, pd int)
;
/* contrib/tsm_system_rows/tsm_system_rows--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION system_rows(internal)
RETURNS tsm_handler
AS '$libdir/tsm_system_rows', 'tsm_system_rows_handler'
LANGUAGE C STRICT
;
CREATE TEMP TABLE arraggtest ( f1 INT[], f2 TEXT[][], f3 FLOAT[])
;
-- partition on boolean column
create table boolspart (a bool) partition by list (a)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_dict
;
create function returns_rw_array(int) returns int[]
language plpgsql as $$
  declare r int[];
  begin r := array[$1, $1]; return r; end;
$$ stable
;
-- need 2 tables for "use_own_xacts"
CREATE TEMP TABLE past_inh_db_parent () ON COMMIT DELETE ROWS
;
alter table inh_nn2 inherit inh_nn1
;
CREATE TABLE cmpart1 PARTITION OF cmpart FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c7 DROP NOT NULL
;
CREATE INDEX brinidx_unlogged ON brintest_unlogged USING brin (n)
;
create table iocdu_tt_parted3 partition of iocdu_tt_parted for values in (3)
;
CREATE FUNCTION pg_catalog.pg_logdir_ls()
RETURNS setof record
AS '$libdir/adminpack', 'pg_logdir_ls'
LANGUAGE C VOLATILE STRICT
;
-- try removing an oid column, should succeed (as it's nonexistent)
alter table atacc1 SET WITHOUT OIDS
;
CREATE TABLE test_pg_c_utf8 (
  t TEXT COLLATE PG_C_UTF8
)
;
ALTER TABLE vtype2 ALTER COLUMN b SET DEFAULT 'yyy'
;
create index on bool_lp (b)
;
-- immutable SQL function --- can be executed at plan time
CREATE FUNCTION PLUS_THREE(i INTEGER) RETURNS INTEGER AS
$$ SELECT i + 3 LIMIT 1 $$ IMMUTABLE LANGUAGE SQL
;
-- test internally compressing datums

-- this tests compressing a datum to a very small size which exercises a
-- corner case in packed-varlena handling: even though small, the compressed
-- datum must be given a 4-byte header because there are no bits to indicate
-- compression in a 1-byte header

CREATE TABLE toasttest (c char(4096))
;
ALTER VIEW IF EXISTS split_view_probe.base_view RENAME TO base_view_renamed
;
--
-- Tests for record_image_{eq,cmp}
--

create type testtype1 as (a int, b int)
;
CREATE INDEX "onek_stringu1" ON "public"."onek" USING "btree" ("stringu1")
;
ALTER TABLE ONLY "public"."mlparted_def" DROP COLUMN "........pg.dropped.4........"
;
CREATE TABLE ctla (aa TEXT)
;
VACUUM (ANALYZE) pg_catalog.pg_shdescription
;
DROP TABLE t
;
CREATE OR REPLACE FUNCTION
  jsonb_path_exists_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                    silent boolean DEFAULT false)
RETURNS boolean
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_exists_tz'
;
drop domain dnull restrict
;
CREATE FUNCTION dblink_open (text, text, text)
RETURNS text
AS '$libdir/dblink','dblink_open'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE SUBSCRIPTION split_sub CONNECTION 'dbname=postgres' PUBLICATION split_pub_renamed WITH (connect = false)
;
-- Various partitioning-related functions return empty/NULL if passed relations
-- of types that cannot be part of a partition tree; for example, views,
-- materialized views, legacy inheritance children or parents, etc.
CREATE VIEW ptif_test_view AS SELECT 1
;
DROP TABLE IF EXISTS multixact_conflict
;
ALTER TABLE pg_temp_9.pg_temp_38133_2 ADD COLUMN newdata pg_temp_9.pg_temp_38133
;
DROP TABLE sch1.tbl1
;
CREATE TABLE amcheck_schema.pg_operator (junk text)
;
DROP SCHEMA pub_test2 CASCADE
;
VACUUM (PARALLEL 2) pg_catalog.pg_publication
;
CREATE TABLE gtest22c (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
-- Test parallel string_agg and array_agg
create table pagg_test (x int, y int) with (autovacuum_enabled = off)
;
-- make test configuration using parser

CREATE TEXT SEARCH CONFIGURATION testcfg (PARSER = testparser)
;
DROP FUNCTION multi_test_trig()
;
create table mcrparted5_def partition of mcrparted5 default
;
--
-- Renaming
--

create domain testdomain1 as int
;
CREATE TABLE created_table AS SELECT * FROM int8_tbl
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_depend
;
CREATE RULE rule1 AS ON INSERT TO rule_merge1
	DO INSTEAD INSERT INTO rule_merge2 VALUES (NEW.*)
;
CREATE TABLE stats_ext_tbl (id INT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, col TEXT)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ffk DETACH PARTITION ffk1
;
----------
[CREATE_TYPE] -- Types
CREATE TYPE stats_type as (f1 numeric(35, 6), f2 numeric(35, 2))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_inet_ops
DEFAULT FOR TYPE inet USING gist
AS
	OPERATOR	1	<   ,
	OPERATOR	2	<=  ,
	OPERATOR	3	=   ,
	OPERATOR	4	>=  ,
	OPERATOR	5	>   ,
	FUNCTION	1	gbt_inet_consistent (internal, inet, int2, oid, internal),
	FUNCTION	2	gbt_inet_union (internal, internal),
	FUNCTION	3	gbt_inet_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_inet_penalty (internal, internal, internal),
	FUNCTION	6	gbt_inet_picksplit (internal, internal),
	FUNCTION	7	gbt_inet_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[CREATE_TABLE] create temp table parent (k int primary key, pd int)
;
----------
[CREATE_PROG_OBJ] /* contrib/tsm_system_rows/tsm_system_rows--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION system_rows(internal)
RETURNS tsm_handler
AS '$libdir/tsm_system_rows', 'tsm_system_rows_handler'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] CREATE TEMP TABLE arraggtest ( f1 INT[], f2 TEXT[][], f3 FLOAT[])
;
----------
[CREATE_TABLE] -- partition on boolean column
create table boolspart (a bool) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_dict
;
----------
[CREATE_PROG_OBJ] create function returns_rw_array(int) returns int[]
language plpgsql as $$
  declare r int[];
  begin r := array[$1, $1]; return r; end;
$$ stable
;
----------
[CREATE_TABLE] -- need 2 tables for "use_own_xacts"
CREATE TEMP TABLE past_inh_db_parent () ON COMMIT DELETE ROWS
;
----------
[ALTER_TABLE] alter table inh_nn2 inherit inh_nn1
;
----------
[CREATE_TABLE] CREATE TABLE cmpart1 PARTITION OF cmpart FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c7 DROP NOT NULL
;
----------
[ADD_INDEX] CREATE INDEX brinidx_unlogged ON brintest_unlogged USING brin (n)
;
----------
[CREATE_TABLE] create table iocdu_tt_parted3 partition of iocdu_tt_parted for values in (3)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_catalog.pg_logdir_ls()
RETURNS setof record
AS '$libdir/adminpack', 'pg_logdir_ls'
LANGUAGE C VOLATILE STRICT
;
----------
[ALTER_TABLE] -- try removing an oid column, should succeed (as it's nonexistent)
alter table atacc1 SET WITHOUT OIDS
;
----------
[CREATE_TABLE] CREATE TABLE test_pg_c_utf8 (
  t TEXT COLLATE PG_C_UTF8
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE vtype2 ALTER COLUMN b SET DEFAULT 'yyy'
;
----------
[ADD_INDEX] create index on bool_lp (b)
;
----------
[CREATE_PROG_OBJ] -- immutable SQL function --- can be executed at plan time
CREATE FUNCTION PLUS_THREE(i INTEGER) RETURNS INTEGER AS
$$ SELECT i + 3 LIMIT 1 $$ IMMUTABLE LANGUAGE SQL
;
----------
[CREATE_TABLE] -- test internally compressing datums

-- this tests compressing a datum to a very small size which exercises a
-- corner case in packed-varlena handling: even though small, the compressed
-- datum must be given a 4-byte header because there are no bits to indicate
-- compression in a 1-byte header

CREATE TABLE toasttest (c char(4096))
;
----------
[RENAME_VIEW] ALTER VIEW IF EXISTS split_view_probe.base_view RENAME TO base_view_renamed
;
----------
[CREATE_TYPE] --
-- Tests for record_image_{eq,cmp}
--

create type testtype1 as (a int, b int)
;
----------
[ADD_INDEX] CREATE INDEX "onek_stringu1" ON "public"."onek" USING "btree" ("stringu1")
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def" DROP COLUMN "........pg.dropped.4........"
;
----------
[CREATE_TABLE] CREATE TABLE ctla (aa TEXT)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_shdescription
;
----------
[DROP_TABLE] DROP TABLE t
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_exists_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                    silent boolean DEFAULT false)
RETURNS boolean
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_exists_tz'
;
----------
[DROP_TYPE] drop domain dnull restrict
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_open (text, text, text)
RETURNS text
AS '$libdir/dblink','dblink_open'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION split_sub CONNECTION 'dbname=postgres' PUBLICATION split_pub_renamed WITH (connect = false)
;
----------
[CREATE_VIEW(SELECT)] -- Various partitioning-related functions return empty/NULL if passed relations
-- of types that cannot be part of a partition tree; for example, views,
-- materialized views, legacy inheritance children or parents, etc.
CREATE VIEW ptif_test_view AS SELECT 1
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS multixact_conflict
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_38133_2 ADD COLUMN newdata pg_temp_9.pg_temp_38133
;
----------
[DROP_TABLE] DROP TABLE sch1.tbl1
;
----------
[CREATE_TABLE] CREATE TABLE amcheck_schema.pg_operator (junk text)
;
----------
[DROP_SCHEMA] DROP SCHEMA pub_test2 CASCADE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_publication
;
----------
[CREATE_TABLE] CREATE TABLE gtest22c (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_TABLE] -- Test parallel string_agg and array_agg
create table pagg_test (x int, y int) with (autovacuum_enabled = off)
;
----------
[CREATE_POLICY] -- make test configuration using parser

CREATE TEXT SEARCH CONFIGURATION testcfg (PARSER = testparser)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION multi_test_trig()
;
----------
[CREATE_TABLE] create table mcrparted5_def partition of mcrparted5 default
;
----------
[CREATE_TYPE] --
-- Renaming
--

create domain testdomain1 as int
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE created_table AS SELECT * FROM int8_tbl
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_depend
;
----------
[CREATE_POLICY] CREATE RULE rule1 AS ON INSERT TO rule_merge1
	DO INSTEAD INSERT INTO rule_merge2 VALUES (NEW.*)
;
----------
[CREATE_TABLE] CREATE TABLE stats_ext_tbl (id INT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, col TEXT)
;
