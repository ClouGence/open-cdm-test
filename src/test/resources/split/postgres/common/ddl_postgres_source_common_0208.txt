create table idxpart (a int4range, b int4range) partition by range (a, b)
;
CREATE INDEX brin_idx ON brin_idx_tbl USING brin (col1, col2, col3) WITH (autosummarize=on)
;
-- tests for on_error option
CREATE TABLE check_ign_err (n int, m int[], k int)
;
ANALYZE pg_catalog.pg_extension
;
CREATE TABLE ext_stats_test (x int, y int, z int)
;
CREATE TABLE "regress_indexing"."pk4" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
-- EXISTS PATH domain over int
CREATE DOMAIN dint4 AS int
;
CREATE TABLE test_varchar (
	i varchar
)
;
-- ok
DROP MATERIALIZED VIEW matview_ine_tab
;
CREATE TABLE gtest_child PARTITION OF gtest_parent
  FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
CREATE TABLE PKTABLE (ptest1 int PRIMARY KEY, someoid oid)
;
CREATE AGGREGATE myaggn20a(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
IMPORT FOREIGN SCHEMA s1 EXCEPT (t1, t2) FROM SERVER s9 INTO public
OPTIONS (option1 'value1', option2 'value2')
;
DROP INDEX pht1_c_idx CASCADE
;
CREATE INDEX idx_pgvector_items_half_l2 ON split_pgvector_probe.items USING hnsw (half_embedding halfvec_l2_ops)
;
ALTER TABLE tststats.priv_test_tbl ENABLE ROW LEVEL SECURITY
;
alter foreign table foo2 options (use_remote_estimate 'true')
;
DROP INDEX base_tbl1_idx
;
create function makedcomp(r float8, i float8) returns dcomptype
as 'select row(r, i)' language sql
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub1, pub2
;
--
-- pg_get_wal_block_info()
--
CREATE FUNCTION pg_get_wal_block_info(IN start_lsn pg_lsn,
	IN end_lsn pg_lsn,
	IN show_data boolean DEFAULT true,
	OUT start_lsn pg_lsn,
	OUT end_lsn pg_lsn,
	OUT prev_lsn pg_lsn,
	OUT block_id int2,
	OUT reltablespace oid,
	OUT reldatabase oid,
	OUT relfilenode oid,
	OUT relforknumber int2,
	OUT relblocknumber int8,
	OUT xid xid,
	OUT resource_manager text,
	OUT record_type text,
	OUT record_length int4,
	OUT main_data_length int4,
	OUT block_data_length int4,
	OUT block_fpi_length int4,
	OUT block_fpi_info text[],
	OUT description text,
	OUT block_data bytea,
	OUT block_fpi_data bytea
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_block_info'
LANGUAGE C STRICT PARALLEL SAFE
;
alter table utrtest attach partition utr2 for values in (2)
;
DROP FUNCTION IF EXISTS test_stat_func2()
;
alter table fkpart0.fk_part add foreign key (a) references fkpart0.pkey
;
TRUNCATE fd_pt1
;
-- SKIP_LOCKED option
VACUUM (SKIP_LOCKED) vactst
;
drop table test_jsonb_subscript
;
CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
ANALYZE prt1_e
;
DROP VIEW viewtest
;
vacuum �ͪ�ߩ�Ѧ��듾�
;
DROP TABLE test_constraints
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
ALTER OPERATOR FAMILY gist_float4_ops USING gist ADD
	FUNCTION	11  (float4, float4) gbt_float4_sortsupport (internal)
;
alter table idxpart add exclude USING GIST (a with =)
;
-- Check that index expressions and predicates are run as the table's owner

-- A dummy index function checking current_user
CREATE FUNCTION sro_ifun(int) RETURNS int AS $$
BEGIN
	-- Below we set the table's owner to regress_sro_user
	ASSERT current_user = 'regress_sro_user',
		format('sro_ifun(%s) called by %s', $1, current_user);
	RETURN $1;
END;
$$ LANGUAGE plpgsql IMMUTABLE
;
-- error, must truncate partitions
TRUNCATE ONLY truncparted
;
CREATE FUNCTION seg_right(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- ok
DROP VIEW tid_view_with_ctid
;
create table p1_c2(constraint f1_pos CHECK (f1 > 0)) inherits (p1)
;
CREATE TABLE tbl2 (a INT)
;
CREATE FUNCTION isngt(issn13, issn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- Show tupleids of dead tuples if any in all_visible pages for a relation.
CREATE FUNCTION pg_check_visible(regclass, t_ctid OUT tid)
RETURNS SETOF tid
AS '$libdir/pg_visibility', 'pg_check_visible'
LANGUAGE C STRICT
;
CREATE SCHEMA evttrig
	CREATE TABLE one (col_a SERIAL PRIMARY KEY, col_b text DEFAULT 'forty two', col_c SERIAL)
	CREATE INDEX one_idx ON one (col_b)
	CREATE TABLE two (col_c INTEGER CHECK (col_c > 0) REFERENCES one DEFAULT 42)
	CREATE TABLE id (col_d int NOT NULL GENERATED ALWAYS AS IDENTITY)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_tablespace
;
CREATE INDEX ON table2(col2)
;
CREATE TABLE test_part_2_1 PARTITION OF test_part FOR VALUES IN (7,8,9,10,11,12) PARTITION BY LIST (a)
;
ANALYZE public.trigger_parted_p1_1
;
--
-- Test multiple multirange types over the same subtype and manual naming of
-- the multirange type.
--

-- should fail
create type textrange1 as range(subtype=text, multirange_type_name=int, collation="C")
;
-- fails
alter table anothertab drop constraint IF EXISTS anothertab_chk
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table idxpart (a int4range, b int4range) partition by range (a, b)
;
----------
[ADD_INDEX] CREATE INDEX brin_idx ON brin_idx_tbl USING brin (col1, col2, col3) WITH (autosummarize=on)
;
----------
[CREATE_TABLE] -- tests for on_error option
CREATE TABLE check_ign_err (n int, m int[], k int)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_extension
;
----------
[CREATE_TABLE] CREATE TABLE ext_stats_test (x int, y int, z int)
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk4" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[CREATE_TYPE] -- EXISTS PATH domain over int
CREATE DOMAIN dint4 AS int
;
----------
[CREATE_TABLE] CREATE TABLE test_varchar (
	i varchar
)
;
----------
[DROP_VIEW] -- ok
DROP MATERIALIZED VIEW matview_ine_tab
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child PARTITION OF gtest_parent
  FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 int PRIMARY KEY, someoid oid)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn20a(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA s1 EXCEPT (t1, t2) FROM SERVER s9 INTO public
OPTIONS (option1 'value1', option2 'value2')
;
----------
[DROP_INDEX] DROP INDEX pht1_c_idx CASCADE
;
----------
[ADD_INDEX] CREATE INDEX idx_pgvector_items_half_l2 ON split_pgvector_probe.items USING hnsw (half_embedding halfvec_l2_ops)
;
----------
[ALTER_TABLE] ALTER TABLE tststats.priv_test_tbl ENABLE ROW LEVEL SECURITY
;
----------
[ALTER_TABLE] alter foreign table foo2 options (use_remote_estimate 'true')
;
----------
[DROP_INDEX] DROP INDEX base_tbl1_idx
;
----------
[CREATE_PROG_OBJ] create function makedcomp(r float8, i float8) returns dcomptype
as 'select row(r, i)' language sql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub1, pub2
;
----------
[CREATE_PROG_OBJ] --
-- pg_get_wal_block_info()
--
CREATE FUNCTION pg_get_wal_block_info(IN start_lsn pg_lsn,
	IN end_lsn pg_lsn,
	IN show_data boolean DEFAULT true,
	OUT start_lsn pg_lsn,
	OUT end_lsn pg_lsn,
	OUT prev_lsn pg_lsn,
	OUT block_id int2,
	OUT reltablespace oid,
	OUT reldatabase oid,
	OUT relfilenode oid,
	OUT relforknumber int2,
	OUT relblocknumber int8,
	OUT xid xid,
	OUT resource_manager text,
	OUT record_type text,
	OUT record_length int4,
	OUT main_data_length int4,
	OUT block_data_length int4,
	OUT block_fpi_length int4,
	OUT block_fpi_info text[],
	OUT description text,
	OUT block_data bytea,
	OUT block_fpi_data bytea
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_block_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table utrtest attach partition utr2 for values in (2)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS test_stat_func2()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table fkpart0.fk_part add foreign key (a) references fkpart0.pkey
;
----------
[TRUNCATE_TABLE] TRUNCATE fd_pt1
;
----------
[ADMIN_TABLE] -- SKIP_LOCKED option
VACUUM (SKIP_LOCKED) vactst
;
----------
[DROP_TABLE] drop table test_jsonb_subscript
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
----------
[ADMIN_TABLE] ANALYZE prt1_e
;
----------
[DROP_VIEW] DROP VIEW viewtest
;
----------
[ADMIN_TABLE] vacuum �ͪ�ߩ�Ѧ��듾�
;
----------
[DROP_TABLE] DROP TABLE test_constraints
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_float4_ops USING gist ADD
	FUNCTION	11  (float4, float4) gbt_float4_sortsupport (internal)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add exclude USING GIST (a with =)
;
----------
[CREATE_PROG_OBJ] -- Check that index expressions and predicates are run as the table's owner

-- A dummy index function checking current_user
CREATE FUNCTION sro_ifun(int) RETURNS int AS $$
BEGIN
	-- Below we set the table's owner to regress_sro_user
	ASSERT current_user = 'regress_sro_user',
		format('sro_ifun(%s) called by %s', $1, current_user);
	RETURN $1;
END;
$$ LANGUAGE plpgsql IMMUTABLE
;
----------
[TRUNCATE_TABLE] -- error, must truncate partitions
TRUNCATE ONLY truncparted
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_right(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_VIEW] -- ok
DROP VIEW tid_view_with_ctid
;
----------
[CREATE_TABLE] create table p1_c2(constraint f1_pos CHECK (f1 > 0)) inherits (p1)
;
----------
[CREATE_TABLE] CREATE TABLE tbl2 (a INT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(issn13, issn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- Show tupleids of dead tuples if any in all_visible pages for a relation.
CREATE FUNCTION pg_check_visible(regclass, t_ctid OUT tid)
RETURNS SETOF tid
AS '$libdir/pg_visibility', 'pg_check_visible'
LANGUAGE C STRICT
;
----------
[CREATE_SCHEMA] CREATE SCHEMA evttrig
	CREATE TABLE one (col_a SERIAL PRIMARY KEY, col_b text DEFAULT 'forty two', col_c SERIAL)
	CREATE INDEX one_idx ON one (col_b)
	CREATE TABLE two (col_c INTEGER CHECK (col_c > 0) REFERENCES one DEFAULT 42)
	CREATE TABLE id (col_d int NOT NULL GENERATED ALWAYS AS IDENTITY)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_tablespace
;
----------
[ADD_INDEX] CREATE INDEX ON table2(col2)
;
----------
[CREATE_TABLE] CREATE TABLE test_part_2_1 PARTITION OF test_part FOR VALUES IN (7,8,9,10,11,12) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] ANALYZE public.trigger_parted_p1_1
;
----------
[CREATE_POLICY] --
-- Test multiple multirange types over the same subtype and manual naming of
-- the multirange type.
--

-- should fail
create type textrange1 as range(subtype=text, multirange_type_name=int, collation="C")
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- fails
alter table anothertab drop constraint IF EXISTS anothertab_chk
;
