INSERT INTO pg_temp_11.pg_temp_41207_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_41207 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_11.pg_temp_41207 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2067', relminmxid = '1'
WHERE oid = '"public"."fast_emp4000"'::pg_catalog.regclass
;
UPDATE temporal_rng SET valid_at = daterange('2018-01-01', '2018-03-01')
  WHERE id = '[1,2)' AND valid_at @> '2018-01-25'::date
;
update rules_src set f2 = f2 + 1
;
insert into emp values ( 'josh' , 10000 , 10 )
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18889', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_m_p1"'::pg_catalog.regclass
;
INSERT INTO document VALUES (1, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'my first novel')
    ON CONFLICT (did) DO UPDATE SET dauthor = 'regress_rls_carol'
;
-- should fail:
UPDATE temporal_partitioned_fk_rng2rng SET valid_at = daterange('2000-01-01', '2000-04-01') WHERE id = '[1,2)'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12634', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_2_2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15920', relminmxid = '3'
WHERE oid = '"public"."event_trigger_test"'::pg_catalog.regclass
;
-- ok
INSERT INTO testschema.atable VALUES(1)
;
UPDATE pgbench_accounts SET abalance = abalance + 1074 WHERE aid = 82766
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16130', relminmxid = '3'
WHERE oid = '"public"."dropcolumnanother"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."fk11"'::pg_catalog.regclass
  AND attname IN ('a')
;
DELETE FROM base_tbl WHERE a=1
;
INSERT INTO test_part_d VALUES (1, 2)
;
-- update both at once
UPDATE  temporal_mltrng3
SET     id = '[21,22)',
        valid_at = '{[2018-01-02,2018-02-03)}'
WHERE   id = '[11,12)'
AND     valid_at @> '2020-01-15'::date
;
INSERT INTO ins_trig
				VALUES (DEFAULT, 'triggered row before' || NEW.id2)
;
DELETE FROM temporal_rng WHERE id = '[5,6)' AND valid_at = daterange('2018-01-01', '2018-02-01')
;
INSERT INTO public.tplain VALUES ('8', 898)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted2"'::pg_catalog.regclass
  AND attname IN ('b', 'a', 'c')
;
UPDATE mytable SET value = 'foo' WHERE key = 2
;
INSERT INTO public.ths_p1 VALUES (6, 696)
;
-- ...so violates actual WITH CHECK OPTION within UPDATE (not INSERT, since
-- alternative UPDATE path happens to be taken):
INSERT INTO document VALUES (2, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_carol', 'my first novel')
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle, dauthor = EXCLUDED.dauthor
;
insert into slave values($1)
;
UPDATE unique_tbl SET i = 2 WHERE i = 4 AND t = 'four'
;
-- id matches but no conflict
INSERT INTO temporal_mltrng (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2010-01-01', '2020-01-01'))) ON CONFLICT DO NOTHING
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."mlparted1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12726', relminmxid = '3'
WHERE oid = '"fkpart3"."pk52"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.17........', -1, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"testxmlschema"."test2"'::pg_catalog.regclass
  AND attname = v.dname
;
INSERT INTO s1.t2 (i, b, ia, ir, t)
				(SELECT gs::INTEGER AS i,
						box(point(gs,gs+5),point(gs*2,gs*3)) AS b,
						array[gs, gs + 1]::int4[] AS ia,
						int4range(gs, gs+100) AS ir,
						repeat('foo', gs) AS t
					 FROM generate_series(1,10000,3000) AS gs)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt2_l_p3_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
insert into lprt_a values(1),(1)
;
update parent set b = parent.b || remt2.b from remt2 where parent.a = remt2.a returning *
;
delete from trigtest
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt4_n_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17946', relminmxid = '3'
WHERE oid = '"public"."test_io_vac_strategy"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (0, 540)
;
INSERT INTO num_exp_sub VALUES (4,9,'32726265.456947420')
;
insert into foo values('qq','rr')
;
INSERT INTO itestv10 OVERRIDING USER VALUE VALUES (11, 'xyz')
;
INSERT INTO gin_idx_tbl
    WITH random_json AS (
        SELECT json_object_agg(key, trunc(random() * 10)) as json_data
            FROM unnest(array['a', 'b', 'c']) as u(key))
          SELECT generate_series(1,500), json_data FROM random_json
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."itest11"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15972', relminmxid = '3'
WHERE oid = '"public"."tt5"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2651', relminmxid = '1'
WHERE oid = '"public"."view_base_table"'::pg_catalog.regclass
;
INSERT INTO trunc_stats_test3 DEFAULT VALUES
;
INSERT INTO num_exp_sub VALUES (0,0,'0')
;
UPDATE trigtest SET data = data || '-ups1'
    WHERE
        noisy_oper('upd', key, '=', 'key-b') AND
        noisy_oper('upk', data, '<>', 'mismatch')
    RETURNING *
;
UPDATE trigtest SET key = 'key-b', data = data || '-tobs1'
    WHERE
        noisy_oper('upk', key, '=', 'key-a') AND
        noisy_oper('upk', data, '<>', 'mismatch')
    RETURNING *
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4278', relminmxid = '1'
WHERE oid = '20916'
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] INSERT INTO pg_temp_11.pg_temp_41207_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_41207 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_11.pg_temp_41207 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2067', relminmxid = '1'
WHERE oid = '"public"."fast_emp4000"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE temporal_rng SET valid_at = daterange('2018-01-01', '2018-03-01')
  WHERE id = '[1,2)' AND valid_at @> '2018-01-25'::date
;
----------
[UPDATE] update rules_src set f2 = f2 + 1
;
----------
[INSERT] insert into emp values ( 'josh' , 10000 , 10 )
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '18889', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_m_p1"'::pg_catalog.regclass
;
----------
[MERGE|SELECT] INSERT INTO document VALUES (1, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'my first novel')
    ON CONFLICT (did) DO UPDATE SET dauthor = 'regress_rls_carol'
;
----------
[UPDATE] -- should fail:
UPDATE temporal_partitioned_fk_rng2rng SET valid_at = daterange('2000-01-01', '2000-04-01') WHERE id = '[1,2)'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12634', relminmxid = '3'
WHERE oid = '"public"."fk_partitioned_fk_2_2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15920', relminmxid = '3'
WHERE oid = '"public"."event_trigger_test"'::pg_catalog.regclass
;
----------
[INSERT] -- ok
INSERT INTO testschema.atable VALUES(1)
;
----------
[UPDATE] UPDATE pgbench_accounts SET abalance = abalance + 1074 WHERE aid = 82766
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16130', relminmxid = '3'
WHERE oid = '"public"."dropcolumnanother"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."fk11"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[DELETE] DELETE FROM base_tbl WHERE a=1
;
----------
[INSERT] INSERT INTO test_part_d VALUES (1, 2)
;
----------
[UPDATE] -- update both at once
UPDATE  temporal_mltrng3
SET     id = '[21,22)',
        valid_at = '{[2018-01-02,2018-02-03)}'
WHERE   id = '[11,12)'
AND     valid_at @> '2020-01-15'::date
;
----------
[INSERT] INSERT INTO ins_trig
				VALUES (DEFAULT, 'triggered row before' || NEW.id2)
;
----------
[DELETE] DELETE FROM temporal_rng WHERE id = '[5,6)' AND valid_at = daterange('2018-01-01', '2018-02-01')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 898)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted2"'::pg_catalog.regclass
  AND attname IN ('b', 'a', 'c')
;
----------
[UPDATE] UPDATE mytable SET value = 'foo' WHERE key = 2
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 696)
;
----------
[MERGE|SELECT] -- ...so violates actual WITH CHECK OPTION within UPDATE (not INSERT, since
-- alternative UPDATE path happens to be taken):
INSERT INTO document VALUES (2, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_carol', 'my first novel')
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle, dauthor = EXCLUDED.dauthor
;
----------
[INSERT] insert into slave values($1)
;
----------
[UPDATE] UPDATE unique_tbl SET i = 2 WHERE i = 4 AND t = 'four'
;
----------
[MERGE|SELECT] -- id matches but no conflict
INSERT INTO temporal_mltrng (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2010-01-01', '2020-01-01'))) ON CONFLICT DO NOTHING
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."mlparted1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12726', relminmxid = '3'
WHERE oid = '"fkpart3"."pk52"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.17........', -1, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"testxmlschema"."test2"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT|SELECT] INSERT INTO s1.t2 (i, b, ia, ir, t)
				(SELECT gs::INTEGER AS i,
						box(point(gs,gs+5),point(gs*2,gs*3)) AS b,
						array[gs, gs + 1]::int4[] AS ia,
						int4range(gs, gs+100) AS ir,
						repeat('foo', gs) AS t
					 FROM generate_series(1,10000,3000) AS gs)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt2_l_p3_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] insert into lprt_a values(1),(1)
;
----------
[UPDATE] update parent set b = parent.b || remt2.b from remt2 where parent.a = remt2.a returning *
;
----------
[DELETE] delete from trigtest
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt4_n_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17946', relminmxid = '3'
WHERE oid = '"public"."test_io_vac_strategy"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 540)
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (4,9,'32726265.456947420')
;
----------
[INSERT] insert into foo values('qq','rr')
;
----------
[INSERT|SELECT] INSERT INTO itestv10 OVERRIDING USER VALUE VALUES (11, 'xyz')
;
----------
[INSERT|SELECT] INSERT INTO gin_idx_tbl
    WITH random_json AS (
        SELECT json_object_agg(key, trunc(random() * 10)) as json_data
            FROM unnest(array['a', 'b', 'c']) as u(key))
          SELECT generate_series(1,500), json_data FROM random_json
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."itest11"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15972', relminmxid = '3'
WHERE oid = '"public"."tt5"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2651', relminmxid = '1'
WHERE oid = '"public"."view_base_table"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO trunc_stats_test3 DEFAULT VALUES
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (0,0,'0')
;
----------
[UPDATE] UPDATE trigtest SET data = data || '-ups1'
    WHERE
        noisy_oper('upd', key, '=', 'key-b') AND
        noisy_oper('upk', data, '<>', 'mismatch')
    RETURNING *
;
----------
[UPDATE] UPDATE trigtest SET key = 'key-b', data = data || '-tobs1'
    WHERE
        noisy_oper('upk', key, '=', 'key-a') AND
        noisy_oper('upk', data, '<>', 'mismatch')
    RETURNING *
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4278', relminmxid = '1'
WHERE oid = '20916'
;
