create unique index collation_index_key on insertconflicttest(key, fruit collate "C")
;
CREATE TABLE TEMP_INT2 (f1 INT2)
;
CREATE TABLE atest6 (one int, two int, blue int)
;
create table date_test ( d date , ts timestamp )
;
create table part_21_30 partition of range_list_parted for values from (21) to (30) partition by list (b)
;
alter table inh_parent add constraint inh_parent_fk foreign key (a) references inh_parent (a)
;
ALTER TABLE pg_toast.pg_toast_30423 RENAME TO reind_con_toast
;
ALTER PUBLICATION tap_pub_ins_only ADD TABLE tab_full
;
--
-- MONEY
--
-- Note that we assume lc_monetary has been set to C.
--

CREATE TABLE money_data (m money)
;
CREATE FUNCTION unaccent_init(internal)
	RETURNS internal
	AS '$libdir/unaccent', 'unaccent_init'
	LANGUAGE C PARALLEL SAFE
;
CREATE TABLE vacparted_i1 PARTITION OF vacparted_i
  FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
-- Bug #17245: Make sure that we don't totally fail to VACUUM individual indexes that
-- happen to be below min_parallel_index_scan_size during parallel VACUUM:
CREATE TABLE parallel_vacuum_table (a int) WITH (autovacuum_enabled = off)
;
CREATE INDEX "sk_a_idx" ON "public"."sk" USING "btree" ("a")
;
ALTER DATABASE conflict_db SET TABLESPACE test_tablespace
;
-- Test comments
COMMENT ON CONSTRAINT constrname_wrong ON FKTABLE IS 'fk constraint comment'
;
--
-- test MERGE
--
CREATE TABLE rule_merge1 (a int, b text)
;
CREATE TABLE tststats.priv_test_tbl (
    a int,
    b int
)
;
create index ��Ƒ�㑻�������index2 on ��Ƒ�㑻������� using btree(��֑�����)
;
CREATE AGGREGATE myaggp16a(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
CREATE UNIQUE INDEX "iface_name" ON "public"."iface" USING "btree" ("slotname")
;
CREATE MATERIALIZED VIEW s1.t1_mv AS SELECT * FROM s1.t1
;
drop table t1
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_rewrite
;
CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=16008 host=/tmp/B3ysgEyCZr dbname=postgres' PUBLICATION pub_dropped_cols
;
-- make sure we decode correctly even if the toast table is gone
DROP TABLE toasttable
;
CREATE TABLE "public"."pagg_tab_ml_p3" (
    "b" integer,
    "c" "text",
    "a" integer
)
PARTITION BY RANGE ("b")
;
--
-- Compare results between plans using sorting and plans using hash
-- aggregation. Force spilling in both cases by setting work_mem low
-- and altering the statistics.
--

create table gs_data_1 as
select g%1000 as g1000, g%100 as g100, g%10 as g10, g
   from generate_series(0,1999) g
;
create or replace function returnsrecord(int) returns record language plpgsql as
$$ begin return row($1,$1+1)::has_hole; end $$
;
-- test jsonb -> python dict
CREATE FUNCTION test1(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {'a': 1, 'c': 'NULL'})
return len(val)
$$
;
alter table atacc1 drop d
;
ALTER TABLE pg_toast.pg_toast_30890 RENAME TO reind_con_toast
;
create function anyarray_anymultirange_func(a anyarray, r anymultirange)
  returns anyelement as 'select $1[1] + lower($2);' language sql
;
-- error, cannot use PARALLEL option without parallel degree

-- Test different combinations of parallel and full options for temporary tables
CREATE TEMPORARY TABLE tmp (a int PRIMARY KEY)
;
CREATE INDEX "reloptions_test_idx" ON "public"."reloptions_test" USING "btree" ("s") WITH ("fillfactor"='40')
;
create table a1 ( id serial primary key , t text , d1 numeric , d2 float8 , c character ( 10 ) )
;
CREATE TRIGGER after_upd_stmt_trig AFTER UPDATE ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('after_view_upd_stmt')
;
ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (issn, ean13),
	OPERATOR 1  = (issn, issn13)
;
CREATE TABLE part_m02 PARTITION OF pa_target
	FOR VALUES FROM ('2017-02-01') TO ('2017-03-01')
	PARTITION BY LIST (tid)
;
drop table inhpar cascade
;
-- fail
SECURITY LABEL ON ROLE regress_dummy_seclabel_user1 IS 'secret'
;
CREATE FUNCTION bt_index_check(index regclass,
    heapallindexed boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE DATABASE "testdb1" WITH TEMPLATE = template0 OID = 16387 STRATEGY = FILE_COPY ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
CREATE TABLE tt9(c integer)
;
create table t9()
;
CREATE TABLE regr_test (x float8, y float8)
;
CREATE TEMP TABLE FKTABLE (ftest1 inet)
;
create type bogus_float8range as range (subtype=float8, subtype_diff=float4mi)
;
ALTER TABLE attmp RENAME TO attmp_new2
;
ALTER TABLE split_alter_table.base_table ALTER COLUMN payload SET STATISTICS -1
;
------------------------------------------------------------------------------------------
[ADD_INDEX] create unique index collation_index_key on insertconflicttest(key, fruit collate "C")
;
----------
[CREATE_TABLE] CREATE TABLE TEMP_INT2 (f1 INT2)
;
----------
[CREATE_TABLE] CREATE TABLE atest6 (one int, two int, blue int)
;
----------
[CREATE_TABLE] create table date_test ( d date , ts timestamp )
;
----------
[CREATE_TABLE] create table part_21_30 partition of range_list_parted for values from (21) to (30) partition by list (b)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table inh_parent add constraint inh_parent_fk foreign key (a) references inh_parent (a)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30423 RENAME TO reind_con_toast
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_ins_only ADD TABLE tab_full
;
----------
[CREATE_TABLE] --
-- MONEY
--
-- Note that we assume lc_monetary has been set to C.
--

CREATE TABLE money_data (m money)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION unaccent_init(internal)
	RETURNS internal
	AS '$libdir/unaccent', 'unaccent_init'
	LANGUAGE C PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE vacparted_i1 PARTITION OF vacparted_i
  FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[CREATE_TABLE] -- Bug #17245: Make sure that we don't totally fail to VACUUM individual indexes that
-- happen to be below min_parallel_index_scan_size during parallel VACUUM:
CREATE TABLE parallel_vacuum_table (a int) WITH (autovacuum_enabled = off)
;
----------
[ADD_INDEX] CREATE INDEX "sk_a_idx" ON "public"."sk" USING "btree" ("a")
;
----------
[ALTER_CATALOG] ALTER DATABASE conflict_db SET TABLESPACE test_tablespace
;
----------
[COMMENT_CONSTRAINT] -- Test comments
COMMENT ON CONSTRAINT constrname_wrong ON FKTABLE IS 'fk constraint comment'
;
----------
[CREATE_TABLE] --
-- test MERGE
--
CREATE TABLE rule_merge1 (a int, b text)
;
----------
[CREATE_TABLE] CREATE TABLE tststats.priv_test_tbl (
    a int,
    b int
)
;
----------
[ADD_INDEX] create index ��Ƒ�㑻�������index2 on ��Ƒ�㑻������� using btree(��֑�����)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp16a(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "iface_name" ON "public"."iface" USING "btree" ("slotname")
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW s1.t1_mv AS SELECT * FROM s1.t1
;
----------
[DROP_TABLE] drop table t1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_rewrite
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=16008 host=/tmp/B3ysgEyCZr dbname=postgres' PUBLICATION pub_dropped_cols
;
----------
[DROP_TABLE] -- make sure we decode correctly even if the toast table is gone
DROP TABLE toasttable
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_ml_p3" (
    "b" integer,
    "c" "text",
    "a" integer
)
PARTITION BY RANGE ("b")
;
----------
[CREATE_TABLE|SELECT] --
-- Compare results between plans using sorting and plans using hash
-- aggregation. Force spilling in both cases by setting work_mem low
-- and altering the statistics.
--

create table gs_data_1 as
select g%1000 as g1000, g%100 as g100, g%10 as g10, g
   from generate_series(0,1999) g
;
----------
[CREATE_PROG_OBJ] create or replace function returnsrecord(int) returns record language plpgsql as
$$ begin return row($1,$1+1)::has_hole; end $$
;
----------
[CREATE_PROG_OBJ] -- test jsonb -> python dict
CREATE FUNCTION test1(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {'a': 1, 'c': 'NULL'})
return len(val)
$$
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table atacc1 drop d
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30890 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] create function anyarray_anymultirange_func(a anyarray, r anymultirange)
  returns anyelement as 'select $1[1] + lower($2);' language sql
;
----------
[CREATE_TABLE] -- error, cannot use PARALLEL option without parallel degree

-- Test different combinations of parallel and full options for temporary tables
CREATE TEMPORARY TABLE tmp (a int PRIMARY KEY)
;
----------
[ADD_INDEX] CREATE INDEX "reloptions_test_idx" ON "public"."reloptions_test" USING "btree" ("s") WITH ("fillfactor"='40')
;
----------
[CREATE_TABLE] create table a1 ( id serial primary key , t text , d1 numeric , d2 float8 , c character ( 10 ) )
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER after_upd_stmt_trig AFTER UPDATE ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('after_view_upd_stmt')
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (issn, ean13),
	OPERATOR 1  = (issn, issn13)
;
----------
[CREATE_TABLE] CREATE TABLE part_m02 PARTITION OF pa_target
	FOR VALUES FROM ('2017-02-01') TO ('2017-03-01')
	PARTITION BY LIST (tid)
;
----------
[DROP_TABLE] drop table inhpar cascade
;
----------
[UNKNOWN] -- fail
SECURITY LABEL ON ROLE regress_dummy_seclabel_user1 IS 'secret'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_index_check(index regclass,
    heapallindexed boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_CATALOG] CREATE DATABASE "testdb1" WITH TEMPLATE = template0 OID = 16387 STRATEGY = FILE_COPY ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[CREATE_TABLE] CREATE TABLE tt9(c integer)
;
----------
[CREATE_TABLE] create table t9()
;
----------
[CREATE_TABLE] CREATE TABLE regr_test (x float8, y float8)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE FKTABLE (ftest1 inet)
;
----------
[CREATE_TYPE] create type bogus_float8range as range (subtype=float8, subtype_diff=float4mi)
;
----------
[RENAME_TABLE] ALTER TABLE attmp RENAME TO attmp_new2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE split_alter_table.base_table ALTER COLUMN payload SET STATISTICS -1
;
