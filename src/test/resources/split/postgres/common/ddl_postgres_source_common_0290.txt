CREATE FUNCTION gbt_date_same(gbtreekey8, gbtreekey8, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE b1 ENABLE ROW LEVEL SECURITY
;
VACUUM (PARALLEL 0) pg_catalog.pg_shdepend
;
CREATE TABLE pk_noparted (
	a			int		PRIMARY KEY
  )
;
alter table parted_validate_test add constraint parted_validate_test_chka check (a > 0) not valid
;
ALTER TABLE parted_fk_naming ATTACH PARTITION parted_fk_naming_1 FOR VALUES IN ('1')
;
CREATE TABLE "public"."num_data" (
    "id" integer,
    "val" numeric(210,10)
)
;
CREATE TYPE "public"."casttesttype"
;
CREATE SCHEMA concur_reindex_schema
;
CREATE FUNCTION connection_parameters() RETURNS text LANGUAGE SQL AS $f$
       SELECT $$dbname='$$||current_database()||$$' port=$$||current_setting('port');
$f$
;
CREATE SEQUENCE s2.seq2
;
CREATE FUNCTION int4_asc_cmp (a int4, b int4) RETURNS int LANGUAGE sql AS $$
		SELECT CASE WHEN $1 = $2 THEN 0 WHEN $1 > $2 THEN 1 ELSE -1 END; $$
;
create trigger trig_null before insert on rem2
	for each row execute procedure trig_null()
;
ANALYZE pg_temp_32.pg_temp_28237_2
;
ALTER TABLE parent ALTER COLUMN a DROP NOT NULL
;
-- test setting columns to null and not null and vice versa
-- test checking for null values and primary key
create table atacc1 (test int not null)
;
CREATE FUNCTION ltree_send(ltree)
RETURNS bytea
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FUNCTION "public"."multirangetypes_sql"("q" "anymultirange", "b" "anyarray", OUT "c" "anyelement") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$ select upper($1) + $2[1] $_$
;
ALTER TYPE public.address_record SET SCHEMA split_domain_type_next
;
CREATE VIEW stats_import.testview AS SELECT * FROM stats_import.test
;
CREATE FUNCTION "public"."first_el_transfn"("anyarray", "anyelement") RETURNS "anyarray"
    LANGUAGE "sql" IMMUTABLE
    AS $_$select $1 || $2$_$
;
VACUUM (FREEZE) pg_catalog.pg_publication
;
ALTER TABLE testxmlschema.test2 DROP COLUMN aaa
;
ALTER OPERATOR FAMILY gist_cash_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
create type testtype2 as (a smallint, b bool)
;
alter table trg_clone add constraint uniq unique (a) deferrable
;
DROP TABLE quuux
;
CREATE TABLE fk_partitioned_fk_2 (b int, fdrop1 int, fdrop2 int, a int)
;
CREATE TRIGGER "tg_hslot_bd" BEFORE DELETE ON "public"."hslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_hslot_bd"()
;
-- Column number mapping: dropped columns in the parent table
create table idxpart(drop_1 int, drop_2 int, col_keep int, drop_3 int) partition by range (col_keep)
;
create table idxpart (col1 int, a int, col3 int, b int) partition by range (a)
;
/*
 * text search configuration for dutch language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * dutch and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
ALTER TABLE pg_toast.pg_toast_28261 RENAME TO reind_con_toast
;
ALTER OPERATOR <= (upc, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE PUBLICATION pub1 FOR ALL TABLES WITH (publish_generated_columns = stored)
;
CREATE VIEW tststats.priv_test_view WITH (security_barrier=true)
    AS SELECT * FROM tststats.priv_test_tbl WHERE false
;
ALTER TABLE test_tab DROP COLUMN c
;
CREATE TABLE constraint_comments_tbl (a int CONSTRAINT the_constraint CHECK (a > 0))
;
ANALYZE public.gtest30
;
TRUNCATE fk_partitioned_fk_full
;
-- Try NOT VALID and then VALIDATE CONSTRAINT, but fails. Delete failure then re-validate
ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full NOT VALID
;
DROP VIEW vw_getrngfunc
;
DROP FOREIGN TABLE ft_part2
;
CREATE TABLE y1 (a int, b text)
;
CREATE UNIQUE INDEX "hash_f8_index_3" ON "public"."hash_f8_heap" USING "btree" ("random") WHERE ("seqno" > 1000)
;
CREATE AGGREGATE myaggn01a(*) (SFUNC = stfnp, STYPE = int4[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
create table child (b varchar(255)) inherits (parent)
;
ALTER TABLE dropped_cols REPLICA IDENTITY FULL
;
CREATE FOREIGN TABLE ft2 (
	c1 int NOT NULL,
	c2 int NOT NULL,
	cx int,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10) default 'ft2',
	c8 user_enum
) SERVER loopback
;
create table hpart0 partition of hash_parted for values with (modulus 4, remainder 0)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_date_same(gbtreekey8, gbtreekey8, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE] ALTER TABLE b1 ENABLE ROW LEVEL SECURITY
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_shdepend
;
----------
[CREATE_TABLE] CREATE TABLE pk_noparted (
	a			int		PRIMARY KEY
  )
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table parted_validate_test add constraint parted_validate_test_chka check (a > 0) not valid
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE parted_fk_naming ATTACH PARTITION parted_fk_naming_1 FOR VALUES IN ('1')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_data" (
    "id" integer,
    "val" numeric(210,10)
)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."casttesttype"
;
----------
[CREATE_SCHEMA] CREATE SCHEMA concur_reindex_schema
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION connection_parameters() RETURNS text LANGUAGE SQL AS $f$
       SELECT $$dbname='$$||current_database()||$$' port=$$||current_setting('port');
$f$
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE s2.seq2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int4_asc_cmp (a int4, b int4) RETURNS int LANGUAGE sql AS $$
		SELECT CASE WHEN $1 = $2 THEN 0 WHEN $1 > $2 THEN 1 ELSE -1 END; $$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_null before insert on rem2
	for each row execute procedure trig_null()
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28237_2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE parent ALTER COLUMN a DROP NOT NULL
;
----------
[CREATE_TABLE] -- test setting columns to null and not null and vice versa
-- test checking for null values and primary key
create table atacc1 (test int not null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_send(ltree)
RETURNS bytea
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."multirangetypes_sql"("q" "anymultirange", "b" "anyarray", OUT "c" "anyelement") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$ select upper($1) + $2[1] $_$
;
----------
[ALTER_TYPE] ALTER TYPE public.address_record SET SCHEMA split_domain_type_next
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW stats_import.testview AS SELECT * FROM stats_import.test
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."first_el_transfn"("anyarray", "anyelement") RETURNS "anyarray"
    LANGUAGE "sql" IMMUTABLE
    AS $_$select $1 || $2$_$
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_publication
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE testxmlschema.test2 DROP COLUMN aaa
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_cash_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TYPE] create type testtype2 as (a smallint, b bool)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table trg_clone add constraint uniq unique (a) deferrable
;
----------
[DROP_TABLE] DROP TABLE quuux
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_2 (b int, fdrop1 int, fdrop2 int, a int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_hslot_bd" BEFORE DELETE ON "public"."hslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_hslot_bd"()
;
----------
[CREATE_TABLE] -- Column number mapping: dropped columns in the parent table
create table idxpart(drop_1 int, drop_2 int, col_keep int, drop_3 int) partition by range (col_keep)
;
----------
[CREATE_TABLE] create table idxpart (col1 int, a int, col3 int, b int) partition by range (a)
;
----------
[CREATE_POLICY] /*
 * text search configuration for dutch language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * dutch and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28261 RENAME TO reind_con_toast
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (upc, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub1 FOR ALL TABLES WITH (publish_generated_columns = stored)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW tststats.priv_test_view WITH (security_barrier=true)
    AS SELECT * FROM tststats.priv_test_tbl WHERE false
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE test_tab DROP COLUMN c
;
----------
[CREATE_TABLE] CREATE TABLE constraint_comments_tbl (a int CONSTRAINT the_constraint CHECK (a > 0))
;
----------
[ADMIN_TABLE] ANALYZE public.gtest30
;
----------
[TRUNCATE_TABLE] TRUNCATE fk_partitioned_fk_full
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Try NOT VALID and then VALIDATE CONSTRAINT, but fails. Delete failure then re-validate
ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full NOT VALID
;
----------
[DROP_VIEW] DROP VIEW vw_getrngfunc
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ft_part2
;
----------
[CREATE_TABLE] CREATE TABLE y1 (a int, b text)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "hash_f8_index_3" ON "public"."hash_f8_heap" USING "btree" ("random") WHERE ("seqno" > 1000)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn01a(*) (SFUNC = stfnp, STYPE = int4[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_TABLE] create table child (b varchar(255)) inherits (parent)
;
----------
[ALTER_TABLE] ALTER TABLE dropped_cols REPLICA IDENTITY FULL
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft2 (
	c1 int NOT NULL,
	c2 int NOT NULL,
	cx int,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10) default 'ft2',
	c8 user_enum
) SERVER loopback
;
----------
[CREATE_TABLE] create table hpart0 partition of hash_parted for values with (modulus 4, remainder 0)
;
