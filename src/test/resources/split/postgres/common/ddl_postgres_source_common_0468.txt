alter extension test_ext7 update to '2.1bad'
;
-- Likewise for the old spelling ~.

ALTER OPERATOR FAMILY gist__int_ops USING gist
DROP OPERATOR 14 (_int4, _int4)
;
TRUNCATE truncparted
;
CREATE TEXT SEARCH DICTIONARY ispell (
                        Template=ispell,
                        DictFile=ispell_sample,
                        AffFile=ispell_sample
)
;
-- Extracting elements functions

CREATE FUNCTION cube_dim(cube)
RETURNS int4
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=19617 host=/tmp/mT1iHGnnPr dbname=postgres' PUBLICATION mypub
;
DROP FUNCTION rngfunc_mat(int,int)
;
CREATE POLICY coll_p ON coll_t USING (c < ('foo'::text COLLATE "C"))
;
CREATE SCHEMA newschema2 AUTHORIZATION "current_user"
;
CREATE TRIGGER tsvectorupdate
BEFORE UPDATE OR INSERT ON test_tsvector
FOR EACH ROW EXECUTE PROCEDURE tsvector_update_trigger(a, 'pg_catalog.english', t)
;
CREATE TABLE dummy_test_tab (i int4)
;
ALTER TABLE ONLY "public"."pagg_tab2" ATTACH PARTITION "public"."pagg_tab2_p1" FOR VALUES FROM (0) TO (10)
;
CREATE TABLE "public"."cmpart2" (
    "f1" "text"
)
;
CREATE RULE gtest_rule_ins AS ON INSERT TO gtest_rule
  DO ALSO INSERT INTO gtest_rule_log VALUES ('INS', NULL, NEW.b)
;
CREATE COLLATION testcoll_lower_first (provider = icu, locale = '@colCaseFirst=lower')
;
-- ===================================================================
-- JOIN queries
-- ===================================================================
-- Analyze ft4 and ft5 so that we have better statistics. These tables do not
-- have use_remote_estimate set.
ANALYZE ft4
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_db_role_setting
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
-- security barrier view

CREATE TABLE base_tbl (person text, visibility text)
;
CREATE TABLE fk_partitioned_pk_6 (a int PRIMARY KEY) PARTITION BY list (a)
;
create table inserttest3 (f1 text default 'foo', f2 text default 'bar', f3 int)
;
CREATE TRIGGER "gtest12_03" BEFORE UPDATE ON "public"."gtest26" FOR EACH ROW EXECUTE FUNCTION "public"."gtest_trigger_func"()
;
ANALYZE public.part1_self_fk
;
create or replace function constant_refcursor() returns refcursor as $$
declare
    rc constant refcursor := 'my_cursor_name';
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
TRUNCATE ONLY trunc_f
;
ALTER TABLE copy_t ENABLE ROW LEVEL SECURITY
;
VACUUM (FREEZE) pg_catalog.pg_enum
;
CREATE VIEW xmlview9 AS SELECT xmlserialize(content 'good' as text)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
ALTER TABLE ONLY "public"."prt2_l_p3" ATTACH PARTITION "public"."prt2_l_p3_p2" FOR VALUES FROM (13) TO (25)
;
CREATE TRIGGER "after_upd_a_b_row_trig" AFTER UPDATE OF "a", "b" ON "public"."main_table" FOR EACH ROW EXECUTE FUNCTION "public"."trigger_func"('after_upd_a_b_row')
;
CREATE TRIGGER "gtest12_01" BEFORE INSERT OR UPDATE ON "generated_stored_tests"."gtest26" FOR EACH ROW EXECUTE FUNCTION "generated_stored_tests"."gtest_trigger_func"()
;
DROP TABLE stuff
;
-- index creation using abbreviated keys, hitting abort
CREATE INDEX abbrev_abort_uuids__abort_increasing_idx ON abbrev_abort_uuids (abort_increasing)
;
create or replace function skip_merge_op() returns trigger
language plpgsql as
$$
BEGIN
	RETURN NULL;
END;
$$
;
DROP FUNCTION exec(text)
;
ALTER TABLE tbl1 ALTER COLUMN val2 TYPE character varying
;
ALTER TABLE notnull_tbl1 INHERIT notnull_chld0
;
VACUUM (PROCESS_TOAST FALSE, FULL) vactst
;
/*
 * 24.9
 * FOREIGN_TABLES view
 */
CREATE VIEW foreign_tables AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_foreign_tables
;
alter table atacc1 drop bar
;
CREATE FUNCTION retset (a int) RETURNS SETOF int AS $$ SELECT 1; $$ LANGUAGE SQL IMMUTABLE
;
CREATE TABLE "public"."cmmove3" (
    "f1" "text"
)
;
CREATE TABLE viewtest_tbl (a int, b int, c numeric(10,1), d text COLLATE "C")
;
ANALYZE public.rtest_emplog
;
CREATE FUNCTION dblink_close (text, text)
RETURNS text
AS '$libdir/dblink','dblink_close'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
ALTER TABLE gtest_normal_child INHERIT gtest_normal
;
-- check that we recognize equivalence with dummy domains in the way
create temp table undername (f1 name, f2 int)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_index
;
CREATE INDEX iprt1_e_p2_ab2 on prt1_e_p2(((a+b)/2))
;
------------------------------------------------------------------------------------------
[ALTER_LIBRARY] alter extension test_ext7 update to '2.1bad'
;
----------
[ALTER_PROG_OBJ] -- Likewise for the old spelling ~.

ALTER OPERATOR FAMILY gist__int_ops USING gist
DROP OPERATOR 14 (_int4, _int4)
;
----------
[TRUNCATE_TABLE] TRUNCATE truncparted
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY ispell (
                        Template=ispell,
                        DictFile=ispell_sample,
                        AffFile=ispell_sample
)
;
----------
[CREATE_PROG_OBJ] -- Extracting elements functions

CREATE FUNCTION cube_dim(cube)
RETURNS int4
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=19617 host=/tmp/mT1iHGnnPr dbname=postgres' PUBLICATION mypub
;
----------
[DROP_PROG_OBJ] DROP FUNCTION rngfunc_mat(int,int)
;
----------
[CREATE_POLICY] CREATE POLICY coll_p ON coll_t USING (c < ('foo'::text COLLATE "C"))
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema2 AUTHORIZATION "current_user"
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER tsvectorupdate
BEFORE UPDATE OR INSERT ON test_tsvector
FOR EACH ROW EXECUTE PROCEDURE tsvector_update_trigger(a, 'pg_catalog.english', t)
;
----------
[CREATE_TABLE] CREATE TABLE dummy_test_tab (i int4)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab2" ATTACH PARTITION "public"."pagg_tab2_p1" FOR VALUES FROM (0) TO (10)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."cmpart2" (
    "f1" "text"
)
;
----------
[CREATE_POLICY] CREATE RULE gtest_rule_ins AS ON INSERT TO gtest_rule
  DO ALSO INSERT INTO gtest_rule_log VALUES ('INS', NULL, NEW.b)
;
----------
[CREATE_POLICY] CREATE COLLATION testcoll_lower_first (provider = icu, locale = '@colCaseFirst=lower')
;
----------
[ADMIN_TABLE] -- ===================================================================
-- JOIN queries
-- ===================================================================
-- Analyze ft4 and ft5 so that we have better statistics. These tables do not
-- have use_remote_estimate set.
ANALYZE ft4
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_db_role_setting
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
----------
[CREATE_TABLE] -- security barrier view

CREATE TABLE base_tbl (person text, visibility text)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_pk_6 (a int PRIMARY KEY) PARTITION BY list (a)
;
----------
[CREATE_TABLE] create table inserttest3 (f1 text default 'foo', f2 text default 'bar', f3 int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "gtest12_03" BEFORE UPDATE ON "public"."gtest26" FOR EACH ROW EXECUTE FUNCTION "public"."gtest_trigger_func"()
;
----------
[ADMIN_TABLE] ANALYZE public.part1_self_fk
;
----------
[CREATE_PROG_OBJ] create or replace function constant_refcursor() returns refcursor as $$
declare
    rc constant refcursor := 'my_cursor_name';
begin
    open rc for select a from rc_test;
    return rc;
end
$$ language plpgsql
;
----------
[TRUNCATE_TABLE] TRUNCATE ONLY trunc_f
;
----------
[ALTER_TABLE] ALTER TABLE copy_t ENABLE ROW LEVEL SECURITY
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_enum
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmlview9 AS SELECT xmlserialize(content 'good' as text)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_l_p3" ATTACH PARTITION "public"."prt2_l_p3_p2" FOR VALUES FROM (13) TO (25)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "after_upd_a_b_row_trig" AFTER UPDATE OF "a", "b" ON "public"."main_table" FOR EACH ROW EXECUTE FUNCTION "public"."trigger_func"('after_upd_a_b_row')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "gtest12_01" BEFORE INSERT OR UPDATE ON "generated_stored_tests"."gtest26" FOR EACH ROW EXECUTE FUNCTION "generated_stored_tests"."gtest_trigger_func"()
;
----------
[DROP_TABLE] DROP TABLE stuff
;
----------
[ADD_INDEX] -- index creation using abbreviated keys, hitting abort
CREATE INDEX abbrev_abort_uuids__abort_increasing_idx ON abbrev_abort_uuids (abort_increasing)
;
----------
[CREATE_PROG_OBJ] create or replace function skip_merge_op() returns trigger
language plpgsql as
$$
BEGIN
	RETURN NULL;
END;
$$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION exec(text)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl1 ALTER COLUMN val2 TYPE character varying
;
----------
[ALTER_TABLE] ALTER TABLE notnull_tbl1 INHERIT notnull_chld0
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, FULL) vactst
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.9
 * FOREIGN_TABLES view
 */
CREATE VIEW foreign_tables AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_foreign_tables
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table atacc1 drop bar
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION retset (a int) RETURNS SETOF int AS $$ SELECT 1; $$ LANGUAGE SQL IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."cmmove3" (
    "f1" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE viewtest_tbl (a int, b int, c numeric(10,1), d text COLLATE "C")
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_emplog
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_close (text, text)
RETURNS text
AS '$libdir/dblink','dblink_close'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ALTER_TABLE] ALTER TABLE gtest_normal_child INHERIT gtest_normal
;
----------
[CREATE_TABLE] -- check that we recognize equivalence with dummy domains in the way
create temp table undername (f1 name, f2 int)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_index
;
----------
[ADD_INDEX] CREATE INDEX iprt1_e_p2_ab2 on prt1_e_p2(((a+b)/2))
;
