-- Ensure that an Append node properly handles a sub-partitioned table
-- matching without any of its leaf partitions matching the clause.
create table listp (a int, b int) partition by list (a)
;
CREATE SUBSCRIPTION "pg_createsubscriber_16385_d63d8bd" CONNECTION 'host=''/tmp/kt5L5daWiA'' port=22041 dbname=''regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ''' PUBLICATION "Pub2" WITH (create_slot = false, enabled = false, slot_name = 'replslot2', copy_data = false)
;
VACUUM (SKIP_LOCKED) information_schema.sql_parts
;
ALTER TABLE ONLY "public"."tbl_include_unique1"
    ADD CONSTRAINT "tbl_include_unique1_idx_unique" UNIQUE ("c1", "c2") INCLUDE ("c3", "c4")
;
CREATE INDEX collate_test1_idx6 ON collate_test1 ((a COLLATE "POSIX"))
;
CREATE RULE "rtest_emp_del" AS
    ON DELETE TO "public"."rtest_emp" DO  INSERT INTO "public"."rtest_emplog" ("ename", "who", "action", "newsal", "oldsal")
  VALUES ("old"."ename", CURRENT_USER, 'fired'::"bpchar", 0.00, "old"."salary")
;
/*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
ALTER TABLE ruletest1 DISABLE RULE rule1
;
create table rtest_t5 (a int4, b text)
;
ALTER TEXT SEARCH CONFIGURATION synonym_tst ALTER MAPPING FOR
	asciiword, hword_asciipart, asciihword
	WITH synonym, english_stem
;
DROP EVENT TRIGGER IF EXISTS split_event_helper_ddl_end
;
CREATE FUNCTION isnle(isbn13, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE MATERIALIZED VIEW mvtest_mv_v_4 (ii) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
CREATE OR REPLACE FUNCTION undroppable() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
	obj record;
BEGIN
	PERFORM 1 FROM pg_tables WHERE tablename = 'undroppable_objs';
	IF NOT FOUND THEN
		RAISE NOTICE 'table undroppable_objs not found, skipping';
		RETURN;
	END IF;
	FOR obj IN
		SELECT * FROM pg_event_trigger_dropped_objects() JOIN
			undroppable_objs USING (object_type, object_identity)
	LOOP
		RAISE EXCEPTION 'object % of type % cannot be dropped',
			obj.object_identity, obj.object_type;
	END LOOP;
END;
$$
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "word" WITH "public"."hunspell_num", "english_stem"
;
-- ok
TRUNCATE TABLE flavians
;
create rule rtest_nothn_r1 as on insert to rtest_nothn1
	where new.a >= 10 and new.a < 20 do instead nothing
;
create table mlparted5 (c text, a int not null, b int not null) partition by list (c)
;
CREATE TABLE trunc_ins (id serial PRIMARY KEY)
;
drop table gtest32
;
-- Ensure we can build statistics for tables with inheritance.
CREATE TABLE ab1 (a INTEGER, b INTEGER)
;
ALTER TABLE s5.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
alter table idxpart drop column drop_1
;
ALTER TABLE "public"."ec1" ENABLE ROW LEVEL SECURITY
;
DO LANGUAGE plpgsql
$$
DECLARE
  my_schema pg_catalog.text := pg_catalog.quote_ident(pg_catalog.current_schema());
  old_path pg_catalog.text := pg_catalog.current_setting('search_path');
BEGIN
-- for safety, transiently set search_path to just pg_catalog+pg_temp
PERFORM pg_catalog.set_config('search_path', 'pg_catalog, pg_temp', true);

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.gseg_compress(internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.gseg_decompress(internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

PERFORM pg_catalog.set_config('search_path', old_path, true);
END
$$
;
CREATE TABLE testschema.foo (i int) TABLESPACE regress_tblspace
;
ALTER RULE NewInsertRule ON rule_v1 RENAME TO "_RETURN"
;
DO $$
BEGIN
    FOR r IN 1..1350 LOOP
        DELETE FROM dedup_unique_test_table;
        INSERT INTO dedup_unique_test_table SELECT 1;
    END LOOP;
END$$
;
ANALYZE public.rtest_view1
;
-- cleanup
DROP TABLE gin_check
;
ALTER TABLE b ADD CONSTRAINT bfk FOREIGN KEY (a_id) REFERENCES a (i) NOT VALID
;
CREATE TABLE part_a_10_a_20 PARTITION OF range_parted FOR VALUES FROM ('a', 10) TO ('a', 20)
;
ALTER TABLE "fkpart3"."fk"
    ADD CONSTRAINT "fk_a_fkey" FOREIGN KEY ("a") REFERENCES "fkpart3"."pk"("a")
;
CREATE FUNCTION castfunc(int) RETURNS priv_testdomain3a AS $$ SELECT $1::priv_testdomain3a $$ LANGUAGE SQL
;
ANALYZE pg_temp_9.pg_temp_26805
;
CREATE POLICY p1 ON rls_test_restrictive USING (data % 2 = 0)
;
TRUNCATE old_system_table
;
ANALYZE pagg_tab2
;
TRUNCATE tab5
;
ALTER TABLE pg_toast.pg_toast_30785 RENAME TO reind_con_toast
;
CREATE FUNCTION isnge(issn, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE mvtest_v (i int, j int)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "sfloat" WITH "simple"
;
create table cth(id serial, rowid text, rowdt timestamp, attribute text, val text)
;
/*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) information_schema.sql_features
;
ALTER FOREIGN TABLE tru_ftable OPTIONS (ADD truncatable 'true')
;
TRUNCATE TABLE truncate_a,trunc_e
;
ALTER TABLE part_2 ALTER COLUMN b TYPE text
;
ALTER TABLE public.attbl ADD CONSTRAINT pk_attbl PRIMARY KEY (p1)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- Ensure that an Append node properly handles a sub-partitioned table
-- matching without any of its leaf partitions matching the clause.
create table listp (a int, b int) partition by list (a)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION "pg_createsubscriber_16385_d63d8bd" CONNECTION 'host=''/tmp/kt5L5daWiA'' port=22041 dbname=''regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ''' PUBLICATION "Pub2" WITH (create_slot = false, enabled = false, slot_name = 'replslot2', copy_data = false)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) information_schema.sql_parts
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."tbl_include_unique1"
    ADD CONSTRAINT "tbl_include_unique1_idx_unique" UNIQUE ("c1", "c2") INCLUDE ("c3", "c4")
;
----------
[ADD_INDEX] CREATE INDEX collate_test1_idx6 ON collate_test1 ((a COLLATE "POSIX"))
;
----------
[CREATE_POLICY] CREATE RULE "rtest_emp_del" AS
    ON DELETE TO "public"."rtest_emp" DO  INSERT INTO "public"."rtest_emplog" ("ename", "who", "action", "newsal", "oldsal")
  VALUES ("old"."ename", CURRENT_USER, 'fired'::"bpchar", 0.00, "old"."salary")
;
----------
[CREATE_POLICY] /*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
----------
[ALTER_TABLE] ALTER TABLE ruletest1 DISABLE RULE rule1
;
----------
[CREATE_TABLE] create table rtest_t5 (a int4, b text)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION synonym_tst ALTER MAPPING FOR
	asciiword, hword_asciipart, asciihword
	WITH synonym, english_stem
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER IF EXISTS split_event_helper_ddl_end
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(isbn13, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv_v_4 (ii) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION undroppable() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
	obj record;
BEGIN
	PERFORM 1 FROM pg_tables WHERE tablename = 'undroppable_objs';
	IF NOT FOUND THEN
		RAISE NOTICE 'table undroppable_objs not found, skipping';
		RETURN;
	END IF;
	FOR obj IN
		SELECT * FROM pg_event_trigger_dropped_objects() JOIN
			undroppable_objs USING (object_type, object_identity)
	LOOP
		RAISE EXCEPTION 'object % of type % cannot be dropped',
			obj.object_identity, obj.object_type;
	END LOOP;
END;
$$
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "word" WITH "public"."hunspell_num", "english_stem"
;
----------
[TRUNCATE_TABLE] -- ok
TRUNCATE TABLE flavians
;
----------
[CREATE_POLICY] create rule rtest_nothn_r1 as on insert to rtest_nothn1
	where new.a >= 10 and new.a < 20 do instead nothing
;
----------
[CREATE_TABLE] create table mlparted5 (c text, a int not null, b int not null) partition by list (c)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_ins (id serial PRIMARY KEY)
;
----------
[DROP_TABLE] drop table gtest32
;
----------
[CREATE_TABLE] -- Ensure we can build statistics for tables with inheritance.
CREATE TABLE ab1 (a INTEGER, b INTEGER)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE s5.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart drop column drop_1
;
----------
[ALTER_TABLE] ALTER TABLE "public"."ec1" ENABLE ROW LEVEL SECURITY
;
----------
[BLOCK] DO LANGUAGE plpgsql
$$
DECLARE
  my_schema pg_catalog.text := pg_catalog.quote_ident(pg_catalog.current_schema());
  old_path pg_catalog.text := pg_catalog.current_setting('search_path');
BEGIN
-- for safety, transiently set search_path to just pg_catalog+pg_temp
PERFORM pg_catalog.set_config('search_path', 'pg_catalog, pg_temp', true);

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.gseg_compress(internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.gseg_decompress(internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

PERFORM pg_catalog.set_config('search_path', old_path, true);
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE testschema.foo (i int) TABLESPACE regress_tblspace
;
----------
[ALTER_POLICY] ALTER RULE NewInsertRule ON rule_v1 RENAME TO "_RETURN"
;
----------
[BLOCK] DO $$
BEGIN
    FOR r IN 1..1350 LOOP
        DELETE FROM dedup_unique_test_table;
        INSERT INTO dedup_unique_test_table SELECT 1;
    END LOOP;
END$$
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_view1
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE gin_check
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE b ADD CONSTRAINT bfk FOREIGN KEY (a_id) REFERENCES a (i) NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE part_a_10_a_20 PARTITION OF range_parted FOR VALUES FROM ('a', 10) TO ('a', 20)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "fkpart3"."fk"
    ADD CONSTRAINT "fk_a_fkey" FOREIGN KEY ("a") REFERENCES "fkpart3"."pk"("a")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION castfunc(int) RETURNS priv_testdomain3a AS $$ SELECT $1::priv_testdomain3a $$ LANGUAGE SQL
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26805
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON rls_test_restrictive USING (data % 2 = 0)
;
----------
[TRUNCATE_TABLE] TRUNCATE old_system_table
;
----------
[ADMIN_TABLE] ANALYZE pagg_tab2
;
----------
[TRUNCATE_TABLE] TRUNCATE tab5
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30785 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(issn, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE mvtest_v (i int, j int)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "sfloat" WITH "simple"
;
----------
[CREATE_TABLE] create table cth(id serial, rowid text, rowdt timestamp, attribute text, val text)
;
----------
[CREATE_POLICY] /*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) information_schema.sql_features
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE tru_ftable OPTIONS (ADD truncatable 'true')
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE truncate_a,trunc_e
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE part_2 ALTER COLUMN b TYPE text
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.attbl ADD CONSTRAINT pk_attbl PRIMARY KEY (p1)
;
