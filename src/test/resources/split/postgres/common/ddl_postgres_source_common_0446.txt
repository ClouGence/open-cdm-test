create trigger before_stmt_trigger
	before update on stmt_trig_on_empty_upd
	execute procedure update_stmt_notice()
;
alter table atacc1 add constraint atacc1_constr_a_valid check(test_a is not null)
;
vacuum freeze htab2
;
CREATE TEMP TABLE pg_temp_11.pg_temp_41153_2 (tid pg_catalog.tid)
;
CREATE TRIGGER "tg_slotlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."phone" FOR EACH ROW EXECUTE FUNCTION "public"."tg_slotlink_a"('PH')
;
DROP TABLE gtest_rule_log
;
CREATE POLICY "p4" ON "regress_rls_schema"."rls_tbl" FOR DELETE USING (("c1" <= 3))
;
CREATE TABLE tbl_created_in_xact(id SERIAL PRIMARY KEY)
;
CREATE FUNCTION "public"."tg_slotlink_a"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    dummy	integer;
begin
    if tg_op = 'INSERT' then
        if new.slotlink != '' then
	    dummy := tg_slotlink_set(new.slotlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = 'UPDATE' then
        if new.slotlink != old.slotlink then
	    if old.slotlink != '' then
	        dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	    end if;
	    if new.slotlink != '' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.slotlink != '' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = 'DELETE' then
        if old.slotlink != '' then
	    dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	end if;
	return old;
    end if;
end;
$$
;
CREATE SCHEMA test_oat_schema
;
create type intmultirange as (x int, y int)
;
CREATE TABLE ins_copy (id serial PRIMARY KEY, id2 int)
;
create trigger mlparted11_trig before insert ON mlparted11
  for each row execute procedure mlparted11_trig_fn()
;
create table sometable (id int, a text, b text)
;
-- succeeds

-- An already validated constraint must not be revalidated
CREATE FUNCTION boo(int) RETURNS int IMMUTABLE STRICT LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'boo: %', $1; RETURN $1; END; $$
;
create foreign table foo2child (f3 int) inherits (foo2)
  server loopback options (table_name 'loct4')
;
CREATE TABLE test_foreign_constraints_inh () INHERITS (test_foreign_constraints)
;
ALTER TABLE test_add_column
	ADD COLUMN c2 integer, 
	ADD COLUMN c3 integer
;
-- Test pushing down UPDATE/DELETE joins to the remote server
create table parent (a int, b text)
;
CREATE OR REPLACE FUNCTION
  pg_stat_reset_slru(target text DEFAULT NULL)
RETURNS void
LANGUAGE INTERNAL
CALLED ON NULL INPUT VOLATILE PARALLEL SAFE
AS 'pg_stat_reset_slru'
;
DROP INDEX CONCURRENTLY "concur_index2"
;
CREATE FUNCTION isngt(isbn, isbn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE sch1.tab2 AS SELECT generate_series(1,10) AS a
;
CREATE FUNCTION buffer_create_toy(rel regclass, blockno int4)
RETURNS pg_catalog.int4 STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = 100.0)
;
CREATE VIEW ptif_test_view AS SELECT 1
;
ALTER TABLE temp_view_test.tmp1 SET SCHEMA testviewschm2
;
CREATE PUBLICATION "pub1" FOR ALL TABLES
;
-- check for partition bound overlap and other invalid specifications

CREATE TABLE list_parted2 (
	a varchar
) PARTITION BY LIST (a)
;
CREATE TABLE ctlt12 (LIKE ctlty1)
;
ALTER TABLE pg_toast.pg_toast_27667 RENAME TO reind_con_toast
;
-- not STRICT; allows (keys,null)

CREATE FUNCTION hstore(text[])
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_array'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
alter table stud_emp rename to aggtest
;
-- bytea check

CREATE TABLE byteatmp (a bytea)
;
DROP TABLE pg_temp_13.pg_temp_26101_2, pg_temp_13.pg_temp_26101
;
CREATE TABLE "fkpart6"."pk11" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
ALTER PUBLICATION testpub_forschema ADD TABLE pub_test.testpub_nopk
;
CREATE POLICY p1 ON y1 FOR ALL USING (a % 2 = 0)
;
CREATE TABLE testschema.part_78 PARTITION OF testschema.part FOR VALUES IN (7, 8)
  PARTITION BY LIST (a)
;
create table part_gg2_2 partition of part_gg2 for values from (5) to (10)
;
CREATE VIEW routine_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class t, pg_namespace nt, pg_attribute a

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = t.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND t.oid = a.attrelid
          AND d.refobjsubid = a.attnum
          AND pg_has_role(t.relowner, 'USAGE')
;
alter index idxpart_expr_idx attach partition idxpart2_expr_idx
;
drop function funcA()
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub3
;
create function count_tt1_s() returns int8 as
'select count(*) from tt1' language sql stable
;
ALTER TABLE attmp ALTER COLUMN i RESET (n_distinct_inherited)
;
COMMENT ON SUBSCRIPTION sub1
					   IS 'comment on subscription'
;
CREATE TABLE tab1_2 PARTITION OF tab1 (c DEFAULT 'sub1_tab1') FOR VALUES IN (4, 5, 6) PARTITION BY LIST (a)
;
CREATE OPERATOR <= (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_le,
        COMMUTATOR = '>=',
	NEGATOR = '>',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ANALYZE public.idxpart1
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger before_stmt_trigger
	before update on stmt_trig_on_empty_upd
	execute procedure update_stmt_notice()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc1_constr_a_valid check(test_a is not null)
;
----------
[ADMIN_TABLE] vacuum freeze htab2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_41153_2 (tid pg_catalog.tid)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_slotlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."phone" FOR EACH ROW EXECUTE FUNCTION "public"."tg_slotlink_a"('PH')
;
----------
[DROP_TABLE] DROP TABLE gtest_rule_log
;
----------
[CREATE_POLICY] CREATE POLICY "p4" ON "regress_rls_schema"."rls_tbl" FOR DELETE USING (("c1" <= 3))
;
----------
[CREATE_TABLE] CREATE TABLE tbl_created_in_xact(id SERIAL PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_slotlink_a"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    dummy	integer;
begin
    if tg_op = 'INSERT' then
        if new.slotlink != '' then
	    dummy := tg_slotlink_set(new.slotlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = 'UPDATE' then
        if new.slotlink != old.slotlink then
	    if old.slotlink != '' then
	        dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	    end if;
	    if new.slotlink != '' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.slotlink != '' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = 'DELETE' then
        if old.slotlink != '' then
	    dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	end if;
	return old;
    end if;
end;
$$
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_oat_schema
;
----------
[CREATE_TYPE] create type intmultirange as (x int, y int)
;
----------
[CREATE_TABLE] CREATE TABLE ins_copy (id serial PRIMARY KEY, id2 int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger mlparted11_trig before insert ON mlparted11
  for each row execute procedure mlparted11_trig_fn()
;
----------
[CREATE_TABLE] create table sometable (id int, a text, b text)
;
----------
[CREATE_PROG_OBJ] -- succeeds

-- An already validated constraint must not be revalidated
CREATE FUNCTION boo(int) RETURNS int IMMUTABLE STRICT LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'boo: %', $1; RETURN $1; END; $$
;
----------
[CREATE_TABLE] create foreign table foo2child (f3 int) inherits (foo2)
  server loopback options (table_name 'loct4')
;
----------
[CREATE_TABLE] CREATE TABLE test_foreign_constraints_inh () INHERITS (test_foreign_constraints)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN c2 integer, 
	ADD COLUMN c3 integer
;
----------
[CREATE_TABLE] -- Test pushing down UPDATE/DELETE joins to the remote server
create table parent (a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  pg_stat_reset_slru(target text DEFAULT NULL)
RETURNS void
LANGUAGE INTERNAL
CALLED ON NULL INPUT VOLATILE PARALLEL SAFE
AS 'pg_stat_reset_slru'
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY "concur_index2"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(isbn, isbn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE sch1.tab2 AS SELECT generate_series(1,10) AS a
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION buffer_create_toy(rel regclass, blockno int4)
RETURNS pg_catalog.int4 STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ADD_INDEX] create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = 100.0)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ptif_test_view AS SELECT 1
;
----------
[ALTER_TABLE] ALTER TABLE temp_view_test.tmp1 SET SCHEMA testviewschm2
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "pub1" FOR ALL TABLES
;
----------
[CREATE_TABLE] -- check for partition bound overlap and other invalid specifications

CREATE TABLE list_parted2 (
	a varchar
) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE ctlt12 (LIKE ctlty1)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27667 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] -- not STRICT; allows (keys,null)

CREATE FUNCTION hstore(text[])
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_array'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[RENAME_TABLE] alter table stud_emp rename to aggtest
;
----------
[CREATE_TABLE] -- bytea check

CREATE TABLE byteatmp (a bytea)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26101_2, pg_temp_13.pg_temp_26101
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart6"."pk11" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_forschema ADD TABLE pub_test.testpub_nopk
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON y1 FOR ALL USING (a % 2 = 0)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part_78 PARTITION OF testschema.part FOR VALUES IN (7, 8)
  PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] create table part_gg2_2 partition of part_gg2 for values from (5) to (10)
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW routine_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class t, pg_namespace nt, pg_attribute a

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = t.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND t.oid = a.attrelid
          AND d.refobjsubid = a.attnum
          AND pg_has_role(t.relowner, 'USAGE')
;
----------
[ALTER_INDEX] alter index idxpart_expr_idx attach partition idxpart2_expr_idx
;
----------
[DROP_PROG_OBJ] drop function funcA()
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub3
;
----------
[CREATE_PROG_OBJ] create function count_tt1_s() returns int8 as
'select count(*) from tt1' language sql stable
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE attmp ALTER COLUMN i RESET (n_distinct_inherited)
;
----------
[ALTER_PUB_SUB] COMMENT ON SUBSCRIPTION sub1
					   IS 'comment on subscription'
;
----------
[CREATE_TABLE] CREATE TABLE tab1_2 PARTITION OF tab1 (c DEFAULT 'sub1_tab1') FOR VALUES IN (4, 5, 6) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_le,
        COMMUTATOR = '>=',
	NEGATOR = '>',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] ANALYZE public.idxpart1
;
