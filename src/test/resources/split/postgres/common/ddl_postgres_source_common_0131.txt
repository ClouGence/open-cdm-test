CREATE FUNCTION handle_get_release()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE SCHEMA schema_two authorization regress_evt_user
;
ANALYZE public.radix_text_tbl
;
CREATE UNIQUE INDEX t2_btree_unique ON s2.t2 USING BTREE (i)
;
CREATE FUNCTION _int_overlap_joinsel(internal, oid, internal, smallint, internal)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
-- basic test for statistics on expressions
CREATE TABLE ab1 (a INTEGER, b INTEGER, c TIMESTAMP, d TIMESTAMPTZ)
;
TRUNCATE trunc_stats_test2
;
/*
 * text search configuration for greek language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * greek and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY greek_stem
	(TEMPLATE = snowball, Language = greek )
;
ALTER TABLE ONLY "public"."transition_table_level2" ALTER COLUMN "level2_no" SET DEFAULT "nextval"('"public"."transition_table_level2_level2_no_seq"'::"regclass")
;
ALTER TABLE s3.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
ALTER OPERATOR <= (seg, seg) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel
)
;
CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
DROP TABLE pg_temp_24.pg_temp_26555_2, pg_temp_24.pg_temp_26555
;
CREATE COLLATION IF NOT EXISTS split_collation_probe.c_from_c FROM "C"
;
ANALYZE public.circle_tbl
;
--
-- First create the test table and some helper functions
--
create table gin_incomplete_splits(i int4[]) with (autovacuum_enabled = off)
;
create table other_events (event_id int primary key)
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '14648'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
CREATE OPERATOR << (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_left,
	COMMUTATOR = '>>',
	RESTRICT = positionsel,
	JOIN = positionjoinsel
)
;
alter index idxpart attach partition idxpart1
;
alter table child2 inherit parent
;
ALTER TABLE itest4 ALTER COLUMN a DROP IDENTITY IF EXISTS
;
CREATE TABLE dump_test.test_table_nn_2 (
							col1 int NOT NULL)
;
ANALYZE pg_temp_3.pg_temp_16651_2
;
create or replace rule r1 as on update to rules_base do instead
  select * from rules_base where f1 = 11 for update of rules_base
;
DROP TABLE vac_tab_auto
;
CREATE TABLE test1cs (x text COLLATE case_sensitive)
;
DROP TABLE loct1
;
CREATE TABLESPACE regress_ts3 LOCATION ''
;
CREATE VIEW v4 AS SELECT * FROM base_table WHERE id IN (SELECT id FROM base_table2)
;
create event trigger regress_event_trigger on ddl_command_start
   execute procedure test_event_trigger()
;
ANALYZE public.a_star
;
VACUUM (PARALLEL 2) pg_catalog.pg_amproc
;
-- fails
DROP TABLE fk_partitioned_fk_full
;
DROP TABLE prt2_adv_p3_2
;
ANALYZE pg_catalog.pg_constraint
;
CREATE OR REPLACE FUNCTION pg_create_logical_replication_slot(
    IN slot_name name, IN plugin name,
    IN temporary boolean DEFAULT false,
    IN twophase boolean DEFAULT false,
    OUT slot_name name, OUT lsn pg_lsn)
RETURNS RECORD
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_create_logical_replication_slot'
;
CREATE OPERATOR CLASS gist_float8_ops
DEFAULT FOR TYPE float8 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_float8_consistent (internal, float8, int2, oid, internal),
	FUNCTION	2	gbt_float8_union (internal, internal),
	FUNCTION	3	gbt_float8_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_float8_penalty (internal, internal, internal),
	FUNCTION	6	gbt_float8_picksplit (internal, internal),
	FUNCTION	7	gbt_float8_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
CREATE FUNCTION "public"."explicitly_binary_coercible"("oid", "oid") RETURNS boolean
    LANGUAGE "plpgsql" STABLE STRICT
    AS $_$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$_$
;
ALTER TABLE pg_toast.pg_toast_30919 RENAME TO reind_con_toast
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_ts_parser
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_statistic_ext_data
;
/*
 * text search configuration for catalan language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * catalan and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY catalan_stem
	(TEMPLATE = snowball, Language = catalan )
;
create table inh_pp1 (f1 int not null)
;
CREATE TABLE "public"."gtest28b" (
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2)) STORED
)
;
alter table atacc1 alter a set not null
;
CREATE TABLE tt7 (x int, q text, y numeric(8,2))
;
CREATE TABLE t_012_tbl (id int)
;
CREATE TABLE tbl_cs_fail(data int not null) WITH (AUTOVACUUM_ENABLED = false)
;
DROP TABLE cic_test
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_get_release()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_SCHEMA] CREATE SCHEMA schema_two authorization regress_evt_user
;
----------
[ADMIN_TABLE] ANALYZE public.radix_text_tbl
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t2_btree_unique ON s2.t2 USING BTREE (i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_overlap_joinsel(internal, oid, internal, smallint, internal)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- basic test for statistics on expressions
CREATE TABLE ab1 (a INTEGER, b INTEGER, c TIMESTAMP, d TIMESTAMPTZ)
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_stats_test2
;
----------
[CREATE_POLICY] /*
 * text search configuration for greek language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * greek and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY greek_stem
	(TEMPLATE = snowball, Language = greek )
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."transition_table_level2" ALTER COLUMN "level2_no" SET DEFAULT "nextval"('"public"."transition_table_level2_level2_no_seq"'::"regclass")
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE s3.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (seg, seg) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel
)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_24.pg_temp_26555_2, pg_temp_24.pg_temp_26555
;
----------
[CREATE_POLICY] CREATE COLLATION IF NOT EXISTS split_collation_probe.c_from_c FROM "C"
;
----------
[ADMIN_TABLE] ANALYZE public.circle_tbl
;
----------
[CREATE_TABLE] --
-- First create the test table and some helper functions
--
create table gin_incomplete_splits(i int4[]) with (autovacuum_enabled = off)
;
----------
[CREATE_TABLE] create table other_events (event_id int primary key)
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '14648'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR << (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_left,
	COMMUTATOR = '>>',
	RESTRICT = positionsel,
	JOIN = positionjoinsel
)
;
----------
[ALTER_INDEX] alter index idxpart attach partition idxpart1
;
----------
[ALTER_TABLE] alter table child2 inherit parent
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest4 ALTER COLUMN a DROP IDENTITY IF EXISTS
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_nn_2 (
							col1 int NOT NULL)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_16651_2
;
----------
[CREATE_POLICY] create or replace rule r1 as on update to rules_base do instead
  select * from rules_base where f1 = 11 for update of rules_base
;
----------
[DROP_TABLE] DROP TABLE vac_tab_auto
;
----------
[CREATE_TABLE] CREATE TABLE test1cs (x text COLLATE case_sensitive)
;
----------
[DROP_TABLE] DROP TABLE loct1
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_ts3 LOCATION ''
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v4 AS SELECT * FROM base_table WHERE id IN (SELECT id FROM base_table2)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger on ddl_command_start
   execute procedure test_event_trigger()
;
----------
[ADMIN_TABLE] ANALYZE public.a_star
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_amproc
;
----------
[DROP_TABLE] -- fails
DROP TABLE fk_partitioned_fk_full
;
----------
[DROP_TABLE] DROP TABLE prt2_adv_p3_2
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_constraint
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_create_logical_replication_slot(
    IN slot_name name, IN plugin name,
    IN temporary boolean DEFAULT false,
    IN twophase boolean DEFAULT false,
    OUT slot_name name, OUT lsn pg_lsn)
RETURNS RECORD
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_create_logical_replication_slot'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_float8_ops
DEFAULT FOR TYPE float8 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_float8_consistent (internal, float8, int2, oid, internal),
	FUNCTION	2	gbt_float8_union (internal, internal),
	FUNCTION	3	gbt_float8_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_float8_penalty (internal, internal, internal),
	FUNCTION	6	gbt_float8_picksplit (internal, internal),
	FUNCTION	7	gbt_float8_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explicitly_binary_coercible"("oid", "oid") RETURNS boolean
    LANGUAGE "plpgsql" STABLE STRICT
    AS $_$
begin
  if $1 = $2 then return true; end if;
  if EXISTS(select 1 from pg_catalog.pg_cast where
            castsource = $1 and casttarget = $2 and
            castmethod = 'b')
  then return true; end if;
  if $2 = 'pg_catalog.any'::pg_catalog.regtype then return true; end if;
  if $2 = 'pg_catalog.anyarray'::pg_catalog.regtype then
    if EXISTS(select 1 from pg_catalog.pg_type where
              oid = $1 and typelem != 0 and typlen = -1)
    then return true; end if;
  end if;
  if $2 = 'pg_catalog.anyrange'::pg_catalog.regtype then
    if (select typtype from pg_catalog.pg_type where oid = $1) = 'r'
    then return true; end if;
  end if;
  return false;
end
$_$
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30919 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_ts_parser
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_POLICY] /*
 * text search configuration for catalan language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * catalan and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY catalan_stem
	(TEMPLATE = snowball, Language = catalan )
;
----------
[CREATE_TABLE] create table inh_pp1 (f1 int not null)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest28b" (
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2)) STORED
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter a set not null
;
----------
[CREATE_TABLE] CREATE TABLE tt7 (x int, q text, y numeric(8,2))
;
----------
[CREATE_TABLE] CREATE TABLE t_012_tbl (id int)
;
----------
[CREATE_TABLE] CREATE TABLE tbl_cs_fail(data int not null) WITH (AUTOVACUUM_ENABLED = false)
;
----------
[DROP_TABLE] DROP TABLE cic_test
;
