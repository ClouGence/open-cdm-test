COMMENT ON CONSTRAINT no_constraint ON DOMAIN constraint_comments_dom IS 'yes, another comment'
;
-- test copy in/out
create table attest (a int4, b int4, c int4)
;
CALL test_proc8c(_a, _b, _c)
;
create function concat(text, variadic anyarray) returns text as $$
  select array_to_string($2, $1);
$$ language sql immutable strict
;
create trigger self_ref_s_trig
  after delete on self_ref referencing old table as old_table
  for each statement execute procedure dump_delete()
;
CREATE TABLE "public"."undroppable_objs" (
    "object_type" "text",
    "object_identity" "text"
)
;
alter table def_view_test alter column c2 set default 'view_default'
;
CREATE TABLE "gin_check_jsonb"("j" jsonb)
;
alter table stud_emp rename to student
;
drop table cnullparent cascade
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
ALTER TABLE public.atacc1 ADD CONSTRAINT atacc1_fkey FOREIGN KEY (a) REFERENCES atacc1(a) NOT VALID
;
CREATE TYPE tablefunc_crosstab_2 AS
(
	row_name TEXT,
	category_1 TEXT,
	category_2 TEXT
)
;
CREATE TABLE gtest_err_5b (a int PRIMARY KEY, b int GENERATED ALWAYS AS identity GENERATED ALWAYS AS (a * 2) STORED)
;
CREATE VIEW "public"."shoelace_candelete" AS
 SELECT "sl_name",
    "sl_avail",
    "sl_color",
    "sl_len",
    "sl_unit",
    "sl_len_cm"
   FROM "public"."shoelace_obsolete"
  WHERE ("sl_avail" = 0)
;
ALTER TABLE tt9 ADD CHECK(c > 1)
;
CREATE INDEX test_index1 ON testschema.test_default_tab USING btree (id)
;
CREATE SCHEMA testlibpq3
;
/* contrib/pageinspect/pageinspect--1.5.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- get_raw_page()
--
CREATE FUNCTION get_raw_page(text, int4)
RETURNS bytea
AS '$libdir/pageinspect', 'get_raw_page'
LANGUAGE C STRICT PARALLEL SAFE
;
alter table atacc3 rename test2 to testx
;
create table pfk (a int references ppk) partition by list (a)
;
DROP EXTENSION test_custom_types
;
/*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
CREATE TABLE p (a integer)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_database
;
-- test copy from with a partitioned table
create table parted_copytest (
	a int,
	b int,
	c text
) partition by list (b)
;
alter table parted_conflict attach partition parted_conflict_1 for values from (0) to (1000)
;
CREATE FUNCTION isngt(ismn, ean13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TEMP TABLE pg_temp_2.pg_temp_28230_2 (tid pg_catalog.tid)
;
CREATE SCHEMA addr_nsp
;
CREATE UNIQUE INDEX test_replica_identity3_id_key ON public.test_replica_identity3 USING btree (id)
;
CREATE TYPE foo_t AS (f1 int, f2 text, f3 int, f4 int8)
;
create table child (a double precision, b decimal(10,4)) inherits (parent)
;
--
-- Verify that triggers are fired for partitioned tables
--
create table parted_stmt_trig (a int) partition by list (a)
;
alter table ac drop constraint ac_aa_check
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "hword_asciipart" WITH "public"."ispell", "english_stem"
;
create table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=98) tablespace "regress_pgbench_tap_1_ts"
;
DROP TABLE pagg_tab3
;
CREATE TEMPORARY TABLE tree(
    id INTEGER PRIMARY KEY,
    parent_id INTEGER REFERENCES tree(id)
)
;
CALL ptest1('a')
;
CREATE DOMAIN "public"."gtestdomainnn" AS integer
	CONSTRAINT "gtestdomainnn_check" CHECK ((VALUE IS NOT NULL))
;
create trigger my_table_update_trig
  after update on my_table referencing old table as old_table new table as new_table
  for each statement execute procedure dump_update()
;
create index on t using spgist(f1) include(f2, f3)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
--- Bit operations
DROP TABLE bit_table
;
ALTER INDEX pg_toast.pg_toast_33236_index RENAME TO reind_con_toast_idx
;
alter table atacc1 add constraint atacc_test1 check (test>3)
;
DROP TYPE rf_bug_status
;
alter index idxpart_a_idx attach partition idxpart1_a_idx
;
CREATE TABLE withoid() WITH (oids)
;
------------------------------------------------------------------------------------------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT no_constraint ON DOMAIN constraint_comments_dom IS 'yes, another comment'
;
----------
[CREATE_TABLE] -- test copy in/out
create table attest (a int4, b int4, c int4)
;
----------
[CALL_PROG_OBJ] CALL test_proc8c(_a, _b, _c)
;
----------
[CREATE_PROG_OBJ] create function concat(text, variadic anyarray) returns text as $$
  select array_to_string($2, $1);
$$ language sql immutable strict
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger self_ref_s_trig
  after delete on self_ref referencing old table as old_table
  for each statement execute procedure dump_delete()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."undroppable_objs" (
    "object_type" "text",
    "object_identity" "text"
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table def_view_test alter column c2 set default 'view_default'
;
----------
[CREATE_TABLE] CREATE TABLE "gin_check_jsonb"("j" jsonb)
;
----------
[RENAME_TABLE] alter table stud_emp rename to student
;
----------
[DROP_TABLE] drop table cnullparent cascade
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.atacc1 ADD CONSTRAINT atacc1_fkey FOREIGN KEY (a) REFERENCES atacc1(a) NOT VALID
;
----------
[CREATE_TYPE] CREATE TYPE tablefunc_crosstab_2 AS
(
	row_name TEXT,
	category_1 TEXT,
	category_2 TEXT
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_5b (a int PRIMARY KEY, b int GENERATED ALWAYS AS identity GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."shoelace_candelete" AS
 SELECT "sl_name",
    "sl_avail",
    "sl_color",
    "sl_len",
    "sl_unit",
    "sl_len_cm"
   FROM "public"."shoelace_obsolete"
  WHERE ("sl_avail" = 0)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD CHECK(c > 1)
;
----------
[ADD_INDEX] CREATE INDEX test_index1 ON testschema.test_default_tab USING btree (id)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA testlibpq3
;
----------
[CREATE_PROG_OBJ] /* contrib/pageinspect/pageinspect--1.5.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- get_raw_page()
--
CREATE FUNCTION get_raw_page(text, int4)
RETURNS bytea
AS '$libdir/pageinspect', 'get_raw_page'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[RENAME_COLUMN] alter table atacc3 rename test2 to testx
;
----------
[CREATE_TABLE] create table pfk (a int references ppk) partition by list (a)
;
----------
[DROP_LIBRARY] DROP EXTENSION test_custom_types
;
----------
[CREATE_POLICY] /*
 * text search configuration for portuguese language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * portuguese and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY portuguese_stem
	(TEMPLATE = snowball, Language = portuguese , StopWords=portuguese)
;
----------
[CREATE_TABLE] CREATE TABLE p (a integer)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_database
;
----------
[CREATE_TABLE] -- test copy from with a partitioned table
create table parted_copytest (
	a int,
	b int,
	c text
) partition by list (b)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_conflict attach partition parted_conflict_1 for values from (0) to (1000)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ismn, ean13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_2.pg_temp_28230_2 (tid pg_catalog.tid)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA addr_nsp
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX test_replica_identity3_id_key ON public.test_replica_identity3 USING btree (id)
;
----------
[CREATE_TYPE] CREATE TYPE foo_t AS (f1 int, f2 text, f3 int, f4 int8)
;
----------
[CREATE_TABLE] create table child (a double precision, b decimal(10,4)) inherits (parent)
;
----------
[CREATE_TABLE] --
-- Verify that triggers are fired for partitioned tables
--
create table parted_stmt_trig (a int) partition by list (a)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table ac drop constraint ac_aa_check
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "hword_asciipart" WITH "public"."ispell", "english_stem"
;
----------
[CREATE_TABLE] create table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=98) tablespace "regress_pgbench_tap_1_ts"
;
----------
[DROP_TABLE] DROP TABLE pagg_tab3
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE tree(
    id INTEGER PRIMARY KEY,
    parent_id INTEGER REFERENCES tree(id)
)
;
----------
[CALL_PROG_OBJ] CALL ptest1('a')
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."gtestdomainnn" AS integer
	CONSTRAINT "gtestdomainnn_check" CHECK ((VALUE IS NOT NULL))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_table_update_trig
  after update on my_table referencing old table as old_table new table as new_table
  for each statement execute procedure dump_update()
;
----------
[ADD_INDEX] create index on t using spgist(f1) include(f2, f3)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[DROP_TABLE] --- Bit operations
DROP TABLE bit_table
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33236_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc_test1 check (test>3)
;
----------
[DROP_TYPE] DROP TYPE rf_bug_status
;
----------
[ALTER_INDEX] alter index idxpart_a_idx attach partition idxpart1_a_idx
;
----------
[CREATE_TABLE] CREATE TABLE withoid() WITH (oids)
;
