ALTER EXTENSION postgres_fdw ADD OPERATOR === (int, int)
;
ALTER TABLE pg_ts_template ADD UNIQUE USING INDEX pg_ts_template_tmplname_index
;
CREATE MATERIALIZED VIEW s5.t1_mv AS SELECT * FROM s5.t1
;
DROP FOREIGN TABLE IF EXISTS split_fdw_repl.remote_accounts
;
CREATE TABLE test_type_diff (f1 int)
;
SECURITY LABEL FOR 'dummy' ON ROLE regress_seclabel_user1 IS 'classified'
;
CREATE INDEX toasty ON toast_bug(buggy)
;
create rule rtest_order_r3 as on insert to rtest_order1 do instead
	insert into rtest_order2 values (new.a, nextval('rtest_seq'),
		'rule 3 - this should run 3rd')
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_trigger
;
CREATE TABLE gtest28b (LIKE gtest28a INCLUDING GENERATED)
;
COMMENT ON CONSTRAINT constrname_wrong ON FKTABLE IS 'fk constraint comment'
;
CREATE TABLE "public"."gtest22c" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
create operator family my_op_family using btree
;
CREATE TABLE ref_tbl (a int PRIMARY KEY)
;
create table idxpart (a int, b int, primary key (a, b)) partition by range (a)
;
/* contrib/btree_gin/btree_gin--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- uuid datatype support new in 1.3.
CREATE FUNCTION gin_extract_value_uuid(uuid, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE gtest_parent ATTACH PARTITION gtest_child FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
create table idxpart (a int primary key, b int) partition by range (b, a)
;
CREATE FUNCTION public.pg_get_replica_identity_index(int)
	 RETURNS regclass LANGUAGE sql AS 'SELECT 1/0'
;
CREATE OR REPLACE FUNCTION measurement_insert_trigger()
RETURNS TRIGGER AS $$
BEGIN
    IF ( NEW.logdate >= DATE '2006-02-01' AND
         NEW.logdate < DATE '2006-03-01' ) THEN
        INSERT INTO measurement_y2006m02 VALUES (NEW.*);
    ELSIF ( NEW.logdate >= DATE '2006-03-01' AND
            NEW.logdate < DATE '2006-04-01' ) THEN
        INSERT INTO measurement_y2006m03 VALUES (NEW.*);
    ELSIF ( NEW.logdate >= DATE '2007-01-01' AND
            NEW.logdate < DATE '2007-02-01' ) THEN
        INSERT INTO measurement_y2007m01 (city_id, logdate, peaktemp, unitsales)
            VALUES (NEW.*);
    ELSE
        RAISE EXCEPTION 'Date out of range.  Fix the measurement_insert_trigger() function!';
    END IF;
    RETURN NULL;
END;
$$ LANGUAGE plpgsql
;
create index on parted_index_col_drop (c)
;
CREATE TABLE test_prepared1(id integer primary key)
;
CREATE TABLE part4 PARTITION OF range_parted2 FOR VALUES FROM (90) TO (100)
;
-- MCV lists
CREATE TABLE mcv_lists (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b VARCHAR,
    filler3 DATE,
    c INT,
    d TEXT
)
;
create table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) partition by range (aid)
;
CREATE TABLE origin_tbl(id serial primary key, data text)
;
CREATE UNIQUE INDEX test_replica_identity5_a_b_key ON test_replica_identity5 (a, b)
;
CREATE INDEX hash_idx ON hash_temp_heap USING hash (x)
;
CREATE TABLE clstrpart12 PARTITION OF clstrpart1 FOR VALUES FROM (5) TO (10) PARTITION BY RANGE (a)
;
CREATE TABLE compositetable(a text, b text)
;
do $$
declare v_test plpgsql_arr_domain := array[1];
begin
  v_test := 0 || v_test;  -- fail
end;
$$
;
DROP TABLE IF EXISTS t4 CASCADE
;
-- Don't drop trans_updatetrig yet. It is required below.

-- Test with transition tuple conversion happening for rows moved into the
-- new partition. This requires a trigger that references transition table
-- (we already have trans_updatetrig). For inserted rows, the conversion
-- is not usually needed, because the original tuple is already compatible with
-- the desired transition tuple format. But conversion happens when there is a
-- BR trigger because the trigger can change the inserted row. So install a
-- BR triggers on those child partitions where the rows will be moved.
CREATE FUNCTION func_parted_mod_b() RETURNS trigger AS $$
BEGIN
   NEW.b = NEW.b + 1;
   return NEW;
END $$ language plpgsql
;
CALL p1(v_cnt := v_cnt)
;
VACUUM (FULL) pg_catalog.pg_extension
;
create table pk (a int primary key) partition by range (a)
;
ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey NOT DEFERRABLE
;
ALTER TABLE pg_toast.pg_toast_33486 RENAME TO reind_con_toast
;
-- failed (not owner)
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf1 RENAME TO alt_ts_conf4
;
DROP INDEX people_names
;
CREATE VIEW "public"."rtest_vview5" AS
 SELECT "rtest_view1"."a",
    "rtest_view1"."b",
    "public"."rtest_viewfunc1"("rtest_view1"."a") AS "refcount"
   FROM "public"."rtest_view1"
;
CREATE TABLE deptest (f1 serial primary key, f2 text)
;
-- foreign key on partitioned table: partition key is referencing column.
-- Make sure truncate did execute on all tables
CREATE FUNCTION tp_ins_data() RETURNS void LANGUAGE plpgsql AS $$
  BEGIN
	INSERT INTO truncprim VALUES (1), (100), (150);
	INSERT INTO truncpart VALUES (1), (100), (150);
  END
$$
;
-- test CLUSTER on expression index
CREATE TABLE clstr_expression(id serial primary key, a int, b text COLLATE "C")
;
create rule def_view_test_ins as
	on insert to def_view_test
	do instead insert into def_test select new.*
;
ALTER TABLE pg_temp_4.pg_temp_27609_2 ADD COLUMN newdata pg_temp_4.pg_temp_27609
;
ANALYZE transition_table_status
;
CREATE TRIGGER z_min_update
BEFORE UPDATE ON min_updates_test
FOR EACH ROW EXECUTE PROCEDURE suppress_redundant_updates_trigger()
;
ALTER TABLE temporal_fk_rng2rng
  DROP CONSTRAINT temporal_fk_rng2rng_fk
;
-- test parallel build with immutable function.
CREATE TABLE btree_test_expr (n int)
;
------------------------------------------------------------------------------------------
[ALTER_LIBRARY] ALTER EXTENSION postgres_fdw ADD OPERATOR === (int, int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_template ADD UNIQUE USING INDEX pg_ts_template_tmplname_index
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW s5.t1_mv AS SELECT * FROM s5.t1
;
----------
[DROP_TABLE] DROP FOREIGN TABLE IF EXISTS split_fdw_repl.remote_accounts
;
----------
[CREATE_TABLE] CREATE TABLE test_type_diff (f1 int)
;
----------
[SECURITY_LABEL] SECURITY LABEL FOR 'dummy' ON ROLE regress_seclabel_user1 IS 'classified'
;
----------
[ADD_INDEX] CREATE INDEX toasty ON toast_bug(buggy)
;
----------
[CREATE_POLICY] create rule rtest_order_r3 as on insert to rtest_order1 do instead
	insert into rtest_order2 values (new.a, nextval('rtest_seq'),
		'rule 3 - this should run 3rd')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_trigger
;
----------
[CREATE_TABLE] CREATE TABLE gtest28b (LIKE gtest28a INCLUDING GENERATED)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT constrname_wrong ON FKTABLE IS 'fk constraint comment'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest22c" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_PROG_OBJ] create operator family my_op_family using btree
;
----------
[CREATE_TABLE] CREATE TABLE ref_tbl (a int PRIMARY KEY)
;
----------
[CREATE_TABLE] create table idxpart (a int, b int, primary key (a, b)) partition by range (a)
;
----------
[CREATE_PROG_OBJ] /* contrib/btree_gin/btree_gin--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- uuid datatype support new in 1.3.
CREATE FUNCTION gin_extract_value_uuid(uuid, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE gtest_parent ATTACH PARTITION gtest_child FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[CREATE_TABLE] create table idxpart (a int primary key, b int) partition by range (b, a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION public.pg_get_replica_identity_index(int)
	 RETURNS regclass LANGUAGE sql AS 'SELECT 1/0'
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION measurement_insert_trigger()
RETURNS TRIGGER AS $$
BEGIN
    IF ( NEW.logdate >= DATE '2006-02-01' AND
         NEW.logdate < DATE '2006-03-01' ) THEN
        INSERT INTO measurement_y2006m02 VALUES (NEW.*);
    ELSIF ( NEW.logdate >= DATE '2006-03-01' AND
            NEW.logdate < DATE '2006-04-01' ) THEN
        INSERT INTO measurement_y2006m03 VALUES (NEW.*);
    ELSIF ( NEW.logdate >= DATE '2007-01-01' AND
            NEW.logdate < DATE '2007-02-01' ) THEN
        INSERT INTO measurement_y2007m01 (city_id, logdate, peaktemp, unitsales)
            VALUES (NEW.*);
    ELSE
        RAISE EXCEPTION 'Date out of range.  Fix the measurement_insert_trigger() function!';
    END IF;
    RETURN NULL;
END;
$$ LANGUAGE plpgsql
;
----------
[ADD_INDEX] create index on parted_index_col_drop (c)
;
----------
[CREATE_TABLE] CREATE TABLE test_prepared1(id integer primary key)
;
----------
[CREATE_TABLE] CREATE TABLE part4 PARTITION OF range_parted2 FOR VALUES FROM (90) TO (100)
;
----------
[CREATE_TABLE] -- MCV lists
CREATE TABLE mcv_lists (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b VARCHAR,
    filler3 DATE,
    c INT,
    d TEXT
)
;
----------
[CREATE_TABLE] create table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) partition by range (aid)
;
----------
[CREATE_TABLE] CREATE TABLE origin_tbl(id serial primary key, data text)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX test_replica_identity5_a_b_key ON test_replica_identity5 (a, b)
;
----------
[ADD_INDEX] CREATE INDEX hash_idx ON hash_temp_heap USING hash (x)
;
----------
[CREATE_TABLE] CREATE TABLE clstrpart12 PARTITION OF clstrpart1 FOR VALUES FROM (5) TO (10) PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE] CREATE TABLE compositetable(a text, b text)
;
----------
[BLOCK] do $$
declare v_test plpgsql_arr_domain := array[1];
begin
  v_test := 0 || v_test;  -- fail
end;
$$
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS t4 CASCADE
;
----------
[CREATE_PROG_OBJ] -- Don't drop trans_updatetrig yet. It is required below.

-- Test with transition tuple conversion happening for rows moved into the
-- new partition. This requires a trigger that references transition table
-- (we already have trans_updatetrig). For inserted rows, the conversion
-- is not usually needed, because the original tuple is already compatible with
-- the desired transition tuple format. But conversion happens when there is a
-- BR trigger because the trigger can change the inserted row. So install a
-- BR triggers on those child partitions where the rows will be moved.
CREATE FUNCTION func_parted_mod_b() RETURNS trigger AS $$
BEGIN
   NEW.b = NEW.b + 1;
   return NEW;
END $$ language plpgsql
;
----------
[CALL_PROG_OBJ] CALL p1(v_cnt := v_cnt)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_extension
;
----------
[CREATE_TABLE] create table pk (a int primary key) partition by range (a)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey NOT DEFERRABLE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33486 RENAME TO reind_con_toast
;
----------
[ALTER_POLICY] -- failed (not owner)
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf1 RENAME TO alt_ts_conf4
;
----------
[DROP_INDEX] DROP INDEX people_names
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_vview5" AS
 SELECT "rtest_view1"."a",
    "rtest_view1"."b",
    "public"."rtest_viewfunc1"("rtest_view1"."a") AS "refcount"
   FROM "public"."rtest_view1"
;
----------
[CREATE_TABLE] CREATE TABLE deptest (f1 serial primary key, f2 text)
;
----------
[CREATE_PROG_OBJ] -- foreign key on partitioned table: partition key is referencing column.
-- Make sure truncate did execute on all tables
CREATE FUNCTION tp_ins_data() RETURNS void LANGUAGE plpgsql AS $$
  BEGIN
	INSERT INTO truncprim VALUES (1), (100), (150);
	INSERT INTO truncpart VALUES (1), (100), (150);
  END
$$
;
----------
[CREATE_TABLE] -- test CLUSTER on expression index
CREATE TABLE clstr_expression(id serial primary key, a int, b text COLLATE "C")
;
----------
[CREATE_POLICY] create rule def_view_test_ins as
	on insert to def_view_test
	do instead insert into def_test select new.*
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_27609_2 ADD COLUMN newdata pg_temp_4.pg_temp_27609
;
----------
[ADMIN_TABLE] ANALYZE transition_table_status
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER z_min_update
BEFORE UPDATE ON min_updates_test
FOR EACH ROW EXECUTE PROCEDURE suppress_redundant_updates_trigger()
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE temporal_fk_rng2rng
  DROP CONSTRAINT temporal_fk_rng2rng_fk
;
----------
[CREATE_TABLE] -- test parallel build with immutable function.
CREATE TABLE btree_test_expr (n int)
;
