create table spgist_box_tbl(id serial, b box)
;
CREATE TEMP TABLE concur_temp (f1 int, f2 text)
  ON COMMIT DROP
;
ALTER TABLE T ADD COLUMN c_time TIME DEFAULT '12:00:00',
              ALTER COLUMN c_num SET DEFAULT 2.000000000000002
;
TRUNCATE TABLE trunc_c,truncate_a,trunc_d
;
CREATE FUNCTION gbt_macad8_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- Now check SELECT FOR UPDATE/SHARE with an inherited source table,
-- where the parent is itself a foreign table
create table loct4 (f1 int, f2 int, f3 int)
;
-- unique index
CREATE UNIQUE INDEX CONCURRENTLY concur_index2 ON concur_heap(f1)
;
ALTER TABLE pg_temp_3.pg_temp_27374_2 ADD COLUMN newdata pg_temp_3.pg_temp_27374
;
drop function tftest(int)
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '
')
;
CREATE FUNCTION "public"."trigfunc"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  begin
    raise notice 'trigger = % fired on table % during %',
                 TG_NAME, TG_TABLE_NAME, TG_OP;
    return null;
  end;
$$
;
CREATE TABLE fk_partitioned_pk_6 (a int PRIMARY KEY)
;
drop table hp2
;
ALTER TABLE ONLY "generated_stored_tests"."gtest28a" DROP COLUMN "........pg.dropped.1........"
;
DROP TABLE multixact_conflict
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_data_wrapper
;
-- LC_CTYPE
CREATE COLLATION coll_dup_chk (LC_CTYPE = "POSIX", LC_CTYPE = "NONSENSE", LC_COLLATE = "POSIX")
;
-- \gexec

create temporary table gexec_test(a int, b text, c date, d float)
;
drop function error1(text)
;
ALTER SUBSCRIPTION regress_mysub1 SET (failover = true)
;
create table rlp5 partition of rlp for values from (31) to (maxvalue) partition by range (a)
;
COMMENT ON TYPE default_test_row IS 'good comment'
;
-- verify that DROP works
DROP TABLE fk_partitioned_fk_2
;
CREATE TABLE pk51 PARTITION OF pk5 FOR VALUES FROM (4000) TO (4500)
;
ALTER TABLE pg_toast.pg_toast_30701 RENAME TO reind_con_toast
;
CREATE TABLE only_inh_parent (a int primary key, b TEXT)
;
CREATE TABLE trunc_f (col1 integer primary key)
;
alter table convslot_test_child add column col2 text not null default 'tutu'
;
ALTER FOREIGN TABLE ft2 ALTER COLUMN c1 OPTIONS (column_name 'C 1')
;
--
-- CREATE_CAST
--

-- Create some types to test with
CREATE TYPE casttesttype
;
DROP TABLE tabx
;
CREATE OR REPLACE FUNCTION ret_setint() RETURNS SETOF integer AS
$$
BEGIN
    RETURN QUERY EXECUTE 'select 1 union all select 1';
END;
$$
LANGUAGE plpgsql IMMUTABLE
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_transform
;
CREATE SUBSCRIPTION testsub CONNECTION $$port=16258 host=/tmp/Z2NRyt87xn dbname='d1'$$ PUBLICATION testpub WITH (create_slot=false, slot_name='testslot')
;
-- copied from equivclass.sql
create type int8alias1
;
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, PRIMARY KEY(ptest1, ptest2))
;
CREATE OPERATOR &> (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_over_right,
	RESTRICT = positionsel,
	JOIN = positionjoinsel
)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_transform
;
ANALYZE public.log_table
;
create table gtest34p_1 partition of gtest34p for values from (1) to (100)
;
create table part_default_p2 partition of part_default for values from (30) to (40)
;
CREATE OR REPLACE FUNCTION
  json_strip_nulls(target json, strip_in_arrays boolean DEFAULT false)
RETURNS json
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'json_strip_nulls'
;
CREATE CAST (isbn13 AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
CREATE SUBSCRIPTION tsub CONNECTION 'port=30858 host=/tmp/fdJ2go18o0 dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
-- prevent using columns of unsupported types in key (type must have a btree operator class)
CREATE TABLE partitioned (
	a point
) PARTITION BY LIST (a)
;
DROP TABLE fkeys2
;
-- fail (not superuser)
SECURITY LABEL ON TABLE dummy_seclabel_tbl3 IS 'unclassified'
;
ALTER TABLE ONLY "public"."notnull_tbl6" ATTACH PARTITION "public"."notnull_tbl6_1" FOR VALUES IN (1)
;
ALTER TABLE pg_temp_10.z ADD CONSTRAINT z_pkey PRIMARY KEY (aa, b)
;
CREATE TABLE part_3_4 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IN (3))
)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table spgist_box_tbl(id serial, b box)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE concur_temp (f1 int, f2 text)
  ON COMMIT DROP
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_time TIME DEFAULT '12:00:00',
              ALTER COLUMN c_num SET DEFAULT 2.000000000000002
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE trunc_c,truncate_a,trunc_d
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad8_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] -- Now check SELECT FOR UPDATE/SHARE with an inherited source table,
-- where the parent is itself a foreign table
create table loct4 (f1 int, f2 int, f3 int)
;
----------
[ADD_INDEX] -- unique index
CREATE UNIQUE INDEX CONCURRENTLY concur_index2 ON concur_heap(f1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_27374_2 ADD COLUMN newdata pg_temp_3.pg_temp_27374
;
----------
[DROP_PROG_OBJ] drop function tftest(int)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '
')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."trigfunc"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  begin
    raise notice 'trigger = % fired on table % during %',
                 TG_NAME, TG_TABLE_NAME, TG_OP;
    return null;
  end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_pk_6 (a int PRIMARY KEY)
;
----------
[DROP_TABLE] drop table hp2
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_stored_tests"."gtest28a" DROP COLUMN "........pg.dropped.1........"
;
----------
[DROP_TABLE] DROP TABLE multixact_conflict
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_POLICY] -- LC_CTYPE
CREATE COLLATION coll_dup_chk (LC_CTYPE = "POSIX", LC_CTYPE = "NONSENSE", LC_COLLATE = "POSIX")
;
----------
[CREATE_TABLE] -- \gexec

create temporary table gexec_test(a int, b text, c date, d float)
;
----------
[DROP_PROG_OBJ] drop function error1(text)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_mysub1 SET (failover = true)
;
----------
[CREATE_TABLE] create table rlp5 partition of rlp for values from (31) to (maxvalue) partition by range (a)
;
----------
[COMMENT_TYPE] COMMENT ON TYPE default_test_row IS 'good comment'
;
----------
[DROP_TABLE] -- verify that DROP works
DROP TABLE fk_partitioned_fk_2
;
----------
[CREATE_TABLE] CREATE TABLE pk51 PARTITION OF pk5 FOR VALUES FROM (4000) TO (4500)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30701 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE only_inh_parent (a int primary key, b TEXT)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_f (col1 integer primary key)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table convslot_test_child add column col2 text not null default 'tutu'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft2 ALTER COLUMN c1 OPTIONS (column_name 'C 1')
;
----------
[CREATE_TYPE] --
-- CREATE_CAST
--

-- Create some types to test with
CREATE TYPE casttesttype
;
----------
[DROP_TABLE] DROP TABLE tabx
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION ret_setint() RETURNS SETOF integer AS
$$
BEGIN
    RETURN QUERY EXECUTE 'select 1 union all select 1';
END;
$$
LANGUAGE plpgsql IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_transform
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub CONNECTION $$port=16258 host=/tmp/Z2NRyt87xn dbname='d1'$$ PUBLICATION testpub WITH (create_slot=false, slot_name='testslot')
;
----------
[CREATE_TYPE] -- copied from equivclass.sql
create type int8alias1
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, PRIMARY KEY(ptest1, ptest2))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR &> (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_over_right,
	RESTRICT = positionsel,
	JOIN = positionjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_transform
;
----------
[ADMIN_TABLE] ANALYZE public.log_table
;
----------
[CREATE_TABLE] create table gtest34p_1 partition of gtest34p for values from (1) to (100)
;
----------
[CREATE_TABLE] create table part_default_p2 partition of part_default for values from (30) to (40)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  json_strip_nulls(target json, strip_in_arrays boolean DEFAULT false)
RETURNS json
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'json_strip_nulls'
;
----------
[CREATE_PROG_OBJ] CREATE CAST (isbn13 AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tsub CONNECTION 'port=30858 host=/tmp/fdJ2go18o0 dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
----------
[CREATE_TABLE] -- prevent using columns of unsupported types in key (type must have a btree operator class)
CREATE TABLE partitioned (
	a point
) PARTITION BY LIST (a)
;
----------
[DROP_TABLE] DROP TABLE fkeys2
;
----------
[UNKNOWN] -- fail (not superuser)
SECURITY LABEL ON TABLE dummy_seclabel_tbl3 IS 'unclassified'
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."notnull_tbl6" ATTACH PARTITION "public"."notnull_tbl6_1" FOR VALUES IN (1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_temp_10.z ADD CONSTRAINT z_pkey PRIMARY KEY (aa, b)
;
----------
[CREATE_TABLE] CREATE TABLE part_3_4 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IN (3))
)
;
