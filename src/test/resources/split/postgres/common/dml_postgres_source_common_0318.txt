insert into uv_iocu_view (a, b) values ('xyxyxy', 1)
   on conflict (a) do update set b = excluded.b
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '21504'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '"public"."plt1_e_p2"'::pg_catalog.regclass
;
DELETE FROM test_stat_tab WHERE key = 'k1'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17241', relminmxid = '3'
WHERE oid = '"public"."xy_1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11928', relminmxid = '3'
WHERE oid = '"fkpart5"."fk3"'::pg_catalog.regclass
;
insert into trig_table values
  (1, 'one a'),
  (1, 'one b'),
  (2, 'two a'),
  (2, 'two b'),
  (3, 'three a'),
  (3, 'three b')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20962'
;
INSERT INTO TIMESTAMP_TBL VALUES ('Feb 13 17:32:01 1997')
;
-- check that CIDR rejects invalid input:
INSERT INTO INET_TBL (c, i) VALUES ('192.168.1.2/30', '192.168.1.226')
;
INSERT INTO b(aa) VALUES('bbbbbbb')
;
INSERT INTO public.ths_p2 VALUES (3, 563)
;
INSERT INTO test_recovery_conflict_table1 SELECT i, 0 FROM generate_series(1,20) i
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part_rev"'::pg_catalog.regclass
  AND attname IN ('b', 'a')
;
INSERT INTO try (name)
VALUES ('a'), ('ab'), ('â'), ('aba'), ('b'), ('ba'), ('bab'), ('AZ')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17386', relminmxid = '3'
WHERE oid = '"public"."part_p2_p1"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('2', 162)
;
INSERT INTO public.tht VALUES ('9', 49)
;
INSERT INTO trunc_fb VALUES (4, 444)
;
-- might as well exercise the widget type while we're here
INSERT INTO mytab VALUES ('(1,2,3)'), ('(-44,5.5,12)')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6921', relminmxid = '1'
WHERE oid = '"public"."bt_i4_heap"'::pg_catalog.regclass
;
-- Verify that 'now' *does* change over a reasonable interval such as 100 msec,
-- and that it doesn't change over the same interval within a transaction block

INSERT INTO TIMESTAMP_TBL VALUES ('now')
;
insert into tabwithcols values(10,20,30,40),(50,60,70,80)
;
INSERT INTO public.tplain VALUES ('4', 524)
;
-- XXX would like to not have to specify row() construct types here ...
insert into comptable
  values (row(1,'foo'), array[row(2,'bar')::comptype, row(3,'baz')::comptype])
;
INSERT INTO local_tbl VALUES (1505, 505, 'foo')
;
insert into part_aa_bb values ('AAa', 1)
;
insert into hash_tbl (id, p)
			  select g, 60 from generate_series(51, 60) g
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '743', relminmxid = '1'
WHERE oid = '"public"."text_tbl"'::pg_catalog.regclass
;
INSERT INTO public.loct1(a, b) VALUES ($1, $2)
;
INSERT INTO public.ths_p1 VALUES (2, 2)
;
INSERT INTO public.order_summary SELECT (diff.newdata).* FROM pg_temp_11.pg_temp_41126_2 diff WHERE tid IS NULL
;
INSERT INTO public.ths_p3 VALUES (1, 991)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest28b"'::pg_catalog.regclass
;
DELETE FROM stats_track_tab
;
insert into xid8_t1 values ('0'), ('010'), ('42'), ('0xffffffffffffffff'), ('-1')
;
update utrtest set b = b || b from (values (1), (2)) s(x) where a = s.x
  returning *, tableoid::regclass, cmin
;
--
-- Same procedure for 2b-3b but this time updating the WSlot instead
-- of the PSlot. Due to the triggers the result is the same:
-- WSlot and corresponding PSlot point to each other.
--
update WSlot set backlink = 'PS.base.a4' where slotname = 'WS.001.2b'
;
insert into tc values(1,1)
;
INSERT INTO public.tplain VALUES ('6', 666)
;
INSERT INTO atest2 VALUES ('foo', true)
;
INSERT INTO public.ths_p3 VALUES (5, 45)
;
INSERT INTO gtest_rule (a) VALUES (1)
;
-- Try to delete something that should fail
DELETE FROM PKTABLE where ptest1=1 and ptest2=2 and ptest3=3
;
DELETE FROM people WHERE firstname = 'first_name_3'
;
-- 11 % 4 -> 3 remainder i.e. valid data for hpart3 partition
insert into hpart3 values(11)
;
update ft2 set c2 = -2 where c2 = 42 and c1 = 10
;
-- with a conflict
INSERT INTO temporal_mltrng3 (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2005-01-01', '2006-01-01'))) ON CONFLICT DO NOTHING
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8735', relminmxid = '1'
WHERE oid = '27994'
;
INSERT INTO num_exp_sub VALUES (1,2,'34338492.215397047')
;
------------------------------------------------------------------------------------------
[MERGE|SELECT] insert into uv_iocu_view (a, b) values ('xyxyxy', 1)
   on conflict (a) do update set b = excluded.b
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '21504'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '"public"."plt1_e_p2"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM test_stat_tab WHERE key = 'k1'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17241', relminmxid = '3'
WHERE oid = '"public"."xy_1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11928', relminmxid = '3'
WHERE oid = '"fkpart5"."fk3"'::pg_catalog.regclass
;
----------
[INSERT] insert into trig_table values
  (1, 'one a'),
  (1, 'one b'),
  (2, 'two a'),
  (2, 'two b'),
  (3, 'three a'),
  (3, 'three b')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '20962'
;
----------
[INSERT] INSERT INTO TIMESTAMP_TBL VALUES ('Feb 13 17:32:01 1997')
;
----------
[INSERT|SELECT] -- check that CIDR rejects invalid input:
INSERT INTO INET_TBL (c, i) VALUES ('192.168.1.2/30', '192.168.1.226')
;
----------
[INSERT|SELECT] INSERT INTO b(aa) VALUES('bbbbbbb')
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 563)
;
----------
[INSERT|SELECT] INSERT INTO test_recovery_conflict_table1 SELECT i, 0 FROM generate_series(1,20) i
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."part_rev"'::pg_catalog.regclass
  AND attname IN ('b', 'a')
;
----------
[INSERT|SELECT] INSERT INTO try (name)
VALUES ('a'), ('ab'), ('â'), ('aba'), ('b'), ('ba'), ('bab'), ('AZ')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17386', relminmxid = '3'
WHERE oid = '"public"."part_p2_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 162)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 49)
;
----------
[INSERT] INSERT INTO trunc_fb VALUES (4, 444)
;
----------
[INSERT] -- might as well exercise the widget type while we're here
INSERT INTO mytab VALUES ('(1,2,3)'), ('(-44,5.5,12)')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6921', relminmxid = '1'
WHERE oid = '"public"."bt_i4_heap"'::pg_catalog.regclass
;
----------
[INSERT] -- Verify that 'now' *does* change over a reasonable interval such as 100 msec,
-- and that it doesn't change over the same interval within a transaction block

INSERT INTO TIMESTAMP_TBL VALUES ('now')
;
----------
[INSERT] insert into tabwithcols values(10,20,30,40),(50,60,70,80)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 524)
;
----------
[INSERT] -- XXX would like to not have to specify row() construct types here ...
insert into comptable
  values (row(1,'foo'), array[row(2,'bar')::comptype, row(3,'baz')::comptype])
;
----------
[INSERT] INSERT INTO local_tbl VALUES (1505, 505, 'foo')
;
----------
[INSERT] insert into part_aa_bb values ('AAa', 1)
;
----------
[INSERT|SELECT] insert into hash_tbl (id, p)
			  select g, 60 from generate_series(51, 60) g
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '743', relminmxid = '1'
WHERE oid = '"public"."text_tbl"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO public.loct1(a, b) VALUES ($1, $2)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 2)
;
----------
[INSERT|SELECT] INSERT INTO public.order_summary SELECT (diff.newdata).* FROM pg_temp_11.pg_temp_41126_2 diff WHERE tid IS NULL
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 991)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtest28b"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM stats_track_tab
;
----------
[INSERT] insert into xid8_t1 values ('0'), ('010'), ('42'), ('0xffffffffffffffff'), ('-1')
;
----------
[UPDATE|SELECT] update utrtest set b = b || b from (values (1), (2)) s(x) where a = s.x
  returning *, tableoid::regclass, cmin
;
----------
[UPDATE] --
-- Same procedure for 2b-3b but this time updating the WSlot instead
-- of the PSlot. Due to the triggers the result is the same:
-- WSlot and corresponding PSlot point to each other.
--
update WSlot set backlink = 'PS.base.a4' where slotname = 'WS.001.2b'
;
----------
[INSERT] insert into tc values(1,1)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 666)
;
----------
[INSERT] INSERT INTO atest2 VALUES ('foo', true)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 45)
;
----------
[INSERT|SELECT] INSERT INTO gtest_rule (a) VALUES (1)
;
----------
[DELETE] -- Try to delete something that should fail
DELETE FROM PKTABLE where ptest1=1 and ptest2=2 and ptest3=3
;
----------
[DELETE] DELETE FROM people WHERE firstname = 'first_name_3'
;
----------
[INSERT] -- 11 % 4 -> 3 remainder i.e. valid data for hpart3 partition
insert into hpart3 values(11)
;
----------
[UPDATE] update ft2 set c2 = -2 where c2 = 42 and c1 = 10
;
----------
[MERGE|SELECT] -- with a conflict
INSERT INTO temporal_mltrng3 (id, valid_at) VALUES ('[1,2)', datemultirange(daterange('2005-01-01', '2006-01-01'))) ON CONFLICT DO NOTHING
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8735', relminmxid = '1'
WHERE oid = '27994'
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (1,2,'34338492.215397047')
;
