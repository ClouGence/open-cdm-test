EXECUTE dumpTableAttach('38824')
;
COPY public.main_table (a, b) TO stdout
;
EXECUTE dumpFunc('18464')
;
EXECUTE dumpOpr('2552')
;
-- should fail

-- make constraint check immediate
BEGIN
;
EXECUTE getDomainConstraints('35700')
;
ALTER VIEW public.rtest_vcomp OWNER TO zyc
;
-- Turn on RLS and create policy on child to show RLS is checked before constraints
SET SESSION AUTHORIZATION regress_rls_alice
;
-- privilege tests
SET ROLE regress_file_fdw_superuser
;
-- Attempt to add a valid option that's not allowed in a user mapping
ALTER USER MAPPING FOR public SERVER testserver1
	OPTIONS (ADD sslmode 'require')
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpTableAttach('38824')
;
----------
[DATA_EXPORT] COPY public.main_table (a, b) TO stdout
;
----------
[UNSAFE] EXECUTE dumpFunc('18464')
;
----------
[UNSAFE] EXECUTE dumpOpr('2552')
;
----------
[TRANSACTION] -- should fail

-- make constraint check immediate
BEGIN
;
----------
[UNSAFE] EXECUTE getDomainConstraints('35700')
;
----------
[TRANSFER_PRIVILEGE] ALTER VIEW public.rtest_vcomp OWNER TO zyc
;
----------
[SWITCH_USER] -- Turn on RLS and create policy on child to show RLS is checked before constraints
SET SESSION AUTHORIZATION regress_rls_alice
;
----------
[SWITCH_ROLE] -- privilege tests
SET ROLE regress_file_fdw_superuser
;
----------
[GRANT] -- Attempt to add a valid option that's not allowed in a user mapping
ALTER USER MAPPING FOR public SERVER testserver1
	OPTIONS (ADD sslmode 'require')
;
