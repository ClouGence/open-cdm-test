EXECUTE dumpFunc('16427')
;
EXECUTE dumpAgg('20710')
;
COMMENT ON STATISTICS ctlt1_expr_stat IS 'ab expr stats'
;
lock TABLE reind_con_wide in EXCLUSIVE MODE
;
SET LOCAL check_function_bodies = on
;
-- check that we can add new values to existing enums in a transaction
-- but we can't use them
BEGIN
;
CREATE ROLE regress_foobar1
;
SET ROLE regress_s1
;
--
-- Blowfish cipher
--
-- ensure consistent test output regardless of the default bytea format
SET bytea_output TO escape
;
LOCK TABLE "public"."plt1_e_p1" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('16427')
;
----------
[UNSAFE] EXECUTE dumpAgg('20710')
;
----------
[ADMIN_PERFORMANCE] COMMENT ON STATISTICS ctlt1_expr_stat IS 'ab expr stats'
;
----------
[SESSION_LOCK] lock TABLE reind_con_wide in EXCLUSIVE MODE
;
----------
[SESSION_SETTING_WRITE] SET LOCAL check_function_bodies = on
;
----------
[TRANSACTION] -- check that we can add new values to existing enums in a transaction
-- but we can't use them
BEGIN
;
----------
[CREATE_ROLE] CREATE ROLE regress_foobar1
;
----------
[SWITCH_ROLE] SET ROLE regress_s1
;
----------
[SESSION_SETTING_WRITE] --
-- Blowfish cipher
--
-- ensure consistent test output regardless of the default bytea format
SET bytea_output TO escape
;
----------
[SESSION_LOCK] LOCK TABLE "public"."plt1_e_p1" IN ACCESS SHARE MODE
;
