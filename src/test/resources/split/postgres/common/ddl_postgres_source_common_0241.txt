CREATE FUNCTION "public"."part_hashint4_noop"("value" integer, "seed" bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE STRICT PARALLEL SAFE
    AS $$
    select value + seed;
    $$
;
DROP TABLE pgss_do_table
;
CREATE VIEW vw_getrngfunc AS
  SELECT * FROM ROWS FROM( getrngfunc6(1) AS (rngfuncid int, rngfuncsubid int, rngfuncname text) )
                WITH ORDINALITY
;
CREATE OPERATOR #<=# (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_le,
       COMMUTATOR = #>=#,
       NEGATOR = #>#,
       RESTRICT = scalarltsel,
       JOIN = scalarltjoinsel
)
;
CREATE FUNCTION wait_pid(int)
   RETURNS void
   AS '/src/contrib/dblink/../../src/test/regress/regress.so'
   LANGUAGE C STRICT
;
CREATE TABLE part3 (tid integer, balance float, val text)
  WITH (autovacuum_enabled=off)
;
---------------------------------------------------
-- ISBN:
CREATE FUNCTION isnlt(isbn, isbn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE ONLY "public"."gtest28a" DROP COLUMN "........pg.dropped.1........"
;
CREATE TABLE ctas_nodata (ii, jj, kk) AS SELECT i, j FROM ctas_base
;
COMMENT ON FUNCTION c_function_test() IS 'FUNCTION test'
;
ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk NOT VALID
;
CREATE TABLE temporal_no_key (id int4range, valid_at daterange, a text)
;
-- error, cannot use both PARALLEL and FULL
VACUUM (PARALLEL) pvactst
;
DROP TABLE foo, bar
;
CALL version()
;
CREATE TABLE conflict_test(x integer, y text)
;
ALTER INDEX pg_toast.pg_toast_27903_index RENAME TO reind_con_toast_idx
;
alter table pp1 alter f1 set not null
;
create table fkest (a int, b int, c int unique, primary key(a,b))
;
-- test trigger
CREATE TABLE test1 (a int, b hstore)
;
CREATE FUNCTION gbt_text_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
drop procedure ptest10
;
CREATE FUNCTION ks_test_uniform_random_bigint_in_range()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 999999999999) / 1000000000000.0 r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
CREATE RULE "rule2" AS
    ON UPDATE TO "public"."rule_merge1" DO INSTEAD  UPDATE "public"."rule_merge2" SET "a" = "new"."a", "b" = "new"."b"
  WHERE ("rule_merge2"."a" = "old"."a")
;
-- inheritance tests

CREATE TABLE base_tbl_parent (a int)
;
-- ************************************************************
-- *
-- * Trigger procedures and functions for the patchfield
-- * test of PL/pgSQL
-- *
-- ************************************************************


-- ************************************************************
-- * AFTER UPDATE on Room
-- *	- If room no changes let wall slots follow
-- ************************************************************
create function tg_room_au() returns trigger as '
begin
    if new.roomno != old.roomno then
        update WSlot set roomno = new.roomno where roomno = old.roomno;
    end if;
    return new;
end;
' language plpgsql
;
ANALYZE public.itest7c
;
CREATE INDEX "six" ON "public"."shighway" USING "btree" ("name")
;
CREATE TABLE footrg (a int, b text) PARTITION BY LIST(a)
;
CREATE VIEW pg_stat_progress_vacuum AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        S.relid AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'scanning heap'
                      WHEN 2 THEN 'vacuuming indexes'
                      WHEN 3 THEN 'vacuuming heap'
                      WHEN 4 THEN 'cleaning up indexes'
                      WHEN 5 THEN 'truncating heap'
                      WHEN 6 THEN 'performing final cleanup'
                      END AS phase,
        S.param2 AS heap_blks_total, S.param3 AS heap_blks_scanned,
        S.param4 AS heap_blks_vacuumed, S.param5 AS index_vacuum_count,
        S.param6 AS max_dead_tuples, S.param7 AS num_dead_tuples
    FROM pg_stat_get_progress_info('VACUUM') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
DROP table tab_gen_to_nogen
;
ALTER OPERATOR <= (ean13, issn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
ALTER TABLE pg_temp_13.pg_temp_26072_2 ADD COLUMN newdata pg_temp_13.pg_temp_26072
;
CREATE TABLE plt2_adv_p2 PARTITION OF plt2_adv FOR VALUES IN ('0003', '0004')
;
DROP OPERATOR ~ (cube, cube)
;
ANALYZE pg_temp_11.pg_temp_41126
;
ALTER TABLE moved SET TABLESPACE other
;
COMMENT ON FOREIGN TABLE ft1 IS NULL
;
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = int4larger
)
;
CREATE RULE foo_del_rule AS ON DELETE TO foo DO INSTEAD
  UPDATE foo SET f2 = f2||' (deleted)', f3 = -1, f4 = -1 WHERE f1 = OLD.f1
  RETURNING *
;
ALTER TABLE part_3_4 ALTER a SET NOT NULL
;
CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=30672 host=/tmp/INMnCgU8So dbname=postgres' PUBLICATION pub_viaroot
;
ALTER TABLE pg_language ADD UNIQUE USING INDEX pg_language_name_index
;
-- test that duplicate values on unique index prevent refresh
CREATE TABLE mvtest_foo(a, b) AS VALUES(1, 10)
;
DROP TEXT SEARCH PARSER IF EXISTS split_ts_next.simple_parser_renamed CASCADE
;
-- This partitioned index will have no partitions.
CREATE INDEX tbspace_reindex_part_index_10 ON ONLY tbspace_reindex_part_10 (c1)
;
CREATE TABLE t2 (a domain1)
;
COMMENT ON SUBSCRIPTION regress_testsub IS 'test subscription'
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_subscription_rel
;
CREATE VIEW pg_aios AS
    SELECT * FROM pg_get_aios()
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."part_hashint4_noop"("value" integer, "seed" bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE STRICT PARALLEL SAFE
    AS $$
    select value + seed;
    $$
;
----------
[DROP_TABLE] DROP TABLE pgss_do_table
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS
  SELECT * FROM ROWS FROM( getrngfunc6(1) AS (rngfuncid int, rngfuncsubid int, rngfuncname text) )
                WITH ORDINALITY
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #<=# (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_le,
       COMMUTATOR = #>=#,
       NEGATOR = #>#,
       RESTRICT = scalarltsel,
       JOIN = scalarltjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION wait_pid(int)
   RETURNS void
   AS '/src/contrib/dblink/../../src/test/regress/regress.so'
   LANGUAGE C STRICT
;
----------
[CREATE_TABLE] CREATE TABLE part3 (tid integer, balance float, val text)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISBN:
CREATE FUNCTION isnlt(isbn, isbn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."gtest28a" DROP COLUMN "........pg.dropped.1........"
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_nodata (ii, jj, kk) AS SELECT i, j FROM ctas_base
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION c_function_test() IS 'FUNCTION test'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE temporal_no_key (id int4range, valid_at daterange, a text)
;
----------
[ADMIN_TABLE] -- error, cannot use both PARALLEL and FULL
VACUUM (PARALLEL) pvactst
;
----------
[DROP_TABLE] DROP TABLE foo, bar
;
----------
[CALL_PROG_OBJ] CALL version()
;
----------
[CREATE_TABLE] CREATE TABLE conflict_test(x integer, y text)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27903_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table pp1 alter f1 set not null
;
----------
[CREATE_TABLE] create table fkest (a int, b int, c int unique, primary key(a,b))
;
----------
[CREATE_TABLE] -- test trigger
CREATE TABLE test1 (a int, b hstore)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_text_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_PROG_OBJ] drop procedure ptest10
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ks_test_uniform_random_bigint_in_range()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 999999999999) / 1000000000000.0 r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
----------
[CREATE_POLICY] CREATE RULE "rule2" AS
    ON UPDATE TO "public"."rule_merge1" DO INSTEAD  UPDATE "public"."rule_merge2" SET "a" = "new"."a", "b" = "new"."b"
  WHERE ("rule_merge2"."a" = "old"."a")
;
----------
[CREATE_TABLE] -- inheritance tests

CREATE TABLE base_tbl_parent (a int)
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- *
-- * Trigger procedures and functions for the patchfield
-- * test of PL/pgSQL
-- *
-- ************************************************************


-- ************************************************************
-- * AFTER UPDATE on Room
-- *	- If room no changes let wall slots follow
-- ************************************************************
create function tg_room_au() returns trigger as '
begin
    if new.roomno != old.roomno then
        update WSlot set roomno = new.roomno where roomno = old.roomno;
    end if;
    return new;
end;
' language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE public.itest7c
;
----------
[ADD_INDEX] CREATE INDEX "six" ON "public"."shighway" USING "btree" ("name")
;
----------
[CREATE_TABLE] CREATE TABLE footrg (a int, b text) PARTITION BY LIST(a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_progress_vacuum AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        S.relid AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'scanning heap'
                      WHEN 2 THEN 'vacuuming indexes'
                      WHEN 3 THEN 'vacuuming heap'
                      WHEN 4 THEN 'cleaning up indexes'
                      WHEN 5 THEN 'truncating heap'
                      WHEN 6 THEN 'performing final cleanup'
                      END AS phase,
        S.param2 AS heap_blks_total, S.param3 AS heap_blks_scanned,
        S.param4 AS heap_blks_vacuumed, S.param5 AS index_vacuum_count,
        S.param6 AS max_dead_tuples, S.param7 AS num_dead_tuples
    FROM pg_stat_get_progress_info('VACUUM') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
----------
[DROP_TABLE] DROP table tab_gen_to_nogen
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ean13, issn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26072_2 ADD COLUMN newdata pg_temp_13.pg_temp_26072
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p2 PARTITION OF plt2_adv FOR VALUES IN ('0003', '0004')
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ~ (cube, cube)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41126
;
----------
[ALTER_TABLE] ALTER TABLE moved SET TABLESPACE other
;
----------
[COMMENT_TABLE] COMMENT ON FOREIGN TABLE ft1 IS NULL
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = int4larger
)
;
----------
[CREATE_POLICY] CREATE RULE foo_del_rule AS ON DELETE TO foo DO INSTEAD
  UPDATE foo SET f2 = f2||' (deleted)', f3 = -1, f4 = -1 WHERE f1 = OLD.f1
  RETURNING *
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE part_3_4 ALTER a SET NOT NULL
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_viaroot CONNECTION 'port=30672 host=/tmp/INMnCgU8So dbname=postgres' PUBLICATION pub_viaroot
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_language ADD UNIQUE USING INDEX pg_language_name_index
;
----------
[CREATE_TABLE|SELECT] -- test that duplicate values on unique index prevent refresh
CREATE TABLE mvtest_foo(a, b) AS VALUES(1, 10)
;
----------
[DROP_POLICY] DROP TEXT SEARCH PARSER IF EXISTS split_ts_next.simple_parser_renamed CASCADE
;
----------
[ADD_INDEX] -- This partitioned index will have no partitions.
CREATE INDEX tbspace_reindex_part_index_10 ON ONLY tbspace_reindex_part_10 (c1)
;
----------
[CREATE_TABLE] CREATE TABLE t2 (a domain1)
;
----------
[ALTER_PUB_SUB] COMMENT ON SUBSCRIPTION regress_testsub IS 'test subscription'
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_subscription_rel
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_aios AS
    SELECT * FROM pg_get_aios()
;
