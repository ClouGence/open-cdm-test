-- Try to cover all the possible states:
--
-- Note: in Cases 1 & 2, we are trying to return P. Therefore, if the transfn
-- is stfnp, tfnp, or tf2p, we must use ffp as finalfn, because stfnp, tfnp,
-- and tf2p do not return P. Conversely, in Cases 3 & 4, we are trying to
-- return N. Therefore, if the transfn is stfp, tfp, or tf1p, we must use ffnp
-- as finalfn, because stfp, tfp, and tf1p do not return N.
--
--     Case1 (R = P) && (B = A)
--     ------------------------
--     S    tf1
--     -------
--     N    N
-- should CREATE
CREATE AGGREGATE myaggp01a(*) (SFUNC = stfnp, STYPE = int4[],
  FINALFUNC = ffp, INITCOND = '{}')
;
create table idxpart1 (c text, a int, b int)
;
do $$ declare a complex[];
begin a[1].i := 11; raise notice 'a = %, a[1].i = %', a, a[1].i; end$$
;
ALTER TABLE ONLY "public"."trigger_parted" ATTACH PARTITION "public"."trigger_parted_p2" FOR VALUES IN (2)
;
CREATE TABLE user_logins(id serial, who text)
;
ANALYZE (SKIP_LOCKED) information_schema.sql_languages
;
ALTER INDEX alter_idx_rename_test_parted_idx RENAME TO alter_idx_rename_test_parted_idx_2
;
ALTER TABLE part_5 ADD CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 5)
;
ANALYZE fkpart5.fk4
;
-- should work. Now c1.name is local and inhcount is 0.
alter table p2 drop column name
;
ALTER TABLE cnn_pk ADD CONSTRAINT cnn_primarykey PRIMARY KEY USING INDEX cnn_uq
;
ALTER OPERATOR FAMILY gist_date_ops USING gist ADD
	OPERATOR	6	<> (date, date) ,
	OPERATOR	15	<-> (date, date) FOR ORDER BY pg_catalog.integer_ops ,
	FUNCTION	8 (date, date) gbt_date_distance (internal, date, int2, oid, internal) ,
	FUNCTION	9 (date, date) gbt_date_fetch (internal)
;
ALTER TABLE tab_stats ALTER COLUMN b TYPE text USING 'data' || b
;
drop extension cube cascade
;
alter function tattle(x int, y int) volatile
;
ALTER TABLE ONLY "fkpart3"."fk" ATTACH PARTITION "fkpart3"."fk2" FOR VALUES FROM (750) TO (3500)
;
CREATE SUBSCRIPTION tap_sub_A_B
	CONNECTION 'port=11382 host=/tmp/C6o4PTAnCK dbname=postgres application_name=tap_sub_A_B'
	PUBLICATION tap_pub_B
	WITH (origin = none, copy_data = off)
;
CREATE VIEW ro_view1 AS SELECT DISTINCT a, b FROM base_tbl
;
-- The following tests computing RETURNING when the source and the destination
-- partitions of a UPDATE row movement operation have different tuple
-- descriptors, which has been shown to be problematic in the cases where the
-- RETURNING targetlist contains non-target relation attributes that are
-- computed by referring to the source partition plan's output tuple.  Also,
-- a trigger on the destination relation may change the tuple, which must be
-- reflected in the RETURNING output, so we test that too.
CREATE TABLE part_c_1_c_20 (LIKE range_parted)
;
ALTER FUNCTION gbt_ts_fetch(internal) PARALLEL SAFE
;
DROP PROCEDURE ptest4a, ptest4c
;
-- Check the GUC for disabling event triggers
CREATE FUNCTION test_event_trigger_guc() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
	obj record;
BEGIN
	FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
	LOOP
		RAISE NOTICE '% dropped %', tg_tag, obj.object_type;
	END LOOP;
END;
$$
;
CREATE TABLE test22_0 PARTITION OF test22 FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
-- Test forcing the remote server to produce sorted data for a merge join,
-- but the foreign table is an inheritance child.
truncate table loct1
;
ANALYZE public.mvtest_tm
;
ALTER TABLE testschema.tablespace_acl ALTER c TYPE bigint
;
VACUUM pg_attribute
;
CREATE POLICY p1 ON dob_t1 TO regress_rls_dob_role1,regress_rls_dob_role2 USING (true)
;
DROP FUNCTION const_func()
;
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 0 btint42cmp(int4, int2)
;
CREATE unlogged TABLE unlogged_transactional (id int not null, data text not null) WITH (AUTOVACUUM_ENABLED = false)
;
CREATE TABLE tbl_cs_fail(id int) WITH (AUTOVACUUM_ENABLED = false)
;
-- Test FORCE_NOT_NULL and FORCE_NULL options
CREATE TEMP TABLE forcetest (
    a INT NOT NULL,
    b TEXT NOT NULL,
    c TEXT,
    d TEXT,
    e TEXT
)
;
-- if there's no constraints, a different code path is taken:
alter domain dcomptype drop constraint dcomptype_check
;
CREATE TABLE pagg_tab_ml_p2_s2 PARTITION OF pagg_tab_ml_p2 FOR VALUES IN ('0003')
;
ALTER TABLE has_volatile ALTER COLUMN col5 TYPE numeric
;
CREATE TABLE pagg_tab_ml (a int, b int, c text) PARTITION BY RANGE(a)
;
CREATE UNIQUE INDEX t1_btree_unique ON s2.t1 USING BTREE (i)
;
ANALYZE pg_catalog.pg_tablespace
;
-- bpchar, lacking planner support for its length coercion function,
-- could behave differently

create table bpchar_tbl (f1 character(16) unique, f2 bpchar)
;
-- Create an expression index with a predicate
CREATE INDEX sro_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
	WHERE sro_ifun(a + 10) > sro_ifun(10)
;
DROP SCHEMA pub_testpart2 CASCADE
;
CREATE TABLE "public"."prt1_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
CREATE TABLE "public"."agg_data_20k" (
    "g" integer
)
;
VACUUM (ANALYZE) information_schema.sql_languages
;
create trigger zzz after insert on parted_trig for each row execute procedure trigger_notice()
;
CREATE OR REPLACE FUNCTION regexp_replace(string citext, pattern citext, replacement text, flags text) returns TEXT AS $$
    SELECT pg_catalog.regexp_replace( $1::pg_catalog.text, $2::pg_catalog.text, $3, CASE WHEN pg_catalog.strpos($4, 'c') = 0 THEN  $4 || 'i' ELSE $4 END);
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE more__int AS SELECT
   
   
   CASE WHEN a IS NULL OR a = '{}' OR a = '{73,23,20}' THEN a ELSE
     (select array_agg(u) || array_agg(u + 1000) || array_agg(u + 2000)
      from unnest(a) u where u < 2000000000)
   END AS a, a as b
   FROM test__int
;
CREATE TABLE bttest_unique_nulls (a serial, b int, c int UNIQUE)
;
ALTER INDEX pg_toast.pg_toast_27679_index RENAME TO reind_con_toast_idx
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- Try to cover all the possible states:
--
-- Note: in Cases 1 & 2, we are trying to return P. Therefore, if the transfn
-- is stfnp, tfnp, or tf2p, we must use ffp as finalfn, because stfnp, tfnp,
-- and tf2p do not return P. Conversely, in Cases 3 & 4, we are trying to
-- return N. Therefore, if the transfn is stfp, tfp, or tf1p, we must use ffnp
-- as finalfn, because stfp, tfp, and tf1p do not return N.
--
--     Case1 (R = P) && (B = A)
--     ------------------------
--     S    tf1
--     -------
--     N    N
-- should CREATE
CREATE AGGREGATE myaggp01a(*) (SFUNC = stfnp, STYPE = int4[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_TABLE] create table idxpart1 (c text, a int, b int)
;
----------
[BLOCK] do $$ declare a complex[];
begin a[1].i := 11; raise notice 'a = %, a[1].i = %', a, a[1].i; end$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."trigger_parted" ATTACH PARTITION "public"."trigger_parted_p2" FOR VALUES IN (2)
;
----------
[CREATE_TABLE] CREATE TABLE user_logins(id serial, who text)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) information_schema.sql_languages
;
----------
[RENAME_INDEX] ALTER INDEX alter_idx_rename_test_parted_idx RENAME TO alter_idx_rename_test_parted_idx_2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE part_5 ADD CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 5)
;
----------
[ADMIN_TABLE] ANALYZE fkpart5.fk4
;
----------
[ALTER_TABLE|DROP_COLUMN] -- should work. Now c1.name is local and inhcount is 0.
alter table p2 drop column name
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE cnn_pk ADD CONSTRAINT cnn_primarykey PRIMARY KEY USING INDEX cnn_uq
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_date_ops USING gist ADD
	OPERATOR	6	<> (date, date) ,
	OPERATOR	15	<-> (date, date) FOR ORDER BY pg_catalog.integer_ops ,
	FUNCTION	8 (date, date) gbt_date_distance (internal, date, int2, oid, internal) ,
	FUNCTION	9 (date, date) gbt_date_fetch (internal)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tab_stats ALTER COLUMN b TYPE text USING 'data' || b
;
----------
[DROP_LIBRARY] drop extension cube cascade
;
----------
[ALTER_PROG_OBJ] alter function tattle(x int, y int) volatile
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."fk" ATTACH PARTITION "fkpart3"."fk2" FOR VALUES FROM (750) TO (3500)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_A_B
	CONNECTION 'port=11382 host=/tmp/C6o4PTAnCK dbname=postgres application_name=tap_sub_A_B'
	PUBLICATION tap_pub_B
	WITH (origin = none, copy_data = off)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view1 AS SELECT DISTINCT a, b FROM base_tbl
;
----------
[CREATE_TABLE] -- The following tests computing RETURNING when the source and the destination
-- partitions of a UPDATE row movement operation have different tuple
-- descriptors, which has been shown to be problematic in the cases where the
-- RETURNING targetlist contains non-target relation attributes that are
-- computed by referring to the source partition plan's output tuple.  Also,
-- a trigger on the destination relation may change the tuple, which must be
-- reflected in the RETURNING output, so we test that too.
CREATE TABLE part_c_1_c_20 (LIKE range_parted)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_ts_fetch(internal) PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE ptest4a, ptest4c
;
----------
[CREATE_PROG_OBJ] -- Check the GUC for disabling event triggers
CREATE FUNCTION test_event_trigger_guc() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
	obj record;
BEGIN
	FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
	LOOP
		RAISE NOTICE '% dropped %', tg_tag, obj.object_type;
	END LOOP;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE test22_0 PARTITION OF test22 FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[TRUNCATE_TABLE] -- Test forcing the remote server to produce sorted data for a merge join,
-- but the foreign table is an inheritance child.
truncate table loct1
;
----------
[ADMIN_TABLE] ANALYZE public.mvtest_tm
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE testschema.tablespace_acl ALTER c TYPE bigint
;
----------
[ADMIN_TABLE] VACUUM pg_attribute
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON dob_t1 TO regress_rls_dob_role1,regress_rls_dob_role2 USING (true)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION const_func()
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 0 btint42cmp(int4, int2)
;
----------
[CREATE_TABLE] CREATE unlogged TABLE unlogged_transactional (id int not null, data text not null) WITH (AUTOVACUUM_ENABLED = false)
;
----------
[CREATE_TABLE] CREATE TABLE tbl_cs_fail(id int) WITH (AUTOVACUUM_ENABLED = false)
;
----------
[CREATE_TABLE] -- Test FORCE_NOT_NULL and FORCE_NULL options
CREATE TEMP TABLE forcetest (
    a INT NOT NULL,
    b TEXT NOT NULL,
    c TEXT,
    d TEXT,
    e TEXT
)
;
----------
[ALTER_TYPE] -- if there's no constraints, a different code path is taken:
alter domain dcomptype drop constraint dcomptype_check
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p2_s2 PARTITION OF pagg_tab_ml_p2 FOR VALUES IN ('0003')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE has_volatile ALTER COLUMN col5 TYPE numeric
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml (a int, b int, c text) PARTITION BY RANGE(a)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t1_btree_unique ON s2.t1 USING BTREE (i)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_tablespace
;
----------
[CREATE_TABLE] -- bpchar, lacking planner support for its length coercion function,
-- could behave differently

create table bpchar_tbl (f1 character(16) unique, f2 bpchar)
;
----------
[ADD_INDEX] -- Create an expression index with a predicate
CREATE INDEX sro_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
	WHERE sro_ifun(a + 10) > sro_ifun(10)
;
----------
[DROP_SCHEMA] DROP SCHEMA pub_testpart2 CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."agg_data_20k" (
    "g" integer
)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) information_schema.sql_languages
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger zzz after insert on parted_trig for each row execute procedure trigger_notice()
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_replace(string citext, pattern citext, replacement text, flags text) returns TEXT AS $$
    SELECT pg_catalog.regexp_replace( $1::pg_catalog.text, $2::pg_catalog.text, $3, CASE WHEN pg_catalog.strpos($4, 'c') = 0 THEN  $4 || 'i' ELSE $4 END);
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE more__int AS SELECT
   
   
   CASE WHEN a IS NULL OR a = '{}' OR a = '{73,23,20}' THEN a ELSE
     (select array_agg(u) || array_agg(u + 1000) || array_agg(u + 2000)
      from unnest(a) u where u < 2000000000)
   END AS a, a as b
   FROM test__int
;
----------
[CREATE_TABLE] CREATE TABLE bttest_unique_nulls (a serial, b int, c int UNIQUE)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27679_index RENAME TO reind_con_toast_idx
;
