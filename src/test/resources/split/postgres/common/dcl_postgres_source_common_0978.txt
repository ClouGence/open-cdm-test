COPY (SELECT 'bar') TO STDOUT
;
EXECUTE dumpFunc('40897')
;
copy header_copytest (c, a, b) from stdin with (header match)
;
COPY testeoc FROM stdin CSV
;
COPY x (b, c, d, e) from stdin delimiter ',' null 'x'
;
COPY public.renamecolumnchild (d, a, w, x) TO stdout
;
ALTER ROLE regress_testrol1 SET application_name to 'SLAM'
;
EXECUTE dumpFunc('35319')
;
GRANT SELECT ON character_sets TO PUBLIC
;
LOCK TABLE public.pagg_tab1_p1 IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[DATA_EXPORT|SELECT] COPY (SELECT 'bar') TO STDOUT
;
----------
[UNSAFE] EXECUTE dumpFunc('40897')
;
----------
[DATA_IMPORT] copy header_copytest (c, a, b) from stdin with (header match)
;
----------
[DATA_IMPORT] COPY testeoc FROM stdin CSV
;
----------
[DATA_IMPORT] COPY x (b, c, d, e) from stdin delimiter ',' null 'x'
;
----------
[DATA_EXPORT] COPY public.renamecolumnchild (d, a, w, x) TO stdout
;
----------
[ALTER_USER] ALTER ROLE regress_testrol1 SET application_name to 'SLAM'
;
----------
[UNSAFE] EXECUTE dumpFunc('35319')
;
----------
[GRANT] GRANT SELECT ON character_sets TO PUBLIC
;
----------
[SESSION_LOCK] LOCK TABLE public.pagg_tab1_p1 IN ACCESS SHARE MODE
;
