-- btree support

CREATE FUNCTION hstore_eq(hstore,hstore)
RETURNS boolean
AS '$libdir/hstore','hstore_eq'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE OPERATOR <-> (
	LEFTARG = timestamp,
	RIGHTARG = timestamp,
	PROCEDURE = ts_dist,
	COMMUTATOR = '<->'
)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
CREATE FUNCTION test_ginpostinglist()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_ginpostinglist' LANGUAGE C
;
ALTER AGGREGATE split_agg_probe.os_sum(ORDER BY integer) SET SCHEMA split_agg_probe_next
;
ALTER TABLE ONLY "generated_stored_tests"."gtest26"
    ADD CONSTRAINT "gtest26_pkey" PRIMARY KEY ("a")
;
ALTER PUBLICATION testpub_default RENAME TO testpub_foo
;
-- test using a volatile expression as partition bound
create table volatile_partbound_test (partkey timestamp) partition by range (partkey)
;
TRUNCATE test_temp
;
alter table tt5 add column c int
;
create rule qqq as on insert to copydml_test do instead (delete from copydml_test; delete from copydml_test)
;
VACUUM pg_catalog.pg_default_acl
;
CREATE TRIGGER "tg_chkslotname" BEFORE INSERT ON "public"."phone" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotname"('PH')
;
CREATE TABLE reloptions_test2 (i int) WITH (toast.not_existing_option = 42)
;
create trigger convslot_test_parent_update
    after update on convslot_test_parent
    referencing old table as old_rows new table as new_rows
    for each statement execute procedure convslot_trig4()
;
ANALYZE public.xy_1
;
CREATE SUBSCRIPTION tap_sub_C
	CONNECTION 'port=18730 host=/tmp/sK49zD6iE3 dbname=postgres application_name=tap_sub_C'
	PUBLICATION tap_pub_B
	WITH (two_phase = on)
;
DROP EVENT TRIGGER regress_event_trigger_report_dropped
;
ALTER TABLE test_add_column
	ADD IF NOT EXISTS c6 integer
;
-- ALTER TABLE ... RENAME on non-table relations
-- renaming indexes (FIXME: this should probably test the index's functionality)
ALTER INDEX IF EXISTS __onek_unique1 RENAME TO attmp_onek_unique1
;
CREATE TEMP TABLE tmp (xd INT, yd TEXT, zd INT)
;
ALTER OPERATOR <= (issn13, issn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
ALTER TABLE tbl REPLICA IDENTITY FULL
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=25183 host=/tmp/0JiZwqTFMc dbname=postgres' PUBLICATION mypub
;
CREATE DOMAIN ordered_named_pair AS named_pair CHECK((VALUE).i <= (VALUE).j)
;
DROP TABLE base_tbl4
;
ALTER TABLE pg_temp_9.pg_temp_26388_2 ADD COLUMN newdata pg_temp_9.pg_temp_26388
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
ALTER TEXT SEARCH CONFIGURATION hungarian ADD MAPPING
    FOR word, hword_part, hword
	WITH hungarian_stem
;
create table mutable(f1 int, f2 text)
;
ALTER TABLE pg_toast.pg_toast_31058 RENAME TO reind_con_toast
;
CREATE TABLE bit_table (a BIT(16), b BIT(16))
;
DROP RULE gtest_rule_ins ON gtest_rule
;
CREATE FUNCTION seg_over_left(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- More than one default partition is not allowed, so this should give error
CREATE TABLE fail_default_part PARTITION OF range_parted2 DEFAULT
;
ALTER TABLE tab5 REPLICA IDENTITY NOTHING
;
create or replace function ret_query2(lim int) returns setof record_type as $$
begin
    return query select fipshash(s.x::text), s.x, s.x > 0
                 from generate_series(-8, lim) s (x) where s.x % 2 = 0;
end;
$$ language plpgsql
;
CREATE TEMP TABLE pg_temp_11.pg_temp_25449_2 (tid pg_catalog.tid)
;
CREATE TABLE gencols (a int, gen1 int GENERATED ALWAYS AS (a * 2) STORED)
;
CREATE FUNCTION pg_get_logical_snapshot_meta(IN filename text,
    OUT magic int4,
    OUT checksum int8,
    OUT version int4
)
AS '$libdir/pg_logicalinspect', 'pg_get_logical_snapshot_meta'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE writetest (a int)
;
drop table p, q
;
ALTER MATERIALIZED VIEW testschema.amv SET TABLESPACE regress_tblspace
;
-- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
ALTER TABLE t DROP COLUMN id
;
-- should be temp
CREATE VIEW v5_temp AS
    SELECT t1.a AS t1_a, t2.a AS t2_a, t3.a AS t3_a
    FROM base_table t1, base_table2 t2, temp_table t3
    WHERE t1.id = t2.id and t2.id = t3.id
;
CREATE FUNCTION bt_page_items(IN page bytea,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text,
    OUT dead boolean,
    OUT htid tid,
    OUT tids tid[])
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE INDEX concur_temp_ind_2 ON concur_temp_tab_2(c2)
;
ALTER OPERATOR FAMILY alt_opf18 USING btree
  ADD FUNCTION 4 (int4, int2) btequalimage(oid)
;
CREATE TABLE tab_test (a int primary key)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- btree support

CREATE FUNCTION hstore_eq(hstore,hstore)
RETURNS boolean
AS '$libdir/hstore','hstore_eq'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = timestamp,
	RIGHTARG = timestamp,
	PROCEDURE = ts_dist,
	COMMUTATOR = '<->'
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_ginpostinglist()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_ginpostinglist' LANGUAGE C
;
----------
[ALTER_PROG_OBJ] ALTER AGGREGATE split_agg_probe.os_sum(ORDER BY integer) SET SCHEMA split_agg_probe_next
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest26"
    ADD CONSTRAINT "gtest26_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default RENAME TO testpub_foo
;
----------
[CREATE_TABLE] -- test using a volatile expression as partition bound
create table volatile_partbound_test (partkey timestamp) partition by range (partkey)
;
----------
[TRUNCATE_TABLE] TRUNCATE test_temp
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tt5 add column c int
;
----------
[CREATE_POLICY] create rule qqq as on insert to copydml_test do instead (delete from copydml_test; delete from copydml_test)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_default_acl
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkslotname" BEFORE INSERT ON "public"."phone" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotname"('PH')
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2 (i int) WITH (toast.not_existing_option = 42)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger convslot_test_parent_update
    after update on convslot_test_parent
    referencing old table as old_rows new table as new_rows
    for each statement execute procedure convslot_trig4()
;
----------
[ADMIN_TABLE] ANALYZE public.xy_1
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_C
	CONNECTION 'port=18730 host=/tmp/sK49zD6iE3 dbname=postgres application_name=tap_sub_C'
	PUBLICATION tap_pub_B
	WITH (two_phase = on)
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER regress_event_trigger_report_dropped
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD IF NOT EXISTS c6 integer
;
----------
[RENAME_INDEX] -- ALTER TABLE ... RENAME on non-table relations
-- renaming indexes (FIXME: this should probably test the index's functionality)
ALTER INDEX IF EXISTS __onek_unique1 RENAME TO attmp_onek_unique1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tmp (xd INT, yd TEXT, zd INT)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (issn13, issn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[ALTER_TABLE] ALTER TABLE tbl REPLICA IDENTITY FULL
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=25183 host=/tmp/0JiZwqTFMc dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_TYPE] CREATE DOMAIN ordered_named_pair AS named_pair CHECK((VALUE).i <= (VALUE).j)
;
----------
[DROP_TABLE] DROP TABLE base_tbl4
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_26388_2 ADD COLUMN newdata pg_temp_9.pg_temp_26388
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION hungarian ADD MAPPING
    FOR word, hword_part, hword
	WITH hungarian_stem
;
----------
[CREATE_TABLE] create table mutable(f1 int, f2 text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_31058 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE bit_table (a BIT(16), b BIT(16))
;
----------
[DROP_POLICY] DROP RULE gtest_rule_ins ON gtest_rule
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_over_left(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- More than one default partition is not allowed, so this should give error
CREATE TABLE fail_default_part PARTITION OF range_parted2 DEFAULT
;
----------
[ALTER_TABLE] ALTER TABLE tab5 REPLICA IDENTITY NOTHING
;
----------
[CREATE_PROG_OBJ] create or replace function ret_query2(lim int) returns setof record_type as $$
begin
    return query select fipshash(s.x::text), s.x, s.x > 0
                 from generate_series(-8, lim) s (x) where s.x % 2 = 0;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_25449_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE gencols (a int, gen1 int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_get_logical_snapshot_meta(IN filename text,
    OUT magic int4,
    OUT checksum int8,
    OUT version int4
)
AS '$libdir/pg_logicalinspect', 'pg_get_logical_snapshot_meta'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE writetest (a int)
;
----------
[DROP_TABLE] drop table p, q
;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW testschema.amv SET TABLESPACE regress_tblspace
;
----------
[CREATE_VIEW] -- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0 WITH NO DATA
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE t DROP COLUMN id
;
----------
[CREATE_VIEW(SELECT)] -- should be temp
CREATE VIEW v5_temp AS
    SELECT t1.a AS t1_a, t2.a AS t2_a, t3.a AS t3_a
    FROM base_table t1, base_table2 t2, temp_table t3
    WHERE t1.id = t2.id and t2.id = t3.id
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_page_items(IN page bytea,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text,
    OUT dead boolean,
    OUT htid tid,
    OUT tids tid[])
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX concur_temp_ind_2 ON concur_temp_tab_2(c2)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf18 USING btree
  ADD FUNCTION 4 (int4, int2) btequalimage(oid)
;
----------
[CREATE_TABLE] CREATE TABLE tab_test (a int primary key)
;
