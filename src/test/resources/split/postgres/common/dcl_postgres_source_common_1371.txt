SET ROLE regress_priv_user1
;
LOCK TABLE public.alpha_neg_p2 IN ACCESS SHARE MODE
;
LOCK TABLE "public"."btree_test" IN ACCESS SHARE MODE
;
EXECUTE dumpFunc('30363')
;
RESET enable_incremental_sort
;
-- failed (no role membership)
ALTER LANGUAGE alt_lang3 OWNER TO regress_alter_generic_user3
;
execute ab_q6(100)
;
--
-- Table inheritance and RLS policy
--
SET SESSION AUTHORIZATION regress_rls_alice
;
-- should succeed, FORCE_NULL and FORCE_NOT_NULL can be both specified
BEGIN
;
-- run test inside own schema and hide other partitions
set search_path to testpart
;
------------------------------------------------------------------------------------------
[SWITCH_ROLE] SET ROLE regress_priv_user1
;
----------
[SESSION_LOCK] LOCK TABLE public.alpha_neg_p2 IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."btree_test" IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpFunc('30363')
;
----------
[SESSION_SETTING_WRITE] RESET enable_incremental_sort
;
----------
[TRANSFER_PRIVILEGE] -- failed (no role membership)
ALTER LANGUAGE alt_lang3 OWNER TO regress_alter_generic_user3
;
----------
[UNSAFE] execute ab_q6(100)
;
----------
[SWITCH_USER] --
-- Table inheritance and RLS policy
--
SET SESSION AUTHORIZATION regress_rls_alice
;
----------
[TRANSACTION] -- should succeed, FORCE_NULL and FORCE_NOT_NULL can be both specified
BEGIN
;
----------
[SESSION_SETTING_WRITE] -- run test inside own schema and hide other partitions
set search_path to testpart
;
