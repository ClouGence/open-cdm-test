CREATE SUBSCRIPTION sub1 CONNECTION 'port=16255 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION pub1
;
COMMENT ON COLUMN dump_test.composite.f1
					   IS 'comment on column of type'
;
ALTER SUBSCRIPTION regress_testsub SET (slot_name = NONE)
;
CREATE TABLE FKTABLE ( ftest1 int, ftest2 int, ftest3 int, ftest4 int,  CONSTRAINT constrname3
			FOREIGN KEY(ftest1, ftest2, ftest3) REFERENCES PKTABLE)
;
create domain domainint4arr int4[1]
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '-', null '=-=')
;
VACUUM (ANALYZE) pg_catalog.pg_ts_dict
;
CREATE UNIQUE INDEX testpub_tbl5_b_key ON testpub_tbl5 (b, c)
;
ALTER TABLE test_tab ADD COLUMN f INT
;
CREATE TABLE async_p3 PARTITION OF async_pt FOR VALUES FROM (3000) TO (4000)
;
CREATE FUNCTION op_leak(record, record) RETURNS bool
    AS 'BEGIN RAISE NOTICE ''op_leak => %, %'', $1, $2; RETURN $1 < $2; END'
    LANGUAGE plpgsql
;
ALTER TABLE parent RENAME COLUMN b TO c
;
ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (ismn13, ean13),
	OPERATOR 1  < (ismn13, ismn),
	OPERATOR 2  <= (ismn13, ean13),
	OPERATOR 2  <= (ismn13, ismn),
	OPERATOR 3  = (ismn13, ean13),
	OPERATOR 3  = (ismn13, ismn),
	OPERATOR 4  >= (ismn13, ean13),
	OPERATOR 4  >= (ismn13, ismn),
	OPERATOR 5  > (ismn13, ean13),
	OPERATOR 5  > (ismn13, ismn),
	FUNCTION 1  btismn13cmp(ismn13, ean13),
	FUNCTION 1  btismn13cmp(ismn13, ismn)
;
DROP CAST (mood AS json)
;
--
-- Extended statistics on sb_2 (x, y, z) improve a bucket size estimation,
-- and the optimizer may choose hash join.
--
CREATE TABLE sb_1 AS
  SELECT gs % 10 AS x, gs % 10 AS y, gs % 10 AS z
  FROM generate_series(1, 1e4) AS gs
;
ALTER OPERATOR FAMILY gist_cidr_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE TABLE testpub_parted1 (LIKE testpub_parted)
;
drop trigger trg1 on trigpart1
;
CREATE TABLE "public"."gtestx" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 22)) STORED,
    "x" integer
)
;
create view domview as select cast(col1 as dom) from domtab
;
create unique index hash_f8_index_2 on hash_f8_heap((seqno + 1), random)
;
create table mc2p1 partition of mc2p for values from (1, minvalue) to (1, 1)
;
ALTER TABLE part_6 DROP c
;
CREATE TABLE ctas_nodata_3 (ii) AS SELECT i, j FROM ctas_base
;
CREATE SCHEMA "fkpart5"
;
ALTER TABLE child_tbl SET (autovacuum_enabled = 'false')
;
ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk5_pkey"
;
CREATE TEMP TABLE node (
    nid SERIAL,
    vid integer NOT NULL default '0',
    type varchar(32) NOT NULL default '',
    title varchar(128) NOT NULL default '',
    uid integer NOT NULL default '0',
    status integer NOT NULL default '1',
    created integer NOT NULL default '0',
    
    PRIMARY KEY (nid, vid)
)
;
--
-- Test that AT TIME ZONE isn't misoptimized when using an index (bug #14504)
--
create temp table tmptz (f1 timestamptz primary key)
;
create trigger mmm after insert on parted_trig_1_1 for each row execute procedure trigger_notice()
;
CREATE TABLE byteatmp (a bytea)
;
alter table anothertab alter column atcol1 type boolean
        using case when atcol1 % 2 = 0 then true else false end
;
ANALYZE public.gtest20b
;
--
-- CREATE_TABLE
--

-- Datatypes
CREATE TABLE datatype_table (
    id             SERIAL,
    id_big         BIGSERIAL,
    is_small       SMALLSERIAL,
    v_bytea        BYTEA,
    v_smallint     SMALLINT,
    v_int          INT,
    v_bigint       BIGINT,
    v_char         CHAR(1),
    v_varchar      VARCHAR(10),
    v_text         TEXT,
    v_bool         BOOLEAN,
    v_inet         INET,
    v_cidr         CIDR,
    v_macaddr      MACADDR,
    v_numeric      NUMERIC(1,0),
    v_real         REAL,
    v_float        FLOAT(1),
    v_float8       FLOAT8,
    v_money        MONEY,
    v_tsquery      TSQUERY,
    v_tsvector     TSVECTOR,
    v_date         DATE,
    v_time         TIME,
    v_time_tz      TIME WITH TIME ZONE,
    v_timestamp    TIMESTAMP,
    v_timestamp_tz TIMESTAMP WITH TIME ZONE,
    v_interval     INTERVAL,
    v_bit          BIT,
    v_bit4         BIT(4),
    v_varbit       VARBIT,
    v_varbit4      VARBIT(4),
    v_box          BOX,
    v_circle       CIRCLE,
    v_lseg         LSEG,
    v_path         PATH,
    v_point        POINT,
    v_polygon      POLYGON,
    v_json         JSON,
    v_xml          XML,
    v_uuid         UUID,
    v_txid_snapshot txid_snapshot,
    v_enum         ENUM_TEST,
    v_postal_code  japanese_postal_code,
    v_int2range    int2range,
    PRIMARY KEY (id),
    UNIQUE (id_big)
)
;
ALTER DOMAIN things ADD CONSTRAINT meow CHECK (VALUE < 11) NOT VALID
;
CREATE TABLE gtesty (x int, b int GENERATED ALWAYS AS (x * 22) STORED)
;
CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
  SELECT 1 / 0 WITH NO DATA
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
CREATE TABLE "regress_indexing"."pk1" (
    "a" integer NOT NULL
)
;
create TEMP TABLE test_jsonb_subscript (
       id int,
       test_json jsonb
)
;
create temp table tt_log(f1 int, data text)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER TABLE IF EXISTS tt8 ADD COLUMN f int
;
CREATE UNIQUE INDEX u0rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  WHERE s.index_row_if(y)
;
--
-- partitioned FK referenced deletes SET DEFAULT
--

DROP TABLE temporal_partitioned_fk_rng2rng
;
ALTER TABLE attmp3 validate constraint attmpconstr
;
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, c, d, 0 AS e FROM viewtest_tbl
;
create function dfunc(a varchar = 'def a', out _a varchar, c numeric = NULL, out _c numeric)
returns record as $$
  select $1, $2;
$$ language sql
;
DROP TABLESPACE ts1
;
CREATE FUNCTION setenv(text, text)
   RETURNS void
   AS '/src/contrib/dblink/../../src/test/regress/regress.so', 'regress_setenv'
   LANGUAGE C STRICT
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=16255 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION pub1
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN dump_test.composite.f1
					   IS 'comment on column of type'
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (slot_name = NONE)
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE ( ftest1 int, ftest2 int, ftest3 int, ftest4 int,  CONSTRAINT constrname3
			FOREIGN KEY(ftest1, ftest2, ftest3) REFERENCES PKTABLE)
;
----------
[CREATE_TYPE] create domain domainint4arr int4[1]
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '-', null '=-=')
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_ts_dict
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX testpub_tbl5_b_key ON testpub_tbl5 (b, c)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_tab ADD COLUMN f INT
;
----------
[CREATE_TABLE] CREATE TABLE async_p3 PARTITION OF async_pt FOR VALUES FROM (3000) TO (4000)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION op_leak(record, record) RETURNS bool
    AS 'BEGIN RAISE NOTICE ''op_leak => %, %'', $1, $2; RETURN $1 < $2; END'
    LANGUAGE plpgsql
;
----------
[RENAME_COLUMN] ALTER TABLE parent RENAME COLUMN b TO c
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (ismn13, ean13),
	OPERATOR 1  < (ismn13, ismn),
	OPERATOR 2  <= (ismn13, ean13),
	OPERATOR 2  <= (ismn13, ismn),
	OPERATOR 3  = (ismn13, ean13),
	OPERATOR 3  = (ismn13, ismn),
	OPERATOR 4  >= (ismn13, ean13),
	OPERATOR 4  >= (ismn13, ismn),
	OPERATOR 5  > (ismn13, ean13),
	OPERATOR 5  > (ismn13, ismn),
	FUNCTION 1  btismn13cmp(ismn13, ean13),
	FUNCTION 1  btismn13cmp(ismn13, ismn)
;
----------
[DROP_PROG_OBJ] DROP CAST (mood AS json)
;
----------
[CREATE_TABLE|SELECT] --
-- Extended statistics on sb_2 (x, y, z) improve a bucket size estimation,
-- and the optimizer may choose hash join.
--
CREATE TABLE sb_1 AS
  SELECT gs % 10 AS x, gs % 10 AS y, gs % 10 AS z
  FROM generate_series(1, 1e4) AS gs
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_cidr_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TABLE] CREATE TABLE testpub_parted1 (LIKE testpub_parted)
;
----------
[DROP_TRIGGER] drop trigger trg1 on trigpart1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtestx" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 22)) STORED,
    "x" integer
)
;
----------
[CREATE_VIEW(SELECT)] create view domview as select cast(col1 as dom) from domtab
;
----------
[ADD_INDEX] create unique index hash_f8_index_2 on hash_f8_heap((seqno + 1), random)
;
----------
[CREATE_TABLE] create table mc2p1 partition of mc2p for values from (1, minvalue) to (1, 1)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE part_6 DROP c
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_nodata_3 (ii) AS SELECT i, j FROM ctas_base
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "fkpart5"
;
----------
[ALTER_TABLE] ALTER TABLE child_tbl SET (autovacuum_enabled = 'false')
;
----------
[ALTER_INDEX] ALTER INDEX "regress_indexing"."pk_pkey" ATTACH PARTITION "regress_indexing"."pk5_pkey"
;
----------
[CREATE_TABLE] CREATE TEMP TABLE node (
    nid SERIAL,
    vid integer NOT NULL default '0',
    type varchar(32) NOT NULL default '',
    title varchar(128) NOT NULL default '',
    uid integer NOT NULL default '0',
    status integer NOT NULL default '1',
    created integer NOT NULL default '0',
    
    PRIMARY KEY (nid, vid)
)
;
----------
[CREATE_TABLE] --
-- Test that AT TIME ZONE isn't misoptimized when using an index (bug #14504)
--
create temp table tmptz (f1 timestamptz primary key)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger mmm after insert on parted_trig_1_1 for each row execute procedure trigger_notice()
;
----------
[CREATE_TABLE] CREATE TABLE byteatmp (a bytea)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table anothertab alter column atcol1 type boolean
        using case when atcol1 % 2 = 0 then true else false end
;
----------
[ADMIN_TABLE] ANALYZE public.gtest20b
;
----------
[CREATE_TABLE] --
-- CREATE_TABLE
--

-- Datatypes
CREATE TABLE datatype_table (
    id             SERIAL,
    id_big         BIGSERIAL,
    is_small       SMALLSERIAL,
    v_bytea        BYTEA,
    v_smallint     SMALLINT,
    v_int          INT,
    v_bigint       BIGINT,
    v_char         CHAR(1),
    v_varchar      VARCHAR(10),
    v_text         TEXT,
    v_bool         BOOLEAN,
    v_inet         INET,
    v_cidr         CIDR,
    v_macaddr      MACADDR,
    v_numeric      NUMERIC(1,0),
    v_real         REAL,
    v_float        FLOAT(1),
    v_float8       FLOAT8,
    v_money        MONEY,
    v_tsquery      TSQUERY,
    v_tsvector     TSVECTOR,
    v_date         DATE,
    v_time         TIME,
    v_time_tz      TIME WITH TIME ZONE,
    v_timestamp    TIMESTAMP,
    v_timestamp_tz TIMESTAMP WITH TIME ZONE,
    v_interval     INTERVAL,
    v_bit          BIT,
    v_bit4         BIT(4),
    v_varbit       VARBIT,
    v_varbit4      VARBIT(4),
    v_box          BOX,
    v_circle       CIRCLE,
    v_lseg         LSEG,
    v_path         PATH,
    v_point        POINT,
    v_polygon      POLYGON,
    v_json         JSON,
    v_xml          XML,
    v_uuid         UUID,
    v_txid_snapshot txid_snapshot,
    v_enum         ENUM_TEST,
    v_postal_code  japanese_postal_code,
    v_int2range    int2range,
    PRIMARY KEY (id),
    UNIQUE (id_big)
)
;
----------
[ALTER_TYPE] ALTER DOMAIN things ADD CONSTRAINT meow CHECK (VALUE < 11) NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE gtesty (x int, b int GENERATED ALWAYS AS (x * 22) STORED)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
  SELECT 1 / 0 WITH NO DATA
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk1" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TABLE] create TEMP TABLE test_jsonb_subscript (
       id int,
       test_json jsonb
)
;
----------
[CREATE_TABLE] create temp table tt_log(f1 int, data text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE IF EXISTS tt8 ADD COLUMN f int
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX u0rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  WHERE s.index_row_if(y)
;
----------
[DROP_TABLE] --
-- partitioned FK referenced deletes SET DEFAULT
--

DROP TABLE temporal_partitioned_fk_rng2rng
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE attmp3 validate constraint attmpconstr
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, c, d, 0 AS e FROM viewtest_tbl
;
----------
[CREATE_PROG_OBJ] create function dfunc(a varchar = 'def a', out _a varchar, c numeric = NULL, out _c numeric)
returns record as $$
  select $1, $2;
$$ language sql
;
----------
[DROP_TABLESPACE] DROP TABLESPACE ts1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION setenv(text, text)
   RETURNS void
   AS '/src/contrib/dblink/../../src/test/regress/regress.so', 'regress_setenv'
   LANGUAGE C STRICT
;
