CREATE FUNCTION isnle(ean13, isbn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE tab_fk_ref (id int PRIMARY KEY, junk text, bid int REFERENCES tab_fk (bid))
;
CREATE TABLE lt1_part1
  PARTITION OF lt1 FOR VALUES FROM (0) TO (1000)
  PARTITION BY RANGE (a)
;
-- Ensure there's tables with toast datums.  To do so, we dynamically
-- create a function returning a large textblob.  We want tables of
-- different kinds: mapped catalog table, unmapped catalog table,
-- shared catalog table and usertable.
CREATE FUNCTION exec(text) returns void language plpgsql volatile
  AS $f$
    BEGIN
      EXECUTE $1;
    END;
$f$
;
--
-- Domains over enums
--
CREATE DOMAIN rgb AS rainbow CHECK (VALUE IN ('red', 'green', 'blue'))
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_ts_config
;
/*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
-- 5.30 ELEMENT_TYPES view appears later.


/*
 * 5.31
 * ENABLED_ROLES view
 */

CREATE VIEW enabled_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS role_name
    FROM pg_authid a
    WHERE pg_has_role(a.oid, 'USAGE')
;
--
-- pgp_pub_decrypt(data, key, psw)
--
CREATE FUNCTION pgp_pub_decrypt(bytea, bytea, text)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE concur_heap (f1 text, f2 text)
;
create function c_sillyaddone(int) returns int language plpgsql as
$$ declare r c_mutable; begin r.f1 := $1; return r.f1 + 1; end $$
;
-- Test comments
COMMENT ON TYPE bad IS 'bad comment'
;
VACUUM FLOAT8_TBL
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
CREATE SCHEMA split_event_helper
;
ALTER TABLE attmp3 add constraint attmpconstr foreign key(c) references attmp2 match full
;
CREATE TABLE test_timetz (
	i timetz
)
;
--
-- alter object set schema
--

create schema alter1
;
-- Modify fillfactor in existing index
alter index spgist_point_idx set (fillfactor = 90)
;
-- This is used in triggers
CREATE FUNCTION lo_manage()
RETURNS pg_catalog.trigger
AS '$libdir/lo'
LANGUAGE C
;
--
-- queries with locking clauses
--
CREATE TABLE pgss_a (id integer PRIMARY KEY)
;
CREATE FUNCTION base_fn_out(base_type) RETURNS cstring AS 'boolout'
    LANGUAGE internal IMMUTABLE STRICT
;
CREATE TABLE "public"."bprime" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
ANALYZE pg_catalog.pg_user_mapping
;
VACUUM (FREEZE) pg_catalog.pg_rewrite
;
CREATE TABLE drop_tbl (d text)
;
create text search configuration alter1.cfg(parser = alter1.prs)
;
CREATE SUBSCRIPTION mysub1 CONNECTION 'port=26512 host=/tmp/gUd_xhlenG dbname=postgres' PUBLICATION mypub, non_existent_pub
;
CREATE FUNCTION dummy_seclabel_dummy()
   RETURNS pg_catalog.void
       AS '$libdir/dummy_seclabel' LANGUAGE C
;
CREATE TABLE "public"."quad_box_tbl_ord_seq1" (
    "n" bigint,
    "dist" double precision,
    "id" integer
)
;
CREATE TYPE test_type3 AS (a int)
;
alter table defpart_attach_test_d add check (a > 1)
;
CREATE TABLE hjtest_matchbits_t1(id int)
;
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box,
				UNIQUE(c1,c2) INCLUDE(c3,c4))
;
VACUUM (FREEZE) pg_catalog.pg_depend
;
DROP PUBLICATION tap_pub_sch
;
ANALYZE public.money_data
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "host" WITH "simple"
;
/* contrib/btree_gin/btree_gin--1.0--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- macaddr8 datatype support new in 10.0.
CREATE FUNCTION gin_extract_value_macaddr8(macaddr8, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
create view uv_ptv_wco as select * from uv_pt where a = 0 with check option
;
ALTER MATERIALIZED VIEW split_view_probe.base_mv_renamed SET SCHEMA split_view_next
;
CREATE FUNCTION pgp_pub_decrypt_bytea(bytea, bytea, text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_bytea'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION PUBLIC
;
alter table idxpart attach partition idxpart2 for values from ('bbb') to ('ccc')
;
DROP COLLATION split_collation_probe.c_locale CASCADE
;
DROP AGGREGATE test_aggregate_exists(int)
;
DROP TABLESPACE regress_create_idx_tblspace
;
ANALYZE public.mlparted_def1
;
CREATE TABLE selinto_schema.tbl_nodata1 (a) AS
  SELECT generate_series(1,3) WITH NO DATA
;
-- fail
drop trigger trg1 on trigpart3
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ean13, isbn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE tab_fk_ref (id int PRIMARY KEY, junk text, bid int REFERENCES tab_fk (bid))
;
----------
[CREATE_TABLE] CREATE TABLE lt1_part1
  PARTITION OF lt1 FOR VALUES FROM (0) TO (1000)
  PARTITION BY RANGE (a)
;
----------
[CREATE_PROG_OBJ] -- Ensure there's tables with toast datums.  To do so, we dynamically
-- create a function returning a large textblob.  We want tables of
-- different kinds: mapped catalog table, unmapped catalog table,
-- shared catalog table and usertable.
CREATE FUNCTION exec(text) returns void language plpgsql volatile
  AS $f$
    BEGIN
      EXECUTE $1;
    END;
$f$
;
----------
[CREATE_TYPE] --
-- Domains over enums
--
CREATE DOMAIN rgb AS rainbow CHECK (VALUE IN ('red', 'green', 'blue'))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_ts_config
;
----------
[CREATE_POLICY] /*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- 5.30 ELEMENT_TYPES view appears later.


/*
 * 5.31
 * ENABLED_ROLES view
 */

CREATE VIEW enabled_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS role_name
    FROM pg_authid a
    WHERE pg_has_role(a.oid, 'USAGE')
;
----------
[CREATE_PROG_OBJ] --
-- pgp_pub_decrypt(data, key, psw)
--
CREATE FUNCTION pgp_pub_decrypt(bytea, bytea, text)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE concur_heap (f1 text, f2 text)
;
----------
[CREATE_PROG_OBJ] create function c_sillyaddone(int) returns int language plpgsql as
$$ declare r c_mutable; begin r.f1 := $1; return r.f1 + 1; end $$
;
----------
[COMMENT_TYPE] -- Test comments
COMMENT ON TYPE bad IS 'bad comment'
;
----------
[ADMIN_TABLE] VACUUM FLOAT8_TBL
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_event_helper
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp3 add constraint attmpconstr foreign key(c) references attmp2 match full
;
----------
[CREATE_TABLE] CREATE TABLE test_timetz (
	i timetz
)
;
----------
[CREATE_SCHEMA] --
-- alter object set schema
--

create schema alter1
;
----------
[ALTER_INDEX] -- Modify fillfactor in existing index
alter index spgist_point_idx set (fillfactor = 90)
;
----------
[CREATE_PROG_OBJ] -- This is used in triggers
CREATE FUNCTION lo_manage()
RETURNS pg_catalog.trigger
AS '$libdir/lo'
LANGUAGE C
;
----------
[CREATE_TABLE] --
-- queries with locking clauses
--
CREATE TABLE pgss_a (id integer PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION base_fn_out(base_type) RETURNS cstring AS 'boolout'
    LANGUAGE internal IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."bprime" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_user_mapping
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_rewrite
;
----------
[CREATE_TABLE] CREATE TABLE drop_tbl (d text)
;
----------
[CREATE_POLICY] create text search configuration alter1.cfg(parser = alter1.prs)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub1 CONNECTION 'port=26512 host=/tmp/gUd_xhlenG dbname=postgres' PUBLICATION mypub, non_existent_pub
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dummy_seclabel_dummy()
   RETURNS pg_catalog.void
       AS '$libdir/dummy_seclabel' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE "public"."quad_box_tbl_ord_seq1" (
    "n" bigint,
    "dist" double precision,
    "id" integer
)
;
----------
[CREATE_TYPE] CREATE TYPE test_type3 AS (a int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table defpart_attach_test_d add check (a > 1)
;
----------
[CREATE_TABLE] CREATE TABLE hjtest_matchbits_t1(id int)
;
----------
[CREATE_TABLE] CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box,
				UNIQUE(c1,c2) INCLUDE(c3,c4))
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_depend
;
----------
[DROP_PUB_SUB] DROP PUBLICATION tap_pub_sch
;
----------
[ADMIN_TABLE] ANALYZE public.money_data
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "host" WITH "simple"
;
----------
[CREATE_PROG_OBJ] /* contrib/btree_gin/btree_gin--1.0--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- macaddr8 datatype support new in 10.0.
CREATE FUNCTION gin_extract_value_macaddr8(macaddr8, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_VIEW(SELECT)] create view uv_ptv_wco as select * from uv_pt where a = 0 with check option
;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW split_view_probe.base_mv_renamed SET SCHEMA split_view_next
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgp_pub_decrypt_bytea(bytea, bytea, text, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_bytea'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION PUBLIC
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart2 for values from ('bbb') to ('ccc')
;
----------
[DROP_POLICY] DROP COLLATION split_collation_probe.c_locale CASCADE
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE test_aggregate_exists(int)
;
----------
[DROP_TABLESPACE] DROP TABLESPACE regress_create_idx_tblspace
;
----------
[ADMIN_TABLE] ANALYZE public.mlparted_def1
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE selinto_schema.tbl_nodata1 (a) AS
  SELECT generate_series(1,3) WITH NO DATA
;
----------
[DROP_TRIGGER] -- fail
drop trigger trg1 on trigpart3
;
