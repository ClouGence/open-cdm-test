LOCK TABLE "public"."part33_self_fk" IN ACCESS SHARE MODE
;
ALTER PROCEDURE "public"."ptest11"(OUT "a" integer, VARIADIC "b" integer[]) OWNER TO zyc
;
ALTER SYSTEM SET log_connections = receipt,authorization,setup_durations
;
EXECUTE dumpAgg('35514')
;
--
-- Test production of per-worker data
--
-- Unfortunately, because we don't know how many worker processes we'll
-- actually get (maybe none at all), we can't examine the "Workers" output
-- in any detail.  We can check that it parses correctly as JSON, and then
-- remove it from the displayed results.

-- Serializable isolation would disable parallel query, so explicitly use an
-- arbitrary other level.
begin isolation level repeatable read
;
SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY
;
EXECUTE dumpFunc('28817')
;
EXECUTE dumpAgg('20305')
;
EXECUTE dumpTableAttach('40452')
;
RESET TIME ZONE
;
------------------------------------------------------------------------------------------
[SESSION_LOCK] LOCK TABLE "public"."part33_self_fk" IN ACCESS SHARE MODE
;
----------
[TRANSFER_PRIVILEGE] ALTER PROCEDURE "public"."ptest11"(OUT "a" integer, VARIADIC "b" integer[]) OWNER TO zyc
;
----------
[SYSTEM_SETTING_WRITE] ALTER SYSTEM SET log_connections = receipt,authorization,setup_durations
;
----------
[UNSAFE] EXECUTE dumpAgg('35514')
;
----------
[TRANSACTION] --
-- Test production of per-worker data
--
-- Unfortunately, because we don't know how many worker processes we'll
-- actually get (maybe none at all), we can't examine the "Workers" output
-- in any detail.  We can check that it parses correctly as JSON, and then
-- remove it from the displayed results.

-- Serializable isolation would disable parallel query, so explicitly use an
-- arbitrary other level.
begin isolation level repeatable read
;
----------
[TRANSACTION|SESSION_SETTING_WRITE] SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY
;
----------
[UNSAFE] EXECUTE dumpFunc('28817')
;
----------
[UNSAFE] EXECUTE dumpAgg('20305')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('40452')
;
----------
[SESSION_SETTING_WRITE] RESET TIME ZONE
;
