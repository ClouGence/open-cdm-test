CREATE VIEW s5.t2_view AS (
				SELECT i*2, t FROM s5.t2
			)
;
drop extension pg_surgery
;
create table gen_part_1
  partition of gen_part for values in (1)
  partition by range (key2)
;
CREATE FUNCTION make_record(n int)
  RETURNS RECORD LANGUAGE plpgsql PARALLEL SAFE AS
$$
BEGIN
  RETURN CASE n
           WHEN 1 THEN ROW(1)
           WHEN 2 THEN ROW(1, 2)
           WHEN 3 THEN ROW(1, 2, 3)
           WHEN 4 THEN ROW(1, 2, 3, 4)
           ELSE ROW(1, 2, 3, 4, 5)
         END;
END;
$$
;
CREATE TABLESPACE regress_tblspacewith LOCATION '' WITH (some_nonexistent_parameter = true)
;
CREATE TABLE test (i int PRIMARY KEY, t text)
;
ALTER TABLE pg_toast.pg_toast_30433 RENAME TO reind_con_toast
;
-- fail, dup name
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key CHECK(c > 5)
;
ALTER TABLE ONLY public.tplain
    ADD CONSTRAINT tplain_data_key UNIQUE (data)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_statistic
;
create table rtest_view2 (a int4)
;
CREATE TABLE tab_mixed (d text default 'local', c numeric, b text, a int primary key)
;
create domain ddtest1d as ddtest1[]
;
CREATE DOMAIN "public"."pos_int" AS integer NOT NULL
	CONSTRAINT "pos_int_check" CHECK ((VALUE > 0))
;
CREATE FUNCTION dblink_open (text, text, boolean)
RETURNS text
AS '$libdir/dblink','dblink_open'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE "Bar".baz(id int)
;
ALTER INDEX pg_toast.pg_toast_30795_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION test_pglz_decompress(bytea, int4, bool)
  RETURNS bytea
  AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
create table part_40_inf_null partition of part_40_inf for values in (null)
;
CREATE SEQUENCE tststats.s
;
-- min will be adjusted
ALTER SEQUENCE sequence_test13 AS int
;
-- error, wrong data type

-- for later
ALTER TABLE itest4 ALTER COLUMN b SET DEFAULT ''
;
ANALYZE strtest
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=22410 host=/tmp/F8upVywyLN dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
ALTER TABLE m ADD UNIQUE (k)
;
ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.7........"
;
create table idxpart1 partition of idxpart for values from (0, 0) to (10, 10)
;
ALTER TABLE comment_test ALTER COLUMN positive_col SET DATA TYPE bigint
;
ALTER TABLE gtest10 DROP COLUMN b
;
VACUUM INT4_TBL
;
--
-- brin_page_items()
--
CREATE FUNCTION brin_page_items(IN page bytea, IN index_oid regclass,
	OUT itemoffset int,
	OUT blknum int,
	OUT attnum int,
	OUT allnulls bool,
	OUT hasnulls bool,
	OUT placeholder bool,
	OUT value text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'brin_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE "public"."notnull_tbl5_child" (
    "a" integer CONSTRAINT "ann" NOT NULL,
    "b" integer CONSTRAINT "bnn" NOT NULL
)
;
-- Extra event to force the use of a snapshot.
CREATE FUNCTION reindex_end_command_snap() RETURNS EVENT_TRIGGER
    AS $$ BEGIN PERFORM 1; END $$ LANGUAGE plpgsql
;
DROP TABLE bmscantest
;
CREATE FUNCTION trig_report() RETURNS TRIGGER LANGUAGE plpgsql AS $body$
    DECLARE
	r_new text;
	r_old text;
	r_ret record;
    BEGIN
	-- In older releases it wasn't allowed to reference OLD/NEW
        -- when not applicable for TG_WHEN
	IF TG_OP = 'INSERT' THEN
	    r_old = NULL;
	    r_new = NEW;
	    r_ret = NEW;
	ELSIF TG_OP = 'DELETE' THEN
	    r_old = OLD;
	    r_new = NULL;
	    r_ret = OLD;
	ELSIF TG_OP = 'UPDATE' THEN
	    r_old = OLD;
	    r_new = NEW;
	    r_ret = NEW;
	END IF;

	IF TG_WHEN = 'AFTER' THEN
	   r_ret = NULL;
	END IF;

        RAISE NOTICE 'trigger: name %; when: %; lev: %s; op: %; old: % new: %',
            TG_NAME, TG_WHEN, TG_LEVEL, TG_OP, r_old, r_new;

	RETURN r_ret;
    END;
    $body$
;
--
-- More complicated constructs
--

--
-- Multiway full join
--

CREATE TABLE t1 (name TEXT, n INTEGER)
;
ALTER TEXT SEARCH CONFIGURATION hungarian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH hungarian_stem
;
create temp table prtx2 (a integer, b integer, c integer)
  partition by range (a)
;
create or replace function simplesql() returns int language sql
as $$select 2 + 2$$
;
DROP TABLE pg_temp_11.pg_temp_26417_2, pg_temp_11.pg_temp_26417
;
CREATE TABLE fail_def_part (LIKE part_1 INCLUDING CONSTRAINTS)
;
ALTER TABLE pitest3
            ALTER COLUMN f3 SET NOT NULL,
            ALTER COLUMN f3 ADD GENERATED ALWAYS AS IDENTITY (START WITH 3)
;
ANALYZE public.rtest_view4
;
CREATE TEXT SEARCH DICTIONARY tsdict_case
(
	Template = ispell,
	"DictFile" = ispell_sample,
	"AffFile" = ispell_sample
)
;
CREATE PROCEDURE test_proc6(a int, INOUT b int, INOUT c int)
LANGUAGE plpgsql
AS $$
BEGIN
    b := b * a;
    c := c * a;
END;
$$
;
-- error cases:
create table notnull_tbl_fail (a serial constraint foo not null constraint bar not null)
;
-- Test enable/disable triggers

create table trigtest (i serial primary key)
;
DROP FUNCTION btree_test_func()
;
COMMENT ON TABLE attmp IS NULL
;
ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser RENAME TO cfg_parser_renamed
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW s5.t2_view AS (
				SELECT i*2, t FROM s5.t2
			)
;
----------
[DROP_LIBRARY] drop extension pg_surgery
;
----------
[CREATE_TABLE] create table gen_part_1
  partition of gen_part for values in (1)
  partition by range (key2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_record(n int)
  RETURNS RECORD LANGUAGE plpgsql PARALLEL SAFE AS
$$
BEGIN
  RETURN CASE n
           WHEN 1 THEN ROW(1)
           WHEN 2 THEN ROW(1, 2)
           WHEN 3 THEN ROW(1, 2, 3)
           WHEN 4 THEN ROW(1, 2, 3, 4)
           ELSE ROW(1, 2, 3, 4, 5)
         END;
END;
$$
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspacewith LOCATION '' WITH (some_nonexistent_parameter = true)
;
----------
[CREATE_TABLE] CREATE TABLE test (i int PRIMARY KEY, t text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30433 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- fail, dup name
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key CHECK(c > 5)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY public.tplain
    ADD CONSTRAINT tplain_data_key UNIQUE (data)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_statistic
;
----------
[CREATE_TABLE] create table rtest_view2 (a int4)
;
----------
[CREATE_TABLE] CREATE TABLE tab_mixed (d text default 'local', c numeric, b text, a int primary key)
;
----------
[CREATE_TYPE] create domain ddtest1d as ddtest1[]
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."pos_int" AS integer NOT NULL
	CONSTRAINT "pos_int_check" CHECK ((VALUE > 0))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_open (text, text, boolean)
RETURNS text
AS '$libdir/dblink','dblink_open'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE "Bar".baz(id int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30795_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_pglz_decompress(bytea, int4, bool)
  RETURNS bytea
  AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
----------
[CREATE_TABLE] create table part_40_inf_null partition of part_40_inf for values in (null)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE tststats.s
;
----------
[ALTER_SEQUENCE] -- min will be adjusted
ALTER SEQUENCE sequence_test13 AS int
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- error, wrong data type

-- for later
ALTER TABLE itest4 ALTER COLUMN b SET DEFAULT ''
;
----------
[ADMIN_TABLE] ANALYZE strtest
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=22410 host=/tmp/F8upVywyLN dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE m ADD UNIQUE (k)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted11" DROP COLUMN "........pg.dropped.7........"
;
----------
[CREATE_TABLE] create table idxpart1 partition of idxpart for values from (0, 0) to (10, 10)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN positive_col SET DATA TYPE bigint
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE gtest10 DROP COLUMN b
;
----------
[ADMIN_TABLE] VACUUM INT4_TBL
;
----------
[CREATE_PROG_OBJ] --
-- brin_page_items()
--
CREATE FUNCTION brin_page_items(IN page bytea, IN index_oid regclass,
	OUT itemoffset int,
	OUT blknum int,
	OUT attnum int,
	OUT allnulls bool,
	OUT hasnulls bool,
	OUT placeholder bool,
	OUT value text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'brin_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl5_child" (
    "a" integer CONSTRAINT "ann" NOT NULL,
    "b" integer CONSTRAINT "bnn" NOT NULL
)
;
----------
[CREATE_PROG_OBJ] -- Extra event to force the use of a snapshot.
CREATE FUNCTION reindex_end_command_snap() RETURNS EVENT_TRIGGER
    AS $$ BEGIN PERFORM 1; END $$ LANGUAGE plpgsql
;
----------
[DROP_TABLE] DROP TABLE bmscantest
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trig_report() RETURNS TRIGGER LANGUAGE plpgsql AS $body$
    DECLARE
	r_new text;
	r_old text;
	r_ret record;
    BEGIN
	-- In older releases it wasn't allowed to reference OLD/NEW
        -- when not applicable for TG_WHEN
	IF TG_OP = 'INSERT' THEN
	    r_old = NULL;
	    r_new = NEW;
	    r_ret = NEW;
	ELSIF TG_OP = 'DELETE' THEN
	    r_old = OLD;
	    r_new = NULL;
	    r_ret = OLD;
	ELSIF TG_OP = 'UPDATE' THEN
	    r_old = OLD;
	    r_new = NEW;
	    r_ret = NEW;
	END IF;

	IF TG_WHEN = 'AFTER' THEN
	   r_ret = NULL;
	END IF;

        RAISE NOTICE 'trigger: name %; when: %; lev: %s; op: %; old: % new: %',
            TG_NAME, TG_WHEN, TG_LEVEL, TG_OP, r_old, r_new;

	RETURN r_ret;
    END;
    $body$
;
----------
[CREATE_TABLE] --
-- More complicated constructs
--

--
-- Multiway full join
--

CREATE TABLE t1 (name TEXT, n INTEGER)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION hungarian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH hungarian_stem
;
----------
[CREATE_TABLE] create temp table prtx2 (a integer, b integer, c integer)
  partition by range (a)
;
----------
[CREATE_PROG_OBJ] create or replace function simplesql() returns int language sql
as $$select 2 + 2$$
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_26417_2, pg_temp_11.pg_temp_26417
;
----------
[CREATE_TABLE] CREATE TABLE fail_def_part (LIKE part_1 INCLUDING CONSTRAINTS)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest3
            ALTER COLUMN f3 SET NOT NULL,
            ALTER COLUMN f3 ADD GENERATED ALWAYS AS IDENTITY (START WITH 3)
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_view4
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY tsdict_case
(
	Template = ispell,
	"DictFile" = ispell_sample,
	"AffFile" = ispell_sample
)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc6(a int, INOUT b int, INOUT c int)
LANGUAGE plpgsql
AS $$
BEGIN
    b := b * a;
    c := c * a;
END;
$$
;
----------
[CREATE_TABLE] -- error cases:
create table notnull_tbl_fail (a serial constraint foo not null constraint bar not null)
;
----------
[CREATE_TABLE] -- Test enable/disable triggers

create table trigtest (i serial primary key)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION btree_test_func()
;
----------
[COMMENT_TABLE] COMMENT ON TABLE attmp IS NULL
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser RENAME TO cfg_parser_renamed
;
