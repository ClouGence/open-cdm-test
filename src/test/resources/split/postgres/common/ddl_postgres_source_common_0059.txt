CREATE TABLE "public"."test_tbl3" (
    "c" "public"."test_type3"
)
;
CREATE TEMP TABLE tmp_pg_description ( 	objoid oid, 	classname name, 	objsubid int4, 	description text)
;
-- verify dependency handling during ALTER TABLE DETACH PARTITION
create table idxpart (a int) partition by range (a)
;
ALTER TABLE itest4 ALTER COLUMN c ADD GENERATED ALWAYS AS IDENTITY
;
create function cachebug() returns void as $$
declare r int;
begin
  drop table if exists temptable cascade;
  create temp table temptable as select * from generate_series(1,3) as f1;
  create temp view vv as select * from temptable;
  for r in select * from vv loop
    raise notice '%', r;
  end loop;
end$$ language plpgsql
;
CREATE TABLE INFINITE_INTERVAL_TBL (i interval)
;
--
-- injection_points_cached()
--
-- Executes the action attached to the injection point, from local cache.
--
CREATE FUNCTION injection_points_cached(IN point_name TEXT,
    IN arg TEXT DEFAULT NULL)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_cached'
LANGUAGE C PARALLEL UNSAFE
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=19501 host=/tmp/sFlUg_qPNa dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
CREATE TABLE am_partitioned(x INT, y INT) PARTITION BY hash (x) USING heap2
;
ALTER TABLE pg_temp_11.pg_temp_26628_2 ADD COLUMN newdata pg_temp_11.pg_temp_26628
;
CREATE FUNCTION gbt_enum_same(gbtreekey8, gbtreekey8, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- Should fail. Procedure should be mandatorily specified
CREATE OPERATOR #@%# (
   rightarg = int8
)
;
DROP VIEW lock_view8
;
DROP TABLE test12pk, test12fk
;
-- error
alter table perm_part_parent attach partition temp_part_child default
;
ANALYZE public.ruletest_tbl2
;
CREATE DATABASE "ecpg1_regression" TEMPLATE=template0 ENCODING='SQL_ASCII'
;
alter table atacc1 alter "........pg.dropped.1........" drop default
;
DROP TABLE test_dc
;
create trigger tg_chkbacklink before insert or update
    on PSlot for each row execute procedure tg_chkbacklink()
;
CREATE TABLE "public"."test_type_diff2_c1" (
    "int_four" bigint,
    "int_eight" bigint,
    "int_two" smallint
)
;
CREATE EVENT TRIGGER always_start ON ddl_command_start
EXECUTE PROCEDURE event_trigger_test()
;
CREATE AGGREGATE wrongreturntype (float8)
(
    stype = float8,
    sfunc = float8pl,
    mstype = float8,
    msfunc = float8pl,
    minvfunc = float8mi_int
)
;
CREATE TABLE "public"."rtest_view3" (
    "a" integer,
    "b" "text"
)
;
TRUNCATE public.tru_rtable_parent CONTINUE IDENTITY RESTRICT
;
create table tprt_6 partition of tprt for values from (4001) to (5001)
;
create function mki8(bigint, bigint) returns int8_tbl as
$$select row($1,$2)::int8_tbl$$ language sql
;
ALTER TABLE pg_toast.pg_toast_27567 RENAME TO reind_con_toast
;
alter table tpart_default_default drop b
;
CREATE INDEX "brinidx_multi" ON "public"."brintest_multi" USING "brin" ("int8col" "int8_minmax_multi_ops", "int2col" "int2_minmax_multi_ops", "int4col" "int4_minmax_multi_ops", "oidcol" "oid_minmax_multi_ops", "tidcol" "tid_minmax_multi_ops", "float4col" "float4_minmax_multi_ops", "float8col" "float8_minmax_multi_ops", "macaddrcol" "macaddr_minmax_multi_ops", "macaddr8col" "macaddr8_minmax_multi_ops", "inetcol" "inet_minmax_multi_ops", "cidrcol" "inet_minmax_multi_ops", "datecol" "date_minmax_multi_ops", "timecol" "time_minmax_multi_ops", "timestampcol" "timestamp_minmax_multi_ops", "timestamptzcol" "timestamptz_minmax_multi_ops", "intervalcol" "interval_minmax_multi_ops", "timetzcol" "timetz_minmax_multi_ops", "numericcol" "numeric_minmax_multi_ops", "uuidcol" "uuid_minmax_multi_ops", "lsncol" "pg_lsn_minmax_multi_ops") WITH ("pages_per_range"='1')
;
CREATE TABLE measurement_y2007m01 (
    filler          text,
    peaktemp        int,
    logdate         date not null,
    city_id         int not null,
    unitsales       int
    CHECK ( logdate >= DATE '2007-01-01' AND logdate < DATE '2007-02-01')
) WITH (autovacuum_enabled=off)
;
CREATE OR REPLACE VIEW "public"."key_dependent_view" AS
 SELECT "key",
    "data"
   FROM "public"."view_base_table"
  GROUP BY "key"
;
CREATE TRIGGER parent_update_trig
  AFTER UPDATE ON range_parted for each statement execute procedure trigfunc()
;
ANALYZE prt4_n
;
CREATE TEMP TABLE pg_temp_24.pg_temp_27043_2 (tid pg_catalog.tid)
;
-- Test the case where the ordering of the scan matches the ordering within the
-- aggregate but cannot be found in the group-by list
CREATE TABLE agg_sort_order (c1 int PRIMARY KEY, c2 int)
;
ANALYZE public.pfield
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=31002 host=/tmp/VvDpZqB5zD dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
/*
 * text search configuration for nepali language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * nepali and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
create table test_storage (a text)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_opclass
;
-- let's try with the foreign key pointing at tables in the partition tree
-- that are not the same as the query's target table

-- 1. foreign key pointing into a non-root ancestor
--
-- A cross-partition update on the root table will fail, because we currently
-- can't enforce the foreign keys pointing into a non-leaf partition
ALTER TABLE fkpart11.fk DROP CONSTRAINT fkey
;
CREATE TABLE test_text (
	i text
)
;
CREATE INDEX t2_brin ON s2.t2 USING BRIN (i)
;
DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(_a, _b);  -- fail, no output argument for c
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
CREATE SCHEMA fkpart12
  CREATE TABLE fk_p ( id int, jd int, PRIMARY KEY(id, jd)) PARTITION BY list (id)
  CREATE TABLE fk_p_1 PARTITION OF fk_p FOR VALUES IN (1) PARTITION BY list (jd)
  CREATE TABLE fk_p_1_1 PARTITION OF fk_p_1 FOR VALUES IN (1)
  CREATE TABLE fk_p_1_2 (x int, y int, jd int NOT NULL, id int NOT NULL)
  CREATE TABLE fk_p_2 PARTITION OF fk_p FOR VALUES IN (2) PARTITION BY list (jd)
  CREATE TABLE fk_p_2_1 PARTITION OF fk_p_2 FOR VALUES IN (1)
  CREATE TABLE fk_p_2_2 PARTITION OF fk_p_2 FOR VALUES IN (2)
  CREATE TABLE fk_r_1 ( p_jd int NOT NULL, x int, id int PRIMARY KEY, p_id int NOT NULL)
  CREATE TABLE fk_r_2 ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL) PARTITION BY list (id)
  CREATE TABLE fk_r_2_1 PARTITION OF fk_r_2 FOR VALUES IN (2, 1)
  CREATE TABLE fk_r   ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL,
       FOREIGN KEY (p_id, p_jd) REFERENCES fk_p (id, jd)
  ) PARTITION BY list (id)
;
CREATE TABLE VARBIT_TABLE(v BIT VARYING(11))
;
ALTER FUNCTION time_dist(time without time zone, time without time zone) PARALLEL SAFE
;
CREATE TABLESPACE tblspc1 LOCATION '/tmp/5tJCEz0j6a/tempdir/tblspc1'
;
CREATE TABLE mic_inputs (inbytes bytea, description text)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "public"."test_tbl3" (
    "c" "public"."test_type3"
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tmp_pg_description ( 	objoid oid, 	classname name, 	objsubid int4, 	description text)
;
----------
[CREATE_TABLE] -- verify dependency handling during ALTER TABLE DETACH PARTITION
create table idxpart (a int) partition by range (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest4 ALTER COLUMN c ADD GENERATED ALWAYS AS IDENTITY
;
----------
[CREATE_PROG_OBJ] create function cachebug() returns void as $$
declare r int;
begin
  drop table if exists temptable cascade;
  create temp table temptable as select * from generate_series(1,3) as f1;
  create temp view vv as select * from temptable;
  for r in select * from vv loop
    raise notice '%', r;
  end loop;
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE INFINITE_INTERVAL_TBL (i interval)
;
----------
[CREATE_PROG_OBJ] --
-- injection_points_cached()
--
-- Executes the action attached to the injection point, from local cache.
--
CREATE FUNCTION injection_points_cached(IN point_name TEXT,
    IN arg TEXT DEFAULT NULL)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_cached'
LANGUAGE C PARALLEL UNSAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=19501 host=/tmp/sFlUg_qPNa dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned(x INT, y INT) PARTITION BY hash (x) USING heap2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26628_2 ADD COLUMN newdata pg_temp_11.pg_temp_26628
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_enum_same(gbtreekey8, gbtreekey8, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] -- Should fail. Procedure should be mandatorily specified
CREATE OPERATOR #@%# (
   rightarg = int8
)
;
----------
[DROP_VIEW] DROP VIEW lock_view8
;
----------
[DROP_TABLE] DROP TABLE test12pk, test12fk
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- error
alter table perm_part_parent attach partition temp_part_child default
;
----------
[ADMIN_TABLE] ANALYZE public.ruletest_tbl2
;
----------
[CREATE_CATALOG] CREATE DATABASE "ecpg1_regression" TEMPLATE=template0 ENCODING='SQL_ASCII'
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter "........pg.dropped.1........" drop default
;
----------
[DROP_TABLE] DROP TABLE test_dc
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_chkbacklink before insert or update
    on PSlot for each row execute procedure tg_chkbacklink()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_type_diff2_c1" (
    "int_four" bigint,
    "int_eight" bigint,
    "int_two" smallint
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER always_start ON ddl_command_start
EXECUTE PROCEDURE event_trigger_test()
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE wrongreturntype (float8)
(
    stype = float8,
    sfunc = float8pl,
    mstype = float8,
    msfunc = float8pl,
    minvfunc = float8mi_int
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_view3" (
    "a" integer,
    "b" "text"
)
;
----------
[TRUNCATE_TABLE] TRUNCATE public.tru_rtable_parent CONTINUE IDENTITY RESTRICT
;
----------
[CREATE_TABLE] create table tprt_6 partition of tprt for values from (4001) to (5001)
;
----------
[CREATE_PROG_OBJ] create function mki8(bigint, bigint) returns int8_tbl as
$$select row($1,$2)::int8_tbl$$ language sql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27567 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table tpart_default_default drop b
;
----------
[ADD_INDEX] CREATE INDEX "brinidx_multi" ON "public"."brintest_multi" USING "brin" ("int8col" "int8_minmax_multi_ops", "int2col" "int2_minmax_multi_ops", "int4col" "int4_minmax_multi_ops", "oidcol" "oid_minmax_multi_ops", "tidcol" "tid_minmax_multi_ops", "float4col" "float4_minmax_multi_ops", "float8col" "float8_minmax_multi_ops", "macaddrcol" "macaddr_minmax_multi_ops", "macaddr8col" "macaddr8_minmax_multi_ops", "inetcol" "inet_minmax_multi_ops", "cidrcol" "inet_minmax_multi_ops", "datecol" "date_minmax_multi_ops", "timecol" "time_minmax_multi_ops", "timestampcol" "timestamp_minmax_multi_ops", "timestamptzcol" "timestamptz_minmax_multi_ops", "intervalcol" "interval_minmax_multi_ops", "timetzcol" "timetz_minmax_multi_ops", "numericcol" "numeric_minmax_multi_ops", "uuidcol" "uuid_minmax_multi_ops", "lsncol" "pg_lsn_minmax_multi_ops") WITH ("pages_per_range"='1')
;
----------
[CREATE_TABLE] CREATE TABLE measurement_y2007m01 (
    filler          text,
    peaktemp        int,
    logdate         date not null,
    city_id         int not null,
    unitsales       int
    CHECK ( logdate >= DATE '2007-01-01' AND logdate < DATE '2007-02-01')
) WITH (autovacuum_enabled=off)
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW "public"."key_dependent_view" AS
 SELECT "key",
    "data"
   FROM "public"."view_base_table"
  GROUP BY "key"
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER parent_update_trig
  AFTER UPDATE ON range_parted for each statement execute procedure trigfunc()
;
----------
[ADMIN_TABLE] ANALYZE prt4_n
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_24.pg_temp_27043_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] -- Test the case where the ordering of the scan matches the ordering within the
-- aggregate but cannot be found in the group-by list
CREATE TABLE agg_sort_order (c1 int PRIMARY KEY, c2 int)
;
----------
[ADMIN_TABLE] ANALYZE public.pfield
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=31002 host=/tmp/VvDpZqB5zD dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_POLICY] /*
 * text search configuration for nepali language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * nepali and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
----------
[CREATE_TABLE] create table test_storage (a text)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_opclass
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- let's try with the foreign key pointing at tables in the partition tree
-- that are not the same as the query's target table

-- 1. foreign key pointing into a non-root ancestor
--
-- A cross-partition update on the root table will fail, because we currently
-- can't enforce the foreign keys pointing into a non-leaf partition
ALTER TABLE fkpart11.fk DROP CONSTRAINT fkey
;
----------
[CREATE_TABLE] CREATE TABLE test_text (
	i text
)
;
----------
[ADD_INDEX] CREATE INDEX t2_brin ON s2.t2 USING BRIN (i)
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(_a, _b);  -- fail, no output argument for c
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart12
  CREATE TABLE fk_p ( id int, jd int, PRIMARY KEY(id, jd)) PARTITION BY list (id)
  CREATE TABLE fk_p_1 PARTITION OF fk_p FOR VALUES IN (1) PARTITION BY list (jd)
  CREATE TABLE fk_p_1_1 PARTITION OF fk_p_1 FOR VALUES IN (1)
  CREATE TABLE fk_p_1_2 (x int, y int, jd int NOT NULL, id int NOT NULL)
  CREATE TABLE fk_p_2 PARTITION OF fk_p FOR VALUES IN (2) PARTITION BY list (jd)
  CREATE TABLE fk_p_2_1 PARTITION OF fk_p_2 FOR VALUES IN (1)
  CREATE TABLE fk_p_2_2 PARTITION OF fk_p_2 FOR VALUES IN (2)
  CREATE TABLE fk_r_1 ( p_jd int NOT NULL, x int, id int PRIMARY KEY, p_id int NOT NULL)
  CREATE TABLE fk_r_2 ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL) PARTITION BY list (id)
  CREATE TABLE fk_r_2_1 PARTITION OF fk_r_2 FOR VALUES IN (2, 1)
  CREATE TABLE fk_r   ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL,
       FOREIGN KEY (p_id, p_jd) REFERENCES fk_p (id, jd)
  ) PARTITION BY list (id)
;
----------
[CREATE_TABLE] CREATE TABLE VARBIT_TABLE(v BIT VARYING(11))
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION time_dist(time without time zone, time without time zone) PARALLEL SAFE
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc1 LOCATION '/tmp/5tJCEz0j6a/tempdir/tblspc1'
;
----------
[CREATE_TABLE] CREATE TABLE mic_inputs (inbytes bytea, description text)
;
