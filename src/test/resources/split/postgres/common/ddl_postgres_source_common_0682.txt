-- Check ALTER
ALTER INDEX reloptions_test_idx SET (fillfactor=40)
;
CREATE TABLE "public"."prt2_m_p1" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
-- fail
alter index idxpart_1_idx attach partition idxpart1_1_idx
;
create table child_20_30 partition of parent_tab
  for values from (20) to (30)
;
drop function p2text(p2)
;
CREATE TABLE tru_rtable0 (id int primary key)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_event_trigger
;
CREATE FUNCTION dblink (text, boolean)
RETURNS setof record
AS '$libdir/dblink','dblink_record'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
--   group w/o existing GROUP BY target under ambiguous condition
--   into a table
CREATE TABLE test_missing_target3 AS
SELECT count(x.b)
FROM test_missing_target x, test_missing_target y
	WHERE x.a = y.a
	GROUP BY x.b/2 ORDER BY x.b/2
;
ALTER TABLE parent SET LOGGED
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_amproc
;
-- Check the invariants.
create function verify(next_i int) returns bool language plpgsql as $$
declare
  a integer[];
  elem integer;
  c integer;
begin
  -- Perform a scan over the trailing part of the index, where the
  -- possible incomplete splits are. (We don't check the whole table,
  -- because that'd be pretty slow.)
  c := 0;
  -- Find all arrays that overlap with the last 200 inserted integers. Or
  -- the next 100, which shouldn't exist.
  for a in select i from gin_incomplete_splits where i && range_array(next_i - 200, next_i + 100)
  loop
    -- count all elements in those arrays in the window.
    foreach elem in ARRAY a loop
      if elem >= next_i then
        raise 'unexpected value % in array', elem;
      end if;
      if elem >= next_i - 200 then
        c := c + 1;
      end if;
    end loop;
  end loop;
  if c <> 200 then
    raise 'unexpected count % ', c;
  end if;

  return true;
end;
$$
;
ALTER TABLE T ALTER COLUMN c_bpchar    DROP DEFAULT,
              ALTER COLUMN c_date      DROP DEFAULT,
              ALTER COLUMN c_text      DROP DEFAULT,
              ALTER COLUMN c_timestamp DROP DEFAULT,
              ALTER COLUMN c_array     DROP DEFAULT
;
-- can't change plain function to aggregate:
create function sum4(int8,int8,int8,int8) returns int8 as
'select $1 + $2 + $3 + $4' language sql strict immutable
;
CREATE TABLE "public"."customer" (
    "cid" integer NOT NULL,
    "name" "text" NOT NULL,
    "tel" "text",
    "passwd" "text"
)
;
ANALYZE fkpart4.droppk21
;
alter database abc set tablespace myspace
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_data_wrapper
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
ANALYZE fkpart5.pk31
;
-- Views with GROUPING SET queries
CREATE VIEW gstest_view AS select a, b, grouping(a,b), sum(c), count(*), max(c)
  from gstest2 group by rollup ((a,b,c),(c,d))
;
VACUUM (FULL) information_schema.sql_sizing_profiles
;
ALTER TABLE reloptions_test SET (fillfactor=31,
	autovacuum_analyze_scale_factor = 0.3)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
-- failed (name conflict)

ALTER OPERATOR CLASS alt_opc3 USING hash RENAME TO alt_opc4
;
create type textrange_supp as range (
   subtype = text,
   subtype_opclass = text_pattern_ops
)
;
DROP DOMAIN js_int_array_2d
;
CREATE INDEX t2_gin ON s4.t2 USING GIN (ia)
;
drop table cnn_pk
;
CREATE FUNCTION "public"."mr_outparam_succeed2"("i" "anymultirange", OUT "r" "anyarray", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select ARRAY[upper($1)], 'foo'::text $_$
;
CREATE TEMP TABLE articles (
    id int CONSTRAINT articles_pkey PRIMARY KEY,
    keywords text,
    title text UNIQUE NOT NULL,
    body text UNIQUE,
    created date
)
;
create table part_xx_yy partition of list_parted for values in ('xx', 'yy') partition by list (a)
;
--
-- Now some cases with inheritance
-- Basic 2 table case: 1 column of matching types.
create table pktable_base (base1 int not null)
;
-- Clean up
DROP TABLE few
;
ALTER TABLE fk_partitioned_fk DROP COLUMN fdrop1
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub5
;
-- these should all fail
ALTER TABLE droppk DETACH PARTITION droppk_d
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
ALTER TABLE pg_temp_13.pg_temp_26049_2 ADD COLUMN newdata pg_temp_13.pg_temp_26049
;
CREATE PUBLICATION pub_empty
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_auth_members
;
CREATE TABLE "public"."comment_test" (
    "id" "text" NOT NULL,
    "positive_col" bigint,
    "indexed_col" "text",
    CONSTRAINT "comment_test_positive_col_check" CHECK (("positive_col" > 0))
)
;
ALTER TABLE toasted_several ALTER COLUMN toasted_col2 SET STORAGE EXTERNAL
;
CREATE TEMP TABLE users (
  id INT PRIMARY KEY,
  name VARCHAR NOT NULL
)
;
COMMENT ON SEQUENCE sequence_test2 IS 'will work'
;
--    P    P        P    P
-- should CREATE
CREATE AGGREGATE myaggp20a(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
-- several levels of shadowing
create or replace function shadowtest(in1 int)
	returns void as $$
declare
in1 int;
begin
	declare
	in1 int;
	begin
	end;
end$$ language plpgsql
;
CREATE UNIQUE INDEX test_replica_identity_nonkey ON test_replica_identity (keya, nonkey)
;
CREATE FUNCTION cube_cmp(cube, cube)
RETURNS int4
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create or replace function dfunc(a variadic int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
------------------------------------------------------------------------------------------
[ALTER_INDEX] -- Check ALTER
ALTER INDEX reloptions_test_idx SET (fillfactor=40)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_m_p1" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[ALTER_INDEX] -- fail
alter index idxpart_1_idx attach partition idxpart1_1_idx
;
----------
[CREATE_TABLE] create table child_20_30 partition of parent_tab
  for values from (20) to (30)
;
----------
[DROP_PROG_OBJ] drop function p2text(p2)
;
----------
[CREATE_TABLE] CREATE TABLE tru_rtable0 (id int primary key)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_event_trigger
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink (text, boolean)
RETURNS setof record
AS '$libdir/dblink','dblink_record'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE|SELECT] --   group w/o existing GROUP BY target under ambiguous condition
--   into a table
CREATE TABLE test_missing_target3 AS
SELECT count(x.b)
FROM test_missing_target x, test_missing_target y
	WHERE x.a = y.a
	GROUP BY x.b/2 ORDER BY x.b/2
;
----------
[ALTER_TABLE] ALTER TABLE parent SET LOGGED
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_amproc
;
----------
[CREATE_PROG_OBJ] -- Check the invariants.
create function verify(next_i int) returns bool language plpgsql as $$
declare
  a integer[];
  elem integer;
  c integer;
begin
  -- Perform a scan over the trailing part of the index, where the
  -- possible incomplete splits are. (We don't check the whole table,
  -- because that'd be pretty slow.)
  c := 0;
  -- Find all arrays that overlap with the last 200 inserted integers. Or
  -- the next 100, which shouldn't exist.
  for a in select i from gin_incomplete_splits where i && range_array(next_i - 200, next_i + 100)
  loop
    -- count all elements in those arrays in the window.
    foreach elem in ARRAY a loop
      if elem >= next_i then
        raise 'unexpected value % in array', elem;
      end if;
      if elem >= next_i - 200 then
        c := c + 1;
      end if;
    end loop;
  end loop;
  if c <> 200 then
    raise 'unexpected count % ', c;
  end if;

  return true;
end;
$$
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE T ALTER COLUMN c_bpchar    DROP DEFAULT,
              ALTER COLUMN c_date      DROP DEFAULT,
              ALTER COLUMN c_text      DROP DEFAULT,
              ALTER COLUMN c_timestamp DROP DEFAULT,
              ALTER COLUMN c_array     DROP DEFAULT
;
----------
[CREATE_PROG_OBJ] -- can't change plain function to aggregate:
create function sum4(int8,int8,int8,int8) returns int8 as
'select $1 + $2 + $3 + $4' language sql strict immutable
;
----------
[CREATE_TABLE] CREATE TABLE "public"."customer" (
    "cid" integer NOT NULL,
    "name" "text" NOT NULL,
    "tel" "text",
    "passwd" "text"
)
;
----------
[ADMIN_TABLE] ANALYZE fkpart4.droppk21
;
----------
[ALTER_CATALOG] alter database abc set tablespace myspace
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_data_wrapper
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
----------
[ADMIN_TABLE] ANALYZE fkpart5.pk31
;
----------
[CREATE_VIEW(SELECT)] -- Views with GROUPING SET queries
CREATE VIEW gstest_view AS select a, b, grouping(a,b), sum(c), count(*), max(c)
  from gstest2 group by rollup ((a,b,c),(c,d))
;
----------
[ADMIN_TABLE] VACUUM (FULL) information_schema.sql_sizing_profiles
;
----------
[ALTER_TABLE] ALTER TABLE reloptions_test SET (fillfactor=31,
	autovacuum_analyze_scale_factor = 0.3)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = issn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[ALTER_PROG_OBJ] -- failed (name conflict)

ALTER OPERATOR CLASS alt_opc3 USING hash RENAME TO alt_opc4
;
----------
[CREATE_TYPE] create type textrange_supp as range (
   subtype = text,
   subtype_opclass = text_pattern_ops
)
;
----------
[DROP_TYPE] DROP DOMAIN js_int_array_2d
;
----------
[ADD_INDEX] CREATE INDEX t2_gin ON s4.t2 USING GIN (ia)
;
----------
[DROP_TABLE] drop table cnn_pk
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."mr_outparam_succeed2"("i" "anymultirange", OUT "r" "anyarray", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select ARRAY[upper($1)], 'foo'::text $_$
;
----------
[CREATE_TABLE] CREATE TEMP TABLE articles (
    id int CONSTRAINT articles_pkey PRIMARY KEY,
    keywords text,
    title text UNIQUE NOT NULL,
    body text UNIQUE,
    created date
)
;
----------
[CREATE_TABLE] create table part_xx_yy partition of list_parted for values in ('xx', 'yy') partition by list (a)
;
----------
[CREATE_TABLE] --
-- Now some cases with inheritance
-- Basic 2 table case: 1 column of matching types.
create table pktable_base (base1 int not null)
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE few
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk_partitioned_fk DROP COLUMN fdrop1
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub5
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- these should all fail
ALTER TABLE droppk DETACH PARTITION droppk_d
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26049_2 ADD COLUMN newdata pg_temp_13.pg_temp_26049
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_empty
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_auth_members
;
----------
[CREATE_TABLE] CREATE TABLE "public"."comment_test" (
    "id" "text" NOT NULL,
    "positive_col" bigint,
    "indexed_col" "text",
    CONSTRAINT "comment_test_positive_col_check" CHECK (("positive_col" > 0))
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE toasted_several ALTER COLUMN toasted_col2 SET STORAGE EXTERNAL
;
----------
[CREATE_TABLE] CREATE TEMP TABLE users (
  id INT PRIMARY KEY,
  name VARCHAR NOT NULL
)
;
----------
[COMMENT_SEQUENCE] COMMENT ON SEQUENCE sequence_test2 IS 'will work'
;
----------
[CREATE_PROG_OBJ] --    P    P        P    P
-- should CREATE
CREATE AGGREGATE myaggp20a(BASETYPE = anyelement, SFUNC = tfp,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] -- several levels of shadowing
create or replace function shadowtest(in1 int)
	returns void as $$
declare
in1 int;
begin
	declare
	in1 int;
	begin
	end;
end$$ language plpgsql
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX test_replica_identity_nonkey ON test_replica_identity (keya, nonkey)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_cmp(cube, cube)
RETURNS int4
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function dfunc(a variadic int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
