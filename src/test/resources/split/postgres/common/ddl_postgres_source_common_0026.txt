DROP TABLE pg_temp_13.pg_temp_26471_2, pg_temp_13.pg_temp_26471
;
ALTER SEQUENCE "public"."sj_t4_id_seq" OWNED BY "public"."sj_t4"."id"
;
CREATE TABLE "public"."bug_16784" (
    "i" integer,
    "j" integer
)
WITH ("autovacuum_enabled"='false')
;
ALTER TABLE split_alter_table.base_table CLUSTER ON base_table_payload_idx
;
ANALYZE pg_catalog.pg_subscription_rel
;
/*
 * 6.45
 * ROLE_TABLE_GRANTS view
 */

CREATE VIEW role_table_grants AS
    SELECT grantor,
           grantee,
           table_catalog,
           table_schema,
           table_name,
           privilege_type,
           is_grantable,
           with_hierarchy
    FROM table_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
CREATE FUNCTION filter_basic_dml_fn() RETURNS TRIGGER AS $$
BEGIN
    IF (TG_OP = 'INSERT') THEN
        IF (NEW.id < 10) THEN
            RETURN NEW;
        ELSE
            RETURN NULL;
        END IF;
    ELSIF (TG_OP = 'UPDATE') THEN
        RETURN NULL;
    ELSE
        RAISE WARNING 'Unknown action';
        RETURN NULL;
    END IF;
END;
$$ LANGUAGE plpgsql
;
ALTER DATABASE contrib_regression SET role = regress_current_user
;
ANALYZE pg_temp_10.pg_temp_26364
;
create domain posint2 as posint check (value % 2 = 0)
;
ALTER FUNCTION gbt_intv_consistent(internal, interval, smallint, oid, internal) PARALLEL SAFE
;
DROP TABLE attmp_new2
;
CREATE FUNCTION test_stat_func() RETURNS VOID LANGUAGE plpgsql AS
      $$BEGIN PERFORM pg_sleep(10e-6); END;$$
;
do $$
declare x record not null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
CREATE TABLE testschema.part (a int) PARTITION BY LIST (a)
;
-- access to call stack from exception
create function inner_func(int)
returns int as $$
declare
  _context text;
  sx int := 5;
begin
  begin
    perform sx / 0;
  exception
    when division_by_zero then
      get diagnostics _context = pg_context;
      raise notice '***%***', _context;
  end;

  -- lets do it again, just for fun..
  get diagnostics _context = pg_context;
  raise notice '***%***', _context;
  raise notice 'lets make sure we didnt break anything';
  return 2 * $1;
end;
$$ language plpgsql
;
CREATE OR REPLACE FUNCTION view_trigger() RETURNS trigger
LANGUAGE plpgsql AS $$
declare
    argstr text := '';
begin
    for i in 0 .. TG_nargs - 1 loop
        if i > 0 then
            argstr := argstr || ', ';
        end if;
        argstr := argstr || TG_argv[i];
    end loop;

    raise notice '% % % % (%)', TG_RELNAME, TG_WHEN, TG_OP, TG_LEVEL, argstr;

    if TG_LEVEL = 'ROW' then
        if TG_OP = 'INSERT' then
            raise NOTICE 'NEW: %', NEW;
            INSERT INTO main_table VALUES (NEW.a, NEW.b);
            RETURN NEW;
        end if;

        if TG_OP = 'UPDATE' then
            raise NOTICE 'OLD: %, NEW: %', OLD, NEW;
            UPDATE main_table SET a = NEW.a, b = NEW.b WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN NEW;
        end if;

        if TG_OP = 'DELETE' then
            raise NOTICE 'OLD: %', OLD;
            DELETE FROM main_table WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN OLD;
        end if;
    end if;

    RETURN NULL;
end;
$$
;
CREATE FUNCTION isnlt(upc, upc)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE test_root_1 PARTITION OF test_root FOR VALUES FROM (1) TO (10)
;
VACUUM full verbose pg_class
;
create function mr_outparam_succeed(i anymultirange, out r anymultirange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
-- Failures with non-GiST index.
CREATE INDEX test_gist_btree on test_gist(t)
;
ANALYZE pg_temp_10.pg_temp_26444
;
/*
 * text search configuration for danish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * danish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY danish_stem
	(TEMPLATE = snowball, Language = danish , StopWords=danish)
;
CREATE TYPE ltree (
	INTERNALLENGTH = -1,
	INPUT = ltree_in,
	OUTPUT = ltree_out,
	STORAGE = extended
)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_init_privs
;
drop index idxpart3_a_idx
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=17833 host=/tmp/D52XFkhi3a dbname=postgres' PUBLICATION mypub
;
DROP TABLE IF EXISTS t1p CASCADE
;
COMMENT ON TEXT SEARCH DICTIONARY german_stem IS 'snowball stemmer for german language'
;
CREATE SUBSCRIPTION regress_testsub4 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false, origin = none)
;
CREATE TABLE pp_nn_1(a int, b int)
;
-- fail

DROP TYPE test_ext_type
;
CREATE FUNCTION reverse_name(name)
   RETURNS name
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT
;
CREATE TABLE seclabel_tbl1 (a int, b text)
;
CREATE OR REPLACE TEMP VIEW v_window AS
	SELECT i, sum(i) over (order by i rows between 1 preceding and 1 following
	exclude no others) as sum_rows FROM generate_series(1, 10) i
;
alter table inh_parent alter a drop not null
;
drop table 計算機用語
;
ALTER TABLE pg_ts_config ADD UNIQUE USING INDEX pg_ts_config_cfgname_index
;
DROP TYPE default_test_row CASCADE
;
ALTER TABLE testschema.test_default_tab ADD CONSTRAINT test_index4 UNIQUE (id) USING INDEX TABLESPACE regress_tblspace
;
CREATE TABLE "public"."pagg_tab_p1" (
    "a" integer,
    "b" integer,
    "c" "text",
    "d" integer
)
;
CREATE TABLE truncparted (a int, b char) PARTITION BY LIST (a)
;
create function exc_using(int, text) returns int as $$
declare i int;
begin
  for i in execute 'select * from generate_series(1,$1)' using $1+1 loop
    raise notice '%', i;
  end loop;
  execute 'select $2 + $2*3 + length($1)' into i using $2,$1;
  return i;
end
$$ language plpgsql
;
DROP PROCEDURE test_ambiguous_procname(text)
;
CREATE SCHEMA split_extension_next_moved
;
CREATE TABLE "public"."float8_tbl" (
    "f1" double precision
)
;
ANALYZE fprt2_p2
;
COMMENT ON LARGE OBJECT 3001 IS 'testing comments'
;
CREATE EXTENSION jsonb_plpython3u CASCADE
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26471_2, pg_temp_13.pg_temp_26471
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."sj_t4_id_seq" OWNED BY "public"."sj_t4"."id"
;
----------
[CREATE_TABLE] CREATE TABLE "public"."bug_16784" (
    "i" integer,
    "j" integer
)
WITH ("autovacuum_enabled"='false')
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table CLUSTER ON base_table_payload_idx
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_subscription_rel
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.45
 * ROLE_TABLE_GRANTS view
 */

CREATE VIEW role_table_grants AS
    SELECT grantor,
           grantee,
           table_catalog,
           table_schema,
           table_name,
           privilege_type,
           is_grantable,
           with_hierarchy
    FROM table_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION filter_basic_dml_fn() RETURNS TRIGGER AS $$
BEGIN
    IF (TG_OP = 'INSERT') THEN
        IF (NEW.id < 10) THEN
            RETURN NEW;
        ELSE
            RETURN NULL;
        END IF;
    ELSIF (TG_OP = 'UPDATE') THEN
        RETURN NULL;
    ELSE
        RAISE WARNING 'Unknown action';
        RETURN NULL;
    END IF;
END;
$$ LANGUAGE plpgsql
;
----------
[ALTER_CATALOG] ALTER DATABASE contrib_regression SET role = regress_current_user
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26364
;
----------
[CREATE_TYPE] create domain posint2 as posint check (value % 2 = 0)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_consistent(internal, interval, smallint, oid, internal) PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE attmp_new2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_stat_func() RETURNS VOID LANGUAGE plpgsql AS
      $$BEGIN PERFORM pg_sleep(10e-6); END;$$
;
----------
[BLOCK] do $$
declare x record not null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part (a int) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] -- access to call stack from exception
create function inner_func(int)
returns int as $$
declare
  _context text;
  sx int := 5;
begin
  begin
    perform sx / 0;
  exception
    when division_by_zero then
      get diagnostics _context = pg_context;
      raise notice '***%***', _context;
  end;

  -- lets do it again, just for fun..
  get diagnostics _context = pg_context;
  raise notice '***%***', _context;
  raise notice 'lets make sure we didnt break anything';
  return 2 * $1;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION view_trigger() RETURNS trigger
LANGUAGE plpgsql AS $$
declare
    argstr text := '';
begin
    for i in 0 .. TG_nargs - 1 loop
        if i > 0 then
            argstr := argstr || ', ';
        end if;
        argstr := argstr || TG_argv[i];
    end loop;

    raise notice '% % % % (%)', TG_RELNAME, TG_WHEN, TG_OP, TG_LEVEL, argstr;

    if TG_LEVEL = 'ROW' then
        if TG_OP = 'INSERT' then
            raise NOTICE 'NEW: %', NEW;
            INSERT INTO main_table VALUES (NEW.a, NEW.b);
            RETURN NEW;
        end if;

        if TG_OP = 'UPDATE' then
            raise NOTICE 'OLD: %, NEW: %', OLD, NEW;
            UPDATE main_table SET a = NEW.a, b = NEW.b WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN NEW;
        end if;

        if TG_OP = 'DELETE' then
            raise NOTICE 'OLD: %', OLD;
            DELETE FROM main_table WHERE a = OLD.a AND b = OLD.b;
            if NOT FOUND then RETURN NULL; end if;
            RETURN OLD;
        end if;
    end if;

    RETURN NULL;
end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(upc, upc)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test_root_1 PARTITION OF test_root FOR VALUES FROM (1) TO (10)
;
----------
[ADMIN_TABLE] VACUUM full verbose pg_class
;
----------
[CREATE_PROG_OBJ] create function mr_outparam_succeed(i anymultirange, out r anymultirange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
----------
[ADD_INDEX] -- Failures with non-GiST index.
CREATE INDEX test_gist_btree on test_gist(t)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26444
;
----------
[CREATE_POLICY] /*
 * text search configuration for danish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * danish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY danish_stem
	(TEMPLATE = snowball, Language = danish , StopWords=danish)
;
----------
[CREATE_TYPE] CREATE TYPE ltree (
	INTERNALLENGTH = -1,
	INPUT = ltree_in,
	OUTPUT = ltree_out,
	STORAGE = extended
)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_init_privs
;
----------
[DROP_INDEX] drop index idxpart3_a_idx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=17833 host=/tmp/D52XFkhi3a dbname=postgres' PUBLICATION mypub
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS t1p CASCADE
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY german_stem IS 'snowball stemmer for german language'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub4 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false, origin = none)
;
----------
[CREATE_TABLE] CREATE TABLE pp_nn_1(a int, b int)
;
----------
[DROP_TYPE] -- fail

DROP TYPE test_ext_type
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION reverse_name(name)
   RETURNS name
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT
;
----------
[CREATE_TABLE] CREATE TABLE seclabel_tbl1 (a int, b text)
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE TEMP VIEW v_window AS
	SELECT i, sum(i) over (order by i rows between 1 preceding and 1 following
	exclude no others) as sum_rows FROM generate_series(1, 10) i
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table inh_parent alter a drop not null
;
----------
[DROP_TABLE] drop table 計算機用語
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_config ADD UNIQUE USING INDEX pg_ts_config_cfgname_index
;
----------
[DROP_TYPE] DROP TYPE default_test_row CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testschema.test_default_tab ADD CONSTRAINT test_index4 UNIQUE (id) USING INDEX TABLESPACE regress_tblspace
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_p1" (
    "a" integer,
    "b" integer,
    "c" "text",
    "d" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE truncparted (a int, b char) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] create function exc_using(int, text) returns int as $$
declare i int;
begin
  for i in execute 'select * from generate_series(1,$1)' using $1+1 loop
    raise notice '%', i;
  end loop;
  execute 'select $2 + $2*3 + length($1)' into i using $2,$1;
  return i;
end
$$ language plpgsql
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE test_ambiguous_procname(text)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_extension_next_moved
;
----------
[CREATE_TABLE] CREATE TABLE "public"."float8_tbl" (
    "f1" double precision
)
;
----------
[ADMIN_TABLE] ANALYZE fprt2_p2
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 3001 IS 'testing comments'
;
----------
[CREATE_LIBRARY] CREATE EXTENSION jsonb_plpython3u CASCADE
;
