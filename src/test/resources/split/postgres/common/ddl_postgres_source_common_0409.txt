CREATE SUBSCRIPTION tap_sub_sch CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION tap_pub_sch
;
VACUUM (FREEZE) pg_catalog.pg_operator
;
-- these should work for sequences
create sequence test_sequence
;
ALTER TABLE pg_temp_9.pg_temp_38025_2 ADD COLUMN newdata pg_temp_9.pg_temp_38025
;
ALTER TABLE quuux ATTACH PARTITION quuux2 FOR VALUES IN (2)
;
CREATE TABLE coll_t (c) AS VALUES ('bar'::text)
;
drop table if exists source
;
CREATE FUNCTION gin_compare_prefix_macaddr(macaddr, macaddr, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE pg_toast.pg_toast_30711 RENAME TO reind_con_toast
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_replication_origin
;
CREATE TABLE "gin_check_multicolumn"(a text[], b text[])
;
CREATE EXTENSION test_tidstore
;
CREATE FUNCTION getrngfunc9(int) RETURNS rngfunc AS 'DECLARE rngfunctup rngfunc%ROWTYPE; BEGIN SELECT * into rngfunctup FROM rngfunc WHERE rngfuncid = $1; RETURN rngfunctup; END;' LANGUAGE plpgsql
;
CREATE INDEX idx_inet ON test_inet USING gin (i)
;
create unique index ec1_expr3 on ec1((ff + 3 + 1))
;
-- partitions with their own identity columns are not allowed, even if the
-- partitioned table does not have an identity column.
CREATE TABLE pitest1_pfail PARTITION OF pitest1 (
    f3 WITH OPTIONS GENERATED ALWAYS AS IDENTITY
) FOR VALUES FROM ('2016-11-01') TO ('2016-12-01')
;
TRUNCATE tab1, tab2
;
-- variadic aggregates
create function least_accum(anyelement, variadic anyarray)
returns anyelement language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
-- function to wait for counters to advance
CREATE FUNCTION wait_for_decode_stats(check_reset bool, check_spill_txns bool) RETURNS void AS $$
DECLARE
  start_time timestamptz := clock_timestamp();
  updated bool;
BEGIN
  -- we don't want to wait forever; loop will exit after 30 seconds
  FOR i IN 1 .. 300 LOOP

    IF check_spill_txns THEN

      -- check to see if all updates have been reset/updated
      SELECT CASE WHEN check_reset THEN (spill_txns = 0)
                  ELSE (spill_txns > 0)
             END
      INTO updated
      FROM pg_stat_replication_slots WHERE slot_name='regression_slot_stats';

    ELSE

      -- check to see if all updates have been reset/updated
      SELECT CASE WHEN check_reset THEN (total_txns = 0)
                  ELSE (total_txns > 0)
             END
      INTO updated
      FROM pg_stat_replication_slots WHERE slot_name='regression_slot_stats';

    END IF;

    exit WHEN updated;

    -- wait a little
    perform pg_sleep_for('100 milliseconds');

    -- reset stats snapshot so we can test again
    perform pg_stat_clear_snapshot();

  END LOOP;

  -- report time waited in postmaster log (where it won't change test output)
  RAISE LOG 'wait_for_decode_stats delayed % seconds',
    extract(epoch from clock_timestamp() - start_time);
END
$$ LANGUAGE plpgsql
;
ALTER TEXT SEARCH CONFIGURATION german ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH german_stem
;
ALTER TABLE hash_parted DETACH PARTITION hpart_4
;
analyze prtx2
;
create function tg_pslot_biu() returns trigger as $proc$
declare
    pfrec	record;
    ps          alias for new;
begin
    select into pfrec * from PField where name = ps.pfname;
    if not found then
        raise exception $$Patchfield "%" does not exist$$, ps.pfname;
    end if;
    return ps;
end;
$proc$ language plpgsql
;
ALTER TABLE gtest31_2 ADD CONSTRAINT cc CHECK ((y).b IS NOT NULL)
;
CREATE MATERIALIZED VIEW matview_schema.mv_nodata1 (a) AS
  SELECT generate_series(1, 10) WITH NO DATA
;
drop table agg_hash_2
;
CREATE TABLE group_agg_pk AS SELECT
  i % 10 AS x,
  i % 2 AS y,
  i % 2 AS z,
  2 AS w,
  i % 10 AS f
FROM generate_series(1,100) AS i
;
create table tbl_nocom(a int8, b int8alias1)
;
CREATE RULE "rtest_t4_ins2" AS
    ON INSERT TO "public"."rtest_t4"
   WHERE (("new"."a" >= 20) AND ("new"."a" < 30)) DO  INSERT INTO "public"."rtest_t6" ("a", "b")
  VALUES ("new"."a", "new"."b")
;
ANALYZE public.prt2_l_p3_p2
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp(2) < $.timestamp(3))'))
;
alter table idxpart attach partition idxpart3 for values from (1000) to (2000)
;
create table pp_intrangepart2inf partition of pp_intrangepart for values in ('[2,)')
;
CREATE TYPE isbn13 (
	INPUT = isbn13_in,
	OUTPUT = ean13_out,
	LIKE = pg_catalog.int8
)
;
CREATE DOMAIN js_int_not_null  AS int     NOT NULL
;
CREATE TABLE foo (
     key INT PRIMARY KEY,
     value INT
  )
;
--
-- Check use of temporary tables with inheritance trees
--
create table inh_perm_parent (a1 int)
;
CREATE TABLE prt2_adv_p1 PARTITION OF prt2_adv FOR VALUES FROM (100) TO (200)
;
create table test_composite(x integer)
;
CREATE OPERATOR === (
    LEFTARG = boolean,
    RIGHTARG = boolean,
    PROCEDURE = fn_op2,
    COMMUTATOR = ===,
    NEGATOR = !==,
    RESTRICT = contsel,
    JOIN = contjoinsel,
    SORT1, SORT2, LTCMP, GTCMP, HASHES, MERGES
)
;
COMMENT ON CONSTRAINT ctlt3_a_check ON ctlt3 IS 't3_a_check'
;
CREATE FUNCTION brin_revmap_data(IN page bytea,
	OUT pages tid)
RETURNS SETOF tid
AS '$libdir/pageinspect', 'brin_revmap_data'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn13,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
COMMENT ON TEXT SEARCH CONFIGURATION hungarian IS 'configuration for hungarian language'
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_statistic_ext
;
DROP FUNCTION get_raw_page(text, int4)
;
-- Inheritance test tables for pg_upgrade
create table constr_parent (a int)
;
DROP TABLE pg_temp_13.pg_temp_26250_2, pg_temp_13.pg_temp_26250
;
CREATE TABLE part_rev (b INT, c INT, a INT)
;
ANALYZE public.rewritemetoo3
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_sch CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION tap_pub_sch
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_operator
;
----------
[CREATE_SEQUENCE] -- these should work for sequences
create sequence test_sequence
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_38025_2 ADD COLUMN newdata pg_temp_9.pg_temp_38025
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE quuux ATTACH PARTITION quuux2 FOR VALUES IN (2)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE coll_t (c) AS VALUES ('bar'::text)
;
----------
[DROP_TABLE] drop table if exists source
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_macaddr(macaddr, macaddr, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30711 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE] CREATE TABLE "gin_check_multicolumn"(a text[], b text[])
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_tidstore
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION getrngfunc9(int) RETURNS rngfunc AS 'DECLARE rngfunctup rngfunc%ROWTYPE; BEGIN SELECT * into rngfunctup FROM rngfunc WHERE rngfuncid = $1; RETURN rngfunctup; END;' LANGUAGE plpgsql
;
----------
[ADD_INDEX] CREATE INDEX idx_inet ON test_inet USING gin (i)
;
----------
[ADD_INDEX] create unique index ec1_expr3 on ec1((ff + 3 + 1))
;
----------
[CREATE_TABLE] -- partitions with their own identity columns are not allowed, even if the
-- partitioned table does not have an identity column.
CREATE TABLE pitest1_pfail PARTITION OF pitest1 (
    f3 WITH OPTIONS GENERATED ALWAYS AS IDENTITY
) FOR VALUES FROM ('2016-11-01') TO ('2016-12-01')
;
----------
[TRUNCATE_TABLE] TRUNCATE tab1, tab2
;
----------
[CREATE_PROG_OBJ] -- variadic aggregates
create function least_accum(anyelement, variadic anyarray)
returns anyelement language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
----------
[CREATE_PROG_OBJ] -- function to wait for counters to advance
CREATE FUNCTION wait_for_decode_stats(check_reset bool, check_spill_txns bool) RETURNS void AS $$
DECLARE
  start_time timestamptz := clock_timestamp();
  updated bool;
BEGIN
  -- we don't want to wait forever; loop will exit after 30 seconds
  FOR i IN 1 .. 300 LOOP

    IF check_spill_txns THEN

      -- check to see if all updates have been reset/updated
      SELECT CASE WHEN check_reset THEN (spill_txns = 0)
                  ELSE (spill_txns > 0)
             END
      INTO updated
      FROM pg_stat_replication_slots WHERE slot_name='regression_slot_stats';

    ELSE

      -- check to see if all updates have been reset/updated
      SELECT CASE WHEN check_reset THEN (total_txns = 0)
                  ELSE (total_txns > 0)
             END
      INTO updated
      FROM pg_stat_replication_slots WHERE slot_name='regression_slot_stats';

    END IF;

    exit WHEN updated;

    -- wait a little
    perform pg_sleep_for('100 milliseconds');

    -- reset stats snapshot so we can test again
    perform pg_stat_clear_snapshot();

  END LOOP;

  -- report time waited in postmaster log (where it won't change test output)
  RAISE LOG 'wait_for_decode_stats delayed % seconds',
    extract(epoch from clock_timestamp() - start_time);
END
$$ LANGUAGE plpgsql
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION german ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH german_stem
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE hash_parted DETACH PARTITION hpart_4
;
----------
[ADMIN_TABLE] analyze prtx2
;
----------
[CREATE_PROG_OBJ] create function tg_pslot_biu() returns trigger as $proc$
declare
    pfrec	record;
    ps          alias for new;
begin
    select into pfrec * from PField where name = ps.pfname;
    if not found then
        raise exception $$Patchfield "%" does not exist$$, ps.pfname;
    end if;
    return ps;
end;
$proc$ language plpgsql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE gtest31_2 ADD CONSTRAINT cc CHECK ((y).b IS NOT NULL)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW matview_schema.mv_nodata1 (a) AS
  SELECT generate_series(1, 10) WITH NO DATA
;
----------
[DROP_TABLE] drop table agg_hash_2
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE group_agg_pk AS SELECT
  i % 10 AS x,
  i % 2 AS y,
  i % 2 AS z,
  2 AS w,
  i % 10 AS f
FROM generate_series(1,100) AS i
;
----------
[CREATE_TABLE] create table tbl_nocom(a int8, b int8alias1)
;
----------
[CREATE_POLICY] CREATE RULE "rtest_t4_ins2" AS
    ON INSERT TO "public"."rtest_t4"
   WHERE (("new"."a" >= 20) AND ("new"."a" < 30)) DO  INSERT INTO "public"."rtest_t6" ("a", "b")
  VALUES ("new"."a", "new"."b")
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_l_p3_p2
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp(2) < $.timestamp(3))'))
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart3 for values from (1000) to (2000)
;
----------
[CREATE_TABLE] create table pp_intrangepart2inf partition of pp_intrangepart for values in ('[2,)')
;
----------
[CREATE_TYPE] CREATE TYPE isbn13 (
	INPUT = isbn13_in,
	OUTPUT = ean13_out,
	LIKE = pg_catalog.int8
)
;
----------
[CREATE_TYPE] CREATE DOMAIN js_int_not_null  AS int     NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE foo (
     key INT PRIMARY KEY,
     value INT
  )
;
----------
[CREATE_TABLE] --
-- Check use of temporary tables with inheritance trees
--
create table inh_perm_parent (a1 int)
;
----------
[CREATE_TABLE] CREATE TABLE prt2_adv_p1 PARTITION OF prt2_adv FOR VALUES FROM (100) TO (200)
;
----------
[CREATE_TABLE] create table test_composite(x integer)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR === (
    LEFTARG = boolean,
    RIGHTARG = boolean,
    PROCEDURE = fn_op2,
    COMMUTATOR = ===,
    NEGATOR = !==,
    RESTRICT = contsel,
    JOIN = contjoinsel,
    SORT1, SORT2, LTCMP, GTCMP, HASHES, MERGES
)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT ctlt3_a_check ON ctlt3 IS 't3_a_check'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION brin_revmap_data(IN page bytea,
	OUT pages tid)
RETURNS SETOF tid
AS '$libdir/pageinspect', 'brin_revmap_data'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = issn13,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION hungarian IS 'configuration for hungarian language'
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_statistic_ext
;
----------
[DROP_PROG_OBJ] DROP FUNCTION get_raw_page(text, int4)
;
----------
[CREATE_TABLE] -- Inheritance test tables for pg_upgrade
create table constr_parent (a int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26250_2, pg_temp_13.pg_temp_26250
;
----------
[CREATE_TABLE] CREATE TABLE part_rev (b INT, c INT, a INT)
;
----------
[ADMIN_TABLE] ANALYZE public.rewritemetoo3
;
