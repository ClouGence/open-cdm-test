create function child_del_func()
  returns trigger language plpgsql as
$$
begin
  update parent set bcnt = bcnt - 1 where aid = old.aid;
  return old;
end;
$$
;
CREATE TABLE fprt2 (a int, b int, c varchar) PARTITION BY RANGE(b)
;
drop function expensivefunc(int)
;
CREATE TABLE fooarr(f1 text, f2 int[], f3 text)
;
CREATE TABLE trgfire (i int) PARTITION BY RANGE (i)
;
ALTER TABLE pg_toast.pg_toast_28068 RENAME TO reind_con_toast
;
ALTER INDEX pg_toast.pg_toast_28029_index RENAME TO reind_con_toast_idx
;
CREATE VIEW "public"."limit_thousand_v_5" AS
 SELECT "thousand"
   FROM "public"."onek"
  WHERE ("thousand" < 995)
  ORDER BY "thousand"
 LIMIT ALL
;
-- try altering a non-existent column, should fail
alter table atacc1 alter bar set not null
;
CREATE TABLE pp_enumpart (a pp_colors)
							PARTITION BY HASH (a)
;
CREATE TABLE test (
    key   integer UNIQUE,
    val   text
  )
;
ALTER OPERATOR <= (issn, issn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
DROP TABLE testpub_rf_tbl1
;
-- dropping/detaching PARTITIONs is prevented if that would break
-- a foreign key's existing data
CREATE TABLE droppk (a int PRIMARY KEY) PARTITION BY RANGE (a)
;
DROP TYPE base_type CASCADE
;
VACUUM (PARALLEL 0) pg_catalog.pg_operator
;
ANALYZE pg_temp_9.pg_temp_26555_2
;
ANALYZE public.pline
;
CREATE FUNCTION isnge(isbn, isbn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TYPE "public"."insenum" ADD VALUE 'i19'
;
create function build_ordered_int8s_3(i int8, j int8) returns ordered_int8s as $$
declare r two_int8s; begin r := row(i,j); return r; end
$$ language plpgsql
;
CREATE TABLE pt (a int, b text) partition by list (a)
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc2(1)
;
ALTER DATABASE "regression" SET lc_time TO 'C'
;
CREATE INDEX ptif_test1_index ON ONLY ptif_test1 (a)
;
CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS 'SELECT $2;'
;
DROP TABLE test_missing_target2
;
CREATE FUNCTION gbt_float8_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
drop function dummy_hashint4(a int4, seed int8)
;
-- Change plt1_adv_p1 to the default partition
ALTER TABLE plt1_adv ATTACH PARTITION plt1_adv_p1 DEFAULT
;
-- Test a primary key with attributes located in later attnum positions
-- compared to the fk attributes.
create table pktable2 (a int, b int, c int, d int, e int, primary key (d, e))
;
create temp table fullname (first text, last text)
;
CREATE TABLE "public"."prt2_l_p3_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
alter view base_tab_def_view alter d set default 'View default'
;
create temp table nestjsonb (j jsonb)
;
create table trigger_parted (a int primary key) partition by list (a)
;
SECURITY LABEL ON COLUMN t5.g IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
ANALYZE public.inhx
;
-- Drop an extra column, in the middle of the existing set.
alter table header_copytest drop column b
;
--
-- Test Index-only plans on GiST indexes
--

create table gist_tbl (b box, p point, c circle)
;
-- tests of column drop with partition tables and indexes using
-- predicates and expressions.
create table part_column_drop (
  useless_1 int,
  id int,
  useless_2 int,
  d int,
  b int,
  useless_3 int
) partition by range (id)
;
-- should succeed

-- Test wholerow & dropped column handling
ALTER TABLE foo DROP COLUMN f3 CASCADE
;
create extension pg_walinspect
;
CREATE INDEX pt_stats2_index ON ONLY pt_stats2 (a)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
CREATE TABLE uaccount (
    pguser      name primary key,
    seclv       int
)
;
CREATE FUNCTION perl2text(text) RETURNS bool
LANGUAGE plperl
TRANSFORM FOR TYPE bool
AS $$
return shift;
$$
;
CREATE FUNCTION test_lfind32()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
-- And try it with a multicolumn GIN index

DROP INDEX intarrayidx, textarrayidx
;
CREATE TEMP TABLE arrtest2 (i integer ARRAY[4], f float8[], n numeric[], t text[], d timestamp[])
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create function child_del_func()
  returns trigger language plpgsql as
$$
begin
  update parent set bcnt = bcnt - 1 where aid = old.aid;
  return old;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE fprt2 (a int, b int, c varchar) PARTITION BY RANGE(b)
;
----------
[DROP_PROG_OBJ] drop function expensivefunc(int)
;
----------
[CREATE_TABLE] CREATE TABLE fooarr(f1 text, f2 int[], f3 text)
;
----------
[CREATE_TABLE] CREATE TABLE trgfire (i int) PARTITION BY RANGE (i)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28068 RENAME TO reind_con_toast
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28029_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."limit_thousand_v_5" AS
 SELECT "thousand"
   FROM "public"."onek"
  WHERE ("thousand" < 995)
  ORDER BY "thousand"
 LIMIT ALL
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- try altering a non-existent column, should fail
alter table atacc1 alter bar set not null
;
----------
[CREATE_TABLE] CREATE TABLE pp_enumpart (a pp_colors)
							PARTITION BY HASH (a)
;
----------
[CREATE_TABLE] CREATE TABLE test (
    key   integer UNIQUE,
    val   text
  )
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (issn, issn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[DROP_TABLE] DROP TABLE testpub_rf_tbl1
;
----------
[CREATE_TABLE] -- dropping/detaching PARTITIONs is prevented if that would break
-- a foreign key's existing data
CREATE TABLE droppk (a int PRIMARY KEY) PARTITION BY RANGE (a)
;
----------
[DROP_TYPE] DROP TYPE base_type CASCADE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_operator
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26555_2
;
----------
[ADMIN_TABLE] ANALYZE public.pline
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(isbn, isbn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i19'
;
----------
[CREATE_PROG_OBJ] create function build_ordered_int8s_3(i int8, j int8) returns ordered_int8s as $$
declare r two_int8s; begin r := row(i,j); return r; end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE pt (a int, b text) partition by list (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc2(1)
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET lc_time TO 'C'
;
----------
[ADD_INDEX] CREATE INDEX ptif_test1_index ON ONLY ptif_test1 (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS 'SELECT $2;'
;
----------
[DROP_TABLE] DROP TABLE test_missing_target2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float8_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_PROG_OBJ] drop function dummy_hashint4(a int4, seed int8)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Change plt1_adv_p1 to the default partition
ALTER TABLE plt1_adv ATTACH PARTITION plt1_adv_p1 DEFAULT
;
----------
[CREATE_TABLE] -- Test a primary key with attributes located in later attnum positions
-- compared to the fk attributes.
create table pktable2 (a int, b int, c int, d int, e int, primary key (d, e))
;
----------
[CREATE_TABLE] create temp table fullname (first text, last text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_l_p3_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[ALTER_VIEW|ALTER_COLUMN] alter view base_tab_def_view alter d set default 'View default'
;
----------
[CREATE_TABLE] create temp table nestjsonb (j jsonb)
;
----------
[CREATE_TABLE] create table trigger_parted (a int primary key) partition by list (a)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON COLUMN t5.g IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
----------
[ADMIN_TABLE] ANALYZE public.inhx
;
----------
[ALTER_TABLE|DROP_COLUMN] -- Drop an extra column, in the middle of the existing set.
alter table header_copytest drop column b
;
----------
[CREATE_TABLE] --
-- Test Index-only plans on GiST indexes
--

create table gist_tbl (b box, p point, c circle)
;
----------
[CREATE_TABLE] -- tests of column drop with partition tables and indexes using
-- predicates and expressions.
create table part_column_drop (
  useless_1 int,
  id int,
  useless_2 int,
  d int,
  b int,
  useless_3 int
) partition by range (id)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- should succeed

-- Test wholerow & dropped column handling
ALTER TABLE foo DROP COLUMN f3 CASCADE
;
----------
[CREATE_LIBRARY] create extension pg_walinspect
;
----------
[ADD_INDEX] CREATE INDEX pt_stats2_index ON ONLY pt_stats2 (a)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
----------
[CREATE_TABLE] CREATE TABLE uaccount (
    pguser      name primary key,
    seclv       int
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION perl2text(text) RETURNS bool
LANGUAGE plperl
TRANSFORM FOR TYPE bool
AS $$
return shift;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_lfind32()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[DROP_INDEX] -- And try it with a multicolumn GIN index

DROP INDEX intarrayidx, textarrayidx
;
----------
[CREATE_TABLE] CREATE TEMP TABLE arrtest2 (i integer ARRAY[4], f float8[], n numeric[], t text[], d timestamp[])
;
