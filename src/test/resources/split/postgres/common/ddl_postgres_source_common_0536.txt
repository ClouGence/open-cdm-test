drop function polyf(x anycompatiblemultirange, y anycompatiblearray)
;
--
-- injection_points_load()
--
-- Load an injection point already attached.
--
CREATE FUNCTION injection_points_load(IN point_name TEXT)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_load'
LANGUAGE C STRICT PARALLEL UNSAFE
;
CREATE TABLE BOOLTBL1 (f1 bool)
;
ALTER TABLE foo ATTACH PARTITION foo2 FOR VALUES IN (2)
;
ALTER TABLE part_attmp1 RENAME TO part_at2tmp1
;
-- verify that it works to reattaching a child with multiple candidate
-- constraints
ALTER TABLE fk_partitioned_fk_5 DETACH PARTITION fk_partitioned_fk_5_1
;
ALTER SEQUENCE identity_dump_logged_a_seq SET UNLOGGED
;
CREATE FUNCTION "public"."ret_query1"(OUT integer, OUT integer) RETURNS SETOF "record"
    LANGUAGE "plpgsql"
    AS $_$
begin
    $1 := -1;
    $2 := -2;
    return next;
    return query select x + 1, x * 10 from generate_series(0, 10) s (x);
    return next;
end;
$_$
;
CREATE FUNCTION btissn13cmp(issn13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "url_path" WITH "simple"
;
drop view uv_ptv, uv_ptv_wco
;
-- DROP NOT NULL gets rid of both the attnotnull flag and the constraint itself
ALTER TABLE notnull_tbl1 ALTER a DROP NOT NULL
;
ALTER TABLE parent ALTER COLUMN c TYPE numeric
;
ALTER FUNCTION tuple_data_split(oid, bytea, integer, integer, text) PARALLEL RESTRICTED
;
CREATE INDEX gin_idx ON gin_idx_tbl USING gin (data)
;
CREATE TEMP TABLE id_alw3 (i int GENERATED ALWAYS AS IDENTITY)
;
CREATE TEMP TABLE nocols()
;
CREATE TABLE "fkpart5"."pk3" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
ALTER TABLE pg_temp_11.pg_temp_25373_2 ADD COLUMN newdata pg_temp_11.pg_temp_25373
;
DROP PUBLICATION pub1
;
ALTER TABLE reloptions_test SET (toast.autovacuum_vacuum_cost_delay = 24)
;
-- Multi-level partitioning at at referenced end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
  PARTITION BY LIST(f1)
;
CREATE TYPE citext
;
CREATE TABLE testschema.part_3 PARTITION OF testschema.part FOR VALUES IN (3)
;
CREATE TABLE hats (
	hat_name    char(10) primary key,
	hat_color   char(10)      
)
;
CREATE TABLE target2 (tid integer, balance integer)
  WITH (autovacuum_enabled=off)
;
create or replace function raise_test() returns void as $$
begin
  raise notice '% % %', 1, 2, 3
     using errcode = '55001', detail = 'some detail info', hint = 'some hint';
  raise '% % %', 1, 2, 3
     using errcode = 'division_by_zero', detail = 'some detail info';
end;
$$ language plpgsql
;
ALTER TABLE corrupt1 SET (autovacuum_enabled=false)
;
CREATE TABLE tab5 (a int PRIMARY KEY, b int, c int, d int)
;
ANALYZE pg_temp_13.pg_temp_26072
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP COLUMN no_column
;
alter table partitioned drop column a
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
CREATE TABLE dupindexcols AS
  SELECT unique1 as id, stringu2::text as f1 FROM tenk1
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_am
;
create table inh_child () inherits (inh_parent)
;
ANALYZE public.credit_card
;
--
--  Test seg datatype
--

CREATE EXTENSION seg
;
ALTER TABLE split_alter_table.base_table ENABLE TRIGGER base_table_bi
;
create schema if not exists authorization aac
;
-- test header line feature

create temp table copytest3 (
	c1 int,
	"col with , comma" text,
	"col with "" quote"  int)
;
CREATE FUNCTION word_similarity(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE SCHEMA split_table_idx
;
-- coercion test
create or replace function pleast(variadic numeric[])
returns numeric as $$
declare aux numeric = $1[array_lower($1,1)];
begin
  for i in array_lower($1,1)+1..array_upper($1,1) loop
    if $1[i] < aux then aux := $1[i]; end if;
  end loop;
  return aux;
end;
$$ language plpgsql immutable strict
;
alter extension postgres_fdw drop function least_accum(anyelement, variadic anyarray)
;
CREATE TABLE bt_f8_heap (
	seqno 		float8,
	random 		int4
)
;
drop index expr_comp_key_index
;
DROP INDEX jidx_array
;
ALTER TABLE ONLY "public"."sj_t4" ALTER COLUMN "id" SET DEFAULT "nextval"('"public"."sj_t4_id_seq"'::"regclass")
;
ALTER FOREIGN TABLE agg_csv ADD CHECK (a >= 0)
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] drop function polyf(x anycompatiblemultirange, y anycompatiblearray)
;
----------
[CREATE_PROG_OBJ] --
-- injection_points_load()
--
-- Load an injection point already attached.
--
CREATE FUNCTION injection_points_load(IN point_name TEXT)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_load'
LANGUAGE C STRICT PARALLEL UNSAFE
;
----------
[CREATE_TABLE] CREATE TABLE BOOLTBL1 (f1 bool)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE foo ATTACH PARTITION foo2 FOR VALUES IN (2)
;
----------
[RENAME_TABLE] ALTER TABLE part_attmp1 RENAME TO part_at2tmp1
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- verify that it works to reattaching a child with multiple candidate
-- constraints
ALTER TABLE fk_partitioned_fk_5 DETACH PARTITION fk_partitioned_fk_5_1
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE identity_dump_logged_a_seq SET UNLOGGED
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ret_query1"(OUT integer, OUT integer) RETURNS SETOF "record"
    LANGUAGE "plpgsql"
    AS $_$
begin
    $1 := -1;
    $2 := -2;
    return next;
    return query select x + 1, x * 10 from generate_series(0, 10) s (x);
    return next;
end;
$_$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btissn13cmp(issn13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "url_path" WITH "simple"
;
----------
[DROP_VIEW] drop view uv_ptv, uv_ptv_wco
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- DROP NOT NULL gets rid of both the attnotnull flag and the constraint itself
ALTER TABLE notnull_tbl1 ALTER a DROP NOT NULL
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE parent ALTER COLUMN c TYPE numeric
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION tuple_data_split(oid, bytea, integer, integer, text) PARALLEL RESTRICTED
;
----------
[ADD_INDEX] CREATE INDEX gin_idx ON gin_idx_tbl USING gin (data)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE id_alw3 (i int GENERATED ALWAYS AS IDENTITY)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE nocols()
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."pk3" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_25373_2 ADD COLUMN newdata pg_temp_11.pg_temp_25373
;
----------
[DROP_PUB_SUB] DROP PUBLICATION pub1
;
----------
[ALTER_TABLE] ALTER TABLE reloptions_test SET (toast.autovacuum_vacuum_cost_delay = 24)
;
----------
[CREATE_TABLE] -- Multi-level partitioning at at referenced end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
  PARTITION BY LIST(f1)
;
----------
[CREATE_TYPE] CREATE TYPE citext
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part_3 PARTITION OF testschema.part FOR VALUES IN (3)
;
----------
[CREATE_TABLE] CREATE TABLE hats (
	hat_name    char(10) primary key,
	hat_color   char(10)      
)
;
----------
[CREATE_TABLE] CREATE TABLE target2 (tid integer, balance integer)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise notice '% % %', 1, 2, 3
     using errcode = '55001', detail = 'some detail info', hint = 'some hint';
  raise '% % %', 1, 2, 3
     using errcode = 'division_by_zero', detail = 'some detail info';
end;
$$ language plpgsql
;
----------
[ALTER_TABLE] ALTER TABLE corrupt1 SET (autovacuum_enabled=false)
;
----------
[CREATE_TABLE] CREATE TABLE tab5 (a int PRIMARY KEY, b int, c int, d int)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26072
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP COLUMN no_column
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table partitioned drop column a
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE dupindexcols AS
  SELECT unique1 as id, stringu2::text as f1 FROM tenk1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_am
;
----------
[CREATE_TABLE] create table inh_child () inherits (inh_parent)
;
----------
[ADMIN_TABLE] ANALYZE public.credit_card
;
----------
[CREATE_LIBRARY] --
--  Test seg datatype
--

CREATE EXTENSION seg
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table ENABLE TRIGGER base_table_bi
;
----------
[CREATE_SCHEMA] create schema if not exists authorization aac
;
----------
[CREATE_TABLE] -- test header line feature

create temp table copytest3 (
	c1 int,
	"col with , comma" text,
	"col with "" quote"  int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION word_similarity(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_table_idx
;
----------
[CREATE_PROG_OBJ] -- coercion test
create or replace function pleast(variadic numeric[])
returns numeric as $$
declare aux numeric = $1[array_lower($1,1)];
begin
  for i in array_lower($1,1)+1..array_upper($1,1) loop
    if $1[i] < aux then aux := $1[i]; end if;
  end loop;
  return aux;
end;
$$ language plpgsql immutable strict
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw drop function least_accum(anyelement, variadic anyarray)
;
----------
[CREATE_TABLE] CREATE TABLE bt_f8_heap (
	seqno 		float8,
	random 		int4
)
;
----------
[DROP_INDEX] drop index expr_comp_key_index
;
----------
[DROP_INDEX] DROP INDEX jidx_array
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."sj_t4" ALTER COLUMN "id" SET DEFAULT "nextval"('"public"."sj_t4_id_seq"'::"regclass")
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER FOREIGN TABLE agg_csv ADD CHECK (a >= 0)
;
