ALTER INDEX pg_toast.pg_toast_27779_index RENAME TO reind_con_toast_idx
;
CREATE TABLE tt4 (x int)
;
create type float8range as range (subtype = float8, subtype_diff = float8mi)
;
CREATE TABLE partitioned (
	a int
) PARTITION BY LIST ((a LIKE (SELECT 1)))
;
drop table gexec_test
;
CREATE TABLE hash_i4_heap (
	seqno 		int4,
	random 		int4
)
;
ALTER TABLE list_parted2 ADD CONSTRAINT check_b CHECK (b <> 'zz')
;
DROP VIEW main_view
;
CREATE FUNCTION gbt_date_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE TYPE split_cov.int4_pair_range AS RANGE (subtype = integer)
;
-- enum check

create type rainbow as enum ('r','o','g','b','i','v')
;
/* Two records with three columns in order x=x, xx=text, y=y */
DROP TABLE inhg
;
create function sp_simple_func(var1 integer) returns integer
as $$
begin
        return var1 + 10;
end;
$$ language plpgsql PARALLEL SAFE
;
CREATE SUBSCRIPTION regress_sub CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION regress_pub
;
CREATE INDEX expr_key_idx_x_t ON expr_key (x, t)
;
CREATE TABLE "public"."mic_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
ALTER TABLE ONLY "public"."mlparted3" DROP COLUMN "........pg.dropped.6........"
;
CREATE TABLE ctlt10 (LIKE ctlseq1)
;
-- this revealed a bug in the original implementation
CREATE FUNCTION testRegexpResultToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
return ('1' =~ m(0\t2));
$$
;
VACUUM CHAR_TBL
;
ALTER TABLE pg_toast.pg_toast_30445 RENAME TO reind_con_toast
;
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_nonkey
;
CREATE EVENT TRIGGER regress_reindex_end ON ddl_command_end
    WHEN TAG IN ('REINDEX')
    EXECUTE PROCEDURE reindex_end_command()
;
CREATE FUNCTION "public"."logging_minvfunc_strict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$ SELECT $1 || '-' || quote_nullable($2) $_$
;
CREATE INDEX clstrpart_only_idx ON ONLY clstrpart (a)
;
CREATE INDEX macaddridx ON macaddrtmp USING gist ( a )
;
ALTER TABLE attmp ADD COLUMN a int4 default 3
;
CREATE AGGREGATE myaggn16b(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, INITCOND = '{}')
;
CREATE TABLE ptnowner(i int unique) PARTITION BY LIST (i)
;
CREATE OR REPLACE FUNCTION test_slru_page_write(bigint, text) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_write' LANGUAGE C
;
CREATE VIEW routine_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np1.nspname AS sql_identifier) AS routine_schema,
           CAST(nameconcatoid(p1.proname, p1.oid) AS sql_identifier) AS routine_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_proc p1, pg_namespace np1

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = p1.oid
          AND d.refclassid = 'pg_catalog.pg_proc'::regclass
          AND p1.pronamespace = np1.oid
          AND p.prokind IN ('f', 'p') AND p1.prokind IN ('f', 'p')
          AND pg_has_role(p1.proowner, 'USAGE')
;
--
-- array expressions and operators
--

-- table creation and INSERTs
CREATE TEMP TABLE arrtest2 (i integer ARRAY[4], f float8[], n numeric[], t text[], d timestamp[])
;
CREATE TABLE PKTABLE ( ptest1 int PRIMARY KEY, ptest2 text )
;
CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
ALTER INDEX pg_toast.pg_toast_30762_index RENAME TO reind_con_toast_idx
;
CREATE TABLE test_like_5x (p int CHECK (p > 0),
   q int GENERATED ALWAYS AS (p * 2) STORED)
;
create table inh_parent_1(f1 int)
;
CREATE TABLE test_ne (
   a  TIMESTAMP,
   b  NUMERIC
)
;
create unique index j1_id2_idx on j1(id2) where id2 is not null
;
CREATE TABLE new_system_table(id serial primary key, othercol text)
;
CREATE TRIGGER error_stmt_when BEFORE UPDATE OF a ON main_table
FOR EACH STATEMENT WHEN (OLD.* IS DISTINCT FROM NEW.*)
EXECUTE PROCEDURE trigger_func('error_stmt_when')
;
create index on parted_index_col_drop (b, c)
;
alter table pgbench_accounts add primary key (aid)
;
DROP EXTENSION test_slru
;
CREATE VIEW xmlview8 AS SELECT xmlserialize(content 'good' as char(10))
;
drop type xfloat8 cascade
;
CREATE TABLE tab1 (a int PRIMARY KEY, c text DEFAULT 'sub2_tab1', b text) PARTITION BY HASH (a)
;
ANALYZE pg_temp_18.pg_temp_26711
;
CREATE MATERIALIZED VIEW "public"."mvtest_tvmm" AS
 SELECT "sum"("totamt") AS "grandtot"
   FROM "mvtest_mvschema"."mvtest_tvm"
  WITH NO DATA
;
/*
 * text search configuration for yiddish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * yiddish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY yiddish_stem
	(TEMPLATE = snowball, Language = yiddish )
;
------------------------------------------------------------------------------------------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27779_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE tt4 (x int)
;
----------
[CREATE_TYPE] create type float8range as range (subtype = float8, subtype_diff = float8mi)
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) PARTITION BY LIST ((a LIKE (SELECT 1)))
;
----------
[DROP_TABLE] drop table gexec_test
;
----------
[CREATE_TABLE] CREATE TABLE hash_i4_heap (
	seqno 		int4,
	random 		int4
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE list_parted2 ADD CONSTRAINT check_b CHECK (b <> 'zz')
;
----------
[DROP_VIEW] DROP VIEW main_view
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_date_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_TYPE] CREATE TYPE split_cov.int4_pair_range AS RANGE (subtype = integer)
;
----------
[CREATE_TYPE] -- enum check

create type rainbow as enum ('r','o','g','b','i','v')
;
----------
[DROP_TABLE] /* Two records with three columns in order x=x, xx=text, y=y */
DROP TABLE inhg
;
----------
[CREATE_PROG_OBJ] create function sp_simple_func(var1 integer) returns integer
as $$
begin
        return var1 + 10;
end;
$$ language plpgsql PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION regress_pub
;
----------
[ADD_INDEX] CREATE INDEX expr_key_idx_x_t ON expr_key (x, t)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mic_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted3" DROP COLUMN "........pg.dropped.6........"
;
----------
[CREATE_TABLE] CREATE TABLE ctlt10 (LIKE ctlseq1)
;
----------
[CREATE_PROG_OBJ] -- this revealed a bug in the original implementation
CREATE FUNCTION testRegexpResultToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
return ('1' =~ m(0\t2));
$$
;
----------
[ADMIN_TABLE] VACUUM CHAR_TBL
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30445 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_nonkey
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER regress_reindex_end ON ddl_command_end
    WHEN TAG IN ('REINDEX')
    EXECUTE PROCEDURE reindex_end_command()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."logging_minvfunc_strict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$ SELECT $1 || '-' || quote_nullable($2) $_$
;
----------
[ADD_INDEX] CREATE INDEX clstrpart_only_idx ON ONLY clstrpart (a)
;
----------
[ADD_INDEX] CREATE INDEX macaddridx ON macaddrtmp USING gist ( a )
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN a int4 default 3
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn16b(BASETYPE = anyelement, SFUNC = tf2p,
  STYPE = anyarray, INITCOND = '{}')
;
----------
[CREATE_TABLE] CREATE TABLE ptnowner(i int unique) PARTITION BY LIST (i)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_write(bigint, text) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_write' LANGUAGE C
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW routine_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np1.nspname AS sql_identifier) AS routine_schema,
           CAST(nameconcatoid(p1.proname, p1.oid) AS sql_identifier) AS routine_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_proc p1, pg_namespace np1

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = p1.oid
          AND d.refclassid = 'pg_catalog.pg_proc'::regclass
          AND p1.pronamespace = np1.oid
          AND p.prokind IN ('f', 'p') AND p1.prokind IN ('f', 'p')
          AND pg_has_role(p1.proowner, 'USAGE')
;
----------
[CREATE_TABLE] --
-- array expressions and operators
--

-- table creation and INSERTs
CREATE TEMP TABLE arrtest2 (i integer ARRAY[4], f float8[], n numeric[], t text[], d timestamp[])
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE ( ptest1 int PRIMARY KEY, ptest2 text )
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30762_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE test_like_5x (p int CHECK (p > 0),
   q int GENERATED ALWAYS AS (p * 2) STORED)
;
----------
[CREATE_TABLE] create table inh_parent_1(f1 int)
;
----------
[CREATE_TABLE] CREATE TABLE test_ne (
   a  TIMESTAMP,
   b  NUMERIC
)
;
----------
[ADD_INDEX] create unique index j1_id2_idx on j1(id2) where id2 is not null
;
----------
[CREATE_TABLE] CREATE TABLE new_system_table(id serial primary key, othercol text)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER error_stmt_when BEFORE UPDATE OF a ON main_table
FOR EACH STATEMENT WHEN (OLD.* IS DISTINCT FROM NEW.*)
EXECUTE PROCEDURE trigger_func('error_stmt_when')
;
----------
[ADD_INDEX] create index on parted_index_col_drop (b, c)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_accounts add primary key (aid)
;
----------
[DROP_LIBRARY] DROP EXTENSION test_slru
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmlview8 AS SELECT xmlserialize(content 'good' as char(10))
;
----------
[DROP_TYPE] drop type xfloat8 cascade
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (a int PRIMARY KEY, c text DEFAULT 'sub2_tab1', b text) PARTITION BY HASH (a)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_26711
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_tvmm" AS
 SELECT "sum"("totamt") AS "grandtot"
   FROM "mvtest_mvschema"."mvtest_tvm"
  WITH NO DATA
;
----------
[CREATE_POLICY] /*
 * text search configuration for yiddish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * yiddish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY yiddish_stem
	(TEMPLATE = snowball, Language = yiddish )
;
