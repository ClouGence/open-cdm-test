CREATE FUNCTION bt_index_check(index regclass,
    heapallindexed boolean, checkunique boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE itest10 (a int generated by default as identity, b text)
;
CREATE INDEX prt2_adv_b_idx ON prt2_adv (b)
;
CREATE TABLE testschema.part_910 PARTITION OF testschema.part FOR VALUES IN (9, 10)
  PARTITION BY LIST (a) TABLESPACE regress_tblspace
;
-- test default partition behavior for list
ALTER TABLE plt1 DETACH PARTITION plt1_p3
;
CREATE FUNCTION lquery_recv(internal)
RETURNS lquery
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP SEQUENCE seq2
;
VACUUM (PARALLEL 2) information_schema.sql_implementation_info
;
-- BRIN index
CREATE INDEX sro_brin ON sro_tab USING brin ((sro_ifun(a) + sro_ifun(0)))
;
CREATE FUNCTION int_custom_gt(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE "public"."check_tbl" (
    "x" integer,
    CONSTRAINT "check_con" CHECK (("x" > 3))
)
;
-- make a serial column the hard way
CREATE SEQUENCE ss1
;
create index on ma_test (b)
;
-- failed (no role membership)
ALTER OPERATOR CLASS alt_opc3 USING hash SET SCHEMA alt_nsp2
;
-- fail, earthdistance requires it

drop extension earthdistance
;
CREATE POLICY pp1 ON part_document AS PERMISSIVE
    USING (dlevel <= (SELECT seclv FROM uaccount WHERE pguser = current_user))
;
create table p1_c1() inherits (p1)
;
create table mcrparted5 partition of mcrparted for values from (30, 21, 20) to (maxvalue, maxvalue, maxvalue)
;
create table part_abc_2 (a int, c bool, b text)
;
CREATE TABLE part_null_z PARTITION OF list_parted2 FOR VALUES IN (null, 'z')
;
-- fail

drop function polyf(a anyelement, b anyarray,
                    c anycompatible, d anycompatible)
;
DROP TABLE concur_temp_tab_1, concur_temp_tab_2, reindex_temp_before
;
create table depth_b (id int not null primary key)
;
-- OK

ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc2
;
ALTER TABLE pg_toast.pg_toast_30617 RENAME TO reind_con_toast
;
ALTER TABLE ONLY "fkpart6"."pk1"
    ADD CONSTRAINT "pk1_pkey" PRIMARY KEY ("a")
;
/*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
-- Test arrays over domains

create domain posint as int check (value > 0)
;
-- Test aggregate functions and sort ordering

CREATE TEMP TABLE srt (
   name CITEXT
)
;
-- Ensure things work sanely with SET STATISTICS 0
CREATE TABLE ab1 (a INTEGER, b INTEGER)
;
-- Cleanup
DROP TABLE user_logins
;
VACUUM (FREEZE) pg_catalog.pg_conversion
;
ALTER FUNCTION gbt_int4_picksplit(internal, internal) PARALLEL SAFE
;
ALTER TABLE "public"."parted_self_fk"
    ADD CONSTRAINT "parted_self_fk_id_abc_fkey" FOREIGN KEY ("id_abc") REFERENCES "public"."parted_self_fk"("id")
;
ALTER INDEX pg_toast.pg_toast_27937_index RENAME TO reind_con_toast_idx
;
-- yes (counts as function here)

DROP FUNCTION testns.foo()
;
CREATE TABLE hash_f8_heap (
	seqno		int4,
	random 		float8
)
;
/*
 * 5.52
 * ROUTINE_SEQUENCE_USAGE view
 */

CREATE VIEW routine_sequence_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(ns.nspname AS sql_identifier) AS sequence_schema,
           CAST(s.relname AS sql_identifier) AS sequence_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class s, pg_namespace ns

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = s.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND s.relnamespace = ns.oid
          AND s.relkind = 'S'
          AND pg_has_role(s.relowner, 'USAGE')
;
do $$ declare x notice%type; begin end $$
;
create index fkeys2_i on fkeys2 (fkey21, fkey22)
;
CREATE TABLE "public"."clstr_tst" (
    "a" integer NOT NULL,
    "b" integer,
    "c" "text",
    "d" "text"
)
;
CREATE TABLE pk (a int PRIMARY KEY) PARTITION BY LIST (a)
;
CREATE TABLE "public"."radix_text_tbl" (
    "t" "text"
)
;
CREATE TABLE tab_gen_to_nogen (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
CREATE TABLE regtest_table_1 (a int, b text)
;
-- Multi-level partitioning at referencing end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
;
-- COPY
TRUNCATE gtest1
;
DROP INDEX part_drop_index_locking_subpart_idx
;
CREATE FUNCTION func_parted_mod_b() returns trigger as $$
BEGIN
   NEW.b = 2; -- This is changing partition key column.
   return NEW;
END $$ LANGUAGE plpgsql
;
create domain ddef4 int4 DEFAULT nextval('ddef4_seq')
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_index_check(index regclass,
    heapallindexed boolean, checkunique boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE itest10 (a int generated by default as identity, b text)
;
----------
[ADD_INDEX] CREATE INDEX prt2_adv_b_idx ON prt2_adv (b)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part_910 PARTITION OF testschema.part FOR VALUES IN (9, 10)
  PARTITION BY LIST (a) TABLESPACE regress_tblspace
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- test default partition behavior for list
ALTER TABLE plt1 DETACH PARTITION plt1_p3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lquery_recv(internal)
RETURNS lquery
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_SEQUENCE] DROP SEQUENCE seq2
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) information_schema.sql_implementation_info
;
----------
[ADD_INDEX] -- BRIN index
CREATE INDEX sro_brin ON sro_tab USING brin ((sro_ifun(a) + sro_ifun(0)))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_gt(int_custom, int_custom)
RETURNS boolean
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."check_tbl" (
    "x" integer,
    CONSTRAINT "check_con" CHECK (("x" > 3))
)
;
----------
[CREATE_SEQUENCE] -- make a serial column the hard way
CREATE SEQUENCE ss1
;
----------
[ADD_INDEX] create index on ma_test (b)
;
----------
[ALTER_PROG_OBJ] -- failed (no role membership)
ALTER OPERATOR CLASS alt_opc3 USING hash SET SCHEMA alt_nsp2
;
----------
[DROP_LIBRARY] -- fail, earthdistance requires it

drop extension earthdistance
;
----------
[CREATE_POLICY] CREATE POLICY pp1 ON part_document AS PERMISSIVE
    USING (dlevel <= (SELECT seclv FROM uaccount WHERE pguser = current_user))
;
----------
[CREATE_TABLE] create table p1_c1() inherits (p1)
;
----------
[CREATE_TABLE] create table mcrparted5 partition of mcrparted for values from (30, 21, 20) to (maxvalue, maxvalue, maxvalue)
;
----------
[CREATE_TABLE] create table part_abc_2 (a int, c bool, b text)
;
----------
[CREATE_TABLE] CREATE TABLE part_null_z PARTITION OF list_parted2 FOR VALUES IN (null, 'z')
;
----------
[DROP_PROG_OBJ] -- fail

drop function polyf(a anyelement, b anyarray,
                    c anycompatible, d anycompatible)
;
----------
[DROP_TABLE] DROP TABLE concur_temp_tab_1, concur_temp_tab_2, reindex_temp_before
;
----------
[CREATE_TABLE] create table depth_b (id int not null primary key)
;
----------
[ALTER_PROG_OBJ] -- OK

ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc2
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30617 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart6"."pk1"
    ADD CONSTRAINT "pk1_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_POLICY] /*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
----------
[CREATE_TYPE] -- Test arrays over domains

create domain posint as int check (value > 0)
;
----------
[CREATE_TABLE] -- Test aggregate functions and sort ordering

CREATE TEMP TABLE srt (
   name CITEXT
)
;
----------
[CREATE_TABLE] -- Ensure things work sanely with SET STATISTICS 0
CREATE TABLE ab1 (a INTEGER, b INTEGER)
;
----------
[DROP_TABLE] -- Cleanup
DROP TABLE user_logins
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_conversion
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int4_picksplit(internal, internal) PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."parted_self_fk"
    ADD CONSTRAINT "parted_self_fk_id_abc_fkey" FOREIGN KEY ("id_abc") REFERENCES "public"."parted_self_fk"("id")
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27937_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] -- yes (counts as function here)

DROP FUNCTION testns.foo()
;
----------
[CREATE_TABLE] CREATE TABLE hash_f8_heap (
	seqno		int4,
	random 		float8
)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.52
 * ROUTINE_SEQUENCE_USAGE view
 */

CREATE VIEW routine_sequence_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(ns.nspname AS sql_identifier) AS sequence_schema,
           CAST(s.relname AS sql_identifier) AS sequence_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class s, pg_namespace ns

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = s.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND s.relnamespace = ns.oid
          AND s.relkind = 'S'
          AND pg_has_role(s.relowner, 'USAGE')
;
----------
[BLOCK] do $$ declare x notice%type; begin end $$
;
----------
[ADD_INDEX] create index fkeys2_i on fkeys2 (fkey21, fkey22)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."clstr_tst" (
    "a" integer NOT NULL,
    "b" integer,
    "c" "text",
    "d" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE pk (a int PRIMARY KEY) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."radix_text_tbl" (
    "t" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE tab_gen_to_nogen (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_TABLE] CREATE TABLE regtest_table_1 (a int, b text)
;
----------
[CREATE_TABLE] -- Multi-level partitioning at referencing end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
;
----------
[TRUNCATE_TABLE] -- COPY
TRUNCATE gtest1
;
----------
[DROP_INDEX] DROP INDEX part_drop_index_locking_subpart_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_parted_mod_b() returns trigger as $$
BEGIN
   NEW.b = 2; -- This is changing partition key column.
   return NEW;
END $$ LANGUAGE plpgsql
;
----------
[CREATE_TYPE] create domain ddef4 int4 DEFAULT nextval('ddef4_seq')
;
