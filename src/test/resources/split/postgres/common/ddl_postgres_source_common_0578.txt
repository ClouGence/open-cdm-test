create table loct2 (a int check (a in (2)), b text)
;
CREATE OPERATOR CLASS part_test_int4_ops_bad FOR TYPE int4 USING hash AS
  FUNCTION 2 part_hashint4_error(int4, int8)
;
create table descr_t2 ( id serial primary key , t text )
;
DROP TABLE IF EXISTS no_such_table
;
CREATE TABLE "public"."subselect_tbl" (
    "f1" integer,
    "f2" integer,
    "f3" double precision
)
;
CREATE TABLE part3 PARTITION OF pa_target FOR VALUES IN (3,8,9)
  WITH (autovacuum_enabled=off)
;
--
--  Input and output functions.
--
CREATE FUNCTION citextin(cstring)
RETURNS citext
AS 'textin'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE list_parted2 ALTER b SET NOT NULL, ADD CONSTRAINT check_a2 CHECK (a > 0)
;
ALTER TABLE logged1 SET UNLOGGED
;
CREATE OPERATOR + (
	LEFTARG = _int4,
	RIGHTARG = int4,
	PROCEDURE = intarray_push_elem
)
;
CREATE FUNCTION test_argresult_array_domain_array(x ordered_pair_domain[])
  RETURNS ordered_pair_domain AS $$
begin
return x[1];
end
$$ LANGUAGE plpgsql
;
CREATE TABLE quuux2 PARTITION OF quuux FOR VALUES IN (2)
;
CREATE INDEX fooi ON foo (f1 DESC)
;
drop table refd_table cascade
;
CREATE TABLE "public"."pagg_tab_para_p2" (
    "x" integer,
    "y" integer
)
WITH ("parallel_workers"='0')
;
DROP TABLE part2
;
DROP TABLE trigger_tab_stats
;
create table public.whereami (f1 text)
;
CREATE SCHEMA pub_test
;
CREATE OPERATOR @> (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contains,
	COMMUTATOR = '<@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
DROP SCHEMA collate_tests CASCADE
;
-- fail, need lc_ctype
CREATE COLLATION testx (locale = 'nonsense')
;
-- PROCESS_TOAST option
ALTER TABLE vactst ADD COLUMN t TEXT
;
create rule irule2 as on insert to inserttest2 do also
  insert into inserttestb (f4[1].if1, f4[1].if2[2])
  values (1,'fool'),(new.f1,new.f2)
;
ALTER TABLE T ALTER COLUMN c_interval DROP DEFAULT,
              ALTER COLUMN c_hugetext SET DEFAULT repeat('poiuyt', 1000)
;
-- error
ALTER INDEX dummy_test_idx SET (option_bool = 'val4')
;
-- no such type1
drop operator = (nonesuch, int4)
;
alter table atacc1 add primary key("........pg.dropped.1........")
;
ALTER TABLE pg_toast.pg_toast_28108 RENAME TO reind_con_toast
;
CREATE SUBSCRIPTION admin_sub CONNECTION 'port=32571 host=/tmp/0jj507HGD9 dbname=postgres' PUBLICATION alice
	WITH (run_as_owner = true, password_required = false)
;
ALTER TABLE list_parted2 ALTER COLUMN b TYPE text
;
CREATE OR REPLACE FUNCTION func_int_custom (p_value int_custom)
  RETURNS int_custom LANGUAGE plpgsql AS $$
  BEGIN
    RETURN p_value;
  END; $$
;
drop table trig_table
;
ALTER INDEX "regress_indexing"."pk5_pkey" ATTACH PARTITION "regress_indexing"."pk52_pkey"
;
/*
 * 6.56
 * SCHEMATA view
 */

CREATE VIEW schemata AS
    SELECT CAST(current_database() AS sql_identifier) AS catalog_name,
           CAST(n.nspname AS sql_identifier) AS schema_name,
           CAST(u.rolname AS sql_identifier) AS schema_owner,
           CAST(null AS sql_identifier) AS default_character_set_catalog,
           CAST(null AS sql_identifier) AS default_character_set_schema,
           CAST(null AS sql_identifier) AS default_character_set_name,
           CAST(null AS character_data) AS sql_path
    FROM pg_namespace n, pg_authid u
    WHERE n.nspowner = u.oid
          AND (pg_has_role(n.nspowner, 'USAGE')
               OR has_schema_privilege(n.oid, 'CREATE, USAGE'))
;
create trigger a after insert on grandparent
for each row execute procedure f()
;
ALTER SUBSCRIPTION regress_sub CONNECTION 'port=31164 host=/tmp/7vA5ZgkYXy dbname=postgres'
;
ALTER OPERATOR ~ (hstore, hstore)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE INDEX "fdeps_abc_idx" ON "public"."functional_dependencies" USING "btree" ("a", "b", "c")
;
VACUUM (FULL) pg_catalog.pg_type
;
ALTER TABLE ONLY "public"."prt2_n" ATTACH PARTITION "public"."prt2_n_p2" FOR VALUES IN ('0001', '0005', '0002', '0009', '0008', '0011')
;
-- A DO ALSO rule should cause each row to be inserted twice. The first
-- insert should behave the same as an auto-updatable view (using table
-- defaults, unless overridden by view defaults). The second insert should
-- behave the same as a rule-updatable view (inserting NULLs where there are
-- no view defaults).
drop rule base_tab_def_view_ins_rule on base_tab_def_view
;
CREATE TABLE alpha (a double precision, b int, c text) PARTITION BY RANGE (a)
;
CREATE TABLE upserttest(key text, data text)
;
-- just generally bad types (with and without column references on the referenced table)
create table fktable(ftest1 cidr, ftest2 int[], foreign key (ftest1, ftest2) references pktable)
;
ALTER TABLE pg_temp_11.pg_temp_41099_2 ADD COLUMN newdata pg_temp_11.pg_temp_41099
;
-- Check handling of invalid indexes
CREATE TABLE concur_reindex_tab4 (c1 int)
;
/*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_largeobject_metadata
;
create table domconnotnulltest
( col1 connotnull
, col2 connotnull
)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table loct2 (a int check (a in (2)), b text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS part_test_int4_ops_bad FOR TYPE int4 USING hash AS
  FUNCTION 2 part_hashint4_error(int4, int8)
;
----------
[CREATE_TABLE] create table descr_t2 ( id serial primary key , t text )
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS no_such_table
;
----------
[CREATE_TABLE] CREATE TABLE "public"."subselect_tbl" (
    "f1" integer,
    "f2" integer,
    "f3" double precision
)
;
----------
[CREATE_TABLE] CREATE TABLE part3 PARTITION OF pa_target FOR VALUES IN (3,8,9)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] --
--  Input and output functions.
--
CREATE FUNCTION citextin(cstring)
RETURNS citext
AS 'textin'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN|ADD_CONSTRAINT] ALTER TABLE list_parted2 ALTER b SET NOT NULL, ADD CONSTRAINT check_a2 CHECK (a > 0)
;
----------
[ALTER_TABLE] ALTER TABLE logged1 SET UNLOGGED
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR + (
	LEFTARG = _int4,
	RIGHTARG = int4,
	PROCEDURE = intarray_push_elem
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_argresult_array_domain_array(x ordered_pair_domain[])
  RETURNS ordered_pair_domain AS $$
begin
return x[1];
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE quuux2 PARTITION OF quuux FOR VALUES IN (2)
;
----------
[ADD_INDEX] CREATE INDEX fooi ON foo (f1 DESC)
;
----------
[DROP_TABLE] drop table refd_table cascade
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_para_p2" (
    "x" integer,
    "y" integer
)
WITH ("parallel_workers"='0')
;
----------
[DROP_TABLE] DROP TABLE part2
;
----------
[DROP_TABLE] DROP TABLE trigger_tab_stats
;
----------
[CREATE_TABLE] create table public.whereami (f1 text)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA pub_test
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @> (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contains,
	COMMUTATOR = '<@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[DROP_SCHEMA] DROP SCHEMA collate_tests CASCADE
;
----------
[CREATE_POLICY] -- fail, need lc_ctype
CREATE COLLATION testx (locale = 'nonsense')
;
----------
[ALTER_TABLE|ADD_COLUMN] -- PROCESS_TOAST option
ALTER TABLE vactst ADD COLUMN t TEXT
;
----------
[CREATE_POLICY] create rule irule2 as on insert to inserttest2 do also
  insert into inserttestb (f4[1].if1, f4[1].if2[2])
  values (1,'fool'),(new.f1,new.f2)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE T ALTER COLUMN c_interval DROP DEFAULT,
              ALTER COLUMN c_hugetext SET DEFAULT repeat('poiuyt', 1000)
;
----------
[ALTER_INDEX] -- error
ALTER INDEX dummy_test_idx SET (option_bool = 'val4')
;
----------
[DROP_PROG_OBJ] -- no such type1
drop operator = (nonesuch, int4)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add primary key("........pg.dropped.1........")
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28108 RENAME TO reind_con_toast
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION admin_sub CONNECTION 'port=32571 host=/tmp/0jj507HGD9 dbname=postgres' PUBLICATION alice
	WITH (run_as_owner = true, password_required = false)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE list_parted2 ALTER COLUMN b TYPE text
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION func_int_custom (p_value int_custom)
  RETURNS int_custom LANGUAGE plpgsql AS $$
  BEGIN
    RETURN p_value;
  END; $$
;
----------
[DROP_TABLE] drop table trig_table
;
----------
[ALTER_INDEX] ALTER INDEX "regress_indexing"."pk5_pkey" ATTACH PARTITION "regress_indexing"."pk52_pkey"
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.56
 * SCHEMATA view
 */

CREATE VIEW schemata AS
    SELECT CAST(current_database() AS sql_identifier) AS catalog_name,
           CAST(n.nspname AS sql_identifier) AS schema_name,
           CAST(u.rolname AS sql_identifier) AS schema_owner,
           CAST(null AS sql_identifier) AS default_character_set_catalog,
           CAST(null AS sql_identifier) AS default_character_set_schema,
           CAST(null AS sql_identifier) AS default_character_set_name,
           CAST(null AS character_data) AS sql_path
    FROM pg_namespace n, pg_authid u
    WHERE n.nspowner = u.oid
          AND (pg_has_role(n.nspowner, 'USAGE')
               OR has_schema_privilege(n.oid, 'CREATE, USAGE'))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger a after insert on grandparent
for each row execute procedure f()
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_sub CONNECTION 'port=31164 host=/tmp/7vA5ZgkYXy dbname=postgres'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ~ (hstore, hstore)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ADD_INDEX] CREATE INDEX "fdeps_abc_idx" ON "public"."functional_dependencies" USING "btree" ("a", "b", "c")
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_type
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_n" ATTACH PARTITION "public"."prt2_n_p2" FOR VALUES IN ('0001', '0005', '0002', '0009', '0008', '0011')
;
----------
[DROP_POLICY] -- A DO ALSO rule should cause each row to be inserted twice. The first
-- insert should behave the same as an auto-updatable view (using table
-- defaults, unless overridden by view defaults). The second insert should
-- behave the same as a rule-updatable view (inserting NULLs where there are
-- no view defaults).
drop rule base_tab_def_view_ins_rule on base_tab_def_view
;
----------
[CREATE_TABLE] CREATE TABLE alpha (a double precision, b int, c text) PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE] CREATE TABLE upserttest(key text, data text)
;
----------
[CREATE_TABLE] -- just generally bad types (with and without column references on the referenced table)
create table fktable(ftest1 cidr, ftest2 int[], foreign key (ftest1, ftest2) references pktable)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_41099_2 ADD COLUMN newdata pg_temp_11.pg_temp_41099
;
----------
[CREATE_TABLE] -- Check handling of invalid indexes
CREATE TABLE concur_reindex_tab4 (c1 int)
;
----------
[CREATE_POLICY] /*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_TABLE] create table domconnotnulltest
( col1 connotnull
, col2 connotnull
)
;
