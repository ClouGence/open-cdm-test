create trigger check_fkeys2_fkey_restrict
	before delete or update on fkeys2
	for each row
	execute procedure check_foreign_key (1, 'restrict', 'pkey23', 'fkeys', 'fkey3')
;
SECURITY LABEL ON VIEW dummy_seclabel_view1 IS 'classified'
;
create function plpgsql_domain_check(val int) returns boolean as $$
begin return val > 0; end
$$ language plpgsql immutable
;
CREATE TABLE ex_mtarget (a int, b int)
  WITH (autovacuum_enabled=off)
;
CREATE TABLE "public"."prt3_n_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
-- error
-- non-concurrent drop is enforced here, so it is a valid case.
drop index concurrently idxpart_temp_a_idx
;
CREATE TABLE "public"."invalid_check_con" (
    "f1" integer
)
;
ALTER TABLE e_star* RENAME COLUMN e TO ee
;
CREATE AGGREGATE "public"."newcnt"(*) (
    SFUNC = "int8inc",
    STYPE = bigint,
    INITCOND = '0',
    PARALLEL = safe
)
;
drop view v_test1
;
CREATE TABLE dump_test.test_compression_method (
						   col1 text
					   )
;
CREATE FUNCTION handle_get()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE OPERATOR <> (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_different,
	COMMUTATOR = '<>',
	NEGATOR = '=',
	RESTRICT = neqsel,
	JOIN = neqjoinsel
)
;
ALTER INDEX pg_toast.pg_toast_31025_index RENAME TO reind_con_toast_idx
;
/*
 * 6.53
 * ROUTINE_SEQUENCE_USAGE view
 */

CREATE VIEW routine_sequence_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(ns.nspname AS sql_identifier) AS sequence_schema,
           CAST(s.relname AS sql_identifier) AS sequence_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class s, pg_namespace ns

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = s.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND s.relnamespace = ns.oid
          AND s.relkind = 'S'
          AND pg_has_role(s.relowner, 'USAGE')
;
CREATE DATABASE testdb1
;
--
-- range PK: test with existing rows
--

ALTER TABLE temporal_rng DROP CONSTRAINT temporal_rng_pk
;
CREATE OR REPLACE FUNCTION dup (inout f2 anyelement, out f3 anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
create table partitioned (a int, b int)
  partition by list ((partitioned))
;
CREATE TABLE fk_partitioned_fk_6_1 PARTITION OF fk_partitioned_fk_6 FOR VALUES IN (1)
;
create table rp_prefix_test2 (a int, b int, c int) partition by range(a, b, c)
;
-- should fail, can't have same filter variable twice
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('create table') and tag in ('CREATE FUNCTION')
   execute procedure test_event_trigger()
;
drop table foo cascade
;
-- Check that opclass parameters are preserved
CREATE TABLE concur_appclass_tab(i tsvector, j tsvector, k tsvector)
;
CREATE FUNCTION test_result_nnint_container(x int, y int)
  RETURNS nnint_container AS $$
begin
return row(x, y)::nnint_container;
end
$$ LANGUAGE plpgsql
;
create table matest3 (id integer primary key) inherits (matest0)
;
ALTER TABLE fk_partitioned_fk_4 ATTACH PARTITION fk_partitioned_fk_4_2 FOR VALUES FROM (100,100) TO (1000,1000)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_trigger
;
ALTER INDEX pg_toast.pg_toast_30666_index RENAME TO reind_con_toast_idx
;
ALTER TABLE articles DROP CONSTRAINT articles_pkey RESTRICT
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub4
;
CREATE INDEX tbl_gist_idx ON tbl_gist using gist (c4) INCLUDE (c1,c2,c3)
;
DROP SCHEMA testns4 CASCADE
;
CREATE TABLE FKTABLE_FAIL3 ( ftest1 int, CONSTRAINT fkfail1 FOREIGN KEY (tableoid) REFERENCES PKTABLE(someoid))
;
ALTER TABLE gtest25 ADD COLUMN x int GENERATED ALWAYS AS (b * 4) STORED
;
ALTER TABLE fail_part ADD CONSTRAINT check_a CHECK (a >= 0)
;
CREATE OPERATOR %# (
       RIGHTARG = hstore,
       PROCEDURE = hstore_to_matrix
)
;
CREATE VIEW pg_stat_archiver AS
    SELECT
        s.archived_count,
        s.last_archived_wal,
        s.last_archived_time,
        s.failed_count,
        s.last_failed_wal,
        s.last_failed_time,
        s.stats_reset
    FROM pg_stat_get_archiver() s
;
CREATE TABLESPACE ts1 LOCATION '/tmp/CM9qkLbLpG'
;
CREATE TABLE "public"."b_star" (
    "class" character(1),
    "aa" integer,
    "bb" "text",
    "a" "text"
)
;
ALTER TABLE pg_default_acl ADD PRIMARY KEY USING INDEX pg_default_acl_oid_index
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=29486 host=/tmp/VFPPd7IrOU dbname=postgres' PUBLICATION mypub
;
ALTER TABLE s2 ENABLE ROW LEVEL SECURITY
;
ANALYZE pg_catalog.pg_type
;
CREATE TEMP VIEW v_window AS
	SELECT i, min(i) over (order by i range between '1 day' preceding and '10 days' following) as min_i
  FROM generate_series(now(), now()+'100 days'::interval, '1 hour') i
;
CREATE INDEX bt_name_index ON bt_name_heap USING btree (seqno name_ops)
;
-- partitioned table children
CREATE TABLE t1p_ones PARTITION OF t1p FOR VALUES FROM ('0') TO ('10')
;
-- INDEX_CLEANUP option
CREATE TABLE no_index_cleanup (i INT PRIMARY KEY, t TEXT)
;
create or replace function foreach_test(anyarray)
returns void as $$
declare x xy_tuple[];
begin
  foreach x slice 1 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
--
-- Tables and rules for the logging test
--
create table rtest_emp (ename char(20), salary money)
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger check_fkeys2_fkey_restrict
	before delete or update on fkeys2
	for each row
	execute procedure check_foreign_key (1, 'restrict', 'pkey23', 'fkeys', 'fkey3')
;
----------
[SECURITY_LABEL] SECURITY LABEL ON VIEW dummy_seclabel_view1 IS 'classified'
;
----------
[CREATE_PROG_OBJ] create function plpgsql_domain_check(val int) returns boolean as $$
begin return val > 0; end
$$ language plpgsql immutable
;
----------
[CREATE_TABLE] CREATE TABLE ex_mtarget (a int, b int)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt3_n_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[DROP_INDEX] -- error
-- non-concurrent drop is enforced here, so it is a valid case.
drop index concurrently idxpart_temp_a_idx
;
----------
[CREATE_TABLE] CREATE TABLE "public"."invalid_check_con" (
    "f1" integer
)
;
----------
[RENAME_COLUMN] ALTER TABLE e_star* RENAME COLUMN e TO ee
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."newcnt"(*) (
    SFUNC = "int8inc",
    STYPE = bigint,
    INITCOND = '0',
    PARALLEL = safe
)
;
----------
[DROP_VIEW] drop view v_test1
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_compression_method (
						   col1 text
					   )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_get()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_different,
	COMMUTATOR = '<>',
	NEGATOR = '=',
	RESTRICT = neqsel,
	JOIN = neqjoinsel
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_31025_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.53
 * ROUTINE_SEQUENCE_USAGE view
 */

CREATE VIEW routine_sequence_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(ns.nspname AS sql_identifier) AS sequence_schema,
           CAST(s.relname AS sql_identifier) AS sequence_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class s, pg_namespace ns

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = s.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND s.relnamespace = ns.oid
          AND s.relkind = 'S'
          AND pg_has_role(s.relowner, 'USAGE')
;
----------
[CREATE_CATALOG] CREATE DATABASE testdb1
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] --
-- range PK: test with existing rows
--

ALTER TABLE temporal_rng DROP CONSTRAINT temporal_rng_pk
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION dup (inout f2 anyelement, out f3 anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
----------
[CREATE_TABLE] create table partitioned (a int, b int)
  partition by list ((partitioned))
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_6_1 PARTITION OF fk_partitioned_fk_6 FOR VALUES IN (1)
;
----------
[CREATE_TABLE] create table rp_prefix_test2 (a int, b int, c int) partition by range(a, b, c)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should fail, can't have same filter variable twice
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('create table') and tag in ('CREATE FUNCTION')
   execute procedure test_event_trigger()
;
----------
[DROP_TABLE] drop table foo cascade
;
----------
[CREATE_TABLE] -- Check that opclass parameters are preserved
CREATE TABLE concur_appclass_tab(i tsvector, j tsvector, k tsvector)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_result_nnint_container(x int, y int)
  RETURNS nnint_container AS $$
begin
return row(x, y)::nnint_container;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] create table matest3 (id integer primary key) inherits (matest0)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk_4 ATTACH PARTITION fk_partitioned_fk_4_2 FOR VALUES FROM (100,100) TO (1000,1000)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_trigger
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30666_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE articles DROP CONSTRAINT articles_pkey RESTRICT
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub4
;
----------
[ADD_INDEX] CREATE INDEX tbl_gist_idx ON tbl_gist using gist (c4) INCLUDE (c1,c2,c3)
;
----------
[DROP_SCHEMA] DROP SCHEMA testns4 CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE_FAIL3 ( ftest1 int, CONSTRAINT fkfail1 FOREIGN KEY (tableoid) REFERENCES PKTABLE(someoid))
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE gtest25 ADD COLUMN x int GENERATED ALWAYS AS (b * 4) STORED
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fail_part ADD CONSTRAINT check_a CHECK (a >= 0)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR %# (
       RIGHTARG = hstore,
       PROCEDURE = hstore_to_matrix
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_archiver AS
    SELECT
        s.archived_count,
        s.last_archived_wal,
        s.last_archived_time,
        s.failed_count,
        s.last_failed_wal,
        s.last_failed_time,
        s.stats_reset
    FROM pg_stat_get_archiver() s
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/tmp/CM9qkLbLpG'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."b_star" (
    "class" character(1),
    "aa" integer,
    "bb" "text",
    "a" "text"
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_default_acl ADD PRIMARY KEY USING INDEX pg_default_acl_oid_index
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=29486 host=/tmp/VFPPd7IrOU dbname=postgres' PUBLICATION mypub
;
----------
[ALTER_TABLE] ALTER TABLE s2 ENABLE ROW LEVEL SECURITY
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_type
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW v_window AS
	SELECT i, min(i) over (order by i range between '1 day' preceding and '10 days' following) as min_i
  FROM generate_series(now(), now()+'100 days'::interval, '1 hour') i
;
----------
[ADD_INDEX] CREATE INDEX bt_name_index ON bt_name_heap USING btree (seqno name_ops)
;
----------
[CREATE_TABLE] -- partitioned table children
CREATE TABLE t1p_ones PARTITION OF t1p FOR VALUES FROM ('0') TO ('10')
;
----------
[CREATE_TABLE] -- INDEX_CLEANUP option
CREATE TABLE no_index_cleanup (i INT PRIMARY KEY, t TEXT)
;
----------
[CREATE_PROG_OBJ] create or replace function foreach_test(anyarray)
returns void as $$
declare x xy_tuple[];
begin
  foreach x slice 1 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
----------
[CREATE_TABLE] --
-- Tables and rules for the logging test
--
create table rtest_emp (ename char(20), salary money)
;
