CREATE FUNCTION gtest_trigger_func4() RETURNS trigger
  LANGUAGE plpgsql
AS $$
BEGIN
  NEW.a = 10;
  NEW.b = 300;
  RETURN NEW;
END;
$$
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_attrdef
;
CREATE TABLE vacuum_tab (a int)
;
CREATE TABLE gtest4 (
    a int,
    b double_int GENERATED ALWAYS AS ((a * 2, a * 3)) STORED
)
;
create table part_abc_3 partition of part_abc for values in (3, 4) partition by range (d)
;
CREATE OPERATOR CLASS interval_ops
DEFAULT FOR TYPE interval USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       interval_cmp(interval,interval),
    FUNCTION        2       gin_extract_value_interval(interval, internal),
    FUNCTION        3       gin_extract_query_interval(interval, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_interval(interval,interval,int2, internal),
STORAGE         interval
;
TRUNCATE tab1, tab2, tab3
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
-- utility "create table" should not be shown
CREATE TEMP TABLE test (a int, b char(20))
;
ANALYZE pg_catalog.pg_default_acl
;
CREATE SCHEMA test_roles_schema AUTHORIZATION pg_signal_backend
;
DROP TYPE textrange2
;
-- verify that attaching and detaching partitions manipulates the inheritance
-- properties of their FK constraints correctly
create schema fkpart2
  create table pkey (a int primary key)
  create table fk_part (a int, constraint fkey foreign key (a) references fkpart2.pkey) partition by list (a)
  create table fk_part_1 partition of fkpart2.fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 (a int, constraint my_fkey foreign key (a) references fkpart2.pkey)
;
CREATE OR REPLACE FUNCTION hs_locks_drop (n integer)
RETURNS void
LANGUAGE plpgsql
AS $$
    BEGIN
      IF n <= 0 THEN
		CHECKPOINT;
		RETURN;
	  END IF;
	  EXECUTE 'DROP TABLE IF EXISTS hs_locks_' || n::text;
      PERFORM hs_locks_drop(n - 1);
      RETURN;
    EXCEPTION WHEN raise_exception THEN NULL; END;
$$
;
CREATE FUNCTION base_fn_in(cstring) RETURNS base_type AS 'boolin'
    LANGUAGE internal IMMUTABLE STRICT
;
create function use_refcursor(rc refcursor) returns int as $$
declare
    rc refcursor;
    x record;
begin
    rc := return_unnamed_refcursor();
    fetch next from rc into x;
    return x.a;
end
$$ language plpgsql
;
CREATE TABLE part2_self_fk PARTITION OF parted_self_fk FOR VALUES FROM (10) TO (20)
;
-- failed
SECURITY LABEL ON COLUMN tpart.o
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
CREATE TABLE testpub_rf_schema1.testpub_rf_tbl5 (h integer)
;
create table inh_child1() inherits(inh_parent)
;
CREATE FOREIGN TABLE fpagg_tab_p1 PARTITION OF pagg_tab FOR VALUES FROM (0) TO (10) SERVER loopback OPTIONS (table_name 'pagg_tab_p1')
;
CREATE TABLE FKTABLE ( ftest1 int DEFAULT -1, ftest2 int DEFAULT -2, ftest3 int, CONSTRAINT constrname2 FOREIGN KEY(ftest1, ftest2)
                       REFERENCES PKTABLE MATCH FULL ON DELETE SET DEFAULT ON UPDATE SET DEFAULT)
;
-- the same with the versioned language name
CREATE FUNCTION test1n(val hstore) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
assert isinstance(val, dict)
plpy.info(sorted(val.items()))
return len(val)
$$
;
ALTER INDEX pg_toast.pg_toast_30607_index RENAME TO reind_con_toast_idx
;
ALTER TABLE test_ext3_table RENAME col_old TO col_new
;
ANALYZE pg_temp_3.pg_temp_29539
;
DROP VIEW fdv3
;
DROP PUBLICATION addr_pub
;
VACUUM (FREEZE) information_schema.sql_packages
;
-- rules bypassing no-op triggers
CREATE RULE european_city_insert_rule AS ON INSERT TO european_city_view
DO INSTEAD INSERT INTO city_view
VALUES (NEW.city_id, NEW.city_name, NEW.population, NEW.country_name, NEW.continent)
RETURNING *
;
create table brin_wi (a int) with (fillfactor = 10)
;
create function tg_room_au() returns trigger as '
begin
    if new.roomno != old.roomno then
        update WSlot set roomno = new.roomno where roomno = old.roomno;
    end if;
    return new;
end;
' language plpgsql
;
COMMENT ON INDEX testcomment_idx1 IS 'test comment'
;
-- test type info caching in json_populate_record()
CREATE TEMP TABLE jspoptest (js json)
;
ALTER OPERATOR FAMILY gist_int2_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_enabled=30.5)
;
create temp table pp_temp_part_1 partition of pp_temp_parent for values in (1)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
--
-- Test DROP behavior of multiply-defined CHECK constraints
--
create table p1(f1 int constraint f1_pos CHECK (f1 > 0))
;
alter table ctrtest attach partition remp1 for values in (1)
;
create table parted_tab_part1 partition of parted_tab for values in (1)
;
ALTER TABLE pg_toast.pg_toast_28623 RENAME TO reind_con_toast
;
DROP TYPE IF EXISTS no_such_schema.foo
;
do $$
declare x var_record_colnn := row(1,null);  -- fail
begin
  raise notice 'x = %', x;
end$$
;
CREATE FUNCTION gbtreekey4_out(gbtreekey4)
RETURNS cstring
AS '$libdir/btree_gist', 'gbtreekey_out'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION tf1p(anyarray,int) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
drop function dfunc(int, int)
;
ALTER TABLE ONLY "public"."alter_table_under_transition_tables" DROP COLUMN "........pg.dropped.2........"
;
CREATE TABLE hard_err(foo widget)
;
CREATE FUNCTION "public"."tg_backlink_unset"(character, character) RETURNS integer
    LANGUAGE "plpgsql"
    AS $_$
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    if mytype = 'PS' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.backlink = blname then
	    update PSlot set backlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'WS' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.backlink = blname then
	    update WSlot set backlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'PL' then
        select into rec * from PLine where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.backlink = blname then
	    update PLine set backlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
end
$_$
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION gtest_trigger_func4() RETURNS trigger
  LANGUAGE plpgsql
AS $$
BEGIN
  NEW.a = 10;
  NEW.b = 300;
  RETURN NEW;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_attrdef
;
----------
[CREATE_TABLE] CREATE TABLE vacuum_tab (a int)
;
----------
[CREATE_TABLE] CREATE TABLE gtest4 (
    a int,
    b double_int GENERATED ALWAYS AS ((a * 2, a * 3)) STORED
)
;
----------
[CREATE_TABLE] create table part_abc_3 partition of part_abc for values in (3, 4) partition by range (d)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS interval_ops
DEFAULT FOR TYPE interval USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       interval_cmp(interval,interval),
    FUNCTION        2       gin_extract_value_interval(interval, internal),
    FUNCTION        3       gin_extract_query_interval(interval, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_interval(interval,interval,int2, internal),
STORAGE         interval
;
----------
[TRUNCATE_TABLE] TRUNCATE tab1, tab2, tab3
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
----------
[CREATE_TABLE] -- utility "create table" should not be shown
CREATE TEMP TABLE test (a int, b char(20))
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_default_acl
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_roles_schema AUTHORIZATION pg_signal_backend
;
----------
[DROP_TYPE] DROP TYPE textrange2
;
----------
[CREATE_SCHEMA] -- verify that attaching and detaching partitions manipulates the inheritance
-- properties of their FK constraints correctly
create schema fkpart2
  create table pkey (a int primary key)
  create table fk_part (a int, constraint fkey foreign key (a) references fkpart2.pkey) partition by list (a)
  create table fk_part_1 partition of fkpart2.fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 (a int, constraint my_fkey foreign key (a) references fkpart2.pkey)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION hs_locks_drop (n integer)
RETURNS void
LANGUAGE plpgsql
AS $$
    BEGIN
      IF n <= 0 THEN
		CHECKPOINT;
		RETURN;
	  END IF;
	  EXECUTE 'DROP TABLE IF EXISTS hs_locks_' || n::text;
      PERFORM hs_locks_drop(n - 1);
      RETURN;
    EXCEPTION WHEN raise_exception THEN NULL; END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION base_fn_in(cstring) RETURNS base_type AS 'boolin'
    LANGUAGE internal IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] create function use_refcursor(rc refcursor) returns int as $$
declare
    rc refcursor;
    x record;
begin
    rc := return_unnamed_refcursor();
    fetch next from rc into x;
    return x.a;
end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE part2_self_fk PARTITION OF parted_self_fk FOR VALUES FROM (10) TO (20)
;
----------
[SECURITY_LABEL] -- failed
SECURITY LABEL ON COLUMN tpart.o
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
----------
[CREATE_TABLE] CREATE TABLE testpub_rf_schema1.testpub_rf_tbl5 (h integer)
;
----------
[CREATE_TABLE] create table inh_child1() inherits(inh_parent)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE fpagg_tab_p1 PARTITION OF pagg_tab FOR VALUES FROM (0) TO (10) SERVER loopback OPTIONS (table_name 'pagg_tab_p1')
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE ( ftest1 int DEFAULT -1, ftest2 int DEFAULT -2, ftest3 int, CONSTRAINT constrname2 FOREIGN KEY(ftest1, ftest2)
                       REFERENCES PKTABLE MATCH FULL ON DELETE SET DEFAULT ON UPDATE SET DEFAULT)
;
----------
[CREATE_PROG_OBJ] -- the same with the versioned language name
CREATE FUNCTION test1n(val hstore) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
assert isinstance(val, dict)
plpy.info(sorted(val.items()))
return len(val)
$$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30607_index RENAME TO reind_con_toast_idx
;
----------
[RENAME_COLUMN] ALTER TABLE test_ext3_table RENAME col_old TO col_new
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_29539
;
----------
[DROP_VIEW] DROP VIEW fdv3
;
----------
[DROP_PUB_SUB] DROP PUBLICATION addr_pub
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) information_schema.sql_packages
;
----------
[CREATE_POLICY] -- rules bypassing no-op triggers
CREATE RULE european_city_insert_rule AS ON INSERT TO european_city_view
DO INSTEAD INSERT INTO city_view
VALUES (NEW.city_id, NEW.city_name, NEW.population, NEW.country_name, NEW.continent)
RETURNING *
;
----------
[CREATE_TABLE] create table brin_wi (a int) with (fillfactor = 10)
;
----------
[CREATE_PROG_OBJ] create function tg_room_au() returns trigger as '
begin
    if new.roomno != old.roomno then
        update WSlot set roomno = new.roomno where roomno = old.roomno;
    end if;
    return new;
end;
' language plpgsql
;
----------
[COMMENT_INDEX] COMMENT ON INDEX testcomment_idx1 IS 'test comment'
;
----------
[CREATE_TABLE] -- test type info caching in json_populate_record()
CREATE TEMP TABLE jspoptest (js json)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_int2_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_enabled=30.5)
;
----------
[CREATE_TABLE] create temp table pp_temp_part_1 partition of pp_temp_parent for values in (1)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
----------
[CREATE_TABLE] --
-- Test DROP behavior of multiply-defined CHECK constraints
--
create table p1(f1 int constraint f1_pos CHECK (f1 > 0))
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table ctrtest attach partition remp1 for values in (1)
;
----------
[CREATE_TABLE] create table parted_tab_part1 partition of parted_tab for values in (1)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28623 RENAME TO reind_con_toast
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS no_such_schema.foo
;
----------
[BLOCK] do $$
declare x var_record_colnn := row(1,null);  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey4_out(gbtreekey4)
RETURNS cstring
AS '$libdir/btree_gist', 'gbtreekey_out'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tf1p(anyarray,int) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
----------
[DROP_PROG_OBJ] drop function dfunc(int, int)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."alter_table_under_transition_tables" DROP COLUMN "........pg.dropped.2........"
;
----------
[CREATE_TABLE] CREATE TABLE hard_err(foo widget)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_backlink_unset"(character, character) RETURNS integer
    LANGUAGE "plpgsql"
    AS $_$
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    if mytype = 'PS' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.backlink = blname then
	    update PSlot set backlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'WS' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.backlink = blname then
	    update WSlot set backlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'PL' then
        select into rec * from PLine where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.backlink = blname then
	    update PLine set backlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
end
$_$
;
