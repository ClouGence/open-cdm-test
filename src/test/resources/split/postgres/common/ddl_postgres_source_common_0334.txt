ALTER TABLE current_check_2 FORCE ROW LEVEL SECURITY
;
ALTER FUNCTION gbtreekey32_out(gbtreekey32) PARALLEL SAFE
;
CREATE TABLE changeresult AS
    SELECT data FROM pg_logical_slot_get_changes('regression_slot', NULL, NULL, 'include-xids', '0', 'skip-empty-xacts', '1')
;
CREATE TRIGGER "tg_chkbacklink" BEFORE INSERT OR UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkbacklink"()
;
CREATE TABLE "fkpart6"."pk11" (
    "a" integer NOT NULL
)
;
DROP TRIGGER trig_d15_20 ON part_d_15_20
;
-- Statistics views

CREATE VIEW pg_stat_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_numscans(C.oid) AS seq_scan,
            pg_stat_get_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_numscans(I.indexrelid))::bigint AS idx_scan,
            sum(pg_stat_get_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_tuples_hot_updated(C.oid) AS n_tup_hot_upd,
            pg_stat_get_live_tuples(C.oid) AS n_live_tup,
            pg_stat_get_dead_tuples(C.oid) AS n_dead_tup,
            pg_stat_get_mod_since_analyze(C.oid) AS n_mod_since_analyze,
            pg_stat_get_last_vacuum_time(C.oid) as last_vacuum,
            pg_stat_get_last_autovacuum_time(C.oid) as last_autovacuum,
            pg_stat_get_last_analyze_time(C.oid) as last_analyze,
            pg_stat_get_last_autoanalyze_time(C.oid) as last_autoanalyze,
            pg_stat_get_vacuum_count(C.oid) AS vacuum_count,
            pg_stat_get_autovacuum_count(C.oid) AS autovacuum_count,
            pg_stat_get_analyze_count(C.oid) AS analyze_count,
            pg_stat_get_autoanalyze_count(C.oid) AS autoanalyze_count
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm')
    GROUP BY C.oid, N.nspname, C.relname
;
ALTER TABLE part_5 ATTACH PARTITION list_parted2 FOR VALUES IN ('b')
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_attrdef
;
CREATE TABLE shoelace_ok (
        ok_name     char(10),
        ok_quant    integer
    )
;
create temp table old_oids as
  select relname, oid as oldoid, relfilenode as oldfilenode
  from pg_class where relname like 'at_partitioned%'
;
alter trigger b on grandparent rename to c
;
CREATE TEMP TABLE pg_temp_9.pg_temp_38133_2 (tid pg_catalog.tid)
;
CREATE POLICY p2 ON rls_tbl_force FOR SELECT USING (c1 = 8)
;
-- inet check

CREATE TABLE inettmp (a inet)
;
create operator alter1.=(procedure = alter1.same, leftarg  = alter1.ctype, rightarg = alter1.ctype)
;
CREATE TEMP TABLE pg_temp_2.pg_temp_28551_2 (tid pg_catalog.tid)
;
CREATE OR REPLACE FUNCTION test_slru_page_readonly(bigint) RETURNS text
  AS '$libdir/test_slru', 'test_slru_page_readonly' LANGUAGE C
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
CREATE FUNCTION testpub_rf_func1(integer, integer) RETURNS boolean AS $$ SELECT hashint4($1) > $2 $$ LANGUAGE SQL
;
CREATE EXTENSION ltree_plpython3u CASCADE
;
CREATE AGGREGATE myaggp10a(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
ALTER TABLE public.test_inh_check ADD CONSTRAINT bnoinherit CHECK ((b > (100)::double precision)) NO INHERIT
;
CREATE INDEX reloptions_test_idx ON reloptions_test (s) WITH (fillfactor=30)
;
CREATE VIEW routine_table_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class t, pg_namespace nt

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = t.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND pg_has_role(t.relowner, 'USAGE')
;
CREATE OPERATOR FAMILY "public"."evttrigopclass" USING "btree"
;
-- should fail

-- check that detaching a partition also detaches the primary key constraint
create table parted_pk_detach_test (a int primary key) partition by list (a)
;
COMMENT ON TEXT SEARCH DICTIONARY serbian_stem IS 'snowball stemmer for serbian language'
;
drop extension cube
;
CREATE FUNCTION unbounded_syntax_test2b(unbounded int) RETURNS TABLE (a int, b int, c int)
LANGUAGE SQL
AS $$
  SELECT sum(unique1) over (rows between unbounded preceding and unbounded following),
         unique1, four
  FROM tenk1 WHERE unique1 < 10;
$$
;
CREATE TABLE parted_si (
  id int not null,
  data text not null,
  -- prevent use of bulk insert by having a volatile function
  rand float8 not null default random()
)
PARTITION BY LIST((id % 2))
;
CREATE EXTENSION test_ext6
;
create table Log ( name text , w text )
;
CREATE TABLE mvtest_foo_data AS SELECT i,
  i+1 AS tid,
  fipshash(random()::text) AS mv,
  fipshash(random()::text) AS newdata,
  fipshash(random()::text) AS newdata2,
  fipshash(random()::text) AS diff
  FROM generate_series(1, 10) i
;
CREATE VIEW atestv4 AS SELECT * FROM atestv3
;
-- Don't support statement-level INSTEAD OF triggers
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_view
EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
CREATE TABLE person (
	name 		text,
	age			int4,
	location 	point
)
;
VACUUM pg_catalog.pg_constraint
;
alter table parted_conflict_test attach partition parted_conflict_test_2 for values in (3)
;
create view rule_v1(x) as values(1,2)
;
DO
LANGUAGE plpgsql
$$
DECLARE
    x int := 3;
    y int := 4;
BEGIN
    CALL test_proc6(2, x, y);
    RAISE INFO 'x = %, y = %', x, y;
END;
$$
;
CREATE FOREIGN TABLE fp PARTITION OF tableam_parted_a_heap2 DEFAULT SERVER x
;
CREATE VIEW pg_replication_origin_status AS
    SELECT *
    FROM pg_show_replication_origin_status()
;
CREATE TYPE gtestdomain1range AS range (subtype = gtestdomain1)
;
ALTER INDEX pg_toast.pg_toast_30825_index RENAME TO reind_con_toast_idx
;
-- no such aggregate
drop aggregate nonesuch (int4)
;
CREATE TABLE "public"."rtest_t5" (
    "a" integer,
    "b" "text"
)
;
CREATE UNIQUE INDEX test_replica_id_full_idxy ON test_replica_id_full(x,y)
;
CREATE FUNCTION isnge(ean13, isbn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE OPERATOR "public".!=- (
    FUNCTION = "factorial",
    RIGHTARG = bigint
)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] ALTER TABLE current_check_2 FORCE ROW LEVEL SECURITY
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbtreekey32_out(gbtreekey32) PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT|LOG_READ|ADMIN_REPLICATION] CREATE TABLE changeresult AS
    SELECT data FROM pg_logical_slot_get_changes('regression_slot', NULL, NULL, 'include-xids', '0', 'skip-empty-xacts', '1')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkbacklink" BEFORE INSERT OR UPDATE ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkbacklink"()
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart6"."pk11" (
    "a" integer NOT NULL
)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_d15_20 ON part_d_15_20
;
----------
[CREATE_VIEW(SELECT)] -- Statistics views

CREATE VIEW pg_stat_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_numscans(C.oid) AS seq_scan,
            pg_stat_get_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_numscans(I.indexrelid))::bigint AS idx_scan,
            sum(pg_stat_get_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_tuples_hot_updated(C.oid) AS n_tup_hot_upd,
            pg_stat_get_live_tuples(C.oid) AS n_live_tup,
            pg_stat_get_dead_tuples(C.oid) AS n_dead_tup,
            pg_stat_get_mod_since_analyze(C.oid) AS n_mod_since_analyze,
            pg_stat_get_last_vacuum_time(C.oid) as last_vacuum,
            pg_stat_get_last_autovacuum_time(C.oid) as last_autovacuum,
            pg_stat_get_last_analyze_time(C.oid) as last_analyze,
            pg_stat_get_last_autoanalyze_time(C.oid) as last_autoanalyze,
            pg_stat_get_vacuum_count(C.oid) AS vacuum_count,
            pg_stat_get_autovacuum_count(C.oid) AS autovacuum_count,
            pg_stat_get_analyze_count(C.oid) AS analyze_count,
            pg_stat_get_autoanalyze_count(C.oid) AS autoanalyze_count
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm')
    GROUP BY C.oid, N.nspname, C.relname
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part_5 ATTACH PARTITION list_parted2 FOR VALUES IN ('b')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_attrdef
;
----------
[CREATE_TABLE] CREATE TABLE shoelace_ok (
        ok_name     char(10),
        ok_quant    integer
    )
;
----------
[CREATE_TABLE|SELECT] create temp table old_oids as
  select relname, oid as oldoid, relfilenode as oldfilenode
  from pg_class where relname like 'at_partitioned%'
;
----------
[RENAME_TRIGGER] alter trigger b on grandparent rename to c
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_38133_2 (tid pg_catalog.tid)
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON rls_tbl_force FOR SELECT USING (c1 = 8)
;
----------
[CREATE_TABLE] -- inet check

CREATE TABLE inettmp (a inet)
;
----------
[CREATE_PROG_OBJ] create operator alter1.=(procedure = alter1.same, leftarg  = alter1.ctype, rightarg = alter1.ctype)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_2.pg_temp_28551_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_readonly(bigint) RETURNS text
  AS '$libdir/test_slru', 'test_slru_page_readonly' LANGUAGE C
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testpub_rf_func1(integer, integer) RETURNS boolean AS $$ SELECT hashint4($1) > $2 $$ LANGUAGE SQL
;
----------
[CREATE_LIBRARY] CREATE EXTENSION ltree_plpython3u CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp10a(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.test_inh_check ADD CONSTRAINT bnoinherit CHECK ((b > (100)::double precision)) NO INHERIT
;
----------
[ADD_INDEX] CREATE INDEX reloptions_test_idx ON reloptions_test (s) WITH (fillfactor=30)
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW routine_table_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class t, pg_namespace nt

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = t.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND pg_has_role(t.relowner, 'USAGE')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY "public"."evttrigopclass" USING "btree"
;
----------
[CREATE_TABLE] -- should fail

-- check that detaching a partition also detaches the primary key constraint
create table parted_pk_detach_test (a int primary key) partition by list (a)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY serbian_stem IS 'snowball stemmer for serbian language'
;
----------
[DROP_LIBRARY] drop extension cube
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION unbounded_syntax_test2b(unbounded int) RETURNS TABLE (a int, b int, c int)
LANGUAGE SQL
AS $$
  SELECT sum(unique1) over (rows between unbounded preceding and unbounded following),
         unique1, four
  FROM tenk1 WHERE unique1 < 10;
$$
;
----------
[CREATE_TABLE] CREATE TABLE parted_si (
  id int not null,
  data text not null,
  -- prevent use of bulk insert by having a volatile function
  rand float8 not null default random()
)
PARTITION BY LIST((id % 2))
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext6
;
----------
[CREATE_TABLE] create table Log ( name text , w text )
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE mvtest_foo_data AS SELECT i,
  i+1 AS tid,
  fipshash(random()::text) AS mv,
  fipshash(random()::text) AS newdata,
  fipshash(random()::text) AS newdata2,
  fipshash(random()::text) AS diff
  FROM generate_series(1, 10) i
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW atestv4 AS SELECT * FROM atestv3
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Don't support statement-level INSTEAD OF triggers
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_view
EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
----------
[CREATE_TABLE] CREATE TABLE person (
	name 		text,
	age			int4,
	location 	point
)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_constraint
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_conflict_test attach partition parted_conflict_test_2 for values in (3)
;
----------
[CREATE_VIEW(SELECT)] create view rule_v1(x) as values(1,2)
;
----------
[BLOCK] DO
LANGUAGE plpgsql
$$
DECLARE
    x int := 3;
    y int := 4;
BEGIN
    CALL test_proc6(2, x, y);
    RAISE INFO 'x = %, y = %', x, y;
END;
$$
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE fp PARTITION OF tableam_parted_a_heap2 DEFAULT SERVER x
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_replication_origin_status AS
    SELECT *
    FROM pg_show_replication_origin_status()
;
----------
[CREATE_TYPE] CREATE TYPE gtestdomain1range AS range (subtype = gtestdomain1)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30825_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] -- no such aggregate
drop aggregate nonesuch (int4)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_t5" (
    "a" integer,
    "b" "text"
)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX test_replica_id_full_idxy ON test_replica_id_full(x,y)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ean13, isbn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".!=- (
    FUNCTION = "factorial",
    RIGHTARG = bigint
)
;
