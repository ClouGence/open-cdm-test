-- no catalog index
-- These are the toast table and index of pg_authid.
REINDEX TABLE CONCURRENTLY pg_toast.pg_toast_1260
;
--
-- We would test collations here, but it's not possible because the error
-- messages tend to be nonportable.
--

--
-- Conversion
--
SET SESSION AUTHORIZATION regress_alter_generic_user1
;
ALTER USER regress_passwordcheck_user1 PASSWORD 'md592350e12ac34e52dd598f90893bb3ae7'
;
SET ROLE regress_file_fdw_user
;
copy parted_copytest from stdin
;
GRANT ALL ON deptest1 TO regress_dep_user1 WITH GRANT OPTION
;
-- not enabled by default, but we want to test it...
SET track_functions TO 'all'
;
COPY check_ign_err2 FROM STDIN WITH (on_error ignore, log_verbosity verbose)
;
REVOKE ALL ON ruletest_t2 FROM regress_rule_user1
;
--
-- We test creation and use of these role names to ensure that the server
-- correctly distinguishes role keywords from quoted names that look like
-- those keywords.  In a test environment, creation of these roles may
-- provoke warnings, so hide the warnings by raising client_min_messages.
--
SET client_min_messages = ERROR
;
------------------------------------------------------------------------------------------
[ADMIN] -- no catalog index
-- These are the toast table and index of pg_authid.
REINDEX TABLE CONCURRENTLY pg_toast.pg_toast_1260
;
----------
[SWITCH_USER] --
-- We would test collations here, but it's not possible because the error
-- messages tend to be nonportable.
--

--
-- Conversion
--
SET SESSION AUTHORIZATION regress_alter_generic_user1
;
----------
[ALTER_USER] ALTER USER regress_passwordcheck_user1 PASSWORD 'md592350e12ac34e52dd598f90893bb3ae7'
;
----------
[SWITCH_ROLE] SET ROLE regress_file_fdw_user
;
----------
[DATA_IMPORT] copy parted_copytest from stdin
;
----------
[GRANT] GRANT ALL ON deptest1 TO regress_dep_user1 WITH GRANT OPTION
;
----------
[SESSION_SETTING_WRITE] -- not enabled by default, but we want to test it...
SET track_functions TO 'all'
;
----------
[DATA_IMPORT] COPY check_ign_err2 FROM STDIN WITH (on_error ignore, log_verbosity verbose)
;
----------
[REVOKE] REVOKE ALL ON ruletest_t2 FROM regress_rule_user1
;
----------
[SESSION_SETTING_WRITE] --
-- We test creation and use of these role names to ensure that the server
-- correctly distinguishes role keywords from quoted names that look like
-- those keywords.  In a test environment, creation of these roles may
-- provoke warnings, so hide the warnings by raising client_min_messages.
--
SET client_min_messages = ERROR
;
