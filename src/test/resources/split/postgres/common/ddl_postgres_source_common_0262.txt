ALTER TABLE ONLY "fkpart4"."droppk2"
    ADD CONSTRAINT "droppk2_pkey" PRIMARY KEY ("a")
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_init_privs
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_ts_dict
;
ALTER TABLE s1.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
ANALYZE pg_temp_11.pg_temp_26215
;
drop function arrayassign1()
;
-- Test whole-row references to the view
create table uv_iocu_tab (a int unique, b text)
;
-- same with arrays
CREATE TABLE test6a (a int, b text[])
;
-- this should succeed because nothing is left in alter1
drop schema alter1
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "float" WITH "simple"
;
CREATE TEMP TABLE pg_temp_4.pg_temp_37808_2 (tid pg_catalog.tid)
;
CREATE OPERATOR "public".#@%# (
    FUNCTION = "factorial",
    RIGHTARG = bigint
)
;
create trigger child2_insert_trig
  after insert on child2 referencing new table as new_table
  for each statement execute procedure dump_insert()
;
-- should work, and drop gc1.name along
alter table c1 drop column name
;
CREATE OR REPLACE FUNCTION
  jsonb_path_query_array(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                         silent boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_query_array'
;
CREATE VIEW pg_stats WITH (security_barrier) AS
    SELECT
        nspname AS schemaname,
        relname AS tablename,
        attname AS attname,
        stainherit AS inherited,
        stanullfrac AS null_frac,
        stawidth AS avg_width,
        stadistinct AS n_distinct,
        CASE
            WHEN stakind1 = 1 THEN stavalues1
            WHEN stakind2 = 1 THEN stavalues2
            WHEN stakind3 = 1 THEN stavalues3
            WHEN stakind4 = 1 THEN stavalues4
            WHEN stakind5 = 1 THEN stavalues5
        END AS most_common_vals,
        CASE
            WHEN stakind1 = 1 THEN stanumbers1
            WHEN stakind2 = 1 THEN stanumbers2
            WHEN stakind3 = 1 THEN stanumbers3
            WHEN stakind4 = 1 THEN stanumbers4
            WHEN stakind5 = 1 THEN stanumbers5
        END AS most_common_freqs,
        CASE
            WHEN stakind1 = 2 THEN stavalues1
            WHEN stakind2 = 2 THEN stavalues2
            WHEN stakind3 = 2 THEN stavalues3
            WHEN stakind4 = 2 THEN stavalues4
            WHEN stakind5 = 2 THEN stavalues5
        END AS histogram_bounds,
        CASE
            WHEN stakind1 = 3 THEN stanumbers1[1]
            WHEN stakind2 = 3 THEN stanumbers2[1]
            WHEN stakind3 = 3 THEN stanumbers3[1]
            WHEN stakind4 = 3 THEN stanumbers4[1]
            WHEN stakind5 = 3 THEN stanumbers5[1]
        END AS correlation,
        CASE
            WHEN stakind1 = 4 THEN stavalues1
            WHEN stakind2 = 4 THEN stavalues2
            WHEN stakind3 = 4 THEN stavalues3
            WHEN stakind4 = 4 THEN stavalues4
            WHEN stakind5 = 4 THEN stavalues5
        END AS most_common_elems,
        CASE
            WHEN stakind1 = 4 THEN stanumbers1
            WHEN stakind2 = 4 THEN stanumbers2
            WHEN stakind3 = 4 THEN stanumbers3
            WHEN stakind4 = 4 THEN stanumbers4
            WHEN stakind5 = 4 THEN stanumbers5
        END AS most_common_elem_freqs,
        CASE
            WHEN stakind1 = 5 THEN stanumbers1
            WHEN stakind2 = 5 THEN stanumbers2
            WHEN stakind3 = 5 THEN stanumbers3
            WHEN stakind4 = 5 THEN stanumbers4
            WHEN stakind5 = 5 THEN stanumbers5
        END AS elem_count_histogram
    FROM pg_statistic s JOIN pg_class c ON (c.oid = s.starelid)
         JOIN pg_attribute a ON (c.oid = attrelid AND attnum = s.staattnum)
         LEFT JOIN pg_namespace n ON (n.oid = c.relnamespace)
    WHERE NOT attisdropped
    AND has_column_privilege(c.oid, a.attnum, 'select')
    AND (c.relrowsecurity = false OR NOT row_security_active(c.oid))
;
ALTER TABLE ONLY "public"."skip_wal_skip_rewrite_index"
    ADD CONSTRAINT "skip_wal_skip_rewrite_index_pkey" PRIMARY KEY ("c")
;
CREATE INDEX ON brin_timestamp_test USING brin (a timestamp_minmax_multi_ops) WITH (pages_per_range=1)
;
CREATE TABLE i_am_a_failure() USING "I do not exist AM"
;
DROP OPERATOR ===(bigint, bigint)
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc3(1)
;
ALTER TABLE c_star* RENAME COLUMN c TO cc
;
ALTER TABLE ONLY "public"."fk_partitioned_fk_2" ATTACH PARTITION "public"."fk_partitioned_fk_2_1" FOR VALUES FROM (0) TO (1000)
;
ALTER TABLE pg_toast.pg_toast_33089 RENAME TO reind_con_toast
;
DROP COLLATION regress_pg_c_utf8
;
alter table pk attach partition pk5 for values from (4000) to (5000)
;
-- error

CREATE TABLE gtestxx_1 (a int NOT NULL, b int)
;
--
-- HASH
--
CREATE INDEX hash_i4_index ON hash_i4_heap USING hash (random int4_ops)
;
CREATE TABLE arrlp (a int[]) PARTITION BY LIST (a)
;
alter table fkpart0.fk_part_56 drop constraint fk_part_a_fkey
;
CREATE TRIGGER invalid_trig BEFORE DELETE ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_del_row')
;
DROP INDEX regtest_pindex_tbl4_y
;
CREATE INDEX idx_cidr ON test_cidr USING gin (i)
;
CREATE FUNCTION gin_extract_query_float4(float4, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER INDEX pg_toast.pg_toast_33190_index RENAME TO reind_con_toast_idx
;
CREATE AGGREGATE myaggn05b(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  INITCOND = '{}')
;
/* contrib/cube/cube--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR <= (cube, cube) SET (
	RESTRICT = scalarlesel, JOIN = scalarlejoinsel
)
;
AlTER EXTENSION pg_stat_statements UPDATE TO '1.9'
;
ANALYZE pg_temp_12.pg_temp_26162_2
;
DROP TABLE source, source2
;
CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
CREATE TABLESPACE regress_tblspace LOCATION '/src/src/bin/pg_upgrade/tmp_check/regress/testtablespace'
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_opfamily
;
CREATE OR REPLACE FUNCTION
  jsonb_path_query_array_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                         silent boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_query_array_tz'
;
CREATE TEMP TABLE pg_temp_9.pg_temp_26388_2 (tid pg_catalog.tid)
;
CREATE PUBLICATION pub FOR TABLE sch2.tbl1_part1 WITH (PUBLISH_VIA_PARTITION_ROOT=1)
;
create table parted1_irreg (b text, fd int, a int)
;
-- tuple_data_split must be restricted because it may fetch TOAST data.
ALTER FUNCTION tuple_data_split(oid, bytea, integer, integer, text) PARALLEL RESTRICTED
;
CREATE TEMP TABLE d (a int, b int)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart4"."droppk2"
    ADD CONSTRAINT "droppk2_pkey" PRIMARY KEY ("a")
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_init_privs
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_ts_dict
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE s1.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26215
;
----------
[DROP_PROG_OBJ] drop function arrayassign1()
;
----------
[CREATE_TABLE] -- Test whole-row references to the view
create table uv_iocu_tab (a int unique, b text)
;
----------
[CREATE_TABLE] -- same with arrays
CREATE TABLE test6a (a int, b text[])
;
----------
[DROP_SCHEMA] -- this should succeed because nothing is left in alter1
drop schema alter1
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "float" WITH "simple"
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_37808_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".#@%# (
    FUNCTION = "factorial",
    RIGHTARG = bigint
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child2_insert_trig
  after insert on child2 referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[ALTER_TABLE|DROP_COLUMN] -- should work, and drop gc1.name along
alter table c1 drop column name
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_query_array(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                         silent boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_query_array'
;
----------
[CREATE_VIEW(SELECT,PERFORMANCE,METADATA)] CREATE VIEW pg_stats WITH (security_barrier) AS
    SELECT
        nspname AS schemaname,
        relname AS tablename,
        attname AS attname,
        stainherit AS inherited,
        stanullfrac AS null_frac,
        stawidth AS avg_width,
        stadistinct AS n_distinct,
        CASE
            WHEN stakind1 = 1 THEN stavalues1
            WHEN stakind2 = 1 THEN stavalues2
            WHEN stakind3 = 1 THEN stavalues3
            WHEN stakind4 = 1 THEN stavalues4
            WHEN stakind5 = 1 THEN stavalues5
        END AS most_common_vals,
        CASE
            WHEN stakind1 = 1 THEN stanumbers1
            WHEN stakind2 = 1 THEN stanumbers2
            WHEN stakind3 = 1 THEN stanumbers3
            WHEN stakind4 = 1 THEN stanumbers4
            WHEN stakind5 = 1 THEN stanumbers5
        END AS most_common_freqs,
        CASE
            WHEN stakind1 = 2 THEN stavalues1
            WHEN stakind2 = 2 THEN stavalues2
            WHEN stakind3 = 2 THEN stavalues3
            WHEN stakind4 = 2 THEN stavalues4
            WHEN stakind5 = 2 THEN stavalues5
        END AS histogram_bounds,
        CASE
            WHEN stakind1 = 3 THEN stanumbers1[1]
            WHEN stakind2 = 3 THEN stanumbers2[1]
            WHEN stakind3 = 3 THEN stanumbers3[1]
            WHEN stakind4 = 3 THEN stanumbers4[1]
            WHEN stakind5 = 3 THEN stanumbers5[1]
        END AS correlation,
        CASE
            WHEN stakind1 = 4 THEN stavalues1
            WHEN stakind2 = 4 THEN stavalues2
            WHEN stakind3 = 4 THEN stavalues3
            WHEN stakind4 = 4 THEN stavalues4
            WHEN stakind5 = 4 THEN stavalues5
        END AS most_common_elems,
        CASE
            WHEN stakind1 = 4 THEN stanumbers1
            WHEN stakind2 = 4 THEN stanumbers2
            WHEN stakind3 = 4 THEN stanumbers3
            WHEN stakind4 = 4 THEN stanumbers4
            WHEN stakind5 = 4 THEN stanumbers5
        END AS most_common_elem_freqs,
        CASE
            WHEN stakind1 = 5 THEN stanumbers1
            WHEN stakind2 = 5 THEN stanumbers2
            WHEN stakind3 = 5 THEN stanumbers3
            WHEN stakind4 = 5 THEN stanumbers4
            WHEN stakind5 = 5 THEN stanumbers5
        END AS elem_count_histogram
    FROM pg_statistic s JOIN pg_class c ON (c.oid = s.starelid)
         JOIN pg_attribute a ON (c.oid = attrelid AND attnum = s.staattnum)
         LEFT JOIN pg_namespace n ON (n.oid = c.relnamespace)
    WHERE NOT attisdropped
    AND has_column_privilege(c.oid, a.attnum, 'select')
    AND (c.relrowsecurity = false OR NOT row_security_active(c.oid))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."skip_wal_skip_rewrite_index"
    ADD CONSTRAINT "skip_wal_skip_rewrite_index_pkey" PRIMARY KEY ("c")
;
----------
[ADD_INDEX] CREATE INDEX ON brin_timestamp_test USING brin (a timestamp_minmax_multi_ops) WITH (pages_per_range=1)
;
----------
[CREATE_TABLE] CREATE TABLE i_am_a_failure() USING "I do not exist AM"
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ===(bigint, bigint)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc3(1)
;
----------
[RENAME_COLUMN] ALTER TABLE c_star* RENAME COLUMN c TO cc
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."fk_partitioned_fk_2" ATTACH PARTITION "public"."fk_partitioned_fk_2_1" FOR VALUES FROM (0) TO (1000)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33089 RENAME TO reind_con_toast
;
----------
[DROP_POLICY] DROP COLLATION regress_pg_c_utf8
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table pk attach partition pk5 for values from (4000) to (5000)
;
----------
[CREATE_TABLE] -- error

CREATE TABLE gtestxx_1 (a int NOT NULL, b int)
;
----------
[ADD_INDEX] --
-- HASH
--
CREATE INDEX hash_i4_index ON hash_i4_heap USING hash (random int4_ops)
;
----------
[CREATE_TABLE] CREATE TABLE arrlp (a int[]) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table fkpart0.fk_part_56 drop constraint fk_part_a_fkey
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig BEFORE DELETE ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_del_row')
;
----------
[DROP_INDEX] DROP INDEX regtest_pindex_tbl4_y
;
----------
[ADD_INDEX] CREATE INDEX idx_cidr ON test_cidr USING gin (i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_float4(float4, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33190_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn05b(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  INITCOND = '{}')
;
----------
[ALTER_PROG_OBJ] /* contrib/cube/cube--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR <= (cube, cube) SET (
	RESTRICT = scalarlesel, JOIN = scalarlejoinsel
)
;
----------
[ALTER_LIBRARY] AlTER EXTENSION pg_stat_statements UPDATE TO '1.9'
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26162_2
;
----------
[DROP_TABLE] DROP TABLE source, source2
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspace LOCATION '/src/src/bin/pg_upgrade/tmp_check/regress/testtablespace'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_opfamily
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_query_array_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                         silent boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_query_array_tz'
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_26388_2 (tid pg_catalog.tid)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub FOR TABLE sch2.tbl1_part1 WITH (PUBLISH_VIA_PARTITION_ROOT=1)
;
----------
[CREATE_TABLE] create table parted1_irreg (b text, fd int, a int)
;
----------
[ALTER_PROG_OBJ] -- tuple_data_split must be restricted because it may fetch TOAST data.
ALTER FUNCTION tuple_data_split(oid, bytea, integer, integer, text) PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TEMP TABLE d (a int, b int)
;
