CREATE TABLE quad_poly_tbl (id int, p polygon)
;
-- fail
CREATE FUNCTION priv_testfunc1(int) RETURNS int AS 'select 2 * $1;' LANGUAGE sql
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c1 OPTIONS (column_name 'C 1')
;
CREATE TABLE users_table_part_1 PARTITION OF users_table_part FOR VALUES FROM (10) TO (20)
;
CREATE FUNCTION gbt_oid_consistent(internal,oid,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE test7 (a text)
;
--
-- tests for "raise" processing
--
create function raise_test1(int) returns int as $$
begin
    raise notice 'This message has too many parameters!', $1;
    return $1;
end;
$$ language plpgsql
;
CREATE AGGREGATE myaggn07b(BASETYPE = anyelement, SFUNC = tfnp, STYPE = int[],
  INITCOND = '{}')
;
-- json extraction functions

CREATE TEMP TABLE test_json (
       json_type text,
       test_json json
)
;
CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C.UTF8')
;
ANALYZE regress_indexing.pk4
;
ALTER TYPE planets ADD VALUE
  'plutoplutoplutoplutoplutoplutoplutoplutoplutoplutoplutoplutoplutopluto'
;
CREATE RULE rw_view1_upd_rule AS ON UPDATE TO rw_view1
  DO INSTEAD UPDATE base_tbl SET b=NEW.b WHERE a=OLD.a RETURNING *
;
alter table tt9 drop column xx
;
create table parted_trigger_3_1 partition of parted_trigger_3 for values from (1) to (3)
;
SECURITY LABEL ON COLUMN t1p_ones.o IS 'system_u:object_r:sepgsql_table_t:s0'
;
DROP CAST IF EXISTS (text AS text)
;
drop table foo2child
;
CREATE TABLE clean_aborted_self(key int, data text)
;
create trigger tg_room_ad after delete
    on Room for each row execute procedure tg_room_ad()
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE FUNCTION uniq(_int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- New functions and views for pg_stat_statements in 1.8
AlTER EXTENSION pg_stat_statements UPDATE TO '1.8'
;
ALTER INDEX t_b_idx ATTACH PARTITION tp_b_a_key
;
-- tests for sequences
create sequence test_sequence start 72057594037927937
;
--
-- The btree indexing operator class.
--

CREATE OPERATOR CLASS citext_ops
DEFAULT FOR TYPE CITEXT USING btree AS
    OPERATOR    1   <  (citext, citext),
    OPERATOR    2   <= (citext, citext),
    OPERATOR    3   =  (citext, citext),
    OPERATOR    4   >= (citext, citext),
    OPERATOR    5   >  (citext, citext),
    FUNCTION    1   citext_cmp(citext, citext)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_shseclabel
;
CREATE TABLE beta_neg_p3 PARTITION OF beta_neg FOR VALUES FROM (350) TO (500)
;
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES FROM ('a', 1) TO ('z')
;
CREATE TABLE harvest(apples integer)
;
ANALYZE pg_catalog.pg_foreign_table
;
ALTER INDEX "fkpart3"."pk_pkey" ATTACH PARTITION "fkpart3"."pk4_pkey"
;
-- clean up
alter event trigger regress_event_trigger disable
;
-- Do the same concurrently
CREATE INDEX CONCURRENTLY sro_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
	WHERE sro_ifun(a + 10) > sro_ifun(10)
;
-- insert/update/delete statement-level triggers on the parent
create trigger trig_ins_before before insert on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
create or replace function rls_f() returns setof rls_t
  stable language sql
  as $$ select exists(select * from rls_t)::text $$
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=26534 host=/tmp/0qySRdF1o7 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
create type jpop as (a text, b int, c timestamp)
;
CREATE TABLE atest12 as
  SELECT x AS a, 10001 - x AS b FROM generate_series(1,10000) x
;
DROP OPERATOR @ (_int4, _int4)
;
alter table parted_trigger attach partition parted_trigger_2 for values from (1000) to (2000)
;
ALTER INDEX pg_toast.pg_toast_30385_index RENAME TO reind_con_toast_idx
;
CREATE DATABASE "regression" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
ALTER INDEX pg_toast.pg_toast_30516_index RENAME TO reind_con_toast_idx
;
alter type insenum add value 'i29' before 'L2'
;
-- Try (and fail) to add constraint due to invalid destination columns explicitly given
ALTER TABLE attmp3 add constraint attmpconstr foreign key(a) references attmp2(b) match full
;
CREATE FUNCTION g_intbig_same(intbig_gkey, intbig_gkey, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_30592 RENAME TO reind_con_toast
;
create view rw_view1 as select b as bb, c as cc, a as aa from base_tbl
;
alter index idxpart_pkey attach partition idxpart1_a_idx
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE quad_poly_tbl (id int, p polygon)
;
----------
[CREATE_PROG_OBJ] -- fail
CREATE FUNCTION priv_testfunc1(int) RETURNS int AS 'select 2 * $1;' LANGUAGE sql
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c1 OPTIONS (column_name 'C 1')
;
----------
[CREATE_TABLE] CREATE TABLE users_table_part_1 PARTITION OF users_table_part FOR VALUES FROM (10) TO (20)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_oid_consistent(internal,oid,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE test7 (a text)
;
----------
[CREATE_PROG_OBJ] --
-- tests for "raise" processing
--
create function raise_test1(int) returns int as $$
begin
    raise notice 'This message has too many parameters!', $1;
    return $1;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn07b(BASETYPE = anyelement, SFUNC = tfnp, STYPE = int[],
  INITCOND = '{}')
;
----------
[CREATE_TABLE] -- json extraction functions

CREATE TEMP TABLE test_json (
       json_type text,
       test_json json
)
;
----------
[CREATE_POLICY] CREATE COLLATION regress_pg_c_utf8 (
  provider = builtin, locale = 'C.UTF8')
;
----------
[ADMIN_TABLE] ANALYZE regress_indexing.pk4
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE
  'plutoplutoplutoplutoplutoplutoplutoplutoplutoplutoplutoplutoplutopluto'
;
----------
[CREATE_POLICY] CREATE RULE rw_view1_upd_rule AS ON UPDATE TO rw_view1
  DO INSTEAD UPDATE base_tbl SET b=NEW.b WHERE a=OLD.a RETURNING *
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table tt9 drop column xx
;
----------
[CREATE_TABLE] create table parted_trigger_3_1 partition of parted_trigger_3 for values from (1) to (3)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON COLUMN t1p_ones.o IS 'system_u:object_r:sepgsql_table_t:s0'
;
----------
[DROP_PROG_OBJ] DROP CAST IF EXISTS (text AS text)
;
----------
[DROP_TABLE] drop table foo2child
;
----------
[CREATE_TABLE] CREATE TABLE clean_aborted_self(key int, data text)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_room_ad after delete
    on Room for each row execute procedure tg_room_ad()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION uniq(_int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_LIBRARY] -- New functions and views for pg_stat_statements in 1.8
AlTER EXTENSION pg_stat_statements UPDATE TO '1.8'
;
----------
[ALTER_INDEX] ALTER INDEX t_b_idx ATTACH PARTITION tp_b_a_key
;
----------
[CREATE_SEQUENCE] -- tests for sequences
create sequence test_sequence start 72057594037927937
;
----------
[CREATE_PROG_OBJ] --
-- The btree indexing operator class.
--

CREATE OPERATOR CLASS citext_ops
DEFAULT FOR TYPE CITEXT USING btree AS
    OPERATOR    1   <  (citext, citext),
    OPERATOR    2   <= (citext, citext),
    OPERATOR    3   =  (citext, citext),
    OPERATOR    4   >= (citext, citext),
    OPERATOR    5   >  (citext, citext),
    FUNCTION    1   citext_cmp(citext, citext)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_shseclabel
;
----------
[CREATE_TABLE] CREATE TABLE beta_neg_p3 PARTITION OF beta_neg FOR VALUES FROM (350) TO (500)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES FROM ('a', 1) TO ('z')
;
----------
[CREATE_TABLE] CREATE TABLE harvest(apples integer)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_foreign_table
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart3"."pk_pkey" ATTACH PARTITION "fkpart3"."pk4_pkey"
;
----------
[ALTER_TRIGGER] -- clean up
alter event trigger regress_event_trigger disable
;
----------
[ADD_INDEX] -- Do the same concurrently
CREATE INDEX CONCURRENTLY sro_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
	WHERE sro_ifun(a + 10) > sro_ifun(10)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- insert/update/delete statement-level triggers on the parent
create trigger trig_ins_before before insert on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[CREATE_PROG_OBJ] create or replace function rls_f() returns setof rls_t
  stable language sql
  as $$ select exists(select * from rls_t)::text $$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=26534 host=/tmp/0qySRdF1o7 dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_TYPE] create type jpop as (a text, b int, c timestamp)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE atest12 as
  SELECT x AS a, 10001 - x AS b FROM generate_series(1,10000) x
;
----------
[DROP_PROG_OBJ] DROP OPERATOR @ (_int4, _int4)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_trigger attach partition parted_trigger_2 for values from (1000) to (2000)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30385_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30516_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TYPE] alter type insenum add value 'i29' before 'L2'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Try (and fail) to add constraint due to invalid destination columns explicitly given
ALTER TABLE attmp3 add constraint attmpconstr foreign key(a) references attmp2(b) match full
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_intbig_same(intbig_gkey, intbig_gkey, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30592 RENAME TO reind_con_toast
;
----------
[CREATE_VIEW(SELECT)] create view rw_view1 as select b as bb, c as cc, a as aa from base_tbl
;
----------
[ALTER_INDEX] alter index idxpart_pkey attach partition idxpart1_a_idx
;
