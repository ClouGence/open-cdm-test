VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_foreign_table
;
ANALYZE public.subselect_tbl
;
ANALYZE "S 1"."T 1"
;
ALTER TABLE tab_rowfilter_1 REPLICA IDENTITY FULL
;
DROP MATERIALIZED VIEW IF EXISTS split_view_next.base_mv_renamed CASCADE
;
DROP TABLE rls_t1 CASCADE
;
ALTER TABLE pg_toast.pg_toast_28137 RENAME TO reind_con_toast
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
-- unsupported relation types
-- materialized view
CREATE MATERIALIZED VIEW mv AS SELECT * FROM target
;
ANALYZE pg_temp_13.pg_temp_26413_2
;
CREATE FUNCTION gin_compare_prefix_char("char", "char", int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
DROP TYPE test_type2
;
-- make sure that all columns covered by unique indexes works
CREATE TABLE mvtest_foo(a, b, c) AS VALUES(1, 2, 3)
;
create table syscolconflicttest(key int4, data text)
;
CREATE INDEX "tenk2_unique2" ON "public"."tenk2" USING "btree" ("unique2")
;
create function polyf(anymultirange) returns anyelement
as 'select lower($1);' language sql
;
DROP DOMAIN domain_stats
;
ALTER TABLE pg_toast.pg_toast_30343 RENAME TO reind_con_toast
;
CREATE TABLE upsert_test_2 (b TEXT, a INT PRIMARY KEY)
;
create table trg_clone3 partition of trg_clone for values from (2000) to (3000)
  partition by range (a)
;
CREATE INDEX pg_descripton_test_index ON pg_description (description)
;
alter database abc reset datestyle
;
CREATE VIEW pg_stat_statements AS
  SELECT * FROM pg_stat_statements(true)
;
-- TRUNCATE doesn't work on foreign tables, either directly or recursively
TRUNCATE fd_pt2_1
;
CREATE TABLE unbounded_range_part PARTITION OF range_parted4 FOR VALUES FROM (MINVALUE, MINVALUE, MINVALUE) TO (MAXVALUE, MAXVALUE, MAXVALUE)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE TABLE regress_schema_1.tab (id int)
;
/*
 * check whether we handle updates/deletes correct with & without a pkey
 */
/* we should handle the case without a key at all more gracefully */
CREATE TABLE table_without_key(id serial, data int)
;
CREATE FUNCTION noisy_oper(p_comment text, p_a anynonarray, p_op text, p_b anynonarray)
    RETURNS bool LANGUAGE plpgsql AS $body$
        DECLARE
            r bool;
        BEGIN
            EXECUTE format('SELECT $1 %s $2', p_op) INTO r USING p_a, p_b;
            RAISE NOTICE '%: % % % % %: %', p_comment, pg_typeof(p_a), p_a, p_op, pg_typeof(p_b), p_b, r;
        RETURN r;
    END;$body$
;
-- fails, as there's no cast

-- Try binary coercion cast
CREATE CAST (text AS casttesttype) WITHOUT FUNCTION
;
create aggregate build_group(int8, integer) (
  SFUNC = add_group,
  STYPE = int8[]
)
;
-- test: use of variable of composite type in return statement
create or replace function compos() returns compostype as $$
declare
  v compostype;
begin
  v := (1, 'hello');
  return v;
end;
$$ language plpgsql
;
CREATE TABLE pub_test.testpub_addpk (id int not null, data int)
;
ALTER INDEX "fkpart3"."pk_pkey" ATTACH PARTITION "fkpart3"."pk5_pkey"
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub6
;
/*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', header 'true')
;
/*
 * 5.42
 * ROLE_ROUTINE_GRANTS view
 */

CREATE VIEW role_routine_grants AS
    SELECT grantor,
           grantee,
           specific_catalog,
           specific_schema,
           specific_name,
           routine_catalog,
           routine_schema,
           routine_name,
           privilege_type,
           is_grantable
    FROM routine_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
create trigger trig_ins_after_child after insert on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
CREATE TABLE tab_1 (a int)
;
CREATE TABLE alice.hashpart_a PARTITION OF alice.hashpart
    FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
create cast (myint as int4) without function
;
CREATE SUBSCRIPTION sub CONNECTION 'port=24828 host=/tmp/OypwZkISo1 dbname=postgres' PUBLICATION pub WITH (slot_name = 'lsub4_slot', create_slot = false)
;
CREATE TABLE tst (i int4, t text)
;
-- Currently this tests polymorphic aggregates and indirectly does some
-- testing of polymorphic SQL functions.  It ought to be extended.
-- Tests for other features related to function-calling have snuck in, too.


-- Legend:
-----------
-- A = type is ANY
-- P = type is polymorphic
-- N = type is non-polymorphic
-- B = aggregate base type
-- S = aggregate state type
-- R = aggregate return type
-- 1 = arg1 of a function
-- 2 = arg2 of a function
-- ag = aggregate
-- tf = trans (state) function
-- ff = final function
-- rt = return type of a function
-- -> = implies
-- => = allowed
-- !> = not allowed
-- E  = exists
-- NE = not-exists
--
-- Possible states:
-- ----------------
-- B = (A || P || N)
--   when (B = A) -> (tf2 = NE)
-- S = (P || N)
-- ff = (E || NE)
-- tf1 = (P || N)
-- tf2 = (NE || P || N)
-- R = (P || N)

-- create functions for use as tf and ff with the needed combinations of
-- argument polymorphism, but within the constraints of valid aggregate
-- functions, i.e. tf arg1 and tf return type must match

-- polymorphic single arg transfn
CREATE FUNCTION stfp(anyarray) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
ALTER TYPE split_domain_type_next.address_type DROP ATTRIBUTE IF EXISTS region RESTRICT
;
DROP TABLE macaddr_data
;
CREATE FUNCTION gin_compare_prefix_int4(int4, int4, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_description
;
--
-- brin_revmap_data()
--
CREATE FUNCTION brin_revmap_data(IN page bytea,
	OUT pages tid)
RETURNS SETOF tid
AS '$libdir/pageinspect', 'brin_revmap_data'
LANGUAGE C STRICT PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_foreign_table
;
----------
[ADMIN_TABLE] ANALYZE public.subselect_tbl
;
----------
[ADMIN_TABLE] ANALYZE "S 1"."T 1"
;
----------
[ALTER_TABLE] ALTER TABLE tab_rowfilter_1 REPLICA IDENTITY FULL
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW IF EXISTS split_view_next.base_mv_renamed CASCADE
;
----------
[DROP_TABLE] DROP TABLE rls_t1 CASCADE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28137 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_dict
;
----------
[CREATE_VIEW(SELECT)] -- unsupported relation types
-- materialized view
CREATE MATERIALIZED VIEW mv AS SELECT * FROM target
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26413_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_char("char", "char", int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[DROP_TYPE] DROP TYPE test_type2
;
----------
[CREATE_TABLE|SELECT] -- make sure that all columns covered by unique indexes works
CREATE TABLE mvtest_foo(a, b, c) AS VALUES(1, 2, 3)
;
----------
[CREATE_TABLE] create table syscolconflicttest(key int4, data text)
;
----------
[ADD_INDEX] CREATE INDEX "tenk2_unique2" ON "public"."tenk2" USING "btree" ("unique2")
;
----------
[CREATE_PROG_OBJ] create function polyf(anymultirange) returns anyelement
as 'select lower($1);' language sql
;
----------
[DROP_TYPE] DROP DOMAIN domain_stats
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30343 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE upsert_test_2 (b TEXT, a INT PRIMARY KEY)
;
----------
[CREATE_TABLE] create table trg_clone3 partition of trg_clone for values from (2000) to (3000)
  partition by range (a)
;
----------
[ADD_INDEX] CREATE INDEX pg_descripton_test_index ON pg_description (description)
;
----------
[ALTER_CATALOG] alter database abc reset datestyle
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_statements AS
  SELECT * FROM pg_stat_statements(true)
;
----------
[TRUNCATE_TABLE] -- TRUNCATE doesn't work on foreign tables, either directly or recursively
TRUNCATE fd_pt2_1
;
----------
[CREATE_TABLE] CREATE TABLE unbounded_range_part PARTITION OF range_parted4 FOR VALUES FROM (MINVALUE, MINVALUE, MINVALUE) TO (MAXVALUE, MAXVALUE, MAXVALUE)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE TABLE regress_schema_1.tab (id int)
;
----------
[CREATE_TABLE] /*
 * check whether we handle updates/deletes correct with & without a pkey
 */
/* we should handle the case without a key at all more gracefully */
CREATE TABLE table_without_key(id serial, data int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION noisy_oper(p_comment text, p_a anynonarray, p_op text, p_b anynonarray)
    RETURNS bool LANGUAGE plpgsql AS $body$
        DECLARE
            r bool;
        BEGIN
            EXECUTE format('SELECT $1 %s $2', p_op) INTO r USING p_a, p_b;
            RAISE NOTICE '%: % % % % %: %', p_comment, pg_typeof(p_a), p_a, p_op, pg_typeof(p_b), p_b, r;
        RETURN r;
    END;$body$
;
----------
[CREATE_PROG_OBJ] -- fails, as there's no cast

-- Try binary coercion cast
CREATE CAST (text AS casttesttype) WITHOUT FUNCTION
;
----------
[CREATE_PROG_OBJ] create aggregate build_group(int8, integer) (
  SFUNC = add_group,
  STYPE = int8[]
)
;
----------
[CREATE_PROG_OBJ] -- test: use of variable of composite type in return statement
create or replace function compos() returns compostype as $$
declare
  v compostype;
begin
  v := (1, 'hello');
  return v;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE pub_test.testpub_addpk (id int not null, data int)
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart3"."pk_pkey" ATTACH PARTITION "fkpart3"."pk5_pkey"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub6
;
----------
[CREATE_POLICY] /*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', header 'true')
;
----------
[CREATE_VIEW(SELECT)] /*
 * 5.42
 * ROLE_ROUTINE_GRANTS view
 */

CREATE VIEW role_routine_grants AS
    SELECT grantor,
           grantee,
           specific_catalog,
           specific_schema,
           specific_name,
           routine_catalog,
           routine_schema,
           routine_name,
           privilege_type,
           is_grantable
    FROM routine_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_ins_after_child after insert on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
----------
[CREATE_TABLE] CREATE TABLE tab_1 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE alice.hashpart_a PARTITION OF alice.hashpart
    FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[CREATE_PROG_OBJ] create cast (myint as int4) without function
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub CONNECTION 'port=24828 host=/tmp/OypwZkISo1 dbname=postgres' PUBLICATION pub WITH (slot_name = 'lsub4_slot', create_slot = false)
;
----------
[CREATE_TABLE] CREATE TABLE tst (i int4, t text)
;
----------
[CREATE_PROG_OBJ] -- Currently this tests polymorphic aggregates and indirectly does some
-- testing of polymorphic SQL functions.  It ought to be extended.
-- Tests for other features related to function-calling have snuck in, too.


-- Legend:
-----------
-- A = type is ANY
-- P = type is polymorphic
-- N = type is non-polymorphic
-- B = aggregate base type
-- S = aggregate state type
-- R = aggregate return type
-- 1 = arg1 of a function
-- 2 = arg2 of a function
-- ag = aggregate
-- tf = trans (state) function
-- ff = final function
-- rt = return type of a function
-- -> = implies
-- => = allowed
-- !> = not allowed
-- E  = exists
-- NE = not-exists
--
-- Possible states:
-- ----------------
-- B = (A || P || N)
--   when (B = A) -> (tf2 = NE)
-- S = (P || N)
-- ff = (E || NE)
-- tf1 = (P || N)
-- tf2 = (NE || P || N)
-- R = (P || N)

-- create functions for use as tf and ff with the needed combinations of
-- argument polymorphism, but within the constraints of valid aggregate
-- functions, i.e. tf arg1 and tf return type must match

-- polymorphic single arg transfn
CREATE FUNCTION stfp(anyarray) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
----------
[ALTER_TYPE] ALTER TYPE split_domain_type_next.address_type DROP ATTRIBUTE IF EXISTS region RESTRICT
;
----------
[DROP_TABLE] DROP TABLE macaddr_data
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_int4(int4, int4, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_description
;
----------
[CREATE_PROG_OBJ] --
-- brin_revmap_data()
--
CREATE FUNCTION brin_revmap_data(IN page bytea,
	OUT pages tid)
RETURNS SETOF tid
AS '$libdir/pageinspect', 'brin_revmap_data'
LANGUAGE C STRICT PARALLEL SAFE
;
