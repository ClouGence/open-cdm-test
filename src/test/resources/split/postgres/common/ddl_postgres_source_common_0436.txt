CREATE TABLE regress_constr_partition1 PARTITION OF regress_constr_partitioned FOR VALUES IN (1)
;
CREATE TABLE tab_mixed (a int primary key, b text, c numeric)
;
ALTER TEXT SEARCH CONFIGURATION german ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
DROP TABLE fewmore
;
CREATE FUNCTION isnne(ismn, ismn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION public.monot_incr(int) RETURNS bool LANGUAGE sql
		AS ' select $1 > max(a) from public.tst_dom_constr; '
;
SECURITY LABEL ON COLUMN customer.ccredit IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
DROP TABLE rule_t1 CASCADE
;
-- trying to specify modulus and remainder for list partitioned table
CREATE TABLE fail_part PARTITION OF list_parted FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
CREATE TEMP VIEW fdv2 AS
SELECT a.id, a.keywords, a.title, aic.category_id, aic.changed
FROM articles AS a JOIN articles_in_category AS aic ON a.id = aic.article_id
WHERE aic.category_id in (14,62,70,53,138)
GROUP BY a.id, aic.category_id, aic.article_id
;
VACUUM pg_catalog.pg_seclabel
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_publication_namespace
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_opfamily
;
CREATE TABLE test_prepared2(id integer primary key)
;
-- can't drop not-null in primary key
CREATE TABLE notnull_tbl2 (a INTEGER PRIMARY KEY)
;
-- partitioned table
CREATE TABLE vacparted (a int, b char) PARTITION BY LIST (a)
;
-- Cleanup
DROP SCHEMA temp_func_test CASCADE
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
-- Statistics views

CREATE VIEW pg_stat_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_numscans(C.oid) AS seq_scan,
            pg_stat_get_lastscan(C.oid) AS last_seq_scan,
            pg_stat_get_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_numscans(I.indexrelid))::bigint AS idx_scan,
            max(pg_stat_get_lastscan(I.indexrelid)) AS last_idx_scan,
            sum(pg_stat_get_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_tuples_hot_updated(C.oid) AS n_tup_hot_upd,
            pg_stat_get_tuples_newpage_updated(C.oid) AS n_tup_newpage_upd,
            pg_stat_get_live_tuples(C.oid) AS n_live_tup,
            pg_stat_get_dead_tuples(C.oid) AS n_dead_tup,
            pg_stat_get_mod_since_analyze(C.oid) AS n_mod_since_analyze,
            pg_stat_get_ins_since_vacuum(C.oid) AS n_ins_since_vacuum,
            pg_stat_get_last_vacuum_time(C.oid) as last_vacuum,
            pg_stat_get_last_autovacuum_time(C.oid) as last_autovacuum,
            pg_stat_get_last_analyze_time(C.oid) as last_analyze,
            pg_stat_get_last_autoanalyze_time(C.oid) as last_autoanalyze,
            pg_stat_get_vacuum_count(C.oid) AS vacuum_count,
            pg_stat_get_autovacuum_count(C.oid) AS autovacuum_count,
            pg_stat_get_analyze_count(C.oid) AS analyze_count,
            pg_stat_get_autoanalyze_count(C.oid) AS autoanalyze_count
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm', 'p')
    GROUP BY C.oid, N.nspname, C.relname
;
CREATE TABLE "fkpart3"."fk2" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer
)
;
create index textrange_test_btree on textrange_test(tr)
;
create type my_crosstab_result as (
  rowid text, rowdt timestamp,
  temperature int4, test_result text, test_startdate timestamp, volts float8)
;
-- You can sort of fake arrays-of-arrays by putting a domain in between
create domain dposinta as posint[]
;
create table self_ref (a int primary key,
                       b int references self_ref(a) on delete cascade)
;
CREATE FUNCTION "public"."dummy_update_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'dummy_update_func(%) called: action = %, old = %, new = %',
    TG_ARGV[0], TG_OP, OLD, NEW;
  RETURN NEW;
END;
$$
;
ALTER TABLE ONLY "regress_indexing"."pk3"
    ADD CONSTRAINT "pk3_pkey" PRIMARY KEY ("a")
;
ALTER TABLE split_alter_table.base_table ADD CONSTRAINT base_table_ref_fk FOREIGN KEY (parent_key) REFERENCES split_alter_table.ref_table(id) DEFERRABLE INITIALLY DEFERRED NOT VALID
;
CREATE FUNCTION test_predtest(query text,
  OUT strong_implied_by bool,
  OUT weak_implied_by bool,
  OUT strong_refuted_by bool,
  OUT weak_refuted_by bool,
  OUT s_i_holds bool,
  OUT w_i_holds bool,
  OUT s_r_holds bool,
  OUT w_r_holds bool)
STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
-- Triggers
CREATE FUNCTION dummy_trigger() RETURNS TRIGGER AS $$
  BEGIN
    RETURN NULL;
  END
$$ language plpgsql
;
alter table parted_inval_tab attach partition parted_inval_tab_1
  for values from (1) to (100)
;
CREATE TABLE temporal_rng2 () INHERITS (temporal_rng)
;
CREATE TYPE "public"."jpop" AS (
	"a" "text",
	"b" integer,
	"c" timestamp without time zone
)
;
CREATE TABLE plt2_p1 PARTITION OF plt2 FOR VALUES IN ('0000', '0003', '0004', '0010')
;
create table parted_sample_2 partition of parted_sample for values in (2)
;
-- fail - can't drop from all tables publication
ALTER PUBLICATION testpub_foralltables DROP TABLE testpub_tbl2
;
-- fail, dup name
ALTER TABLE tt9 ADD CONSTRAINT foo UNIQUE(c)
;
DROP INDEX test_index_exists
;
create index hidx on testhstore using gist(h)
;
create table T ( Item1 int , Item2 int )
;
-- test that SET STORAGE propagates to index correctly
create index test_storage_idx on test_storage (b, a)
;
ANALYZE only_inh_parent
;
ALTER INDEX pg_toast.pg_toast_30870_index RENAME TO reind_con_toast_idx
;
drop table tbl1, tprt
;
-- This partitioned table should remain with no partitions.
CREATE TABLE ptif_test3 PARTITION OF ptif_test
  FOR VALUES FROM (200) TO (maxvalue) PARTITION BY list (b)
;
DROP SCHEMA test_maint_search_path CASCADE
;
CREATE TABLE trunc_faa (col3 text) INHERITS (trunc_fa)
;
-- Read-only tests

CREATE TABLE writetest (a int)
;
alter table mlparted4 add a int not null
;
CREATE MATERIALIZED VIEW mvtest1 AS SELECT 1 AS x
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE regress_constr_partition1 PARTITION OF regress_constr_partitioned FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE tab_mixed (a int primary key, b text, c numeric)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION german ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[DROP_TABLE] DROP TABLE fewmore
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ismn, ismn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION public.monot_incr(int) RETURNS bool LANGUAGE sql
		AS ' select $1 > max(a) from public.tst_dom_constr; '
;
----------
[UNKNOWN] SECURITY LABEL ON COLUMN customer.ccredit IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
----------
[DROP_TABLE] DROP TABLE rule_t1 CASCADE
;
----------
[CREATE_TABLE] -- trying to specify modulus and remainder for list partitioned table
CREATE TABLE fail_part PARTITION OF list_parted FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW fdv2 AS
SELECT a.id, a.keywords, a.title, aic.category_id, aic.changed
FROM articles AS a JOIN articles_in_category AS aic ON a.id = aic.article_id
WHERE aic.category_id in (14,62,70,53,138)
GROUP BY a.id, aic.category_id, aic.article_id
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_seclabel
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_publication_namespace
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_opfamily
;
----------
[CREATE_TABLE] CREATE TABLE test_prepared2(id integer primary key)
;
----------
[CREATE_TABLE] -- can't drop not-null in primary key
CREATE TABLE notnull_tbl2 (a INTEGER PRIMARY KEY)
;
----------
[CREATE_TABLE] -- partitioned table
CREATE TABLE vacparted (a int, b char) PARTITION BY LIST (a)
;
----------
[DROP_SCHEMA] -- Cleanup
DROP SCHEMA temp_func_test CASCADE
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
----------
[CREATE_VIEW(SELECT)] -- Statistics views

CREATE VIEW pg_stat_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_numscans(C.oid) AS seq_scan,
            pg_stat_get_lastscan(C.oid) AS last_seq_scan,
            pg_stat_get_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_numscans(I.indexrelid))::bigint AS idx_scan,
            max(pg_stat_get_lastscan(I.indexrelid)) AS last_idx_scan,
            sum(pg_stat_get_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_tuples_hot_updated(C.oid) AS n_tup_hot_upd,
            pg_stat_get_tuples_newpage_updated(C.oid) AS n_tup_newpage_upd,
            pg_stat_get_live_tuples(C.oid) AS n_live_tup,
            pg_stat_get_dead_tuples(C.oid) AS n_dead_tup,
            pg_stat_get_mod_since_analyze(C.oid) AS n_mod_since_analyze,
            pg_stat_get_ins_since_vacuum(C.oid) AS n_ins_since_vacuum,
            pg_stat_get_last_vacuum_time(C.oid) as last_vacuum,
            pg_stat_get_last_autovacuum_time(C.oid) as last_autovacuum,
            pg_stat_get_last_analyze_time(C.oid) as last_analyze,
            pg_stat_get_last_autoanalyze_time(C.oid) as last_autoanalyze,
            pg_stat_get_vacuum_count(C.oid) AS vacuum_count,
            pg_stat_get_autovacuum_count(C.oid) AS autovacuum_count,
            pg_stat_get_analyze_count(C.oid) AS analyze_count,
            pg_stat_get_autoanalyze_count(C.oid) AS autoanalyze_count
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm', 'p')
    GROUP BY C.oid, N.nspname, C.relname
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."fk2" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer
)
;
----------
[ADD_INDEX] create index textrange_test_btree on textrange_test(tr)
;
----------
[CREATE_TYPE] create type my_crosstab_result as (
  rowid text, rowdt timestamp,
  temperature int4, test_result text, test_startdate timestamp, volts float8)
;
----------
[CREATE_TYPE] -- You can sort of fake arrays-of-arrays by putting a domain in between
create domain dposinta as posint[]
;
----------
[CREATE_TABLE] create table self_ref (a int primary key,
                       b int references self_ref(a) on delete cascade)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."dummy_update_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'dummy_update_func(%) called: action = %, old = %, new = %',
    TG_ARGV[0], TG_OP, OLD, NEW;
  RETURN NEW;
END;
$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "regress_indexing"."pk3"
    ADD CONSTRAINT "pk3_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE split_alter_table.base_table ADD CONSTRAINT base_table_ref_fk FOREIGN KEY (parent_key) REFERENCES split_alter_table.ref_table(id) DEFERRABLE INITIALLY DEFERRED NOT VALID
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_predtest(query text,
  OUT strong_implied_by bool,
  OUT weak_implied_by bool,
  OUT strong_refuted_by bool,
  OUT weak_refuted_by bool,
  OUT s_i_holds bool,
  OUT w_i_holds bool,
  OUT s_r_holds bool,
  OUT w_r_holds bool)
STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] -- Triggers
CREATE FUNCTION dummy_trigger() RETURNS TRIGGER AS $$
  BEGIN
    RETURN NULL;
  END
$$ language plpgsql
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_inval_tab attach partition parted_inval_tab_1
  for values from (1) to (100)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng2 () INHERITS (temporal_rng)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."jpop" AS (
	"a" "text",
	"b" integer,
	"c" timestamp without time zone
)
;
----------
[CREATE_TABLE] CREATE TABLE plt2_p1 PARTITION OF plt2 FOR VALUES IN ('0000', '0003', '0004', '0010')
;
----------
[CREATE_TABLE] create table parted_sample_2 partition of parted_sample for values in (2)
;
----------
[ALTER_PUB_SUB] -- fail - can't drop from all tables publication
ALTER PUBLICATION testpub_foralltables DROP TABLE testpub_tbl2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- fail, dup name
ALTER TABLE tt9 ADD CONSTRAINT foo UNIQUE(c)
;
----------
[DROP_INDEX] DROP INDEX test_index_exists
;
----------
[ADD_INDEX] create index hidx on testhstore using gist(h)
;
----------
[CREATE_TABLE] create table T ( Item1 int , Item2 int )
;
----------
[ADD_INDEX] -- test that SET STORAGE propagates to index correctly
create index test_storage_idx on test_storage (b, a)
;
----------
[ADMIN_TABLE] ANALYZE only_inh_parent
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30870_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] drop table tbl1, tprt
;
----------
[CREATE_TABLE] -- This partitioned table should remain with no partitions.
CREATE TABLE ptif_test3 PARTITION OF ptif_test
  FOR VALUES FROM (200) TO (maxvalue) PARTITION BY list (b)
;
----------
[DROP_SCHEMA] DROP SCHEMA test_maint_search_path CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE trunc_faa (col3 text) INHERITS (trunc_fa)
;
----------
[CREATE_TABLE] -- Read-only tests

CREATE TABLE writetest (a int)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table mlparted4 add a int not null
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest1 AS SELECT 1 AS x
;
