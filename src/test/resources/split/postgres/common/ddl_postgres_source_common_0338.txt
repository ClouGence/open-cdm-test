ALTER TABLE regress_constr_partition1 ADD PRIMARY KEY (a)
;
CREATE TABLE collate_test2 (
    a int,
    b text COLLATE "sv-x-icu"
)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_tablespace
;
create unique index on t_opf (a record_image_ops)
;
ANALYZE pg_temp_81.pg_temp_30251
;
CREATE FUNCTION "public"."infinite_recurse"() RETURNS integer
    LANGUAGE "sql"
    AS $$select infinite_recurse()$$
;
CREATE SCHEMA "has space"
;
CREATE TABLE group_tbl (a INT, b INT)
;
ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        3       pg_catalog.~~ (text, text),
        OPERATOR        4       pg_catalog.~~* (text, text)
;
CREATE TYPE type_op5 AS ENUM ('new', 'open', 'closed')
;
DROP TABLE past_inh_db_child
;
CREATE TABLE num_exp_sub (id1 int4, id2 int4, expected numeric(210,10))
;
ALTER TABLE pg_temp_12.pg_temp_25379_2 ADD COLUMN newdata pg_temp_12.pg_temp_25379
;
CREATE FUNCTION gbt_int8_consistent(internal,int8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE list_parted2_def PARTITION OF list_parted2 DEFAULT
;
CREATE TABLE toasttest(f1 bytea)
;
create table part_21_30_ab partition of part_21_30 for values in ('ab')
;
CREATE LANGUAGE pltestlang
					   HANDLER dump_test.pltestlang_call_handler
;
create function check_estimated_rows(text) returns table (estimated int, actual int)
language plpgsql as
$$
declare
    ln text;
    tmp text[];
    first_row bool := true;
begin
    for ln in
        execute format('explain analyze %s', $1)
    loop
        if first_row then
            first_row := false;
            tmp := regexp_match(ln, 'rows=(\d*) .* rows=(\d*)');
            return query select tmp[1]::int, tmp[2]::int;
        end if;
    end loop;
end;
$$
;
CREATE FUNCTION pg_get_wal_record_info(IN in_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
AS '$libdir/pg_walinspect', 'pg_get_wal_record_info'
LANGUAGE C STRICT PARALLEL SAFE
;
create table ma_test (a int, b int) partition by range (a)
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=12543 host=/tmp/sGoWJzeqjX dbname=postgres' PUBLICATION mypub
;
alter domain dnotnulltest drop constraint dnotnulltest_notnull
;
ANALYZE public.onek
;
-- Verify bugfix with index rewrite on ALTER TABLE / SET DATA TYPE
-- https://postgr.es/m/CAKcux6mxNCGsgATwf5CGMF8g4WSupCXicCVMeKUTuWbyxHOMsQ@mail.gmail.com
CREATE TABLE idxpart (a INT, b TEXT, c INT) PARTITION BY RANGE(a)
;
CREATE TABLE ref_c1 PARTITION OF ref_c FOR VALUES FROM (100) TO (200)
;
CREATE TABLE "regress_indexing"."pk1" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
ALTER INDEX pg_toast.pg_toast_30939_index RENAME TO reind_con_toast_idx
;
ALTER INDEX pg_toast.pg_toast_30751_index RENAME TO reind_con_toast_idx
;
DROP TABLE pg_temp_4.pg_temp_37808_2, pg_temp_4.pg_temp_37808
;
TRUNCATE fd_pt2
;
DROP TABLE pgss_wal_tab
;
CREATE FUNCTION cp_testfunc1(a int) RETURNS int LANGUAGE SQL AS $$ SELECT a $$
;
/* End test case for bug #16242 */
/* Test case for bug #18970 */
create table attbl(a int)
;
--
-- injection_points_wakeup()
--
-- Wakes up a waiting injection point.
--
CREATE FUNCTION injection_points_wakeup(IN point_name TEXT)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_wakeup'
LANGUAGE C STRICT PARALLEL UNSAFE
;
ALTER TYPE "public"."stoplight" ADD VALUE 'red'
;
CREATE TABLE "public"."part33_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
;
CREATE FUNCTION fn_opf12  (int4, int2) RETURNS BIGINT AS 'SELECT NULL::BIGINT;' LANGUAGE SQL
;
/* Now redefine */
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8,
    OUT temp_blk_read_time float8,
    OUT temp_blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric,
    OUT jit_functions int8,
    OUT jit_generation_time float8,
    OUT jit_inlining_count int8,
    OUT jit_inlining_time float8,
    OUT jit_optimization_count int8,
    OUT jit_optimization_time float8,
    OUT jit_emission_count int8,
    OUT jit_emission_time float8
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_10'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
/* contrib/amcheck/amcheck--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- In order to avoid issues with dependencies when updating amcheck to 1.2,
-- create new, overloaded version of the 1.1 function signature

--
-- bt_index_parent_check()
--
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean, rootdescend boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE "public"."tab_core_types" (
    "point" "point",
    "line" "line",
    "lseg" "lseg",
    "box" "box",
    "openedpath" "path",
    "closedpath" "path",
    "polygon" "polygon",
    "circle" "circle",
    "date" "date",
    "time" time without time zone,
    "timestamp" timestamp without time zone,
    "timetz" time with time zone,
    "timestamptz" timestamp with time zone,
    "interval" interval,
    "json" "json",
    "jsonb" "jsonb",
    "jsonpath" "jsonpath",
    "inet" "inet",
    "cidr" "cidr",
    "macaddr8" "macaddr8",
    "macaddr" "macaddr",
    "int2" smallint,
    "int4" integer,
    "int8" bigint,
    "float4" real,
    "float8" double precision,
    "pi" numeric,
    "char" "char",
    "bpchar" "bpchar",
    "varchar" character varying,
    "name" "name",
    "text" "text",
    "bool" boolean,
    "bytea" "bytea",
    "bit" bit(1),
    "varbit" bit varying,
    "money" "money",
    "refcursor" "refcursor",
    "int2vector" "int2vector",
    "oidvector" "oidvector",
    "aclitem" "aclitem",
    "tsvector" "tsvector",
    "tsquery" "tsquery",
    "uuid" "uuid",
    "xid8" "xid8",
    "regclass" "regclass",
    "type" "regtype",
    "regrole" "regrole",
    "oid" "oid",
    "tid" "tid",
    "xid" "xid",
    "cid" "cid",
    "txid_snapshot" "txid_snapshot",
    "pg_snapshot" "pg_snapshot",
    "pg_lsn" "pg_lsn",
    "cardinal_number" "information_schema"."cardinal_number",
    "character_data" "information_schema"."character_data",
    "sql_identifier" "information_schema"."sql_identifier",
    "time_stamp" "information_schema"."time_stamp",
    "yes_or_no" "information_schema"."yes_or_no",
    "int4range" "int4range",
    "int4multirange" "int4multirange",
    "int8range" "int8range",
    "int8multirange" "int8multirange",
    "numrange" "numrange",
    "nummultirange" "nummultirange",
    "daterange" "daterange",
    "datemultirange" "datemultirange",
    "tsrange" "tsrange",
    "tsmultirange" "tsmultirange",
    "tstzrange" "tstzrange",
    "tstzmultirange" "tstzmultirange"
)
;
-- Check that partitioned tables can be clustered
CREATE TABLE clstrpart (a int) PARTITION BY RANGE (a)
;
CREATE TYPE dump_test.undefined
;
--
-- For fkeys:
-- 	(fkey1, fkey2)	--> pkeys (pkey1, pkey2)
-- 	(fkey3)		--> fkeys2 (pkey23)
--
create trigger check_fkeys_pkey_exist
	after insert or update on fkeys
	for each row
	execute function
	check_primary_key ('fkey1', 'fkey2', 'pkeys', 'pkey1', 'pkey2')
;
DROP TABLE pg_temp_14.pg_temp_26565_2, pg_temp_14.pg_temp_26565
;
CREATE TABLE tt28(c int)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_policy
;
-- invalid: input/output function incompatibility
CREATE TYPE bogus_type (INPUT = array_in,
    OUTPUT = array_out,
    ELEMENT = int,
    INTERNALLENGTH = 32)
;
--
-- basic tests of LIKE with indexes
--

CREATE TABLE texttest (a text PRIMARY KEY, b int)
;
CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE SEQUENCE schema_not_existing.seq
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE regress_constr_partition1 ADD PRIMARY KEY (a)
;
----------
[CREATE_TABLE] CREATE TABLE collate_test2 (
    a int,
    b text COLLATE "sv-x-icu"
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_tablespace
;
----------
[ADD_INDEX] create unique index on t_opf (a record_image_ops)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_81.pg_temp_30251
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."infinite_recurse"() RETURNS integer
    LANGUAGE "sql"
    AS $$select infinite_recurse()$$
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "has space"
;
----------
[CREATE_TABLE] CREATE TABLE group_tbl (a INT, b INT)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        3       pg_catalog.~~ (text, text),
        OPERATOR        4       pg_catalog.~~* (text, text)
;
----------
[CREATE_TYPE] CREATE TYPE type_op5 AS ENUM ('new', 'open', 'closed')
;
----------
[DROP_TABLE] DROP TABLE past_inh_db_child
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_sub (id1 int4, id2 int4, expected numeric(210,10))
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_25379_2 ADD COLUMN newdata pg_temp_12.pg_temp_25379
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int8_consistent(internal,int8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE list_parted2_def PARTITION OF list_parted2 DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE toasttest(f1 bytea)
;
----------
[CREATE_TABLE] create table part_21_30_ab partition of part_21_30 for values in ('ab')
;
----------
[CREATE_LANGUAGE] CREATE LANGUAGE pltestlang
					   HANDLER dump_test.pltestlang_call_handler
;
----------
[CREATE_PROG_OBJ] create function check_estimated_rows(text) returns table (estimated int, actual int)
language plpgsql as
$$
declare
    ln text;
    tmp text[];
    first_row bool := true;
begin
    for ln in
        execute format('explain analyze %s', $1)
    loop
        if first_row then
            first_row := false;
            tmp := regexp_match(ln, 'rows=(\d*) .* rows=(\d*)');
            return query select tmp[1]::int, tmp[2]::int;
        end if;
    end loop;
end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_get_wal_record_info(IN in_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
AS '$libdir/pg_walinspect', 'pg_get_wal_record_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table ma_test (a int, b int) partition by range (a)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=12543 host=/tmp/sGoWJzeqjX dbname=postgres' PUBLICATION mypub
;
----------
[ALTER_TYPE] alter domain dnotnulltest drop constraint dnotnulltest_notnull
;
----------
[ADMIN_TABLE] ANALYZE public.onek
;
----------
[CREATE_TABLE] -- Verify bugfix with index rewrite on ALTER TABLE / SET DATA TYPE
-- https://postgr.es/m/CAKcux6mxNCGsgATwf5CGMF8g4WSupCXicCVMeKUTuWbyxHOMsQ@mail.gmail.com
CREATE TABLE idxpart (a INT, b TEXT, c INT) PARTITION BY RANGE(a)
;
----------
[CREATE_TABLE] CREATE TABLE ref_c1 PARTITION OF ref_c FOR VALUES FROM (100) TO (200)
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk1" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30939_index RENAME TO reind_con_toast_idx
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30751_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_37808_2, pg_temp_4.pg_temp_37808
;
----------
[TRUNCATE_TABLE] TRUNCATE fd_pt2
;
----------
[DROP_TABLE] DROP TABLE pgss_wal_tab
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cp_testfunc1(a int) RETURNS int LANGUAGE SQL AS $$ SELECT a $$
;
----------
[CREATE_TABLE] /* End test case for bug #16242 */
/* Test case for bug #18970 */
create table attbl(a int)
;
----------
[CREATE_PROG_OBJ] --
-- injection_points_wakeup()
--
-- Wakes up a waiting injection point.
--
CREATE FUNCTION injection_points_wakeup(IN point_name TEXT)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_wakeup'
LANGUAGE C STRICT PARALLEL UNSAFE
;
----------
[ALTER_TYPE] ALTER TYPE "public"."stoplight" ADD VALUE 'red'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part33_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn_opf12  (int4, int2) RETURNS BIGINT AS 'SELECT NULL::BIGINT;' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] /* Now redefine */
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8,
    OUT temp_blk_read_time float8,
    OUT temp_blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric,
    OUT jit_functions int8,
    OUT jit_generation_time float8,
    OUT jit_inlining_count int8,
    OUT jit_inlining_time float8,
    OUT jit_optimization_count int8,
    OUT jit_optimization_time float8,
    OUT jit_emission_count int8,
    OUT jit_emission_time float8
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_10'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] /* contrib/amcheck/amcheck--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- In order to avoid issues with dependencies when updating amcheck to 1.2,
-- create new, overloaded version of the 1.1 function signature

--
-- bt_index_parent_check()
--
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean, rootdescend boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tab_core_types" (
    "point" "point",
    "line" "line",
    "lseg" "lseg",
    "box" "box",
    "openedpath" "path",
    "closedpath" "path",
    "polygon" "polygon",
    "circle" "circle",
    "date" "date",
    "time" time without time zone,
    "timestamp" timestamp without time zone,
    "timetz" time with time zone,
    "timestamptz" timestamp with time zone,
    "interval" interval,
    "json" "json",
    "jsonb" "jsonb",
    "jsonpath" "jsonpath",
    "inet" "inet",
    "cidr" "cidr",
    "macaddr8" "macaddr8",
    "macaddr" "macaddr",
    "int2" smallint,
    "int4" integer,
    "int8" bigint,
    "float4" real,
    "float8" double precision,
    "pi" numeric,
    "char" "char",
    "bpchar" "bpchar",
    "varchar" character varying,
    "name" "name",
    "text" "text",
    "bool" boolean,
    "bytea" "bytea",
    "bit" bit(1),
    "varbit" bit varying,
    "money" "money",
    "refcursor" "refcursor",
    "int2vector" "int2vector",
    "oidvector" "oidvector",
    "aclitem" "aclitem",
    "tsvector" "tsvector",
    "tsquery" "tsquery",
    "uuid" "uuid",
    "xid8" "xid8",
    "regclass" "regclass",
    "type" "regtype",
    "regrole" "regrole",
    "oid" "oid",
    "tid" "tid",
    "xid" "xid",
    "cid" "cid",
    "txid_snapshot" "txid_snapshot",
    "pg_snapshot" "pg_snapshot",
    "pg_lsn" "pg_lsn",
    "cardinal_number" "information_schema"."cardinal_number",
    "character_data" "information_schema"."character_data",
    "sql_identifier" "information_schema"."sql_identifier",
    "time_stamp" "information_schema"."time_stamp",
    "yes_or_no" "information_schema"."yes_or_no",
    "int4range" "int4range",
    "int4multirange" "int4multirange",
    "int8range" "int8range",
    "int8multirange" "int8multirange",
    "numrange" "numrange",
    "nummultirange" "nummultirange",
    "daterange" "daterange",
    "datemultirange" "datemultirange",
    "tsrange" "tsrange",
    "tsmultirange" "tsmultirange",
    "tstzrange" "tstzrange",
    "tstzmultirange" "tstzmultirange"
)
;
----------
[CREATE_TABLE] -- Check that partitioned tables can be clustered
CREATE TABLE clstrpart (a int) PARTITION BY RANGE (a)
;
----------
[CREATE_TYPE] CREATE TYPE dump_test.undefined
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- For fkeys:
-- 	(fkey1, fkey2)	--> pkeys (pkey1, pkey2)
-- 	(fkey3)		--> fkeys2 (pkey23)
--
create trigger check_fkeys_pkey_exist
	after insert or update on fkeys
	for each row
	execute function
	check_primary_key ('fkey1', 'fkey2', 'pkeys', 'pkey1', 'pkey2')
;
----------
[DROP_TABLE] DROP TABLE pg_temp_14.pg_temp_26565_2, pg_temp_14.pg_temp_26565
;
----------
[CREATE_TABLE] CREATE TABLE tt28(c int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_policy
;
----------
[CREATE_TYPE] -- invalid: input/output function incompatibility
CREATE TYPE bogus_type (INPUT = array_in,
    OUTPUT = array_out,
    ELEMENT = int,
    INTERNALLENGTH = 32)
;
----------
[CREATE_TABLE] --
-- basic tests of LIKE with indexes
--

CREATE TABLE texttest (a text PRIMARY KEY, b int)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE SEQUENCE schema_not_existing.seq
;
