create index on prtx2 (c)
;
-- Also check deparsing of such cases
create view composite_v as
with cte(c) as materialized (select row(1, 2)),
     cte2(c) as (select * from cte)
select 1 as one from cte2 as t
where (select * from (select c as c1) s
       where (select (c1).f1 > 0)) is not null
;
CREATE TYPE deptest_range AS RANGE (SUBTYPE = int4)
;
CREATE TABLE brinopers_bloom (colname name, typ text,
	op text[], value text[], matches int[],
	check (cardinality(op) = cardinality(value)),
	check (cardinality(op) = cardinality(matches)))
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."itest7a" INHERIT "public"."itest7"
;
ANALYZE public.gtest24
;
alter table alterlock set (toast.autovacuum_enabled = off)
;
VACUUM (disable_page_skipping) vacuum_test
;
ALTER TABLE list_parted2 DETACH PARTITION part_4
;
DROP TABLE noinh_con_copy, noinh_con_copy1
;
CREATE INDEX ggpolygonind ON gpolygon_tbl USING gist (f1)
;
-- cache tests
CREATE SEQUENCE test_seq1 CACHE 10
;
alter table p1_c1 inherit p1
;
CREATE TABLE tab_main(a int)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_subscription
;
-- Check "auto" case of index_cleanup and "truncate" controlled by
-- its GUC.
CREATE TABLE vac_tab_auto(i int, j text) WITH
  (autovacuum_enabled=false,
   vacuum_index_cleanup=auto, toast.vacuum_index_cleanup=auto)
;
CREATE TABLE ctlt1_inh (LIKE ctlt1 INCLUDING CONSTRAINTS INCLUDING COMMENTS) INHERITS (ctlt1)
;
-- ************************************************************
-- * Support function to set the opponents slotlink field
-- * if it does not already point to the requested slot
-- ************************************************************
create function tg_slotlink_set(bpchar, bpchar)
returns integer as '
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    link	char(4);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    link := mytype || substr(blname, 1, 2);
    if link = ''PHPH'' then
        raise exception
		''slotlink between two phones does not make sense'';
    end if;
    if link in (''PHHS'', ''HSPH'') then
        raise exception
		''link of phone to hub does not make sense'';
    end if;
    if link in (''PHIF'', ''IFPH'') then
        raise exception
		''link of phone to hub does not make sense'';
    end if;
    if link in (''PSWS'', ''WSPS'') then
        raise exception
		''slotlink from patchslot to wallslot not permitted'';
    end if;
    if mytype = ''PS'' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update PSlot set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''WS'' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update WSlot set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''IF'' then
        select into rec * from IFace where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update IFace set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''HS'' then
        select into rec * from HSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update HSlot set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''PH'' then
        select into rec * from PHone where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update PHone set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    raise exception ''illegal slotlink beginning with %'', mytype;
end;
' language plpgsql
;
-- error

-- kinda silly to change property in the same command, but it should work
ALTER TABLE itest3
  ADD COLUMN c int GENERATED BY DEFAULT AS IDENTITY,
  ALTER COLUMN c SET GENERATED ALWAYS
;
CREATE DOMAIN "public"."constraint_comments_dom" AS integer
;
CREATE TABLE drop_schema_test1.drop_tab_test1 AS SELECT generate_series(1,100) AS a
;
DROP SUBSCRIPTION regress_sub1_gen_to_nogen
;
DROP TABLE temporal_fk_mltrng2mltrng
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_language
;
-- allow subquery to reference unpopulated matview if WITH NO DATA is specified
CREATE MATERIALIZED VIEW mvtest_mv1 AS SELECT 1 AS col1 WITH NO DATA
;
CREATE FUNCTION "public"."tg_system_au"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.name != old.name then
        update IFace set sysname = new.name where sysname = old.name;
    end if;
    return new;
end;
$$
;
DROP TABLE batch_table_p1
;
--
-- Tests for IS NULL/IS NOT NULL with b-tree indexes
--

CREATE TABLE onek_with_null AS SELECT unique1, unique2 FROM onek
;
CREATE FUNCTION citext(bpchar)
RETURNS citext
AS 'rtrim1'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
ALTER FUNCTION gbt_cash_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
create table selfconflict (f1 int primary key, f2 int)
;
CREATE FUNCTION test_integerset()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_integerset' LANGUAGE C
;
ALTER TABLE ONLY "public"."prt2" ATTACH PARTITION "public"."prt2_p2" FOR VALUES FROM (250) TO (500)
;
DROP TABLE test1, test2
;
create unique index WSlot_name on WSlot using btree (slotname bpchar_ops)
;
CREATE FUNCTION isnne(upc, upc)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE temporal3 ALTER COLUMN valid_at TYPE tstzrange USING tstzrange(lower(valid_at), upper(valid_at))
;
drop index idxpart2_a_idx
;
-- after creating and attaching this, both idxpart1_a_idx and idxpart_a_idx
-- should become valid
create index on idxpart11 (a)
;
-- Triggers on leaf partition part_c_1_100
CREATE TRIGGER c1_delete_trig
  AFTER DELETE ON part_c_1_100 for each statement execute procedure trigfunc()
;
DROP TABLE pg_temp_3.pg_temp_28852_2, pg_temp_3.pg_temp_28852
;
-- For binary upgrade, set up inherited constraint.
ALTER TABLE ONLY "public"."test_inh_check_child"  ADD CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
;
ALTER INDEX regress_pg_dump_schema.extdependtab_pkey DEPENDS ON EXTENSION test_pg_dump
;
-- fails
DROP TABLE unlogged1
;
-- Don't auto-attach incompatible indexes
create table idxpart (a int, b int) partition by range (a)
;
ALTER FUNCTION gbt_int4_consistent(internal, integer, smallint, oid, internal) PARALLEL SAFE
;
ALTER TABLE fail_part ALTER b TYPE char (2) COLLATE "POSIX"
;
CREATE SUBSCRIPTION tap_sub_B_A
	CONNECTION 'port=11381 host=/tmp/C6o4PTAnCK dbname=postgres application_name=tap_sub_B_A'
	PUBLICATION tap_pub_A
	WITH (origin = none)
;
create table ths_p3 partition of ths for values with (modulus 3, remainder 2)
;
create unique index insertconflicti1 on insertconflict(coalesce(a, 0))
;
------------------------------------------------------------------------------------------
[ADD_INDEX] create index on prtx2 (c)
;
----------
[CREATE_VIEW(SELECT)] -- Also check deparsing of such cases
create view composite_v as
with cte(c) as materialized (select row(1, 2)),
     cte2(c) as (select * from cte)
select 1 as one from cte2 as t
where (select * from (select c as c1) s
       where (select (c1).f1 > 0)) is not null
;
----------
[CREATE_TYPE] CREATE TYPE deptest_range AS RANGE (SUBTYPE = int4)
;
----------
[CREATE_TABLE] CREATE TABLE brinopers_bloom (colname name, typ text,
	op text[], value text[], matches int[],
	check (cardinality(op) = cardinality(value)),
	check (cardinality(op) = cardinality(matches)))
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."itest7a" INHERIT "public"."itest7"
;
----------
[ADMIN_TABLE] ANALYZE public.gtest24
;
----------
[ALTER_TABLE] alter table alterlock set (toast.autovacuum_enabled = off)
;
----------
[ADMIN_TABLE] VACUUM (disable_page_skipping) vacuum_test
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 DETACH PARTITION part_4
;
----------
[DROP_TABLE] DROP TABLE noinh_con_copy, noinh_con_copy1
;
----------
[ADD_INDEX] CREATE INDEX ggpolygonind ON gpolygon_tbl USING gist (f1)
;
----------
[CREATE_SEQUENCE] -- cache tests
CREATE SEQUENCE test_seq1 CACHE 10
;
----------
[ALTER_TABLE] alter table p1_c1 inherit p1
;
----------
[CREATE_TABLE] CREATE TABLE tab_main(a int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] -- Check "auto" case of index_cleanup and "truncate" controlled by
-- its GUC.
CREATE TABLE vac_tab_auto(i int, j text) WITH
  (autovacuum_enabled=false,
   vacuum_index_cleanup=auto, toast.vacuum_index_cleanup=auto)
;
----------
[CREATE_TABLE] CREATE TABLE ctlt1_inh (LIKE ctlt1 INCLUDING CONSTRAINTS INCLUDING COMMENTS) INHERITS (ctlt1)
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * Support function to set the opponents slotlink field
-- * if it does not already point to the requested slot
-- ************************************************************
create function tg_slotlink_set(bpchar, bpchar)
returns integer as '
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    link	char(4);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    link := mytype || substr(blname, 1, 2);
    if link = ''PHPH'' then
        raise exception
		''slotlink between two phones does not make sense'';
    end if;
    if link in (''PHHS'', ''HSPH'') then
        raise exception
		''link of phone to hub does not make sense'';
    end if;
    if link in (''PHIF'', ''IFPH'') then
        raise exception
		''link of phone to hub does not make sense'';
    end if;
    if link in (''PSWS'', ''WSPS'') then
        raise exception
		''slotlink from patchslot to wallslot not permitted'';
    end if;
    if mytype = ''PS'' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update PSlot set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''WS'' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update WSlot set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''IF'' then
        select into rec * from IFace where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update IFace set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''HS'' then
        select into rec * from HSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update HSlot set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''PH'' then
        select into rec * from PHone where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.slotlink != blname then
	    update PHone set slotlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    raise exception ''illegal slotlink beginning with %'', mytype;
end;
' language plpgsql
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] -- error

-- kinda silly to change property in the same command, but it should work
ALTER TABLE itest3
  ADD COLUMN c int GENERATED BY DEFAULT AS IDENTITY,
  ALTER COLUMN c SET GENERATED ALWAYS
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."constraint_comments_dom" AS integer
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE drop_schema_test1.drop_tab_test1 AS SELECT generate_series(1,100) AS a
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION regress_sub1_gen_to_nogen
;
----------
[DROP_TABLE] DROP TABLE temporal_fk_mltrng2mltrng
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_language
;
----------
[CREATE_VIEW(SELECT)] -- allow subquery to reference unpopulated matview if WITH NO DATA is specified
CREATE MATERIALIZED VIEW mvtest_mv1 AS SELECT 1 AS col1 WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_system_au"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.name != old.name then
        update IFace set sysname = new.name where sysname = old.name;
    end if;
    return new;
end;
$$
;
----------
[DROP_TABLE] DROP TABLE batch_table_p1
;
----------
[CREATE_TABLE|SELECT] --
-- Tests for IS NULL/IS NOT NULL with b-tree indexes
--

CREATE TABLE onek_with_null AS SELECT unique1, unique2 FROM onek
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext(bpchar)
RETURNS citext
AS 'rtrim1'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_cash_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] create table selfconflict (f1 int primary key, f2 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_integerset()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_integerset' LANGUAGE C
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2" ATTACH PARTITION "public"."prt2_p2" FOR VALUES FROM (250) TO (500)
;
----------
[DROP_TABLE] DROP TABLE test1, test2
;
----------
[ADD_INDEX] create unique index WSlot_name on WSlot using btree (slotname bpchar_ops)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(upc, upc)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE temporal3 ALTER COLUMN valid_at TYPE tstzrange USING tstzrange(lower(valid_at), upper(valid_at))
;
----------
[DROP_INDEX] drop index idxpart2_a_idx
;
----------
[ADD_INDEX] -- after creating and attaching this, both idxpart1_a_idx and idxpart_a_idx
-- should become valid
create index on idxpart11 (a)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Triggers on leaf partition part_c_1_100
CREATE TRIGGER c1_delete_trig
  AFTER DELETE ON part_c_1_100 for each statement execute procedure trigfunc()
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_28852_2, pg_temp_3.pg_temp_28852
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- For binary upgrade, set up inherited constraint.
ALTER TABLE ONLY "public"."test_inh_check_child"  ADD CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
;
----------
[UNKNOWN] ALTER INDEX regress_pg_dump_schema.extdependtab_pkey DEPENDS ON EXTENSION test_pg_dump
;
----------
[DROP_TABLE] -- fails
DROP TABLE unlogged1
;
----------
[CREATE_TABLE] -- Don't auto-attach incompatible indexes
create table idxpart (a int, b int) partition by range (a)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int4_consistent(internal, integer, smallint, oid, internal) PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fail_part ALTER b TYPE char (2) COLLATE "POSIX"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_A
	CONNECTION 'port=11381 host=/tmp/C6o4PTAnCK dbname=postgres application_name=tap_sub_B_A'
	PUBLICATION tap_pub_A
	WITH (origin = none)
;
----------
[CREATE_TABLE] create table ths_p3 partition of ths for values with (modulus 3, remainder 2)
;
----------
[ADD_INDEX] create unique index insertconflicti1 on insertconflict(coalesce(a, 0))
;
