DROP ROLE regress_ae_role
;
LOCK TABLE "public"."b" IN ACCESS SHARE MODE
;
-- Test handling of string literals.

set standard_conforming_strings = off
;
EXECUTE dumpOpr('758')
;
CREATE SERVER s1 FOREIGN DATA WRAPPER foo
;
-- failed (name conflict)
ALTER SERVER alt_fserv1 RENAME TO alt_fserv3
;
-- check that parallel execution produces the same result
BEGIN
;
EXECUTE dumpCompositeType('38603')
;
ALTER USER regress_user1 PASSWORD 'tooshrt'
;
-- parallel with parallel-oblivious hash join
savepoint settings
;
------------------------------------------------------------------------------------------
[DROP_ROLE] DROP ROLE regress_ae_role
;
----------
[SESSION_LOCK] LOCK TABLE "public"."b" IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] -- Test handling of string literals.

set standard_conforming_strings = off
;
----------
[UNSAFE] EXECUTE dumpOpr('758')
;
----------
[SYSTEM_SETTING_WRITE] CREATE SERVER s1 FOREIGN DATA WRAPPER foo
;
----------
[SYSTEM_SETTING_WRITE] -- failed (name conflict)
ALTER SERVER alt_fserv1 RENAME TO alt_fserv3
;
----------
[TRANSACTION] -- check that parallel execution produces the same result
BEGIN
;
----------
[UNSAFE] EXECUTE dumpCompositeType('38603')
;
----------
[ALTER_USER] ALTER USER regress_user1 PASSWORD 'tooshrt'
;
----------
[TRANSACTION] -- parallel with parallel-oblivious hash join
savepoint settings
;
