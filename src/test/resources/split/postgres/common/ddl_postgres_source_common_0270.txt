create index testpart_orange_index on testpart_orange(logdate)
;
CREATE TEMP TABLE pg_temp_13.pg_temp_25481_2 (tid pg_catalog.tid)
;
VACUUM (INDEX_CLEANUP AUTO) vactst
;
-- fail, seq should have been dropped

-- partitioned table
CREATE TABLE truncparted (a int, b char) PARTITION BY LIST (a)
;
DROP POLICY p1 ON dob_t1
;
ALTER TABLE t SET tablespace regress_ts4
;
----
-- Make sure index cases succeed
----

-- succeed, primary key
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_pkey
;
create table atacc2 (test2 int) inherits (atacc1)
;
-- With a defined row type, the typcache can inspect the type's fields
-- for hashability.
create type ct1 as (f1 money)
;
CREATE FUNCTION seg_overlap(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE s.x ADD CONSTRAINT e0rows EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll WITH s.=)
  USING INDEX TABLESPACE regress_create_idx_tblspace
  WHERE (s.index_row_if(y))
;
alter type insenum add value 'i23' before 'L2'
;
CREATE FUNCTION isnle(ean13, issn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER FOREIGN TABLE agg_bad OPTIONS (SET reject_limit '2')
;
DROP DOMAIN qf_char_domain, qf_jsonb_domain
;
CREATE TABLE "fkpart3"."pk1" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
ANALYZE public.prt2_p2
;
drop function whoami()
;
create index trgm_idx2 on test_trgm2 using gin (t gin_trgm_ops)
;
ALTER TABLE pg_temp_3.pg_temp_29555_2 ADD COLUMN newdata pg_temp_3.pg_temp_29555
;
CREATE TABLE newly_created (a int, b text)
;
CALL cursor_fail_during_commit()
;
CREATE COLLATION coll_dup_chk (LC_COLLATE = "POSIX", LC_CTYPE = "POSIX", LOCALE = '')
;
ALTER TABLE prt2_adv DETACH PARTITION prt2_adv_p3
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
alter table inh_pp1 alter column f1 set not null
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_event_trigger
;
analyze exists_tbl
;
DROP PUBLICATION testpib_ins_trunct
;
CREATE TABLE "public"."mlparted2" (
    "b" integer NOT NULL,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
DROP COLLATION IF EXISTS split_obj_priv.c_from_c
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_enum
;
CREATE TABLE batch_cp_upd_test (a int) PARTITION BY LIST (a)
;
CREATE POLICY p3_with_default ON document FOR UPDATE
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
;
/*
 * 24.14
 * USER_MAPPINGS view
 */
CREATE VIEW user_mappings AS
    SELECT authorization_identifier,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_user_mappings
;
DROP VIEW instead_of_insert_tbl_view_2
;
DROP SCHEMA regtest_schema CASCADE
;
create view v2 as select * from tt2 join tt3 using (b,c) join tt4 using (b)
;
CREATE TABLE fk_partitioned_fk_4 (a int, b int, FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk(a, b) ON UPDATE CASCADE ON DELETE CASCADE) PARTITION BY RANGE (b, a)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
CREATE TABLE test_like_5c (LIKE test_like_4 INCLUDING ALL)
  INHERITS (test_like_5, test_like_5x)
;
CREATE TEMP TABLE pg_temp_4.pg_temp_37830_2 (tid pg_catalog.tid)
;
CREATE TABLE mcic_two (
	id int
  )
;
--
-- partitioned FK referenced deletes SET DEFAULT
--

DROP TABLE temporal_partitioned_fk_mltrng2mltrng
;
ALTER TABLE not_leftmost ADD CONSTRAINT not_leftmost_pk PRIMARY KEY (c)
;
alter table idxpart add exclude USING GIST (a with =, b with =, c with &&)
;
ALTER TABLE bttest_a SET (autovacuum_enabled = false)
;
CREATE INDEX "dupindexcols_i" ON "public"."dupindexcols" USING "btree" ("f1", "id", "f1" "text_pattern_ops")
;
-- If CREATE INDEX ONLY, don't create indexes on partitions; and existing
-- indexes on partitions don't change parent.  ALTER INDEX ATTACH can change
-- the parent after the fact.
create table idxpart (a int) partition by range (a)
;
DROP TABLE ex_msource, ex_mtarget
;
------------------------------------------------------------------------------------------
[ADD_INDEX] create index testpart_orange_index on testpart_orange(logdate)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_25481_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP AUTO) vactst
;
----------
[CREATE_TABLE] -- fail, seq should have been dropped

-- partitioned table
CREATE TABLE truncparted (a int, b char) PARTITION BY LIST (a)
;
----------
[DROP_POLICY] DROP POLICY p1 ON dob_t1
;
----------
[ALTER_TABLE] ALTER TABLE t SET tablespace regress_ts4
;
----------
[ALTER_TABLE] ----
-- Make sure index cases succeed
----

-- succeed, primary key
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_pkey
;
----------
[CREATE_TABLE] create table atacc2 (test2 int) inherits (atacc1)
;
----------
[CREATE_TYPE] -- With a defined row type, the typcache can inspect the type's fields
-- for hashability.
create type ct1 as (f1 money)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_overlap(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE s.x ADD CONSTRAINT e0rows EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll WITH s.=)
  USING INDEX TABLESPACE regress_create_idx_tblspace
  WHERE (s.index_row_if(y))
;
----------
[ALTER_TYPE] alter type insenum add value 'i23' before 'L2'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ean13, issn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE agg_bad OPTIONS (SET reject_limit '2')
;
----------
[DROP_TYPE] DROP DOMAIN qf_char_domain, qf_jsonb_domain
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk1" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_p2
;
----------
[DROP_PROG_OBJ] drop function whoami()
;
----------
[ADD_INDEX] create index trgm_idx2 on test_trgm2 using gin (t gin_trgm_ops)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_29555_2 ADD COLUMN newdata pg_temp_3.pg_temp_29555
;
----------
[CREATE_TABLE] CREATE TABLE newly_created (a int, b text)
;
----------
[CALL_PROG_OBJ] CALL cursor_fail_during_commit()
;
----------
[CREATE_POLICY] CREATE COLLATION coll_dup_chk (LC_COLLATE = "POSIX", LC_CTYPE = "POSIX", LOCALE = '')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt2_adv DETACH PARTITION prt2_adv_p3
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table inh_pp1 alter column f1 set not null
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_event_trigger
;
----------
[ADMIN_TABLE] analyze exists_tbl
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpib_ins_trunct
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted2" (
    "b" integer NOT NULL,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
----------
[DROP_POLICY] DROP COLLATION IF EXISTS split_obj_priv.c_from_c
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_enum
;
----------
[CREATE_TABLE] CREATE TABLE batch_cp_upd_test (a int) PARTITION BY LIST (a)
;
----------
[CREATE_POLICY] CREATE POLICY p3_with_default ON document FOR UPDATE
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.14
 * USER_MAPPINGS view
 */
CREATE VIEW user_mappings AS
    SELECT authorization_identifier,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_user_mappings
;
----------
[DROP_VIEW] DROP VIEW instead_of_insert_tbl_view_2
;
----------
[DROP_SCHEMA] DROP SCHEMA regtest_schema CASCADE
;
----------
[CREATE_VIEW(SELECT)] create view v2 as select * from tt2 join tt3 using (b,c) join tt4 using (b)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_4 (a int, b int, FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk(a, b) ON UPDATE CASCADE ON DELETE CASCADE) PARTITION BY RANGE (b, a)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
----------
[CREATE_TABLE] CREATE TABLE test_like_5c (LIKE test_like_4 INCLUDING ALL)
  INHERITS (test_like_5, test_like_5x)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_37830_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE mcic_two (
	id int
  )
;
----------
[DROP_TABLE] --
-- partitioned FK referenced deletes SET DEFAULT
--

DROP TABLE temporal_partitioned_fk_mltrng2mltrng
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE not_leftmost ADD CONSTRAINT not_leftmost_pk PRIMARY KEY (c)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add exclude USING GIST (a with =, b with =, c with &&)
;
----------
[ALTER_TABLE] ALTER TABLE bttest_a SET (autovacuum_enabled = false)
;
----------
[ADD_INDEX] CREATE INDEX "dupindexcols_i" ON "public"."dupindexcols" USING "btree" ("f1", "id", "f1" "text_pattern_ops")
;
----------
[CREATE_TABLE] -- If CREATE INDEX ONLY, don't create indexes on partitions; and existing
-- indexes on partitions don't change parent.  ALTER INDEX ATTACH can change
-- the parent after the fact.
create table idxpart (a int) partition by range (a)
;
----------
[DROP_TABLE] DROP TABLE ex_msource, ex_mtarget
;
