-- Make a simple relation with well distributed keys and correctly
-- estimated size.
create table simple as
  select generate_series(1, 20000) AS id, 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
CREATE TABLE idxpart (a INT, b TEXT, c INT) PARTITION BY RANGE(a)
;
-- remove from pp1, should succeed
alter table pp1 alter column f1 drop not null
;
-- Things that shouldn't work:

CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS 'SELECT ''not an integer'';'
;
CREATE PUBLICATION tap_pub_rep_full FOR TABLE people
;
DROP EVENT TRIGGER split_event_helper_ddl_end
;
CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a < b
  WITH LOCAL CHECK OPTION
;
CREATE TABLE TIMESTAMPTZ_TST (a int , b timestamptz)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_range
;
VACUUM (SKIP_LOCKED) part1, part2
;
create trigger trig_table_before_trig
  before insert or update or delete on trig_table
  for each statement execute procedure trigger_func('trig_table')
;
CREATE TABLE ATACC3 (PRIMARY KEY (a)) INHERITS (ATACC1)
;
CREATE FUNCTION functest_F_2(int) RETURNS bool LANGUAGE 'sql'
       CALLED ON NULL INPUT AS 'SELECT $1 = 50'
;
--
-- TIMESTAMPTZ
--

CREATE TABLE TIMESTAMPTZ_TBL (d1 timestamp(2) with time zone)
;
DROP TRIGGER rw_view1_ins_trig on base_tbl
;
CREATE TABLE hash_txt_heap (
	seqno 		int4,
	random 		text
)
;
ALTER TABLE split_alter_table.base_table REPLICA IDENTITY USING INDEX base_table_code_unique
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_namespace
;
ANALYZE public.alpha_pos_p3
;
create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2) $$ language sql
;
CREATE TYPE "public"."int4_d_range" AS RANGE (
    subtype = "public"."int4_d",
    multirange_type_name = "public"."int4_d_multirange"
)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_extension
;
CREATE SCHEMA testns3
;
ANALYZE pg_temp_13.pg_temp_26616
;
DROP TABLE test_table
;
CREATE OPERATOR <@ (
        LEFTARG = ltree,
	RIGHTARG = _ltree,
	PROCEDURE = _ltree_r_isparent,
        COMMUTATOR = '@>',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ALTER TEXT SEARCH CONFIGURATION arabic ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH arabic_stem
;
CREATE TABLE follower (a int REFERENCES leader ON DELETE CASCADE, b int)
;
CREATE VIEW pg_available_extension_versions AS
    SELECT E.name, E.version, (X.extname IS NOT NULL) AS installed,
           E.superuser, E.relocatable, E.schema, E.requires, E.comment
      FROM pg_available_extension_versions() AS E
           LEFT JOIN pg_extension AS X
             ON E.name = X.extname AND E.version = X.extversion
;
drop table temp_inh_oncommit_test
;
TRUNCATE tab1
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '
')
;
CREATE PROCEDURE transaction_test11()
LANGUAGE plpgsql
AS $$
DECLARE
  s1 timestamp with time zone;
  s2 timestamp with time zone;
  s3 timestamp with time zone;
  t1 timestamp with time zone;
  t2 timestamp with time zone;
  t3 timestamp with time zone;
BEGIN
  s1 := statement_timestamp();
  t1 := transaction_timestamp();
  ASSERT s1 = t1;
  PERFORM pg_sleep(0.001);
  COMMIT;
  s2 := statement_timestamp();
  t2 := transaction_timestamp();
  ASSERT s2 = s1;
  ASSERT t2 > t1;
  PERFORM pg_sleep(0.001);
  ROLLBACK;
  s3 := statement_timestamp();
  t3 := transaction_timestamp();
  ASSERT s3 = s1;
  ASSERT t3 > t2;
END;
$$
;
CREATE TABLE BIT_TABLE(b BIT(11))
;
CREATE TABLE range_parted4_2 PARTITION OF range_parted4 FOR VALUES FROM (3, 4, 5) TO (6, 7, MAXVALUE)
;
create table covidxpart3 (b int, c int, a int)
;
VACUUM (FREEZE) pg_catalog.pg_shdescription
;
create temp table boolindex (b bool, i int, unique(b, i), junk float)
;
/*
 * 4. CREATE INDEX CONCURRENTLY
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
--
-- Test that wholerow references to ON CONFLICT's EXCLUDED work
--
create unique index plain on insertconflicttest(key)
;
CREATE TABLE fail_default_part PARTITION OF hash_parted DEFAULT
;
ALTER TABLE "public"."invalid_check_con"
    ADD CONSTRAINT "inh_check_constraint" CHECK (("f1" > 0)) NOT VALID
;
CREATE TEMP TABLE concur_temp (f1 int, f2 text)
  ON COMMIT PRESERVE ROWS
;
-- case where parent will have a dropped column, but the partition won't
alter table parted_conflict_test drop b, add b char
;
CREATE FUNCTION pgstatindex(IN relname regclass,
    OUT version INT,
    OUT tree_level INT,
    OUT index_size BIGINT,
    OUT root_block_no BIGINT,
    OUT internal_pages BIGINT,
    OUT leaf_pages BIGINT,
    OUT empty_pages BIGINT,
    OUT deleted_pages BIGINT,
    OUT avg_leaf_density FLOAT8,
    OUT leaf_fragmentation FLOAT8)
AS '$libdir/pgstattuple', 'pgstatindexbyid'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP FUNCTION IF EXISTS f3() CASCADE
;
alter table atacc1 alter test_a drop not null, alter test_b drop not null
;
drop function count_tt1_v()
;
create or replace function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return false;
  else
    return coalesce($1::int,0) = coalesce($2::int, 0);
  end if;
end;
$$ language plpgsql immutable
;
------------------------------------------------------------------------------------------
[CREATE_TABLE|SELECT] -- Make a simple relation with well distributed keys and correctly
-- estimated size.
create table simple as
  select generate_series(1, 20000) AS id, 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_TABLE] CREATE TABLE idxpart (a INT, b TEXT, c INT) PARTITION BY RANGE(a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- remove from pp1, should succeed
alter table pp1 alter column f1 drop not null
;
----------
[CREATE_PROG_OBJ] -- Things that shouldn't work:

CREATE FUNCTION test1 (int) RETURNS int LANGUAGE SQL
    AS 'SELECT ''not an integer'';'
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_rep_full FOR TABLE people
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER split_event_helper_ddl_end
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a < b
  WITH LOCAL CHECK OPTION
;
----------
[CREATE_TABLE] CREATE TABLE TIMESTAMPTZ_TST (a int , b timestamptz)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_range
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) part1, part2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_table_before_trig
  before insert or update or delete on trig_table
  for each statement execute procedure trigger_func('trig_table')
;
----------
[CREATE_TABLE] CREATE TABLE ATACC3 (PRIMARY KEY (a)) INHERITS (ATACC1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_F_2(int) RETURNS bool LANGUAGE 'sql'
       CALLED ON NULL INPUT AS 'SELECT $1 = 50'
;
----------
[CREATE_TABLE] --
-- TIMESTAMPTZ
--

CREATE TABLE TIMESTAMPTZ_TBL (d1 timestamp(2) with time zone)
;
----------
[DROP_TRIGGER] DROP TRIGGER rw_view1_ins_trig on base_tbl
;
----------
[CREATE_TABLE] CREATE TABLE hash_txt_heap (
	seqno 		int4,
	random 		text
)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table REPLICA IDENTITY USING INDEX base_table_code_unique
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_namespace
;
----------
[ADMIN_TABLE] ANALYZE public.alpha_pos_p3
;
----------
[CREATE_PROG_OBJ] create or replace function rngfuncbar(out integer, out numeric) as
$$ select (1, 2) $$ language sql
;
----------
[CREATE_TYPE] CREATE TYPE "public"."int4_d_range" AS RANGE (
    subtype = "public"."int4_d",
    multirange_type_name = "public"."int4_d_multirange"
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_extension
;
----------
[CREATE_SCHEMA] CREATE SCHEMA testns3
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26616
;
----------
[DROP_TABLE] DROP TABLE test_table
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <@ (
        LEFTARG = ltree,
	RIGHTARG = _ltree,
	PROCEDURE = _ltree_r_isparent,
        COMMUTATOR = '@>',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION arabic ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH arabic_stem
;
----------
[CREATE_TABLE] CREATE TABLE follower (a int REFERENCES leader ON DELETE CASCADE, b int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_available_extension_versions AS
    SELECT E.name, E.version, (X.extname IS NOT NULL) AS installed,
           E.superuser, E.relocatable, E.schema, E.requires, E.comment
      FROM pg_available_extension_versions() AS E
           LEFT JOIN pg_extension AS X
             ON E.name = X.extname AND E.version = X.extversion
;
----------
[DROP_TABLE] drop table temp_inh_oncommit_test
;
----------
[TRUNCATE_TABLE] TRUNCATE tab1
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '
')
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test11()
LANGUAGE plpgsql
AS $$
DECLARE
  s1 timestamp with time zone;
  s2 timestamp with time zone;
  s3 timestamp with time zone;
  t1 timestamp with time zone;
  t2 timestamp with time zone;
  t3 timestamp with time zone;
BEGIN
  s1 := statement_timestamp();
  t1 := transaction_timestamp();
  ASSERT s1 = t1;
  PERFORM pg_sleep(0.001);
  COMMIT;
  s2 := statement_timestamp();
  t2 := transaction_timestamp();
  ASSERT s2 = s1;
  ASSERT t2 > t1;
  PERFORM pg_sleep(0.001);
  ROLLBACK;
  s3 := statement_timestamp();
  t3 := transaction_timestamp();
  ASSERT s3 = s1;
  ASSERT t3 > t2;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE BIT_TABLE(b BIT(11))
;
----------
[CREATE_TABLE] CREATE TABLE range_parted4_2 PARTITION OF range_parted4 FOR VALUES FROM (3, 4, 5) TO (6, 7, MAXVALUE)
;
----------
[CREATE_TABLE] create table covidxpart3 (b int, c int, a int)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_shdescription
;
----------
[CREATE_TABLE] create temp table boolindex (b bool, i int, unique(b, i), junk float)
;
----------
[CREATE_TABLE] /*
 * 4. CREATE INDEX CONCURRENTLY
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
----------
[ADD_INDEX] --
-- Test that wholerow references to ON CONFLICT's EXCLUDED work
--
create unique index plain on insertconflicttest(key)
;
----------
[CREATE_TABLE] CREATE TABLE fail_default_part PARTITION OF hash_parted DEFAULT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."invalid_check_con"
    ADD CONSTRAINT "inh_check_constraint" CHECK (("f1" > 0)) NOT VALID
;
----------
[CREATE_TABLE] CREATE TEMP TABLE concur_temp (f1 int, f2 text)
  ON COMMIT PRESERVE ROWS
;
----------
[ALTER_TABLE|DROP_COLUMN|ADD_COLUMN] -- case where parent will have a dropped column, but the partition won't
alter table parted_conflict_test drop b, add b char
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgstatindex(IN relname regclass,
    OUT version INT,
    OUT tree_level INT,
    OUT index_size BIGINT,
    OUT root_block_no BIGINT,
    OUT internal_pages BIGINT,
    OUT leaf_pages BIGINT,
    OUT empty_pages BIGINT,
    OUT deleted_pages BIGINT,
    OUT avg_leaf_density FLOAT8,
    OUT leaf_fragmentation FLOAT8)
AS '$libdir/pgstattuple', 'pgstatindexbyid'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS f3() CASCADE
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter test_a drop not null, alter test_b drop not null
;
----------
[DROP_PROG_OBJ] drop function count_tt1_v()
;
----------
[CREATE_PROG_OBJ] create or replace function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return false;
  else
    return coalesce($1::int,0) = coalesce($2::int, 0);
  end if;
end;
$$ language plpgsql immutable
;
