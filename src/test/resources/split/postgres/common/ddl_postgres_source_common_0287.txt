-- sql, proretset = t, prorettype = b
CREATE FUNCTION getrngfunc3(int) RETURNS setof text AS 'SELECT rngfuncname FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
CREATE MATERIALIZED VIEW testschema.amv AS SELECT * FROM testschema.atable
;
CREATE TABLE "public"."shoelace_data" (
    "sl_name" character(10),
    "sl_avail" integer,
    "sl_color" character(10),
    "sl_len" double precision,
    "sl_unit" character(8)
)
;
-- ERROR

-- on_error, log_verbosity and reject_limit tests
ALTER FOREIGN TABLE agg_bad OPTIONS (ADD on_error 'ignore')
;
create event trigger regress_event_trigger on elephant_bootstrap
   execute procedure test_event_trigger()
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_class
;
create rule t1_ins_1 as on insert to t1
	where new.a >= 0 and new.a < 10
	do instead
	insert into t1_1 values (new.a)
;
create aggregate aggfns(integer,integer,text) (
   sfunc = aggfns_trans, stype = aggtype[], sspace = 10000,
   initcond = '{}'
)
;
DROP TABLE num_result
;
ALTER DATABASE regression_utf8 SET TABLESPACE regress_tblspace
;
-- test attinhcount tracking with merged columns

create table depth0()
;
-- Test brin_summarize_range
CREATE TABLE brin_summarize_multi (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
ALTER FUNCTION gbt_ts_compress(internal) PARALLEL SAFE
;
CREATE TABLE vacparted1 PARTITION OF vacparted FOR VALUES IN (1)
;
-- should fail

CREATE OR REPLACE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a < 10
  WITH LOCAL CHECK OPTION
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
ALTER TABLE ONLY "public"."stud_emp" INHERIT "public"."emp"
;
DROP TABLE testpub_rf_tbl2
;
CREATE OPERATOR FAMILY alt_opf14 USING btree
;
-- check implicit coercion
create function dfunc(a int DEFAULT 1.0, int DEFAULT '-1') returns int as $$
  select $1 + $2;
$$ language sql
;
CREATE DATABASE "regression	 !""x""#$%&'()*+-./0123456" WITH TEMPLATE = template0 ENCODING = 'LATIN1' LOCALE_PROVIDER = libc LOCALE = 'C'
;
CREATE FUNCTION gbt_intv_same(gbtreekey32, gbtreekey32, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE ref_tbl (a int)
;
create table mlparted (a int, b int) partition by range (a, b)
;
ANALYZE vactst (i), vacparted (does_not_exist)
;
CREATE SUBSCRIPTION regression_sub CONNECTION 'port=10246 host=/tmp/jZENFq3HZU dbname=postgres' PUBLICATION alice
;
create table lp_bc partition of lp for values in ('b', 'c')
;
CREATE INDEX onek2_u2_prtl ON onek2 USING btree(unique2 int4_ops)
	where stringu1 < 'B'
;
create unlogged table pgbench_history(tid int,bid int,aid    int,delta int,mtime timestamp,filler char(22)) tablespace "regress_pgbench_tap_1_ts"
;
ALTER TABLE ONLY "public"."prt2_m" ATTACH PARTITION "public"."prt2_m_p3" FOR VALUES FROM (500, 500) TO (600, 600)
;
create schema alter2
;
-- try some things with short-header and toasted subscript values

drop table test_jsonb_subscript
;
CREATE OPERATOR %% (
       RIGHTARG = hstore,
       PROCEDURE = hstore_to_array
)
;
ALTER SUBSCRIPTION regress_testsub SET (streaming = parallel)
;
alter table anothertab
  add exclude using btree (f3 with =)
;
VACUUM (ANALYZE) pg_catalog.pg_ts_parser
;
/* contrib/pg_walinspect/pg_walinspect--1.0--1.1.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Unsupported functions after 1.1.
DROP FUNCTION pg_get_wal_records_info_till_end_of_wal(pg_lsn)
;
CREATE FUNCTION brin_page_items(IN page bytea, IN index_oid regclass,
	OUT itemoffset int,
	OUT blknum int,
	OUT attnum int,
	OUT allnulls bool,
	OUT hasnulls bool,
	OUT placeholder bool,
	OUT value text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'brin_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE FUNCTION "public"."outparam_succeed2"("i" "anyrange", OUT "r" "anyarray", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select ARRAY[upper($1)], 'foo'::text $_$
;
create rule r1 as on update to rules_src do also
  insert into rules_log values(old.*, 'old', default), (new.*, 'new', default)
;
ALTER TABLE ONLY "public"."cc2" INHERIT "public"."pp1"
;
CREATE TABLE pagg_tab_p2 PARTITION OF pagg_tab FOR VALUES IN ('0005', '0006', '0007', '0008')
;
create temp table root(f1 int primary key)
;
ALTER TABLE ONLY "regress_indexing"."pk52"
    ADD CONSTRAINT "pk52_pkey" PRIMARY KEY ("a")
;
CREATE FUNCTION gen_salt(text, int4)
RETURNS text
AS '$libdir/pgcrypto', 'pg_gen_salt_rounds'
LANGUAGE C VOLATILE STRICT PARALLEL SAFE
;
CREATE FUNCTION "public"."max_xacttest"() RETURNS smallint
    LANGUAGE "plpgsql"
    AS $$begin return max(a) from xacttest; end$$
;
ALTER TABLE clstr_tst CLUSTER ON clstr_tst_b_c
;
DROP TABLE notnull_inhparent, notnull_inhchild, notnull_inhgrand
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."shighway" INHERIT "public"."road"
;
CREATE OPERATOR > (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_gt,
        COMMUTATOR = '<',
	NEGATOR = '<=',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- sql, proretset = t, prorettype = b
CREATE FUNCTION getrngfunc3(int) RETURNS setof text AS 'SELECT rngfuncname FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW testschema.amv AS SELECT * FROM testschema.atable
;
----------
[CREATE_TABLE] CREATE TABLE "public"."shoelace_data" (
    "sl_name" character(10),
    "sl_avail" integer,
    "sl_color" character(10),
    "sl_len" double precision,
    "sl_unit" character(8)
)
;
----------
[ALTER_TABLE] -- ERROR

-- on_error, log_verbosity and reject_limit tests
ALTER FOREIGN TABLE agg_bad OPTIONS (ADD on_error 'ignore')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger on elephant_bootstrap
   execute procedure test_event_trigger()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_class
;
----------
[CREATE_POLICY] create rule t1_ins_1 as on insert to t1
	where new.a >= 0 and new.a < 10
	do instead
	insert into t1_1 values (new.a)
;
----------
[CREATE_PROG_OBJ] create aggregate aggfns(integer,integer,text) (
   sfunc = aggfns_trans, stype = aggtype[], sspace = 10000,
   initcond = '{}'
)
;
----------
[DROP_TABLE] DROP TABLE num_result
;
----------
[ALTER_CATALOG] ALTER DATABASE regression_utf8 SET TABLESPACE regress_tblspace
;
----------
[CREATE_TABLE] -- test attinhcount tracking with merged columns

create table depth0()
;
----------
[CREATE_TABLE] -- Test brin_summarize_range
CREATE TABLE brin_summarize_multi (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_ts_compress(internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE vacparted1 PARTITION OF vacparted FOR VALUES IN (1)
;
----------
[CREATE_VIEW(SELECT)] -- should fail

CREATE OR REPLACE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a < 10
  WITH LOCAL CHECK OPTION
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."stud_emp" INHERIT "public"."emp"
;
----------
[DROP_TABLE] DROP TABLE testpub_rf_tbl2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf14 USING btree
;
----------
[CREATE_PROG_OBJ] -- check implicit coercion
create function dfunc(a int DEFAULT 1.0, int DEFAULT '-1') returns int as $$
  select $1 + $2;
$$ language sql
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression	 !""x""#$%&'()*+-./0123456" WITH TEMPLATE = template0 ENCODING = 'LATIN1' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_intv_same(gbtreekey32, gbtreekey32, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE ref_tbl (a int)
;
----------
[CREATE_TABLE] create table mlparted (a int, b int) partition by range (a, b)
;
----------
[ADMIN_TABLE] ANALYZE vactst (i), vacparted (does_not_exist)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regression_sub CONNECTION 'port=10246 host=/tmp/jZENFq3HZU dbname=postgres' PUBLICATION alice
;
----------
[CREATE_TABLE] create table lp_bc partition of lp for values in ('b', 'c')
;
----------
[ADD_INDEX] CREATE INDEX onek2_u2_prtl ON onek2 USING btree(unique2 int4_ops)
	where stringu1 < 'B'
;
----------
[CREATE_TABLE] create unlogged table pgbench_history(tid int,bid int,aid    int,delta int,mtime timestamp,filler char(22)) tablespace "regress_pgbench_tap_1_ts"
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_m" ATTACH PARTITION "public"."prt2_m_p3" FOR VALUES FROM (500, 500) TO (600, 600)
;
----------
[CREATE_SCHEMA] create schema alter2
;
----------
[DROP_TABLE] -- try some things with short-header and toasted subscript values

drop table test_jsonb_subscript
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR %% (
       RIGHTARG = hstore,
       PROCEDURE = hstore_to_array
)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (streaming = parallel)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table anothertab
  add exclude using btree (f3 with =)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_ts_parser
;
----------
[DROP_PROG_OBJ] /* contrib/pg_walinspect/pg_walinspect--1.0--1.1.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Unsupported functions after 1.1.
DROP FUNCTION pg_get_wal_records_info_till_end_of_wal(pg_lsn)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION brin_page_items(IN page bytea, IN index_oid regclass,
	OUT itemoffset int,
	OUT blknum int,
	OUT attnum int,
	OUT allnulls bool,
	OUT hasnulls bool,
	OUT placeholder bool,
	OUT value text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'brin_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."outparam_succeed2"("i" "anyrange", OUT "r" "anyarray", OUT "t" "text") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select ARRAY[upper($1)], 'foo'::text $_$
;
----------
[CREATE_POLICY] create rule r1 as on update to rules_src do also
  insert into rules_log values(old.*, 'old', default), (new.*, 'new', default)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."cc2" INHERIT "public"."pp1"
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_p2 PARTITION OF pagg_tab FOR VALUES IN ('0005', '0006', '0007', '0008')
;
----------
[CREATE_TABLE] create temp table root(f1 int primary key)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "regress_indexing"."pk52"
    ADD CONSTRAINT "pk52_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gen_salt(text, int4)
RETURNS text
AS '$libdir/pgcrypto', 'pg_gen_salt_rounds'
LANGUAGE C VOLATILE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."max_xacttest"() RETURNS smallint
    LANGUAGE "plpgsql"
    AS $$begin return max(a) from xacttest; end$$
;
----------
[ALTER_TABLE] ALTER TABLE clstr_tst CLUSTER ON clstr_tst_b_c
;
----------
[DROP_TABLE] DROP TABLE notnull_inhparent, notnull_inhchild, notnull_inhgrand
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."shighway" INHERIT "public"."road"
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_gt,
        COMMUTATOR = '<',
	NEGATOR = '<=',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
