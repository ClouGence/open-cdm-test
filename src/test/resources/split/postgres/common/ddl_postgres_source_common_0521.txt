-- but let's put them in an extension ...
ALTER EXTENSION postgres_fdw ADD FUNCTION postgres_fdw_abs(int)
;
COMMENT ON TABLE attmp IS 'table comment'
;
CREATE FUNCTION isnle(issn, issn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
alter table fullname add column suffix text default ''
;
DROP FUNCTION test_stat_func_ifexists()
;
CREATE TABLE "generated_stored_tests"."gtest23q" (
    "a" integer NOT NULL,
    "b" integer
)
;
VACUUM (FREEZE) pg_catalog.pg_authid
;
CREATE INDEX varlena_bug_idx on varlena_bug(v)
;
--
-- pg_get_wal_records_info_till_end_of_wal()
--
CREATE FUNCTION pg_get_wal_records_info_till_end_of_wal(IN start_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_records_info_till_end_of_wal'
LANGUAGE C STRICT PARALLEL SAFE
;
-- test hstore -> python
CREATE FUNCTION test1(val hstore) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
assert isinstance(val, dict)
plpy.info(sorted(val.items()))
return len(val)
$$
;
create temp view view_a as select * from table_a
;
ALTER INDEX pg_toast.pg_toast_33348_index RENAME TO reind_con_toast_idx
;
CREATE INDEX brin_summarize_bloom_idx ON brin_summarize_bloom USING brin (value) WITH (pages_per_range=2)
;
CREATE TABLE ctlt_all (LIKE ctlt1 INCLUDING ALL)
;
CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=25084 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION pub_dropped_cols
;
create table range_parted (
	a text,
	b int
) partition by range (a, (b+0))
;
create table rlp4_1 partition of rlp4 for values from (20) to (25)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_aggregate
;
DROP FUNCTION iamalongfunction()
;
ANALYZE public.attach_parted_part1
;
CREATE RULE v1_upd_rule AS ON UPDATE TO v1 DO INSTEAD
  UPDATE t1 SET a = NEW.a WHERE a = OLD.a
;
-- changes name of a constraint recursively
ALTER TABLE fd_pt1 RENAME CONSTRAINT fd_pt1chk3 TO f2_check
;
--
-- Text Search Template
--
CREATE TEXT SEARCH TEMPLATE alt_ts_temp1 (lexize=dsimple_lexize)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
CREATE FUNCTION _int_contained(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- this won't get inlined because of type coercion, but it shouldn't fail

create or replace function rngfuncbar() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar ; $$
language sql stable
;
COMMENT ON CONSTRAINT "comment_test_positive_col_check" ON "public"."comment_test" IS 'CHECK constraint on comment_test.positive_col'
;
ALTER TABLE ctlt3 ALTER COLUMN a SET STORAGE MAIN
;
DROP TABLE temporal_rng3
;
ALTER TABLE test_oat_schema.test_oat_tab DISABLE ROW LEVEL SECURITY
;
CREATE VIEW nontemp3 AS SELECT * FROM t1 LEFT JOIN t2 ON t1.num = t2.num2
;
CREATE TEMP TABLE temp_parted (a int) PARTITION BY LIST (a)
;
CREATE TABLE public.ths (
    mod integer,
    data integer
)
PARTITION BY HASH (mod)
;
--
-- Test that replica identity can be set on an index that's not yet valid.
-- (This matches the way pg_dump will try to dump a partitioned table.)
--
CREATE TABLE test_replica_identity4(id integer NOT NULL) PARTITION BY LIST (id)
;
DROP TABLE footrg
;
CREATE VIEW pg_hba_file_rules AS
   SELECT * FROM pg_hba_file_rules() AS A
;
create unique index parted_replica_idx
  on only parted_replica_tab using btree (id)
;
alter table pp1 add primary key (f1)
;
ALTER TABLE pg_toast.pg_toast_30737 RENAME TO reind_con_toast
;
create type arrayrange as range (subtype=int4[])
;
-- fail

-- prevent using prohibited expressions in the key
CREATE FUNCTION retset (a int) RETURNS SETOF int AS $$ SELECT 1; $$ LANGUAGE SQL IMMUTABLE
;
CREATE FUNCTION isnlt(isbn, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE range_parted (
	a int,
	b int
) PARTITION BY RANGE (a, b)
;
DROP TABLE orderstest cascade
;
DROP TABLE unparted, fail_part
;
CREATE TABLE "fkpart6"."pk12" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
-- attaching idxpart22 is not enough to set idxpart22_a_idx valid ...
alter index idxpart2_a_idx attach partition idxpart22_a_idx
;
CREATE FUNCTION translate( citext, citext, text ) RETURNS TEXT AS $$
    SELECT pg_catalog.translate( pg_catalog.translate( $1::pg_catalog.text, pg_catalog.lower($2::pg_catalog.text), $3), pg_catalog.upper($2::pg_catalog.text), $3);
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf3 RENAME TO alt_ts_conf4
;
ALTER EVENT TRIGGER split_event_probe DISABLE
;
------------------------------------------------------------------------------------------
[ALTER_LIBRARY] -- but let's put them in an extension ...
ALTER EXTENSION postgres_fdw ADD FUNCTION postgres_fdw_abs(int)
;
----------
[COMMENT_TABLE] COMMENT ON TABLE attmp IS 'table comment'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(issn, issn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table fullname add column suffix text default ''
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_stat_func_ifexists()
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest23q" (
    "a" integer NOT NULL,
    "b" integer
)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_authid
;
----------
[ADD_INDEX] CREATE INDEX varlena_bug_idx on varlena_bug(v)
;
----------
[CREATE_PROG_OBJ] --
-- pg_get_wal_records_info_till_end_of_wal()
--
CREATE FUNCTION pg_get_wal_records_info_till_end_of_wal(IN start_lsn pg_lsn,
    OUT start_lsn pg_lsn,
    OUT end_lsn pg_lsn,
    OUT prev_lsn pg_lsn,
    OUT xid xid,
    OUT resource_manager text,
    OUT record_type text,
    OUT record_length int4,
    OUT main_data_length int4,
    OUT fpi_length int4,
    OUT description text,
    OUT block_ref text
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_records_info_till_end_of_wal'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- test hstore -> python
CREATE FUNCTION test1(val hstore) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
assert isinstance(val, dict)
plpy.info(sorted(val.items()))
return len(val)
$$
;
----------
[CREATE_VIEW(SELECT)] create temp view view_a as select * from table_a
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33348_index RENAME TO reind_con_toast_idx
;
----------
[ADD_INDEX] CREATE INDEX brin_summarize_bloom_idx ON brin_summarize_bloom USING brin (value) WITH (pages_per_range=2)
;
----------
[CREATE_TABLE] CREATE TABLE ctlt_all (LIKE ctlt1 INCLUDING ALL)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=25084 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION pub_dropped_cols
;
----------
[CREATE_TABLE] create table range_parted (
	a text,
	b int
) partition by range (a, (b+0))
;
----------
[CREATE_TABLE] create table rlp4_1 partition of rlp4 for values from (20) to (25)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_aggregate
;
----------
[DROP_PROG_OBJ] DROP FUNCTION iamalongfunction()
;
----------
[ADMIN_TABLE] ANALYZE public.attach_parted_part1
;
----------
[CREATE_POLICY] CREATE RULE v1_upd_rule AS ON UPDATE TO v1 DO INSTEAD
  UPDATE t1 SET a = NEW.a WHERE a = OLD.a
;
----------
[RENAME_CONSTRAINT] -- changes name of a constraint recursively
ALTER TABLE fd_pt1 RENAME CONSTRAINT fd_pt1chk3 TO f2_check
;
----------
[CREATE_POLICY] --
-- Text Search Template
--
CREATE TEXT SEARCH TEMPLATE alt_ts_temp1 (lexize=dsimple_lexize)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_contained(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- this won't get inlined because of type coercion, but it shouldn't fail

create or replace function rngfuncbar() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar ; $$
language sql stable
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT "comment_test_positive_col_check" ON "public"."comment_test" IS 'CHECK constraint on comment_test.positive_col'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ctlt3 ALTER COLUMN a SET STORAGE MAIN
;
----------
[DROP_TABLE] DROP TABLE temporal_rng3
;
----------
[ALTER_TABLE] ALTER TABLE test_oat_schema.test_oat_tab DISABLE ROW LEVEL SECURITY
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW nontemp3 AS SELECT * FROM t1 LEFT JOIN t2 ON t1.num = t2.num2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temp_parted (a int) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE public.ths (
    mod integer,
    data integer
)
PARTITION BY HASH (mod)
;
----------
[CREATE_TABLE] --
-- Test that replica identity can be set on an index that's not yet valid.
-- (This matches the way pg_dump will try to dump a partitioned table.)
--
CREATE TABLE test_replica_identity4(id integer NOT NULL) PARTITION BY LIST (id)
;
----------
[DROP_TABLE] DROP TABLE footrg
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_hba_file_rules AS
   SELECT * FROM pg_hba_file_rules() AS A
;
----------
[ADD_INDEX] create unique index parted_replica_idx
  on only parted_replica_tab using btree (id)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pp1 add primary key (f1)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30737 RENAME TO reind_con_toast
;
----------
[CREATE_TYPE] create type arrayrange as range (subtype=int4[])
;
----------
[CREATE_PROG_OBJ] -- fail

-- prevent using prohibited expressions in the key
CREATE FUNCTION retset (a int) RETURNS SETOF int AS $$ SELECT 1; $$ LANGUAGE SQL IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(isbn, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE range_parted (
	a int,
	b int
) PARTITION BY RANGE (a, b)
;
----------
[DROP_TABLE] DROP TABLE orderstest cascade
;
----------
[DROP_TABLE] DROP TABLE unparted, fail_part
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart6"."pk12" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[ALTER_INDEX] -- attaching idxpart22 is not enough to set idxpart22_a_idx valid ...
alter index idxpart2_a_idx attach partition idxpart22_a_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION translate( citext, citext, text ) RETURNS TEXT AS $$
    SELECT pg_catalog.translate( pg_catalog.translate( $1::pg_catalog.text, pg_catalog.lower($2::pg_catalog.text), $3), pg_catalog.upper($2::pg_catalog.text), $3);
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION alt_ts_conf3 RENAME TO alt_ts_conf4
;
----------
[ALTER_TRIGGER] ALTER EVENT TRIGGER split_event_probe DISABLE
;
