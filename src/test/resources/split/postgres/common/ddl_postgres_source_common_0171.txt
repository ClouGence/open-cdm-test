ANALYZE pg_temp_2.pg_temp_30214
;
-- error


-- tests for functions related to TID handling

CREATE TABLE tid_tab (a int)
;
create trigger check_fkeys2_pkey_exist
	after insert or update on fkeys2
	for each row
	execute procedure
	check_primary_key ('fkey21', 'fkey22', 'pkeys', 'pkey1', 'pkey2')
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_replication_origin
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_transform
;
-- it should roll back on error, though
create or replace function myfunc(int) returns text as $$
begin
  set work_mem = '2MB';
  perform 1/$1;
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
-- Test that parent and child CHECK constraints can be created in either order
create table p1(f1 int)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_ts_template
;
ANALYZE public.domain_test
;
CREATE TABLE pagg_tab6_p2 PARTITION OF pagg_tab6 FOR VALUES IN ('c', 'd')
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
CREATE INDEX guid1_btree ON guid1 USING BTREE (guid_field)
;
drop table mlparted5
;
CREATE TABLE pp_enumpart2 PARTITION OF pp_enumpart
							FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
ALTER TABLE ONLY "public"."customer"
    ADD CONSTRAINT "customer_pkey" PRIMARY KEY ("cid")
;
ALTER DATABASE split_db_probe SET work_mem = '8MB'
;
CREATE FUNCTION gbt_enum_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- not supported
ALTER TABLE regtest_table SET TABLESPACE pg_default
;
--
-- Verify trigger creation for partitioned tables, and drop behavior
--
create table trigpart (a int, b int) partition by range (a)
;
ALTER TABLE ONLY "regress_rls_schema"."rls_tbl_force" FORCE ROW LEVEL SECURITY
;
DROP AGGREGATE regtest_agg(int)
;
ALTER TABLE ONLY "public"."itest5" ALTER COLUMN "a" SET DEFAULT "nextval"('"public"."itest5_a_seq"'::"regclass")
;
CREATE PUBLICATION pub_for_tab FOR TABLE test1
;
-- procedure 1 appears first time
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
ALTER TABLE pg_toast.pg_toast_28181 RENAME TO reind_con_toast
;
DROP PUBLICATION tap_pub_B_2
;
--
-- Test CREATE/ALTER TYPE using a type that's compatible with varchar,
-- so we can re-use those support functions
--
CREATE TYPE myvarchar
;
CREATE TABLE gtest10a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED)
;
CREATE TABLE gtest31_1 (a int, b text GENERATED ALWAYS AS ('hello') STORED, c text)
;
drop function cast_invoker(integer)
;
CREATE TYPE "public"."casttesttype" (
    INTERNALLENGTH = variable,
    INPUT = "public"."casttesttype_in",
    OUTPUT = "public"."casttesttype_out",
    ALIGNMENT = int4,
    STORAGE = plain
)
;
CREATE FUNCTION gin_compare_prefix_float8(float8, float8, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_init_privs
;
ANALYZE pg_catalog.pg_publication_namespace
;
CREATE TABLE foo (a oid, b oid, c int, FOREIGN KEY (a, b, c) REFERENCES pg_description)
;
create function explain_parallel_sort_stats() returns setof text
language plpgsql as
$$
declare ln text;
begin
    for ln in
        explain (analyze, timing off, summary off, costs off)
          select * from
          (select ten from tenk1 where ten < 100 order by ten) ss
          right join (values (1),(2),(3)) v(x) on true
    loop
        ln := regexp_replace(ln, 'Memory: \S*',  'Memory: xxx');
        return next ln;
    end loop;
end;
$$
;
alter table tt3 add column e int
;
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING FOR word
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (log_verbosity 'unsupported')
;
ALTER TABLE ONLY "public"."pitest2" ATTACH PARTITION "public"."pitest2_p2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
-- Check inheritance cases

CREATE TEMP TABLE foochild (fc int) INHERITS (foo)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_proc
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) public.vacview
;
VACUUM pg_catalog.pg_sequence
;
ALTER DATABASE "regression" SET "lc_messages" TO 'C'
;
ALTER TABLE part3_self_fk DETACH PARTITION part33_self_fk
;
CREATE TABLE "public"."hash_f8_heap" (
    "seqno" integer,
    "random" double precision
)
;
create table cnn2_parted(a int) partition by list (a)
;
-- statistics on a mix columns and expressions
CREATE TABLE expr_stats (a int, b int, c int)
;
-- fail

create temp table dtest(f1 dtop)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_30214
;
----------
[CREATE_TABLE] -- error


-- tests for functions related to TID handling

CREATE TABLE tid_tab (a int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger check_fkeys2_pkey_exist
	after insert or update on fkeys2
	for each row
	execute procedure
	check_primary_key ('fkey21', 'fkey22', 'pkeys', 'pkey1', 'pkey2')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_replication_origin
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_transform
;
----------
[CREATE_PROG_OBJ] -- it should roll back on error, though
create or replace function myfunc(int) returns text as $$
begin
  set work_mem = '2MB';
  perform 1/$1;
  return current_setting('work_mem');
end $$
language plpgsql
set work_mem = '1MB'
;
----------
[CREATE_TABLE] -- Test that parent and child CHECK constraints can be created in either order
create table p1(f1 int)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_ts_template
;
----------
[ADMIN_TABLE] ANALYZE public.domain_test
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab6_p2 PARTITION OF pagg_tab6 FOR VALUES IN ('c', 'd')
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
----------
[ADD_INDEX] CREATE INDEX guid1_btree ON guid1 USING BTREE (guid_field)
;
----------
[DROP_TABLE] drop table mlparted5
;
----------
[CREATE_TABLE] CREATE TABLE pp_enumpart2 PARTITION OF pp_enumpart
							FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."customer"
    ADD CONSTRAINT "customer_pkey" PRIMARY KEY ("cid")
;
----------
[ALTER_CATALOG] ALTER DATABASE split_db_probe SET work_mem = '8MB'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_enum_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE] -- not supported
ALTER TABLE regtest_table SET TABLESPACE pg_default
;
----------
[CREATE_TABLE] --
-- Verify trigger creation for partitioned tables, and drop behavior
--
create table trigpart (a int, b int) partition by range (a)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "regress_rls_schema"."rls_tbl_force" FORCE ROW LEVEL SECURITY
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE regtest_agg(int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."itest5" ALTER COLUMN "a" SET DEFAULT "nextval"('"public"."itest5_a_seq"'::"regclass")
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_for_tab FOR TABLE test1
;
----------
[ALTER_PROG_OBJ] -- procedure 1 appears first time
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28181 RENAME TO reind_con_toast
;
----------
[DROP_PUB_SUB] DROP PUBLICATION tap_pub_B_2
;
----------
[CREATE_TYPE] --
-- Test CREATE/ALTER TYPE using a type that's compatible with varchar,
-- so we can re-use those support functions
--
CREATE TYPE myvarchar
;
----------
[CREATE_TABLE] CREATE TABLE gtest10a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[CREATE_TABLE] CREATE TABLE gtest31_1 (a int, b text GENERATED ALWAYS AS ('hello') STORED, c text)
;
----------
[DROP_PROG_OBJ] drop function cast_invoker(integer)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."casttesttype" (
    INTERNALLENGTH = variable,
    INPUT = "public"."casttesttype_in",
    OUTPUT = "public"."casttesttype_out",
    ALIGNMENT = int4,
    STORAGE = plain
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_float8(float8, float8, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_init_privs
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_publication_namespace
;
----------
[CREATE_TABLE] CREATE TABLE foo (a oid, b oid, c int, FOREIGN KEY (a, b, c) REFERENCES pg_description)
;
----------
[CREATE_PROG_OBJ] create function explain_parallel_sort_stats() returns setof text
language plpgsql as
$$
declare ln text;
begin
    for ln in
        explain (analyze, timing off, summary off, costs off)
          select * from
          (select ten from tenk1 where ten < 100 order by ten) ss
          right join (values (1),(2),(3)) v(x) on true
    loop
        ln := regexp_replace(ln, 'Memory: \S*',  'Memory: xxx');
        return next ln;
    end loop;
end;
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tt3 add column e int
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING FOR word
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (log_verbosity 'unsupported')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pitest2" ATTACH PARTITION "public"."pitest2_p2" FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
----------
[CREATE_TABLE] -- Check inheritance cases

CREATE TEMP TABLE foochild (fc int) INHERITS (foo)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_proc
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) public.vacview
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_sequence
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET "lc_messages" TO 'C'
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part3_self_fk DETACH PARTITION part33_self_fk
;
----------
[CREATE_TABLE] CREATE TABLE "public"."hash_f8_heap" (
    "seqno" integer,
    "random" double precision
)
;
----------
[CREATE_TABLE] create table cnn2_parted(a int) partition by list (a)
;
----------
[CREATE_TABLE] -- statistics on a mix columns and expressions
CREATE TABLE expr_stats (a int, b int, c int)
;
----------
[CREATE_TABLE] -- fail

create temp table dtest(f1 dtop)
;
