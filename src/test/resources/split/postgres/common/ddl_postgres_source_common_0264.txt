COMMENT ON TRIGGER check_fkeys2_pkey_exist ON fkeys2 IS NULL
;
ANALYZE pg_temp_4.pg_temp_37874
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE TABLE plt1_e_p2 PARTITION OF plt1_e FOR VALUES IN ('0001', '0005', '0002', '0009')
;
create table ma_test_p2 partition of ma_test for values from (10) to (20)
;
CREATE FUNCTION deptest_func() RETURNS void LANGUAGE plpgsql
  AS $$ BEGIN END; $$
;
-- error

ALTER SEQUENCE sequence_test2 RESTART WITH -24 START WITH -24
  INCREMENT BY -4 MINVALUE -36 MAXVALUE -5 CYCLE
;
CREATE POLICY p3 ON rls_tbl_force FOR UPDATE USING (c1 = 8) WITH CHECK (c1 >= 5)
;
--
-- For fkeys2:
-- 	(fkey21, fkey22)	--> pkeys (pkey1, pkey2)
--
create trigger check_fkeys2_pkey_exist
	after insert or update on fkeys2
	for each row
	execute procedure
	check_primary_key ('fkey21', 'fkey22', 'pkeys', 'pkey1', 'pkey2')
;
CREATE COLLATION coll_dup_chk (LC_CTYPE = "POSIX", LC_CTYPE = "NONSENSE", LC_COLLATE = "POSIX")
;
CREATE OPERATOR CLASS char_ops
DEFAULT FOR TYPE "char" USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btcharcmp("char","char"),
    FUNCTION        2       gin_extract_value_char("char", internal),
    FUNCTION        3       gin_extract_query_char("char", internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_char("char","char",int2, internal),
STORAGE         "char"
;
-- variadic aggregate
create function least_accum(anyelement, variadic anyarray)
returns anyelement language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
CREATE INDEX CONCURRENTLY concur_temp_ind ON concur_temp(f1)
;
CREATE FUNCTION equipment_named_ambiguous_1a(hobby hobbies_r)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where hobby = equipment_named_ambiguous_1a.hobby.name'
   LANGUAGE SQL
;
COMMENT ON TEXT SEARCH DICTIONARY catalan_stem IS 'snowball stemmer for catalan language'
;
--
-- Test case for bug with BEFORE trigger followed by AFTER trigger with WHEN
--

CREATE TABLE some_t (some_col boolean NOT NULL)
;
CREATE INDEX i1 ON newly_indexed (c)
;
DROP SCHEMA selinto_schema CASCADE
;
CREATE TEMP TABLE tt1(f1 int)
;
CREATE FUNCTION seg_cmp(seg, seg)
RETURNS int4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table returningwrtest1 partition of returningwrtest for values in (1)
;
VACUUM (PARALLEL 0) pg_catalog.pg_statistic_ext
;
create table nummultirange_test2(nmr nummultirange)
;
TRUNCATE brin_test_multi_1
;
ANALYZE a_star
;
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
CREATE TEMP TABLE fktable (
        x1      INT4 REFERENCES pktable(id1),
        x2      VARCHAR(4) REFERENCES pktable(id2),
        x3      REAL REFERENCES pktable(id3),
        x4      TEXT,
        x5      INT2
)
;
DROP TABLE unlogged3
;
-- fail, no not-null constraint
alter table idxpart0 alter column a set not null
;
CREATE FUNCTION bqarr_out(query_int)
RETURNS cstring
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP FUNCTION explain_filter
;
-- Add function and aggregate into extension
alter extension postgres_fdw add function least_accum(anyelement, variadic anyarray)
;
CREATE OR REPLACE FUNCTION test_read_multixact(xid) RETURNS VOID
  AS 'MODULE_PATHNAME', 'test_read_multixact'LANGUAGE C
;
-- fail
ALTER TABLE ONLY constraint_rename_test RENAME CONSTRAINT con1 TO con1foo
;
create or replace function compos() returns compostype as $$
declare x int := 42;
begin
  return x;
end;
$$ language plpgsql
;
CREATE SUBSCRIPTION admin_sub CONNECTION 'port=23898 host=/tmp/cSEy8QSEGS dbname=postgres' PUBLICATION alice
	WITH (run_as_owner = true, password_required = false)
;
DROP TABLE sro_tab
;
create cast (xfloat4 as float4) without function
;
ALTER TABLE pg_auth_members ADD UNIQUE USING INDEX pg_auth_members_member_role_index
;
-- Likewise for procedures.
CREATE PROCEDURE test_ambiguous_procname(int) as $$ begin end; $$ language plpgsql
;
CREATE FUNCTION merge_func() RETURNS integer LANGUAGE plpgsql AS $$
DECLARE
  result integer;
BEGIN
MERGE INTO pa_target t
  USING pa_source s
  ON t.tid = s.sid
  WHEN MATCHED THEN
    UPDATE SET tid = tid + 1, balance = balance + delta, val = val || ' updated by merge'
  WHEN NOT MATCHED THEN
    INSERT VALUES (sid, delta, 'inserted by merge')
  WHEN NOT MATCHED BY SOURCE THEN
    UPDATE SET tid = 1, val = val || ' not matched by source';
IF FOUND THEN
  GET DIAGNOSTICS result := ROW_COUNT;
END IF;
RETURN result;
END;
$$
;
DROP TABLE pg_temp_4.pg_temp_37830_2, pg_temp_4.pg_temp_37830
;
CREATE POLICY policy_range_parted_wholerow on range_parted AS RESTRICTIVE for UPDATE USING (true)
   WITH CHECK (range_parted = row('b', 10, 112, 1, NULL)::range_parted)
;
CREATE UNIQUE INDEX "covidxpart1_a_b_idx" ON "public"."covidxpart1" USING "btree" ("a") INCLUDE ("b")
;
alter table inh_child inherit inh_parent
;
CREATE FUNCTION dblink_get_notify(
    OUT notify_name TEXT,
    OUT be_pid INT4,
    OUT extra TEXT
)
RETURNS setof record
AS '$libdir/dblink', 'dblink_get_notify'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
create index ab_a3_b3_a_idx on ab_a3_b3 (a)
;
alter table child1 no inherit parent
;
DROP FUNCTION op_leak(int, int)
;
------------------------------------------------------------------------------------------
[COMMENT_TRIGGER] COMMENT ON TRIGGER check_fkeys2_pkey_exist ON fkeys2 IS NULL
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37874
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn13,
	RIGHTARG = isbn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] CREATE TABLE plt1_e_p2 PARTITION OF plt1_e FOR VALUES IN ('0001', '0005', '0002', '0009')
;
----------
[CREATE_TABLE] create table ma_test_p2 partition of ma_test for values from (10) to (20)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION deptest_func() RETURNS void LANGUAGE plpgsql
  AS $$ BEGIN END; $$
;
----------
[ALTER_SEQUENCE] -- error

ALTER SEQUENCE sequence_test2 RESTART WITH -24 START WITH -24
  INCREMENT BY -4 MINVALUE -36 MAXVALUE -5 CYCLE
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON rls_tbl_force FOR UPDATE USING (c1 = 8) WITH CHECK (c1 >= 5)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- For fkeys2:
-- 	(fkey21, fkey22)	--> pkeys (pkey1, pkey2)
--
create trigger check_fkeys2_pkey_exist
	after insert or update on fkeys2
	for each row
	execute procedure
	check_primary_key ('fkey21', 'fkey22', 'pkeys', 'pkey1', 'pkey2')
;
----------
[CREATE_POLICY] CREATE COLLATION coll_dup_chk (LC_CTYPE = "POSIX", LC_CTYPE = "NONSENSE", LC_COLLATE = "POSIX")
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS char_ops
DEFAULT FOR TYPE "char" USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       btcharcmp("char","char"),
    FUNCTION        2       gin_extract_value_char("char", internal),
    FUNCTION        3       gin_extract_query_char("char", internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_char("char","char",int2, internal),
STORAGE         "char"
;
----------
[CREATE_PROG_OBJ] -- variadic aggregate
create function least_accum(anyelement, variadic anyarray)
returns anyelement language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY concur_temp_ind ON concur_temp(f1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION equipment_named_ambiguous_1a(hobby hobbies_r)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where hobby = equipment_named_ambiguous_1a.hobby.name'
   LANGUAGE SQL
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY catalan_stem IS 'snowball stemmer for catalan language'
;
----------
[CREATE_TABLE] --
-- Test case for bug with BEFORE trigger followed by AFTER trigger with WHEN
--

CREATE TABLE some_t (some_col boolean NOT NULL)
;
----------
[ADD_INDEX] CREATE INDEX i1 ON newly_indexed (c)
;
----------
[DROP_SCHEMA] DROP SCHEMA selinto_schema CASCADE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tt1(f1 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_cmp(seg, seg)
RETURNS int4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table returningwrtest1 partition of returningwrtest for values in (1)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_statistic_ext
;
----------
[CREATE_TABLE] create table nummultirange_test2(nmr nummultirange)
;
----------
[TRUNCATE_TABLE] TRUNCATE brin_test_multi_1
;
----------
[ADMIN_TABLE] ANALYZE a_star
;
----------
[CREATE_TABLE] CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
----------
[CREATE_TABLE] CREATE TEMP TABLE fktable (
        x1      INT4 REFERENCES pktable(id1),
        x2      VARCHAR(4) REFERENCES pktable(id2),
        x3      REAL REFERENCES pktable(id3),
        x4      TEXT,
        x5      INT2
)
;
----------
[DROP_TABLE] DROP TABLE unlogged3
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fail, no not-null constraint
alter table idxpart0 alter column a set not null
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bqarr_out(query_int)
RETURNS cstring
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP FUNCTION explain_filter
;
----------
[ALTER_LIBRARY] -- Add function and aggregate into extension
alter extension postgres_fdw add function least_accum(anyelement, variadic anyarray)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_read_multixact(xid) RETURNS VOID
  AS 'MODULE_PATHNAME', 'test_read_multixact'LANGUAGE C
;
----------
[RENAME_CONSTRAINT] -- fail
ALTER TABLE ONLY constraint_rename_test RENAME CONSTRAINT con1 TO con1foo
;
----------
[CREATE_PROG_OBJ] create or replace function compos() returns compostype as $$
declare x int := 42;
begin
  return x;
end;
$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION admin_sub CONNECTION 'port=23898 host=/tmp/cSEy8QSEGS dbname=postgres' PUBLICATION alice
	WITH (run_as_owner = true, password_required = false)
;
----------
[DROP_TABLE] DROP TABLE sro_tab
;
----------
[CREATE_PROG_OBJ] create cast (xfloat4 as float4) without function
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_auth_members ADD UNIQUE USING INDEX pg_auth_members_member_role_index
;
----------
[CREATE_PROG_OBJ] -- Likewise for procedures.
CREATE PROCEDURE test_ambiguous_procname(int) as $$ begin end; $$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION merge_func() RETURNS integer LANGUAGE plpgsql AS $$
DECLARE
  result integer;
BEGIN
MERGE INTO pa_target t
  USING pa_source s
  ON t.tid = s.sid
  WHEN MATCHED THEN
    UPDATE SET tid = tid + 1, balance = balance + delta, val = val || ' updated by merge'
  WHEN NOT MATCHED THEN
    INSERT VALUES (sid, delta, 'inserted by merge')
  WHEN NOT MATCHED BY SOURCE THEN
    UPDATE SET tid = 1, val = val || ' not matched by source';
IF FOUND THEN
  GET DIAGNOSTICS result := ROW_COUNT;
END IF;
RETURN result;
END;
$$
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_37830_2, pg_temp_4.pg_temp_37830
;
----------
[CREATE_POLICY] CREATE POLICY policy_range_parted_wholerow on range_parted AS RESTRICTIVE for UPDATE USING (true)
   WITH CHECK (range_parted = row('b', 10, 112, 1, NULL)::range_parted)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "covidxpart1_a_b_idx" ON "public"."covidxpart1" USING "btree" ("a") INCLUDE ("b")
;
----------
[ALTER_TABLE] alter table inh_child inherit inh_parent
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_get_notify(
    OUT notify_name TEXT,
    OUT be_pid INT4,
    OUT extra TEXT
)
RETURNS setof record
AS '$libdir/dblink', 'dblink_get_notify'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ADD_INDEX] create index ab_a3_b3_a_idx on ab_a3_b3 (a)
;
----------
[ALTER_TABLE] alter table child1 no inherit parent
;
----------
[DROP_PROG_OBJ] DROP FUNCTION op_leak(int, int)
;
