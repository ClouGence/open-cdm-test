DROP SUBSCRIPTION regress_addr_sub
;
drop table descr_t2
;
ANALYZE expr_stats
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_extension
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_language
;
ALTER TABLE pg_toast.pg_toast_27993 RENAME TO reind_con_toast
;
-- test comments
COMMENT ON AGGREGATE newavg_wrong (int4) IS 'an agg comment'
;
-- ************************************************************
-- * AFTER UPDATE on PField
-- *	- Let PSlots of this field follow
-- ************************************************************
create function tg_pfield_au() returns trigger as '
begin
    if new.name != old.name then
        update PSlot set pfname = new.name where pfname = old.name;
    end if;
    return new;
end;
' language plpgsql
;
-- check that run to completion happens in proper ordering

TRUNCATE TABLE y
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26358_2 (tid pg_catalog.tid)
;
ANALYZE pg_catalog.pg_am
;
CREATE TEMP TABLE pg_temp_37.pg_temp_29701_2 (tid pg_catalog.tid)
;
alter table test_01 drop column a
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE FUNCTION hstore_out(hstore)
RETURNS cstring
AS '$libdir/hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
TRUNCATE tab2
;
CREATE FUNCTION isnle(ismn13, ismn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE "public"."itest17" ALTER COLUMN "a" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest17_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
drop type compostype
;
CREATE FUNCTION plperl_to_hstore(val internal) RETURNS hstore
LANGUAGE C STRICT IMMUTABLE
AS '$libdir/hstore_plperl'
;
ANALYZE pg_temp_11.pg_temp_41180_2
;
COMMENT ON LARGE OBJECT 39080 IS 'I Wandered Lonely as a Cloud'
;
CREATE TABLE "public"."system" (
    "name" "text",
    "comment" "text"
)
;
create table mc2p (a int, b int) partition by range (a, b)
;
ANALYZE public.c
;
create type testtype6 as (a int, b point)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13808 host=/tmp/nKhpfOVKvG dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
DROP TABLE pg_temp_26.pg_temp_28293_2, pg_temp_26.pg_temp_28293
;
-- ON COMMIT DELETE ROWS
CREATE TEMP TABLE concur_temp_tab_2 (c1 int, c2 text)
  ON COMMIT DELETE ROWS
;
CREATE TABLE "S 1"."T 1" (
	"C 1" int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10),
	c8 user_enum,
	CONSTRAINT t1_pkey PRIMARY KEY ("C 1")
)
;
CREATE TABLE dump_test.test_table_generated_child1 ()
						 INHERITS (dump_test.test_table_generated)
;
ALTER DOMAIN split_domain_type_next.email_domain DROP DEFAULT
;
create table renameColumnChild (b int) inherits (renameColumn)
;
-- not supported
ALTER TABLE regtest_table ENABLE  TRIGGER regtest_test_trig
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
ANALYZE pg_temp_10.pg_temp_25340_2
;
-- test end of copy marker
CREATE TEMP TABLE testeoc (a text)
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (sum(1))
;
ALTER MATERIALIZED VIEW split_view_probe.base_mv RENAME COLUMN qty TO amount
;
ALTER PUBLICATION tap_pub_B ADD TABLE tab_new
;
/* Test multiple inheritance of column defaults */
CREATE TABLE firstparent (tomorrow date default now()::date + 1)
;
DROP TABLE fract_t
;
-- Check for uniform distribution using the Kolmogorov-Smirnov test.

CREATE FUNCTION ks_test_uniform_random()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
create table atacc1 (test int)
;
create table header_copytest (
	a int,
	b int,
	c text
)
;
CREATE VIEW view_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS view_catalog,
           CAST(nv.nspname AS sql_identifier) AS view_schema,
           CAST(v.relname AS sql_identifier) AS view_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_namespace nv, pg_class v, pg_depend dv,
         pg_depend dt, pg_class t, pg_namespace nt,
         pg_attribute a

    WHERE nv.oid = v.relnamespace
          AND v.relkind = 'v'
          AND v.oid = dv.refobjid
          AND dv.refclassid = 'pg_catalog.pg_class'::regclass
          AND dv.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dv.deptype = 'i'
          AND dv.objid = dt.objid
          AND dv.refobjid <> dt.refobjid
          AND dt.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dt.refclassid = 'pg_catalog.pg_class'::regclass
          AND dt.refobjid = t.oid
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND t.oid = a.attrelid
          AND dt.refobjsubid = a.attnum
          AND pg_has_role(t.relowner, 'USAGE')
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_foreign_table
;
CREATE FUNCTION ltree_lt(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE text
;
drop function test_pg_index_toast_func
;
------------------------------------------------------------------------------------------
[DROP_PUB_SUB] DROP SUBSCRIPTION regress_addr_sub
;
----------
[DROP_TABLE] drop table descr_t2
;
----------
[ADMIN_TABLE] ANALYZE expr_stats
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_extension
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_language
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27993 RENAME TO reind_con_toast
;
----------
[COMMENT_PROG_OBJ] -- test comments
COMMENT ON AGGREGATE newavg_wrong (int4) IS 'an agg comment'
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * AFTER UPDATE on PField
-- *	- Let PSlots of this field follow
-- ************************************************************
create function tg_pfield_au() returns trigger as '
begin
    if new.name != old.name then
        update PSlot set pfname = new.name where pfname = old.name;
    end if;
    return new;
end;
' language plpgsql
;
----------
[TRUNCATE_TABLE] -- check that run to completion happens in proper ordering

TRUNCATE TABLE y
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26358_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_am
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_37.pg_temp_29701_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table test_01 drop column a
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_out(hstore)
RETURNS cstring
AS '$libdir/hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[TRUNCATE_TABLE] TRUNCATE tab2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ismn13, ismn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest17" ALTER COLUMN "a" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest17_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[DROP_TYPE] drop type compostype
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION plperl_to_hstore(val internal) RETURNS hstore
LANGUAGE C STRICT IMMUTABLE
AS '$libdir/hstore_plperl'
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41180_2
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 39080 IS 'I Wandered Lonely as a Cloud'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."system" (
    "name" "text",
    "comment" "text"
)
;
----------
[CREATE_TABLE] create table mc2p (a int, b int) partition by range (a, b)
;
----------
[ADMIN_TABLE] ANALYZE public.c
;
----------
[CREATE_TYPE] create type testtype6 as (a int, b point)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=13808 host=/tmp/nKhpfOVKvG dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_26.pg_temp_28293_2, pg_temp_26.pg_temp_28293
;
----------
[CREATE_TABLE] -- ON COMMIT DELETE ROWS
CREATE TEMP TABLE concur_temp_tab_2 (c1 int, c2 text)
  ON COMMIT DELETE ROWS
;
----------
[CREATE_TABLE] CREATE TABLE "S 1"."T 1" (
	"C 1" int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10),
	c8 user_enum,
	CONSTRAINT t1_pkey PRIMARY KEY ("C 1")
)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_generated_child1 ()
						 INHERITS (dump_test.test_table_generated)
;
----------
[ALTER_TYPE] ALTER DOMAIN split_domain_type_next.email_domain DROP DEFAULT
;
----------
[CREATE_TABLE] create table renameColumnChild (b int) inherits (renameColumn)
;
----------
[ALTER_TABLE] -- not supported
ALTER TABLE regtest_table ENABLE  TRIGGER regtest_test_trig
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_25340_2
;
----------
[CREATE_TABLE] -- test end of copy marker
CREATE TEMP TABLE testeoc (a text)
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (sum(1))
;
----------
[RENAME_COLUMN] ALTER MATERIALIZED VIEW split_view_probe.base_mv RENAME COLUMN qty TO amount
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_B ADD TABLE tab_new
;
----------
[CREATE_TABLE] /* Test multiple inheritance of column defaults */
CREATE TABLE firstparent (tomorrow date default now()::date + 1)
;
----------
[DROP_TABLE] DROP TABLE fract_t
;
----------
[CREATE_PROG_OBJ] -- Check for uniform distribution using the Kolmogorov-Smirnov test.

CREATE FUNCTION ks_test_uniform_random()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
----------
[CREATE_TABLE] create table atacc1 (test int)
;
----------
[CREATE_TABLE] create table header_copytest (
	a int,
	b int,
	c text
)
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW view_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS view_catalog,
           CAST(nv.nspname AS sql_identifier) AS view_schema,
           CAST(v.relname AS sql_identifier) AS view_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_namespace nv, pg_class v, pg_depend dv,
         pg_depend dt, pg_class t, pg_namespace nt,
         pg_attribute a

    WHERE nv.oid = v.relnamespace
          AND v.relkind = 'v'
          AND v.oid = dv.refobjid
          AND dv.refclassid = 'pg_catalog.pg_class'::regclass
          AND dv.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dv.deptype = 'i'
          AND dv.objid = dt.objid
          AND dv.refobjid <> dt.refobjid
          AND dt.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dt.refclassid = 'pg_catalog.pg_class'::regclass
          AND dt.refobjid = t.oid
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND t.oid = a.attrelid
          AND dt.refobjsubid = a.attnum
          AND pg_has_role(t.relowner, 'USAGE')
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_foreign_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_lt(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE text
;
----------
[DROP_PROG_OBJ] drop function test_pg_index_toast_func
;
