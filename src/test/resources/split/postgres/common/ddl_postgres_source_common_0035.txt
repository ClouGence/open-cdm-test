CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN ('1' collate "POSIX")
;
CREATE FUNCTION regexp_replace( citext, citext, text, text ) returns TEXT AS $$
    SELECT pg_catalog.regexp_replace( $1::pg_catalog.text, $2::pg_catalog.text, $3, CASE WHEN pg_catalog.strpos($4, 'c') = 0 THEN  $4 || 'i' ELSE $4 END);
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION gbt_inet_consistent(internal,inet,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_index
;
CREATE AGGREGATE myaggn15b(BASETYPE = anyelement, SFUNC = tfnp,
  STYPE = anyarray, INITCOND = '{}')
;
ALTER INDEX pg_toast.pg_toast_28603_index RENAME TO reind_con_toast_idx
;
---------------------------------------------------
-- ISSN:
CREATE FUNCTION btissncmp(issn, issn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE found_test_tbl (a int)
;
CREATE FUNCTION hstore_send(hstore)
RETURNS bytea
AS '$libdir/hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE ONLY "public"."transition_table_level1"
    ADD CONSTRAINT "transition_table_level1_pkey" PRIMARY KEY ("level1_no")
;
-- error
create temp table temp_part partition of perm_parted default
;
DROP TABLE parent_tbl
;
CREATE PROCEDURE transaction_test8()
LANGUAGE plpgsql
AS $$
BEGIN
    EXECUTE 'CALL transaction_test1(10, $x$baz$x$)';
END;
$$
;
ALTER INDEX pg_toast.pg_toast_30748_index RENAME TO reind_con_toast_idx
;
ALTER INDEX testschema.part_a_idx SET TABLESPACE pg_default
;
CREATE TYPE test7b AS (a int, b priv_testdomain1)
;
DROP TYPE IF EXISTS split_obj_priv.cast_mood
;
CREATE VIEW ext_cor_view AS
  SELECT 'ext_cor_view: original'::text AS col
;
/* New stuff in 1.5 begins here */

CREATE OR REPLACE FUNCTION pgstathashindex(IN relname regclass,
	OUT version INTEGER,
	OUT bucket_pages BIGINT,
	OUT overflow_pages BIGINT,
	OUT bitmap_pages BIGINT,
	OUT unused_pages BIGINT,
	OUT live_items BIGINT,
	OUT dead_items BIGINT,
	OUT free_percent FLOAT8)
AS '$libdir/pgstattuple', 'pgstathashindex'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TEMP TABLE bug6051 AS
  select i from generate_series(1,3) as t(i)
;
ALTER TABLE pitest1_p2 ALTER COLUMN f3 ADD GENERATED BY DEFAULT AS IDENTITY
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', escape ':')
;
CREATE TABLE lock_tbl1a (a BIGINT)
;
CREATE FUNCTION _intbig_in(cstring)
RETURNS intbig_gkey
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE notnull_tbl1_3 VALIDATE CONSTRAINT nn3
;
COMMENT ON FUNCTION _int_contains(_int4, _int4) IS 'contains'
;
CREATE FUNCTION widget_out(widget)
   RETURNS cstring
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT IMMUTABLE
;
CREATE TEMPORARY SEQUENCE rngfunc_rescan_seq1
;
DROP TABLE pg_temp_3.pg_temp_16652_2, pg_temp_3.pg_temp_16652
;
/*
 * 3. REINDEX
 */
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
CREATE TEMP TABLE pg_temp_9.pg_temp_38052_2 (tid pg_catalog.tid)
;
-- slicing over array of composite types
create or replace function foreach_test(anyarray)
returns void as $$
declare x xy_tuple[];
begin
  foreach x slice 1 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
ANALYZE test_tsvector
;
CREATE TABLESPACE tblspc3 LOCATION '/tmp/tsqNVC_rsv/tempdir/superlongname_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
;
CREATE FUNCTION gtrgm_out(gtrgm)
RETURNS cstring
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
alter table permtest_child attach partition permtest_grandchild for values in ('a')
;
-- 5.43 ROLE_ROUTINE_GRANTS view is based on 5.50 ROUTINE_PRIVILEGES and is defined there instead.


-- 5.44 ROLE_TABLE_GRANTS view is based on 5.63 TABLE_PRIVILEGES and is defined there instead.


/*
 * 5.45
 * ROLE_TABLE_METHOD_GRANTS view
 */

-- feature not supported



-- 5.46 ROLE_USAGE_GRANTS view is based on 5.75 USAGE_PRIVILEGES and is defined there instead.


-- 5.47 ROLE_UDT_GRANTS view is based on 5.74 UDT_PRIVILEGES and is defined there instead.


/*
 * 5.48
 * ROUTINE_COLUMN_USAGE view
 */

CREATE VIEW routine_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class t, pg_namespace nt, pg_attribute a

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = t.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND t.oid = a.attrelid
          AND d.refobjsubid = a.attnum
          AND pg_has_role(t.relowner, 'USAGE')
;
CREATE INDEX on tbl USING rtree(c3) INCLUDE (c1, c4)
;
CREATE MATERIALIZED VIEW tableam_tblmv_heapx USING heap2 AS SELECT * FROM tableam_tbl_heapx
;
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, create_slot = false)
;
create table pparent (pid int, txt text)
;
CREATE TABLE "fkpart5"."fk3" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer
)
;
CREATE TEXT SEARCH DICTIONARY hunspell_long (
                        Template=ispell,
                        DictFile=hunspell_sample_long,
                        AffFile=hunspell_sample_long
)
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=25930 host=/tmp/1gCCONH3rb dbname=postgres' PUBLICATION pub2
;
CREATE VIEW test_view_exists AS select * from test_exists
;
CREATE SUBSCRIPTION regress_test_sub CONNECTION 'port=25714 host=/tmp/mUR9A5zNgJ user=regress_test_user dbname=postgres' PUBLICATION regress_test_pub
;
CREATE VIEW "public"."xmlview1" AS
 SELECT "xmlcomment"('test'::"text") AS "xmlcomment"
;
create table boolrangep_ff1 partition of boolrangep for values from ('false', 'false', 0) to ('false', 'false', 50)
;
TRUNCATE s1.t
;
ALTER TABLE fk_r_1 DROP COLUMN x
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN ('1' collate "POSIX")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regexp_replace( citext, citext, text, text ) returns TEXT AS $$
    SELECT pg_catalog.regexp_replace( $1::pg_catalog.text, $2::pg_catalog.text, $3, CASE WHEN pg_catalog.strpos($4, 'c') = 0 THEN  $4 || 'i' ELSE $4 END);
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_inet_consistent(internal,inet,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_index
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn15b(BASETYPE = anyelement, SFUNC = tfnp,
  STYPE = anyarray, INITCOND = '{}')
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28603_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISSN:
CREATE FUNCTION btissncmp(issn, issn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE found_test_tbl (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_send(hstore)
RETURNS bytea
AS '$libdir/hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."transition_table_level1"
    ADD CONSTRAINT "transition_table_level1_pkey" PRIMARY KEY ("level1_no")
;
----------
[CREATE_TABLE] -- error
create temp table temp_part partition of perm_parted default
;
----------
[DROP_TABLE] DROP TABLE parent_tbl
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test8()
LANGUAGE plpgsql
AS $$
BEGIN
    EXECUTE 'CALL transaction_test1(10, $x$baz$x$)';
END;
$$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30748_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_INDEX] ALTER INDEX testschema.part_a_idx SET TABLESPACE pg_default
;
----------
[CREATE_TYPE] CREATE TYPE test7b AS (a int, b priv_testdomain1)
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS split_obj_priv.cast_mood
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ext_cor_view AS
  SELECT 'ext_cor_view: original'::text AS col
;
----------
[CREATE_PROG_OBJ] /* New stuff in 1.5 begins here */

CREATE OR REPLACE FUNCTION pgstathashindex(IN relname regclass,
	OUT version INTEGER,
	OUT bucket_pages BIGINT,
	OUT overflow_pages BIGINT,
	OUT bitmap_pages BIGINT,
	OUT unused_pages BIGINT,
	OUT live_items BIGINT,
	OUT dead_items BIGINT,
	OUT free_percent FLOAT8)
AS '$libdir/pgstattuple', 'pgstathashindex'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE bug6051 AS
  select i from generate_series(1,3) as t(i)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest1_p2 ALTER COLUMN f3 ADD GENERATED BY DEFAULT AS IDENTITY
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', escape ':')
;
----------
[CREATE_TABLE] CREATE TABLE lock_tbl1a (a BIGINT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _intbig_in(cstring)
RETURNS intbig_gkey
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE notnull_tbl1_3 VALIDATE CONSTRAINT nn3
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION _int_contains(_int4, _int4) IS 'contains'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION widget_out(widget)
   RETURNS cstring
   AS '/src/src/test/regress/regress.so'
   LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_SEQUENCE] CREATE TEMPORARY SEQUENCE rngfunc_rescan_seq1
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_16652_2, pg_temp_3.pg_temp_16652
;
----------
[CREATE_TABLE] /*
 * 3. REINDEX
 */
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_38052_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] -- slicing over array of composite types
create or replace function foreach_test(anyarray)
returns void as $$
declare x xy_tuple[];
begin
  foreach x slice 1 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE test_tsvector
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc3 LOCATION '/tmp/tsqNVC_rsv/tempdir/superlongname_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_out(gtrgm)
RETURNS cstring
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table permtest_child attach partition permtest_grandchild for values in ('a')
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- 5.43 ROLE_ROUTINE_GRANTS view is based on 5.50 ROUTINE_PRIVILEGES and is defined there instead.


-- 5.44 ROLE_TABLE_GRANTS view is based on 5.63 TABLE_PRIVILEGES and is defined there instead.


/*
 * 5.45
 * ROLE_TABLE_METHOD_GRANTS view
 */

-- feature not supported



-- 5.46 ROLE_USAGE_GRANTS view is based on 5.75 USAGE_PRIVILEGES and is defined there instead.


-- 5.47 ROLE_UDT_GRANTS view is based on 5.74 UDT_PRIVILEGES and is defined there instead.


/*
 * 5.48
 * ROUTINE_COLUMN_USAGE view
 */

CREATE VIEW routine_column_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class t, pg_namespace nt, pg_attribute a

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = t.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND t.oid = a.attrelid
          AND d.refobjsubid = a.attnum
          AND pg_has_role(t.relowner, 'USAGE')
;
----------
[ADD_INDEX] CREATE INDEX on tbl USING rtree(c3) INCLUDE (c1, c4)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW tableam_tblmv_heapx USING heap2 AS SELECT * FROM tableam_tbl_heapx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, create_slot = false)
;
----------
[CREATE_TABLE] create table pparent (pid int, txt text)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."fk3" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer
)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hunspell_long (
                        Template=ispell,
                        DictFile=hunspell_sample_long,
                        AffFile=hunspell_sample_long
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=25930 host=/tmp/1gCCONH3rb dbname=postgres' PUBLICATION pub2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW test_view_exists AS select * from test_exists
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_test_sub CONNECTION 'port=25714 host=/tmp/mUR9A5zNgJ user=regress_test_user dbname=postgres' PUBLICATION regress_test_pub
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."xmlview1" AS
 SELECT "xmlcomment"('test'::"text") AS "xmlcomment"
;
----------
[CREATE_TABLE] create table boolrangep_ff1 partition of boolrangep for values from ('false', 'false', 0) to ('false', 'false', 50)
;
----------
[TRUNCATE_TABLE] TRUNCATE s1.t
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk_r_1 DROP COLUMN x
;
