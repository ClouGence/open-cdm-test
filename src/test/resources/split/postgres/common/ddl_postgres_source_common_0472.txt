CREATE POLICY comment_1 ON comment USING (blog_id < 4)
;
ALTER TABLE regular_table DETACH PARTITION any_name
;
ALTER INDEX pg_toast.pg_toast_31036_index RENAME TO reind_con_toast_idx
;
/* Has ee entry */
CREATE TABLE inhf (LIKE inhx, LIKE inhx)
;
ALTER TABLE temporal_unique REPLICA IDENTITY FULL
;
alter table p1 drop column f1
;
CREATE TEMP TABLE FLOAT8_TBL(f1 float8)
;
ALTER TABLE pg_toast.pg_toast_30758 RENAME TO reind_con_toast
;
-- text check

CREATE TABLE texttmp (a text)
;
--
-- WINDOW FUNCTIONS
--

CREATE TEMPORARY TABLE empsalary (
    depname varchar,
    empno bigint,
    salary int,
    enroll_date date
)
;
DROP FUNCTION trig_report()
;
CREATE VIEW v10_temp AS SELECT * FROM v7_temp
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE VIEW pg_statio_all_sequences AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_blocks_fetched(C.oid) -
                    pg_stat_get_blocks_hit(C.oid) AS blks_read,
            pg_stat_get_blocks_hit(C.oid) AS blks_hit
    FROM pg_class C
            LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind = 'S'
;
--
-- Tests for component access / FieldSelect
--
CREATE TABLE compositetable(a text, b text)
;
CREATE TABLE "public"."phone" (
    "slotname" character(20),
    "comment" "text",
    "slotlink" character(20)
)
;
COMMENT ON AGGREGATE "public"."newcnt"(*) IS 'an agg(*) comment'
;
DROP TABLE test3
;
-- doesn't exist
ALTER RULE NewInsertRule ON rule_v1 RENAME TO "_RETURN"
;
create schema s1
  create table abc (f1 int)
;
drop table agg_group_1
;
CREATE TABLE table_three(a varchar)
;
VACUUM (ANALYZE) pg_catalog.pg_authid
;
CALL ptest1(y)
;
CREATE SUBSCRIPTION regress_sub2_gen_to_nogen CONNECTION 'port=20926 host=/tmp/tC9mgW2RJi dbname=postgres' PUBLICATION regress_pub2_gen_to_nogen WITH (copy_data = true)
;
CREATE SCHEMA test_func_dep1
;
CREATE SCHEMA split_prog
;
create table rangep_0_to_100_1 partition of rangep_0_to_100 for values in(1)
;
-- Overriden mapping change with duplicated tokens.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ALTER MAPPING FOR word, word WITH ispell
;
ANALYZE public.btree_tall_tbl
;
CREATE INDEX typically_sized_index ON parallel_vacuum_table(a)
;
DROP TABLE groupby_collation_t
;
DROP OPERATOR CLASS IF EXISTS test_operator_class USING no_such_am
;
ALTER TABLE ONLY "generated_stored_tests"."gtest22b"
    ADD CONSTRAINT "gtest22b_pkey" PRIMARY KEY ("a", "b")
;
ALTER FUNCTION gbt_int2_distance(internal, smallint, smallint, oid, internal) PARALLEL SAFE
;
create index on gin_incomplete_splits using gin (i) with (fastupdate = off)
;
create view vview as
  select pparent.pid, txt, descrip from
    pparent left join cchild using (pid)
;
DROP TABLE temp_pg_buffercache
;
create table idxpart11 partition of idxpart1 for values from (0) to (500)
;
DROP EXTENSION test_ext5
;
ALTER TYPE test_type2 DROP ATTRIBUTE b CASCADE
;
CREATE INDEX t1_spgist ON s3.t1 USING SPGIST (ir)
;
-- cleanup
drop table fktable
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26616_2 (tid pg_catalog.tid)
;
alter table atref drop constraint atref_b_check
;
CREATE EXTENSION plperl
;
CREATE OR REPLACE FUNCTION blurt_and_lock(text) RETURNS text IMMUTABLE LANGUAGE plpgsql AS $$
     BEGIN
        RAISE NOTICE 'called for %', $1;

	-- depending on lock state, wait for lock 2 or 3
        IF pg_try_advisory_xact_lock(current_setting('spec.session')::int, 1) THEN
            RAISE NOTICE 'blocking 2';
            PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 2);
        ELSE
            RAISE NOTICE 'blocking 3';
            PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 3);
        END IF;
    RETURN $1;
    END;$$
;
CREATE INDEX i2 ON newly_indexed (c)
;
CREATE TABLE evttrig.part_10_20 PARTITION OF evttrig.parted (id)
  FOR VALUES FROM (10) TO (20) PARTITION BY RANGE (id)
;
DROP TABLE rf_tbl_abcd_nopk
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] CREATE POLICY comment_1 ON comment USING (blog_id < 4)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE regular_table DETACH PARTITION any_name
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_31036_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] /* Has ee entry */
CREATE TABLE inhf (LIKE inhx, LIKE inhx)
;
----------
[ALTER_TABLE] ALTER TABLE temporal_unique REPLICA IDENTITY FULL
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table p1 drop column f1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE FLOAT8_TBL(f1 float8)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30758 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- text check

CREATE TABLE texttmp (a text)
;
----------
[CREATE_TABLE] --
-- WINDOW FUNCTIONS
--

CREATE TEMPORARY TABLE empsalary (
    depname varchar,
    empno bigint,
    salary int,
    enroll_date date
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION trig_report()
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v10_temp AS SELECT * FROM v7_temp
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_statio_all_sequences AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_blocks_fetched(C.oid) -
                    pg_stat_get_blocks_hit(C.oid) AS blks_read,
            pg_stat_get_blocks_hit(C.oid) AS blks_hit
    FROM pg_class C
            LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind = 'S'
;
----------
[CREATE_TABLE] --
-- Tests for component access / FieldSelect
--
CREATE TABLE compositetable(a text, b text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."phone" (
    "slotname" character(20),
    "comment" "text",
    "slotlink" character(20)
)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE "public"."newcnt"(*) IS 'an agg(*) comment'
;
----------
[DROP_TABLE] DROP TABLE test3
;
----------
[ALTER_POLICY] -- doesn't exist
ALTER RULE NewInsertRule ON rule_v1 RENAME TO "_RETURN"
;
----------
[CREATE_SCHEMA] create schema s1
  create table abc (f1 int)
;
----------
[DROP_TABLE] drop table agg_group_1
;
----------
[CREATE_TABLE] CREATE TABLE table_three(a varchar)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_authid
;
----------
[CALL_PROG_OBJ] CALL ptest1(y)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub2_gen_to_nogen CONNECTION 'port=20926 host=/tmp/tC9mgW2RJi dbname=postgres' PUBLICATION regress_pub2_gen_to_nogen WITH (copy_data = true)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_func_dep1
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_prog
;
----------
[CREATE_TABLE] create table rangep_0_to_100_1 partition of rangep_0_to_100 for values in(1)
;
----------
[ALTER_POLICY] -- Overriden mapping change with duplicated tokens.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ALTER MAPPING FOR word, word WITH ispell
;
----------
[ADMIN_TABLE] ANALYZE public.btree_tall_tbl
;
----------
[ADD_INDEX] CREATE INDEX typically_sized_index ON parallel_vacuum_table(a)
;
----------
[DROP_TABLE] DROP TABLE groupby_collation_t
;
----------
[DROP_PROG_OBJ] DROP OPERATOR CLASS IF EXISTS test_operator_class USING no_such_am
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest22b"
    ADD CONSTRAINT "gtest22b_pkey" PRIMARY KEY ("a", "b")
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int2_distance(internal, smallint, smallint, oid, internal) PARALLEL SAFE
;
----------
[ADD_INDEX] create index on gin_incomplete_splits using gin (i) with (fastupdate = off)
;
----------
[CREATE_VIEW(SELECT)] create view vview as
  select pparent.pid, txt, descrip from
    pparent left join cchild using (pid)
;
----------
[DROP_TABLE] DROP TABLE temp_pg_buffercache
;
----------
[CREATE_TABLE] create table idxpart11 partition of idxpart1 for values from (0) to (500)
;
----------
[DROP_LIBRARY] DROP EXTENSION test_ext5
;
----------
[ALTER_TYPE] ALTER TYPE test_type2 DROP ATTRIBUTE b CASCADE
;
----------
[ADD_INDEX] CREATE INDEX t1_spgist ON s3.t1 USING SPGIST (ir)
;
----------
[DROP_TABLE] -- cleanup
drop table fktable
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26616_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table atref drop constraint atref_b_check
;
----------
[CREATE_LIBRARY] CREATE EXTENSION plperl
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION blurt_and_lock(text) RETURNS text IMMUTABLE LANGUAGE plpgsql AS $$
     BEGIN
        RAISE NOTICE 'called for %', $1;

	-- depending on lock state, wait for lock 2 or 3
        IF pg_try_advisory_xact_lock(current_setting('spec.session')::int, 1) THEN
            RAISE NOTICE 'blocking 2';
            PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 2);
        ELSE
            RAISE NOTICE 'blocking 3';
            PERFORM pg_advisory_xact_lock(current_setting('spec.session')::int, 3);
        END IF;
    RETURN $1;
    END;$$
;
----------
[ADD_INDEX] CREATE INDEX i2 ON newly_indexed (c)
;
----------
[CREATE_TABLE] CREATE TABLE evttrig.part_10_20 PARTITION OF evttrig.parted (id)
  FOR VALUES FROM (10) TO (20) PARTITION BY RANGE (id)
;
----------
[DROP_TABLE] DROP TABLE rf_tbl_abcd_nopk
;
