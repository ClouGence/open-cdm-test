CREATE FUNCTION isnle(isbn13, isbn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP SUBSCRIPTION test_tab2_sub
;
ALTER TABLE pagg_tab_ml_p3 ATTACH PARTITION pagg_tab_ml_p3_s1 FOR VALUES FROM (0) TO (7)
;
CREATE OR REPLACE FUNCTION pg_temp.long() RETURNS text LANGUAGE sql AS $body$ SELECT '1:0.2790636105885118|2:0.3093931668756629|3:0.7915710037681036|4:0.43433091804526924|5:0.6407299379243261|6:0.5650812624776844|7:0.6163660903173565|8:0.32435202182820677|9:0.9813197449402398|10:0.45365617419857585|11:0.6190963057354235|12:0.0280168247615864|13:0.9161938537829071|14:0.28790040925001104|15:0.3227131993346297|16:0.9021698257957476|17:0.9239474779858714|18:0.9741456142160039|19:0.4691335059616515|20:0.39299711007153726|21:0.9738649031337037|22:0.9027248369688572|23:0.7196301065071389|24:0.016355608469655225|25:0.3319171137832766|26:0.5052295850651443|27:0.29965967653154246|28:0.6338688895522977|29:0.3624830618675001|30:0.7706283604527269|31:0.43526109294355875|32:0.7770868193505507|33:0.25703024674037356|34:0.6247642608820876|35:0.398109458035232|36:0.8465785044223846|37:0.6264236584486629|38:0.22354097547096252|39:0.8104763181055046|40:0.3557912832074457|41:0.8439050828554546|42:0.8462133507145444|43:0.25095343050645424|44:0.8559083737616435|45:0.6083075198740213|46:0.30924933141779287|47:0.31470573821638625|48:0.07595432525420853|49:0.8417732967615312|50:0.5979657438176069|51:0.28366635971670173|52:0.23849029680824896|53:0.7396068231383255|54:0.039344278433930846|55:0.1061192710144161|56:0.7549555060290924|57:0.7648267438746785|58:0.835351356515933|59:0.8870824944179987|60:0.15071062666386004|61:0.20049692591259438|62:0.8323498124510629|63:0.9544794137793666|64:0.7197452370894688|65:0.8229974425139239|66:0.5816161025848126|67:0.25023632015771846|68:0.2080398222586517|69:0.06564168121303648|70:0.8843636458878572|71:0.37816633102650976|72:0.767611810238428|73:0.12552902841289515|74:0.30649971932990816|75:0.5321558520858152|76:0.42967979466631423|77:0.05931727536961007|78:0.12786158921231783|79:0.43840054988300037|80:0.8821098663336351|81:0.8932969425009079|82:0.19772549244687565|83:0.0042558331938087335|84:0.3469111220945851|85:0.667417022562127|86:0.2915902329956126|87:0.9381999302095505|88:0.8233726995342334|89:0.7324882754083726|90:0.9418910195595822|91:0.1257976249711894|92:0.498384286080777|93:0.8584391585769815|94:0.8651879353514371|95:0.5386935209476817|96:0.8406757779635448|97:0.3673847325709585|98:0.5319740465979987|99:0.6590760815341543|100:0.9778200400206765'; $body$
;
VACUUM (ANALYZE) pg_catalog.pg_foreign_data_wrapper
;
create function wslot_slotlink_view(bpchar)
returns text as '
declare
    rec		record;
    sltype	char(2);
    retval	text;
begin
    select into rec * from WSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if rec.slotlink = '''' then
        return ''-'';
    end if;
    sltype := substr(rec.slotlink, 1, 2);
    if sltype = ''PH'' then
        select into rec * from PHone where slotname = rec.slotlink;
	retval := ''Phone '' || trim(rec.slotname);
	if rec.comment != '''' then
	    retval := retval || '' ('';
	    retval := retval || rec.comment;
	    retval := retval || '')'';
	end if;
	return retval;
    end if;
    if sltype = ''IF'' then
	declare
	    syrow	System%RowType;
	    ifrow	IFace%ROWTYPE;
        begin
	    select into ifrow * from IFace where slotname = rec.slotlink;
	    select into syrow * from System where name = ifrow.sysname;
	    retval := syrow.name || '' IF '';
	    retval := retval || ifrow.ifname;
	    if syrow.comment != '''' then
	        retval := retval || '' ('';
		retval := retval || syrow.comment;
		retval := retval || '')'';
	    end if;
	    return retval;
	end;
    end if;
    return rec.slotlink;
end;
' language plpgsql
;
ANALYZE pg_temp_13.pg_temp_26366
;
alter table defpart_attach_test attach partition defpart_attach_test_d default
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_parameter_acl
;
create procedure p1()
language plpgsql
as $$
begin
  create table event_trigger_fire7 (a int);
end $$
;
CREATE CAST (citext AS varchar) WITHOUT FUNCTION AS IMPLICIT
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_sequence
;
-- test restriction on non-system view expansion.
create table tt27v_tbl (a int)
;
CREATE FUNCTION isneq(upc, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
/*
 * 6.15
 * CHECK_CONSTRAINTS view
 */

CREATE VIEW check_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(substring(pg_get_constraintdef(con.oid) from 7) AS character_data)
             AS check_clause
    FROM pg_constraint con
           LEFT OUTER JOIN pg_namespace rs ON (rs.oid = con.connamespace)
           LEFT OUTER JOIN pg_class c ON (c.oid = con.conrelid)
           LEFT OUTER JOIN pg_type t ON (t.oid = con.contypid)
    WHERE pg_has_role(coalesce(c.relowner, t.typowner), 'USAGE')
      AND con.contype = 'c'

    UNION
    -- not-null constraints

    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(n.nspname AS sql_identifier) AS constraint_schema,
           CAST(CAST(n.oid AS text) || '_' || CAST(r.oid AS text) || '_' || CAST(a.attnum AS text) || '_not_null' AS sql_identifier) AS constraint_name, -- XXX
           CAST(a.attname || ' IS NOT NULL' AS character_data)
             AS check_clause
    FROM pg_namespace n, pg_class r, pg_attribute a
    WHERE n.oid = r.relnamespace
      AND r.oid = a.attrelid
      AND a.attnum > 0
      AND NOT a.attisdropped
      AND a.attnotnull
      AND r.relkind IN ('r', 'p')
      AND pg_has_role(r.relowner, 'USAGE')
;
CREATE TABLE test_replica_identity4_1(id integer NOT NULL)
;
create or replace function dump_update_old() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, old table = %', TG_NAME,
                 (select string_agg(old_table::text, ', ' order by a) from old_table);
    return null;
  end;
$$
;
CREATE FUNCTION "public"."binary_coercible"("oid", "oid") RETURNS boolean
    LANGUAGE "c" STABLE STRICT PARALLEL SAFE
    AS '/src/src/test/regress/regress.so', 'binary_coercible'
;
CREATE FUNCTION _int_same(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create unique index op_index_key on insertconflicttest(key, fruit text_pattern_ops)
;
drop table at_partitioned
;
-- clean up after output format tests

drop table psql_serial_tab
;
/* src/test/modules/dummy_seclabel/dummy_seclabel--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION dummy_seclabel_dummy()
   RETURNS pg_catalog.void
       AS '$libdir/dummy_seclabel' LANGUAGE C
;
CREATE TABLE "public"."fast_emp4000" (
    "home_base" "box"
)
;
CREATE FUNCTION "public"."refcursor_test1"("refcursor") RETURNS "refcursor"
    LANGUAGE "plpgsql"
    AS $_$
begin
    perform return_refcursor($1);
    return $1;
end
$_$
;
-- ************************************************************
-- * BEFORE INSERT or UPDATE on PSlot
-- *	- Ensure that our patchfield does exist
-- ************************************************************
create function tg_pslot_biu() returns trigger as $proc$
declare
    pfrec	record;
    ps          alias for new;
begin
    select into pfrec * from PField where name = ps.pfname;
    if not found then
        raise exception $$Patchfield "%" does not exist$$, ps.pfname;
    end if;
    return ps;
end;
$proc$ language plpgsql
;
CREATE TABLE "public"."prt1_e_p3" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
ANALYZE pubme.t1
;
CALL ptest1('xy' || 'zzy')
;
--
-- bt_metap()
--
DROP FUNCTION bt_metap(text)
;
ALTER TABLE pg_temp_13.pg_temp_27032_2 ADD COLUMN newdata pg_temp_13.pg_temp_27032
;
-- trying to create range partition with empty range
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (1) TO (0)
;
CREATE FUNCTION func_d_1_15() RETURNS trigger AS $$
BEGIN
   NEW.c = NEW.c + 1; -- Make even numbers odd, or vice versa
   return NEW;
END $$ LANGUAGE plpgsql
;
CREATE TABLE fail_part (like list_parted, c int)
;
ALTER TABLE parent ADD CONSTRAINT a_pos CHECK (a > 0)
;
CREATE TYPE pp_colors AS
							ENUM ('green', 'blue', 'black')
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_parser
;
CREATE UNIQUE INDEX "sl_a_b_idx" ON "public"."sl" USING "btree" ("a", "b")
;
CREATE FUNCTION outer_f (f1 int) RETURNS void
AS $$
BEGIN
  RAISE NOTICE 'outer_f(%)', f1;
  CALL inner_p(f(f1));
END
$$ LANGUAGE plpgsql
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=19283 host=/tmp/IkN8UQ8huW dbname=postgres' PUBLICATION mypub
;
CREATE VIEW lock_view2(a,b) AS SELECT * FROM lock_tbl1, lock_tbl1a
;
CREATE TABLE tbl (c1 int, c2 int)
;
CREATE TABLE "public"."shiftjis2004_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
DROP TABLE parttab
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_type
;
-- this transform would be quite unsafe to leave lying around,
-- except that the SQL language pays no attention to transforms:
CREATE TRANSFORM FOR int LANGUAGE SQL (
    FROM SQL WITH FUNCTION prsd_lextype(internal),
    TO SQL WITH FUNCTION int4recv(internal))
;
CREATE TABLE collate_test_fail (
    a int,
    b text COLLATE "ja_JP.eucjp"
)
;
CREATE FUNCTION handle_get()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
DROP TABLE vaccluster
;
CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=13917 host=/tmp/H6QOc033E6 dbname=postgres application_name=sub_dropped_cols' PUBLICATION pub_dropped_cols
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(isbn13, isbn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION test_tab2_sub
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pagg_tab_ml_p3 ATTACH PARTITION pagg_tab_ml_p3_s1 FOR VALUES FROM (0) TO (7)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_temp.long() RETURNS text LANGUAGE sql AS $body$ SELECT '1:0.2790636105885118|2:0.3093931668756629|3:0.7915710037681036|4:0.43433091804526924|5:0.6407299379243261|6:0.5650812624776844|7:0.6163660903173565|8:0.32435202182820677|9:0.9813197449402398|10:0.45365617419857585|11:0.6190963057354235|12:0.0280168247615864|13:0.9161938537829071|14:0.28790040925001104|15:0.3227131993346297|16:0.9021698257957476|17:0.9239474779858714|18:0.9741456142160039|19:0.4691335059616515|20:0.39299711007153726|21:0.9738649031337037|22:0.9027248369688572|23:0.7196301065071389|24:0.016355608469655225|25:0.3319171137832766|26:0.5052295850651443|27:0.29965967653154246|28:0.6338688895522977|29:0.3624830618675001|30:0.7706283604527269|31:0.43526109294355875|32:0.7770868193505507|33:0.25703024674037356|34:0.6247642608820876|35:0.398109458035232|36:0.8465785044223846|37:0.6264236584486629|38:0.22354097547096252|39:0.8104763181055046|40:0.3557912832074457|41:0.8439050828554546|42:0.8462133507145444|43:0.25095343050645424|44:0.8559083737616435|45:0.6083075198740213|46:0.30924933141779287|47:0.31470573821638625|48:0.07595432525420853|49:0.8417732967615312|50:0.5979657438176069|51:0.28366635971670173|52:0.23849029680824896|53:0.7396068231383255|54:0.039344278433930846|55:0.1061192710144161|56:0.7549555060290924|57:0.7648267438746785|58:0.835351356515933|59:0.8870824944179987|60:0.15071062666386004|61:0.20049692591259438|62:0.8323498124510629|63:0.9544794137793666|64:0.7197452370894688|65:0.8229974425139239|66:0.5816161025848126|67:0.25023632015771846|68:0.2080398222586517|69:0.06564168121303648|70:0.8843636458878572|71:0.37816633102650976|72:0.767611810238428|73:0.12552902841289515|74:0.30649971932990816|75:0.5321558520858152|76:0.42967979466631423|77:0.05931727536961007|78:0.12786158921231783|79:0.43840054988300037|80:0.8821098663336351|81:0.8932969425009079|82:0.19772549244687565|83:0.0042558331938087335|84:0.3469111220945851|85:0.667417022562127|86:0.2915902329956126|87:0.9381999302095505|88:0.8233726995342334|89:0.7324882754083726|90:0.9418910195595822|91:0.1257976249711894|92:0.498384286080777|93:0.8584391585769815|94:0.8651879353514371|95:0.5386935209476817|96:0.8406757779635448|97:0.3673847325709585|98:0.5319740465979987|99:0.6590760815341543|100:0.9778200400206765'; $body$
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_PROG_OBJ] create function wslot_slotlink_view(bpchar)
returns text as '
declare
    rec		record;
    sltype	char(2);
    retval	text;
begin
    select into rec * from WSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if rec.slotlink = '''' then
        return ''-'';
    end if;
    sltype := substr(rec.slotlink, 1, 2);
    if sltype = ''PH'' then
        select into rec * from PHone where slotname = rec.slotlink;
	retval := ''Phone '' || trim(rec.slotname);
	if rec.comment != '''' then
	    retval := retval || '' ('';
	    retval := retval || rec.comment;
	    retval := retval || '')'';
	end if;
	return retval;
    end if;
    if sltype = ''IF'' then
	declare
	    syrow	System%RowType;
	    ifrow	IFace%ROWTYPE;
        begin
	    select into ifrow * from IFace where slotname = rec.slotlink;
	    select into syrow * from System where name = ifrow.sysname;
	    retval := syrow.name || '' IF '';
	    retval := retval || ifrow.ifname;
	    if syrow.comment != '''' then
	        retval := retval || '' ('';
		retval := retval || syrow.comment;
		retval := retval || '')'';
	    end if;
	    return retval;
	end;
    end if;
    return rec.slotlink;
end;
' language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26366
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table defpart_attach_test attach partition defpart_attach_test_d default
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_parameter_acl
;
----------
[CREATE_PROG_OBJ] create procedure p1()
language plpgsql
as $$
begin
  create table event_trigger_fire7 (a int);
end $$
;
----------
[CREATE_PROG_OBJ] CREATE CAST (citext AS varchar) WITHOUT FUNCTION AS IMPLICIT
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_sequence
;
----------
[CREATE_TABLE] -- test restriction on non-system view expansion.
create table tt27v_tbl (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(upc, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.15
 * CHECK_CONSTRAINTS view
 */

CREATE VIEW check_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(substring(pg_get_constraintdef(con.oid) from 7) AS character_data)
             AS check_clause
    FROM pg_constraint con
           LEFT OUTER JOIN pg_namespace rs ON (rs.oid = con.connamespace)
           LEFT OUTER JOIN pg_class c ON (c.oid = con.conrelid)
           LEFT OUTER JOIN pg_type t ON (t.oid = con.contypid)
    WHERE pg_has_role(coalesce(c.relowner, t.typowner), 'USAGE')
      AND con.contype = 'c'

    UNION
    -- not-null constraints

    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(n.nspname AS sql_identifier) AS constraint_schema,
           CAST(CAST(n.oid AS text) || '_' || CAST(r.oid AS text) || '_' || CAST(a.attnum AS text) || '_not_null' AS sql_identifier) AS constraint_name, -- XXX
           CAST(a.attname || ' IS NOT NULL' AS character_data)
             AS check_clause
    FROM pg_namespace n, pg_class r, pg_attribute a
    WHERE n.oid = r.relnamespace
      AND r.oid = a.attrelid
      AND a.attnum > 0
      AND NOT a.attisdropped
      AND a.attnotnull
      AND r.relkind IN ('r', 'p')
      AND pg_has_role(r.relowner, 'USAGE')
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_identity4_1(id integer NOT NULL)
;
----------
[CREATE_PROG_OBJ] create or replace function dump_update_old() returns trigger language plpgsql as
$$
  begin
    raise notice 'trigger = %, old table = %', TG_NAME,
                 (select string_agg(old_table::text, ', ' order by a) from old_table);
    return null;
  end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."binary_coercible"("oid", "oid") RETURNS boolean
    LANGUAGE "c" STABLE STRICT PARALLEL SAFE
    AS '/src/src/test/regress/regress.so', 'binary_coercible'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_same(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] create unique index op_index_key on insertconflicttest(key, fruit text_pattern_ops)
;
----------
[DROP_TABLE] drop table at_partitioned
;
----------
[DROP_TABLE] -- clean up after output format tests

drop table psql_serial_tab
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/dummy_seclabel/dummy_seclabel--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION dummy_seclabel_dummy()
   RETURNS pg_catalog.void
       AS '$libdir/dummy_seclabel' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fast_emp4000" (
    "home_base" "box"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."refcursor_test1"("refcursor") RETURNS "refcursor"
    LANGUAGE "plpgsql"
    AS $_$
begin
    perform return_refcursor($1);
    return $1;
end
$_$
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * BEFORE INSERT or UPDATE on PSlot
-- *	- Ensure that our patchfield does exist
-- ************************************************************
create function tg_pslot_biu() returns trigger as $proc$
declare
    pfrec	record;
    ps          alias for new;
begin
    select into pfrec * from PField where name = ps.pfname;
    if not found then
        raise exception $$Patchfield "%" does not exist$$, ps.pfname;
    end if;
    return ps;
end;
$proc$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_e_p3" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[ADMIN_TABLE] ANALYZE pubme.t1
;
----------
[CALL_PROG_OBJ] CALL ptest1('xy' || 'zzy')
;
----------
[DROP_PROG_OBJ] --
-- bt_metap()
--
DROP FUNCTION bt_metap(text)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_27032_2 ADD COLUMN newdata pg_temp_13.pg_temp_27032
;
----------
[CREATE_TABLE] -- trying to create range partition with empty range
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (1) TO (0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_d_1_15() RETURNS trigger AS $$
BEGIN
   NEW.c = NEW.c + 1; -- Make even numbers odd, or vice versa
   return NEW;
END $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE fail_part (like list_parted, c int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE parent ADD CONSTRAINT a_pos CHECK (a > 0)
;
----------
[CREATE_TYPE] CREATE TYPE pp_colors AS
							ENUM ('green', 'blue', 'black')
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_parser
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "sl_a_b_idx" ON "public"."sl" USING "btree" ("a", "b")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION outer_f (f1 int) RETURNS void
AS $$
BEGIN
  RAISE NOTICE 'outer_f(%)', f1;
  CALL inner_p(f(f1));
END
$$ LANGUAGE plpgsql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=19283 host=/tmp/IkN8UQ8huW dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW lock_view2(a,b) AS SELECT * FROM lock_tbl1, lock_tbl1a
;
----------
[CREATE_TABLE] CREATE TABLE tbl (c1 int, c2 int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."shiftjis2004_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
----------
[DROP_TABLE] DROP TABLE parttab
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_type
;
----------
[UNKNOWN] -- this transform would be quite unsafe to leave lying around,
-- except that the SQL language pays no attention to transforms:
CREATE TRANSFORM FOR int LANGUAGE SQL (
    FROM SQL WITH FUNCTION prsd_lextype(internal),
    TO SQL WITH FUNCTION int4recv(internal))
;
----------
[CREATE_TABLE] CREATE TABLE collate_test_fail (
    a int,
    b text COLLATE "ja_JP.eucjp"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_get()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[DROP_TABLE] DROP TABLE vaccluster
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=13917 host=/tmp/H6QOc033E6 dbname=postgres application_name=sub_dropped_cols' PUBLICATION pub_dropped_cols
;
