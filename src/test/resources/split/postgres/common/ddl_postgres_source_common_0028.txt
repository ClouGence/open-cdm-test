CREATE TABLE textrange_test (tr textrange)
;
-- stable because depends on pg_trgm.word_similarity_threshold

CREATE FUNCTION strict_word_similarity_commutator_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
create trigger tg_hslot_biu before insert or update
    on HSlot for each row execute procedure tg_hslot_biu()
;
ANALYZE pg_temp_10.pg_temp_26213_2
;
ALTER INDEX pg_toast.pg_toast_30511_index RENAME TO reind_con_toast_idx
;
create table inserttestb (f3 insert_test_domain, f4 insert_test_domain[])
;
CREATE OPERATOR CLASS timestamp_ops
DEFAULT FOR TYPE timestamp USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       timestamp_cmp(timestamp,timestamp),
    FUNCTION        2       gin_extract_value_timestamp(timestamp, internal),
    FUNCTION        3       gin_extract_query_timestamp(timestamp, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_timestamp(timestamp,timestamp,int2, internal),
STORAGE         timestamp
;
CREATE SUBSCRIPTION testsub1 CONNECTION 'port=25800 host=/tmp/REnOPq3M6y dbname=postgres' PUBLICATION testpub1
;
CALL sum_one(3)
;
create index concurrently on idxpart1 ((a/b))
;
ALTER TABLE unparted ATTACH PARTITION fail_part FOR VALUES IN ('a')
;
/*
 * text search configuration for hindi language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hindi and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
-- fails

ALTER TYPE test_type DROP ATTRIBUTE IF EXISTS c
;
drop sequence ttdummy_seq
;
-- Also test old-style where the STORAGE clause is disallowed
CREATE OPERATOR CLASS name_ops_old FOR TYPE name
USING spgist AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	spgist_name_config(internal, internal),
	FUNCTION	2	spgist_name_choose(internal, internal),
	FUNCTION	3	spg_text_picksplit(internal, internal),
	FUNCTION	4	spgist_name_inner_consistent(internal, internal),
	FUNCTION	5	spgist_name_leaf_consistent(internal, internal),
	FUNCTION	6	spgist_name_compress(name)
;
DROP TRIGGER parent_delete_trig ON range_parted
;
analyze foo
;
CREATE TABLE reind_fdw_parent (c1 int) PARTITION BY RANGE (c1)
;
CREATE MATERIALIZED VIEW mv_test3 AS SELECT * FROM mv_test2 WHERE moo = 12345
;
ANALYZE pg_temp_24.pg_temp_26481
;
create table attbl(a int)
;
DROP TABLE pk_noparted, fk_parted_pk, fk_noparted, fk_noparted_sn
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
CREATE TABLE test20_1 PARTITION OF test20 FOR VALUES IN ('abc')
;
ANALYZE pg_temp_9.pg_temp_26570
;
ALTER TABLE unlogged1 SET LOGGED
;
CREATE database regex_testdb
;
create domain d_fail as int4 not null default 3 default 3
;
CREATE TABLE ctoast (key int primary key, val text)
;
CREATE TABLE "public"."emp" (
    "name" "text",
    "age" integer,
    "location" "point",
    "salary" integer,
    "manager" "name"
)
;
DROP TABLE prevstats
;
CREATE RULE base_tbl_del_rule AS ON DELETE TO base_tbl
  DO INSTEAD
    UPDATE base_tbl SET deleted = true WHERE id = old.id
;
CREATE SCHEMA split_pub18_a
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_amproc
;
ALTER INDEX tbl_idx ALTER COLUMN 1 SET STATISTICS 1000
;
CREATE SEQUENCE "public"."clstr_tst_a_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
ALTER FUNCTION gbt_macad8_penalty(internal, internal, internal) PARALLEL SAFE
;
CREATE TABLE corrupt2 AS SELECT a FROM generate_series(1,10000) AS a
;
DROP CAST IF EXISTS (split_obj_priv.cast_mood AS text)
;
CREATE TABLE copy_default (
		id integer PRIMARY KEY,
		text_value text NOT NULL DEFAULT 'test',
		ts_value timestamp without time zone NOT NULL DEFAULT '2022-07-05'
	)
;
CREATE POLICY genpol ON addr_nsp.gentable
;
CREATE EXTENSION cube
;
CREATE DOMAIN "public"."str_domain2" AS "text" DEFAULT 'foo'::"text"
	CONSTRAINT "str_domain2_check" CHECK ((VALUE <> 'foo'::"text"))
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=21597 host=/tmp/a8jVVp7J9n dbname=postgres' PUBLICATION pub1
;
DROP TABLE ATACC1, ATACC2
;
DROP TABLE a_temp_tbl
;
alter table mlparted4 drop a
;
CREATE TABLE regress_test_table (t text)
;
CREATE TABLESPACE space_test LOCATION ''
;
-- ISSN vs other types:
CREATE FUNCTION btissncmp(issn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE textrange_test (tr textrange)
;
----------
[CREATE_PROG_OBJ] -- stable because depends on pg_trgm.word_similarity_threshold

CREATE FUNCTION strict_word_similarity_commutator_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_hslot_biu before insert or update
    on HSlot for each row execute procedure tg_hslot_biu()
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26213_2
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30511_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table inserttestb (f3 insert_test_domain, f4 insert_test_domain[])
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS timestamp_ops
DEFAULT FOR TYPE timestamp USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       timestamp_cmp(timestamp,timestamp),
    FUNCTION        2       gin_extract_value_timestamp(timestamp, internal),
    FUNCTION        3       gin_extract_query_timestamp(timestamp, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_timestamp(timestamp,timestamp,int2, internal),
STORAGE         timestamp
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub1 CONNECTION 'port=25800 host=/tmp/REnOPq3M6y dbname=postgres' PUBLICATION testpub1
;
----------
[CALL_PROG_OBJ] CALL sum_one(3)
;
----------
[ADD_INDEX] create index concurrently on idxpart1 ((a/b))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE unparted ATTACH PARTITION fail_part FOR VALUES IN ('a')
;
----------
[CREATE_POLICY] /*
 * text search configuration for hindi language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hindi and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
----------
[ALTER_TYPE] -- fails

ALTER TYPE test_type DROP ATTRIBUTE IF EXISTS c
;
----------
[DROP_SEQUENCE] drop sequence ttdummy_seq
;
----------
[CREATE_PROG_OBJ] -- Also test old-style where the STORAGE clause is disallowed
CREATE OPERATOR CLASS name_ops_old FOR TYPE name
USING spgist AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	spgist_name_config(internal, internal),
	FUNCTION	2	spgist_name_choose(internal, internal),
	FUNCTION	3	spg_text_picksplit(internal, internal),
	FUNCTION	4	spgist_name_inner_consistent(internal, internal),
	FUNCTION	5	spgist_name_leaf_consistent(internal, internal),
	FUNCTION	6	spgist_name_compress(name)
;
----------
[DROP_TRIGGER] DROP TRIGGER parent_delete_trig ON range_parted
;
----------
[ADMIN_TABLE] analyze foo
;
----------
[CREATE_TABLE] CREATE TABLE reind_fdw_parent (c1 int) PARTITION BY RANGE (c1)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mv_test3 AS SELECT * FROM mv_test2 WHERE moo = 12345
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_26481
;
----------
[CREATE_TABLE] create table attbl(a int)
;
----------
[DROP_TABLE] DROP TABLE pk_noparted, fk_parted_pk, fk_noparted, fk_noparted_sn
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
----------
[CREATE_TABLE] CREATE TABLE test20_1 PARTITION OF test20 FOR VALUES IN ('abc')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26570
;
----------
[ALTER_TABLE] ALTER TABLE unlogged1 SET LOGGED
;
----------
[CREATE_CATALOG] CREATE database regex_testdb
;
----------
[CREATE_TYPE] create domain d_fail as int4 not null default 3 default 3
;
----------
[CREATE_TABLE] CREATE TABLE ctoast (key int primary key, val text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."emp" (
    "name" "text",
    "age" integer,
    "location" "point",
    "salary" integer,
    "manager" "name"
)
;
----------
[DROP_TABLE] DROP TABLE prevstats
;
----------
[CREATE_POLICY] CREATE RULE base_tbl_del_rule AS ON DELETE TO base_tbl
  DO INSTEAD
    UPDATE base_tbl SET deleted = true WHERE id = old.id
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_pub18_a
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_amproc
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX tbl_idx ALTER COLUMN 1 SET STATISTICS 1000
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."clstr_tst_a_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad8_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE corrupt2 AS SELECT a FROM generate_series(1,10000) AS a
;
----------
[DROP_PROG_OBJ] DROP CAST IF EXISTS (split_obj_priv.cast_mood AS text)
;
----------
[CREATE_TABLE] CREATE TABLE copy_default (
		id integer PRIMARY KEY,
		text_value text NOT NULL DEFAULT 'test',
		ts_value timestamp without time zone NOT NULL DEFAULT '2022-07-05'
	)
;
----------
[CREATE_POLICY] CREATE POLICY genpol ON addr_nsp.gentable
;
----------
[CREATE_LIBRARY] CREATE EXTENSION cube
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."str_domain2" AS "text" DEFAULT 'foo'::"text"
	CONSTRAINT "str_domain2_check" CHECK ((VALUE <> 'foo'::"text"))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=21597 host=/tmp/a8jVVp7J9n dbname=postgres' PUBLICATION pub1
;
----------
[DROP_TABLE] DROP TABLE ATACC1, ATACC2
;
----------
[DROP_TABLE] DROP TABLE a_temp_tbl
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table mlparted4 drop a
;
----------
[CREATE_TABLE] CREATE TABLE regress_test_table (t text)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE space_test LOCATION ''
;
----------
[CREATE_PROG_OBJ] -- ISSN vs other types:
CREATE FUNCTION btissncmp(issn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
