VACUUM ANALYZE sto1
;
drop table tab_progress_reporting
;
create temp table arr_pk_tbl (pk int4 primary key, f1 int[])
;
drop schema abc cascade
;
CREATE FUNCTION set_pgservicefile(text) RETURNS void LANGUAGE SQL
    AS $$SELECT setenv('PGSERVICEFILE', '/src/contrib/dblink/' || $1)$$
;
CREATE TABLE "public"."ruletest_tbl" (
    "a" integer,
    "b" integer
)
;
--
-- BTREE
--
CREATE INDEX onek_unique1 ON onek USING btree(unique1 int4_ops)
;
CREATE PUBLICATION pub_mix_8 FOR TABLE test_mix_4
;
-- sequence

DROP SEQUENCE test_sequence_exists
;
CREATE INDEX idx_macaddr8 ON test_macaddr8 USING gin (i)
;
CREATE TEMP TABLE temp_table_commit_drop (
    id INT PRIMARY KEY
)
ON COMMIT DROP
;
CREATE TABLE ctv_data (v, h, c, i, d) AS
VALUES
   ('v1','h2','foo', 3, '2015-04-01'::date),
   ('v2','h1','bar', 3, '2015-01-02'),
   ('v1','h0','baz', NULL, '2015-07-12'),
   ('v0','h4','qux', 4, '2015-07-15'),
   ('v0','h4','dbl', -3, '2014-12-15'),
   ('v0',NULL,'qux', 5, '2014-07-15'),
   ('v1','h2','quux',7, '2015-04-04')
;
/*
 * PostgreSQL System Views
 *
 * Copyright (c) 1996-2019, PostgreSQL Global Development Group
 *
 * src/backend/catalog/system_views.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
ALTER TABLE ONLY "fkpart5"."pk31"
    ADD CONSTRAINT "pk31_pkey" PRIMARY KEY ("a")
;
CREATE VIEW view_heap_psql AS SELECT f1 from tbl_heap_psql
;
--
-- self-referential FKs are even more fun
--

create table self_ref (a int primary key,
                       b int references self_ref(a) on delete cascade)
;
CREATE TRIGGER merge_brd BEFORE DELETE ON target FOR EACH ROW EXECUTE PROCEDURE merge_trigfunc ()
;
-- tablespace should not change even if there is an index rewrite
ALTER TABLE testschema.test_default_tab_p ALTER val TYPE bigint
;
DROP SCHEMA split_pub18_a CASCADE
;
CREATE SCHEMA "schema2" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
ALTER FOREIGN TABLE ft1 ADD COLUMN c8 integer DEFAULT 0
;
CREATE FUNCTION gbt_numeric_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE VIEW rw_view2 AS SELECT b AS bb, c AS cc, a AS aa FROM base_tbl
;
CREATE TEMP TABLE ucchild () inherits (uctest)
;
-- Again, so should this...
CREATE TEMP TABLE FKTABLE (ftest1 cidr, ftest2 timestamp)
;
CREATE TABLE inmaster_tab (a int)
;
DROP TABLE IF EXISTS source
;
--    N    N        N    P
-- should CREATE
CREATE AGGREGATE myaggn06a(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
DROP FOREIGN TABLE remt2
;
-- Check similarity threshold (bug #14202)

CREATE TEMP TABLE restaurants (city text)
;
ANALYZE regress_indexing.pk51
;
CREATE FUNCTION "public"."least_accum"("anyelement", VARIADIC "anyarray") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$select least($1, min($2[i])) from generate_subscripts($2,1) g(i)$_$
;
-- Should work. Textbook case of ALTER OPERATOR FAMILY ... ADD OPERATOR with FOR ORDER BY
CREATE OPERATOR FAMILY alt_opf9 USING gist
;
-- this should complain "does not exist"
DROP VIEW atestv4
;
CREATE OPERATOR ^@ (
        LEFTARG = _ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = _ltxtq_exec,
        COMMUTATOR = '^@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ALTER EXTENSION hstore UPDATE TO '1.8'
;
ALTER SUBSCRIPTION regress_testsub SET (password_required = true)
;
create temp table parent1(f1 int, f2 int)
;
CREATE MATERIALIZED VIEW d AS SELECT * FROM a
;
ALTER TABLE pg_publication_rel ADD PRIMARY KEY USING INDEX pg_publication_rel_oid_index
;
CREATE TABLE CASE_TBL (
  i integer,
  f double precision
)
;
CREATE TABLE stxdinh2() INHERITS(stxdinh)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
-- error: not a procedure
DROP PROCEDURE nonexistent()
;
CREATE FUNCTION gbt_ts_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create function test_event_trigger_arg(name text)
returns event_trigger as $$ BEGIN RETURN 1; END $$ language plpgsql
;
create table at_tab2 (x int, y text, check((x,y)::at_tab1 = (1,'42')::at_tab1))
;
ALTER TABLE pg_toast.pg_toast_28435 RENAME TO reind_con_toast
;
ALTER TABLE attmp ADD COLUMN q point
;
do $$ declare x notice%rowtype; begin end $$
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM ANALYZE sto1
;
----------
[DROP_TABLE] drop table tab_progress_reporting
;
----------
[CREATE_TABLE] create temp table arr_pk_tbl (pk int4 primary key, f1 int[])
;
----------
[DROP_SCHEMA] drop schema abc cascade
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION set_pgservicefile(text) RETURNS void LANGUAGE SQL
    AS $$SELECT setenv('PGSERVICEFILE', '/src/contrib/dblink/' || $1)$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ruletest_tbl" (
    "a" integer,
    "b" integer
)
;
----------
[ADD_INDEX] --
-- BTREE
--
CREATE INDEX onek_unique1 ON onek USING btree(unique1 int4_ops)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_mix_8 FOR TABLE test_mix_4
;
----------
[DROP_SEQUENCE] -- sequence

DROP SEQUENCE test_sequence_exists
;
----------
[ADD_INDEX] CREATE INDEX idx_macaddr8 ON test_macaddr8 USING gin (i)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temp_table_commit_drop (
    id INT PRIMARY KEY
)
ON COMMIT DROP
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctv_data (v, h, c, i, d) AS
VALUES
   ('v1','h2','foo', 3, '2015-04-01'::date),
   ('v2','h1','bar', 3, '2015-01-02'),
   ('v1','h0','baz', NULL, '2015-07-12'),
   ('v0','h4','qux', 4, '2015-07-15'),
   ('v0','h4','dbl', -3, '2014-12-15'),
   ('v0',NULL,'qux', 5, '2014-07-15'),
   ('v1','h2','quux',7, '2015-04-04')
;
----------
[CREATE_VIEW(SELECT)] /*
 * PostgreSQL System Views
 *
 * Copyright (c) 1996-2019, PostgreSQL Global Development Group
 *
 * src/backend/catalog/system_views.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart5"."pk31"
    ADD CONSTRAINT "pk31_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW view_heap_psql AS SELECT f1 from tbl_heap_psql
;
----------
[CREATE_TABLE] --
-- self-referential FKs are even more fun
--

create table self_ref (a int primary key,
                       b int references self_ref(a) on delete cascade)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER merge_brd BEFORE DELETE ON target FOR EACH ROW EXECUTE PROCEDURE merge_trigfunc ()
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- tablespace should not change even if there is an index rewrite
ALTER TABLE testschema.test_default_tab_p ALTER val TYPE bigint
;
----------
[DROP_SCHEMA] DROP SCHEMA split_pub18_a CASCADE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "schema2" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD COLUMN c8 integer DEFAULT 0
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_numeric_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS SELECT b AS bb, c AS cc, a AS aa FROM base_tbl
;
----------
[CREATE_TABLE] CREATE TEMP TABLE ucchild () inherits (uctest)
;
----------
[CREATE_TABLE] -- Again, so should this...
CREATE TEMP TABLE FKTABLE (ftest1 cidr, ftest2 timestamp)
;
----------
[CREATE_TABLE] CREATE TABLE inmaster_tab (a int)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS source
;
----------
[CREATE_PROG_OBJ] --    N    N        N    P
-- should CREATE
CREATE AGGREGATE myaggn06a(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[DROP_TABLE] DROP FOREIGN TABLE remt2
;
----------
[CREATE_TABLE] -- Check similarity threshold (bug #14202)

CREATE TEMP TABLE restaurants (city text)
;
----------
[ADMIN_TABLE] ANALYZE regress_indexing.pk51
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."least_accum"("anyelement", VARIADIC "anyarray") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$select least($1, min($2[i])) from generate_subscripts($2,1) g(i)$_$
;
----------
[CREATE_PROG_OBJ] -- Should work. Textbook case of ALTER OPERATOR FAMILY ... ADD OPERATOR with FOR ORDER BY
CREATE OPERATOR FAMILY alt_opf9 USING gist
;
----------
[DROP_VIEW] -- this should complain "does not exist"
DROP VIEW atestv4
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^@ (
        LEFTARG = _ltree,
	RIGHTARG = ltxtquery,
	PROCEDURE = _ltxtq_exec,
        COMMUTATOR = '^@',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION hstore UPDATE TO '1.8'
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (password_required = true)
;
----------
[CREATE_TABLE] create temp table parent1(f1 int, f2 int)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW d AS SELECT * FROM a
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_publication_rel ADD PRIMARY KEY USING INDEX pg_publication_rel_oid_index
;
----------
[CREATE_TABLE] CREATE TABLE CASE_TBL (
  i integer,
  f double precision
)
;
----------
[CREATE_TABLE] CREATE TABLE stxdinh2() INHERITS(stxdinh)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
----------
[DROP_PROG_OBJ] -- error: not a procedure
DROP PROCEDURE nonexistent()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_ts_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] create function test_event_trigger_arg(name text)
returns event_trigger as $$ BEGIN RETURN 1; END $$ language plpgsql
;
----------
[CREATE_TABLE] create table at_tab2 (x int, y text, check((x,y)::at_tab1 = (1,'42')::at_tab1))
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28435 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN q point
;
----------
[BLOCK] do $$ declare x notice%rowtype; begin end $$
;
