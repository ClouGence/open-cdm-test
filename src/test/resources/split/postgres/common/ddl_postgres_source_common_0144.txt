ALTER RULE InsertRule ON rule_v1 RENAME to NewInsertRule
;
-- failed (not aggregate)
ALTER AGGREGATE alt_func1(int) SET SCHEMA alt_nsp2
;
CALL test_proc4(66)
;
create table ddtest2(f1 rposint)
;
-- Table publication that does not include the parent table
CREATE PUBLICATION pub FOR TABLE sch2.tbl1_part1 WITH (PUBLISH_VIA_PARTITION_ROOT=1)
;
CREATE TYPE citext (
    INPUT          = citextin,
    OUTPUT         = citextout,
    RECEIVE        = citextrecv,
    SEND           = citextsend,
    INTERNALLENGTH = VARIABLE,
    STORAGE        = extended,
    -- make it a non-preferred member of string type category
    CATEGORY       = 'S',
    PREFERRED      = false,
    COLLATABLE     = true
)
;
create table gen_part (
  key1 integer not null,
  key2 integer not null
) partition by list (key1)
;
CREATE AGGREGATE myaggp17a(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
CREATE TEMP TABLE tab_search_diff_1 (a int, b int, c int)
;
CREATE FUNCTION test1list(val hstore) RETURNS int
LANGUAGE plperlu
TRANSFORM FOR TYPE hstore
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_[0]));
return scalar(keys %{$_[0]});
$$
;
-- Parallel VACUUM will never be used unless there are at least two indexes
-- that exceed min_parallel_index_scan_size.  Create two such indexes, and
-- a third index that is smaller than min_parallel_index_scan_size.
CREATE INDEX regular_sized_index ON parallel_vacuum_table(a)
;
CREATE VIEW pg_group AS
    SELECT
        rolname AS groname,
        oid AS grosysid,
        ARRAY(SELECT member FROM pg_auth_members WHERE roleid = oid) AS grolist
    FROM pg_authid
    WHERE NOT rolcanlogin
;
--
-- Test STRICT limiter in both planned and EXECUTE invocations.
-- Note that a data-modifying query is quasi strict (disallow multi rows)
-- by default in the planned case, but not in EXECUTE.
--

create temp table foo (f1 int, f2 int)
;
-- check validation when attaching hash partitions

-- check that violating rows are correctly reported
CREATE TABLE hpart_2 (LIKE hash_parted)
;
-- Test that child does not override inheritable constraints of the parent
create table c2 (constraint p2chk check (ff1 > 10) no inherit) inherits (p1)
;
CREATE TABLE "public"."cminh" (
    "f1" "text"
)
;
ALTER TABLE pk ATTACH PARTITION pk5 FOR VALUES FROM (4000) TO (5000)
;
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
;
-- dual non-polymorphic transfn
CREATE FUNCTION tfnp(int[],int) RETURNS int[] AS
'select $1 || $2' LANGUAGE SQL
;
CREATE UNIQUE INDEX onek_nulltest ON onek_with_null (unique2)
;
create table p1p2_c1 (f1 int) inherits (p1, p2)
;
CREATE FUNCTION functest_IS_1(a int, b int default 1, c text default 'foo')
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT $1 + $2'
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
DO $d$
        BEGIN
            EXECUTE $$CREATE SERVER loopback FOREIGN DATA WRAPPER postgres_fdw
                OPTIONS (dbname '$$||current_database()||$$',
                         port '$$||current_setting('port')||$$',
                         use_remote_estimate 'true'
                )$$;
        END;
    $d$
;
create trigger trig_upd_after_3 after update on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
ALTER TABLE ONLY fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk
;
CREATE OPERATOR = (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_eq,
       COMMUTATOR = =,
       NEGATOR = <>,
       RESTRICT = eqsel,
       JOIN = eqjoinsel,
       MERGES,
       HASHES
)
;
create table loct1 (a int, b text)
;
ALTER PUBLICATION pub3 ADD TABLE dump_test.test_table
;
VACUUM ANALYZE stxdinh, stxdinh1, stxdinh2
;
DROP TABLE pvactst
;
create table mc3p7 partition of mc3p for values from (20, 20, 20) to (maxvalue, maxvalue, maxvalue)
;
create function set_role_and_error(int) returns int as
  $$ select 1 / $1 $$ language sql parallel safe
  set role = regress_parallel_worker
;
CREATE INDEX "btree_idx_err" ON "public"."btree_test" USING "btree" ("a") WITH ("vacuum_cleanup_index_scale_factor"='100.0')
;
DROP EXTENSION IF EXISTS hstore RESTRICT
;
DROP FUNCTION tftest
;
COMMENT ON TEXT SEARCH DICTIONARY norwegian_stem IS 'snowball stemmer for norwegian language'
;
create table list_part3 partition of list_part for values in (3)
;
CREATE TABLE "generated_stored_tests"."gtest_parent" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
PARTITION BY RANGE ("f1")
;
CREATE FUNCTION rw_view1_trig_fn()
RETURNS trigger AS
$$
BEGIN
  IF TG_OP = 'INSERT' THEN
    UPDATE base_tbl SET b=NEW.b WHERE a=1;
    RETURN NULL;
  END IF;
  RETURN NULL;
END;
$$
LANGUAGE plpgsql
;
CREATE TABLE lock_tbl3 () INHERITS (lock_tbl2)
;
create table rules_foo (f1 int)
;
CREATE TABLE "public"."notnull_tbl1_upg" (
    "a" integer,
    "b" integer
)
;
DROP VIEW ptif_test_view
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=26320 host=/tmp/b6KHE4BaiC dbname=postgres' PUBLICATION mypub
;
ALTER TABLE ONLY "public"."dropcolumnanother" INHERIT "public"."dropcolumnchild"
;
ANALYZE public.student
;
-- Test case of an event trigger run in an extension upgrade script.
-- See: https://postgr.es/m/20200902193715.6e0269d4@firost
CREATE EXTENSION test_ext_evttrig
;
ALTER TABLE fd_pt1 RENAME COLUMN c2 TO f2
;
DROP TABLE trunc_f CASCADE
;
------------------------------------------------------------------------------------------
[ALTER_POLICY] ALTER RULE InsertRule ON rule_v1 RENAME to NewInsertRule
;
----------
[ALTER_PROG_OBJ] -- failed (not aggregate)
ALTER AGGREGATE alt_func1(int) SET SCHEMA alt_nsp2
;
----------
[CALL_PROG_OBJ] CALL test_proc4(66)
;
----------
[CREATE_TABLE] create table ddtest2(f1 rposint)
;
----------
[CREATE_PUB_SUB] -- Table publication that does not include the parent table
CREATE PUBLICATION pub FOR TABLE sch2.tbl1_part1 WITH (PUBLISH_VIA_PARTITION_ROOT=1)
;
----------
[CREATE_TYPE] CREATE TYPE citext (
    INPUT          = citextin,
    OUTPUT         = citextout,
    RECEIVE        = citextrecv,
    SEND           = citextsend,
    INTERNALLENGTH = VARIABLE,
    STORAGE        = extended,
    -- make it a non-preferred member of string type category
    CATEGORY       = 'S',
    PREFERRED      = false,
    COLLATABLE     = true
)
;
----------
[CREATE_TABLE] create table gen_part (
  key1 integer not null,
  key2 integer not null
) partition by list (key1)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp17a(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tab_search_diff_1 (a int, b int, c int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1list(val hstore) RETURNS int
LANGUAGE plperlu
TRANSFORM FOR TYPE hstore
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_[0]));
return scalar(keys %{$_[0]});
$$
;
----------
[ADD_INDEX] -- Parallel VACUUM will never be used unless there are at least two indexes
-- that exceed min_parallel_index_scan_size.  Create two such indexes, and
-- a third index that is smaller than min_parallel_index_scan_size.
CREATE INDEX regular_sized_index ON parallel_vacuum_table(a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_group AS
    SELECT
        rolname AS groname,
        oid AS grosysid,
        ARRAY(SELECT member FROM pg_auth_members WHERE roleid = oid) AS grolist
    FROM pg_authid
    WHERE NOT rolcanlogin
;
----------
[CREATE_TABLE] --
-- Test STRICT limiter in both planned and EXECUTE invocations.
-- Note that a data-modifying query is quasi strict (disallow multi rows)
-- by default in the planned case, but not in EXECUTE.
--

create temp table foo (f1 int, f2 int)
;
----------
[CREATE_TABLE] -- check validation when attaching hash partitions

-- check that violating rows are correctly reported
CREATE TABLE hpart_2 (LIKE hash_parted)
;
----------
[CREATE_TABLE] -- Test that child does not override inheritable constraints of the parent
create table c2 (constraint p2chk check (ff1 > 10) no inherit) inherits (p1)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."cminh" (
    "f1" "text"
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pk ATTACH PARTITION pk5 FOR VALUES FROM (4000) TO (5000)
;
----------
[CREATE_TABLE] CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
;
----------
[CREATE_PROG_OBJ] -- dual non-polymorphic transfn
CREATE FUNCTION tfnp(int[],int) RETURNS int[] AS
'select $1 || $2' LANGUAGE SQL
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX onek_nulltest ON onek_with_null (unique2)
;
----------
[CREATE_TABLE] create table p1p2_c1 (f1 int) inherits (p1, p2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_IS_1(a int, b int default 1, c text default 'foo')
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT $1 + $2'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[BLOCK] DO $d$
        BEGIN
            EXECUTE $$CREATE SERVER loopback FOREIGN DATA WRAPPER postgres_fdw
                OPTIONS (dbname '$$||current_database()||$$',
                         port '$$||current_setting('port')||$$',
                         use_remote_estimate 'true'
                )$$;
        END;
    $d$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_upd_after_3 after update on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_eq,
       COMMUTATOR = =,
       NEGATOR = <>,
       RESTRICT = eqsel,
       JOIN = eqjoinsel,
       MERGES,
       HASHES
)
;
----------
[CREATE_TABLE] create table loct1 (a int, b text)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub3 ADD TABLE dump_test.test_table
;
----------
[ADMIN_TABLE] VACUUM ANALYZE stxdinh, stxdinh1, stxdinh2
;
----------
[DROP_TABLE] DROP TABLE pvactst
;
----------
[CREATE_TABLE] create table mc3p7 partition of mc3p for values from (20, 20, 20) to (maxvalue, maxvalue, maxvalue)
;
----------
[CREATE_PROG_OBJ] create function set_role_and_error(int) returns int as
  $$ select 1 / $1 $$ language sql parallel safe
  set role = regress_parallel_worker
;
----------
[ADD_INDEX] CREATE INDEX "btree_idx_err" ON "public"."btree_test" USING "btree" ("a") WITH ("vacuum_cleanup_index_scale_factor"='100.0')
;
----------
[DROP_LIBRARY] DROP EXTENSION IF EXISTS hstore RESTRICT
;
----------
[DROP_PROG_OBJ] DROP FUNCTION tftest
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY norwegian_stem IS 'snowball stemmer for norwegian language'
;
----------
[CREATE_TABLE] create table list_part3 partition of list_part for values in (3)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest_parent" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
PARTITION BY RANGE ("f1")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rw_view1_trig_fn()
RETURNS trigger AS
$$
BEGIN
  IF TG_OP = 'INSERT' THEN
    UPDATE base_tbl SET b=NEW.b WHERE a=1;
    RETURN NULL;
  END IF;
  RETURN NULL;
END;
$$
LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE lock_tbl3 () INHERITS (lock_tbl2)
;
----------
[CREATE_TABLE] create table rules_foo (f1 int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl1_upg" (
    "a" integer,
    "b" integer
)
;
----------
[DROP_VIEW] DROP VIEW ptif_test_view
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=26320 host=/tmp/b6KHE4BaiC dbname=postgres' PUBLICATION mypub
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."dropcolumnanother" INHERIT "public"."dropcolumnchild"
;
----------
[ADMIN_TABLE] ANALYZE public.student
;
----------
[CREATE_LIBRARY] -- Test case of an event trigger run in an extension upgrade script.
-- See: https://postgr.es/m/20200902193715.6e0269d4@firost
CREATE EXTENSION test_ext_evttrig
;
----------
[RENAME_COLUMN] ALTER TABLE fd_pt1 RENAME COLUMN c2 TO f2
;
----------
[DROP_TABLE] DROP TABLE trunc_f CASCADE
;
