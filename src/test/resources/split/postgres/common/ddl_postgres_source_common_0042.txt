ALTER FOREIGN TABLE ft2 ADD CONSTRAINT fd_pt1chk2 CHECK (c2 <> '')
;
ANALYZE pg_temp_10.pg_temp_26679
;
DROP TABLE indtoasttest
;
ALTER TABLE "public"."itest10" ALTER COLUMN "a" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."itest10_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
CREATE FUNCTION test_enc_conversion(bytea, name, name, bool, validlen OUT int, result OUT bytea)
    AS '/src/src/test/regress/regress.so', 'test_enc_conversion'
    LANGUAGE C STRICT
;
alter table atacc1 add constraint atacc1_constr_a_valid
	 check(test_a is not null)
;
-- For partitioned tables
CREATE TABLE regtest_ptable_4 (x int, y int, z int) PARTITION BY RANGE (x)
;
DO $$
DECLARE
  num_rows integer;
  waiters integer;
BEGIN
  -- Check if we are the first client
  SELECT COUNT(*) FROM first_client_table INTO STRICT num_rows;
  IF num_rows = 1 THEN
	-- This code should not be used in a new transaction after an error
	INSERT INTO first_client_table VALUES (2);

	-- Let the second client begin
	PERFORM pg_advisory_unlock(0);
	PERFORM pg_advisory_unlock(1);

	-- Make sure the second client is ready for deadlock
	LOOP
	  SELECT COUNT(*) INTO STRICT waiters FROM pg_locks WHERE
	  locktype = 'advisory' AND
	  objsubid = 1 AND
	  ((classid::bigint << 32) | objid::bigint = 2::bigint) AND
	  NOT granted;

	  IF waiters = 1 THEN
	    -- Exit loop
		EXIT;
	  END IF;
	END LOOP;

	PERFORM pg_advisory_lock(0);
    -- And the second client took care by itself of the lock 1
  END IF;
END$$
;
CREATE TABLE "public"."prt2_l_p2_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
DROP SCHEMA testviewschm2 CASCADE
;
ALTER TABLE collate_test22 ADD FOREIGN KEY (f2) REFERENCES collate_test20
;
-- GROUP BY not supported
CREATE VIEW ro_view3 AS SELECT 1 FROM base_tbl HAVING max(a) > 0
;
ALTER TABLE testpub_tbl5 REPLICA IDENTITY USING INDEX testpub_tbl5_b_key
;
do $$
  declare
    x text;
  begin
    select test1.b into x from test1;
    delete from test1;
    commit;
    perform pg_advisory_lock(1);
    raise notice 'length(x) = %', length(x);
  end;
$$
;
-- recreate the temp table (this demonstrates that the raw plan is
-- purely textual and doesn't depend on OIDs, for instance)
CREATE TEMP TABLE pcachetest AS SELECT * FROM int8_tbl ORDER BY 2
;
CREATE VIEW "public"."mvtest_tv" AS
 SELECT "type",
    "sum"("amt") AS "totamt"
   FROM "public"."mvtest_t"
  GROUP BY "type"
;
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=true, MATCHORIG=false, KEEPSYNONYMS=false, MATCHSYNONYMS=true)
;
CREATE TABLE "public"."prt1_l_p3_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
create table coll_pruning_multi (a text) partition by range (substr(a, 1) collate "POSIX", substr(a, 1) collate "C")
;
/*
 * 5.17
 * COLUMN_COLUMN_USAGE view
 */

CREATE VIEW column_column_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(n.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(ac.attname AS sql_identifier) AS column_name,
           CAST(ad.attname AS sql_identifier) AS dependent_column

    FROM pg_namespace n, pg_class c, pg_depend d,
         pg_attribute ac, pg_attribute ad

    WHERE n.oid = c.relnamespace
          AND c.oid = ac.attrelid
          AND c.oid = ad.attrelid
          AND d.classid = 'pg_catalog.pg_class'::regclass
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND d.objid = d.refobjid
          AND c.oid = d.objid
          AND d.objsubid = ad.attnum
          AND d.refobjsubid = ac.attnum
          AND ad.attgenerated <> ''
          AND pg_has_role(c.relowner, 'USAGE')
;
CREATE TABLE seclabel_tbl2 (x int, y text)
;
alter table trigpart attach partition trigpart3 for values from (2000) to (3000)
;
CREATE COLLATION user_collation FROM "C"
;
ALTER TABLE test9a ALTER COLUMN b TYPE priv_testdomain1
;
CREATE FUNCTION dblink_get_result(text, bool)
RETURNS SETOF record
AS '$libdir/dblink', 'dblink_get_result'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TYPE ltree_gist (
	internallength = -1,
	input = ltree_gist_in,
	output = ltree_gist_out,
	storage = plain
)
;
CREATE OPERATOR CLASS ean13_ops DEFAULT
	FOR TYPE ean13 USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btean13cmp(ean13, ean13)
;
CREATE TABLE "public"."rtest_emplog" (
    "ename" character(20),
    "who" "name",
    "action" character(10),
    "newsal" "money",
    "oldsal" "money"
)
;
ALTER TABLE fk_partitioned_fk_5 ATTACH PARTITION fk_partitioned_fk_5_1 FOR VALUES FROM (0) TO (10)
;
CREATE TRIGGER trig_row_before BEFORE INSERT ON ftable
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
ALTER OPERATOR CLASS split_operator_probe.int_hash_ops_renamed USING hash SET SCHEMA split_operator_probe_next
;
DROP PUBLICATION regress_pub1_gen_to_nogen
;
create table parted_trig_1 partition of parted_trig
       for values from (0) to (1000) partition by range (a)
;
CREATE FUNCTION isnlt(issn13, issn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP TABLE vistest
;
CREATE TABLE sto1 (c int NOT NULL)
;
CREATE PUBLICATION testpub_fortable FOR TABLE "CURRENT_SCHEMA"."CURRENT_SCHEMA"
;
CREATE TEXT SEARCH CONFIGURATION alt_ts_conf2 (copy=english)
;
create function missing_return_expr() returns int as $$
begin
    return ;
end;$$ language plpgsql
;
CREATE FUNCTION my_int_eq(int, int) RETURNS bool
  LANGUAGE internal STRICT IMMUTABLE PARALLEL SAFE
  AS $$int4eq$$
;
CREATE INDEX t1_brin ON s1.t1 USING BRIN (i)
;
ALTER OPERATOR FAMILY alt_opf18 USING btree DROP FUNCTION 2 (int4, int4)
;
create or replace function unnest2(anyarray)
returns setof anyelement as $$
select $1[s1][s2] from generate_subscripts($1,1) g1(s1),
                   generate_subscripts($1,2) g2(s2);
$$ language sql immutable
;
alter index idxpart_a_idx attach partition idxpart2_a_idx
;
alter table user_table add column address varchar(255)
;
create function tg_chkslotlink() returns trigger as '
begin
    if new.slotlink isnull then
        new.slotlink := '''';
    end if;
    return new;
end;
' language plpgsql
;
ALTER CONVERSION alt_conv1 RENAME TO alt_conv2
;
ALTER INDEX pg_toast.pg_toast_30332_index RENAME TO reind_con_toast_idx
;
VACUUM (ANALYZE) pg_catalog.pg_statistic
;
ALTER SUBSCRIPTION regress_testsub4 SET (origin = any)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER FOREIGN TABLE ft2 ADD CONSTRAINT fd_pt1chk2 CHECK (c2 <> '')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26679
;
----------
[DROP_TABLE] DROP TABLE indtoasttest
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest10" ALTER COLUMN "a" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."itest10_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_enc_conversion(bytea, name, name, bool, validlen OUT int, result OUT bytea)
    AS '/src/src/test/regress/regress.so', 'test_enc_conversion'
    LANGUAGE C STRICT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add constraint atacc1_constr_a_valid
	 check(test_a is not null)
;
----------
[CREATE_TABLE] -- For partitioned tables
CREATE TABLE regtest_ptable_4 (x int, y int, z int) PARTITION BY RANGE (x)
;
----------
[BLOCK] DO $$
DECLARE
  num_rows integer;
  waiters integer;
BEGIN
  -- Check if we are the first client
  SELECT COUNT(*) FROM first_client_table INTO STRICT num_rows;
  IF num_rows = 1 THEN
	-- This code should not be used in a new transaction after an error
	INSERT INTO first_client_table VALUES (2);

	-- Let the second client begin
	PERFORM pg_advisory_unlock(0);
	PERFORM pg_advisory_unlock(1);

	-- Make sure the second client is ready for deadlock
	LOOP
	  SELECT COUNT(*) INTO STRICT waiters FROM pg_locks WHERE
	  locktype = 'advisory' AND
	  objsubid = 1 AND
	  ((classid::bigint << 32) | objid::bigint = 2::bigint) AND
	  NOT granted;

	  IF waiters = 1 THEN
	    -- Exit loop
		EXIT;
	  END IF;
	END LOOP;

	PERFORM pg_advisory_lock(0);
    -- And the second client took care by itself of the lock 1
  END IF;
END$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_l_p2_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[DROP_SCHEMA] DROP SCHEMA testviewschm2 CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE collate_test22 ADD FOREIGN KEY (f2) REFERENCES collate_test20
;
----------
[CREATE_VIEW(SELECT)] -- GROUP BY not supported
CREATE VIEW ro_view3 AS SELECT 1 FROM base_tbl HAVING max(a) > 0
;
----------
[ALTER_TABLE] ALTER TABLE testpub_tbl5 REPLICA IDENTITY USING INDEX testpub_tbl5_b_key
;
----------
[BLOCK] do $$
  declare
    x text;
  begin
    select test1.b into x from test1;
    delete from test1;
    commit;
    perform pg_advisory_lock(1);
    raise notice 'length(x) = %', length(x);
  end;
$$
;
----------
[CREATE_TABLE|SELECT] -- recreate the temp table (this demonstrates that the raw plan is
-- purely textual and doesn't depend on OIDs, for instance)
CREATE TEMP TABLE pcachetest AS SELECT * FROM int8_tbl ORDER BY 2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."mvtest_tv" AS
 SELECT "type",
    "sum"("amt") AS "totamt"
   FROM "public"."mvtest_t"
  GROUP BY "type"
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=true, MATCHORIG=false, KEEPSYNONYMS=false, MATCHSYNONYMS=true)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_l_p3_p1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[CREATE_TABLE] create table coll_pruning_multi (a text) partition by range (substr(a, 1) collate "POSIX", substr(a, 1) collate "C")
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.17
 * COLUMN_COLUMN_USAGE view
 */

CREATE VIEW column_column_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(n.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(ac.attname AS sql_identifier) AS column_name,
           CAST(ad.attname AS sql_identifier) AS dependent_column

    FROM pg_namespace n, pg_class c, pg_depend d,
         pg_attribute ac, pg_attribute ad

    WHERE n.oid = c.relnamespace
          AND c.oid = ac.attrelid
          AND c.oid = ad.attrelid
          AND d.classid = 'pg_catalog.pg_class'::regclass
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND d.objid = d.refobjid
          AND c.oid = d.objid
          AND d.objsubid = ad.attnum
          AND d.refobjsubid = ac.attnum
          AND ad.attgenerated <> ''
          AND pg_has_role(c.relowner, 'USAGE')
;
----------
[CREATE_TABLE] CREATE TABLE seclabel_tbl2 (x int, y text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table trigpart attach partition trigpart3 for values from (2000) to (3000)
;
----------
[CREATE_POLICY] CREATE COLLATION user_collation FROM "C"
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test9a ALTER COLUMN b TYPE priv_testdomain1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_get_result(text, bool)
RETURNS SETOF record
AS '$libdir/dblink', 'dblink_get_result'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TYPE] CREATE TYPE ltree_gist (
	internallength = -1,
	input = ltree_gist_in,
	output = ltree_gist_out,
	storage = plain
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS ean13_ops DEFAULT
	FOR TYPE ean13 USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btean13cmp(ean13, ean13)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_emplog" (
    "ename" character(20),
    "who" "name",
    "action" character(10),
    "newsal" "money",
    "oldsal" "money"
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk_5 ATTACH PARTITION fk_partitioned_fk_5_1 FOR VALUES FROM (0) TO (10)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_before BEFORE INSERT ON ftable
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR CLASS split_operator_probe.int_hash_ops_renamed USING hash SET SCHEMA split_operator_probe_next
;
----------
[DROP_PUB_SUB] DROP PUBLICATION regress_pub1_gen_to_nogen
;
----------
[CREATE_TABLE] create table parted_trig_1 partition of parted_trig
       for values from (0) to (1000) partition by range (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(issn13, issn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE vistest
;
----------
[CREATE_TABLE] CREATE TABLE sto1 (c int NOT NULL)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_fortable FOR TABLE "CURRENT_SCHEMA"."CURRENT_SCHEMA"
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION alt_ts_conf2 (copy=english)
;
----------
[CREATE_PROG_OBJ] create function missing_return_expr() returns int as $$
begin
    return ;
end;$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION my_int_eq(int, int) RETURNS bool
  LANGUAGE internal STRICT IMMUTABLE PARALLEL SAFE
  AS $$int4eq$$
;
----------
[ADD_INDEX] CREATE INDEX t1_brin ON s1.t1 USING BRIN (i)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf18 USING btree DROP FUNCTION 2 (int4, int4)
;
----------
[CREATE_PROG_OBJ] create or replace function unnest2(anyarray)
returns setof anyelement as $$
select $1[s1][s2] from generate_subscripts($1,1) g1(s1),
                   generate_subscripts($1,2) g2(s2);
$$ language sql immutable
;
----------
[ALTER_INDEX] alter index idxpart_a_idx attach partition idxpart2_a_idx
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table user_table add column address varchar(255)
;
----------
[CREATE_PROG_OBJ] create function tg_chkslotlink() returns trigger as '
begin
    if new.slotlink isnull then
        new.slotlink := '''';
    end if;
    return new;
end;
' language plpgsql
;
----------
[ALTER_POLICY] ALTER CONVERSION alt_conv1 RENAME TO alt_conv2
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30332_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_statistic
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub4 SET (origin = any)
;
