EXECUTE dumpFunc('16596')
;
-- =============================================================================
-- test case for having multiple cached connections for a foreign server
-- =============================================================================
CREATE ROLE regress_multi_conn_user1 SUPERUSER
;
set parallel_tuple_cost to 0
;
-- Verify that a deferrable, initially deferred foreign key still works
-- as expected after being set to NOT ENFORCED and then re-enabled
BEGIN
;
EXECUTE dumpTableAttach('20720')
;
EXECUTE dumpOpr('509')
;
LOCK TABLE "public"."mlparted1" IN ACCESS SHARE MODE
;
LOCK TABLE public.mlparted_def2 IN ACCESS SHARE MODE
;
SET search_path = s
;
SET LOCAL TimeZone TO 'UTC'
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('16596')
;
----------
[CREATE_ROLE] -- =============================================================================
-- test case for having multiple cached connections for a foreign server
-- =============================================================================
CREATE ROLE regress_multi_conn_user1 SUPERUSER
;
----------
[SESSION_SETTING_WRITE] set parallel_tuple_cost to 0
;
----------
[TRANSACTION] -- Verify that a deferrable, initially deferred foreign key still works
-- as expected after being set to NOT ENFORCED and then re-enabled
BEGIN
;
----------
[UNSAFE] EXECUTE dumpTableAttach('20720')
;
----------
[UNSAFE] EXECUTE dumpOpr('509')
;
----------
[SESSION_LOCK] LOCK TABLE "public"."mlparted1" IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE public.mlparted_def2 IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] SET search_path = s
;
----------
[SESSION_SETTING_WRITE] SET LOCAL TimeZone TO 'UTC'
;
