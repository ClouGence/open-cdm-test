-- As does this...
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest2, ftest1)
     references pktable(ptest1, ptest2)
;
ALTER TABLE fk_notpartitioned_pk ALTER COLUMN a TYPE bigint
;
ANALYZE pg_temp_9.pg_temp_37998_2
;
CREATE TABLE partrem2 PARTITION OF partrem FOR VALUES IN (2)
;
CREATE TABLE testschema.dflt (a int PRIMARY KEY) PARTITION BY LIST (a) TABLESPACE regress_tblspace
;
ALTER FUNCTION gbt_bytea_picksplit(internal, internal) PARALLEL SAFE
;
--
-- Hot Standby tests
--
-- hs_primary_extremes.sql
--

drop table if exists hs_extreme
;
-- check whole-row-Var handling in nested lateral functions (bug #11703)

create function extractq2(t int8_tbl) returns int8 as $$
  select t.q2
$$ language sql immutable
;
ANALYZE public.rtest_nothn2
;
create index brin_packdate_idx on journal using brin (packdate(d))
	   with (autosummarize = on, pages_per_range = 1)
;
ALTER TABLE notnull_tbl1 ALTER a SET NOT NULL
;
ANALYZE public.prt3_n_p1
;
ALTER INDEX concur_reindex_part_index ATTACH PARTITION concur_reindex_part_index_10
;
--
-- bt_page_items(bytea)
--
DROP FUNCTION bt_page_items(bytea)
;
CREATE TABLE TEMP_INT4 (f1 INT4)
;
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box,
				EXCLUDE USING btree (c1 WITH =) INCLUDE(c3,c4))
;
CREATE TABLE tru_rtable_child (id int)
;
CREATE TABLE FLOAT4_TBL (f1  float4)
;
CREATE FUNCTION is_valid(isbn)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER EXTENSION postgres_fdw ADD FUNCTION postgres_fdw_abs(int)
;
drop type xy_tuple
;
COMMENT ON TABLE dump_test.test_table
					   IS 'comment on table'
;
ALTER TABLE hash_parted ATTACH PARTITION fail_part FOR VALUES WITH (MODULUS 8, REMAINDER 4)
;
/*
 * 24.5
 * FOREIGN_DATA_WRAPPERS view
 */
CREATE VIEW foreign_data_wrappers AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           authorization_identifier,
           CAST(NULL AS character_data) AS library_name,
           foreign_data_wrapper_language
    FROM _pg_foreign_data_wrappers w
;
-- As above, but SRF is defined to return a composite type
CREATE TYPE foo_t AS (f1 int, f2 text, f3 int, f4 int8)
;
DROP TABLE temptest
;
COMMENT ON TRIGGER check_fkeys2_pkey_bad ON fkeys2 IS 'wrong'
;
CREATE FUNCTION "public"."tg_phone_bu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotname != old.slotname then
        delete from PHone where slotname = old.slotname;
	insert into PHone (
		    slotname,
		    comment,
		    slotlink
		) values (
		    new.slotname,
		    new.comment,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
$$
;
CREATE FUNCTION "public"."explain_analyze_inc_sort_nodes"("query" "text") RETURNS "jsonb"
    LANGUAGE "plpgsql"
    AS $$
declare
  elements jsonb;
  element jsonb;
  matching_nodes jsonb := '[]'::jsonb;
begin
  execute 'explain (analyze, costs off, summary off, timing off, buffers off, format ''json'') ' || query into strict elements;
  while jsonb_array_length(elements) > 0 loop
    element := elements->0;
    elements := elements - 0;
    case jsonb_typeof(element)
    when 'array' then
      if jsonb_array_length(element) > 0 then
        elements := elements || element;
      end if;
    when 'object' then
      if element ? 'Plan' then
        elements := elements || jsonb_build_array(element->'Plan');
        element := element - 'Plan';
      else
        if element ? 'Plans' then
          elements := elements || jsonb_build_array(element->'Plans');
          element := element - 'Plans';
        end if;
        if (element->>'Node Type')::text = 'Incremental Sort' then
          matching_nodes := matching_nodes || element;
        end if;
      end if;
    end case;
  end loop;
  return matching_nodes;
end;
$$
;
create or replace function foreach_test(anyarray)
returns void as $$
declare x int[];
begin
  foreach x slice 2 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
CREATE TABLE horizons_tst (data int unique) WITH (autovacuum_enabled = off)
;
CREATE TABLE public.ths_p1 (
    mod integer,
    data integer
)
;
CREATE TRIGGER "after_upd_stmt_trig" AFTER UPDATE ON "public"."main_table" FOR EACH STATEMENT EXECUTE FUNCTION "public"."trigger_func"('after_upd_stmt')
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub8
;
CREATE UNIQUE INDEX ON mvtest_mv(a)
;
DROP POLICY policy_range_parted_subplan ON range_parted
;
COMMENT ON TEXT SEARCH DICTIONARY irish_stem IS 'snowball stemmer for irish language'
;
-- Create the operator class
CREATE OPERATOR CLASS gist_interval_ops
DEFAULT FOR TYPE interval USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_intv_consistent (internal, interval, int2, oid, internal),
	FUNCTION	2	gbt_intv_union (internal, internal),
	FUNCTION	3	gbt_intv_compress (internal),
	FUNCTION	4	gbt_intv_decompress (internal),
	FUNCTION	5	gbt_intv_penalty (internal, internal, internal),
	FUNCTION	6	gbt_intv_picksplit (internal, internal),
	FUNCTION	7	gbt_intv_same (gbtreekey32, gbtreekey32, internal),
	STORAGE		gbtreekey32
;
CREATE TABLE part1 PARTITION OF pa_target FOR VALUES IN (1,4)
  WITH (autovacuum_enabled=off)
;
CREATE TABLE temptest(col int)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_attrdef
;
create temp table cc (f1 int references pp on update no action on delete no action)
;
ALTER TABLE pred_parent ALTER a DROP NOT NULL
;
-- also ok if the rules are disabled
ALTER TABLE rule_merge1 DISABLE RULE rule1
;
alter table users drop column todrop
;
CREATE FUNCTION gbt_float4_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE SCHEMA "schema4" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
VACUUM (PARALLEL 2) pg_catalog.pg_constraint
;
CREATE OPERATOR ~>=~ (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = ~<~,
    COMMUTATOR = ~<=~,
    PROCEDURE  = citext_pattern_ge,
    RESTRICT   = scalargtsel,
    JOIN       = scalargtjoinsel
)
;
VACUUM (SKIP_DATABASE_STATS, FULL) information_schema.sql_features
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] -- As does this...
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest2, ftest1)
     references pktable(ptest1, ptest2)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fk_notpartitioned_pk ALTER COLUMN a TYPE bigint
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_37998_2
;
----------
[CREATE_TABLE] CREATE TABLE partrem2 PARTITION OF partrem FOR VALUES IN (2)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.dflt (a int PRIMARY KEY) PARTITION BY LIST (a) TABLESPACE regress_tblspace
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bytea_picksplit(internal, internal) PARALLEL SAFE
;
----------
[DROP_TABLE] --
-- Hot Standby tests
--
-- hs_primary_extremes.sql
--

drop table if exists hs_extreme
;
----------
[CREATE_PROG_OBJ] -- check whole-row-Var handling in nested lateral functions (bug #11703)

create function extractq2(t int8_tbl) returns int8 as $$
  select t.q2
$$ language sql immutable
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_nothn2
;
----------
[ADD_INDEX] create index brin_packdate_idx on journal using brin (packdate(d))
	   with (autosummarize = on, pages_per_range = 1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE notnull_tbl1 ALTER a SET NOT NULL
;
----------
[ADMIN_TABLE] ANALYZE public.prt3_n_p1
;
----------
[ALTER_INDEX] ALTER INDEX concur_reindex_part_index ATTACH PARTITION concur_reindex_part_index_10
;
----------
[DROP_PROG_OBJ] --
-- bt_page_items(bytea)
--
DROP FUNCTION bt_page_items(bytea)
;
----------
[CREATE_TABLE] CREATE TABLE TEMP_INT4 (f1 INT4)
;
----------
[CREATE_TABLE] CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box,
				EXCLUDE USING btree (c1 WITH =) INCLUDE(c3,c4))
;
----------
[CREATE_TABLE] CREATE TABLE tru_rtable_child (id int)
;
----------
[CREATE_TABLE] CREATE TABLE FLOAT4_TBL (f1  float4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION is_valid(isbn)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_LIBRARY] ALTER EXTENSION postgres_fdw ADD FUNCTION postgres_fdw_abs(int)
;
----------
[DROP_TYPE] drop type xy_tuple
;
----------
[COMMENT_TABLE] COMMENT ON TABLE dump_test.test_table
					   IS 'comment on table'
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE hash_parted ATTACH PARTITION fail_part FOR VALUES WITH (MODULUS 8, REMAINDER 4)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.5
 * FOREIGN_DATA_WRAPPERS view
 */
CREATE VIEW foreign_data_wrappers AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           authorization_identifier,
           CAST(NULL AS character_data) AS library_name,
           foreign_data_wrapper_language
    FROM _pg_foreign_data_wrappers w
;
----------
[CREATE_TYPE] -- As above, but SRF is defined to return a composite type
CREATE TYPE foo_t AS (f1 int, f2 text, f3 int, f4 int8)
;
----------
[DROP_TABLE] DROP TABLE temptest
;
----------
[COMMENT_TRIGGER] COMMENT ON TRIGGER check_fkeys2_pkey_bad ON fkeys2 IS 'wrong'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_phone_bu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotname != old.slotname then
        delete from PHone where slotname = old.slotname;
	insert into PHone (
		    slotname,
		    comment,
		    slotlink
		) values (
		    new.slotname,
		    new.comment,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_analyze_inc_sort_nodes"("query" "text") RETURNS "jsonb"
    LANGUAGE "plpgsql"
    AS $$
declare
  elements jsonb;
  element jsonb;
  matching_nodes jsonb := '[]'::jsonb;
begin
  execute 'explain (analyze, costs off, summary off, timing off, buffers off, format ''json'') ' || query into strict elements;
  while jsonb_array_length(elements) > 0 loop
    element := elements->0;
    elements := elements - 0;
    case jsonb_typeof(element)
    when 'array' then
      if jsonb_array_length(element) > 0 then
        elements := elements || element;
      end if;
    when 'object' then
      if element ? 'Plan' then
        elements := elements || jsonb_build_array(element->'Plan');
        element := element - 'Plan';
      else
        if element ? 'Plans' then
          elements := elements || jsonb_build_array(element->'Plans');
          element := element - 'Plans';
        end if;
        if (element->>'Node Type')::text = 'Incremental Sort' then
          matching_nodes := matching_nodes || element;
        end if;
      end if;
    end case;
  end loop;
  return matching_nodes;
end;
$$
;
----------
[CREATE_PROG_OBJ] create or replace function foreach_test(anyarray)
returns void as $$
declare x int[];
begin
  foreach x slice 2 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE horizons_tst (data int unique) WITH (autovacuum_enabled = off)
;
----------
[CREATE_TABLE] CREATE TABLE public.ths_p1 (
    mod integer,
    data integer
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "after_upd_stmt_trig" AFTER UPDATE ON "public"."main_table" FOR EACH STATEMENT EXECUTE FUNCTION "public"."trigger_func"('after_upd_stmt')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub8
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON mvtest_mv(a)
;
----------
[DROP_POLICY] DROP POLICY policy_range_parted_subplan ON range_parted
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY irish_stem IS 'snowball stemmer for irish language'
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_interval_ops
DEFAULT FOR TYPE interval USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_intv_consistent (internal, interval, int2, oid, internal),
	FUNCTION	2	gbt_intv_union (internal, internal),
	FUNCTION	3	gbt_intv_compress (internal),
	FUNCTION	4	gbt_intv_decompress (internal),
	FUNCTION	5	gbt_intv_penalty (internal, internal, internal),
	FUNCTION	6	gbt_intv_picksplit (internal, internal),
	FUNCTION	7	gbt_intv_same (gbtreekey32, gbtreekey32, internal),
	STORAGE		gbtreekey32
;
----------
[CREATE_TABLE] CREATE TABLE part1 PARTITION OF pa_target FOR VALUES IN (1,4)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE] CREATE TABLE temptest(col int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_attrdef
;
----------
[CREATE_TABLE] create temp table cc (f1 int references pp on update no action on delete no action)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pred_parent ALTER a DROP NOT NULL
;
----------
[ALTER_TABLE] -- also ok if the rules are disabled
ALTER TABLE rule_merge1 DISABLE RULE rule1
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table users drop column todrop
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float4_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "schema4" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_constraint
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~>=~ (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = ~<~,
    COMMUTATOR = ~<=~,
    PROCEDURE  = citext_pattern_ge,
    RESTRICT   = scalargtsel,
    JOIN       = scalargtjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) information_schema.sql_features
;
