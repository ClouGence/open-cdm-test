VACUUM (ANALYZE) pg_catalog.pg_subscription
;
CREATE TABLE "public"."temporal_fk2_rng2rng" (
    "id" "int4range" NOT NULL,
    "valid_at" "daterange" NOT NULL,
    "parent_id1" "int4range",
    "parent_id2" "int4range"
)
;
ANALYZE pg_temp_9.pg_temp_26805_2
;
create table parted_index_col_drop1 partition of parted_index_col_drop
  for values in (1) partition by list (a)
;
-- fail here
drop materialized view mvtest_error
;
-- these three should fail
alter table renameColumnChild rename column a to d
;
-- fail, foo.f1 requires it

drop table foo
;
-- Failure for non-BRIN index.
CREATE INDEX test1_a_btree ON test1 (a)
;
alter table pk2 drop column b
;
create function fcompos2(v compos) returns void as $$
select fcompos1(v);
$$ language sql
;
CREATE TABLE part_m01_even PARTITION OF part_m01
	FOR VALUES IN (2,4,6,8) WITH (autovacuum_enabled=off)
;
IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest4
;
CREATE TABLE "public"."constr_child" (
    "a" integer CONSTRAINT "constr_parent_a_not_null" NOT NULL
)
;
alter table idxpart1 drop constraint idxpart1_pkey
;
create function xfloat8in(cstring) returns xfloat8 immutable strict
  language internal as 'int8in'
;
ANALYZE pg_temp_13.pg_temp_25576_2
;
ALTER TABLE pg_temp_13.pg_temp_26471_2 ADD COLUMN newdata pg_temp_13.pg_temp_26471
;
CREATE TABLE reind_con_tab(id serial primary key, data text)
;
CREATE TYPE hstore
;
CREATE TABLE "public"."tt2" (
    "x" integer,
    "y" numeric(9,2)
)
;
vacuum analyze smalltbl
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."depth1" INHERIT "public"."depth0"
;
CREATE OR REPLACE FUNCTION start_command()
RETURNS event_trigger AS $$
BEGIN
RAISE NOTICE '% - ddl_command_start', tg_tag;
END;
$$ LANGUAGE plpgsql
;
CREATE TABLE "public"."atnnpart1" (
    "col1" integer,
    "id" integer CONSTRAINT "another_constr" NOT NULL
)
;
CREATE FOREIGN TABLE agg_text (
	a	int2 CHECK (a >= 0),
	b	float4
) SERVER file_server
OPTIONS (format 'text', filename '/src/contrib/file_fdw/data/agg.data', delimiter '	', null '\N')
;
DROP FOREIGN TABLE ftable
;
CREATE OPERATOR #@%# (
   rightarg = int8,
   procedure = factorial,
   invalid_att = int8
)
;
ALTER INDEX pg_toast.pg_toast_30974_index RENAME TO reind_con_toast_idx
;
create index idxpart_c on only idxpart (c)
;
CREATE TABLE table_a (id integer, value text)
;
CREATE TABLE test_cube (c cube)
;
VACUUM (PARALLEL 2) pg_catalog.pg_subscription_rel
;
CREATE SCHEMA split_extension_next
;
CREATE FUNCTION ks_test_uniform_random_int_in_range()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 999999) / 1000000.0 r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
ALTER PUBLICATION testpub_forparted DROP TABLE testpub_parted
;
CREATE INDEX at_partitioned_b_idx ON public.at_partitioned USING btree (b)
;
ALTER OPERATOR FAMILY gist_float8_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE FUNCTION regress_seg_schema.compare(oid, regclass) RETURNS boolean AS
  'BEGIN RAISE EXCEPTION ''overloaded compare() called by %'', current_user; END;' LANGUAGE plpgsql
;
CREATE INDEX t2_gin ON s5.t2 USING GIN (ia)
;
CREATE FOREIGN TABLE tru_ftable__p1 PARTITION OF tru_ptable
                                    FOR VALUES WITH (MODULUS 2, REMAINDER 1)
       SERVER loopback OPTIONS (table_name 'tru_rtable1')
;
DO LANGUAGE plpgsql $$
BEGIN
  COPY copy1 TO '/src/src/pl/plpgsql/src/results/copy1.data';
END;
$$
;
-- Should show notice that nothing was done
ALTER TABLE ALL IN TABLESPACE regress_tblspace_renamed SET TABLESPACE pg_default
;
CREATE TABLE am_partitioned(x INT, y INT)
  PARTITION BY hash (x)
;
-- Tests for range data types.

create type textrange as range (subtype=text, collation="C")
;
/*
 * text search configuration for nepali language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * nepali and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
create table child3 (b int, a text)
;
ALTER OPERATOR ^@ (ltxtquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
ALTER INDEX pg_toast.pg_toast_33360_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION autoinc()
RETURNS trigger
AS '$libdir/autoinc'
LANGUAGE C
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] CREATE TABLE "public"."temporal_fk2_rng2rng" (
    "id" "int4range" NOT NULL,
    "valid_at" "daterange" NOT NULL,
    "parent_id1" "int4range",
    "parent_id2" "int4range"
)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26805_2
;
----------
[CREATE_TABLE] create table parted_index_col_drop1 partition of parted_index_col_drop
  for values in (1) partition by list (a)
;
----------
[DROP_VIEW] -- fail here
drop materialized view mvtest_error
;
----------
[RENAME_COLUMN] -- these three should fail
alter table renameColumnChild rename column a to d
;
----------
[DROP_TABLE] -- fail, foo.f1 requires it

drop table foo
;
----------
[ADD_INDEX] -- Failure for non-BRIN index.
CREATE INDEX test1_a_btree ON test1 (a)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table pk2 drop column b
;
----------
[CREATE_PROG_OBJ] create function fcompos2(v compos) returns void as $$
select fcompos1(v);
$$ language sql
;
----------
[CREATE_TABLE] CREATE TABLE part_m01_even PARTITION OF part_m01
	FOR VALUES IN (2,4,6,8) WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest4
;
----------
[CREATE_TABLE] CREATE TABLE "public"."constr_child" (
    "a" integer CONSTRAINT "constr_parent_a_not_null" NOT NULL
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table idxpart1 drop constraint idxpart1_pkey
;
----------
[CREATE_PROG_OBJ] create function xfloat8in(cstring) returns xfloat8 immutable strict
  language internal as 'int8in'
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25576_2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26471_2 ADD COLUMN newdata pg_temp_13.pg_temp_26471
;
----------
[CREATE_TABLE] CREATE TABLE reind_con_tab(id serial primary key, data text)
;
----------
[CREATE_TYPE] CREATE TYPE hstore
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tt2" (
    "x" integer,
    "y" numeric(9,2)
)
;
----------
[ADMIN_TABLE] vacuum analyze smalltbl
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_namespace
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."depth1" INHERIT "public"."depth0"
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION start_command()
RETURNS event_trigger AS $$
BEGIN
RAISE NOTICE '% - ddl_command_start', tg_tag;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."atnnpart1" (
    "col1" integer,
    "id" integer CONSTRAINT "another_constr" NOT NULL
)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE agg_text (
	a	int2 CHECK (a >= 0),
	b	float4
) SERVER file_server
OPTIONS (format 'text', filename '/src/contrib/file_fdw/data/agg.data', delimiter '	', null '\N')
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ftable
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #@%# (
   rightarg = int8,
   procedure = factorial,
   invalid_att = int8
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30974_index RENAME TO reind_con_toast_idx
;
----------
[ADD_INDEX] create index idxpart_c on only idxpart (c)
;
----------
[CREATE_TABLE] CREATE TABLE table_a (id integer, value text)
;
----------
[CREATE_TABLE] CREATE TABLE test_cube (c cube)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_subscription_rel
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_extension_next
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ks_test_uniform_random_int_in_range()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random(0, 999999) / 1000000.0 r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_forparted DROP TABLE testpub_parted
;
----------
[ADD_INDEX] CREATE INDEX at_partitioned_b_idx ON public.at_partitioned USING btree (b)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_float8_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regress_seg_schema.compare(oid, regclass) RETURNS boolean AS
  'BEGIN RAISE EXCEPTION ''overloaded compare() called by %'', current_user; END;' LANGUAGE plpgsql
;
----------
[ADD_INDEX] CREATE INDEX t2_gin ON s5.t2 USING GIN (ia)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tru_ftable__p1 PARTITION OF tru_ptable
                                    FOR VALUES WITH (MODULUS 2, REMAINDER 1)
       SERVER loopback OPTIONS (table_name 'tru_rtable1')
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
  COPY copy1 TO '/src/src/pl/plpgsql/src/results/copy1.data';
END;
$$
;
----------
[ALTER_TABLE] -- Should show notice that nothing was done
ALTER TABLE ALL IN TABLESPACE regress_tblspace_renamed SET TABLESPACE pg_default
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned(x INT, y INT)
  PARTITION BY hash (x)
;
----------
[CREATE_POLICY] -- Tests for range data types.

create type textrange as range (subtype=text, collation="C")
;
----------
[CREATE_POLICY] /*
 * text search configuration for nepali language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * nepali and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY nepali_stem
	(TEMPLATE = snowball, Language = nepali , StopWords=nepali)
;
----------
[CREATE_TABLE] create table child3 (b int, a text)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^@ (ltxtquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33360_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION autoinc()
RETURNS trigger
AS '$libdir/autoinc'
LANGUAGE C
;
