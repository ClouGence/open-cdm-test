CREATE PROCEDURE "public"."ptest7"("a" "text", "b" "text")
    LANGUAGE "sql"
    AS $$
SELECT a = b;
$$
;
create table ab_a1 partition of ab for values in(1) partition by list (b)
;
ALTER TABLE pg_toast.pg_toast_30764 RENAME TO reind_con_toast
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
create temp table t3 as select generate_series(-1000,1000) as x
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc2(1) WITH ORDINALITY AS t1(v,o)
;
create table cities (
	name		text,
	population	float8,
	altitude	int		
)
;
DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 20878 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
CREATE TABLE num_result (id1 int4, id2 int4, result numeric(210,10))
;
CREATE TABLE rules_parted_table (a int) PARTITION BY LIST (a)
;
CREATE TABLE partrem3 PARTITION OF partrem FOR VALUES IN (3)
;
ANALYZE public.date_tbl
;
DO $$
BEGIN
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
  ROLLBACK;
END
$$
;
create function at_test_sql_partop (int4, int4) returns int language sql
as $$ select case when $1 = $2 then 0 when $1 > $2 then 1 else -1 end; $$
;
-- partitions mixing temporary and permanent relations
create table perm_parted (a int) partition by list (a)
;
-- check that collations are assigned in partition bound expressions
create table parted_boolean_col (a bool, b text) partition by list(a)
;
--
-- Test deferred FK check on a tuple deleted by a rolled-back subtransaction
--
create table pktable2(f1 int primary key)
;
CREATE UNIQUE INDEX mvtest_boxmv_id ON mvtest_boxmv (id)
;
create or replace view agg_view1 as
  select aggfns(a,a,c order by b)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c)
;
CREATE TYPE "public"."planets" AS ENUM (
)
;
CREATE TRIGGER city_update_trig INSTEAD OF UPDATE ON city_view
FOR EACH ROW EXECUTE PROCEDURE city_update()
;
CREATE TABLE plt1_adv_default PARTITION OF plt1_adv DEFAULT
;
ALTER FOREIGN TABLE ft2 DROP COLUMN cx
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_config
;
create table ab_a3_b3 partition of ab_a3 for values in (3)
;
CREATE FUNCTION "public"."mr_inoutparam_succeed"(OUT "i" "anyelement", INOUT "r" "anymultirange") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select upper($1), $1 $_$
;
ALTER FOREIGN TABLE ft1 DROP COLUMN c9
;
CREATE INDEX "bt_name_index" ON "public"."bt_name_heap" USING "btree" ("seqno")
;
CREATE TEMP TABLE articles_in_category (
    article_id int,
    category_id int,
    changed date,
    PRIMARY KEY (article_id, category_id)
)
;
CREATE VIEW lock_view6 AS SELECT * from (select * from lock_tbl1) sub
;
-- fail
TRUNCATE atestp1
;
ALTER TABLE pg_toast.pg_toast_30477 RENAME TO reind_con_toast
;
CREATE FUNCTION fn_x_after () RETURNS TRIGGER AS '
  BEGIN
		UPDATE x set e=''after trigger fired'' where c=''stuff'';
		return NULL;
	END;
' LANGUAGE plpgsql
;
-- verify all a<=5 are intact

DROP TABLE t1, t11, t12, t111 CASCADE
;
CREATE FUNCTION "public"."trig_nothing"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$ begin return null; end $$
;
-- not a table
CREATE SEQUENCE sequence_testx OWNED BY pg_class.relname
;
DROP SCHEMA reindex_schema CASCADE
;
CREATE TABLE "public"."notnull_tbl5" (
    "a" integer,
    "b" integer
)
;
ALTER TABLE ONLY "fkpart3"."pk52"
    ADD CONSTRAINT "pk52_pkey" PRIMARY KEY ("a")
;
ALTER TEXT SEARCH CONFIGURATION danish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
-- Test brin_summarize_range
CREATE TABLE brin_summarize_bloom (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
create or replace view my_locks as
select case when c.relname like 'pg_toast%' then 'pg_toast' else c.relname end, max(mode::lockmodes) as max_lockmode
from pg_locks l join pg_class c on l.relation = c.oid
where virtualtransaction = (
        select virtualtransaction
        from pg_locks
        where transactionid = txid_current()::integer)
and locktype = 'relation'
and relnamespace != (select oid from pg_namespace where nspname = 'pg_catalog')
and c.relname = 'my_locks'
group by c.relname
;
analyze test_range_gist
;
ANALYZE public.prt2_l_p2_p1
;
-- conditional WHEN clause
CREATE TABLE wq_target (tid integer not null, balance integer DEFAULT -1)
  WITH (autovacuum_enabled=off)
;
CREATE FUNCTION casttesttype_eq(casttesttype, casttesttype)
returns boolean language sql immutable as $$
    SELECT true
$$
;
CREATE RULE gtest_rule_qual AS ON UPDATE TO gtest_rule WHERE NEW.b > 100
  DO INSTEAD NOTHING
;
CREATE TABLE numrange_test (nr NUMRANGE)
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (force_not_null '*')
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest7"("a" "text", "b" "text")
    LANGUAGE "sql"
    AS $$
SELECT a = b;
$$
;
----------
[CREATE_TABLE] create table ab_a1 partition of ab for values in(1) partition by list (b)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30764 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ismn,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE|SELECT] create temp table t3 as select generate_series(-1000,1000) as x
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc2(1) WITH ORDINALITY AS t1(v,o)
;
----------
[CREATE_TABLE] create table cities (
	name		text,
	population	float8,
	altitude	int		
)
;
----------
[BLOCK] DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 20878 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
----------
[CREATE_TABLE] CREATE TABLE num_result (id1 int4, id2 int4, result numeric(210,10))
;
----------
[CREATE_TABLE] CREATE TABLE rules_parted_table (a int) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE partrem3 PARTITION OF partrem FOR VALUES IN (3)
;
----------
[ADMIN_TABLE] ANALYZE public.date_tbl
;
----------
[BLOCK] DO $$
BEGIN
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
  ROLLBACK;
END
$$
;
----------
[CREATE_PROG_OBJ] create function at_test_sql_partop (int4, int4) returns int language sql
as $$ select case when $1 = $2 then 0 when $1 > $2 then 1 else -1 end; $$
;
----------
[CREATE_TABLE] -- partitions mixing temporary and permanent relations
create table perm_parted (a int) partition by list (a)
;
----------
[CREATE_TABLE] -- check that collations are assigned in partition bound expressions
create table parted_boolean_col (a bool, b text) partition by list(a)
;
----------
[CREATE_TABLE] --
-- Test deferred FK check on a tuple deleted by a rolled-back subtransaction
--
create table pktable2(f1 int primary key)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX mvtest_boxmv_id ON mvtest_boxmv (id)
;
----------
[CREATE_VIEW(SELECT)] create or replace view agg_view1 as
  select aggfns(a,a,c order by b)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."planets" AS ENUM (
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER city_update_trig INSTEAD OF UPDATE ON city_view
FOR EACH ROW EXECUTE PROCEDURE city_update()
;
----------
[CREATE_TABLE] CREATE TABLE plt1_adv_default PARTITION OF plt1_adv DEFAULT
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE ft2 DROP COLUMN cx
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_ts_config
;
----------
[CREATE_TABLE] create table ab_a3_b3 partition of ab_a3 for values in (3)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."mr_inoutparam_succeed"(OUT "i" "anyelement", INOUT "r" "anymultirange") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select upper($1), $1 $_$
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER FOREIGN TABLE ft1 DROP COLUMN c9
;
----------
[ADD_INDEX] CREATE INDEX "bt_name_index" ON "public"."bt_name_heap" USING "btree" ("seqno")
;
----------
[CREATE_TABLE] CREATE TEMP TABLE articles_in_category (
    article_id int,
    category_id int,
    changed date,
    PRIMARY KEY (article_id, category_id)
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW lock_view6 AS SELECT * from (select * from lock_tbl1) sub
;
----------
[TRUNCATE_TABLE] -- fail
TRUNCATE atestp1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30477 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn_x_after () RETURNS TRIGGER AS '
  BEGIN
		UPDATE x set e=''after trigger fired'' where c=''stuff'';
		return NULL;
	END;
' LANGUAGE plpgsql
;
----------
[DROP_TABLE] -- verify all a<=5 are intact

DROP TABLE t1, t11, t12, t111 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."trig_nothing"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$ begin return null; end $$
;
----------
[CREATE_SEQUENCE] -- not a table
CREATE SEQUENCE sequence_testx OWNED BY pg_class.relname
;
----------
[DROP_SCHEMA] DROP SCHEMA reindex_schema CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl5" (
    "a" integer,
    "b" integer
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart3"."pk52"
    ADD CONSTRAINT "pk52_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION danish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_TABLE] -- Test brin_summarize_range
CREATE TABLE brin_summarize_bloom (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
----------
[CREATE_VIEW(SELECT)] create or replace view my_locks as
select case when c.relname like 'pg_toast%' then 'pg_toast' else c.relname end, max(mode::lockmodes) as max_lockmode
from pg_locks l join pg_class c on l.relation = c.oid
where virtualtransaction = (
        select virtualtransaction
        from pg_locks
        where transactionid = txid_current()::integer)
and locktype = 'relation'
and relnamespace != (select oid from pg_namespace where nspname = 'pg_catalog')
and c.relname = 'my_locks'
group by c.relname
;
----------
[ADMIN_TABLE] analyze test_range_gist
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_l_p2_p1
;
----------
[CREATE_TABLE] -- conditional WHEN clause
CREATE TABLE wq_target (tid integer not null, balance integer DEFAULT -1)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION casttesttype_eq(casttesttype, casttesttype)
returns boolean language sql immutable as $$
    SELECT true
$$
;
----------
[CREATE_POLICY] CREATE RULE gtest_rule_qual AS ON UPDATE TO gtest_rule WHERE NEW.b > 100
  DO INSTEAD NOTHING
;
----------
[CREATE_TABLE] CREATE TABLE numrange_test (nr NUMRANGE)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (force_not_null '*')
;
