create temp table temp_parted_oncommit_test1
  partition of temp_parted_oncommit_test
  for values in (1) on commit delete rows
;
-- Tests for relation options
CREATE EXTENSION dummy_index_am
;
CREATE TABLE shoelace_arrive (
        arr_name    char(10),
        arr_quant   integer
    )
;
ALTER TABLE ONLY "generated_stored_tests"."gtest27" DROP COLUMN "........pg.dropped.3........"
;
--     P    P
-- should ERROR: we have no way to resolve S
CREATE AGGREGATE myaggp04a(*) (SFUNC = stfp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
create unique index hash_f8_index_1 on hash_f8_heap(abs(random))
;
CREATE TABLE target (key int primary key, balance integer, status text, val text)
;
create function anyctest(anycompatible, anycompatiblerange)
returns anycompatiblerange as $$
  select $2
$$ language sql
;
CREATE FUNCTION gin_extract_query_numeric(numeric, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
-- UPDATE partition-key with FROM clause. If join produces multiple output
-- rows for the same row to be modified, we should tuple-route the row only
-- once. There should not be any rows inserted.
CREATE TABLE non_parted (id int)
;
ALTER TABLE reloptions_test RESET (illegal_option)
;
VACUUM pg_catalog.pg_shdepend
;
-- operator 1 requested first-time
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD OPERATOR 1 < (int4, int4)
;
ANALYZE transition_table_level1
;
CREATE TABLE prt3_n (a int, b int, c text) PARTITION BY LIST(c)
;
CREATE TABLE beta (a double precision, b int, c text) PARTITION BY RANGE (a)
;
COMMENT ON CONSTRAINT the_constraint ON DOMAIN constraint_comments_dom IS 'yes, another comment'
;
ALTER TABLE ONLY "public"."mlparted_def2" DROP COLUMN "........pg.dropped.6........"
;
create table notnull_tbl1 (a int primary key constraint foo not null)
;
CREATE TEXT SEARCH PARSER alt_ts_prs2
    (start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
create type e as enum ('0', '2', '3')
;
CREATE OR REPLACE FUNCTION foo(a INT) RETURNS TEXT AS $$
DECLARE res TEXT := '';
        i INT;
BEGIN
  i := 0;
  WHILE (i < a) LOOP
    res := res || chr(ascii('a') + i);
    i := i + 1;
  END LOOP;
  RETURN res;
END; $$ LANGUAGE PLPGSQL STABLE
;
-- The previous invalid index is selected, invalidating all the indexes up to
-- the top-most parent.
create index parted_isvalid_idx on parted_isvalid_tab ((a/b))
;
CREATE SEQUENCE sequence_testx INCREMENT BY -1 MINVALUE 20
;
-- Verify that multiple inserts to subfields of a domain-over-container
-- check the domain constraints only on the finished value

create domain insert_nnarray as int[]
  check (value[1] is not null and value[2] is not null)
;
CREATE FUNCTION btissn13cmp(issn13, issn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ANALYZE public.num_data
;
ALTER OPERATOR >= (issn, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
alter table idxpart drop column col1, drop column col2
;
--    P    N        N    N
-- should ERROR: tfnp(anyarray, int) not matched by tfnp(int[],int)
CREATE AGGREGATE myaggn13a(BASETYPE = int, SFUNC = tfnp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_opclass
;
DROP FUNCTION bt_page_stats(text, int4)
;
-- ok
CREATE MATERIALIZED VIEW mvtest_mv_v_3 (ii, jj, kk) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
CREATE FUNCTION rngfunct(int) returns setof rngfunc2 as 'SELECT * FROM rngfunc2 WHERE rngfuncid = $1 ORDER BY f2;' LANGUAGE SQL
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_cast
;
ALTER TABLE s2.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub_mix_3, pub_mix_4
;
CREATE DATABASE regress_db
;
CREATE INDEX idx_postgis_places_area_spgist ON split_postgis.places USING spgist (area)
;
ALTER TABLE t SET tablespace regress_ts3
;
/*
 * 6.75
 * UDT_PRIVILEGES view
 */

CREATE VIEW udt_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(n.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST('TYPE USAGE' AS character_data) AS privilege_type, -- sic
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, t.typowner, 'USAGE')
                  OR t.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, typname, typnamespace, typtype, typowner, (aclexplode(coalesce(typacl, acldefault('T', typowner)))).* FROM pg_type
         ) AS t (oid, typname, typnamespace, typtype, typowner, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE t.typnamespace = n.oid
          AND t.typtype = 'c'
          AND t.grantee = grantee.oid
          AND t.grantor = u_grantor.oid
          AND t.prtype IN ('USAGE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
-- Ok
ALTER OPERATOR FAMILY alt_opf19 USING btree DROP FUNCTION 5 (int4, int4)
;
ANALYZE expr_stats_incompatible_test
;
ALTER TABLE pg_foreign_data_wrapper ADD PRIMARY KEY USING INDEX pg_foreign_data_wrapper_oid_index
;
create table idxpart1 (drop_1 int, drop_2 int, col_keep int, drop_3 int)
;
DO
LANGUAGE plpgsql
$$
BEGIN
    FOR i IN 0..9 LOOP
        INSERT INTO test1 (a) VALUES (i);
        IF i % 2 = 0 THEN
            COMMIT;
        ELSE
            ROLLBACK;
        END IF;
    END LOOP;
END
$$
;
/* Now redefine */
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_8'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
ALTER TABLE comment_test ALTER COLUMN positive_col SET DATA TYPE int
;
CREATE SEQUENCE s3.seq1
;
CREATE TABLE tab4 (a int PRIMARY KEY) PARTITION BY LIST (a)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create temp table temp_parted_oncommit_test1
  partition of temp_parted_oncommit_test
  for values in (1) on commit delete rows
;
----------
[CREATE_LIBRARY] -- Tests for relation options
CREATE EXTENSION dummy_index_am
;
----------
[CREATE_TABLE] CREATE TABLE shoelace_arrive (
        arr_name    char(10),
        arr_quant   integer
    )
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_stored_tests"."gtest27" DROP COLUMN "........pg.dropped.3........"
;
----------
[CREATE_PROG_OBJ] --     P    P
-- should ERROR: we have no way to resolve S
CREATE AGGREGATE myaggp04a(*) (SFUNC = stfp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ADD_INDEX] create unique index hash_f8_index_1 on hash_f8_heap(abs(random))
;
----------
[CREATE_TABLE] CREATE TABLE target (key int primary key, balance integer, status text, val text)
;
----------
[CREATE_PROG_OBJ] create function anyctest(anycompatible, anycompatiblerange)
returns anycompatiblerange as $$
  select $2
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_numeric(numeric, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] -- UPDATE partition-key with FROM clause. If join produces multiple output
-- rows for the same row to be modified, we should tuple-route the row only
-- once. There should not be any rows inserted.
CREATE TABLE non_parted (id int)
;
----------
[ALTER_TABLE] ALTER TABLE reloptions_test RESET (illegal_option)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_shdepend
;
----------
[ALTER_PROG_OBJ] -- operator 1 requested first-time
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD OPERATOR 1 < (int4, int4)
;
----------
[ADMIN_TABLE] ANALYZE transition_table_level1
;
----------
[CREATE_TABLE] CREATE TABLE prt3_n (a int, b int, c text) PARTITION BY LIST(c)
;
----------
[CREATE_TABLE] CREATE TABLE beta (a double precision, b int, c text) PARTITION BY RANGE (a)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT the_constraint ON DOMAIN constraint_comments_dom IS 'yes, another comment'
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def2" DROP COLUMN "........pg.dropped.6........"
;
----------
[CREATE_TABLE] create table notnull_tbl1 (a int primary key constraint foo not null)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH PARSER alt_ts_prs2
    (start = prsd_start, gettoken = prsd_nexttoken, end = prsd_end, lextypes = prsd_lextype)
;
----------
[CREATE_TYPE] create type e as enum ('0', '2', '3')
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION foo(a INT) RETURNS TEXT AS $$
DECLARE res TEXT := '';
        i INT;
BEGIN
  i := 0;
  WHILE (i < a) LOOP
    res := res || chr(ascii('a') + i);
    i := i + 1;
  END LOOP;
  RETURN res;
END; $$ LANGUAGE PLPGSQL STABLE
;
----------
[ADD_INDEX] -- The previous invalid index is selected, invalidating all the indexes up to
-- the top-most parent.
create index parted_isvalid_idx on parted_isvalid_tab ((a/b))
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_testx INCREMENT BY -1 MINVALUE 20
;
----------
[CREATE_TYPE] -- Verify that multiple inserts to subfields of a domain-over-container
-- check the domain constraints only on the finished value

create domain insert_nnarray as int[]
  check (value[1] is not null and value[2] is not null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btissn13cmp(issn13, issn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.num_data
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (issn, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart drop column col1, drop column col2
;
----------
[CREATE_PROG_OBJ] --    P    N        N    N
-- should ERROR: tfnp(anyarray, int) not matched by tfnp(int[],int)
CREATE AGGREGATE myaggn13a(BASETYPE = int, SFUNC = tfnp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_opclass
;
----------
[DROP_PROG_OBJ] DROP FUNCTION bt_page_stats(text, int4)
;
----------
[CREATE_VIEW(SELECT)] -- ok
CREATE MATERIALIZED VIEW mvtest_mv_v_3 (ii, jj, kk) AS SELECT i, j FROM mvtest_v WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rngfunct(int) returns setof rngfunc2 as 'SELECT * FROM rngfunc2 WHERE rngfuncid = $1 ORDER BY f2;' LANGUAGE SQL
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_cast
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE s2.t2
				ALTER COLUMN t
				SET STORAGE EXTERNAL
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub_mix_3, pub_mix_4
;
----------
[CREATE_CATALOG] CREATE DATABASE regress_db
;
----------
[ADD_INDEX] CREATE INDEX idx_postgis_places_area_spgist ON split_postgis.places USING spgist (area)
;
----------
[ALTER_TABLE] ALTER TABLE t SET tablespace regress_ts3
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.75
 * UDT_PRIVILEGES view
 */

CREATE VIEW udt_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(n.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST('TYPE USAGE' AS character_data) AS privilege_type, -- sic
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, t.typowner, 'USAGE')
                  OR t.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, typname, typnamespace, typtype, typowner, (aclexplode(coalesce(typacl, acldefault('T', typowner)))).* FROM pg_type
         ) AS t (oid, typname, typnamespace, typtype, typowner, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE t.typnamespace = n.oid
          AND t.typtype = 'c'
          AND t.grantee = grantee.oid
          AND t.grantor = u_grantor.oid
          AND t.prtype IN ('USAGE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
----------
[ALTER_PROG_OBJ] -- Ok
ALTER OPERATOR FAMILY alt_opf19 USING btree DROP FUNCTION 5 (int4, int4)
;
----------
[ADMIN_TABLE] ANALYZE expr_stats_incompatible_test
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_foreign_data_wrapper ADD PRIMARY KEY USING INDEX pg_foreign_data_wrapper_oid_index
;
----------
[CREATE_TABLE] create table idxpart1 (drop_1 int, drop_2 int, col_keep int, drop_3 int)
;
----------
[BLOCK] DO
LANGUAGE plpgsql
$$
BEGIN
    FOR i IN 0..9 LOOP
        INSERT INTO test1 (a) VALUES (i);
        IF i % 2 = 0 THEN
            COMMIT;
        ELSE
            ROLLBACK;
        END IF;
    END LOOP;
END
$$
;
----------
[CREATE_PROG_OBJ] /* Now redefine */
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_8'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN positive_col SET DATA TYPE int
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE s3.seq1
;
----------
[CREATE_TABLE] CREATE TABLE tab4 (a int PRIMARY KEY) PARTITION BY LIST (a)
;
