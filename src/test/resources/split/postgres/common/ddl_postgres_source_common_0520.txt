CREATE FUNCTION _ltree_same(ltree_gist, ltree_gist, internal)
RETURNS internal
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ANALYZE public.num_input_test
;
ALTER TABLE dump_test_second_schema.measurement_y2006m3 DISABLE TRIGGER test_trigger
;
-- test that a non-mapping result is correctly rejected
CREATE FUNCTION test1bad() RETURNS hstore
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
return "foo"
$$
;
CREATE OPERATOR CLASS ean13_ops DEFAULT
	FOR TYPE ean13 USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashean13(ean13)
;
CREATE FUNCTION cube_eq(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create table invalid_check_con_child() inherits(invalid_check_con)
;
ALTER INDEX onek_unique1 RENAME TO fail
;
drop index idxpart1_a_idx
;
CREATE TABLE alice.hashpart (i INTEGER) PARTITION BY HASH (i)
;
/*
 * 6.14
 * CHECK_CONSTRAINT_ROUTINE_USAGE view
 */

CREATE VIEW check_constraint_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name
    FROM pg_namespace nc, pg_constraint c, pg_depend d, pg_proc p, pg_namespace np
    WHERE nc.oid = c.connamespace
      AND c.contype = 'c'
      AND c.oid = d.objid
      AND d.classid = 'pg_catalog.pg_constraint'::regclass
      AND d.refobjid = p.oid
      AND d.refclassid = 'pg_catalog.pg_proc'::regclass
      AND p.pronamespace = np.oid
      AND pg_has_role(p.proowner, 'USAGE')
;
CREATE TABLE DEFAULTEXPR_TBL (i1 int DEFAULT 100 + (200-199) * 2,
	i2 int DEFAULT nextval('default_seq'))
;
-- Create the operator class
CREATE OPERATOR CLASS gist_int4_ops
DEFAULT FOR TYPE int4 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_int4_consistent (internal, int4, int2, oid, internal),
	FUNCTION	2	gbt_int4_union (internal, internal),
	FUNCTION	3	gbt_int4_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_int4_penalty (internal, internal, internal),
	FUNCTION	6	gbt_int4_picksplit (internal, internal),
	FUNCTION	7	gbt_int4_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
alter table tt5 drop column c
;
CREATE TEMP TABLE pg_temp_10.pg_temp_25428_2 (tid pg_catalog.tid)
;
create table testtable_orange(logdate date)
;
DROP TABLE test_replica_identity_t3
;
ANALYZE prt2_adv
;
ALTER TABLE measurement ADD CONSTRAINT mcheck CHECK (city_id = 0) NO INHERIT
;
CREATE VIEW rw_view AS SELECT * FROM parent_tbl WHERE a < 5 WITH CHECK OPTION
;
-- error: reference remains

ALTER TABLE parted_self_fk DETACH PARTITION part2_self_fk
;
--
-- Test @extschema@ syntax.
--
CREATE SCHEMA "has space"
;
CREATE TEMP TABLE temptest(tcol float)
;
ALTER TABLE T ADD COLUMN c_bpchar BPCHAR(5) DEFAULT foo(4),
              ALTER COLUMN c_int SET DEFAULT LENGTH(foo(8))
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
CREATE OPERATOR ~ (
        LEFTARG = ltree,
	RIGHTARG = lquery,
	PROCEDURE = ltq_regex,
	COMMUTATOR = '~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_am
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest27" DROP COLUMN "........pg.dropped.3........"
;
ALTER TABLE ONLY "public"."mlparted1" DROP COLUMN "........pg.dropped.7........"
;
ALTER TABLE tab_rowfilter_partitioned_2 ATTACH PARTITION tab_rowfilter_partition DEFAULT
;
DROP INDEX CONCURRENTLY "concur_index2", "concur_index3"
;
ALTER TABLE fkpart11.pk1 ATTACH PARTITION fkpart11.pk12 FOR VALUES IN (2)
;
/*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2023
 *
 * Copyright (c) 2003-2023, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 6.2
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=16972 host=/tmp/kMt7cqXM2r dbname=postgres' PUBLICATION mypub
;
create table tht_p2 partition of tht for values with (modulus 3, remainder 1)
;
ALTER INDEX pg_toast.pg_toast_33474_index RENAME TO reind_con_toast_idx
;
-- check that the new partition won't overlap with an existing partition
CREATE TABLE fail_part (LIKE part_1 INCLUDING CONSTRAINTS)
;
ALTER TYPE "public"."planets" ADD VALUE 'pluto'
;
ALTER TABLE pg_toast.pg_toast_33078 RENAME TO reind_con_toast
;
alter table parted_conflict_1 drop column drp
;
CREATE TABLE regress_pg_dump_table_added (col1 int not null, col2 int)
;
ALTER TABLE pg_temp_4.pg_temp_37830_2 ADD COLUMN newdata pg_temp_4.pg_temp_37830
;
CREATE TEXT SEARCH DICTIONARY greek_stem
	(TEMPLATE = snowball, Language = greek )
;
-- No cast yet, should fail

CREATE CAST (int4 AS casttesttype) WITH INOUT
;
DROP FUNCTION plusone(int)
;
CREATE OPERATOR CLASS dump_test.op_class_empty
		                 FOR TYPE bigint USING btree FAMILY dump_test.op_family
						 AS STORAGE bigint
;
ALTER TABLE ONLY "public"."tableam_parted_heap2" ATTACH PARTITION "public"."tableam_parted_a_heap2" FOR VALUES IN ('a')
;
CREATE TABLE dummy(i int)
;
ALTER TABLE gtestxx_1 INHERIT gtest1
;
CREATE FUNCTION test4() RETURNS trigger
LANGUAGE plperlu
TRANSFORM FOR TYPE hstore
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_TD->{new}));
if ($_TD->{new}{a} == 1) {
    $_TD->{new}{b} = {a => 1, b => 'boo', c => undef};
}

return "MODIFY";
$$
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_same(ltree_gist, ltree_gist, internal)
RETURNS internal
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.num_input_test
;
----------
[ALTER_TABLE] ALTER TABLE dump_test_second_schema.measurement_y2006m3 DISABLE TRIGGER test_trigger
;
----------
[CREATE_PROG_OBJ] -- test that a non-mapping result is correctly rejected
CREATE FUNCTION test1bad() RETURNS hstore
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
return "foo"
$$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS ean13_ops DEFAULT
	FOR TYPE ean13 USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashean13(ean13)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_eq(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table invalid_check_con_child() inherits(invalid_check_con)
;
----------
[RENAME_INDEX] ALTER INDEX onek_unique1 RENAME TO fail
;
----------
[DROP_INDEX] drop index idxpart1_a_idx
;
----------
[CREATE_TABLE] CREATE TABLE alice.hashpart (i INTEGER) PARTITION BY HASH (i)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.14
 * CHECK_CONSTRAINT_ROUTINE_USAGE view
 */

CREATE VIEW check_constraint_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name
    FROM pg_namespace nc, pg_constraint c, pg_depend d, pg_proc p, pg_namespace np
    WHERE nc.oid = c.connamespace
      AND c.contype = 'c'
      AND c.oid = d.objid
      AND d.classid = 'pg_catalog.pg_constraint'::regclass
      AND d.refobjid = p.oid
      AND d.refclassid = 'pg_catalog.pg_proc'::regclass
      AND p.pronamespace = np.oid
      AND pg_has_role(p.proowner, 'USAGE')
;
----------
[CREATE_TABLE] CREATE TABLE DEFAULTEXPR_TBL (i1 int DEFAULT 100 + (200-199) * 2,
	i2 int DEFAULT nextval('default_seq'))
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_int4_ops
DEFAULT FOR TYPE int4 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_int4_consistent (internal, int4, int2, oid, internal),
	FUNCTION	2	gbt_int4_union (internal, internal),
	FUNCTION	3	gbt_int4_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_int4_penalty (internal, internal, internal),
	FUNCTION	6	gbt_int4_picksplit (internal, internal),
	FUNCTION	7	gbt_int4_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table tt5 drop column c
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_25428_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table testtable_orange(logdate date)
;
----------
[DROP_TABLE] DROP TABLE test_replica_identity_t3
;
----------
[ADMIN_TABLE] ANALYZE prt2_adv
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE measurement ADD CONSTRAINT mcheck CHECK (city_id = 0) NO INHERIT
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view AS SELECT * FROM parent_tbl WHERE a < 5 WITH CHECK OPTION
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- error: reference remains

ALTER TABLE parted_self_fk DETACH PARTITION part2_self_fk
;
----------
[CREATE_SCHEMA] --
-- Test @extschema@ syntax.
--
CREATE SCHEMA "has space"
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest(tcol float)
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_bpchar BPCHAR(5) DEFAULT foo(4),
              ALTER COLUMN c_int SET DEFAULT LENGTH(foo(8))
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~ (
        LEFTARG = ltree,
	RIGHTARG = lquery,
	PROCEDURE = ltq_regex,
	COMMUTATOR = '~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_am
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_virtual_tests"."gtest27" DROP COLUMN "........pg.dropped.3........"
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted1" DROP COLUMN "........pg.dropped.7........"
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab_rowfilter_partitioned_2 ATTACH PARTITION tab_rowfilter_partition DEFAULT
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY "concur_index2", "concur_index3"
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fkpart11.pk1 ATTACH PARTITION fkpart11.pk12 FOR VALUES IN (2)
;
----------
[CREATE_SCHEMA] /*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2023
 *
 * Copyright (c) 2003-2023, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 6.2
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=16972 host=/tmp/kMt7cqXM2r dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_TABLE] create table tht_p2 partition of tht for values with (modulus 3, remainder 1)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33474_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- check that the new partition won't overlap with an existing partition
CREATE TABLE fail_part (LIKE part_1 INCLUDING CONSTRAINTS)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."planets" ADD VALUE 'pluto'
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33078 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table parted_conflict_1 drop column drp
;
----------
[CREATE_TABLE] CREATE TABLE regress_pg_dump_table_added (col1 int not null, col2 int)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_37830_2 ADD COLUMN newdata pg_temp_4.pg_temp_37830
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY greek_stem
	(TEMPLATE = snowball, Language = greek )
;
----------
[CREATE_PROG_OBJ] -- No cast yet, should fail

CREATE CAST (int4 AS casttesttype) WITH INOUT
;
----------
[DROP_PROG_OBJ] DROP FUNCTION plusone(int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS dump_test.op_class_empty
		                 FOR TYPE bigint USING btree FAMILY dump_test.op_family
						 AS STORAGE bigint
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."tableam_parted_heap2" ATTACH PARTITION "public"."tableam_parted_a_heap2" FOR VALUES IN ('a')
;
----------
[CREATE_TABLE] CREATE TABLE dummy(i int)
;
----------
[ALTER_TABLE] ALTER TABLE gtestxx_1 INHERIT gtest1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test4() RETURNS trigger
LANGUAGE plperlu
TRANSFORM FOR TYPE hstore
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_TD->{new}));
if ($_TD->{new}{a} == 1) {
    $_TD->{new}{b} = {a => 1, b => 'boo', c => undef};
}

return "MODIFY";
$$
;
