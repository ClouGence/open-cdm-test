create table listp_1 partition of listp for values in(1) partition by list (b)
;
ALTER TABLE ONLY "public"."parted_self_fk" ATTACH PARTITION "public"."part3_self_fk" FOR VALUES FROM ('20') TO ('40')
;
CREATE INDEX test_index2 ON testschema.test_default_tab_p USING btree (val) TABLESPACE regress_tblspace
;
ALTER TABLE ONLY "public"."prt2" ATTACH PARTITION "public"."prt2_p1" FOR VALUES FROM (0) TO (250)
;
CREATE TABLE test_part_c_1 PARTITION OF test_part_c FOR VALUES IN (1,3)
;
DROP TABLE trunc_stats_test, trunc_stats_test1, trunc_stats_test2, trunc_stats_test3, trunc_stats_test4
;
create table trigpart3 (like trigpart)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub1
;
CREATE CONVERSION split_conversion_probe.latin1_to_utf8_probe FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
create table stmt_trig_on_empty_upd1 () inherits (stmt_trig_on_empty_upd)
;
-- On the other hand, this should work because int implicitly promotes to
-- numeric, and we allow promotion on the FK side
CREATE TABLE PKTABLE (ptest1 numeric PRIMARY KEY)
;
alter operator class alter1.ctype_hash_ops using hash set schema alter2
;
ALTER FOREIGN TABLE ft1 OPTIONS (schema_name 'S 1', table_name 'T 1')
;
CREATE INDEX "clstr_tst_c_b" ON "public"."clstr_tst" USING "btree" ("c", "b")
;
--
-- Tests for named- and mixed-notation function calling
--

create function dfunc(a int, b int, c int = 0, d int = 0)
  returns table (a int, b int, c int, d int) as $$
  select $1, $2, $3, $4;
$$ language sql
;
create table mcrparted8_ge_d partition of mcrparted for values from ('d', minvalue) to (maxvalue, maxvalue)
;
CREATE TABLE test_inh_check (a float check (a > 10.2), b float)
;
drop type comp
;
CREATE TABLE foo3 PARTITION OF foo FOR VALUES IN (3)
;
ALTER OPERATOR FAMILY alt_opf7 USING btree DROP OPERATOR 1 (int4, int2, int8)
;
DROP TABLE pg_temp_26.pg_temp_27076_2, pg_temp_26.pg_temp_27076
;
ALTER TYPE "public"."insenum" ADD VALUE 'i16'
;
drop table cta_test1
;
ALTER TABLE ONLY "public"."prt1_m" ATTACH PARTITION "public"."prt1_m_p1" FOR VALUES FROM (0, 0) TO (250, 250)
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn13,
	RIGHTARG = issn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
--
-- Cleanup
--
DROP TABLE IF EXISTS t1 CASCADE
;
ANALYZE pg_catalog.pg_inherits
;
create index idxpart_brin on idxpart using brin(b)
;
/*
 * 1.test CREATE INDEX
 *
 * Deliberately avoid dropping objects in this section, to get some pg_dump
 * coverage.
 */
-- Regular index with included columns
CREATE TABLE tbl_include_reg (c1 int, c2 int, c3 int, c4 box)
;
-- Test ALTER DOMAIN .. CONSTRAINT .. NOT VALID
create domain things AS INT
;
CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 OID = 16384 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
alter table bar set (autovacuum_enabled = 'false')
;
ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_5" FOR VALUES IN (4500)
;
ALTER TABLE pg_ts_config ADD PRIMARY KEY USING INDEX pg_ts_config_oid_index
;
ANALYZE public.test_range_excl
;
DROP VIEW jsonb_table_view4
;
ALTER TABLE pg_temp_11.pg_temp_26345_2 ADD COLUMN newdata pg_temp_11.pg_temp_26345
;
create temp table prtx2_1 partition of prtx2 for values from (1) to (11)
;
CREATE TABLE "generated_stored_tests"."gtest10" (
    "a" integer NOT NULL,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "........pg.dropped.3........" INTEGER /* dummy */
)
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN c3 integer primary key
;
CREATE FOREIGN TABLE fb (i int) SERVER loopback OPTIONS (table_name 'b')
;
CREATE TABLE attmp(i integer)
;
CREATE TABLE "public"."prt2_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
CREATE FUNCTION isneq(issn, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create table parted (a int, b int, c text) partition by list ((a + b))
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub5
;
-- check that "do nothing" BR triggers work with tuple-routing (this checks
-- that estate->es_result_relation_info is appropriately set/reset for each
-- routed tuple)
create table donothingbrtrig_test (a int, b text) partition by list (a)
;
CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') NOT NULL,
	c2 text OPTIONS (param2 'val2', param3 'val3'),
	c3 date,
	UNIQUE (c3)
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
CREATE TABLE dump_test.regress_pg_dump_table_am_1 (col1 int) USING regress_table_am
;
ALTER TABLE pg_description RENAME COLUMN description TO comment
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table listp_1 partition of listp for values in(1) partition by list (b)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."parted_self_fk" ATTACH PARTITION "public"."part3_self_fk" FOR VALUES FROM ('20') TO ('40')
;
----------
[ADD_INDEX] CREATE INDEX test_index2 ON testschema.test_default_tab_p USING btree (val) TABLESPACE regress_tblspace
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2" ATTACH PARTITION "public"."prt2_p1" FOR VALUES FROM (0) TO (250)
;
----------
[CREATE_TABLE] CREATE TABLE test_part_c_1 PARTITION OF test_part_c FOR VALUES IN (1,3)
;
----------
[DROP_TABLE] DROP TABLE trunc_stats_test, trunc_stats_test1, trunc_stats_test2, trunc_stats_test3, trunc_stats_test4
;
----------
[CREATE_TABLE] create table trigpart3 (like trigpart)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=29048 host=/tmp/NHy0AFC7Ir dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_POLICY] CREATE CONVERSION split_conversion_probe.latin1_to_utf8_probe FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[CREATE_TABLE] create table stmt_trig_on_empty_upd1 () inherits (stmt_trig_on_empty_upd)
;
----------
[CREATE_TABLE] -- On the other hand, this should work because int implicitly promotes to
-- numeric, and we allow promotion on the FK side
CREATE TABLE PKTABLE (ptest1 numeric PRIMARY KEY)
;
----------
[ALTER_PROG_OBJ] alter operator class alter1.ctype_hash_ops using hash set schema alter2
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE ft1 OPTIONS (schema_name 'S 1', table_name 'T 1')
;
----------
[ADD_INDEX] CREATE INDEX "clstr_tst_c_b" ON "public"."clstr_tst" USING "btree" ("c", "b")
;
----------
[CREATE_PROG_OBJ] --
-- Tests for named- and mixed-notation function calling
--

create function dfunc(a int, b int, c int = 0, d int = 0)
  returns table (a int, b int, c int, d int) as $$
  select $1, $2, $3, $4;
$$ language sql
;
----------
[CREATE_TABLE] create table mcrparted8_ge_d partition of mcrparted for values from ('d', minvalue) to (maxvalue, maxvalue)
;
----------
[CREATE_TABLE] CREATE TABLE test_inh_check (a float check (a > 10.2), b float)
;
----------
[DROP_TYPE] drop type comp
;
----------
[CREATE_TABLE] CREATE TABLE foo3 PARTITION OF foo FOR VALUES IN (3)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf7 USING btree DROP OPERATOR 1 (int4, int2, int8)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_26.pg_temp_27076_2, pg_temp_26.pg_temp_27076
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i16'
;
----------
[DROP_TABLE] drop table cta_test1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_m" ATTACH PARTITION "public"."prt1_m_p1" FOR VALUES FROM (0, 0) TO (250, 250)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn13,
	RIGHTARG = issn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[DROP_TABLE] --
-- Cleanup
--
DROP TABLE IF EXISTS t1 CASCADE
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_inherits
;
----------
[ADD_INDEX] create index idxpart_brin on idxpart using brin(b)
;
----------
[CREATE_TABLE] /*
 * 1.test CREATE INDEX
 *
 * Deliberately avoid dropping objects in this section, to get some pg_dump
 * coverage.
 */
-- Regular index with included columns
CREATE TABLE tbl_include_reg (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_TYPE] -- Test ALTER DOMAIN .. CONSTRAINT .. NOT VALID
create domain things AS INT
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 OID = 16384 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[ALTER_TABLE] alter table bar set (autovacuum_enabled = 'false')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_5" FOR VALUES IN (4500)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_config ADD PRIMARY KEY USING INDEX pg_ts_config_oid_index
;
----------
[ADMIN_TABLE] ANALYZE public.test_range_excl
;
----------
[DROP_VIEW] DROP VIEW jsonb_table_view4
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26345_2 ADD COLUMN newdata pg_temp_11.pg_temp_26345
;
----------
[CREATE_TABLE] create temp table prtx2_1 partition of prtx2 for values from (1) to (11)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest10" (
    "a" integer NOT NULL,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "........pg.dropped.3........" INTEGER /* dummy */
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN c3 integer primary key
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE fb (i int) SERVER loopback OPTIONS (table_name 'b')
;
----------
[CREATE_TABLE] CREATE TABLE attmp(i integer)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(issn, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] create table parted (a int, b int, c text) partition by list ((a + b))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_TABLE] -- check that "do nothing" BR triggers work with tuple-routing (this checks
-- that estate->es_result_relation_info is appropriately set/reset for each
-- routed tuple)
create table donothingbrtrig_test (a int, b text) partition by list (a)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') NOT NULL,
	c2 text OPTIONS (param2 'val2', param3 'val3'),
	c3 date,
	UNIQUE (c3)
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.regress_pg_dump_table_am_1 (col1 int) USING regress_table_am
;
----------
[RENAME_COLUMN] ALTER TABLE pg_description RENAME COLUMN description TO comment
;
