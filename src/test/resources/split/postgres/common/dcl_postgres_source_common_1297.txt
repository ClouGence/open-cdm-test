ALTER TABLE public.nums OWNER TO zyc
;
GRANT SELECT ON z1_blacklist TO regress_rls_carol
;
EXECUTE dumpDomain('35634')
;
ALTER SERVER fdtest OPTIONS (ADD oauth_client_id 'myID')
;
SAVEPOINT subxact
;
EXECUTE dumpOpr('488')
;
-- The language object, but not the functions, can be owned by a non-superuser.
ALTER LANGUAGE plpgsql OWNER TO zyc
;
DROP FOREIGN DATA WRAPPER alt_fdw3 CASCADE
;
-- ok

-- child's permissions do not apply when operating on parent
SET SESSION AUTHORIZATION regress_priv_user1
;
CREATE ROLE regress_lr_replication REPLICATION
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE public.nums OWNER TO zyc
;
----------
[GRANT] GRANT SELECT ON z1_blacklist TO regress_rls_carol
;
----------
[UNSAFE] EXECUTE dumpDomain('35634')
;
----------
[SYSTEM_SETTING_WRITE] ALTER SERVER fdtest OPTIONS (ADD oauth_client_id 'myID')
;
----------
[TRANSACTION] SAVEPOINT subxact
;
----------
[UNSAFE] EXECUTE dumpOpr('488')
;
----------
[TRANSFER_PRIVILEGE] -- The language object, but not the functions, can be owned by a non-superuser.
ALTER LANGUAGE plpgsql OWNER TO zyc
;
----------
[SYSTEM_SETTING_WRITE] DROP FOREIGN DATA WRAPPER alt_fdw3 CASCADE
;
----------
[SWITCH_USER] -- ok

-- child's permissions do not apply when operating on parent
SET SESSION AUTHORIZATION regress_priv_user1
;
----------
[CREATE_ROLE] CREATE ROLE regress_lr_replication REPLICATION
;
