ALTER TYPE test_type2 ALTER ATTRIBUTE b TYPE varchar CASCADE
;
--
-- GiST (rtree-equivalent opclasses only)
--
CREATE INDEX grect2ind ON fast_emp4000 USING gist (home_base)
;
ALTER TABLE ONLY "public"."prt1" ATTACH PARTITION "public"."prt1_p1" FOR VALUES FROM (0) TO (250)
;
ALTER SUBSCRIPTION sub1 SET PUBLICATION pub2, pub1
;
--
-- additional checks for pg_index_column_has_property
--
CREATE TEMP TABLE foo (f1 int, f2 int, f3 int, f4 int)
;
CREATE TABLE am_partitioned_5p PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 5) PARTITION BY hash(y)
;
ALTER TABLE ONLY "public"."notnull_part1_upg" ATTACH PARTITION "public"."notnull_part1_3_upg" FOR VALUES IN (NULL, 5)
;
CREATE TEXT SEARCH DICTIONARY "public"."hunspell_long" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_long', afffile = 'hunspell_sample_long' )
;
CREATE TABLE "gin_check_text_array"("Column1" text[])
;
CREATE FUNCTION cube_is_point(cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION ltreeparentsel(internal, oid, internal, integer)
RETURNS float8
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."nv_child_2010" INHERIT "public"."nv_parent"
;
vacuum htab2
;
-- repeat the test for table with primary key index with included columns
CREATE TABLE foo_1(f1 int, f2 text, f3 text[], primary key (f1,f2) include (f3))
;
-- invalid: non-lowercase quoted identifiers
CREATE TYPE case_int42 (
	"Internallength" = 4,
	"Input" = int42_in,
	"Output" = int42_out,
	"Alignment" = int4,
	"Default" = 42,
	"Passedbyvalue"
)
;
alter extension postgres_fdw drop operator public.=^(int, int)
;
CREATE FUNCTION g_int_union(internal, internal)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE gtest21a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (nullif(a, 0)) STORED NOT NULL)
;
CREATE TABLE fail_part OF mytype
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 SET STORAGE PLAIN
;
CREATE TABLE lcu_table (id INTEGER PRIMARY KEY, value TEXT)
;
CREATE FUNCTION "public"."explain_parallel_append"("text") RETURNS SETOF "text"
    LANGUAGE "plpgsql"
    AS $_$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off, buffers off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+(?:\.\d+)? loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        perform regexp_matches(ln, 'Index Searches: \d+');
        if found then
          continue;
        end if;
        return next ln;
    end loop;
end;
$_$
;
CREATE RULE test_rule_exists AS ON INSERT TO test_exists
    DO INSTEAD
    INSERT INTO test_exists VALUES (NEW.a, NEW.b || NEW.a::text)
;
CREATE TABLE brin_summarize_multi (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
CREATE POLICY p6 ON dump_test.test_table AS RESTRICTIVE
						   USING (false)
;
create index test_ginidx on test using gin (b)
;
CREATE INDEX idx_int8 ON test_int8 USING gin (i)
;
--
-- The int_custom type definition
--
-- This type is identical to int4 in storage, and is used in subsequent
-- tests to have different properties.
--
CREATE TYPE int_custom (
  INPUT = int_custom_in,
  OUTPUT = int_custom_out,
  LIKE = int4
)
;
-- check that dependencies of partition columns are handled correctly
create domain intdom1 as int
;
alter table atref alter column c1 set data type bigint
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26637_2 (tid pg_catalog.tid)
;
CREATE TABLE ruletest_tbl (a int, b int)
;
-- test enabling and disabling
alter event trigger regress_event_trigger disable
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
create table cc3 () inherits (pp1,cc1,cc2)
;
VACUUM pg_catalog.pg_statistic_ext
;
DROP TABLE person, project
;
-- add a primary key constraint
alter table atacc1 add constraint atacc_test1 primary key (test, test2)
;
ALTER TABLE pg_toast.pg_toast_28425 RENAME TO reind_con_toast
;
CREATE TABLE "public"."tableam_parted_heap2" (
    "a" "text",
    "b" integer
)
PARTITION BY LIST ("a")
;
DROP TABLE pg_temp_11.pg_temp_41261_2, pg_temp_11.pg_temp_41261
;
alter table atacc1 rename "........pg.dropped.1........" to x
;
do $$
declare x var_record_colnn := row(1,2);
begin
  raise notice 'x = %', x;
  x := row(null,null);  -- fail
end$$
;
create table idxpart1 partition of idxpart for values from (1) to (1000) partition by range (a)
;
CREATE TABLE upsert_2 PARTITION OF upsert FOR VALUES FROM (100) TO (200)
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=19285 host=/tmp/ZBWsOYky2b dbname=postgres' PUBLICATION pub_all
;
create function part_ins_func() returns trigger language plpgsql as $$
begin
  return new;
end;
$$
;
create table ab_a2 partition of ab for values in(2) partition by list (b)
;
DROP FUNCTION myvarcharsend(myvarchar)
;
CREATE TRIGGER trig_row_before_delete
BEFORE DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] ALTER TYPE test_type2 ALTER ATTRIBUTE b TYPE varchar CASCADE
;
----------
[ADD_INDEX] --
-- GiST (rtree-equivalent opclasses only)
--
CREATE INDEX grect2ind ON fast_emp4000 USING gist (home_base)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1" ATTACH PARTITION "public"."prt1_p1" FOR VALUES FROM (0) TO (250)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION sub1 SET PUBLICATION pub2, pub1
;
----------
[CREATE_TABLE] --
-- additional checks for pg_index_column_has_property
--
CREATE TEMP TABLE foo (f1 int, f2 int, f3 int, f4 int)
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned_5p PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 5) PARTITION BY hash(y)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."notnull_part1_upg" ATTACH PARTITION "public"."notnull_part1_3_upg" FOR VALUES IN (NULL, 5)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."hunspell_long" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_long', afffile = 'hunspell_sample_long' )
;
----------
[CREATE_TABLE] CREATE TABLE "gin_check_text_array"("Column1" text[])
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_is_point(cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltreeparentsel(internal, oid, internal, integer)
RETURNS float8
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."nv_child_2010" INHERIT "public"."nv_parent"
;
----------
[ADMIN_TABLE] vacuum htab2
;
----------
[CREATE_TABLE] -- repeat the test for table with primary key index with included columns
CREATE TABLE foo_1(f1 int, f2 text, f3 text[], primary key (f1,f2) include (f3))
;
----------
[CREATE_TYPE] -- invalid: non-lowercase quoted identifiers
CREATE TYPE case_int42 (
	"Internallength" = 4,
	"Input" = int42_in,
	"Output" = int42_out,
	"Alignment" = int4,
	"Default" = 42,
	"Passedbyvalue"
)
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw drop operator public.=^(int, int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_int_union(internal, internal)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE gtest21a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (nullif(a, 0)) STORED NOT NULL)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part OF mytype
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 SET STORAGE PLAIN
;
----------
[CREATE_TABLE] CREATE TABLE lcu_table (id INTEGER PRIMARY KEY, value TEXT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_parallel_append"("text") RETURNS SETOF "text"
    LANGUAGE "plpgsql"
    AS $_$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off, buffers off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+(?:\.\d+)? loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        perform regexp_matches(ln, 'Index Searches: \d+');
        if found then
          continue;
        end if;
        return next ln;
    end loop;
end;
$_$
;
----------
[CREATE_POLICY] CREATE RULE test_rule_exists AS ON INSERT TO test_exists
    DO INSTEAD
    INSERT INTO test_exists VALUES (NEW.a, NEW.b || NEW.a::text)
;
----------
[CREATE_TABLE] CREATE TABLE brin_summarize_multi (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
----------
[CREATE_POLICY] CREATE POLICY p6 ON dump_test.test_table AS RESTRICTIVE
						   USING (false)
;
----------
[ADD_INDEX] create index test_ginidx on test using gin (b)
;
----------
[ADD_INDEX] CREATE INDEX idx_int8 ON test_int8 USING gin (i)
;
----------
[CREATE_TYPE] --
-- The int_custom type definition
--
-- This type is identical to int4 in storage, and is used in subsequent
-- tests to have different properties.
--
CREATE TYPE int_custom (
  INPUT = int_custom_in,
  OUTPUT = int_custom_out,
  LIKE = int4
)
;
----------
[CREATE_TYPE] -- check that dependencies of partition columns are handled correctly
create domain intdom1 as int
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atref alter column c1 set data type bigint
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26637_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE ruletest_tbl (a int, b int)
;
----------
[ALTER_TRIGGER] -- test enabling and disabling
alter event trigger regress_event_trigger disable
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = issn,
	RIGHTARG = issn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] create table cc3 () inherits (pp1,cc1,cc2)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_statistic_ext
;
----------
[DROP_TABLE] DROP TABLE person, project
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a primary key constraint
alter table atacc1 add constraint atacc_test1 primary key (test, test2)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28425 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tableam_parted_heap2" (
    "a" "text",
    "b" integer
)
PARTITION BY LIST ("a")
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_41261_2, pg_temp_11.pg_temp_41261
;
----------
[RENAME_COLUMN] alter table atacc1 rename "........pg.dropped.1........" to x
;
----------
[BLOCK] do $$
declare x var_record_colnn := row(1,2);
begin
  raise notice 'x = %', x;
  x := row(null,null);  -- fail
end$$
;
----------
[CREATE_TABLE] create table idxpart1 partition of idxpart for values from (1) to (1000) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE upsert_2 PARTITION OF upsert FOR VALUES FROM (100) TO (200)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=19285 host=/tmp/ZBWsOYky2b dbname=postgres' PUBLICATION pub_all
;
----------
[CREATE_PROG_OBJ] create function part_ins_func() returns trigger language plpgsql as $$
begin
  return new;
end;
$$
;
----------
[CREATE_TABLE] create table ab_a2 partition of ab for values in(2) partition by list (b)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION myvarcharsend(myvarchar)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_before_delete
BEFORE DELETE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
