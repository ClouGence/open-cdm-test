CREATE TABLE large(id serial primary key, dataa text, datab text)
;
create table some_tab (a int)
;
create table ab (a int not null, b int not null) partition by list (a)
;
-- verify that attaching and detaching partitions maintains the right set of
-- triggers
create schema fkpart1
  create table pkey (a int primary key)
  create table fk_part (a int) partition by list (a)
  create table fk_part_1 partition of fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 partition of fk_part_1 for values in (1)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_ts_config
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_amproc
;
CREATE TABLE pagg_tab1(x int, y int) PARTITION BY RANGE(x)
;
ALTER TABLE ONLY "public"."test_inh_check_child" INHERIT "public"."test_inh_check"
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c6 SET NOT NULL
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '---')
;
-- ************************************************************
-- * AFTER DELETE on Room
-- *	- delete wall slots in this room
-- ************************************************************
create function tg_room_ad() returns trigger as '
begin
    delete from WSlot where roomno = old.roomno;
    return old;
end;
' language plpgsql
;
DROP TRANSFORM FOR hstore LANGUAGE plperl
;
-- test inheritance

create table dropColumn (a int, b int, e int)
;
DROP TABLE pxtest1
;
CREATE TABLE regress_constr_partitioned (a integer) PARTITION BY LIST (a)
;
create function bad_sql1() returns int as $$
declare a int;
begin
    a := 5;
    Johnny Yuma;
    a := 10;
    return a;
end$$ language plpgsql
;
CREATE AGGREGATE "public"."myaggp10a"(integer) (
    SFUNC = "public"."tfp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
create table attest1 as select * from atacc1
;
-- fail, already exists
CREATE SCHEMA IF NOT EXISTS test_ns_schema_renamed
;
CREATE FUNCTION create_and_insert() RETURNS VOID LANGUAGE sql AS $$
  create table ddl_test (f1 int);
  insert into ddl_test values (1.2);
$$
;
DROP EXTENSION test_extension_exists
;
CREATE FUNCTION dogrant_ok() RETURNS void LANGUAGE sql SECURITY DEFINER AS
	'GRANT regress_priv_group2 TO regress_priv_user5'
;
drop index gist_tbl_multi_index
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "int" WITH "simple"
;
DROP TABLE ruletest_t2
;
ALTER TABLE notnull_tbl3 DROP CONSTRAINT pk
;
alter index idxpart_2_idx attach partition idxpart1_2_idx
;
create extension cube with schema c
;
CREATE TABLE collate_test_fail (
    a int,
    b text COLLATE "ja_JP.eucjp-x-icu"
)
;
CREATE TABLE "public"."mlparted12" (
    "b" integer CONSTRAINT "mlparted1_b_not_null" NOT NULL,
    "a" integer CONSTRAINT "mlparted1_a_not_null" NOT NULL,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
--
-- INT2
--

CREATE TABLE INT2_TBL(f1 int2)
;
create trigger table1_trig
  after insert on table1 referencing new table as new_table
  for each statement execute procedure dump_insert()
;
CREATE TABLE target_pa1 PARTITION OF target_pa FOR VALUES IN (1)
;
-- No-op setting already false hashes and merges to false works
ALTER OPERATOR === (boolean, real) SET (MERGES = false)
;
ALTER INDEX pg_toast.pg_toast_28127_index RENAME TO reind_con_toast_idx
;
create trigger loc2_trig_row_before_insert before insert on loc2
	for each row execute procedure trig_row_before_insupdate()
;
create index on only idxpart (a text_pattern_ops)
;
CREATE SCHEMA tststats
;
CREATE TABLE fk2 PARTITION OF fk FOR VALUES IN (2)
;
ANALYZE t1
;
CREATE TEXT SEARCH DICTIONARY hunspell (
                        Template=ispell,
                        DictFile=ispell_sample,
                        AffFile=hunspell_sample
)
;
create text search dictionary alter1.dict(template = alter1.tmpl)
;
CREATE TABLE tab_stats_crash_discard_test1 AS SELECT generate_series(1,100) AS a
;
CREATE TABLE inh_nn_lvl2 () INHERITS (inh_nn_lvl1)
;
alter table trigtest disable trigger user
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_seclabel
;
CREATE FUNCTION pg_relpages(IN relname regclass)
RETURNS BIGINT
AS '$libdir/pgstattuple', 'pg_relpagesbyid'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE FUNCTION gin_extract_value_macaddr8(macaddr8, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE pg_toast.pg_toast_28210 RENAME TO reind_con_toast
;
CREATE TABLE vacuum_test AS SELECT 42 i
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE large(id serial primary key, dataa text, datab text)
;
----------
[CREATE_TABLE] create table some_tab (a int)
;
----------
[CREATE_TABLE] create table ab (a int not null, b int not null) partition by list (a)
;
----------
[CREATE_SCHEMA] -- verify that attaching and detaching partitions maintains the right set of
-- triggers
create schema fkpart1
  create table pkey (a int primary key)
  create table fk_part (a int) partition by list (a)
  create table fk_part_1 partition of fk_part for values in (1) partition by list (a)
  create table fk_part_1_1 partition of fk_part_1 for values in (1)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_ts_config
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_amproc
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab1(x int, y int) PARTITION BY RANGE(x)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."test_inh_check_child" INHERIT "public"."test_inh_check"
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c6 SET NOT NULL
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '---')
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * AFTER DELETE on Room
-- *	- delete wall slots in this room
-- ************************************************************
create function tg_room_ad() returns trigger as '
begin
    delete from WSlot where roomno = old.roomno;
    return old;
end;
' language plpgsql
;
----------
[DROP_TRANSFORM] DROP TRANSFORM FOR hstore LANGUAGE plperl
;
----------
[CREATE_TABLE] -- test inheritance

create table dropColumn (a int, b int, e int)
;
----------
[DROP_TABLE] DROP TABLE pxtest1
;
----------
[CREATE_TABLE] CREATE TABLE regress_constr_partitioned (a integer) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] create function bad_sql1() returns int as $$
declare a int;
begin
    a := 5;
    Johnny Yuma;
    a := 10;
    return a;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp10a"(integer) (
    SFUNC = "public"."tfp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
----------
[CREATE_TABLE|SELECT] create table attest1 as select * from atacc1
;
----------
[CREATE_SCHEMA] -- fail, already exists
CREATE SCHEMA IF NOT EXISTS test_ns_schema_renamed
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION create_and_insert() RETURNS VOID LANGUAGE sql AS $$
  create table ddl_test (f1 int);
  insert into ddl_test values (1.2);
$$
;
----------
[DROP_LIBRARY] DROP EXTENSION test_extension_exists
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dogrant_ok() RETURNS void LANGUAGE sql SECURITY DEFINER AS
	'GRANT regress_priv_group2 TO regress_priv_user5'
;
----------
[DROP_INDEX] drop index gist_tbl_multi_index
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "int" WITH "simple"
;
----------
[DROP_TABLE] DROP TABLE ruletest_t2
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE notnull_tbl3 DROP CONSTRAINT pk
;
----------
[ALTER_INDEX] alter index idxpart_2_idx attach partition idxpart1_2_idx
;
----------
[CREATE_LIBRARY] create extension cube with schema c
;
----------
[CREATE_TABLE] CREATE TABLE collate_test_fail (
    a int,
    b text COLLATE "ja_JP.eucjp-x-icu"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted12" (
    "b" integer CONSTRAINT "mlparted1_b_not_null" NOT NULL,
    "a" integer CONSTRAINT "mlparted1_a_not_null" NOT NULL,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
----------
[CREATE_TABLE] --
-- INT2
--

CREATE TABLE INT2_TBL(f1 int2)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger table1_trig
  after insert on table1 referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[CREATE_TABLE] CREATE TABLE target_pa1 PARTITION OF target_pa FOR VALUES IN (1)
;
----------
[ALTER_PROG_OBJ] -- No-op setting already false hashes and merges to false works
ALTER OPERATOR === (boolean, real) SET (MERGES = false)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28127_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger loc2_trig_row_before_insert before insert on loc2
	for each row execute procedure trig_row_before_insupdate()
;
----------
[ADD_INDEX] create index on only idxpart (a text_pattern_ops)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA tststats
;
----------
[CREATE_TABLE] CREATE TABLE fk2 PARTITION OF fk FOR VALUES IN (2)
;
----------
[ADMIN_TABLE] ANALYZE t1
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hunspell (
                        Template=ispell,
                        DictFile=ispell_sample,
                        AffFile=hunspell_sample
)
;
----------
[CREATE_POLICY] create text search dictionary alter1.dict(template = alter1.tmpl)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tab_stats_crash_discard_test1 AS SELECT generate_series(1,100) AS a
;
----------
[CREATE_TABLE] CREATE TABLE inh_nn_lvl2 () INHERITS (inh_nn_lvl1)
;
----------
[ALTER_TABLE] alter table trigtest disable trigger user
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_seclabel
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_relpages(IN relname regclass)
RETURNS BIGINT
AS '$libdir/pgstattuple', 'pg_relpagesbyid'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_macaddr8(macaddr8, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28210 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE vacuum_test AS SELECT 42 i
;
