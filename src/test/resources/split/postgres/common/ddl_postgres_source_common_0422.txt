DROP FUNCTION reindex_start_command()
;
drop function nonesuch()
;
--
-- disallow dropping a view's rule (bug #5072)
--

create view rules_fooview as select 'rules_foo'::text
;
-- failed (not aggregate)

ALTER FUNCTION alt_func1(int) RENAME TO alt_func2
;
DROP OPERATOR FAMILY alt_opf9 USING gist
;
CREATE TABLE "public"."mlparted11" (
    "b" integer NOT NULL,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */,
    "........pg.dropped.8........" INTEGER /* dummy */,
    "........pg.dropped.9........" INTEGER /* dummy */
)
;
-- Cleanup
DROP TABLE test
;
alter table atacc1 alter bar drop not null
;
ANALYZE pg_temp_2.pg_temp_28230_2
;
CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest1_1" INHERIT "public"."gtest1"
;
-- Try a partition with an invalid constraint and create a PK on the parent.
CREATE TABLE pp_nn (a int) PARTITION BY HASH (a)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_description
;
CREATE TABLE "public"."shoelace_log" (
    "sl_name" character(10),
    "sl_avail" integer,
    "log_who" "name",
    "log_when" timestamp without time zone
)
;
CREATE FUNCTION is_valid(issn)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP TABLE tableam_parted_heap2
;
CREATE FUNCTION functest_F_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 50'
;
CREATE TABLE fk_notpartitioned_pk (a int, b int, primary key (a, b))
;
DROP VIEW json_array_view
;
-- Common table needed for multiple test scenarios.
CREATE TABLE mintab(c1 int)
;
-- ok
CREATE TABLE test AS SELECT * FROM writetest
;
CREATE TABLE like_datatype_table (
  LIKE datatype_table
  EXCLUDING ALL
)
;
ALTER PUBLICATION testpub_foralltables SET TABLE pub_test.testpub_nopk
;
CREATE TABLE fk_noparted_sn (
	a			int		REFERENCES pk_noparted ON DELETE SET NULL
  )
;
CREATE FOREIGN TABLE foreign_part PARTITION OF temp_parted DEFAULT
  SERVER s0
;
CREATE TABLE xpath_test (id integer NOT NULL, t text)
;
CREATE OR REPLACE FUNCTION ctoast_large_val() RETURNS text LANGUAGE SQL AS $$ select string_agg(encode(sha256(g::text::bytea),'hex'), '')::text from generate_series(1, 133) g $$
;
-- now check same queries using a bulk-loaded index
drop index test_range_spgist_idx
;
CREATE TABLE "generated_stored_tests"."gtest22b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" / 2)) STORED NOT NULL
)
;
CREATE FUNCTION g_intbig_decompress(internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- ************************************************************
-- * BEFORE INSERT or UPDATE on HSlot
-- *	- prevent from manual manipulation
-- *	- set the slotname to HS.hubname.slotno
-- ************************************************************
create function tg_hslot_biu() returns trigger as '
declare
    sname	text;
    xname	HSlot.slotname%TYPE;
    hubrec	record;
begin
    select into hubrec * from Hub where name = new.hubname;
    if not found then
        raise exception ''no manual manipulation of HSlot'';
    end if;
    if new.slotno < 1 or new.slotno > hubrec.nslots then
        raise exception ''no manual manipulation of HSlot'';
    end if;
    if tg_op = ''UPDATE'' and new.hubname != old.hubname then
	if count(*) > 0 from Hub where name = old.hubname then
	    raise exception ''no manual manipulation of HSlot'';
	end if;
    end if;
    sname := ''HS.'' || trim(new.hubname);
    sname := sname || ''.'';
    sname := sname || new.slotno::text;
    if length(sname) > 20 then
        raise exception ''HSlot slotname "%" too long (20 char max)'', sname;
    end if;
    new.slotname := sname;
    return new;
end;
' language plpgsql
;
vacuum ��ٸ����
;
CREATE TABLE itest_err_2 (a int generated always as identity generated by default as identity)
;
ALTER TYPE "public"."insenum" ADD VALUE 'i1'
;
CREATE TABLE toasttest (c char(4096))
;
VACUUM (ANALYZE) pg_catalog.pg_default_acl
;
create table rlp1 partition of rlp for values from (minvalue) to (1)
;
CREATE VIEW v4 AS SELECT * FROM ft4
;
CREATE AGGREGATE max(citext)  (
    SFUNC = citext_larger,
    STYPE = citext,
    SORTOP = >,
    PARALLEL = SAFE,
    COMBINEFUNC = citext_larger
)
;
COMMENT ON COLUMN ft1.c1 IS 'ft1.c1'
;
CREATE UNIQUE INDEX sj_fn_idx ON sj((a * a))
;
/* Single entry with value 'text' */

ALTER TABLE inhx add constraint foo CHECK (xx = 'text')
;
CREATE FUNCTION gbtreekey8_in(cstring)
RETURNS gbtreekey8
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE pg_aggregate ADD PRIMARY KEY USING INDEX pg_aggregate_fnoid_index
;
create table s5.p1 (a int, b int) PARTITION BY list (a)
;
CREATE PUBLICATION testpub_xxx WITH (foo)
;
-- Should fail. Only two arguments required for ALTER OPERATOR FAMILY ... DROP OPERATOR
CREATE OPERATOR FAMILY alt_opf7 USING btree
;
--
-- Hash index / opclass with the = operator
--
CREATE INDEX enumtest_hash ON enumtest USING hash (col)
;
DROP SCHEMA IF EXISTS split_conversion_probe CASCADE
;
-- Create the operator class
CREATE OPERATOR CLASS gist_float4_ops
DEFAULT FOR TYPE float4 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_float4_consistent (internal, float4, int2, oid, internal),
	FUNCTION	2	gbt_float4_union (internal, internal),
	FUNCTION	3	gbt_float4_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_float4_penalty (internal, internal, internal),
	FUNCTION	6	gbt_float4_picksplit (internal, internal),
	FUNCTION	7	gbt_float4_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] DROP FUNCTION reindex_start_command()
;
----------
[DROP_PROG_OBJ] drop function nonesuch()
;
----------
[CREATE_VIEW(SELECT)] --
-- disallow dropping a view's rule (bug #5072)
--

create view rules_fooview as select 'rules_foo'::text
;
----------
[RENAME_PROG_OBJ] -- failed (not aggregate)

ALTER FUNCTION alt_func1(int) RENAME TO alt_func2
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf9 USING gist
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted11" (
    "b" integer NOT NULL,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */,
    "........pg.dropped.8........" INTEGER /* dummy */,
    "........pg.dropped.9........" INTEGER /* dummy */
)
;
----------
[DROP_TABLE] -- Cleanup
DROP TABLE test
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter bar drop not null
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_28230_2
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtest1_1" INHERIT "public"."gtest1"
;
----------
[CREATE_TABLE] -- Try a partition with an invalid constraint and create a PK on the parent.
CREATE TABLE pp_nn (a int) PARTITION BY HASH (a)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_description
;
----------
[CREATE_TABLE] CREATE TABLE "public"."shoelace_log" (
    "sl_name" character(10),
    "sl_avail" integer,
    "log_who" "name",
    "log_when" timestamp without time zone
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION is_valid(issn)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE tableam_parted_heap2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_F_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 50'
;
----------
[CREATE_TABLE] CREATE TABLE fk_notpartitioned_pk (a int, b int, primary key (a, b))
;
----------
[DROP_VIEW] DROP VIEW json_array_view
;
----------
[CREATE_TABLE] -- Common table needed for multiple test scenarios.
CREATE TABLE mintab(c1 int)
;
----------
[CREATE_TABLE|SELECT] -- ok
CREATE TABLE test AS SELECT * FROM writetest
;
----------
[CREATE_TABLE] CREATE TABLE like_datatype_table (
  LIKE datatype_table
  EXCLUDING ALL
)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_foralltables SET TABLE pub_test.testpub_nopk
;
----------
[CREATE_TABLE] CREATE TABLE fk_noparted_sn (
	a			int		REFERENCES pk_noparted ON DELETE SET NULL
  )
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE foreign_part PARTITION OF temp_parted DEFAULT
  SERVER s0
;
----------
[CREATE_TABLE] CREATE TABLE xpath_test (id integer NOT NULL, t text)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION ctoast_large_val() RETURNS text LANGUAGE SQL AS $$ select string_agg(encode(sha256(g::text::bytea),'hex'), '')::text from generate_series(1, 133) g $$
;
----------
[DROP_INDEX] -- now check same queries using a bulk-loaded index
drop index test_range_spgist_idx
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest22b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" / 2)) STORED NOT NULL
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_intbig_decompress(internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * BEFORE INSERT or UPDATE on HSlot
-- *	- prevent from manual manipulation
-- *	- set the slotname to HS.hubname.slotno
-- ************************************************************
create function tg_hslot_biu() returns trigger as '
declare
    sname	text;
    xname	HSlot.slotname%TYPE;
    hubrec	record;
begin
    select into hubrec * from Hub where name = new.hubname;
    if not found then
        raise exception ''no manual manipulation of HSlot'';
    end if;
    if new.slotno < 1 or new.slotno > hubrec.nslots then
        raise exception ''no manual manipulation of HSlot'';
    end if;
    if tg_op = ''UPDATE'' and new.hubname != old.hubname then
	if count(*) > 0 from Hub where name = old.hubname then
	    raise exception ''no manual manipulation of HSlot'';
	end if;
    end if;
    sname := ''HS.'' || trim(new.hubname);
    sname := sname || ''.'';
    sname := sname || new.slotno::text;
    if length(sname) > 20 then
        raise exception ''HSlot slotname "%" too long (20 char max)'', sname;
    end if;
    new.slotname := sname;
    return new;
end;
' language plpgsql
;
----------
[ADMIN_TABLE] vacuum ��ٸ����
;
----------
[CREATE_TABLE] CREATE TABLE itest_err_2 (a int generated always as identity generated by default as identity)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i1'
;
----------
[CREATE_TABLE] CREATE TABLE toasttest (c char(4096))
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] create table rlp1 partition of rlp for values from (minvalue) to (1)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v4 AS SELECT * FROM ft4
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE max(citext)  (
    SFUNC = citext_larger,
    STYPE = citext,
    SORTOP = >,
    PARALLEL = SAFE,
    COMBINEFUNC = citext_larger
)
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN ft1.c1 IS 'ft1.c1'
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX sj_fn_idx ON sj((a * a))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] /* Single entry with value 'text' */

ALTER TABLE inhx add constraint foo CHECK (xx = 'text')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey8_in(cstring)
RETURNS gbtreekey8
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_aggregate ADD PRIMARY KEY USING INDEX pg_aggregate_fnoid_index
;
----------
[CREATE_TABLE] create table s5.p1 (a int, b int) PARTITION BY list (a)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_xxx WITH (foo)
;
----------
[CREATE_PROG_OBJ] -- Should fail. Only two arguments required for ALTER OPERATOR FAMILY ... DROP OPERATOR
CREATE OPERATOR FAMILY alt_opf7 USING btree
;
----------
[ADD_INDEX] --
-- Hash index / opclass with the = operator
--
CREATE INDEX enumtest_hash ON enumtest USING hash (col)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_conversion_probe CASCADE
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_float4_ops
DEFAULT FOR TYPE float4 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_float4_consistent (internal, float4, int2, oid, internal),
	FUNCTION	2	gbt_float4_union (internal, internal),
	FUNCTION	3	gbt_float4_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_float4_penalty (internal, internal, internal),
	FUNCTION	6	gbt_float4_picksplit (internal, internal),
	FUNCTION	7	gbt_float4_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
