GRANT CREATE ON SCHEMA sts_sch2 TO regress_stats_user1
;
SET LOCAL enable_mergejoin = off
;
LOCK TABLE regress_indexing.pk2 IN ACCESS SHARE MODE
;
-- fail
COPY atest5 (one,two) TO stdout
;
COPY public.tab_1 TO STDOUT
;
-- we are OK too if we drop the privileges all at once
REVOKE ALL ON deptest FROM regress_dep_user2
;
--
-- RLS with JOIN
--
SET SESSION AUTHORIZATION regress_rls_alice
;
ALTER TABLE public.prt3_n_p2 OWNER TO zyc
;
LOCK TABLE "public"."prt1_l_p3" IN ACCESS SHARE MODE
;
LOCK TABLE "public"."radix_text_tbl" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[GRANT] GRANT CREATE ON SCHEMA sts_sch2 TO regress_stats_user1
;
----------
[SESSION_SETTING_WRITE] SET LOCAL enable_mergejoin = off
;
----------
[SESSION_LOCK] LOCK TABLE regress_indexing.pk2 IN ACCESS SHARE MODE
;
----------
[DATA_EXPORT] -- fail
COPY atest5 (one,two) TO stdout
;
----------
[DATA_EXPORT] COPY public.tab_1 TO STDOUT
;
----------
[REVOKE] -- we are OK too if we drop the privileges all at once
REVOKE ALL ON deptest FROM regress_dep_user2
;
----------
[SWITCH_USER] --
-- RLS with JOIN
--
SET SESSION AUTHORIZATION regress_rls_alice
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.prt3_n_p2 OWNER TO zyc
;
----------
[SESSION_LOCK] LOCK TABLE "public"."prt1_l_p3" IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."radix_text_tbl" IN ACCESS SHARE MODE
;
