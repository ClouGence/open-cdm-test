create table rtest_t6 (a int4, b text)
;
DROP TABLE inh_temp_child_s1
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_rewrite
;
create function polyf(x anyelement) returns anyrange as $$
  select array[x + 1, x + 2]
$$ language sql
;
ANALYZE public.test_type_diff
;
ANALYZE pg_temp_10.pg_temp_26308
;
CALL ptest6b(1.1, null, null)
;
CREATE TABLE fktable (
	id		INT4 PRIMARY KEY,
	fk		INT4 REFERENCES pktable DEFERRABLE
)
;
CREATE OPERATOR |> (
        PROCEDURE = int8gt,
        LEFTARG = bigint,
        RIGHTARG = bigint,
        NEGATOR = <|,
        COMMUTATOR = <|
)
;
DROP PUBLICATION tap_pub_forall
;
-- now it works
CREATE SUBSCRIPTION regress_testsub4 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false, origin = none)
;
ALTER FUNCTION gbt_bit_union(internal, internal) PARALLEL SAFE
;
-- failed (no role membership)
ALTER TEXT SEARCH DICTIONARY alt_ts_dict3 SET SCHEMA alt_nsp2
;
CREATE TABLE pa_target (tid integer, balance float, val text)
	PARTITION BY LIST (tid)
;
CREATE FUNCTION postgres_fdw_get_connections (OUT server_name text,
    OUT valid boolean)
RETURNS SETOF record
AS '$libdir/postgres_fdw'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE EXTENSION tsm_system_time
;
COMMENT ON FUNCTION cube_gt(cube, cube) IS 'greater than'
;
ALTER TABLE ONLY "public"."b" INHERIT "public"."a"
;
CREATE TABLE few(id int, dataa text, datab text)
;
CREATE TEMP TABLE temptest(col) ON COMMIT DELETE ROWS AS SELECT 1
;
-- not supported by sepgsql
ALTER TABLE regtest_table_part ALTER p SET NOT NULL
;
VACUUM (FREEZE) pg_catalog.pg_ts_dict
;
-- now drop column 'name'
ALTER TABLE alter_table_under_transition_tables
  DROP column name
;
CREATE UNIQUE INDEX CONCURRENTLY concur_index3 ON concur_heap(f2)
;
ANALYZE public.idxpart22
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_db_role_setting
;
ANALYZE public.itest9
;
ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_chk CHECK ((atcol1 <= 3))
;
ALTER TABLE ONLY "public"."part" ATTACH PARTITION "public"."part_rev" FOR VALUES IN (3)
;
ALTER TABLE dump_test.measurement ADD PRIMARY KEY (city_id, logdate)
;
DROP SUBSCRIPTION admin_sub
;
---
-- partitioned table parent
ALTER TABLE regtest_ptable_1 RENAME TO regtest_ptable
;
COMMENT ON FUNCTION cube_lt(cube, cube) IS 'lower than'
;
CREATE TABLE num_exp_div (id1 int4, id2 int4, expected numeric(210,10))
;
-- Inserts an array with 'n' rows to the test table. Pass :next_i as
-- the first argument, returns the new value for :next_i.
create function insert_n(first_i int, n int) returns int language plpgsql as $$
begin
  insert into gin_incomplete_splits values (range_array(first_i, first_i+n));
  return first_i + n;
end;
$$
;
-- should fail
drop type intr_multirange
;
CREATE INDEX alttype_cluster_ind ON public.alttype_cluster USING btree (a)
;
drop function self_ref_trigger_del_func()
;
CREATE TABLE tru_pk_table(id int primary key)
;
-- Two columns, same table
-- Make sure this still works...
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest3,
ptest4) REFERENCES pktable(ptest1, ptest2))
;
ALTER FOREIGN TABLE foreign_schema.ft1 RENAME c1 TO foreign_column_1
;
DO $$BEGIN EXECUTE format(
	'ALTER DATABASE %I SET session_authorization = regress_session_user',
	current_catalog); END$$
;
VACUUM (DISABLE_PAGE_SKIPPING) vaccluster
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_publication_rel
;
analyze j3
;
create temp table zt1 (f1 int primary key)
;
CREATE CAST (isbn13 AS isbn) WITHOUT FUNCTION AS ASSIGNMENT
;
-- streaming test for toast changes
ALTER TABLE stream_test ALTER COLUMN data set storage external
;
ALTER TABLE rls_t1 ENABLE ROW LEVEL SECURITY
;
CLUSTER pg_class USING pg_class_oid_index
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table rtest_t6 (a int4, b text)
;
----------
[DROP_TABLE] DROP TABLE inh_temp_child_s1
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_rewrite
;
----------
[CREATE_PROG_OBJ] create function polyf(x anyelement) returns anyrange as $$
  select array[x + 1, x + 2]
$$ language sql
;
----------
[ADMIN_TABLE] ANALYZE public.test_type_diff
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26308
;
----------
[CALL_PROG_OBJ] CALL ptest6b(1.1, null, null)
;
----------
[CREATE_TABLE] CREATE TABLE fktable (
	id		INT4 PRIMARY KEY,
	fk		INT4 REFERENCES pktable DEFERRABLE
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR |> (
        PROCEDURE = int8gt,
        LEFTARG = bigint,
        RIGHTARG = bigint,
        NEGATOR = <|,
        COMMUTATOR = <|
)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION tap_pub_forall
;
----------
[CREATE_PUB_SUB] -- now it works
CREATE SUBSCRIPTION regress_testsub4 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false, origin = none)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bit_union(internal, internal) PARALLEL SAFE
;
----------
[ALTER_POLICY] -- failed (no role membership)
ALTER TEXT SEARCH DICTIONARY alt_ts_dict3 SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] CREATE TABLE pa_target (tid integer, balance float, val text)
	PARTITION BY LIST (tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION postgres_fdw_get_connections (OUT server_name text,
    OUT valid boolean)
RETURNS SETOF record
AS '$libdir/postgres_fdw'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_LIBRARY] CREATE EXTENSION tsm_system_time
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_gt(cube, cube) IS 'greater than'
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."b" INHERIT "public"."a"
;
----------
[CREATE_TABLE] CREATE TABLE few(id int, dataa text, datab text)
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE temptest(col) ON COMMIT DELETE ROWS AS SELECT 1
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- not supported by sepgsql
ALTER TABLE regtest_table_part ALTER p SET NOT NULL
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_ts_dict
;
----------
[ALTER_TABLE|DROP_COLUMN] -- now drop column 'name'
ALTER TABLE alter_table_under_transition_tables
  DROP column name
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX CONCURRENTLY concur_index3 ON concur_heap(f2)
;
----------
[ADMIN_TABLE] ANALYZE public.idxpart22
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_db_role_setting
;
----------
[ADMIN_TABLE] ANALYZE public.itest9
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_chk CHECK ((atcol1 <= 3))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."part" ATTACH PARTITION "public"."part_rev" FOR VALUES IN (3)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE dump_test.measurement ADD PRIMARY KEY (city_id, logdate)
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION admin_sub
;
----------
[RENAME_TABLE] ---
-- partitioned table parent
ALTER TABLE regtest_ptable_1 RENAME TO regtest_ptable
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_lt(cube, cube) IS 'lower than'
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_div (id1 int4, id2 int4, expected numeric(210,10))
;
----------
[CREATE_PROG_OBJ] -- Inserts an array with 'n' rows to the test table. Pass :next_i as
-- the first argument, returns the new value for :next_i.
create function insert_n(first_i int, n int) returns int language plpgsql as $$
begin
  insert into gin_incomplete_splits values (range_array(first_i, first_i+n));
  return first_i + n;
end;
$$
;
----------
[DROP_TYPE] -- should fail
drop type intr_multirange
;
----------
[ADD_INDEX] CREATE INDEX alttype_cluster_ind ON public.alttype_cluster USING btree (a)
;
----------
[DROP_PROG_OBJ] drop function self_ref_trigger_del_func()
;
----------
[CREATE_TABLE] CREATE TABLE tru_pk_table(id int primary key)
;
----------
[CREATE_TABLE] -- Two columns, same table
-- Make sure this still works...
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest3,
ptest4) REFERENCES pktable(ptest1, ptest2))
;
----------
[RENAME_COLUMN] ALTER FOREIGN TABLE foreign_schema.ft1 RENAME c1 TO foreign_column_1
;
----------
[BLOCK] DO $$BEGIN EXECUTE format(
	'ALTER DATABASE %I SET session_authorization = regress_session_user',
	current_catalog); END$$
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) vaccluster
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_publication_rel
;
----------
[ADMIN_TABLE] analyze j3
;
----------
[CREATE_TABLE] create temp table zt1 (f1 int primary key)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (isbn13 AS isbn) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- streaming test for toast changes
ALTER TABLE stream_test ALTER COLUMN data set storage external
;
----------
[ALTER_TABLE] ALTER TABLE rls_t1 ENABLE ROW LEVEL SECURITY
;
----------
[ADMIN_TABLE] CLUSTER pg_class USING pg_class_oid_index
;
