set local enable_parallel_hash = on
;
BEGIN WORK READ ONLY
;
--
-- The default permissions for functions mean that anyone can execute them.
-- A number of functions shouldn't be executable by just anyone, but rather
-- than use explicit 'superuser()' checks in those functions, we use the GRANT
-- system to REVOKE access to those functions at initdb time.  Administrators
-- can later change who can access these functions, or leave them as only
-- available to superuser / cluster owner, if they choose.
--

REVOKE EXECUTE ON FUNCTION pg_backup_start(text, boolean) FROM public
;
ALTER OPERATOR CLASS alt_opc3 USING hash OWNER TO regress_alter_generic_user2
;
LOCK TABLE "public"."cmmove2" IN ACCESS SHARE MODE
;
LOCK TABLE "public"."comment_test" IN ACCESS SHARE MODE
;
-- no

-- Functions accessible by users with role pg_read_server_files.
GRANT pg_read_server_files TO regress_pg_walinspect
;
-- ERROR
ALTER FOREIGN DATA WRAPPER foo HANDLER test_fdw_handler
;
-- OK

ALTER SERVER alt_fserv1 RENAME TO alt_fserv2
;
EXECUTE dumpOpr('92')
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] set local enable_parallel_hash = on
;
----------
[TRANSACTION] BEGIN WORK READ ONLY
;
----------
[REVOKE] --
-- The default permissions for functions mean that anyone can execute them.
-- A number of functions shouldn't be executable by just anyone, but rather
-- than use explicit 'superuser()' checks in those functions, we use the GRANT
-- system to REVOKE access to those functions at initdb time.  Administrators
-- can later change who can access these functions, or leave them as only
-- available to superuser / cluster owner, if they choose.
--

REVOKE EXECUTE ON FUNCTION pg_backup_start(text, boolean) FROM public
;
----------
[TRANSFER_PRIVILEGE] ALTER OPERATOR CLASS alt_opc3 USING hash OWNER TO regress_alter_generic_user2
;
----------
[SESSION_LOCK] LOCK TABLE "public"."cmmove2" IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."comment_test" IN ACCESS SHARE MODE
;
----------
[GRANT] -- no

-- Functions accessible by users with role pg_read_server_files.
GRANT pg_read_server_files TO regress_pg_walinspect
;
----------
[SYSTEM_SETTING_WRITE] -- ERROR
ALTER FOREIGN DATA WRAPPER foo HANDLER test_fdw_handler
;
----------
[SYSTEM_SETTING_WRITE] -- OK

ALTER SERVER alt_fserv1 RENAME TO alt_fserv2
;
----------
[UNSAFE] EXECUTE dumpOpr('92')
;
