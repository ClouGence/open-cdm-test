CREATE STATISTICS expr_stats_1 (mcv) ON (a+b), (a-b), (2*a), (3*b) FROM expr_stats
;
EXECUTE dumpTableAttach('41491')
;
COPY COPY_TBL FROM '/src/src/test/regress/data/constro.data'
;
COMMIT PREPARED 'c3'
;
DROP FOREIGN DATA WRAPPER postgresql CASCADE
;
-- Admin options

SET SESSION AUTHORIZATION regress_priv_user4
;
-- test aggregate state sharing to ensure it works if one aggregate has a
-- finalfn and the other one has none.
begin work
;
LOCK TABLE "public"."line_tbl" IN ACCESS SHARE MODE
;
SET enable_hashjoin to false
;
EXECUTE dumpDomain('27285')
;
------------------------------------------------------------------------------------------
[ADMIN_PERFORMANCE] CREATE STATISTICS expr_stats_1 (mcv) ON (a+b), (a-b), (2*a), (3*b) FROM expr_stats
;
----------
[UNSAFE] EXECUTE dumpTableAttach('41491')
;
----------
[DATA_IMPORT] COPY COPY_TBL FROM '/src/src/test/regress/data/constro.data'
;
----------
[TRANSACTION] COMMIT PREPARED 'c3'
;
----------
[SYSTEM_SETTING_WRITE] DROP FOREIGN DATA WRAPPER postgresql CASCADE
;
----------
[SWITCH_USER] -- Admin options

SET SESSION AUTHORIZATION regress_priv_user4
;
----------
[TRANSACTION] -- test aggregate state sharing to ensure it works if one aggregate has a
-- finalfn and the other one has none.
begin work
;
----------
[SESSION_LOCK] LOCK TABLE "public"."line_tbl" IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] SET enable_hashjoin to false
;
----------
[UNSAFE] EXECUTE dumpDomain('27285')
;
