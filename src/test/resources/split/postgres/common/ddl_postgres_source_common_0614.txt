create operator = (procedure=bogus_int8_text_eq, leftarg=int8, rightarg=text)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_init_privs
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_default_acl
;
create view alter1.v1 as select * from alter1.t1
;
CREATE TYPE hash_test_t2 AS (a money, b text)
;
CREATE FUNCTION xid_current() RETURNS xid LANGUAGE SQL AS $$SELECT (txid_current() % ((1::int8<<32)))::text::xid;$$
;
CREATE AGGREGATE testagg5(int2) (SFUNC = int2_sum, STYPE = int8)
;
ALTER INDEX pg_toast.pg_toast_27814_index RENAME TO reind_con_toast_idx
;
ALTER TABLE rule_merge1 DISABLE RULE rule1
;
ALTER TABLE test_io_shared SET TABLESPACE regress_tblspace
;
-- lexing of <=, >=, <>, != has a number of edge cases
-- (=> is tested elsewhere)

-- this is legal because ! is not allowed in sql ops
CREATE OPERATOR !=- (
   leftarg = int8,		-- right unary
   procedure = factorial
)
;
create function lock_share(int,int) returns int language sql as
  'select 1 - lock_share($1)' parallel safe
;
CREATE TABLE testschema.dflt (a int PRIMARY KEY) PARTITION BY LIST (a) TABLESPACE pg_default
;
CREATE TABLE trgfire1 PARTITION OF trgfire FOR VALUES FROM (1) TO (10)
;
-- legacy definition for compatibility with 9.3
CREATE OR REPLACE FUNCTION
  json_populate_record(base anyelement, from_json json, use_json_as_text boolean DEFAULT false)
  RETURNS anyelement LANGUAGE internal STABLE AS 'json_populate_record' PARALLEL SAFE
;
-- fail - already added
ALTER PUBLICATION testpub_fortbl ADD TABLE testpub_tbl1
;
create table boolp_t partition of boolp for values in('t')
;
CREATE TYPE jbpop AS (a text, b int, c timestamp)
;
create table inh_fk_2_child () inherits (inh_fk_2)
;
--GIN

CREATE FUNCTION ginint4_queryextract(_int4, internal, int2, internal, internal, internal, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create view rw_view3 as select b as bb, a as aa from base_tbl
;
create index test_mulrirange_gist_idx on test_multirange_gist using gist (mr)
;
alter table parted_copytest attach partition parted_copytest_a2 for values in(2)
;
create function namedparmcursor_test8() returns int4 as $$
declare
  c1 cursor (p1 int, p2 int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2;
  n int4;
begin
  open c1 (77 -- test
  , 42);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
-- This simpler variant of the above test has been observed to fail differently

create table events (event_id int primary key)
;
CREATE INDEX test_tab_b_idx ON testschema.test_tab USING btree (b)
;
DROP TABLE arrlp
;
ALTER TABLE regtest_ptable DROP COLUMN q CASCADE
;
-- fail

DROP TYPE myvarchar CASCADE
;
create table rlp_default_default partition of rlp_default default
;
VACUUM FREEZE indtoasttest
;
drop function lock_share(int,int)
;
ALTER TEXT SEARCH CONFIGURATION turkish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
create function f1(x anycompatiblerange, y anycompatiblearray) returns anycompatiblerange as $$
begin
  return x;
end$$ language plpgsql
;
ALTER TYPE "public"."planets" ADD VALUE 'saturn'
;
drop table tab_part_attach
;
ALTER TABLE pg_toast.pg_toast_33236 RENAME TO reind_con_toast
;
DROP OPERATOR ~ (seg, seg)
;
/*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, binary = foo)
;
CREATE TABLE "public"."plt1_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE FOREIGN TABLE t0 (a int) SERVER s0
;
create table hp_prefix_test (a int, b int, c int, d int)
  partition by hash (a part_test_int4_ops, b part_test_int4_ops, c part_test_int4_ops, d part_test_int4_ops)
;
CREATE TEMP TABLE tt (num2 int, value text)
;
-- not supported by sepgsql
ALTER TABLE regtest_table_part DROP CONSTRAINT test_ck
;
CREATE TEMP TABLE jsonb_table_test (js jsonb)
;
ALTER COLLATION test11 SET SCHEMA test_schema
;
CREATE TRIGGER after_del_stmt_trig AFTER DELETE ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('after_view_del_stmt')
;
cluster attbl using pk_attbl
;
CREATE FOREIGN TABLE ft1_nopw (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10) default 'ft1',
	c8 user_enum
) SERVER loopback_nopw OPTIONS (schema_name 'public', table_name 'ft1')
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create operator = (procedure=bogus_int8_text_eq, leftarg=int8, rightarg=text)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_init_privs
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_default_acl
;
----------
[CREATE_VIEW(SELECT)] create view alter1.v1 as select * from alter1.t1
;
----------
[CREATE_TYPE] CREATE TYPE hash_test_t2 AS (a money, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION xid_current() RETURNS xid LANGUAGE SQL AS $$SELECT (txid_current() % ((1::int8<<32)))::text::xid;$$
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testagg5(int2) (SFUNC = int2_sum, STYPE = int8)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27814_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE] ALTER TABLE rule_merge1 DISABLE RULE rule1
;
----------
[ALTER_TABLE] ALTER TABLE test_io_shared SET TABLESPACE regress_tblspace
;
----------
[CREATE_PROG_OBJ] -- lexing of <=, >=, <>, != has a number of edge cases
-- (=> is tested elsewhere)

-- this is legal because ! is not allowed in sql ops
CREATE OPERATOR !=- (
   leftarg = int8,		-- right unary
   procedure = factorial
)
;
----------
[CREATE_PROG_OBJ] create function lock_share(int,int) returns int language sql as
  'select 1 - lock_share($1)' parallel safe
;
----------
[CREATE_TABLE] CREATE TABLE testschema.dflt (a int PRIMARY KEY) PARTITION BY LIST (a) TABLESPACE pg_default
;
----------
[CREATE_TABLE] CREATE TABLE trgfire1 PARTITION OF trgfire FOR VALUES FROM (1) TO (10)
;
----------
[CREATE_PROG_OBJ] -- legacy definition for compatibility with 9.3
CREATE OR REPLACE FUNCTION
  json_populate_record(base anyelement, from_json json, use_json_as_text boolean DEFAULT false)
  RETURNS anyelement LANGUAGE internal STABLE AS 'json_populate_record' PARALLEL SAFE
;
----------
[ALTER_PUB_SUB] -- fail - already added
ALTER PUBLICATION testpub_fortbl ADD TABLE testpub_tbl1
;
----------
[CREATE_TABLE] create table boolp_t partition of boolp for values in('t')
;
----------
[CREATE_TYPE] CREATE TYPE jbpop AS (a text, b int, c timestamp)
;
----------
[CREATE_TABLE] create table inh_fk_2_child () inherits (inh_fk_2)
;
----------
[CREATE_PROG_OBJ] --GIN

CREATE FUNCTION ginint4_queryextract(_int4, internal, int2, internal, internal, internal, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] create view rw_view3 as select b as bb, a as aa from base_tbl
;
----------
[ADD_INDEX] create index test_mulrirange_gist_idx on test_multirange_gist using gist (mr)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_copytest attach partition parted_copytest_a2 for values in(2)
;
----------
[CREATE_PROG_OBJ] create function namedparmcursor_test8() returns int4 as $$
declare
  c1 cursor (p1 int, p2 int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2;
  n int4;
begin
  open c1 (77 -- test
  , 42);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
----------
[CREATE_TABLE] -- This simpler variant of the above test has been observed to fail differently

create table events (event_id int primary key)
;
----------
[ADD_INDEX] CREATE INDEX test_tab_b_idx ON testschema.test_tab USING btree (b)
;
----------
[DROP_TABLE] DROP TABLE arrlp
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE regtest_ptable DROP COLUMN q CASCADE
;
----------
[DROP_TYPE] -- fail

DROP TYPE myvarchar CASCADE
;
----------
[CREATE_TABLE] create table rlp_default_default partition of rlp_default default
;
----------
[ADMIN_TABLE] VACUUM FREEZE indtoasttest
;
----------
[DROP_PROG_OBJ] drop function lock_share(int,int)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION turkish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_PROG_OBJ] create function f1(x anycompatiblerange, y anycompatiblearray) returns anycompatiblerange as $$
begin
  return x;
end$$ language plpgsql
;
----------
[ALTER_TYPE] ALTER TYPE "public"."planets" ADD VALUE 'saturn'
;
----------
[DROP_TABLE] drop table tab_part_attach
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33236 RENAME TO reind_con_toast
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ~ (seg, seg)
;
----------
[CREATE_POLICY] /*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, binary = foo)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt1_p2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t0 (a int) SERVER s0
;
----------
[CREATE_TABLE] create table hp_prefix_test (a int, b int, c int, d int)
  partition by hash (a part_test_int4_ops, b part_test_int4_ops, c part_test_int4_ops, d part_test_int4_ops)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tt (num2 int, value text)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- not supported by sepgsql
ALTER TABLE regtest_table_part DROP CONSTRAINT test_ck
;
----------
[CREATE_TABLE] CREATE TEMP TABLE jsonb_table_test (js jsonb)
;
----------
[ALTER_POLICY] ALTER COLLATION test11 SET SCHEMA test_schema
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER after_del_stmt_trig AFTER DELETE ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('after_view_del_stmt')
;
----------
[ADMIN_TABLE] cluster attbl using pk_attbl
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft1_nopw (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10) default 'ft1',
	c8 user_enum
) SERVER loopback_nopw OPTIONS (schema_name 'public', table_name 'ft1')
;
