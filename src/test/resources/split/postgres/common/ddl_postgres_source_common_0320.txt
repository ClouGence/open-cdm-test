CREATE FUNCTION "public"."large_val"() RETURNS "text"
    LANGUAGE "sql"
    AS $$select array_agg(fipshash(g::text))::text from generate_series(1, 256) g$$
;
create table idxpart3 partition of idxpart for values from (2000) to (3000)
;
create table mlparted_tab_part2b partition of mlparted_tab_part2 for values in ('b')
;
ALTER FOREIGN TABLE ft1 ALTER CONSTRAINT ft1_c9_check DEFERRABLE
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_shdescription
;
--
--  The type itself.
--

CREATE TYPE citext (
    INPUT          = citextin,
    OUTPUT         = citextout,
    RECEIVE        = citextrecv,
    SEND           = citextsend,
    INTERNALLENGTH = VARIABLE,
    STORAGE        = extended,
    -- make it a non-preferred member of string type category
    CATEGORY       = 'S',
    PREFERRED      = false,
    COLLATABLE     = true
)
;
CREATE SEQUENCE "public"."transition_table_level2_level2_no_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
-- we've also seen issues with check constraints being validated at the wrong
-- time when there's a pending table rewrite.
create table atacc1 (a bigint, b int)
;
CREATE VIEW pg_replication_slots AS
    SELECT
            L.slot_name,
            L.plugin,
            L.slot_type,
            L.datoid,
            D.datname AS database,
            L.temporary,
            L.active,
            L.active_pid,
            L.xmin,
            L.catalog_xmin,
            L.restart_lsn,
            L.confirmed_flush_lsn,
            L.wal_status,
            L.safe_wal_size,
            L.two_phase
    FROM pg_get_replication_slots() AS L
            LEFT JOIN pg_database D ON (L.datoid = D.oid)
;
CREATE FUNCTION test_dsa_resowners()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
DROP TABLE gtest_normal, gtest_normal_child
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_server
;
DROP TABLE tbl1_part
;
-- ensure dependencies are straight
DROP FUNCTION myvarcharsend(myvarchar)
;
CREATE FUNCTION _ltree_gist_options(internal)
RETURNS void
AS '$libdir/ltree', '_ltree_gist_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
-- via a VIEW
CREATE TEMPORARY VIEW vsubdepartment AS
	WITH RECURSIVE subdepartment AS
	(
		 -- non recursive term
		SELECT * FROM department WHERE name = 'A'
		UNION ALL
		-- recursive term
		SELECT d.* FROM department AS d, subdepartment AS sd
			WHERE d.parent_department = sd.id
	)
	SELECT * FROM subdepartment
;
alter event trigger regress_event_trigger rename to regress_event_trigger3
;
CREATE TABLE chartmp (a char(32))
;
create or replace function stacked_diagnostics_test() returns void as $$
declare _detail text;
        _hint text;
        _message text;
begin
  perform raise_test();
exception when others then
  get stacked diagnostics
        _message = message_text,
        _detail = pg_exception_detail,
        _hint = pg_exception_hint;
  raise notice 'message: %, detail: %, hint: %', _message, _detail, _hint;
end;
$$ language plpgsql
;
CREATE TRIGGER insert_measurement_trigger
    BEFORE INSERT ON measurement
    FOR EACH ROW EXECUTE PROCEDURE measurement_insert_trigger()
;
-- check multi-column range partitioning with minvalue/maxvalue constraints
create table mcrparted (a text, b int) partition by range(a, b)
;
CREATE OR REPLACE FUNCTION test_slru_page_sync(int) RETURNS VOID
  AS 'MODULE_PATHNAME', 'test_slru_page_sync' LANGUAGE C
;
-- the generic crosstab function:
CREATE FUNCTION crosstab(text)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
CREATE FUNCTION gbt_bit_same(gbtreekey_var, gbtreekey_var, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (80) TO (90)
;
DROP TABLE concur_reindex_part
;
CREATE TRIGGER rw_view1_ins_trig INSTEAD OF INSERT ON rw_view1
  FOR EACH ROW EXECUTE PROCEDURE rw_view1_trig_fn()
;
ALTER OPERATOR FAMILY gist_bpchar_ops USING gist ADD
	FUNCTION	11  (bpchar, bpchar) gbt_bpchar_sortsupport (internal)
;
VACUUM FULL concur_heap
;
ALTER TABLE pitest1 ALTER COLUMN f3 ADD GENERATED BY DEFAULT AS IDENTITY
;
ANALYZE pg_temp_4.pg_temp_37830
;
CREATE TABLE fast_emp4000 (
	home_base	 box
)
;
CREATE TABLE num_exp_sqrt (id int4, expected numeric(210,10))
;
ALTER INDEX "public"."idxpart2_a_idx" ATTACH PARTITION "public"."idxpart22_a_idx"
;
CREATE TABLE T (pk INT NOT NULL PRIMARY KEY)
;
CREATE TABLE ctas_stats_1 AS SELECT 2 AS a
;
CREATE TABLESPACE tblspc2 LOCATION '/tmp/54tbCZ2rt3/tempdir/tbl=spc2'
;
create function trap_matching_test(int) returns int as $$
declare x int;
	sx smallint;
	y int;
begin
	begin	-- start a subtransaction
		x := 100 / $1;
		sx := $1;
		select into y data from match_source where id =
			(select id from match_source b where ten = $1);
	exception
		when data_exception then  -- category match
			raise notice 'caught data_exception';
			x := -1;
		when NUMERIC_VALUE_OUT_OF_RANGE OR CARDINALITY_VIOLATION then
			raise notice 'caught numeric_value_out_of_range or cardinality_violation';
			x := -2;
	end;
	return x;
end$$ language plpgsql
;
CREATE TABLE "public"."polygon_tbl" (
    "f1" "polygon"
)
;
ALTER TABLE tbl2 ALTER COLUMN val2 TYPE boolean
;
CREATE TABLE newly_indexed (c int)
;
-- Create some partitioned indexes
CREATE INDEX concur_reindex_part_index ON ONLY concur_reindex_part (c1)
;
ALTER TABLE temporal_fk_rng2rng
  ALTER COLUMN valid_at TYPE daterange USING daterange(lower(valid_at)::date, upper(valid_at)::date)
;
ALTER TABLE fd_pt2_1 ADD c4 char
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE TABLE selinto_schema.tbl_withdata2 (a) AS
  SELECT generate_series(1,3) WITH DATA
;
-- Test in_range for other numeric datatypes

create temp table numerics(
    id int,
    f_float4 float4,
    f_float8 float8,
    f_numeric numeric
)
;
VACUUM pg_catalog.pg_index
;
CREATE DATABASE "regression" TEMPLATE=template0
;
CREATE VIEW pg_timezone_abbrevs AS
    SELECT * FROM pg_timezone_abbrevs_zone() z
    UNION ALL
    (SELECT * FROM pg_timezone_abbrevs_abbrevs() a
     WHERE NOT EXISTS (SELECT 1 FROM pg_timezone_abbrevs_zone() z2
                       WHERE z2.abbrev = a.abbrev))
    ORDER BY abbrev
;
VACUUM pg_catalog.pg_attrdef
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."large_val"() RETURNS "text"
    LANGUAGE "sql"
    AS $$select array_agg(fipshash(g::text))::text from generate_series(1, 256) g$$
;
----------
[CREATE_TABLE] create table idxpart3 partition of idxpart for values from (2000) to (3000)
;
----------
[CREATE_TABLE] create table mlparted_tab_part2b partition of mlparted_tab_part2 for values in ('b')
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER FOREIGN TABLE ft1 ALTER CONSTRAINT ft1_c9_check DEFERRABLE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_shdescription
;
----------
[CREATE_TYPE] --
--  The type itself.
--

CREATE TYPE citext (
    INPUT          = citextin,
    OUTPUT         = citextout,
    RECEIVE        = citextrecv,
    SEND           = citextsend,
    INTERNALLENGTH = VARIABLE,
    STORAGE        = extended,
    -- make it a non-preferred member of string type category
    CATEGORY       = 'S',
    PREFERRED      = false,
    COLLATABLE     = true
)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."transition_table_level2_level2_no_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_TABLE] -- we've also seen issues with check constraints being validated at the wrong
-- time when there's a pending table rewrite.
create table atacc1 (a bigint, b int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_replication_slots AS
    SELECT
            L.slot_name,
            L.plugin,
            L.slot_type,
            L.datoid,
            D.datname AS database,
            L.temporary,
            L.active,
            L.active_pid,
            L.xmin,
            L.catalog_xmin,
            L.restart_lsn,
            L.confirmed_flush_lsn,
            L.wal_status,
            L.safe_wal_size,
            L.two_phase
    FROM pg_get_replication_slots() AS L
            LEFT JOIN pg_database D ON (L.datoid = D.oid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_dsa_resowners()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[DROP_TABLE] DROP TABLE gtest_normal, gtest_normal_child
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_server
;
----------
[DROP_TABLE] DROP TABLE tbl1_part
;
----------
[DROP_PROG_OBJ] -- ensure dependencies are straight
DROP FUNCTION myvarcharsend(myvarchar)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_gist_options(internal)
RETURNS void
AS '$libdir/ltree', '_ltree_gist_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] -- via a VIEW
CREATE TEMPORARY VIEW vsubdepartment AS
	WITH RECURSIVE subdepartment AS
	(
		 -- non recursive term
		SELECT * FROM department WHERE name = 'A'
		UNION ALL
		-- recursive term
		SELECT d.* FROM department AS d, subdepartment AS sd
			WHERE d.parent_department = sd.id
	)
	SELECT * FROM subdepartment
;
----------
[RENAME_TRIGGER] alter event trigger regress_event_trigger rename to regress_event_trigger3
;
----------
[CREATE_TABLE] CREATE TABLE chartmp (a char(32))
;
----------
[CREATE_PROG_OBJ] create or replace function stacked_diagnostics_test() returns void as $$
declare _detail text;
        _hint text;
        _message text;
begin
  perform raise_test();
exception when others then
  get stacked diagnostics
        _message = message_text,
        _detail = pg_exception_detail,
        _hint = pg_exception_hint;
  raise notice 'message: %, detail: %, hint: %', _message, _detail, _hint;
end;
$$ language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER insert_measurement_trigger
    BEFORE INSERT ON measurement
    FOR EACH ROW EXECUTE PROCEDURE measurement_insert_trigger()
;
----------
[CREATE_TABLE] -- check multi-column range partitioning with minvalue/maxvalue constraints
create table mcrparted (a text, b int) partition by range(a, b)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_sync(int) RETURNS VOID
  AS 'MODULE_PATHNAME', 'test_slru_page_sync' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] -- the generic crosstab function:
CREATE FUNCTION crosstab(text)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bit_same(gbtreekey_var, gbtreekey_var, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (80) TO (90)
;
----------
[DROP_TABLE] DROP TABLE concur_reindex_part
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER rw_view1_ins_trig INSTEAD OF INSERT ON rw_view1
  FOR EACH ROW EXECUTE PROCEDURE rw_view1_trig_fn()
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bpchar_ops USING gist ADD
	FUNCTION	11  (bpchar, bpchar) gbt_bpchar_sortsupport (internal)
;
----------
[ADMIN_TABLE] VACUUM FULL concur_heap
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest1 ALTER COLUMN f3 ADD GENERATED BY DEFAULT AS IDENTITY
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37830
;
----------
[CREATE_TABLE] CREATE TABLE fast_emp4000 (
	home_base	 box
)
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_sqrt (id int4, expected numeric(210,10))
;
----------
[ALTER_INDEX] ALTER INDEX "public"."idxpart2_a_idx" ATTACH PARTITION "public"."idxpart22_a_idx"
;
----------
[CREATE_TABLE] CREATE TABLE T (pk INT NOT NULL PRIMARY KEY)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_stats_1 AS SELECT 2 AS a
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc2 LOCATION '/tmp/54tbCZ2rt3/tempdir/tbl=spc2'
;
----------
[CREATE_PROG_OBJ] create function trap_matching_test(int) returns int as $$
declare x int;
	sx smallint;
	y int;
begin
	begin	-- start a subtransaction
		x := 100 / $1;
		sx := $1;
		select into y data from match_source where id =
			(select id from match_source b where ten = $1);
	exception
		when data_exception then  -- category match
			raise notice 'caught data_exception';
			x := -1;
		when NUMERIC_VALUE_OUT_OF_RANGE OR CARDINALITY_VIOLATION then
			raise notice 'caught numeric_value_out_of_range or cardinality_violation';
			x := -2;
	end;
	return x;
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."polygon_tbl" (
    "f1" "polygon"
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl2 ALTER COLUMN val2 TYPE boolean
;
----------
[CREATE_TABLE] CREATE TABLE newly_indexed (c int)
;
----------
[ADD_INDEX] -- Create some partitioned indexes
CREATE INDEX concur_reindex_part_index ON ONLY concur_reindex_part (c1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE temporal_fk_rng2rng
  ALTER COLUMN valid_at TYPE daterange USING daterange(lower(valid_at)::date, upper(valid_at)::date)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE fd_pt2_1 ADD c4 char
;
----------
[CREATE_TABLE|SELECT] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE TABLE selinto_schema.tbl_withdata2 (a) AS
  SELECT generate_series(1,3) WITH DATA
;
----------
[CREATE_TABLE] -- Test in_range for other numeric datatypes

create temp table numerics(
    id int,
    f_float4 float4,
    f_float8 float8,
    f_numeric numeric
)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_index
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression" TEMPLATE=template0
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_timezone_abbrevs AS
    SELECT * FROM pg_timezone_abbrevs_zone() z
    UNION ALL
    (SELECT * FROM pg_timezone_abbrevs_abbrevs() a
     WHERE NOT EXISTS (SELECT 1 FROM pg_timezone_abbrevs_zone() z2
                       WHERE z2.abbrev = a.abbrev))
    ORDER BY abbrev
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_attrdef
;
