-- renaming constraint should rename index as well
ALTER TABLE onek ADD CONSTRAINT onek_unique1_constraint UNIQUE (unique1)
;
CREATE TABLE test_tab (a int primary key, b text, c INT, d INT, e INT, f INT)
;
VACUUM (ANALYZE) pg_catalog.pg_pltemplate
;
--    N    P        N    N
-- should CREATE
CREATE AGGREGATE myaggn09a(BASETYPE = int, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
create table child (c text collate "POSIX") inherits (parent)
;
ALTER DOMAIN domain_stats SET DEFAULT '3'
;
create table rp_prefix_test1_p2 partition of rp_prefix_test1 for values from (2, 'a') to (2, 'b')
;
CREATE AGGREGATE "public"."cleast_agg"(VARIADIC "items" "anycompatiblearray") (
    SFUNC = "public"."cleast_accum",
    STYPE = "anycompatible"
)
;
CREATE TABLE inhg (LIKE inhx)
;
CREATE INDEX "alter_idx_rename_test_parted_idx_3" ON ONLY "public"."alter_idx_rename_test_parted_2" USING "btree" ("a")
;
create table tt5 (a int, b int)
;
CREATE TABLE TBL2 (i int4)
;
CREATE VIEW pg_stat_wal AS
    SELECT
        w.wal_records,
        w.wal_fpi,
        w.wal_bytes,
        w.wal_buffers_full,
        w.stats_reset
    FROM pg_stat_get_wal() w
;
CREATE TABLE "public"."rtest_comp" (
    "part" "text",
    "unit" character(4),
    "size" double precision
)
;
create index on only idxpart1 (a)
;
ALTER TABLE atnnpart1 ADD PRIMARY KEY (id)
;
drop table table_with_oids
;
drop function at_test_sql_partop
;
ALTER TABLE ONLY "public"."rule_and_refint_t3"
    ADD CONSTRAINT "rule_and_refint_t3_pkey" PRIMARY KEY ("id3a", "id3b", "id3c")
;
-- corresponding toast table should not have label and permission checks
ALTER TABLE regtest_table_2 ADD COLUMN b text
;
CREATE DOMAIN seclabel_domain AS text
;
-- Test DELETE/UPDATE/MERGE on a partitioned table when all partitions
-- are excluded and only the dummy root result relation remains. The
-- operation is a no-op but should not fail regardless of whether the
-- foreign child was processed (pruning off) or not (pruning on).
DROP TABLE p2
;
CREATE TABLE testns.acltest1 (x int)
;
ALTER TYPE "public"."test_type3" DROP ATTRIBUTE "........pg.dropped.1........"
;
CREATE TABLE a (i int)
;
ALTER INDEX part_attmp1_pkey RENAME TO part_attmp1_index
;
create table parted_3 partition of parted for values in (3)
;
CREATE PROCEDURE "public"."ptest3"("y" "text")
    LANGUAGE "sql"
    AS $_$
CALL ptest1(y);
CALL ptest1($1);
$_$
;
--
--
--
-- macaddr ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_macad_consistent(internal,macaddr,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE join_tbl (a1 int, b1 int, c1 text, a2 int, b2 int, c2 text)
;
/* src/test/modules/test_ddl_deparse/test_ddl_deparse--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION get_command_type(pg_ddl_command)
  RETURNS text IMMUTABLE STRICT
  AS '$libdir/test_ddl_deparse' LANGUAGE C
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
DROP SCHEMA fast_default
;
create table at_tab1 (a int, b text) partition by list(a)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_attrdef
;
CREATE OPERATOR CLASS "public"."part_test_text_ops"
    FOR TYPE "text" USING "hash" FAMILY "public"."part_test_text_ops" AS
    OPERATOR 1 =("text","text")
;
create table part_ee_ff3_2 partition of part_ee_ff3 for values from (25) to (30)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_namespace
;
alter extension test_ext7 add sequence old_table1_col1_seq
;
CREATE TABLE tab3_1 (c text DEFAULT 'sub1_tab3_1', b text, a int NOT NULL PRIMARY KEY)
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26350_2 (tid pg_catalog.tid)
;
CREATE FUNCTION psql_error(msg TEXT) RETURNS BOOLEAN AS $$
  BEGIN
    RAISE EXCEPTION 'error %', msg;
  END;
$$ LANGUAGE plpgsql
;
--
-- LINE
-- Infinite lines
--

--DROP TABLE LINE_TBL;
CREATE TABLE LINE_TBL (s line)
;
ALTER TABLE pg_temp_9.pg_temp_38106_2 ADD COLUMN newdata pg_temp_9.pg_temp_38106
;
ALTER OPERATOR FAMILY alt_opf12 USING btree ADD FUNCTION 1 fn_opf12(int4, int2)
;
vacuum pgbench_branches
;
CREATE FUNCTION test2dom(fn text) RETURNS hstore_foo
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
return {'a': 1, fn: 'boo', 'c': None}
$$
;
CREATE VIEW "public"."shoelace_obsolete" AS
 SELECT "shoelace"."sl_name",
    "shoelace"."sl_avail",
    "shoelace"."sl_color",
    "shoelace"."sl_len",
    "shoelace"."sl_unit",
    "shoelace"."sl_len_cm"
   FROM "public"."shoelace"
  WHERE (NOT (EXISTS ( SELECT "shoe"."shoename"
           FROM "public"."shoe"
          WHERE ("shoe"."slcolor" = "shoelace"."sl_color"))))
;
CREATE INDEX t1_hash ON s4.t1 USING HASH (i)
;
CREATE TABLE pagg_tab_m_p2 PARTITION OF pagg_tab_m FOR VALUES FROM (10, 10) TO (20, 20)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] -- renaming constraint should rename index as well
ALTER TABLE onek ADD CONSTRAINT onek_unique1_constraint UNIQUE (unique1)
;
----------
[CREATE_TABLE] CREATE TABLE test_tab (a int primary key, b text, c INT, d INT, e INT, f INT)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_pltemplate
;
----------
[CREATE_PROG_OBJ] --    N    P        N    N
-- should CREATE
CREATE AGGREGATE myaggn09a(BASETYPE = int, SFUNC = tf1p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_TABLE] create table child (c text collate "POSIX") inherits (parent)
;
----------
[ALTER_TYPE] ALTER DOMAIN domain_stats SET DEFAULT '3'
;
----------
[CREATE_TABLE] create table rp_prefix_test1_p2 partition of rp_prefix_test1 for values from (2, 'a') to (2, 'b')
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."cleast_agg"(VARIADIC "items" "anycompatiblearray") (
    SFUNC = "public"."cleast_accum",
    STYPE = "anycompatible"
)
;
----------
[CREATE_TABLE] CREATE TABLE inhg (LIKE inhx)
;
----------
[ADD_INDEX] CREATE INDEX "alter_idx_rename_test_parted_idx_3" ON ONLY "public"."alter_idx_rename_test_parted_2" USING "btree" ("a")
;
----------
[CREATE_TABLE] create table tt5 (a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE TBL2 (i int4)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_wal AS
    SELECT
        w.wal_records,
        w.wal_fpi,
        w.wal_bytes,
        w.wal_buffers_full,
        w.stats_reset
    FROM pg_stat_get_wal() w
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_comp" (
    "part" "text",
    "unit" character(4),
    "size" double precision
)
;
----------
[ADD_INDEX] create index on only idxpart1 (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE atnnpart1 ADD PRIMARY KEY (id)
;
----------
[DROP_TABLE] drop table table_with_oids
;
----------
[DROP_PROG_OBJ] drop function at_test_sql_partop
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."rule_and_refint_t3"
    ADD CONSTRAINT "rule_and_refint_t3_pkey" PRIMARY KEY ("id3a", "id3b", "id3c")
;
----------
[ALTER_TABLE|ADD_COLUMN] -- corresponding toast table should not have label and permission checks
ALTER TABLE regtest_table_2 ADD COLUMN b text
;
----------
[CREATE_TYPE] CREATE DOMAIN seclabel_domain AS text
;
----------
[DROP_TABLE] -- Test DELETE/UPDATE/MERGE on a partitioned table when all partitions
-- are excluded and only the dummy root result relation remains. The
-- operation is a no-op but should not fail regardless of whether the
-- foreign child was processed (pruning off) or not (pruning on).
DROP TABLE p2
;
----------
[CREATE_TABLE] CREATE TABLE testns.acltest1 (x int)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."test_type3" DROP ATTRIBUTE "........pg.dropped.1........"
;
----------
[CREATE_TABLE] CREATE TABLE a (i int)
;
----------
[RENAME_INDEX] ALTER INDEX part_attmp1_pkey RENAME TO part_attmp1_index
;
----------
[CREATE_TABLE] create table parted_3 partition of parted for values in (3)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest3"("y" "text")
    LANGUAGE "sql"
    AS $_$
CALL ptest1(y);
CALL ptest1($1);
$_$
;
----------
[CREATE_PROG_OBJ] --
--
--
-- macaddr ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_macad_consistent(internal,macaddr,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE join_tbl (a1 int, b1 int, c1 text, a2 int, b2 int, c2 text)
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_ddl_deparse/test_ddl_deparse--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION get_command_type(pg_ddl_command)
  RETURNS text IMMUTABLE STRICT
  AS '$libdir/test_ddl_deparse' LANGUAGE C
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
----------
[DROP_SCHEMA] DROP SCHEMA fast_default
;
----------
[CREATE_TABLE] create table at_tab1 (a int, b text) partition by list(a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_attrdef
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS "public"."part_test_text_ops"
    FOR TYPE "text" USING "hash" FAMILY "public"."part_test_text_ops" AS
    OPERATOR 1 =("text","text")
;
----------
[CREATE_TABLE] create table part_ee_ff3_2 partition of part_ee_ff3 for values from (25) to (30)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_namespace
;
----------
[ALTER_LIBRARY] alter extension test_ext7 add sequence old_table1_col1_seq
;
----------
[CREATE_TABLE] CREATE TABLE tab3_1 (c text DEFAULT 'sub1_tab3_1', b text, a int NOT NULL PRIMARY KEY)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26350_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION psql_error(msg TEXT) RETURNS BOOLEAN AS $$
  BEGIN
    RAISE EXCEPTION 'error %', msg;
  END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] --
-- LINE
-- Infinite lines
--

--DROP TABLE LINE_TBL;
CREATE TABLE LINE_TBL (s line)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_38106_2 ADD COLUMN newdata pg_temp_9.pg_temp_38106
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf12 USING btree ADD FUNCTION 1 fn_opf12(int4, int2)
;
----------
[ADMIN_TABLE] vacuum pgbench_branches
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test2dom(fn text) RETURNS hstore_foo
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
return {'a': 1, fn: 'boo', 'c': None}
$$
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."shoelace_obsolete" AS
 SELECT "shoelace"."sl_name",
    "shoelace"."sl_avail",
    "shoelace"."sl_color",
    "shoelace"."sl_len",
    "shoelace"."sl_unit",
    "shoelace"."sl_len_cm"
   FROM "public"."shoelace"
  WHERE (NOT (EXISTS ( SELECT "shoe"."shoename"
           FROM "public"."shoe"
          WHERE ("shoe"."slcolor" = "shoelace"."sl_color"))))
;
----------
[ADD_INDEX] CREATE INDEX t1_hash ON s4.t1 USING HASH (i)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_m_p2 PARTITION OF pagg_tab_m FOR VALUES FROM (10, 10) TO (20, 20)
;
