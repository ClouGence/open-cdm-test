create table pktable_base(base1 int not null)
;
CREATE TABLE "public"."test_type_diff" (
    "f1" integer,
    "f2" bigint
)
;
-- intentionally leave some objects around
create table idxpart (a int) partition by range (a)
;
ALTER TABLE gtest27 ALTER COLUMN a TYPE text
;
-- ******************************************************************
-- *                                                                *
-- * Test inheritance (example taken from tutorial)                 *
-- *                                                                *
-- ******************************************************************
create table cities (
	name		text,
	population	float8,
	altitude	int		-- (in ft)
)
;
ALTER TABLE ref ALTER CONSTRAINT ref_f1_f2_fkey1
  DEFERRABLE INITIALLY DEFERRED
;
create temp table prtx1_2 partition of prtx1 for values from (11) to (21)
;
-- replace the trigger function with one that restarts the deletion after
-- having modified a child
create or replace function parent_del_func()
  returns trigger language plpgsql as
$$
begin
  delete from child where aid = old.aid;
  if found then
    delete from parent where aid = old.aid;
    return null; -- cancel outer deletion
  end if;
  return old;
end;
$$
;
create temp table q1 partition of q for values in (1) partition by list (b)
;
CREATE TYPE jsbrec AS (
	i	int,
	ia	_int4,
	ia1	int[],
	ia2	int[][],
	ia3	int[][][],
	ia1d	jsb_int_array_1d,
	ia2d	jsb_int_array_2d,
	t	text,
	ta	text[],
	c	char(10),
	ca	char(10)[],
	ts	timestamp,
	js	json,
	jsb	jsonb,
	jsa	json[],
	rec	jbpop,
	reca	jbpop[]
)
;
ALTER OPERATOR FAMILY gist_timestamptz_ops USING gist ADD
	FUNCTION	11  (timestamptz, timestamptz) gbt_ts_sortsupport (internal)
;
CREATE FUNCTION isn_weak()
	RETURNS boolean
	AS '$libdir/isn', 'weak_input_status'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL RESTRICTED
;
ANALYZE ab1
;
CREATE TABLE vac_tab_off_toast_on(i int, j text) WITH
  (autovacuum_enabled=false,
   vacuum_index_cleanup=false, toast.vacuum_index_cleanup=true,
   vacuum_truncate=false, toast.vacuum_truncate=true)
;
ANALYZE public.fk_partitioned_fk_2_1
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
ANALYZE pg_temp_12.pg_temp_26358
;
COMMENT ON COLUMN default_test_row.f1 IS NULL
;
VACUUM (PARALLEL 0) pg_catalog.pg_depend
;
ALTER TABLE ONLY "public"."gtest23p"
    ADD CONSTRAINT "gtest23p_pkey" PRIMARY KEY ("y")
;
/* contrib/pageinspect/pageinspect--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- HASH functions
--

--
-- hash_page_type()
--
CREATE FUNCTION hash_page_type(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'hash_page_type'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE tid_tab (a int)
;
DROP TRIGGER t2 ON pg_description
;
ALTER OPERATOR FAMILY gist_enum_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE TABLE "public"."part_rp100" (
    "a" integer,
    CONSTRAINT "part_rp100_a_check" CHECK ((("a" >= 123) AND ("a" < 133) AND ("a" IS NOT NULL)))
)
;
-- ok
create table inh_perm_child () inherits (inh_temp_parent)
;
CREATE FUNCTION isnge(ismn13, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION gtrgm_in(cstring)
RETURNS gtrgm
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create trigger check_fkeys_pkey2_exist
	after insert or update on fkeys
	for each row
	execute function check_primary_key ('fkey3', 'fkeys2', 'pkey23')
;
ALTER TABLE fd_pt1 ALTER COLUMN c8 SET STORAGE EXTERNAL
;
alter database "abc" is_template true
;
ANALYZE pg_temp_18.pg_temp_25732
;
DROP TRIGGER trig_row_after_insupd ON rem1
;
CREATE TABLE deptest2 (f1 int)
;
CREATE TEXT SEARCH CONFIGURATION estonian
	(PARSER = default)
;
CREATE FUNCTION test_argresult_domain_array(x integer[])
  RETURNS ordered_pair_domain[] AS $$
begin
return array[x::ordered_pair_domain, x::ordered_pair_domain];
end
$$ LANGUAGE plpgsql
;
CREATE VIEW key_column_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc_nspname AS sql_identifier) AS constraint_schema,
           CAST(conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr_nspname AS sql_identifier) AS table_schema,
           CAST(relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(CASE WHEN contype = 'f' THEN
                       _pg_index_position(ss.conindid, ss.confkey[(ss.x).n])
                     ELSE NULL
                END AS cardinal_number)
             AS position_in_unique_constraint
    FROM pg_attribute a,
         (SELECT r.oid AS roid, r.relname, r.relowner,
                 nc.nspname AS nc_nspname, nr.nspname AS nr_nspname,
                 c.oid AS coid, c.conname, c.contype, c.conindid,
                 c.confkey, c.confrelid,
                 _pg_expandarray(c.conkey) AS x
          FROM pg_namespace nr, pg_class r, pg_namespace nc,
               pg_constraint c
          WHERE nr.oid = r.relnamespace
                AND r.oid = c.conrelid
                AND nc.oid = c.connamespace
                AND c.contype IN ('p', 'u', 'f')
                AND r.relkind IN ('r', 'p')
                AND (NOT pg_is_other_temp_schema(nr.oid)) ) AS ss
    WHERE ss.roid = a.attrelid
          AND a.attnum = (ss.x).x
          AND NOT a.attisdropped
          AND (pg_has_role(relowner, 'USAGE')
               OR has_column_privilege(roid, a.attnum,
                                       'SELECT, INSERT, UPDATE, REFERENCES'))
;
comment on function psql_df_plpgsql () is 'some comment'
;
ALTER TABLE pg_temp_12.pg_temp_25367_2 ADD COLUMN newdata pg_temp_12.pg_temp_25367
;
ALTER INDEX pg_toast.pg_toast_30414_index RENAME TO reind_con_toast_idx
;
-- cannot set column as generated-as-identity if it has an invalid not-null
ALTER TABLE notnull_tbl1 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
ANALYZE pg_temp_11.pg_temp_26171
;
alter table nv_child_2010 add check (d between '2010-01-01'::date and '2010-12-31'::date) not valid
;
DROP TABLE past_inh_db_parent, past_inh_db_other
;
create table gen_part_1_1
  partition of gen_part_1 for values from (1) to (2)
;
-- ok
SECURITY LABEL ON TABLE t2
    IS 'invalid security context'
;
CREATE FUNCTION g_int_picksplit(internal, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
/*
 * text search configuration for norwegian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * norwegian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
CREATE FUNCTION isneq(isbn13, isbn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE ONLY "regress_indexing"."pk5"
    ADD CONSTRAINT "pk5_pkey" PRIMARY KEY ("a")
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table pktable_base(base1 int not null)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_type_diff" (
    "f1" integer,
    "f2" bigint
)
;
----------
[CREATE_TABLE] -- intentionally leave some objects around
create table idxpart (a int) partition by range (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest27 ALTER COLUMN a TYPE text
;
----------
[CREATE_TABLE] -- ******************************************************************
-- *                                                                *
-- * Test inheritance (example taken from tutorial)                 *
-- *                                                                *
-- ******************************************************************
create table cities (
	name		text,
	population	float8,
	altitude	int		-- (in ft)
)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE ref ALTER CONSTRAINT ref_f1_f2_fkey1
  DEFERRABLE INITIALLY DEFERRED
;
----------
[CREATE_TABLE] create temp table prtx1_2 partition of prtx1 for values from (11) to (21)
;
----------
[CREATE_PROG_OBJ] -- replace the trigger function with one that restarts the deletion after
-- having modified a child
create or replace function parent_del_func()
  returns trigger language plpgsql as
$$
begin
  delete from child where aid = old.aid;
  if found then
    delete from parent where aid = old.aid;
    return null; -- cancel outer deletion
  end if;
  return old;
end;
$$
;
----------
[CREATE_TABLE] create temp table q1 partition of q for values in (1) partition by list (b)
;
----------
[CREATE_TYPE] CREATE TYPE jsbrec AS (
	i	int,
	ia	_int4,
	ia1	int[],
	ia2	int[][],
	ia3	int[][][],
	ia1d	jsb_int_array_1d,
	ia2d	jsb_int_array_2d,
	t	text,
	ta	text[],
	c	char(10),
	ca	char(10)[],
	ts	timestamp,
	js	json,
	jsb	jsonb,
	jsa	json[],
	rec	jbpop,
	reca	jbpop[]
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timestamptz_ops USING gist ADD
	FUNCTION	11  (timestamptz, timestamptz) gbt_ts_sortsupport (internal)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isn_weak()
	RETURNS boolean
	AS '$libdir/isn', 'weak_input_status'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL RESTRICTED
;
----------
[ADMIN_TABLE] ANALYZE ab1
;
----------
[CREATE_TABLE] CREATE TABLE vac_tab_off_toast_on(i int, j text) WITH
  (autovacuum_enabled=false,
   vacuum_index_cleanup=false, toast.vacuum_index_cleanup=true,
   vacuum_truncate=false, toast.vacuum_truncate=true)
;
----------
[ADMIN_TABLE] ANALYZE public.fk_partitioned_fk_2_1
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26358
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN default_test_row.f1 IS NULL
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_depend
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest23p"
    ADD CONSTRAINT "gtest23p_pkey" PRIMARY KEY ("y")
;
----------
[CREATE_PROG_OBJ] /* contrib/pageinspect/pageinspect--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- HASH functions
--

--
-- hash_page_type()
--
CREATE FUNCTION hash_page_type(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'hash_page_type'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE tid_tab (a int)
;
----------
[DROP_TRIGGER] DROP TRIGGER t2 ON pg_description
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_enum_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part_rp100" (
    "a" integer,
    CONSTRAINT "part_rp100_a_check" CHECK ((("a" >= 123) AND ("a" < 133) AND ("a" IS NOT NULL)))
)
;
----------
[CREATE_TABLE] -- ok
create table inh_perm_child () inherits (inh_temp_parent)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ismn13, ean13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_in(cstring)
RETURNS gtrgm
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger check_fkeys_pkey2_exist
	after insert or update on fkeys
	for each row
	execute function check_primary_key ('fkey3', 'fkeys2', 'pkey23')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c8 SET STORAGE EXTERNAL
;
----------
[ALTER_CATALOG] alter database "abc" is_template true
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_25732
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_after_insupd ON rem1
;
----------
[CREATE_TABLE] CREATE TABLE deptest2 (f1 int)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION estonian
	(PARSER = default)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_argresult_domain_array(x integer[])
  RETURNS ordered_pair_domain[] AS $$
begin
return array[x::ordered_pair_domain, x::ordered_pair_domain];
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW key_column_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc_nspname AS sql_identifier) AS constraint_schema,
           CAST(conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr_nspname AS sql_identifier) AS table_schema,
           CAST(relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(CASE WHEN contype = 'f' THEN
                       _pg_index_position(ss.conindid, ss.confkey[(ss.x).n])
                     ELSE NULL
                END AS cardinal_number)
             AS position_in_unique_constraint
    FROM pg_attribute a,
         (SELECT r.oid AS roid, r.relname, r.relowner,
                 nc.nspname AS nc_nspname, nr.nspname AS nr_nspname,
                 c.oid AS coid, c.conname, c.contype, c.conindid,
                 c.confkey, c.confrelid,
                 _pg_expandarray(c.conkey) AS x
          FROM pg_namespace nr, pg_class r, pg_namespace nc,
               pg_constraint c
          WHERE nr.oid = r.relnamespace
                AND r.oid = c.conrelid
                AND nc.oid = c.connamespace
                AND c.contype IN ('p', 'u', 'f')
                AND r.relkind IN ('r', 'p')
                AND (NOT pg_is_other_temp_schema(nr.oid)) ) AS ss
    WHERE ss.roid = a.attrelid
          AND a.attnum = (ss.x).x
          AND NOT a.attisdropped
          AND (pg_has_role(relowner, 'USAGE')
               OR has_column_privilege(roid, a.attnum,
                                       'SELECT, INSERT, UPDATE, REFERENCES'))
;
----------
[COMMENT_PROG_OBJ] comment on function psql_df_plpgsql () is 'some comment'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_25367_2 ADD COLUMN newdata pg_temp_12.pg_temp_25367
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30414_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- cannot set column as generated-as-identity if it has an invalid not-null
ALTER TABLE notnull_tbl1 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26171
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table nv_child_2010 add check (d between '2010-01-01'::date and '2010-12-31'::date) not valid
;
----------
[DROP_TABLE] DROP TABLE past_inh_db_parent, past_inh_db_other
;
----------
[CREATE_TABLE] create table gen_part_1_1
  partition of gen_part_1 for values from (1) to (2)
;
----------
[UNKNOWN] -- ok
SECURITY LABEL ON TABLE t2
    IS 'invalid security context'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_int_picksplit(internal, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_POLICY] /*
 * text search configuration for norwegian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * norwegian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(isbn13, isbn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "regress_indexing"."pk5"
    ADD CONSTRAINT "pk5_pkey" PRIMARY KEY ("a")
;
