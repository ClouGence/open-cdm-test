--
--
--

CREATE TABLE NAME_TBL(f1 name)
;
-- now create a trigger that forces all Datums to be indirect ones
CREATE FUNCTION update_using_indirect()
        RETURNS trigger
        LANGUAGE plpgsql AS $$
BEGIN
    NEW := make_tuple_indirect(NEW);
    RETURN NEW;
END$$
;
DROP TABLE tab_expr_stats
;
create temp table zt2 (f2 int primary key)
;
ANALYZE pg_temp_11.pg_temp_26722
;
-- conflict
alter table stud_emp rename to aggtest
;
CREATE TABLE pa_source (sid integer, delta float)
;
-- ===================================================================
-- conversion error
-- ===================================================================
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE int
;
-- Cleanup of partition tree used for REINDEX test.
DROP TABLE concur_reindex_part
;
create table part_21_30_cd partition of part_21_30 for values in ('cd')
;
ALTER TABLE notnull_tbl1 ALTER COLUMN a SET NOT NULL
;
alter table rls_t enable row level security
;
ALTER TABLE evttrig.one DROP CONSTRAINT one_pkey
;
-- duplicate name
ALTER TABLE notnull_tbl1 ADD COLUMN b INT CONSTRAINT notnull_tbl1_a_not_null NOT NULL
;
-- Create and test some dummy functions
CREATE FUNCTION plsample_result_text(a1 numeric, a2 text, a3 integer[])
RETURNS TEXT
AS $$
  Example of source with text result.
$$ LANGUAGE plsample
;
create or replace function retc(int) returns two_int8s language plpgsql as
$$ declare r record; begin r := row($1::int8, 1::int8, 42); return r; end $$
;
DROP TABLE pitest3
;
CREATE DATABASE tbuiltin6 ENCODING ' LATIN1' TEMPLATE template0 BUILTIN_LOCALE 'C.UTF-8' LC_COLLATE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
CREATE FUNCTION isneq(isbn, isbn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE parent (a text, b int) PARTITION BY LIST (a)
;
CREATE TABLE "generated_stored_tests"."gtest1" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
CREATE OR REPLACE FUNCTION pg_catalog.pg_file_rename(text, text)
RETURNS bool
AS 'SELECT pg_catalog.pg_file_rename($1, $2, NULL::pg_catalog.text);'
LANGUAGE SQL VOLATILE STRICT
;
ANALYZE public.rtest_t1
;
CREATE OR REPLACE FUNCTION test_slru_delete_all() RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_delete_all' LANGUAGE C
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_am
;
ALTER TABLE ONLY list_parted2 ADD COLUMN c int
;
ALTER TABLE ONLY "public"."prt2_l" ATTACH PARTITION "public"."prt2_l_p2" FOR VALUES FROM (250) TO (500)
;
CREATE UNIQUE INDEX "hash_f8_index_2" ON "public"."hash_f8_heap" USING "btree" ((("seqno" + 1)), "random")
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
create table d4_primary2 partition of d4_primary for values in (2)
;
CREATE TABLE tab1_1 (b text, a int NOT NULL)
;
CREATE FUNCTION gbt_macad8_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE INDEX distinct_on_tbl_x_y_idx ON distinct_on_tbl (x, y)
;
CREATE FUNCTION dep_req1() RETURNS text
LANGUAGE SQL AS $$ SELECT 'req1' $$
;
/* contrib/ltree/ltree--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION hash_ltree(ltree)
RETURNS integer
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE parent ALTER COLUMN c SET DEFAULT 0
;
CREATE FUNCTION gbt_float4_distance(internal,float4,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE INDEX ptif_test11_index ON ptif_test11 (a)
;
---
-- Check automatic naming of multiranges
---

create type intr as range(subtype=int)
;
CREATE TABLE concur_exprs_tab (c1 int , c2 boolean)
;
CREATE SCHEMA dump_test_second_schema
;
CREATE SCHEMA fkpart10
  CREATE TABLE tbl1(f1 int PRIMARY KEY) PARTITION BY RANGE(f1)
  CREATE TABLE tbl1_p1 PARTITION OF tbl1 FOR VALUES FROM (minvalue) TO (1)
  CREATE TABLE tbl1_p2 PARTITION OF tbl1 FOR VALUES FROM (1) TO (maxvalue)
  CREATE TABLE tbl2(f1 int REFERENCES tbl1 DEFERRABLE INITIALLY DEFERRED)
  CREATE TABLE tbl3(f1 int PRIMARY KEY) PARTITION BY RANGE(f1)
  CREATE TABLE tbl3_p1 PARTITION OF tbl3 FOR VALUES FROM (minvalue) TO (1)
  CREATE TABLE tbl3_p2 PARTITION OF tbl3 FOR VALUES FROM (1) TO (maxvalue)
  CREATE TABLE tbl4(f1 int REFERENCES tbl3 DEFERRABLE INITIALLY DEFERRED)
;
SECURITY LABEL ON TABLE julio_claudians IS 'system_u:object_r:sepgsql_regtest_foo_table_t:s0'
;
-- cleanup
drop domain ddef1 restrict
;
ANALYZE pg_temp_9.pg_temp_26996
;
CREATE OPERATOR "public".= (
    FUNCTION = "public"."int8alias1eq",
    LEFTARG = "public"."int8alias1",
    RIGHTARG = "public"."int8alias2",
    MERGES,
    RESTRICT = "eqsel",
    JOIN = "eqjoinsel"
)
;
ANALYZE fprt1_p1
;
CREATE UNIQUE INDEX concur_exprs_index_pred ON concur_exprs_tab (c1)
  WHERE (c1::text > 500000000::text COLLATE "C")
;
ANALYZE public.rc_test
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] --
--
--

CREATE TABLE NAME_TBL(f1 name)
;
----------
[CREATE_PROG_OBJ] -- now create a trigger that forces all Datums to be indirect ones
CREATE FUNCTION update_using_indirect()
        RETURNS trigger
        LANGUAGE plpgsql AS $$
BEGIN
    NEW := make_tuple_indirect(NEW);
    RETURN NEW;
END$$
;
----------
[DROP_TABLE] DROP TABLE tab_expr_stats
;
----------
[CREATE_TABLE] create temp table zt2 (f2 int primary key)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26722
;
----------
[RENAME_TABLE] -- conflict
alter table stud_emp rename to aggtest
;
----------
[CREATE_TABLE] CREATE TABLE pa_source (sid integer, delta float)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ===================================================================
-- conversion error
-- ===================================================================
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE int
;
----------
[DROP_TABLE] -- Cleanup of partition tree used for REINDEX test.
DROP TABLE concur_reindex_part
;
----------
[CREATE_TABLE] create table part_21_30_cd partition of part_21_30 for values in ('cd')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE notnull_tbl1 ALTER COLUMN a SET NOT NULL
;
----------
[ALTER_TABLE] alter table rls_t enable row level security
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE evttrig.one DROP CONSTRAINT one_pkey
;
----------
[ALTER_TABLE|ADD_COLUMN] -- duplicate name
ALTER TABLE notnull_tbl1 ADD COLUMN b INT CONSTRAINT notnull_tbl1_a_not_null NOT NULL
;
----------
[CREATE_PROG_OBJ] -- Create and test some dummy functions
CREATE FUNCTION plsample_result_text(a1 numeric, a2 text, a3 integer[])
RETURNS TEXT
AS $$
  Example of source with text result.
$$ LANGUAGE plsample
;
----------
[CREATE_PROG_OBJ] create or replace function retc(int) returns two_int8s language plpgsql as
$$ declare r record; begin r := row($1::int8, 1::int8, 42); return r; end $$
;
----------
[DROP_TABLE] DROP TABLE pitest3
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin6 ENCODING ' LATIN1' TEMPLATE template0 BUILTIN_LOCALE 'C.UTF-8' LC_COLLATE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(isbn, isbn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE parent (a text, b int) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest1" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_catalog.pg_file_rename(text, text)
RETURNS bool
AS 'SELECT pg_catalog.pg_file_rename($1, $2, NULL::pg_catalog.text);'
LANGUAGE SQL VOLATILE STRICT
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_t1
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_delete_all() RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_delete_all' LANGUAGE C
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_am
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE ONLY list_parted2 ADD COLUMN c int
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_l" ATTACH PARTITION "public"."prt2_l_p2" FOR VALUES FROM (250) TO (500)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "hash_f8_index_2" ON "public"."hash_f8_heap" USING "btree" ((("seqno" + 1)), "random")
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
----------
[CREATE_TABLE] create table d4_primary2 partition of d4_primary for values in (2)
;
----------
[CREATE_TABLE] CREATE TABLE tab1_1 (b text, a int NOT NULL)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad8_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADD_INDEX] CREATE INDEX distinct_on_tbl_x_y_idx ON distinct_on_tbl (x, y)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dep_req1() RETURNS text
LANGUAGE SQL AS $$ SELECT 'req1' $$
;
----------
[CREATE_PROG_OBJ] /* contrib/ltree/ltree--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION hash_ltree(ltree)
RETURNS integer
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE parent ALTER COLUMN c SET DEFAULT 0
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float4_distance(internal,float4,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = ismn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ADD_INDEX] CREATE INDEX ptif_test11_index ON ptif_test11 (a)
;
----------
[CREATE_TYPE] ---
-- Check automatic naming of multiranges
---

create type intr as range(subtype=int)
;
----------
[CREATE_TABLE] CREATE TABLE concur_exprs_tab (c1 int , c2 boolean)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA dump_test_second_schema
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart10
  CREATE TABLE tbl1(f1 int PRIMARY KEY) PARTITION BY RANGE(f1)
  CREATE TABLE tbl1_p1 PARTITION OF tbl1 FOR VALUES FROM (minvalue) TO (1)
  CREATE TABLE tbl1_p2 PARTITION OF tbl1 FOR VALUES FROM (1) TO (maxvalue)
  CREATE TABLE tbl2(f1 int REFERENCES tbl1 DEFERRABLE INITIALLY DEFERRED)
  CREATE TABLE tbl3(f1 int PRIMARY KEY) PARTITION BY RANGE(f1)
  CREATE TABLE tbl3_p1 PARTITION OF tbl3 FOR VALUES FROM (minvalue) TO (1)
  CREATE TABLE tbl3_p2 PARTITION OF tbl3 FOR VALUES FROM (1) TO (maxvalue)
  CREATE TABLE tbl4(f1 int REFERENCES tbl3 DEFERRABLE INITIALLY DEFERRED)
;
----------
[UNKNOWN] SECURITY LABEL ON TABLE julio_claudians IS 'system_u:object_r:sepgsql_regtest_foo_table_t:s0'
;
----------
[DROP_TYPE] -- cleanup
drop domain ddef1 restrict
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26996
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".= (
    FUNCTION = "public"."int8alias1eq",
    LEFTARG = "public"."int8alias1",
    RIGHTARG = "public"."int8alias2",
    MERGES,
    RESTRICT = "eqsel",
    JOIN = "eqjoinsel"
)
;
----------
[ADMIN_TABLE] ANALYZE fprt1_p1
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX concur_exprs_index_pred ON concur_exprs_tab (c1)
  WHERE (c1::text > 500000000::text COLLATE "C")
;
----------
[ADMIN_TABLE] ANALYZE public.rc_test
;
