ALTER TABLE generated_virtual_tests.gtestnn_parent OWNER TO zyc
;
SET SESSION AUTHORIZATION regress_priv_user2
;
set local timezone = 'UTC'
;
-- Should not get that error once a user mapping is created
CREATE USER MAPPING FOR regress_view_owner_another SERVER loopback OPTIONS (password_required 'false')
;
-- ERROR
DROP USER MAPPING FOR regress_test_role SERVER s6
;
DROP USER MAPPING FOR regress_view_owner_another SERVER loopback
;
COPY public.cc1 (f1, f2, f3, a2) TO stdout
;
EXECUTE dumpBaseType('3615')
;
BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE READ ONLY DEFERRABLE
;
LOCK TABLE "public"."mcv_lists" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[TRANSFER_PRIVILEGE] ALTER TABLE generated_virtual_tests.gtestnn_parent OWNER TO zyc
;
----------
[SWITCH_USER] SET SESSION AUTHORIZATION regress_priv_user2
;
----------
[SESSION_SETTING_WRITE] set local timezone = 'UTC'
;
----------
[GRANT] -- Should not get that error once a user mapping is created
CREATE USER MAPPING FOR regress_view_owner_another SERVER loopback OPTIONS (password_required 'false')
;
----------
[REVOKE] -- ERROR
DROP USER MAPPING FOR regress_test_role SERVER s6
;
----------
[REVOKE] DROP USER MAPPING FOR regress_view_owner_another SERVER loopback
;
----------
[DATA_EXPORT] COPY public.cc1 (f1, f2, f3, a2) TO stdout
;
----------
[UNSAFE] EXECUTE dumpBaseType('3615')
;
----------
[TRANSACTION] BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE READ ONLY DEFERRABLE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."mcv_lists" IN ACCESS SHARE MODE
;
