ALTER TABLE pg_temp_9.pg_temp_26520_2 ADD COLUMN newdata pg_temp_9.pg_temp_26520
;
ALTER DOMAIN split_domain_type_next.email_domain DROP CONSTRAINT IF EXISTS email_len_chk RESTRICT
;
/*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
DROP TABLE test_tab
;
ANALYZE pg_temp_4.pg_temp_27487
;
CREATE TABLE brintest_multi (
	int8col bigint,
	int2col smallint,
	int4col integer,
	oidcol oid,
	tidcol tid,
	float4col real,
	float8col double precision,
	macaddrcol macaddr,
	macaddr8col macaddr8,
	inetcol inet,
	cidrcol cidr,
	datecol date,
	timecol time without time zone,
	timestampcol timestamp without time zone,
	timestamptzcol timestamp with time zone,
	intervalcol interval,
	timetzcol time with time zone,
	numericcol numeric,
	uuidcol uuid,
	lsncol pg_lsn
) WITH (fillfactor=10)
;
ALTER TABLE ONLY "fkpart5"."pk"
    ADD CONSTRAINT "pk_pkey" PRIMARY KEY ("a")
;
CREATE VIEW column_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(x.relname AS sql_identifier) AS table_name,
           CAST(x.attname AS sql_identifier) AS column_name,
           CAST(x.prtype AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(x.grantee, x.relowner, 'USAGE')
                  OR x.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
           SELECT pr_c.grantor,
                  pr_c.grantee,
                  attname,
                  relname,
                  relnamespace,
                  pr_c.prtype,
                  pr_c.grantable,
                  pr_c.relowner
           FROM (SELECT oid, relname, relnamespace, relowner, (aclexplode(coalesce(relacl, acldefault('r', relowner)))).*
                 FROM pg_class
                 WHERE relkind IN ('r', 'v', 'f', 'p')
                ) pr_c (oid, relname, relnamespace, relowner, grantor, grantee, prtype, grantable),
                pg_attribute a
           WHERE a.attrelid = pr_c.oid
                 AND a.attnum > 0
                 AND NOT a.attisdropped
           UNION
           SELECT pr_a.grantor,
                  pr_a.grantee,
                  attname,
                  relname,
                  relnamespace,
                  pr_a.prtype,
                  pr_a.grantable,
                  c.relowner
           FROM (SELECT attrelid, attname, (aclexplode(coalesce(attacl, acldefault('c', relowner)))).*
                 FROM pg_attribute a JOIN pg_class cc ON (a.attrelid = cc.oid)
                 WHERE attnum > 0
                       AND NOT attisdropped
                ) pr_a (attrelid, attname, grantor, grantee, prtype, grantable),
                pg_class c
           WHERE pr_a.attrelid = c.oid
                 AND relkind IN ('r', 'v', 'f', 'p')
         ) x,
         pg_namespace nc,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE x.relnamespace = nc.oid
          AND x.grantee = grantee.oid
          AND x.grantor = u_grantor.oid
          AND x.prtype IN ('INSERT', 'SELECT', 'UPDATE', 'REFERENCES')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
ALTER TABLE regtest_table ADD COLUMN z int
;
ALTER TABLE fk ATTACH PARTITION fk4 FOR VALUES IN (50)
;
CREATE DOMAIN priv_testdomain2a AS priv_testdomain1
;
COMMENT ON CONSTRAINT "comment_test_pk" ON "public"."comment_test" IS 'PRIMARY KEY constraint of comment_test'
;
ALTER TABLE ONLY "public"."constr_child3" INHERIT "public"."constr_parent2"
;
create rule base_tab_def_view_ins_rule as on insert to base_tab_def_view
  do also insert into base_tab_def (a, b, e) select new.a, new.b, 'xxx'
;
CREATE FUNCTION ltree_union(internal, internal)
RETURNS ltree_gist as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ANALYZE pg_temp_9.pg_temp_38079
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
--
-- TIMESTAMP
--

CREATE TABLE TIMESTAMP_TBL (d1 timestamp(2) without time zone)
;
CREATE DATABASE standby_afterpromotion
;
CREATE TEXT SEARCH CONFIGURATION testcfg (PARSER = testparser)
;
drop schema if exists abc,aac cascade
;
CREATE TABLE b (bb TEXT) INHERITS (a)
;
ALTER INDEX pg_toast.pg_toast_28405_index RENAME TO reind_con_toast_idx
;
CREATE TRIGGER no_op_trig INSTEAD OF INSERT OR UPDATE OR DELETE
ON european_city_view FOR EACH ROW EXECUTE PROCEDURE no_op_trig_fn()
;
DROP TABLE pg_temp_10.pg_temp_26679_2, pg_temp_10.pg_temp_26679
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_subscription_rel
;
-- should fail, SQL functions cannot be event triggers
create function test_event_trigger_sql() returns event_trigger as $$
SELECT 1 $$ language sql
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_collation
;
--fail
DROP POLICY pp1 ON part_document
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_foreign_server
;
CREATE FUNCTION citext_cmp(citext, citext)
RETURNS int4
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FUNCTION verify_heapam(relation regclass,
							  on_error_stop boolean default false,
							  check_toast boolean default false,
							  skip text default 'none',
							  startblock bigint default null,
							  endblock bigint default null,
							  blkno OUT bigint,
							  offnum OUT integer,
							  attnum OUT integer,
							  msg OUT text)
RETURNS SETOF record
AS '$libdir/amcheck', 'verify_heapam'
LANGUAGE C
;
CREATE TEMP TABLE VARCHAR_TBL(f1 varchar(1))
;
CREATE OPERATOR <<< (procedure = leak, leftarg = integer, rightarg = integer,
                     restrict = scalarltsel)
;
CREATE SUBSCRIPTION tsub CONNECTION 'port=21554 host=/tmp/mgn7EplLtc dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
ALTER TABLE test_type_diff2 ALTER COLUMN int_four TYPE int4 USING (pg_column_size(test_type_diff2))
;
VACUUM (FREEZE) pg_catalog.pg_amproc
;
create temp table t(f1 int, f2 int[], f3 text)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=14451 host=/tmp/YRwPwnTUkC dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
ALTER TABLE stream_test ADD COLUMN i INT
;
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 6 < (int4, int2)
;
ALTER TABLE fk_r DROP CONSTRAINT fk_r_p_id_p_jd_fkey1
;
ANALYZE public.gtest10
;
DROP TABLE tidrangescan
;
ALTER TABLE pg_toast.pg_toast_30535 RENAME TO reind_con_toast
;
CREATE TABLE alive(status text)
;
CREATE TABLE "fkpart5"."pk32" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer NOT NULL
)
;
drop operator ===(int4, int4)
;
--DROP TABLE testpub_rf_tbl6;
DROP TABLE testpub_rf_schema1.testpub_rf_tbl5
;
CREATE OPERATOR FAMILY "public"."at_test_sql_partop" USING "btree"
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_26520_2 ADD COLUMN newdata pg_temp_9.pg_temp_26520
;
----------
[ALTER_TYPE] ALTER DOMAIN split_domain_type_next.email_domain DROP CONSTRAINT IF EXISTS email_len_chk RESTRICT
;
----------
[CREATE_POLICY] /*
 * text search configuration for tamil language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * tamil and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY tamil_stem
	(TEMPLATE = snowball, Language = tamil )
;
----------
[DROP_TABLE] DROP TABLE test_tab
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_27487
;
----------
[CREATE_TABLE] CREATE TABLE brintest_multi (
	int8col bigint,
	int2col smallint,
	int4col integer,
	oidcol oid,
	tidcol tid,
	float4col real,
	float8col double precision,
	macaddrcol macaddr,
	macaddr8col macaddr8,
	inetcol inet,
	cidrcol cidr,
	datecol date,
	timecol time without time zone,
	timestampcol timestamp without time zone,
	timestamptzcol timestamp with time zone,
	intervalcol interval,
	timetzcol time with time zone,
	numericcol numeric,
	uuidcol uuid,
	lsncol pg_lsn
) WITH (fillfactor=10)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart5"."pk"
    ADD CONSTRAINT "pk_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW column_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(x.relname AS sql_identifier) AS table_name,
           CAST(x.attname AS sql_identifier) AS column_name,
           CAST(x.prtype AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(x.grantee, x.relowner, 'USAGE')
                  OR x.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
           SELECT pr_c.grantor,
                  pr_c.grantee,
                  attname,
                  relname,
                  relnamespace,
                  pr_c.prtype,
                  pr_c.grantable,
                  pr_c.relowner
           FROM (SELECT oid, relname, relnamespace, relowner, (aclexplode(coalesce(relacl, acldefault('r', relowner)))).*
                 FROM pg_class
                 WHERE relkind IN ('r', 'v', 'f', 'p')
                ) pr_c (oid, relname, relnamespace, relowner, grantor, grantee, prtype, grantable),
                pg_attribute a
           WHERE a.attrelid = pr_c.oid
                 AND a.attnum > 0
                 AND NOT a.attisdropped
           UNION
           SELECT pr_a.grantor,
                  pr_a.grantee,
                  attname,
                  relname,
                  relnamespace,
                  pr_a.prtype,
                  pr_a.grantable,
                  c.relowner
           FROM (SELECT attrelid, attname, (aclexplode(coalesce(attacl, acldefault('c', relowner)))).*
                 FROM pg_attribute a JOIN pg_class cc ON (a.attrelid = cc.oid)
                 WHERE attnum > 0
                       AND NOT attisdropped
                ) pr_a (attrelid, attname, grantor, grantee, prtype, grantable),
                pg_class c
           WHERE pr_a.attrelid = c.oid
                 AND relkind IN ('r', 'v', 'f', 'p')
         ) x,
         pg_namespace nc,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE x.relnamespace = nc.oid
          AND x.grantee = grantee.oid
          AND x.grantor = u_grantor.oid
          AND x.prtype IN ('INSERT', 'SELECT', 'UPDATE', 'REFERENCES')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE regtest_table ADD COLUMN z int
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk ATTACH PARTITION fk4 FOR VALUES IN (50)
;
----------
[CREATE_TYPE] CREATE DOMAIN priv_testdomain2a AS priv_testdomain1
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT "comment_test_pk" ON "public"."comment_test" IS 'PRIMARY KEY constraint of comment_test'
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."constr_child3" INHERIT "public"."constr_parent2"
;
----------
[CREATE_POLICY] create rule base_tab_def_view_ins_rule as on insert to base_tab_def_view
  do also insert into base_tab_def (a, b, e) select new.a, new.b, 'xxx'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_union(internal, internal)
RETURNS ltree_gist as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38079
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] --
-- TIMESTAMP
--

CREATE TABLE TIMESTAMP_TBL (d1 timestamp(2) without time zone)
;
----------
[CREATE_CATALOG] CREATE DATABASE standby_afterpromotion
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION testcfg (PARSER = testparser)
;
----------
[DROP_SCHEMA] drop schema if exists abc,aac cascade
;
----------
[CREATE_TABLE] CREATE TABLE b (bb TEXT) INHERITS (a)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28405_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER no_op_trig INSTEAD OF INSERT OR UPDATE OR DELETE
ON european_city_view FOR EACH ROW EXECUTE PROCEDURE no_op_trig_fn()
;
----------
[DROP_TABLE] DROP TABLE pg_temp_10.pg_temp_26679_2, pg_temp_10.pg_temp_26679
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_subscription_rel
;
----------
[CREATE_PROG_OBJ] -- should fail, SQL functions cannot be event triggers
create function test_event_trigger_sql() returns event_trigger as $$
SELECT 1 $$ language sql
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_collation
;
----------
[DROP_POLICY] --fail
DROP POLICY pp1 ON part_document
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_foreign_server
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_cmp(citext, citext)
RETURNS int4
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION verify_heapam(relation regclass,
							  on_error_stop boolean default false,
							  check_toast boolean default false,
							  skip text default 'none',
							  startblock bigint default null,
							  endblock bigint default null,
							  blkno OUT bigint,
							  offnum OUT integer,
							  attnum OUT integer,
							  msg OUT text)
RETURNS SETOF record
AS '$libdir/amcheck', 'verify_heapam'
LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TEMP TABLE VARCHAR_TBL(f1 varchar(1))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <<< (procedure = leak, leftarg = integer, rightarg = integer,
                     restrict = scalarltsel)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tsub CONNECTION 'port=21554 host=/tmp/mgn7EplLtc dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_type_diff2 ALTER COLUMN int_four TYPE int4 USING (pg_column_size(test_type_diff2))
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_amproc
;
----------
[CREATE_TABLE] create temp table t(f1 int, f2 int[], f3 text)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=14451 host=/tmp/YRwPwnTUkC dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE stream_test ADD COLUMN i INT
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf4 USING btree ADD OPERATOR 6 < (int4, int2)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE fk_r DROP CONSTRAINT fk_r_p_id_p_jd_fkey1
;
----------
[ADMIN_TABLE] ANALYZE public.gtest10
;
----------
[DROP_TABLE] DROP TABLE tidrangescan
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30535 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE alive(status text)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."pk32" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "a" integer NOT NULL
)
;
----------
[DROP_PROG_OBJ] drop operator ===(int4, int4)
;
----------
[DROP_TABLE] --DROP TABLE testpub_rf_tbl6;
DROP TABLE testpub_rf_schema1.testpub_rf_tbl5
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY "public"."at_test_sql_partop" USING "btree"
;
