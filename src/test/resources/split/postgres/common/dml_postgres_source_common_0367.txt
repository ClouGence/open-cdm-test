INSERT INTO tru_rtable0 (SELECT x FROM generate_series(5,9) x)
;
UPDATE pgbench_accounts SET abalance = abalance + 232 WHERE aid = 16783
;
insert into WSlot values ('WS.003.2b', '003', '', '')
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_m_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO public.tplain VALUES ('8', 598)
;
INSERT INTO pg_temp_9.pg_temp_26555_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_26555 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_9.pg_temp_26555 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO pg_temp_12.pg_temp_26420_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_12.pg_temp_26420 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_12.pg_temp_26420 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO public.tht VALUES ('7', 737)
;
UPDATE ft2 SET c3 = 'bar' WHERE postgres_fdw_abs(c1) > 2000 RETURNING *
;
DELETE FROM ONLY "public"."fktable" WHERE $1 OPERATOR(pg_catalog.=) "ftest1"
;
INSERT INTO public.ths_p1 VALUES (8, 108)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1540', relminmxid = '1'
WHERE oid = '"public"."aggtest"'::pg_catalog.regclass
;
-- test update of a toasted key without changing it
UPDATE toasted_key SET toasted_col2 = toasted_col1
;
DELETE FROM target_pa t WHERE t.key = 1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1075', relminmxid = '1'
WHERE oid = '"public"."num_exp_div"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."regress_constr_partition1"'::pg_catalog.regclass
  AND attname IN ('a')
;
INSERT INTO caster (box)           VALUES ('(0,0),(1,1)'::citext)
;
INSERT INTO prt4_n SELECT i, i, to_char(i, 'FM0000') FROM generate_series(0, 599, 2) i
;
INSERT INTO public.tplain VALUES ('1', 501)
;
INSERT INTO fkpart11.fk_parted VALUES (1), (3)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17067', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_para_p1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."brin_summarize"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '38815'
;
INSERT INTO num_exp_ln VALUES (7,'18.23469429965478772991')
;
INSERT INTO num_exp_mul VALUES (9,8,'-1866544013697.195857020')
;
INSERT INTO public.tplain VALUES ('3', 533)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11133', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtestnn_childdef"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10877', relminmxid = '1'
WHERE oid = '"public"."ruletest_tbl"'::pg_catalog.regclass
;
insert into IFace values ('IF', 'orion', 'ethernet_interface_name_too_long', '')
;
INSERT INTO public.ths_p3 VALUES (5, 195)
;
delete from PHone where slotname = old.slotname
;
INSERT INTO TEXT_TBL VALUES ('hi de ho neighbor')
;
insert into itrtest values (1, 'foo') returning *
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9245', relminmxid = '1'
WHERE oid = '"public"."functional_dependencies"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('1', 11)
;
INSERT INTO pg_temp_11.pg_temp_41180_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_41180 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_11.pg_temp_41180 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
insert into rtest_view1 values (4, 'item 4', 'f')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11892', relminmxid = '3'
WHERE oid = '"public"."domnotnull"'::pg_catalog.regclass
;
INSERT INTO tmp_corr SELECT generate_series(1, 10000)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15508', relminmxid = '3'
WHERE oid = '"public"."nv_child_2009"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('4', 284)
;
INSERT INTO quad_box_tbl
VALUES
  (11001, NULL),
  (11002, NULL),
  (11003, '((-infinity,-infinity),(infinity,infinity))'),
  (11004, '((-infinity,100),(-infinity,500))'),
  (11005, '((-infinity,-infinity),(700,infinity))')
;
insert into defp values (0), (1), (2)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17666', relminmxid = '3'
WHERE oid = '"public"."beta_pos_p2"'::pg_catalog.regclass
;
INSERT INTO num_exp_sub VALUES (5,0,'16397.038491')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9210', relminmxid = '1'
WHERE oid = '"public"."brinopers_bloom"'::pg_catalog.regclass
;
insert into inserttest (f3.if1, f3.if2) values (1,array['foo'])
;
INSERT INTO public.tplain VALUES ('1', 31)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt2_l_p3_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17219', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_m_p3"'::pg_catalog.regclass
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] INSERT INTO tru_rtable0 (SELECT x FROM generate_series(5,9) x)
;
----------
[UPDATE] UPDATE pgbench_accounts SET abalance = abalance + 232 WHERE aid = 16783
;
----------
[INSERT] insert into WSlot values ('WS.003.2b', '003', '', '')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_m_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('8', 598)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_9.pg_temp_26555_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_9.pg_temp_26555 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_9.pg_temp_26555 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_12.pg_temp_26420_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_12.pg_temp_26420 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_12.pg_temp_26420 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] INSERT INTO public.tht VALUES ('7', 737)
;
----------
[UPDATE] UPDATE ft2 SET c3 = 'bar' WHERE postgres_fdw_abs(c1) > 2000 RETURNING *
;
----------
[DELETE] DELETE FROM ONLY "public"."fktable" WHERE $1 OPERATOR(pg_catalog.=) "ftest1"
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 108)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1540', relminmxid = '1'
WHERE oid = '"public"."aggtest"'::pg_catalog.regclass
;
----------
[UPDATE] -- test update of a toasted key without changing it
UPDATE toasted_key SET toasted_col2 = toasted_col1
;
----------
[DELETE] DELETE FROM target_pa t WHERE t.key = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1075', relminmxid = '1'
WHERE oid = '"public"."num_exp_div"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."regress_constr_partition1"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[INSERT|SELECT] INSERT INTO caster (box)           VALUES ('(0,0),(1,1)'::citext)
;
----------
[INSERT|SELECT] INSERT INTO prt4_n SELECT i, i, to_char(i, 'FM0000') FROM generate_series(0, 599, 2) i
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 501)
;
----------
[INSERT] INSERT INTO fkpart11.fk_parted VALUES (1), (3)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17067', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_para_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."brin_summarize"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16734', relminmxid = '3'
WHERE oid = '38815'
;
----------
[INSERT] INSERT INTO num_exp_ln VALUES (7,'18.23469429965478772991')
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (9,8,'-1866544013697.195857020')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 533)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '11133', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtestnn_childdef"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10877', relminmxid = '1'
WHERE oid = '"public"."ruletest_tbl"'::pg_catalog.regclass
;
----------
[INSERT] insert into IFace values ('IF', 'orion', 'ethernet_interface_name_too_long', '')
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 195)
;
----------
[DELETE] delete from PHone where slotname = old.slotname
;
----------
[INSERT] INSERT INTO TEXT_TBL VALUES ('hi de ho neighbor')
;
----------
[INSERT] insert into itrtest values (1, 'foo') returning *
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9245', relminmxid = '1'
WHERE oid = '"public"."functional_dependencies"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 11)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_11.pg_temp_41180_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_11.pg_temp_41180 AS newdata FROM public.order_summary mv FULL JOIN pg_temp_11.pg_temp_41180 newdata ON (newdata.date OPERATOR(pg_catalog.=) mv.date AND newdata.item OPERATOR(pg_catalog.=) mv.item AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] insert into rtest_view1 values (4, 'item 4', 'f')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11892', relminmxid = '3'
WHERE oid = '"public"."domnotnull"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO tmp_corr SELECT generate_series(1, 10000)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15508', relminmxid = '3'
WHERE oid = '"public"."nv_child_2009"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 284)
;
----------
[INSERT] INSERT INTO quad_box_tbl
VALUES
  (11001, NULL),
  (11002, NULL),
  (11003, '((-infinity,-infinity),(infinity,infinity))'),
  (11004, '((-infinity,100),(-infinity,500))'),
  (11005, '((-infinity,-infinity),(700,infinity))')
;
----------
[INSERT] insert into defp values (0), (1), (2)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17666', relminmxid = '3'
WHERE oid = '"public"."beta_pos_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (5,0,'16397.038491')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9210', relminmxid = '1'
WHERE oid = '"public"."brinopers_bloom"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into inserttest (f3.if1, f3.if2) values (1,array['foo'])
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 31)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt2_l_p3_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17219', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_m_p3"'::pg_catalog.regclass
;
