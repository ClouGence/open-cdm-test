ALTER TABLE T ADD COLUMN c_text TEXT  DEFAULT 'world',
              ALTER COLUMN c_bpchar SET DEFAULT 'dog'
;
-- remove constraints
alter table target drop CONSTRAINT target_pkey
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) information_schema.sql_features
;
CREATE TABLE "public"."ndistinct" (
    "filler1" "text",
    "filler2" numeric,
    "a" integer,
    "b" integer,
    "filler3" "date",
    "c" integer,
    "d" integer
)
;
ALTER TABLE pg_toast.pg_toast_33313 RENAME TO reind_con_toast
;
CREATE UNIQUE INDEX ind_expr ON reindex_inj.tbl(ABS(i))
;
create trigger tnoticetrigger after insert on tt for each row
execute procedure noticetrigger()
;
create table sl(a int, b int, c int)
;
CREATE TEMPORARY TABLE tbl_temp(id int) WITH (AUTOVACUUM_ENABLED = false)
;
CREATE TABLE test_part_c_2 PARTITION OF test_part_c FOR VALUES IN (2,4)
;
CREATE FUNCTION "public"."tf2p"(integer[], "anyelement") RETURNS integer[]
    LANGUAGE "sql"
    AS $_$select $1$_$
;
create temp table xx1 as select f1 as x1, -f1 as x2 from int4_tbl
;
CREATE TABLE brintest_2 (n numrange)
;
DROP FUNCTION dummy_trigger()
;
-- fail, no value for b

drop function dfunc(int, int, int, int)
;
alter table tt5 add column cc int
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', quote '---')
;
CREATE FUNCTION report_count() RETURNS bigint
STABLE LANGUAGE sql
AS $$ SELECT COUNT(*) FROM test2 $$
;
CREATE TABLE tab_notrep (a int)
;
DROP TABLE pg_temp_3.pg_temp_27503_2, pg_temp_3.pg_temp_27503
;
ALTER TABLE part_5 ADD CONSTRAINT check_a CHECK (a IN (5)), ALTER a SET NOT NULL
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub_test_root, pub_test_root_1
;
ALTER INDEX pg_toast.pg_toast_28137_index RENAME TO reind_con_toast_idx
;
CREATE INDEX spgist_pvactst ON pvactst USING spgist (p)
;
--
-- Alter sequence
--

ALTER SEQUENCE IF EXISTS sequence_test2 RESTART WITH 24
  INCREMENT BY 4 MAXVALUE 36 MINVALUE 5 CYCLE
;
CREATE FUNCTION split_agg_probe.sum_variadic_state(integer, VARIADIC integer[])
RETURNS integer
LANGUAGE sql IMMUTABLE STRICT
AS 'SELECT $1 + COALESCE((SELECT sum(v) FROM unnest($2) AS t(v)), 0)'
;
CREATE TABLE ab1c () INHERITS (ab1)
;
/*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
CREATE TABLE unknowntab (
	u unknown    
)
;
--
-- LIKE operators don't prune
--
create table like_op_noprune (a text) partition by list (a)
;
DROP TABLE brin_parallel_test
;
create temp table innertab (id int8 primary key, dat1 int8)
;
CREATE TABLE "public"."itest7b" (
    "a" integer
)
;
CREATE FOREIGN TABLE ft6 (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text
) SERVER loopback2 OPTIONS (schema_name 'S 1', table_name 'T 4')
;
CREATE FUNCTION regexp_split_to_table( citext, citext ) RETURNS SETOF TEXT AS $$
    SELECT pg_catalog.regexp_split_to_table( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE fk_notpartitioned_pk (a int, b int, PRIMARY KEY (a, b))
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub3
;
ALTER TABLE testschema.test_default_tab ALTER id TYPE bigint
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=24431 host=/tmp/qIgaMd5L8y dbname=postgres' PUBLICATION tap_pub
;
alter table rewriteme alter column foo type numeric
;
DROP TABLE test_like_6, test_like_6c
;
-- try setting and resetting some properties for the new tablespace
ALTER TABLESPACE regress_tblspace SET (random_page_cost = 1.0, seq_page_cost = 1.1)
;
CREATE TABLE parent (
	parent_key	int		PRIMARY KEY,
	aux			text	NOT NULL
  )
;
CREATE EXTENSION test_ext1 SCHEMA has$dollar
;
CREATE DATABASE master_afterpromotion
;
CREATE TEMP TABLE concur_temp_tab_2 (c1 int, c2 text)
  ON COMMIT DELETE ROWS
;
CREATE FUNCTION explain_filter(text) RETURNS SETOF text
LANGUAGE plpgsql AS
$$
DECLARE
    ln text;
BEGIN
    FOR ln IN EXECUTE $1
	LOOP
		ln := regexp_replace(ln, 'Relation OIDs:( \m\d+\M)+',
								 'Relation OIDs: NNN...', 'g');
		ln := regexp_replace(ln, '<Relation-OIDs>( ?\m\d+\M)+</Relation-OIDs>',
								 '<Relation-OIDs>NNN...</Relation-OIDs>', 'g');
		ln := regexp_replace(ln, 'actual rows=\d+\.\d+',
								 'actual rows=N.NN', 'g');
		RETURN NEXT ln;
	END LOOP;
END;
$$
;
CREATE TABLE "public"."pagg_tab_ml_p2_s1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
ALTER TABLE stream_test ALTER COLUMN data set storage external
;
CREATE FUNCTION lquery_send(lquery)
RETURNS bytea
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_text TEXT  DEFAULT 'world',
              ALTER COLUMN c_bpchar SET DEFAULT 'dog'
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- remove constraints
alter table target drop CONSTRAINT target_pkey
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) information_schema.sql_features
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ndistinct" (
    "filler1" "text",
    "filler2" numeric,
    "a" integer,
    "b" integer,
    "filler3" "date",
    "c" integer,
    "d" integer
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33313 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ind_expr ON reindex_inj.tbl(ABS(i))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tnoticetrigger after insert on tt for each row
execute procedure noticetrigger()
;
----------
[CREATE_TABLE] create table sl(a int, b int, c int)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE tbl_temp(id int) WITH (AUTOVACUUM_ENABLED = false)
;
----------
[CREATE_TABLE] CREATE TABLE test_part_c_2 PARTITION OF test_part_c FOR VALUES IN (2,4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tf2p"(integer[], "anyelement") RETURNS integer[]
    LANGUAGE "sql"
    AS $_$select $1$_$
;
----------
[CREATE_TABLE|SELECT] create temp table xx1 as select f1 as x1, -f1 as x2 from int4_tbl
;
----------
[CREATE_TABLE] CREATE TABLE brintest_2 (n numrange)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION dummy_trigger()
;
----------
[DROP_PROG_OBJ] -- fail, no value for b

drop function dfunc(int, int, int, int)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tt5 add column cc int
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', quote '---')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION report_count() RETURNS bigint
STABLE LANGUAGE sql
AS $$ SELECT COUNT(*) FROM test2 $$
;
----------
[CREATE_TABLE] CREATE TABLE tab_notrep (a int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_27503_2, pg_temp_3.pg_temp_27503
;
----------
[ALTER_TABLE|ADD_CONSTRAINT|ALTER_COLUMN] ALTER TABLE part_5 ADD CONSTRAINT check_a CHECK (a IN (5)), ALTER a SET NOT NULL
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub_test_root, pub_test_root_1
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28137_index RENAME TO reind_con_toast_idx
;
----------
[ADD_INDEX] CREATE INDEX spgist_pvactst ON pvactst USING spgist (p)
;
----------
[ALTER_SEQUENCE] --
-- Alter sequence
--

ALTER SEQUENCE IF EXISTS sequence_test2 RESTART WITH 24
  INCREMENT BY 4 MAXVALUE 36 MINVALUE 5 CYCLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_agg_probe.sum_variadic_state(integer, VARIADIC integer[])
RETURNS integer
LANGUAGE sql IMMUTABLE STRICT
AS 'SELECT $1 + COALESCE((SELECT sum(v) FROM unnest($2) AS t(v)), 0)'
;
----------
[CREATE_TABLE] CREATE TABLE ab1c () INHERITS (ab1)
;
----------
[CREATE_POLICY] /*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
----------
[CREATE_TABLE] CREATE TABLE unknowntab (
	u unknown    
)
;
----------
[CREATE_TABLE] --
-- LIKE operators don't prune
--
create table like_op_noprune (a text) partition by list (a)
;
----------
[DROP_TABLE] DROP TABLE brin_parallel_test
;
----------
[CREATE_TABLE] create temp table innertab (id int8 primary key, dat1 int8)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest7b" (
    "a" integer
)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft6 (
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text
) SERVER loopback2 OPTIONS (schema_name 'S 1', table_name 'T 4')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regexp_split_to_table( citext, citext ) RETURNS SETOF TEXT AS $$
    SELECT pg_catalog.regexp_split_to_table( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE fk_notpartitioned_pk (a int, b int, PRIMARY KEY (a, b))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub3
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE testschema.test_default_tab ALTER id TYPE bigint
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=24431 host=/tmp/qIgaMd5L8y dbname=postgres' PUBLICATION tap_pub
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table rewriteme alter column foo type numeric
;
----------
[DROP_TABLE] DROP TABLE test_like_6, test_like_6c
;
----------
[ALTER_TABLESPACE] -- try setting and resetting some properties for the new tablespace
ALTER TABLESPACE regress_tblspace SET (random_page_cost = 1.0, seq_page_cost = 1.1)
;
----------
[CREATE_TABLE] CREATE TABLE parent (
	parent_key	int		PRIMARY KEY,
	aux			text	NOT NULL
  )
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext1 SCHEMA has$dollar
;
----------
[CREATE_CATALOG] CREATE DATABASE master_afterpromotion
;
----------
[CREATE_TABLE] CREATE TEMP TABLE concur_temp_tab_2 (c1 int, c2 text)
  ON COMMIT DELETE ROWS
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION explain_filter(text) RETURNS SETOF text
LANGUAGE plpgsql AS
$$
DECLARE
    ln text;
BEGIN
    FOR ln IN EXECUTE $1
	LOOP
		ln := regexp_replace(ln, 'Relation OIDs:( \m\d+\M)+',
								 'Relation OIDs: NNN...', 'g');
		ln := regexp_replace(ln, '<Relation-OIDs>( ?\m\d+\M)+</Relation-OIDs>',
								 '<Relation-OIDs>NNN...</Relation-OIDs>', 'g');
		ln := regexp_replace(ln, 'actual rows=\d+\.\d+',
								 'actual rows=N.NN', 'g');
		RETURN NEXT ln;
	END LOOP;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_ml_p2_s1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE stream_test ALTER COLUMN data set storage external
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lquery_send(lquery)
RETURNS bytea
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
