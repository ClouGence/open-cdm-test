ALTER TABLE tr_sub_ddl ALTER COLUMN data TYPE text
;
alter table inh_child1 no inherit inh_parent
;
CREATE PROCEDURE priv_testproc1(int) AS 'select $1;' LANGUAGE sql
;
CREATE OR REPLACE RULE myrule AS ON INSERT TO ruletest_tbl
	DO INSTEAD INSERT INTO ruletest_tbl2 VALUES (1000, 1000)
;
DROP EVENT TRIGGER start_rls_command
;
-- no recursion because ONLY
alter table parent enable always trigger tg_stmt
;
CREATE TYPE test8b AS (a int, b int)
;
DROP SCHEMA regress_create_schema_role CASCADE
;
VACUUM (PARALLEL 2) pg_catalog.pg_am
;
-- drop the column
ALTER TABLE fkpart13_t2 DROP COLUMN column_to_drop
;
VACUUM (ANALYZE) public.vacview
;
DROP LANGUAGE IF EXISTS test_language_exists
;
ALTER INDEX test_replica_identity4_pkey
  ATTACH PARTITION test_replica_identity4_1_pkey
;
DROP TABLE plt1_adv_p1_null
;
CREATE TRIGGER invalid_trig AFTER DELETE ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_del_row')
;
drop view rw_view2
;
-- fail
SECURITY LABEL ON ROLE regress_seclabel_user1 IS '...invalid label...'
;
-- ERROR
CREATE FOREIGN TABLE ft_part2 (a INT NOT NULL) SERVER s0
;
create or replace function strtest() returns text as $$
begin
  raise notice 'foo\\bar\041baz';
  return 'foo\\bar\041baz';
end
$$ language plpgsql
;
alter table renameColumn rename column a to d
;
ALTER TABLE pg_toast.pg_toast_30726 RENAME TO reind_con_toast
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_cast
;
VACUUM (FREEZE) pg_catalog.pg_proc
;
--
-- Typanalyze function that returns invalid stats
--
CREATE FUNCTION int_custom_typanalyze_invalid(internal)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C STRICT
;
CREATE SUBSCRIPTION sub CONNECTION 'port=30538 host=/tmp/ydXclDSoIH dbname=postgres' PUBLICATION pub WITH (disable_on_error = true, streaming = on, two_phase = on)
;
--
-- CHECK syntax
--

CREATE TABLE CHECK_TBL (x int,
	CONSTRAINT CHECK_CON CHECK (x > 3))
;
create table trigger_parted_p2 partition of trigger_parted for values in (2)
  partition by list (a)
;
ALTER PUBLICATION testpub_foo RENAME TO testpub_default
;
DROP TABLE pa_target CASCADE
;
CREATE FUNCTION inj_io_short_read_attach(result int)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
CREATE TEMP TABLE t (a integer) PARTITION BY LIST (a)
;
CREATE TABLE dump_test_second_schema.measurement_y2006m5
						PARTITION OF dump_test.measurement
						FOR VALUES FROM ('2006-05-01') TO ('2006-06-01')
;
ALTER TABLE fd_pt1 ADD CONSTRAINT fd_pt1chk2 CHECK (c2 <> '')
;
-- Cleanup: range_parted no longer needed.
DROP TABLE range_parted
;
CREATE FUNCTION pg_catalog.pg_file_unlink(text)
RETURNS bool
AS '$libdir/adminpack', 'pg_file_unlink'
LANGUAGE C VOLATILE STRICT
;
do $$ <<b>> declare c two_int8s; begin b.c.x = 1; end $$
;
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor='string')
;
CREATE FUNCTION "public"."perform_test_func"() RETURNS "void"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	PERFORM perform_simple_func(5);

	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	PERFORM perform_simple_func(50);

	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	RETURN;
END;$$
;
SECURITY LABEL ON TABLE foo_tbl
	IS 'system_u:object_r:sepgsql_regtest_foo_table_t:s0'
;
CREATE EXTENSION test_ext_cyclic1 CASCADE
;
-- Test check constraints
alter table loc2 add constraint loc2_f1positive check (f1 >= 0)
;
DROP OPERATOR FAMILY IF EXISTS no_such_schema.float_ops USING btree
;
CREATE TABLE gtest_child3 PARTITION OF gtest_parent (
    f3 GENERATED ALWAYS AS (f2 * 2) STORED  -- error
) FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
CREATE SCHEMA newschema3 AUTHORIZATION SESSION_USER
;
CREATE COLLATION testcoll_upper_first (provider = icu, locale = '@colCaseFirst=upper')
;
-- fail
alter table idxpart1 drop constraint idxpart1_pkey
;
-- test triggers
create or replace function merge_trigfunc () returns trigger
language plpgsql as
$$
DECLARE
	line text;
BEGIN
	SELECT INTO line format('%s %s %s trigger%s',
		TG_WHEN, TG_OP, TG_LEVEL, CASE
		WHEN TG_OP = 'INSERT' AND TG_LEVEL = 'ROW'
			THEN format(' row: %s', NEW)
		WHEN TG_OP = 'UPDATE' AND TG_LEVEL = 'ROW'
			THEN format(' row: %s -> %s', OLD, NEW)
		WHEN TG_OP = 'DELETE' AND TG_LEVEL = 'ROW'
			THEN format(' row: %s', OLD)
		END);

	RAISE NOTICE '%', line;
	IF (TG_WHEN = 'BEFORE' AND TG_LEVEL = 'ROW') THEN
		IF (TG_OP = 'DELETE') THEN
			RETURN OLD;
		ELSE
			RETURN NEW;
		END IF;
	ELSE
		RETURN NULL;
	END IF;
END;
$$
;
CREATE INDEX "onek_unique1" ON "public"."onek" USING "btree" ("unique1")
;
DROP TABLE grantor_test1, grantor_test2, grantor_test3
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_description
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tr_sub_ddl ALTER COLUMN data TYPE text
;
----------
[ALTER_TABLE] alter table inh_child1 no inherit inh_parent
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE priv_testproc1(int) AS 'select $1;' LANGUAGE sql
;
----------
[CREATE_POLICY] CREATE OR REPLACE RULE myrule AS ON INSERT TO ruletest_tbl
	DO INSTEAD INSERT INTO ruletest_tbl2 VALUES (1000, 1000)
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER start_rls_command
;
----------
[ALTER_TABLE] -- no recursion because ONLY
alter table parent enable always trigger tg_stmt
;
----------
[CREATE_TYPE] CREATE TYPE test8b AS (a int, b int)
;
----------
[DROP_SCHEMA] DROP SCHEMA regress_create_schema_role CASCADE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_am
;
----------
[ALTER_TABLE|DROP_COLUMN] -- drop the column
ALTER TABLE fkpart13_t2 DROP COLUMN column_to_drop
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) public.vacview
;
----------
[DROP_LANGUAGE] DROP LANGUAGE IF EXISTS test_language_exists
;
----------
[ALTER_INDEX] ALTER INDEX test_replica_identity4_pkey
  ATTACH PARTITION test_replica_identity4_1_pkey
;
----------
[DROP_TABLE] DROP TABLE plt1_adv_p1_null
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig AFTER DELETE ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_del_row')
;
----------
[DROP_VIEW] drop view rw_view2
;
----------
[SECURITY_LABEL] -- fail
SECURITY LABEL ON ROLE regress_seclabel_user1 IS '...invalid label...'
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE ft_part2 (a INT NOT NULL) SERVER s0
;
----------
[CREATE_PROG_OBJ] create or replace function strtest() returns text as $$
begin
  raise notice 'foo\\bar\041baz';
  return 'foo\\bar\041baz';
end
$$ language plpgsql
;
----------
[RENAME_COLUMN] alter table renameColumn rename column a to d
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30726 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_cast
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_proc
;
----------
[CREATE_PROG_OBJ] --
-- Typanalyze function that returns invalid stats
--
CREATE FUNCTION int_custom_typanalyze_invalid(internal)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C STRICT
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub CONNECTION 'port=30538 host=/tmp/ydXclDSoIH dbname=postgres' PUBLICATION pub WITH (disable_on_error = true, streaming = on, two_phase = on)
;
----------
[CREATE_TABLE] --
-- CHECK syntax
--

CREATE TABLE CHECK_TBL (x int,
	CONSTRAINT CHECK_CON CHECK (x > 3))
;
----------
[CREATE_TABLE] create table trigger_parted_p2 partition of trigger_parted for values in (2)
  partition by list (a)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_foo RENAME TO testpub_default
;
----------
[DROP_TABLE] DROP TABLE pa_target CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION inj_io_short_read_attach(result int)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TEMP TABLE t (a integer) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test_second_schema.measurement_y2006m5
						PARTITION OF dump_test.measurement
						FOR VALUES FROM ('2006-05-01') TO ('2006-06-01')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fd_pt1 ADD CONSTRAINT fd_pt1chk2 CHECK (c2 <> '')
;
----------
[DROP_TABLE] -- Cleanup: range_parted no longer needed.
DROP TABLE range_parted
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_catalog.pg_file_unlink(text)
RETURNS bool
AS '$libdir/adminpack', 'pg_file_unlink'
LANGUAGE C VOLATILE STRICT
;
----------
[BLOCK] do $$ <<b>> declare c two_int8s; begin b.c.x = 1; end $$
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (fillfactor='string')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."perform_test_func"() RETURNS "void"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	PERFORM perform_simple_func(5);

	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	PERFORM perform_simple_func(50);

	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	RETURN;
END;$$
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE foo_tbl
	IS 'system_u:object_r:sepgsql_regtest_foo_table_t:s0'
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext_cyclic1 CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Test check constraints
alter table loc2 add constraint loc2_f1positive check (f1 >= 0)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY IF EXISTS no_such_schema.float_ops USING btree
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child3 PARTITION OF gtest_parent (
    f3 GENERATED ALWAYS AS (f2 * 2) STORED  -- error
) FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema3 AUTHORIZATION SESSION_USER
;
----------
[CREATE_POLICY] CREATE COLLATION testcoll_upper_first (provider = icu, locale = '@colCaseFirst=upper')
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- fail
alter table idxpart1 drop constraint idxpart1_pkey
;
----------
[CREATE_PROG_OBJ] -- test triggers
create or replace function merge_trigfunc () returns trigger
language plpgsql as
$$
DECLARE
	line text;
BEGIN
	SELECT INTO line format('%s %s %s trigger%s',
		TG_WHEN, TG_OP, TG_LEVEL, CASE
		WHEN TG_OP = 'INSERT' AND TG_LEVEL = 'ROW'
			THEN format(' row: %s', NEW)
		WHEN TG_OP = 'UPDATE' AND TG_LEVEL = 'ROW'
			THEN format(' row: %s -> %s', OLD, NEW)
		WHEN TG_OP = 'DELETE' AND TG_LEVEL = 'ROW'
			THEN format(' row: %s', OLD)
		END);

	RAISE NOTICE '%', line;
	IF (TG_WHEN = 'BEFORE' AND TG_LEVEL = 'ROW') THEN
		IF (TG_OP = 'DELETE') THEN
			RETURN OLD;
		ELSE
			RETURN NEW;
		END IF;
	ELSE
		RETURN NULL;
	END IF;
END;
$$
;
----------
[ADD_INDEX] CREATE INDEX "onek_unique1" ON "public"."onek" USING "btree" ("unique1")
;
----------
[DROP_TABLE] DROP TABLE grantor_test1, grantor_test2, grantor_test3
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_description
;
