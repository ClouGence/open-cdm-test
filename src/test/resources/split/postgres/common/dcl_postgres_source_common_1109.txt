SET SESSION AUTHORIZATION regress_tenant2
;
EXECUTE dumpFunc('35385')
;
-- Check dependencies between ON COMMIT actions with a partitioned
-- table and its partitions.  Using ON COMMIT DROP on a parent removes
-- the whole set.
begin
;
EXECUTE dumpFunc('16644')
;
ALTER TABLE public.test_tsquery OWNER TO zyc
;
EXECUTE dumpAgg('39647')
;
ALTER VIEW public.mvtest_tvvmv OWNER TO zyc
;
REVOKE ALL ON FUNCTION pg_freespace(regclass, bigint) FROM PUBLIC
;
PREPARE TRANSACTION 'regress_p1'
;
-- When work_mem is not enough to sort the entire table, incremental sort
-- may be faster if individual groups still fit into work_mem.
set work_mem to '2MB'
;
------------------------------------------------------------------------------------------
[SWITCH_USER] SET SESSION AUTHORIZATION regress_tenant2
;
----------
[UNSAFE] EXECUTE dumpFunc('35385')
;
----------
[TRANSACTION] -- Check dependencies between ON COMMIT actions with a partitioned
-- table and its partitions.  Using ON COMMIT DROP on a parent removes
-- the whole set.
begin
;
----------
[UNSAFE] EXECUTE dumpFunc('16644')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.test_tsquery OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpAgg('39647')
;
----------
[TRANSFER_PRIVILEGE] ALTER VIEW public.mvtest_tvvmv OWNER TO zyc
;
----------
[REVOKE] REVOKE ALL ON FUNCTION pg_freespace(regclass, bigint) FROM PUBLIC
;
----------
[TRANSACTION] PREPARE TRANSACTION 'regress_p1'
;
----------
[SESSION_SETTING_WRITE] -- When work_mem is not enough to sort the entire table, incremental sort
-- may be faster if individual groups still fit into work_mem.
set work_mem to '2MB'
;
