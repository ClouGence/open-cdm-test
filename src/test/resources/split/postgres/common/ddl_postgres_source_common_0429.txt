CREATE UNIQUE INDEX u2rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  TABLESPACE regress_create_idx_tblspace
  WHERE s.index_row_if(y)
;
ALTER TABLE "public"."nv_parent"
    ADD CONSTRAINT "nv_parent_d_check" CHECK ((("d" >= '2001-01-01'::"date") AND ("d" <= '2099-12-31'::"date"))) NOT VALID
;
CREATE SCHEMA lock_schema1
;
CREATE TABLE prt2_n (a int, b int, c text) PARTITION BY LIST(c)
;
CREATE PROCEDURE ptestx(a int DEFAULT 42, b OUT int) LANGUAGE SQL
  AS $$ SELECT a $$
;
DROP SCHEMA regress_roleoption
;
CREATE SCHEMA IF NOT EXISTS newschema5 AUTHORIZATION "Public"
;
create table inh_parent (a int primary key)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_depend
;
ALTER INDEX pg_toast.pg_toast_30647_index RENAME TO reind_con_toast_idx
;
create table mlparted4 (like mlparted)
;
-- user-defined operator/function
CREATE FUNCTION postgres_fdw_abs(int) RETURNS int AS $$
BEGIN
RETURN abs($1);
END
$$ LANGUAGE plpgsql IMMUTABLE
;
CREATE OR REPLACE FUNCTION pgstathashindex(IN relname regclass,
	OUT version INTEGER,
	OUT bucket_pages BIGINT,
	OUT overflow_pages BIGINT,
	OUT bitmap_pages BIGINT,
	OUT unused_pages BIGINT,
	OUT live_items BIGINT,
	OUT dead_items BIGINT,
	OUT free_percent FLOAT8)
AS '$libdir/pgstattuple', 'pgstathashindex'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE VIEW rw_view14 AS SELECT ctid, a, b FROM base_tbl
;
CREATE TABLE notnull_tbl1_upg (a int, b int)
;
DROP TABLE rngfunc2
;
VACUUM pg_catalog.pg_subscription_rel
;
DROP TABLE testpub_gencol
;
create function sillyaddone(int) returns int language plpgsql as
$$ declare r mutable; begin r.f1 := $1; return r.f1 + 1; end $$
;
alter table mlparted attach partition mlparted4 for values from (1, 30) to (1, 40)
;
-- ok

DROP PUBLICATION testpub2
;
ALTER TYPE planets ADD VALUE 'mercury'
;
CREATE FUNCTION bad_cmp1 (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT
			CASE WHEN ($1 = 768 AND $2 = 769) OR
					  ($1 = 769 AND $2 = 768) THEN 0
				 ELSE public.ok_cmp($1, $2)
			END;
	$$
;
-- negator
CREATE OPERATOR !==== (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
CREATE PROCEDURE test_proc7cc(_x int)
LANGUAGE plpgsql
AS $$
DECLARE _a int; _b numeric;
BEGIN
  CALL test_proc7c(_x, _a, _b);
  RAISE NOTICE '_x: %,_a: %, _b: %', _x, _a, _b;
END
$$
;
create trigger trig_stmt_before before insert on rem2
	for each statement execute procedure trigger_func()
;
/* End test case for bug #17409 */

/* Test case for bug #18970 */

create table attbl(a int)
;
DROP TABLE brin_timestamp_test
;
DROP TABLE ruletest1
;
CREATE TABLE "public"."mlparted3" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
CREATE TEMP TABLE temptest(col int)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_class
;
create index ab_a2_b3_a_idx on ab_a2_b3 (a)
;
DROP TABLE pg_temp_12.pg_temp_26295_2, pg_temp_12.pg_temp_26295
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE TABLE public.tt(a int, b int)
;
CREATE FUNCTION "public"."tg_pslot_bu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotname != old.slotname then
        delete from PSlot where slotname = old.slotname;
	insert into PSlot (
		    slotname,
		    pfname,
		    slotlink,
		    backlink
		) values (
		    new.slotname,
		    new.pfname,
		    new.slotlink,
		    new.backlink
		);
        return null;
    end if;
    return new;
end;
$$
;
CREATE UNIQUE INDEX tbl_include_box_idx_unique ON tbl_include_box using btree (c1, c2) INCLUDE (c3, c4)
;
alter table atacc1 SET WITHOUT OIDS
;
-- Test that we return correct results when using the skip fetch optimization
-- VACUUM FREEZE will set all the pages in the relation all-visible, enabling
-- the optimization.
VACUUM (FREEZE) bmscantest
;
-- Test error handling, if accessing one of the foreign partitions errors out
CREATE FOREIGN TABLE async_p_broken PARTITION OF async_pt FOR VALUES FROM (10000) TO (10001)
  SERVER loopback OPTIONS (table_name 'non_existent_table')
;
-- Test pg_routine_oid
create function current_function(text)
returns regprocedure as $$
declare
  fn_oid regprocedure;
begin
  get diagnostics fn_oid = pg_routine_oid;
  return fn_oid;
end;
$$ language plpgsql
;
CREATE FUNCTION tftest(int) returns table(a int, b int) as $$
SELECT $1, $1+i FROM generate_series(1,5) g(i);
$$ LANGUAGE sql IMMUTABLE STRICT
;
DROP FUNCTION dup(f1 anycompatiblerange)
;
vacuum smalltbl
;
-- invalid use of set-returning function
CREATE TABLE default_expr_agg (a int DEFAULT (generate_series(1,3)))
;
CREATE TABLE persons4 OF person_type (
    name WITH OPTIONS NOT NULL,
    name WITH OPTIONS DEFAULT ''  -- error, specified more than once
)
;
CREATE POLICY p5 ON dump_test.test_table
						   FOR DELETE TO regress_dump_test_role USING (true)
;
ANALYZE slow_emp4000
;
CREATE TABLE "public"."mcv_lists_arrays" (
    "a" "text"[],
    "b" numeric[],
    "c" integer[]
)
WITH ("autovacuum_enabled"='off')
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE UNIQUE INDEX u2rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  TABLESPACE regress_create_idx_tblspace
  WHERE s.index_row_if(y)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "public"."nv_parent"
    ADD CONSTRAINT "nv_parent_d_check" CHECK ((("d" >= '2001-01-01'::"date") AND ("d" <= '2099-12-31'::"date"))) NOT VALID
;
----------
[CREATE_SCHEMA] CREATE SCHEMA lock_schema1
;
----------
[CREATE_TABLE] CREATE TABLE prt2_n (a int, b int, c text) PARTITION BY LIST(c)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptestx(a int DEFAULT 42, b OUT int) LANGUAGE SQL
  AS $$ SELECT a $$
;
----------
[DROP_SCHEMA] DROP SCHEMA regress_roleoption
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema5 AUTHORIZATION "Public"
;
----------
[CREATE_TABLE] create table inh_parent (a int primary key)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_depend
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30647_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table mlparted4 (like mlparted)
;
----------
[CREATE_PROG_OBJ] -- user-defined operator/function
CREATE FUNCTION postgres_fdw_abs(int) RETURNS int AS $$
BEGIN
RETURN abs($1);
END
$$ LANGUAGE plpgsql IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pgstathashindex(IN relname regclass,
	OUT version INTEGER,
	OUT bucket_pages BIGINT,
	OUT overflow_pages BIGINT,
	OUT bitmap_pages BIGINT,
	OUT unused_pages BIGINT,
	OUT live_items BIGINT,
	OUT dead_items BIGINT,
	OUT free_percent FLOAT8)
AS '$libdir/pgstattuple', 'pgstathashindex'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view14 AS SELECT ctid, a, b FROM base_tbl
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1_upg (a int, b int)
;
----------
[DROP_TABLE] DROP TABLE rngfunc2
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_subscription_rel
;
----------
[DROP_TABLE] DROP TABLE testpub_gencol
;
----------
[CREATE_PROG_OBJ] create function sillyaddone(int) returns int language plpgsql as
$$ declare r mutable; begin r.f1 := $1; return r.f1 + 1; end $$
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table mlparted attach partition mlparted4 for values from (1, 30) to (1, 40)
;
----------
[DROP_PUB_SUB] -- ok

DROP PUBLICATION testpub2
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE 'mercury'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bad_cmp1 (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT
			CASE WHEN ($1 = 768 AND $2 = 769) OR
					  ($1 = 769 AND $2 = 768) THEN 0
				 ELSE public.ok_cmp($1, $2)
			END;
	$$
;
----------
[CREATE_PROG_OBJ] -- negator
CREATE OPERATOR !==== (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc7cc(_x int)
LANGUAGE plpgsql
AS $$
DECLARE _a int; _b numeric;
BEGIN
  CALL test_proc7c(_x, _a, _b);
  RAISE NOTICE '_x: %,_a: %, _b: %', _x, _a, _b;
END
$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_stmt_before before insert on rem2
	for each statement execute procedure trigger_func()
;
----------
[CREATE_TABLE] /* End test case for bug #17409 */

/* Test case for bug #18970 */

create table attbl(a int)
;
----------
[DROP_TABLE] DROP TABLE brin_timestamp_test
;
----------
[DROP_TABLE] DROP TABLE ruletest1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted3" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest(col int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_class
;
----------
[ADD_INDEX] create index ab_a2_b3_a_idx on ab_a2_b3 (a)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_12.pg_temp_26295_2, pg_temp_12.pg_temp_26295
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = isbn,
	RIGHTARG = isbn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE public.tt(a int, b int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_pslot_bu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.slotname != old.slotname then
        delete from PSlot where slotname = old.slotname;
	insert into PSlot (
		    slotname,
		    pfname,
		    slotlink,
		    backlink
		) values (
		    new.slotname,
		    new.pfname,
		    new.slotlink,
		    new.backlink
		);
        return null;
    end if;
    return new;
end;
$$
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX tbl_include_box_idx_unique ON tbl_include_box using btree (c1, c2) INCLUDE (c3, c4)
;
----------
[ALTER_TABLE] alter table atacc1 SET WITHOUT OIDS
;
----------
[ADMIN_TABLE] -- Test that we return correct results when using the skip fetch optimization
-- VACUUM FREEZE will set all the pages in the relation all-visible, enabling
-- the optimization.
VACUUM (FREEZE) bmscantest
;
----------
[CREATE_TABLE] -- Test error handling, if accessing one of the foreign partitions errors out
CREATE FOREIGN TABLE async_p_broken PARTITION OF async_pt FOR VALUES FROM (10000) TO (10001)
  SERVER loopback OPTIONS (table_name 'non_existent_table')
;
----------
[CREATE_PROG_OBJ] -- Test pg_routine_oid
create function current_function(text)
returns regprocedure as $$
declare
  fn_oid regprocedure;
begin
  get diagnostics fn_oid = pg_routine_oid;
  return fn_oid;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tftest(int) returns table(a int, b int) as $$
SELECT $1, $1+i FROM generate_series(1,5) g(i);
$$ LANGUAGE sql IMMUTABLE STRICT
;
----------
[DROP_PROG_OBJ] DROP FUNCTION dup(f1 anycompatiblerange)
;
----------
[ADMIN_TABLE] vacuum smalltbl
;
----------
[CREATE_TABLE] -- invalid use of set-returning function
CREATE TABLE default_expr_agg (a int DEFAULT (generate_series(1,3)))
;
----------
[CREATE_TABLE] CREATE TABLE persons4 OF person_type (
    name WITH OPTIONS NOT NULL,
    name WITH OPTIONS DEFAULT ''  -- error, specified more than once
)
;
----------
[CREATE_POLICY] CREATE POLICY p5 ON dump_test.test_table
						   FOR DELETE TO regress_dump_test_role USING (true)
;
----------
[ADMIN_TABLE] ANALYZE slow_emp4000
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mcv_lists_arrays" (
    "a" "text"[],
    "b" numeric[],
    "c" integer[]
)
WITH ("autovacuum_enabled"='off')
;
