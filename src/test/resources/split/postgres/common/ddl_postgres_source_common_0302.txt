ANALYZE pg_catalog.pg_event_trigger
;
DROP TABLE testschema.test_default_tab_p
;
CREATE FUNCTION functest_C_2(int) RETURNS bool LANGUAGE 'sql'
       SECURITY DEFINER AS 'SELECT $1 = 0'
;
/*
 * 5.20
 * COLUMN_UDT_USAGE view
 */

CREATE VIEW column_udt_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(coalesce(nbt.nspname, nt.nspname) AS sql_identifier) AS udt_schema,
           CAST(coalesce(bt.typname, t.typname) AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_attribute a, pg_class c, pg_namespace nc,
         (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid))
           LEFT JOIN (pg_type bt JOIN pg_namespace nbt ON (bt.typnamespace = nbt.oid))
           ON (t.typtype = 'd' AND t.typbasetype = bt.oid)

    WHERE a.attrelid = c.oid
          AND a.atttypid = t.oid
          AND nc.oid = c.relnamespace
          AND a.attnum > 0 AND NOT a.attisdropped
          AND c.relkind in ('r', 'v', 'f', 'p')
          AND pg_has_role(coalesce(bt.typowner, t.typowner), 'USAGE')
;
create table pk52 partition of pk5 for values from (4500) to (5000)
;
CREATE UNIQUE INDEX mvtest_tvmm_pred ON mvtest_tvmm (grandtot) WHERE grandtot < 0
;
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('create table', 'CREATE FUNCTION')
   execute procedure test_event_trigger()
;
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('create table', 'create skunkcabbage')
   execute procedure test_event_trigger()
;
create trigger check_fkeys2_fkey_restrict
	after delete or update on fkeys2
	for each row
	execute procedure check_foreign_key (1, 'restrict', 'pkey23', 'fkeys', 'fkey3')
;
create domain vchar4 varchar(4)
;
CREATE FUNCTION "public"."explain_memoize"("query" "text", "hide_hitmiss" boolean) RETURNS SETOF "text"
    LANGUAGE "plpgsql"
    AS $$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off) %s',
            query)
    loop
        if hide_hitmiss = true then
                ln := regexp_replace(ln, 'Hits: 0', 'Hits: Zero');
                ln := regexp_replace(ln, 'Hits: \d+', 'Hits: N');
                ln := regexp_replace(ln, 'Misses: 0', 'Misses: Zero');
                ln := regexp_replace(ln, 'Misses: \d+', 'Misses: N');
        end if;
        ln := regexp_replace(ln, 'Evictions: 0', 'Evictions: Zero');
        ln := regexp_replace(ln, 'Evictions: \d+', 'Evictions: N');
        ln := regexp_replace(ln, 'Memory Usage: \d+', 'Memory Usage: N');
	ln := regexp_replace(ln, 'Heap Fetches: \d+', 'Heap Fetches: N');
	ln := regexp_replace(ln, 'loops=\d+', 'loops=N');
        return next ln;
    end loop;
end;
$$
;
CREATE TABLE c2 () INHERITS (p)
;
ALTER TABLE pg_temp_14.pg_temp_27052_2 ADD COLUMN newdata pg_temp_14.pg_temp_27052
;
CREATE TABLE distinct_group_2 AS
SELECT DISTINCT (g%1000)::text FROM generate_series(0,9999) g
;
ANALYZE pg_temp_9.pg_temp_27112
;
TRUNCATE temporal_partitioned_rng, temporal_partitioned_fk_rng2rng
;
ANALYZE dump_test.has_stats, dump_test.has_stats_mv
;
-- String
ALTER INDEX dummy_test_idx SET (option_string_val = 4)
;
CREATE FUNCTION test_lfind8()
	RETURNS pg_catalog.void
	AS '$libdir/test_lfind' LANGUAGE C
;
DROP TABLE join_tbl
;
CREATE TABLE NAME_TBL(f1 name)
;
CREATE TABLE c (cc TEXT) INHERITS (a)
;
ALTER FOREIGN TABLE foreign_tbl NO INHERIT parent_tbl
;
ALTER TABLE ONLY "fkpart6"."pk1" ATTACH PARTITION "fkpart6"."pk11" FOR VALUES FROM (1) TO (50)
;
CREATE FOREIGN TABLE ft_part1
  PARTITION OF lt1 FOR VALUES FROM (0) TO (1000) SERVER s0
;
drop index trgm_idx
;
CREATE TABLE "public"."idxpart32" (
    "a" integer
)
;
DROP SCHEMA IF EXISTS split_lock_next CASCADE
;
VACUUM (PARALLEL 2) pg_catalog.pg_largeobject_metadata
;
CREATE FUNCTION _int_contains(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE fk_partitioned_fk (b int, a int) PARTITION BY RANGE (a, b)
;
CREATE FUNCTION equipment_named_ambiguous_2a(hobby text)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where hobby = equipment_named_ambiguous_2a.hobby'
   LANGUAGE SQL
;
CREATE TABLE test_stats_temp AS SELECT 17
;
--
-- Tests for mismatched types
--
-- Basic one column, two table setup
CREATE TABLE PKTABLE (ptest1 int PRIMARY KEY)
;
-- tablespace should not change even if there is an index rewrite
ALTER TABLE testschema.test_default_tab_p ALTER val TYPE int
;
DROP TABLE logged2
;
-- ===================================================================
-- local type can be different from remote type in some cases,
-- in particular if similarly-named operators do equivalent things
-- ===================================================================
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE text
;
CREATE TABLE cwi_test( a int , b varchar(10), c char)
;
ALTER SEQUENCE sequence_test2 RESTART WITH 24
  NO CYCLE
;
-- ************************************************************
-- * AFTER INSERT or UPDATE or DELETE on Hub
-- *	- insert/delete/rename slots as required
-- ************************************************************
create function tg_hub_a() returns trigger as '
declare
    hname	text;
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
	dummy := tg_hub_adjustslots(new.name, 0, new.nslots);
	return new;
    end if;
    if tg_op = ''UPDATE'' then
	if new.name != old.name then
	    update HSlot set hubname = new.name where hubname = old.name;
	end if;
	dummy := tg_hub_adjustslots(new.name, old.nslots, new.nslots);
	return new;
    end if;
    if tg_op = ''DELETE'' then
	dummy := tg_hub_adjustslots(old.name, old.nslots, 0);
	return old;
    end if;
end;
' language plpgsql
;
--
-- Test FOREACH over arrays
--

create function foreach_test(anyarray)
returns void as $$
declare x int;
begin
  foreach x in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
CREATE TABLE regress_pg_dump_schema.test_table (
	col1 int,
	col2 int check (col2 > 0)
)
;
CREATE INDEX tenk1_thous_tenthous ON tenk1 (thousand, tenthous)
;
-- pg_stat_have_stats returns true for reindex CONCURRENTLY
CREATE index stats_test_idx1 on stats_test_tab1(a)
;
CREATE DATABASE newdb TEMPLATE template_db STRATEGY=WAL_LOG
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_depend
;
CREATE TABLE part_1 (
	a int NOT NULL,
	b char(2) COLLATE "C",
	CONSTRAINT check_a CHECK (a > 0)
)
;
CREATE FUNCTION noisy_oper(p_comment text, p_a anynonarray, p_op text, p_b anynonarray)
 RETURNS bool LANGUAGE plpgsql AS $$
 DECLARE
  r bool;
  BEGIN
  EXECUTE format('SELECT $1 %s $2', p_op) INTO r USING p_a, p_b;
  RAISE WARNING '%: % % % % %: %', p_comment, pg_typeof(p_a), p_a, p_op, pg_typeof(p_b), p_b, r;
  RETURN r;
  END;$$
;
CREATE SUBSCRIPTION "pg_createsubscriber_16384_c2d526de" CONNECTION  E'host=''/tmp/nxZdRqejP7'' port=13926 dbname=''regression\\\\"\\\\	 !"#$%&\\''()*+,-\\\\\\\\"\\\\\\\\\\\\''' PUBLICATION "pub1" WITH (create_slot = false, enabled = false, slot_name = 'replslot1', copy_data = false, two_phase = true)
;
create domain intdom1 as int
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_event_trigger
;
----------
[DROP_TABLE] DROP TABLE testschema.test_default_tab_p
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_C_2(int) RETURNS bool LANGUAGE 'sql'
       SECURITY DEFINER AS 'SELECT $1 = 0'
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.20
 * COLUMN_UDT_USAGE view
 */

CREATE VIEW column_udt_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(coalesce(nbt.nspname, nt.nspname) AS sql_identifier) AS udt_schema,
           CAST(coalesce(bt.typname, t.typname) AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(a.attname AS sql_identifier) AS column_name

    FROM pg_attribute a, pg_class c, pg_namespace nc,
         (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid))
           LEFT JOIN (pg_type bt JOIN pg_namespace nbt ON (bt.typnamespace = nbt.oid))
           ON (t.typtype = 'd' AND t.typbasetype = bt.oid)

    WHERE a.attrelid = c.oid
          AND a.atttypid = t.oid
          AND nc.oid = c.relnamespace
          AND a.attnum > 0 AND NOT a.attisdropped
          AND c.relkind in ('r', 'v', 'f', 'p')
          AND pg_has_role(coalesce(bt.typowner, t.typowner), 'USAGE')
;
----------
[CREATE_TABLE] create table pk52 partition of pk5 for values from (4500) to (5000)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX mvtest_tvmm_pred ON mvtest_tvmm (grandtot) WHERE grandtot < 0
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('create table', 'CREATE FUNCTION')
   execute procedure test_event_trigger()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('create table', 'create skunkcabbage')
   execute procedure test_event_trigger()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger check_fkeys2_fkey_restrict
	after delete or update on fkeys2
	for each row
	execute procedure check_foreign_key (1, 'restrict', 'pkey23', 'fkeys', 'fkey3')
;
----------
[CREATE_TYPE] create domain vchar4 varchar(4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_memoize"("query" "text", "hide_hitmiss" boolean) RETURNS SETOF "text"
    LANGUAGE "plpgsql"
    AS $$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off) %s',
            query)
    loop
        if hide_hitmiss = true then
                ln := regexp_replace(ln, 'Hits: 0', 'Hits: Zero');
                ln := regexp_replace(ln, 'Hits: \d+', 'Hits: N');
                ln := regexp_replace(ln, 'Misses: 0', 'Misses: Zero');
                ln := regexp_replace(ln, 'Misses: \d+', 'Misses: N');
        end if;
        ln := regexp_replace(ln, 'Evictions: 0', 'Evictions: Zero');
        ln := regexp_replace(ln, 'Evictions: \d+', 'Evictions: N');
        ln := regexp_replace(ln, 'Memory Usage: \d+', 'Memory Usage: N');
	ln := regexp_replace(ln, 'Heap Fetches: \d+', 'Heap Fetches: N');
	ln := regexp_replace(ln, 'loops=\d+', 'loops=N');
        return next ln;
    end loop;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE c2 () INHERITS (p)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_14.pg_temp_27052_2 ADD COLUMN newdata pg_temp_14.pg_temp_27052
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE distinct_group_2 AS
SELECT DISTINCT (g%1000)::text FROM generate_series(0,9999) g
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_27112
;
----------
[TRUNCATE_TABLE] TRUNCATE temporal_partitioned_rng, temporal_partitioned_fk_rng2rng
;
----------
[ADMIN_TABLE] ANALYZE dump_test.has_stats, dump_test.has_stats_mv
;
----------
[ALTER_INDEX] -- String
ALTER INDEX dummy_test_idx SET (option_string_val = 4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_lfind8()
	RETURNS pg_catalog.void
	AS '$libdir/test_lfind' LANGUAGE C
;
----------
[DROP_TABLE] DROP TABLE join_tbl
;
----------
[CREATE_TABLE] CREATE TABLE NAME_TBL(f1 name)
;
----------
[CREATE_TABLE] CREATE TABLE c (cc TEXT) INHERITS (a)
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE foreign_tbl NO INHERIT parent_tbl
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart6"."pk1" ATTACH PARTITION "fkpart6"."pk11" FOR VALUES FROM (1) TO (50)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft_part1
  PARTITION OF lt1 FOR VALUES FROM (0) TO (1000) SERVER s0
;
----------
[DROP_INDEX] drop index trgm_idx
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart32" (
    "a" integer
)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_lock_next CASCADE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_contains(_int4, _int4)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk (b int, a int) PARTITION BY RANGE (a, b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION equipment_named_ambiguous_2a(hobby text)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where hobby = equipment_named_ambiguous_2a.hobby'
   LANGUAGE SQL
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test_stats_temp AS SELECT 17
;
----------
[CREATE_TABLE] --
-- Tests for mismatched types
--
-- Basic one column, two table setup
CREATE TABLE PKTABLE (ptest1 int PRIMARY KEY)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- tablespace should not change even if there is an index rewrite
ALTER TABLE testschema.test_default_tab_p ALTER val TYPE int
;
----------
[DROP_TABLE] DROP TABLE logged2
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ===================================================================
-- local type can be different from remote type in some cases,
-- in particular if similarly-named operators do equivalent things
-- ===================================================================
ALTER FOREIGN TABLE ft1 ALTER COLUMN c8 TYPE text
;
----------
[CREATE_TABLE] CREATE TABLE cwi_test( a int , b varchar(10), c char)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test2 RESTART WITH 24
  NO CYCLE
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * AFTER INSERT or UPDATE or DELETE on Hub
-- *	- insert/delete/rename slots as required
-- ************************************************************
create function tg_hub_a() returns trigger as '
declare
    hname	text;
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
	dummy := tg_hub_adjustslots(new.name, 0, new.nslots);
	return new;
    end if;
    if tg_op = ''UPDATE'' then
	if new.name != old.name then
	    update HSlot set hubname = new.name where hubname = old.name;
	end if;
	dummy := tg_hub_adjustslots(new.name, old.nslots, new.nslots);
	return new;
    end if;
    if tg_op = ''DELETE'' then
	dummy := tg_hub_adjustslots(old.name, old.nslots, 0);
	return old;
    end if;
end;
' language plpgsql
;
----------
[CREATE_PROG_OBJ] --
-- Test FOREACH over arrays
--

create function foreach_test(anyarray)
returns void as $$
declare x int;
begin
  foreach x in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE regress_pg_dump_schema.test_table (
	col1 int,
	col2 int check (col2 > 0)
)
;
----------
[ADD_INDEX] CREATE INDEX tenk1_thous_tenthous ON tenk1 (thousand, tenthous)
;
----------
[ADD_INDEX] -- pg_stat_have_stats returns true for reindex CONCURRENTLY
CREATE index stats_test_idx1 on stats_test_tab1(a)
;
----------
[CREATE_CATALOG] CREATE DATABASE newdb TEMPLATE template_db STRATEGY=WAL_LOG
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_depend
;
----------
[CREATE_TABLE] CREATE TABLE part_1 (
	a int NOT NULL,
	b char(2) COLLATE "C",
	CONSTRAINT check_a CHECK (a > 0)
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION noisy_oper(p_comment text, p_a anynonarray, p_op text, p_b anynonarray)
 RETURNS bool LANGUAGE plpgsql AS $$
 DECLARE
  r bool;
  BEGIN
  EXECUTE format('SELECT $1 %s $2', p_op) INTO r USING p_a, p_b;
  RAISE WARNING '%: % % % % %: %', p_comment, pg_typeof(p_a), p_a, p_op, pg_typeof(p_b), p_b, r;
  RETURN r;
  END;$$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION "pg_createsubscriber_16384_c2d526de" CONNECTION  E'host=''/tmp/nxZdRqejP7'' port=13926 dbname=''regression\\\\"\\\\	 !"#$%&\\''()*+,-\\\\\\\\"\\\\\\\\\\\\''' PUBLICATION "pub1" WITH (create_slot = false, enabled = false, slot_name = 'replslot1', copy_data = false, two_phase = true)
;
----------
[CREATE_TYPE] create domain intdom1 as int
;
