CREATE SUBSCRIPTION regress_sub5 CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION regress_pub5
;
CREATE TEXT SEARCH DICTIONARY alt_ts_dict2 (template=simple)
;
CREATE FUNCTION seclabel_four() RETURNS integer AS $$SELECT 4$$ language sql
;
ALTER TABLE ONLY "generated_stored_tests"."gtest1_1" INHERIT "generated_stored_tests"."gtest1"
;
CREATE FUNCTION isnge(issn13, issn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE INDEX iix ON ihighway USING btree (name text_ops)
;
--
-- hash based crosstab
--
create table cth(id serial, rowid text, rowdt timestamp, attribute text, val text)
;
CREATE FUNCTION gbt_ts_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- Test index handling in alter table column type (cf. bugs #15835, #15865)
create table anothertab(f1 int primary key, f2 int unique,
                        f3 int, f4 int, f5 int)
;
-- clean up
DROP EVENT TRIGGER always_start, always_end, always_drop, always_rewrite
;
ALTER INDEX pg_toast.pg_toast_30433_index RENAME TO reind_con_toast_idx
;
CREATE TEXT SEARCH DICTIONARY hunspell_invalid_3 (
						Template=ispell,
						DictFile=hunspell_sample_num,
						AffFile=ispell_sample
)
;
-- Should fail. Invalid values for ALTER OPERATOR FAMILY .. ADD / DROP
CREATE OPERATOR FAMILY alt_opf4 USING btree
;
ALTER TABLE tx2 DROP COLUMN c
;
CREATE OPERATOR = (procedure = inline_eq,
                   leftarg = foodomain, rightarg = foodomain)
;
CLUSTER VERBOSE
;
CALL priv_testproc1(6)
;
CREATE INDEX t1_brin ON s2.t1 USING BRIN (i)
;
CREATE TABLE test (c1 int) PARTITION BY RANGE (c1)
;
ALTER TABLE split_alter_table.typed_probe NOT OF
;
DROP RULE test_rule_exists ON no_such_table
;
CREATE UNIQUE INDEX onek_nulltest ON onek_with_null (unique2  nulls first,unique1)
;
DROP TYPE IF EXISTS test_type_exists
;
DROP TABLE IF EXISTS d_listp, d_listp1, d_listp2
;
CREATE OPERATOR CLASS gist_enum_ops
DEFAULT FOR TYPE anyenum USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_enum_consistent (internal, anyenum, int2, oid, internal),
	FUNCTION	2	gbt_enum_union (internal, internal),
	FUNCTION	3	gbt_enum_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_enum_penalty (internal, internal, internal),
	FUNCTION	6	gbt_enum_picksplit (internal, internal),
	FUNCTION	7	gbt_enum_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
-- The point opclass's choose method only uses the spgMatchNode action,
-- so the other actions are not tested by the above. Create an index using
-- text opclass, which uses the others actions.

create table spgist_text_tbl(id int4, t text)
;
-- Test that HEAP2_MULTI_INSERT insertions with and without toasted
-- columns are handled correctly
CREATE TABLE toasted_copy (
    id int primary key, -- no default, copy didn't use to handle that with multi inserts
    data text
)
;
CREATE FOREIGN TABLE addr_nsp.genftable (a int) SERVER addr_fserv
;
ANALYZE public.gist_point_tbl
;
CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE TRIGGER schema_trig BEFORE INSERT ON schema_not_existing.tab
  EXECUTE FUNCTION schema_trig.no_func()
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=16262 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION pub1
;
DROP TABLE test_squash
;
CREATE FUNCTION "generated_virtual_tests"."gtest_trigger_func4"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  NEW.a = 10;
  NEW.b = 300;
  RETURN NEW;
END;
$$
;
--
-- UPC operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE TABLE "public"."beta_pos_p2" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
CREATE TABLE "public"."prt1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("a")
;
ANALYZE public.persons
;
create table events_child () inherits (events)
;
ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        7       %> (text, text),
        FUNCTION        6      (text,text) gin_trgm_triconsistent (internal, int2, text, int4, internal, internal, internal)
;
CREATE FUNCTION dblink_close (text, boolean)
RETURNS text
AS '$libdir/dblink','dblink_close'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
-- Test grammar for configurations
CREATE TEXT SEARCH CONFIGURATION dummy_tst (COPY=english)
;
CREATE TEMP TABLE pg_temp_3.pg_temp_16652_2 (tid pg_catalog.tid)
;
DROP TRIGGER insert_a ON main_table
;
CREATE FUNCTION isngt(isbn13, isbn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE INDEX t2_spgist ON s4.t2 USING SPGIST (ir)
;
ALTER TABLE t ADD COLUMN x int
;
--
-- Constraint triggers and partitioned tables
create table parted_constr_ancestor (a int, b text)
  partition by range (b)
;
ALTER PUBLICATION tap_pub ADD TABLE tab_rep, tab_full, tab_full2, tab_mixed, tab_include, tab_nothing, tab_full_pk, tab_no_replidentity_index, tab_no_col
;
COMMENT ON TEXT SEARCH DICTIONARY dump_test.alt_ts_dict1
					   IS 'comment on text search dictionary'
;
CREATE VIEW pg_stat_gssapi AS
    SELECT
            S.pid,
            S.gss_auth AS gss_authenticated,
            S.gss_princ AS principal,
            S.gss_enc AS encrypted
    FROM pg_stat_get_activity(NULL) AS S
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub5 CONNECTION 'port=30152 host=/tmp/mhtoQcUS4B dbname=postgres' PUBLICATION regress_pub5
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY alt_ts_dict2 (template=simple)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seclabel_four() RETURNS integer AS $$SELECT 4$$ language sql
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_stored_tests"."gtest1_1" INHERIT "generated_stored_tests"."gtest1"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(issn13, issn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX iix ON ihighway USING btree (name text_ops)
;
----------
[CREATE_TABLE] --
-- hash based crosstab
--
create table cth(id serial, rowid text, rowdt timestamp, attribute text, val text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_ts_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] -- Test index handling in alter table column type (cf. bugs #15835, #15865)
create table anothertab(f1 int primary key, f2 int unique,
                        f3 int, f4 int, f5 int)
;
----------
[DROP_TRIGGER] -- clean up
DROP EVENT TRIGGER always_start, always_end, always_drop, always_rewrite
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30433_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hunspell_invalid_3 (
						Template=ispell,
						DictFile=hunspell_sample_num,
						AffFile=ispell_sample
)
;
----------
[CREATE_PROG_OBJ] -- Should fail. Invalid values for ALTER OPERATOR FAMILY .. ADD / DROP
CREATE OPERATOR FAMILY alt_opf4 USING btree
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tx2 DROP COLUMN c
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (procedure = inline_eq,
                   leftarg = foodomain, rightarg = foodomain)
;
----------
[ADMIN_TABLE] CLUSTER VERBOSE
;
----------
[CALL_PROG_OBJ] CALL priv_testproc1(6)
;
----------
[ADD_INDEX] CREATE INDEX t1_brin ON s2.t1 USING BRIN (i)
;
----------
[CREATE_TABLE] CREATE TABLE test (c1 int) PARTITION BY RANGE (c1)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.typed_probe NOT OF
;
----------
[DROP_POLICY] DROP RULE test_rule_exists ON no_such_table
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX onek_nulltest ON onek_with_null (unique2  nulls first,unique1)
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS test_type_exists
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS d_listp, d_listp1, d_listp2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_enum_ops
DEFAULT FOR TYPE anyenum USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_enum_consistent (internal, anyenum, int2, oid, internal),
	FUNCTION	2	gbt_enum_union (internal, internal),
	FUNCTION	3	gbt_enum_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_enum_penalty (internal, internal, internal),
	FUNCTION	6	gbt_enum_picksplit (internal, internal),
	FUNCTION	7	gbt_enum_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
----------
[CREATE_TABLE] -- The point opclass's choose method only uses the spgMatchNode action,
-- so the other actions are not tested by the above. Create an index using
-- text opclass, which uses the others actions.

create table spgist_text_tbl(id int4, t text)
;
----------
[CREATE_TABLE] -- Test that HEAP2_MULTI_INSERT insertions with and without toasted
-- columns are handled correctly
CREATE TABLE toasted_copy (
    id int primary key, -- no default, copy didn't use to handle that with multi inserts
    data text
)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE addr_nsp.genftable (a int) SERVER addr_fserv
;
----------
[ADMIN_TABLE] ANALYZE public.gist_point_tbl
;
----------
[CREATE_SCHEMA(CALL_PROG_OBJ)] CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE TRIGGER schema_trig BEFORE INSERT ON schema_not_existing.tab
  EXECUTE FUNCTION schema_trig.no_func()
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=16262 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION pub1
;
----------
[DROP_TABLE] DROP TABLE test_squash
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "generated_virtual_tests"."gtest_trigger_func4"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  NEW.a = 10;
  NEW.b = 300;
  RETURN NEW;
END;
$$
;
----------
[CREATE_PROG_OBJ] --
-- UPC operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."beta_pos_p2" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("a")
;
----------
[ADMIN_TABLE] ANALYZE public.persons
;
----------
[CREATE_TABLE] create table events_child () inherits (events)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        7       %> (text, text),
        FUNCTION        6      (text,text) gin_trgm_triconsistent (internal, int2, text, int4, internal, internal, internal)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_close (text, boolean)
RETURNS text
AS '$libdir/dblink','dblink_close'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_POLICY] -- Test grammar for configurations
CREATE TEXT SEARCH CONFIGURATION dummy_tst (COPY=english)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_16652_2 (tid pg_catalog.tid)
;
----------
[DROP_TRIGGER] DROP TRIGGER insert_a ON main_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(isbn13, isbn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX t2_spgist ON s4.t2 USING SPGIST (ir)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t ADD COLUMN x int
;
----------
[CREATE_TABLE] --
-- Constraint triggers and partitioned tables
create table parted_constr_ancestor (a int, b text)
  partition by range (b)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub ADD TABLE tab_rep, tab_full, tab_full2, tab_mixed, tab_include, tab_nothing, tab_full_pk, tab_no_replidentity_index, tab_no_col
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY dump_test.alt_ts_dict1
					   IS 'comment on text search dictionary'
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_gssapi AS
    SELECT
            S.pid,
            S.gss_auth AS gss_authenticated,
            S.gss_princ AS principal,
            S.gss_enc AS encrypted
    FROM pg_stat_get_activity(NULL) AS S
;
