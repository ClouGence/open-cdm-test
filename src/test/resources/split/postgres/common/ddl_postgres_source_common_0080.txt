DROP OPERATOR FAMILY alt_opf14 USING btree
;
CREATE FUNCTION gbt_timetz_consistent(internal,timetz,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE INDEX ON mb USING gist(s gist_trgm_ops)
;
CREATE PUBLICATION tap_pub_forall FOR ALL TABLES
;
COMMENT ON DATABASE "postgres" IS 'default administrative connection database'
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_conversion
;
CREATE TABLE trans_barbaz (a int)
;
create domain dia as int[]
;
CREATE TABLE quuux (a int, b text) PARTITION BY LIST (a)
;
CREATE PUBLICATION dump_pub_2ct FOR TABLE ONLY pubme.t0, pubme.t1
;
create table part_default_p1 partition of part_default for values from (20) to (30)
;
CREATE FUNCTION cube_coord(cube, int4)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE INDEX test_hash_part_idx ON test_hash_part USING hash(b)
;
CREATE TEMP TABLE jsbpoptest (js jsonb)
;
CREATE SEQUENCE "public"."sj_t2_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
CREATE TEXT SEARCH TEMPLATE intdict_template (
        LEXIZE = dintdict_lexize,
	INIT   = dintdict_init
)
;
ALTER TABLE ONLY "public"."b_star" INHERIT "public"."a_star"
;
CREATE RULE "rtest_nothn_r4" AS
    ON INSERT TO "public"."rtest_nothn2" DO INSTEAD NOTHING
;
DROP POLICY p1 ON dob_t2
;
drop table defcheck
;
DROP FUNCTION test2arr()
;
/*
 * text search configuration for yiddish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * yiddish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY yiddish_stem
	(TEMPLATE = snowball, Language = yiddish )
;
CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=28775 host=/tmp/A2VbQelPDP dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
CREATE TABLE ptnowner1 PARTITION OF ptnowner FOR VALUES IN (1)
;
CREATE INDEX t1_gin ON s1.t1 USING GIN (ia)
;
ALTER TABLE testpub_tbl8_1 DROP CONSTRAINT testpub_tbl8_1_pkey
;
ALTER TABLE notnull_part1_upg ATTACH PARTITION notnull_part1_3_upg FOR VALUES IN (NULL,5)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = upc,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
-- Test with a trigger returning NULL
CREATE FUNCTION trig_null() RETURNS TRIGGER AS $$
  BEGIN
    RETURN NULL;
  END
$$ language plpgsql
;
ALTER TYPE "public"."insenum" ADD VALUE 'i6'
;
TRUNCATE test_part_c
;
DROP SCHEMA split_pub18_b CASCADE
;
DROP TABLE  BOOLTBL4
;
DROP TABLE stats_plan_test
;
create function raise_test3(int) returns int as $$
begin
    raise notice 'This message has no parameters (despite having %% signs in it)!';
    return $1;
end;
$$ language plpgsql
;
-- Add binary input/output handlers
CREATE FUNCTION cube_recv(internal)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE OPERATOR "public".@#@ (
    FUNCTION = "factorial",
    RIGHTARG = bigint
)
;
CREATE SUBSCRIPTION testsub1 CONNECTION 'port=14074 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION testpub1
;
ALTER INDEX pg_toast.pg_toast_27993_index RENAME TO reind_con_toast_idx
;
CREATE TEMP TABLE point_tbl AS SELECT * FROM public.point_tbl
;
DROP TABLE syscol_table
;
create function parallel_safe_volatile(a int) returns int as
  $$ begin return a; end; $$ parallel safe volatile language plpgsql
;
CREATE INDEX collate_test1_idx5 ON collate_test1 (a COLLATE "POSIX")
;
ALTER TEXT SEARCH CONFIGURATION italian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
DROP TABLE testpub_parted1, testpub_parted2
;
-- OK
create event trigger regress_event_trigger on ddl_command_start
   execute procedure test_event_trigger()
;
CREATE TABLE pa_targetp PARTITION OF pa_target DEFAULT
;
DROP TABLE bttest_a
;
-- test correct handling of GENERATED column that's only in child
CREATE TABLE gtestp (f1 int)
;
create temporary table gexec_test(a int, b text, c date, d float)
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf14 USING btree
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_timetz_consistent(internal,timetz,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADD_INDEX] CREATE INDEX ON mb USING gist(s gist_trgm_ops)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_forall FOR ALL TABLES
;
----------
[COMMENT_CATALOG] COMMENT ON DATABASE "postgres" IS 'default administrative connection database'
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_conversion
;
----------
[CREATE_TABLE] CREATE TABLE trans_barbaz (a int)
;
----------
[CREATE_TYPE] create domain dia as int[]
;
----------
[CREATE_TABLE] CREATE TABLE quuux (a int, b text) PARTITION BY LIST (a)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION dump_pub_2ct FOR TABLE ONLY pubme.t0, pubme.t1
;
----------
[CREATE_TABLE] create table part_default_p1 partition of part_default for values from (20) to (30)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_coord(cube, int4)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX test_hash_part_idx ON test_hash_part USING hash(b)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE jsbpoptest (js jsonb)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sj_t2_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH TEMPLATE intdict_template (
        LEXIZE = dintdict_lexize,
	INIT   = dintdict_init
)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."b_star" INHERIT "public"."a_star"
;
----------
[CREATE_POLICY] CREATE RULE "rtest_nothn_r4" AS
    ON INSERT TO "public"."rtest_nothn2" DO INSTEAD NOTHING
;
----------
[DROP_POLICY] DROP POLICY p1 ON dob_t2
;
----------
[DROP_TABLE] drop table defcheck
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test2arr()
;
----------
[CREATE_POLICY] /*
 * text search configuration for yiddish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * yiddish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY yiddish_stem
	(TEMPLATE = snowball, Language = yiddish )
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=28775 host=/tmp/A2VbQelPDP dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
----------
[CREATE_TABLE] CREATE TABLE ptnowner1 PARTITION OF ptnowner FOR VALUES IN (1)
;
----------
[ADD_INDEX] CREATE INDEX t1_gin ON s1.t1 USING GIN (ia)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE testpub_tbl8_1 DROP CONSTRAINT testpub_tbl8_1_pkey
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE notnull_part1_upg ATTACH PARTITION notnull_part1_3_upg FOR VALUES IN (NULL,5)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = upc,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_PROG_OBJ] -- Test with a trigger returning NULL
CREATE FUNCTION trig_null() RETURNS TRIGGER AS $$
  BEGIN
    RETURN NULL;
  END
$$ language plpgsql
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i6'
;
----------
[TRUNCATE_TABLE] TRUNCATE test_part_c
;
----------
[DROP_SCHEMA] DROP SCHEMA split_pub18_b CASCADE
;
----------
[DROP_TABLE] DROP TABLE  BOOLTBL4
;
----------
[DROP_TABLE] DROP TABLE stats_plan_test
;
----------
[CREATE_PROG_OBJ] create function raise_test3(int) returns int as $$
begin
    raise notice 'This message has no parameters (despite having %% signs in it)!';
    return $1;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] -- Add binary input/output handlers
CREATE FUNCTION cube_recv(internal)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".@#@ (
    FUNCTION = "factorial",
    RIGHTARG = bigint
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub1 CONNECTION 'port=14074 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION testpub1
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27993_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE point_tbl AS SELECT * FROM public.point_tbl
;
----------
[DROP_TABLE] DROP TABLE syscol_table
;
----------
[CREATE_PROG_OBJ] create function parallel_safe_volatile(a int) returns int as
  $$ begin return a; end; $$ parallel safe volatile language plpgsql
;
----------
[ADD_INDEX] CREATE INDEX collate_test1_idx5 ON collate_test1 (a COLLATE "POSIX")
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION italian ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[DROP_TABLE] DROP TABLE testpub_parted1, testpub_parted2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- OK
create event trigger regress_event_trigger on ddl_command_start
   execute procedure test_event_trigger()
;
----------
[CREATE_TABLE] CREATE TABLE pa_targetp PARTITION OF pa_target DEFAULT
;
----------
[DROP_TABLE] DROP TABLE bttest_a
;
----------
[CREATE_TABLE] -- test correct handling of GENERATED column that's only in child
CREATE TABLE gtestp (f1 int)
;
----------
[CREATE_TABLE] create temporary table gexec_test(a int, b text, c date, d float)
;
