-- good

-- Check that ALTER DOMAIN tests columns of derived types

create domain posint as int4
;
CREATE OPERATOR = (procedure = ad_eq,
                   leftarg = arrdomain, rightarg = arrdomain)
;
CREATE TABLE dump_test.test_seventh_table (
						   col1 int,
						   col2 text,
						   col3 bytea
					   )
;
CREATE TEMP TABLE pg_temp_9.pg_temp_26805_2 (tid pg_catalog.tid)
;
ALTER TABLE table_without_key REPLICA IDENTITY NOTHING
;
CREATE TABLE split_select_lock.parent (
    id integer PRIMARY KEY,
    note text
)
;
DROP TABLE pg_temp_9.pg_temp_26761_2, pg_temp_9.pg_temp_26761
;
DROP PUBLICATION testpub_fortable_forschema
;
DROP TABLE constraint_rename_cache
;
CREATE FUNCTION "public"."tg_wslot_biu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if count(*) = 0 from Room where roomno = new.roomno then
        raise exception 'Room % does not exist', new.roomno;
    end if;
    return new;
end;
$$
;
ALTER INDEX pg_toast.pg_toast_30649_index RENAME TO reind_con_toast_idx
;
-- macaddr check

CREATE TABLE macaddrtmp (a macaddr)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdescription
;
DROP TABLE rls_t
;
DROP FUNCTION get_raw_page(text, text, int4)
;
CREATE TABLE testxmlschema.test2 (z int, y varchar(500), x char(6),
    w numeric(9,2), v smallint, u bigint, t real,
    s time, stz timetz, r timestamp, rtz timestamptz, q date,
    p xml, o testxmldomain, n bool, m bytea, aaa text)
;
VACUUM (PARALLEL 0) pg_catalog.pg_db_role_setting
;
create table permtest_parent (a int, b text, c text) partition by list (a)
;
-- failed (name conflict)
ALTER LANGUAGE alt_lang2 RENAME TO alt_lang3
;
CREATE TABLE conf_tab (a int PRIMARY KEY, b int UNIQUE, c int UNIQUE)
;
CREATE TYPE user_enum AS ENUM ('foo', 'bar', 'buz')
;
ALTER TABLE "S 1"."T 3" SET (autovacuum_enabled = 'false')
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "url" WITH "simple"
;
ALTER TABLE test_part_c_2 REPLICA IDENTITY USING INDEX test_part_c_2_pkey
;
create table text_hashp0 partition of text_hashp for values with (modulus 2, remainder 0)
;
ALTER TABLE pg_toast.pg_toast_30525 RENAME TO reind_con_toast
;
-- fail
drop procedure ptest10(int, int, int)
;
ANALYZE public.emp
;
alter table rewriteme alter column foo type numeric(12,4)
;
ALTER TABLE ctl_stats4_parent DROP COLUMN b
;
alter table tt14t alter column f4 type integer using f4::integer
;
ALTER TABLE attmp ADD COLUMN d float8
;
DROP VIEW atestv3 CASCADE
;
CREATE CAST (text[] AS hstore)
  WITH FUNCTION hstore(text[])
;
ANALYZE pg_temp_11.pg_temp_41099_2
;
ALTER INDEX "fkpart6"."pk_pkey" ATTACH PARTITION "fkpart6"."pk1_pkey"
;
-- disallowed commands

ANALYZE hs1
;
-- can't remove the default once it exists
create or replace function dfunc(a variadic int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
CREATE TABLESPACE regress_dump_tablespace
			OWNER regress_dump_test_role LOCATION ''
;
create table partitioned2
  partition of partitioned for values in ('(2,4)'::partitioned)
;
alter table pk attach partition pk2 for values from (1000) to (2000)
;
ALTER TABLE articles_in_category DROP CONSTRAINT articles_in_category_pkey RESTRICT
;
CREATE FUNCTION isnle(issn13, issn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TYPE cube (
	INTERNALLENGTH = variable,
	INPUT = cube_in,
	OUTPUT = cube_out,
	ALIGNMENT = double
)
;
alter table bigger_than_it_looks set (parallel_workers = 2)
;
CREATE TABLE "public"."test_type_diff2_c2" (
    "int_eight" bigint,
    "int_two" smallint,
    "int_four" bigint
)
;
TRUNCATE TABLE truncate_a
;
create index on idxpart1 (a)
;
ALTER INDEX pg_toast.pg_toast_30815_index RENAME TO reind_con_toast_idx
;
CREATE TABLE tp1 PARTITION OF temporal_partitioned FOR VALUES IN ('[1,2)', '[2,3)')
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] -- good

-- Check that ALTER DOMAIN tests columns of derived types

create domain posint as int4
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (procedure = ad_eq,
                   leftarg = arrdomain, rightarg = arrdomain)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_seventh_table (
						   col1 int,
						   col2 text,
						   col3 bytea
					   )
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_26805_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE] ALTER TABLE table_without_key REPLICA IDENTITY NOTHING
;
----------
[CREATE_TABLE] CREATE TABLE split_select_lock.parent (
    id integer PRIMARY KEY,
    note text
)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_26761_2, pg_temp_9.pg_temp_26761
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_fortable_forschema
;
----------
[DROP_TABLE] DROP TABLE constraint_rename_cache
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_wslot_biu"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if count(*) = 0 from Room where roomno = new.roomno then
        raise exception 'Room % does not exist', new.roomno;
    end if;
    return new;
end;
$$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30649_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- macaddr check

CREATE TABLE macaddrtmp (a macaddr)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdescription
;
----------
[DROP_TABLE] DROP TABLE rls_t
;
----------
[DROP_PROG_OBJ] DROP FUNCTION get_raw_page(text, text, int4)
;
----------
[CREATE_TABLE] CREATE TABLE testxmlschema.test2 (z int, y varchar(500), x char(6),
    w numeric(9,2), v smallint, u bigint, t real,
    s time, stz timetz, r timestamp, rtz timestamptz, q date,
    p xml, o testxmldomain, n bool, m bytea, aaa text)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_db_role_setting
;
----------
[CREATE_TABLE] create table permtest_parent (a int, b text, c text) partition by list (a)
;
----------
[RENAME_PROG_OBJ] -- failed (name conflict)
ALTER LANGUAGE alt_lang2 RENAME TO alt_lang3
;
----------
[CREATE_TABLE] CREATE TABLE conf_tab (a int PRIMARY KEY, b int UNIQUE, c int UNIQUE)
;
----------
[CREATE_TYPE] CREATE TYPE user_enum AS ENUM ('foo', 'bar', 'buz')
;
----------
[ALTER_TABLE] ALTER TABLE "S 1"."T 3" SET (autovacuum_enabled = 'false')
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "url" WITH "simple"
;
----------
[ALTER_TABLE] ALTER TABLE test_part_c_2 REPLICA IDENTITY USING INDEX test_part_c_2_pkey
;
----------
[CREATE_TABLE] create table text_hashp0 partition of text_hashp for values with (modulus 2, remainder 0)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30525 RENAME TO reind_con_toast
;
----------
[DROP_PROG_OBJ] -- fail
drop procedure ptest10(int, int, int)
;
----------
[ADMIN_TABLE] ANALYZE public.emp
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table rewriteme alter column foo type numeric(12,4)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ctl_stats4_parent DROP COLUMN b
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table tt14t alter column f4 type integer using f4::integer
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN d float8
;
----------
[DROP_VIEW] DROP VIEW atestv3 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE CAST (text[] AS hstore)
  WITH FUNCTION hstore(text[])
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41099_2
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart6"."pk_pkey" ATTACH PARTITION "fkpart6"."pk1_pkey"
;
----------
[ADMIN_TABLE] -- disallowed commands

ANALYZE hs1
;
----------
[CREATE_PROG_OBJ] -- can't remove the default once it exists
create or replace function dfunc(a variadic int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_dump_tablespace
			OWNER regress_dump_test_role LOCATION ''
;
----------
[CREATE_TABLE] create table partitioned2
  partition of partitioned for values in ('(2,4)'::partitioned)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table pk attach partition pk2 for values from (1000) to (2000)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE articles_in_category DROP CONSTRAINT articles_in_category_pkey RESTRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(issn13, issn)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TYPE] CREATE TYPE cube (
	INTERNALLENGTH = variable,
	INPUT = cube_in,
	OUTPUT = cube_out,
	ALIGNMENT = double
)
;
----------
[ALTER_TABLE] alter table bigger_than_it_looks set (parallel_workers = 2)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_type_diff2_c2" (
    "int_eight" bigint,
    "int_two" smallint,
    "int_four" bigint
)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE truncate_a
;
----------
[ADD_INDEX] create index on idxpart1 (a)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30815_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE tp1 PARTITION OF temporal_partitioned FOR VALUES IN ('[1,2)', '[2,3)')
;
