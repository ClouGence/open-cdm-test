-- CALL
CREATE OR REPLACE PROCEDURE sum_one(i int) AS $$
DECLARE
  r int;
BEGIN
  SELECT (i + i)::int INTO r;
END; $$ LANGUAGE plpgsql
;
-- If the partition to be attached already has a primary key, fail if
-- it doesn't match the parent's PK.
CREATE TABLE idxpart (c1 INT PRIMARY KEY, c2 INT, c3 VARCHAR(10)) PARTITION BY RANGE(c1)
;
--    P    P        N    P
-- should ERROR: tfp(anyarray, int) not matched by tfp(anyarray, anyelement)
CREATE AGGREGATE myaggn18a(BASETYPE = int, SFUNC = tfp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE OR REPLACE RECURSIVE VIEW nums (n) AS
    VALUES (1)
UNION ALL
    SELECT n+1 FROM nums WHERE n < 6
;
create table tpart(i int, j text) partition by range(i)
;
create table d4_fk (a int references d4_primary)
;
create table strdbase ( strval varchar ( 10 ) )
;
--
--
--
-- numeric ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_numeric_consistent(internal,numeric,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- fail
ALTER TABLE part_rev DROP COLUMN c
;
CREATE TABLE of_tt_enum_type OF tt_enum_type
;
CREATE FUNCTION "public"."no_op_trig_fn"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$begin RETURN NULL; end$$
;
CREATE TEXT SEARCH CONFIGURATION italian
	(PARSER = default)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
CREATE TABLE clstr_tst_s (rf_a SERIAL PRIMARY KEY,
	b INT)
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "file" WITH "simple"
;
-- set PUBLISH_VIA_PARTITION_ROOT to true and test row filter for partitioned
-- table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=1)
;
CREATE TYPE typ1 AS (m1 int, m2 varchar)
;
CREATE TABLE gtest_child (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED)
;
CREATE EVENT TRIGGER on_login_trigger ON login EXECUTE FUNCTION on_login_proc()
;
create temp table numerics(
    id int,
    f_float4 float4,
    f_float8 float8,
    f_numeric numeric
)
;
CREATE TABLE INSERT_TBL (x INT DEFAULT nextval('insert_seq'),
	y TEXT DEFAULT '-NULL-',
	z INT DEFAULT -1 * currval('insert_seq'),
	CONSTRAINT INSERT_TBL_CON CHECK (x >= 3 AND y <> 'check failed' AND x < 8),
	CHECK (x + z = 0))
;
ANALYZE public.transition_table_status
;
drop table grandparent
;
ALTER OPERATOR FAMILY gist_int2_ops USING gist ADD
	FUNCTION	11  (int2, int2) gbt_int2_sortsupport (internal)
;
CREATE TABLE clstr_2 (a INT PRIMARY KEY)
;
/*
 * text search configuration for irish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * irish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
CREATE FUNCTION isnlt(ismn13, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE pg_temp_13.pg_temp_25576_2 ADD COLUMN newdata pg_temp_13.pg_temp_25576
;
CREATE FUNCTION spgist_name_compress(name)
RETURNS text IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
-- Test bitmap AND and OR


-- Generate enough data that we can test the lossy bitmaps.

-- There's 55 tuples per page in the table. 53 is just
-- below 55, so that an index scan with qual a = constant
-- will return at least one hit per page. 59 is just above
-- 55, so that an index scan with qual b = constant will return
-- hits on most but not all pages. 53 and 59 are prime, so that
-- there's a maximum number of a,b combinations in the table.
-- That allows us to test all the different combinations of
-- lossy and non-lossy pages with the minimum amount of data

CREATE TABLE bmscantest (a int, b int, t text)
;
CREATE INDEX "brin_summarize_bloom_idx" ON "public"."brin_summarize_bloom" USING "brin" ("value") WITH ("pages_per_range"='2')
;
-- Now the same test with plpgsql (since it depends on SPI which is different)
create or replace function max_xacttest() returns smallint language plpgsql as
'begin return max(a) from xacttest; end' stable
;
-- other types
create type testtype2 as (a smallint, b bool)
;
ALTER TABLE fprt1_p2 SET (autovacuum_enabled = 'false')
;
CREATE DOMAIN "public"."rgb" AS "public"."rainbow"
	CONSTRAINT "rgb_check" CHECK ((VALUE = ANY (ARRAY['red'::"public"."rainbow", 'green'::"public"."rainbow", 'blue'::"public"."rainbow"])))
;
create database abc with owner = zyc
;
ALTER VIEW rw_view1 SET (security_barrier = true)
;
CREATE TABLE "public"."path_tbl" (
    "f1" "path"
)
;
TRUNCATE emp1
;
DROP OPERATOR === (boolean, real)
;
-- nor used in expressions
CREATE INDEX ON syscol_table ((ctid >= '(1000,0)'))
;
CREATE OPERATOR @ (
        LEFTARG = ltxtquery,
	RIGHTARG = ltree,
	PROCEDURE = ltxtq_rexec,
	COMMUTATOR = '@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE TABLE fk_partitioned_fk_1 (fdrop1 int, fdrop2 int, a int, fdrop3 int, b int)
;
CREATE TABLE part_7_a_null (
	c int,
	d int,
	e int,
	LIKE list_parted2,  
	CONSTRAINT check_b CHECK (b IS NULL OR b = 'a'),
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
)
;
DROP SUBSCRIPTION sub1
;
drop function least_accum(int8, int8)
;
DROP DATABASE template_db
;
CREATE TABLE "public"."tab_core_types" (
    "point" "point",
    "line" "line",
    "lseg" "lseg",
    "box" "box",
    "openedpath" "path",
    "closedpath" "path",
    "polygon" "polygon",
    "circle" "circle",
    "date" "date",
    "time" time without time zone,
    "timestamp" timestamp without time zone,
    "timetz" time with time zone,
    "timestamptz" timestamp with time zone,
    "interval" interval,
    "json" "json",
    "jsonb" "jsonb",
    "jsonpath" "jsonpath",
    "inet" "inet",
    "cidr" "cidr",
    "macaddr8" "macaddr8",
    "macaddr" "macaddr",
    "int2" smallint,
    "int4" integer,
    "int8" bigint,
    "float4" real,
    "float8" double precision,
    "pi" numeric,
    "char" "char",
    "bpchar" "bpchar",
    "varchar" character varying,
    "name" "name",
    "text" "text",
    "bool" boolean,
    "bytea" "bytea",
    "bit" bit(1),
    "varbit" bit varying,
    "money" "money",
    "refcursor" "refcursor",
    "int2vector" "int2vector",
    "oidvector" "oidvector",
    "aclitem" "aclitem",
    "tsvector" "tsvector",
    "tsquery" "tsquery",
    "uuid" "uuid",
    "xid8" "xid8",
    "regclass" "regclass",
    "regtype" "regtype",
    "regrole" "regrole",
    "oid" "oid",
    "tid" "tid",
    "xid" "xid",
    "cid" "cid",
    "txid_snapshot" "txid_snapshot",
    "pg_snapshot" "pg_snapshot",
    "pg_lsn" "pg_lsn",
    "cardinal_number" "information_schema"."cardinal_number",
    "character_data" "information_schema"."character_data",
    "sql_identifier" "information_schema"."sql_identifier",
    "time_stamp" "information_schema"."time_stamp",
    "yes_or_no" "information_schema"."yes_or_no",
    "planets" "public"."planets",
    "insenum" "public"."insenum",
    "int4range" "int4range",
    "int8range" "int8range",
    "float8range" "public"."float8range",
    "numrange" "numrange",
    "textrange" "public"."textrange",
    "cashrange" "public"."cashrange",
    "daterange" "daterange",
    "tsrange" "tsrange",
    "tstzrange" "tstzrange",
    "arrayrange" "public"."arrayrange"
)
;
-- higher level of slicing
create or replace function foreach_test(anyarray)
returns void as $$
declare x int[];
begin
  foreach x slice 2 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- CALL
CREATE OR REPLACE PROCEDURE sum_one(i int) AS $$
DECLARE
  r int;
BEGIN
  SELECT (i + i)::int INTO r;
END; $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] -- If the partition to be attached already has a primary key, fail if
-- it doesn't match the parent's PK.
CREATE TABLE idxpart (c1 INT PRIMARY KEY, c2 INT, c3 VARCHAR(10)) PARTITION BY RANGE(c1)
;
----------
[CREATE_PROG_OBJ] --    P    P        N    P
-- should ERROR: tfp(anyarray, int) not matched by tfp(anyarray, anyelement)
CREATE AGGREGATE myaggn18a(BASETYPE = int, SFUNC = tfp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE RECURSIVE VIEW nums (n) AS
    VALUES (1)
UNION ALL
    SELECT n+1 FROM nums WHERE n < 6
;
----------
[CREATE_TABLE] create table tpart(i int, j text) partition by range(i)
;
----------
[CREATE_TABLE] create table d4_fk (a int references d4_primary)
;
----------
[CREATE_TABLE] create table strdbase ( strval varchar ( 10 ) )
;
----------
[CREATE_PROG_OBJ] --
--
--
-- numeric ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_numeric_consistent(internal,numeric,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|DROP_COLUMN] -- fail
ALTER TABLE part_rev DROP COLUMN c
;
----------
[CREATE_TABLE] CREATE TABLE of_tt_enum_type OF tt_enum_type
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."no_op_trig_fn"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$begin RETURN NULL; end$$
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION italian
	(PARSER = default)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_attrdef
;
----------
[CREATE_TABLE] CREATE TABLE clstr_tst_s (rf_a SERIAL PRIMARY KEY,
	b INT)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "file" WITH "simple"
;
----------
[ALTER_PUB_SUB] -- set PUBLISH_VIA_PARTITION_ROOT to true and test row filter for partitioned
-- table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=1)
;
----------
[CREATE_TYPE] CREATE TYPE typ1 AS (m1 int, m2 varchar)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER on_login_trigger ON login EXECUTE FUNCTION on_login_proc()
;
----------
[CREATE_TABLE] create temp table numerics(
    id int,
    f_float4 float4,
    f_float8 float8,
    f_numeric numeric
)
;
----------
[CREATE_TABLE] CREATE TABLE INSERT_TBL (x INT DEFAULT nextval('insert_seq'),
	y TEXT DEFAULT '-NULL-',
	z INT DEFAULT -1 * currval('insert_seq'),
	CONSTRAINT INSERT_TBL_CON CHECK (x >= 3 AND y <> 'check failed' AND x < 8),
	CHECK (x + z = 0))
;
----------
[ADMIN_TABLE] ANALYZE public.transition_table_status
;
----------
[DROP_TABLE] drop table grandparent
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_int2_ops USING gist ADD
	FUNCTION	11  (int2, int2) gbt_int2_sortsupport (internal)
;
----------
[CREATE_TABLE] CREATE TABLE clstr_2 (a INT PRIMARY KEY)
;
----------
[CREATE_POLICY] /*
 * text search configuration for irish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * irish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ismn13, ean13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_25576_2 ADD COLUMN newdata pg_temp_13.pg_temp_25576
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION spgist_name_compress(name)
RETURNS text IMMUTABLE PARALLEL SAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_TABLE] -- Test bitmap AND and OR


-- Generate enough data that we can test the lossy bitmaps.

-- There's 55 tuples per page in the table. 53 is just
-- below 55, so that an index scan with qual a = constant
-- will return at least one hit per page. 59 is just above
-- 55, so that an index scan with qual b = constant will return
-- hits on most but not all pages. 53 and 59 are prime, so that
-- there's a maximum number of a,b combinations in the table.
-- That allows us to test all the different combinations of
-- lossy and non-lossy pages with the minimum amount of data

CREATE TABLE bmscantest (a int, b int, t text)
;
----------
[ADD_INDEX] CREATE INDEX "brin_summarize_bloom_idx" ON "public"."brin_summarize_bloom" USING "brin" ("value") WITH ("pages_per_range"='2')
;
----------
[CREATE_PROG_OBJ] -- Now the same test with plpgsql (since it depends on SPI which is different)
create or replace function max_xacttest() returns smallint language plpgsql as
'begin return max(a) from xacttest; end' stable
;
----------
[CREATE_TYPE] -- other types
create type testtype2 as (a smallint, b bool)
;
----------
[ALTER_TABLE] ALTER TABLE fprt1_p2 SET (autovacuum_enabled = 'false')
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."rgb" AS "public"."rainbow"
	CONSTRAINT "rgb_check" CHECK ((VALUE = ANY (ARRAY['red'::"public"."rainbow", 'green'::"public"."rainbow", 'blue'::"public"."rainbow"])))
;
----------
[CREATE_CATALOG] create database abc with owner = zyc
;
----------
[ALTER_VIEW] ALTER VIEW rw_view1 SET (security_barrier = true)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."path_tbl" (
    "f1" "path"
)
;
----------
[TRUNCATE_TABLE] TRUNCATE emp1
;
----------
[DROP_PROG_OBJ] DROP OPERATOR === (boolean, real)
;
----------
[ADD_INDEX] -- nor used in expressions
CREATE INDEX ON syscol_table ((ctid >= '(1000,0)'))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @ (
        LEFTARG = ltxtquery,
	RIGHTARG = ltree,
	PROCEDURE = ltxtq_rexec,
	COMMUTATOR = '@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_1 (fdrop1 int, fdrop2 int, a int, fdrop3 int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE part_7_a_null (
	c int,
	d int,
	e int,
	LIKE list_parted2,  
	CONSTRAINT check_b CHECK (b IS NULL OR b = 'a'),
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
)
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION sub1
;
----------
[DROP_PROG_OBJ] drop function least_accum(int8, int8)
;
----------
[DROP_CATALOG] DROP DATABASE template_db
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tab_core_types" (
    "point" "point",
    "line" "line",
    "lseg" "lseg",
    "box" "box",
    "openedpath" "path",
    "closedpath" "path",
    "polygon" "polygon",
    "circle" "circle",
    "date" "date",
    "time" time without time zone,
    "timestamp" timestamp without time zone,
    "timetz" time with time zone,
    "timestamptz" timestamp with time zone,
    "interval" interval,
    "json" "json",
    "jsonb" "jsonb",
    "jsonpath" "jsonpath",
    "inet" "inet",
    "cidr" "cidr",
    "macaddr8" "macaddr8",
    "macaddr" "macaddr",
    "int2" smallint,
    "int4" integer,
    "int8" bigint,
    "float4" real,
    "float8" double precision,
    "pi" numeric,
    "char" "char",
    "bpchar" "bpchar",
    "varchar" character varying,
    "name" "name",
    "text" "text",
    "bool" boolean,
    "bytea" "bytea",
    "bit" bit(1),
    "varbit" bit varying,
    "money" "money",
    "refcursor" "refcursor",
    "int2vector" "int2vector",
    "oidvector" "oidvector",
    "aclitem" "aclitem",
    "tsvector" "tsvector",
    "tsquery" "tsquery",
    "uuid" "uuid",
    "xid8" "xid8",
    "regclass" "regclass",
    "regtype" "regtype",
    "regrole" "regrole",
    "oid" "oid",
    "tid" "tid",
    "xid" "xid",
    "cid" "cid",
    "txid_snapshot" "txid_snapshot",
    "pg_snapshot" "pg_snapshot",
    "pg_lsn" "pg_lsn",
    "cardinal_number" "information_schema"."cardinal_number",
    "character_data" "information_schema"."character_data",
    "sql_identifier" "information_schema"."sql_identifier",
    "time_stamp" "information_schema"."time_stamp",
    "yes_or_no" "information_schema"."yes_or_no",
    "planets" "public"."planets",
    "insenum" "public"."insenum",
    "int4range" "int4range",
    "int8range" "int8range",
    "float8range" "public"."float8range",
    "numrange" "numrange",
    "textrange" "public"."textrange",
    "cashrange" "public"."cashrange",
    "daterange" "daterange",
    "tsrange" "tsrange",
    "tstzrange" "tstzrange",
    "arrayrange" "public"."arrayrange"
)
;
----------
[CREATE_PROG_OBJ] -- higher level of slicing
create or replace function foreach_test(anyarray)
returns void as $$
declare x int[];
begin
  foreach x slice 2 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
