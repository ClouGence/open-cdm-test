CREATE INDEX test_dc_data ON test_dc(data)
;
create table default_parted_trig partition of parted_trig default
;
ALTER SUBSCRIPTION tap_sub SET (binary = on)
;
ALTER TABLE ONLY "public"."transition_table_status"
    ADD CONSTRAINT "transition_table_status_pkey" PRIMARY KEY ("level", "node_no")
;
CREATE TABLE "public"."nv_child_2010" (
    "d" "date"
)
;
VACUUM (FULL) information_schema.sql_languages
;
create temp table clstr_temp (col1 int primary key, col2 text)
;
ALTER SEQUENCE fkey_table_seq
  MINVALUE 10
  START 20
  CACHE 1
  NO CYCLE
;
CREATE MATERIALIZED VIEW matview_ine_tab AS SELECT 1
;
create table ma_test_p3 partition of ma_test for values from (20) to (30)
;
ALTER TABLE tt9 ADD CHECK(c > 2)
;
COMMENT ON LARGE OBJECT 32010 IS 'I Wandered Lonely as a Cloud'
;
SECURITY LABEL ON COLUMN t1p.q IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
CREATE INDEX idx_bytea ON test_bytea USING gin (i)
;
-- same as last 4 tests but here the last original column has a NULL value
-- 2 new columns, both have defaults
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
CREATE TABLE table_dropped_index_no_pk (a int NOT NULL, b int, c int)
;
create table atref (c1 int references attbl(p1))
;
DROP TABLE parted_si
;
CREATE TABLE "fkpart3"."pk5" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "a" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
-- this does not work currently (no implicit casting)
create or replace function compos() returns compostype as $$
begin
  return (1, 'hello');
end;
$$ language plpgsql
;
create table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=98) tablespace "regress_pgbench_tap_1_ts"
;
DROP TABLE regress_roleoption.t1
;
/*
 * 9. Alter column type.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
DROP SCHEMA split_agg_probe_next
;
create table atacc1 (test int not null)
;
TRUNCATE TABLE trunc_a1 CASCADE
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
ALTER INDEX pg_toast.pg_toast_28363_index RENAME TO reind_con_toast_idx
;
CREATE TEMP TABLE b (id int PRIMARY KEY, a_id int)
;
CREATE FUNCTION g_cube_distance (internal, cube, smallint, oid, internal)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE pg_temp_3.pg_temp_27598_2 ADD COLUMN newdata pg_temp_3.pg_temp_27598
;
create table inhcld1(f2 name, f1 int primary key)
;
CREATE FUNCTION isneq(ean13, isbn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub1
;
-- Test a large sample of different datatypes
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY, c_int INT DEFAULT 1)
;
create rule qqq as on update to copydml_test do also delete from copydml_test
;
CREATE TABLE booltmp (a bool)
;
ALTER TYPE "public"."planets" ADD VALUE 'venus'
;
CREATE INDEX idx_text ON test_text USING gin (i)
;
create table rtest_t2 (a int4, b int4)
;
-- Only toast index is cleaned up.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = false,
    toast.vacuum_index_cleanup = true)
;
CREATE TABLE distinct_hash_1 AS
SELECT DISTINCT g%1000 FROM generate_series(0,9999) g
;
ALTER TABLE regtest_table ADD CONSTRAINT test_fk FOREIGN KEY (a) REFERENCES regtest_table_3(x)
;
create or replace function composrec() returns record as $$
begin
  return (1, 'hello');
end;
$$ language plpgsql
;
alter table tt7 add column z int
;
ALTER TABLE pg_temp_88.pg_temp_30263_2 ADD COLUMN newdata pg_temp_88.pg_temp_30263
;
create table child partition of parent for values in ('AAA')
;
create temp table q22 partition of q2 for values in (2)
;
drop function anyctest(anycompatible, anycompatiblemultirange)
;
ALTER TABLE cmpart ATTACH PARTITION cmpart2 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX test_dc_data ON test_dc(data)
;
----------
[CREATE_TABLE] create table default_parted_trig partition of parted_trig default
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub SET (binary = on)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."transition_table_status"
    ADD CONSTRAINT "transition_table_status_pkey" PRIMARY KEY ("level", "node_no")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."nv_child_2010" (
    "d" "date"
)
;
----------
[ADMIN_TABLE] VACUUM (FULL) information_schema.sql_languages
;
----------
[CREATE_TABLE] create temp table clstr_temp (col1 int primary key, col2 text)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE fkey_table_seq
  MINVALUE 10
  START 20
  CACHE 1
  NO CYCLE
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW matview_ine_tab AS SELECT 1
;
----------
[CREATE_TABLE] create table ma_test_p3 partition of ma_test for values from (20) to (30)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD CHECK(c > 2)
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 32010 IS 'I Wandered Lonely as a Cloud'
;
----------
[UNKNOWN] SECURITY LABEL ON COLUMN t1p.q IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
----------
[ADD_INDEX] CREATE INDEX idx_bytea ON test_bytea USING gin (i)
;
----------
[CREATE_TABLE] -- same as last 4 tests but here the last original column has a NULL value
-- 2 new columns, both have defaults
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
----------
[CREATE_TABLE] CREATE TABLE table_dropped_index_no_pk (a int NOT NULL, b int, c int)
;
----------
[CREATE_TABLE] create table atref (c1 int references attbl(p1))
;
----------
[DROP_TABLE] DROP TABLE parted_si
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk5" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "a" integer NOT NULL
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_PROG_OBJ] -- this does not work currently (no implicit casting)
create or replace function compos() returns compostype as $$
begin
  return (1, 'hello');
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] create table pgbench_tellers(tid int not null,bid int,tbalance int,filler char(84)) with (fillfactor=98) tablespace "regress_pgbench_tap_1_ts"
;
----------
[DROP_TABLE] DROP TABLE regress_roleoption.t1
;
----------
[CREATE_TABLE] /*
 * 9. Alter column type.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
----------
[DROP_SCHEMA] DROP SCHEMA split_agg_probe_next
;
----------
[CREATE_TABLE] create table atacc1 (test int not null)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE trunc_a1 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28363_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TEMP TABLE b (id int PRIMARY KEY, a_id int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_cube_distance (internal, cube, smallint, oid, internal)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_27598_2 ADD COLUMN newdata pg_temp_3.pg_temp_27598
;
----------
[CREATE_TABLE] create table inhcld1(f2 name, f1 int primary key)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ean13, isbn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TABLE] -- Test a large sample of different datatypes
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY, c_int INT DEFAULT 1)
;
----------
[CREATE_POLICY] create rule qqq as on update to copydml_test do also delete from copydml_test
;
----------
[CREATE_TABLE] CREATE TABLE booltmp (a bool)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."planets" ADD VALUE 'venus'
;
----------
[ADD_INDEX] CREATE INDEX idx_text ON test_text USING gin (i)
;
----------
[CREATE_TABLE] create table rtest_t2 (a int4, b int4)
;
----------
[ALTER_TABLE] -- Only toast index is cleaned up.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = false,
    toast.vacuum_index_cleanup = true)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE distinct_hash_1 AS
SELECT DISTINCT g%1000 FROM generate_series(0,9999) g
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE regtest_table ADD CONSTRAINT test_fk FOREIGN KEY (a) REFERENCES regtest_table_3(x)
;
----------
[CREATE_PROG_OBJ] create or replace function composrec() returns record as $$
begin
  return (1, 'hello');
end;
$$ language plpgsql
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table tt7 add column z int
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_88.pg_temp_30263_2 ADD COLUMN newdata pg_temp_88.pg_temp_30263
;
----------
[CREATE_TABLE] create table child partition of parent for values in ('AAA')
;
----------
[CREATE_TABLE] create temp table q22 partition of q2 for values in (2)
;
----------
[DROP_PROG_OBJ] drop function anyctest(anycompatible, anycompatiblemultirange)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE cmpart ATTACH PARTITION cmpart2 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
