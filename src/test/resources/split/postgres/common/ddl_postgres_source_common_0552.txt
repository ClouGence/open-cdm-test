ALTER TABLE T ADD COLUMN c_text TEXT  DEFAULT foo(6),
              ALTER COLUMN c_bpchar SET DEFAULT foo(3)
;
CREATE TABLE XLogFlush ()
;
CREATE FUNCTION citext_hash_extended(citext, int8)
RETURNS int8
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_30638_index RENAME TO reind_con_toast_idx
;
VACUUM ANALYZE student
;
ALTER EXTENSION test_parser ADD function testprs_getlexeme(internal,internal,internal)
;
CREATE TABLE inhtb () INHERITS (inhta)
;
CREATE TABLE tab1_part1 (b text, c text, a int NOT NULL)
;
CREATE FUNCTION pg_get_wal_stats(IN start_lsn pg_lsn,
    IN end_lsn pg_lsn,
    IN  per_record boolean DEFAULT false,
    OUT "resource_manager/record_type" text,
    OUT count int8,
    OUT count_percentage float8,
    OUT record_size int8,
    OUT record_size_percentage float8,
    OUT fpi_size int8,
    OUT fpi_size_percentage float8,
    OUT combined_size int8,
    OUT combined_size_percentage float8
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER TABLE pitest2 DETACH PARTITION pitest2_p1
;
CREATE TYPE "public"."float8range" AS RANGE (
    subtype = double precision,
    multirange_type_name = "public"."float8multirange",
    subtype_diff = "float8mi"
)
;
CREATE PUBLICATION dummy_pub
;
alter table idxpart add exclude USING GIST (a with =, b with &&)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=22165 host=/tmp/COE1fEXnZH dbname=postgres' PUBLICATION tap_pub
;
create domain d_fail as int4 constraint cc generated by default as identity
;
CREATE TABLE "public"."gtest29" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer
)
;
ALTER PUBLICATION tap_pub_ins_only ADD TABLE tab_ins
;
create temp table q111 partition of q11 for values in (1)
;
CREATE TABLE encoding_tests (encoding text, description text, input bytea)
;
ALTER SCHEMA test_ns_schema_1 RENAME TO test_ns_schema_renamed
;
-- Try it with a function that requires an implicit cast

CREATE FUNCTION bar_int4_text(int4) RETURNS text LANGUAGE SQL AS
$$ SELECT ('bar'::text || $1::text); $$
;
create table list_parted_tbl1 partition of list_parted_tbl
  for values in (1) partition by list(b)
;
CREATE EVENT TRIGGER sql_drop_command ON sql_drop
    WHEN TAG IN ('DROP POLICY') EXECUTE PROCEDURE drop_sql_command()
;
-- test handling of nonstandard null marker that violates escaping rules

CREATE TEMP TABLE testnull(a int, b text)
;
CREATE FUNCTION isnlt(ismn, ismn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- clean up, lest opr_sanity complain
drop type xfloat4 cascade
;
create table phv_boolpart_null partition of phv_boolpart default
;
COMMENT ON TEXT SEARCH DICTIONARY portuguese_stem IS 'snowball stemmer for portuguese language'
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_statistic_ext_data
;
SECURITY LABEL ON TABLE dummy_seclabel_tbl2 IS 'unclassified'
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
DROP TABLE reind_con_wide
;
DROP AGGREGATE test_aggregate_exists(*)
;
drop index idxpart_spgist
;
CREATE TABLE d_listp1 PARTITION OF d_listp FOR VALUES IN (1)
;
CREATE SCHEMA regress_rls_schema
;
create table inhcld2(f1 int primary key, f2 name)
;
-- operator # appears twice in same statement
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD OPERATOR 1 < (int4, int4)
;
VACUUM pg_catalog.pg_class
;
--
--  Test citext datatype
--

CREATE EXTENSION citext
;
CREATE SCHEMA sch2
;
CREATE TABLE "regress_indexing"."pk3" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
CREATE FUNCTION "public"."explain_parallel_append"("text") RETURNS SETOF "text"
    LANGUAGE "plpgsql"
    AS $_$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+ loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        return next ln;
    end loop;
end;
$_$
;
CREATE POLICY r2 ON rec2 USING (a = (SELECT x FROM rec1v WHERE y = b))
;
CREATE FUNCTION gbt_int2_same(gbtreekey4, gbtreekey4, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE SEQUENCE sequence_testx AS text
;
ALTER TEXT SEARCH DICTIONARY split_ts_probe.simple_dict_renamed SET SCHEMA split_ts_next
;
ANALYZE stats_import.test
;
create function alter1.same(alter1.ctype, alter1.ctype) returns boolean language sql
as 'select $1.f1 is not distinct from $2.f1 and $1.f2 is not distinct from $2.f2'
;
drop trigger loct2_br_insert_trigger on loct2
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_text TEXT  DEFAULT foo(6),
              ALTER COLUMN c_bpchar SET DEFAULT foo(3)
;
----------
[CREATE_TABLE] CREATE TABLE XLogFlush ()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_hash_extended(citext, int8)
RETURNS int8
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30638_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM ANALYZE student
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_parser ADD function testprs_getlexeme(internal,internal,internal)
;
----------
[CREATE_TABLE] CREATE TABLE inhtb () INHERITS (inhta)
;
----------
[CREATE_TABLE] CREATE TABLE tab1_part1 (b text, c text, a int NOT NULL)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_get_wal_stats(IN start_lsn pg_lsn,
    IN end_lsn pg_lsn,
    IN  per_record boolean DEFAULT false,
    OUT "resource_manager/record_type" text,
    OUT count int8,
    OUT count_percentage float8,
    OUT record_size int8,
    OUT record_size_percentage float8,
    OUT fpi_size int8,
    OUT fpi_size_percentage float8,
    OUT combined_size int8,
    OUT combined_size_percentage float8
)
RETURNS SETOF record
AS '$libdir/pg_walinspect', 'pg_get_wal_stats'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pitest2 DETACH PARTITION pitest2_p1
;
----------
[CREATE_TYPE] CREATE TYPE "public"."float8range" AS RANGE (
    subtype = double precision,
    multirange_type_name = "public"."float8multirange",
    subtype_diff = "float8mi"
)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION dummy_pub
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add exclude USING GIST (a with =, b with &&)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=22165 host=/tmp/COE1fEXnZH dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_TYPE] create domain d_fail as int4 constraint cc generated by default as identity
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest29" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer
)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_ins_only ADD TABLE tab_ins
;
----------
[CREATE_TABLE] create temp table q111 partition of q11 for values in (1)
;
----------
[CREATE_TABLE] CREATE TABLE encoding_tests (encoding text, description text, input bytea)
;
----------
[RENAME_SCHEMA] ALTER SCHEMA test_ns_schema_1 RENAME TO test_ns_schema_renamed
;
----------
[CREATE_PROG_OBJ] -- Try it with a function that requires an implicit cast

CREATE FUNCTION bar_int4_text(int4) RETURNS text LANGUAGE SQL AS
$$ SELECT ('bar'::text || $1::text); $$
;
----------
[CREATE_TABLE] create table list_parted_tbl1 partition of list_parted_tbl
  for values in (1) partition by list(b)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER sql_drop_command ON sql_drop
    WHEN TAG IN ('DROP POLICY') EXECUTE PROCEDURE drop_sql_command()
;
----------
[CREATE_TABLE] -- test handling of nonstandard null marker that violates escaping rules

CREATE TEMP TABLE testnull(a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ismn, ismn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TYPE] -- clean up, lest opr_sanity complain
drop type xfloat4 cascade
;
----------
[CREATE_TABLE] create table phv_boolpart_null partition of phv_boolpart default
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY portuguese_stem IS 'snowball stemmer for portuguese language'
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_statistic_ext_data
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE dummy_seclabel_tbl2 IS 'unclassified'
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
----------
[DROP_TABLE] DROP TABLE reind_con_wide
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE test_aggregate_exists(*)
;
----------
[DROP_INDEX] drop index idxpart_spgist
;
----------
[CREATE_TABLE] CREATE TABLE d_listp1 PARTITION OF d_listp FOR VALUES IN (1)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_rls_schema
;
----------
[CREATE_TABLE] create table inhcld2(f1 int primary key, f2 name)
;
----------
[ALTER_PROG_OBJ] -- operator # appears twice in same statement
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD OPERATOR 1 < (int4, int4)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_class
;
----------
[CREATE_LIBRARY] --
--  Test citext datatype
--

CREATE EXTENSION citext
;
----------
[CREATE_SCHEMA] CREATE SCHEMA sch2
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk3" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_parallel_append"("text") RETURNS SETOF "text"
    LANGUAGE "plpgsql"
    AS $_$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+ loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        return next ln;
    end loop;
end;
$_$
;
----------
[CREATE_POLICY] CREATE POLICY r2 ON rec2 USING (a = (SELECT x FROM rec1v WHERE y = b))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int2_same(gbtreekey4, gbtreekey4, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_testx AS text
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY split_ts_probe.simple_dict_renamed SET SCHEMA split_ts_next
;
----------
[ADMIN_TABLE] ANALYZE stats_import.test
;
----------
[CREATE_PROG_OBJ] create function alter1.same(alter1.ctype, alter1.ctype) returns boolean language sql
as 'select $1.f1 is not distinct from $2.f1 and $1.f2 is not distinct from $2.f2'
;
----------
[DROP_TRIGGER] drop trigger loct2_br_insert_trigger on loct2
;
