CREATE TABLE harvest(apples int, pears int)
;
-- check that gist index works correctly
CREATE INDEX inet_idx2 ON inet_tbl using gist (i inet_ops)
;
CREATE FOREIGN TABLE t4 (
  c1 integer OPTIONS (column_name 'c1')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't4')
;
/*
 * 24.7
 * FOREIGN_SERVER_OPTIONS view
 */
CREATE VIEW foreign_server_options AS
    SELECT foreign_server_catalog,
           foreign_server_name,
           CAST((pg_options_to_table(s.srvoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(s.srvoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_servers s
;
ALTER SEQUENCE test_seq RESTART WITH 51
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_authid
;
DROP TABLE distinct_hash_1
;
TRUNCATE TABLE y
;
CREATE SUBSCRIPTION testsub1 CONNECTION 'port=16259 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION testpub1
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_user_mapping
;
ANALYZE pg_temp_24.pg_temp_25663_2
;
drop function strtest()
;
DROP MATERIALIZED VIEW heapmv
;
CREATE TABLE min_updates_test (
	f1	text,
	f2 int,
	f3 int)
;
ALTER TABLE ONLY "public"."mlparted_def" ATTACH PARTITION "public"."mlparted_defd" DEFAULT
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
CREATE INDEX idx ON cmdata(f1)
;
CREATE TYPE text_w_default (
   internallength = variable,
   input = text_w_default_in,
   output = text_w_default_out,
   alignment = int4,
   default = 'zippo'
)
;
-- will be ignored -- already there:
ALTER TABLE new_system_table SET SCHEMA pg_catalog
;
DROP TABLE remember_node_subid
;
-- fail
create materialized view mvtest_error as select 1/0 as x with no data
;
-- A unique index defined as NULLS NOT DISTINCT does not need a supporting NOT
-- NULL constraint on the indexed columns.  Ensure the redundant columns are
-- removed from the GROUP BY for such a table.
drop index t2_z_uidx
;
DROP TABLE ruletest2
;
create trigger trig_del_before_3 before delete on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
CREATE FUNCTION isneq(ismn13, ismn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
alter type insenum add value 'i11' before 'L2'
;
-- fail
drop trigger trg1 on trigpart2
;
create temp table p1 partition of p for values in (1)
;
VACUUM (FREEZE) pg_catalog.pg_inherits
;
ANALYZE pg_catalog.pg_collation
;
-- tables from dynamic sources
CREATE TABLE testschema.asselect TABLESPACE regress_tblspace AS SELECT 1
;
ANALYZE public.pagg_tab1_p2
;
ALTER FUNCTION gbt_uuid_penalty(internal, internal, internal) PARALLEL SAFE
;
drop domain posint cascade
;
create temp table tc (id int primary key, aid int)
;
VACUUM (FULL) pg_catalog.pg_foreign_table
;
ALTER TABLE ONLY tab1 ENABLE REPLICA TRIGGER sub2_tab1_log_op_trigger
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_parameter_acl
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- too many rows, no params
  select * from foo where f1 > 3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
ALTER FUNCTION gbt_text_penalty(internal, internal, internal) PARALLEL SAFE
;
/* contrib/citext/citext--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR <= (citext, citext) SET (
    RESTRICT   = scalarlesel,
    JOIN       = scalarlejoinsel
)
;
CREATE TABLE tableam_tbl_heap2(f1 int) USING heap2
;
-- Disable batch inserting into foreign tables with BEFORE ROW INSERT triggers
-- even if the batch_size option is enabled.
ALTER FOREIGN TABLE ftable OPTIONS ( SET batch_size '10' )
;
DROP TYPE IF EXISTS split_domain_type_next.int4_demo_range CASCADE
;
DROP FUNCTION IF EXISTS f4() CASCADE
;
alter function alter1.plus1(int) set schema alter2
;
drop table if exists tlu_job
;
ALTER TABLE ONLY "public"."prt1_e" ATTACH PARTITION "public"."prt1_e_p3" FOR VALUES FROM (500) TO (600)
;
DROP TABLE changeresult
;
ALTER DATABASE moveme_db SET TABLESPACE target_ts
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE harvest(apples int, pears int)
;
----------
[ADD_INDEX] -- check that gist index works correctly
CREATE INDEX inet_idx2 ON inet_tbl using gist (i inet_ops)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t4 (
  c1 integer OPTIONS (column_name 'c1')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't4')
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.7
 * FOREIGN_SERVER_OPTIONS view
 */
CREATE VIEW foreign_server_options AS
    SELECT foreign_server_catalog,
           foreign_server_name,
           CAST((pg_options_to_table(s.srvoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(s.srvoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_servers s
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE test_seq RESTART WITH 51
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_authid
;
----------
[DROP_TABLE] DROP TABLE distinct_hash_1
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE y
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub1 CONNECTION 'port=16259 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION testpub1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_user_mapping
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_25663_2
;
----------
[DROP_PROG_OBJ] drop function strtest()
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW heapmv
;
----------
[CREATE_TABLE] CREATE TABLE min_updates_test (
	f1	text,
	f2 int,
	f3 int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted_def" ATTACH PARTITION "public"."mlparted_defd" DEFAULT
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
----------
[ADD_INDEX] CREATE INDEX idx ON cmdata(f1)
;
----------
[CREATE_TYPE] CREATE TYPE text_w_default (
   internallength = variable,
   input = text_w_default_in,
   output = text_w_default_out,
   alignment = int4,
   default = 'zippo'
)
;
----------
[ALTER_TABLE] -- will be ignored -- already there:
ALTER TABLE new_system_table SET SCHEMA pg_catalog
;
----------
[DROP_TABLE] DROP TABLE remember_node_subid
;
----------
[CREATE_VIEW(SELECT)] -- fail
create materialized view mvtest_error as select 1/0 as x with no data
;
----------
[DROP_INDEX] -- A unique index defined as NULLS NOT DISTINCT does not need a supporting NOT
-- NULL constraint on the indexed columns.  Ensure the redundant columns are
-- removed from the GROUP BY for such a table.
drop index t2_z_uidx
;
----------
[DROP_TABLE] DROP TABLE ruletest2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_del_before_3 before delete on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ismn13, ismn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TYPE] alter type insenum add value 'i11' before 'L2'
;
----------
[DROP_TRIGGER] -- fail
drop trigger trg1 on trigpart2
;
----------
[CREATE_TABLE] create temp table p1 partition of p for values in (1)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_inherits
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_collation
;
----------
[CREATE_TABLE|SELECT] -- tables from dynamic sources
CREATE TABLE testschema.asselect TABLESPACE regress_tblspace AS SELECT 1
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab1_p2
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_uuid_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[DROP_TYPE] drop domain posint cascade
;
----------
[CREATE_TABLE] create temp table tc (id int primary key, aid int)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_foreign_table
;
----------
[ALTER_TABLE] ALTER TABLE ONLY tab1 ENABLE REPLICA TRIGGER sub2_tab1_log_op_trigger
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_parameter_acl
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- too many rows, no params
  select * from foo where f1 > 3 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_text_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] /* contrib/citext/citext--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR <= (citext, citext) SET (
    RESTRICT   = scalarlesel,
    JOIN       = scalarlejoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE tableam_tbl_heap2(f1 int) USING heap2
;
----------
[ALTER_TABLE] -- Disable batch inserting into foreign tables with BEFORE ROW INSERT triggers
-- even if the batch_size option is enabled.
ALTER FOREIGN TABLE ftable OPTIONS ( SET batch_size '10' )
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS split_domain_type_next.int4_demo_range CASCADE
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS f4() CASCADE
;
----------
[ALTER_PROG_OBJ] alter function alter1.plus1(int) set schema alter2
;
----------
[DROP_TABLE] drop table if exists tlu_job
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_e" ATTACH PARTITION "public"."prt1_e_p3" FOR VALUES FROM (500) TO (600)
;
----------
[DROP_TABLE] DROP TABLE changeresult
;
----------
[ALTER_CATALOG] ALTER DATABASE moveme_db SET TABLESPACE target_ts
;
