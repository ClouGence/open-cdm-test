drop function raise_test()
;
ALTER TABLE t ADD COLUMN x int NOT NULL DEFAULT 4
;
-- whole-row Var in partition key works too
create table partitioned (a int, b int)
  partition by list ((partitioned))
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_auth_members
;
-- inlining of this function results in unusual number of hash clauses,
-- which we can still cope with
create or replace function bogus_int8_text_eq(int8, text) returns boolean
language sql as 'select $1::text = $2 and $1::text = $2'
;
DROP TABLE test_replica_identity_othertable
;
create temp table q11 partition of q1 for values in (1) partition by list (c)
;
CREATE INDEX idx_numeric ON test_numeric USING gin (i)
;
-- references to other generated columns, including self-references
CREATE TABLE gtest_err_2a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (b * 2) STORED)
;
-- wrong column
DROP TABLE sequence_test_table
;
VACUUM (ANALYZE) pg_catalog.pg_amop
;
ALTER TABLE pg_toast.pg_toast_32988 RENAME TO reind_con_toast
;
alter event trigger regress_event_trigger enable
;
CREATE INDEX fdeps_abc_idx ON functional_dependencies (a, b, c)
;
-- check change of column type doesn't break it
ALTER TABLE functional_dependencies ALTER COLUMN c TYPE numeric
;
CREATE TABLE tbl_include_box (c1 int, c2 int, c3 int, c4 box)
;
DROP TABLE table_dropped_index_with_pk
;
-- Combine with other DDL
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY)
;
-- cannot contain whole-row references
CREATE TABLE partitioned (
	a	int
) PARTITION BY RANGE ((partitioned))
;
-- not supported
ALTER TABLE regtest_table DROP CONSTRAINT test_ck
;
ALTER PUBLICATION testpub_default RENAME TO testpub_dummy
;
create table test2 (a int, b text)
;
CREATE FUNCTION "public"."test_support_func"("internal") RETURNS "internal"
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'test_support_func'
;
ALTER INDEX pg_toast.pg_toast_30684_index RENAME TO reind_con_toast_idx
;
ALTER INDEX ptif_test_index ATTACH PARTITION ptif_test0_index
;
-- cleanup
drop table parent_noinh_convalid, child_noinh_convalid
;
CREATE FUNCTION errno_from_string(sym text)
RETURNS pg_catalog.int4 STRICT
AS '$libdir/test_aio' LANGUAGE C
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opfamily
;
create trigger check_fkeys_pkey2_exist
	before insert or update on fkeys
	for each row
	execute function check_primary_key ('fkey3', 'fkeys2', 'pkey23')
;
create table parted_uniq_detach_test (a int unique) partition by list (a)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_ts_parser
;
--
-- Tests for default labeling behavior
--
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_user_t:s0
CREATE TABLE t3 (s int, t text)
;
CREATE TABLE enumtest_bogus_child(parent bogus REFERENCES enumtest_parent)
;
/* src/test/modules/test_extensions/test_ext_set_schema--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Create various object types that need extra handling by SET SCHEMA.

CREATE TABLE ess_table (f1 int primary key, f2 int, f3 text,
                        constraint ess_c check (f1 != f2))
;
create table child2 (x int, a text, b int)
;
CREATE FUNCTION gin_extract_value_float8(float8, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ANALYZE public.itest3
;
-- check that circular inheritance is not allowed
ALTER TABLE part_5 ATTACH PARTITION list_parted2 FOR VALUES IN ('b')
;
ALTER TABLE notnull_tbl1 VALIDATE CONSTRAINT nn_parent
;
-- parted_replica_idx becomes valid here.
alter index parted_replica_idx_1 ATTACH PARTITION parted_replica_idx_11
;
CREATE TABLE "public"."target_parted" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("a")
;
DROP TABLE testpub_rf_tbl4
;
--fail

alter domain dom drop constraint domchkgt6 restrict
;
CREATE FUNCTION dump_test.trigger_func()
					   RETURNS trigger LANGUAGE plpgsql
					   AS $$ BEGIN RETURN NULL; END;$$
;
CREATE VIEW aliased_view_4 AS
  select * from temp_view_test.tt1
    where exists (select 1 from tt1 where temp_view_test.tt1.y1 = tt1.f1)
;
COMMENT ON TEXT SEARCH CONFIGURATION estonian IS 'configuration for estonian language'
;
CREATE SEQUENCE "public"."sequence_test8"
    AS smallint
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    MAXVALUE 20000
    CACHE 1
;
DROP TABLE pg_temp_9.pg_temp_38106_2, pg_temp_9.pg_temp_38106
;
-- foreign key
CREATE TABLE logged3(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES logged3)
;
alter table tenk2 set (parallel_workers = 2)
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] drop function raise_test()
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t ADD COLUMN x int NOT NULL DEFAULT 4
;
----------
[CREATE_TABLE] -- whole-row Var in partition key works too
create table partitioned (a int, b int)
  partition by list ((partitioned))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_auth_members
;
----------
[CREATE_PROG_OBJ] -- inlining of this function results in unusual number of hash clauses,
-- which we can still cope with
create or replace function bogus_int8_text_eq(int8, text) returns boolean
language sql as 'select $1::text = $2 and $1::text = $2'
;
----------
[DROP_TABLE] DROP TABLE test_replica_identity_othertable
;
----------
[CREATE_TABLE] create temp table q11 partition of q1 for values in (1) partition by list (c)
;
----------
[ADD_INDEX] CREATE INDEX idx_numeric ON test_numeric USING gin (i)
;
----------
[CREATE_TABLE] -- references to other generated columns, including self-references
CREATE TABLE gtest_err_2a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (b * 2) STORED)
;
----------
[DROP_TABLE] -- wrong column
DROP TABLE sequence_test_table
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_amop
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_32988 RENAME TO reind_con_toast
;
----------
[ALTER_TRIGGER] alter event trigger regress_event_trigger enable
;
----------
[ADD_INDEX] CREATE INDEX fdeps_abc_idx ON functional_dependencies (a, b, c)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- check change of column type doesn't break it
ALTER TABLE functional_dependencies ALTER COLUMN c TYPE numeric
;
----------
[CREATE_TABLE] CREATE TABLE tbl_include_box (c1 int, c2 int, c3 int, c4 box)
;
----------
[DROP_TABLE] DROP TABLE table_dropped_index_with_pk
;
----------
[CREATE_TABLE] -- Combine with other DDL
CREATE TABLE T(pk INT NOT NULL PRIMARY KEY)
;
----------
[CREATE_TABLE] -- cannot contain whole-row references
CREATE TABLE partitioned (
	a	int
) PARTITION BY RANGE ((partitioned))
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- not supported
ALTER TABLE regtest_table DROP CONSTRAINT test_ck
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default RENAME TO testpub_dummy
;
----------
[CREATE_TABLE] create table test2 (a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_support_func"("internal") RETURNS "internal"
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'test_support_func'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30684_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_INDEX] ALTER INDEX ptif_test_index ATTACH PARTITION ptif_test0_index
;
----------
[DROP_TABLE] -- cleanup
drop table parent_noinh_convalid, child_noinh_convalid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION errno_from_string(sym text)
RETURNS pg_catalog.int4 STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opfamily
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger check_fkeys_pkey2_exist
	before insert or update on fkeys
	for each row
	execute function check_primary_key ('fkey3', 'fkeys2', 'pkey23')
;
----------
[CREATE_TABLE] create table parted_uniq_detach_test (a int unique) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_ts_parser
;
----------
[CREATE_TABLE] --
-- Tests for default labeling behavior
--
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_user_t:s0
CREATE TABLE t3 (s int, t text)
;
----------
[CREATE_TABLE] CREATE TABLE enumtest_bogus_child(parent bogus REFERENCES enumtest_parent)
;
----------
[CREATE_TABLE] /* src/test/modules/test_extensions/test_ext_set_schema--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Create various object types that need extra handling by SET SCHEMA.

CREATE TABLE ess_table (f1 int primary key, f2 int, f3 text,
                        constraint ess_c check (f1 != f2))
;
----------
[CREATE_TABLE] create table child2 (x int, a text, b int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_float8(float8, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] ANALYZE public.itest3
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- check that circular inheritance is not allowed
ALTER TABLE part_5 ATTACH PARTITION list_parted2 FOR VALUES IN ('b')
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE notnull_tbl1 VALIDATE CONSTRAINT nn_parent
;
----------
[ALTER_INDEX] -- parted_replica_idx becomes valid here.
alter index parted_replica_idx_1 ATTACH PARTITION parted_replica_idx_11
;
----------
[CREATE_TABLE] CREATE TABLE "public"."target_parted" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("a")
;
----------
[DROP_TABLE] DROP TABLE testpub_rf_tbl4
;
----------
[ALTER_TYPE] --fail

alter domain dom drop constraint domchkgt6 restrict
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dump_test.trigger_func()
					   RETURNS trigger LANGUAGE plpgsql
					   AS $$ BEGIN RETURN NULL; END;$$
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW aliased_view_4 AS
  select * from temp_view_test.tt1
    where exists (select 1 from tt1 where temp_view_test.tt1.y1 = tt1.f1)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION estonian IS 'configuration for estonian language'
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test8"
    AS smallint
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    MAXVALUE 20000
    CACHE 1
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_38106_2, pg_temp_9.pg_temp_38106
;
----------
[CREATE_TABLE] -- foreign key
CREATE TABLE logged3(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES logged3)
;
----------
[ALTER_TABLE] alter table tenk2 set (parallel_workers = 2)
;
