create domain dnotnull varchar(15) NOT NULL
;
CREATE OPERATOR CLASS gist_text_ops
DEFAULT FOR TYPE text USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_text_consistent (internal, text, int2, oid, internal),
	FUNCTION	2	gbt_text_union (internal, internal),
	FUNCTION	3	gbt_text_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_text_penalty (internal, internal, internal),
	FUNCTION	6	gbt_text_picksplit (internal, internal),
	FUNCTION	7	gbt_text_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
CREATE TABLE "public"."num_exp_mul" (
    "id1" integer,
    "id2" integer,
    "expected" numeric(210,10)
)
;
CREATE FUNCTION gbt_int2_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
DROP FUNCTION test1arr(hstore[])
;
-- ===================================================================
-- test generated columns
-- ===================================================================
create table gloc1 (
  a int,
  b int generated always as (a * 2) stored,
  c int
)
;
CREATE FUNCTION ltree_recv(internal)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (issn13, ean13),
	OPERATOR 1  < (issn13, issn),
	OPERATOR 2  <= (issn13, ean13),
	OPERATOR 2  <= (issn13, issn),
	OPERATOR 3  = (issn13, ean13),
	OPERATOR 3  = (issn13, issn),
	OPERATOR 4  >= (issn13, ean13),
	OPERATOR 4  >= (issn13, issn),
	OPERATOR 5  > (issn13, ean13),
	OPERATOR 5  > (issn13, issn),
	FUNCTION 1  btissn13cmp(issn13, ean13),
	FUNCTION 1  btissn13cmp(issn13, issn)
;
create view rw_view4 as select aa, bb, cc FROM rw_view1
;
CREATE TABLE test_prepared1(id int)
;
create table part_abc_log (action text, a int, b text, c bool)
;
ALTER TABLE itest4 ALTER COLUMN a DROP IDENTITY
;
create table rp2 partition of rp for values from (2) to (maxvalue)
;
CREATE INDEX IF NOT EXISTS onek_unique1 ON onek USING btree(unique1 int4_ops)
;
--
-- Simple test of qualified ON INSERT ... this did not work in 7.0 ...
--
create table rules_foo (f1 int)
;
CREATE TABLE tx3 (c integer)
;
CREATE INDEX fooi ON foo (f1 DESC NULLS LAST)
;
CREATE TABLE tab_rowfilter_viaroot_part_1 PARTITION OF tab_rowfilter_viaroot_part FOR VALUES FROM (1) TO (20)
;
create table boolrangep (a bool, b bool, c int) partition by range (a,b,c)
;
drop table syscolconflicttest
;
explain (analyze,buffers off,serialize) create temp table explain_temp as select * from int8_tbl i8
;
create table pktable(ptest1 inet, ptest2 inet[], primary key(base1, ptest1), foreign key(base2, ptest2) references
                                             pktable(base1, ptest1)) inherits (pktable_base)
;
CREATE FUNCTION gin_extract_value_timetz(timetz, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
--
-- hash_bitmap_info()
--
CREATE FUNCTION hash_bitmap_info(IN index_oid regclass, IN blkno int8,
	OUT bitmapblkno int8,
	OUT bitmapbit int4,
	OUT bitstatus bool)
RETURNS SETOF record
AS '$libdir/pageinspect', 'hash_bitmap_info'
LANGUAGE C STRICT PARALLEL SAFE
;
-- fail, can't infer result type:
create function anyctest(anycompatible)
returns anycompatiblerange as $$
  select $1
$$ language sql
;
CREATE DATABASE "template1" WITH TEMPLATE = template0 OID = 1 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
CREATE TABLE partitioned (
	a int,
	EXCLUDE USING gist (a WITH &&)
) PARTITION BY RANGE (a)
;
-- Should fail without CASCADE due to dependency on row security qual?

DROP TABLE dependee CASCADE
;
ALTER TABLE ONLY "public"."gtest10a"
    ADD CONSTRAINT "gtest10a_pkey" PRIMARY KEY ("a")
;
CREATE TABLE "public"."atnotnull1" (
    "a" integer NOT NULL,
    "b" integer NOT NULL,
    "c" integer NOT NULL
)
;
CREATE POLICY pguc ON event_trigger_test USING (FALSE)
;
create table i8r_array (f1 int, f2 int8range[])
;
--
-- DEFAULT syntax
--

CREATE TABLE DEFAULT_TBL (i int DEFAULT 100,
	x text DEFAULT 'vadim', f float8 DEFAULT 123.456)
;
create table tbl_timestamp0 (d1 timestamp(0) without time zone) with (fillfactor=10)
;
alter table p1 add constraint p1chk check (ff1 > 0) no inherit
;
CREATE TABLE "public"."covidxpart3" (
    "b" integer,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "a" integer
)
;
CREATE TEMPORARY TABLE horizons_tst (data int unique) WITH (autovacuum_enabled = off)
;
CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
CREATE SEQUENCE sequence_testx OWNED BY nobody
;
CREATE UNIQUE INDEX onek_nulltest ON onek_with_null (unique2,unique1)
;
CREATE INDEX ptif_test01_index ON ptif_test01 (a)
;
drop table dposintatable
;
TRUNCATE trunc_trigger_test
;
CREATE INDEX CONCURRENTLY oscar ON quebec(i)
;
-- Test assorted behaviors around the implicit transaction block created
-- when multiple SQL commands are sent in a single Query message.  These
-- tests rely on the fact that psql will not break SQL commands apart at a
-- backslash-quoted semicolon, but will send them as one Query.

create temp table i_table (f1 int)
;
CREATE FUNCTION gin_compare_prefix_inet(inet, inet, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
-- Make sure it works with dropped columns
alter table header_copytest drop column c
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
-- Altering a type referenced by a foreign key needs to drop/recreate the FK.
-- Ensure that works.
CREATE TABLE fk_notpartitioned_pk (a INT, PRIMARY KEY(a), CHECK (a > 0))
;
create table s1.p1 (a int, b int) PARTITION BY list (a)
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] create domain dnotnull varchar(15) NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_text_ops
DEFAULT FOR TYPE text USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_text_consistent (internal, text, int2, oid, internal),
	FUNCTION	2	gbt_text_union (internal, internal),
	FUNCTION	3	gbt_text_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_text_penalty (internal, internal, internal),
	FUNCTION	6	gbt_text_picksplit (internal, internal),
	FUNCTION	7	gbt_text_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_exp_mul" (
    "id1" integer,
    "id2" integer,
    "expected" numeric(210,10)
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int2_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test1arr(hstore[])
;
----------
[CREATE_TABLE] -- ===================================================================
-- test generated columns
-- ===================================================================
create table gloc1 (
  a int,
  b int generated always as (a * 2) stored,
  c int
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_recv(internal)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING btree ADD
	OPERATOR 1  < (issn13, ean13),
	OPERATOR 1  < (issn13, issn),
	OPERATOR 2  <= (issn13, ean13),
	OPERATOR 2  <= (issn13, issn),
	OPERATOR 3  = (issn13, ean13),
	OPERATOR 3  = (issn13, issn),
	OPERATOR 4  >= (issn13, ean13),
	OPERATOR 4  >= (issn13, issn),
	OPERATOR 5  > (issn13, ean13),
	OPERATOR 5  > (issn13, issn),
	FUNCTION 1  btissn13cmp(issn13, ean13),
	FUNCTION 1  btissn13cmp(issn13, issn)
;
----------
[CREATE_VIEW(SELECT)] create view rw_view4 as select aa, bb, cc FROM rw_view1
;
----------
[CREATE_TABLE] CREATE TABLE test_prepared1(id int)
;
----------
[CREATE_TABLE] create table part_abc_log (action text, a int, b text, c bool)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest4 ALTER COLUMN a DROP IDENTITY
;
----------
[CREATE_TABLE] create table rp2 partition of rp for values from (2) to (maxvalue)
;
----------
[ADD_INDEX] CREATE INDEX IF NOT EXISTS onek_unique1 ON onek USING btree(unique1 int4_ops)
;
----------
[CREATE_TABLE] --
-- Simple test of qualified ON INSERT ... this did not work in 7.0 ...
--
create table rules_foo (f1 int)
;
----------
[CREATE_TABLE] CREATE TABLE tx3 (c integer)
;
----------
[ADD_INDEX] CREATE INDEX fooi ON foo (f1 DESC NULLS LAST)
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_viaroot_part_1 PARTITION OF tab_rowfilter_viaroot_part FOR VALUES FROM (1) TO (20)
;
----------
[CREATE_TABLE] create table boolrangep (a bool, b bool, c int) partition by range (a,b,c)
;
----------
[DROP_TABLE] drop table syscolconflicttest
;
----------
[CREATE_TABLE|SELECT] explain (analyze,buffers off,serialize) create temp table explain_temp as select * from int8_tbl i8
;
----------
[CREATE_TABLE] create table pktable(ptest1 inet, ptest2 inet[], primary key(base1, ptest1), foreign key(base2, ptest2) references
                                             pktable(base1, ptest1)) inherits (pktable_base)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_timetz(timetz, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] --
-- hash_bitmap_info()
--
CREATE FUNCTION hash_bitmap_info(IN index_oid regclass, IN blkno int8,
	OUT bitmapblkno int8,
	OUT bitmapbit int4,
	OUT bitstatus bool)
RETURNS SETOF record
AS '$libdir/pageinspect', 'hash_bitmap_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- fail, can't infer result type:
create function anyctest(anycompatible)
returns anycompatiblerange as $$
  select $1
$$ language sql
;
----------
[CREATE_CATALOG] CREATE DATABASE "template1" WITH TEMPLATE = template0 OID = 1 ENCODING = 'UTF8' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int,
	EXCLUDE USING gist (a WITH &&)
) PARTITION BY RANGE (a)
;
----------
[DROP_TABLE] -- Should fail without CASCADE due to dependency on row security qual?

DROP TABLE dependee CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest10a"
    ADD CONSTRAINT "gtest10a_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."atnotnull1" (
    "a" integer NOT NULL,
    "b" integer NOT NULL,
    "c" integer NOT NULL
)
;
----------
[CREATE_POLICY] CREATE POLICY pguc ON event_trigger_test USING (FALSE)
;
----------
[CREATE_TABLE] create table i8r_array (f1 int, f2 int8range[])
;
----------
[CREATE_TABLE] --
-- DEFAULT syntax
--

CREATE TABLE DEFAULT_TBL (i int DEFAULT 100,
	x text DEFAULT 'vadim', f float8 DEFAULT 123.456)
;
----------
[CREATE_TABLE] create table tbl_timestamp0 (d1 timestamp(0) without time zone) with (fillfactor=10)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1 add constraint p1chk check (ff1 > 0) no inherit
;
----------
[CREATE_TABLE] CREATE TABLE "public"."covidxpart3" (
    "b" integer,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "a" integer
)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE horizons_tst (data int unique) WITH (autovacuum_enabled = off)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY english_stem
	(TEMPLATE = snowball, Language = english , StopWords=english)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_testx OWNED BY nobody
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX onek_nulltest ON onek_with_null (unique2,unique1)
;
----------
[ADD_INDEX] CREATE INDEX ptif_test01_index ON ptif_test01 (a)
;
----------
[DROP_TABLE] drop table dposintatable
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_trigger_test
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY oscar ON quebec(i)
;
----------
[CREATE_TABLE] -- Test assorted behaviors around the implicit transaction block created
-- when multiple SQL commands are sent in a single Query message.  These
-- tests rely on the fact that psql will not break SQL commands apart at a
-- backslash-quoted semicolon, but will send them as one Query.

create temp table i_table (f1 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_inet(inet, inet, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_TABLE|DROP_COLUMN] -- Make sure it works with dropped columns
alter table header_copytest drop column c
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
----------
[CREATE_TABLE] -- Altering a type referenced by a foreign key needs to drop/recreate the FK.
-- Ensure that works.
CREATE TABLE fk_notpartitioned_pk (a INT, PRIMARY KEY(a), CHECK (a > 0))
;
----------
[CREATE_TABLE] create table s1.p1 (a int, b int) PARTITION BY list (a)
;
