-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt3_n_p1"'::pg_catalog.regclass
;
-- bad values for parser testing
INSERT INTO PATH_TBL VALUES ('[]')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6900', relminmxid = '1'
WHERE oid = '"public"."hash_name_heap"'::pg_catalog.regclass
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (3, 1, 30113, -442, CURRENT_TIMESTAMP)
;
delete from testtr
;
INSERT INTO heaptest (a, b)
	(SELECT gs, repeat('x', gs)
		FROM generate_series(1,50) gs)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17629', relminmxid = '3'
WHERE oid = '"public"."alpha_neg_p2"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('0', 640)
;
insert into emp1 values (2, 1)
;
INSERT INTO public.ths_p3 VALUES (0, 600)
;
update aitest set price_on = 0
;
INSERT INTO num_exp_add VALUES (7,2,'-994877526002807691688882220594983.04353023451866590387054959174736129501310680280823383331007646306243540953499740615246583399296334239109936336446284803020643582102868247857009494139535009572740621288230740389545481395')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."gtestxx_3"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_n_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
insert into gist_tbl
  select box(point(0.05*i, 0.05*i)) from generate_series(0,10) as i
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."alpha_pos_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
insert into PSlot values ('PS.base.ta3', 'PF0_X', '', '')
;
UPDATE orders SET name = 'order of olivier (3)', user_id = 1 WHERE id = 1
;
INSERT INTO public.tht VALUES ('4', 104)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt2_n_p1"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('7', 307)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13138', relminmxid = '3'
WHERE oid = '"public"."xmltest"'::pg_catalog.regclass
;
INSERT INTO rw_view14 (a, b) VALUES (3, 'Row 3')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '19086', relminmxid = '3'
WHERE oid = '44162'
;
UPDATE table_a SET value = 'newTableAValue' WHERE id = 1
;
INSERT INTO test_tab SELECT i, md5(i::text), -i FROM generate_series(5, 1000) s(i)
;
insert into base_tab_def_view values (11)
;
INSERT INTO public.tht VALUES ('3', 243)
;
DELETE FROM trunc_stats_test1 WHERE id = 3
;
INSERT INTO tab_fk_ref (id, bid) VALUES (2, 2)
;
DELETE FROM public.mvtest_tm mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_12.pg_temp_25367_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
UPDATE arrtest
  SET a[1:2] = '{16,25}'
  WHERE NOT a = '{}'::_int2
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9075', relminmxid = '1'
WHERE oid = '"public"."datdba_only"'::pg_catalog.regclass
;
insert into atacc1 (test,test2) values (NULL,NULL)
;
UPDATE tab2 SET c = 5 where a = 1
;
insert into range_parted values ('a', 10)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."alpha_neg_p3"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('5', 25)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."itest5"'::pg_catalog.regclass
;
INSERT INTO ft2 (c1,c2,c3)
  SELECT id, id % 10, to_char(id, 'FM00000') FROM generate_series(2001, 2010) id
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO INET_TBL (c, i) VALUES (cidr('192.168.1.2/30'), '192.168.1.226')
;
insert into HSlot values ('HS', 'base.hub1', 1, '')
;
UPDATE public.loct SET a = 2 WHERE ((b = 'foo')) RETURNING a, b
;
INSERT INTO public.tplain VALUES ('4', 24)
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt2_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
UPDATE pg_catalog.pg_database   SET encoding = 6,       datlocprovider = 'c',       datcollate = 'C',       datctype = 'C',       daticulocale = NULL   WHERE datname = 'template0'
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt2_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO fkpart10.tbl1 VALUES (0), (1)
;
-- not allowed
INSERT INTO rw_view2 VALUES ('Row 4', 4.0, 4)
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt3_n_p1"'::pg_catalog.regclass
;
----------
[INSERT] -- bad values for parser testing
INSERT INTO PATH_TBL VALUES ('[]')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6900', relminmxid = '1'
WHERE oid = '"public"."hash_name_heap"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (3, 1, 30113, -442, CURRENT_TIMESTAMP)
;
----------
[DELETE] delete from testtr
;
----------
[INSERT|SELECT] INSERT INTO heaptest (a, b)
	(SELECT gs, repeat('x', gs)
		FROM generate_series(1,50) gs)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17629', relminmxid = '3'
WHERE oid = '"public"."alpha_neg_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 640)
;
----------
[INSERT] insert into emp1 values (2, 1)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 600)
;
----------
[UPDATE] update aitest set price_on = 0
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (7,2,'-994877526002807691688882220594983.04353023451866590387054959174736129501310680280823383331007646306243540953499740615246583399296334239109936336446284803020643582102868247857009494139535009572740621288230740389545481395')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"public"."gtestxx_3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_n_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT|SELECT] insert into gist_tbl
  select box(point(0.05*i, 0.05*i)) from generate_series(0,10) as i
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."alpha_pos_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] insert into PSlot values ('PS.base.ta3', 'PF0_X', '', '')
;
----------
[UPDATE] UPDATE orders SET name = 'order of olivier (3)', user_id = 1 WHERE id = 1
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 104)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."prt2_n_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 307)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13138', relminmxid = '3'
WHERE oid = '"public"."xmltest"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO rw_view14 (a, b) VALUES (3, 'Row 3')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '19086', relminmxid = '3'
WHERE oid = '44162'
;
----------
[UPDATE] UPDATE table_a SET value = 'newTableAValue' WHERE id = 1
;
----------
[INSERT|SELECT] INSERT INTO test_tab SELECT i, md5(i::text), -i FROM generate_series(5, 1000) s(i)
;
----------
[INSERT] insert into base_tab_def_view values (11)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('3', 243)
;
----------
[DELETE] DELETE FROM trunc_stats_test1 WHERE id = 3
;
----------
[INSERT|SELECT] INSERT INTO tab_fk_ref (id, bid) VALUES (2, 2)
;
----------
[DELETE|SELECT] DELETE FROM public.mvtest_tm mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_12.pg_temp_25367_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE] UPDATE arrtest
  SET a[1:2] = '{16,25}'
  WHERE NOT a = '{}'::_int2
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9075', relminmxid = '1'
WHERE oid = '"public"."datdba_only"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into atacc1 (test,test2) values (NULL,NULL)
;
----------
[UPDATE] UPDATE tab2 SET c = 5 where a = 1
;
----------
[INSERT] insert into range_parted values ('a', 10)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."alpha_neg_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('5', 25)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."itest5"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO ft2 (c1,c2,c3)
  SELECT id, id % 10, to_char(id, 'FM00000') FROM generate_series(2001, 2010) id
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT|SELECT] INSERT INTO INET_TBL (c, i) VALUES (cidr('192.168.1.2/30'), '192.168.1.226')
;
----------
[INSERT] insert into HSlot values ('HS', 'base.hub1', 1, '')
;
----------
[UPDATE] UPDATE public.loct SET a = 2 WHERE ((b = 'foo')) RETURNING a, b
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 24)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt2_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_database   SET encoding = 6,       datlocprovider = 'c',       datcollate = 'C',       datctype = 'C',       daticulocale = NULL   WHERE datname = 'template0'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt2_p2"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] INSERT INTO fkpart10.tbl1 VALUES (0), (1)
;
----------
[INSERT] -- not allowed
INSERT INTO rw_view2 VALUES ('Row 4', 4.0, 4)
;
