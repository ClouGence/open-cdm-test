CREATE EXTENSION IF NOT EXISTS vector
;
--
-- Clean up
--

DROP TABLE CASE_TBL
;
ANALYZE public.gtest_normal_child
;
CREATE FUNCTION volfoo(text) returns foodomain as
  'begin return $1::foodomain; end' language plpgsql volatile
;
drop table foo
;
CREATE OPERATOR CLASS isbn13_ops DEFAULT
	FOR TYPE isbn13 USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btisbn13cmp(isbn13, isbn13)
;
CREATE VIEW lock_view5 AS SELECT * from lock_tbl1 where a in (select * from lock_tbl1a)
;
ALTER TABLE pk5 DROP COLUMN b, DROP COLUMN c
;
drop table coercepart
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_description
;
CREATE FUNCTION triggerfunc1() RETURNS trigger
LANGUAGE plpgsql
AS $$
DECLARE
    z int := 0;
BEGIN
    CALL test_proc6(2, NEW.a, NEW.a);
    RETURN NEW;
END;
$$
;
ANALYZE pg_temp_9.pg_temp_26555
;
--
-- Test functional index
--
CREATE TABLE func_index_heap (f1 text, f2 text)
;
CREATE TABLE local_tbl (a int, b int, c text)
;
ALTER TABLE ONLY "fkpart5"."pk3"
    ADD CONSTRAINT "pk3_pkey" PRIMARY KEY ("a")
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_tablespace
;
ALTER TABLE parttab ATTACH PARTITION parttab1 FOR VALUES IN (1)
;
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c2negative CHECK (c2 < 0)
;
-- fail - invalid connection string during ALTER
ALTER SUBSCRIPTION regress_testsub CONNECTION 'foobar'
;
CREATE FUNCTION hashissn(issn)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ANALYZE pg_temp_13.pg_temp_26306
;
DROP TABLE itest5
;
create table cnn_part1 partition of cnn2_parted for values in (1, null)
;
ALTER TABLE testpub_parted DETACH PARTITION testpub_parted1
;
-- 155
TRUNCATE tru_ptable
;
CREATE TYPE dump_test.int42 (
						   internallength = 4,
						   input = dump_test.int42_in,
						   output = dump_test.int42_out,
						   alignment = int4,
						   default = 42,
						   passedbyvalue)
;
alter table abc drop constraint abc_fk
;
alter table donothingbrtrig_test2 drop column c
;
-- check that the table being attached has every column of the parent
CREATE TABLE fail_part (a int NOT NULL)
;
create function sillytrig() returns trigger language plpgsql as
$$begin
  raise notice 'old.ctid = %', old.ctid;
  raise notice 'old.tableoid = %', old.tableoid::regclass;
  return new;
end$$
;
ALTER TABLE ONLY "public"."gtest1"
    ADD CONSTRAINT "gtest1_pkey" PRIMARY KEY ("a")
;
CREATE TABLE testpub_merge_pk (a int primary key, b int)
;
CREATE CAST (bigint AS "public"."int8alias2") WITHOUT FUNCTION
;
create temp table dest (f1 textandtext[])
;
create index minmaxtesti on minmaxtest(f1)
;
CREATE OPERATOR ? (
        LEFTARG = _lquery,
	RIGHTARG = _ltree,
	PROCEDURE = _lt_q_rregex,
        COMMUTATOR = '?',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_opfamily
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_shseclabel
;
CREATE VIEW pg_stat_gssapi AS
    SELECT
            S.pid,
            S.gss_auth AS gss_authenticated,
            S.gss_princ AS principal,
            S.gss_enc AS encrypted,
            S.gss_delegation AS credentials_delegated
    FROM pg_stat_get_activity(NULL) AS S
    WHERE S.client_port IS NOT NULL
;
CREATE FUNCTION test_assign_nnint(x int, y int) RETURNS nnint AS $$
declare v nnint := x;
begin
v := y;
return v;
end
$$ LANGUAGE plpgsql
;
ALTER SEQUENCE sequence_test8 AS smallint MAXVALUE 20000
;
-- Testing USING OPERATOR() in ORDER BY within aggregate.
-- For this, we need user defined operators along with operator family and
-- operator class.  Create those and then add them in extension.  Note that
-- user defined objects are considered unshippable unless they are part of
-- the extension.
create operator public.<^ (
 leftarg = int4,
 rightarg = int4,
 procedure = int4eq
)
;
CREATE TABLE hash_split_heap (keycol INT)
;
create domain ddtest1d as ddtest1
;
-- Test for ON DELETE SET NULL/DEFAULT (column_list);
CREATE TABLE PKTABLE (tid int, id int, PRIMARY KEY (tid, id))
;
CREATE TABLE ptif_li_parent ()
;
CREATE FUNCTION "public"."array_to_set"("anyarray") RETURNS SETOF "record"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$_$
;
drop domain positiveint
;
/* src/test/modules/test_extensions/test_ext3--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE TABLE test_ext3_table (col_old INT)
;
ALTER DATABASE slotsync_test_db SET SEARCH_PATH TO myschema,pg_catalog
;
------------------------------------------------------------------------------------------
[CREATE_LIBRARY] CREATE EXTENSION IF NOT EXISTS vector
;
----------
[DROP_TABLE] --
-- Clean up
--

DROP TABLE CASE_TBL
;
----------
[ADMIN_TABLE] ANALYZE public.gtest_normal_child
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION volfoo(text) returns foodomain as
  'begin return $1::foodomain; end' language plpgsql volatile
;
----------
[DROP_TABLE] drop table foo
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS isbn13_ops DEFAULT
	FOR TYPE isbn13 USING btree FAMILY isn_ops AS
	OPERATOR 1  <,
	OPERATOR 2  <=,
	OPERATOR 3  =,
	OPERATOR 4  >=,
	OPERATOR 5  >,
	FUNCTION 1  btisbn13cmp(isbn13, isbn13)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW lock_view5 AS SELECT * from lock_tbl1 where a in (select * from lock_tbl1a)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE pk5 DROP COLUMN b, DROP COLUMN c
;
----------
[DROP_TABLE] drop table coercepart
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_description
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION triggerfunc1() RETURNS trigger
LANGUAGE plpgsql
AS $$
DECLARE
    z int := 0;
BEGIN
    CALL test_proc6(2, NEW.a, NEW.a);
    RETURN NEW;
END;
$$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26555
;
----------
[CREATE_TABLE] --
-- Test functional index
--
CREATE TABLE func_index_heap (f1 text, f2 text)
;
----------
[CREATE_TABLE] CREATE TABLE local_tbl (a int, b int, c text)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart5"."pk3"
    ADD CONSTRAINT "pk3_pkey" PRIMARY KEY ("a")
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_tablespace
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE parttab ATTACH PARTITION parttab1 FOR VALUES IN (1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c2negative CHECK (c2 < 0)
;
----------
[ALTER_PUB_SUB] -- fail - invalid connection string during ALTER
ALTER SUBSCRIPTION regress_testsub CONNECTION 'foobar'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hashissn(issn)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26306
;
----------
[DROP_TABLE] DROP TABLE itest5
;
----------
[CREATE_TABLE] create table cnn_part1 partition of cnn2_parted for values in (1, null)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE testpub_parted DETACH PARTITION testpub_parted1
;
----------
[TRUNCATE_TABLE] -- 155
TRUNCATE tru_ptable
;
----------
[CREATE_TYPE] CREATE TYPE dump_test.int42 (
						   internallength = 4,
						   input = dump_test.int42_in,
						   output = dump_test.int42_out,
						   alignment = int4,
						   default = 42,
						   passedbyvalue)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table abc drop constraint abc_fk
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table donothingbrtrig_test2 drop column c
;
----------
[CREATE_TABLE] -- check that the table being attached has every column of the parent
CREATE TABLE fail_part (a int NOT NULL)
;
----------
[CREATE_PROG_OBJ] create function sillytrig() returns trigger language plpgsql as
$$begin
  raise notice 'old.ctid = %', old.ctid;
  raise notice 'old.tableoid = %', old.tableoid::regclass;
  return new;
end$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest1"
    ADD CONSTRAINT "gtest1_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE testpub_merge_pk (a int primary key, b int)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (bigint AS "public"."int8alias2") WITHOUT FUNCTION
;
----------
[CREATE_TABLE] create temp table dest (f1 textandtext[])
;
----------
[ADD_INDEX] create index minmaxtesti on minmaxtest(f1)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ? (
        LEFTARG = _lquery,
	RIGHTARG = _ltree,
	PROCEDURE = _lt_q_rregex,
        COMMUTATOR = '?',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_opfamily
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_shseclabel
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_gssapi AS
    SELECT
            S.pid,
            S.gss_auth AS gss_authenticated,
            S.gss_princ AS principal,
            S.gss_enc AS encrypted,
            S.gss_delegation AS credentials_delegated
    FROM pg_stat_get_activity(NULL) AS S
    WHERE S.client_port IS NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_assign_nnint(x int, y int) RETURNS nnint AS $$
declare v nnint := x;
begin
v := y;
return v;
end
$$ LANGUAGE plpgsql
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test8 AS smallint MAXVALUE 20000
;
----------
[CREATE_PROG_OBJ] -- Testing USING OPERATOR() in ORDER BY within aggregate.
-- For this, we need user defined operators along with operator family and
-- operator class.  Create those and then add them in extension.  Note that
-- user defined objects are considered unshippable unless they are part of
-- the extension.
create operator public.<^ (
 leftarg = int4,
 rightarg = int4,
 procedure = int4eq
)
;
----------
[CREATE_TABLE] CREATE TABLE hash_split_heap (keycol INT)
;
----------
[CREATE_TYPE] create domain ddtest1d as ddtest1
;
----------
[CREATE_TABLE] -- Test for ON DELETE SET NULL/DEFAULT (column_list);
CREATE TABLE PKTABLE (tid int, id int, PRIMARY KEY (tid, id))
;
----------
[CREATE_TABLE] CREATE TABLE ptif_li_parent ()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."array_to_set"("anyarray") RETURNS SETOF "record"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$_$
;
----------
[DROP_TYPE] drop domain positiveint
;
----------
[CREATE_TABLE] /* src/test/modules/test_extensions/test_ext3--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE TABLE test_ext3_table (col_old INT)
;
----------
[ALTER_CATALOG] ALTER DATABASE slotsync_test_db SET SEARCH_PATH TO myschema,pg_catalog
;
