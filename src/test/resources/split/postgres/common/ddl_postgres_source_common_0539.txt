CREATE VIEW rw_view1 AS
  SELECT ctid, sin(a) s, a, cos(a) c
  FROM base_tbl
  WHERE a != 0
  ORDER BY abs(a)
;
-- Views
-- ctid not defined in the view
CREATE VIEW tid_view_no_ctid AS SELECT a FROM tid_tab
;
VACUUM pg_catalog.pg_foreign_server
;
VACUUM (ANALYZE) vacparted_i
;
ALTER FUNCTION gbt_tstz_compress(internal) PARALLEL SAFE
;
-- Create a table that exercises pg_upgrade
CREATE TABLE regress_notnull1 (a integer)
;
create unique index PHone_name on PHone using btree (slotname bpchar_ops)
;
create table parted_conflict (a int, b text, c int) partition by range (a)
;
ALTER TYPE "public"."insenum" ADD VALUE 'i8'
;
CREATE OPERATOR && (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_overlap,
	COMMUTATOR = '&&',
	RESTRICT = areasel,
	JOIN = areajoinsel
)
;
CREATE FUNCTION gbt_uuid_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE "public"."gtest27" (
    "a" bigint,
    "b" bigint,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "x" bigint GENERATED ALWAYS AS ((("a" + "b") * 2)) STORED
)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_conversion
;
CREATE TABLE "public"."inhx" (
    "xx" "text" DEFAULT 'text'::"text" NOT NULL,
    CONSTRAINT "foo" CHECK (("xx" = 'text'::"text"))
)
;
CREATE FUNCTION connectby(text,text,text,text,text,int,text)
RETURNS setof record
AS '$libdir/tablefunc','connectby_text_serial'
LANGUAGE C STABLE STRICT
;
-- Failed builds are left invalid by VACUUM FULL, fixed by REINDEX
VACUUM FULL concur_heap
;
DROP MATERIALIZED VIEW refresh_test
;
CREATE FUNCTION gbt_enum_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest1_y" INHERIT "generated_virtual_tests"."gtesty"
;
ALTER TABLE errtst_child_fastdef ADD CONSTRAINT errtest_child_fastdef_data_check CHECK (data < 10)
;
DROP FUNCTION test_mblen_func
;
DROP FUNCTION dogrant_fails()
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
DROP EXTENSION IF EXISTS hstore_plperl CASCADE
;
-- Test cases where a partition on one side matches multiple partitions on
-- the other side; we currently can't do partitioned join in such cases
ALTER TABLE plt2_adv DETACH PARTITION plt2_adv_p2
;
CREATE TABLE logged2(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES logged1)
;
-- cleanup
drop table hash_parted
;
ALTER TABLE list_parted2 ATTACH PARTITION part_3_4 FOR VALUES IN (3, 4)
;
CREATE TABLE public.foo (f integer)
;
CREATE TRIGGER "tg_chkslotlink" BEFORE INSERT OR UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotlink"()
;
CREATE FUNCTION ean13_out(ismn13)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create or replace function simplecaller() returns int language plpgsql
as $$
declare x int;
begin
  select simplesql() into x;
  return x;
end$$
;
create function end_label4() returns void as $$
<<outer_label>>
begin
  for _i in 1 .. 10 loop
    exit;
  end loop outer_label;
end;
$$ language plpgsql
;
DROP TABLE IF EXISTS harvest
;
ALTER VIEW split_view_probe.base_view RESET (security_barrier, security_invoker, check_option)
;
ALTER TABLE pg_toast.pg_toast_28467 RENAME TO reind_con_toast
;
VACUUM (PARALLEL 2) pg_catalog.pg_partitioned_table
;
-- check that DDL in aborted subtransactions handled correctly
CREATE TABLE tr_sub_ddl(data int)
;
create function getf2(record) returns int language plpgsql as
$$ begin return $1.f2; end $$
;
CREATE OPERATOR !~~ (
    PROCEDURE = texticnlike,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = ~~,
    RESTRICT  = icnlikesel,
    JOIN      = icnlikejoinsel
)
;
-- unsupported constraint types for partitioned tables
CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (a, (a+b+1))
;
-- fails
CREATE TABLE collate_test4 (
    a int,
    b testdomain_sv
)
;
CREATE TABLE t (id int NOT NULL, txt text) WITH (fillfactor=50)
;
ALTER INDEX pg_toast.pg_toast_27959_index RENAME TO reind_con_toast_idx
;
create view wcowrtest_v as select * from wcowrtest where wcowrtest = '(2)'::wcowrtest with check option
;
ALTER TABLE pg_language ADD PRIMARY KEY USING INDEX pg_language_oid_index
;
CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') REFERENCES ref_table (id),
	c2 text OPTIONS (param2 'val2', param3 'val3'),
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
CREATE TABLE tab5 (a int)
;
CREATE TRIGGER delete_a AFTER DELETE ON main_table
FOR EACH ROW WHEN (OLD.a = 123) EXECUTE PROCEDURE trigger_func('delete_a')
;
ALTER TABLE notnull_part1_upg ATTACH PARTITION notnull_part1_2_upg FOR VALUES IN (3,4)
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS
  SELECT ctid, sin(a) s, a, cos(a) c
  FROM base_tbl
  WHERE a != 0
  ORDER BY abs(a)
;
----------
[CREATE_VIEW(SELECT)] -- Views
-- ctid not defined in the view
CREATE VIEW tid_view_no_ctid AS SELECT a FROM tid_tab
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_foreign_server
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) vacparted_i
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_tstz_compress(internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Create a table that exercises pg_upgrade
CREATE TABLE regress_notnull1 (a integer)
;
----------
[ADD_INDEX] create unique index PHone_name on PHone using btree (slotname bpchar_ops)
;
----------
[CREATE_TABLE] create table parted_conflict (a int, b text, c int) partition by range (a)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i8'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR && (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_overlap,
	COMMUTATOR = '&&',
	RESTRICT = areasel,
	JOIN = areajoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_uuid_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest27" (
    "a" bigint,
    "b" bigint,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "x" bigint GENERATED ALWAYS AS ((("a" + "b") * 2)) STORED
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_conversion
;
----------
[CREATE_TABLE] CREATE TABLE "public"."inhx" (
    "xx" "text" DEFAULT 'text'::"text" NOT NULL,
    CONSTRAINT "foo" CHECK (("xx" = 'text'::"text"))
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION connectby(text,text,text,text,text,int,text)
RETURNS setof record
AS '$libdir/tablefunc','connectby_text_serial'
LANGUAGE C STABLE STRICT
;
----------
[ADMIN_TABLE] -- Failed builds are left invalid by VACUUM FULL, fixed by REINDEX
VACUUM FULL concur_heap
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW refresh_test
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_enum_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_virtual_tests"."gtest1_y" INHERIT "generated_virtual_tests"."gtesty"
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE errtst_child_fastdef ADD CONSTRAINT errtest_child_fastdef_data_check CHECK (data < 10)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_mblen_func
;
----------
[DROP_PROG_OBJ] DROP FUNCTION dogrant_fails()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[DROP_LIBRARY] DROP EXTENSION IF EXISTS hstore_plperl CASCADE
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Test cases where a partition on one side matches multiple partitions on
-- the other side; we currently can't do partitioned join in such cases
ALTER TABLE plt2_adv DETACH PARTITION plt2_adv_p2
;
----------
[CREATE_TABLE] CREATE TABLE logged2(f1 SERIAL PRIMARY KEY, f2 INTEGER REFERENCES logged1)
;
----------
[DROP_TABLE] -- cleanup
drop table hash_parted
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 ATTACH PARTITION part_3_4 FOR VALUES IN (3, 4)
;
----------
[CREATE_TABLE] CREATE TABLE public.foo (f integer)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkslotlink" BEFORE INSERT OR UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotlink"()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ean13_out(ismn13)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function simplecaller() returns int language plpgsql
as $$
declare x int;
begin
  select simplesql() into x;
  return x;
end$$
;
----------
[CREATE_PROG_OBJ] create function end_label4() returns void as $$
<<outer_label>>
begin
  for _i in 1 .. 10 loop
    exit;
  end loop outer_label;
end;
$$ language plpgsql
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS harvest
;
----------
[ALTER_VIEW] ALTER VIEW split_view_probe.base_view RESET (security_barrier, security_invoker, check_option)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28467 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_partitioned_table
;
----------
[CREATE_TABLE] -- check that DDL in aborted subtransactions handled correctly
CREATE TABLE tr_sub_ddl(data int)
;
----------
[CREATE_PROG_OBJ] create function getf2(record) returns int language plpgsql as
$$ begin return $1.f2; end $$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !~~ (
    PROCEDURE = texticnlike,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = ~~,
    RESTRICT  = icnlikesel,
    JOIN      = icnlikejoinsel
)
;
----------
[CREATE_TABLE] -- unsupported constraint types for partitioned tables
CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (a, (a+b+1))
;
----------
[CREATE_TABLE] -- fails
CREATE TABLE collate_test4 (
    a int,
    b testdomain_sv
)
;
----------
[CREATE_TABLE] CREATE TABLE t (id int NOT NULL, txt text) WITH (fillfactor=50)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27959_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] create view wcowrtest_v as select * from wcowrtest where wcowrtest = '(2)'::wcowrtest with check option
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_language ADD PRIMARY KEY USING INDEX pg_language_oid_index
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') REFERENCES ref_table (id),
	c2 text OPTIONS (param2 'val2', param3 'val3'),
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[CREATE_TABLE] CREATE TABLE tab5 (a int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER delete_a AFTER DELETE ON main_table
FOR EACH ROW WHEN (OLD.a = 123) EXECUTE PROCEDURE trigger_func('delete_a')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE notnull_part1_upg ATTACH PARTITION notnull_part1_2_upg FOR VALUES IN (3,4)
;
