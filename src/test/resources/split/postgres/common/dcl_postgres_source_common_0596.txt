EXECUTE dumpFunc('18640')
;
GRANT USAGE ON SCHEMA "generated_stored_tests" TO PUBLIC
;
-- The "optimal" case: the hash table fits in memory; we plan for 1
-- batch, we stick to that number, and peak memory usage stays within
-- our work_mem budget

-- non-parallel
savepoint settings
;
EXECUTE dumpTableAttach('32138')
;
COPY public.clstr_tst_inh (a, b, c, d) TO stdout
;
EXECUTE dumpTableAttach('45300')
;
GRANT SELECT ON TABLE alice.hashpart TO regress_admin
;
-- to increase the parallel query test coverage
SAVEPOINT settings
;
EXECUTE dumpFunc('34980')
;
-- These may look like valid MD5 verifiers, but they are not, so they
-- should be hashed with SCRAM-SHA-256.
-- trailing garbage at the end
CREATE ROLE regress_passwd7 PASSWORD 'md5012345678901234567890123456789zz'
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('18640')
;
----------
[GRANT] GRANT USAGE ON SCHEMA "generated_stored_tests" TO PUBLIC
;
----------
[TRANSACTION] -- The "optimal" case: the hash table fits in memory; we plan for 1
-- batch, we stick to that number, and peak memory usage stays within
-- our work_mem budget

-- non-parallel
savepoint settings
;
----------
[UNSAFE] EXECUTE dumpTableAttach('32138')
;
----------
[DATA_EXPORT] COPY public.clstr_tst_inh (a, b, c, d) TO stdout
;
----------
[UNSAFE] EXECUTE dumpTableAttach('45300')
;
----------
[GRANT] GRANT SELECT ON TABLE alice.hashpart TO regress_admin
;
----------
[TRANSACTION] -- to increase the parallel query test coverage
SAVEPOINT settings
;
----------
[UNSAFE] EXECUTE dumpFunc('34980')
;
----------
[CREATE_ROLE] -- These may look like valid MD5 verifiers, but they are not, so they
-- should be hashed with SCRAM-SHA-256.
-- trailing garbage at the end
CREATE ROLE regress_passwd7 PASSWORD 'md5012345678901234567890123456789zz'
;
