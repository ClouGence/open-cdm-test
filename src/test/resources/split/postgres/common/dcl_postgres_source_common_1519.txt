SET custom.my.qualified.guc = 'foo'
;
COPY public.rtest_t4 (a, b) TO stdout
;
-- error
ALTER ROLE nonexistent WITH NOREPLICATION
;
DROP GROUP IF EXISTS split_group_renamed, split_group_probe
;
EXECUTE dumpFunc('31222')
;
CREATE ROLE regress_file_fdw_superuser LOGIN SUPERUSER
;
-- This query should detect the broken connection when starting new remote
-- transaction, reestablish new connection, and then succeed.
BEGIN
;
ALTER AGGREGATE "public"."myaggp20a"("anyelement") OWNER TO zyc
;
-- test a couple rounding cases that changed since 8.3 w/ HAVE_INT64_TIMESTAMP.
SET IntervalStyle to postgres_verbose
;
ALTER TABLE public.prt2_e_p2 OWNER TO zyc
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] SET custom.my.qualified.guc = 'foo'
;
----------
[DATA_EXPORT] COPY public.rtest_t4 (a, b) TO stdout
;
----------
[ALTER_USER] -- error
ALTER ROLE nonexistent WITH NOREPLICATION
;
----------
[DROP_ROLE] DROP GROUP IF EXISTS split_group_renamed, split_group_probe
;
----------
[UNSAFE] EXECUTE dumpFunc('31222')
;
----------
[CREATE_ROLE] CREATE ROLE regress_file_fdw_superuser LOGIN SUPERUSER
;
----------
[TRANSACTION] -- This query should detect the broken connection when starting new remote
-- transaction, reestablish new connection, and then succeed.
BEGIN
;
----------
[TRANSFER_PRIVILEGE] ALTER AGGREGATE "public"."myaggp20a"("anyelement") OWNER TO zyc
;
----------
[SESSION_SETTING_WRITE] -- test a couple rounding cases that changed since 8.3 w/ HAVE_INT64_TIMESTAMP.
SET IntervalStyle to postgres_verbose
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.prt2_e_p2 OWNER TO zyc
;
