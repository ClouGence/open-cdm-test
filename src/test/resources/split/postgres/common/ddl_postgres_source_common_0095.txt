-- ... but this one is.
create index on idxpart21 (a)
;
drop table domarrtest
;
-- this revealed a bug in the original implementation
CREATE FUNCTION testRegexpResultToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
return ('1' =~ m(0\t2));
$$
;
CREATE OR REPLACE FUNCTION regexp_split_to_array(string citext, pattern citext) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_split_to_array( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
-- 5.2 INFORMATION_SCHEMA_CATALOG_NAME view appears later.


/*
 * 5.3
 * CARDINAL_NUMBER domain
 */

CREATE DOMAIN cardinal_number AS integer
    CONSTRAINT cardinal_number_domain_check CHECK (value >= 0)
;
CREATE FUNCTION hs_contains(hstore,hstore)
RETURNS bool
AS '$libdir/hstore','hstore_contains'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE testpub_tbl6 REPLICA IDENTITY FULL
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26230_2 (tid pg_catalog.tid)
;
VACUUM (FULL) pg_catalog.pg_user_mapping
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
create or replace view agg_view1 as
  select aggfns(distinct a,b,c order by b)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c),
         generate_series(1,3) i
;
/*
 * 5.62
 * TABLE_METHOD_PRIVILEGES view
 */

-- feature not supported


/*
 * 5.63
 * TABLE_PRIVILEGES view
 */

CREATE VIEW table_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.prtype AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, c.relowner, 'USAGE')
                  OR c.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable,
           CAST(CASE WHEN c.prtype = 'SELECT' THEN 'YES' ELSE 'NO' END AS yes_or_no) AS with_hierarchy

    FROM (
            SELECT oid, relname, relnamespace, relkind, relowner, (aclexplode(coalesce(relacl, acldefault('r', relowner)))).* FROM pg_class
         ) AS c (oid, relname, relnamespace, relkind, relowner, grantor, grantee, prtype, grantable),
         pg_namespace nc,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE c.relnamespace = nc.oid
          AND c.relkind IN ('r', 'v', 'f', 'p')
          AND c.grantee = grantee.oid
          AND c.grantor = u_grantor.oid
          AND c.prtype IN ('INSERT', 'SELECT', 'UPDATE', 'DELETE', 'TRUNCATE', 'REFERENCES', 'TRIGGER')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
CREATE FUNCTION word_similarity_dist_op(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE "public"."gtest10a" (
    "a" integer NOT NULL,
    "........pg.dropped.2........" INTEGER /* dummy */
)
;
DROP TABLE leader
;
-- Ensure partitions are scanned for null values when adding a PK
create table cnn2_parted(a int) partition by list (a)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_opfamily
;
create table regular_table (a int, b text)
;
DROP SCHEMA temp_func_test CASCADE
;
DO LANGUAGE plpgsql 'BEGIN EXECUTE ''COPY copy1 FROM ''''/src/src/pl/plpgsql/src/results/copy1.data''''''; END'
;
create table testhstore0 (a integer, b text, c numeric, d float8)
;
CREATE TEXT SEARCH DICTIONARY thesaurus (
                        Template=thesaurus,
						DictFile=thesaurus_sample,
						Dictionary=english_stem
)
;
CREATE SCHEMA newschema4 AUTHORIZATION SESSION_USER
;
ALTER TABLE pg_toast.pg_toast_33068 RENAME TO reind_con_toast
;
ALTER INDEX pg_toast.pg_toast_28229_index RENAME TO reind_con_toast_idx
;
drop trigger child1_insert_trig on child1
;
--
-- Clean up
--
DROP VIEW lock_view8
;
ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (isbn, ean13),
	OPERATOR 1  = (isbn, isbn13)
;
CREATE OPERATOR FAMILY isn_ops USING btree
;
--    P    N        N    P
-- should ERROR: tf2p(anyarray, int) not matched by tf2p(int[],anyelement)
CREATE AGGREGATE myaggp14a(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
-- add a primary key column without a default (fails).
alter table atacc1 add column test2 int primary key
;
ALTER TABLE ONLY "public"."pagg_tab_ml_p2" ATTACH PARTITION "public"."pagg_tab_ml_p2_s2" FOR VALUES IN ('0003')
;
CREATE TRIGGER trig_ins_pk AFTER INSERT ON fkpart11.pk FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
CREATE VIEW pg_statio_sys_sequences AS
    SELECT * FROM pg_statio_all_sequences
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
DROP TABLE testpub_tbl3, testpub_tbl3a
;
-- test inheritance mismatches between parent and child
CREATE TABLE gtestx (x int, b int DEFAULT 10) INHERITS (gtest1)
;
create index matest0i on matest0 ((1-id))
;
/* contrib/adminpack/adminpack--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


/* ***********************************************
 * Administrative functions for PostgreSQL
 * *********************************************** */

/* generic file access functions */

CREATE FUNCTION pg_catalog.pg_file_write(text, text, bool)
RETURNS bigint
AS '$libdir/adminpack', 'pg_file_write'
LANGUAGE C VOLATILE STRICT
;
--
-- Test that ORDER BY for UNION ALL can be pushed down to inheritance
-- children.
--

CREATE TEMP TABLE t1c (b text, a text)
;
VACUUM ANALYZE ihighway
;
create function tg_slotlink_unset(bpchar, bpchar)
returns integer as '
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    if mytype = ''PS'' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''WS'' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update WSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''IF'' then
        select into rec * from IFace where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update IFace set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''HS'' then
        select into rec * from HSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update HSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''PH'' then
        select into rec * from PHone where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PHone set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
end;
' language plpgsql
;
ANALYZE pg_temp_12.pg_temp_25379
;
CREATE TABLE list_parted2 (
	a int,
	b char
	) PARTITION BY LIST (a)
;
create type record_type as (x text, y int, z boolean)
;
-- foreign table cannot be part of partition tree made of temporary
-- relations.
CREATE TEMP TABLE temp_parted (a int) PARTITION BY LIST (a)
;
DROP TABLE tx2
;
DROP CAST IF EXISTS (INTEGER AS no_such_schema.bar)
;
alter table fkpart0.fk_part_4 drop constraint fk_part_a_fkey
;
CREATE TABLE pxtest4 (a int)
;
-- OK, already there
ALTER FUNCTION alt_func2(int) SET SCHEMA alt_nsp2
;
------------------------------------------------------------------------------------------
[ADD_INDEX] -- ... but this one is.
create index on idxpart21 (a)
;
----------
[DROP_TABLE] drop table domarrtest
;
----------
[CREATE_PROG_OBJ] -- this revealed a bug in the original implementation
CREATE FUNCTION testRegexpResultToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
return ('1' =~ m(0\t2));
$$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_split_to_array(string citext, pattern citext) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_split_to_array( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TYPE] -- 5.2 INFORMATION_SCHEMA_CATALOG_NAME view appears later.


/*
 * 5.3
 * CARDINAL_NUMBER domain
 */

CREATE DOMAIN cardinal_number AS integer
    CONSTRAINT cardinal_number_domain_check CHECK (value >= 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hs_contains(hstore,hstore)
RETURNS bool
AS '$libdir/hstore','hstore_contains'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE testpub_tbl6 REPLICA IDENTITY FULL
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26230_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_user_mapping
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_VIEW(SELECT)] create or replace view agg_view1 as
  select aggfns(distinct a,b,c order by b)
    from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c),
         generate_series(1,3) i
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.62
 * TABLE_METHOD_PRIVILEGES view
 */

-- feature not supported


/*
 * 5.63
 * TABLE_PRIVILEGES view
 */

CREATE VIEW table_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.prtype AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, c.relowner, 'USAGE')
                  OR c.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable,
           CAST(CASE WHEN c.prtype = 'SELECT' THEN 'YES' ELSE 'NO' END AS yes_or_no) AS with_hierarchy

    FROM (
            SELECT oid, relname, relnamespace, relkind, relowner, (aclexplode(coalesce(relacl, acldefault('r', relowner)))).* FROM pg_class
         ) AS c (oid, relname, relnamespace, relkind, relowner, grantor, grantee, prtype, grantable),
         pg_namespace nc,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE c.relnamespace = nc.oid
          AND c.relkind IN ('r', 'v', 'f', 'p')
          AND c.grantee = grantee.oid
          AND c.grantor = u_grantor.oid
          AND c.prtype IN ('INSERT', 'SELECT', 'UPDATE', 'DELETE', 'TRUNCATE', 'REFERENCES', 'TRIGGER')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION word_similarity_dist_op(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest10a" (
    "a" integer NOT NULL,
    "........pg.dropped.2........" INTEGER /* dummy */
)
;
----------
[DROP_TABLE] DROP TABLE leader
;
----------
[CREATE_TABLE] -- Ensure partitions are scanned for null values when adding a PK
create table cnn2_parted(a int) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_opfamily
;
----------
[CREATE_TABLE] create table regular_table (a int, b text)
;
----------
[DROP_SCHEMA] DROP SCHEMA temp_func_test CASCADE
;
----------
[BLOCK] DO LANGUAGE plpgsql 'BEGIN EXECUTE ''COPY copy1 FROM ''''/src/src/pl/plpgsql/src/results/copy1.data''''''; END'
;
----------
[CREATE_TABLE] create table testhstore0 (a integer, b text, c numeric, d float8)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY thesaurus (
                        Template=thesaurus,
						DictFile=thesaurus_sample,
						Dictionary=english_stem
)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema4 AUTHORIZATION SESSION_USER
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33068 RENAME TO reind_con_toast
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28229_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TRIGGER] drop trigger child1_insert_trig on child1
;
----------
[DROP_VIEW] --
-- Clean up
--
DROP VIEW lock_view8
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (isbn, ean13),
	OPERATOR 1  = (isbn, isbn13)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY isn_ops USING btree
;
----------
[CREATE_PROG_OBJ] --    P    N        N    P
-- should ERROR: tf2p(anyarray, int) not matched by tf2p(int[],anyelement)
CREATE AGGREGATE myaggp14a(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ALTER_TABLE|ADD_COLUMN] -- add a primary key column without a default (fails).
alter table atacc1 add column test2 int primary key
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml_p2" ATTACH PARTITION "public"."pagg_tab_ml_p2_s2" FOR VALUES IN ('0003')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_ins_pk AFTER INSERT ON fkpart11.pk FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_statio_sys_sequences AS
    SELECT * FROM pg_statio_all_sequences
    WHERE schemaname IN ('pg_catalog', 'information_schema') OR
          schemaname ~ '^pg_toast'
;
----------
[DROP_TABLE] DROP TABLE testpub_tbl3, testpub_tbl3a
;
----------
[CREATE_TABLE] -- test inheritance mismatches between parent and child
CREATE TABLE gtestx (x int, b int DEFAULT 10) INHERITS (gtest1)
;
----------
[ADD_INDEX] create index matest0i on matest0 ((1-id))
;
----------
[CREATE_PROG_OBJ] /* contrib/adminpack/adminpack--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


/* ***********************************************
 * Administrative functions for PostgreSQL
 * *********************************************** */

/* generic file access functions */

CREATE FUNCTION pg_catalog.pg_file_write(text, text, bool)
RETURNS bigint
AS '$libdir/adminpack', 'pg_file_write'
LANGUAGE C VOLATILE STRICT
;
----------
[CREATE_TABLE] --
-- Test that ORDER BY for UNION ALL can be pushed down to inheritance
-- children.
--

CREATE TEMP TABLE t1c (b text, a text)
;
----------
[ADMIN_TABLE] VACUUM ANALYZE ihighway
;
----------
[CREATE_PROG_OBJ] create function tg_slotlink_unset(bpchar, bpchar)
returns integer as '
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    if mytype = ''PS'' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''WS'' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update WSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''IF'' then
        select into rec * from IFace where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update IFace set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''HS'' then
        select into rec * from HSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update HSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''PH'' then
        select into rec * from PHone where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PHone set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
end;
' language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_25379
;
----------
[CREATE_TABLE] CREATE TABLE list_parted2 (
	a int,
	b char
	) PARTITION BY LIST (a)
;
----------
[CREATE_TYPE] create type record_type as (x text, y int, z boolean)
;
----------
[CREATE_TABLE] -- foreign table cannot be part of partition tree made of temporary
-- relations.
CREATE TEMP TABLE temp_parted (a int) PARTITION BY LIST (a)
;
----------
[DROP_TABLE] DROP TABLE tx2
;
----------
[DROP_PROG_OBJ] DROP CAST IF EXISTS (INTEGER AS no_such_schema.bar)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table fkpart0.fk_part_4 drop constraint fk_part_a_fkey
;
----------
[CREATE_TABLE] CREATE TABLE pxtest4 (a int)
;
----------
[ALTER_PROG_OBJ] -- OK, already there
ALTER FUNCTION alt_func2(int) SET SCHEMA alt_nsp2
;
