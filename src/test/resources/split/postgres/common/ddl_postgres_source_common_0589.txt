CREATE FUNCTION "public"."cache_test_2"() RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
begin
	return f1 from v1;
end$$
;
CREATE FUNCTION b() RETURNS TRIGGER LANGUAGE plpgsql AS
   $$ BEGIN NEW.a1 := NEW.a1 + 42; RETURN NEW; END; $$
;
DROP AGGREGATE priv_testagg1(int)
;
drop trigger trg1 on trigpart
;
TRUNCATE vistest
;
--
-- Test triggers on views
--

CREATE VIEW main_view AS SELECT a, b FROM main_table
;
CREATE INDEX ON test4c (x)
;
ALTER TABLE fd_pt1 DROP COLUMN c4
;
CREATE TABLE "public"."btree_tall_tbl" (
    "id" integer,
    "t" "text"
)
;
-- multiple tables specified
VACUUM vaccluster, vactst
;
CREATE TABLE d3_listp1 PARTITION OF d3_listp FOR VALUES IN (1)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_sequence
;
ALTER TABLE pg_temp_11.pg_temp_41126_2 ADD COLUMN newdata pg_temp_11.pg_temp_41126
;
VACUUM (FREEZE) pg_catalog.pg_shdepend
;
-- fail
CREATE TABLE collate_test4 (
    a int,
    b testdomain_p
)
;
CREATE TRIGGER c1_delete_trig
  AFTER DELETE ON part_c_1_100 for each statement execute procedure trigfunc()
;
CREATE TABLE dependee (x integer, y integer)
;
CREATE TRIGGER y_trig BEFORE INSERT ON y FOR EACH ROW
    EXECUTE PROCEDURE y_trigger()
;
-- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0
;
-- Test behavior of the constraint together with attaching and detaching
-- partitions.
CREATE TABLE fk_partitioned_fk_2 PARTITION OF fk_partitioned_fk FOR VALUES IN (1500,1502)
;
-- Clean up
DROP FOREIGN TABLE foreign_tbl CASCADE
;
DROP TABLE pg_temp_10.pg_temp_26308_2, pg_temp_10.pg_temp_26308
;
ALTER TABLE public.atref ADD CONSTRAINT atref_b_check CHECK (((b).a IS NOT NULL))
;
-- error

-- foreign key actions
-- Some of the behaviors are most easily visible with a
-- case-insensitive collation.
CREATE TABLE test12pk (x text COLLATE case_insensitive PRIMARY KEY)
;
DROP DOMAIN domain3
;
--    P    N        P    N
-- should ERROR: tfnp(anyarray, anyelement) not matched by tfnp(int[],int)
CREATE AGGREGATE myaggn15a(BASETYPE = anyelement, SFUNC = tfnp,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE TYPE "public"."jpop2" AS (
	"a" integer,
	"b" json,
	"c" integer,
	"d" integer
)
;
CREATE TEMPORARY TABLE pgss_ctas_2 AS EXECUTE test_prepare_pgss
;
ALTER TEXT SEARCH CONFIGURATION estonian ADD MAPPING
    FOR word, hword_part, hword
	WITH estonian_stem
;
CREATE EXTENSION hstore WITH SCHEMA split_extension_next VERSION '1.8'
;
ALTER SEQUENCE "public"."sj_t1_id_seq" OWNED BY "public"."sj_t1"."id"
;
ANALYZE pg_temp_9.pg_temp_26339
;
-- cross-partition update that uses ReturningExpr nodes, without returning
-- old/new table values
CREATE VIEW foo_parted_v AS SELECT *, 'xxx' AS dummy FROM foo_parted
;
CREATE TABLE fk_partitioned_fk_3 (fdrop1 int, fdrop2 int, fdrop3 int, fdrop4 int, b int, a int)
  PARTITION BY HASH (a)
;
ALTER TABLE pg_toast.pg_toast_30850 RENAME TO reind_con_toast
;
-- a view should also be temporary if it references a temporary view
CREATE VIEW v10_temp AS SELECT * FROM v7_temp
;
ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = non_existent_func)
;
DROP FUNCTION ctoast_lock_func(int)
;
--
-- Unique keys
--

CREATE TABLE UNIQUE_TBL (i int UNIQUE, t text)
;
CREATE TABLE pktable (
	id		INT4 PRIMARY KEY,
	other	INT4
)
;
ALTER TABLE ONLY "public"."prt2" ATTACH PARTITION "public"."prt2_p3" FOR VALUES FROM (500) TO (600)
;
create table mcrparted4 partition of mcrparted for values from (21, minvalue, 0) to (30, 20, minvalue)
;
CREATE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a > 0
;
ALTER TABLE pg_toast.pg_toast_31047 RENAME TO reind_con_toast
;
DROP VIEW mvtest_vt1 CASCADE
;
CREATE TABLE "public"."tt4" (
    "x" integer
)
;
CREATE OR REPLACE RULE voo_i AS ON INSERT TO voo DO INSTEAD
  INSERT INTO foo VALUES(new.*, 57) RETURNING *
;
VACUUM (ANALYZE) pg_catalog.pg_db_role_setting
;
-- fail

alter domain di drop constraint pos
;
CREATE INDEX "brin_test_a_idx" ON "public"."brin_test" USING "brin" ("a") WITH ("pages_per_range"='2')
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."cache_test_2"() RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
begin
	return f1 from v1;
end$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION b() RETURNS TRIGGER LANGUAGE plpgsql AS
   $$ BEGIN NEW.a1 := NEW.a1 + 42; RETURN NEW; END; $$
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE priv_testagg1(int)
;
----------
[DROP_TRIGGER] drop trigger trg1 on trigpart
;
----------
[TRUNCATE_TABLE] TRUNCATE vistest
;
----------
[CREATE_VIEW(SELECT)] --
-- Test triggers on views
--

CREATE VIEW main_view AS SELECT a, b FROM main_table
;
----------
[ADD_INDEX] CREATE INDEX ON test4c (x)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fd_pt1 DROP COLUMN c4
;
----------
[CREATE_TABLE] CREATE TABLE "public"."btree_tall_tbl" (
    "id" integer,
    "t" "text"
)
;
----------
[ADMIN_TABLE] -- multiple tables specified
VACUUM vaccluster, vactst
;
----------
[CREATE_TABLE] CREATE TABLE d3_listp1 PARTITION OF d3_listp FOR VALUES IN (1)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_sequence
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_41126_2 ADD COLUMN newdata pg_temp_11.pg_temp_41126
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_shdepend
;
----------
[CREATE_TABLE] -- fail
CREATE TABLE collate_test4 (
    a int,
    b testdomain_p
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER c1_delete_trig
  AFTER DELETE ON part_c_1_100 for each statement execute procedure trigfunc()
;
----------
[CREATE_TABLE] CREATE TABLE dependee (x integer, y integer)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER y_trig BEFORE INSERT ON y FOR EACH ROW
    EXECUTE PROCEDURE y_trigger()
;
----------
[CREATE_VIEW] -- ok
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0
;
----------
[CREATE_TABLE] -- Test behavior of the constraint together with attaching and detaching
-- partitions.
CREATE TABLE fk_partitioned_fk_2 PARTITION OF fk_partitioned_fk FOR VALUES IN (1500,1502)
;
----------
[DROP_TABLE] -- Clean up
DROP FOREIGN TABLE foreign_tbl CASCADE
;
----------
[DROP_TABLE] DROP TABLE pg_temp_10.pg_temp_26308_2, pg_temp_10.pg_temp_26308
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.atref ADD CONSTRAINT atref_b_check CHECK (((b).a IS NOT NULL))
;
----------
[CREATE_TABLE] -- error

-- foreign key actions
-- Some of the behaviors are most easily visible with a
-- case-insensitive collation.
CREATE TABLE test12pk (x text COLLATE case_insensitive PRIMARY KEY)
;
----------
[DROP_TYPE] DROP DOMAIN domain3
;
----------
[CREATE_PROG_OBJ] --    P    N        P    N
-- should ERROR: tfnp(anyarray, anyelement) not matched by tfnp(int[],int)
CREATE AGGREGATE myaggn15a(BASETYPE = anyelement, SFUNC = tfnp,
  STYPE = anyarray, FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_TYPE] CREATE TYPE "public"."jpop2" AS (
	"a" integer,
	"b" json,
	"c" integer,
	"d" integer
)
;
----------
[CREATE_TABLE|UNSAFE] CREATE TEMPORARY TABLE pgss_ctas_2 AS EXECUTE test_prepare_pgss
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION estonian ADD MAPPING
    FOR word, hword_part, hword
	WITH estonian_stem
;
----------
[CREATE_LIBRARY] CREATE EXTENSION hstore WITH SCHEMA split_extension_next VERSION '1.8'
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."sj_t1_id_seq" OWNED BY "public"."sj_t1"."id"
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26339
;
----------
[CREATE_VIEW(SELECT)] -- cross-partition update that uses ReturningExpr nodes, without returning
-- old/new table values
CREATE VIEW foo_parted_v AS SELECT *, 'xxx' AS dummy FROM foo_parted
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_3 (fdrop1 int, fdrop2 int, fdrop3 int, fdrop4 int, b int, a int)
  PARTITION BY HASH (a)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30850 RENAME TO reind_con_toast
;
----------
[CREATE_VIEW(SELECT)] -- a view should also be temporary if it references a temporary view
CREATE VIEW v10_temp AS SELECT * FROM v7_temp
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = non_existent_func)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION ctoast_lock_func(int)
;
----------
[CREATE_TABLE] --
-- Unique keys
--

CREATE TABLE UNIQUE_TBL (i int UNIQUE, t text)
;
----------
[CREATE_TABLE] CREATE TABLE pktable (
	id		INT4 PRIMARY KEY,
	other	INT4
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2" ATTACH PARTITION "public"."prt2_p3" FOR VALUES FROM (500) TO (600)
;
----------
[CREATE_TABLE] create table mcrparted4 partition of mcrparted for values from (21, minvalue, 0) to (30, 20, minvalue)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a > 0
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_31047 RENAME TO reind_con_toast
;
----------
[DROP_VIEW] DROP VIEW mvtest_vt1 CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tt4" (
    "x" integer
)
;
----------
[CREATE_POLICY] CREATE OR REPLACE RULE voo_i AS ON INSERT TO voo DO INSTEAD
  INSERT INTO foo VALUES(new.*, 57) RETURNING *
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_db_role_setting
;
----------
[ALTER_TYPE] -- fail

alter domain di drop constraint pos
;
----------
[ADD_INDEX] CREATE INDEX "brin_test_a_idx" ON "public"."brin_test" USING "brin" ("a") WITH ("pages_per_range"='2')
;
