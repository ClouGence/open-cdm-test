CREATE SCHEMA import_dest4
;
ALTER OPERATOR FAMILY gist_timestamp_ops USING gist ADD
	OPERATOR	6	<> (timestamp, timestamp) ,
	OPERATOR	15	<-> (timestamp, timestamp) FOR ORDER BY pg_catalog.interval_ops ,
	FUNCTION	8 (timestamp, timestamp) gbt_ts_distance (internal, timestamp, int2, oid, internal) ,
	FUNCTION	9 (timestamp, timestamp) gbt_ts_fetch (internal)
;
DROP TYPE IF EXISTS split_table_idx.typed_row
;
create function namedparmcursor_test6() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77);
end
$$ language plpgsql
;
CREATE AGGREGATE split_agg_probe.sum_variadic(VARIADIC integer[]) (
    SFUNC = split_agg_probe.sum_variadic_state,
    STYPE = integer,
    INITCOND = '0'
)
;
CREATE OPERATOR ~~* (
    PROCEDURE = texticlike,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = !~~*,
    RESTRICT  = iclikesel,
    JOIN      = iclikejoinsel
)
;
drop domain domaindroptest cascade
;
ANALYZE pg_temp_3.pg_temp_29539_2
;
CREATE TABLE parttab1 (key INTEGER PRIMARY KEY, col1 text, col2 text)
;
create table mlparted11 (like mlparted1)
;
-- set null update / set default delete
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, PRIMARY KEY(ptest1, ptest2, ptest3) )
;
ALTER TABLE pg_temp_10.pg_temp_26708_2 ADD COLUMN newdata pg_temp_10.pg_temp_26708
;
CREATE TABLE pitest2 (f1 date NOT NULL, f2 text) PARTITION BY RANGE (f1)
;
DROP TABLE will_get_dropped_in_ts
;
alter table c_star set (parallel_workers = 0)
;
ALTER TABLE T ADD COLUMN c_text TEXT DEFAULT 'Hello'
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_conversion
;
-- table partitions

-- partitions inherit identity column and share sequence
CREATE TABLE pitest1 (f1 date NOT NULL, f2 text, f3 bigint generated always as identity) PARTITION BY RANGE (f1)
;
ALTER TABLE pg_toast.pg_toast_28250 RENAME TO reind_con_toast
;
CREATE TABLE inhg (x text, LIKE inhx INCLUDING CONSTRAINTS, y text)
;
create function test_ret_set_scalar(int,int) returns setof int as '
DECLARE
	i int;
BEGIN
	FOR i IN $1 .. $2 LOOP
		RETURN NEXT i + 1;
	END LOOP;
	RETURN;
END;' language plpgsql
;
-- For binary upgrade, set up inherited constraint.
ALTER TABLE ONLY "public"."test_inh_check_child" ADD CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_language
;
CREATE TABLE pagg_tab_ml_p2_s2 PARTITION OF pagg_tab_ml_p2 FOR VALUES IN ('0002', '0003')
;
-- check that violating rows are correctly reported when attaching as the
-- default partition
create table defpart_attach_test (a int) partition by list (a)
;
/* New stuff in 1.2 begins here */

CREATE OR REPLACE FUNCTION pgstattuple(IN reloid regclass,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuplebyid_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE PUBLICATION tap_pub for table tab_rep
;
create view insertconflictv as
  select * from insertconflict with cascaded check option
;
CREATE TABLE regtest_ptable_4_ones PARTITION OF regtest_ptable_4 FOR VALUES FROM ('0') TO ('10')
;
ALTER OPERATOR ? (hstore, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
create function excpt_test1() returns void as $$
begin
    raise notice '% %', sqlstate, sqlerrm;
end; $$ language plpgsql
;
-- tests for array aggregates
CREATE TEMP TABLE arraggtest ( f1 INT[], f2 TEXT[][], f3 FLOAT[])
;
ALTER SEQUENCE sequence_test10 AS smallint
;
CREATE FUNCTION gin_compare_prefix_bpchar(bpchar, bpchar, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE public.tbl ADD CONSTRAINT tbl_c1_c2_c3_c4_key UNIQUE (c1, c2) INCLUDE (c3, c4)
;
CREATE PUBLICATION pub_gencol FOR TABLE testpub_gencol
;
create table dyntest ( name char ( 14 ) , d float8 , i int , bignumber int8 , b boolean , comment text , day date )
;
vacuum analyze t
;
create trigger brtrigpartcon1trig before insert on brtrigpartcon1 for each row execute procedure brtrigpartcon1trigf()
;
create function inner_func(int)
returns int as $$
declare
  _context text;
  sx int := 5;
begin
  begin
    perform sx / 0;
  exception
    when division_by_zero then
      get diagnostics _context = pg_context;
      raise notice '***%***', _context;
  end;

  -- lets do it again, just for fun..
  get diagnostics _context = pg_context;
  raise notice '***%***', _context;
  raise notice 'lets make sure we didnt break anything';
  return 2 * $1;
end;
$$ language plpgsql
;
ALTER TABLE ONLY "public"."gtest21a"
    ADD CONSTRAINT "gtest21a_pkey" PRIMARY KEY ("a")
;
alter table fkpart2.fk_part detach partition fkpart2.fk_part_1
;
CREATE SCHEMA s1
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_type
;
ALTER TABLE t ADD COLUMN a int DEFAULT 1
;
CREATE INDEX t1_gin ON s5.t1 USING GIN (ia)
;
-- fail, violating row:
alter table atacc2 add constraint foo check (test>0) no inherit
;
-- Check standard normal distribution using the Kolmogorov-Smirnov test.

CREATE FUNCTION ks_test_normal_random()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random_normal() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs((1+erf(r/sqrt(2)))/2 - i/n)) < c / sqrt(n)
    FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
DO LANGUAGE plpgsql 'BEGIN COPY copy1 TO ''/src/src/pl/plpgsql/src/results/copy1.data''; END'
;
CREATE VIEW triggered_update_columns AS
    SELECT CAST(current_database() AS sql_identifier) AS trigger_catalog,
           CAST(n.nspname AS sql_identifier) AS trigger_schema,
           CAST(t.tgname AS sql_identifier) AS trigger_name,
           CAST(current_database() AS sql_identifier) AS event_object_catalog,
           CAST(n.nspname AS sql_identifier) AS event_object_schema,
           CAST(c.relname AS sql_identifier) AS event_object_table,
           CAST(a.attname AS sql_identifier) AS event_object_column

    FROM pg_namespace n, pg_class c, pg_trigger t,
         (SELECT tgoid, (ta0.tgat).x AS tgattnum, (ta0.tgat).n AS tgattpos
          FROM (SELECT oid AS tgoid, information_schema._pg_expandarray(tgattr) AS tgat FROM pg_trigger) AS ta0) AS ta,
         pg_attribute a

    WHERE n.oid = c.relnamespace
          AND c.oid = t.tgrelid
          AND t.oid = ta.tgoid
          AND (a.attrelid, a.attnum) = (t.tgrelid, ta.tgattnum)
          AND NOT t.tgisinternal
          AND (NOT pg_is_other_temp_schema(n.oid))
          AND (pg_has_role(c.relowner, 'USAGE')
               -- SELECT privilege omitted, per SQL standard
               OR has_column_privilege(c.oid, a.attnum, 'INSERT, UPDATE, REFERENCES') )
;
------------------------------------------------------------------------------------------
[CREATE_SCHEMA] CREATE SCHEMA import_dest4
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timestamp_ops USING gist ADD
	OPERATOR	6	<> (timestamp, timestamp) ,
	OPERATOR	15	<-> (timestamp, timestamp) FOR ORDER BY pg_catalog.interval_ops ,
	FUNCTION	8 (timestamp, timestamp) gbt_ts_distance (internal, timestamp, int2, oid, internal) ,
	FUNCTION	9 (timestamp, timestamp) gbt_ts_fetch (internal)
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS split_table_idx.typed_row
;
----------
[CREATE_PROG_OBJ] create function namedparmcursor_test6() returns void as $$
declare
  c1 cursor (p1 int, p2 int) for
    select * from tenk1 where thousand = p1 and tenthous = p2;
begin
  open c1 (p2 := 77);
end
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE split_agg_probe.sum_variadic(VARIADIC integer[]) (
    SFUNC = split_agg_probe.sum_variadic_state,
    STYPE = integer,
    INITCOND = '0'
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~~* (
    PROCEDURE = texticlike,
    LEFTARG   = citext,
    RIGHTARG  = text,
    NEGATOR   = !~~*,
    RESTRICT  = iclikesel,
    JOIN      = iclikejoinsel
)
;
----------
[DROP_TYPE] drop domain domaindroptest cascade
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_29539_2
;
----------
[CREATE_TABLE] CREATE TABLE parttab1 (key INTEGER PRIMARY KEY, col1 text, col2 text)
;
----------
[CREATE_TABLE] create table mlparted11 (like mlparted1)
;
----------
[CREATE_TABLE] -- set null update / set default delete
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, PRIMARY KEY(ptest1, ptest2, ptest3) )
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_26708_2 ADD COLUMN newdata pg_temp_10.pg_temp_26708
;
----------
[CREATE_TABLE] CREATE TABLE pitest2 (f1 date NOT NULL, f2 text) PARTITION BY RANGE (f1)
;
----------
[DROP_TABLE] DROP TABLE will_get_dropped_in_ts
;
----------
[ALTER_TABLE] alter table c_star set (parallel_workers = 0)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE T ADD COLUMN c_text TEXT DEFAULT 'Hello'
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_conversion
;
----------
[CREATE_TABLE] -- table partitions

-- partitions inherit identity column and share sequence
CREATE TABLE pitest1 (f1 date NOT NULL, f2 text, f3 bigint generated always as identity) PARTITION BY RANGE (f1)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28250 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE inhg (x text, LIKE inhx INCLUDING CONSTRAINTS, y text)
;
----------
[CREATE_PROG_OBJ] create function test_ret_set_scalar(int,int) returns setof int as '
DECLARE
	i int;
BEGIN
	FOR i IN $1 .. $2 LOOP
		RETURN NEXT i + 1;
	END LOOP;
	RETURN;
END;' language plpgsql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- For binary upgrade, set up inherited constraint.
ALTER TABLE ONLY "public"."test_inh_check_child" ADD CONSTRAINT "test_inh_check_a_check" CHECK ((("a")::double precision > (10.2)::double precision))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_language
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_ml_p2_s2 PARTITION OF pagg_tab_ml_p2 FOR VALUES IN ('0002', '0003')
;
----------
[CREATE_TABLE] -- check that violating rows are correctly reported when attaching as the
-- default partition
create table defpart_attach_test (a int) partition by list (a)
;
----------
[CREATE_PROG_OBJ] /* New stuff in 1.2 begins here */

CREATE OR REPLACE FUNCTION pgstattuple(IN reloid regclass,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuplebyid_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub for table tab_rep
;
----------
[CREATE_VIEW(SELECT)] create view insertconflictv as
  select * from insertconflict with cascaded check option
;
----------
[CREATE_TABLE] CREATE TABLE regtest_ptable_4_ones PARTITION OF regtest_ptable_4 FOR VALUES FROM ('0') TO ('10')
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ? (hstore, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_PROG_OBJ] create function excpt_test1() returns void as $$
begin
    raise notice '% %', sqlstate, sqlerrm;
end; $$ language plpgsql
;
----------
[CREATE_TABLE] -- tests for array aggregates
CREATE TEMP TABLE arraggtest ( f1 INT[], f2 TEXT[][], f3 FLOAT[])
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test10 AS smallint
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_bpchar(bpchar, bpchar, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.tbl ADD CONSTRAINT tbl_c1_c2_c3_c4_key UNIQUE (c1, c2) INCLUDE (c3, c4)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub_gencol FOR TABLE testpub_gencol
;
----------
[CREATE_TABLE] create table dyntest ( name char ( 14 ) , d float8 , i int , bignumber int8 , b boolean , comment text , day date )
;
----------
[ADMIN_TABLE] vacuum analyze t
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger brtrigpartcon1trig before insert on brtrigpartcon1 for each row execute procedure brtrigpartcon1trigf()
;
----------
[CREATE_PROG_OBJ] create function inner_func(int)
returns int as $$
declare
  _context text;
  sx int := 5;
begin
  begin
    perform sx / 0;
  exception
    when division_by_zero then
      get diagnostics _context = pg_context;
      raise notice '***%***', _context;
  end;

  -- lets do it again, just for fun..
  get diagnostics _context = pg_context;
  raise notice '***%***', _context;
  raise notice 'lets make sure we didnt break anything';
  return 2 * $1;
end;
$$ language plpgsql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest21a"
    ADD CONSTRAINT "gtest21a_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table fkpart2.fk_part detach partition fkpart2.fk_part_1
;
----------
[CREATE_SCHEMA] CREATE SCHEMA s1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_type
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t ADD COLUMN a int DEFAULT 1
;
----------
[ADD_INDEX] CREATE INDEX t1_gin ON s5.t1 USING GIN (ia)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- fail, violating row:
alter table atacc2 add constraint foo check (test>0) no inherit
;
----------
[CREATE_PROG_OBJ] -- Check standard normal distribution using the Kolmogorov-Smirnov test.

CREATE FUNCTION ks_test_normal_random()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random_normal() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs((1+erf(r/sqrt(2)))/2 - i/n)) < c / sqrt(n)
    FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
----------
[BLOCK] DO LANGUAGE plpgsql 'BEGIN COPY copy1 TO ''/src/src/pl/plpgsql/src/results/copy1.data''; END'
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW triggered_update_columns AS
    SELECT CAST(current_database() AS sql_identifier) AS trigger_catalog,
           CAST(n.nspname AS sql_identifier) AS trigger_schema,
           CAST(t.tgname AS sql_identifier) AS trigger_name,
           CAST(current_database() AS sql_identifier) AS event_object_catalog,
           CAST(n.nspname AS sql_identifier) AS event_object_schema,
           CAST(c.relname AS sql_identifier) AS event_object_table,
           CAST(a.attname AS sql_identifier) AS event_object_column

    FROM pg_namespace n, pg_class c, pg_trigger t,
         (SELECT tgoid, (ta0.tgat).x AS tgattnum, (ta0.tgat).n AS tgattpos
          FROM (SELECT oid AS tgoid, information_schema._pg_expandarray(tgattr) AS tgat FROM pg_trigger) AS ta0) AS ta,
         pg_attribute a

    WHERE n.oid = c.relnamespace
          AND c.oid = t.tgrelid
          AND t.oid = ta.tgoid
          AND (a.attrelid, a.attnum) = (t.tgrelid, ta.tgattnum)
          AND NOT t.tgisinternal
          AND (NOT pg_is_other_temp_schema(n.oid))
          AND (pg_has_role(c.relowner, 'USAGE')
               -- SELECT privilege omitted, per SQL standard
               OR has_column_privilege(c.oid, a.attnum, 'INSERT, UPDATE, REFERENCES') )
;
