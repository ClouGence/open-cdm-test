COPY forcetest (a, b, c) FROM STDIN WITH (FORMAT csv, FORCE_NOT_NULL *, FORCE_NULL *)
;
SET min_parallel_table_scan_size TO 0
;
EXECUTE dumpFunc('16500')
;
-- create statistics on expressions
CREATE STATISTICS func_deps_stat (dependencies) ON (a * 2), upper(b), (c + 1) FROM functional_dependencies
;
CREATE STATISTICS mcv_lists_uuid_stats (mcv) ON a, b, c
  FROM mcv_lists_uuid
;
NOTIFY c1, 'p2'
;
LOCK TABLE "public"."prt2_l_p1" IN ACCESS SHARE MODE
;
COPY public.hobbies_r (name, person) TO stdout
;
-- ok, we're a superuser
ALTER SUBSCRIPTION regress_testsub OWNER TO regress_subscription_user2
;
ALTER TABLE public.temporal_fk_mltrng2mltrng OWNER TO zyc
;
------------------------------------------------------------------------------------------
[DATA_IMPORT] COPY forcetest (a, b, c) FROM STDIN WITH (FORMAT csv, FORCE_NOT_NULL *, FORCE_NULL *)
;
----------
[SESSION_SETTING_WRITE] SET min_parallel_table_scan_size TO 0
;
----------
[UNSAFE] EXECUTE dumpFunc('16500')
;
----------
[ADMIN_PERFORMANCE] -- create statistics on expressions
CREATE STATISTICS func_deps_stat (dependencies) ON (a * 2), upper(b), (c + 1) FROM functional_dependencies
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS mcv_lists_uuid_stats (mcv) ON a, b, c
  FROM mcv_lists_uuid
;
----------
[ADMIN_PUB_SUB] NOTIFY c1, 'p2'
;
----------
[SESSION_LOCK] LOCK TABLE "public"."prt2_l_p1" IN ACCESS SHARE MODE
;
----------
[DATA_EXPORT] COPY public.hobbies_r (name, person) TO stdout
;
----------
[TRANSFER_PRIVILEGE] -- ok, we're a superuser
ALTER SUBSCRIPTION regress_testsub OWNER TO regress_subscription_user2
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.temporal_fk_mltrng2mltrng OWNER TO zyc
;
