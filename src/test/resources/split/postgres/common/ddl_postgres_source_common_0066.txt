ALTER SEQUENCE IF EXISTS sequence_test2 RESTART WITH 24
  INCREMENT BY 4 MAXVALUE 36 MINVALUE 5 CYCLE
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_db_role_setting
;
SECURITY LABEL ON SUBSCRIPTION dummy_sub IS 'classified'
;
ALTER FUNCTION gbt_tstz_consistent(internal, timestamp with time zone, smallint, oid, internal) PARALLEL SAFE
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_constraint
;
ALTER TABLE ONLY "public"."idxpart" ATTACH PARTITION "public"."idxpart1" FOR VALUES FROM (0) TO (100)
;
-- Add operators that are new in 9.3.

ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        5       pg_catalog.~ (text, text),
        OPERATOR        6       pg_catalog.~* (text, text)
;
ALTER INDEX ptif_test1_index ATTACH PARTITION ptif_test11_index
;
create table child_30_40 partition of parent_tab
for values from (30) to (40)
  partition by range(id)
;
create trigger my_table_insert_trig
  after insert on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
create table hs2 (col1 integer primary key)
;
-- Login event triggers
CREATE TABLE user_logins(id serial, who text)
;
ALTER TABLE part_5 DROP CONSTRAINT check_a
;
CREATE TYPE "generated_stored_tests"."gtestdomain1range" AS RANGE (
    subtype = "generated_stored_tests"."gtestdomain1",
    multirange_type_name = "generated_stored_tests"."gtestdomain1multirange"
)
;
drop table gtest34p
;
CREATE AGGREGATE "public"."myaggp20b"("anyelement") (
    SFUNC = "public"."tfp",
    STYPE = "anyarray",
    INITCOND = '{}'
)
;
ALTER TABLE pg_shseclabel ADD PRIMARY KEY USING INDEX pg_shseclabel_object_index
;
create operator class custom_opclass for type int4 using hash as
operator 1 = , function 2 dummy_hashint4(int4, int8)
;
--     P    N
-- should ERROR: stfnp(anyarray) not matched by stfnp(int[])
CREATE AGGREGATE myaggn02a(*) (SFUNC = stfnp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
ALTER TABLE fk_r_2 DROP CONSTRAINT fk_r_p_id_p_jd_fkey
;
ALTER TABLE pg_toast.pg_toast_30742 RENAME TO reind_con_toast
;
CREATE AGGREGATE "public"."myaggp03a"(*) (
    SFUNC = "public"."stfp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
VACUUM (PARALLEL 0) pg_catalog.pg_tablespace
;
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_othertable_pkey
;
-- should fail, require cascade
DROP TABLE test_ext.a CASCADE
;
DROP TABLE large_tuple_test
;
ANALYZE pg_temp_32.pg_temp_28090
;
/* Renaming of existing backend functions for pgAdmin compatibility */

CREATE FUNCTION pg_catalog.pg_file_read(text, bigint, bigint)
RETURNS text
AS 'pg_read_file'
LANGUAGE INTERNAL VOLATILE STRICT
;
DROP TABLE atest6
;
CREATE SUBSCRIPTION tap_sub_rep_full CONNECTION 'port=20267 host=/tmp/QFEOcZYPah dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_rep_full
;
ALTER TABLE plt2_adv DETACH PARTITION plt2_adv_p3
;
COMMENT ON CONSTRAINT no_constraint ON constraint_comments_tbl IS 'yes, the comment'
;
COMMENT ON OPERATOR split_operator_probe_next.## (integer, integer) IS 'split addition operator'
;
CREATE INDEX hash_i4_index ON hash_i4_heap USING hash (random int4_ops)
;
create table p2(f1 int constraint f1_pos CHECK (f1 > 0))
;
-- should be temp (one join rel is temp)
CREATE VIEW v4_temp AS
    SELECT t1.a AS t1_a, t2.a AS t2_a
    FROM base_table t1, temp_table t2
    WHERE t1.id = t2.id
;
-- parameters of raise stmt can be expressions
create function raise_exprs() returns void as $$
declare
    a integer[] = '{10,20,30}';
    c varchar = 'xyz';
    i integer;
begin
    i := 2;
    raise notice '%; %; %; %; %; %', a, a[i], c, (select c || 'abc'), row(10,'aaa',NULL,30), NULL;
end;$$ language plpgsql
;
-- test a custom range type with a non-default operator class
create type textrange_supp as range (
   subtype = text,
   subtype_opclass = text_pattern_ops
)
;
DO $$
DECLARE BEGIN
  EXECUTE 'CREATE TABLE pgss_do_table (id INT); DROP TABLE pgss_do_table';
  EXECUTE 'SELECT a FROM pgss_do_util_tab_1; SELECT a FROM pgss_do_util_tab_2';
END $$
;
CREATE TABLE range_parted (
	a date
) PARTITION BY RANGE (a)
;
-- btree
CREATE INDEX jidx ON testjsonb USING btree (j)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
ALTER OPERATOR FAMILY gist_text_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
VACUUM (FREEZE) pg_catalog.pg_largeobject
;
CREATE FUNCTION gbt_bool_union(internal, internal)
RETURNS gbtreekey2
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE DATABASE dropme_db2 WITH TABLESPACE dropme_ts2 STRATEGY=FILE_COPY
;
-- this exercises tuple conversion:
create function xtrig()
  returns trigger language plpgsql
as $$
  declare
    r record;
  begin
    for r in select * from old loop
      raise info 'a=%, b=%', r.a, r.b;
    end loop;
    return NULL;
  end;
$$
;
DROP TRIGGER trig_d_1_15 ON part_d_1_15
;
-- invalid options parsing function
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 6 btint42cmp(int4, int2)
;
VACUUM (PARALLEL 0) pg_catalog.pg_cast
;
------------------------------------------------------------------------------------------
[ALTER_SEQUENCE] ALTER SEQUENCE IF EXISTS sequence_test2 RESTART WITH 24
  INCREMENT BY 4 MAXVALUE 36 MINVALUE 5 CYCLE
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_db_role_setting
;
----------
[UNKNOWN] SECURITY LABEL ON SUBSCRIPTION dummy_sub IS 'classified'
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_tstz_consistent(internal, timestamp with time zone, smallint, oid, internal) PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_constraint
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."idxpart" ATTACH PARTITION "public"."idxpart1" FOR VALUES FROM (0) TO (100)
;
----------
[ALTER_PROG_OBJ] -- Add operators that are new in 9.3.

ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        5       pg_catalog.~ (text, text),
        OPERATOR        6       pg_catalog.~* (text, text)
;
----------
[ALTER_INDEX] ALTER INDEX ptif_test1_index ATTACH PARTITION ptif_test11_index
;
----------
[CREATE_TABLE] create table child_30_40 partition of parent_tab
for values from (30) to (40)
  partition by range(id)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger my_table_insert_trig
  after insert on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[CREATE_TABLE] create table hs2 (col1 integer primary key)
;
----------
[CREATE_TABLE] -- Login event triggers
CREATE TABLE user_logins(id serial, who text)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE part_5 DROP CONSTRAINT check_a
;
----------
[CREATE_TYPE] CREATE TYPE "generated_stored_tests"."gtestdomain1range" AS RANGE (
    subtype = "generated_stored_tests"."gtestdomain1",
    multirange_type_name = "generated_stored_tests"."gtestdomain1multirange"
)
;
----------
[DROP_TABLE] drop table gtest34p
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp20b"("anyelement") (
    SFUNC = "public"."tfp",
    STYPE = "anyarray",
    INITCOND = '{}'
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_shseclabel ADD PRIMARY KEY USING INDEX pg_shseclabel_object_index
;
----------
[CREATE_PROG_OBJ] create operator class custom_opclass for type int4 using hash as
operator 1 = , function 2 dummy_hashint4(int4, int8)
;
----------
[CREATE_PROG_OBJ] --     P    N
-- should ERROR: stfnp(anyarray) not matched by stfnp(int[])
CREATE AGGREGATE myaggn02a(*) (SFUNC = stfnp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE fk_r_2 DROP CONSTRAINT fk_r_p_id_p_jd_fkey
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30742 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp03a"(*) (
    SFUNC = "public"."stfp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_tablespace
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_othertable_pkey
;
----------
[DROP_TABLE] -- should fail, require cascade
DROP TABLE test_ext.a CASCADE
;
----------
[DROP_TABLE] DROP TABLE large_tuple_test
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28090
;
----------
[CREATE_PROG_OBJ] /* Renaming of existing backend functions for pgAdmin compatibility */

CREATE FUNCTION pg_catalog.pg_file_read(text, bigint, bigint)
RETURNS text
AS 'pg_read_file'
LANGUAGE INTERNAL VOLATILE STRICT
;
----------
[DROP_TABLE] DROP TABLE atest6
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_rep_full CONNECTION 'port=20267 host=/tmp/QFEOcZYPah dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_rep_full
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt2_adv DETACH PARTITION plt2_adv_p3
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT no_constraint ON constraint_comments_tbl IS 'yes, the comment'
;
----------
[COMMENT_PROG_OBJ] COMMENT ON OPERATOR split_operator_probe_next.## (integer, integer) IS 'split addition operator'
;
----------
[ADD_INDEX] CREATE INDEX hash_i4_index ON hash_i4_heap USING hash (random int4_ops)
;
----------
[CREATE_TABLE] create table p2(f1 int constraint f1_pos CHECK (f1 > 0))
;
----------
[CREATE_VIEW(SELECT)] -- should be temp (one join rel is temp)
CREATE VIEW v4_temp AS
    SELECT t1.a AS t1_a, t2.a AS t2_a
    FROM base_table t1, temp_table t2
    WHERE t1.id = t2.id
;
----------
[CREATE_PROG_OBJ] -- parameters of raise stmt can be expressions
create function raise_exprs() returns void as $$
declare
    a integer[] = '{10,20,30}';
    c varchar = 'xyz';
    i integer;
begin
    i := 2;
    raise notice '%; %; %; %; %; %', a, a[i], c, (select c || 'abc'), row(10,'aaa',NULL,30), NULL;
end;$$ language plpgsql
;
----------
[CREATE_TYPE] -- test a custom range type with a non-default operator class
create type textrange_supp as range (
   subtype = text,
   subtype_opclass = text_pattern_ops
)
;
----------
[BLOCK] DO $$
DECLARE BEGIN
  EXECUTE 'CREATE TABLE pgss_do_table (id INT); DROP TABLE pgss_do_table';
  EXECUTE 'SELECT a FROM pgss_do_util_tab_1; SELECT a FROM pgss_do_util_tab_2';
END $$
;
----------
[CREATE_TABLE] CREATE TABLE range_parted (
	a date
) PARTITION BY RANGE (a)
;
----------
[ADD_INDEX] -- btree
CREATE INDEX jidx ON testjsonb USING btree (j)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_text_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_largeobject
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bool_union(internal, internal)
RETURNS gbtreekey2
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_CATALOG] CREATE DATABASE dropme_db2 WITH TABLESPACE dropme_ts2 STRATEGY=FILE_COPY
;
----------
[CREATE_PROG_OBJ] -- this exercises tuple conversion:
create function xtrig()
  returns trigger language plpgsql
as $$
  declare
    r record;
  begin
    for r in select * from old loop
      raise info 'a=%, b=%', r.a, r.b;
    end loop;
    return NULL;
  end;
$$
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_d_1_15 ON part_d_1_15
;
----------
[ALTER_PROG_OBJ] -- invalid options parsing function
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 6 btint42cmp(int4, int2)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_cast
;
