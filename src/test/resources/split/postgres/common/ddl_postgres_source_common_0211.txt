CREATE SUBSCRIPTION mysub1 CONNECTION 'port=16972 host=/tmp/kMt7cqXM2r dbname=postgres' PUBLICATION mypub, non_existent_pub
;
CREATE DOMAIN collate_dep_dom1 AS text COLLATE test0
;
VACUUM (FULL) pg_catalog.pg_index
;
CREATE TABLE test23a_1 PARTITION OF test23a FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
ANALYZE pg_catalog.pg_init_privs
;
-- ===================================================================
-- test error case for create publication on foreign table
-- ===================================================================
CREATE PUBLICATION testpub_ftbl FOR TABLE ft1
;
CREATE FUNCTION isnlt(ean13, upc)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE corrupt2 AS SELECT b FROM generate_series(1,2) AS b
;
DROP TABLE FKTABLE_FAIL1
;
CREATE OR REPLACE FUNCTION test_slru_page_sync(bigint) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_sync' LANGUAGE C
;
CREATE TABLE regress_notnull2 () INHERITS (regress_notnull1)
;
CREATE TABLE split_cov.part_table_2026 PARTITION OF split_cov.part_table FOR VALUES FROM ('2026-01-01') TO ('2027-01-01')
;
create table atacc3 (test3 int) inherits (atacc1, atacc2)
;
CREATE PUBLICATION "regress_pub" FOR ALL TABLES WITH (publish = 'insert, update, delete, truncate')
;
DROP PUBLICATION pub8
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_foreign_data_wrapper
;
ALTER OPERATOR <= (isbn, isbn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE RULE "shoelace_ins" AS
    ON INSERT TO "public"."shoelace" DO INSTEAD  INSERT INTO "public"."shoelace_data" ("sl_name", "sl_avail", "sl_color", "sl_len", "sl_unit")
  VALUES ("new"."sl_name", "new"."sl_avail", "new"."sl_color", "new"."sl_len", "new"."sl_unit")
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=24666 host=/tmp/8XhuxZ1AUD dbname=postgres' PUBLICATION pub1
;
create type rewritetype as (a int)
;
-- Check DDL via SPI, immediately followed by SPI plan re-use
-- (bug in original coding)

create function cachebug() returns void as $$
declare r int;
begin
  drop table if exists temptable cascade;
  create temp table temptable as select * from generate_series(1,3) as f1;
  create temp view vv as select * from temptable;
  for r in select * from vv loop
    raise notice '%', r;
  end loop;
end$$ language plpgsql
;
ANALYZE public.pt_part1
;
CREATE TABLE test_like_id_1 (a bigint GENERATED ALWAYS AS IDENTITY, b text)
;
ALTER FUNCTION gbt_uuid_same(gbtreekey32, gbtreekey32, internal) PARALLEL SAFE
;
CREATE FUNCTION testprs_lextype(internal)
RETURNS internal
AS '$libdir/test_parser'
LANGUAGE C STRICT
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
create table numrange_test2(nr numrange)
;
ALTER TABLE ONLY "public"."notnull_part1_upg" ATTACH PARTITION "public"."notnull_part1_1_upg" FOR VALUES IN (1, 2)
;
ANALYZE pg_temp_13.pg_temp_26200
;
ALTER OPERATOR FAMILY gist_time_ops USING gist ADD
	FUNCTION	11  (time, time) gbt_time_sortsupport (internal)
;
ALTER TABLE ONLY "public"."atnnpart1"
    ADD CONSTRAINT "atnnpart1_pkey" PRIMARY KEY ("id")
;
ALTER EXTENSION pageinspect UPDATE TO '1.12'
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_database
;
CREATE TABLE foo AS SELECT generate_series(1, 100)::int a
;
-- money check

CREATE TABLE moneytmp (a money)
;
CREATE TYPE "public"."myshell"
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
DROP TABLE tab_settings_flags
;
create function table_succeed(r anyrange)
  returns table(l anyelement, u anyelement)
  as $$ select lower($1), upper($1) $$
  language sql
;
-- Typed table

CREATE TABLE employees OF employee_type (
    PRIMARY KEY (name),
    salary WITH OPTIONS DEFAULT 1000
)
;
DROP TABLE cmdata2
;
-- check that btree index works correctly
CREATE INDEX inet_idx1 ON inet_tbl(i)
;
ALTER TABLE inh_nn_lvl1 ADD PRIMARY KEY (a)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=27547 host=/tmp/wtFfnGwmnH dbname=postgres' PUBLICATION pub1
;
CREATE TABLE test_constraints_inh () INHERITS (test_constraints)
;
VACUUM ANALYZE road
;
CREATE FUNCTION pg_stat_statements_info(
    OUT dealloc bigint,
    OUT stats_reset timestamp with time zone
)
RETURNS record
AS '$libdir/pg_stat_statements'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
ALTER TRIGGER accounts_check_update ON split_trigger_probe.accounts RENAME TO accounts_check_update_renamed
;
-- fail
SECURITY LABEL FOR 'dummy' ON TABLE seclabel_tbl1 IS 'classified'
;
CREATE TABLE fail_part PARTITION OF unparted FOR VALUES IN ('a')
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub1 CONNECTION 'port=16972 host=/tmp/kMt7cqXM2r dbname=postgres' PUBLICATION mypub, non_existent_pub
;
----------
[CREATE_TYPE] CREATE DOMAIN collate_dep_dom1 AS text COLLATE test0
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_index
;
----------
[CREATE_TABLE] CREATE TABLE test23a_1 PARTITION OF test23a FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_init_privs
;
----------
[CREATE_PUB_SUB] -- ===================================================================
-- test error case for create publication on foreign table
-- ===================================================================
CREATE PUBLICATION testpub_ftbl FOR TABLE ft1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ean13, upc)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE corrupt2 AS SELECT b FROM generate_series(1,2) AS b
;
----------
[DROP_TABLE] DROP TABLE FKTABLE_FAIL1
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_sync(bigint) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_sync' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE regress_notnull2 () INHERITS (regress_notnull1)
;
----------
[CREATE_TABLE] CREATE TABLE split_cov.part_table_2026 PARTITION OF split_cov.part_table FOR VALUES FROM ('2026-01-01') TO ('2027-01-01')
;
----------
[CREATE_TABLE] create table atacc3 (test3 int) inherits (atacc1, atacc2)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "regress_pub" FOR ALL TABLES WITH (publish = 'insert, update, delete, truncate')
;
----------
[DROP_PUB_SUB] DROP PUBLICATION pub8
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_foreign_data_wrapper
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (isbn, isbn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_POLICY] CREATE RULE "shoelace_ins" AS
    ON INSERT TO "public"."shoelace" DO INSTEAD  INSERT INTO "public"."shoelace_data" ("sl_name", "sl_avail", "sl_color", "sl_len", "sl_unit")
  VALUES ("new"."sl_name", "new"."sl_avail", "new"."sl_color", "new"."sl_len", "new"."sl_unit")
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=24666 host=/tmp/8XhuxZ1AUD dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TYPE] create type rewritetype as (a int)
;
----------
[CREATE_PROG_OBJ] -- Check DDL via SPI, immediately followed by SPI plan re-use
-- (bug in original coding)

create function cachebug() returns void as $$
declare r int;
begin
  drop table if exists temptable cascade;
  create temp table temptable as select * from generate_series(1,3) as f1;
  create temp view vv as select * from temptable;
  for r in select * from vv loop
    raise notice '%', r;
  end loop;
end$$ language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE public.pt_part1
;
----------
[CREATE_TABLE] CREATE TABLE test_like_id_1 (a bigint GENERATED ALWAYS AS IDENTITY, b text)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_uuid_same(gbtreekey32, gbtreekey32, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testprs_lextype(internal)
RETURNS internal
AS '$libdir/test_parser'
LANGUAGE C STRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
----------
[CREATE_TABLE] create table numrange_test2(nr numrange)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."notnull_part1_upg" ATTACH PARTITION "public"."notnull_part1_1_upg" FOR VALUES IN (1, 2)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26200
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_time_ops USING gist ADD
	FUNCTION	11  (time, time) gbt_time_sortsupport (internal)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."atnnpart1"
    ADD CONSTRAINT "atnnpart1_pkey" PRIMARY KEY ("id")
;
----------
[ALTER_LIBRARY] ALTER EXTENSION pageinspect UPDATE TO '1.12'
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_database
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE foo AS SELECT generate_series(1, 100)::int a
;
----------
[CREATE_TABLE] -- money check

CREATE TABLE moneytmp (a money)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."myshell"
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdepend
;
----------
[DROP_TABLE] DROP TABLE tab_settings_flags
;
----------
[CREATE_PROG_OBJ] create function table_succeed(r anyrange)
  returns table(l anyelement, u anyelement)
  as $$ select lower($1), upper($1) $$
  language sql
;
----------
[CREATE_TABLE] -- Typed table

CREATE TABLE employees OF employee_type (
    PRIMARY KEY (name),
    salary WITH OPTIONS DEFAULT 1000
)
;
----------
[DROP_TABLE] DROP TABLE cmdata2
;
----------
[ADD_INDEX] -- check that btree index works correctly
CREATE INDEX inet_idx1 ON inet_tbl(i)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE inh_nn_lvl1 ADD PRIMARY KEY (a)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=27547 host=/tmp/wtFfnGwmnH dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TABLE] CREATE TABLE test_constraints_inh () INHERITS (test_constraints)
;
----------
[ADMIN_TABLE] VACUUM ANALYZE road
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_stat_statements_info(
    OUT dealloc bigint,
    OUT stats_reset timestamp with time zone
)
RETURNS record
AS '$libdir/pg_stat_statements'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[RENAME_TRIGGER] ALTER TRIGGER accounts_check_update ON split_trigger_probe.accounts RENAME TO accounts_check_update_renamed
;
----------
[UNKNOWN] -- fail
SECURITY LABEL FOR 'dummy' ON TABLE seclabel_tbl1 IS 'classified'
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF unparted FOR VALUES IN ('a')
;
