CREATE TABLE local_tbl (c1 int NOT NULL, c2 int NOT NULL, c3 text, CONSTRAINT local_tbl_pkey PRIMARY KEY (c1))
;
create table idxpart2 partition of idxpart for values from (10) to (100)
	partition by range (b)
;
CREATE FUNCTION batch_end()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
DROP TABLE tid_tab CASCADE
;
CREATE FUNCTION handle_get_twice()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
create table parted_collate_must_match1 partition of parted_collate_must_match
  (a collate "POSIX") for values from ('a') to ('m')
;
CREATE TABLE mcv_lists (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b VARCHAR,
    filler3 DATE,
    c INT,
    d TEXT,
    ia INT[]
)
WITH (autovacuum_enabled = off)
;
CREATE FUNCTION gbt_ts_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- non-top-level command
create procedure p1()
language plpgsql
as $$
begin
  create table event_trigger_fire7 (a int);
end $$
;
--
-- pgp_pub_decrypt(data, key, psw, arg)
--
CREATE FUNCTION pgp_pub_decrypt(bytea, bytea, text, text)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_33068_index RENAME TO reind_con_toast_idx
;
CREATE SCHEMA fkpart8
  CREATE TABLE tbl1(f1 int PRIMARY KEY)
  CREATE TABLE tbl2(f1 int REFERENCES tbl1 DEFERRABLE INITIALLY DEFERRED) PARTITION BY RANGE(f1)
  CREATE TABLE tbl2_p1 PARTITION OF tbl2 FOR VALUES FROM (minvalue) TO (maxvalue)
;
/*
 * 5.45
 * ROLE_USAGE_GRANTS view
 */

CREATE VIEW role_usage_grants AS
    SELECT grantor,
           grantee,
           object_catalog,
           object_schema,
           object_name,
           object_type,
           privilege_type,
           is_grantable
    FROM usage_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
ALTER TABLE replication_metadata ADD COLUMN rewritemeornot int
;
CREATE DOMAIN testdomain_p AS text COLLATE "POSIX"
;
DROP TABLE test_squash_numeric
;
TRUNCATE TABLE test1
;
-- test parallel build with immutable function.
CREATE TABLE brintest_expr (n int)
;
DROP EXTENSION pageinspect
;
DROP TABLE smalltbl
;
-- Test the same constraint name for different columns in different parents
create table inh_parent1(a int constraint nn not null)
;
DROP SCHEMA split_ts_probe
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
CREATE TABLE gtest_rule (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
ALTER TABLE rewrite_test
      ADD COLUMN empty1 text,
      ADD COLUMN notempty1_rewrite serial
;
--------------
CREATE FUNCTION intset(int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table ab_a1_b2 partition of ab_a1 for values in (2)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_proc
;
-- check that default_tablespace doesn't affect ALTER TABLE index rebuilds
CREATE TABLE testschema.test_default_tab(id bigint) TABLESPACE regress_tblspace
;
CREATE DOMAIN js_int_array_2d  AS int[][] CHECK(array_length(VALUE, 2) = 3)
;
CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified', c serial)
;
ALTER OPERATOR FAMILY alt_opf19 USING btree ADD FUNCTION 5 (int4, int2) btint42cmp(int4, int2)
;
ANALYZE pg_temp_26.pg_temp_28109
;
-- can be pushed down
DROP TRIGGER trig_stmt_after ON rem1
;
create or replace function compos() returns setof compostype as $$
begin
  for i in 1..3
  loop
    return next (1, 'hello'::varchar);
  end loop;
  return next null::compostype;
  return next (2, 'goodbye')::compostype;
end;
$$ language plpgsql
;
drop table agg_hash_4
;
DROP TABLE IF EXISTS foo
;
VACUUM (ANALYZE) pg_catalog.pg_description
;
-- can't have a parent
CREATE TABLE tt7 (x int, q text, y numeric(8,2))
;
-- try additional syntax
ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey NOT DEFERRABLE
;
-- fail, no NOT NULL constraint
alter table idxpart0 alter column a set not null
;
CREATE TABLE bttest_unique3 (i int4)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
-- MERGE with incomplete set of INSTEAD OF triggers
DROP TRIGGER rw_view1_del_trig ON rw_view1
;
create or replace function unreserved_test() returns int as $$
declare
  comment int := 21;
begin
  comment := comment * 2;
  comment on function unreserved_test() is 'this is a test';
  return comment;
end
$$ language plpgsql
;
ALTER TABLE pg_toast.pg_toast_30467 RENAME TO reind_con_toast
;
-- test primary key constraint adding

create table atacc1 ( id serial, test int)
;
CREATE TABLE non_parted (id int)
;
ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        9       %>> (text, text),
        OPERATOR        10       <->>> (text, text) FOR ORDER BY pg_catalog.float_ops
;
drop table events_child, events, other_events
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE local_tbl (c1 int NOT NULL, c2 int NOT NULL, c3 text, CONSTRAINT local_tbl_pkey PRIMARY KEY (c1))
;
----------
[CREATE_TABLE] create table idxpart2 partition of idxpart for values from (10) to (100)
	partition by range (b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION batch_end()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[DROP_TABLE] DROP TABLE tid_tab CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_get_twice()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_TABLE] create table parted_collate_must_match1 partition of parted_collate_must_match
  (a collate "POSIX") for values from ('a') to ('m')
;
----------
[CREATE_TABLE] CREATE TABLE mcv_lists (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b VARCHAR,
    filler3 DATE,
    c INT,
    d TEXT,
    ia INT[]
)
WITH (autovacuum_enabled = off)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_ts_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] -- non-top-level command
create procedure p1()
language plpgsql
as $$
begin
  create table event_trigger_fire7 (a int);
end $$
;
----------
[CREATE_PROG_OBJ] --
-- pgp_pub_decrypt(data, key, psw, arg)
--
CREATE FUNCTION pgp_pub_decrypt(bytea, bytea, text, text)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33068_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart8
  CREATE TABLE tbl1(f1 int PRIMARY KEY)
  CREATE TABLE tbl2(f1 int REFERENCES tbl1 DEFERRABLE INITIALLY DEFERRED) PARTITION BY RANGE(f1)
  CREATE TABLE tbl2_p1 PARTITION OF tbl2 FOR VALUES FROM (minvalue) TO (maxvalue)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 5.45
 * ROLE_USAGE_GRANTS view
 */

CREATE VIEW role_usage_grants AS
    SELECT grantor,
           grantee,
           object_catalog,
           object_schema,
           object_name,
           object_type,
           privilege_type,
           is_grantable
    FROM usage_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE replication_metadata ADD COLUMN rewritemeornot int
;
----------
[CREATE_TYPE] CREATE DOMAIN testdomain_p AS text COLLATE "POSIX"
;
----------
[DROP_TABLE] DROP TABLE test_squash_numeric
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE test1
;
----------
[CREATE_TABLE] -- test parallel build with immutable function.
CREATE TABLE brintest_expr (n int)
;
----------
[DROP_LIBRARY] DROP EXTENSION pageinspect
;
----------
[DROP_TABLE] DROP TABLE smalltbl
;
----------
[CREATE_TABLE] -- Test the same constraint name for different columns in different parents
create table inh_parent1(a int constraint nn not null)
;
----------
[DROP_SCHEMA] DROP SCHEMA split_ts_probe
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_rule (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE rewrite_test
      ADD COLUMN empty1 text,
      ADD COLUMN notempty1_rewrite serial
;
----------
[CREATE_PROG_OBJ] --------------
CREATE FUNCTION intset(int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table ab_a1_b2 partition of ab_a1 for values in (2)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_proc
;
----------
[CREATE_TABLE] -- check that default_tablespace doesn't affect ALTER TABLE index rebuilds
CREATE TABLE testschema.test_default_tab(id bigint) TABLESPACE regress_tblspace
;
----------
[CREATE_TYPE] CREATE DOMAIN js_int_array_2d  AS int[][] CHECK(array_length(VALUE, 2) = 3)
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified', c serial)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf19 USING btree ADD FUNCTION 5 (int4, int2) btint42cmp(int4, int2)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_26.pg_temp_28109
;
----------
[DROP_TRIGGER] -- can be pushed down
DROP TRIGGER trig_stmt_after ON rem1
;
----------
[CREATE_PROG_OBJ] create or replace function compos() returns setof compostype as $$
begin
  for i in 1..3
  loop
    return next (1, 'hello'::varchar);
  end loop;
  return next null::compostype;
  return next (2, 'goodbye')::compostype;
end;
$$ language plpgsql
;
----------
[DROP_TABLE] drop table agg_hash_4
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS foo
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_description
;
----------
[CREATE_TABLE] -- can't have a parent
CREATE TABLE tt7 (x int, q text, y numeric(8,2))
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- try additional syntax
ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey NOT DEFERRABLE
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fail, no NOT NULL constraint
alter table idxpart0 alter column a set not null
;
----------
[CREATE_TABLE] CREATE TABLE bttest_unique3 (i int4)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
----------
[DROP_TRIGGER] -- MERGE with incomplete set of INSTEAD OF triggers
DROP TRIGGER rw_view1_del_trig ON rw_view1
;
----------
[CREATE_PROG_OBJ] create or replace function unreserved_test() returns int as $$
declare
  comment int := 21;
begin
  comment := comment * 2;
  comment on function unreserved_test() is 'this is a test';
  return comment;
end
$$ language plpgsql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30467 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- test primary key constraint adding

create table atacc1 ( id serial, test int)
;
----------
[CREATE_TABLE] CREATE TABLE non_parted (id int)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        9       %>> (text, text),
        OPERATOR        10       <->>> (text, text) FOR ORDER BY pg_catalog.float_ops
;
----------
[DROP_TABLE] drop table events_child, events, other_events
;
