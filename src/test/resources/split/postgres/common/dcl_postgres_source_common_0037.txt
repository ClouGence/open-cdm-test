EXECUTE dumpFunc('36270')
;
CREATE SERVER dummy_server FOREIGN DATA WRAPPER dummy
;
-- fail, but see columns with SELECT rights, or being modified

SET SESSION AUTHORIZATION regress_priv_user1
;
-- Other compression algorithms may cause the compressed data to be stored
-- inline.  pglz guarantees that the data is externalized, so stick to it.
SET default_toast_compression = 'pglz'
;
REVOKE EXECUTE ON FUNCTION pgstattuple(regclass) FROM PUBLIC
;
COPY fkpart4.droppk21 (a) TO stdout
;
-- test whole-tree commit on an aborted subtransaction
BEGIN
;
EXECUTE dumpFunc('17229')
;
EXECUTE dumpCompositeType('20490')
;
EXECUTE dumpFunc('37403')
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('36270')
;
----------
[SYSTEM_SETTING_WRITE] CREATE SERVER dummy_server FOREIGN DATA WRAPPER dummy
;
----------
[SWITCH_USER] -- fail, but see columns with SELECT rights, or being modified

SET SESSION AUTHORIZATION regress_priv_user1
;
----------
[SESSION_SETTING_WRITE] -- Other compression algorithms may cause the compressed data to be stored
-- inline.  pglz guarantees that the data is externalized, so stick to it.
SET default_toast_compression = 'pglz'
;
----------
[REVOKE] REVOKE EXECUTE ON FUNCTION pgstattuple(regclass) FROM PUBLIC
;
----------
[DATA_EXPORT] COPY fkpart4.droppk21 (a) TO stdout
;
----------
[TRANSACTION] -- test whole-tree commit on an aborted subtransaction
BEGIN
;
----------
[UNSAFE] EXECUTE dumpFunc('17229')
;
----------
[UNSAFE] EXECUTE dumpCompositeType('20490')
;
----------
[UNSAFE] EXECUTE dumpFunc('37403')
;
