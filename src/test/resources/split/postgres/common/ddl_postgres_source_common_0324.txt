-- min and max will be adjusted

---
--- test creation of SERIAL column
---

CREATE TABLE serialTest1 (f1 text, f2 serial)
;
----
-- Make sure we detect ineligible indexes
----

-- fail, not unique
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_keyab
;
CREATE SEQUENCE s4.seq1
;
CREATE TRIGGER "alter_table_under_transition_tables_upd_trigger" AFTER UPDATE ON "public"."alter_table_under_transition_tables" REFERENCING OLD TABLE AS "d" NEW TABLE AS "i" FOR EACH STATEMENT EXECUTE FUNCTION "public"."alter_table_under_transition_tables_upd_func"()
;
ALTER TABLE pg_toast.pg_toast_27891 RENAME TO reind_con_toast
;
-- PARALLEL option
CREATE TABLE pvactst (i INT, a INT[], p POINT) with (autovacuum_enabled = off)
;
CREATE DATABASE foobar3 TEMPLATE foobar2 LOCALE 'C'
;
CREATE TABLE tab_rowfilter_viaroot_part (a int) PARTITION BY RANGE (a)
;
create unlogged table pgbench_accounts_2
  partition of pgbench_accounts
  for values with (modulus 2, remainder 1) with (fillfactor=98)
;
create index parent_index on parent_tab (id)
;
-- test qualified variable names

create function pl_qual_names (param1 int) returns void as $$
<<outerblock>>
declare
  param1 int := 1;
begin
  <<innerblock>>
  declare
    param1 int := 2;
  begin
    raise notice 'param1 = %', param1;
    raise notice 'pl_qual_names.param1 = %', pl_qual_names.param1;
    raise notice 'outerblock.param1 = %', outerblock.param1;
    raise notice 'innerblock.param1 = %', innerblock.param1;
  end;
end;
$$ language plpgsql
;
DO LANGUAGE plpgsql $$
BEGIN
  -- this is a SELECT
  PERFORM 'hello world'::TEXT;
END;
$$
;
VACUUM (BUFFER_USAGE_LIMIT 0) vac_option_tab
;
DROP PUBLICATION testpub3, testpub4
;
CREATE INDEX CONCURRENTLY concur_index1 ON concur_heap(f2,f1)
;
-- second partition has replica identity "b"
CREATE TABLE testpub_tbl8_1 (a int, b text, c text)
;
DROP TABLE clstr_2
;
create or replace function conflict_test() returns setof int8_tbl as $$
#variable_conflict use_variable
declare r record;
  q1 bigint := 42;
begin
  for r in select q1,q2 from int8_tbl loop
    return next r;
  end loop;
end;
$$ language plpgsql
;
-- early shutdown of set-returning functions

CREATE FUNCTION functest_srf0() RETURNS SETOF int
LANGUAGE SQL
AS $$ SELECT i FROM generate_series(1, 100) i $$
;
ALTER INDEX pg_toast.pg_toast_30443_index RENAME TO reind_con_toast_idx
;
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'secret'
;
ANALYZE public.gtest1_1
;
alter table parent enable always trigger tg_stmt
;
CREATE OR REPLACE FUNCTION y_trigger() RETURNS trigger AS $$
begin
  raise notice 'y_trigger';
  return null;
end;
$$ LANGUAGE plpgsql
;
create table at_partitioned (a int, b text) partition by range (a)
;
CREATE DATABASE foobar2
;
VACUUM tidrangescan
;
CREATE TABLE ATACC2 () INHERITS (ATACC1)
;
ALTER FUNCTION int8_dist(bigint, bigint) PARALLEL SAFE
;
create table pk3 partition of pk for values from (2000) to (3000)
;
alter table bc drop constraint ac_aa_check
;
CREATE TABLE tru_rtable_parent (id int)
;
ALTER TABLE ONLY "public"."gtest1_y" INHERIT "public"."gtesty"
;
CREATE TYPE "public"."int8alias2" (
    INTERNALLENGTH = 8,
    INPUT = "public"."int8alias2in",
    OUTPUT = "public"."int8alias2out",
    ALIGNMENT = double,
    STORAGE = plain,
    PASSEDBYVALUE
)
;
CALL transaction_test1(9, 'error')
;
CREATE SEQUENCE DEFAULT_SEQ
;
CREATE FUNCTION hstore(text[])
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_array'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE VIEW "public"."test_tablesample_v1" AS
 SELECT "id"
   FROM "public"."test_tablesample" TABLESAMPLE "system" ((10 * 2)) REPEATABLE (2)
;
VACUUM (SKIP_LOCKED) information_schema.sql_implementation_info
;
CREATE RULE european_city_delete_rule AS ON DELETE TO european_city_view
DO INSTEAD DELETE FROM city_view WHERE city_id = OLD.city_id RETURNING *
;
CREATE PUBLICATION testpub_both_filters
;
create table pfk1 (a int not null)
;
alter database abc set datestyle = 'iso, mdy'
;
-- Test case for bug #14010: indexed row comparisons fail with nulls
create temp table test_table (a text, b text)
;
drop type mytype
;
CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE VIEW schema_not_existing.view AS SELECT 1
;
-- check partitioned tables support tablesample
create table parted_sample (a int) partition by list (a)
;
CREATE TEXT SEARCH CONFIGURATION thesaurus_tst (
						COPY=synonym_tst
)
;
alter table f_star reset (parallel_workers)
;
-- Should fail. Ensure correct ordering methods in ALTER OPERATOR FAMILY ... ADD OPERATOR .. FOR ORDER BY
CREATE OPERATOR FAMILY alt_opf10 USING btree
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- min and max will be adjusted

---
--- test creation of SERIAL column
---

CREATE TABLE serialTest1 (f1 text, f2 serial)
;
----------
[ALTER_TABLE] ----
-- Make sure we detect ineligible indexes
----

-- fail, not unique
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_keyab
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE s4.seq1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "alter_table_under_transition_tables_upd_trigger" AFTER UPDATE ON "public"."alter_table_under_transition_tables" REFERENCING OLD TABLE AS "d" NEW TABLE AS "i" FOR EACH STATEMENT EXECUTE FUNCTION "public"."alter_table_under_transition_tables_upd_func"()
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27891 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- PARALLEL option
CREATE TABLE pvactst (i INT, a INT[], p POINT) with (autovacuum_enabled = off)
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar3 TEMPLATE foobar2 LOCALE 'C'
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_viaroot_part (a int) PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE] create unlogged table pgbench_accounts_2
  partition of pgbench_accounts
  for values with (modulus 2, remainder 1) with (fillfactor=98)
;
----------
[ADD_INDEX] create index parent_index on parent_tab (id)
;
----------
[CREATE_PROG_OBJ] -- test qualified variable names

create function pl_qual_names (param1 int) returns void as $$
<<outerblock>>
declare
  param1 int := 1;
begin
  <<innerblock>>
  declare
    param1 int := 2;
  begin
    raise notice 'param1 = %', param1;
    raise notice 'pl_qual_names.param1 = %', pl_qual_names.param1;
    raise notice 'outerblock.param1 = %', outerblock.param1;
    raise notice 'innerblock.param1 = %', innerblock.param1;
  end;
end;
$$ language plpgsql
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
  -- this is a SELECT
  PERFORM 'hello world'::TEXT;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM (BUFFER_USAGE_LIMIT 0) vac_option_tab
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub3, testpub4
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY concur_index1 ON concur_heap(f2,f1)
;
----------
[CREATE_TABLE] -- second partition has replica identity "b"
CREATE TABLE testpub_tbl8_1 (a int, b text, c text)
;
----------
[DROP_TABLE] DROP TABLE clstr_2
;
----------
[CREATE_PROG_OBJ] create or replace function conflict_test() returns setof int8_tbl as $$
#variable_conflict use_variable
declare r record;
  q1 bigint := 42;
begin
  for r in select q1,q2 from int8_tbl loop
    return next r;
  end loop;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] -- early shutdown of set-returning functions

CREATE FUNCTION functest_srf0() RETURNS SETOF int
LANGUAGE SQL
AS $$ SELECT i FROM generate_series(1, 100) i $$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30443_index RENAME TO reind_con_toast_idx
;
----------
[UNKNOWN] SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'secret'
;
----------
[ADMIN_TABLE] ANALYZE public.gtest1_1
;
----------
[ALTER_TABLE] alter table parent enable always trigger tg_stmt
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION y_trigger() RETURNS trigger AS $$
begin
  raise notice 'y_trigger';
  return null;
end;
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] create table at_partitioned (a int, b text) partition by range (a)
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar2
;
----------
[ADMIN_TABLE] VACUUM tidrangescan
;
----------
[CREATE_TABLE] CREATE TABLE ATACC2 () INHERITS (ATACC1)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION int8_dist(bigint, bigint) PARALLEL SAFE
;
----------
[CREATE_TABLE] create table pk3 partition of pk for values from (2000) to (3000)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table bc drop constraint ac_aa_check
;
----------
[CREATE_TABLE] CREATE TABLE tru_rtable_parent (id int)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."gtest1_y" INHERIT "public"."gtesty"
;
----------
[CREATE_TYPE] CREATE TYPE "public"."int8alias2" (
    INTERNALLENGTH = 8,
    INPUT = "public"."int8alias2in",
    OUTPUT = "public"."int8alias2out",
    ALIGNMENT = double,
    STORAGE = plain,
    PASSEDBYVALUE
)
;
----------
[CALL_PROG_OBJ] CALL transaction_test1(9, 'error')
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE DEFAULT_SEQ
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore(text[])
RETURNS hstore
AS '$libdir/hstore', 'hstore_from_array'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."test_tablesample_v1" AS
 SELECT "id"
   FROM "public"."test_tablesample" TABLESAMPLE "system" ((10 * 2)) REPEATABLE (2)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) information_schema.sql_implementation_info
;
----------
[CREATE_POLICY] CREATE RULE european_city_delete_rule AS ON DELETE TO european_city_view
DO INSTEAD DELETE FROM city_view WHERE city_id = OLD.city_id RETURNING *
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_both_filters
;
----------
[CREATE_TABLE] create table pfk1 (a int not null)
;
----------
[ALTER_CATALOG] alter database abc set datestyle = 'iso, mdy'
;
----------
[CREATE_TABLE] -- Test case for bug #14010: indexed row comparisons fail with nulls
create temp table test_table (a text, b text)
;
----------
[DROP_TYPE] drop type mytype
;
----------
[CREATE_SCHEMA(SELECT)] CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE VIEW schema_not_existing.view AS SELECT 1
;
----------
[CREATE_TABLE] -- check partitioned tables support tablesample
create table parted_sample (a int) partition by list (a)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION thesaurus_tst (
						COPY=synonym_tst
)
;
----------
[ALTER_TABLE] alter table f_star reset (parallel_workers)
;
----------
[CREATE_PROG_OBJ] -- Should fail. Ensure correct ordering methods in ALTER OPERATOR FAMILY ... ADD OPERATOR .. FOR ORDER BY
CREATE OPERATOR FAMILY alt_opf10 USING btree
;
