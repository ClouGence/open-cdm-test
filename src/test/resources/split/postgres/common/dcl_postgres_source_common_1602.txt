ALTER SYSTEM SET summarize_wal = off
;
EXECUTE dumpFunc('18412')
;
EXECUTE dumpTableAttach('16424')
;
GRANT USAGE ON TYPE regress_pg_dump_schema.test_type TO regress_dump_test_role
;
COPY public.beta_neg_p2 (a, b, c) TO stdout
;
COPY x from stdin (format BINARY, null 'x')
;
-- Verify that hash key expressions reference the correct
-- nodes. Hashjoin's hashkeys need to reference its outer plan, Hash's
-- need to reference Hash's outer plan (which is below HashJoin's
-- inner plan). It's not trivial to verify that the references are
-- correct (we don't display the hashkeys themselves), but if the
-- hashkeys contain subplan references, those will be displayed. Force
-- subplans to appear just about everywhere.
--
-- Bug report:
-- https://www.postgresql.org/message-id/CAPpHfdvGVegF_TKKRiBrSmatJL2dR9uwFCuR%2BteQ_8tEXU8mxg%40mail.gmail.com
--
BEGIN
;
grant insert on key_desc to regress_insert_other_user
;
EXECUTE dumpCompositeType('38600')
;
DEALLOCATE pgsql_fdw_prep_167
;
------------------------------------------------------------------------------------------
[SYSTEM_SETTING_WRITE] ALTER SYSTEM SET summarize_wal = off
;
----------
[UNSAFE] EXECUTE dumpFunc('18412')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('16424')
;
----------
[GRANT] GRANT USAGE ON TYPE regress_pg_dump_schema.test_type TO regress_dump_test_role
;
----------
[DATA_EXPORT] COPY public.beta_neg_p2 (a, b, c) TO stdout
;
----------
[DATA_IMPORT] COPY x from stdin (format BINARY, null 'x')
;
----------
[TRANSACTION] -- Verify that hash key expressions reference the correct
-- nodes. Hashjoin's hashkeys need to reference its outer plan, Hash's
-- need to reference Hash's outer plan (which is below HashJoin's
-- inner plan). It's not trivial to verify that the references are
-- correct (we don't display the hashkeys themselves), but if the
-- hashkeys contain subplan references, those will be displayed. Force
-- subplans to appear just about everywhere.
--
-- Bug report:
-- https://www.postgresql.org/message-id/CAPpHfdvGVegF_TKKRiBrSmatJL2dR9uwFCuR%2BteQ_8tEXU8mxg%40mail.gmail.com
--
BEGIN
;
----------
[GRANT] grant insert on key_desc to regress_insert_other_user
;
----------
[UNSAFE] EXECUTE dumpCompositeType('38600')
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_167
;
