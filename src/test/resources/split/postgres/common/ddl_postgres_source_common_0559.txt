CREATE SCHEMA split_merge18
;
ANALYZE public.rule_and_refint_t1
;
ALTER TABLE part_rev DROP COLUMN c
;
CREATE FUNCTION gbt_tstz_distance(internal,timestamptz,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE ONLY "fkpart4"."droppk" ATTACH PARTITION "fkpart4"."droppk1" FOR VALUES FROM (0) TO (1000)
;
DROP SUBSCRIPTION IF EXISTS regress_testsub
;
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('CREATE TABLESPACE')
   execute procedure test_event_trigger()
;
drop table twoconstraints
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
CREATE FUNCTION testprs_getlexeme(internal, internal, internal)
RETURNS internal
AS '$libdir/test_parser'
LANGUAGE C STRICT
;
-- alter attributes recursively
ALTER TABLE fd_pt1 ALTER COLUMN c4 SET DEFAULT 0
;
ALTER TABLE pg_replication_origin ADD UNIQUE USING INDEX pg_replication_origin_roname_index
;
--
-- PARALLEL
--

create function sp_parallel_restricted(int) returns int as
  $$begin return $1; end$$ language plpgsql parallel restricted
;
create table psql_serial_tab (id serial)
;
CREATE TABLE past_inh_child () INHERITS (past_inh_parent)
;
create function tg_slotlink_a() returns trigger as '
declare
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
        if new.slotlink != '''' then
	    dummy := tg_slotlink_set(new.slotlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = ''UPDATE'' then
        if new.slotlink != old.slotlink then
	    if old.slotlink != '''' then
	        dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	    end if;
	    if new.slotlink != '''' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.slotlink != '''' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = ''DELETE'' then
        if old.slotlink != '''' then
	    dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	end if;
	return old;
    end if;
end;
' language plpgsql
;
CREATE INDEX "tenk2_unique1" ON "public"."tenk2" USING "btree" ("unique1")
;
CREATE TABLE prt1_adv_p1 PARTITION OF prt1_adv FOR VALUES FROM (100) TO (200)
;
CREATE OR REPLACE FUNCTION pgstattuple_approx(IN reloid regclass,
    OUT table_len BIGINT,               -- physical table length in bytes
    OUT scanned_percent FLOAT8,         -- what percentage of the table's pages was scanned
    OUT approx_tuple_count BIGINT,      -- estimated number of live tuples
    OUT approx_tuple_len BIGINT,        -- estimated total length in bytes of live tuples
    OUT approx_tuple_percent FLOAT8,    -- live tuples in % (based on estimate)
    OUT dead_tuple_count BIGINT,        -- exact number of dead tuples
    OUT dead_tuple_len BIGINT,          -- exact total length in bytes of dead tuples
    OUT dead_tuple_percent FLOAT8,      -- dead tuples in % (based on estimate)
    OUT approx_free_space BIGINT,       -- estimated free space in bytes
    OUT approx_free_percent FLOAT8)     -- free space in % (based on estimate)
AS '$libdir/pgstattuple', 'pgstattuple_approx_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
create unique index pkeys_i on pkeys (pkey1, pkey2)
;
-- fail
--
-- cross-type RI should fail
--
CREATE TYPE bogus AS ENUM('good', 'bad', 'ugly')
;
CREATE TABLE "public"."prt4_n" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY RANGE ("a")
;
alter table bug_16784 set (autovacuum_enabled = 'false')
;
CREATE FUNCTION casttesttype_out(casttesttype)
   RETURNS cstring
   AS 'textout'
   LANGUAGE internal STRICT IMMUTABLE
;
CREATE TEMP TABLE pg_temp_26.pg_temp_28149_2 (tid pg_catalog.tid)
;
CREATE TABLE table_three_one(a varchar)
;
VACUUM (SKIP_LOCKED) information_schema.sql_sizing_profiles
;
create function bad_sql2() returns int as $$
declare r record;
begin
    for r in select I fought the law, the law won LOOP
        raise notice 'in loop';
    end loop;
    return 5;
end;$$ language plpgsql
;
-- child must have parent's INHERIT constraints
ALTER FOREIGN TABLE ft2 INHERIT fd_pt1
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_sequence
;
create domain d_fail as int4 constraint cc check (values > 1) no inherit
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '13894'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
CREATE SEQUENCE "public"."sequence_test12"
    AS integer
    START WITH -1
    INCREMENT BY -1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
CREATE INDEX test_index1 on testschema.test_default_tab_p (val)
;
alter table c1 drop column f1
;
ALTER INDEX pg_toast.pg_toast_28159_index RENAME TO reind_con_toast_idx
;
-- check comment is still there

drop type comptype cascade
;
DROP TABLE inhta, inhtb, inhtc, inhtd
;
CREATE TABLE test1 (a int)
;
create table middle partition of grandparent for values from (1) to (10)
partition by range (id)
;
alter table gloc1 set (autovacuum_enabled = 'false')
;
DROP TABLE cluster_tab
;
ALTER OPERATOR FAMILY gist_macaddr8_ops USING gist ADD
	FUNCTION	11  (macaddr8, macaddr8) gbt_macad8_sortsupport (internal)
;
VACUUM (ANALYZE) information_schema.sql_sizing
;
CREATE AGGREGATE myaggn14a(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_publication
;
CREATE TABLE dump_test.regress_pg_dump_table_am_child_2
			  PARTITION OF dump_test.regress_pg_dump_table_am_parent FOR VALUES IN (2) USING heap
;
ALTER TABLE pg_toast.pg_toast_30762 RENAME TO reind_con_toast
;
CREATE AGGREGATE alt_agg1 (
  sfunc1 = int4pl, basetype = int4, stype1 = int4, initcond = 0
)
;
------------------------------------------------------------------------------------------
[CREATE_SCHEMA] CREATE SCHEMA split_merge18
;
----------
[ADMIN_TABLE] ANALYZE public.rule_and_refint_t1
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE part_rev DROP COLUMN c
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_tstz_distance(internal,timestamptz,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart4"."droppk" ATTACH PARTITION "fkpart4"."droppk1" FOR VALUES FROM (0) TO (1000)
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION IF EXISTS regress_testsub
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('CREATE TABLESPACE')
   execute procedure test_event_trigger()
;
----------
[DROP_TABLE] drop table twoconstraints
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testprs_getlexeme(internal, internal, internal)
RETURNS internal
AS '$libdir/test_parser'
LANGUAGE C STRICT
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- alter attributes recursively
ALTER TABLE fd_pt1 ALTER COLUMN c4 SET DEFAULT 0
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_replication_origin ADD UNIQUE USING INDEX pg_replication_origin_roname_index
;
----------
[CREATE_PROG_OBJ] --
-- PARALLEL
--

create function sp_parallel_restricted(int) returns int as
  $$begin return $1; end$$ language plpgsql parallel restricted
;
----------
[CREATE_TABLE] create table psql_serial_tab (id serial)
;
----------
[CREATE_TABLE] CREATE TABLE past_inh_child () INHERITS (past_inh_parent)
;
----------
[CREATE_PROG_OBJ] create function tg_slotlink_a() returns trigger as '
declare
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
        if new.slotlink != '''' then
	    dummy := tg_slotlink_set(new.slotlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = ''UPDATE'' then
        if new.slotlink != old.slotlink then
	    if old.slotlink != '''' then
	        dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	    end if;
	    if new.slotlink != '''' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.slotlink != '''' then
	        dummy := tg_slotlink_set(new.slotlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = ''DELETE'' then
        if old.slotlink != '''' then
	    dummy := tg_slotlink_unset(old.slotlink, old.slotname);
	end if;
	return old;
    end if;
end;
' language plpgsql
;
----------
[ADD_INDEX] CREATE INDEX "tenk2_unique1" ON "public"."tenk2" USING "btree" ("unique1")
;
----------
[CREATE_TABLE] CREATE TABLE prt1_adv_p1 PARTITION OF prt1_adv FOR VALUES FROM (100) TO (200)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pgstattuple_approx(IN reloid regclass,
    OUT table_len BIGINT,               -- physical table length in bytes
    OUT scanned_percent FLOAT8,         -- what percentage of the table's pages was scanned
    OUT approx_tuple_count BIGINT,      -- estimated number of live tuples
    OUT approx_tuple_len BIGINT,        -- estimated total length in bytes of live tuples
    OUT approx_tuple_percent FLOAT8,    -- live tuples in % (based on estimate)
    OUT dead_tuple_count BIGINT,        -- exact number of dead tuples
    OUT dead_tuple_len BIGINT,          -- exact total length in bytes of dead tuples
    OUT dead_tuple_percent FLOAT8,      -- dead tuples in % (based on estimate)
    OUT approx_free_space BIGINT,       -- estimated free space in bytes
    OUT approx_free_percent FLOAT8)     -- free space in % (based on estimate)
AS '$libdir/pgstattuple', 'pgstattuple_approx_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] create unique index pkeys_i on pkeys (pkey1, pkey2)
;
----------
[CREATE_TYPE] -- fail
--
-- cross-type RI should fail
--
CREATE TYPE bogus AS ENUM('good', 'bad', 'ugly')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt4_n" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY RANGE ("a")
;
----------
[ALTER_TABLE] alter table bug_16784 set (autovacuum_enabled = 'false')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION casttesttype_out(casttesttype)
   RETURNS cstring
   AS 'textout'
   LANGUAGE internal STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_26.pg_temp_28149_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE table_three_one(a varchar)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) information_schema.sql_sizing_profiles
;
----------
[CREATE_PROG_OBJ] create function bad_sql2() returns int as $$
declare r record;
begin
    for r in select I fought the law, the law won LOOP
        raise notice 'in loop';
    end loop;
    return 5;
end;$$ language plpgsql
;
----------
[ALTER_TABLE] -- child must have parent's INHERIT constraints
ALTER FOREIGN TABLE ft2 INHERIT fd_pt1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_sequence
;
----------
[CREATE_TYPE] create domain d_fail as int4 constraint cc check (values > 1) no inherit
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '13894'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test12"
    AS integer
    START WITH -1
    INCREMENT BY -1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ADD_INDEX] CREATE INDEX test_index1 on testschema.test_default_tab_p (val)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table c1 drop column f1
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28159_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TYPE] -- check comment is still there

drop type comptype cascade
;
----------
[DROP_TABLE] DROP TABLE inhta, inhtb, inhtc, inhtd
;
----------
[CREATE_TABLE] CREATE TABLE test1 (a int)
;
----------
[CREATE_TABLE] create table middle partition of grandparent for values from (1) to (10)
partition by range (id)
;
----------
[ALTER_TABLE] alter table gloc1 set (autovacuum_enabled = 'false')
;
----------
[DROP_TABLE] DROP TABLE cluster_tab
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_macaddr8_ops USING gist ADD
	FUNCTION	11  (macaddr8, macaddr8) gbt_macad8_sortsupport (internal)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) information_schema.sql_sizing
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn14a(BASETYPE = int, SFUNC = tf2p, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_publication
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.regress_pg_dump_table_am_child_2
			  PARTITION OF dump_test.regress_pg_dump_table_am_parent FOR VALUES IN (2) USING heap
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30762 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE alt_agg1 (
  sfunc1 = int4pl, basetype = int4, stype1 = int4, initcond = 0
)
;
