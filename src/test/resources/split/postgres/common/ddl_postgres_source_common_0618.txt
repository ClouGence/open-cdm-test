-- in case of conflict, non-variadic version is preferred
create or replace function pleast(numeric)
returns numeric as $$
begin
  raise notice 'non-variadic function called';
  return $1;
end;
$$ language plpgsql immutable strict
;
ALTER TABLE persons RENAME COLUMN id TO num
;
CREATE TABLE hashagg_ij(x INTEGER)
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
ALTER TABLE pitest2_p1 ALTER COLUMN f3 SET GENERATED BY DEFAULT
;
-- Test old extension version entry points.

CREATE EXTENSION pg_walinspect WITH VERSION '1.0'
;
CREATE FUNCTION "public"."part_hashtext_length"("value" "text", "seed" bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE STRICT PARALLEL SAFE
    AS $$
    select length(coalesce(value, ''))::int8
    $$
;
create table boolspart_f partition of boolspart for values in (false)
;
-- should work and drop the attribute in all tables
alter table p2 drop column height
;
alter table d_star reset (parallel_workers)
;
CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~" WITH TEMPLATE = template0 OID = 16386 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
CREATE TYPE "public"."int8alias1" (
    INTERNALLENGTH = 8,
    INPUT = "public"."int8alias1in",
    OUTPUT = "public"."int8alias1out",
    ALIGNMENT = double,
    STORAGE = plain,
    PASSEDBYVALUE
)
;
CREATE FOREIGN TABLE t2 (
  c1 integer OPTIONS (column_name 'c1'),
  c2 character varying OPTIONS (column_name 'c2'),
  c3 text OPTIONS (column_name 'c3')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't2')
;
CREATE OPERATOR CLASS "public"."evttrigopclass"
    FOR TYPE integer USING "btree" FAMILY "public"."evttrigopclass" AS
    STORAGE integer
;
-- We intentionally don't vacuum point_tbl here; geometry depends on that

CREATE TABLE TEXT_TBL (f1 text)
;
CREATE POLICY p1 ON rls_test_both USING (data % 2 = 0)
;
CREATE FUNCTION is_catalog_text_unique_index_oid(oid) RETURNS bool
    AS '/src/src/test/regress/regress.so', 'is_catalog_text_unique_index_oid'
    LANGUAGE C STRICT
;
ALTER TEXT SEARCH CONFIGURATION greek ADD MAPPING
    FOR word, hword_part, hword
	WITH greek_stem
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16531 host=/tmp/wfVN1kltEQ dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_1, tap_pub_2, tap_pub_3, tap_pub_4a, tap_pub_4b, tap_pub_5a, tap_pub_5b, tap_pub_toast, tap_pub_inherits, tap_pub_viaroot_2, tap_pub_viaroot_1, tap_pub_parent_sync, tap_pub_child_sync
;
DROP TABLE pg_temp_9.pg_temp_38052_2, pg_temp_9.pg_temp_38052
;
VACUUM (PROCESS_MAIN FALSE) vac_option_tab
;
CREATE FUNCTION "public"."check_estimated_rows"("text") RETURNS TABLE("estimated" integer, "actual" integer)
    LANGUAGE "plpgsql"
    AS $_$
declare
    ln text;
    tmp text[];
    first_row bool := true;
begin
    for ln in
        execute format('explain analyze %s', $1)
    loop
        if first_row then
            first_row := false;
            tmp := regexp_match(ln, 'rows=(\d*) .* rows=(\d*)');
            return query select tmp[1]::int, tmp[2]::int;
        end if;
    end loop;
end;
$_$
;
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_keyab_key
;
CREATE VIEW rw_view2 AS SELECT * FROM rw_view1
;
CREATE TABLE "public"."default_tbl" (
    "i" integer DEFAULT 100,
    "x" "text" DEFAULT 'vadim'::"text",
    "f" double precision DEFAULT 123.456
)
;
CREATE COLLATION case_insensitive (provider = icu, locale = '@colStrength=secondary', deterministic = false)
;
--
-- ALTER TEXT SEARCH CONFIGURATION
--

CREATE TEXT SEARCH CONFIGURATION en (copy=english)
;
DROP TABLESPACE IF EXISTS split_ts_space
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) information_schema.sql_sizing
;
CREATE SCHEMA split_domain_type_next
;
CREATE VIEW "public"."pfield_v1" AS
 SELECT "pfname",
    "slotname",
    "public"."pslot_backlink_view"("slotname") AS "backside",
    "public"."pslot_slotlink_view"("slotname") AS "patch"
   FROM "public"."pslot" "pf"
;
create trigger trigger_zed
	before insert or update on trigtest
	for each row execute procedure f1_times_10()
;
ALTER INDEX pg_toast.pg_toast_28148_index RENAME TO reind_con_toast_idx
;
CREATE TABLE errtst_child_fastdef (
    partid int not null,
    shdata int not null,
    CONSTRAINT shdata_small CHECK(shdata < 3)
)
;
DROP FUNCTION test1list(hstore)
;
-- renaming views
CREATE VIEW attmp_view (unique1) AS SELECT unique1 FROM tenk1
;
CREATE FUNCTION consume_xids(nxids bigint)
RETURNS xid8 VOLATILE PARALLEL UNSAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
create table test_part1 partition of test_partitioned for values from ( 1 ) to (100)
;
VACUUM FULL vaccluster
;
DROP RULE IF EXISTS foo ON no_such_schema.bar
;
CREATE FUNCTION gbtreekey16_out(gbtreekey16)
RETURNS cstring
AS '$libdir/btree_gist', 'gbtreekey_out'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE pk ATTACH PARTITION pk4 FOR VALUES FROM (3000) TO (4000)
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
ALTER SUBSCRIPTION tap_sub_B
	SET (streaming = on)
;
VACUUM (SKIP_DATABASE_STATS, FULL) information_schema.sql_implementation_info
;
ALTER TABLE onek RENAME CONSTRAINT onek_check_constraint TO onek_check_constraint_foo
;
create trigger tg_hslot_bd before delete
    on HSlot for each row execute procedure tg_hslot_bd()
;
ANALYZE pagg_tab_para
;
alter domain dom set not null
;
CREATE INDEX "hash_i4_index" ON "public"."hash_i4_heap" USING "hash" ("random")
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- in case of conflict, non-variadic version is preferred
create or replace function pleast(numeric)
returns numeric as $$
begin
  raise notice 'non-variadic function called';
  return $1;
end;
$$ language plpgsql immutable strict
;
----------
[RENAME_COLUMN] ALTER TABLE persons RENAME COLUMN id TO num
;
----------
[CREATE_TABLE] CREATE TABLE hashagg_ij(x INTEGER)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = issn,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest2_p1 ALTER COLUMN f3 SET GENERATED BY DEFAULT
;
----------
[CREATE_LIBRARY] -- Test old extension version entry points.

CREATE EXTENSION pg_walinspect WITH VERSION '1.0'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."part_hashtext_length"("value" "text", "seed" bigint) RETURNS bigint
    LANGUAGE "sql" IMMUTABLE STRICT PARALLEL SAFE
    AS $$
    select length(coalesce(value, ''))::int8
    $$
;
----------
[CREATE_TABLE] create table boolspart_f partition of boolspart for values in (false)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- should work and drop the attribute in all tables
alter table p2 drop column height
;
----------
[ALTER_TABLE] alter table d_star reset (parallel_workers)
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~" WITH TEMPLATE = template0 OID = 16386 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[CREATE_TYPE] CREATE TYPE "public"."int8alias1" (
    INTERNALLENGTH = 8,
    INPUT = "public"."int8alias1in",
    OUTPUT = "public"."int8alias1out",
    ALIGNMENT = double,
    STORAGE = plain,
    PASSEDBYVALUE
)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t2 (
  c1 integer OPTIONS (column_name 'c1'),
  c2 character varying OPTIONS (column_name 'c2'),
  c3 text OPTIONS (column_name 'c3')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't2')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS "public"."evttrigopclass"
    FOR TYPE integer USING "btree" FAMILY "public"."evttrigopclass" AS
    STORAGE integer
;
----------
[CREATE_TABLE] -- We intentionally don't vacuum point_tbl here; geometry depends on that

CREATE TABLE TEXT_TBL (f1 text)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON rls_test_both USING (data % 2 = 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION is_catalog_text_unique_index_oid(oid) RETURNS bool
    AS '/src/src/test/regress/regress.so', 'is_catalog_text_unique_index_oid'
    LANGUAGE C STRICT
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION greek ADD MAPPING
    FOR word, hword_part, hword
	WITH greek_stem
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16531 host=/tmp/wfVN1kltEQ dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_1, tap_pub_2, tap_pub_3, tap_pub_4a, tap_pub_4b, tap_pub_5a, tap_pub_5b, tap_pub_toast, tap_pub_inherits, tap_pub_viaroot_2, tap_pub_viaroot_1, tap_pub_parent_sync, tap_pub_child_sync
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_38052_2, pg_temp_9.pg_temp_38052
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE) vac_option_tab
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."check_estimated_rows"("text") RETURNS TABLE("estimated" integer, "actual" integer)
    LANGUAGE "plpgsql"
    AS $_$
declare
    ln text;
    tmp text[];
    first_row bool := true;
begin
    for ln in
        execute format('explain analyze %s', $1)
    loop
        if first_row then
            first_row := false;
            tmp := regexp_match(ln, 'rows=(\d*) .* rows=(\d*)');
            return query select tmp[1]::int, tmp[2]::int;
        end if;
    end loop;
end;
$_$
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_keyab_key
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS SELECT * FROM rw_view1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."default_tbl" (
    "i" integer DEFAULT 100,
    "x" "text" DEFAULT 'vadim'::"text",
    "f" double precision DEFAULT 123.456
)
;
----------
[CREATE_POLICY] CREATE COLLATION case_insensitive (provider = icu, locale = '@colStrength=secondary', deterministic = false)
;
----------
[CREATE_POLICY] --
-- ALTER TEXT SEARCH CONFIGURATION
--

CREATE TEXT SEARCH CONFIGURATION en (copy=english)
;
----------
[DROP_TABLESPACE] DROP TABLESPACE IF EXISTS split_ts_space
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) information_schema.sql_sizing
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_domain_type_next
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."pfield_v1" AS
 SELECT "pfname",
    "slotname",
    "public"."pslot_backlink_view"("slotname") AS "backside",
    "public"."pslot_slotlink_view"("slotname") AS "patch"
   FROM "public"."pslot" "pf"
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trigger_zed
	before insert or update on trigtest
	for each row execute procedure f1_times_10()
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28148_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE errtst_child_fastdef (
    partid int not null,
    shdata int not null,
    CONSTRAINT shdata_small CHECK(shdata < 3)
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test1list(hstore)
;
----------
[CREATE_VIEW(SELECT)] -- renaming views
CREATE VIEW attmp_view (unique1) AS SELECT unique1 FROM tenk1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION consume_xids(nxids bigint)
RETURNS xid8 VOLATILE PARALLEL UNSAFE STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] create table test_part1 partition of test_partitioned for values from ( 1 ) to (100)
;
----------
[ADMIN_TABLE] VACUUM FULL vaccluster
;
----------
[DROP_POLICY] DROP RULE IF EXISTS foo ON no_such_schema.bar
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey16_out(gbtreekey16)
RETURNS cstring
AS '$libdir/btree_gist', 'gbtreekey_out'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pk ATTACH PARTITION pk4 FOR VALUES FROM (3000) TO (4000)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub_B
	SET (streaming = on)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) information_schema.sql_implementation_info
;
----------
[RENAME_CONSTRAINT] ALTER TABLE onek RENAME CONSTRAINT onek_check_constraint TO onek_check_constraint_foo
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_hslot_bd before delete
    on HSlot for each row execute procedure tg_hslot_bd()
;
----------
[ADMIN_TABLE] ANALYZE pagg_tab_para
;
----------
[ALTER_TYPE] alter domain dom set not null
;
----------
[ADD_INDEX] CREATE INDEX "hash_i4_index" ON "public"."hash_i4_heap" USING "hash" ("random")
;
