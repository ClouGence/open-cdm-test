CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION nonexistent
;
ALTER TABLE pg_toast.pg_toast_28170 RENAME TO reind_con_toast
;
CREATE TABLE public.tst_one_array (
		a INTEGER PRIMARY KEY,
		b INTEGER[]
		)
;
--
-- JOIN
-- Test JOIN clauses
--

CREATE TABLE J1_TBL (
  i integer,
  j integer,
  t text
)
;
CREATE INDEX btg_y_x_w_idx ON btg(y, x, w)
;
ALTER OPERATOR FAMILY alt_opf1 USING hash RENAME TO alt_opf4
;
CREATE FUNCTION g_int_options(internal)
RETURNS void
AS '$libdir/_int', 'g_int_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
create index idxpart1_2c_idx on idxpart1 ((b + a)) where b > 1
;
CREATE TABLE ctlt2 (c text)
;
-- Fail, not empty
DROP TABLESPACE regress_tblspace
;
CREATE INDEX part_drop_index_locking_subpart_idx ON part_drop_index_locking_subpart(id)
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- too many rows
  execute 'select * from foo where f1 > $1' using 1 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
CREATE SCHEMA alter2
;
CREATE FUNCTION myvarcharsend(myvarchar) RETURNS bytea
        LANGUAGE internal STABLE PARALLEL SAFE STRICT AS 'varcharsend'
;
ALTER TABLE ONLY "public"."mlparted4" DROP COLUMN "........pg.dropped.1........"
;
CREATE SCHEMA reindex_inj
;
-- fail

drop function anyctest(anycompatiblenonarray, anycompatiblenonarray)
;
CREATE DOMAIN "generated_virtual_tests"."gtestdomainnn" AS integer
	CONSTRAINT "gtestdomainnn_check" CHECK ((VALUE IS NOT NULL))
;
/* contrib/intarray/intarray--1.2.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- Create the user-defined type for the 1-D integer arrays (_int4)
--

-- Query type
CREATE FUNCTION bqarr_in(cstring)
RETURNS query_int
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
drop function foreach_test(anyarray)
;
create index idxpart1_c on idxpart1 (c)
;
alter table user_table add column address varchar(255) unique
;
create table test_partitioned (a int) partition by list (a)
;
ALTER TYPE "public"."insenum" ADD VALUE 'i20'
;
ALTER TABLE unlogged2 SET LOGGED
;
CREATE FUNCTION word_similarity_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
CREATE TABLE part_3_4 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IN (3)))
;
CREATE OPERATOR ~~* (
    PROCEDURE = texticlike,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = !~~*,
    RESTRICT  = iclikesel,
    JOIN      = iclikejoinsel
)
;
CREATE VIEW regtest_pview AS SELECT * FROM regtest_ptable WHERE a < 99
;
ALTER TABLE split_alter_table.part_parent ATTACH PARTITION split_alter_table.part_us FOR VALUES IN ('us')
;
create trigger tg_system_au after update
    on System for each row execute procedure tg_system_au()
;
CREATE DOMAIN js_int_array_1d  AS int[]   CHECK(array_length(VALUE, 1) = 3)
;
drop type intr
;
CREATE INDEX concur_reindex_ind2 ON concur_reindex_tab(c2)
;
CREATE VIEW foreign_data_wrappers AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           authorization_identifier,
           CAST(NULL AS character_data) AS library_name,
           foreign_data_wrapper_language
    FROM _pg_foreign_data_wrappers w
;
CREATE TEXT SEARCH CONFIGURATION basque
	(PARSER = default)
;
ALTER INDEX concur_reindex_part_index_0 ATTACH PARTITION concur_reindex_part_index_0_2
;
CREATE TABLE brin_timestamp_test(a TIMESTAMP)
;
CREATE OR REPLACE FUNCTION test_evtrig_dropped_objects() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
    LOOP
        IF obj.object_type = 'table' THEN
                EXECUTE format('DROP TABLE IF EXISTS audit_tbls.%I',
					format('%s_%s', obj.schema_name, obj.object_name));
        END IF;

	INSERT INTO dropped_objects
		(object_type, schema_name, object_name,
		 object_identity, address_names, address_args,
		 is_temporary, original, normal) VALUES
		(obj.object_type, obj.schema_name, obj.object_name,
		 obj.object_identity, obj.address_names, obj.address_args,
		 obj.is_temporary, obj.original, obj.normal);
    END LOOP;
END
$$
;
/*
 * Test decoding relation rewrite with toast. The insert into tbl2 within the
 * same transaction is there to check that there is no remaining toast_hash not
 * being reset.
 */
CREATE TABLE tbl1 (a INT, b TEXT)
;
-- this is the correct way:
create rule rules_foorule as on insert to rules_foo where f1 < 100
do instead insert into rules_foo2 values (new.f1)
;
CREATE FUNCTION intarray_push_array(_int4, _int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- Add into extension
alter extension postgres_fdw add operator class my_op_class using btree
;
-- Test pg_trigger_depth()

create table depth_a (id int not null primary key)
;
drop index idxpart_a_idx
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=12478 host=/tmp/EWVfXZ8w5d dbname=postgres' PUBLICATION tap_pub
;
-- Test view decompilation in the face of column addition/deletion/renaming

create table tt2 (a int, b int, c int)
;
-- Tests for casting between the rowtypes of parent and child
-- tables. See the pgsql-hackers thread beginning Dec. 4/04
create table base (i integer)
;
COMMENT ON CONSTRAINT comment_test_positive_col_check ON comment_test IS 'CHECK constraint on comment_test.positive_col'
;
create table hp_contradict_test_p2 partition of hp_contradict_test for values with (modulus 2, remainder 1)
;
------------------------------------------------------------------------------------------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema6 AUTHORIZATION nonexistent
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28170 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE public.tst_one_array (
		a INTEGER PRIMARY KEY,
		b INTEGER[]
		)
;
----------
[CREATE_TABLE] --
-- JOIN
-- Test JOIN clauses
--

CREATE TABLE J1_TBL (
  i integer,
  j integer,
  t text
)
;
----------
[ADD_INDEX] CREATE INDEX btg_y_x_w_idx ON btg(y, x, w)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf1 USING hash RENAME TO alt_opf4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_int_options(internal)
RETURNS void
AS '$libdir/_int', 'g_int_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] create index idxpart1_2c_idx on idxpart1 ((b + a)) where b > 1
;
----------
[CREATE_TABLE] CREATE TABLE ctlt2 (c text)
;
----------
[DROP_TABLESPACE] -- Fail, not empty
DROP TABLESPACE regress_tblspace
;
----------
[ADD_INDEX] CREATE INDEX part_drop_index_locking_subpart_idx ON part_drop_index_locking_subpart(id)
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- too many rows
  execute 'select * from foo where f1 > $1' using 1 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[CREATE_SCHEMA] CREATE SCHEMA alter2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION myvarcharsend(myvarchar) RETURNS bytea
        LANGUAGE internal STABLE PARALLEL SAFE STRICT AS 'varcharsend'
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted4" DROP COLUMN "........pg.dropped.1........"
;
----------
[CREATE_SCHEMA] CREATE SCHEMA reindex_inj
;
----------
[DROP_PROG_OBJ] -- fail

drop function anyctest(anycompatiblenonarray, anycompatiblenonarray)
;
----------
[CREATE_TYPE] CREATE DOMAIN "generated_virtual_tests"."gtestdomainnn" AS integer
	CONSTRAINT "gtestdomainnn_check" CHECK ((VALUE IS NOT NULL))
;
----------
[CREATE_PROG_OBJ] /* contrib/intarray/intarray--1.2.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- Create the user-defined type for the 1-D integer arrays (_int4)
--

-- Query type
CREATE FUNCTION bqarr_in(cstring)
RETURNS query_int
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] drop function foreach_test(anyarray)
;
----------
[ADD_INDEX] create index idxpart1_c on idxpart1 (c)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table user_table add column address varchar(255) unique
;
----------
[CREATE_TABLE] create table test_partitioned (a int) partition by list (a)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i20'
;
----------
[ALTER_TABLE] ALTER TABLE unlogged2 SET LOGGED
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION word_similarity_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE part_3_4 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IN (3)))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~~* (
    PROCEDURE = texticlike,
    LEFTARG   = citext,
    RIGHTARG  = citext,
    NEGATOR   = !~~*,
    RESTRICT  = iclikesel,
    JOIN      = iclikejoinsel
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW regtest_pview AS SELECT * FROM regtest_ptable WHERE a < 99
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE split_alter_table.part_parent ATTACH PARTITION split_alter_table.part_us FOR VALUES IN ('us')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_system_au after update
    on System for each row execute procedure tg_system_au()
;
----------
[CREATE_TYPE] CREATE DOMAIN js_int_array_1d  AS int[]   CHECK(array_length(VALUE, 1) = 3)
;
----------
[DROP_TYPE] drop type intr
;
----------
[ADD_INDEX] CREATE INDEX concur_reindex_ind2 ON concur_reindex_tab(c2)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW foreign_data_wrappers AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           authorization_identifier,
           CAST(NULL AS character_data) AS library_name,
           foreign_data_wrapper_language
    FROM _pg_foreign_data_wrappers w
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION basque
	(PARSER = default)
;
----------
[ALTER_INDEX] ALTER INDEX concur_reindex_part_index_0 ATTACH PARTITION concur_reindex_part_index_0_2
;
----------
[CREATE_TABLE] CREATE TABLE brin_timestamp_test(a TIMESTAMP)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_evtrig_dropped_objects() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
    LOOP
        IF obj.object_type = 'table' THEN
                EXECUTE format('DROP TABLE IF EXISTS audit_tbls.%I',
					format('%s_%s', obj.schema_name, obj.object_name));
        END IF;

	INSERT INTO dropped_objects
		(object_type, schema_name, object_name,
		 object_identity, address_names, address_args,
		 is_temporary, original, normal) VALUES
		(obj.object_type, obj.schema_name, obj.object_name,
		 obj.object_identity, obj.address_names, obj.address_args,
		 obj.is_temporary, obj.original, obj.normal);
    END LOOP;
END
$$
;
----------
[CREATE_TABLE] /*
 * Test decoding relation rewrite with toast. The insert into tbl2 within the
 * same transaction is there to check that there is no remaining toast_hash not
 * being reset.
 */
CREATE TABLE tbl1 (a INT, b TEXT)
;
----------
[CREATE_POLICY] -- this is the correct way:
create rule rules_foorule as on insert to rules_foo where f1 < 100
do instead insert into rules_foo2 values (new.f1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION intarray_push_array(_int4, _int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_LIBRARY] -- Add into extension
alter extension postgres_fdw add operator class my_op_class using btree
;
----------
[CREATE_TABLE] -- Test pg_trigger_depth()

create table depth_a (id int not null primary key)
;
----------
[DROP_INDEX] drop index idxpart_a_idx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=12478 host=/tmp/EWVfXZ8w5d dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_TABLE] -- Test view decompilation in the face of column addition/deletion/renaming

create table tt2 (a int, b int, c int)
;
----------
[CREATE_TABLE] -- Tests for casting between the rowtypes of parent and child
-- tables. See the pgsql-hackers thread beginning Dec. 4/04
create table base (i integer)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT comment_test_positive_col_check ON comment_test IS 'CHECK constraint on comment_test.positive_col'
;
----------
[CREATE_TABLE] create table hp_contradict_test_p2 partition of hp_contradict_test for values with (modulus 2, remainder 1)
;
