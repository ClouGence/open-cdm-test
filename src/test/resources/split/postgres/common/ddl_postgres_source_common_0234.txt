---------------------------------------------------
-- ISMN13:
CREATE FUNCTION btismn13cmp(ismn13, ismn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP TABLE  BOOLTBL2
;
DROP SUBSCRIPTION regress_sub1
;
-- should fail
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, c, d COLLATE "POSIX" FROM viewtest_tbl
;
drop trigger child3_delete_trig on child3
;
CREATE TABLE testtab6 (a int)
;
ALTER ROUTINE ptest1(text) RENAME TO ptest1a
;
CREATE TABLE d_lp_fk_1 PARTITION OF d_lp_fk FOR VALUES IN (1)
;
ALTER TABLE alter_table_under_transition_tables
  DROP column name
;
CREATE TABLE "public"."event_trigger_test" (
    "a" integer,
    "b" "text"
)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
CREATE TABLE "public"."trigger_parted_p2_2" (
    "a" integer CONSTRAINT "trigger_parted_a_not_null" NOT NULL
)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest10"
    ADD CONSTRAINT "gtest10_pkey" PRIMARY KEY ("a")
;
--
-- test FOREIGN KEY, range references range
--

-- test table setup
DROP TABLE temporal_rng
;
ALTER TABLE inh_parent ADD COLUMN i int NOT NULL
;
CREATE FUNCTION "public"."autoinc"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/autoinc.so', 'autoinc'
;
create domain domaintext text
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
CREATE TABLE split_stats_next.items (
    id integer,
    category text,
    amount numeric,
    created_at timestamp
)
;
DROP TABLE pitest1_pfail
;
-- Test the case where BR UPDATE triggers change the partition key.
CREATE FUNCTION func_parted_mod_b() returns trigger as $$
BEGIN
   NEW.b = 2; -- This is changing partition key column.
   return NEW;
END $$ LANGUAGE plpgsql
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_event_trigger
;
create function test_variable_storage() returns text as $$
declare x text;
begin
  x := '1234';
  begin
    x := x || '5678';
    -- force error inside subtransaction SPI context
    perform trap_zero_divide(-100);
  exception
    when others then
      x := x || '9012';
  end;
  return x;
end$$ language plpgsql
;
CREATE FUNCTION fn_op6(int8, int8)
RETURNS type_op6 AS $$
    SELECT NULL::type_op6;
$$ LANGUAGE sql IMMUTABLE
;
ANALYZE pg_catalog.pg_foreign_server
;
CREATE OPERATOR @-@ ( leftarg = int4, rightarg = int4, procedure = int4mi )
;
CREATE EXTENSION jsonb_plpython2u CASCADE
;
-- Adding a NOT VALID foreign key on a partitioned table referencing
-- a non-partitioned table fails.
ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk NOT VALID
;
CREATE FUNCTION ltxtq_in(cstring)
RETURNS ltxtquery
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER OPERATOR FAMILY alt_opf19 USING btree ADD FUNCTION 5 (int4) btint42cmp(int4, int2)
;
create table ��ג�������ђ�� (��ђ�� text, �ʬ������������ varchar, ������1A������ char(16))
;
DROP FOREIGN TABLE remote_tbl
;
DROP TABLE trans_barbaz
;
ALTER TABLE deptest2 ALTER f1 SET DEFAULT nextval('ss1')
;
CREATE TABLE varbittmp (a varbit)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
DROP TABLE ataddindex
;
CREATE FOREIGN TABLE ctl_foreign_table1(LIKE ctl_table EXCLUDING ALL) SERVER ctl_s0
;
CREATE UNIQUE INDEX t_a_idx ON t (a)
;
DROP INDEX CONCURRENTLY regress_test_table_t_idx
;
create schema alter1
;
DROP TYPE hash_test_t2
;
create trigger trig_upd_before before update on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
CALL ptest10(a => null, b => 8, c => 2)
;
--
-- Test the basic moving-aggregate machinery
--

-- create aggregates that record the series of transform calls (these are
-- intentionally not true inverses)

CREATE FUNCTION logging_sfunc_nonstrict(text, anyelement) RETURNS text AS
$$ SELECT COALESCE($1, '') || '*' || quote_nullable($2) $$
LANGUAGE SQL IMMUTABLE
;
create table dcomptable (d1 dcomptype unique)
;
CREATE FUNCTION "public"."test_evtrig_dropped_objects"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
    LOOP
        IF obj.object_type = 'table' THEN
                EXECUTE format('DROP TABLE IF EXISTS audit_tbls.%I',
					format('%s_%s', obj.schema_name, obj.object_name));
        END IF;

	INSERT INTO dropped_objects
		(object_type, schema_name, object_name,
		 object_identity, address_names, address_args,
		 is_temporary, original, normal) VALUES
		(obj.object_type, obj.schema_name, obj.object_name,
		 obj.object_identity, obj.address_names, obj.address_args,
		 obj.is_temporary, obj.original, obj.normal);
    END LOOP;
END
$$
;
CREATE TABLE reindex_before AS
SELECT oid, relname, relfilenode, relkind, reltoastrelid
	FROM pg_class
	where relnamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'schema_to_reindex')
;
CREATE SCHEMA test_func_dep2
;
drop function compos()
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISMN13:
CREATE FUNCTION btismn13cmp(ismn13, ismn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE  BOOLTBL2
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION regress_sub1
;
----------
[CREATE_VIEW(SELECT)] -- should fail
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, c, d COLLATE "POSIX" FROM viewtest_tbl
;
----------
[DROP_TRIGGER] drop trigger child3_delete_trig on child3
;
----------
[CREATE_TABLE] CREATE TABLE testtab6 (a int)
;
----------
[RENAME_PROG_OBJ] ALTER ROUTINE ptest1(text) RENAME TO ptest1a
;
----------
[CREATE_TABLE] CREATE TABLE d_lp_fk_1 PARTITION OF d_lp_fk FOR VALUES IN (1)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE alter_table_under_transition_tables
  DROP column name
;
----------
[CREATE_TABLE] CREATE TABLE "public"."event_trigger_test" (
    "a" integer,
    "b" "text"
)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted_p2_2" (
    "a" integer CONSTRAINT "trigger_parted_a_not_null" NOT NULL
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest10"
    ADD CONSTRAINT "gtest10_pkey" PRIMARY KEY ("a")
;
----------
[DROP_TABLE] --
-- test FOREIGN KEY, range references range
--

-- test table setup
DROP TABLE temporal_rng
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE inh_parent ADD COLUMN i int NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."autoinc"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/autoinc.so', 'autoinc'
;
----------
[CREATE_TYPE] create domain domaintext text
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_policy
;
----------
[CREATE_TABLE] CREATE TABLE split_stats_next.items (
    id integer,
    category text,
    amount numeric,
    created_at timestamp
)
;
----------
[DROP_TABLE] DROP TABLE pitest1_pfail
;
----------
[CREATE_PROG_OBJ] -- Test the case where BR UPDATE triggers change the partition key.
CREATE FUNCTION func_parted_mod_b() returns trigger as $$
BEGIN
   NEW.b = 2; -- This is changing partition key column.
   return NEW;
END $$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_event_trigger
;
----------
[CREATE_PROG_OBJ] create function test_variable_storage() returns text as $$
declare x text;
begin
  x := '1234';
  begin
    x := x || '5678';
    -- force error inside subtransaction SPI context
    perform trap_zero_divide(-100);
  exception
    when others then
      x := x || '9012';
  end;
  return x;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn_op6(int8, int8)
RETURNS type_op6 AS $$
    SELECT NULL::type_op6;
$$ LANGUAGE sql IMMUTABLE
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_foreign_server
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @-@ ( leftarg = int4, rightarg = int4, procedure = int4mi )
;
----------
[CREATE_LIBRARY] CREATE EXTENSION jsonb_plpython2u CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Adding a NOT VALID foreign key on a partitioned table referencing
-- a non-partitioned table fails.
ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk NOT VALID
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltxtq_in(cstring)
RETURNS ltxtquery
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf19 USING btree ADD FUNCTION 5 (int4) btint42cmp(int4, int2)
;
----------
[CREATE_TABLE] create table ��ג�������ђ�� (��ђ�� text, �ʬ������������ varchar, ������1A������ char(16))
;
----------
[DROP_TABLE] DROP FOREIGN TABLE remote_tbl
;
----------
[DROP_TABLE] DROP TABLE trans_barbaz
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE deptest2 ALTER f1 SET DEFAULT nextval('ss1')
;
----------
[CREATE_TABLE] CREATE TABLE varbittmp (a varbit)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
----------
[DROP_TABLE] DROP TABLE ataddindex
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ctl_foreign_table1(LIKE ctl_table EXCLUDING ALL) SERVER ctl_s0
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t_a_idx ON t (a)
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY regress_test_table_t_idx
;
----------
[CREATE_SCHEMA] create schema alter1
;
----------
[DROP_TYPE] DROP TYPE hash_test_t2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_upd_before before update on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[CALL_PROG_OBJ] CALL ptest10(a => null, b => 8, c => 2)
;
----------
[CREATE_PROG_OBJ] --
-- Test the basic moving-aggregate machinery
--

-- create aggregates that record the series of transform calls (these are
-- intentionally not true inverses)

CREATE FUNCTION logging_sfunc_nonstrict(text, anyelement) RETURNS text AS
$$ SELECT COALESCE($1, '') || '*' || quote_nullable($2) $$
LANGUAGE SQL IMMUTABLE
;
----------
[CREATE_TABLE] create table dcomptable (d1 dcomptype unique)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_evtrig_dropped_objects"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
    LOOP
        IF obj.object_type = 'table' THEN
                EXECUTE format('DROP TABLE IF EXISTS audit_tbls.%I',
					format('%s_%s', obj.schema_name, obj.object_name));
        END IF;

	INSERT INTO dropped_objects
		(object_type, schema_name, object_name,
		 object_identity, address_names, address_args,
		 is_temporary, original, normal) VALUES
		(obj.object_type, obj.schema_name, obj.object_name,
		 obj.object_identity, obj.address_names, obj.address_args,
		 obj.is_temporary, obj.original, obj.normal);
    END LOOP;
END
$$
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE reindex_before AS
SELECT oid, relname, relfilenode, relkind, reltoastrelid
	FROM pg_class
	where relnamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'schema_to_reindex')
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_func_dep2
;
----------
[DROP_PROG_OBJ] drop function compos()
;
