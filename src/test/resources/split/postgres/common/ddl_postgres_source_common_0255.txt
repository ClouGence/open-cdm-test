DROP VIEW ext_cor_view
;
ALTER TEXT SEARCH DICTIONARY alt_ts_dict2 SET SCHEMA alt_nsp2
;
create table text_hashp (a text) partition by hash (a)
;
CREATE FUNCTION gin_extract_value_float4(float4, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE TABLE default_expr_agg_column (id int DEFAULT (avg(id)))
;
CREATE INDEX brinidx_multi ON brintest_multi USING brin (
	int8col int8_minmax_multi_ops,
	int2col int2_minmax_multi_ops,
	int4col int4_minmax_multi_ops,
	oidcol oid_minmax_multi_ops,
	tidcol tid_minmax_multi_ops,
	float4col float4_minmax_multi_ops,
	float8col float8_minmax_multi_ops,
	macaddrcol macaddr_minmax_multi_ops,
	macaddr8col macaddr8_minmax_multi_ops,
	inetcol inet_minmax_multi_ops,
	cidrcol inet_minmax_multi_ops,
	datecol date_minmax_multi_ops,
	timecol time_minmax_multi_ops,
	timestampcol timestamp_minmax_multi_ops,
	timestamptzcol timestamptz_minmax_multi_ops,
	intervalcol interval_minmax_multi_ops,
	timetzcol timetz_minmax_multi_ops,
	numericcol numeric_minmax_multi_ops,
	uuidcol uuid_minmax_multi_ops,
	lsncol pg_lsn_minmax_multi_ops
) with (pages_per_range = 1)
;
drop event trigger regress_event_trigger
;
CREATE FUNCTION gbt_tstz_consistent(internal,timestamptz,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE OPERATOR #<# (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_lt,
       COMMUTATOR = #>#,
       NEGATOR = #>=#,
       RESTRICT = scalarltsel,
       JOIN = scalarltjoinsel
)
;
-- define the GiST support methods
CREATE FUNCTION gseg_consistent(internal,seg,smallint,oid,internal)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
drop table ͪߩѦ���
;
create rule rtest_pers_del as on delete to rtest_person do also
	delete from rtest_admin where pname = old.pname
;
create table part_40_inf partition of range_list_parted for values from (40) to (maxvalue) partition by list (b)
;
CREATE EVENT TRIGGER undroppable ON sql_drop
	EXECUTE PROCEDURE undroppable()
;
DROP VIEW atestv2
;
CREATE TABLE collate_test5 (
    a int,
    b testdomain_sv COLLATE "en_US"
)
;
CREATE FUNCTION rwagg_finalfunc(x anyarray) RETURNS anyarray
LANGUAGE plpgsql STRICT IMMUTABLE AS $$
DECLARE
    res x%TYPE;
BEGIN
    -- assignment is essential for this test, it expands the array to R/W
    res := array_fill(x[1], ARRAY[4]);
    RETURN res;
END;
$$
;
CREATE TRIGGER "tg_pslot_bu" BEFORE UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pslot_bu"()
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
-- fail
alter table idxpart drop constraint idxpart_pkey
;
ALTER ROUTINE cp_testfunc1(int) RENAME TO cp_testfunc1a
;
ALTER TABLE FKTABLE ADD CONSTRAINT fkdd FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION DEFERRABLE INITIALLY DEFERRED
;
-- OK
SECURITY LABEL ON VIEW dummy_seclabel_view1 IS 'classified'
;
CREATE AGGREGATE oldcnt (
   sfunc = int8inc, basetype = 'ANY', stype = int8,
   initcond = '0'
)
;
CREATE POLICY "p1" ON "regress_rls_schema"."rls_tbl" USING (("c1" > 5))
;
VACUUM test1
;
VACUUM (PARALLEL 2) pg_catalog.pg_opclass
;
CREATE TEMP TABLE parent ( id int, val text )
;
ALTER EVENT TRIGGER split_event_probe ENABLE REPLICA
;
create table wcowrtest2 (b text, c int, a int)
;
CREATE FOREIGN TABLE ftable
	PARTITION OF parent
	FOR VALUES IN ('AAA')
	SERVER loopback
	OPTIONS (table_name 'batch_table', batch_size '2')
;
do $$ declare x public.foo%rowtype; begin end $$
;
VACUUM (VERBOSE, ANALYZE) split_maint.maint_table
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_db_role_setting
;
create index concurrently on idxpart2 ((a/b))
;
create type ct1 as (f1 varbit)
;
CREATE TABLE byteatest (a bytea PRIMARY KEY, b int)
;
ALTER TABLE pcachetest ADD COLUMN q3 bigint
;
-- Test various RETURN statements in BEFORE triggers.

CREATE FUNCTION trig_row_before_insupdate() RETURNS TRIGGER AS $$
  BEGIN
    NEW.f2 := NEW.f2 || ' triggered !';
    RETURN NEW;
  END
$$ language plpgsql
;
create temp table tt1(f1 int)
;
DROP TABLE pg_temp_24.pg_temp_27043_2, pg_temp_24.pg_temp_27043
;
ALTER TABLE pg_attrdef ADD UNIQUE USING INDEX pg_attrdef_adrelid_adnum_index
;
CREATE OPERATOR CLASS date_ops
DEFAULT FOR TYPE date USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       date_cmp(date,date),
    FUNCTION        2       gin_extract_value_date(date, internal),
    FUNCTION        3       gin_extract_query_date(date, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_date(date,date,int2, internal),
STORAGE         date
;
alter table mutable drop column f1
;
CREATE FUNCTION getrngfunc8(int) RETURNS int AS 'DECLARE rngfuncint int; BEGIN SELECT rngfuncid into rngfuncint FROM rngfunc WHERE rngfuncid = $1; RETURN rngfuncint; END;' LANGUAGE plpgsql
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=25082 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION tap_pub
;
-- ok

TRUNCATE TABLE truncate_a RESTRICT
;
drop table pparent
;
CREATE VIEW xmlview3 AS SELECT xmlelement(name element, xmlattributes (1 as ":one:", 'deuce' as two), 'content&')
;
CREATE EXTENSION dict_int
;
------------------------------------------------------------------------------------------
[DROP_VIEW] DROP VIEW ext_cor_view
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY alt_ts_dict2 SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] create table text_hashp (a text) partition by hash (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_float4(float4, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE default_expr_agg_column (id int DEFAULT (avg(id)))
;
----------
[ADD_INDEX] CREATE INDEX brinidx_multi ON brintest_multi USING brin (
	int8col int8_minmax_multi_ops,
	int2col int2_minmax_multi_ops,
	int4col int4_minmax_multi_ops,
	oidcol oid_minmax_multi_ops,
	tidcol tid_minmax_multi_ops,
	float4col float4_minmax_multi_ops,
	float8col float8_minmax_multi_ops,
	macaddrcol macaddr_minmax_multi_ops,
	macaddr8col macaddr8_minmax_multi_ops,
	inetcol inet_minmax_multi_ops,
	cidrcol inet_minmax_multi_ops,
	datecol date_minmax_multi_ops,
	timecol time_minmax_multi_ops,
	timestampcol timestamp_minmax_multi_ops,
	timestamptzcol timestamptz_minmax_multi_ops,
	intervalcol interval_minmax_multi_ops,
	timetzcol timetz_minmax_multi_ops,
	numericcol numeric_minmax_multi_ops,
	uuidcol uuid_minmax_multi_ops,
	lsncol pg_lsn_minmax_multi_ops
) with (pages_per_range = 1)
;
----------
[DROP_TRIGGER] drop event trigger regress_event_trigger
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_tstz_consistent(internal,timestamptz,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #<# (
       LEFTARG = hstore,
       RIGHTARG = hstore,
       PROCEDURE = hstore_lt,
       COMMUTATOR = #>#,
       NEGATOR = #>=#,
       RESTRICT = scalarltsel,
       JOIN = scalarltjoinsel
)
;
----------
[CREATE_PROG_OBJ] -- define the GiST support methods
CREATE FUNCTION gseg_consistent(internal,seg,smallint,oid,internal)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] drop table ͪߩѦ���
;
----------
[CREATE_POLICY] create rule rtest_pers_del as on delete to rtest_person do also
	delete from rtest_admin where pname = old.pname
;
----------
[CREATE_TABLE] create table part_40_inf partition of range_list_parted for values from (40) to (maxvalue) partition by list (b)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER undroppable ON sql_drop
	EXECUTE PROCEDURE undroppable()
;
----------
[DROP_VIEW] DROP VIEW atestv2
;
----------
[CREATE_TABLE] CREATE TABLE collate_test5 (
    a int,
    b testdomain_sv COLLATE "en_US"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rwagg_finalfunc(x anyarray) RETURNS anyarray
LANGUAGE plpgsql STRICT IMMUTABLE AS $$
DECLARE
    res x%TYPE;
BEGIN
    -- assignment is essential for this test, it expands the array to R/W
    res := array_fill(x[1], ARRAY[4]);
    RETURN res;
END;
$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_pslot_bu" BEFORE UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pslot_bu"()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- fail
alter table idxpart drop constraint idxpart_pkey
;
----------
[RENAME_PROG_OBJ] ALTER ROUTINE cp_testfunc1(int) RENAME TO cp_testfunc1a
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD CONSTRAINT fkdd FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION DEFERRABLE INITIALLY DEFERRED
;
----------
[SECURITY_LABEL] -- OK
SECURITY LABEL ON VIEW dummy_seclabel_view1 IS 'classified'
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE oldcnt (
   sfunc = int8inc, basetype = 'ANY', stype = int8,
   initcond = '0'
)
;
----------
[CREATE_POLICY] CREATE POLICY "p1" ON "regress_rls_schema"."rls_tbl" USING (("c1" > 5))
;
----------
[ADMIN_TABLE] VACUUM test1
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_opclass
;
----------
[CREATE_TABLE] CREATE TEMP TABLE parent ( id int, val text )
;
----------
[ALTER_TRIGGER] ALTER EVENT TRIGGER split_event_probe ENABLE REPLICA
;
----------
[CREATE_TABLE] create table wcowrtest2 (b text, c int, a int)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ftable
	PARTITION OF parent
	FOR VALUES IN ('AAA')
	SERVER loopback
	OPTIONS (table_name 'batch_table', batch_size '2')
;
----------
[BLOCK] do $$ declare x public.foo%rowtype; begin end $$
;
----------
[ADMIN_TABLE] VACUUM (VERBOSE, ANALYZE) split_maint.maint_table
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_db_role_setting
;
----------
[ADD_INDEX] create index concurrently on idxpart2 ((a/b))
;
----------
[CREATE_TYPE] create type ct1 as (f1 varbit)
;
----------
[CREATE_TABLE] CREATE TABLE byteatest (a bytea PRIMARY KEY, b int)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pcachetest ADD COLUMN q3 bigint
;
----------
[CREATE_PROG_OBJ] -- Test various RETURN statements in BEFORE triggers.

CREATE FUNCTION trig_row_before_insupdate() RETURNS TRIGGER AS $$
  BEGIN
    NEW.f2 := NEW.f2 || ' triggered !';
    RETURN NEW;
  END
$$ language plpgsql
;
----------
[CREATE_TABLE] create temp table tt1(f1 int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_24.pg_temp_27043_2, pg_temp_24.pg_temp_27043
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_attrdef ADD UNIQUE USING INDEX pg_attrdef_adrelid_adnum_index
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS date_ops
DEFAULT FOR TYPE date USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       date_cmp(date,date),
    FUNCTION        2       gin_extract_value_date(date, internal),
    FUNCTION        3       gin_extract_query_date(date, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_date(date,date,int2, internal),
STORAGE         date
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table mutable drop column f1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION getrngfunc8(int) RETURNS int AS 'DECLARE rngfuncint int; BEGIN SELECT rngfuncid into rngfuncint FROM rngfunc WHERE rngfuncid = $1; RETURN rngfuncint; END;' LANGUAGE plpgsql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=25082 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION tap_pub
;
----------
[TRUNCATE_TABLE] -- ok

TRUNCATE TABLE truncate_a RESTRICT
;
----------
[DROP_TABLE] drop table pparent
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmlview3 AS SELECT xmlelement(name element, xmlattributes (1 as ":one:", 'deuce' as two), 'content&')
;
----------
[CREATE_LIBRARY] CREATE EXTENSION dict_int
;
