--
-- The default permissions for functions mean that anyone can execute them.
-- A number of functions shouldn't be executable by just anyone, but rather
-- than use explicit 'superuser()' checks in those functions, we use the GRANT
-- system to REVOKE access to those functions at initdb time.  Administrators
-- can later change who can access these functions, or leave them as only
-- available to superuser / cluster owner, if they choose.
--

REVOKE EXECUTE ON FUNCTION pg_start_backup(text, boolean, boolean) FROM public
;
GRANT SELECT ON constraint_column_usage TO PUBLIC
;
COPY public.gtest30_1 (a) TO stdout
;
--fail
COMMIT
;
-- succeeds
COMMIT
;
-- failed
COPY t1p_tens (o) FROM '/dev/null'
;
EXECUTE dumpOpr('1399')
;
EXECUTE dumpBaseType('30')
;
EXECUTE dumpFunc('34722')
;
EXECUTE dumpTableAttach('33136')
;
------------------------------------------------------------------------------------------
[REVOKE] --
-- The default permissions for functions mean that anyone can execute them.
-- A number of functions shouldn't be executable by just anyone, but rather
-- than use explicit 'superuser()' checks in those functions, we use the GRANT
-- system to REVOKE access to those functions at initdb time.  Administrators
-- can later change who can access these functions, or leave them as only
-- available to superuser / cluster owner, if they choose.
--

REVOKE EXECUTE ON FUNCTION pg_start_backup(text, boolean, boolean) FROM public
;
----------
[GRANT] GRANT SELECT ON constraint_column_usage TO PUBLIC
;
----------
[DATA_EXPORT] COPY public.gtest30_1 (a) TO stdout
;
----------
[TRANSACTION] --fail
COMMIT
;
----------
[TRANSACTION] -- succeeds
COMMIT
;
----------
[DATA_IMPORT] -- failed
COPY t1p_tens (o) FROM '/dev/null'
;
----------
[UNSAFE] EXECUTE dumpOpr('1399')
;
----------
[UNSAFE] EXECUTE dumpBaseType('30')
;
----------
[UNSAFE] EXECUTE dumpFunc('34722')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('33136')
;
