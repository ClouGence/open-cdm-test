-- LOCK TABLE and TRUNCATE permission
GRANT TRUNCATE ON lock_table TO regress_locktable_user
;
explain (analyze, costs off, summary off, timing off, buffers off) execute ab_q1 (1, 2, 3)
;
-- this user can only cluster clstr_1 and clstr_3, but the latter
-- has not been clustered
SET SESSION AUTHORIZATION regress_clstr_user
;
LOCK TABLE public."strange aaa
name" IN ACCESS SHARE MODE
;
COPY (SELECT unique1 FROM onek ORDER BY unique1 LIMIT 10) TO '/src/src/bin/pg_upgrade/tmp_check/results/psql-output1'
;
SET SESSION CHARACTERISTICS AS TRANSACTION READ WRITE
;
EXECUTE dumpAgg('40028')
;
GRANT SELECT ON hs1 TO PUBLIC
;
LOCK TABLE "public"."bit_defaults" IN ACCESS SHARE MODE
;
EXECUTE dumpTableAttach('36663')
;
------------------------------------------------------------------------------------------
[GRANT] -- LOCK TABLE and TRUNCATE permission
GRANT TRUNCATE ON lock_table TO regress_locktable_user
;
----------
[UNSAFE] explain (analyze, costs off, summary off, timing off, buffers off) execute ab_q1 (1, 2, 3)
;
----------
[SWITCH_USER] -- this user can only cluster clstr_1 and clstr_3, but the latter
-- has not been clustered
SET SESSION AUTHORIZATION regress_clstr_user
;
----------
[SESSION_LOCK] LOCK TABLE public."strange aaa
name" IN ACCESS SHARE MODE
;
----------
[DATA_EXPORT|SELECT] COPY (SELECT unique1 FROM onek ORDER BY unique1 LIMIT 10) TO '/src/src/bin/pg_upgrade/tmp_check/results/psql-output1'
;
----------
[TRANSACTION|SESSION_SETTING_WRITE] SET SESSION CHARACTERISTICS AS TRANSACTION READ WRITE
;
----------
[UNSAFE] EXECUTE dumpAgg('40028')
;
----------
[GRANT] GRANT SELECT ON hs1 TO PUBLIC
;
----------
[SESSION_LOCK] LOCK TABLE "public"."bit_defaults" IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpTableAttach('36663')
;
