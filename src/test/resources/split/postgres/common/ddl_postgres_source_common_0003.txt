CREATE VIEW administrable_role_authorizations AS
    SELECT *
    FROM applicable_roles
    WHERE is_grantable = 'YES'
;
alter table tenk2 set (parallel_workers = 0)
;
DROP TABLE mvtest_t CASCADE
;
-- Error
CREATE TABLE selinto_schema.tmp3 (a,b,c)
	   AS SELECT oid,relname,relacl FROM pg_class
	   WHERE relname like '%c%'
;
CREATE SCHEMA test_ns_schema_renamed
;
CREATE TABLE test_like_gen_3 (LIKE test_like_gen_1 INCLUDING GENERATED)
;
-- Remove @ and ~
DROP OPERATOR @ (hstore, hstore)
;
ALTER TABLE attmp3 ADD CONSTRAINT b_greater_than_ten CHECK (b > 10) NOT VALID
;
ALTER TABLE ONLY "fkpart3"."pk"
    ADD CONSTRAINT "pk_pkey" PRIMARY KEY ("a")
;
-- should work
alter table p1 drop column f1
;
ALTER TEXT SEARCH CONFIGURATION irish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
CREATE RULE r2 AS ON UPDATE TO x DO INSTEAD
    WITH t AS (SELECT OLD.*) UPDATE y SET a = t.n FROM t
;
/* Use primary key imported by LIKE for self-referential FK constraint */
CREATE TABLE inhz (x text REFERENCES inhz, LIKE inhx INCLUDING INDEXES)
;
COMMENT ON TEXT SEARCH CONFIGURATION hindi IS 'configuration for hindi language'
;
create table idxpart (col1 int, col2 int, a int, b int) partition by range (a)
;
-- Test UPDATE where source relation has run-time pruning enabled
truncate ab
;
alter view uv_iocu_view alter column bb set default 'view default'
;
create unique index Room_rno on Room using btree (roomno bpchar_ops)
;
CREATE FUNCTION btissncmp(issn, issn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- without finalfunc; test obsolete spellings 'sfunc1' etc
CREATE AGGREGATE newsum (
   sfunc1 = int4pl, basetype = int4, stype1 = int4,
   initcond1 = '0'
)
;
CREATE TABLE test11b AS (SELECT 1::priv_testdomain1 AS a)
;
-- COPY tests

CREATE TABLE itest9 (a int GENERATED ALWAYS AS IDENTITY, b text, c bigint)
;
CREATE FUNCTION citext(boolean)
RETURNS citext
AS 'booltext'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
ANALYZE a, b, c
;
create trigger trig_upd_before_3 before update on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
ANALYZE e_star
;
CREATE OR REPLACE FUNCTION pg_catalog.pg_logdir_ls()
RETURNS setof record
AS '$libdir/adminpack', 'pg_logdir_ls_v1_1'
LANGUAGE C VOLATILE STRICT
;
drop function sp_id_user(text)
;
ALTER OPERATOR <= (ean13, ismn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE OR REPLACE FUNCTION test_slru_page_delete(bigint) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_delete' LANGUAGE C
;
do $$
declare r record; d ordered_texts;
begin
  for r in select * from sometable loop
    raise notice 'processing row %', r.id;
    d := null;
    d.f1 := r.a;
    d.f2 := r.b;
  end loop;
end$$
;
create table parted_conflict_test_2 (b char, a int unique)
;
ANALYZE public.slow_emp4000
;
drop table base
;
CREATE TABLE gtestxx_4 (b int GENERATED ALWAYS AS (a * 2) STORED, a int NOT NULL)
;
CREATE DATABASE primary_afterpromotion
;
create or replace function hash_join_batches(query text)
returns table (original int, final int) language plpgsql
as
$$
declare
  whole_plan json;
  hash_node json;
begin
  for whole_plan in
    execute 'explain (analyze, format ''json'') ' || query
  loop
    hash_node := find_hash(json_extract_path(whole_plan, '0', 'Plan'));
    original := hash_node->>'Original Hash Batches';
    final := hash_node->>'Hash Batches';
    return next;
  end loop;
end;
$$
;
-- Can't have two constraints with the same name
CREATE TABLE notnull_tbl2 (a INTEGER CONSTRAINT blah NOT NULL, b INTEGER CONSTRAINT blah NOT NULL)
;
alter table idxpart attach partition idxpart1 for values from (0) to (10000)
;
-- OK
CREATE TABLE ctas_nodata_4 (ii) AS SELECT i, j FROM ctas_base WITH NO DATA
;
CREATE FUNCTION regexp_replace( citext, citext, text ) returns TEXT AS $$
    SELECT pg_catalog.regexp_replace( $1::pg_catalog.text, $2::pg_catalog.text, $3, 'i');
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE "public"."trigger_parted_p1_1" (
    "a" integer NOT NULL
)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_operator
;
CREATE TABLE tab_rowfilter_child_sync (a int)
;
--     N    P
-- should CREATE
CREATE AGGREGATE myaggp03a(*) (SFUNC = stfp, STYPE = int4[],
  FINALFUNC = ffp, INITCOND = '{}')
;
DROP TABLE pg_temp_11.pg_temp_26230_2, pg_temp_11.pg_temp_26230
;
ALTER PUBLICATION testpub_forschema DROP TABLE pub_test.testpub_nopk
;
CREATE TABLE "public"."slow_emp4000" (
    "home_base" "box"
)
;
CREATE INDEX concur_reindex_part_index_0_2 ON ONLY concur_reindex_part_0_2 (c1)
;
VACUUM (PARALLEL 1, FULL FALSE) tmp
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW administrable_role_authorizations AS
    SELECT *
    FROM applicable_roles
    WHERE is_grantable = 'YES'
;
----------
[ALTER_TABLE] alter table tenk2 set (parallel_workers = 0)
;
----------
[DROP_TABLE] DROP TABLE mvtest_t CASCADE
;
----------
[CREATE_TABLE|SELECT] -- Error
CREATE TABLE selinto_schema.tmp3 (a,b,c)
	   AS SELECT oid,relname,relacl FROM pg_class
	   WHERE relname like '%c%'
;
----------
[CREATE_SCHEMA] CREATE SCHEMA test_ns_schema_renamed
;
----------
[CREATE_TABLE] CREATE TABLE test_like_gen_3 (LIKE test_like_gen_1 INCLUDING GENERATED)
;
----------
[DROP_PROG_OBJ] -- Remove @ and ~
DROP OPERATOR @ (hstore, hstore)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp3 ADD CONSTRAINT b_greater_than_ten CHECK (b > 10) NOT VALID
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart3"."pk"
    ADD CONSTRAINT "pk_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLE|DROP_COLUMN] -- should work
alter table p1 drop column f1
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION irish ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_POLICY] CREATE RULE r2 AS ON UPDATE TO x DO INSTEAD
    WITH t AS (SELECT OLD.*) UPDATE y SET a = t.n FROM t
;
----------
[CREATE_TABLE] /* Use primary key imported by LIKE for self-referential FK constraint */
CREATE TABLE inhz (x text REFERENCES inhz, LIKE inhx INCLUDING INDEXES)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION hindi IS 'configuration for hindi language'
;
----------
[CREATE_TABLE] create table idxpart (col1 int, col2 int, a int, b int) partition by range (a)
;
----------
[TRUNCATE_TABLE] -- Test UPDATE where source relation has run-time pruning enabled
truncate ab
;
----------
[ALTER_VIEW|ALTER_COLUMN] alter view uv_iocu_view alter column bb set default 'view default'
;
----------
[ADD_INDEX] create unique index Room_rno on Room using btree (roomno bpchar_ops)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btissncmp(issn, issn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- without finalfunc; test obsolete spellings 'sfunc1' etc
CREATE AGGREGATE newsum (
   sfunc1 = int4pl, basetype = int4, stype1 = int4,
   initcond1 = '0'
)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test11b AS (SELECT 1::priv_testdomain1 AS a)
;
----------
[CREATE_TABLE] -- COPY tests

CREATE TABLE itest9 (a int GENERATED ALWAYS AS IDENTITY, b text, c bigint)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext(boolean)
RETURNS citext
AS 'booltext'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE a, b, c
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_upd_before_3 before update on parted2_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[ADMIN_TABLE] ANALYZE e_star
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_catalog.pg_logdir_ls()
RETURNS setof record
AS '$libdir/adminpack', 'pg_logdir_ls_v1_1'
LANGUAGE C VOLATILE STRICT
;
----------
[DROP_PROG_OBJ] drop function sp_id_user(text)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ean13, ismn) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_delete(bigint) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_delete' LANGUAGE C
;
----------
[BLOCK] do $$
declare r record; d ordered_texts;
begin
  for r in select * from sometable loop
    raise notice 'processing row %', r.id;
    d := null;
    d.f1 := r.a;
    d.f2 := r.b;
  end loop;
end$$
;
----------
[CREATE_TABLE] create table parted_conflict_test_2 (b char, a int unique)
;
----------
[ADMIN_TABLE] ANALYZE public.slow_emp4000
;
----------
[DROP_TABLE] drop table base
;
----------
[CREATE_TABLE] CREATE TABLE gtestxx_4 (b int GENERATED ALWAYS AS (a * 2) STORED, a int NOT NULL)
;
----------
[CREATE_CATALOG] CREATE DATABASE primary_afterpromotion
;
----------
[CREATE_PROG_OBJ] create or replace function hash_join_batches(query text)
returns table (original int, final int) language plpgsql
as
$$
declare
  whole_plan json;
  hash_node json;
begin
  for whole_plan in
    execute 'explain (analyze, format ''json'') ' || query
  loop
    hash_node := find_hash(json_extract_path(whole_plan, '0', 'Plan'));
    original := hash_node->>'Original Hash Batches';
    final := hash_node->>'Hash Batches';
    return next;
  end loop;
end;
$$
;
----------
[CREATE_TABLE] -- Can't have two constraints with the same name
CREATE TABLE notnull_tbl2 (a INTEGER CONSTRAINT blah NOT NULL, b INTEGER CONSTRAINT blah NOT NULL)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart1 for values from (0) to (10000)
;
----------
[CREATE_TABLE|SELECT] -- OK
CREATE TABLE ctas_nodata_4 (ii) AS SELECT i, j FROM ctas_base WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regexp_replace( citext, citext, text ) returns TEXT AS $$
    SELECT pg_catalog.regexp_replace( $1::pg_catalog.text, $2::pg_catalog.text, $3, 'i');
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted_p1_1" (
    "a" integer NOT NULL
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_operator
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_child_sync (a int)
;
----------
[CREATE_PROG_OBJ] --     N    P
-- should CREATE
CREATE AGGREGATE myaggp03a(*) (SFUNC = stfp, STYPE = int4[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_26230_2, pg_temp_11.pg_temp_26230
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_forschema DROP TABLE pub_test.testpub_nopk
;
----------
[CREATE_TABLE] CREATE TABLE "public"."slow_emp4000" (
    "home_base" "box"
)
;
----------
[ADD_INDEX] CREATE INDEX concur_reindex_part_index_0_2 ON ONLY concur_reindex_part_0_2 (c1)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 1, FULL FALSE) tmp
;
