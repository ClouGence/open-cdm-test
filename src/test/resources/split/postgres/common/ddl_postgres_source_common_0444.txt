ALTER TABLE attmp ADD COLUMN w interval
;
DROP TABLE mvtest_t
;
CREATE PUBLICATION tap_pub_5a FOR TABLE tab_rowfilter_partitioned_2
;
Drop Table If Exists tab_stats
;
CREATE FUNCTION gbt_float4_consistent(internal,float4,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE tab6 ADD PRIMARY KEY (b)
;
CREATE FUNCTION isnge(ismn, ismn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
--    N    P        P    P
-- should ERROR: tfp(int[],anyelement) not matched by tfp(anyarray,anyelement)
CREATE AGGREGATE myaggp12a(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
CREATE TABLE fract_only (id int, val numeric(4,4))
;
CREATE TABLE itest_parent (f1 date NOT NULL, f2 text, f3 bigint) PARTITION BY RANGE (f1)
;
CREATE SEQUENCE "public"."itest14_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
CREATE FUNCTION gbt_float4_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
create operator public.>^ (
 leftarg = int4,
 rightarg = int4,
 procedure = int4gt
)
;
-- check that leaf partitions of default partition are scanned when
-- attaching a partitioned table.
ALTER TABLE part_5 DROP CONSTRAINT check_a
;
--
-- Verify that you can't create a trigger with transition tables for
-- more than one event.
--

create trigger my_table_multievent_trig
  after insert or update on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
create index on test_mode (a)
;
DROP TABLE inh_nn_parent cascade
;
DROP DATABASE regression_invalid
;
CREATE TRIGGER error_ins_when BEFORE INSERT OR UPDATE ON main_table
FOR EACH ROW WHEN (OLD.a <> NEW.a)
EXECUTE PROCEDURE trigger_func('error_ins_old')
;
-- cleanup
drop table p
;
create table dep_priv_test (a int)
;
-- constant
CREATE DOMAIN domain2 AS int DEFAULT random(min=>10, max=>100)
;
DROP EXTENSION IF EXISTS plperl CASCADE
;
CREATE INDEX CONCURRENTLY mcic_one_pkey ON mcic_one (id)
		WHERE lck_shr(281457)
;
-- overlapping remainders
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 100, REMAINDER 3)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_subscription
;
-- try altering non-existent table, should fail
alter table nosuchtable drop column bar
;
-- OWNED BY errors
CREATE SEQUENCE sequence_testx OWNED BY nobody
;
-- no 'fetch' function, as the compress function is lossy.


--
--
--
-- date ops
--
--
--

CREATE FUNCTION gbt_date_consistent(internal,date,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE tab_rowfilter_3 (a int primary key, b boolean)
;
-- fail

create temp table op (f1 orderedpair)
;
drop function vari(int[])
;
create table inserttest2 (f1 bigint, f2 text)
;
/* New stuff in 1.3 begins here */

CREATE OR REPLACE FUNCTION pgstattuple_approx(IN reloid regclass,
    OUT table_len BIGINT,               -- physical table length in bytes
    OUT scanned_percent FLOAT8,         -- what percentage of the table's pages was scanned
    OUT approx_tuple_count BIGINT,      -- estimated number of live tuples
    OUT approx_tuple_len BIGINT,        -- estimated total length in bytes of live tuples
    OUT approx_tuple_percent FLOAT8,    -- live tuples in % (based on estimate)
    OUT dead_tuple_count BIGINT,        -- exact number of dead tuples
    OUT dead_tuple_len BIGINT,          -- exact total length in bytes of dead tuples
    OUT dead_tuple_percent FLOAT8,      -- dead tuples in % (based on estimate)
    OUT approx_free_space BIGINT,       -- estimated free space in bytes
    OUT approx_free_percent FLOAT8)     -- free space in % (based on estimate)
AS '$libdir/pgstattuple', 'pgstattuple_approx_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TEMP TABLE pg_temp_14.pg_temp_26565_2 (tid pg_catalog.tid)
;
CREATE TABLE test4 (a int, b text)
;
ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2 FOR VALUES FROM (1000,1000) TO (2000,2000)
;
CREATE TABLE test_cidr (
	i cidr
)
;
ANALYZE l, t
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=28775 host=/tmp/A2VbQelPDP dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
CREATE TABLE itest8 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
-- no such relation
alter table nonesuch rename to newnonesuch
;
ALTER TABLE regtest_table DROP COLUMN d
;
ALTER TABLE ONLY "public"."fk_notpartitioned_pk"
    ADD CONSTRAINT "fk_notpartitioned_pk_pkey" PRIMARY KEY ("a", "b")
;
DROP TABLE brin_iso
;
CREATE TABLE check_ign_err2 (n int, m int[], k int, l dcheck_ign_err2)
;
CREATE OR REPLACE FUNCTION test_ddl_deparse()
  RETURNS event_trigger LANGUAGE plpgsql AS
$$
DECLARE
	r record;
	r2 record;
	cmdtype text;
	objtype text;
	tag text;
BEGIN
	FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
	LOOP
		-- verify that tags match
		tag = public.get_command_tag(r.command);
		IF tag <> r.command_tag THEN
			RAISE NOTICE 'tag % doesn''t match %', tag, r.command_tag;
		END IF;

		-- log the operation
		cmdtype = public.get_command_type(r.command);
		IF cmdtype <> 'grant' THEN
			RAISE NOTICE 'DDL test: type %, tag %', cmdtype, tag;
		ELSE
			RAISE NOTICE 'DDL test: type %, object type %', cmdtype, r.object_type;
		END IF;

		-- if alter table, log more
		IF cmdtype = 'alter table' THEN
			FOR r2 IN SELECT *
						FROM unnest(public.get_altertable_subcmdtypes(r.command))
			LOOP
				RAISE NOTICE '  subcommand: %', r2.unnest;
			END LOOP;
		END IF;
	END LOOP;
END;
$$
;
ANALYZE public.prt1_l_p2_p2
;
DROP TABLE pg_temp_13.pg_temp_26923_2, pg_temp_13.pg_temp_26923
;
alter table atacc1 alter "........pg.dropped.1........" drop not null
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN w interval
;
----------
[DROP_TABLE] DROP TABLE mvtest_t
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_5a FOR TABLE tab_rowfilter_partitioned_2
;
----------
[DROP_TABLE] Drop Table If Exists tab_stats
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float4_consistent(internal,float4,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tab6 ADD PRIMARY KEY (b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ismn, ismn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] --    N    P        P    P
-- should ERROR: tfp(int[],anyelement) not matched by tfp(anyarray,anyelement)
CREATE AGGREGATE myaggp12a(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_TABLE] CREATE TABLE fract_only (id int, val numeric(4,4))
;
----------
[CREATE_TABLE] CREATE TABLE itest_parent (f1 date NOT NULL, f2 text, f3 bigint) PARTITION BY RANGE (f1)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."itest14_id_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float4_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_PROG_OBJ] create operator public.>^ (
 leftarg = int4,
 rightarg = int4,
 procedure = int4gt
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- check that leaf partitions of default partition are scanned when
-- attaching a partitioned table.
ALTER TABLE part_5 DROP CONSTRAINT check_a
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- Verify that you can't create a trigger with transition tables for
-- more than one event.
--

create trigger my_table_multievent_trig
  after insert or update on my_table referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[ADD_INDEX] create index on test_mode (a)
;
----------
[DROP_TABLE] DROP TABLE inh_nn_parent cascade
;
----------
[DROP_CATALOG] DROP DATABASE regression_invalid
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER error_ins_when BEFORE INSERT OR UPDATE ON main_table
FOR EACH ROW WHEN (OLD.a <> NEW.a)
EXECUTE PROCEDURE trigger_func('error_ins_old')
;
----------
[DROP_TABLE] -- cleanup
drop table p
;
----------
[CREATE_TABLE] create table dep_priv_test (a int)
;
----------
[CREATE_TYPE] -- constant
CREATE DOMAIN domain2 AS int DEFAULT random(min=>10, max=>100)
;
----------
[DROP_LIBRARY] DROP EXTENSION IF EXISTS plperl CASCADE
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY mcic_one_pkey ON mcic_one (id)
		WHERE lck_shr(281457)
;
----------
[CREATE_TABLE] -- overlapping remainders
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES WITH (MODULUS 100, REMAINDER 3)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_subscription
;
----------
[ALTER_TABLE|DROP_COLUMN] -- try altering non-existent table, should fail
alter table nosuchtable drop column bar
;
----------
[CREATE_SEQUENCE] -- OWNED BY errors
CREATE SEQUENCE sequence_testx OWNED BY nobody
;
----------
[CREATE_PROG_OBJ] -- no 'fetch' function, as the compress function is lossy.


--
--
--
-- date ops
--
--
--

CREATE FUNCTION gbt_date_consistent(internal,date,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_3 (a int primary key, b boolean)
;
----------
[CREATE_TABLE] -- fail

create temp table op (f1 orderedpair)
;
----------
[DROP_PROG_OBJ] drop function vari(int[])
;
----------
[CREATE_TABLE] create table inserttest2 (f1 bigint, f2 text)
;
----------
[CREATE_PROG_OBJ] /* New stuff in 1.3 begins here */

CREATE OR REPLACE FUNCTION pgstattuple_approx(IN reloid regclass,
    OUT table_len BIGINT,               -- physical table length in bytes
    OUT scanned_percent FLOAT8,         -- what percentage of the table's pages was scanned
    OUT approx_tuple_count BIGINT,      -- estimated number of live tuples
    OUT approx_tuple_len BIGINT,        -- estimated total length in bytes of live tuples
    OUT approx_tuple_percent FLOAT8,    -- live tuples in % (based on estimate)
    OUT dead_tuple_count BIGINT,        -- exact number of dead tuples
    OUT dead_tuple_len BIGINT,          -- exact total length in bytes of dead tuples
    OUT dead_tuple_percent FLOAT8,      -- dead tuples in % (based on estimate)
    OUT approx_free_space BIGINT,       -- estimated free space in bytes
    OUT approx_free_percent FLOAT8)     -- free space in % (based on estimate)
AS '$libdir/pgstattuple', 'pgstattuple_approx_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_14.pg_temp_26565_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE test4 (a int, b text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk ATTACH PARTITION fk_partitioned_fk_2 FOR VALUES FROM (1000,1000) TO (2000,2000)
;
----------
[CREATE_TABLE] CREATE TABLE test_cidr (
	i cidr
)
;
----------
[ADMIN_TABLE] ANALYZE l, t
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=28775 host=/tmp/A2VbQelPDP dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
----------
[CREATE_TABLE] CREATE TABLE itest8 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
----------
[RENAME_TABLE] -- no such relation
alter table nonesuch rename to newnonesuch
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE regtest_table DROP COLUMN d
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."fk_notpartitioned_pk"
    ADD CONSTRAINT "fk_notpartitioned_pk_pkey" PRIMARY KEY ("a", "b")
;
----------
[DROP_TABLE] DROP TABLE brin_iso
;
----------
[CREATE_TABLE] CREATE TABLE check_ign_err2 (n int, m int[], k int, l dcheck_ign_err2)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_ddl_deparse()
  RETURNS event_trigger LANGUAGE plpgsql AS
$$
DECLARE
	r record;
	r2 record;
	cmdtype text;
	objtype text;
	tag text;
BEGIN
	FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
	LOOP
		-- verify that tags match
		tag = public.get_command_tag(r.command);
		IF tag <> r.command_tag THEN
			RAISE NOTICE 'tag % doesn''t match %', tag, r.command_tag;
		END IF;

		-- log the operation
		cmdtype = public.get_command_type(r.command);
		IF cmdtype <> 'grant' THEN
			RAISE NOTICE 'DDL test: type %, tag %', cmdtype, tag;
		ELSE
			RAISE NOTICE 'DDL test: type %, object type %', cmdtype, r.object_type;
		END IF;

		-- if alter table, log more
		IF cmdtype = 'alter table' THEN
			FOR r2 IN SELECT *
						FROM unnest(public.get_altertable_subcmdtypes(r.command))
			LOOP
				RAISE NOTICE '  subcommand: %', r2.unnest;
			END LOOP;
		END IF;
	END LOOP;
END;
$$
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_l_p2_p2
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26923_2, pg_temp_13.pg_temp_26923
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter "........pg.dropped.1........" drop not null
;
