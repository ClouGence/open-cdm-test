-- should be int[] not dia
drop domain dia
;
ALTER TABLE constraint_rename_test ADD CONSTRAINT con2 CHECK (b > 0) NO INHERIT
;
CREATE OPERATOR ===
(
	"Leftarg" = box,
	"Rightarg" = box,
	"Procedure" = area_equal_function,
	"Commutator" = ===,
	"Negator" = !==,
	"Restrict" = area_restriction_function,
	"Join" = area_join_function,
	"Hashes",
	"Merges"
)
;
VACUUM FULL test2
;
CREATE TABLE foo_part_s1 PARTITION OF foo_parted FOR VALUES IN (1)
;
CREATE FUNCTION decrypt_iv(bytea, bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_decrypt_iv'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE "public"."pht2_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
create schema if not exists abc authorization session_user
;
CREATE FUNCTION sum_int_randrestart_minvfunc(int4, int4) RETURNS int4 AS
$$ SELECT CASE WHEN random() < 0.2 THEN NULL ELSE $1 - $2 END $$
LANGUAGE SQL STRICT
;
CREATE VIEW my_property_normal AS
       SELECT * FROM customer WHERE name = current_user
;
create table trigger_parted_p1 partition of trigger_parted for values in (1)
  partition by list (a)
;
CREATE TABLE "public"."rtest_view4" (
    "a" integer,
    "b" "text",
    "c" integer
)
;
DROP TABLE plt3_adv
;
create table extremely_skewed (id int, t text)
;
VACUUM pg_catalog.pg_replication_origin
;
DROP TRIGGER gtest1 ON gtest26
;
/* src/test/modules/test_predtest/test_predtest--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_predtest(query text,
  OUT strong_implied_by bool,
  OUT weak_implied_by bool,
  OUT strong_refuted_by bool,
  OUT weak_refuted_by bool,
  OUT s_i_holds bool,
  OUT w_i_holds bool,
  OUT s_r_holds bool,
  OUT w_r_holds bool)
STRICT
AS '$libdir/test_predtest' LANGUAGE C
;
CREATE FUNCTION fn_opf14 (int4) RETURNS BIGINT AS 'SELECT NULL::BIGINT;' LANGUAGE SQL
;
-- test old extension version entry points

CREATE EXTENSION pg_stat_statements WITH VERSION '1.4'
;
CREATE TABLE tx1 (x1 int, x2 int, x3 text)
;
ALTER FUNCTION gbt_int4_compress(internal) PARALLEL SAFE
;
DROP TRIGGER trig_stmt_before ON rem1
;
DO $$
DECLARE
	objtype text;
	names	text[];
	args	text[];
BEGIN
	FOR objtype IN VALUES
		('table'), ('index'), ('sequence'), ('view'),
		('materialized view'), ('foreign table'),
		('table column'), ('foreign table column'),
		('aggregate'), ('function'), ('procedure'), ('type'), ('cast'),
		('table constraint'), ('domain constraint'), ('conversion'), ('default value'),
		('operator'), ('operator class'), ('operator family'), ('rule'), ('trigger'),
		('text search parser'), ('text search dictionary'),
		('text search template'), ('text search configuration'),
		('policy'), ('user mapping'), ('default acl'), ('transform'),
		('operator of access method'), ('function of access method'),
		('publication relation')
	LOOP
		FOR names IN VALUES ('{eins}'), ('{addr_nsp, zwei}'), ('{eins, zwei, drei}')
		LOOP
			FOR args IN VALUES ('{}'), ('{integer}')
			LOOP
				BEGIN
					PERFORM pg_get_object_address(objtype, names, args);
				EXCEPTION WHEN OTHERS THEN
						RAISE WARNING 'error for %,%,%: %', objtype, names, args, sqlerrm;
				END;
			END LOOP;
		END LOOP;
	END LOOP;
END;
$$
;
ALTER TABLE ONLY "generated_stored_tests"."gtest10a"
    ADD CONSTRAINT "gtest10a_pkey" PRIMARY KEY ("a")
;
ALTER TABLE pg_toast.pg_toast_33212 RENAME TO reind_con_toast
;
create index on idxpart (a, b)
;
create function mr_outparam_succeed3(i anymultirange, out r anyrange, out t text)
  as $$ select range_merge($1), 'foo'::text $$ language sql
;
ALTER TABLE attmp ADD COLUMN m xid
;
CREATE FUNCTION dxsyn_lexize(internal, internal, internal, internal)
        RETURNS internal
        AS '$libdir/dict_xsyn'
        LANGUAGE C STRICT
;
drop table hp
;
DROP TABLE pg_temp_24.pg_temp_26747_2, pg_temp_24.pg_temp_26747
;
CREATE PROCEDURE consume_xid(cnt int)
AS $$
DECLARE
    i int;
    BEGIN
        FOR i in 1..cnt LOOP
            EXECUTE 'SELECT pg_current_xact_id()';
            COMMIT;
        END LOOP;
    END;
$$
LANGUAGE plpgsql
;
-- test an unlogged table, mostly to get coverage of brinbuildempty
CREATE UNLOGGED TABLE brintest_unlogged (n numrange)
;
ALTER TABLE pg_toast.pg_toast_33644 RENAME TO reind_con_toast
;
CLUSTER hs2 using hs1_pkey
;
CREATE FUNCTION postgres_fdw_get_connections (
    IN check_conn boolean DEFAULT false, OUT server_name text,
    OUT user_name text, OUT valid boolean, OUT used_in_xact boolean,
    OUT closed boolean, OUT remote_backend_pid int4)
RETURNS SETOF record
AS '$libdir/postgres_fdw', 'postgres_fdw_get_connections_1_2'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
DROP SEQUENCE seq
;
CREATE FUNCTION functest_IS_4b(x int DEFAULT functest_IS_4a()) RETURNS int LANGUAGE SQL AS 'SELECT x'
;
DROP TABLE stats_test
;
CREATE TABLE dump_test.test_third_table (
						f1 int, junk int,
						g1 int generated always as (f1 * 2) stored,
						"F3" int,
						g2 int generated always as ("F3" * 3) stored
					)
;
CREATE TABLE alice.unpartitioned (i INTEGER)
;
ALTER TEXT SEARCH CONFIGURATION serbian ADD MAPPING
    FOR word, hword_part, hword
	WITH serbian_stem
;
/*
 * text search configuration for dutch language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * dutch and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
CALL outer_p(42)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_db_role_setting
;
CREATE EVENT TRIGGER regress_event_trigger_report_dropped ON sql_drop
    EXECUTE PROCEDURE event_trigger_report_dropped()
;
--
-- Hash partitioning.
--

-- Use hand-rolled hash functions and operator classes to get predictable
-- result on different machines.  See the definitions of
-- part_test_int4_ops and part_test_text_ops in test_setup.sql.

CREATE TABLE mchash (a int, b text, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_text_ops)
;
create table refd_table (a int primary key, b text)
;
CREATE TABLE cj_target (tid integer, balance float, val text)
  WITH (autovacuum_enabled=off)
;
create index ginidx on gin_tbl using gin(p) with (fastupdate = off)
;
------------------------------------------------------------------------------------------
[DROP_TYPE] -- should be int[] not dia
drop domain dia
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE constraint_rename_test ADD CONSTRAINT con2 CHECK (b > 0) NO INHERIT
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ===
(
	"Leftarg" = box,
	"Rightarg" = box,
	"Procedure" = area_equal_function,
	"Commutator" = ===,
	"Negator" = !==,
	"Restrict" = area_restriction_function,
	"Join" = area_join_function,
	"Hashes",
	"Merges"
)
;
----------
[ADMIN_TABLE] VACUUM FULL test2
;
----------
[CREATE_TABLE] CREATE TABLE foo_part_s1 PARTITION OF foo_parted FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION decrypt_iv(bytea, bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_decrypt_iv'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht2_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_SCHEMA] create schema if not exists abc authorization session_user
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION sum_int_randrestart_minvfunc(int4, int4) RETURNS int4 AS
$$ SELECT CASE WHEN random() < 0.2 THEN NULL ELSE $1 - $2 END $$
LANGUAGE SQL STRICT
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW my_property_normal AS
       SELECT * FROM customer WHERE name = current_user
;
----------
[CREATE_TABLE] create table trigger_parted_p1 partition of trigger_parted for values in (1)
  partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_view4" (
    "a" integer,
    "b" "text",
    "c" integer
)
;
----------
[DROP_TABLE] DROP TABLE plt3_adv
;
----------
[CREATE_TABLE] create table extremely_skewed (id int, t text)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_replication_origin
;
----------
[DROP_TRIGGER] DROP TRIGGER gtest1 ON gtest26
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_predtest/test_predtest--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_predtest(query text,
  OUT strong_implied_by bool,
  OUT weak_implied_by bool,
  OUT strong_refuted_by bool,
  OUT weak_refuted_by bool,
  OUT s_i_holds bool,
  OUT w_i_holds bool,
  OUT s_r_holds bool,
  OUT w_r_holds bool)
STRICT
AS '$libdir/test_predtest' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn_opf14 (int4) RETURNS BIGINT AS 'SELECT NULL::BIGINT;' LANGUAGE SQL
;
----------
[CREATE_LIBRARY] -- test old extension version entry points

CREATE EXTENSION pg_stat_statements WITH VERSION '1.4'
;
----------
[CREATE_TABLE] CREATE TABLE tx1 (x1 int, x2 int, x3 text)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int4_compress(internal) PARALLEL SAFE
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_stmt_before ON rem1
;
----------
[BLOCK] DO $$
DECLARE
	objtype text;
	names	text[];
	args	text[];
BEGIN
	FOR objtype IN VALUES
		('table'), ('index'), ('sequence'), ('view'),
		('materialized view'), ('foreign table'),
		('table column'), ('foreign table column'),
		('aggregate'), ('function'), ('procedure'), ('type'), ('cast'),
		('table constraint'), ('domain constraint'), ('conversion'), ('default value'),
		('operator'), ('operator class'), ('operator family'), ('rule'), ('trigger'),
		('text search parser'), ('text search dictionary'),
		('text search template'), ('text search configuration'),
		('policy'), ('user mapping'), ('default acl'), ('transform'),
		('operator of access method'), ('function of access method'),
		('publication relation')
	LOOP
		FOR names IN VALUES ('{eins}'), ('{addr_nsp, zwei}'), ('{eins, zwei, drei}')
		LOOP
			FOR args IN VALUES ('{}'), ('{integer}')
			LOOP
				BEGIN
					PERFORM pg_get_object_address(objtype, names, args);
				EXCEPTION WHEN OTHERS THEN
						RAISE WARNING 'error for %,%,%: %', objtype, names, args, sqlerrm;
				END;
			END LOOP;
		END LOOP;
	END LOOP;
END;
$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest10a"
    ADD CONSTRAINT "gtest10a_pkey" PRIMARY KEY ("a")
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33212 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] create index on idxpart (a, b)
;
----------
[CREATE_PROG_OBJ] create function mr_outparam_succeed3(i anymultirange, out r anyrange, out t text)
  as $$ select range_merge($1), 'foo'::text $$ language sql
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN m xid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dxsyn_lexize(internal, internal, internal, internal)
        RETURNS internal
        AS '$libdir/dict_xsyn'
        LANGUAGE C STRICT
;
----------
[DROP_TABLE] drop table hp
;
----------
[DROP_TABLE] DROP TABLE pg_temp_24.pg_temp_26747_2, pg_temp_24.pg_temp_26747
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE consume_xid(cnt int)
AS $$
DECLARE
    i int;
    BEGIN
        FOR i in 1..cnt LOOP
            EXECUTE 'SELECT pg_current_xact_id()';
            COMMIT;
        END LOOP;
    END;
$$
LANGUAGE plpgsql
;
----------
[CREATE_TABLE] -- test an unlogged table, mostly to get coverage of brinbuildempty
CREATE UNLOGGED TABLE brintest_unlogged (n numrange)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33644 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] CLUSTER hs2 using hs1_pkey
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION postgres_fdw_get_connections (
    IN check_conn boolean DEFAULT false, OUT server_name text,
    OUT user_name text, OUT valid boolean, OUT used_in_xact boolean,
    OUT closed boolean, OUT remote_backend_pid int4)
RETURNS SETOF record
AS '$libdir/postgres_fdw', 'postgres_fdw_get_connections_1_2'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[DROP_SEQUENCE] DROP SEQUENCE seq
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_IS_4b(x int DEFAULT functest_IS_4a()) RETURNS int LANGUAGE SQL AS 'SELECT x'
;
----------
[DROP_TABLE] DROP TABLE stats_test
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_third_table (
						f1 int, junk int,
						g1 int generated always as (f1 * 2) stored,
						"F3" int,
						g2 int generated always as ("F3" * 3) stored
					)
;
----------
[CREATE_TABLE] CREATE TABLE alice.unpartitioned (i INTEGER)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION serbian ADD MAPPING
    FOR word, hword_part, hword
	WITH serbian_stem
;
----------
[CREATE_POLICY] /*
 * text search configuration for dutch language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * dutch and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
----------
[CALL_PROG_OBJ] CALL outer_p(42)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_db_role_setting
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER regress_event_trigger_report_dropped ON sql_drop
    EXECUTE PROCEDURE event_trigger_report_dropped()
;
----------
[CREATE_TABLE] --
-- Hash partitioning.
--

-- Use hand-rolled hash functions and operator classes to get predictable
-- result on different machines.  See the definitions of
-- part_test_int4_ops and part_test_text_ops in test_setup.sql.

CREATE TABLE mchash (a int, b text, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_text_ops)
;
----------
[CREATE_TABLE] create table refd_table (a int primary key, b text)
;
----------
[CREATE_TABLE] CREATE TABLE cj_target (tid integer, balance float, val text)
  WITH (autovacuum_enabled=off)
;
----------
[ADD_INDEX] create index ginidx on gin_tbl using gin(p) with (fastupdate = off)
;
