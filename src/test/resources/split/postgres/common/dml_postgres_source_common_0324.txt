-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."gtest30_1"'::pg_catalog.regclass
;
INSERT INTO clean_aborted_self (key, data) VALUES (-1, 'just to allocate metapage')
;
-- failed
-- partitioned table children
UPDATE t1p_ones SET o = 9
;
INSERT INTO public.ths_p1 VALUES (6, 676)
;
insert into test1 select generate_series(1, 10000)
;
UPDATE pgbench_tellers SET tbalance = tbalance + -2661 WHERE tid = 3
;
insert into rtest_emp values ('wiecc', '5000.00')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '25999'
;
INSERT INTO public.ths_p3 VALUES (1, 331)
;
UPDATE tab_freeze SET x = x + 1 WHERE id = 3
;
INSERT INTO pg_temp_12.pg_temp_26162_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_12.pg_temp_26162 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_12.pg_temp_26162 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO parent_tbl (a) VALUES(1),(5)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17909', relminmxid = '3'
WHERE oid = '"public"."depth1"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('0', 220)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9162', relminmxid = '1'
WHERE oid = '"testxmlschema"."test2"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('7', 227)
;
WITH deleted AS (DELETE FROM schema13.counted WHERE type = 'delta' RETURNING value), total AS (SELECT coalesce(sum(value), 0) as sum FROM deleted) UPDATE schema13.counted SET value = counted.value + total.sum FROM total WHERE type = 'total' RETURNING counted.value
;
INSERT INTO temporal_rng (id, valid_at) VALUES ('[2,3)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT (id, valid_at) DO UPDATE SET id = EXCLUDED.id + '[4,5)'
;
INSERT INTO document VALUES (2, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_carol', 'my first novel')
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle, dauthor = EXCLUDED.dauthor
;
INSERT INTO tst SELECT i%10, substr(encode(sha256(i::text::bytea), 'hex'), 1, 1) FROM generate_series(160001,170000) i
;
INSERT INTO public.tht VALUES ('1', 671)
;
INSERT INTO tab_replidentity_index VALUES(1, 1),(2, 2)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10974', relminmxid = '1'
WHERE oid = '"public"."rules_log"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('6', 616)
;
update tttest set price_on = -1 where price_id = 1
;
UPDATE	pg_catalog.pg_class SET	relfrozenxid = '17425' WHERE	relkind IN ('r', 'm', 't')
;
INSERT INTO temporal_mltrng (id, valid_at) VALUES
  ('[2,3)', datemultirange(daterange('2018-01-01', '2018-03-01')))
;
-- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.1........'
  AND attrelid = '"public"."test_type3"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10530', relminmxid = '1'
WHERE oid = '29395'
;
UPDATE base_tbl SET a=a, c=c
;
insert into atacc2 (test) values (-3)
;
insert into inserttestb (f3.if2[1], f3.if2[2]) values ('foo', 'bar')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."gs_data_1"'::pg_catalog.regclass
;
INSERT INTO num_exp_mul VALUES (5,5,'268862871.275335557081')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3073', relminmxid = '1'
WHERE oid = '"public"."kd_point_tbl"'::pg_catalog.regclass
;
DELETE FROM foo WHERE f2 = 'zit' RETURNING *
;
INSERT INTO temporal_rng (id, valid_at) VALUES ('[1,2)', daterange('2010-01-01', '2020-01-01')) ON CONFLICT ON CONSTRAINT temporal_rng_pk DO UPDATE SET id = EXCLUDED.id + '[3,4)'
;
INSERT INTO temporal_rng (id, valid_at) VALUES ('[5,6)', daterange('2018-01-01', '2018-02-01'))
;
INSERT INTO plt1_e SELECT i, i, 'A' || to_char(i/50, 'FM0000') FROM generate_series(0, 599, 2) i
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."identity_dump_logged"'::pg_catalog.regclass
;
-- under-estimates when using only per-column statistics
INSERT INTO ndistinct (a, b, c, filler1)
     SELECT mod(i,13), mod(i,17), mod(i,19),
            mod(i,23) || ' dollars and zero cents'
       FROM generate_series(1,1000) s(i)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15980', relminmxid = '3'
WHERE oid = '36866'
;
UPDATE temporal_mltrng SET valid_at = datemultirange(daterange('2016-02-01', '2016-03-01'))
  WHERE id = '[5,6)' AND valid_at = datemultirange(daterange('2018-02-01', '2018-03-01'))
;
INSERT INTO test_part_a VALUES (6, 4)
;
INSERT INTO foo_ptbl VALUES (0, 'aaa'), (9,'bbb'), (10,'ccc'), (99,'ddd')
;
UPDATE pg_catalog.pg_amproc
		SET amproc = 'int4_asc_cmp'::regproc
		WHERE amproc = 'int4_desc_cmp'::regproc
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '17994'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '25955'
;
UPDATE pk SET a = 3002 WHERE a = 3000
;
INSERT INTO test2cs VALUES ('ABC'), ('ghi')
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."gtest30_1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO clean_aborted_self (key, data) VALUES (-1, 'just to allocate metapage')
;
----------
[UPDATE] -- failed
-- partitioned table children
UPDATE t1p_ones SET o = 9
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 676)
;
----------
[INSERT|SELECT] insert into test1 select generate_series(1, 10000)
;
----------
[UPDATE] UPDATE pgbench_tellers SET tbalance = tbalance + -2661 WHERE tid = 3
;
----------
[INSERT] insert into rtest_emp values ('wiecc', '5000.00')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '25999'
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 331)
;
----------
[UPDATE] UPDATE tab_freeze SET x = x + 1 WHERE id = 3
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_12.pg_temp_26162_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_12.pg_temp_26162 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_12.pg_temp_26162 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT|SELECT] INSERT INTO parent_tbl (a) VALUES(1),(5)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '17909', relminmxid = '3'
WHERE oid = '"public"."depth1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 220)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9162', relminmxid = '1'
WHERE oid = '"testxmlschema"."test2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 227)
;
----------
[UPDATE|DELETE|SELECT] WITH deleted AS (DELETE FROM schema13.counted WHERE type = 'delta' RETURNING value), total AS (SELECT coalesce(sum(value), 0) as sum FROM deleted) UPDATE schema13.counted SET value = counted.value + total.sum FROM total WHERE type = 'total' RETURNING counted.value
;
----------
[MERGE|SELECT] INSERT INTO temporal_rng (id, valid_at) VALUES ('[2,3)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT (id, valid_at) DO UPDATE SET id = EXCLUDED.id + '[4,5)'
;
----------
[MERGE|SELECT] INSERT INTO document VALUES (2, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_carol', 'my first novel')
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle, dauthor = EXCLUDED.dauthor
;
----------
[INSERT|SELECT] INSERT INTO tst SELECT i%10, substr(encode(sha256(i::text::bytea), 'hex'), 1, 1) FROM generate_series(160001,170000) i
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 671)
;
----------
[INSERT] INSERT INTO tab_replidentity_index VALUES(1, 1),(2, 2)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10974', relminmxid = '1'
WHERE oid = '"public"."rules_log"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 616)
;
----------
[UPDATE] update tttest set price_on = -1 where price_id = 1
;
----------
[UPDATE|METADATA] UPDATE	pg_catalog.pg_class SET	relfrozenxid = '17425' WHERE	relkind IN ('r', 'm', 't')
;
----------
[INSERT|SELECT] INSERT INTO temporal_mltrng (id, valid_at) VALUES
  ('[2,3)', datemultirange(daterange('2018-01-01', '2018-03-01')))
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.1........'
  AND attrelid = '"public"."test_type3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10530', relminmxid = '1'
WHERE oid = '29395'
;
----------
[UPDATE] UPDATE base_tbl SET a=a, c=c
;
----------
[INSERT|SELECT] insert into atacc2 (test) values (-3)
;
----------
[INSERT|SELECT] insert into inserttestb (f3.if2[1], f3.if2[2]) values ('foo', 'bar')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."gs_data_1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (5,5,'268862871.275335557081')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3073', relminmxid = '1'
WHERE oid = '"public"."kd_point_tbl"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM foo WHERE f2 = 'zit' RETURNING *
;
----------
[MERGE|SELECT] INSERT INTO temporal_rng (id, valid_at) VALUES ('[1,2)', daterange('2010-01-01', '2020-01-01')) ON CONFLICT ON CONSTRAINT temporal_rng_pk DO UPDATE SET id = EXCLUDED.id + '[3,4)'
;
----------
[INSERT|SELECT] INSERT INTO temporal_rng (id, valid_at) VALUES ('[5,6)', daterange('2018-01-01', '2018-02-01'))
;
----------
[INSERT|SELECT] INSERT INTO plt1_e SELECT i, i, 'A' || to_char(i/50, 'FM0000') FROM generate_series(0, 599, 2) i
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."identity_dump_logged"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- under-estimates when using only per-column statistics
INSERT INTO ndistinct (a, b, c, filler1)
     SELECT mod(i,13), mod(i,17), mod(i,19),
            mod(i,23) || ' dollars and zero cents'
       FROM generate_series(1,1000) s(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15980', relminmxid = '3'
WHERE oid = '36866'
;
----------
[UPDATE] UPDATE temporal_mltrng SET valid_at = datemultirange(daterange('2016-02-01', '2016-03-01'))
  WHERE id = '[5,6)' AND valid_at = datemultirange(daterange('2018-02-01', '2018-03-01'))
;
----------
[INSERT] INSERT INTO test_part_a VALUES (6, 4)
;
----------
[INSERT] INSERT INTO foo_ptbl VALUES (0, 'aaa'), (9,'bbb'), (10,'ccc'), (99,'ddd')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_amproc
		SET amproc = 'int4_asc_cmp'::regproc
		WHERE amproc = 'int4_desc_cmp'::regproc
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '17994'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '25955'
;
----------
[UPDATE] UPDATE pk SET a = 3002 WHERE a = 3000
;
----------
[INSERT] INSERT INTO test2cs VALUES ('ABC'), ('ghi')
;
