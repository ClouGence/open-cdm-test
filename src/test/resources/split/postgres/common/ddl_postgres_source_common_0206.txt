CREATE TABLE concur_reindex_tab2 (c1 int REFERENCES concur_reindex_tab)
;
CREATE TEMP TABLE ptnowner_oldnodes AS
  SELECT oid, relname, relfilenode FROM pg_partition_tree('ptnowner') AS tree
  JOIN pg_class AS c ON c.oid=tree.relid
;
-- Test ispell dictionary with hunspell affix in configuration
CREATE TEXT SEARCH CONFIGURATION hunspell_tst (
						COPY=ispell_tst
)
;
-- test that domains are transformed like their base types

CREATE DOMAIN testboolxmldomain AS bool
;
DROP TABLE heaptest
;
CREATE FUNCTION public.bt_index_parent_check(index regclass,
													 heapallindexed boolean default false,
													 rootdescend boolean default false)
		RETURNS VOID AS $$
		BEGIN
			RAISE EXCEPTION 'Invoked wrong bt_index_parent_check!';
		END;
		$$ LANGUAGE plpgsql
;
CREATE SCHEMA "schema10" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
drop table j3
;
-- time check

CREATE TABLE timetmp (a time)
;
call test_catalog.test.test_procedure(1)
;
create table mclparted3_5 partition of mclparted for values in(3,5)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=25003 host=/tmp/V91Iv72Nga dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
CREATE TABLE "public"."mvtest_t" (
    "id" integer NOT NULL,
    "type" "text" NOT NULL,
    "amt" numeric NOT NULL
)
;
CREATE EXTENSION hstore
;
ALTER TABLE pg_ts_dict ADD UNIQUE USING INDEX pg_ts_dict_dictname_index
;
-- likewise, altering a referenced column's type is prohibited ...
alter table tt14t alter column f4 type integer using f4::integer
;
--
-- Type casts:
--
---------------------------------------------------
CREATE FUNCTION isbn13(ean13)
RETURNS isbn13
AS '$libdir/isn', 'isbn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE SEQUENCE sequence_test
;
ALTER POLICY p2 ON s2 USING (x in (select a from s1 where b like '%d2%'))
;
ALTER INDEX pg_toast.pg_toast_28051_index RENAME TO reind_con_toast_idx
;
ALTER TABLE pg_statistic_ext_data ADD PRIMARY KEY USING INDEX pg_statistic_ext_data_stxoid_inh_index
;
VACUUM (ANALYZE) vacowned_part2
;
ALTER INDEX attmp_idx ALTER COLUMN 2 SET STATISTICS 1000
;
-- ===================================================================
-- test WITH CHECK OPTION constraints
-- ===================================================================

CREATE FUNCTION row_before_insupd_trigfunc() RETURNS trigger AS $$BEGIN NEW.a := NEW.a + 10; RETURN NEW; END$$ LANGUAGE plpgsql
;
CREATE TABLE xmltest (
    id int,
    data xml
)
;
CREATE FUNCTION split_prog.touch_updated_at()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
    NEW.updated_at := clock_timestamp();
    RETURN NEW;
END
$$
;
CREATE TABLE macaddrtmp (a macaddr)
;
CREATE AGGREGATE "public"."aggfstr"(integer, integer, "text") (
    SFUNC = "public"."aggf_trans",
    STYPE = "public"."aggtype"[],
    INITCOND = '{}'
)
;
-- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_user_t:s0
CREATE TABLE tpart (o int, p text) PARTITION BY RANGE (o)
;
CREATE TABLE table_with_unique_not_null(id serial unique, data int)
;
CREATE FUNCTION int_custom_typanalyze_invalid(internal)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C STRICT
;
create or replace function shadowtest()
	returns void as $$
declare
f1 int;
begin
	declare
	f1 int;
	begin
	end;
end$$ language plpgsql
;
CREATE TABLE regress_pg_dump_schema.external_tab
						   (col1 int)
;
CREATE VIEW lock_view7 AS SELECT * from lock_view2
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_auth_members
;
create table hash_parted (
	a int,
	b int
) partition by hash (a custom_opclass, b custom_opclass)
;
CREATE AGGREGATE "public"."test_percentile_disc"(double precision ORDER BY "anyelement") (
    SFUNC = "ordered_set_transition",
    STYPE = "internal",
    FINALFUNC = "percentile_disc_final",
    FINALFUNC_EXTRA
)
;
CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (max(a)) TO ('2019-01-01')
;
CREATE UNIQUE INDEX "wslot_name" ON "public"."wslot" USING "btree" ("slotname")
;
CREATE TABLE gtest2 (a int, b text GENERATED ALWAYS AS (a || ' sec') STORED)
;
create unique index expr_part_comp_key_index on insertconflicttest(key, lower(fruit)) where key < 5
;
CREATE FUNCTION isnge(ean13, issn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create table bool_lp_false partition of bool_lp for values in(false)
;
CREATE TEMP TABLE combocidtest (foobar int)
;
--
-- SELECT INTO and INSERT permission, if owner is not allowed to insert.
--
CREATE SCHEMA selinto_schema
;
-- drop attributes recursively
ALTER TABLE fd_pt1 DROP COLUMN c4
;
CREATE VIEW view_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nv.nspname AS sql_identifier) AS table_schema,
           CAST(v.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name

    FROM pg_namespace nv, pg_class v, pg_depend dv,
         pg_depend dp, pg_proc p, pg_namespace np

    WHERE nv.oid = v.relnamespace
          AND v.relkind = 'v'
          AND v.oid = dv.refobjid
          AND dv.refclassid = 'pg_catalog.pg_class'::regclass
          AND dv.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dv.deptype = 'i'
          AND dv.objid = dp.objid
          AND dp.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dp.refclassid = 'pg_catalog.pg_proc'::regclass
          AND dp.refobjid = p.oid
          AND p.pronamespace = np.oid
          AND pg_has_role(p.proowner, 'USAGE')
;
ANALYZE pagg_tab_m
;
create function convslot_trig4() returns trigger as
$$begin raise exception 'BOOM!'; end$$ language plpgsql
;
ALTER TABLE gtest27 ALTER COLUMN x TYPE boolean USING x <> 0
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE concur_reindex_tab2 (c1 int REFERENCES concur_reindex_tab)
;
----------
[CREATE_TABLE|SELECT|METADATA] CREATE TEMP TABLE ptnowner_oldnodes AS
  SELECT oid, relname, relfilenode FROM pg_partition_tree('ptnowner') AS tree
  JOIN pg_class AS c ON c.oid=tree.relid
;
----------
[CREATE_POLICY] -- Test ispell dictionary with hunspell affix in configuration
CREATE TEXT SEARCH CONFIGURATION hunspell_tst (
						COPY=ispell_tst
)
;
----------
[CREATE_TYPE] -- test that domains are transformed like their base types

CREATE DOMAIN testboolxmldomain AS bool
;
----------
[DROP_TABLE] DROP TABLE heaptest
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION public.bt_index_parent_check(index regclass,
													 heapallindexed boolean default false,
													 rootdescend boolean default false)
		RETURNS VOID AS $$
		BEGIN
			RAISE EXCEPTION 'Invoked wrong bt_index_parent_check!';
		END;
		$$ LANGUAGE plpgsql
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "schema10" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
----------
[DROP_TABLE] drop table j3
;
----------
[CREATE_TABLE] -- time check

CREATE TABLE timetmp (a time)
;
----------
[CALL_PROG_OBJ] call test_catalog.test.test_procedure(1)
;
----------
[CREATE_TABLE] create table mclparted3_5 partition of mclparted for values in(3,5)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=25003 host=/tmp/V91Iv72Nga dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mvtest_t" (
    "id" integer NOT NULL,
    "type" "text" NOT NULL,
    "amt" numeric NOT NULL
)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION hstore
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_ts_dict ADD UNIQUE USING INDEX pg_ts_dict_dictname_index
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- likewise, altering a referenced column's type is prohibited ...
alter table tt14t alter column f4 type integer using f4::integer
;
----------
[CREATE_PROG_OBJ] --
-- Type casts:
--
---------------------------------------------------
CREATE FUNCTION isbn13(ean13)
RETURNS isbn13
AS '$libdir/isn', 'isbn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test
;
----------
[ALTER_POLICY] ALTER POLICY p2 ON s2 USING (x in (select a from s1 where b like '%d2%'))
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28051_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|PERFORMANCE|ADD_CONSTRAINT] ALTER TABLE pg_statistic_ext_data ADD PRIMARY KEY USING INDEX pg_statistic_ext_data_stxoid_inh_index
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) vacowned_part2
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX attmp_idx ALTER COLUMN 2 SET STATISTICS 1000
;
----------
[CREATE_PROG_OBJ] -- ===================================================================
-- test WITH CHECK OPTION constraints
-- ===================================================================

CREATE FUNCTION row_before_insupd_trigfunc() RETURNS trigger AS $$BEGIN NEW.a := NEW.a + 10; RETURN NEW; END$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE xmltest (
    id int,
    data xml
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_prog.touch_updated_at()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
    NEW.updated_at := clock_timestamp();
    RETURN NEW;
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE macaddrtmp (a macaddr)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."aggfstr"(integer, integer, "text") (
    SFUNC = "public"."aggf_trans",
    STYPE = "public"."aggtype"[],
    INITCOND = '{}'
)
;
----------
[CREATE_TABLE] -- @SECURITY-CONTEXT=unconfined_u:unconfined_r:sepgsql_regtest_user_t:s0
CREATE TABLE tpart (o int, p text) PARTITION BY RANGE (o)
;
----------
[CREATE_TABLE] CREATE TABLE table_with_unique_not_null(id serial unique, data int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_typanalyze_invalid(internal)
RETURNS boolean
AS '$libdir/test_custom_types'
LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] create or replace function shadowtest()
	returns void as $$
declare
f1 int;
begin
	declare
	f1 int;
	begin
	end;
end$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE regress_pg_dump_schema.external_tab
						   (col1 int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW lock_view7 AS SELECT * from lock_view2
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_auth_members
;
----------
[CREATE_TABLE] create table hash_parted (
	a int,
	b int
) partition by hash (a custom_opclass, b custom_opclass)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."test_percentile_disc"(double precision ORDER BY "anyelement") (
    SFUNC = "ordered_set_transition",
    STYPE = "internal",
    FINALFUNC = "percentile_disc_final",
    FINALFUNC_EXTRA
)
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF range_parted
  FOR VALUES FROM (max(a)) TO ('2019-01-01')
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "wslot_name" ON "public"."wslot" USING "btree" ("slotname")
;
----------
[CREATE_TABLE] CREATE TABLE gtest2 (a int, b text GENERATED ALWAYS AS (a || ' sec') STORED)
;
----------
[ADD_INDEX] create unique index expr_part_comp_key_index on insertconflicttest(key, lower(fruit)) where key < 5
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ean13, issn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] create table bool_lp_false partition of bool_lp for values in(false)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE combocidtest (foobar int)
;
----------
[CREATE_SCHEMA] --
-- SELECT INTO and INSERT permission, if owner is not allowed to insert.
--
CREATE SCHEMA selinto_schema
;
----------
[ALTER_TABLE|DROP_COLUMN] -- drop attributes recursively
ALTER TABLE fd_pt1 DROP COLUMN c4
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW view_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nv.nspname AS sql_identifier) AS table_schema,
           CAST(v.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name

    FROM pg_namespace nv, pg_class v, pg_depend dv,
         pg_depend dp, pg_proc p, pg_namespace np

    WHERE nv.oid = v.relnamespace
          AND v.relkind = 'v'
          AND v.oid = dv.refobjid
          AND dv.refclassid = 'pg_catalog.pg_class'::regclass
          AND dv.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dv.deptype = 'i'
          AND dv.objid = dp.objid
          AND dp.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dp.refclassid = 'pg_catalog.pg_proc'::regclass
          AND dp.refobjid = p.oid
          AND p.pronamespace = np.oid
          AND pg_has_role(p.proowner, 'USAGE')
;
----------
[ADMIN_TABLE] ANALYZE pagg_tab_m
;
----------
[CREATE_PROG_OBJ] create function convslot_trig4() returns trigger as
$$begin raise exception 'BOOM!'; end$$ language plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest27 ALTER COLUMN x TYPE boolean USING x <> 0
;
