ALTER OPERATOR FAMILY gist_macaddr_ops USING gist ADD
	FUNCTION	11  (macaddr, macaddr) gbt_macaddr_sortsupport (internal)
;
CREATE INDEX t1_brin ON s5.t1 USING BRIN (i)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_opfamily
;
VACUUM (ANALYZE) pg_catalog.pg_publication_rel
;
DROP AGGREGATE myavg (numeric)
;
-- Verify that columns are mapped correctly in expression indexes
create table idxpart (col1 int, col2 int, a int, b int) partition by range (a)
;
-- to see whether it's there
ALTER TABLE onek DROP CONSTRAINT onek_unique1_constraint_foo
;
create domain things AS INT
;
CREATE FUNCTION "public"."overpaid"("public"."emp") RETURNS boolean
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'overpaid'
;
ALTER TABLE test_inh_check_child ADD CONSTRAINT blocal CHECK (b < 1000)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_shdepend
;
CREATE TABLE "generated_stored_tests"."gtest_tableoid" (
    "a" integer NOT NULL,
    "b" boolean GENERATED ALWAYS AS (("tableoid" = ('"generated_stored_tests"."gtest_tableoid"'::"regclass")::"oid")) STORED,
    "c" "regclass" GENERATED ALWAYS AS ("tableoid") STORED
)
;
CREATE INDEX brin_test_multi_1_idx_1 ON brin_test_multi_1 USING brin (a int4_minmax_multi_ops) WITH (pages_per_range=5)
;
ALTER TABLE tr_pkey ADD COLUMN id serial primary key
;
ALTER MATERIALIZED VIEW IF EXISTS split_view_probe.base_mv RENAME TO base_mv_renamed
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub_mix_1, pub_mix_2
;
CREATE PUBLICATION "pub2" FOR ALL TABLES
;
CREATE COLLATION coll_dup_chk (VERSION = '1', VERSION = "NONSENSE", LOCALE = '')
;
-- but now we're not allowed to make it inherit anymore
alter table child inherit parent
;
-- trying to create default partition for the hash partitioned table
CREATE TABLE fail_default_part PARTITION OF hash_parted DEFAULT
;
DROP TABLE c1
;
-- corner cases with empty join conditions

create view tt21v as
select * from tt5 natural inner join tt6
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=15737 host=/tmp/Ez8p0KXCzD dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
CREATE TABLE "public"."cmmove2" (
    "f1" "text"
)
;
ALTER INDEX "fkpart3"."pk_pkey" ATTACH PARTITION "fkpart3"."pk2_pkey"
;
create temp table rtype (id int, ar text[])
;
CREATE FUNCTION intarray_push_elem(_int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP TABLE bit_table
;
ALTER TABLE ONLY "public"."fk_partitioned_fk_5_1"
    ADD CONSTRAINT "fk_partitioned_fk_5_1_a_b_fkey" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b")
;
IMPORT FOREIGN SCHEMA s1 FROM SERVER s9 INTO public
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS EXECUTE ctas_ine_query
;
ALTER TEXT SEARCH CONFIGURATION portuguese ADD MAPPING
    FOR word, hword_part, hword
	WITH portuguese_stem
;
CREATE TABLE plt1_e_p1 PARTITION OF plt1_e FOR VALUES IN ('0000', '0003', '0004', '0010')
;
ALTER TABLE test_part_a_1 ADD PRIMARY KEY (a)
;
create table parted_uniq_detach_test1 partition of parted_uniq_detach_test for values in (1)
;
-- Test generic object addressing/identification functions
CREATE SCHEMA addr_nsp
;
ALTER TABLE pg_toast.pg_toast_30815 RENAME TO reind_con_toast
;
CREATE DATABASE "postgres" WITH TEMPLATE = template0 OID = 5 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
do $$
declare x var_record_nn;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
CREATE TABLE parted (
	a text,
	b int NOT NULL DEFAULT 0,
	CONSTRAINT check_a CHECK (length(a) > 0)
) PARTITION BY LIST (a)
;
ALTER TABLE tbl2 DROP COLUMN val3
;
-- we used to get confused by a single output argument that is composite
CREATE PROCEDURE ptest4c(INOUT comp int8_tbl)
LANGUAGE SQL
AS $$
SELECT ROW(1, 2);
$$
;
CREATE FUNCTION gbt_float8_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER INDEX pg_toast.pg_toast_30764_index RENAME TO reind_con_toast_idx
;
create database abc owner = zyc
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_largeobject_metadata
;
drop table agg_hash_1
;
CREATE TABLE prt2_n_p2 PARTITION OF prt2_n FOR VALUES IN ('0001', '0005', '0002', '0009', '0008', '0011')
;
--
-- Check constraints on INSERT
--

CREATE SEQUENCE INSERT_SEQ
;
-- Test manipulation of temp schema's placement in search path

create table public.whereami (f1 text)
;
------------------------------------------------------------------------------------------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_macaddr_ops USING gist ADD
	FUNCTION	11  (macaddr, macaddr) gbt_macaddr_sortsupport (internal)
;
----------
[ADD_INDEX] CREATE INDEX t1_brin ON s5.t1 USING BRIN (i)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_opfamily
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_publication_rel
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE myavg (numeric)
;
----------
[CREATE_TABLE] -- Verify that columns are mapped correctly in expression indexes
create table idxpart (col1 int, col2 int, a int, b int) partition by range (a)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- to see whether it's there
ALTER TABLE onek DROP CONSTRAINT onek_unique1_constraint_foo
;
----------
[CREATE_TYPE] create domain things AS INT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."overpaid"("public"."emp") RETURNS boolean
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'overpaid'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE test_inh_check_child ADD CONSTRAINT blocal CHECK (b < 1000)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_shdepend
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest_tableoid" (
    "a" integer NOT NULL,
    "b" boolean GENERATED ALWAYS AS (("tableoid" = ('"generated_stored_tests"."gtest_tableoid"'::"regclass")::"oid")) STORED,
    "c" "regclass" GENERATED ALWAYS AS ("tableoid") STORED
)
;
----------
[ADD_INDEX] CREATE INDEX brin_test_multi_1_idx_1 ON brin_test_multi_1 USING brin (a int4_minmax_multi_ops) WITH (pages_per_range=5)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tr_pkey ADD COLUMN id serial primary key
;
----------
[RENAME_VIEW] ALTER MATERIALIZED VIEW IF EXISTS split_view_probe.base_mv RENAME TO base_mv_renamed
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub_mix_1, pub_mix_2
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION "pub2" FOR ALL TABLES
;
----------
[CREATE_POLICY] CREATE COLLATION coll_dup_chk (VERSION = '1', VERSION = "NONSENSE", LOCALE = '')
;
----------
[ALTER_TABLE] -- but now we're not allowed to make it inherit anymore
alter table child inherit parent
;
----------
[CREATE_TABLE] -- trying to create default partition for the hash partitioned table
CREATE TABLE fail_default_part PARTITION OF hash_parted DEFAULT
;
----------
[DROP_TABLE] DROP TABLE c1
;
----------
[CREATE_VIEW(SELECT)] -- corner cases with empty join conditions

create view tt21v as
select * from tt5 natural inner join tt6
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=15737 host=/tmp/Ez8p0KXCzD dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."cmmove2" (
    "f1" "text"
)
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart3"."pk_pkey" ATTACH PARTITION "fkpart3"."pk2_pkey"
;
----------
[CREATE_TABLE] create temp table rtype (id int, ar text[])
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION intarray_push_elem(_int4, int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE bit_table
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."fk_partitioned_fk_5_1"
    ADD CONSTRAINT "fk_partitioned_fk_5_1_a_b_fkey" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b")
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA s1 FROM SERVER s9 INTO public
;
----------
[CREATE_TABLE|UNSAFE] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS EXECUTE ctas_ine_query
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION portuguese ADD MAPPING
    FOR word, hword_part, hword
	WITH portuguese_stem
;
----------
[CREATE_TABLE] CREATE TABLE plt1_e_p1 PARTITION OF plt1_e FOR VALUES IN ('0000', '0003', '0004', '0010')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE test_part_a_1 ADD PRIMARY KEY (a)
;
----------
[CREATE_TABLE] create table parted_uniq_detach_test1 partition of parted_uniq_detach_test for values in (1)
;
----------
[CREATE_SCHEMA] -- Test generic object addressing/identification functions
CREATE SCHEMA addr_nsp
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30815 RENAME TO reind_con_toast
;
----------
[CREATE_CATALOG] CREATE DATABASE "postgres" WITH TEMPLATE = template0 OID = 5 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[BLOCK] do $$
declare x var_record_nn;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[CREATE_TABLE] CREATE TABLE parted (
	a text,
	b int NOT NULL DEFAULT 0,
	CONSTRAINT check_a CHECK (length(a) > 0)
) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tbl2 DROP COLUMN val3
;
----------
[CREATE_PROG_OBJ] -- we used to get confused by a single output argument that is composite
CREATE PROCEDURE ptest4c(INOUT comp int8_tbl)
LANGUAGE SQL
AS $$
SELECT ROW(1, 2);
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float8_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30764_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_CATALOG] create database abc owner = zyc
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_largeobject_metadata
;
----------
[DROP_TABLE] drop table agg_hash_1
;
----------
[CREATE_TABLE] CREATE TABLE prt2_n_p2 PARTITION OF prt2_n FOR VALUES IN ('0001', '0005', '0002', '0009', '0008', '0011')
;
----------
[CREATE_SEQUENCE] --
-- Check constraints on INSERT
--

CREATE SEQUENCE INSERT_SEQ
;
----------
[CREATE_TABLE] -- Test manipulation of temp schema's placement in search path

create table public.whereami (f1 text)
;
