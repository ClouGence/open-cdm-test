ALTER OPERATOR >= (issn13, issn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
DROP TABLE num_exp_sqrt
;
DROP SUBSCRIPTION testsub1
;
CREATE VIEW rw_view2 AS
  SELECT * FROM rw_view1 WHERE a > 0 WITH LOCAL CHECK OPTION
;
ALTER INDEX pg_toast.pg_toast_28283_index RENAME TO reind_con_toast_idx
;
-- Cleanup
DROP SCHEMA foreign_schema CASCADE
;
CREATE OPERATOR >= (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_ge,
	COMMUTATOR = '<=', NEGATOR = '<',
	RESTRICT = scalargtsel, JOIN = scalargtjoinsel
)
;
CREATE FUNCTION test_stat_func2() RETURNS VOID LANGUAGE plpgsql AS
      $$BEGIN PERFORM pg_sleep(10e-6); END;$$
;
CREATE TABLE beta_neg PARTITION OF beta FOR VALUES FROM (-10.0) TO (0) PARTITION BY RANGE (b)
;
CREATE TABLE rls_t1 (a int, b int, c int)
;
-- 126
TRUNCATE tru_ftable_parent
;
ALTER DATABASE split_db_probe WITH ALLOW_CONNECTIONS false CONNECTION LIMIT 2 IS_TEMPLATE false
;
CREATE TABLE "public"."part_rev" (
    "b" integer,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "a" integer
)
;
-- ok
ALTER TABLE mvtest_v RENAME COLUMN i TO x
;
-- Use of row type in an expression is defended differently
create table at_tab2 (x int, y text, check((x,y)::at_tab1 = (1,'42')::at_tab1))
;
/* src/test/modules/test_extensions/test_ext9--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- check handling of types as extension members
create type varbitrange as range (subtype = varbit)
;
CREATE TABLE idxpart_two (col2 INT)
;
ANALYZE pg_catalog.pg_attribute
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_aggregate
;
-- triggers
CREATE FUNCTION tf1() RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN NULL;
END $$
;
CREATE TABLE trgfire3 (LIKE trgfire)
;
create trigger tg after insert on parent
  for each row execute procedure trig_nothing()
;
-- multi-arg polymorphic
CREATE AGGREGATE mysum2(anyelement,anyelement) (SFUNC = sum3,
  STYPE = anyelement, INITCOND = '0')
;
ANALYZE base_tbl1
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn13,
	RIGHTARG = issn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
ALTER TABLE test_add_column
	ADD COLUMN c2 integer, -- fail because c2 already exists
	ADD COLUMN c3 integer
;
comment on table event_trigger_fire1 is 'here is a comment'
;
create index tstidx on ltreetest using hash (t)
;
CREATE TABLE past_part PARTITION OF past_parted FOR VALUES IN (1)
;
ALTER TABLE test_replica_identity5 REPLICA IDENTITY USING INDEX test_replica_identity5_a_b_key
;
DROP TABLESPACE regress_tblspace_renamed
;
-- Create an index, and then attempt to force a nested loop with inner index
-- scan so that we can see parameter-related information. Also, let's try
-- actually running the query, but try to suppress potentially variable output.
CREATE INDEX ON vegetables (id)
;
CREATE TABLE "public"."fk_partitioned_fk_2_2" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
VACUUM (ANALYZE) pg_catalog.pg_shdepend
;
VACUUM information_schema.sql_languages
;
-- partitioning
CREATE TABLE test20 (a int, b text COLLATE case_insensitive) PARTITION BY LIST (b)
;
create view v4 as select * from v1
;
create function f1(x anyrange) returns anyarray as $$
begin
  return array[lower(x), upper(x)];
end$$ language plpgsql
;
ALTER TYPE test_type ALTER ATTRIBUTE b SET DATA TYPE varchar
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (generate_series(4, 6))
;
CREATE TABLE tableam_parted_a_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('a')
;
-- ok
drop table perm_parted cascade
;
CREATE TABLE "public"."dropcolumnchild" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "e" integer,
    "........pg.dropped.4........" INTEGER /* dummy */
)
;
--
-- pgp_pub_encrypt(data, key)
--
CREATE FUNCTION pgp_pub_encrypt(text, bytea)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_pub_encrypt_text'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS ("a=b" 'true')
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_config
;
--
-- FLOAT4
--

CREATE TABLE FLOAT4_TBL (f1  float4)
;
/*
 * PostgreSQL System Views
 *
 * Copyright (c) 1996-2022, PostgreSQL Global Development Group
 *
 * src/backend/catalog/system_views.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_ts_dict
;
------------------------------------------------------------------------------------------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (issn13, issn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[DROP_TABLE] DROP TABLE num_exp_sqrt
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION testsub1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS
  SELECT * FROM rw_view1 WHERE a > 0 WITH LOCAL CHECK OPTION
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28283_index RENAME TO reind_con_toast_idx
;
----------
[DROP_SCHEMA] -- Cleanup
DROP SCHEMA foreign_schema CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_ge,
	COMMUTATOR = '<=', NEGATOR = '<',
	RESTRICT = scalargtsel, JOIN = scalargtjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_stat_func2() RETURNS VOID LANGUAGE plpgsql AS
      $$BEGIN PERFORM pg_sleep(10e-6); END;$$
;
----------
[CREATE_TABLE] CREATE TABLE beta_neg PARTITION OF beta FOR VALUES FROM (-10.0) TO (0) PARTITION BY RANGE (b)
;
----------
[CREATE_TABLE] CREATE TABLE rls_t1 (a int, b int, c int)
;
----------
[TRUNCATE_TABLE] -- 126
TRUNCATE tru_ftable_parent
;
----------
[ALTER_CATALOG] ALTER DATABASE split_db_probe WITH ALLOW_CONNECTIONS false CONNECTION LIMIT 2 IS_TEMPLATE false
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part_rev" (
    "b" integer,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "a" integer
)
;
----------
[RENAME_COLUMN] -- ok
ALTER TABLE mvtest_v RENAME COLUMN i TO x
;
----------
[CREATE_TABLE] -- Use of row type in an expression is defended differently
create table at_tab2 (x int, y text, check((x,y)::at_tab1 = (1,'42')::at_tab1))
;
----------
[CREATE_TYPE] /* src/test/modules/test_extensions/test_ext9--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- check handling of types as extension members
create type varbitrange as range (subtype = varbit)
;
----------
[CREATE_TABLE] CREATE TABLE idxpart_two (col2 INT)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_attribute
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_aggregate
;
----------
[CREATE_PROG_OBJ] -- triggers
CREATE FUNCTION tf1() RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN NULL;
END $$
;
----------
[CREATE_TABLE] CREATE TABLE trgfire3 (LIKE trgfire)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg after insert on parent
  for each row execute procedure trig_nothing()
;
----------
[CREATE_PROG_OBJ] -- multi-arg polymorphic
CREATE AGGREGATE mysum2(anyelement,anyelement) (SFUNC = sum3,
  STYPE = anyelement, INITCOND = '0')
;
----------
[ADMIN_TABLE] ANALYZE base_tbl1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn13,
	RIGHTARG = issn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN c2 integer, -- fail because c2 already exists
	ADD COLUMN c3 integer
;
----------
[COMMENT_TABLE] comment on table event_trigger_fire1 is 'here is a comment'
;
----------
[ADD_INDEX] create index tstidx on ltreetest using hash (t)
;
----------
[CREATE_TABLE] CREATE TABLE past_part PARTITION OF past_parted FOR VALUES IN (1)
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity5 REPLICA IDENTITY USING INDEX test_replica_identity5_a_b_key
;
----------
[DROP_TABLESPACE] DROP TABLESPACE regress_tblspace_renamed
;
----------
[ADD_INDEX] -- Create an index, and then attempt to force a nested loop with inner index
-- scan so that we can see parameter-related information. Also, let's try
-- actually running the query, but try to suppress potentially variable output.
CREATE INDEX ON vegetables (id)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_2_2" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_shdepend
;
----------
[ADMIN_TABLE] VACUUM information_schema.sql_languages
;
----------
[CREATE_TABLE] -- partitioning
CREATE TABLE test20 (a int, b text COLLATE case_insensitive) PARTITION BY LIST (b)
;
----------
[CREATE_VIEW(SELECT)] create view v4 as select * from v1
;
----------
[CREATE_PROG_OBJ] create function f1(x anyrange) returns anyarray as $$
begin
  return array[lower(x), upper(x)];
end$$ language plpgsql
;
----------
[ALTER_TYPE] ALTER TYPE test_type ALTER ATTRIBUTE b SET DATA TYPE varchar
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (generate_series(4, 6))
;
----------
[CREATE_TABLE] CREATE TABLE tableam_parted_a_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('a')
;
----------
[DROP_TABLE] -- ok
drop table perm_parted cascade
;
----------
[CREATE_TABLE] CREATE TABLE "public"."dropcolumnchild" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "e" integer,
    "........pg.dropped.4........" INTEGER /* dummy */
)
;
----------
[CREATE_PROG_OBJ] --
-- pgp_pub_encrypt(data, key)
--
CREATE FUNCTION pgp_pub_encrypt(text, bytea)
RETURNS bytea
AS '$libdir/pgcrypto', 'pgp_pub_encrypt_text'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS ("a=b" 'true')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_config
;
----------
[CREATE_TABLE] --
-- FLOAT4
--

CREATE TABLE FLOAT4_TBL (f1  float4)
;
----------
[CREATE_VIEW(SELECT)] /*
 * PostgreSQL System Views
 *
 * Copyright (c) 1996-2022, PostgreSQL Global Development Group
 *
 * src/backend/catalog/system_views.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE VIEW pg_roles AS
    SELECT
        rolname,
        rolsuper,
        rolinherit,
        rolcreaterole,
        rolcreatedb,
        rolcanlogin,
        rolreplication,
        rolconnlimit,
        '********'::text as rolpassword,
        rolvaliduntil,
        rolbypassrls,
        setconfig as rolconfig,
        pg_authid.oid
    FROM pg_authid LEFT JOIN pg_db_role_setting s
    ON (pg_authid.oid = setrole AND setdatabase = 0)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_ts_dict
;
