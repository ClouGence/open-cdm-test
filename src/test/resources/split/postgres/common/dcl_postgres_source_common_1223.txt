--
-- Note: the built-in conversions are exercised in opr_sanity.sql,
-- so there's no need to do that here.
--
--
-- return to the super user
--
RESET SESSION AUTHORIZATION
;
RESET debug_parallel_query
;
SET password_encryption = 'novalue'
;
ALTER ROLE regress_priv_user1 NOINHERIT
;
EXECUTE dumpDomain('30230')
;
-- fails: SET ROLE did not help

SET SESSION AUTHORIZATION regress_priv_group2
;
CREATE USER MAPPING FOR current_user SERVER s7
;
EXECUTE dumpFunc('16750')
;
LOCK TABLE "public"."part_rev" IN ACCESS SHARE MODE
;
DEALLOCATE pgsql_fdw_prep_117
;
------------------------------------------------------------------------------------------
[SWITCH_USER] --
-- Note: the built-in conversions are exercised in opr_sanity.sql,
-- so there's no need to do that here.
--
--
-- return to the super user
--
RESET SESSION AUTHORIZATION
;
----------
[SESSION_SETTING_WRITE] RESET debug_parallel_query
;
----------
[SESSION_SETTING_WRITE] SET password_encryption = 'novalue'
;
----------
[ALTER_USER] ALTER ROLE regress_priv_user1 NOINHERIT
;
----------
[UNSAFE] EXECUTE dumpDomain('30230')
;
----------
[SWITCH_USER] -- fails: SET ROLE did not help

SET SESSION AUTHORIZATION regress_priv_group2
;
----------
[GRANT] CREATE USER MAPPING FOR current_user SERVER s7
;
----------
[UNSAFE] EXECUTE dumpFunc('16750')
;
----------
[SESSION_LOCK] LOCK TABLE "public"."part_rev" IN ACCESS SHARE MODE
;
----------
[UNSAFE] DEALLOCATE pgsql_fdw_prep_117
;
