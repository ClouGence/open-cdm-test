do $$
BEGIN
  EXECUTE 'CREATE COLLATION test0 (provider = icu, locale = ' ||
          quote_literal((SELECT CASE WHEN datlocprovider='i' THEN datlocale ELSE datcollate END FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
CREATE TABLE prt2_adv_extra PARTITION OF prt2_adv FOR VALUES FROM (500) TO (MAXVALUE)
;
DROP TABLE temp_parted
;
CREATE FUNCTION bad_cmp (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT
			CASE WHEN ($1 = 768 AND $2 = 769) OR
					  ($1 = 769 AND $2 = 768) THEN 0
				 WHEN $1 < $2 THEN -1
				 WHEN $1 > $2 THEN  1
				 ELSE 0
			END;
	$$
;
ALTER TABLE test_prepared1 ADD COLUMN data text
;
CREATE FUNCTION bt_page_items(IN page bytea,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER OPERATOR <= (isbn, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE INDEX ON brin_hot_2 USING brin (b) WHERE a = 2
;
-- not supported
ALTER TABLE regtest_table_2 INHERIT regtest_table
;
CREATE INDEX quad_box_tbl_idx ON quad_box_tbl USING spgist(b)
;
/*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
DROP TABLE brin_test_inet
;
CREATE CAST (integer AS "public"."casttesttype") WITH FUNCTION "public"."int4_casttesttype"(integer) AS IMPLICIT
;
CREATE FUNCTION regress_seg_schema.exfun(i int) RETURNS int AS $$
BEGIN
  CREATE EXTENSION seg VERSION '1.2';

  CREATE FUNCTION regress_seg_schema.compare(oid, regclass) RETURNS boolean AS
  'BEGIN RAISE EXCEPTION ''overloaded compare() called by %'', current_user; END;' LANGUAGE plpgsql;

  CREATE OPERATOR = (LEFTARG = oid, RIGHTARG = regclass, PROCEDURE = regress_seg_schema.compare);

  ALTER EXTENSION seg UPDATE TO '1.3';

  RETURN i;
END; $$ LANGUAGE plpgsql
;
ALTER TABLE pg_toast.pg_toast_30658 RENAME TO reind_con_toast
;
create function self_ref_trigger_del_func()
  returns trigger language plpgsql as
$$
begin
  if old.parent is not null then
    update self_ref_trigger set nchildren = nchildren - 1
      where id = old.parent;
  end if;
  return old;
end;
$$
;
DROP TABLE IF EXISTS split_cursor_next.items
;
DROP TABLE enumtest_parent
;
DROP TABLE regress_tblspace_test_tbl
;
-- ERROR
ALTER FOREIGN TABLE foreign_schema.ft1 SET TABLESPACE ts
;
CREATE TABLE atestp2 (fx int, fy int)
;
CREATE TABLE tidscan(id integer)
;
ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk MATCH SIMPLE
  ON DELETE SET NULL ON UPDATE SET NULL
;
alter table parent drop a
;
ALTER INDEX dummy_test_idx SET (option_enum = true)
;
drop table def_test
;
create table matest1() inherits (matest0)
;
CREATE TABLESPACE ts1 LOCATION '/tmp/2T6CBi5Hzk'
;
ALTER TABLE FKTABLE ADD CONSTRAINT fkdi FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION DEFERRABLE INITIALLY IMMEDIATE
;
DROP EVENT TRIGGER on_login_trigger
;
VACUUM (PROCESS_TOAST FALSE) information_schema.sql_parts
;
-- Remember the TO bound is exclusive
ALTER TABLE range_parted ATTACH PARTITION part1 FOR VALUES FROM (1, 1) TO (1, 10)
;
create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "POSIX") to ('g')
;
CREATE VIEW mvtest_tvv AS SELECT sum(totamt) AS grandtot FROM mvtest_tv
;
ALTER FUNCTION gbt_int4_penalty(internal, internal, internal) PARALLEL SAFE
;
DROP PUBLICATION testpub_forschema_fortable
;
CREATE TABLE a8 ()
;
-- Restore plt1_adv_p1
ALTER TABLE plt1_adv ATTACH PARTITION plt1_adv_p1 FOR VALUES IN ('0001', '0003')
;
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT queryid bigint,
    OUT query text,
    OUT calls int8,
    OUT total_time float8,
    OUT min_time float8,
    OUT max_time float8,
    OUT mean_time float8,
    OUT stddev_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_3'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
alter table anothertab
  add unique(f1,f4)
;
CREATE TABLE "public"."prt4_n_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE UNIQUE INDEX num_exp_mul_idx ON num_exp_mul (id1, id2)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16006 host=/tmp/B3ysgEyCZr dbname=postgres' PUBLICATION tap_pub
;
--
-- init pgcrypto
--

CREATE EXTENSION pgcrypto
;
CREATE FOREIGN TABLE ft2 () INHERITS (fd_pt1)
  SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
-- Duplicate column names
CREATE INDEX concur_reindex_ind4 ON concur_reindex_tab(c1, c1, c2)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_config_map
;
DROP TABLE test_stat_oid
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c8 OPTIONS (SET p2 'V2', DROP p1)
;
ALTER TABLE r1 NO FORCE ROW LEVEL SECURITY
;
------------------------------------------------------------------------------------------
[BLOCK] do $$
BEGIN
  EXECUTE 'CREATE COLLATION test0 (provider = icu, locale = ' ||
          quote_literal((SELECT CASE WHEN datlocprovider='i' THEN datlocale ELSE datcollate END FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
----------
[CREATE_TABLE] CREATE TABLE prt2_adv_extra PARTITION OF prt2_adv FOR VALUES FROM (500) TO (MAXVALUE)
;
----------
[DROP_TABLE] DROP TABLE temp_parted
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bad_cmp (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT
			CASE WHEN ($1 = 768 AND $2 = 769) OR
					  ($1 = 769 AND $2 = 768) THEN 0
				 WHEN $1 < $2 THEN -1
				 WHEN $1 > $2 THEN  1
				 ELSE 0
			END;
	$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_prepared1 ADD COLUMN data text
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_page_items(IN page bytea,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (isbn, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[ADD_INDEX] CREATE INDEX ON brin_hot_2 USING brin (b) WHERE a = 2
;
----------
[ALTER_TABLE] -- not supported
ALTER TABLE regtest_table_2 INHERIT regtest_table
;
----------
[ADD_INDEX] CREATE INDEX quad_box_tbl_idx ON quad_box_tbl USING spgist(b)
;
----------
[CREATE_POLICY] /*
 * text search configuration for indonesian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * indonesian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY indonesian_stem
	(TEMPLATE = snowball, Language = indonesian )
;
----------
[DROP_TABLE] DROP TABLE brin_test_inet
;
----------
[CREATE_PROG_OBJ] CREATE CAST (integer AS "public"."casttesttype") WITH FUNCTION "public"."int4_casttesttype"(integer) AS IMPLICIT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regress_seg_schema.exfun(i int) RETURNS int AS $$
BEGIN
  CREATE EXTENSION seg VERSION '1.2';

  CREATE FUNCTION regress_seg_schema.compare(oid, regclass) RETURNS boolean AS
  'BEGIN RAISE EXCEPTION ''overloaded compare() called by %'', current_user; END;' LANGUAGE plpgsql;

  CREATE OPERATOR = (LEFTARG = oid, RIGHTARG = regclass, PROCEDURE = regress_seg_schema.compare);

  ALTER EXTENSION seg UPDATE TO '1.3';

  RETURN i;
END; $$ LANGUAGE plpgsql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30658 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] create function self_ref_trigger_del_func()
  returns trigger language plpgsql as
$$
begin
  if old.parent is not null then
    update self_ref_trigger set nchildren = nchildren - 1
      where id = old.parent;
  end if;
  return old;
end;
$$
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS split_cursor_next.items
;
----------
[DROP_TABLE] DROP TABLE enumtest_parent
;
----------
[DROP_TABLE] DROP TABLE regress_tblspace_test_tbl
;
----------
[ALTER_TABLE] -- ERROR
ALTER FOREIGN TABLE foreign_schema.ft1 SET TABLESPACE ts
;
----------
[CREATE_TABLE] CREATE TABLE atestp2 (fx int, fy int)
;
----------
[CREATE_TABLE] CREATE TABLE tidscan(id integer)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk MATCH SIMPLE
  ON DELETE SET NULL ON UPDATE SET NULL
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table parent drop a
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_enum = true)
;
----------
[DROP_TABLE] drop table def_test
;
----------
[CREATE_TABLE] create table matest1() inherits (matest0)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/tmp/2T6CBi5Hzk'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD CONSTRAINT fkdi FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION DEFERRABLE INITIALLY IMMEDIATE
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER on_login_trigger
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) information_schema.sql_parts
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Remember the TO bound is exclusive
ALTER TABLE range_parted ATTACH PARTITION part1 FOR VALUES FROM (1, 1) TO (1, 10)
;
----------
[CREATE_TABLE] create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "POSIX") to ('g')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mvtest_tvv AS SELECT sum(totamt) AS grandtot FROM mvtest_tv
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int4_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_forschema_fortable
;
----------
[CREATE_TABLE] CREATE TABLE a8 ()
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Restore plt1_adv_p1
ALTER TABLE plt1_adv ATTACH PARTITION plt1_adv_p1 FOR VALUES IN ('0001', '0003')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT queryid bigint,
    OUT query text,
    OUT calls int8,
    OUT total_time float8,
    OUT min_time float8,
    OUT max_time float8,
    OUT mean_time float8,
    OUT stddev_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT blk_read_time float8,
    OUT blk_write_time float8
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_3'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table anothertab
  add unique(f1,f4)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt4_n_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX num_exp_mul_idx ON num_exp_mul (id1, id2)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16006 host=/tmp/B3ysgEyCZr dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_LIBRARY] --
-- init pgcrypto
--

CREATE EXTENSION pgcrypto
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft2 () INHERITS (fd_pt1)
  SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[ADD_INDEX] -- Duplicate column names
CREATE INDEX concur_reindex_ind4 ON concur_reindex_tab(c1, c1, c2)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_ts_config_map
;
----------
[DROP_TABLE] DROP TABLE test_stat_oid
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c8 OPTIONS (SET p2 'V2', DROP p1)
;
----------
[ALTER_TABLE] ALTER TABLE r1 NO FORCE ROW LEVEL SECURITY
;
