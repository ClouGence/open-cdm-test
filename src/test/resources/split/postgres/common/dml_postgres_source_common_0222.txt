INSERT INTO PKTABLE VALUES (2, 'Test2')
;
UPDATE "S 1"."T 1" SET c3 = 'bar'::text WHERE (("C 1" = 1200))
;
insert into t values (1, 2)
;
INSERT INTO public.ths_p3 VALUES (9, 649)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
;
insert into hs2 values (12)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16382', relminmxid = '3'
WHERE oid = '"public"."xy_1"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('2', 972)
;
-- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '479', datminmxid = '1'
WHERE datname = 'regression\"\	 !"#$%&''()*+,-\\"\\\'
;
-- Try to delete something that should not have a cascade
DELETE FROM PKTABLE where ptest1=2
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1544', relminmxid = '1'
WHERE oid = '"public"."hash_f8_heap"'::pg_catalog.regclass
;
insert into �ͪ�ߩ�Ѧ��듾� values('��ēǻ��͓����Γ�ד�����', '���Z01���')
;
update test_jsonb_subscript set test_json[5] = '1'
;
insert into rtest_view3 select * from rtest_vview1 where a < 7
;
INSERT INTO public.ths_p3 VALUES (5, 595)
;
INSERT INTO num_exp_sub VALUES (7,3,'-83028489.31')
;
INSERT INTO test_part_b VALUES (2, 3)
;
INSERT INTO test1 VALUES ($1, $2)
;
INSERT INTO tab1 VALUES (1), (0)
;
INSERT INTO ndistinct (a, b, c, filler1)
     SELECT i/100, i/100, i/100, (i/100) || ' dollars and zero cents'
       FROM generate_series(1,1000) s(i)
;
INSERT INTO clstr_tst (b, c) VALUES (8, 'ocho')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17902', relminmxid = '3'
WHERE oid = '"public"."dropcolumnexists"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (5, 275)
;
DELETE FROM foo_parted
  RETURNING old.tableoid::regclass, old.ctid, old.*,
            new.tableoid::regclass, new.ctid, new.*, *
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16838', relminmxid = '3'
WHERE oid = '"public"."pht2_p3"'::pg_catalog.regclass
;
-- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.1........'
  AND attrelid = '"public"."gtest28a"'::pg_catalog.regclass
;
INSERT INTO dump_test.test_third_table VALUES (123, DEFAULT, 456)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '738', relminmxid = '1'
WHERE oid = '"public"."tbl"'::pg_catalog.regclass
;
INSERT INTO r2 VALUES (10), (20)
;
INSERT INTO public.ths_p1 VALUES (4, 564)
;
DELETE from test_visibility
;
DELETE FROM no_index_cleanup WHERE i < 45
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3089', relminmxid = '1'
WHERE oid = '"public"."ramp"'::pg_catalog.regclass
;
INSERT INTO clstr_tst (b, c) VALUES (21, 'veintiuno')
;
insert into phv_boolpart values (true, 'yes'), (false, 'no'), (null, 'unknown')
;
INSERT INTO timetzcmp (r_id,a) SELECT 15,count(*) FROM timetztmp WHERE a >  '07:46:45 GMT+2'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9989', relminmxid = '1'
WHERE oid = '"public"."rule_and_refint_t2"'::pg_catalog.regclass
;
INSERT INTO num_exp_sub VALUES (1,1,'0')
;
INSERT INTO public.ths_p3 VALUES (5, 575)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17377', relminmxid = '3'
WHERE oid = '"public"."boolvalues"'::pg_catalog.regclass
;
INSERT INTO T VALUES (1), (2)
;
INSERT INTO public.tht VALUES ('6', 196)
;
INSERT INTO public.ths_p2 VALUES (7, 67)
;
INSERT INTO public.tplain VALUES ('4', 4)
;
INSERT INTO ac_test VALUES (4)
;
insert into PField values ('PF1_1', 'Wallslots first floor')
;
DELETE FROM public.mvtest_mv_foo mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_14.pg_temp_27052_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
DELETE FROM savepoints WHERE a=1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15470', relminmxid = '3'
WHERE oid = '"public"."nv_parent"'::pg_catalog.regclass
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted_def1"'::pg_catalog.regclass
  AND attname = v.dname
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO PKTABLE VALUES (2, 'Test2')
;
----------
[UPDATE] UPDATE "S 1"."T 1" SET c3 = 'bar'::text WHERE (("C 1" = 1200))
;
----------
[INSERT] insert into t values (1, 2)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 649)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
;
----------
[INSERT] insert into hs2 values (12)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16382', relminmxid = '3'
WHERE oid = '"public"."xy_1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('2', 972)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '479', datminmxid = '1'
WHERE datname = 'regression\"\	 !"#$%&''()*+,-\\"\\\'
;
----------
[DELETE] -- Try to delete something that should not have a cascade
DELETE FROM PKTABLE where ptest1=2
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1544', relminmxid = '1'
WHERE oid = '"public"."hash_f8_heap"'::pg_catalog.regclass
;
----------
[INSERT] insert into �ͪ�ߩ�Ѧ��듾� values('��ēǻ��͓����Γ�ד�����', '���Z01���')
;
----------
[UPDATE] update test_jsonb_subscript set test_json[5] = '1'
;
----------
[INSERT|SELECT] insert into rtest_view3 select * from rtest_vview1 where a < 7
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 595)
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (7,3,'-83028489.31')
;
----------
[INSERT] INSERT INTO test_part_b VALUES (2, 3)
;
----------
[INSERT] INSERT INTO test1 VALUES ($1, $2)
;
----------
[INSERT] INSERT INTO tab1 VALUES (1), (0)
;
----------
[INSERT|SELECT] INSERT INTO ndistinct (a, b, c, filler1)
     SELECT i/100, i/100, i/100, (i/100) || ' dollars and zero cents'
       FROM generate_series(1,1000) s(i)
;
----------
[INSERT|SELECT] INSERT INTO clstr_tst (b, c) VALUES (8, 'ocho')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '17902', relminmxid = '3'
WHERE oid = '"public"."dropcolumnexists"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 275)
;
----------
[DELETE] DELETE FROM foo_parted
  RETURNING old.tableoid::regclass, old.ctid, old.*,
            new.tableoid::regclass, new.ctid, new.*, *
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16838', relminmxid = '3'
WHERE oid = '"public"."pht2_p3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.1........'
  AND attrelid = '"public"."gtest28a"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO dump_test.test_third_table VALUES (123, DEFAULT, 456)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '738', relminmxid = '1'
WHERE oid = '"public"."tbl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO r2 VALUES (10), (20)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 564)
;
----------
[DELETE] DELETE from test_visibility
;
----------
[DELETE] DELETE FROM no_index_cleanup WHERE i < 45
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3089', relminmxid = '1'
WHERE oid = '"public"."ramp"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO clstr_tst (b, c) VALUES (21, 'veintiuno')
;
----------
[INSERT] insert into phv_boolpart values (true, 'yes'), (false, 'no'), (null, 'unknown')
;
----------
[INSERT|SELECT] INSERT INTO timetzcmp (r_id,a) SELECT 15,count(*) FROM timetztmp WHERE a >  '07:46:45 GMT+2'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9989', relminmxid = '1'
WHERE oid = '"public"."rule_and_refint_t2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (1,1,'0')
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 575)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17377', relminmxid = '3'
WHERE oid = '"public"."boolvalues"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO T VALUES (1), (2)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 196)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 67)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 4)
;
----------
[INSERT] INSERT INTO ac_test VALUES (4)
;
----------
[INSERT] insert into PField values ('PF1_1', 'Wallslots first floor')
;
----------
[DELETE|SELECT] DELETE FROM public.mvtest_mv_foo mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_14.pg_temp_27052_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[DELETE] DELETE FROM savepoints WHERE a=1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15470', relminmxid = '3'
WHERE oid = '"public"."nv_parent"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted_def1"'::pg_catalog.regclass
  AND attname = v.dname
;
