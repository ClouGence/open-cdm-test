DROP DATABASE test_db
;
CREATE SUBSCRIPTION testsub2 CONNECTION 'port=25080 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION testpub2
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
CREATE TEMP TABLE pg_temp_32.pg_temp_28302_2 (tid pg_catalog.tid)
;
DROP VIEW is_json_view
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtestxx_4" INHERIT "public"."gtest1"
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_init_privs
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "hword" WITH "english_stem"
;
/* src/test/modules/test_lfind/test_lfind--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_lfind32()
	RETURNS pg_catalog.void
	AS '$libdir/test_lfind' LANGUAGE C
;
COMMENT ON LARGE OBJECT 33914 IS 'I Wandered Lonely as a Cloud'
;
create aggregate first_el_agg_any(anyelement) (
  SFUNC = first_el_transfn,
  STYPE = anyarray,
  FINALFUNC = first_el
)
;
create index test_storage_idx on test_storage (b, a)
;
/*
 * text search configuration for armenian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * armenian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY armenian_stem
	(TEMPLATE = snowball, Language = armenian )
;
create table parted_conflict_test_4 partition of parted_conflict_test for values in (5) partition by list (a)
;
-- fails
ALTER TABLE pitest2 ALTER COLUMN f3 SET GENERATED BY DEFAULT SET INCREMENT BY 2 SET START WITH 1000 RESTART
;
CREATE TEXT SEARCH CONFIGURATION split_prog.english_copy (COPY = pg_catalog.english)
;
ALTER INDEX tbspace_reindex_part_index_0 ATTACH PARTITION tbspace_reindex_part_index_0_2
;
--
-- hash_metapage_info()
--
CREATE FUNCTION hash_metapage_info(IN page bytea,
    OUT magic int8,
    OUT version int8,
    OUT ntuples double precision,
    OUT ffactor int4,
    OUT bsize int4,
    OUT bmsize int4,
    OUT bmshift int4,
    OUT maxbucket int8,
    OUT highmask int8,
    OUT lowmask int8,
    OUT ovflpoint int8,
    OUT firstfree int8,
    OUT nmaps int8,
    OUT procid oid,
    OUT spares int8[],
    OUT mapp int8[])
AS '$libdir/pageinspect', 'hash_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP FUNCTION base_tbl_trig_fn()
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
CREATE TABLE "public"."prt1_n_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
CREATE FUNCTION "public"."logging_msfunc_nonstrict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE
    AS $_$ SELECT COALESCE($1, '') || '+' || quote_nullable($2) $_$
;
ALTER TEXT SEARCH CONFIGURATION lithuanian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH lithuanian_stem
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn13,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
ALTER TABLE pg_toast.pg_toast_30505 RENAME TO reind_con_toast
;
SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'classified'
;
CREATE INDEX strtest_n_idx ON strtest (n)
;
CREATE TABLE pagg_tab_para_p1 PARTITION OF pagg_tab_para FOR VALUES FROM (0) TO (12)
;
ALTER PUBLICATION tap_pub_tmp RENAME TO pub_empty
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub_mix_1, pub_mix_2
;
ANALYZE pg_temp_10.pg_temp_26364_2
;
CREATE TABLE sch1.tab1_parent (a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
CREATE FUNCTION gbt_bytea_same(gbtreekey_var, gbtreekey_var, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- ************************************************************
-- * Support function to clear out the slotlink field if
-- * it still points to specific slot
-- ************************************************************
create function tg_slotlink_unset(bpchar, bpchar)
returns integer as '
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    if mytype = ''PS'' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''WS'' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update WSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''IF'' then
        select into rec * from IFace where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update IFace set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''HS'' then
        select into rec * from HSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update HSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''PH'' then
        select into rec * from PHone where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PHone set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
end;
' language plpgsql
;
ALTER FUNCTION isn_weak() STABLE PARALLEL SAFE
;
-- Table that is not part of any partition tree is not listed.
CREATE TABLE ptif_normal_table(a int)
;
ALTER TABLE part_7 DROP CONSTRAINT check_a
;
CREATE FUNCTION getrngfunc1(int) RETURNS int AS 'SELECT $1;' LANGUAGE SQL
;
CREATE FUNCTION joinview_upd_trig_fn() RETURNS trigger
LANGUAGE plpgsql AS
$$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING new.f1, new.f4 INTO new.f1, new.f4;  -- should fail
  RETURN NEW;
END;
$$
;
ALTER TABLE ffk1 ADD FOREIGN KEY (a) REFERENCES pk
;
CREATE TRIGGER "tg_pslot_biu" BEFORE INSERT OR UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pslot_biu"()
;
CREATE TEMP TABLE disttable (f1 integer)
;
CREATE DATABASE moveme_db TABLESPACE source_ts STRATEGY=FILE_COPY
;
ALTER TABLE pg_temp_13.pg_temp_25047_2 ADD COLUMN newdata pg_temp_13.pg_temp_25047
;
-- Validate the constraint
ALTER TABLE fk_partitioned_fk VALIDATE CONSTRAINT fk_partitioned_fk_a_b_fkey
;
CREATE TYPE regress_pg_dump_schema.test_type AS (col1 int)
;
CREATE TRIGGER base_tbl_trig BEFORE INSERT OR UPDATE ON base_tbl
  FOR EACH ROW EXECUTE PROCEDURE base_tbl_trig_fn()
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub4
;
CREATE OPERATOR #*# (
   leftarg = SETOF int8,
   procedure = factorial
)
;
CREATE TABLE like_fkey_table (
  LIKE fkey_table
  INCLUDING DEFAULTS
  INCLUDING INDEXES
  INCLUDING STORAGE
)
;
------------------------------------------------------------------------------------------
[DROP_CATALOG] DROP DATABASE test_db
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub2 CONNECTION 'port=25080 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION testpub2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_32.pg_temp_28302_2 (tid pg_catalog.tid)
;
----------
[DROP_VIEW] DROP VIEW is_json_view
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."gtestxx_4" INHERIT "public"."gtest1"
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_init_privs
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "hword" WITH "english_stem"
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_lfind/test_lfind--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_lfind32()
	RETURNS pg_catalog.void
	AS '$libdir/test_lfind' LANGUAGE C
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 33914 IS 'I Wandered Lonely as a Cloud'
;
----------
[CREATE_PROG_OBJ] create aggregate first_el_agg_any(anyelement) (
  SFUNC = first_el_transfn,
  STYPE = anyarray,
  FINALFUNC = first_el
)
;
----------
[ADD_INDEX] create index test_storage_idx on test_storage (b, a)
;
----------
[CREATE_POLICY] /*
 * text search configuration for armenian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * armenian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY armenian_stem
	(TEMPLATE = snowball, Language = armenian )
;
----------
[CREATE_TABLE] create table parted_conflict_test_4 partition of parted_conflict_test for values in (5) partition by list (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails
ALTER TABLE pitest2 ALTER COLUMN f3 SET GENERATED BY DEFAULT SET INCREMENT BY 2 SET START WITH 1000 RESTART
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION split_prog.english_copy (COPY = pg_catalog.english)
;
----------
[ALTER_INDEX] ALTER INDEX tbspace_reindex_part_index_0 ATTACH PARTITION tbspace_reindex_part_index_0_2
;
----------
[CREATE_PROG_OBJ] --
-- hash_metapage_info()
--
CREATE FUNCTION hash_metapage_info(IN page bytea,
    OUT magic int8,
    OUT version int8,
    OUT ntuples double precision,
    OUT ffactor int4,
    OUT bsize int4,
    OUT bmsize int4,
    OUT bmshift int4,
    OUT maxbucket int8,
    OUT highmask int8,
    OUT lowmask int8,
    OUT ovflpoint int8,
    OUT firstfree int8,
    OUT nmaps int8,
    OUT procid oid,
    OUT spares int8[],
    OUT mapp int8[])
AS '$libdir/pageinspect', 'hash_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] DROP FUNCTION base_tbl_trig_fn()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = ismn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_n_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."logging_msfunc_nonstrict"("text", "anyelement") RETURNS "text"
    LANGUAGE "sql" IMMUTABLE
    AS $_$ SELECT COALESCE($1, '') || '+' || quote_nullable($2) $_$
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION lithuanian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH lithuanian_stem
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn13,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30505 RENAME TO reind_con_toast
;
----------
[UNKNOWN] SECURITY LABEL ON ROLE regress_dummy_seclabel_user3 IS 'classified'
;
----------
[ADD_INDEX] CREATE INDEX strtest_n_idx ON strtest (n)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_para_p1 PARTITION OF pagg_tab_para FOR VALUES FROM (0) TO (12)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_tmp RENAME TO pub_empty
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub_mix_1, pub_mix_2
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26364_2
;
----------
[CREATE_TABLE] CREATE TABLE sch1.tab1_parent (a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bytea_same(gbtreekey_var, gbtreekey_var, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * Support function to clear out the slotlink field if
-- * it still points to specific slot
-- ************************************************************
create function tg_slotlink_unset(bpchar, bpchar)
returns integer as '
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    if mytype = ''PS'' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''WS'' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update WSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''IF'' then
        select into rec * from IFace where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update IFace set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''HS'' then
        select into rec * from HSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update HSlot set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''PH'' then
        select into rec * from PHone where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PHone set slotlink = '''' where slotname = myname;
	end if;
	return 0;
    end if;
end;
' language plpgsql
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION isn_weak() STABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Table that is not part of any partition tree is not listed.
CREATE TABLE ptif_normal_table(a int)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE part_7 DROP CONSTRAINT check_a
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION getrngfunc1(int) RETURNS int AS 'SELECT $1;' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION joinview_upd_trig_fn() RETURNS trigger
LANGUAGE plpgsql AS
$$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING new.f1, new.f4 INTO new.f1, new.f4;  -- should fail
  RETURN NEW;
END;
$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ffk1 ADD FOREIGN KEY (a) REFERENCES pk
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_pslot_biu" BEFORE INSERT OR UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_pslot_biu"()
;
----------
[CREATE_TABLE] CREATE TEMP TABLE disttable (f1 integer)
;
----------
[CREATE_CATALOG] CREATE DATABASE moveme_db TABLESPACE source_ts STRATEGY=FILE_COPY
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_25047_2 ADD COLUMN newdata pg_temp_13.pg_temp_25047
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- Validate the constraint
ALTER TABLE fk_partitioned_fk VALIDATE CONSTRAINT fk_partitioned_fk_a_b_fkey
;
----------
[CREATE_TYPE] CREATE TYPE regress_pg_dump_schema.test_type AS (col1 int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER base_tbl_trig BEFORE INSERT OR UPDATE ON base_tbl
  FOR EACH ROW EXECUTE PROCEDURE base_tbl_trig_fn()
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub4
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #*# (
   leftarg = SETOF int8,
   procedure = factorial
)
;
----------
[CREATE_TABLE] CREATE TABLE like_fkey_table (
  LIKE fkey_table
  INCLUDING DEFAULTS
  INCLUDING INDEXES
  INCLUDING STORAGE
)
;
