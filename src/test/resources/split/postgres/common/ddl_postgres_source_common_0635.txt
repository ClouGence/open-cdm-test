create table returningwrtest (a int) partition by list (a)
;
DROP DATABASE IF EXISTS sepgsql_test_regression_1
;
DROP TABLE local_tbl
;
CREATE OPERATOR CLASS ismn_ops DEFAULT
	FOR TYPE ismn USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashismn(ismn)
;
CREATE UNIQUE INDEX test_replica_identity_partial ON test_replica_identity (keya, keyb) WHERE keyb != '3'
;
ALTER RULE InsertRule ON rule_v1 RENAME TO NewInsertRule
;
ALTER EXTENSION test_ext_req_schema2 SET SCHEMA test_s_dep
;
CREATE TEMP TABLE pg_temp_9.pg_temp_26761_2 (tid pg_catalog.tid)
;
create table hp_prefix_test_p4 partition of hp_prefix_test for values with (modulus 8, remainder 4)
;
create table tuplesest_tab (a int, b int)
;
drop table boolspart
;
ANALYZE public.gin_test_tbl
;
VACUUM pg_catalog.pg_range
;
drop table partitioned
;
ANALYZE pg_temp_32.pg_temp_28153_2
;
do $$
declare x var_record_nn := row(1,2);
begin
  raise notice 'x = %', x;
  x := row(null,null);  -- ok
  x := null;  -- fail
end$$
;
/*
 * 6.58
 * SQL_FEATURES table
 */

CREATE TABLE sql_features (
    feature_id          character_data,
    feature_name        character_data,
    sub_feature_id      character_data,
    sub_feature_name    character_data,
    is_supported        yes_or_no,
    is_verified_by      character_data,
    comments            character_data
)
;
ALTER TABLE ONLY "public"."domnotnull" DROP COLUMN "........pg.dropped.2........"
;
CREATE FUNCTION explain_merge(query text) RETURNS SETOF text
LANGUAGE plpgsql AS
$$
DECLARE ln text;
BEGIN
    FOR ln IN
        EXECUTE 'explain (analyze, timing off, summary off, costs off, buffers off) ' ||
		  query
    LOOP
        ln := regexp_replace(ln, '(Memory( Usage)?|Buckets|Batches): \S*',  '\1: xxx', 'g');
        RETURN NEXT ln;
    END LOOP;
END;
$$
;
CREATE FUNCTION transition_table_base_ins_func()
  RETURNS trigger
  LANGUAGE plpgsql
AS $$
DECLARE
  t text;
  l text;
BEGIN
  t = '';
  FOR l IN EXECUTE
           $q$
             EXPLAIN (TIMING off, COSTS off, VERBOSE on)
             SELECT * FROM newtable
           $q$ LOOP
    t = t || l || E'\n';
  END LOOP;

  RAISE INFO '%', t;
  RETURN new;
END;
$$
;
CREATE OPERATOR CLASS name_ops DEFAULT FOR TYPE name
USING spgist AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	spgist_name_config(internal, internal),
	FUNCTION	2	spgist_name_choose(internal, internal),
	FUNCTION	3	spg_text_picksplit(internal, internal),
	FUNCTION	4	spgist_name_inner_consistent(internal, internal),
	FUNCTION	5	spgist_name_leaf_consistent(internal, internal),
	FUNCTION	6	spgist_name_compress(name),
	STORAGE text
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=12684 host=/tmp/CjdiX0nvl6 dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
CREATE TABLE inh_grandchild () INHERITS (inh_parent, inh_child)
;
ALTER OPERATOR FAMILY gist_timetz_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE TABLE test_stat_oid(name text NOT NULL, oid oid)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_pltemplate
;
ANALYZE public.timestamp_tbl
;
create table iboolpart_t partition of iboolpart for values in ('false')
;
ALTER TABLE toasted_key ALTER COLUMN toasted_key SET STORAGE EXTERNAL
;
create table rules_fooview (x int, y text) partition by list (x)
;
CREATE DATABASE tbuiltin1 TEMPLATE template0 LOCALE_PROVIDER builtin
;
ALTER TABLE ruletest1 ENABLE RULE rule1
;
CREATE TABLE int4tmp (a int4)
;
DROP TABLE reindex_verbose
;
TRUNCATE tbl2
;
-- Some error cases.
ALTER TABLE comment_test ALTER COLUMN xmin SET DATA TYPE x
;
drop table join_ut1
;
CREATE VIEW applicable_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS grantee,
           CAST(b.rolname AS sql_identifier) AS role_name,
           CAST(CASE WHEN m.admin_option THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable
    FROM (SELECT member, roleid, admin_option FROM pg_auth_members
          -- This UNION could be UNION ALL, but UNION works even if we start
          -- to allow explicit pg_database_owner membership.
          UNION
          SELECT datdba, pg_authid.oid, false
          FROM pg_database, pg_authid
          WHERE datname = current_database() AND rolname = 'pg_database_owner'
         )  m
         JOIN pg_authid a ON (m.member = a.oid)
         JOIN pg_authid b ON (m.roleid = b.oid)
    WHERE pg_has_role(a.oid, 'USAGE')
;
-- Check optimization disabling if it will violate special join conditions.
-- Two identical joined relations satisfies self join removal conditions but
-- stay in different special join infos.
CREATE TABLE sj_t1 (id serial, a int)
;
--
-- Basic table creation, row selection
--
CREATE TABLE enumtest (col rainbow)
;
CREATE FUNCTION test_regex(pattern text, string text, flags text)
RETURNS SETOF text[]
STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
ALTER FUNCTION gbt_var_decompress(internal) PARALLEL SAFE
;
COMMENT ON TEXT SEARCH CONFIGURATION dutch IS 'configuration for dutch language'
;
ANALYZE pg_catalog.pg_policy
;
CREATE TABLE test_numeric (
	i numeric
)
;
-- set up a new table with a redirected line pointer
-- use a temp table so that vacuum behavior doesn't depend on global xmin
create temp table htab2(a int)
;
ALTER FUNCTION gbt_int4_fetch(internal) PARALLEL SAFE
;
CREATE TABLE pht1_e (a int, b int, c text) PARTITION BY HASH(ltrim(c, 'A'))
;
-- test jsonb[] -> python list
-- simple list
CREATE FUNCTION test2arr(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, list)
assert(val == [12, 1])
return len(val)
$$
;
DROP SCHEMA IF EXISTS split_pub18_b CASCADE
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table returningwrtest (a int) partition by list (a)
;
----------
[DROP_CATALOG] DROP DATABASE IF EXISTS sepgsql_test_regression_1
;
----------
[DROP_TABLE] DROP TABLE local_tbl
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS ismn_ops DEFAULT
	FOR TYPE ismn USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashismn(ismn)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX test_replica_identity_partial ON test_replica_identity (keya, keyb) WHERE keyb != '3'
;
----------
[ALTER_POLICY] ALTER RULE InsertRule ON rule_v1 RENAME TO NewInsertRule
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_ext_req_schema2 SET SCHEMA test_s_dep
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_9.pg_temp_26761_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table hp_prefix_test_p4 partition of hp_prefix_test for values with (modulus 8, remainder 4)
;
----------
[CREATE_TABLE] create table tuplesest_tab (a int, b int)
;
----------
[DROP_TABLE] drop table boolspart
;
----------
[ADMIN_TABLE] ANALYZE public.gin_test_tbl
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_range
;
----------
[DROP_TABLE] drop table partitioned
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28153_2
;
----------
[BLOCK] do $$
declare x var_record_nn := row(1,2);
begin
  raise notice 'x = %', x;
  x := row(null,null);  -- ok
  x := null;  -- fail
end$$
;
----------
[CREATE_TABLE] /*
 * 6.58
 * SQL_FEATURES table
 */

CREATE TABLE sql_features (
    feature_id          character_data,
    feature_name        character_data,
    sub_feature_id      character_data,
    sub_feature_name    character_data,
    is_supported        yes_or_no,
    is_verified_by      character_data,
    comments            character_data
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."domnotnull" DROP COLUMN "........pg.dropped.2........"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION explain_merge(query text) RETURNS SETOF text
LANGUAGE plpgsql AS
$$
DECLARE ln text;
BEGIN
    FOR ln IN
        EXECUTE 'explain (analyze, timing off, summary off, costs off, buffers off) ' ||
		  query
    LOOP
        ln := regexp_replace(ln, '(Memory( Usage)?|Buckets|Batches): \S*',  '\1: xxx', 'g');
        RETURN NEXT ln;
    END LOOP;
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION transition_table_base_ins_func()
  RETURNS trigger
  LANGUAGE plpgsql
AS $$
DECLARE
  t text;
  l text;
BEGIN
  t = '';
  FOR l IN EXECUTE
           $q$
             EXPLAIN (TIMING off, COSTS off, VERBOSE on)
             SELECT * FROM newtable
           $q$ LOOP
    t = t || l || E'\n';
  END LOOP;

  RAISE INFO '%', t;
  RETURN new;
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS name_ops DEFAULT FOR TYPE name
USING spgist AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	spgist_name_config(internal, internal),
	FUNCTION	2	spgist_name_choose(internal, internal),
	FUNCTION	3	spg_text_picksplit(internal, internal),
	FUNCTION	4	spgist_name_inner_consistent(internal, internal),
	FUNCTION	5	spgist_name_leaf_consistent(internal, internal),
	FUNCTION	6	spgist_name_compress(name),
	STORAGE text
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=12684 host=/tmp/CjdiX0nvl6 dbname=postgres' PUBLICATION tap_pub WITH (slot_name = tap_sub_slot)
;
----------
[CREATE_TABLE] CREATE TABLE inh_grandchild () INHERITS (inh_parent, inh_child)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timetz_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TABLE] CREATE TABLE test_stat_oid(name text NOT NULL, oid oid)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_pltemplate
;
----------
[ADMIN_TABLE] ANALYZE public.timestamp_tbl
;
----------
[CREATE_TABLE] create table iboolpart_t partition of iboolpart for values in ('false')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE toasted_key ALTER COLUMN toasted_key SET STORAGE EXTERNAL
;
----------
[CREATE_TABLE] create table rules_fooview (x int, y text) partition by list (x)
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin1 TEMPLATE template0 LOCALE_PROVIDER builtin
;
----------
[ALTER_TABLE] ALTER TABLE ruletest1 ENABLE RULE rule1
;
----------
[CREATE_TABLE] CREATE TABLE int4tmp (a int4)
;
----------
[DROP_TABLE] DROP TABLE reindex_verbose
;
----------
[TRUNCATE_TABLE] TRUNCATE tbl2
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Some error cases.
ALTER TABLE comment_test ALTER COLUMN xmin SET DATA TYPE x
;
----------
[DROP_TABLE] drop table join_ut1
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW applicable_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS grantee,
           CAST(b.rolname AS sql_identifier) AS role_name,
           CAST(CASE WHEN m.admin_option THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable
    FROM (SELECT member, roleid, admin_option FROM pg_auth_members
          -- This UNION could be UNION ALL, but UNION works even if we start
          -- to allow explicit pg_database_owner membership.
          UNION
          SELECT datdba, pg_authid.oid, false
          FROM pg_database, pg_authid
          WHERE datname = current_database() AND rolname = 'pg_database_owner'
         )  m
         JOIN pg_authid a ON (m.member = a.oid)
         JOIN pg_authid b ON (m.roleid = b.oid)
    WHERE pg_has_role(a.oid, 'USAGE')
;
----------
[CREATE_TABLE] -- Check optimization disabling if it will violate special join conditions.
-- Two identical joined relations satisfies self join removal conditions but
-- stay in different special join infos.
CREATE TABLE sj_t1 (id serial, a int)
;
----------
[CREATE_TABLE] --
-- Basic table creation, row selection
--
CREATE TABLE enumtest (col rainbow)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_regex(pattern text, string text, flags text)
RETURNS SETOF text[]
STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_var_decompress(internal) PARALLEL SAFE
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION dutch IS 'configuration for dutch language'
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_policy
;
----------
[CREATE_TABLE] CREATE TABLE test_numeric (
	i numeric
)
;
----------
[CREATE_TABLE] -- set up a new table with a redirected line pointer
-- use a temp table so that vacuum behavior doesn't depend on global xmin
create temp table htab2(a int)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int4_fetch(internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE pht1_e (a int, b int, c text) PARTITION BY HASH(ltrim(c, 'A'))
;
----------
[CREATE_PROG_OBJ] -- test jsonb[] -> python list
-- simple list
CREATE FUNCTION test2arr(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, list)
assert(val == [12, 1])
return len(val)
$$
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_pub18_b CASCADE
;
