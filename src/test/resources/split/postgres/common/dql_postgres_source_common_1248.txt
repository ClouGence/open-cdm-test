SELECT c.reltype AS crel FROM pg_catalog.pg_class c WHERE c.oid = '36714'::pg_catalog.oid
;
select 'aa=>bb , "cc"=>dd'::hstore
;
SELECT r.conname, pg_catalog.pg_get_constraintdef(r.oid, true)
FROM pg_catalog.pg_constraint r
WHERE r.conrelid = '21874' AND r.contype = 'c'
ORDER BY 1
;
-- these operators are ignored
select websearch_to_tsquery('simple', 'abc & def')
;
SELECT lo_put(39994, 5, decode('afafafaf', 'hex'))
;
SELECT abalance FROM pgbench_accounts WHERE aid = 95150
;
-- For binary upgrade, must preserve pg_type array oid
SELECT pg_catalog.binary_upgrade_set_next_array_pg_type_oid('30754'::pg_catalog.oid)
;
SELECT m.*
FROM pg_statistic_ext AS s,
     pg_statistic_ext_data AS d,
     pg_mcv_list_items(d.stxdmcv) AS m
WHERE s.stxnamespace = 'split_mcv'::regnamespace
  AND s.stxname = 'items_mcv'
  AND d.stxoid = s.oid
ORDER BY m.index
;
SELECT t1.a, t1.c, t2.b, t2.c FROM prt1_adv t1 LEFT JOIN prt2_adv t2 ON (t1.a = t2.b) WHERE t1.b = 0 ORDER BY t1.a, t2.b
;
SELECT pubname
FROM pg_catalog.pg_publication p
JOIN pg_catalog.pg_publication_rel pr ON p.oid = pr.prpubid
WHERE pr.prrelid = '37475'
UNION ALL
SELECT pubname
FROM pg_catalog.pg_publication p
WHERE p.puballtables AND pg_catalog.pg_relation_is_publishable('37475')
ORDER BY 1
;
SELECT '0/1499568' <= replay_lsn AND state = 'streaming'
         FROM pg_catalog.pg_stat_replication
         WHERE application_name IN ('admin_sub', 'walreceiver')
;
EXPLAIN (COSTS OFF)
SELECT * FROM
  (SELECT empno,
          salary,
          count(*) OVER (ORDER BY salary DESC ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING) c
   FROM empsalary) emp
WHERE c >= 3
;
SELECT date 'today infinity'
;
SELECT true as sametable, conname,
  pg_catalog.pg_get_constraintdef(r.oid, true) as condef,
  conrelid::pg_catalog.regclass AS ontable
FROM pg_catalog.pg_constraint r
WHERE r.conrelid = '37269' AND r.contype = 'f'
     AND conparentid = 0
ORDER BY conname
;
SELECT pg_catalog.binary_upgrade_set_next_heap_pg_class_oid('36926'::pg_catalog.oid)
;
SELECT c.relchecks, c.relkind, c.relhasindex, c.relhasrules, c.relhastriggers, c.relrowsecurity, c.relforcerowsecurity, false AS relhasoids, c.relispartition, '', c.reltablespace, CASE WHEN c.reloftype = 0 THEN '' ELSE c.reloftype::pg_catalog.regtype::pg_catalog.text END, c.relpersistence, c.relreplident, am.amname
FROM pg_catalog.pg_class c
 LEFT JOIN pg_catalog.pg_class tc ON (c.reltoastrelid = tc.oid)
LEFT JOIN pg_catalog.pg_am am ON (c.relam = am.oid)
WHERE c.oid = '31770'
;
SELECT '{1.2.3.4.5}'::ltree[] @> '1.2.3.4'
;
SELECT c.reltoastrelid, i.indexrelid FROM pg_catalog.pg_class c LEFT JOIN pg_catalog.pg_index i ON (c.reltoastrelid = i.indrelid AND i.indisvalid) WHERE c.oid = '32150'::pg_catalog.oid
;
SELECT '0/F00188' <= replay_lsn AND state = 'streaming' FROM pg_catalog.pg_stat_replication WHERE application_name = 'standby_1'
;
SELECT c.reltype AS crel, t.reltype AS trel FROM pg_catalog.pg_class c LEFT JOIN pg_catalog.pg_class t ON   (c.reltoastrelid = t.oid AND c.relkind <> 'p') WHERE c.oid = '32651'::pg_catalog.oid
;
-- For binary upgrade, must preserve pg_class oids
SELECT pg_catalog.binary_upgrade_set_next_heap_pg_class_oid('31166'::pg_catalog.oid)
;
SELECT 'bear' NOT LIKE 'b_ear' ESCAPE '_' AS "false"
;
SELECT pubname
     , NULL
     , NULL
FROM pg_catalog.pg_publication p
     JOIN pg_catalog.pg_publication_namespace pn ON p.oid = pn.pnpubid
     JOIN pg_catalog.pg_class pc ON pc.relnamespace = pn.pnnspid
WHERE pc.oid ='32012' and pg_catalog.pg_relation_is_publishable('32012')
UNION
SELECT pubname
     , pg_get_expr(pr.prqual, c.oid)
     , (CASE WHEN pr.prattrs IS NOT NULL THEN
         (SELECT string_agg(attname, ', ')
           FROM pg_catalog.generate_series(0, pg_catalog.array_upper(pr.prattrs::pg_catalog.int2[], 1)) s,
                pg_catalog.pg_attribute
          WHERE attrelid = pr.prrelid AND attnum = prattrs[s])
        ELSE NULL END) FROM pg_catalog.pg_publication p
     JOIN pg_catalog.pg_publication_rel pr ON p.oid = pr.prpubid
     JOIN pg_catalog.pg_class c ON c.oid = pr.prrelid
WHERE pr.prrelid = '32012'
UNION
SELECT pubname
     , NULL
     , NULL
FROM pg_catalog.pg_publication p
WHERE p.puballtables AND pg_catalog.pg_relation_is_publishable('32012')
ORDER BY 1
;
SELECT c.oid::pg_catalog.regclass
FROM pg_catalog.pg_class c, pg_catalog.pg_inherits i
WHERE c.oid = i.inhparent AND i.inhrelid = '23752'
  AND c.relkind != 'p' AND c.relkind != 'I'
ORDER BY inhseqno
;
SELECT pg_logical_emit_message(true, '', repeat('a', 7648)) - '0/0'
;
SELECT a.attname,
  pg_catalog.format_type(a.atttypid, a.atttypmod),
  (SELECT pg_catalog.pg_get_expr(d.adbin, d.adrelid, true)
   FROM pg_catalog.pg_attrdef d
   WHERE d.adrelid = a.attrelid AND d.adnum = a.attnum AND a.atthasdef),
  a.attnotnull,
  (SELECT c.collname FROM pg_catalog.pg_collation c, pg_catalog.pg_type t
   WHERE c.oid = a.attcollation AND t.oid = a.atttypid AND a.attcollation <> t.typcollation) AS attcollation,
  a.attidentity,
  a.attgenerated,
  a.attstorage,
  CASE WHEN a.attstattarget=-1 THEN NULL ELSE a.attstattarget END AS attstattarget,
  pg_catalog.col_description(a.attrelid, a.attnum)
FROM pg_catalog.pg_attribute a
WHERE a.attrelid = '17607' AND a.attnum > 0 AND NOT a.attisdropped
ORDER BY a.attnum
;
SELECT c.reltoastrelid, i.indexrelid FROM pg_catalog.pg_class c LEFT JOIN pg_catalog.pg_index i ON (c.reltoastrelid = i.indrelid AND i.indisvalid) WHERE c.oid = '27786'::pg_catalog.oid
;
-- should be at least 2x as large than temp_buffers
SELECT pg_relation_size('test_temp') / current_setting('block_size')::int8 > 200
;
SELECT ''::text AS two, unique1, unique2, stringu1
		FROM onek WHERE unique1 > 50
		ORDER BY unique1 LIMIT 2
;
SELECT pg_catalog.binary_upgrade_set_next_index_relfilenode('41810'::pg_catalog.oid)
;
-- For binary upgrade, must preserve pg_class oids
SELECT pg_catalog.binary_upgrade_set_next_index_pg_class_oid('25440'::pg_catalog.oid)
;
SELECT ctid, stxowner FROM pg_statistic_ext fk WHERE NOT EXISTS(SELECT 1 FROM pg_authid pk WHERE pk.oid = fk.stxowner)
;
SELECT c.oid::pg_catalog.regclass
FROM pg_catalog.pg_class c, pg_catalog.pg_inherits i
WHERE c.oid = i.inhparent AND i.inhrelid = '21246'
  AND c.relkind != 'p' AND c.relkind != 'I'
ORDER BY inhseqno
;
SELECT abalance FROM pgbench_accounts WHERE aid = 20893
;
SELECT typarray FROM pg_catalog.pg_type WHERE oid = '28629'::pg_catalog.oid
;
SELECT count(*) FROM moneytmp WHERE a <= '22649.64'::money
;
SELECT pg_catalog.binary_upgrade_set_next_toast_pg_class_oid('35902'::pg_catalog.oid)
;
SELECT 1 FROM ONLY "pg_temp_2"."pp" x WHERE "f1" OPERATOR(pg_catalog.=) $1 FOR KEY SHARE OF x
;
select count(*) > 0 from pg_visibility('test_partition', 0)
;
-- For binary upgrade, must preserve pg_class oids and relfilenodes
SELECT pg_catalog.binary_upgrade_set_next_index_pg_class_oid('34820'::pg_catalog.oid)
;
SELECT oid, stxrelid::pg_catalog.regclass, stxnamespace::pg_catalog.regnamespace AS nsp, stxname,
  (SELECT pg_catalog.string_agg(pg_catalog.quote_ident(attname),', ')
   FROM pg_catalog.unnest(stxkeys) s(attnum)
   JOIN pg_catalog.pg_attribute a ON (stxrelid = a.attrelid AND
        a.attnum = s.attnum AND NOT attisdropped)) AS columns,
  'd' = any(stxkind) AS ndist_enabled,
  'f' = any(stxkind) AS deps_enabled,
  'm' = any(stxkind) AS mcv_enabled
FROM pg_catalog.pg_statistic_ext stat WHERE stxrelid = '36195'
ORDER BY 1
;
SELECT t.tgname, t.tgfoid::pg_catalog.regproc AS tgfname, pg_catalog.pg_get_triggerdef(t.oid, false) AS tgdef, t.tgenabled, t.tableoid, t.oid, t.tgisinternal FROM pg_catalog.pg_trigger t LEFT JOIN pg_catalog.pg_trigger u ON u.oid = t.tgparentid WHERE t.tgrelid = '31041'::pg_catalog.oid AND (NOT t.tgisinternal OR t.tgenabled != u.tgenabled)
;
SELECT str as "value", typ as "type",
       pg_input_is_valid(str,typ) as ok,
       errinfo.sql_error_code,
       errinfo.message,
       errinfo.detail,
       errinfo.hint
FROM (VALUES ('.2.3', 'ltree'),
             ('1.2.', 'ltree'),
             ('1.2.3','ltree'),
             ('@.2.3','lquery'),
             (' 2.3', 'lquery'),
             ('1.2.3','lquery'),
             ('$tree & aWdf@*','ltxtquery'),
             ('!tree & aWdf@*','ltxtquery'))
      AS a(str,typ),
     LATERAL pg_input_error_info(a.str, a.typ) as errinfo
;
SELECT newdata.*::pg_temp_32.pg_temp_28597 FROM pg_temp_32.pg_temp_28597 newdata WHERE newdata.* IS NOT NULL AND EXISTS (SELECT 1 FROM pg_temp_32.pg_temp_28597 newdata2 WHERE newdata2.* IS NOT NULL AND newdata2.* OPERATOR(pg_catalog.*=) newdata.* AND newdata2.ctid OPERATOR(pg_catalog.<>) newdata.ctid)
;
SELECT typarray FROM pg_catalog.pg_type WHERE oid = '38047'::pg_catalog.oid
;
SELECT c.oid::pg_catalog.regclass
FROM pg_catalog.pg_class c, pg_catalog.pg_inherits i
WHERE c.oid = i.inhparent AND i.inhrelid = '18239'
  AND c.relkind != 'p' AND c.relkind != 'I'
ORDER BY inhseqno
;
SELECT pg_get_partkeydef('45152')
;
SELECT true as sametable, conname,
  pg_catalog.pg_get_constraintdef(r.oid, true) as condef,
  conrelid::pg_catalog.regclass AS ontable
FROM pg_catalog.pg_constraint r
WHERE r.conrelid = '23145' AND r.contype = 'f'
     AND conparentid = 0
ORDER BY conname
;
EXPLAIN SELECT array_agg(ctid) FROM brintest WHERE inetcol >>= '10.2.14.231/24'::inet
;
SELECT pg_get_partkeydef('38891')
;
------------------------------------------------------------------------------------------
[METADATA] SELECT c.reltype AS crel FROM pg_catalog.pg_class c WHERE c.oid = '36714'::pg_catalog.oid
;
----------
[SELECT] select 'aa=>bb , "cc"=>dd'::hstore
;
----------
[METADATA] SELECT r.conname, pg_catalog.pg_get_constraintdef(r.oid, true)
FROM pg_catalog.pg_constraint r
WHERE r.conrelid = '21874' AND r.contype = 'c'
ORDER BY 1
;
----------
[SELECT] -- these operators are ignored
select websearch_to_tsquery('simple', 'abc & def')
;
----------
[SELECT] SELECT lo_put(39994, 5, decode('afafafaf', 'hex'))
;
----------
[SELECT] SELECT abalance FROM pgbench_accounts WHERE aid = 95150
;
----------
[SELECT] -- For binary upgrade, must preserve pg_type array oid
SELECT pg_catalog.binary_upgrade_set_next_array_pg_type_oid('30754'::pg_catalog.oid)
;
----------
[SELECT|PERFORMANCE] SELECT m.*
FROM pg_statistic_ext AS s,
     pg_statistic_ext_data AS d,
     pg_mcv_list_items(d.stxdmcv) AS m
WHERE s.stxnamespace = 'split_mcv'::regnamespace
  AND s.stxname = 'items_mcv'
  AND d.stxoid = s.oid
ORDER BY m.index
;
----------
[SELECT] SELECT t1.a, t1.c, t2.b, t2.c FROM prt1_adv t1 LEFT JOIN prt2_adv t2 ON (t1.a = t2.b) WHERE t1.b = 0 ORDER BY t1.a, t2.b
;
----------
[METADATA] SELECT pubname
FROM pg_catalog.pg_publication p
JOIN pg_catalog.pg_publication_rel pr ON p.oid = pr.prpubid
WHERE pr.prrelid = '37475'
UNION ALL
SELECT pubname
FROM pg_catalog.pg_publication p
WHERE p.puballtables AND pg_catalog.pg_relation_is_publishable('37475')
ORDER BY 1
;
----------
[METADATA] SELECT '0/1499568' <= replay_lsn AND state = 'streaming'
         FROM pg_catalog.pg_stat_replication
         WHERE application_name IN ('admin_sub', 'walreceiver')
;
----------
[PERFORMANCE] EXPLAIN (COSTS OFF)
SELECT * FROM
  (SELECT empno,
          salary,
          count(*) OVER (ORDER BY salary DESC ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING) c
   FROM empsalary) emp
WHERE c >= 3
;
----------
[SELECT] SELECT date 'today infinity'
;
----------
[METADATA] SELECT true as sametable, conname,
  pg_catalog.pg_get_constraintdef(r.oid, true) as condef,
  conrelid::pg_catalog.regclass AS ontable
FROM pg_catalog.pg_constraint r
WHERE r.conrelid = '37269' AND r.contype = 'f'
     AND conparentid = 0
ORDER BY conname
;
----------
[SELECT] SELECT pg_catalog.binary_upgrade_set_next_heap_pg_class_oid('36926'::pg_catalog.oid)
;
----------
[METADATA] SELECT c.relchecks, c.relkind, c.relhasindex, c.relhasrules, c.relhastriggers, c.relrowsecurity, c.relforcerowsecurity, false AS relhasoids, c.relispartition, '', c.reltablespace, CASE WHEN c.reloftype = 0 THEN '' ELSE c.reloftype::pg_catalog.regtype::pg_catalog.text END, c.relpersistence, c.relreplident, am.amname
FROM pg_catalog.pg_class c
 LEFT JOIN pg_catalog.pg_class tc ON (c.reltoastrelid = tc.oid)
LEFT JOIN pg_catalog.pg_am am ON (c.relam = am.oid)
WHERE c.oid = '31770'
;
----------
[SELECT] SELECT '{1.2.3.4.5}'::ltree[] @> '1.2.3.4'
;
----------
[METADATA] SELECT c.reltoastrelid, i.indexrelid FROM pg_catalog.pg_class c LEFT JOIN pg_catalog.pg_index i ON (c.reltoastrelid = i.indrelid AND i.indisvalid) WHERE c.oid = '32150'::pg_catalog.oid
;
----------
[METADATA] SELECT '0/F00188' <= replay_lsn AND state = 'streaming' FROM pg_catalog.pg_stat_replication WHERE application_name = 'standby_1'
;
----------
[METADATA] SELECT c.reltype AS crel, t.reltype AS trel FROM pg_catalog.pg_class c LEFT JOIN pg_catalog.pg_class t ON   (c.reltoastrelid = t.oid AND c.relkind <> 'p') WHERE c.oid = '32651'::pg_catalog.oid
;
----------
[SELECT] -- For binary upgrade, must preserve pg_class oids
SELECT pg_catalog.binary_upgrade_set_next_heap_pg_class_oid('31166'::pg_catalog.oid)
;
----------
[SELECT] SELECT 'bear' NOT LIKE 'b_ear' ESCAPE '_' AS "false"
;
----------
[METADATA] SELECT pubname
     , NULL
     , NULL
FROM pg_catalog.pg_publication p
     JOIN pg_catalog.pg_publication_namespace pn ON p.oid = pn.pnpubid
     JOIN pg_catalog.pg_class pc ON pc.relnamespace = pn.pnnspid
WHERE pc.oid ='32012' and pg_catalog.pg_relation_is_publishable('32012')
UNION
SELECT pubname
     , pg_get_expr(pr.prqual, c.oid)
     , (CASE WHEN pr.prattrs IS NOT NULL THEN
         (SELECT string_agg(attname, ', ')
           FROM pg_catalog.generate_series(0, pg_catalog.array_upper(pr.prattrs::pg_catalog.int2[], 1)) s,
                pg_catalog.pg_attribute
          WHERE attrelid = pr.prrelid AND attnum = prattrs[s])
        ELSE NULL END) FROM pg_catalog.pg_publication p
     JOIN pg_catalog.pg_publication_rel pr ON p.oid = pr.prpubid
     JOIN pg_catalog.pg_class c ON c.oid = pr.prrelid
WHERE pr.prrelid = '32012'
UNION
SELECT pubname
     , NULL
     , NULL
FROM pg_catalog.pg_publication p
WHERE p.puballtables AND pg_catalog.pg_relation_is_publishable('32012')
ORDER BY 1
;
----------
[METADATA] SELECT c.oid::pg_catalog.regclass
FROM pg_catalog.pg_class c, pg_catalog.pg_inherits i
WHERE c.oid = i.inhparent AND i.inhrelid = '23752'
  AND c.relkind != 'p' AND c.relkind != 'I'
ORDER BY inhseqno
;
----------
[SELECT|ADMIN_REPLICATION] SELECT pg_logical_emit_message(true, '', repeat('a', 7648)) - '0/0'
;
----------
[METADATA] SELECT a.attname,
  pg_catalog.format_type(a.atttypid, a.atttypmod),
  (SELECT pg_catalog.pg_get_expr(d.adbin, d.adrelid, true)
   FROM pg_catalog.pg_attrdef d
   WHERE d.adrelid = a.attrelid AND d.adnum = a.attnum AND a.atthasdef),
  a.attnotnull,
  (SELECT c.collname FROM pg_catalog.pg_collation c, pg_catalog.pg_type t
   WHERE c.oid = a.attcollation AND t.oid = a.atttypid AND a.attcollation <> t.typcollation) AS attcollation,
  a.attidentity,
  a.attgenerated,
  a.attstorage,
  CASE WHEN a.attstattarget=-1 THEN NULL ELSE a.attstattarget END AS attstattarget,
  pg_catalog.col_description(a.attrelid, a.attnum)
FROM pg_catalog.pg_attribute a
WHERE a.attrelid = '17607' AND a.attnum > 0 AND NOT a.attisdropped
ORDER BY a.attnum
;
----------
[METADATA] SELECT c.reltoastrelid, i.indexrelid FROM pg_catalog.pg_class c LEFT JOIN pg_catalog.pg_index i ON (c.reltoastrelid = i.indrelid AND i.indisvalid) WHERE c.oid = '27786'::pg_catalog.oid
;
----------
[METADATA] -- should be at least 2x as large than temp_buffers
SELECT pg_relation_size('test_temp') / current_setting('block_size')::int8 > 200
;
----------
[SELECT] SELECT ''::text AS two, unique1, unique2, stringu1
		FROM onek WHERE unique1 > 50
		ORDER BY unique1 LIMIT 2
;
----------
[SELECT] SELECT pg_catalog.binary_upgrade_set_next_index_relfilenode('41810'::pg_catalog.oid)
;
----------
[SELECT] -- For binary upgrade, must preserve pg_class oids
SELECT pg_catalog.binary_upgrade_set_next_index_pg_class_oid('25440'::pg_catalog.oid)
;
----------
[SELECT|PERFORMANCE] SELECT ctid, stxowner FROM pg_statistic_ext fk WHERE NOT EXISTS(SELECT 1 FROM pg_authid pk WHERE pk.oid = fk.stxowner)
;
----------
[METADATA] SELECT c.oid::pg_catalog.regclass
FROM pg_catalog.pg_class c, pg_catalog.pg_inherits i
WHERE c.oid = i.inhparent AND i.inhrelid = '21246'
  AND c.relkind != 'p' AND c.relkind != 'I'
ORDER BY inhseqno
;
----------
[SELECT] SELECT abalance FROM pgbench_accounts WHERE aid = 20893
;
----------
[METADATA] SELECT typarray FROM pg_catalog.pg_type WHERE oid = '28629'::pg_catalog.oid
;
----------
[SELECT] SELECT count(*) FROM moneytmp WHERE a <= '22649.64'::money
;
----------
[SELECT] SELECT pg_catalog.binary_upgrade_set_next_toast_pg_class_oid('35902'::pg_catalog.oid)
;
----------
[SELECT|QUERY_LOCK] SELECT 1 FROM ONLY "pg_temp_2"."pp" x WHERE "f1" OPERATOR(pg_catalog.=) $1 FOR KEY SHARE OF x
;
----------
[SELECT] select count(*) > 0 from pg_visibility('test_partition', 0)
;
----------
[SELECT] -- For binary upgrade, must preserve pg_class oids and relfilenodes
SELECT pg_catalog.binary_upgrade_set_next_index_pg_class_oid('34820'::pg_catalog.oid)
;
----------
[SELECT|METADATA|PERFORMANCE] SELECT oid, stxrelid::pg_catalog.regclass, stxnamespace::pg_catalog.regnamespace AS nsp, stxname,
  (SELECT pg_catalog.string_agg(pg_catalog.quote_ident(attname),', ')
   FROM pg_catalog.unnest(stxkeys) s(attnum)
   JOIN pg_catalog.pg_attribute a ON (stxrelid = a.attrelid AND
        a.attnum = s.attnum AND NOT attisdropped)) AS columns,
  'd' = any(stxkind) AS ndist_enabled,
  'f' = any(stxkind) AS deps_enabled,
  'm' = any(stxkind) AS mcv_enabled
FROM pg_catalog.pg_statistic_ext stat WHERE stxrelid = '36195'
ORDER BY 1
;
----------
[METADATA] SELECT t.tgname, t.tgfoid::pg_catalog.regproc AS tgfname, pg_catalog.pg_get_triggerdef(t.oid, false) AS tgdef, t.tgenabled, t.tableoid, t.oid, t.tgisinternal FROM pg_catalog.pg_trigger t LEFT JOIN pg_catalog.pg_trigger u ON u.oid = t.tgparentid WHERE t.tgrelid = '31041'::pg_catalog.oid AND (NOT t.tgisinternal OR t.tgenabled != u.tgenabled)
;
----------
[SELECT] SELECT str as "value", typ as "type",
       pg_input_is_valid(str,typ) as ok,
       errinfo.sql_error_code,
       errinfo.message,
       errinfo.detail,
       errinfo.hint
FROM (VALUES ('.2.3', 'ltree'),
             ('1.2.', 'ltree'),
             ('1.2.3','ltree'),
             ('@.2.3','lquery'),
             (' 2.3', 'lquery'),
             ('1.2.3','lquery'),
             ('$tree & aWdf@*','ltxtquery'),
             ('!tree & aWdf@*','ltxtquery'))
      AS a(str,typ),
     LATERAL pg_input_error_info(a.str, a.typ) as errinfo
;
----------
[SELECT] SELECT newdata.*::pg_temp_32.pg_temp_28597 FROM pg_temp_32.pg_temp_28597 newdata WHERE newdata.* IS NOT NULL AND EXISTS (SELECT 1 FROM pg_temp_32.pg_temp_28597 newdata2 WHERE newdata2.* IS NOT NULL AND newdata2.* OPERATOR(pg_catalog.*=) newdata.* AND newdata2.ctid OPERATOR(pg_catalog.<>) newdata.ctid)
;
----------
[METADATA] SELECT typarray FROM pg_catalog.pg_type WHERE oid = '38047'::pg_catalog.oid
;
----------
[METADATA] SELECT c.oid::pg_catalog.regclass
FROM pg_catalog.pg_class c, pg_catalog.pg_inherits i
WHERE c.oid = i.inhparent AND i.inhrelid = '18239'
  AND c.relkind != 'p' AND c.relkind != 'I'
ORDER BY inhseqno
;
----------
[METADATA] SELECT pg_get_partkeydef('45152')
;
----------
[METADATA] SELECT true as sametable, conname,
  pg_catalog.pg_get_constraintdef(r.oid, true) as condef,
  conrelid::pg_catalog.regclass AS ontable
FROM pg_catalog.pg_constraint r
WHERE r.conrelid = '23145' AND r.contype = 'f'
     AND conparentid = 0
ORDER BY conname
;
----------
[PERFORMANCE] EXPLAIN SELECT array_agg(ctid) FROM brintest WHERE inetcol >>= '10.2.14.231/24'::inet
;
----------
[METADATA] SELECT pg_get_partkeydef('38891')
;
