ANALYZE public.bprime
;
DROP FUNCTION predicate_stable()
;
drop schema testpart
;
CREATE TABLE default_expr_agg (a int DEFAULT (generate_series(1,3)))
;
ALTER TABLE ONLY "public"."itest7e" INHERIT "public"."itest7d"
;
drop table part_pa_test
;
CREATE OPERATOR @ (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contains,
	COMMUTATOR = '~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE VIEW "public"."test_tablesample_v2" AS
 SELECT "test_tablesample"."id"
   FROM "public"."test_tablesample" TABLESAMPLE "system" (99)
;
CREATE TABLE "public"."prt2_l_p3" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("a")
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
CREATE OR REPLACE FUNCTION pg_logical_slot_get_binary_changes(
    IN slot_name name, IN upto_lsn pg_lsn, IN upto_nchanges int, VARIADIC options text[] DEFAULT '{}',
    OUT lsn pg_lsn, OUT xid xid, OUT data bytea)
RETURNS SETOF RECORD
LANGUAGE INTERNAL
VOLATILE ROWS 1000 COST 1000
AS 'pg_logical_slot_get_binary_changes'
;
CREATE TABLE t_1 PARTITION OF s1.t FOR VALUES FROM (1) TO (10)
;
CREATE FUNCTION gbt_float4_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER FUNCTION gbt_intv_union(internal, internal) PARALLEL SAFE
;
-- fail, must install cube first
CREATE EXTENSION cube
;
DROP TABLE psql_comics
;
-- fail, can't infer type:
create function polyf(x anyelement) returns anyrange as $$
  select array[x + 1, x + 2]
$$ language sql
;
-- disable ENFORCE_REGRESSION_TEST_NAME_RESTRICTIONS warning
CREATE TABLESPACE pg_foo LOCATION '/no/such/location'
;
--
-- Test precision and scale typemods
--

CREATE TABLE num_typemod_test (
  millions numeric(3, -6),
  thousands numeric(3, -3),
  units numeric(3, 0),
  thousandths numeric(3, 3),
  millionths numeric(3, 6)
)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_opfamily
;
CREATE TEMP TABLE kd_point_tbl_ord_idx3 AS
SELECT row_number() OVER (ORDER BY p <-> '333,400') n, p <-> '333,400' dist, p
FROM kd_point_tbl WHERE p IS NOT NULL
;
create temp table snapshot_test (
	nr	integer,
	snap	pg_snapshot
)
;
create trigger bbb after insert on parted_trig for each row execute procedure trigger_notice()
;
ALTER TABLE split_event_helper.rewrite_probe
    ALTER COLUMN payload TYPE bigint USING payload::bigint
;
ALTER OPERATOR >= (issn13, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
CREATE SCHEMA fkpart9
;
create temp table pp_temp_part_def partition of pp_temp_parent default
;
CREATE SEQUENCE "public"."sequence_test3"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
-- Ambiguity should be reported only if there's not a better match available

create function dfunc(int = 1, int = 2, int = 3) returns int as $$
  select 3;
$$ language sql
;
CREATE AGGREGATE myaggp01a(*) (SFUNC = stfnp, STYPE = int4[],
  FINALFUNC = ffp, INITCOND = '{}')
;
-- table with toast relation
CREATE TABLE regress_tblspace_test_tbl (num1 bigint, num2 double precision, t text)
;
ALTER TABLE pg_toast.pg_toast_33260 RENAME TO reind_con_toast
;
--
-- negative testcases
--
CREATE TABLE prt1_n (a int, b int, c varchar) PARTITION BY RANGE(c)
;
ALTER TABLE replication_metadata SET (user_catalog_table = false)
;
create table mclparted1 partition of mclparted for values in(1)
;
drop index test_pg_index_toast_index
;
CREATE SUBSCRIPTION regress_sub3 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub3 WITH (enabled = false)
;
CREATE EVENT TRIGGER on_login_trigger ON login EXECUTE PROCEDURE on_login_proc()
;
CREATE FUNCTION cube(float8[]) RETURNS cube
AS '$libdir/cube', 'cube_a_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION gbtreekey32_in(cstring)
RETURNS gbtreekey32
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE vac_horizon_floor_table(col1 int)
		WITH (autovacuum_enabled=false, fillfactor=10)
;
alter function test_strict(text) called on null input
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_default_acl
;
CREATE FUNCTION pg_catalog.pg_file_rename(text, text, text)
RETURNS bool
AS '$libdir/adminpack', 'pg_file_rename'
LANGUAGE C VOLATILE
;
create domain restrictedrange as int4range check (upper(value) < 10)
;
create function sp_id_user(a_login text) returns int as $$
declare x int;
begin
  select into x id from users where login = a_login;
  if found then return x; end if;
  return 0;
end$$ language plpgsql stable
;
CREATE TABLE footrg1 PARTITION OF footrg FOR VALUES IN (1)
;
CREATE FUNCTION dsnowball_init(INTERNAL)
    RETURNS INTERNAL AS '$libdir/dict_snowball', 'dsnowball_init'
LANGUAGE C STRICT
;
ALTER TABLE "public"."pitest1" ALTER COLUMN "f3" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."pitest1_f3_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE public.bprime
;
----------
[DROP_PROG_OBJ] DROP FUNCTION predicate_stable()
;
----------
[DROP_SCHEMA] drop schema testpart
;
----------
[CREATE_TABLE] CREATE TABLE default_expr_agg (a int DEFAULT (generate_series(1,3)))
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."itest7e" INHERIT "public"."itest7d"
;
----------
[DROP_TABLE] drop table part_pa_test
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @ (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contains,
	COMMUTATOR = '~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."test_tablesample_v2" AS
 SELECT "test_tablesample"."id"
   FROM "public"."test_tablesample" TABLESAMPLE "system" (99)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_l_p3" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_logical_slot_get_binary_changes(
    IN slot_name name, IN upto_lsn pg_lsn, IN upto_nchanges int, VARIADIC options text[] DEFAULT '{}',
    OUT lsn pg_lsn, OUT xid xid, OUT data bytea)
RETURNS SETOF RECORD
LANGUAGE INTERNAL
VOLATILE ROWS 1000 COST 1000
AS 'pg_logical_slot_get_binary_changes'
;
----------
[CREATE_TABLE] CREATE TABLE t_1 PARTITION OF s1.t FOR VALUES FROM (1) TO (10)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float4_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_LIBRARY] -- fail, must install cube first
CREATE EXTENSION cube
;
----------
[DROP_TABLE] DROP TABLE psql_comics
;
----------
[CREATE_PROG_OBJ] -- fail, can't infer type:
create function polyf(x anyelement) returns anyrange as $$
  select array[x + 1, x + 2]
$$ language sql
;
----------
[CREATE_TABLESPACE] -- disable ENFORCE_REGRESSION_TEST_NAME_RESTRICTIONS warning
CREATE TABLESPACE pg_foo LOCATION '/no/such/location'
;
----------
[CREATE_TABLE] --
-- Test precision and scale typemods
--

CREATE TABLE num_typemod_test (
  millions numeric(3, -6),
  thousands numeric(3, -3),
  units numeric(3, 0),
  thousandths numeric(3, 3),
  millionths numeric(3, 6)
)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_opfamily
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE kd_point_tbl_ord_idx3 AS
SELECT row_number() OVER (ORDER BY p <-> '333,400') n, p <-> '333,400' dist, p
FROM kd_point_tbl WHERE p IS NOT NULL
;
----------
[CREATE_TABLE] create temp table snapshot_test (
	nr	integer,
	snap	pg_snapshot
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger bbb after insert on parted_trig for each row execute procedure trigger_notice()
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE split_event_helper.rewrite_probe
    ALTER COLUMN payload TYPE bigint USING payload::bigint
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (issn13, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA fkpart9
;
----------
[CREATE_TABLE] create temp table pp_temp_part_def partition of pp_temp_parent default
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test3"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_PROG_OBJ] -- Ambiguity should be reported only if there's not a better match available

create function dfunc(int = 1, int = 2, int = 3) returns int as $$
  select 3;
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp01a(*) (SFUNC = stfnp, STYPE = int4[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_TABLE] -- table with toast relation
CREATE TABLE regress_tblspace_test_tbl (num1 bigint, num2 double precision, t text)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33260 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] --
-- negative testcases
--
CREATE TABLE prt1_n (a int, b int, c varchar) PARTITION BY RANGE(c)
;
----------
[ALTER_TABLE] ALTER TABLE replication_metadata SET (user_catalog_table = false)
;
----------
[CREATE_TABLE] create table mclparted1 partition of mclparted for values in(1)
;
----------
[DROP_INDEX] drop index test_pg_index_toast_index
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub3 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub3 WITH (enabled = false)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER on_login_trigger ON login EXECUTE PROCEDURE on_login_proc()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube(float8[]) RETURNS cube
AS '$libdir/cube', 'cube_a_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey32_in(cstring)
RETURNS gbtreekey32
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE vac_horizon_floor_table(col1 int)
		WITH (autovacuum_enabled=false, fillfactor=10)
;
----------
[ALTER_PROG_OBJ] alter function test_strict(text) called on null input
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_default_acl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_catalog.pg_file_rename(text, text, text)
RETURNS bool
AS '$libdir/adminpack', 'pg_file_rename'
LANGUAGE C VOLATILE
;
----------
[CREATE_TYPE] create domain restrictedrange as int4range check (upper(value) < 10)
;
----------
[CREATE_PROG_OBJ] create function sp_id_user(a_login text) returns int as $$
declare x int;
begin
  select into x id from users where login = a_login;
  if found then return x; end if;
  return 0;
end$$ language plpgsql stable
;
----------
[CREATE_TABLE] CREATE TABLE footrg1 PARTITION OF footrg FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dsnowball_init(INTERNAL)
    RETURNS INTERNAL AS '$libdir/dict_snowball', 'dsnowball_init'
LANGUAGE C STRICT
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."pitest1" ALTER COLUMN "f3" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."pitest1_f3_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
