EXECUTE dumpFunc('20254')
;
GRANT EXECUTE ON FUNCTION pg_buffercache_summary() TO pg_monitor
;
-- should succeed with effect ("c" remains NULL)
BEGIN
;
explain (analyze, costs off, summary off, timing off, buffers off) execute ab_q5 (2, 3, 3)
;
-- VtA is supported for custom plans where params are substituted with
-- constants.  VtA is not supported with generic plans where params prevent
-- us from building a constant array.
PREPARE test (int, numeric, text) AS
  SELECT ten FROM onek WHERE sin(two) * four / ($3::real) IN (VALUES (sin($2)), (2), ($1))
;
GRANT regress_rls_group1 TO regress_rls_bob
;
ALTER DEFAULT PRIVILEGES GRANT ALL ON SCHEMAS TO regress_priv_user2
;
/*
 * Test concurrent abort with toast data. When streaming the second insertion, we
 * detect that the subtransaction was aborted, and reset the transaction while having
 * the TOAST changes in memory, resulting in deallocating both decoded changes and
 * TOAST reconstruction data. Memory usage counters must be updated correctly.
 */
BEGIN
;
COPY t3(id, a,b,c) FROM stdin
;
PREPARE getDomainConstraints(pg_catalog.oid) AS
SELECT tableoid, oid, conname, pg_catalog.pg_get_constraintdef(oid) AS consrc, convalidated FROM pg_catalog.pg_constraint WHERE contypid = $1 ORDER BY conname
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('20254')
;
----------
[GRANT] GRANT EXECUTE ON FUNCTION pg_buffercache_summary() TO pg_monitor
;
----------
[TRANSACTION] -- should succeed with effect ("c" remains NULL)
BEGIN
;
----------
[UNSAFE] explain (analyze, costs off, summary off, timing off, buffers off) execute ab_q5 (2, 3, 3)
;
----------
[UNSAFE] -- VtA is supported for custom plans where params are substituted with
-- constants.  VtA is not supported with generic plans where params prevent
-- us from building a constant array.
PREPARE test (int, numeric, text) AS
  SELECT ten FROM onek WHERE sin(two) * four / ($3::real) IN (VALUES (sin($2)), (2), ($1))
;
----------
[GRANT] GRANT regress_rls_group1 TO regress_rls_bob
;
----------
[GRANT] ALTER DEFAULT PRIVILEGES GRANT ALL ON SCHEMAS TO regress_priv_user2
;
----------
[TRANSACTION] /*
 * Test concurrent abort with toast data. When streaming the second insertion, we
 * detect that the subtransaction was aborted, and reset the transaction while having
 * the TOAST changes in memory, resulting in deallocating both decoded changes and
 * TOAST reconstruction data. Memory usage counters must be updated correctly.
 */
BEGIN
;
----------
[DATA_IMPORT] COPY t3(id, a,b,c) FROM stdin
;
----------
[UNSAFE] PREPARE getDomainConstraints(pg_catalog.oid) AS
SELECT tableoid, oid, conname, pg_catalog.pg_get_constraintdef(oid) AS consrc, convalidated FROM pg_catalog.pg_constraint WHERE contypid = $1 ORDER BY conname
;
