ALTER TABLE IF EXISTS tt8 ADD CONSTRAINT xxx PRIMARY KEY(f)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_opclass
;
CREATE TABLE errtst_parent (
    partid int not null,
    shdata int not null,
    data int NOT NULL DEFAULT 0,
    CONSTRAINT shdata_small CHECK(shdata < 3)
) PARTITION BY RANGE (partid)
;
-- operator without argument types
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 0 btint42cmp(int4, int2)
;
CREATE SCHEMA dump_test
;
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest2, ftest1) REFERENCES pktable(ptest1, ptest2))
;
CREATE TABLE bttest_a(id int8)
;
CREATE AGGREGATE myaggn05a(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
DROP POLICY p ON tbl1
;
CREATE TABLE regression_vacuumdb_part1 PARTITION OF regression_vacuumdb_parted FOR VALUES IN (1)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
ALTER INDEX pg_toast.pg_toast_33541_index RENAME TO reind_con_toast_idx
;
ALTER OPERATOR FAMILY alt_opf19 USING btree DROP FUNCTION 5 (int4, int4)
;
CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=20063 host=/tmp/Fjny4O22w8 dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
CREATE TABLE idx_hint (c int PRIMARY KEY)
;
create function tg_chkslotname() returns trigger as '
begin
    if substr(new.slotname, 1, 2) != tg_argv[0] then
        raise exception ''slotname must begin with %'', tg_argv[0];
    end if;
    return new;
end;
' language plpgsql
;
-- tuple -> jsonb
CREATE FUNCTION testTuple() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = (1, 'String', None)
return x
$$
;
VACUUM (FULL) pg_catalog.pg_authid
;
CREATE FUNCTION getrngfunc2(int) RETURNS setof int AS 'SELECT rngfuncid FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
CREATE TABLE tab3 ("a'" int PRIMARY KEY, "B" varchar, "c'" int)
;
CREATE SUBSCRIPTION test_tab2_sub CONNECTION 'port=22086 host=/tmp/8RkngElzkl dbname=postgres' PUBLICATION test_tab2_pub
;
ALTER FUNCTION gbt_enum_compress(internal) PARALLEL SAFE
;
ALTER PUBLICATION "dump_pub_all" ADD TABLE ONLY "pubme"."t0"
;
CREATE EXTENSION tablefunc
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
CREATE FUNCTION exec(text) returns void language plpgsql volatile
  AS $f$
    BEGIN
      EXECUTE $1;
    END;
$f$
;
ALTER TABLE temp_parted ATTACH PARTITION foreign_part DEFAULT
;
CREATE TEMP TABLE t1 (a int, b int)
;
-- cleanup
DROP TABLE test_prepared1
;
create table inh_child (a int primary key) inherits (inh_parent)
;
ANALYZE pg_temp_18.pg_temp_26711_2
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26234_2 (tid pg_catalog.tid)
;
ALTER TABLE pg_toast.pg_toast_33474 RENAME TO reind_con_toast
;
CREATE DATABASE "foo

bar"
;
CREATE UNIQUE INDEX ON test1ci (x)
;
CREATE TABLE "public"."wslot" (
    "slotname" character(20),
    "roomno" character(8),
    "slotlink" character(20),
    "backlink" character(20)
)
;
CREATE RULE "r6" AS
    ON UPDATE TO "public"."rules_src" DO INSTEAD  UPDATE "public"."rules_log" "trgt" SET "tag" = 'updated'::"text"
  WHERE ("trgt"."f1" = "new"."f1")
;
create table test ( a serial , b numeric ( 12 , 3 ) , c varchar , d varchar ( 3 ) , e char ( 4 ) , f timestamptz , g boolean , h box , i inet )
;
CREATE TABLE notnull_tbl4_cld2 (PRIMARY KEY (a) DEFERRABLE) INHERITS (notnull_tbl4)
;
CREATE TABLE PKTABLE (ptest1 numeric PRIMARY KEY)
;
create or replace function returnqueryf()
returns setof tabwithcols as $$
begin
  return query select * from tabwithcols;
  return query execute 'select * from tabwithcols';
end;
$$ language plpgsql
;
ALTER SUBSCRIPTION tap_sub SET PUBLICATION tap_pub_ins_only WITH (copy_data = false)
;
ALTER TABLE tenant_table ADD COLUMN t text
;
CREATE TABLE PRIMARY_TBL (i int PRIMARY KEY, t text)
;
drop table gs_hash_1
;
CREATE FUNCTION seg_contained(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
VACUUM ANALYZE shighway
;
-- expect clean failure

drop view usersview
;
CREATE TEMP TABLE foo (f1 serial, f2 text, f3 int default 42)
;
ANALYZE public.timetz_tbl
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE IF EXISTS tt8 ADD CONSTRAINT xxx PRIMARY KEY(f)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_opclass
;
----------
[CREATE_TABLE] CREATE TABLE errtst_parent (
    partid int not null,
    shdata int not null,
    data int NOT NULL DEFAULT 0,
    CONSTRAINT shdata_small CHECK(shdata < 3)
) PARTITION BY RANGE (partid)
;
----------
[ALTER_PROG_OBJ] -- operator without argument types
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 0 btint42cmp(int4, int2)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA dump_test
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest2, ftest1) REFERENCES pktable(ptest1, ptest2))
;
----------
[CREATE_TABLE] CREATE TABLE bttest_a(id int8)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn05a(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[DROP_POLICY] DROP POLICY p ON tbl1
;
----------
[CREATE_TABLE] CREATE TABLE regression_vacuumdb_part1 PARTITION OF regression_vacuumdb_parted FOR VALUES IN (1)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33541_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf19 USING btree DROP FUNCTION 5 (int4, int4)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=20063 host=/tmp/Fjny4O22w8 dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[CREATE_TABLE] CREATE TABLE idx_hint (c int PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] create function tg_chkslotname() returns trigger as '
begin
    if substr(new.slotname, 1, 2) != tg_argv[0] then
        raise exception ''slotname must begin with %'', tg_argv[0];
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_PROG_OBJ] -- tuple -> jsonb
CREATE FUNCTION testTuple() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = (1, 'String', None)
return x
$$
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_authid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION getrngfunc2(int) RETURNS setof int AS 'SELECT rngfuncid FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE tab3 ("a'" int PRIMARY KEY, "B" varchar, "c'" int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION test_tab2_sub CONNECTION 'port=22086 host=/tmp/8RkngElzkl dbname=postgres' PUBLICATION test_tab2_pub
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_enum_compress(internal) PARALLEL SAFE
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION "dump_pub_all" ADD TABLE ONLY "pubme"."t0"
;
----------
[CREATE_LIBRARY] CREATE EXTENSION tablefunc
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION exec(text) returns void language plpgsql volatile
  AS $f$
    BEGIN
      EXECUTE $1;
    END;
$f$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE temp_parted ATTACH PARTITION foreign_part DEFAULT
;
----------
[CREATE_TABLE] CREATE TEMP TABLE t1 (a int, b int)
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE test_prepared1
;
----------
[CREATE_TABLE] create table inh_child (a int primary key) inherits (inh_parent)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_26711_2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26234_2 (tid pg_catalog.tid)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33474 RENAME TO reind_con_toast
;
----------
[CREATE_CATALOG] CREATE DATABASE "foo

bar"
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON test1ci (x)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."wslot" (
    "slotname" character(20),
    "roomno" character(8),
    "slotlink" character(20),
    "backlink" character(20)
)
;
----------
[CREATE_POLICY] CREATE RULE "r6" AS
    ON UPDATE TO "public"."rules_src" DO INSTEAD  UPDATE "public"."rules_log" "trgt" SET "tag" = 'updated'::"text"
  WHERE ("trgt"."f1" = "new"."f1")
;
----------
[CREATE_TABLE] create table test ( a serial , b numeric ( 12 , 3 ) , c varchar , d varchar ( 3 ) , e char ( 4 ) , f timestamptz , g boolean , h box , i inet )
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl4_cld2 (PRIMARY KEY (a) DEFERRABLE) INHERITS (notnull_tbl4)
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 numeric PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] create or replace function returnqueryf()
returns setof tabwithcols as $$
begin
  return query select * from tabwithcols;
  return query execute 'select * from tabwithcols';
end;
$$ language plpgsql
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub SET PUBLICATION tap_pub_ins_only WITH (copy_data = false)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tenant_table ADD COLUMN t text
;
----------
[CREATE_TABLE] CREATE TABLE PRIMARY_TBL (i int PRIMARY KEY, t text)
;
----------
[DROP_TABLE] drop table gs_hash_1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_contained(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM ANALYZE shighway
;
----------
[DROP_VIEW] -- expect clean failure

drop view usersview
;
----------
[CREATE_TABLE] CREATE TEMP TABLE foo (f1 serial, f2 text, f3 int default 42)
;
----------
[ADMIN_TABLE] ANALYZE public.timetz_tbl
;
