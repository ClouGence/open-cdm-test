CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a > 0
;
DROP TABLE mvtest_v CASCADE
;
CREATE OPERATOR - (
	LEFTARG = hstore,
	RIGHTARG = text,
	PROCEDURE = delete
)
;
CREATE INDEX index_stats ON tab_stats(b, (b || 'data1'), (b || 'data2')) WHERE a > 0
;
CREATE FUNCTION isnlt(isbn, isbn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION "public"."boo"(integer) RETURNS integer
    LANGUAGE "plpgsql" IMMUTABLE STRICT
    AS $_$ BEGIN RAISE NOTICE 'boo: %', $1; RETURN $1; END; $_$
;
create sequence pg_temp.twophase_seq
;
ALTER TABLE pg_toast.pg_toast_28029 RENAME TO reind_con_toast
;
CREATE TABLE "public"."mlparted_def2" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
-- check that the table being attached has only columns present in the parent
CREATE TABLE fail_part (like list_parted, c int)
;
ALTER INDEX pg_toast.pg_toast_30637_index RENAME TO reind_con_toast_idx
;
CREATE TABLE pht2_p1 PARTITION OF pht2 FOR VALUES WITH (MODULUS 3, REMAINDER 0)
;
CREATE INDEX regtest_pindex_tbl4_z ON regtest_ptable_4_ones(z)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
DROP TABLE tx1
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_type
;
CREATE TABLE functional_dependencies (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b TEXT,
    filler3 DATE,
    c INT,
    d TEXT
)
;
--extractors
CREATE FUNCTION _ltree_extract_isparent(_ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FUNCTION func_stats(a text DEFAULT 'a_data', b text DEFAULT lower('b_data'))
  RETURNS text AS $$ SELECT $1::text || '_' || $2::text; $$ LANGUAGE SQL
  SET work_mem = '256kB'
;
alter table p11 add a int
;
CREATE FUNCTION testSVToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
$val = 1;
return $val;
$$
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_cast
;
-- Error
CREATE TABLE ctas_nodata (ii, jj) AS SELECT i, j FROM ctas_base
;
-- must be first write in xact
    DROP TYPE IF EXISTS basket
;
do $$
declare x var_record := row(1,2);
begin
  raise notice 'x = %', x;
end$$
;
CREATE FUNCTION citext_larger(citext, citext)
RETURNS citext
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE pht1_p2 PARTITION OF pht1 FOR VALUES WITH (MODULUS 3, REMAINDER 1)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
CREATE TABLE target_pa2 PARTITION OF target_pa FOR VALUES FROM (200) TO (1000)
;
-- cleanup
DROP TABLE list_parted, list_parted2, range_parted
;
CREATE TABLE partrem1 PARTITION OF partrem FOR VALUES IN (1)
;
--
-- different collations for different keys with same expression
--
create table coll_pruning_multi (a text) partition by range (substr(a, 1) collate "POSIX", substr(a, 1) collate "C")
;
-- first create an index with a single page range, to force compaction
-- due to exceeding the number of values per summary
CREATE INDEX brinidx_multi ON brintest_multi USING brin (
	int8col int8_minmax_multi_ops,
	int2col int2_minmax_multi_ops,
	int4col int4_minmax_multi_ops,
	oidcol oid_minmax_multi_ops,
	tidcol tid_minmax_multi_ops,
	float4col float4_minmax_multi_ops,
	float8col float8_minmax_multi_ops,
	macaddrcol macaddr_minmax_multi_ops,
	macaddr8col macaddr8_minmax_multi_ops,
	inetcol inet_minmax_multi_ops,
	cidrcol inet_minmax_multi_ops,
	datecol date_minmax_multi_ops,
	timecol time_minmax_multi_ops,
	timestampcol timestamp_minmax_multi_ops,
	timestamptzcol timestamptz_minmax_multi_ops,
	intervalcol interval_minmax_multi_ops,
	timetzcol timetz_minmax_multi_ops,
	numericcol numeric_minmax_multi_ops,
	uuidcol uuid_minmax_multi_ops,
	lsncol pg_lsn_minmax_multi_ops
)
;
CREATE TYPE base_type(INPUT = base_fn_in, OUTPUT = base_fn_out)
;
CREATE TABLE "public"."tbl_include_box_pk" (
    "c1" integer,
    "c2" integer,
    "c3" integer,
    "c4" "box"
)
;
ALTER TABLE ONLY "fkpart3"."fk" ATTACH PARTITION "fkpart3"."fk3" FOR VALUES FROM (3500) TO (5000)
;
DROP RULE rule2 ON ruletest_t1
;
CREATE TABLE xpto (
    id serial primary key,
    toasted_col1 text,
    rand1 float8 DEFAULT nextval('xpto_rand_seq'),
    toasted_col2 text,
    rand2 float8 DEFAULT nextval('xpto_rand_seq')
)
;
DROP VIEW vx1
;
CREATE INDEX test1_y_idx ON test1 USING gin (y) WITH (fastupdate = off)
;
alter index parted_replica_idx ATTACH PARTITION parted_replica_idx_1
;
CREATE VIEW rls_tbl_view AS SELECT * FROM rls_tbl
;
CREATE TABLE parted_col_comment (a int, b text) PARTITION BY LIST (a)
;
CREATE TABLE tbl1 ( a int, b int)
;
VACUUM pg_catalog.pg_shseclabel
;
CREATE TRIGGER "gtest12_03" BEFORE INSERT OR UPDATE ON "generated_virtual_tests"."gtest26" FOR EACH ROW EXECUTE FUNCTION "generated_virtual_tests"."gtest_trigger_func"()
;
create table locp (a int check (a in (2)), b text)
;
CREATE TABLESPACE tblspc1 LOCATION '/tmp/EY2MH5Kl39/tempdir/tblspc1'
;
CREATE EXTENSION invalid
;
-- fails
alter table anothertab alter column atcol1 type integer
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a > 0
;
----------
[DROP_TABLE] DROP TABLE mvtest_v CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR - (
	LEFTARG = hstore,
	RIGHTARG = text,
	PROCEDURE = delete
)
;
----------
[ADD_INDEX] CREATE INDEX index_stats ON tab_stats(b, (b || 'data1'), (b || 'data2')) WHERE a > 0
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(isbn, isbn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."boo"(integer) RETURNS integer
    LANGUAGE "plpgsql" IMMUTABLE STRICT
    AS $_$ BEGIN RAISE NOTICE 'boo: %', $1; RETURN $1; END; $_$
;
----------
[CREATE_SEQUENCE] create sequence pg_temp.twophase_seq
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28029 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted_def2" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
----------
[CREATE_TABLE] -- check that the table being attached has only columns present in the parent
CREATE TABLE fail_part (like list_parted, c int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30637_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE pht2_p1 PARTITION OF pht2 FOR VALUES WITH (MODULUS 3, REMAINDER 0)
;
----------
[ADD_INDEX] CREATE INDEX regtest_pindex_tbl4_z ON regtest_ptable_4_ones(z)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
----------
[DROP_TABLE] DROP TABLE tx1
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_type
;
----------
[CREATE_TABLE] CREATE TABLE functional_dependencies (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b TEXT,
    filler3 DATE,
    c INT,
    d TEXT
)
;
----------
[CREATE_PROG_OBJ] --extractors
CREATE FUNCTION _ltree_extract_isparent(_ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_stats(a text DEFAULT 'a_data', b text DEFAULT lower('b_data'))
  RETURNS text AS $$ SELECT $1::text || '_' || $2::text; $$ LANGUAGE SQL
  SET work_mem = '256kB'
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table p11 add a int
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testSVToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
$val = 1;
return $val;
$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_cast
;
----------
[CREATE_TABLE|SELECT] -- Error
CREATE TABLE ctas_nodata (ii, jj) AS SELECT i, j FROM ctas_base
;
----------
[DROP_TYPE] -- must be first write in xact
    DROP TYPE IF EXISTS basket
;
----------
[BLOCK] do $$
declare x var_record := row(1,2);
begin
  raise notice 'x = %', x;
end$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext_larger(citext, citext)
RETURNS citext
AS '$libdir/citext'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE pht1_p2 PARTITION OF pht1 FOR VALUES WITH (MODULUS 3, REMAINDER 1)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
----------
[CREATE_TABLE] CREATE TABLE target_pa2 PARTITION OF target_pa FOR VALUES FROM (200) TO (1000)
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE list_parted, list_parted2, range_parted
;
----------
[CREATE_TABLE] CREATE TABLE partrem1 PARTITION OF partrem FOR VALUES IN (1)
;
----------
[CREATE_TABLE] --
-- different collations for different keys with same expression
--
create table coll_pruning_multi (a text) partition by range (substr(a, 1) collate "POSIX", substr(a, 1) collate "C")
;
----------
[ADD_INDEX] -- first create an index with a single page range, to force compaction
-- due to exceeding the number of values per summary
CREATE INDEX brinidx_multi ON brintest_multi USING brin (
	int8col int8_minmax_multi_ops,
	int2col int2_minmax_multi_ops,
	int4col int4_minmax_multi_ops,
	oidcol oid_minmax_multi_ops,
	tidcol tid_minmax_multi_ops,
	float4col float4_minmax_multi_ops,
	float8col float8_minmax_multi_ops,
	macaddrcol macaddr_minmax_multi_ops,
	macaddr8col macaddr8_minmax_multi_ops,
	inetcol inet_minmax_multi_ops,
	cidrcol inet_minmax_multi_ops,
	datecol date_minmax_multi_ops,
	timecol time_minmax_multi_ops,
	timestampcol timestamp_minmax_multi_ops,
	timestamptzcol timestamptz_minmax_multi_ops,
	intervalcol interval_minmax_multi_ops,
	timetzcol timetz_minmax_multi_ops,
	numericcol numeric_minmax_multi_ops,
	uuidcol uuid_minmax_multi_ops,
	lsncol pg_lsn_minmax_multi_ops
)
;
----------
[CREATE_TYPE] CREATE TYPE base_type(INPUT = base_fn_in, OUTPUT = base_fn_out)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tbl_include_box_pk" (
    "c1" integer,
    "c2" integer,
    "c3" integer,
    "c4" "box"
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."fk" ATTACH PARTITION "fkpart3"."fk3" FOR VALUES FROM (3500) TO (5000)
;
----------
[DROP_POLICY] DROP RULE rule2 ON ruletest_t1
;
----------
[CREATE_TABLE] CREATE TABLE xpto (
    id serial primary key,
    toasted_col1 text,
    rand1 float8 DEFAULT nextval('xpto_rand_seq'),
    toasted_col2 text,
    rand2 float8 DEFAULT nextval('xpto_rand_seq')
)
;
----------
[DROP_VIEW] DROP VIEW vx1
;
----------
[ADD_INDEX] CREATE INDEX test1_y_idx ON test1 USING gin (y) WITH (fastupdate = off)
;
----------
[ALTER_INDEX] alter index parted_replica_idx ATTACH PARTITION parted_replica_idx_1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rls_tbl_view AS SELECT * FROM rls_tbl
;
----------
[CREATE_TABLE] CREATE TABLE parted_col_comment (a int, b text) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE tbl1 ( a int, b int)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_shseclabel
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "gtest12_03" BEFORE INSERT OR UPDATE ON "generated_virtual_tests"."gtest26" FOR EACH ROW EXECUTE FUNCTION "generated_virtual_tests"."gtest_trigger_func"()
;
----------
[CREATE_TABLE] create table locp (a int check (a in (2)), b text)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc1 LOCATION '/tmp/EY2MH5Kl39/tempdir/tblspc1'
;
----------
[CREATE_LIBRARY] CREATE EXTENSION invalid
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails
alter table anothertab alter column atcol1 type integer
;
