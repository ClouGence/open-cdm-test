ALTER TABLE hash_parted ATTACH PARTITION hpart_2 FOR VALUES WITH (MODULUS 4, REMAINDER 1)
;
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ADD MAPPING FOR not_a_token WITH ispell
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=28913 host=/tmp/XeBgNWHTrn dbname=postgres' PUBLICATION tap_pub
;
-- without the "offset 0", this function gets optimized quite differently

create function extractq2_2_opt(t int8_tbl) returns table(ret1 int8) as $$
  select extractq2(t)
$$ language sql immutable
;
ALTER TABLE fd_pt2 ADD CONSTRAINT fd_pt2chk1 CHECK (c1 > 0)
;
CREATE INDEX on tbl USING btree(c1, c2) INCLUDE (c3, c4)
;
CREATE TABLE gtest_err_4 (a int PRIMARY KEY, b double precision GENERATED ALWAYS AS (random()) STORED)
;
-- Change column type of parent
ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE text
;
CREATE TRIGGER "tg_slotlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_slotlink_a"('PS')
;
CREATE TEMP TABLE users (
    uid integer NOT NULL default '0',
    name varchar(60) NOT NULL default '',
    pass varchar(32) NOT NULL default '',
    
    PRIMARY KEY (uid),
    UNIQUE (name)
)
;
DROP TABLE fd_pt2
;
CREATE TRIGGER transition_table_level2_ri_child_ins_trigger
  AFTER INSERT ON transition_table_level2
  REFERENCING NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level2_ri_child_insupd_func()
;
create function mr_table_fail(i anyelement) returns table(i anyelement, r anymultirange)
  as $$ select $1, '[1,10]' $$ language sql
;
CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=11382 host=/tmp/C6o4PTAnCK dbname=postgres' PUBLICATION tap_pub_B_2 WITH (origin = none, copy_data = on)
;
CREATE TYPE stats_import.complex_type AS (
    a integer,
    b real,
    c text,
    d date,
    e jsonb)
;
ALTER TABLE ONLY test_drop_constr_parent DROP CONSTRAINT "test_drop_constr_parent_c_check"
;
DROP TABLE gtest30 CASCADE
;
-- but this doesn't work
create table rewritemetoo3 (a rewritetype)
;
-- Test copy tuple routing
create table ctrtest (a int, b text) partition by list (a)
;
CREATE OPERATOR > (
  LEFTARG = int_custom,
  RIGHTARG = int_custom,
  FUNCTION = int_custom_gt,
  COMMUTATOR = <,
  NEGATOR = <=,
  RESTRICT = scalargtsel,
  JOIN = scalargtjoinsel
)
;
DROP OPERATOR CLASS test_int4_ops USING btree
;
-- text is compatible with varchar
ALTER TABLE fktable ADD CONSTRAINT fk_4_2
FOREIGN KEY (x4) REFERENCES pktable(id2)
;
CREATE DOMAIN "public"."testdatexmldomain" AS "date"
;
CREATE TABLE part_a PARTITION OF parted FOR VALUES IN ('a')
;
/* Multiple primary keys creation should fail */
CREATE TABLE inhg (x text, LIKE inhx INCLUDING INDEXES, PRIMARY KEY(x))
;
CREATE TABLE "public"."mcv_lists" (
    "filler1" "text",
    "filler2" numeric,
    "a" integer,
    "b" character varying,
    "filler3" "date",
    "c" numeric,
    "d" character varying(64)
)
;
CREATE FUNCTION test_argresult_booltrue(x booltrue, y bool) RETURNS booltrue AS $$
begin
return y;
end
$$ LANGUAGE plpgsql
;
CREATE TABLE attmp7 () INHERITS (attmp3)
;
CREATE TABLE "public"."persons3" (
    "id" integer NOT NULL,
    "name" "text" DEFAULT ''::"text" NOT NULL
)
;
DROP SCHEMA IF EXISTS split_pg18 CASCADE
;
CREATE RULE id_alw3_ins AS ON INSERT TO id_alw3 DO INSTEAD
  WITH t1 AS (INSERT INTO id_alw1 DEFAULT VALUES RETURNING i)
    INSERT INTO id_alw2_view DEFAULT VALUES RETURNING i
;
-- detaching it first works
alter table parent detach partition child
;
ALTER OPERATOR @ (ltxtquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
-- partitions and foreign tables
CREATE TABLE reind_fdw_parent (c1 int) PARTITION BY RANGE (c1)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_ts_parser
;
AlTER EXTENSION pg_stat_statements UPDATE TO '1.6'
;
VACUUM (PARALLEL 2) pvactst
;
CREATE TABLE "public"."tt6" (
    "x" integer CONSTRAINT "tt0_x_not_null" NOT NULL,
    "y" numeric(8,2)
)
;
ALTER TABLE pg_toast.pg_toast_33633 RENAME TO reind_con_toast
;
CREATE FOREIGN TABLE ft2 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
CREATE FUNCTION test1 (anyelement) RETURNS anyarray LANGUAGE SQL
    AS ''
;
CREATE TEMP TABLE pg_temp_14.pg_temp_27052_2 (tid pg_catalog.tid)
;
create policy p1 on rls_t for select to regress_rls_alice using (true)
;
CREATE INDEX ios_bitmap_b ON ios_bitmap(b)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
CREATE INDEX hash_f8_index2 ON hash_f8_heap USING hash (random float8_ops)
	WITH (fillfactor=101)
;
COMMENT ON TYPE isbn13
	IS 'International Standard Book Number 13 (ISBN13)'
;
CREATE INDEX inetidx ON inettmp USING gist ( a )
;
create table pktable (ptest1 int, primary key(base1), unique(base1, ptest1)) inherits (pktable_base)
;
DO
LANGUAGE plpgsql
$$
DECLARE
    x constant int := 3;
    y int := 4;
BEGIN
    CALL test_proc6(2, x, y);  -- error because x is constant
END;
$$
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE hash_parted ATTACH PARTITION hpart_2 FOR VALUES WITH (MODULUS 4, REMAINDER 1)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ADD MAPPING FOR not_a_token WITH ispell
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=28913 host=/tmp/XeBgNWHTrn dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_PROG_OBJ] -- without the "offset 0", this function gets optimized quite differently

create function extractq2_2_opt(t int8_tbl) returns table(ret1 int8) as $$
  select extractq2(t)
$$ language sql immutable
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fd_pt2 ADD CONSTRAINT fd_pt2chk1 CHECK (c1 > 0)
;
----------
[ADD_INDEX] CREATE INDEX on tbl USING btree(c1, c2) INCLUDE (c3, c4)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_4 (a int PRIMARY KEY, b double precision GENERATED ALWAYS AS (random()) STORED)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Change column type of parent
ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE text
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_slotlink_a" AFTER INSERT OR DELETE OR UPDATE ON "public"."pslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_slotlink_a"('PS')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE users (
    uid integer NOT NULL default '0',
    name varchar(60) NOT NULL default '',
    pass varchar(32) NOT NULL default '',
    
    PRIMARY KEY (uid),
    UNIQUE (name)
)
;
----------
[DROP_TABLE] DROP TABLE fd_pt2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER transition_table_level2_ri_child_ins_trigger
  AFTER INSERT ON transition_table_level2
  REFERENCING NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level2_ri_child_insupd_func()
;
----------
[CREATE_PROG_OBJ] create function mr_table_fail(i anyelement) returns table(i anyelement, r anymultirange)
  as $$ select $1, '[1,10]' $$ language sql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C CONNECTION 'port=11382 host=/tmp/C6o4PTAnCK dbname=postgres' PUBLICATION tap_pub_B_2 WITH (origin = none, copy_data = on)
;
----------
[CREATE_TYPE] CREATE TYPE stats_import.complex_type AS (
    a integer,
    b real,
    c text,
    d date,
    e jsonb)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE ONLY test_drop_constr_parent DROP CONSTRAINT "test_drop_constr_parent_c_check"
;
----------
[DROP_TABLE] DROP TABLE gtest30 CASCADE
;
----------
[CREATE_TABLE] -- but this doesn't work
create table rewritemetoo3 (a rewritetype)
;
----------
[CREATE_TABLE] -- Test copy tuple routing
create table ctrtest (a int, b text) partition by list (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
  LEFTARG = int_custom,
  RIGHTARG = int_custom,
  FUNCTION = int_custom_gt,
  COMMUTATOR = <,
  NEGATOR = <=,
  RESTRICT = scalargtsel,
  JOIN = scalargtjoinsel
)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR CLASS test_int4_ops USING btree
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- text is compatible with varchar
ALTER TABLE fktable ADD CONSTRAINT fk_4_2
FOREIGN KEY (x4) REFERENCES pktable(id2)
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."testdatexmldomain" AS "date"
;
----------
[CREATE_TABLE] CREATE TABLE part_a PARTITION OF parted FOR VALUES IN ('a')
;
----------
[CREATE_TABLE] /* Multiple primary keys creation should fail */
CREATE TABLE inhg (x text, LIKE inhx INCLUDING INDEXES, PRIMARY KEY(x))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mcv_lists" (
    "filler1" "text",
    "filler2" numeric,
    "a" integer,
    "b" character varying,
    "filler3" "date",
    "c" numeric,
    "d" character varying(64)
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_argresult_booltrue(x booltrue, y bool) RETURNS booltrue AS $$
begin
return y;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE attmp7 () INHERITS (attmp3)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."persons3" (
    "id" integer NOT NULL,
    "name" "text" DEFAULT ''::"text" NOT NULL
)
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_pg18 CASCADE
;
----------
[CREATE_POLICY] CREATE RULE id_alw3_ins AS ON INSERT TO id_alw3 DO INSTEAD
  WITH t1 AS (INSERT INTO id_alw1 DEFAULT VALUES RETURNING i)
    INSERT INTO id_alw2_view DEFAULT VALUES RETURNING i
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- detaching it first works
alter table parent detach partition child
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @ (ltxtquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_TABLE] -- partitions and foreign tables
CREATE TABLE reind_fdw_parent (c1 int) PARTITION BY RANGE (c1)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_ts_parser
;
----------
[ALTER_LIBRARY] AlTER EXTENSION pg_stat_statements UPDATE TO '1.6'
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pvactst
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tt6" (
    "x" integer CONSTRAINT "tt0_x_not_null" NOT NULL,
    "y" numeric(8,2)
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33633 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft2 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1 (anyelement) RETURNS anyarray LANGUAGE SQL
    AS ''
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_14.pg_temp_27052_2 (tid pg_catalog.tid)
;
----------
[CREATE_POLICY] create policy p1 on rls_t for select to regress_rls_alice using (true)
;
----------
[ADD_INDEX] CREATE INDEX ios_bitmap_b ON ios_bitmap(b)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_extension
;
----------
[ADD_INDEX] CREATE INDEX hash_f8_index2 ON hash_f8_heap USING hash (random float8_ops)
	WITH (fillfactor=101)
;
----------
[COMMENT_TYPE] COMMENT ON TYPE isbn13
	IS 'International Standard Book Number 13 (ISBN13)'
;
----------
[ADD_INDEX] CREATE INDEX inetidx ON inettmp USING gist ( a )
;
----------
[CREATE_TABLE] create table pktable (ptest1 int, primary key(base1), unique(base1, ptest1)) inherits (pktable_base)
;
----------
[BLOCK] DO
LANGUAGE plpgsql
$$
DECLARE
    x constant int := 3;
    y int := 4;
BEGIN
    CALL test_proc6(2, x, y);  -- error because x is constant
END;
$$
;
