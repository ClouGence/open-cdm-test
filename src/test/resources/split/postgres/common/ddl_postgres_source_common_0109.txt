create view rls_v with (security_invoker) as select * from rls_t
;
ANALYZE public.test_type_diff2
;
-- MERGE tests
CREATE TABLE itest15 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
DROP TABLE FLOAT8_TBL
;
ALTER TABLE pg_toast.pg_toast_33282 RENAME TO reind_con_toast
;
DROP TABLE ctl_stats4_child
;
ALTER TABLE pg_amop ADD PRIMARY KEY USING INDEX pg_amop_oid_index
;
ALTER SUBSCRIPTION regress_mysub1 SET (failover = false)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "float" WITH "simple"
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=23259 host=/tmp/x3EXkWV6Fo dbname=postgres' PUBLICATION pub1
;
ALTER INDEX pg_toast.pg_toast_30525_index RENAME TO reind_con_toast_idx
;
create table trg_clone2 partition of trg_clone for values from (1000) to (2000)
;
CREATE FUNCTION ghstore_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- test that expression indexes and partial indexes work concurrently
CREATE INDEX CONCURRENTLY concur_index4 on concur_heap(f2) WHERE f1='a'
;
-- Check we receive an ambiguous function error when there are
-- multiple matching functions.
CREATE FUNCTION test_ambiguous_funcname(int) returns int as $$ select $1; $$ language sql
;
-- EXPLAIN
CREATE TABLE ex_mtarget (a int, b int)
  WITH (autovacuum_enabled=off)
;
CREATE VIEW xmlview7 AS SELECT xmlroot(xml '<foo/>', version no value, standalone yes)
;
CREATE OR REPLACE FUNCTION pg_relpages(IN relname regclass)
RETURNS BIGINT
AS '$libdir/pgstattuple', 'pg_relpagesbyid_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE FUNCTION gbt_int4_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION get_altertable_subcmdinfo(IN cmd pg_ddl_command,
    OUT cmdtype text,
    OUT objdesc text)
  RETURNS SETOF record IMMUTABLE STRICT
  AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE FUNCTION gin_compare_prefix_bit(bit, bit, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
drop function dfunc(int, int, int)
;
CREATE FUNCTION "public"."explain_query_json"("query_sql" "text") RETURNS TABLE("explain_line" json)
    LANGUAGE "plpgsql"
    AS $$
begin
  set enable_seqscan = off;
  set enable_bitmapscan = on;
  return query execute 'EXPLAIN (ANALYZE, FORMAT json) ' || query_sql;
end;
$$
;
DROP TABLE INSERT_CHILD
;
create table parted_copytest (
	a int,
	b int,
	c text
) partition by list (b)
;
ALTER EXTENSION test_pg_dump DROP AGGREGATE newavg(int4)
;
ANALYZE public.num_exp_sqrt
;
CREATE FUNCTION test_argresult_domain_array2(x ordered_pair_domain)
  RETURNS integer AS $$
begin
return x[1];
end
$$ LANGUAGE plpgsql
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_type
;
CREATE FUNCTION "public"."insert_tt2"("text", "text") RETURNS SETOF integer
    LANGUAGE "sql"
    AS $_$ insert into tt(data) values($1),($2) returning f1 $_$
;
CREATE TABLE foo (a int primary key, b text) PARTITION BY LIST(a)
;
DROP TABLE fk_notpartitioned_fk, fk_partitioned_pk
;
--
-- bt_page_items_bytea()
--
CREATE FUNCTION bt_page_items(IN page bytea,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
--
-- OUT/INOUT/TABLE functions
--

-- infer anymultirange from anymultirange
create function mr_outparam_succeed(i anymultirange, out r anymultirange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
-- will still be there if prepared xacts are disabled
DROP TABLE pxtest4
;
CREATE TABLE drop_stats_test()
;
CREATE SEQUENCE sequence_test8 AS integer MAXVALUE 100000
;
ALTER TABLE ONLY "public"."prt1_n" ATTACH PARTITION "public"."prt1_n_p1" FOR VALUES FROM ('0000') TO ('0250')
;
CREATE INDEX test_cube_ix ON test_cube USING gist (c)
;
--    P    P        P    N
-- should ERROR: tf1p(anyarray, anyelement) not matched by tf1p(anyarray, int)
CREATE AGGREGATE myaggp19a(BASETYPE = anyelement, SFUNC = tf1p,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=17866 host=/tmp/YJpKtfHUyD dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (two_phase = on)
;
CREATE PUBLICATION pub8 FOR TABLE test_part_c WITH (publish_via_partition_root = false)
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
CREATE TABLE test21_1 PARTITION OF test21 FOR VALUES FROM ('ABC') TO ('DEF')
;
ALTER TABLE main_table DROP CONSTRAINT main_table_a_key
;
CREATE TABLE logged1(f1 SERIAL PRIMARY KEY, f2 TEXT)
;
CREATE FUNCTION "public"."city_insert"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    ctry_id int;
begin
    if NEW.country_name IS NOT NULL then
        SELECT country_id, continent INTO ctry_id, NEW.continent
            FROM country_table WHERE country_name = NEW.country_name;
        if NOT FOUND then
            raise exception 'No such country: "%"', NEW.country_name;
        end if;
    else
        NEW.continent := NULL;
    end if;

    if NEW.city_id IS NOT NULL then
        INSERT INTO city_table
            VALUES(NEW.city_id, NEW.city_name, NEW.population, ctry_id);
    else
        INSERT INTO city_table(city_name, population, country_id)
            VALUES(NEW.city_name, NEW.population, ctry_id)
            RETURNING city_id INTO NEW.city_id;
    end if;

    RETURN NEW;
end;
$$
;
CREATE TABLE test_squash_numeric (id int, data numeric(5, 2))
;
ALTER OPERATOR >= (issn, issn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
DROP DOMAIN domain1
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] create view rls_v with (security_invoker) as select * from rls_t
;
----------
[ADMIN_TABLE] ANALYZE public.test_type_diff2
;
----------
[CREATE_TABLE] -- MERGE tests
CREATE TABLE itest15 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
----------
[DROP_TABLE] DROP TABLE FLOAT8_TBL
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33282 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] DROP TABLE ctl_stats4_child
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_amop ADD PRIMARY KEY USING INDEX pg_amop_oid_index
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_mysub1 SET (failover = false)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "float" WITH "simple"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=23259 host=/tmp/x3EXkWV6Fo dbname=postgres' PUBLICATION pub1
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30525_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table trg_clone2 partition of trg_clone for values from (1000) to (2000)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ghstore_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] -- test that expression indexes and partial indexes work concurrently
CREATE INDEX CONCURRENTLY concur_index4 on concur_heap(f2) WHERE f1='a'
;
----------
[CREATE_PROG_OBJ] -- Check we receive an ambiguous function error when there are
-- multiple matching functions.
CREATE FUNCTION test_ambiguous_funcname(int) returns int as $$ select $1; $$ language sql
;
----------
[CREATE_TABLE] -- EXPLAIN
CREATE TABLE ex_mtarget (a int, b int)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmlview7 AS SELECT xmlroot(xml '<foo/>', version no value, standalone yes)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_relpages(IN relname regclass)
RETURNS BIGINT
AS '$libdir/pgstattuple', 'pg_relpagesbyid_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int4_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION get_altertable_subcmdinfo(IN cmd pg_ddl_command,
    OUT cmdtype text,
    OUT objdesc text)
  RETURNS SETOF record IMMUTABLE STRICT
  AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_bit(bit, bit, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[DROP_PROG_OBJ] drop function dfunc(int, int, int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_query_json"("query_sql" "text") RETURNS TABLE("explain_line" json)
    LANGUAGE "plpgsql"
    AS $$
begin
  set enable_seqscan = off;
  set enable_bitmapscan = on;
  return query execute 'EXPLAIN (ANALYZE, FORMAT json) ' || query_sql;
end;
$$
;
----------
[DROP_TABLE] DROP TABLE INSERT_CHILD
;
----------
[CREATE_TABLE] create table parted_copytest (
	a int,
	b int,
	c text
) partition by list (b)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP AGGREGATE newavg(int4)
;
----------
[ADMIN_TABLE] ANALYZE public.num_exp_sqrt
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_argresult_domain_array2(x ordered_pair_domain)
  RETURNS integer AS $$
begin
return x[1];
end
$$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_type
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."insert_tt2"("text", "text") RETURNS SETOF integer
    LANGUAGE "sql"
    AS $_$ insert into tt(data) values($1),($2) returning f1 $_$
;
----------
[CREATE_TABLE] CREATE TABLE foo (a int primary key, b text) PARTITION BY LIST(a)
;
----------
[DROP_TABLE] DROP TABLE fk_notpartitioned_fk, fk_partitioned_pk
;
----------
[CREATE_PROG_OBJ] --
-- bt_page_items_bytea()
--
CREATE FUNCTION bt_page_items(IN page bytea,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items_bytea'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] --
-- OUT/INOUT/TABLE functions
--

-- infer anymultirange from anymultirange
create function mr_outparam_succeed(i anymultirange, out r anymultirange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
----------
[DROP_TABLE] -- will still be there if prepared xacts are disabled
DROP TABLE pxtest4
;
----------
[CREATE_TABLE] CREATE TABLE drop_stats_test()
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test8 AS integer MAXVALUE 100000
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_n" ATTACH PARTITION "public"."prt1_n_p1" FOR VALUES FROM ('0000') TO ('0250')
;
----------
[ADD_INDEX] CREATE INDEX test_cube_ix ON test_cube USING gist (c)
;
----------
[CREATE_PROG_OBJ] --    P    P        P    N
-- should ERROR: tf1p(anyarray, anyelement) not matched by tf1p(anyarray, int)
CREATE AGGREGATE myaggp19a(BASETYPE = anyelement, SFUNC = tf1p,
  STYPE = anyarray, FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=17866 host=/tmp/YJpKtfHUyD dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (two_phase = on)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub8 FOR TABLE test_part_c WITH (publish_via_partition_root = false)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn,
	RIGHTARG = ean13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_TABLE] CREATE TABLE test21_1 PARTITION OF test21 FOR VALUES FROM ('ABC') TO ('DEF')
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE main_table DROP CONSTRAINT main_table_a_key
;
----------
[CREATE_TABLE] CREATE TABLE logged1(f1 SERIAL PRIMARY KEY, f2 TEXT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."city_insert"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
declare
    ctry_id int;
begin
    if NEW.country_name IS NOT NULL then
        SELECT country_id, continent INTO ctry_id, NEW.continent
            FROM country_table WHERE country_name = NEW.country_name;
        if NOT FOUND then
            raise exception 'No such country: "%"', NEW.country_name;
        end if;
    else
        NEW.continent := NULL;
    end if;

    if NEW.city_id IS NOT NULL then
        INSERT INTO city_table
            VALUES(NEW.city_id, NEW.city_name, NEW.population, ctry_id);
    else
        INSERT INTO city_table(city_name, population, country_id)
            VALUES(NEW.city_name, NEW.population, ctry_id)
            RETURNING city_id INTO NEW.city_id;
    end if;

    RETURN NEW;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE test_squash_numeric (id int, data numeric(5, 2))
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (issn, issn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[DROP_TYPE] DROP DOMAIN domain1
;
