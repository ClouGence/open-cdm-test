VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
-- Check that invalidation deals with regclass constants

create temp sequence seq
;
CREATE FUNCTION make_some_array(int,int) returns int[] as
$$declare x int[];
  begin
    x[1] := $1;
    x[2] := $2;
    return x;
  end$$ language plpgsql parallel safe
;
COMMENT ON TRIGGER before_ins_stmt_trig ON main_table IS 'right'
;
ALTER INDEX pg_toast.pg_toast_33100_index RENAME TO reind_con_toast_idx
;
ALTER TABLE regtest_table_part ADD CONSTRAINT test_ck CHECK (p like '%abc%') NOT VALID
;
alter table "catalog1"."schema1"."user_table" add if not exists address varchar(255)
;
CREATE DATABASE "CamelCase"
;
alter table atacc2 inherit atacc3
;
/* contrib/pg_stat_statements/pg_stat_statements--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Register functions.
CREATE FUNCTION pg_stat_statements_reset()
RETURNS void
AS '$libdir/pg_stat_statements'
LANGUAGE C PARALLEL SAFE
;
create function convslot_trig3()
returns trigger
language plpgsql
AS $$
begin
raise notice 'trigger = %, old_table = %, new table = %',
          TG_NAME,
          (select string_agg(old_table::text, ', ' order by col1) from old_table),
          (select string_agg(new_table::text, ', ' order by col1) from new_table);
return null;
end; $$
;
/*
 * 4. Update, delete values in indexed table.
 */
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_foreign_table
;
DROP FOREIGN TABLE ft2 CASCADE
;
-- infer anyelement from anymultirange
create function mr_inoutparam_succeed(out i anyelement, inout r anymultirange)
  as $$ select upper($1), $1 $$ language sql
;
VACUUM (PARALLEL 0) pg_catalog.pg_transform
;
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p2 FOR VALUES IN ('0004', '0006')
;
create rule t1_ins_2 as on insert to t1
	where new.a >= 10 and new.a < 20
	do instead
	insert into t1_2 values (new.a)
;
CREATE TABLE fail_part (LIKE part_1 INCLUDING CONSTRAINTS)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
create function first_el(anyarray) returns anyelement as
'select $1[1]' language sql strict immutable
;
ALTER TABLE pg_toast.pg_toast_27981 RENAME TO reind_con_toast
;
create index btree_tall_idx on btree_tall_tbl (t, id) with (fillfactor = 10)
;
CREATE TABLE tbl_include_reg (c1 int, c2 int, c3 int, c4 box)
;
ALTER TABLE tab_rowfilter_partitioned ATTACH PARTITION tab_rowfilter_greater_10k FOR VALUES FROM (10000) TO (MAXVALUE)
;
COMMENT ON COLUMN ctlt3.a IS 'A3'
;
CREATE FUNCTION gtrgm_union(internal, internal)
RETURNS gtrgm
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- Clean up
DROP TRIGGER trig_row_before ON ftable
;
-- Test NULL partitions
ALTER TABLE plt1_adv DETACH PARTITION plt1_adv_p1
;
CREATE FOREIGN TABLE ft3 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
) INHERITS(ft2)
  SERVER s0
;
DROP VIEW lock_view3 CASCADE
;
drop table gs_group_1
;
truncate table pgbench_accounts, pgbench_branches, pgbench_history, pgbench_tellers
;
CREATE VIEW role_udt_grants AS
    SELECT grantor,
           grantee,
           udt_catalog,
           udt_schema,
           udt_name,
           privilege_type,
           is_grantable
    FROM udt_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
alter table idxpart_c attach partition idxpart1_c for values from (10) to (20)
;
ALTER TABLE heaptest SET TABLESPACE regress_test_stats_tblspc
;
ALTER TEXT SEARCH CONFIGURATION finnish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH finnish_stem
;
CREATE TRIGGER trigtest_after_stmt AFTER INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH STATEMENT
EXECUTE PROCEDURE dummy_trigger()
;
DROP TABLESPACE regress_test_stats_tblspc
;
ALTER TABLE ONLY notnull_tbl6 ALTER b DROP NOT NULL
;
create function unreserved_test() returns int as $$
declare
  forward int := 21;
begin
  forward := forward * 2;
  return forward;
end
$$ language plpgsql
;
-- Test with columns defined in varying orders between each level
create table part_abc (a int not null, b int not null, c int not null) partition by list (a)
;
CREATE TEMP TABLE pg_temp_3.pg_temp_27606_2 (tid pg_catalog.tid)
;
-- Check ALTER on empty reloption list
CREATE INDEX reloptions_test_idx3 ON reloptions_test (s)
;
create index tprt1_idx on tprt_1 (col1)
;
-- Clean up.
DROP TABLE hash_cleanup_heap
;
CREATE TABLE varlena_bug (v text)
;
alter index gist_pointidx SET (fillfactor = 40)
;
CREATE OPERATOR <-> (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_distance,
	COMMUTATOR = '<->'
)
;
CREATE SCHEMA pg_foo
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_proc
;
----------
[CREATE_SEQUENCE] -- Check that invalidation deals with regclass constants

create temp sequence seq
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_some_array(int,int) returns int[] as
$$declare x int[];
  begin
    x[1] := $1;
    x[2] := $2;
    return x;
  end$$ language plpgsql parallel safe
;
----------
[COMMENT_TRIGGER] COMMENT ON TRIGGER before_ins_stmt_trig ON main_table IS 'right'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33100_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE regtest_table_part ADD CONSTRAINT test_ck CHECK (p like '%abc%') NOT VALID
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table "catalog1"."schema1"."user_table" add if not exists address varchar(255)
;
----------
[CREATE_CATALOG] CREATE DATABASE "CamelCase"
;
----------
[ALTER_TABLE] alter table atacc2 inherit atacc3
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_stat_statements/pg_stat_statements--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Register functions.
CREATE FUNCTION pg_stat_statements_reset()
RETURNS void
AS '$libdir/pg_stat_statements'
LANGUAGE C PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function convslot_trig3()
returns trigger
language plpgsql
AS $$
begin
raise notice 'trigger = %, old_table = %, new table = %',
          TG_NAME,
          (select string_agg(old_table::text, ', ' order by col1) from old_table),
          (select string_agg(new_table::text, ', ' order by col1) from new_table);
return null;
end; $$
;
----------
[CREATE_TABLE] /*
 * 4. Update, delete values in indexed table.
 */
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_foreign_table
;
----------
[DROP_TABLE] DROP FOREIGN TABLE ft2 CASCADE
;
----------
[CREATE_PROG_OBJ] -- infer anyelement from anymultirange
create function mr_inoutparam_succeed(out i anyelement, inout r anymultirange)
  as $$ select upper($1), $1 $$ language sql
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_transform
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p2 FOR VALUES IN ('0004', '0006')
;
----------
[CREATE_POLICY] create rule t1_ins_2 as on insert to t1
	where new.a >= 10 and new.a < 20
	do instead
	insert into t1_2 values (new.a)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part (LIKE part_1 INCLUDING CONSTRAINTS)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_PROG_OBJ] create function first_el(anyarray) returns anyelement as
'select $1[1]' language sql strict immutable
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27981 RENAME TO reind_con_toast
;
----------
[ADD_INDEX] create index btree_tall_idx on btree_tall_tbl (t, id) with (fillfactor = 10)
;
----------
[CREATE_TABLE] CREATE TABLE tbl_include_reg (c1 int, c2 int, c3 int, c4 box)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab_rowfilter_partitioned ATTACH PARTITION tab_rowfilter_greater_10k FOR VALUES FROM (10000) TO (MAXVALUE)
;
----------
[COMMENT_COLUMN] COMMENT ON COLUMN ctlt3.a IS 'A3'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_union(internal, internal)
RETURNS gtrgm
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TRIGGER] -- Clean up
DROP TRIGGER trig_row_before ON ftable
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Test NULL partitions
ALTER TABLE plt1_adv DETACH PARTITION plt1_adv_p1
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft3 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
) INHERITS(ft2)
  SERVER s0
;
----------
[DROP_VIEW] DROP VIEW lock_view3 CASCADE
;
----------
[DROP_TABLE] drop table gs_group_1
;
----------
[TRUNCATE_TABLE] truncate table pgbench_accounts, pgbench_branches, pgbench_history, pgbench_tellers
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW role_udt_grants AS
    SELECT grantor,
           grantee,
           udt_catalog,
           udt_schema,
           udt_name,
           privilege_type,
           is_grantable
    FROM udt_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart_c attach partition idxpart1_c for values from (10) to (20)
;
----------
[ALTER_TABLE] ALTER TABLE heaptest SET TABLESPACE regress_test_stats_tblspc
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION finnish ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH finnish_stem
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trigtest_after_stmt AFTER INSERT OR UPDATE OR DELETE
ON foreign_schema.foreign_table_1
FOR EACH STATEMENT
EXECUTE PROCEDURE dummy_trigger()
;
----------
[DROP_TABLESPACE] DROP TABLESPACE regress_test_stats_tblspc
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY notnull_tbl6 ALTER b DROP NOT NULL
;
----------
[CREATE_PROG_OBJ] create function unreserved_test() returns int as $$
declare
  forward int := 21;
begin
  forward := forward * 2;
  return forward;
end
$$ language plpgsql
;
----------
[CREATE_TABLE] -- Test with columns defined in varying orders between each level
create table part_abc (a int not null, b int not null, c int not null) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_27606_2 (tid pg_catalog.tid)
;
----------
[ADD_INDEX] -- Check ALTER on empty reloption list
CREATE INDEX reloptions_test_idx3 ON reloptions_test (s)
;
----------
[ADD_INDEX] create index tprt1_idx on tprt_1 (col1)
;
----------
[DROP_TABLE] -- Clean up.
DROP TABLE hash_cleanup_heap
;
----------
[CREATE_TABLE] CREATE TABLE varlena_bug (v text)
;
----------
[ALTER_INDEX] alter index gist_pointidx SET (fillfactor = 40)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_distance,
	COMMUTATOR = '<->'
)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA pg_foo
;
