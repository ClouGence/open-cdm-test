-- Cannot set an operator that already has a negator as the negator
ALTER OPERATOR @!=(boolean, real) SET (NEGATOR = ===)
;
ALTER TABLE fk_partitioned_fk_2 DROP COLUMN fdrop1, DROP COLUMN fdrop2
;
create or replace function explain_analyze_inc_sort_nodes_without_memory(query text)
returns jsonb language plpgsql
as
$$
declare
  nodes jsonb := '[]'::jsonb;
  node jsonb;
  group_key text;
  space_key text;
begin
  for node in select * from jsonb_array_elements(explain_analyze_inc_sort_nodes(query)) t loop
    for group_key in select unnest(array['Full-sort Groups', 'Pre-sorted Groups']::text[]) t loop
      for space_key in select unnest(array['Sort Space Memory', 'Sort Space Disk']::text[]) t loop
        node := jsonb_set(node, array[group_key, space_key, 'Average Sort Space Used'], '"NN"', false);
        node := jsonb_set(node, array[group_key, space_key, 'Peak Sort Space Used'], '"NN"', false);
      end loop;
    end loop;
    nodes := nodes || node;
  end loop;
  return nodes;
end;
$$
;
CREATE DOMAIN d2 AS text COLLATE "C"
;
CREATE DATABASE "contrib_regression" TEMPLATE=template0 ENCODING='UTF8' LC_COLLATE='C' LC_CTYPE='C'
;
ALTER TABLE ONLY "public"."mlparted4" DROP COLUMN "........pg.dropped.6........"
;
CREATE TABLE otherchild (tomorrow date default now())
  INHERITS (firstparent, thirdparent)
;
ALTER TABLE test_tab1 REPLICA IDENTITY FULL
;
-- failed (not owner)
ALTER CONVERSION alt_conv1 RENAME TO alt_conv4
;
-- Test gist_page_items with included columns.
-- Non-leaf pages contain only the key attributes, and leaf pages contain
-- the included attributes.
ALTER TABLE test_gist ADD COLUMN i int DEFAULT NULL
;
CREATE TABLE pht1_e_p1 PARTITION OF pht1_e FOR VALUES WITH (MODULUS 3, REMAINDER 0)
;
create temporary view vw_ord as select * from (values (1)) v(n) join rows from(rngfunct(1),rngfunct(2)) with ordinality as z(a,b,c,d,ord) on (n=ord)
;
ALTER TABLE pg_toast.pg_toast_27915 RENAME TO reind_con_toast
;
ALTER INDEX pg_toast.pg_toast_28312_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION trans_updatetrigfunc() RETURNS trigger LANGUAGE plpgsql AS
$$
  begin
    raise notice 'trigger = %, old table = %, new table = %',
                 TG_NAME,
                 (select string_agg(old_table::text, ', ' ORDER BY a) FROM old_table),
                 (select string_agg(new_table::text, ', ' ORDER BY a) FROM new_table);
    return null;
  end;
$$
;
create table fkpart0.fk_part_56 partition of fkpart0.fk_part
    for values in (5,6) partition by list (a)
;
CREATE TRIGGER modified_any BEFORE UPDATE OF a ON main_table
FOR EACH ROW WHEN (OLD.* IS DISTINCT FROM NEW.*) EXECUTE PROCEDURE trigger_func('modified_any')
;
-- EXECUTE and WITH NO DATA, passes.
CREATE TABLE selinto_schema.tbl_nodata3 (a) AS
  EXECUTE data_sel WITH NO DATA
;
CREATE TABLE "public"."fk_partitioned_fk_4_2" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
CREATE POLICY p1 ON x1 FOR SELECT USING (a % 2 = 0)
;
create table s3.p2_2 partition of s3.p2 for values in (4, 5, 6)
;
--   group w/o existing GROUP BY target under ambiguous condition
--   into a table
CREATE TABLE test_missing_target2 AS
SELECT count(*)
FROM test_missing_target x, test_missing_target y
	WHERE x.a = y.a
	GROUP BY x.b ORDER BY x.b
;
CREATE FUNCTION func_with_set_params() RETURNS integer
    AS 'select 1;'
    LANGUAGE SQL
    SET search_path TO PG_CATALOG
    SET extra_float_digits TO 2
    SET work_mem TO '4MB'
    SET datestyle to iso, mdy
    SET local_preload_libraries TO "Mixed/Case", 'c:/''a"/path', '', '0123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789'
    IMMUTABLE STRICT
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_parameter_acl
;
CREATE TEMPORARY TABLE reload_output(
  lineno int NOT NULL GENERATED ALWAYS AS IDENTITY,
  line text
)
;
alter table at_partitioned alter column name type varchar(127)
;
ALTER TEXT SEARCH CONFIGURATION hindi ADD MAPPING
    FOR word, hword_part, hword
	WITH hindi_stem
;
DROP TYPE comp_abc
;
CREATE UNIQUE INDEX concur_exprs_index_expr
  ON concur_exprs_tab ((c1::text COLLATE "C"))
;
CREATE TABLE enumtest_child (parent rainbow REFERENCES enumtest_parent)
;
-- table rewrite should happen
ALTER TABLE t2 ADD COLUMN e domain2
;
DROP TABLE old_system_table
;
-- LIKE must respect NO INHERIT property of constraints
CREATE TABLE noinh_con_copy (a int CHECK (a > 0) NO INHERIT)
;
create table parted_conflict (a int, b text) partition by range (a)
;
CREATE FUNCTION "public"."raise_test3"(integer) RETURNS integer
    LANGUAGE "plpgsql"
    AS $_$
begin
    raise notice 'This message has no parameters (despite having %% signs in it)!';
    return $1;
end;
$_$
;
CREATE INDEX t2_brin ON s1.t2 USING BRIN (i)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "hword_numpart" WITH "public"."ispell", "english_stem"
;
-- ok
TRUNCATE TABLE trunc_c,trunc_d,trunc_e,truncate_a
;
alter table wcowrtest2 drop c
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_range
;
CREATE UNIQUE INDEX "num_exp_power_10_ln_idx" ON "public"."num_exp_power_10_ln" USING "btree" ("id")
;
create index tstidx on ltreetest using gist (t)
;
CREATE TRIGGER trig_local_before BEFORE INSERT OR UPDATE ON loc1
FOR EACH ROW EXECUTE PROCEDURE trig_row_before_insupdate()
;
CREATE TABLE fkpart11.pk11 (b text, a int NOT NULL)
;
CREATE TABLE async_pt (a int, b int, c text) PARTITION BY RANGE (a)
;
VACUUM (FULL) pg_catalog.pg_constraint
;
CREATE OPERATOR CLASS gist_interval_ops
DEFAULT FOR TYPE interval USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_intv_consistent (internal, interval, int2, oid, internal),
	FUNCTION	2	gbt_intv_union (internal, internal),
	FUNCTION	3	gbt_intv_compress (internal),
	FUNCTION	4	gbt_intv_decompress (internal),
	FUNCTION	5	gbt_intv_penalty (internal, internal, internal),
	FUNCTION	6	gbt_intv_picksplit (internal, internal),
	FUNCTION	7	gbt_intv_same (gbtreekey32, gbtreekey32, internal),
	STORAGE		gbtreekey32
;
create or replace function case_comment(int) returns text as $$
begin
  case $1
    when 1 -- comment before THEN
      then return 'one';
    else
      return 'other';
  end case;
end;
$$ language plpgsql immutable
;
DROP FUNCTION priv_testfunc2(int)
;
CREATE TABLE TEMP_GROUP (f1 INT4, f2 INT4, f3 FLOAT8)
;
------------------------------------------------------------------------------------------
[ALTER_PROG_OBJ] -- Cannot set an operator that already has a negator as the negator
ALTER OPERATOR @!=(boolean, real) SET (NEGATOR = ===)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fk_partitioned_fk_2 DROP COLUMN fdrop1, DROP COLUMN fdrop2
;
----------
[CREATE_PROG_OBJ] create or replace function explain_analyze_inc_sort_nodes_without_memory(query text)
returns jsonb language plpgsql
as
$$
declare
  nodes jsonb := '[]'::jsonb;
  node jsonb;
  group_key text;
  space_key text;
begin
  for node in select * from jsonb_array_elements(explain_analyze_inc_sort_nodes(query)) t loop
    for group_key in select unnest(array['Full-sort Groups', 'Pre-sorted Groups']::text[]) t loop
      for space_key in select unnest(array['Sort Space Memory', 'Sort Space Disk']::text[]) t loop
        node := jsonb_set(node, array[group_key, space_key, 'Average Sort Space Used'], '"NN"', false);
        node := jsonb_set(node, array[group_key, space_key, 'Peak Sort Space Used'], '"NN"', false);
      end loop;
    end loop;
    nodes := nodes || node;
  end loop;
  return nodes;
end;
$$
;
----------
[CREATE_TYPE] CREATE DOMAIN d2 AS text COLLATE "C"
;
----------
[CREATE_CATALOG] CREATE DATABASE "contrib_regression" TEMPLATE=template0 ENCODING='UTF8' LC_COLLATE='C' LC_CTYPE='C'
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted4" DROP COLUMN "........pg.dropped.6........"
;
----------
[CREATE_TABLE] CREATE TABLE otherchild (tomorrow date default now())
  INHERITS (firstparent, thirdparent)
;
----------
[ALTER_TABLE] ALTER TABLE test_tab1 REPLICA IDENTITY FULL
;
----------
[ALTER_POLICY] -- failed (not owner)
ALTER CONVERSION alt_conv1 RENAME TO alt_conv4
;
----------
[ALTER_TABLE|ADD_COLUMN] -- Test gist_page_items with included columns.
-- Non-leaf pages contain only the key attributes, and leaf pages contain
-- the included attributes.
ALTER TABLE test_gist ADD COLUMN i int DEFAULT NULL
;
----------
[CREATE_TABLE] CREATE TABLE pht1_e_p1 PARTITION OF pht1_e FOR VALUES WITH (MODULUS 3, REMAINDER 0)
;
----------
[CREATE_VIEW(SELECT)] create temporary view vw_ord as select * from (values (1)) v(n) join rows from(rngfunct(1),rngfunct(2)) with ordinality as z(a,b,c,d,ord) on (n=ord)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27915 RENAME TO reind_con_toast
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28312_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trans_updatetrigfunc() RETURNS trigger LANGUAGE plpgsql AS
$$
  begin
    raise notice 'trigger = %, old table = %, new table = %',
                 TG_NAME,
                 (select string_agg(old_table::text, ', ' ORDER BY a) FROM old_table),
                 (select string_agg(new_table::text, ', ' ORDER BY a) FROM new_table);
    return null;
  end;
$$
;
----------
[CREATE_TABLE] create table fkpart0.fk_part_56 partition of fkpart0.fk_part
    for values in (5,6) partition by list (a)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER modified_any BEFORE UPDATE OF a ON main_table
FOR EACH ROW WHEN (OLD.* IS DISTINCT FROM NEW.*) EXECUTE PROCEDURE trigger_func('modified_any')
;
----------
[CREATE_TABLE|UNSAFE] -- EXECUTE and WITH NO DATA, passes.
CREATE TABLE selinto_schema.tbl_nodata3 (a) AS
  EXECUTE data_sel WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_4_2" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON x1 FOR SELECT USING (a % 2 = 0)
;
----------
[CREATE_TABLE] create table s3.p2_2 partition of s3.p2 for values in (4, 5, 6)
;
----------
[CREATE_TABLE|SELECT] --   group w/o existing GROUP BY target under ambiguous condition
--   into a table
CREATE TABLE test_missing_target2 AS
SELECT count(*)
FROM test_missing_target x, test_missing_target y
	WHERE x.a = y.a
	GROUP BY x.b ORDER BY x.b
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_with_set_params() RETURNS integer
    AS 'select 1;'
    LANGUAGE SQL
    SET search_path TO PG_CATALOG
    SET extra_float_digits TO 2
    SET work_mem TO '4MB'
    SET datestyle to iso, mdy
    SET local_preload_libraries TO "Mixed/Case", 'c:/''a"/path', '', '0123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890123456789'
    IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_parameter_acl
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE reload_output(
  lineno int NOT NULL GENERATED ALWAYS AS IDENTITY,
  line text
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table at_partitioned alter column name type varchar(127)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION hindi ADD MAPPING
    FOR word, hword_part, hword
	WITH hindi_stem
;
----------
[DROP_TYPE] DROP TYPE comp_abc
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX concur_exprs_index_expr
  ON concur_exprs_tab ((c1::text COLLATE "C"))
;
----------
[CREATE_TABLE] CREATE TABLE enumtest_child (parent rainbow REFERENCES enumtest_parent)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- table rewrite should happen
ALTER TABLE t2 ADD COLUMN e domain2
;
----------
[DROP_TABLE] DROP TABLE old_system_table
;
----------
[CREATE_TABLE] -- LIKE must respect NO INHERIT property of constraints
CREATE TABLE noinh_con_copy (a int CHECK (a > 0) NO INHERIT)
;
----------
[CREATE_TABLE] create table parted_conflict (a int, b text) partition by range (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."raise_test3"(integer) RETURNS integer
    LANGUAGE "plpgsql"
    AS $_$
begin
    raise notice 'This message has no parameters (despite having %% signs in it)!';
    return $1;
end;
$_$
;
----------
[ADD_INDEX] CREATE INDEX t2_brin ON s1.t2 USING BRIN (i)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "hword_numpart" WITH "public"."ispell", "english_stem"
;
----------
[TRUNCATE_TABLE] -- ok
TRUNCATE TABLE trunc_c,trunc_d,trunc_e,truncate_a
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table wcowrtest2 drop c
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_range
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "num_exp_power_10_ln_idx" ON "public"."num_exp_power_10_ln" USING "btree" ("id")
;
----------
[ADD_INDEX] create index tstidx on ltreetest using gist (t)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_local_before BEFORE INSERT OR UPDATE ON loc1
FOR EACH ROW EXECUTE PROCEDURE trig_row_before_insupdate()
;
----------
[CREATE_TABLE] CREATE TABLE fkpart11.pk11 (b text, a int NOT NULL)
;
----------
[CREATE_TABLE] CREATE TABLE async_pt (a int, b int, c text) PARTITION BY RANGE (a)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_constraint
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_interval_ops
DEFAULT FOR TYPE interval USING gist
AS
	OPERATOR	1	< ,
	OPERATOR	2	<= ,
	OPERATOR	3	= ,
	OPERATOR	4	>= ,
	OPERATOR	5	> ,
	FUNCTION	1	gbt_intv_consistent (internal, interval, int2, oid, internal),
	FUNCTION	2	gbt_intv_union (internal, internal),
	FUNCTION	3	gbt_intv_compress (internal),
	FUNCTION	4	gbt_intv_decompress (internal),
	FUNCTION	5	gbt_intv_penalty (internal, internal, internal),
	FUNCTION	6	gbt_intv_picksplit (internal, internal),
	FUNCTION	7	gbt_intv_same (gbtreekey32, gbtreekey32, internal),
	STORAGE		gbtreekey32
;
----------
[CREATE_PROG_OBJ] create or replace function case_comment(int) returns text as $$
begin
  case $1
    when 1 -- comment before THEN
      then return 'one';
    else
      return 'other';
  end case;
end;
$$ language plpgsql immutable
;
----------
[DROP_PROG_OBJ] DROP FUNCTION priv_testfunc2(int)
;
----------
[CREATE_TABLE] CREATE TABLE TEMP_GROUP (f1 INT4, f2 INT4, f3 FLOAT8)
;
