CREATE AGGREGATE "public"."myaggn08b"("anyelement") (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}'
)
;
-- check rename of partitioned tables and indexes also
CREATE TABLE part_attmp (a int primary key) partition by range (a)
;
-- same behavior for copy if there is an index (interesting because rows are
-- captured by a different code path in copyfrom.c if there are indexes)
create index on parent(b)
;
ALTER TABLE ONLY "regress_indexing"."pk" ATTACH PARTITION "regress_indexing"."pk4" FOR VALUES FROM (3000) TO (4000)
;
DROP RULE IF EXISTS test_rule_exists ON no_such_schema.no_such_table
;
COMMENT ON LARGE OBJECT 34054 IS 'I Wandered Lonely as a Cloud'
;
ALTER TABLE pg_toast.pg_toast_27948 RENAME TO reind_con_toast
;
CREATE VIEW rls_view WITH (security_invoker) AS
    SELECT * FROM z1 WHERE f_leak(b)
;
CREATE OPERATOR <<% (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_op,
        COMMUTATOR = '%>>',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
ALTER TABLE ONLY "public"."pagg_tab_m" ATTACH PARTITION "public"."pagg_tab_m_p3" FOR VALUES FROM (20, 20) TO (30, 30)
;
SECURITY LABEL ON TABLE dummy_seclabel_tbl3 IS 'unclassified'
;
VACUUM ANALYZE num_exp_power_10_ln
;
-- Doesn't work for ranges, either
create type rposint as range (subtype = posint)
;
CREATE TABLE errtst(a text, b text NOT NULL, c text, secret1 text, secret2 text) PARTITION BY LIST (a)
;
-- Test comments
COMMENT ON FUNCTION tg_hub_adjustslots_wrong(bpchar, integer, integer) IS 'function with args'
;
CREATE FUNCTION make_table() RETURNS VOID
AS $$
  CREATE TABLE created_table AS SELECT * FROM int8_tbl;
$$ LANGUAGE SQL
;
DROP OPERATOR <|(bigint, bigint)
;
VACUUM (FULL) pg_catalog.pg_db_role_setting
;
-- direct partition inserts should check hash partition bound constraint

create table hash_parted (
	a int
) partition by hash (a part_test_int4_ops)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_server
;
ALTER TABLE tx1 SET SCHEMA temp_view_test
;
CREATE TABLE trgfire4 PARTITION OF trgfire FOR VALUES FROM (30) TO (40) PARTITION BY LIST (i)
;
ANALYZE tststats.priv_test_tbl
;
do $$ declare a int[];
begin a := * from onecol for update; raise notice 'a = %', a; end$$
;
alter table idxpart drop column col1, drop column col3
;
create trigger failed before insert or update or delete on parted_trig
  for each row execute procedure trigger_nothing()
;
-- failed
SECURITY LABEL ON COLUMN tpart
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
DROP TABLE instead_of_insert_tbl
;
CREATE VIEW view_stats_1 AS
  SELECT a AS col1, 2::int AS col2
    FROM generate_series(1, 10) AS tab(a) WHERE a < 5 AND a > 2
;
IMPORT FOREIGN SCHEMA public LIMIT TO (t2) FROM SERVER db2_fdw INTO public
;
CREATE TABLE fk_notpartitioned_pk (a INT, PRIMARY KEY(a), CHECK (a > 0))
;
ALTER TABLE ONLY public.tht ATTACH PARTITION public.tht_p1 FOR VALUES WITH (modulus 3, remainder 0)
;
CREATE TABLE past_inh_parent ()
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
CREATE FUNCTION gin_compare_prefix_interval(interval, interval, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE FUNCTION int_custom_out(int_custom)
RETURNS cstring
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
DROP TABLE plt2_adv_p1
;
-- only CREATE TYPE AS types may be used

DROP TABLE stuff
;
CREATE EXTENSION test_ddl_deparse
;
-- Try to create gist2 index on fast_emp4000: fail because opclass doesn't exist
CREATE INDEX grect2ind2 ON fast_emp4000 USING gist2 (home_base)
;
/*
 * text search configuration for yiddish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * yiddish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY yiddish_stem
	(TEMPLATE = snowball, Language = yiddish )
;
ALTER TABLE IF EXISTS tt8 RENAME COLUMN f TO f1
;
ALTER TABLE tbl_gist DROP COLUMN c1
;
ALTER OPERATOR ^<@ (ltree, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE FUNCTION btismn13cmp(ismn13, ismn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ANALYZE public.pagg_tab_para_p3
;
DROP TABLE ctl, receipt
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_publication_namespace
;
DROP TABLE copy_t
;
CREATE FUNCTION ginint4_consistent(internal, int2, _int4, int4, internal, internal, internal, internal)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn08b"("anyelement") (
    SFUNC = "public"."tf2p",
    STYPE = integer[],
    INITCOND = '{}'
)
;
----------
[CREATE_TABLE] -- check rename of partitioned tables and indexes also
CREATE TABLE part_attmp (a int primary key) partition by range (a)
;
----------
[ADD_INDEX] -- same behavior for copy if there is an index (interesting because rows are
-- captured by a different code path in copyfrom.c if there are indexes)
create index on parent(b)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "regress_indexing"."pk" ATTACH PARTITION "regress_indexing"."pk4" FOR VALUES FROM (3000) TO (4000)
;
----------
[DROP_POLICY] DROP RULE IF EXISTS test_rule_exists ON no_such_schema.no_such_table
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 34054 IS 'I Wandered Lonely as a Cloud'
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27948 RENAME TO reind_con_toast
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rls_view WITH (security_invoker) AS
    SELECT * FROM z1 WHERE f_leak(b)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <<% (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_op,
        COMMUTATOR = '%>>',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_m" ATTACH PARTITION "public"."pagg_tab_m_p3" FOR VALUES FROM (20, 20) TO (30, 30)
;
----------
[UNKNOWN] SECURITY LABEL ON TABLE dummy_seclabel_tbl3 IS 'unclassified'
;
----------
[ADMIN_TABLE] VACUUM ANALYZE num_exp_power_10_ln
;
----------
[CREATE_TYPE] -- Doesn't work for ranges, either
create type rposint as range (subtype = posint)
;
----------
[CREATE_TABLE] CREATE TABLE errtst(a text, b text NOT NULL, c text, secret1 text, secret2 text) PARTITION BY LIST (a)
;
----------
[COMMENT_PROG_OBJ] -- Test comments
COMMENT ON FUNCTION tg_hub_adjustslots_wrong(bpchar, integer, integer) IS 'function with args'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_table() RETURNS VOID
AS $$
  CREATE TABLE created_table AS SELECT * FROM int8_tbl;
$$ LANGUAGE SQL
;
----------
[DROP_PROG_OBJ] DROP OPERATOR <|(bigint, bigint)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_db_role_setting
;
----------
[CREATE_TABLE] -- direct partition inserts should check hash partition bound constraint

create table hash_parted (
	a int
) partition by hash (a part_test_int4_ops)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_server
;
----------
[ALTER_TABLE] ALTER TABLE tx1 SET SCHEMA temp_view_test
;
----------
[CREATE_TABLE] CREATE TABLE trgfire4 PARTITION OF trgfire FOR VALUES FROM (30) TO (40) PARTITION BY LIST (i)
;
----------
[ADMIN_TABLE] ANALYZE tststats.priv_test_tbl
;
----------
[BLOCK] do $$ declare a int[];
begin a := * from onecol for update; raise notice 'a = %', a; end$$
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart drop column col1, drop column col3
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger failed before insert or update or delete on parted_trig
  for each row execute procedure trigger_nothing()
;
----------
[UNKNOWN] -- failed
SECURITY LABEL ON COLUMN tpart
    IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
----------
[DROP_TABLE] DROP TABLE instead_of_insert_tbl
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW view_stats_1 AS
  SELECT a AS col1, 2::int AS col2
    FROM generate_series(1, 10) AS tab(a) WHERE a < 5 AND a > 2
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA public LIMIT TO (t2) FROM SERVER db2_fdw INTO public
;
----------
[CREATE_TABLE] CREATE TABLE fk_notpartitioned_pk (a INT, PRIMARY KEY(a), CHECK (a > 0))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY public.tht ATTACH PARTITION public.tht_p1 FOR VALUES WITH (modulus 3, remainder 0)
;
----------
[CREATE_TABLE] CREATE TABLE past_inh_parent ()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_interval(interval, interval, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int_custom_out(int_custom)
RETURNS cstring
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] DROP TABLE plt2_adv_p1
;
----------
[DROP_TABLE] -- only CREATE TYPE AS types may be used

DROP TABLE stuff
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ddl_deparse
;
----------
[ADD_INDEX] -- Try to create gist2 index on fast_emp4000: fail because opclass doesn't exist
CREATE INDEX grect2ind2 ON fast_emp4000 USING gist2 (home_base)
;
----------
[CREATE_POLICY] /*
 * text search configuration for yiddish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * yiddish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY yiddish_stem
	(TEMPLATE = snowball, Language = yiddish )
;
----------
[RENAME_COLUMN] ALTER TABLE IF EXISTS tt8 RENAME COLUMN f TO f1
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tbl_gist DROP COLUMN c1
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^<@ (ltree, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btismn13cmp(ismn13, ismn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab_para_p3
;
----------
[DROP_TABLE] DROP TABLE ctl, receipt
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_publication_namespace
;
----------
[DROP_TABLE] DROP TABLE copy_t
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ginint4_consistent(internal, int2, _int4, int4, internal, internal, internal, internal)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
