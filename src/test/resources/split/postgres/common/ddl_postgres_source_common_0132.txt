ALTER TABLE ONLY "public"."mlparted1" DROP COLUMN "........pg.dropped.5........"
;
CREATE TABLE parent_tbl (a text, b int) PARTITION BY LIST (a)
;
create table defcheck_def (a int, c int, b int)
;
CREATE SCHEMA matview_schema
;
CREATE TEMP TABLE pg_temp_13.pg_temp_25962_2 (tid pg_catalog.tid)
;
CREATE TRIGGER gtest4 AFTER INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (NEW.b < 0)  -- ok
  EXECUTE PROCEDURE gtest_trigger_func()
;
--
-- Test that get_steps_using_prefix() handles IS NULL clauses correctly
--
create table hp_prefix_test (a int, b int, c int, d int)
  partition by hash (a part_test_int4_ops, b part_test_int4_ops, c part_test_int4_ops, d part_test_int4_ops)
;
drop table tuplesest_parted
;
CREATE FUNCTION test1 (int) RETURNS int LANGUAGE C
    AS 'nosuchfile'
;
create table coercepart_bc partition of coercepart for values in ('bc')
;
CREATE TABLE circles (
  c1 CIRCLE,
  c2 TEXT,
  EXCLUDE USING gist
    (c1 WITH &&, (c2::circle) WITH &&)
    WHERE (circle_center(c1) <> '(0,0)')
)
;
drop view at_view_2
;
-- skipping because c2 already exists
ALTER TABLE ONLY test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer
;
CREATE INDEX index_stats ON pg_temp.tab_stats USING btree (b, (((b)::text || 'data1'::text)), (((b)::text || 'data2'::text))) WHERE (a > 0)
;
CREATE VIEW collate_dep_test3 AS SELECT text 'foo' COLLATE test0 AS foo
;
CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (9) TO (maxvalue)
;
DROP FUNCTION test_valid_server_encoding
;
CREATE TRIGGER "aft_row" AFTER INSERT OR UPDATE ON "public"."trigger_parted" FOR EACH ROW EXECUTE FUNCTION "public"."trigger_parted_trigfunc"()
;
CREATE TEMPORARY SEQUENCE seq1_temp
;
-- make sure that matview rows can be referenced as source rows (bug #9398)
CREATE TABLE mvtest_v AS SELECT generate_series(1,10) AS a
;
ALTER TABLE ONLY "public"."test_storage" ALTER COLUMN "a" SET STORAGE EXTERNAL
;
CREATE SEQUENCE ctlseq1
;
create unique index both_index_expr_key on insertconflicttest(key, lower(fruit) collate "C" text_pattern_ops)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub9
;
-- table partitions (currently not supported)

CREATE TABLE itest_parent (f1 date NOT NULL, f2 text, f3 bigint) PARTITION BY RANGE (f1)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_largeobject
;
ALTER TABLE pagg_tab_para_p3 SET (parallel_workers = 0)
;
VACUUM (ANALYZE) pg_catalog.pg_publication
;
CREATE TABLE table_two(a varchar)
;
ALTER TYPE test_type2 ADD ATTRIBUTE c text CASCADE
;
-- joins: if any of the join relations are temporary, the view
-- should also be temporary

-- should be non-temp
CREATE VIEW v3 AS
    SELECT t1.a AS t1_a, t2.a AS t2_a
    FROM base_table t1, base_table2 t2
    WHERE t1.id = t2.id
;
create function tg_system_au() returns trigger as '
begin
    if new.name != old.name then
        update IFace set sysname = new.name where sysname = old.name;
    end if;
    return new;
end;
' language plpgsql
;
drop table asptab
;
-- Clean up
DROP TABLE batch_table, batch_cp_upd_test, batch_table_p0, batch_table_p1 CASCADE
;
create operator class ctype_hash_ops
  default for type ctype using hash as
  operator 1 =(ctype, ctype)
;
-- No support for partitioned tables.
CREATE TABLE am_partitioned(x INT, y INT)
  PARTITION BY hash (x)
;
ANALYZE public.vactable(b)
;
-- error
CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
  SELECT 1 / 0 WITH NO DATA
;
CREATE AGGREGATE myaggp02a(*) (SFUNC = stfnp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
CREATE TABLE hash_name_heap (
	seqno 		int4,
	random 		name
)
;
CREATE TABLE notnull_tbl6 (a int CONSTRAINT ann NOT NULL,
	b int CONSTRAINT bnn NOT NULL, check (a > 0)) PARTITION BY LIST (a)
;
CREATE TABLE mcv_lists_bool (
    a BOOL,
    b BOOL,
    c BOOL
)
WITH (autovacuum_enabled = off)
;
ALTER TABLE ONLY "fkpart3"."fk2" DROP COLUMN "........pg.dropped.1........"
;
-- can be pushed down
DROP TRIGGER trig_row_before_insert ON rem1
;
ALTER OPERATOR & (bit, bit) SET ("Restrict" = _int_contsel, "Join" = _int_contjoinsel)
;
drop view test_view
;
create table t1 ( id integer , t text , d1 numeric , d2 float8 , c char ( 10 ) , big bigint )
;
create temp table temp_parted_oncommit_test2
  partition of temp_parted_oncommit_test
  for values in (2) on commit drop
;
CREATE TABLE rls_tbl (a int, b int, c int)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted1" DROP COLUMN "........pg.dropped.5........"
;
----------
[CREATE_TABLE] CREATE TABLE parent_tbl (a text, b int) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] create table defcheck_def (a int, c int, b int)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA matview_schema
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_25962_2 (tid pg_catalog.tid)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest4 AFTER INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  WHEN (NEW.b < 0)  -- ok
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[CREATE_TABLE] --
-- Test that get_steps_using_prefix() handles IS NULL clauses correctly
--
create table hp_prefix_test (a int, b int, c int, d int)
  partition by hash (a part_test_int4_ops, b part_test_int4_ops, c part_test_int4_ops, d part_test_int4_ops)
;
----------
[DROP_TABLE] drop table tuplesest_parted
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1 (int) RETURNS int LANGUAGE C
    AS 'nosuchfile'
;
----------
[CREATE_TABLE] create table coercepart_bc partition of coercepart for values in ('bc')
;
----------
[CREATE_TABLE] CREATE TABLE circles (
  c1 CIRCLE,
  c2 TEXT,
  EXCLUDE USING gist
    (c1 WITH &&, (c2::circle) WITH &&)
    WHERE (circle_center(c1) <> '(0,0)')
)
;
----------
[DROP_VIEW] drop view at_view_2
;
----------
[ALTER_TABLE|ADD_COLUMN] -- skipping because c2 already exists
ALTER TABLE ONLY test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer
;
----------
[ADD_INDEX] CREATE INDEX index_stats ON pg_temp.tab_stats USING btree (b, (((b)::text || 'data1'::text)), (((b)::text || 'data2'::text))) WHERE (a > 0)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW collate_dep_test3 AS SELECT text 'foo' COLLATE test0 AS foo
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted2 FOR VALUES FROM (9) TO (maxvalue)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_valid_server_encoding
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "aft_row" AFTER INSERT OR UPDATE ON "public"."trigger_parted" FOR EACH ROW EXECUTE FUNCTION "public"."trigger_parted_trigfunc"()
;
----------
[CREATE_SEQUENCE] CREATE TEMPORARY SEQUENCE seq1_temp
;
----------
[CREATE_TABLE|SELECT] -- make sure that matview rows can be referenced as source rows (bug #9398)
CREATE TABLE mvtest_v AS SELECT generate_series(1,10) AS a
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."test_storage" ALTER COLUMN "a" SET STORAGE EXTERNAL
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE ctlseq1
;
----------
[ADD_INDEX] create unique index both_index_expr_key on insertconflicttest(key, lower(fruit) collate "C" text_pattern_ops)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub9
;
----------
[CREATE_TABLE] -- table partitions (currently not supported)

CREATE TABLE itest_parent (f1 date NOT NULL, f2 text, f3 bigint) PARTITION BY RANGE (f1)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_largeobject
;
----------
[ALTER_TABLE] ALTER TABLE pagg_tab_para_p3 SET (parallel_workers = 0)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_publication
;
----------
[CREATE_TABLE] CREATE TABLE table_two(a varchar)
;
----------
[ALTER_TYPE] ALTER TYPE test_type2 ADD ATTRIBUTE c text CASCADE
;
----------
[CREATE_VIEW(SELECT)] -- joins: if any of the join relations are temporary, the view
-- should also be temporary

-- should be non-temp
CREATE VIEW v3 AS
    SELECT t1.a AS t1_a, t2.a AS t2_a
    FROM base_table t1, base_table2 t2
    WHERE t1.id = t2.id
;
----------
[CREATE_PROG_OBJ] create function tg_system_au() returns trigger as '
begin
    if new.name != old.name then
        update IFace set sysname = new.name where sysname = old.name;
    end if;
    return new;
end;
' language plpgsql
;
----------
[DROP_TABLE] drop table asptab
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE batch_table, batch_cp_upd_test, batch_table_p0, batch_table_p1 CASCADE
;
----------
[CREATE_PROG_OBJ] create operator class ctype_hash_ops
  default for type ctype using hash as
  operator 1 =(ctype, ctype)
;
----------
[CREATE_TABLE] -- No support for partitioned tables.
CREATE TABLE am_partitioned(x INT, y INT)
  PARTITION BY hash (x)
;
----------
[ADMIN_TABLE] ANALYZE public.vactable(b)
;
----------
[CREATE_VIEW(SELECT)] -- error
CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
  SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp02a(*) (SFUNC = stfnp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_TABLE] CREATE TABLE hash_name_heap (
	seqno 		int4,
	random 		name
)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl6 (a int CONSTRAINT ann NOT NULL,
	b int CONSTRAINT bnn NOT NULL, check (a > 0)) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE mcv_lists_bool (
    a BOOL,
    b BOOL,
    c BOOL
)
WITH (autovacuum_enabled = off)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "fkpart3"."fk2" DROP COLUMN "........pg.dropped.1........"
;
----------
[DROP_TRIGGER] -- can be pushed down
DROP TRIGGER trig_row_before_insert ON rem1
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR & (bit, bit) SET ("Restrict" = _int_contsel, "Join" = _int_contjoinsel)
;
----------
[DROP_VIEW] drop view test_view
;
----------
[CREATE_TABLE] create table t1 ( id integer , t text , d1 numeric , d2 float8 , c char ( 10 ) , big bigint )
;
----------
[CREATE_TABLE] create temp table temp_parted_oncommit_test2
  partition of temp_parted_oncommit_test
  for values in (2) on commit drop
;
----------
[CREATE_TABLE] CREATE TABLE rls_tbl (a int, b int, c int)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
