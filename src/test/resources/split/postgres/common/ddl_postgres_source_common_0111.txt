CREATE AGGREGATE "public"."newavg"(integer) (
    SFUNC = "int4_avg_accum",
    STYPE = bigint[],
    INITCOND = '{0,0}',
    FINALFUNC = "int8_avg"
)
;
ALTER OPERATOR >= (ltree, ltree)
  SET (RESTRICT = scalargesel, JOIN = scalargejoinsel)
;
alter index idxpart_a_b_idx attach partition idxpart1_a_b_idx
;
CREATE TABLE "public"."comment_test_child" (
    "id" "text"
)
;
CREATE INDEX onek_unique2 ON onek USING btree(unique2 int4_ops)
;
ALTER TABLE test_type_diff2_c3 INHERIT test_type_diff2
;
CREATE TABLE ctlt3 (a text CHECK (length(a) < 5), c text CHECK (length(c) < 7))
;
CREATE TABLE alt_regress_2 (a INTEGER, b INTEGER)
;
ALTER TYPE test_typex DROP ATTRIBUTE a
;
DO $$
DECLARE r record;
BEGIN
    FOR r IN SELECT rtrim(roomno) AS roomno, comment FROM Room ORDER BY roomno
    LOOP
        RAISE NOTICE '%, %', r.roomno, r.comment;
    END LOOP;
END$$
;
create table idxpart (a int, b text, c int[]) partition by range (a)
;
CREATE TABLE "public"."pagg_tab" (
    "a" integer,
    "b" integer,
    "c" "text",
    "d" integer
)
PARTITION BY LIST ("c")
;
ALTER TABLE fk_partitioned_fk_1 ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk
;
ALTER DATABASE "contrib_regression" SET bytea_output TO 'hex'
;
ALTER INDEX public.ths_mod_data_key ATTACH PARTITION public.ths_p3_mod_data_key
;
DROP SUBSCRIPTION tap_sub2
;
ALTER INDEX regress_pg_dump_schema.extdependtab_col2_idx DEPENDS ON EXTENSION test_pg_dump
;
CREATE TABLE tbl(i int)
;
ALTER TABLE pg_toast.pg_toast_28035 RENAME TO reind_con_toast
;
drop trigger trig_row_before on rem2
;
-- Resetting all values causes the column to become null
ALTER TABLE reloptions_test RESET (autovacuum_enabled,
	autovacuum_analyze_scale_factor)
;
ALTER INDEX pg_toast.pg_toast_33552_index RENAME TO reind_con_toast_idx
;
DROP FUNCTION f()
;
ALTER AGGREGATE alt_agg3(int) SET SCHEMA alt_nsp2
;
create temp table self_ref_trigger (
    id int primary key,
    parent int references self_ref_trigger,
    data text,
    nchildren int not null default 0
)
;
alter table pgbench_branches add primary key (bid)
;
CREATE FUNCTION gbt_time_consistent(internal,time,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
drop function least_accum(anycompatible, anycompatible) cascade
;
ALTER TYPE split_domain_type_next.address_type RENAME TO address_record
;
-- ensure that FULL correctly deals with new columns
ALTER TABLE table_without_key ADD COLUMN new_column text
;
-- fail because cursor has no query bound to it

create or replace function forc_bad() returns void as $$
declare
  c refcursor;
begin
  for r in c loop
    raise notice '%', r.i;
  end loop;
end;
$$ language plpgsql
;
CREATE TABLE testpub_parted (a int) PARTITION BY LIST (a)
;
CREATE FUNCTION test_assign_ordered_named_pairs(x int, y int, z int)
  RETURNS ordered_named_pair[] AS $$
declare v ordered_named_pair[] := array[row(x, y)];
begin
-- ideally this would work, but it doesn't yet:
-- v[1].j := z;
return v;
end
$$ LANGUAGE plpgsql
;
CREATE TABLE part_6 (
	c int,
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 6)
)
;
ALTER INDEX pg_toast.pg_toast_28571_index RENAME TO reind_con_toast_idx
;
ALTER TABLE copy_rel_to ENABLE ROW LEVEL SECURITY
;
CREATE FUNCTION gin_extract_value_varbit(varbit, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE FUNCTION btisbn13cmp(isbn13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_28089 RENAME TO reind_con_toast
;
drop table ab, lprt_a
;
CREATE VIEW lock_view8 WITH (security_invoker) AS SELECT * FROM lock_tbl1
;
VACUUM t
;
CREATE TABLE table_one(a varchar)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_sequence
;
CREATE TABLE ab1 (a INTEGER, b INTEGER, c INTEGER)
;
CREATE INDEX gtest22c_expr_idx ON generated_stored_tests.gtest22c USING btree (((b * 3)))
;
--
-- Check cases involving dropped/altered columns in a function's rowtype result
--

create table tt14t (f1 text, f2 text, f3 text, f4 text)
;
ANALYZE pg_temp_10.pg_temp_26642_2
;
-- generation test
TRUNCATE guid1
;
ALTER TABLE pg_toast.pg_toast_30795 RENAME TO reind_con_toast
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."newavg"(integer) (
    SFUNC = "int4_avg_accum",
    STYPE = bigint[],
    INITCOND = '{0,0}',
    FINALFUNC = "int8_avg"
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ltree, ltree)
  SET (RESTRICT = scalargesel, JOIN = scalargejoinsel)
;
----------
[ALTER_INDEX] alter index idxpart_a_b_idx attach partition idxpart1_a_b_idx
;
----------
[CREATE_TABLE] CREATE TABLE "public"."comment_test_child" (
    "id" "text"
)
;
----------
[ADD_INDEX] CREATE INDEX onek_unique2 ON onek USING btree(unique2 int4_ops)
;
----------
[ALTER_TABLE] ALTER TABLE test_type_diff2_c3 INHERIT test_type_diff2
;
----------
[CREATE_TABLE] CREATE TABLE ctlt3 (a text CHECK (length(a) < 5), c text CHECK (length(c) < 7))
;
----------
[CREATE_TABLE] CREATE TABLE alt_regress_2 (a INTEGER, b INTEGER)
;
----------
[ALTER_TYPE] ALTER TYPE test_typex DROP ATTRIBUTE a
;
----------
[BLOCK] DO $$
DECLARE r record;
BEGIN
    FOR r IN SELECT rtrim(roomno) AS roomno, comment FROM Room ORDER BY roomno
    LOOP
        RAISE NOTICE '%, %', r.roomno, r.comment;
    END LOOP;
END$$
;
----------
[CREATE_TABLE] create table idxpart (a int, b text, c int[]) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab" (
    "a" integer,
    "b" integer,
    "c" "text",
    "d" integer
)
PARTITION BY LIST ("c")
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk_1 ADD FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk
;
----------
[ALTER_CATALOG] ALTER DATABASE "contrib_regression" SET bytea_output TO 'hex'
;
----------
[ALTER_INDEX] ALTER INDEX public.ths_mod_data_key ATTACH PARTITION public.ths_p3_mod_data_key
;
----------
[DROP_PUB_SUB] DROP SUBSCRIPTION tap_sub2
;
----------
[UNKNOWN] ALTER INDEX regress_pg_dump_schema.extdependtab_col2_idx DEPENDS ON EXTENSION test_pg_dump
;
----------
[CREATE_TABLE] CREATE TABLE tbl(i int)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28035 RENAME TO reind_con_toast
;
----------
[DROP_TRIGGER] drop trigger trig_row_before on rem2
;
----------
[ALTER_TABLE] -- Resetting all values causes the column to become null
ALTER TABLE reloptions_test RESET (autovacuum_enabled,
	autovacuum_analyze_scale_factor)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33552_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] DROP FUNCTION f()
;
----------
[ALTER_PROG_OBJ] ALTER AGGREGATE alt_agg3(int) SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] create temp table self_ref_trigger (
    id int primary key,
    parent int references self_ref_trigger,
    data text,
    nchildren int not null default 0
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_branches add primary key (bid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_consistent(internal,time,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_PROG_OBJ] drop function least_accum(anycompatible, anycompatible) cascade
;
----------
[RENAME_TYPE] ALTER TYPE split_domain_type_next.address_type RENAME TO address_record
;
----------
[ALTER_TABLE|ADD_COLUMN] -- ensure that FULL correctly deals with new columns
ALTER TABLE table_without_key ADD COLUMN new_column text
;
----------
[CREATE_PROG_OBJ] -- fail because cursor has no query bound to it

create or replace function forc_bad() returns void as $$
declare
  c refcursor;
begin
  for r in c loop
    raise notice '%', r.i;
  end loop;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE testpub_parted (a int) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_assign_ordered_named_pairs(x int, y int, z int)
  RETURNS ordered_named_pair[] AS $$
declare v ordered_named_pair[] := array[row(x, y)];
begin
-- ideally this would work, but it doesn't yet:
-- v[1].j := z;
return v;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE part_6 (
	c int,
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 6)
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28571_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE] ALTER TABLE copy_rel_to ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_varbit(varbit, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btisbn13cmp(isbn13, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28089 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] drop table ab, lprt_a
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW lock_view8 WITH (security_invoker) AS SELECT * FROM lock_tbl1
;
----------
[ADMIN_TABLE] VACUUM t
;
----------
[CREATE_TABLE] CREATE TABLE table_one(a varchar)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_sequence
;
----------
[CREATE_TABLE] CREATE TABLE ab1 (a INTEGER, b INTEGER, c INTEGER)
;
----------
[ADD_INDEX] CREATE INDEX gtest22c_expr_idx ON generated_stored_tests.gtest22c USING btree (((b * 3)))
;
----------
[CREATE_TABLE] --
-- Check cases involving dropped/altered columns in a function's rowtype result
--

create table tt14t (f1 text, f2 text, f3 text, f4 text)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26642_2
;
----------
[TRUNCATE_TABLE] -- generation test
TRUNCATE guid1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30795 RENAME TO reind_con_toast
;
