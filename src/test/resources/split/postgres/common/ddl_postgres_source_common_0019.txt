CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16531 host=/tmp/wfVN1kltEQ dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_allinschema
;
ANALYZE pg_temp_13.pg_temp_26072_2
;
ALTER TABLE ab1 ALTER a SET STATISTICS -1
;
CREATE OPERATOR < (
  LEFTARG = int_custom,
  RIGHTARG = int_custom,
  FUNCTION = int_custom_lt,
  COMMUTATOR = >,
  NEGATOR = >=,
  RESTRICT = scalarltsel,
  JOIN = scalarltjoinsel
)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_range
;
create temp table copytest4 (
	c1 int,
	"colname with tab: 	" text)
;
VACUUM (PARALLEL 2) pg_catalog.pg_language
;
create type var_record as (f1 int4, f2 int4)
;
create domain d_fail int4 DEFAULT 3 + 'h'
;
ALTER TABLE fktable ADD CONSTRAINT fk_123_123
FOREIGN KEY (x1,x2,x3) REFERENCES pktable(id1,id2,id3)
;
DROP SCHEMA regress_schema_1 CASCADE
;
drop database if exists abc
;
COMMENT ON COLLATION test0 IS 'US English'
;
--
-- Test that SET DEFAULT actions recognize updates to default values
--
create temp table defp (f1 int primary key)
;
--
-- check renaming to a table's array type's autogenerated name
-- (the array type's name should get out of the way)
--
CREATE TABLE attmp_array (id int)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_ts_parser
;
-- SQLSTATE specification in WHEN
create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = '1234F', detail = 'some detail info';
  exception
    when sqlstate '1234F' then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
-- now do the same, but insert the rows with the indexes already created
-- so that we don't use the "build callback" and instead use the regular
-- approach of adding rows into existing ranges
TRUNCATE brin_test_multi_1
;
ALTER TEXT SEARCH CONFIGURATION dutch ADD MAPPING
    FOR word, hword_part, hword
	WITH dutch_stem
;
CREATE SUBSCRIPTION mysub1 CONNECTION 'port=19617 host=/tmp/mT1iHGnnPr dbname=postgres' PUBLICATION mypub, non_existent_pub
;
CREATE FUNCTION city_update() RETURNS trigger LANGUAGE plpgsql AS $$
declare
    ctry_id int;
begin
    if NEW.country_name IS DISTINCT FROM OLD.country_name then
        SELECT country_id, continent INTO ctry_id, NEW.continent
            FROM country_table WHERE country_name = NEW.country_name;
        if NOT FOUND then
            raise exception 'No such country: "%"', NEW.country_name;
        end if;

        UPDATE city_table SET city_name = NEW.city_name,
                              population = NEW.population,
                              country_id = ctry_id
            WHERE city_id = OLD.city_id;
    else
        UPDATE city_table SET city_name = NEW.city_name,
                              population = NEW.population
            WHERE city_id = OLD.city_id;
        NEW.continent := OLD.continent;
    end if;

    if NOT FOUND then RETURN NULL; end if;
    RETURN NEW;
end;
$$
;
ALTER TABLE tab5 REPLICA IDENTITY USING INDEX tab5_a_idx
;
-- Toast inherits the value from its parent table.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = false)
;
CREATE OPERATOR <-> (
	LEFTARG = timestamptz,
	RIGHTARG = timestamptz,
	PROCEDURE = tstz_dist,
	COMMUTATOR = '<->'
)
;
TRUNCATE psql_comics
;
drop schema if exists abc restrict
;
CREATE FUNCTION isnne(isbn, isbn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ANALYZE public.bt_name_heap
;
ALTER TABLE pg_collation ADD PRIMARY KEY USING INDEX pg_collation_oid_index
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION pub1
;
create table fktable2 (d int, e int, foreign key (d, e) references pktable2)
;
COMMENT ON TABLE datatype_table IS 'This table should contain all native datatypes'
;
ALTER OPERATOR #<=# (hstore, hstore) SET (
       RESTRICT = scalarlesel,
       JOIN = scalarlejoinsel
)
;
CREATE TRIGGER gtest12_01 BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func()
;
CREATE AGGREGATE "public"."myaggn09a"(integer) (
    SFUNC = "public"."tf1p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
DROP TABLE pg_temp_4.pg_temp_27487_2, pg_temp_4.pg_temp_27487
;
CREATE FUNCTION test_dsa_basic()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE TABLE copy_encoding_tab (t text)
;
CREATE TABLE "public"."constr_parent" (
    "a" integer
)
;
create table inh_child2(f1 int)
;
CREATE INDEX i_temptest ON temptest(tcol)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_event_trigger
;
CREATE TABLE test_like_4 (b int DEFAULT 42,
  c int GENERATED ALWAYS AS (a * 2) STORED,
  a int CHECK (a > 0))
;
ALTER TABLE inht1 RENAME b TO bb
;
CREATE TABLE pgss_ctas AS SELECT a, 'ctas' b FROM generate_series(1, 10) a
;
CREATE TABLE "public"."pitest1_p1" (
    "f1" "date" NOT NULL,
    "f2" "text",
    "f3" bigint NOT NULL
)
;
CALL sum_two(1,2)
;
-- adding row trigger with transition table fails
create trigger child_row_trig
  after insert on child referencing new table as new_table
  for each row execute procedure dump_insert()
;
-- record hashing with non-hashable field type
CREATE TYPE hash_test_t2 AS (a money, b text)
;
-- check that violating rows are correctly reported
CREATE TABLE part_2 (LIKE list_parted2)
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16531 host=/tmp/wfVN1kltEQ dbname=postgres application_name=tap_sub' PUBLICATION tap_pub_x, tap_pub_allinschema
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26072_2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ab1 ALTER a SET STATISTICS -1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
  LEFTARG = int_custom,
  RIGHTARG = int_custom,
  FUNCTION = int_custom_lt,
  COMMUTATOR = >,
  NEGATOR = >=,
  RESTRICT = scalarltsel,
  JOIN = scalarltjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_range
;
----------
[CREATE_TABLE] create temp table copytest4 (
	c1 int,
	"colname with tab: 	" text)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_language
;
----------
[CREATE_TYPE] create type var_record as (f1 int4, f2 int4)
;
----------
[CREATE_TYPE] create domain d_fail int4 DEFAULT 3 + 'h'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_123_123
FOREIGN KEY (x1,x2,x3) REFERENCES pktable(id1,id2,id3)
;
----------
[DROP_SCHEMA] DROP SCHEMA regress_schema_1 CASCADE
;
----------
[DROP_CATALOG] drop database if exists abc
;
----------
[ALTER_POLICY] COMMENT ON COLLATION test0 IS 'US English'
;
----------
[CREATE_TABLE] --
-- Test that SET DEFAULT actions recognize updates to default values
--
create temp table defp (f1 int primary key)
;
----------
[CREATE_TABLE] --
-- check renaming to a table's array type's autogenerated name
-- (the array type's name should get out of the way)
--
CREATE TABLE attmp_array (id int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_ts_parser
;
----------
[CREATE_PROG_OBJ] -- SQLSTATE specification in WHEN
create or replace function raise_test() returns void as $$
begin
  raise 'check me'
     using errcode = '1234F', detail = 'some detail info';
  exception
    when sqlstate '1234F' then
      raise notice 'SQLSTATE: % SQLERRM: %', sqlstate, sqlerrm;
      raise;
end;
$$ language plpgsql
;
----------
[TRUNCATE_TABLE] -- now do the same, but insert the rows with the indexes already created
-- so that we don't use the "build callback" and instead use the regular
-- approach of adding rows into existing ranges
TRUNCATE brin_test_multi_1
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION dutch ADD MAPPING
    FOR word, hword_part, hword
	WITH dutch_stem
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub1 CONNECTION 'port=19617 host=/tmp/mT1iHGnnPr dbname=postgres' PUBLICATION mypub, non_existent_pub
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION city_update() RETURNS trigger LANGUAGE plpgsql AS $$
declare
    ctry_id int;
begin
    if NEW.country_name IS DISTINCT FROM OLD.country_name then
        SELECT country_id, continent INTO ctry_id, NEW.continent
            FROM country_table WHERE country_name = NEW.country_name;
        if NOT FOUND then
            raise exception 'No such country: "%"', NEW.country_name;
        end if;

        UPDATE city_table SET city_name = NEW.city_name,
                              population = NEW.population,
                              country_id = ctry_id
            WHERE city_id = OLD.city_id;
    else
        UPDATE city_table SET city_name = NEW.city_name,
                              population = NEW.population
            WHERE city_id = OLD.city_id;
        NEW.continent := OLD.continent;
    end if;

    if NOT FOUND then RETURN NULL; end if;
    RETURN NEW;
end;
$$
;
----------
[ALTER_TABLE] ALTER TABLE tab5 REPLICA IDENTITY USING INDEX tab5_a_idx
;
----------
[ALTER_TABLE] -- Toast inherits the value from its parent table.
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = false)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = timestamptz,
	RIGHTARG = timestamptz,
	PROCEDURE = tstz_dist,
	COMMUTATOR = '<->'
)
;
----------
[TRUNCATE_TABLE] TRUNCATE psql_comics
;
----------
[DROP_SCHEMA] drop schema if exists abc restrict
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(isbn, isbn)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.bt_name_heap
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_collation ADD PRIMARY KEY USING INDEX pg_collation_oid_index
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TABLE] create table fktable2 (d int, e int, foreign key (d, e) references pktable2)
;
----------
[COMMENT_TABLE] COMMENT ON TABLE datatype_table IS 'This table should contain all native datatypes'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR #<=# (hstore, hstore) SET (
       RESTRICT = scalarlesel,
       JOIN = scalarlejoinsel
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest12_01 BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn09a"(integer) (
    SFUNC = "public"."tf1p",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_27487_2, pg_temp_4.pg_temp_27487
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_dsa_basic()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE copy_encoding_tab (t text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."constr_parent" (
    "a" integer
)
;
----------
[CREATE_TABLE] create table inh_child2(f1 int)
;
----------
[ADD_INDEX] CREATE INDEX i_temptest ON temptest(tcol)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_event_trigger
;
----------
[CREATE_TABLE] CREATE TABLE test_like_4 (b int DEFAULT 42,
  c int GENERATED ALWAYS AS (a * 2) STORED,
  a int CHECK (a > 0))
;
----------
[RENAME_COLUMN] ALTER TABLE inht1 RENAME b TO bb
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE pgss_ctas AS SELECT a, 'ctas' b FROM generate_series(1, 10) a
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pitest1_p1" (
    "f1" "date" NOT NULL,
    "f2" "text",
    "f3" bigint NOT NULL
)
;
----------
[CALL_PROG_OBJ] CALL sum_two(1,2)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- adding row trigger with transition table fails
create trigger child_row_trig
  after insert on child referencing new table as new_table
  for each row execute procedure dump_insert()
;
----------
[CREATE_TYPE] -- record hashing with non-hashable field type
CREATE TYPE hash_test_t2 AS (a money, b text)
;
----------
[CREATE_TABLE] -- check that violating rows are correctly reported
CREATE TABLE part_2 (LIKE list_parted2)
;
