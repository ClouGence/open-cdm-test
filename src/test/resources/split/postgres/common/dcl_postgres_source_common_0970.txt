CREATE FOREIGN DATA WRAPPER dblink_fdw VALIDATOR dblink_fdw_validator
;
LOCK TABLE public.tab_core_types IN ACCESS SHARE MODE
;
PREPARE q5(int, text) AS
	SELECT * FROM tenk1 WHERE unique1 = $1 OR stringu1 = $2
	ORDER BY unique1
;
GRANT SELECT ON domain_udt_usage TO PUBLIC
;
COPY public.mlparted_defd (a, b, c) TO stdout
;
-- database non-superuser with bypass privilege can bypass RLS policy when disabled
SET SESSION AUTHORIZATION regress_rls_exempt_user
;
COPY public.generated_cols TO STDOUT
;
SET SCHEMA 'public'
;
DROP STATISTICS mcv_lists_stats
;
EXECUTE dumpOpr('1420')
;
------------------------------------------------------------------------------------------
[SYSTEM_SETTING_WRITE] CREATE FOREIGN DATA WRAPPER dblink_fdw VALIDATOR dblink_fdw_validator
;
----------
[SESSION_LOCK] LOCK TABLE public.tab_core_types IN ACCESS SHARE MODE
;
----------
[UNSAFE] PREPARE q5(int, text) AS
	SELECT * FROM tenk1 WHERE unique1 = $1 OR stringu1 = $2
	ORDER BY unique1
;
----------
[GRANT] GRANT SELECT ON domain_udt_usage TO PUBLIC
;
----------
[DATA_EXPORT] COPY public.mlparted_defd (a, b, c) TO stdout
;
----------
[SWITCH_USER] -- database non-superuser with bypass privilege can bypass RLS policy when disabled
SET SESSION AUTHORIZATION regress_rls_exempt_user
;
----------
[DATA_EXPORT] COPY public.generated_cols TO STDOUT
;
----------
[SWITCH_SCHEMA] SET SCHEMA 'public'
;
----------
[ADMIN_PERFORMANCE] DROP STATISTICS mcv_lists_stats
;
----------
[UNSAFE] EXECUTE dumpOpr('1420')
;
