EXECUTE dumpBaseType('4072')
;
-- fail, privileges remain
DROP ROLE regress_host_resource_newadmin
;
CREATE STATISTICS s10 (ndistinct) ON a, b, (2*c) FROM ndistinct
;
CREATE STATISTICS s_col ON id, col FROM stats_ext_tbl
;
EXECUTE dumpFunc('18501')
;
-- replace statistics by somewhat overlapping ones (this expected to get worse estimate
-- because the first statistics shall be applied to 3 columns, and the second one can't
-- be really applied)

DROP STATISTICS s11
;
EXECUTE dumpFunc('40750')
;
-- fail - must be owner of subscription
SET ROLE regress_subscription_user_dummy
;
COPY  test_cube FROM STDIN
;
ALTER TABLE public.notnull_child_upg OWNER TO zyc
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpBaseType('4072')
;
----------
[DROP_ROLE] -- fail, privileges remain
DROP ROLE regress_host_resource_newadmin
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS s10 (ndistinct) ON a, b, (2*c) FROM ndistinct
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS s_col ON id, col FROM stats_ext_tbl
;
----------
[UNSAFE] EXECUTE dumpFunc('18501')
;
----------
[ADMIN_PERFORMANCE] -- replace statistics by somewhat overlapping ones (this expected to get worse estimate
-- because the first statistics shall be applied to 3 columns, and the second one can't
-- be really applied)

DROP STATISTICS s11
;
----------
[UNSAFE] EXECUTE dumpFunc('40750')
;
----------
[SWITCH_ROLE] -- fail - must be owner of subscription
SET ROLE regress_subscription_user_dummy
;
----------
[DATA_IMPORT] COPY  test_cube FROM STDIN
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.notnull_child_upg OWNER TO zyc
;
