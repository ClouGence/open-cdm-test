EXECUTE dumpOpr('642')
;
REFRESH MATERIALIZED VIEW sro_mv
;
ALTER DEFAULT PRIVILEGES FOR ROLE regress_addr_user IN SCHEMA public GRANT ALL ON TABLES TO regress_addr_user
;
CREATE USER MAPPING FOR regress_dump_test_role SERVER s1
;
EXECUTE dumpTableAttach('17944')
;
EXECUTE dumpFunc('27140')
;
CREATE ROLE regress_rol_op3
;
SAVEPOINT x
;
-- Test serial full hash join.
-- Resetting parallel_setup_cost should force a serial plan.
-- Just to be safe, however, set enable_parallel_hash to off, as parallel full
-- hash joins are only supported with shared hashtables.
RESET parallel_setup_cost
;
--
-- Verify that index scans encountering dead rows produced by an
-- aborted subtransaction of the current transaction can utilize the
-- kill_prio_tuple optimization
--
-- NB: The table size is currently *not* expected to stay the same, we
-- don't have logic to trigger opportunistic pruning in cases like
-- this.
BEGIN
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpOpr('642')
;
----------
[ADMIN] REFRESH MATERIALIZED VIEW sro_mv
;
----------
[GRANT] ALTER DEFAULT PRIVILEGES FOR ROLE regress_addr_user IN SCHEMA public GRANT ALL ON TABLES TO regress_addr_user
;
----------
[GRANT] CREATE USER MAPPING FOR regress_dump_test_role SERVER s1
;
----------
[UNSAFE] EXECUTE dumpTableAttach('17944')
;
----------
[UNSAFE] EXECUTE dumpFunc('27140')
;
----------
[CREATE_ROLE] CREATE ROLE regress_rol_op3
;
----------
[TRANSACTION] SAVEPOINT x
;
----------
[SESSION_SETTING_WRITE] -- Test serial full hash join.
-- Resetting parallel_setup_cost should force a serial plan.
-- Just to be safe, however, set enable_parallel_hash to off, as parallel full
-- hash joins are only supported with shared hashtables.
RESET parallel_setup_cost
;
----------
[TRANSACTION] --
-- Verify that index scans encountering dead rows produced by an
-- aborted subtransaction of the current transaction can utilize the
-- kill_prio_tuple optimization
--
-- NB: The table size is currently *not* expected to stay the same, we
-- don't have logic to trigger opportunistic pruning in cases like
-- this.
BEGIN
;
