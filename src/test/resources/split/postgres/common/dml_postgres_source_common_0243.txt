-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4660', relminmxid = '1'
WHERE oid = '21413'
;
-- should now fail, because parent's publication replicates updates
UPDATE testpub_parted1 SET a = 1
;
INSERT INTO public.ths_p1 VALUES (8, 78)
;
insert into cth values(DEFAULT,'test1','01 March 2003','test_result','PASS')
;
INSERT INTO TIMESTAMPTZ_TBL VALUES ('tomorrow zulu')
;
INSERT INTO num_exp_sub VALUES (3,2,'34338496.525397047')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14686', relminmxid = '3'
WHERE oid = '"public"."idxpart1"'::pg_catalog.regclass
;
-- degenerate polygons
INSERT INTO POLYGON_TBL(f1) VALUES ('(0.0,0.0)')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16838', relminmxid = '3'
WHERE oid = '"public"."pht2_p2"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16935', relminmxid = '3'
WHERE oid = '38860'
;
-- ok
INSERT INTO rw_view1(a) VALUES (10)
;
INSERT INTO num_exp_div VALUES (3,4,'.00000055260225961552')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."brintest"'::pg_catalog.regclass
;
insert into rule_and_refint_t1 values (2, 22)
;
INSERT INTO num_exp_sub VALUES (7,4,'-90827946.4119')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10432', relminmxid = '1'
WHERE oid = '29030'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '16450'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11971', relminmxid = '1'
WHERE oid = '"public"."rtest_t8"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8636', relminmxid = '1'
WHERE oid = '"public"."rtest_view2"'::pg_catalog.regclass
;
INSERT INTO new_measurement VALUES (1, '2006-03-01', 20, 10)
;
INSERT INTO expr_key (x, t)
SELECT d1::numeric, d1::text FROM (
    SELECT round((d / pi())::numeric, 7) AS d1 FROM generate_series(1, 20) AS d
) t
;
INSERT INTO orders (id, name) VALUES (1, 'order of olivier (1)')
;
insert into hs3 values (113)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1537', relminmxid = '1'
WHERE oid = '"public"."d_star"'::pg_catalog.regclass
;
-- failed
DELETE FROM t5
;
insert into a1 ( id , t , d1 , d2 , c ) values ( default , null , null , null , null )
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pht1_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- test FK referenced updates SET NULL
INSERT INTO temporal_rng (id, valid_at) VALUES ('[9,10)', daterange('2018-01-01', '2021-01-01'))
;
--
-- test with rows already
--

DELETE FROM temporal_fk_mltrng2mltrng
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"fkpart5"."fk11"'::pg_catalog.regclass
;
INSERT INTO FKTABLE VALUES (NULL, NULL, 0)
;
INSERT INTO gtest21a (a) VALUES (1)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16838', relminmxid = '3'
WHERE oid = '38828'
;
INSERT INTO local_tbl SELECT id, id % 10, to_char(id, 'FM0000') FROM generate_series(1, 1000) id
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1523', relminmxid = '1'
WHERE oid = '"public"."emp"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14328', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_para_p2"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (5, 835)
;
INSERT INTO public.tht VALUES ('0', 620)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16370', relminmxid = '3'
WHERE oid = '"public"."test_type_diff2_c1"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '16410'
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pt_part2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
UPDATE pvactst SET i = i WHERE i < 1000
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '27809'
;
INSERT INTO ins_copy VALUES (DEFAULT, 1)
;
INSERT INTO table_dropped_index_no_pk VALUES (1,1,1), (2,2,2), (3,3,3)
;
INSERT INTO parent VALUES ( 1, 'p1' )
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."mlparted4"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relispopulated = 't'
WHERE oid = '"public"."mvtest_tvmm"'::pg_catalog.regclass
;
-- with a conflict
INSERT INTO temporal_rng (id, valid_at) VALUES ('[1,2)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT ON CONSTRAINT temporal_rng_pk DO NOTHING
;
UPDATE large SET datab = 5
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4660', relminmxid = '1'
WHERE oid = '21413'
;
----------
[UPDATE] -- should now fail, because parent's publication replicates updates
UPDATE testpub_parted1 SET a = 1
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 78)
;
----------
[INSERT] insert into cth values(DEFAULT,'test1','01 March 2003','test_result','PASS')
;
----------
[INSERT] INSERT INTO TIMESTAMPTZ_TBL VALUES ('tomorrow zulu')
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (3,2,'34338496.525397047')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14686', relminmxid = '3'
WHERE oid = '"public"."idxpart1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- degenerate polygons
INSERT INTO POLYGON_TBL(f1) VALUES ('(0.0,0.0)')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16838', relminmxid = '3'
WHERE oid = '"public"."pht2_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16935', relminmxid = '3'
WHERE oid = '38860'
;
----------
[INSERT|SELECT] -- ok
INSERT INTO rw_view1(a) VALUES (10)
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (3,4,'.00000055260225961552')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."brintest"'::pg_catalog.regclass
;
----------
[INSERT] insert into rule_and_refint_t1 values (2, 22)
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (7,4,'-90827946.4119')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10432', relminmxid = '1'
WHERE oid = '29030'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '16450'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '11971', relminmxid = '1'
WHERE oid = '"public"."rtest_t8"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8636', relminmxid = '1'
WHERE oid = '"public"."rtest_view2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO new_measurement VALUES (1, '2006-03-01', 20, 10)
;
----------
[INSERT|SELECT] INSERT INTO expr_key (x, t)
SELECT d1::numeric, d1::text FROM (
    SELECT round((d / pi())::numeric, 7) AS d1 FROM generate_series(1, 20) AS d
) t
;
----------
[INSERT|SELECT] INSERT INTO orders (id, name) VALUES (1, 'order of olivier (1)')
;
----------
[INSERT] insert into hs3 values (113)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1537', relminmxid = '1'
WHERE oid = '"public"."d_star"'::pg_catalog.regclass
;
----------
[DELETE] -- failed
DELETE FROM t5
;
----------
[INSERT|SELECT] insert into a1 ( id , t , d1 , d2 , c ) values ( default , null , null , null , null )
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pht1_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT|SELECT] -- test FK referenced updates SET NULL
INSERT INTO temporal_rng (id, valid_at) VALUES ('[9,10)', daterange('2018-01-01', '2021-01-01'))
;
----------
[DELETE] --
-- test with rows already
--

DELETE FROM temporal_fk_mltrng2mltrng
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"fkpart5"."fk11"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO FKTABLE VALUES (NULL, NULL, 0)
;
----------
[INSERT|SELECT] INSERT INTO gtest21a (a) VALUES (1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16838', relminmxid = '3'
WHERE oid = '38828'
;
----------
[INSERT|SELECT] INSERT INTO local_tbl SELECT id, id % 10, to_char(id, 'FM0000') FROM generate_series(1, 1000) id
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1523', relminmxid = '1'
WHERE oid = '"public"."emp"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14328', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_para_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 835)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 620)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16370', relminmxid = '3'
WHERE oid = '"public"."test_type_diff2_c1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '16410'
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pt_part2"'::pg_catalog.regclass
  AND attname IN ('a', 'b')
;
----------
[UPDATE] UPDATE pvactst SET i = i WHERE i < 1000
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '27809'
;
----------
[INSERT] INSERT INTO ins_copy VALUES (DEFAULT, 1)
;
----------
[INSERT] INSERT INTO table_dropped_index_no_pk VALUES (1,1,1), (2,2,2), (3,3,3)
;
----------
[INSERT] INSERT INTO parent VALUES ( 1, 'p1' )
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."mlparted4"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relispopulated = 't'
WHERE oid = '"public"."mvtest_tvmm"'::pg_catalog.regclass
;
----------
[MERGE|SELECT] -- with a conflict
INSERT INTO temporal_rng (id, valid_at) VALUES ('[1,2)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT ON CONSTRAINT temporal_rng_pk DO NOTHING
;
----------
[UPDATE] UPDATE large SET datab = 5
;
