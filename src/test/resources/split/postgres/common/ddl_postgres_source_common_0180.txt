ALTER TEXT SEARCH CONFIGURATION nepali ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
ALTER OPERATOR >= (ean13, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
CREATE TABLE pagg_tab6 (c text collate case_insensitive, b text collate case_insensitive) PARTITION BY LIST (b collate case_insensitive)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
ALTER INDEX pg_toast.pg_toast_33508_index RENAME TO reind_con_toast_idx
;
-- function number should be between 1 and 5
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 6 btint42cmp(int4, int2)
;
-- Now try domain over fixed-length type
CREATE DOMAIN sqljson_char2 AS char(2) CHECK (VALUE NOT IN ('12'))
;
DROP OPERATOR !! (NONE, priv_testdomain1)
;
CREATE TABLE constraint_rename_cache (a int,
  CONSTRAINT chk_a CHECK (a > 0),
  PRIMARY KEY (a))
;
-- test jsonb string -> python string
CREATE FUNCTION test1string(val jsonb) RETURNS text
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == "a")
return val
$$
;
drop trigger trig_row_before_insert on loc2
;
DROP FUNCTION wait_for_backend_termination(int)
;
ANALYZE pg_temp_3.pg_temp_27281_2
;
ALTER TABLE inhts RENAME d TO dd
;
ALTER TABLE tab_default ADD COLUMN b bool DEFAULT false NOT NULL
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=16952 host=/tmp/EYZ95ycoqO dbname=postgres' PUBLICATION mypub
;
--
-- Support functions for indexing.
--

CREATE FUNCTION citext_cmp(citext, citext)
RETURNS int4
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- generation expression must be immutable
CREATE TABLE gtest_err_4 (a int PRIMARY KEY, b double precision GENERATED ALWAYS AS (random()) STORED)
;
ALTER TABLE ONLY "public"."notnull_tbl4_cld3" INHERIT "public"."notnull_tbl4"
;
create unique index PLine_name on PLine using btree (slotname bpchar_ops)
;
CALL ptest1($1)
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
VACUUM (PARALLEL 0) pg_catalog.pg_default_acl
;
CREATE UNIQUE INDEX cwi_uniq_idx ON cwi_test(a , b)
;
CREATE INDEX ptif_test0_index ON ONLY ptif_test0 (a)
;
ALTER TABLE fk_p_1 ATTACH PARTITION fk_p_1_2 FOR VALUES IN (2)
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.datetime("HH:MI TZH") < $.datetime("HH:MI TZH"))'))
;
CREATE FUNCTION "public"."rangetypes_sql"("q" "anyrange", "b" "anyarray", OUT "c" "anyelement") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$ select upper($1) + $2[1] $_$
;
CREATE FUNCTION dep_req3b() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT public.dep_req2() || ' req3b' $$
;
create function explain_parallel_append(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off, buffers off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+(?:\.\d+)? loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        perform regexp_matches(ln, 'Index Searches: \d+');
        if found then
          continue;
        end if;
        return next ln;
    end loop;
end;
$$
;
ALTER TABLE ONLY "generated_virtual_tests"."gtestnn_parent" ATTACH PARTITION "generated_virtual_tests"."gtestnn_childdef" DEFAULT
;
CREATE TABLE plt1_adv_p2 PARTITION OF plt1_adv FOR VALUES IN ('0003', '0004')
;
DROP FUNCTION explain_json(text)
;
CREATE TABLE gtest_err_7b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (row_number() OVER (ORDER BY a)) STORED)
;
ALTER TEXT SEARCH CONFIGURATION catalan ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
CREATE INDEX "bt_i4_index" ON "public"."bt_i4_heap" USING "btree" ("seqno")
;
CREATE INDEX "sp_radix_ind" ON "public"."radix_text_tbl" USING "spgist" ("t")
;
CREATE TEMP TABLE sometable(f1 int)
;
ANALYZE pg_catalog.pg_namespace
;
create table parted2_stmt_trig1 partition of parted2_stmt_trig for values in (1)
;
DROP EXTENSION hstore CASCADE
;
-- ************************************************************
-- * Describe the backside of a patchfield slot
-- ************************************************************
create function pslot_backlink_view(bpchar)
returns text as '
<<outer>>
declare
    rec		record;
    bltype	char(2);
    retval	text;
begin
    select into rec * from PSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if rec.backlink = '''' then
        return ''-'';
    end if;
    bltype := substr(rec.backlink, 1, 2);
    if bltype = ''PL'' then
        declare
	    rec		record;
	begin
	    select into rec * from PLine where slotname = "outer".rec.backlink;
	    retval := ''Phone line '' || trim(rec.phonenumber);
	    if rec.comment != '''' then
	        retval := retval || '' ('';
		retval := retval || rec.comment;
		retval := retval || '')'';
	    end if;
	    return retval;
	end;
    end if;
    if bltype = ''WS'' then
        select into rec * from WSlot where slotname = rec.backlink;
	retval := trim(rec.slotname) || '' in room '';
	retval := retval || trim(rec.roomno);
	retval := retval || '' -> '';
	return retval || wslot_slotlink_view(rec.slotname);
    end if;
    return rec.backlink;
end;
' language plpgsql
;
CREATE FUNCTION "public"."explain_analyze_inc_sort_nodes_without_memory"("query" "text") RETURNS "jsonb"
    LANGUAGE "plpgsql"
    AS $$
declare
  nodes jsonb := '[]'::jsonb;
  node jsonb;
  group_key text;
  space_key text;
begin
  for node in select * from jsonb_array_elements(explain_analyze_inc_sort_nodes(query)) t loop
    for group_key in select unnest(array['Full-sort Groups', 'Pre-sorted Groups']::text[]) t loop
      for space_key in select unnest(array['Sort Space Memory', 'Sort Space Disk']::text[]) t loop
        node := jsonb_set(node, array[group_key, space_key, 'Average Sort Space Used'], '"NN"', false);
        node := jsonb_set(node, array[group_key, space_key, 'Peak Sort Space Used'], '"NN"', false);
      end loop;
    end loop;
    nodes := nodes || node;
  end loop;
  return nodes;
end;
$$
;
create sequence ttdummy_seq increment 10 start 0 minvalue 0
;
IMPORT FOREIGN SCHEMA public LIMIT TO (t) FROM SERVER db1_fdw INTO public
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
ALTER TABLE tbl2 ALTER COLUMN val3 TYPE text
;
CREATE TEXT SEARCH DICTIONARY hunspell_err (
						Template=ispell,
						DictFile=hunspell_sample_num,
						AffFile=hunspell_sample_long
)
;
CREATE TABLE attmp (initial int4)
;
ANALYZE t11
;
------------------------------------------------------------------------------------------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION nepali ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ean13, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab6 (c text collate case_insensitive, b text collate case_insensitive) PARTITION BY LIST (b collate case_insensitive)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33508_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_PROG_OBJ] -- function number should be between 1 and 5
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 6 btint42cmp(int4, int2)
;
----------
[CREATE_TYPE] -- Now try domain over fixed-length type
CREATE DOMAIN sqljson_char2 AS char(2) CHECK (VALUE NOT IN ('12'))
;
----------
[DROP_PROG_OBJ] DROP OPERATOR !! (NONE, priv_testdomain1)
;
----------
[CREATE_TABLE] CREATE TABLE constraint_rename_cache (a int,
  CONSTRAINT chk_a CHECK (a > 0),
  PRIMARY KEY (a))
;
----------
[CREATE_PROG_OBJ] -- test jsonb string -> python string
CREATE FUNCTION test1string(val jsonb) RETURNS text
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == "a")
return val
$$
;
----------
[DROP_TRIGGER] drop trigger trig_row_before_insert on loc2
;
----------
[DROP_PROG_OBJ] DROP FUNCTION wait_for_backend_termination(int)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27281_2
;
----------
[RENAME_COLUMN] ALTER TABLE inhts RENAME d TO dd
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tab_default ADD COLUMN b bool DEFAULT false NOT NULL
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=16952 host=/tmp/EYZ95ycoqO dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_PROG_OBJ] --
-- Support functions for indexing.
--

CREATE FUNCTION citext_cmp(citext, citext)
RETURNS int4
AS '$libdir/citext'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- generation expression must be immutable
CREATE TABLE gtest_err_4 (a int PRIMARY KEY, b double precision GENERATED ALWAYS AS (random()) STORED)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."notnull_tbl4_cld3" INHERIT "public"."notnull_tbl4"
;
----------
[ADD_INDEX] create unique index PLine_name on PLine using btree (slotname bpchar_ops)
;
----------
[CALL_PROG_OBJ] CALL ptest1($1)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ean13,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_default_acl
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX cwi_uniq_idx ON cwi_test(a , b)
;
----------
[ADD_INDEX] CREATE INDEX ptif_test0_index ON ONLY ptif_test0 (a)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_p_1 ATTACH PARTITION fk_p_1_2 FOR VALUES IN (2)
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.datetime("HH:MI TZH") < $.datetime("HH:MI TZH"))'))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."rangetypes_sql"("q" "anyrange", "b" "anyarray", OUT "c" "anyelement") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$ select upper($1) + $2[1] $_$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dep_req3b() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT public.dep_req2() || ' req3b' $$
;
----------
[CREATE_PROG_OBJ] create function explain_parallel_append(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off, buffers off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+(?:\.\d+)? loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        perform regexp_matches(ln, 'Index Searches: \d+');
        if found then
          continue;
        end if;
        return next ln;
    end loop;
end;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "generated_virtual_tests"."gtestnn_parent" ATTACH PARTITION "generated_virtual_tests"."gtestnn_childdef" DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE plt1_adv_p2 PARTITION OF plt1_adv FOR VALUES IN ('0003', '0004')
;
----------
[DROP_PROG_OBJ] DROP FUNCTION explain_json(text)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_7b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (row_number() OVER (ORDER BY a)) STORED)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION catalan ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[ADD_INDEX] CREATE INDEX "bt_i4_index" ON "public"."bt_i4_heap" USING "btree" ("seqno")
;
----------
[ADD_INDEX] CREATE INDEX "sp_radix_ind" ON "public"."radix_text_tbl" USING "spgist" ("t")
;
----------
[CREATE_TABLE] CREATE TEMP TABLE sometable(f1 int)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_namespace
;
----------
[CREATE_TABLE] create table parted2_stmt_trig1 partition of parted2_stmt_trig for values in (1)
;
----------
[DROP_LIBRARY] DROP EXTENSION hstore CASCADE
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * Describe the backside of a patchfield slot
-- ************************************************************
create function pslot_backlink_view(bpchar)
returns text as '
<<outer>>
declare
    rec		record;
    bltype	char(2);
    retval	text;
begin
    select into rec * from PSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if rec.backlink = '''' then
        return ''-'';
    end if;
    bltype := substr(rec.backlink, 1, 2);
    if bltype = ''PL'' then
        declare
	    rec		record;
	begin
	    select into rec * from PLine where slotname = "outer".rec.backlink;
	    retval := ''Phone line '' || trim(rec.phonenumber);
	    if rec.comment != '''' then
	        retval := retval || '' ('';
		retval := retval || rec.comment;
		retval := retval || '')'';
	    end if;
	    return retval;
	end;
    end if;
    if bltype = ''WS'' then
        select into rec * from WSlot where slotname = rec.backlink;
	retval := trim(rec.slotname) || '' in room '';
	retval := retval || trim(rec.roomno);
	retval := retval || '' -> '';
	return retval || wslot_slotlink_view(rec.slotname);
    end if;
    return rec.backlink;
end;
' language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_analyze_inc_sort_nodes_without_memory"("query" "text") RETURNS "jsonb"
    LANGUAGE "plpgsql"
    AS $$
declare
  nodes jsonb := '[]'::jsonb;
  node jsonb;
  group_key text;
  space_key text;
begin
  for node in select * from jsonb_array_elements(explain_analyze_inc_sort_nodes(query)) t loop
    for group_key in select unnest(array['Full-sort Groups', 'Pre-sorted Groups']::text[]) t loop
      for space_key in select unnest(array['Sort Space Memory', 'Sort Space Disk']::text[]) t loop
        node := jsonb_set(node, array[group_key, space_key, 'Average Sort Space Used'], '"NN"', false);
        node := jsonb_set(node, array[group_key, space_key, 'Peak Sort Space Used'], '"NN"', false);
      end loop;
    end loop;
    nodes := nodes || node;
  end loop;
  return nodes;
end;
$$
;
----------
[CREATE_SEQUENCE] create sequence ttdummy_seq increment 10 start 0 minvalue 0
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA public LIMIT TO (t) FROM SERVER db1_fdw INTO public
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl2 ALTER COLUMN val3 TYPE text
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hunspell_err (
						Template=ispell,
						DictFile=hunspell_sample_num,
						AffFile=hunspell_sample_long
)
;
----------
[CREATE_TABLE] CREATE TABLE attmp (initial int4)
;
----------
[ADMIN_TABLE] ANALYZE t11
;
