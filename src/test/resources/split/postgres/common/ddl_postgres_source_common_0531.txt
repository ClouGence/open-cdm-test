VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_subscription_rel
;
ALTER PUBLICATION tap_pub ADD TABLE tab_rep, tab_full, tab_full2, tab_mixed, tab_include, tab_nothing, tab_full_pk, tab_no_replidentity_index
;
CREATE TABLE s2.t2(a int)
;
CREATE INDEX test_index1 ON testschema.test_default_tab_p USING btree (val)
;
-- cleanup
DROP VIEW upview
;
create table idxpart3 partition of idxpart for values from ('ccc') to ('ddd')
;
CREATE TABLE test_bit (
	i bit(3)
)
;
CREATE TABLE "public"."tab_core_types" (
    "point" "point",
    "line" "line",
    "lseg" "lseg",
    "box" "box",
    "openedpath" "path",
    "closedpath" "path",
    "polygon" "polygon",
    "circle" "circle",
    "date" "date",
    "time" time without time zone,
    "timestamp" timestamp without time zone,
    "timetz" time with time zone,
    "timestamptz" timestamp with time zone,
    "interval" interval,
    "json" "json",
    "jsonb" "jsonb",
    "jsonpath" "jsonpath",
    "inet" "inet",
    "cidr" "cidr",
    "macaddr8" "macaddr8",
    "macaddr" "macaddr",
    "int2" smallint,
    "int4" integer,
    "int8" bigint,
    "float4" real,
    "float8" double precision,
    "pi" numeric,
    "char" "char",
    "bpchar" "bpchar",
    "varchar" character varying,
    "name" "name",
    "text" "text",
    "bool" boolean,
    "bytea" "bytea",
    "bit" bit(1),
    "varbit" bit varying,
    "money" "money",
    "refcursor" "refcursor",
    "int2vector" "int2vector",
    "oidvector" "oidvector",
    "aclitem" "aclitem",
    "tsvector" "tsvector",
    "tsquery" "tsquery",
    "uuid" "uuid",
    "xid8" "xid8",
    "regclass" "regclass",
    "type" "regtype",
    "regrole" "regrole",
    "oid" "oid",
    "tid" "tid",
    "xid" "xid",
    "cid" "cid",
    "txid_snapshot" "txid_snapshot",
    "pg_snapshot" "pg_snapshot",
    "pg_lsn" "pg_lsn",
    "cardinal_number" "information_schema"."cardinal_number",
    "character_data" "information_schema"."character_data",
    "sql_identifier" "information_schema"."sql_identifier",
    "time_stamp" "information_schema"."time_stamp",
    "yes_or_no" "information_schema"."yes_or_no",
    "planets" "public"."planets",
    "insenum" "public"."insenum",
    "int4range" "int4range",
    "int4multirange" "int4multirange",
    "int8range" "int8range",
    "int8multirange" "int8multirange",
    "float8range" "public"."float8range",
    "float8multirange" "public"."float8multirange",
    "numrange" "numrange",
    "nummultirange" "nummultirange",
    "textrange" "public"."textrange",
    "textmultirange" "public"."textmultirange",
    "cashrange" "public"."cashrange",
    "cashmultirange" "public"."cashmultirange",
    "daterange" "daterange",
    "datemultirange" "datemultirange",
    "tsrange" "tsrange",
    "tsmultirange" "tsmultirange",
    "tstzrange" "tstzrange",
    "tstzmultirange" "tstzmultirange",
    "arrayrange" "public"."arrayrange",
    "arraymultirange" "public"."arraymultirange"
)
;
CREATE TEMP TABLE new_table (f1 float8)
;
DROP TABLE constraint_rename_test2
;
--ERROR
IMPORT FOREIGN SCHEMA s1 EXCEPT (t1) FROM SERVER s9 INTO public
;
CREATE TABLE tab1 (a int PRIMARY KEY)
;
SECURITY LABEL ON ROLE regress_seclabel_user1 IS '...invalid label...'
;
alter table ctrtest attach partition remp2 for values in (2)
;
-- generated NOT NULL constraint names must not collide with explicitly named constraints
CREATE TABLE two_not_null_constraints (
   col integer NOT NULL,
   CONSTRAINT two_not_null_constraints_col_not_null CHECK (col IS NOT NULL)
)
;
CREATE TABLE "fkpart3"."pk52" (
    "a" integer NOT NULL
)
;
DROP TABLE child_local
;
CREATE TYPE unknown_comptype AS (
	u unknown    -- fail
)
;
ALTER OPERATOR >= (isbn, isbn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
ALTER TABLE FKTABLE ADD CONSTRAINT fkdi2 FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION NOT DEFERRABLE
;
/*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
CREATE TEMP TABLE department (
	id INTEGER PRIMARY KEY,  
	parent_department INTEGER REFERENCES department, 
	name TEXT 
)
;
CREATE INDEX vacuum_in_leader_small_index ON parallel_vacuum_table((1))
;
-- OK
CREATE TEMPORARY TABLE unlogged2 (a int primary key)
;
ALTER TABLE pg_range ADD PRIMARY KEY USING INDEX pg_range_rngtypid_index
;
create unlogged table spgist_unlogged_tbl(id serial, b box)
;
-- No validation is done on the encoding itself, just the length to avoid
-- overruns, so some of the byte sequences below are bogus.  They cover
-- all code branches, server encodings only for now.
CREATE TABLE encoding_tests (encoding text, description text, input bytea)
;
DO $$ BEGIN
PERFORM pg_terminate_backend(pid, 180000) FROM pg_stat_activity
  WHERE application_name = 'fdw_conn_check';
END $$
;
CREATE FUNCTION postgres_fdw_abs(int) RETURNS int AS $$
BEGIN
RETURN abs($1);
END
$$ LANGUAGE plpgsql IMMUTABLE
;
CREATE UNIQUE INDEX t1_btree_unique ON s1.t1 USING BTREE (i)
;
DROP TABLE group_agg_pk
;
-- First enforce presence of temporary schema.
CREATE TEMP TABLE test_ext4_tab ()
;
CREATE SEQUENCE seq3
;
CREATE FUNCTION lck_shr(bigint) RETURNS bool IMMUTABLE LANGUAGE plpgsql AS $$
     BEGIN PERFORM pg_advisory_lock_shared($1); RETURN true; END;
  $$
;
CREATE TABLE schema_one.table_one(a int)
;
-- omit COLUMN
ALTER TABLE test_add_column
	DROP IF EXISTS c6
;
CREATE TABLE t1(c1 int, c2 int GENERATED ALWAYS AS (c1 + 2) STORED, c3 int GENERATED ALWAYS AS (c1 + 2) STORED)
;
-- Fail if option is specified twice
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=30, fillfactor=40)
;
CREATE TABLE testschema.test_default_tab(id bigint) TABLESPACE regress_tblspace
;
CREATE FUNCTION test1n(val ltree) RETURNS int
LANGUAGE plpython2u
TRANSFORM FOR TYPE ltree
AS $$
plpy.info(repr(val))
return len(val)
$$
;
CREATE TABLE test22_1 PARTITION OF test22 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
drop table inh_parent1, inh_parent2, inh_child1, inh_child2
;
CREATE TABLE person (
    id          INT NOT NULL PRIMARY KEY,
	name 		text,
	age			int4,
	location 	point
)
;
ALTER INDEX pg_toast.pg_toast_27755_index RENAME TO reind_con_toast_idx
;
--
-- CALLED ON NULL INPUT | RETURNS NULL ON NULL INPUT | STRICT
--
CREATE FUNCTION functest_F_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 50'
;
-- failed (no role membership)
ALTER OPERATOR @+@(int4, int4) SET SCHEMA alt_nsp2
;
CREATE TABLE plt2_adv_p2 PARTITION OF plt2_adv FOR VALUES IN ('0004', '0006')
;
-- fail
alter table atacc3 no inherit atacc2
;
-- should fail
drop domain restrictedrange
;
ALTER TABLE notnull_tbl1 ATTACH PARTITION notnull_tbl1_3 FOR VALUES IN (NULL,5)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_subscription_rel
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub ADD TABLE tab_rep, tab_full, tab_full2, tab_mixed, tab_include, tab_nothing, tab_full_pk, tab_no_replidentity_index
;
----------
[CREATE_TABLE] CREATE TABLE s2.t2(a int)
;
----------
[ADD_INDEX] CREATE INDEX test_index1 ON testschema.test_default_tab_p USING btree (val)
;
----------
[DROP_VIEW] -- cleanup
DROP VIEW upview
;
----------
[CREATE_TABLE] create table idxpart3 partition of idxpart for values from ('ccc') to ('ddd')
;
----------
[CREATE_TABLE] CREATE TABLE test_bit (
	i bit(3)
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tab_core_types" (
    "point" "point",
    "line" "line",
    "lseg" "lseg",
    "box" "box",
    "openedpath" "path",
    "closedpath" "path",
    "polygon" "polygon",
    "circle" "circle",
    "date" "date",
    "time" time without time zone,
    "timestamp" timestamp without time zone,
    "timetz" time with time zone,
    "timestamptz" timestamp with time zone,
    "interval" interval,
    "json" "json",
    "jsonb" "jsonb",
    "jsonpath" "jsonpath",
    "inet" "inet",
    "cidr" "cidr",
    "macaddr8" "macaddr8",
    "macaddr" "macaddr",
    "int2" smallint,
    "int4" integer,
    "int8" bigint,
    "float4" real,
    "float8" double precision,
    "pi" numeric,
    "char" "char",
    "bpchar" "bpchar",
    "varchar" character varying,
    "name" "name",
    "text" "text",
    "bool" boolean,
    "bytea" "bytea",
    "bit" bit(1),
    "varbit" bit varying,
    "money" "money",
    "refcursor" "refcursor",
    "int2vector" "int2vector",
    "oidvector" "oidvector",
    "aclitem" "aclitem",
    "tsvector" "tsvector",
    "tsquery" "tsquery",
    "uuid" "uuid",
    "xid8" "xid8",
    "regclass" "regclass",
    "type" "regtype",
    "regrole" "regrole",
    "oid" "oid",
    "tid" "tid",
    "xid" "xid",
    "cid" "cid",
    "txid_snapshot" "txid_snapshot",
    "pg_snapshot" "pg_snapshot",
    "pg_lsn" "pg_lsn",
    "cardinal_number" "information_schema"."cardinal_number",
    "character_data" "information_schema"."character_data",
    "sql_identifier" "information_schema"."sql_identifier",
    "time_stamp" "information_schema"."time_stamp",
    "yes_or_no" "information_schema"."yes_or_no",
    "planets" "public"."planets",
    "insenum" "public"."insenum",
    "int4range" "int4range",
    "int4multirange" "int4multirange",
    "int8range" "int8range",
    "int8multirange" "int8multirange",
    "float8range" "public"."float8range",
    "float8multirange" "public"."float8multirange",
    "numrange" "numrange",
    "nummultirange" "nummultirange",
    "textrange" "public"."textrange",
    "textmultirange" "public"."textmultirange",
    "cashrange" "public"."cashrange",
    "cashmultirange" "public"."cashmultirange",
    "daterange" "daterange",
    "datemultirange" "datemultirange",
    "tsrange" "tsrange",
    "tsmultirange" "tsmultirange",
    "tstzrange" "tstzrange",
    "tstzmultirange" "tstzmultirange",
    "arrayrange" "public"."arrayrange",
    "arraymultirange" "public"."arraymultirange"
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE new_table (f1 float8)
;
----------
[DROP_TABLE] DROP TABLE constraint_rename_test2
;
----------
[CREATE_TABLE] --ERROR
IMPORT FOREIGN SCHEMA s1 EXCEPT (t1) FROM SERVER s9 INTO public
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (a int PRIMARY KEY)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON ROLE regress_seclabel_user1 IS '...invalid label...'
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table ctrtest attach partition remp2 for values in (2)
;
----------
[CREATE_TABLE] -- generated NOT NULL constraint names must not collide with explicitly named constraints
CREATE TABLE two_not_null_constraints (
   col integer NOT NULL,
   CONSTRAINT two_not_null_constraints_col_not_null CHECK (col IS NOT NULL)
)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart3"."pk52" (
    "a" integer NOT NULL
)
;
----------
[DROP_TABLE] DROP TABLE child_local
;
----------
[CREATE_TYPE] CREATE TYPE unknown_comptype AS (
	u unknown    -- fail
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (isbn, isbn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD CONSTRAINT fkdi2 FOREIGN KEY(ftest1) REFERENCES pktable
  ON DELETE CASCADE ON UPDATE NO ACTION NOT DEFERRABLE
;
----------
[CREATE_POLICY] /*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE department (
	id INTEGER PRIMARY KEY,  
	parent_department INTEGER REFERENCES department, 
	name TEXT 
)
;
----------
[ADD_INDEX] CREATE INDEX vacuum_in_leader_small_index ON parallel_vacuum_table((1))
;
----------
[CREATE_TABLE] -- OK
CREATE TEMPORARY TABLE unlogged2 (a int primary key)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_range ADD PRIMARY KEY USING INDEX pg_range_rngtypid_index
;
----------
[CREATE_TABLE] create unlogged table spgist_unlogged_tbl(id serial, b box)
;
----------
[CREATE_TABLE] -- No validation is done on the encoding itself, just the length to avoid
-- overruns, so some of the byte sequences below are bogus.  They cover
-- all code branches, server encodings only for now.
CREATE TABLE encoding_tests (encoding text, description text, input bytea)
;
----------
[BLOCK] DO $$ BEGIN
PERFORM pg_terminate_backend(pid, 180000) FROM pg_stat_activity
  WHERE application_name = 'fdw_conn_check';
END $$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION postgres_fdw_abs(int) RETURNS int AS $$
BEGIN
RETURN abs($1);
END
$$ LANGUAGE plpgsql IMMUTABLE
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t1_btree_unique ON s1.t1 USING BTREE (i)
;
----------
[DROP_TABLE] DROP TABLE group_agg_pk
;
----------
[CREATE_TABLE] -- First enforce presence of temporary schema.
CREATE TEMP TABLE test_ext4_tab ()
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE seq3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lck_shr(bigint) RETURNS bool IMMUTABLE LANGUAGE plpgsql AS $$
     BEGIN PERFORM pg_advisory_lock_shared($1); RETURN true; END;
  $$
;
----------
[CREATE_TABLE] CREATE TABLE schema_one.table_one(a int)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- omit COLUMN
ALTER TABLE test_add_column
	DROP IF EXISTS c6
;
----------
[CREATE_TABLE] CREATE TABLE t1(c1 int, c2 int GENERATED ALWAYS AS (c1 + 2) STORED, c3 int GENERATED ALWAYS AS (c1 + 2) STORED)
;
----------
[CREATE_TABLE] -- Fail if option is specified twice
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor=30, fillfactor=40)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.test_default_tab(id bigint) TABLESPACE regress_tblspace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1n(val ltree) RETURNS int
LANGUAGE plpython2u
TRANSFORM FOR TYPE ltree
AS $$
plpy.info(repr(val))
return len(val)
$$
;
----------
[CREATE_TABLE] CREATE TABLE test22_1 PARTITION OF test22 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[DROP_TABLE] drop table inh_parent1, inh_parent2, inh_child1, inh_child2
;
----------
[CREATE_TABLE] CREATE TABLE person (
    id          INT NOT NULL PRIMARY KEY,
	name 		text,
	age			int4,
	location 	point
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27755_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] --
-- CALLED ON NULL INPUT | RETURNS NULL ON NULL INPUT | STRICT
--
CREATE FUNCTION functest_F_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 50'
;
----------
[ALTER_PROG_OBJ] -- failed (no role membership)
ALTER OPERATOR @+@(int4, int4) SET SCHEMA alt_nsp2
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p2 PARTITION OF plt2_adv FOR VALUES IN ('0004', '0006')
;
----------
[ALTER_TABLE] -- fail
alter table atacc3 no inherit atacc2
;
----------
[DROP_TYPE] -- should fail
drop domain restrictedrange
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE notnull_tbl1 ATTACH PARTITION notnull_tbl1_3 FOR VALUES IN (NULL,5)
;
