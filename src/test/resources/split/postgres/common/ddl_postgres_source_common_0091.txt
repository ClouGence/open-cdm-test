alter table alttype_cluster cluster on alttype_cluster_ind
;
-- but ordinary functions are not
create function missing_return_expr() returns int as $$
begin
    perform 2+2;
end;$$ language plpgsql
;
drop table inh_nn1, inh_nn2, inh_nn3, inh_nn4
;
-- In the default case, dboid and roleoid fall back to their respective GUCs.
CREATE FUNCTION worker_spi_launch(index int4,
  dboid oid DEFAULT 0,
  roleoid oid DEFAULT 0,
  flags text[] DEFAULT '{}')
RETURNS pg_catalog.int4 STRICT
AS 'MODULE_PATHNAME'
LANGUAGE C
;
CREATE DOMAIN foodomain AS text
;
CREATE TABLE range2_default PARTITION OF range_parted2 DEFAULT
;
CREATE TABLE "public"."trigger_parted_p2" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
CREATE TABLE "public"."domtab" (
    "col1" integer
)
;
CREATE FUNCTION "public"."wslot_slotlink_view"(character) RETURNS "text"
    LANGUAGE "plpgsql"
    AS $_$
declare
    rec		record;
    sltype	char(2);
    retval	text;
begin
    select into rec * from WSlot where slotname = $1;
    if not found then
        return '';
    end if;
    if rec.slotlink = '' then
        return '-';
    end if;
    sltype := substr(rec.slotlink, 1, 2);
    if sltype = 'PH' then
        select into rec * from PHone where slotname = rec.slotlink;
	retval := 'Phone ' || trim(rec.slotname);
	if rec.comment != '' then
	    retval := retval || ' (';
	    retval := retval || rec.comment;
	    retval := retval || ')';
	end if;
	return retval;
    end if;
    if sltype = 'IF' then
	declare
	    syrow	System%RowType;
	    ifrow	IFace%ROWTYPE;
        begin
	    select into ifrow * from IFace where slotname = rec.slotlink;
	    select into syrow * from System where name = ifrow.sysname;
	    retval := syrow.name || ' IF ';
	    retval := retval || ifrow.ifname;
	    if syrow.comment != '' then
	        retval := retval || ' (';
		retval := retval || syrow.comment;
		retval := retval || ')';
	    end if;
	    return retval;
	end;
    end if;
    return rec.slotlink;
end;
$_$
;
drop table test_partitioned
;
-- can be pushed down
DROP TRIGGER trig_row_after_update ON rem1
;
DROP TABLE fk
;
CREATE TABLE parted_self_fk (
    id bigint NOT NULL PRIMARY KEY,
    id_abc bigint,
    FOREIGN KEY (id_abc) REFERENCES parted_self_fk(id)
)
PARTITION BY RANGE (id)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
create type mydomainrange as range(subtype=mydomain)
;
ALTER OPERATOR FAMILY gist_numeric_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
alter table def_test alter column c2 set default 20
;
CREATE OR REPLACE FUNCTION list_partitioned_table()
RETURNS SETOF partitioned_table.a%TYPE AS $$
DECLARE
    row partitioned_table%ROWTYPE;
    a_val partitioned_table.a%TYPE;
BEGIN
    FOR row IN SELECT * FROM partitioned_table ORDER BY a LOOP
        a_val := row.a;
        RETURN NEXT a_val;
    END LOOP;
    RETURN;
END; $$ LANGUAGE plpgsql
;
CREATE TABLE fk11 PARTITION OF fk1 FOR VALUES FROM (1) TO (10)
;
CREATE TABLE bools_true PARTITION OF bools FOR VALUES IN (1)
;
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c2positive CHECK (c2 >= 0)
;
CREATE FUNCTION dintdict_lexize(internal, internal, internal, internal)
        RETURNS internal
        AS '$libdir/dict_int'
        LANGUAGE C STRICT
;
DROP VIEW vac_option_tab_counts
;
CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=25930 host=/tmp/1gCCONH3rb dbname=postgres' PUBLICATION pub5
;
CREATE TABLE "public"."transition_table_level1" (
    "level1_no" integer NOT NULL,
    "level1_node_name" character varying(255)
)
;
ALTER TABLE import_source."x 5" DROP COLUMN c1
;
DROP COLLATION mycoll1
;
create table other_tbl(v int4)
;
CREATE TABLE indtoasttest(descr text, cnt int DEFAULT 0, f1 text, f2 text)
;
--
-- brin_metapage_info()
--
CREATE FUNCTION brin_metapage_info(IN page bytea, OUT magic text,
	OUT version integer, OUT pagesperrange integer, OUT lastrevmappage bigint)
AS '$libdir/pageinspect', 'brin_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
create temp view v1 as
  select 2+2 as f1
;
--
--  CLUSTER
--

CREATE TABLE clstr_tst_s (rf_a SERIAL PRIMARY KEY,
	b INT)
;
create index "testing_idx" on atacc1(a)
;
CREATE TABLE part_6 (
	c int,
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 6)
	)
;
create table testpart_apple(logdate date) partition by range(logdate)
;
CREATE VIEW foo AS SELECT 1 INTO b
;
create table matest1 (id integer primary key) inherits (matest0)
;
CREATE INDEX CONCURRENTLY sro_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
	WHERE sro_ifun(a + 10) > sro_ifun(10)
;
alter table anothertab
  alter column f2 type bigint,
  alter column f3 type bigint,
  alter column f4 type bigint
;
DROP TABLE testpub_rf_tbl3
;
-- 5.43 ROLE_ROUTINE_GRANTS view is based on 5.50 ROUTINE_PRIVILEGES and is defined there instead.


-- 5.44 ROLE_TABLE_GRANTS view is based on 5.63 TABLE_PRIVILEGES and is defined there instead.


/*
 * 5.45
 * ROLE_TABLE_METHOD_GRANTS view
 */

-- feature not supported



-- 5.46 ROLE_USAGE_GRANTS view is based on 5.75 USAGE_PRIVILEGES and is defined there instead.


-- 5.47 ROLE_UDT_GRANTS view is based on 5.74 UDT_PRIVILEGES and is defined there instead.


/*
 * 5.48
 * ROUTINE_COLUMN_USAGE view
 */

-- not tracked by PostgreSQL


/*
 * 5.49
 * ROUTINE_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 5.50
 * ROUTINE_PRIVILEGES view
 */

CREATE VIEW routine_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(n.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST('EXECUTE' AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, p.proowner, 'USAGE')
                  OR p.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, proname, proowner, pronamespace, (aclexplode(coalesce(proacl, acldefault('f', proowner)))).* FROM pg_proc
         ) p (oid, proname, proowner, pronamespace, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE p.pronamespace = n.oid
          AND grantee.oid = p.grantee
          AND u_grantor.oid = p.grantor
          AND p.prtype IN ('EXECUTE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
CREATE TABLE "public"."hslot" (
    "slotname" character(20),
    "hubname" character(14),
    "slotno" integer,
    "slotlink" character(20)
)
;
CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_tbl1, pub_test.testpub_nopk
;
create function depth_c_tf() returns trigger
  language plpgsql as $$
begin
  raise notice '%: depth = %', tg_name, pg_trigger_depth();
  if new.id = 1 then
    raise exception sqlstate 'U9999';
  end if;
  raise notice '%: depth = %', tg_name, pg_trigger_depth();
  return new;
end;
$$
;
create table pk2 (b int, a int)
;
create temp table temp_part_parent (a int) partition by list (a)
;
CREATE TRIGGER my_trigger_func BEFORE INSERT OR UPDATE ON my_table
       FOR EACH ROW EXECUTE FUNCTION my_trigger_func()
;
CREATE FUNCTION btissncmp(issn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
--
-- POLYGON
--
-- polygon logic
--

CREATE TABLE POLYGON_TBL(f1 polygon)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] alter table alttype_cluster cluster on alttype_cluster_ind
;
----------
[CREATE_PROG_OBJ] -- but ordinary functions are not
create function missing_return_expr() returns int as $$
begin
    perform 2+2;
end;$$ language plpgsql
;
----------
[DROP_TABLE] drop table inh_nn1, inh_nn2, inh_nn3, inh_nn4
;
----------
[CREATE_PROG_OBJ] -- In the default case, dboid and roleoid fall back to their respective GUCs.
CREATE FUNCTION worker_spi_launch(index int4,
  dboid oid DEFAULT 0,
  roleoid oid DEFAULT 0,
  flags text[] DEFAULT '{}')
RETURNS pg_catalog.int4 STRICT
AS 'MODULE_PATHNAME'
LANGUAGE C
;
----------
[CREATE_TYPE] CREATE DOMAIN foodomain AS text
;
----------
[CREATE_TABLE] CREATE TABLE range2_default PARTITION OF range_parted2 DEFAULT
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted_p2" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."domtab" (
    "col1" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."wslot_slotlink_view"(character) RETURNS "text"
    LANGUAGE "plpgsql"
    AS $_$
declare
    rec		record;
    sltype	char(2);
    retval	text;
begin
    select into rec * from WSlot where slotname = $1;
    if not found then
        return '';
    end if;
    if rec.slotlink = '' then
        return '-';
    end if;
    sltype := substr(rec.slotlink, 1, 2);
    if sltype = 'PH' then
        select into rec * from PHone where slotname = rec.slotlink;
	retval := 'Phone ' || trim(rec.slotname);
	if rec.comment != '' then
	    retval := retval || ' (';
	    retval := retval || rec.comment;
	    retval := retval || ')';
	end if;
	return retval;
    end if;
    if sltype = 'IF' then
	declare
	    syrow	System%RowType;
	    ifrow	IFace%ROWTYPE;
        begin
	    select into ifrow * from IFace where slotname = rec.slotlink;
	    select into syrow * from System where name = ifrow.sysname;
	    retval := syrow.name || ' IF ';
	    retval := retval || ifrow.ifname;
	    if syrow.comment != '' then
	        retval := retval || ' (';
		retval := retval || syrow.comment;
		retval := retval || ')';
	    end if;
	    return retval;
	end;
    end if;
    return rec.slotlink;
end;
$_$
;
----------
[DROP_TABLE] drop table test_partitioned
;
----------
[DROP_TRIGGER] -- can be pushed down
DROP TRIGGER trig_row_after_update ON rem1
;
----------
[DROP_TABLE] DROP TABLE fk
;
----------
[CREATE_TABLE] CREATE TABLE parted_self_fk (
    id bigint NOT NULL PRIMARY KEY,
    id_abc bigint,
    FOREIGN KEY (id_abc) REFERENCES parted_self_fk(id)
)
PARTITION BY RANGE (id)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
----------
[CREATE_TYPE] create type mydomainrange as range(subtype=mydomain)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_numeric_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table def_test alter column c2 set default 20
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION list_partitioned_table()
RETURNS SETOF partitioned_table.a%TYPE AS $$
DECLARE
    row partitioned_table%ROWTYPE;
    a_val partitioned_table.a%TYPE;
BEGIN
    FOR row IN SELECT * FROM partitioned_table ORDER BY a LOOP
        a_val := row.a;
        RETURN NEXT a_val;
    END LOOP;
    RETURN;
END; $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE fk11 PARTITION OF fk1 FOR VALUES FROM (1) TO (10)
;
----------
[CREATE_TABLE] CREATE TABLE bools_true PARTITION OF bools FOR VALUES IN (1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c2positive CHECK (c2 >= 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dintdict_lexize(internal, internal, internal, internal)
        RETURNS internal
        AS '$libdir/dict_int'
        LANGUAGE C STRICT
;
----------
[DROP_VIEW] DROP VIEW vac_option_tab_counts
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=25930 host=/tmp/1gCCONH3rb dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_TABLE] CREATE TABLE "public"."transition_table_level1" (
    "level1_no" integer NOT NULL,
    "level1_node_name" character varying(255)
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE import_source."x 5" DROP COLUMN c1
;
----------
[DROP_POLICY] DROP COLLATION mycoll1
;
----------
[CREATE_TABLE] create table other_tbl(v int4)
;
----------
[CREATE_TABLE] CREATE TABLE indtoasttest(descr text, cnt int DEFAULT 0, f1 text, f2 text)
;
----------
[CREATE_PROG_OBJ] --
-- brin_metapage_info()
--
CREATE FUNCTION brin_metapage_info(IN page bytea, OUT magic text,
	OUT version integer, OUT pagesperrange integer, OUT lastrevmappage bigint)
AS '$libdir/pageinspect', 'brin_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] create temp view v1 as
  select 2+2 as f1
;
----------
[CREATE_TABLE] --
--  CLUSTER
--

CREATE TABLE clstr_tst_s (rf_a SERIAL PRIMARY KEY,
	b INT)
;
----------
[ADD_INDEX] create index "testing_idx" on atacc1(a)
;
----------
[CREATE_TABLE] CREATE TABLE part_6 (
	c int,
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 6)
	)
;
----------
[CREATE_TABLE] create table testpart_apple(logdate date) partition by range(logdate)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW foo AS SELECT 1 INTO b
;
----------
[CREATE_TABLE] create table matest1 (id integer primary key) inherits (matest0)
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY sro_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
	WHERE sro_ifun(a + 10) > sro_ifun(10)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table anothertab
  alter column f2 type bigint,
  alter column f3 type bigint,
  alter column f4 type bigint
;
----------
[DROP_TABLE] DROP TABLE testpub_rf_tbl3
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- 5.43 ROLE_ROUTINE_GRANTS view is based on 5.50 ROUTINE_PRIVILEGES and is defined there instead.


-- 5.44 ROLE_TABLE_GRANTS view is based on 5.63 TABLE_PRIVILEGES and is defined there instead.


/*
 * 5.45
 * ROLE_TABLE_METHOD_GRANTS view
 */

-- feature not supported



-- 5.46 ROLE_USAGE_GRANTS view is based on 5.75 USAGE_PRIVILEGES and is defined there instead.


-- 5.47 ROLE_UDT_GRANTS view is based on 5.74 UDT_PRIVILEGES and is defined there instead.


/*
 * 5.48
 * ROUTINE_COLUMN_USAGE view
 */

-- not tracked by PostgreSQL


/*
 * 5.49
 * ROUTINE_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 5.50
 * ROUTINE_PRIVILEGES view
 */

CREATE VIEW routine_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(n.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST('EXECUTE' AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, p.proowner, 'USAGE')
                  OR p.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, proname, proowner, pronamespace, (aclexplode(coalesce(proacl, acldefault('f', proowner)))).* FROM pg_proc
         ) p (oid, proname, proowner, pronamespace, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE p.pronamespace = n.oid
          AND grantee.oid = p.grantee
          AND u_grantor.oid = p.grantor
          AND p.prtype IN ('EXECUTE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."hslot" (
    "slotname" character(20),
    "hubname" character(14),
    "slotno" integer,
    "slotlink" character(20)
)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_tbl1, pub_test.testpub_nopk
;
----------
[CREATE_PROG_OBJ] create function depth_c_tf() returns trigger
  language plpgsql as $$
begin
  raise notice '%: depth = %', tg_name, pg_trigger_depth();
  if new.id = 1 then
    raise exception sqlstate 'U9999';
  end if;
  raise notice '%: depth = %', tg_name, pg_trigger_depth();
  return new;
end;
$$
;
----------
[CREATE_TABLE] create table pk2 (b int, a int)
;
----------
[CREATE_TABLE] create temp table temp_part_parent (a int) partition by list (a)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER my_trigger_func BEFORE INSERT OR UPDATE ON my_table
       FOR EACH ROW EXECUTE FUNCTION my_trigger_func()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btissncmp(issn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] --
-- POLYGON
--
-- polygon logic
--

CREATE TABLE POLYGON_TBL(f1 polygon)
;
