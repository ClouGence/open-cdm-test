CREATE INDEX gin_relopts_test ON array_index_op_test USING gin (i)
  WITH (FASTUPDATE=on, GIN_PENDING_LIST_LIMIT=128)
;
ALTER TABLE part_2 DROP COLUMN b
;
--
-- test LATERAL reference propagation down a multi-level inheritance hierarchy
-- produced for a multi-level partitioned table hierarchy.
--
create table join_pt1 (a int, b int, c varchar) partition by range(a)
;
CREATE TABLESPACE tblspc2 LOCATION '/tmp/EY2MH5Kl39/tempdir/tbl=spc2'
;
ANALYZE parted
;
create temp table p2 partition of p for values from (10) to (20)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_replication_origin
;
create table parted_trig3 (like parted_trig)
;
CREATE FUNCTION functest_A_2(text[]) RETURNS int LANGUAGE 'sql'
       AS 'SELECT $1[0]::int'
;
-- mixing named and positional: param2 is given twice, once in named notation
-- and second time in positional notation. Should throw an error at parse time
create function namedparmcursor_test3() returns void as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
begin
    open c1(param2 := 20, 21);
end
$$ language plpgsql
;
CREATE TABLE sysuser_data (n) AS SELECT NULL FROM generate_series(1, 10)
;
ALTER INDEX pg_toast.pg_toast_33497_index RENAME TO reind_con_toast_idx
;
ANALYZE fprt2_p1
;
CREATE TABLE r1 (a int PRIMARY KEY)
;
create function testoa(x1 int, x2 int, x3 int) returns orderedarray
language plpgsql as $$
declare res orderedarray;
begin
  res := array[x1, x2];
  res[2] := x3;
  return res;
end$$
;
create view base_tab_def_view as select * from base_tab_def
;
CREATE TEMP TABLE jsonpaths (path) AS SELECT '$'
;
CREATE TABLE rls_test_permissive (
    username        name,
    supervisor      name,
    data            integer
)
;
ALTER INDEX pg_toast.pg_toast_33159_index RENAME TO reind_con_toast_idx
;
CREATE TABLE "public"."tenk1" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
ALTER TABLE pg_temp_10.pg_temp_26350_2 ADD COLUMN newdata pg_temp_10.pg_temp_26350
;
DROP TABLE fkpart11.fk
;
-- But inconsistent check constraints provide inconsistent results
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c2negative CHECK (c2 < 0)
;
CREATE FUNCTION isneq(isbn, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FUNCTION "public"."stable_one"() RETURNS integer
    LANGUAGE "plpgsql" STABLE
    AS $$ begin return 1; end; $$
;
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = true,
    toast.vacuum_index_cleanup = false)
;
create database abc template template1
;
DROP TABLE ceil_floor_round
;
ALTER TABLE ONLY "public"."covidxpart" ATTACH PARTITION "public"."covidxpart1" FOR VALUES IN (1)
;
-- no such type2
drop operator = (int4, nonesuch)
;
create rule updlog as on update to tt15v do also
  insert into tt15v_log values(old, new, row(old,old) < row(new,new))
;
VACUUM (DISABLE_PAGE_SKIPPING) test1
;
create type mytype as range (subtype=int4, multirange_type_name=mr_sch.mr_type)
;
CREATE PUBLICATION addr_pub FOR TABLE addr_nsp.gentable
;
--
-- Now test longer arrays of char
--

CREATE TABLE CHAR_TBL(f1 char(4))
;
CREATE TABLE "public"."c" (
    "aa" integer,
    "cc" "text"
)
;
-- partitioned table cannot participate in regular inheritance
CREATE TABLE nonpartitioned (
	a int,
	b int
)
;
CREATE FUNCTION btean13cmp(ean13, isbn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- LIMIT not supported
CREATE VIEW ro_view10 AS SELECT 1 AS a
;
CREATE TABLE "regress_rls_schema"."rls_tbl" (
    "c1" integer
)
;
COMMENT ON TEXT SEARCH CONFIGURATION catalan IS 'configuration for catalan language'
;
CREATE TABLE var_tbl(x int, y text)
;
DROP TABLE num_variance
;
CREATE AGGREGATE myaggp13a(BASETYPE = int, SFUNC = tfnp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
CREATE FUNCTION gbt_int8_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub7
;
CREATE TABLE hash_heap_float4 (x float4, y int)
;
-- check handling of views with immediately-renamed columns

create view tt23v (col_a, col_b) as
select q1 as other_name1, q2 as other_name2 from int8_tbl
union
select 42, 43
;
-- should fail here.

--
-- test FK referenced updates NO ACTION
--

TRUNCATE temporal_mltrng, temporal_fk_mltrng2mltrng
;
/* contrib/pg_trgm/pg_trgm--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION strict_word_similarity(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX gin_relopts_test ON array_index_op_test USING gin (i)
  WITH (FASTUPDATE=on, GIN_PENDING_LIST_LIMIT=128)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE part_2 DROP COLUMN b
;
----------
[CREATE_TABLE] --
-- test LATERAL reference propagation down a multi-level inheritance hierarchy
-- produced for a multi-level partitioned table hierarchy.
--
create table join_pt1 (a int, b int, c varchar) partition by range(a)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc2 LOCATION '/tmp/EY2MH5Kl39/tempdir/tbl=spc2'
;
----------
[ADMIN_TABLE] ANALYZE parted
;
----------
[CREATE_TABLE] create temp table p2 partition of p for values from (10) to (20)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE] create table parted_trig3 (like parted_trig)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_A_2(text[]) RETURNS int LANGUAGE 'sql'
       AS 'SELECT $1[0]::int'
;
----------
[CREATE_PROG_OBJ] -- mixing named and positional: param2 is given twice, once in named notation
-- and second time in positional notation. Should throw an error at parse time
create function namedparmcursor_test3() returns void as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
begin
    open c1(param2 := 20, 21);
end
$$ language plpgsql
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE sysuser_data (n) AS SELECT NULL FROM generate_series(1, 10)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33497_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE fprt2_p1
;
----------
[CREATE_TABLE] CREATE TABLE r1 (a int PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] create function testoa(x1 int, x2 int, x3 int) returns orderedarray
language plpgsql as $$
declare res orderedarray;
begin
  res := array[x1, x2];
  res[2] := x3;
  return res;
end$$
;
----------
[CREATE_VIEW(SELECT)] create view base_tab_def_view as select * from base_tab_def
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE jsonpaths (path) AS SELECT '$'
;
----------
[CREATE_TABLE] CREATE TABLE rls_test_permissive (
    username        name,
    supervisor      name,
    data            integer
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33159_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tenk1" (
    "unique1" integer,
    "unique2" integer,
    "two" integer,
    "four" integer,
    "ten" integer,
    "twenty" integer,
    "hundred" integer,
    "thousand" integer,
    "twothousand" integer,
    "fivethous" integer,
    "tenthous" integer,
    "odd" integer,
    "even" integer,
    "stringu1" "name",
    "stringu2" "name",
    "string4" "name"
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_26350_2 ADD COLUMN newdata pg_temp_10.pg_temp_26350
;
----------
[DROP_TABLE] DROP TABLE fkpart11.fk
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- But inconsistent check constraints provide inconsistent results
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c2negative CHECK (c2 < 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(isbn, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."stable_one"() RETURNS integer
    LANGUAGE "plpgsql" STABLE
    AS $$ begin return 1; end; $$
;
----------
[ALTER_TABLE] ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = true,
    toast.vacuum_index_cleanup = false)
;
----------
[CREATE_CATALOG] create database abc template template1
;
----------
[DROP_TABLE] DROP TABLE ceil_floor_round
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."covidxpart" ATTACH PARTITION "public"."covidxpart1" FOR VALUES IN (1)
;
----------
[DROP_PROG_OBJ] -- no such type2
drop operator = (int4, nonesuch)
;
----------
[CREATE_POLICY] create rule updlog as on update to tt15v do also
  insert into tt15v_log values(old, new, row(old,old) < row(new,new))
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) test1
;
----------
[CREATE_TYPE] create type mytype as range (subtype=int4, multirange_type_name=mr_sch.mr_type)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION addr_pub FOR TABLE addr_nsp.gentable
;
----------
[CREATE_TABLE] --
-- Now test longer arrays of char
--

CREATE TABLE CHAR_TBL(f1 char(4))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."c" (
    "aa" integer,
    "cc" "text"
)
;
----------
[CREATE_TABLE] -- partitioned table cannot participate in regular inheritance
CREATE TABLE nonpartitioned (
	a int,
	b int
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btean13cmp(ean13, isbn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] -- LIMIT not supported
CREATE VIEW ro_view10 AS SELECT 1 AS a
;
----------
[CREATE_TABLE] CREATE TABLE "regress_rls_schema"."rls_tbl" (
    "c1" integer
)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION catalan IS 'configuration for catalan language'
;
----------
[CREATE_TABLE] CREATE TABLE var_tbl(x int, y text)
;
----------
[DROP_TABLE] DROP TABLE num_variance
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp13a(BASETYPE = int, SFUNC = tfnp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int8_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub7
;
----------
[CREATE_TABLE] CREATE TABLE hash_heap_float4 (x float4, y int)
;
----------
[CREATE_VIEW(SELECT)] -- check handling of views with immediately-renamed columns

create view tt23v (col_a, col_b) as
select q1 as other_name1, q2 as other_name2 from int8_tbl
union
select 42, 43
;
----------
[TRUNCATE_TABLE] -- should fail here.

--
-- test FK referenced updates NO ACTION
--

TRUNCATE temporal_mltrng, temporal_fk_mltrng2mltrng
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_trgm/pg_trgm--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION strict_word_similarity(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
