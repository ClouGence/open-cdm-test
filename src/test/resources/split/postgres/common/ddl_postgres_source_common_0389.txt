drop table lparted_nonullpart
;
drop table base_tab
;
CREATE TEMP TABLE relation_filenode_check(c1 int)
;
CREATE TABLE upsert (i int PRIMARY KEY, j int, k int) PARTITION BY RANGE (i)
;
ALTER TABLE pg_toast.pg_toast_30773 RENAME TO reind_con_toast
;
-- Rescan tests --
CREATE TEMPORARY SEQUENCE rngfunc_rescan_seq1
;
ALTER FUNCTION functest_B_3(int) COST 100
;
ALTER VIEW IF EXISTS split_view_probe.base_view ALTER COLUMN note DROP DEFAULT
;
ANALYZE ft_empty
;
-- mix variadic with anyelement
create function formarray(anyelement, variadic anyarray) returns anyarray as $$
  select array_prepend($1, $2);
$$ language sql immutable strict
;
create function bogus_func(int)
  returns anymultirange as 'select int4multirange(int4range(1,10))' language sql
;
CREATE EXTENSION intarray
;
CREATE TABLE tab2_2 PARTITION OF tab2 FOR VALUES IN (5, 6)
;
CREATE DATABASE foobar7s STRATEGY "FILE_COPY" TEMPLATE foobar2
;
CREATE SUBSCRIPTION regress_testsub5 CONNECTION 'port=-1' PUBLICATION testpub
;
CREATE INDEX ctlt3_fnidx ON ctlt3 ((a || c))
;
DROP INDEX box_spgist
;
CREATE PUBLICATION testpub_forunloggedtbl FOR TABLE testpub_unloggedtbl
;
-- Verify that constraint names and NO INHERIT are properly considered when
-- multiple constraint are specified, either explicitly or via SERIAL/PK/etc,
-- and that conflicting cases are rejected.  Mind that table constraints
-- handle this separately from column constraints.
create table notnull_tbl1 (a int primary key constraint foo not null)
;
CREATE SCHEMA split_agg_probe_next
;
ALTER TABLE pa_target ATTACH PARTITION part2 FOR VALUES IN (2,5,6)
;
CREATE DOMAIN "public"."plpgsql_arr_domain" AS integer[]
	CONSTRAINT "plpgsql_arr_domain_check" CHECK ("public"."plpgsql_arr_domain_check"(VALUE))
;
CREATE TABLE gtestc (f2 int GENERATED ALWAYS AS (f1+1) STORED) INHERITS(gtestp)
;
-- fail

DROP SEQUENCE ext_cine_seq
;
---------------------------------------------------
-- UPC:
CREATE FUNCTION btupccmp(upc, upc)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_publication_rel
;
DROP FUNCTION test4()
;
create or replace function shadowtest(f1 int)
	returns boolean as $$
declare f1 int; begin return 1; end $$ language plpgsql
;
-- cycled

ALTER SEQUENCE sequence_test2 RESTART WITH 24
  NO CYCLE
;
CREATE TABLE l (i int, v text)
;
CREATE TEXT SEARCH CONFIGURATION norwegian
	(PARSER = default)
;
CREATE INDEX i2 ON t2 USING btree (x)
;
-- failed (name conflict)
ALTER CONVERSION alt_conv1 RENAME TO alt_conv3
;
-- When a table is attached as partition to a partitioned table that has
-- a foreign key to another partitioned table, it acquires a clone of the
-- FK.  Upon detach, this clone is not removed, but instead becomes an
-- independent FK.  If it then attaches to the partitioned table again,
-- the FK from the parent "takes over" ownership of the independent FK rather
-- than creating a separate one.
CREATE SCHEMA fkpart12
  CREATE TABLE fk_p ( id int, jd int, PRIMARY KEY(id, jd)) PARTITION BY list (id)
  CREATE TABLE fk_p_1 PARTITION OF fk_p FOR VALUES IN (1) PARTITION BY list (jd)
  CREATE TABLE fk_p_1_1 PARTITION OF fk_p_1 FOR VALUES IN (1)
  CREATE TABLE fk_p_1_2 (x int, y int, jd int NOT NULL, id int NOT NULL)
  CREATE TABLE fk_p_2 PARTITION OF fk_p FOR VALUES IN (2) PARTITION BY list (jd)
  CREATE TABLE fk_p_2_1 PARTITION OF fk_p_2 FOR VALUES IN (1)
  CREATE TABLE fk_p_2_2 PARTITION OF fk_p_2 FOR VALUES IN (2)
  CREATE TABLE fk_r_1 ( p_jd int NOT NULL, x int, id int PRIMARY KEY, p_id int NOT NULL)
  CREATE TABLE fk_r_2 ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL) PARTITION BY list (id)
  CREATE TABLE fk_r_2_1 PARTITION OF fk_r_2 FOR VALUES IN (2, 1)
  CREATE TABLE fk_r   ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL,
       FOREIGN KEY (p_id, p_jd) REFERENCES fk_p (id, jd)
  ) PARTITION BY list (id)
;
--
-- test for bug #18634 (wrong varnullingrels error)
--
CREATE TABLE bug18634t (a int, b int, c text)
;
CREATE TABLE ctas_nodata_4 (ii) AS SELECT i, j FROM ctas_base WITH NO DATA
;
-- fails
drop table at_tab2
;
CREATE INDEX t2_gist ON s3.t2 USING GIST (b)
;
ALTER TABLE ONLY test_replica_identity4
  REPLICA IDENTITY USING INDEX test_replica_identity4_pkey
;
CREATE TABLE FKTABLE (ftest1 numeric REFERENCES pktable)
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26383_2 (tid pg_catalog.tid)
;
CREATE FUNCTION isneq(ean13, upc)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE split_alter_table.base_table REPLICA IDENTITY FULL
;
-- PK from LIKE:
CREATE TABLE temporal_rng2 (LIKE temporal_rng INCLUDING ALL)
;
-- test parallel plans for queries containing un-correlated subplans.
alter table tenk2 set (parallel_workers = 0)
;
TRUNCATE tru_ftable_parent
;
-- check detach/reattach behavior; statement triggers with transition tables
-- should not prevent a table from becoming a partition again
alter table parent detach partition child1
;
ANALYZE pg_temp_3.pg_temp_29541_2
;
COMMENT ON AGGREGATE "public"."newcnt"("any") IS 'an agg(any) comment'
;
CREATE TABLE range_parted (
	a int,
	b int
	) PARTITION BY RANGE (a, b)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] drop table lparted_nonullpart
;
----------
[DROP_TABLE] drop table base_tab
;
----------
[CREATE_TABLE] CREATE TEMP TABLE relation_filenode_check(c1 int)
;
----------
[CREATE_TABLE] CREATE TABLE upsert (i int PRIMARY KEY, j int, k int) PARTITION BY RANGE (i)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30773 RENAME TO reind_con_toast
;
----------
[CREATE_SEQUENCE] -- Rescan tests --
CREATE TEMPORARY SEQUENCE rngfunc_rescan_seq1
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_B_3(int) COST 100
;
----------
[ALTER_VIEW|ALTER_COLUMN] ALTER VIEW IF EXISTS split_view_probe.base_view ALTER COLUMN note DROP DEFAULT
;
----------
[ADMIN_TABLE] ANALYZE ft_empty
;
----------
[CREATE_PROG_OBJ] -- mix variadic with anyelement
create function formarray(anyelement, variadic anyarray) returns anyarray as $$
  select array_prepend($1, $2);
$$ language sql immutable strict
;
----------
[CREATE_PROG_OBJ] create function bogus_func(int)
  returns anymultirange as 'select int4multirange(int4range(1,10))' language sql
;
----------
[CREATE_LIBRARY] CREATE EXTENSION intarray
;
----------
[CREATE_TABLE] CREATE TABLE tab2_2 PARTITION OF tab2 FOR VALUES IN (5, 6)
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar7s STRATEGY "FILE_COPY" TEMPLATE foobar2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub5 CONNECTION 'port=-1' PUBLICATION testpub
;
----------
[ADD_INDEX] CREATE INDEX ctlt3_fnidx ON ctlt3 ((a || c))
;
----------
[DROP_INDEX] DROP INDEX box_spgist
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_forunloggedtbl FOR TABLE testpub_unloggedtbl
;
----------
[CREATE_TABLE] -- Verify that constraint names and NO INHERIT are properly considered when
-- multiple constraint are specified, either explicitly or via SERIAL/PK/etc,
-- and that conflicting cases are rejected.  Mind that table constraints
-- handle this separately from column constraints.
create table notnull_tbl1 (a int primary key constraint foo not null)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_agg_probe_next
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pa_target ATTACH PARTITION part2 FOR VALUES IN (2,5,6)
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."plpgsql_arr_domain" AS integer[]
	CONSTRAINT "plpgsql_arr_domain_check" CHECK ("public"."plpgsql_arr_domain_check"(VALUE))
;
----------
[CREATE_TABLE] CREATE TABLE gtestc (f2 int GENERATED ALWAYS AS (f1+1) STORED) INHERITS(gtestp)
;
----------
[DROP_SEQUENCE] -- fail

DROP SEQUENCE ext_cine_seq
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- UPC:
CREATE FUNCTION btupccmp(upc, upc)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_publication_rel
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test4()
;
----------
[CREATE_PROG_OBJ] create or replace function shadowtest(f1 int)
	returns boolean as $$
declare f1 int; begin return 1; end $$ language plpgsql
;
----------
[ALTER_SEQUENCE] -- cycled

ALTER SEQUENCE sequence_test2 RESTART WITH 24
  NO CYCLE
;
----------
[CREATE_TABLE] CREATE TABLE l (i int, v text)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION norwegian
	(PARSER = default)
;
----------
[ADD_INDEX] CREATE INDEX i2 ON t2 USING btree (x)
;
----------
[ALTER_POLICY] -- failed (name conflict)
ALTER CONVERSION alt_conv1 RENAME TO alt_conv3
;
----------
[CREATE_SCHEMA] -- When a table is attached as partition to a partitioned table that has
-- a foreign key to another partitioned table, it acquires a clone of the
-- FK.  Upon detach, this clone is not removed, but instead becomes an
-- independent FK.  If it then attaches to the partitioned table again,
-- the FK from the parent "takes over" ownership of the independent FK rather
-- than creating a separate one.
CREATE SCHEMA fkpart12
  CREATE TABLE fk_p ( id int, jd int, PRIMARY KEY(id, jd)) PARTITION BY list (id)
  CREATE TABLE fk_p_1 PARTITION OF fk_p FOR VALUES IN (1) PARTITION BY list (jd)
  CREATE TABLE fk_p_1_1 PARTITION OF fk_p_1 FOR VALUES IN (1)
  CREATE TABLE fk_p_1_2 (x int, y int, jd int NOT NULL, id int NOT NULL)
  CREATE TABLE fk_p_2 PARTITION OF fk_p FOR VALUES IN (2) PARTITION BY list (jd)
  CREATE TABLE fk_p_2_1 PARTITION OF fk_p_2 FOR VALUES IN (1)
  CREATE TABLE fk_p_2_2 PARTITION OF fk_p_2 FOR VALUES IN (2)
  CREATE TABLE fk_r_1 ( p_jd int NOT NULL, x int, id int PRIMARY KEY, p_id int NOT NULL)
  CREATE TABLE fk_r_2 ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL) PARTITION BY list (id)
  CREATE TABLE fk_r_2_1 PARTITION OF fk_r_2 FOR VALUES IN (2, 1)
  CREATE TABLE fk_r   ( id int PRIMARY KEY, p_id int NOT NULL, p_jd int NOT NULL,
       FOREIGN KEY (p_id, p_jd) REFERENCES fk_p (id, jd)
  ) PARTITION BY list (id)
;
----------
[CREATE_TABLE] --
-- test for bug #18634 (wrong varnullingrels error)
--
CREATE TABLE bug18634t (a int, b int, c text)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_nodata_4 (ii) AS SELECT i, j FROM ctas_base WITH NO DATA
;
----------
[DROP_TABLE] -- fails
drop table at_tab2
;
----------
[ADD_INDEX] CREATE INDEX t2_gist ON s3.t2 USING GIST (b)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY test_replica_identity4
  REPLICA IDENTITY USING INDEX test_replica_identity4_pkey
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 numeric REFERENCES pktable)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26383_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ean13, upc)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table REPLICA IDENTITY FULL
;
----------
[CREATE_TABLE] -- PK from LIKE:
CREATE TABLE temporal_rng2 (LIKE temporal_rng INCLUDING ALL)
;
----------
[ALTER_TABLE] -- test parallel plans for queries containing un-correlated subplans.
alter table tenk2 set (parallel_workers = 0)
;
----------
[TRUNCATE_TABLE] TRUNCATE tru_ftable_parent
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- check detach/reattach behavior; statement triggers with transition tables
-- should not prevent a table from becoming a partition again
alter table parent detach partition child1
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_29541_2
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE "public"."newcnt"("any") IS 'an agg(any) comment'
;
----------
[CREATE_TABLE] CREATE TABLE range_parted (
	a int,
	b int
	) PARTITION BY RANGE (a, b)
;
