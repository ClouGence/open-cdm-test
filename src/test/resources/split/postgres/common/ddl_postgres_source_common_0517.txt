ANALYZE public.pagg_tab2_p3
;
CREATE TABLE notnull_tbl1 (a int, b int) PARTITION BY LIST (a)
;
CREATE FUNCTION gin_extract_query_timestamptz(timestamptz, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE TABLE gtest24 (a int PRIMARY KEY, b gtestdomain1 GENERATED ALWAYS AS (a * 2) STORED)
;
-- raise exception
alter table my_locks set (autovacuum_enabled = false)
;
-- TRUNCATE changes the relfilenode and sends relcache invalidation
  TRUNCATE tbl_created_outside_xact
;
CREATE SCHEMA newschemax AUTHORIZATION "public"
;
ALTER TABLE public.test_replica_identity2 ADD CONSTRAINT test_replica_identity2_id_key UNIQUE (id)
;
CREATE TABLE "public"."xmldata" (
    "data" "xml"
)
;
CREATE OR REPLACE FUNCTION subxacts() returns void as $$
    BEGIN
      FOR i in 1 .. 128 LOOP
        BEGIN
          INSERT INTO harvest VALUES (42);
        EXCEPTION
        WHEN OTHERS THEN
	  RAISE;
        END;
    END LOOP;
    END; $$LANGUAGE 'plpgsql'
;
drop function shadowtest()
;
create function namedparmcursor_test2(int, int) returns boolean as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
    nonsense record;
begin
    open c1(param1 := $1, $2);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$$ language plpgsql
;
VACUUM (ANALYZE) pg_catalog.pg_partitioned_table
;
SECURITY LABEL ON TABLE auth_tbl
    IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
drop function sc_test()
;
ALTER TABLE table_without_key ADD COLUMN new_column text
;
ALTER TABLE ONLY "public"."stud_emp" INHERIT "public"."student"
;
DROP RULE r2 ON pg_description
;
--
-- Input/output functions for int_custom type
--
CREATE FUNCTION int_custom_in(cstring)
RETURNS int_custom
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE public.agg_data_2k
;
ALTER TABLE ONLY "public"."part" ATTACH PARTITION "public"."part_p1" FOR VALUES IN ('-2', '-1', 0, 1, 2)
;
DROP MATERIALIZED VIEW IF EXISTS no_such_mv
;
DROP TYPE itest_type CASCADE
;
create temp table prtx2_2 partition of prtx2 for values from (11) to (21)
;
create table merge_source_table (a int, b text)
;
CREATE FUNCTION is_valid(ean13)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE ONLY "public"."prt1_l_p3" ATTACH PARTITION "public"."prt1_l_p3_p2" FOR VALUES FROM (13) TO (25)
;
-- rule returns NEW
DROP RULE rw_view1_upd_rule ON rw_view1
;
ANALYZE bt_txt_heap
;
-- MERGE into inheritance root table
DROP TRIGGER insert_measurement_trigger ON measurement
;
ALTER TABLE cwi_test ADD UNIQUE USING INDEX cwi_uniq4_idx
;
ALTER TABLE pg_temp_9.pg_temp_37969_2 ADD COLUMN newdata pg_temp_9.pg_temp_37969
;
CREATE OPERATOR <-> (
	LEFTARG = money,
	RIGHTARG = money,
	PROCEDURE = cash_dist,
	COMMUTATOR = '<->'
)
;
CREATE TABLE test2 (a int) TABLESPACE tblspc2
;
drop table idxpart
;
ANALYZE pg_temp_13.pg_temp_26101_2
;
/*
 * 1.1. test CREATE INDEX with buffered build
 */
-- Regular index with included columns
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
ANALYZE public.pagg_tab1_p3
;
ANALYZE plt1
;
/*
 * 5.80
 * VIEW_TABLE_USAGE
 */

CREATE VIEW view_table_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS view_catalog,
           CAST(nv.nspname AS sql_identifier) AS view_schema,
           CAST(v.relname AS sql_identifier) AS view_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name

    FROM pg_namespace nv, pg_class v, pg_depend dv,
         pg_depend dt, pg_class t, pg_namespace nt

    WHERE nv.oid = v.relnamespace
          AND v.relkind = 'v'
          AND v.oid = dv.refobjid
          AND dv.refclassid = 'pg_catalog.pg_class'::regclass
          AND dv.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dv.deptype = 'i'
          AND dv.objid = dt.objid
          AND dv.refobjid <> dt.refobjid
          AND dt.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dt.refclassid = 'pg_catalog.pg_class'::regclass
          AND dt.refobjid = t.oid
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND pg_has_role(t.relowner, 'USAGE')
;
COMMENT ON CONSTRAINT noinh_con_copy_b_not_null ON noinh_con_copy IS 'not null b'
;
create table tt7a (x date, xx int, y int)
;
-- Check case where multiple partitions use the same connection
CREATE TABLE base_tbl3 (a int, b int, c text)
;
CREATE POLICY p3 ON z1 AS RESTRICTIVE USING (a NOT IN (SELECT a FROM z1_blacklist))
;
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_view
FOR EACH ROW WHEN (OLD.a <> NEW.a) EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
CREATE TABLE part_d_15_20 PARTITION OF part_c_100_200 FOR VALUES FROM (15) TO (20)
;
CREATE FUNCTION isnle(issn13, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE ONLY "public"."prt2_l_p3" ATTACH PARTITION "public"."prt2_l_p3_p1" FOR VALUES FROM (0) TO (13)
;
ANALYZE fkpart5.pk2
;
CREATE TRIGGER rep_b_u BEFORE UPDATE ON trigtest FOR EACH ROW EXECUTE PROCEDURE trig_report()
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE public.pagg_tab2_p3
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1 (a int, b int) PARTITION BY LIST (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_timestamptz(timestamptz, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE gtest24 (a int PRIMARY KEY, b gtestdomain1 GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[ALTER_TABLE] -- raise exception
alter table my_locks set (autovacuum_enabled = false)
;
----------
[TRUNCATE_TABLE] -- TRUNCATE changes the relfilenode and sends relcache invalidation
  TRUNCATE tbl_created_outside_xact
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschemax AUTHORIZATION "public"
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.test_replica_identity2 ADD CONSTRAINT test_replica_identity2_id_key UNIQUE (id)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."xmldata" (
    "data" "xml"
)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION subxacts() returns void as $$
    BEGIN
      FOR i in 1 .. 128 LOOP
        BEGIN
          INSERT INTO harvest VALUES (42);
        EXCEPTION
        WHEN OTHERS THEN
	  RAISE;
        END;
    END LOOP;
    END; $$LANGUAGE 'plpgsql'
;
----------
[DROP_PROG_OBJ] drop function shadowtest()
;
----------
[CREATE_PROG_OBJ] create function namedparmcursor_test2(int, int) returns boolean as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
    nonsense record;
begin
    open c1(param1 := $1, $2);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_partitioned_table
;
----------
[UNKNOWN] SECURITY LABEL ON TABLE auth_tbl
    IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
----------
[DROP_PROG_OBJ] drop function sc_test()
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE table_without_key ADD COLUMN new_column text
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."stud_emp" INHERIT "public"."student"
;
----------
[DROP_POLICY] DROP RULE r2 ON pg_description
;
----------
[CREATE_PROG_OBJ] --
-- Input/output functions for int_custom type
--
CREATE FUNCTION int_custom_in(cstring)
RETURNS int_custom
AS 'MODULE_PATHNAME'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE public.agg_data_2k
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."part" ATTACH PARTITION "public"."part_p1" FOR VALUES IN ('-2', '-1', 0, 1, 2)
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW IF EXISTS no_such_mv
;
----------
[DROP_TYPE] DROP TYPE itest_type CASCADE
;
----------
[CREATE_TABLE] create temp table prtx2_2 partition of prtx2 for values from (11) to (21)
;
----------
[CREATE_TABLE] create table merge_source_table (a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION is_valid(ean13)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_l_p3" ATTACH PARTITION "public"."prt1_l_p3_p2" FOR VALUES FROM (13) TO (25)
;
----------
[DROP_POLICY] -- rule returns NEW
DROP RULE rw_view1_upd_rule ON rw_view1
;
----------
[ADMIN_TABLE] ANALYZE bt_txt_heap
;
----------
[DROP_TRIGGER] -- MERGE into inheritance root table
DROP TRIGGER insert_measurement_trigger ON measurement
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE cwi_test ADD UNIQUE USING INDEX cwi_uniq4_idx
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_37969_2 ADD COLUMN newdata pg_temp_9.pg_temp_37969
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = money,
	RIGHTARG = money,
	PROCEDURE = cash_dist,
	COMMUTATOR = '<->'
)
;
----------
[CREATE_TABLE] CREATE TABLE test2 (a int) TABLESPACE tblspc2
;
----------
[DROP_TABLE] drop table idxpart
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26101_2
;
----------
[CREATE_TABLE] /*
 * 1.1. test CREATE INDEX with buffered build
 */
-- Regular index with included columns
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab1_p3
;
----------
[ADMIN_TABLE] ANALYZE plt1
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.80
 * VIEW_TABLE_USAGE
 */

CREATE VIEW view_table_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS view_catalog,
           CAST(nv.nspname AS sql_identifier) AS view_schema,
           CAST(v.relname AS sql_identifier) AS view_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name

    FROM pg_namespace nv, pg_class v, pg_depend dv,
         pg_depend dt, pg_class t, pg_namespace nt

    WHERE nv.oid = v.relnamespace
          AND v.relkind = 'v'
          AND v.oid = dv.refobjid
          AND dv.refclassid = 'pg_catalog.pg_class'::regclass
          AND dv.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dv.deptype = 'i'
          AND dv.objid = dt.objid
          AND dv.refobjid <> dt.refobjid
          AND dt.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dt.refclassid = 'pg_catalog.pg_class'::regclass
          AND dt.refobjid = t.oid
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND pg_has_role(t.relowner, 'USAGE')
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT noinh_con_copy_b_not_null ON noinh_con_copy IS 'not null b'
;
----------
[CREATE_TABLE] create table tt7a (x date, xx int, y int)
;
----------
[CREATE_TABLE] -- Check case where multiple partitions use the same connection
CREATE TABLE base_tbl3 (a int, b int, c text)
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON z1 AS RESTRICTIVE USING (a NOT IN (SELECT a FROM z1_blacklist))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_view
FOR EACH ROW WHEN (OLD.a <> NEW.a) EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
----------
[CREATE_TABLE] CREATE TABLE part_d_15_20 PARTITION OF part_c_100_200 FOR VALUES FROM (15) TO (20)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(issn13, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_l_p3" ATTACH PARTITION "public"."prt2_l_p3_p1" FOR VALUES FROM (0) TO (13)
;
----------
[ADMIN_TABLE] ANALYZE fkpart5.pk2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER rep_b_u BEFORE UPDATE ON trigtest FOR EACH ROW EXECUTE PROCEDURE trig_report()
;
