ALTER TYPE "public"."insenum" ADD VALUE 'i4'
;
-- ok
DROP TABLE ctas_ine_tbl
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_user_mapping
;
-- order of "skipping" warnings may vary
CLUSTER
;
ALTER INDEX reloptions_test_idx3 SET (fillfactor=40)
;
CREATE INDEX tbl_gist_idx ON tbl_gist using gist (c4) INCLUDE (c1,c3)
;
CREATE TRIGGER "tg_chkslotname" BEFORE INSERT ON "public"."pline" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotname"('PL')
;
CREATE VIEW addr_nsp.genview AS SELECT * from addr_nsp.gentable
;
CREATE UNIQUE INDEX bt_tsq ON test_tsquery (keyword)
;
-- adding schemas and tables
CREATE SCHEMA pub_test1
;
-- no-op, same schema
alter table alter1.t1 set schema alter2
;
CREATE FUNCTION btismncmp(ismn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE list_parted ATTACH PARTITION parent FOR VALUES IN (1)
;
-- this makes a shell "point <<@@ polygon" operator too
CREATE OPERATOR @@>> ( PROCEDURE = poly_contain_pt,
  LEFTARG = polygon, RIGHTARG = point,
  COMMUTATOR = <<@@ )
;
CREATE FUNCTION "public"."stfnp"(integer[]) RETURNS integer[]
    LANGUAGE "sql"
    AS $_$select $1$_$
;
alter type comptype drop attribute r
;
ALTER OPERATOR FAMILY gist_cube_ops USING gist drop function 4 (cube)
;
ALTER TABLE test_add_column
	ADD COLUMN c2 integer, 
	ADD COLUMN c3 integer primary key
;
ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f1_f4_key UNIQUE (f1, f4)
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_language
;
-- Table inheritance
CREATE TABLE fd_pt1 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
)
;
CREATE INDEX i0 ON funcidx ((f1(x)))
;
CREATE PROCEDURE transaction_test9b(cnt int)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'count = %', cnt;
END
$$
;
drop table range_parted, list_parted
;
CREATE SUBSCRIPTION tap_sub_A_B CONNECTION 'port=29890 host=/tmp/7Wj1Y48wnU dbname=postgres' PUBLICATION tap_pub_A
;
CREATE FUNCTION "public"."get_from_partitioned_table"(integer) RETURNS "public"."partitioned_table"
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
    a_val partitioned_table.a%TYPE;
    result partitioned_table%ROWTYPE;
BEGIN
    a_val := $1;
    SELECT * INTO result FROM partitioned_table WHERE a = a_val;
    RETURN result;
END; $_$
;
create table table1 (a int)
;
CREATE TABLE testschema.dflt (a int PRIMARY KEY USING INDEX TABLESPACE pg_default) PARTITION BY LIST (a)
;
ALTER TABLE pg_temp_11.pg_temp_41180_2 ADD COLUMN newdata pg_temp_11.pg_temp_41180
;
-- Test for propagation of nullability constraints into sub-joins

create temp table x (x1 int, x2 int)
;
CREATE POLICY "p3" ON "regress_rls_schema"."rls_tbl" FOR UPDATE USING (("c1" <= 3)) WITH CHECK (("c1" > 5))
;
DROP RULE IF EXISTS account_delete_multi ON split_trigger_probe.account_audit RESTRICT
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_range
;
-- test MERGE
CREATE TABLE gtestm (
  id int PRIMARY KEY,
  f1 int,
  f2 int,
  f3 int GENERATED ALWAYS AS (f1 * 2) STORED,
  f4 int GENERATED ALWAYS AS (f2 * 2) STORED
)
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
-- failed (not owner)
ALTER FUNCTION alt_func2(int) SET SCHEMA alt_nsp2
;
alter table f_star set (parallel_workers = 0)
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp_tz() < $.timestamp_tz())'))
;
CREATE FUNCTION fn(INT) RETURNS INT IMMUTABLE LANGUAGE plpgsql AS $$
  BEGIN
    RAISE NOTICE 'current search_path: %', current_setting('search_path');
    RETURN $1;
  END;
$$
;
CREATE SEQUENCE "public"."sequence_test13"
    AS integer
    START WITH -32768
    INCREMENT BY 1
    MINVALUE -2147483648
    NO MAXVALUE
    CACHE 1
;
-- set defaults on a non-existent column: this should fail
alter table def_test alter column c3 set default 30
;
create temp table tbl_rs(a int, b int)
;
create temp table tt3(f1 int, f2 text)
;
CREATE TABLE "public"."test_range_gist" (
    "ir" "int4range"
)
;
CREATE FUNCTION "public"."get_columns_length"("oid"[]) RETURNS integer
    LANGUAGE "c" STABLE STRICT PARALLEL SAFE
    AS '/src/src/test/regress/regress.so', 'get_columns_length'
;
ALTER EXTENSION test_pg_dump ADD FUNCTION test_pg_dump(int)
;
create table inserttesta (f1 insert_nnarray)
;
CREATE TABLE vistest (LIKE testeoc)
;
-- disallow duplicated not-null constraints
create domain int_domain1 as int constraint nn1 not null constraint nn2 not null
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i4'
;
----------
[DROP_TABLE] -- ok
DROP TABLE ctas_ine_tbl
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_user_mapping
;
----------
[ADMIN_TABLE] -- order of "skipping" warnings may vary
CLUSTER
;
----------
[ALTER_INDEX] ALTER INDEX reloptions_test_idx3 SET (fillfactor=40)
;
----------
[ADD_INDEX] CREATE INDEX tbl_gist_idx ON tbl_gist using gist (c4) INCLUDE (c1,c3)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkslotname" BEFORE INSERT ON "public"."pline" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotname"('PL')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW addr_nsp.genview AS SELECT * from addr_nsp.gentable
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX bt_tsq ON test_tsquery (keyword)
;
----------
[CREATE_SCHEMA] -- adding schemas and tables
CREATE SCHEMA pub_test1
;
----------
[ALTER_TABLE] -- no-op, same schema
alter table alter1.t1 set schema alter2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btismncmp(ismn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted ATTACH PARTITION parent FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] -- this makes a shell "point <<@@ polygon" operator too
CREATE OPERATOR @@>> ( PROCEDURE = poly_contain_pt,
  LEFTARG = polygon, RIGHTARG = point,
  COMMUTATOR = <<@@ )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."stfnp"(integer[]) RETURNS integer[]
    LANGUAGE "sql"
    AS $_$select $1$_$
;
----------
[ALTER_TYPE] alter type comptype drop attribute r
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_cube_ops USING gist drop function 4 (cube)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN c2 integer, 
	ADD COLUMN c3 integer primary key
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f1_f4_key UNIQUE (f1, f4)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_language
;
----------
[CREATE_TABLE] -- Table inheritance
CREATE TABLE fd_pt1 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
)
;
----------
[ADD_INDEX] CREATE INDEX i0 ON funcidx ((f1(x)))
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test9b(cnt int)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'count = %', cnt;
END
$$
;
----------
[DROP_TABLE] drop table range_parted, list_parted
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_A_B CONNECTION 'port=29890 host=/tmp/7Wj1Y48wnU dbname=postgres' PUBLICATION tap_pub_A
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."get_from_partitioned_table"(integer) RETURNS "public"."partitioned_table"
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
    a_val partitioned_table.a%TYPE;
    result partitioned_table%ROWTYPE;
BEGIN
    a_val := $1;
    SELECT * INTO result FROM partitioned_table WHERE a = a_val;
    RETURN result;
END; $_$
;
----------
[CREATE_TABLE] create table table1 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.dflt (a int PRIMARY KEY USING INDEX TABLESPACE pg_default) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_41180_2 ADD COLUMN newdata pg_temp_11.pg_temp_41180
;
----------
[CREATE_TABLE] -- Test for propagation of nullability constraints into sub-joins

create temp table x (x1 int, x2 int)
;
----------
[CREATE_POLICY] CREATE POLICY "p3" ON "regress_rls_schema"."rls_tbl" FOR UPDATE USING (("c1" <= 3)) WITH CHECK (("c1" > 5))
;
----------
[DROP_POLICY] DROP RULE IF EXISTS account_delete_multi ON split_trigger_probe.account_audit RESTRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_range
;
----------
[CREATE_TABLE] -- test MERGE
CREATE TABLE gtestm (
  id int PRIMARY KEY,
  f1 int,
  f2 int,
  f3 int GENERATED ALWAYS AS (f1 * 2) STORED,
  f4 int GENERATED ALWAYS AS (f2 * 2) STORED
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ismn,
	RIGHTARG = ismn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ALTER_PROG_OBJ] -- failed (not owner)
ALTER FUNCTION alt_func2(int) SET SCHEMA alt_nsp2
;
----------
[ALTER_TABLE] alter table f_star set (parallel_workers = 0)
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.timestamp_tz() < $.timestamp_tz())'))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn(INT) RETURNS INT IMMUTABLE LANGUAGE plpgsql AS $$
  BEGIN
    RAISE NOTICE 'current search_path: %', current_setting('search_path');
    RETURN $1;
  END;
$$
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test13"
    AS integer
    START WITH -32768
    INCREMENT BY 1
    MINVALUE -2147483648
    NO MAXVALUE
    CACHE 1
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- set defaults on a non-existent column: this should fail
alter table def_test alter column c3 set default 30
;
----------
[CREATE_TABLE] create temp table tbl_rs(a int, b int)
;
----------
[CREATE_TABLE] create temp table tt3(f1 int, f2 text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_range_gist" (
    "ir" "int4range"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."get_columns_length"("oid"[]) RETURNS integer
    LANGUAGE "c" STABLE STRICT PARALLEL SAFE
    AS '/src/src/test/regress/regress.so', 'get_columns_length'
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD FUNCTION test_pg_dump(int)
;
----------
[CREATE_TABLE] create table inserttesta (f1 insert_nnarray)
;
----------
[CREATE_TABLE] CREATE TABLE vistest (LIKE testeoc)
;
----------
[CREATE_TYPE] -- disallow duplicated not-null constraints
create domain int_domain1 as int constraint nn1 not null constraint nn2 not null
;
