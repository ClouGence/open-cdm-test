EXECUTE dumpFunc('16611')
;
EXECUTE dumpFunc('16403')
;
EXECUTE dumpFunc('21736')
;
CREATE STATISTICS mcv_lists_bool_stats (mcv) ON a, b, c
  FROM mcv_lists_bool
;
EXECUTE dumpTableAttach('39868')
;
EXECUTE dumpTableAttach('31840')
;
CREATE USER regress_view_user1
;
ALTER TABLE public.part_rp100 OWNER TO zyc
;
-- fail, view has reference

-- We used to have a bug that would allow the above to succeed, posing
-- hazards for later execution of the view.  Check that the internal
-- defenses for those hazards haven't bit-rotted, in case some other
-- bug with similar symptoms emerges.
begin
;
create statistics atref_stat on ((b).a is not null) from atref
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('16611')
;
----------
[UNSAFE] EXECUTE dumpFunc('16403')
;
----------
[UNSAFE] EXECUTE dumpFunc('21736')
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS mcv_lists_bool_stats (mcv) ON a, b, c
  FROM mcv_lists_bool
;
----------
[UNSAFE] EXECUTE dumpTableAttach('39868')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('31840')
;
----------
[CREATE_USER] CREATE USER regress_view_user1
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.part_rp100 OWNER TO zyc
;
----------
[TRANSACTION] -- fail, view has reference

-- We used to have a bug that would allow the above to succeed, posing
-- hazards for later execution of the view.  Check that the internal
-- defenses for those hazards haven't bit-rotted, in case some other
-- bug with similar symptoms emerges.
begin
;
----------
[ADMIN_PERFORMANCE] create statistics atref_stat on ((b).a is not null) from atref
;
