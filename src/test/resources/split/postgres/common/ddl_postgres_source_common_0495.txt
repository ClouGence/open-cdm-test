ANALYZE pg_temp_3.pg_temp_27374
;
--
-- For pkeys:
-- 	ON DELETE/UPDATE (pkey1, pkey2) CASCADE:
-- 		fkeys (fkey1, fkey2) and fkeys2 (fkey21, fkey22)
--
create trigger check_pkeys_fkey_cascade
	before delete or update on pkeys
	for each row
	execute procedure
	check_foreign_key (2, 'cascade', 'pkey1', 'pkey2',
	'fkeys', 'fkey1', 'fkey2', 'fkeys2', 'fkey21', 'fkey22')
;
CREATE UNIQUE INDEX tab5_a_idx ON tab5 (a)
;
CREATE FUNCTION gbt_date_distance(internal,date,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- Test handling of inet netmasks with inet_minmax_multi_ops
CREATE TABLE brin_test_inet (a inet)
;
DROP TABLE test_jsonb_constraints
;
create function excpt_test2() returns void as $$
begin
    begin
        begin
            raise notice '% %', sqlstate, sqlerrm;
        end;
    end;
end; $$ language plpgsql
;
-- it's still updatable if we add a DO ALSO rule

CREATE TABLE base_tbl_hist(ts timestamptz default now(), a int, b text)
;
CREATE OPERATOR CLASS gist__intbig_ops
FOR TYPE _int4 USING gist
AS
	OPERATOR	3	&&,
	OPERATOR	6	= (anyarray, anyarray),
	OPERATOR	7	@>,
	OPERATOR	8	<@,
	OPERATOR	13	@,
	OPERATOR	14	~,
	OPERATOR	20	@@ (_int4, query_int),
	FUNCTION	1	g_intbig_consistent (internal, _int4, smallint, oid, internal),
	FUNCTION	2	g_intbig_union (internal, internal),
	FUNCTION	3	g_intbig_compress (internal),
	FUNCTION	4	g_intbig_decompress (internal),
	FUNCTION	5	g_intbig_penalty (internal, internal, internal),
	FUNCTION	6	g_intbig_picksplit (internal, internal),
	FUNCTION	7	g_intbig_same (intbig_gkey, intbig_gkey, internal),
	STORAGE		intbig_gkey
;
DROP OPERATOR IF EXISTS + (no_such_schema.no_such_type, no_such_schema.no_such_type)
;
CREATE TABLE "public"."delete_test_table" (
    "a" bigint NOT NULL,
    "b" bigint NOT NULL,
    "c" bigint NOT NULL,
    "d" bigint NOT NULL
)
;
CREATE FUNCTION getrngfunc3(int) RETURNS setof text AS 'SELECT rngfuncname FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
CREATE TEMP TABLE pg_temp_24.pg_temp_26555_2 (tid pg_catalog.tid)
;
ANALYZE pg_temp_4.pg_temp_29544
;
DROP TABLE d3_listp1
;
ALTER TABLE tab_nothing REPLICA IDENTITY NOTHING
;
create table inh_lp (a int, value int)
;
create function count_tt1_v() returns int8 as
'select count(*) from tt1' language sql volatile
;
create index idxpart_spgist on idxpart using spgist(b)
;
create table mc3p (a int, b int, c int) partition by range (a, abs(b), c)
;
ALTER TABLE ONLY "public"."clstr_tst"
    ADD CONSTRAINT "clstr_tst_con" FOREIGN KEY ("b") REFERENCES "public"."clstr_tst_s"("rf_a")
;
-- test case for problems with revalidating an open relation during abort
create function inverse(int) returns float8 as
$$
begin
  analyze revalidate_bug;
  return 1::float8/$1;
exception
  when division_by_zero then return 0;
end$$ language plpgsql volatile
;
CREATE UNIQUE INDEX "pslot_name" ON "public"."pslot" USING "btree" ("slotname")
;
-- just to verify the script ran
CREATE TABLE ext_cine_tab3 (z int)
;
create table coll_pruning_a partition of coll_pruning for values in ('a')
;
ALTER TABLE ONLY "public"."atnotnull1"
    ADD CONSTRAINT "atnotnull1_pkey" PRIMARY KEY ("c")
;
ALTER INDEX pg_toast.pg_toast_30434_index RENAME TO reind_con_toast_idx
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (force_null '*')
;
drop function excpt_test1()
;
ALTER TABLE tt9 ADD CONSTRAINT foo CHECK(c > 3)
;
CREATE POLICY p1 ON t1 FOR ALL TO PUBLIC USING (a % 2 = 0)
;
create table at_tab2 (x int, y at_tab1)
;
DROP TABLE test_ex_constraints
;
CREATE TABLE "public"."mlparted_defd" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
ALTER TABLE do_write ADD COLUMN addedbys2 int
;
create type comptype as (f1 int, f2 text)
;
VACUUM FREEZE public.test
;
DROP OPERATOR FAMILY alt_opf4 USING btree
;
CREATE RULE "rtest_v1_del" AS
    ON DELETE TO "public"."rtest_v1" DO INSTEAD  DELETE FROM "public"."rtest_t1"
  WHERE ("rtest_t1"."a" = "old"."a")
;
CREATE TABLE "public"."itest9" (
    "a" integer NOT NULL,
    "b" "text",
    "c" bigint
)
;
CREATE TABLE ptif_test1 PARTITION OF ptif_test
  FOR VALUES FROM (0) TO (100) PARTITION BY list (b)
;
CREATE TABLE real_city (
	pop			int4,
	cname		text,
	outline 	path
)
;
-- If a partitioned table has a constraint whose index is not valid,
-- attaching a missing partition makes it valid.
create table idxpart (a int) partition by range (a)
;
CREATE OPERATOR CLASS timetz_ops
DEFAULT FOR TYPE timetz USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       timetz_cmp(timetz,timetz),
    FUNCTION        2       gin_extract_value_timetz(timetz, internal),
    FUNCTION        3       gin_extract_query_timetz(timetz, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_timetz(timetz,timetz,int2, internal),
STORAGE         timetz
;
-- statistics on integer expressions
CREATE TABLE expr_stats (a int, b int, c int)
;
create table parted_boolean_less partition of parted_boolean_col
  for values in ('foo' < 'bar')
;
-- Check LIMIT TO and EXCEPT
CREATE SCHEMA import_dest4
;
create table phv_part (a int, b text) partition by list (a)
;
ANALYZE pg_temp_11.pg_temp_41070
;
CREATE TABLE plt2_adv_default PARTITION OF plt2_adv DEFAULT
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27374
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- For pkeys:
-- 	ON DELETE/UPDATE (pkey1, pkey2) CASCADE:
-- 		fkeys (fkey1, fkey2) and fkeys2 (fkey21, fkey22)
--
create trigger check_pkeys_fkey_cascade
	before delete or update on pkeys
	for each row
	execute procedure
	check_foreign_key (2, 'cascade', 'pkey1', 'pkey2',
	'fkeys', 'fkey1', 'fkey2', 'fkeys2', 'fkey21', 'fkey22')
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX tab5_a_idx ON tab5 (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_date_distance(internal,date,int2,oid,internal)
RETURNS float8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] -- Test handling of inet netmasks with inet_minmax_multi_ops
CREATE TABLE brin_test_inet (a inet)
;
----------
[DROP_TABLE] DROP TABLE test_jsonb_constraints
;
----------
[CREATE_PROG_OBJ] create function excpt_test2() returns void as $$
begin
    begin
        begin
            raise notice '% %', sqlstate, sqlerrm;
        end;
    end;
end; $$ language plpgsql
;
----------
[CREATE_TABLE] -- it's still updatable if we add a DO ALSO rule

CREATE TABLE base_tbl_hist(ts timestamptz default now(), a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist__intbig_ops
FOR TYPE _int4 USING gist
AS
	OPERATOR	3	&&,
	OPERATOR	6	= (anyarray, anyarray),
	OPERATOR	7	@>,
	OPERATOR	8	<@,
	OPERATOR	13	@,
	OPERATOR	14	~,
	OPERATOR	20	@@ (_int4, query_int),
	FUNCTION	1	g_intbig_consistent (internal, _int4, smallint, oid, internal),
	FUNCTION	2	g_intbig_union (internal, internal),
	FUNCTION	3	g_intbig_compress (internal),
	FUNCTION	4	g_intbig_decompress (internal),
	FUNCTION	5	g_intbig_penalty (internal, internal, internal),
	FUNCTION	6	g_intbig_picksplit (internal, internal),
	FUNCTION	7	g_intbig_same (intbig_gkey, intbig_gkey, internal),
	STORAGE		intbig_gkey
;
----------
[DROP_PROG_OBJ] DROP OPERATOR IF EXISTS + (no_such_schema.no_such_type, no_such_schema.no_such_type)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."delete_test_table" (
    "a" bigint NOT NULL,
    "b" bigint NOT NULL,
    "c" bigint NOT NULL,
    "d" bigint NOT NULL
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION getrngfunc3(int) RETURNS setof text AS 'SELECT rngfuncname FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_24.pg_temp_26555_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_29544
;
----------
[DROP_TABLE] DROP TABLE d3_listp1
;
----------
[ALTER_TABLE] ALTER TABLE tab_nothing REPLICA IDENTITY NOTHING
;
----------
[CREATE_TABLE] create table inh_lp (a int, value int)
;
----------
[CREATE_PROG_OBJ] create function count_tt1_v() returns int8 as
'select count(*) from tt1' language sql volatile
;
----------
[ADD_INDEX] create index idxpart_spgist on idxpart using spgist(b)
;
----------
[CREATE_TABLE] create table mc3p (a int, b int, c int) partition by range (a, abs(b), c)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."clstr_tst"
    ADD CONSTRAINT "clstr_tst_con" FOREIGN KEY ("b") REFERENCES "public"."clstr_tst_s"("rf_a")
;
----------
[CREATE_PROG_OBJ] -- test case for problems with revalidating an open relation during abort
create function inverse(int) returns float8 as
$$
begin
  analyze revalidate_bug;
  return 1::float8/$1;
exception
  when division_by_zero then return 0;
end$$ language plpgsql volatile
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "pslot_name" ON "public"."pslot" USING "btree" ("slotname")
;
----------
[CREATE_TABLE] -- just to verify the script ran
CREATE TABLE ext_cine_tab3 (z int)
;
----------
[CREATE_TABLE] create table coll_pruning_a partition of coll_pruning for values in ('a')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."atnotnull1"
    ADD CONSTRAINT "atnotnull1_pkey" PRIMARY KEY ("c")
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30434_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (force_null '*')
;
----------
[DROP_PROG_OBJ] drop function excpt_test1()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD CONSTRAINT foo CHECK(c > 3)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON t1 FOR ALL TO PUBLIC USING (a % 2 = 0)
;
----------
[CREATE_TABLE] create table at_tab2 (x int, y at_tab1)
;
----------
[DROP_TABLE] DROP TABLE test_ex_constraints
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted_defd" (
    "a" integer,
    "b" integer,
    "c" "text",
    "........pg.dropped.4........" INTEGER /* dummy */,
    "........pg.dropped.5........" INTEGER /* dummy */,
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE do_write ADD COLUMN addedbys2 int
;
----------
[CREATE_TYPE] create type comptype as (f1 int, f2 text)
;
----------
[ADMIN_TABLE] VACUUM FREEZE public.test
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf4 USING btree
;
----------
[CREATE_POLICY] CREATE RULE "rtest_v1_del" AS
    ON DELETE TO "public"."rtest_v1" DO INSTEAD  DELETE FROM "public"."rtest_t1"
  WHERE ("rtest_t1"."a" = "old"."a")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest9" (
    "a" integer NOT NULL,
    "b" "text",
    "c" bigint
)
;
----------
[CREATE_TABLE] CREATE TABLE ptif_test1 PARTITION OF ptif_test
  FOR VALUES FROM (0) TO (100) PARTITION BY list (b)
;
----------
[CREATE_TABLE] CREATE TABLE real_city (
	pop			int4,
	cname		text,
	outline 	path
)
;
----------
[CREATE_TABLE] -- If a partitioned table has a constraint whose index is not valid,
-- attaching a missing partition makes it valid.
create table idxpart (a int) partition by range (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS timetz_ops
DEFAULT FOR TYPE timetz USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       timetz_cmp(timetz,timetz),
    FUNCTION        2       gin_extract_value_timetz(timetz, internal),
    FUNCTION        3       gin_extract_query_timetz(timetz, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_timetz(timetz,timetz,int2, internal),
STORAGE         timetz
;
----------
[CREATE_TABLE] -- statistics on integer expressions
CREATE TABLE expr_stats (a int, b int, c int)
;
----------
[CREATE_TABLE] create table parted_boolean_less partition of parted_boolean_col
  for values in ('foo' < 'bar')
;
----------
[CREATE_SCHEMA] -- Check LIMIT TO and EXCEPT
CREATE SCHEMA import_dest4
;
----------
[CREATE_TABLE] create table phv_part (a int, b text) partition by list (a)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41070
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_default PARTITION OF plt2_adv DEFAULT
;
