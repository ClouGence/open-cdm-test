ALTER INDEX ptif_test_index ATTACH PARTITION ptif_test3_index
;
CALL sum(1)
;
TRUNCATE tru_ptable
;
create table rule_dest(f1 int, f2 int[], tag text)
;
DROP FUNCTION functest_IS_1(int, int, text), functest_IS_2(int), functest_IS_3(int)
;
create unique index IFace_name on IFace using btree (slotname bpchar_ops)
;
CALL proc_with_utility_stmt()
;
CREATE EXTENSION seg
;
CREATE FUNCTION isnge(ean13, issn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- test FOR UPDATE in rules

create table rules_base(f1 int, f2 int)
;
CREATE FUNCTION _int_union(_int4, _int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
alter table idxpart detach partition idxpart3
;
-- should fail
DO $d$
    BEGIN
        EXECUTE $$ALTER SERVER loopback
            OPTIONS (SET dbname '$$||current_database()||$$')$$;
    END;
$d$
;
CREATE TRUSTED LANGUAGE plpgsql
  HANDLER plpgsql_call_handler
  INLINE plpgsql_inline_handler
  VALIDATOR plpgsql_validator
;
CREATE TABLE parttbl1 PARTITION OF parttbl FOR VALUES IN (1)
;
ANALYZE public.testjsonb
;
CREATE FUNCTION isnne(ismn13, ismn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE trunc_copy (id serial PRIMARY KEY, id2 int)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
CREATE TABLE test_like_gen_2 (LIKE test_like_gen_1)
;
-- fail
alter type comptype drop attribute i
;
alter function report_guc(text) reset all
;
DO 'DECLARE loid oid; BEGIN SELECT tbl.loid INTO loid FROM lotest_stash_values tbl; PERFORM lo_export(loid, ''/src/src/bin/pg_upgrade/tmp_check/results/invalid/path''); EXCEPTION WHEN UNDEFINED_FILE THEN RAISE NOTICE ''could not open file, as expected''; END'
;
CREATE FUNCTION pg_catalog.pg_file_rename(text, text)
RETURNS bool
AS 'SELECT pg_catalog.pg_file_rename($1, $2, NULL::pg_catalog.text);'
LANGUAGE SQL VOLATILE STRICT
;
CREATE SCHEMA tableam_display
;
create function cast_invoker(integer) returns date as $$
begin
  return $1;
end$$ language plpgsql
;
CREATE TABLESPACE target_ts  LOCATION ''
;
/*
 * text search configuration for armenian language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * armenian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY armenian_stem
	(TEMPLATE = snowball, Language = armenian )
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_authid
;
CREATE INDEX "iprt1_p1_a" ON "public"."prt1_p1" USING "btree" ("a")
;
CREATE FUNCTION ltree_ge(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_30582 RENAME TO reind_con_toast
;
drop table trigtest
;
CREATE TABLE dump_test.test_table_fk (
							col1 int references dump_test.test_table)
							PARTITION BY RANGE (col1)
;
DROP FUNCTION measurement_insert_trigger()
;
-- FROM [ONLY]
CREATE TABLE tbl_inh(id integer)
;
CREATE TABLE "public"."pagg_tab_p2" (
    "a" integer,
    "b" integer,
    "c" "text",
    "d" integer
)
;
CREATE TABLE ctl (k text NOT NULL PRIMARY KEY, deposit_date date NOT NULL)
;
DROP TABLE easi, easi2
;
CREATE FOREIGN TABLE fd_pt2_1 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
CREATE TABLE ctl_stats3_child (LIKE ctl_stats3_parent INCLUDING STATISTICS)
;
create table volatile_partbound_test2 partition of volatile_partbound_test for values from (current_timestamp) to (maxvalue)
;
-- failed (not owner)
ALTER OPERATOR FAMILY alt_opf1 USING hash RENAME TO alt_opf4
;
-- System columns cannot be indexed
CREATE INDEX ON syscolcol_table (ctid)
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server
;
create table fktable(ftest1 int, ftest2 inet, foreign key(ftest2, ftest1) references pktable)
;
ANALYZE person
;
CREATE TABLE regtest_table_2 (a int)
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = upc,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
create table atacc1 (a bigint, b int)
;
------------------------------------------------------------------------------------------
[ALTER_INDEX] ALTER INDEX ptif_test_index ATTACH PARTITION ptif_test3_index
;
----------
[CALL_PROG_OBJ] CALL sum(1)
;
----------
[TRUNCATE_TABLE] TRUNCATE tru_ptable
;
----------
[CREATE_TABLE] create table rule_dest(f1 int, f2 int[], tag text)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION functest_IS_1(int, int, text), functest_IS_2(int), functest_IS_3(int)
;
----------
[ADD_INDEX] create unique index IFace_name on IFace using btree (slotname bpchar_ops)
;
----------
[CALL_PROG_OBJ] CALL proc_with_utility_stmt()
;
----------
[CREATE_LIBRARY] CREATE EXTENSION seg
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ean13, issn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- test FOR UPDATE in rules

create table rules_base(f1 int, f2 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_union(_int4, _int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart detach partition idxpart3
;
----------
[BLOCK] -- should fail
DO $d$
    BEGIN
        EXECUTE $$ALTER SERVER loopback
            OPTIONS (SET dbname '$$||current_database()||$$')$$;
    END;
$d$
;
----------
[UNKNOWN] CREATE TRUSTED LANGUAGE plpgsql
  HANDLER plpgsql_call_handler
  INLINE plpgsql_inline_handler
  VALIDATOR plpgsql_validator
;
----------
[CREATE_TABLE] CREATE TABLE parttbl1 PARTITION OF parttbl FOR VALUES IN (1)
;
----------
[ADMIN_TABLE] ANALYZE public.testjsonb
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(ismn13, ismn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE trunc_copy (id serial PRIMARY KEY, id2 int)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
----------
[CREATE_TABLE] CREATE TABLE test_like_gen_2 (LIKE test_like_gen_1)
;
----------
[ALTER_TYPE] -- fail
alter type comptype drop attribute i
;
----------
[ALTER_PROG_OBJ] alter function report_guc(text) reset all
;
----------
[BLOCK] DO 'DECLARE loid oid; BEGIN SELECT tbl.loid INTO loid FROM lotest_stash_values tbl; PERFORM lo_export(loid, ''/src/src/bin/pg_upgrade/tmp_check/results/invalid/path''); EXCEPTION WHEN UNDEFINED_FILE THEN RAISE NOTICE ''could not open file, as expected''; END'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_catalog.pg_file_rename(text, text)
RETURNS bool
AS 'SELECT pg_catalog.pg_file_rename($1, $2, NULL::pg_catalog.text);'
LANGUAGE SQL VOLATILE STRICT
;
----------
[CREATE_SCHEMA] CREATE SCHEMA tableam_display
;
----------
[CREATE_PROG_OBJ] create function cast_invoker(integer) returns date as $$
begin
  return $1;
end$$ language plpgsql
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE target_ts  LOCATION ''
;
----------
[CREATE_POLICY] /*
 * text search configuration for armenian language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * armenian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY armenian_stem
	(TEMPLATE = snowball, Language = armenian )
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_authid
;
----------
[ADD_INDEX] CREATE INDEX "iprt1_p1_a" ON "public"."prt1_p1" USING "btree" ("a")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_ge(ltree,ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30582 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] drop table trigtest
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_fk (
							col1 int references dump_test.test_table)
							PARTITION BY RANGE (col1)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION measurement_insert_trigger()
;
----------
[CREATE_TABLE] -- FROM [ONLY]
CREATE TABLE tbl_inh(id integer)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_p2" (
    "a" integer,
    "b" integer,
    "c" "text",
    "d" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE ctl (k text NOT NULL PRIMARY KEY, deposit_date date NOT NULL)
;
----------
[DROP_TABLE] DROP TABLE easi, easi2
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE fd_pt2_1 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[CREATE_TABLE] CREATE TABLE ctl_stats3_child (LIKE ctl_stats3_parent INCLUDING STATISTICS)
;
----------
[CREATE_TABLE] create table volatile_partbound_test2 partition of volatile_partbound_test for values from (current_timestamp) to (maxvalue)
;
----------
[ALTER_PROG_OBJ] -- failed (not owner)
ALTER OPERATOR FAMILY alt_opf1 USING hash RENAME TO alt_opf4
;
----------
[ADD_INDEX] -- System columns cannot be indexed
CREATE INDEX ON syscolcol_table (ctid)
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server
;
----------
[CREATE_TABLE] create table fktable(ftest1 int, ftest2 inet, foreign key(ftest2, ftest1) references pktable)
;
----------
[ADMIN_TABLE] ANALYZE person
;
----------
[CREATE_TABLE] CREATE TABLE regtest_table_2 (a int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ean13,
	RIGHTARG = upc,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] create table atacc1 (a bigint, b int)
;
