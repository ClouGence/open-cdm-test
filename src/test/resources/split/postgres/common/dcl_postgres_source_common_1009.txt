EXECUTE dumpOpr('615')
;
-- check source-side whole-row references
BEGIN
;
-- create statistics
CREATE STATISTICS func_deps_stat (dependencies) ON (mod(a,11)), (mod(b::int, 13)), (mod(c, 7)) FROM functional_dependencies
;
-- Test rescan of an async Append node with do_exec_prune=false
SET enable_hashjoin TO false
;
ALTER TABLE public.pagg_tab_para_p1 OWNER TO zyc
;
EXECUTE dumpFunc('29227')
;
SET LOCAL TIME ZONE 10.5
;
ALTER FUNCTION "public"."int4_casttesttype"(integer) OWNER TO zyc
;
EXECUTE dumpTableAttach('37120')
;
GRANT EXECUTE ON function pg_catalog.pg_ls_dir(text, boolean, boolean)
		  TO rewind_user
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpOpr('615')
;
----------
[TRANSACTION] -- check source-side whole-row references
BEGIN
;
----------
[ADMIN_PERFORMANCE] -- create statistics
CREATE STATISTICS func_deps_stat (dependencies) ON (mod(a,11)), (mod(b::int, 13)), (mod(c, 7)) FROM functional_dependencies
;
----------
[SESSION_SETTING_WRITE] -- Test rescan of an async Append node with do_exec_prune=false
SET enable_hashjoin TO false
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.pagg_tab_para_p1 OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpFunc('29227')
;
----------
[SESSION_SETTING_WRITE] SET LOCAL TIME ZONE 10.5
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."int4_casttesttype"(integer) OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpTableAttach('37120')
;
----------
[GRANT] GRANT EXECUTE ON function pg_catalog.pg_ls_dir(text, boolean, boolean)
		  TO rewind_user
;
