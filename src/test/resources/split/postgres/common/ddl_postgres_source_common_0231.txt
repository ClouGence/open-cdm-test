-- this must be rejected to avoid self-inclusion issues:
alter type two_ints add attribute c two_ints_range
;
DROP TABLE test_foreign_constraints_inh
;
ANALYZE public.tenk1
;
CREATE EXTENSION pg_stat_statements WITH VERSION '1.4'
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_rewrite
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_conversion
;
DROP TABLE child
;
--
-- hash partitioned by expression
--
CREATE TABLE pht1_e (a int, b int, c text) PARTITION BY HASH(ltrim(c, 'A'))
;
ANALYZE hash_i4_heap
;
create domain di as int
;
ALTER PUBLICATION pub1 ADD TABLE dump_test.test_table
;
CREATE TRIGGER gtest11 BEFORE UPDATE OF b ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func3()
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
create table attach_parted (a int, b int) partition by list (b)
;
CREATE AGGREGATE myaggp03b(*) (SFUNC = stfp, STYPE = int4[],
  INITCOND = '{}')
;
DROP POLICY p3 ON document
;
ANALYZE pg_temp_18.pg_temp_27287
;
ALTER TABLE ONLY "public"."mlparted1" ATTACH PARTITION "public"."mlparted12" FOR VALUES FROM (5) TO (10)
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should fail, no rows
  select * from foo where f1 = 0 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
ANALYZE public.covidxpart4
;
create table pp_arrpart2 partition of pp_arrpart for values in ('{2, 3}', '{4, 5}')
;
CREATE TABLE tab_rowfilter_parent_sync (a int) PARTITION BY RANGE (a)
;
create index on idxpart22 (a)
;
ALTER TABLE public.test_inh_check ADD CONSTRAINT test_inh_check_a_check CHECK ((a > (10.2)::double precision))
;
-- and after clustering on clstr_expression_upper_b
CLUSTER clstr_expression USING clstr_expression_upper_b
;
VACUUM ANALYZE stud_emp
;
ALTER TABLE testschema.atable SET TABLESPACE regress_tblspace
;
DROP SCHEMA IF EXISTS split_cursor_next CASCADE
;
alter table alttype_cluster cluster on alttype_cluster_pkey
;
CREATE DATABASE "postgres" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
create function duplic(in i anycompatiblerange, out j anycompatible, out k anycompatiblearray) as $$
begin
  j := lower(i);
  k := array[lower(i),upper(i)];
  return;
end$$ language plpgsql
;
CREATE OPERATOR ~ (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_contained,
	COMMUTATOR = '@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
create index parted_inval_ixd_1 on only parted_inval_tab_1 (a)
;
-- 255
TRUNCATE tru_ftable
;
VACUUM pg_catalog.pg_proc
;
DROP TABLE test6b
;
ALTER OPERATOR @ (hstore, hstore)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE INDEX t1_a_idx ON t1(a)
;
ALTER TABLE itest17 ADD COLUMN c int GENERATED ALWAYS AS IDENTITY
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_attribute
;
CREATE TABLE num_exp_div (id1 int4, id2 int4, expected numeric(1000,800))
;
CREATE TABLE range_parted (
	a text,
	b bigint,
	c numeric,
	d int,
	e varchar
) PARTITION BY RANGE (a, b)
;
CREATE UNIQUE INDEX idx1 ON cmdata2 ((f1 || f2))
;
ALTER POLICY r2 ON rec2 USING (a = (SELECT x FROM rec1v WHERE y = b))
;
create function outparam2_succeed(r anyrange, out lu anyarray, out ul anyarray)
  as $$ select array[lower($1), upper($1)], array[upper($1), lower($1)] $$
  language sql
;
CREATE FUNCTION gin_extract_query_money(money, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE TABLE connectby_text(keyid text, parent_keyid text, pos int)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_shdescription
;
-- ATSimplePermissions()
ALTER TABLE pg_description ALTER COLUMN description SET NOT NULL
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] -- this must be rejected to avoid self-inclusion issues:
alter type two_ints add attribute c two_ints_range
;
----------
[DROP_TABLE] DROP TABLE test_foreign_constraints_inh
;
----------
[ADMIN_TABLE] ANALYZE public.tenk1
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pg_stat_statements WITH VERSION '1.4'
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_rewrite
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_conversion
;
----------
[DROP_TABLE] DROP TABLE child
;
----------
[CREATE_TABLE] --
-- hash partitioned by expression
--
CREATE TABLE pht1_e (a int, b int, c text) PARTITION BY HASH(ltrim(c, 'A'))
;
----------
[ADMIN_TABLE] ANALYZE hash_i4_heap
;
----------
[CREATE_TYPE] create domain di as int
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub1 ADD TABLE dump_test.test_table
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest11 BEFORE UPDATE OF b ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func3()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ean13,
	RIGHTARG = isbn13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[CREATE_TABLE] create table attach_parted (a int, b int) partition by list (b)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp03b(*) (SFUNC = stfp, STYPE = int4[],
  INITCOND = '{}')
;
----------
[DROP_POLICY] DROP POLICY p3 ON document
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_27287
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted1" ATTACH PARTITION "public"."mlparted12" FOR VALUES FROM (5) TO (10)
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- should fail, no rows
  select * from foo where f1 = 0 into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[ADMIN_TABLE] ANALYZE public.covidxpart4
;
----------
[CREATE_TABLE] create table pp_arrpart2 partition of pp_arrpart for values in ('{2, 3}', '{4, 5}')
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_parent_sync (a int) PARTITION BY RANGE (a)
;
----------
[ADD_INDEX] create index on idxpart22 (a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.test_inh_check ADD CONSTRAINT test_inh_check_a_check CHECK ((a > (10.2)::double precision))
;
----------
[ADMIN_TABLE] -- and after clustering on clstr_expression_upper_b
CLUSTER clstr_expression USING clstr_expression_upper_b
;
----------
[ADMIN_TABLE] VACUUM ANALYZE stud_emp
;
----------
[ALTER_TABLE] ALTER TABLE testschema.atable SET TABLESPACE regress_tblspace
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_cursor_next CASCADE
;
----------
[ALTER_TABLE] alter table alttype_cluster cluster on alttype_cluster_pkey
;
----------
[CREATE_CATALOG] CREATE DATABASE "postgres" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LOCALE = 'C'
;
----------
[CREATE_PROG_OBJ] create function duplic(in i anycompatiblerange, out j anycompatible, out k anycompatiblearray) as $$
begin
  j := lower(i);
  k := array[lower(i),upper(i)];
  return;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~ (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_contained,
	COMMUTATOR = '@',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
----------
[ADD_INDEX] create index parted_inval_ixd_1 on only parted_inval_tab_1 (a)
;
----------
[TRUNCATE_TABLE] -- 255
TRUNCATE tru_ftable
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_proc
;
----------
[DROP_TABLE] DROP TABLE test6b
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @ (hstore, hstore)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ADD_INDEX] CREATE INDEX t1_a_idx ON t1(a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE itest17 ADD COLUMN c int GENERATED ALWAYS AS IDENTITY
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_attribute
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_div (id1 int4, id2 int4, expected numeric(1000,800))
;
----------
[CREATE_TABLE] CREATE TABLE range_parted (
	a text,
	b bigint,
	c numeric,
	d int,
	e varchar
) PARTITION BY RANGE (a, b)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX idx1 ON cmdata2 ((f1 || f2))
;
----------
[ALTER_POLICY] ALTER POLICY r2 ON rec2 USING (a = (SELECT x FROM rec1v WHERE y = b))
;
----------
[CREATE_PROG_OBJ] create function outparam2_succeed(r anyrange, out lu anyarray, out ul anyarray)
  as $$ select array[lower($1), upper($1)], array[upper($1), lower($1)] $$
  language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_money(money, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE connectby_text(keyid text, parent_keyid text, pos int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_shdescription
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- ATSimplePermissions()
ALTER TABLE pg_description ALTER COLUMN description SET NOT NULL
;
