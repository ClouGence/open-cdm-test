COMMENT ON DOMAIN japanese_postal_code IS 'DOMAIN test'
;
CREATE TRIGGER stmt_trig AFTER INSERT ON batch_cp_upd_test3
	FOR EACH STATEMENT EXECUTE FUNCTION log_stmt()
;
drop function f1(x anycompatiblerange, y anycompatible, z anycompatible)
;
ALTER TABLE fk_r ATTACH PARTITION fk_r_1 FOR VALUES IN (1)
;
ALTER SUBSCRIPTION regress_testsub RENAME TO regress_testsub_foo
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN IF NOT EXISTS c3 integer
;
VACUUM (FULL) pg_catalog.pg_sequence
;
CREATE TABLE regress_roleoption.t3 (a int)
;
CREATE TABLE t_1 PARTITION OF t FOR VALUES FROM (1) TO (10)
		   PARTITION BY RANGE (a)
;
CREATE TABLE tab_psql_single (a int)
;
create or replace function max_xacttest() returns smallint language sql as
'select max(a) from xacttest' stable
;
alter table "catalog1"."schema1"."user_table" add column address varchar(255),add column name varchar(255)
;
create table bar2 (a integer)
;
CREATE FUNCTION functest_B_3(int) RETURNS bool LANGUAGE 'sql'
       STABLE AS 'SELECT $1 = 0'
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=20063 host=/tmp/Fjny4O22w8 dbname=postgres' PUBLICATION tap_pub
;
CREATE TABLE pxtest2 (a int)
;
ANALYZE (BUFFER_USAGE_LIMIT '512 kB') vac_option_tab
;
ALTER TABLE ONLY "fkpart5"."pk32" DROP COLUMN "........pg.dropped.1........"
;
drop function array_elem_check(numeric)
;
CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE INDEX ON schema_not_existing.tab (id)
;
drop trigger child3_update_trig on child3
;
CREATE TABLE t1 (a int, b anyarray)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_proc
;
CREATE INDEX test_gist_idx ON test_gist USING gist (p)
;
ANALYZE prt3_adv
;
CREATE AGGREGATE logging_agg_nonstrict_initcond (anyelement)
(
	stype = text,
	sfunc = logging_sfunc_nonstrict,
	mstype = text,
	msfunc = logging_msfunc_nonstrict,
	minvfunc = logging_minvfunc_nonstrict,
	initcond = 'I',
	minitcond = 'MI'
)
;
CREATE FUNCTION "public"."recurse"(double precision) RETURNS double precision
    LANGUAGE "plpgsql"
    AS $_$
begin
  if ($1 > 0) then
    return sql_recurse($1 - 1);
  else
    return $1;
  end if;
end;
$_$
;
-- should fail
alter table fkpart2.fk_part_1_1 drop constraint my_fkey
;
ALTER INDEX pg_toast.pg_toast_28119_index RENAME TO reind_con_toast_idx
;
VACUUM (PARALLEL 0) pg_catalog.pg_user_mapping
;
-- succeeds
ALTER TABLE attmp3 VALIDATE CONSTRAINT b_greater_than_ten
;
CREATE TABLE errtst_part_1(secret2 text, c text, a text, b text NOT NULL, secret1 text)
;
/*
 * 6.13
 * CHARACTER_SETS view
 */

CREATE VIEW character_sets AS
    SELECT CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name,
           CAST(CASE WHEN getdatabaseencoding() = 'UTF8' THEN 'UCS' ELSE getdatabaseencoding() END AS sql_identifier) AS character_repertoire,
           CAST(getdatabaseencoding() AS sql_identifier) AS form_of_use,
           CAST(current_database() AS sql_identifier) AS default_collate_catalog,
           CAST(nc.nspname AS sql_identifier) AS default_collate_schema,
           CAST(c.collname AS sql_identifier) AS default_collate_name
    FROM pg_database d
         LEFT JOIN (pg_collation c JOIN pg_namespace nc ON (c.collnamespace = nc.oid))
             ON (datcollate = collcollate AND datctype = collctype)
    WHERE d.datname = current_database()
    ORDER BY char_length(c.collname) DESC, c.collname ASC -- prefer full/canonical name
    LIMIT 1
;
CREATE TABLE "public"."test_range_spgist" (
    "ir" "int4range"
)
;
create table inh_nn4 (f5 int, f4 float, f2 text, f3 int, f1 int)
;
CALL test_proc3a()
;
CREATE TABLE prt1_l_p2 PARTITION OF prt1_l FOR VALUES FROM (250) TO (500) PARTITION BY LIST (c)
;
-- test jsonb -> python dict
-- complex dict with dicts as value
CREATE FUNCTION test1complex(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {"d": {"d": 1}})
return len(val)
$$
;
CREATE TEXT SEARCH CONFIGURATION "public"."thesaurus_tst" (
    PARSER = "pg_catalog"."default" )
;
-- while at it, also check that statement-level triggers fire
create function some_tab_stmt_trig_func() returns trigger as
$$begin raise notice 'updating some_tab'; return NULL; end;$$
language plpgsql
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_extension
;
ALTER TABLE pg_toast.pg_toast_30721 RENAME TO reind_con_toast
;
do $$
declare
  t test_01;
begin
  select 1, 2 into t;  -- should be ok
  raise notice 'ok';
  select 1, 2, 3 into t; -- should fail;
end;
$$
;
DROP FUNCTION volatile_one, stable_one
;
CREATE FUNCTION gbt_macad_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE public.char_tbl
;
CLUSTER clustertest
;
create table mclparted_null partition of mclparted for values in(null)
;
CREATE FUNCTION gin_compare_prefix_bool(bool, bool, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
TRUNCATE sysuser_data
;
------------------------------------------------------------------------------------------
[COMMENT_TYPE] COMMENT ON DOMAIN japanese_postal_code IS 'DOMAIN test'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER stmt_trig AFTER INSERT ON batch_cp_upd_test3
	FOR EACH STATEMENT EXECUTE FUNCTION log_stmt()
;
----------
[DROP_PROG_OBJ] drop function f1(x anycompatiblerange, y anycompatible, z anycompatible)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_r ATTACH PARTITION fk_r_1 FOR VALUES IN (1)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub RENAME TO regress_testsub_foo
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN IF NOT EXISTS c3 integer
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_sequence
;
----------
[CREATE_TABLE] CREATE TABLE regress_roleoption.t3 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE t_1 PARTITION OF t FOR VALUES FROM (1) TO (10)
		   PARTITION BY RANGE (a)
;
----------
[CREATE_TABLE] CREATE TABLE tab_psql_single (a int)
;
----------
[CREATE_PROG_OBJ] create or replace function max_xacttest() returns smallint language sql as
'select max(a) from xacttest' stable
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table "catalog1"."schema1"."user_table" add column address varchar(255),add column name varchar(255)
;
----------
[CREATE_TABLE] create table bar2 (a integer)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_B_3(int) RETURNS bool LANGUAGE 'sql'
       STABLE AS 'SELECT $1 = 0'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=20063 host=/tmp/Fjny4O22w8 dbname=postgres' PUBLICATION tap_pub
;
----------
[CREATE_TABLE] CREATE TABLE pxtest2 (a int)
;
----------
[ADMIN_TABLE] ANALYZE (BUFFER_USAGE_LIMIT '512 kB') vac_option_tab
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "fkpart5"."pk32" DROP COLUMN "........pg.dropped.1........"
;
----------
[DROP_PROG_OBJ] drop function array_elem_check(numeric)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA AUTHORIZATION CURRENT_USER
  CREATE INDEX ON schema_not_existing.tab (id)
;
----------
[DROP_TRIGGER] drop trigger child3_update_trig on child3
;
----------
[CREATE_TABLE] CREATE TABLE t1 (a int, b anyarray)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_proc
;
----------
[ADD_INDEX] CREATE INDEX test_gist_idx ON test_gist USING gist (p)
;
----------
[ADMIN_TABLE] ANALYZE prt3_adv
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE logging_agg_nonstrict_initcond (anyelement)
(
	stype = text,
	sfunc = logging_sfunc_nonstrict,
	mstype = text,
	msfunc = logging_msfunc_nonstrict,
	minvfunc = logging_minvfunc_nonstrict,
	initcond = 'I',
	minitcond = 'MI'
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."recurse"(double precision) RETURNS double precision
    LANGUAGE "plpgsql"
    AS $_$
begin
  if ($1 > 0) then
    return sql_recurse($1 - 1);
  else
    return $1;
  end if;
end;
$_$
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- should fail
alter table fkpart2.fk_part_1_1 drop constraint my_fkey
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28119_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_user_mapping
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- succeeds
ALTER TABLE attmp3 VALIDATE CONSTRAINT b_greater_than_ten
;
----------
[CREATE_TABLE] CREATE TABLE errtst_part_1(secret2 text, c text, a text, b text NOT NULL, secret1 text)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.13
 * CHARACTER_SETS view
 */

CREATE VIEW character_sets AS
    SELECT CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name,
           CAST(CASE WHEN getdatabaseencoding() = 'UTF8' THEN 'UCS' ELSE getdatabaseencoding() END AS sql_identifier) AS character_repertoire,
           CAST(getdatabaseencoding() AS sql_identifier) AS form_of_use,
           CAST(current_database() AS sql_identifier) AS default_collate_catalog,
           CAST(nc.nspname AS sql_identifier) AS default_collate_schema,
           CAST(c.collname AS sql_identifier) AS default_collate_name
    FROM pg_database d
         LEFT JOIN (pg_collation c JOIN pg_namespace nc ON (c.collnamespace = nc.oid))
             ON (datcollate = collcollate AND datctype = collctype)
    WHERE d.datname = current_database()
    ORDER BY char_length(c.collname) DESC, c.collname ASC -- prefer full/canonical name
    LIMIT 1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_range_spgist" (
    "ir" "int4range"
)
;
----------
[CREATE_TABLE] create table inh_nn4 (f5 int, f4 float, f2 text, f3 int, f1 int)
;
----------
[CALL_PROG_OBJ] CALL test_proc3a()
;
----------
[CREATE_TABLE] CREATE TABLE prt1_l_p2 PARTITION OF prt1_l FOR VALUES FROM (250) TO (500) PARTITION BY LIST (c)
;
----------
[CREATE_PROG_OBJ] -- test jsonb -> python dict
-- complex dict with dicts as value
CREATE FUNCTION test1complex(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, dict)
assert(val == {"d": {"d": 1}})
return len(val)
$$
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION "public"."thesaurus_tst" (
    PARSER = "pg_catalog"."default" )
;
----------
[CREATE_PROG_OBJ] -- while at it, also check that statement-level triggers fire
create function some_tab_stmt_trig_func() returns trigger as
$$begin raise notice 'updating some_tab'; return NULL; end;$$
language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_extension
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30721 RENAME TO reind_con_toast
;
----------
[BLOCK] do $$
declare
  t test_01;
begin
  select 1, 2 into t;  -- should be ok
  raise notice 'ok';
  select 1, 2, 3 into t; -- should fail;
end;
$$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION volatile_one, stable_one
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE public.char_tbl
;
----------
[ADMIN_TABLE] CLUSTER clustertest
;
----------
[CREATE_TABLE] create table mclparted_null partition of mclparted for values in(null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_bool(bool, bool, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[TRUNCATE_TABLE] TRUNCATE sysuser_data
;
