DROP ROLE regress_password_null
;
EXECUTE dumpFunc('26401')
;
COPY public.tableam_parted_a_heap2 (a, b) TO stdout
;
ALTER TABLE generated_stored_tests.gtest20b OWNER TO zyc
;
explain (analyze, costs off, summary off, timing off)
execute ps2(1)
;
EXECUTE dumpFunc('16465')
;
COPY (UPDATE stats_track_tab SET x = 2 WHERE x = 1 RETURNING x) TO stdout
;
ALTER TABLE testtab6 OWNER TO nonexistent
;
EXECUTE dumpOpr('2640')
;
-- check predicate lock on CTID
BEGIN ISOLATION LEVEL SERIALIZABLE
;
------------------------------------------------------------------------------------------
[DROP_ROLE] DROP ROLE regress_password_null
;
----------
[UNSAFE] EXECUTE dumpFunc('26401')
;
----------
[DATA_EXPORT] COPY public.tableam_parted_a_heap2 (a, b) TO stdout
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE generated_stored_tests.gtest20b OWNER TO zyc
;
----------
[UNSAFE] explain (analyze, costs off, summary off, timing off)
execute ps2(1)
;
----------
[UNSAFE] EXECUTE dumpFunc('16465')
;
----------
[DATA_EXPORT|UPDATE] COPY (UPDATE stats_track_tab SET x = 2 WHERE x = 1 RETURNING x) TO stdout
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE testtab6 OWNER TO nonexistent
;
----------
[UNSAFE] EXECUTE dumpOpr('2640')
;
----------
[TRANSACTION] -- check predicate lock on CTID
BEGIN ISOLATION LEVEL SERIALIZABLE
;
