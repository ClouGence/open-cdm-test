CREATE INDEX idx_bpchar ON test_bpchar USING gin (i)
;
CREATE FOREIGN TABLE gft1 (a int, b text, c text GENERATED ALWAYS AS ('foo') STORED) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/list1.csv', delimiter ',')
;
VACUUM (FREEZE) pg_catalog.pg_ts_config
;
COMMENT ON FUNCTION seg_same(seg, seg) IS
'same as'
;
ALTER OPERATOR >= (issn, issn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
ALTER TABLE tbl DROP COLUMN c1
;
-- It's generally bad style to use CREATE OR REPLACE unnecessarily.
-- Test what happens if an extension does it anyway.

CREATE OR REPLACE FUNCTION ext_cor_func() RETURNS text
  AS $$ SELECT 'ext_cor_func: from extension'::text $$ LANGUAGE sql
;
CREATE TABLE grantor_test3 ()
;
CREATE OR REPLACE PROCEDURE ptest5(a int, b text, c int default 100)
LANGUAGE SQL
AS $$
INSERT INTO cp_test VALUES(a, b);
INSERT INTO cp_test VALUES(c, b);
$$
;
alter table utrtest attach partition locp for values in (2)
;
CREATE FUNCTION s.const() RETURNS bool IMMUTABLE
  LANGUAGE SQL AS $$SELECT public.setter()$$
;
CREATE UNIQUE INDEX "dedup_unique" ON "public"."dedup_unique_test_table" USING "btree" ("a") WITH ("deduplicate_items"='on')
;
-- With a partitioned table, and nothing else.
CREATE TABLE concur_reindex_part (id int) PARTITION BY RANGE (id)
;
TRUNCATE TABLE truncprim, truncpart
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_database
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_constraint
;
DROP TABLE brin_test_multi_1
;
CREATE TABLE tab_full AS SELECT generate_series(1,10) AS a
;
CREATE TABLE am_partitioned_0 PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 0)
;
create index test_range_gist_idx on test_range_gist using gist (ir)
;
ALTER TABLE attmp_view RENAME TO attmp_view_new
;
CREATE TABLE "public"."part1_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
;
DROP TABLE will_get_dropped
;
create or replace function simplesql(int) returns int language sql
      as 'select $1 + 100'
;
create table parted_constr_ancestor (a int, b text)
  partition by range (b)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_publication
;
-- not a composite type at all
DROP TYPE tt_enum_type
;
-- fail
TRUNCATE atest2
;
DROP TABLE IF EXISTS audit_tbls."schema_one_table two"
;
DO $$
DECLARE
  result integer;
BEGIN
MERGE INTO pa_target t
  USING pa_source s
  ON t.tid = s.sid
  WHEN MATCHED THEN
    UPDATE SET balance = balance + delta, val = val || ' updated by merge'
  WHEN NOT MATCHED THEN
    INSERT VALUES (sid, delta, 'inserted by merge')
  WHEN NOT MATCHED BY SOURCE THEN
    UPDATE SET val = val || ' not matched by source';
GET DIAGNOSTICS result := ROW_COUNT;
RAISE NOTICE 'ROW_COUNT = %', result;
END;
$$
;
CREATE VIEW v13_temp AS SELECT seq1_temp.is_called FROM seq1_temp
;
drop table gist_point_tbl
;
CREATE TEMP TABLE concur_temp (f1 int, f2 text)
  ON COMMIT DELETE ROWS
;
DROP TABLE btree_part
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_am
;
-- error - "stored" setting does not affect virtual column
CREATE PUBLICATION pub_gencol FOR TABLE testpub_gencol with (publish_generated_columns = stored)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=25930 host=/tmp/1gCCONH3rb dbname=postgres' PUBLICATION pub1
;
ALTER TABLE ONLY "public"."c" INHERIT "public"."a"
;
ALTER TABLE r2 FORCE ROW LEVEL SECURITY
;
create rule rtest_t4_ins1 as on insert to rtest_t4
		where new.a >= 10 and new.a < 20 do instead
	insert into rtest_t5 values (new.a, new.b)
;
-- Analyze the X_star tables for better plan stability in later tests
ANALYZE a_star
;
CREATE OPERATOR !+! (PROCEDURE = int4pl, LEFTARG = priv_testdomain1, RIGHTARG = priv_testdomain1)
;
CREATE TABLE beta_pos PARTITION OF beta FOR VALUES FROM (0) TO ('Infinity') PARTITION BY LIST (c)
;
CREATE TABLE tab4_1_1 PARTITION OF tab4_1 FOR VALUES IN (0, 1)
;
CREATE TABLE "public"."rules_log" (
    "f1" integer,
    "f2" integer,
    "tag" "text",
    "id" integer NOT NULL
)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub6
;
alter table idxpart add exclude USING GIST (b with =, c with &&)
;
DROP SCHEMA test_schema
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) information_schema.sql_features
;
DROP DOMAIN constraint_enforced_dom
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE INDEX idx_bpchar ON test_bpchar USING gin (i)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE gft1 (a int, b text, c text GENERATED ALWAYS AS ('foo') STORED) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/list1.csv', delimiter ',')
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_ts_config
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_same(seg, seg) IS
'same as'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (issn, issn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tbl DROP COLUMN c1
;
----------
[CREATE_PROG_OBJ] -- It's generally bad style to use CREATE OR REPLACE unnecessarily.
-- Test what happens if an extension does it anyway.

CREATE OR REPLACE FUNCTION ext_cor_func() RETURNS text
  AS $$ SELECT 'ext_cor_func: from extension'::text $$ LANGUAGE sql
;
----------
[CREATE_TABLE] CREATE TABLE grantor_test3 ()
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE PROCEDURE ptest5(a int, b text, c int default 100)
LANGUAGE SQL
AS $$
INSERT INTO cp_test VALUES(a, b);
INSERT INTO cp_test VALUES(c, b);
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table utrtest attach partition locp for values in (2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION s.const() RETURNS bool IMMUTABLE
  LANGUAGE SQL AS $$SELECT public.setter()$$
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "dedup_unique" ON "public"."dedup_unique_test_table" USING "btree" ("a") WITH ("deduplicate_items"='on')
;
----------
[CREATE_TABLE] -- With a partitioned table, and nothing else.
CREATE TABLE concur_reindex_part (id int) PARTITION BY RANGE (id)
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE truncprim, truncpart
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_database
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_constraint
;
----------
[DROP_TABLE] DROP TABLE brin_test_multi_1
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tab_full AS SELECT generate_series(1,10) AS a
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned_0 PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 0)
;
----------
[ADD_INDEX] create index test_range_gist_idx on test_range_gist using gist (ir)
;
----------
[RENAME_TABLE] ALTER TABLE attmp_view RENAME TO attmp_view_new
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part1_self_fk" (
    "id" bigint NOT NULL,
    "id_abc" bigint
)
;
----------
[DROP_TABLE] DROP TABLE will_get_dropped
;
----------
[CREATE_PROG_OBJ] create or replace function simplesql(int) returns int language sql
      as 'select $1 + 100'
;
----------
[CREATE_TABLE] create table parted_constr_ancestor (a int, b text)
  partition by range (b)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_publication
;
----------
[DROP_TYPE] -- not a composite type at all
DROP TYPE tt_enum_type
;
----------
[TRUNCATE_TABLE] -- fail
TRUNCATE atest2
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS audit_tbls."schema_one_table two"
;
----------
[BLOCK] DO $$
DECLARE
  result integer;
BEGIN
MERGE INTO pa_target t
  USING pa_source s
  ON t.tid = s.sid
  WHEN MATCHED THEN
    UPDATE SET balance = balance + delta, val = val || ' updated by merge'
  WHEN NOT MATCHED THEN
    INSERT VALUES (sid, delta, 'inserted by merge')
  WHEN NOT MATCHED BY SOURCE THEN
    UPDATE SET val = val || ' not matched by source';
GET DIAGNOSTICS result := ROW_COUNT;
RAISE NOTICE 'ROW_COUNT = %', result;
END;
$$
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v13_temp AS SELECT seq1_temp.is_called FROM seq1_temp
;
----------
[DROP_TABLE] drop table gist_point_tbl
;
----------
[CREATE_TABLE] CREATE TEMP TABLE concur_temp (f1 int, f2 text)
  ON COMMIT DELETE ROWS
;
----------
[DROP_TABLE] DROP TABLE btree_part
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_am
;
----------
[CREATE_PUB_SUB] -- error - "stored" setting does not affect virtual column
CREATE PUBLICATION pub_gencol FOR TABLE testpub_gencol with (publish_generated_columns = stored)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=25930 host=/tmp/1gCCONH3rb dbname=postgres' PUBLICATION pub1
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."c" INHERIT "public"."a"
;
----------
[ALTER_TABLE] ALTER TABLE r2 FORCE ROW LEVEL SECURITY
;
----------
[CREATE_POLICY] create rule rtest_t4_ins1 as on insert to rtest_t4
		where new.a >= 10 and new.a < 20 do instead
	insert into rtest_t5 values (new.a, new.b)
;
----------
[ADMIN_TABLE] -- Analyze the X_star tables for better plan stability in later tests
ANALYZE a_star
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !+! (PROCEDURE = int4pl, LEFTARG = priv_testdomain1, RIGHTARG = priv_testdomain1)
;
----------
[CREATE_TABLE] CREATE TABLE beta_pos PARTITION OF beta FOR VALUES FROM (0) TO ('Infinity') PARTITION BY LIST (c)
;
----------
[CREATE_TABLE] CREATE TABLE tab4_1_1 PARTITION OF tab4_1 FOR VALUES IN (0, 1)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rules_log" (
    "f1" integer,
    "f2" integer,
    "tag" "text",
    "id" integer NOT NULL
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14127 host=/tmp/rx8hUQkqAl dbname=postgres' PUBLICATION pub6
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add exclude USING GIST (b with =, c with &&)
;
----------
[DROP_SCHEMA] DROP SCHEMA test_schema
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) information_schema.sql_features
;
----------
[DROP_TYPE] DROP DOMAIN constraint_enforced_dom
;
