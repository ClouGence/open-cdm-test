-- Partitioned table with primary key

CREATE TABLE pa_target (tid integer PRIMARY KEY) PARTITION BY LIST (tid)
;
create table test_t (c text)
;
CREATE OR REPLACE FUNCTION test_slru_page_readonly(int) RETURNS text
  AS '$libdir/test_slru', 'test_slru_page_readonly' LANGUAGE C
;
ALTER TABLE notnull_tbl1 ADD column d float8 default random()
;
VACUUM (PARALLEL 2) pg_catalog.pg_database
;
DROP TABLE plt2_adv_p2_ext
;
create index spgist_point_idx on spgist_point_tbl using spgist(p) with (fillfactor = 75)
;
DROP TABLE pt
;
do $$
declare x int;
begin
  select v from generate_series(1,2) g(v) into x;
end;
$$
;
-- should fail

-- these should all fail
ALTER TABLE fk_r_1 DROP CONSTRAINT fk_r_p_id_p_jd_fkey
;
CREATE FUNCTION ks_test_uniform_random()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
DROP TABLE vtype
;
ALTER TABLE lt1_part1 ATTACH PARTITION ft_part_1_2 FOR VALUES FROM (100) TO (200)
;
-- cannot drop inherited NOT NULL or check constraints from partition
ALTER TABLE list_parted2 ALTER b SET NOT NULL, ADD CONSTRAINT check_a2 CHECK (a > 0)
;
DROP TABLE blog, comment
;
CREATE TABLE "public"."clstr_tst_inh" (
    "a" integer DEFAULT "nextval"('"public"."clstr_tst_a_seq"'::"regclass") CONSTRAINT "clstr_tst_a_not_null" NOT NULL,
    "b" integer,
    "c" "text",
    "d" "text"
)
;
DO LANGUAGE plpgsql $$
BEGIN
    CREATE TABLE test3 (y int UNIQUE DEFERRABLE INITIALLY DEFERRED);
    COMMIT;
    INSERT INTO test3 (y) VALUES (1);
    COMMIT;
    INSERT INTO test3 (y) VALUES (1);
    INSERT INTO test3 (y) VALUES (2);
    COMMIT;
    INSERT INTO test3 (y) VALUES (3);  -- won't get here
END;
$$
;
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE EXCEPTION 'rewrites not allowed';
END;
$$
;
CREATE POLICY p1 ON current_check FOR SELECT USING (currentid % 2 = 0)
;
CREATE OR REPLACE FUNCTION list_partitioned_table()
RETURNS SETOF public.partitioned_table.a%TYPE AS $$
DECLARE
    row public.partitioned_table%ROWTYPE;
    a_val public.partitioned_table.a%TYPE;
BEGIN
    FOR row IN SELECT * FROM public.partitioned_table ORDER BY a LOOP
        a_val := row.a;
        RETURN NEXT a_val;
    END LOOP;
    RETURN;
END; $$ LANGUAGE plpgsql
;
analyze bigt
;
-- create new table in pg_catalog
CREATE TABLE pg_catalog.test (a int)
;
alter table part_abc attach partition part_abc_1 for values in (1)
;
-- specify PARTITION BY for a partition
CREATE TABLE fail_part_col_not_found PARTITION OF parted FOR VALUES IN ('c') PARTITION BY RANGE (c)
;
CREATE TABLE trans_bar (a int)
;
CREATE DEFAULT CONVERSION split_conversion_probe.default_latin1_to_utf8_probe FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
drop function depth_a_tf()
;
ALTER TABLE pg_temp_12.pg_temp_26560_2 ADD COLUMN newdata pg_temp_12.pg_temp_26560
;
CREATE RULE regtest_test_rule AS ON INSERT TO regtest_table_3 DO ALSO NOTHING
;
-- verify invalidation of partition tables for schema publication that has
-- parent and child tables of different partition hierarchies
CREATE TABLE pub_testpart2.parent2 (a int) partition by list (a)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) information_schema.sql_implementation_info
;
-- collation matches the partitioning collation, pruning works
create table coll_pruning (a text collate "C") partition by list (a)
;
CREATE TABLE bla(s TEXT)
;
-- test non-temp function is dropped when depending on temp table
    CREATE TEMPORARY TABLE just_give_me_a_type(id serial primary key)
;
-- Repeat the same queries with an extended data set. The data set is the
-- same that we used before, except that each element in the array is
-- repeated three times, offset by 1000 and 2000. For example, {1, 5}
-- becomes {1, 1001, 2001, 5, 1005, 2005}.
--
-- That has proven to be unreasonably effective at exercising codepaths in
-- core GiST code related to splitting parent pages, which is not covered by
-- other tests. This is a bit out-of-place as the point is to test core GiST
-- code rather than this extension, but there is no suitable GiST opclass in
-- core that would reach the same codepaths.
CREATE TABLE more__int AS SELECT
   -- Leave alone NULLs, empty arrays and the one row that we use to test
   -- equality; also skip INT_MAX
   CASE WHEN a IS NULL OR a = '{}' OR a = '{73,23,20}' THEN a ELSE
     (select array_agg(u) || array_agg(u + 1000) || array_agg(u + 2000)
      from unnest(a) u where u < 2000000000)
   END AS a, a as b
   FROM test__int
;
-- With support function that knows it's int4eq, we get a different plan
ALTER FUNCTION my_int_eq(int, int) SUPPORT test_support_func
;
ALTER TABLE fd_pt1 DROP COLUMN c5
;
-- check combinations of temporary and permanent relations when attaching
-- partitions.
create table perm_part_parent (a int) partition by list (a)
;
ALTER INDEX pg_toast.pg_toast_30900_index RENAME TO reind_con_toast_idx
;
ALTER FUNCTION gbt_int4_distance(internal, integer, smallint, oid, internal) PARALLEL SAFE
;
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c9_check CHECK (c9 < 0) NOT VALID
;
create schema abc authorization aac create table test_user (id int primary key,name varchar(50))
;
CREATE RULE "r5" AS
    ON INSERT TO "public"."rules_src" DO INSTEAD  INSERT INTO "public"."rules_log" AS "trgt" ("f1", "f2")  SELECT "new"."f1",
            "new"."f2"
  RETURNING "trgt"."f1",
    "trgt"."f2"
;
ALTER TABLE fd_pt1 ALTER COLUMN c7 DROP NOT NULL
;
CREATE TABLE ploc2 (f1 int, f2 text)
;
CREATE TABLE test9a (a int, b int)
;
DROP TABLE trunc_trigger_log
;
alter table atacc1 drop constraint atacc_test1 restrict
;
CREATE VIEW rw_view2 AS
  SELECT s, c, s/c t, a base_a, ctid
  FROM rw_view1
;
ALTER TABLE pg_toast.pg_toast_30899 RENAME TO reind_con_toast
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- Partitioned table with primary key

CREATE TABLE pa_target (tid integer PRIMARY KEY) PARTITION BY LIST (tid)
;
----------
[CREATE_TABLE] create table test_t (c text)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_readonly(int) RETURNS text
  AS '$libdir/test_slru', 'test_slru_page_readonly' LANGUAGE C
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE notnull_tbl1 ADD column d float8 default random()
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_database
;
----------
[DROP_TABLE] DROP TABLE plt2_adv_p2_ext
;
----------
[ADD_INDEX] create index spgist_point_idx on spgist_point_tbl using spgist(p) with (fillfactor = 75)
;
----------
[DROP_TABLE] DROP TABLE pt
;
----------
[BLOCK] do $$
declare x int;
begin
  select v from generate_series(1,2) g(v) into x;
end;
$$
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- should fail

-- these should all fail
ALTER TABLE fk_r_1 DROP CONSTRAINT fk_r_p_id_p_jd_fkey
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ks_test_uniform_random()
RETURNS boolean AS
$$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
LANGUAGE plpgsql
;
----------
[DROP_TABLE] DROP TABLE vtype
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE lt1_part1 ATTACH PARTITION ft_part_1_2 FOR VALUES FROM (100) TO (200)
;
----------
[ALTER_TABLE|ALTER_COLUMN|ADD_CONSTRAINT] -- cannot drop inherited NOT NULL or check constraints from partition
ALTER TABLE list_parted2 ALTER b SET NOT NULL, ADD CONSTRAINT check_a2 CHECK (a > 0)
;
----------
[DROP_TABLE] DROP TABLE blog, comment
;
----------
[CREATE_TABLE] CREATE TABLE "public"."clstr_tst_inh" (
    "a" integer DEFAULT "nextval"('"public"."clstr_tst_a_seq"'::"regclass") CONSTRAINT "clstr_tst_a_not_null" NOT NULL,
    "b" integer,
    "c" "text",
    "d" "text"
)
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    CREATE TABLE test3 (y int UNIQUE DEFERRABLE INITIALLY DEFERRED);
    COMMIT;
    INSERT INTO test3 (y) VALUES (1);
    COMMIT;
    INSERT INTO test3 (y) VALUES (1);
    INSERT INTO test3 (y) VALUES (2);
    COMMIT;
    INSERT INTO test3 (y) VALUES (3);  -- won't get here
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE EXCEPTION 'rewrites not allowed';
END;
$$
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON current_check FOR SELECT USING (currentid % 2 = 0)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION list_partitioned_table()
RETURNS SETOF public.partitioned_table.a%TYPE AS $$
DECLARE
    row public.partitioned_table%ROWTYPE;
    a_val public.partitioned_table.a%TYPE;
BEGIN
    FOR row IN SELECT * FROM public.partitioned_table ORDER BY a LOOP
        a_val := row.a;
        RETURN NEXT a_val;
    END LOOP;
    RETURN;
END; $$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] analyze bigt
;
----------
[CREATE_TABLE] -- create new table in pg_catalog
CREATE TABLE pg_catalog.test (a int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table part_abc attach partition part_abc_1 for values in (1)
;
----------
[CREATE_TABLE] -- specify PARTITION BY for a partition
CREATE TABLE fail_part_col_not_found PARTITION OF parted FOR VALUES IN ('c') PARTITION BY RANGE (c)
;
----------
[CREATE_TABLE] CREATE TABLE trans_bar (a int)
;
----------
[CREATE_POLICY] CREATE DEFAULT CONVERSION split_conversion_probe.default_latin1_to_utf8_probe FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[DROP_PROG_OBJ] drop function depth_a_tf()
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26560_2 ADD COLUMN newdata pg_temp_12.pg_temp_26560
;
----------
[CREATE_POLICY] CREATE RULE regtest_test_rule AS ON INSERT TO regtest_table_3 DO ALSO NOTHING
;
----------
[CREATE_TABLE] -- verify invalidation of partition tables for schema publication that has
-- parent and child tables of different partition hierarchies
CREATE TABLE pub_testpart2.parent2 (a int) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) information_schema.sql_implementation_info
;
----------
[CREATE_TABLE] -- collation matches the partitioning collation, pruning works
create table coll_pruning (a text collate "C") partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE bla(s TEXT)
;
----------
[CREATE_TABLE] -- test non-temp function is dropped when depending on temp table
    CREATE TEMPORARY TABLE just_give_me_a_type(id serial primary key)
;
----------
[CREATE_TABLE|SELECT] -- Repeat the same queries with an extended data set. The data set is the
-- same that we used before, except that each element in the array is
-- repeated three times, offset by 1000 and 2000. For example, {1, 5}
-- becomes {1, 1001, 2001, 5, 1005, 2005}.
--
-- That has proven to be unreasonably effective at exercising codepaths in
-- core GiST code related to splitting parent pages, which is not covered by
-- other tests. This is a bit out-of-place as the point is to test core GiST
-- code rather than this extension, but there is no suitable GiST opclass in
-- core that would reach the same codepaths.
CREATE TABLE more__int AS SELECT
   -- Leave alone NULLs, empty arrays and the one row that we use to test
   -- equality; also skip INT_MAX
   CASE WHEN a IS NULL OR a = '{}' OR a = '{73,23,20}' THEN a ELSE
     (select array_agg(u) || array_agg(u + 1000) || array_agg(u + 2000)
      from unnest(a) u where u < 2000000000)
   END AS a, a as b
   FROM test__int
;
----------
[ALTER_PROG_OBJ] -- With support function that knows it's int4eq, we get a different plan
ALTER FUNCTION my_int_eq(int, int) SUPPORT test_support_func
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fd_pt1 DROP COLUMN c5
;
----------
[CREATE_TABLE] -- check combinations of temporary and permanent relations when attaching
-- partitions.
create table perm_part_parent (a int) partition by list (a)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30900_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int4_distance(internal, integer, smallint, oid, internal) PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c9_check CHECK (c9 < 0) NOT VALID
;
----------
[CREATE_SCHEMA] create schema abc authorization aac create table test_user (id int primary key,name varchar(50))
;
----------
[CREATE_POLICY] CREATE RULE "r5" AS
    ON INSERT TO "public"."rules_src" DO INSTEAD  INSERT INTO "public"."rules_log" AS "trgt" ("f1", "f2")  SELECT "new"."f1",
            "new"."f2"
  RETURNING "trgt"."f1",
    "trgt"."f2"
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c7 DROP NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE ploc2 (f1 int, f2 text)
;
----------
[CREATE_TABLE] CREATE TABLE test9a (a int, b int)
;
----------
[DROP_TABLE] DROP TABLE trunc_trigger_log
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table atacc1 drop constraint atacc_test1 restrict
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS
  SELECT s, c, s/c t, a base_a, ctid
  FROM rw_view1
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30899 RENAME TO reind_con_toast
;
