-- not supported

CREATE TRIGGER regtest_test_trig BEFORE UPDATE ON regtest_table
    FOR EACH ROW EXECUTE PROCEDURE suppress_redundant_updates_trigger()
;
CREATE FUNCTION balkifnull(int8, int8)
RETURNS int8
PARALLEL SAFE
STRICT
LANGUAGE plpgsql AS $$
BEGIN
    IF $1 IS NULL THEN
       RAISE 'erroneously called with NULL argument';
    END IF;
    RETURN NULL;
END$$
;
CREATE TABLE ruletest_t3 (x int)
;
-- check multi-column range partitioning expression enforces the same
-- constraint as what tuple-routing would determine it to be
create table mcrparted0 partition of mcrparted for values from (minvalue, minvalue, minvalue) to (1, maxvalue, maxvalue)
;
CREATE TABLE s4.t1 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_amop
;
create function testpolym(int, out a int, out a int) returns int as $$ select 1;$$ language sql
;
-- fail at update

drop function arrayassign1()
;
CREATE TEMP TABLE kd_point_tbl_ord_idx2 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM kd_point_tbl WHERE p <@ box '(200,200,1000,1000)'
;
CREATE OR REPLACE FUNCTION rngfuncb(in f1 int, inout f2 int, out text)
AS $$select $2-1, $1::text || 'z'$$ LANGUAGE sql
;
DROP TABLE tidscan
;
/*
 * text search configuration for catalan language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * catalan and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY catalan_stem
	(TEMPLATE = snowball, Language = catalan )
;
-- clean up, lest opr_sanity complain
drop type xfloat8 cascade
;
ALTER TABLE ONLY "public"."mlparted_def1" DROP COLUMN "........pg.dropped.7........"
;
ANALYZE pg_temp_9.pg_temp_38052_2
;
CREATE TABLESPACE tblspc2 LOCATION '/tmp/7jY_B3_coT/tempdir/tbl=spc2'
;
create domain str_domain2 as text check (value <> 'foo') default 'foo'
;
DROP TRIGGER trig_null ON rem1
;
CREATE TABLE "generated_stored_tests"."gtestx" (
    "a" integer CONSTRAINT "gtest1_a_not_null" NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 22)) STORED,
    "x" integer
)
;
create trigger parent_delete_trig
  after delete on parent referencing old table as old_table
  for each statement execute procedure dump_delete()
;
ALTER TABLE testschema.test_tab ADD CONSTRAINT test_tab_unique UNIQUE (a)
;
truncate test
;
create function f1(in i int, out j int, out k text) as $$
begin
  j := i;
  j := j+1;
  k := 'foo';
end$$ language plpgsql
;
CREATE TYPE intbig_gkey (
        INTERNALLENGTH = -1,
        INPUT = _intbig_in,
        OUTPUT = _intbig_out
)
;
-- Bigint, implicit cast is squashed
CREATE TABLE test_squash_bigint (id int, data bigint)
;
drop table inh_parent_2
;
CREATE VIEW rw_view1 AS SELECT a,10 AS b FROM base_tbl
;
ANALYZE public.rtest_t7
;
CREATE FUNCTION fkpart11.print_row () RETURNS TRIGGER LANGUAGE plpgsql AS $$
  BEGIN
    RAISE NOTICE 'TABLE: %, OP: %, OLD: %, NEW: %', TG_RELNAME, TG_OP, OLD, NEW;
    RETURN NULL;
  END;
$$
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_namespace
;
DROP TABLE vacowned
;
CREATE INDEX gtest31_2_y_idx ON public.gtest31_2 USING btree (((y).b))
;
DROP OPERATOR FAMILY test_operator_family USING btree
;
--
-- EXPLAIN
--
-- There are many test cases elsewhere that use EXPLAIN as a vehicle for
-- checking something else (usually planner behavior).  This file is
-- concerned with testing EXPLAIN in its own right.
--

-- To produce stable regression test output, it's usually necessary to
-- ignore details such as exact costs or row counts.  These filter
-- functions replace changeable output details with fixed strings.

create function explain_filter(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just 'N'
        ln := regexp_replace(ln, '-?\m\d+\M', 'N', 'g');
        -- In sort output, the above won't match units-suffixed numbers
        ln := regexp_replace(ln, '\m\d+kB', 'NkB', 'g');
        -- Ignore text-mode buffers output because it varies depending
        -- on the system state
        CONTINUE WHEN (ln ~ ' +Buffers: .*');
        -- Ignore text-mode "Planning:" line because whether it's output
        -- varies depending on the system state
        CONTINUE WHEN (ln = 'Planning:');
        return next ln;
    end loop;
end;
$$
;
-- multiple partitioning columns of the same type
CREATE TABLE mcinthash (a int, b int, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_int4_ops)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
DROP TABLE concur_heap
;
CREATE FUNCTION alter_op_test_fn(boolean, boolean)
RETURNS boolean AS $$ SELECT NULL::BOOLEAN; $$ LANGUAGE sql IMMUTABLE
;
vacuum analyze atacc1(a)
;
-- fail, scalar result type

drop type rngfunc_type cascade
;
drop function dfunc(anyelement, anyelement, bool)
;
alter extension postgres_fdw add operator family my_op_family using btree
;
CREATE TABLE room_reservation (room_id text NOT NULL, start_time timestamp with time zone NOT NULL, end_time timestamp with time zone NOT NULL, description text NOT NULL, CONSTRAINT room_reservation_pkey PRIMARY KEY (room_id, start_time))
;
DROP TABLE IF EXISTS split_pg18.temporal_child
;
DROP TABLE TEMP_TIMESTAMP
;
VACUUM (FREEZE) pg_catalog.pg_parameter_acl
;
create domain dcomptype as comptype
;
-- Another UNION ALL test, but containing a mix of exec init and exec run-time pruning.
create table xy_1 (x int, y int)
;
ANALYZE public.prt2_l_p3_p1
;
DROP TABLE brin_date_test
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- not supported

CREATE TRIGGER regtest_test_trig BEFORE UPDATE ON regtest_table
    FOR EACH ROW EXECUTE PROCEDURE suppress_redundant_updates_trigger()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION balkifnull(int8, int8)
RETURNS int8
PARALLEL SAFE
STRICT
LANGUAGE plpgsql AS $$
BEGIN
    IF $1 IS NULL THEN
       RAISE 'erroneously called with NULL argument';
    END IF;
    RETURN NULL;
END$$
;
----------
[CREATE_TABLE] CREATE TABLE ruletest_t3 (x int)
;
----------
[CREATE_TABLE] -- check multi-column range partitioning expression enforces the same
-- constraint as what tuple-routing would determine it to be
create table mcrparted0 partition of mcrparted for values from (minvalue, minvalue, minvalue) to (1, maxvalue, maxvalue)
;
----------
[CREATE_TABLE] CREATE TABLE s4.t1 (
				i INTEGER,
				b BOX,
				ia int4[],
				ir int4range,
				t TEXT
			)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_amop
;
----------
[CREATE_PROG_OBJ] create function testpolym(int, out a int, out a int) returns int as $$ select 1;$$ language sql
;
----------
[DROP_PROG_OBJ] -- fail at update

drop function arrayassign1()
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE kd_point_tbl_ord_idx2 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM kd_point_tbl WHERE p <@ box '(200,200,1000,1000)'
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION rngfuncb(in f1 int, inout f2 int, out text)
AS $$select $2-1, $1::text || 'z'$$ LANGUAGE sql
;
----------
[DROP_TABLE] DROP TABLE tidscan
;
----------
[CREATE_POLICY] /*
 * text search configuration for catalan language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * catalan and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY catalan_stem
	(TEMPLATE = snowball, Language = catalan )
;
----------
[DROP_TYPE] -- clean up, lest opr_sanity complain
drop type xfloat8 cascade
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def1" DROP COLUMN "........pg.dropped.7........"
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38052_2
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc2 LOCATION '/tmp/7jY_B3_coT/tempdir/tbl=spc2'
;
----------
[CREATE_TYPE] create domain str_domain2 as text check (value <> 'foo') default 'foo'
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_null ON rem1
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtestx" (
    "a" integer CONSTRAINT "gtest1_a_not_null" NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 22)) STORED,
    "x" integer
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parent_delete_trig
  after delete on parent referencing old table as old_table
  for each statement execute procedure dump_delete()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testschema.test_tab ADD CONSTRAINT test_tab_unique UNIQUE (a)
;
----------
[TRUNCATE_TABLE] truncate test
;
----------
[CREATE_PROG_OBJ] create function f1(in i int, out j int, out k text) as $$
begin
  j := i;
  j := j+1;
  k := 'foo';
end$$ language plpgsql
;
----------
[CREATE_TYPE] CREATE TYPE intbig_gkey (
        INTERNALLENGTH = -1,
        INPUT = _intbig_in,
        OUTPUT = _intbig_out
)
;
----------
[CREATE_TABLE] -- Bigint, implicit cast is squashed
CREATE TABLE test_squash_bigint (id int, data bigint)
;
----------
[DROP_TABLE] drop table inh_parent_2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT a,10 AS b FROM base_tbl
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_t7
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fkpart11.print_row () RETURNS TRIGGER LANGUAGE plpgsql AS $$
  BEGIN
    RAISE NOTICE 'TABLE: %, OP: %, OLD: %, NEW: %', TG_RELNAME, TG_OP, OLD, NEW;
    RETURN NULL;
  END;
$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_namespace
;
----------
[DROP_TABLE] DROP TABLE vacowned
;
----------
[ADD_INDEX] CREATE INDEX gtest31_2_y_idx ON public.gtest31_2 USING btree (((y).b))
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY test_operator_family USING btree
;
----------
[CREATE_PROG_OBJ] --
-- EXPLAIN
--
-- There are many test cases elsewhere that use EXPLAIN as a vehicle for
-- checking something else (usually planner behavior).  This file is
-- concerned with testing EXPLAIN in its own right.
--

-- To produce stable regression test output, it's usually necessary to
-- ignore details such as exact costs or row counts.  These filter
-- functions replace changeable output details with fixed strings.

create function explain_filter(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just 'N'
        ln := regexp_replace(ln, '-?\m\d+\M', 'N', 'g');
        -- In sort output, the above won't match units-suffixed numbers
        ln := regexp_replace(ln, '\m\d+kB', 'NkB', 'g');
        -- Ignore text-mode buffers output because it varies depending
        -- on the system state
        CONTINUE WHEN (ln ~ ' +Buffers: .*');
        -- Ignore text-mode "Planning:" line because whether it's output
        -- varies depending on the system state
        CONTINUE WHEN (ln = 'Planning:');
        return next ln;
    end loop;
end;
$$
;
----------
[CREATE_TABLE] -- multiple partitioning columns of the same type
CREATE TABLE mcinthash (a int, b int, c jsonb)
  PARTITION BY HASH (a part_test_int4_ops, b part_test_int4_ops)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) information_schema.sql_implementation_info
;
----------
[DROP_TABLE] DROP TABLE concur_heap
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION alter_op_test_fn(boolean, boolean)
RETURNS boolean AS $$ SELECT NULL::BOOLEAN; $$ LANGUAGE sql IMMUTABLE
;
----------
[ADMIN_TABLE] vacuum analyze atacc1(a)
;
----------
[DROP_TYPE] -- fail, scalar result type

drop type rngfunc_type cascade
;
----------
[DROP_PROG_OBJ] drop function dfunc(anyelement, anyelement, bool)
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw add operator family my_op_family using btree
;
----------
[CREATE_TABLE] CREATE TABLE room_reservation (room_id text NOT NULL, start_time timestamp with time zone NOT NULL, end_time timestamp with time zone NOT NULL, description text NOT NULL, CONSTRAINT room_reservation_pkey PRIMARY KEY (room_id, start_time))
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS split_pg18.temporal_child
;
----------
[DROP_TABLE] DROP TABLE TEMP_TIMESTAMP
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_parameter_acl
;
----------
[CREATE_TYPE] create domain dcomptype as comptype
;
----------
[CREATE_TABLE] -- Another UNION ALL test, but containing a mix of exec init and exec run-time pruning.
create table xy_1 (x int, y int)
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_l_p3_p1
;
----------
[DROP_TABLE] DROP TABLE brin_date_test
;
