ALTER TABLE part_7 ATTACH PARTITION part_7_a_null FOR VALUES IN ('a', null)
;
CREATE FUNCTION delete(hstore,text)
RETURNS hstore
AS '$libdir/hstore','hstore_delete'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE "fkpart5"."pk1" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
CREATE FUNCTION part_hashint4_error(value int4, seed int8) RETURNS int8
LANGUAGE SQL STRICT IMMUTABLE PARALLEL SAFE AS
$$ SELECT value + seed + random()::int/0 $$
;
drop table rules_foo2
;
ALTER TABLE check_fk_presence_2 DROP CONSTRAINT check_fk_presence_2_id_fkey
;
CREATE TABLE "public"."iso8859_5_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
drop table fktable
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_extension
;
DROP TABLE pg_temp_22.pg_temp_28002_2, pg_temp_22.pg_temp_28002
;
CREATE DATABASE testdb
;
-- check if constraint is enforced properly at build time
CREATE UNIQUE INDEX CONCURRENTLY concur_index3 ON concur_heap(f2)
;
create function int8alias1eq(int8alias1, int8alias2) returns bool
  strict immutable language internal as 'int8eq'
;
create function report_guc(text) returns text as
$$ select current_setting($1) $$ language sql
set work_mem = '1MB'
;
-- should be created in temp_view_test schema
CREATE VIEW v1 AS SELECT * FROM base_table
;
-- Not a token supported by the configuration's parser, fails even
-- with IF EXISTS.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING IF EXISTS FOR not_a_token, not_a_token
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=19047 host=/tmp/cQ5BudHRYT dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
CREATE TABLE toasted_key (
    id serial,
    toasted_key text PRIMARY KEY,
    toasted_col1 text,
    toasted_col2 text
)
;
-- Test a combination of local and remote triggers
create trigger rem2_trig_row_before before insert on rem2
	for each row execute procedure trigger_data(23,'skidoo')
;
DO $$DECLARE r record;
      BEGIN
      SELECT INTO r reltoastrelid::regclass::text AS table_name FROM pg_class
        WHERE oid = 'reind_con_wide'::regclass;
      EXECUTE 'ALTER TABLE ' || r.table_name || ' RENAME TO reind_con_toast;';
      SELECT INTO r indexrelid::regclass::text AS index_name FROM pg_index
        WHERE indrelid = (SELECT oid FROM pg_class where relname = 'reind_con_toast');
      EXECUTE 'ALTER INDEX ' || r.index_name || ' RENAME TO reind_con_toast_idx;';
    END$$
;
--
-- External C-functions for R-tree methods
--

-- Comparison methods

CREATE FUNCTION cube_eq(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
--
  -- Setup to test an error from GetTupleForTrigger
  --
  CREATE TABLE footrg (a int, b text) PARTITION BY LIST(a)
;
-- error
CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION PUBLIC
;
CREATE FUNCTION plpgsql_call_handler() RETURNS language_handler
  LANGUAGE c AS '$libdir/plpgsql'
;
VACUUM (ANALYZE) pg_catalog.pg_sequence
;
CREATE TEXT SEARCH DICTIONARY hunspell_invalid_1 (
						Template=ispell,
						DictFile=hunspell_sample_long,
						AffFile=ispell_sample
)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_am
;
CREATE TABLE vtype( a integer)
;
drop table p1, p1_c1
;
CREATE FUNCTION gin_extract_query_int4(int4, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER OPERATOR FAMILY dump_test.op_family USING btree ADD
						 OPERATOR 1 <(bigint,int4),
						 OPERATOR 2 <=(bigint,int4),
						 OPERATOR 3 =(bigint,int4),
						 OPERATOR 4 >=(bigint,int4),
						 OPERATOR 5 >(bigint,int4),
						 FUNCTION 1 (int4, int4) btint4cmp(int4,int4),
						 FUNCTION 2 (int4, int4) btint4sortsupport(internal)
;
ALTER TABLE fk_partitioned_fk_6 ATTACH PARTITION fk_partitioned_fk_6_1 FOR VALUES IN (1)
;
DROP TABLE brin_insert_optimization
;
CREATE INDEX dummy_test_idx ON dummy_test_tab
  USING dummy_index_am (i) WITH (
  option_bool = false,
  option_int = 5,
  option_real = 3.1,
  option_enum = 'two',
  option_string_val = NULL,
  option_string_null = 'val')
;
TRUNCATE public.tru_rtable0, public.tru_pk_table CONTINUE IDENTITY CASCADE
;
CREATE TYPE test_ext_type AS ENUM('x', 'y')
;
-- For binary upgrade, set up typed tables this way.
ALTER TABLE ONLY "public"."persons" OF "public"."person_type"
;
CREATE TABLE x2 (a int) TABLESPACE ts1
;
DROP TABLE reindex_schema.tab_dropped
;
DROP TABLE pg_temp_10.pg_temp_25340_2, pg_temp_10.pg_temp_25340
;
ALTER INDEX pg_toast.pg_toast_28582_index RENAME TO reind_con_toast_idx
;
ALTER DATABASE split_db_probe SET enable_indexscan TO off
;
CREATE TABLE "public"."rtest_nothn3" (
    "a" integer,
    "b" "text"
)
;
CREATE FUNCTION date_dist(date, date)
RETURNS int4
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER EXTENSION test_parser ADD function testprs_start(internal,integer)
;
ALTER TABLE tab1_2 ATTACH PARTITION tab1_2_1 FOR VALUES IN (5)
;
create table agg_hash_1 as
select g%10000 as c1, sum(g::numeric) as c2, count(*) as c3
  from agg_data_20k group by g%10000
;
ALTER POLICY p1 ON event_trigger_test USING (TRUE)
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_schema.mv_nodata2 (a) AS
  SELECT generate_series(1, 10) WITH NO DATA
;
CREATE FUNCTION gin_compare_prefix_int8(int8, int8, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE part_7 ATTACH PARTITION part_7_a_null FOR VALUES IN ('a', null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION delete(hstore,text)
RETURNS hstore
AS '$libdir/hstore','hstore_delete'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."pk1" (
    "a" integer NOT NULL
)
PARTITION BY LIST ("a")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION part_hashint4_error(value int4, seed int8) RETURNS int8
LANGUAGE SQL STRICT IMMUTABLE PARALLEL SAFE AS
$$ SELECT value + seed + random()::int/0 $$
;
----------
[DROP_TABLE] drop table rules_foo2
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE check_fk_presence_2 DROP CONSTRAINT check_fk_presence_2_id_fkey
;
----------
[CREATE_TABLE] CREATE TABLE "public"."iso8859_5_inputs" (
    "inbytes" "bytea",
    "description" "text"
)
;
----------
[DROP_TABLE] drop table fktable
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_extension
;
----------
[DROP_TABLE] DROP TABLE pg_temp_22.pg_temp_28002_2, pg_temp_22.pg_temp_28002
;
----------
[CREATE_CATALOG] CREATE DATABASE testdb
;
----------
[ADD_INDEX] -- check if constraint is enforced properly at build time
CREATE UNIQUE INDEX CONCURRENTLY concur_index3 ON concur_heap(f2)
;
----------
[CREATE_PROG_OBJ] create function int8alias1eq(int8alias1, int8alias2) returns bool
  strict immutable language internal as 'int8eq'
;
----------
[CREATE_PROG_OBJ] create function report_guc(text) returns text as
$$ select current_setting($1) $$ language sql
set work_mem = '1MB'
;
----------
[CREATE_VIEW(SELECT)] -- should be created in temp_view_test schema
CREATE VIEW v1 AS SELECT * FROM base_table
;
----------
[ALTER_POLICY] -- Not a token supported by the configuration's parser, fails even
-- with IF EXISTS.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING IF EXISTS FOR not_a_token, not_a_token
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=19047 host=/tmp/cQ5BudHRYT dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[CREATE_TABLE] CREATE TABLE toasted_key (
    id serial,
    toasted_key text PRIMARY KEY,
    toasted_col1 text,
    toasted_col2 text
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Test a combination of local and remote triggers
create trigger rem2_trig_row_before before insert on rem2
	for each row execute procedure trigger_data(23,'skidoo')
;
----------
[BLOCK] DO $$DECLARE r record;
      BEGIN
      SELECT INTO r reltoastrelid::regclass::text AS table_name FROM pg_class
        WHERE oid = 'reind_con_wide'::regclass;
      EXECUTE 'ALTER TABLE ' || r.table_name || ' RENAME TO reind_con_toast;';
      SELECT INTO r indexrelid::regclass::text AS index_name FROM pg_index
        WHERE indrelid = (SELECT oid FROM pg_class where relname = 'reind_con_toast');
      EXECUTE 'ALTER INDEX ' || r.index_name || ' RENAME TO reind_con_toast_idx;';
    END$$
;
----------
[CREATE_PROG_OBJ] --
-- External C-functions for R-tree methods
--

-- Comparison methods

CREATE FUNCTION cube_eq(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] --
  -- Setup to test an error from GetTupleForTrigger
  --
  CREATE TABLE footrg (a int, b text) PARTITION BY LIST(a)
;
----------
[CREATE_SCHEMA] -- error
CREATE SCHEMA IF NOT EXISTS newschemax AUTHORIZATION PUBLIC
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION plpgsql_call_handler() RETURNS language_handler
  LANGUAGE c AS '$libdir/plpgsql'
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_sequence
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY hunspell_invalid_1 (
						Template=ispell,
						DictFile=hunspell_sample_long,
						AffFile=ispell_sample
)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_am
;
----------
[CREATE_TABLE] CREATE TABLE vtype( a integer)
;
----------
[DROP_TABLE] drop table p1, p1_c1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_int4(int4, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY dump_test.op_family USING btree ADD
						 OPERATOR 1 <(bigint,int4),
						 OPERATOR 2 <=(bigint,int4),
						 OPERATOR 3 =(bigint,int4),
						 OPERATOR 4 >=(bigint,int4),
						 OPERATOR 5 >(bigint,int4),
						 FUNCTION 1 (int4, int4) btint4cmp(int4,int4),
						 FUNCTION 2 (int4, int4) btint4sortsupport(internal)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fk_partitioned_fk_6 ATTACH PARTITION fk_partitioned_fk_6_1 FOR VALUES IN (1)
;
----------
[DROP_TABLE] DROP TABLE brin_insert_optimization
;
----------
[ADD_INDEX] CREATE INDEX dummy_test_idx ON dummy_test_tab
  USING dummy_index_am (i) WITH (
  option_bool = false,
  option_int = 5,
  option_real = 3.1,
  option_enum = 'two',
  option_string_val = NULL,
  option_string_null = 'val')
;
----------
[TRUNCATE_TABLE] TRUNCATE public.tru_rtable0, public.tru_pk_table CONTINUE IDENTITY CASCADE
;
----------
[CREATE_TYPE] CREATE TYPE test_ext_type AS ENUM('x', 'y')
;
----------
[ALTER_TABLE] -- For binary upgrade, set up typed tables this way.
ALTER TABLE ONLY "public"."persons" OF "public"."person_type"
;
----------
[CREATE_TABLE] CREATE TABLE x2 (a int) TABLESPACE ts1
;
----------
[DROP_TABLE] DROP TABLE reindex_schema.tab_dropped
;
----------
[DROP_TABLE] DROP TABLE pg_temp_10.pg_temp_25340_2, pg_temp_10.pg_temp_25340
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28582_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_CATALOG] ALTER DATABASE split_db_probe SET enable_indexscan TO off
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_nothn3" (
    "a" integer,
    "b" "text"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION date_dist(date, date)
RETURNS int4
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_parser ADD function testprs_start(internal,integer)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab1_2 ATTACH PARTITION tab1_2_1 FOR VALUES IN (5)
;
----------
[CREATE_TABLE|SELECT] create table agg_hash_1 as
select g%10000 as c1, sum(g::numeric) as c2, count(*) as c3
  from agg_data_20k group by g%10000
;
----------
[ALTER_POLICY] ALTER POLICY p1 ON event_trigger_test USING (TRUE)
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW matview_schema.mv_nodata2 (a) AS
  SELECT generate_series(1, 10) WITH NO DATA
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_int8(int8, int8, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
