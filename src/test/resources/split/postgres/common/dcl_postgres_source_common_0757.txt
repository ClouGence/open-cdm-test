-- test that we can insert the result of a get_changes call into a
-- logged relation. That's really not a good idea in practical terms,
-- but provides a nice test.

-- predictability
SET synchronous_commit = on
;
-- Subtransactions, basic tests
-- create & drop tables
SET SESSION CHARACTERISTICS AS TRANSACTION READ WRITE
;
ALTER FUNCTION "public"."trigfunc"() OWNER TO zyc
;
ROLLBACK WORK TO SAVEPOINT split_ext_savepoint
;
-- Test with invalid stored and server keys.
--
-- The first is valid, to act as a control. The others have too long
-- stored/server keys. They will be re-hashed.
CREATE ROLE regress_passwd_sha_len0 PASSWORD 'SCRAM-SHA-256$4096:A6xHKoH/494E941doaPOYg==$Ky+A30sewHIH3VHQLRN9vYsuzlgNyGNKCh37dy96Rqw=:COPdlNiIkrsacU5QoxydEuOH6e/KfiipeETb/bPw8ZI='
;
-- no

ALTER DEFAULT PRIVILEGES REVOKE USAGE ON SCHEMAS FROM regress_priv_user2
;
EXECUTE dumpOpr('1205')
;
ALTER OPERATOR "public".= ("public"."int8alias2", "public"."int8alias2") OWNER TO zyc
;
REVOKE SELECT ON atest4 FROM regress_priv_user2
;
reset plpgsql.extra_errors
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] -- test that we can insert the result of a get_changes call into a
-- logged relation. That's really not a good idea in practical terms,
-- but provides a nice test.

-- predictability
SET synchronous_commit = on
;
----------
[TRANSACTION|SESSION_SETTING_WRITE] -- Subtransactions, basic tests
-- create & drop tables
SET SESSION CHARACTERISTICS AS TRANSACTION READ WRITE
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."trigfunc"() OWNER TO zyc
;
----------
[TRANSACTION] ROLLBACK WORK TO SAVEPOINT split_ext_savepoint
;
----------
[CREATE_ROLE] -- Test with invalid stored and server keys.
--
-- The first is valid, to act as a control. The others have too long
-- stored/server keys. They will be re-hashed.
CREATE ROLE regress_passwd_sha_len0 PASSWORD 'SCRAM-SHA-256$4096:A6xHKoH/494E941doaPOYg==$Ky+A30sewHIH3VHQLRN9vYsuzlgNyGNKCh37dy96Rqw=:COPdlNiIkrsacU5QoxydEuOH6e/KfiipeETb/bPw8ZI='
;
----------
[REVOKE] -- no

ALTER DEFAULT PRIVILEGES REVOKE USAGE ON SCHEMAS FROM regress_priv_user2
;
----------
[UNSAFE] EXECUTE dumpOpr('1205')
;
----------
[TRANSFER_PRIVILEGE] ALTER OPERATOR "public".= ("public"."int8alias2", "public"."int8alias2") OWNER TO zyc
;
----------
[REVOKE] REVOKE SELECT ON atest4 FROM regress_priv_user2
;
----------
[SESSION_SETTING_WRITE] reset plpgsql.extra_errors
;
