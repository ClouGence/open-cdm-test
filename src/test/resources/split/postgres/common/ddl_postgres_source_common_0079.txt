drop function parent_upd_func()
;
--
-- tests for cursors with named parameter arguments
--
create function namedparmcursor_test1(int, int) returns boolean as $$
declare
    c1 cursor (param1 int, param12 int) for select * from rc_test where a > param1 and b > param12;
    nonsense record;
begin
    open c1(param12 := $2, param1 := $1);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$$ language plpgsql
;
CREATE TABLE test_tsvector(
	t text,
	a tsvector
)
;
TRUNCATE subxids
;
create or replace function trigger_notice() returns trigger as $$
  begin
    raise notice 'trigger % on % % % for %', TG_NAME, TG_TABLE_NAME, TG_WHEN, TG_OP, TG_LEVEL;
    if TG_LEVEL = 'ROW' then
      if TG_OP = 'DELETE' then
        return OLD;
      else
        return NEW;
      end if;
    end if;
    return null;
  end;
  $$ language plpgsql
;
DROP TEXT SEARCH PARSER IF EXISTS test_tsparser_exists
;
CREATE TEMP TABLE quad_point_tbl_ord_seq2 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM quad_point_tbl WHERE p <@ box '(200,200,1000,1000)'
;
ALTER TABLE ONLY "fkpart6"."pk"
    ADD CONSTRAINT "pk_pkey" PRIMARY KEY ("a")
;
CREATE TABLE testpub_insert_onconfl_parted (a int unique, b int) PARTITION by RANGE (a)
;
create table trigger_parted_p2_2 partition of trigger_parted_p2 for values in (2)
;
drop table gin_tbl
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_inherits
;
/*
 * 24.4
 * FOREIGN_DATA_WRAPPER_OPTIONS view
 */
CREATE VIEW foreign_data_wrapper_options AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_data_wrappers w
;
--
-- Test unique index inference with operator class specifications and
-- named collations
--
create unique index op_index_key on insertconflicttest(key, fruit text_pattern_ops)
;
create trigger trigger_alpha
	before insert or update on trigtest
	for each row execute procedure f1_times_10()
;
alter table atacc1 alter test_a drop not null
;
ALTER TABLE blog ENABLE ROW LEVEL SECURITY
;
ALTER INDEX pg_toast.pg_toast_33519_index RENAME TO reind_con_toast_idx
;
-- fail

CREATE VIEW ctlv1 AS SELECT * FROM ctlt4
;
VACUUM (PARALLEL 0) pg_catalog.pg_database
;
ALTER TABLE list_parted2 ATTACH PARTITION list_parted2_def default
;
DROP OPERATOR ###### (int4, int8)
;
-- no table rewrite
ALTER TABLE t2 ADD COLUMN d domain4
;
create table part4 partition of range_parted for values from ('b', 10) to ('b', 20)
;
-- Cleanup
drop view insertconflictview
;
CREATE TYPE tt_enum_type AS ENUM ('a')
;
CREATE FUNCTION ginint4_queryextract(_int4, internal, int2, internal, internal, internal, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
alter text search dictionary alter1.dict set schema alter2
;
VACUUM FULL vactst
;
alter table non_existent alter column bar set not null
;
create cast (integer as xfloat4) without function
;
CREATE FUNCTION gtrgm_distance(internal,text,smallint,oid,internal)
RETURNS float8
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- cannot use more than 1 column as partition key for list partitioned table
CREATE TABLE partitioned (
	a1 int,
	a2 int
) PARTITION BY LIST (a1, a2)
;
-- Setup
CREATE SCHEMA lock_schema1
;
CREATE TRIGGER "transition_table_level1_ri_parent_upd_trigger" AFTER UPDATE ON "public"."transition_table_level1" REFERENCING OLD TABLE AS "d" NEW TABLE AS "i" FOR EACH STATEMENT EXECUTE FUNCTION "public"."transition_table_level1_ri_parent_upd_func"()
;
create sequence ddef4_seq
;
CREATE TRANSFORM FOR int LANGUAGE SQL (FROM SQL WITH FUNCTION prsd_lextype(internal), TO SQL WITH FUNCTION int4recv(internal))
;
CREATE FUNCTION gbt_uuid_consistent(internal,uuid,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
DROP TABLE ATACC1, ATACC2, ATACC3
;
-- check that the table being attached is not part of regular inheritance
CREATE TABLE parent (LIKE list_parted)
;
DROP COLLATION test0
;
-- skips partition
CREATE UNIQUE INDEX ON lt1 (a)
;
CREATE FUNCTION split_operator_probe.int_add(left_value integer, right_value integer)
RETURNS integer
LANGUAGE SQL
IMMUTABLE
AS $$ SELECT left_value + right_value $$
;
DROP FUNCTION trigger_data()
;
ALTER TABLE ctl_table ADD CONSTRAINT foo CHECK (b = 'text')
;
CREATE TABLE FKTABLE ( ftest1 int REFERENCES PKTABLE MATCH FULL ON DELETE CASCADE ON UPDATE CASCADE, ftest2 int )
;
alter table "catalog1".schema1."test" alter column "a1" set statistics 100
;
ANALYZE pg_temp_6.pg_temp_33826_2
;
CREATE TABLE sql_sizing (
    sizing_id       cardinal_number,
    sizing_name     character_data,
    supported_value cardinal_number,
    comments        character_data
)
;
CREATE TABLE cluster_part_tab1 PARTITION OF cluster_part_tab FOR VALUES IN (1)
;
------------------------------------------------------------------------------------------
[DROP_PROG_OBJ] drop function parent_upd_func()
;
----------
[CREATE_PROG_OBJ] --
-- tests for cursors with named parameter arguments
--
create function namedparmcursor_test1(int, int) returns boolean as $$
declare
    c1 cursor (param1 int, param12 int) for select * from rc_test where a > param1 and b > param12;
    nonsense record;
begin
    open c1(param12 := $2, param1 := $1);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE test_tsvector(
	t text,
	a tsvector
)
;
----------
[TRUNCATE_TABLE] TRUNCATE subxids
;
----------
[CREATE_PROG_OBJ] create or replace function trigger_notice() returns trigger as $$
  begin
    raise notice 'trigger % on % % % for %', TG_NAME, TG_TABLE_NAME, TG_WHEN, TG_OP, TG_LEVEL;
    if TG_LEVEL = 'ROW' then
      if TG_OP = 'DELETE' then
        return OLD;
      else
        return NEW;
      end if;
    end if;
    return null;
  end;
  $$ language plpgsql
;
----------
[DROP_POLICY] DROP TEXT SEARCH PARSER IF EXISTS test_tsparser_exists
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE quad_point_tbl_ord_seq2 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM quad_point_tbl WHERE p <@ box '(200,200,1000,1000)'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart6"."pk"
    ADD CONSTRAINT "pk_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE testpub_insert_onconfl_parted (a int unique, b int) PARTITION by RANGE (a)
;
----------
[CREATE_TABLE] create table trigger_parted_p2_2 partition of trigger_parted_p2 for values in (2)
;
----------
[DROP_TABLE] drop table gin_tbl
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_inherits
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.4
 * FOREIGN_DATA_WRAPPER_OPTIONS view
 */
CREATE VIEW foreign_data_wrapper_options AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_data_wrappers w
;
----------
[ADD_INDEX] --
-- Test unique index inference with operator class specifications and
-- named collations
--
create unique index op_index_key on insertconflicttest(key, fruit text_pattern_ops)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trigger_alpha
	before insert or update on trigtest
	for each row execute procedure f1_times_10()
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter test_a drop not null
;
----------
[ALTER_TABLE] ALTER TABLE blog ENABLE ROW LEVEL SECURITY
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33519_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] -- fail

CREATE VIEW ctlv1 AS SELECT * FROM ctlt4
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_database
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 ATTACH PARTITION list_parted2_def default
;
----------
[DROP_PROG_OBJ] DROP OPERATOR ###### (int4, int8)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- no table rewrite
ALTER TABLE t2 ADD COLUMN d domain4
;
----------
[CREATE_TABLE] create table part4 partition of range_parted for values from ('b', 10) to ('b', 20)
;
----------
[DROP_VIEW] -- Cleanup
drop view insertconflictview
;
----------
[CREATE_TYPE] CREATE TYPE tt_enum_type AS ENUM ('a')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ginint4_queryextract(_int4, internal, int2, internal, internal, internal, internal)
RETURNS internal
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_POLICY] alter text search dictionary alter1.dict set schema alter2
;
----------
[ADMIN_TABLE] VACUUM FULL vactst
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table non_existent alter column bar set not null
;
----------
[CREATE_PROG_OBJ] create cast (integer as xfloat4) without function
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_distance(internal,text,smallint,oid,internal)
RETURNS float8
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] -- cannot use more than 1 column as partition key for list partitioned table
CREATE TABLE partitioned (
	a1 int,
	a2 int
) PARTITION BY LIST (a1, a2)
;
----------
[CREATE_SCHEMA] -- Setup
CREATE SCHEMA lock_schema1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "transition_table_level1_ri_parent_upd_trigger" AFTER UPDATE ON "public"."transition_table_level1" REFERENCING OLD TABLE AS "d" NEW TABLE AS "i" FOR EACH STATEMENT EXECUTE FUNCTION "public"."transition_table_level1_ri_parent_upd_func"()
;
----------
[CREATE_SEQUENCE] create sequence ddef4_seq
;
----------
[CREATE_TRANSFORM] CREATE TRANSFORM FOR int LANGUAGE SQL (FROM SQL WITH FUNCTION prsd_lextype(internal), TO SQL WITH FUNCTION int4recv(internal))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_uuid_consistent(internal,uuid,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] DROP TABLE ATACC1, ATACC2, ATACC3
;
----------
[CREATE_TABLE] -- check that the table being attached is not part of regular inheritance
CREATE TABLE parent (LIKE list_parted)
;
----------
[DROP_POLICY] DROP COLLATION test0
;
----------
[ADD_INDEX] -- skips partition
CREATE UNIQUE INDEX ON lt1 (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_operator_probe.int_add(left_value integer, right_value integer)
RETURNS integer
LANGUAGE SQL
IMMUTABLE
AS $$ SELECT left_value + right_value $$
;
----------
[DROP_PROG_OBJ] DROP FUNCTION trigger_data()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ctl_table ADD CONSTRAINT foo CHECK (b = 'text')
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE ( ftest1 int REFERENCES PKTABLE MATCH FULL ON DELETE CASCADE ON UPDATE CASCADE, ftest2 int )
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table "catalog1".schema1."test" alter column "a1" set statistics 100
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_6.pg_temp_33826_2
;
----------
[CREATE_TABLE] CREATE TABLE sql_sizing (
    sizing_id       cardinal_number,
    sizing_name     character_data,
    supported_value cardinal_number,
    comments        character_data
)
;
----------
[CREATE_TABLE] CREATE TABLE cluster_part_tab1 PARTITION OF cluster_part_tab FOR VALUES IN (1)
;
