create table idxpart1 (a int, b int)
;
drop domain ddef3 restrict
;
ALTER TABLE reloptions_test RESET (fillfactor=12)
;
drop table pktable2, fktable2
;
ALTER TABLE pg_temp_26.pg_temp_28293_2 ADD COLUMN newdata pg_temp_26.pg_temp_28293
;
ANALYZE pg_temp_13.pg_temp_26115
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '14831'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
create table fktable(ftest1 int, ftest2 inet, foreign key(ftest1, ftest2) references pktable(ptest1, base1))
;
ALTER TABLE cwi_test ADD PRIMARY KEY USING INDEX cwi_a_nnd
;
create rule r1 as on update to rules_base do instead
  select * from rules_base where f1 = 1 for update
;
-- Verify that re-attaching an already-attached partition index can
-- validate the parent index if it was still invalid, including
-- indirect ancestors in subpartitions.
create table idxpart (a int, b int) partition by range (a)
;
CREATE TABLE "public"."plt1_e_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE TABLE "public"."renamecolumnanother" (
    "d" integer,
    "a" integer,
    "c" integer,
    "w" integer,
    "x" integer
)
;
create index ��ג�������ђ��index2 on ��ג�������ђ�� using hash (�ʬ������������)
;
drop table matest0 cascade
;
DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(_a, c => _c, b => _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(c => _c, b => _b, a => _a);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
-- failed (no role membership)
ALTER CONVERSION alt_conv3 SET SCHEMA alt_nsp2
;
CREATE INDEX "textrange_test_btree" ON "public"."textrange_test" USING "btree" ("tr")
;
CREATE TABLE gtestp (f1 int)
;
create or replace function retc(int) returns two_int8s language plpgsql as
$$ declare r record; begin r := row($1::int8, 1::int8); return r; end $$
;
CREATE TABLE gtest_err_7c (a int PRIMARY KEY, b int GENERATED ALWAYS AS ((SELECT a)) STORED)
;
CREATE TABLE stats_import.part_parent ( i integer ) PARTITION BY RANGE(i)
;
CREATE FUNCTION exists_all(hstore,text[])
RETURNS bool
AS '$libdir/hstore','hstore_exists_all'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE UNIQUE INDEX "num_exp_sub_idx" ON "public"."num_exp_sub" USING "btree" ("id1", "id2")
;
DROP FOREIGN TABLE tru_ftable_parent, tru_ftable_child, tru_pk_ftable,tru_ftable__p1,tru_ftable
;
VACUUM (ANALYZE) pg_catalog.pg_shseclabel
;
DROP TABLE onek_with_null
;
ALTER FOREIGN TABLE agg_bad ADD CHECK (a >= 0)
;
-- Cannot set self as negator
ALTER OPERATOR === (boolean, real) SET (NEGATOR = ===)
;
CREATE FUNCTION "public"."tg_pfield_au"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.name != old.name then
        update PSlot set pfname = new.name where pfname = old.name;
    end if;
    return new;
end;
$$
;
-- certain ALTER TABLE operations on typed tables are not allowed
ALTER TABLE persons ADD COLUMN comment text
;
ALTER TABLE fk_partitioned_fk_full ADD FOREIGN KEY (x, y) REFERENCES fk_notpartitioned_pk MATCH FULL
;
ANALYZE pg_catalog.pg_operator
;
-- should fail

-- Check inherited UPDATE with all children excluded
create table some_tab (a int, b int)
;
CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
CREATE FUNCTION stats_test_func2() RETURNS VOID LANGUAGE plpgsql AS $$BEGIN END;$$
;
DROP VIEW lock_view4
;
alter table trigtest enable always trigger trigtest_a_stmt_tg
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
--
-- Test B-tree fast path (cache rightmost leaf page) optimization.
--

-- First create a tree that's at least three levels deep (i.e. has one level
-- between the root and leaf levels). The text inserted is long.  It won't be
-- TOAST compressed because we use plain storage in the table.  Only a few
-- index tuples fit on each internal page, allowing us to get a tall tree with
-- few pages.  (A tall tree is required to trigger caching.)
--
-- The text column must be the leading column in the index, since suffix
-- truncation would otherwise truncate tuples on internal pages, leaving us
-- with a short tree.
create table btree_tall_tbl(id int4, t text)
;
CREATE TABLE test_int4 (
	i int4
)
;
ANALYZE vactst
;
CALL ptest1(substring(random()::numeric(20,15)::text, 1, 1))
;
CREATE TABLE parttbl2 PARTITION OF parttbl
   (d WITH OPTIONS GENERATED ALWAYS AS (a + b + 1000) STORED)
   FOR VALUES IN (2)
;
VACUUM (TRUNCATE FALSE, DISABLE_PAGE_SKIPPING) vac_truncate_test
;
CREATE TABLE tbl_inh_1() INHERITS (tbl_inh)
;
--
-- OUT/INOUT/TABLE functions
--

-- infer anyrange from anyrange
create function outparam_succeed(i anyrange, out r anyrange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
CREATE TABLE brintest (byteacol bytea,
	charcol "char",
	namecol name,
	int8col bigint,
	int2col smallint,
	int4col integer,
	textcol text,
	oidcol oid,
	tidcol tid,
	float4col real,
	float8col double precision,
	macaddrcol macaddr,
	inetcol inet,
	cidrcol cidr,
	bpcharcol character,
	datecol date,
	timecol time without time zone,
	timestampcol timestamp without time zone,
	timestamptzcol timestamp with time zone,
	intervalcol interval,
	timetzcol time with time zone,
	bitcol bit(10),
	varbitcol bit varying(16),
	numericcol numeric,
	uuidcol uuid,
	int4rangecol int4range,
	lsncol pg_lsn,
	boxcol box
) WITH (fillfactor=10, autovacuum_enabled=off)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_namespace
;
ALTER OPERATOR <= (ismn, ismn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table idxpart1 (a int, b int)
;
----------
[DROP_TYPE] drop domain ddef3 restrict
;
----------
[ALTER_TABLE] ALTER TABLE reloptions_test RESET (fillfactor=12)
;
----------
[DROP_TABLE] drop table pktable2, fktable2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_26.pg_temp_28293_2 ADD COLUMN newdata pg_temp_26.pg_temp_28293
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26115
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '14831'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
----------
[CREATE_TABLE] create table fktable(ftest1 int, ftest2 inet, foreign key(ftest1, ftest2) references pktable(ptest1, base1))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE cwi_test ADD PRIMARY KEY USING INDEX cwi_a_nnd
;
----------
[CREATE_POLICY] create rule r1 as on update to rules_base do instead
  select * from rules_base where f1 = 1 for update
;
----------
[CREATE_TABLE] -- Verify that re-attaching an already-attached partition index can
-- validate the parent index if it was still invalid, including
-- indirect ancestors in subpartitions.
create table idxpart (a int, b int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt1_e_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."renamecolumnanother" (
    "d" integer,
    "a" integer,
    "c" integer,
    "w" integer,
    "x" integer
)
;
----------
[ADD_INDEX] create index ��ג�������ђ��index2 on ��ג�������ђ�� using hash (�ʬ������������)
;
----------
[DROP_TABLE] drop table matest0 cascade
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(_a, c => _c, b => _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
  _a := 10; _b := 30; _c := 50;
  CALL test_proc8c(c => _c, b => _b, a => _a);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
----------
[ALTER_POLICY] -- failed (no role membership)
ALTER CONVERSION alt_conv3 SET SCHEMA alt_nsp2
;
----------
[ADD_INDEX] CREATE INDEX "textrange_test_btree" ON "public"."textrange_test" USING "btree" ("tr")
;
----------
[CREATE_TABLE] CREATE TABLE gtestp (f1 int)
;
----------
[CREATE_PROG_OBJ] create or replace function retc(int) returns two_int8s language plpgsql as
$$ declare r record; begin r := row($1::int8, 1::int8); return r; end $$
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_7c (a int PRIMARY KEY, b int GENERATED ALWAYS AS ((SELECT a)) STORED)
;
----------
[CREATE_TABLE] CREATE TABLE stats_import.part_parent ( i integer ) PARTITION BY RANGE(i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION exists_all(hstore,text[])
RETURNS bool
AS '$libdir/hstore','hstore_exists_all'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "num_exp_sub_idx" ON "public"."num_exp_sub" USING "btree" ("id1", "id2")
;
----------
[DROP_TABLE] DROP FOREIGN TABLE tru_ftable_parent, tru_ftable_child, tru_pk_ftable,tru_ftable__p1,tru_ftable
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_shseclabel
;
----------
[DROP_TABLE] DROP TABLE onek_with_null
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER FOREIGN TABLE agg_bad ADD CHECK (a >= 0)
;
----------
[ALTER_PROG_OBJ] -- Cannot set self as negator
ALTER OPERATOR === (boolean, real) SET (NEGATOR = ===)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_pfield_au"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.name != old.name then
        update PSlot set pfname = new.name where pfname = old.name;
    end if;
    return new;
end;
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] -- certain ALTER TABLE operations on typed tables are not allowed
ALTER TABLE persons ADD COLUMN comment text
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk_full ADD FOREIGN KEY (x, y) REFERENCES fk_notpartitioned_pk MATCH FULL
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_operator
;
----------
[CREATE_TABLE] -- should fail

-- Check inherited UPDATE with all children excluded
create table some_tab (a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES WITH (MODULUS 10, REMAINDER 1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION stats_test_func2() RETURNS VOID LANGUAGE plpgsql AS $$BEGIN END;$$
;
----------
[DROP_VIEW] DROP VIEW lock_view4
;
----------
[ALTER_TABLE] alter table trigtest enable always trigger trigtest_a_stmt_tg
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] --
-- Test B-tree fast path (cache rightmost leaf page) optimization.
--

-- First create a tree that's at least three levels deep (i.e. has one level
-- between the root and leaf levels). The text inserted is long.  It won't be
-- TOAST compressed because we use plain storage in the table.  Only a few
-- index tuples fit on each internal page, allowing us to get a tall tree with
-- few pages.  (A tall tree is required to trigger caching.)
--
-- The text column must be the leading column in the index, since suffix
-- truncation would otherwise truncate tuples on internal pages, leaving us
-- with a short tree.
create table btree_tall_tbl(id int4, t text)
;
----------
[CREATE_TABLE] CREATE TABLE test_int4 (
	i int4
)
;
----------
[ADMIN_TABLE] ANALYZE vactst
;
----------
[CALL_PROG_OBJ] CALL ptest1(substring(random()::numeric(20,15)::text, 1, 1))
;
----------
[CREATE_TABLE] CREATE TABLE parttbl2 PARTITION OF parttbl
   (d WITH OPTIONS GENERATED ALWAYS AS (a + b + 1000) STORED)
   FOR VALUES IN (2)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, DISABLE_PAGE_SKIPPING) vac_truncate_test
;
----------
[CREATE_TABLE] CREATE TABLE tbl_inh_1() INHERITS (tbl_inh)
;
----------
[CREATE_PROG_OBJ] --
-- OUT/INOUT/TABLE functions
--

-- infer anyrange from anyrange
create function outparam_succeed(i anyrange, out r anyrange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
----------
[CREATE_TABLE] CREATE TABLE brintest (byteacol bytea,
	charcol "char",
	namecol name,
	int8col bigint,
	int2col smallint,
	int4col integer,
	textcol text,
	oidcol oid,
	tidcol tid,
	float4col real,
	float8col double precision,
	macaddrcol macaddr,
	inetcol inet,
	cidrcol cidr,
	bpcharcol character,
	datecol date,
	timecol time without time zone,
	timestampcol timestamp without time zone,
	timestamptzcol timestamp with time zone,
	intervalcol interval,
	timetzcol time with time zone,
	bitcol bit(10),
	varbitcol bit varying(16),
	numericcol numeric,
	uuidcol uuid,
	int4rangecol int4range,
	lsncol pg_lsn,
	boxcol box
) WITH (fillfactor=10, autovacuum_enabled=off)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_namespace
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ismn, ismn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
