create table hpart1 partition of hash_parted for values with (modulus 2, remainder 1)
;
-- check validation when attaching range partitions
CREATE TABLE range_parted (
	a int,
	b int
) PARTITION BY RANGE (a, b)
;
create table dcomptable (f1 dcomptype[])
;
-- enum check

create type rainbow as enum ('r','o','y','g','b','i','v')
;
DROP TABLE testcase
;
ALTER TABLE pg_temp_12.pg_temp_26389_2 ADD COLUMN newdata pg_temp_12.pg_temp_26389
;
ALTER TABLE ONLY public.ths ATTACH PARTITION public.ths_p1 FOR VALUES WITH (modulus 3, remainder 0)
;
VACUUM (FULL) pg_catalog.pg_foreign_server
;
/*
 * text search configuration for russian language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * russian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
CREATE FUNCTION is_valid(issn13)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE COLLATION coll_dup_chk (FROM = "C", VERSION = "1")
;
-- ordinary view on top of security invoker view permissions

CREATE TABLE base_tbl(a int, b text, c float)
;
CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (b)
;
/*
 * 5.65
 * TRANSFORMS view
 */

CREATE VIEW transforms AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(l.lanname AS sql_identifier) AS group_name,
           CAST('FROM SQL' AS character_data) AS transform_type
    FROM pg_type t JOIN pg_transform x ON t.oid = x.trftype
         JOIN pg_language l ON x.trflang = l.oid
         JOIN pg_proc p ON x.trffromsql = p.oid
         JOIN pg_namespace nt ON t.typnamespace = nt.oid
         JOIN pg_namespace np ON p.pronamespace = np.oid

  UNION

    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(l.lanname AS sql_identifier) AS group_name,
           CAST('TO SQL' AS character_data) AS transform_type
    FROM pg_type t JOIN pg_transform x ON t.oid = x.trftype
         JOIN pg_language l ON x.trflang = l.oid
         JOIN pg_proc p ON x.trftosql = p.oid
         JOIN pg_namespace nt ON t.typnamespace = nt.oid
         JOIN pg_namespace np ON p.pronamespace = np.oid

  ORDER BY udt_catalog, udt_schema, udt_name, group_name, transform_type  -- some sensible grouping for interactive use
;
ALTER TABLE "public"."rule_merge1" DISABLE RULE "rule3"
;
DROP FUNCTION snoop(anyelement)
;
create or replace function sc_test() returns setof integer as $$
declare
  c cursor for select * from generate_series(1, 10);
  x integer;
begin
  open c;
  move forward all in c;
  fetch backward from c into x;
  if found then
    return next x;
  end if;
  close c;
end;
$$ language plpgsql
;
CREATE SUBSCRIPTION testsub2 CONNECTION 'port=16260 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION testpub2
;
CREATE TEMP TABLE pg_temp_13.pg_temp_25576_2 (tid pg_catalog.tid)
;
create temp table nt2 (
  id int primary key,
  nt1_id int,
  b1 boolean,
  b2 boolean,
  foreign key (nt1_id) references nt1(id)
)
;
-- CLUSTER
CREATE INDEX sro_cluster_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
;
alter table only idxpart add primary key (a)
;
CREATE INDEX ON syscolcol_table (ctid)
;
CREATE FOREIGN TABLE ftable ( x int ) SERVER loopback OPTIONS ( table_name 'batch_table', batch_size '1' )
;
CREATE FUNCTION isneq(ean13, issn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE SUBSCRIPTION tap_sub_schema CONNECTION 'port=30643 host=/tmp/nKvmvnSQwd dbname=postgres' PUBLICATION tap_pub_schema
;
ALTER TABLE ONLY "public"."mlparted1" ATTACH PARTITION "public"."mlparted11" FOR VALUES FROM (2) TO (5)
;
CREATE INDEX ptif_test2_index ON ptif_test2 (a)
;
VACUUM (ANALYZE) pg_catalog.pg_cast
;
CREATE TEMP TABLE rescan_bhs (a INT)
;
create table convslot_test_parent (col1 text primary key)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest21a"
    ADD CONSTRAINT "gtest21a_pkey" PRIMARY KEY ("a")
;
CREATE RULE "rule1" AS
    ON INSERT TO "public"."rule_merge1" DO INSTEAD  INSERT INTO "public"."rule_merge2" ("a", "b")
  VALUES ("new"."a", "new"."b")
;
CREATE TEMP TABLE CHAR_TBL(f1 char)
;
CREATE TABLE itest15 (id integer GENERATED ALWAYS AS IDENTITY NOT NULL)
;
create function mr_outparam_fail(i anyelement, out r anymultirange, out t text)
  as $$ select '[1,10]', 'foo' $$ language sql
;
CREATE PROCEDURE inner_p (f1 int)
AS $$
BEGIN
  RAISE NOTICE 'inner_p(%)', f1;
END
$$ LANGUAGE plpgsql
;
-- instead create in public first, move to catalog
CREATE TABLE new_system_table(id serial primary key, othercol text)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
create index on idxpart (a)
;
drop table stmt_trig_on_empty_upd cascade
;
CREATE TABLE public.test_myvarchar (
        a myvarchar
    )
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_aggregate
;
-- Test case of ALTER INDEX with abuse of column names for indexes.
-- This grammar is not officially supported, but the parser allows it.
CREATE INDEX btree_tall_idx2 ON btree_tall_tbl (id)
;
CREATE TABLE quuux_default PARTITION OF quuux DEFAULT PARTITION BY LIST (b)
;
DROP EXTENSION tsm_system_time
;
-- fail
CREATE UNIQUE INDEX cwi_uniq4_idx ON cwi_test(b collate "POSIX")
;
VACUUM (PARALLEL 2) pg_catalog.pg_default_acl
;
create table reservations ( room_id integer not null, booked_during daterange )
;
drop function dump_insert()
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table hpart1 partition of hash_parted for values with (modulus 2, remainder 1)
;
----------
[CREATE_TABLE] -- check validation when attaching range partitions
CREATE TABLE range_parted (
	a int,
	b int
) PARTITION BY RANGE (a, b)
;
----------
[CREATE_TABLE] create table dcomptable (f1 dcomptype[])
;
----------
[CREATE_TYPE] -- enum check

create type rainbow as enum ('r','o','y','g','b','i','v')
;
----------
[DROP_TABLE] DROP TABLE testcase
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26389_2 ADD COLUMN newdata pg_temp_12.pg_temp_26389
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY public.ths ATTACH PARTITION public.ths_p1 FOR VALUES WITH (modulus 3, remainder 0)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_foreign_server
;
----------
[CREATE_POLICY] /*
 * text search configuration for russian language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * russian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION is_valid(issn13)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE COLLATION coll_dup_chk (FROM = "C", VERSION = "1")
;
----------
[CREATE_TABLE] -- ordinary view on top of security invoker view permissions

CREATE TABLE base_tbl(a int, b text, c float)
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (b)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 5.65
 * TRANSFORMS view
 */

CREATE VIEW transforms AS
    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(l.lanname AS sql_identifier) AS group_name,
           CAST('FROM SQL' AS character_data) AS transform_type
    FROM pg_type t JOIN pg_transform x ON t.oid = x.trftype
         JOIN pg_language l ON x.trflang = l.oid
         JOIN pg_proc p ON x.trffromsql = p.oid
         JOIN pg_namespace nt ON t.typnamespace = nt.oid
         JOIN pg_namespace np ON p.pronamespace = np.oid

  UNION

    SELECT CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(l.lanname AS sql_identifier) AS group_name,
           CAST('TO SQL' AS character_data) AS transform_type
    FROM pg_type t JOIN pg_transform x ON t.oid = x.trftype
         JOIN pg_language l ON x.trflang = l.oid
         JOIN pg_proc p ON x.trftosql = p.oid
         JOIN pg_namespace nt ON t.typnamespace = nt.oid
         JOIN pg_namespace np ON p.pronamespace = np.oid

  ORDER BY udt_catalog, udt_schema, udt_name, group_name, transform_type  -- some sensible grouping for interactive use
;
----------
[ALTER_TABLE] ALTER TABLE "public"."rule_merge1" DISABLE RULE "rule3"
;
----------
[DROP_PROG_OBJ] DROP FUNCTION snoop(anyelement)
;
----------
[CREATE_PROG_OBJ] create or replace function sc_test() returns setof integer as $$
declare
  c cursor for select * from generate_series(1, 10);
  x integer;
begin
  open c;
  move forward all in c;
  fetch backward from c into x;
  if found then
    return next x;
  end if;
  close c;
end;
$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub2 CONNECTION 'port=16260 host=/tmp/Z2NRyt87xn dbname=postgres' PUBLICATION testpub2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_25576_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create temp table nt2 (
  id int primary key,
  nt1_id int,
  b1 boolean,
  b2 boolean,
  foreign key (nt1_id) references nt1(id)
)
;
----------
[ADD_INDEX] -- CLUSTER
CREATE INDEX sro_cluster_idx ON sro_tab ((sro_ifun(a) + sro_ifun(0)))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table only idxpart add primary key (a)
;
----------
[ADD_INDEX] CREATE INDEX ON syscolcol_table (ctid)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ftable ( x int ) SERVER loopback OPTIONS ( table_name 'batch_table', batch_size '1' )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ean13, issn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_schema CONNECTION 'port=30643 host=/tmp/nKvmvnSQwd dbname=postgres' PUBLICATION tap_pub_schema
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted1" ATTACH PARTITION "public"."mlparted11" FOR VALUES FROM (2) TO (5)
;
----------
[ADD_INDEX] CREATE INDEX ptif_test2_index ON ptif_test2 (a)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_cast
;
----------
[CREATE_TABLE] CREATE TEMP TABLE rescan_bhs (a INT)
;
----------
[CREATE_TABLE] create table convslot_test_parent (col1 text primary key)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest21a"
    ADD CONSTRAINT "gtest21a_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_POLICY] CREATE RULE "rule1" AS
    ON INSERT TO "public"."rule_merge1" DO INSTEAD  INSERT INTO "public"."rule_merge2" ("a", "b")
  VALUES ("new"."a", "new"."b")
;
----------
[CREATE_TABLE] CREATE TEMP TABLE CHAR_TBL(f1 char)
;
----------
[CREATE_TABLE] CREATE TABLE itest15 (id integer GENERATED ALWAYS AS IDENTITY NOT NULL)
;
----------
[CREATE_PROG_OBJ] create function mr_outparam_fail(i anyelement, out r anymultirange, out t text)
  as $$ select '[1,10]', 'foo' $$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE inner_p (f1 int)
AS $$
BEGIN
  RAISE NOTICE 'inner_p(%)', f1;
END
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] -- instead create in public first, move to catalog
CREATE TABLE new_system_table(id serial primary key, othercol text)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
----------
[ADD_INDEX] create index on idxpart (a)
;
----------
[DROP_TABLE] drop table stmt_trig_on_empty_upd cascade
;
----------
[CREATE_TABLE] CREATE TABLE public.test_myvarchar (
        a myvarchar
    )
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_aggregate
;
----------
[ADD_INDEX] -- Test case of ALTER INDEX with abuse of column names for indexes.
-- This grammar is not officially supported, but the parser allows it.
CREATE INDEX btree_tall_idx2 ON btree_tall_tbl (id)
;
----------
[CREATE_TABLE] CREATE TABLE quuux_default PARTITION OF quuux DEFAULT PARTITION BY LIST (b)
;
----------
[DROP_LIBRARY] DROP EXTENSION tsm_system_time
;
----------
[ADD_INDEX] -- fail
CREATE UNIQUE INDEX cwi_uniq4_idx ON cwi_test(b collate "POSIX")
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_default_acl
;
----------
[CREATE_TABLE] create table reservations ( room_id integer not null, booked_during daterange )
;
----------
[DROP_PROG_OBJ] drop function dump_insert()
;
