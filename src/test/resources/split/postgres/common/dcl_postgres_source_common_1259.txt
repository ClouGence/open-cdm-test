-- fail
revoke usage on type textrange1 from public
;
-- semantic error
GRANT USAGE ON FUNCTION priv_testagg1(int) TO regress_priv_user3
;
-- Second permanent schema
SET search_path = 'pgss_schema_2'
;
GRANT SELECT ON atestv4 TO regress_priv_user2
;
-- Test GroupAggregate paths by disabling hash aggregates.
SET enable_hashagg TO false
;
explain (analyze, costs off, summary off, timing off, buffers off)  execute q1 (2,2)
;
-- fail, no SET option any more
SET ROLE pg_read_all_stats
;
EXECUTE dumpOpr('1129')
;
EXECUTE dumpOpr('587')
;
-- Each DefineIndex() ACL check uses either the original userid or the table
-- owner userid; see its header comment.  Here, confirm that DefineIndex()
-- uses its original userid where necessary.  The test works by creating
-- indexes that refer to as many sorts of objects as possible, with the table
-- owner having as few applicable privileges as possible.  (The privileges.sql
-- regress_sro_user tests look for the opposite defect; they confirm that
-- DefineIndex() uses the table owner userid where necessary.)

-- Don't override tablespaces; this version lacks allow_in_place_tablespaces.

BEGIN
;
------------------------------------------------------------------------------------------
[REVOKE] -- fail
revoke usage on type textrange1 from public
;
----------
[GRANT] -- semantic error
GRANT USAGE ON FUNCTION priv_testagg1(int) TO regress_priv_user3
;
----------
[SESSION_SETTING_WRITE] -- Second permanent schema
SET search_path = 'pgss_schema_2'
;
----------
[GRANT] GRANT SELECT ON atestv4 TO regress_priv_user2
;
----------
[SESSION_SETTING_WRITE] -- Test GroupAggregate paths by disabling hash aggregates.
SET enable_hashagg TO false
;
----------
[UNSAFE] explain (analyze, costs off, summary off, timing off, buffers off)  execute q1 (2,2)
;
----------
[SWITCH_ROLE] -- fail, no SET option any more
SET ROLE pg_read_all_stats
;
----------
[UNSAFE] EXECUTE dumpOpr('1129')
;
----------
[UNSAFE] EXECUTE dumpOpr('587')
;
----------
[TRANSACTION] -- Each DefineIndex() ACL check uses either the original userid or the table
-- owner userid; see its header comment.  Here, confirm that DefineIndex()
-- uses its original userid where necessary.  The test works by creating
-- indexes that refer to as many sorts of objects as possible, with the table
-- owner having as few applicable privileges as possible.  (The privileges.sql
-- regress_sro_user tests look for the opposite defect; they confirm that
-- DefineIndex() uses the table owner userid where necessary.)

-- Don't override tablespaces; this version lacks allow_in_place_tablespaces.

BEGIN
;
