CREATE AGGREGATE "public"."myaggn01a"(*) (
    SFUNC = "public"."stfnp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
CREATE TABLE pk11 PARTITION OF pk1 FOR VALUES IN (1)
;
CREATE PUBLICATION regress_pub1
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
ANALYZE pg_temp_9.pg_temp_26894
;
-- Test WHEN conditions

CREATE TRIGGER trig_row_before_insupd
BEFORE INSERT OR UPDATE ON rem1
FOR EACH ROW
WHEN (NEW.f2 like '%update%')
EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
CREATE TABLE "public"."gtest30" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
-- Check that RESET works well
ALTER TABLE reloptions_test RESET (fillfactor)
;
do $$
declare x var_record_colnn;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
DROP TRIGGER IF EXISTS account_view_insert ON split_trigger_probe.account_view
;
CREATE FUNCTION "public"."widget_in"("cstring") RETURNS "public"."widget"
    LANGUAGE "c" IMMUTABLE STRICT
    AS '/src/src/test/regress/regress.so', 'widget_in'
;
DROP TYPE test_ext_type
;
CREATE SCHEMA testpub_rf_schema2
;
COMMENT ON FUNCTION cube_ge(cube, cube) IS 'greater than or equal to'
;
CREATE POLICY p1 ON dob_t1 TO regress_rls_dob_role1,regress_rls_dob_role1,regress_rls_dob_role2 USING (true)
;
CREATE INDEX "bt_f8_index" ON "public"."bt_f8_heap" USING "btree" ("seqno")
;
DROP TABLE pg_temp_18.pg_temp_26711_2, pg_temp_18.pg_temp_26711
;
CREATE COLLATION mycoll1 FROM "C"
;
CREATE TABLE t_009_tbl4 (id int, msg text)
;
create temp table tt5(f1 int, f2 int)
;
-- Opclasses

CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING bloom AS
	OPERATOR	1	=(int4, int4),
	FUNCTION	1	hashint4(int4)
;
create table alterlock (f1 int primary key, f2 text)
;
-- Empty-table DefineIndex()
CREATE UNIQUE INDEX u0rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  TABLESPACE regress_create_idx_tblspace
  WHERE s.index_row_if(y)
;
COMMENT ON INDEX comment_test_pk IS 'Index backing the PRIMARY KEY of comment_test'
;
CREATE TABLE var_ptbl_ones PARTITION OF var_ptbl FOR VALUES FROM ('0') TO ('10')
;
CREATE POLICY p3 ON rls_tbl FOR UPDATE USING (c1 <= 3) WITH CHECK (c1 > 5)
;
-- test with INSTEAD OF INSERT trigger on a view
CREATE TABLE instead_of_insert_tbl(id serial, name text)
;
CREATE TABLE clstrpart3 PARTITION OF clstrpart DEFAULT PARTITION BY RANGE (a)
;
CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=18237 host=/tmp/6EQjqv0kuf dbname=postgres' PUBLICATION pub5
;
-- also drops inh_child table
drop table inh_parent_1 cascade
;
DROP OPERATOR FAMILY alt_opf5 USING btree
;
DROP PROCEDURE ptest1
;
CREATE TABLE part32_self_fk PARTITION OF part3_self_fk FOR VALUES FROM (20) TO (30)
;
CREATE FUNCTION lquery_out(lquery)
RETURNS cstring
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE PUBLICATION testpub_xxx WITH (publish_generated_columns = foo)
;
ALTER TABLE tx1 DROP COLUMN b
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_statistic_ext_data
;
create trigger tg_wslot_biu before insert or update
    on WSlot for each row execute procedure tg_wslot_biu()
;
COMMENT ON FUNCTION seg_over_right(seg, seg) IS
'overlaps or is right of'
;
ALTER OPERATOR <= (upc, upc) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0
;
do $$ <<b>> declare c two_int8s; d nested_int8s;
begin
  b.c := row(1,2);
  b.d := row(b.c, row(b.c.q1, b.c.q2+1));
  raise notice 'b.c = %, b.d = %', b.c, b.d;
  b.c.q1 := 10;
  b.d.c1 := row(11,12);
  b.d.c2.q2 := 42;
  raise notice 'b.c = %, b.d = %', b.c, b.d;
  raise notice 'b.c.q1 = %, b.d.c2 = %', b.c.q1, b.d.c2;
  raise notice '(b.d).c2.q2 = %', (b.d).c2.q2;  -- doesn't work without parens
  raise notice '(b.d.c2).q2 = %', (b.d.c2).q2;  -- doesn't work without parens
end$$
;
-- Test snapshot management in serializable transactions involving triggers
-- per bug report in 6bc73d4c0910042358k3d1adff3qa36f8df75198ecea@mail.gmail.com
CREATE FUNCTION serializable_update_trig() RETURNS trigger LANGUAGE plpgsql AS
$$
declare
	rec record;
begin
	new.description = 'updated in trigger';
	return new;
end;
$$
;
SECURITY LABEL ON FUNCTION f5(text)
    IS 'system_u:object_r:sepgsql_regtest_trusted_proc_exec_t:s0'
;
CALL test_proc10(a => _a, b => _b, c => _c)
;
/*
 * 5.13
 * CHECK_CONSTRAINT_ROUTINE_USAGE view
 */

CREATE VIEW check_constraint_routine_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name
    FROM pg_namespace nc, pg_constraint c, pg_depend d, pg_proc p, pg_namespace np
    WHERE nc.oid = c.connamespace
      AND c.contype = 'c'
      AND c.oid = d.objid
      AND d.classid = 'pg_catalog.pg_constraint'::regclass
      AND d.refobjid = p.oid
      AND d.refclassid = 'pg_catalog.pg_proc'::regclass
      AND p.pronamespace = np.oid
      AND pg_has_role(p.proowner, 'USAGE')
;
CREATE INDEX "gist_pointidx" ON "public"."gist_point_tbl" USING "gist" ("p") WITH ("fillfactor"='40')
;
CREATE INDEX idx_b_id_for_A_1231377909 ON A1 USING btree(b_id "pg_catalog"."int4_ops" asc NULLS LAST)
;
create aggregate newcnt1 (sfunc = int4inc,
			  stype = int4,
			  initcond = '0')
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."student" INHERIT "public"."person"
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn01a"(*) (
    SFUNC = "public"."stfnp",
    STYPE = integer[],
    INITCOND = '{}',
    FINALFUNC = "public"."ffnp"
)
;
----------
[CREATE_TABLE] CREATE TABLE pk11 PARTITION OF pk1 FOR VALUES IN (1)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION regress_pub1
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26894
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Test WHEN conditions

CREATE TRIGGER trig_row_before_insupd
BEFORE INSERT OR UPDATE ON rem1
FOR EACH ROW
WHEN (NEW.f2 like '%update%')
EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest30" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[ALTER_TABLE] -- Check that RESET works well
ALTER TABLE reloptions_test RESET (fillfactor)
;
----------
[BLOCK] do $$
declare x var_record_colnn;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[DROP_TRIGGER] DROP TRIGGER IF EXISTS account_view_insert ON split_trigger_probe.account_view
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."widget_in"("cstring") RETURNS "public"."widget"
    LANGUAGE "c" IMMUTABLE STRICT
    AS '/src/src/test/regress/regress.so', 'widget_in'
;
----------
[DROP_TYPE] DROP TYPE test_ext_type
;
----------
[CREATE_SCHEMA] CREATE SCHEMA testpub_rf_schema2
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_ge(cube, cube) IS 'greater than or equal to'
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON dob_t1 TO regress_rls_dob_role1,regress_rls_dob_role1,regress_rls_dob_role2 USING (true)
;
----------
[ADD_INDEX] CREATE INDEX "bt_f8_index" ON "public"."bt_f8_heap" USING "btree" ("seqno")
;
----------
[DROP_TABLE] DROP TABLE pg_temp_18.pg_temp_26711_2, pg_temp_18.pg_temp_26711
;
----------
[CREATE_POLICY] CREATE COLLATION mycoll1 FROM "C"
;
----------
[CREATE_TABLE] CREATE TABLE t_009_tbl4 (id int, msg text)
;
----------
[CREATE_TABLE] create temp table tt5(f1 int, f2 int)
;
----------
[CREATE_PROG_OBJ] -- Opclasses

CREATE OPERATOR CLASS int4_ops
DEFAULT FOR TYPE int4 USING bloom AS
	OPERATOR	1	=(int4, int4),
	FUNCTION	1	hashint4(int4)
;
----------
[CREATE_TABLE] create table alterlock (f1 int primary key, f2 text)
;
----------
[ADD_INDEX] -- Empty-table DefineIndex()
CREATE UNIQUE INDEX u0rows ON s.x USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll s.citext_pattern_ops)
  TABLESPACE regress_create_idx_tblspace
  WHERE s.index_row_if(y)
;
----------
[COMMENT_INDEX] COMMENT ON INDEX comment_test_pk IS 'Index backing the PRIMARY KEY of comment_test'
;
----------
[CREATE_TABLE] CREATE TABLE var_ptbl_ones PARTITION OF var_ptbl FOR VALUES FROM ('0') TO ('10')
;
----------
[CREATE_POLICY] CREATE POLICY p3 ON rls_tbl FOR UPDATE USING (c1 <= 3) WITH CHECK (c1 > 5)
;
----------
[CREATE_TABLE] -- test with INSTEAD OF INSERT trigger on a view
CREATE TABLE instead_of_insert_tbl(id serial, name text)
;
----------
[CREATE_TABLE] CREATE TABLE clstrpart3 PARTITION OF clstrpart DEFAULT PARTITION BY RANGE (a)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=18237 host=/tmp/6EQjqv0kuf dbname=postgres' PUBLICATION pub5
;
----------
[DROP_TABLE] -- also drops inh_child table
drop table inh_parent_1 cascade
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf5 USING btree
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE ptest1
;
----------
[CREATE_TABLE] CREATE TABLE part32_self_fk PARTITION OF part3_self_fk FOR VALUES FROM (20) TO (30)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lquery_out(lquery)
RETURNS cstring
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_xxx WITH (publish_generated_columns = foo)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE tx1 DROP COLUMN b
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_wslot_biu before insert or update
    on WSlot for each row execute procedure tg_wslot_biu()
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_over_right(seg, seg) IS
'overlaps or is right of'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (upc, upc) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_TABLE|SELECT] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0
;
----------
[BLOCK] do $$ <<b>> declare c two_int8s; d nested_int8s;
begin
  b.c := row(1,2);
  b.d := row(b.c, row(b.c.q1, b.c.q2+1));
  raise notice 'b.c = %, b.d = %', b.c, b.d;
  b.c.q1 := 10;
  b.d.c1 := row(11,12);
  b.d.c2.q2 := 42;
  raise notice 'b.c = %, b.d = %', b.c, b.d;
  raise notice 'b.c.q1 = %, b.d.c2 = %', b.c.q1, b.d.c2;
  raise notice '(b.d).c2.q2 = %', (b.d).c2.q2;  -- doesn't work without parens
  raise notice '(b.d.c2).q2 = %', (b.d.c2).q2;  -- doesn't work without parens
end$$
;
----------
[CREATE_PROG_OBJ] -- Test snapshot management in serializable transactions involving triggers
-- per bug report in 6bc73d4c0910042358k3d1adff3qa36f8df75198ecea@mail.gmail.com
CREATE FUNCTION serializable_update_trig() RETURNS trigger LANGUAGE plpgsql AS
$$
declare
	rec record;
begin
	new.description = 'updated in trigger';
	return new;
end;
$$
;
----------
[UNKNOWN] SECURITY LABEL ON FUNCTION f5(text)
    IS 'system_u:object_r:sepgsql_regtest_trusted_proc_exec_t:s0'
;
----------
[CALL_PROG_OBJ] CALL test_proc10(a => _a, b => _b, c => _c)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.13
 * CHECK_CONSTRAINT_ROUTINE_USAGE view
 */

CREATE VIEW check_constraint_routine_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name
    FROM pg_namespace nc, pg_constraint c, pg_depend d, pg_proc p, pg_namespace np
    WHERE nc.oid = c.connamespace
      AND c.contype = 'c'
      AND c.oid = d.objid
      AND d.classid = 'pg_catalog.pg_constraint'::regclass
      AND d.refobjid = p.oid
      AND d.refclassid = 'pg_catalog.pg_proc'::regclass
      AND p.pronamespace = np.oid
      AND pg_has_role(p.proowner, 'USAGE')
;
----------
[ADD_INDEX] CREATE INDEX "gist_pointidx" ON "public"."gist_point_tbl" USING "gist" ("p") WITH ("fillfactor"='40')
;
----------
[ADD_INDEX] CREATE INDEX idx_b_id_for_A_1231377909 ON A1 USING btree(b_id "pg_catalog"."int4_ops" asc NULLS LAST)
;
----------
[CREATE_PROG_OBJ] create aggregate newcnt1 (sfunc = int4inc,
			  stype = int4,
			  initcond = '0')
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."student" INHERIT "public"."person"
;
