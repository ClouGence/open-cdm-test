create table rlp (a int, b varchar) partition by range (a)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_extension
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table rlp (a int, b varchar) partition by range (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_extension
;
