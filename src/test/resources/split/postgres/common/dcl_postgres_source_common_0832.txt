EXECUTE dumpFunc('36289')
;
-- Ensure no ordering is requested when enable_presorted_aggregate is off
set enable_presorted_aggregate to off
;
LOCK TABLE pg_catalog.pg_amop IN ACCESS SHARE MODE
;
LOCK TABLE "public"."rule_merge1" IN ACCESS SHARE MODE
;
-- Try with no matching partitions. One subplan should remain in this case,
-- but it shouldn't be executed.
explain (analyze, costs off, summary off, timing off)  execute q1 (0,0)
;
ALTER SUBSCRIPTION tap_sub REFRESH PUBLICATION WITH (copy_data = true)
;
ALTER TABLE public.inet_tbl OWNER TO zyc
;
SET LOCAL ivfflat.probes = 1
;
copy rc_test from stdin
;
CREATE STATISTICS tststats.s10 ON a, b FROM pg_toast.pg_toast_33040
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('36289')
;
----------
[SESSION_SETTING_WRITE] -- Ensure no ordering is requested when enable_presorted_aggregate is off
set enable_presorted_aggregate to off
;
----------
[SESSION_LOCK] LOCK TABLE pg_catalog.pg_amop IN ACCESS SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."rule_merge1" IN ACCESS SHARE MODE
;
----------
[UNSAFE] -- Try with no matching partitions. One subplan should remain in this case,
-- but it shouldn't be executed.
explain (analyze, costs off, summary off, timing off)  execute q1 (0,0)
;
----------
[ADMIN_PUB_SUB] ALTER SUBSCRIPTION tap_sub REFRESH PUBLICATION WITH (copy_data = true)
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.inet_tbl OWNER TO zyc
;
----------
[SESSION_SETTING_WRITE] SET LOCAL ivfflat.probes = 1
;
----------
[DATA_IMPORT] copy rc_test from stdin
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS tststats.s10 ON a, b FROM pg_toast.pg_toast_33040
;
