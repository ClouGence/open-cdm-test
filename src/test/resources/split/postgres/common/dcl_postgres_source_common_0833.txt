EXECUTE dumpOpr('1865')
;
ALTER TABLE public.transition_table_status OWNER TO zyc
;
EXECUTE dumpFunc('16695')
;
EXECUTE dumpAgg('20455')
;
rollback to f
;
CREATE STATISTICS regression_vacuumdb_test_stat ON a, b FROM regression_vacuumdb_test
;
COMMIT PREPARED 'xact_009_5'
;
ALTER TABLE public.pagg_tab_m_p1 OWNER TO zyc
;
LOCK TABLE public.array_op_test IN ACCESS SHARE MODE
;
PREPARE dumpOpr(pg_catalog.oid) AS
SELECT oprkind, oprcode::pg_catalog.regprocedure, oprleft::pg_catalog.regtype, oprright::pg_catalog.regtype, oprcom, oprnegate, oprrest::pg_catalog.regprocedure, oprjoin::pg_catalog.regprocedure, oprcanmerge, oprcanhash FROM pg_catalog.pg_operator WHERE oid = $1
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpOpr('1865')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.transition_table_status OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpFunc('16695')
;
----------
[UNSAFE] EXECUTE dumpAgg('20455')
;
----------
[TRANSACTION] rollback to f
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS regression_vacuumdb_test_stat ON a, b FROM regression_vacuumdb_test
;
----------
[TRANSACTION] COMMIT PREPARED 'xact_009_5'
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.pagg_tab_m_p1 OWNER TO zyc
;
----------
[SESSION_LOCK] LOCK TABLE public.array_op_test IN ACCESS SHARE MODE
;
----------
[UNSAFE] PREPARE dumpOpr(pg_catalog.oid) AS
SELECT oprkind, oprcode::pg_catalog.regprocedure, oprleft::pg_catalog.regtype, oprright::pg_catalog.regtype, oprcom, oprnegate, oprrest::pg_catalog.regprocedure, oprjoin::pg_catalog.regprocedure, oprcanmerge, oprcanhash FROM pg_catalog.pg_operator WHERE oid = $1
;
