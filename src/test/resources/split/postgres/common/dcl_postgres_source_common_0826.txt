EXECUTE dumpCompositeType('35874')
;
EXECUTE dumpOpr('3248')
;
COPY public.tab_include (a, b) TO STDOUT
;
--
-- Test RESET.  We use datestyle because the reset value is forced by
-- pg_regress, so it doesn't depend on the installation's configuration.
--
SET datestyle = iso, ymd
;
EXECUTE dumpFunc('28883')
;
EXECUTE dumpTableAttach('18156')
;
DROP OWNED BY regress_test_role2
;
EXECUTE dumpTableAttach('41450')
;
ALTER TABLE public.sequence_test2 OWNER TO zyc
;
LOCK TABLE "public"."xacttest" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpCompositeType('35874')
;
----------
[UNSAFE] EXECUTE dumpOpr('3248')
;
----------
[DATA_EXPORT] COPY public.tab_include (a, b) TO STDOUT
;
----------
[SESSION_SETTING_WRITE] --
-- Test RESET.  We use datestyle because the reset value is forced by
-- pg_regress, so it doesn't depend on the installation's configuration.
--
SET datestyle = iso, ymd
;
----------
[UNSAFE] EXECUTE dumpFunc('28883')
;
----------
[UNSAFE] EXECUTE dumpTableAttach('18156')
;
----------
[REVOKE] DROP OWNED BY regress_test_role2
;
----------
[UNSAFE] EXECUTE dumpTableAttach('41450')
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.sequence_test2 OWNER TO zyc
;
----------
[SESSION_LOCK] LOCK TABLE "public"."xacttest" IN ACCESS SHARE MODE
;
