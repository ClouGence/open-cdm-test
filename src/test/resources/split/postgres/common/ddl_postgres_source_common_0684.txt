/* src/test/modules/test_extensions/test_ext8--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- create some random data type
create domain posint as int check (value > 0)
;
CREATE TABLE gtest12s (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (gf1(b)) STORED)
;
DROP PUBLICATION testpub_table_ins, testpub_fortable, testpub_fortable_insert, testpub_col_list, testpub_tbl9
;
CREATE FUNCTION make_ad(int,int) returns arrdomain as
  'declare x arrdomain;
   begin
     x := array[$1,$2];
     return x;
   end' language plpgsql volatile
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_shdescription
;
ALTER TABLE pg_amop ADD UNIQUE USING INDEX pg_amop_opr_fam_index
;
-- materialized view
CREATE MATERIALIZED VIEW mv AS SELECT * FROM target
;
DROP SCHEMA IF EXISTS split_agg_probe_next CASCADE
;
CREATE TABLE test_bool (
	i boolean
)
;
drop table inh_perm_parent cascade
;
DROP TABLE IF EXISTS split_table_idx.method_table
;
create or replace function retc(int) returns two_int8s language plpgsql as
$$ declare r record; begin r := row($1,1); return r; end $$
;
CREATE TEMP TABLE c (id int PRIMARY KEY)
;
CREATE TABLE attmp_array (id int)
;
CREATE POLICY foo ON pg_description FOR SELECT USING (description NOT LIKE 'secret%')
;
ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (isbn13, ean13),
	OPERATOR 1  = (isbn13, isbn)
;
create function tg_backlink_a() returns trigger as '
declare
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
        if new.backlink != '''' then
	    dummy := tg_backlink_set(new.backlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = ''UPDATE'' then
        if new.backlink != old.backlink then
	    if old.backlink != '''' then
	        dummy := tg_backlink_unset(old.backlink, old.slotname);
	    end if;
	    if new.backlink != '''' then
	        dummy := tg_backlink_set(new.backlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.backlink != '''' then
	        dummy := tg_slotlink_set(new.backlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = ''DELETE'' then
        if old.backlink != '''' then
	    dummy := tg_backlink_unset(old.backlink, old.slotname);
	end if;
	return old;
    end if;
end;
' language plpgsql
;
alter table skip_wal_skip_rewrite_index alter c type varchar(20)
;
CREATE PROCEDURE test_proc3(x int)
LANGUAGE plpgsql
AS $$
BEGIN
    INSERT INTO test1 VALUES (x);
END;
$$
;
CREATE UNIQUE INDEX ataddindexi0 ON public.ataddindex USING btree (f1)
;
create materialized view mvw as select a from generate_series(1, 3) a
;
alter table utrtest attach partition remp for values in (3)
;
--
-- range UQ: test with existing rows
--

CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at daterange
)
;
ALTER INDEX pg_toast.pg_toast_30476_index RENAME TO reind_con_toast_idx
;
DROP FUNCTION leak2(integer, integer) CASCADE
;
create table j3 (id1 int, id2 int, primary key(id1,id2))
;
CREATE OPERATOR > (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_gt,
	COMMUTATOR = '<',
	NEGATOR = '<=',
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel
)
;
ALTER TABLE ONLY "public"."alpha_pos" ATTACH PARTITION "public"."alpha_pos_p2" FOR VALUES IN ('0004', '0006')
;
CREATE TABLE clstr_3 (a INT PRIMARY KEY)
;
CREATE TABLE notnull_tbl1_copy (LIKE notnull_tbl1)
;
CREATE TABLE parted_col_comment (a int, b text) PARTITION BY LIST (a) WITH (fillfactor=100)
;
DROP OPERATOR @#@ (int8, int8)
;
--
-- Test cases to catch situations where rule rewriter fails to propagate
-- hasSubLinks flag correctly.  Per example from Kyle Bateman.
--

create temp table parts (
    partnum     text,
    cost        float8
)
;
CREATE TABLE "public"."notnull_tbl4_cld3" (
    "a" integer CONSTRAINT "a_nn" NOT NULL
)
;
CREATE TABLESPACE split_ts_space LOCATION '/tmp/split_ts_space' WITH (seq_page_cost = 1.5, random_page_cost = 2.5)
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26364_2 (tid pg_catalog.tid)
;
CREATE FUNCTION batch_start()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
CREATE FUNCTION gbt_text_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE TABLE nametbl (c1 int, c2 name, c3 float)
;
CREATE TABLE fail_part PARTITION OF temp_parted FOR VALUES IN ('a')
;
DROP POLICY p2 ON event_trigger_test
;
-- ===================================================================
-- test IMPORT FOREIGN SCHEMA
-- ===================================================================

CREATE SCHEMA import_source
;
CREATE TABLE copy1 (a int, b float)
;
CREATE OPERATOR CLASS alt_opc2 FOR TYPE uuid USING hash AS STORAGE uuid
;
ALTER TABLE pp_nn ATTACH PARTITION pp_nn_1 FOR VALUES IN (NULL,5)
;
CREATE AGGREGATE myaggn08a(BASETYPE = anyelement, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE FUNCTION "public"."text_w_default_in"("cstring") RETURNS "public"."text_w_default"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$textin$$
;
-- cast
DROP CAST (text AS text)
;
CREATE INDEX ON vaccluster(wrap_do_analyze(i))
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_attrdef
;
------------------------------------------------------------------------------------------
[CREATE_TYPE] /* src/test/modules/test_extensions/test_ext8--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- create some random data type
create domain posint as int check (value > 0)
;
----------
[CREATE_TABLE] CREATE TABLE gtest12s (a int PRIMARY KEY, b int, c int GENERATED ALWAYS AS (gf1(b)) STORED)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_table_ins, testpub_fortable, testpub_fortable_insert, testpub_col_list, testpub_tbl9
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_ad(int,int) returns arrdomain as
  'declare x arrdomain;
   begin
     x := array[$1,$2];
     return x;
   end' language plpgsql volatile
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_shdescription
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_amop ADD UNIQUE USING INDEX pg_amop_opr_fam_index
;
----------
[CREATE_VIEW(SELECT)] -- materialized view
CREATE MATERIALIZED VIEW mv AS SELECT * FROM target
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_agg_probe_next CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE test_bool (
	i boolean
)
;
----------
[DROP_TABLE] drop table inh_perm_parent cascade
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS split_table_idx.method_table
;
----------
[CREATE_PROG_OBJ] create or replace function retc(int) returns two_int8s language plpgsql as
$$ declare r record; begin r := row($1,1); return r; end $$
;
----------
[CREATE_TABLE] CREATE TEMP TABLE c (id int PRIMARY KEY)
;
----------
[CREATE_TABLE] CREATE TABLE attmp_array (id int)
;
----------
[CREATE_POLICY] CREATE POLICY foo ON pg_description FOR SELECT USING (description NOT LIKE 'secret%')
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (isbn13, ean13),
	OPERATOR 1  = (isbn13, isbn)
;
----------
[CREATE_PROG_OBJ] create function tg_backlink_a() returns trigger as '
declare
    dummy	integer;
begin
    if tg_op = ''INSERT'' then
        if new.backlink != '''' then
	    dummy := tg_backlink_set(new.backlink, new.slotname);
	end if;
	return new;
    end if;
    if tg_op = ''UPDATE'' then
        if new.backlink != old.backlink then
	    if old.backlink != '''' then
	        dummy := tg_backlink_unset(old.backlink, old.slotname);
	    end if;
	    if new.backlink != '''' then
	        dummy := tg_backlink_set(new.backlink, new.slotname);
	    end if;
	else
	    if new.slotname != old.slotname and new.backlink != '''' then
	        dummy := tg_slotlink_set(new.backlink, new.slotname);
	    end if;
	end if;
	return new;
    end if;
    if tg_op = ''DELETE'' then
        if old.backlink != '''' then
	    dummy := tg_backlink_unset(old.backlink, old.slotname);
	end if;
	return old;
    end if;
end;
' language plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table skip_wal_skip_rewrite_index alter c type varchar(20)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc3(x int)
LANGUAGE plpgsql
AS $$
BEGIN
    INSERT INTO test1 VALUES (x);
END;
$$
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ataddindexi0 ON public.ataddindex USING btree (f1)
;
----------
[CREATE_VIEW(SELECT)] create materialized view mvw as select a from generate_series(1, 3) a
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table utrtest attach partition remp for values in (3)
;
----------
[CREATE_TABLE] --
-- range UQ: test with existing rows
--

CREATE TABLE temporal_rng3 (
  id int4range,
  valid_at daterange
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30476_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] DROP FUNCTION leak2(integer, integer) CASCADE
;
----------
[CREATE_TABLE] create table j3 (id1 int, id2 int, primary key(id1,id2))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_gt,
	COMMUTATOR = '<',
	NEGATOR = '<=',
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."alpha_pos" ATTACH PARTITION "public"."alpha_pos_p2" FOR VALUES IN ('0004', '0006')
;
----------
[CREATE_TABLE] CREATE TABLE clstr_3 (a INT PRIMARY KEY)
;
----------
[CREATE_TABLE] CREATE TABLE notnull_tbl1_copy (LIKE notnull_tbl1)
;
----------
[CREATE_TABLE] CREATE TABLE parted_col_comment (a int, b text) PARTITION BY LIST (a) WITH (fillfactor=100)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR @#@ (int8, int8)
;
----------
[CREATE_TABLE] --
-- Test cases to catch situations where rule rewriter fails to propagate
-- hasSubLinks flag correctly.  Per example from Kyle Bateman.
--

create temp table parts (
    partnum     text,
    cost        float8
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl4_cld3" (
    "a" integer CONSTRAINT "a_nn" NOT NULL
)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE split_ts_space LOCATION '/tmp/split_ts_space' WITH (seq_page_cost = 1.5, random_page_cost = 2.5)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26364_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION batch_start()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_text_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE nametbl (c1 int, c2 name, c3 float)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF temp_parted FOR VALUES IN ('a')
;
----------
[DROP_POLICY] DROP POLICY p2 ON event_trigger_test
;
----------
[CREATE_SCHEMA] -- ===================================================================
-- test IMPORT FOREIGN SCHEMA
-- ===================================================================

CREATE SCHEMA import_source
;
----------
[CREATE_TABLE] CREATE TABLE copy1 (a int, b float)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS alt_opc2 FOR TYPE uuid USING hash AS STORAGE uuid
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pp_nn ATTACH PARTITION pp_nn_1 FOR VALUES IN (NULL,5)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn08a(BASETYPE = anyelement, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."text_w_default_in"("cstring") RETURNS "public"."text_w_default"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$textin$$
;
----------
[DROP_PROG_OBJ] -- cast
DROP CAST (text AS text)
;
----------
[ADD_INDEX] CREATE INDEX ON vaccluster(wrap_do_analyze(i))
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_attrdef
;
