-- create a schema we can use
CREATE SCHEMA testschema
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
-- fail - view
ALTER PUBLICATION testpub_default ADD TABLE testpub_view
;
-- Add range column and the PK at the same time
CREATE TABLE temporal3 (
  id int4range
)
;
ALTER INDEX tbl_idx ALTER COLUMN 2 SET STATISTICS 1000
;
CREATE FUNCTION trunc_trig_before_stat_trig() RETURNS trigger
		  LANGUAGE plpgsql as $$
		  BEGIN
			INSERT INTO trunc_trig VALUES (DEFAULT, 'triggered stat before');
			RETURN NULL;
		  END; $$
;
ALTER FUNCTION test_ext.b() DEPENDS ON EXTENSION test_ext5
;
create aggregate aggfstr(integer,integer,text) (
   sfunc = aggf_trans, stype = aggtype[],
   initcond = '{}'
)
;
CREATE FUNCTION ltree_out(ltree)
RETURNS cstring
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TEMP TABLE node (
    nid SERIAL,
    vid integer NOT NULL default '0',
    type varchar(32) NOT NULL default '',
    title varchar(128) NOT NULL default '',
    uid integer NOT NULL default '0',
    status integer NOT NULL default '1',
    created integer NOT NULL default '0',
    -- snip
    PRIMARY KEY (nid, vid)
)
;
CREATE TABLE not_owned_by_me (LIKE list_parted)
;
ALTER TABLE notnull_tbl3 ADD b int, ADD CONSTRAINT pk PRIMARY KEY (a, b)
;
--
-- PGP key ID
--
CREATE FUNCTION pgp_key_id(bytea)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_key_id_w'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE PROCEDURE ptest2()
LANGUAGE SQL
AS $$
SELECT 5;
$$
;
-- Dave is only allowed to see cid < 55
CREATE POLICY pp1r ON part_document AS RESTRICTIVE TO regress_rls_dave
    USING (cid < 55)
;
ALTER TABLE lt1 ADD PRIMARY KEY (a)
;
create table listp (a int) partition by list(a)
;
CREATE FUNCTION "public"."extractq2"("t" "public"."int8_tbl") RETURNS bigint
    LANGUAGE "sql" IMMUTABLE
    AS $$
  select t.q2
$$
;
ALTER TABLE pg_temp_12.pg_temp_26722_2 ADD COLUMN newdata pg_temp_12.pg_temp_26722
;
CREATE TYPE "public"."int8alias2"
;
ALTER TABLE range_parted ATTACH PARTITION range_part2 FOR VALUES FROM (1, 10) TO (1, 20)
;
-- Test comments
COMMENT ON VIEW noview IS 'no view'
;
ALTER TABLE inh_nn_parent ALTER a SET NOT NULL
;
alter table dropColumnChild drop column c
;
DROP TABLE test_replica_identity
;
CREATE VIEW v5 AS SELECT * FROM ft5
;
CREATE TRIGGER trig_row_after_insert
AFTER INSERT ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
CREATE TABLE truncpart_2 PARTITION OF truncpart FOR VALUES FROM (100) TO (200)
  PARTITION BY RANGE (a)
;
drop table mytable
;
SECURITY LABEL ON ROLE regress_dummy_seclabel_user4 IS 'unclassified'
;
VACUUM (ANALYZE) pg_catalog.pg_type
;
-- fail due to "does not exist"
alter table trigpart detach partition trigpart4
;
create table btree_test(a int)
;
CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
CREATE TABLE t1 (a integer, b text)
;
ALTER TABLE f_star RENAME COLUMN f TO ff
;
ALTER TABLE rec2 ENABLE ROW LEVEL SECURITY
;
alter table inh_parent add constraint inh_parent_uq unique (a)
;
CREATE TABLE gtest_parent (f1 date NOT NULL, f2 text, f3 bigint) PARTITION BY RANGE (f1)
;
VACUUM (PARALLEL 0) pg_catalog.pg_attribute
;
CREATE FUNCTION gtrgm_same(gtrgm, gtrgm, internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE pg_namespace ADD PRIMARY KEY USING INDEX pg_namespace_oid_index
;
DROP TABLE testpub_tbl5, testpub_tbl6, testpub_tbl7, testpub_tbl8, testpub_tbl8_1
;
ALTER TABLE partitioned ADD CONSTRAINT chk_a CHECK (a > 0) NO INHERIT
;
-- should log the full old row now
ALTER TABLE table_without_key REPLICA IDENTITY FULL
;
CREATE TABLE "public"."prt2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("b")
;
create table bool_rp_true_2k partition of bool_rp for values from (true,1000) to (true,2000)
;
CREATE INDEX wowidx ON test_tsvector USING gin (a)
;
drop index plain
;
-- Enabling OLD TABLE capture for both DELETE as well as UPDATE stmt triggers
-- should not cause DELETEd rows to be captured twice. Similar thing for
-- INSERT triggers and inserted rows.
CREATE TRIGGER trans_deletetrig
  AFTER DELETE ON range_parted REFERENCING OLD TABLE AS old_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trans_updatetrigfunc()
;
------------------------------------------------------------------------------------------
[CREATE_SCHEMA] -- create a schema we can use
CREATE SCHEMA testschema
;
----------
[CREATE_TABLE|SELECT] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
----------
[ALTER_PUB_SUB] -- fail - view
ALTER PUBLICATION testpub_default ADD TABLE testpub_view
;
----------
[CREATE_TABLE] -- Add range column and the PK at the same time
CREATE TABLE temporal3 (
  id int4range
)
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX tbl_idx ALTER COLUMN 2 SET STATISTICS 1000
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION trunc_trig_before_stat_trig() RETURNS trigger
		  LANGUAGE plpgsql as $$
		  BEGIN
			INSERT INTO trunc_trig VALUES (DEFAULT, 'triggered stat before');
			RETURN NULL;
		  END; $$
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION test_ext.b() DEPENDS ON EXTENSION test_ext5
;
----------
[CREATE_PROG_OBJ] create aggregate aggfstr(integer,integer,text) (
   sfunc = aggf_trans, stype = aggtype[],
   initcond = '{}'
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_out(ltree)
RETURNS cstring
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE node (
    nid SERIAL,
    vid integer NOT NULL default '0',
    type varchar(32) NOT NULL default '',
    title varchar(128) NOT NULL default '',
    uid integer NOT NULL default '0',
    status integer NOT NULL default '1',
    created integer NOT NULL default '0',
    -- snip
    PRIMARY KEY (nid, vid)
)
;
----------
[CREATE_TABLE] CREATE TABLE not_owned_by_me (LIKE list_parted)
;
----------
[ALTER_TABLE|ADD_COLUMN|ADD_CONSTRAINT] ALTER TABLE notnull_tbl3 ADD b int, ADD CONSTRAINT pk PRIMARY KEY (a, b)
;
----------
[CREATE_PROG_OBJ] --
-- PGP key ID
--
CREATE FUNCTION pgp_key_id(bytea)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_key_id_w'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest2()
LANGUAGE SQL
AS $$
SELECT 5;
$$
;
----------
[CREATE_POLICY] -- Dave is only allowed to see cid < 55
CREATE POLICY pp1r ON part_document AS RESTRICTIVE TO regress_rls_dave
    USING (cid < 55)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE lt1 ADD PRIMARY KEY (a)
;
----------
[CREATE_TABLE] create table listp (a int) partition by list(a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."extractq2"("t" "public"."int8_tbl") RETURNS bigint
    LANGUAGE "sql" IMMUTABLE
    AS $$
  select t.q2
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26722_2 ADD COLUMN newdata pg_temp_12.pg_temp_26722
;
----------
[CREATE_TYPE] CREATE TYPE "public"."int8alias2"
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE range_parted ATTACH PARTITION range_part2 FOR VALUES FROM (1, 10) TO (1, 20)
;
----------
[COMMENT_VIEW] -- Test comments
COMMENT ON VIEW noview IS 'no view'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE inh_nn_parent ALTER a SET NOT NULL
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table dropColumnChild drop column c
;
----------
[DROP_TABLE] DROP TABLE test_replica_identity
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v5 AS SELECT * FROM ft5
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_after_insert
AFTER INSERT ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[CREATE_TABLE] CREATE TABLE truncpart_2 PARTITION OF truncpart FOR VALUES FROM (100) TO (200)
  PARTITION BY RANGE (a)
;
----------
[DROP_TABLE] drop table mytable
;
----------
[UNKNOWN] SECURITY LABEL ON ROLE regress_dummy_seclabel_user4 IS 'unclassified'
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_type
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- fail due to "does not exist"
alter table trigpart detach partition trigpart4
;
----------
[CREATE_TABLE] create table btree_test(a int)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
----------
[CREATE_TABLE] CREATE TABLE t1 (a integer, b text)
;
----------
[RENAME_COLUMN] ALTER TABLE f_star RENAME COLUMN f TO ff
;
----------
[ALTER_TABLE] ALTER TABLE rec2 ENABLE ROW LEVEL SECURITY
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table inh_parent add constraint inh_parent_uq unique (a)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_parent (f1 date NOT NULL, f2 text, f3 bigint) PARTITION BY RANGE (f1)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_attribute
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_same(gtrgm, gtrgm, internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_namespace ADD PRIMARY KEY USING INDEX pg_namespace_oid_index
;
----------
[DROP_TABLE] DROP TABLE testpub_tbl5, testpub_tbl6, testpub_tbl7, testpub_tbl8, testpub_tbl8_1
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE partitioned ADD CONSTRAINT chk_a CHECK (a > 0) NO INHERIT
;
----------
[ALTER_TABLE] -- should log the full old row now
ALTER TABLE table_without_key REPLICA IDENTITY FULL
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("b")
;
----------
[CREATE_TABLE] create table bool_rp_true_2k partition of bool_rp for values from (true,1000) to (true,2000)
;
----------
[ADD_INDEX] CREATE INDEX wowidx ON test_tsvector USING gin (a)
;
----------
[DROP_INDEX] drop index plain
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Enabling OLD TABLE capture for both DELETE as well as UPDATE stmt triggers
-- should not cause DELETEd rows to be captured twice. Similar thing for
-- INSERT triggers and inserted rows.
CREATE TRIGGER trans_deletetrig
  AFTER DELETE ON range_parted REFERENCING OLD TABLE AS old_table
  FOR EACH STATEMENT EXECUTE PROCEDURE trans_updatetrigfunc()
;
