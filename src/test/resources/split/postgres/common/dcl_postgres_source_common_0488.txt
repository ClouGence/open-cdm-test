LOCK TABLE "public"."nv_parent" IN ACCESS SHARE MODE
;
SET search_path = 'pgss_schema_1'
;
EXECUTE dumpAgg('34583')
;
EXECUTE dumpFunc('29027')
;
-- error
ROLLBACK AND CHAIN
;
-- ===================================================================
-- test invalid server and foreign table options
-- ===================================================================
-- Invalid fdw_startup_cost option
CREATE SERVER inv_scst FOREIGN DATA WRAPPER postgres_fdw
	OPTIONS(fdw_startup_cost '100$%$#$#')
;
GRANT SELECT ON my_credit_card_secure TO public
;
ALTER TABLE public.part2_self_fk OWNER TO zyc
;
-- This test case is for closing the connection in pgfdw_xact_callback
BEGIN
;
-- up to 5 executions, custom plan is used
set plan_cache_mode to auto
;
------------------------------------------------------------------------------------------
[SESSION_LOCK] LOCK TABLE "public"."nv_parent" IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] SET search_path = 'pgss_schema_1'
;
----------
[UNSAFE] EXECUTE dumpAgg('34583')
;
----------
[UNSAFE] EXECUTE dumpFunc('29027')
;
----------
[TRANSACTION] -- error
ROLLBACK AND CHAIN
;
----------
[SYSTEM_SETTING_WRITE] -- ===================================================================
-- test invalid server and foreign table options
-- ===================================================================
-- Invalid fdw_startup_cost option
CREATE SERVER inv_scst FOREIGN DATA WRAPPER postgres_fdw
	OPTIONS(fdw_startup_cost '100$%$#$#')
;
----------
[GRANT] GRANT SELECT ON my_credit_card_secure TO public
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.part2_self_fk OWNER TO zyc
;
----------
[TRANSACTION] -- This test case is for closing the connection in pgfdw_xact_callback
BEGIN
;
----------
[SESSION_SETTING_WRITE] -- up to 5 executions, custom plan is used
set plan_cache_mode to auto
;
