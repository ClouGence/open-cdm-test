CREATE FUNCTION encrypt(bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_encrypt'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- Cleanup
DROP FUNCTION func_int_custom
;
create function build_ordered_int8s_4(i int8, j int8) returns ordered_int8s as $$
declare r ordered_int8s; begin r := row(i,j); return r; end
$$ language plpgsql
;
CREATE TEMP TABLE pg_temp_11.pg_temp_41126_2 (tid pg_catalog.tid)
;
DROP TABLE tab_with_flag
;
--
-- Test @extschema:extname@ syntax and no_relocate option
--
CREATE EXTENSION test_ext_req_schema1 SCHEMA has$dollar
;
alter table nonesuchrel rename column nonesuchatt to newnonesuchatt
;
alter extension postgres_fdw add function my_op_cmp(a int, b int)
;
alter table p1 attach partition p11 for values from (2) to (5)
;
CREATE OR REPLACE FUNCTION
  jsonb_path_query_first(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                         silent boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_query_first'
;
-- query to exercise expand_tuple function
CREATE TABLE t1 AS
SELECT 1::int AS a , 2::int AS b
FROM generate_series(1,20) q
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdescription
;
DROP TABLE testpub_insert_onconfl_no_ri
;
--
-- CREATE Permission checks
--
CREATE DATABASE sepgsql_test_regression
;
DO LANGUAGE plpgsql 'BEGIN COPY copy1 FROM ''/src/src/pl/plpgsql/src/data/copy1.data''; END'
;
CREATE TABLE num_data (id int4, val numeric(210,10))
;
CREATE UNIQUE INDEX tbl_idx ON tbl using btree(c1, c2) INCLUDE(c3,c4)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_publication_namespace
;
CREATE TRIGGER gtest12_02 BEFORE UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func4()
;
drop table temp_parted_oncommit
;
CREATE TABLE fail_default_part PARTITION OF range_parted2 DEFAULT
;
ANALYZE public.covidxpart1
;
CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((gtest_parent is not null))
;
drop function f1(x anycompatible, y anycompatible)
;
ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "url_path" WITH "simple"
;
VACUUM ANALYZE vactst, vacparted (a)
;
CREATE OR REPLACE FUNCTION pgstattuple(IN reloid regclass,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuplebyid_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP TABLE connectby_int
;
create function read_ordered_int8s(p ordered_int8s) returns int8 as $$
begin return p.q1 + p.q2; end
$$ language plpgsql
;
CREATE TABLE toast_3b_utf8 (c text)
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'testconn' PUBLICATION testpub WITH (create_slot)
;
ANALYZE public.rtest_emp
;
CREATE TRIGGER trigtest_after_stmt_tt AFTER INSERT OR UPDATE OR DELETE 
ON foreign_schema.foreign_table_1
REFERENCING NEW TABLE AS new_table
FOR EACH STATEMENT
EXECUTE PROCEDURE dummy_trigger()
;
ALTER SUBSCRIPTION regress_testsub2 RENAME TO regress_testsub
;
CREATE TABLE current_check (currentid int, payload text)
;
CREATE TEMP TABLE pg_temp_13.pg_temp_25000_2 (tid pg_catalog.tid)
;
-- Fails when running in a transaction.
CREATE INDEX CONCURRENTLY concur_temp_ind ON concur_temp(f1)
;
CREATE FOREIGN TABLE ftprt1_p1 PARTITION OF fprt1 FOR VALUES FROM (0) TO (250)
	SERVER loopback OPTIONS (table_name 'fprt1_p1', use_remote_estimate 'true')
;
CALL ptest9(NULL)
;
VACUUM (SKIP_LOCKED, FULL) parted
;
COMMENT ON VIEW datatype_view IS 'This is a view'
;
-- this transform would be quite unsafe to leave lying around,
-- except that the SQL language pays no attention to transforms:
CREATE TRANSFORM FOR int LANGUAGE SQL (
	FROM SQL WITH FUNCTION prsd_lextype(internal),
	TO SQL WITH FUNCTION int4recv(internal))
;
CREATE COLLATION coll_dup_chk (LC_CTYPE = "POSIX", LOCALE = '')
;
CREATE OR REPLACE FUNCTION
  jsonb_path_exists(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                    silent boolean DEFAULT false)
RETURNS boolean
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_exists'
;
create function simplesql() returns setof int language sql
as $$select 22 + 22$$
;
-- should fail
create function outparam_fail(i anyelement, out r anyrange, out t text)
  as $$ select '[1,10]', 'foo' $$ language sql
;
create function cleast_accum(anycompatible, variadic anycompatiblearray)
returns anycompatible language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
CREATE TABLE "public"."part32_self_fk" (
    "id" bigint CONSTRAINT "part3_self_fk_id_not_null" NOT NULL,
    "id_abc" bigint
)
;
--
-- MULE_INTERNAL
--
CREATE TABLE mic_inputs (inbytes bytea, description text)
;
ALTER FUNCTION gbt_float4_penalty(internal, internal, internal) PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION encrypt(bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_encrypt'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_PROG_OBJ] -- Cleanup
DROP FUNCTION func_int_custom
;
----------
[CREATE_PROG_OBJ] create function build_ordered_int8s_4(i int8, j int8) returns ordered_int8s as $$
declare r ordered_int8s; begin r := row(i,j); return r; end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_41126_2 (tid pg_catalog.tid)
;
----------
[DROP_TABLE] DROP TABLE tab_with_flag
;
----------
[CREATE_LIBRARY] --
-- Test @extschema:extname@ syntax and no_relocate option
--
CREATE EXTENSION test_ext_req_schema1 SCHEMA has$dollar
;
----------
[RENAME_COLUMN] alter table nonesuchrel rename column nonesuchatt to newnonesuchatt
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw add function my_op_cmp(a int, b int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table p1 attach partition p11 for values from (2) to (5)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_query_first(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                         silent boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_query_first'
;
----------
[CREATE_TABLE|SELECT] -- query to exercise expand_tuple function
CREATE TABLE t1 AS
SELECT 1::int AS a , 2::int AS b
FROM generate_series(1,20) q
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdescription
;
----------
[DROP_TABLE] DROP TABLE testpub_insert_onconfl_no_ri
;
----------
[CREATE_CATALOG] --
-- CREATE Permission checks
--
CREATE DATABASE sepgsql_test_regression
;
----------
[BLOCK] DO LANGUAGE plpgsql 'BEGIN COPY copy1 FROM ''/src/src/pl/plpgsql/src/data/copy1.data''; END'
;
----------
[CREATE_TABLE] CREATE TABLE num_data (id int4, val numeric(210,10))
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX tbl_idx ON tbl using btree(c1, c2) INCLUDE(c3,c4)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_publication_namespace
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest12_02 BEFORE UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func4()
;
----------
[DROP_TABLE] drop table temp_parted_oncommit
;
----------
[CREATE_TABLE] CREATE TABLE fail_default_part PARTITION OF range_parted2 DEFAULT
;
----------
[ADMIN_TABLE] ANALYZE public.covidxpart1
;
----------
[CREATE_TABLE] CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((gtest_parent is not null))
;
----------
[DROP_PROG_OBJ] drop function f1(x anycompatible, y anycompatible)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."synonym_tst"
    ADD MAPPING FOR "url_path" WITH "simple"
;
----------
[ADMIN_TABLE] VACUUM ANALYZE vactst, vacparted (a)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pgstattuple(IN reloid regclass,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuplebyid_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE connectby_int
;
----------
[CREATE_PROG_OBJ] create function read_ordered_int8s(p ordered_int8s) returns int8 as $$
begin return p.q1 + p.q2; end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE toast_3b_utf8 (c text)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'testconn' PUBLICATION testpub WITH (create_slot)
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_emp
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trigtest_after_stmt_tt AFTER INSERT OR UPDATE OR DELETE 
ON foreign_schema.foreign_table_1
REFERENCING NEW TABLE AS new_table
FOR EACH STATEMENT
EXECUTE PROCEDURE dummy_trigger()
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub2 RENAME TO regress_testsub
;
----------
[CREATE_TABLE] CREATE TABLE current_check (currentid int, payload text)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_25000_2 (tid pg_catalog.tid)
;
----------
[ADD_INDEX] -- Fails when running in a transaction.
CREATE INDEX CONCURRENTLY concur_temp_ind ON concur_temp(f1)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ftprt1_p1 PARTITION OF fprt1 FOR VALUES FROM (0) TO (250)
	SERVER loopback OPTIONS (table_name 'fprt1_p1', use_remote_estimate 'true')
;
----------
[CALL_PROG_OBJ] CALL ptest9(NULL)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED, FULL) parted
;
----------
[COMMENT_VIEW] COMMENT ON VIEW datatype_view IS 'This is a view'
;
----------
[CREATE_TRANSFORM] -- this transform would be quite unsafe to leave lying around,
-- except that the SQL language pays no attention to transforms:
CREATE TRANSFORM FOR int LANGUAGE SQL (
	FROM SQL WITH FUNCTION prsd_lextype(internal),
	TO SQL WITH FUNCTION int4recv(internal))
;
----------
[CREATE_POLICY] CREATE COLLATION coll_dup_chk (LC_CTYPE = "POSIX", LOCALE = '')
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_exists(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                    silent boolean DEFAULT false)
RETURNS boolean
LANGUAGE INTERNAL
STRICT IMMUTABLE PARALLEL SAFE
AS 'jsonb_path_exists'
;
----------
[CREATE_PROG_OBJ] create function simplesql() returns setof int language sql
as $$select 22 + 22$$
;
----------
[CREATE_PROG_OBJ] -- should fail
create function outparam_fail(i anyelement, out r anyrange, out t text)
  as $$ select '[1,10]', 'foo' $$ language sql
;
----------
[CREATE_PROG_OBJ] create function cleast_accum(anycompatible, variadic anycompatiblearray)
returns anycompatible language sql as
  'select least($1, min($2[i])) from generate_subscripts($2,1) g(i)'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part32_self_fk" (
    "id" bigint CONSTRAINT "part3_self_fk_id_not_null" NOT NULL,
    "id_abc" bigint
)
;
----------
[CREATE_TABLE] --
-- MULE_INTERNAL
--
CREATE TABLE mic_inputs (inbytes bytea, description text)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float4_penalty(internal, internal, internal) PARALLEL SAFE
;
