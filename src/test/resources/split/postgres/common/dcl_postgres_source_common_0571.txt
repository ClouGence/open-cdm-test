COPY atest5 (one,two) TO stdout
;
COPY public."tab'le" (a) TO STDOUT
;
EXECUTE dumpFunc('30158')
;
EXECUTE dumpFunc('16441')
;
--
-- Validate WAL generation metrics
--

SET pg_stat_statements.track_utility = FALSE
;
prepare q1 (int,int,int,int) as select * from listp where b in($1,$2) and $3 <> b and $4 <> b
;
ALTER SYSTEM RESET work_mem
;
ALTER SERVER testserver1 OPTIONS (ADD extensions 'foo, bar')
;
-- Ensure MergeAppend behaves correctly when no subplans match
explain (analyze, costs off, summary off, timing off, buffers off) execute mt_q1(35)
;
EXECUTE getDomainConstraints('34726')
;
------------------------------------------------------------------------------------------
[DATA_EXPORT] COPY atest5 (one,two) TO stdout
;
----------
[DATA_EXPORT] COPY public."tab'le" (a) TO STDOUT
;
----------
[UNSAFE] EXECUTE dumpFunc('30158')
;
----------
[UNSAFE] EXECUTE dumpFunc('16441')
;
----------
[SESSION_SETTING_WRITE] --
-- Validate WAL generation metrics
--

SET pg_stat_statements.track_utility = FALSE
;
----------
[UNSAFE] prepare q1 (int,int,int,int) as select * from listp where b in($1,$2) and $3 <> b and $4 <> b
;
----------
[SYSTEM_SETTING_WRITE] ALTER SYSTEM RESET work_mem
;
----------
[SYSTEM_SETTING_WRITE] ALTER SERVER testserver1 OPTIONS (ADD extensions 'foo, bar')
;
----------
[UNSAFE] -- Ensure MergeAppend behaves correctly when no subplans match
explain (analyze, costs off, summary off, timing off, buffers off) execute mt_q1(35)
;
----------
[UNSAFE] EXECUTE getDomainConstraints('34726')
;
