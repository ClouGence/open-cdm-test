create table empl ( idnum integer , name char ( 20 ) , accs smallint , string1 char ( 10 ) , string2 char ( 10 ) , string3 char ( 10 ) )
;
ALTER FUNCTION gbt_macad_picksplit(internal, internal) PARALLEL SAFE
;
ALTER OPERATOR CLASS split_operator_probe.int_hash_ops USING hash RENAME TO int_hash_ops_renamed
;
DROP COLLATION ext_cine_coll
;
ALTER TABLE pg_subscription_rel ADD PRIMARY KEY USING INDEX pg_subscription_rel_srrelid_srsubid_index
;
/*
 * SQL_SIZING_PROFILES table
 * removed in SQL:2011
 */

-- The data in this table are defined by various profiles of SQL.
-- Since we don't have any information about such profiles, we provide
-- an empty table.

CREATE TABLE sql_sizing_profiles (
    sizing_id       cardinal_number,
    sizing_name     character_data,
    profile_id      character_data,
    required_value  cardinal_number,
    comments        character_data
)
;
ALTER TABLE fktable ADD CONSTRAINT fk_1_2
FOREIGN KEY (x1) REFERENCES pktable(id2)
;
CREATE TEMP TABLE pg_temp_26.pg_temp_28381_2 (tid pg_catalog.tid)
;
CREATE SUBSCRIPTION test_tab2_sub CONNECTION 'port=22821 host=/tmp/Ajgausiysd dbname=postgres' PUBLICATION test_tab2_pub
;
ALTER TEXT SEARCH CONFIGURATION portuguese ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
CREATE EXTENSION worker_spi
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
CREATE TABLE tab_rowfilter_partitioned (a int primary key, b integer) PARTITION BY RANGE(a)
;
CREATE UNIQUE INDEX concur_exprs_index_pred_2
  ON concur_exprs_tab ((1 / c1))
  WHERE ('-H') >= (c2::TEXT) COLLATE "C"
;
alter table nonesuch rename to stud_emp
;
create schema mr_sch
;
-- infer anyarray from anymultirange
create function mr_outparam_succeed2(i anymultirange, out r anyarray, out t text)
  as $$ select ARRAY[upper($1)], 'foo'::text $$ language sql
;
ALTER TABLE pg_toast.pg_toast_28603 RENAME TO reind_con_toast
;
ALTER TABLE notnull_tbl2 ALTER a DROP NOT NULL
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
CREATE TABLE itest9 (a int GENERATED ALWAYS AS IDENTITY, b text, c bigint)
;
ALTER TABLE pg_toast.pg_toast_30395 RENAME TO reind_con_toast
;
create type jsb_i_gt_1_rec as (a jsb_i_gt_1)
;
CREATE FUNCTION explain_mask_costs(query text, do_analyze bool,
    hide_costs bool, hide_row_est bool, hide_width bool) RETURNS setof text
LANGUAGE plpgsql AS
$$
DECLARE
    ln text;
    analyze_str text;
BEGIN
    IF do_analyze = true THEN
        analyze_str := 'on';
    ELSE
        analyze_str := 'off';
    END IF;

    -- avoid jit related output by disabling it
    SET LOCAL jit = 0;

    FOR ln IN
        EXECUTE format('explain (analyze %s, costs on, summary off, timing off, buffers off) %s',
            analyze_str, query)
    LOOP
        IF hide_costs = true THEN
            ln := regexp_replace(ln, 'cost=\d+\.\d\d\.\.\d+\.\d\d', 'cost=N..N');
        END IF;

        IF hide_row_est = true THEN
            -- don't use 'g' so that we leave the actual rows intact
            ln := regexp_replace(ln, 'rows=\d+', 'rows=N');
        END IF;

        IF hide_width = true THEN
            ln := regexp_replace(ln, 'width=\d+', 'width=N');
        END IF;

        RETURN NEXT ln;
    END LOOP;
END;
$$
;
CREATE SUBSCRIPTION tsub CONNECTION 'port=30481 host=/tmp/UBekCAWDSf dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
CLUSTER clustertest USING clustertest_pkey
;
ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full
;
-- error

-- Read-only tests

CREATE TABLE writetest (a int)
;
CREATE FUNCTION equipment_named(hobby hobbies_r)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where equipment_r.hobby = equipment_named.hobby.name'
   LANGUAGE SQL
;
CREATE TABLE tab5 (a int NOT NULL, b int, c int) PARTITION BY LIST (a)
;
CREATE TABLE gin_idx_tbl (id bigserial PRIMARY KEY, data jsonb)
;
CREATE FOREIGN TABLE ft_part_1_2 (a INT NOT NULL) SERVER s0
;
--- test selecting bool through SPI

CREATE FUNCTION spi_test()  RETURNS void
LANGUAGE plperl
TRANSFORM FOR TYPE bool
AS $$
my $rv = spi_exec_query('SELECT true t, false f, NULL n')->{rows}->[0];

die("TRUE mistransformed to UNDEF in SPI") if (!defined ($rv->{t}));
die("FALSE mistransformed to UNDEF in SPI") if (!defined ($rv->{f}));
die("NULL mistransformed in SPI") if (defined ($rv->{n}));
die("TRUE mistransformed in SPI") if (!$rv->{t});
die("FALSE mistransformed in SPI") if ($rv->{f});
$$
;
-- Test to make sure short-circuiting and NULL handling is
-- correct. Use a table as source to prevent constant simplification
-- from interfering.
CREATE TABLE booltbl4(isfalse bool, istrue bool, isnul bool)
;
--
-- Previous tests all managed to not test any expressions requiring
-- planner preprocessing ...
--
create table insertconflict (a bigint, b bigint)
;
alter table user_table add column address varchar(255) primary key
;
CREATE TEXT SEARCH CONFIGURATION spanish
	(PARSER = default)
;
ALTER FUNCTION gbt_macad8_union(internal, internal) PARALLEL SAFE
;
CREATE FUNCTION "public"."make_table"() RETURNS "void"
    LANGUAGE "sql"
    AS $$
  CREATE TABLE created_table AS SELECT * FROM int8_tbl;
$$
;
-- range type doesn't fit

drop function f1(x anycompatiblerange, y anycompatible, z anycompatible)
;
create table insertconflict (f1 int primary key, f2 text)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_ts_template
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_statistic_ext_data
;
CREATE FUNCTION foo_trg() RETURNS trigger AS $$ BEGIN RETURN NEW; END $$ LANGUAGE plpgsql
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
-- error
CREATE SCHEMA newschema6 AUTHORIZATION PUBLIC
;
CREATE POLICY r2 ON rec2 USING (a = (SELECT x FROM rec1 WHERE y = b))
;
CREATE FUNCTION f1(int) RETURNS int LANGUAGE SQL AS 'SELECT f0($1)'
;
-- validator tests
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (foo 'bar')
;
create function raise_test2(int) returns int as $$
begin
    raise notice 'This message has too few parameters: %, %, %', $1, $1;
    return $1;
end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table empl ( idnum integer , name char ( 20 ) , accs smallint , string1 char ( 10 ) , string2 char ( 10 ) , string3 char ( 10 ) )
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad_picksplit(internal, internal) PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR CLASS split_operator_probe.int_hash_ops USING hash RENAME TO int_hash_ops_renamed
;
----------
[DROP_POLICY] DROP COLLATION ext_cine_coll
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_subscription_rel ADD PRIMARY KEY USING INDEX pg_subscription_rel_srrelid_srsubid_index
;
----------
[CREATE_TABLE] /*
 * SQL_SIZING_PROFILES table
 * removed in SQL:2011
 */

-- The data in this table are defined by various profiles of SQL.
-- Since we don't have any information about such profiles, we provide
-- an empty table.

CREATE TABLE sql_sizing_profiles (
    sizing_id       cardinal_number,
    sizing_name     character_data,
    profile_id      character_data,
    required_value  cardinal_number,
    comments        character_data
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_1_2
FOREIGN KEY (x1) REFERENCES pktable(id2)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_26.pg_temp_28381_2 (tid pg_catalog.tid)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION test_tab2_sub CONNECTION 'port=22821 host=/tmp/Ajgausiysd dbname=postgres' PUBLICATION test_tab2_pub
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION portuguese ADD MAPPING
	FOR email, url, url_path, host, file, version,
	    sfloat, float, int, uint,
	    numword, hword_numpart, numhword
	WITH simple
;
----------
[CREATE_LIBRARY] CREATE EXTENSION worker_spi
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_partitioned (a int primary key, b integer) PARTITION BY RANGE(a)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX concur_exprs_index_pred_2
  ON concur_exprs_tab ((1 / c1))
  WHERE ('-H') >= (c2::TEXT) COLLATE "C"
;
----------
[RENAME_TABLE] alter table nonesuch rename to stud_emp
;
----------
[CREATE_SCHEMA] create schema mr_sch
;
----------
[CREATE_PROG_OBJ] -- infer anyarray from anymultirange
create function mr_outparam_succeed2(i anymultirange, out r anyarray, out t text)
  as $$ select ARRAY[upper($1)], 'foo'::text $$ language sql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28603 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE notnull_tbl2 ALTER a DROP NOT NULL
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
----------
[CREATE_TABLE] CREATE TABLE itest9 (a int GENERATED ALWAYS AS IDENTITY, b text, c bigint)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30395 RENAME TO reind_con_toast
;
----------
[CREATE_TYPE] create type jsb_i_gt_1_rec as (a jsb_i_gt_1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION explain_mask_costs(query text, do_analyze bool,
    hide_costs bool, hide_row_est bool, hide_width bool) RETURNS setof text
LANGUAGE plpgsql AS
$$
DECLARE
    ln text;
    analyze_str text;
BEGIN
    IF do_analyze = true THEN
        analyze_str := 'on';
    ELSE
        analyze_str := 'off';
    END IF;

    -- avoid jit related output by disabling it
    SET LOCAL jit = 0;

    FOR ln IN
        EXECUTE format('explain (analyze %s, costs on, summary off, timing off, buffers off) %s',
            analyze_str, query)
    LOOP
        IF hide_costs = true THEN
            ln := regexp_replace(ln, 'cost=\d+\.\d\d\.\.\d+\.\d\d', 'cost=N..N');
        END IF;

        IF hide_row_est = true THEN
            -- don't use 'g' so that we leave the actual rows intact
            ln := regexp_replace(ln, 'rows=\d+', 'rows=N');
        END IF;

        IF hide_width = true THEN
            ln := regexp_replace(ln, 'width=\d+', 'width=N');
        END IF;

        RETURN NEXT ln;
    END LOOP;
END;
$$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tsub CONNECTION 'port=30481 host=/tmp/UBekCAWDSf dbname=postgres' PUBLICATION tpub WITH (slot_name = tpub_slot, binary = true)
;
----------
[ADMIN_TABLE] CLUSTER clustertest USING clustertest_pkey
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp3 add constraint attmpconstr foreign key (a) references attmp2 match full
;
----------
[CREATE_TABLE] -- error

-- Read-only tests

CREATE TABLE writetest (a int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION equipment_named(hobby hobbies_r)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where equipment_r.hobby = equipment_named.hobby.name'
   LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE tab5 (a int NOT NULL, b int, c int) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE gin_idx_tbl (id bigserial PRIMARY KEY, data jsonb)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft_part_1_2 (a INT NOT NULL) SERVER s0
;
----------
[CREATE_PROG_OBJ] --- test selecting bool through SPI

CREATE FUNCTION spi_test()  RETURNS void
LANGUAGE plperl
TRANSFORM FOR TYPE bool
AS $$
my $rv = spi_exec_query('SELECT true t, false f, NULL n')->{rows}->[0];

die("TRUE mistransformed to UNDEF in SPI") if (!defined ($rv->{t}));
die("FALSE mistransformed to UNDEF in SPI") if (!defined ($rv->{f}));
die("NULL mistransformed in SPI") if (defined ($rv->{n}));
die("TRUE mistransformed in SPI") if (!$rv->{t});
die("FALSE mistransformed in SPI") if ($rv->{f});
$$
;
----------
[CREATE_TABLE] -- Test to make sure short-circuiting and NULL handling is
-- correct. Use a table as source to prevent constant simplification
-- from interfering.
CREATE TABLE booltbl4(isfalse bool, istrue bool, isnul bool)
;
----------
[CREATE_TABLE] --
-- Previous tests all managed to not test any expressions requiring
-- planner preprocessing ...
--
create table insertconflict (a bigint, b bigint)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table user_table add column address varchar(255) primary key
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION spanish
	(PARSER = default)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad8_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."make_table"() RETURNS "void"
    LANGUAGE "sql"
    AS $$
  CREATE TABLE created_table AS SELECT * FROM int8_tbl;
$$
;
----------
[DROP_PROG_OBJ] -- range type doesn't fit

drop function f1(x anycompatiblerange, y anycompatible, z anycompatible)
;
----------
[CREATE_TABLE] create table insertconflict (f1 int primary key, f2 text)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_ts_template
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION foo_trg() RETURNS trigger AS $$ BEGIN RETURN NEW; END $$ LANGUAGE plpgsql
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_collation
;
----------
[CREATE_SCHEMA] -- error
CREATE SCHEMA newschema6 AUTHORIZATION PUBLIC
;
----------
[CREATE_POLICY] CREATE POLICY r2 ON rec2 USING (a = (SELECT x FROM rec1 WHERE y = b))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION f1(int) RETURNS int LANGUAGE SQL AS 'SELECT f0($1)'
;
----------
[CREATE_TABLE] -- validator tests
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (foo 'bar')
;
----------
[CREATE_PROG_OBJ] create function raise_test2(int) returns int as $$
begin
    raise notice 'This message has too few parameters: %, %, %', $1, $1;
    return $1;
end;
$$ language plpgsql
;
