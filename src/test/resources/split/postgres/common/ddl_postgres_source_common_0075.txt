CREATE EXTENSION test_ext1 SCHEMA test_ext
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_rewrite
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
ALTER TABLE tab2 DROP b, ADD COLUMN c text DEFAULT 'pub_tab2', ADD b text
;
--
-- check indirection (field/array assignment), cf bug #14265
--
-- these tests are aware that transformInsertStmt has 3 separate code paths
--

create type insert_test_type as (if1 int, if2 text[])
;
ANALYZE pg_catalog.pg_shdepend
;
VACUUM pg_catalog.pg_auth_members
;
create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = true)
;
ALTER TABLE ONLY "generated_stored_tests"."gtest22a"
    ADD CONSTRAINT "gtest22a_b_key" UNIQUE ("b")
;
-- Try it with a view, which isn't directly used in the resulting plan
-- but should trigger invalidation anyway
CREATE TEMP VIEW pcacheview AS
  SELECT * FROM pcachetest
;
-- ok
CREATE FUNCTION priv_testfunc3(int) RETURNS int AS 'select 2 * $1;' LANGUAGE sql
;
ALTER OPERATOR FAMILY gist_bool_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
CREATE TABLE INT4_TBL(f1 int4)
;
CREATE TABLE test_qual_pushdown (
    abc text
)
;
ANALYZE pg_catalog.pg_db_role_setting
;
-- Generic extended statistics support

-- We will be checking execution plans without/with statistics, so
-- let's make sure we get simple non-parallel plans. Also set the
-- work_mem low so that we can use small amounts of data.

-- check the number of estimated/actual rows in the top node
create function check_estimated_rows(text) returns table (estimated int, actual int)
language plpgsql as
$$
declare
    ln text;
    tmp text[];
    first_row bool := true;
begin
    for ln in
        execute format('explain analyze %s', $1)
    loop
        if first_row then
            first_row := false;
            tmp := regexp_match(ln, 'rows=(\d*) .* rows=(\d*)');
            return query select tmp[1]::int, tmp[2]::int;
        end if;
    end loop;
end;
$$
;
-- error
DROP TABLE itest_parent
;
CREATE DOMAIN character_data AS character varying COLLATE "C"
;
ALTER TABLE ONLY "public"."pht1_e" ATTACH PARTITION "public"."pht1_e_p1" FOR VALUES WITH (modulus 3, remainder 0)
;
create table test_json_agg (f1 text, f2 hstore)
;
DROP INDEX plain_unique
;
--now ok

-- parents and child not-null will all be validated.
ALTER TABLE notnull_tbl1 VALIDATE CONSTRAINT nn_parent
;
-- some checks after successful creation of a partitioned table
CREATE FUNCTION plusone(a int) RETURNS INT AS $$ SELECT a+1; $$ LANGUAGE SQL
;
ALTER TABLE plt1_adv ATTACH PARTITION plt1_adv_p1 FOR VALUES IN ('0001', '0003')
;
alter type two_ints add attribute c two_ints_multirange
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_seclabel
;
create or replace function raise_test() returns void as $$
begin
  raise exception 'custom exception'
     using detail = 'some detail of custom exception',
           hint = 'some hint related to custom exception';
end;
$$ language plpgsql
;
-- inlining of this function causes LHS and RHS to be switched,
-- which we can't cope with, so hashing should be abandoned
create or replace function bogus_int8_text_eq(int8, text) returns boolean
language sql as 'select $2 = $1::text'
;
CREATE OPERATOR CLASS split_operator_probe.int_hash_ops
FOR TYPE integer USING hash FAMILY split_operator_probe.int_hash_family AS
    OPERATOR 1 = (integer, integer),
    FUNCTION 1 hashint4(integer)
;
CREATE FUNCTION priv_testfunc6b(b int) RETURNS priv_testdomain1 LANGUAGE SQL AS $$ SELECT $1::priv_testdomain1 $$
;
CREATE DATABASE tbuiltin5 ENCODING ' UTF-8' TEMPLATE template0 BUILTIN_LOCALE 'C.UTF8' LC_COLLATE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_policy
;
ALTER TABLE attmp7 ADD CONSTRAINT identity CHECK (b = boo(b))
;
ANALYZE public.domnotnull
;
ALTER TABLE pg_temp_10.pg_temp_26754_2 ADD COLUMN newdata pg_temp_10.pg_temp_26754
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_shdescription
;
create table idxpart (a int, b int, primary key (a)) partition by range (a)
;
create unlogged table pgbench_accounts_2
  partition of pgbench_accounts
  for values from (33335) to (66669) with (fillfactor=100)
;
alter domain testdomain1 drop constraint unsigned_foo
;
ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_3" FOR VALUES IN (2500, 2501, 2502, 2503)
;
CREATE TEMP TABLE pg_temp_2.pg_temp_28316_2 (tid pg_catalog.tid)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
CREATE INDEX t2_spgist ON s3.t2 USING SPGIST (ir)
;
do $$ declare c two_int8s;
begin c := row(1,2); raise notice 'c = %', c; end$$
;
ALTER TABLE ONLY "fkpart6"."pk11"
    ADD CONSTRAINT "pk11_pkey" PRIMARY KEY ("a")
;
VACUUM (PARALLEL 0) pg_catalog.pg_attrdef
;
--
-- Tests for ANALYZE ONLY / VACUUM ONLY on partitioned tables
--
CREATE TABLE only_parted (a int, b text) PARTITION BY LIST (a)
;
CREATE TABLE range_part2 (
	a int NOT NULL CHECK (a = 1),
	b int NOT NULL CHECK (b >= 10 and b < 18)
)
;
ANALYZE pg_temp_9.pg_temp_26761_2
;
create index part_column_drop_b_pred on part_column_drop(b) where b = 1
;
------------------------------------------------------------------------------------------
[CREATE_LIBRARY] CREATE EXTENSION test_ext1 SCHEMA test_ext
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_rewrite
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_partitioned_table
;
----------
[ALTER_TABLE|DROP_COLUMN|ADD_COLUMN] ALTER TABLE tab2 DROP b, ADD COLUMN c text DEFAULT 'pub_tab2', ADD b text
;
----------
[CREATE_TYPE] --
-- check indirection (field/array assignment), cf bug #14265
--
-- these tests are aware that transformInsertStmt has 3 separate code paths
--

create type insert_test_type as (if1 int, if2 text[])
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_shdepend
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_auth_members
;
----------
[ADD_INDEX] create index btree_idx_err on btree_test(a) with (vacuum_cleanup_index_scale_factor = true)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest22a"
    ADD CONSTRAINT "gtest22a_b_key" UNIQUE ("b")
;
----------
[CREATE_VIEW(SELECT)] -- Try it with a view, which isn't directly used in the resulting plan
-- but should trigger invalidation anyway
CREATE TEMP VIEW pcacheview AS
  SELECT * FROM pcachetest
;
----------
[CREATE_PROG_OBJ] -- ok
CREATE FUNCTION priv_testfunc3(int) RETURNS int AS 'select 2 * $1;' LANGUAGE sql
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_bool_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_TABLE] CREATE TABLE INT4_TBL(f1 int4)
;
----------
[CREATE_TABLE] CREATE TABLE test_qual_pushdown (
    abc text
)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_db_role_setting
;
----------
[CREATE_PROG_OBJ] -- Generic extended statistics support

-- We will be checking execution plans without/with statistics, so
-- let's make sure we get simple non-parallel plans. Also set the
-- work_mem low so that we can use small amounts of data.

-- check the number of estimated/actual rows in the top node
create function check_estimated_rows(text) returns table (estimated int, actual int)
language plpgsql as
$$
declare
    ln text;
    tmp text[];
    first_row bool := true;
begin
    for ln in
        execute format('explain analyze %s', $1)
    loop
        if first_row then
            first_row := false;
            tmp := regexp_match(ln, 'rows=(\d*) .* rows=(\d*)');
            return query select tmp[1]::int, tmp[2]::int;
        end if;
    end loop;
end;
$$
;
----------
[DROP_TABLE] -- error
DROP TABLE itest_parent
;
----------
[CREATE_TYPE] CREATE DOMAIN character_data AS character varying COLLATE "C"
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pht1_e" ATTACH PARTITION "public"."pht1_e_p1" FOR VALUES WITH (modulus 3, remainder 0)
;
----------
[CREATE_TABLE] create table test_json_agg (f1 text, f2 hstore)
;
----------
[DROP_INDEX] DROP INDEX plain_unique
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] --now ok

-- parents and child not-null will all be validated.
ALTER TABLE notnull_tbl1 VALIDATE CONSTRAINT nn_parent
;
----------
[CREATE_PROG_OBJ] -- some checks after successful creation of a partitioned table
CREATE FUNCTION plusone(a int) RETURNS INT AS $$ SELECT a+1; $$ LANGUAGE SQL
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt1_adv ATTACH PARTITION plt1_adv_p1 FOR VALUES IN ('0001', '0003')
;
----------
[ALTER_TYPE] alter type two_ints add attribute c two_ints_multirange
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_seclabel
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise exception 'custom exception'
     using detail = 'some detail of custom exception',
           hint = 'some hint related to custom exception';
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] -- inlining of this function causes LHS and RHS to be switched,
-- which we can't cope with, so hashing should be abandoned
create or replace function bogus_int8_text_eq(int8, text) returns boolean
language sql as 'select $2 = $1::text'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS split_operator_probe.int_hash_ops
FOR TYPE integer USING hash FAMILY split_operator_probe.int_hash_family AS
    OPERATOR 1 = (integer, integer),
    FUNCTION 1 hashint4(integer)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION priv_testfunc6b(b int) RETURNS priv_testdomain1 LANGUAGE SQL AS $$ SELECT $1::priv_testdomain1 $$
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin5 ENCODING ' UTF-8' TEMPLATE template0 BUILTIN_LOCALE 'C.UTF8' LC_COLLATE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_policy
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp7 ADD CONSTRAINT identity CHECK (b = boo(b))
;
----------
[ADMIN_TABLE] ANALYZE public.domnotnull
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_10.pg_temp_26754_2 ADD COLUMN newdata pg_temp_10.pg_temp_26754
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_shdescription
;
----------
[CREATE_TABLE] create table idxpart (a int, b int, primary key (a)) partition by range (a)
;
----------
[CREATE_TABLE] create unlogged table pgbench_accounts_2
  partition of pgbench_accounts
  for values from (33335) to (66669) with (fillfactor=100)
;
----------
[ALTER_TYPE] alter domain testdomain1 drop constraint unsigned_foo
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_3" FOR VALUES IN (2500, 2501, 2502, 2503)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_2.pg_temp_28316_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
----------
[ADD_INDEX] CREATE INDEX t2_spgist ON s3.t2 USING SPGIST (ir)
;
----------
[BLOCK] do $$ declare c two_int8s;
begin c := row(1,2); raise notice 'c = %', c; end$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart6"."pk11"
    ADD CONSTRAINT "pk11_pkey" PRIMARY KEY ("a")
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_attrdef
;
----------
[CREATE_TABLE] --
-- Tests for ANALYZE ONLY / VACUUM ONLY on partitioned tables
--
CREATE TABLE only_parted (a int, b text) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE range_part2 (
	a int NOT NULL CHECK (a = 1),
	b int NOT NULL CHECK (b >= 10 and b < 18)
)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26761_2
;
----------
[ADD_INDEX] create index part_column_drop_b_pred on part_column_drop(b) where b = 1
;
