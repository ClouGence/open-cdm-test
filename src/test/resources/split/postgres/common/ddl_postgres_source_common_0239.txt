ALTER TABLE spc_hint SET TABLESPACE other
;
-- obsolete:
CREATE FUNCTION crosstab(text,int)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
create table customers ( c varchar ( 50 ) , p int )
;
CREATE TEMP TABLE pg_temp_4.pg_temp_37874_2 (tid pg_catalog.tid)
;
CREATE SEQUENCE "public"."sequence_test2"
    START WITH 32
    INCREMENT BY 4
    MINVALUE 5
    MAXVALUE 36
    CACHE 1
    CYCLE
;
ALTER TABLE pg_temp_32.pg_temp_27252_2 ADD COLUMN newdata pg_temp_32.pg_temp_27252
;
COMMENT ON TEXT SEARCH DICTIONARY swedish_stem IS 'snowball stemmer for swedish language'
;
CREATE FUNCTION isnne(issn, issn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
alter table loct add check (a in (3))
;
CREATE DATABASE "template1" WITH TEMPLATE = template0 OID = 1 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
ALTER TABLE replication_example DROP COLUMN bar
;
-- wait until the worker completes its initialization
DO $$
DECLARE
	visible bool;
	loops int := 0;
BEGIN
	LOOP
		visible := table_name IS NOT NULL
			FROM information_schema.tables
			WHERE table_schema = 'schema4' AND table_name = 'counted';
		IF visible OR loops > 120 * 10 THEN EXIT; END IF;
		PERFORM pg_sleep(0.1);
		loops := loops + 1;
	END LOOP;
END
$$
;
CREATE FUNCTION gbt_float8_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE OPERATOR CLASS int4_custom_ops1 FOR TYPE int4 USING btree AS
		OPERATOR 1 < (int4, int4), OPERATOR 2 <= (int4, int4),
		OPERATOR 3 = (int4, int4), OPERATOR 4 >= (int4, int4),
		OPERATOR 5 > (int4, int4), FUNCTION 1 ok_cmp1(int4, int4)
;
ALTER OPERATOR FAMILY alt_opf13 USING hash ADD FUNCTION 1 fn_opf13(int4)
;
VACUUM (PARALLEL 2) pg_catalog.pg_largeobject
;
CREATE TABLE t_009_tbl_standby_mvcc (id int, msg text)
;
ALTER SUBSCRIPTION split_sub SET (slot_name = NONE)
;
DROP TRIGGER trig ON part_c_1_c_20
;
create or replace view at_view_1 as select *, 2+2 as more from at_base_table bt
;
CREATE TABLE "public"."part_p1" (
    "a" integer,
    "b" integer
)
;
create function noticetrigger() returns trigger as $$
begin
  raise notice 'noticetrigger % %', new.f1, new.data;
  return null;
end $$ language plpgsql
;
VACUUM (FULL) vactst
;
-- invalid: non-lowercase quoted identifiers
CREATE TYPE bogus_type (
	"Internallength" = 4,
	"Input" = int42_in,
	"Output" = int42_out,
	"Alignment" = int4,
	"Default" = 42,
	"Passedbyvalue"
)
;
CREATE VIEW mysecview10 WITH (security_invoker=100)	-- Error
       AS SELECT * FROM tbl1 WHERE a <> 100
;
/*
 * 5.51
 * ROUTINE_ROUTINE_USAGE view
 */

CREATE VIEW routine_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np1.nspname AS sql_identifier) AS routine_schema,
           CAST(nameconcatoid(p1.proname, p1.oid) AS sql_identifier) AS routine_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_proc p1, pg_namespace np1

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = p1.oid
          AND d.refclassid = 'pg_catalog.pg_proc'::regclass
          AND p1.pronamespace = np1.oid
          AND p.prokind IN ('f', 'p') AND p1.prokind IN ('f', 'p')
          AND pg_has_role(p1.proowner, 'USAGE')
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "url" WITH "simple"
;
DROP TABLE tab_policy_stats
;
--
-- OUT/INOUT/TABLE functions
--

create function outparam_succeed(i anyrange, out r anyrange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
CREATE FUNCTION pg_visibility_map_summary(regclass,
    OUT all_visible bigint, OUT all_frozen bigint)
RETURNS record
AS '$libdir/pg_visibility', 'pg_visibility_map_summary'
LANGUAGE C STRICT
;
ALTER TABLE pg_class ADD UNIQUE USING INDEX pg_class_relname_nsp_index
;
DROP TABLE test_repl_stat
;
CREATE AGGREGATE myaggn08b(BASETYPE = anyelement, SFUNC = tf2p, STYPE = int[],
  INITCOND = '{}')
;
ANALYZE vacparted (b), vactst
;
CREATE TABLE cj_source2 (sid2 integer, sval text)
  WITH (autovacuum_enabled=off)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_subscription
;
ALTER TABLE pg_toast.pg_toast_30354 RENAME TO reind_con_toast
;
create trigger check_fkeys_pkey_exist
	before insert or update on fkeys
	for each row
	execute function
	check_primary_key ('fkey1', 'fkey2', 'pkeys', 'pkey1', 'pkey2')
;
create trigger trig_del_before_child before delete on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
--
-- LEAKPROOF
--
CREATE FUNCTION functest_E_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 100'
;
--    Case4 (R = N) && ((B = P) || (B = N))
--    -------------------------------------
--    S    tf1      B    tf2
--    -----------------------
--    N    N        N    N
-- should CREATE
CREATE AGGREGATE myaggn05a(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE OPERATOR ==== (
    LEFTARG = real,
    RIGHTARG = boolean,
    PROCEDURE = alter_op_test_fn_real_bool
)
;
CREATE TABLE "public"."dropcolumnanother" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "e" integer,
    "........pg.dropped.4........" INTEGER /* dummy */,
    "d" integer
)
;
-- case with multi-level partitioning
create table parted_conflict_test_4 partition of parted_conflict_test for values in (5) partition by list (a)
;
ALTER TABLE itest8
  ADD COLUMN f2 int NOT NULL,
  ALTER COLUMN f2 ADD GENERATED ALWAYS AS IDENTITY
;
VACUUM (PARALLEL 0) pg_catalog.pg_ts_config_map
;
DROP TABLE pg_temp_2.pg_temp_28316_2, pg_temp_2.pg_temp_28316
;
CREATE VIEW udt_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(n.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST('TYPE USAGE' AS character_data) AS privilege_type, -- sic
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, t.typowner, 'USAGE')
                  OR t.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, typname, typnamespace, typtype, typowner, (aclexplode(coalesce(typacl, acldefault('T', typowner)))).* FROM pg_type
         ) AS t (oid, typname, typnamespace, typtype, typowner, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE t.typnamespace = n.oid
          AND t.typtype = 'c'
          AND t.grantee = grantee.oid
          AND t.grantor = u_grantor.oid
          AND t.prtype IN ('USAGE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
--not-indexed
CREATE OPERATOR ^~ (
        LEFTARG = ltree,
	RIGHTARG = lquery,
	PROCEDURE = ltq_regex,
	COMMUTATOR = '^~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
ANALYZE fkpart5.pk11
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] ALTER TABLE spc_hint SET TABLESPACE other
;
----------
[CREATE_PROG_OBJ] -- obsolete:
CREATE FUNCTION crosstab(text,int)
RETURNS setof record
AS '$libdir/tablefunc','crosstab'
LANGUAGE C STABLE STRICT
;
----------
[CREATE_TABLE] create table customers ( c varchar ( 50 ) , p int )
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_37874_2 (tid pg_catalog.tid)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test2"
    START WITH 32
    INCREMENT BY 4
    MINVALUE 5
    MAXVALUE 36
    CACHE 1
    CYCLE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_32.pg_temp_27252_2 ADD COLUMN newdata pg_temp_32.pg_temp_27252
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY swedish_stem IS 'snowball stemmer for swedish language'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(issn, issn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table loct add check (a in (3))
;
----------
[CREATE_CATALOG] CREATE DATABASE "template1" WITH TEMPLATE = template0 OID = 1 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE replication_example DROP COLUMN bar
;
----------
[BLOCK] -- wait until the worker completes its initialization
DO $$
DECLARE
	visible bool;
	loops int := 0;
BEGIN
	LOOP
		visible := table_name IS NOT NULL
			FROM information_schema.tables
			WHERE table_schema = 'schema4' AND table_name = 'counted';
		IF visible OR loops > 120 * 10 THEN EXIT; END IF;
		PERFORM pg_sleep(0.1);
		loops := loops + 1;
	END LOOP;
END
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float8_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS int4_custom_ops1 FOR TYPE int4 USING btree AS
		OPERATOR 1 < (int4, int4), OPERATOR 2 <= (int4, int4),
		OPERATOR 3 = (int4, int4), OPERATOR 4 >= (int4, int4),
		OPERATOR 5 > (int4, int4), FUNCTION 1 ok_cmp1(int4, int4)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf13 USING hash ADD FUNCTION 1 fn_opf13(int4)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_largeobject
;
----------
[CREATE_TABLE] CREATE TABLE t_009_tbl_standby_mvcc (id int, msg text)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION split_sub SET (slot_name = NONE)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig ON part_c_1_c_20
;
----------
[CREATE_VIEW(SELECT)] create or replace view at_view_1 as select *, 2+2 as more from at_base_table bt
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part_p1" (
    "a" integer,
    "b" integer
)
;
----------
[CREATE_PROG_OBJ] create function noticetrigger() returns trigger as $$
begin
  raise notice 'noticetrigger % %', new.f1, new.data;
  return null;
end $$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (FULL) vactst
;
----------
[CREATE_TYPE] -- invalid: non-lowercase quoted identifiers
CREATE TYPE bogus_type (
	"Internallength" = 4,
	"Input" = int42_in,
	"Output" = int42_out,
	"Alignment" = int4,
	"Default" = 42,
	"Passedbyvalue"
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mysecview10 WITH (security_invoker=100)	-- Error
       AS SELECT * FROM tbl1 WHERE a <> 100
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.51
 * ROUTINE_ROUTINE_USAGE view
 */

CREATE VIEW routine_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np1.nspname AS sql_identifier) AS routine_schema,
           CAST(nameconcatoid(p1.proname, p1.oid) AS sql_identifier) AS routine_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_proc p1, pg_namespace np1

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = p1.oid
          AND d.refclassid = 'pg_catalog.pg_proc'::regclass
          AND p1.pronamespace = np1.oid
          AND p.prokind IN ('f', 'p') AND p1.prokind IN ('f', 'p')
          AND pg_has_role(p1.proowner, 'USAGE')
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "url" WITH "simple"
;
----------
[DROP_TABLE] DROP TABLE tab_policy_stats
;
----------
[CREATE_PROG_OBJ] --
-- OUT/INOUT/TABLE functions
--

create function outparam_succeed(i anyrange, out r anyrange, out t text)
  as $$ select $1, 'foo'::text $$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_visibility_map_summary(regclass,
    OUT all_visible bigint, OUT all_frozen bigint)
RETURNS record
AS '$libdir/pg_visibility', 'pg_visibility_map_summary'
LANGUAGE C STRICT
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_class ADD UNIQUE USING INDEX pg_class_relname_nsp_index
;
----------
[DROP_TABLE] DROP TABLE test_repl_stat
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn08b(BASETYPE = anyelement, SFUNC = tf2p, STYPE = int[],
  INITCOND = '{}')
;
----------
[ADMIN_TABLE] ANALYZE vacparted (b), vactst
;
----------
[CREATE_TABLE] CREATE TABLE cj_source2 (sid2 integer, sval text)
  WITH (autovacuum_enabled=off)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_subscription
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30354 RENAME TO reind_con_toast
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger check_fkeys_pkey_exist
	before insert or update on fkeys
	for each row
	execute function
	check_primary_key ('fkey1', 'fkey2', 'pkeys', 'pkey1', 'pkey2')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_del_before_child before delete on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
----------
[CREATE_PROG_OBJ] --
-- LEAKPROOF
--
CREATE FUNCTION functest_E_1(int) RETURNS bool LANGUAGE 'sql'
       AS 'SELECT $1 > 100'
;
----------
[CREATE_PROG_OBJ] --    Case4 (R = N) && ((B = P) || (B = N))
--    -------------------------------------
--    S    tf1      B    tf2
--    -----------------------
--    N    N        N    N
-- should CREATE
CREATE AGGREGATE myaggn05a(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ==== (
    LEFTARG = real,
    RIGHTARG = boolean,
    PROCEDURE = alter_op_test_fn_real_bool
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."dropcolumnanother" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "e" integer,
    "........pg.dropped.4........" INTEGER /* dummy */,
    "d" integer
)
;
----------
[CREATE_TABLE] -- case with multi-level partitioning
create table parted_conflict_test_4 partition of parted_conflict_test for values in (5) partition by list (a)
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE itest8
  ADD COLUMN f2 int NOT NULL,
  ALTER COLUMN f2 ADD GENERATED ALWAYS AS IDENTITY
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_ts_config_map
;
----------
[DROP_TABLE] DROP TABLE pg_temp_2.pg_temp_28316_2, pg_temp_2.pg_temp_28316
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW udt_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(n.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST('TYPE USAGE' AS character_data) AS privilege_type, -- sic
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, t.typowner, 'USAGE')
                  OR t.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable

    FROM (
            SELECT oid, typname, typnamespace, typtype, typowner, (aclexplode(coalesce(typacl, acldefault('T', typowner)))).* FROM pg_type
         ) AS t (oid, typname, typnamespace, typtype, typowner, grantor, grantee, prtype, grantable),
         pg_namespace n,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE t.typnamespace = n.oid
          AND t.typtype = 'c'
          AND t.grantee = grantee.oid
          AND t.grantor = u_grantor.oid
          AND t.prtype IN ('USAGE')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
----------
[CREATE_PROG_OBJ] --not-indexed
CREATE OPERATOR ^~ (
        LEFTARG = ltree,
	RIGHTARG = lquery,
	PROCEDURE = ltq_regex,
	COMMUTATOR = '^~',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[ADMIN_TABLE] ANALYZE fkpart5.pk11
;
