CREATE VIEW rule_v1 AS SELECT * FROM rule_t1
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
-- match any word but return nothing
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=false, MATCHORIG=true, KEEPSYNONYMS=false, MATCHSYNONYMS=true)
;
CREATE TABLE "fkpart5"."fk11" (
    "a" integer
)
;
CREATE VIEW pg_statio_user_sequences AS
    SELECT * FROM pg_statio_all_sequences
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema') AND
          schemaname !~ '^pg_toast'
;
CREATE FUNCTION dblink_current_query ()
RETURNS text
AS '$libdir/dblink','dblink_current_query'
LANGUAGE C PARALLEL RESTRICTED
;
CREATE TEMP TABLE test_ext4_tab ()
;
CREATE TABLE alpha_pos_p1 PARTITION OF alpha_pos FOR VALUES IN ('0001', '0003')
;
CREATE INDEX ON vegetables (id)
;
CREATE TABLE i_am_a_failure() USING "btree"
;
CREATE FUNCTION citextin(cstring)
RETURNS citext
AS 'textin'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
ANALYZE public.itest7
;
/* contrib/tsm_system_time/tsm_system_time--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION system_time(internal)
RETURNS tsm_handler
AS '$libdir/tsm_system_time', 'tsm_system_time_handler'
LANGUAGE C STRICT
;
alter table idxpart2 drop column c
;
ALTER INDEX pg_toast.pg_toast_30768_index RENAME TO reind_con_toast_idx
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', escape ':')
;
CREATE TABLE "public"."quad_point_tbl" (
    "p" "point"
)
;
CREATE TABLE gtest_normal (a int, b int)
;
CREATE TABLE thirdparent (tomorrow date default now()::date - 1)
;
VACUUM (FREEZE) pg_catalog.pg_language
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
-- Now 2 columns 2 tables, matching types
create table fktable (ftest1 int, ftest2 int, foreign key(ftest1, ftest2) references pktable(base1, ptest1))
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn13,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
CREATE FUNCTION gin_extract_value_bit(bit, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE SCHEMA reindex_schema
;
create function f1_times_10() returns trigger as
$$ begin new.f1 := new.f1 * 10; return new; end $$ language plpgsql
;
ALTER TABLE IF EXISTS tt8 ALTER COLUMN f SET DEFAULT 0
;
ALTER INDEX pg_toast.pg_toast_28035_index RENAME TO reind_con_toast_idx
;
CREATE TEXT SEARCH CONFIGURATION hungarian
	(PARSER = default)
;
-- Test code that uses Append nodes in place of MergeAppend when the
-- partition ordering matches the desired ordering.

create index mcrparted_a_abs_c_idx on mcrparted (a, abs(b), c)
;
CREATE TEMPORARY TABLE tmp_ok(data int not null)
;
ALTER TABLE test_tab2 REPLICA IDENTITY FULL
;
CREATE TRIGGER silent_exception_trigger
AFTER INSERT OR UPDATE ON t1
FOR EACH ROW
EXECUTE FUNCTION handle_exception_trigger()
;
--
-- Test the FOUND magic variable
--
CREATE TABLE found_test_tbl (a int)
;
ALTER TABLE ONLY "public"."regress_notnull2" INHERIT "public"."regress_notnull1"
;
CALL ptest10(b => 8, c => 2, a => 0)
;
drop table another
;
DROP MATERIALIZED VIEW matview_stats_1
;
-- test naming a constraint in a partition when a conflict exists
CREATE TABLE parted_fk_naming (
    id bigint NOT NULL default 1,
    id_abc bigint,
    CONSTRAINT dummy_constr FOREIGN KEY (id_abc)
        REFERENCES parted_fk_naming (id),
    PRIMARY KEY (id)
)
PARTITION BY LIST (id)
;
COMMENT ON CONSTRAINT us_postal_code_check
						 ON DOMAIN dump_test.us_postal_code IS 'check it'
;
ANALYZE public.delete_test_table
;
ALTER TABLE pg_publication_namespace ADD UNIQUE USING INDEX pg_publication_namespace_pnnspid_pnpubid_index
;
DROP TABLE pg_temp_2.pg_temp_28230_2, pg_temp_2.pg_temp_28230
;
drop domain di
;
DROP FOREIGN TABLE foreign_part
;
CREATE TABLE "generated_stored_tests"."gtest28a" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2)) STORED
)
;
create table mlparted_tab (a int, b char, c text) partition by list (a)
;
alter index idxpart1_expr_idx attach partition idxpart11_expr_idx
;
create type ctype as (f1 int, f2 text)
;
CREATE TABLE pred_tab (a int, b int)
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW rule_v1 AS SELECT * FROM rule_t1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_largeobject
;
----------
[ALTER_POLICY] -- match any word but return nothing
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=false, MATCHORIG=true, KEEPSYNONYMS=false, MATCHSYNONYMS=true)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."fk11" (
    "a" integer
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_statio_user_sequences AS
    SELECT * FROM pg_statio_all_sequences
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema') AND
          schemaname !~ '^pg_toast'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_current_query ()
RETURNS text
AS '$libdir/dblink','dblink_current_query'
LANGUAGE C PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TEMP TABLE test_ext4_tab ()
;
----------
[CREATE_TABLE] CREATE TABLE alpha_pos_p1 PARTITION OF alpha_pos FOR VALUES IN ('0001', '0003')
;
----------
[ADD_INDEX] CREATE INDEX ON vegetables (id)
;
----------
[CREATE_TABLE] CREATE TABLE i_am_a_failure() USING "btree"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citextin(cstring)
RETURNS citext
AS 'textin'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE public.itest7
;
----------
[CREATE_PROG_OBJ] /* contrib/tsm_system_time/tsm_system_time--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION system_time(internal)
RETURNS tsm_handler
AS '$libdir/tsm_system_time', 'tsm_system_time_handler'
LANGUAGE C STRICT
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart2 drop column c
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30768_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'binary', escape ':')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."quad_point_tbl" (
    "p" "point"
)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_normal (a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE thirdparent (tomorrow date default now()::date - 1)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_language
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] -- Now 2 columns 2 tables, matching types
create table fktable (ftest1 int, ftest2 int, foreign key(ftest1, ftest2) references pktable(base1, ptest1))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = isbn13,
	RIGHTARG = ean13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_bit(bit, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_SCHEMA] CREATE SCHEMA reindex_schema
;
----------
[CREATE_PROG_OBJ] create function f1_times_10() returns trigger as
$$ begin new.f1 := new.f1 * 10; return new; end $$ language plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE IF EXISTS tt8 ALTER COLUMN f SET DEFAULT 0
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28035_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION hungarian
	(PARSER = default)
;
----------
[ADD_INDEX] -- Test code that uses Append nodes in place of MergeAppend when the
-- partition ordering matches the desired ordering.

create index mcrparted_a_abs_c_idx on mcrparted (a, abs(b), c)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE tmp_ok(data int not null)
;
----------
[ALTER_TABLE] ALTER TABLE test_tab2 REPLICA IDENTITY FULL
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER silent_exception_trigger
AFTER INSERT OR UPDATE ON t1
FOR EACH ROW
EXECUTE FUNCTION handle_exception_trigger()
;
----------
[CREATE_TABLE] --
-- Test the FOUND magic variable
--
CREATE TABLE found_test_tbl (a int)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."regress_notnull2" INHERIT "public"."regress_notnull1"
;
----------
[CALL_PROG_OBJ] CALL ptest10(b => 8, c => 2, a => 0)
;
----------
[DROP_TABLE] drop table another
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW matview_stats_1
;
----------
[CREATE_TABLE] -- test naming a constraint in a partition when a conflict exists
CREATE TABLE parted_fk_naming (
    id bigint NOT NULL default 1,
    id_abc bigint,
    CONSTRAINT dummy_constr FOREIGN KEY (id_abc)
        REFERENCES parted_fk_naming (id),
    PRIMARY KEY (id)
)
PARTITION BY LIST (id)
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT us_postal_code_check
						 ON DOMAIN dump_test.us_postal_code IS 'check it'
;
----------
[ADMIN_TABLE] ANALYZE public.delete_test_table
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_publication_namespace ADD UNIQUE USING INDEX pg_publication_namespace_pnnspid_pnpubid_index
;
----------
[DROP_TABLE] DROP TABLE pg_temp_2.pg_temp_28230_2, pg_temp_2.pg_temp_28230
;
----------
[DROP_TYPE] drop domain di
;
----------
[DROP_TABLE] DROP FOREIGN TABLE foreign_part
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest28a" (
    "........pg.dropped.1........" INTEGER /* dummy */,
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2)) STORED
)
;
----------
[CREATE_TABLE] create table mlparted_tab (a int, b char, c text) partition by list (a)
;
----------
[ALTER_INDEX] alter index idxpart1_expr_idx attach partition idxpart11_expr_idx
;
----------
[CREATE_TYPE] create type ctype as (f1 int, f2 text)
;
----------
[CREATE_TABLE] CREATE TABLE pred_tab (a int, b int)
;
