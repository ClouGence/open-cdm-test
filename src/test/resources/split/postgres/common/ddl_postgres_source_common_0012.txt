CREATE OR REPLACE FUNCTION
  jsonb_path_match_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                   silent boolean DEFAULT false)
RETURNS boolean
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_match_tz'
;
-- extension
DROP EXTENSION test_extension_exists
;
ANALYZE public.shoelace_log
;
create table tab_batch_sharded (id int, data text) partition by hash(id)
;
CREATE FUNCTION bar_int4_text(int4) RETURNS text LANGUAGE SQL AS
$$ SELECT ('bar'::text || $1::text); $$
;
create database regress_pg_dump_test
;
create domain dcomptype as comptype check ((value).cf1 > 0)
;
DROP TABLE inhg
;
CREATE INDEX "comment_test_index" ON "public"."comment_test" USING "btree" ("indexed_col")
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_user_mapping
;
CREATE VIEW pg_stat_bgwriter AS
    SELECT
        pg_stat_get_bgwriter_buf_written_clean() AS buffers_clean,
        pg_stat_get_bgwriter_maxwritten_clean() AS maxwritten_clean,
        pg_stat_get_buf_alloc() AS buffers_alloc,
        pg_stat_get_bgwriter_stat_reset_time() AS stats_reset
;
CREATE SUBSCRIPTION dummy_sub CONNECTION '' PUBLICATION foo WITH (connect = false, slot_name = NONE)
;
ALTER SUBSCRIPTION tap_sub RENAME TO tap_sub_renamed
;
CREATE AGGREGATE regress_pg_dump_schema.test_agg(int2)
(SFUNC = int2_sum, STYPE = int8)
;
CREATE TABLE pgss_a (id integer PRIMARY KEY)
;
ALTER FUNCTION float8_dist(double precision, double precision) PARALLEL SAFE
;
CREATE TABLE part3 PARTITION OF range_parted2 FOR VALUES FROM (30) TO (40)
;
---
--- ALTER_TYPE_ENUM
---

ALTER TYPE enum_test ADD VALUE 'zzz' AFTER 'baz'
;
ALTER TABLE part_c_100_200 ADD COLUMN b bigint
;
-- conversion
DROP CONVERSION test_conversion_exists
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=26577 host=/tmp/Lnf6IHniY8 dbname=postgres' PUBLICATION pub_all
;
ALTER EXTENSION test_pg_dump ADD FOREIGN TABLE ft1
;
CREATE TABLE unique_tbl (i int UNIQUE DEFERRABLE, t text)
;
CREATE RULE "rtest_emp_ins" AS
    ON INSERT TO "public"."rtest_emp" DO  INSERT INTO "public"."rtest_emplog" ("ename", "who", "action", "newsal", "oldsal")
  VALUES ("new"."ename", CURRENT_USER, 'hired'::"bpchar", "new"."salary", '$0.00'::"money")
;
DROP TABLE ruletest_t3
;
alter table fkpart1.fk_part detach partition fkpart1.fk_part_1
;
DROP VIEW atestv0
;
create or replace view my_locks as
select case when c.relname like 'pg_toast%' then 'pg_toast' else c.relname end, max(mode::lockmodes) as max_lockmode
from pg_locks l join pg_class c on l.relation = c.oid
where virtualtransaction = (
        select virtualtransaction
        from pg_locks
        where transactionid = txid_current()::integer)
and locktype = 'relation'
and relnamespace != (select oid from pg_namespace where nspname = 'pg_catalog')
and c.relname != 'my_locks'
group by c.relname
;
CREATE PUBLICATION testpub2
;
ALTER SUBSCRIPTION admin_sub SET (run_as_owner = false)
;
-- check that, after being detached, attinhcount/coninhcount is dropped to 0 and
-- attislocal/conislocal is set to true
ALTER TABLE list_parted2 DETACH PARTITION part_3_4
;
-- EAN13 vs other types:
CREATE FUNCTION btean13cmp(ean13, isbn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE fk_notpartitioned_pk (fdrop1 int, a int, fdrop2 int, b int,
  PRIMARY KEY (a, b))
;
CREATE TABLE "public"."dept" (
    "dname" "name",
    "mgrname" "text"
)
;
-- exercise record typmod remapping between backends
CREATE FUNCTION make_record(n int)
  RETURNS RECORD LANGUAGE plpgsql PARALLEL SAFE AS
$$
BEGIN
  RETURN CASE n
           WHEN 1 THEN ROW(1)
           WHEN 2 THEN ROW(1, 2)
           WHEN 3 THEN ROW(1, 2, 3)
           WHEN 4 THEN ROW(1, 2, 3, 4)
           ELSE ROW(1, 2, 3, 4, 5)
         END;
END;
$$
;
CREATE INDEX "spgist_domain_idx" ON "public"."spgist_domain_tbl" USING "spgist" ("f1")
;
CREATE TABLE "public"."part2_self_fk" (
    "id" bigint CONSTRAINT "parted_self_fk_id_not_null" NOT NULL,
    "id_abc" bigint
)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
--
-- Tables and rules for the instead nothing test
--
create table rtest_nothn1 (a int4, b text)
;
DROP TYPE collate_dep_test2
;
CREATE OR REPLACE FUNCTION event_trigger_report_dropped()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r record;
BEGIN
    FOR r IN SELECT * from pg_event_trigger_dropped_objects()
    LOOP
    IF NOT r.normal AND NOT r.original THEN
        CONTINUE;
    END IF;
    RAISE NOTICE 'NORMAL: orig=% normal=% istemp=% type=% identity=% name=% args=%',
        r.original, r.normal, r.is_temporary, r.object_type,
        r.object_identity, r.address_names, r.address_args;
    END LOOP;
END; $$
;
CREATE TABLE pagg_tab_p1 PARTITION OF pagg_tab FOR VALUES IN ('0000', '0001', '0002', '0003')
;
CREATE FUNCTION isnlt(ean13, isbn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE "public"."pht2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY HASH ("c")
;
ALTER DATABASE "ecpg1_regression" SET lc_time TO 'C'
;
CREATE TABLE prt1_adv (a int, b int, c varchar) PARTITION BY RANGE (a)
;
comment on table schema1.table_name is 'ss'
;
ANALYZE distinct_tbl
;
DROP TABLE temporal_mltrng
;
CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=27783 host=/tmp/WPXzmhPeul dbname=postgres' PUBLICATION pub5
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_match_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                   silent boolean DEFAULT false)
RETURNS boolean
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_match_tz'
;
----------
[DROP_LIBRARY] -- extension
DROP EXTENSION test_extension_exists
;
----------
[ADMIN_TABLE] ANALYZE public.shoelace_log
;
----------
[CREATE_TABLE] create table tab_batch_sharded (id int, data text) partition by hash(id)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bar_int4_text(int4) RETURNS text LANGUAGE SQL AS
$$ SELECT ('bar'::text || $1::text); $$
;
----------
[CREATE_CATALOG] create database regress_pg_dump_test
;
----------
[CREATE_TYPE] create domain dcomptype as comptype check ((value).cf1 > 0)
;
----------
[DROP_TABLE] DROP TABLE inhg
;
----------
[ADD_INDEX] CREATE INDEX "comment_test_index" ON "public"."comment_test" USING "btree" ("indexed_col")
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_user_mapping
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_bgwriter AS
    SELECT
        pg_stat_get_bgwriter_buf_written_clean() AS buffers_clean,
        pg_stat_get_bgwriter_maxwritten_clean() AS maxwritten_clean,
        pg_stat_get_buf_alloc() AS buffers_alloc,
        pg_stat_get_bgwriter_stat_reset_time() AS stats_reset
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION dummy_sub CONNECTION '' PUBLICATION foo WITH (connect = false, slot_name = NONE)
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub RENAME TO tap_sub_renamed
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE regress_pg_dump_schema.test_agg(int2)
(SFUNC = int2_sum, STYPE = int8)
;
----------
[CREATE_TABLE] CREATE TABLE pgss_a (id integer PRIMARY KEY)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION float8_dist(double precision, double precision) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE part3 PARTITION OF range_parted2 FOR VALUES FROM (30) TO (40)
;
----------
[ALTER_TYPE] ---
--- ALTER_TYPE_ENUM
---

ALTER TYPE enum_test ADD VALUE 'zzz' AFTER 'baz'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE part_c_100_200 ADD COLUMN b bigint
;
----------
[DROP_POLICY] -- conversion
DROP CONVERSION test_conversion_exists
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=26577 host=/tmp/Lnf6IHniY8 dbname=postgres' PUBLICATION pub_all
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD FOREIGN TABLE ft1
;
----------
[CREATE_TABLE] CREATE TABLE unique_tbl (i int UNIQUE DEFERRABLE, t text)
;
----------
[CREATE_POLICY] CREATE RULE "rtest_emp_ins" AS
    ON INSERT TO "public"."rtest_emp" DO  INSERT INTO "public"."rtest_emplog" ("ename", "who", "action", "newsal", "oldsal")
  VALUES ("new"."ename", CURRENT_USER, 'hired'::"bpchar", "new"."salary", '$0.00'::"money")
;
----------
[DROP_TABLE] DROP TABLE ruletest_t3
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table fkpart1.fk_part detach partition fkpart1.fk_part_1
;
----------
[DROP_VIEW] DROP VIEW atestv0
;
----------
[CREATE_VIEW(SELECT)] create or replace view my_locks as
select case when c.relname like 'pg_toast%' then 'pg_toast' else c.relname end, max(mode::lockmodes) as max_lockmode
from pg_locks l join pg_class c on l.relation = c.oid
where virtualtransaction = (
        select virtualtransaction
        from pg_locks
        where transactionid = txid_current()::integer)
and locktype = 'relation'
and relnamespace != (select oid from pg_namespace where nspname = 'pg_catalog')
and c.relname != 'my_locks'
group by c.relname
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub2
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION admin_sub SET (run_as_owner = false)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- check that, after being detached, attinhcount/coninhcount is dropped to 0 and
-- attislocal/conislocal is set to true
ALTER TABLE list_parted2 DETACH PARTITION part_3_4
;
----------
[CREATE_PROG_OBJ] -- EAN13 vs other types:
CREATE FUNCTION btean13cmp(ean13, isbn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE fk_notpartitioned_pk (fdrop1 int, a int, fdrop2 int, b int,
  PRIMARY KEY (a, b))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."dept" (
    "dname" "name",
    "mgrname" "text"
)
;
----------
[CREATE_PROG_OBJ] -- exercise record typmod remapping between backends
CREATE FUNCTION make_record(n int)
  RETURNS RECORD LANGUAGE plpgsql PARALLEL SAFE AS
$$
BEGIN
  RETURN CASE n
           WHEN 1 THEN ROW(1)
           WHEN 2 THEN ROW(1, 2)
           WHEN 3 THEN ROW(1, 2, 3)
           WHEN 4 THEN ROW(1, 2, 3, 4)
           ELSE ROW(1, 2, 3, 4, 5)
         END;
END;
$$
;
----------
[ADD_INDEX] CREATE INDEX "spgist_domain_idx" ON "public"."spgist_domain_tbl" USING "spgist" ("f1")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."part2_self_fk" (
    "id" bigint CONSTRAINT "parted_self_fk_id_not_null" NOT NULL,
    "id_abc" bigint
)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
----------
[CREATE_TABLE] --
-- Tables and rules for the instead nothing test
--
create table rtest_nothn1 (a int4, b text)
;
----------
[DROP_TYPE] DROP TYPE collate_dep_test2
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION event_trigger_report_dropped()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r record;
BEGIN
    FOR r IN SELECT * from pg_event_trigger_dropped_objects()
    LOOP
    IF NOT r.normal AND NOT r.original THEN
        CONTINUE;
    END IF;
    RAISE NOTICE 'NORMAL: orig=% normal=% istemp=% type=% identity=% name=% args=%',
        r.original, r.normal, r.is_temporary, r.object_type,
        r.object_identity, r.address_names, r.address_args;
    END LOOP;
END; $$
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_p1 PARTITION OF pagg_tab FOR VALUES IN ('0000', '0001', '0002', '0003')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ean13, isbn)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht2" (
    "a" integer,
    "b" integer,
    "c" "text"
)
PARTITION BY HASH ("c")
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg1_regression" SET lc_time TO 'C'
;
----------
[CREATE_TABLE] CREATE TABLE prt1_adv (a int, b int, c varchar) PARTITION BY RANGE (a)
;
----------
[COMMENT_TABLE] comment on table schema1.table_name is 'ss'
;
----------
[ADMIN_TABLE] ANALYZE distinct_tbl
;
----------
[DROP_TABLE] DROP TABLE temporal_mltrng
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_2 CONNECTION 'port=27783 host=/tmp/WPXzmhPeul dbname=postgres' PUBLICATION pub5
;
