ALTER INDEX pg_toast.pg_toast_28633_index RENAME TO reind_con_toast_idx
;
-- check merging of outer CTE with CTE in a rule action
CREATE TEMP TABLE bug6051 AS
  select i from generate_series(1,3) as t(i)
;
alter table atacc1 alter "........pg.dropped.1........" set storage plain
;
ALTER TABLE pg_temp_12.pg_temp_26162_2 ADD COLUMN newdata pg_temp_12.pg_temp_26162
;
CREATE DATABASE db2
;
-- test compression with inheritance, error
CREATE TABLE cminh() INHERITS(cmdata, cmdata1)
;
CREATE TABLE split_alter_table.ref_table (id integer PRIMARY KEY)
;
drop table t_gin_test_tbl
;
--
-- Clean up.  Many of these table names will be re-used if the user is
-- trying to run any platform-specific collation tests later, so we
-- must get rid of them.
--
DROP SCHEMA collate_tests CASCADE
;
CREATE TABLE clstr_expression(id serial primary key, a int, b text COLLATE "C")
;
COMMENT ON CONSTRAINT the_constraint ON DOMAIN constraint_comments_dom IS 'no, another comment'
;
CREATE INDEX idx_macaddr ON test_macaddr USING gin (i)
;
DROP TABLE has_volatile
;
ALTER INDEX "fkpart4"."droppk_pkey" ATTACH PARTITION "fkpart4"."droppk2_pkey"
;
-- ON DELETE SET DEFAULT column_list
ALTER TABLE fk_partitioned_fk DROP CONSTRAINT fk_partitioned_fk_a_b_fkey
;
CREATE VIEW atest12v AS
  SELECT * FROM atest12 WHERE b <<< 5
;
ANALYZE fkpart3.pk3
;
ALTER TABLE ONLY "public"."part3_self_fk" ATTACH PARTITION "public"."part33_self_fk" FOR VALUES FROM ('30') TO ('40')
;
create index on idxpart (a collate "C")
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE INDEX brin_summarize_multi_idx ON brin_summarize_multi USING brin (value) WITH (pages_per_range=2)
;
CREATE TABLESPACE ts_corrupt LOCATION '/src/src/bin/pg_checksums/tmp_check/t_002_actions_node_checksum_data/ts_corrupt_dir'
;
ALTER TABLE testpub_parted ATTACH PARTITION testpub_parted1 FOR VALUES IN (1)
;
-- security definer functions override the user who queued the trigger
alter function whoami() security definer
;
ALTER OPERATOR FAMILY gist_numeric_ops USING gist ADD
	FUNCTION	11  (numeric, numeric) gbt_numeric_sortsupport (internal)
;
CREATE TYPE dump_test.planets
					   AS ENUM ( 'venus', 'earth', 'mars' )
;
CREATE PUBLICATION test_pub1 FOR ALL TABLES
;
alter database abc set datestyle to 'iso, mdy'
;
drop table insertconflict
;
TRUNCATE split_cov.child_table, split_cov.parent_table RESTART IDENTITY CASCADE
;
CREATE TABLE enumtmp (a rainbow)
;
ALTER TABLE ONLY "regress_indexing"."pk"
    ADD CONSTRAINT "pk_pkey" PRIMARY KEY ("a")
;
CREATE FUNCTION "public"."constant_refcursor"() RETURNS "refcursor"
    LANGUAGE "plpgsql"
    AS $$
declare
    rc constant refcursor := 'my_cursor_name';
begin
    open rc for select a from rc_test;
    return rc;
end
$$
;
CREATE FUNCTION alter_table_under_transition_tables_upd_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
BEGIN
  RAISE WARNING 'old table = %, new table = %',
                  (SELECT string_agg(id || '=' || name, ',') FROM d),
                  (SELECT string_agg(id || '=' || name, ',') FROM i);
  RAISE NOTICE 'one = %', (SELECT 1 FROM alter_table_under_transition_tables LIMIT 1);
  RETURN NULL;
END;
$$
;
DROP TABLE serialTest1, serialTest2
;
--
-- Verify behavior of INSERT ... ON CONFLICT DO UPDATE ... with
-- transition tables.
--

create table my_table (a int primary key, b text)
;
DROP TABLE horizons_tst
;
CREATE FUNCTION dblink (text, text, boolean)
RETURNS setof record
AS '$libdir/dblink','dblink_record'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
ALTER DATABASE "regression" SET timezone_abbreviations TO 'Default'
;
ALTER FUNCTION alt_func1(int) RENAME TO alt_func2
;
ALTER TABLE fk ADD FOREIGN KEY (a) REFERENCES pk ON UPDATE RESTRICT ON DELETE RESTRICT
;
drop table list_parted_tbl
;
TRUNCATE temporal_partitioned_mltrng, temporal_partitioned_fk_mltrng2mltrng
;
create table d4_primary1 partition of d4_primary for values in (1)
;
/* contrib/cube/cube--1.2.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Create the user-defined type for N-dimensional boxes

CREATE FUNCTION cube_in(cstring)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create function testpolym(a int, inout a int) returns int as $$ select 1;$$ language sql
;
CREATE TABLE FKTABLE ( ftest1 int, ftest2 int, ftest3 int, CONSTRAINT constrname FOREIGN KEY(ftest1, ftest2)
                       REFERENCES PKTABLE MATCH FULL ON DELETE SET NULL ON UPDATE SET NULL)
;
ANALYZE public.test_tablesample
;
CREATE TYPE ismn13 (
	INPUT = ismn13_in,
	OUTPUT = ean13_out,
	LIKE = pg_catalog.int8
)
;
CREATE TEMP TABLE duplicates (a INT NOT NULL)
;
------------------------------------------------------------------------------------------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28633_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE|SELECT] -- check merging of outer CTE with CTE in a rule action
CREATE TEMP TABLE bug6051 AS
  select i from generate_series(1,3) as t(i)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter "........pg.dropped.1........" set storage plain
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26162_2 ADD COLUMN newdata pg_temp_12.pg_temp_26162
;
----------
[CREATE_CATALOG] CREATE DATABASE db2
;
----------
[CREATE_TABLE] -- test compression with inheritance, error
CREATE TABLE cminh() INHERITS(cmdata, cmdata1)
;
----------
[CREATE_TABLE] CREATE TABLE split_alter_table.ref_table (id integer PRIMARY KEY)
;
----------
[DROP_TABLE] drop table t_gin_test_tbl
;
----------
[DROP_SCHEMA] --
-- Clean up.  Many of these table names will be re-used if the user is
-- trying to run any platform-specific collation tests later, so we
-- must get rid of them.
--
DROP SCHEMA collate_tests CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE clstr_expression(id serial primary key, a int, b text COLLATE "C")
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT the_constraint ON DOMAIN constraint_comments_dom IS 'no, another comment'
;
----------
[ADD_INDEX] CREATE INDEX idx_macaddr ON test_macaddr USING gin (i)
;
----------
[DROP_TABLE] DROP TABLE has_volatile
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart4"."droppk_pkey" ATTACH PARTITION "fkpart4"."droppk2_pkey"
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- ON DELETE SET DEFAULT column_list
ALTER TABLE fk_partitioned_fk DROP CONSTRAINT fk_partitioned_fk_a_b_fkey
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW atest12v AS
  SELECT * FROM atest12 WHERE b <<< 5
;
----------
[ADMIN_TABLE] ANALYZE fkpart3.pk3
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."part3_self_fk" ATTACH PARTITION "public"."part33_self_fk" FOR VALUES FROM ('30') TO ('40')
;
----------
[ADD_INDEX] create index on idxpart (a collate "C")
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = isbn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ADD_INDEX] CREATE INDEX brin_summarize_multi_idx ON brin_summarize_multi USING brin (value) WITH (pages_per_range=2)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts_corrupt LOCATION '/src/src/bin/pg_checksums/tmp_check/t_002_actions_node_checksum_data/ts_corrupt_dir'
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE testpub_parted ATTACH PARTITION testpub_parted1 FOR VALUES IN (1)
;
----------
[ALTER_PROG_OBJ] -- security definer functions override the user who queued the trigger
alter function whoami() security definer
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_numeric_ops USING gist ADD
	FUNCTION	11  (numeric, numeric) gbt_numeric_sortsupport (internal)
;
----------
[CREATE_TYPE] CREATE TYPE dump_test.planets
					   AS ENUM ( 'venus', 'earth', 'mars' )
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION test_pub1 FOR ALL TABLES
;
----------
[ALTER_CATALOG] alter database abc set datestyle to 'iso, mdy'
;
----------
[DROP_TABLE] drop table insertconflict
;
----------
[TRUNCATE_TABLE] TRUNCATE split_cov.child_table, split_cov.parent_table RESTART IDENTITY CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE enumtmp (a rainbow)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "regress_indexing"."pk"
    ADD CONSTRAINT "pk_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."constant_refcursor"() RETURNS "refcursor"
    LANGUAGE "plpgsql"
    AS $$
declare
    rc constant refcursor := 'my_cursor_name';
begin
    open rc for select a from rc_test;
    return rc;
end
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION alter_table_under_transition_tables_upd_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
BEGIN
  RAISE WARNING 'old table = %, new table = %',
                  (SELECT string_agg(id || '=' || name, ',') FROM d),
                  (SELECT string_agg(id || '=' || name, ',') FROM i);
  RAISE NOTICE 'one = %', (SELECT 1 FROM alter_table_under_transition_tables LIMIT 1);
  RETURN NULL;
END;
$$
;
----------
[DROP_TABLE] DROP TABLE serialTest1, serialTest2
;
----------
[CREATE_TABLE] --
-- Verify behavior of INSERT ... ON CONFLICT DO UPDATE ... with
-- transition tables.
--

create table my_table (a int primary key, b text)
;
----------
[DROP_TABLE] DROP TABLE horizons_tst
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink (text, text, boolean)
RETURNS setof record
AS '$libdir/dblink','dblink_record'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET timezone_abbreviations TO 'Default'
;
----------
[RENAME_PROG_OBJ] ALTER FUNCTION alt_func1(int) RENAME TO alt_func2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk ADD FOREIGN KEY (a) REFERENCES pk ON UPDATE RESTRICT ON DELETE RESTRICT
;
----------
[DROP_TABLE] drop table list_parted_tbl
;
----------
[TRUNCATE_TABLE] TRUNCATE temporal_partitioned_mltrng, temporal_partitioned_fk_mltrng2mltrng
;
----------
[CREATE_TABLE] create table d4_primary1 partition of d4_primary for values in (1)
;
----------
[CREATE_PROG_OBJ] /* contrib/cube/cube--1.2.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Create the user-defined type for N-dimensional boxes

CREATE FUNCTION cube_in(cstring)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function testpolym(a int, inout a int) returns int as $$ select 1;$$ language sql
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE ( ftest1 int, ftest2 int, ftest3 int, CONSTRAINT constrname FOREIGN KEY(ftest1, ftest2)
                       REFERENCES PKTABLE MATCH FULL ON DELETE SET NULL ON UPDATE SET NULL)
;
----------
[ADMIN_TABLE] ANALYZE public.test_tablesample
;
----------
[CREATE_TYPE] CREATE TYPE ismn13 (
	INPUT = ismn13_in,
	OUTPUT = ean13_out,
	LIKE = pg_catalog.int8
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE duplicates (a INT NOT NULL)
;
