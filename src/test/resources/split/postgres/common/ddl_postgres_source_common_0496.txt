COMMENT ON CONSTRAINT constr_parent2_a_not_null ON constr_parent2 IS 'this constraint is invalid'
;
drop view rw_view1
;
-- Partition-less partitioned table
-- (the bug we are checking for appeared only if table had partitions before)

DROP TABLE pa_targetp
;
ALTER TABLE pg_toast.pg_toast_30846 RENAME TO reind_con_toast
;
CREATE TABLE fk_partitioned_pk_3_1 PARTITION OF fk_partitioned_pk_3 FOR VALUES FROM (2000) TO (2100)
;
CREATE COLLATION testx (provider = icu, locale = 'nonsense-nowhere')
;
ANALYZE pg_temp_2.pg_temp_28072
;
CREATE FUNCTION "public"."ret_query2"("lim" integer) RETURNS SETOF "public"."record_type"
    LANGUAGE "plpgsql"
    AS $$
begin
    return query select fipshash(s.x::text), s.x, s.x > 0
                 from generate_series(-8, lim) s (x) where s.x % 2 = 0;
end;
$$
;
CREATE TABLE errtst_part_2(secret1 text, secret2 text, a text, c text, b text NOT NULL)
;
create table parallel_write as execute prep_stmt
;
ALTER TABLE pg_temp_4.pg_temp_37962_2 ADD COLUMN newdata pg_temp_4.pg_temp_37962
;
CREATE PROCEDURE ptestx() LANGUAGE SQL STRICT AS $$ INSERT INTO cp_test VALUES (1, 'a') $$
;
create function qqq_trig() returns trigger as $$
begin
if tg_op in ('INSERT', 'UPDATE') then
    raise notice '% %', tg_op, new.id;
    return new;
else
    raise notice '% %', tg_op, old.id;
    return old;
end if;
end
$$ language plpgsql
;
/* contrib/fuzzystrmatch/fuzzystrmatch--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION levenshtein (text,text) RETURNS int
AS '$libdir/fuzzystrmatch','levenshtein'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create table mc2p4 partition of mc2p for values from (2, 1) to (2, maxvalue)
;
/* contrib/intarray/intarray--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Remove <@ from the GiST opclasses, as it's not usefully indexable
-- due to mishandling of empty arrays.  (It's OK in GIN.)

ALTER OPERATOR FAMILY gist__int_ops USING gist
DROP OPERATOR 8 (_int4, _int4)
;
create table agg_hash_2 as
select * from
  (values (100), (300), (500)) as r(a),
  lateral (
    select (g/2)::numeric as c1,
           array_agg(g::numeric) as c2,
	   count(*) as c3
    from agg_data_2k
    where g < r.a
    group by g/2) as s
;
ANALYZE pg_temp_11.pg_temp_26215_2
;
CREATE FUNCTION gbt_int4_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE inh_child (i int) INHERITS (inh_parent)
;
CREATE FUNCTION gbt_oid_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE PROCEDURE test_ambiguous_procname(text) as $$ begin end; $$ language plpgsql
;
CREATE TABLE concur_clustered(i int)
;
ALTER TABLE ONLY "public"."persons2"
    ADD CONSTRAINT "persons2_name_key" UNIQUE ("name")
;
create table tht_p3 partition of tht for values with (modulus 3, remainder 2)
;
create table t as select now from (select now(), pg_sleep(1)) f
;
VACUUM FULL pg_am
;
CREATE TEMP TABLE child2 ( ) INHERITS ( parent )
;
-- As does this
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest1, ptest2))
;
DROP FUNCTION compare_relfilenode_part
;
-- ensure create aggregate works.
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = numeric_avg_combine,
	finalfunc_modify = shareable  -- just to test a non-default setting
)
;
CREATE TABLE dump_test.test_table_nn_chld1 (
							) INHERITS (dump_test.test_table_nn)
;
CREATE PROCEDURE test_ambiguous_procname(int) as $$ begin end; $$ language plpgsql
;
CREATE TABLE target_tbl(id serial primary key, data text)
;
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean, rootdescend boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE FUNCTION isnlt(ismn, ismn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- 8
TRUNCATE tru_ftable, tru_pk_ftable CASCADE
;
-- no such type
drop type nonesuch
;
drop function sillysrf(int)
;
create table target_parted (a int, b int) partition by list (a)
;
ALTER INDEX pg_toast.pg_toast_30656_index RENAME TO reind_con_toast_idx
;
CREATE INDEX onek2_u1_prtl ON onek2 USING btree(unique1 int4_ops)
	where unique1 < 20 or unique1 > 980
;
CREATE TABLE test_pg_unicode_fast (
  t TEXT COLLATE PG_UNICODE_FAST
)
;
DROP TRIGGER trig_row_before_update ON rem1
;
CREATE TABLE fk_partitioned_pk_1 PARTITION OF fk_partitioned_pk FOR VALUES FROM (0,0) TO (1000,1000)
;
create domain d_fail as int4 PRIMARY key
;
CREATE FUNCTION hs_concat(hstore,hstore)
RETURNS hstore
AS '$libdir/hstore','hstore_concat'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE "public"."notnull_part1_2_upg" (
    "a" integer CONSTRAINT "nn2" NOT NULL,
    "b" integer
)
;
CREATE CAST (int4 AS casttesttype) WITH INOUT
;
create table dropColumnChild (c int) inherits (dropColumn)
;
------------------------------------------------------------------------------------------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT constr_parent2_a_not_null ON constr_parent2 IS 'this constraint is invalid'
;
----------
[DROP_VIEW] drop view rw_view1
;
----------
[DROP_TABLE] -- Partition-less partitioned table
-- (the bug we are checking for appeared only if table had partitions before)

DROP TABLE pa_targetp
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30846 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_pk_3_1 PARTITION OF fk_partitioned_pk_3 FOR VALUES FROM (2000) TO (2100)
;
----------
[CREATE_POLICY] CREATE COLLATION testx (provider = icu, locale = 'nonsense-nowhere')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_28072
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ret_query2"("lim" integer) RETURNS SETOF "public"."record_type"
    LANGUAGE "plpgsql"
    AS $$
begin
    return query select fipshash(s.x::text), s.x, s.x > 0
                 from generate_series(-8, lim) s (x) where s.x % 2 = 0;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE errtst_part_2(secret1 text, secret2 text, a text, c text, b text NOT NULL)
;
----------
[CREATE_TABLE|UNSAFE] create table parallel_write as execute prep_stmt
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_37962_2 ADD COLUMN newdata pg_temp_4.pg_temp_37962
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptestx() LANGUAGE SQL STRICT AS $$ INSERT INTO cp_test VALUES (1, 'a') $$
;
----------
[CREATE_PROG_OBJ] create function qqq_trig() returns trigger as $$
begin
if tg_op in ('INSERT', 'UPDATE') then
    raise notice '% %', tg_op, new.id;
    return new;
else
    raise notice '% %', tg_op, old.id;
    return old;
end if;
end
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] /* contrib/fuzzystrmatch/fuzzystrmatch--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION levenshtein (text,text) RETURNS int
AS '$libdir/fuzzystrmatch','levenshtein'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table mc2p4 partition of mc2p for values from (2, 1) to (2, maxvalue)
;
----------
[ALTER_PROG_OBJ] /* contrib/intarray/intarray--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Remove <@ from the GiST opclasses, as it's not usefully indexable
-- due to mishandling of empty arrays.  (It's OK in GIN.)

ALTER OPERATOR FAMILY gist__int_ops USING gist
DROP OPERATOR 8 (_int4, _int4)
;
----------
[CREATE_TABLE|SELECT] create table agg_hash_2 as
select * from
  (values (100), (300), (500)) as r(a),
  lateral (
    select (g/2)::numeric as c1,
           array_agg(g::numeric) as c2,
	   count(*) as c3
    from agg_data_2k
    where g < r.a
    group by g/2) as s
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26215_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int4_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE inh_child (i int) INHERITS (inh_parent)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_oid_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_ambiguous_procname(text) as $$ begin end; $$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE concur_clustered(i int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."persons2"
    ADD CONSTRAINT "persons2_name_key" UNIQUE ("name")
;
----------
[CREATE_TABLE] create table tht_p3 partition of tht for values with (modulus 3, remainder 2)
;
----------
[CREATE_TABLE|SELECT] create table t as select now from (select now(), pg_sleep(1)) f
;
----------
[ADMIN_TABLE] VACUUM FULL pg_am
;
----------
[CREATE_TABLE] CREATE TEMP TABLE child2 ( ) INHERITS ( parent )
;
----------
[CREATE_TABLE] -- As does this
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable(ptest1, ptest2))
;
----------
[DROP_PROG_OBJ] DROP FUNCTION compare_relfilenode_part
;
----------
[CREATE_PROG_OBJ] -- ensure create aggregate works.
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = numeric_avg_combine,
	finalfunc_modify = shareable  -- just to test a non-default setting
)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_nn_chld1 (
							) INHERITS (dump_test.test_table_nn)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_ambiguous_procname(int) as $$ begin end; $$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE target_tbl(id serial primary key, data text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean, rootdescend boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ismn, ismn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[TRUNCATE_TABLE] -- 8
TRUNCATE tru_ftable, tru_pk_ftable CASCADE
;
----------
[DROP_TYPE] -- no such type
drop type nonesuch
;
----------
[DROP_PROG_OBJ] drop function sillysrf(int)
;
----------
[CREATE_TABLE] create table target_parted (a int, b int) partition by list (a)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30656_index RENAME TO reind_con_toast_idx
;
----------
[ADD_INDEX] CREATE INDEX onek2_u1_prtl ON onek2 USING btree(unique1 int4_ops)
	where unique1 < 20 or unique1 > 980
;
----------
[CREATE_TABLE] CREATE TABLE test_pg_unicode_fast (
  t TEXT COLLATE PG_UNICODE_FAST
)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_before_update ON rem1
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_pk_1 PARTITION OF fk_partitioned_pk FOR VALUES FROM (0,0) TO (1000,1000)
;
----------
[CREATE_TYPE] create domain d_fail as int4 PRIMARY key
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hs_concat(hstore,hstore)
RETURNS hstore
AS '$libdir/hstore','hstore_concat'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_part1_2_upg" (
    "a" integer CONSTRAINT "nn2" NOT NULL,
    "b" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (int4 AS casttesttype) WITH INOUT
;
----------
[CREATE_TABLE] create table dropColumnChild (c int) inherits (dropColumn)
;
