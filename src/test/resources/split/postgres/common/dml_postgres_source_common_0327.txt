-- insert a few rows in the target table (odd numbered tid)
INSERT INTO pa_target SELECT id, id * 100, 'initial' FROM generate_series(1,15,2) AS id
;
WITH funcdescs AS ( SELECT p.oid as p_oid, o.oid as o_oid, oprname FROM pg_proc p JOIN pg_operator o ON oprcode = p.oid ) INSERT INTO pg_description   SELECT p_oid, 'pg_proc'::regclass, 0,     'implementation of ' || oprname || ' operator'   FROM funcdescs   WHERE NOT EXISTS (SELECT 1 FROM pg_description    WHERE objoid = p_oid AND classoid = 'pg_proc'::regclass)   AND NOT EXISTS (SELECT 1 FROM pg_description    WHERE objoid = o_oid AND classoid = 'pg_operator'::regclass         AND description LIKE 'deprecated%')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."constr_parent2"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '19797', relminmxid = '3'
WHERE oid = '"public"."alpha_pos_p3"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (2, 922)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."persons3"'::pg_catalog.regclass
;
INSERT INTO pg_temp_4.pg_temp_26914_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_4.pg_temp_26914 AS newdata FROM public.sro_index_mv mv FULL JOIN pg_temp_4.pg_temp_26914 newdata ON (newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO num_input_test(n1) VALUES ('123._456')
;
UPDATE ft1 SET c2 = -c2 WHERE c1 = 1
;
insert into tttest values (2, 2, null)
;
-- check that the correct row is shown when constraint check_b fails after
-- "(1, 2)" is routed to mlparted11 (actually "(1, 4)" would be shown due
-- to the BR trigger mlparted11_trig_fn)
insert into mlparted values (1, 2)
;
UPDATE pg_toast.pg_toast_1213 SET chunk_id = 1
;
INSERT INTO bar VALUES (3, 1)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."idxpart2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9107', relminmxid = '1'
WHERE oid = '"public"."brin_summarize_bloom"'::pg_catalog.regclass
;
insert into parent values (1, 2, 3)
;
INSERT INTO public.tplain VALUES ('9', 409)
;
insert into family ( name , age ) values ( 'Child 3' , 9 )
;
INSERT INTO test_inet VALUES
	( '1.2.3.4/16' ),
	( '1.2.4.4/16' ),
	( '1.2.5.4/16' ),
	( '1.2.6.4/16' ),
	( '1.2.7.4/16' ),
	( '1.2.8.4/16' )
;
INSERT INTO public.tht VALUES ('5', 425)
;
INSERT INTO num_exp_div VALUES (0,6,'0')
;
INSERT INTO public.ths_p2 VALUES (7, 877)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."fk_partitioned_fk_2_1"'::pg_catalog.regclass
;
INSERT INTO num_typemod_test (millionths) VALUES (0.0009995)
;
INSERT INTO public.ths_p1 VALUES (8, 528)
;
update voo set f1 = f1 + 1 where f2 = 'zoo2' RETURNING *, f1*2
;
update PSlot set backlink = 'WS.001.1b' where slotname = 'PS.base.a2'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17514', relminmxid = '3'
WHERE oid = '39935'
;
INSERT INTO public.tht VALUES ('5', 675)
;
INSERT INTO foo VALUES (4)
  RETURNING old.tableoid::regclass, old.ctid, old.*,
            new.tableoid::regclass, new.ctid, new.*, *
;
UPDATE update_test SET (a,b) = ROW(v.*) FROM (VALUES(21, 100)) AS v(i, j)
  WHERE update_test.a = v.i
;
UPDATE bank_account SET balance = -11 WHERE id = 'X'
;
INSERT INTO INET_TBL (c, i) VALUES ('10.1', '10.1.2.3/16')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7369', relminmxid = '1'
WHERE oid = '"public"."itest1"'::pg_catalog.regclass
;
insert into cc values('("(4,5)",6)')
;
INSERT INTO public.ths_p3 VALUES (1, 111)
;
INSERT INTO test_visibility VALUES ('first insert')
;
INSERT INTO tab_rowfilter_parent_sync(a) VALUES(14), (16)
;
insert into parted_constr_ancestor values (3, 'aasvogel')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2900', relminmxid = '1'
WHERE oid = '"public"."mlparted11"'::pg_catalog.regclass
;
-- fail
UPDATE list_default set a = 'a' WHERE a = 'd'
;
INSERT INTO public.ths_p1 VALUES (4, 104)
;
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"generated_virtual_tests"."gtest28a"'::pg_catalog.regclass
  AND attname = v.dname
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15424', relminmxid = '3'
WHERE oid = '"public"."domain_test"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"regress_rls_schema"."rls_tbl_force"'::pg_catalog.regclass
;
insert into PLine values ('PL.029', '-502', 'Fax first floor', 'PS.first.ta1')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8716', relminmxid = '1'
WHERE oid = '"public"."shoelace_arrive"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (6, 546)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '"public"."plt1_e_p1"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_m_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] -- insert a few rows in the target table (odd numbered tid)
INSERT INTO pa_target SELECT id, id * 100, 'initial' FROM generate_series(1,15,2) AS id
;
----------
[INSERT|SELECT] WITH funcdescs AS ( SELECT p.oid as p_oid, o.oid as o_oid, oprname FROM pg_proc p JOIN pg_operator o ON oprcode = p.oid ) INSERT INTO pg_description   SELECT p_oid, 'pg_proc'::regclass, 0,     'implementation of ' || oprname || ' operator'   FROM funcdescs   WHERE NOT EXISTS (SELECT 1 FROM pg_description    WHERE objoid = p_oid AND classoid = 'pg_proc'::regclass)   AND NOT EXISTS (SELECT 1 FROM pg_description    WHERE objoid = o_oid AND classoid = 'pg_operator'::regclass         AND description LIKE 'deprecated%')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."constr_parent2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '19797', relminmxid = '3'
WHERE oid = '"public"."alpha_pos_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 922)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."persons3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_4.pg_temp_26914_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_4.pg_temp_26914 AS newdata FROM public.sro_index_mv mv FULL JOIN pg_temp_4.pg_temp_26914 newdata ON (newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT|SELECT] INSERT INTO num_input_test(n1) VALUES ('123._456')
;
----------
[UPDATE] UPDATE ft1 SET c2 = -c2 WHERE c1 = 1
;
----------
[INSERT] insert into tttest values (2, 2, null)
;
----------
[INSERT] -- check that the correct row is shown when constraint check_b fails after
-- "(1, 2)" is routed to mlparted11 (actually "(1, 4)" would be shown due
-- to the BR trigger mlparted11_trig_fn)
insert into mlparted values (1, 2)
;
----------
[UPDATE] UPDATE pg_toast.pg_toast_1213 SET chunk_id = 1
;
----------
[INSERT] INSERT INTO bar VALUES (3, 1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."idxpart2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9107', relminmxid = '1'
WHERE oid = '"public"."brin_summarize_bloom"'::pg_catalog.regclass
;
----------
[INSERT] insert into parent values (1, 2, 3)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 409)
;
----------
[INSERT|SELECT] insert into family ( name , age ) values ( 'Child 3' , 9 )
;
----------
[INSERT] INSERT INTO test_inet VALUES
	( '1.2.3.4/16' ),
	( '1.2.4.4/16' ),
	( '1.2.5.4/16' ),
	( '1.2.6.4/16' ),
	( '1.2.7.4/16' ),
	( '1.2.8.4/16' )
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 425)
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (0,6,'0')
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 877)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."fk_partitioned_fk_2_1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO num_typemod_test (millionths) VALUES (0.0009995)
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 528)
;
----------
[UPDATE] update voo set f1 = f1 + 1 where f2 = 'zoo2' RETURNING *, f1*2
;
----------
[UPDATE] update PSlot set backlink = 'WS.001.1b' where slotname = 'PS.base.a2'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17514', relminmxid = '3'
WHERE oid = '39935'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('5', 675)
;
----------
[INSERT] INSERT INTO foo VALUES (4)
  RETURNING old.tableoid::regclass, old.ctid, old.*,
            new.tableoid::regclass, new.ctid, new.*, *
;
----------
[UPDATE|SELECT] UPDATE update_test SET (a,b) = ROW(v.*) FROM (VALUES(21, 100)) AS v(i, j)
  WHERE update_test.a = v.i
;
----------
[UPDATE] UPDATE bank_account SET balance = -11 WHERE id = 'X'
;
----------
[INSERT|SELECT] INSERT INTO INET_TBL (c, i) VALUES ('10.1', '10.1.2.3/16')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7369', relminmxid = '1'
WHERE oid = '"public"."itest1"'::pg_catalog.regclass
;
----------
[INSERT] insert into cc values('("(4,5)",6)')
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (1, 111)
;
----------
[INSERT] INSERT INTO test_visibility VALUES ('first insert')
;
----------
[INSERT|SELECT] INSERT INTO tab_rowfilter_parent_sync(a) VALUES(14), (16)
;
----------
[INSERT] insert into parted_constr_ancestor values (3, 'aasvogel')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2900', relminmxid = '1'
WHERE oid = '"public"."mlparted11"'::pg_catalog.regclass
;
----------
[UPDATE] -- fail
UPDATE list_default set a = 'a' WHERE a = 'd'
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 104)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"generated_virtual_tests"."gtest28a"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15424', relminmxid = '3'
WHERE oid = '"public"."domain_test"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6763', relminmxid = '1'
WHERE oid = '"regress_rls_schema"."rls_tbl_force"'::pg_catalog.regclass
;
----------
[INSERT] insert into PLine values ('PL.029', '-502', 'Fax first floor', 'PS.first.ta1')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8716', relminmxid = '1'
WHERE oid = '"public"."shoelace_arrive"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 546)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '"public"."plt1_e_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pagg_tab_m_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
