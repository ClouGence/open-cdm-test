ALTER SEQUENCE regtest_seq INCREMENT BY 10 START WITH 1000
;
CREATE POLICY p4 ON dump_test.test_table FOR UPDATE
						   TO regress_dump_test_role USING (true) WITH CHECK (true)
;
TRUNCATE gtest1
;
ALTER INDEX pg_toast.pg_toast_30753_index RENAME TO reind_con_toast_idx
;
-- fails
alter table anothertab alter column atcol1 drop default
;
-- check validation when attaching hash partitions

-- Use hand-rolled hash functions and operator class to get predictable result
-- on different machines. part_test_int4_ops is defined in test_setup.sql.

-- check that the new partition won't overlap with an existing partition
CREATE TABLE hash_parted (
	a int,
	b int
) PARTITION BY HASH (a part_test_int4_ops)
;
ALTER FOREIGN TABLE ft1 DROP CONSTRAINT IF EXISTS no_const
;
CREATE TEMP TABLE FKTABLE (ftest1 int, ftest2 inet)
;
-- Table publication that does not include the parent table
CREATE PUBLICATION pub FOR TABLE sch2.tbl1_part1 WITH (PUBLISH_VIA_PARTITION_ROOT=0)
;
drop table wcowrtest, sometable
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE selinto_schema.tbl_withdata4 (a) AS
  EXECUTE data_sel WITH DATA
;
/*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
DROP TABLE pg_temp_24.pg_temp_26683_2, pg_temp_24.pg_temp_26683
;
CREATE DATABASE db_sql_ascii ENCODING "sql_ascii" TEMPLATE template0
;
--
-- adding new values
--

CREATE TYPE planets AS ENUM ( 'venus', 'earth', 'mars' )
;
ALTER INDEX pg_toast.pg_toast_30598_index RENAME TO reind_con_toast_idx
;
-- ===================================================================
-- test handling of collations
-- ===================================================================
create table loct3 (f1 text collate "C" unique, f2 text, f3 varchar(10) unique)
;
ALTER INDEX pg_toast.pg_toast_30932_index RENAME TO reind_con_toast_idx
;
DROP TABLE plt2_adv_p1_null
;
ALTER TABLE ONLY notnull_tbl5 ALTER b DROP NOT NULL
;
ALTER TABLE ONLY "public"."pagg_tab_m" ATTACH PARTITION "public"."pagg_tab_m_p2" FOR VALUES FROM (12, 12) TO (22, 22)
;
DROP OPERATOR IF EXISTS @#@ (int, int)
;
CREATE TEMP TABLE kd_point_tbl_ord_idx1 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM kd_point_tbl
;
CREATE TABLESPACE tblspc2 LOCATION '/tmp/BquxA6nmQA/tempdir/tbl=spc2'
;
CREATE INDEX part_a_idx ON testschema.part (a) TABLESPACE regress_tblspace
;
CREATE FUNCTION cube(float8, float8) RETURNS cube
AS '$libdir/cube', 'cube_f8_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DROP TABLE IF EXISTS audit_tbls.schema_one_table_one
;
DROP TABLE _attmp_array
;
ALTER TYPE enum_test ADD VALUE 'zzz' AFTER 'baz'
;
CREATE TABLE pagg_tab2_p3 PARTITION OF pagg_tab2 FOR VALUES FROM (20) TO (30)
;
ALTER TABLE temporal_rng3 DROP CONSTRAINT temporal_rng3_pk
;
create table alttype_cluster (a int)
;
DROP TABLE pg_temp_13.pg_temp_26413_2, pg_temp_13.pg_temp_26413
;
--
-- test PERFORM
--

create table perform_test (
	a	INT,
	b	INT
)
;
VACUUM (FULL) pg_catalog.pg_subscription_rel
;
-- Test mutabilily of query functions
CREATE TABLE test_jsonb_mutability(js jsonb, b int)
;
CREATE VIEW pg_ident_file_mappings AS
   SELECT * FROM pg_ident_file_mappings() AS A
;
CREATE TRIGGER gtest12_03 BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func()
;
COMMENT ON TEXT SEARCH CONFIGURATION norwegian IS 'configuration for norwegian language'
;
-- Cleanup
drop operator class my_op_class using btree
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '1')
;
CREATE TABLE result_tbl (a int, b int, c text)
;
ANALYZE information_schema.sql_features
;
CREATE TABLE ess_table (f1 int primary key, f2 int, f3 text,
                        constraint ess_c check (f1 != f2))
;
CREATE CAST (upc AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
-- fail
create table child (b decimal(10,7)) inherits (parent)
;
CREATE UNLOGGED TABLE base_unlogged (id int)
;
COMMENT ON FUNCTION ts_debug(regconfig,text) IS
    'debug function for text search configuration'
;
DROP PUBLICATION "pub1"
;
CREATE FUNCTION isneq(issn13, issn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[ALTER_SEQUENCE] ALTER SEQUENCE regtest_seq INCREMENT BY 10 START WITH 1000
;
----------
[CREATE_POLICY] CREATE POLICY p4 ON dump_test.test_table FOR UPDATE
						   TO regress_dump_test_role USING (true) WITH CHECK (true)
;
----------
[TRUNCATE_TABLE] TRUNCATE gtest1
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30753_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails
alter table anothertab alter column atcol1 drop default
;
----------
[CREATE_TABLE] -- check validation when attaching hash partitions

-- Use hand-rolled hash functions and operator class to get predictable result
-- on different machines. part_test_int4_ops is defined in test_setup.sql.

-- check that the new partition won't overlap with an existing partition
CREATE TABLE hash_parted (
	a int,
	b int
) PARTITION BY HASH (a part_test_int4_ops)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER FOREIGN TABLE ft1 DROP CONSTRAINT IF EXISTS no_const
;
----------
[CREATE_TABLE] CREATE TEMP TABLE FKTABLE (ftest1 int, ftest2 inet)
;
----------
[CREATE_PUB_SUB] -- Table publication that does not include the parent table
CREATE PUBLICATION pub FOR TABLE sch2.tbl1_part1 WITH (PUBLISH_VIA_PARTITION_ROOT=0)
;
----------
[DROP_TABLE] drop table wcowrtest, sometable
;
----------
[CREATE_TABLE|UNSAFE] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE selinto_schema.tbl_withdata4 (a) AS
  EXECUTE data_sel WITH DATA
;
----------
[CREATE_POLICY] /*
 * text search configuration for finnish language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * finnish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY finnish_stem
	(TEMPLATE = snowball, Language = finnish , StopWords=finnish)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_24.pg_temp_26683_2, pg_temp_24.pg_temp_26683
;
----------
[CREATE_CATALOG] CREATE DATABASE db_sql_ascii ENCODING "sql_ascii" TEMPLATE template0
;
----------
[CREATE_TYPE] --
-- adding new values
--

CREATE TYPE planets AS ENUM ( 'venus', 'earth', 'mars' )
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30598_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- ===================================================================
-- test handling of collations
-- ===================================================================
create table loct3 (f1 text collate "C" unique, f2 text, f3 varchar(10) unique)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30932_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] DROP TABLE plt2_adv_p1_null
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY notnull_tbl5 ALTER b DROP NOT NULL
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_m" ATTACH PARTITION "public"."pagg_tab_m_p2" FOR VALUES FROM (12, 12) TO (22, 22)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR IF EXISTS @#@ (int, int)
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE kd_point_tbl_ord_idx1 AS
SELECT row_number() OVER (ORDER BY p <-> '0,0') n, p <-> '0,0' dist, p
FROM kd_point_tbl
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc2 LOCATION '/tmp/BquxA6nmQA/tempdir/tbl=spc2'
;
----------
[ADD_INDEX] CREATE INDEX part_a_idx ON testschema.part (a) TABLESPACE regress_tblspace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube(float8, float8) RETURNS cube
AS '$libdir/cube', 'cube_f8_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS audit_tbls.schema_one_table_one
;
----------
[DROP_TABLE] DROP TABLE _attmp_array
;
----------
[ALTER_TYPE] ALTER TYPE enum_test ADD VALUE 'zzz' AFTER 'baz'
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab2_p3 PARTITION OF pagg_tab2 FOR VALUES FROM (20) TO (30)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE temporal_rng3 DROP CONSTRAINT temporal_rng3_pk
;
----------
[CREATE_TABLE] create table alttype_cluster (a int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_26413_2, pg_temp_13.pg_temp_26413
;
----------
[CREATE_TABLE] --
-- test PERFORM
--

create table perform_test (
	a	INT,
	b	INT
)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_subscription_rel
;
----------
[CREATE_TABLE] -- Test mutabilily of query functions
CREATE TABLE test_jsonb_mutability(js jsonb, b int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_ident_file_mappings AS
   SELECT * FROM pg_ident_file_mappings() AS A
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER gtest12_03 BEFORE INSERT OR UPDATE ON gtest26
  FOR EACH ROW
  EXECUTE PROCEDURE gtest_trigger_func()
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION norwegian IS 'configuration for norwegian language'
;
----------
[DROP_PROG_OBJ] -- Cleanup
drop operator class my_op_class using btree
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '1')
;
----------
[CREATE_TABLE] CREATE TABLE result_tbl (a int, b int, c text)
;
----------
[ADMIN_TABLE] ANALYZE information_schema.sql_features
;
----------
[CREATE_TABLE] CREATE TABLE ess_table (f1 int primary key, f2 int, f3 text,
                        constraint ess_c check (f1 != f2))
;
----------
[CREATE_PROG_OBJ] CREATE CAST (upc AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[CREATE_TABLE] -- fail
create table child (b decimal(10,7)) inherits (parent)
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE base_unlogged (id int)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION ts_debug(regconfig,text) IS
    'debug function for text search configuration'
;
----------
[DROP_PUB_SUB] DROP PUBLICATION "pub1"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(issn13, issn)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
