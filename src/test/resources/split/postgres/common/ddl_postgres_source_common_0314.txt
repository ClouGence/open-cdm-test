ALTER TABLE z1 ENABLE ROW LEVEL SECURITY
;
CREATE LANGUAGE alt_lang2 HANDLER plpgsql_call_handler
;
-- hide WARNINGs for other users' tables
ANALYZE
;
ANALYZE public.nv_child_2011
;
DROP SCHEMA testns5 CASCADE
;
--
-- regression test for 8.1 merge right join bug
--

CREATE TEMP TABLE tt1 ( tt1_id int4, joincol int4 )
;
CREATE UNIQUE INDEX "ec1_expr4" ON "public"."ec1" USING "btree" ((("ff" + 4)))
;
alter table idxpart attach partition idxpart0 for values from (0) to (1000)
;
ALTER TABLE pa_target ENABLE ROW LEVEL SECURITY
;
create temp table notininner (b int not null)
;
VACUUM (FREEZE) pg_catalog.pg_namespace
;
CREATE FUNCTION invalidate_rel_block(rel regclass, blockno int)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
ALTER TABLE list_parted2 ATTACH PARTITION part_5 FOR VALUES IN (5)
;
create trigger tg_slotlink_a after insert or update or delete
    on IFace for each row execute procedure tg_slotlink_a('IF')
;
create temp table temptable as select * from generate_series(1,3) as f1
;
-- Should fail. Not allowed to have cross-type equalimage function.
ALTER OPERATOR FAMILY alt_opf18 USING btree
  ADD FUNCTION 4 (int4, int2) btequalimage(oid)
;
create table parted_trig_1 partition of parted_trig for values from (0) to (1000)
   partition by range (a)
;
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
-- PK constraint
CREATE TABLE tbl_include_pk (c1 int, c2 int, c3 int, c4 box)
;
VACUUM (ANALYZE) pg_catalog.pg_transform
;
ALTER TABLE pg_toast.pg_toast_30828 RENAME TO reind_con_toast
;
analyze extremely_skewed
;
CREATE TRIGGER test_trigger_exists
    BEFORE UPDATE ON test_exists
    FOR EACH ROW EXECUTE PROCEDURE suppress_redundant_updates_trigger()
;
CREATE FUNCTION upc(ean13)
RETURNS upc
AS '$libdir/isn', 'upc_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create trigger tg_hub_a after insert or update or delete
    on Hub for each row execute procedure tg_hub_a()
;
-- try adding a second primary key (should fail)
alter table atacc1 add constraint atacc_oid1 primary key(id)
;
CREATE FUNCTION lca(ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE UNLOGGED TABLE seeded_random(seed INT8 NOT NULL, rand TEXT NOT NULL, val INTEGER NOT NULL)
;
comment on column atacc1.a is 'testing'
;
ALTER TABLE temp_parted ATTACH PARTITION perm_part FOR VALUES IN (1)
;
CREATE TABLE s1.t (a int, b int, c int) PARTITION BY RANGE (a)
;
alter table abc drop constraint abc_uk
;
CREATE FOREIGN TABLE prem1 (f1 int, f2 text)
  SERVER loopback OPTIONS (table_name 'ploc1')
;
CREATE TABLE "public"."notnull_parent_upg" (
    "a" integer
)
;
/*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
ALTER TABLE pg_toast.pg_toast_28051 RENAME TO reind_con_toast
;
CREATE TABLE fk_parted_pk (
	a			int		PRIMARY KEY REFERENCES pk_noparted ON DELETE CASCADE
  ) PARTITION BY LIST (a)
;
create index idxparti2 on idxpart (c, b)
;
CREATE TABLE prt3_n_p2 PARTITION OF prt3_n FOR VALUES IN ('0001', '0002', '0008', '0010')
;
DROP INDEX inet_idx1
;
-- Some unsupported features
create table idxpart (a int, b int, c text) partition by range (a)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_collation
;
DO $$
DECLARE _a int; _b int;
BEGIN
  _a := 10; _b := 30;
  CALL test_proc9(_a, _b);
  RAISE NOTICE '_a: %, _b: %', _a, _b;
END
$$
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_sequence
;
ALTER TABLE pg_temp_14.pg_temp_27156_2 ADD COLUMN newdata pg_temp_14.pg_temp_27156
;
create table my_table (a int primary key, b text)
;
ANALYZE public.test_range_gist
;
CREATE TABLE grouping_unique (x integer)
;
CREATE TABLE "fkpart5"."pk1" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
PARTITION BY LIST ("a")
;
------------------------------------------------------------------------------------------
[ALTER_TABLE] ALTER TABLE z1 ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_LANGUAGE] CREATE LANGUAGE alt_lang2 HANDLER plpgsql_call_handler
;
----------
[ADMIN_TABLE] -- hide WARNINGs for other users' tables
ANALYZE
;
----------
[ADMIN_TABLE] ANALYZE public.nv_child_2011
;
----------
[DROP_SCHEMA] DROP SCHEMA testns5 CASCADE
;
----------
[CREATE_TABLE] --
-- regression test for 8.1 merge right join bug
--

CREATE TEMP TABLE tt1 ( tt1_id int4, joincol int4 )
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "ec1_expr4" ON "public"."ec1" USING "btree" ((("ff" + 4)))
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart0 for values from (0) to (1000)
;
----------
[ALTER_TABLE] ALTER TABLE pa_target ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] create temp table notininner (b int not null)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_namespace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION invalidate_rel_block(rel regclass, blockno int)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 ATTACH PARTITION part_5 FOR VALUES IN (5)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_slotlink_a after insert or update or delete
    on IFace for each row execute procedure tg_slotlink_a('IF')
;
----------
[CREATE_TABLE|SELECT] create temp table temptable as select * from generate_series(1,3) as f1
;
----------
[ALTER_PROG_OBJ] -- Should fail. Not allowed to have cross-type equalimage function.
ALTER OPERATOR FAMILY alt_opf18 USING btree
  ADD FUNCTION 4 (int4, int2) btequalimage(oid)
;
----------
[CREATE_TABLE] create table parted_trig_1 partition of parted_trig for values from (0) to (1000)
   partition by range (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] -- PK constraint
CREATE TABLE tbl_include_pk (c1 int, c2 int, c3 int, c4 box)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_transform
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30828 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] analyze extremely_skewed
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER test_trigger_exists
    BEFORE UPDATE ON test_exists
    FOR EACH ROW EXECUTE PROCEDURE suppress_redundant_updates_trigger()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION upc(ean13)
RETURNS upc
AS '$libdir/isn', 'upc_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_hub_a after insert or update or delete
    on Hub for each row execute procedure tg_hub_a()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- try adding a second primary key (should fail)
alter table atacc1 add constraint atacc_oid1 primary key(id)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lca(ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE seeded_random(seed INT8 NOT NULL, rand TEXT NOT NULL, val INTEGER NOT NULL)
;
----------
[COMMENT_COLUMN] comment on column atacc1.a is 'testing'
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE temp_parted ATTACH PARTITION perm_part FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE s1.t (a int, b int, c int) PARTITION BY RANGE (a)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table abc drop constraint abc_uk
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE prem1 (f1 int, f2 text)
  SERVER loopback OPTIONS (table_name 'ploc1')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_parent_upg" (
    "a" integer
)
;
----------
[CREATE_POLICY] /*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28051 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE fk_parted_pk (
	a			int		PRIMARY KEY REFERENCES pk_noparted ON DELETE CASCADE
  ) PARTITION BY LIST (a)
;
----------
[ADD_INDEX] create index idxparti2 on idxpart (c, b)
;
----------
[CREATE_TABLE] CREATE TABLE prt3_n_p2 PARTITION OF prt3_n FOR VALUES IN ('0001', '0002', '0008', '0010')
;
----------
[DROP_INDEX] DROP INDEX inet_idx1
;
----------
[CREATE_TABLE] -- Some unsupported features
create table idxpart (a int, b int, c text) partition by range (a)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_collation
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int;
BEGIN
  _a := 10; _b := 30;
  CALL test_proc9(_a, _b);
  RAISE NOTICE '_a: %, _b: %', _a, _b;
END
$$
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_sequence
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_14.pg_temp_27156_2 ADD COLUMN newdata pg_temp_14.pg_temp_27156
;
----------
[CREATE_TABLE] create table my_table (a int primary key, b text)
;
----------
[ADMIN_TABLE] ANALYZE public.test_range_gist
;
----------
[CREATE_TABLE] CREATE TABLE grouping_unique (x integer)
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."pk1" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
PARTITION BY LIST ("a")
;
