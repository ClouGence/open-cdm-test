VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_replication_origin
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_enum
;
ALTER INDEX attmp_idx ALTER COLUMN 1 SET STATISTICS 1000
;
ALTER TABLE pg_database ADD PRIMARY KEY USING INDEX pg_database_oid_index
;
CREATE TABLE truncparted1 PARTITION OF truncparted FOR VALUES IN (1)
;
create or replace function tftest(a1 int) returns table(a int, b int) as $$
begin
  a := a1; b := a1 + 1;
  return next;
  a := a1 * 10; b := a1 * 10 + 1;
  return next;
end;
$$ language plpgsql immutable strict
;
-- Cleanup
DROP TABLE stats_plan_test
;
CREATE FUNCTION slice_array(hstore,text[])
RETURNS text[]
AS '$libdir/hstore','hstore_slice_to_array'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
vacuum (disable_page_skipping) test_partition
;
CREATE INDEX tbspace_reindex_part_index_0_1 ON ONLY tbspace_reindex_part_0_1 (c1)
;
ALTER TABLE ONLY "public"."tableam_parted_heap2" ATTACH PARTITION "public"."tableam_parted_d_heap2" FOR VALUES IN ('d')
;
ANALYZE functional_dependencies
;
ANALYZE public.check2_tbl
;
ALTER TABLE pg_toast.pg_toast_28154 RENAME TO reind_con_toast
;
ALTER TABLE regtest_table_part ALTER p SET STORAGE PLAIN
;
CREATE TABLE test_macaddr8 (
	i macaddr8
)
;
create table phv_part_1 partition of phv_part for values in (1)
;
VACUUM (PARALLEL 0) pg_catalog.pg_inherits
;
COMMENT ON CONVERSION myconv IS NULL
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."renamecolumnanother" INHERIT "public"."renamecolumnchild"
;
CREATE FUNCTION uuid_version_bits(uuid) RETURNS varbit AS
$$ SELECT ('x' || substr($1::text, 15, 2))::bit(8) & '11110000' $$
LANGUAGE SQL STRICT IMMUTABLE
;
CREATE TABLE equipment_r (
	name 		text,
	hobby		text
)
;
CREATE FUNCTION roundtrip(val jsonb, ref text = '') RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_[0]));
die 'unexpected '.(ref($_[0]) || 'not a').' reference'
    if ref($_[0]) ne $_[1];
return $_[0];
$$
;
ANALYZE pagg_tab_ml
;
CREATE FOREIGN TABLE inv_bsz (c1 int )
	SERVER loopback OPTIONS (batch_size '100$%$#$#')
;
VACUUM ANALYZE tenk1
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '\')
;
CREATE DOMAIN "public"."sqljson_char2" AS character(2)
	CONSTRAINT "sqljson_char2_check" CHECK ((VALUE <> '12'::"bpchar"))
;
CREATE TABLE temporal3 (
  id int4range,
  valid_at daterange
)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_language
;
ANALYZE public.mvtest_t
;
DROP SEQUENCE t1_f1_seq
;
CREATE SEQUENCE INSERT_SEQ
;
create table tuplesest_parted2 partition of tuplesest_parted for values from (100) to (200)
;
ALTER OPERATOR FAMILY gist_inet_ops USING gist ADD
	OPERATOR	6	<>  (inet, inet)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
ALTER TABLE s.x ADD CONSTRAINT e0rows EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll WITH s.=)
  WHERE (s.index_row_if(y))
;
COMMENT ON TEXT SEARCH CONFIGURATION indonesian IS 'configuration for indonesian language'
;
create constraint trigger parted_trig after insert on parted_constr_ancestor
  deferrable
  for each row execute procedure trigger_notice_ab()
;
CREATE TEMP TABLE tmp_foo (data text) ON COMMIT DELETE ROWS
;
CREATE VIEW vx1 AS SELECT a FROM tx1 WHERE EXISTS(SELECT 1 FROM tx2 JOIN tx3 ON b=c)
;
CREATE TABLE only_parted (a int, b text) PARTITION BY LIST (a)
;
create index t3i on t3 (expensivefunc(x))
;
CREATE TABLE part2 (val text, key integer)
;
-- ************************************************************
-- * BEFORE INSERT on all slots
-- *	- Check name prefix
-- ************************************************************
create function tg_chkslotname() returns trigger as '
begin
    if substr(new.slotname, 1, 2) != tg_argv[0] then
        raise exception ''slotname must begin with %'', tg_argv[0];
    end if;
    return new;
end;
' language plpgsql
;
-- Verify bugfix with query on indexed partitioned table with no partitions
-- https://postgr.es/m/20180124162006.pmapfiznhgngwtjf@alvherre.pgsql
CREATE TABLE idxpart (col1 INT) PARTITION BY RANGE (col1)
;
ALTER TABLE pg_temp_3.pg_temp_26915_2 ADD COLUMN newdata pg_temp_3.pg_temp_26915
;
ANALYZE pg_temp_13.pg_temp_25947
;
CREATE INDEX test2_col1_idx ON test2(col1)
;
ALTER TABLE pg_publication_rel ADD UNIQUE USING INDEX pg_publication_rel_prrelid_prpubid_index
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_replication_origin
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_enum
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX attmp_idx ALTER COLUMN 1 SET STATISTICS 1000
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_database ADD PRIMARY KEY USING INDEX pg_database_oid_index
;
----------
[CREATE_TABLE] CREATE TABLE truncparted1 PARTITION OF truncparted FOR VALUES IN (1)
;
----------
[CREATE_PROG_OBJ] create or replace function tftest(a1 int) returns table(a int, b int) as $$
begin
  a := a1; b := a1 + 1;
  return next;
  a := a1 * 10; b := a1 * 10 + 1;
  return next;
end;
$$ language plpgsql immutable strict
;
----------
[DROP_TABLE] -- Cleanup
DROP TABLE stats_plan_test
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION slice_array(hstore,text[])
RETURNS text[]
AS '$libdir/hstore','hstore_slice_to_array'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] vacuum (disable_page_skipping) test_partition
;
----------
[ADD_INDEX] CREATE INDEX tbspace_reindex_part_index_0_1 ON ONLY tbspace_reindex_part_0_1 (c1)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."tableam_parted_heap2" ATTACH PARTITION "public"."tableam_parted_d_heap2" FOR VALUES IN ('d')
;
----------
[ADMIN_TABLE] ANALYZE functional_dependencies
;
----------
[ADMIN_TABLE] ANALYZE public.check2_tbl
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28154 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_table_part ALTER p SET STORAGE PLAIN
;
----------
[CREATE_TABLE] CREATE TABLE test_macaddr8 (
	i macaddr8
)
;
----------
[CREATE_TABLE] create table phv_part_1 partition of phv_part for values in (1)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_inherits
;
----------
[ALTER_POLICY] COMMENT ON CONVERSION myconv IS NULL
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."renamecolumnanother" INHERIT "public"."renamecolumnchild"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION uuid_version_bits(uuid) RETURNS varbit AS
$$ SELECT ('x' || substr($1::text, 15, 2))::bit(8) & '11110000' $$
LANGUAGE SQL STRICT IMMUTABLE
;
----------
[CREATE_TABLE] CREATE TABLE equipment_r (
	name 		text,
	hobby		text
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION roundtrip(val jsonb, ref text = '') RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_[0]));
die 'unexpected '.(ref($_[0]) || 'not a').' reference'
    if ref($_[0]) ne $_[1];
return $_[0];
$$
;
----------
[ADMIN_TABLE] ANALYZE pagg_tab_ml
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE inv_bsz (c1 int )
	SERVER loopback OPTIONS (batch_size '100$%$#$#')
;
----------
[ADMIN_TABLE] VACUUM ANALYZE tenk1
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '\')
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."sqljson_char2" AS character(2)
	CONSTRAINT "sqljson_char2_check" CHECK ((VALUE <> '12'::"bpchar"))
;
----------
[CREATE_TABLE] CREATE TABLE temporal3 (
  id int4range,
  valid_at daterange
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_language
;
----------
[ADMIN_TABLE] ANALYZE public.mvtest_t
;
----------
[DROP_SEQUENCE] DROP SEQUENCE t1_f1_seq
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE INSERT_SEQ
;
----------
[CREATE_TABLE] create table tuplesest_parted2 partition of tuplesest_parted for values from (100) to (200)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_inet_ops USING gist ADD
	OPERATOR	6	<>  (inet, inet)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_parser
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE s.x ADD CONSTRAINT e0rows EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE s.coll WITH s.=)
  WHERE (s.index_row_if(y))
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION indonesian IS 'configuration for indonesian language'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create constraint trigger parted_trig after insert on parted_constr_ancestor
  deferrable
  for each row execute procedure trigger_notice_ab()
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tmp_foo (data text) ON COMMIT DELETE ROWS
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vx1 AS SELECT a FROM tx1 WHERE EXISTS(SELECT 1 FROM tx2 JOIN tx3 ON b=c)
;
----------
[CREATE_TABLE] CREATE TABLE only_parted (a int, b text) PARTITION BY LIST (a)
;
----------
[ADD_INDEX] create index t3i on t3 (expensivefunc(x))
;
----------
[CREATE_TABLE] CREATE TABLE part2 (val text, key integer)
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * BEFORE INSERT on all slots
-- *	- Check name prefix
-- ************************************************************
create function tg_chkslotname() returns trigger as '
begin
    if substr(new.slotname, 1, 2) != tg_argv[0] then
        raise exception ''slotname must begin with %'', tg_argv[0];
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_TABLE] -- Verify bugfix with query on indexed partitioned table with no partitions
-- https://postgr.es/m/20180124162006.pmapfiznhgngwtjf@alvherre.pgsql
CREATE TABLE idxpart (col1 INT) PARTITION BY RANGE (col1)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_26915_2 ADD COLUMN newdata pg_temp_3.pg_temp_26915
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25947
;
----------
[ADD_INDEX] CREATE INDEX test2_col1_idx ON test2(col1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_publication_rel ADD UNIQUE USING INDEX pg_publication_rel_prrelid_prpubid_index
;
