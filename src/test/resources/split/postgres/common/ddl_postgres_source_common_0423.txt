CREATE EVENT TRIGGER test_ddl_deparse
ON ddl_command_end EXECUTE PROCEDURE test_ddl_deparse()
;
CREATE COLLATION coll_dup_chk (PROVIDER = icu, PROVIDER = NONSENSE, LC_COLLATE = "POSIX", LC_CTYPE = "POSIX")
;
drop index mcrparted_a_abs_c_idx
;
create view my_view as select * from my_table
;
create view tt20v as
select * from
  coalesce(1,2) as c,
  collation for ('x'::text) col,
  current_date as d,
  localtimestamp(3) as t,
  cast(1+2 as int4) as i4,
  cast(1+2 as int8) as i8
;
DROP TYPE gtest_type CASCADE
;
CREATE TEMP VIEW v2_temp AS SELECT * FROM base_table
;
alter table idxpart add primary key(i)
;
CREATE TABLE tab1 (a int PRIMARY KEY, c text DEFAULT 'sub2_tab1', b text)
;
CREATE TABLE no_index_cleanup (i INT PRIMARY KEY, t TEXT)
;
CREATE TABLE truncate_tab (a int)
;
create trigger self_ref_trigger_del_trig before delete on self_ref_trigger
  for each row execute procedure self_ref_trigger_del_func()
;
--
--
--
-- inet/cidr ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_inet_consistent(internal,inet,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE public.prt1_e_p3
;
CREATE INDEX "spgist_text_idx" ON "public"."spgist_text_tbl" USING "spgist" ("t")
;
ALTER TABLE ONLY "public"."beta_neg" ATTACH PARTITION "public"."beta_neg_p2" FOR VALUES FROM (200) TO (300)
;
create table p1_c1() inherits(p1)
;
-- SRF in rangetable
CREATE VIEW ro_view13 AS SELECT a, b FROM (SELECT * FROM base_tbl) AS t
;
CREATE TABLE list_parted3_1 PARTITION OF list_parted3 FOR VALUES IN (1)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_statistic_ext_data
;
CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=28913 host=/tmp/XeBgNWHTrn dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION pub_dropped_cols
;
-- commutator
CREATE OPERATOR ==== (
    LEFTARG = real,
    RIGHTARG = boolean,
    PROCEDURE = alter_op_test_fn_real_bool
)
;
CREATE FUNCTION gbt_numeric_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE pg_toast.pg_toast_33124 RENAME TO reind_con_toast
;
-- check that recursive CTE processing doesn't rewrite a CTE more than once
-- (must not try to expand GENERATED ALWAYS IDENTITY columns more than once)
CREATE TEMP TABLE id_alw1 (i int GENERATED ALWAYS AS IDENTITY)
;
ALTER TABLE pg_temp_9.pg_temp_26805_2 ADD COLUMN newdata pg_temp_9.pg_temp_26805
;
CREATE FUNCTION _int_contained_joinsel(internal, oid, internal, smallint, internal)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
-- not supported
ALTER TABLE regtest_table VALIDATE CONSTRAINT test_ck
;
CREATE OPERATOR CLASS gist_int8_ops
DEFAULT FOR TYPE int8 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_int8_consistent (internal, int8, int2, oid, internal),
	FUNCTION	2	gbt_int8_union (internal, internal),
	FUNCTION	3	gbt_int8_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_int8_penalty (internal, internal, internal),
	FUNCTION	6	gbt_int8_picksplit (internal, internal),
	FUNCTION	7	gbt_int8_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
ALTER INDEX pg_toast.pg_toast_30515_index RENAME TO reind_con_toast_idx
;
-- clean up
drop table other_partitioned_fk
;
CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
VACUUM (INDEX_CLEANUP FALSE) vaccluster
;
ALTER TABLE ONLY "public"."tbl_include_pk"
    ADD CONSTRAINT "tbl_include_pk_pkey" PRIMARY KEY ("c1", "c2") INCLUDE ("c3", "c4")
;
CREATE FUNCTION gin_compare_prefix_macaddr8(macaddr8, macaddr8, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
-- leave a collation for pg_upgrade test
CREATE COLLATION coll_icu_upgrade FROM "und-x-icu"
;
CREATE TABLE testtab2 (a int)
;
CREATE TYPE test_type_empty AS ()
;
-- add a primary key (fails)
alter table atacc1 add constraint atacc_test1 primary key (test)
;
ALTER PUBLICATION testpub_fortable DROP TABLE testpub_tbl6
;
CREATE OPERATOR FAMILY alt_opf8 USING btree
;
CREATE TABLE receipt (receipt_no int NOT NULL PRIMARY KEY, deposit_date date NOT NULL, amount numeric(13,2))
;
CREATE TABLE fk_partitioned_fk_1 PARTITION OF fk_partitioned_fk FOR VALUES FROM (MINVALUE) TO (MAXVALUE)
;
ALTER TABLE ONLY test_replica_identity4
  ADD CONSTRAINT test_replica_identity4_pkey PRIMARY KEY (id)
;
-- timestamp check

CREATE TABLE timestamptmp (a timestamp)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_index
;
DROP TRIGGER d15_insert_trig ON part_d_15_20
;
CREATE FUNCTION pg_buffercache_usage_counts(
    OUT usage_count int4,
    OUT buffers int4,
    OUT dirty int4,
    OUT pinned int4)
RETURNS SETOF record
AS '$libdir/pg_buffercache', 'pg_buffercache_usage_counts'
LANGUAGE C PARALLEL SAFE
;
-- Split prt2_adv_p3 into two partitions so that prt1_adv_p3 matches both
CREATE TABLE prt2_adv_p3_1 PARTITION OF prt2_adv FOR VALUES FROM (350) TO (375)
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER test_ddl_deparse
ON ddl_command_end EXECUTE PROCEDURE test_ddl_deparse()
;
----------
[CREATE_POLICY] CREATE COLLATION coll_dup_chk (PROVIDER = icu, PROVIDER = NONSENSE, LC_COLLATE = "POSIX", LC_CTYPE = "POSIX")
;
----------
[DROP_INDEX] drop index mcrparted_a_abs_c_idx
;
----------
[CREATE_VIEW(SELECT)] create view my_view as select * from my_table
;
----------
[CREATE_VIEW(METADATA)] create view tt20v as
select * from
  coalesce(1,2) as c,
  collation for ('x'::text) col,
  current_date as d,
  localtimestamp(3) as t,
  cast(1+2 as int4) as i4,
  cast(1+2 as int8) as i8
;
----------
[DROP_TYPE] DROP TYPE gtest_type CASCADE
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW v2_temp AS SELECT * FROM base_table
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add primary key(i)
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (a int PRIMARY KEY, c text DEFAULT 'sub2_tab1', b text)
;
----------
[CREATE_TABLE] CREATE TABLE no_index_cleanup (i INT PRIMARY KEY, t TEXT)
;
----------
[CREATE_TABLE] CREATE TABLE truncate_tab (a int)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger self_ref_trigger_del_trig before delete on self_ref_trigger
  for each row execute procedure self_ref_trigger_del_func()
;
----------
[CREATE_PROG_OBJ] --
--
--
-- inet/cidr ops
--
--
--
-- define the GiST support methods
CREATE FUNCTION gbt_inet_consistent(internal,inet,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_e_p3
;
----------
[ADD_INDEX] CREATE INDEX "spgist_text_idx" ON "public"."spgist_text_tbl" USING "spgist" ("t")
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."beta_neg" ATTACH PARTITION "public"."beta_neg_p2" FOR VALUES FROM (200) TO (300)
;
----------
[CREATE_TABLE] create table p1_c1() inherits(p1)
;
----------
[CREATE_VIEW(SELECT)] -- SRF in rangetable
CREATE VIEW ro_view13 AS SELECT a, b FROM (SELECT * FROM base_tbl) AS t
;
----------
[CREATE_TABLE] CREATE TABLE list_parted3_1 PARTITION OF list_parted3 FOR VALUES IN (1)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub2 CONNECTION 'port=28913 host=/tmp/XeBgNWHTrn dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub_dropped_cols CONNECTION 'port=14071 host=/tmp/cxWTX1q2yg dbname=postgres' PUBLICATION pub_dropped_cols
;
----------
[CREATE_PROG_OBJ] -- commutator
CREATE OPERATOR ==== (
    LEFTARG = real,
    RIGHTARG = boolean,
    PROCEDURE = alter_op_test_fn_real_bool
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_numeric_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33124 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- check that recursive CTE processing doesn't rewrite a CTE more than once
-- (must not try to expand GENERATED ALWAYS IDENTITY columns more than once)
CREATE TEMP TABLE id_alw1 (i int GENERATED ALWAYS AS IDENTITY)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_26805_2 ADD COLUMN newdata pg_temp_9.pg_temp_26805
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_contained_joinsel(internal, oid, internal, smallint, internal)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- not supported
ALTER TABLE regtest_table VALIDATE CONSTRAINT test_ck
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_int8_ops
DEFAULT FOR TYPE int8 USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_int8_consistent (internal, int8, int2, oid, internal),
	FUNCTION	2	gbt_int8_union (internal, internal),
	FUNCTION	3	gbt_int8_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_int8_penalty (internal, internal, internal),
	FUNCTION	6	gbt_int8_picksplit (internal, internal),
	FUNCTION	7	gbt_int8_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30515_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] -- clean up
drop table other_partitioned_fk
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) vaccluster
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."tbl_include_pk"
    ADD CONSTRAINT "tbl_include_pk_pkey" PRIMARY KEY ("c1", "c2") INCLUDE ("c3", "c4")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_macaddr8(macaddr8, macaddr8, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_POLICY] -- leave a collation for pg_upgrade test
CREATE COLLATION coll_icu_upgrade FROM "und-x-icu"
;
----------
[CREATE_TABLE] CREATE TABLE testtab2 (a int)
;
----------
[CREATE_TYPE] CREATE TYPE test_type_empty AS ()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a primary key (fails)
alter table atacc1 add constraint atacc_test1 primary key (test)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_fortable DROP TABLE testpub_tbl6
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf8 USING btree
;
----------
[CREATE_TABLE] CREATE TABLE receipt (receipt_no int NOT NULL PRIMARY KEY, deposit_date date NOT NULL, amount numeric(13,2))
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_1 PARTITION OF fk_partitioned_fk FOR VALUES FROM (MINVALUE) TO (MAXVALUE)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY test_replica_identity4
  ADD CONSTRAINT test_replica_identity4_pkey PRIMARY KEY (id)
;
----------
[CREATE_TABLE] -- timestamp check

CREATE TABLE timestamptmp (a timestamp)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_index
;
----------
[DROP_TRIGGER] DROP TRIGGER d15_insert_trig ON part_d_15_20
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_buffercache_usage_counts(
    OUT usage_count int4,
    OUT buffers int4,
    OUT dirty int4,
    OUT pinned int4)
RETURNS SETOF record
AS '$libdir/pg_buffercache', 'pg_buffercache_usage_counts'
LANGUAGE C PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Split prt2_adv_p3 into two partitions so that prt1_adv_p3 matches both
CREATE TABLE prt2_adv_p3_1 PARTITION OF prt2_adv FOR VALUES FROM (350) TO (375)
;
