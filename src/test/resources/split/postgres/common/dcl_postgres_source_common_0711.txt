set plpgsql.extra_errors to 'none'
;
EXECUTE dumpFunc('36020')
;
-- chunked results with an error after the first chunk
-- (we must disable parallel query here, else the behavior is timing-dependent)
set debug_parallel_query = off
;
START TRANSACTION ISOLATION LEVEL REPEATABLE READ, READ WRITE, DEFERRABLE
;
ALTER DOMAIN generated_virtual_tests.gtestdomain1 OWNER TO zyc
;
-- drop as non-superuser should fail
create role regress_evt_user
;
EXECUTE dumpTableAttach('24068')
;
EXECUTE dumpFunc('16677')
;
COPY testxmlschema.test2 (z, y, x, w, v, u, t, s, stz, r, rtz, q, p, o, n, m) TO stdout
;
-- someday this should work
drop statistics atref_stat
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] set plpgsql.extra_errors to 'none'
;
----------
[UNSAFE] EXECUTE dumpFunc('36020')
;
----------
[SESSION_SETTING_WRITE] -- chunked results with an error after the first chunk
-- (we must disable parallel query here, else the behavior is timing-dependent)
set debug_parallel_query = off
;
----------
[TRANSACTION] START TRANSACTION ISOLATION LEVEL REPEATABLE READ, READ WRITE, DEFERRABLE
;
----------
[TRANSFER_PRIVILEGE] ALTER DOMAIN generated_virtual_tests.gtestdomain1 OWNER TO zyc
;
----------
[CREATE_ROLE] -- drop as non-superuser should fail
create role regress_evt_user
;
----------
[UNSAFE] EXECUTE dumpTableAttach('24068')
;
----------
[UNSAFE] EXECUTE dumpFunc('16677')
;
----------
[DATA_EXPORT] COPY testxmlschema.test2 (z, y, x, w, v, u, t, s, stz, r, rtz, q, p, o, n, m) TO stdout
;
----------
[ADMIN_PERFORMANCE] -- someday this should work
drop statistics atref_stat
;
