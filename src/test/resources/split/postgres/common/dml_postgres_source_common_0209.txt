INSERT INTO test_timestamptz VALUES
	( '2004-10-26 03:55:08' ),
	( '2004-10-26 04:55:08' ),
	( '2004-10-26 05:55:08' ),
	( '2004-10-26 08:55:08' ),
	( '2004-10-26 09:55:08' ),
	( '2004-10-26 10:55:08' )
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (8, 1, 42669, -3952, CURRENT_TIMESTAMP)
;
INSERT INTO public.ths_p2 VALUES (3, 133)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6902', relminmxid = '1'
WHERE oid = '"public"."subselect_tbl"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2991', relminmxid = '1'
WHERE oid = '"public"."real_city"'::pg_catalog.regclass
;
INSERT INTO caster (citext)        VALUES ('12'::money)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13537', relminmxid = '3'
WHERE oid = '34346'
;
-- matches
INSERT INTO hjtest_2(a, id, b, c) VALUES (true, 3, 'another', 7)
;
-- ******************************
-- * Division check
-- ******************************
DELETE FROM num_result
;
INSERT INTO public.tplain VALUES ('0', 190)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt2_m_p2"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt2_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
INSERT INTO y SELECT generate_series(1, 10)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11932', relminmxid = '3'
WHERE oid = '"public"."clstr_tst"'::pg_catalog.regclass
;
DELETE FROM gtest1 WHERE a >= 3
;
INSERT INTO public.ths_p1 VALUES (6, 636)
;
INSERT INTO clean_aborted_self SELECT g.i, 'rolling back in a sec' FROM generate_series(1, 100) g(i)
;
DELETE FROM inh_parent WHERE a = 2
;
INSERT INTO brin_timestamp_test
SELECT '294276-12-01 00:00:01'::timestamptz + (i || ' seconds')::interval
  FROM generate_series(1,30) s(i)
;
INSERT INTO public.tru_pk_table(id) VALUES ($1)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '17191'
;
INSERT INTO FKTABLE VALUES (2, 3)
;
DELETE FROM foo WHERE f1 = 5
  RETURNING old.tableoid::regclass, old.ctid, old.*,
            new.tableoid::regclass, new.ctid, new.*, *
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i'),
             ('........pg.dropped.4........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."dropcolumnanother"'::pg_catalog.regclass
  AND attname = v.dname
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."pagg_tab_p2"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (8, 688)
;
INSERT INTO public.tht VALUES ('9', 709)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '18000'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14574', relminmxid = '3'
WHERE oid = '36558'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14105', relminmxid = '3'
WHERE oid = '"public"."part_p2_p1"'::pg_catalog.regclass
;
INSERT INTO timetzcmp (r_id,a) SELECT  5,count(*) FROM timetztmp WHERE a >  '07:46:45 GMT+3'
;
INSERT INTO public.tplain VALUES ('1', 351)
;
DELETE FROM rw_view3 WHERE s = sin(0.1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13958', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('0', 230)
;
insert into test_multirange_gist select int4multirange(int4range(g*10, g*20, '(]'), int4range(g*20, NULL, '(]')) from generate_series(1,100) g
;
INSERT INTO clustertest VALUES (10)
;
DELETE FROM public.sro_index_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_3.pg_temp_27598_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12900', relminmxid = '3'
WHERE oid = '"fkpart6"."pk11"'::pg_catalog.regclass
;
--
-- TOASTed value test
--
insert into inserttest values(30, 50, repeat('x', 10000))
;
INSERT INTO brin_hot_2 VALUES (1, 100)
;
INSERT INTO b_star (class) VALUES ('b')
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"public"."cc2"'::pg_catalog.regclass AND
conname IN ('pp1_f1_not_null')
;
INSERT INTO test_recovery_conflict_table1(a) SELECT generate_series(1, 100) i
;
INSERT INTO clstr_tst_inh VALUES (0, 100, 'in child table')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14310', relminmxid = '3'
WHERE oid = '"public"."mic_inputs"'::pg_catalog.regclass
;
insert into itrtest values (1, 'foo')
;
INSERT INTO t (a,b,c) VALUES (1,2,NULL)
;
DELETE FROM join_tbl
;
-- failure
INSERT INTO rls_test_both VALUES ('regress_r1','regress_s1',7)
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO test_timestamptz VALUES
	( '2004-10-26 03:55:08' ),
	( '2004-10-26 04:55:08' ),
	( '2004-10-26 05:55:08' ),
	( '2004-10-26 08:55:08' ),
	( '2004-10-26 09:55:08' ),
	( '2004-10-26 10:55:08' )
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (8, 1, 42669, -3952, CURRENT_TIMESTAMP)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 133)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6902', relminmxid = '1'
WHERE oid = '"public"."subselect_tbl"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2991', relminmxid = '1'
WHERE oid = '"public"."real_city"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO caster (citext)        VALUES ('12'::money)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13537', relminmxid = '3'
WHERE oid = '34346'
;
----------
[INSERT|SELECT] -- matches
INSERT INTO hjtest_2(a, id, b, c) VALUES (true, 3, 'another', 7)
;
----------
[DELETE] -- ******************************
-- * Division check
-- ******************************
DELETE FROM num_result
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 190)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt2_m_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt2_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT|SELECT] INSERT INTO y SELECT generate_series(1, 10)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11932', relminmxid = '3'
WHERE oid = '"public"."clstr_tst"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM gtest1 WHERE a >= 3
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 636)
;
----------
[INSERT|SELECT] INSERT INTO clean_aborted_self SELECT g.i, 'rolling back in a sec' FROM generate_series(1, 100) g(i)
;
----------
[DELETE] DELETE FROM inh_parent WHERE a = 2
;
----------
[INSERT|SELECT] INSERT INTO brin_timestamp_test
SELECT '294276-12-01 00:00:01'::timestamptz + (i || ' seconds')::interval
  FROM generate_series(1,30) s(i)
;
----------
[INSERT|SELECT] INSERT INTO public.tru_pk_table(id) VALUES ($1)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '17191'
;
----------
[INSERT] INSERT INTO FKTABLE VALUES (2, 3)
;
----------
[DELETE] DELETE FROM foo WHERE f1 = 5
  RETURNING old.tableoid::regclass, old.ctid, old.*,
            new.tableoid::regclass, new.ctid, new.*, *
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i'),
             ('........pg.dropped.4........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."dropcolumnanother"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."pagg_tab_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 688)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 709)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '18000'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14574', relminmxid = '3'
WHERE oid = '36558'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14105', relminmxid = '3'
WHERE oid = '"public"."part_p2_p1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO timetzcmp (r_id,a) SELECT  5,count(*) FROM timetztmp WHERE a >  '07:46:45 GMT+3'
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 351)
;
----------
[DELETE] DELETE FROM rw_view3 WHERE s = sin(0.1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13958', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('0', 230)
;
----------
[INSERT|SELECT] insert into test_multirange_gist select int4multirange(int4range(g*10, g*20, '(]'), int4range(g*20, NULL, '(]')) from generate_series(1,100) g
;
----------
[INSERT] INSERT INTO clustertest VALUES (10)
;
----------
[DELETE|SELECT] DELETE FROM public.sro_index_mv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_3.pg_temp_27598_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12900', relminmxid = '3'
WHERE oid = '"fkpart6"."pk11"'::pg_catalog.regclass
;
----------
[INSERT] --
-- TOASTed value test
--
insert into inserttest values(30, 50, repeat('x', 10000))
;
----------
[INSERT] INSERT INTO brin_hot_2 VALUES (1, 100)
;
----------
[INSERT|SELECT] INSERT INTO b_star (class) VALUES ('b')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"public"."cc2"'::pg_catalog.regclass AND
conname IN ('pp1_f1_not_null')
;
----------
[INSERT|SELECT] INSERT INTO test_recovery_conflict_table1(a) SELECT generate_series(1, 100) i
;
----------
[INSERT] INSERT INTO clstr_tst_inh VALUES (0, 100, 'in child table')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14310', relminmxid = '3'
WHERE oid = '"public"."mic_inputs"'::pg_catalog.regclass
;
----------
[INSERT] insert into itrtest values (1, 'foo')
;
----------
[INSERT|SELECT] INSERT INTO t (a,b,c) VALUES (1,2,NULL)
;
----------
[DELETE] DELETE FROM join_tbl
;
----------
[INSERT] -- failure
INSERT INTO rls_test_both VALUES ('regress_r1','regress_s1',7)
;
