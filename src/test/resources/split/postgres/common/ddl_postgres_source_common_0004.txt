ALTER TYPE split_domain_type_next.mood ADD VALUE 'angry' BEFORE 'sad'
;
create type t_rec as (x numeric)
;
CREATE FUNCTION hmac(bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_hmac'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create index spgist_unlogged_idx on spgist_unlogged_tbl using spgist (b)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
CREATE TABLE "public"."tableam_parted_c_heap2" (
    "a" "text",
    "b" integer
)
;
-- regression test for the uuid datatype
-- creating test tables
CREATE TABLE guid1
(
	guid_field UUID,
	text_field TEXT DEFAULT(now())
)
;
VACUUM (PARALLEL 0) pg_catalog.pg_rewrite
;
--
-- check multi-row VALUES in rules
--

create table rules_src(f1 int, f2 int default 0)
;
ANALYZE public.gtest20a
;
create table parted_2 partition of parted for values in (2)
;
CREATE FUNCTION unaccent_lexize(internal,internal,internal,internal)
	RETURNS internal
	AS '$libdir/unaccent', 'unaccent_lexize'
	LANGUAGE C PARALLEL SAFE
;
-- Test that we do not account for nullingrels when looking up statistics
CREATE TABLE group_tbl (a INT, b INT)
;
-- Try (and fail) to add constraint due to invalid source columns
ALTER TABLE attmp3 add constraint attmpconstr foreign key(c) references attmp2 match full
;
create temp table recur1 (f1 int)
;
CREATE EXTENSION plsample
;
CREATE FUNCTION ghstore_picksplit(internal, internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE DATABASE "template1" WITH TEMPLATE = template0 OID = 1 STRATEGY = FILE_COPY ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
ALTER FUNCTION alt_func3(int) RENAME TO alt_func4
;
ALTER SEQUENCE sequence_test8 AS smallint
;
ANALYZE pg_temp_9.pg_temp_38133_2
;
/* contrib/dict_int/dict_int--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION dintdict_init(internal)
        RETURNS internal
        AS '$libdir/dict_int'
        LANGUAGE C STRICT
;
drop table perm_part_parent cascade
;
CREATE TABLE dump_test.has_stats
			AS SELECT g.g AS x, g.g / 2 AS y FROM generate_series(1,100) AS g(g)
;
/*
 * text search configuration for hindi language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hindi and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
ALTER TYPE "public"."insenum" ADD VALUE 'i15'
;
DO $d$
    BEGIN
        EXECUTE $$CREATE SERVER fdtest FOREIGN DATA WRAPPER dblink_fdw
            OPTIONS (dbname '$$||current_database()||$$',
                     port '$$||current_setting('port')||$$'
            )$$;
    END;
$d$
;
ALTER OPERATOR FAMILY gist_macaddr_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
DROP TRIGGER t ON trunc_trigger_test
;
ALTER INDEX pg_toast.pg_toast_30497_index RENAME TO reind_con_toast_idx
;
CREATE SUBSCRIPTION tap_sub_A_B_2
        CONNECTION 'port=31667 host=/tmp/EP7K8mZqpu dbname=postgres application_name=tap_sub_A_B_2'
        PUBLICATION tap_pub_B
        WITH (origin = none, copy_data = on)
;
ALTER EVENT TRIGGER table_rewrite_trg DISABLE
;
DROP TABLE t1
;
ANALYZE pg_temp_11.pg_temp_41180
;
CREATE CAST (split_obj_priv.cast_mood AS integer)
WITH FUNCTION split_obj_priv.mood_rank(split_obj_priv.cast_mood)
AS ASSIGNMENT
;
ALTER FUNCTION gbt_bytea_penalty(internal, internal, internal) PARALLEL SAFE
;
create temp table gstest5(id integer primary key, v integer)
;
-- set PUBLISH_VIA_PARTITION_ROOT to true and test column list for partitioned
-- table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=1)
;
DROP TABLE IF EXISTS foo_tbl
;
drop table parent, child
;
create table trigtest (i serial primary key)
;
-- can be pushed down
DROP TRIGGER trig_stmt_before ON rem1
;
CREATE TABLE "public"."num_exp_div" (
    "id1" integer,
    "id2" integer,
    "expected" numeric(210,10)
)
;
ALTER TABLE notnull_tbl1 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
TRUNCATE functional_dependencies
;
CREATE INDEX ptnowner_i_idx ON ptnowner(i)
;
CREATE TRIGGER trig_row_before_update
BEFORE UPDATE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
COMMENT ON CONSTRAINT amount_positive ON split_maint.maint_table IS 'non-negative amount'
;
ALTER TABLE truncate_a ADD FOREIGN KEY (col1) REFERENCES trunc_c
;
-- support table for output-format tests (useful to create a footer)

create table psql_serial_tab (id serial)
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] ALTER TYPE split_domain_type_next.mood ADD VALUE 'angry' BEFORE 'sad'
;
----------
[CREATE_TYPE] create type t_rec as (x numeric)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hmac(bytea, bytea, text)
RETURNS bytea
AS '$libdir/pgcrypto', 'pg_hmac'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] create index spgist_unlogged_idx on spgist_unlogged_tbl using spgist (b)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tableam_parted_c_heap2" (
    "a" "text",
    "b" integer
)
;
----------
[CREATE_TABLE] -- regression test for the uuid datatype
-- creating test tables
CREATE TABLE guid1
(
	guid_field UUID,
	text_field TEXT DEFAULT(now())
)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_rewrite
;
----------
[CREATE_TABLE] --
-- check multi-row VALUES in rules
--

create table rules_src(f1 int, f2 int default 0)
;
----------
[ADMIN_TABLE] ANALYZE public.gtest20a
;
----------
[CREATE_TABLE] create table parted_2 partition of parted for values in (2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION unaccent_lexize(internal,internal,internal,internal)
	RETURNS internal
	AS '$libdir/unaccent', 'unaccent_lexize'
	LANGUAGE C PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Test that we do not account for nullingrels when looking up statistics
CREATE TABLE group_tbl (a INT, b INT)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Try (and fail) to add constraint due to invalid source columns
ALTER TABLE attmp3 add constraint attmpconstr foreign key(c) references attmp2 match full
;
----------
[CREATE_TABLE] create temp table recur1 (f1 int)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION plsample
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ghstore_picksplit(internal, internal)
RETURNS internal
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_CATALOG] CREATE DATABASE "template1" WITH TEMPLATE = template0 OID = 1 STRATEGY = FILE_COPY ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[RENAME_PROG_OBJ] ALTER FUNCTION alt_func3(int) RENAME TO alt_func4
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test8 AS smallint
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38133_2
;
----------
[CREATE_PROG_OBJ] /* contrib/dict_int/dict_int--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION dintdict_init(internal)
        RETURNS internal
        AS '$libdir/dict_int'
        LANGUAGE C STRICT
;
----------
[DROP_TABLE] drop table perm_part_parent cascade
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE dump_test.has_stats
			AS SELECT g.g AS x, g.g / 2 AS y FROM generate_series(1,100) AS g(g)
;
----------
[CREATE_POLICY] /*
 * text search configuration for hindi language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hindi and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hindi_stem
	(TEMPLATE = snowball, Language = hindi )
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i15'
;
----------
[BLOCK] DO $d$
    BEGIN
        EXECUTE $$CREATE SERVER fdtest FOREIGN DATA WRAPPER dblink_fdw
            OPTIONS (dbname '$$||current_database()||$$',
                     port '$$||current_setting('port')||$$'
            )$$;
    END;
$d$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_macaddr_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[DROP_TRIGGER] DROP TRIGGER t ON trunc_trigger_test
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30497_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_A_B_2
        CONNECTION 'port=31667 host=/tmp/EP7K8mZqpu dbname=postgres application_name=tap_sub_A_B_2'
        PUBLICATION tap_pub_B
        WITH (origin = none, copy_data = on)
;
----------
[ALTER_TRIGGER] ALTER EVENT TRIGGER table_rewrite_trg DISABLE
;
----------
[DROP_TABLE] DROP TABLE t1
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41180
;
----------
[CREATE_PROG_OBJ] CREATE CAST (split_obj_priv.cast_mood AS integer)
WITH FUNCTION split_obj_priv.mood_rank(split_obj_priv.cast_mood)
AS ASSIGNMENT
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bytea_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] create temp table gstest5(id integer primary key, v integer)
;
----------
[ALTER_PUB_SUB] -- set PUBLISH_VIA_PARTITION_ROOT to true and test column list for partitioned
-- table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=1)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS foo_tbl
;
----------
[DROP_TABLE] drop table parent, child
;
----------
[CREATE_TABLE] create table trigtest (i serial primary key)
;
----------
[DROP_TRIGGER] -- can be pushed down
DROP TRIGGER trig_stmt_before ON rem1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_exp_div" (
    "id1" integer,
    "id2" integer,
    "expected" numeric(210,10)
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE notnull_tbl1 ALTER COLUMN a ADD GENERATED ALWAYS AS IDENTITY
;
----------
[TRUNCATE_TABLE] TRUNCATE functional_dependencies
;
----------
[ADD_INDEX] CREATE INDEX ptnowner_i_idx ON ptnowner(i)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_before_update
BEFORE UPDATE ON rem1
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT amount_positive ON split_maint.maint_table IS 'non-negative amount'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE truncate_a ADD FOREIGN KEY (col1) REFERENCES trunc_c
;
----------
[CREATE_TABLE] -- support table for output-format tests (useful to create a footer)

create table psql_serial_tab (id serial)
;
