explain (analyze, costs off, summary off, timing off) execute mt_q1(15)
;
EXECUTE dumpFunc('24161')
;
EXECUTE dumpCompositeType('34475')
;
EXECUTE getDomainConstraints('28942')
;
COPY public.dropcolumnanother (b, e, d) TO stdout
;
EXECUTE dumpAgg('39515')
;
DROP ROLE regress_subscription_user3
;
EXECUTE dumpTableAttach('45236')
;
ALTER PUBLICATION dump_pub_all OWNER TO zyc
;
-- Drop access method cascade
-- To prevent a (rare) deadlock against autovacuum,
-- we must lock the table that owns the index that will be dropped
BEGIN
;
------------------------------------------------------------------------------------------
[UNSAFE] explain (analyze, costs off, summary off, timing off) execute mt_q1(15)
;
----------
[UNSAFE] EXECUTE dumpFunc('24161')
;
----------
[UNSAFE] EXECUTE dumpCompositeType('34475')
;
----------
[UNSAFE] EXECUTE getDomainConstraints('28942')
;
----------
[DATA_EXPORT] COPY public.dropcolumnanother (b, e, d) TO stdout
;
----------
[UNSAFE] EXECUTE dumpAgg('39515')
;
----------
[DROP_ROLE] DROP ROLE regress_subscription_user3
;
----------
[UNSAFE] EXECUTE dumpTableAttach('45236')
;
----------
[TRANSFER_PRIVILEGE] ALTER PUBLICATION dump_pub_all OWNER TO zyc
;
----------
[TRANSACTION] -- Drop access method cascade
-- To prevent a (rare) deadlock against autovacuum,
-- we must lock the table that owns the index that will be dropped
BEGIN
;
