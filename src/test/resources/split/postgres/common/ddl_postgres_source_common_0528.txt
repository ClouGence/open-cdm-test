CREATE UNIQUE INDEX "func_index_index" ON "public"."func_index_heap" USING "btree" ((("f1" || "f2")))
;
ANALYZE pg_catalog.pg_ts_dict
;
ALTER TABLE generated_stored_tests.gtest31_2 ADD CONSTRAINT cc CHECK (((y).b IS NOT NULL))
;
ALTER TABLE pg_toast.pg_toast_33427 RENAME TO reind_con_toast
;
VACUUM (FULL) pg_catalog.pg_shseclabel
;
CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') NOT NULL,
	c2 text OPTIONS (param2 'val2', param3 'val3') CHECK (c2 <> ''),
	c3 date,
	CHECK (c3 BETWEEN '1994-01-01'::date AND '1994-01-31'::date)
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
create function sum3(int8,int8,int8) returns int8 as
'select $1 + $2 + $3' language sql strict immutable
;
CREATE VIEW character_sets AS
    SELECT CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name,
           CAST(CASE WHEN getdatabaseencoding() = 'UTF8' THEN 'UCS' ELSE getdatabaseencoding() END AS sql_identifier) AS character_repertoire,
           CAST(getdatabaseencoding() AS sql_identifier) AS form_of_use,
           CAST(current_database() AS sql_identifier) AS default_collate_catalog,
           CAST(nc.nspname AS sql_identifier) AS default_collate_schema,
           CAST(c.collname AS sql_identifier) AS default_collate_name
    FROM pg_database d
         LEFT JOIN (pg_collation c JOIN pg_namespace nc ON (c.collnamespace = nc.oid))
             ON (datcollate = collcollate AND datctype = collctype)
    WHERE d.datname = current_database()
    ORDER BY char_length(c.collname) DESC, c.collname ASC -- prefer full/canonical name
    LIMIT 1
;
/*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
-- This should fail, because we just chose really odd types
CREATE TEMP TABLE FKTABLE (ftest1 cidr, ftest2 timestamp)
;
CREATE TRIGGER my_trigger_func2 AFTER INSERT OR UPDATE ON my_table
       FOR EACH ROW EXECUTE FUNCTION my_trigger_func(8)
;
CREATE TABLE trgfire4_30 PARTITION OF trgfire4 FOR VALUES IN (30)
;
CREATE TABLE parent (
	a int
)
;
DROP FUNCTION PLUS_TWO(INTEGER)
;
alter table loct1 set (autovacuum_enabled = 'false')
;
-- not supported by sepgsql
ALTER TABLE regtest_table_part ALTER p DROP DEFAULT
;
DROP SCHEMA fkpart8 CASCADE
;
ALTER FUNCTION get_raw_page(text, int8) PARALLEL RESTRICTED
;
alter table trigtest enable trigger trigtest_a_stmt_tg
;
ANALYZE check_fk_presence_2
;
CREATE TABLE split_table_idx.list_part (
    region text NOT NULL,
    amount integer NOT NULL
) PARTITION BY LIST (region)
;
ALTER TABLE ONLY "public"."mlparted12" DROP COLUMN "........pg.dropped.7........"
;
CREATE TABLE part_drop_index_locking_subpart PARTITION OF part_drop_index_locking FOR VALUES FROM (1) TO (100) PARTITION BY RANGE(id)
;
create table lparted_by_int2_16384 partition of lparted_by_int2 for values in (16384)
;
ALTER TABLE replication_example ADD COLUMN zaphod1 int
;
-- ... but this does
create or replace function compos() returns compostype as $$
begin
  return (1, 'hello')::compostype;
end;
$$ language plpgsql
;
-- Should fail. Invalid attribute
CREATE OPERATOR #@%# (
   leftarg = int8,		-- right unary
   procedure = factorial,
   invalid_att = int8
)
;
ALTER EXTENSION test_pg_dump ADD TYPE test_pg_dump_e1
;
CREATE FUNCTION "public"."array_to_set"("anyarray") RETURNS SETOF "record"
    LANGUAGE "sql" IMMUTABLE
    AS $_$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$_$
;
CREATE FUNCTION addr_nsp.trig() RETURNS TRIGGER LANGUAGE plpgsql AS $$ BEGIN END; $$
;
drop function pleast(numeric[])
;
ALTER TABLE t1 ENABLE ROW LEVEL SECURITY
;
-- stable
CREATE DOMAIN domain4 AS text[]
  DEFAULT ('{"This", "is", "' || foo(4) || '","the", "real", "world"}')::TEXT[]
;
-- Test behavior of triggers that turn UPDATE/DELETE into no-ops
create or replace function skip_merge_op() returns trigger
language plpgsql as
$$
BEGIN
	RETURN NULL;
END;
$$
;
CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int) RETURNS float
AS 'select $1+1' LANGUAGE sql
;
ALTER TABLE pg_toast.pg_toast_30567 RENAME TO reind_con_toast
;
CREATE FUNCTION isngt(issn, issn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (ANALYZE) information_schema.sql_parts
;
--
-- fsm_page_contents()
--
CREATE FUNCTION fsm_page_contents(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'fsm_page_contents'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box, EXCLUDE USING gist (c4 WITH &&) INCLUDE (c1, c2, c3))
;
-- ALTER TABLE INHERIT ensures that the child has not-null constraints
create table inh_parent (a int not null)
;
ALTER TABLE ONLY "public"."covidxpart" ATTACH PARTITION "public"."covidxpart2" FOR VALUES IN (2)
;
CREATE VIEW atestv2 AS SELECT * FROM atest2
;
CREATE INDEX bloomidx ON tst USING bloom (i, t) WITH (col1 = 3)
;
TRUNCATE tbl1
;
-- routine usage views

CREATE FUNCTION functest_IS_4a() RETURNS int LANGUAGE SQL AS 'SELECT 1'
;
create table rules_src(f1 int, f2 int default 0)
;
DROP TABLE freespace_tab
;
CREATE INDEX cluster_ind ON cluster_tab(a)
;
-- Clean up
DROP TABLE batch_cp_upd_test
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE UNIQUE INDEX "func_index_index" ON "public"."func_index_heap" USING "btree" ((("f1" || "f2")))
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_ts_dict
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE generated_stored_tests.gtest31_2 ADD CONSTRAINT cc CHECK (((y).b IS NOT NULL))
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33427 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_shseclabel
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft1 (
	c1 integer OPTIONS ("param 1" 'val1') NOT NULL,
	c2 text OPTIONS (param2 'val2', param3 'val3') CHECK (c2 <> ''),
	c3 date,
	CHECK (c3 BETWEEN '1994-01-01'::date AND '1994-01-31'::date)
) SERVER s0 OPTIONS (delimiter ',', quote '"', "be quoted" 'value')
;
----------
[CREATE_PROG_OBJ] create function sum3(int8,int8,int8) returns int8 as
'select $1 + $2 + $3' language sql strict immutable
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW character_sets AS
    SELECT CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name,
           CAST(CASE WHEN getdatabaseencoding() = 'UTF8' THEN 'UCS' ELSE getdatabaseencoding() END AS sql_identifier) AS character_repertoire,
           CAST(getdatabaseencoding() AS sql_identifier) AS form_of_use,
           CAST(current_database() AS sql_identifier) AS default_collate_catalog,
           CAST(nc.nspname AS sql_identifier) AS default_collate_schema,
           CAST(c.collname AS sql_identifier) AS default_collate_name
    FROM pg_database d
         LEFT JOIN (pg_collation c JOIN pg_namespace nc ON (c.collnamespace = nc.oid))
             ON (datcollate = collcollate AND datctype = collctype)
    WHERE d.datname = current_database()
    ORDER BY char_length(c.collname) DESC, c.collname ASC -- prefer full/canonical name
    LIMIT 1
;
----------
[CREATE_POLICY] /*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
----------
[CREATE_TABLE] -- This should fail, because we just chose really odd types
CREATE TEMP TABLE FKTABLE (ftest1 cidr, ftest2 timestamp)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER my_trigger_func2 AFTER INSERT OR UPDATE ON my_table
       FOR EACH ROW EXECUTE FUNCTION my_trigger_func(8)
;
----------
[CREATE_TABLE] CREATE TABLE trgfire4_30 PARTITION OF trgfire4 FOR VALUES IN (30)
;
----------
[CREATE_TABLE] CREATE TABLE parent (
	a int
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION PLUS_TWO(INTEGER)
;
----------
[ALTER_TABLE] alter table loct1 set (autovacuum_enabled = 'false')
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- not supported by sepgsql
ALTER TABLE regtest_table_part ALTER p DROP DEFAULT
;
----------
[DROP_SCHEMA] DROP SCHEMA fkpart8 CASCADE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION get_raw_page(text, int8) PARALLEL RESTRICTED
;
----------
[ALTER_TABLE] alter table trigtest enable trigger trigtest_a_stmt_tg
;
----------
[ADMIN_TABLE] ANALYZE check_fk_presence_2
;
----------
[CREATE_TABLE] CREATE TABLE split_table_idx.list_part (
    region text NOT NULL,
    amount integer NOT NULL
) PARTITION BY LIST (region)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted12" DROP COLUMN "........pg.dropped.7........"
;
----------
[CREATE_TABLE] CREATE TABLE part_drop_index_locking_subpart PARTITION OF part_drop_index_locking FOR VALUES FROM (1) TO (100) PARTITION BY RANGE(id)
;
----------
[CREATE_TABLE] create table lparted_by_int2_16384 partition of lparted_by_int2 for values in (16384)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE replication_example ADD COLUMN zaphod1 int
;
----------
[CREATE_PROG_OBJ] -- ... but this does
create or replace function compos() returns compostype as $$
begin
  return (1, 'hello')::compostype;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] -- Should fail. Invalid attribute
CREATE OPERATOR #@%# (
   leftarg = int8,		-- right unary
   procedure = factorial,
   invalid_att = int8
)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD TYPE test_pg_dump_e1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."array_to_set"("anyarray") RETURNS SETOF "record"
    LANGUAGE "sql" IMMUTABLE
    AS $_$
  select i AS "index", $1[i] AS "value" from generate_subscripts($1, 1) i
$_$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION addr_nsp.trig() RETURNS TRIGGER LANGUAGE plpgsql AS $$ BEGIN END; $$
;
----------
[DROP_PROG_OBJ] drop function pleast(numeric[])
;
----------
[ALTER_TABLE] ALTER TABLE t1 ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TYPE] -- stable
CREATE DOMAIN domain4 AS text[]
  DEFAULT ('{"This", "is", "' || foo(4) || '","the", "real", "world"}')::TEXT[]
;
----------
[CREATE_PROG_OBJ] -- Test behavior of triggers that turn UPDATE/DELETE into no-ops
create or replace function skip_merge_op() returns trigger
language plpgsql as
$$
BEGIN
	RETURN NULL;
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION rngfunc(in f1 int, out f2 int) RETURNS float
AS 'select $1+1' LANGUAGE sql
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30567 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(issn, issn13)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) information_schema.sql_parts
;
----------
[CREATE_PROG_OBJ] --
-- fsm_page_contents()
--
CREATE FUNCTION fsm_page_contents(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'fsm_page_contents'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box, EXCLUDE USING gist (c4 WITH &&) INCLUDE (c1, c2, c3))
;
----------
[CREATE_TABLE] -- ALTER TABLE INHERIT ensures that the child has not-null constraints
create table inh_parent (a int not null)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."covidxpart" ATTACH PARTITION "public"."covidxpart2" FOR VALUES IN (2)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW atestv2 AS SELECT * FROM atest2
;
----------
[ADD_INDEX] CREATE INDEX bloomidx ON tst USING bloom (i, t) WITH (col1 = 3)
;
----------
[TRUNCATE_TABLE] TRUNCATE tbl1
;
----------
[CREATE_PROG_OBJ] -- routine usage views

CREATE FUNCTION functest_IS_4a() RETURNS int LANGUAGE SQL AS 'SELECT 1'
;
----------
[CREATE_TABLE] create table rules_src(f1 int, f2 int default 0)
;
----------
[DROP_TABLE] DROP TABLE freespace_tab
;
----------
[ADD_INDEX] CREATE INDEX cluster_ind ON cluster_tab(a)
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE batch_cp_upd_test
;
