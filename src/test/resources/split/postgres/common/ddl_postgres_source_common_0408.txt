-- OK
ALTER TEXT SEARCH TEMPLATE alt_ts_temp2 SET SCHEMA alt_nsp2
;
VACUUM (PROCESS_MAIN FALSE, FULL) vac_option_tab
;
CREATE TABLE "fkpart5"."fk2" (
    "a" integer
)
;
CREATE TABLE part1 PARTITION OF parted FOR VALUES IN (1)
;
CREATE TABLE tbl (i INT PRIMARY KEY, t TEXT)
;
DROP TABLE part_3_4
;
ANALYZE pg_temp_10.pg_temp_26308_2
;
create rule qqq as on delete to copydml_test do also insert into copydml_test default values
;
CREATE FUNCTION test_copy_to_callback(pg_catalog.regclass)
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
alter extension test_ext7 add table old_table1
;
CREATE FUNCTION texticlike(citext, citext)
RETURNS bool AS 'texticlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE fk1 PARTITION OF fk FOR VALUES IN (1) PARTITION BY LIST (a)
;
-- This should fail, because we just chose really odd types
CREATE TABLE FKTABLE (ftest1 cidr, ftest2 timestamp, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable)
;
ALTER TABLE pg_temp_4.pg_temp_37896_2 ADD COLUMN newdata pg_temp_4.pg_temp_37896
;
CREATE RULE rw_view1_upd_rule AS ON UPDATE TO rw_view1
  DO INSTEAD UPDATE base_tbl SET a=NEW.a WHERE a=OLD.a
;
CREATE PROCEDURE ptestx(OUT a int) LANGUAGE SQL AS $$ INSERT INTO cp_test VALUES (1, 'a') $$
;
drop table ��Ƒ�㑻�������
;
CREATE POLICY p2 ON rls_tbl FOR SELECT USING (c1 <= 3)
;
-- SET STATISTICS
ALTER TABLE pg_description ALTER COLUMN description SET STATISTICS -1
;
CREATE TEXT SEARCH TEMPLATE split_ts_probe.simple_template (LEXIZE = dsimple_lexize)
;
ALTER DOMAIN things ADD CONSTRAINT meow CHECK (VALUE < 11)
;
CREATE TABLE test_pg_dump_t1 (c1 int, junk text)
;
CREATE TABLE table2(col1 SERIAL PRIMARY KEY, col2 TEXT NOT NULL)
;
ALTER INDEX pg_toast.pg_toast_33248_index RENAME TO reind_con_toast_idx
;
-- fails
CREATE TABLE bla(s TEXT)
;
--
-- regression test for improper pushing of constants across outer-join clauses
-- (as seen in early 8.2.x releases)
--

create temp table zt1 (f1 int primary key)
;
DROP TABLE pg_temp_14.pg_temp_26714_2, pg_temp_14.pg_temp_26714
;
ALTER TABLE attmp_array RENAME TO _attmp_array
;
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box,
				CONSTRAINT covering PRIMARY KEY(c1,c2) INCLUDE(c3,c4))
;
CREATE FUNCTION "public"."int8alias1in"("cstring") RETURNS "public"."int8alias1"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8in$$
;
create table test_range_elem(i int4)
;
ALTER INDEX pg_toast.pg_toast_33047_index RENAME TO reind_con_toast_idx
;
ANALYZE pg_temp_3.pg_temp_16649_2
;
AlTER EXTENSION pg_stat_statements UPDATE TO '1.5'
;
ALTER PUBLICATION testpub_default ADD TABLE pub_test.testpub_addpk
;
-- fail, can no longer change an input parameter's name
create or replace function dfunc(a varchar = 'def a', out _a varchar, x numeric = NULL, out _c numeric)
returns record as $$
  select $1, $2;
$$ language sql
;
-- VERSION
CREATE COLLATION coll_dup_chk (VERSION = '1', VERSION = "NONSENSE", LOCALE = '')
;
ANALYZE b_star
;
ANALYZE pg_temp_9.pg_temp_38106_2
;
-- Test the process_outer_partition() code path
CREATE TABLE plt1_adv (a int, b int, c text) PARTITION BY LIST (c)
;
ALTER TABLE ONLY "public"."notnull_tbl4_cld" INHERIT "public"."notnull_tbl4"
;
-- Error
CREATE TABLE ctas_nodata (ii, jj, kk) AS SELECT i, j FROM ctas_base WITH NO DATA
;
CREATE TABLE pagg_tab_para_p2 PARTITION OF pagg_tab_para FOR VALUES FROM (12) TO (22)
;
-- Restore prt1_adv_p3
ALTER TABLE prt1_adv ATTACH PARTITION prt1_adv_p3 FOR VALUES FROM (300) TO (400)
;
ANALYZE pg_temp_11.pg_temp_25373
;
CREATE FUNCTION isbn13_in(cstring)
	RETURNS isbn13
	AS '$libdir/isn', 'isbn_in'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create function convslot_trig1()
returns trigger
language plpgsql
AS $$
begin
raise notice 'trigger = %, old_table = %',
          TG_NAME,
          (select string_agg(old_table::text, ', ' order by col1) from old_table);
return null;
end; $$
;
--Should be in temp object schema
CREATE   VIEW  mytempview AS SELECT * FROM tbl1 WHERE tbl1.a
BETWEEN (SELECT d FROM tbl2 WHERE c = 1) AND (SELECT e FROM tbl3 WHERE f = 2)
AND EXISTS (SELECT g FROM tbl4 LEFT JOIN tbl3 ON tbl4.h = tbl3.f)
AND NOT EXISTS (SELECT g FROM tbl4 LEFT JOIN tmptbl ON tbl4.h = tmptbl.j)
;
CREATE OR REPLACE FUNCTION pg_logical_emit_message(
    transactional boolean,
    prefix text,
    message text,
    flush boolean DEFAULT false)
RETURNS pg_lsn
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_logical_emit_message_text'
;
create table parted_inval_tab_1_2 partition of parted_inval_tab_1
  for values from (10) to (20)
;
------------------------------------------------------------------------------------------
[ALTER_POLICY] -- OK
ALTER TEXT SEARCH TEMPLATE alt_ts_temp2 SET SCHEMA alt_nsp2
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, FULL) vac_option_tab
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."fk2" (
    "a" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE part1 PARTITION OF parted FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE tbl (i INT PRIMARY KEY, t TEXT)
;
----------
[DROP_TABLE] DROP TABLE part_3_4
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26308_2
;
----------
[CREATE_POLICY] create rule qqq as on delete to copydml_test do also insert into copydml_test default values
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_copy_to_callback(pg_catalog.regclass)
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ALTER_LIBRARY] alter extension test_ext7 add table old_table1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION texticlike(citext, citext)
RETURNS bool AS 'texticlike'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE fk1 PARTITION OF fk FOR VALUES IN (1) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] -- This should fail, because we just chose really odd types
CREATE TABLE FKTABLE (ftest1 cidr, ftest2 timestamp, FOREIGN KEY(ftest1, ftest2) REFERENCES pktable)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_37896_2 ADD COLUMN newdata pg_temp_4.pg_temp_37896
;
----------
[CREATE_POLICY] CREATE RULE rw_view1_upd_rule AS ON UPDATE TO rw_view1
  DO INSTEAD UPDATE base_tbl SET a=NEW.a WHERE a=OLD.a
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptestx(OUT a int) LANGUAGE SQL AS $$ INSERT INTO cp_test VALUES (1, 'a') $$
;
----------
[DROP_TABLE] drop table ��Ƒ�㑻�������
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON rls_tbl FOR SELECT USING (c1 <= 3)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- SET STATISTICS
ALTER TABLE pg_description ALTER COLUMN description SET STATISTICS -1
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH TEMPLATE split_ts_probe.simple_template (LEXIZE = dsimple_lexize)
;
----------
[ALTER_TYPE] ALTER DOMAIN things ADD CONSTRAINT meow CHECK (VALUE < 11)
;
----------
[CREATE_TABLE] CREATE TABLE test_pg_dump_t1 (c1 int, junk text)
;
----------
[CREATE_TABLE] CREATE TABLE table2(col1 SERIAL PRIMARY KEY, col2 TEXT NOT NULL)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33248_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- fails
CREATE TABLE bla(s TEXT)
;
----------
[CREATE_TABLE] --
-- regression test for improper pushing of constants across outer-join clauses
-- (as seen in early 8.2.x releases)
--

create temp table zt1 (f1 int primary key)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_14.pg_temp_26714_2, pg_temp_14.pg_temp_26714
;
----------
[RENAME_TABLE] ALTER TABLE attmp_array RENAME TO _attmp_array
;
----------
[CREATE_TABLE] CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box,
				CONSTRAINT covering PRIMARY KEY(c1,c2) INCLUDE(c3,c4))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int8alias1in"("cstring") RETURNS "public"."int8alias1"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8in$$
;
----------
[CREATE_TABLE] create table test_range_elem(i int4)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33047_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_16649_2
;
----------
[ALTER_LIBRARY] AlTER EXTENSION pg_stat_statements UPDATE TO '1.5'
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default ADD TABLE pub_test.testpub_addpk
;
----------
[CREATE_PROG_OBJ] -- fail, can no longer change an input parameter's name
create or replace function dfunc(a varchar = 'def a', out _a varchar, x numeric = NULL, out _c numeric)
returns record as $$
  select $1, $2;
$$ language sql
;
----------
[CREATE_POLICY] -- VERSION
CREATE COLLATION coll_dup_chk (VERSION = '1', VERSION = "NONSENSE", LOCALE = '')
;
----------
[ADMIN_TABLE] ANALYZE b_star
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38106_2
;
----------
[CREATE_TABLE] -- Test the process_outer_partition() code path
CREATE TABLE plt1_adv (a int, b int, c text) PARTITION BY LIST (c)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."notnull_tbl4_cld" INHERIT "public"."notnull_tbl4"
;
----------
[CREATE_TABLE|SELECT] -- Error
CREATE TABLE ctas_nodata (ii, jj, kk) AS SELECT i, j FROM ctas_base WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_para_p2 PARTITION OF pagg_tab_para FOR VALUES FROM (12) TO (22)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- Restore prt1_adv_p3
ALTER TABLE prt1_adv ATTACH PARTITION prt1_adv_p3 FOR VALUES FROM (300) TO (400)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_25373
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isbn13_in(cstring)
	RETURNS isbn13
	AS '$libdir/isn', 'isbn_in'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function convslot_trig1()
returns trigger
language plpgsql
AS $$
begin
raise notice 'trigger = %, old_table = %',
          TG_NAME,
          (select string_agg(old_table::text, ', ' order by col1) from old_table);
return null;
end; $$
;
----------
[CREATE_VIEW(SELECT)] --Should be in temp object schema
CREATE   VIEW  mytempview AS SELECT * FROM tbl1 WHERE tbl1.a
BETWEEN (SELECT d FROM tbl2 WHERE c = 1) AND (SELECT e FROM tbl3 WHERE f = 2)
AND EXISTS (SELECT g FROM tbl4 LEFT JOIN tbl3 ON tbl4.h = tbl3.f)
AND NOT EXISTS (SELECT g FROM tbl4 LEFT JOIN tmptbl ON tbl4.h = tmptbl.j)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_logical_emit_message(
    transactional boolean,
    prefix text,
    message text,
    flush boolean DEFAULT false)
RETURNS pg_lsn
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_logical_emit_message_text'
;
----------
[CREATE_TABLE] create table parted_inval_tab_1_2 partition of parted_inval_tab_1
  for values from (10) to (20)
;
