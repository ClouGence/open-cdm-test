CREATE TABLE prt3_n_p1 PARTITION OF prt3_n FOR VALUES IN ('0000', '0004', '0006', '0007')
;
create index t_trgm_idx on t_test_trgm using gin (t gin_trgm_ops)
;
-- portable "random"
CREATE TABLE xpto (
    id serial primary key,
    toasted_col1 text,
    rand1 float8 DEFAULT nextval('xpto_rand_seq'),
    toasted_col2 text,
    rand2 float8 DEFAULT nextval('xpto_rand_seq')
)
;
CREATE FUNCTION testAVToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
$val = [{a => 1, b => 'boo', c => undef}, {d => 2}];
return $val;
$$
;
-- no PK from INHERITS:
CREATE TABLE temporal_rng2 () INHERITS (temporal_rng)
;
DROP TABLE pg_temp_11.pg_temp_25449_2, pg_temp_11.pg_temp_25449
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13912 host=/tmp/H6QOc033E6 dbname=postgres' PUBLICATION pub1
;
drop table test3
;
CREATE POLICY p2 ON t1 TO regress_rls_carol USING ((a % 4) = 0)
;
CREATE TEMP TABLE pg_temp_4.pg_temp_37852_2 (tid pg_catalog.tid)
;
ALTER FUNCTION gbt_float4_distance(internal, real, smallint, oid, internal) PARALLEL SAFE
;
CREATE MATERIALIZED VIEW mvtest_mv_foo AS SELECT * FROM mvtest_foo_data
;
ALTER TABLE parent ALTER COLUMN a DROP IDENTITY
;
CREATE FOREIGN TABLE ftprt2_p2 PARTITION OF fprt2 FOR VALUES FROM (250) TO (500)
	SERVER loopback OPTIONS (table_name 'fprt2_p2', use_remote_estimate 'true')
;
create table perm_part partition of temp_parted default
;
CREATE TABLE pk3 PARTITION OF pk FOR VALUES FROM (2000) TO (3000)
;
ALTER TABLE ONLY "public"."trigger_parted_p2" ATTACH PARTITION "public"."trigger_parted_p2_2" FOR VALUES IN (2)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) information_schema.sql_parts
;
CREATE TRIGGER trig_d_1_15 BEFORE INSERT ON part_d_1_15
   FOR EACH ROW EXECUTE PROCEDURE func_d_1_15()
;
CREATE TABLE gtest_err_3 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (c * 2) STORED)
;
CREATE SCHEMA alt_nsp2
;
-- fail
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS '...invalid label...'
;
ALTER SCHEMA pub_test1 RENAME to pub_test1_renamed
;
VACUUM (ANALYZE) pg_catalog.pg_constraint
;
alter table loct2 set (autovacuum_enabled = 'false')
;
alter table parted_pk_detach_test detach partition parted_pk_detach_test1
;
DROP TABLE prt1_adv_p3
;
-- error
ALTER INDEX dummy_test_idx SET (option_enum = 'three')
;
analyze remt2
;
ALTER TYPE "public"."rainbow" ADD VALUE 'orange'
;
ALTER TABLE gtest27 ALTER COLUMN x DROP DEFAULT
;
CREATE FUNCTION city_insert() RETURNS trigger LANGUAGE plpgsql AS $$
declare
    ctry_id int;
begin
    if NEW.country_name IS NOT NULL then
        SELECT country_id, continent INTO ctry_id, NEW.continent
            FROM country_table WHERE country_name = NEW.country_name;
        if NOT FOUND then
            raise exception 'No such country: "%"', NEW.country_name;
        end if;
    else
        NEW.continent := NULL;
    end if;

    if NEW.city_id IS NOT NULL then
        INSERT INTO city_table
            VALUES(NEW.city_id, NEW.city_name, NEW.population, ctry_id);
    else
        INSERT INTO city_table(city_name, population, country_id)
            VALUES(NEW.city_name, NEW.population, ctry_id)
            RETURNING city_id INTO NEW.city_id;
    end if;

    RETURN NEW;
end;
$$
;
SECURITY LABEL ON TABLE seclabel_tbl1 IS 'classified'
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub_test_root, pub_test_root_1
;
alter operator family integer_ops using btree add
  operator 3 = (int8alias1, int8alias2)
;
ALTER TEXT SEARCH DICTIONARY split_ts_probe.simple_dict RENAME TO simple_dict_renamed
;
ANALYZE past_inh_parent
;
-- check that we log nothing despite having a pkey
ALTER TABLE table_without_key REPLICA IDENTITY NOTHING
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
CREATE EXTENSION test_ext_set_schema SCHEMA s1
;
-- OK

CREATE VIEW rw_view3 AS
  SELECT s, c, s/c t, ctid
  FROM rw_view1
;
ALTER SUBSCRIPTION regress_testsub SET (failover)
;
alter event trigger regress_event_trigger disable
;
-- Partition that would be reindexed, still nothing.
CREATE TABLE concur_reindex_child PARTITION OF concur_reindex_part
  FOR VALUES FROM (0) TO (10)
;
-- check adding a column not itself requiring a rewrite, together with
-- a column requiring a default (bug #16038)

-- ensure that rewrites aren't silently optimized away, removing the
-- value of the test
CREATE FUNCTION check_ddl_rewrite(p_tablename regclass, p_ddl text)
RETURNS boolean
LANGUAGE plpgsql AS $$
DECLARE
    v_relfilenode oid;
BEGIN
    v_relfilenode := relfilenode FROM pg_class WHERE oid = p_tablename;

    EXECUTE p_ddl;

    RETURN v_relfilenode <> (SELECT relfilenode FROM pg_class WHERE oid = p_tablename);
END;
$$
;
DROP SCHEMA alter2
;
drop table at_base_table
;
CREATE TABLE am_partitioned_2 PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 2)
;
DROP FUNCTION create_extension_with_temp_schema()
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_ts_config
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE prt3_n_p1 PARTITION OF prt3_n FOR VALUES IN ('0000', '0004', '0006', '0007')
;
----------
[ADD_INDEX] create index t_trgm_idx on t_test_trgm using gin (t gin_trgm_ops)
;
----------
[CREATE_TABLE] -- portable "random"
CREATE TABLE xpto (
    id serial primary key,
    toasted_col1 text,
    rand1 float8 DEFAULT nextval('xpto_rand_seq'),
    toasted_col2 text,
    rand2 float8 DEFAULT nextval('xpto_rand_seq')
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testAVToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
$val = [{a => 1, b => 'boo', c => undef}, {d => 2}];
return $val;
$$
;
----------
[CREATE_TABLE] -- no PK from INHERITS:
CREATE TABLE temporal_rng2 () INHERITS (temporal_rng)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_25449_2, pg_temp_11.pg_temp_25449
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13912 host=/tmp/H6QOc033E6 dbname=postgres' PUBLICATION pub1
;
----------
[DROP_TABLE] drop table test3
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON t1 TO regress_rls_carol USING ((a % 4) = 0)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_37852_2 (tid pg_catalog.tid)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_float4_distance(internal, real, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv_foo AS SELECT * FROM mvtest_foo_data
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE parent ALTER COLUMN a DROP IDENTITY
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ftprt2_p2 PARTITION OF fprt2 FOR VALUES FROM (250) TO (500)
	SERVER loopback OPTIONS (table_name 'fprt2_p2', use_remote_estimate 'true')
;
----------
[CREATE_TABLE] create table perm_part partition of temp_parted default
;
----------
[CREATE_TABLE] CREATE TABLE pk3 PARTITION OF pk FOR VALUES FROM (2000) TO (3000)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."trigger_parted_p2" ATTACH PARTITION "public"."trigger_parted_p2_2" FOR VALUES IN (2)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) information_schema.sql_parts
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_d_1_15 BEFORE INSERT ON part_d_1_15
   FOR EACH ROW EXECUTE PROCEDURE func_d_1_15()
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_3 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (c * 2) STORED)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA alt_nsp2
;
----------
[SECURITY_LABEL] -- fail
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS '...invalid label...'
;
----------
[RENAME_SCHEMA] ALTER SCHEMA pub_test1 RENAME to pub_test1_renamed
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_constraint
;
----------
[ALTER_TABLE] alter table loct2 set (autovacuum_enabled = 'false')
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_pk_detach_test detach partition parted_pk_detach_test1
;
----------
[DROP_TABLE] DROP TABLE prt1_adv_p3
;
----------
[ALTER_INDEX] -- error
ALTER INDEX dummy_test_idx SET (option_enum = 'three')
;
----------
[ADMIN_TABLE] analyze remt2
;
----------
[ALTER_TYPE] ALTER TYPE "public"."rainbow" ADD VALUE 'orange'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest27 ALTER COLUMN x DROP DEFAULT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION city_insert() RETURNS trigger LANGUAGE plpgsql AS $$
declare
    ctry_id int;
begin
    if NEW.country_name IS NOT NULL then
        SELECT country_id, continent INTO ctry_id, NEW.continent
            FROM country_table WHERE country_name = NEW.country_name;
        if NOT FOUND then
            raise exception 'No such country: "%"', NEW.country_name;
        end if;
    else
        NEW.continent := NULL;
    end if;

    if NEW.city_id IS NOT NULL then
        INSERT INTO city_table
            VALUES(NEW.city_id, NEW.city_name, NEW.population, ctry_id);
    else
        INSERT INTO city_table(city_name, population, country_id)
            VALUES(NEW.city_name, NEW.population, ctry_id)
            RETURNING city_id INTO NEW.city_id;
    end if;

    RETURN NEW;
end;
$$
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE seclabel_tbl1 IS 'classified'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub_test_root, pub_test_root_1
;
----------
[ALTER_PROG_OBJ] alter operator family integer_ops using btree add
  operator 3 = (int8alias1, int8alias2)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY split_ts_probe.simple_dict RENAME TO simple_dict_renamed
;
----------
[ADMIN_TABLE] ANALYZE past_inh_parent
;
----------
[ALTER_TABLE] -- check that we log nothing despite having a pkey
ALTER TABLE table_without_key REPLICA IDENTITY NOTHING
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext_set_schema SCHEMA s1
;
----------
[CREATE_VIEW(SELECT)] -- OK

CREATE VIEW rw_view3 AS
  SELECT s, c, s/c t, ctid
  FROM rw_view1
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (failover)
;
----------
[ALTER_TRIGGER] alter event trigger regress_event_trigger disable
;
----------
[CREATE_TABLE] -- Partition that would be reindexed, still nothing.
CREATE TABLE concur_reindex_child PARTITION OF concur_reindex_part
  FOR VALUES FROM (0) TO (10)
;
----------
[CREATE_PROG_OBJ] -- check adding a column not itself requiring a rewrite, together with
-- a column requiring a default (bug #16038)

-- ensure that rewrites aren't silently optimized away, removing the
-- value of the test
CREATE FUNCTION check_ddl_rewrite(p_tablename regclass, p_ddl text)
RETURNS boolean
LANGUAGE plpgsql AS $$
DECLARE
    v_relfilenode oid;
BEGIN
    v_relfilenode := relfilenode FROM pg_class WHERE oid = p_tablename;

    EXECUTE p_ddl;

    RETURN v_relfilenode <> (SELECT relfilenode FROM pg_class WHERE oid = p_tablename);
END;
$$
;
----------
[DROP_SCHEMA] DROP SCHEMA alter2
;
----------
[DROP_TABLE] drop table at_base_table
;
----------
[CREATE_TABLE] CREATE TABLE am_partitioned_2 PARTITION OF am_partitioned
  FOR VALUES WITH (MODULUS 10, REMAINDER 2)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION create_extension_with_temp_schema()
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_ts_config
;
