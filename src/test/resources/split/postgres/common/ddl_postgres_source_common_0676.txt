CREATE PUBLICATION testpub_fortable_insert WITH (publish = 'insert')
;
-- polymorphic parameter test
create function dfunc(anyelement = 'World'::text) returns text as $$
  select 'Hello, ' || $1::text;
$$ language sql
;
-- fail
drop index idxpart1_pkey
;
create trigger tg_stmt after insert on parent
  for statement execute procedure trig_nothing()
;
CREATE TABLE tab_main(a int) PARTITION BY RANGE(a)
;
CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_enabled=12)
;
CREATE SCHEMA newschema1 AUTHORIZATION CURRENT_USER
;
drop extension test_ext9
;
CREATE FUNCTION strpos( citext, citext ) RETURNS INT AS $$
    SELECT pg_catalog.strpos( pg_catalog.lower( $1::pg_catalog.text ), pg_catalog.lower( $2::pg_catalog.text ) );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION "public"."int8alias2eq"("public"."int8alias2", "public"."int8alias2") RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8eq$$
;
ALTER TABLE ONLY constraint_rename_test RENAME CONSTRAINT con1 TO con1foo
;
ALTER POLICY p2 ON s2 USING (x % 2 = 0)
;
drop table parted_trig
;
CREATE UNIQUE INDEX "hash_f8_index_1" ON "public"."hash_f8_heap" USING "btree" ("abs"("random"))
;
CREATE FUNCTION split_event_helper.capture_ddl_commands()
RETURNS event_trigger
LANGUAGE plpgsql
AS $event$
BEGIN
    INSERT INTO split_event_helper.ddl_log (
        classid, objid, objsubid, command_tag, object_type,
        schema_name, object_identity, in_extension
    )
    SELECT
        classid, objid, objsubid, command_tag, object_type,
        schema_name, object_identity, in_extension
    FROM pg_event_trigger_ddl_commands();
END;
$event$
;
CREATE TEMP TABLE pg_temp_52.pg_temp_29691_2 (tid pg_catalog.tid)
;
create index on some_tab_child(f1, f2)
;
CREATE EXTENSION postgres_fdw
;
CREATE TABLE d_listp (a int) PARTITION BY LIST(a)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
CREATE POLICY p1 ON rls_tbl USING (a < 0)
;
ALTER TABLE pg_temp_32.pg_temp_28710_2 ADD COLUMN newdata pg_temp_32.pg_temp_28710
;
DROP TABLE IF EXISTS audit_tbls.audit_tbls_schema_two_table_three
;
CREATE FUNCTION make_valid(ismn)
	RETURNS ismn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
create table pp_recpart (a pp_rectype) partition by list (a)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_statistic_ext
;
CREATE TEMP TABLE past_inh_db_parent () ON COMMIT DELETE ROWS
;
--
-- Tests for ANYCOMPATIBLE polymorphism family
--

create function anyctest(anycompatible, anycompatible)
returns anycompatible as $$
  select greatest($1, $2)
$$ language sql
;
/* contrib/hstore/hstore--1.7--1.8.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION hstore_subscript_handler(internal)
RETURNS internal
AS '$libdir/hstore', 'hstore_subscript_handler'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CALL transaction_test5b()
;
ALTER TABLE test_replica_identity2 ALTER COLUMN id TYPE bigint
;
ALTER POLICY r1 ON rec1 USING (x = (SELECT a FROM rec2 WHERE b = y))
;
ALTER INDEX pg_toast.pg_toast_27543_index RENAME TO reind_con_toast_idx
;
COMMENT ON FUNCTION tg_hub_adjustslots(bpchar, integer, integer) IS NULL
;
create table domtab (col1 integer)
;
VACUUM (FULL) pg_catalog.pg_foreign_data_wrapper
;
CREATE TABLE "public"."prt4_n_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
ANALYZE public.alpha_neg_p3
;
CREATE TABLE "public"."prt1_e_p1" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
ALTER TABLE "S 1"."T 1" ADD CONSTRAINT c2positive CHECK (c2 >= 0)
;
DROP TABLE orders
;
CREATE FUNCTION cube_ll_coord(cube, int4)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER TABLE public.test ALTER COLUMN c SET STORAGE EXTERNAL
;
CREATE UNIQUE INDEX idx_replidentity_index_a ON tab_replidentity_index(a)
;
ALTER TABLE ONLY "regress_indexing"."pk51"
    ADD CONSTRAINT "pk51_pkey" PRIMARY KEY ("a")
;
ALTER TABLE FKTABLE ALTER CONSTRAINT fknd2 NOT DEFERRABLE
;
alter table inh_child2 no inherit inh_parent2
;
CREATE PUBLICATION tap_pub_A FOR TABLE tab_part2
;
-- null


-- clean up
DROP TABLE guid1, guid2, guid3 CASCADE
;
CREATE OPERATOR #%# (
   leftarg = int8,		-- right unary
   procedure = factorial
)
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_fortable_insert WITH (publish = 'insert')
;
----------
[CREATE_PROG_OBJ] -- polymorphic parameter test
create function dfunc(anyelement = 'World'::text) returns text as $$
  select 'Hello, ' || $1::text;
$$ language sql
;
----------
[DROP_INDEX] -- fail
drop index idxpart1_pkey
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_stmt after insert on parent
  for statement execute procedure trig_nothing()
;
----------
[CREATE_TABLE] CREATE TABLE tab_main(a int) PARTITION BY RANGE(a)
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_enabled=12)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema1 AUTHORIZATION CURRENT_USER
;
----------
[DROP_LIBRARY] drop extension test_ext9
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION strpos( citext, citext ) RETURNS INT AS $$
    SELECT pg_catalog.strpos( pg_catalog.lower( $1::pg_catalog.text ), pg_catalog.lower( $2::pg_catalog.text ) );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int8alias2eq"("public"."int8alias2", "public"."int8alias2") RETURNS boolean
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int8eq$$
;
----------
[RENAME_CONSTRAINT] ALTER TABLE ONLY constraint_rename_test RENAME CONSTRAINT con1 TO con1foo
;
----------
[ALTER_POLICY] ALTER POLICY p2 ON s2 USING (x % 2 = 0)
;
----------
[DROP_TABLE] drop table parted_trig
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "hash_f8_index_1" ON "public"."hash_f8_heap" USING "btree" ("abs"("random"))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_event_helper.capture_ddl_commands()
RETURNS event_trigger
LANGUAGE plpgsql
AS $event$
BEGIN
    INSERT INTO split_event_helper.ddl_log (
        classid, objid, objsubid, command_tag, object_type,
        schema_name, object_identity, in_extension
    )
    SELECT
        classid, objid, objsubid, command_tag, object_type,
        schema_name, object_identity, in_extension
    FROM pg_event_trigger_ddl_commands();
END;
$event$
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_52.pg_temp_29691_2 (tid pg_catalog.tid)
;
----------
[ADD_INDEX] create index on some_tab_child(f1, f2)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION postgres_fdw
;
----------
[CREATE_TABLE] CREATE TABLE d_listp (a int) PARTITION BY LIST(a)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON rls_tbl USING (a < 0)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_32.pg_temp_28710_2 ADD COLUMN newdata pg_temp_32.pg_temp_28710
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS audit_tbls.audit_tbls_schema_two_table_three
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_valid(ismn)
	RETURNS ismn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] create table pp_recpart (a pp_rectype) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_statistic_ext
;
----------
[CREATE_TABLE] CREATE TEMP TABLE past_inh_db_parent () ON COMMIT DELETE ROWS
;
----------
[CREATE_PROG_OBJ] --
-- Tests for ANYCOMPATIBLE polymorphism family
--

create function anyctest(anycompatible, anycompatible)
returns anycompatible as $$
  select greatest($1, $2)
$$ language sql
;
----------
[CREATE_PROG_OBJ] /* contrib/hstore/hstore--1.7--1.8.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION hstore_subscript_handler(internal)
RETURNS internal
AS '$libdir/hstore', 'hstore_subscript_handler'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CALL_PROG_OBJ] CALL transaction_test5b()
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_replica_identity2 ALTER COLUMN id TYPE bigint
;
----------
[ALTER_POLICY] ALTER POLICY r1 ON rec1 USING (x = (SELECT a FROM rec2 WHERE b = y))
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27543_index RENAME TO reind_con_toast_idx
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION tg_hub_adjustslots(bpchar, integer, integer) IS NULL
;
----------
[CREATE_TABLE] create table domtab (col1 integer)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt4_n_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ADMIN_TABLE] ANALYZE public.alpha_neg_p3
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_e_p1" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "S 1"."T 1" ADD CONSTRAINT c2positive CHECK (c2 >= 0)
;
----------
[DROP_TABLE] DROP TABLE orders
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_ll_coord(cube, int4)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE public.test ALTER COLUMN c SET STORAGE EXTERNAL
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX idx_replidentity_index_a ON tab_replidentity_index(a)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "regress_indexing"."pk51"
    ADD CONSTRAINT "pk51_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE FKTABLE ALTER CONSTRAINT fknd2 NOT DEFERRABLE
;
----------
[ALTER_TABLE] alter table inh_child2 no inherit inh_parent2
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_A FOR TABLE tab_part2
;
----------
[DROP_TABLE] -- null


-- clean up
DROP TABLE guid1, guid2, guid3 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #%# (
   leftarg = int8,		-- right unary
   procedure = factorial
)
;
