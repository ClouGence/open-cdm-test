alter table mlparted drop e
;
CREATE FUNCTION hash_metapage_info(IN page bytea,
    OUT magic int8,
    OUT version int8,
    OUT ntuples double precision,
    OUT ffactor int4,
    OUT bsize int4,
    OUT bmsize int4,
    OUT bmshift int4,
    OUT maxbucket int8,
    OUT highmask int8,
    OUT lowmask int8,
    OUT ovflpoint int8,
    OUT firstfree int8,
    OUT nmaps int8,
    OUT procid oid,
    OUT spares int8[],
    OUT mapp int8[])
AS '$libdir/pageinspect', 'hash_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE "public"."transition_table_status" (
    "level" integer NOT NULL,
    "node_no" integer NOT NULL,
    "status" integer
)
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "version" WITH "simple"
;
CREATE SEQUENCE regress_seq_dumpable
;
/*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
CREATE TABLE "public"."gtest21a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULLIF("a", 0)) STORED NOT NULL
)
;
CREATE TABLE measurement (
    city_id         int not null,
    logdate         date not null,
    peaktemp        int,
    unitsales       int
) WITH (autovacuum_enabled=off)
;
CREATE TABLE person (person_id int NOT NULL PRIMARY KEY, name text NOT NULL, is_project_manager bool NOT NULL)
;
ALTER TABLE list_parted ATTACH PARTITION part_fail FOR VALUES IN (2)
;
CREATE VIEW rw_view15 AS SELECT a, upper(b) FROM base_tbl
;
-- test access to exception data
create function zero_divide() returns int as $$
declare v int := 0;
begin
  return 10 / v;
end;
$$ language plpgsql
;
CREATE TABLE ref (ab int, cd int)
;
CREATE TABLE trunc_stats_test3(id serial, stuff text)
;
create function dfunc(a int, b int, c int = 0, d int = 0)
  returns table (a int, b int, c int, d int) as $$
  select $1, $2, $3, $4;
$$ language sql
;
create aggregate first_el_agg_f8(float8) (
  SFUNC = array_append,
  STYPE = float8[],
  FINALFUNC = first_el
)
;
create or replace function sillytrig() returns trigger language plpgsql as
$$begin
  return row(new.*);
end$$
;
CREATE DOMAIN dint4 AS int
;
drop function testrngfunc()
;
DROP DATABASE "foo

bar"
;
--
-- Verify behavior of before and after triggers with INSERT...ON CONFLICT
-- DO UPDATE
--
create table upsert (key int4 primary key, color text)
;
--
-- Btree index / opclass with the various operators
--
CREATE UNIQUE INDEX enumtest_btree ON enumtest USING btree (col)
;
DROP TRIGGER trig_stmt_after ON rem1
;
CREATE COLLATION s.coll (LOCALE="C")
;
CREATE TRIGGER before_upd_a_stmt_trig BEFORE UPDATE OF a ON main_table
FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func('before_upd_a_stmt')
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_trigger
;
CREATE TABLE datatype_table (
    id             SERIAL,
    id_big         BIGSERIAL,
    is_small       SMALLSERIAL,
    v_bytea        BYTEA,
    v_smallint     SMALLINT,
    v_int          INT,
    v_bigint       BIGINT,
    v_char         CHAR(1),
    v_varchar      VARCHAR(10),
    v_text         TEXT,
    v_bool         BOOLEAN,
    v_inet         INET,
    v_cidr         CIDR,
    v_macaddr      MACADDR,
    v_numeric      NUMERIC(1,0),
    v_real         REAL,
    v_float        FLOAT(1),
    v_float8       FLOAT8,
    v_money        MONEY,
    v_tsquery      TSQUERY,
    v_tsvector     TSVECTOR,
    v_date         DATE,
    v_time         TIME,
    v_time_tz      TIME WITH TIME ZONE,
    v_timestamp    TIMESTAMP,
    v_timestamp_tz TIMESTAMP WITH TIME ZONE,
    v_interval     INTERVAL,
    v_bit          BIT,
    v_bit4         BIT(4),
    v_varbit       VARBIT,
    v_varbit4      VARBIT(4),
    v_box          BOX,
    v_circle       CIRCLE,
    v_lseg         LSEG,
    v_path         PATH,
    v_point        POINT,
    v_polygon      POLYGON,
    v_json         JSON,
    v_xml          XML,
    v_uuid         UUID,
    v_pg_snapshot  pg_snapshot,
    v_enum         ENUM_TEST,
    v_postal_code  japanese_postal_code,
    v_int2range    int2range,
    PRIMARY KEY (id),
    UNIQUE (id_big)
)
;
create function mr_outparam_succeed4(i anyrange, out r anymultirange, out t text)
  as $$ select multirange($1), 'foo'::text $$ language sql
;
CREATE TRIGGER trig_upd_pk AFTER UPDATE ON fkpart11.pk FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
CREATE TABLESPACE regress_tblspacewith LOCATION '' WITH (random_page_cost = 3.0)
;
ALTER TABLE pg_temp_4.pg_temp_37874_2 ADD COLUMN newdata pg_temp_4.pg_temp_37874
;
ANALYZE pg_catalog.pg_conversion
;
-- cannot create a partition that says column b is allowed to range
-- from -infinity to +infinity, while there exist partitions that have
-- more specific ranges
CREATE TABLE fail_part PARTITION OF range_parted3 FOR VALUES FROM (1, minvalue) TO (1, maxvalue)
;
CALL p1(v_cnt := v_cnt, v_Text := v_Text)
;
CREATE TABLE "public"."pagg_tab1_p1" (
    "x" integer,
    "y" integer
)
;
ALTER TABLE test_type_diff2_c1 INHERIT test_type_diff2
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_seclabel
;
CREATE TABLE tab1 (c text, a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
CREATE TABLE d_star (
	d 			float8
) INHERITS (b_star, c_star)
;
ANALYZE fpagg_tab_p3
;
-- Verify NOT NULL VALID/NOT VALID with partition table.
DROP TABLE notnull_tbl1
;
DROP TABLE tenant2_table
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_subscription
;
CREATE TABLE atestp1 (f1 int, f2 int)
;
-- failure: replica identity has to be covered by the column list
ALTER TABLE testpub_tbl8_1 DROP CONSTRAINT testpub_tbl8_1_pkey
;
CREATE TABLE part_p1 PARTITION OF part FOR VALUES IN (-2,-1,0,1,2)
;
CREATE FUNCTION check_primary_key()
RETURNS trigger
AS '$libdir/refint'
LANGUAGE C
;
CREATE DATABASE "regression	 !""x""#$%&'()*+-./0123456"
;
ALTER SUBSCRIPTION regress_testsub RENAME TO regress_testsub2
;
create table renameColumn (a int)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|DROP_COLUMN] alter table mlparted drop e
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hash_metapage_info(IN page bytea,
    OUT magic int8,
    OUT version int8,
    OUT ntuples double precision,
    OUT ffactor int4,
    OUT bsize int4,
    OUT bmsize int4,
    OUT bmshift int4,
    OUT maxbucket int8,
    OUT highmask int8,
    OUT lowmask int8,
    OUT ovflpoint int8,
    OUT firstfree int8,
    OUT nmaps int8,
    OUT procid oid,
    OUT spares int8[],
    OUT mapp int8[])
AS '$libdir/pageinspect', 'hash_metapage_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."transition_table_status" (
    "level" integer NOT NULL,
    "node_no" integer NOT NULL,
    "status" integer
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "version" WITH "simple"
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE regress_seq_dumpable
;
----------
[CREATE_POLICY] /*
 * text search configuration for turkish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * turkish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY turkish_stem
	(TEMPLATE = snowball, Language = turkish , StopWords=turkish)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest21a" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (NULLIF("a", 0)) STORED NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE measurement (
    city_id         int not null,
    logdate         date not null,
    peaktemp        int,
    unitsales       int
) WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE] CREATE TABLE person (person_id int NOT NULL PRIMARY KEY, name text NOT NULL, is_project_manager bool NOT NULL)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted ATTACH PARTITION part_fail FOR VALUES IN (2)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view15 AS SELECT a, upper(b) FROM base_tbl
;
----------
[CREATE_PROG_OBJ] -- test access to exception data
create function zero_divide() returns int as $$
declare v int := 0;
begin
  return 10 / v;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE ref (ab int, cd int)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_stats_test3(id serial, stuff text)
;
----------
[CREATE_PROG_OBJ] create function dfunc(a int, b int, c int = 0, d int = 0)
  returns table (a int, b int, c int, d int) as $$
  select $1, $2, $3, $4;
$$ language sql
;
----------
[CREATE_PROG_OBJ] create aggregate first_el_agg_f8(float8) (
  SFUNC = array_append,
  STYPE = float8[],
  FINALFUNC = first_el
)
;
----------
[CREATE_PROG_OBJ] create or replace function sillytrig() returns trigger language plpgsql as
$$begin
  return row(new.*);
end$$
;
----------
[CREATE_TYPE] CREATE DOMAIN dint4 AS int
;
----------
[DROP_PROG_OBJ] drop function testrngfunc()
;
----------
[DROP_CATALOG] DROP DATABASE "foo

bar"
;
----------
[CREATE_TABLE] --
-- Verify behavior of before and after triggers with INSERT...ON CONFLICT
-- DO UPDATE
--
create table upsert (key int4 primary key, color text)
;
----------
[ADD_INDEX] --
-- Btree index / opclass with the various operators
--
CREATE UNIQUE INDEX enumtest_btree ON enumtest USING btree (col)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_stmt_after ON rem1
;
----------
[CREATE_POLICY] CREATE COLLATION s.coll (LOCALE="C")
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER before_upd_a_stmt_trig BEFORE UPDATE OF a ON main_table
FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func('before_upd_a_stmt')
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_trigger
;
----------
[CREATE_TABLE] CREATE TABLE datatype_table (
    id             SERIAL,
    id_big         BIGSERIAL,
    is_small       SMALLSERIAL,
    v_bytea        BYTEA,
    v_smallint     SMALLINT,
    v_int          INT,
    v_bigint       BIGINT,
    v_char         CHAR(1),
    v_varchar      VARCHAR(10),
    v_text         TEXT,
    v_bool         BOOLEAN,
    v_inet         INET,
    v_cidr         CIDR,
    v_macaddr      MACADDR,
    v_numeric      NUMERIC(1,0),
    v_real         REAL,
    v_float        FLOAT(1),
    v_float8       FLOAT8,
    v_money        MONEY,
    v_tsquery      TSQUERY,
    v_tsvector     TSVECTOR,
    v_date         DATE,
    v_time         TIME,
    v_time_tz      TIME WITH TIME ZONE,
    v_timestamp    TIMESTAMP,
    v_timestamp_tz TIMESTAMP WITH TIME ZONE,
    v_interval     INTERVAL,
    v_bit          BIT,
    v_bit4         BIT(4),
    v_varbit       VARBIT,
    v_varbit4      VARBIT(4),
    v_box          BOX,
    v_circle       CIRCLE,
    v_lseg         LSEG,
    v_path         PATH,
    v_point        POINT,
    v_polygon      POLYGON,
    v_json         JSON,
    v_xml          XML,
    v_uuid         UUID,
    v_pg_snapshot  pg_snapshot,
    v_enum         ENUM_TEST,
    v_postal_code  japanese_postal_code,
    v_int2range    int2range,
    PRIMARY KEY (id),
    UNIQUE (id_big)
)
;
----------
[CREATE_PROG_OBJ] create function mr_outparam_succeed4(i anyrange, out r anymultirange, out t text)
  as $$ select multirange($1), 'foo'::text $$ language sql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_upd_pk AFTER UPDATE ON fkpart11.pk FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_tblspacewith LOCATION '' WITH (random_page_cost = 3.0)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_37874_2 ADD COLUMN newdata pg_temp_4.pg_temp_37874
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_conversion
;
----------
[CREATE_TABLE] -- cannot create a partition that says column b is allowed to range
-- from -infinity to +infinity, while there exist partitions that have
-- more specific ranges
CREATE TABLE fail_part PARTITION OF range_parted3 FOR VALUES FROM (1, minvalue) TO (1, maxvalue)
;
----------
[CALL_PROG_OBJ] CALL p1(v_cnt := v_cnt, v_Text := v_Text)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab1_p1" (
    "x" integer,
    "y" integer
)
;
----------
[ALTER_TABLE] ALTER TABLE test_type_diff2_c1 INHERIT test_type_diff2
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_seclabel
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (c text, a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] CREATE TABLE d_star (
	d 			float8
) INHERITS (b_star, c_star)
;
----------
[ADMIN_TABLE] ANALYZE fpagg_tab_p3
;
----------
[DROP_TABLE] -- Verify NOT NULL VALID/NOT VALID with partition table.
DROP TABLE notnull_tbl1
;
----------
[DROP_TABLE] DROP TABLE tenant2_table
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] CREATE TABLE atestp1 (f1 int, f2 int)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- failure: replica identity has to be covered by the column list
ALTER TABLE testpub_tbl8_1 DROP CONSTRAINT testpub_tbl8_1_pkey
;
----------
[CREATE_TABLE] CREATE TABLE part_p1 PARTITION OF part FOR VALUES IN (-2,-1,0,1,2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION check_primary_key()
RETURNS trigger
AS '$libdir/refint'
LANGUAGE C
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression	 !""x""#$%&'()*+-./0123456"
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub RENAME TO regress_testsub2
;
----------
[CREATE_TABLE] create table renameColumn (a int)
;
