create table foo (f1 cube, f2 int)
;
--
-- CREATE TABLE AS/SELECT INTO as last command in a SQL function
-- have been known to cause problems
--
CREATE FUNCTION make_table() RETURNS VOID
AS $$
  CREATE TABLE created_table AS SELECT * FROM int8_tbl;
$$ LANGUAGE SQL
;
ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        11       pg_catalog.= (text, text)
;
CREATE INDEX "iprt2_p1_b" ON "public"."prt2_p1" USING "btree" ("b")
;
CREATE TABLESPACE regress_ts1 LOCATION '/src/src/test/modules/test_misc/tmp_check/t_002_tablespace_main_data/ts1'
;
ALTER DATABASE "ecpg2_regression" SET timezone_abbreviations TO 'Default'
;
ANALYZE pg_temp_3.pg_temp_27281
;
CREATE PUBLICATION pub FOR TABLE target_tbl
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN IF NOT EXISTS c3 integer primary key
;
create function lock_share(bigint) returns int language internal as
  'pg_advisory_xact_lock_shared_int8' strict parallel safe
;
drop table other_partitioned_fk
;
ALTER TABLE vac_option_tab ALTER COLUMN t SET STORAGE EXTERNAL
;
-- can change stype:
CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = numeric,
	sfunc = numeric_add
)
;
CREATE TABLE a (a1 int)
;
CREATE FUNCTION binary_coercible(oid, oid)
    RETURNS bool
    AS '/src/src/test/regress/regress.so', 'binary_coercible'
    LANGUAGE C STRICT STABLE PARALLEL SAFE
;
CALL transaction_test5()
;
CREATE TRIGGER before_upd_stmt_trig BEFORE UPDATE ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('before_view_upd_stmt')
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub7
;
ALTER TABLE pg_toast.pg_toast_28321 RENAME TO reind_con_toast
;
create table trans_tab_parent (a text) partition by list (a)
;
CALL test_proc7(100, -1, -1)
;
ALTER DATABASE "ecpg2_regression" SET lc_time TO 'C'
;
CREATE TABLE "public"."prt2_l" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("b")
;
CREATE FUNCTION isneq(upc, upc)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_statistic
;
-- test notice about expensive referential integrity checks,
-- where the index cannot be used because of type incompatibilities.

CREATE TEMP TABLE pktable (
        id1     INT4 PRIMARY KEY,
        id2     VARCHAR(4) UNIQUE,
        id3     REAL UNIQUE,
        UNIQUE(id1, id2, id3)
)
;
CREATE TABLE plt2_adv_p2_2 PARTITION OF plt2_adv FOR VALUES IN ('0006')
;
CREATE TABLE "public"."idxpart31" (
    "a" integer
)
;
-- Not a token supported by the configuration's parser, fails.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING FOR not_a_token, not_a_token
;
CREATE OPERATOR CLASS int4_unique_ops FOR TYPE int4 USING btree AS
		OPERATOR 1 < (int4, int4), OPERATOR 2 <= (int4, int4),
		OPERATOR 3 = (int4, int4), OPERATOR 4 >= (int4, int4),
		OPERATOR 5 > (int4, int4), FUNCTION 1 ok_cmp(int4, int4)
;
TRUNCATE ft2
;
drop table twophase_tab
;
-- not supported

CREATE RULE regtest_test_rule AS ON INSERT TO regtest_table_3 DO ALSO NOTHING
;
CREATE FUNCTION bad (f1 int, out f2 anyelement, out f3 anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
create table rules_log(f1 int, f2 int, tag text, id serial)
;
CREATE TABLE batch_table ( x int, field1 text, field2 text) PARTITION BY HASH (x)
;
CREATE OPERATOR @@ (
	LEFTARG = _int4,
	RIGHTARG = query_int,
	PROCEDURE = boolop,
	COMMUTATOR = '~~',
	RESTRICT = _int_matchsel,
	JOIN = contjoinsel
)
;
DROP TABLE pg_temp_19.pg_temp_33778_2, pg_temp_19.pg_temp_33778
;
CREATE INDEX spgist_idx ON gist_idx_tbl USING spgist (p)
;
CREATE TABLE split_trigger_probe.accounts (
    id integer PRIMARY KEY,
    balance integer,
    note text
)
;
DROP FUNCTION pg_buffercache_evict(integer)
;
-- ERROR
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c9_check CHECK (c9 < 0) NOT VALID
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN ((1+1) collate "POSIX")
;
CREATE TABLE pt_part2 PARTITION OF partitioned_table FOR VALUES IN (2)
;
alter index idxpart_1_idx attach partition idxpart1_1_idx
;
ALTER TABLE idxpart1 ADD PRIMARY KEY (c1, c2)
;
drop table inh_parent, inh_child, inh_grandchild
;
create rule rule_and_refint_t3_ins as on insert to rule_and_refint_t3
	where (exists (select 1 from rule_and_refint_t3
			where (((rule_and_refint_t3.id3a = new.id3a)
			and (rule_and_refint_t3.id3b = new.id3b))
			and (rule_and_refint_t3.id3c = new.id3c))))
	do instead update rule_and_refint_t3 set data = new.data
	where (((rule_and_refint_t3.id3a = new.id3a)
	and (rule_and_refint_t3.id3b = new.id3b))
	and (rule_and_refint_t3.id3c = new.id3c))
;
CREATE INDEX base_tbl1_idx ON base_tbl1 (a)
;
ALTER TABLE attmp ADD COLUMN x float8[]
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table foo (f1 cube, f2 int)
;
----------
[CREATE_PROG_OBJ] --
-- CREATE TABLE AS/SELECT INTO as last command in a SQL function
-- have been known to cause problems
--
CREATE FUNCTION make_table() RETURNS VOID
AS $$
  CREATE TABLE created_table AS SELECT * FROM int8_tbl;
$$ LANGUAGE SQL
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        11       pg_catalog.= (text, text)
;
----------
[ADD_INDEX] CREATE INDEX "iprt2_p1_b" ON "public"."prt2_p1" USING "btree" ("b")
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_ts1 LOCATION '/src/src/test/modules/test_misc/tmp_check/t_002_tablespace_main_data/ts1'
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg2_regression" SET timezone_abbreviations TO 'Default'
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27281
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub FOR TABLE target_tbl
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN IF NOT EXISTS c3 integer primary key
;
----------
[CREATE_PROG_OBJ] create function lock_share(bigint) returns int language internal as
  'pg_advisory_xact_lock_shared_int8' strict parallel safe
;
----------
[DROP_TABLE] drop table other_partitioned_fk
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE vac_option_tab ALTER COLUMN t SET STORAGE EXTERNAL
;
----------
[CREATE_PROG_OBJ] -- can change stype:
CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = numeric,
	sfunc = numeric_add
)
;
----------
[CREATE_TABLE] CREATE TABLE a (a1 int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION binary_coercible(oid, oid)
    RETURNS bool
    AS '/src/src/test/regress/regress.so', 'binary_coercible'
    LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CALL_PROG_OBJ] CALL transaction_test5()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER before_upd_stmt_trig BEFORE UPDATE ON main_view
FOR EACH STATEMENT EXECUTE PROCEDURE view_trigger('before_view_upd_stmt')
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub7
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28321 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] create table trans_tab_parent (a text) partition by list (a)
;
----------
[CALL_PROG_OBJ] CALL test_proc7(100, -1, -1)
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg2_regression" SET lc_time TO 'C'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_l" (
    "a" integer,
    "b" integer,
    "c" character varying
)
PARTITION BY RANGE ("b")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(upc, upc)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_statistic
;
----------
[CREATE_TABLE] -- test notice about expensive referential integrity checks,
-- where the index cannot be used because of type incompatibilities.

CREATE TEMP TABLE pktable (
        id1     INT4 PRIMARY KEY,
        id2     VARCHAR(4) UNIQUE,
        id3     REAL UNIQUE,
        UNIQUE(id1, id2, id3)
)
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_p2_2 PARTITION OF plt2_adv FOR VALUES IN ('0006')
;
----------
[CREATE_TABLE] CREATE TABLE "public"."idxpart31" (
    "a" integer
)
;
----------
[ALTER_POLICY] -- Not a token supported by the configuration's parser, fails.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING FOR not_a_token, not_a_token
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS int4_unique_ops FOR TYPE int4 USING btree AS
		OPERATOR 1 < (int4, int4), OPERATOR 2 <= (int4, int4),
		OPERATOR 3 = (int4, int4), OPERATOR 4 >= (int4, int4),
		OPERATOR 5 > (int4, int4), FUNCTION 1 ok_cmp(int4, int4)
;
----------
[TRUNCATE_TABLE] TRUNCATE ft2
;
----------
[DROP_TABLE] drop table twophase_tab
;
----------
[CREATE_POLICY] -- not supported

CREATE RULE regtest_test_rule AS ON INSERT TO regtest_table_3 DO ALSO NOTHING
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bad (f1 int, out f2 anyelement, out f3 anyarray)
AS 'select $1, array[$1,$1]' LANGUAGE sql
;
----------
[CREATE_TABLE] create table rules_log(f1 int, f2 int, tag text, id serial)
;
----------
[CREATE_TABLE] CREATE TABLE batch_table ( x int, field1 text, field2 text) PARTITION BY HASH (x)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @@ (
	LEFTARG = _int4,
	RIGHTARG = query_int,
	PROCEDURE = boolop,
	COMMUTATOR = '~~',
	RESTRICT = _int_matchsel,
	JOIN = contjoinsel
)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_19.pg_temp_33778_2, pg_temp_19.pg_temp_33778
;
----------
[ADD_INDEX] CREATE INDEX spgist_idx ON gist_idx_tbl USING spgist (p)
;
----------
[CREATE_TABLE] CREATE TABLE split_trigger_probe.accounts (
    id integer PRIMARY KEY,
    balance integer,
    note text
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION pg_buffercache_evict(integer)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- ERROR
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c9_check CHECK (c9 < 0) NOT VALID
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN ((1+1) collate "POSIX")
;
----------
[CREATE_TABLE] CREATE TABLE pt_part2 PARTITION OF partitioned_table FOR VALUES IN (2)
;
----------
[ALTER_INDEX] alter index idxpart_1_idx attach partition idxpart1_1_idx
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE idxpart1 ADD PRIMARY KEY (c1, c2)
;
----------
[DROP_TABLE] drop table inh_parent, inh_child, inh_grandchild
;
----------
[CREATE_POLICY] create rule rule_and_refint_t3_ins as on insert to rule_and_refint_t3
	where (exists (select 1 from rule_and_refint_t3
			where (((rule_and_refint_t3.id3a = new.id3a)
			and (rule_and_refint_t3.id3b = new.id3b))
			and (rule_and_refint_t3.id3c = new.id3c))))
	do instead update rule_and_refint_t3 set data = new.data
	where (((rule_and_refint_t3.id3a = new.id3a)
	and (rule_and_refint_t3.id3b = new.id3b))
	and (rule_and_refint_t3.id3c = new.id3c))
;
----------
[ADD_INDEX] CREATE INDEX base_tbl1_idx ON base_tbl1 (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN x float8[]
;
