CREATE TABLE fail_part PARTITION OF list_parted FOR VALUES FROM (1) TO (2)
;
VACUUM (SKIP_DATABASE_STATS, FULL) information_schema.sql_sizing
;
-- error cases
CREATE PUBLICATION testpub_xxx WITH (foo)
;
ALTER TABLE partitioned ALTER COLUMN b TYPE char(5)
;
CREATE TABLE tab_freeze (
    id int PRIMARY KEY,
    name char(3),
    x int)
;
CREATE INDEX i1a ON t1 USING btree (a)
;
ALTER TABLE sub_parted ATTACH PARTITION sub_part1 for VALUES in (1)
;
create view tt21v as
select * from tt5 natural inner join tt6
;
drop type intr_multirange
;
CREATE UNLOGGED TABLE public.unlogged2 (a int primary key)
;
-- 6.31 ELEMENT_TYPES view appears later.


/*
 * 6.32
 * ENABLED_ROLES view
 */

CREATE VIEW enabled_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS role_name
    FROM pg_authid a
    WHERE pg_has_role(a.oid, 'USAGE')
;
-- keep these tests aligned with generated_virtual.sql


CREATE SCHEMA generated_stored_tests
;
CREATE TABLE jointchild () INHERITS (firstparent, secondparent)
;
ALTER TYPE planets ADD VALUE 'uranus'
;
-- behave reasonably if someone tries to modify a transition table
CREATE FUNCTION transition_table_level2_bad_usage_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
  BEGIN
    INSERT INTO dx VALUES (1000000, 1000000, 'x');
    RETURN NULL;
  END;
$$
;
ALTER TABLE prt2 ATTACH PARTITION prt2_p3 DEFAULT
;
-- Overloaded functions.
CREATE OR REPLACE PROCEDURE overload(i int) AS $$
DECLARE
  r int;
BEGIN
  SELECT (i + i)::int INTO r;
END; $$ LANGUAGE plpgsql
;
DROP SCHEMA evttrig CASCADE
;
DROP OPERATOR FAMILY alt_opf19 USING btree
;
VACUUM (FREEZE) pg_catalog.pg_am
;
VACUUM (FREEZE) pg_catalog.pg_policy
;
-- REFRESH of this MV will queue a GRANT at end of transaction
CREATE MATERIALIZED VIEW sro_mv AS SELECT mv_action() WITH NO DATA
;
drop event trigger if exists regress_event_trigger2
;
-- Try a non-verified CHECK constraint
ALTER TABLE attmp3 ADD CONSTRAINT b_greater_than_ten CHECK (b > 10)
;
CREATE TABLE copy_stats (a int, b int)
;
CREATE DATABASE slotsync_test_db
;
ALTER TABLE toast_3b_utf8 RENAME TO toast_4b_utf8
;
ALTER TABLE pg_auth_members ADD PRIMARY KEY USING INDEX pg_auth_members_oid_index
;
CREATE INDEX concur_reindex_part_index_0_1 ON ONLY concur_reindex_part_0_1 (c1)
;
alter table another
  alter f1 type text using f2 || ' more',
  alter f2 type bigint using f1 * 10
;
VACUUM vacowned_part2
;
create table gist_point_tbl(id int4, p point)
;
-- can't change return type:
CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = numeric,
	sfunc = numeric_add,
	finalfunc = numeric_out
)
;
ALTER INDEX btree_part_idx ALTER COLUMN id SET (n_distinct=100)
;
DROP SCHEMA fkpart11 CASCADE
;
CREATE FUNCTION seg_gt(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER INDEX part_attmp1_index RENAME TO fail
;
ALTER OPERATOR FAMILY gist_int2_ops USING gist ADD
	OPERATOR	6	<> (int2, int2) ,
	OPERATOR	15	<-> (int2, int2) FOR ORDER BY pg_catalog.integer_ops ,
	FUNCTION	8 (int2, int2) gbt_int2_distance (internal, int2, int2, oid, internal) ,
	FUNCTION	9 (int2, int2) gbt_int2_fetch (internal)
;
DROP INDEX e
;
alter table cnn2_parted add primary key (a)
;
-- now it works
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, disable_on_error = false)
;
ALTER EXTENSION test_pg_dump DROP FOREIGN DATA WRAPPER dummy
;
drop function duplic(anyelement)
;
create function rttest()
returns setof int as $$
declare rc int;
begin
  return query values(10),(20);
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query select * from (values(10),(20)) f(a) where false;
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query execute 'values(10),(20)';
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query execute 'select * from (values(10),(20)) f(a) where false';
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
end;
$$ language plpgsql
;
CREATE FUNCTION vol(text) returns text as
  'begin return $1; end' language plpgsql volatile
;
CREATE COLLATION ignore_accents (provider = icu, locale = '@colStrength=primary;colCaseLevel=yes', deterministic = false)
;
DROP FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int4,
    OUT level int4,
    OUT fastroot int4,
    OUT fastlevel int4)
;
DROP TABLE zerocol
;
DROP POLICY p1 ON document
;
create or replace function foreach_test(anyarray)
returns void as $$
declare r record;
begin
  foreach r in array $1
  loop
    raise notice '%', r;
  end loop;
  end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF list_parted FOR VALUES FROM (1) TO (2)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) information_schema.sql_sizing
;
----------
[CREATE_PUB_SUB] -- error cases
CREATE PUBLICATION testpub_xxx WITH (foo)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE partitioned ALTER COLUMN b TYPE char(5)
;
----------
[CREATE_TABLE] CREATE TABLE tab_freeze (
    id int PRIMARY KEY,
    name char(3),
    x int)
;
----------
[ADD_INDEX] CREATE INDEX i1a ON t1 USING btree (a)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE sub_parted ATTACH PARTITION sub_part1 for VALUES in (1)
;
----------
[CREATE_VIEW(SELECT)] create view tt21v as
select * from tt5 natural inner join tt6
;
----------
[DROP_TYPE] drop type intr_multirange
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE public.unlogged2 (a int primary key)
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- 6.31 ELEMENT_TYPES view appears later.


/*
 * 6.32
 * ENABLED_ROLES view
 */

CREATE VIEW enabled_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS role_name
    FROM pg_authid a
    WHERE pg_has_role(a.oid, 'USAGE')
;
----------
[CREATE_SCHEMA] -- keep these tests aligned with generated_virtual.sql


CREATE SCHEMA generated_stored_tests
;
----------
[CREATE_TABLE] CREATE TABLE jointchild () INHERITS (firstparent, secondparent)
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE 'uranus'
;
----------
[CREATE_PROG_OBJ] -- behave reasonably if someone tries to modify a transition table
CREATE FUNCTION transition_table_level2_bad_usage_func()
  RETURNS TRIGGER
  LANGUAGE plpgsql
AS $$
  BEGIN
    INSERT INTO dx VALUES (1000000, 1000000, 'x');
    RETURN NULL;
  END;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt2 ATTACH PARTITION prt2_p3 DEFAULT
;
----------
[CREATE_PROG_OBJ] -- Overloaded functions.
CREATE OR REPLACE PROCEDURE overload(i int) AS $$
DECLARE
  r int;
BEGIN
  SELECT (i + i)::int INTO r;
END; $$ LANGUAGE plpgsql
;
----------
[DROP_SCHEMA] DROP SCHEMA evttrig CASCADE
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY alt_opf19 USING btree
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_am
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_policy
;
----------
[CREATE_VIEW(SELECT)] -- REFRESH of this MV will queue a GRANT at end of transaction
CREATE MATERIALIZED VIEW sro_mv AS SELECT mv_action() WITH NO DATA
;
----------
[DROP_TRIGGER] drop event trigger if exists regress_event_trigger2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Try a non-verified CHECK constraint
ALTER TABLE attmp3 ADD CONSTRAINT b_greater_than_ten CHECK (b > 10)
;
----------
[CREATE_TABLE] CREATE TABLE copy_stats (a int, b int)
;
----------
[CREATE_CATALOG] CREATE DATABASE slotsync_test_db
;
----------
[RENAME_TABLE] ALTER TABLE toast_3b_utf8 RENAME TO toast_4b_utf8
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_auth_members ADD PRIMARY KEY USING INDEX pg_auth_members_oid_index
;
----------
[ADD_INDEX] CREATE INDEX concur_reindex_part_index_0_1 ON ONLY concur_reindex_part_0_1 (c1)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table another
  alter f1 type text using f2 || ' more',
  alter f2 type bigint using f1 * 10
;
----------
[ADMIN_TABLE] VACUUM vacowned_part2
;
----------
[CREATE_TABLE] create table gist_point_tbl(id int4, p point)
;
----------
[CREATE_PROG_OBJ] -- can't change return type:
CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = numeric,
	sfunc = numeric_add,
	finalfunc = numeric_out
)
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX btree_part_idx ALTER COLUMN id SET (n_distinct=100)
;
----------
[DROP_SCHEMA] DROP SCHEMA fkpart11 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_gt(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX part_attmp1_index RENAME TO fail
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_int2_ops USING gist ADD
	OPERATOR	6	<> (int2, int2) ,
	OPERATOR	15	<-> (int2, int2) FOR ORDER BY pg_catalog.integer_ops ,
	FUNCTION	8 (int2, int2) gbt_int2_distance (internal, int2, int2, oid, internal) ,
	FUNCTION	9 (int2, int2) gbt_int2_fetch (internal)
;
----------
[DROP_INDEX] DROP INDEX e
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table cnn2_parted add primary key (a)
;
----------
[CREATE_PUB_SUB] -- now it works
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, disable_on_error = false)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump DROP FOREIGN DATA WRAPPER dummy
;
----------
[DROP_PROG_OBJ] drop function duplic(anyelement)
;
----------
[CREATE_PROG_OBJ] create function rttest()
returns setof int as $$
declare rc int;
begin
  return query values(10),(20);
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query select * from (values(10),(20)) f(a) where false;
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query execute 'values(10),(20)';
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
  return query execute 'select * from (values(10),(20)) f(a) where false';
  get diagnostics rc = row_count;
  raise notice '% %', found, rc;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION vol(text) returns text as
  'begin return $1; end' language plpgsql volatile
;
----------
[CREATE_POLICY] CREATE COLLATION ignore_accents (provider = icu, locale = '@colStrength=primary;colCaseLevel=yes', deterministic = false)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int4,
    OUT level int4,
    OUT fastroot int4,
    OUT fastlevel int4)
;
----------
[DROP_TABLE] DROP TABLE zerocol
;
----------
[DROP_POLICY] DROP POLICY p1 ON document
;
----------
[CREATE_PROG_OBJ] create or replace function foreach_test(anyarray)
returns void as $$
declare r record;
begin
  foreach r in array $1
  loop
    raise notice '%', r;
  end loop;
  end;
$$ language plpgsql
;
