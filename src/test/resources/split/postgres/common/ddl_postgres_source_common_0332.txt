create function hashint8alias1(int8alias1) returns int
  strict immutable language internal as 'hashint8'
;
---------------------------------------------------
-- ISBN13:
CREATE FUNCTION btisbn13cmp(isbn13, isbn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM ANALYZE num_exp_sub
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=30195 host=/tmp/7ZQ3PdhFoo dbname=postgres' PUBLICATION mypub
;
ALTER TABLE ataddindex
  ADD PRIMARY KEY (id),
  ADD FOREIGN KEY (ref_id) REFERENCES ataddindex
;
VACUUM pg_catalog.pg_attribute
;
-- Verify propagation of trigger arguments to partitions
create table parted_trig (a int) partition by list (a)
;
do $$
begin
  assert NULL;  -- should fail
end;
$$
;
ANALYZE regress_indexing.pk3
;
ALTER INDEX pg_toast.pg_toast_28201_index RENAME TO reind_con_toast_idx
;
-- fail


-- backwards parsing

CREATE VIEW collview1 AS SELECT * FROM collate_test1 WHERE b COLLATE "C" >= 'bbc'
;
-- Valid INSTEAD OF triggers
CREATE TRIGGER instead_of_insert_trig INSTEAD OF INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_ins')
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '14683'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
CREATE FUNCTION dummy_trigger() RETURNS TRIGGER AS $$
  BEGIN
    RETURN NULL;
  END
$$ language plpgsql
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
ALTER TABLE pg_toast.pg_toast_30454 RENAME TO reind_con_toast
;
create table mcrparted3_c_to_common partition of mcrparted for values from ('c', minvalue) to ('common', minvalue)
;
VACUUM (PARALLEL 2) pg_catalog.pg_ts_config_map
;
-- Test defaults with array assignments
create table base_tab (a serial, b int[], c text, d text default 'Table default')
;
alter table rlp attach partition rlp3 for values from (15) to (20)
;
-- Boolean
ALTER INDEX dummy_test_idx SET (option_bool = 4)
;
create table 計算機用語 (用語 text, 分類コード varchar, 備考1Aだよ char(16))
;
CREATE TABLE "public"."quad_box_tbl_ord_seq2" (
    "n" bigint,
    "dist" double precision,
    "id" integer
)
;
CREATE OR REPLACE FUNCTION gen_subxids (n integer)
 RETURNS VOID
 LANGUAGE plpgsql
AS $$
BEGIN
  IF n <= 0 THEN
	UPDATE subxids SET val = 1 WHERE subx = 0;
    RETURN;
  ELSE
    PERFORM gen_subxids(n - 1);
    RETURN;
  END IF;
EXCEPTION /* generates a subxid */
  WHEN raise_exception THEN NULL;
END;
$$
;
CREATE RULE "rtest_order_r4" AS
    ON INSERT TO "public"."rtest_order1"
   WHERE ("new"."a" < 100) DO INSTEAD  INSERT INTO "public"."rtest_order2" ("a", "b", "c")
  VALUES ("new"."a", "nextval"('"public"."rtest_seq"'::"regclass"), 'rule 4 - this should run 4th'::"text")
;
CREATE TABLE bigintp (
	a bigint
) PARTITION BY LIST (a)
;
create table pt (category int, sdata seg, tdata text)
  partition by list (category)
;
CREATE SCHEMA testns5
;
CREATE SUBSCRIPTION regress_mysub2 CONNECTION 'port=29713 host=/tmp/jOoI3lrZrW dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub2_slot)
;
CREATE FOREIGN TABLE batch_table_p0f
	PARTITION OF batch_table
	FOR VALUES WITH (MODULUS 3, REMAINDER 0)
	SERVER loopback
	OPTIONS (table_name 'batch_table_p0', batch_size '10')
;
create view insertconflictview as select * from insertconflicttest
;
--
-- Test polymorphic type system
--

create function anyarray_anyrange_func(a anyarray, r anyrange)
  returns anyelement as 'select $1[1] + lower($2);' language sql
;
-- Check support of both 1B and 4B header sizes of short varlena datum
CREATE TABLE varlena_bug (v text)
;
/* src/pl/plpgsql/src/plpgsql--1.0.sql */

CREATE FUNCTION plpgsql_call_handler() RETURNS language_handler
  LANGUAGE c AS '$libdir/plpgsql'
;
ANALYZE public.part_7_a_null
;
CREATE TEXT SEARCH TEMPLATE dump_test.alt_ts_temp1 (lexize=dsimple_lexize)
;
create temp table prtx1_1 partition of prtx1 for values from (1) to (11)
;
CREATE TEXT SEARCH CONFIGURATION "public"."hunspell_tst" (
    PARSER = "pg_catalog"."default" )
;
CREATE FOREIGN TABLE ft1 (
	c0 int,
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10) default 'ft1',
	c8 user_enum
) SERVER loopback
;
DROP SCHEMA IF EXISTS split_extension_next_moved CASCADE
;
CREATE TABLE split_table_idx.method_table (
    id integer,
    payload text
) USING heap WITH (fillfactor = 80)
;
CREATE INDEX tbl_include_reg_idx ON tbl_include_reg (c1, c2) INCLUDE (c3, c4)
;
create domain mynums as numeric(4,2)[1]
;
CREATE SEQUENCE test_sequence
;
ALTER TABLE fktable ADD CONSTRAINT fk_123_231
FOREIGN KEY (x1,x2,x3) REFERENCES pktable(id2,id3,id1)
;
CREATE INDEX "gtest22c_pred_idx" ON "public"."gtest22c" USING "btree" ("a") WHERE ("b" > 0)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_subscription
;
ANALYZE public.quad_box_tbl_ord_seq1
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_user_mapping
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=25972 host=/tmp/pOsjRHdBUp dbname=postgres' PUBLICATION pub1
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create function hashint8alias1(int8alias1) returns int
  strict immutable language internal as 'hashint8'
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISBN13:
CREATE FUNCTION btisbn13cmp(isbn13, isbn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM ANALYZE num_exp_sub
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=30195 host=/tmp/7ZQ3PdhFoo dbname=postgres' PUBLICATION mypub
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ataddindex
  ADD PRIMARY KEY (id),
  ADD FOREIGN KEY (ref_id) REFERENCES ataddindex
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_attribute
;
----------
[CREATE_TABLE] -- Verify propagation of trigger arguments to partitions
create table parted_trig (a int) partition by list (a)
;
----------
[BLOCK] do $$
begin
  assert NULL;  -- should fail
end;
$$
;
----------
[ADMIN_TABLE] ANALYZE regress_indexing.pk3
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28201_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] -- fail


-- backwards parsing

CREATE VIEW collview1 AS SELECT * FROM collate_test1 WHERE b COLLATE "C" >= 'bbc'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Valid INSTEAD OF triggers
CREATE TRIGGER instead_of_insert_trig INSTEAD OF INSERT ON main_view
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_ins')
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_stat_activity where pid = '
    '14683'
  ') > 0 loop c := c + 1; perform pg_stat_clear_snapshot(); end loop;
  raise log ''test_extensions looped % times'', c;
end'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dummy_trigger() RETURNS TRIGGER AS $$
  BEGIN
    RETURN NULL;
  END
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30454 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] create table mcrparted3_c_to_common partition of mcrparted for values from ('c', minvalue) to ('common', minvalue)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_ts_config_map
;
----------
[CREATE_TABLE] -- Test defaults with array assignments
create table base_tab (a serial, b int[], c text, d text default 'Table default')
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table rlp attach partition rlp3 for values from (15) to (20)
;
----------
[ALTER_INDEX] -- Boolean
ALTER INDEX dummy_test_idx SET (option_bool = 4)
;
----------
[CREATE_TABLE] create table 計算機用語 (用語 text, 分類コード varchar, 備考1Aだよ char(16))
;
----------
[CREATE_TABLE] CREATE TABLE "public"."quad_box_tbl_ord_seq2" (
    "n" bigint,
    "dist" double precision,
    "id" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION gen_subxids (n integer)
 RETURNS VOID
 LANGUAGE plpgsql
AS $$
BEGIN
  IF n <= 0 THEN
	UPDATE subxids SET val = 1 WHERE subx = 0;
    RETURN;
  ELSE
    PERFORM gen_subxids(n - 1);
    RETURN;
  END IF;
EXCEPTION /* generates a subxid */
  WHEN raise_exception THEN NULL;
END;
$$
;
----------
[CREATE_POLICY] CREATE RULE "rtest_order_r4" AS
    ON INSERT TO "public"."rtest_order1"
   WHERE ("new"."a" < 100) DO INSTEAD  INSERT INTO "public"."rtest_order2" ("a", "b", "c")
  VALUES ("new"."a", "nextval"('"public"."rtest_seq"'::"regclass"), 'rule 4 - this should run 4th'::"text")
;
----------
[CREATE_TABLE] CREATE TABLE bigintp (
	a bigint
) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] create table pt (category int, sdata seg, tdata text)
  partition by list (category)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA testns5
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_mysub2 CONNECTION 'port=29713 host=/tmp/jOoI3lrZrW dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub2_slot)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE batch_table_p0f
	PARTITION OF batch_table
	FOR VALUES WITH (MODULUS 3, REMAINDER 0)
	SERVER loopback
	OPTIONS (table_name 'batch_table_p0', batch_size '10')
;
----------
[CREATE_VIEW(SELECT)] create view insertconflictview as select * from insertconflicttest
;
----------
[CREATE_PROG_OBJ] --
-- Test polymorphic type system
--

create function anyarray_anyrange_func(a anyarray, r anyrange)
  returns anyelement as 'select $1[1] + lower($2);' language sql
;
----------
[CREATE_TABLE] -- Check support of both 1B and 4B header sizes of short varlena datum
CREATE TABLE varlena_bug (v text)
;
----------
[CREATE_PROG_OBJ] /* src/pl/plpgsql/src/plpgsql--1.0.sql */

CREATE FUNCTION plpgsql_call_handler() RETURNS language_handler
  LANGUAGE c AS '$libdir/plpgsql'
;
----------
[ADMIN_TABLE] ANALYZE public.part_7_a_null
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH TEMPLATE dump_test.alt_ts_temp1 (lexize=dsimple_lexize)
;
----------
[CREATE_TABLE] create temp table prtx1_1 partition of prtx1 for values from (1) to (11)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION "public"."hunspell_tst" (
    PARSER = "pg_catalog"."default" )
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft1 (
	c0 int,
	c1 int NOT NULL,
	c2 int NOT NULL,
	c3 text,
	c4 timestamptz,
	c5 timestamp,
	c6 varchar(10),
	c7 char(10) default 'ft1',
	c8 user_enum
) SERVER loopback
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_extension_next_moved CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE split_table_idx.method_table (
    id integer,
    payload text
) USING heap WITH (fillfactor = 80)
;
----------
[ADD_INDEX] CREATE INDEX tbl_include_reg_idx ON tbl_include_reg (c1, c2) INCLUDE (c3, c4)
;
----------
[CREATE_TYPE] create domain mynums as numeric(4,2)[1]
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE test_sequence
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_123_231
FOREIGN KEY (x1,x2,x3) REFERENCES pktable(id2,id3,id1)
;
----------
[ADD_INDEX] CREATE INDEX "gtest22c_pred_idx" ON "public"."gtest22c" USING "btree" ("a") WHERE ("b" > 0)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_subscription
;
----------
[ADMIN_TABLE] ANALYZE public.quad_box_tbl_ord_seq1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_user_mapping
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=25972 host=/tmp/pOsjRHdBUp dbname=postgres' PUBLICATION pub1
;
