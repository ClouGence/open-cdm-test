alter table test alter column a1 set default '23'
;
TRUNCATE TABLE truncprim CASCADE
;
ANALYZE ft1
;
create table p1_c1c2() inherits (p1_c1, p1_c2)
;
ALTER FUNCTION gbt_bytea_same(gbtreekey_var, gbtreekey_var, internal) PARALLEL SAFE
;
-- Ensure the parent and child are analyzed
ANALYZE only_inh_parent
;
create type two_ints_range as range (subtype = two_ints)
;
ALTER FUNCTION gbt_bit_penalty(internal, internal, internal) PARALLEL SAFE
;
COMMENT ON CONSTRAINT constrname ON FKTABLE IS 'fk constraint comment'
;
-- Test multiplication and division with intervals.
-- Floating point arithmetic rounding errors can lead to unexpected results,
-- though the code attempts to do the right thing and round up to days and
-- minutes to avoid results such as '3 days 24:00 hours' or '14:20:60'.
-- Note that it is expected for some day components to be greater than 29 and
-- some time components be greater than 23:59:59 due to how intervals are
-- stored internally.

CREATE TABLE INTERVAL_MULDIV_TBL (span interval)
;
ALTER TABLE ONLY "public"."pagg_tab_ml_p3" ATTACH PARTITION "public"."pagg_tab_ml_p3_s1" FOR VALUES FROM (0) TO (5)
;
create table idxpart3 (c int, b int, a int) partition by range (a)
;
CREATE UNIQUE INDEX t_a_idx ON t (a, b)
;
CREATE INDEX tab1_c_brin_idx ON tab1 USING brin (c)
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest1_1" INHERIT "generated_virtual_tests"."gtest1"
;
ALTER TABLE has_volatile ADD col2 int DEFAULT 1
;
--
-- Tests to exercise the plan caching/invalidation mechanism
--

CREATE TEMP TABLE pcachetest AS SELECT * FROM int8_tbl
;
CREATE FUNCTION g_cube_consistent(internal,cube,smallint,oid,internal)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_28131_index RENAME TO reind_con_toast_idx
;
create table tttest (
	price_id	int4,
	price_val	int4,
	price_on	int4,
	price_off	int4 default 999999
)
;
--
-- Verify prohibition of row triggers with transition triggers on
-- inheritance children
--
create table parent (a text, b int)
;
TRUNCATE trunc_f
;
CREATE FUNCTION unwanted_grant() RETURNS void LANGUAGE sql AS
	'GRANT regress_priv_group2 TO regress_sro_user'
;
--fail - alter of two_phase option not supported.
ALTER SUBSCRIPTION regress_testsub SET (two_phase = false)
;
ANALYZE pg_temp_13.pg_temp_26765
;
VACUUM FULL pg_description
;
ALTER OPERATOR FAMILY gist_oid_ops USING gist ADD
	OPERATOR	6	<> (oid, oid) ,
	OPERATOR	15	<-> (oid, oid) FOR ORDER BY pg_catalog.oid_ops ,
	FUNCTION	8 (oid, oid) gbt_oid_distance (internal, oid, int2, oid, internal) ,
	-- Also add support function for index-only-scans, added in 9.5.
	FUNCTION	9 (oid, oid) gbt_oid_fetch (internal)
;
ALTER CONVERSION alt_conv3 RENAME TO alt_conv4
;
create trigger depth_a_tr before insert on depth_a
  for each row execute procedure depth_a_tf()
;
CREATE OPERATOR FAMILY alt_opf9 USING gist
;
CREATE VIEW itestv11 AS SELECT * FROM itest11
;
/* Test case for bug #16242 */
-- We create a parent and child where the child has missing
-- non-null attribute values, and arrange to pass them through
-- tuple conversion from the child to the parent tupdesc
create table bar1 (a integer, b integer not null default 1)
  partition by range (a)
;
vacuum test
;
create table hp_prefix_test_p6 partition of hp_prefix_test for values with (modulus 8, remainder 6)
;
--
-- NUMEROLOGY
-- Test various combinations of numeric types and functions.
--

--
-- Test implicit type conversions
-- This fails for Postgres v6.1 (and earlier?)
--  so let's try explicit conversions for now - tgl 97/05/07
--

CREATE TABLE TEMP_FLOAT (f1 FLOAT8)
;
alter table rewriteme alter column bar type timestamp
;
ANALYZE pg_temp_18.pg_temp_26864
;
ANALYZE pg_temp_32.pg_temp_28597_2
;
CREATE OR REPLACE AGGREGATE myavg (order by numeric)
(
	stype = numeric,
	sfunc = numeric_add
)
;
CREATE CAST (mood AS json) WITH FUNCTION mood_to_json(mood) AS IMPLICIT
;
CREATE TABLE mcv_lists_bool (
    a BOOL,
    b BOOL,
    c BOOL
)
;
-- use ALTER TABLE to add a primary key
create table idxpart (a int, b int, c text) partition by range (a, b)
;
/*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE NOTICE 'Table ''%'' is being rewritten (reason = %)',
               pg_event_trigger_table_rewrite_oid()::regclass,
               pg_event_trigger_table_rewrite_reason();
END;
$$
;
-- mixing named and positional argument notations
create function namedparmcursor_test2(int, int) returns boolean as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
    nonsense record;
begin
    open c1(param1 := $1, $2);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$$ language plpgsql
;
CREATE INDEX "onek2_u1_prtl" ON "public"."onek2" USING "btree" ("unique1") WHERE (("unique1" < 20) OR ("unique1" > 980))
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION mypub
       WITH (connect = false, create_slot = false, copy_data = false)
;
-- Disable autovacuum for these tables to avoid unexpected effects of that
ALTER TABLE "S 1"."T 1" SET (autovacuum_enabled = 'false')
;
DROP TABLE IF EXISTS audit_tbls.schema_one_table_three
;
create index on anothertab(f2,f3)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] alter table test alter column a1 set default '23'
;
----------
[TRUNCATE_TABLE] TRUNCATE TABLE truncprim CASCADE
;
----------
[ADMIN_TABLE] ANALYZE ft1
;
----------
[CREATE_TABLE] create table p1_c1c2() inherits (p1_c1, p1_c2)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bytea_same(gbtreekey_var, gbtreekey_var, internal) PARALLEL SAFE
;
----------
[ADMIN_TABLE] -- Ensure the parent and child are analyzed
ANALYZE only_inh_parent
;
----------
[CREATE_TYPE] create type two_ints_range as range (subtype = two_ints)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_bit_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT constrname ON FKTABLE IS 'fk constraint comment'
;
----------
[CREATE_TABLE] -- Test multiplication and division with intervals.
-- Floating point arithmetic rounding errors can lead to unexpected results,
-- though the code attempts to do the right thing and round up to days and
-- minutes to avoid results such as '3 days 24:00 hours' or '14:20:60'.
-- Note that it is expected for some day components to be greater than 29 and
-- some time components be greater than 23:59:59 due to how intervals are
-- stored internally.

CREATE TABLE INTERVAL_MULDIV_TBL (span interval)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml_p3" ATTACH PARTITION "public"."pagg_tab_ml_p3_s1" FOR VALUES FROM (0) TO (5)
;
----------
[CREATE_TABLE] create table idxpart3 (c int, b int, a int) partition by range (a)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t_a_idx ON t (a, b)
;
----------
[ADD_INDEX] CREATE INDEX tab1_c_brin_idx ON tab1 USING brin (c)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_virtual_tests"."gtest1_1" INHERIT "generated_virtual_tests"."gtest1"
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE has_volatile ADD col2 int DEFAULT 1
;
----------
[CREATE_TABLE|SELECT] --
-- Tests to exercise the plan caching/invalidation mechanism
--

CREATE TEMP TABLE pcachetest AS SELECT * FROM int8_tbl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_cube_consistent(internal,cube,smallint,oid,internal)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28131_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table tttest (
	price_id	int4,
	price_val	int4,
	price_on	int4,
	price_off	int4 default 999999
)
;
----------
[CREATE_TABLE] --
-- Verify prohibition of row triggers with transition triggers on
-- inheritance children
--
create table parent (a text, b int)
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_f
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION unwanted_grant() RETURNS void LANGUAGE sql AS
	'GRANT regress_priv_group2 TO regress_sro_user'
;
----------
[ALTER_PUB_SUB] --fail - alter of two_phase option not supported.
ALTER SUBSCRIPTION regress_testsub SET (two_phase = false)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26765
;
----------
[ADMIN_TABLE] VACUUM FULL pg_description
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_oid_ops USING gist ADD
	OPERATOR	6	<> (oid, oid) ,
	OPERATOR	15	<-> (oid, oid) FOR ORDER BY pg_catalog.oid_ops ,
	FUNCTION	8 (oid, oid) gbt_oid_distance (internal, oid, int2, oid, internal) ,
	-- Also add support function for index-only-scans, added in 9.5.
	FUNCTION	9 (oid, oid) gbt_oid_fetch (internal)
;
----------
[ALTER_POLICY] ALTER CONVERSION alt_conv3 RENAME TO alt_conv4
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger depth_a_tr before insert on depth_a
  for each row execute procedure depth_a_tf()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf9 USING gist
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW itestv11 AS SELECT * FROM itest11
;
----------
[CREATE_TABLE] /* Test case for bug #16242 */
-- We create a parent and child where the child has missing
-- non-null attribute values, and arrange to pass them through
-- tuple conversion from the child to the parent tupdesc
create table bar1 (a integer, b integer not null default 1)
  partition by range (a)
;
----------
[ADMIN_TABLE] vacuum test
;
----------
[CREATE_TABLE] create table hp_prefix_test_p6 partition of hp_prefix_test for values with (modulus 8, remainder 6)
;
----------
[CREATE_TABLE] --
-- NUMEROLOGY
-- Test various combinations of numeric types and functions.
--

--
-- Test implicit type conversions
-- This fails for Postgres v6.1 (and earlier?)
--  so let's try explicit conversions for now - tgl 97/05/07
--

CREATE TABLE TEMP_FLOAT (f1 FLOAT8)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table rewriteme alter column bar type timestamp
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_26864
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28597_2
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE AGGREGATE myavg (order by numeric)
(
	stype = numeric,
	sfunc = numeric_add
)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (mood AS json) WITH FUNCTION mood_to_json(mood) AS IMPLICIT
;
----------
[CREATE_TABLE] CREATE TABLE mcv_lists_bool (
    a BOOL,
    b BOOL,
    c BOOL
)
;
----------
[CREATE_TABLE] -- use ALTER TABLE to add a primary key
create table idxpart (a int, b int, c text) partition by range (a, b)
;
----------
[CREATE_POLICY] /*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE NOTICE 'Table ''%'' is being rewritten (reason = %)',
               pg_event_trigger_table_rewrite_oid()::regclass,
               pg_event_trigger_table_rewrite_reason();
END;
$$
;
----------
[CREATE_PROG_OBJ] -- mixing named and positional argument notations
create function namedparmcursor_test2(int, int) returns boolean as $$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
    nonsense record;
begin
    open c1(param1 := $1, $2);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$$ language plpgsql
;
----------
[ADD_INDEX] CREATE INDEX "onek2_u1_prtl" ON "public"."onek2" USING "btree" ("unique1") WHERE (("unique1" < 20) OR ("unique1" > 980))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION mypub
       WITH (connect = false, create_slot = false, copy_data = false)
;
----------
[ALTER_TABLE] -- Disable autovacuum for these tables to avoid unexpected effects of that
ALTER TABLE "S 1"."T 1" SET (autovacuum_enabled = 'false')
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS audit_tbls.schema_one_table_three
;
----------
[ADD_INDEX] create index on anothertab(f2,f3)
;
