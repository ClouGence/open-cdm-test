COPY public.utf8_inputs (inbytes, description) TO stdout
;
EXECUTE dumpFunc('34689')
;
REVOKE UPDATE, DELETE ON alice.unpartitioned FROM regress_alice
;
REVOKE ALL ON PROCEDURE regress_zeropriv_proc() FROM CURRENT_USER, PUBLIC
;
prepare transaction 'p1'
;
copy attest(a) from stdin
;
-- INSERT will be in a subxact
SAVEPOINT savept1
;
-- Revoke all conceivably-relevant ACLs within the extension.  The system
-- doesn't check all these ACLs, but this will provide some coverage if that
-- ever changes.
REVOKE ALL ON TYPE s.citext FROM PUBLIC
;
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf2 OWNER TO regress_alter_generic_user3
;
LOCK TABLE "public"."itest13" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[DATA_EXPORT] COPY public.utf8_inputs (inbytes, description) TO stdout
;
----------
[UNSAFE] EXECUTE dumpFunc('34689')
;
----------
[REVOKE] REVOKE UPDATE, DELETE ON alice.unpartitioned FROM regress_alice
;
----------
[REVOKE] REVOKE ALL ON PROCEDURE regress_zeropriv_proc() FROM CURRENT_USER, PUBLIC
;
----------
[TRANSACTION] prepare transaction 'p1'
;
----------
[DATA_IMPORT] copy attest(a) from stdin
;
----------
[TRANSACTION] -- INSERT will be in a subxact
SAVEPOINT savept1
;
----------
[REVOKE] -- Revoke all conceivably-relevant ACLs within the extension.  The system
-- doesn't check all these ACLs, but this will provide some coverage if that
-- ever changes.
REVOKE ALL ON TYPE s.citext FROM PUBLIC
;
----------
[TRANSFER_PRIVILEGE] ALTER TEXT SEARCH CONFIGURATION alt_ts_conf2 OWNER TO regress_alter_generic_user3
;
----------
[SESSION_LOCK] LOCK TABLE "public"."itest13" IN ACCESS SHARE MODE
;
