CREATE FUNCTION mytrigger() RETURNS trigger LANGUAGE plpgsql as $$
begin
	if row(old.*) = row(new.*) then
		raise notice 'row % not changed', new.f1;
	else
		raise notice 'row % changed', new.f1;
	end if;
	return new;
end$$
;
CREATE OPERATOR CLASS gist_timetz_ops
DEFAULT FOR TYPE timetz USING gist
AS
	OPERATOR	1	<   ,
	OPERATOR	2	<=  ,
	OPERATOR	3	=   ,
	OPERATOR	4	>=  ,
	OPERATOR	5	>   ,
	FUNCTION	1	gbt_timetz_consistent (internal, timetz, int2, oid, internal),
	FUNCTION	2	gbt_time_union (internal, internal),
	FUNCTION	3	gbt_timetz_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_time_penalty (internal, internal, internal),
	FUNCTION	6	gbt_time_picksplit (internal, internal),
	FUNCTION	7	gbt_time_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
CREATE INDEX qq ON test_tsquery USING gist (keyword tsquery_ops)
;
DROP TABLE IF EXISTS split_pg18.temporal_parent
;
create table p1 (f1 int)
;
ALTER TABLE pg_temp_12.pg_temp_26637_2 ADD COLUMN newdata pg_temp_12.pg_temp_26637
;
ALTER TABLE pg_event_trigger ADD PRIMARY KEY USING INDEX pg_event_trigger_oid_index
;
CREATE INDEX regtest_index_tbl4_y ON regtest_table_4(y)
;
CREATE DATABASE db_newly_created
;
CREATE FUNCTION inj_io_reopen_attach()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
create sequence aitest_seq increment 10 start 0 minvalue 0
;
CREATE VIEW pg_stat_gssapi AS
    SELECT
            S.pid,
            S.gss_auth AS gss_authenticated,
            S.gss_princ AS principal,
            S.gss_enc AS encrypted
    FROM pg_stat_get_activity(NULL) AS S
    WHERE S.client_port IS NOT NULL
;
DO $$
DECLARE
  v_Text text;
  v_cnt  integer := 42;
BEGIN
  CALL p1(v_cnt := v_cnt, v_Text := v_Text);
  RAISE NOTICE '%', v_Text;
END;
$$
;
CREATE TABLE "public"."pagg_tab_para" (
    "x" integer,
    "y" integer
)
PARTITION BY RANGE ("x")
;
CREATE FUNCTION _ltxtq_rexec(ltxtquery, _ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE ruletest2 (b int)
;
ALTER TABLE ONLY "public"."renamecolumnchild" INHERIT "public"."renamecolumn"
;
-- Tests for stats with inheritance
CREATE TABLE stxdinh(a int, b int)
;
ALTER INDEX pg_toast.pg_toast_30889_index RENAME TO reind_con_toast_idx
;
CREATE TABLE cwi_test(a int, b int)
;
ALTER EXTENSION test_parser ADD function testprs_end(internal)
;
DROP TABLE dependee CASCADE
;
drop index idxpart_brin
;
/*
 * text search configuration for norwegian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * norwegian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
CREATE INDEX ginidx ON tbl USING gin(j)
;
CREATE TABLE "public"."dedup_unique_test_table" (
    "a" integer
)
WITH ("autovacuum_enabled"='false')
;
/*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
CREATE TABLE tenant2_table (i integer)
;
ALTER TABLE temporal3 ALTER COLUMN valid_at DROP NOT NULL
;
SECURITY LABEL ON FUNCTION customer_credit(int)
    IS 'system_u:object_r:sepgsql_trusted_proc_exec_t:s0'
;
create table rp0 partition of rp for values from (minvalue) to (1)
;
ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key CHECK(c > 5)
;
CREATE TABLE rule_merge2 (a int, b text)
;
ANALYZE public.test_inh_check_child
;
CREATE RULE european_city_update_rule AS ON UPDATE TO european_city_view
DO INSTEAD UPDATE city_view SET
    city_name = NEW.city_name,
    population = NEW.population,
    country_name = NEW.country_name
WHERE city_id = OLD.city_id
RETURNING NEW.*
;
CREATE FUNCTION test_future_xid_status(bigint)
RETURNS void
LANGUAGE plpgsql
AS
$$
BEGIN
  PERFORM txid_status($1);
  RAISE EXCEPTION 'didn''t ERROR at xid in the future as expected';
EXCEPTION
  WHEN invalid_parameter_value THEN
    RAISE NOTICE 'Got expected error for xid in the future';
END;
$$
;
CREATE FUNCTION cube_contains(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
/*
 * 24.6
 * FOREIGN_SERVER_OPTIONS view
 */
CREATE VIEW foreign_server_options AS
    SELECT foreign_server_catalog,
           foreign_server_name,
           CAST((pg_options_to_table(s.srvoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(s.srvoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_servers s
;
DROP TABLE a1, a2
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_init_privs
;
DROP SEQUENCE IF EXISTS no_such_schema.foo
;
CREATE FUNCTION delete(hstore,text[])
RETURNS hstore
AS '$libdir/hstore','hstore_delete_array'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE dump_test.test_table_nn_chld1 VALIDATE CONSTRAINT nn
;
CREATE MATERIALIZED VIEW IF NOT EXISTS mvtest_mv_foo AS SELECT * FROM mvtest_foo_data
;
ALTER FUNCTION ptest1(text) VOLATILE
;
CREATE FUNCTION PLUS_THREE(i INTEGER) RETURNS INTEGER AS
$$ SELECT i + 3 LIMIT 1 $$ IMMUTABLE LANGUAGE SQL
;
create table anothertab (atcol1 serial8, atcol2 boolean,
	constraint anothertab_chk check (atcol1 <= 3))
;
ANALYZE public.rtest_t9
;
truncate parted_tab
;
VACUUM (PARALLEL 0) pg_catalog.pg_largeobject
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION mytrigger() RETURNS trigger LANGUAGE plpgsql as $$
begin
	if row(old.*) = row(new.*) then
		raise notice 'row % not changed', new.f1;
	else
		raise notice 'row % changed', new.f1;
	end if;
	return new;
end$$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_timetz_ops
DEFAULT FOR TYPE timetz USING gist
AS
	OPERATOR	1	<   ,
	OPERATOR	2	<=  ,
	OPERATOR	3	=   ,
	OPERATOR	4	>=  ,
	OPERATOR	5	>   ,
	FUNCTION	1	gbt_timetz_consistent (internal, timetz, int2, oid, internal),
	FUNCTION	2	gbt_time_union (internal, internal),
	FUNCTION	3	gbt_timetz_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_time_penalty (internal, internal, internal),
	FUNCTION	6	gbt_time_picksplit (internal, internal),
	FUNCTION	7	gbt_time_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[ADD_INDEX] CREATE INDEX qq ON test_tsquery USING gist (keyword tsquery_ops)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS split_pg18.temporal_parent
;
----------
[CREATE_TABLE] create table p1 (f1 int)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26637_2 ADD COLUMN newdata pg_temp_12.pg_temp_26637
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_event_trigger ADD PRIMARY KEY USING INDEX pg_event_trigger_oid_index
;
----------
[ADD_INDEX] CREATE INDEX regtest_index_tbl4_y ON regtest_table_4(y)
;
----------
[CREATE_CATALOG] CREATE DATABASE db_newly_created
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION inj_io_reopen_attach()
RETURNS pg_catalog.void STRICT
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_SEQUENCE] create sequence aitest_seq increment 10 start 0 minvalue 0
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_gssapi AS
    SELECT
            S.pid,
            S.gss_auth AS gss_authenticated,
            S.gss_princ AS principal,
            S.gss_enc AS encrypted
    FROM pg_stat_get_activity(NULL) AS S
    WHERE S.client_port IS NOT NULL
;
----------
[BLOCK] DO $$
DECLARE
  v_Text text;
  v_cnt  integer := 42;
BEGIN
  CALL p1(v_cnt := v_cnt, v_Text := v_Text);
  RAISE NOTICE '%', v_Text;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_para" (
    "x" integer,
    "y" integer
)
PARTITION BY RANGE ("x")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltxtq_rexec(ltxtquery, _ltree)
RETURNS bool
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE ruletest2 (b int)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."renamecolumnchild" INHERIT "public"."renamecolumn"
;
----------
[CREATE_TABLE] -- Tests for stats with inheritance
CREATE TABLE stxdinh(a int, b int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30889_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE cwi_test(a int, b int)
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_parser ADD function testprs_end(internal)
;
----------
[DROP_TABLE] DROP TABLE dependee CASCADE
;
----------
[DROP_INDEX] drop index idxpart_brin
;
----------
[CREATE_POLICY] /*
 * text search configuration for norwegian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * norwegian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY norwegian_stem
	(TEMPLATE = snowball, Language = norwegian , StopWords=norwegian)
;
----------
[ADD_INDEX] CREATE INDEX ginidx ON tbl USING gin(j)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."dedup_unique_test_table" (
    "a" integer
)
WITH ("autovacuum_enabled"='false')
;
----------
[CREATE_POLICY] /*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
----------
[CREATE_TABLE] CREATE TABLE tenant2_table (i integer)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE temporal3 ALTER COLUMN valid_at DROP NOT NULL
;
----------
[SECURITY_LABEL] SECURITY LABEL ON FUNCTION customer_credit(int)
    IS 'system_u:object_r:sepgsql_trusted_proc_exec_t:s0'
;
----------
[CREATE_TABLE] create table rp0 partition of rp for values from (minvalue) to (1)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD CONSTRAINT tt9_c_key CHECK(c > 5)
;
----------
[CREATE_TABLE] CREATE TABLE rule_merge2 (a int, b text)
;
----------
[ADMIN_TABLE] ANALYZE public.test_inh_check_child
;
----------
[CREATE_POLICY] CREATE RULE european_city_update_rule AS ON UPDATE TO european_city_view
DO INSTEAD UPDATE city_view SET
    city_name = NEW.city_name,
    population = NEW.population,
    country_name = NEW.country_name
WHERE city_id = OLD.city_id
RETURNING NEW.*
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_future_xid_status(bigint)
RETURNS void
LANGUAGE plpgsql
AS
$$
BEGIN
  PERFORM txid_status($1);
  RAISE EXCEPTION 'didn''t ERROR at xid in the future as expected';
EXCEPTION
  WHEN invalid_parameter_value THEN
    RAISE NOTICE 'Got expected error for xid in the future';
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_contains(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.6
 * FOREIGN_SERVER_OPTIONS view
 */
CREATE VIEW foreign_server_options AS
    SELECT foreign_server_catalog,
           foreign_server_name,
           CAST((pg_options_to_table(s.srvoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(s.srvoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_servers s
;
----------
[DROP_TABLE] DROP TABLE a1, a2
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_init_privs
;
----------
[DROP_SEQUENCE] DROP SEQUENCE IF EXISTS no_such_schema.foo
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION delete(hstore,text[])
RETURNS hstore
AS '$libdir/hstore','hstore_delete_array'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE dump_test.test_table_nn_chld1 VALIDATE CONSTRAINT nn
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW IF NOT EXISTS mvtest_mv_foo AS SELECT * FROM mvtest_foo_data
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION ptest1(text) VOLATILE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION PLUS_THREE(i INTEGER) RETURNS INTEGER AS
$$ SELECT i + 3 LIMIT 1 $$ IMMUTABLE LANGUAGE SQL
;
----------
[CREATE_TABLE] create table anothertab (atcol1 serial8, atcol2 boolean,
	constraint anothertab_chk check (atcol1 <= 3))
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_t9
;
----------
[TRUNCATE_TABLE] truncate parted_tab
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_largeobject
;
