ALTER TABLE pg_toast.pg_toast_33416 RENAME TO reind_con_toast
;
ALTER SEQUENCE "public"."transition_table_level1_level1_no_seq" OWNED BY "public"."transition_table_level1"."level1_no"
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16883 host=/tmp/YG8WP4etb2 dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
CREATE TABLE measurement_y2006m03 (
    CHECK ( logdate >= DATE '2006-03-01' AND logdate < DATE '2006-04-01' )
) INHERITS (measurement) WITH (autovacuum_enabled=off)
;
CREATE AGGREGATE balk(int4)
(
    SFUNC = balkifnull(int8, int4),
    STYPE = int8,
    PARALLEL = SAFE,
    INITCOND = '0'
)
;
-- This file is based on tests/reg.test from the Tcl distribution,
-- which is marked
-- # Copyright (c) 1998, 1999 Henry Spencer.  All rights reserved.
-- The full copyright notice can be found in src/backend/regex/COPYRIGHT.
-- Most commented lines below are copied from reg.test.  Each
-- test case is followed by an equivalent test using test_regex().

create extension test_regex
;
DROP SCHEMA split_returning18 CASCADE
;
create domain int_nn as int not null
;
CREATE FUNCTION int4_desc_cmp (int4, int4) RETURNS int LANGUAGE sql AS $$
		SELECT CASE WHEN $1 = $2 THEN 0 WHEN $1 > $2 THEN -1 ELSE 1 END; $$
;
DROP TABLE dont_forget
;
create table trigpart2 partition of trigpart for values from (1000) to (2000)
;
CREATE TYPE "public"."rewritetype" AS (
	"a" "text"
)
;
CREATE FUNCTION logging_sfunc_nonstrict(text, anyelement) RETURNS text AS
$$ SELECT COALESCE($1, '') || '*' || quote_nullable($2) $$
LANGUAGE SQL IMMUTABLE
;
CREATE TRIGGER stmt_trig AFTER INSERT ON batch_cp_upd_test1
	FOR EACH STATEMENT EXECUTE FUNCTION log_stmt()
;
ALTER TYPE comptype ALTER ATTRIBUTE r TYPE bigint
;
-- Inline-able wrappers that will produce an integer "1" result:
  create function lock_share(int,int) returns int language sql as
  'select 1 - lock_share($1)' parallel safe
;
-- fail
ALTER SUBSCRIPTION regress_doesnotexist CONNECTION 'dbname=regress_doesnotexist2'
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub8
;
CREATE AGGREGATE myaggp05a(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
create or replace function compos() returns compostype as $$
declare
  v record;
begin
  v := (1, 'hello'::varchar);
  return v;
end;
$$ language plpgsql
;
CREATE CONSTRAINT TRIGGER trig_ins_fk_parted AFTER INSERT ON fkpart11.fk_parted INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with &&)) partition by range (a)
;
-- make sure that create WITH NO DATA does not plan the query (bug #13907)
create materialized view mvtest_error as select 1/0 as x
;
DROP TABLE inhz
;
DROP DOMAIN jsonb_test_domain
;
CREATE VIEW mysecview9 WITH (security_invoker)
       AS SELECT * FROM tbl1 WHERE a < 100
;
drop function child_ins_func()
;
CREATE TABLE vacowned_part2 PARTITION OF vacowned_parted FOR VALUES IN (2)
;
DROP FUNCTION IF EXISTS test_function_exists()
;
CREATE TEMP TABLE filenode_mapping AS
SELECT
    oid, mapped_oid, reltablespace, relfilenode, relname
FROM pg_class,
    pg_filenode_relation(reltablespace, pg_relation_filenode(oid)) AS mapped_oid
WHERE relkind IN ('r', 'i', 'S', 't', 'm') AND mapped_oid IS DISTINCT FROM oid
;
do $$ <<b>> declare c nested_int8s; begin b.d.c2.x = 1; end $$
;
-- should fail
create function bogus_func(anyelement)
  returns anymultirange as 'select int4multirange(int4range(1,10))' language sql
;
CREATE TEMP TABLE pg_temp_24.pg_temp_26683_2 (tid pg_catalog.tid)
;
--
-- Check inheritance of defaults and constraints
--

CREATE TABLE INSERT_CHILD (cx INT default 42,
	cy INT CHECK (cy > x))
	INHERITS (INSERT_TBL)
;
CREATE OR REPLACE FUNCTION large_val() RETURNS TEXT LANGUAGE SQL AS
'select array_agg(md5(g::text))::text from generate_series(1, 256) g'
;
CREATE FUNCTION g_cube_same(cube, cube, internal)
RETURNS internal
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
DO LANGUAGE plpgsql $$
      BEGIN
        BEGIN
          INSERT INTO harvest VALUES (41);
	EXCEPTION WHEN OTHERS THEN RAISE;
	END;
      END $$
;
alter table inh_child2 alter column f1 set not null
;
CREATE OR REPLACE FUNCTION distinct_func(a INT) RETURNS INT AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL SAFE
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_event_trigger
;
-- fail - cannot set PUBLISH_VIA_PARTITION_ROOT to false if any row filter is
-- used for partitioned table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=0)
;
--
-- brin_page_items()
--
DROP FUNCTION brin_page_items(IN page bytea, IN index_oid regclass)
;
ALTER PUBLICATION pub2 SET (publish_generated_columns = none)
;
CREATE TABLE VARCHAR_TBL(f1 varchar(4))
;
ALTER INDEX attmp_idx ALTER COLUMN 4 SET STATISTICS 1000
;
CREATE INDEX nametbl_c1_c2_idx ON nametbl (c2, c1) INCLUDE (c3)
;
create temp table with_test (i int)
;
ALTER INDEX pg_toast.pg_toast_33372_index RENAME TO reind_con_toast_idx
;
/* Ok to create multiple unique indexes */
CREATE TABLE inhg (x text UNIQUE, LIKE inhz INCLUDING INDEXES)
;
------------------------------------------------------------------------------------------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33416 RENAME TO reind_con_toast
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE "public"."transition_table_level1_level1_no_seq" OWNED BY "public"."transition_table_level1"."level1_no"
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16883 host=/tmp/YG8WP4etb2 dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
----------
[CREATE_TABLE] CREATE TABLE measurement_y2006m03 (
    CHECK ( logdate >= DATE '2006-03-01' AND logdate < DATE '2006-04-01' )
) INHERITS (measurement) WITH (autovacuum_enabled=off)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE balk(int4)
(
    SFUNC = balkifnull(int8, int4),
    STYPE = int8,
    PARALLEL = SAFE,
    INITCOND = '0'
)
;
----------
[CREATE_LIBRARY] -- This file is based on tests/reg.test from the Tcl distribution,
-- which is marked
-- # Copyright (c) 1998, 1999 Henry Spencer.  All rights reserved.
-- The full copyright notice can be found in src/backend/regex/COPYRIGHT.
-- Most commented lines below are copied from reg.test.  Each
-- test case is followed by an equivalent test using test_regex().

create extension test_regex
;
----------
[DROP_SCHEMA] DROP SCHEMA split_returning18 CASCADE
;
----------
[CREATE_TYPE] create domain int_nn as int not null
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int4_desc_cmp (int4, int4) RETURNS int LANGUAGE sql AS $$
		SELECT CASE WHEN $1 = $2 THEN 0 WHEN $1 > $2 THEN -1 ELSE 1 END; $$
;
----------
[DROP_TABLE] DROP TABLE dont_forget
;
----------
[CREATE_TABLE] create table trigpart2 partition of trigpart for values from (1000) to (2000)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."rewritetype" AS (
	"a" "text"
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION logging_sfunc_nonstrict(text, anyelement) RETURNS text AS
$$ SELECT COALESCE($1, '') || '*' || quote_nullable($2) $$
LANGUAGE SQL IMMUTABLE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER stmt_trig AFTER INSERT ON batch_cp_upd_test1
	FOR EACH STATEMENT EXECUTE FUNCTION log_stmt()
;
----------
[ALTER_TYPE] ALTER TYPE comptype ALTER ATTRIBUTE r TYPE bigint
;
----------
[CREATE_PROG_OBJ] -- Inline-able wrappers that will produce an integer "1" result:
  create function lock_share(int,int) returns int language sql as
  'select 1 - lock_share($1)' parallel safe
;
----------
[ALTER_PUB_SUB] -- fail
ALTER SUBSCRIPTION regress_doesnotexist CONNECTION 'dbname=regress_doesnotexist2'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub8
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp05a(BASETYPE = int, SFUNC = tfnp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] create or replace function compos() returns compostype as $$
declare
  v record;
begin
  v := (1, 'hello'::varchar);
  return v;
end;
$$ language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE CONSTRAINT TRIGGER trig_ins_fk_parted AFTER INSERT ON fkpart11.fk_parted INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_cast
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with &&)) partition by range (a)
;
----------
[CREATE_VIEW(SELECT)] -- make sure that create WITH NO DATA does not plan the query (bug #13907)
create materialized view mvtest_error as select 1/0 as x
;
----------
[DROP_TABLE] DROP TABLE inhz
;
----------
[DROP_TYPE] DROP DOMAIN jsonb_test_domain
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW mysecview9 WITH (security_invoker)
       AS SELECT * FROM tbl1 WHERE a < 100
;
----------
[DROP_PROG_OBJ] drop function child_ins_func()
;
----------
[CREATE_TABLE] CREATE TABLE vacowned_part2 PARTITION OF vacowned_parted FOR VALUES IN (2)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION IF EXISTS test_function_exists()
;
----------
[CREATE_TABLE|SELECT|METADATA] CREATE TEMP TABLE filenode_mapping AS
SELECT
    oid, mapped_oid, reltablespace, relfilenode, relname
FROM pg_class,
    pg_filenode_relation(reltablespace, pg_relation_filenode(oid)) AS mapped_oid
WHERE relkind IN ('r', 'i', 'S', 't', 'm') AND mapped_oid IS DISTINCT FROM oid
;
----------
[BLOCK] do $$ <<b>> declare c nested_int8s; begin b.d.c2.x = 1; end $$
;
----------
[CREATE_PROG_OBJ] -- should fail
create function bogus_func(anyelement)
  returns anymultirange as 'select int4multirange(int4range(1,10))' language sql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_24.pg_temp_26683_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] --
-- Check inheritance of defaults and constraints
--

CREATE TABLE INSERT_CHILD (cx INT default 42,
	cy INT CHECK (cy > x))
	INHERITS (INSERT_TBL)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION large_val() RETURNS TEXT LANGUAGE SQL AS
'select array_agg(md5(g::text))::text from generate_series(1, 256) g'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_cube_same(cube, cube, internal)
RETURNS internal
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
      BEGIN
        BEGIN
          INSERT INTO harvest VALUES (41);
	EXCEPTION WHEN OTHERS THEN RAISE;
	END;
      END $$
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table inh_child2 alter column f1 set not null
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION distinct_func(a INT) RETURNS INT AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_event_trigger
;
----------
[ALTER_PUB_SUB] -- fail - cannot set PUBLISH_VIA_PARTITION_ROOT to false if any row filter is
-- used for partitioned table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=0)
;
----------
[DROP_PROG_OBJ] --
-- brin_page_items()
--
DROP FUNCTION brin_page_items(IN page bytea, IN index_oid regclass)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub2 SET (publish_generated_columns = none)
;
----------
[CREATE_TABLE] CREATE TABLE VARCHAR_TBL(f1 varchar(4))
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX attmp_idx ALTER COLUMN 4 SET STATISTICS 1000
;
----------
[ADD_INDEX] CREATE INDEX nametbl_c1_c2_idx ON nametbl (c2, c1) INCLUDE (c3)
;
----------
[CREATE_TABLE] create temp table with_test (i int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33372_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] /* Ok to create multiple unique indexes */
CREATE TABLE inhg (x text UNIQUE, LIKE inhz INCLUDING INDEXES)
;
