VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_range
;
create temp table tmptz (f1 timestamptz primary key)
;
/*
 * 1.1. test CREATE INDEX with buffered build
 */

CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
CREATE TYPE price_input AS (
    id INTEGER,
    price NUMERIC
)
;
CREATE DATABASE "template1" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LC_COLLATE = 'C' LC_CTYPE = 'C'
;
CALL test_proc3(y)
;
drop type rposint
;
CREATE FUNCTION _pg_numeric_precision(typid oid, typmod int4) RETURNS integer
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT
  CASE $1
         WHEN 21 /*int2*/ THEN 16
         WHEN 23 /*int4*/ THEN 32
         WHEN 20 /*int8*/ THEN 64
         WHEN 1700 /*numeric*/ THEN
              CASE WHEN $2 = -1
                   THEN null
                   ELSE (($2 - 4) >> 16) & 65535
                   END
         WHEN 700 /*float4*/ THEN 24 /*FLT_MANT_DIG*/
         WHEN 701 /*float8*/ THEN 53 /*DBL_MANT_DIG*/
         ELSE null
  END$$
;
CREATE INDEX "mvtest_aa" ON "public"."mvtest_bb" USING "btree" ("grandtot")
;
CREATE TABLE part_m02_odd PARTITION OF part_m02
	FOR VALUES IN (1,3,5,7,9) WITH (autovacuum_enabled=off)
;
create table tpart_default_default (a int, i int, b int, j text)
;
CREATE FUNCTION g_cube_decompress(internal)
RETURNS internal
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE MATERIALIZED VIEW mvtest_mv1 AS SELECT 1 AS col1 WITH NO DATA
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26628_2 (tid pg_catalog.tid)
;
/*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
CREATE TABLE num_exp_sub (id1 int4, id2 int4, expected numeric(1000,800))
;
CREATE TEXT SEARCH TEMPLATE xsyn_template (
        LEXIZE = dxsyn_lexize,
	INIT   = dxsyn_init
)
;
ALTER TABLE ONLY "public"."pagg_tab_ml" ATTACH PARTITION "public"."pagg_tab_ml_p1" FOR VALUES FROM (0) TO (12)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) information_schema.sql_features
;
drop function exc_using(int)
;
CREATE TABLE inhtd () INHERITS (inhta, inhtb, inhtc)
;
CREATE OPERATOR ^? (
        LEFTARG = _ltree,
	RIGHTARG = _lquery,
	PROCEDURE = _lt_q_regex,
        COMMUTATOR = '^?',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE FUNCTION "public"."text_w_default_out"("public"."text_w_default") RETURNS "cstring"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$textout$$
;
create table rlp_default_10 partition of rlp_default for values in (10)
;
alter table parted_pk_detach_test1 drop constraint parted_pk_detach_test1_pkey
;
create function int8alias1eq(int8alias1, int8alias1) returns bool
  strict immutable language internal as 'int8eq'
;
ALTER TEXT SEARCH CONFIGURATION ispell_tst ALTER MAPPING FOR
	word, numword, asciiword, hword, numhword, asciihword, hword_part, hword_numpart, hword_asciipart
	WITH ispell, english_stem
;
alter domain testdomain1 rename constraint unsigned to unsigned_foo
;
CREATE TRIGGER "tg_chkslotname" BEFORE INSERT ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotname"('WS')
;
DROP TABLE pg_temp_4.pg_temp_27293_2, pg_temp_4.pg_temp_27293
;
ALTER TABLE d3_listp1 ALTER a DROP NOT NULL
;
CREATE TABLE "public"."pline" (
    "slotname" character(20),
    "phonenumber" character(20),
    "comment" "text",
    "backlink" character(20)
)
;
CREATE TABLE gtestx (x int, y int)
;
-- Ensure STORAGE is not a part of ALTER OPERATOR FAMILY
DROP OPERATOR FAMILY alt_opf4 USING btree
;
create unique index on only parted_conflict (a)
;
DO $$
BEGIN
 BEGIN
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
 EXCEPTION WHEN division_by_zero THEN
   RAISE NOTICE '%', SQLERRM;
 END;
  ROLLBACK;
END
$$
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
CREATE SCHEMA s4
;
CREATE TEXT SEARCH DICTIONARY "public"."ispell" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'ispell_sample', afffile = 'ispell_sample' )
;
-- foreign keys (mixing different nondeterministic collations not allowed)
CREATE TABLE test10pk (x text COLLATE case_sensitive PRIMARY KEY)
;
drop function concat(text, anyarray)
;
ALTER TABLE attmp ADD COLUMN r lseg
;
CREATE TABLE "public"."alpha" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
PARTITION BY RANGE ("a")
;
CREATE OPERATOR ===!!! (
    leftarg = integer,
    rightarg = integer,
    procedure = int4ne,
    negator = ===!!!
)
;
drop aggregate nonesuch (int4)
;
ALTER TABLE old_system_table DROP CONSTRAINT new_system_table_pkey
;
-- fail for lack of + operator

drop function polyf(x anyelement)
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest10"
    ADD CONSTRAINT "gtest10_pkey" PRIMARY KEY ("a")
;
CREATE TABLE "public"."plt1_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
CREATE TABLE ataddindex(f1 INT)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_range
;
----------
[CREATE_TABLE] create temp table tmptz (f1 timestamptz primary key)
;
----------
[CREATE_TABLE] /*
 * 1.1. test CREATE INDEX with buffered build
 */

CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[CREATE_TYPE] CREATE TYPE price_input AS (
    id INTEGER,
    price NUMERIC
)
;
----------
[CREATE_CATALOG] CREATE DATABASE "template1" WITH TEMPLATE = template0 ENCODING = 'SQL_ASCII' LC_COLLATE = 'C' LC_CTYPE = 'C'
;
----------
[CALL_PROG_OBJ] CALL test_proc3(y)
;
----------
[DROP_TYPE] drop type rposint
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _pg_numeric_precision(typid oid, typmod int4) RETURNS integer
    LANGUAGE sql
    IMMUTABLE
    PARALLEL SAFE
    RETURNS NULL ON NULL INPUT
    AS
$$SELECT
  CASE $1
         WHEN 21 /*int2*/ THEN 16
         WHEN 23 /*int4*/ THEN 32
         WHEN 20 /*int8*/ THEN 64
         WHEN 1700 /*numeric*/ THEN
              CASE WHEN $2 = -1
                   THEN null
                   ELSE (($2 - 4) >> 16) & 65535
                   END
         WHEN 700 /*float4*/ THEN 24 /*FLT_MANT_DIG*/
         WHEN 701 /*float8*/ THEN 53 /*DBL_MANT_DIG*/
         ELSE null
  END$$
;
----------
[ADD_INDEX] CREATE INDEX "mvtest_aa" ON "public"."mvtest_bb" USING "btree" ("grandtot")
;
----------
[CREATE_TABLE] CREATE TABLE part_m02_odd PARTITION OF part_m02
	FOR VALUES IN (1,3,5,7,9) WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE] create table tpart_default_default (a int, i int, b int, j text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_cube_decompress(internal)
RETURNS internal
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv1 AS SELECT 1 AS col1 WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26628_2 (tid pg_catalog.tid)
;
----------
[CREATE_POLICY] /*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_sub (id1 int4, id2 int4, expected numeric(1000,800))
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH TEMPLATE xsyn_template (
        LEXIZE = dxsyn_lexize,
	INIT   = dxsyn_init
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml" ATTACH PARTITION "public"."pagg_tab_ml_p1" FOR VALUES FROM (0) TO (12)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) information_schema.sql_features
;
----------
[DROP_PROG_OBJ] drop function exc_using(int)
;
----------
[CREATE_TABLE] CREATE TABLE inhtd () INHERITS (inhta, inhtb, inhtc)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^? (
        LEFTARG = _ltree,
	RIGHTARG = _lquery,
	PROCEDURE = _lt_q_regex,
        COMMUTATOR = '^?',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."text_w_default_out"("public"."text_w_default") RETURNS "cstring"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$textout$$
;
----------
[CREATE_TABLE] create table rlp_default_10 partition of rlp_default for values in (10)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table parted_pk_detach_test1 drop constraint parted_pk_detach_test1_pkey
;
----------
[CREATE_PROG_OBJ] create function int8alias1eq(int8alias1, int8alias1) returns bool
  strict immutable language internal as 'int8eq'
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION ispell_tst ALTER MAPPING FOR
	word, numword, asciiword, hword, numhword, asciihword, hword_part, hword_numpart, hword_asciipart
	WITH ispell, english_stem
;
----------
[RENAME_CONSTRAINT] alter domain testdomain1 rename constraint unsigned to unsigned_foo
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_chkslotname" BEFORE INSERT ON "public"."wslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_chkslotname"('WS')
;
----------
[DROP_TABLE] DROP TABLE pg_temp_4.pg_temp_27293_2, pg_temp_4.pg_temp_27293
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE d3_listp1 ALTER a DROP NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pline" (
    "slotname" character(20),
    "phonenumber" character(20),
    "comment" "text",
    "backlink" character(20)
)
;
----------
[CREATE_TABLE] CREATE TABLE gtestx (x int, y int)
;
----------
[DROP_PROG_OBJ] -- Ensure STORAGE is not a part of ALTER OPERATOR FAMILY
DROP OPERATOR FAMILY alt_opf4 USING btree
;
----------
[ADD_INDEX] create unique index on only parted_conflict (a)
;
----------
[BLOCK] DO $$
BEGIN
 BEGIN
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
 EXCEPTION WHEN division_by_zero THEN
   RAISE NOTICE '%', SQLERRM;
 END;
  ROLLBACK;
END
$$
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
----------
[CREATE_SCHEMA] CREATE SCHEMA s4
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."ispell" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'ispell_sample', afffile = 'ispell_sample' )
;
----------
[CREATE_TABLE] -- foreign keys (mixing different nondeterministic collations not allowed)
CREATE TABLE test10pk (x text COLLATE case_sensitive PRIMARY KEY)
;
----------
[DROP_PROG_OBJ] drop function concat(text, anyarray)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN r lseg
;
----------
[CREATE_TABLE] CREATE TABLE "public"."alpha" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
PARTITION BY RANGE ("a")
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ===!!! (
    leftarg = integer,
    rightarg = integer,
    procedure = int4ne,
    negator = ===!!!
)
;
----------
[DROP_PROG_OBJ] drop aggregate nonesuch (int4)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE old_system_table DROP CONSTRAINT new_system_table_pkey
;
----------
[DROP_PROG_OBJ] -- fail for lack of + operator

drop function polyf(x anyelement)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest10"
    ADD CONSTRAINT "gtest10_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt1_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE ataddindex(f1 INT)
;
