-- Register a view on the function for ease of use.
CREATE VIEW pg_stat_statements AS
  SELECT * FROM pg_stat_statements(true)
;
ALTER TABLE pg_toast.pg_toast_28079 RENAME TO reind_con_toast
;
CREATE FUNCTION text2ltree(text)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE INDEX fdeps_ab_idx ON functional_dependencies (a, b)
;
ANALYZE pg_temp_10.pg_temp_25340
;
-- disallow recursive containment of row types
create temp table recur1 (f1 int)
;
comment on column catalog1.schema1.table_name.column1 is 'ss'
;
CREATE TABLE "public"."itest6" (
    "a" integer NOT NULL,
    "b" "text"
)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_conversion
;
DROP INDEX IF EXISTS split_table_idx.idx_method_lower_include
;
-- ensure assertions are not trapped by 'others'
do $$
begin
  assert 1=0, 'unhandled assertion';
exception when others then
  null; -- do nothing
end;
$$
;
DROP TABLE vac_tab_off_toast_on
;
ANALYZE pg_temp_26.pg_temp_28149
;
create table conttesttbl(idx serial, v integer)
;
CREATE UNLOGGED TABLE tblspc1_unlogged (id int) TABLESPACE tblspc1
;
TRUNCATE atest2
;
ALTER TABLE pg_toast.pg_toast_27601 RENAME TO reind_con_toast
;
CREATE FUNCTION "public"."cleast_accum"("anycompatible", VARIADIC "anycompatiblearray") RETURNS "anycompatible"
    LANGUAGE "sql"
    AS $_$select least($1, min($2[i])) from generate_subscripts($2,1) g(i)$_$
;
ALTER TABLE ONLY "fkpart5"."fk3" DROP COLUMN "........pg.dropped.1........"
;
-- Check that partitioned tables (the parent ones) which don't have visibility
-- maps are rejected
CREATE TABLE test_partitioned (a int, b text default repeat('x', 5000))
			 PARTITION BY list (a)
;
ALTER TABLE test_inh_check ALTER COLUMN b TYPE numeric
;
CREATE TABLE t1 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
CREATE FUNCTION isnle(issn, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (FREEZE) pg_catalog.pg_sequence
;
CREATE INDEX sp_radix_ind ON radix_text_tbl USING spgist (t)
;
-- should fail on existing data without the WHERE clause
ALTER TABLE circles ADD EXCLUDE USING gist
  (c1 WITH &&, (c2::circle) WITH &&)
;
ANALYZE public.brinopers
;
-- UNIQUE index with expression
CREATE UNIQUE INDEX concur_reindex_ind3 ON concur_reindex_tab(abs(c1))
;
CREATE OR REPLACE FUNCTION regexp_replace(string citext, pattern citext, replacement text) returns TEXT AS $$
    SELECT pg_catalog.regexp_replace( $1::pg_catalog.text, $2::pg_catalog.text, $3, 'i');
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
DROP INDEX cwi_replaced_pkey
;
ANALYZE pg_catalog.pg_shdescription
;
-- unless IF NOT EXISTS is specified
ALTER TYPE planets ADD VALUE IF NOT EXISTS 'mercury'
;
ALTER TABLE pg_toast.pg_toast_33541 RENAME TO reind_con_toast
;
create table trigtest2 (i int references trigtest(i) on delete cascade)
;
drop function current_function(text)
;
ANALYZE pg_temp_13.pg_temp_25047_2
;
VACUUM (FULL) vacparted_i
;
-- some error cases
create domain d_fail as no_such_type
;
CREATE OR REPLACE FUNCTION
  is_normalized(text, text DEFAULT 'NFC')
RETURNS boolean
LANGUAGE internal
STRICT IMMUTABLE PARALLEL SAFE
AS 'unicode_is_normalized'
;
create temp table exists_tbl_def partition of exists_tbl default
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."test_type_diff2_c3" INHERIT "public"."test_type_diff2"
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_proc
;
ANALYZE pg_temp_2.pg_temp_27071
;
CREATE TABLE gtest_normal_child2 (a int, b int GENERATED ALWAYS AS (a * 3) STORED)
;
ALTER FUNCTION gbt_time_union(internal, internal) PARALLEL SAFE
;
create function sum4(int8,int8,int8,int8) returns int8 as
'select $1 + $2 + $3 + $4' language sql strict immutable
;
-- pre-existing partition
create table pt12 partition of pt for values in (1,2)
;
-- ERROR

DROP FOREIGN TABLE fd_pt2_1
;
create temp table temp_inh_oncommit_test1 ()
  inherits(temp_inh_oncommit_test) on commit delete rows
;
CREATE INDEX iprt_p2_a ON prt_p2 (a)
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] -- Register a view on the function for ease of use.
CREATE VIEW pg_stat_statements AS
  SELECT * FROM pg_stat_statements(true)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28079 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION text2ltree(text)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX fdeps_ab_idx ON functional_dependencies (a, b)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_25340
;
----------
[CREATE_TABLE] -- disallow recursive containment of row types
create temp table recur1 (f1 int)
;
----------
[COMMENT_COLUMN] comment on column catalog1.schema1.table_name.column1 is 'ss'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest6" (
    "a" integer NOT NULL,
    "b" "text"
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_conversion
;
----------
[DROP_INDEX] DROP INDEX IF EXISTS split_table_idx.idx_method_lower_include
;
----------
[BLOCK] -- ensure assertions are not trapped by 'others'
do $$
begin
  assert 1=0, 'unhandled assertion';
exception when others then
  null; -- do nothing
end;
$$
;
----------
[DROP_TABLE] DROP TABLE vac_tab_off_toast_on
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_26.pg_temp_28149
;
----------
[CREATE_TABLE] create table conttesttbl(idx serial, v integer)
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE tblspc1_unlogged (id int) TABLESPACE tblspc1
;
----------
[TRUNCATE_TABLE] TRUNCATE atest2
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27601 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."cleast_accum"("anycompatible", VARIADIC "anycompatiblearray") RETURNS "anycompatible"
    LANGUAGE "sql"
    AS $_$select least($1, min($2[i])) from generate_subscripts($2,1) g(i)$_$
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "fkpart5"."fk3" DROP COLUMN "........pg.dropped.1........"
;
----------
[CREATE_TABLE] -- Check that partitioned tables (the parent ones) which don't have visibility
-- maps are rejected
CREATE TABLE test_partitioned (a int, b text default repeat('x', 5000))
			 PARTITION BY list (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_inh_check ALTER COLUMN b TYPE numeric
;
----------
[CREATE_TABLE] CREATE TABLE t1 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(issn, ean13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_sequence
;
----------
[ADD_INDEX] CREATE INDEX sp_radix_ind ON radix_text_tbl USING spgist (t)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- should fail on existing data without the WHERE clause
ALTER TABLE circles ADD EXCLUDE USING gist
  (c1 WITH &&, (c2::circle) WITH &&)
;
----------
[ADMIN_TABLE] ANALYZE public.brinopers
;
----------
[ADD_INDEX] -- UNIQUE index with expression
CREATE UNIQUE INDEX concur_reindex_ind3 ON concur_reindex_tab(abs(c1))
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION regexp_replace(string citext, pattern citext, replacement text) returns TEXT AS $$
    SELECT pg_catalog.regexp_replace( $1::pg_catalog.text, $2::pg_catalog.text, $3, 'i');
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_INDEX] DROP INDEX cwi_replaced_pkey
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_shdescription
;
----------
[ALTER_TYPE] -- unless IF NOT EXISTS is specified
ALTER TYPE planets ADD VALUE IF NOT EXISTS 'mercury'
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33541 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] create table trigtest2 (i int references trigtest(i) on delete cascade)
;
----------
[DROP_PROG_OBJ] drop function current_function(text)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25047_2
;
----------
[ADMIN_TABLE] VACUUM (FULL) vacparted_i
;
----------
[CREATE_TYPE] -- some error cases
create domain d_fail as no_such_type
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  is_normalized(text, text DEFAULT 'NFC')
RETURNS boolean
LANGUAGE internal
STRICT IMMUTABLE PARALLEL SAFE
AS 'unicode_is_normalized'
;
----------
[CREATE_TABLE] create temp table exists_tbl_def partition of exists_tbl default
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."test_type_diff2_c3" INHERIT "public"."test_type_diff2"
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_proc
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_27071
;
----------
[CREATE_TABLE] CREATE TABLE gtest_normal_child2 (a int, b int GENERATED ALWAYS AS (a * 3) STORED)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_time_union(internal, internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function sum4(int8,int8,int8,int8) returns int8 as
'select $1 + $2 + $3 + $4' language sql strict immutable
;
----------
[CREATE_TABLE] -- pre-existing partition
create table pt12 partition of pt for values in (1,2)
;
----------
[DROP_TABLE] -- ERROR

DROP FOREIGN TABLE fd_pt2_1
;
----------
[CREATE_TABLE] create temp table temp_inh_oncommit_test1 ()
  inherits(temp_inh_oncommit_test) on commit delete rows
;
----------
[ADD_INDEX] CREATE INDEX iprt_p2_a ON prt_p2 (a)
;
