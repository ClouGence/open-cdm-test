---------------------------------------------------
-- ISMN13:
CREATE FUNCTION isnlt(ismn13, ismn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '-', null '=-=')
;
CREATE VIEW testpub_view AS SELECT 1
;
CREATE VIEW pg_replication_slots AS
    SELECT
            L.slot_name,
            L.plugin,
            L.slot_type,
            L.datoid,
            D.datname AS database,
            L.temporary,
            L.active,
            L.active_pid,
            L.xmin,
            L.catalog_xmin,
            L.restart_lsn,
            L.confirmed_flush_lsn,
            L.wal_status,
            L.safe_wal_size,
            L.two_phase,
            L.inactive_since,
            L.conflicting,
            L.invalidation_reason,
            L.failover,
            L.synced
    FROM pg_get_replication_slots() AS L
            LEFT JOIN pg_database D ON (L.datoid = D.oid)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub1
;
ALTER TABLE ONLY "public"."dropcolumnchild" DROP COLUMN "........pg.dropped.4........"
;
ANALYZE pg_temp_9.pg_temp_38160_2
;
ALTER TABLE spc_commit SET TABLESPACE other
;
-- Create a test table and GiST index.
CREATE TABLE test_gist AS SELECT point(i,i) p, i::text t FROM
    generate_series(1,1000) i
;
CREATE TABLE split_pgvector_probe.items (
    id bigserial PRIMARY KEY,
    category_id integer,
    embedding vector(3),
    half_embedding halfvec(3),
    sparse_embedding sparsevec(5),
    signature bit(4)
)
;
alter table tpart_default drop a
;
create table at_part_2 (b text, a int)
;
DROP FUNCTION nonsimple_expr_test()
;
CREATE TYPE "public"."arrayrange" AS RANGE (
    subtype = integer[]
)
;
VACUUM (PARALLEL 0) pg_catalog.pg_replication_origin
;
EXPLAIN ANALYZE CREATE TABLE easi2 AS SELECT * FROM int8_tbl WITH NO DATA
;
CREATE TEMP TABLE pg_temp_18.pg_temp_25732_2 (tid pg_catalog.tid)
;
ANALYZE public.itest1
;
-- should fail
create function bogus_func(anycompatible)
  returns anycompatiblerange as 'select int4range(1,10)' language sql
;
DROP TABLE reind_con_tab
;
ALTER TABLE fd_pt1 ADD COLUMN c5 integer DEFAULT 0
;
CREATE VIEW parameters AS
    SELECT CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n_nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(proname, p_oid) AS sql_identifier) AS specific_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(
             CASE WHEN proargmodes IS NULL THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 'i' THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 'o' THEN 'OUT'
                WHEN proargmodes[(ss.x).n] = 'b' THEN 'INOUT'
                WHEN proargmodes[(ss.x).n] = 'v' THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 't' THEN 'OUT'
             END AS character_data) AS parameter_mode,
           CAST('NO' AS yes_or_no) AS is_result,
           CAST('NO' AS yes_or_no) AS as_locator,
           CAST(NULLIF(proargnames[(ss.x).n], '') AS sql_identifier) AS parameter_name,
           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nt.nspname = 'pg_catalog' THEN format_type(t.oid, null)
                  ELSE 'USER-DEFINED' END AS character_data)
             AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,
           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST((ss.x).n AS sql_identifier) AS dtd_identifier,
           CAST(
             CASE WHEN pg_has_role(proowner, 'USAGE')
                  THEN pg_get_function_arg_default(p_oid, (ss.x).n)
                  ELSE NULL END
             AS character_data) AS parameter_default

    FROM pg_type t, pg_namespace nt,
         (SELECT n.nspname AS n_nspname, p.proname, p.oid AS p_oid, p.proowner,
                 p.proargnames, p.proargmodes,
                 _pg_expandarray(coalesce(p.proallargtypes, p.proargtypes::oid[])) AS x
          FROM pg_namespace n, pg_proc p
          WHERE n.oid = p.pronamespace
                AND (pg_has_role(p.proowner, 'USAGE') OR
                     has_function_privilege(p.oid, 'EXECUTE'))) AS ss
    WHERE t.oid = (ss.x).x AND t.typnamespace = nt.oid
;
CREATE OPERATOR #@%# (
   leftarg = int8
)
;
CREATE TABLE "public"."trigger_parted_p2_2" (
    "a" integer NOT NULL
)
;
CREATE TEMP TABLE pg_temp_3.pg_temp_27598_2 (tid pg_catalog.tid)
;
ALTER TABLE temporal_no_key REPLICA IDENTITY FULL
;
CREATE MATERIALIZED VIEW test_maint_mv AS SELECT fn(i) FROM test_maint
;
ALTER TABLE inht1 RENAME a TO aa
;
drop table bigt
;
ALTER TABLE document ENABLE ROW LEVEL SECURITY
;
CREATE SEQUENCE test_sequence_exists
;
create table idxpart4 partition of idxpart for values from ('ddd') to ('eee')
;
CREATE FUNCTION gbt_macad_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
drop index concurrently idxpart_a_idx
;
-- Check shell type create for existing types
CREATE TYPE text_w_default
;
DROP TABLE partitioned, nonpartitioned
;
/* Empty view is a corner case that failed in 9.2. */
CREATE VIEW atestv0 AS SELECT 0 as x WHERE false
;
-- OUT parameters

CREATE PROCEDURE ptest9(OUT a int)
LANGUAGE SQL
AS $$
INSERT INTO cp_test VALUES (1, 'a');
SELECT 1;
$$
;
create table rls_t (c text)
;
-- error
CREATE TABLE gtestx (x int, b int DEFAULT 10) INHERITS (gtest1)
;
CREATE TABLE foo (
	id int PRIMARY KEY,
	data text NOT NULL
  )
;
CREATE FUNCTION gbt_date_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE OR REPLACE FUNCTION test_slru_page_delete(bigint) RETURNS VOID
  AS 'MODULE_PATHNAME', 'test_slru_page_delete' LANGUAGE C
;
DROP MATERIALIZED VIEW mv_unspecified_types
;
VACUUM (FREEZE) pg_catalog.pg_subscription
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_partitioned_table
;
create function inverse(int) returns float8 as
$$
begin
  analyze revalidate_bug;
  return 1::float8/$1;
exception
  when division_by_zero then return 0;
end$$ language plpgsql volatile
;
ANALYZE public.person
;
CREATE VIEW _pg_foreign_servers AS
    SELECT s.oid,
           s.srvoptions,
           CAST(current_database() AS sql_identifier) AS foreign_server_catalog,
           CAST(srvname AS sql_identifier) AS foreign_server_name,
           CAST(current_database() AS sql_identifier) AS foreign_data_wrapper_catalog,
           CAST(w.fdwname AS sql_identifier) AS foreign_data_wrapper_name,
           CAST(srvtype AS character_data) AS foreign_server_type,
           CAST(srvversion AS character_data) AS foreign_server_version,
           CAST(u.rolname AS sql_identifier) AS authorization_identifier
    FROM pg_foreign_server s, pg_foreign_data_wrapper w, pg_authid u
    WHERE w.oid = s.srvfdw
          AND u.oid = s.srvowner
          AND (pg_has_role(s.srvowner, 'USAGE')
               OR has_server_privilege(s.oid, 'USAGE'))
;
ANALYZE public.mvtest_tmm
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISMN13:
CREATE FUNCTION isnlt(ismn13, ismn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', delimiter '-', null '=-=')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW testpub_view AS SELECT 1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_replication_slots AS
    SELECT
            L.slot_name,
            L.plugin,
            L.slot_type,
            L.datoid,
            D.datname AS database,
            L.temporary,
            L.active,
            L.active_pid,
            L.xmin,
            L.catalog_xmin,
            L.restart_lsn,
            L.confirmed_flush_lsn,
            L.wal_status,
            L.safe_wal_size,
            L.two_phase,
            L.inactive_since,
            L.conflicting,
            L.invalidation_reason,
            L.failover,
            L.synced
    FROM pg_get_replication_slots() AS L
            LEFT JOIN pg_database D ON (L.datoid = D.oid)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub1
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."dropcolumnchild" DROP COLUMN "........pg.dropped.4........"
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_38160_2
;
----------
[ALTER_TABLE] ALTER TABLE spc_commit SET TABLESPACE other
;
----------
[CREATE_TABLE|SELECT] -- Create a test table and GiST index.
CREATE TABLE test_gist AS SELECT point(i,i) p, i::text t FROM
    generate_series(1,1000) i
;
----------
[CREATE_TABLE] CREATE TABLE split_pgvector_probe.items (
    id bigserial PRIMARY KEY,
    category_id integer,
    embedding vector(3),
    half_embedding halfvec(3),
    sparse_embedding sparsevec(5),
    signature bit(4)
)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table tpart_default drop a
;
----------
[CREATE_TABLE] create table at_part_2 (b text, a int)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION nonsimple_expr_test()
;
----------
[CREATE_TYPE] CREATE TYPE "public"."arrayrange" AS RANGE (
    subtype = integer[]
)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE|SELECT] EXPLAIN ANALYZE CREATE TABLE easi2 AS SELECT * FROM int8_tbl WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_18.pg_temp_25732_2 (tid pg_catalog.tid)
;
----------
[ADMIN_TABLE] ANALYZE public.itest1
;
----------
[CREATE_PROG_OBJ] -- should fail
create function bogus_func(anycompatible)
  returns anycompatiblerange as 'select int4range(1,10)' language sql
;
----------
[DROP_TABLE] DROP TABLE reind_con_tab
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE fd_pt1 ADD COLUMN c5 integer DEFAULT 0
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW parameters AS
    SELECT CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n_nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(proname, p_oid) AS sql_identifier) AS specific_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(
             CASE WHEN proargmodes IS NULL THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 'i' THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 'o' THEN 'OUT'
                WHEN proargmodes[(ss.x).n] = 'b' THEN 'INOUT'
                WHEN proargmodes[(ss.x).n] = 'v' THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 't' THEN 'OUT'
             END AS character_data) AS parameter_mode,
           CAST('NO' AS yes_or_no) AS is_result,
           CAST('NO' AS yes_or_no) AS as_locator,
           CAST(NULLIF(proargnames[(ss.x).n], '') AS sql_identifier) AS parameter_name,
           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nt.nspname = 'pg_catalog' THEN format_type(t.oid, null)
                  ELSE 'USER-DEFINED' END AS character_data)
             AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,
           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST((ss.x).n AS sql_identifier) AS dtd_identifier,
           CAST(
             CASE WHEN pg_has_role(proowner, 'USAGE')
                  THEN pg_get_function_arg_default(p_oid, (ss.x).n)
                  ELSE NULL END
             AS character_data) AS parameter_default

    FROM pg_type t, pg_namespace nt,
         (SELECT n.nspname AS n_nspname, p.proname, p.oid AS p_oid, p.proowner,
                 p.proargnames, p.proargmodes,
                 _pg_expandarray(coalesce(p.proallargtypes, p.proargtypes::oid[])) AS x
          FROM pg_namespace n, pg_proc p
          WHERE n.oid = p.pronamespace
                AND (pg_has_role(p.proowner, 'USAGE') OR
                     has_function_privilege(p.oid, 'EXECUTE'))) AS ss
    WHERE t.oid = (ss.x).x AND t.typnamespace = nt.oid
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #@%# (
   leftarg = int8
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."trigger_parted_p2_2" (
    "a" integer NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_27598_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE] ALTER TABLE temporal_no_key REPLICA IDENTITY FULL
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW test_maint_mv AS SELECT fn(i) FROM test_maint
;
----------
[RENAME_COLUMN] ALTER TABLE inht1 RENAME a TO aa
;
----------
[DROP_TABLE] drop table bigt
;
----------
[ALTER_TABLE] ALTER TABLE document ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE test_sequence_exists
;
----------
[CREATE_TABLE] create table idxpart4 partition of idxpart for values from ('ddd') to ('eee')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad_fetch(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_INDEX] drop index concurrently idxpart_a_idx
;
----------
[CREATE_TYPE] -- Check shell type create for existing types
CREATE TYPE text_w_default
;
----------
[DROP_TABLE] DROP TABLE partitioned, nonpartitioned
;
----------
[CREATE_VIEW(SELECT)] /* Empty view is a corner case that failed in 9.2. */
CREATE VIEW atestv0 AS SELECT 0 as x WHERE false
;
----------
[CREATE_PROG_OBJ] -- OUT parameters

CREATE PROCEDURE ptest9(OUT a int)
LANGUAGE SQL
AS $$
INSERT INTO cp_test VALUES (1, 'a');
SELECT 1;
$$
;
----------
[CREATE_TABLE] create table rls_t (c text)
;
----------
[CREATE_TABLE] -- error
CREATE TABLE gtestx (x int, b int DEFAULT 10) INHERITS (gtest1)
;
----------
[CREATE_TABLE] CREATE TABLE foo (
	id int PRIMARY KEY,
	data text NOT NULL
  )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_date_union(internal, internal)
RETURNS gbtreekey8
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_delete(bigint) RETURNS VOID
  AS 'MODULE_PATHNAME', 'test_slru_page_delete' LANGUAGE C
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW mv_unspecified_types
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_subscription
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_partitioned_table
;
----------
[CREATE_PROG_OBJ] create function inverse(int) returns float8 as
$$
begin
  analyze revalidate_bug;
  return 1::float8/$1;
exception
  when division_by_zero then return 0;
end$$ language plpgsql volatile
;
----------
[ADMIN_TABLE] ANALYZE public.person
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW _pg_foreign_servers AS
    SELECT s.oid,
           s.srvoptions,
           CAST(current_database() AS sql_identifier) AS foreign_server_catalog,
           CAST(srvname AS sql_identifier) AS foreign_server_name,
           CAST(current_database() AS sql_identifier) AS foreign_data_wrapper_catalog,
           CAST(w.fdwname AS sql_identifier) AS foreign_data_wrapper_name,
           CAST(srvtype AS character_data) AS foreign_server_type,
           CAST(srvversion AS character_data) AS foreign_server_version,
           CAST(u.rolname AS sql_identifier) AS authorization_identifier
    FROM pg_foreign_server s, pg_foreign_data_wrapper w, pg_authid u
    WHERE w.oid = s.srvfdw
          AND u.oid = s.srvowner
          AND (pg_has_role(s.srvowner, 'USAGE')
               OR has_server_privilege(s.oid, 'USAGE'))
;
----------
[ADMIN_TABLE] ANALYZE public.mvtest_tmm
;
