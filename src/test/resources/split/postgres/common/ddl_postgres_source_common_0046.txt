CREATE PUBLICATION testpub FOR TABLE t
;
-- Cases where schema creation fails as objects are qualified with a schema
-- that does not match with what's expected.
-- This checks all the object types that include schema qualifications.
CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE SEQUENCE schema_not_existing.seq
;
----
-- Make sure non index cases work
----
ALTER TABLE test_replica_identity REPLICA IDENTITY DEFAULT
;
create table rangep_0_to_100_3 partition of rangep_0_to_100 for values in(3)
;
ALTER TYPE "public"."rainbow" ADD VALUE 'red'
;
ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE x
;
-- fail, no versions for libc

CREATE COLLATION test4 FROM nonsense
;
CREATE MATERIALIZED VIEW tableam_tblmv_heap2 USING heap2 AS SELECT * FROM tableam_tbl_heap2
;
create table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=100)
;
-- Float4 type.
CREATE TABLE hash_heap_float4 (x float4, y int)
;
-- but the method can be explicitly specified
CREATE TABLE tableam_parted_c_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('c') USING heap
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_default_acl
;
CREATE FUNCTION citext(inet)
RETURNS citext
AS 'network_show'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
create type intr_multirange as (x int, y int)
;
create table ppk1 partition of ppk for values in (1)
;
drop table t_opf
;
CREATE TABLE t2 (x int, y text)
;
DROP TABLE brin_large_range
;
create table inh_lp1 (a int, value int, check(a = 1)) inherits (inh_lp)
;
ALTER TABLE tab_replidentity_index REPLICA IDENTITY USING INDEX idx_replidentity_index_b
;
drop index trgm_idx2
;
-- Use only relations with a physical size of zero.
CREATE TABLE tbl_heap_psql(f1 int, f2 char(100)) using heap_psql
;
TRUNCATE atestc
;
do $$ declare h hstore;
begin h['a'] := 'b'; raise notice 'h = %, h[a] = %', h, h['a']; end $$
;
create trigger parted_conflict_update
    after update on parted_conflict
    referencing new table as inserted
    for each statement
    execute procedure parted_conflict_update_func()
;
CREATE DATABASE conflict_db TEMPLATE conflict_db_template OID = 50001
;
CREATE TABLE replication_metadata (
    id serial primary key,
    relation name NOT NULL,
    options text[]
)
WITH (user_catalog_table = true)
;
DROP TABLE hard_err
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_enum
;
CREATE FUNCTION cube(cube, float8, float8) RETURNS cube
AS '$libdir/cube', 'cube_c_f8_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE "public"."prt2_e_p1" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
ALTER TABLE pg_toast.pg_toast_28187 RENAME TO reind_con_toast
;
CREATE TEXT SEARCH CONFIGURATION serbian
	(PARSER = default)
;
create table public.stuffs (stuff text)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_database
;
-- all of the above should be accepted

-- tests for array aggregates
CREATE TEMP TABLE arraggtest ( f1 INT[], f2 TEXT[][], f3 FLOAT[])
;
ALTER TABLE attmp ADD COLUMN k int4
;
create table booleans as
select
  case i%3 when 0 then true when 1 then false else null end as x,
  case (i/3)%3 when 0 then true when 1 then false else null end as y,
  case (i/9)%3 when 0 then true when 1 then false else null end as z,
  case (i/27)%3 when 0 then true when 1 then false else null end as w
from generate_series(0, 3*3*3*3-1) i
;
-- int4 check

CREATE TABLE int4tmp (a int4)
;
CREATE TABLE test_replica_id_full (x int, y int)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=10962 host=/tmp/u67lV1GsQY dbname=postgres' PUBLICATION pub1
;
CREATE AGGREGATE myaggp04a(*) (SFUNC = stfp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
drop table header_copytest
;
create function bogus_func(int)
  returns anyrange as 'select int4range(1,10)' language sql
;
CREATE FUNCTION seg_over_right(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE corrupt2 SET TABLESPACE ts_corrupt
;
-- verify that attaching a table checks that the existing data satisfies the
-- constraint
CREATE TABLE fk_partitioned_fk_2 (a int, b int) PARTITION BY RANGE (b)
;
CREATE TEMP TABLE pg_temp_10.pg_temp_26679_2 (tid pg_catalog.tid)
;
CREATE TABLE cmdata3(f1 text)
;
CREATE FUNCTION brin_page_items(IN page bytea, IN index_oid regclass,
    OUT itemoffset int,
    OUT blknum int8,
    OUT attnum int,
    OUT allnulls bool,
    OUT hasnulls bool,
    OUT placeholder bool,
    OUT value text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'brin_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub FOR TABLE t
;
----------
[CREATE_SCHEMA] -- Cases where schema creation fails as objects are qualified with a schema
-- that does not match with what's expected.
-- This checks all the object types that include schema qualifications.
CREATE SCHEMA AUTHORIZATION regress_create_schema_role
  CREATE SEQUENCE schema_not_existing.seq
;
----------
[ALTER_TABLE] ----
-- Make sure non index cases work
----
ALTER TABLE test_replica_identity REPLICA IDENTITY DEFAULT
;
----------
[CREATE_TABLE] create table rangep_0_to_100_3 partition of rangep_0_to_100 for values in(3)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."rainbow" ADD VALUE 'red'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN id SET DATA TYPE x
;
----------
[CREATE_POLICY] -- fail, no versions for libc

CREATE COLLATION test4 FROM nonsense
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW tableam_tblmv_heap2 USING heap2 AS SELECT * FROM tableam_tbl_heap2
;
----------
[CREATE_TABLE] create table pgbench_branches(bid int not null,bbalance int,filler char(88)) with (fillfactor=100)
;
----------
[CREATE_TABLE] -- Float4 type.
CREATE TABLE hash_heap_float4 (x float4, y int)
;
----------
[CREATE_TABLE] -- but the method can be explicitly specified
CREATE TABLE tableam_parted_c_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('c') USING heap
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_default_acl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION citext(inet)
RETURNS citext
AS 'network_show'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TYPE] create type intr_multirange as (x int, y int)
;
----------
[CREATE_TABLE] create table ppk1 partition of ppk for values in (1)
;
----------
[DROP_TABLE] drop table t_opf
;
----------
[CREATE_TABLE] CREATE TABLE t2 (x int, y text)
;
----------
[DROP_TABLE] DROP TABLE brin_large_range
;
----------
[CREATE_TABLE] create table inh_lp1 (a int, value int, check(a = 1)) inherits (inh_lp)
;
----------
[ALTER_TABLE] ALTER TABLE tab_replidentity_index REPLICA IDENTITY USING INDEX idx_replidentity_index_b
;
----------
[DROP_INDEX] drop index trgm_idx2
;
----------
[CREATE_TABLE] -- Use only relations with a physical size of zero.
CREATE TABLE tbl_heap_psql(f1 int, f2 char(100)) using heap_psql
;
----------
[TRUNCATE_TABLE] TRUNCATE atestc
;
----------
[BLOCK] do $$ declare h hstore;
begin h['a'] := 'b'; raise notice 'h = %, h[a] = %', h, h['a']; end $$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parted_conflict_update
    after update on parted_conflict
    referencing new table as inserted
    for each statement
    execute procedure parted_conflict_update_func()
;
----------
[CREATE_CATALOG] CREATE DATABASE conflict_db TEMPLATE conflict_db_template OID = 50001
;
----------
[CREATE_TABLE] CREATE TABLE replication_metadata (
    id serial primary key,
    relation name NOT NULL,
    options text[]
)
WITH (user_catalog_table = true)
;
----------
[DROP_TABLE] DROP TABLE hard_err
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_enum
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube(cube, float8, float8) RETURNS cube
AS '$libdir/cube', 'cube_c_f8_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_e_p1" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28187 RENAME TO reind_con_toast
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION serbian
	(PARSER = default)
;
----------
[CREATE_TABLE] create table public.stuffs (stuff text)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_database
;
----------
[CREATE_TABLE] -- all of the above should be accepted

-- tests for array aggregates
CREATE TEMP TABLE arraggtest ( f1 INT[], f2 TEXT[][], f3 FLOAT[])
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN k int4
;
----------
[CREATE_TABLE|SELECT] create table booleans as
select
  case i%3 when 0 then true when 1 then false else null end as x,
  case (i/3)%3 when 0 then true when 1 then false else null end as y,
  case (i/9)%3 when 0 then true when 1 then false else null end as z,
  case (i/27)%3 when 0 then true when 1 then false else null end as w
from generate_series(0, 3*3*3*3-1) i
;
----------
[CREATE_TABLE] -- int4 check

CREATE TABLE int4tmp (a int4)
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_id_full (x int, y int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=10962 host=/tmp/u67lV1GsQY dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp04a(*) (SFUNC = stfp, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[DROP_TABLE] drop table header_copytest
;
----------
[CREATE_PROG_OBJ] create function bogus_func(int)
  returns anyrange as 'select int4range(1,10)' language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_over_right(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE corrupt2 SET TABLESPACE ts_corrupt
;
----------
[CREATE_TABLE] -- verify that attaching a table checks that the existing data satisfies the
-- constraint
CREATE TABLE fk_partitioned_fk_2 (a int, b int) PARTITION BY RANGE (b)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_10.pg_temp_26679_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE cmdata3(f1 text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION brin_page_items(IN page bytea, IN index_oid regclass,
    OUT itemoffset int,
    OUT blknum int8,
    OUT attnum int,
    OUT allnulls bool,
    OUT hasnulls bool,
    OUT placeholder bool,
    OUT value text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'brin_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
