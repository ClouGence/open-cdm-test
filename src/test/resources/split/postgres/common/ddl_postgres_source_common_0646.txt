ALTER TABLE fd_pt1 ALTER COLUMN c4 SET DEFAULT 0
;
CREATE TABLE t2 (cc int)
;
CREATE FUNCTION ok_cmp1 (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT public.ok_cmp($1, $2);
	$$
;
ANALYZE target
;
-- Only one default partition is allowed, hence, following should give error
CREATE TABLE partr_def2 (LIKE part1 INCLUDING CONSTRAINTS)
;
CREATE TABLE "public"."fk_partitioned_fk_5_1" (
    "a" integer,
    "b" integer
)
;
-- not supported
ALTER TABLE regtest_table ADD CONSTRAINT test_ck CHECK (b like '%abc%') NOT VALID
;
ALTER TABLE "public"."pitest2" ALTER COLUMN "f3" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."pitest2_f3_seq"
    START WITH 1000
    INCREMENT BY 2
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
create table parted_trig_2 partition of parted_trig for values from (1000) to (2000)
;
CREATE TABLE plt1_adv_extra PARTITION OF plt1_adv FOR VALUES IN (NULL)
;
DROP TEXT SEARCH CONFIGURATION IF EXISTS split_ts_probe.cfg_copy RESTRICT
;
-- Temporary tables and on-commit actions, where CONCURRENTLY is ignored.
-- ON COMMIT PRESERVE ROWS, the default.
CREATE TEMP TABLE concur_temp_tab_1 (c1 int, c2 text)
  ON COMMIT PRESERVE ROWS
;
create rule qqq as on update to copydml_test where new.t <> 'f' do instead delete from copydml_test
;
DROP TABLE itest16
;
ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser
    ALTER MAPPING FOR asciiword WITH simple
;
drop table mcrparted5
;
CREATE INDEX "iix" ON "public"."ihighway" USING "btree" ("name")
;
CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE ((42))
;
CREATE TABLE testschema.dflt (a int PRIMARY KEY USING INDEX TABLESPACE regress_tblspace) PARTITION BY LIST (a) TABLESPACE regress_tblspace
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_rel
;
drop table agg_group_3
;
create table s3.p2 (a int, b int) PARTITION BY list (a)
;
--
-- Test range types over domains
--

create domain mydomain as int4
;
DROP CAST IF EXISTS (split_obj_priv.cast_mood AS integer)
;
drop type insert_test_type cascade
;
CREATE SEQUENCE "public"."clstr_tst_s_rf_a_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
CREATE INDEX "grect2ind" ON "public"."fast_emp4000" USING "gist" ("home_base")
;
DROP TABLE queue
;
comment on table "catalog1"."schema1"."table_name" is 'ss'
;
CREATE TEMP TABLE pg_temp_6.pg_temp_33826_2 (tid pg_catalog.tid)
;
CREATE TABLE "public"."credit_card" (
    "cid" integer,
    "cnum" "text",
    "climit" integer
)
;
-- DO block - top-level tracking.
CREATE TABLE stats_track_tab (x int)
;
-- sequences, views and foreign servers shouldn't have an AM
CREATE VIEW tableam_view_heapx AS SELECT * FROM tableam_tbl_heapx
;
CREATE FUNCTION testprs_end(internal)
RETURNS void
AS 'MODULE_PATHNAME'
LANGUAGE C STRICT
;
create table parted_copytest_a2 (a int, c text, b int)
;
drop table test_mode
;
VACUUM (FULL) pg_catalog.pg_largeobject
;
CREATE OPERATOR CLASS gist_seg_ops
DEFAULT FOR TYPE seg USING gist
AS
	OPERATOR	1	<< ,
	OPERATOR	2	&< ,
	OPERATOR	3	&& ,
	OPERATOR	4	&> ,
	OPERATOR	5	>> ,
	OPERATOR	6	= ,
	OPERATOR	7	@> ,
	OPERATOR	8	<@ ,
	OPERATOR	13	@ ,
	OPERATOR	14	~ ,
	FUNCTION	1	gseg_consistent (internal, seg, smallint, oid, internal),
	FUNCTION	2	gseg_union (internal, internal),
	FUNCTION	3	gseg_compress (internal),
	FUNCTION	4	gseg_decompress (internal),
	FUNCTION	5	gseg_penalty (internal, internal, internal),
	FUNCTION	6	gseg_picksplit (internal, internal),
	FUNCTION	7	gseg_same (seg, seg, internal)
;
CREATE SEQUENCE xpto_rand_seq START 79 INCREMENT 1499
;
-- Test corner cases in dropping of inherited columns

create table p1 (f1 int, f2 int)
;
ALTER OPERATOR ~ (lquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
DROP PROCEDURE IF EXISTS test_ambiguous_procname
;
DO $$
DECLARE
  loid oid;
BEGIN
  SELECT tbl.loid INTO loid FROM lotest_stash_values tbl;
  PERFORM lo_export(loid, '/src/src/test/regress/results/invalid/path');
EXCEPTION
  WHEN UNDEFINED_FILE THEN RAISE NOTICE 'could not open file, as expected';
END;
$$
;
ANALYZE public.prt2_n_p1
;
create table inh_nn2 (f2 text, f3 int, f1 int)
;
--
-- A unique index can prove functional dependency for GROUP BY column
-- removal only if its per-column collation agrees on equality with
-- the GROUP BY column's collation.  An index built under a different
-- (deterministic) collation would otherwise let remove_useless_groupby_columns
-- drop other columns whose values still differ within a nondeterministic
-- group.
--
CREATE TABLE groupby_collation_t (a text COLLATE case_insensitive NOT NULL, b text)
;
create table range_list_parted (
	a	int,
	b	char(2)
) partition by range (a)
;
CREATE TABLE part3_self_fk (	-- a partitioned partition
	id bigint NOT NULL PRIMARY KEY,
	id_abc bigint
) PARTITION BY RANGE (id)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
CREATE TABLE brin_test_multi (a INT, b INT)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c4 SET DEFAULT 0
;
----------
[CREATE_TABLE] CREATE TABLE t2 (cc int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ok_cmp1 (int4, int4)
	RETURNS int LANGUAGE sql AS
	$$
		SELECT public.ok_cmp($1, $2);
	$$
;
----------
[ADMIN_TABLE] ANALYZE target
;
----------
[CREATE_TABLE] -- Only one default partition is allowed, hence, following should give error
CREATE TABLE partr_def2 (LIKE part1 INCLUDING CONSTRAINTS)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_5_1" (
    "a" integer,
    "b" integer
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- not supported
ALTER TABLE regtest_table ADD CONSTRAINT test_ck CHECK (b like '%abc%') NOT VALID
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."pitest2" ALTER COLUMN "f3" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."pitest2_f3_seq"
    START WITH 1000
    INCREMENT BY 2
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[CREATE_TABLE] create table parted_trig_2 partition of parted_trig for values from (1000) to (2000)
;
----------
[CREATE_TABLE] CREATE TABLE plt1_adv_extra PARTITION OF plt1_adv FOR VALUES IN (NULL)
;
----------
[DROP_POLICY] DROP TEXT SEARCH CONFIGURATION IF EXISTS split_ts_probe.cfg_copy RESTRICT
;
----------
[CREATE_TABLE] -- Temporary tables and on-commit actions, where CONCURRENTLY is ignored.
-- ON COMMIT PRESERVE ROWS, the default.
CREATE TEMP TABLE concur_temp_tab_1 (c1 int, c2 text)
  ON COMMIT PRESERVE ROWS
;
----------
[CREATE_POLICY] create rule qqq as on update to copydml_test where new.t <> 'f' do instead delete from copydml_test
;
----------
[DROP_TABLE] DROP TABLE itest16
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser
    ALTER MAPPING FOR asciiword WITH simple
;
----------
[DROP_TABLE] drop table mcrparted5
;
----------
[ADD_INDEX] CREATE INDEX "iix" ON "public"."ihighway" USING "btree" ("name")
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE ((42))
;
----------
[CREATE_TABLE] CREATE TABLE testschema.dflt (a int PRIMARY KEY USING INDEX TABLESPACE regress_tblspace) PARTITION BY LIST (a) TABLESPACE regress_tblspace
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_rel
;
----------
[DROP_TABLE] drop table agg_group_3
;
----------
[CREATE_TABLE] create table s3.p2 (a int, b int) PARTITION BY list (a)
;
----------
[CREATE_TYPE] --
-- Test range types over domains
--

create domain mydomain as int4
;
----------
[DROP_PROG_OBJ] DROP CAST IF EXISTS (split_obj_priv.cast_mood AS integer)
;
----------
[DROP_TYPE] drop type insert_test_type cascade
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."clstr_tst_s_rf_a_seq"
    AS integer
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ADD_INDEX] CREATE INDEX "grect2ind" ON "public"."fast_emp4000" USING "gist" ("home_base")
;
----------
[DROP_TABLE] DROP TABLE queue
;
----------
[COMMENT_TABLE] comment on table "catalog1"."schema1"."table_name" is 'ss'
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_6.pg_temp_33826_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."credit_card" (
    "cid" integer,
    "cnum" "text",
    "climit" integer
)
;
----------
[CREATE_TABLE] -- DO block - top-level tracking.
CREATE TABLE stats_track_tab (x int)
;
----------
[CREATE_VIEW(SELECT)] -- sequences, views and foreign servers shouldn't have an AM
CREATE VIEW tableam_view_heapx AS SELECT * FROM tableam_tbl_heapx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testprs_end(internal)
RETURNS void
AS 'MODULE_PATHNAME'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] create table parted_copytest_a2 (a int, c text, b int)
;
----------
[DROP_TABLE] drop table test_mode
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_largeobject
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_seg_ops
DEFAULT FOR TYPE seg USING gist
AS
	OPERATOR	1	<< ,
	OPERATOR	2	&< ,
	OPERATOR	3	&& ,
	OPERATOR	4	&> ,
	OPERATOR	5	>> ,
	OPERATOR	6	= ,
	OPERATOR	7	@> ,
	OPERATOR	8	<@ ,
	OPERATOR	13	@ ,
	OPERATOR	14	~ ,
	FUNCTION	1	gseg_consistent (internal, seg, smallint, oid, internal),
	FUNCTION	2	gseg_union (internal, internal),
	FUNCTION	3	gseg_compress (internal),
	FUNCTION	4	gseg_decompress (internal),
	FUNCTION	5	gseg_penalty (internal, internal, internal),
	FUNCTION	6	gseg_picksplit (internal, internal),
	FUNCTION	7	gseg_same (seg, seg, internal)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE xpto_rand_seq START 79 INCREMENT 1499
;
----------
[CREATE_TABLE] -- Test corner cases in dropping of inherited columns

create table p1 (f1 int, f2 int)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ~ (lquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE IF EXISTS test_ambiguous_procname
;
----------
[BLOCK] DO $$
DECLARE
  loid oid;
BEGIN
  SELECT tbl.loid INTO loid FROM lotest_stash_values tbl;
  PERFORM lo_export(loid, '/src/src/test/regress/results/invalid/path');
EXCEPTION
  WHEN UNDEFINED_FILE THEN RAISE NOTICE 'could not open file, as expected';
END;
$$
;
----------
[ADMIN_TABLE] ANALYZE public.prt2_n_p1
;
----------
[CREATE_TABLE] create table inh_nn2 (f2 text, f3 int, f1 int)
;
----------
[CREATE_TABLE] --
-- A unique index can prove functional dependency for GROUP BY column
-- removal only if its per-column collation agrees on equality with
-- the GROUP BY column's collation.  An index built under a different
-- (deterministic) collation would otherwise let remove_useless_groupby_columns
-- drop other columns whose values still differ within a nondeterministic
-- group.
--
CREATE TABLE groupby_collation_t (a text COLLATE case_insensitive NOT NULL, b text)
;
----------
[CREATE_TABLE] create table range_list_parted (
	a	int,
	b	char(2)
) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE part3_self_fk (	-- a partitioned partition
	id bigint NOT NULL PRIMARY KEY,
	id_abc bigint
) PARTITION BY RANGE (id)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_language
;
----------
[CREATE_TABLE] CREATE TABLE brin_test_multi (a INT, b INT)
;
