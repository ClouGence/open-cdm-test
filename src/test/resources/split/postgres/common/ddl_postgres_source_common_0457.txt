ANALYZE public.beta_pos_p2
;
CREATE TABLE pred_tab (a int NOT NULL, b int, c int NOT NULL)
;
DROP TABLE pg_temp_14.pg_temp_25613_2, pg_temp_14.pg_temp_25613
;
ANALYZE pg_temp_14.pg_temp_26504
;
CREATE TRIGGER "gtest12_02" BEFORE INSERT OR UPDATE ON "generated_stored_tests"."gtest26" FOR EACH ROW EXECUTE FUNCTION "generated_stored_tests"."gtest_trigger_func4"()
;
ALTER INDEX "regress_indexing"."pk5_pkey" ATTACH PARTITION "regress_indexing"."pk51_pkey"
;
-- 2 new columns, neither has default
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
CREATE TABLE onek (
	unique1		int4,
	unique2		int4,
	two			int4,
	four		int4,
	ten			int4,
	twenty		int4,
	hundred		int4,
	thousand	int4,
	twothousand	int4,
	fivethous	int4,
	tenthous	int4,
	odd			int4,
	even		int4,
	stringu1	name,
	stringu2	name,
	string4		name
)
;
--
-- ISMN operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
alter table idxpart1 drop column col1, drop column col2, drop column col3
;
-- Now let's try it with an AFTER trigger

DROP TRIGGER t_raster ON image
;
VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_parts
;
CREATE SCHEMA generated_stored_tests
;
-- WHERE CURRENT OF may someday work with views, but today is not that day.
-- For now, just make sure it errors out cleanly.
CREATE TEMP VIEW ucview AS SELECT * FROM uctest
;
CREATE SEQUENCE wgo_then_regular
;
CREATE TRIGGER trig_row_after
AFTER UPDATE OR DELETE ON bar2
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
CREATE EVENT TRIGGER start_rls_command ON ddl_command_start
    WHEN TAG IN ('CREATE POLICY', 'ALTER POLICY', 'DROP POLICY') EXECUTE PROCEDURE start_command()
;
alter table parted_index_col_drop drop column c
;
alter table atacc1 drop c
;
ALTER SEQUENCE sequence_test2 START WITH 1
;
-- LIKE INCLUDING on partition
CREATE TABLE pitest1_p1_like (LIKE pitest1_p1 INCLUDING IDENTITY)
;
CREATE FUNCTION myvarcharout(myvarchar) RETURNS cstring
LANGUAGE internal IMMUTABLE PARALLEL SAFE STRICT AS 'varcharout'
;
ALTER INDEX pg_toast.pg_toast_33564_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION btisbncmp(isbn, isbn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP TYPE IF EXISTS split_domain_type_next.mood CASCADE
;
DROP SCHEMA public
;
DROP TABLE lcu_table
;
DROP TRANSFORM IF EXISTS FOR fake_type LANGUAGE plperl
;
ALTER TABLE t ATTACH PARTITION tp FOR VALUES IN (1)
;
CREATE TABLE reloptions_test2(i INT) WITH (not_existing_namespace.fillfactor=2)
;
ALTER TABLE parent ALTER COLUMN a SET NOT NULL
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_ts_dict
;
create index idxpart1_a_b_idx on idxpart1 (a, b)
;
CREATE FUNCTION foo_f() RETURNS SETOF foo_t AS
  $$ SELECT * FROM foo OFFSET 0 $$ LANGUAGE sql STABLE
;
VACUUM pg_catalog.pg_namespace
;
CREATE FUNCTION dup (f1 anycompatiblerange, f2 out anycompatible, f3 out anycompatiblearray, f4 out anycompatiblerange)
AS 'select lower($1), array[lower($1), upper($1)], $1' LANGUAGE sql
;
-- Check that comments on constraints and indexes are not lost at ALTER TABLE.
CREATE TABLE comment_test (
  id int,
  positive_col int CHECK (positive_col > 0),
  indexed_col int,
  CONSTRAINT comment_test_pk PRIMARY KEY (id))
;
create temp view v1 as
select f1::int from t1 left join t2 using (f1) group by f1
;
-- rebuild the index with a different fillfactor
alter index gist_pointidx SET (fillfactor = 40)
;
ALTER TABLE pg_description RENAME TO pg_comment
;
create table rparted_by_int2_16384 partition of rparted_by_int2 for values from (10) to (16384)
;
DROP TABLE sub_parent
;
DROP TABLE measurement, new_measurement CASCADE
;
CREATE INDEX "hash_txt_index" ON "public"."hash_txt_heap" USING "hash" ("random")
;
CREATE TEMP TABLE past_inh_db_child () INHERITS (past_inh_db_parent)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_ts_dict
;
-- fails
DROP INDEX tid_ind
;
CREATE TABLE testpub_tbl8_0 (a int, b text, c text)
;
create function explain_parallel_append(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+ loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        return next ln;
    end loop;
end;
$$
;
SECURITY LABEL ON FUNCTION f4()
    IS 'system_u:object_r:sepgsql_nosuch_trusted_proc_exec_t:s0'
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE public.beta_pos_p2
;
----------
[CREATE_TABLE] CREATE TABLE pred_tab (a int NOT NULL, b int, c int NOT NULL)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_14.pg_temp_25613_2, pg_temp_14.pg_temp_25613
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_14.pg_temp_26504
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "gtest12_02" BEFORE INSERT OR UPDATE ON "generated_stored_tests"."gtest26" FOR EACH ROW EXECUTE FUNCTION "generated_stored_tests"."gtest_trigger_func4"()
;
----------
[ALTER_INDEX] ALTER INDEX "regress_indexing"."pk5_pkey" ATTACH PARTITION "regress_indexing"."pk51_pkey"
;
----------
[CREATE_TABLE] -- 2 new columns, neither has default
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
----------
[CREATE_TABLE] CREATE TABLE onek (
	unique1		int4,
	unique2		int4,
	two			int4,
	four		int4,
	ten			int4,
	twenty		int4,
	hundred		int4,
	thousand	int4,
	twothousand	int4,
	fivethous	int4,
	tenthous	int4,
	odd			int4,
	even		int4,
	stringu1	name,
	stringu2	name,
	string4		name
)
;
----------
[CREATE_PROG_OBJ] --
-- ISMN operators:
--
---------------------------------------------------
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn,
	RIGHTARG = ismn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart1 drop column col1, drop column col2, drop column col3
;
----------
[DROP_TRIGGER] -- Now let's try it with an AFTER trigger

DROP TRIGGER t_raster ON image
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) information_schema.sql_parts
;
----------
[CREATE_SCHEMA] CREATE SCHEMA generated_stored_tests
;
----------
[CREATE_VIEW(SELECT)] -- WHERE CURRENT OF may someday work with views, but today is not that day.
-- For now, just make sure it errors out cleanly.
CREATE TEMP VIEW ucview AS SELECT * FROM uctest
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE wgo_then_regular
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_after
AFTER UPDATE OR DELETE ON bar2
FOR EACH ROW EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE EVENT TRIGGER start_rls_command ON ddl_command_start
    WHEN TAG IN ('CREATE POLICY', 'ALTER POLICY', 'DROP POLICY') EXECUTE PROCEDURE start_command()
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table parted_index_col_drop drop column c
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table atacc1 drop c
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test2 START WITH 1
;
----------
[CREATE_TABLE] -- LIKE INCLUDING on partition
CREATE TABLE pitest1_p1_like (LIKE pitest1_p1 INCLUDING IDENTITY)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION myvarcharout(myvarchar) RETURNS cstring
LANGUAGE internal IMMUTABLE PARALLEL SAFE STRICT AS 'varcharout'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33564_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btisbncmp(isbn, isbn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TYPE] DROP TYPE IF EXISTS split_domain_type_next.mood CASCADE
;
----------
[DROP_SCHEMA] DROP SCHEMA public
;
----------
[DROP_TABLE] DROP TABLE lcu_table
;
----------
[UNKNOWN] DROP TRANSFORM IF EXISTS FOR fake_type LANGUAGE plperl
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE t ATTACH PARTITION tp FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (not_existing_namespace.fillfactor=2)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE parent ALTER COLUMN a SET NOT NULL
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_ts_dict
;
----------
[ADD_INDEX] create index idxpart1_a_b_idx on idxpart1 (a, b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION foo_f() RETURNS SETOF foo_t AS
  $$ SELECT * FROM foo OFFSET 0 $$ LANGUAGE sql STABLE
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_namespace
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dup (f1 anycompatiblerange, f2 out anycompatible, f3 out anycompatiblearray, f4 out anycompatiblerange)
AS 'select lower($1), array[lower($1), upper($1)], $1' LANGUAGE sql
;
----------
[CREATE_TABLE] -- Check that comments on constraints and indexes are not lost at ALTER TABLE.
CREATE TABLE comment_test (
  id int,
  positive_col int CHECK (positive_col > 0),
  indexed_col int,
  CONSTRAINT comment_test_pk PRIMARY KEY (id))
;
----------
[CREATE_VIEW(SELECT)] create temp view v1 as
select f1::int from t1 left join t2 using (f1) group by f1
;
----------
[ALTER_INDEX] -- rebuild the index with a different fillfactor
alter index gist_pointidx SET (fillfactor = 40)
;
----------
[RENAME_TABLE] ALTER TABLE pg_description RENAME TO pg_comment
;
----------
[CREATE_TABLE] create table rparted_by_int2_16384 partition of rparted_by_int2 for values from (10) to (16384)
;
----------
[DROP_TABLE] DROP TABLE sub_parent
;
----------
[DROP_TABLE] DROP TABLE measurement, new_measurement CASCADE
;
----------
[ADD_INDEX] CREATE INDEX "hash_txt_index" ON "public"."hash_txt_heap" USING "hash" ("random")
;
----------
[CREATE_TABLE] CREATE TEMP TABLE past_inh_db_child () INHERITS (past_inh_db_parent)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_ts_dict
;
----------
[DROP_INDEX] -- fails
DROP INDEX tid_ind
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl8_0 (a int, b text, c text)
;
----------
[CREATE_PROG_OBJ] create function explain_parallel_append(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+ loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        return next ln;
    end loop;
end;
$$
;
----------
[UNKNOWN] SECURITY LABEL ON FUNCTION f4()
    IS 'system_u:object_r:sepgsql_nosuch_trusted_proc_exec_t:s0'
;
