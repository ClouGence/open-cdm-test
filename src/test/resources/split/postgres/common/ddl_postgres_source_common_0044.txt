CREATE VIEW "public"."pfield_v1" AS
 SELECT "pf"."pfname",
    "pf"."slotname",
    "public"."pslot_backlink_view"("pf"."slotname") AS "backside",
    "public"."pslot_slotlink_view"("pf"."slotname") AS "patch"
   FROM "public"."pslot" "pf"
;
CREATE TABLE part_d_1_15 PARTITION OF part_c_100_200 FOR VALUES FROM (1) TO (15)
;
CREATE TEMP TABLE z2 (b TEXT, UNIQUE(aa, b)) inherits (a)
;
alter index idxpart_a_b_idx attach partition idxpart1_2_a_b
;
CREATE FUNCTION modify_rel_block(rel regclass, blockno int,
  zero bool DEFAULT false,
  corrupt_header bool DEFAULT false,
  corrupt_checksum bool DEFAULT false)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
CREATE AGGREGATE "public"."myaggp20a"("anyelement") (
    SFUNC = "public"."tfp",
    STYPE = "anyarray",
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
CREATE TYPE "public"."complex" AS (
	"r" double precision,
	"i" double precision
)
;
CREATE TABLE base_tbl(id int PRIMARY KEY, data text, deleted boolean)
;
-- ISMN vs other types:
CREATE FUNCTION btismncmp(ismn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_auth_members
;
CREATE DOMAIN "public"."gtestdomain1" AS integer
	CONSTRAINT "gtestdomain1_check" CHECK ((VALUE < 10))
;
CREATE TABLE constraint_rename_test (a int CONSTRAINT con1 CHECK (a > 0), b int, c int)
;
ALTER TABLE tbl2 ADD COLUMN val3 INTEGER
;
CREATE TABLE pitest_pfail PARTITION OF pitest3 (
    f3 WITH OPTIONS GENERATED ALWAYS AS IDENTITY
) FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
DROP TABLE test_squash_jsonb
;
-- fail, can't infer element type

drop function polyf(x anyarray)
;
drop function void_return_expr()
;
CREATE RULE rule1 AS ON INSERT TO ruletest_v1
    DO INSTEAD INSERT INTO ruletest_t2 VALUES (NEW.*)
;
ALTER INDEX pg_toast.pg_toast_27791_index RENAME TO reind_con_toast_idx
;
-- not supported by sepgsql
ALTER TABLE regtest_ptable ALTER p SET DEFAULT 'XYZ'
;
TRUNCATE bttest_multi
;
ANALYZE pg_temp_12.pg_temp_25367_2
;
create temp view usersview as
SELECT * FROM ROWS FROM(get_users(), generate_series(10,11)) WITH ORDINALITY
;
CREATE TABLE testpub_tbl3a (b text) INHERITS (testpub_tbl3)
;
-- check dependency restrictions
ALTER TABLE main_table DROP COLUMN b
;
ALTER FOREIGN TABLE split_fdw_repl.remote_accounts ADD COLUMN note2 text OPTIONS (column_name 'note2')
;
ALTER TABLE test_replica_identity5 DROP CONSTRAINT test_replica_identity5_pkey
;
CREATE FUNCTION btisbncmp(isbn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE my_schema_1.pts1 (o int, p text) PARTITION BY RANGE (o)
;
create table idxpart (a int unique, b int) partition by range ((b + a))
;
--
-- Also try building functional, expressional, and partial indexes on
-- tables that already contain data.
--
create unique index hash_f8_index_1 on hash_f8_heap(abs(random))
;
CREATE EXTENSION test_dsa
;
DO LANGUAGE plpgsql $$begin return 1; end$$
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (sum(a))
;
DROP SCHEMA IF EXISTS split_obj_priv CASCADE
;
CREATE TYPE int_custom (
  INPUT = int_custom_in,
  OUTPUT = int_custom_out,
  LIKE = int4
)
;
vacuum (disable_page_skipping) regular_table
;
-- check tables with dropped indexes used in REPLICA IDENTITY
-- table with primary key
CREATE TABLE table_dropped_index_with_pk (a int PRIMARY KEY, b int, c int)
;
-- not supported by sepgsql
ALTER TABLE regtest_ptable ALTER p SET NOT NULL
;
ALTER TYPE test_type1 ALTER ATTRIBUTE b TYPE varchar
;
-- Try again with an equality function that treats NULLs as equal to 0.
create or replace function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return false;
  else
    return coalesce($1::int,0) = coalesce($2::int, 0);
  end if;
end;
$$ language plpgsql immutable
;
CREATE TABLE pk5 (c int, b int, a int NOT NULL) PARTITION BY RANGE (a)
;
CREATE FUNCTION isn_out(issn)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE droppk2_d PARTITION OF droppk2 DEFAULT
;
---------------------------------------------------
-- ISBN13:
CREATE FUNCTION isnlt(isbn13, isbn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
drop event trigger no_rewrite_allowed
;
create table part_abc_3_1 partition of part_abc_3 for values from (minvalue) to (1)
;
VACUUM ANALYZE parted
;
-- ERROR
ALTER FOREIGN TABLE fd_pt2_1 ADD CONSTRAINT fd_pt2chk1 CHECK (c1 > 0)
;
create table regular_table (a int)
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."pfield_v1" AS
 SELECT "pf"."pfname",
    "pf"."slotname",
    "public"."pslot_backlink_view"("pf"."slotname") AS "backside",
    "public"."pslot_slotlink_view"("pf"."slotname") AS "patch"
   FROM "public"."pslot" "pf"
;
----------
[CREATE_TABLE] CREATE TABLE part_d_1_15 PARTITION OF part_c_100_200 FOR VALUES FROM (1) TO (15)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE z2 (b TEXT, UNIQUE(aa, b)) inherits (a)
;
----------
[ALTER_INDEX] alter index idxpart_a_b_idx attach partition idxpart1_2_a_b
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION modify_rel_block(rel regclass, blockno int,
  zero bool DEFAULT false,
  corrupt_header bool DEFAULT false,
  corrupt_checksum bool DEFAULT false)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp20a"("anyelement") (
    SFUNC = "public"."tfp",
    STYPE = "anyarray",
    INITCOND = '{}',
    FINALFUNC = "public"."ffp"
)
;
----------
[CREATE_TYPE] CREATE TYPE "public"."complex" AS (
	"r" double precision,
	"i" double precision
)
;
----------
[CREATE_TABLE] CREATE TABLE base_tbl(id int PRIMARY KEY, data text, deleted boolean)
;
----------
[CREATE_PROG_OBJ] -- ISMN vs other types:
CREATE FUNCTION btismncmp(ismn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_auth_members
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."gtestdomain1" AS integer
	CONSTRAINT "gtestdomain1_check" CHECK ((VALUE < 10))
;
----------
[CREATE_TABLE] CREATE TABLE constraint_rename_test (a int CONSTRAINT con1 CHECK (a > 0), b int, c int)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tbl2 ADD COLUMN val3 INTEGER
;
----------
[CREATE_TABLE] CREATE TABLE pitest_pfail PARTITION OF pitest3 (
    f3 WITH OPTIONS GENERATED ALWAYS AS IDENTITY
) FOR VALUES FROM ('2016-07-01') TO ('2016-08-01')
;
----------
[DROP_TABLE] DROP TABLE test_squash_jsonb
;
----------
[DROP_PROG_OBJ] -- fail, can't infer element type

drop function polyf(x anyarray)
;
----------
[DROP_PROG_OBJ] drop function void_return_expr()
;
----------
[CREATE_POLICY] CREATE RULE rule1 AS ON INSERT TO ruletest_v1
    DO INSTEAD INSERT INTO ruletest_t2 VALUES (NEW.*)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27791_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- not supported by sepgsql
ALTER TABLE regtest_ptable ALTER p SET DEFAULT 'XYZ'
;
----------
[TRUNCATE_TABLE] TRUNCATE bttest_multi
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_25367_2
;
----------
[CREATE_VIEW(SELECT)] create temp view usersview as
SELECT * FROM ROWS FROM(get_users(), generate_series(10,11)) WITH ORDINALITY
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl3a (b text) INHERITS (testpub_tbl3)
;
----------
[ALTER_TABLE|DROP_COLUMN] -- check dependency restrictions
ALTER TABLE main_table DROP COLUMN b
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE split_fdw_repl.remote_accounts ADD COLUMN note2 text OPTIONS (column_name 'note2')
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE test_replica_identity5 DROP CONSTRAINT test_replica_identity5_pkey
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btisbncmp(isbn, ean13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE my_schema_1.pts1 (o int, p text) PARTITION BY RANGE (o)
;
----------
[CREATE_TABLE] create table idxpart (a int unique, b int) partition by range ((b + a))
;
----------
[ADD_INDEX] --
-- Also try building functional, expressional, and partial indexes on
-- tables that already contain data.
--
create unique index hash_f8_index_1 on hash_f8_heap(abs(random))
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_dsa
;
----------
[BLOCK] DO LANGUAGE plpgsql $$begin return 1; end$$
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (sum(a))
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_obj_priv CASCADE
;
----------
[CREATE_TYPE] CREATE TYPE int_custom (
  INPUT = int_custom_in,
  OUTPUT = int_custom_out,
  LIKE = int4
)
;
----------
[ADMIN_TABLE] vacuum (disable_page_skipping) regular_table
;
----------
[CREATE_TABLE] -- check tables with dropped indexes used in REPLICA IDENTITY
-- table with primary key
CREATE TABLE table_dropped_index_with_pk (a int PRIMARY KEY, b int, c int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- not supported by sepgsql
ALTER TABLE regtest_ptable ALTER p SET NOT NULL
;
----------
[ALTER_TYPE] ALTER TYPE test_type1 ALTER ATTRIBUTE b TYPE varchar
;
----------
[CREATE_PROG_OBJ] -- Try again with an equality function that treats NULLs as equal to 0.
create or replace function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return false;
  else
    return coalesce($1::int,0) = coalesce($2::int, 0);
  end if;
end;
$$ language plpgsql immutable
;
----------
[CREATE_TABLE] CREATE TABLE pk5 (c int, b int, a int NOT NULL) PARTITION BY RANGE (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isn_out(issn)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE droppk2_d PARTITION OF droppk2 DEFAULT
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISBN13:
CREATE FUNCTION isnlt(isbn13, isbn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TRIGGER] drop event trigger no_rewrite_allowed
;
----------
[CREATE_TABLE] create table part_abc_3_1 partition of part_abc_3 for values from (minvalue) to (1)
;
----------
[ADMIN_TABLE] VACUUM ANALYZE parted
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- ERROR
ALTER FOREIGN TABLE fd_pt2_1 ADD CONSTRAINT fd_pt2chk1 CHECK (c1 > 0)
;
----------
[CREATE_TABLE] create table regular_table (a int)
;
