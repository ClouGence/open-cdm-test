/* End test case for bug #16242 */

/* Test case for bug #17409 */

create table attbl (p1 int constraint pk_attbl primary key)
;
-- Verify that columns are mapped correctly for WHERE in a partial index
create table idxpart (col1 int, a int, col3 int, b int) partition by range (a)
;
-- Two scalar columns
DROP TABLE temporal_rng2
;
/* src/test/modules/test_extensions/test_event_trigger--1.0--2.0.sql */
-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Test extension upgrade with event trigger.
ALTER EVENT TRIGGER table_rewrite_trg DISABLE
;
CREATE OR REPLACE FUNCTION pg_stop_backup (
        exclusive boolean, wait_for_archive boolean DEFAULT true,
        OUT lsn pg_lsn, OUT labelfile text, OUT spcmapfile text)
  RETURNS SETOF record STRICT VOLATILE LANGUAGE internal as 'pg_stop_backup_v2'
  PARALLEL RESTRICTED
;
drop index concurrently idxpart_temp_a_idx
;
create table test ( a int , b text )
;
CREATE FUNCTION my_other_func(int) RETURNS int LANGUAGE SQL
AS $$ SELECT $1 + 1 $$
;
alter table idxpart0 add unique (a)
;
CREATE TABLE "public"."tableam_parted_a_heap2" (
    "a" "text",
    "b" integer
)
;
ALTER TABLE ONLY "public"."mlparted_def1" DROP COLUMN "........pg.dropped.5........"
;
ALTER OPERATOR >= (isbn13, isbn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
DROP TABLE conflict_test
;
ALTER TABLE pg_toast.pg_toast_33136 RENAME TO reind_con_toast
;
-- check if compatible with the specified parent

-- cannot create as partition of a non-partitioned table
CREATE TABLE unparted (
	a int
)
;
ANALYZE public.idxpart31
;
CREATE AGGREGATE myaggn18a(BASETYPE = int, SFUNC = tfp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
ANALYZE prt1_n
;
ALTER FOREIGN TABLE async_p2 OPTIONS (DROP use_remote_estimate)
;
create trigger aft_row after insert or update on trigger_parted
  for each row execute function trigger_parted_trigfunc()
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_default_acl
;
DROP INDEX grect2ind
;
CREATE DATABASE foobar4 TEMPLATE template0 LOCALE_PROVIDER icu
;
ALTER TABLE ONLY "generated_stored_tests"."gtest24"
    ADD CONSTRAINT "gtest24_pkey" PRIMARY KEY ("a")
;
create table �׻����Ѹ� (�Ѹ� text, ʬ�ॳ���� varchar, ����1A���� char(16))
;
CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_analyze_scale_factor=true)
;
ALTER TABLE pg_temp_11.pg_temp_26571_2 ADD COLUMN newdata pg_temp_11.pg_temp_26571
;
create temp table p (a int, b int) partition by range (a)
;
create or replace function donothingbrtrig_func() returns trigger as $$begin raise notice 'b: %', new.b; return NULL; end$$ language plpgsql
;
DROP TABLE atest3
;
ALTER TABLE ONLY "generated_stored_tests"."gtest29" DROP COLUMN "........pg.dropped.1........"
;
ALTER TABLE rf_tbl_abcd_nopk REPLICA IDENTITY USING INDEX idx_abcd_nopk_c
;
DROP INDEX concur_reindex_ind5_ccnew
;
ALTER TABLE tbl ALTER c3 TYPE bigint
;
CREATE FUNCTION _ltree_picksplit(internal, internal)
RETURNS internal
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE test1ci (x text COLLATE case_insensitive)
;
create table idxpart1 partition of idxpart for values from (0) to (100)
;
CREATE INDEX sp_kd_ind ON kd_point_tbl USING spgist (p kd_point_ops)
;
-- invalid use of aggregate
CREATE TABLE default_expr_agg (a int DEFAULT (avg(1)))
;
CREATE PROCEDURE "public"."ptest6b"(IN "a" "anyelement", OUT "b" "anyelement", OUT "c" "anyarray")
    LANGUAGE "sql"
    AS $_$
SELECT $1, array[$1];
$_$
;
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD
  -- int4 vs int2
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
CREATE FUNCTION test_pglz_compress(bytea)
  RETURNS bytea
  AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
CREATE FUNCTION "public"."equipment_named"("hobby" "public"."hobbies_r") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where equipment_r.hobby = equipment_named.hobby.name$$
;
CREATE VIEW city_view AS
    SELECT city_id, city_name, population, country_name, continent
    FROM city_table ci
    LEFT JOIN country_table co ON co.country_id = ci.country_id
;
drop table �t�Ӹ��
;
CREATE FUNCTION btean13cmp(ean13, ismn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_28131 RENAME TO reind_con_toast
;
-- a child with a different column order
create table child3 (b int, a text)
;
ALTER TABLE ONLY "public"."pagg_tab_ml" ATTACH PARTITION "public"."pagg_tab_ml_p1" FOR VALUES FROM (0) TO (10)
;
create index hash_idx on hash_tbl using hash(p)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] /* End test case for bug #16242 */

/* Test case for bug #17409 */

create table attbl (p1 int constraint pk_attbl primary key)
;
----------
[CREATE_TABLE] -- Verify that columns are mapped correctly for WHERE in a partial index
create table idxpart (col1 int, a int, col3 int, b int) partition by range (a)
;
----------
[DROP_TABLE] -- Two scalar columns
DROP TABLE temporal_rng2
;
----------
[ALTER_TRIGGER] /* src/test/modules/test_extensions/test_event_trigger--1.0--2.0.sql */
-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Test extension upgrade with event trigger.
ALTER EVENT TRIGGER table_rewrite_trg DISABLE
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_stop_backup (
        exclusive boolean, wait_for_archive boolean DEFAULT true,
        OUT lsn pg_lsn, OUT labelfile text, OUT spcmapfile text)
  RETURNS SETOF record STRICT VOLATILE LANGUAGE internal as 'pg_stop_backup_v2'
  PARALLEL RESTRICTED
;
----------
[DROP_INDEX] drop index concurrently idxpart_temp_a_idx
;
----------
[CREATE_TABLE] create table test ( a int , b text )
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION my_other_func(int) RETURNS int LANGUAGE SQL
AS $$ SELECT $1 + 1 $$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart0 add unique (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tableam_parted_a_heap2" (
    "a" "text",
    "b" integer
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def1" DROP COLUMN "........pg.dropped.5........"
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (isbn13, isbn) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[DROP_TABLE] DROP TABLE conflict_test
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33136 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- check if compatible with the specified parent

-- cannot create as partition of a non-partitioned table
CREATE TABLE unparted (
	a int
)
;
----------
[ADMIN_TABLE] ANALYZE public.idxpart31
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn18a(BASETYPE = int, SFUNC = tfp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ADMIN_TABLE] ANALYZE prt1_n
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE async_p2 OPTIONS (DROP use_remote_estimate)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger aft_row after insert or update on trigger_parted
  for each row execute function trigger_parted_trigfunc()
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_default_acl
;
----------
[DROP_INDEX] DROP INDEX grect2ind
;
----------
[CREATE_CATALOG] CREATE DATABASE foobar4 TEMPLATE template0 LOCALE_PROVIDER icu
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest24"
    ADD CONSTRAINT "gtest24_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TABLE] create table �׻����Ѹ� (�Ѹ� text, ʬ�ॳ���� varchar, ����1A���� char(16))
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (autovacuum_analyze_scale_factor=true)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26571_2 ADD COLUMN newdata pg_temp_11.pg_temp_26571
;
----------
[CREATE_TABLE] create temp table p (a int, b int) partition by range (a)
;
----------
[CREATE_PROG_OBJ] create or replace function donothingbrtrig_func() returns trigger as $$begin raise notice 'b: %', new.b; return NULL; end$$ language plpgsql
;
----------
[DROP_TABLE] DROP TABLE atest3
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_stored_tests"."gtest29" DROP COLUMN "........pg.dropped.1........"
;
----------
[ALTER_TABLE] ALTER TABLE rf_tbl_abcd_nopk REPLICA IDENTITY USING INDEX idx_abcd_nopk_c
;
----------
[DROP_INDEX] DROP INDEX concur_reindex_ind5_ccnew
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl ALTER c3 TYPE bigint
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_picksplit(internal, internal)
RETURNS internal
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE test1ci (x text COLLATE case_insensitive)
;
----------
[CREATE_TABLE] create table idxpart1 partition of idxpart for values from (0) to (100)
;
----------
[ADD_INDEX] CREATE INDEX sp_kd_ind ON kd_point_tbl USING spgist (p kd_point_ops)
;
----------
[CREATE_TABLE] -- invalid use of aggregate
CREATE TABLE default_expr_agg (a int DEFAULT (avg(1)))
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest6b"(IN "a" "anyelement", OUT "b" "anyelement", OUT "c" "anyarray")
    LANGUAGE "sql"
    AS $_$
SELECT $1, array[$1];
$_$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf4 USING btree ADD
  -- int4 vs int2
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_pglz_compress(bytea)
  RETURNS bytea
  AS '/src/src/test/regress/regress.so' LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."equipment_named"("hobby" "public"."hobbies_r") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where equipment_r.hobby = equipment_named.hobby.name$$
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW city_view AS
    SELECT city_id, city_name, population, country_name, continent
    FROM city_table ci
    LEFT JOIN country_table co ON co.country_id = ci.country_id
;
----------
[DROP_TABLE] drop table �t�Ӹ��
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btean13cmp(ean13, ismn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28131 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- a child with a different column order
create table child3 (b int, a text)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."pagg_tab_ml" ATTACH PARTITION "public"."pagg_tab_ml_p1" FOR VALUES FROM (0) TO (10)
;
----------
[ADD_INDEX] create index hash_idx on hash_tbl using hash(p)
;
