ANALYZE pg_temp_13.pg_temp_26115_2
;
DROP TABLE bug18634t CASCADE
;
ALTER TABLESPACE regress_tblspace RESET (random_page_cost, effective_io_concurrency)
;
ALTER TABLE ONLY "public"."gtest23q"
    ADD CONSTRAINT "gtest23q_pkey" PRIMARY KEY ("a")
;
ALTER INDEX "public"."trigger_parted_pkey" ATTACH PARTITION "public"."trigger_parted_p2_pkey"
;
ALTER FUNCTION interval_dist(interval, interval) PARALLEL SAFE
;
/*
 * text search configuration for hungarian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hungarian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
create function cache_test_2() returns int as $$
begin
	return f1 from v1;
end$$ language plpgsql
;
ALTER INDEX "public"."idxpart3_a_idx" ATTACH PARTITION "public"."idxpart32_a_idx"
;
CREATE TYPE nnint_container AS (f1 int, f2 nnint)
;
create table bigger_than_it_looks as
  select generate_series(1, 20000) as id, 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'
;
CREATE TABLE persons2 OF person_type (
    id WITH OPTIONS PRIMARY KEY,
    UNIQUE (name)
)
;
CREATE FUNCTION test_argresult_array_domain(x ordered_pair_domain)
  RETURNS ordered_pair_domain AS $$
begin
return x;
end
$$ LANGUAGE plpgsql
;
ALTER TABLE tab5 ATTACH PARTITION tab5_1 DEFAULT
;
CREATE SUBSCRIPTION regress_mysub1 CONNECTION 'port=29713 host=/tmp/jOoI3lrZrW dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub1_slot, copy_data = false, failover = true, enabled = false)
;
-- no attach partition validation occurs for foreign tables
ALTER TABLE fd_pt2 ATTACH PARTITION fd_pt2_1 FOR VALUES IN (1)
;
ALTER TYPE "public"."insenum" ADD VALUE 'i3'
;
-- renaming index should rename constraint as well
ALTER TABLE onek ADD CONSTRAINT onek_unique1_constraint UNIQUE (unique1)
;
CREATE TABLE rls_part PARTITION OF rls_ptbl FOR VALUES FROM (-100) TO (100)
;
CREATE FUNCTION base_tbl_trig_fn()
RETURNS trigger AS
$$
BEGIN
  NEW.b := 10;
  RETURN NEW;
END;
$$
LANGUAGE plpgsql
;
CREATE FUNCTION equipment(hobbies_r)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where hobby = $1.name'
   LANGUAGE SQL
;
-- fail - invalid connection string
CREATE SUBSCRIPTION regress_testsub CONNECTION 'testconn' PUBLICATION testpub
;
create table mlparted_def1 partition of mlparted_def for values from (40) to (50)
;
-- this must be rejected to avoid self-inclusion issues:
alter type two_ints add attribute c two_ints_multirange
;
drop table convslot_test_parent
;
CREATE TABLE test_tab_2 (a int)
;
-- various prohibited constructs
CREATE TABLE gtest_err_7a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (avg(a)) STORED)
;
ALTER PUBLICATION testpub_fortable DROP TABLE testpub_tbl5
;
create index gist_tbl_box_index_forcing_buffering on gist_tbl using gist (p)
  with (buffering=on, fillfactor=50)
;
CREATE FUNCTION ttdummy ()
        RETURNS trigger
        AS '/src/src/test/regress/regress.so'
        LANGUAGE C
;
ALTER TABLE ONLY "public"."nv_child_2010" INHERIT "public"."nv_parent"
;
ALTER DATABASE "regression" SET "bytea_output" TO 'hex'
;
ALTER OPERATOR @> (ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
drop table text_support_test
;
ANALYZE pg_temp_9.pg_temp_26686
;
CREATE TYPE split_maint.status_enum AS ENUM ('new', 'done')
;
ALTER TABLE temporal3 DROP COLUMN valid_thru
;
do $$ declare c two_int8s; d nested_int8s;
begin
  c := row(1,2);
  d := row(c, row(c.q1, c.q2+1));
  raise notice 'c = %, d = %', c, d;
  c.q1 := 10;
  d.c1 := row(11,12);
  d.c2.q2 := 42;
  raise notice 'c = %, d = %', c, d;
  raise notice 'c.q1 = %, d.c2 = %', c.q1, d.c2;
  raise notice '(d).c2.q2 = %', (d).c2.q2;  -- doesn't work without parens
  raise notice '(d.c2).q2 = %', (d.c2).q2;  -- doesn't work without parens
end$$
;
ALTER OPERATOR ~ (_ltree, lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
DROP TABLE gtestx
;
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, PRIMARY KEY(ptest1, ptest2) )
;
CREATE TABLE tableam_parted_d_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('d') USING heap2
;
ALTER TABLE "public"."itest18" ALTER COLUMN "a" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest18_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
create table test ( f float , i int , a int [ 10 ] , text char ( 10 ) , ts timestamp [ 10 ] , n numeric [ 10 ] , d date [ 10 ] , inter interval [ 10 ] )
;
CREATE UNIQUE INDEX citext_matview_id
  ON citext_matview (id)
;
CREATE FUNCTION ismn_in(cstring)
	RETURNS ismn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- but if it's in a shippable extension, it can be shipped
ALTER EXTENSION postgres_fdw ADD TEXT SEARCH CONFIGURATION public.custom_search
;
DROP INDEX CONCURRENTLY idx
;
alter table test alter column a1 set default 23
;
/*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26115_2
;
----------
[DROP_TABLE] DROP TABLE bug18634t CASCADE
;
----------
[ALTER_TABLESPACE] ALTER TABLESPACE regress_tblspace RESET (random_page_cost, effective_io_concurrency)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest23q"
    ADD CONSTRAINT "gtest23q_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_INDEX] ALTER INDEX "public"."trigger_parted_pkey" ATTACH PARTITION "public"."trigger_parted_p2_pkey"
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION interval_dist(interval, interval) PARALLEL SAFE
;
----------
[CREATE_POLICY] /*
 * text search configuration for hungarian language
 *
 * Copyright (c) 2007-2019, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hungarian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
----------
[CREATE_PROG_OBJ] create function cache_test_2() returns int as $$
begin
	return f1 from v1;
end$$ language plpgsql
;
----------
[ALTER_INDEX] ALTER INDEX "public"."idxpart3_a_idx" ATTACH PARTITION "public"."idxpart32_a_idx"
;
----------
[CREATE_TYPE] CREATE TYPE nnint_container AS (f1 int, f2 nnint)
;
----------
[CREATE_TABLE|SELECT] create table bigger_than_it_looks as
  select generate_series(1, 20000) as id, 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'
;
----------
[CREATE_TABLE] CREATE TABLE persons2 OF person_type (
    id WITH OPTIONS PRIMARY KEY,
    UNIQUE (name)
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_argresult_array_domain(x ordered_pair_domain)
  RETURNS ordered_pair_domain AS $$
begin
return x;
end
$$ LANGUAGE plpgsql
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE tab5 ATTACH PARTITION tab5_1 DEFAULT
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_mysub1 CONNECTION 'port=29713 host=/tmp/jOoI3lrZrW dbname=postgres' PUBLICATION regress_mypub WITH (slot_name = lsub1_slot, copy_data = false, failover = true, enabled = false)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- no attach partition validation occurs for foreign tables
ALTER TABLE fd_pt2 ATTACH PARTITION fd_pt2_1 FOR VALUES IN (1)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i3'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- renaming index should rename constraint as well
ALTER TABLE onek ADD CONSTRAINT onek_unique1_constraint UNIQUE (unique1)
;
----------
[CREATE_TABLE] CREATE TABLE rls_part PARTITION OF rls_ptbl FOR VALUES FROM (-100) TO (100)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION base_tbl_trig_fn()
RETURNS trigger AS
$$
BEGIN
  NEW.b := 10;
  RETURN NEW;
END;
$$
LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION equipment(hobbies_r)
   RETURNS setof equipment_r
   AS 'select * from equipment_r where hobby = $1.name'
   LANGUAGE SQL
;
----------
[CREATE_PUB_SUB] -- fail - invalid connection string
CREATE SUBSCRIPTION regress_testsub CONNECTION 'testconn' PUBLICATION testpub
;
----------
[CREATE_TABLE] create table mlparted_def1 partition of mlparted_def for values from (40) to (50)
;
----------
[ALTER_TYPE] -- this must be rejected to avoid self-inclusion issues:
alter type two_ints add attribute c two_ints_multirange
;
----------
[DROP_TABLE] drop table convslot_test_parent
;
----------
[CREATE_TABLE] CREATE TABLE test_tab_2 (a int)
;
----------
[CREATE_TABLE] -- various prohibited constructs
CREATE TABLE gtest_err_7a (a int PRIMARY KEY, b int GENERATED ALWAYS AS (avg(a)) STORED)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_fortable DROP TABLE testpub_tbl5
;
----------
[ADD_INDEX] create index gist_tbl_box_index_forcing_buffering on gist_tbl using gist (p)
  with (buffering=on, fillfactor=50)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ttdummy ()
        RETURNS trigger
        AS '/src/src/test/regress/regress.so'
        LANGUAGE C
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."nv_child_2010" INHERIT "public"."nv_parent"
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET "bytea_output" TO 'hex'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @> (ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_TABLE] drop table text_support_test
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26686
;
----------
[CREATE_TYPE] CREATE TYPE split_maint.status_enum AS ENUM ('new', 'done')
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE temporal3 DROP COLUMN valid_thru
;
----------
[BLOCK] do $$ declare c two_int8s; d nested_int8s;
begin
  c := row(1,2);
  d := row(c, row(c.q1, c.q2+1));
  raise notice 'c = %, d = %', c, d;
  c.q1 := 10;
  d.c1 := row(11,12);
  d.c2.q2 := 42;
  raise notice 'c = %, d = %', c, d;
  raise notice 'c.q1 = %, d.c2 = %', c.q1, d.c2;
  raise notice '(d).c2.q2 = %', (d).c2.q2;  -- doesn't work without parens
  raise notice '(d.c2).q2 = %', (d.c2).q2;  -- doesn't work without parens
end$$
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ~ (_ltree, lquery)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_TABLE] DROP TABLE gtestx
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, PRIMARY KEY(ptest1, ptest2) )
;
----------
[CREATE_TABLE] CREATE TABLE tableam_parted_d_heap2 PARTITION OF tableam_parted_heap2 FOR VALUES IN ('d') USING heap2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest18" ALTER COLUMN "a" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."itest18_a_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[CREATE_TABLE] create table test ( f float , i int , a int [ 10 ] , text char ( 10 ) , ts timestamp [ 10 ] , n numeric [ 10 ] , d date [ 10 ] , inter interval [ 10 ] )
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX citext_matview_id
  ON citext_matview (id)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ismn_in(cstring)
	RETURNS ismn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_LIBRARY] -- but if it's in a shippable extension, it can be shipped
ALTER EXTENSION postgres_fdw ADD TEXT SEARCH CONFIGURATION public.custom_search
;
----------
[DROP_INDEX] DROP INDEX CONCURRENTLY idx
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table test alter column a1 set default 23
;
----------
[CREATE_POLICY] /*
 * text search configuration for spanish language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * spanish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY spanish_stem
	(TEMPLATE = snowball, Language = spanish , StopWords=spanish)
;
