CREATE FUNCTION "public"."sql_if"(boolean, "anyelement", "anyelement") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$
select case when $1 then $2 else $3 end $_$
;
CREATE CAST (ismn13 AS ismn) WITHOUT FUNCTION AS ASSIGNMENT
;
-- add attributes recursively
ALTER TABLE fd_pt1 ADD COLUMN c4 integer
;
-- Tests to check soft-error support for populate_record_field()

-- populate_scalar()
create type jsb_char2 as (a char(2))
;
ALTER SUBSCRIPTION regress_testsub SET (streaming = false)
;
create table parted_validate_test (a int) partition by list (a)
;
-- requires NOT NULL constraint
ALTER TABLE pitest1_p2 ALTER COLUMN f3 SET NOT NULL
;
alter table pgbench_branches add primary key (bid) using index tablespace "pg_default"
;
DROP TABLE two_not_null_constraints
;
ALTER PUBLICATION pub2 SET (publish = 'insert, truncate')
;
ALTER TABLE gtest31_2 DROP CONSTRAINT cc
;
VACUUM FREEZE test_recovery_conflict_table1
;
ALTER OPERATOR >= (seg, seg) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel
)
;
create table part_default partition of list_parted default partition by range(b)
;
CREATE DATABASE "regression	 !""x""#$%&'()*+-./0123456" WITH TEMPLATE = template0 ENCODING = 'LATIN1' LOCALE = 'C'
;
CREATE VIEW european_city_view AS
    SELECT * FROM city_view WHERE continent = 'Europe'
;
alter operator family alter1.ctype_hash_ops using hash set schema alter2
;
create operator = (
    procedure = int8alias1eq,
    leftarg = int8, rightarg = int8alias1,
    restrict = eqsel, join = eqjoinsel,
    merges
)
;
CREATE SUBSCRIPTION tap_sub_sch CONNECTION 'port=25797 host=/tmp/REnOPq3M6y dbname=postgres' PUBLICATION tap_pub_sch
;
CREATE INDEX brin_test_multi_1_idx_2 ON brin_test_multi_1 USING brin (b int8_minmax_multi_ops) WITH (pages_per_range=5)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
CREATE FUNCTION get_raw_page(text, int4)
RETURNS bytea
AS '$libdir/pageinspect', 'get_raw_page'
LANGUAGE C STRICT PARALLEL SAFE
;
DROP TYPE test2
;
CREATE OPERATOR % (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = similarity_op,
        COMMUTATOR = '%',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
COMMENT ON TEXT SEARCH DICTIONARY spanish_stem IS 'snowball stemmer for spanish language'
;
create table rangep_100_to_200 partition of rangep for values from (100) to (200)
;
---------------------------------------------------
-- ISSN13:
CREATE FUNCTION isnlt(issn13, issn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- Test ALTER DOMAIN .. DEFAULT ..
create table domdeftest (col1 ddef1)
;
CREATE FUNCTION get_command_type(pg_ddl_command)
  RETURNS text IMMUTABLE STRICT
  AS '$libdir/test_ddl_deparse' LANGUAGE C
;
CREATE FUNCTION pgstatginindex(IN relname regclass,
    OUT version INT4,
    OUT pending_pages INT4,
    OUT pending_tuples BIGINT)
AS '$libdir/pgstattuple', 'pgstatginindex'
LANGUAGE C STRICT PARALLEL SAFE
;
create or replace function stricttest() returns void as $$
declare x record;
begin
  -- too many rows, no parameters
  execute 'select * from foo where f1 > 3' into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
DROP EXTENSION file_fdw CASCADE
;
--
-- check that stable query clauses are only used in run-time pruning
--
create table stable_qual_pruning (a timestamp) partition by range (a)
;
/*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
ANALYZE d_star
;
DROP TABLE copy_t CASCADE
;
-- sql, proretset = f, prorettype = b
CREATE FUNCTION getrngfunc1(int) RETURNS int AS 'SELECT $1;' LANGUAGE SQL
;
DROP TABLE parttbl
;
-- Create a default partition for range partitioned table
CREATE TABLE range2_default PARTITION OF range_parted2 DEFAULT
;
ALTER TEXT SEARCH DICTIONARY alt_ts_dict3 SET SCHEMA alt_nsp2
;
CREATE FUNCTION gin_extract_query_bytea(bytea, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
-- lets do some naming tests
create table atacc1 (test int, test2 int, primary key(test))
;
create table iocdu_tt_parted2 partition of iocdu_tt_parted for values in (2)
;
-- Check that we can re-use a SQLFunctionCache after a run-time error.

-- This function will fail with zero-divide at run time (not plan time).
CREATE FUNCTION part_hashint4_error(value int4, seed int8) RETURNS int8
LANGUAGE SQL STRICT IMMUTABLE PARALLEL SAFE AS
$$ SELECT value + seed + random()::int/0 $$
;
-- ok
/* The next *should* fail, but it's not implemented that way yet. */
CREATE VIEW atestv2 AS SELECT * FROM atest2
;
--
-- FLOAT8
--

CREATE TABLE FLOAT8_TBL(f1 float8)
;
CREATE TABLE large_tuple_test (a int, b text) WITH (fillfactor = 10)
;
create trigger parent_insert_trig
  after insert on parent referencing new table as new_table
  for each statement execute procedure dump_insert()
;
ALTER TABLE main_table DROP COLUMN b
;
create table hs4 (col1 integer)
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."sql_if"(boolean, "anyelement", "anyelement") RETURNS "anyelement"
    LANGUAGE "sql"
    AS $_$
select case when $1 then $2 else $3 end $_$
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ismn13 AS ismn) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[ALTER_TABLE|ADD_COLUMN] -- add attributes recursively
ALTER TABLE fd_pt1 ADD COLUMN c4 integer
;
----------
[CREATE_TYPE] -- Tests to check soft-error support for populate_record_field()

-- populate_scalar()
create type jsb_char2 as (a char(2))
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION regress_testsub SET (streaming = false)
;
----------
[CREATE_TABLE] create table parted_validate_test (a int) partition by list (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- requires NOT NULL constraint
ALTER TABLE pitest1_p2 ALTER COLUMN f3 SET NOT NULL
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_branches add primary key (bid) using index tablespace "pg_default"
;
----------
[DROP_TABLE] DROP TABLE two_not_null_constraints
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub2 SET (publish = 'insert, truncate')
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE gtest31_2 DROP CONSTRAINT cc
;
----------
[ADMIN_TABLE] VACUUM FREEZE test_recovery_conflict_table1
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (seg, seg) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel
)
;
----------
[CREATE_TABLE] create table part_default partition of list_parted default partition by range(b)
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression	 !""x""#$%&'()*+-./0123456" WITH TEMPLATE = template0 ENCODING = 'LATIN1' LOCALE = 'C'
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW european_city_view AS
    SELECT * FROM city_view WHERE continent = 'Europe'
;
----------
[ALTER_PROG_OBJ] alter operator family alter1.ctype_hash_ops using hash set schema alter2
;
----------
[CREATE_PROG_OBJ] create operator = (
    procedure = int8alias1eq,
    leftarg = int8, rightarg = int8alias1,
    restrict = eqsel, join = eqjoinsel,
    merges
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_sch CONNECTION 'port=25797 host=/tmp/REnOPq3M6y dbname=postgres' PUBLICATION tap_pub_sch
;
----------
[ADD_INDEX] CREATE INDEX brin_test_multi_1_idx_2 ON brin_test_multi_1 USING brin (b int8_minmax_multi_ops) WITH (pages_per_range=5)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_parameter_acl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION get_raw_page(text, int4)
RETURNS bytea
AS '$libdir/pageinspect', 'get_raw_page'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[DROP_TYPE] DROP TYPE test2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR % (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = similarity_op,
        COMMUTATOR = '%',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY spanish_stem IS 'snowball stemmer for spanish language'
;
----------
[CREATE_TABLE] create table rangep_100_to_200 partition of rangep for values from (100) to (200)
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISSN13:
CREATE FUNCTION isnlt(issn13, issn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- Test ALTER DOMAIN .. DEFAULT ..
create table domdeftest (col1 ddef1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION get_command_type(pg_ddl_command)
  RETURNS text IMMUTABLE STRICT
  AS '$libdir/test_ddl_deparse' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgstatginindex(IN relname regclass,
    OUT version INT4,
    OUT pending_pages INT4,
    OUT pending_tuples BIGINT)
AS '$libdir/pgstattuple', 'pgstatginindex'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare x record;
begin
  -- too many rows, no parameters
  execute 'select * from foo where f1 > 3' into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[DROP_LIBRARY] DROP EXTENSION file_fdw CASCADE
;
----------
[CREATE_TABLE] --
-- check that stable query clauses are only used in run-time pruning
--
create table stable_qual_pruning (a timestamp) partition by range (a)
;
----------
[CREATE_POLICY] /*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
----------
[ADMIN_TABLE] ANALYZE d_star
;
----------
[DROP_TABLE] DROP TABLE copy_t CASCADE
;
----------
[CREATE_PROG_OBJ] -- sql, proretset = f, prorettype = b
CREATE FUNCTION getrngfunc1(int) RETURNS int AS 'SELECT $1;' LANGUAGE SQL
;
----------
[DROP_TABLE] DROP TABLE parttbl
;
----------
[CREATE_TABLE] -- Create a default partition for range partitioned table
CREATE TABLE range2_default PARTITION OF range_parted2 DEFAULT
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY alt_ts_dict3 SET SCHEMA alt_nsp2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_bytea(bytea, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_TABLE] -- lets do some naming tests
create table atacc1 (test int, test2 int, primary key(test))
;
----------
[CREATE_TABLE] create table iocdu_tt_parted2 partition of iocdu_tt_parted for values in (2)
;
----------
[CREATE_PROG_OBJ] -- Check that we can re-use a SQLFunctionCache after a run-time error.

-- This function will fail with zero-divide at run time (not plan time).
CREATE FUNCTION part_hashint4_error(value int4, seed int8) RETURNS int8
LANGUAGE SQL STRICT IMMUTABLE PARALLEL SAFE AS
$$ SELECT value + seed + random()::int/0 $$
;
----------
[CREATE_VIEW(SELECT)] -- ok
/* The next *should* fail, but it's not implemented that way yet. */
CREATE VIEW atestv2 AS SELECT * FROM atest2
;
----------
[CREATE_TABLE] --
-- FLOAT8
--

CREATE TABLE FLOAT8_TBL(f1 float8)
;
----------
[CREATE_TABLE] CREATE TABLE large_tuple_test (a int, b text) WITH (fillfactor = 10)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger parent_insert_trig
  after insert on parent referencing new table as new_table
  for each statement execute procedure dump_insert()
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE main_table DROP COLUMN b
;
----------
[CREATE_TABLE] create table hs4 (col1 integer)
;
