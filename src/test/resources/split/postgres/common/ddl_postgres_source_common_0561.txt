create temp table t1 (f1 numeric(14,0), f2 varchar(30))
;
ALTER TABLE ONLY "public"."prt2_l_p2" ATTACH PARTITION "public"."prt2_l_p2_p1" FOR VALUES IN ('0000', '0001')
;
CREATE TABLE tab4_1 PARTITION OF tab4 FOR VALUES IN (-1, 0, 1) PARTITION BY LIST (a)
;
CREATE MATERIALIZED VIEW mvtest_tm AS SELECT type, sum(amt) AS totamt FROM mvtest_t GROUP BY type WITH NO DATA
;
/*
 * text search configuration for dutch language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * dutch and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
CREATE FUNCTION ltree_gist_in(cstring)
RETURNS ltree_gist
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE truncpart_2_1 PARTITION OF truncpart_2 FOR VALUES FROM (100) TO (150)
;
CREATE RULE "rtest_v1_upd" AS
    ON UPDATE TO "public"."rtest_v1" DO INSTEAD  UPDATE "public"."rtest_t1" SET "a" = "new"."a", "b" = "new"."b"
  WHERE ("rtest_t1"."a" = "old"."a")
;
CREATE UNIQUE INDEX table_dropped_index_with_pk_idx
  ON table_dropped_index_with_pk(a)
;
CREATE FUNCTION "public"."test_ret_rec_dyn"(integer) RETURNS "record"
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
	retval RECORD;
BEGIN
	IF $1 > 10 THEN
		SELECT INTO retval 5, 10, 15;
		RETURN retval;
	ELSE
		SELECT INTO retval 50, 5::numeric, 'xxx'::text;
		RETURN retval;
	END IF;
END;$_$
;
DROP TABLE testpub_rf_tbl5
;
CREATE VIEW pg_stat_recovery_prefetch AS
    SELECT
            s.stats_reset,
            s.prefetch,
            s.hit,
            s.skip_init,
            s.skip_new,
            s.skip_fpw,
            s.skip_rep,
            s.wal_distance,
            s.block_distance,
            s.io_depth
     FROM pg_stat_get_recovery_prefetch() s
;
ALTER TYPE bogus RENAME VALUE 'good' to 'bad'
;
CREATE INDEX testcomment_idx1 ON testcomment (i)
;
CREATE TABLE "public"."itest2" (
    "a" bigint NOT NULL,
    "b" "text"
)
;
DROP FUNCTION dup(anyelement)
;
DROP TABLE test_partition
;
call "test_catalog".test.test_procedure(1,test_function(3))
;
CREATE TRIGGER invalid_trig INSTEAD OF DELETE ON main_table
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_del')
;
drop table if exists temptable cascade
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) information_schema.sql_features
;
create table rtest_nothn4 (a int4, b text)
;
-- Overridden mapping change with duplicated tokens.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ALTER MAPPING FOR word, word WITH ispell
;
ALTER TABLE testpub_tbl8_1 REPLICA IDENTITY FULL
;
alter table p1 add column f2 text
;
CREATE TABLE test_replica_identity4(id integer NOT NULL) PARTITION BY LIST (id)
;
create table idxpart (col1 int, a int, col2 int, b int) partition by range (a)
;
--
-- Check NO INHERIT type of constraints and inheritance
--

CREATE TABLE ATACC1 (TEST INT
	CHECK (TEST > 0) NO INHERIT)
;
CREATE TABLESPACE regress_pgbench_tap_1_ts LOCATION '/src/src/bin/pgbench/tmp_check/t_001_pgbench_with_server_main_data/regress_pgbench_tap_1_ts_dir'
;
CREATE SCHEMA pub_test3
;
create table part_abc_p1 (a int not null, b int not null, c int not null)
;
DROP TYPE jb_unordered_pair
;
-- Ditto for the ATTACH PARTITION case
create table idxpart (a int unique, b int) partition by range (a)
;
ALTER TABLE pg_temp_9.pg_temp_26570_2 ADD COLUMN newdata pg_temp_9.pg_temp_26570
;
CREATE SCHEMA split_pgvector_probe
;
CREATE FUNCTION isbn13(ean13)
RETURNS isbn13
AS '$libdir/isn', 'isbn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_fVgM'
;
ALTER TABLE ONLY "public"."part_rev" DROP COLUMN "........pg.dropped.2........"
;
ALTER INDEX pg_toast.pg_toast_28040_index RENAME TO reind_con_toast_idx
;
create table test ( i int primary key , j int , c text )
;
DROP TABLE J1_TBL
;
CREATE DATABASE db3
;
-- keep these tests aligned with generated_stored.sql


CREATE SCHEMA generated_virtual_tests
;
-- OK
SECURITY LABEL FOR 'unknown_seclabel' ON ROLE regress_dummy_seclabel_user1 IS 'unclassified'
;
DROP TABLE num_exp_power_10_ln
;
create or replace function stacked_diagnostics_test() returns void as $$
declare _detail text;
        _hint text;
        _message text;
begin
  get stacked diagnostics
        _message = message_text,
        _detail = pg_exception_detail,
        _hint = pg_exception_hint;
  raise notice 'message: %, detail: %, hint: %', _message, _detail, _hint;
end;
$$ language plpgsql
;
CREATE SUBSCRIPTION admin_sub CONNECTION 'port=15895 host=/tmp/eTSpS1gcF3 dbname=postgres' PUBLICATION alice
	WITH (run_as_owner = true, password_required = false)
;
create function fcompos3(v compos) returns void as $$
select fcompos1(fcompos3.v.*);
$$ language sql
;
create operator class at_test_sql_partop for type int4 using btree as
    operator 1 < (int4, int4), operator 2 <= (int4, int4),
    operator 3 = (int4, int4), operator 4 >= (int4, int4),
    operator 5 > (int4, int4), function 1 at_test_sql_partop(int4, int4)
;
CREATE FUNCTION "public"."outparam2_succeed"("r" "anyrange", OUT "lu" "anyarray", OUT "ul" "anyarray") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select array[lower($1), upper($1)], array[upper($1), lower($1)] $_$
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create temp table t1 (f1 numeric(14,0), f2 varchar(30))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_l_p2" ATTACH PARTITION "public"."prt2_l_p2_p1" FOR VALUES IN ('0000', '0001')
;
----------
[CREATE_TABLE] CREATE TABLE tab4_1 PARTITION OF tab4 FOR VALUES IN (-1, 0, 1) PARTITION BY LIST (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_tm AS SELECT type, sum(amt) AS totamt FROM mvtest_t GROUP BY type WITH NO DATA
;
----------
[CREATE_POLICY] /*
 * text search configuration for dutch language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * dutch and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY dutch_stem
	(TEMPLATE = snowball, Language = dutch , StopWords=dutch)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_gist_in(cstring)
RETURNS ltree_gist
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE truncpart_2_1 PARTITION OF truncpart_2 FOR VALUES FROM (100) TO (150)
;
----------
[CREATE_POLICY] CREATE RULE "rtest_v1_upd" AS
    ON UPDATE TO "public"."rtest_v1" DO INSTEAD  UPDATE "public"."rtest_t1" SET "a" = "new"."a", "b" = "new"."b"
  WHERE ("rtest_t1"."a" = "old"."a")
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX table_dropped_index_with_pk_idx
  ON table_dropped_index_with_pk(a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_ret_rec_dyn"(integer) RETURNS "record"
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
	retval RECORD;
BEGIN
	IF $1 > 10 THEN
		SELECT INTO retval 5, 10, 15;
		RETURN retval;
	ELSE
		SELECT INTO retval 50, 5::numeric, 'xxx'::text;
		RETURN retval;
	END IF;
END;$_$
;
----------
[DROP_TABLE] DROP TABLE testpub_rf_tbl5
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_recovery_prefetch AS
    SELECT
            s.stats_reset,
            s.prefetch,
            s.hit,
            s.skip_init,
            s.skip_new,
            s.skip_fpw,
            s.skip_rep,
            s.wal_distance,
            s.block_distance,
            s.io_depth
     FROM pg_stat_get_recovery_prefetch() s
;
----------
[ALTER_TYPE] ALTER TYPE bogus RENAME VALUE 'good' to 'bad'
;
----------
[ADD_INDEX] CREATE INDEX testcomment_idx1 ON testcomment (i)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest2" (
    "a" bigint NOT NULL,
    "b" "text"
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION dup(anyelement)
;
----------
[DROP_TABLE] DROP TABLE test_partition
;
----------
[CALL_PROG_OBJ] call "test_catalog".test.test_procedure(1,test_function(3))
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig INSTEAD OF DELETE ON main_table
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_del')
;
----------
[DROP_TABLE] drop table if exists temptable cascade
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) information_schema.sql_features
;
----------
[CREATE_TABLE] create table rtest_nothn4 (a int4, b text)
;
----------
[ALTER_POLICY] -- Overridden mapping change with duplicated tokens.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ALTER MAPPING FOR word, word WITH ispell
;
----------
[ALTER_TABLE] ALTER TABLE testpub_tbl8_1 REPLICA IDENTITY FULL
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table p1 add column f2 text
;
----------
[CREATE_TABLE] CREATE TABLE test_replica_identity4(id integer NOT NULL) PARTITION BY LIST (id)
;
----------
[CREATE_TABLE] create table idxpart (col1 int, a int, col2 int, b int) partition by range (a)
;
----------
[CREATE_TABLE] --
-- Check NO INHERIT type of constraints and inheritance
--

CREATE TABLE ATACC1 (TEST INT
	CHECK (TEST > 0) NO INHERIT)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE regress_pgbench_tap_1_ts LOCATION '/src/src/bin/pgbench/tmp_check/t_001_pgbench_with_server_main_data/regress_pgbench_tap_1_ts_dir'
;
----------
[CREATE_SCHEMA] CREATE SCHEMA pub_test3
;
----------
[CREATE_TABLE] create table part_abc_p1 (a int not null, b int not null, c int not null)
;
----------
[DROP_TYPE] DROP TYPE jb_unordered_pair
;
----------
[CREATE_TABLE] -- Ditto for the ATTACH PARTITION case
create table idxpart (a int unique, b int) partition by range (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_26570_2 ADD COLUMN newdata pg_temp_9.pg_temp_26570
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_pgvector_probe
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isbn13(ean13)
RETURNS isbn13
AS '$libdir/isn', 'isbn_cast_from_ean13'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_fVgM'
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."part_rev" DROP COLUMN "........pg.dropped.2........"
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28040_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table test ( i int primary key , j int , c text )
;
----------
[DROP_TABLE] DROP TABLE J1_TBL
;
----------
[CREATE_CATALOG] CREATE DATABASE db3
;
----------
[CREATE_SCHEMA] -- keep these tests aligned with generated_stored.sql


CREATE SCHEMA generated_virtual_tests
;
----------
[SECURITY_LABEL] -- OK
SECURITY LABEL FOR 'unknown_seclabel' ON ROLE regress_dummy_seclabel_user1 IS 'unclassified'
;
----------
[DROP_TABLE] DROP TABLE num_exp_power_10_ln
;
----------
[CREATE_PROG_OBJ] create or replace function stacked_diagnostics_test() returns void as $$
declare _detail text;
        _hint text;
        _message text;
begin
  get stacked diagnostics
        _message = message_text,
        _detail = pg_exception_detail,
        _hint = pg_exception_hint;
  raise notice 'message: %, detail: %, hint: %', _message, _detail, _hint;
end;
$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION admin_sub CONNECTION 'port=15895 host=/tmp/eTSpS1gcF3 dbname=postgres' PUBLICATION alice
	WITH (run_as_owner = true, password_required = false)
;
----------
[CREATE_PROG_OBJ] create function fcompos3(v compos) returns void as $$
select fcompos1(fcompos3.v.*);
$$ language sql
;
----------
[CREATE_PROG_OBJ] create operator class at_test_sql_partop for type int4 using btree as
    operator 1 < (int4, int4), operator 2 <= (int4, int4),
    operator 3 = (int4, int4), operator 4 >= (int4, int4),
    operator 5 > (int4, int4), function 1 at_test_sql_partop(int4, int4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."outparam2_succeed"("r" "anyrange", OUT "lu" "anyarray", OUT "ul" "anyarray") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select array[lower($1), upper($1)], array[upper($1), lower($1)] $_$
;
