INSERT INTO tab_full2 VALUES ('a'), ('b'), ('b')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."fk_partitioned_fk_3"'::pg_catalog.regclass
;
INSERT INTO DATE_TBL VALUES ('1997-02-29')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14717', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p2_p2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9163', relminmxid = '1'
WHERE oid = '"public"."clstr_tst_inh"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('9', 299)
;
insert into WSlot values ('WS.101.2a', '101', '', '')
;
insert into tt(data) values($1) returning f1
;
INSERT INTO public.ths_p1 VALUES (6, 56)
;
INSERT INTO rw_view16 VALUES (3, 'Row 3', 3)
;
-- ok
DELETE FROM atestc
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11395', relminmxid = '1'
WHERE oid = '"public"."parallel_vacuum_table"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12730', relminmxid = '3'
WHERE oid = '"public"."nv_child_2011"'::pg_catalog.regclass
;
insert into rem1 values(1,'insert')
;
INSERT INTO ruletest1 VALUES (5)
;
-- under-estimates when using only per-column statistics
INSERT INTO ndistinct (a, b, c, filler1)
     SELECT mod(i,50), mod(i,51), mod(i,32),
            cash_words(mod(i,33)::int::money)
       FROM generate_series(1,5000) s(i)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."plt1_e_p2"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1533', relminmxid = '1'
WHERE oid = '17392'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."mvtest_tvvm"'::pg_catalog.regclass
;
INSERT INTO public.ths_p3 VALUES (0, 330)
;
INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (8, 1, 9113, 3123, CURRENT_TIMESTAMP)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13292', relminmxid = '3'
WHERE oid = '"public"."xmltest"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."pht1_e_p2"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."mlparted12"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1578', relminmxid = '1'
WHERE oid = '"public"."a_star"'::pg_catalog.regclass
;
UPDATE part_c_100_200 set c = c - 20, d = c WHERE c = 105
;
INSERT INTO public.ths_p1 VALUES (4, 174)
;
INSERT INTO gtest24 (a) VALUES (6)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18600', relminmxid = '3'
WHERE oid = '43320'
;
insert into copydml_test (t) values ('e')
;
-- Delete failing row
DELETE FROM attmp3 where a=5
;
INSERT INTO tr_sub(path) VALUES ('5-top-1-#1')
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt4_n_p3"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '730', relminmxid = '1'
WHERE oid = 2613
;
-- fail (partition key (b+0) is null)
insert into range_parted values ('a')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '25939'
;
INSERT INTO connectby_int VALUES(10,9)
;
INSERT INTO TIMESTAMPTZ_TBL VALUES ('Mon Feb 10 17:32:01.000001 1997 PST')
;
delete from testtab where ctid > '(8,0)'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11240', relminmxid = '1'
WHERE oid = '"public"."mcv_lists"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18786', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo2"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15726', relminmxid = '3'
WHERE oid = '"public"."query"'::pg_catalog.regclass
;
-- point_tbl was already created and filled in test_setup.sql.
-- Here we just try to insert bad values.

INSERT INTO POINT_TBL(f1) VALUES ('asdfasdf')
;
INSERT INTO arraggtest (f1, f2, f3) VALUES
('{2}','{{black,red},{green,orange}}','{1.6,2.2,2.6,0.4}')
;
INSERT INTO num_exp_log10 VALUES (4,'6.89206461372691743345')
;
INSERT INTO macaddr8_data VALUES (19, '0800.2b01.0203.0405')
;
INSERT INTO public.tplain VALUES ('6', 246)
;
INSERT INTO rw_view VALUES (0, 15), (0, 5)
;
INSERT INTO trans_abc VALUES (5)
;
INSERT INTO tbl SELECT x, 2*x, NULL, NULL FROM generate_series(1,10) AS x
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO tab_full2 VALUES ('a'), ('b'), ('b')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."fk_partitioned_fk_3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO DATE_TBL VALUES ('1997-02-29')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14717', relminmxid = '3'
WHERE oid = '"public"."prt1_l_p2_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9163', relminmxid = '1'
WHERE oid = '"public"."clstr_tst_inh"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 299)
;
----------
[INSERT] insert into WSlot values ('WS.101.2a', '101', '', '')
;
----------
[INSERT|SELECT] insert into tt(data) values($1) returning f1
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (6, 56)
;
----------
[INSERT] INSERT INTO rw_view16 VALUES (3, 'Row 3', 3)
;
----------
[DELETE] -- ok
DELETE FROM atestc
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11395', relminmxid = '1'
WHERE oid = '"public"."parallel_vacuum_table"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12730', relminmxid = '3'
WHERE oid = '"public"."nv_child_2011"'::pg_catalog.regclass
;
----------
[INSERT] insert into rem1 values(1,'insert')
;
----------
[INSERT] INSERT INTO ruletest1 VALUES (5)
;
----------
[INSERT|SELECT] -- under-estimates when using only per-column statistics
INSERT INTO ndistinct (a, b, c, filler1)
     SELECT mod(i,50), mod(i,51), mod(i,32),
            cash_words(mod(i,33)::int::money)
       FROM generate_series(1,5000) s(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."plt1_e_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1533', relminmxid = '1'
WHERE oid = '17392'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."mvtest_tvvm"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (0, 330)
;
----------
[INSERT|SELECT] INSERT INTO pgbench_history (tid, bid, aid, delta, mtime) VALUES (8, 1, 9113, 3123, CURRENT_TIMESTAMP)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13292', relminmxid = '3'
WHERE oid = '"public"."xmltest"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."pht1_e_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'c'
  AND attrelid = '"public"."mlparted12"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1578', relminmxid = '1'
WHERE oid = '"public"."a_star"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE part_c_100_200 set c = c - 20, d = c WHERE c = 105
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 174)
;
----------
[INSERT|SELECT] INSERT INTO gtest24 (a) VALUES (6)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '18600', relminmxid = '3'
WHERE oid = '43320'
;
----------
[INSERT|SELECT] insert into copydml_test (t) values ('e')
;
----------
[DELETE] -- Delete failing row
DELETE FROM attmp3 where a=5
;
----------
[INSERT|SELECT] INSERT INTO tr_sub(path) VALUES ('5-top-1-#1')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."prt4_n_p3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '730', relminmxid = '1'
WHERE oid = 2613
;
----------
[INSERT] -- fail (partition key (b+0) is null)
insert into range_parted values ('a')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '25939'
;
----------
[INSERT] INSERT INTO connectby_int VALUES(10,9)
;
----------
[INSERT] INSERT INTO TIMESTAMPTZ_TBL VALUES ('Mon Feb 10 17:32:01.000001 1997 PST')
;
----------
[DELETE] delete from testtab where ctid > '(8,0)'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11240', relminmxid = '1'
WHERE oid = '"public"."mcv_lists"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18786', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '15726', relminmxid = '3'
WHERE oid = '"public"."query"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- point_tbl was already created and filled in test_setup.sql.
-- Here we just try to insert bad values.

INSERT INTO POINT_TBL(f1) VALUES ('asdfasdf')
;
----------
[INSERT|SELECT] INSERT INTO arraggtest (f1, f2, f3) VALUES
('{2}','{{black,red},{green,orange}}','{1.6,2.2,2.6,0.4}')
;
----------
[INSERT] INSERT INTO num_exp_log10 VALUES (4,'6.89206461372691743345')
;
----------
[INSERT] INSERT INTO macaddr8_data VALUES (19, '0800.2b01.0203.0405')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 246)
;
----------
[INSERT] INSERT INTO rw_view VALUES (0, 15), (0, 5)
;
----------
[INSERT] INSERT INTO trans_abc VALUES (5)
;
----------
[INSERT|SELECT] INSERT INTO tbl SELECT x, 2*x, NULL, NULL FROM generate_series(1,10) AS x
;
