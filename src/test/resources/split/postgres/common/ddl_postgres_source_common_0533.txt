CREATE TRIGGER trig_stmt_after AFTER DELETE OR INSERT OR UPDATE OR TRUNCATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
CREATE TABLE "generated_stored_tests"."gtest1_1" (
    "a" integer CONSTRAINT "gtest1_a_not_null" NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
--
-- Test multiple range types over the same subtype
--

create type textrange1 as range(subtype=text, collation="C")
;
CREATE TABLE timetmp (a time)
;
ALTER INDEX pg_toast.pg_toast_33644_index RENAME TO reind_con_toast_idx
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
-- Nothing is cleaned up.
VACUUM no_index_cleanup
;
CREATE FUNCTION voidtest3(a int) RETURNS VOID LANGUAGE SQL AS
$$ INSERT INTO sometable VALUES(a + 1) $$
;
CREATE TABLE regtest_ptable_3_ones PARTITION OF regtest_ptable_3 FOR VALUES FROM ('0') to ('10')
;
create policy p2 on rls_t for select to regress_rls_bob using (false)
;
create rule r1 as on delete to t1 do delete from t2 where t2.b = old.a
;
--
-- Check we behave sanely in corner case of empty SELECT list (bug #8648)
--
create temp table nocolumns()
;
DO $pl$
DECLARE
	detail text;
BEGIN
	PERFORM wait_pid(crash_pid)
	FROM dblink(connection_parameters(), $$
		SELECT pg_backend_pid() FROM dblink(
			'service=test_ldap '||connection_parameters(),
			-- This string concatenation is a hack to shoehorn a
			-- set_pgservicefile call into the SQL statement.
			'SELECT 1' || set_pgservicefile('pg_service.conf')
		) t(c int)
	$$) AS t(crash_pid int);
EXCEPTION WHEN OTHERS THEN
	GET STACKED DIAGNOSTICS detail = PG_EXCEPTION_DETAIL;
	-- Expected error in a non-LDAP build.
	IF NOT detail LIKE 'syntax error in service file%' THEN RAISE; END IF;
END
$pl$
;
analyze remt1
;
CREATE TRIGGER y_trig AFTER INSERT ON y FOR EACH ROW
    EXECUTE PROCEDURE y_trigger()
;
-- silly example to verify that hasModifyingCTE flag is propagated
CREATE TEMP TABLE bug6051_3 AS
  SELECT a FROM generate_series(11,13) AS a
;
alter type two_ints add attribute c two_ints_range
;
ALTER INDEX pg_toast.pg_toast_30627_index RENAME TO reind_con_toast_idx
;
CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_4W6X'
;
CREATE TABLE tr_sub_ddl(data int)
;
ANALYZE public.part2_self_fk
;
ALTER TABLE pg_toast.pg_toast_27655 RENAME TO reind_con_toast
;
drop trigger rem2_trig_row_after on rem2
;
CREATE VIEW rec2v AS SELECT * FROM rec2
;
ALTER TABLE ONLY "public"."mlparted_def" DROP COLUMN "........pg.dropped.5........"
;
-- fail
CREATE TRANSFORM FOR hstore LANGUAGE foo (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
CREATE FUNCTION "public"."check_primary_key"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/refint.so', 'check_primary_key'
;
CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
COMMENT ON TABLE parted_col_comment IS 'Am partitioned table'
;
CREATE SEQUENCE s5.seq2
;
create temp table undername (f1 name, f2 int)
;
CREATE TABLE plt1_p3 PARTITION OF plt1 FOR VALUES IN ('0006', '0007', '0008', '0011')
;
CREATE OPERATOR === (
    leftarg = integer,
    rightarg = integer,
    procedure = int4eq,
    commutator = =
)
;
DROP SEQUENCE IF EXISTS temp_seq_probe
;
ALTER TABLE spc_abort SET TABLESPACE other
;
-- Parallel append

-- Parallel queries won't necessarily get as many workers as the planner
-- asked for.  This affects not only the "Workers Launched:" field of EXPLAIN
-- results, but also row counts and loop counts for parallel scans, Gathers,
-- and everything in between.  This function filters out the values we can't
-- rely on to be stable.
-- This removes enough info that you might wonder why bother with EXPLAIN
-- ANALYZE at all.  The answer is that we need to see '(never executed)'
-- notations because that's the only way to verify runtime pruning.
create function explain_parallel_append(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off, buffers off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+(?:\.\d+)? loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        perform regexp_matches(ln, 'Index Searches: \d+');
        if found then
          continue;
        end if;
        return next ln;
    end loop;
end;
$$
;
drop table gen_part
;
CREATE FUNCTION "public"."tg_room_au"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.roomno != old.roomno then
        update WSlot set roomno = new.roomno where roomno = old.roomno;
    end if;
    return new;
end;
$$
;
CREATE TABLE public.test_mismatching_types (
        a int PRIMARY KEY
    )
;
-- Should work. Textbook case of ALTER OPERATOR FAMILY ... ADD OPERATOR with FOR ORDER BY
CREATE OPERATOR FAMILY alt_opf11 USING gist
;
VACUUM FREEZE tab_freeze
;
ALTER OPERATOR === (boolean, real) SET (COMMUTATOR = @=)
;
alter table users drop column moredrop
;
CREATE TABLE shoe_data (
	shoename   char(10),      
	sh_avail   integer,       
	slcolor    char(10),      
	slminlen   float,         
	slmaxlen   float,         
	slunit     char(8)        
)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opclass
;
CREATE RULE rule_2 AS
  ON UPDATE
  TO datatype_table
  DO INSERT INTO unlogged_table (id) VALUES(NEW.id)
;
CREATE TABLE "public"."fk_partitioned_fk_2_1" (
    "a" integer,
    "b" integer
)
;
CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	OPERATOR 13	~,
	OPERATOR 14	@,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
CREATE FUNCTION unwanted_grant_nofail(int) RETURNS int
	IMMUTABLE LANGUAGE plpgsql AS $$
BEGIN
	PERFORM public.unwanted_grant();
	RAISE WARNING 'owned';
	RETURN 1;
EXCEPTION WHEN OTHERS THEN
	RETURN 2;
END$$
;
CREATE OR REPLACE FUNCTION explain_json(p_query text)
    RETURNS json
    LANGUAGE plpgsql AS $$
        DECLARE
            v_ret json;
        BEGIN
            EXECUTE p_query INTO STRICT v_ret;
            RETURN v_ret;
        END;$$
;
------------------------------------------------------------------------------------------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_stmt_after AFTER DELETE OR INSERT OR UPDATE OR TRUNCATE ON rem1
	FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func()
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest1_1" (
    "a" integer CONSTRAINT "gtest1_a_not_null" NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_POLICY] --
-- Test multiple range types over the same subtype
--

create type textrange1 as range(subtype=text, collation="C")
;
----------
[CREATE_TABLE] CREATE TABLE timetmp (a time)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33644_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
----------
[ADMIN_TABLE] -- Nothing is cleaned up.
VACUUM no_index_cleanup
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION voidtest3(a int) RETURNS VOID LANGUAGE SQL AS
$$ INSERT INTO sometable VALUES(a + 1) $$
;
----------
[CREATE_TABLE] CREATE TABLE regtest_ptable_3_ones PARTITION OF regtest_ptable_3 FOR VALUES FROM ('0') to ('10')
;
----------
[CREATE_POLICY] create policy p2 on rls_t for select to regress_rls_bob using (false)
;
----------
[CREATE_POLICY] create rule r1 as on delete to t1 do delete from t2 where t2.b = old.a
;
----------
[CREATE_TABLE] --
-- Check we behave sanely in corner case of empty SELECT list (bug #8648)
--
create temp table nocolumns()
;
----------
[BLOCK] DO $pl$
DECLARE
	detail text;
BEGIN
	PERFORM wait_pid(crash_pid)
	FROM dblink(connection_parameters(), $$
		SELECT pg_backend_pid() FROM dblink(
			'service=test_ldap '||connection_parameters(),
			-- This string concatenation is a hack to shoehorn a
			-- set_pgservicefile call into the SQL statement.
			'SELECT 1' || set_pgservicefile('pg_service.conf')
		) t(c int)
	$$) AS t(crash_pid int);
EXCEPTION WHEN OTHERS THEN
	GET STACKED DIAGNOSTICS detail = PG_EXCEPTION_DETAIL;
	-- Expected error in a non-LDAP build.
	IF NOT detail LIKE 'syntax error in service file%' THEN RAISE; END IF;
END
$pl$
;
----------
[ADMIN_TABLE] analyze remt1
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER y_trig AFTER INSERT ON y FOR EACH ROW
    EXECUTE PROCEDURE y_trigger()
;
----------
[CREATE_TABLE|SELECT] -- silly example to verify that hasModifyingCTE flag is propagated
CREATE TEMP TABLE bug6051_3 AS
  SELECT a FROM generate_series(11,13) AS a
;
----------
[ALTER_TYPE] alter type two_ints add attribute c two_ints_range
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30627_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_4W6X'
;
----------
[CREATE_TABLE] CREATE TABLE tr_sub_ddl(data int)
;
----------
[ADMIN_TABLE] ANALYZE public.part2_self_fk
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27655 RENAME TO reind_con_toast
;
----------
[DROP_TRIGGER] drop trigger rem2_trig_row_after on rem2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rec2v AS SELECT * FROM rec2
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted_def" DROP COLUMN "........pg.dropped.5........"
;
----------
[UNKNOWN] -- fail
CREATE TRANSFORM FOR hstore LANGUAGE foo (FROM SQL WITH FUNCTION hstore_to_plperl(internal), TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."check_primary_key"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/refint.so', 'check_primary_key'
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY russian_stem
	(TEMPLATE = snowball, Language = russian , StopWords=russian)
;
----------
[COMMENT_TABLE] COMMENT ON TABLE parted_col_comment IS 'Am partitioned table'
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE s5.seq2
;
----------
[CREATE_TABLE] create temp table undername (f1 name, f2 int)
;
----------
[CREATE_TABLE] CREATE TABLE plt1_p3 PARTITION OF plt1 FOR VALUES IN ('0006', '0007', '0008', '0011')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR === (
    leftarg = integer,
    rightarg = integer,
    procedure = int4eq,
    commutator = =
)
;
----------
[DROP_SEQUENCE] DROP SEQUENCE IF EXISTS temp_seq_probe
;
----------
[ALTER_TABLE] ALTER TABLE spc_abort SET TABLESPACE other
;
----------
[CREATE_PROG_OBJ] -- Parallel append

-- Parallel queries won't necessarily get as many workers as the planner
-- asked for.  This affects not only the "Workers Launched:" field of EXPLAIN
-- results, but also row counts and loop counts for parallel scans, Gathers,
-- and everything in between.  This function filters out the values we can't
-- rely on to be stable.
-- This removes enough info that you might wonder why bother with EXPLAIN
-- ANALYZE at all.  The answer is that we need to see '(never executed)'
-- notations because that's the only way to verify runtime pruning.
create function explain_parallel_append(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in
        execute format('explain (analyze, costs off, summary off, timing off, buffers off) %s',
            $1)
    loop
        ln := regexp_replace(ln, 'Workers Launched: \d+', 'Workers Launched: N');
        ln := regexp_replace(ln, 'actual rows=\d+(?:\.\d+)? loops=\d+', 'actual rows=N loops=N');
        ln := regexp_replace(ln, 'Rows Removed by Filter: \d+', 'Rows Removed by Filter: N');
        perform regexp_matches(ln, 'Index Searches: \d+');
        if found then
          continue;
        end if;
        return next ln;
    end loop;
end;
$$
;
----------
[DROP_TABLE] drop table gen_part
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_room_au"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.roomno != old.roomno then
        update WSlot set roomno = new.roomno where roomno = old.roomno;
    end if;
    return new;
end;
$$
;
----------
[CREATE_TABLE] CREATE TABLE public.test_mismatching_types (
        a int PRIMARY KEY
    )
;
----------
[CREATE_PROG_OBJ] -- Should work. Textbook case of ALTER OPERATOR FAMILY ... ADD OPERATOR with FOR ORDER BY
CREATE OPERATOR FAMILY alt_opf11 USING gist
;
----------
[ADMIN_TABLE] VACUUM FREEZE tab_freeze
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, real) SET (COMMUTATOR = @=)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table users drop column moredrop
;
----------
[CREATE_TABLE] CREATE TABLE shoe_data (
	shoename   char(10),      
	sh_avail   integer,       
	slcolor    char(10),      
	slminlen   float,         
	slmaxlen   float,         
	slunit     char(8)        
)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opclass
;
----------
[CREATE_POLICY] CREATE RULE rule_2 AS
  ON UPDATE
  TO datatype_table
  DO INSERT INTO unlogged_table (id) VALUES(NEW.id)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_2_1" (
    "a" integer,
    "b" integer
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS box_ops DEFAULT
	FOR TYPE box USING gist2 AS
	OPERATOR 1	<<,
	OPERATOR 2	&<,
	OPERATOR 3	&&,
	OPERATOR 4	&>,
	OPERATOR 5	>>,
	OPERATOR 6	~=,
	OPERATOR 7	@>,
	OPERATOR 8	<@,
	OPERATOR 9	&<|,
	OPERATOR 10	<<|,
	OPERATOR 11	|>>,
	OPERATOR 12	|&>,
	OPERATOR 13	~,
	OPERATOR 14	@,
	FUNCTION 1	gist_box_consistent(internal, box, smallint, oid, internal),
	FUNCTION 2	gist_box_union(internal, internal),
	
	FUNCTION 5	gist_box_penalty(internal, internal, internal),
	FUNCTION 6	gist_box_picksplit(internal, internal),
	FUNCTION 7	gist_box_same(box, box, internal)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION unwanted_grant_nofail(int) RETURNS int
	IMMUTABLE LANGUAGE plpgsql AS $$
BEGIN
	PERFORM public.unwanted_grant();
	RAISE WARNING 'owned';
	RETURN 1;
EXCEPTION WHEN OTHERS THEN
	RETURN 2;
END$$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION explain_json(p_query text)
    RETURNS json
    LANGUAGE plpgsql AS $$
        DECLARE
            v_ret json;
        BEGIN
            EXECUTE p_query INTO STRICT v_ret;
            RETURN v_ret;
        END;$$
;
