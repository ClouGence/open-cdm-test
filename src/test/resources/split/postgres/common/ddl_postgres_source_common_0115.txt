CREATE FUNCTION get_command_tag(pg_ddl_command)
  RETURNS text IMMUTABLE STRICT
  AS 'MODULE_PATHNAME' LANGUAGE C
;
drop table insertconflicttest
;
--
-- CREATE_VIEW
--

CREATE VIEW static_view AS
  SELECT 'foo'::TEXT AS col
;
DROP TABLE prt2_adv_p3_1
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest_parent" ATTACH PARTITION "generated_virtual_tests"."gtest_child3" FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
CREATE TABLE tab1 (a int PRIMARY KEY, "B" int, c int)
;
DROP TABLE pg_temp_32.pg_temp_28302_2, pg_temp_32.pg_temp_28302
;
ALTER TABLE gtestxx_3 INHERIT gtest1
;
CREATE TABLE aggtest (
	a 			int2,
	b			float4
)
;
drop table bmscantest
;
DROP SCHEMA IF EXISTS my_schema_2 CASCADE
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_rel
;
CREATE VIEW ro_view3 AS SELECT 1 FROM base_tbl HAVING max(a) > 0
;
-- can't be pushed down
DROP TRIGGER trig_row_after_delete ON rem1
;
-- bad

create temp table quadtable(f1 int, q quad)
;
DROP DOMAIN domain2
;
-- unsupported relation types
-- view
CREATE VIEW tv AS SELECT * FROM target
;
CREATE FUNCTION explain_merge(query text) RETURNS SETOF text
LANGUAGE plpgsql AS
$$
DECLARE ln text;
BEGIN
    FOR ln IN
        EXECUTE 'explain (analyze, timing off, summary off, costs off) ' ||
		  query
    LOOP
        ln := regexp_replace(ln, '(Memory( Usage)?|Buckets|Batches): \S*',  '\1: xxx', 'g');
        RETURN NEXT ln;
    END LOOP;
END;
$$
;
CREATE FUNCTION merge_into_sq_target(sid int, balance int, delta int,
                                     OUT action text, OUT tid int, OUT new_balance int)
LANGUAGE sql AS
$$
    MERGE INTO sq_target t
    USING (VALUES ($1, $2, $3)) AS v(sid, balance, delta)
    ON tid = v.sid
    WHEN MATCHED AND tid >= 2 THEN
        UPDATE SET balance = t.balance + v.delta
    WHEN NOT MATCHED THEN
        INSERT (balance, tid) VALUES (v.balance + v.delta, v.sid)
    WHEN MATCHED AND tid < 2 THEN
        DELETE
    RETURNING merge_action(), t.*;
$$
;
ALTER INDEX pg_toast.pg_toast_27845_index RENAME TO reind_con_toast_idx
;
-- Unnamed FULL JOIN USING is lots of fun too

create table tt7 (x int, xx int, y int)
;
CREATE VIEW v1 WITH (security_barrier=true) AS
SELECT *, (SELECT d FROM t11 WHERE t11.a = t1.a LIMIT 1) AS d
FROM t1
WHERE a > 5 AND EXISTS(SELECT 1 FROM t12 WHERE t12.a = t1.a)
;
CREATE TRIGGER rep_b_d BEFORE DELETE ON trigtest FOR EACH ROW EXECUTE PROCEDURE trig_report()
;
CREATE TABLE plt1_e (a int, b int, c text) PARTITION BY LIST(ltrim(c, 'A'))
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_replication_origin
;
-- clean up (regress_priv_user1's objects are all dropped later)
DROP FUNCTION leak2(integer, integer) CASCADE
;
-- cleanup
DROP TABLE parted, list_parted, range_parted, list_parted2, range_parted2, range_parted3
;
DROP TABLE seeded_random
;
DROP TABLE seclabel_tbl2
;
CREATE TABLE ctas_nodata (ii, jj) AS SELECT i, j FROM ctas_base
;
CREATE FUNCTION gbt_macad8_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
IMPORT FOREIGN SCHEMA split_import_src EXCEPT (import_source) FROM SERVER split_pg_import INTO split_import_dst
;
-- test jsonb null -> python None
CREATE FUNCTION test1null(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == None)
return 1
$$
;
CREATE OR REPLACE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a < 10
  WITH LOCAL CHECK OPTION
;
-- test jsonb[] -> python list
-- simple list
CREATE FUNCTION test2arr(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, list)
assert(val == [12, 1])
return len(val)
$$
;
DROP TABLE stxdinp
;
ANALYZE pg_catalog.pg_am(amname)
;
VACUUM (FULL TRUE) no_index_cleanup
;
ALTER TABLE ONLY "public"."alter_table_under_transition_tables"
    ADD CONSTRAINT "alter_table_under_transition_tables_pkey" PRIMARY KEY ("id")
;
CREATE TABLE "public"."notnull_tbl4_lk" (
    "a" integer CONSTRAINT "notnull_tbl4_a_not_null" NOT NULL
)
;
ALTER TABLE ONLY "public"."prt4_n" ATTACH PARTITION "public"."prt4_n_p2" FOR VALUES FROM (300) TO (500)
;
-- test pg_mcv_list_items with a very simple (single item) MCV list
TRUNCATE mcv_lists
;
CREATE TABLE pht1_e_p3 PARTITION OF pht1_e FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
CREATE VIEW pg_stat_statements_info AS
  SELECT * FROM pg_stat_statements_info()
;
CREATE TABLE "public"."gs_data_1" (
    "g1000" integer,
    "g100" integer,
    "g10" integer,
    "g" integer
)
WITH ("autovacuum_enabled"='false')
;
ALTER FUNCTION plpgsql_function_trigger_1 ()
   SET SCHEMA foo
;
CREATE SEQUENCE ext_cine_seq
;
create table fktable(ftest1 int, ftest2 inet, foreign key(ftest2, ftest1) references pktable(base1, ptest1))
;
ALTER FOREIGN TABLE tru_ftable OPTIONS (SET truncatable 'false')
;
drop table if exists T
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION get_command_tag(pg_ddl_command)
  RETURNS text IMMUTABLE STRICT
  AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[DROP_TABLE] drop table insertconflicttest
;
----------
[CREATE_VIEW(SELECT)] --
-- CREATE_VIEW
--

CREATE VIEW static_view AS
  SELECT 'foo'::TEXT AS col
;
----------
[DROP_TABLE] DROP TABLE prt2_adv_p3_1
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "generated_virtual_tests"."gtest_parent" ATTACH PARTITION "generated_virtual_tests"."gtest_child3" FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (a int PRIMARY KEY, "B" int, c int)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_32.pg_temp_28302_2, pg_temp_32.pg_temp_28302
;
----------
[ALTER_TABLE] ALTER TABLE gtestxx_3 INHERIT gtest1
;
----------
[CREATE_TABLE] CREATE TABLE aggtest (
	a 			int2,
	b			float4
)
;
----------
[DROP_TABLE] drop table bmscantest
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS my_schema_2 CASCADE
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_rel
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view3 AS SELECT 1 FROM base_tbl HAVING max(a) > 0
;
----------
[DROP_TRIGGER] -- can't be pushed down
DROP TRIGGER trig_row_after_delete ON rem1
;
----------
[CREATE_TABLE] -- bad

create temp table quadtable(f1 int, q quad)
;
----------
[DROP_TYPE] DROP DOMAIN domain2
;
----------
[CREATE_VIEW(SELECT)] -- unsupported relation types
-- view
CREATE VIEW tv AS SELECT * FROM target
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION explain_merge(query text) RETURNS SETOF text
LANGUAGE plpgsql AS
$$
DECLARE ln text;
BEGIN
    FOR ln IN
        EXECUTE 'explain (analyze, timing off, summary off, costs off) ' ||
		  query
    LOOP
        ln := regexp_replace(ln, '(Memory( Usage)?|Buckets|Batches): \S*',  '\1: xxx', 'g');
        RETURN NEXT ln;
    END LOOP;
END;
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION merge_into_sq_target(sid int, balance int, delta int,
                                     OUT action text, OUT tid int, OUT new_balance int)
LANGUAGE sql AS
$$
    MERGE INTO sq_target t
    USING (VALUES ($1, $2, $3)) AS v(sid, balance, delta)
    ON tid = v.sid
    WHEN MATCHED AND tid >= 2 THEN
        UPDATE SET balance = t.balance + v.delta
    WHEN NOT MATCHED THEN
        INSERT (balance, tid) VALUES (v.balance + v.delta, v.sid)
    WHEN MATCHED AND tid < 2 THEN
        DELETE
    RETURNING merge_action(), t.*;
$$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27845_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] -- Unnamed FULL JOIN USING is lots of fun too

create table tt7 (x int, xx int, y int)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v1 WITH (security_barrier=true) AS
SELECT *, (SELECT d FROM t11 WHERE t11.a = t1.a LIMIT 1) AS d
FROM t1
WHERE a > 5 AND EXISTS(SELECT 1 FROM t12 WHERE t12.a = t1.a)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER rep_b_d BEFORE DELETE ON trigtest FOR EACH ROW EXECUTE PROCEDURE trig_report()
;
----------
[CREATE_TABLE] CREATE TABLE plt1_e (a int, b int, c text) PARTITION BY LIST(ltrim(c, 'A'))
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_replication_origin
;
----------
[DROP_PROG_OBJ] -- clean up (regress_priv_user1's objects are all dropped later)
DROP FUNCTION leak2(integer, integer) CASCADE
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE parted, list_parted, range_parted, list_parted2, range_parted2, range_parted3
;
----------
[DROP_TABLE] DROP TABLE seeded_random
;
----------
[DROP_TABLE] DROP TABLE seclabel_tbl2
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE ctas_nodata (ii, jj) AS SELECT i, j FROM ctas_base
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad8_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA split_import_src EXCEPT (import_source) FROM SERVER split_pg_import INTO split_import_dst
;
----------
[CREATE_PROG_OBJ] -- test jsonb null -> python None
CREATE FUNCTION test1null(val jsonb) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == None)
return 1
$$
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW rw_view2 AS SELECT * FROM rw_view1 WHERE a < 10
  WITH LOCAL CHECK OPTION
;
----------
[CREATE_PROG_OBJ] -- test jsonb[] -> python list
-- simple list
CREATE FUNCTION test2arr(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert isinstance(val, list)
assert(val == [12, 1])
return len(val)
$$
;
----------
[DROP_TABLE] DROP TABLE stxdinp
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_am(amname)
;
----------
[ADMIN_TABLE] VACUUM (FULL TRUE) no_index_cleanup
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."alter_table_under_transition_tables"
    ADD CONSTRAINT "alter_table_under_transition_tables_pkey" PRIMARY KEY ("id")
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl4_lk" (
    "a" integer CONSTRAINT "notnull_tbl4_a_not_null" NOT NULL
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt4_n" ATTACH PARTITION "public"."prt4_n_p2" FOR VALUES FROM (300) TO (500)
;
----------
[TRUNCATE_TABLE] -- test pg_mcv_list_items with a very simple (single item) MCV list
TRUNCATE mcv_lists
;
----------
[CREATE_TABLE] CREATE TABLE pht1_e_p3 PARTITION OF pht1_e FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_statements_info AS
  SELECT * FROM pg_stat_statements_info()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gs_data_1" (
    "g1000" integer,
    "g100" integer,
    "g10" integer,
    "g" integer
)
WITH ("autovacuum_enabled"='false')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION plpgsql_function_trigger_1 ()
   SET SCHEMA foo
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE ext_cine_seq
;
----------
[CREATE_TABLE] create table fktable(ftest1 int, ftest2 inet, foreign key(ftest2, ftest1) references pktable(base1, ptest1))
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE tru_ftable OPTIONS (SET truncatable 'false')
;
----------
[DROP_TABLE] drop table if exists T
;
