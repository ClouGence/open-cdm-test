CREATE TABLE default_expr_non_column (a int DEFAULT (avg(non_existent)))
;
CREATE OR REPLACE FUNCTION chkrolattr()
 RETURNS TABLE ("role" name, rolekeyword text, canlogin bool, replication bool)
 AS $$
SELECT r.rolname, v.keyword, r.rolcanlogin, r.rolreplication
 FROM pg_roles r
 JOIN (VALUES(CURRENT_USER, 'current_user'),
             (SESSION_USER, 'session_user'),
             ('current_user', '-'),
             ('session_user', '-'),
             ('Public', '-'),
             ('None', '-'))
      AS v(uname, keyword)
      ON (r.rolname = v.uname)
 ORDER BY 1;
$$ LANGUAGE SQL
;
ALTER TABLE rls_test_permissive ENABLE ROW LEVEL SECURITY
;
-- test jsonb string -> python string
CREATE FUNCTION test1string(val jsonb) RETURNS text
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == "a")
return val
$$
;
create table perform_test (
	a	INT,
	b	INT
)
;
CREATE FUNCTION seg_union(seg, seg)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = upc,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
VACUUM  verbose pg_class
;
VACUUM (FULL) pg_catalog.pg_pltemplate
;
-- named and default parameters

CREATE OR REPLACE PROCEDURE ptest5(a int, b text, c int default 100)
LANGUAGE SQL
AS $$
INSERT INTO cp_test VALUES(a, b);
INSERT INTO cp_test VALUES(c, b);
$$
;
CREATE INDEX abbrev_abort_uuids__abort_decreasing_idx ON abbrev_abort_uuids (abort_decreasing)
;
ANALYZE public.plt2_p1
;
CREATE SCHEMA "schema3" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
-- This does not work when the table isn't empty.  That's intentional,
-- since ADD GENERATED should only affect later insertions:
ALTER TABLE itest8
  ADD COLUMN f22 int NOT NULL,
  ALTER COLUMN f22 ADD GENERATED ALWAYS AS IDENTITY
;
CREATE TEMP TABLE temptest3(col int PRIMARY KEY) ON COMMIT DELETE ROWS
;
create table inh_nn3 (f4 float) inherits (inh_nn2)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_class
;
create temp table parttbl1 partition of parttbl for values from (1) to (100)
;
ANALYZE public.prt1_m_p1
;
create table t11()
;
CREATE INDEX CONCURRENTLY ginidx ON tbl USING gin(j)
;
/*
 * 24.13
 * USER_MAPPINGS view
 */
CREATE VIEW user_mappings AS
    SELECT authorization_identifier,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_user_mappings
;
CREATE FOREIGN TABLE ftable (a text, b int)
	SERVER loopback
	OPTIONS (table_name 'batch_table', batch_size '2')
;
-- If we freeze the only tuple on test1, the infomask should
-- always be the same in all test runs.
VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) test1
;
ALTER OPERATOR FAMILY gist_int4_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
DROP TABLE tab_rf_x
;
-- invalid: non-lowercase quoted identifiers

CREATE AGGREGATE case_agg ( -- old syntax
	"Sfunc1" = int4pl,
	"Basetype" = int4,
	"Stype1" = int4,
	"Initcond1" = '0',
	"Parallel" = safe
)
;
ANALYZE fkpart6.pk12
;
--
-- cannot make default conversion with same schema/for_encoding/to_encoding
--
CREATE DEFAULT CONVERSION public.mydef2 FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26250_2 (tid pg_catalog.tid)
;
DROP TRIGGER trig_row_before_insert ON rem1
;
/*
 * 1.1. test CREATE INDEX with buffered build
 */

-- Regular index with included columns
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
ALTER TABLE pg_toast.pg_toast_30684 RENAME TO reind_con_toast
;
ALTER TABLE pg_temp_38.pg_temp_28037_2 ADD COLUMN newdata pg_temp_38.pg_temp_28037
;
ANALYZE pg_temp_11.pg_temp_41234_2
;
CREATE TABLE "public"."shighway" (
    "name" "text",
    "thepath" "path",
    "surface" "text"
)
;
CREATE PUBLICATION tap_pub FOR TABLE tab_full
;
-- name datatype support new in 1.3.
CREATE FUNCTION gin_extract_value_name(name, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
CREATE FUNCTION gbt_bpchar_consistent(internal,bpchar,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
ANALYZE public.onek2
;
CREATE CAST (ean13 AS ismn) WITH FUNCTION ismn(ean13)
;
-- test indexes on arrays
create temp table arr_tbl (f1 int[] unique)
;
CREATE AGGREGATE "public"."newcnt"("any") (
    SFUNC = "int8inc_any",
    STYPE = bigint,
    INITCOND = '0'
)
;
VACUUM (SKIP_LOCKED) information_schema.sql_sizing
;
CREATE SCHEMA split_lock_next
;
ALTER TABLE fd_pt1 ADD COLUMN c7 integer NOT NULL
;
CREATE TEMP TABLE pg_temp_24.pg_temp_25663_2 (tid pg_catalog.tid)
;
CREATE FUNCTION cube(float8) RETURNS cube
AS '$libdir/cube', 'cube_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE VIEW key_dependent_view_no_cols AS
   SELECT FROM view_base_table GROUP BY key HAVING length(data) > 0
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE default_expr_non_column (a int DEFAULT (avg(non_existent)))
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION chkrolattr()
 RETURNS TABLE ("role" name, rolekeyword text, canlogin bool, replication bool)
 AS $$
SELECT r.rolname, v.keyword, r.rolcanlogin, r.rolreplication
 FROM pg_roles r
 JOIN (VALUES(CURRENT_USER, 'current_user'),
             (SESSION_USER, 'session_user'),
             ('current_user', '-'),
             ('session_user', '-'),
             ('Public', '-'),
             ('None', '-'))
      AS v(uname, keyword)
      ON (r.rolname = v.uname)
 ORDER BY 1;
$$ LANGUAGE SQL
;
----------
[ALTER_TABLE] ALTER TABLE rls_test_permissive ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_PROG_OBJ] -- test jsonb string -> python string
CREATE FUNCTION test1string(val jsonb) RETURNS text
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == "a")
return val
$$
;
----------
[CREATE_TABLE] create table perform_test (
	a	INT,
	b	INT
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_union(seg, seg)
RETURNS seg
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR > (
	PROCEDURE = isngt,
	LEFTARG = upc,
	RIGHTARG = ean13,
	COMMUTATOR = <,
	NEGATOR = <=,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ADMIN_TABLE] VACUUM  verbose pg_class
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_pltemplate
;
----------
[CREATE_PROG_OBJ] -- named and default parameters

CREATE OR REPLACE PROCEDURE ptest5(a int, b text, c int default 100)
LANGUAGE SQL
AS $$
INSERT INTO cp_test VALUES(a, b);
INSERT INTO cp_test VALUES(c, b);
$$
;
----------
[ADD_INDEX] CREATE INDEX abbrev_abort_uuids__abort_decreasing_idx ON abbrev_abort_uuids (abort_decreasing)
;
----------
[ADMIN_TABLE] ANALYZE public.plt2_p1
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "schema3" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] -- This does not work when the table isn't empty.  That's intentional,
-- since ADD GENERATED should only affect later insertions:
ALTER TABLE itest8
  ADD COLUMN f22 int NOT NULL,
  ALTER COLUMN f22 ADD GENERATED ALWAYS AS IDENTITY
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest3(col int PRIMARY KEY) ON COMMIT DELETE ROWS
;
----------
[CREATE_TABLE] create table inh_nn3 (f4 float) inherits (inh_nn2)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_class
;
----------
[CREATE_TABLE] create temp table parttbl1 partition of parttbl for values from (1) to (100)
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_m_p1
;
----------
[CREATE_TABLE] create table t11()
;
----------
[ADD_INDEX] CREATE INDEX CONCURRENTLY ginidx ON tbl USING gin(j)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.13
 * USER_MAPPINGS view
 */
CREATE VIEW user_mappings AS
    SELECT authorization_identifier,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_user_mappings
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ftable (a text, b int)
	SERVER loopback
	OPTIONS (table_name 'batch_table', batch_size '2')
;
----------
[ADMIN_TABLE] -- If we freeze the only tuple on test1, the infomask should
-- always be the same in all test runs.
VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) test1
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_int4_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[DROP_TABLE] DROP TABLE tab_rf_x
;
----------
[CREATE_PROG_OBJ] -- invalid: non-lowercase quoted identifiers

CREATE AGGREGATE case_agg ( -- old syntax
	"Sfunc1" = int4pl,
	"Basetype" = int4,
	"Stype1" = int4,
	"Initcond1" = '0',
	"Parallel" = safe
)
;
----------
[ADMIN_TABLE] ANALYZE fkpart6.pk12
;
----------
[CREATE_POLICY] --
-- cannot make default conversion with same schema/for_encoding/to_encoding
--
CREATE DEFAULT CONVERSION public.mydef2 FOR 'LATIN1' TO 'UTF8' FROM iso8859_1_to_utf8
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26250_2 (tid pg_catalog.tid)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_before_insert ON rem1
;
----------
[CREATE_TABLE] /*
 * 1.1. test CREATE INDEX with buffered build
 */

-- Regular index with included columns
CREATE TABLE tbl_gist (c1 int, c2 int, c3 int, c4 box)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30684 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_38.pg_temp_28037_2 ADD COLUMN newdata pg_temp_38.pg_temp_28037
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_41234_2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."shighway" (
    "name" "text",
    "thepath" "path",
    "surface" "text"
)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub FOR TABLE tab_full
;
----------
[CREATE_PROG_OBJ] -- name datatype support new in 1.3.
CREATE FUNCTION gin_extract_value_name(name, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bpchar_consistent(internal,bpchar,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_transform
;
----------
[ADMIN_TABLE] ANALYZE public.onek2
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ean13 AS ismn) WITH FUNCTION ismn(ean13)
;
----------
[CREATE_TABLE] -- test indexes on arrays
create temp table arr_tbl (f1 int[] unique)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."newcnt"("any") (
    SFUNC = "int8inc_any",
    STYPE = bigint,
    INITCOND = '0'
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) information_schema.sql_sizing
;
----------
[CREATE_SCHEMA] CREATE SCHEMA split_lock_next
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE fd_pt1 ADD COLUMN c7 integer NOT NULL
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_24.pg_temp_25663_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube(float8) RETURNS cube
AS '$libdir/cube', 'cube_f8'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW key_dependent_view_no_cols AS
   SELECT FROM view_base_table GROUP BY key HAVING length(data) > 0
;
