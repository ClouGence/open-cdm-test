drop table parted_stmt_trig, parted2_stmt_trig
;
CREATE INDEX "onek_unique2" ON "public"."onek" USING "btree" ("unique2")
;
VACUUM FULL cmdata
;
CREATE TABLE shoelace_data (
	sl_name    char(10),      -- primary key
	sl_avail   integer,       -- available # of pairs
	sl_color   char(10),      -- shoelace color
	sl_len     float,         -- shoelace length
	sl_unit    char(8)        -- length unit
)
;
CREATE FUNCTION isnne(isbn13, isbn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- test for bug reported in
-- CABRT9RC81YUf1=jsmWopcKJEro=VoeG2ou6sPwyOUTx_qteRsg@mail.gmail.com
CREATE TABLE IF NOT EXISTS testcase(
	id int PRIMARY KEY,
	balance numeric
)
;
-- OK
CREATE TABLE ctas_nodata_2 (ii, jj) AS SELECT i, j FROM ctas_base WITH NO DATA
;
--
-- page_checksum()
--
DROP FUNCTION page_checksum(IN page bytea, IN blkno int4)
;
create table canary (id int)
;
CREATE FUNCTION "public"."myleast"(VARIADIC "anyarray") RETURNS "anyelement"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$
  select min($1[i]) from generate_subscripts($1,1) g(i)
$_$
;
CREATE OR REPLACE FUNCTION event_trigger_report_end()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'END: command_tag=% type=% identity=%',
            r.command_tag, r.object_type, r.object_identity;
    END LOOP;
EXCEPTION WHEN SQLSTATE 'XX000' THEN
	RAISE NOTICE 'END: got internal exception';
END; $$
;
CREATE SUBSCRIPTION sub3 CONNECTION 'port=15683 host=/tmp/7RF1JxeTxg dbname=postgres' PUBLICATION pub3
;
DROP MATERIALIZED VIEW mvtest_mv1 CASCADE
;
-- cleanup
DROP TABLE gin_check_text_array
;
CREATE TABLE gtest_err_2c (a int PRIMARY KEY,
    b int GENERATED ALWAYS AS (num_nulls(gtest_err_2c)) STORED)
;
create table rangep_0_to_100_2 partition of rangep_0_to_100 for values in(2)
;
CREATE OR REPLACE FUNCTION pgstatginindex(IN relname regclass,
    OUT version INT4,
    OUT pending_pages INT4,
    OUT pending_tuples BIGINT)
AS '$libdir/pgstattuple', 'pgstatginindex_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE fk_partitioned_fk (a int default 2501, b int default 142857) PARTITION BY LIST (a)
;
DROP DOMAIN jsb_int_array_1d
;
CREATE TYPE casetestenum AS ENUM ('e', 'f', 'g')
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_dict
;
CREATE FUNCTION "public"."int42_out"("public"."int42") RETURNS "cstring"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int4out$$
;
CREATE FUNCTION "public"."tg_chkbacklink"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.backlink isnull then
        new.backlink := '';
    end if;
    return new;
end;
$$
;
DROP RULE IF EXISTS account_insert_skip ON split_trigger_probe.account_audit
;
CREATE FUNCTION gbt_intv_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE fd_pt1 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
)
;
ALTER TABLE T ADD COLUMN c_int INT NOT NULL DEFAULT -1
;
drop table indicator_test
;
create trigger trig_upd_after_child after update on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
/* contrib/pg_freespacemap/pg_freespacemap--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Register the C function.
CREATE FUNCTION pg_freespace(regclass, bigint)
RETURNS int2
AS '$libdir/pg_freespacemap', 'pg_freespace'
LANGUAGE C STRICT PARALLEL SAFE
;
ALTER TABLE "public"."truncate_b" ALTER COLUMN "id" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."truncate_b_id_seq"
    START WITH 44
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
-- shouldn't fail in DO, even though there's no useful data
do $$
declare
  fn_oid oid;
begin
  get diagnostics fn_oid = pg_routine_oid;
  raise notice 'pg_routine_oid = %', fn_oid;
end;
$$
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_foreign_table
;
CREATE FUNCTION split_event_helper.capture_table_rewrite()
RETURNS event_trigger
LANGUAGE plpgsql
AS $event$
BEGIN
    INSERT INTO split_event_helper.rewrite_log(relation_oid, reason)
    VALUES (
        pg_event_trigger_table_rewrite_oid(),
        pg_event_trigger_table_rewrite_reason()
    );
END;
$event$
;
alter table tpart attach partition tpart_1 for values from(0) to (100)
;
/* contrib/intarray/intarray--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Remove @ and ~
DROP OPERATOR @ (_int4, _int4)
;
CREATE AGGREGATE "public"."oldcnt"(*) (
    SFUNC = "int8inc",
    STYPE = bigint,
    INITCOND = '0'
)
;
CREATE FUNCTION g_int_consistent(internal,_int4,smallint,oid,internal)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create function f1(x anycompatible) returns anycompatiblerange as $$
begin
  return array[x + 1, x + 2];
end$$ language plpgsql
;
VACUUM (FULL) pg_catalog.pg_attrdef
;
CREATE TABLE prt1_e_p1 PARTITION OF prt1_e FOR VALUES FROM (0) TO (250)
;
ALTER TABLE ONLY "public"."fk_partitioned_fk_4" ATTACH PARTITION "public"."fk_partitioned_fk_4_2" FOR VALUES FROM (100, 100) TO (1000, 1000)
;
-- Create a trigger on part_d_1_15
CREATE FUNCTION func_d_1_15() RETURNS trigger AS $$
BEGIN
   NEW.c = NEW.c + 1; -- Make even numbers odd, or vice versa
   return NEW;
END $$ LANGUAGE plpgsql
;
ALTER TABLE tbl_gist ALTER c1 TYPE bigint
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_user_mapping
;
--
-- Test some simple polymorphism cases.
--

create function f1(x anyelement) returns anyelement as $$
begin
  return x + 1;
end$$ language plpgsql
;
CREATE DATABASE otherdb
;
CREATE POLICY evtrg_nontemp_pol ON evtrg_nontemp_table USING (f2 > 0)
;
alter table alterlock reset (fillfactor)
;
CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE NOTICE 'Table is being rewritten (reason = %)',
               pg_event_trigger_table_rewrite_reason();
END;
$$
;
------------------------------------------------------------------------------------------
[DROP_TABLE] drop table parted_stmt_trig, parted2_stmt_trig
;
----------
[ADD_INDEX] CREATE INDEX "onek_unique2" ON "public"."onek" USING "btree" ("unique2")
;
----------
[ADMIN_TABLE] VACUUM FULL cmdata
;
----------
[CREATE_TABLE] CREATE TABLE shoelace_data (
	sl_name    char(10),      -- primary key
	sl_avail   integer,       -- available # of pairs
	sl_color   char(10),      -- shoelace color
	sl_len     float,         -- shoelace length
	sl_unit    char(8)        -- length unit
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnne(isbn13, isbn13)
	RETURNS boolean
	AS 'int8ne'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- test for bug reported in
-- CABRT9RC81YUf1=jsmWopcKJEro=VoeG2ou6sPwyOUTx_qteRsg@mail.gmail.com
CREATE TABLE IF NOT EXISTS testcase(
	id int PRIMARY KEY,
	balance numeric
)
;
----------
[CREATE_TABLE|SELECT] -- OK
CREATE TABLE ctas_nodata_2 (ii, jj) AS SELECT i, j FROM ctas_base WITH NO DATA
;
----------
[DROP_PROG_OBJ] --
-- page_checksum()
--
DROP FUNCTION page_checksum(IN page bytea, IN blkno int4)
;
----------
[CREATE_TABLE] create table canary (id int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."myleast"(VARIADIC "anyarray") RETURNS "anyelement"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$
  select min($1[i]) from generate_subscripts($1,1) g(i)
$_$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION event_trigger_report_end()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'END: command_tag=% type=% identity=%',
            r.command_tag, r.object_type, r.object_identity;
    END LOOP;
EXCEPTION WHEN SQLSTATE 'XX000' THEN
	RAISE NOTICE 'END: got internal exception';
END; $$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub3 CONNECTION 'port=15683 host=/tmp/7RF1JxeTxg dbname=postgres' PUBLICATION pub3
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW mvtest_mv1 CASCADE
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE gin_check_text_array
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_2c (a int PRIMARY KEY,
    b int GENERATED ALWAYS AS (num_nulls(gtest_err_2c)) STORED)
;
----------
[CREATE_TABLE] create table rangep_0_to_100_2 partition of rangep_0_to_100 for values in(2)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pgstatginindex(IN relname regclass,
    OUT version INT4,
    OUT pending_pages INT4,
    OUT pending_tuples BIGINT)
AS '$libdir/pgstattuple', 'pgstatginindex_v1_5'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk (a int default 2501, b int default 142857) PARTITION BY LIST (a)
;
----------
[DROP_TYPE] DROP DOMAIN jsb_int_array_1d
;
----------
[CREATE_TYPE] CREATE TYPE casetestenum AS ENUM ('e', 'f', 'g')
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_ts_dict
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."int42_out"("public"."int42") RETURNS "cstring"
    LANGUAGE "internal" IMMUTABLE STRICT
    AS $$int4out$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_chkbacklink"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
    if new.backlink isnull then
        new.backlink := '';
    end if;
    return new;
end;
$$
;
----------
[DROP_POLICY] DROP RULE IF EXISTS account_insert_skip ON split_trigger_probe.account_audit
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_intv_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE fd_pt1 (
	c1 integer NOT NULL,
	c2 text,
	c3 date
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE T ADD COLUMN c_int INT NOT NULL DEFAULT -1
;
----------
[DROP_TABLE] drop table indicator_test
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_upd_after_child after update on parted_stmt_trig1
  for each row execute procedure trigger_notice()
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_freespacemap/pg_freespacemap--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Register the C function.
CREATE FUNCTION pg_freespace(regclass, bigint)
RETURNS int2
AS '$libdir/pg_freespacemap', 'pg_freespace'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."truncate_b" ALTER COLUMN "id" ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME "public"."truncate_b_id_seq"
    START WITH 44
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[BLOCK] -- shouldn't fail in DO, even though there's no useful data
do $$
declare
  fn_oid oid;
begin
  get diagnostics fn_oid = pg_routine_oid;
  raise notice 'pg_routine_oid = %', fn_oid;
end;
$$
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_foreign_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_event_helper.capture_table_rewrite()
RETURNS event_trigger
LANGUAGE plpgsql
AS $event$
BEGIN
    INSERT INTO split_event_helper.rewrite_log(relation_oid, reason)
    VALUES (
        pg_event_trigger_table_rewrite_oid(),
        pg_event_trigger_table_rewrite_reason()
    );
END;
$event$
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table tpart attach partition tpart_1 for values from(0) to (100)
;
----------
[DROP_PROG_OBJ] /* contrib/intarray/intarray--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Remove @ and ~
DROP OPERATOR @ (_int4, _int4)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."oldcnt"(*) (
    SFUNC = "int8inc",
    STYPE = bigint,
    INITCOND = '0'
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_int_consistent(internal,_int4,smallint,oid,internal)
RETURNS bool
AS '$libdir/_int'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function f1(x anycompatible) returns anycompatiblerange as $$
begin
  return array[x + 1, x + 2];
end$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_attrdef
;
----------
[CREATE_TABLE] CREATE TABLE prt1_e_p1 PARTITION OF prt1_e FOR VALUES FROM (0) TO (250)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."fk_partitioned_fk_4" ATTACH PARTITION "public"."fk_partitioned_fk_4_2" FOR VALUES FROM (100, 100) TO (1000, 1000)
;
----------
[CREATE_PROG_OBJ] -- Create a trigger on part_d_1_15
CREATE FUNCTION func_d_1_15() RETURNS trigger AS $$
BEGIN
   NEW.c = NEW.c + 1; -- Make even numbers odd, or vice versa
   return NEW;
END $$ LANGUAGE plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl_gist ALTER c1 TYPE bigint
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_user_mapping
;
----------
[CREATE_PROG_OBJ] --
-- Test some simple polymorphism cases.
--

create function f1(x anyelement) returns anyelement as $$
begin
  return x + 1;
end$$ language plpgsql
;
----------
[CREATE_CATALOG] CREATE DATABASE otherdb
;
----------
[CREATE_POLICY] CREATE POLICY evtrg_nontemp_pol ON evtrg_nontemp_table USING (f2 > 0)
;
----------
[ALTER_TABLE] alter table alterlock reset (fillfactor)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_evtrig_no_rewrite() RETURNS event_trigger
LANGUAGE plpgsql AS $$
BEGIN
  RAISE NOTICE 'Table is being rewritten (reason = %)',
               pg_event_trigger_table_rewrite_reason();
END;
$$
;
