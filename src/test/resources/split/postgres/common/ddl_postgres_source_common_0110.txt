ALTER TABLE pg_toast.pg_toast_28643 RENAME TO reind_con_toast
;
ALTER DATABASE "isolation_regression" SET lc_messages TO 'C'
;
DROP TABLE part_rp
;
-- error: not a procedure
ALTER PROCEDURE nonexistent() VOLATILE
;
CREATE TABLE part_5 (
	LIKE list_parted2
) PARTITION BY LIST (b)
;
CREATE TABLE y2 (a int, b text)
;
CREATE temporary TABLE tbl_zero(id int) WITH (AUTOVACUUM_ENABLED = false)
;
-- test hstore -> python
CREATE FUNCTION test1(val hstore) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
assert isinstance(val, dict)
plpy.info(sorted(val.items()))
return len(val)
$$
;
ALTER TABLE attmp ADD COLUMN y float4[]
;
-- RLS policies with update-row-movement
-----------------------------------------

ALTER TABLE range_parted ENABLE ROW LEVEL SECURITY
;
-- error

-- check that unlogged propagates to sequence
CREATE UNLOGGED TABLE itest17 (a int NOT NULL, b text)
;
ALTER TABLE fd_pt1 RENAME COLUMN c1 TO f1
;
drop table tab_part_create
;
CREATE TABLE split_alter_table.inherit_child (id integer, code text, child_note text)
;
create table boolpart_null partition of boolpart for values in (null)
;
CREATE FUNCTION hash_page_type(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'hash_page_type'
LANGUAGE C STRICT PARALLEL SAFE
;
--
-- Get the base type of a domain
--
create domain mytext as text
;
CREATE TABLE dump_test.test_second_table (
						   col1 int,
						   col2 text
					   )
;
CREATE POLICY p ON t USING (c % 2 = 1)
;
CREATE FUNCTION wait_for_hot_stats() RETURNS void AS $$
DECLARE
  start_time timestamptz := clock_timestamp();
  updated bool;
BEGIN
  -- we don't want to wait forever; loop will exit after 30 seconds
  FOR i IN 1 .. 300 LOOP
    SELECT (pg_stat_get_tuples_hot_updated('brin_hot'::regclass::oid) > 0) INTO updated;
    EXIT WHEN updated;

    -- wait a little
    PERFORM pg_sleep_for('100 milliseconds');
    -- reset stats snapshot so we can test again
    PERFORM pg_stat_clear_snapshot();
  END LOOP;
  -- report time waited in postmaster log (where it won't change test output)
  RAISE log 'wait_for_hot_stats delayed % seconds',
    EXTRACT(epoch FROM clock_timestamp() - start_time);
END
$$ LANGUAGE plpgsql
;
ALTER TABLE products ADD PRIMARY KEY (product_id)
;
-- check error reporting (bug #14476)
CREATE TABLE ctv_data (x int, y int, v text)
;
-- fails
TRUNCATE fk_partitioned_fk_full
;
-- Test MVCC-safety of cluster. There isn't much we can do to verify the
-- results with a single backend...

CREATE TABLE clustertest (key int PRIMARY KEY)
;
CREATE TABLE "generated_stored_tests"."gtest30_1" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
CREATE TABLE macaddr8tmp (a macaddr8)
;
VACUUM psql_pipeline
;
-- check that NOT NULL and default value are inherited correctly
create table parted_notnull_inh_test (a int default 1, b int not null default 0) partition by list (a)
;
CREATE TABLE "public"."test_tsvector" (
    "t" "text",
    "a" "tsvector"
)
WITH ("parallel_workers"='2')
;
-- test the case where a check constraint on default partition allows
-- to avoid scanning it when adding a new partition
create table defcheck (a int, b int) partition by list (b)
;
CREATE INDEX ON lt1 (a)
;
CREATE TABLE test_part_d (a int, b int) PARTITION BY LIST (a)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_namespace
;
DROP FUNCTION test_trigger()
;
CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
create table domain_test (a int, b int)
;
CREATE TEMP VIEW fdv1 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
;
TRUNCATE tab_seq_unlogged3 RESTART IDENTITY
;
ANALYZE bt_i4_heap
;
CREATE TABLE "generated_stored_tests"."gtest30" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
create table parted_2 partition of parted for values in (3, 4)
;
CREATE TABLE "generated_stored_tests"."gtest20" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 3)) STORED,
    CONSTRAINT "gtest20_b_check" CHECK (("b" < 50))
)
;
CREATE TABLE undroppable_objs (
	object_type text,
	object_identity text
)
;
DROP OPERATOR FAMILY IF EXISTS test_operator_family USING btree
;
ALTER OPERATOR FAMILY alt_opf16 USING gist ADD FUNCTION 1 btint42cmp(int4, int2)
;
-- check it works with a named result rowtype

create type my_crosstab_result as (
  rowid text, rowdt timestamp,
  temperature int4, test_result text, test_startdate timestamp, volts float8)
;
create domain dependenttypetest domaindroptest
;
ALTER TABLE gtest20c ADD CONSTRAINT whole_row_check CHECK (gtest20c IS NOT NULL)
;
CREATE TABLE stream_test2(data bytea)
;
DROP TABLE taby
;
------------------------------------------------------------------------------------------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28643 RENAME TO reind_con_toast
;
----------
[ALTER_CATALOG] ALTER DATABASE "isolation_regression" SET lc_messages TO 'C'
;
----------
[DROP_TABLE] DROP TABLE part_rp
;
----------
[ALTER_PROG_OBJ] -- error: not a procedure
ALTER PROCEDURE nonexistent() VOLATILE
;
----------
[CREATE_TABLE] CREATE TABLE part_5 (
	LIKE list_parted2
) PARTITION BY LIST (b)
;
----------
[CREATE_TABLE] CREATE TABLE y2 (a int, b text)
;
----------
[CREATE_TABLE] CREATE temporary TABLE tbl_zero(id int) WITH (AUTOVACUUM_ENABLED = false)
;
----------
[CREATE_PROG_OBJ] -- test hstore -> python
CREATE FUNCTION test1(val hstore) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
assert isinstance(val, dict)
plpy.info(sorted(val.items()))
return len(val)
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN y float4[]
;
----------
[ALTER_TABLE] -- RLS policies with update-row-movement
-----------------------------------------

ALTER TABLE range_parted ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] -- error

-- check that unlogged propagates to sequence
CREATE UNLOGGED TABLE itest17 (a int NOT NULL, b text)
;
----------
[RENAME_COLUMN] ALTER TABLE fd_pt1 RENAME COLUMN c1 TO f1
;
----------
[DROP_TABLE] drop table tab_part_create
;
----------
[CREATE_TABLE] CREATE TABLE split_alter_table.inherit_child (id integer, code text, child_note text)
;
----------
[CREATE_TABLE] create table boolpart_null partition of boolpart for values in (null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hash_page_type(IN page bytea)
RETURNS text
AS '$libdir/pageinspect', 'hash_page_type'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TYPE] --
-- Get the base type of a domain
--
create domain mytext as text
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_second_table (
						   col1 int,
						   col2 text
					   )
;
----------
[CREATE_POLICY] CREATE POLICY p ON t USING (c % 2 = 1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION wait_for_hot_stats() RETURNS void AS $$
DECLARE
  start_time timestamptz := clock_timestamp();
  updated bool;
BEGIN
  -- we don't want to wait forever; loop will exit after 30 seconds
  FOR i IN 1 .. 300 LOOP
    SELECT (pg_stat_get_tuples_hot_updated('brin_hot'::regclass::oid) > 0) INTO updated;
    EXIT WHEN updated;

    -- wait a little
    PERFORM pg_sleep_for('100 milliseconds');
    -- reset stats snapshot so we can test again
    PERFORM pg_stat_clear_snapshot();
  END LOOP;
  -- report time waited in postmaster log (where it won't change test output)
  RAISE log 'wait_for_hot_stats delayed % seconds',
    EXTRACT(epoch FROM clock_timestamp() - start_time);
END
$$ LANGUAGE plpgsql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE products ADD PRIMARY KEY (product_id)
;
----------
[CREATE_TABLE] -- check error reporting (bug #14476)
CREATE TABLE ctv_data (x int, y int, v text)
;
----------
[TRUNCATE_TABLE] -- fails
TRUNCATE fk_partitioned_fk_full
;
----------
[CREATE_TABLE] -- Test MVCC-safety of cluster. There isn't much we can do to verify the
-- results with a single backend...

CREATE TABLE clustertest (key int PRIMARY KEY)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest30_1" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_TABLE] CREATE TABLE macaddr8tmp (a macaddr8)
;
----------
[ADMIN_TABLE] VACUUM psql_pipeline
;
----------
[CREATE_TABLE] -- check that NOT NULL and default value are inherited correctly
create table parted_notnull_inh_test (a int default 1, b int not null default 0) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test_tsvector" (
    "t" "text",
    "a" "tsvector"
)
WITH ("parallel_workers"='2')
;
----------
[CREATE_TABLE] -- test the case where a check constraint on default partition allows
-- to avoid scanning it when adding a new partition
create table defcheck (a int, b int) partition by list (b)
;
----------
[ADD_INDEX] CREATE INDEX ON lt1 (a)
;
----------
[CREATE_TABLE] CREATE TABLE test_part_d (a int, b int) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_namespace
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_trigger()
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_TABLE] create table domain_test (a int, b int)
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW fdv1 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
;
----------
[TRUNCATE_TABLE] TRUNCATE tab_seq_unlogged3 RESTART IDENTITY
;
----------
[ADMIN_TABLE] ANALYZE bt_i4_heap
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest30" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_TABLE] create table parted_2 partition of parted for values in (3, 4)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest20" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 3)) STORED,
    CONSTRAINT "gtest20_b_check" CHECK (("b" < 50))
)
;
----------
[CREATE_TABLE] CREATE TABLE undroppable_objs (
	object_type text,
	object_identity text
)
;
----------
[DROP_PROG_OBJ] DROP OPERATOR FAMILY IF EXISTS test_operator_family USING btree
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf16 USING gist ADD FUNCTION 1 btint42cmp(int4, int2)
;
----------
[CREATE_TYPE] -- check it works with a named result rowtype

create type my_crosstab_result as (
  rowid text, rowdt timestamp,
  temperature int4, test_result text, test_startdate timestamp, volts float8)
;
----------
[CREATE_TYPE] create domain dependenttypetest domaindroptest
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE gtest20c ADD CONSTRAINT whole_row_check CHECK (gtest20c IS NOT NULL)
;
----------
[CREATE_TABLE] CREATE TABLE stream_test2(data bytea)
;
----------
[DROP_TABLE] DROP TABLE taby
;
