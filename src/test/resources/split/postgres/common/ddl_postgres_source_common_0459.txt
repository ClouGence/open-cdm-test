CREATE FUNCTION bt_page_stats(IN relname text, IN blkno int8,
    OUT blkno int8,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int8,
    OUT btpo_next int8,
    OUT btpo_level int8,
    OUT btpo_flags int4)
AS '$libdir/pageinspect', 'bt_page_stats_1_9'
LANGUAGE C STRICT PARALLEL SAFE
;
create table tprt_3 partition of tprt for values from (1001) to (2001)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_collation
;
CREATE DATABASE "ecpg1_regression" TEMPLATE=template0
;
CREATE TABLE cj_source1 (sid1 integer, scat integer, delta integer)
  WITH (autovacuum_enabled=off)
;
CREATE EXTENSION lo
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c7 integer NOT NULL
;
DROP TABLE alter_idx_rename_test_2
;
DROP DATABASE dropme
;
-- Make sure bad values are refused
create index gist_pointidx5 on gist_point_tbl using gist(p) with (buffering = invalid_value)
;
create table list_parted_tbl (a int,b int) partition by list (a)
;
DROP TABLE atestp1
;
CREATE TABLE "public"."prt1_l_p2_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
/*
 * 5.8
 * ADMINISTRABLE_ROLE_AUTHORIZATIONS view
 */

CREATE VIEW administrable_role_authorizations AS
    SELECT *
    FROM applicable_roles
    WHERE is_grantable = 'YES'
;
CREATE OPERATOR !=- (
   leftarg = int8,		
   procedure = factorial
)
;
CREATE TABLE testschema.test_default_tab_p1 PARTITION OF testschema.test_default_tab_p
    FOR VALUES IN (1)
;
do $$
declare r ordered_int8s;
begin
  r := null;
  r := row(null,null);
  r := row(1,2);
  r := row(2,1);  -- fail
end$$
;
DROP TABLE lock_tbl1a
;
DROP TABLE upsert
;
CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (xmin)
;
CREATE FUNCTION ghstore_options(internal)
RETURNS void
AS '$libdir/hstore', 'ghstore_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
CREATE INDEX "gin_check_jsonb_idx" on gin_check_jsonb USING GIN("j" jsonb_path_ops)
;
-- Test that a valid child can have not-valid parent, but not vice versa
create table invalid_check_con(f1 int)
;
analyze test_mode
;
create table idxpart2 partition of idxpart for values from (250) to (500)
;
CREATE OR REPLACE PROCEDURE overload(i text) AS $$
DECLARE
  r text;
BEGIN
  SELECT i::text INTO r;
END; $$ LANGUAGE plpgsql
;
-- test default partition
create table part_default partition of list_parted default
;
CREATE SEQUENCE "public"."sequence_test10"
    AS smallint
    START WITH 1
    INCREMENT BY 1
    MINVALUE -20000
    NO MAXVALUE
    CACHE 1
;
CREATE FUNCTION test_lfind8()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
CREATE TABLE sro_ptab (a int) PARTITION BY RANGE (a)
;
CREATE VIEW rw_view2 WITH (security_barrier = true) AS
  SELECT * FROM rw_view1 WHERE snoop(person)
;
ALTER TABLE pg_toast.pg_toast_30515 RENAME TO reind_con_toast
;
DROP TABLESPACE regress_ts1
;
COMMENT ON SEQUENCE fkey_table_seq IS 'SEQUENCE test'
;
ALTER OPERATOR FAMILY gist__intbig_ops USING gist
ADD FUNCTION 10 (_int4) g_intbig_options (internal)
;
CREATE AGGREGATE testagg8(int2) (SFUNC = int2_sum, STYPE = int8)
;
-- operator
CREATE OPERATOR === (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
drop schema if exists abc,aac restrict
;
ALTER TABLE ONLY "public"."gtest29" DROP COLUMN "........pg.dropped.1........"
;
DROP FUNCTION xid_current()
;
-- gone


--
-- Partitions
--

-- check partition bound syntax

CREATE TABLE list_parted (
	a int
) PARTITION BY LIST (a)
;
ANALYZE alpha
;
CREATE TABLE vacowned_parted (a int) PARTITION BY LIST (a)
;
DROP TABLE spill_test
;
CREATE TRIGGER trg1 BEFORE INSERT ON t1 EXECUTE FUNCTION foo_trg()
;
create table s2.p2_1 partition of s2.p2 for values in (1, 2, 3)
;
CREATE FUNCTION testHVToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
$val = {a => 1, b => 'boo', c => undef};
return $val;
$$
;
ANALYZE pg_temp_81.pg_temp_30251_2
;
COMMENT ON LARGE OBJECT 31345 IS 'I Wandered Lonely as a Cloud'
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_page_stats(IN relname text, IN blkno int8,
    OUT blkno int8,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int8,
    OUT btpo_next int8,
    OUT btpo_level int8,
    OUT btpo_flags int4)
AS '$libdir/pageinspect', 'bt_page_stats_1_9'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create table tprt_3 partition of tprt for values from (1001) to (2001)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_collation
;
----------
[CREATE_CATALOG] CREATE DATABASE "ecpg1_regression" TEMPLATE=template0
;
----------
[CREATE_TABLE] CREATE TABLE cj_source1 (sid1 integer, scat integer, delta integer)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION lo
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c7 integer NOT NULL
;
----------
[DROP_TABLE] DROP TABLE alter_idx_rename_test_2
;
----------
[DROP_CATALOG] DROP DATABASE dropme
;
----------
[ADD_INDEX] -- Make sure bad values are refused
create index gist_pointidx5 on gist_point_tbl using gist(p) with (buffering = invalid_value)
;
----------
[CREATE_TABLE] create table list_parted_tbl (a int,b int) partition by list (a)
;
----------
[DROP_TABLE] DROP TABLE atestp1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt1_l_p2_p2" (
    "a" integer,
    "b" integer,
    "c" character varying
)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 5.8
 * ADMINISTRABLE_ROLE_AUTHORIZATIONS view
 */

CREATE VIEW administrable_role_authorizations AS
    SELECT *
    FROM applicable_roles
    WHERE is_grantable = 'YES'
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !=- (
   leftarg = int8,		
   procedure = factorial
)
;
----------
[CREATE_TABLE] CREATE TABLE testschema.test_default_tab_p1 PARTITION OF testschema.test_default_tab_p
    FOR VALUES IN (1)
;
----------
[BLOCK] do $$
declare r ordered_int8s;
begin
  r := null;
  r := row(null,null);
  r := row(1,2);
  r := row(2,1);  -- fail
end$$
;
----------
[DROP_TABLE] DROP TABLE lock_tbl1a
;
----------
[DROP_TABLE] DROP TABLE upsert
;
----------
[CREATE_TABLE] CREATE TABLE partitioned (
	a int
) PARTITION BY RANGE (xmin)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ghstore_options(internal)
RETURNS void
AS '$libdir/hstore', 'ghstore_options'
LANGUAGE C IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX "gin_check_jsonb_idx" on gin_check_jsonb USING GIN("j" jsonb_path_ops)
;
----------
[CREATE_TABLE] -- Test that a valid child can have not-valid parent, but not vice versa
create table invalid_check_con(f1 int)
;
----------
[ADMIN_TABLE] analyze test_mode
;
----------
[CREATE_TABLE] create table idxpart2 partition of idxpart for values from (250) to (500)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE PROCEDURE overload(i text) AS $$
DECLARE
  r text;
BEGIN
  SELECT i::text INTO r;
END; $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] -- test default partition
create table part_default partition of list_parted default
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test10"
    AS smallint
    START WITH 1
    INCREMENT BY 1
    MINVALUE -20000
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_lfind8()
	RETURNS pg_catalog.void
	AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE sro_ptab (a int) PARTITION BY RANGE (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 WITH (security_barrier = true) AS
  SELECT * FROM rw_view1 WHERE snoop(person)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30515 RENAME TO reind_con_toast
;
----------
[DROP_TABLESPACE] DROP TABLESPACE regress_ts1
;
----------
[COMMENT_SEQUENCE] COMMENT ON SEQUENCE fkey_table_seq IS 'SEQUENCE test'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist__intbig_ops USING gist
ADD FUNCTION 10 (_int4) g_intbig_options (internal)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testagg8(int2) (SFUNC = int2_sum, STYPE = int8)
;
----------
[CREATE_PROG_OBJ] -- operator
CREATE OPERATOR === (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
----------
[DROP_SCHEMA] drop schema if exists abc,aac restrict
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."gtest29" DROP COLUMN "........pg.dropped.1........"
;
----------
[DROP_PROG_OBJ] DROP FUNCTION xid_current()
;
----------
[CREATE_TABLE] -- gone


--
-- Partitions
--

-- check partition bound syntax

CREATE TABLE list_parted (
	a int
) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] ANALYZE alpha
;
----------
[CREATE_TABLE] CREATE TABLE vacowned_parted (a int) PARTITION BY LIST (a)
;
----------
[DROP_TABLE] DROP TABLE spill_test
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trg1 BEFORE INSERT ON t1 EXECUTE FUNCTION foo_trg()
;
----------
[CREATE_TABLE] create table s2.p2_1 partition of s2.p2 for values in (1, 2, 3)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testHVToJsonb() RETURNS jsonb
LANGUAGE plperl
TRANSFORM FOR TYPE jsonb
AS $$
$val = {a => 1, b => 'boo', c => undef};
return $val;
$$
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_81.pg_temp_30251_2
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 31345 IS 'I Wandered Lonely as a Cloud'
;
