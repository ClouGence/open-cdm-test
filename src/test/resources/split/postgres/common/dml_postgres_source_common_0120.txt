-- Multiple partitions
insert into tbl1 values (1001), (1010), (1011)
;
UPDATE hash_txt_heap
   SET seqno = 20000
   WHERE hash_txt_heap.random = '959363399'::text
;
INSERT INTO stats_test SELECT 'serialize-toobig--1:'||g.i FROM generate_series(1, 5000) g(i)
;
delete from c1
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14927', relminmxid = '3'
WHERE oid = '"fkpart5"."fk4"'::pg_catalog.regclass
;
UPDATE rw_view14 SET b='ROW 3' WHERE a=3
;
INSERT INTO pa_target SELECT '2017-01-31', id, id * 100, 'initial' FROM generate_series(1,9,3) AS id
;
INSERT INTO nummultirange_test2 VALUES(nummultirange(numrange(1.1, 2.2,'()')))
;
DELETE FROM gtest1 WHERE a = 2000000000
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i'),
             ('........pg.dropped.4........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."dropcolumnchild"'::pg_catalog.regclass
  AND attname = v.dname
;
UPDATE pgbench_branches SET bbalance = bbalance + 4422 WHERE bid = 1
;
-- No row found
UPDATE part_c_1_100 set c = c + 20 WHERE c = 98
;
INSERT INTO PKTABLE VALUES (2, NULL, 4, 'test3')
;
INSERT INTO gtest23b VALUES (1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8424', relminmxid = '1'
WHERE oid = '"public"."rtest_t6"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('9', 659)
;
DELETE FROM temporal_partitioned_mltrng WHERE id = '[5,6)' AND valid_at = datemultirange(daterange('2018-01-01', '2018-02-01'))
;
INSERT INTO rw_view2 VALUES (-5)
;
-- This ignores the 30 and uses the sequence value instead:
INSERT INTO itest2 OVERRIDING USER VALUE VALUES (30, 'xyz')
;
INSERT INTO INT2_TBL(f1) VALUES ('34.5')
;
-- if an alias for the target table is specified, don't allow references
-- to the original table name
UPDATE update_test AS t SET b = update_test.b + 10 WHERE t.a = 10
;
insert into WSlot values ('WS.102.2b', '102', '', '')
;
with u as (update another_parttbl set a = 1 returning another_parttbl.*)
	update parttbl p set a = u.a from u where p.a = u.a and p.c = 1 returning p.*
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15866', relminmxid = '3'
WHERE oid = '36639'
;
INSERT INTO public.tht VALUES ('6', 596)
;
INSERT INTO caster (tsquery)       VALUES ('fat & rat'::text)
;
INSERT INTO POLYGON_TBL(f1) VALUES ('(2.0,0.0),(2.0,4.0),(0.0,0.0)')
;
INSERT INTO public.ths_p2 VALUES (7, 167)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16206', relminmxid = '3'
WHERE oid = '37438'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '772', relminmxid = '1'
WHERE oid = '"public"."text_tbl"'::pg_catalog.regclass
;
INSERT INTO t_009_tbl VALUES (26, 'issued to london')
;
INSERT INTO caster (char)          VALUES ('f'::citext)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '21294'
;
INSERT INTO pagg_tab2 SELECT i % 20, i % 30 FROM generate_series(0, 299, 3) i
;
INSERT INTO attmp2 values (2)
;
INSERT INTO public.ths_p3 VALUES (9, 19)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12437', relminmxid = '3'
WHERE oid = '"public"."shiftjis2004_inputs"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17951', relminmxid = '3'
WHERE oid = '"public"."idxpart31"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt1_e_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_m_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
with wcte as (insert into table1 values (43))
  insert into table1 values (44)
;
UPDATE ONLY "public"."fktable" SET "ftest1" = NULL, "ftest2" = NULL, "ftest3" = NULL WHERE $1 OPERATOR(pg_catalog.=) "ftest1" AND $2 OPERATOR(pg_catalog.=) "ftest2" AND $3 OPERATOR(pg_catalog.=) "ftest3"
;
insert into inserttest values (DEFAULT, 7)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."c"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"public"."itest14"'::pg_catalog.regclass
;
INSERT INTO fk_notpartitioned_pk (a,b) VALUES (2502, 2503)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '"public"."plt1_e_p3"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11941', relminmxid = '1'
WHERE oid = '"public"."rtest_emp"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17282', relminmxid = '3'
WHERE oid = '39554'
;
INSERT INTO temporal3 (id, valid_at) VALUES ('[2,3)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT (id, valid_at) DO NOTHING
;
------------------------------------------------------------------------------------------
[INSERT] -- Multiple partitions
insert into tbl1 values (1001), (1010), (1011)
;
----------
[UPDATE] UPDATE hash_txt_heap
   SET seqno = 20000
   WHERE hash_txt_heap.random = '959363399'::text
;
----------
[INSERT|SELECT] INSERT INTO stats_test SELECT 'serialize-toobig--1:'||g.i FROM generate_series(1, 5000) g(i)
;
----------
[DELETE] delete from c1
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '14927', relminmxid = '3'
WHERE oid = '"fkpart5"."fk4"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE rw_view14 SET b='ROW 3' WHERE a=3
;
----------
[INSERT|SELECT] INSERT INTO pa_target SELECT '2017-01-31', id, id * 100, 'initial' FROM generate_series(1,9,3) AS id
;
----------
[INSERT] INSERT INTO nummultirange_test2 VALUES(nummultirange(numrange(1.1, 2.2,'()')))
;
----------
[DELETE] DELETE FROM gtest1 WHERE a = 2000000000
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.1........', 4, 'i'),
             ('........pg.dropped.4........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."dropcolumnchild"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[UPDATE] UPDATE pgbench_branches SET bbalance = bbalance + 4422 WHERE bid = 1
;
----------
[UPDATE] -- No row found
UPDATE part_c_1_100 set c = c + 20 WHERE c = 98
;
----------
[INSERT] INSERT INTO PKTABLE VALUES (2, NULL, 4, 'test3')
;
----------
[INSERT] INSERT INTO gtest23b VALUES (1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8424', relminmxid = '1'
WHERE oid = '"public"."rtest_t6"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('9', 659)
;
----------
[DELETE] DELETE FROM temporal_partitioned_mltrng WHERE id = '[5,6)' AND valid_at = datemultirange(daterange('2018-01-01', '2018-02-01'))
;
----------
[INSERT] INSERT INTO rw_view2 VALUES (-5)
;
----------
[INSERT|SELECT] -- This ignores the 30 and uses the sequence value instead:
INSERT INTO itest2 OVERRIDING USER VALUE VALUES (30, 'xyz')
;
----------
[INSERT|SELECT] INSERT INTO INT2_TBL(f1) VALUES ('34.5')
;
----------
[UPDATE] -- if an alias for the target table is specified, don't allow references
-- to the original table name
UPDATE update_test AS t SET b = update_test.b + 10 WHERE t.a = 10
;
----------
[INSERT] insert into WSlot values ('WS.102.2b', '102', '', '')
;
----------
[UPDATE] with u as (update another_parttbl set a = 1 returning another_parttbl.*)
	update parttbl p set a = u.a from u where p.a = u.a and p.c = 1 returning p.*
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15866', relminmxid = '3'
WHERE oid = '36639'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 596)
;
----------
[INSERT|SELECT] INSERT INTO caster (tsquery)       VALUES ('fat & rat'::text)
;
----------
[INSERT|SELECT] INSERT INTO POLYGON_TBL(f1) VALUES ('(2.0,0.0),(2.0,4.0),(0.0,0.0)')
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 167)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16206', relminmxid = '3'
WHERE oid = '37438'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '772', relminmxid = '1'
WHERE oid = '"public"."text_tbl"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO t_009_tbl VALUES (26, 'issued to london')
;
----------
[INSERT|SELECT] INSERT INTO caster (char)          VALUES ('f'::citext)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '21294'
;
----------
[INSERT|SELECT] INSERT INTO pagg_tab2 SELECT i % 20, i % 30 FROM generate_series(0, 299, 3) i
;
----------
[INSERT] INSERT INTO attmp2 values (2)
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (9, 19)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12437', relminmxid = '3'
WHERE oid = '"public"."shiftjis2004_inputs"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17951', relminmxid = '3'
WHERE oid = '"public"."idxpart31"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."plt1_e_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt1_m_p1"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] with wcte as (insert into table1 values (43))
  insert into table1 values (44)
;
----------
[UPDATE] UPDATE ONLY "public"."fktable" SET "ftest1" = NULL, "ftest2" = NULL, "ftest3" = NULL WHERE $1 OPERATOR(pg_catalog.=) "ftest1" AND $2 OPERATOR(pg_catalog.=) "ftest2" AND $3 OPERATOR(pg_catalog.=) "ftest3"
;
----------
[INSERT] insert into inserttest values (DEFAULT, 7)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."c"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"public"."itest14"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO fk_notpartitioned_pk (a,b) VALUES (2502, 2503)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '"public"."plt1_e_p3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '11941', relminmxid = '1'
WHERE oid = '"public"."rtest_emp"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17282', relminmxid = '3'
WHERE oid = '39554'
;
----------
[MERGE|SELECT] INSERT INTO temporal3 (id, valid_at) VALUES ('[2,3)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT (id, valid_at) DO NOTHING
;
