ANALYZE pg_temp_9.pg_temp_26388_2
;
CREATE TABLE moved (id int)
;
ALTER TABLE dump_test.test_table ALTER COLUMN col1 SET STATISTICS 90
;
-- Add PK to already inheriting table:
CREATE TABLE temporal_rng (
  id int4range,
  valid_at daterange
)
;
CREATE SUBSCRIPTION tap_sub_schema CONNECTION 'port=12176 host=/tmp/HYB8U9Fr3a dbname=postgres' PUBLICATION tap_pub_schema
;
ALTER TABLE ONLY "public"."comment_test_child"
    ADD CONSTRAINT "comment_test_child_fk" FOREIGN KEY ("id") REFERENCES "public"."comment_test"("id")
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=24651 host=/tmp/TH9eCEnfHC dbname=postgres' PUBLICATION mypub
;
CREATE TYPE stats_type as (f1 numeric(35, 6), f2 numeric(35, 2))
;
DO $$
DECLARE
	archived bool;
	loops int := 0;
BEGIN
	LOOP
		archived := count(*) > 0 FROM pg_ls_dir('.', false, false) a
			WHERE a ~ '^[0-9A-F]{24}$';
		IF archived OR loops > 120 * 10 THEN EXIT; END IF;
		PERFORM pg_sleep(0.1);
		loops := loops + 1;
	END LOOP;
END
$$
;
-- error
CREATE UNIQUE INDEX ON test3ci (x)
;
--
-- if neither 'FOR EACH ROW' nor 'FOR EACH STATEMENT' was specified,
-- CREATE TRIGGER should default to 'FOR EACH STATEMENT'
--
CREATE TRIGGER after_upd_stmt_trig AFTER UPDATE ON main_table
EXECUTE PROCEDURE trigger_func('after_upd_stmt')
;
create function zero_divide() returns int as $$
declare v int := 0;
begin
  return 10 / v;
end;
$$ language plpgsql parallel safe
;
DROP DOMAIN dint4, dint4_0
;
--
-- Matching citext in string comparison functions.
-- XXX TODO Ideally these would be implemented in C.
--

CREATE FUNCTION regexp_match( citext, citext ) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
-- Test lastval()
CREATE SEQUENCE seq
;
create table pph_arrpart2 partition of pph_arrpart for values with (modulus 2, remainder 1)
;
CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = numeric,
	sfunc = numeric_add,
	finalfunc = numeric_out
)
;
--
-- Test invalid options.
--
ALTER OPERATOR === (boolean, boolean) SET (COMMUTATOR = ====)
;
CREATE TABLE "generated_stored_tests"."gtest28b" (
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2)) STORED
)
;
CREATE FUNCTION bt_multi_page_stats(IN relname text, IN blkno int8, IN blk_count int8,
    OUT blkno int8,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int8,
    OUT btpo_next int8,
    OUT btpo_level int8,
    OUT btpo_flags int4)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_multi_page_stats'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
VACUUM (FULL) pg_catalog.pg_ts_config_map
;
CREATE TABLE "public"."hash_i4_heap" (
    "seqno" integer,
    "random" integer
)
;
ALTER TABLE pg_toast.pg_toast_28272 RENAME TO reind_con_toast
;
CREATE FUNCTION "public"."tg_backlink_set"("myname" character, "blname" character) RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
declare
    mytype	char(2);
    link	char(4);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    link := mytype || substr(blname, 1, 2);
    if link = 'PLPL' then
        raise exception
		'backlink between two phone lines does not make sense';
    end if;
    if link in ('PLWS', 'WSPL') then
        raise exception
		'direct link of phone line to wall slot not permitted';
    end if;
    if mytype = 'PS' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    raise exception '% does not exist', myname;
	end if;
	if rec.backlink != blname then
	    update PSlot set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'WS' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    raise exception '% does not exist', myname;
	end if;
	if rec.backlink != blname then
	    update WSlot set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'PL' then
        select into rec * from PLine where slotname = myname;
	if not found then
	    raise exception '% does not exist', myname;
	end if;
	if rec.backlink != blname then
	    update PLine set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    raise exception 'illegal backlink beginning with %', mytype;
end;
$$
;
-- check defaults for variadics

create function dfunc(a variadic int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
ALTER PUBLICATION testpub_default DROP TABLE pub_test.testpub_nopk
;
ANALYZE public.rtest_person
;
ALTER INDEX pg_toast.pg_toast_28084_index RENAME TO reind_con_toast_idx
;
CREATE TABLE reloptions_test2(i INT) WITH (fillfactor)
;
CREATE VIEW v1 AS SELECT null::int AS a
;
SECURITY LABEL ON TABLE var_ptbl
	IS 'system_u:object_r:sepgsql_regtest_var_table_t:s0'
;
CREATE TABLE FKTABLE ( ftest1 int DEFAULT 0, ftest2 int DEFAULT -1, ftest3 int DEFAULT -2, ftest4 int, CONSTRAINT constrname3
			FOREIGN KEY(ftest1, ftest2, ftest3) REFERENCES PKTABLE
			ON DELETE SET NULL ON UPDATE SET DEFAULT)
;
-- make sure changing the column list is propagated to the catalog
CREATE TABLE testpub_tbl7 (a int primary key, b text, c text)
;
-- Disable batch insert
CREATE FOREIGN TABLE ftable ( x int ) SERVER loopback OPTIONS ( table_name 'batch_table', batch_size '1' )
;
CREATE VIEW user_mappings AS
    SELECT authorization_identifier,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_user_mappings
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0
;
-- CREATE FOREIGN TABLE
CREATE SCHEMA foreign_schema
;
-- Check state of index_cleanup and truncate in VACUUM.
CREATE TABLE vac_tab_on_toast_off(i int, j text) WITH
  (autovacuum_enabled=false,
   vacuum_index_cleanup=true, toast.vacuum_index_cleanup=false,
   vacuum_truncate=true, toast.vacuum_truncate=false)
;
ALTER TABLE ONLY "public"."d" INHERIT "public"."b"
;
CREATE FUNCTION "public"."set_ttdummy"(integer) RETURNS integer
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'set_ttdummy'
;
/* src/test/modules/test_custom_rmgrs/test_custom_rmgrs--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- test_custom_rmgrs_insert_wal_record()
--
-- Writes a simple message into WAL with the help of custom WAL
-- resource manager.
--
CREATE FUNCTION test_custom_rmgrs_insert_wal_record(IN payload TEXT,
    OUT lsn pg_lsn
)
AS '$libdir/test_custom_rmgrs', 'test_custom_rmgrs_insert_wal_record'
LANGUAGE C STRICT PARALLEL UNSAFE
;
-- test hstore[] -> perl
CREATE FUNCTION test1arr(val hstore[]) RETURNS int
LANGUAGE plperlu
TRANSFORM FOR TYPE hstore
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_[0]->[0], $_[0]->[1]));
return scalar(keys %{$_[0]});
$$
;
create table rewritemetoo1 of rewritetype
;
CREATE PUBLICATION test_pub2 FOR ALL TABLES
;
CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_view
;
-- read-only view with WHERE clause
CREATE VIEW european_city_view AS
    SELECT * FROM city_view WHERE continent = 'Europe'
;
CREATE FUNCTION gbtreekey16_in(cstring)
RETURNS gbtreekey16
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
create rule irule1 as on insert to inserttest2 do also
  insert into inserttest (f3.if2[1], f3.if2[2])
  values (new.f1,new.f2)
;
-- check that the table being attached is not already a partition
ALTER TABLE list_parted2 ATTACH PARTITION part_2 FOR VALUES IN (2)
;
create table anothertab(f1 int primary key, f2 int unique,
                        f3 int, f4 int, f5 int)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE pg_temp_9.pg_temp_26388_2
;
----------
[CREATE_TABLE] CREATE TABLE moved (id int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE dump_test.test_table ALTER COLUMN col1 SET STATISTICS 90
;
----------
[CREATE_TABLE] -- Add PK to already inheriting table:
CREATE TABLE temporal_rng (
  id int4range,
  valid_at daterange
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_schema CONNECTION 'port=12176 host=/tmp/HYB8U9Fr3a dbname=postgres' PUBLICATION tap_pub_schema
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."comment_test_child"
    ADD CONSTRAINT "comment_test_child_fk" FOREIGN KEY ("id") REFERENCES "public"."comment_test"("id")
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=24651 host=/tmp/TH9eCEnfHC dbname=postgres' PUBLICATION mypub
;
----------
[CREATE_TYPE] CREATE TYPE stats_type as (f1 numeric(35, 6), f2 numeric(35, 2))
;
----------
[BLOCK] DO $$
DECLARE
	archived bool;
	loops int := 0;
BEGIN
	LOOP
		archived := count(*) > 0 FROM pg_ls_dir('.', false, false) a
			WHERE a ~ '^[0-9A-F]{24}$';
		IF archived OR loops > 120 * 10 THEN EXIT; END IF;
		PERFORM pg_sleep(0.1);
		loops := loops + 1;
	END LOOP;
END
$$
;
----------
[ADD_INDEX] -- error
CREATE UNIQUE INDEX ON test3ci (x)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] --
-- if neither 'FOR EACH ROW' nor 'FOR EACH STATEMENT' was specified,
-- CREATE TRIGGER should default to 'FOR EACH STATEMENT'
--
CREATE TRIGGER after_upd_stmt_trig AFTER UPDATE ON main_table
EXECUTE PROCEDURE trigger_func('after_upd_stmt')
;
----------
[CREATE_PROG_OBJ] create function zero_divide() returns int as $$
declare v int := 0;
begin
  return 10 / v;
end;
$$ language plpgsql parallel safe
;
----------
[DROP_TYPE] DROP DOMAIN dint4, dint4_0
;
----------
[CREATE_PROG_OBJ] --
-- Matching citext in string comparison functions.
-- XXX TODO Ideally these would be implemented in C.
--

CREATE FUNCTION regexp_match( citext, citext ) RETURNS TEXT[] AS $$
    SELECT pg_catalog.regexp_match( $1::pg_catalog.text, $2::pg_catalog.text, 'i' );
$$ LANGUAGE SQL IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_SEQUENCE] -- Test lastval()
CREATE SEQUENCE seq
;
----------
[CREATE_TABLE] create table pph_arrpart2 partition of pph_arrpart for values with (modulus 2, remainder 1)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = numeric,
	sfunc = numeric_add,
	finalfunc = numeric_out
)
;
----------
[ALTER_PROG_OBJ] --
-- Test invalid options.
--
ALTER OPERATOR === (boolean, boolean) SET (COMMUTATOR = ====)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest28b" (
    "b" integer,
    "c" integer,
    "x" integer GENERATED ALWAYS AS (("b" * 2)) STORED
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION bt_multi_page_stats(IN relname text, IN blkno int8, IN blk_count int8,
    OUT blkno int8,
    OUT type "char",
    OUT live_items int4,
    OUT dead_items int4,
    OUT avg_item_size int4,
    OUT page_size int4,
    OUT free_size int4,
    OUT btpo_prev int8,
    OUT btpo_next int8,
    OUT btpo_level int8,
    OUT btpo_flags int4)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_multi_page_stats'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_ts_config_map
;
----------
[CREATE_TABLE] CREATE TABLE "public"."hash_i4_heap" (
    "seqno" integer,
    "random" integer
)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28272 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_backlink_set"("myname" character, "blname" character) RETURNS integer
    LANGUAGE "plpgsql"
    AS $$
declare
    mytype	char(2);
    link	char(4);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    link := mytype || substr(blname, 1, 2);
    if link = 'PLPL' then
        raise exception
		'backlink between two phone lines does not make sense';
    end if;
    if link in ('PLWS', 'WSPL') then
        raise exception
		'direct link of phone line to wall slot not permitted';
    end if;
    if mytype = 'PS' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    raise exception '% does not exist', myname;
	end if;
	if rec.backlink != blname then
	    update PSlot set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'WS' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    raise exception '% does not exist', myname;
	end if;
	if rec.backlink != blname then
	    update WSlot set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'PL' then
        select into rec * from PLine where slotname = myname;
	if not found then
	    raise exception '% does not exist', myname;
	end if;
	if rec.backlink != blname then
	    update PLine set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    raise exception 'illegal backlink beginning with %', mytype;
end;
$$
;
----------
[CREATE_PROG_OBJ] -- check defaults for variadics

create function dfunc(a variadic int[]) returns int as
$$ select array_upper($1, 1) $$ language sql
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default DROP TABLE pub_test.testpub_nopk
;
----------
[ADMIN_TABLE] ANALYZE public.rtest_person
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28084_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test2(i INT) WITH (fillfactor)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v1 AS SELECT null::int AS a
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE var_ptbl
	IS 'system_u:object_r:sepgsql_regtest_var_table_t:s0'
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE ( ftest1 int DEFAULT 0, ftest2 int DEFAULT -1, ftest3 int DEFAULT -2, ftest4 int, CONSTRAINT constrname3
			FOREIGN KEY(ftest1, ftest2, ftest3) REFERENCES PKTABLE
			ON DELETE SET NULL ON UPDATE SET DEFAULT)
;
----------
[CREATE_TABLE] -- make sure changing the column list is propagated to the catalog
CREATE TABLE testpub_tbl7 (a int primary key, b text, c text)
;
----------
[CREATE_TABLE] -- Disable batch insert
CREATE FOREIGN TABLE ftable ( x int ) SERVER loopback OPTIONS ( table_name 'batch_table', batch_size '1' )
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW user_mappings AS
    SELECT authorization_identifier,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_user_mappings
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_ine_tab AS
    SELECT 1 / 0
;
----------
[CREATE_SCHEMA] -- CREATE FOREIGN TABLE
CREATE SCHEMA foreign_schema
;
----------
[CREATE_TABLE] -- Check state of index_cleanup and truncate in VACUUM.
CREATE TABLE vac_tab_on_toast_off(i int, j text) WITH
  (autovacuum_enabled=false,
   vacuum_index_cleanup=true, toast.vacuum_index_cleanup=false,
   vacuum_truncate=true, toast.vacuum_truncate=false)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."d" INHERIT "public"."b"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."set_ttdummy"(integer) RETURNS integer
    LANGUAGE "c" STRICT
    AS '/src/src/test/regress/regress.so', 'set_ttdummy'
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_custom_rmgrs/test_custom_rmgrs--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


--
-- test_custom_rmgrs_insert_wal_record()
--
-- Writes a simple message into WAL with the help of custom WAL
-- resource manager.
--
CREATE FUNCTION test_custom_rmgrs_insert_wal_record(IN payload TEXT,
    OUT lsn pg_lsn
)
AS '$libdir/test_custom_rmgrs', 'test_custom_rmgrs_insert_wal_record'
LANGUAGE C STRICT PARALLEL UNSAFE
;
----------
[CREATE_PROG_OBJ] -- test hstore[] -> perl
CREATE FUNCTION test1arr(val hstore[]) RETURNS int
LANGUAGE plperlu
TRANSFORM FOR TYPE hstore
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_[0]->[0], $_[0]->[1]));
return scalar(keys %{$_[0]});
$$
;
----------
[CREATE_TABLE] create table rewritemetoo1 of rewritetype
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION test_pub2 FOR ALL TABLES
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_fortbl FOR TABLE testpub_view
;
----------
[CREATE_VIEW(SELECT)] -- read-only view with WHERE clause
CREATE VIEW european_city_view AS
    SELECT * FROM city_view WHERE continent = 'Europe'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbtreekey16_in(cstring)
RETURNS gbtreekey16
AS '$libdir/btree_gist', 'gbtreekey_in'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_POLICY] create rule irule1 as on insert to inserttest2 do also
  insert into inserttest (f3.if2[1], f3.if2[2])
  values (new.f1,new.f2)
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- check that the table being attached is not already a partition
ALTER TABLE list_parted2 ATTACH PARTITION part_2 FOR VALUES IN (2)
;
----------
[CREATE_TABLE] create table anothertab(f1 int primary key, f2 int unique,
                        f3 int, f4 int, f5 int)
;
