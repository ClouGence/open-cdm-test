ALTER OPERATOR <= (ismn13, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
CREATE TABLE pgss_parallel_tab (a int)
;
CREATE SCHEMA my_schema_2
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=20140 host=/tmp/pqMP6ufioD dbname=postgres' PUBLICATION tap_pub
;
analyze onerow
;
CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
-- test multi layer inheritance tree
create table inh_p1(f1 int not null)
;
DROP TABLE pg_temp_10.pg_temp_26364_2, pg_temp_10.pg_temp_26364
;
CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE (f3)
;
ANALYZE pg_catalog.pg_opclass
;
CREATE TEMP TABLE department (
	id INTEGER PRIMARY KEY,  -- department ID
	parent_department INTEGER REFERENCES department, -- upper department ID
	name TEXT -- department name
)
;
CREATE TABLE tt26(c int)
;
CREATE TYPE bogus AS ENUM('good')
;
-- obsolete:
CREATE OPERATOR @ (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contains,
	COMMUTATOR = '~',
	RESTRICT = _int_contains_sel,
	JOIN = _int_contains_joinsel
)
;
create operator public.<^ (
 leftarg = int4,
 rightarg = int4,
 procedure = int4eq
)
;
CREATE TABLE source2 (sid integer, delta integer)
  WITH (autovacuum_enabled=off)
;
-- test inheritance
create table parent (a int, b int, c int)
;
ANALYZE past_inh_db_parent
;
CREATE INDEX fooi ON foo (f1)
;
create index testpart_apple_index on testpart_apple(logdate)
;
CREATE TABLE t2 AS SELECT * FROM t1 WHERE a % 2 = 0
;
CREATE FUNCTION testprs_getlexeme(internal, internal, internal)
RETURNS internal
AS 'MODULE_PATHNAME'
LANGUAGE C STRICT
;
CREATE TABLE itest5 (a serial, b text)
;
drop table hats
;
ALTER TABLE ONLY "public"."constr_child" INHERIT "public"."constr_parent"
;
create table tt8 (x int, z int)
;
ALTER INDEX pg_toast.pg_toast_30413_index RENAME TO reind_con_toast_idx
;
-- test reporting of a simple syntax error in an extension script
CREATE FUNCTION my_okay_func(int) RETURNS int LANGUAGE SQL
AS $$ SELECT $1 + 1 $$
;
CREATE FUNCTION gin_extract_value_bool(bool, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
COMMENT ON LARGE OBJECT 33926 IS 'I Wandered Lonely as a Cloud'
;
CREATE PUBLICATION testpub3 FOR TABLE testpub_tbl3
;
CREATE TYPE "public"."person_type" AS (
	"id" integer,
	"name" "text"
)
;
CREATE RECURSIVE VIEW nums_1_100 (n) AS
    VALUES (1)
UNION ALL
    SELECT n+1 FROM nums_1_100 WHERE n < 100
;
create table i8mr_array (f1 int, f2 int8multirange[])
;
analyze t
;
create table parted_inval_tab (a int) partition by range (a)
;
-- Make a relation whose size we will under-estimate.  We want stats
-- to say 1000 rows, but actually there are 20,000 rows.
create table bigger_than_it_looks as
  select generate_series(1, 20000) as id, 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'
;
-- This will be OK, we can create the FDW
DO $d$
    BEGIN
        EXECUTE $$CREATE SERVER loopback_nopw FOREIGN DATA WRAPPER postgres_fdw
            OPTIONS (dbname '$$||current_database()||$$',
                     port '$$||current_setting('port')||$$'
            )$$;
    END;
$d$
;
CREATE FUNCTION merge_into_sq_target(sid int, balance int, delta int,
                                     OUT r_action text, OUT r_tid int, OUT r_balance int)
LANGUAGE plpgsql AS
$$
BEGIN
    MERGE INTO sq_target t
    USING (VALUES ($1, $2, $3)) AS v(sid, balance, delta)
    ON tid = v.sid
    WHEN MATCHED AND tid >= 2 THEN
        UPDATE SET balance = t.balance + v.delta
    WHEN NOT MATCHED THEN
        INSERT (balance, tid) VALUES (v.balance + v.delta, v.sid)
    WHEN MATCHED AND tid < 2 THEN
        DELETE
    RETURNING merge_action(), t.* INTO r_action, r_tid, r_balance;
END;
$$
;
ALTER TABLE errtst_parent ATTACH PARTITION errtst_child_fastdef FOR VALUES FROM (0) TO (10)
;
CREATE INDEX "gtest22c_b_idx" ON "public"."gtest22c" USING "btree" ("b")
;
ALTER TABLE pg_temp_3.pg_temp_28852_2 ADD COLUMN newdata pg_temp_3.pg_temp_28852
;
-- OK
CREATE TABLE ctas_nodata_3 (ii) AS SELECT i, j FROM ctas_base
;
ALTER TABLE pg_temp_3.pg_temp_28840_2 ADD COLUMN newdata pg_temp_3.pg_temp_28840
;
DROP TABLE range_parted4
;
CREATE FUNCTION gtest_trigger_func() RETURNS trigger
  LANGUAGE plpgsql
AS $$
BEGIN
  IF tg_op IN ('DELETE', 'UPDATE') THEN
    RAISE INFO '%: %: old = %', TG_NAME, TG_WHEN, OLD;
  END IF;
  IF tg_op IN ('INSERT', 'UPDATE') THEN
    RAISE INFO '%: %: new = %', TG_NAME, TG_WHEN, NEW;
  END IF;
  IF tg_op = 'DELETE' THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END
$$
;
create or replace function f1(inout i int) as $$
begin
  i := i+1;
end$$ language plpgsql
;
CREATE SEQUENCE "public"."rtest_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a = ANY (b)
  WITH CHECK OPTION
;
ALTER INDEX pg_toast.pg_toast_28089_index RENAME TO reind_con_toast_idx
;
------------------------------------------------------------------------------------------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ismn13, ean13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE pgss_parallel_tab (a int)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA my_schema_2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=20140 host=/tmp/pqMP6ufioD dbname=postgres' PUBLICATION tap_pub
;
----------
[ADMIN_TABLE] analyze onerow
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR = (
	PROCEDURE = isneq,
	LEFTARG = issn,
	RIGHTARG = issn13,
	COMMUTATOR = =,
	NEGATOR = <>,
	RESTRICT = eqsel,
	JOIN = eqjoinsel,
	MERGES,
	HASHES)
;
----------
[CREATE_TABLE] -- test multi layer inheritance tree
create table inh_p1(f1 int not null)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_10.pg_temp_26364_2, pg_temp_10.pg_temp_26364
;
----------
[CREATE_TABLE] CREATE TABLE gtest_parent (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE (f3)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_opclass
;
----------
[CREATE_TABLE] CREATE TEMP TABLE department (
	id INTEGER PRIMARY KEY,  -- department ID
	parent_department INTEGER REFERENCES department, -- upper department ID
	name TEXT -- department name
)
;
----------
[CREATE_TABLE] CREATE TABLE tt26(c int)
;
----------
[CREATE_TYPE] CREATE TYPE bogus AS ENUM('good')
;
----------
[CREATE_PROG_OBJ] -- obsolete:
CREATE OPERATOR @ (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contains,
	COMMUTATOR = '~',
	RESTRICT = _int_contains_sel,
	JOIN = _int_contains_joinsel
)
;
----------
[CREATE_PROG_OBJ] create operator public.<^ (
 leftarg = int4,
 rightarg = int4,
 procedure = int4eq
)
;
----------
[CREATE_TABLE] CREATE TABLE source2 (sid integer, delta integer)
  WITH (autovacuum_enabled=off)
;
----------
[CREATE_TABLE] -- test inheritance
create table parent (a int, b int, c int)
;
----------
[ADMIN_TABLE] ANALYZE past_inh_db_parent
;
----------
[ADD_INDEX] CREATE INDEX fooi ON foo (f1)
;
----------
[ADD_INDEX] create index testpart_apple_index on testpart_apple(logdate)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE t2 AS SELECT * FROM t1 WHERE a % 2 = 0
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testprs_getlexeme(internal, internal, internal)
RETURNS internal
AS 'MODULE_PATHNAME'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] CREATE TABLE itest5 (a serial, b text)
;
----------
[DROP_TABLE] drop table hats
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."constr_child" INHERIT "public"."constr_parent"
;
----------
[CREATE_TABLE] create table tt8 (x int, z int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30413_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] -- test reporting of a simple syntax error in an extension script
CREATE FUNCTION my_okay_func(int) RETURNS int LANGUAGE SQL
AS $$ SELECT $1 + 1 $$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_bool(bool, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 33926 IS 'I Wandered Lonely as a Cloud'
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub3 FOR TABLE testpub_tbl3
;
----------
[CREATE_TYPE] CREATE TYPE "public"."person_type" AS (
	"id" integer,
	"name" "text"
)
;
----------
[CREATE_VIEW(SELECT)] CREATE RECURSIVE VIEW nums_1_100 (n) AS
    VALUES (1)
UNION ALL
    SELECT n+1 FROM nums_1_100 WHERE n < 100
;
----------
[CREATE_TABLE] create table i8mr_array (f1 int, f2 int8multirange[])
;
----------
[ADMIN_TABLE] analyze t
;
----------
[CREATE_TABLE] create table parted_inval_tab (a int) partition by range (a)
;
----------
[CREATE_TABLE|SELECT] -- Make a relation whose size we will under-estimate.  We want stats
-- to say 1000 rows, but actually there are 20,000 rows.
create table bigger_than_it_looks as
  select generate_series(1, 20000) as id, 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'
;
----------
[BLOCK] -- This will be OK, we can create the FDW
DO $d$
    BEGIN
        EXECUTE $$CREATE SERVER loopback_nopw FOREIGN DATA WRAPPER postgres_fdw
            OPTIONS (dbname '$$||current_database()||$$',
                     port '$$||current_setting('port')||$$'
            )$$;
    END;
$d$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION merge_into_sq_target(sid int, balance int, delta int,
                                     OUT r_action text, OUT r_tid int, OUT r_balance int)
LANGUAGE plpgsql AS
$$
BEGIN
    MERGE INTO sq_target t
    USING (VALUES ($1, $2, $3)) AS v(sid, balance, delta)
    ON tid = v.sid
    WHEN MATCHED AND tid >= 2 THEN
        UPDATE SET balance = t.balance + v.delta
    WHEN NOT MATCHED THEN
        INSERT (balance, tid) VALUES (v.balance + v.delta, v.sid)
    WHEN MATCHED AND tid < 2 THEN
        DELETE
    RETURNING merge_action(), t.* INTO r_action, r_tid, r_balance;
END;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE errtst_parent ATTACH PARTITION errtst_child_fastdef FOR VALUES FROM (0) TO (10)
;
----------
[ADD_INDEX] CREATE INDEX "gtest22c_b_idx" ON "public"."gtest22c" USING "btree" ("b")
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_28852_2 ADD COLUMN newdata pg_temp_3.pg_temp_28852
;
----------
[CREATE_TABLE|SELECT] -- OK
CREATE TABLE ctas_nodata_3 (ii) AS SELECT i, j FROM ctas_base
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_3.pg_temp_28840_2 ADD COLUMN newdata pg_temp_3.pg_temp_28840
;
----------
[DROP_TABLE] DROP TABLE range_parted4
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtest_trigger_func() RETURNS trigger
  LANGUAGE plpgsql
AS $$
BEGIN
  IF tg_op IN ('DELETE', 'UPDATE') THEN
    RAISE INFO '%: %: old = %', TG_NAME, TG_WHEN, OLD;
  END IF;
  IF tg_op IN ('INSERT', 'UPDATE') THEN
    RAISE INFO '%: %: new = %', TG_NAME, TG_WHEN, NEW;
  END IF;
  IF tg_op = 'DELETE' THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END
$$
;
----------
[CREATE_PROG_OBJ] create or replace function f1(inout i int) as $$
begin
  i := i+1;
end$$ language plpgsql
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."rtest_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT * FROM base_tbl WHERE a = ANY (b)
  WITH CHECK OPTION
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28089_index RENAME TO reind_con_toast_idx
;
