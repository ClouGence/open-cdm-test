ALTER DATABASE "isolation_regression" SET lc_time TO 'C'
;
-- ok
DROP SEQUENCE tid_seq
;
ALTER TABLE rls_t1 FORCE ROW LEVEL SECURITY
;
ALTER OPERATOR === (boolean, real) SET (COMMUTATOR = ====)
;
DROP TABLE attmp6
;
ALTER FUNCTION gbt_oid_compress(internal) PARALLEL SAFE
;
CREATE FUNCTION _ltree_extract_isparent(_ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE INDEX freespace_brin ON freespace_tab USING brin (c1)
;
-- GiST support methods
ALTER FUNCTION gbt_oid_consistent(internal, oid, smallint, oid, internal) PARALLEL SAFE
;
-- A few simple tests for arrays of composite types

create type comptype as (f1 int, f2 text)
;
create unique index expr_key_index on insertconflicttest(lower(fruit))
;
create function cache_test(int) returns int as $$
declare total int;
begin
	create temp table t1(f1 int);
	insert into t1 values($1);
	insert into t1 values(11);
	insert into t1 values(12);
	insert into t1 values(13);
	select sum(f1) into total from t1;
	drop table t1;
	return total;
end
$$ language plpgsql
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26295_2 (tid pg_catalog.tid)
;
CREATE TABLE t ()
;
CREATE OPERATOR <-> (
	LEFTARG = int2,
	RIGHTARG = int2,
	PROCEDURE = int2_dist,
	COMMUTATOR = '<->'
)
;
create temp table yy (pkyy int, pkxx int)
;
ALTER TABLE parent_tbl ATTACH PARTITION foreign_tbl FOR VALUES IN ('AAA')
;
-- But a volatile function can see the partial results of the calling query
create or replace function max_xacttest() returns smallint language sql as
'select max(a) from xacttest' volatile
;
ALTER TABLE pitest3 ATTACH PARTITION pitest1_pfail FOR VALUES FROM ('2016-11-01') TO ('2016-12-01')
;
ALTER TABLE ONLY "generated_stored_tests"."gtest23p"
    ADD CONSTRAINT "gtest23p_pkey" PRIMARY KEY ("y")
;
DROP PROCEDURE test_ambiguous_procname(int)
;
DO LANGUAGE plpgsql $$
BEGIN
  EXECUTE 'COPY copy1 FROM ''/src/src/pl/plpgsql/src/data/copy1.data''';
END;
$$
;
create table parttmp (
  id int,
  valid_at daterange,
  exclude using gist (id with =, valid_at with &&)
) partition by range (id)
;
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD OPERATOR 1 < (int4, int4)
;
-- Token supported by the configuration's parser, succeeds.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING FOR word, word
;
-- fail
CREATE TABLE unlogged1 (a int) PARTITION BY RANGE (a)
;
ANALYZE public.hslot
;
VACUUM (ANALYZE) pg_catalog.pg_foreign_table
;
CREATE TABLE prt2_adv_p3_1 PARTITION OF prt2_adv FOR VALUES FROM (350) TO (375)
;
CREATE FUNCTION show_limit()
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
CREATE POLICY p1 ON document FOR SELECT USING (true)
;
-- if a parent has a valid not null constraint then a child table cannot
-- have an invalid one
CREATE TABLE notnull_tbl1 (a int)
;
CREATE TABLE part_m01_odd PARTITION OF part_m01
	FOR VALUES IN (1,3,5,7,9) WITH (autovacuum_enabled=off)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION pub1
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE selinto_schema.tbl_nodata2 (a) AS
  SELECT generate_series(1,3) WITH NO DATA
;
-- should fail, no elephant_bootstrap entry point
create event trigger regress_event_trigger on elephant_bootstrap
   execute procedure test_event_trigger()
;
analyze agg_data_20k
;
--
-- Tables and rules for the constraint update/delete test
--
-- Note:
-- 	Now that we have multiple action rule support, we check
-- 	both possible syntaxes to define them (The last action
--  can but must not have a semicolon at the end).
--
create table rtest_system (sysname text, sysdesc text)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."itest7e" INHERIT "public"."itest7d"
;
CREATE FUNCTION longitude(earth)
RETURNS float8
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
AS 'SELECT degrees(atan2(cube_ll_coord($1, 2), cube_ll_coord($1, 1)))'
;
CREATE TABLE vacparted_i (a int primary key, b varchar(100))
  PARTITION BY HASH (a)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) information_schema.sql_parts
;
ALTER TEXT SEARCH CONFIGURATION russian ADD MAPPING
    FOR word, hword_part, hword
	WITH russian_stem
;
CREATE POLICY "p3" ON "regress_rls_schema"."rls_tbl_force" FOR UPDATE USING (("c1" = 8)) WITH CHECK (("c1" >= 5))
;
CREATE TABLE test_recovery_conflict_table1(a int, b int)
;
do $$
declare x int not null := 42;
begin
  raise notice 'x = %', x;
  x := null;  -- fail
end$$
;
CREATE INDEX inet_idx1 ON inet_tbl(i)
;
CREATE FUNCTION "public"."test_found"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
  declare
  begin
  insert into found_test_tbl values (1);
  if FOUND then
     insert into found_test_tbl values (2);
  end if;

  update found_test_tbl set a = 100 where a = 1;
  if FOUND then
    insert into found_test_tbl values (3);
  end if;

  delete from found_test_tbl where a = 9999; -- matches no rows
  if not FOUND then
    insert into found_test_tbl values (4);
  end if;

  for i in 1 .. 10 loop
    -- no need to do anything
  end loop;
  if FOUND then
    insert into found_test_tbl values (5);
  end if;

  -- never executes the loop
  for i in 2 .. 1 loop
    -- no need to do anything
  end loop;
  if not FOUND then
    insert into found_test_tbl values (6);
  end if;
  return true;
  end;$$
;
create or replace function raise_test() returns void as $$
begin
  raise division_by_zero;
end;
$$ language plpgsql
;
CREATE FUNCTION pgstattuple(IN reloid regclass,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuplebyid'
LANGUAGE C STRICT PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[ALTER_CATALOG] ALTER DATABASE "isolation_regression" SET lc_time TO 'C'
;
----------
[DROP_SEQUENCE] -- ok
DROP SEQUENCE tid_seq
;
----------
[ALTER_TABLE] ALTER TABLE rls_t1 FORCE ROW LEVEL SECURITY
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, real) SET (COMMUTATOR = ====)
;
----------
[DROP_TABLE] DROP TABLE attmp6
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_oid_compress(internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_extract_isparent(_ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX freespace_brin ON freespace_tab USING brin (c1)
;
----------
[ALTER_PROG_OBJ] -- GiST support methods
ALTER FUNCTION gbt_oid_consistent(internal, oid, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TYPE] -- A few simple tests for arrays of composite types

create type comptype as (f1 int, f2 text)
;
----------
[ADD_INDEX] create unique index expr_key_index on insertconflicttest(lower(fruit))
;
----------
[CREATE_PROG_OBJ] create function cache_test(int) returns int as $$
declare total int;
begin
	create temp table t1(f1 int);
	insert into t1 values($1);
	insert into t1 values(11);
	insert into t1 values(12);
	insert into t1 values(13);
	select sum(f1) into total from t1;
	drop table t1;
	return total;
end
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26295_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE t ()
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = int2,
	RIGHTARG = int2,
	PROCEDURE = int2_dist,
	COMMUTATOR = '<->'
)
;
----------
[CREATE_TABLE] create temp table yy (pkyy int, pkxx int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE parent_tbl ATTACH PARTITION foreign_tbl FOR VALUES IN ('AAA')
;
----------
[CREATE_PROG_OBJ] -- But a volatile function can see the partial results of the calling query
create or replace function max_xacttest() returns smallint language sql as
'select max(a) from xacttest' volatile
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pitest3 ATTACH PARTITION pitest1_pfail FOR VALUES FROM ('2016-11-01') TO ('2016-12-01')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest23p"
    ADD CONSTRAINT "gtest23p_pkey" PRIMARY KEY ("y")
;
----------
[DROP_PROG_OBJ] DROP PROCEDURE test_ambiguous_procname(int)
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
  EXECUTE 'COPY copy1 FROM ''/src/src/pl/plpgsql/src/data/copy1.data''';
END;
$$
;
----------
[CREATE_TABLE] create table parttmp (
  id int,
  valid_at daterange,
  exclude using gist (id with =, valid_at with &&)
) partition by range (id)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf17 USING btree ADD OPERATOR 1 < (int4, int4)
;
----------
[ALTER_POLICY] -- Token supported by the configuration's parser, succeeds.
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  DROP MAPPING FOR word, word
;
----------
[CREATE_TABLE] -- fail
CREATE TABLE unlogged1 (a int) PARTITION BY RANGE (a)
;
----------
[ADMIN_TABLE] ANALYZE public.hslot
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_foreign_table
;
----------
[CREATE_TABLE] CREATE TABLE prt2_adv_p3_1 PARTITION OF prt2_adv FOR VALUES FROM (350) TO (375)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION show_limit()
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON document FOR SELECT USING (true)
;
----------
[CREATE_TABLE] -- if a parent has a valid not null constraint then a child table cannot
-- have an invalid one
CREATE TABLE notnull_tbl1 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE part_m01_odd PARTITION OF part_m01
	FOR VALUES IN (1,3,5,7,9) WITH (autovacuum_enabled=off)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13396 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TABLE|SELECT] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE selinto_schema.tbl_nodata2 (a) AS
  SELECT generate_series(1,3) WITH NO DATA
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should fail, no elephant_bootstrap entry point
create event trigger regress_event_trigger on elephant_bootstrap
   execute procedure test_event_trigger()
;
----------
[ADMIN_TABLE] analyze agg_data_20k
;
----------
[CREATE_TABLE] --
-- Tables and rules for the constraint update/delete test
--
-- Note:
-- 	Now that we have multiple action rule support, we check
-- 	both possible syntaxes to define them (The last action
--  can but must not have a semicolon at the end).
--
create table rtest_system (sysname text, sysdesc text)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."itest7e" INHERIT "public"."itest7d"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION longitude(earth)
RETURNS float8
LANGUAGE SQL
IMMUTABLE STRICT
PARALLEL SAFE
AS 'SELECT degrees(atan2(cube_ll_coord($1, 2), cube_ll_coord($1, 1)))'
;
----------
[CREATE_TABLE] CREATE TABLE vacparted_i (a int primary key, b varchar(100))
  PARTITION BY HASH (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) information_schema.sql_parts
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION russian ADD MAPPING
    FOR word, hword_part, hword
	WITH russian_stem
;
----------
[CREATE_POLICY] CREATE POLICY "p3" ON "regress_rls_schema"."rls_tbl_force" FOR UPDATE USING (("c1" = 8)) WITH CHECK (("c1" >= 5))
;
----------
[CREATE_TABLE] CREATE TABLE test_recovery_conflict_table1(a int, b int)
;
----------
[BLOCK] do $$
declare x int not null := 42;
begin
  raise notice 'x = %', x;
  x := null;  -- fail
end$$
;
----------
[ADD_INDEX] CREATE INDEX inet_idx1 ON inet_tbl(i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_found"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
  declare
  begin
  insert into found_test_tbl values (1);
  if FOUND then
     insert into found_test_tbl values (2);
  end if;

  update found_test_tbl set a = 100 where a = 1;
  if FOUND then
    insert into found_test_tbl values (3);
  end if;

  delete from found_test_tbl where a = 9999; -- matches no rows
  if not FOUND then
    insert into found_test_tbl values (4);
  end if;

  for i in 1 .. 10 loop
    -- no need to do anything
  end loop;
  if FOUND then
    insert into found_test_tbl values (5);
  end if;

  -- never executes the loop
  for i in 2 .. 1 loop
    -- no need to do anything
  end loop;
  if not FOUND then
    insert into found_test_tbl values (6);
  end if;
  return true;
  end;$$
;
----------
[CREATE_PROG_OBJ] create or replace function raise_test() returns void as $$
begin
  raise division_by_zero;
end;
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pgstattuple(IN reloid regclass,
    OUT table_len BIGINT,		-- physical table length in bytes
    OUT tuple_count BIGINT,		-- number of live tuples
    OUT tuple_len BIGINT,		-- total tuples length in bytes
    OUT tuple_percent FLOAT8,		-- live tuples in %
    OUT dead_tuple_count BIGINT,	-- number of dead tuples
    OUT dead_tuple_len BIGINT,		-- total dead tuples length in bytes
    OUT dead_tuple_percent FLOAT8,	-- dead tuples in %
    OUT free_space BIGINT,		-- free space in bytes
    OUT free_percent FLOAT8)		-- free space in %
AS '$libdir/pgstattuple', 'pgstattuplebyid'
LANGUAGE C STRICT PARALLEL SAFE
;
