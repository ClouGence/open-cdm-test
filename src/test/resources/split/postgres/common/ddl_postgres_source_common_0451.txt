create foreign table remt1 (a int, b text)
  server loopback options (table_name 'loct1')
;
DROP TABLE attmp4
;
-- cannot add NO INHERIT constraint to partitioned tables
ALTER TABLE partitioned ADD CONSTRAINT chk_a CHECK (a > 0) NO INHERIT
;
CREATE SCHEMA alice
;
-- cannot drop or alter type of partition key columns of lower level
-- partitioned tables; for example, part_5, which is list_parted2's
-- partition, is partitioned on b;
ALTER TABLE list_parted2 DROP COLUMN b
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest30_1" INHERIT "generated_virtual_tests"."gtest30"
;
/*
 * 5.4
 * CHARACTER_DATA domain
 */

CREATE DOMAIN character_data AS character varying COLLATE "C"
;
ALTER TABLE pg_subscription ADD PRIMARY KEY USING INDEX pg_subscription_oid_index
;
CREATE VIEW nontemp1 AS SELECT * FROM t1 CROSS JOIN t2
;
ALTER TABLE pg_operator ADD PRIMARY KEY USING INDEX pg_operator_oid_index
;
ANALYZE public.rule_and_refint_t3
;
--
-- OPERATORS
--

CREATE OPERATOR < (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_lt,
	COMMUTATOR = '>',
	NEGATOR = '>=',
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel
)
;
CREATE TRIGGER before_accounts_update
BEFORE UPDATE ON split_prog.accounts
FOR EACH ROW EXECUTE FUNCTION split_prog.touch_updated_at()
;
ANALYZE public.gtestxx_1
;
DROP INDEX inet_idx2
;
alter table idxpart3 drop column b, drop column c
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c8 TYPE char(10)
;
ANALYZE pg_temp_10.pg_temp_26309
;
CREATE FUNCTION plsample_call_handler() RETURNS language_handler
  AS 'MODULE_PATHNAME' LANGUAGE C
;
-- Test ispell dictionary with hunspell affix with FLAG long in configuration
ALTER TEXT SEARCH CONFIGURATION hunspell_tst ALTER MAPPING
	REPLACE hunspell WITH hunspell_long
;
-- a child matching parent
create table child1 () inherits (parent)
;
--
-- Check enforcement of array-level domain constraints
--

create domain orderedpair as int[2] check (value[1] < value[2])
;
alter type insenum add value 'i26' before 'L2'
;
ANALYZE public.itest13
;
create table mlparted_tab_part2a partition of mlparted_tab_part2 for values in ('a')
;
create rule silly as on delete to dcomptable do instead
  update dcomptable set d1.r = (d1).r - 1, d1.i = (d1).i + 1 where (d1).i > 0
;
CREATE TEMPORARY SEQUENCE rngfunc_rescan_seq2
;
create trigger trigtest_b_stmt_tg before insert or update or delete on trigtest
for each statement execute procedure trigtest()
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_aggregate
;
CREATE FUNCTION levenshtein_less_equal (text,text,int) RETURNS int
AS '$libdir/fuzzystrmatch','levenshtein_less_equal'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION is_valid(isbn13)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c4 integer
;
ALTER TABLE pg_toast.pg_toast_33324 RENAME TO reind_con_toast
;
ALTER INDEX pg_toast.pg_toast_33058_index RENAME TO reind_con_toast_idx
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
alter table parent attach partition child for values in ('AAA')
;
CREATE VIEW v2 WITH (security_barrier = true) AS
  SELECT * FROM v1 WHERE EXISTS (SELECT 1 FROM t2 WHERE t2.cc = v1.c)
  WITH CHECK OPTION
;
--
-- add information about BRIN empty ranges
--
DROP FUNCTION brin_page_items(IN page bytea, IN index_oid regclass)
;
CREATE TABLE hpart_2 PARTITION OF hash_parted FOR VALUES WITH (MODULUS 50, REMAINDER 1)
;
CREATE DATABASE regression_createdb_source
;
-- Make sure that constraints of newly-added domain columns are
-- enforced correctly, even if there's no default value for the new
-- column. Per bug #1433
create domain str_domain as text not null
;
-- okay
DROP TABLE temporal_rng4
;
CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=32633 host=/tmp/2uCjl_qJmA dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
CREATE TABLE "public"."num_exp_sqrt" (
    "id" integer,
    "expected" numeric(210,10)
)
;
CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_iISw'
;
ANALYZE public.rules_src
;
CREATE TABLE testpub_rf_tbl5 (a xml)
;
CREATE OPERATOR @ (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contains,
	COMMUTATOR = '~',
	RESTRICT = _int_contains_sel,
	JOIN = _int_contains_joinsel
)
;
CREATE VIEW rw_view AS SELECT * FROM foreign_tbl
  WHERE a < b WITH CHECK OPTION
;
DROP INDEX evttrig.one_idx
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create foreign table remt1 (a int, b text)
  server loopback options (table_name 'loct1')
;
----------
[DROP_TABLE] DROP TABLE attmp4
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- cannot add NO INHERIT constraint to partitioned tables
ALTER TABLE partitioned ADD CONSTRAINT chk_a CHECK (a > 0) NO INHERIT
;
----------
[CREATE_SCHEMA] CREATE SCHEMA alice
;
----------
[ALTER_TABLE|DROP_COLUMN] -- cannot drop or alter type of partition key columns of lower level
-- partitioned tables; for example, part_5, which is list_parted2's
-- partition, is partitioned on b;
ALTER TABLE list_parted2 DROP COLUMN b
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_virtual_tests"."gtest30_1" INHERIT "generated_virtual_tests"."gtest30"
;
----------
[CREATE_TYPE] /*
 * 5.4
 * CHARACTER_DATA domain
 */

CREATE DOMAIN character_data AS character varying COLLATE "C"
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_subscription ADD PRIMARY KEY USING INDEX pg_subscription_oid_index
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW nontemp1 AS SELECT * FROM t1 CROSS JOIN t2
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_operator ADD PRIMARY KEY USING INDEX pg_operator_oid_index
;
----------
[ADMIN_TABLE] ANALYZE public.rule_and_refint_t3
;
----------
[CREATE_PROG_OBJ] --
-- OPERATORS
--

CREATE OPERATOR < (
	LEFTARG = seg,
	RIGHTARG = seg,
	PROCEDURE = seg_lt,
	COMMUTATOR = '>',
	NEGATOR = '>=',
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER before_accounts_update
BEFORE UPDATE ON split_prog.accounts
FOR EACH ROW EXECUTE FUNCTION split_prog.touch_updated_at()
;
----------
[ADMIN_TABLE] ANALYZE public.gtestxx_1
;
----------
[DROP_INDEX] DROP INDEX inet_idx2
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart3 drop column b, drop column c
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ALTER COLUMN c8 TYPE char(10)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_10.pg_temp_26309
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION plsample_call_handler() RETURNS language_handler
  AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[ALTER_POLICY] -- Test ispell dictionary with hunspell affix with FLAG long in configuration
ALTER TEXT SEARCH CONFIGURATION hunspell_tst ALTER MAPPING
	REPLACE hunspell WITH hunspell_long
;
----------
[CREATE_TABLE] -- a child matching parent
create table child1 () inherits (parent)
;
----------
[CREATE_TYPE] --
-- Check enforcement of array-level domain constraints
--

create domain orderedpair as int[2] check (value[1] < value[2])
;
----------
[ALTER_TYPE] alter type insenum add value 'i26' before 'L2'
;
----------
[ADMIN_TABLE] ANALYZE public.itest13
;
----------
[CREATE_TABLE] create table mlparted_tab_part2a partition of mlparted_tab_part2 for values in ('a')
;
----------
[CREATE_POLICY] create rule silly as on delete to dcomptable do instead
  update dcomptable set d1.r = (d1).r - 1, d1.i = (d1).i + 1 where (d1).i > 0
;
----------
[CREATE_SEQUENCE] CREATE TEMPORARY SEQUENCE rngfunc_rescan_seq2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trigtest_b_stmt_tg before insert or update or delete on trigtest
for each statement execute procedure trigtest()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_aggregate
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION levenshtein_less_equal (text,text,int) RETURNS int
AS '$libdir/fuzzystrmatch','levenshtein_less_equal'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION is_valid(isbn13)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c4 integer
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33324 RENAME TO reind_con_toast
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33058_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = ismn13,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parent attach partition child for values in ('AAA')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v2 WITH (security_barrier = true) AS
  SELECT * FROM v1 WHERE EXISTS (SELECT 1 FROM t2 WHERE t2.cc = v1.c)
  WITH CHECK OPTION
;
----------
[DROP_PROG_OBJ] --
-- add information about BRIN empty ranges
--
DROP FUNCTION brin_page_items(IN page bytea, IN index_oid regclass)
;
----------
[CREATE_TABLE] CREATE TABLE hpart_2 PARTITION OF hash_parted FOR VALUES WITH (MODULUS 50, REMAINDER 1)
;
----------
[CREATE_CATALOG] CREATE DATABASE regression_createdb_source
;
----------
[CREATE_TYPE] -- Make sure that constraints of newly-added domain columns are
-- enforced correctly, even if there's no default value for the new
-- column. Per bug #1433
create domain str_domain as text not null
;
----------
[DROP_TABLE] -- okay
DROP TABLE temporal_rng4
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=32633 host=/tmp/2uCjl_qJmA dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_exp_sqrt" (
    "id" integer,
    "expected" numeric(210,10)
)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/src/src/test/recovery/tmp_check/tmp_test_iISw'
;
----------
[ADMIN_TABLE] ANALYZE public.rules_src
;
----------
[CREATE_TABLE] CREATE TABLE testpub_rf_tbl5 (a xml)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @ (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_contains,
	COMMUTATOR = '~',
	RESTRICT = _int_contains_sel,
	JOIN = _int_contains_joinsel
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view AS SELECT * FROM foreign_tbl
  WHERE a < b WITH CHECK OPTION
;
----------
[DROP_INDEX] DROP INDEX evttrig.one_idx
;
