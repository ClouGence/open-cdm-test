-- test interaction with set-returning functions
SAVEPOINT settings
;
COPY (UPDATE copy_stats SET b = b + 1 RETURNING *) TO STDOUT
;
SET SESSION AUTHORIZATION regress_evt_user
;
EXECUTE dumpTableAttach('39873')
;
COPY pg_catalog.pg_seclabel (objoid, classoid, objsubid, provider, label) TO stdout
;
ALTER TABLE public.gtest29 OWNER TO zyc
;
EXECUTE getDomainConstraints('35316')
;
-- Even for an empty table, CREATE INDEX checks ii_Predicate permissions.
GRANT EXECUTE ON FUNCTION s.index_row_if TO regress_minimal
;
COPY x from stdin (quote ':', quote ':')
;
EXECUTE dumpFunc('18326')
;
------------------------------------------------------------------------------------------
[TRANSACTION] -- test interaction with set-returning functions
SAVEPOINT settings
;
----------
[DATA_EXPORT|UPDATE] COPY (UPDATE copy_stats SET b = b + 1 RETURNING *) TO STDOUT
;
----------
[SWITCH_USER] SET SESSION AUTHORIZATION regress_evt_user
;
----------
[UNSAFE] EXECUTE dumpTableAttach('39873')
;
----------
[DATA_EXPORT] COPY pg_catalog.pg_seclabel (objoid, classoid, objsubid, provider, label) TO stdout
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.gtest29 OWNER TO zyc
;
----------
[UNSAFE] EXECUTE getDomainConstraints('35316')
;
----------
[GRANT] -- Even for an empty table, CREATE INDEX checks ii_Predicate permissions.
GRANT EXECUTE ON FUNCTION s.index_row_if TO regress_minimal
;
----------
[DATA_IMPORT] COPY x from stdin (quote ':', quote ':')
;
----------
[UNSAFE] EXECUTE dumpFunc('18326')
;
