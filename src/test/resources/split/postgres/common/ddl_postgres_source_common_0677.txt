DROP TABLE temporal_mltrng3
;
CREATE TABLE "public"."rtest_t2" (
    "a" integer,
    "b" integer
)
;
ALTER TABLE fkpart11.fk ADD CONSTRAINT fkey FOREIGN KEY (a) REFERENCES fkpart11.pk11 (a) ON UPDATE CASCADE ON DELETE CASCADE
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_largeobject_metadata
;
-- support functions for gin
CREATE FUNCTION gin_extract_value_trgm(text, internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
create temp table foo (f1 int, f2 int)
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_foreign_server
;
CREATE INDEX timetzidx ON timetztmp USING gist ( a )
;
CREATE POLICY p4 ON x1 FOR DELETE USING (a < 8)
;
CREATE OR REPLACE FUNCTION fn_op2(boolean, boolean)
RETURNS boolean AS $$
    SELECT NULL::BOOLEAN;
$$ LANGUAGE sql IMMUTABLE
;
drop table pp1 cascade
;
ALTER TYPE "public"."insenum" ADD VALUE 'i30'
;
CREATE TRIGGER sub1_tab1_2_2_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1_2_2
  FOR EACH ROW EXECUTE PROCEDURE sub1_trigger_activity_func()
;
--
-- partitioned table
--
CREATE TABLE testschema.part (a int) PARTITION BY LIST (a)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_authid
;
CREATE TABLE "public"."rtest_view2" (
    "a" integer
)
;
CREATE TEMP TABLE pg_temp_3.pg_temp_27281_2 (tid pg_catalog.tid)
;
create table smalltbl
        as select i as id from generate_series(1,20) i
;
create temp table t1(f1 int)
;
CREATE TABLE tab1 (a int PRIMARY KEY, b int)
;
CREATE OR REPLACE FUNCTION ctoast_lock_func(int) RETURNS INT IMMUTABLE LANGUAGE SQL AS 'select pg_advisory_xact_lock_shared(1); select $1;'
;
CREATE VIEW "public"."test_tablesample_v2" AS
 SELECT "id"
   FROM "public"."test_tablesample" TABLESAMPLE "system" (99)
;
COMMENT ON TYPE enum_test IS 'ENUM test'
;
VACUUM (FREEZE) pg_catalog.pg_publication_rel
;
DROP TABLE macaddr8_data
;
DROP TABLE gtest11
;
create table parted_conflict_test_1 partition of parted_conflict_test (b unique) for values in (1, 2)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_amop
;
drop index part_comp_key_index
;
ALTER TABLE ONLY "public"."mlparted1" DROP COLUMN "........pg.dropped.6........"
;
CREATE TABLE test_int2 (
	i int2
)
;
-- Both DELETE and UPDATE allow the specification of additional tables
-- to "join" against to determine which rows should be modified.

CREATE TEMP TABLE t1 (a int, b int)
;
-- Statistics
CREATE TABLE tab_expr_stats (a int, b int)
;
CREATE TABLE t2 AS SELECT g, g + 1 FROM generate_series(1,10) g(g)
;
ALTER TABLE clstrpart SET WITHOUT CLUSTER
;
DROP TABLE pgss_a, pgss_b CASCADE
;
ALTER TABLE pg_toast.pg_toast_30496 RENAME TO reind_con_toast
;
ANALYZE public.plt1_p3
;
create or replace function retc(int) returns two_int8s language plpgsql as
$$ begin return row($1,1); end $$
;
COMMENT ON LARGE OBJECT 32039 IS 'I Wandered Lonely as a Cloud'
;
CREATE SEQUENCE "public"."check_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_foreign_table
;
CREATE POLICY pp3 ON part_document_satire AS RESTRICTIVE
    USING (cid < 55)
;
create temp table pp (f1 text)
;
CREATE FUNCTION fn_opf15 (int4, int2) RETURNS BIGINT AS 'SELECT NULL::BIGINT;' LANGUAGE SQL
;
CREATE TABLE brintest_bloom (byteacol bytea,
	charcol "char",
	namecol name,
	int8col bigint,
	int2col smallint,
	int4col integer,
	textcol text,
	oidcol oid,
	float4col real,
	float8col double precision,
	macaddrcol macaddr,
	inetcol inet,
	cidrcol cidr,
	bpcharcol character,
	datecol date,
	timecol time without time zone,
	timestampcol timestamp without time zone,
	timestamptzcol timestamp with time zone,
	intervalcol interval,
	timetzcol time with time zone,
	numericcol numeric,
	uuidcol uuid,
	lsncol pg_lsn
) WITH (fillfactor=10)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub_mix_7, pub_mix_8
;
CREATE VIEW foo_parted_v AS SELECT *, 'xxx' AS dummy FROM foo_parted
;
CREATE TABLE gtest23q (a int PRIMARY KEY, b int REFERENCES gtest23p (y))
;
CREATE FUNCTION test_oat_schema.test_trigger()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  IF TG_OP = 'DELETE'
  THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END; $$
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE temporal_mltrng3
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_t2" (
    "a" integer,
    "b" integer
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fkpart11.fk ADD CONSTRAINT fkey FOREIGN KEY (a) REFERENCES fkpart11.pk11 (a) ON UPDATE CASCADE ON DELETE CASCADE
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_PROG_OBJ] -- support functions for gin
CREATE FUNCTION gin_extract_value_trgm(text, internal)
RETURNS internal
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] create temp table foo (f1 int, f2 int)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_foreign_server
;
----------
[ADD_INDEX] CREATE INDEX timetzidx ON timetztmp USING gist ( a )
;
----------
[CREATE_POLICY] CREATE POLICY p4 ON x1 FOR DELETE USING (a < 8)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION fn_op2(boolean, boolean)
RETURNS boolean AS $$
    SELECT NULL::BOOLEAN;
$$ LANGUAGE sql IMMUTABLE
;
----------
[DROP_TABLE] drop table pp1 cascade
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i30'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER sub1_tab1_2_2_log_op_trigger
  AFTER INSERT OR UPDATE ON tab1_2_2
  FOR EACH ROW EXECUTE PROCEDURE sub1_trigger_activity_func()
;
----------
[CREATE_TABLE] --
-- partitioned table
--
CREATE TABLE testschema.part (a int) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_authid
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_view2" (
    "a" integer
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_27281_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE|SELECT] create table smalltbl
        as select i as id from generate_series(1,20) i
;
----------
[CREATE_TABLE] create temp table t1(f1 int)
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (a int PRIMARY KEY, b int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION ctoast_lock_func(int) RETURNS INT IMMUTABLE LANGUAGE SQL AS 'select pg_advisory_xact_lock_shared(1); select $1;'
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."test_tablesample_v2" AS
 SELECT "id"
   FROM "public"."test_tablesample" TABLESAMPLE "system" (99)
;
----------
[COMMENT_TYPE] COMMENT ON TYPE enum_test IS 'ENUM test'
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_publication_rel
;
----------
[DROP_TABLE] DROP TABLE macaddr8_data
;
----------
[DROP_TABLE] DROP TABLE gtest11
;
----------
[CREATE_TABLE] create table parted_conflict_test_1 partition of parted_conflict_test (b unique) for values in (1, 2)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_amop
;
----------
[DROP_INDEX] drop index part_comp_key_index
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted1" DROP COLUMN "........pg.dropped.6........"
;
----------
[CREATE_TABLE] CREATE TABLE test_int2 (
	i int2
)
;
----------
[CREATE_TABLE] -- Both DELETE and UPDATE allow the specification of additional tables
-- to "join" against to determine which rows should be modified.

CREATE TEMP TABLE t1 (a int, b int)
;
----------
[CREATE_TABLE] -- Statistics
CREATE TABLE tab_expr_stats (a int, b int)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE t2 AS SELECT g, g + 1 FROM generate_series(1,10) g(g)
;
----------
[ALTER_TABLE] ALTER TABLE clstrpart SET WITHOUT CLUSTER
;
----------
[DROP_TABLE] DROP TABLE pgss_a, pgss_b CASCADE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30496 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] ANALYZE public.plt1_p3
;
----------
[CREATE_PROG_OBJ] create or replace function retc(int) returns two_int8s language plpgsql as
$$ begin return row($1,1); end $$
;
----------
[COMMENT_LARGE_OBJECT] COMMENT ON LARGE OBJECT 32039 IS 'I Wandered Lonely as a Cloud'
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."check_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_foreign_table
;
----------
[CREATE_POLICY] CREATE POLICY pp3 ON part_document_satire AS RESTRICTIVE
    USING (cid < 55)
;
----------
[CREATE_TABLE] create temp table pp (f1 text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn_opf15 (int4, int2) RETURNS BIGINT AS 'SELECT NULL::BIGINT;' LANGUAGE SQL
;
----------
[CREATE_TABLE] CREATE TABLE brintest_bloom (byteacol bytea,
	charcol "char",
	namecol name,
	int8col bigint,
	int2col smallint,
	int4col integer,
	textcol text,
	oidcol oid,
	float4col real,
	float8col double precision,
	macaddrcol macaddr,
	inetcol inet,
	cidrcol cidr,
	bpcharcol character,
	datecol date,
	timecol time without time zone,
	timestampcol timestamp without time zone,
	timestamptzcol timestamp with time zone,
	intervalcol interval,
	timetzcol time with time zone,
	numericcol numeric,
	uuidcol uuid,
	lsncol pg_lsn
) WITH (fillfactor=10)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub_mix_7, pub_mix_8
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW foo_parted_v AS SELECT *, 'xxx' AS dummy FROM foo_parted
;
----------
[CREATE_TABLE] CREATE TABLE gtest23q (a int PRIMARY KEY, b int REFERENCES gtest23p (y))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_oat_schema.test_trigger()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  IF TG_OP = 'DELETE'
  THEN
    RETURN OLD;
  ELSE
    RETURN NEW;
  END IF;
END; $$
;
