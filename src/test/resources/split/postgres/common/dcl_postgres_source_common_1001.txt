-- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
EXECUTE dumpFunc('40942')
;
COPY pg_catalog.pg_event_trigger (oid, evtname, evtevent, evtowner, evtfoid, evtenabled, evttags) TO stdout
;
EXECUTE dumpTableAttach('43685')
;
SET SESSION AUTHORIZATION regress_constraint_comments
;
copy copytest3 to stdout csv header
;
EXECUTE dumpOpr('352')
;
EXECUTE dumpRangeType('16656')
;
EXECUTE dumpFunc('41583')
;
ALTER FUNCTION "public"."trans_updatetrigfunc"() OWNER TO zyc
;
------------------------------------------------------------------------------------------
[SESSION_SETTING_WRITE] -- Language-specific snowball dictionaries
/*
 * Create underlying C functions for Snowball stemmers
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball_func.sql.in
 *
 * This file is combined with multiple instances of snowball.sql.in to
 * build snowball_create.sql, which is executed during initdb.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

SET search_path = pg_catalog
;
----------
[UNSAFE] EXECUTE dumpFunc('40942')
;
----------
[DATA_EXPORT] COPY pg_catalog.pg_event_trigger (oid, evtname, evtevent, evtowner, evtfoid, evtenabled, evttags) TO stdout
;
----------
[UNSAFE] EXECUTE dumpTableAttach('43685')
;
----------
[SWITCH_USER] SET SESSION AUTHORIZATION regress_constraint_comments
;
----------
[DATA_EXPORT] copy copytest3 to stdout csv header
;
----------
[UNSAFE] EXECUTE dumpOpr('352')
;
----------
[UNSAFE] EXECUTE dumpRangeType('16656')
;
----------
[UNSAFE] EXECUTE dumpFunc('41583')
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."trans_updatetrigfunc"() OWNER TO zyc
;
