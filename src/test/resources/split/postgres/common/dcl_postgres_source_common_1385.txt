CREATE STATISTICS ctlt1_expr_stat ON (a || b) FROM ctlt1
;
EXECUTE dumpFunc('16462')
;
REINDEX TABLE CONCURRENTLY information_schema.sql_parts
;
EXECUTE dumpOpr('1130')
;
-- fail without permissions on the table referenced by the view
BEGIN
;
CREATE STATISTICS mcv_lists_stats_3 ON (mod(c,5)) FROM mcv_lists
;
EXECUTE dumpOpr('665')
;
LOCK TABLE public.shoelace_arrive IN ACCESS SHARE MODE
;
ALTER TABLE regress_indexing.pk1 OWNER TO zyc
;
-- Test REASSIGN OWNED
GRANT ALL ON deptest1 TO regress_dep_user1
;
------------------------------------------------------------------------------------------
[ADMIN_PERFORMANCE] CREATE STATISTICS ctlt1_expr_stat ON (a || b) FROM ctlt1
;
----------
[UNSAFE] EXECUTE dumpFunc('16462')
;
----------
[ADMIN] REINDEX TABLE CONCURRENTLY information_schema.sql_parts
;
----------
[UNSAFE] EXECUTE dumpOpr('1130')
;
----------
[TRANSACTION] -- fail without permissions on the table referenced by the view
BEGIN
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS mcv_lists_stats_3 ON (mod(c,5)) FROM mcv_lists
;
----------
[UNSAFE] EXECUTE dumpOpr('665')
;
----------
[SESSION_LOCK] LOCK TABLE public.shoelace_arrive IN ACCESS SHARE MODE
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE regress_indexing.pk1 OWNER TO zyc
;
----------
[GRANT] -- Test REASSIGN OWNED
GRANT ALL ON deptest1 TO regress_dep_user1
;
