-- add an inherited NOT VALID constraint
alter table nv_parent add check (d between '2001-01-01'::date and '2099-12-31'::date) not valid
;
CREATE EXTENSION pg_walinspect
;
CREATE INDEX users_table_part_idx ON users_table_part(user_id, value_1)
;
--
-- test FK referenced updates RESTRICT
--

TRUNCATE temporal_mltrng, temporal_fk_mltrng2mltrng
;
CREATE VIEW rw_view AS SELECT * FROM parent_tbl
  WHERE a < b WITH CHECK OPTION
;
--
-- Test unique index with included columns
--
CREATE TABLE covering_index_heap (f1 int, f2 int, f3 text)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_config
;
ALTER EXTENSION test_pg_dump ADD MATERIALIZED VIEW test_pg_dump_mv1
;
create table idxpart0 (like idxpart)
;
drop table parted_validate_test
;
ALTER TABLE pg_toast.pg_toast_27702 RENAME TO reind_con_toast
;
CREATE FUNCTION "public"."namedparmcursor_test2"(integer, integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
    nonsense record;
begin
    open c1(param1 := $1, $2);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$_$
;
COMMENT ON FUNCTION cube_ne(cube, cube) IS 'different'
;
CREATE INDEX accounts_owner_trgm_idx ON split_prog.accounts USING gin (owner_name gin_trgm_ops)
;
-- make the function parallel safe
CREATE OR REPLACE FUNCTION distinct_func(a INT) RETURNS INT AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL SAFE
;
CREATE INDEX t1_gist ON s3.t1 USING GIST (b)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_attribute
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
DROP TABLE ctas_stats_2
;
DROP DOMAIN queryfuncs_d_varbit3
;
CREATE EXTENSION test_custom_ext_paths
;
-- fail
create table child (c text collate "POSIX") inherits (parent)
;
CREATE VIEW ro_view8 AS SELECT a, b FROM base_tbl ORDER BY a OFFSET 1
;
COMMENT ON TEXT SEARCH CONFIGURATION irish IS 'configuration for irish language'
;
ALTER PUBLICATION pub ADD TABLE sch1.tbl1
;
CREATE VIEW regtest_view_2 AS SELECT * FROM regtest_table_3 WHERE x < y
;
create index spgist_domain_idx on spgist_domain_tbl using spgist(f1)
;
CREATE TABLE "public"."dropped_objects" (
    "type" "text",
    "schema" "text",
    "object" "text"
)
;
CREATE TABLE brintest_3 (a text, b text, c text, d text)
;
DROP TABLE temporal_fk2_rng2rng
;
CREATE TABLE part (
	a int
) PARTITION BY RANGE (a)
;
ALTER TABLE ONLY "public"."sj_t2" ALTER COLUMN "id" SET DEFAULT "nextval"('"public"."sj_t2_id_seq"'::"regclass")
;
drop type textrange2
;
-- drop the trigger, and now we're allowed to make it inherit again
drop trigger child_row_trig on child
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.datetime()'))
;
CREATE INDEX "onek2_stu1_prtl" ON "public"."onek2" USING "btree" ("stringu1") WHERE (("stringu1" >= 'J'::"name") AND ("stringu1" < 'K'::"name"))
;
CREATE SCHEMA s5
;
CREATE OPERATOR schema_op1.#*# (
   leftarg = int8,		-- right unary
   procedure = factorial
)
;
/*
 * 5.16
 * COLLATION_CHARACTER_SET_APPLICABILITY view
 */

CREATE VIEW collation_character_set_applicability AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
create index test_partitioned_index on test_partitioned(a)
;
create function ttdummy () returns trigger language plpgsql as
$$ begin return new; end $$
;
CREATE COLLATION builtin2 ( PROVIDER = builtin, LC_CTYPE = "C", LC_COLLATE = "C" )
;
ALTER TABLE regtest_ptable_1_ones SET SCHEMA regtest_schema_2
;
ALTER FUNCTION gbt_timetz_consistent(internal, time with time zone, smallint, oid, internal) PARALLEL SAFE
;
CREATE TYPE planets AS ENUM ( 'venus', 'earth', 'mars' )
;
DROP FUNCTION g_cube_compress(pg_catalog.internal)
;
CREATE TABLE pht2_p3 PARTITION OF pht2 FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
ALTER TABLE pg_toast.pg_toast_27767 RENAME TO reind_con_toast
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=16002 host=/tmp/B3ysgEyCZr dbname=postgres' PUBLICATION pub1
;
create temp table q21 partition of q2 for values in (1)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_CONSTRAINT] -- add an inherited NOT VALID constraint
alter table nv_parent add check (d between '2001-01-01'::date and '2099-12-31'::date) not valid
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pg_walinspect
;
----------
[ADD_INDEX] CREATE INDEX users_table_part_idx ON users_table_part(user_id, value_1)
;
----------
[TRUNCATE_TABLE] --
-- test FK referenced updates RESTRICT
--

TRUNCATE temporal_mltrng, temporal_fk_mltrng2mltrng
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view AS SELECT * FROM parent_tbl
  WHERE a < b WITH CHECK OPTION
;
----------
[CREATE_TABLE] --
-- Test unique index with included columns
--
CREATE TABLE covering_index_heap (f1 int, f2 int, f3 text)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_config
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD MATERIALIZED VIEW test_pg_dump_mv1
;
----------
[CREATE_TABLE] create table idxpart0 (like idxpart)
;
----------
[DROP_TABLE] drop table parted_validate_test
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27702 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."namedparmcursor_test2"(integer, integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
declare
    c1 cursor (param1 int, param2 int) for select * from rc_test where a > param1 and b > param2;
    nonsense record;
begin
    open c1(param1 := $1, $2);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$_$
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION cube_ne(cube, cube) IS 'different'
;
----------
[ADD_INDEX] CREATE INDEX accounts_owner_trgm_idx ON split_prog.accounts USING gin (owner_name gin_trgm_ops)
;
----------
[CREATE_PROG_OBJ] -- make the function parallel safe
CREATE OR REPLACE FUNCTION distinct_func(a INT) RETURNS INT AS $$
  BEGIN
    RETURN a;
  END;
$$ LANGUAGE plpgsql PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX t1_gist ON s3.t1 USING GIST (b)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_attribute
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
----------
[DROP_TABLE] DROP TABLE ctas_stats_2
;
----------
[DROP_TYPE] DROP DOMAIN queryfuncs_d_varbit3
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_custom_ext_paths
;
----------
[CREATE_TABLE] -- fail
create table child (c text collate "POSIX") inherits (parent)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view8 AS SELECT a, b FROM base_tbl ORDER BY a OFFSET 1
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION irish IS 'configuration for irish language'
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION pub ADD TABLE sch1.tbl1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW regtest_view_2 AS SELECT * FROM regtest_table_3 WHERE x < y
;
----------
[ADD_INDEX] create index spgist_domain_idx on spgist_domain_tbl using spgist(f1)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."dropped_objects" (
    "type" "text",
    "schema" "text",
    "object" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE brintest_3 (a text, b text, c text, d text)
;
----------
[DROP_TABLE] DROP TABLE temporal_fk2_rng2rng
;
----------
[CREATE_TABLE] CREATE TABLE part (
	a int
) PARTITION BY RANGE (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE ONLY "public"."sj_t2" ALTER COLUMN "id" SET DEFAULT "nextval"('"public"."sj_t2_id_seq"'::"regclass")
;
----------
[DROP_TYPE] drop type textrange2
;
----------
[DROP_TRIGGER] -- drop the trigger, and now we're allowed to make it inherit again
drop trigger child_row_trig on child
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.datetime()'))
;
----------
[ADD_INDEX] CREATE INDEX "onek2_stu1_prtl" ON "public"."onek2" USING "btree" ("stringu1") WHERE (("stringu1" >= 'J'::"name") AND ("stringu1" < 'K'::"name"))
;
----------
[CREATE_SCHEMA] CREATE SCHEMA s5
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR schema_op1.#*# (
   leftarg = int8,		-- right unary
   procedure = factorial
)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 5.16
 * COLLATION_CHARACTER_SET_APPLICABILITY view
 */

CREATE VIEW collation_character_set_applicability AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(getdatabaseencoding() AS sql_identifier) AS character_set_name
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
----------
[ADD_INDEX] create index test_partitioned_index on test_partitioned(a)
;
----------
[CREATE_PROG_OBJ] create function ttdummy () returns trigger language plpgsql as
$$ begin return new; end $$
;
----------
[CREATE_POLICY] CREATE COLLATION builtin2 ( PROVIDER = builtin, LC_CTYPE = "C", LC_COLLATE = "C" )
;
----------
[ALTER_TABLE] ALTER TABLE regtest_ptable_1_ones SET SCHEMA regtest_schema_2
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_timetz_consistent(internal, time with time zone, smallint, oid, internal) PARALLEL SAFE
;
----------
[CREATE_TYPE] CREATE TYPE planets AS ENUM ( 'venus', 'earth', 'mars' )
;
----------
[DROP_PROG_OBJ] DROP FUNCTION g_cube_compress(pg_catalog.internal)
;
----------
[CREATE_TABLE] CREATE TABLE pht2_p3 PARTITION OF pht2 FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27767 RENAME TO reind_con_toast
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=16002 host=/tmp/B3ysgEyCZr dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TABLE] create temp table q21 partition of q2 for values in (1)
;
