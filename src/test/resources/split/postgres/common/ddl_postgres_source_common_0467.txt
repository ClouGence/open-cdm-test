ALTER TABLE pg_toast.pg_toast_27835 RENAME TO reind_con_toast
;
create unlogged table pgbench_accounts_1
  partition of pgbench_accounts
  for values from (minvalue) to (33335) with (fillfactor=100)
;
ALTER INDEX pg_toast.pg_toast_30477_index RENAME TO reind_con_toast_idx
;
DROP TABLESPACE regress_ts3
;
CREATE TABLE import_source.t3 (c1 timestamptz default now(), c2 typ1)
;
CREATE TRIGGER my_trigger AFTER UPDATE ON multi_test
  REFERENCING NEW TABLE AS new_test OLD TABLE as old_test
  FOR EACH STATEMENT EXECUTE PROCEDURE multi_test_trig()
;
ANALYZE public.main_table
;
CREATE AGGREGATE testns.agg1(int) (sfunc = int4pl, stype = int4)
;
ALTER TABLE testschema.test_tab ADD CONSTRAINT test_tab_unique UNIQUE (id)
;
create table p11 (like p1)
;
COMMENT ON INDEX six IS 'good index'
;
CREATE INDEX brinidx ON brin_iso USING brin (value) WITH (pages_per_range=1)
;
CREATE TABLE fk3 PARTITION OF fk FOR VALUES FROM (3500) TO (5000)
;
alter table atacc3 add test2 int
;
create trigger tg after insert on child1
  for each row execute function trig_nothing()
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_shseclabel
;
-- should fail, doesn't exist any more
drop event trigger regress_event_trigger
;
-- Not OK more than one equal column: partition keys are a proper superset of constraint
create table idxpart (a int4range, b int4range) partition by range (a, b)
;
CREATE TEMP VIEW fdv4 AS
SELECT * FROM articles WHERE title IN (SELECT title FROM articles GROUP BY id)
;
-- rules
CREATE RULE r1 AS ON INSERT TO pg_description DO INSTEAD NOTHING
;
CREATE POLICY seeall ON range_parted AS PERMISSIVE FOR SELECT USING (true)
;
--
-- Test the SP-GiST index on the larger volume of data
--
CREATE TABLE quad_box_tbl (b box)
;
ANALYZE public.part33_self_fk
;
CREATE TABLESPACE tblspc1 LOCATION '/tmp/BquxA6nmQA/tempdir/tblspc1'
;
CREATE SCHEMA schema_rf_x
;
CREATE TABLE "public"."gtest0" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (55) STORED
)
;
CREATE POLICY p1 ON rls_test_permissive USING (data % 2 = 0)
;
alter table idxpart drop column drop_3
;
CREATE FUNCTION buffer_call_start_io(buffer int, for_input bool, nowait bool)
RETURNS pg_catalog.bool STRICT
AS '$libdir/test_aio' LANGUAGE C
;
VACUUM (ANALYZE) pg_catalog.pg_largeobject
;
ALTER TABLE delete_test_table ADD PRIMARY KEY (a,b,c,d)
;
ANALYZE pg_temp_32.pg_temp_28302
;
CREATE INDEX test_index2 ON testschema.test_default_tab USING btree (id) TABLESPACE regress_tblspace
;
-- inlining of set-returning functions

CREATE TABLE functest3 (a int)
;
-- PROVIDER
CREATE COLLATION coll_dup_chk (PROVIDER = icu, PROVIDER = NONSENSE, LC_COLLATE = "POSIX", LC_CTYPE = "POSIX")
;
-- check detach behavior
create trigger trg1 after insert on trigpart for each row execute procedure trigger_nothing()
;
CREATE FUNCTION spgist_name_inner_consistent(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS '$libdir/spgist_name_ops' LANGUAGE C
;
DROP TABLE another_parttbl
;
CREATE FUNCTION test_dsa_basic()
	RETURNS pg_catalog.void
	AS '$libdir/test_dsa' LANGUAGE C
;
VACUUM ANALYZE num_exp_sqrt
;
ALTER TABLE tt1 OF tt_t0
;
CREATE TABLE test_like_5 (x point, y point, z point)
;
ALTER TABLE attmp ADD COLUMN l tid
;
CREATE TEMP TABLE pg_temp_4.pg_temp_37940_2 (tid pg_catalog.tid)
;
do $$
declare x int_nn;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
CREATE DOMAIN split_cov.positive_int AS integer CHECK (VALUE > 0)
;
ALTER TABLE testschema.test_default_tab_p ALTER val TYPE bigint
;
create table hash_parted (
	a int
) partition by hash (a part_test_int4_ops)
;
CREATE TABLE test_uuid (
	i uuid
)
;
alter table b_star reset (parallel_workers)
;
------------------------------------------------------------------------------------------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27835 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] create unlogged table pgbench_accounts_1
  partition of pgbench_accounts
  for values from (minvalue) to (33335) with (fillfactor=100)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30477_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLESPACE] DROP TABLESPACE regress_ts3
;
----------
[CREATE_TABLE] CREATE TABLE import_source.t3 (c1 timestamptz default now(), c2 typ1)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER my_trigger AFTER UPDATE ON multi_test
  REFERENCING NEW TABLE AS new_test OLD TABLE as old_test
  FOR EACH STATEMENT EXECUTE PROCEDURE multi_test_trig()
;
----------
[ADMIN_TABLE] ANALYZE public.main_table
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testns.agg1(int) (sfunc = int4pl, stype = int4)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testschema.test_tab ADD CONSTRAINT test_tab_unique UNIQUE (id)
;
----------
[CREATE_TABLE] create table p11 (like p1)
;
----------
[COMMENT_INDEX] COMMENT ON INDEX six IS 'good index'
;
----------
[ADD_INDEX] CREATE INDEX brinidx ON brin_iso USING brin (value) WITH (pages_per_range=1)
;
----------
[CREATE_TABLE] CREATE TABLE fk3 PARTITION OF fk FOR VALUES FROM (3500) TO (5000)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table atacc3 add test2 int
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg after insert on child1
  for each row execute function trig_nothing()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_shseclabel
;
----------
[DROP_TRIGGER] -- should fail, doesn't exist any more
drop event trigger regress_event_trigger
;
----------
[CREATE_TABLE] -- Not OK more than one equal column: partition keys are a proper superset of constraint
create table idxpart (a int4range, b int4range) partition by range (a, b)
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW fdv4 AS
SELECT * FROM articles WHERE title IN (SELECT title FROM articles GROUP BY id)
;
----------
[CREATE_POLICY] -- rules
CREATE RULE r1 AS ON INSERT TO pg_description DO INSTEAD NOTHING
;
----------
[CREATE_POLICY] CREATE POLICY seeall ON range_parted AS PERMISSIVE FOR SELECT USING (true)
;
----------
[CREATE_TABLE] --
-- Test the SP-GiST index on the larger volume of data
--
CREATE TABLE quad_box_tbl (b box)
;
----------
[ADMIN_TABLE] ANALYZE public.part33_self_fk
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc1 LOCATION '/tmp/BquxA6nmQA/tempdir/tblspc1'
;
----------
[CREATE_SCHEMA] CREATE SCHEMA schema_rf_x
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest0" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (55) STORED
)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON rls_test_permissive USING (data % 2 = 0)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table idxpart drop column drop_3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION buffer_call_start_io(buffer int, for_input bool, nowait bool)
RETURNS pg_catalog.bool STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_largeobject
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE delete_test_table ADD PRIMARY KEY (a,b,c,d)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28302
;
----------
[ADD_INDEX] CREATE INDEX test_index2 ON testschema.test_default_tab USING btree (id) TABLESPACE regress_tblspace
;
----------
[CREATE_TABLE] -- inlining of set-returning functions

CREATE TABLE functest3 (a int)
;
----------
[CREATE_POLICY] -- PROVIDER
CREATE COLLATION coll_dup_chk (PROVIDER = icu, PROVIDER = NONSENSE, LC_COLLATE = "POSIX", LC_CTYPE = "POSIX")
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- check detach behavior
create trigger trg1 after insert on trigpart for each row execute procedure trigger_nothing()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION spgist_name_inner_consistent(internal, internal)
RETURNS void IMMUTABLE PARALLEL SAFE STRICT
AS '$libdir/spgist_name_ops' LANGUAGE C
;
----------
[DROP_TABLE] DROP TABLE another_parttbl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_dsa_basic()
	RETURNS pg_catalog.void
	AS '$libdir/test_dsa' LANGUAGE C
;
----------
[ADMIN_TABLE] VACUUM ANALYZE num_exp_sqrt
;
----------
[ALTER_TABLE] ALTER TABLE tt1 OF tt_t0
;
----------
[CREATE_TABLE] CREATE TABLE test_like_5 (x point, y point, z point)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN l tid
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_37940_2 (tid pg_catalog.tid)
;
----------
[BLOCK] do $$
declare x int_nn;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[CREATE_TYPE] CREATE DOMAIN split_cov.positive_int AS integer CHECK (VALUE > 0)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE testschema.test_default_tab_p ALTER val TYPE bigint
;
----------
[CREATE_TABLE] create table hash_parted (
	a int
) partition by hash (a part_test_int4_ops)
;
----------
[CREATE_TABLE] CREATE TABLE test_uuid (
	i uuid
)
;
----------
[ALTER_TABLE] alter table b_star reset (parallel_workers)
;
