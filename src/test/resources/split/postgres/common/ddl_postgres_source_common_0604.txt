CREATE UNIQUE INDEX bttest_unique_idx2
						ON bttest_unique2
						USING btree (i int4_custom_ops2)
						WITH (deduplicate_items = off)
;
ALTER DOMAIN split_domain_type_next.email_domain VALIDATE CONSTRAINT email_length_chk
;
-- Partitioned table at referenced end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
  PARTITION BY LIST(f1)
;
VACUUM (PARALLEL 0) pg_catalog.pg_init_privs
;
drop table tab_batch_sharded_p1_remote
;
CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc4(1) WITH ORDINALITY AS t1(a,b,c,o)
;
DROP INDEX t_b_partial_1_idx
;
CREATE FUNCTION isneq(issn, issn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ANALYZE prt
;
ALTER TABLE pg_toast.pg_toast_33519 RENAME TO reind_con_toast
;
CREATE TABLE gtest3 (a int, b int GENERATED ALWAYS AS (a * 3) STORED)
;
DROP TABLE IF EXISTS reind_con_wide
;
CALL ptest6b(1, null, null)
;
create table parted_isvalid_tab_1 partition of parted_isvalid_tab
  for values from (1) to (10) partition by range (a)
;
CREATE TABLE test_date (
	i date
)
;
ALTER OPERATOR ^@ (ltxtquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
drop table users
;
-- triggers

TRUNCATE TABLE y
;
-- statistics on expressions with different data types
CREATE TABLE expr_stats (a int, b name, c text)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub2, pub3
;
CREATE FUNCTION hashissn13(issn13)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_30805_index RENAME TO reind_con_toast_idx
;
CREATE VIEW pg_stat_io AS
SELECT
       b.backend_type,
       b.object,
       b.context,
       b.reads,
       b.read_time,
       b.writes,
       b.write_time,
       b.writebacks,
       b.writeback_time,
       b.extends,
       b.extend_time,
       b.op_bytes,
       b.hits,
       b.evictions,
       b.reuses,
       b.fsyncs,
       b.fsync_time,
       b.stats_reset
FROM pg_stat_get_io() b
;
create table pp_enumpart_blue partition of pp_enumpart for values in ('blue')
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER TABLE pg_temp_8.z ADD CONSTRAINT z_pkey PRIMARY KEY (aa, b)
;
CREATE FUNCTION subltree(ltree,int4,int4)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER PROCEDURE nonexistent() VOLATILE
;
CREATE UNIQUE INDEX "regress_constr_partitioned_a_idx" ON ONLY "public"."regress_constr_partitioned" USING "btree" ("a")
;
TRUNCATE gtest3
;
-- succeeds, inserts domain default
-- We used to treat SET DEFAULT NULL as equivalent to DROP DEFAULT; wrong
alter table defaulttest alter column col5 set default null
;
DO LANGUAGE plpgsql $$
BEGIN
  COPY copy1 FROM stdin;
END;
$$
;
CREATE SCHEMA pub_test1
;
ALTER SUBSCRIPTION tsub SET (binary = false)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_statistic
;
CREATE TABLE dump_test_second_schema.measurement_y2006m3
						PARTITION OF dump_test.measurement
						FOR VALUES FROM ('2006-03-01') TO ('2006-04-01')
;
-- bit check

CREATE TABLE bittmp (a bit(33))
;
ALTER TABLE has_volatile ADD col4 int DEFAULT (random() * 10000)::int
;
ALTER TABLE pg_db_role_setting ADD PRIMARY KEY USING INDEX pg_db_role_setting_databaseid_rol_index
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26162_2 (tid pg_catalog.tid)
;
ALTER TABLE pg_temp_11.pg_temp_26330_2 ADD COLUMN newdata pg_temp_11.pg_temp_26330
;
ALTER TABLE pg_toast.pg_toast_33190 RENAME TO reind_con_toast
;
-- Check compatibility for foreign keys and comments. This is done
-- separately as rebuilding the column type of the parent leads
-- to an error and would reduce the test scope.
CREATE TABLE comment_test_child (
  id text CONSTRAINT comment_test_child_fk REFERENCES comment_test)
;
comment on column schema1.table_name.column1 is 'ss'
;
CREATE UNLOGGED TABLE "public"."spgist_unlogged_tbl" (
    "id" integer NOT NULL,
    "b" "box"
)
;
CREATE TABLE "public"."alpha_pos_p3" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
do $$
declare
  x int;
  y int;
begin
  select 1 into x, y;
  select 1,2 into x, y;
  select 1,2,3 into x, y;
end
$$
;
CREATE OR REPLACE FUNCTION compare_relfilenode_part(tabname text)
  RETURNS TABLE (relname name, relkind "char", state text) AS
  $func$
  BEGIN
    RETURN QUERY EXECUTE
      format(
        'SELECT  b.relname,
                 b.relkind,
                 CASE WHEN a.relfilenode = b.relfilenode THEN ''relfilenode is unchanged''
                 ELSE ''relfilenode has changed'' END
           -- Do not join with OID here as CONCURRENTLY changes it.
           FROM %I b JOIN pg_class a ON b.relname = a.relname
           ORDER BY 1;', tabname);
  END
  $func$ LANGUAGE plpgsql
;
CREATE SCHEMA testns2
;
DROP TABLE pg_temp_18.pg_temp_25732_2, pg_temp_18.pg_temp_25732
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE UNIQUE INDEX bttest_unique_idx2
						ON bttest_unique2
						USING btree (i int4_custom_ops2)
						WITH (deduplicate_items = off)
;
----------
[ALTER_TYPE] ALTER DOMAIN split_domain_type_next.email_domain VALIDATE CONSTRAINT email_length_chk
;
----------
[CREATE_TABLE] -- Partitioned table at referenced end
CREATE TABLE pt(f1 int, f2 int, f3 int, PRIMARY KEY(f1,f2))
  PARTITION BY LIST(f1)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_init_privs
;
----------
[DROP_TABLE] drop table tab_batch_sharded_p1_remote
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW vw_getrngfunc AS SELECT * FROM getrngfunc4(1) WITH ORDINALITY AS t1(a,b,c,o)
;
----------
[DROP_INDEX] DROP INDEX t_b_partial_1_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(issn, issn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE prt
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33519 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE gtest3 (a int, b int GENERATED ALWAYS AS (a * 3) STORED)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS reind_con_wide
;
----------
[CALL_PROG_OBJ] CALL ptest6b(1, null, null)
;
----------
[CREATE_TABLE] create table parted_isvalid_tab_1 partition of parted_isvalid_tab
  for values from (1) to (10) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE test_date (
	i date
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^@ (ltxtquery, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[DROP_TABLE] drop table users
;
----------
[TRUNCATE_TABLE] -- triggers

TRUNCATE TABLE y
;
----------
[CREATE_TABLE] -- statistics on expressions with different data types
CREATE TABLE expr_stats (a int, b name, c text)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub2, pub3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hashissn13(issn13)
	RETURNS int4
	AS 'hashint8'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30805_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_io AS
SELECT
       b.backend_type,
       b.object,
       b.context,
       b.reads,
       b.read_time,
       b.writes,
       b.write_time,
       b.writebacks,
       b.writeback_time,
       b.extends,
       b.extend_time,
       b.op_bytes,
       b.hits,
       b.evictions,
       b.reuses,
       b.fsyncs,
       b.fsync_time,
       b.stats_reset
FROM pg_stat_get_io() b
;
----------
[CREATE_TABLE] create table pp_enumpart_blue partition of pp_enumpart for values in ('blue')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ean13,
	RIGHTARG = issn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_temp_8.z ADD CONSTRAINT z_pkey PRIMARY KEY (aa, b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION subltree(ltree,int4,int4)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER PROCEDURE nonexistent() VOLATILE
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "regress_constr_partitioned_a_idx" ON ONLY "public"."regress_constr_partitioned" USING "btree" ("a")
;
----------
[TRUNCATE_TABLE] TRUNCATE gtest3
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- succeeds, inserts domain default
-- We used to treat SET DEFAULT NULL as equivalent to DROP DEFAULT; wrong
alter table defaulttest alter column col5 set default null
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
  COPY copy1 FROM stdin;
END;
$$
;
----------
[CREATE_SCHEMA] CREATE SCHEMA pub_test1
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tsub SET (binary = false)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_statistic
;
----------
[CREATE_TABLE] CREATE TABLE dump_test_second_schema.measurement_y2006m3
						PARTITION OF dump_test.measurement
						FOR VALUES FROM ('2006-03-01') TO ('2006-04-01')
;
----------
[CREATE_TABLE] -- bit check

CREATE TABLE bittmp (a bit(33))
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE has_volatile ADD col4 int DEFAULT (random() * 10000)::int
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_db_role_setting ADD PRIMARY KEY USING INDEX pg_db_role_setting_databaseid_rol_index
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26162_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_11.pg_temp_26330_2 ADD COLUMN newdata pg_temp_11.pg_temp_26330
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33190 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] -- Check compatibility for foreign keys and comments. This is done
-- separately as rebuilding the column type of the parent leads
-- to an error and would reduce the test scope.
CREATE TABLE comment_test_child (
  id text CONSTRAINT comment_test_child_fk REFERENCES comment_test)
;
----------
[COMMENT_COLUMN] comment on column schema1.table_name.column1 is 'ss'
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE "public"."spgist_unlogged_tbl" (
    "id" integer NOT NULL,
    "b" "box"
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."alpha_pos_p3" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[BLOCK] do $$
declare
  x int;
  y int;
begin
  select 1 into x, y;
  select 1,2 into x, y;
  select 1,2,3 into x, y;
end
$$
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION compare_relfilenode_part(tabname text)
  RETURNS TABLE (relname name, relkind "char", state text) AS
  $func$
  BEGIN
    RETURN QUERY EXECUTE
      format(
        'SELECT  b.relname,
                 b.relkind,
                 CASE WHEN a.relfilenode = b.relfilenode THEN ''relfilenode is unchanged''
                 ELSE ''relfilenode has changed'' END
           -- Do not join with OID here as CONCURRENTLY changes it.
           FROM %I b JOIN pg_class a ON b.relname = a.relname
           ORDER BY 1;', tabname);
  END
  $func$ LANGUAGE plpgsql
;
----------
[CREATE_SCHEMA] CREATE SCHEMA testns2
;
----------
[DROP_TABLE] DROP TABLE pg_temp_18.pg_temp_25732_2, pg_temp_18.pg_temp_25732
;
