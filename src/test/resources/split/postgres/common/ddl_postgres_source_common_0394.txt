DROP TABLE IF EXISTS tpart CASCADE
;
create table inh_child2 () inherits (inh_parent)
;
-- test: use invalid expr in return statement.
create or replace function compos() returns compostype as $$
begin
  return 1 + 1;
end;
$$ language plpgsql
;
-- test an unlogged table, mostly to get coverage of ginbuildempty
create unlogged table t_gin_test_tbl(i int4[], j int4[])
;
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf3 SET SCHEMA alt_nsp2
;
CREATE OPERATOR CLASS gist_oid_ops
DEFAULT FOR TYPE oid USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_oid_consistent (internal, oid, int2, oid, internal),
	FUNCTION	2	gbt_oid_union (internal, internal),
	FUNCTION	3	gbt_oid_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_oid_penalty (internal, internal, internal),
	FUNCTION	6	gbt_oid_picksplit (internal, internal),
	FUNCTION	7	gbt_oid_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
/*
 * Check that changes are handled correctly when interleaved with ddl
 */
CREATE TABLE replication_example(id SERIAL PRIMARY KEY, somedata int, text varchar(120))
;
drop table trigpart
;
create table cnullparent (f1 int)
;
-- Failing to use the full partition key is not allowed
create table idxpart (a int unique, b int) partition by range (a, b)
;
CREATE TABLE vacowned (a int)
;
CREATE TABLE brin_iso (
        value int
    ) WITH (fillfactor=10)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_ts_config
;
CREATE FUNCTION "public"."namedparmcursor_test1"(integer, integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
declare
    c1 cursor (param1 int, param12 int) for select * from rc_test where a > param1 and b > param12;
    nonsense record;
begin
    open c1(param12 := $2, param1 := $1);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$_$
;
CREATE OPERATOR CLASS int_custom_ops
  DEFAULT FOR TYPE int_custom USING btree AS
    OPERATOR    1     <,
    OPERATOR    2     <=,
    OPERATOR    3     =,
    OPERATOR    4     >=,
    OPERATOR    5     >,
    FUNCTION    1     int_custom_cmp(int_custom, int_custom)
;
DROP TABLE relation_filenode_check
;
CREATE FUNCTION lca(ltree,ltree,ltree,ltree,ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FUNCTION test_atomic_ops()
    RETURNS bool
    AS '/src/src/test/regress/regress.so'
    LANGUAGE C
;
DROP TABLE NAME_TBL
;
CREATE FUNCTION target_tg_trig_fn() RETURNS trigger LANGUAGE plpgsql AS
  $$
  BEGIN
    IF tg_op = 'INSERT' THEN
      RAISE NOTICE 'Insert: %', NEW;
      RETURN NEW;
    ELSIF tg_op = 'UPDATE' THEN
      RAISE NOTICE 'Update: % -> %', OLD, NEW;
      RETURN NEW;
    ELSE
      RAISE NOTICE 'Delete: %', OLD;
      RETURN OLD;
    END IF;
  END
  $$
;
--
-- Test GROUP BY matching of join columns that are type-coerced due to USING
--

create temp table t1(f1 int, f2 bigint)
;
ANALYZE public.query
;
ALTER TABLE attmp ALTER COLUMN i SET (n_distinct = 1, n_distinct_inherited = 2)
;
DROP TABLE other_tbl_parent CASCADE
;
CREATE TABLE pgss_wal_tab (a int, b char(20))
;
CREATE OR REPLACE FUNCTION rngfunc(int)
RETURNS TABLE(a int, b int)
AS $$ SELECT a, b
         FROM generate_series(1,$1) a(a),
              generate_series(1,$1) b(b) $$ LANGUAGE sql
;
create temp table quadtable(f1 int, q quad)
;
CREATE TRIGGER trig_del_pk AFTER DELETE ON fkpart11.pk FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
ALTER DOMAIN constraint_comments_dom ADD CONSTRAINT inv_ck CHECK (value > 0) NOT VALID
;
create table t(f1 name, f2 integer, f3 text)
;
alter table user_table add column address varchar(255) references abc(id2)
;
-- ************************************************************
-- * Describe the front of a wall connector slot
-- ************************************************************
create function wslot_slotlink_view(bpchar)
returns text as '
declare
    rec		record;
    sltype	char(2);
    retval	text;
begin
    select into rec * from WSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if rec.slotlink = '''' then
        return ''-'';
    end if;
    sltype := substr(rec.slotlink, 1, 2);
    if sltype = ''PH'' then
        select into rec * from PHone where slotname = rec.slotlink;
	retval := ''Phone '' || trim(rec.slotname);
	if rec.comment != '''' then
	    retval := retval || '' ('';
	    retval := retval || rec.comment;
	    retval := retval || '')'';
	end if;
	return retval;
    end if;
    if sltype = ''IF'' then
	declare
	    syrow	System%RowType;
	    ifrow	IFace%ROWTYPE;
        begin
	    select into ifrow * from IFace where slotname = rec.slotlink;
	    select into syrow * from System where name = ifrow.sysname;
	    retval := syrow.name || '' IF '';
	    retval := retval || ifrow.ifname;
	    if syrow.comment != '''' then
	        retval := retval || '' ('';
		retval := retval || syrow.comment;
		retval := retval || '')'';
	    end if;
	    return retval;
	end;
    end if;
    return rec.slotlink;
end;
' language plpgsql
;
CREATE INDEX collate_test1_idx1 ON collate_test1 (b)
;
CREATE VIEW rw_view1 AS SELECT b AS bb, a AS aa FROM base_tbl WHERE a>0
;
create table insertconflict (a bigint, b bigint)
;
CREATE FUNCTION "public"."tg_slotlink_unset"(character, character) RETURNS integer
    LANGUAGE "plpgsql"
    AS $_$
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    if mytype = 'PS' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PSlot set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'WS' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update WSlot set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'IF' then
        select into rec * from IFace where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update IFace set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'HS' then
        select into rec * from HSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update HSlot set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'PH' then
        select into rec * from PHone where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PHone set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
end;
$_$
;
create operator class part_test_int4_ops2
for type int4
using hash as
operator 1 ===,
function 2 part_hashint4_noop(int4, int8)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_largeobject_metadata
;
CREATE TEMP TABLE pg_temp_24.pg_temp_26747_2 (tid pg_catalog.tid)
;
/*
 * 3.2 Test ALTER TABLE DROP COLUMN.
 * Included column deletion leads to the index deletion.
 * AS well AS key columns deletion. It's explained in documentation.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
ANALYZE public.num_exp_mul
;
CREATE FUNCTION testpub_rf_func2() RETURNS integer AS $$ BEGIN RETURN 123; END; $$ LANGUAGE plpgsql
;
/* End test case for bug #17409 */
/* Test case for bug #18970 */
create table attbl(a int)
;
CREATE TABLE table1(col1 SERIAL PRIMARY KEY)
;
-- mb<->wchar conversions
CREATE FUNCTION test_encoding(encoding text, description text, input bytea)
RETURNS VOID LANGUAGE plpgsql AS
$$
DECLARE
	prefix text;
	len int;
	wchars int[];
	round_trip bytea;
	result text;
BEGIN
	prefix := rpad(encoding || ' ' || description || ':', 28);

	-- XXX could also test validation, length functions and include client
	-- only encodings with these test cases

	IF test_valid_server_encoding(encoding) THEN
		wchars := test_text_to_wchars(encoding, test_bytea_to_text(input));
		round_trip = test_text_to_bytea(test_wchars_to_text(encoding, wchars));
		if input = round_trip then
			result := 'OK';
		elsif length(input) > length(round_trip) and round_trip = substr(input, 1, length(round_trip)) then
			result := 'truncated';
		else
			result := 'failed';
		end if;
		RAISE NOTICE '% % -> % -> % = %', prefix, input, wchars, round_trip, result;
	END IF;
END;
$$
;
-- HANDLER related checks
CREATE FUNCTION invalid_fdw_handler() RETURNS int LANGUAGE SQL AS 'SELECT 1;'
;
CREATE FUNCTION isnge(ismn13, ismn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP EVENT TRIGGER end_rls_command
;
CREATE TABLE testpub_tbl8 (a int, b text, c text) PARTITION BY HASH (a)
;
ALTER TABLE pg_temp_6.pg_temp_33826_2 ADD COLUMN newdata pg_temp_6.pg_temp_33826
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE IF EXISTS tpart CASCADE
;
----------
[CREATE_TABLE] create table inh_child2 () inherits (inh_parent)
;
----------
[CREATE_PROG_OBJ] -- test: use invalid expr in return statement.
create or replace function compos() returns compostype as $$
begin
  return 1 + 1;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] -- test an unlogged table, mostly to get coverage of ginbuildempty
create unlogged table t_gin_test_tbl(i int4[], j int4[])
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION alt_ts_conf3 SET SCHEMA alt_nsp2
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_oid_ops
DEFAULT FOR TYPE oid USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_oid_consistent (internal, oid, int2, oid, internal),
	FUNCTION	2	gbt_oid_union (internal, internal),
	FUNCTION	3	gbt_oid_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_oid_penalty (internal, internal, internal),
	FUNCTION	6	gbt_oid_picksplit (internal, internal),
	FUNCTION	7	gbt_oid_same (gbtreekey8, gbtreekey8, internal),
	STORAGE		gbtreekey8
;
----------
[CREATE_TABLE] /*
 * Check that changes are handled correctly when interleaved with ddl
 */
CREATE TABLE replication_example(id SERIAL PRIMARY KEY, somedata int, text varchar(120))
;
----------
[DROP_TABLE] drop table trigpart
;
----------
[CREATE_TABLE] create table cnullparent (f1 int)
;
----------
[CREATE_TABLE] -- Failing to use the full partition key is not allowed
create table idxpart (a int unique, b int) partition by range (a, b)
;
----------
[CREATE_TABLE] CREATE TABLE vacowned (a int)
;
----------
[CREATE_TABLE] CREATE TABLE brin_iso (
        value int
    ) WITH (fillfactor=10)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_ts_config
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."namedparmcursor_test1"(integer, integer) RETURNS boolean
    LANGUAGE "plpgsql"
    AS $_$
declare
    c1 cursor (param1 int, param12 int) for select * from rc_test where a > param1 and b > param12;
    nonsense record;
begin
    open c1(param12 := $2, param1 := $1);
    fetch c1 into nonsense;
    close c1;
    if found then
        return true;
    else
        return false;
    end if;
end
$_$
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS int_custom_ops
  DEFAULT FOR TYPE int_custom USING btree AS
    OPERATOR    1     <,
    OPERATOR    2     <=,
    OPERATOR    3     =,
    OPERATOR    4     >=,
    OPERATOR    5     >,
    FUNCTION    1     int_custom_cmp(int_custom, int_custom)
;
----------
[DROP_TABLE] DROP TABLE relation_filenode_check
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lca(ltree,ltree,ltree,ltree,ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_atomic_ops()
    RETURNS bool
    AS '/src/src/test/regress/regress.so'
    LANGUAGE C
;
----------
[DROP_TABLE] DROP TABLE NAME_TBL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION target_tg_trig_fn() RETURNS trigger LANGUAGE plpgsql AS
  $$
  BEGIN
    IF tg_op = 'INSERT' THEN
      RAISE NOTICE 'Insert: %', NEW;
      RETURN NEW;
    ELSIF tg_op = 'UPDATE' THEN
      RAISE NOTICE 'Update: % -> %', OLD, NEW;
      RETURN NEW;
    ELSE
      RAISE NOTICE 'Delete: %', OLD;
      RETURN OLD;
    END IF;
  END
  $$
;
----------
[CREATE_TABLE] --
-- Test GROUP BY matching of join columns that are type-coerced due to USING
--

create temp table t1(f1 int, f2 bigint)
;
----------
[ADMIN_TABLE] ANALYZE public.query
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE attmp ALTER COLUMN i SET (n_distinct = 1, n_distinct_inherited = 2)
;
----------
[DROP_TABLE] DROP TABLE other_tbl_parent CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE pgss_wal_tab (a int, b char(20))
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION rngfunc(int)
RETURNS TABLE(a int, b int)
AS $$ SELECT a, b
         FROM generate_series(1,$1) a(a),
              generate_series(1,$1) b(b) $$ LANGUAGE sql
;
----------
[CREATE_TABLE] create temp table quadtable(f1 int, q quad)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_del_pk AFTER DELETE ON fkpart11.pk FOR EACH ROW EXECUTE FUNCTION fkpart11.print_row()
;
----------
[ALTER_TYPE] ALTER DOMAIN constraint_comments_dom ADD CONSTRAINT inv_ck CHECK (value > 0) NOT VALID
;
----------
[CREATE_TABLE] create table t(f1 name, f2 integer, f3 text)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table user_table add column address varchar(255) references abc(id2)
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * Describe the front of a wall connector slot
-- ************************************************************
create function wslot_slotlink_view(bpchar)
returns text as '
declare
    rec		record;
    sltype	char(2);
    retval	text;
begin
    select into rec * from WSlot where slotname = $1;
    if not found then
        return '''';
    end if;
    if rec.slotlink = '''' then
        return ''-'';
    end if;
    sltype := substr(rec.slotlink, 1, 2);
    if sltype = ''PH'' then
        select into rec * from PHone where slotname = rec.slotlink;
	retval := ''Phone '' || trim(rec.slotname);
	if rec.comment != '''' then
	    retval := retval || '' ('';
	    retval := retval || rec.comment;
	    retval := retval || '')'';
	end if;
	return retval;
    end if;
    if sltype = ''IF'' then
	declare
	    syrow	System%RowType;
	    ifrow	IFace%ROWTYPE;
        begin
	    select into ifrow * from IFace where slotname = rec.slotlink;
	    select into syrow * from System where name = ifrow.sysname;
	    retval := syrow.name || '' IF '';
	    retval := retval || ifrow.ifname;
	    if syrow.comment != '''' then
	        retval := retval || '' ('';
		retval := retval || syrow.comment;
		retval := retval || '')'';
	    end if;
	    return retval;
	end;
    end if;
    return rec.slotlink;
end;
' language plpgsql
;
----------
[ADD_INDEX] CREATE INDEX collate_test1_idx1 ON collate_test1 (b)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT b AS bb, a AS aa FROM base_tbl WHERE a>0
;
----------
[CREATE_TABLE] create table insertconflict (a bigint, b bigint)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."tg_slotlink_unset"(character, character) RETURNS integer
    LANGUAGE "plpgsql"
    AS $_$
declare
    myname	alias for $1;
    blname	alias for $2;
    mytype	char(2);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    if mytype = 'PS' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PSlot set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'WS' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update WSlot set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'IF' then
        select into rec * from IFace where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update IFace set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'HS' then
        select into rec * from HSlot where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update HSlot set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = 'PH' then
        select into rec * from PHone where slotname = myname;
	if not found then
	    return 0;
	end if;
	if rec.slotlink = blname then
	    update PHone set slotlink = '' where slotname = myname;
	end if;
	return 0;
    end if;
end;
$_$
;
----------
[CREATE_PROG_OBJ] create operator class part_test_int4_ops2
for type int4
using hash as
operator 1 ===,
function 2 part_hashint4_noop(int4, int8)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_largeobject_metadata
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_24.pg_temp_26747_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] /*
 * 3.2 Test ALTER TABLE DROP COLUMN.
 * Included column deletion leads to the index deletion.
 * AS well AS key columns deletion. It's explained in documentation.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 box, UNIQUE(c1, c2) INCLUDE(c3,c4))
;
----------
[ADMIN_TABLE] ANALYZE public.num_exp_mul
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testpub_rf_func2() RETURNS integer AS $$ BEGIN RETURN 123; END; $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] /* End test case for bug #17409 */
/* Test case for bug #18970 */
create table attbl(a int)
;
----------
[CREATE_TABLE] CREATE TABLE table1(col1 SERIAL PRIMARY KEY)
;
----------
[CREATE_PROG_OBJ] -- mb<->wchar conversions
CREATE FUNCTION test_encoding(encoding text, description text, input bytea)
RETURNS VOID LANGUAGE plpgsql AS
$$
DECLARE
	prefix text;
	len int;
	wchars int[];
	round_trip bytea;
	result text;
BEGIN
	prefix := rpad(encoding || ' ' || description || ':', 28);

	-- XXX could also test validation, length functions and include client
	-- only encodings with these test cases

	IF test_valid_server_encoding(encoding) THEN
		wchars := test_text_to_wchars(encoding, test_bytea_to_text(input));
		round_trip = test_text_to_bytea(test_wchars_to_text(encoding, wchars));
		if input = round_trip then
			result := 'OK';
		elsif length(input) > length(round_trip) and round_trip = substr(input, 1, length(round_trip)) then
			result := 'truncated';
		else
			result := 'failed';
		end if;
		RAISE NOTICE '% % -> % -> % = %', prefix, input, wchars, round_trip, result;
	END IF;
END;
$$
;
----------
[CREATE_PROG_OBJ] -- HANDLER related checks
CREATE FUNCTION invalid_fdw_handler() RETURNS int LANGUAGE SQL AS 'SELECT 1;'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ismn13, ismn13)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER end_rls_command
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl8 (a int, b text, c text) PARTITION BY HASH (a)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_6.pg_temp_33826_2 ADD COLUMN newdata pg_temp_6.pg_temp_33826
;
