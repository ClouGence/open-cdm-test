ALTER DATABASE "ecpg2_regression" SET lc_monetary TO 'C'
;
CREATE DOMAIN int4range_d AS int4range CHECK (VALUE <> '[10,11)')
;
drop sequence ddef4_seq
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
-- fires _trigger2 and _trigger_end should fire, but not _trigger
create table event_trigger_fire1 (a int)
;
drop table parent_tab cascade
;
-- A case with nested MergeAppend with its own PartitionPruneInfo.
create index on part_abc (a)
;
ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk4" FOR VALUES FROM (3000) TO (4000)
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."tt6" INHERIT "public"."tt0"
;
CREATE TABLE testpub_tbl4(a int)
;
CREATE TABLE tab3 (a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
ALTER TABLE pg_toast.pg_toast_33170 RENAME TO reind_con_toast
;
CREATE TABLE moneyp_11 PARTITION OF moneyp FOR VALUES IN ('11')
;
CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
CREATE TABLE tab_int AS SELECT generate_series(1,1000) AS a
;
CREATE TABLE "public"."notnull_part1_upg" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("a")
;
CREATE FUNCTION dblink_connect_u (text, text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED SECURITY DEFINER
;
-- trying to specify range for the hash partitioned table
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES FROM ('a', 1) TO ('z')
;
create table inh_nn3 () inherits (inh_nn2)
;
create table nulltest
           ( col1 dnotnull
           , col2 dnotnull NULL  -- NOT NULL in the domain cannot be overridden
           , col3 dnull    NOT NULL
           , col4 dnull
           , col5 dcheck CHECK (col5 IN ('c', 'd'))
           )
;
DO LANGUAGE plpgsql $$
BEGIN
  EXECUTE 'COPY copy1 FROM ''/src/src/pl/plpgsql/src/results/copy1.data''';
END;
$$
;
ALTER TABLE pg_temp_9.pg_temp_25561_2 ADD COLUMN newdata pg_temp_9.pg_temp_25561
;
create type jsb_char2 as (a char(2))
;
CREATE TABLE my_schema_1.ts1 (a int, b text)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_publication_rel
;
-- Test EXCLUDING ALL
CREATE FOREIGN TABLE ctl_foreign_table1(LIKE ctl_table EXCLUDING ALL) SERVER ctl_s0
;
CREATE EXTENSION dummy_seclabel
;
ANALYZE public.fk_notpartitioned_pk
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_trigger
;
ANALYZE pg_temp_12.pg_temp_26560_2
;
analyze tt3
;
-- fail
SECURITY LABEL FOR 'dummy' ON ROLE regress_seclabel_user1 IS 'classified'
;
CREATE FUNCTION test_destroy()
RETURNS void STRICT PARALLEL UNSAFE
AS 'MODULE_PATHNAME' LANGUAGE C
;
--
-- ROWTYPES
--

-- Make both a standalone composite type and a table rowtype

create type complex as (r float8, i float8)
;
ALTER TABLE attmp ADD COLUMN xmin integer
;
CREATE TEMP TABLE foo (serial_num int, name text, type text)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) information_schema.sql_parts
;
-- ************************************************************
-- * BEFORE INSERT or UPDATE on all slots with slotlink
-- *	- Set slotlink to empty string if NULL value given
-- ************************************************************
create function tg_chkslotlink() returns trigger as '
begin
    if new.slotlink isnull then
        new.slotlink := '''';
    end if;
    return new;
end;
' language plpgsql
;
CREATE CAST (ismn AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
DROP TABLE pg_temp_13.pg_temp_25576_2, pg_temp_13.pg_temp_25576
;
CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=22425 host=/tmp/lyw0fq4545 dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (two_phase = on)
;
DROP INDEX IF EXISTS no_such_schema.foo
;
CREATE TABLE "public"."prt2_e_p3" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
ALTER TABLE test_part_a_2 REPLICA IDENTITY USING INDEX test_part_a_2_pkey
;
DROP TABLE test_replica_identity2
;
CREATE TRIGGER invalid_trig BEFORE UPDATE ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_upd_row')
;
CREATE OPERATOR #%# (
   leftarg = int8,		-- fail, postfix is no longer supported
   procedure = factorial
)
;
create table sometable (a int, b text)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_depend
;
create table part_null_xy partition of list_parted for values in (null, 'xy')
;
------------------------------------------------------------------------------------------
[ALTER_CATALOG] ALTER DATABASE "ecpg2_regression" SET lc_monetary TO 'C'
;
----------
[CREATE_TYPE] CREATE DOMAIN int4range_d AS int4range CHECK (VALUE <> '[10,11)')
;
----------
[DROP_SEQUENCE] drop sequence ddef4_seq
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_template
;
----------
[CREATE_TABLE] -- fires _trigger2 and _trigger_end should fire, but not _trigger
create table event_trigger_fire1 (a int)
;
----------
[DROP_TABLE] drop table parent_tab cascade
;
----------
[ADD_INDEX] -- A case with nested MergeAppend with its own PartitionPruneInfo.
create index on part_abc (a)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk4" FOR VALUES FROM (3000) TO (4000)
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."tt6" INHERIT "public"."tt0"
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl4(a int)
;
----------
[CREATE_TABLE] CREATE TABLE tab3 (a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33170 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE moneyp_11 PARTITION OF moneyp FOR VALUES IN ('11')
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY irish_stem
	(TEMPLATE = snowball, Language = irish )
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tab_int AS SELECT generate_series(1,1000) AS a
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_part1_upg" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("a")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_connect_u (text, text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED SECURITY DEFINER
;
----------
[CREATE_TABLE] -- trying to specify range for the hash partitioned table
CREATE TABLE fail_part PARTITION OF hash_parted FOR VALUES FROM ('a', 1) TO ('z')
;
----------
[CREATE_TABLE] create table inh_nn3 () inherits (inh_nn2)
;
----------
[CREATE_TABLE] create table nulltest
           ( col1 dnotnull
           , col2 dnotnull NULL  -- NOT NULL in the domain cannot be overridden
           , col3 dnull    NOT NULL
           , col4 dnull
           , col5 dcheck CHECK (col5 IN ('c', 'd'))
           )
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
  EXECUTE 'COPY copy1 FROM ''/src/src/pl/plpgsql/src/results/copy1.data''';
END;
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_9.pg_temp_25561_2 ADD COLUMN newdata pg_temp_9.pg_temp_25561
;
----------
[CREATE_TYPE] create type jsb_char2 as (a char(2))
;
----------
[CREATE_TABLE] CREATE TABLE my_schema_1.ts1 (a int, b text)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_publication_rel
;
----------
[CREATE_TABLE] -- Test EXCLUDING ALL
CREATE FOREIGN TABLE ctl_foreign_table1(LIKE ctl_table EXCLUDING ALL) SERVER ctl_s0
;
----------
[CREATE_LIBRARY] CREATE EXTENSION dummy_seclabel
;
----------
[ADMIN_TABLE] ANALYZE public.fk_notpartitioned_pk
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_trigger
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26560_2
;
----------
[ADMIN_TABLE] analyze tt3
;
----------
[SECURITY_LABEL] -- fail
SECURITY LABEL FOR 'dummy' ON ROLE regress_seclabel_user1 IS 'classified'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_destroy()
RETURNS void STRICT PARALLEL UNSAFE
AS 'MODULE_PATHNAME' LANGUAGE C
;
----------
[CREATE_TYPE] --
-- ROWTYPES
--

-- Make both a standalone composite type and a table rowtype

create type complex as (r float8, i float8)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE attmp ADD COLUMN xmin integer
;
----------
[CREATE_TABLE] CREATE TEMP TABLE foo (serial_num int, name text, type text)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) information_schema.sql_parts
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * BEFORE INSERT or UPDATE on all slots with slotlink
-- *	- Set slotlink to empty string if NULL value given
-- ************************************************************
create function tg_chkslotlink() returns trigger as '
begin
    if new.slotlink isnull then
        new.slotlink := '''';
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ismn AS ean13) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_25576_2, pg_temp_13.pg_temp_25576
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=22425 host=/tmp/lyw0fq4545 dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (two_phase = on)
;
----------
[DROP_INDEX] DROP INDEX IF EXISTS no_such_schema.foo
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_e_p3" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[ALTER_TABLE] ALTER TABLE test_part_a_2 REPLICA IDENTITY USING INDEX test_part_a_2_pkey
;
----------
[DROP_TABLE] DROP TABLE test_replica_identity2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig BEFORE UPDATE ON main_view
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_upd_row')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #%# (
   leftarg = int8,		-- fail, postfix is no longer supported
   procedure = factorial
)
;
----------
[CREATE_TABLE] create table sometable (a int, b text)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_depend
;
----------
[CREATE_TABLE] create table part_null_xy partition of list_parted for values in (null, 'xy')
;
