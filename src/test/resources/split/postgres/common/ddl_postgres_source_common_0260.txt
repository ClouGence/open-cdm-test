VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_largeobject_metadata
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_enum
;
ALTER TYPE test_type RENAME ATTRIBUTE d TO dd
;
ALTER TABLE ONLY "public"."idxpart3" ATTACH PARTITION "public"."idxpart32" FOR VALUES FROM (1200) TO (1400)
;
CREATE TABLE upsert (key text not null, payload text)
;
-- this combination used to have order-of-execution problems (bug #15580)
create table atacc1 (a int)
;
ALTER INDEX dummy_test_idx RESET (option_int)
;
ALTER FUNCTION int2_dist(smallint, smallint) PARALLEL SAFE
;
CREATE TABLE a (i int PRIMARY KEY)
;
-- int2 is part of integer opfamily as of 8.0
ALTER TABLE fktable ADD CONSTRAINT fk_5_1
FOREIGN KEY (x5) REFERENCES pktable(id1)
;
CREATE SCHEMA regress_schema_2
;
VACUUM (FREEZE) pg_catalog.pg_transform
;
-- schema

DROP SCHEMA test_schema_exists
;
create table part_aa_bb partition of list_parted FOR VALUES IN ('aa', 'bb')
;
alter table dropColumn drop column a
;
CREATE TABLE "public"."brin_test_bloom" (
    "a" integer,
    "b" integer
)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "numword" WITH "public"."ispell", "english_stem"
;
CREATE INDEX brin_test_parallel_idx ON brin_parallel_test
 USING brin (a int4_minmax_ops, a int4_bloom_ops, b, c int8_minmax_multi_ops)
  WITH (pages_per_range=7)
 WHERE NOT (a BETWEEN 1000 and 1500)
;
CREATE FUNCTION isngt(ismn13, ismn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_30701_index RENAME TO reind_con_toast_idx
;
ALTER TABLE ONLY "public"."mlparted2" DROP COLUMN "........pg.dropped.7........"
;
create table parted1_constr (a int, b text)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=21056 host=/tmp/yv8G3V4Xk8 dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
CREATE FUNCTION hobbies(person)
   RETURNS setof hobbies_r
   AS 'select * from hobbies_r where person = $1.name'
   LANGUAGE SQL
;
-- After terminating the remote backend, since the connection is closed,
-- "closed" should be TRUE, or NULL if the connection status check
-- is not available. Despite the termination, remote_backend_pid should
-- still show the non-zero PID of the terminated remote backend.
DO $$ BEGIN
PERFORM pg_terminate_backend(pid, 180000) FROM pg_stat_activity
  WHERE application_name = 'fdw_conn_check';
END $$
;
CREATE FOREIGN TABLE prem2 (f1 int, f2 text)
  SERVER loopback2 OPTIONS (table_name 'ploc2')
;
CREATE FUNCTION dsnowball_lexize(INTERNAL, INTERNAL, INTERNAL, INTERNAL)
    RETURNS INTERNAL AS '$libdir/dict_snowball', 'dsnowball_lexize'
LANGUAGE C STRICT
;
ALTER INDEX dummy_test_idx SET (option_real = 'val5')
;
create or replace function rngfuncbar() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar ; $$
language sql stable
;
CREATE AGGREGATE array_larger_accum (anyarray)
(
    sfunc = array_larger,
    stype = anyarray,
    initcond = '{}'
)
;
VACUUM pg_catalog.pg_aggregate
;
-- make an operator to allow it to succeed
create function bogus_int8_text_eq(int8, text) returns boolean
language sql as 'select $1::text = $2'
;
CREATE FUNCTION testHVToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
$val = {a => 1, b => 'boo', c => undef};
return $val;
$$
;
VACUUM (FREEZE) pg_catalog.pg_opclass
;
drop table pp_temp_parent
;
VACUUM (PARALLEL 0) pg_catalog.pg_statistic_ext_data
;
CREATE TABLE xpath_test (id integer NOT NULL, t xml)
;
create table cc2(f4 float) inherits(pp1,cc1)
;
DROP TABLE deptest
;
-- Test interaction of partitioned join with partition pruning
CREATE TABLE plt1_adv (a int, b int, c text) PARTITION BY LIST (c)
;
CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, c, d FROM viewtest_tbl WHERE a > 5 ORDER BY b DESC
;
--
-- regression test for problems of the sort depicted in bug #3494
--

create temp table tt5(f1 int, f2 int)
;
DO 'BEGIN CALL transaction_test1(9, $x$baz$x$); END;'
;
CREATE FUNCTION rngfunc_mat(int,int) RETURNS setof rngfunc_rescan_t AS 'begin for i in $1..$2 loop return next (i, nextval(''rngfunc_rescan_seq2'')); end loop; end;' LANGUAGE plpgsql
;
CREATE OPERATOR !==== (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) information_schema.sql_features
;
ALTER TABLE pg_toast.pg_toast_30434 RENAME TO reind_con_toast
;
CREATE SUBSCRIPTION tap_sub_copy
	CONNECTION 'port=12942 host=/tmp/OOswabqf8K dbname=postgres application_name=appname_copy'
	PUBLICATION tap_pub_copy
	WITH (two_phase=on, copy_data=false)
;
-- cleanup
DROP FOREIGN TABLE ft1
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = issn13,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_largeobject_metadata
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_enum
;
----------
[ALTER_TYPE] ALTER TYPE test_type RENAME ATTRIBUTE d TO dd
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."idxpart3" ATTACH PARTITION "public"."idxpart32" FOR VALUES FROM (1200) TO (1400)
;
----------
[CREATE_TABLE] CREATE TABLE upsert (key text not null, payload text)
;
----------
[CREATE_TABLE] -- this combination used to have order-of-execution problems (bug #15580)
create table atacc1 (a int)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx RESET (option_int)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION int2_dist(smallint, smallint) PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE a (i int PRIMARY KEY)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- int2 is part of integer opfamily as of 8.0
ALTER TABLE fktable ADD CONSTRAINT fk_5_1
FOREIGN KEY (x5) REFERENCES pktable(id1)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_schema_2
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_transform
;
----------
[DROP_SCHEMA] -- schema

DROP SCHEMA test_schema_exists
;
----------
[CREATE_TABLE] create table part_aa_bb partition of list_parted FOR VALUES IN ('aa', 'bb')
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table dropColumn drop column a
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brin_test_bloom" (
    "a" integer,
    "b" integer
)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "numword" WITH "public"."ispell", "english_stem"
;
----------
[ADD_INDEX] CREATE INDEX brin_test_parallel_idx ON brin_parallel_test
 USING brin (a int4_minmax_ops, a int4_bloom_ops, b, c int8_minmax_multi_ops)
  WITH (pages_per_range=7)
 WHERE NOT (a BETWEEN 1000 and 1500)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(ismn13, ismn)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30701_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted2" DROP COLUMN "........pg.dropped.7........"
;
----------
[CREATE_TABLE] create table parted1_constr (a int, b text)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=21056 host=/tmp/yv8G3V4Xk8 dbname=postgres' PUBLICATION tap_pub WITH (copy_data = false)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hobbies(person)
   RETURNS setof hobbies_r
   AS 'select * from hobbies_r where person = $1.name'
   LANGUAGE SQL
;
----------
[BLOCK] -- After terminating the remote backend, since the connection is closed,
-- "closed" should be TRUE, or NULL if the connection status check
-- is not available. Despite the termination, remote_backend_pid should
-- still show the non-zero PID of the terminated remote backend.
DO $$ BEGIN
PERFORM pg_terminate_backend(pid, 180000) FROM pg_stat_activity
  WHERE application_name = 'fdw_conn_check';
END $$
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE prem2 (f1 int, f2 text)
  SERVER loopback2 OPTIONS (table_name 'ploc2')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dsnowball_lexize(INTERNAL, INTERNAL, INTERNAL, INTERNAL)
    RETURNS INTERNAL AS '$libdir/dict_snowball', 'dsnowball_lexize'
LANGUAGE C STRICT
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_real = 'val5')
;
----------
[CREATE_PROG_OBJ] create or replace function rngfuncbar() returns setof text as
$$ select 'foo'::varchar union all select 'bar'::varchar ; $$
language sql stable
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE array_larger_accum (anyarray)
(
    sfunc = array_larger,
    stype = anyarray,
    initcond = '{}'
)
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_aggregate
;
----------
[CREATE_PROG_OBJ] -- make an operator to allow it to succeed
create function bogus_int8_text_eq(int8, text) returns boolean
language sql as 'select $1::text = $2'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION testHVToJsonb() RETURNS jsonb
LANGUAGE plperlu
TRANSFORM FOR TYPE jsonb
AS $$
$val = {a => 1, b => 'boo', c => undef};
return $val;
$$
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_opclass
;
----------
[DROP_TABLE] drop table pp_temp_parent
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_TABLE] CREATE TABLE xpath_test (id integer NOT NULL, t xml)
;
----------
[CREATE_TABLE] create table cc2(f4 float) inherits(pp1,cc1)
;
----------
[DROP_TABLE] DROP TABLE deptest
;
----------
[CREATE_TABLE] -- Test interaction of partitioned join with partition pruning
CREATE TABLE plt1_adv (a int, b int, c text) PARTITION BY LIST (c)
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW viewtest AS
	SELECT a, b, c, d FROM viewtest_tbl WHERE a > 5 ORDER BY b DESC
;
----------
[CREATE_TABLE] --
-- regression test for problems of the sort depicted in bug #3494
--

create temp table tt5(f1 int, f2 int)
;
----------
[BLOCK] DO 'BEGIN CALL transaction_test1(9, $x$baz$x$); END;'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rngfunc_mat(int,int) RETURNS setof rngfunc_rescan_t AS 'begin for i in $1..$2 loop return next (i, nextval(''rngfunc_rescan_seq2'')); end loop; end;' LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !==== (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) information_schema.sql_features
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30434 RENAME TO reind_con_toast
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_copy
	CONNECTION 'port=12942 host=/tmp/OOswabqf8K dbname=postgres application_name=appname_copy'
	PUBLICATION tap_pub_copy
	WITH (two_phase=on, copy_data=false)
;
----------
[DROP_TABLE] -- cleanup
DROP FOREIGN TABLE ft1
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = issn13,
	RIGHTARG = ean13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
