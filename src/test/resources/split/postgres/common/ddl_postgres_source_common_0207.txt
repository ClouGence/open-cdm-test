CREATE TABLE testschema.test_default_tab_p(id bigint, val bigint)
    PARTITION BY LIST (id) TABLESPACE regress_tblspace
;
create trigger trig_del_after after delete on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
CREATE TABLE part_attmp (a int primary key) partition by range (a)
;
CREATE TABLE ctl_stats4_child (LIKE ctl_stats4_parent INCLUDING STATISTICS)
;
CREATE FOREIGN TABLE remote_application_name (application_name text)
  SERVER loopback2
  OPTIONS (schema_name 'public', table_name 'my_application_name')
;
create table s4.p2_1 partition of s4.p2 for values in (1, 2, 3)
;
-- notice

-- failures
DROP INDEX CONCURRENTLY "concur_index2", "concur_index3"
;
ANALYZE pg_temp_24.pg_temp_26747_2
;
VACUUM (ANALYZE) pg_catalog.pg_opclass
;
-- same things with one created with INHERIT
create table atacc1 (test int)
;
create table rtest_t3 (a int4, b int4)
;
ALTER TABLE FKTABLE ALTER CONSTRAINT fkdd2 DEFERRABLE INITIALLY DEFERRED
;
create trigger tg_chkslotname before insert
    on PSlot for each row execute procedure tg_chkslotname('PS')
;
create table agg_group_1 as
select g%10000 as c1, sum(g::numeric) as c2, count(*) as c3
  from agg_data_20k group by g%10000
;
alter table idxpart attach partition idxpart1 for values from (0) to (10)
;
CREATE TABLE upsert_1 PARTITION OF upsert FOR VALUES FROM (1) TO (100)
;
-- check some of the allowed relkinds
create table regular_table (a int)
;
-- yes

DROP DOMAIN testns.priv_testdomain1
;
--
-- Test advanced partition-matching algorithm for partitioned join
--

-- Tests for range-partitioned tables
CREATE TABLE prt1_adv (a int, b int, c varchar) PARTITION BY RANGE (a)
;
drop foreign table test_foreign_table
;
ALTER OPERATOR >= (ean13, isbn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
ALTER TABLE a_star* ADD COLUMN a text
;
ALTER TABLE ONLY "generated_stored_tests"."gtest10" DROP COLUMN "........pg.dropped.2........"
;
-- We need to wait a bit for all transactions to complete, so that the
-- vacuum actually removes the TOAST rows. Creating an index concurrently
-- is a one way to achieve that, because it does exactly such wait.
CREATE INDEX CONCURRENTLY brin_test_temp_idx ON brintest_3(a)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_class
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (a)
;
ALTER INDEX pg_toast.pg_toast_30839_index RENAME TO reind_con_toast_idx
;
alter table pg_toast_stud_emp rename to stud_emp
;
create table depth_c (id int not null primary key)
;
CREATE EXTENSION pgstattuple
;
ALTER TABLE ONLY "public"."part32_self_fk"
    ADD CONSTRAINT "part32_self_fk_pkey" PRIMARY KEY ("id")
;
DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN SELECT * FROM test2 ORDER BY x LOOP
        INSERT INTO test1 (a) VALUES (r.x);
        COMMIT;
    END LOOP;
END;
$$
;
alter table alterlock set (fillfactor = 100)
;
ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        2       <-> (text, text) FOR ORDER BY pg_catalog.float_ops,
        OPERATOR        3       pg_catalog.~~ (text, text),
        OPERATOR        4       pg_catalog.~~* (text, text),
        FUNCTION        8 (text, text)  gtrgm_distance (internal, text, smallint, oid, internal)
;
ALTER OPERATOR @ (ltxtquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE TABLE "public"."timestamptz_tbl" (
    "d1" timestamp(2) with time zone
)
;
CREATE TABLE concur_replident(i int NOT NULL)
;
drop table alttype_cluster
;
CREATE COLLATION case_sensitive (LOCALE = '', LOCALE = "NONSENSE")
;
-- check handling of regular btree constraint along with gist constraint

create table twoconstraints (f1 int unique, f2 box,
                             exclude using gist(f2 with &&))
;
create table mlparted5a (a int not null, c text, b int not null)
;
create index test_range_spgist_idx on test_range_spgist using spgist (ir)
;
create table uv_iocu_tab (a text unique, b float)
;
ANALYZE public.prt3_n_p2
;
-- A unique index proves uniqueness only under its own opfamily.  When the
-- GROUP BY's eqop comes from a different opfamily with looser equality,
-- rows the index regards as distinct can collapse into one GROUP BY group,
-- so the index is not usable for removing redundant columns.
create type t_rec as (x numeric)
;
ALTER OPERATOR FAMILY gist_timestamptz_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
/*
 * 24.3
 * COLUMN_OPTIONS view
 */
CREATE VIEW column_options AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(c.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.attname AS sql_identifier) AS column_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_table_columns c
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_namespace
;
ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (upc, ean13)
;
drop sequence x_seq
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE testschema.test_default_tab_p(id bigint, val bigint)
    PARTITION BY LIST (id) TABLESPACE regress_tblspace
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_del_after after delete on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[CREATE_TABLE] CREATE TABLE part_attmp (a int primary key) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE ctl_stats4_child (LIKE ctl_stats4_parent INCLUDING STATISTICS)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE remote_application_name (application_name text)
  SERVER loopback2
  OPTIONS (schema_name 'public', table_name 'my_application_name')
;
----------
[CREATE_TABLE] create table s4.p2_1 partition of s4.p2 for values in (1, 2, 3)
;
----------
[DROP_INDEX] -- notice

-- failures
DROP INDEX CONCURRENTLY "concur_index2", "concur_index3"
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_26747_2
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_opclass
;
----------
[CREATE_TABLE] -- same things with one created with INHERIT
create table atacc1 (test int)
;
----------
[CREATE_TABLE] create table rtest_t3 (a int4, b int4)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE FKTABLE ALTER CONSTRAINT fkdd2 DEFERRABLE INITIALLY DEFERRED
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_chkslotname before insert
    on PSlot for each row execute procedure tg_chkslotname('PS')
;
----------
[CREATE_TABLE|SELECT] create table agg_group_1 as
select g%10000 as c1, sum(g::numeric) as c2, count(*) as c3
  from agg_data_20k group by g%10000
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart1 for values from (0) to (10)
;
----------
[CREATE_TABLE] CREATE TABLE upsert_1 PARTITION OF upsert FOR VALUES FROM (1) TO (100)
;
----------
[CREATE_TABLE] -- check some of the allowed relkinds
create table regular_table (a int)
;
----------
[DROP_TYPE] -- yes

DROP DOMAIN testns.priv_testdomain1
;
----------
[CREATE_TABLE] --
-- Test advanced partition-matching algorithm for partitioned join
--

-- Tests for range-partitioned tables
CREATE TABLE prt1_adv (a int, b int, c varchar) PARTITION BY RANGE (a)
;
----------
[DROP_TABLE] drop foreign table test_foreign_table
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ean13, isbn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE a_star* ADD COLUMN a text
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "generated_stored_tests"."gtest10" DROP COLUMN "........pg.dropped.2........"
;
----------
[ADD_INDEX] -- We need to wait a bit for all transactions to complete, so that the
-- vacuum actually removes the TOAST rows. Creating an index concurrently
-- is a one way to achieve that, because it does exactly such wait.
CREATE INDEX CONCURRENTLY brin_test_temp_idx ON brintest_3(a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_class
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (a)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30839_index RENAME TO reind_con_toast_idx
;
----------
[RENAME_TABLE] alter table pg_toast_stud_emp rename to stud_emp
;
----------
[CREATE_TABLE] create table depth_c (id int not null primary key)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pgstattuple
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."part32_self_fk"
    ADD CONSTRAINT "part32_self_fk_pkey" PRIMARY KEY ("id")
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
DECLARE
    r RECORD;
BEGIN
    FOR r IN SELECT * FROM test2 ORDER BY x LOOP
        INSERT INTO test1 (a) VALUES (r.x);
        COMMIT;
    END LOOP;
END;
$$
;
----------
[ALTER_TABLE] alter table alterlock set (fillfactor = 100)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        2       <-> (text, text) FOR ORDER BY pg_catalog.float_ops,
        OPERATOR        3       pg_catalog.~~ (text, text),
        OPERATOR        4       pg_catalog.~~* (text, text),
        FUNCTION        8 (text, text)  gtrgm_distance (internal, text, smallint, oid, internal)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @ (ltxtquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."timestamptz_tbl" (
    "d1" timestamp(2) with time zone
)
;
----------
[CREATE_TABLE] CREATE TABLE concur_replident(i int NOT NULL)
;
----------
[DROP_TABLE] drop table alttype_cluster
;
----------
[CREATE_POLICY] CREATE COLLATION case_sensitive (LOCALE = '', LOCALE = "NONSENSE")
;
----------
[CREATE_TABLE] -- check handling of regular btree constraint along with gist constraint

create table twoconstraints (f1 int unique, f2 box,
                             exclude using gist(f2 with &&))
;
----------
[CREATE_TABLE] create table mlparted5a (a int not null, c text, b int not null)
;
----------
[ADD_INDEX] create index test_range_spgist_idx on test_range_spgist using spgist (ir)
;
----------
[CREATE_TABLE] create table uv_iocu_tab (a text unique, b float)
;
----------
[ADMIN_TABLE] ANALYZE public.prt3_n_p2
;
----------
[CREATE_TYPE] -- A unique index proves uniqueness only under its own opfamily.  When the
-- GROUP BY's eqop comes from a different opfamily with looser equality,
-- rows the index regards as distinct can collapse into one GROUP BY group,
-- so the index is not usable for removing redundant columns.
create type t_rec as (x numeric)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_timestamptz_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.3
 * COLUMN_OPTIONS view
 */
CREATE VIEW column_options AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(c.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.attname AS sql_identifier) AS column_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(c.attfdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_table_columns c
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_namespace
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (upc, ean13)
;
----------
[DROP_SEQUENCE] drop sequence x_seq
;
