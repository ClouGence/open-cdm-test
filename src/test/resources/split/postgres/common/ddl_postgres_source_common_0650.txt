ALTER OPERATOR FAMILY gist_cube_ops USING gist drop function 3 (cube)
;
-- error
DROP FOREIGN TABLE reindex_foreign
;
VACUUM (PARALLEL 0) pg_catalog.pg_policy
;
CREATE VIEW "public"."shoelace_candelete" AS
 SELECT "shoelace_obsolete"."sl_name",
    "shoelace_obsolete"."sl_avail",
    "shoelace_obsolete"."sl_color",
    "shoelace_obsolete"."sl_len",
    "shoelace_obsolete"."sl_unit",
    "shoelace_obsolete"."sl_len_cm"
   FROM "public"."shoelace_obsolete"
  WHERE ("shoelace_obsolete"."sl_avail" = 0)
;
create cast (int4 as myint) without function
;
-- null


-- clean up
DROP TABLE guid1, guid2 CASCADE
;
CREATE FUNCTION comp() RETURNS TEXT
AS $$
BEGIN
  RETURN (SELECT CASE
               WHEN m.id = c.relfilenode THEN 'Unchanged'
               ELSE 'Rewritten'
               END
           FROM m, pg_class AS c, pg_namespace AS s
           WHERE c.relname = 't'
               AND c.relnamespace = s.oid
               AND s.nspname = 'fast_default');
END;
$$ LANGUAGE 'plpgsql'
;
create index on idxpart1 (a) where b > 1
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', quote '-', null '=-=')
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "asciiword" WITH "public"."synonym", "public"."thesaurus", "english_stem"
;
ALTER MATERIALIZED VIEW d DEPENDS ON EXTENSION test_ext5
;
CREATE FUNCTION nonsimple_expr_test() RETURNS integer AS $$
declare
   i integer NOT NULL := 0;
begin
  begin
    i := (SELECT NULL::integer);  -- should throw error
  exception
    WHEN OTHERS THEN
      i := (SELECT 1::integer);
  end;
  return i;
end;
$$ LANGUAGE plpgsql
;
CREATE INDEX concur_temp_ind_1 ON concur_temp_tab_1(c2)
;
CREATE SEQUENCE "public"."insert_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_db_role_setting
;
CREATE TEMP TABLE pg_temp_46.pg_temp_28044_2 (tid pg_catalog.tid)
;
ALTER TABLE pg_temp_12.pg_temp_26234_2 ADD COLUMN newdata pg_temp_12.pg_temp_26234
;
-- Should fail. Procedure should be mandatorily specified
CREATE OPERATOR #@%# (
   leftarg = int8
)
;
CREATE TRIGGER transition_table_base_upd_trig
  AFTER UPDATE ON transition_table_base
  REFERENCING OLD TABLE AS oldtable NEW TABLE AS newtable
  FOR EACH STATEMENT
  EXECUTE PROCEDURE transition_table_base_upd_func()
;
ALTER TABLE clstr_tst SET WITHOUT CLUSTER
;
ANALYZE (SKIP_LOCKED) part1, part2
;
--
-- Test user-defined range of floats
-- (type float8range was already made in test_setup.sql)
--

--should fail
create type bogus_float8range as range (subtype=float8, subtype_diff=float4mi)
;
ALTER TABLE ONLY "regress_indexing"."pk5" ATTACH PARTITION "regress_indexing"."pk52" FOR VALUES FROM (4500) TO (5000)
;
create table rtest_emp (ename char(20), salary numeric)
;
ALTER DATABASE "ecpg1_regression" SET lc_messages TO 'C'
;
CREATE FOREIGN TABLE remt2 (c1 int, c2 text) SERVER loopback OPTIONS (table_name 'loct2')
;
create table eifoo (i integer, y integer)
;
CREATE VIEW my_credit_card_secure WITH (security_barrier) AS
       SELECT * FROM customer l NATURAL JOIN credit_card r
       WHERE l.name = current_user
;
CREATE FUNCTION "public"."test_relpath"() RETURNS "void"
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'test_relpath'
;
create table parted_collate_must_match2 partition of parted_collate_must_match
  (b collate "POSIX") for values from ('m') to ('z')
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_statistic
;
ANALYZE rls_ptbl, rls_part
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
create event trigger regress_event_trigger2 on ddl_command_start
   when food in ('sandwich')
   execute procedure test_event_trigger()
;
CREATE FUNCTION report_count() RETURNS int
STABLE LANGUAGE sql
AS $$ SELECT COUNT(*) FROM test2 $$
;
--
-- Test handling of vars from correlated subqueries in quals from outer
-- security barrier views, per bug #13988
--
CREATE TABLE t1 (a int, b text, c int)
;
-- error: change the replica identity to "b", and column list to (a, c)
-- then update fails, because (a, c) does not cover replica identity
ALTER TABLE testpub_tbl5 REPLICA IDENTITY USING INDEX testpub_tbl5_b_key
;
CREATE OPERATOR <<< (procedure = op_leak, leftarg = record, rightarg = record,
                     restrict = scalarltsel)
;
DROP TABLE gin_check_text_array
;
CREATE TABLE pagg_tab_p3 PARTITION OF pagg_tab FOR VALUES IN ('0008', '0009', '0010', '0011')
;
CREATE PROCEDURE transaction_test9b(cnt bigint)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'count = %', cnt;
END
$$
;
ALTER TABLE b VALIDATE CONSTRAINT bfk
;
DO LANGUAGE plpgsql $$
BEGIN
  -- this is a SELECT
  PERFORM 'hello world'::TEXT;
END; $$
;
CREATE TABLE ins_trig (id serial PRIMARY KEY, id2 text)
;
ALTER TABLE gtest10 DROP COLUMN b CASCADE
;
ALTER OPERATOR ^~ (lquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
create table idxpart1 partition of idxpart for values from (0) to (250)
;
-- Check multi-level default partition
create table mlparted_def partition of mlparted default partition by range(a)
;
VACUUM (FULL) pg_catalog.pg_cast
;
DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, _b, c => _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(a => _a, b => _b, c => _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, c => _c, b => _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, b => _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(b => _b, a => _a);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
------------------------------------------------------------------------------------------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_cube_ops USING gist drop function 3 (cube)
;
----------
[DROP_TABLE] -- error
DROP FOREIGN TABLE reindex_foreign
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_policy
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."shoelace_candelete" AS
 SELECT "shoelace_obsolete"."sl_name",
    "shoelace_obsolete"."sl_avail",
    "shoelace_obsolete"."sl_color",
    "shoelace_obsolete"."sl_len",
    "shoelace_obsolete"."sl_unit",
    "shoelace_obsolete"."sl_len_cm"
   FROM "public"."shoelace_obsolete"
  WHERE ("shoelace_obsolete"."sl_avail" = 0)
;
----------
[CREATE_PROG_OBJ] create cast (int4 as myint) without function
;
----------
[DROP_TABLE] -- null


-- clean up
DROP TABLE guid1, guid2 CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION comp() RETURNS TEXT
AS $$
BEGIN
  RETURN (SELECT CASE
               WHEN m.id = c.relfilenode THEN 'Unchanged'
               ELSE 'Rewritten'
               END
           FROM m, pg_class AS c, pg_namespace AS s
           WHERE c.relname = 't'
               AND c.relnamespace = s.oid
               AND s.nspname = 'fast_default');
END;
$$ LANGUAGE 'plpgsql'
;
----------
[ADD_INDEX] create index on idxpart1 (a) where b > 1
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', quote '-', null '=-=')
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "asciiword" WITH "public"."synonym", "public"."thesaurus", "english_stem"
;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW d DEPENDS ON EXTENSION test_ext5
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION nonsimple_expr_test() RETURNS integer AS $$
declare
   i integer NOT NULL := 0;
begin
  begin
    i := (SELECT NULL::integer);  -- should throw error
  exception
    WHEN OTHERS THEN
      i := (SELECT 1::integer);
  end;
  return i;
end;
$$ LANGUAGE plpgsql
;
----------
[ADD_INDEX] CREATE INDEX concur_temp_ind_1 ON concur_temp_tab_1(c2)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."insert_seq"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_db_role_setting
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_46.pg_temp_28044_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_12.pg_temp_26234_2 ADD COLUMN newdata pg_temp_12.pg_temp_26234
;
----------
[CREATE_PROG_OBJ] -- Should fail. Procedure should be mandatorily specified
CREATE OPERATOR #@%# (
   leftarg = int8
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER transition_table_base_upd_trig
  AFTER UPDATE ON transition_table_base
  REFERENCING OLD TABLE AS oldtable NEW TABLE AS newtable
  FOR EACH STATEMENT
  EXECUTE PROCEDURE transition_table_base_upd_func()
;
----------
[ALTER_TABLE] ALTER TABLE clstr_tst SET WITHOUT CLUSTER
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) part1, part2
;
----------
[CREATE_TYPE] --
-- Test user-defined range of floats
-- (type float8range was already made in test_setup.sql)
--

--should fail
create type bogus_float8range as range (subtype=float8, subtype_diff=float4mi)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "regress_indexing"."pk5" ATTACH PARTITION "regress_indexing"."pk52" FOR VALUES FROM (4500) TO (5000)
;
----------
[CREATE_TABLE] create table rtest_emp (ename char(20), salary numeric)
;
----------
[ALTER_CATALOG] ALTER DATABASE "ecpg1_regression" SET lc_messages TO 'C'
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE remt2 (c1 int, c2 text) SERVER loopback OPTIONS (table_name 'loct2')
;
----------
[CREATE_TABLE] create table eifoo (i integer, y integer)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW my_credit_card_secure WITH (security_barrier) AS
       SELECT * FROM customer l NATURAL JOIN credit_card r
       WHERE l.name = current_user
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_relpath"() RETURNS "void"
    LANGUAGE "c"
    AS '/src/src/test/regress/regress.so', 'test_relpath'
;
----------
[CREATE_TABLE] create table parted_collate_must_match2 partition of parted_collate_must_match
  (b collate "POSIX") for values from ('m') to ('z')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_statistic
;
----------
[ADMIN_TABLE] ANALYZE rls_ptbl, rls_part
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = upc,
	RIGHTARG = upc,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger2 on ddl_command_start
   when food in ('sandwich')
   execute procedure test_event_trigger()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION report_count() RETURNS int
STABLE LANGUAGE sql
AS $$ SELECT COUNT(*) FROM test2 $$
;
----------
[CREATE_TABLE] --
-- Test handling of vars from correlated subqueries in quals from outer
-- security barrier views, per bug #13988
--
CREATE TABLE t1 (a int, b text, c int)
;
----------
[ALTER_TABLE] -- error: change the replica identity to "b", and column list to (a, c)
-- then update fails, because (a, c) does not cover replica identity
ALTER TABLE testpub_tbl5 REPLICA IDENTITY USING INDEX testpub_tbl5_b_key
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <<< (procedure = op_leak, leftarg = record, rightarg = record,
                     restrict = scalarltsel)
;
----------
[DROP_TABLE] DROP TABLE gin_check_text_array
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_p3 PARTITION OF pagg_tab FOR VALUES IN ('0008', '0009', '0010', '0011')
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test9b(cnt bigint)
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE NOTICE 'count = %', cnt;
END
$$
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE b VALIDATE CONSTRAINT bfk
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
  -- this is a SELECT
  PERFORM 'hello world'::TEXT;
END; $$
;
----------
[CREATE_TABLE] CREATE TABLE ins_trig (id serial PRIMARY KEY, id2 text)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE gtest10 DROP COLUMN b CASCADE
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^~ (lquery, _ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_TABLE] create table idxpart1 partition of idxpart for values from (0) to (250)
;
----------
[CREATE_TABLE] -- Check multi-level default partition
create table mlparted_def partition of mlparted default partition by range(a)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_cast
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, _b, c => _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(a => _a, b => _b, c => _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, c => _c, b => _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(_a, b => _b);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;

  _a := 10; _b := 30; _c := 7;
  CALL test_proc10(b => _b, a => _a);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
