INSERT INTO tab_phys_slot VALUES (generate_series(1,10))
;
INSERT INTO tab_rowfilter_1 (a, b) VALUES (1600, 'test 1600')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15741', relminmxid = '3'
WHERE oid = '"public"."plt1_p3"'::pg_catalog.regclass
;
INSERT INTO DATE_TBL VALUES ('2000-04-03')
;
-- success
INSERT INTO rls_test_restrictive VALUES ('regress_r1','regress_s1',8)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10067', relminmxid = '1'
WHERE oid = '"public"."rtest_t1"'::pg_catalog.regclass
;
delete from rtest_nothn1
;
DELETE FROM pg_shdepend
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."itest3"'::pg_catalog.regclass
;
INSERT INTO itest5 OVERRIDING SYSTEM VALUE VALUES (-5, 'ff'), (DEFAULT, 'gg')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15718', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p1"'::pg_catalog.regclass
;
-- uncompressed external toast data
INSERT INTO toasttable(toasted_col1) SELECT string_agg(g.i::text, '') FROM generate_series(1, 2000) g(i)
;
INSERT INTO public.tplain VALUES ('3', 893)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16953', relminmxid = '3'
WHERE oid = '"public"."idxpart31"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16853', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_para_p2"'::pg_catalog.regclass
;
INSERT INTO combocidtest VALUES (1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6747', relminmxid = '1'
WHERE oid = '"public"."mvtest_tm"'::pg_catalog.regclass
;
INSERT INTO tab_rowfilter_3 (a, b) SELECT x, (x % 3 = 0) FROM generate_series(1, 10) x
;
DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_10.pg_temp_26444_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
INSERT INTO public.tplain VALUES ('1', 151)
;
insert into euc_jis_2004_inputs  values
  ('\x666f6f',		'valid, pure ASCII'),
  ('\x666f6fbedd',	'valid'),
  ('\xa5f7',		'valid, translates to two UTF-8 chars '),
  ('\xbeddbe',		'incomplete char '),
  ('\x666f6f00bedd',	'invalid, NUL byte'),
  ('\x666f6fbe00dd',	'invalid, NUL byte'),
  ('\x666f6fbedd00',	'invalid, NUL byte'),
  ('\xbe04',		'invalid byte sequence')
;
INSERT INTO test_prepared1 VALUES (1)
;
INSERT INTO num_exp_div VALUES (8,5,'4.56673929509287019456')
;
INSERT INTO itest3 DEFAULT VALUES
;
UPDATE base_tbl SET b=NEW.b WHERE a=1
;
-- error, type mismatch

INSERT INTO arrtest (b[1:2]) VALUES(now())
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"regress_indexing"."pk51"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '756', relminmxid = '1'
WHERE oid = '"public"."point_tbl"'::pg_catalog.regclass
;
UPDATE update_test SET (b,a) = (select a+1,b from update_test where a = 1000)
  WHERE a = 11
;
INSERT INTO num_exp_mul VALUES (6,4,'732381731243.745115764094')
;
INSERT INTO public.tht VALUES ('8', 408)
;
INSERT INTO tr_etoomuch (id, data)
SELECT g.i, -g.i FROM generate_series(8000, 12000) g(i)
ON CONFLICT(id) DO UPDATE SET data = EXCLUDED.data
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10346', relminmxid = '1'
WHERE oid = '28831'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17557', relminmxid = '3'
WHERE oid = '"public"."alpha_neg_p3"'::pg_catalog.regclass
;
insert into Room values ('002', 'Office')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8671', relminmxid = '1'
WHERE oid = '"public"."shoe_data"'::pg_catalog.regclass
;
update trigger_test set v = 'update' where i = 1
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14917', relminmxid = '3'
WHERE oid = '"public"."nv_parent"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4495', relminmxid = '1'
WHERE oid = '"public"."log_table"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (8, 278)
;
INSERT INTO pg_temp_2.pg_temp_28316_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_2.pg_temp_28316 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_2.pg_temp_28316 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."fk_partitioned_fk_1"'::pg_catalog.regclass
;
insert into pgbench_branches(bid,bbalance) select bid, 0 from generate_series(1, 1) as bid
;
INSERT INTO num_exp_mul VALUES (6,2,'-3224438592470.18449811926184222')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9163', relminmxid = '1'
WHERE oid = '"public"."ec1"'::pg_catalog.regclass
;
-- Insert test data into PKTABLE
INSERT INTO PKTABLE VALUES (1, 2, 'Test1')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2584', relminmxid = '1'
WHERE oid = '"public"."iportaltest"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('7', 447)
;
-- Add enough TIDs to cause the store to appear "full", compared
-- to the allocated memory it started out with. This is easier
-- with memory contexts in local memory.
INSERT INTO hideblocks (blockno)
SELECT do_set_block_offsets(blk, ARRAY[1,31,32,63,64,200]::int2[])
  FROM generate_series(1000, 2000, 1) blk
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted_def"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO tab_phys_slot VALUES (generate_series(1,10))
;
----------
[INSERT|SELECT] INSERT INTO tab_rowfilter_1 (a, b) VALUES (1600, 'test 1600')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15741', relminmxid = '3'
WHERE oid = '"public"."plt1_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO DATE_TBL VALUES ('2000-04-03')
;
----------
[INSERT] -- success
INSERT INTO rls_test_restrictive VALUES ('regress_r1','regress_s1',8)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10067', relminmxid = '1'
WHERE oid = '"public"."rtest_t1"'::pg_catalog.regclass
;
----------
[DELETE] delete from rtest_nothn1
;
----------
[DELETE] DELETE FROM pg_shdepend
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7399', relminmxid = '1'
WHERE oid = '"public"."itest3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO itest5 OVERRIDING SYSTEM VALUE VALUES (-5, 'ff'), (DEFAULT, 'gg')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15718', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- uncompressed external toast data
INSERT INTO toasttable(toasted_col1) SELECT string_agg(g.i::text, '') FROM generate_series(1, 2000) g(i)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 893)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16953', relminmxid = '3'
WHERE oid = '"public"."idxpart31"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16853', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_para_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO combocidtest VALUES (1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6747', relminmxid = '1'
WHERE oid = '"public"."mvtest_tm"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO tab_rowfilter_3 (a, b) SELECT x, (x % 3 = 0) FROM generate_series(1, 10) x
;
----------
[DELETE|SELECT] DELETE FROM public.mvtest_boxmv mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_10.pg_temp_26444_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('1', 151)
;
----------
[INSERT] insert into euc_jis_2004_inputs  values
  ('\x666f6f',		'valid, pure ASCII'),
  ('\x666f6fbedd',	'valid'),
  ('\xa5f7',		'valid, translates to two UTF-8 chars '),
  ('\xbeddbe',		'incomplete char '),
  ('\x666f6f00bedd',	'invalid, NUL byte'),
  ('\x666f6fbe00dd',	'invalid, NUL byte'),
  ('\x666f6fbedd00',	'invalid, NUL byte'),
  ('\xbe04',		'invalid byte sequence')
;
----------
[INSERT] INSERT INTO test_prepared1 VALUES (1)
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (8,5,'4.56673929509287019456')
;
----------
[INSERT] INSERT INTO itest3 DEFAULT VALUES
;
----------
[UPDATE] UPDATE base_tbl SET b=NEW.b WHERE a=1
;
----------
[INSERT|SELECT] -- error, type mismatch

INSERT INTO arrtest (b[1:2]) VALUES(now())
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"regress_indexing"."pk51"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '756', relminmxid = '1'
WHERE oid = '"public"."point_tbl"'::pg_catalog.regclass
;
----------
[UPDATE|SELECT] UPDATE update_test SET (b,a) = (select a+1,b from update_test where a = 1000)
  WHERE a = 11
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (6,4,'732381731243.745115764094')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 408)
;
----------
[MERGE|SELECT] INSERT INTO tr_etoomuch (id, data)
SELECT g.i, -g.i FROM generate_series(8000, 12000) g(i)
ON CONFLICT(id) DO UPDATE SET data = EXCLUDED.data
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10346', relminmxid = '1'
WHERE oid = '28831'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17557', relminmxid = '3'
WHERE oid = '"public"."alpha_neg_p3"'::pg_catalog.regclass
;
----------
[INSERT] insert into Room values ('002', 'Office')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8671', relminmxid = '1'
WHERE oid = '"public"."shoe_data"'::pg_catalog.regclass
;
----------
[UPDATE] update trigger_test set v = 'update' where i = 1
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14917', relminmxid = '3'
WHERE oid = '"public"."nv_parent"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4495', relminmxid = '1'
WHERE oid = '"public"."log_table"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 278)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_2.pg_temp_28316_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_2.pg_temp_28316 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_2.pg_temp_28316 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."fk_partitioned_fk_1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into pgbench_branches(bid,bbalance) select bid, 0 from generate_series(1, 1) as bid
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (6,2,'-3224438592470.18449811926184222')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9163', relminmxid = '1'
WHERE oid = '"public"."ec1"'::pg_catalog.regclass
;
----------
[INSERT] -- Insert test data into PKTABLE
INSERT INTO PKTABLE VALUES (1, 2, 'Test1')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2584', relminmxid = '1'
WHERE oid = '"public"."iportaltest"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 447)
;
----------
[INSERT|SELECT] -- Add enough TIDs to cause the store to appear "full", compared
-- to the allocated memory it started out with. This is easier
-- with memory contexts in local memory.
INSERT INTO hideblocks (blockno)
SELECT do_set_block_offsets(blk, ARRAY[1,31,32,63,64,200]::int2[])
  FROM generate_series(1000, 2000, 1) blk
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."mlparted_def"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
