CREATE TABLE public.tst_one_comp (
		a INTEGER PRIMARY KEY,
		b public.tst_comp_basic_t
		)
;
CREATE TABLE ct(id int, rowclass text, rowid text, attribute text, val text)
;
ANALYZE pg_temp_13.pg_temp_26293_2
;
create table hp_prefix_test_p0 partition of hp_prefix_test for values with (modulus 8, remainder 0)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_am
;
CREATE MATERIALIZED VIEW refresh_test AS SELECT 1
;
create function int8alias2out(int8alias2) returns cstring
  strict immutable language internal as 'int8out'
;
CREATE OR REPLACE FUNCTION reindex_start_command()
RETURNS event_trigger AS $$
BEGIN
    RAISE NOTICE 'REINDEX START: % %', tg_event, tg_tag;
END;
$$ LANGUAGE plpgsql
;
-- generation test for v7
TRUNCATE guid1
;
drop function child_del_func()
;
-- Rules
CREATE TABLE tab_rule_stats (a int, b int)
;
create index concurrently on idxpart11 ((a/b))
;
ALTER INDEX btree_tall_idx2 ALTER COLUMN id SET (n_distinct=100)
;
create function int8alias1out(int8alias1) returns cstring
  strict immutable language internal as 'int8out'
;
CREATE TABLE list_parted2 (
	a int,
	b char
) PARTITION BY LIST (a)
;
CREATE INDEX idx_name ON employees(name)
;
CREATE TABLE tableam_parted_2_heapx PARTITION OF tableam_parted_heapx FOR VALUES IN ('c', 'd') USING heap
;
ALTER TABLE gtest10a DROP COLUMN b
;
ALTER EVENT TRIGGER split_event_probe RENAME TO split_event_probe_renamed
;
ALTER TABLE list_parted ATTACH PARTITION list_part1 for VALUES in (2,3)
;
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_partial
;
--
-- CREATE INDEX, ALTER INDEX for btrees
--

CREATE INDEX reloptions_test_idx ON reloptions_test (s) WITH (fillfactor=30)
;
-- We can remove the join even if we find the join can't duplicate rows and
-- the base quals of each side are different.  In the following case we end up
-- moving quals over to s1 to make it so it can't match any rows.
create table sl(a int, b int, c int)
;
-- identity was not copied
CREATE TABLE test_like_id_3 (LIKE test_like_id_1 INCLUDING IDENTITY)
;
CREATE OPERATOR & (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	COMMUTATOR = &,
	PROCEDURE = _int_inter
)
;
ANALYZE fast_emp4000
;
CREATE TABLE attmp4 (a int, b int, unique(a,b))
;
alter table atacc1 add primary key(a)
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_shseclabel
;
COMMENT ON CONSTRAINT "comment_test_child_fk" ON "public"."comment_test_child" IS 'FOREIGN KEY constraint of comment_test_child'
;
CREATE DOMAIN qf_char_domain AS char(1)
;
-- OK
CREATE TEMP VIEW fdv1 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
;
ANALYZE pg_temp_13.pg_temp_26675_2
;
ALTER INDEX hash_split_index SET (fillfactor = 10)
;
CREATE TABLE part2 PARTITION OF range_parted2 FOR VALUES FROM (20) TO (30)
;
ALTER TABLE tbl2 ALTER COLUMN val3 TYPE int USING val3::integer
;
-- Can't attach an index without a corresponding constraint
create table idxpart (a int, b int) partition by range (a)
;
CREATE TABLE FKTABLE (ftest1 inet REFERENCES pktable)
;
CREATE FUNCTION gbt_timetz_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create index idxpart1_1b_idx on idxpart1 (b)
;
ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = false,
    toast.vacuum_index_cleanup = true)
;
-- test inlining of polymorphic SQL functions
create function bleat(int) returns int as $$
begin
  raise notice 'bleat %', $1;
  return $1;
end$$ language plpgsql
;
CREATE PUBLICATION test_tab2_pub FOR TABLE test_tab2
;
-- changing the type of column c causes all its stats to be dropped, reverting
-- to default estimates without any statistics, i.e. 0.5% selectivity for each
-- condition
ALTER TABLE functional_dependencies ALTER COLUMN c TYPE numeric
;
CREATE SCHEMA "schema1" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
ANALYZE public.comment_test
;
CREATE OR REPLACE FUNCTION pg_catalog.pg_file_rename(text, text, text)
RETURNS bool
AS '$libdir/adminpack', 'pg_file_rename_v1_1'
LANGUAGE C VOLATILE
;
CREATE POLICY p1 ON document FOR SELECT
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
;
create table atacc1 ( test int )
;
create temp table outer_text (f1 text, f2 text)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE public.tst_one_comp (
		a INTEGER PRIMARY KEY,
		b public.tst_comp_basic_t
		)
;
----------
[CREATE_TABLE] CREATE TABLE ct(id int, rowclass text, rowid text, attribute text, val text)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26293_2
;
----------
[CREATE_TABLE] create table hp_prefix_test_p0 partition of hp_prefix_test for values with (modulus 8, remainder 0)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_am
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW refresh_test AS SELECT 1
;
----------
[CREATE_PROG_OBJ] create function int8alias2out(int8alias2) returns cstring
  strict immutable language internal as 'int8out'
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION reindex_start_command()
RETURNS event_trigger AS $$
BEGIN
    RAISE NOTICE 'REINDEX START: % %', tg_event, tg_tag;
END;
$$ LANGUAGE plpgsql
;
----------
[TRUNCATE_TABLE] -- generation test for v7
TRUNCATE guid1
;
----------
[DROP_PROG_OBJ] drop function child_del_func()
;
----------
[CREATE_TABLE] -- Rules
CREATE TABLE tab_rule_stats (a int, b int)
;
----------
[ADD_INDEX] create index concurrently on idxpart11 ((a/b))
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX btree_tall_idx2 ALTER COLUMN id SET (n_distinct=100)
;
----------
[CREATE_PROG_OBJ] create function int8alias1out(int8alias1) returns cstring
  strict immutable language internal as 'int8out'
;
----------
[CREATE_TABLE] CREATE TABLE list_parted2 (
	a int,
	b char
) PARTITION BY LIST (a)
;
----------
[ADD_INDEX] CREATE INDEX idx_name ON employees(name)
;
----------
[CREATE_TABLE] CREATE TABLE tableam_parted_2_heapx PARTITION OF tableam_parted_heapx FOR VALUES IN ('c', 'd') USING heap
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE gtest10a DROP COLUMN b
;
----------
[RENAME_TRIGGER] ALTER EVENT TRIGGER split_event_probe RENAME TO split_event_probe_renamed
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted ATTACH PARTITION list_part1 for VALUES in (2,3)
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_partial
;
----------
[ADD_INDEX] --
-- CREATE INDEX, ALTER INDEX for btrees
--

CREATE INDEX reloptions_test_idx ON reloptions_test (s) WITH (fillfactor=30)
;
----------
[CREATE_TABLE] -- We can remove the join even if we find the join can't duplicate rows and
-- the base quals of each side are different.  In the following case we end up
-- moving quals over to s1 to make it so it can't match any rows.
create table sl(a int, b int, c int)
;
----------
[CREATE_TABLE] -- identity was not copied
CREATE TABLE test_like_id_3 (LIKE test_like_id_1 INCLUDING IDENTITY)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR & (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	COMMUTATOR = &,
	PROCEDURE = _int_inter
)
;
----------
[ADMIN_TABLE] ANALYZE fast_emp4000
;
----------
[CREATE_TABLE] CREATE TABLE attmp4 (a int, b int, unique(a,b))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add primary key(a)
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_shseclabel
;
----------
[COMMENT_CONSTRAINT] COMMENT ON CONSTRAINT "comment_test_child_fk" ON "public"."comment_test_child" IS 'FOREIGN KEY constraint of comment_test_child'
;
----------
[CREATE_TYPE] CREATE DOMAIN qf_char_domain AS char(1)
;
----------
[CREATE_VIEW(SELECT)] -- OK
CREATE TEMP VIEW fdv1 AS
SELECT id, keywords, title, body, created
FROM articles
GROUP BY id
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26675_2
;
----------
[ALTER_INDEX] ALTER INDEX hash_split_index SET (fillfactor = 10)
;
----------
[CREATE_TABLE] CREATE TABLE part2 PARTITION OF range_parted2 FOR VALUES FROM (20) TO (30)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl2 ALTER COLUMN val3 TYPE int USING val3::integer
;
----------
[CREATE_TABLE] -- Can't attach an index without a corresponding constraint
create table idxpart (a int, b int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE (ftest1 inet REFERENCES pktable)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_timetz_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADD_INDEX] create index idxpart1_1b_idx on idxpart1 (b)
;
----------
[ALTER_TABLE] ALTER TABLE no_index_cleanup SET (vacuum_index_cleanup = false,
    toast.vacuum_index_cleanup = true)
;
----------
[CREATE_PROG_OBJ] -- test inlining of polymorphic SQL functions
create function bleat(int) returns int as $$
begin
  raise notice 'bleat %', $1;
  return $1;
end$$ language plpgsql
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION test_tab2_pub FOR TABLE test_tab2
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- changing the type of column c causes all its stats to be dropped, reverting
-- to default estimates without any statistics, i.e. 0.5% selectivity for each
-- condition
ALTER TABLE functional_dependencies ALTER COLUMN c TYPE numeric
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "schema1" CREATE TABLE "counted" (		type text CHECK (type IN ('total', 'delta')), 		value	integer)CREATE UNIQUE INDEX "counted_unique_total" ON "counted" (type) WHERE type = 'total'
;
----------
[ADMIN_TABLE] ANALYZE public.comment_test
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_catalog.pg_file_rename(text, text, text)
RETURNS bool
AS '$libdir/adminpack', 'pg_file_rename_v1_1'
LANGUAGE C VOLATILE
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON document FOR SELECT
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
;
----------
[CREATE_TABLE] create table atacc1 ( test int )
;
----------
[CREATE_TABLE] create temp table outer_text (f1 text, f2 text)
;
