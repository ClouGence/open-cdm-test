create table ͪߩѦ��� (��� text, ��׾�ڵ� varchar, ���1A�� char(16))
;
create table mlparted5_cd partition of mlparted5
  for values from ('c') to ('e') partition by list (c)
;
ANALYZE public.array_op_test
;
CREATE TABLE split_table_idx.typed_table OF split_table_idx.typed_row (
    id WITH OPTIONS PRIMARY KEY,
    note WITH OPTIONS DEFAULT 'typed'
)
;
ALTER TABLE ONLY "fkpart5"."pk" ATTACH PARTITION "fkpart5"."pk2" FOR VALUES IN (2)
;
-- Test out-of-range fillfactor values
CREATE INDEX hash_f8_index2 ON hash_f8_heap USING hash (random float8_ops)
	WITH (fillfactor=9)
;
ALTER TABLE pg_toast.pg_toast_30647 RENAME TO reind_con_toast
;
-- but Dave isn't allowed to anything at cid 50 or above
-- this is to make sure that we sort the policies by name first
-- when applying WITH CHECK, a later INSERT by Dave should fail due
-- to p1r first
CREATE POLICY p2r ON document AS RESTRICTIVE TO regress_rls_dave
    USING (cid <> 44 AND cid < 50)
;
CREATE TABLE tbl1 (d text)
;
CREATE DOMAIN "public"."orderedarray" AS integer[]
	CONSTRAINT "sorted" CHECK (((VALUE)[1] < (VALUE)[2]))
;
create index spgist_box_idx on spgist_box_tbl using spgist (b)
;
ALTER TABLE ONLY "public"."prt2_m" ATTACH PARTITION "public"."prt2_m_p2" FOR VALUES FROM (250, 250) TO (500, 500)
;
CREATE TABLE float8tmp (a float8)
;
TRUNCATE spc_nest
;
-- Tables referred to by security invoker views require explicit permission to
-- be locked.
CREATE VIEW lock_view8 WITH (security_invoker) AS SELECT * FROM lock_tbl1
;
CREATE TABLE droppedtest (c int)
;
ALTER TABLE pg_temp_4.pg_temp_37918_2 ADD COLUMN newdata pg_temp_4.pg_temp_37918
;
CREATE TABLE "fkpart4"."droppk1" (
    "a" integer CONSTRAINT "droppk_a_not_null" NOT NULL
)
;
create type jsb_ia as (a int[])
;
/*
 * 5.43
 * ROLE_TABLE_GRANTS view
 */

CREATE VIEW role_table_grants AS
    SELECT grantor,
           grantee,
           table_catalog,
           table_schema,
           table_name,
           privilege_type,
           is_grantable,
           with_hierarchy
    FROM table_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
CREATE AGGREGATE logging_agg_strict (text)
(
	stype = text,
	sfunc = logging_sfunc_strict,
	mstype = text,
	msfunc = logging_msfunc_strict,
	minvfunc = logging_minvfunc_strict
)
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER DATABASE contrib_regression RESET role
;
-- check that spgist index works correctly
CREATE INDEX inet_idx3 ON inet_tbl using spgist (i)
;
create table trg_clone (a int) partition by range (a)
;
ALTER TABLE pg_toast.pg_toast_30414 RENAME TO reind_con_toast
;
VACUUM (PARALLEL 2) pg_catalog.pg_ts_parser
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
ANALYZE public.time_tbl
;
ANALYZE pg_temp_12.pg_temp_25367
;
/* src/pl/plpgsql/src/plpgsql--1.0.sql */

/*
 * Currently, all the interesting stuff is done by CREATE LANGUAGE.
 * Later we will probably "dumb down" that command and put more of the
 * knowledge into this script.
 */

CREATE LANGUAGE plpgsql
;
create table parted_collate_must_match (a text collate "C", b text collate "C")
  partition by range (a)
;
ALTER TABLE pitest3_p1 ALTER COLUMN f3 DROP IDENTITY
;
-- Check use of a whole-row variable for an inlined set-returning function
CREATE FUNCTION foo_f() RETURNS SETOF foo AS
  $$ SELECT * FROM foo OFFSET 0 $$ LANGUAGE sql STABLE
;
CREATE OPERATOR CLASS gist_vbit_ops
DEFAULT FOR TYPE varbit USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bit_consistent (internal, bit, int2, oid, internal),
	FUNCTION	2	gbt_bit_union (internal, internal),
	FUNCTION	3	gbt_bit_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_bit_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bit_picksplit (internal, internal),
	FUNCTION	7	gbt_bit_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
-- fail, can't create objects owned as regress_tenant
CREATE SCHEMA regress_tenant_schema AUTHORIZATION regress_tenant
;
CREATE PUBLICATION testpub_foralltables FOR ALL TABLES WITH (publish = 'insert')
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_shdescription
;
CREATE TABLE test32_1 PARTITION OF test32 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
CREATE FUNCTION gtrgm_consistent(internal,text,smallint,oid,internal)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
VACUUM ANALYZE onek2
;
create table inh_nn2 (f2 text, f3 int) inherits (inh_nn1)
;
CREATE FUNCTION test_assign_nnint_container(x int, y int, z int)
  RETURNS nnint_container AS $$
declare v nnint_container := row(x, y);
begin
v.f2 := z;
return v;
end
$$ LANGUAGE plpgsql
;
-- check that violating rows are correctly reported
CREATE TABLE part_5_a PARTITION OF part_5 FOR VALUES IN ('a')
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_config_map
;
CREATE RULE "rule3" AS
    ON DELETE TO "public"."rule_merge1" DO INSTEAD  DELETE FROM "public"."rule_merge2"
  WHERE ("rule_merge2"."a" = "old"."a")
;
-- fails

ALTER TABLE fk_r DETACH PARTITION fk_r_1
;
create or replace function explain_analyze_without_memory(query text)
returns table (out_line text) language plpgsql
as
$$
declare
  line text;
begin
  for line in
    execute 'explain (analyze, costs off, summary off, timing off) ' || query
  loop
    out_line := regexp_replace(line, '\d+kB', 'NNkB', 'g');
    return next;
  end loop;
end;
$$
;
CLUSTER clstr_1_pkey ON clstr_1
;
DROP TABLE trigtest
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table ͪߩѦ��� (��� text, ��׾�ڵ� varchar, ���1A�� char(16))
;
----------
[CREATE_TABLE] create table mlparted5_cd partition of mlparted5
  for values from ('c') to ('e') partition by list (c)
;
----------
[ADMIN_TABLE] ANALYZE public.array_op_test
;
----------
[CREATE_TABLE] CREATE TABLE split_table_idx.typed_table OF split_table_idx.typed_row (
    id WITH OPTIONS PRIMARY KEY,
    note WITH OPTIONS DEFAULT 'typed'
)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart5"."pk" ATTACH PARTITION "fkpart5"."pk2" FOR VALUES IN (2)
;
----------
[ADD_INDEX] -- Test out-of-range fillfactor values
CREATE INDEX hash_f8_index2 ON hash_f8_heap USING hash (random float8_ops)
	WITH (fillfactor=9)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30647 RENAME TO reind_con_toast
;
----------
[CREATE_POLICY] -- but Dave isn't allowed to anything at cid 50 or above
-- this is to make sure that we sort the policies by name first
-- when applying WITH CHECK, a later INSERT by Dave should fail due
-- to p1r first
CREATE POLICY p2r ON document AS RESTRICTIVE TO regress_rls_dave
    USING (cid <> 44 AND cid < 50)
;
----------
[CREATE_TABLE] CREATE TABLE tbl1 (d text)
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."orderedarray" AS integer[]
	CONSTRAINT "sorted" CHECK (((VALUE)[1] < (VALUE)[2]))
;
----------
[ADD_INDEX] create index spgist_box_idx on spgist_box_tbl using spgist (b)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt2_m" ATTACH PARTITION "public"."prt2_m_p2" FOR VALUES FROM (250, 250) TO (500, 500)
;
----------
[CREATE_TABLE] CREATE TABLE float8tmp (a float8)
;
----------
[TRUNCATE_TABLE] TRUNCATE spc_nest
;
----------
[CREATE_VIEW(SELECT)] -- Tables referred to by security invoker views require explicit permission to
-- be locked.
CREATE VIEW lock_view8 WITH (security_invoker) AS SELECT * FROM lock_tbl1
;
----------
[CREATE_TABLE] CREATE TABLE droppedtest (c int)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_4.pg_temp_37918_2 ADD COLUMN newdata pg_temp_4.pg_temp_37918
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart4"."droppk1" (
    "a" integer CONSTRAINT "droppk_a_not_null" NOT NULL
)
;
----------
[CREATE_TYPE] create type jsb_ia as (a int[])
;
----------
[CREATE_VIEW(SELECT)] /*
 * 5.43
 * ROLE_TABLE_GRANTS view
 */

CREATE VIEW role_table_grants AS
    SELECT grantor,
           grantee,
           table_catalog,
           table_schema,
           table_name,
           privilege_type,
           is_grantable,
           with_hierarchy
    FROM table_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE logging_agg_strict (text)
(
	stype = text,
	sfunc = logging_sfunc_strict,
	mstype = text,
	msfunc = logging_msfunc_strict,
	minvfunc = logging_minvfunc_strict
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_CATALOG] ALTER DATABASE contrib_regression RESET role
;
----------
[ADD_INDEX] -- check that spgist index works correctly
CREATE INDEX inet_idx3 ON inet_tbl using spgist (i)
;
----------
[CREATE_TABLE] create table trg_clone (a int) partition by range (a)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30414 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_ts_parser
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription_rel
;
----------
[ADMIN_TABLE] ANALYZE public.time_tbl
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_25367
;
----------
[CREATE_LANGUAGE] /* src/pl/plpgsql/src/plpgsql--1.0.sql */

/*
 * Currently, all the interesting stuff is done by CREATE LANGUAGE.
 * Later we will probably "dumb down" that command and put more of the
 * knowledge into this script.
 */

CREATE LANGUAGE plpgsql
;
----------
[CREATE_TABLE] create table parted_collate_must_match (a text collate "C", b text collate "C")
  partition by range (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest3_p1 ALTER COLUMN f3 DROP IDENTITY
;
----------
[CREATE_PROG_OBJ] -- Check use of a whole-row variable for an inlined set-returning function
CREATE FUNCTION foo_f() RETURNS SETOF foo AS
  $$ SELECT * FROM foo OFFSET 0 $$ LANGUAGE sql STABLE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS gist_vbit_ops
DEFAULT FOR TYPE varbit USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_bit_consistent (internal, bit, int2, oid, internal),
	FUNCTION	2	gbt_bit_union (internal, internal),
	FUNCTION	3	gbt_bit_compress (internal),
	FUNCTION	4	gbt_var_decompress (internal),
	FUNCTION	5	gbt_bit_penalty (internal, internal, internal),
	FUNCTION	6	gbt_bit_picksplit (internal, internal),
	FUNCTION	7	gbt_bit_same (gbtreekey_var, gbtreekey_var, internal),
	STORAGE			gbtreekey_var
;
----------
[CREATE_SCHEMA] -- fail, can't create objects owned as regress_tenant
CREATE SCHEMA regress_tenant_schema AUTHORIZATION regress_tenant
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_foralltables FOR ALL TABLES WITH (publish = 'insert')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_shdescription
;
----------
[CREATE_TABLE] CREATE TABLE test32_1 PARTITION OF test32 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gtrgm_consistent(internal,text,smallint,oid,internal)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM ANALYZE onek2
;
----------
[CREATE_TABLE] create table inh_nn2 (f2 text, f3 int) inherits (inh_nn1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_assign_nnint_container(x int, y int, z int)
  RETURNS nnint_container AS $$
declare v nnint_container := row(x, y);
begin
v.f2 := z;
return v;
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] -- check that violating rows are correctly reported
CREATE TABLE part_5_a PARTITION OF part_5 FOR VALUES IN ('a')
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_config_map
;
----------
[CREATE_POLICY] CREATE RULE "rule3" AS
    ON DELETE TO "public"."rule_merge1" DO INSTEAD  DELETE FROM "public"."rule_merge2"
  WHERE ("rule_merge2"."a" = "old"."a")
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- fails

ALTER TABLE fk_r DETACH PARTITION fk_r_1
;
----------
[CREATE_PROG_OBJ] create or replace function explain_analyze_without_memory(query text)
returns table (out_line text) language plpgsql
as
$$
declare
  line text;
begin
  for line in
    execute 'explain (analyze, costs off, summary off, timing off) ' || query
  loop
    out_line := regexp_replace(line, '\d+kB', 'NNkB', 'g');
    return next;
  end loop;
end;
$$
;
----------
[ADMIN_TABLE] CLUSTER clstr_1_pkey ON clstr_1
;
----------
[DROP_TABLE] DROP TABLE trigtest
;
