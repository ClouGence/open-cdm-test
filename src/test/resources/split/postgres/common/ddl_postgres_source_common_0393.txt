-- cursor parameter name can match plpgsql variable or unreserved keyword
create function namedparmcursor_test9(p1 int) returns int4 as $$
declare
  c1 cursor (p1 int, p2 int, debug int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2
      and four = debug;
  p2 int4 := 1006;
  n int4;
begin
  -- use both supported syntaxes for named arguments
  open c1 (p1 := p1, p2 => p2, debug => 2);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
-- Test comments
COMMENT ON SEQUENCE asdf IS 'won''t work'
;
COMMENT ON INDEX comment_test_child_fk IS 'Index backing the FOREIGN KEY of comment_test_child'
;
ALTER TYPE "public"."tt_t0" DROP ATTRIBUTE "........pg.dropped.1........"
;
CREATE TABLE partitioned_table (a int, b text) PARTITION BY LIST (a)
;
DROP TABLE clustertest
;
DO LANGUAGE plpgsql $$
BEGIN
    SAVEPOINT foo;
END;
$$
;
-- Creating an index on a partitioned table makes the partitions
-- automatically get the index
create table idxpart (a int, b int, c text) partition by range (a)
;
CREATE TEMP TABLE pg_temp_4.pg_temp_29544_2 (tid pg_catalog.tid)
;
CREATE TABLE fk_partitioned_fk_1 (a int, b int)
;
CREATE SCHEMA deptest
;
drop function bark(text)
;
CREATE RULE shoelace_upd AS ON UPDATE TO shoelace
        DO INSTEAD
        UPDATE shoelace_data SET
               sl_name = NEW.sl_name,
               sl_avail = NEW.sl_avail,
               sl_color = NEW.sl_color,
               sl_len = NEW.sl_len,
               sl_unit = NEW.sl_unit
         WHERE sl_name = OLD.sl_name
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_sequence
;
ALTER INDEX pg_toast.pg_toast_30994_index RENAME TO reind_con_toast_idx
;
VACUUM pg_catalog.pg_pltemplate
;
CREATE FUNCTION autoprewarm_start_worker()
RETURNS VOID STRICT
AS '$libdir/pg_prewarm', 'autoprewarm_start_worker'
LANGUAGE C
;
CREATE TRIGGER merge_bsd BEFORE DELETE ON target FOR EACH STATEMENT EXECUTE PROCEDURE merge_trigfunc ()
;
DROP EVENT TRIGGER regress_event_trigger_drop_objects
;
CREATE PUBLICATION regress_pub FOR ALL TABLES
;
DROP MATERIALIZED VIEW mv
;
CREATE RULE "rtest_order_r2" AS
    ON INSERT TO "public"."rtest_order1" DO  INSERT INTO "public"."rtest_order2" ("a", "b", "c")
  VALUES ("new"."a", "nextval"('"public"."rtest_seq"'::"regclass"), 'rule 2 - this should run 2nd'::"text")
;
drop table at_tab2
;
CREATE TABLE moneyp_10 PARTITION OF moneyp FOR VALUES IN (10)
;
-- check that RETURNING works correctly with tuple-routing
alter table mlparted drop constraint check_b
;
/*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
DROP TABLE users, orders
;
ALTER TABLE ONLY "public"."mlparted" DROP COLUMN "........pg.dropped.5........"
;
-- conflict
alter table emp rename column salary to manager
;
create temp table temp_part partition of perm_parted default
;
DROP TABLE itest15
;
ALTER FOREIGN TABLE split_fdw_repl.remote_accounts OPTIONS (ADD fetch_size '1000', SET table_name 'accounts_remote')
;
drop table p2 cascade
;
create table join_pt1 (a int, b int, c varchar) partition by range(a)
;
ALTER TEXT SEARCH CONFIGURATION romanian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH romanian_stem
;
CREATE TABLE users_table_part_0 PARTITION OF users_table_part FOR VALUES FROM (0) TO (10)
;
CALL ptest10(null, c => 4, b => 11)
;
DROP TABLE prt
;
CREATE PUBLICATION tap_pub_4b FOR TABLE tab_rowfilter_4
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_statistic
;
CREATE FUNCTION stats_test_func1() RETURNS VOID LANGUAGE plpgsql AS $$BEGIN END;$$
;
-- Test that unique constraints are working
create table idxpart (a int, b text, primary key (a, b)) partition by range (a)
;
CREATE TABLE inhg (x text UNIQUE, LIKE inhz INCLUDING INDEXES)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_namespace
;
ALTER TABLE public.comment_test ADD CONSTRAINT comment_test_pk PRIMARY KEY (id)
;
create trigger ttserial
	before insert or update on tttest
	for each row
	execute procedure
	autoinc (price_on, ttdummy_seq)
;
CREATE TYPE split_cov.mood AS ENUM ('sad','ok','happy')
;
CREATE TABLE FKTABLE_FAIL2 ( ftest1 int, CONSTRAINT fkfail1 FOREIGN KEY (ftest1) REFERENCES PKTABLE(ptest2))
;
ANALYZE pg_temp_11.pg_temp_26571_2
;
CREATE FUNCTION cube_le(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] -- cursor parameter name can match plpgsql variable or unreserved keyword
create function namedparmcursor_test9(p1 int) returns int4 as $$
declare
  c1 cursor (p1 int, p2 int, debug int) for
    select count(*) from tenk1 where thousand = p1 and tenthous = p2
      and four = debug;
  p2 int4 := 1006;
  n int4;
begin
  -- use both supported syntaxes for named arguments
  open c1 (p1 := p1, p2 => p2, debug => 2);
  fetch c1 into n;
  return n;
end $$ language plpgsql
;
----------
[COMMENT_SEQUENCE] -- Test comments
COMMENT ON SEQUENCE asdf IS 'won''t work'
;
----------
[COMMENT_INDEX] COMMENT ON INDEX comment_test_child_fk IS 'Index backing the FOREIGN KEY of comment_test_child'
;
----------
[ALTER_TYPE] ALTER TYPE "public"."tt_t0" DROP ATTRIBUTE "........pg.dropped.1........"
;
----------
[CREATE_TABLE] CREATE TABLE partitioned_table (a int, b text) PARTITION BY LIST (a)
;
----------
[DROP_TABLE] DROP TABLE clustertest
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    SAVEPOINT foo;
END;
$$
;
----------
[CREATE_TABLE] -- Creating an index on a partitioned table makes the partitions
-- automatically get the index
create table idxpart (a int, b int, c text) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_4.pg_temp_29544_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_1 (a int, b int)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA deptest
;
----------
[DROP_PROG_OBJ] drop function bark(text)
;
----------
[CREATE_POLICY] CREATE RULE shoelace_upd AS ON UPDATE TO shoelace
        DO INSTEAD
        UPDATE shoelace_data SET
               sl_name = NEW.sl_name,
               sl_avail = NEW.sl_avail,
               sl_color = NEW.sl_color,
               sl_len = NEW.sl_len,
               sl_unit = NEW.sl_unit
         WHERE sl_name = OLD.sl_name
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_sequence
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30994_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_pltemplate
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION autoprewarm_start_worker()
RETURNS VOID STRICT
AS '$libdir/pg_prewarm', 'autoprewarm_start_worker'
LANGUAGE C
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER merge_bsd BEFORE DELETE ON target FOR EACH STATEMENT EXECUTE PROCEDURE merge_trigfunc ()
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER regress_event_trigger_drop_objects
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION regress_pub FOR ALL TABLES
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW mv
;
----------
[CREATE_POLICY] CREATE RULE "rtest_order_r2" AS
    ON INSERT TO "public"."rtest_order1" DO  INSERT INTO "public"."rtest_order2" ("a", "b", "c")
  VALUES ("new"."a", "nextval"('"public"."rtest_seq"'::"regclass"), 'rule 2 - this should run 2nd'::"text")
;
----------
[DROP_TABLE] drop table at_tab2
;
----------
[CREATE_TABLE] CREATE TABLE moneyp_10 PARTITION OF moneyp FOR VALUES IN (10)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- check that RETURNING works correctly with tuple-routing
alter table mlparted drop constraint check_b
;
----------
[CREATE_POLICY] /*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2023, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
----------
[DROP_TABLE] DROP TABLE users, orders
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted" DROP COLUMN "........pg.dropped.5........"
;
----------
[RENAME_COLUMN] -- conflict
alter table emp rename column salary to manager
;
----------
[CREATE_TABLE] create temp table temp_part partition of perm_parted default
;
----------
[DROP_TABLE] DROP TABLE itest15
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE split_fdw_repl.remote_accounts OPTIONS (ADD fetch_size '1000', SET table_name 'accounts_remote')
;
----------
[DROP_TABLE] drop table p2 cascade
;
----------
[CREATE_TABLE] create table join_pt1 (a int, b int, c varchar) partition by range(a)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION romanian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH romanian_stem
;
----------
[CREATE_TABLE] CREATE TABLE users_table_part_0 PARTITION OF users_table_part FOR VALUES FROM (0) TO (10)
;
----------
[CALL_PROG_OBJ] CALL ptest10(null, c => 4, b => 11)
;
----------
[DROP_TABLE] DROP TABLE prt
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_4b FOR TABLE tab_rowfilter_4
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_statistic
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION stats_test_func1() RETURNS VOID LANGUAGE plpgsql AS $$BEGIN END;$$
;
----------
[CREATE_TABLE] -- Test that unique constraints are working
create table idxpart (a int, b text, primary key (a, b)) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE inhg (x text UNIQUE, LIKE inhz INCLUDING INDEXES)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_namespace
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.comment_test ADD CONSTRAINT comment_test_pk PRIMARY KEY (id)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger ttserial
	before insert or update on tttest
	for each row
	execute procedure
	autoinc (price_on, ttdummy_seq)
;
----------
[CREATE_TYPE] CREATE TYPE split_cov.mood AS ENUM ('sad','ok','happy')
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE_FAIL2 ( ftest1 int, CONSTRAINT fkfail1 FOREIGN KEY (ftest1) REFERENCES PKTABLE(ptest2))
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_26571_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_le(cube, cube)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
