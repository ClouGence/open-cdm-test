CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub7
;
-- Runtime pruning on MERGE using a stable function
create function stable_one() returns int as $$ begin return 1; end; $$ language plpgsql stable
;
ALTER TYPE "public"."insenum" ADD VALUE 'i18'
;
-- Remove the before last attribute.
alter table mlparted add e int, add d int
;
CREATE TABLE utf8_inputs (inbytes bytea, description text)
;
CREATE TABLE brin_interval_test(a INTERVAL)
;
CREATE FUNCTION _ltq_extract_regex(_ltree,lquery)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE cmdata2 ALTER COLUMN f1 TYPE varchar
;
ALTER TYPE "public"."insenum" ADD VALUE 'L1'
;
-- Fail on non-existent options in toast namespace
CREATE TABLE reloptions_test2 (i int) WITH (toast.not_existing_option = 42)
;
CREATE POLICY p1 ON rls_tbl USING (EXISTS (SELECT 1 FROM ref_tbl))
;
ALTER TYPE test_type2 ADD ATTRIBUTE c text
;
DO $$
DECLARE
  result integer;
BEGIN
MERGE INTO target t
USING source AS s
ON t.tid = s.sid
WHEN MATCHED AND s.sid = 3 THEN UPDATE SET balance = t.balance + s.delta
WHEN MATCHED THEN DELETE
WHEN NOT MATCHED THEN INSERT VALUES (sid, delta);
IF FOUND THEN
  RAISE NOTICE 'Found';
ELSE
  RAISE NOTICE 'Not found';
END IF;
GET DIAGNOSTICS result := ROW_COUNT;
RAISE NOTICE 'ROW_COUNT = %', result;
END;
$$
;
CREATE TABLE "public"."d_star" (
    "class" character(1),
    "aa" integer,
    "bb" "text",
    "cc" "name",
    "dd" double precision,
    "a" "text"
)
;
ALTER OPERATOR FAMILY "public"."part_test_int4_ops" USING "hash" ADD
    FUNCTION 2 (integer, integer) "public"."part_hashint4_noop"(integer,bigint)
;
ALTER INDEX ptif_test0_index ATTACH PARTITION ptif_test01_index
;
DROP TABLE gtest_parent, gtest_child
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_constraint
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_publication_rel
;
CREATE TABLE twophase (id serial PRIMARY KEY)
;
create trigger trig_upd_after_parent after update on parted_stmt_trig
  for each row execute procedure trigger_notice()
;
CREATE TABLE test_like_gen_1 (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
ANALYZE regress_rls_schema.rls_tbl_force
;
create function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return true;
  else
    return $1::int = $2::int;
  end if;
end;
$$ language plpgsql immutable
;
CREATE TABLE "public"."fk_partitioned_fk_4_1" (
    "a" integer,
    "b" integer
)
;
-- check display of assorted RTE_FUNCTION expressions

create view tt20v as
select * from
  coalesce(1,2) as c,
  collation for ('x'::text) col,
  current_date as d,
  localtimestamp(3) as t,
  cast(1+2 as int4) as i4,
  cast(1+2 as int8) as i8
;
create table part_column_drop_1_10 partition of
  part_column_drop for values from (1) to (10)
;
-- Test that remote triggers work with insert tuple routing
create function br_insert_trigfunc() returns trigger as $$
begin
	new.b := new.b || ' triggered !';
	return new;
end
$$ language plpgsql
;
create trigger trans_tab_parent_update_trig
  after update on trans_tab_parent referencing old table as old_table
  for each statement execute procedure dump_update_old()
;
ALTER TABLE gtest29 DROP COLUMN a
;
-- Test interaction of partitioned join with partition pruning
CREATE TABLE prt1_adv (a int, b int, c varchar) PARTITION BY RANGE (a)
;
CREATE CAST (timestamptz AS interval) WITH FUNCTION age(timestamptz) AS ASSIGNMENT
;
DROP TABLE pg_temp_32.pg_temp_28153_2, pg_temp_32.pg_temp_28153
;
ALTER TABLE ONLY "public"."gtest_parent" ATTACH PARTITION "public"."gtest_child3" FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
ANALYZE public.tt5
;
-- Computation of the stats fails, no data generated.
ANALYZE test_table
;
CREATE PUBLICATION tap_pub_ins_only WITH (publish = insert)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub1, pub2
;
-- test output (and round-trip safety) of various values.
-- To ensure we're testing what we think we're testing, start with
-- float values specified by bit patterns (as a useful side effect,
-- this means we'll fail on non-IEEE platforms).

create type xfloat4
;
-- Test toast.* options
DROP TABLE reloptions_test
;
create table idxpart (col_keep int) partition by range (col_keep)
;
alter table domain_test add column d str_domain2
;
ANALYZE public.pht2_p1
;
CREATE VIEW v3 AS
    SELECT t1.a AS t1_a, t2.a AS t2_a
    FROM base_table t1, base_table2 t2
    WHERE t1.id = t2.id
;
CREATE TYPE sqljsonb_reca AS (reca sqljsonb_rec[])
;
CREATE FUNCTION wrap_do_analyze(c INT) RETURNS INT IMMUTABLE LANGUAGE SQL
	AS 'SELECT $1 FROM do_analyze()'
;
ALTER TABLE split_alter_table.base_table ALTER COLUMN payload RESET (n_distinct_inherited)
;
ANALYZE mcv_lists_partial
;
-- test jsonb int -> python int
CREATE FUNCTION test1int(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == 1)
return val
$$
;
CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.time() < $.time_tz())'))
;
------------------------------------------------------------------------------------------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14269 host=/tmp/534TSsbOop dbname=postgres' PUBLICATION pub7
;
----------
[CREATE_PROG_OBJ] -- Runtime pruning on MERGE using a stable function
create function stable_one() returns int as $$ begin return 1; end; $$ language plpgsql stable
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i18'
;
----------
[ALTER_TABLE|ADD_COLUMN] -- Remove the before last attribute.
alter table mlparted add e int, add d int
;
----------
[CREATE_TABLE] CREATE TABLE utf8_inputs (inbytes bytea, description text)
;
----------
[CREATE_TABLE] CREATE TABLE brin_interval_test(a INTERVAL)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltq_extract_regex(_ltree,lquery)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE cmdata2 ALTER COLUMN f1 TYPE varchar
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'L1'
;
----------
[CREATE_TABLE] -- Fail on non-existent options in toast namespace
CREATE TABLE reloptions_test2 (i int) WITH (toast.not_existing_option = 42)
;
----------
[CREATE_POLICY] CREATE POLICY p1 ON rls_tbl USING (EXISTS (SELECT 1 FROM ref_tbl))
;
----------
[ALTER_TYPE] ALTER TYPE test_type2 ADD ATTRIBUTE c text
;
----------
[BLOCK] DO $$
DECLARE
  result integer;
BEGIN
MERGE INTO target t
USING source AS s
ON t.tid = s.sid
WHEN MATCHED AND s.sid = 3 THEN UPDATE SET balance = t.balance + s.delta
WHEN MATCHED THEN DELETE
WHEN NOT MATCHED THEN INSERT VALUES (sid, delta);
IF FOUND THEN
  RAISE NOTICE 'Found';
ELSE
  RAISE NOTICE 'Not found';
END IF;
GET DIAGNOSTICS result := ROW_COUNT;
RAISE NOTICE 'ROW_COUNT = %', result;
END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."d_star" (
    "class" character(1),
    "aa" integer,
    "bb" "text",
    "cc" "name",
    "dd" double precision,
    "a" "text"
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY "public"."part_test_int4_ops" USING "hash" ADD
    FUNCTION 2 (integer, integer) "public"."part_hashint4_noop"(integer,bigint)
;
----------
[ALTER_INDEX] ALTER INDEX ptif_test0_index ATTACH PARTITION ptif_test01_index
;
----------
[DROP_TABLE] DROP TABLE gtest_parent, gtest_child
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_constraint
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_publication_rel
;
----------
[CREATE_TABLE] CREATE TABLE twophase (id serial PRIMARY KEY)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_upd_after_parent after update on parted_stmt_trig
  for each row execute procedure trigger_notice()
;
----------
[CREATE_TABLE] CREATE TABLE test_like_gen_1 (a int, b int GENERATED ALWAYS AS (a * 2) STORED)
;
----------
[ADMIN_TABLE] ANALYZE regress_rls_schema.rls_tbl_force
;
----------
[CREATE_PROG_OBJ] create function myinteq(myint, myint) returns bool as $$
begin
  if $1 is null and $2 is null then
    return true;
  else
    return $1::int = $2::int;
  end if;
end;
$$ language plpgsql immutable
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_4_1" (
    "a" integer,
    "b" integer
)
;
----------
[CREATE_VIEW(METADATA)] -- check display of assorted RTE_FUNCTION expressions

create view tt20v as
select * from
  coalesce(1,2) as c,
  collation for ('x'::text) col,
  current_date as d,
  localtimestamp(3) as t,
  cast(1+2 as int4) as i4,
  cast(1+2 as int8) as i8
;
----------
[CREATE_TABLE] create table part_column_drop_1_10 partition of
  part_column_drop for values from (1) to (10)
;
----------
[CREATE_PROG_OBJ] -- Test that remote triggers work with insert tuple routing
create function br_insert_trigfunc() returns trigger as $$
begin
	new.b := new.b || ' triggered !';
	return new;
end
$$ language plpgsql
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trans_tab_parent_update_trig
  after update on trans_tab_parent referencing old table as old_table
  for each statement execute procedure dump_update_old()
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE gtest29 DROP COLUMN a
;
----------
[CREATE_TABLE] -- Test interaction of partitioned join with partition pruning
CREATE TABLE prt1_adv (a int, b int, c varchar) PARTITION BY RANGE (a)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (timestamptz AS interval) WITH FUNCTION age(timestamptz) AS ASSIGNMENT
;
----------
[DROP_TABLE] DROP TABLE pg_temp_32.pg_temp_28153_2, pg_temp_32.pg_temp_28153
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."gtest_parent" ATTACH PARTITION "public"."gtest_child3" FOR VALUES FROM ('2016-09-01') TO ('2016-10-01')
;
----------
[ADMIN_TABLE] ANALYZE public.tt5
;
----------
[ADMIN_TABLE] -- Computation of the stats fails, no data generated.
ANALYZE test_table
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_ins_only WITH (publish = insert)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub1, pub2
;
----------
[CREATE_TYPE] -- test output (and round-trip safety) of various values.
-- To ensure we're testing what we think we're testing, start with
-- float values specified by bit patterns (as a useful side effect,
-- this means we'll fail on non-IEEE platforms).

create type xfloat4
;
----------
[DROP_TABLE] -- Test toast.* options
DROP TABLE reloptions_test
;
----------
[CREATE_TABLE] create table idxpart (col_keep int) partition by range (col_keep)
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table domain_test add column d str_domain2
;
----------
[ADMIN_TABLE] ANALYZE public.pht2_p1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v3 AS
    SELECT t1.a AS t1_a, t2.a AS t2_a
    FROM base_table t1, base_table2 t2
    WHERE t1.id = t2.id
;
----------
[CREATE_TYPE] CREATE TYPE sqljsonb_reca AS (reca sqljsonb_rec[])
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION wrap_do_analyze(c INT) RETURNS INT IMMUTABLE LANGUAGE SQL
	AS 'SELECT $1 FROM do_analyze()'
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE split_alter_table.base_table ALTER COLUMN payload RESET (n_distinct_inherited)
;
----------
[ADMIN_TABLE] ANALYZE mcv_lists_partial
;
----------
[CREATE_PROG_OBJ] -- test jsonb int -> python int
CREATE FUNCTION test1int(val jsonb) RETURNS int
LANGUAGE plpython3u
TRANSFORM FOR TYPE jsonb
AS $$
assert(val == 1)
return val
$$
;
----------
[ADD_INDEX] CREATE INDEX ON test_jsonb_mutability (JSON_QUERY(js, '$.a ? (@.time() < $.time_tz())'))
;
