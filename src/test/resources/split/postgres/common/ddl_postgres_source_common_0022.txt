CREATE FUNCTION bqarr_in(cstring)
RETURNS query_int
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- test type info caching in jsonb_populate_record()
CREATE TEMP TABLE jsbpoptest (js jsonb)
;
-- ERROR
TRUNCATE fd_pt2
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
-- insert will execute to completion even if function needs just 1 row
create or replace function insert_tt(text) returns int as
$$ insert into tt(data) values($1),($1||$1) returning f1 $$
language sql
;
-- Inherit test for pg_upgrade
CREATE TABLE notnull_parent_upg (a int)
;
CREATE OR REPLACE RULE myrule AS ON INSERT TO ruletest_tbl
	DO INSTEAD INSERT INTO ruletest_tbl2 VALUES (10, 10)
;
create table inh_cc1 (f2 text, f3 int) inherits (inh_pp1)
;
-- Test WHEN condition accessing system columns.
create table table_with_oids(a int)
;
CREATE TABLE collate_test_fail (
    a int COLLATE "en-x-icu",
    b text
)
;
ALTER TABLE r2 ENABLE ROW LEVEL SECURITY
;
vacuum ���������
;
CREATE TABLE gtest_part_key (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((f3))
;
-- allows easy changing of join order

CREATE TABLE hjtest_1 (a text, b int, id int, c bool)
;
CREATE AGGREGATE alt_agg2 (
  sfunc1 = int4mi, basetype = int4, stype1 = int4, initcond = -100
)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=12066 host=/tmp/Ia3hZucwmd dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
CREATE TABLE test_part_b (a int, b int) PARTITION BY LIST (a)
;
ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey DEFERRABLE INITIALLY IMMEDIATE
;
-- Not allowed
create view rw_view3 as select b as bb, a as aa from base_tbl
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=27819 host=/tmp/YLvPiW89Kg dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
DROP TABLE PKTABLE CASCADE
;
DROP TRIGGER parent_insert_trig ON range_parted
;
CREATE TABLE "public"."rtest_t7" (
    "a" integer,
    "b" "text"
)
;
CREATE TABLE unknowntab (
	u unknown    -- fail
)
;
-- Ensure correct behavior for citext with materialized views.
CREATE TABLE citext_table (
  id serial primary key,
  name citext
)
;
-- one may only update documents in 'novel' category and new dlevel must be > 0
CREATE POLICY p3 ON document FOR UPDATE
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
  WITH CHECK (dlevel > 0)
;
ANALYZE c_star
;
ALTER FUNCTION gbt_intv_fetch(internal) PARALLEL SAFE
;
CREATE OPERATOR @> (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_isparent,
        COMMUTATOR = '<@',
        RESTRICT = ltreeparentsel,
	JOIN = contjoinsel
)
;
CREATE COLLATION ctest_nondet (provider = icu, locale = '', deterministic = false)
;
CREATE FUNCTION unaccent(text)
	RETURNS text
	AS '$libdir/unaccent', 'unaccent_dict'
	LANGUAGE C STABLE STRICT PARALLEL SAFE
;
CREATE FUNCTION isneq(ismn, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE AGGREGATE myaggn06a(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
--
-- Tables and rules for the multiple cascaded qualified instead
-- rule test
--
create table rtest_t4 (a int4, b text)
;
VACUUM no_index_cleanup
;
CREATE TABLE gtest_varlena (a varchar, b varchar GENERATED ALWAYS AS (a) STORED)
;
CREATE MATERIALIZED VIEW mvtest2 AS SELECT 1 AS x WITH NO DATA
;
ANALYZE public.fk_partitioned_fk_1
;
CREATE CAST (ismn AS ismn13) WITHOUT FUNCTION AS ASSIGNMENT
;
DROP TABLE ptnowner
;
create table idxpart (a int, b int, primary key (a, b)) partition by range (a, b)
;
CREATE TRIGGER transition_table_level2_bad_usage_trigger
  AFTER DELETE ON transition_table_level2
  REFERENCING OLD TABLE AS dx
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level2_bad_usage_func()
;
-- inheritance
CREATE TABLE gtest1_1 () INHERITS (gtest1)
;
--
-- check that pruning works properly when the partition key is of a
-- pseudotype
--

-- array type list partition key
create table pp_arrpart (a int[]) partition by list (a)
;
CREATE OR REPLACE FUNCTION test_slru_page_sync(int) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_sync' LANGUAGE C
;
ALTER TABLE ONLY "public"."notnull_tbl4_cld2"
    ADD CONSTRAINT "notnull_tbl4_cld2_pkey" PRIMARY KEY ("a") DEFERRABLE
;
CREATE TEXT SEARCH TEMPLATE alt_ts_temp1 (lexize=dsimple_lexize)
;
VACUUM (FULL) pg_catalog.pg_opfamily
;
CREATE POLICY "p1" ON "public"."ec1" USING (("f1" OPERATOR("public".<) '5'::"public"."int8alias1"))
;
drop trigger child1_delete_trig on child1
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION bqarr_in(cstring)
RETURNS query_int
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] -- test type info caching in jsonb_populate_record()
CREATE TEMP TABLE jsbpoptest (js jsonb)
;
----------
[TRUNCATE_TABLE] -- ERROR
TRUNCATE fd_pt2
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
----------
[CREATE_PROG_OBJ] -- insert will execute to completion even if function needs just 1 row
create or replace function insert_tt(text) returns int as
$$ insert into tt(data) values($1),($1||$1) returning f1 $$
language sql
;
----------
[CREATE_TABLE] -- Inherit test for pg_upgrade
CREATE TABLE notnull_parent_upg (a int)
;
----------
[CREATE_POLICY] CREATE OR REPLACE RULE myrule AS ON INSERT TO ruletest_tbl
	DO INSTEAD INSERT INTO ruletest_tbl2 VALUES (10, 10)
;
----------
[CREATE_TABLE] create table inh_cc1 (f2 text, f3 int) inherits (inh_pp1)
;
----------
[CREATE_TABLE] -- Test WHEN condition accessing system columns.
create table table_with_oids(a int)
;
----------
[CREATE_TABLE] CREATE TABLE collate_test_fail (
    a int COLLATE "en-x-icu",
    b text
)
;
----------
[ALTER_TABLE] ALTER TABLE r2 ENABLE ROW LEVEL SECURITY
;
----------
[ADMIN_TABLE] vacuum ���������
;
----------
[CREATE_TABLE] CREATE TABLE gtest_part_key (f1 date NOT NULL, f2 bigint, f3 bigint GENERATED ALWAYS AS (f2 * 2) STORED) PARTITION BY RANGE ((f3))
;
----------
[CREATE_TABLE] -- allows easy changing of join order

CREATE TABLE hjtest_1 (a text, b int, id int, c bool)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE alt_agg2 (
  sfunc1 = int4mi, basetype = int4, stype1 = int4, initcond = -100
)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=12066 host=/tmp/Ia3hZucwmd dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
----------
[CREATE_TABLE] CREATE TABLE test_part_b (a int, b int) PARTITION BY LIST (a)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE fktable ALTER CONSTRAINT fktable_fk_fkey DEFERRABLE INITIALLY IMMEDIATE
;
----------
[CREATE_VIEW(SELECT)] -- Not allowed
create view rw_view3 as select b as bb, a as aa from base_tbl
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=27819 host=/tmp/YLvPiW89Kg dbname=postgres application_name=tap_sub' PUBLICATION tap_pub WITH (streaming = on)
;
----------
[DROP_TABLE] DROP TABLE PKTABLE CASCADE
;
----------
[DROP_TRIGGER] DROP TRIGGER parent_insert_trig ON range_parted
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_t7" (
    "a" integer,
    "b" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE unknowntab (
	u unknown    -- fail
)
;
----------
[CREATE_TABLE] -- Ensure correct behavior for citext with materialized views.
CREATE TABLE citext_table (
  id serial primary key,
  name citext
)
;
----------
[CREATE_POLICY] -- one may only update documents in 'novel' category and new dlevel must be > 0
CREATE POLICY p3 ON document FOR UPDATE
  USING (cid = (SELECT cid from category WHERE cname = 'novel'))
  WITH CHECK (dlevel > 0)
;
----------
[ADMIN_TABLE] ANALYZE c_star
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_fetch(internal) PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @> (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_isparent,
        COMMUTATOR = '<@',
        RESTRICT = ltreeparentsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_POLICY] CREATE COLLATION ctest_nondet (provider = icu, locale = '', deterministic = false)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION unaccent(text)
	RETURNS text
	AS '$libdir/unaccent', 'unaccent_dict'
	LANGUAGE C STABLE STRICT PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ismn, ean13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn06a(BASETYPE = int, SFUNC = tf2p, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_TABLE] --
-- Tables and rules for the multiple cascaded qualified instead
-- rule test
--
create table rtest_t4 (a int4, b text)
;
----------
[ADMIN_TABLE] VACUUM no_index_cleanup
;
----------
[CREATE_TABLE] CREATE TABLE gtest_varlena (a varchar, b varchar GENERATED ALWAYS AS (a) STORED)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest2 AS SELECT 1 AS x WITH NO DATA
;
----------
[ADMIN_TABLE] ANALYZE public.fk_partitioned_fk_1
;
----------
[CREATE_PROG_OBJ] CREATE CAST (ismn AS ismn13) WITHOUT FUNCTION AS ASSIGNMENT
;
----------
[DROP_TABLE] DROP TABLE ptnowner
;
----------
[CREATE_TABLE] create table idxpart (a int, b int, primary key (a, b)) partition by range (a, b)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER transition_table_level2_bad_usage_trigger
  AFTER DELETE ON transition_table_level2
  REFERENCING OLD TABLE AS dx
  FOR EACH STATEMENT EXECUTE PROCEDURE
    transition_table_level2_bad_usage_func()
;
----------
[CREATE_TABLE] -- inheritance
CREATE TABLE gtest1_1 () INHERITS (gtest1)
;
----------
[CREATE_TABLE] --
-- check that pruning works properly when the partition key is of a
-- pseudotype
--

-- array type list partition key
create table pp_arrpart (a int[]) partition by list (a)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_sync(int) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_sync' LANGUAGE C
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."notnull_tbl4_cld2"
    ADD CONSTRAINT "notnull_tbl4_cld2_pkey" PRIMARY KEY ("a") DEFERRABLE
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH TEMPLATE alt_ts_temp1 (lexize=dsimple_lexize)
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_opfamily
;
----------
[CREATE_POLICY] CREATE POLICY "p1" ON "public"."ec1" USING (("f1" OPERATOR("public".<) '5'::"public"."int8alias1"))
;
----------
[DROP_TRIGGER] drop trigger child1_delete_trig on child1
;
