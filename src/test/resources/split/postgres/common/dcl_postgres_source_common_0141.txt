EXECUTE dumpFunc('28627')
;
execute pstmt_def_insert(2)
;
SET XML OPTION CONTENT
;
-- Should work. Sample text-book case
BEGIN TRANSACTION
;
EXECUTE dumpFunc('36505')
;
LOCK TABLE pg_catalog.pg_description IN ACCESS SHARE MODE
;
-- Make sure that we don't print any JIT-related information, as that
-- would also make results unstable.
SET jit = off
;
-- ok, regress_tenant can create objects within the database
SET SESSION AUTHORIZATION regress_tenant
;
LOCK lock_tbl1 IN ROW SHARE MODE
;
LOCK TABLE "public"."credit_usage" IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('28627')
;
----------
[UNSAFE] execute pstmt_def_insert(2)
;
----------
[SESSION_SETTING_WRITE] SET XML OPTION CONTENT
;
----------
[TRANSACTION] -- Should work. Sample text-book case
BEGIN TRANSACTION
;
----------
[UNSAFE] EXECUTE dumpFunc('36505')
;
----------
[SESSION_LOCK] LOCK TABLE pg_catalog.pg_description IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] -- Make sure that we don't print any JIT-related information, as that
-- would also make results unstable.
SET jit = off
;
----------
[SWITCH_USER] -- ok, regress_tenant can create objects within the database
SET SESSION AUTHORIZATION regress_tenant
;
----------
[SESSION_LOCK] LOCK lock_tbl1 IN ROW SHARE MODE
;
----------
[SESSION_LOCK] LOCK TABLE "public"."credit_usage" IN ACCESS SHARE MODE
;
