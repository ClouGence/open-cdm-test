INSERT INTO BOOLTBL2 (f1) VALUES (bool 'FALSE')
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."pk1"'::pg_catalog.regclass
  AND attname IN ('a')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17991', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9264', relminmxid = '1'
WHERE oid = '"public"."brin_summarize_bloom"'::pg_catalog.regclass
;
--
-- check ALTER CONSTRAINT
--

INSERT INTO fktable VALUES (1, 5)
;
INSERT INTO public.tht VALUES ('8', 838)
;
DELETE FROM test_tab WHERE (a > 2)
;
INSERT INTO TIMESTAMPTZ_TBL VALUES ('1997-01-02 03:04:05')
;
INSERT INTO savepoints VALUES (6)
;
INSERT INTO public.mvtest_mv SELECT (diff.newdata).* FROM pg_temp_11.pg_temp_26230_2 diff WHERE tid IS NULL
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10505', relminmxid = '1'
WHERE oid = '29352'
;
INSERT INTO public.tht VALUES ('4', 424)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."itest7d"'::pg_catalog.regclass
;
-- default is immediate so this should fail right away
UPDATE unique_tbl SET i = 1 WHERE i = 0
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtest20"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14685', relminmxid = '3'
WHERE oid = '"public"."part_p2_p1"'::pg_catalog.regclass
;
INSERT INTO DATE_TBL VALUES ('2040-04-10')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '"public"."mlparted12"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"generated_virtual_tests"."gtestnn_childdef"'::pg_catalog.regclass AND
conname IN ('gtestnn_parent_f3_not_null', 'gtestnn_parent_c_not_null')
;
INSERT INTO user_cat VALUES (1)
;
update PSlot set pfname = new.name where pfname = old.name
;
INSERT INTO pxtest2 VALUES (2)
;
-- need an additional row in j2, if we want j2_id1_idx to be preferred
insert into j2 values(1,2)
;
-- should fail

UPDATE rw_view1 SET a = a + 5
;
INSERT INTO replication_example(somedata, text) VALUES (3, 3)
;
INSERT INTO mcv_lists (a, b, c, d)
     SELECT
         NULL, -- always NULL
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 'x' END),
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 0 END),
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 'x' END)
     FROM generate_series(1,5000) s(i)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1182', relminmxid = '1'
WHERE oid = '17110'
;
INSERT INTO num_exp_add VALUES (7,0,'-83028485')
;
INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(3, 5000) s(i)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtesty"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '"public"."trigger_parted_p2_2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15658', relminmxid = '3'
WHERE oid = '"public"."test_inh_check_child"'::pg_catalog.regclass
;
INSERT INTO test_tab VALUES (12, sha256(12::text::bytea))
;
INSERT INTO public.tht VALUES ('6', 226)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '35670'
;
insert into fkeys2 values (30, '3', 2)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13490', relminmxid = '3'
WHERE oid = '"public"."customer"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3037', relminmxid = '1'
WHERE oid = '"public"."tbl_include_unique1"'::pg_catalog.regclass
;
INSERT INTO num_exp_div VALUES (4,7,'-.09393717604145131637')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8455', relminmxid = '1'
WHERE oid = '27635'
;
INSERT INTO mcv_lists_arrays (a, b, c)
     SELECT
         ARRAY[fipshash((i/100)::text), fipshash((i/100-1)::text), fipshash((i/100+1)::text)],
         ARRAY[(i/100-1)::numeric/1000, (i/100)::numeric/1000, (i/100+1)::numeric/1000],
         ARRAY[(i/100-1), i/100, (i/100+1)]
     FROM generate_series(1,5000) s(i)
;
-- inference fails:
insert into insertconflicttest values (3, 'Kiwi') on conflict (key, fruit) do update set fruit = excluded.fruit
;
DELETE FROM t_012_tbl
;
UPDATE ONLY "pg_temp_4"."tasks" SET "checked_by" = $1 WHERE $2 OPERATOR(pg_catalog.=) "checked_by"
;
INSERT INTO tab_rowfilter_greater_10k (a, b) VALUES(16000, 103)
;
-- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '478', datminmxid = '1'
WHERE datname = 'postgres'
;
INSERT INTO public.tht VALUES ('1', 451)
;
insert into PSlot values ('PS.base.c6', 'PF0_1', '', 'WS.003.3b')
;
INSERT INTO public.tht VALUES ('7', 327)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14555', relminmxid = '3'
WHERE oid = '"public"."dropcolumnanother"'::pg_catalog.regclass
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] INSERT INTO BOOLTBL2 (f1) VALUES (bool 'FALSE')
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"fkpart5"."pk1"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17991', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9264', relminmxid = '1'
WHERE oid = '"public"."brin_summarize_bloom"'::pg_catalog.regclass
;
----------
[INSERT] --
-- check ALTER CONSTRAINT
--

INSERT INTO fktable VALUES (1, 5)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('8', 838)
;
----------
[DELETE] DELETE FROM test_tab WHERE (a > 2)
;
----------
[INSERT] INSERT INTO TIMESTAMPTZ_TBL VALUES ('1997-01-02 03:04:05')
;
----------
[INSERT] INSERT INTO savepoints VALUES (6)
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_mv SELECT (diff.newdata).* FROM pg_temp_11.pg_temp_26230_2 diff WHERE tid IS NULL
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10505', relminmxid = '1'
WHERE oid = '29352'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 424)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."itest7d"'::pg_catalog.regclass
;
----------
[UPDATE] -- default is immediate so this should fail right away
UPDATE unique_tbl SET i = 1 WHERE i = 0
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtest20"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14685', relminmxid = '3'
WHERE oid = '"public"."part_p2_p1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO DATE_TBL VALUES ('2040-04-10')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6392', relminmxid = '1'
WHERE oid = '"public"."mlparted12"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"generated_virtual_tests"."gtestnn_childdef"'::pg_catalog.regclass AND
conname IN ('gtestnn_parent_f3_not_null', 'gtestnn_parent_c_not_null')
;
----------
[INSERT] INSERT INTO user_cat VALUES (1)
;
----------
[UPDATE] update PSlot set pfname = new.name where pfname = old.name
;
----------
[INSERT] INSERT INTO pxtest2 VALUES (2)
;
----------
[INSERT] -- need an additional row in j2, if we want j2_id1_idx to be preferred
insert into j2 values(1,2)
;
----------
[UPDATE] -- should fail

UPDATE rw_view1 SET a = a + 5
;
----------
[INSERT|SELECT] INSERT INTO replication_example(somedata, text) VALUES (3, 3)
;
----------
[INSERT|SELECT] INSERT INTO mcv_lists (a, b, c, d)
     SELECT
         NULL, -- always NULL
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 'x' END),
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 0 END),
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 'x' END)
     FROM generate_series(1,5000) s(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1182', relminmxid = '1'
WHERE oid = '17110'
;
----------
[INSERT] INSERT INTO num_exp_add VALUES (7,0,'-83028485')
;
----------
[INSERT|SELECT] INSERT INTO test_tab SELECT i, md5(i::text) FROM generate_series(3, 5000) s(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"public"."gtesty"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '"public"."trigger_parted_p2_2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15658', relminmxid = '3'
WHERE oid = '"public"."test_inh_check_child"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO test_tab VALUES (12, sha256(12::text::bytea))
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 226)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '13981', relminmxid = '3'
WHERE oid = '35670'
;
----------
[INSERT] insert into fkeys2 values (30, '3', 2)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '13490', relminmxid = '3'
WHERE oid = '"public"."customer"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3037', relminmxid = '1'
WHERE oid = '"public"."tbl_include_unique1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (4,7,'-.09393717604145131637')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8455', relminmxid = '1'
WHERE oid = '27635'
;
----------
[INSERT|SELECT] INSERT INTO mcv_lists_arrays (a, b, c)
     SELECT
         ARRAY[fipshash((i/100)::text), fipshash((i/100-1)::text), fipshash((i/100+1)::text)],
         ARRAY[(i/100-1)::numeric/1000, (i/100)::numeric/1000, (i/100+1)::numeric/1000],
         ARRAY[(i/100-1), i/100, (i/100+1)]
     FROM generate_series(1,5000) s(i)
;
----------
[MERGE] -- inference fails:
insert into insertconflicttest values (3, 'Kiwi') on conflict (key, fruit) do update set fruit = excluded.fruit
;
----------
[DELETE] DELETE FROM t_012_tbl
;
----------
[UPDATE] UPDATE ONLY "pg_temp_4"."tasks" SET "checked_by" = $1 WHERE $2 OPERATOR(pg_catalog.=) "checked_by"
;
----------
[INSERT|SELECT] INSERT INTO tab_rowfilter_greater_10k (a, b) VALUES(16000, 103)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set datfrozenxid and datminmxid.
UPDATE pg_catalog.pg_database
SET datfrozenxid = '478', datminmxid = '1'
WHERE datname = 'postgres'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 451)
;
----------
[INSERT] insert into PSlot values ('PS.base.c6', 'PF0_1', '', 'WS.003.3b')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('7', 327)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14555', relminmxid = '3'
WHERE oid = '"public"."dropcolumnanother"'::pg_catalog.regclass
;
