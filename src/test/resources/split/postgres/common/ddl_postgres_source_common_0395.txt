CREATE MATERIALIZED VIEW mvtest_mv2 AS SELECT * FROM mvtest_mv1
  WHERE col1 = (SELECT LEAST(col1) FROM mvtest_mv1) WITH NO DATA
;
-- fail (not superuser)
SECURITY LABEL ON ROLE regress_dummy_seclabel_user4 IS 'unclassified'
;
CREATE FUNCTION rwagg_sfunc(x anyarray, y anyarray) RETURNS anyarray
LANGUAGE plpgsql IMMUTABLE AS $$
BEGIN
    RETURN array_fill(y[1], ARRAY[4]);
END;
$$
;
ALTER TABLE remember_node_subid ALTER c TYPE bigint
;
DROP SEQUENCE myseq3
;
ALTER TABLE ONLY "public"."tableam_parted_heap2" ATTACH PARTITION "public"."tableam_parted_b_heap2" FOR VALUES IN ('b')
;
-- test if default partition works as expected for multi-level partitioned
-- table as well as when default partition itself is further partitioned
drop table part_default
;
CREATE SUBSCRIPTION mysub CONNECTION 'port=20351 host=/tmp/egrE9XeeZj dbname=postgres' PUBLICATION mypub
;
ALTER INDEX part_attmp_pkey RENAME TO part_attmp_index
;
CREATE FUNCTION gbt_oid_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
-- fail
alter domain posint add constraint c2 check(value > 0)
;
create temp table gstest3 (a integer, b integer, c integer, d integer)
;
ALTER INDEX dump_test.index_with_stats
						   ALTER COLUMN 3 SET STATISTICS 500
;
-- While here, also check matching when creating an index after the fact.
create index on idxpart1 ((a+b)) where d = true
;
alter index parted_replica_idx_1 ATTACH PARTITION parted_replica_idx_11
;
CREATE OPERATOR ~~ (
	LEFTARG = query_int,
	RIGHTARG = _int4,
	PROCEDURE = rboolop,
	COMMUTATOR = '@@',
	RESTRICT = _int_matchsel,
	JOIN = contjoinsel
)
;
ALTER TABLE gtest27
  ALTER COLUMN a TYPE float8,
  ALTER COLUMN b TYPE float8
;
analyze ma_test
;
create database abc with template template1
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_operator
;
-- check that ALTER TABLE ... ALTER TYPE does the right thing

CREATE TABLE vtype( a integer)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
CREATE FUNCTION dblink_open (text, text)
RETURNS text
AS '$libdir/dblink','dblink_open'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE prt2_e_p1 PARTITION OF prt2_e FOR VALUES FROM (0) TO (250)
;
CREATE TYPE dump_test.textrange
					   AS RANGE (subtype=text, collation="C")
;
ALTER TABLE dump_test.test_third_table DROP COLUMN junk
;
DROP VIEW tv
;
CREATE FUNCTION isexists(hstore,text)
RETURNS bool
AS '$libdir/hstore','hstore_exists'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TRIGGER trig_row_after_delete
AFTER DELETE ON rem1
FOR EACH ROW
WHEN (OLD.f2 like '%update%')
EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
alter table atacc1 alter column test type integer using 0
;
ALTER TABLE tbl2 ADD COLUMN val3 FLOAT
;
CREATE TABLE part2_1 PARTITION OF partitioned2 FOR VALUES FROM (-1, 'aaaaa') TO (100, 'ccccc')
;
CREATE TABLE t2 (a integer, b text)
;
--
-- injection_points_run()
--
-- Executes the action attached to the injection point.
--
CREATE FUNCTION injection_points_run(IN point_name TEXT,
    IN arg TEXT DEFAULT NULL)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_run'
LANGUAGE C PARALLEL UNSAFE
;
-- do some inequality tests
CREATE TABLE brin_test_multi_1 (a INT, b BIGINT) WITH (fillfactor=10)
;
create table wcowrtest (a int) partition by list (a)
;
ANALYZE public.shoelace_ok
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_attrdef
;
CREATE EXTENSION earthdistance
;
-- Verify that attaching to a parent with two identical constraints work
CREATE TABLE fk_partitioned_pk_6 (a int PRIMARY KEY)
;
DROP TRIGGER trig_row_before_insupd2 ON rem1
;
DROP FUNCTION testpub_rf_func2()
;
CREATE TABLE "public"."pagg_tab_ml_p3_s2" (
    "b" integer,
    "c" "text",
    "a" integer
)
;
CREATE TABLE part_7 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
) PARTITION BY LIST (b)
;
ALTER TABLE pg_toast.pg_toast_33248 RENAME TO reind_con_toast
;
CREATE TABLE "public"."beta_neg_p2" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
CREATE OPERATOR || (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_addltree
)
;
-- cannot add column to a partition
ALTER TABLE fd_pt2_1 ADD c4 char
;
CREATE TEXT SEARCH CONFIGURATION en (copy=english)
;
CREATE TABLE fk (a int REFERENCES pk DEFERRABLE INITIALLY IMMEDIATE)
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv2 AS SELECT * FROM mvtest_mv1
  WHERE col1 = (SELECT LEAST(col1) FROM mvtest_mv1) WITH NO DATA
;
----------
[UNKNOWN] -- fail (not superuser)
SECURITY LABEL ON ROLE regress_dummy_seclabel_user4 IS 'unclassified'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION rwagg_sfunc(x anyarray, y anyarray) RETURNS anyarray
LANGUAGE plpgsql IMMUTABLE AS $$
BEGIN
    RETURN array_fill(y[1], ARRAY[4]);
END;
$$
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE remember_node_subid ALTER c TYPE bigint
;
----------
[DROP_SEQUENCE] DROP SEQUENCE myseq3
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."tableam_parted_heap2" ATTACH PARTITION "public"."tableam_parted_b_heap2" FOR VALUES IN ('b')
;
----------
[DROP_TABLE] -- test if default partition works as expected for multi-level partitioned
-- table as well as when default partition itself is further partitioned
drop table part_default
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION mysub CONNECTION 'port=20351 host=/tmp/egrE9XeeZj dbname=postgres' PUBLICATION mypub
;
----------
[RENAME_INDEX] ALTER INDEX part_attmp_pkey RENAME TO part_attmp_index
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_oid_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[ALTER_TYPE] -- fail
alter domain posint add constraint c2 check(value > 0)
;
----------
[CREATE_TABLE] create temp table gstest3 (a integer, b integer, c integer, d integer)
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX dump_test.index_with_stats
						   ALTER COLUMN 3 SET STATISTICS 500
;
----------
[ADD_INDEX] -- While here, also check matching when creating an index after the fact.
create index on idxpart1 ((a+b)) where d = true
;
----------
[ALTER_INDEX] alter index parted_replica_idx_1 ATTACH PARTITION parted_replica_idx_11
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~~ (
	LEFTARG = query_int,
	RIGHTARG = _int4,
	PROCEDURE = rboolop,
	COMMUTATOR = '@@',
	RESTRICT = _int_matchsel,
	JOIN = contjoinsel
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE gtest27
  ALTER COLUMN a TYPE float8,
  ALTER COLUMN b TYPE float8
;
----------
[ADMIN_TABLE] analyze ma_test
;
----------
[CREATE_CATALOG] create database abc with template template1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_operator
;
----------
[CREATE_TABLE] -- check that ALTER TABLE ... ALTER TYPE does the right thing

CREATE TABLE vtype( a integer)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_open (text, text)
RETURNS text
AS '$libdir/dblink','dblink_open'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE prt2_e_p1 PARTITION OF prt2_e FOR VALUES FROM (0) TO (250)
;
----------
[CREATE_POLICY] CREATE TYPE dump_test.textrange
					   AS RANGE (subtype=text, collation="C")
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE dump_test.test_third_table DROP COLUMN junk
;
----------
[DROP_VIEW] DROP VIEW tv
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isexists(hstore,text)
RETURNS bool
AS '$libdir/hstore','hstore_exists'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_row_after_delete
AFTER DELETE ON rem1
FOR EACH ROW
WHEN (OLD.f2 like '%update%')
EXECUTE PROCEDURE trigger_data(23,'skidoo')
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter column test type integer using 0
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE tbl2 ADD COLUMN val3 FLOAT
;
----------
[CREATE_TABLE] CREATE TABLE part2_1 PARTITION OF partitioned2 FOR VALUES FROM (-1, 'aaaaa') TO (100, 'ccccc')
;
----------
[CREATE_TABLE] CREATE TABLE t2 (a integer, b text)
;
----------
[CREATE_PROG_OBJ] --
-- injection_points_run()
--
-- Executes the action attached to the injection point.
--
CREATE FUNCTION injection_points_run(IN point_name TEXT,
    IN arg TEXT DEFAULT NULL)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_run'
LANGUAGE C PARALLEL UNSAFE
;
----------
[CREATE_TABLE] -- do some inequality tests
CREATE TABLE brin_test_multi_1 (a INT, b BIGINT) WITH (fillfactor=10)
;
----------
[CREATE_TABLE] create table wcowrtest (a int) partition by list (a)
;
----------
[ADMIN_TABLE] ANALYZE public.shoelace_ok
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_attrdef
;
----------
[CREATE_LIBRARY] CREATE EXTENSION earthdistance
;
----------
[CREATE_TABLE] -- Verify that attaching to a parent with two identical constraints work
CREATE TABLE fk_partitioned_pk_6 (a int PRIMARY KEY)
;
----------
[DROP_TRIGGER] DROP TRIGGER trig_row_before_insupd2 ON rem1
;
----------
[DROP_PROG_OBJ] DROP FUNCTION testpub_rf_func2()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_ml_p3_s2" (
    "b" integer,
    "c" "text",
    "a" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE part_7 (
	LIKE list_parted2,
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
) PARTITION BY LIST (b)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33248 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE "public"."beta_neg_p2" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR || (
        LEFTARG = ltree,
	RIGHTARG = ltree,
	PROCEDURE = ltree_addltree
)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- cannot add column to a partition
ALTER TABLE fd_pt2_1 ADD c4 char
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION en (copy=english)
;
----------
[CREATE_TABLE] CREATE TABLE fk (a int REFERENCES pk DEFERRABLE INITIALLY IMMEDIATE)
;
