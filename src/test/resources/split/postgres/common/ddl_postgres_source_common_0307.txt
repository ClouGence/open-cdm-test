CREATE TABLE tab1_1 (b text, c text DEFAULT 'sub1_tab1', a int NOT NULL)
;
CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 OID = 16384 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_amproc
;
CREATE SUBSCRIPTION "pg_createsubscriber_16385_b9eb2afa" CONNECTION 'host=''/tmp/nxZdRqejP7'' port=13926 dbname=''regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ''' PUBLICATION "pub2" WITH (create_slot = false, enabled = false, slot_name = 'replslot2', copy_data = false, two_phase = true)
;
ALTER INDEX pg_toast.pg_toast_28191_index RENAME TO reind_con_toast_idx
;
DROP VIEW json_object_view
;
CREATE INDEX t2_hash ON s4.t2 USING HASH (i)
;
ALTER TABLE ONLY "generated_virtual_tests"."gtestnn_parent" ATTACH PARTITION "generated_virtual_tests"."gtestnn_child" FOR VALUES FROM (1) TO (5)
;
ALTER TABLE regress_tblspace_test_tbl SET TABLESPACE regress_tblspace
;
create table pc_list_part_null partition of pc_list_parted for values in (null)
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_foreign_data_wrapper
;
-- no such operator by that name
drop operator === (int4, int4)
;
-- succeeds
ALTER TABLE attmp3 VALIDATE CONSTRAINT b_greater_than_ten_not_enforced
;
ALTER TABLE pg_toast.pg_toast_33336 RENAME TO reind_con_toast
;
CREATE TABLE test_mix_1 (a int PRIMARY KEY, b int, c int)
;
CREATE OR REPLACE FUNCTION event_trigger_report_end()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'END: command_tag=% type=% identity=%',
            r.command_tag, r.object_type, r.object_identity;
    END LOOP;
END; $$
;
create table matest0 (a int, b int, c int, d int)
;
DROP TABLESPACE tblspc3
;
-- cleanup
DROP TABLE bttest_a
;
-- Change the datatype of all the columns. ALTER TABLE is optimized to not
-- rebuild an index if the new data type is binary compatible with the old
-- one. Check do a dummy ALTER TABLE that doesn't change the datatype
-- first, to test that no-op codepath, and another one that does.
ALTER TABLE comment_test ALTER COLUMN indexed_col SET DATA TYPE int
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_conversion
;
create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('DROP EVENT TRIGGER')
   execute procedure test_event_trigger()
;
-- should fail

-- Check that domain constraints on plpgsql function parameters, results,
-- and local variables are enforced correctly.

create function doubledecrement(p1 pos_int) returns pos_int as $$
declare v pos_int;
begin
    return p1;
end$$ language plpgsql
;
CREATE FUNCTION is_valid(ismn)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- tuple order might be unstable here

drop table inhpar cascade
;
do
$$
  declare procedure int;  -- check we still recognize non-keywords as vars
  begin
  create function test1() returns int
    begin atomic
      select 2 + 2;
    end;
  create or replace procedure test2(x int)
    begin atomic
      select x + 2;
    end;
  end
$$
;
CREATE TABLE test_tab (a int primary key, b bytea, c timestamptz DEFAULT now(), d bigint DEFAULT 999)
;
CREATE TABLE part_7_a_null (
	c int,
	d int,
	e int,
	LIKE list_parted2,  -- 'a' will have attnum = 4
	CONSTRAINT check_b CHECK (b IS NULL OR b = 'a'),
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
)
;
CREATE TEMP TABLE pg_temp_26.pg_temp_28293_2 (tid pg_catalog.tid)
;
create table mcrparted5 partition of mcrparted for values from (20, 20, 20) to (maxvalue, maxvalue, maxvalue) partition by list (a)
;
ALTER TABLE ONLY "public"."dropcolumnchild" DROP COLUMN "........pg.dropped.1........"
;
ALTER FUNCTION gbt_macad8_picksplit(internal, internal) PARALLEL SAFE
;
ALTER TABLE ONLY "public"."trigger_parted_p1"
    ADD CONSTRAINT "trigger_parted_p1_pkey" PRIMARY KEY ("a")
;
ALTER INDEX pg_toast.pg_toast_28057_index RENAME TO reind_con_toast_idx
;
ALTER TABLE s.x ADD CONSTRAINT underqualified EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE coll WITH s.=)
  WHERE (s.index_row_if(y))
;
ALTER OPERATOR > (ltree, ltree)
  SET (RESTRICT = scalargtsel, JOIN = scalargtjoinsel)
;
ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "uint" WITH "simple"
;
create function stacked_diagnostics_test() returns void as $$
declare _sqlstate text;
        _message text;
        _context text;
begin
  perform zero_divide();
exception when others then
  get stacked diagnostics
        _sqlstate = returned_sqlstate,
        _message = message_text,
        _context = pg_exception_context;
  raise notice 'sqlstate: %, message: %, context: [%]',
    _sqlstate, _message, replace(_context, E'\n', ' <- ');
end;
$$ language plpgsql
;
CREATE TABLE tab_fk_ref (id int PRIMARY KEY, bid int REFERENCES tab_fk (bid), junk text)
;
DROP TABLE pg_temp_3.pg_temp_29533_2, pg_temp_3.pg_temp_29533
;
create function aggf_trans(aggtype[],integer,integer,text) returns aggtype[]
as 'select array_append($1,ROW($2,$3,$4)::aggtype)'
language sql strict immutable
;
CREATE TABLE "public"."gtest_child3" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 33)) STORED
)
;
-- straight index variant
CREATE TABLE test_replica_identity3 (id int NOT NULL)
;
alter database abc allow_connections false
;
CREATE TABLE "fkpart5"."pk31" (
    "a" integer CONSTRAINT "pk3_a_not_null" NOT NULL
)
;
ALTER FUNCTION heap_page_item_attrs(bytea, regclass) PARALLEL RESTRICTED
;
create table c1 () inherits(p1)
;
DROP PUBLICATION testpub2
;
CREATE TABLE "public"."cmdata" (
    "f1" "text"
)
;
-- check that the constraint matches in definition with parent's constraint
ALTER TABLE fail_part ADD CONSTRAINT check_a CHECK (a >= 0)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE tab1_1 (b text, c text DEFAULT 'sub1_tab1', a int NOT NULL)
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression\""\	 !""#$%&'()*+,-\\""\\\" WITH TEMPLATE = template0 OID = 16384 ENCODING = 'UTF8' LOCALE_PROVIDER = builtin LOCALE = 'C' BUILTIN_LOCALE = 'C.UTF-8' COLLATION_VERSION = '1'
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_amproc
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION "pg_createsubscriber_16385_b9eb2afa" CONNECTION 'host=''/tmp/nxZdRqejP7'' port=13926 dbname=''regression./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ''' PUBLICATION "pub2" WITH (create_slot = false, enabled = false, slot_name = 'replslot2', copy_data = false, two_phase = true)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28191_index RENAME TO reind_con_toast_idx
;
----------
[DROP_VIEW] DROP VIEW json_object_view
;
----------
[ADD_INDEX] CREATE INDEX t2_hash ON s4.t2 USING HASH (i)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "generated_virtual_tests"."gtestnn_parent" ATTACH PARTITION "generated_virtual_tests"."gtestnn_child" FOR VALUES FROM (1) TO (5)
;
----------
[ALTER_TABLE] ALTER TABLE regress_tblspace_test_tbl SET TABLESPACE regress_tblspace
;
----------
[CREATE_TABLE] create table pc_list_part_null partition of pc_list_parted for values in (null)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_foreign_data_wrapper
;
----------
[DROP_PROG_OBJ] -- no such operator by that name
drop operator === (int4, int4)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] -- succeeds
ALTER TABLE attmp3 VALIDATE CONSTRAINT b_greater_than_ten_not_enforced
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33336 RENAME TO reind_con_toast
;
----------
[CREATE_TABLE] CREATE TABLE test_mix_1 (a int PRIMARY KEY, b int, c int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION event_trigger_report_end()
 RETURNS event_trigger
 LANGUAGE plpgsql
AS $$
DECLARE r RECORD;
BEGIN
    FOR r IN SELECT * FROM pg_event_trigger_ddl_commands()
    LOOP
        RAISE NOTICE 'END: command_tag=% type=% identity=%',
            r.command_tag, r.object_type, r.object_identity;
    END LOOP;
END; $$
;
----------
[CREATE_TABLE] create table matest0 (a int, b int, c int, d int)
;
----------
[DROP_TABLESPACE] DROP TABLESPACE tblspc3
;
----------
[DROP_TABLE] -- cleanup
DROP TABLE bttest_a
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- Change the datatype of all the columns. ALTER TABLE is optimized to not
-- rebuild an index if the new data type is binary compatible with the old
-- one. Check do a dummy ALTER TABLE that doesn't change the datatype
-- first, to test that no-op codepath, and another one that does.
ALTER TABLE comment_test ALTER COLUMN indexed_col SET DATA TYPE int
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_conversion
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger regress_event_trigger2 on ddl_command_start
   when tag in ('DROP EVENT TRIGGER')
   execute procedure test_event_trigger()
;
----------
[CREATE_PROG_OBJ] -- should fail

-- Check that domain constraints on plpgsql function parameters, results,
-- and local variables are enforced correctly.

create function doubledecrement(p1 pos_int) returns pos_int as $$
declare v pos_int;
begin
    return p1;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION is_valid(ismn)
	RETURNS boolean
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_TABLE] -- tuple order might be unstable here

drop table inhpar cascade
;
----------
[BLOCK] do
$$
  declare procedure int;  -- check we still recognize non-keywords as vars
  begin
  create function test1() returns int
    begin atomic
      select 2 + 2;
    end;
  create or replace procedure test2(x int)
    begin atomic
      select x + 2;
    end;
  end
$$
;
----------
[CREATE_TABLE] CREATE TABLE test_tab (a int primary key, b bytea, c timestamptz DEFAULT now(), d bigint DEFAULT 999)
;
----------
[CREATE_TABLE] CREATE TABLE part_7_a_null (
	c int,
	d int,
	e int,
	LIKE list_parted2,  -- 'a' will have attnum = 4
	CONSTRAINT check_b CHECK (b IS NULL OR b = 'a'),
	CONSTRAINT check_a CHECK (a IS NOT NULL AND a = 7)
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_26.pg_temp_28293_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table mcrparted5 partition of mcrparted for values from (20, 20, 20) to (maxvalue, maxvalue, maxvalue) partition by list (a)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."dropcolumnchild" DROP COLUMN "........pg.dropped.1........"
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad8_picksplit(internal, internal) PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."trigger_parted_p1"
    ADD CONSTRAINT "trigger_parted_p1_pkey" PRIMARY KEY ("a")
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28057_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE s.x ADD CONSTRAINT underqualified EXCLUDE USING btree
  ((s.index_this_expr(y, s.const())) COLLATE coll WITH s.=)
  WHERE (s.index_row_if(y))
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR > (ltree, ltree)
  SET (RESTRICT = scalargtsel, JOIN = scalargtjoinsel)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."ispell_tst"
    ADD MAPPING FOR "uint" WITH "simple"
;
----------
[CREATE_PROG_OBJ] create function stacked_diagnostics_test() returns void as $$
declare _sqlstate text;
        _message text;
        _context text;
begin
  perform zero_divide();
exception when others then
  get stacked diagnostics
        _sqlstate = returned_sqlstate,
        _message = message_text,
        _context = pg_exception_context;
  raise notice 'sqlstate: %, message: %, context: [%]',
    _sqlstate, _message, replace(_context, E'\n', ' <- ');
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE tab_fk_ref (id int PRIMARY KEY, bid int REFERENCES tab_fk (bid), junk text)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_29533_2, pg_temp_3.pg_temp_29533
;
----------
[CREATE_PROG_OBJ] create function aggf_trans(aggtype[],integer,integer,text) returns aggtype[]
as 'select array_append($1,ROW($2,$3,$4)::aggtype)'
language sql strict immutable
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest_child3" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 33)) STORED
)
;
----------
[CREATE_TABLE] -- straight index variant
CREATE TABLE test_replica_identity3 (id int NOT NULL)
;
----------
[ALTER_CATALOG] alter database abc allow_connections false
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."pk31" (
    "a" integer CONSTRAINT "pk3_a_not_null" NOT NULL
)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION heap_page_item_attrs(bytea, regclass) PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] create table c1 () inherits(p1)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub2
;
----------
[CREATE_TABLE] CREATE TABLE "public"."cmdata" (
    "f1" "text"
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- check that the constraint matches in definition with parent's constraint
ALTER TABLE fail_part ADD CONSTRAINT check_a CHECK (a >= 0)
;
