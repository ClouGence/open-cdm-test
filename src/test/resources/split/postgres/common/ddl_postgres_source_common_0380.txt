--error, notnull_tbl1_3 have null values
ALTER TABLE notnull_tbl1_3 VALIDATE CONSTRAINT nn3
;
ALTER TABLE test_replica_identity3 ALTER COLUMN id DROP NOT NULL
;
DROP TRANSFORM IF EXISTS FOR hstore LANGUAGE plperl
;
CREATE FUNCTION dblink_fetch (text, int, boolean)
RETURNS setof record
AS '$libdir/dblink','dblink_fetch'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE TABLE "public"."invalid_check_con_child" (
    "f1" integer,
    CONSTRAINT "inh_check_constraint" CHECK (("f1" > 0))
)
;
CREATE OPERATOR <@ (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contained,
	COMMUTATOR = '@>',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE TABLE tru_rtable1 (id int primary key)
;
CREATE POLICY p ON t USING (max(c))
;
alter table regular_table alter column b set storage external
;
CALL test_proc10(_a, b => _b)
;
DROP TABLE forcetest
;
-- test case adapted from subselect.sql
CREATE TEMP TABLE outer_text (f1 text COLLATE case_insensitive, f2 text)
;
DROP TABLE pg_temp_11.pg_temp_25373_2, pg_temp_11.pg_temp_25373
;
ANALYZE pg_temp_13.pg_temp_25576
;
CREATE FUNCTION gbt_macad8_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_foreign_server
;
-- test slices with empty lower and/or upper index
CREATE TEMP TABLE arrtest_s (
  a       int2[],
  b       int2[][]
)
;
CREATE TABLE h2part_2 PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 8, REMAINDER 0)
;
CREATE FUNCTION gbt_float4_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
create function polyf(x anycompatiblemultirange, y anycompatiblearray) returns anycompatiblemultirange as $$
  select x
$$ language sql
;
CREATE EXTENSION IF NOT EXISTS postgres_fdw
;
DROP TRIGGER d1_delete_trig ON part_d_1_15
;
CREATE TABLE pp_nn_1 PARTITION OF pp_nn FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
ALTER OPERATOR ^<@ (ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0
;
ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_4" FOR VALUES IN (3500, 3502)
;
IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest2
  OPTIONS (import_default 'true')
;
CREATE TABLE atest5 (one int, two int unique, three int, four int unique)
;
CREATE TABLE parted_uniq_tbl_2 PARTITION OF parted_uniq_tbl FOR VALUES FROM (20) TO (30)
;
DROP VIEW rls_v
;
ALTER INDEX pg_toast.pg_toast_27744_index RENAME TO reind_con_toast_idx
;
/* Test multiple inheritance of column defaults */

CREATE TABLE firstparent (tomorrow date default now()::date + 1)
;
CREATE TABLE tbl_include_pk (c1 int, c2 int, c3 int, c4 box)
;
-- error
CALL random()
;
create trigger tg_chkslotname before insert
    on IFace for each row execute procedure tg_chkslotname('IF')
;
ALTER TABLE public.pktable ADD CONSTRAINT pktable_pkey PRIMARY KEY (ptest1, ptest2)
;
drop table parallel_write
;
CREATE DATABASE noconndb ALLOW_CONNECTIONS false
;
ALTER TABLE pitest1 ATTACH PARTITION pitest1_p2 FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
CREATE TABLE grantor_test1 ()
;
drop function outer_outer_func(int)
;
DROP TABLE IF EXISTS test CASCADE
;
-- ************************************************************
-- * View of a patchfield describing backside and patches
-- ************************************************************
create view Pfield_v1 as select PF.pfname, PF.slotname,
	pslot_backlink_view(PF.slotname) as backside,
	pslot_slotlink_view(PF.slotname) as patch
    from PSlot PF
;
CREATE TABLE "public"."quad_poly_tbl" (
    "id" integer,
    "p" "polygon"
)
;
DROP TABLE cwi_test
;
-- miscellaneous

CREATE FUNCTION seg_center(seg)
RETURNS float4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
drop table atacc2 cascade
;
create sequence test_sequence
;
CREATE TABLE "public"."notnull_tbl4_lk2" (
    "a" integer CONSTRAINT "notnull_tbl4_a_not_null" NOT NULL
)
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_CONSTRAINT] --error, notnull_tbl1_3 have null values
ALTER TABLE notnull_tbl1_3 VALIDATE CONSTRAINT nn3
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_replica_identity3 ALTER COLUMN id DROP NOT NULL
;
----------
[DROP_TRANSFORM] DROP TRANSFORM IF EXISTS FOR hstore LANGUAGE plperl
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_fetch (text, int, boolean)
RETURNS setof record
AS '$libdir/dblink','dblink_fetch'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_TABLE] CREATE TABLE "public"."invalid_check_con_child" (
    "f1" integer,
    CONSTRAINT "inh_check_constraint" CHECK (("f1" > 0))
)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <@ (
	LEFTARG = hstore,
	RIGHTARG = hstore,
	PROCEDURE = hs_contained,
	COMMUTATOR = '@>',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE tru_rtable1 (id int primary key)
;
----------
[CREATE_POLICY] CREATE POLICY p ON t USING (max(c))
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table regular_table alter column b set storage external
;
----------
[CALL_PROG_OBJ] CALL test_proc10(_a, b => _b)
;
----------
[DROP_TABLE] DROP TABLE forcetest
;
----------
[CREATE_TABLE] -- test case adapted from subselect.sql
CREATE TEMP TABLE outer_text (f1 text COLLATE case_insensitive, f2 text)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_25373_2, pg_temp_11.pg_temp_25373
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25576
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad8_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_foreign_server
;
----------
[CREATE_TABLE] -- test slices with empty lower and/or upper index
CREATE TEMP TABLE arrtest_s (
  a       int2[],
  b       int2[][]
)
;
----------
[CREATE_TABLE] CREATE TABLE h2part_2 PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 8, REMAINDER 0)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_float4_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] create function polyf(x anycompatiblemultirange, y anycompatiblearray) returns anycompatiblemultirange as $$
  select x
$$ language sql
;
----------
[CREATE_LIBRARY] CREATE EXTENSION IF NOT EXISTS postgres_fdw
;
----------
[DROP_TRIGGER] DROP TRIGGER d1_delete_trig ON part_d_1_15
;
----------
[CREATE_TABLE] CREATE TABLE pp_nn_1 PARTITION OF pp_nn FOR VALUES WITH (MODULUS 2, REMAINDER 0)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR ^<@ (ltree, ltree)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_TABLE|SELECT] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE IF NOT EXISTS ctas_ine_tbl AS SELECT 1 / 0
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_4" FOR VALUES IN (3500, 3502)
;
----------
[CREATE_TABLE] IMPORT FOREIGN SCHEMA import_source FROM SERVER loopback INTO import_dest2
  OPTIONS (import_default 'true')
;
----------
[CREATE_TABLE] CREATE TABLE atest5 (one int, two int unique, three int, four int unique)
;
----------
[CREATE_TABLE] CREATE TABLE parted_uniq_tbl_2 PARTITION OF parted_uniq_tbl FOR VALUES FROM (20) TO (30)
;
----------
[DROP_VIEW] DROP VIEW rls_v
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27744_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] /* Test multiple inheritance of column defaults */

CREATE TABLE firstparent (tomorrow date default now()::date + 1)
;
----------
[CREATE_TABLE] CREATE TABLE tbl_include_pk (c1 int, c2 int, c3 int, c4 box)
;
----------
[CALL_PROG_OBJ] -- error
CALL random()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_chkslotname before insert
    on IFace for each row execute procedure tg_chkslotname('IF')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.pktable ADD CONSTRAINT pktable_pkey PRIMARY KEY (ptest1, ptest2)
;
----------
[DROP_TABLE] drop table parallel_write
;
----------
[CREATE_CATALOG] CREATE DATABASE noconndb ALLOW_CONNECTIONS false
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pitest1 ATTACH PARTITION pitest1_p2 FOR VALUES FROM ('2016-08-01') TO ('2016-09-01')
;
----------
[CREATE_TABLE] CREATE TABLE grantor_test1 ()
;
----------
[DROP_PROG_OBJ] drop function outer_outer_func(int)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS test CASCADE
;
----------
[CREATE_VIEW(SELECT)] -- ************************************************************
-- * View of a patchfield describing backside and patches
-- ************************************************************
create view Pfield_v1 as select PF.pfname, PF.slotname,
	pslot_backlink_view(PF.slotname) as backside,
	pslot_slotlink_view(PF.slotname) as patch
    from PSlot PF
;
----------
[CREATE_TABLE] CREATE TABLE "public"."quad_poly_tbl" (
    "id" integer,
    "p" "polygon"
)
;
----------
[DROP_TABLE] DROP TABLE cwi_test
;
----------
[CREATE_PROG_OBJ] -- miscellaneous

CREATE FUNCTION seg_center(seg)
RETURNS float4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] drop table atacc2 cascade
;
----------
[CREATE_SEQUENCE] create sequence test_sequence
;
----------
[CREATE_TABLE] CREATE TABLE "public"."notnull_tbl4_lk2" (
    "a" integer CONSTRAINT "notnull_tbl4_a_not_null" NOT NULL
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_subscription
;
