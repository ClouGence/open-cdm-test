INSERT INTO fkpart13_t2 (part_id) VALUES (1)
;
INSERT INTO public.tht VALUES ('2', 322)
;
DELETE FROM tr_etoomuch WHERE id < 5000
;
insert into fk_notpartitioned_pk (a, b)
  select 2048, x from generate_series(1,10) x
;
UPDATE "S 1"."T 1" SET c2 = 44 WHERE ((c2 = 4))
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6265', relminmxid = '1'
WHERE oid = '"public"."array_op_test"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15378', relminmxid = '3'
WHERE oid = '38881'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12709', relminmxid = '3'
WHERE oid = '"fkpart3"."pk1"'::pg_catalog.regclass
;
INSERT INTO num_exp_sub VALUES (7,0,'-818934540071845742')
;
INSERT INTO temporal_rng3 (id, valid_at) VALUES ('[1,2)', daterange('2018-03-03', '2018-04-04'))
;
-- Cannot cast to uuid on assignment.
INSERT INTO caster (uuid)          VALUES ('a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::text)
;
INSERT INTO num_exp_div VALUES (7,2,'2.41794207151503385700')
;
INSERT INTO public.tplain VALUES ('9', 789)
;
INSERT INTO pg_temp_24.pg_temp_26555_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_24.pg_temp_26555 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_24.pg_temp_26555 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO public.tplain VALUES ('3', 983)
;
UPDATE xy SET y = y + 256 WHERE x = 1 AND pg_advisory_lock(1) IS NOT NULL
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."cminh"'::pg_catalog.regclass
  AND attname IN ('f1')
;
INSERT INTO hash_cleanup_heap SELECT 1 FROM generate_series(1, 500) as i
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17924', relminmxid = '3'
WHERE oid = '"public"."undroppable_objs"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1567', relminmxid = '1'
WHERE oid = '17385'
;
-- updates aren't supported
INSERT INTO agg_csv VALUES(1,2.0)
;
INSERT INTO LINE_TBL VALUES ('{0,0,1')
;
INSERT INTO public.ths_p2 VALUES (3, 943)
;
insert into inh_temp_child_2 values (4)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"public"."pitest2_p2"'::pg_catalog.regclass
;
-- try varchar's of greater than 1 length
INSERT INTO VARCHAR_TBL (f1) VALUES ('cd')
;
INSERT INTO public.tht VALUES ('6', 6)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."mlparted_defd"'::pg_catalog.regclass
;
INSERT INTO VARCHAR_TBL (f1) VALUES ('abcd')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '26024'
;
insert into capitals values ('Las Vegas', 5.83E+5, 2222, 'NV') on conflict (name) do update set population = excluded.population
;
-- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.9........'
  AND attrelid = '"public"."mlparted11"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('3', 383)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3796', relminmxid = '1'
WHERE oid = '"public"."copy_tbl"'::pg_catalog.regclass
;
INSERT INTO mcv_lists_uuid (a, b, c)
     SELECT
         md5(mod(i,100)::text)::uuid,
         md5(mod(i,50)::text)::uuid,
         md5(mod(i,25)::text)::uuid
     FROM generate_series(1,5000) s(i)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '"public"."xacttest"'::pg_catalog.regclass
;
INSERT INTO LINE_TBL VALUES (line(point '(3,1)', point '(3,2)'))
;
INSERT INTO public.ths_p2 VALUES (7, 487)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '"public"."mlparted2"'::pg_catalog.regclass
;
update insupd_test set a = 4 where a = 3
;
UPDATE "public"."fk_partitioned_fk" SET "a" = NULL, "b" = NULL WHERE $1 OPERATOR(pg_catalog.=) "a" AND $2 OPERATOR(pg_catalog.=) "b"
;
insert into hpart2 values (2, 5)
;
UPDATE pgbench_accounts SET abalance = abalance + 3689 WHERE aid = 24740
;
UPDATE test1 SET b = b || ' ' || b WHERE a = r.a
;
INSERT INTO pg_temp_88.pg_temp_30263_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_88.pg_temp_30263 AS newdata FROM public.parallel_mat_view mv FULL JOIN pg_temp_88.pg_temp_30263 newdata ON (newdata.length OPERATOR(pg_catalog.=) mv.length AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest_normal_child"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6544', relminmxid = '1'
WHERE oid = '"public"."agg_data_20k"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('1', 1)
;
INSERT INTO pg_temp_10.pg_temp_26364_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_10.pg_temp_26364 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_10.pg_temp_26364 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
INSERT INTO public.tplain VALUES ('0', 480)
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] INSERT INTO fkpart13_t2 (part_id) VALUES (1)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('2', 322)
;
----------
[DELETE] DELETE FROM tr_etoomuch WHERE id < 5000
;
----------
[INSERT|SELECT] insert into fk_notpartitioned_pk (a, b)
  select 2048, x from generate_series(1,10) x
;
----------
[UPDATE] UPDATE "S 1"."T 1" SET c2 = 44 WHERE ((c2 = 4))
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6265', relminmxid = '1'
WHERE oid = '"public"."array_op_test"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '15378', relminmxid = '3'
WHERE oid = '38881'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12709', relminmxid = '3'
WHERE oid = '"fkpart3"."pk1"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_sub VALUES (7,0,'-818934540071845742')
;
----------
[INSERT|SELECT] INSERT INTO temporal_rng3 (id, valid_at) VALUES ('[1,2)', daterange('2018-03-03', '2018-04-04'))
;
----------
[INSERT|SELECT] -- Cannot cast to uuid on assignment.
INSERT INTO caster (uuid)          VALUES ('a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::text)
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (7,2,'2.41794207151503385700')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('9', 789)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_24.pg_temp_26555_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_24.pg_temp_26555 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_24.pg_temp_26555 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 983)
;
----------
[UPDATE|SESSION_LOCK] UPDATE xy SET y = y + 256 WHERE x = 1 AND pg_advisory_lock(1) IS NOT NULL
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."cminh"'::pg_catalog.regclass
  AND attname IN ('f1')
;
----------
[INSERT|SELECT] INSERT INTO hash_cleanup_heap SELECT 1 FROM generate_series(1, 500) as i
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17924', relminmxid = '3'
WHERE oid = '"public"."undroppable_objs"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1567', relminmxid = '1'
WHERE oid = '17385'
;
----------
[INSERT] -- updates aren't supported
INSERT INTO agg_csv VALUES(1,2.0)
;
----------
[INSERT] INSERT INTO LINE_TBL VALUES ('{0,0,1')
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 943)
;
----------
[INSERT] insert into inh_temp_child_2 values (4)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"public"."pitest2_p2"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- try varchar's of greater than 1 length
INSERT INTO VARCHAR_TBL (f1) VALUES ('cd')
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 6)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."mlparted_defd"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO VARCHAR_TBL (f1) VALUES ('abcd')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '26024'
;
----------
[MERGE] insert into capitals values ('Las Vegas', 5.83E+5, 2222, 'NV') on conflict (name) do update set population = excluded.population
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.9........'
  AND attrelid = '"public"."mlparted11"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 383)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '3796', relminmxid = '1'
WHERE oid = '"public"."copy_tbl"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO mcv_lists_uuid (a, b, c)
     SELECT
         md5(mod(i,100)::text)::uuid,
         md5(mod(i,50)::text)::uuid,
         md5(mod(i,25)::text)::uuid
     FROM generate_series(1,5000) s(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '"public"."xacttest"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO LINE_TBL VALUES (line(point '(3,1)', point '(3,2)'))
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 487)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6263', relminmxid = '1'
WHERE oid = '"public"."mlparted2"'::pg_catalog.regclass
;
----------
[UPDATE] update insupd_test set a = 4 where a = 3
;
----------
[UPDATE] UPDATE "public"."fk_partitioned_fk" SET "a" = NULL, "b" = NULL WHERE $1 OPERATOR(pg_catalog.=) "a" AND $2 OPERATOR(pg_catalog.=) "b"
;
----------
[INSERT] insert into hpart2 values (2, 5)
;
----------
[UPDATE] UPDATE pgbench_accounts SET abalance = abalance + 3689 WHERE aid = 24740
;
----------
[UPDATE] UPDATE test1 SET b = b || ' ' || b WHERE a = r.a
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_88.pg_temp_30263_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_88.pg_temp_30263 AS newdata FROM public.parallel_mat_view mv FULL JOIN pg_temp_88.pg_temp_30263 newdata ON (newdata.length OPERATOR(pg_catalog.=) mv.length AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest_normal_child"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6544', relminmxid = '1'
WHERE oid = '"public"."agg_data_20k"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 1)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_10.pg_temp_26364_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_10.pg_temp_26364 AS newdata FROM public.mvtest_mv_v mv FULL JOIN pg_temp_10.pg_temp_26364 newdata ON (newdata.ii OPERATOR(pg_catalog.=) mv.ii AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 480)
;
