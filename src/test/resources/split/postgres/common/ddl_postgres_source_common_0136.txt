-- nondeterministic collations

CREATE COLLATION ctest_det (provider = icu, locale = '', deterministic = true)
;
-- Test re-RAISE inside a nested exception block.  This case is allowed
-- by Oracle's PL/SQL but was handled differently by PG before 9.1.

CREATE FUNCTION reraise_test() RETURNS void AS $$
BEGIN
   BEGIN
       RAISE syntax_error;
   EXCEPTION
       WHEN syntax_error THEN
           BEGIN
               raise notice 'exception % thrown in inner block, reraising', sqlerrm;
               RAISE;
           EXCEPTION
               WHEN OTHERS THEN
                   raise notice 'RIGHT - exception % caught in inner block', sqlerrm;
           END;
   END;
EXCEPTION
   WHEN OTHERS THEN
       raise notice 'WRONG - exception % caught in outer block', sqlerrm;
END;
$$ LANGUAGE plpgsql
;
CREATE UNIQUE INDEX upserttest_key_uniq_idx ON upserttest((blurt_and_lock_123(key)))
;
CREATE FUNCTION test_dsa_resowners()
	RETURNS pg_catalog.void
	AS '$libdir/test_dsa' LANGUAGE C
;
CREATE TABLE unlogged1 (a int) PARTITION BY RANGE (a)
;
CREATE DOMAIN testdomain_i AS int COLLATE "sv_SE"
;
-- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."renamecolumnchild" INHERIT "public"."renamecolumn"
;
CREATE UNIQUE INDEX t2_btree_unique ON s1.t2 USING BTREE (i)
;
/*
 * 5.53
 * ROUTINE_TABLE_USAGE view
 */

CREATE VIEW routine_table_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class t, pg_namespace nt

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = t.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND pg_has_role(t.relowner, 'USAGE')
;
CREATE FUNCTION isnle(isbn, isbn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE withoid() WITH (oids = true)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_operator
;
CREATE TABLE "public"."defaultexpr_tbl" (
    "i1" integer DEFAULT (100 + ((200 - 199) * 2)),
    "i2" integer DEFAULT "nextval"('"public"."default_seq"'::"regclass")
)
;
-- Tests for WITH NO DATA and column name consistency
CREATE TABLE ctas_base (i int, j int)
;
ALTER TABLE pg_parameter_acl ADD PRIMARY KEY USING INDEX pg_parameter_acl_oid_index
;
CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
ALTER TABLE pg_tablespace ADD UNIQUE USING INDEX pg_tablespace_spcname_index
;
create unique index part_comp_key_index on insertconflicttest(key, fruit) where key < 5
;
ALTER TABLE parent ADD COLUMN b serial
;
CREATE TABLE pk3 PARTITION OF pk FOR VALUES IN (3)
;
-- ok

-- circular references
ALTER TABLE truncate_a ADD FOREIGN KEY (col1) REFERENCES trunc_c
;
-- alter noexisting table
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c4 integer
;
CREATE SCHEMA import_dest5
;
-- CREATE/DROP COLLATION

CREATE COLLATION builtin_c ( PROVIDER = builtin, LOCALE = "C" )
;
CREATE AGGREGATE newcnt (*) (
   sfunc = int8inc, stype = int8,
   initcond = '0', parallel = safe
)
;
-- create a normal heap table and insert some rows.
-- use a temp table so that vacuum behavior doesn't depend on global xmin
create temp table htab (a int)
;
create function anycompatiblerange_anycompatiblemultirange_func(r anycompatiblerange, mr anycompatiblemultirange)
  returns anycompatible as 'select lower($1) + lower($2);' language sql
;
DROP TABLE IF EXISTS split_lock_next.parent
;
CREATE TABLE test_interval (
	i interval
)
;
CREATE RULE bug6051_3_ins AS ON INSERT TO bug6051_3 DO INSTEAD
  SELECT i FROM bug6051_2
;
create index part_column_drop_d_expr on part_column_drop((d = 2))
;
create function tg_pline_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from PLine where slotname = old.slotname;
	insert into PLine (
		    slotname,
		    phonenumber,
		    comment,
		    backlink
		) values (
		    new.slotname,
		    new.phonenumber,
		    new.comment,
		    new.backlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
CREATE FUNCTION isneq(ismn, ismn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = upc,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
ANALYZE public.ec1
;
DROP TABLE regress_roleoption.t2
;
COMMENT ON TEXT SEARCH DICTIONARY russian_stem IS 'snowball stemmer for russian language'
;
ALTER INDEX pg_toast.pg_toast_30868_index RENAME TO reind_con_toast_idx
;
drop trigger part_ins_trig on parted_copytest_a2
;
create table inh_p2(f1 int not null)
;
CREATE INDEX gtest31_2_y_idx ON generated_stored_tests.gtest31_2 USING btree (((y).b))
;
ALTER TABLE test_oat_schema.test_oat_tab ENABLE TRIGGER test_oat_trigger
;
CREATE TABLE instead_of_insert_tbl(id serial, name text)
;
ANALYZE public.shoe_data
;
DROP TABLE batch_cp_upd_test1
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_subscription
;
ALTER INDEX dummy_test_idx RESET (option_string_null)
;
COMMENT ON FUNCTION boolop(_int4, query_int) IS 'boolean operation with array'
;
CREATE FUNCTION "public"."test_evtrig_dropped_objects"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
    LOOP
        IF obj.object_type = 'table' THEN
                EXECUTE format('DROP TABLE IF EXISTS audit_tbls.%I',
					format('%s_%s', obj.schema_name, obj.object_name));
        END IF;

	INSERT INTO dropped_objects
		(type, schema, object) VALUES
		(obj.object_type, obj.schema_name, obj.object_identity);
    END LOOP;
END
$$
;
ANALYZE public.defpart_attach_test_2
;
------------------------------------------------------------------------------------------
[CREATE_POLICY] -- nondeterministic collations

CREATE COLLATION ctest_det (provider = icu, locale = '', deterministic = true)
;
----------
[CREATE_PROG_OBJ] -- Test re-RAISE inside a nested exception block.  This case is allowed
-- by Oracle's PL/SQL but was handled differently by PG before 9.1.

CREATE FUNCTION reraise_test() RETURNS void AS $$
BEGIN
   BEGIN
       RAISE syntax_error;
   EXCEPTION
       WHEN syntax_error THEN
           BEGIN
               raise notice 'exception % thrown in inner block, reraising', sqlerrm;
               RAISE;
           EXCEPTION
               WHEN OTHERS THEN
                   raise notice 'RIGHT - exception % caught in inner block', sqlerrm;
           END;
   END;
EXCEPTION
   WHEN OTHERS THEN
       raise notice 'WRONG - exception % caught in outer block', sqlerrm;
END;
$$ LANGUAGE plpgsql
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX upserttest_key_uniq_idx ON upserttest((blurt_and_lock_123(key)))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_dsa_resowners()
	RETURNS pg_catalog.void
	AS '$libdir/test_dsa' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE unlogged1 (a int) PARTITION BY RANGE (a)
;
----------
[CREATE_TYPE] CREATE DOMAIN testdomain_i AS int COLLATE "sv_SE"
;
----------
[ALTER_TABLE] -- For binary upgrade, set up inheritance this way.
ALTER TABLE ONLY "public"."renamecolumnchild" INHERIT "public"."renamecolumn"
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX t2_btree_unique ON s1.t2 USING BTREE (i)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.53
 * ROUTINE_TABLE_USAGE view
 */

CREATE VIEW routine_table_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name,
           CAST(current_database() AS sql_identifier) AS routine_catalog,
           CAST(np.nspname AS sql_identifier) AS routine_schema,
           CAST(p.proname AS sql_identifier) AS routine_name,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nt.nspname AS sql_identifier) AS table_schema,
           CAST(t.relname AS sql_identifier) AS table_name

    FROM pg_namespace np, pg_proc p, pg_depend d,
         pg_class t, pg_namespace nt

    WHERE np.oid = p.pronamespace
          AND p.oid = d.objid
          AND d.classid = 'pg_catalog.pg_proc'::regclass
          AND d.refobjid = t.oid
          AND d.refclassid = 'pg_catalog.pg_class'::regclass
          AND t.relnamespace = nt.oid
          AND t.relkind IN ('r', 'v', 'f', 'p')
          AND pg_has_role(t.relowner, 'USAGE')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(isbn, isbn13)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE withoid() WITH (oids = true)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_operator
;
----------
[CREATE_TABLE] CREATE TABLE "public"."defaultexpr_tbl" (
    "i1" integer DEFAULT (100 + ((200 - 199) * 2)),
    "i2" integer DEFAULT "nextval"('"public"."default_seq"'::"regclass")
)
;
----------
[CREATE_TABLE] -- Tests for WITH NO DATA and column name consistency
CREATE TABLE ctas_base (i int, j int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_parameter_acl ADD PRIMARY KEY USING INDEX pg_parameter_acl_oid_index
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_tablespace ADD UNIQUE USING INDEX pg_tablespace_spcname_index
;
----------
[ADD_INDEX] create unique index part_comp_key_index on insertconflicttest(key, fruit) where key < 5
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE parent ADD COLUMN b serial
;
----------
[CREATE_TABLE] CREATE TABLE pk3 PARTITION OF pk FOR VALUES IN (3)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- ok

-- circular references
ALTER TABLE truncate_a ADD FOREIGN KEY (col1) REFERENCES trunc_c
;
----------
[ALTER_TABLE|ADD_COLUMN] -- alter noexisting table
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c4 integer
;
----------
[CREATE_SCHEMA] CREATE SCHEMA import_dest5
;
----------
[CREATE_POLICY] -- CREATE/DROP COLLATION

CREATE COLLATION builtin_c ( PROVIDER = builtin, LOCALE = "C" )
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE newcnt (*) (
   sfunc = int8inc, stype = int8,
   initcond = '0', parallel = safe
)
;
----------
[CREATE_TABLE] -- create a normal heap table and insert some rows.
-- use a temp table so that vacuum behavior doesn't depend on global xmin
create temp table htab (a int)
;
----------
[CREATE_PROG_OBJ] create function anycompatiblerange_anycompatiblemultirange_func(r anycompatiblerange, mr anycompatiblemultirange)
  returns anycompatible as 'select lower($1) + lower($2);' language sql
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS split_lock_next.parent
;
----------
[CREATE_TABLE] CREATE TABLE test_interval (
	i interval
)
;
----------
[CREATE_POLICY] CREATE RULE bug6051_3_ins AS ON INSERT TO bug6051_3 DO INSTEAD
  SELECT i FROM bug6051_2
;
----------
[ADD_INDEX] create index part_column_drop_d_expr on part_column_drop((d = 2))
;
----------
[CREATE_PROG_OBJ] create function tg_pline_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from PLine where slotname = old.slotname;
	insert into PLine (
		    slotname,
		    phonenumber,
		    comment,
		    backlink
		) values (
		    new.slotname,
		    new.phonenumber,
		    new.comment,
		    new.backlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isneq(ismn, ismn13)
	RETURNS boolean
	AS 'int8eq'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR >= (
	PROCEDURE = isnge,
	LEFTARG = upc,
	RIGHTARG = ean13,
	COMMUTATOR = <=,
	NEGATOR = <,
	RESTRICT = scalargtsel,
	JOIN = scalargtjoinsel )
;
----------
[ADMIN_TABLE] ANALYZE public.ec1
;
----------
[DROP_TABLE] DROP TABLE regress_roleoption.t2
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY russian_stem IS 'snowball stemmer for russian language'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30868_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TRIGGER] drop trigger part_ins_trig on parted_copytest_a2
;
----------
[CREATE_TABLE] create table inh_p2(f1 int not null)
;
----------
[ADD_INDEX] CREATE INDEX gtest31_2_y_idx ON generated_stored_tests.gtest31_2 USING btree (((y).b))
;
----------
[ALTER_TABLE] ALTER TABLE test_oat_schema.test_oat_tab ENABLE TRIGGER test_oat_trigger
;
----------
[CREATE_TABLE] CREATE TABLE instead_of_insert_tbl(id serial, name text)
;
----------
[ADMIN_TABLE] ANALYZE public.shoe_data
;
----------
[DROP_TABLE] DROP TABLE batch_cp_upd_test1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_subscription
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx RESET (option_string_null)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION boolop(_int4, query_int) IS 'boolean operation with array'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_evtrig_dropped_objects"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
    obj record;
BEGIN
    FOR obj IN SELECT * FROM pg_event_trigger_dropped_objects()
    LOOP
        IF obj.object_type = 'table' THEN
                EXECUTE format('DROP TABLE IF EXISTS audit_tbls.%I',
					format('%s_%s', obj.schema_name, obj.object_name));
        END IF;

	INSERT INTO dropped_objects
		(type, schema, object) VALUES
		(obj.object_type, obj.schema_name, obj.object_identity);
    END LOOP;
END
$$
;
----------
[ADMIN_TABLE] ANALYZE public.defpart_attach_test_2
;
