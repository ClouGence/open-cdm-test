-- ALTER TABLE recreates the indexes, which should keep their collations.
ALTER TABLE concur_exprs_tab ALTER c2 TYPE TEXT
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_shdescription
;
CREATE TEMP VIEW joinview AS
  SELECT foo.*, other FROM foo JOIN joinme ON (f2 = f2j)
;
CREATE TRIGGER after_upd_row_trig AFTER UPDATE ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('after_upd_row')
;
create unique index on cwi_test (a)
;
DROP DOMAIN jb_ordered_pair
;
ALTER TABLE rf_tbl_abcd_pk REPLICA IDENTITY FULL
;
alter table pgbench_history add constraint pgbench_history_bid_fkey foreign key (bid) references pgbench_branches
;
VACUUM (FULL) pg_catalog.pg_shdescription
;
ALTER TABLE evttrig.one DROP COLUMN col_c
;
CREATE TABLE IF NOT EXISTS as_select1 AS SELECT * FROM pg_class WHERE relkind = 'r'
;
CREATE TRIGGER invalid_trig BEFORE TRUNCATE ON main_view
EXECUTE PROCEDURE trigger_func('before_tru_row')
;
--
-- It's difficult to come up with platform-independent test cases for
-- the pgstattuple functions, but the results for empty tables and
-- indexes should be that.
--

create table test (a int primary key, b int[])
;
create table ec2 (xf int8 primary key, x1 int8alias1, x2 int8alias2)
;
DROP VIEW tenant_view
;
DROP TABLE bttest_multi
;
CREATE TABLE mvtest_t (id int NOT NULL PRIMARY KEY, type text NOT NULL, amt numeric NOT NULL)
;
CREATE PROCEDURE transaction_test5b()
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
BEGIN
    COMMIT;
END;
$$
;
CREATE DOMAIN split_domain_type_next.int_array_domain AS integer[] CHECK (array_length(VALUE, 1) IS NULL OR array_length(VALUE, 1) <= 4)
;
ALTER TABLE pg_toast.pg_toast_30656 RENAME TO reind_con_toast
;
-- check behavior with duplicate column mentions
VACUUM ANALYZE vacparted(a,b,a)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_index
;
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1) references pktable(ptest1)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16203 host=/tmp/JYQ_zMXWdQ dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
CREATE SUBSCRIPTION tap_sub_B_C
	CONNECTION 'port=29892 host=/tmp/7Wj1Y48wnU dbname=postgres application_name=tap_sub_B_C'
	PUBLICATION tap_pub_C
	WITH (origin = none)
;
CREATE TABLE public.test (a INT, b INT, c INT)
;
CREATE TABLE num_variance (a numeric)
;
create table listp_2 partition of listp for values in(2) partition by list (b)
;
ALTER INDEX "fkpart4"."droppk2_pkey" ATTACH PARTITION "fkpart4"."droppk2_d_pkey"
;
create temp table q2 as select 0 as q2
;
-- failed (name conflict)
ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc3
;
ALTER INDEX pg_toast.pg_toast_28529_index RENAME TO reind_con_toast_idx
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_constraint
;
CREATE OPERATOR "public".#@%# (
    FUNCTION = "factorial",
    LEFTARG = bigint
)
;
ANALYZE public.has_fast_default
;
CREATE DATABASE "ecpg2_regression" TEMPLATE=template0 ENCODING='SQL_ASCII'
;
CREATE FUNCTION merge_sq_source_into_sq_target()
RETURNS TABLE (action text, tid int, balance int)
LANGUAGE sql AS
$$
    MERGE INTO sq_target t
    USING sq_source s
    ON tid = sid
    WHEN MATCHED AND tid >= 2 THEN
        UPDATE SET balance = t.balance + delta
    WHEN NOT MATCHED THEN
        INSERT (balance, tid) VALUES (balance + delta, sid)
    WHEN MATCHED AND tid < 2 THEN
        DELETE
    RETURNING merge_action(), t.*;
$$
;
-- Truncate the visibility map fork.
CREATE FUNCTION pg_truncate_visibility_map(regclass)
RETURNS void
AS '$libdir/pg_visibility', 'pg_truncate_visibility_map'
LANGUAGE C STRICT
PARALLEL UNSAFE
;
DROP INDEX concur_reindex_schema.ind
;
CREATE RULE pg_settings_n AS
    ON UPDATE TO pg_settings
    DO INSTEAD NOTHING
;
--
-- EXECUTE ... INTO test
--

create table eifoo (i integer, y integer)
;
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=false, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=false)
;
CREATE FOREIGN TABLE t2 (
  g integer OPTIONS (column_name 'g'),
  "?column?" integer OPTIONS (column_name '?column?')
) SERVER db2_fdw
OPTIONS (schema_name 'public', table_name 't2')
;
--- Check that change of search_path is honored when re-using cached plan

create schema s1
  create table abc (f1 int)
;
---

ALTER VIEW regtest_view SET (security_barrier)
;
CREATE TABLE partitioned2 (
	a int,
	b text
) PARTITION BY RANGE ((a+1), substr(b, 1, 5))
;
--
-- Type casting functions for those situations where the I/O casts don't
-- automatically kick in.
--

CREATE FUNCTION citext(bpchar)
RETURNS citext
AS 'rtrim1'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
CREATE INDEX ON atest12 (abs(a))
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
create table s4.p1_2 partition of s4.p1 for values in (4, 5, 6)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] -- ALTER TABLE recreates the indexes, which should keep their collations.
ALTER TABLE concur_exprs_tab ALTER c2 TYPE TEXT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_shdescription
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMP VIEW joinview AS
  SELECT foo.*, other FROM foo JOIN joinme ON (f2 = f2j)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER after_upd_row_trig AFTER UPDATE ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('after_upd_row')
;
----------
[ADD_INDEX] create unique index on cwi_test (a)
;
----------
[DROP_TYPE] DROP DOMAIN jb_ordered_pair
;
----------
[ALTER_TABLE] ALTER TABLE rf_tbl_abcd_pk REPLICA IDENTITY FULL
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table pgbench_history add constraint pgbench_history_bid_fkey foreign key (bid) references pgbench_branches
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_shdescription
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE evttrig.one DROP COLUMN col_c
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE IF NOT EXISTS as_select1 AS SELECT * FROM pg_class WHERE relkind = 'r'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER invalid_trig BEFORE TRUNCATE ON main_view
EXECUTE PROCEDURE trigger_func('before_tru_row')
;
----------
[CREATE_TABLE] --
-- It's difficult to come up with platform-independent test cases for
-- the pgstattuple functions, but the results for empty tables and
-- indexes should be that.
--

create table test (a int primary key, b int[])
;
----------
[CREATE_TABLE] create table ec2 (xf int8 primary key, x1 int8alias1, x2 int8alias2)
;
----------
[DROP_VIEW] DROP VIEW tenant_view
;
----------
[DROP_TABLE] DROP TABLE bttest_multi
;
----------
[CREATE_TABLE] CREATE TABLE mvtest_t (id int NOT NULL PRIMARY KEY, type text NOT NULL, amt numeric NOT NULL)
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE transaction_test5b()
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
BEGIN
    COMMIT;
END;
$$
;
----------
[CREATE_TYPE] CREATE DOMAIN split_domain_type_next.int_array_domain AS integer[] CHECK (array_length(VALUE, 1) IS NULL OR array_length(VALUE, 1) <= 4)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30656 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] -- check behavior with duplicate column mentions
VACUUM ANALYZE vacparted(a,b,a)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_index
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1) references pktable(ptest1)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=16203 host=/tmp/JYQ_zMXWdQ dbname=postgres' PUBLICATION tap_pub_1, tap_pub_2
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C
	CONNECTION 'port=29892 host=/tmp/7Wj1Y48wnU dbname=postgres application_name=tap_sub_B_C'
	PUBLICATION tap_pub_C
	WITH (origin = none)
;
----------
[CREATE_TABLE] CREATE TABLE public.test (a INT, b INT, c INT)
;
----------
[CREATE_TABLE] CREATE TABLE num_variance (a numeric)
;
----------
[CREATE_TABLE] create table listp_2 partition of listp for values in(2) partition by list (b)
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart4"."droppk2_pkey" ATTACH PARTITION "fkpart4"."droppk2_d_pkey"
;
----------
[CREATE_TABLE|SELECT] create temp table q2 as select 0 as q2
;
----------
[ALTER_PROG_OBJ] -- failed (name conflict)
ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc3
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28529_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_constraint
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".#@%# (
    FUNCTION = "factorial",
    LEFTARG = bigint
)
;
----------
[ADMIN_TABLE] ANALYZE public.has_fast_default
;
----------
[CREATE_CATALOG] CREATE DATABASE "ecpg2_regression" TEMPLATE=template0 ENCODING='SQL_ASCII'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION merge_sq_source_into_sq_target()
RETURNS TABLE (action text, tid int, balance int)
LANGUAGE sql AS
$$
    MERGE INTO sq_target t
    USING sq_source s
    ON tid = sid
    WHEN MATCHED AND tid >= 2 THEN
        UPDATE SET balance = t.balance + delta
    WHEN NOT MATCHED THEN
        INSERT (balance, tid) VALUES (balance + delta, sid)
    WHEN MATCHED AND tid < 2 THEN
        DELETE
    RETURNING merge_action(), t.*;
$$
;
----------
[CREATE_PROG_OBJ] -- Truncate the visibility map fork.
CREATE FUNCTION pg_truncate_visibility_map(regclass)
RETURNS void
AS '$libdir/pg_visibility', 'pg_truncate_visibility_map'
LANGUAGE C STRICT
PARALLEL UNSAFE
;
----------
[DROP_INDEX] DROP INDEX concur_reindex_schema.ind
;
----------
[CREATE_POLICY] CREATE RULE pg_settings_n AS
    ON UPDATE TO pg_settings
    DO INSTEAD NOTHING
;
----------
[CREATE_TABLE] --
-- EXECUTE ... INTO test
--

create table eifoo (i integer, y integer)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=false, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=false)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t2 (
  g integer OPTIONS (column_name 'g'),
  "?column?" integer OPTIONS (column_name '?column?')
) SERVER db2_fdw
OPTIONS (schema_name 'public', table_name 't2')
;
----------
[CREATE_SCHEMA] --- Check that change of search_path is honored when re-using cached plan

create schema s1
  create table abc (f1 int)
;
----------
[ALTER_VIEW] ---

ALTER VIEW regtest_view SET (security_barrier)
;
----------
[CREATE_TABLE] CREATE TABLE partitioned2 (
	a int,
	b text
) PARTITION BY RANGE ((a+1), substr(b, 1, 5))
;
----------
[CREATE_PROG_OBJ] --
-- Type casting functions for those situations where the I/O casts don't
-- automatically kick in.
--

CREATE FUNCTION citext(bpchar)
RETURNS citext
AS 'rtrim1'
LANGUAGE internal IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX ON atest12 (abs(a))
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_TABLE] create table s4.p1_2 partition of s4.p1 for values in (4, 5, 6)
;
