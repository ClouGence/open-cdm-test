VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_rel
;
CREATE TABLE "public"."num_exp_power_10_ln" (
    "id" integer,
    "expected" numeric(210,10)
)
;
DROP FUNCTION test2()
;
CREATE TYPE seg (
	INTERNALLENGTH = 12,
	INPUT = seg_in,
	OUTPUT = seg_out
)
;
CREATE FUNCTION "public"."trigtest"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
	raise notice '% % % %', TG_RELNAME, TG_OP, TG_WHEN, TG_LEVEL;
	return new;
end;$$
;
DROP MATERIALIZED VIEW concur_reindex_matview
;
-- This fails because we mixed up the column ordering
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest2, ftest1) REFERENCES pktable)
;
ALTER TABLE gtest_normal_child2 INHERIT gtest_normal
;
alter table p1 add column a1 int check (a1 > 0)
;
create index trgm_idx on test_trgm using gist (t gist_trgm_ops)
;
DROP TABLE attmp5
;
CREATE VIEW pg_statio_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_blocks_fetched(C.oid) -
                    pg_stat_get_blocks_hit(C.oid) AS heap_blks_read,
            pg_stat_get_blocks_hit(C.oid) AS heap_blks_hit,
            sum(pg_stat_get_blocks_fetched(I.indexrelid) -
                    pg_stat_get_blocks_hit(I.indexrelid))::bigint AS idx_blks_read,
            sum(pg_stat_get_blocks_hit(I.indexrelid))::bigint AS idx_blks_hit,
            pg_stat_get_blocks_fetched(T.oid) -
                    pg_stat_get_blocks_hit(T.oid) AS toast_blks_read,
            pg_stat_get_blocks_hit(T.oid) AS toast_blks_hit,
            pg_stat_get_blocks_fetched(X.indexrelid) -
                    pg_stat_get_blocks_hit(X.indexrelid) AS tidx_blks_read,
            pg_stat_get_blocks_hit(X.indexrelid) AS tidx_blks_hit
    FROM pg_class C LEFT JOIN
            pg_index I ON C.oid = I.indrelid LEFT JOIN
            pg_class T ON C.reltoastrelid = T.oid LEFT JOIN
            pg_index X ON T.oid = X.indrelid
            LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm')
    GROUP BY C.oid, N.nspname, C.relname, T.oid, X.indexrelid
;
CREATE TABLE identity_dump_logged (a INT GENERATED ALWAYS AS IDENTITY)
;
create function explain_filter_to_json(text) returns jsonb
language plpgsql as
$$
declare
    data text := '';
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just '0'
        ln := regexp_replace(ln, '\m\d+\M', '0', 'g');
        data := data || ln;
    end loop;
    return data::jsonb;
end;
$$
;
COMMENT ON COLLATION split_collation_next.c_from_c_renamed IS 'comparison and sort policy'
;
ALTER TABLE ONLY "fkpart5"."pk32"
    ADD CONSTRAINT "pk32_pkey" PRIMARY KEY ("a")
;
DROP AGGREGATE priv_testagg1b(priv_testdomain1)
;
COMMENT ON FUNCTION seg_lt(seg, seg) IS
'less than'
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "hword_numpart" WITH "public"."hunspell_num", "english_stem"
;
CREATE OR REPLACE VIEW "public"."key_dependent_view_no_cols" AS
 SELECT
   FROM "public"."view_base_table"
  GROUP BY "view_base_table"."key"
 HAVING ("length"(("view_base_table"."data")::"text") > 0)
;
-- invalid special area size
CREATE INDEX test1_a_brin ON test1 USING brin(a)
;
CREATE INDEX regress_tblspace_test_tbl_idx ON regress_tblspace_test_tbl (num1)
;
drop function self_ref_trigger_ins_func()
;
create table at_base_table(id int, stuff text)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opfamily
;
ALTER OPERATOR FAMILY alt_opf11 USING gist DROP OPERATOR 1 (int4, int4)
;
CREATE FUNCTION dblink_disconnect ()
RETURNS text
AS '$libdir/dblink','dblink_disconnect'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE OR REPLACE AGGREGATE sum3 (int8,int8,int8)
(
	stype = int8,
	sfunc = sum4
)
;
DROP TABLE parent CASCADE
;
CREATE AGGREGATE "public"."myaggn01b"(*) (
    SFUNC = "public"."stfnp",
    STYPE = integer[],
    INITCOND = '{}'
)
;
/*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
ALTER TABLE list_parted2 ALTER b SET NOT NULL
;
create function consumes_rw_array(int[]) returns int
language plpgsql as $$
  begin return $1[1]; end;
$$ stable
;
COMMENT ON LARGE OBJECT 16539 IS 'comment on large object'
;
DROP FUNCTION invalid_fdw_handler()
;
create event trigger no_rewrite_allowed on table_rewrite
  execute procedure test_evtrig_no_rewrite()
;
ALTER TABLE ONLY "public"."gtest2"
    ADD CONSTRAINT "gtest2_pkey" PRIMARY KEY ("a")
;
create trigger trig_ins_after after insert on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
create cast (float8 as xfloat8) without function
;
CREATE FUNCTION "generated_stored_tests"."gtest_trigger_func3"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'OK';
  RETURN NEW;
END
$$
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', escape '---')
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
ALTER TABLE pg_toast.pg_toast_30964 RENAME TO reind_con_toast
;
alter table fullname add column longname text
;
CREATE TABLE dump_test.regress_pg_dump_table_am_2() USING heap
;
-- This tests errors raised within event triggers; the one in audit_tbls
-- uses 2nd-level recursive invocation via test_evtrig_dropped_objects().
CREATE OR REPLACE FUNCTION undroppable() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
	obj record;
BEGIN
	PERFORM 1 FROM pg_tables WHERE tablename = 'undroppable_objs';
	IF NOT FOUND THEN
		RAISE NOTICE 'table undroppable_objs not found, skipping';
		RETURN;
	END IF;
	FOR obj IN
		SELECT * FROM pg_event_trigger_dropped_objects() JOIN
			undroppable_objs USING (object_type, object_identity)
	LOOP
		RAISE EXCEPTION 'object % of type % cannot be dropped',
			obj.object_identity, obj.object_type;
	END LOOP;
END;
$$
;
DROP TYPE jsrec
;
create index test_range_elem_idx on test_range_elem (i)
;
CREATE FUNCTION functest_IS_4a() RETURNS int LANGUAGE SQL AS 'SELECT 1'
;
alter table domain_test add column c str_domain
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_publication_rel
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_exp_power_10_ln" (
    "id" integer,
    "expected" numeric(210,10)
)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test2()
;
----------
[CREATE_TYPE] CREATE TYPE seg (
	INTERNALLENGTH = 12,
	INPUT = seg_in,
	OUTPUT = seg_out
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."trigtest"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
begin
	raise notice '% % % %', TG_RELNAME, TG_OP, TG_WHEN, TG_LEVEL;
	return new;
end;$$
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW concur_reindex_matview
;
----------
[CREATE_TABLE] -- This fails because we mixed up the column ordering
CREATE TABLE FKTABLE (ftest1 int, ftest2 inet, FOREIGN KEY(ftest2, ftest1) REFERENCES pktable)
;
----------
[ALTER_TABLE] ALTER TABLE gtest_normal_child2 INHERIT gtest_normal
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table p1 add column a1 int check (a1 > 0)
;
----------
[ADD_INDEX] create index trgm_idx on test_trgm using gist (t gist_trgm_ops)
;
----------
[DROP_TABLE] DROP TABLE attmp5
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_statio_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_blocks_fetched(C.oid) -
                    pg_stat_get_blocks_hit(C.oid) AS heap_blks_read,
            pg_stat_get_blocks_hit(C.oid) AS heap_blks_hit,
            sum(pg_stat_get_blocks_fetched(I.indexrelid) -
                    pg_stat_get_blocks_hit(I.indexrelid))::bigint AS idx_blks_read,
            sum(pg_stat_get_blocks_hit(I.indexrelid))::bigint AS idx_blks_hit,
            pg_stat_get_blocks_fetched(T.oid) -
                    pg_stat_get_blocks_hit(T.oid) AS toast_blks_read,
            pg_stat_get_blocks_hit(T.oid) AS toast_blks_hit,
            pg_stat_get_blocks_fetched(X.indexrelid) -
                    pg_stat_get_blocks_hit(X.indexrelid) AS tidx_blks_read,
            pg_stat_get_blocks_hit(X.indexrelid) AS tidx_blks_hit
    FROM pg_class C LEFT JOIN
            pg_index I ON C.oid = I.indrelid LEFT JOIN
            pg_class T ON C.reltoastrelid = T.oid LEFT JOIN
            pg_index X ON T.oid = X.indrelid
            LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm')
    GROUP BY C.oid, N.nspname, C.relname, T.oid, X.indexrelid
;
----------
[CREATE_TABLE] CREATE TABLE identity_dump_logged (a INT GENERATED ALWAYS AS IDENTITY)
;
----------
[CREATE_PROG_OBJ] create function explain_filter_to_json(text) returns jsonb
language plpgsql as
$$
declare
    data text := '';
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just '0'
        ln := regexp_replace(ln, '\m\d+\M', '0', 'g');
        data := data || ln;
    end loop;
    return data::jsonb;
end;
$$
;
----------
[ALTER_POLICY] COMMENT ON COLLATION split_collation_next.c_from_c_renamed IS 'comparison and sort policy'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "fkpart5"."pk32"
    ADD CONSTRAINT "pk32_pkey" PRIMARY KEY ("a")
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE priv_testagg1b(priv_testdomain1)
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION seg_lt(seg, seg) IS
'less than'
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "hword_numpart" WITH "public"."hunspell_num", "english_stem"
;
----------
[CREATE_VIEW(SELECT)] CREATE OR REPLACE VIEW "public"."key_dependent_view_no_cols" AS
 SELECT
   FROM "public"."view_base_table"
  GROUP BY "view_base_table"."key"
 HAVING ("length"(("view_base_table"."data")::"text") > 0)
;
----------
[ADD_INDEX] -- invalid special area size
CREATE INDEX test1_a_brin ON test1 USING brin(a)
;
----------
[ADD_INDEX] CREATE INDEX regress_tblspace_test_tbl_idx ON regress_tblspace_test_tbl (num1)
;
----------
[DROP_PROG_OBJ] drop function self_ref_trigger_ins_func()
;
----------
[CREATE_TABLE] create table at_base_table(id int, stuff text)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_opfamily
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf11 USING gist DROP OPERATOR 1 (int4, int4)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_disconnect ()
RETURNS text
AS '$libdir/dblink','dblink_disconnect'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE AGGREGATE sum3 (int8,int8,int8)
(
	stype = int8,
	sfunc = sum4
)
;
----------
[DROP_TABLE] DROP TABLE parent CASCADE
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggn01b"(*) (
    SFUNC = "public"."stfnp",
    STYPE = integer[],
    INITCOND = '{}'
)
;
----------
[CREATE_POLICY] /*
 * text search configuration for german language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * german and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY german_stem
	(TEMPLATE = snowball, Language = german , StopWords=german)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE list_parted2 ALTER b SET NOT NULL
;
----------
[CREATE_PROG_OBJ] create function consumes_rw_array(int[]) returns int
language plpgsql as $$
  begin return $1[1]; end;
$$ stable
;
----------
[UNKNOWN] COMMENT ON LARGE OBJECT 16539 IS 'comment on large object'
;
----------
[DROP_PROG_OBJ] DROP FUNCTION invalid_fdw_handler()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create event trigger no_rewrite_allowed on table_rewrite
  execute procedure test_evtrig_no_rewrite()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."gtest2"
    ADD CONSTRAINT "gtest2_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_ins_after after insert on parted_stmt_trig
  for each statement execute procedure trigger_notice()
;
----------
[CREATE_PROG_OBJ] create cast (float8 as xfloat8) without function
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "generated_stored_tests"."gtest_trigger_func3"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'OK';
  RETURN NEW;
END
$$
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', escape '---')
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_foreign_table
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30964 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table fullname add column longname text
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.regress_pg_dump_table_am_2() USING heap
;
----------
[CREATE_PROG_OBJ] -- This tests errors raised within event triggers; the one in audit_tbls
-- uses 2nd-level recursive invocation via test_evtrig_dropped_objects().
CREATE OR REPLACE FUNCTION undroppable() RETURNS event_trigger
LANGUAGE plpgsql AS $$
DECLARE
	obj record;
BEGIN
	PERFORM 1 FROM pg_tables WHERE tablename = 'undroppable_objs';
	IF NOT FOUND THEN
		RAISE NOTICE 'table undroppable_objs not found, skipping';
		RETURN;
	END IF;
	FOR obj IN
		SELECT * FROM pg_event_trigger_dropped_objects() JOIN
			undroppable_objs USING (object_type, object_identity)
	LOOP
		RAISE EXCEPTION 'object % of type % cannot be dropped',
			obj.object_identity, obj.object_type;
	END LOOP;
END;
$$
;
----------
[DROP_TYPE] DROP TYPE jsrec
;
----------
[ADD_INDEX] create index test_range_elem_idx on test_range_elem (i)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_IS_4a() RETURNS int LANGUAGE SQL AS 'SELECT 1'
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table domain_test add column c str_domain
;
