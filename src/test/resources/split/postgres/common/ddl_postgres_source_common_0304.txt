create function myintin(cstring) returns myint strict immutable language
  internal as 'int4in'
;
VACUUM (PARALLEL 2) pg_catalog.pg_subscription
;
CREATE TABLE pagg_tab4_p1 PARTITION OF pagg_tab4 FOR VALUES IN ('a', 'b')
;
ALTER TABLE pg_policy ADD UNIQUE USING INDEX pg_policy_polrelid_polname_index
;
CREATE FUNCTION hstore_eq(hstore,hstore)
RETURNS boolean
AS '$libdir/hstore','hstore_eq'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
/*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
create index ab_a1_b2_a_idx on ab_a1_b2 (a)
;
CREATE FUNCTION func_stats(a text DEFAULT 'a_data', b text DEFAULT lower('b_data'))
  RETURNS text AS $$ SELECT $1::text || '_' || $2::text; $$ LANGUAGE SQL
;
create table convslot_test_child (col1 text primary key,
	foreign key (col1) references convslot_test_parent(col1) on delete cascade on update cascade
)
;
CREATE TEMPORARY TABLE temp_table (a int, id int)
;
ALTER TABLE fkpart8.tbl2 DROP CONSTRAINT tbl2_f1_fkey
;
CREATE TABLE "generated_stored_tests"."gtest20b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
CREATE TABLE r2 (a int REFERENCES r1 ON DELETE CASCADE)
;
ALTER TABLE T ADD COLUMN c_num NUMERIC DEFAULT 1.00000000001,
              ALTER COLUMN c_big SET DEFAULT -9999999999999999
;
CREATE FOREIGN TABLE split_fdw_repl.remote_accounts (
    id integer OPTIONS (column_name 'id') NOT NULL,
    owner_name text OPTIONS (column_name 'owner_name') DEFAULT 'nobody',
    amount integer CHECK (amount >= 0)
) SERVER split_dummy_server_renamed OPTIONS (schema_name 'public', table_name 'accounts')
;
CREATE TYPE "public"."aggtype" AS (
	"a" integer,
	"b" integer,
	"c" "text"
)
;
ALTER TABLE "regress_rls_schema"."rls_tbl_force" ENABLE ROW LEVEL SECURITY
;
DROP FUNCTION test_pglz_decompress
;
ALTER FUNCTION gbt_intv_distance(internal, interval, smallint, oid, internal) PARALLEL SAFE
;
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=0)
;
DROP TABLE bttest_unique_nulls
;
DROP TABLE IF EXISTS split_maint.maint_table
;
analyze tbl_rs
;
CREATE FOREIGN TABLE ft_part2 (a INT) SERVER s0
;
create type textrange_en_us as range(subtype=text, collation="en-x-icu")
;
CREATE INDEX test_index2 on testschema.test_default_tab (id) TABLESPACE regress_tblspace
;
--------------
-- Some more update-partition-key test scenarios below. This time use list
-- partitions.
--------------

-- Setup for list partitions
CREATE TABLE list_parted (a numeric, b int, c int8) PARTITION BY list (a)
;
CREATE TABLE pagg_tab5_p1 PARTITION OF pagg_tab5 FOR VALUES IN ('a', 'b')
;
create index gist_tbl_point_index on gist_tbl using gist (p)
;
ALTER TABLE fkpart7.pkpart ADD PRIMARY KEY (a)
;
ALTER SEQUENCE sequence_test_unlogged SET UNLOGGED
;
-- not other types of index-based constraints
create table idxpart (a int, exclude (a with = )) partition by range (a)
;
CREATE TABLE "public"."num_exp_add" (
    "id1" integer,
    "id2" integer,
    "expected" numeric(210,10)
)
;
DROP TABLE attmp2
;
ALTER TABLE pagg_tab_ml_p3 ATTACH PARTITION pagg_tab_ml_p3_s1 FOR VALUES FROM (0) TO (5)
;
CREATE TABLE prt1_m_p1 PARTITION OF prt1_m FOR VALUES FROM (0, 0) TO (250, 250)
;
CREATE OPERATOR #@%# (
   procedure = factorial
)
;
-- same as above, but ensure the constraint violation is detected
create table atacc1 (a bigint, b int)
;
-- This will squeeze the bucket and remove overflow pages.
VACUUM hash_cleanup_heap
;
-- fail, foo.f1 requires it

drop schema c
;
DROP TABLE test_exists
;
alter table renameColumnChild rename column a to d
;
CREATE FUNCTION gbt_ts_consistent(internal,timestamp,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE FUNCTION gbt_int8_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE INDEX enumtest_hash ON enumtest USING hash (col)
;
DO $$
DECLARE
  loid oid;
BEGIN
  SELECT tbl.loid INTO loid FROM lotest_stash_values tbl;
  PERFORM lo_export(loid, '/src/src/bin/pg_upgrade/tmp_check/regress/results/invalid/path');
EXCEPTION
  WHEN UNDEFINED_FILE THEN RAISE NOTICE 'could not open file, as expected';
END;
$$
;
-- check that the table being attach is with valid modulus and remainder value
CREATE TABLE fail_part(LIKE hash_parted)
;
CREATE FUNCTION gbt_macad8_consistent(internal,macaddr8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
drop table tuplesest_tab
;
DROP MATERIALIZED VIEW tid_matview
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] create function myintin(cstring) returns myint strict immutable language
  internal as 'int4in'
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_subscription
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab4_p1 PARTITION OF pagg_tab4 FOR VALUES IN ('a', 'b')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_policy ADD UNIQUE USING INDEX pg_policy_polrelid_polname_index
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_eq(hstore,hstore)
RETURNS boolean
AS '$libdir/hstore','hstore_eq'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_POLICY] /*
 * text search configuration for swedish language
 *
 * Copyright (c) 2007-2021, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * swedish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY swedish_stem
	(TEMPLATE = snowball, Language = swedish , StopWords=swedish)
;
----------
[ADD_INDEX] create index ab_a1_b2_a_idx on ab_a1_b2 (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION func_stats(a text DEFAULT 'a_data', b text DEFAULT lower('b_data'))
  RETURNS text AS $$ SELECT $1::text || '_' || $2::text; $$ LANGUAGE SQL
;
----------
[CREATE_TABLE] create table convslot_test_child (col1 text primary key,
	foreign key (col1) references convslot_test_parent(col1) on delete cascade on update cascade
)
;
----------
[CREATE_TABLE] CREATE TEMPORARY TABLE temp_table (a int, id int)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE fkpart8.tbl2 DROP CONSTRAINT tbl2_f1_fkey
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtest20b" (
    "a" integer NOT NULL,
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[CREATE_TABLE] CREATE TABLE r2 (a int REFERENCES r1 ON DELETE CASCADE)
;
----------
[ALTER_TABLE|ADD_COLUMN|ALTER_COLUMN] ALTER TABLE T ADD COLUMN c_num NUMERIC DEFAULT 1.00000000001,
              ALTER COLUMN c_big SET DEFAULT -9999999999999999
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE split_fdw_repl.remote_accounts (
    id integer OPTIONS (column_name 'id') NOT NULL,
    owner_name text OPTIONS (column_name 'owner_name') DEFAULT 'nobody',
    amount integer CHECK (amount >= 0)
) SERVER split_dummy_server_renamed OPTIONS (schema_name 'public', table_name 'accounts')
;
----------
[CREATE_TYPE] CREATE TYPE "public"."aggtype" AS (
	"a" integer,
	"b" integer,
	"c" "text"
)
;
----------
[ALTER_TABLE] ALTER TABLE "regress_rls_schema"."rls_tbl_force" ENABLE ROW LEVEL SECURITY
;
----------
[DROP_PROG_OBJ] DROP FUNCTION test_pglz_decompress
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_distance(internal, interval, smallint, oid, internal) PARALLEL SAFE
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=0)
;
----------
[DROP_TABLE] DROP TABLE bttest_unique_nulls
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS split_maint.maint_table
;
----------
[ADMIN_TABLE] analyze tbl_rs
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft_part2 (a INT) SERVER s0
;
----------
[CREATE_POLICY] create type textrange_en_us as range(subtype=text, collation="en-x-icu")
;
----------
[ADD_INDEX] CREATE INDEX test_index2 on testschema.test_default_tab (id) TABLESPACE regress_tblspace
;
----------
[CREATE_TABLE] --------------
-- Some more update-partition-key test scenarios below. This time use list
-- partitions.
--------------

-- Setup for list partitions
CREATE TABLE list_parted (a numeric, b int, c int8) PARTITION BY list (a)
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab5_p1 PARTITION OF pagg_tab5 FOR VALUES IN ('a', 'b')
;
----------
[ADD_INDEX] create index gist_tbl_point_index on gist_tbl using gist (p)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fkpart7.pkpart ADD PRIMARY KEY (a)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test_unlogged SET UNLOGGED
;
----------
[CREATE_TABLE] -- not other types of index-based constraints
create table idxpart (a int, exclude (a with = )) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."num_exp_add" (
    "id1" integer,
    "id2" integer,
    "expected" numeric(210,10)
)
;
----------
[DROP_TABLE] DROP TABLE attmp2
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pagg_tab_ml_p3 ATTACH PARTITION pagg_tab_ml_p3_s1 FOR VALUES FROM (0) TO (5)
;
----------
[CREATE_TABLE] CREATE TABLE prt1_m_p1 PARTITION OF prt1_m FOR VALUES FROM (0, 0) TO (250, 250)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR #@%# (
   procedure = factorial
)
;
----------
[CREATE_TABLE] -- same as above, but ensure the constraint violation is detected
create table atacc1 (a bigint, b int)
;
----------
[ADMIN_TABLE] -- This will squeeze the bucket and remove overflow pages.
VACUUM hash_cleanup_heap
;
----------
[DROP_SCHEMA] -- fail, foo.f1 requires it

drop schema c
;
----------
[DROP_TABLE] DROP TABLE test_exists
;
----------
[RENAME_COLUMN] alter table renameColumnChild rename column a to d
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_ts_consistent(internal,timestamp,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int8_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADD_INDEX] CREATE INDEX enumtest_hash ON enumtest USING hash (col)
;
----------
[BLOCK] DO $$
DECLARE
  loid oid;
BEGIN
  SELECT tbl.loid INTO loid FROM lotest_stash_values tbl;
  PERFORM lo_export(loid, '/src/src/bin/pg_upgrade/tmp_check/regress/results/invalid/path');
EXCEPTION
  WHEN UNDEFINED_FILE THEN RAISE NOTICE 'could not open file, as expected';
END;
$$
;
----------
[CREATE_TABLE] -- check that the table being attach is with valid modulus and remainder value
CREATE TABLE fail_part(LIKE hash_parted)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_macad8_consistent(internal,macaddr8,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] drop table tuplesest_tab
;
----------
[DROP_VIEW] DROP MATERIALIZED VIEW tid_matview
;
