analyze gtest32
;
create view def_view_test as select * from def_test
;
VACUUM (PROCESS_TOAST FALSE) information_schema.sql_implementation_info
;
VACUUM ANALYZE expr_key
;
CREATE FUNCTION split_maint.add_amount(a numeric, b numeric)
RETURNS numeric
LANGUAGE SQL
IMMUTABLE
AS $$ SELECT a + b $$
;
CREATE TABLE ATACC1 (TEST INT
	CHECK (TEST > 0) NO INHERIT)
;
CREATE FUNCTION test_is_full()
RETURNS bool STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
ANALYZE public.gtest3
;
drop table child1, child2, child3, parent
;
ANALYZE (SKIP_LOCKED) information_schema.sql_sizing_profiles
;
ALTER TABLE cwi_test ADD primary key USING INDEX cwi_uniq_idx
;
CREATE TABLE r2 (a int REFERENCES r1)
;
CREATE TABLE tab_rowfilter_toast (a text NOT NULL, b text NOT NULL)
;
-- test that indexing commands work with TOASTed values in pg_index
create table test_pg_index_toast_table (a int)
;
VACUUM pg_database
;
CREATE TABLE "public"."char_tbl" (
    "f1" character(4)
)
;
alter table p1_c2 drop constraint f1_pos
;
ALTER TABLE "public"."trigger_parted_p2_2" ENABLE ALWAYS TRIGGER "aft_row"
;
/*
 * 6.5
 * CHARACTER_DATA domain
 */

CREATE DOMAIN character_data AS character varying COLLATE "C"
;
-- arg2 only polymorphic transfn
CREATE FUNCTION tf2p(int[],anyelement) RETURNS int[] AS
'select $1' LANGUAGE SQL
;
/* contrib/cube/cube--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Remove @ and ~
DROP OPERATOR @ (cube, cube)
;
ALTER TABLE pg_largeobject_metadata ADD PRIMARY KEY USING INDEX pg_largeobject_metadata_oid_index
;
CREATE TABLE droppk1 PARTITION OF droppk FOR VALUES FROM (0) TO (1000)
;
ANALYZE pg_catalog.pg_replication_origin
;
CREATE TABLE fail () INHERITS (partitioned2)
;
ALTER TABLE list_parted ATTACH PARTITION fail_part FOR VALUES IN (1)
;
ANALYZE fkpart5.pk32
;
CREATE FUNCTION "public"."extractq2_2"("t" "public"."int8_tbl") RETURNS TABLE("ret1" bigint)
    LANGUAGE "sql" IMMUTABLE
    AS $$
  select extractq2(t) offset 0
$$
;
ALTER INDEX pg_toast.pg_toast_27825_index RENAME TO reind_con_toast_idx
;
CREATE VIEW pg_stat_progress_analyze AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        CAST(S.relid AS oid) AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'acquiring sample rows'
                      WHEN 2 THEN 'acquiring inherited sample rows'
                      WHEN 3 THEN 'computing statistics'
                      WHEN 4 THEN 'computing extended statistics'
                      WHEN 5 THEN 'finalizing analyze'
                      END AS phase,
        S.param2 AS sample_blks_total,
        S.param3 AS sample_blks_scanned,
        S.param4 AS ext_stats_total,
        S.param5 AS ext_stats_computed,
        S.param6 AS child_tables_total,
        S.param7 AS child_tables_done,
        CAST(S.param8 AS oid) AS current_child_table_relid,
        S.param9 / 1000000::double precision AS delay_time
    FROM pg_stat_get_progress_info('ANALYZE') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
CREATE FUNCTION hstore_ge(hstore,hstore)
RETURNS boolean
AS '$libdir/hstore','hstore_ge'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER FUNCTION gbt_numeric_same(gbtreekey_var, gbtreekey_var, internal) PARALLEL SAFE
;
create temporary table t2 as select 1 as i from generate_series(1, 10000)
;
create rule qqq as on delete to copydml_test do instead nothing
;
CREATE TABLE fail_part PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 0, REMAINDER 1)
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
-- ALTER MATERIALIZED VIEW SET ACCESS METHOD
CREATE MATERIALIZED VIEW heapmv USING heap AS SELECT * FROM heaptable
;
create table testtab (a int, b char(100))
;
ALTER OPERATOR FAMILY gist_float4_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
-- test statistics and selectivity estimation as well
--
-- We don't check the accuracy of selectivity estimation, but at least check
-- it doesn't fall.
analyze test_multirange_gist
;
CREATE TRIGGER "gtest12_02" BEFORE UPDATE ON "public"."gtest26" FOR EACH ROW EXECUTE FUNCTION "public"."gtest_trigger_func4"()
;
CREATE INDEX idx_int4 ON test_int4 USING gin (i)
;
-- collation
DROP COLLATION IF EXISTS test_collation_exists
;
CREATE TABLE testpub_tbl8_0 PARTITION OF testpub_tbl8 FOR VALUES WITH (modulus 2, remainder 0)
;
ALTER TABLE y2 ENABLE ROW LEVEL SECURITY
;
create table loc3 (f1 int, f2 text)
;
DROP DATABASE db_will_get_dropped
;
CREATE TABLE not_leftmost (c text)
;
CREATE TEMP TABLE point_gist_tbl(f1 point)
;
CREATE VIEW pg_stats_ext WITH (security_barrier) AS
    SELECT cn.nspname AS schemaname,
           c.relname AS tablename,
           sn.nspname AS statistics_schemaname,
           s.stxname AS statistics_name,
           pg_get_userbyid(s.stxowner) AS statistics_owner,
           ( SELECT array_agg(a.attname ORDER BY a.attnum)
             FROM unnest(s.stxkeys) k
                  JOIN pg_attribute a
                       ON (a.attrelid = s.stxrelid AND a.attnum = k)
           ) AS attnames,
           pg_get_statisticsobjdef_expressions(s.oid) as exprs,
           s.stxkind AS kinds,
           sd.stxdinherit AS inherited,
           sd.stxdndistinct AS n_distinct,
           sd.stxddependencies AS dependencies,
           m.most_common_vals,
           m.most_common_val_nulls,
           m.most_common_freqs,
           m.most_common_base_freqs
    FROM pg_statistic_ext s JOIN pg_class c ON (c.oid = s.stxrelid)
         JOIN pg_statistic_ext_data sd ON (s.oid = sd.stxoid)
         LEFT JOIN pg_namespace cn ON (cn.oid = c.relnamespace)
         LEFT JOIN pg_namespace sn ON (sn.oid = s.stxnamespace)
         LEFT JOIN LATERAL
                   ( SELECT array_agg(values) AS most_common_vals,
                            array_agg(nulls) AS most_common_val_nulls,
                            array_agg(frequency) AS most_common_freqs,
                            array_agg(base_frequency) AS most_common_base_freqs
                     FROM pg_mcv_list_items(sd.stxdmcv)
                   ) m ON sd.stxdmcv IS NOT NULL
    WHERE pg_has_role(c.relowner, 'USAGE')
    AND (c.relrowsecurity = false OR NOT row_security_active(c.oid))
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] analyze gtest32
;
----------
[CREATE_VIEW(SELECT)] create view def_view_test as select * from def_test
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) information_schema.sql_implementation_info
;
----------
[ADMIN_TABLE] VACUUM ANALYZE expr_key
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION split_maint.add_amount(a numeric, b numeric)
RETURNS numeric
LANGUAGE SQL
IMMUTABLE
AS $$ SELECT a + b $$
;
----------
[CREATE_TABLE] CREATE TABLE ATACC1 (TEST INT
	CHECK (TEST > 0) NO INHERIT)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_is_full()
RETURNS bool STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
----------
[ADMIN_TABLE] ANALYZE public.gtest3
;
----------
[DROP_TABLE] drop table child1, child2, child3, parent
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) information_schema.sql_sizing_profiles
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE cwi_test ADD primary key USING INDEX cwi_uniq_idx
;
----------
[CREATE_TABLE] CREATE TABLE r2 (a int REFERENCES r1)
;
----------
[CREATE_TABLE] CREATE TABLE tab_rowfilter_toast (a text NOT NULL, b text NOT NULL)
;
----------
[CREATE_TABLE] -- test that indexing commands work with TOASTed values in pg_index
create table test_pg_index_toast_table (a int)
;
----------
[ADMIN_TABLE] VACUUM pg_database
;
----------
[CREATE_TABLE] CREATE TABLE "public"."char_tbl" (
    "f1" character(4)
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table p1_c2 drop constraint f1_pos
;
----------
[ALTER_TABLE] ALTER TABLE "public"."trigger_parted_p2_2" ENABLE ALWAYS TRIGGER "aft_row"
;
----------
[CREATE_TYPE] /*
 * 6.5
 * CHARACTER_DATA domain
 */

CREATE DOMAIN character_data AS character varying COLLATE "C"
;
----------
[CREATE_PROG_OBJ] -- arg2 only polymorphic transfn
CREATE FUNCTION tf2p(int[],anyelement) RETURNS int[] AS
'select $1' LANGUAGE SQL
;
----------
[DROP_PROG_OBJ] /* contrib/cube/cube--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


-- Remove @ and ~
DROP OPERATOR @ (cube, cube)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_largeobject_metadata ADD PRIMARY KEY USING INDEX pg_largeobject_metadata_oid_index
;
----------
[CREATE_TABLE] CREATE TABLE droppk1 PARTITION OF droppk FOR VALUES FROM (0) TO (1000)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE] CREATE TABLE fail () INHERITS (partitioned2)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted ATTACH PARTITION fail_part FOR VALUES IN (1)
;
----------
[ADMIN_TABLE] ANALYZE fkpart5.pk32
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."extractq2_2"("t" "public"."int8_tbl") RETURNS TABLE("ret1" bigint)
    LANGUAGE "sql" IMMUTABLE
    AS $$
  select extractq2(t) offset 0
$$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27825_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_progress_analyze AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        CAST(S.relid AS oid) AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'acquiring sample rows'
                      WHEN 2 THEN 'acquiring inherited sample rows'
                      WHEN 3 THEN 'computing statistics'
                      WHEN 4 THEN 'computing extended statistics'
                      WHEN 5 THEN 'finalizing analyze'
                      END AS phase,
        S.param2 AS sample_blks_total,
        S.param3 AS sample_blks_scanned,
        S.param4 AS ext_stats_total,
        S.param5 AS ext_stats_computed,
        S.param6 AS child_tables_total,
        S.param7 AS child_tables_done,
        CAST(S.param8 AS oid) AS current_child_table_relid,
        S.param9 / 1000000::double precision AS delay_time
    FROM pg_stat_get_progress_info('ANALYZE') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_ge(hstore,hstore)
RETURNS boolean
AS '$libdir/hstore','hstore_ge'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_numeric_same(gbtreekey_var, gbtreekey_var, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE|SELECT] create temporary table t2 as select 1 as i from generate_series(1, 10000)
;
----------
[CREATE_POLICY] create rule qqq as on delete to copydml_test do instead nothing
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 0, REMAINDER 1)
;
----------
[CREATE_TABLE|SELECT] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE ctas_ine_tbl AS SELECT 1 / 0 WITH NO DATA
;
----------
[CREATE_VIEW(SELECT)] -- ALTER MATERIALIZED VIEW SET ACCESS METHOD
CREATE MATERIALIZED VIEW heapmv USING heap AS SELECT * FROM heaptable
;
----------
[CREATE_TABLE] create table testtab (a int, b char(100))
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_float4_ops USING gist ADD
	FUNCTION 12 ("any", "any") gist_translate_cmptype_btree (int)
;
----------
[ADMIN_TABLE] -- test statistics and selectivity estimation as well
--
-- We don't check the accuracy of selectivity estimation, but at least check
-- it doesn't fall.
analyze test_multirange_gist
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "gtest12_02" BEFORE UPDATE ON "public"."gtest26" FOR EACH ROW EXECUTE FUNCTION "public"."gtest_trigger_func4"()
;
----------
[ADD_INDEX] CREATE INDEX idx_int4 ON test_int4 USING gin (i)
;
----------
[DROP_POLICY] -- collation
DROP COLLATION IF EXISTS test_collation_exists
;
----------
[CREATE_TABLE] CREATE TABLE testpub_tbl8_0 PARTITION OF testpub_tbl8 FOR VALUES WITH (modulus 2, remainder 0)
;
----------
[ALTER_TABLE] ALTER TABLE y2 ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] create table loc3 (f1 int, f2 text)
;
----------
[DROP_CATALOG] DROP DATABASE db_will_get_dropped
;
----------
[CREATE_TABLE] CREATE TABLE not_leftmost (c text)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE point_gist_tbl(f1 point)
;
----------
[CREATE_VIEW(SELECT,METADATA,PERFORMANCE)] CREATE VIEW pg_stats_ext WITH (security_barrier) AS
    SELECT cn.nspname AS schemaname,
           c.relname AS tablename,
           sn.nspname AS statistics_schemaname,
           s.stxname AS statistics_name,
           pg_get_userbyid(s.stxowner) AS statistics_owner,
           ( SELECT array_agg(a.attname ORDER BY a.attnum)
             FROM unnest(s.stxkeys) k
                  JOIN pg_attribute a
                       ON (a.attrelid = s.stxrelid AND a.attnum = k)
           ) AS attnames,
           pg_get_statisticsobjdef_expressions(s.oid) as exprs,
           s.stxkind AS kinds,
           sd.stxdinherit AS inherited,
           sd.stxdndistinct AS n_distinct,
           sd.stxddependencies AS dependencies,
           m.most_common_vals,
           m.most_common_val_nulls,
           m.most_common_freqs,
           m.most_common_base_freqs
    FROM pg_statistic_ext s JOIN pg_class c ON (c.oid = s.stxrelid)
         JOIN pg_statistic_ext_data sd ON (s.oid = sd.stxoid)
         LEFT JOIN pg_namespace cn ON (cn.oid = c.relnamespace)
         LEFT JOIN pg_namespace sn ON (sn.oid = s.stxnamespace)
         LEFT JOIN LATERAL
                   ( SELECT array_agg(values) AS most_common_vals,
                            array_agg(nulls) AS most_common_val_nulls,
                            array_agg(frequency) AS most_common_freqs,
                            array_agg(base_frequency) AS most_common_base_freqs
                     FROM pg_mcv_list_items(sd.stxdmcv)
                   ) m ON sd.stxdmcv IS NOT NULL
    WHERE pg_has_role(c.relowner, 'USAGE')
    AND (c.relrowsecurity = false OR NOT row_security_active(c.oid))
;
