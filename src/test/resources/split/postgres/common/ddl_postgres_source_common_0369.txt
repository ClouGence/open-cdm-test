alter table pp1 add column a2 int check (a2 > 0)
;
CREATE MATERIALIZED VIEW IF NOT EXISTS ext_cine_mv AS SELECT 42 AS f1
;
create table mcrparted4_common_lt_0 partition of mcrparted for values from ('common', minvalue) to ('common', 0)
;
VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) test1
;
create function error2(p_name_table text) returns text language plpgsql as $$
begin
  return error1(p_name_table);
end$$
;
ALTER TABLE ONLY list_parted3 DROP CONSTRAINT list_parted3_a_not_null
;
ANALYZE pg_temp_3.pg_temp_27598_2
;
CREATE TEMP TABLE temptest1(col int PRIMARY KEY)
;
ALTER INDEX pg_toast.pg_toast_30522_index RENAME TO reind_con_toast_idx
;
CREATE VIEW ro_view20 AS SELECT a, b, generate_series(1, a) g FROM base_tbl
;
ANALYZE public.tt7
;
create trigger child2_delete_trig
  after delete on child2 referencing old table as old_table
  for each statement execute procedure dump_delete()
;
-- Check behavior when trigger returns unmodified trigtuple
create table trigtest (f1 int, f2 text)
;
CREATE OR REPLACE FUNCTION mytrigger() RETURNS trigger LANGUAGE plpgsql as $$
begin
	if row(old.*) is distinct from row(new.*) then
		raise notice 'row % changed', new.f1;
	else
		raise notice 'row % not changed', new.f1;
	end if;
	return new;
end$$
;
create table table2 (a text)
;
DROP TABLE num_exp_add
;
create index idx_xpath on t1 ( xpath_string
('/attributes/attribute[@name="attr_1"]/text()', xml_data::text))
;
CREATE OPERATOR < (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_lt,
	COMMUTATOR = '>', NEGATOR = '>=',
	RESTRICT = scalarltsel, JOIN = scalarltjoinsel
)
;
drop table rewriteme
;
ALTER INDEX pg_toast.pg_toast_33292_index RENAME TO reind_con_toast_idx
;
CREATE CAST (priv_testdomain1 AS priv_testdomain3a) WITH FUNCTION castfunc(int)
;
-- should fail, name collision
alter event trigger regress_event_trigger rename to regress_event_trigger2
;
DROP TABLE vac_option_tab
;
ANALYZE public.quad_point_tbl
;
DROP SCHEMA IF EXISTS split_alter18 CASCADE
;
-- Test 8:
-- cleanup and make sure results are also empty
DROP TABLE test_prepared1
;
CREATE TABLE "public"."gtest24" (
    "a" integer NOT NULL,
    "b" "public"."gtestdomain1" GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
ALTER TABLE fd_pt2_1 ADD CONSTRAINT p21chk CHECK (c2 <> '')
;
--     N    P
-- should CREATE
CREATE AGGREGATE myaggn03a(*) (SFUNC = stfp, STYPE = int4[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
CREATE FOREIGN TABLE t2 (
  c1 integer OPTIONS (column_name 'c1'),
  c2 character varying OPTIONS (column_name 'c2') COLLATE pg_catalog."default",
  c3 text OPTIONS (column_name 'c3') COLLATE pg_catalog."POSIX"
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't2')
;
drop table cta_test2
;
CREATE TABLE brin_test_bloom (a INT, b INT)
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN c3 integer
;
CREATE DOMAIN "generated_virtual_tests"."gtestdomain1" AS integer
	CONSTRAINT "gtestdomain1_check" CHECK ((VALUE < 10))
;
-- Inheritance
CREATE TABLE person (
    id          INT NOT NULL PRIMARY KEY,
	name 		text,
	age			int4,
	location 	point
)
;
/*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2023
 *
 * Copyright (c) 2003-2024, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 6.2
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
-- test elem <@ range operator
create table test_range_elem(i int4)
;
-- check for rollback of ANALYZE corrupting table property flags (bug #11638)
CREATE TABLE check_fk_presence_1 (id int PRIMARY KEY, t text)
;
CREATE TEMP TABLE split_values_copy AS EXECUTE split_values_plan WITH NO DATA
;
-- pg_stat_have_stats returns true for committed index creation
CREATE table stats_test_tab1 as select generate_series(1,10) a
;
-- without AR trigger, cascaded deletes all end up in one transition table
drop trigger self_ref_r_trig on self_ref
;
CREATE VIEW "public"."key_dependent_view" AS
SELECT
    NULL::integer AS "key",
    NULL::character varying(20) AS "data"
;
DROP TRIGGER c1_update_trig ON part_c_1_100
;
--
-- Tests for polymorphic SQL functions and aggregates based on them.
-- Tests for other features related to function-calling have snuck in, too.
--

create function polyf(x anyelement) returns anyelement as $$
  select x + 1
$$ language sql
;
--
-- Non-spurious duplicate violation tests
--
create unique index key_index on insertconflicttest(key)
;
create table rlp_default_30 partition of rlp_default for values in (30)
;
CREATE PUBLICATION tap_pub_3 FOR TABLE tab_3
;
VACUUM (PARALLEL 2, INDEX_CLEANUP FALSE) pvactst
;
CREATE FUNCTION cube_ur_coord(cube, int4)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE prt2_e_p3 PARTITION OF prt2_e FOR VALUES FROM (500) TO (600)
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] alter table pp1 add column a2 int check (a2 > 0)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW IF NOT EXISTS ext_cine_mv AS SELECT 42 AS f1
;
----------
[CREATE_TABLE] create table mcrparted4_common_lt_0 partition of mcrparted for values from ('common', minvalue) to ('common', 0)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE, DISABLE_PAGE_SKIPPING) test1
;
----------
[CREATE_PROG_OBJ] create function error2(p_name_table text) returns text language plpgsql as $$
begin
  return error1(p_name_table);
end$$
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE ONLY list_parted3 DROP CONSTRAINT list_parted3_a_not_null
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_3.pg_temp_27598_2
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temptest1(col int PRIMARY KEY)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30522_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view20 AS SELECT a, b, generate_series(1, a) g FROM base_tbl
;
----------
[ADMIN_TABLE] ANALYZE public.tt7
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger child2_delete_trig
  after delete on child2 referencing old table as old_table
  for each statement execute procedure dump_delete()
;
----------
[CREATE_TABLE] -- Check behavior when trigger returns unmodified trigtuple
create table trigtest (f1 int, f2 text)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION mytrigger() RETURNS trigger LANGUAGE plpgsql as $$
begin
	if row(old.*) is distinct from row(new.*) then
		raise notice 'row % changed', new.f1;
	else
		raise notice 'row % not changed', new.f1;
	end if;
	return new;
end$$
;
----------
[CREATE_TABLE] create table table2 (a text)
;
----------
[DROP_TABLE] DROP TABLE num_exp_add
;
----------
[ADD_INDEX] create index idx_xpath on t1 ( xpath_string
('/attributes/attribute[@name="attr_1"]/text()', xml_data::text))
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	LEFTARG = cube, RIGHTARG = cube, PROCEDURE = cube_lt,
	COMMUTATOR = '>', NEGATOR = '>=',
	RESTRICT = scalarltsel, JOIN = scalarltjoinsel
)
;
----------
[DROP_TABLE] drop table rewriteme
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33292_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE CAST (priv_testdomain1 AS priv_testdomain3a) WITH FUNCTION castfunc(int)
;
----------
[RENAME_TRIGGER] -- should fail, name collision
alter event trigger regress_event_trigger rename to regress_event_trigger2
;
----------
[DROP_TABLE] DROP TABLE vac_option_tab
;
----------
[ADMIN_TABLE] ANALYZE public.quad_point_tbl
;
----------
[DROP_SCHEMA] DROP SCHEMA IF EXISTS split_alter18 CASCADE
;
----------
[DROP_TABLE] -- Test 8:
-- cleanup and make sure results are also empty
DROP TABLE test_prepared1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest24" (
    "a" integer NOT NULL,
    "b" "public"."gtestdomain1" GENERATED ALWAYS AS (("a" * 2)) STORED
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fd_pt2_1 ADD CONSTRAINT p21chk CHECK (c2 <> '')
;
----------
[CREATE_PROG_OBJ] --     N    P
-- should CREATE
CREATE AGGREGATE myaggn03a(*) (SFUNC = stfp, STYPE = int4[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t2 (
  c1 integer OPTIONS (column_name 'c1'),
  c2 character varying OPTIONS (column_name 'c2') COLLATE pg_catalog."default",
  c3 text OPTIONS (column_name 'c3') COLLATE pg_catalog."POSIX"
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't2')
;
----------
[DROP_TABLE] drop table cta_test2
;
----------
[CREATE_TABLE] CREATE TABLE brin_test_bloom (a INT, b INT)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, -- skipping because c2 already exists
	ADD COLUMN c3 integer
;
----------
[CREATE_TYPE] CREATE DOMAIN "generated_virtual_tests"."gtestdomain1" AS integer
	CONSTRAINT "gtestdomain1_check" CHECK ((VALUE < 10))
;
----------
[CREATE_TABLE] -- Inheritance
CREATE TABLE person (
    id          INT NOT NULL PRIMARY KEY,
	name 		text,
	age			int4,
	location 	point
)
;
----------
[CREATE_SCHEMA] /*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2023
 *
 * Copyright (c) 2003-2024, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 6.2
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
----------
[CREATE_TABLE] -- test elem <@ range operator
create table test_range_elem(i int4)
;
----------
[CREATE_TABLE] -- check for rollback of ANALYZE corrupting table property flags (bug #11638)
CREATE TABLE check_fk_presence_1 (id int PRIMARY KEY, t text)
;
----------
[CREATE_TABLE|UNSAFE] CREATE TEMP TABLE split_values_copy AS EXECUTE split_values_plan WITH NO DATA
;
----------
[CREATE_TABLE|SELECT] -- pg_stat_have_stats returns true for committed index creation
CREATE table stats_test_tab1 as select generate_series(1,10) a
;
----------
[DROP_TRIGGER] -- without AR trigger, cascaded deletes all end up in one transition table
drop trigger self_ref_r_trig on self_ref
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."key_dependent_view" AS
SELECT
    NULL::integer AS "key",
    NULL::character varying(20) AS "data"
;
----------
[DROP_TRIGGER] DROP TRIGGER c1_update_trig ON part_c_1_100
;
----------
[CREATE_PROG_OBJ] --
-- Tests for polymorphic SQL functions and aggregates based on them.
-- Tests for other features related to function-calling have snuck in, too.
--

create function polyf(x anyelement) returns anyelement as $$
  select x + 1
$$ language sql
;
----------
[ADD_INDEX] --
-- Non-spurious duplicate violation tests
--
create unique index key_index on insertconflicttest(key)
;
----------
[CREATE_TABLE] create table rlp_default_30 partition of rlp_default for values in (30)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_3 FOR TABLE tab_3
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2, INDEX_CLEANUP FALSE) pvactst
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION cube_ur_coord(cube, int4)
RETURNS float8
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE prt2_e_p3 PARTITION OF prt2_e FOR VALUES FROM (500) TO (600)
;
