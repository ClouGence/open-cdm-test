-- check schema propagation from parent

CREATE TABLE parted (
	a text,
	b int NOT NULL DEFAULT 0,
	CONSTRAINT check_a CHECK (length(a) > 0)
) PARTITION BY LIST (a)
;
-- let's do another one where the primary key constraint fails when added
create table atacc1 ( test int )
;
ALTER SUBSCRIPTION mysub SET (slot_name = NONE)
;
CREATE FUNCTION isn_out(ismn)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TABLE pg_toast.pg_toast_28582 RENAME TO reind_con_toast
;
ANALYZE public.mvtest_bb
;
DROP VIEW upview
;
ALTER DATABASE "isolation_regression" SET lc_monetary TO 'C'
;
CREATE FUNCTION strict_word_similarity_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
-- check proper resolution of data types for polymorphic transfn/finalfn

create function first_el_transfn(anyarray, anyelement) returns anyarray as
'select $1 || $2' language sql immutable
;
-- OK more than one equal column
create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with =)) partition by range (a, b)
;
CALL test_proc8b(_a, c => _c, b => _b)
;
ALTER TABLE ONLY "public"."tt0" OF "public"."tt_t0"
;
CREATE TYPE regtest_comptype AS (a int, b text)
;
CREATE FUNCTION btisbn13cmp(isbn13, isbn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE "public"."brinopers_multi" (
    "colname" "name",
    "typ" "text",
    "op" "text"[],
    "value" "text"[],
    "matches" integer[],
    CONSTRAINT "brinopers_multi_check" CHECK (("cardinality"("op") = "cardinality"("value"))),
    CONSTRAINT "brinopers_multi_check1" CHECK (("cardinality"("op") = "cardinality"("matches")))
)
;
ALTER TABLE old_system_table DROP COLUMN othercol
;
create unique index on parted_conflict (a, b)
;
CREATE TABLE rf_bug (id serial, description text, status rf_bug_status)
;
DROP TABLE atest2
;
DROP TABLE rls_ptbl
;
create table parted_conflict_1 (drp text, c int, a int, b text)
;
CREATE TABLE a6 ()
;
create table test_thread ( tstamp timestamp not null default cast ( timeofday ( ) as timestamp ) , thread text not null , iteration integer not null , primary key ( thread , iteration ) )
;
CREATE VIEW role_routine_grants AS
    SELECT grantor,
           grantee,
           specific_catalog,
           specific_schema,
           specific_name,
           routine_catalog,
           routine_schema,
           routine_name,
           privilege_type,
           is_grantable
    FROM routine_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
CREATE TABLE "public"."prt2_e" (
    "a" integer,
    "b" integer,
    "c" integer
)
PARTITION BY RANGE (((("b" + "a") / 2)))
;
CREATE TABLE test_part_a_2 PARTITION OF test_part_a FOR VALUES IN (6,7,8,9,10)
;
-- error
CREATE TABLE gtest1_y (b int GENERATED ALWAYS AS (x + 1) STORED) INHERITS (gtest1, gtesty)
;
drop function depth_c_tf()
;
ALTER TABLE pg_toast.pg_toast_27733 RENAME TO reind_con_toast
;
drop table tpart
;
ALTER DATABASE "template0" ALLOW_CONNECTIONS = false
;
CREATE FUNCTION dblink_connect (text, text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
-- ok

-- fail: defaults must be at end of argument list
create function dfunc(a int = 1, b int) returns int as $$
  select $1 + $2;
$$ language sql
;
CREATE VIEW pg_stat_xact_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_xact_numscans(C.oid) AS seq_scan,
            pg_stat_get_xact_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_xact_numscans(I.indexrelid))::bigint AS idx_scan,
            sum(pg_stat_get_xact_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_xact_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_xact_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_xact_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_xact_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_xact_tuples_hot_updated(C.oid) AS n_tup_hot_upd
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm', 'p')
    GROUP BY C.oid, N.nspname, C.relname
;
DROP FUNCTION page_header(IN page bytea)
;
ANALYZE loct1
;
CREATE TABLE cursor (a int)
;
-- This case is similar to above, but the referenced relation is one level
-- lower in the hierarchy.  This one fails in a different way as the above,
-- because we don't bother to protect against this case explicitly.  If the
-- current error stops happening, we'll need to add a better protection.
CREATE TABLE fk_partitioned_pk_6 (a int PRIMARY KEY) PARTITION BY list (a)
;
-- float8 check

CREATE TABLE float8tmp (a float8)
;
DROP FOREIGN TABLE copy_default
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_parser
;
DROP TABLE drop_stats_test_xact
;
CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=11528 host=/tmp/qAw2OODPba dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (two_phase = on)
;
ALTER TYPE "public"."insenum" ADD VALUE 'i5'
;
DROP COLLATION user_collation
;
ALTER INDEX "public"."trigger_parted_pkey" ATTACH PARTITION "public"."trigger_parted_p1_pkey"
;
CREATE TABLE sub_parted PARTITION OF list_parted for VALUES in (1) PARTITION BY list (b)
;
CREATE TABLE gtest29 (
    a int,
    b int GENERATED ALWAYS AS (a * 2) STORED
)
;
-- direct partition inserts should check partition bound constraint
create table range_parted (
	a text,
	b int
) partition by range (a, (b+0))
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- check schema propagation from parent

CREATE TABLE parted (
	a text,
	b int NOT NULL DEFAULT 0,
	CONSTRAINT check_a CHECK (length(a) > 0)
) PARTITION BY LIST (a)
;
----------
[CREATE_TABLE] -- let's do another one where the primary key constraint fails when added
create table atacc1 ( test int )
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION mysub SET (slot_name = NONE)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isn_out(ismn)
	RETURNS cstring
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28582 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] ANALYZE public.mvtest_bb
;
----------
[DROP_VIEW] DROP VIEW upview
;
----------
[ALTER_CATALOG] ALTER DATABASE "isolation_regression" SET lc_monetary TO 'C'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION strict_word_similarity_op(text,text)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- check proper resolution of data types for polymorphic transfn/finalfn

create function first_el_transfn(anyarray, anyelement) returns anyarray as
'select $1 || $2' language sql immutable
;
----------
[CREATE_TABLE] -- OK more than one equal column
create table idxpart (a int4range, b int4range, exclude USING GIST (a with =, b with =)) partition by range (a, b)
;
----------
[CALL_PROG_OBJ] CALL test_proc8b(_a, c => _c, b => _b)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."tt0" OF "public"."tt_t0"
;
----------
[CREATE_TYPE] CREATE TYPE regtest_comptype AS (a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION btisbn13cmp(isbn13, isbn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brinopers_multi" (
    "colname" "name",
    "typ" "text",
    "op" "text"[],
    "value" "text"[],
    "matches" integer[],
    CONSTRAINT "brinopers_multi_check" CHECK (("cardinality"("op") = "cardinality"("value"))),
    CONSTRAINT "brinopers_multi_check1" CHECK (("cardinality"("op") = "cardinality"("matches")))
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE old_system_table DROP COLUMN othercol
;
----------
[ADD_INDEX] create unique index on parted_conflict (a, b)
;
----------
[CREATE_TABLE] CREATE TABLE rf_bug (id serial, description text, status rf_bug_status)
;
----------
[DROP_TABLE] DROP TABLE atest2
;
----------
[DROP_TABLE] DROP TABLE rls_ptbl
;
----------
[CREATE_TABLE] create table parted_conflict_1 (drp text, c int, a int, b text)
;
----------
[CREATE_TABLE] CREATE TABLE a6 ()
;
----------
[CREATE_TABLE] create table test_thread ( tstamp timestamp not null default cast ( timeofday ( ) as timestamp ) , thread text not null , iteration integer not null , primary key ( thread , iteration ) )
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW role_routine_grants AS
    SELECT grantor,
           grantee,
           specific_catalog,
           specific_schema,
           specific_name,
           routine_catalog,
           routine_schema,
           routine_name,
           privilege_type,
           is_grantable
    FROM routine_privileges
    WHERE grantor IN (SELECT role_name FROM enabled_roles)
          OR grantee IN (SELECT role_name FROM enabled_roles)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."prt2_e" (
    "a" integer,
    "b" integer,
    "c" integer
)
PARTITION BY RANGE (((("b" + "a") / 2)))
;
----------
[CREATE_TABLE] CREATE TABLE test_part_a_2 PARTITION OF test_part_a FOR VALUES IN (6,7,8,9,10)
;
----------
[CREATE_TABLE] -- error
CREATE TABLE gtest1_y (b int GENERATED ALWAYS AS (x + 1) STORED) INHERITS (gtest1, gtesty)
;
----------
[DROP_PROG_OBJ] drop function depth_c_tf()
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_27733 RENAME TO reind_con_toast
;
----------
[DROP_TABLE] drop table tpart
;
----------
[ALTER_CATALOG] ALTER DATABASE "template0" ALLOW_CONNECTIONS = false
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_connect (text, text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] -- ok

-- fail: defaults must be at end of argument list
create function dfunc(a int = 1, b int) returns int as $$
  select $1 + $2;
$$ language sql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_xact_all_tables AS
    SELECT
            C.oid AS relid,
            N.nspname AS schemaname,
            C.relname AS relname,
            pg_stat_get_xact_numscans(C.oid) AS seq_scan,
            pg_stat_get_xact_tuples_returned(C.oid) AS seq_tup_read,
            sum(pg_stat_get_xact_numscans(I.indexrelid))::bigint AS idx_scan,
            sum(pg_stat_get_xact_tuples_fetched(I.indexrelid))::bigint +
            pg_stat_get_xact_tuples_fetched(C.oid) AS idx_tup_fetch,
            pg_stat_get_xact_tuples_inserted(C.oid) AS n_tup_ins,
            pg_stat_get_xact_tuples_updated(C.oid) AS n_tup_upd,
            pg_stat_get_xact_tuples_deleted(C.oid) AS n_tup_del,
            pg_stat_get_xact_tuples_hot_updated(C.oid) AS n_tup_hot_upd
    FROM pg_class C LEFT JOIN
         pg_index I ON C.oid = I.indrelid
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE C.relkind IN ('r', 't', 'm', 'p')
    GROUP BY C.oid, N.nspname, C.relname
;
----------
[DROP_PROG_OBJ] DROP FUNCTION page_header(IN page bytea)
;
----------
[ADMIN_TABLE] ANALYZE loct1
;
----------
[CREATE_TABLE] CREATE TABLE cursor (a int)
;
----------
[CREATE_TABLE] -- This case is similar to above, but the referenced relation is one level
-- lower in the hierarchy.  This one fails in a different way as the above,
-- because we don't bother to protect against this case explicitly.  If the
-- current error stops happening, we'll need to add a better protection.
CREATE TABLE fk_partitioned_pk_6 (a int PRIMARY KEY) PARTITION BY list (a)
;
----------
[CREATE_TABLE] -- float8 check

CREATE TABLE float8tmp (a float8)
;
----------
[DROP_TABLE] DROP FOREIGN TABLE copy_default
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_ts_parser
;
----------
[DROP_TABLE] DROP TABLE drop_stats_test_xact
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=11528 host=/tmp/qAw2OODPba dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (two_phase = on)
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i5'
;
----------
[DROP_POLICY] DROP COLLATION user_collation
;
----------
[ALTER_INDEX] ALTER INDEX "public"."trigger_parted_pkey" ATTACH PARTITION "public"."trigger_parted_p1_pkey"
;
----------
[CREATE_TABLE] CREATE TABLE sub_parted PARTITION OF list_parted for VALUES in (1) PARTITION BY list (b)
;
----------
[CREATE_TABLE] CREATE TABLE gtest29 (
    a int,
    b int GENERATED ALWAYS AS (a * 2) STORED
)
;
----------
[CREATE_TABLE] -- direct partition inserts should check partition bound constraint
create table range_parted (
	a text,
	b int
) partition by range (a, (b+0))
;
