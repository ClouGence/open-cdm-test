create view vv1 as select * from (tt5 cross join tt6) j(aa,bb,cc,dd)
;
ALTER TABLE pg_toast.pg_toast_28062 RENAME TO reind_con_toast
;
ALTER TABLE ONLY "public"."mlparted" ATTACH PARTITION "public"."mlparted3" FOR VALUES FROM (1, 20) TO (1, 30)
;
create table hp0 partition of hp for values with (modulus 4, remainder 0)
;
CREATE TABLE functest3 (a int)
;
CREATE TABLE "public"."check2_tbl" (
    "x" integer,
    "y" "text",
    "z" integer,
    CONSTRAINT "sequence_con" CHECK ((("x" > 3) AND ("y" <> 'check failed'::"text") AND ("z" < 8)))
)
;
drop type textrange_en_us
;
/*
 * 10. Test coverage for names stored as cstrings in indexes
 */
CREATE TABLE nametbl (c1 int, c2 name, c3 float)
;
create index 計算機用語index1 on 計算機用語 using btree (用語)
;
drop table itrtest
;
-- cannot drop column that is part of the partition key
CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (a, (a+b+1))
;
-- ok; partition collation silently overrides the default collation of type 'name'
create table test_part_coll_cast2 partition of test_part_coll_posix for values from (name 's') to ('z')
;
DROP TABLE batch_table_p0
;
CREATE SEQUENCE s1.seq1
;
ALTER INDEX pg_toast.pg_toast_28099_index RENAME TO reind_con_toast_idx
;
create table idxpart (a int4range, b int4range, c int4range) partition by range (a)
;
CREATE schema tststats
;
-- ===================================================================
-- test check constraints
-- ===================================================================

-- Consistent check constraints provide consistent results
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c2positive CHECK (c2 >= 0)
;
/*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2016
 *
 * Copyright (c) 2003-2019, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 5.1
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
CREATE VIEW xmlview2 AS SELECT xmlconcat('hello', 'you')
;
ALTER TABLE pg_description SET SCHEMA public
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_inherits
;
ALTER TABLE ONLY "public"."prt1" ATTACH PARTITION "public"."prt1_p2" FOR VALUES FROM (250) TO (500)
;
CREATE FUNCTION "public"."alter_table_under_transition_tables_upd_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE WARNING 'old table = %, new table = %',
                  (SELECT string_agg(id || '=' || name, ',') FROM d),
                  (SELECT string_agg(id || '=' || name, ',') FROM i);
  RAISE NOTICE 'one = %', (SELECT 1 FROM alter_table_under_transition_tables LIMIT 1);
  RETURN NULL;
END;
$$
;
CREATE TEXT SEARCH DICTIONARY "public"."hunspell_num" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_num', afffile = 'hunspell_sample_num' )
;
CREATE TABLE collate_test3 (
    a int,
    b text COLLATE "C"
)
;
CREATE TABLE h2part_1 PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 4, REMAINDER 2)
;
/*
 * 5.38
 * PERIODS view
 */

-- feature not supported


/*
 * 5.39
 * PRIVATE_PARAMETERS view
 */

-- feature not supported


/*
 * 5.40
 * REFERENCED_TYPES view
 */

-- feature not supported


/*
 * 5.41
 * REFERENTIAL_CONSTRAINTS view
 */

CREATE VIEW referential_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(ncon.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(
             CASE WHEN npkc.nspname IS NULL THEN NULL
                  ELSE current_database() END
             AS sql_identifier) AS unique_constraint_catalog,
           CAST(npkc.nspname AS sql_identifier) AS unique_constraint_schema,
           CAST(pkc.conname AS sql_identifier) AS unique_constraint_name,

           CAST(
             CASE con.confmatchtype WHEN 'f' THEN 'FULL'
                                    WHEN 'p' THEN 'PARTIAL'
                                    WHEN 's' THEN 'NONE' END
             AS character_data) AS match_option,

           CAST(
             CASE con.confupdtype WHEN 'c' THEN 'CASCADE'
                                  WHEN 'n' THEN 'SET NULL'
                                  WHEN 'd' THEN 'SET DEFAULT'
                                  WHEN 'r' THEN 'RESTRICT'
                                  WHEN 'a' THEN 'NO ACTION' END
             AS character_data) AS update_rule,

           CAST(
             CASE con.confdeltype WHEN 'c' THEN 'CASCADE'
                                  WHEN 'n' THEN 'SET NULL'
                                  WHEN 'd' THEN 'SET DEFAULT'
                                  WHEN 'r' THEN 'RESTRICT'
                                  WHEN 'a' THEN 'NO ACTION' END
             AS character_data) AS delete_rule

    FROM (pg_namespace ncon
          INNER JOIN pg_constraint con ON ncon.oid = con.connamespace
          INNER JOIN pg_class c ON con.conrelid = c.oid AND con.contype = 'f')
         LEFT JOIN pg_depend d1  -- find constraint's dependency on an index
          ON d1.objid = con.oid AND d1.classid = 'pg_constraint'::regclass
             AND d1.refclassid = 'pg_class'::regclass AND d1.refobjsubid = 0
         LEFT JOIN pg_depend d2  -- find pkey/unique constraint for that index
          ON d2.refclassid = 'pg_constraint'::regclass
             AND d2.classid = 'pg_class'::regclass
             AND d2.objid = d1.refobjid AND d2.objsubid = 0
             AND d2.deptype = 'i'
         LEFT JOIN pg_constraint pkc ON pkc.oid = d2.refobjid
            AND pkc.contype IN ('p', 'u')
            AND pkc.conrelid = con.confrelid
         LEFT JOIN pg_namespace npkc ON pkc.connamespace = npkc.oid

    WHERE pg_has_role(c.relowner, 'USAGE')
          -- SELECT privilege omitted, per SQL standard
          OR has_table_privilege(c.oid, 'INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
          OR has_any_column_privilege(c.oid, 'INSERT, UPDATE, REFERENCES')
;
ALTER TABLE ONLY "generated_virtual_tests"."gtest21a"
    ADD CONSTRAINT "gtest21a_pkey" PRIMARY KEY ("a")
;
CREATE FUNCTION defined(hstore,text)
RETURNS bool
AS '$libdir/hstore','hstore_defined'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- ************************************************************
-- * Support function to set the opponents backlink field
-- * if it does not already point to the requested slot
-- ************************************************************
create function tg_backlink_set(myname bpchar, blname bpchar)
returns integer as '
declare
    mytype	char(2);
    link	char(4);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    link := mytype || substr(blname, 1, 2);
    if link = ''PLPL'' then
        raise exception
		''backlink between two phone lines does not make sense'';
    end if;
    if link in (''PLWS'', ''WSPL'') then
        raise exception
		''direct link of phone line to wall slot not permitted'';
    end if;
    if mytype = ''PS'' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.backlink != blname then
	    update PSlot set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''WS'' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.backlink != blname then
	    update WSlot set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''PL'' then
        select into rec * from PLine where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.backlink != blname then
	    update PLine set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    raise exception ''illegal backlink beginning with %'', mytype;
end;
' language plpgsql
;
-- Check case of a type present only on the remote server.
-- We can fake this by dropping the type locally in our transaction.
CREATE TYPE "Colors" AS ENUM ('red', 'green', 'blue')
;
ALTER INDEX pg_toast.pg_toast_27643_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION "public"."transition_table_level2_ri_child_insupd_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  BEGIN
    PERFORM FROM i
      LEFT JOIN transition_table_level1 p
        ON p.level1_no IS NOT NULL AND p.level1_no = i.parent_no
      WHERE p.level1_no IS NULL;
    IF FOUND THEN
      RAISE EXCEPTION 'RI error';
    END IF;
    RETURN NULL;
  END;
$$
;
CREATE TABLE parallel_vacuum_table (a int) WITH (autovacuum_enabled = off)
;
CREATE TABLE fail_part (LIKE list_parted)
;
CREATE TABLE trunc_trig (id serial PRIMARY KEY, id2 text)
;
-- sql, proretset = t, prorettype = c
CREATE FUNCTION getrngfunc5(int) RETURNS setof rngfunc AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
create trigger merge_skip BEFORE INSERT OR UPDATE or DELETE
  ON target FOR EACH ROW EXECUTE FUNCTION skip_merge_op()
;
CREATE FUNCTION fn_x_before () RETURNS TRIGGER AS '
  BEGIN
		NEW.e := ''before trigger fired''::text;
		return NEW;
	END;
' LANGUAGE plpgsql
;
ALTER TABLE testpub_tbl8 ATTACH PARTITION testpub_tbl8_1 FOR VALUES WITH (modulus 2, remainder 1)
;
-- Check that we allow access to a child table's statistics when the user
-- has permissions only for the parent table.
create table permtest_parent (a int, b text, c text) partition by list (a)
;
CREATE TABLE test23 (a int, b text COLLATE case_insensitive) PARTITION BY HASH (b)
;
--
-- ALTER and CREATE/DROP extra attribute permissions
--
CREATE TABLE regtest_table_4 (x int primary key, y int, z int)
;
alter table ac add constraint ac_check check (aa is not null)
;
CREATE FUNCTION isnge(isbn13, isbn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
--
-- CREATE SEQUENCE
--

-- various error cases
CREATE UNLOGGED SEQUENCE sequence_testx
;
CREATE FUNCTION isdefined(hstore,text)
RETURNS bool
AS '$libdir/hstore','hstore_defined'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FUNCTION gbt_cash_consistent(internal,money,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE pg_temp_13.pg_temp_26101
;
------------------------------------------------------------------------------------------
[CREATE_VIEW(SELECT)] create view vv1 as select * from (tt5 cross join tt6) j(aa,bb,cc,dd)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28062 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted" ATTACH PARTITION "public"."mlparted3" FOR VALUES FROM (1, 20) TO (1, 30)
;
----------
[CREATE_TABLE] create table hp0 partition of hp for values with (modulus 4, remainder 0)
;
----------
[CREATE_TABLE] CREATE TABLE functest3 (a int)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."check2_tbl" (
    "x" integer,
    "y" "text",
    "z" integer,
    CONSTRAINT "sequence_con" CHECK ((("x" > 3) AND ("y" <> 'check failed'::"text") AND ("z" < 8)))
)
;
----------
[DROP_TYPE] drop type textrange_en_us
;
----------
[CREATE_TABLE] /*
 * 10. Test coverage for names stored as cstrings in indexes
 */
CREATE TABLE nametbl (c1 int, c2 name, c3 float)
;
----------
[ADD_INDEX] create index 計算機用語index1 on 計算機用語 using btree (用語)
;
----------
[DROP_TABLE] drop table itrtest
;
----------
[CREATE_TABLE] -- cannot drop column that is part of the partition key
CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (a, (a+b+1))
;
----------
[CREATE_TABLE] -- ok; partition collation silently overrides the default collation of type 'name'
create table test_part_coll_cast2 partition of test_part_coll_posix for values from (name 's') to ('z')
;
----------
[DROP_TABLE] DROP TABLE batch_table_p0
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE s1.seq1
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28099_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range, c int4range) partition by range (a)
;
----------
[CREATE_SCHEMA] CREATE schema tststats
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- ===================================================================
-- test check constraints
-- ===================================================================

-- Consistent check constraints provide consistent results
ALTER FOREIGN TABLE ft1 ADD CONSTRAINT ft1_c2positive CHECK (c2 >= 0)
;
----------
[CREATE_SCHEMA] /*
 * SQL Information Schema
 * as defined in ISO/IEC 9075-11:2016
 *
 * Copyright (c) 2003-2019, PostgreSQL Global Development Group
 *
 * src/backend/catalog/information_schema.sql
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

/*
 * Note: Generally, the definitions in this file should be ordered
 * according to the clause numbers in the SQL standard, which is also the
 * alphabetical order.  In some cases it is convenient or necessary to
 * define one information schema view by using another one; in that case,
 * put the referencing view at the very end and leave a note where it
 * should have been put.
 */


/*
 * 5.1
 * INFORMATION_SCHEMA schema
 */

CREATE SCHEMA information_schema
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmlview2 AS SELECT xmlconcat('hello', 'you')
;
----------
[ALTER_TABLE] ALTER TABLE pg_description SET SCHEMA public
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_inherits
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1" ATTACH PARTITION "public"."prt1_p2" FOR VALUES FROM (250) TO (500)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."alter_table_under_transition_tables_upd_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE WARNING 'old table = %, new table = %',
                  (SELECT string_agg(id || '=' || name, ',') FROM d),
                  (SELECT string_agg(id || '=' || name, ',') FROM i);
  RAISE NOTICE 'one = %', (SELECT 1 FROM alter_table_under_transition_tables LIMIT 1);
  RETURN NULL;
END;
$$
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."hunspell_num" (
    TEMPLATE = "pg_catalog"."ispell",
    dictfile = 'hunspell_sample_num', afffile = 'hunspell_sample_num' )
;
----------
[CREATE_TABLE] CREATE TABLE collate_test3 (
    a int,
    b text COLLATE "C"
)
;
----------
[CREATE_TABLE] CREATE TABLE h2part_1 PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 4, REMAINDER 2)
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.38
 * PERIODS view
 */

-- feature not supported


/*
 * 5.39
 * PRIVATE_PARAMETERS view
 */

-- feature not supported


/*
 * 5.40
 * REFERENCED_TYPES view
 */

-- feature not supported


/*
 * 5.41
 * REFERENTIAL_CONSTRAINTS view
 */

CREATE VIEW referential_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(ncon.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(
             CASE WHEN npkc.nspname IS NULL THEN NULL
                  ELSE current_database() END
             AS sql_identifier) AS unique_constraint_catalog,
           CAST(npkc.nspname AS sql_identifier) AS unique_constraint_schema,
           CAST(pkc.conname AS sql_identifier) AS unique_constraint_name,

           CAST(
             CASE con.confmatchtype WHEN 'f' THEN 'FULL'
                                    WHEN 'p' THEN 'PARTIAL'
                                    WHEN 's' THEN 'NONE' END
             AS character_data) AS match_option,

           CAST(
             CASE con.confupdtype WHEN 'c' THEN 'CASCADE'
                                  WHEN 'n' THEN 'SET NULL'
                                  WHEN 'd' THEN 'SET DEFAULT'
                                  WHEN 'r' THEN 'RESTRICT'
                                  WHEN 'a' THEN 'NO ACTION' END
             AS character_data) AS update_rule,

           CAST(
             CASE con.confdeltype WHEN 'c' THEN 'CASCADE'
                                  WHEN 'n' THEN 'SET NULL'
                                  WHEN 'd' THEN 'SET DEFAULT'
                                  WHEN 'r' THEN 'RESTRICT'
                                  WHEN 'a' THEN 'NO ACTION' END
             AS character_data) AS delete_rule

    FROM (pg_namespace ncon
          INNER JOIN pg_constraint con ON ncon.oid = con.connamespace
          INNER JOIN pg_class c ON con.conrelid = c.oid AND con.contype = 'f')
         LEFT JOIN pg_depend d1  -- find constraint's dependency on an index
          ON d1.objid = con.oid AND d1.classid = 'pg_constraint'::regclass
             AND d1.refclassid = 'pg_class'::regclass AND d1.refobjsubid = 0
         LEFT JOIN pg_depend d2  -- find pkey/unique constraint for that index
          ON d2.refclassid = 'pg_constraint'::regclass
             AND d2.classid = 'pg_class'::regclass
             AND d2.objid = d1.refobjid AND d2.objsubid = 0
             AND d2.deptype = 'i'
         LEFT JOIN pg_constraint pkc ON pkc.oid = d2.refobjid
            AND pkc.contype IN ('p', 'u')
            AND pkc.conrelid = con.confrelid
         LEFT JOIN pg_namespace npkc ON pkc.connamespace = npkc.oid

    WHERE pg_has_role(c.relowner, 'USAGE')
          -- SELECT privilege omitted, per SQL standard
          OR has_table_privilege(c.oid, 'INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
          OR has_any_column_privilege(c.oid, 'INSERT, UPDATE, REFERENCES')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_virtual_tests"."gtest21a"
    ADD CONSTRAINT "gtest21a_pkey" PRIMARY KEY ("a")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION defined(hstore,text)
RETURNS bool
AS '$libdir/hstore','hstore_defined'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- ************************************************************
-- * Support function to set the opponents backlink field
-- * if it does not already point to the requested slot
-- ************************************************************
create function tg_backlink_set(myname bpchar, blname bpchar)
returns integer as '
declare
    mytype	char(2);
    link	char(4);
    rec		record;
begin
    mytype := substr(myname, 1, 2);
    link := mytype || substr(blname, 1, 2);
    if link = ''PLPL'' then
        raise exception
		''backlink between two phone lines does not make sense'';
    end if;
    if link in (''PLWS'', ''WSPL'') then
        raise exception
		''direct link of phone line to wall slot not permitted'';
    end if;
    if mytype = ''PS'' then
        select into rec * from PSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.backlink != blname then
	    update PSlot set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''WS'' then
        select into rec * from WSlot where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.backlink != blname then
	    update WSlot set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    if mytype = ''PL'' then
        select into rec * from PLine where slotname = myname;
	if not found then
	    raise exception ''% does not exist'', myname;
	end if;
	if rec.backlink != blname then
	    update PLine set backlink = blname where slotname = myname;
	end if;
	return 0;
    end if;
    raise exception ''illegal backlink beginning with %'', mytype;
end;
' language plpgsql
;
----------
[CREATE_TYPE] -- Check case of a type present only on the remote server.
-- We can fake this by dropping the type locally in our transaction.
CREATE TYPE "Colors" AS ENUM ('red', 'green', 'blue')
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_27643_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."transition_table_level2_ri_child_insupd_func"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
  BEGIN
    PERFORM FROM i
      LEFT JOIN transition_table_level1 p
        ON p.level1_no IS NOT NULL AND p.level1_no = i.parent_no
      WHERE p.level1_no IS NULL;
    IF FOUND THEN
      RAISE EXCEPTION 'RI error';
    END IF;
    RETURN NULL;
  END;
$$
;
----------
[CREATE_TABLE] CREATE TABLE parallel_vacuum_table (a int) WITH (autovacuum_enabled = off)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part (LIKE list_parted)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_trig (id serial PRIMARY KEY, id2 text)
;
----------
[CREATE_PROG_OBJ] -- sql, proretset = t, prorettype = c
CREATE FUNCTION getrngfunc5(int) RETURNS setof rngfunc AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger merge_skip BEFORE INSERT OR UPDATE or DELETE
  ON target FOR EACH ROW EXECUTE FUNCTION skip_merge_op()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION fn_x_before () RETURNS TRIGGER AS '
  BEGIN
		NEW.e := ''before trigger fired''::text;
		return NEW;
	END;
' LANGUAGE plpgsql
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE testpub_tbl8 ATTACH PARTITION testpub_tbl8_1 FOR VALUES WITH (modulus 2, remainder 1)
;
----------
[CREATE_TABLE] -- Check that we allow access to a child table's statistics when the user
-- has permissions only for the parent table.
create table permtest_parent (a int, b text, c text) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE test23 (a int, b text COLLATE case_insensitive) PARTITION BY HASH (b)
;
----------
[CREATE_TABLE] --
-- ALTER and CREATE/DROP extra attribute permissions
--
CREATE TABLE regtest_table_4 (x int primary key, y int, z int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table ac add constraint ac_check check (aa is not null)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(isbn13, isbn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_SEQUENCE] --
-- CREATE SEQUENCE
--

-- various error cases
CREATE UNLOGGED SEQUENCE sequence_testx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isdefined(hstore,text)
RETURNS bool
AS '$libdir/hstore','hstore_defined'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_cash_consistent(internal,money,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26101
;
