-- too few params
EXECUTE q3('bool')
;
LOCK TABLE "public"."itest6" IN ACCESS SHARE MODE
;
EXECUTE dumpBaseType('18462')
;
-- ... not that the initial replacement didn't have some bugs too
begin
;
-- More isolation levels
BEGIN TRANSACTION DEFERRABLE
;
-- The ndistinct extended statistics on (x, y, z) provides more reliable value
-- of bucket size.
CREATE STATISTICS extstat_sb_2 (ndistinct) ON x, y, z FROM sb_2
;
DROP USER MAPPING FOR user SERVER ss4
;
EXECUTE dumpTableAttach('39222')
;
ALTER VIEW public.toyemp OWNER TO zyc
;
LOCK TABLE fkpart4.dropfk IN ACCESS SHARE MODE
;
------------------------------------------------------------------------------------------
[UNSAFE] -- too few params
EXECUTE q3('bool')
;
----------
[SESSION_LOCK] LOCK TABLE "public"."itest6" IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpBaseType('18462')
;
----------
[TRANSACTION] -- ... not that the initial replacement didn't have some bugs too
begin
;
----------
[TRANSACTION] -- More isolation levels
BEGIN TRANSACTION DEFERRABLE
;
----------
[ADMIN_PERFORMANCE] -- The ndistinct extended statistics on (x, y, z) provides more reliable value
-- of bucket size.
CREATE STATISTICS extstat_sb_2 (ndistinct) ON x, y, z FROM sb_2
;
----------
[REVOKE] DROP USER MAPPING FOR user SERVER ss4
;
----------
[UNSAFE] EXECUTE dumpTableAttach('39222')
;
----------
[TRANSFER_PRIVILEGE] ALTER VIEW public.toyemp OWNER TO zyc
;
----------
[SESSION_LOCK] LOCK TABLE fkpart4.dropfk IN ACCESS SHARE MODE
;
