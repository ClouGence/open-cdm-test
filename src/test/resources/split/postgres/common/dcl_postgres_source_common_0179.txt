-- explicit transaction
BEGIN
;
LOCK TABLE "public"."xy_1" IN ACCESS SHARE MODE
;
EXECUTE dumpFunc('36929')
;
-- permissions
SET ROLE regress_publication_user2
;
LOCK TABLE public.trigger_parted_p1 IN ACCESS SHARE MODE
;
ALTER SEQUENCE public.rules_log_id_seq OWNER TO zyc
;
-- create table as execute
PREPARE q5(int, text) AS
	SELECT * FROM tenk1 WHERE unique1 = $1 OR stringu1 = $2
	ORDER BY unique1
;
ALTER FUNCTION "public"."stricttest"() OWNER TO zyc
;
-- error
ROLLBACK
;
ALTER TABLE public.pagg_tab_para_p2 OWNER TO zyc
;
------------------------------------------------------------------------------------------
[TRANSACTION] -- explicit transaction
BEGIN
;
----------
[SESSION_LOCK] LOCK TABLE "public"."xy_1" IN ACCESS SHARE MODE
;
----------
[UNSAFE] EXECUTE dumpFunc('36929')
;
----------
[SWITCH_ROLE] -- permissions
SET ROLE regress_publication_user2
;
----------
[SESSION_LOCK] LOCK TABLE public.trigger_parted_p1 IN ACCESS SHARE MODE
;
----------
[TRANSFER_PRIVILEGE] ALTER SEQUENCE public.rules_log_id_seq OWNER TO zyc
;
----------
[UNSAFE] -- create table as execute
PREPARE q5(int, text) AS
	SELECT * FROM tenk1 WHERE unique1 = $1 OR stringu1 = $2
	ORDER BY unique1
;
----------
[TRANSFER_PRIVILEGE] ALTER FUNCTION "public"."stricttest"() OWNER TO zyc
;
----------
[TRANSACTION] -- error
ROLLBACK
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.pagg_tab_para_p2 OWNER TO zyc
;
