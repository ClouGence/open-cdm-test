drop trigger trg1 on trigpart3
;
ANALYZE public.dropcolumnexists
;
DROP TABLE pg_temp_32.pg_temp_28237_2, pg_temp_32.pg_temp_28237
;
ALTER INDEX pg_toast.pg_toast_30899_index RENAME TO reind_con_toast_idx
;
ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk
  ON DELETE CASCADE ON UPDATE CASCADE
;
create table tab_batch_sharded_p1_remote (id int, data text)
;
CREATE TABLE plt1_adv_p2 PARTITION OF plt1_adv FOR VALUES IN ('0002')
;
COMMENT ON AGGREGATE split_agg_probe.sum_int(integer) IS NULL
;
VACUUM (FREEZE) pg_catalog.pg_trigger
;
drop schema if exists abc,aac
;
CREATE FUNCTION pg_buffercache_pages()
RETURNS SETOF RECORD
AS '$libdir/pg_buffercache', 'pg_buffercache_pages'
LANGUAGE C PARALLEL SAFE
;
create function simplesql() returns int language sql
as $$select 1 / 0$$
;
ALTER TABLE ONLY "public"."alpha" ATTACH PARTITION "public"."alpha_pos" FOR VALUES FROM ('0') TO ('10')
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_namespace
;
ALTER FOREIGN TABLE ft1 ADD PRIMARY KEY (c7)
;
VACUUM (PARALLEL 2) pg_catalog.pg_description
;
--ok
CREATE TABLE notnull_tbl1_1 PARTITION OF notnull_tbl1 FOR VALUES IN (1,2)
;
CREATE TABLE reindex_schema.tab_locked (a int PRIMARY KEY)
;
alter table idxpart add exclude USING GIST (a with =, b with =)
;
create trigger trig_table_update_trig
  after update on trig_table referencing old table as old_table new table as new_table
  for each statement execute procedure dump_update()
;
alter table parted_irreg attach partition parted1_irreg
  for values from ('aaaa') to ('bbbb')
;
VACUUM ANALYZE brin_test_bloom
;
CREATE RULE shoelace_ins AS ON INSERT TO shoelace
        DO INSTEAD
        INSERT INTO shoelace_data VALUES (
               NEW.sl_name,
               NEW.sl_avail,
               NEW.sl_color,
               NEW.sl_len,
               NEW.sl_unit)
;
CREATE TABLE stream_test2(data text)
;
create table mc3p1 partition of mc3p
  for values from (1, 1, 1) to (2, minvalue, minvalue)
;
ALTER INDEX pg_toast.pg_toast_31098_index RENAME TO reind_con_toast_idx
;
--
-- multiple levels of partitioning
--
CREATE TABLE prt1_l (a int, b int, c varchar) PARTITION BY RANGE(a)
;
ALTER TABLE tbl2 ALTER COLUMN val3 TYPE character varying
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_attrdef
;
CREATE FUNCTION gist_translate_cmptype_btree(int)
RETURNS smallint
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
CREATE TABLE target_pa1 PARTITION OF target_pa FOR VALUES FROM (0) TO (200)
;
CREATE TABLE tt0 (x int NOT NULL, y numeric(8,2))
;
ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk3" FOR VALUES FROM (2000) TO (3000)
;
-- test handling of a mix of compatible and incompatible expressions
CREATE TABLE expr_stats_incompatible_test (
    c0 double precision,
    c1 boolean NOT NULL
)
;
create table t3()
;
CREATE TABLE "public"."rtest_t1" (
    "a" integer,
    "b" integer
)
;
-- check change of column type resets the MCV statistics
ALTER TABLE mcv_lists ALTER COLUMN c TYPE numeric
;
DROP FUNCTION immut_func(int)
;
drop function makedcomp(float8, float8)
;
create table sk (a int, b int)
;
--- test copying in CSV mode with various styles
--- of embedded line ending characters

create temp table copytest (
	style	text,
	test 	text,
	filler	int)
;
CREATE INDEX t1_brin ON s3.t1 USING BRIN (i)
;
CREATE SCHEMA pub_testpart2
;
ALTER PUBLICATION split_pub18_cols DROP TABLE ONLY split_pub18_a.accounts_extra
;
DROP DATABASE dropme_db2
;
ANALYZE stats_import.part_parent
;
--
-- list partitioned by expression
--
CREATE TABLE plt1_e (a int, b int, c text) PARTITION BY LIST(ltrim(c, 'A'))
;
-- record type returning with quotes behavior.
CREATE TYPE comp_abc AS (a text, b int, c timestamp)
;
create table part_gg1 partition of part_gg for values from (minvalue) to (1)
;
ALTER MATERIALIZED VIEW split_view_probe.base_mv_renamed RESET (fillfactor)
;
------------------------------------------------------------------------------------------
[DROP_TRIGGER] drop trigger trg1 on trigpart3
;
----------
[ADMIN_TABLE] ANALYZE public.dropcolumnexists
;
----------
[DROP_TABLE] DROP TABLE pg_temp_32.pg_temp_28237_2, pg_temp_32.pg_temp_28237
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30899_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk
  ON DELETE CASCADE ON UPDATE CASCADE
;
----------
[CREATE_TABLE] create table tab_batch_sharded_p1_remote (id int, data text)
;
----------
[CREATE_TABLE] CREATE TABLE plt1_adv_p2 PARTITION OF plt1_adv FOR VALUES IN ('0002')
;
----------
[COMMENT_PROG_OBJ] COMMENT ON AGGREGATE split_agg_probe.sum_int(integer) IS NULL
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_trigger
;
----------
[DROP_SCHEMA] drop schema if exists abc,aac
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_buffercache_pages()
RETURNS SETOF RECORD
AS '$libdir/pg_buffercache', 'pg_buffercache_pages'
LANGUAGE C PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] create function simplesql() returns int language sql
as $$select 1 / 0$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."alpha" ATTACH PARTITION "public"."alpha_pos" FOR VALUES FROM ('0') TO ('10')
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_namespace
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER FOREIGN TABLE ft1 ADD PRIMARY KEY (c7)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_description
;
----------
[CREATE_TABLE] --ok
CREATE TABLE notnull_tbl1_1 PARTITION OF notnull_tbl1 FOR VALUES IN (1,2)
;
----------
[CREATE_TABLE] CREATE TABLE reindex_schema.tab_locked (a int PRIMARY KEY)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table idxpart add exclude USING GIST (a with =, b with =)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trig_table_update_trig
  after update on trig_table referencing old table as old_table new table as new_table
  for each statement execute procedure dump_update()
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table parted_irreg attach partition parted1_irreg
  for values from ('aaaa') to ('bbbb')
;
----------
[ADMIN_TABLE] VACUUM ANALYZE brin_test_bloom
;
----------
[CREATE_POLICY] CREATE RULE shoelace_ins AS ON INSERT TO shoelace
        DO INSTEAD
        INSERT INTO shoelace_data VALUES (
               NEW.sl_name,
               NEW.sl_avail,
               NEW.sl_color,
               NEW.sl_len,
               NEW.sl_unit)
;
----------
[CREATE_TABLE] CREATE TABLE stream_test2(data text)
;
----------
[CREATE_TABLE] create table mc3p1 partition of mc3p
  for values from (1, 1, 1) to (2, minvalue, minvalue)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_31098_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] --
-- multiple levels of partitioning
--
CREATE TABLE prt1_l (a int, b int, c varchar) PARTITION BY RANGE(a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE tbl2 ALTER COLUMN val3 TYPE character varying
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_attrdef
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gist_translate_cmptype_btree(int)
RETURNS smallint
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE target_pa1 PARTITION OF target_pa FOR VALUES FROM (0) TO (200)
;
----------
[CREATE_TABLE] CREATE TABLE tt0 (x int NOT NULL, y numeric(8,2))
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk3" FOR VALUES FROM (2000) TO (3000)
;
----------
[CREATE_TABLE] -- test handling of a mix of compatible and incompatible expressions
CREATE TABLE expr_stats_incompatible_test (
    c0 double precision,
    c1 boolean NOT NULL
)
;
----------
[CREATE_TABLE] create table t3()
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_t1" (
    "a" integer,
    "b" integer
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- check change of column type resets the MCV statistics
ALTER TABLE mcv_lists ALTER COLUMN c TYPE numeric
;
----------
[DROP_PROG_OBJ] DROP FUNCTION immut_func(int)
;
----------
[DROP_PROG_OBJ] drop function makedcomp(float8, float8)
;
----------
[CREATE_TABLE] create table sk (a int, b int)
;
----------
[CREATE_TABLE] --- test copying in CSV mode with various styles
--- of embedded line ending characters

create temp table copytest (
	style	text,
	test 	text,
	filler	int)
;
----------
[ADD_INDEX] CREATE INDEX t1_brin ON s3.t1 USING BRIN (i)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA pub_testpart2
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION split_pub18_cols DROP TABLE ONLY split_pub18_a.accounts_extra
;
----------
[DROP_CATALOG] DROP DATABASE dropme_db2
;
----------
[ADMIN_TABLE] ANALYZE stats_import.part_parent
;
----------
[CREATE_TABLE] --
-- list partitioned by expression
--
CREATE TABLE plt1_e (a int, b int, c text) PARTITION BY LIST(ltrim(c, 'A'))
;
----------
[CREATE_TYPE] -- record type returning with quotes behavior.
CREATE TYPE comp_abc AS (a text, b int, c timestamp)
;
----------
[CREATE_TABLE] create table part_gg1 partition of part_gg for values from (minvalue) to (1)
;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW split_view_probe.base_mv_renamed RESET (fillfactor)
;
