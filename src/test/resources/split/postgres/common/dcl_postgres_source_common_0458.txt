EXECUTE dumpAgg('40056')
;
RESET force_parallel_mode
;
DROP ROLE regress_readallstats
;
ALTER TABLE public.timestamptz_tbl OWNER TO zyc
;
LOCK TABLE dump_test.table_index_stats IN ACCESS SHARE MODE NOWAIT
;
EXECUTE dumpFunc('30873')
;
-- The invalid connections get closed in pgfdw_xact_callback during commit.
COMMIT
;
ALTER USER MAPPING FOR public SERVER testserver1
	OPTIONS (ADD oauth_issuer 'https://example.com')
;
-- Only partitioned table owned by other user.
ALTER TABLE vacowned_parted OWNER TO regress_vacuum
;
ALTER TABLE public.gin_test_tbl OWNER TO zyc
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpAgg('40056')
;
----------
[SESSION_SETTING_WRITE] RESET force_parallel_mode
;
----------
[DROP_ROLE] DROP ROLE regress_readallstats
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.timestamptz_tbl OWNER TO zyc
;
----------
[SESSION_LOCK] LOCK TABLE dump_test.table_index_stats IN ACCESS SHARE MODE NOWAIT
;
----------
[UNSAFE] EXECUTE dumpFunc('30873')
;
----------
[TRANSACTION] -- The invalid connections get closed in pgfdw_xact_callback during commit.
COMMIT
;
----------
[GRANT] ALTER USER MAPPING FOR public SERVER testserver1
	OPTIONS (ADD oauth_issuer 'https://example.com')
;
----------
[TRANSFER_PRIVILEGE] -- Only partitioned table owned by other user.
ALTER TABLE vacowned_parted OWNER TO regress_vacuum
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.gin_test_tbl OWNER TO zyc
;
