DROP TABLE corrupt2
;
CREATE VIEW rls_view AS SELECT * FROM rls_tbl
;
-- Test that an index-only scan is not chosen, when the query involves the
-- circle column (the circle opclass does not support index-only scans).
create index gist_tbl_multi_index on gist_tbl using gist (p, c)
;
ALTER INDEX pg_toast.pg_toast_28261_index RENAME TO reind_con_toast_idx
;
drop function sum4(int8,int8,int8,int8)
;
--- test hash index

create index tstidx on ltreetest using hash (t)
;
drop table copyfreeze
;
CREATE TABLE clstr_tst (a SERIAL PRIMARY KEY,
	b INT,
	c TEXT,
	d TEXT,
	CONSTRAINT clstr_tst_con FOREIGN KEY (b) REFERENCES clstr_tst_s)
;
ALTER SEQUENCE sequence_test12 AS int
;
DROP DATABASE sepgsql_test_regression
;
CREATE FUNCTION gin_compare_prefix_uuid(uuid, uuid, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
ALTER TABLE x1 ENABLE ROW LEVEL SECURITY
;
DROP VIEW view_stats_1
;
CREATE FUNCTION read_rel_block_ll(
    rel regclass,
    blockno int,
    nblocks int DEFAULT 1,
    wait_complete bool DEFAULT true,
    batchmode_enter bool DEFAULT false,
    smgrreleaseall bool DEFAULT false,
    batchmode_exit bool DEFAULT false,
    zero_on_error bool DEFAULT false)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
-- error, ambiguous


-- CREATE OR REPLACE tests

CREATE FUNCTION functest1(a int) RETURNS int LANGUAGE SQL AS 'SELECT $1'
;
DROP VIEW IF EXISTS no_such_schema.foo
;
-- error, already set
ALTER TABLE itest4 ALTER COLUMN b ADD GENERATED ALWAYS AS IDENTITY
;
-- output arguments

CREATE PROCEDURE ptest4a(INOUT a int, INOUT b int)
LANGUAGE SQL
AS $$
SELECT 1, 2;
$$
;
DROP TABLE country_table
;
create table idxpart2 (c int, like idxpart)
;
CREATE TEXT SEARCH DICTIONARY "public"."thesaurus" (
    TEMPLATE = "pg_catalog"."thesaurus",
    dictfile = 'thesaurus_sample', dictionary = 'english_stem' )
;
CREATE TRIGGER error_del_when BEFORE INSERT OR UPDATE ON main_table
FOR EACH ROW WHEN (NEW.tableoid <> 0)
EXECUTE PROCEDURE trigger_func('error_when_sys_column')
;
-- test python -> hstore[]
CREATE FUNCTION test2arr() RETURNS hstore[]
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
val = [{'a': 1, 'b': 'boo', 'c': None}, {'d': 2}]
return val
$$
;
CREATE TABLE tx2 (b integer, c integer)
;
-- error

CREATE TABLE test11pk (x text COLLATE case_insensitive PRIMARY KEY)
;
alter table myview alter column test drop not null
;
-- reattach the leaf partition index; parents should now be valid
alter index idxpart1_expr_idx attach partition idxpart11_expr_idx
;
drop function rngfuncbar()
;
-- CREATE MATERIALIZED VIEW ... IF NOT EXISTS
CREATE MATERIALIZED VIEW matview_ine_tab AS SELECT 1
;
CREATE FUNCTION gbt_bpchar_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
-- CREATE/DROP COLLATION

CREATE COLLATION mycoll1 FROM "C"
;
ALTER TABLE ONLY "public"."plt1" ATTACH PARTITION "public"."plt1_p2" FOR VALUES IN ('0001', '0005', '0002', '0009')
;
CREATE TABLE a2 ()
;
CREATE FOREIGN TABLE ftprt2_p1 (b int, c varchar, a int)
	SERVER loopback OPTIONS (table_name 'fprt2_p1', use_remote_estimate 'true')
;
-- pg_freespace shows the recorded space avail at each block in a relation
CREATE FUNCTION
  pg_freespace(rel regclass, blkno OUT bigint, avail OUT int2)
RETURNS SETOF RECORD
AS $$
  SELECT blkno, pg_freespace($1, blkno) AS avail
  FROM generate_series(0, pg_relation_size($1) / current_setting('block_size')::bigint - 1) AS blkno;
$$
LANGUAGE SQL PARALLEL SAFE
;
-- ALTER INDEX .. RESET
ALTER INDEX dummy_test_idx RESET (option_int)
;
CREATE FUNCTION heap_page_items(IN page bytea,
    OUT lp smallint,
    OUT lp_off smallint,
    OUT lp_flags smallint,
    OUT lp_len smallint,
    OUT t_xmin xid,
    OUT t_xmax xid,
    OUT t_field3 int4,
    OUT t_ctid tid,
    OUT t_infomask2 integer,
    OUT t_infomask integer,
    OUT t_hoff smallint,
    OUT t_bits text,
    OUT t_oid oid,
    OUT t_data bytea)
RETURNS SETOF record
AS '$libdir/pageinspect', 'heap_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
VACUUM FULL pg_type
;
DROP SCHEMA ext_cine_schema
;
ALTER TABLE pg_temp_13.pg_temp_26413_2 ADD COLUMN newdata pg_temp_13.pg_temp_26413
;
-- triggers will fire, too
create function noticetrigger() returns trigger as $$
begin
  raise notice 'noticetrigger % %', new.f1, new.data;
  return null;
end $$ language plpgsql
;
DO LANGUAGE plpgsql $$
BEGIN
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
END;
$$
;
ALTER SUBSCRIPTION tap_sub_C
	SET (streaming = on)
;
CREATE FUNCTION sort_desc(_int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_trigger
;
ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk
  ON DELETE SET DEFAULT ON UPDATE SET DEFAULT
;
ALTER OPERATOR === (boolean, boolean) SET (NEGATOR = ====)
;
CREATE FUNCTION "public"."rtest_viewfunc1"(integer) RETURNS integer
    LANGUAGE "sql"
    AS $_$select count(*)::int4 from rtest_view2 where a = $1$_$
;
CREATE TABLE "public"."gtest_normal_child2" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 3)) STORED
)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE corrupt2
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rls_view AS SELECT * FROM rls_tbl
;
----------
[ADD_INDEX] -- Test that an index-only scan is not chosen, when the query involves the
-- circle column (the circle opclass does not support index-only scans).
create index gist_tbl_multi_index on gist_tbl using gist (p, c)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28261_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] drop function sum4(int8,int8,int8,int8)
;
----------
[ADD_INDEX] --- test hash index

create index tstidx on ltreetest using hash (t)
;
----------
[DROP_TABLE] drop table copyfreeze
;
----------
[CREATE_TABLE] CREATE TABLE clstr_tst (a SERIAL PRIMARY KEY,
	b INT,
	c TEXT,
	d TEXT,
	CONSTRAINT clstr_tst_con FOREIGN KEY (b) REFERENCES clstr_tst_s)
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE sequence_test12 AS int
;
----------
[DROP_CATALOG] DROP DATABASE sepgsql_test_regression
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_uuid(uuid, uuid, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_TABLE] ALTER TABLE x1 ENABLE ROW LEVEL SECURITY
;
----------
[DROP_VIEW] DROP VIEW view_stats_1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION read_rel_block_ll(
    rel regclass,
    blockno int,
    nblocks int DEFAULT 1,
    wait_complete bool DEFAULT true,
    batchmode_enter bool DEFAULT false,
    smgrreleaseall bool DEFAULT false,
    batchmode_exit bool DEFAULT false,
    zero_on_error bool DEFAULT false)
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_trigger
;
----------
[CREATE_PROG_OBJ] -- error, ambiguous


-- CREATE OR REPLACE tests

CREATE FUNCTION functest1(a int) RETURNS int LANGUAGE SQL AS 'SELECT $1'
;
----------
[DROP_VIEW] DROP VIEW IF EXISTS no_such_schema.foo
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- error, already set
ALTER TABLE itest4 ALTER COLUMN b ADD GENERATED ALWAYS AS IDENTITY
;
----------
[CREATE_PROG_OBJ] -- output arguments

CREATE PROCEDURE ptest4a(INOUT a int, INOUT b int)
LANGUAGE SQL
AS $$
SELECT 1, 2;
$$
;
----------
[DROP_TABLE] DROP TABLE country_table
;
----------
[CREATE_TABLE] create table idxpart2 (c int, like idxpart)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH DICTIONARY "public"."thesaurus" (
    TEMPLATE = "pg_catalog"."thesaurus",
    dictfile = 'thesaurus_sample', dictionary = 'english_stem' )
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER error_del_when BEFORE INSERT OR UPDATE ON main_table
FOR EACH ROW WHEN (NEW.tableoid <> 0)
EXECUTE PROCEDURE trigger_func('error_when_sys_column')
;
----------
[CREATE_PROG_OBJ] -- test python -> hstore[]
CREATE FUNCTION test2arr() RETURNS hstore[]
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
val = [{'a': 1, 'b': 'boo', 'c': None}, {'d': 2}]
return val
$$
;
----------
[CREATE_TABLE] CREATE TABLE tx2 (b integer, c integer)
;
----------
[CREATE_TABLE] -- error

CREATE TABLE test11pk (x text COLLATE case_insensitive PRIMARY KEY)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table myview alter column test drop not null
;
----------
[ALTER_INDEX] -- reattach the leaf partition index; parents should now be valid
alter index idxpart1_expr_idx attach partition idxpart11_expr_idx
;
----------
[DROP_PROG_OBJ] drop function rngfuncbar()
;
----------
[CREATE_VIEW(SELECT)] -- CREATE MATERIALIZED VIEW ... IF NOT EXISTS
CREATE MATERIALIZED VIEW matview_ine_tab AS SELECT 1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bpchar_sortsupport(internal)
RETURNS void
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE PARALLEL SAFE STRICT
;
----------
[CREATE_POLICY] -- CREATE/DROP COLLATION

CREATE COLLATION mycoll1 FROM "C"
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."plt1" ATTACH PARTITION "public"."plt1_p2" FOR VALUES IN ('0001', '0005', '0002', '0009')
;
----------
[CREATE_TABLE] CREATE TABLE a2 ()
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ftprt2_p1 (b int, c varchar, a int)
	SERVER loopback OPTIONS (table_name 'fprt2_p1', use_remote_estimate 'true')
;
----------
[CREATE_PROG_OBJ] -- pg_freespace shows the recorded space avail at each block in a relation
CREATE FUNCTION
  pg_freespace(rel regclass, blkno OUT bigint, avail OUT int2)
RETURNS SETOF RECORD
AS $$
  SELECT blkno, pg_freespace($1, blkno) AS avail
  FROM generate_series(0, pg_relation_size($1) / current_setting('block_size')::bigint - 1) AS blkno;
$$
LANGUAGE SQL PARALLEL SAFE
;
----------
[ALTER_INDEX] -- ALTER INDEX .. RESET
ALTER INDEX dummy_test_idx RESET (option_int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION heap_page_items(IN page bytea,
    OUT lp smallint,
    OUT lp_off smallint,
    OUT lp_flags smallint,
    OUT lp_len smallint,
    OUT t_xmin xid,
    OUT t_xmax xid,
    OUT t_field3 int4,
    OUT t_ctid tid,
    OUT t_infomask2 integer,
    OUT t_infomask integer,
    OUT t_hoff smallint,
    OUT t_bits text,
    OUT t_oid oid,
    OUT t_data bytea)
RETURNS SETOF record
AS '$libdir/pageinspect', 'heap_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM FULL pg_type
;
----------
[DROP_SCHEMA] DROP SCHEMA ext_cine_schema
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_13.pg_temp_26413_2 ADD COLUMN newdata pg_temp_13.pg_temp_26413
;
----------
[CREATE_PROG_OBJ] -- triggers will fire, too
create function noticetrigger() returns trigger as $$
begin
  raise notice 'noticetrigger % %', new.f1, new.data;
  return null;
end $$ language plpgsql
;
----------
[BLOCK] DO LANGUAGE plpgsql $$
BEGIN
    SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
END;
$$
;
----------
[ALTER_PUB_SUB] ALTER SUBSCRIPTION tap_sub_C
	SET (streaming = on)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION sort_desc(_int4)
RETURNS _int4
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_trigger
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk_partitioned_fk ADD FOREIGN KEY (a, b)
  REFERENCES fk_notpartitioned_pk
  ON DELETE SET DEFAULT ON UPDATE SET DEFAULT
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, boolean) SET (NEGATOR = ====)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."rtest_viewfunc1"(integer) RETURNS integer
    LANGUAGE "sql"
    AS $_$select count(*)::int4 from rtest_view2 where a = $1$_$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest_normal_child2" (
    "a" integer,
    "b" integer GENERATED ALWAYS AS (("a" * 3)) STORED
)
;
