CREATE FUNCTION "public"."wrap_do_analyze"("c" integer) RETURNS integer
    LANGUAGE "sql" IMMUTABLE
    AS $_$SELECT $1 FROM do_analyze()$_$
;
CREATE SUBSCRIPTION test_tab1_sub CONNECTION 'port=10712 host=/tmp/WeWhs1a6fA dbname=postgres' PUBLICATION test_tab1_pub
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_attribute
;
CREATE FUNCTION build_ordered_named_pair(i int, j int) RETURNS ordered_named_pair AS $$
begin
return row(i, j);
end
$$ LANGUAGE plpgsql
;
-- This formulation cannot handle relocation of the required extension.
CREATE FUNCTION dep_req3() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT @extschema:test_ext_req_schema1@.dep_req1() || ' req3' $$
;
COMMENT ON TEXT SEARCH DICTIONARY split_ts_probe.simple_dict IS 'simple dictionary probe'
;
SECURITY LABEL FOR 'unknown_seclabel' ON TABLE dummy_seclabel_tbl1 IS 'classified'
;
create table test (t text)
;
CREATE TABLE stxdinp1 PARTITION OF stxdinp FOR VALUES FROM (1) TO (100)
;
-- create a level-2 partition
CREATE TABLE part_c_1_10 PARTITION OF part_c FOR VALUES FROM (1) TO (10)
;
CREATE FUNCTION soundex(text) RETURNS text
AS '$libdir/fuzzystrmatch', 'soundex'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE "public"."check_con_tbl" (
    "f1" integer
)
;
ALTER FOREIGN TABLE ft1 ADD COLUMN c4 integer
;
CREATE TABLE test_tbl2_subclass () INHERITS (test_tbl2)
;
DROP TABLE temp2
;
ALTER FOREIGN TABLE ft1 ADD COLUMN c10 integer OPTIONS (p1 'v1')
;
CREATE AGGREGATE split_agg_probe.hypo_sum(integer ORDER BY integer) (
    SFUNC = int4pl,
    STYPE = integer,
    INITCOND = '0',
    HYPOTHETICAL
)
;
CREATE FUNCTION regtest_func_2(int) RETURNS bool LANGUAGE plpgsql
           AS 'BEGIN RETURN $1 * $1 < 100; END'
;
create temp table selfref (
    a int primary key,
    b int,
    foreign key (b) references selfref (a)
        on update cascade on delete cascade
)
;
ALTER TABLE public.atref ADD CONSTRAINT atref_c1_fkey FOREIGN KEY (c1) REFERENCES attbl(p1)
;
--    N    P        N    P
-- should CREATE
CREATE AGGREGATE myaggn10a(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
-- if we drop the object, we can drop the user too
DROP TABLE deptest
;
CREATE SCHEMA sts_sch1 CREATE TABLE sts_sch1.tbl (a INT, b INT, c INT GENERATED ALWAYS AS (b * 2) STORED)
;
create table atacc1 ( test int, test2 int, test3 int)
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) information_schema.sql_features
;
CREATE TABLE "public"."test2" (
    "generate_series" integer
)
;
CREATE VIEW "public"."sums_1_100" AS
 WITH RECURSIVE "t"("n") AS (
         VALUES (1)
        UNION ALL
         SELECT ("t_1"."n" + 1)
           FROM "t" "t_1"
          WHERE ("t_1"."n" < 100)
        )
 SELECT "sum"("t"."n") AS "sum"
   FROM "t"
;
ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_init_privs
;
ALTER TEXT SEARCH CONFIGURATION alt_ts_conf1 RENAME TO alt_ts_conf2
;
ALTER TABLE pg_toast.pg_toast_30929 RENAME TO reind_con_toast
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_user_mapping
;
/*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
/* contrib/earthdistance/earthdistance--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- earth() returns the radius of the earth in meters. This is the only
-- place you need to change things for the cube base distance functions
-- in order to use different units (or a better value for the Earth's radius).

CREATE FUNCTION earth() RETURNS float8
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS 'SELECT ''6378168''::float8'
;
create table parted_stmt_trig2 partition of parted_stmt_trig for values in (2)
;
-- only the last of these should trigger a rewrite
ALTER TABLE has_volatile ADD col1 int
;
create database abc template = zyc
;
ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk5" FOR VALUES FROM (4000) TO (5000)
;
CREATE TABLE "need""q(uot" (")x" text)
;
DROP TABLE trigger_test
;
-- error - generated column "b" must be published explicitly as it is
-- part of the REPLICA IDENTITY.
ALTER TABLE testpub_gencol REPLICA IDENTITY FULL
;
-- should fail, food is not a valid filter variable
create event trigger regress_event_trigger2 on ddl_command_start
   when food in ('sandwich')
   execute procedure test_event_trigger()
;
DROP TABLE temporal_mltrng2
;
ALTER TABLE pg_description ALTER COLUMN description SET STATISTICS -1
;
/* src/test/modules/test_resowner/test_resowner--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_resowner_priorities(nkinds pg_catalog.int4, nresources pg_catalog.int4)
	RETURNS pg_catalog.void
	AS '$libdir/test_resowner' LANGUAGE C
;
ALTER DATABASE "regression" SET lc_numeric TO 'C'
;
CREATE TABLE src (a int, b int, c int, d int)
;
ANALYZE pg_temp_46.pg_temp_28044
;
CREATE TABLE rls_tbl_force (c1 int)
;
CREATE OR REPLACE FUNCTION leakproof(anyelement)
RETURNS boolean AS
$$
BEGIN
  RETURN true;
END;
$$
LANGUAGE plpgsql STRICT IMMUTABLE LEAKPROOF
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."wrap_do_analyze"("c" integer) RETURNS integer
    LANGUAGE "sql" IMMUTABLE
    AS $_$SELECT $1 FROM do_analyze()$_$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION test_tab1_sub CONNECTION 'port=10712 host=/tmp/WeWhs1a6fA dbname=postgres' PUBLICATION test_tab1_pub
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_attribute
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION build_ordered_named_pair(i int, j int) RETURNS ordered_named_pair AS $$
begin
return row(i, j);
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] -- This formulation cannot handle relocation of the required extension.
CREATE FUNCTION dep_req3() RETURNS text
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS $$ SELECT @extschema:test_ext_req_schema1@.dep_req1() || ' req3' $$
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY split_ts_probe.simple_dict IS 'simple dictionary probe'
;
----------
[UNKNOWN] SECURITY LABEL FOR 'unknown_seclabel' ON TABLE dummy_seclabel_tbl1 IS 'classified'
;
----------
[CREATE_TABLE] create table test (t text)
;
----------
[CREATE_TABLE] CREATE TABLE stxdinp1 PARTITION OF stxdinp FOR VALUES FROM (1) TO (100)
;
----------
[CREATE_TABLE] -- create a level-2 partition
CREATE TABLE part_c_1_10 PARTITION OF part_c FOR VALUES FROM (1) TO (10)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION soundex(text) RETURNS text
AS '$libdir/fuzzystrmatch', 'soundex'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."check_con_tbl" (
    "f1" integer
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD COLUMN c4 integer
;
----------
[CREATE_TABLE] CREATE TABLE test_tbl2_subclass () INHERITS (test_tbl2)
;
----------
[DROP_TABLE] DROP TABLE temp2
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD COLUMN c10 integer OPTIONS (p1 'v1')
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE split_agg_probe.hypo_sum(integer ORDER BY integer) (
    SFUNC = int4pl,
    STYPE = integer,
    INITCOND = '0',
    HYPOTHETICAL
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION regtest_func_2(int) RETURNS bool LANGUAGE plpgsql
           AS 'BEGIN RETURN $1 * $1 < 100; END'
;
----------
[CREATE_TABLE] create temp table selfref (
    a int primary key,
    b int,
    foreign key (b) references selfref (a)
        on update cascade on delete cascade
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.atref ADD CONSTRAINT atref_c1_fkey FOREIGN KEY (c1) REFERENCES attbl(p1)
;
----------
[CREATE_PROG_OBJ] --    N    P        N    P
-- should CREATE
CREATE AGGREGATE myaggn10a(BASETYPE = int, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[DROP_TABLE] -- if we drop the object, we can drop the user too
DROP TABLE deptest
;
----------
[CREATE_SCHEMA] CREATE SCHEMA sts_sch1 CREATE TABLE sts_sch1.tbl (a INT, b INT, c INT GENERATED ALWAYS AS (b * 2) STORED)
;
----------
[CREATE_TABLE] create table atacc1 ( test int, test2 int, test3 int)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) information_schema.sql_features
;
----------
[CREATE_TABLE] CREATE TABLE "public"."test2" (
    "generate_series" integer
)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."sums_1_100" AS
 WITH RECURSIVE "t"("n") AS (
         VALUES (1)
        UNION ALL
         SELECT ("t_1"."n" + 1)
           FROM "t" "t_1"
          WHERE ("t_1"."n" < 100)
        )
 SELECT "sum"("t"."n") AS "sum"
   FROM "t"
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY alt_opf17 USING btree ADD
  OPERATOR 1 < (int4, int2) ,
  OPERATOR 2 <= (int4, int2) ,
  OPERATOR 3 = (int4, int2) ,
  OPERATOR 4 >= (int4, int2) ,
  OPERATOR 5 > (int4, int2) ,
  FUNCTION 1 btint42cmp(int4, int2)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_init_privs
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION alt_ts_conf1 RENAME TO alt_ts_conf2
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30929 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_user_mapping
;
----------
[CREATE_POLICY] /*
 * text search configuration for french language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * french and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY french_stem
	(TEMPLATE = snowball, Language = french , StopWords=french)
;
----------
[CREATE_PROG_OBJ] /* contrib/earthdistance/earthdistance--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- earth() returns the radius of the earth in meters. This is the only
-- place you need to change things for the cube base distance functions
-- in order to use different units (or a better value for the Earth's radius).

CREATE FUNCTION earth() RETURNS float8
LANGUAGE SQL IMMUTABLE PARALLEL SAFE
AS 'SELECT ''6378168''::float8'
;
----------
[CREATE_TABLE] create table parted_stmt_trig2 partition of parted_stmt_trig for values in (2)
;
----------
[ALTER_TABLE|ADD_COLUMN] -- only the last of these should trigger a rewrite
ALTER TABLE has_volatile ADD col1 int
;
----------
[CREATE_CATALOG] create database abc template = zyc
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "fkpart3"."pk" ATTACH PARTITION "fkpart3"."pk5" FOR VALUES FROM (4000) TO (5000)
;
----------
[CREATE_TABLE] CREATE TABLE "need""q(uot" (")x" text)
;
----------
[DROP_TABLE] DROP TABLE trigger_test
;
----------
[ALTER_TABLE] -- error - generated column "b" must be published explicitly as it is
-- part of the REPLICA IDENTITY.
ALTER TABLE testpub_gencol REPLICA IDENTITY FULL
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should fail, food is not a valid filter variable
create event trigger regress_event_trigger2 on ddl_command_start
   when food in ('sandwich')
   execute procedure test_event_trigger()
;
----------
[DROP_TABLE] DROP TABLE temporal_mltrng2
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pg_description ALTER COLUMN description SET STATISTICS -1
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_resowner/test_resowner--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_resowner_priorities(nkinds pg_catalog.int4, nresources pg_catalog.int4)
	RETURNS pg_catalog.void
	AS '$libdir/test_resowner' LANGUAGE C
;
----------
[ALTER_CATALOG] ALTER DATABASE "regression" SET lc_numeric TO 'C'
;
----------
[CREATE_TABLE] CREATE TABLE src (a int, b int, c int, d int)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_46.pg_temp_28044
;
----------
[CREATE_TABLE] CREATE TABLE rls_tbl_force (c1 int)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION leakproof(anyelement)
RETURNS boolean AS
$$
BEGIN
  RETURN true;
END;
$$
LANGUAGE plpgsql STRICT IMMUTABLE LEAKPROOF
;
