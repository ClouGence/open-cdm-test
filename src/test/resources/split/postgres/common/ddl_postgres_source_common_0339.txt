CREATE TABLE attmp_array2 (id int)
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn13,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
create temp table x (x1 int, x2 int)
;
ANALYZE pht1
;
ALTER TABLE replication_metadata RESET (user_catalog_table)
;
DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19319 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
CREATE TABLE test_drop_constr_child () INHERITS (test_drop_constr_parent)
;
CREATE TABLE inhg (x text, LIKE inhx INCLUDING INDEXES, y text)
;
CREATE INDEX idx_name ON test_name USING gin (i)
;
DROP FUNCTION pg_stat_statements_reset(Oid, Oid, bigint)
;
DROP TABLE concur_clustered
;
alter table atacc1 add foreign key ("........pg.dropped.1........") references atacc2(id)
;
CREATE FUNCTION ltxtq_send(ltxtquery)
RETURNS bytea
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TYPE bogus_type (
	"Internallength" = 4,
	"Input" = int42_in,
	"Output" = int42_out,
	"Alignment" = int4,
	"Default" = 42,
	"Passedbyvalue"
)
;
create table atacc1 (a int4 not null, b int4, c int4 not null, d int4)
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_database
;
CREATE TABLE tx1 (a integer, b integer)
;
ALTER TABLE pg_toast.pg_toast_33201 RENAME TO reind_con_toast
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_amproc
;
ALTER FOREIGN TABLE ft1 ALTER COLUMN c5 DROP DEFAULT
;
drop table test_pg_index_toast_table
;
ALTER DATABASE split_db_probe RESET work_mem
;
CREATE PROCEDURE "public"."ptest10"(IN "a" integer, IN "b" integer, IN "c" integer)
    LANGUAGE "sql"
    AS $$ SELECT a + b - c $$
;
CREATE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'DECLARE c CURSOR WITH HOLD FOR SELECT unwanted_grant(); SELECT true'
;
CREATE TABLE "public"."brintest" (
    "byteacol" "bytea",
    "charcol" "char",
    "namecol" "name",
    "int8col" bigint,
    "int2col" smallint,
    "int4col" integer,
    "textcol" "text",
    "oidcol" "oid",
    "tidcol" "tid",
    "float4col" real,
    "float8col" double precision,
    "macaddrcol" "macaddr",
    "inetcol" "inet",
    "cidrcol" "cidr",
    "bpcharcol" character(1),
    "datecol" "date",
    "timecol" time without time zone,
    "timestampcol" timestamp without time zone,
    "timestamptzcol" timestamp with time zone,
    "intervalcol" interval,
    "timetzcol" time with time zone,
    "bitcol" bit(10),
    "varbitcol" bit varying(16),
    "numericcol" numeric,
    "uuidcol" "uuid",
    "int4rangecol" "int4range",
    "lsncol" "pg_lsn",
    "boxcol" "box"
)
WITH ("fillfactor"='10', "autovacuum_enabled"='off')
;
VACUUM (FREEZE) pg_catalog.pg_seclabel
;
-- invalid options parsing function
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 7 btint42cmp(int4, int2)
;
CREATE OPERATOR FAMILY alt_opf10 USING btree
;
create schema abc
;
CREATE TRIGGER "tg_hslot_biu" BEFORE INSERT OR UPDATE ON "public"."hslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_hslot_biu"()
;
CREATE FUNCTION test_build_uint2_array(x int2) RETURNS uint2[] AS $$
begin
return array[x, x];
end
$$ LANGUAGE plpgsql
;
CREATE TABLE "public"."gtest23p" (
    "x" integer,
    "y" integer GENERATED ALWAYS AS (("x" * 2)) STORED NOT NULL
)
;
VACUUM (PROCESS_TOAST TRUE) vac_option_tab
;
ALTER OPERATOR @> (hstore, hstore)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
CREATE FUNCTION isnge(ean13, ismn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE TABLE "public"."c_star" (
    "class" character(1),
    "aa" integer,
    "cc" "name",
    "a" "text"
)
;
CREATE TABLE toasttest(f1 text)
;
CREATE TABLE INT8_TBL(q1 int8, q2 int8)
;
ALTER TABLE split_alter_table.base_table SET UNLOGGED
;
create or replace function compos() returns compostype as $$
begin
  return (1, 'hello')::compostype;
end;
$$ language plpgsql
;
CREATE TABLE BOOLTBL2 (f1 bool)
;
CREATE FUNCTION tf1() RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN NULL;
END $$
;
CREATE TRIGGER instead_of_delete_trig INSTEAD OF DELETE ON main_view
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_del')
;
ALTER TABLE pg_toast.pg_toast_33058 RENAME TO reind_con_toast
;
DROP VIEW fdv1
;
CREATE TABLE will_grow (a int, b text)
;
alter table parent drop c
;
CREATE FUNCTION test2() RETURNS ltree
LANGUAGE plpython3u
TRANSFORM FOR TYPE ltree
AS $$
return ['foo', 'bar', 'baz']
$$
;
CREATE TABLE "testxmlschema"."test3" (
    "c1" boolean,
    "c2" "public"."testboolxmldomain",
    "c3" "date",
    "c4" "public"."testdatexmldomain"
)
;
CREATE TABLE trgfire2 PARTITION OF trgfire FOR VALUES FROM (10) TO (20)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE attmp_array2 (id int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = issn13,
	RIGHTARG = ean13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE] create temp table x (x1 int, x2 int)
;
----------
[ADMIN_TABLE] ANALYZE pht1
;
----------
[ALTER_TABLE] ALTER TABLE replication_metadata RESET (user_catalog_table)
;
----------
[BLOCK] DO $c$
DECLARE
  c INT;
BEGIN
  LOOP
    SELECT COUNT(*) INTO c FROM pg_locks WHERE pid = 19319 AND NOT granted;
    IF c > 0 THEN
      EXIT;
    END IF;
  END LOOP;
END; $c$
;
----------
[CREATE_TABLE] CREATE TABLE test_drop_constr_child () INHERITS (test_drop_constr_parent)
;
----------
[CREATE_TABLE] CREATE TABLE inhg (x text, LIKE inhx INCLUDING INDEXES, y text)
;
----------
[ADD_INDEX] CREATE INDEX idx_name ON test_name USING gin (i)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION pg_stat_statements_reset(Oid, Oid, bigint)
;
----------
[DROP_TABLE] DROP TABLE concur_clustered
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add foreign key ("........pg.dropped.1........") references atacc2(id)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltxtq_send(ltxtquery)
RETURNS bytea
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TYPE] CREATE TYPE bogus_type (
	"Internallength" = 4,
	"Input" = int42_in,
	"Output" = int42_out,
	"Alignment" = int4,
	"Default" = 42,
	"Passedbyvalue"
)
;
----------
[CREATE_TABLE] create table atacc1 (a int4 not null, b int4, c int4 not null, d int4)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_database
;
----------
[CREATE_TABLE] CREATE TABLE tx1 (a integer, b integer)
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33201 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_amproc
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE ft1 ALTER COLUMN c5 DROP DEFAULT
;
----------
[DROP_TABLE] drop table test_pg_index_toast_table
;
----------
[ALTER_CATALOG] ALTER DATABASE split_db_probe RESET work_mem
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE "public"."ptest10"(IN "a" integer, IN "b" integer, IN "c" integer)
    LANGUAGE "sql"
    AS $$ SELECT a + b - c $$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION mv_action() RETURNS bool LANGUAGE sql AS
	'DECLARE c CURSOR WITH HOLD FOR SELECT unwanted_grant(); SELECT true'
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brintest" (
    "byteacol" "bytea",
    "charcol" "char",
    "namecol" "name",
    "int8col" bigint,
    "int2col" smallint,
    "int4col" integer,
    "textcol" "text",
    "oidcol" "oid",
    "tidcol" "tid",
    "float4col" real,
    "float8col" double precision,
    "macaddrcol" "macaddr",
    "inetcol" "inet",
    "cidrcol" "cidr",
    "bpcharcol" character(1),
    "datecol" "date",
    "timecol" time without time zone,
    "timestampcol" timestamp without time zone,
    "timestamptzcol" timestamp with time zone,
    "intervalcol" interval,
    "timetzcol" time with time zone,
    "bitcol" bit(10),
    "varbitcol" bit varying(16),
    "numericcol" numeric,
    "uuidcol" "uuid",
    "int4rangecol" "int4range",
    "lsncol" "pg_lsn",
    "boxcol" "box"
)
WITH ("fillfactor"='10', "autovacuum_enabled"='off')
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_seclabel
;
----------
[ALTER_PROG_OBJ] -- invalid options parsing function
ALTER OPERATOR FAMILY alt_opf4 USING btree ADD FUNCTION 7 btint42cmp(int4, int2)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR FAMILY alt_opf10 USING btree
;
----------
[CREATE_SCHEMA] create schema abc
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER "tg_hslot_biu" BEFORE INSERT OR UPDATE ON "public"."hslot" FOR EACH ROW EXECUTE FUNCTION "public"."tg_hslot_biu"()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_build_uint2_array(x int2) RETURNS uint2[] AS $$
begin
return array[x, x];
end
$$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest23p" (
    "x" integer,
    "y" integer GENERATED ALWAYS AS (("x" * 2)) STORED NOT NULL
)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST TRUE) vac_option_tab
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR @> (hstore, hstore)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(ean13, ismn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."c_star" (
    "class" character(1),
    "aa" integer,
    "cc" "name",
    "a" "text"
)
;
----------
[CREATE_TABLE] CREATE TABLE toasttest(f1 text)
;
----------
[CREATE_TABLE] CREATE TABLE INT8_TBL(q1 int8, q2 int8)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table SET UNLOGGED
;
----------
[CREATE_PROG_OBJ] create or replace function compos() returns compostype as $$
begin
  return (1, 'hello')::compostype;
end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE BOOLTBL2 (f1 bool)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tf1() RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN NULL;
END $$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER instead_of_delete_trig INSTEAD OF DELETE ON main_view
FOR EACH ROW EXECUTE PROCEDURE view_trigger('instead_of_del')
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33058 RENAME TO reind_con_toast
;
----------
[DROP_VIEW] DROP VIEW fdv1
;
----------
[CREATE_TABLE] CREATE TABLE will_grow (a int, b text)
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table parent drop c
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test2() RETURNS ltree
LANGUAGE plpython3u
TRANSFORM FOR TYPE ltree
AS $$
return ['foo', 'bar', 'baz']
$$
;
----------
[CREATE_TABLE] CREATE TABLE "testxmlschema"."test3" (
    "c1" boolean,
    "c2" "public"."testboolxmldomain",
    "c3" "date",
    "c4" "public"."testdatexmldomain"
)
;
----------
[CREATE_TABLE] CREATE TABLE trgfire2 PARTITION OF trgfire FOR VALUES FROM (10) TO (20)
;
