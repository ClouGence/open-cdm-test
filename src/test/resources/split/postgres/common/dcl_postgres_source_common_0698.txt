EXECUTE dumpFunc('21815')
;
LOCK TABLE pg_catalog.pg_transform IN ACCESS SHARE MODE
;
ALTER TABLE public.fk_partitioned_fk_3 OWNER TO zyc
;
EXECUTE dumpTableAttach('39750')
;
EXECUTE dumpAgg('35552')
;
EXECUTE dumpOpr('1519')
;
EXECUTE dumpFunc('26547')
;
-- test that SET EXPRESSION is rejected, because it could affect a row filter
SET client_min_messages = 'ERROR'
;
DROP ROLE regress_nosuch_dbowner
;
-- We document "The DEFAULT and RESET forms reset the session and current user
-- identifiers to be the originally authenticated user name."  If we let
-- session_authorization in setconfig have an effect, we'll need to decide
-- whether to make RESET differ from DEFAULT.
RESET SESSION AUTHORIZATION
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('21815')
;
----------
[SESSION_LOCK] LOCK TABLE pg_catalog.pg_transform IN ACCESS SHARE MODE
;
----------
[TRANSFER_PRIVILEGE] ALTER TABLE public.fk_partitioned_fk_3 OWNER TO zyc
;
----------
[UNSAFE] EXECUTE dumpTableAttach('39750')
;
----------
[UNSAFE] EXECUTE dumpAgg('35552')
;
----------
[UNSAFE] EXECUTE dumpOpr('1519')
;
----------
[UNSAFE] EXECUTE dumpFunc('26547')
;
----------
[SESSION_SETTING_WRITE] -- test that SET EXPRESSION is rejected, because it could affect a row filter
SET client_min_messages = 'ERROR'
;
----------
[DROP_ROLE] DROP ROLE regress_nosuch_dbowner
;
----------
[SWITCH_USER] -- We document "The DEFAULT and RESET forms reset the session and current user
-- identifiers to be the originally authenticated user name."  If we let
-- session_authorization in setconfig have an effect, we'll need to decide
-- whether to make RESET differ from DEFAULT.
RESET SESSION AUTHORIZATION
;
