-- We can exercise some MemoryContext type stats functions.  Most of the
-- column values are too platform-dependant to display.

-- Ensure stats from the bump allocator look sane.  Bump isn't a commonly
-- used context, but it is used in tuplesort.c, so open a cursor to keep
-- the tuplesort alive long enough for us to query the context stats.
begin
;
-- declarative partitioning tests
SET ROLE regress_file_fdw_superuser
;
-- ok, privileges have been granted
RESET max_stack_depth
;
CREATE STATISTICS func_deps_stat (dependencies) ON (a * 2), upper(b), (c + 1) FROM functional_dependencies
;
EXECUTE dumpTableAttach('39588')
;
LOCK TABLE "public"."prt2_n_p1" IN ACCESS SHARE MODE
;
ALTER SUBSCRIPTION regress_testsub DISABLE
;
EXECUTE dumpTableAttach('33128')
;
-- PL/pgSQL function with MERGE ... RETURNING ... INTO
BEGIN
;
-- load all rows via COPY FREEZE and ensure that all pages are set all-visible
-- and all-frozen.
begin
;
------------------------------------------------------------------------------------------
[TRANSACTION] -- We can exercise some MemoryContext type stats functions.  Most of the
-- column values are too platform-dependant to display.

-- Ensure stats from the bump allocator look sane.  Bump isn't a commonly
-- used context, but it is used in tuplesort.c, so open a cursor to keep
-- the tuplesort alive long enough for us to query the context stats.
begin
;
----------
[SWITCH_ROLE] -- declarative partitioning tests
SET ROLE regress_file_fdw_superuser
;
----------
[SESSION_SETTING_WRITE] -- ok, privileges have been granted
RESET max_stack_depth
;
----------
[ADMIN_PERFORMANCE] CREATE STATISTICS func_deps_stat (dependencies) ON (a * 2), upper(b), (c + 1) FROM functional_dependencies
;
----------
[UNSAFE] EXECUTE dumpTableAttach('39588')
;
----------
[SESSION_LOCK] LOCK TABLE "public"."prt2_n_p1" IN ACCESS SHARE MODE
;
----------
[ADMIN_PUB_SUB] ALTER SUBSCRIPTION regress_testsub DISABLE
;
----------
[UNSAFE] EXECUTE dumpTableAttach('33128')
;
----------
[TRANSACTION] -- PL/pgSQL function with MERGE ... RETURNING ... INTO
BEGIN
;
----------
[TRANSACTION] -- load all rows via COPY FREEZE and ensure that all pages are set all-visible
-- and all-frozen.
begin
;
