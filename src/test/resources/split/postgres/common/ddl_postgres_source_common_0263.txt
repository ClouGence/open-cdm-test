VACUUM FULL pg_amop
;
-- fail, can't infer type:
create function f1(x anyelement) returns anyrange as $$
begin
  return array[x + 1, x + 2];
end$$ language plpgsql
;
-- ADD COLUMN

CREATE TABLE itest13 (a int)
;
-- check multikey cases, especially out-of-order column lists

-- these should work

ALTER TABLE fktable ADD CONSTRAINT fk_123_123
FOREIGN KEY (x1,x2,x3) REFERENCES pktable(id1,id2,id3)
;
create index on mcrparted4 (a, abs(b), c)
;
create rule base_tab_def_view_ins_rule as on insert to base_tab_def_view
  do instead insert into base_tab_def values (new.a, new.b, new.c, new.d, new.e)
;
CREATE TABLE "public"."temporal_rng2" (
    "id1" "int4range" NOT NULL,
    "id2" "int4range" NOT NULL,
    "valid_at" "daterange" NOT NULL
)
;
/* src/test/modules/test_extensions/test_event_trigger--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Base table with event trigger, used in a regression test involving
-- extension upgrades.
CREATE TABLE t (id text)
;
CREATE EXTENSION IF NOT EXISTS "postgres_fdw"
;
-- fail
CREATE INDEX collate_test1_idx6 ON collate_test1 ((a COLLATE "POSIX"))
;
ALTER OPERATOR %> (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
-- Doesn't make sense to add NO INHERIT constraints on partitioned tables
ALTER TABLE list_parted2 add constraint check_b2 check (b <> 'zz') NO INHERIT
;
/*
 * text search configuration for danish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * danish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY danish_stem
	(TEMPLATE = snowball, Language = danish , StopWords=danish)
;
DROP TABLE CASE_TBL
;
drop table parted_conflict_test
;
DROP TABLE testpub_tbl4
;
/* contrib/hstore/hstore--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR #<=# (hstore, hstore) SET (
       RESTRICT = scalarlesel,
       JOIN = scalarlejoinsel
)
;
CREATE TABLE prt1_n_p1 PARTITION OF prt1_n FOR VALUES FROM ('0000') TO ('0250')
;
ALTER TABLE split_alter_table.base_table ALTER CONSTRAINT base_table_ref_fk NOT DEFERRABLE INITIALLY IMMEDIATE
;
CREATE TABLE OID_TBL(f1 oid)
;
CREATE TYPE basket AS (apples integer, pears integer, mangos integer)
;
CREATE TABLE daucus PARTITION OF vegetables FOR VALUES IN ('daucus')
;
CREATE TABLE tab_notrep AS SELECT generate_series(1,10) AS a
;
--
-- Try some concurrent index drops
--
DROP INDEX CONCURRENTLY "concur_index2"
;
CREATE TRIGGER rw_view1_ins_trig AFTER INSERT ON base_tbl
  FOR EACH ROW EXECUTE PROCEDURE rw_view1_trig_fn()
;
CREATE SEQUENCE sequence_test11 AS smallint
;
CREATE UNIQUE INDEX tbl_idx ON tbl using btree(c1, c2, c3, c4)
;
CREATE TABLE copy_rel_to (a integer, b text)
;
CREATE TABLE fk_parted_pk_2 PARTITION OF fk_parted_pk FOR VALUES IN (2)
;
CREATE TABLE arrtest (
	a 			int2[],
	b 			int4[][][],
	c 			name[],
	d			text[][],
	e 			float8[],
	f			char(5)[],
	g			varchar(5)[]
)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_publication_namespace
;
CREATE VIEW constraint_table_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name

    FROM pg_constraint c, pg_namespace nc,
         pg_class r, pg_namespace nr

    WHERE c.connamespace = nc.oid AND r.relnamespace = nr.oid
          AND ( (c.contype = 'f' AND c.confrelid = r.oid)
             OR (c.contype IN ('p', 'u') AND c.conrelid = r.oid) )
          AND r.relkind IN ('r', 'p')
          AND pg_has_role(r.relowner, 'USAGE')
;
ALTER TABLE hash_parted ATTACH PARTITION fail_part FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
CREATE TABLE tab_part2(a int) PARTITION BY RANGE(a)
;
drop domain ddef2 restrict
;
DO $$
DECLARE
    wal_segsize int := setting::int FROM pg_settings WHERE name = 'wal_segment_size';
    remain int;
    iters  int := 0;
BEGIN
    LOOP
        INSERT into filler
        select g, repeat(md5(g::text), (random() * 60 + 1)::int)
        from generate_series(1, 10) g;

        remain := wal_segsize - (pg_current_wal_insert_lsn() - '0/0') % wal_segsize;
        IF remain < 2 * setting::int from pg_settings where name = 'block_size' THEN
            RAISE log 'exiting after % iterations, % bytes to end of WAL segment', iters, remain;
            EXIT;
        END IF;
        iters := iters + 1;
    END LOOP;
END
$$
;
/*
 * 6.16
 * COLLATIONS view
 */

CREATE VIEW collations AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST('NO PAD' AS character_data) AS pad_attribute
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (sum(somename))
;
create rule rtest_pers_upd as on update to rtest_person do also
	update rtest_admin set pname = new.pname where pname = old.pname
;
CREATE TABLE "fkpart4"."droppk21" (
    "a" integer CONSTRAINT "droppk_a_not_null" NOT NULL
)
;
CREATE TABLE tab_new (a int PRIMARY KEY)
;
DROP TABLE inht1, inhs1 CASCADE
;
create table idxpart (a int) partition by range (a)
;
CREATE TABLE parted_fk_naming (
    id bigint NOT NULL default 1,
    id_abc bigint,
    CONSTRAINT dummy_constr FOREIGN KEY (id_abc)
        REFERENCES parted_fk_naming (id),
    PRIMARY KEY (id)
)
PARTITION BY LIST (id)
;
CREATE TABLE "generated_stored_tests"."gtesty" (
    "x" integer,
    "b" integer GENERATED ALWAYS AS (("x" * 22)) STORED
)
;
create unique index on idxpart1 (a)
;
/* src/test/modules/test_tidstore/test_tidstore--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_create(
shared bool)
RETURNS void STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
-- Check that stats for relations are dropped. For that we need to access stats
-- by oid after the DROP TABLE. Save oids.
CREATE TABLE drop_stats_test()
;
-- Check allowed ranges
CREATE INDEX reloptions_test_idx2 ON reloptions_test (s) WITH (fillfactor=1)
;
alter extension test_ext7 update to '2.2bad'
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM FULL pg_amop
;
----------
[CREATE_PROG_OBJ] -- fail, can't infer type:
create function f1(x anyelement) returns anyrange as $$
begin
  return array[x + 1, x + 2];
end$$ language plpgsql
;
----------
[CREATE_TABLE] -- ADD COLUMN

CREATE TABLE itest13 (a int)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- check multikey cases, especially out-of-order column lists

-- these should work

ALTER TABLE fktable ADD CONSTRAINT fk_123_123
FOREIGN KEY (x1,x2,x3) REFERENCES pktable(id1,id2,id3)
;
----------
[ADD_INDEX] create index on mcrparted4 (a, abs(b), c)
;
----------
[CREATE_POLICY] create rule base_tab_def_view_ins_rule as on insert to base_tab_def_view
  do instead insert into base_tab_def values (new.a, new.b, new.c, new.d, new.e)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."temporal_rng2" (
    "id1" "int4range" NOT NULL,
    "id2" "int4range" NOT NULL,
    "valid_at" "daterange" NOT NULL
)
;
----------
[CREATE_TABLE] /* src/test/modules/test_extensions/test_event_trigger--1.0.sql */
-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- Base table with event trigger, used in a regression test involving
-- extension upgrades.
CREATE TABLE t (id text)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION IF NOT EXISTS "postgres_fdw"
;
----------
[ADD_INDEX] -- fail
CREATE INDEX collate_test1_idx6 ON collate_test1 ((a COLLATE "POSIX"))
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR %> (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- Doesn't make sense to add NO INHERIT constraints on partitioned tables
ALTER TABLE list_parted2 add constraint check_b2 check (b <> 'zz') NO INHERIT
;
----------
[CREATE_POLICY] /*
 * text search configuration for danish language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * danish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY danish_stem
	(TEMPLATE = snowball, Language = danish , StopWords=danish)
;
----------
[DROP_TABLE] DROP TABLE CASE_TBL
;
----------
[DROP_TABLE] drop table parted_conflict_test
;
----------
[DROP_TABLE] DROP TABLE testpub_tbl4
;
----------
[ALTER_PROG_OBJ] /* contrib/hstore/hstore--1.4--1.5.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


ALTER OPERATOR #<=# (hstore, hstore) SET (
       RESTRICT = scalarlesel,
       JOIN = scalarlejoinsel
)
;
----------
[CREATE_TABLE] CREATE TABLE prt1_n_p1 PARTITION OF prt1_n FOR VALUES FROM ('0000') TO ('0250')
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE split_alter_table.base_table ALTER CONSTRAINT base_table_ref_fk NOT DEFERRABLE INITIALLY IMMEDIATE
;
----------
[CREATE_TABLE] CREATE TABLE OID_TBL(f1 oid)
;
----------
[CREATE_TYPE] CREATE TYPE basket AS (apples integer, pears integer, mangos integer)
;
----------
[CREATE_TABLE] CREATE TABLE daucus PARTITION OF vegetables FOR VALUES IN ('daucus')
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tab_notrep AS SELECT generate_series(1,10) AS a
;
----------
[DROP_INDEX] --
-- Try some concurrent index drops
--
DROP INDEX CONCURRENTLY "concur_index2"
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER rw_view1_ins_trig AFTER INSERT ON base_tbl
  FOR EACH ROW EXECUTE PROCEDURE rw_view1_trig_fn()
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test11 AS smallint
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX tbl_idx ON tbl using btree(c1, c2, c3, c4)
;
----------
[CREATE_TABLE] CREATE TABLE copy_rel_to (a integer, b text)
;
----------
[CREATE_TABLE] CREATE TABLE fk_parted_pk_2 PARTITION OF fk_parted_pk FOR VALUES IN (2)
;
----------
[CREATE_TABLE] CREATE TABLE arrtest (
	a 			int2[],
	b 			int4[][][],
	c 			name[],
	d			text[][],
	e 			float8[],
	f			char(5)[],
	g			varchar(5)[]
)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_publication_namespace
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW constraint_table_usage AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nr.nspname AS sql_identifier) AS table_schema,
           CAST(r.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name

    FROM pg_constraint c, pg_namespace nc,
         pg_class r, pg_namespace nr

    WHERE c.connamespace = nc.oid AND r.relnamespace = nr.oid
          AND ( (c.contype = 'f' AND c.confrelid = r.oid)
             OR (c.contype IN ('p', 'u') AND c.conrelid = r.oid) )
          AND r.relkind IN ('r', 'p')
          AND pg_has_role(r.relowner, 'USAGE')
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE hash_parted ATTACH PARTITION fail_part FOR VALUES WITH (MODULUS 3, REMAINDER 2)
;
----------
[CREATE_TABLE] CREATE TABLE tab_part2(a int) PARTITION BY RANGE(a)
;
----------
[DROP_TYPE] drop domain ddef2 restrict
;
----------
[BLOCK] DO $$
DECLARE
    wal_segsize int := setting::int FROM pg_settings WHERE name = 'wal_segment_size';
    remain int;
    iters  int := 0;
BEGIN
    LOOP
        INSERT into filler
        select g, repeat(md5(g::text), (random() * 60 + 1)::int)
        from generate_series(1, 10) g;

        remain := wal_segsize - (pg_current_wal_insert_lsn() - '0/0') % wal_segsize;
        IF remain < 2 * setting::int from pg_settings where name = 'block_size' THEN
            RAISE log 'exiting after % iterations, % bytes to end of WAL segment', iters, remain;
            EXIT;
        END IF;
        iters := iters + 1;
    END LOOP;
END
$$
;
----------
[CREATE_VIEW(SELECT)] /*
 * 6.16
 * COLLATIONS view
 */

CREATE VIEW collations AS
    SELECT CAST(current_database() AS sql_identifier) AS collation_catalog,
           CAST(nc.nspname AS sql_identifier) AS collation_schema,
           CAST(c.collname AS sql_identifier) AS collation_name,
           CAST('NO PAD' AS character_data) AS pad_attribute
    FROM pg_collation c, pg_namespace nc
    WHERE c.collnamespace = nc.oid
          AND collencoding IN (-1, (SELECT encoding FROM pg_database WHERE datname = current_database()))
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (sum(somename))
;
----------
[CREATE_POLICY] create rule rtest_pers_upd as on update to rtest_person do also
	update rtest_admin set pname = new.pname where pname = old.pname
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart4"."droppk21" (
    "a" integer CONSTRAINT "droppk_a_not_null" NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE tab_new (a int PRIMARY KEY)
;
----------
[DROP_TABLE] DROP TABLE inht1, inhs1 CASCADE
;
----------
[CREATE_TABLE] create table idxpart (a int) partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE parted_fk_naming (
    id bigint NOT NULL default 1,
    id_abc bigint,
    CONSTRAINT dummy_constr FOREIGN KEY (id_abc)
        REFERENCES parted_fk_naming (id),
    PRIMARY KEY (id)
)
PARTITION BY LIST (id)
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtesty" (
    "x" integer,
    "b" integer GENERATED ALWAYS AS (("x" * 22)) STORED
)
;
----------
[ADD_INDEX] create unique index on idxpart1 (a)
;
----------
[CREATE_PROG_OBJ] /* src/test/modules/test_tidstore/test_tidstore--1.0.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION test_create(
shared bool)
RETURNS void STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
----------
[CREATE_TABLE] -- Check that stats for relations are dropped. For that we need to access stats
-- by oid after the DROP TABLE. Save oids.
CREATE TABLE drop_stats_test()
;
----------
[ADD_INDEX] -- Check allowed ranges
CREATE INDEX reloptions_test_idx2 ON reloptions_test (s) WITH (fillfactor=1)
;
----------
[ALTER_LIBRARY] alter extension test_ext7 update to '2.2bad'
;
