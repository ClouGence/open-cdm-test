ANALYZE prt2_l
;
DROP TABLE fk_partitioned_pk_6, fk_partitioned_fk_6
;
create table boolrangep_ft partition of boolrangep for values from ('false', 'true', 0) to ('false', 'true', 100)
;
CREATE TABLE intervaltmp (a interval)
;
CREATE TABLE reloptions_test (s VARCHAR) WITH
	(toast.autovacuum_vacuum_cost_delay = 23,
	autovacuum_vacuum_cost_delay = 24, fillfactor = 40)
;
VACUUM (PARALLEL 2) pg_catalog.pg_cast
;
create temp table defc (f1 int default 0
                        references defp on delete set default)
;
DROP TABLE testschema.part
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_enum
;
-- Tests for row filters
CREATE TABLE testpub_rf_tbl1 (a integer, b text)
;
/*
 * text search configuration for hungarian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hungarian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
CREATE FUNCTION myschema.myintne(bigint, int) RETURNS bool as $$
		BEGIN
		  RETURN $1 <> $2;
		END;
	  $$ LANGUAGE plpgsql immutable
;
CREATE TABLE "public"."gtest_child3" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
CREATE SCHEMA IF NOT EXISTS newschema4 AUTHORIZATION SESSION_USER
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_schema.mv_nodata2 (a) AS
  SELECT generate_series(1, 10) WITH NO DATA
;
VACUUM (PARALLEL 2) pg_catalog.pg_sequence
;
-- test value merging in add_value
CREATE TABLE brintest_2 (n numrange)
;
ALTER TABLE split_alter_table.base_table ENABLE RULE base_table_notify
;
CREATE VIEW lock_view3 AS SELECT * from lock_view2
;
ALTER PUBLICATION testpub_default SET (publish = update)
;
ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "int" WITH "simple"
;
CREATE TABLE part5_def PARTITION OF part_5 DEFAULT PARTITION BY LIST(a)
;
CREATE FUNCTION ltree_addtext(ltree,text)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- infer anyarray from anyrange
create function outparam_succeed2(i anyrange, out r anyarray, out t text)
  as $$ select ARRAY[upper($1)], 'foo'::text $$ language sql
;
CREATE VIEW rw_view2 AS
  SELECT aa AS aaa, bb AS bbb, c AS c1, 'Const2' AS c2 FROM rw_view1 WHERE aa<10
;
CREATE INDEX ON maintain_test (a)
;
create temp table child (k int unique, cd int)
;
VACUUM (FULL, FREEZE) vactst
;
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1, ftest2) REFERENCES PKTABLE MATCH FULL
;
CREATE FUNCTION hobby_construct_named(name text, hobby text)
   RETURNS hobbies_r
   AS 'select name, hobby'
   LANGUAGE SQL
;
DROP FUNCTION foo_update
;
COMMENT ON TEXT SEARCH CONFIGURATION nepali IS 'configuration for nepali language'
;
-- ok

-- MERGE with INSTEAD OF triggers on auto-updatable view
CREATE TRIGGER rw_view2_upd_trig INSTEAD OF UPDATE ON rw_view2
  FOR EACH ROW EXECUTE PROCEDURE rw_view1_trig_fn()
;
ALTER TEXT SEARCH CONFIGURATION english ADD MAPPING
    FOR word, hword_part, hword
	WITH english_stem
;
drop function set_role_and_error(int)
;
CREATE MATERIALIZED VIEW "public"."mvtest_tm" AS
 SELECT "mvtest_t"."type",
    "sum"("mvtest_t"."amt") AS "totamt"
   FROM "public"."mvtest_t"
  GROUP BY "mvtest_t"."type"
  WITH NO DATA
;
CREATE MATERIALIZED VIEW testmv1 AS SELECT * FROM test1
;
--
-- Renaming domain constraints
--

create domain testdomain1 as int constraint unsigned check (value > 0)
;
ALTER TABLE pg_extension ADD PRIMARY KEY USING INDEX pg_extension_oid_index
;
--
-- Test the planner's ability to reorder the distinctClause Pathkeys to match
-- the input path's ordering
--

CREATE TABLE distinct_tbl (x int, y int)
;
ALTER TABLE comment_test ALTER COLUMN xmin SET DATA TYPE x
;
ALTER TABLE prt1_adv DETACH PARTITION prt1_adv_p1
;
CREATE INDEX hash_split_index on hash_split_heap USING HASH (keycol)
;
CREATE TABLE parttbl (a int, b int, c int,
   d int GENERATED ALWAYS AS (a + b) STORED) PARTITION BY LIST (a)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_replication_origin
;
CREATE TABLE "fkpart4"."droppk2_d" (
    "a" integer NOT NULL
)
;
ALTER INDEX pg_toast.pg_toast_33530_index RENAME TO reind_con_toast_idx
;
CREATE TABLE var_ptbl_tens PARTITION OF var_ptbl FOR VALUES FROM ('10') TO ('100')
;
do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '36411'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
create table mcrparted (a text, b int) partition by range(a, b)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE prt2_l
;
----------
[DROP_TABLE] DROP TABLE fk_partitioned_pk_6, fk_partitioned_fk_6
;
----------
[CREATE_TABLE] create table boolrangep_ft partition of boolrangep for values from ('false', 'true', 0) to ('false', 'true', 100)
;
----------
[CREATE_TABLE] CREATE TABLE intervaltmp (a interval)
;
----------
[CREATE_TABLE] CREATE TABLE reloptions_test (s VARCHAR) WITH
	(toast.autovacuum_vacuum_cost_delay = 23,
	autovacuum_vacuum_cost_delay = 24, fillfactor = 40)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_cast
;
----------
[CREATE_TABLE] create temp table defc (f1 int default 0
                        references defp on delete set default)
;
----------
[DROP_TABLE] DROP TABLE testschema.part
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_enum
;
----------
[CREATE_TABLE] -- Tests for row filters
CREATE TABLE testpub_rf_tbl1 (a integer, b text)
;
----------
[CREATE_POLICY] /*
 * text search configuration for hungarian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * hungarian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY hungarian_stem
	(TEMPLATE = snowball, Language = hungarian , StopWords=hungarian)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION myschema.myintne(bigint, int) RETURNS bool as $$
		BEGIN
		  RETURN $1 <> $2;
		END;
	  $$ LANGUAGE plpgsql immutable
;
----------
[CREATE_TABLE] CREATE TABLE "public"."gtest_child3" (
    "f1" "date" NOT NULL,
    "f2" bigint,
    "f3" bigint GENERATED ALWAYS AS (("f2" * 2)) STORED
)
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS newschema4 AUTHORIZATION SESSION_USER
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE MATERIALIZED VIEW matview_schema.mv_nodata2 (a) AS
  SELECT generate_series(1, 10) WITH NO DATA
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_sequence
;
----------
[CREATE_TABLE] -- test value merging in add_value
CREATE TABLE brintest_2 (n numrange)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table ENABLE RULE base_table_notify
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW lock_view3 AS SELECT * from lock_view2
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_default SET (publish = update)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."hunspell_tst"
    ADD MAPPING FOR "int" WITH "simple"
;
----------
[CREATE_TABLE] CREATE TABLE part5_def PARTITION OF part_5 DEFAULT PARTITION BY LIST(a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_addtext(ltree,text)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- infer anyarray from anyrange
create function outparam_succeed2(i anyrange, out r anyarray, out t text)
  as $$ select ARRAY[upper($1)], 'foo'::text $$ language sql
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view2 AS
  SELECT aa AS aaa, bb AS bbb, c AS c1, 'Const2' AS c2 FROM rw_view1 WHERE aa<10
;
----------
[ADD_INDEX] CREATE INDEX ON maintain_test (a)
;
----------
[CREATE_TABLE] create temp table child (k int unique, cd int)
;
----------
[ADMIN_TABLE] VACUUM (FULL, FREEZE) vactst
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1, ftest2) REFERENCES PKTABLE MATCH FULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hobby_construct_named(name text, hobby text)
   RETURNS hobbies_r
   AS 'select name, hobby'
   LANGUAGE SQL
;
----------
[DROP_PROG_OBJ] DROP FUNCTION foo_update
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION nepali IS 'configuration for nepali language'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- ok

-- MERGE with INSTEAD OF triggers on auto-updatable view
CREATE TRIGGER rw_view2_upd_trig INSTEAD OF UPDATE ON rw_view2
  FOR EACH ROW EXECUTE PROCEDURE rw_view1_trig_fn()
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION english ADD MAPPING
    FOR word, hword_part, hword
	WITH english_stem
;
----------
[DROP_PROG_OBJ] drop function set_role_and_error(int)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_tm" AS
 SELECT "mvtest_t"."type",
    "sum"("mvtest_t"."amt") AS "totamt"
   FROM "public"."mvtest_t"
  GROUP BY "mvtest_t"."type"
  WITH NO DATA
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW testmv1 AS SELECT * FROM test1
;
----------
[CREATE_TYPE] --
-- Renaming domain constraints
--

create domain testdomain1 as int constraint unsigned check (value > 0)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_extension ADD PRIMARY KEY USING INDEX pg_extension_oid_index
;
----------
[CREATE_TABLE] --
-- Test the planner's ability to reorder the distinctClause Pathkeys to match
-- the input path's ordering
--

CREATE TABLE distinct_tbl (x int, y int)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE comment_test ALTER COLUMN xmin SET DATA TYPE x
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt1_adv DETACH PARTITION prt1_adv_p1
;
----------
[ADD_INDEX] CREATE INDEX hash_split_index on hash_split_heap USING HASH (keycol)
;
----------
[CREATE_TABLE] CREATE TABLE parttbl (a int, b int, c int,
   d int GENERATED ALWAYS AS (a + b) STORED) PARTITION BY LIST (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 0) pg_catalog.pg_replication_origin
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart4"."droppk2_d" (
    "a" integer NOT NULL
)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33530_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE var_ptbl_tens PARTITION OF var_ptbl FOR VALUES FROM ('10') TO ('100')
;
----------
[BLOCK] do 'declare c int = 0;
begin
  while (select count(*) from pg_replication_slots where active_pid = '
    '36411'
  ') > 0 loop c := c + 1; perform pg_sleep(0.01); end loop;
  raise log ''slot test looped % times'', c;
end'
;
----------
[CREATE_TABLE] create table mcrparted (a text, b int) partition by range(a, b)
;
