INSERT INTO seeded_random(seed, rand, val) VALUES
  (796727079, 'uniform', 1016),
  (796727079, 'exponential', 2109),
  (796727079, 'gaussian', 3456),
  (796727079, 'zipfian', 4040)
;
INSERT INTO test (a) VALUES (('{' || 'ssssssssss' || random_string(1900) ||'}')::text[])
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."alpha_neg_p3"'::pg_catalog.regclass
;
insert into insertconflicttest AS ict values (6, 'Passionfruit') on conflict (key) do update set fruit = insertconflicttest.fruit
;
INSERT INTO public.tht VALUES ('6', 136)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '20287', relminmxid = '3'
WHERE oid = '"public"."covidxpart2"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."itest7"'::pg_catalog.regclass
;
-- fail
update parted set c = c || 'v5'
;
insert into fkeys2 values (40, '4', 5)
;
INSERT INTO bv1 VALUES (11, 'xxx')
;
INSERT INTO caster (int8)          VALUES ('12'::text)
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."gtest1_1"'::pg_catalog.regclass
;
-- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted_defd"'::pg_catalog.regclass
  AND attname = v.dname
;
INSERT INTO public.ths_p2 VALUES (3, 853)
;
INSERT INTO public.tht VALUES ('4', 484)
;
insert into matest1 (name) values ('Test 2')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '20509', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo3"'::pg_catalog.regclass
;
DELETE FROM ONLY "public"."convslot_test_child" WHERE $1 OPERATOR(pg_catalog.=) "col1"
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9129', relminmxid = '1'
WHERE oid = '28434'
;
-- Delete another row from PK TABLE
DELETE FROM PKTABLE WHERE ptest1=5 and ptest2=10
;
INSERT INTO public.ths_p1 VALUES (2, 862)
;
UPDATE tab_mixed SET c = 3.3 WHERE a = 2
;
INSERT INTO rls_test_restrictive VALUES ('regress_r3','regress_s3',3)
;
INSERT INTO pg_temp_24.pg_temp_26683_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_24.pg_temp_26683 AS newdata FROM public.mvtest_boxmv mv FULL JOIN pg_temp_24.pg_temp_26683 newdata ON (newdata.id OPERATOR(pg_catalog.=) mv.id AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
update xx1 set x2 = f1 from (select * from int4_tbl where f1 = xx1.x1) ss
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1245', relminmxid = '1'
WHERE oid = '"public"."num_typemod_test"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1921', relminmxid = '1'
WHERE oid = '"public"."tab_core_types"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '38533'
;
insert into outer_text values ('b', null)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11598', relminmxid = '3'
WHERE oid = '32405'
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16798', relminmxid = '3'
WHERE oid = '"public"."alpha_neg_p1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest27"'::pg_catalog.regclass
;
insert into tt6 values(1, 2)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10080', relminmxid = '1'
WHERE oid = '"public"."rules_src"'::pg_catalog.regclass
;
UPDATE tr_etoomuch SET id = -id WHERE id = 5000
;
INSERT INTO copy_t (SELECT x, md5(x::text) FROM generate_series(0,10) x)
;
INSERT INTO unique_tbl VALUES (2, 'two')
;
DELETE FROM public.citext_matview mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_3.pg_temp_16652_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
INSERT INTO public.ths_p2 VALUES (7, 907)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9224', relminmxid = '1'
WHERE oid = '"public"."brin_test_bloom"'::pg_catalog.regclass
;
-- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"regress_indexing"."pk3"'::pg_catalog.regclass
  AND attname IN ('a')
;
update self_ref_trigger set nchildren = nchildren - 1
      where id = old.parent
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17386', relminmxid = '3'
WHERE oid = '39682'
;
-- succeed, because violation is ignored
INSERT INTO circles VALUES('<(20,20), 10>', '<(0,0), 4>')
  ON CONFLICT ON CONSTRAINT circles_c1_c2_excl DO NOTHING
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15637', relminmxid = '3'
WHERE oid = '"public"."idxpart22"'::pg_catalog.regclass
;
insert into parted_stmt_trig values (1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7392', relminmxid = '1'
WHERE oid = '"public"."mvtest_tm"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('3', 263)
;
UPDATE main_table SET a = a + 2 WHERE b > 100
;
INSERT INTO trans_abc VALUES (19)
;
------------------------------------------------------------------------------------------
[INSERT|SELECT] INSERT INTO seeded_random(seed, rand, val) VALUES
  (796727079, 'uniform', 1016),
  (796727079, 'exponential', 2109),
  (796727079, 'gaussian', 3456),
  (796727079, 'zipfian', 4040)
;
----------
[INSERT|SELECT] INSERT INTO test (a) VALUES (('{' || 'ssssssssss' || random_string(1900) ||'}')::text[])
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."alpha_neg_p3"'::pg_catalog.regclass
;
----------
[MERGE] insert into insertconflicttest AS ict values (6, 'Passionfruit') on conflict (key) do update set fruit = insertconflicttest.fruit
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 136)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '20287', relminmxid = '3'
WHERE oid = '"public"."covidxpart2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."itest7"'::pg_catalog.regclass
;
----------
[UPDATE] -- fail
update parted set c = c || 'v5'
;
----------
[INSERT] insert into fkeys2 values (40, '4', 5)
;
----------
[INSERT] INSERT INTO bv1 VALUES (11, 'xxx')
;
----------
[INSERT|SELECT] INSERT INTO caster (int8)          VALUES ('12'::text)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."gtest1_1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped columns.
UPDATE pg_catalog.pg_attribute
SET attlen = v.dlen, attalign = v.dalign, attbyval = false
FROM (VALUES ('........pg.dropped.4........', 4, 'i'),
             ('........pg.dropped.5........', 4, 'i'),
             ('........pg.dropped.6........', 4, 'i'),
             ('........pg.dropped.7........', 4, 'i')) v(dname, dlen, dalign)
WHERE attrelid = '"public"."mlparted_defd"'::pg_catalog.regclass
  AND attname = v.dname
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 853)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 484)
;
----------
[INSERT|SELECT] insert into matest1 (name) values ('Test 2')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '20509', relminmxid = '3'
WHERE oid = '"public"."rewritemetoo3"'::pg_catalog.regclass
;
----------
[DELETE] DELETE FROM ONLY "public"."convslot_test_child" WHERE $1 OPERATOR(pg_catalog.=) "col1"
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9129', relminmxid = '1'
WHERE oid = '28434'
;
----------
[DELETE] -- Delete another row from PK TABLE
DELETE FROM PKTABLE WHERE ptest1=5 and ptest2=10
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 862)
;
----------
[UPDATE] UPDATE tab_mixed SET c = 3.3 WHERE a = 2
;
----------
[INSERT] INSERT INTO rls_test_restrictive VALUES ('regress_r3','regress_s3',3)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_24.pg_temp_26683_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_24.pg_temp_26683 AS newdata FROM public.mvtest_boxmv mv FULL JOIN pg_temp_24.pg_temp_26683 newdata ON (newdata.id OPERATOR(pg_catalog.=) mv.id AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|SELECT] update xx1 set x2 = f1 from (select * from int4_tbl where f1 = xx1.x1) ss
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1245', relminmxid = '1'
WHERE oid = '"public"."num_typemod_test"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1921', relminmxid = '1'
WHERE oid = '"public"."tab_core_types"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '38533'
;
----------
[INSERT] insert into outer_text values ('b', null)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '11598', relminmxid = '3'
WHERE oid = '32405'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16798', relminmxid = '3'
WHERE oid = '"public"."alpha_neg_p1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest27"'::pg_catalog.regclass
;
----------
[INSERT] insert into tt6 values(1, 2)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10080', relminmxid = '1'
WHERE oid = '"public"."rules_src"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE tr_etoomuch SET id = -id WHERE id = 5000
;
----------
[INSERT|SELECT] INSERT INTO copy_t (SELECT x, md5(x::text) FROM generate_series(0,10) x)
;
----------
[INSERT] INSERT INTO unique_tbl VALUES (2, 'two')
;
----------
[DELETE|SELECT] DELETE FROM public.citext_matview mv WHERE ctid OPERATOR(pg_catalog.=) ANY (SELECT diff.tid FROM pg_temp_3.pg_temp_16652_2 diff WHERE diff.tid IS NOT NULL AND diff.newdata IS NULL)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (7, 907)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '9224', relminmxid = '1'
WHERE oid = '"public"."brin_test_bloom"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited columns.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"regress_indexing"."pk3"'::pg_catalog.regclass
  AND attname IN ('a')
;
----------
[UPDATE] update self_ref_trigger set nchildren = nchildren - 1
      where id = old.parent
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '17386', relminmxid = '3'
WHERE oid = '39682'
;
----------
[MERGE] -- succeed, because violation is ignored
INSERT INTO circles VALUES('<(20,20), 10>', '<(0,0), 4>')
  ON CONFLICT ON CONSTRAINT circles_c1_c2_excl DO NOTHING
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15637', relminmxid = '3'
WHERE oid = '"public"."idxpart22"'::pg_catalog.regclass
;
----------
[INSERT] insert into parted_stmt_trig values (1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7392', relminmxid = '1'
WHERE oid = '"public"."mvtest_tm"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('3', 263)
;
----------
[UPDATE] UPDATE main_table SET a = a + 2 WHERE b > 100
;
----------
[INSERT] INSERT INTO trans_abc VALUES (19)
;
