--
-- Tests for BooleanTest
--
CREATE TABLE BOOLTBL3 (d text, b bool, o int)
;
ANALYZE array_index_op_test
;
ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = contsel)
;
create trigger t2 before update on parted
  for each row execute function parted_trigfunc2()
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE selinto_schema.tbl_nodata4 (a) AS
  EXECUTE data_sel WITH NO DATA
;
-- error
VACUUM (PARALLEL 2, INDEX_CLEANUP FALSE) pvactst
;
ALTER TABLE ONLY "public"."test_type_diff_c" INHERIT "public"."test_type_diff"
;
CREATE TEMP TABLE id_alw2 (i int GENERATED ALWAYS AS IDENTITY)
;
-- OK
ALTER OPERATOR CLASS alt_opc2 USING hash SET SCHEMA alt_nsp2
;
VACUUM TEXT_TBL
;
DROP TABLE pg_temp_9.pg_temp_26388_2, pg_temp_9.pg_temp_26388
;
CREATE TABLE brin_parallel_test (a int, b text, c bigint) WITH (fillfactor=40)
;
CREATE FUNCTION gbt_ts_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER FUNCTION functest_F_2(int) STRICT
;
ANALYZE btg
;
create rule base_tab_def_view_ins_rule as on insert to base_tab_def_view
  do also insert into base_tab_def values (new.a, new.b, new.c, new.d, new.e)
;
/*
 * 7. Check various AMs. All but btree and gist must fail.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 box, c4 box)
;
VACUUM ANALYZE person
;
alter table utrtest attach partition remp for values in (1)
;
CREATE FUNCTION gbt_bool_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- Clean up
DROP TABLE pxtest1
;
create domain var_record_nn as var_record not null
;
create temp table p1 partition of p for values from (0) to (10)
;
-- fast defaults lead to attribute mapping being used in one
-- direction, but not the other
CREATE TABLE errtst_child_fastdef (
    partid int not null,
    shdata int not null,
    CONSTRAINT shdata_small CHECK(shdata < 3)
)
;
create temp table t1(f1 int, f2 bigint)
;
CREATE FUNCTION no_op_trig_fn() RETURNS trigger LANGUAGE plpgsql
AS 'begin RETURN NULL; end'
;
CREATE FUNCTION ext_cor_func() RETURNS text
  AS $$ SELECT 'ext_cor_func: original'::text $$ LANGUAGE sql
;
ALTER TABLE test_replica_identity3 REPLICA IDENTITY FULL
;
CREATE POLICY p2r ON document AS RESTRICTIVE TO regress_rls_dave
    USING (cid <> 44 AND cid < 50)
;
CREATE FUNCTION g_cube_union(internal, internal)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE gtest_err_2b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED, c int GENERATED ALWAYS AS (b * 3) STORED)
;
analyze q2
;
CREATE SEQUENCE sequence_test7 AS bigint
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN IF NOT EXISTS c3 integer, 
	ADD COLUMN c4 integer
;
ALTER POLICY p1 ON t1 RENAME TO p2
;
create unique index t2_z_uidx on t2(z)
;
SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS 'unclassified'
;
ALTER INDEX pg_toast.pg_toast_33633_index RENAME TO reind_con_toast_idx
;
CREATE TYPE "public"."widget" (
    INTERNALLENGTH = 24,
    INPUT = "public"."widget_in",
    OUTPUT = "public"."widget_out",
    TYPMOD_IN = "numerictypmodin",
    TYPMOD_OUT = "numerictypmodout",
    ALIGNMENT = double,
    STORAGE = plain
)
;
create view rw_view5 as select aa, bb FROM rw_view1
;
drop table rp_prefix_test1
;
DROP FUNCTION testpub_rf_func1(integer, integer)
;
CREATE FUNCTION test_predtest(query text,
  OUT strong_implied_by bool,
  OUT weak_implied_by bool,
  OUT strong_refuted_by bool,
  OUT weak_refuted_by bool,
  OUT s_i_holds bool,
  OUT w_i_holds bool,
  OUT s_r_holds bool,
  OUT w_r_holds bool)
STRICT
AS '$libdir/test_predtest' LANGUAGE C
;
DROP TABLE pg_temp_11.pg_temp_26571_2, pg_temp_11.pg_temp_26571
;
VACUUM ANALYZE atest12
;
create table testtable_apple(logdate date)
;
DROP INDEX brin_test_temp_idx
;
--
-- VARCHAR
--

--
-- Build a table for testing
-- (This temporarily hides the table created in test_setup.sql)
--

CREATE TEMP TABLE VARCHAR_TBL(f1 varchar(1))
;
CREATE TABLE CASE2_TBL (
  i integer,
  j integer
)
;
create or replace function parted_conflict_update_func() returns trigger as $$
declare
    r record;
begin
 for r in select * from inserted loop
	raise notice 'a = %, b = %, c = %', r.a, r.b, r.c;
 end loop;
 return new;
end;
$$ language plpgsql
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] --
-- Tests for BooleanTest
--
CREATE TABLE BOOLTBL3 (d text, b bool, o int)
;
----------
[ADMIN_TABLE] ANALYZE array_index_op_test
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR === (boolean, boolean) SET (RESTRICT = contsel)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger t2 before update on parted
  for each row execute function parted_trigfunc2()
;
----------
[CREATE_TABLE|UNSAFE] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF)
  CREATE TABLE selinto_schema.tbl_nodata4 (a) AS
  EXECUTE data_sel WITH NO DATA
;
----------
[ADMIN_TABLE] -- error
VACUUM (PARALLEL 2, INDEX_CLEANUP FALSE) pvactst
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."test_type_diff_c" INHERIT "public"."test_type_diff"
;
----------
[CREATE_TABLE] CREATE TEMP TABLE id_alw2 (i int GENERATED ALWAYS AS IDENTITY)
;
----------
[ALTER_PROG_OBJ] -- OK
ALTER OPERATOR CLASS alt_opc2 USING hash SET SCHEMA alt_nsp2
;
----------
[ADMIN_TABLE] VACUUM TEXT_TBL
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_26388_2, pg_temp_9.pg_temp_26388
;
----------
[CREATE_TABLE] CREATE TABLE brin_parallel_test (a int, b text, c bigint) WITH (fillfactor=40)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_ts_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_F_2(int) STRICT
;
----------
[ADMIN_TABLE] ANALYZE btg
;
----------
[CREATE_POLICY] create rule base_tab_def_view_ins_rule as on insert to base_tab_def_view
  do also insert into base_tab_def values (new.a, new.b, new.c, new.d, new.e)
;
----------
[CREATE_TABLE] /*
 * 7. Check various AMs. All but btree and gist must fail.
 */
CREATE TABLE tbl (c1 int,c2 int, c3 box, c4 box)
;
----------
[ADMIN_TABLE] VACUUM ANALYZE person
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table utrtest attach partition remp for values in (1)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bool_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] -- Clean up
DROP TABLE pxtest1
;
----------
[CREATE_TYPE] create domain var_record_nn as var_record not null
;
----------
[CREATE_TABLE] create temp table p1 partition of p for values from (0) to (10)
;
----------
[CREATE_TABLE] -- fast defaults lead to attribute mapping being used in one
-- direction, but not the other
CREATE TABLE errtst_child_fastdef (
    partid int not null,
    shdata int not null,
    CONSTRAINT shdata_small CHECK(shdata < 3)
)
;
----------
[CREATE_TABLE] create temp table t1(f1 int, f2 bigint)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION no_op_trig_fn() RETURNS trigger LANGUAGE plpgsql
AS 'begin RETURN NULL; end'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ext_cor_func() RETURNS text
  AS $$ SELECT 'ext_cor_func: original'::text $$ LANGUAGE sql
;
----------
[ALTER_TABLE] ALTER TABLE test_replica_identity3 REPLICA IDENTITY FULL
;
----------
[CREATE_POLICY] CREATE POLICY p2r ON document AS RESTRICTIVE TO regress_rls_dave
    USING (cid <> 44 AND cid < 50)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION g_cube_union(internal, internal)
RETURNS cube
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE gtest_err_2b (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 2) STORED, c int GENERATED ALWAYS AS (b * 3) STORED)
;
----------
[ADMIN_TABLE] analyze q2
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_test7 AS bigint
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c2 integer, 
	ADD COLUMN IF NOT EXISTS c3 integer, 
	ADD COLUMN c4 integer
;
----------
[ALTER_POLICY] ALTER POLICY p1 ON t1 RENAME TO p2
;
----------
[ADD_INDEX] create unique index t2_z_uidx on t2(z)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE dummy_seclabel_tbl1 IS 'unclassified'
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33633_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TYPE] CREATE TYPE "public"."widget" (
    INTERNALLENGTH = 24,
    INPUT = "public"."widget_in",
    OUTPUT = "public"."widget_out",
    TYPMOD_IN = "numerictypmodin",
    TYPMOD_OUT = "numerictypmodout",
    ALIGNMENT = double,
    STORAGE = plain
)
;
----------
[CREATE_VIEW(SELECT)] create view rw_view5 as select aa, bb FROM rw_view1
;
----------
[DROP_TABLE] drop table rp_prefix_test1
;
----------
[DROP_PROG_OBJ] DROP FUNCTION testpub_rf_func1(integer, integer)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_predtest(query text,
  OUT strong_implied_by bool,
  OUT weak_implied_by bool,
  OUT strong_refuted_by bool,
  OUT weak_refuted_by bool,
  OUT s_i_holds bool,
  OUT w_i_holds bool,
  OUT s_r_holds bool,
  OUT w_r_holds bool)
STRICT
AS '$libdir/test_predtest' LANGUAGE C
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_26571_2, pg_temp_11.pg_temp_26571
;
----------
[ADMIN_TABLE] VACUUM ANALYZE atest12
;
----------
[CREATE_TABLE] create table testtable_apple(logdate date)
;
----------
[DROP_INDEX] DROP INDEX brin_test_temp_idx
;
----------
[CREATE_TABLE] --
-- VARCHAR
--

--
-- Build a table for testing
-- (This temporarily hides the table created in test_setup.sql)
--

CREATE TEMP TABLE VARCHAR_TBL(f1 varchar(1))
;
----------
[CREATE_TABLE] CREATE TABLE CASE2_TBL (
  i integer,
  j integer
)
;
----------
[CREATE_PROG_OBJ] create or replace function parted_conflict_update_func() returns trigger as $$
declare
    r record;
begin
 for r in select * from inserted loop
	raise notice 'a = %, b = %, c = %', r.a, r.b, r.c;
 end loop;
 return new;
end;
$$ language plpgsql
;
