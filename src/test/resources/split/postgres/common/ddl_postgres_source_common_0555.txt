DROP TABLE flt
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_enum
;
CREATE SCHEMA import_source
;
-- can't change the column type if it's used elsewhere
CREATE TABLE use_ft1_column_type (x ft1)
;
/*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
CREATE TABLE "public"."covidxpart" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("a")
;
-- test with multiple rows
truncate parted_conflict_test
;
CREATE TABLE test_tab1(a int primary key)
;
drop domain domainint4 restrict
;
CREATE FUNCTION seg_le(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
DROP TABLE tab_search_same, tab_search_diff_1, tab_search_diff_2
;
ALTER TABLE pg_toast.pg_toast_30855 RENAME TO reind_con_toast
;
ALTER TABLE list_parted2 DETACH PARTITION part_7
;
ANALYZE public.ihighway
;
VACUUM (PARALLEL 0) pg_catalog.pg_subscription_rel
;
CREATE TEMP TABLE withoutoid() WITH (oids = false)
;
-- Tests for currtid2() with various relation kinds

-- Materialized view
CREATE MATERIALIZED VIEW tid_matview AS SELECT a FROM tid_tab
;
VACUUM (SKIP_LOCKED) information_schema.sql_packages
;
DROP TABLE d_listp2
;
CREATE INDEX onek_hundred ON onek USING btree(hundred int4_ops)
;
-- define the GiST support methods
CREATE FUNCTION g_cube_consistent(internal,cube,smallint,oid,internal)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
-- Check behavior with a non-comparable rowtype
create type cantcompare as (p point, r float8)
;
CREATE TABLE gtest_child3 (f1 date NOT NULL, f2 bigint, f3 bigint)
;
CREATE TABLE dump_test.test_table_nn_chld2 (
								col1 int
							) INHERITS (dump_test.test_table_nn)
;
-- Test domains over array types

create domain domainint4arr int4[1]
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_depend
;
VACUUM pg_catalog.pg_trigger
;
CREATE OPERATOR CLASS isbn13_ops DEFAULT
	FOR TYPE isbn13 USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashisbn13(isbn13)
;
ALTER INDEX pg_toast.pg_toast_28210_index RENAME TO reind_con_toast_idx
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_proc
;
CREATE PUBLICATION pub5 WITH (publish_generated_columns = stored)
;
CREATE TABLE tpart_ones PARTITION OF tpart FOR VALUES FROM ('0') TO ('10')
;
CREATE SCHEMA newschema5 AUTHORIZATION regress_testrolx
;
-- Change the tablespaces so that the temporary table is rewritten to other
-- local buffers, exercising a different codepath than standard local buffer
-- writes.
ALTER TABLE test_io_local SET TABLESPACE regress_tblspace
;
DROP DATABASE test_pgc_true
;
alter table gtest32 alter column e type bigint using b
;
-- fail
DROP AGGREGATE priv_testagg1(int)
;
CREATE TABLE temporal_rng (
  id int4range,
  valid_at daterange
)
;
VACUUM (ANALYZE) vacparted
;
CREATE POLICY p2 ON r1 FOR INSERT WITH CHECK (true)
;
CREATE TABLE parent_tbl (a int, b text, c numeric) PARTITION BY RANGE(a)
;
-- Load some test data
CREATE TABLE test_tsvector(
	t text,
	a tsvector
)
;
-- range type partition key
create table pp_intrangepart (a int4range) partition by list (a)
;
drop table donothingbrtrig_test
;
ALTER TABLE pg_attribute ADD UNIQUE USING INDEX pg_attribute_relid_attnam_index
;
-- should work
CREATE TRIGGER alter_table_under_transition_tables_upd_trigger
  AFTER UPDATE ON alter_table_under_transition_tables
  REFERENCING OLD TABLE AS d NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    alter_table_under_transition_tables_upd_func()
;
CREATE FOREIGN TABLE t4_part (
  c1 integer OPTIONS (column_name 'c1')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't4_part')
;
-- This should also fail for the same reason, but here we
-- give the column name
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1) references pktable(ptest1)
;
-- try a table
CREATE TABLE testschema.foo (i int) TABLESPACE regress_tblspace
;
/* contrib/pageinspect/pageinspect--1.6--1.7.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- bt_metap()
--
DROP FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int4,
    OUT level int4,
    OUT fastroot int4,
    OUT fastlevel int4)
;
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE flt
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_enum
;
----------
[CREATE_SCHEMA] CREATE SCHEMA import_source
;
----------
[CREATE_TABLE] -- can't change the column type if it's used elsewhere
CREATE TABLE use_ft1_column_type (x ft1)
;
----------
[CREATE_POLICY] /*
 * text search configuration for arabic language
 *
 * Copyright (c) 2007-2024, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * arabic and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY arabic_stem
	(TEMPLATE = snowball, Language = arabic )
;
----------
[CREATE_TABLE] CREATE TABLE "public"."covidxpart" (
    "a" integer,
    "b" integer
)
PARTITION BY LIST ("a")
;
----------
[TRUNCATE_TABLE] -- test with multiple rows
truncate parted_conflict_test
;
----------
[CREATE_TABLE] CREATE TABLE test_tab1(a int primary key)
;
----------
[DROP_TYPE] drop domain domainint4 restrict
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_le(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE tab_search_same, tab_search_diff_1, tab_search_diff_2
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30855 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE list_parted2 DETACH PARTITION part_7
;
----------
[ADMIN_TABLE] ANALYZE public.ihighway
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_subscription_rel
;
----------
[CREATE_TABLE] CREATE TEMP TABLE withoutoid() WITH (oids = false)
;
----------
[CREATE_VIEW(SELECT)] -- Tests for currtid2() with various relation kinds

-- Materialized view
CREATE MATERIALIZED VIEW tid_matview AS SELECT a FROM tid_tab
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) information_schema.sql_packages
;
----------
[DROP_TABLE] DROP TABLE d_listp2
;
----------
[ADD_INDEX] CREATE INDEX onek_hundred ON onek USING btree(hundred int4_ops)
;
----------
[CREATE_PROG_OBJ] -- define the GiST support methods
CREATE FUNCTION g_cube_consistent(internal,cube,smallint,oid,internal)
RETURNS bool
AS '$libdir/cube'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TYPE] -- Check behavior with a non-comparable rowtype
create type cantcompare as (p point, r float8)
;
----------
[CREATE_TABLE] CREATE TABLE gtest_child3 (f1 date NOT NULL, f2 bigint, f3 bigint)
;
----------
[CREATE_TABLE] CREATE TABLE dump_test.test_table_nn_chld2 (
								col1 int
							) INHERITS (dump_test.test_table_nn)
;
----------
[CREATE_TYPE] -- Test domains over array types

create domain domainint4arr int4[1]
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_depend
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_trigger
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS isbn13_ops DEFAULT
	FOR TYPE isbn13 USING hash FAMILY isn_ops AS
	OPERATOR 1  =,
	FUNCTION 1  hashisbn13(isbn13)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28210_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_proc
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub5 WITH (publish_generated_columns = stored)
;
----------
[CREATE_TABLE] CREATE TABLE tpart_ones PARTITION OF tpart FOR VALUES FROM ('0') TO ('10')
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema5 AUTHORIZATION regress_testrolx
;
----------
[ALTER_TABLE] -- Change the tablespaces so that the temporary table is rewritten to other
-- local buffers, exercising a different codepath than standard local buffer
-- writes.
ALTER TABLE test_io_local SET TABLESPACE regress_tblspace
;
----------
[DROP_CATALOG] DROP DATABASE test_pgc_true
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table gtest32 alter column e type bigint using b
;
----------
[DROP_PROG_OBJ] -- fail
DROP AGGREGATE priv_testagg1(int)
;
----------
[CREATE_TABLE] CREATE TABLE temporal_rng (
  id int4range,
  valid_at daterange
)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) vacparted
;
----------
[CREATE_POLICY] CREATE POLICY p2 ON r1 FOR INSERT WITH CHECK (true)
;
----------
[CREATE_TABLE] CREATE TABLE parent_tbl (a int, b text, c numeric) PARTITION BY RANGE(a)
;
----------
[CREATE_TABLE] -- Load some test data
CREATE TABLE test_tsvector(
	t text,
	a tsvector
)
;
----------
[CREATE_TABLE] -- range type partition key
create table pp_intrangepart (a int4range) partition by list (a)
;
----------
[DROP_TABLE] drop table donothingbrtrig_test
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_attribute ADD UNIQUE USING INDEX pg_attribute_relid_attnam_index
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- should work
CREATE TRIGGER alter_table_under_transition_tables_upd_trigger
  AFTER UPDATE ON alter_table_under_transition_tables
  REFERENCING OLD TABLE AS d NEW TABLE AS i
  FOR EACH STATEMENT EXECUTE PROCEDURE
    alter_table_under_transition_tables_upd_func()
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE t4_part (
  c1 integer OPTIONS (column_name 'c1')
) SERVER loopback
OPTIONS (schema_name 'import_source', table_name 't4_part')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- This should also fail for the same reason, but here we
-- give the column name
ALTER TABLE FKTABLE ADD FOREIGN KEY(ftest1) references pktable(ptest1)
;
----------
[CREATE_TABLE] -- try a table
CREATE TABLE testschema.foo (i int) TABLESPACE regress_tblspace
;
----------
[DROP_PROG_OBJ] /* contrib/pageinspect/pageinspect--1.6--1.7.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- bt_metap()
--
DROP FUNCTION bt_metap(IN relname text,
    OUT magic int4,
    OUT version int4,
    OUT root int4,
    OUT level int4,
    OUT fastroot int4,
    OUT fastlevel int4)
;
