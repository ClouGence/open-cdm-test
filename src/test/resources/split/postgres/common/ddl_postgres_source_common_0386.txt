-- first partition has replica identity "a"
CREATE TABLE testpub_tbl8_0 PARTITION OF testpub_tbl8 FOR VALUES WITH (modulus 2, remainder 0)
;
DROP TABLE IF EXISTS replication_example
;
-- failed (not owner)
ALTER OPERATOR CLASS alt_opc2 USING hash SET SCHEMA alt_nsp2
;
CREATE PROCEDURE ptest6(a int, b anyelement)
LANGUAGE SQL
AS $$
SELECT NULL::int;
$$
;
DROP TABLE prt1_adv
;
CREATE SUBSCRIPTION tap_sub_B_C
	CONNECTION 'port=11383 host=/tmp/C6o4PTAnCK dbname=postgres application_name=tap_sub_B_C'
	PUBLICATION tap_pub_C
	WITH (origin = none)
;
-- test python -> hstore[]
CREATE FUNCTION test2arr() RETURNS hstore[]
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
val = [{'a': 1, 'b': 'boo', 'c': None}, {'d': 2}]
return val
$$
;
ALTER TABLE pg_temp_2.pg_temp_28230_2 ADD COLUMN newdata pg_temp_2.pg_temp_28230
;
CREATE OR REPLACE FUNCTION
  jsonb_path_query_first_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                         silent boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_query_first_tz'
;
ALTER TABLE attmp3 ADD CONSTRAINT b_greater_than_ten CHECK (b > 10)
;
ALTER TABLE prt2_adv ATTACH PARTITION prt2_adv_p3 DEFAULT
;
DROP TABLE mvtest_boxes CASCADE
;
drop function intercept_insert()
;
analyze rows
;
CREATE TABLE local_tbl (a text, b int)
;
/* Test case for bug #16242 */

-- We create a parent and child where the child has missing
-- non-null attribute values, and arrange to pass them through
-- tuple conversion from the child to the parent tupdesc
create table bar1 (a integer, b integer not null default 1)
  partition by range (a)
;
-- These ALTER TABLE variants will not recurse.
ALTER TABLE itest7 ALTER COLUMN a SET GENERATED BY DEFAULT
;
create aggregate my_sum(int4)
(
   stype = int4,
   sfunc = sum_transfn
)
;
CREATE TEMP TABLE tt2 ( tt2_id int4, joincol int4 )
;
create table inh_pp1 (f1 int)
;
DROP PUBLICATION tap_pub_temp1
;
VACUUM (FULL) pg_catalog.pg_event_trigger
;
create or replace function parted_trigfunc() returns trigger language plpgsql as $$
begin
  new.a = new.a + new.b;
  return new;
end;
$$
;
-- Not OK with equals and &&, and equals is not the partition key
create table idxpart (a int4range, b int4range, c int4range, exclude USING GIST (b with =, c with &&)) partition by range (a)
;
CREATE FUNCTION "public"."explain_filter_to_json"("text") RETURNS "jsonb"
    LANGUAGE "plpgsql"
    AS $_$
declare
    data text := '';
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just '0'
        ln := regexp_replace(ln, '\m\d+\M', '0', 'g');
        data := data || ln;
    end loop;
    return data::jsonb;
end;
$_$
;
ALTER INDEX pg_toast.pg_toast_33148_index RENAME TO reind_con_toast_idx
;
drop operator public.>^(int, int)
;
CREATE TABLE tab_core_types AS SELECT
  '(11,12)'::point,
  '(1,1),(2,2)'::line,
  '((11,11),(12,12))'::lseg,
  '((11,11),(13,13))'::box,
  '((11,12),(13,13),(14,14))'::path AS openedpath,
  '[(11,12),(13,13),(14,14)]'::path AS closedpath,
  '((11,12),(13,13),(14,14))'::polygon,
  '1,1,1'::circle,
  'today'::date,
  'now'::time,
  'now'::timestamp,
  'now'::timetz,
  'now'::timestamptz,
  '12 seconds'::interval,
  '{"reason":"because"}'::json,
  '{"when":"now"}'::jsonb,
  '$.a[*] ? (@ > 2)'::jsonpath,
  '127.0.0.1'::inet,
  '127.0.0.0/8'::cidr,
  '00:01:03:86:1c:ba'::macaddr8,
  '00:01:03:86:1c:ba'::macaddr,
  2::int2, 4::int4, 8::int8,
  4::float4, '8'::float8, pi()::numeric,
  'foo'::"char",
  'c'::bpchar,
  'abc'::varchar,
  'name'::name,
  'txt'::text,
  true::bool,
  E'\\xDEADBEEF'::bytea,
  B'10001'::bit,
  B'10001'::varbit AS varbit,
  '12.34'::money,
  'abc'::refcursor,
  '1 2'::int2vector,
  '1 2'::oidvector,
  format('%I=UC/%I', USER, USER)::aclitem AS aclitem,
  'a fat cat sat on a mat and ate a fat rat'::tsvector,
  'fat & rat'::tsquery,
  'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::uuid,
  'pg_class'::regclass,
  'regtype'::regtype,
  'pg_monitor'::regrole,
  'pg_class'::regclass::oid,
  '(1,1)'::tid, '2'::xid, '3'::cid,
  '10:20:10,14,15'::txid_snapshot,
  '16/B374D848'::pg_lsn,
  1::information_schema.cardinal_number,
  'l'::information_schema.character_data,
  'n'::information_schema.sql_identifier,
  'now'::information_schema.time_stamp,
  'YES'::information_schema.yes_or_no,
  'venus'::planets,
  'i16'::insenum,
  '(1,2)'::int4range,
  '(3,4)'::int8range,
  '(1,2)'::float8range,
  '(3,4)'::numrange,
  '(a,b)'::textrange,
  '(12.34, 56.78)'::cashrange,
  '(2020-01-02, 2021-02-03)'::daterange,
  '(2020-01-02 03:04:05, 2021-02-03 06:07:08)'::tsrange,
  '(2020-01-02 03:04:05, 2021-02-03 06:07:08)'::tstzrange,
  arrayrange(ARRAY[1,2], ARRAY[2,1])
;
-- fail

DROP VIEW fdv3
;
CREATE TABLE num_exp_ln (id int4, expected numeric(1000,800))
;
-- add a unique constraint (fails)
alter table atacc1 add constraint atacc_test1 unique (test1)
;
DROP TABLE IF EXISTS parted
;
-- ok
CREATE OR REPLACE TRANSFORM FOR hstore LANGUAGE plperl (TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
-- fail
TRUNCATE TABLE trunc_c,trunc_d,trunc_e
;
CREATE TRIGGER trig_d1_15 BEFORE UPDATE OR INSERT ON part_d_1_15
   FOR EACH ROW EXECUTE PROCEDURE func_parted_mod_b()
;
DROP SCHEMA testns3 CASCADE
;
do $$ declare a int[];
begin a := array[1,2]; a[2:3] := array[3,4]; raise notice 'a = %', a; end$$
;
ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ADD MAPPING FOR word, word WITH ispell
;
ALTER TABLE attmp3 ADD CONSTRAINT b_le_20 CHECK (b <= 20) NOT VALID
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_shdepend
;
create trigger aaa after insert on parted_trig_1 for each row execute procedure trigger_notice()
;
DROP RULE IF EXISTS test_rule_exists ON no_such_table
;
create table parted_trigger_3 (b text, a int) partition by range (length(b))
;
CREATE TABLE pgss_schema_1.tab_search_diff_1 (a int)
;
create function exit_block1() returns void as $$
begin
    <<begin_block1>>
    begin
        loop
            exit begin_block1;
            raise exception 'should not get here';
        end loop;
    end;
end;
$$ language plpgsql
;
-- incompatible with VACUUM FULL error
VACUUM (BUFFER_USAGE_LIMIT '512 kB', FULL) vac_option_tab
;
CREATE FUNCTION price_key_from_input(price_input) RETURNS price_key AS $$
    SELECT $1.id
$$ LANGUAGE SQL
;
-- fails
DROP INDEX clstrpart_only_idx
;
VACUUM pg_catalog.pg_ts_template
;
-- ok, skipped
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (lc_collate = ' ||
          quote_literal((SELECT datcollate FROM pg_database WHERE datname = current_database())) ||
          ', lc_ctype = ' ||
          quote_literal((SELECT datctype FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] -- first partition has replica identity "a"
CREATE TABLE testpub_tbl8_0 PARTITION OF testpub_tbl8 FOR VALUES WITH (modulus 2, remainder 0)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS replication_example
;
----------
[ALTER_PROG_OBJ] -- failed (not owner)
ALTER OPERATOR CLASS alt_opc2 USING hash SET SCHEMA alt_nsp2
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest6(a int, b anyelement)
LANGUAGE SQL
AS $$
SELECT NULL::int;
$$
;
----------
[DROP_TABLE] DROP TABLE prt1_adv
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_B_C
	CONNECTION 'port=11383 host=/tmp/C6o4PTAnCK dbname=postgres application_name=tap_sub_B_C'
	PUBLICATION tap_pub_C
	WITH (origin = none)
;
----------
[CREATE_PROG_OBJ] -- test python -> hstore[]
CREATE FUNCTION test2arr() RETURNS hstore[]
LANGUAGE plpython3u
TRANSFORM FOR TYPE hstore
AS $$
val = [{'a': 1, 'b': 'boo', 'c': None}, {'d': 2}]
return val
$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_2.pg_temp_28230_2 ADD COLUMN newdata pg_temp_2.pg_temp_28230
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  jsonb_path_query_first_tz(target jsonb, path jsonpath, vars jsonb DEFAULT '{}',
                         silent boolean DEFAULT false)
RETURNS jsonb
LANGUAGE INTERNAL
STRICT STABLE PARALLEL SAFE
AS 'jsonb_path_query_first_tz'
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp3 ADD CONSTRAINT b_greater_than_ten CHECK (b > 10)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE prt2_adv ATTACH PARTITION prt2_adv_p3 DEFAULT
;
----------
[DROP_TABLE] DROP TABLE mvtest_boxes CASCADE
;
----------
[DROP_PROG_OBJ] drop function intercept_insert()
;
----------
[ADMIN_TABLE] analyze rows
;
----------
[CREATE_TABLE] CREATE TABLE local_tbl (a text, b int)
;
----------
[CREATE_TABLE] /* Test case for bug #16242 */

-- We create a parent and child where the child has missing
-- non-null attribute values, and arrange to pass them through
-- tuple conversion from the child to the parent tupdesc
create table bar1 (a integer, b integer not null default 1)
  partition by range (a)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- These ALTER TABLE variants will not recurse.
ALTER TABLE itest7 ALTER COLUMN a SET GENERATED BY DEFAULT
;
----------
[CREATE_PROG_OBJ] create aggregate my_sum(int4)
(
   stype = int4,
   sfunc = sum_transfn
)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tt2 ( tt2_id int4, joincol int4 )
;
----------
[CREATE_TABLE] create table inh_pp1 (f1 int)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION tap_pub_temp1
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_event_trigger
;
----------
[CREATE_PROG_OBJ] create or replace function parted_trigfunc() returns trigger language plpgsql as $$
begin
  new.a = new.a + new.b;
  return new;
end;
$$
;
----------
[CREATE_TABLE] -- Not OK with equals and &&, and equals is not the partition key
create table idxpart (a int4range, b int4range, c int4range, exclude USING GIST (b with =, c with &&)) partition by range (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."explain_filter_to_json"("text") RETURNS "jsonb"
    LANGUAGE "plpgsql"
    AS $_$
declare
    data text := '';
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just '0'
        ln := regexp_replace(ln, '\m\d+\M', '0', 'g');
        data := data || ln;
    end loop;
    return data::jsonb;
end;
$_$
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33148_index RENAME TO reind_con_toast_idx
;
----------
[DROP_PROG_OBJ] drop operator public.>^(int, int)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tab_core_types AS SELECT
  '(11,12)'::point,
  '(1,1),(2,2)'::line,
  '((11,11),(12,12))'::lseg,
  '((11,11),(13,13))'::box,
  '((11,12),(13,13),(14,14))'::path AS openedpath,
  '[(11,12),(13,13),(14,14)]'::path AS closedpath,
  '((11,12),(13,13),(14,14))'::polygon,
  '1,1,1'::circle,
  'today'::date,
  'now'::time,
  'now'::timestamp,
  'now'::timetz,
  'now'::timestamptz,
  '12 seconds'::interval,
  '{"reason":"because"}'::json,
  '{"when":"now"}'::jsonb,
  '$.a[*] ? (@ > 2)'::jsonpath,
  '127.0.0.1'::inet,
  '127.0.0.0/8'::cidr,
  '00:01:03:86:1c:ba'::macaddr8,
  '00:01:03:86:1c:ba'::macaddr,
  2::int2, 4::int4, 8::int8,
  4::float4, '8'::float8, pi()::numeric,
  'foo'::"char",
  'c'::bpchar,
  'abc'::varchar,
  'name'::name,
  'txt'::text,
  true::bool,
  E'\\xDEADBEEF'::bytea,
  B'10001'::bit,
  B'10001'::varbit AS varbit,
  '12.34'::money,
  'abc'::refcursor,
  '1 2'::int2vector,
  '1 2'::oidvector,
  format('%I=UC/%I', USER, USER)::aclitem AS aclitem,
  'a fat cat sat on a mat and ate a fat rat'::tsvector,
  'fat & rat'::tsquery,
  'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'::uuid,
  'pg_class'::regclass,
  'regtype'::regtype,
  'pg_monitor'::regrole,
  'pg_class'::regclass::oid,
  '(1,1)'::tid, '2'::xid, '3'::cid,
  '10:20:10,14,15'::txid_snapshot,
  '16/B374D848'::pg_lsn,
  1::information_schema.cardinal_number,
  'l'::information_schema.character_data,
  'n'::information_schema.sql_identifier,
  'now'::information_schema.time_stamp,
  'YES'::information_schema.yes_or_no,
  'venus'::planets,
  'i16'::insenum,
  '(1,2)'::int4range,
  '(3,4)'::int8range,
  '(1,2)'::float8range,
  '(3,4)'::numrange,
  '(a,b)'::textrange,
  '(12.34, 56.78)'::cashrange,
  '(2020-01-02, 2021-02-03)'::daterange,
  '(2020-01-02 03:04:05, 2021-02-03 06:07:08)'::tsrange,
  '(2020-01-02 03:04:05, 2021-02-03 06:07:08)'::tstzrange,
  arrayrange(ARRAY[1,2], ARRAY[2,1])
;
----------
[DROP_VIEW] -- fail

DROP VIEW fdv3
;
----------
[CREATE_TABLE] CREATE TABLE num_exp_ln (id int4, expected numeric(1000,800))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- add a unique constraint (fails)
alter table atacc1 add constraint atacc_test1 unique (test1)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS parted
;
----------
[CREATE_TRANSFORM] -- ok
CREATE OR REPLACE TRANSFORM FOR hstore LANGUAGE plperl (TO SQL WITH FUNCTION plperl_to_hstore(internal))
;
----------
[TRUNCATE_TABLE] -- fail
TRUNCATE TABLE trunc_c,trunc_d,trunc_e
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER trig_d1_15 BEFORE UPDATE OR INSERT ON part_d_1_15
   FOR EACH ROW EXECUTE PROCEDURE func_parted_mod_b()
;
----------
[DROP_SCHEMA] DROP SCHEMA testns3 CASCADE
;
----------
[BLOCK] do $$ declare a int[];
begin a := array[1,2]; a[2:3] := array[3,4]; raise notice 'a = %', a; end$$
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION dummy_tst
  ADD MAPPING FOR word, word WITH ispell
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE attmp3 ADD CONSTRAINT b_le_20 CHECK (b <= 20) NOT VALID
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_shdepend
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger aaa after insert on parted_trig_1 for each row execute procedure trigger_notice()
;
----------
[DROP_POLICY] DROP RULE IF EXISTS test_rule_exists ON no_such_table
;
----------
[CREATE_TABLE] create table parted_trigger_3 (b text, a int) partition by range (length(b))
;
----------
[CREATE_TABLE] CREATE TABLE pgss_schema_1.tab_search_diff_1 (a int)
;
----------
[CREATE_PROG_OBJ] create function exit_block1() returns void as $$
begin
    <<begin_block1>>
    begin
        loop
            exit begin_block1;
            raise exception 'should not get here';
        end loop;
    end;
end;
$$ language plpgsql
;
----------
[ADMIN_TABLE] -- incompatible with VACUUM FULL error
VACUUM (BUFFER_USAGE_LIMIT '512 kB', FULL) vac_option_tab
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION price_key_from_input(price_input) RETURNS price_key AS $$
    SELECT $1.id
$$ LANGUAGE SQL
;
----------
[DROP_INDEX] -- fails
DROP INDEX clstrpart_only_idx
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_ts_template
;
----------
[BLOCK] -- ok, skipped
do $$
BEGIN
  EXECUTE 'CREATE COLLATION test1 (lc_collate = ' ||
          quote_literal((SELECT datcollate FROM pg_database WHERE datname = current_database())) ||
          ', lc_ctype = ' ||
          quote_literal((SELECT datctype FROM pg_database WHERE datname = current_database())) || ');';
END
$$
;
