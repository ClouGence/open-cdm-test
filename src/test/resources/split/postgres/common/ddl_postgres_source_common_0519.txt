CREATE TABLE prt1_e_p3 PARTITION OF prt1_e FOR VALUES FROM (500) TO (600)
;
CREATE MATERIALIZED VIEW sro_mv AS SELECT mv_action() WITH NO DATA
;
ANALYZE pg_temp_18.pg_temp_26637
;
analyze atacc1(a)
;
CREATE TABLE testpub_rf_tbl3 (e integer)
;
--
-- test non-strict join clause
--
CREATE TABLE src (a int, b text)
;
CREATE FUNCTION dblink_exec (text, text, boolean)
RETURNS text
AS '$libdir/dblink','dblink_exec'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
ALTER TABLE regtest_ptable_4 ALTER COLUMN y TYPE float
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_collation
;
DROP TABLE mcinthash
;
VACUUM (PARALLEL 2) pg_catalog.pg_shseclabel
;
-- not all of these would use index by default

CREATE INDEX text_idx on test__int using gist ( a gist__int_ops )
;
CREATE TEXT SEARCH TEMPLATE alt_ts_temp2 (lexize=dsimple_lexize)
;
CREATE VIEW pg_stat_wal AS
    SELECT
        w.wal_records,
        w.wal_fpi,
        w.wal_bytes,
        w.wal_buffers_full,
        w.wal_write,
        w.wal_sync,
        w.wal_write_time,
        w.wal_sync_time,
        w.stats_reset
    FROM pg_stat_get_wal() w
;
CREATE TRIGGER footrg_mod_a BEFORE UPDATE ON footrg1
   FOR EACH ROW EXECUTE PROCEDURE func_footrg_mod_a()
;
CREATE TABLE pagg_tab_p2 PARTITION OF pagg_tab FOR VALUES IN ('0004', '0005', '0006', '0007')
;
-- detach and re-attach multiple times just to ensure everything is kosher
ALTER TABLE parted_self_fk DETACH PARTITION part2_self_fk
;
alter table atacc1 alter test set not null
;
DROP TABLE tab1, tab2, tab3, tab4, tab5, tab6, tab7,
			   test_part, test_part_a, test_part_b, test_part_c, test_part_d
;
CALL p1(v_Text := v_Text, v_cnt := v_cnt)
;
CREATE FUNCTION "public"."ks_test_uniform_random"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
;
ALTER TABLE ONLY "public"."prt1_l_p2" ATTACH PARTITION "public"."prt1_l_p2_p1" FOR VALUES IN ('0000', '0001')
;
CREATE TABLE prt2_l_p2_p2 PARTITION OF prt2_l_p2 FOR VALUES IN ('0002', '0003')
;
-- failed (name conflict)
ALTER TEXT SEARCH DICTIONARY alt_ts_dict1 RENAME TO alt_ts_dict3
;
ALTER FUNCTION gbt_enum_penalty(internal, internal, internal) PARALLEL SAFE
;
ALTER TABLE ONLY "public"."emp" INHERIT "public"."person"
;
CREATE TABLE foo(f1 int, f2 text, f3 text[], primary key (f1,f2))
;
CREATE TABLE i_am_a_failure() USING i_do_not_exist_am
;
-- fail

drop table dtest
;
-- Test out-of-range fillfactor values
create index spgist_point_idx2 on spgist_point_tbl using spgist(p) with (fillfactor = 9)
;
CREATE OR REPLACE FUNCTION pg_create_physical_replication_slot(
    IN slot_name name, IN immediately_reserve boolean DEFAULT false,
    IN temporary boolean DEFAULT false,
    OUT slot_name name, OUT lsn pg_lsn)
RETURNS RECORD
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_create_physical_replication_slot'
;
DROP TABLE fk_partitioned_fk_full
;
-- should truncate everything
TRUNCATE TABLE truncprim CASCADE
;
create unique index on sl(a, b)
;
ANALYZE public.test_type_diff2_c3
;
CREATE OPERATOR ^? (
        LEFTARG = ltree,
	RIGHTARG = _lquery,
	PROCEDURE = lt_q_regex,
	COMMUTATOR = '^?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
CREATE FUNCTION hstore_to_json_loose(hstore)
RETURNS json
AS '$libdir/hstore', 'hstore_to_json_loose'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CALL split_prog.add_account(1, 'alice')
;
create table minmaxtest2() inherits (minmaxtest)
;
create table rlp3_default partition of rlp3 default
;
ALTER TEXT SEARCH CONFIGURATION indonesian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH indonesian_stem
;
CREATE TABLESPACE tblspc3 LOCATION '/tmp/6rkcUeuPpi/tempdir/superlongname_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
;
create rule rtest_order_r4 as on insert to rtest_order1
		where a < 100 do instead
	insert into rtest_order2 values (new.a, nextval('rtest_seq'),
		'rule 4 - this should run 4th')
;
-- invalid reference
CREATE TABLE gtest_err_3 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (c * 2) STORED)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=25797 host=/tmp/REnOPq3M6y dbname=postgres' PUBLICATION pub1
;
-- should fail, missing NOT NULL constraint
alter table inh_child2 inherit inh_child1
;
DROP PUBLICATION testpub_fortbl
;
create table lparted_by_int2 (a smallint) partition by list (a)
;
create function dfunc(text) returns text as $$
  select $1;
$$ language sql
;
CREATE FUNCTION ghstore_union(internal, internal)
RETURNS ghstore
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE prt1_e_p3 PARTITION OF prt1_e FOR VALUES FROM (500) TO (600)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW sro_mv AS SELECT mv_action() WITH NO DATA
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_18.pg_temp_26637
;
----------
[ADMIN_TABLE] analyze atacc1(a)
;
----------
[CREATE_TABLE] CREATE TABLE testpub_rf_tbl3 (e integer)
;
----------
[CREATE_TABLE] --
-- test non-strict join clause
--
CREATE TABLE src (a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_exec (text, text, boolean)
RETURNS text
AS '$libdir/dblink','dblink_exec'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE regtest_ptable_4 ALTER COLUMN y TYPE float
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_collation
;
----------
[DROP_TABLE] DROP TABLE mcinthash
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_shseclabel
;
----------
[ADD_INDEX] -- not all of these would use index by default

CREATE INDEX text_idx on test__int using gist ( a gist__int_ops )
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH TEMPLATE alt_ts_temp2 (lexize=dsimple_lexize)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_wal AS
    SELECT
        w.wal_records,
        w.wal_fpi,
        w.wal_bytes,
        w.wal_buffers_full,
        w.wal_write,
        w.wal_sync,
        w.wal_write_time,
        w.wal_sync_time,
        w.stats_reset
    FROM pg_stat_get_wal() w
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER footrg_mod_a BEFORE UPDATE ON footrg1
   FOR EACH ROW EXECUTE PROCEDURE func_footrg_mod_a()
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab_p2 PARTITION OF pagg_tab FOR VALUES IN ('0004', '0005', '0006', '0007')
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- detach and re-attach multiple times just to ensure everything is kosher
ALTER TABLE parted_self_fk DETACH PARTITION part2_self_fk
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table atacc1 alter test set not null
;
----------
[DROP_TABLE] DROP TABLE tab1, tab2, tab3, tab4, tab5, tab6, tab7,
			   test_part, test_part_a, test_part_b, test_part_c, test_part_d
;
----------
[CALL_PROG_OBJ] CALL p1(v_Text := v_Text, v_cnt := v_cnt)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ks_test_uniform_random"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs(i/n-r)) < c / sqrt(n) FROM indexed_samples
  );
  RETURN ok;
END
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."prt1_l_p2" ATTACH PARTITION "public"."prt1_l_p2_p1" FOR VALUES IN ('0000', '0001')
;
----------
[CREATE_TABLE] CREATE TABLE prt2_l_p2_p2 PARTITION OF prt2_l_p2 FOR VALUES IN ('0002', '0003')
;
----------
[ALTER_POLICY] -- failed (name conflict)
ALTER TEXT SEARCH DICTIONARY alt_ts_dict1 RENAME TO alt_ts_dict3
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_enum_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."emp" INHERIT "public"."person"
;
----------
[CREATE_TABLE] CREATE TABLE foo(f1 int, f2 text, f3 text[], primary key (f1,f2))
;
----------
[CREATE_TABLE] CREATE TABLE i_am_a_failure() USING i_do_not_exist_am
;
----------
[DROP_TABLE] -- fail

drop table dtest
;
----------
[ADD_INDEX] -- Test out-of-range fillfactor values
create index spgist_point_idx2 on spgist_point_tbl using spgist(p) with (fillfactor = 9)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION pg_create_physical_replication_slot(
    IN slot_name name, IN immediately_reserve boolean DEFAULT false,
    IN temporary boolean DEFAULT false,
    OUT slot_name name, OUT lsn pg_lsn)
RETURNS RECORD
LANGUAGE INTERNAL
STRICT VOLATILE
AS 'pg_create_physical_replication_slot'
;
----------
[DROP_TABLE] DROP TABLE fk_partitioned_fk_full
;
----------
[TRUNCATE_TABLE] -- should truncate everything
TRUNCATE TABLE truncprim CASCADE
;
----------
[ADD_INDEX] create unique index on sl(a, b)
;
----------
[ADMIN_TABLE] ANALYZE public.test_type_diff2_c3
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ^? (
        LEFTARG = ltree,
	RIGHTARG = _lquery,
	PROCEDURE = lt_q_regex,
	COMMUTATOR = '^?',
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION hstore_to_json_loose(hstore)
RETURNS json
AS '$libdir/hstore', 'hstore_to_json_loose'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CALL_PROG_OBJ] CALL split_prog.add_account(1, 'alice')
;
----------
[CREATE_TABLE] create table minmaxtest2() inherits (minmaxtest)
;
----------
[CREATE_TABLE] create table rlp3_default partition of rlp3 default
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION indonesian ADD MAPPING
    FOR asciiword, hword_asciipart, asciihword
	WITH indonesian_stem
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc3 LOCATION '/tmp/6rkcUeuPpi/tempdir/superlongname_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
;
----------
[CREATE_POLICY] create rule rtest_order_r4 as on insert to rtest_order1
		where a < 100 do instead
	insert into rtest_order2 values (new.a, nextval('rtest_seq'),
		'rule 4 - this should run 4th')
;
----------
[CREATE_TABLE] -- invalid reference
CREATE TABLE gtest_err_3 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (c * 2) STORED)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=25797 host=/tmp/REnOPq3M6y dbname=postgres' PUBLICATION pub1
;
----------
[ALTER_TABLE] -- should fail, missing NOT NULL constraint
alter table inh_child2 inherit inh_child1
;
----------
[DROP_PUB_SUB] DROP PUBLICATION testpub_fortbl
;
----------
[CREATE_TABLE] create table lparted_by_int2 (a smallint) partition by list (a)
;
----------
[CREATE_PROG_OBJ] create function dfunc(text) returns text as $$
  select $1;
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ghstore_union(internal, internal)
RETURNS ghstore
AS '$libdir/hstore'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
