-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4274', relminmxid = '1'
WHERE oid = '"public"."persons"'::pg_catalog.regclass
;
-- can be pushed down
DELETE FROM ft2 WHERE c1 % 10 = 5 RETURNING c1, c4
;
INSERT INTO num_exp_div VALUES (9,9,'1.00000000000000000000')
;
INSERT INTO public.ths_p1 VALUES (4, 64)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18644', relminmxid = '3'
WHERE oid = '"public"."cmmove3"'::pg_catalog.regclass
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10929', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtesty"'::pg_catalog.regclass
;
insert into WSlot values ('WS.105.3b', '105', '', '')
;
INSERT INTO public.tplain VALUES ('4', 144)
;
INSERT INTO shoelace_data VALUES ('sl3', 0, 'black', 35.0 , 'inch')
;
UPDATE pgbench_branches SET bbalance = bbalance + -4618 WHERE bid = 1
;
INSERT INTO public.tplain VALUES ('7', 147)
;
UPDATE rw_view2 SET a = -5 WHERE a = 5
;
INSERT INTO FKTABLE VALUES (2, NULL, 3, 3)
;
insert into tb values(1,1)
;
INSERT INTO pg_temp_13.pg_temp_25980_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_13.pg_temp_25980 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_13.pg_temp_25980 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
DELETE FROM ft2
  USING ft4 INNER JOIN ft5 ON (ft4.c1 === ft5.c1)
  WHERE ft2.c1 > 2000 AND ft2.c2 = ft4.c1
  RETURNING ft2.c1, ft2.c2, ft2.c3
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '772', relminmxid = '1'
WHERE oid = '"public"."ihighway"'::pg_catalog.regclass
;
UPDATE temporal_partitioned_fk_rng2rng SET valid_at = daterange('2000-01-01', '2000-02-13') WHERE id = '[2,3)'
;
insert into rtest_order1 values (1)
;
insert into range_parted values ('a')
;
INSERT INTO department VALUES (1, 0, 'A')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15861', relminmxid = '3'
WHERE oid = '"public"."undroppable_objs"'::pg_catalog.regclass
;
-- should fail
INSERT INTO rw_view14 (a, b) VALUES (3, 'Row 3')
;
INSERT INTO caster (boolean)       VALUES ('t'::text)
;
INSERT INTO test_part_b VALUES (7, 4)
;
INSERT INTO public.ths_p2 VALUES (3, 933)
;
INSERT INTO abbrev_abort_uuids (abort_increasing, abort_decreasing, noabort_increasing, noabort_decreasing)
    SELECT
        ('00000000-0000-0000-0000-'||to_char(g.i, '000000000000FM'))::uuid abort_increasing,
        ('00000000-0000-0000-0000-'||to_char(20000 - g.i, '000000000000FM'))::uuid abort_decreasing,
        (to_char(g.i % 10009, '00000000FM')||'-0000-0000-0000-'||to_char(g.i, '000000000000FM'))::uuid noabort_increasing,
        (to_char(((20000 - g.i) % 10009), '00000000FM')||'-0000-0000-0000-'||to_char(20000 - g.i, '000000000000FM'))::uuid noabort_decreasing
    FROM generate_series(0, 20000, 1) g(i)
;
INSERT INTO public.tht VALUES ('1', 811)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '20380', relminmxid = '3'
WHERE oid = '"public"."cmpart2"'::pg_catalog.regclass
;
INSERT INTO serialTest1 VALUES ('bar')
;
INSERT INTO vactst VALUES (1)
;
INSERT INTO public.tplain VALUES ('6', 746)
;
DELETE FROM foo WHERE a=1
;
WITH aa AS (SELECT 1 a, 2 b)
INSERT INTO withz VALUES(1, 'insert')
ON CONFLICT (k) DO UPDATE SET v = (SELECT b || ' update' FROM aa WHERE a = 1 LIMIT 1)
;
DELETE FROM people WHERE firstname = 'no-name'
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtest25"'::pg_catalog.regclass
;
INSERT INTO macaddr_data VALUES (5, '0800.2b01.0203')
;
DELETE FROM transition_table_level1
  WHERE level1_no BETWEEN 100000000 AND 100000010
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1091', relminmxid = '1'
WHERE oid = '"public"."num_exp_ln"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('2', 632)
;
INSERT INTO dont_forget VALUES (1)
;
INSERT INTO mcv_lists (a, b, c, d)
     SELECT
         NULL, 
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 'x' END),
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 0 END),
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 'x' END)
     FROM generate_series(1,5000) s(i)
;
-- Try to delete something that should set default
DELETE FROM PKTABLE where ptest1=2 and ptest2=3 and ptest3=4
;
insert into master values(1)
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt2_m_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
insert into pfk1 values (1)
;
INSERT INTO pxtest2 VALUES (3)
;
-- Try to update something that should not cascade
UPDATE PKTABLE set ptest1=1 WHERE ptest2=3
;
INSERT INTO replayed(val) SELECT coalesce(max(val),0) + 1 AS newval FROM replayed RETURNING val
;
INSERT INTO deferred_excl VALUES(1)
;
------------------------------------------------------------------------------------------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '4274', relminmxid = '1'
WHERE oid = '"public"."persons"'::pg_catalog.regclass
;
----------
[DELETE] -- can be pushed down
DELETE FROM ft2 WHERE c1 % 10 = 5 RETURNING c1, c4
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (9,9,'1.00000000000000000000')
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (4, 64)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18644', relminmxid = '3'
WHERE oid = '"public"."cmmove3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '10929', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtesty"'::pg_catalog.regclass
;
----------
[INSERT] insert into WSlot values ('WS.105.3b', '105', '', '')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 144)
;
----------
[INSERT] INSERT INTO shoelace_data VALUES ('sl3', 0, 'black', 35.0 , 'inch')
;
----------
[UPDATE] UPDATE pgbench_branches SET bbalance = bbalance + -4618 WHERE bid = 1
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 147)
;
----------
[UPDATE] UPDATE rw_view2 SET a = -5 WHERE a = 5
;
----------
[INSERT] INSERT INTO FKTABLE VALUES (2, NULL, 3, 3)
;
----------
[INSERT] insert into tb values(1,1)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_13.pg_temp_25980_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_13.pg_temp_25980 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_13.pg_temp_25980 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[DELETE] DELETE FROM ft2
  USING ft4 INNER JOIN ft5 ON (ft4.c1 === ft5.c1)
  WHERE ft2.c1 > 2000 AND ft2.c2 = ft4.c1
  RETURNING ft2.c1, ft2.c2, ft2.c3
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '772', relminmxid = '1'
WHERE oid = '"public"."ihighway"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE temporal_partitioned_fk_rng2rng SET valid_at = daterange('2000-01-01', '2000-02-13') WHERE id = '[2,3)'
;
----------
[INSERT] insert into rtest_order1 values (1)
;
----------
[INSERT] insert into range_parted values ('a')
;
----------
[INSERT] INSERT INTO department VALUES (1, 0, 'A')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15861', relminmxid = '3'
WHERE oid = '"public"."undroppable_objs"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- should fail
INSERT INTO rw_view14 (a, b) VALUES (3, 'Row 3')
;
----------
[INSERT|SELECT] INSERT INTO caster (boolean)       VALUES ('t'::text)
;
----------
[INSERT] INSERT INTO test_part_b VALUES (7, 4)
;
----------
[INSERT] INSERT INTO public.ths_p2 VALUES (3, 933)
;
----------
[INSERT|SELECT] INSERT INTO abbrev_abort_uuids (abort_increasing, abort_decreasing, noabort_increasing, noabort_decreasing)
    SELECT
        ('00000000-0000-0000-0000-'||to_char(g.i, '000000000000FM'))::uuid abort_increasing,
        ('00000000-0000-0000-0000-'||to_char(20000 - g.i, '000000000000FM'))::uuid abort_decreasing,
        (to_char(g.i % 10009, '00000000FM')||'-0000-0000-0000-'||to_char(g.i, '000000000000FM'))::uuid noabort_increasing,
        (to_char(((20000 - g.i) % 10009), '00000000FM')||'-0000-0000-0000-'||to_char(20000 - g.i, '000000000000FM'))::uuid noabort_decreasing
    FROM generate_series(0, 20000, 1) g(i)
;
----------
[INSERT] INSERT INTO public.tht VALUES ('1', 811)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '20380', relminmxid = '3'
WHERE oid = '"public"."cmpart2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO serialTest1 VALUES ('bar')
;
----------
[INSERT] INSERT INTO vactst VALUES (1)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('6', 746)
;
----------
[DELETE] DELETE FROM foo WHERE a=1
;
----------
[MERGE|SELECT] WITH aa AS (SELECT 1 a, 2 b)
INSERT INTO withz VALUES(1, 'insert')
ON CONFLICT (k) DO UPDATE SET v = (SELECT b || ' update' FROM aa WHERE a = 1 LIMIT 1)
;
----------
[DELETE] DELETE FROM people WHERE firstname = 'no-name'
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '8147', relminmxid = '1'
WHERE oid = '"generated_stored_tests"."gtest25"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO macaddr_data VALUES (5, '0800.2b01.0203')
;
----------
[DELETE] DELETE FROM transition_table_level1
  WHERE level1_no BETWEEN 100000000 AND 100000010
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '1091', relminmxid = '1'
WHERE oid = '"public"."num_exp_ln"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 632)
;
----------
[INSERT] INSERT INTO dont_forget VALUES (1)
;
----------
[INSERT|SELECT] INSERT INTO mcv_lists (a, b, c, d)
     SELECT
         NULL, 
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 'x' END),
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 0 END),
         (CASE WHEN mod(i,2) = 0 THEN NULL ELSE 'x' END)
     FROM generate_series(1,5000) s(i)
;
----------
[DELETE] -- Try to delete something that should set default
DELETE FROM PKTABLE where ptest1=2 and ptest2=3 and ptest3=4
;
----------
[INSERT] insert into master values(1)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."prt2_m_p3"'::pg_catalog.regclass
  AND attname IN ('a', 'b', 'c')
;
----------
[INSERT] insert into pfk1 values (1)
;
----------
[INSERT] INSERT INTO pxtest2 VALUES (3)
;
----------
[UPDATE] -- Try to update something that should not cascade
UPDATE PKTABLE set ptest1=1 WHERE ptest2=3
;
----------
[INSERT|SELECT] INSERT INTO replayed(val) SELECT coalesce(max(val),0) + 1 AS newval FROM replayed RETURNING val
;
----------
[INSERT] INSERT INTO deferred_excl VALUES(1)
;
