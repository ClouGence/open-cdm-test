--
-- Check argument name is used instead of $n in error message
--
CREATE FUNCTION fx(x WSlot) RETURNS void AS $$
BEGIN
  GET DIAGNOSTICS x = ROW_COUNT;
  RETURN;
END; $$ LANGUAGE plpgsql
;
-- Test that the WHEN clause is set properly to partitions
create table parted_trigger (a int, b text) partition by range (a)
;
create trigger donothingbrtrig2 before insert on donothingbrtrig_test2 for each row execute procedure donothingbrtrig_func()
;
ANALYZE pg_temp_24.pg_temp_26555
;
CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
ALTER TABLE pg_cast ADD UNIQUE USING INDEX pg_cast_source_target_index
;
DROP SEQUENCE ext_cine_seq
;
DROP AGGREGATE IF EXISTS foo(no_such_type)
;
-- WITH LOCAL CHECK OPTION with INSTEAD OF trigger on base view

CREATE TABLE base_tbl (a int, b int)
;
CREATE TEMP TABLE pg_temp_24.pg_temp_27148_2 (tid pg_catalog.tid)
;
-- fail

create function testoa(x1 int, x2 int, x3 int) returns orderedarray
language plpgsql as $$
declare res orderedarray;
begin
  res := array[x1, x2];
  res[2] := x3;
  return res;
end$$
;
ALTER TABLE pa_target ATTACH PARTITION part4 DEFAULT
;
create table trigpart1 partition of trigpart for values from (0) to (1000)
;
-- Expression/function may be part of an updatable view
CREATE VIEW rw_view16 AS SELECT a, b, a AS aa FROM base_tbl
;
create function sql_is_distinct_from(anyelement, anyelement)
returns boolean language sql
as 'select $1 is distinct from $2 limit 1'
;
DROP TABLE testpub_insert_onconfl_parted
;
ALTER TABLE regtest_table_part ENABLE  TRIGGER regtest_part_test_trig
;
ALTER TABLE tt3 OF tt_t0
;
CREATE FUNCTION public.verify_heapam(relation regclass,
											 on_error_stop boolean default false,
											 check_toast boolean default false,
											 skip text default 'none',
											 startblock bigint default null,
											 endblock bigint default null,
											 blkno OUT bigint,
											 offnum OUT integer,
											 attnum OUT integer,
											 msg OUT text)
		RETURNS SETOF record AS $$
		BEGIN
			RAISE EXCEPTION 'Invoked wrong verify_heapam!';
		END;
		$$ LANGUAGE plpgsql
;
create function least_accum(int8, int8) returns int8 language sql as
  'select least($1, $2)'
;
drop function get_first_user()
;
drop function rls_f(text)
;
VACUUM INT8_TBL
;
CREATE TABLE city_table (
    city_id        serial primary key,
    city_name    text not null,
    population    bigint,
    country_id    int references country_table
)
;
CREATE FUNCTION get_all_persons() RETURNS SETOF person_type
LANGUAGE SQL
AS $$
    SELECT * FROM persons;
$$
;
ALTER TABLE tbl_include_box add PRIMARY KEY USING INDEX tbl_include_box_idx_unique
;
drop function dump_update()
;
drop schema abc, ccc ,aac
;
create unique index ec1_expr2 on ec1((ff + 2 + 1))
;
DROP TRIGGER trigtest_before_row ON foreign_schema.foreign_table_1
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_am
;
DO $d$
    BEGIN
        EXECUTE $$CREATE SERVER loopback_nopw FOREIGN DATA WRAPPER postgres_fdw
            OPTIONS (dbname '$$||current_database()||$$',
                     port '$$||current_setting('port')||$$'
            )$$;
    END;
$d$
;
--
-- injection_points_run()
--
-- Executes the action attached to the injection point.
--
CREATE FUNCTION injection_points_run(IN point_name TEXT)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_run'
LANGUAGE C STRICT PARALLEL UNSAFE
;
ANALYZE pg_temp_11.pg_temp_25373_2
;
CREATE INDEX float4idx ON float4tmp USING gist ( a )
;
DROP TABLE compositetable
;
CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~" WITH TEMPLATE = template0 OID = 16386 ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
CREATE TABLE xacttest (a smallint, b real)
;
create function part_hashtext_length(value text, seed int8)
    returns int8 as $$
    select length(coalesce(value, ''))::int8
    $$ language sql strict immutable parallel safe
;
/* never fails with ICU */  DROP COLLATION testx
;
-- fail
create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "C") to ('g')
;
DROP VIEW rls_sbv
;
create domain domainvarchar varchar(5)
;
-- this should fail because f1 is not exposed for unqualified reference:
create rule rules_foorule as on insert to rules_foo where f1 < 100
do instead insert into rules_foo2 values (f1)
;
CREATE OR REPLACE FUNCTION
  json_populate_record(base anyelement, from_json json, use_json_as_text boolean DEFAULT false)
  RETURNS anyelement LANGUAGE internal STABLE AS 'json_populate_record' PARALLEL SAFE
;
create trigger trans_tab_parent_update_trig
  after update on trans_tab_parent referencing new table as new_table
  for each statement execute procedure dump_update_new()
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_aggregate
;
create table mlparted5_ab partition of mlparted5
  for values from ('a') to ('c') partition by list (c)
;
CREATE FUNCTION "public"."joinview_upd_trig_fn"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING WITH (new AS n) new.f1, n.f4 INTO new.f1, new.f4;  -- now ok
  RETURN NEW;
END;
$$
;
-- The gist_page_opaque_info() function prints the page's LSN.
-- Use an unlogged index, so that the LSN is predictable.
CREATE UNLOGGED TABLE test_gist AS SELECT point(i,i) p, i::text t FROM
    generate_series(1,1000) i
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] --
-- Check argument name is used instead of $n in error message
--
CREATE FUNCTION fx(x WSlot) RETURNS void AS $$
BEGIN
  GET DIAGNOSTICS x = ROW_COUNT;
  RETURN;
END; $$ LANGUAGE plpgsql
;
----------
[CREATE_TABLE] -- Test that the WHEN clause is set properly to partitions
create table parted_trigger (a int, b text) partition by range (a)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger donothingbrtrig2 before insert on donothingbrtrig_test2 for each row execute procedure donothingbrtrig_func()
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_24.pg_temp_26555
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
	PROCEDURE = isnne,
	LEFTARG = ismn13,
	RIGHTARG = ismn,
	COMMUTATOR = <>,
	NEGATOR = =,
	RESTRICT = neqsel,
	JOIN = neqjoinsel)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_cast ADD UNIQUE USING INDEX pg_cast_source_target_index
;
----------
[DROP_SEQUENCE] DROP SEQUENCE ext_cine_seq
;
----------
[DROP_PROG_OBJ] DROP AGGREGATE IF EXISTS foo(no_such_type)
;
----------
[CREATE_TABLE] -- WITH LOCAL CHECK OPTION with INSTEAD OF trigger on base view

CREATE TABLE base_tbl (a int, b int)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_24.pg_temp_27148_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] -- fail

create function testoa(x1 int, x2 int, x3 int) returns orderedarray
language plpgsql as $$
declare res orderedarray;
begin
  res := array[x1, x2];
  res[2] := x3;
  return res;
end$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE pa_target ATTACH PARTITION part4 DEFAULT
;
----------
[CREATE_TABLE] create table trigpart1 partition of trigpart for values from (0) to (1000)
;
----------
[CREATE_VIEW(SELECT)] -- Expression/function may be part of an updatable view
CREATE VIEW rw_view16 AS SELECT a, b, a AS aa FROM base_tbl
;
----------
[CREATE_PROG_OBJ] create function sql_is_distinct_from(anyelement, anyelement)
returns boolean language sql
as 'select $1 is distinct from $2 limit 1'
;
----------
[DROP_TABLE] DROP TABLE testpub_insert_onconfl_parted
;
----------
[ALTER_TABLE] ALTER TABLE regtest_table_part ENABLE  TRIGGER regtest_part_test_trig
;
----------
[ALTER_TABLE] ALTER TABLE tt3 OF tt_t0
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION public.verify_heapam(relation regclass,
											 on_error_stop boolean default false,
											 check_toast boolean default false,
											 skip text default 'none',
											 startblock bigint default null,
											 endblock bigint default null,
											 blkno OUT bigint,
											 offnum OUT integer,
											 attnum OUT integer,
											 msg OUT text)
		RETURNS SETOF record AS $$
		BEGIN
			RAISE EXCEPTION 'Invoked wrong verify_heapam!';
		END;
		$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] create function least_accum(int8, int8) returns int8 language sql as
  'select least($1, $2)'
;
----------
[DROP_PROG_OBJ] drop function get_first_user()
;
----------
[DROP_PROG_OBJ] drop function rls_f(text)
;
----------
[ADMIN_TABLE] VACUUM INT8_TBL
;
----------
[CREATE_TABLE] CREATE TABLE city_table (
    city_id        serial primary key,
    city_name    text not null,
    population    bigint,
    country_id    int references country_table
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION get_all_persons() RETURNS SETOF person_type
LANGUAGE SQL
AS $$
    SELECT * FROM persons;
$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tbl_include_box add PRIMARY KEY USING INDEX tbl_include_box_idx_unique
;
----------
[DROP_PROG_OBJ] drop function dump_update()
;
----------
[DROP_SCHEMA] drop schema abc, ccc ,aac
;
----------
[ADD_INDEX] create unique index ec1_expr2 on ec1((ff + 2 + 1))
;
----------
[DROP_TRIGGER] DROP TRIGGER trigtest_before_row ON foreign_schema.foreign_table_1
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_am
;
----------
[BLOCK] DO $d$
    BEGIN
        EXECUTE $$CREATE SERVER loopback_nopw FOREIGN DATA WRAPPER postgres_fdw
            OPTIONS (dbname '$$||current_database()||$$',
                     port '$$||current_setting('port')||$$'
            )$$;
    END;
$d$
;
----------
[CREATE_PROG_OBJ] --
-- injection_points_run()
--
-- Executes the action attached to the injection point.
--
CREATE FUNCTION injection_points_run(IN point_name TEXT)
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_run'
LANGUAGE C STRICT PARALLEL UNSAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_25373_2
;
----------
[ADD_INDEX] CREATE INDEX float4idx ON float4tmp USING gist ( a )
;
----------
[DROP_TABLE] DROP TABLE compositetable
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~" WITH TEMPLATE = template0 OID = 16386 ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[CREATE_TABLE] CREATE TABLE xacttest (a smallint, b real)
;
----------
[CREATE_PROG_OBJ] create function part_hashtext_length(value text, seed int8)
    returns int8 as $$
    select length(coalesce(value, ''))::int8
    $$ language sql strict immutable parallel safe
;
----------
[DROP_POLICY] /* never fails with ICU */  DROP COLLATION testx
;
----------
[CREATE_TABLE] -- fail
create table test_part_coll partition of test_part_coll_posix for values from ('a' collate "C") to ('g')
;
----------
[DROP_VIEW] DROP VIEW rls_sbv
;
----------
[CREATE_TYPE] create domain domainvarchar varchar(5)
;
----------
[CREATE_POLICY] -- this should fail because f1 is not exposed for unqualified reference:
create rule rules_foorule as on insert to rules_foo where f1 < 100
do instead insert into rules_foo2 values (f1)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION
  json_populate_record(base anyelement, from_json json, use_json_as_text boolean DEFAULT false)
  RETURNS anyelement LANGUAGE internal STABLE AS 'json_populate_record' PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger trans_tab_parent_update_trig
  after update on trans_tab_parent referencing new table as new_table
  for each statement execute procedure dump_update_new()
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_aggregate
;
----------
[CREATE_TABLE] create table mlparted5_ab partition of mlparted5
  for values from ('a') to ('c') partition by list (c)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."joinview_upd_trig_fn"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'UPDATE: % -> %', old, new;
  UPDATE foo SET f1 = new.f1, f3 = new.f3, f4 = new.f4 * 10
    FROM joinme WHERE f2 = f2j AND f2 = old.f2
    RETURNING WITH (new AS n) new.f1, n.f4 INTO new.f1, new.f4;  -- now ok
  RETURN NEW;
END;
$$
;
----------
[CREATE_TABLE|SELECT] -- The gist_page_opaque_info() function prints the page's LSN.
-- Use an unlogged index, so that the LSN is predictable.
CREATE UNLOGGED TABLE test_gist AS SELECT point(i,i) p, i::text t FROM
    generate_series(1,1000) i
;
