create table rtest_view4 (a int4, b text, c int4)
;
DROP SCHEMA fkpart7 CASCADE
;
ALTER TYPE bogon ADD VALUE 'bad'
;
ALTER FUNCTION functest_F_1(int) IMMUTABLE
;
-- Don't support WHEN clauses with INSTEAD OF triggers
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_view
FOR EACH ROW WHEN (OLD.a <> NEW.a) EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
ALTER TABLE rls_test_both ENABLE ROW LEVEL SECURITY
;
alter table mutable drop column f3
;
CREATE TEMP TABLE products (product_id int, name text, price numeric)
;
VACUUM (ANALYZE) pg_catalog.pg_extension
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_largeobject
;
ANALYZE prt2_n
;
drop table My_Table
;
CREATE SEQUENCE split_sequence_next.seq_full AS integer INCREMENT BY 2 MINVALUE 1 MAXVALUE 99 START WITH 5 CACHE 3 CYCLE OWNED BY split_sequence_next.owner_table.id
;
ALTER INDEX pg_toast.pg_toast_33600_index RENAME TO reind_con_toast_idx
;
CREATE TABLE pagg_tab3_p1 PARTITION OF pagg_tab3 FOR VALUES IN ('a', 'b')
;
/*
 * A few supporting functions first ...
 */

/* Expand any 1-D array into a set with integers 1..N */
CREATE FUNCTION _pg_expandarray(IN anyarray, OUT x anyelement, OUT n int)
    RETURNS SETOF RECORD
    LANGUAGE sql STRICT IMMUTABLE PARALLEL SAFE
    AS 'select $1[s],
        s operator(pg_catalog.-) pg_catalog.array_lower($1,1) operator(pg_catalog.+) 1
        from pg_catalog.generate_series(pg_catalog.array_lower($1,1),
                                        pg_catalog.array_upper($1,1),
                                        1) as g(s)'
;
-- 100 distinct combinations with NULL values, all in the MCV list
TRUNCATE mcv_lists
;
CREATE RULE "rtest_emp_del" AS
    ON DELETE TO "public"."rtest_emp" DO  INSERT INTO "public"."rtest_emplog" ("ename", "who", "action", "newsal", "oldsal")
  VALUES ("old"."ename", CURRENT_USER, 'fired'::"bpchar", '$0.00'::"money", "old"."salary")
;
ALTER FOREIGN TABLE ft1 ADD COLUMN c6 integer
;
CREATE COLLATION split_obj_priv.c_locale (locale = 'C')
;
ALTER FUNCTION gbt_date_fetch(internal) PARALLEL SAFE
;
ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c5 SERIAL CHECK (c5 > 8)
;
CREATE OPERATOR @!= (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
drop table convslot_test_child, convslot_test_parent
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '.')
;
CREATE TEMP TABLE arrtest_i (f0 int, f1 text, f2 int)
;
ALTER TABLE droppk2 DETACH PARTITION droppk2_d
;
create unique index cnn_uq_idx on cnn_uq (a)
;
ALTER TABLE testschema.part SET TABLESPACE pg_default
;
VACUUM FULL will_get_rewritten
;
ALTER TABLE part_document ENABLE ROW LEVEL SECURITY
;
create table cnullchild (check (f1 = 1 or f1 = null)) inherits(cnullparent)
;
create table idxpart1 partition of idxpart for values from (1, 1) to (10, 10)
;
create domain ddef1 int4 DEFAULT 3
;
CREATE TEXT SEARCH CONFIGURATION "public"."synonym_tst" (
    PARSER = "pg_catalog"."default" )
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=18417 host=/tmp/jotseMAL4u dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
CREATE TEMP TABLE pg_temp_11.pg_temp_41180_2 (tid pg_catalog.tid)
;
create table covidxpart2 partition of covidxpart for values in (2)
;
create function explain_filter(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just 'N'
        ln := regexp_replace(ln, '\m\d+\M', 'N', 'g');
        -- In sort output, the above won't match units-suffixed numbers
        ln := regexp_replace(ln, '\m\d+kB', 'NkB', 'g');
        -- Ignore text-mode buffers output because it varies depending
        -- on the system state
        CONTINUE WHEN (ln ~ ' +Buffers: .*');
        -- Ignore text-mode "Planning:" line because whether it's output
        -- varies depending on the system state
        CONTINUE WHEN (ln = 'Planning:');
        return next ln;
    end loop;
end;
$$
;
create trigger p after insert on middle for each statement execute function f()
;
-- Add operators and support functions that are new in 9.1.  We do it like
-- this, leaving them "loose" in the operator family rather than bound into
-- the gist_trgm_ops opclass, because that's the only state that can be
-- reproduced during an upgrade from 9.0 (see pg_trgm--unpackaged--1.0.sql).

ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        2       <-> (text, text) FOR ORDER BY pg_catalog.float_ops,
        OPERATOR        3       pg_catalog.~~ (text, text),
        OPERATOR        4       pg_catalog.~~* (text, text),
        FUNCTION        8 (text, text)  gtrgm_distance (internal, text, smallint, oid, internal)
;
CREATE EXTENSION ltree
;
CREATE FUNCTION isnge(isbn, isbn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- fail
TRUNCATE TABLE truncate_a,trunc_b
;
-- check storage of generated columns
-- stored
create table test9s (a int not null, b int generated always as (a * 2) stored)
;
VACUUM (SKIP_LOCKED) pg_catalog.pg_extension
;
CREATE FUNCTION ghstore_out(ghstore)
RETURNS cstring
AS '$libdir/hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
create table idxpart (i int) partition by hash (i)
;
ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        11       pg_catalog.= (text, text)
;
-- fails

CREATE VIEW key_dependent_view_no_cols AS
   SELECT FROM view_base_table GROUP BY key HAVING length(data) > 0
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table rtest_view4 (a int4, b text, c int4)
;
----------
[DROP_SCHEMA] DROP SCHEMA fkpart7 CASCADE
;
----------
[ALTER_TYPE] ALTER TYPE bogon ADD VALUE 'bad'
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION functest_F_1(int) IMMUTABLE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Don't support WHEN clauses with INSTEAD OF triggers
CREATE TRIGGER invalid_trig INSTEAD OF UPDATE ON main_view
FOR EACH ROW WHEN (OLD.a <> NEW.a) EXECUTE PROCEDURE view_trigger('instead_of_upd')
;
----------
[ALTER_TABLE] ALTER TABLE rls_test_both ENABLE ROW LEVEL SECURITY
;
----------
[ALTER_TABLE|DROP_COLUMN] alter table mutable drop column f3
;
----------
[CREATE_TABLE] CREATE TEMP TABLE products (product_id int, name text, price numeric)
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_extension
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_largeobject
;
----------
[ADMIN_TABLE] ANALYZE prt2_n
;
----------
[DROP_TABLE] drop table My_Table
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE split_sequence_next.seq_full AS integer INCREMENT BY 2 MINVALUE 1 MAXVALUE 99 START WITH 5 CACHE 3 CYCLE OWNED BY split_sequence_next.owner_table.id
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33600_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE pagg_tab3_p1 PARTITION OF pagg_tab3 FOR VALUES IN ('a', 'b')
;
----------
[CREATE_PROG_OBJ] /*
 * A few supporting functions first ...
 */

/* Expand any 1-D array into a set with integers 1..N */
CREATE FUNCTION _pg_expandarray(IN anyarray, OUT x anyelement, OUT n int)
    RETURNS SETOF RECORD
    LANGUAGE sql STRICT IMMUTABLE PARALLEL SAFE
    AS 'select $1[s],
        s operator(pg_catalog.-) pg_catalog.array_lower($1,1) operator(pg_catalog.+) 1
        from pg_catalog.generate_series(pg_catalog.array_lower($1,1),
                                        pg_catalog.array_upper($1,1),
                                        1) as g(s)'
;
----------
[TRUNCATE_TABLE] -- 100 distinct combinations with NULL values, all in the MCV list
TRUNCATE mcv_lists
;
----------
[CREATE_POLICY] CREATE RULE "rtest_emp_del" AS
    ON DELETE TO "public"."rtest_emp" DO  INSERT INTO "public"."rtest_emplog" ("ename", "who", "action", "newsal", "oldsal")
  VALUES ("old"."ename", CURRENT_USER, 'fired'::"bpchar", '$0.00'::"money", "old"."salary")
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD COLUMN c6 integer
;
----------
[CREATE_POLICY] CREATE COLLATION split_obj_priv.c_locale (locale = 'C')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_date_fetch(internal) PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE test_add_column
	ADD COLUMN IF NOT EXISTS c5 SERIAL CHECK (c5 > 8)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @!= (
    LEFTARG = boolean,
    RIGHTARG = real,
    PROCEDURE = alter_op_test_fn_bool_real
)
;
----------
[DROP_TABLE] drop table convslot_test_child, convslot_test_parent
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', delimiter '.')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE arrtest_i (f0 int, f1 text, f2 int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE droppk2 DETACH PARTITION droppk2_d
;
----------
[ADD_INDEX] create unique index cnn_uq_idx on cnn_uq (a)
;
----------
[ALTER_TABLE] ALTER TABLE testschema.part SET TABLESPACE pg_default
;
----------
[ADMIN_TABLE] VACUUM FULL will_get_rewritten
;
----------
[ALTER_TABLE] ALTER TABLE part_document ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] create table cnullchild (check (f1 = 1 or f1 = null)) inherits(cnullparent)
;
----------
[CREATE_TABLE] create table idxpart1 partition of idxpart for values from (1, 1) to (10, 10)
;
----------
[CREATE_TYPE] create domain ddef1 int4 DEFAULT 3
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION "public"."synonym_tst" (
    PARSER = "pg_catalog"."default" )
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=18417 host=/tmp/jotseMAL4u dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_41180_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] create table covidxpart2 partition of covidxpart for values in (2)
;
----------
[CREATE_PROG_OBJ] create function explain_filter(text) returns setof text
language plpgsql as
$$
declare
    ln text;
begin
    for ln in execute $1
    loop
        -- Replace any numeric word with just 'N'
        ln := regexp_replace(ln, '\m\d+\M', 'N', 'g');
        -- In sort output, the above won't match units-suffixed numbers
        ln := regexp_replace(ln, '\m\d+kB', 'NkB', 'g');
        -- Ignore text-mode buffers output because it varies depending
        -- on the system state
        CONTINUE WHEN (ln ~ ' +Buffers: .*');
        -- Ignore text-mode "Planning:" line because whether it's output
        -- varies depending on the system state
        CONTINUE WHEN (ln = 'Planning:');
        return next ln;
    end loop;
end;
$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger p after insert on middle for each statement execute function f()
;
----------
[ALTER_PROG_OBJ] -- Add operators and support functions that are new in 9.1.  We do it like
-- this, leaving them "loose" in the operator family rather than bound into
-- the gist_trgm_ops opclass, because that's the only state that can be
-- reproduced during an upgrade from 9.0 (see pg_trgm--unpackaged--1.0.sql).

ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        2       <-> (text, text) FOR ORDER BY pg_catalog.float_ops,
        OPERATOR        3       pg_catalog.~~ (text, text),
        OPERATOR        4       pg_catalog.~~* (text, text),
        FUNCTION        8 (text, text)  gtrgm_distance (internal, text, smallint, oid, internal)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION ltree
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnge(isbn, isbn)
	RETURNS boolean
	AS 'int8ge'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[TRUNCATE_TABLE] -- fail
TRUNCATE TABLE truncate_a,trunc_b
;
----------
[CREATE_TABLE] -- check storage of generated columns
-- stored
create table test9s (a int not null, b int generated always as (a * 2) stored)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_extension
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ghstore_out(ghstore)
RETURNS cstring
AS '$libdir/hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] create table idxpart (i int) partition by hash (i)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gin_trgm_ops USING gin ADD
        OPERATOR        11       pg_catalog.= (text, text)
;
----------
[CREATE_VIEW(SELECT)] -- fails

CREATE VIEW key_dependent_view_no_cols AS
   SELECT FROM view_base_table GROUP BY key HAVING length(data) > 0
;
