CREATE FUNCTION gf1(a int) RETURNS int AS $$ SELECT a * 3 $$ IMMUTABLE LANGUAGE SQL
;
DROP RULE joinview_u ON joinview
;
-- fails

ALTER TABLE attmp ADD COLUMN a int4 default 3
;
CREATE AGGREGATE "public"."build_group"("anyelement", integer) (
    SFUNC = "public"."add_group",
    STYPE = "anyarray"
)
;
ALTER FOREIGN TABLE foreign_schema.ft1 SET TABLESPACE ts
;
drop function dump_delete()
;
CREATE UNIQUE INDEX idx_temporal3_uq ON temporal3 (id, valid_at)
;
DROP TABLE pg_temp_13.pg_temp_25047_2, pg_temp_13.pg_temp_25047
;
-- check individual constraints with alter table.

-- should fail

-- varchar does not promote to real
ALTER TABLE fktable ADD CONSTRAINT fk_2_3
FOREIGN KEY (x2) REFERENCES pktable(id3)
;
DROP TABLE pg_temp_9.pg_temp_26555_2, pg_temp_9.pg_temp_26555
;
COMMENT ON VIEW toyemp IS 'is a view'
;
create table tt10 (x int, z int)
;
CREATE FUNCTION gbt_time_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ALTER TABLE pg_toast.pg_toast_30556 RENAME TO reind_con_toast
;
ALTER TABLE test_oat_schema.test_oat_tab DISABLE RULE test_oat_notify
;
create function simplecaller() returns int language plpgsql
as $$
declare
  sum int := 0;
begin
  for n in 1..10 loop
    sum := sum + simplesql(n);
    if n = 5 then
      create or replace function simplesql(int) returns int language sql
      as 'select $1 + 100';
    end if;
  end loop;
  return sum;
end$$
;
COMMENT ON FUNCTION _int_contained(_int4, _int4) IS 'contained in'
;
/*
 * 5.78
 * VIEW_PERIOD_USAGE
 */

-- feature not supported


/*
 * 5.79
 * VIEW_ROUTINE_USAGE
 */

CREATE VIEW view_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nv.nspname AS sql_identifier) AS table_schema,
           CAST(v.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name

    FROM pg_namespace nv, pg_class v, pg_depend dv,
         pg_depend dp, pg_proc p, pg_namespace np

    WHERE nv.oid = v.relnamespace
          AND v.relkind = 'v'
          AND v.oid = dv.refobjid
          AND dv.refclassid = 'pg_catalog.pg_class'::regclass
          AND dv.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dv.deptype = 'i'
          AND dv.objid = dp.objid
          AND dp.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dp.refclassid = 'pg_catalog.pg_proc'::regclass
          AND dp.refobjid = p.oid
          AND p.pronamespace = np.oid
          AND pg_has_role(p.proowner, 'USAGE')
;
CREATE TABLE tab_ins AS SELECT generate_series(1,1002) AS a
;
CREATE TABLE "public"."tt0" (
    "x" integer NOT NULL,
    "y" numeric(8,2)
)
;
/*
 * check whether we handle updates/deletes correct with & without a pkey
 */

/* we should handle the case without a key at all more gracefully */
CREATE TABLE table_without_key(id serial, data int)
;
CREATE INDEX bitidx ON bittmp USING GIST ( a )
;
ALTER EXTENSION seg DROP function gseg_decompress(pg_catalog.internal)
;
create table rtest_emplog (ename char(20), who name, action char(10), newsal numeric, oldsal numeric)
;
ALTER TYPE planets ADD VALUE 'neptune' AFTER 'uranus'
;
create function polyf(anyrange) returns anymultirange
as 'select multirange($1);' language sql
;
alter table nv_parent add check (d between '2001-01-01'::date and '2099-12-31'::date) not valid
;
CREATE TABLE timetztmp (a timetz)
;
DROP TABLE test_ex_constraints_inh
;
-- the following commands should see search_path as pg_catalog, pg_temp

CREATE INDEX test_maint_idx ON test_maint_search_path.test_maint (fn(i))
;
CREATE FUNCTION handle_get_release()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
-- fail
CREATE TABLE itest15 (id integer NULL GENERATED ALWAYS AS IDENTITY)
;
ALTER FUNCTION gbt_int8_compress(internal) PARALLEL SAFE
;
ALTER INDEX pg_toast.pg_toast_33427_index RENAME TO reind_con_toast_idx
;
CREATE TABLE thethings (stuff things)
;
CREATE FUNCTION isnlt(issn, issn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- test COPY FROM with foreign table created in the same transaction
create table loc3 (f1 int, f2 text)
;
ANALYZE pg_temp_13.pg_temp_26923_2
;
CREATE TABLE temptest(col int) ON COMMIT DELETE ROWS
;
-- ok
DROP MATERIALIZED VIEW tid_matview
;
CREATE SCHEMA "Bar"
;
/*
 * text search configuration for danish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * danish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY danish_stem
	(TEMPLATE = snowball, Language = danish , StopWords=danish)
;
CREATE FUNCTION _int_contained_sel(internal, oid, internal, integer)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
DROP PUBLICATION IF EXISTS split_pub_renamed
;
-- Ensure stats are dropped sanely, and test IF NOT EXISTS while at it
CREATE TABLE ab1 (a INTEGER, b INTEGER, c INTEGER)
;
CREATE INDEX fickleidx ON int4tbl USING btree (i int4_fickle_ops)
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_seclabel
;
CREATE VIEW tables AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,

           CAST(
             CASE WHEN nc.oid = pg_my_temp_schema() THEN 'LOCAL TEMPORARY'
                  WHEN c.relkind IN ('r', 'p') THEN 'BASE TABLE'
                  WHEN c.relkind = 'v' THEN 'VIEW'
                  WHEN c.relkind = 'f' THEN 'FOREIGN'
                  ELSE null END
             AS character_data) AS table_type,

           CAST(null AS sql_identifier) AS self_referencing_column_name,
           CAST(null AS character_data) AS reference_generation,

           CAST(CASE WHEN t.typname IS NOT NULL THEN current_database() ELSE null END AS sql_identifier) AS user_defined_type_catalog,
           CAST(nt.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(t.typname AS sql_identifier) AS user_defined_type_name,

           CAST(CASE WHEN c.relkind IN ('r', 'p') OR
                          (c.relkind IN ('v', 'f') AND
                           -- 1 << CMD_INSERT
                           pg_relation_is_updatable(c.oid, false) & 8 = 8)
                THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_insertable_into,

           CAST(CASE WHEN t.typname IS NOT NULL THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_typed,
           CAST(null AS character_data) AS commit_action

    FROM pg_namespace nc JOIN pg_class c ON (nc.oid = c.relnamespace)
           LEFT JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON (c.reloftype = t.oid)

    WHERE c.relkind IN ('r', 'v', 'f', 'p')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_table_privilege(c.oid, 'SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(c.oid, 'SELECT, INSERT, UPDATE, REFERENCES') )
;
SECURITY LABEL ON FUNCTION f3()
    IS 'system_u:object_r:sepgsql_trusted_proc_exec_t:s0'
;
CREATE VIEW table_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.prtype AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, c.relowner, 'USAGE')
                  OR c.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable,
           CAST(CASE WHEN c.prtype = 'SELECT' THEN 'YES' ELSE 'NO' END AS yes_or_no) AS with_hierarchy

    FROM (
            SELECT oid, relname, relnamespace, relkind, relowner, (aclexplode(coalesce(relacl, acldefault('r', relowner)))).* FROM pg_class
         ) AS c (oid, relname, relnamespace, relkind, relowner, grantor, grantee, prtype, grantable),
         pg_namespace nc,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE c.relnamespace = nc.oid
          AND c.relkind IN ('r', 'v', 'f', 'p')
          AND c.grantee = grantee.oid
          AND c.grantor = u_grantor.oid
          AND c.prtype IN ('INSERT', 'SELECT', 'UPDATE', 'DELETE', 'TRUNCATE', 'REFERENCES', 'TRIGGER')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION gf1(a int) RETURNS int AS $$ SELECT a * 3 $$ IMMUTABLE LANGUAGE SQL
;
----------
[DROP_POLICY] DROP RULE joinview_u ON joinview
;
----------
[ALTER_TABLE|ADD_COLUMN] -- fails

ALTER TABLE attmp ADD COLUMN a int4 default 3
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."build_group"("anyelement", integer) (
    SFUNC = "public"."add_group",
    STYPE = "anyarray"
)
;
----------
[ALTER_TABLE] ALTER FOREIGN TABLE foreign_schema.ft1 SET TABLESPACE ts
;
----------
[DROP_PROG_OBJ] drop function dump_delete()
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX idx_temporal3_uq ON temporal3 (id, valid_at)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_13.pg_temp_25047_2, pg_temp_13.pg_temp_25047
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- check individual constraints with alter table.

-- should fail

-- varchar does not promote to real
ALTER TABLE fktable ADD CONSTRAINT fk_2_3
FOREIGN KEY (x2) REFERENCES pktable(id3)
;
----------
[DROP_TABLE] DROP TABLE pg_temp_9.pg_temp_26555_2, pg_temp_9.pg_temp_26555
;
----------
[COMMENT_VIEW] COMMENT ON VIEW toyemp IS 'is a view'
;
----------
[CREATE_TABLE] create table tt10 (x int, z int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_time_same(gbtreekey16, gbtreekey16, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30556 RENAME TO reind_con_toast
;
----------
[ALTER_TABLE] ALTER TABLE test_oat_schema.test_oat_tab DISABLE RULE test_oat_notify
;
----------
[CREATE_PROG_OBJ] create function simplecaller() returns int language plpgsql
as $$
declare
  sum int := 0;
begin
  for n in 1..10 loop
    sum := sum + simplesql(n);
    if n = 5 then
      create or replace function simplesql(int) returns int language sql
      as 'select $1 + 100';
    end if;
  end loop;
  return sum;
end$$
;
----------
[COMMENT_PROG_OBJ] COMMENT ON FUNCTION _int_contained(_int4, _int4) IS 'contained in'
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.78
 * VIEW_PERIOD_USAGE
 */

-- feature not supported


/*
 * 5.79
 * VIEW_ROUTINE_USAGE
 */

CREATE VIEW view_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nv.nspname AS sql_identifier) AS table_schema,
           CAST(v.relname AS sql_identifier) AS table_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name

    FROM pg_namespace nv, pg_class v, pg_depend dv,
         pg_depend dp, pg_proc p, pg_namespace np

    WHERE nv.oid = v.relnamespace
          AND v.relkind = 'v'
          AND v.oid = dv.refobjid
          AND dv.refclassid = 'pg_catalog.pg_class'::regclass
          AND dv.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dv.deptype = 'i'
          AND dv.objid = dp.objid
          AND dp.classid = 'pg_catalog.pg_rewrite'::regclass
          AND dp.refclassid = 'pg_catalog.pg_proc'::regclass
          AND dp.refobjid = p.oid
          AND p.pronamespace = np.oid
          AND pg_has_role(p.proowner, 'USAGE')
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE tab_ins AS SELECT generate_series(1,1002) AS a
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tt0" (
    "x" integer NOT NULL,
    "y" numeric(8,2)
)
;
----------
[CREATE_TABLE] /*
 * check whether we handle updates/deletes correct with & without a pkey
 */

/* we should handle the case without a key at all more gracefully */
CREATE TABLE table_without_key(id serial, data int)
;
----------
[ADD_INDEX] CREATE INDEX bitidx ON bittmp USING GIST ( a )
;
----------
[ALTER_LIBRARY] ALTER EXTENSION seg DROP function gseg_decompress(pg_catalog.internal)
;
----------
[CREATE_TABLE] create table rtest_emplog (ename char(20), who name, action char(10), newsal numeric, oldsal numeric)
;
----------
[ALTER_TYPE] ALTER TYPE planets ADD VALUE 'neptune' AFTER 'uranus'
;
----------
[CREATE_PROG_OBJ] create function polyf(anyrange) returns anymultirange
as 'select multirange($1);' language sql
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table nv_parent add check (d between '2001-01-01'::date and '2099-12-31'::date) not valid
;
----------
[CREATE_TABLE] CREATE TABLE timetztmp (a timetz)
;
----------
[DROP_TABLE] DROP TABLE test_ex_constraints_inh
;
----------
[ADD_INDEX] -- the following commands should see search_path as pg_catalog, pg_temp

CREATE INDEX test_maint_idx ON test_maint_search_path.test_maint (fn(i))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION handle_get_release()
RETURNS pg_catalog.void STRICT
AS '$libdir/test_aio' LANGUAGE C
;
----------
[CREATE_TABLE] -- fail
CREATE TABLE itest15 (id integer NULL GENERATED ALWAYS AS IDENTITY)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_int8_compress(internal) PARALLEL SAFE
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33427_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE thethings (stuff things)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(issn, issn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_TABLE] -- test COPY FROM with foreign table created in the same transaction
create table loc3 (f1 int, f2 text)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26923_2
;
----------
[CREATE_TABLE] CREATE TABLE temptest(col int) ON COMMIT DELETE ROWS
;
----------
[DROP_VIEW] -- ok
DROP MATERIALIZED VIEW tid_matview
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "Bar"
;
----------
[CREATE_POLICY] /*
 * text search configuration for danish language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * danish and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY danish_stem
	(TEMPLATE = snowball, Language = danish , StopWords=danish)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _int_contained_sel(internal, oid, internal, integer)
RETURNS float8
AS '$libdir/_int'
LANGUAGE C STRICT STABLE PARALLEL SAFE
;
----------
[DROP_PUB_SUB] DROP PUBLICATION IF EXISTS split_pub_renamed
;
----------
[CREATE_TABLE] -- Ensure stats are dropped sanely, and test IF NOT EXISTS while at it
CREATE TABLE ab1 (a INTEGER, b INTEGER, c INTEGER)
;
----------
[ADD_INDEX] CREATE INDEX fickleidx ON int4tbl USING btree (i int4_fickle_ops)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_seclabel
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW tables AS
    SELECT CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,

           CAST(
             CASE WHEN nc.oid = pg_my_temp_schema() THEN 'LOCAL TEMPORARY'
                  WHEN c.relkind IN ('r', 'p') THEN 'BASE TABLE'
                  WHEN c.relkind = 'v' THEN 'VIEW'
                  WHEN c.relkind = 'f' THEN 'FOREIGN'
                  ELSE null END
             AS character_data) AS table_type,

           CAST(null AS sql_identifier) AS self_referencing_column_name,
           CAST(null AS character_data) AS reference_generation,

           CAST(CASE WHEN t.typname IS NOT NULL THEN current_database() ELSE null END AS sql_identifier) AS user_defined_type_catalog,
           CAST(nt.nspname AS sql_identifier) AS user_defined_type_schema,
           CAST(t.typname AS sql_identifier) AS user_defined_type_name,

           CAST(CASE WHEN c.relkind IN ('r', 'p') OR
                          (c.relkind IN ('v', 'f') AND
                           -- 1 << CMD_INSERT
                           pg_relation_is_updatable(c.oid, false) & 8 = 8)
                THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_insertable_into,

           CAST(CASE WHEN t.typname IS NOT NULL THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_typed,
           CAST(null AS character_data) AS commit_action

    FROM pg_namespace nc JOIN pg_class c ON (nc.oid = c.relnamespace)
           LEFT JOIN (pg_type t JOIN pg_namespace nt ON (t.typnamespace = nt.oid)) ON (c.reloftype = t.oid)

    WHERE c.relkind IN ('r', 'v', 'f', 'p')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_table_privilege(c.oid, 'SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(c.oid, 'SELECT, INSERT, UPDATE, REFERENCES') )
;
----------
[UNKNOWN] SECURITY LABEL ON FUNCTION f3()
    IS 'system_u:object_r:sepgsql_trusted_proc_exec_t:s0'
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW table_privileges AS
    SELECT CAST(u_grantor.rolname AS sql_identifier) AS grantor,
           CAST(grantee.rolname AS sql_identifier) AS grantee,
           CAST(current_database() AS sql_identifier) AS table_catalog,
           CAST(nc.nspname AS sql_identifier) AS table_schema,
           CAST(c.relname AS sql_identifier) AS table_name,
           CAST(c.prtype AS character_data) AS privilege_type,
           CAST(
             CASE WHEN
                  -- object owner always has grant options
                  pg_has_role(grantee.oid, c.relowner, 'USAGE')
                  OR c.grantable
                  THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable,
           CAST(CASE WHEN c.prtype = 'SELECT' THEN 'YES' ELSE 'NO' END AS yes_or_no) AS with_hierarchy

    FROM (
            SELECT oid, relname, relnamespace, relkind, relowner, (aclexplode(coalesce(relacl, acldefault('r', relowner)))).* FROM pg_class
         ) AS c (oid, relname, relnamespace, relkind, relowner, grantor, grantee, prtype, grantable),
         pg_namespace nc,
         pg_authid u_grantor,
         (
           SELECT oid, rolname FROM pg_authid
           UNION ALL
           SELECT 0::oid, 'PUBLIC'
         ) AS grantee (oid, rolname)

    WHERE c.relnamespace = nc.oid
          AND c.relkind IN ('r', 'v', 'f', 'p')
          AND c.grantee = grantee.oid
          AND c.grantor = u_grantor.oid
          AND c.prtype IN ('INSERT', 'SELECT', 'UPDATE', 'DELETE', 'TRUNCATE', 'REFERENCES', 'TRIGGER')
          AND (pg_has_role(u_grantor.oid, 'USAGE')
               OR pg_has_role(grantee.oid, 'USAGE')
               OR grantee.rolname = 'PUBLIC')
;
