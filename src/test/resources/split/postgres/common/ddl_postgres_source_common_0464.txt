DROP SEQUENCE myseq2
;
-- detaching a partition removes identity property
ALTER TABLE pitest2 DETACH PARTITION pitest2_p1
;
CREATE TABLE test_type_diff_c (extra smallint) INHERITS (test_type_diff)
;
CREATE FUNCTION test_trigger()
RETURNS trigger
LANGUAGE plpgsql
AS $$

begin
    raise notice 'old tuple: %', to_json(OLD)::text;
    if TG_OP = 'DELETE'
    then
       return OLD;
    else
       return NEW;
    end if;
end;

$$
;
ALTER TYPE "public"."insenum" ADD VALUE 'i2'
;
CREATE SEQUENCE "public"."sequence_test7"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
analyze numrange_test
;
ALTER TABLE coll_t ENABLE ROW LEVEL SECURITY
;
-- Want "log_who" to be CURRENT_USER,
-- but that is non-portable for the regression test
-- - thomas 1999-02-21

    CREATE RULE log_shoelace AS ON UPDATE TO shoelace_data
        WHERE NEW.sl_avail != OLD.sl_avail
        DO INSERT INTO shoelace_log VALUES (
                                        NEW.sl_name,
                                        NEW.sl_avail,
                                        'Al Bundy',
                                        'epoch'
                                    )
;
CALL ptest5(b => 'Hello', a => 10)
;
-- Create the operator class
CREATE OPERATOR CLASS gist_timestamptz_ops
DEFAULT FOR TYPE timestamptz USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_tstz_consistent (internal, timestamptz, int2, oid, internal),
	FUNCTION	2	gbt_ts_union (internal, internal),
	FUNCTION	3	gbt_tstz_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_ts_penalty (internal, internal, internal),
	FUNCTION	6	gbt_ts_picksplit (internal, internal),
	FUNCTION	7	gbt_ts_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_statistic_ext
;
SECURITY LABEL ON COLUMN t1p_tens.q IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
ALTER TABLE t ADD COLUMN y int
;
-- drop the table

DROP TABLE IF EXISTS test_exists
;
CREATE SCHEMA schema_op1
;
CREATE TABLE plt2_adv_extra PARTITION OF plt2_adv FOR VALUES IN ('0000')
;
-- complex numbers -> jsonb
CREATE FUNCTION testComplexNumbers() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = 1 + 2j
return x
$$
;
CREATE POLICY dep_p1 ON dep1 TO regress_rls_bob USING (c1 > (select max(dep2.c1) from dep2))
;
create database abc with owner = zyc template zyc
;
alter table atacc1 add unique (test2)
;
alter table def_test alter column c1 drop default
;
alter extension test_ext9 add table sometable
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (reject_limit '1')
;
CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = numeric_avg_combine,
	finalfunc_modify = shareable  -- just to test a non-default setting
)
;
-- match any word and return all words except first one
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=false, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=true)
;
CREATE TEMP TABLE pg_temp_14.pg_temp_25613_2 (tid pg_catalog.tid)
;
CREATE TABLE import_source."x 5" (c1 float8)
;
create function tg_pfield_ad() returns trigger as '
begin
    delete from PSlot where pfname = old.name;
    return old;
end;
' language plpgsql
;
ALTER TABLE deferred_excl DROP CONSTRAINT deferred_excl_con
;
SECURITY LABEL ON COLUMN t1p_tens.o IS 'system_u:object_r:sepgsql_table_t:s0'
;
DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 7;
  CALL test_proc11(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
ANALYZE public.wslot
;
CREATE FUNCTION "public"."inoutparam_succeed"(OUT "i" "anyelement", INOUT "r" "anyrange") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select upper($1), $1 $_$
;
CREATE CAST (bpchar AS citext)  WITH FUNCTION citext(bpchar)  AS ASSIGNMENT
;
-- Test with multi-level partitioning scheme

CREATE TABLE pagg_tab_ml (a int, b int, c text) PARTITION BY RANGE(a)
;
CREATE TABLE test_nowait (
	id integer PRIMARY KEY,
	value integer not null
  )
;
CREATE TABLE test_oid (
	i oid
)
;
-- CREATE MATERIALIZED VIEW
-- SELECT queries are normalized, creating matching query IDs.
CREATE MATERIALIZED VIEW matview_stats_1 AS
  SELECT a AS col1, 2::int AS col2
    FROM generate_series(1, 10) AS tab(a) WHERE a < 5 AND a > 2
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
-- Should fail. Invalid attribute
CREATE OPERATOR #@%# (
   rightarg = int8,
   procedure = factorial,
   invalid_att = int8
)
;
ANALYZE pg_temp_12.pg_temp_26722
;
CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, copy_data = true)
;
CREATE FUNCTION gin_extract_query_float8(float8, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
VACUUM (FREEZE) information_schema.sql_languages
;
ALTER OPERATOR % (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
ALTER OPERATOR >= (ismn, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
CREATE UNIQUE INDEX ON mvtest_mv (a)
;
VACUUM (FREEZE) pg_catalog.pg_shseclabel
;
alter schema sch1 rename to sch2
;
------------------------------------------------------------------------------------------
[DROP_SEQUENCE] DROP SEQUENCE myseq2
;
----------
[ALTER_TABLE|ALTER_PARTITION] -- detaching a partition removes identity property
ALTER TABLE pitest2 DETACH PARTITION pitest2_p1
;
----------
[CREATE_TABLE] CREATE TABLE test_type_diff_c (extra smallint) INHERITS (test_type_diff)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_trigger()
RETURNS trigger
LANGUAGE plpgsql
AS $$

begin
    raise notice 'old tuple: %', to_json(OLD)::text;
    if TG_OP = 'DELETE'
    then
       return OLD;
    else
       return NEW;
    end if;
end;

$$
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i2'
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE "public"."sequence_test7"
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
;
----------
[ADMIN_TABLE] analyze numrange_test
;
----------
[ALTER_TABLE] ALTER TABLE coll_t ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_POLICY] -- Want "log_who" to be CURRENT_USER,
-- but that is non-portable for the regression test
-- - thomas 1999-02-21

    CREATE RULE log_shoelace AS ON UPDATE TO shoelace_data
        WHERE NEW.sl_avail != OLD.sl_avail
        DO INSERT INTO shoelace_log VALUES (
                                        NEW.sl_name,
                                        NEW.sl_avail,
                                        'Al Bundy',
                                        'epoch'
                                    )
;
----------
[CALL_PROG_OBJ] CALL ptest5(b => 'Hello', a => 10)
;
----------
[CREATE_PROG_OBJ] -- Create the operator class
CREATE OPERATOR CLASS gist_timestamptz_ops
DEFAULT FOR TYPE timestamptz USING gist
AS
	OPERATOR	1	<  ,
	OPERATOR	2	<= ,
	OPERATOR	3	=  ,
	OPERATOR	4	>= ,
	OPERATOR	5	>  ,
	FUNCTION	1	gbt_tstz_consistent (internal, timestamptz, int2, oid, internal),
	FUNCTION	2	gbt_ts_union (internal, internal),
	FUNCTION	3	gbt_tstz_compress (internal),
	FUNCTION	4	gbt_decompress (internal),
	FUNCTION	5	gbt_ts_penalty (internal, internal, internal),
	FUNCTION	6	gbt_ts_picksplit (internal, internal),
	FUNCTION	7	gbt_ts_same (gbtreekey16, gbtreekey16, internal),
	STORAGE		gbtreekey16
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_statistic_ext
;
----------
[SECURITY_LABEL] SECURITY LABEL ON COLUMN t1p_tens.q IS 'system_u:object_r:sepgsql_secret_table_t:s0'
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE t ADD COLUMN y int
;
----------
[DROP_TABLE] -- drop the table

DROP TABLE IF EXISTS test_exists
;
----------
[CREATE_SCHEMA] CREATE SCHEMA schema_op1
;
----------
[CREATE_TABLE] CREATE TABLE plt2_adv_extra PARTITION OF plt2_adv FOR VALUES IN ('0000')
;
----------
[CREATE_PROG_OBJ] -- complex numbers -> jsonb
CREATE FUNCTION testComplexNumbers() RETURNS jsonb
LANGUAGE plpythonu
TRANSFORM FOR TYPE jsonb
AS $$
x = 1 + 2j
return x
$$
;
----------
[CREATE_POLICY] CREATE POLICY dep_p1 ON dep1 TO regress_rls_bob USING (c1 > (select max(dep2.c1) from dep2))
;
----------
[CREATE_CATALOG] create database abc with owner = zyc template zyc
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc1 add unique (test2)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table def_test alter column c1 drop default
;
----------
[ALTER_LIBRARY] alter extension test_ext9 add table sometable
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (reject_limit '1')
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = numeric_avg_combine,
	finalfunc_modify = shareable  -- just to test a non-default setting
)
;
----------
[ALTER_POLICY] -- match any word and return all words except first one
ALTER TEXT SEARCH DICTIONARY xsyn (RULES='xsyn_sample', KEEPORIG=false, MATCHORIG=true, KEEPSYNONYMS=true, MATCHSYNONYMS=true)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_14.pg_temp_25613_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE import_source."x 5" (c1 float8)
;
----------
[CREATE_PROG_OBJ] create function tg_pfield_ad() returns trigger as '
begin
    delete from PSlot where pfname = old.name;
    return old;
end;
' language plpgsql
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE deferred_excl DROP CONSTRAINT deferred_excl_con
;
----------
[SECURITY_LABEL] SECURITY LABEL ON COLUMN t1p_tens.o IS 'system_u:object_r:sepgsql_table_t:s0'
;
----------
[BLOCK] DO $$
DECLARE _a int; _b int; _c int;
BEGIN
  _a := 10; _b := 30; _c := 7;
  CALL test_proc11(_a, _b, _c);
  RAISE NOTICE '_a: %, _b: %, _c: %', _a, _b, _c;
END
$$
;
----------
[ADMIN_TABLE] ANALYZE public.wslot
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."inoutparam_succeed"(OUT "i" "anyelement", INOUT "r" "anyrange") RETURNS "record"
    LANGUAGE "sql"
    AS $_$ select upper($1), $1 $_$
;
----------
[CREATE_PROG_OBJ] CREATE CAST (bpchar AS citext)  WITH FUNCTION citext(bpchar)  AS ASSIGNMENT
;
----------
[CREATE_TABLE] -- Test with multi-level partitioning scheme

CREATE TABLE pagg_tab_ml (a int, b int, c text) PARTITION BY RANGE(a)
;
----------
[CREATE_TABLE] CREATE TABLE test_nowait (
	id integer PRIMARY KEY,
	value integer not null
  )
;
----------
[CREATE_TABLE] CREATE TABLE test_oid (
	i oid
)
;
----------
[CREATE_VIEW(SELECT)] -- CREATE MATERIALIZED VIEW
-- SELECT queries are normalized, creating matching query IDs.
CREATE MATERIALIZED VIEW matview_stats_1 AS
  SELECT a AS col1, 2::int AS col2
    FROM generate_series(1, 10) AS tab(a) WHERE a < 5 AND a > 2
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic_ext_data
;
----------
[CREATE_PROG_OBJ] -- Should fail. Invalid attribute
CREATE OPERATOR #@%# (
   rightarg = int8,
   procedure = factorial,
   invalid_att = int8
)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26722
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub2 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, copy_data = true)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_query_float8(float8, internal, int2, internal, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) information_schema.sql_languages
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR % (text, text)
  SET (RESTRICT = matchingsel, JOIN = matchingjoinsel)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ismn, ean13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX ON mvtest_mv (a)
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_shseclabel
;
----------
[RENAME_SCHEMA] alter schema sch1 rename to sch2
;
