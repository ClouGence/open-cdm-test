ALTER PUBLICATION tap_pub_2 ADD TABLE tab_2
;
create unique index parent_idx on parent (i)
;
CREATE TABLE test_bytea (
	i bytea
)
;
alter table invalid_check_con add constraint inh_check_constraint check(f1 > 0) not valid
;
create or replace function stricttest() returns void as $$
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- too many rows
  select * from foo where f1 > p1 or f1::text = p3  into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
CREATE FUNCTION check_primary_key ()
	RETURNS trigger
	AS '/src/src/test/regress/refint.so'
	LANGUAGE C
;
CREATE TABLE tab_gen_to_nogen (a int, b int)
;
CREATE TABLE trunc_b (a int REFERENCES truncate_a)
;
create table rtest_interface (sysname text, ifname text)
;
TRUNCATE public.tru_pk_table CONTINUE IDENTITY RESTRICT
;
COMMENT ON TEXT SEARCH CONFIGURATION german IS 'configuration for german language'
;
ANALYZE test_table
;
ANALYZE public.tableam_parted_c_heap2
;
-- error, not a sequence

CREATE SEQUENCE sequence_test2 START WITH 32
;
CREATE TEMP SEQUENCE temp_seq_probe AS smallint START WITH -3 INCREMENT BY -1 MINVALUE -10 MAXVALUE -1 CACHE 1 NO CYCLE
;
CREATE OPERATOR <> (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = =,
    COMMUTATOR = <>,
    PROCEDURE  = citext_ne,
    RESTRICT   = neqsel,
    JOIN       = neqjoinsel
)
;
--
-- Partial index tests, no inference predicate specified
--
create unique index part_comp_key_index on insertconflicttest(key, fruit) where key < 5
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_constraint
;
VACUUM (FULL) information_schema.sql_packages
;
CREATE FUNCTION _pg_keysequal(smallint[], smallint[]) RETURNS boolean
    LANGUAGE sql IMMUTABLE PARALLEL SAFE  -- intentionally not STRICT, to allow inlining
    AS 'select $1 operator(pg_catalog.<@) $2 and $2 operator(pg_catalog.<@) $1'
;
CREATE TABLE test_time(data text)
;
CREATE OPERATOR -> (
	LEFTARG = cube, RIGHTARG = int, PROCEDURE = cube_coord
)
;
drop operator === (int4, int4)
;
CREATE TEMP TABLE pg_temp_11.pg_temp_25373_2 (tid pg_catalog.tid)
;
CREATE TABLE "public"."mlparted11" (
    "b" integer CONSTRAINT "mlparted1_b_not_null" NOT NULL,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */,
    "........pg.dropped.8........" INTEGER /* dummy */,
    "........pg.dropped.9........" INTEGER /* dummy */
)
;
CREATE INDEX brinidx_2 ON brintest_2 USING brin (n)
;
ALTER TABLE fkpart11.pk12 DROP c
;
CREATE VIEW key_dependent_view AS
   SELECT * FROM view_base_table GROUP BY key
;
CREATE VIEW check_constraint_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name
    FROM pg_namespace nc, pg_constraint c, pg_depend d, pg_proc p, pg_namespace np
    WHERE nc.oid = c.connamespace
      AND c.contype = 'c'
      AND c.oid = d.objid
      AND d.classid = 'pg_catalog.pg_constraint'::regclass
      AND d.refobjid = p.oid
      AND d.refclassid = 'pg_catalog.pg_proc'::regclass
      AND p.pronamespace = np.oid
      AND pg_has_role(p.proowner, 'USAGE')
;
/* Base view for foreign tables */
CREATE VIEW _pg_foreign_tables AS
    SELECT
           CAST(current_database() AS sql_identifier) AS foreign_table_catalog,
           CAST(n.nspname AS sql_identifier) AS foreign_table_schema,
           CAST(c.relname AS sql_identifier) AS foreign_table_name,
           t.ftoptions AS ftoptions,
           CAST(current_database() AS sql_identifier) AS foreign_server_catalog,
           CAST(srvname AS sql_identifier) AS foreign_server_name,
           CAST(u.rolname AS sql_identifier) AS authorization_identifier
    FROM pg_foreign_table t, pg_foreign_server s, pg_foreign_data_wrapper w,
         pg_authid u, pg_namespace n, pg_class c
    WHERE w.oid = s.srvfdw
          AND u.oid = c.relowner
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_table_privilege(c.oid, 'SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(c.oid, 'SELECT, INSERT, UPDATE, REFERENCES'))
          AND n.oid = c.relnamespace
          AND c.oid = t.ftrelid
          AND c.relkind = 'f'
          AND s.oid = t.ftserver
;
-- however, this should work:
create function dfunc(a int = 1, out sum int, b int = 2) as $$
  select $1 + $2;
$$ language sql
;
-- error

CREATE TABLE test_pg_dump_t1 (c1 int)
;
DROP TABLE parent_tbl CASCADE
;
ALTER TABLE pg_foreign_server ADD UNIQUE USING INDEX pg_foreign_server_name_index
;
ALTER TABLE fd_pt1 ALTER COLUMN c8 SET STATISTICS -1
;
create temp table b (
     a char not null,
     num integer not null,
     constraint b_pk primary key (a, num)
)
;
CREATE TYPE itest_type AS (f1 integer, f2 text, f3 bigint)
;
CREATE TABLE loct_empty (c1 int NOT NULL, c2 text)
;
drop function testpolym(int)
;
-- Create a view for convenient access.
CREATE VIEW pg_buffercache AS
	SELECT P.* FROM pg_buffercache_pages() AS P
	(bufferid integer, relfilenode oid, reltablespace oid, reldatabase oid,
	 relforknumber int2, relblocknumber int8, isdirty bool, usagecount int2,
	 pinning_backends int4)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN IF NOT EXISTS c6 integer
;
create table parted_trigger_1 partition of parted_trigger for values from (0) to (1000)
;
drop function composrec()
;
CREATE SUBSCRIPTION tap_sub_C
	CONNECTION 'port=15418 host=/tmp/cn3j2BSt7P dbname=postgres application_name=tap_sub_C'
	PUBLICATION tap_pub_B
	WITH (two_phase = on)
;
ALTER INDEX pg_toast.pg_toast_30500_index RENAME TO reind_con_toast_idx
;
ALTER TABLE ONLY "public"."mlparted" ATTACH PARTITION "public"."mlparted2" FOR VALUES FROM (1, 10) TO (1, 20)
;
create table rule_and_refint_t3 (
	id3a integer,
	id3b integer,
	id3c integer,
	data text,
	primary key (id3a, id3b, id3c),
	foreign key (id3a, id3b) references rule_and_refint_t1 (id1a, id1b),
	foreign key (id3a, id3c) references rule_and_refint_t2 (id2a, id2c)
)
;
CREATE TEMP TABLE rows AS
SELECT x, 'txt' || x as y
FROM generate_series(1,3) AS x
;
CREATE VIEW view_stats_1 AS
  SELECT a AS col1, 4::int AS col2
    FROM generate_series(1, 5) AS tab(a) WHERE a < 4 AND a > 3
;
CREATE EXTENSION IF NOT EXISTS "tcn"
;
------------------------------------------------------------------------------------------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub_2 ADD TABLE tab_2
;
----------
[ADD_INDEX] create unique index parent_idx on parent (i)
;
----------
[CREATE_TABLE] CREATE TABLE test_bytea (
	i bytea
)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table invalid_check_con add constraint inh_check_constraint check(f1 > 0) not valid
;
----------
[CREATE_PROG_OBJ] create or replace function stricttest() returns void as $$
declare
x record;
p1 int := 2;
p3 text := 'foo';
begin
  -- too many rows
  select * from foo where f1 > p1 or f1::text = p3  into strict x;
  raise notice 'x.f1 = %, x.f2 = %', x.f1, x.f2;
end$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION check_primary_key ()
	RETURNS trigger
	AS '/src/src/test/regress/refint.so'
	LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE tab_gen_to_nogen (a int, b int)
;
----------
[CREATE_TABLE] CREATE TABLE trunc_b (a int REFERENCES truncate_a)
;
----------
[CREATE_TABLE] create table rtest_interface (sysname text, ifname text)
;
----------
[TRUNCATE_TABLE] TRUNCATE public.tru_pk_table CONTINUE IDENTITY RESTRICT
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION german IS 'configuration for german language'
;
----------
[ADMIN_TABLE] ANALYZE test_table
;
----------
[ADMIN_TABLE] ANALYZE public.tableam_parted_c_heap2
;
----------
[CREATE_SEQUENCE] -- error, not a sequence

CREATE SEQUENCE sequence_test2 START WITH 32
;
----------
[CREATE_SEQUENCE] CREATE TEMP SEQUENCE temp_seq_probe AS smallint START WITH -3 INCREMENT BY -1 MINVALUE -10 MAXVALUE -1 CACHE 1 NO CYCLE
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <> (
    LEFTARG    = CITEXT,
    RIGHTARG   = CITEXT,
    NEGATOR    = =,
    COMMUTATOR = <>,
    PROCEDURE  = citext_ne,
    RESTRICT   = neqsel,
    JOIN       = neqjoinsel
)
;
----------
[ADD_INDEX] --
-- Partial index tests, no inference predicate specified
--
create unique index part_comp_key_index on insertconflicttest(key, fruit) where key < 5
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_constraint
;
----------
[ADMIN_TABLE] VACUUM (FULL) information_schema.sql_packages
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _pg_keysequal(smallint[], smallint[]) RETURNS boolean
    LANGUAGE sql IMMUTABLE PARALLEL SAFE  -- intentionally not STRICT, to allow inlining
    AS 'select $1 operator(pg_catalog.<@) $2 and $2 operator(pg_catalog.<@) $1'
;
----------
[CREATE_TABLE] CREATE TABLE test_time(data text)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR -> (
	LEFTARG = cube, RIGHTARG = int, PROCEDURE = cube_coord
)
;
----------
[DROP_PROG_OBJ] drop operator === (int4, int4)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_25373_2 (tid pg_catalog.tid)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."mlparted11" (
    "b" integer CONSTRAINT "mlparted1_b_not_null" NOT NULL,
    "........pg.dropped.2........" INTEGER /* dummy */,
    "........pg.dropped.3........" INTEGER /* dummy */,
    "a" integer NOT NULL,
    "c" "text",
    "........pg.dropped.6........" INTEGER /* dummy */,
    "........pg.dropped.7........" INTEGER /* dummy */,
    "........pg.dropped.8........" INTEGER /* dummy */,
    "........pg.dropped.9........" INTEGER /* dummy */
)
;
----------
[ADD_INDEX] CREATE INDEX brinidx_2 ON brintest_2 USING brin (n)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE fkpart11.pk12 DROP c
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW key_dependent_view AS
   SELECT * FROM view_base_table GROUP BY key
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW check_constraint_routine_usage AS
    SELECT DISTINCT
           CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(nc.nspname AS sql_identifier) AS constraint_schema,
           CAST(c.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(np.nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(p.proname, p.oid) AS sql_identifier) AS specific_name
    FROM pg_namespace nc, pg_constraint c, pg_depend d, pg_proc p, pg_namespace np
    WHERE nc.oid = c.connamespace
      AND c.contype = 'c'
      AND c.oid = d.objid
      AND d.classid = 'pg_catalog.pg_constraint'::regclass
      AND d.refobjid = p.oid
      AND d.refclassid = 'pg_catalog.pg_proc'::regclass
      AND p.pronamespace = np.oid
      AND pg_has_role(p.proowner, 'USAGE')
;
----------
[CREATE_VIEW(SELECT,METADATA)] /* Base view for foreign tables */
CREATE VIEW _pg_foreign_tables AS
    SELECT
           CAST(current_database() AS sql_identifier) AS foreign_table_catalog,
           CAST(n.nspname AS sql_identifier) AS foreign_table_schema,
           CAST(c.relname AS sql_identifier) AS foreign_table_name,
           t.ftoptions AS ftoptions,
           CAST(current_database() AS sql_identifier) AS foreign_server_catalog,
           CAST(srvname AS sql_identifier) AS foreign_server_name,
           CAST(u.rolname AS sql_identifier) AS authorization_identifier
    FROM pg_foreign_table t, pg_foreign_server s, pg_foreign_data_wrapper w,
         pg_authid u, pg_namespace n, pg_class c
    WHERE w.oid = s.srvfdw
          AND u.oid = c.relowner
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_table_privilege(c.oid, 'SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
               OR has_any_column_privilege(c.oid, 'SELECT, INSERT, UPDATE, REFERENCES'))
          AND n.oid = c.relnamespace
          AND c.oid = t.ftrelid
          AND c.relkind = 'f'
          AND s.oid = t.ftserver
;
----------
[CREATE_PROG_OBJ] -- however, this should work:
create function dfunc(a int = 1, out sum int, b int = 2) as $$
  select $1 + $2;
$$ language sql
;
----------
[CREATE_TABLE] -- error

CREATE TABLE test_pg_dump_t1 (c1 int)
;
----------
[DROP_TABLE] DROP TABLE parent_tbl CASCADE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_foreign_server ADD UNIQUE USING INDEX pg_foreign_server_name_index
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt1 ALTER COLUMN c8 SET STATISTICS -1
;
----------
[CREATE_TABLE] create temp table b (
     a char not null,
     num integer not null,
     constraint b_pk primary key (a, num)
)
;
----------
[CREATE_TYPE] CREATE TYPE itest_type AS (f1 integer, f2 text, f3 bigint)
;
----------
[CREATE_TABLE] CREATE TABLE loct_empty (c1 int NOT NULL, c2 text)
;
----------
[DROP_PROG_OBJ] drop function testpolym(int)
;
----------
[CREATE_VIEW(SELECT)] -- Create a view for convenient access.
CREATE VIEW pg_buffercache AS
	SELECT P.* FROM pg_buffercache_pages() AS P
	(bufferid integer, relfilenode oid, reltablespace oid, reldatabase oid,
	 relforknumber int2, relblocknumber int8, isdirty bool, usagecount int2,
	 pinning_backends int4)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN IF NOT EXISTS c6 integer
;
----------
[CREATE_TABLE] create table parted_trigger_1 partition of parted_trigger for values from (0) to (1000)
;
----------
[DROP_PROG_OBJ] drop function composrec()
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_C
	CONNECTION 'port=15418 host=/tmp/cn3j2BSt7P dbname=postgres application_name=tap_sub_C'
	PUBLICATION tap_pub_B
	WITH (two_phase = on)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30500_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted" ATTACH PARTITION "public"."mlparted2" FOR VALUES FROM (1, 10) TO (1, 20)
;
----------
[CREATE_TABLE] create table rule_and_refint_t3 (
	id3a integer,
	id3b integer,
	id3c integer,
	data text,
	primary key (id3a, id3b, id3c),
	foreign key (id3a, id3b) references rule_and_refint_t1 (id1a, id1b),
	foreign key (id3a, id3c) references rule_and_refint_t2 (id2a, id2c)
)
;
----------
[CREATE_TABLE|SELECT] CREATE TEMP TABLE rows AS
SELECT x, 'txt' || x as y
FROM generate_series(1,3) AS x
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW view_stats_1 AS
  SELECT a AS col1, 4::int AS col2
    FROM generate_series(1, 5) AS tab(a) WHERE a < 4 AND a > 3
;
----------
[CREATE_LIBRARY] CREATE EXTENSION IF NOT EXISTS "tcn"
;
