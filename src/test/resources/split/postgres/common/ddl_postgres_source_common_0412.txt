VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_opclass
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_replication_origin
;
ALTER TEXT SEARCH DICTIONARY alt_ts_dict3 RENAME TO alt_ts_dict4
;
VACUUM (PARALLEL 0) pg_catalog.pg_foreign_data_wrapper
;
-- copied from alter_table.sql
create type ctype as (f1 int, f2 text)
;
CREATE MATERIALIZED VIEW "public"."mvtest_tm" AS
 SELECT "type",
    "sum"("amt") AS "totamt"
   FROM "public"."mvtest_t"
  GROUP BY "type"
  WITH NO DATA
;
create function tg_phone_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from PHone where slotname = old.slotname;
	insert into PHone (
		    slotname,
		    comment,
		    slotlink
		) values (
		    new.slotname,
		    new.comment,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
--
-- GiST (rtree-equivalent opclasses only)
--

CREATE TABLE slow_emp4000 (
	home_base	 box
)
;
ALTER DATABASE "contrib_regression" SET timezone_abbreviations TO 'Default'
;
CREATE DATABASE d2
;
CREATE TABLE bttest_unique2 (i int4)
;
CALL transaction_test1(9, 'foo')
;
DO LANGUAGE plpgsql
$$
DECLARE
  my_schema pg_catalog.text := pg_catalog.quote_ident(pg_catalog.current_schema());
  old_path pg_catalog.text := pg_catalog.current_setting('search_path');
BEGIN
-- for safety, transiently set search_path to just pg_catalog+pg_temp
PERFORM pg_catalog.set_config('search_path', 'pg_catalog, pg_temp', true);

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.g_cube_compress(pg_catalog.internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.g_cube_decompress(pg_catalog.internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

PERFORM pg_catalog.set_config('search_path', old_path, true);
END
$$
;
-- Exclusion constraints cannot be added
create table idxpart (a int, b int) partition by range (a)
;
CREATE TABLESPACE tblspc1 LOCATION '/tmp/tsqNVC_rsv/tempdir/tblspc1'
;
CREATE TABLE addr_nsp.parttable (
	a int PRIMARY KEY
) PARTITION BY RANGE (a)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_publication_namespace
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
ANALYZE public.mcv_lists_arrays
;
CREATE FUNCTION "public"."first_el"("anyarray") RETURNS "anyelement"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$select $1[1]$_$
;
create index on idxpart2 (a collate "POSIX")
;
CREATE INDEX regular_sized_index ON parallel_vacuum_table(a)
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_foreign_server
;
ANALYZE public.boolvalues
;
DROP TYPE test8b
;
CREATE TRIGGER target_tg_trig BEFORE INSERT OR UPDATE OR DELETE ON target_tg
    FOR EACH ROW EXECUTE FUNCTION target_tg_trig_fn()
;
ALTER FUNCTION gbt_macad_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
create temp table t1(f1 int, f2 int)
;
DROP SCHEMA temp_view_test CASCADE
;
DROP TABLE pg_temp_3.pg_temp_16656_2, pg_temp_3.pg_temp_16656
;
create table p1(f1 int constraint f1_pos CHECK (f1 > 0))
;
/* contrib/seg/seg--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- Get rid of unnecessary compress and decompress support functions.
--
-- To be allowed to drop the opclass entry for a support function,
-- we must change the entry's dependency type from 'internal' to 'auto',
-- as though it were a loose member of the opfamily rather than being
-- bound into a particular opclass.  There's no SQL command for that,
-- so fake it with a manual update on pg_depend.
--
DO LANGUAGE plpgsql
$$
DECLARE
  my_schema pg_catalog.text := pg_catalog.quote_ident(pg_catalog.current_schema());
  old_path pg_catalog.text := pg_catalog.current_setting('search_path');
BEGIN
-- for safety, transiently set search_path to just pg_catalog+pg_temp
PERFORM pg_catalog.set_config('search_path', 'pg_catalog, pg_temp', true);

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.gseg_compress(internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.gseg_decompress(internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

PERFORM pg_catalog.set_config('search_path', old_path, true);
END
$$
;
CREATE TRIGGER before_upd_a_row_trig BEFORE UPDATE OF a ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_upd_a_row')
;
CREATE TEMP TABLE pg_temp_3.pg_temp_26915_2 (tid pg_catalog.tid)
;
CREATE FUNCTION tfp(anyarray,anyelement) RETURNS anyarray AS
'select $1 || $2' LANGUAGE SQL
;
CREATE INDEX btree_test_expr_idx ON btree_test_expr USING btree (btree_test_func())
;
DROP COLLATION split_collation_probe.c_lc_parts
;
-- identity was copied and applied
DROP TABLE test_like_id_1, test_like_id_2, test_like_id_3
;
CREATE SCHEMA newschema6 AUTHORIZATION "Public"
;
create table mclparted2 partition of mclparted for values in(2)
;
CREATE VIEW iexit AS
   SELECT ih.name, ih.thepath,
	interpt_pp(ih.thepath, r.thepath) AS exit
   FROM ihighway ih, ramp r
   WHERE ih.thepath ## r.thepath
;
create table rules_foo2 (f1 int)
;
alter table idxpart attach partition idxpart2 for values from (0) to (1)
;
CREATE VIEW pg_stat_database_conflicts AS
    SELECT
            D.oid AS datid,
            D.datname AS datname,
            pg_stat_get_db_conflict_tablespace(D.oid) AS confl_tablespace,
            pg_stat_get_db_conflict_lock(D.oid) AS confl_lock,
            pg_stat_get_db_conflict_snapshot(D.oid) AS confl_snapshot,
            pg_stat_get_db_conflict_bufferpin(D.oid) AS confl_bufferpin,
            pg_stat_get_db_conflict_startup_deadlock(D.oid) AS confl_deadlock,
            pg_stat_get_db_conflict_logicalslot(D.oid) AS confl_active_logicalslot
    FROM pg_database D
;
alter domain con add check (VALUE > 0)
;
CREATE TABLE test_part_1_1 PARTITION OF test_part FOR VALUES IN (1,2,3,4,5,6)
;
-- make plans more stable
ANALYZE ctv_data
;
ALTER TABLE ataddindex
  ADD UNIQUE (id),
  ADD FOREIGN KEY (ref_id) REFERENCES ataddindex (id)
;
DROP CAST IF EXISTS (no_such_type1 AS INTEGER)
;
create index on idxpart2 (c, b)
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_opclass
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_replication_origin
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH DICTIONARY alt_ts_dict3 RENAME TO alt_ts_dict4
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_foreign_data_wrapper
;
----------
[CREATE_TYPE] -- copied from alter_table.sql
create type ctype as (f1 int, f2 text)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."mvtest_tm" AS
 SELECT "type",
    "sum"("amt") AS "totamt"
   FROM "public"."mvtest_t"
  GROUP BY "type"
  WITH NO DATA
;
----------
[CREATE_PROG_OBJ] create function tg_phone_bu() returns trigger as '
begin
    if new.slotname != old.slotname then
        delete from PHone where slotname = old.slotname;
	insert into PHone (
		    slotname,
		    comment,
		    slotlink
		) values (
		    new.slotname,
		    new.comment,
		    new.slotlink
		);
        return null;
    end if;
    return new;
end;
' language plpgsql
;
----------
[CREATE_TABLE] --
-- GiST (rtree-equivalent opclasses only)
--

CREATE TABLE slow_emp4000 (
	home_base	 box
)
;
----------
[ALTER_CATALOG] ALTER DATABASE "contrib_regression" SET timezone_abbreviations TO 'Default'
;
----------
[CREATE_CATALOG] CREATE DATABASE d2
;
----------
[CREATE_TABLE] CREATE TABLE bttest_unique2 (i int4)
;
----------
[CALL_PROG_OBJ] CALL transaction_test1(9, 'foo')
;
----------
[BLOCK] DO LANGUAGE plpgsql
$$
DECLARE
  my_schema pg_catalog.text := pg_catalog.quote_ident(pg_catalog.current_schema());
  old_path pg_catalog.text := pg_catalog.current_setting('search_path');
BEGIN
-- for safety, transiently set search_path to just pg_catalog+pg_temp
PERFORM pg_catalog.set_config('search_path', 'pg_catalog, pg_temp', true);

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.g_cube_compress(pg_catalog.internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.g_cube_decompress(pg_catalog.internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

PERFORM pg_catalog.set_config('search_path', old_path, true);
END
$$
;
----------
[CREATE_TABLE] -- Exclusion constraints cannot be added
create table idxpart (a int, b int) partition by range (a)
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE tblspc1 LOCATION '/tmp/tsqNVC_rsv/tempdir/tblspc1'
;
----------
[CREATE_TABLE] CREATE TABLE addr_nsp.parttable (
	a int PRIMARY KEY
) PARTITION BY RANGE (a)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_publication_namespace
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
----------
[ADMIN_TABLE] ANALYZE public.mcv_lists_arrays
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."first_el"("anyarray") RETURNS "anyelement"
    LANGUAGE "sql" IMMUTABLE STRICT
    AS $_$select $1[1]$_$
;
----------
[ADD_INDEX] create index on idxpart2 (a collate "POSIX")
;
----------
[ADD_INDEX] CREATE INDEX regular_sized_index ON parallel_vacuum_table(a)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_foreign_server
;
----------
[ADMIN_TABLE] ANALYZE public.boolvalues
;
----------
[DROP_TYPE] DROP TYPE test8b
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER target_tg_trig BEFORE INSERT OR UPDATE OR DELETE ON target_tg
    FOR EACH ROW EXECUTE FUNCTION target_tg_trig_fn()
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_macad_same(gbtreekey16, gbtreekey16, internal) PARALLEL SAFE
;
----------
[CREATE_TABLE] create temp table t1(f1 int, f2 int)
;
----------
[DROP_SCHEMA] DROP SCHEMA temp_view_test CASCADE
;
----------
[DROP_TABLE] DROP TABLE pg_temp_3.pg_temp_16656_2, pg_temp_3.pg_temp_16656
;
----------
[CREATE_TABLE] create table p1(f1 int constraint f1_pos CHECK (f1 > 0))
;
----------
[BLOCK] /* contrib/seg/seg--1.2--1.3.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- Get rid of unnecessary compress and decompress support functions.
--
-- To be allowed to drop the opclass entry for a support function,
-- we must change the entry's dependency type from 'internal' to 'auto',
-- as though it were a loose member of the opfamily rather than being
-- bound into a particular opclass.  There's no SQL command for that,
-- so fake it with a manual update on pg_depend.
--
DO LANGUAGE plpgsql
$$
DECLARE
  my_schema pg_catalog.text := pg_catalog.quote_ident(pg_catalog.current_schema());
  old_path pg_catalog.text := pg_catalog.current_setting('search_path');
BEGIN
-- for safety, transiently set search_path to just pg_catalog+pg_temp
PERFORM pg_catalog.set_config('search_path', 'pg_catalog, pg_temp', true);

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.gseg_compress(internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

UPDATE pg_catalog.pg_depend
SET deptype = 'a'
WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
  AND objid =
    (SELECT objid
     FROM pg_catalog.pg_depend
     WHERE classid = 'pg_catalog.pg_amproc'::pg_catalog.regclass
       AND refclassid = 'pg_catalog.pg_proc'::pg_catalog.regclass
       AND (refobjid = (my_schema || '.gseg_decompress(internal)')::pg_catalog.regprocedure))
  AND refclassid = 'pg_catalog.pg_opclass'::pg_catalog.regclass
  AND deptype = 'i';

PERFORM pg_catalog.set_config('search_path', old_path, true);
END
$$
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER before_upd_a_row_trig BEFORE UPDATE OF a ON main_table
FOR EACH ROW EXECUTE PROCEDURE trigger_func('before_upd_a_row')
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_3.pg_temp_26915_2 (tid pg_catalog.tid)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION tfp(anyarray,anyelement) RETURNS anyarray AS
'select $1 || $2' LANGUAGE SQL
;
----------
[ADD_INDEX] CREATE INDEX btree_test_expr_idx ON btree_test_expr USING btree (btree_test_func())
;
----------
[DROP_POLICY] DROP COLLATION split_collation_probe.c_lc_parts
;
----------
[DROP_TABLE] -- identity was copied and applied
DROP TABLE test_like_id_1, test_like_id_2, test_like_id_3
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema6 AUTHORIZATION "Public"
;
----------
[CREATE_TABLE] create table mclparted2 partition of mclparted for values in(2)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW iexit AS
   SELECT ih.name, ih.thepath,
	interpt_pp(ih.thepath, r.thepath) AS exit
   FROM ihighway ih, ramp r
   WHERE ih.thepath ## r.thepath
;
----------
[CREATE_TABLE] create table rules_foo2 (f1 int)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table idxpart attach partition idxpart2 for values from (0) to (1)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_database_conflicts AS
    SELECT
            D.oid AS datid,
            D.datname AS datname,
            pg_stat_get_db_conflict_tablespace(D.oid) AS confl_tablespace,
            pg_stat_get_db_conflict_lock(D.oid) AS confl_lock,
            pg_stat_get_db_conflict_snapshot(D.oid) AS confl_snapshot,
            pg_stat_get_db_conflict_bufferpin(D.oid) AS confl_bufferpin,
            pg_stat_get_db_conflict_startup_deadlock(D.oid) AS confl_deadlock,
            pg_stat_get_db_conflict_logicalslot(D.oid) AS confl_active_logicalslot
    FROM pg_database D
;
----------
[ALTER_TYPE] alter domain con add check (VALUE > 0)
;
----------
[CREATE_TABLE] CREATE TABLE test_part_1_1 PARTITION OF test_part FOR VALUES IN (1,2,3,4,5,6)
;
----------
[ADMIN_TABLE] -- make plans more stable
ANALYZE ctv_data
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ataddindex
  ADD UNIQUE (id),
  ADD FOREIGN KEY (ref_id) REFERENCES ataddindex (id)
;
----------
[DROP_PROG_OBJ] DROP CAST IF EXISTS (no_such_type1 AS INTEGER)
;
----------
[ADD_INDEX] create index on idxpart2 (c, b)
;
