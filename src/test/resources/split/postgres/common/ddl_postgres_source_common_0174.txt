ALTER TYPE test_type DROP ATTRIBUTE IF EXISTS c
;
ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f4_excl EXCLUDE USING btree (f4 WITH =) WHERE ((f4 IS NOT NULL))
;
alter table p1 add constraint inh_check_constraint1 check (f1 > 0)
;
alter table defc alter column f1 set default 1
;
COMMENT ON CONVERSION myconv IS 'bar'
;
CREATE TABLE pht1 (a int, b int, c text) PARTITION BY HASH(c)
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c8 integer
;
VACUUM FULL pg_class
;
create function list_part_fn(int) returns int as $$ begin return $1; end;$$ language plpgsql stable
;
create table defpart_attach_test (a int) partition by list (a)
;
CREATE INDEX idx_bool ON test_bool USING gin (i)
;
DROP TABLE TEMP_GROUP
;
CREATE FOREIGN TABLE agg_bad (
	a	int2,
	b	float4
) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/agg.bad', header 'true', delimiter ';', quote '@', escape '"', null '')
;
-- fail, deferrable
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_unique_defer
;
CREATE SCHEMA myschema
;
CREATE TEMP TABLE t1c (b text, a text)
;
ALTER TABLE pg_attribute ADD PRIMARY KEY USING INDEX pg_attribute_relid_attnum_index
;
CREATE TABLE shiftjis2004_inputs (inbytes bytea, description text)
;
create function testpolym(a int, a int) returns int as $$ select 1;$$ language sql
;
-- Drupal example, http://drupal.org/node/555530

CREATE TEMP TABLE node (
    nid SERIAL,
    vid integer NOT NULL default '0',
    type varchar(32) NOT NULL default '',
    title varchar(128) NOT NULL default '',
    uid integer NOT NULL default '0',
    status integer NOT NULL default '1',
    created integer NOT NULL default '0',
    -- snip
    PRIMARY KEY (nid, vid)
)
;
CREATE TABLE "public"."itest3" (
    "a" integer NOT NULL,
    "b" "text",
    "c" integer NOT NULL
)
;
alter extension postgres_fdw add operator class my_op_class using btree
;
do $$
declare x var_record not null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
ALTER TABLE pg_temp_24.pg_temp_26683_2 ADD COLUMN newdata pg_temp_24.pg_temp_26683
;
CREATE TRIGGER account_view_insert
    INSTEAD OF INSERT ON split_trigger_probe.account_view
    FOR EACH ROW
    EXECUTE FUNCTION split_trigger_probe.view_insert_row()
;
CREATE TRIGGER d15_update_trig
  AFTER UPDATE ON part_d_15_20 for each statement execute procedure trigfunc()
;
/* contrib/pg_buffercache/pg_buffercache--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION pg_buffercache_summary(
    OUT buffers_used int4,
    OUT buffers_unused int4,
    OUT buffers_dirty int4,
    OUT buffers_pinned int4,
    OUT usagecount_avg float8)
AS '$libdir/pg_buffercache', 'pg_buffercache_summary'
LANGUAGE C PARALLEL SAFE
;
-- error

DROP VIEW vac_option_tab_counts
;
--
-- ISO-8859-5
--
CREATE TABLE iso8859_5_inputs (inbytes bytea, description text)
;
CREATE FUNCTION "public"."ks_test_normal_random"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random_normal() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs((1+erf(r/sqrt(2)))/2 - i/n)) < c / sqrt(n)
    FROM indexed_samples
  );
  RETURN ok;
END
$$
;
/*
 * 24.5
 * FOREIGN_DATA_WRAPPER_OPTIONS view
 */
CREATE VIEW foreign_data_wrapper_options AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_data_wrappers w
;
drop trigger trg1 on trigpart2
;
DROP TABLE IF EXISTS tbl
;
CREATE TYPE ess_range_type AS RANGE (subtype = text)
;
create index on idxpart_temp(a)
;
ALTER INDEX tbl_idx ALTER COLUMN 4 SET STATISTICS 1000
;
-- 2 new columns, first has default
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
-- test case with whole-row Var in a check constraint
create table check_con_tbl (f1 int)
;
ALTER INDEX pg_toast.pg_toast_30929_index RENAME TO reind_con_toast_idx
;
ALTER TABLE fd_pt2 DETACH PARTITION fd_pt2_1
;
CREATE TABLE "public"."rc_test" (
    "a" integer,
    "b" integer
)
;
CREATE INDEX idx_child_embedding_hnsw ON split_cov.child_table USING hnsw (embedding vector_l2_ops)
;
CREATE SUBSCRIPTION regress_sub2 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub2
;
create function perform_test_func() returns void as '
BEGIN
	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	PERFORM perform_simple_func(5);

	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	PERFORM perform_simple_func(50);

	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	RETURN;
END;' language plpgsql
;
DROP TABLE num_exp_sub
;
CREATE OPERATOR !== (
        PROCEDURE = int8ne,
        LEFTARG = bigint,
        RIGHTARG = bigint,
        NEGATOR = ===,
        COMMUTATOR = !==
)
;
CREATE INDEX hash_f8_index ON hash_f8_heap USING hash (random float8_ops)
  WITH (fillfactor=60)
;
ALTER TABLE split_alter_table.base_table SET LOGGED
;
ANALYZE fkpart3.fk2
;
create index on only idxpart2 ((a/b))
;
------------------------------------------------------------------------------------------
[ALTER_TYPE] ALTER TYPE test_type DROP ATTRIBUTE IF EXISTS c
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.anothertab ADD CONSTRAINT anothertab_f4_excl EXCLUDE USING btree (f4 WITH =) WHERE ((f4 IS NOT NULL))
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table p1 add constraint inh_check_constraint1 check (f1 > 0)
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table defc alter column f1 set default 1
;
----------
[ALTER_POLICY] COMMENT ON CONVERSION myconv IS 'bar'
;
----------
[CREATE_TABLE] CREATE TABLE pht1 (a int, b int, c text) PARTITION BY HASH(c)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 ADD COLUMN c8 integer
;
----------
[ADMIN_TABLE] VACUUM FULL pg_class
;
----------
[CREATE_PROG_OBJ] create function list_part_fn(int) returns int as $$ begin return $1; end;$$ language plpgsql stable
;
----------
[CREATE_TABLE] create table defpart_attach_test (a int) partition by list (a)
;
----------
[ADD_INDEX] CREATE INDEX idx_bool ON test_bool USING gin (i)
;
----------
[DROP_TABLE] DROP TABLE TEMP_GROUP
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE agg_bad (
	a	int2,
	b	float4
) SERVER file_server
OPTIONS (format 'csv', filename '/src/contrib/file_fdw/data/agg.bad', header 'true', delimiter ';', quote '@', escape '"', null '')
;
----------
[ALTER_TABLE] -- fail, deferrable
ALTER TABLE test_replica_identity REPLICA IDENTITY USING INDEX test_replica_identity_unique_defer
;
----------
[CREATE_SCHEMA] CREATE SCHEMA myschema
;
----------
[CREATE_TABLE] CREATE TEMP TABLE t1c (b text, a text)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_attribute ADD PRIMARY KEY USING INDEX pg_attribute_relid_attnum_index
;
----------
[CREATE_TABLE] CREATE TABLE shiftjis2004_inputs (inbytes bytea, description text)
;
----------
[CREATE_PROG_OBJ] create function testpolym(a int, a int) returns int as $$ select 1;$$ language sql
;
----------
[CREATE_TABLE] -- Drupal example, http://drupal.org/node/555530

CREATE TEMP TABLE node (
    nid SERIAL,
    vid integer NOT NULL default '0',
    type varchar(32) NOT NULL default '',
    title varchar(128) NOT NULL default '',
    uid integer NOT NULL default '0',
    status integer NOT NULL default '1',
    created integer NOT NULL default '0',
    -- snip
    PRIMARY KEY (nid, vid)
)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."itest3" (
    "a" integer NOT NULL,
    "b" "text",
    "c" integer NOT NULL
)
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw add operator class my_op_class using btree
;
----------
[BLOCK] do $$
declare x var_record not null;  -- fail
begin
  raise notice 'x = %', x;
end$$
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_24.pg_temp_26683_2 ADD COLUMN newdata pg_temp_24.pg_temp_26683
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER account_view_insert
    INSTEAD OF INSERT ON split_trigger_probe.account_view
    FOR EACH ROW
    EXECUTE FUNCTION split_trigger_probe.view_insert_row()
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER d15_update_trig
  AFTER UPDATE ON part_d_15_20 for each statement execute procedure trigfunc()
;
----------
[CREATE_PROG_OBJ] /* contrib/pg_buffercache/pg_buffercache--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION pg_buffercache_summary(
    OUT buffers_used int4,
    OUT buffers_unused int4,
    OUT buffers_dirty int4,
    OUT buffers_pinned int4,
    OUT usagecount_avg float8)
AS '$libdir/pg_buffercache', 'pg_buffercache_summary'
LANGUAGE C PARALLEL SAFE
;
----------
[DROP_VIEW] -- error

DROP VIEW vac_option_tab_counts
;
----------
[CREATE_TABLE] --
-- ISO-8859-5
--
CREATE TABLE iso8859_5_inputs (inbytes bytea, description text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."ks_test_normal_random"() RETURNS boolean
    LANGUAGE "plpgsql"
    AS $$
DECLARE
  n int := 1000;        -- Number of samples
  c float8 := 1.94947;  -- Critical value for 99.9% confidence
  ok boolean;
BEGIN
  ok := (
    WITH samples AS (
      SELECT random_normal() r FROM generate_series(1, n) ORDER BY 1
    ), indexed_samples AS (
      SELECT (row_number() OVER())-1.0 i, r FROM samples
    )
    SELECT max(abs((1+erf(r/sqrt(2)))/2 - i/n)) < c / sqrt(n)
    FROM indexed_samples
  );
  RETURN ok;
END
$$
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.5
 * FOREIGN_DATA_WRAPPER_OPTIONS view
 */
CREATE VIEW foreign_data_wrapper_options AS
    SELECT foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_name AS sql_identifier) AS option_name,
           CAST((pg_options_to_table(w.fdwoptions)).option_value AS character_data) AS option_value
    FROM _pg_foreign_data_wrappers w
;
----------
[DROP_TRIGGER] drop trigger trg1 on trigpart2
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS tbl
;
----------
[CREATE_TYPE] CREATE TYPE ess_range_type AS RANGE (subtype = text)
;
----------
[ADD_INDEX] create index on idxpart_temp(a)
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX tbl_idx ALTER COLUMN 4 SET STATISTICS 1000
;
----------
[CREATE_TABLE] -- 2 new columns, first has default
CREATE TABLE t (id serial PRIMARY KEY, a int, b int, c int)
;
----------
[CREATE_TABLE] -- test case with whole-row Var in a check constraint
create table check_con_tbl (f1 int)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30929_index RENAME TO reind_con_toast_idx
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE fd_pt2 DETACH PARTITION fd_pt2_1
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rc_test" (
    "a" integer,
    "b" integer
)
;
----------
[ADD_INDEX] CREATE INDEX idx_child_embedding_hnsw ON split_cov.child_table USING hnsw (embedding vector_l2_ops)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_sub2 CONNECTION 'port=19675 host=/tmp/Lt9SsSrZvF dbname=postgres' PUBLICATION regress_pub2
;
----------
[CREATE_PROG_OBJ] create function perform_test_func() returns void as '
BEGIN
	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	PERFORM perform_simple_func(5);

	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	PERFORM perform_simple_func(50);

	IF FOUND then
		INSERT INTO perform_test VALUES (100, 100);
	END IF;

	RETURN;
END;' language plpgsql
;
----------
[DROP_TABLE] DROP TABLE num_exp_sub
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR !== (
        PROCEDURE = int8ne,
        LEFTARG = bigint,
        RIGHTARG = bigint,
        NEGATOR = ===,
        COMMUTATOR = !==
)
;
----------
[ADD_INDEX] CREATE INDEX hash_f8_index ON hash_f8_heap USING hash (random float8_ops)
  WITH (fillfactor=60)
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table SET LOGGED
;
----------
[ADMIN_TABLE] ANALYZE fkpart3.fk2
;
----------
[ADD_INDEX] create index on only idxpart2 ((a/b))
;
