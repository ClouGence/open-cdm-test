create table parent (a int)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
CREATE TABLE test2 AS SELECT generate_series(200, 300)
;
vacuum analyze pgbench_history
;
CREATE TABLE inhx (xx text DEFAULT 'text')
;
do $$
declare r ordered_int8s;
begin
  r.q1 := null;
  r.q2 := 43;
  r.q1 := 42;
  r.q2 := 41;  -- fail
end$$
;
ALTER TABLE pg_toast.pg_toast_33372 RENAME TO reind_con_toast
;
CREATE FUNCTION event_trigger_test()
RETURNS event_trigger AS $$
  BEGIN RAISE NOTICE 'event %: %', TG_EVENT, TG_TAG; END;
$$ LANGUAGE plpgsql
;
drop event trigger regress_event_trigger_end
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
ANALYZE public.dropcolumnchild
;
ALTER TABLE a_star* RENAME COLUMN a TO aa
;
CREATE VIEW xmlview4 AS SELECT xmlelement(name employee, xmlforest(name, age, salary as pay)) FROM emp
;
DROP SCHEMA test_func_dep2
;
CREATE TABLE harvest(fruits basket)
;
CREATE TABLE prt2_adv_p3_2 PARTITION OF prt2_adv FOR VALUES FROM (375) TO (500)
;
CREATE TABLE "public"."domcontest" (
    "col1" "public"."con"
)
;
create table test_3 (id integer primary key) inherits (id)
;
CREATE TABLE "public"."bt_i4_heap" (
    "seqno" integer,
    "random" integer
)
;
CREATE TABLE split_cov.child_table (
    id integer GENERATED ALWAYS AS IDENTITY,
    parent_id integer REFERENCES split_cov.parent_table(id),
    mood split_cov.mood,
    tags text[],
    doc jsonb,
    embedding vector(3),
    created_at timestamptz DEFAULT now(),
    amount numeric GENERATED ALWAYS AS (parent_id * 2) STORED
)
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_tablespace
;
CREATE MATERIALIZED VIEW "mvtest_mvschema"."mvtest_tvm" AS
 SELECT "mvtest_tv"."type",
    "mvtest_tv"."totamt"
   FROM "public"."mvtest_tv"
  ORDER BY "mvtest_tv"."type"
  WITH NO DATA
;
ALTER PUBLICATION testpub_forparted1 SET (publish='insert')
;
ALTER TABLE ONLY "generated_stored_tests"."gtest25"
    ADD CONSTRAINT "gtest25_pkey" PRIMARY KEY ("a")
;
ALTER TABLE itest3 ALTER COLUMN a TYPE int
;
create index on mcrparted3 (a, abs(b), c)
;
create table tpart_default (a int, j text, i int) partition by list (j)
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
-- test that disabling RI triggers works
create table trigtest2 (i int references trigtest(i) on delete cascade)
;
-- fail - duplicate publications
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION foo, testpub, foo WITH (connect = false)
;
--
-- test for ability to use a cartesian join when necessary
--

create temp table q1 as select 1 as q1
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub1
;
ALTER TABLE ONLY "public"."ihighway" INHERIT "public"."road"
;
CREATE FUNCTION "public"."test_ret_set_rec_dyn"(integer) RETURNS SETOF "record"
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
	retval RECORD;
BEGIN
	IF $1 > 10 THEN
		SELECT INTO retval 5, 10, 15;
		RETURN NEXT retval;
		RETURN NEXT retval;
	ELSE
		SELECT INTO retval 50, 5::numeric, 'xxx'::text;
		RETURN NEXT retval;
		RETURN NEXT retval;
	END IF;
	RETURN;
END;$_$
;
-- int4

CREATE TABLE TEMP_INT4 (f1 INT4)
;
CREATE TABLE itest15 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
CREATE MATERIALIZED VIEW "public"."tableam_tblmv_heap2" AS
 SELECT "f1"
   FROM "public"."tableam_tbl_heap2"
  WITH NO DATA
;
CREATE TABLE foo_part_d2 (b float8, c text, a int)
;
CREATE INDEX gtest22c_b_idx ON public.gtest22c USING btree (b)
;
CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest3,
ptest4) REFERENCES pktable)
;
create table tt4 (ay int, b int, q int)
;
create table boolpart_default partition of boolpart default
;
CREATE FUNCTION foo1(a int) RETURNS int AS $$ select $1 $$ LANGUAGE sql
;
ANALYZE pg_temp_13.pg_temp_25000
;
create function int8alias1cmp(int8, int8alias1) returns int
  strict immutable language internal as 'btint8cmp'
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, binary = true)
;
DROP TABLE part_hp
;
-- ok

-- string_agg, min, max bytea tests
create table bytea_test_table(v bytea)
;
CREATE VIEW sequences AS
    SELECT CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(nc.nspname AS sql_identifier) AS sequence_schema,
           CAST(c.relname AS sql_identifier) AS sequence_name,
           CAST(format_type(s.seqtypid, null) AS character_data) AS data_type,
           CAST(_pg_numeric_precision(s.seqtypid, -1) AS cardinal_number) AS numeric_precision,
           CAST(2 AS cardinal_number) AS numeric_precision_radix,
           CAST(0 AS cardinal_number) AS numeric_scale,
           CAST(s.seqstart AS character_data) AS start_value,
           CAST(s.seqmin AS character_data) AS minimum_value,
           CAST(s.seqmax AS character_data) AS maximum_value,
           CAST(s.seqincrement AS character_data) AS increment,
           CAST(CASE WHEN s.seqcycle THEN 'YES' ELSE 'NO' END AS yes_or_no) AS cycle_option
    FROM pg_namespace nc, pg_class c, pg_sequence s
    WHERE c.relnamespace = nc.oid
          AND c.relkind = 'S'
          AND NOT EXISTS (SELECT 1 FROM pg_depend WHERE classid = 'pg_class'::regclass AND objid = c.oid AND deptype = 'i')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND c.oid = s.seqrelid
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_sequence_privilege(c.oid, 'SELECT, UPDATE, USAGE') )
;
---------------------------------------------------
-- ISSN13:
CREATE FUNCTION btissn13cmp(issn13, issn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table parent (a int)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE test2 AS SELECT generate_series(200, 300)
;
----------
[ADMIN_TABLE] vacuum analyze pgbench_history
;
----------
[CREATE_TABLE] CREATE TABLE inhx (xx text DEFAULT 'text')
;
----------
[BLOCK] do $$
declare r ordered_int8s;
begin
  r.q1 := null;
  r.q2 := 43;
  r.q1 := 42;
  r.q2 := 41;  -- fail
end$$
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_33372 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION event_trigger_test()
RETURNS event_trigger AS $$
  BEGIN RAISE NOTICE 'event %: %', TG_EVENT, TG_TAG; END;
$$ LANGUAGE plpgsql
;
----------
[DROP_TRIGGER] drop event trigger regress_event_trigger_end
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_namespace
;
----------
[ADMIN_TABLE] ANALYZE public.dropcolumnchild
;
----------
[RENAME_COLUMN] ALTER TABLE a_star* RENAME COLUMN a TO aa
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW xmlview4 AS SELECT xmlelement(name employee, xmlforest(name, age, salary as pay)) FROM emp
;
----------
[DROP_SCHEMA] DROP SCHEMA test_func_dep2
;
----------
[CREATE_TABLE] CREATE TABLE harvest(fruits basket)
;
----------
[CREATE_TABLE] CREATE TABLE prt2_adv_p3_2 PARTITION OF prt2_adv FOR VALUES FROM (375) TO (500)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."domcontest" (
    "col1" "public"."con"
)
;
----------
[CREATE_TABLE] create table test_3 (id integer primary key) inherits (id)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."bt_i4_heap" (
    "seqno" integer,
    "random" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE split_cov.child_table (
    id integer GENERATED ALWAYS AS IDENTITY,
    parent_id integer REFERENCES split_cov.parent_table(id),
    mood split_cov.mood,
    tags text[],
    doc jsonb,
    embedding vector(3),
    created_at timestamptz DEFAULT now(),
    amount numeric GENERATED ALWAYS AS (parent_id * 2) STORED
)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_tablespace
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "mvtest_mvschema"."mvtest_tvm" AS
 SELECT "mvtest_tv"."type",
    "mvtest_tv"."totamt"
   FROM "public"."mvtest_tv"
  ORDER BY "mvtest_tv"."type"
  WITH NO DATA
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION testpub_forparted1 SET (publish='insert')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "generated_stored_tests"."gtest25"
    ADD CONSTRAINT "gtest25_pkey" PRIMARY KEY ("a")
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest3 ALTER COLUMN a TYPE int
;
----------
[ADD_INDEX] create index on mcrparted3 (a, abs(b), c)
;
----------
[CREATE_TABLE] create table tpart_default (a int, j text, i int) partition by list (j)
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_database
;
----------
[CREATE_TABLE] -- test that disabling RI triggers works
create table trigtest2 (i int references trigtest(i) on delete cascade)
;
----------
[CREATE_PUB_SUB] -- fail - duplicate publications
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION foo, testpub, foo WITH (connect = false)
;
----------
[CREATE_TABLE|SELECT] --
-- test for ability to use a cartesian join when necessary
--

create temp table q1 as select 1 as q1
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13279 host=/tmp/IqjY25k8GL dbname=postgres' PUBLICATION pub1
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "public"."ihighway" INHERIT "public"."road"
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_ret_set_rec_dyn"(integer) RETURNS SETOF "record"
    LANGUAGE "plpgsql"
    AS $_$
DECLARE
	retval RECORD;
BEGIN
	IF $1 > 10 THEN
		SELECT INTO retval 5, 10, 15;
		RETURN NEXT retval;
		RETURN NEXT retval;
	ELSE
		SELECT INTO retval 50, 5::numeric, 'xxx'::text;
		RETURN NEXT retval;
		RETURN NEXT retval;
	END IF;
	RETURN;
END;$_$
;
----------
[CREATE_TABLE] -- int4

CREATE TABLE TEMP_INT4 (f1 INT4)
;
----------
[CREATE_TABLE] CREATE TABLE itest15 (a int GENERATED ALWAYS AS IDENTITY, b text)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW "public"."tableam_tblmv_heap2" AS
 SELECT "f1"
   FROM "public"."tableam_tbl_heap2"
  WITH NO DATA
;
----------
[CREATE_TABLE] CREATE TABLE foo_part_d2 (b float8, c text, a int)
;
----------
[ADD_INDEX] CREATE INDEX gtest22c_b_idx ON public.gtest22c USING btree (b)
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 int, ptest2 inet, ptest3 int, ptest4 inet, PRIMARY KEY(ptest1, ptest2), FOREIGN KEY(ptest3,
ptest4) REFERENCES pktable)
;
----------
[CREATE_TABLE] create table tt4 (ay int, b int, q int)
;
----------
[CREATE_TABLE] create table boolpart_default partition of boolpart default
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION foo1(a int) RETURNS int AS $$ select $1 $$ LANGUAGE sql
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_25000
;
----------
[CREATE_PROG_OBJ] create function int8alias1cmp(int8, int8alias1) returns int
  strict immutable language internal as 'btint8cmp'
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, binary = true)
;
----------
[DROP_TABLE] DROP TABLE part_hp
;
----------
[CREATE_TABLE] -- ok

-- string_agg, min, max bytea tests
create table bytea_test_table(v bytea)
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW sequences AS
    SELECT CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(nc.nspname AS sql_identifier) AS sequence_schema,
           CAST(c.relname AS sql_identifier) AS sequence_name,
           CAST(format_type(s.seqtypid, null) AS character_data) AS data_type,
           CAST(_pg_numeric_precision(s.seqtypid, -1) AS cardinal_number) AS numeric_precision,
           CAST(2 AS cardinal_number) AS numeric_precision_radix,
           CAST(0 AS cardinal_number) AS numeric_scale,
           CAST(s.seqstart AS character_data) AS start_value,
           CAST(s.seqmin AS character_data) AS minimum_value,
           CAST(s.seqmax AS character_data) AS maximum_value,
           CAST(s.seqincrement AS character_data) AS increment,
           CAST(CASE WHEN s.seqcycle THEN 'YES' ELSE 'NO' END AS yes_or_no) AS cycle_option
    FROM pg_namespace nc, pg_class c, pg_sequence s
    WHERE c.relnamespace = nc.oid
          AND c.relkind = 'S'
          AND NOT EXISTS (SELECT 1 FROM pg_depend WHERE classid = 'pg_class'::regclass AND objid = c.oid AND deptype = 'i')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND c.oid = s.seqrelid
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_sequence_privilege(c.oid, 'SELECT, UPDATE, USAGE') )
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISSN13:
CREATE FUNCTION btissn13cmp(issn13, issn13)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
