CREATE TABLE test_constraints (id int, val1 varchar, val2 int, UNIQUE(val1, val2))
;
CREATE TABLE itest2 (a bigint generated always as identity, b text)
;
CREATE TABLE accounts (accountid text PRIMARY KEY, balance numeric not null,
   balance2 numeric GENERATED ALWAYS AS (balance * 2) STORED)
;
CREATE TEMP TABLE tt1 ( tt1_id int4, joincol int4 )
;
--    P    P        N    N
-- should ERROR: we have no way to resolve S
CREATE AGGREGATE myaggp17a(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
CREATE OPERATOR ?| (
	LEFTARG = hstore,
	RIGHTARG = text[],
	PROCEDURE = exists_any,
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
-- ok
CREATE MATERIALIZED VIEW mvtest_mv_v_2 (ii) AS SELECT i, j FROM mvtest_v
;
-- ERROR
IMPORT FOREIGN SCHEMA s1 EXCEPT (t1, t2) FROM SERVER s9 INTO public
OPTIONS (option1 'value1', option2 'value2')
;
alter table atacc2 add foreign key (id) references atacc1("........pg.dropped.1........")
;
CREATE FUNCTION issn_in(cstring)
	RETURNS issn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
CREATE OR REPLACE PROCEDURE in_out(i int, i2 OUT int, i3 INOUT int) AS $$
DECLARE
  r int;
BEGIN
  i2 := i;
  i3 := i3 + i;
END; $$ LANGUAGE plpgsql
;
DROP TABLE test_temp
;
-- set PUBLISH_VIA_PARTITION_ROOT to true
-- can use row filter for partitioned table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=1)
;
CREATE SUBSCRIPTION regress_testsub3 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false)
;
create view tt23v (col_a, col_b) as
select q1 as other_name1, q2 as other_name2 from int8_tbl
union
select 42, 43
;
--
--
--
-- interval ops
--
--
--

CREATE FUNCTION gbt_intv_consistent(internal,interval,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- cleanup
drop table donothingbrtrig_test
;
-- fail - streaming must be boolean
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = foo)
;
--util functions

CREATE FUNCTION subltree(ltree,int4,int4)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE part_c_100_200 DROP COLUMN e, DROP COLUMN c, DROP COLUMN a
;
CREATE TABLE mtarget (a int, b text)
;
CREATE FUNCTION ltree_consistent(internal,ltree,int2,oid,internal)
RETURNS bool as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
VACUUM (PARALLEL 0) pg_catalog.pg_aggregate
;
drop domain vchar4 cascade
;
CREATE FUNCTION dblink_fetch (text, text, int)
RETURNS setof record
AS '$libdir/dblink','dblink_fetch'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
DROP TABLE ctas_nodata_2
;
CREATE FUNCTION "public"."test_table_func_row"() RETURNS SETOF "public"."found_test_tbl"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
	row found_test_tbl%ROWTYPE;
BEGIN
	FOR row IN select * from found_test_tbl LOOP
		RETURN NEXT row;
	END LOOP;
	RETURN;
END;$$
;
create table another (f1 int, f2 text, f3 text)
;
drop table iocdu_tt_parted
;
-- Show visibility map information for each block in a relation.
CREATE FUNCTION pg_visibility_map(regclass, blkno OUT bigint,
								  all_visible OUT boolean,
								  all_frozen OUT boolean)
RETURNS SETOF record
AS '$libdir/pg_visibility', 'pg_visibility_map_rel'
LANGUAGE C STRICT
;
CREATE TABLE pt1_2 PARTITION OF pt FOR VALUES IN (1, 2) PARTITION BY LIST (f1)
;
SECURITY LABEL ON TABLE foo_ptbl
	IS 'system_u:object_r:sepgsql_regtest_foo_table_t:s0'
;
CREATE FUNCTION set(tabname name) RETURNS VOID
AS $$
BEGIN
  UPDATE m
  SET id = (SELECT c.relfilenode
            FROM pg_class AS c, pg_namespace AS s
            WHERE c.relname = tabname
                AND c.relnamespace = s.oid
                AND s.nspname = 'fast_default');
END;
$$ LANGUAGE 'plpgsql'
;
drop table pktable
;
-- fails because NOT NULL is not dropped
ALTER TABLE itest4 ALTER COLUMN a DROP NOT NULL
;
VACUUM (FREEZE) pg_catalog.pg_type
;
TRUNCATE trunc_ins
;
VACUUM (FREEZE) information_schema.sql_implementation_info
;
DROP TRIGGER trans_updatetrig ON range_parted
;
create table float8range_test(f8r float8range, i int)
;
CREATE FUNCTION stfp(anyarray) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
-- ensure combine function parameters are checked
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = int4larger
)
;
create or replace function foreach_test(anyarray)
returns void as $$
declare x int;
begin
  foreach x slice 1 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
CREATE TABLE prt1_m (a int, b int, c int) PARTITION BY RANGE(a, ((a + b)/2))
;
drop view my_view
;
CREATE FUNCTION slice(hstore,text[])
RETURNS hstore
AS '$libdir/hstore','hstore_slice_to_hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE brin_summarize_bloom (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
/* contrib/adminpack/adminpack--1.1--2.0.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* ***********************************************
 * Administrative functions for PostgreSQL
 * *********************************************** */

/* generic file access functions */

CREATE OR REPLACE FUNCTION pg_catalog.pg_file_write(text, text, bool)
RETURNS bigint
AS '$libdir/adminpack', 'pg_file_write_v1_1'
LANGUAGE C VOLATILE STRICT
;
create view vv6 as select x,y,z,q from
  (tt11 join tt12 using(x)) join tt13 using(z)
;
VACUUM (PARALLEL 0) pg_catalog.pg_opclass
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE test_constraints (id int, val1 varchar, val2 int, UNIQUE(val1, val2))
;
----------
[CREATE_TABLE] CREATE TABLE itest2 (a bigint generated always as identity, b text)
;
----------
[CREATE_TABLE] CREATE TABLE accounts (accountid text PRIMARY KEY, balance numeric not null,
   balance2 numeric GENERATED ALWAYS AS (balance * 2) STORED)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE tt1 ( tt1_id int4, joincol int4 )
;
----------
[CREATE_PROG_OBJ] --    P    P        N    N
-- should ERROR: we have no way to resolve S
CREATE AGGREGATE myaggp17a(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ?| (
	LEFTARG = hstore,
	RIGHTARG = text[],
	PROCEDURE = exists_any,
	RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[CREATE_VIEW(SELECT)] -- ok
CREATE MATERIALIZED VIEW mvtest_mv_v_2 (ii) AS SELECT i, j FROM mvtest_v
;
----------
[CREATE_TABLE] -- ERROR
IMPORT FOREIGN SCHEMA s1 EXCEPT (t1, t2) FROM SERVER s9 INTO public
OPTIONS (option1 'value1', option2 'value2')
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] alter table atacc2 add foreign key (id) references atacc1("........pg.dropped.1........")
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION issn_in(cstring)
	RETURNS issn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE PROCEDURE in_out(i int, i2 OUT int, i3 INOUT int) AS $$
DECLARE
  r int;
BEGIN
  i2 := i;
  i3 := i3 + i;
END; $$ LANGUAGE plpgsql
;
----------
[DROP_TABLE] DROP TABLE test_temp
;
----------
[ALTER_PUB_SUB] -- set PUBLISH_VIA_PARTITION_ROOT to true
-- can use row filter for partitioned table
ALTER PUBLICATION testpub6 SET (PUBLISH_VIA_PARTITION_ROOT=1)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub3 CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (slot_name = NONE, connect = false)
;
----------
[CREATE_VIEW(SELECT)] create view tt23v (col_a, col_b) as
select q1 as other_name1, q2 as other_name2 from int8_tbl
union
select 42, 43
;
----------
[CREATE_PROG_OBJ] --
--
--
-- interval ops
--
--
--

CREATE FUNCTION gbt_intv_consistent(internal,interval,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[DROP_TABLE] -- cleanup
drop table donothingbrtrig_test
;
----------
[CREATE_PUB_SUB] -- fail - streaming must be boolean
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, streaming = foo)
;
----------
[CREATE_PROG_OBJ] --util functions

CREATE FUNCTION subltree(ltree,int4,int4)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE part_c_100_200 DROP COLUMN e, DROP COLUMN c, DROP COLUMN a
;
----------
[CREATE_TABLE] CREATE TABLE mtarget (a int, b text)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_consistent(internal,ltree,int2,oid,internal)
RETURNS bool as '$libdir/ltree' LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_aggregate
;
----------
[DROP_TYPE] drop domain vchar4 cascade
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_fetch (text, text, int)
RETURNS setof record
AS '$libdir/dblink','dblink_fetch'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[DROP_TABLE] DROP TABLE ctas_nodata_2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."test_table_func_row"() RETURNS SETOF "public"."found_test_tbl"
    LANGUAGE "plpgsql"
    AS $$
DECLARE
	row found_test_tbl%ROWTYPE;
BEGIN
	FOR row IN select * from found_test_tbl LOOP
		RETURN NEXT row;
	END LOOP;
	RETURN;
END;$$
;
----------
[CREATE_TABLE] create table another (f1 int, f2 text, f3 text)
;
----------
[DROP_TABLE] drop table iocdu_tt_parted
;
----------
[CREATE_PROG_OBJ] -- Show visibility map information for each block in a relation.
CREATE FUNCTION pg_visibility_map(regclass, blkno OUT bigint,
								  all_visible OUT boolean,
								  all_frozen OUT boolean)
RETURNS SETOF record
AS '$libdir/pg_visibility', 'pg_visibility_map_rel'
LANGUAGE C STRICT
;
----------
[CREATE_TABLE] CREATE TABLE pt1_2 PARTITION OF pt FOR VALUES IN (1, 2) PARTITION BY LIST (f1)
;
----------
[SECURITY_LABEL] SECURITY LABEL ON TABLE foo_ptbl
	IS 'system_u:object_r:sepgsql_regtest_foo_table_t:s0'
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION set(tabname name) RETURNS VOID
AS $$
BEGIN
  UPDATE m
  SET id = (SELECT c.relfilenode
            FROM pg_class AS c, pg_namespace AS s
            WHERE c.relname = tabname
                AND c.relnamespace = s.oid
                AND s.nspname = 'fast_default');
END;
$$ LANGUAGE 'plpgsql'
;
----------
[DROP_TABLE] drop table pktable
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fails because NOT NULL is not dropped
ALTER TABLE itest4 ALTER COLUMN a DROP NOT NULL
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) pg_catalog.pg_type
;
----------
[TRUNCATE_TABLE] TRUNCATE trunc_ins
;
----------
[ADMIN_TABLE] VACUUM (FREEZE) information_schema.sql_implementation_info
;
----------
[DROP_TRIGGER] DROP TRIGGER trans_updatetrig ON range_parted
;
----------
[CREATE_TABLE] create table float8range_test(f8r float8range, i int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION stfp(anyarray) RETURNS anyarray AS
'select $1' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] -- ensure combine function parameters are checked
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	serialfunc = numeric_avg_serialize,
	deserialfunc = numeric_avg_deserialize,
	combinefunc = int4larger
)
;
----------
[CREATE_PROG_OBJ] create or replace function foreach_test(anyarray)
returns void as $$
declare x int;
begin
  foreach x slice 1 in array $1
  loop
    raise notice '%', x;
  end loop;
  end;
$$ language plpgsql
;
----------
[CREATE_TABLE] CREATE TABLE prt1_m (a int, b int, c int) PARTITION BY RANGE(a, ((a + b)/2))
;
----------
[DROP_VIEW] drop view my_view
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION slice(hstore,text[])
RETURNS hstore
AS '$libdir/hstore','hstore_slice_to_hstore'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE brin_summarize_bloom (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
----------
[CREATE_PROG_OBJ] /* contrib/adminpack/adminpack--1.1--2.0.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


/* ***********************************************
 * Administrative functions for PostgreSQL
 * *********************************************** */

/* generic file access functions */

CREATE OR REPLACE FUNCTION pg_catalog.pg_file_write(text, text, bool)
RETURNS bigint
AS '$libdir/adminpack', 'pg_file_write_v1_1'
LANGUAGE C VOLATILE STRICT
;
----------
[CREATE_VIEW(SELECT)] create view vv6 as select x,y,z,q from
  (tt11 join tt12 using(x)) join tt13 using(z)
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_opclass
;
