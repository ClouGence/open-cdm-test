ANALYZE public.hash_f8_heap
;
CREATE FUNCTION gin_compare_prefix_int2(int2, int2, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
drop table parted_irreg_ancestor
;
ALTER TABLE inht1 RENAME aa TO aaa
;
CREATE FUNCTION "public"."sp_parallel_restricted"(integer) RETURNS integer
    LANGUAGE "plpgsql" PARALLEL RESTRICTED
    AS $_$begin return $1; end$_$
;
DROP TRIGGER gtest3 ON gtest26
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_transform
;
--
-- grouping sets
--

-- test data sources

create temp view gstest1(a,b,v)
  as values (1,1,10),(1,1,11),(1,2,12),(1,2,13),(1,3,14),
            (2,3,15),
            (3,3,16),(3,4,17),
            (4,1,18),(4,1,19)
;
DROP TABLE tab_rule_stats, tab_rule_stats_2
;
DROP DATABASE test
;
ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc2
;
drop trigger trig_row_before_insert on rem2
;
-- fail, connection string does not parse
CREATE SUBSCRIPTION regress_testsub5 CONNECTION 'i_dont_exist=param' PUBLICATION testpub
;
ALTER TABLE fk ADD FOREIGN KEY (a) REFERENCES pk
;
ANALYZE pg_temp_11.pg_temp_25449_2
;
ALTER INDEX pg_toast.pg_toast_30726_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION "public"."check_foreign_key"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/refint.so', 'check_foreign_key'
;
create rule qqq as on delete to copydml_test where old.t <> 'f' do instead insert into copydml_test default values
;
CREATE TABLE PKTABLE (ptest1 int, ptest2 int, UNIQUE(ptest1, ptest2))
;
CREATE INDEX dupindexcols_i ON dupindexcols (f1, id, f1 text_pattern_ops)
;
CREATE VIEW ro_view18 AS SELECT * FROM (VALUES(1)) AS tmp(a)
;
ALTER FUNCTION gbt_intv_penalty(internal, internal, internal) PARALLEL SAFE
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=25075 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION pub1
;
create table idxpart (a int4range, b int4range, c int4range) partition by range (a, b)
;
-- Note: vacuum_in_leader_small_index can apply deduplication, making it ~3x
-- smaller than the other indexes
CREATE INDEX vacuum_in_leader_small_index ON parallel_vacuum_table((1))
;
-- Will not be created, and will not be handled by the
-- event trigger
CREATE SCHEMA IF NOT EXISTS baz
;
create function fcompos1(v compos) returns void as $$
insert into compos values (v);  -- fail
$$ language sql
;
CREATE OPERATOR CLASS bytea_ops
DEFAULT FOR TYPE bytea USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       byteacmp(bytea,bytea),
    FUNCTION        2       gin_extract_value_bytea(bytea, internal),
    FUNCTION        3       gin_extract_query_bytea(bytea, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_bytea(bytea,bytea,int2, internal),
STORAGE         bytea
;
ALTER INDEX pg_toast.pg_toast_30671_index RENAME TO reind_con_toast_idx
;
CREATE FUNCTION gbt_bytea_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
-- support functions for gist
CREATE FUNCTION gtrgm_consistent(internal,text,smallint,oid,internal)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE TABLE "public"."bt_txt_heap" (
    "seqno" "text",
    "random" integer
)
;
ALTER TABLE split_alter_table.base_table DROP CONSTRAINT IF EXISTS base_table_payload_check RESTRICT
;
create table parted_isvalid_tab (a int, b int) partition by range (a)
;
drop table base_tbl
;
-- The following views have dependencies that force them to appear out of order.

/*
 * 6.26
 * DATA_TYPE_PRIVILEGES view
 */

CREATE VIEW data_type_privileges AS
    SELECT CAST(current_database() AS sql_identifier) AS object_catalog,
           CAST(x.objschema AS sql_identifier) AS object_schema,
           CAST(x.objname AS sql_identifier) AS object_name,
           CAST(x.objtype AS character_data) AS object_type,
           CAST(x.objdtdid AS sql_identifier) AS dtd_identifier

    FROM
      (
        SELECT udt_schema, udt_name, 'USER-DEFINED TYPE'::text, dtd_identifier FROM attributes
        UNION ALL
        SELECT table_schema, table_name, 'TABLE'::text, dtd_identifier FROM columns
        UNION ALL
        SELECT domain_schema, domain_name, 'DOMAIN'::text, dtd_identifier FROM domains
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM parameters
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM routines
      ) AS x (objschema, objname, objtype, objdtdid)
;
CREATE CAST (inet AS citext)    WITH FUNCTION citext(inet)    AS ASSIGNMENT
;
ALTER TABLE gtest20b VALIDATE CONSTRAINT chk
;
CREATE TABLE tab_upgraded(id int)
;
ALTER TABLE loct SET (autovacuum_enabled = 'false')
;
CREATE TABLE unique_tbl (i int, t text)
;
ALTER TABLE fktable ADD CONSTRAINT fk_5_1
FOREIGN KEY (x5) REFERENCES pktable(id1)
;
DROP TABLE tab_new
;
VACUUM (PARALLEL 0) pg_catalog.pg_opfamily
;
/*
 * 6.39
 * PERIODS view
 */

-- feature not supported


/*
 * 6.40
 * PRIVATE_PARAMETERS view
 */

-- feature not supported


/*
 * 6.41
 * REFERENCED_TYPES view
 */

-- feature not supported


/*
 * 6.42
 * REFERENTIAL_CONSTRAINTS view
 */

CREATE VIEW referential_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(ncon.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(
             CASE WHEN npkc.nspname IS NULL THEN NULL
                  ELSE current_database() END
             AS sql_identifier) AS unique_constraint_catalog,
           CAST(npkc.nspname AS sql_identifier) AS unique_constraint_schema,
           CAST(pkc.conname AS sql_identifier) AS unique_constraint_name,

           CAST(
             CASE con.confmatchtype WHEN 'f' THEN 'FULL'
                                    WHEN 'p' THEN 'PARTIAL'
                                    WHEN 's' THEN 'NONE' END
             AS character_data) AS match_option,

           CAST(
             CASE con.confupdtype WHEN 'c' THEN 'CASCADE'
                                  WHEN 'n' THEN 'SET NULL'
                                  WHEN 'd' THEN 'SET DEFAULT'
                                  WHEN 'r' THEN 'RESTRICT'
                                  WHEN 'a' THEN 'NO ACTION' END
             AS character_data) AS update_rule,

           CAST(
             CASE con.confdeltype WHEN 'c' THEN 'CASCADE'
                                  WHEN 'n' THEN 'SET NULL'
                                  WHEN 'd' THEN 'SET DEFAULT'
                                  WHEN 'r' THEN 'RESTRICT'
                                  WHEN 'a' THEN 'NO ACTION' END
             AS character_data) AS delete_rule

    FROM (pg_namespace ncon
          INNER JOIN pg_constraint con ON ncon.oid = con.connamespace
          INNER JOIN pg_class c ON con.conrelid = c.oid AND con.contype = 'f')
         LEFT JOIN pg_depend d1  -- find constraint's dependency on an index
          ON d1.objid = con.oid AND d1.classid = 'pg_constraint'::regclass
             AND d1.refclassid = 'pg_class'::regclass AND d1.refobjsubid = 0
         LEFT JOIN pg_depend d2  -- find pkey/unique constraint for that index
          ON d2.refclassid = 'pg_constraint'::regclass
             AND d2.classid = 'pg_class'::regclass
             AND d2.objid = d1.refobjid AND d2.objsubid = 0
             AND d2.deptype = 'i'
         LEFT JOIN pg_constraint pkc ON pkc.oid = d2.refobjid
            AND pkc.contype IN ('p', 'u')
            AND pkc.conrelid = con.confrelid
         LEFT JOIN pg_namespace npkc ON pkc.connamespace = npkc.oid

    WHERE pg_has_role(c.relowner, 'USAGE')
          -- SELECT privilege omitted, per SQL standard
          OR has_table_privilege(c.oid, 'INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
          OR has_any_column_privilege(c.oid, 'INSERT, UPDATE, REFERENCES')
;
ANALYZE public.mlparted3
;
DROP FUNCTION functest_b_1
;
-- test handling of temp objects created by extensions
create extension test_ext8
;
VACUUM vacowned
;
CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~"
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE public.hash_f8_heap
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_compare_prefix_int2(int2, int2, int2, internal)
RETURNS int4
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[DROP_TABLE] drop table parted_irreg_ancestor
;
----------
[RENAME_COLUMN] ALTER TABLE inht1 RENAME aa TO aaa
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."sp_parallel_restricted"(integer) RETURNS integer
    LANGUAGE "plpgsql" PARALLEL RESTRICTED
    AS $_$begin return $1; end$_$
;
----------
[DROP_TRIGGER] DROP TRIGGER gtest3 ON gtest26
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_transform
;
----------
[CREATE_VIEW(SELECT)] --
-- grouping sets
--

-- test data sources

create temp view gstest1(a,b,v)
  as values (1,1,10),(1,1,11),(1,2,12),(1,2,13),(1,3,14),
            (2,3,15),
            (3,3,16),(3,4,17),
            (4,1,18),(4,1,19)
;
----------
[DROP_TABLE] DROP TABLE tab_rule_stats, tab_rule_stats_2
;
----------
[DROP_CATALOG] DROP DATABASE test
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR CLASS alt_opc1 USING hash RENAME TO alt_opc2
;
----------
[DROP_TRIGGER] drop trigger trig_row_before_insert on rem2
;
----------
[CREATE_PUB_SUB] -- fail, connection string does not parse
CREATE SUBSCRIPTION regress_testsub5 CONNECTION 'i_dont_exist=param' PUBLICATION testpub
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fk ADD FOREIGN KEY (a) REFERENCES pk
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_11.pg_temp_25449_2
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30726_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."check_foreign_key"() RETURNS "trigger"
    LANGUAGE "c"
    AS '/src/src/test/regress/refint.so', 'check_foreign_key'
;
----------
[CREATE_POLICY] create rule qqq as on delete to copydml_test where old.t <> 'f' do instead insert into copydml_test default values
;
----------
[CREATE_TABLE] CREATE TABLE PKTABLE (ptest1 int, ptest2 int, UNIQUE(ptest1, ptest2))
;
----------
[ADD_INDEX] CREATE INDEX dupindexcols_i ON dupindexcols (f1, id, f1 text_pattern_ops)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ro_view18 AS SELECT * FROM (VALUES(1)) AS tmp(a)
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_intv_penalty(internal, internal, internal) PARALLEL SAFE
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=25075 host=/tmp/sxNfZEUQ8y dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TABLE] create table idxpart (a int4range, b int4range, c int4range) partition by range (a, b)
;
----------
[ADD_INDEX] -- Note: vacuum_in_leader_small_index can apply deduplication, making it ~3x
-- smaller than the other indexes
CREATE INDEX vacuum_in_leader_small_index ON parallel_vacuum_table((1))
;
----------
[CREATE_SCHEMA] -- Will not be created, and will not be handled by the
-- event trigger
CREATE SCHEMA IF NOT EXISTS baz
;
----------
[CREATE_PROG_OBJ] create function fcompos1(v compos) returns void as $$
insert into compos values (v);  -- fail
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS bytea_ops
DEFAULT FOR TYPE bytea USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       byteacmp(bytea,bytea),
    FUNCTION        2       gin_extract_value_bytea(bytea, internal),
    FUNCTION        3       gin_extract_query_bytea(bytea, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_bytea(bytea,bytea,int2, internal),
STORAGE         bytea
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30671_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_bytea_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_PROG_OBJ] -- support functions for gist
CREATE FUNCTION gtrgm_consistent(internal,text,smallint,oid,internal)
RETURNS bool
AS '$libdir/pg_trgm'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE "public"."bt_txt_heap" (
    "seqno" "text",
    "random" integer
)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE split_alter_table.base_table DROP CONSTRAINT IF EXISTS base_table_payload_check RESTRICT
;
----------
[CREATE_TABLE] create table parted_isvalid_tab (a int, b int) partition by range (a)
;
----------
[DROP_TABLE] drop table base_tbl
;
----------
[CREATE_VIEW(SELECT)] -- The following views have dependencies that force them to appear out of order.

/*
 * 6.26
 * DATA_TYPE_PRIVILEGES view
 */

CREATE VIEW data_type_privileges AS
    SELECT CAST(current_database() AS sql_identifier) AS object_catalog,
           CAST(x.objschema AS sql_identifier) AS object_schema,
           CAST(x.objname AS sql_identifier) AS object_name,
           CAST(x.objtype AS character_data) AS object_type,
           CAST(x.objdtdid AS sql_identifier) AS dtd_identifier

    FROM
      (
        SELECT udt_schema, udt_name, 'USER-DEFINED TYPE'::text, dtd_identifier FROM attributes
        UNION ALL
        SELECT table_schema, table_name, 'TABLE'::text, dtd_identifier FROM columns
        UNION ALL
        SELECT domain_schema, domain_name, 'DOMAIN'::text, dtd_identifier FROM domains
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM parameters
        UNION ALL
        SELECT specific_schema, specific_name, 'ROUTINE'::text, dtd_identifier FROM routines
      ) AS x (objschema, objname, objtype, objdtdid)
;
----------
[CREATE_PROG_OBJ] CREATE CAST (inet AS citext)    WITH FUNCTION citext(inet)    AS ASSIGNMENT
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE gtest20b VALIDATE CONSTRAINT chk
;
----------
[CREATE_TABLE] CREATE TABLE tab_upgraded(id int)
;
----------
[ALTER_TABLE] ALTER TABLE loct SET (autovacuum_enabled = 'false')
;
----------
[CREATE_TABLE] CREATE TABLE unique_tbl (i int, t text)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_5_1
FOREIGN KEY (x5) REFERENCES pktable(id1)
;
----------
[DROP_TABLE] DROP TABLE tab_new
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 0) pg_catalog.pg_opfamily
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.39
 * PERIODS view
 */

-- feature not supported


/*
 * 6.40
 * PRIVATE_PARAMETERS view
 */

-- feature not supported


/*
 * 6.41
 * REFERENCED_TYPES view
 */

-- feature not supported


/*
 * 6.42
 * REFERENTIAL_CONSTRAINTS view
 */

CREATE VIEW referential_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(ncon.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(
             CASE WHEN npkc.nspname IS NULL THEN NULL
                  ELSE current_database() END
             AS sql_identifier) AS unique_constraint_catalog,
           CAST(npkc.nspname AS sql_identifier) AS unique_constraint_schema,
           CAST(pkc.conname AS sql_identifier) AS unique_constraint_name,

           CAST(
             CASE con.confmatchtype WHEN 'f' THEN 'FULL'
                                    WHEN 'p' THEN 'PARTIAL'
                                    WHEN 's' THEN 'NONE' END
             AS character_data) AS match_option,

           CAST(
             CASE con.confupdtype WHEN 'c' THEN 'CASCADE'
                                  WHEN 'n' THEN 'SET NULL'
                                  WHEN 'd' THEN 'SET DEFAULT'
                                  WHEN 'r' THEN 'RESTRICT'
                                  WHEN 'a' THEN 'NO ACTION' END
             AS character_data) AS update_rule,

           CAST(
             CASE con.confdeltype WHEN 'c' THEN 'CASCADE'
                                  WHEN 'n' THEN 'SET NULL'
                                  WHEN 'd' THEN 'SET DEFAULT'
                                  WHEN 'r' THEN 'RESTRICT'
                                  WHEN 'a' THEN 'NO ACTION' END
             AS character_data) AS delete_rule

    FROM (pg_namespace ncon
          INNER JOIN pg_constraint con ON ncon.oid = con.connamespace
          INNER JOIN pg_class c ON con.conrelid = c.oid AND con.contype = 'f')
         LEFT JOIN pg_depend d1  -- find constraint's dependency on an index
          ON d1.objid = con.oid AND d1.classid = 'pg_constraint'::regclass
             AND d1.refclassid = 'pg_class'::regclass AND d1.refobjsubid = 0
         LEFT JOIN pg_depend d2  -- find pkey/unique constraint for that index
          ON d2.refclassid = 'pg_constraint'::regclass
             AND d2.classid = 'pg_class'::regclass
             AND d2.objid = d1.refobjid AND d2.objsubid = 0
             AND d2.deptype = 'i'
         LEFT JOIN pg_constraint pkc ON pkc.oid = d2.refobjid
            AND pkc.contype IN ('p', 'u')
            AND pkc.conrelid = con.confrelid
         LEFT JOIN pg_namespace npkc ON pkc.connamespace = npkc.oid

    WHERE pg_has_role(c.relowner, 'USAGE')
          -- SELECT privilege omitted, per SQL standard
          OR has_table_privilege(c.oid, 'INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER')
          OR has_any_column_privilege(c.oid, 'INSERT, UPDATE, REFERENCES')
;
----------
[ADMIN_TABLE] ANALYZE public.mlparted3
;
----------
[DROP_PROG_OBJ] DROP FUNCTION functest_b_1
;
----------
[CREATE_LIBRARY] -- test handling of temp objects created by extensions
create extension test_ext8
;
----------
[ADMIN_TABLE] VACUUM vacowned
;
----------
[CREATE_CATALOG] CREATE DATABASE "regression[\]^_`abcdefghijklmnopqrstuvwxyz{|}~"
;
