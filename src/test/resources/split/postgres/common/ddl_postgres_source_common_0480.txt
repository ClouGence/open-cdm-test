-- error
DROP DOMAIN d1, d2
;
-- brin_page_items might be parallel safe, because it seems to touch
-- only index metadata, but I don't think there's a point in risking it.
-- Likewise for gist_page_items.
ALTER FUNCTION brin_page_items(bytea, regclass) PARALLEL RESTRICTED
;
ANALYZE public.pht1_p2
;
create index �t�Ӹ��index2 on �t�Ӹ�� using hash (���q���Y)
;
COMMENT ON DATABASE "template1" IS 'default template for new databases'
;
CREATE TABLE reindex_after AS SELECT oid, relname, relfilenode, relkind
	FROM pg_class
	where relnamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'schema_to_reindex')
;
CREATE VIEW rw_view1 AS SELECT * FROM base_tbl_parent
;
ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP CONSTRAINT ft1_c1_check
;
create table parted_conflict_test_4_1 partition of parted_conflict_test_4 for values in (5)
;
drop table parted_sample, parted_sample_1, parted_sample_2
;
CREATE INDEX test1_a_btree ON test1 (a)
;
--
-- Test some other plan types in a partitionwise join (unfortunately,
-- we need larger tables to get the planner to choose these plan types)
--
create temp table prtx1 (a integer, b integer, c integer)
  partition by range (a)
;
CREATE TABLE "public"."ruletest_tbl2" (
    "a" integer,
    "b" integer
)
;
-- Test for proper handling of cast-expression caching

create function sql_to_date(integer) returns date as $$
select $1::text::date
$$ language sql immutable strict
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_ts_dict
;
ALTER TABLE ataddindex
  ALTER f1 SET DATA TYPE TEXT,
  ADD EXCLUDE ((f1 LIKE 'a') WITH =)
;
analyze fkest
;
-- restrict with null values
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, UNIQUE(ptest1, ptest2, ptest3) )
;
create index "testing_idx" on atacc1("........pg.dropped.1........")
;
/* contrib/hstore/hstore--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION hstore_hash_extended(hstore, int8)
RETURNS int8
AS '$libdir/hstore','hstore_hash_extended'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
-- SET NOT NULL puts both back
ALTER TABLE notnull_tbl1 ALTER a SET NOT NULL
;
DROP TABLE bools
;
create table testhstore1 (a integer, b text, c numeric, d float8, e hstestdom1)
;
CREATE VIEW pg_sequences AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS sequencename,
        pg_get_userbyid(C.relowner) AS sequenceowner,
        S.seqtypid::regtype AS data_type,
        S.seqstart AS start_value,
        S.seqmin AS min_value,
        S.seqmax AS max_value,
        S.seqincrement AS increment_by,
        S.seqcycle AS cycle,
        S.seqcache AS cache_size,
        CASE
            WHEN has_sequence_privilege(C.oid, 'SELECT,USAGE'::text)
                THEN pg_sequence_last_value(C.oid)
            ELSE NULL
        END AS last_value
    FROM pg_sequence S JOIN pg_class C ON (C.oid = S.seqrelid)
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE NOT pg_is_other_temp_schema(N.oid)
          AND relkind = 'S'
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_statistic_ext
;
--
-- pgp_pub_decrypt(data, key)
--
CREATE FUNCTION pgp_pub_decrypt(bytea, bytea)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
drop table temp_parted cascade
;
DROP TABLE ctlt1, ctlt2, ctlt3, ctlt4, ctlt12_storage, ctlt12_comments, ctlt1_inh, ctlt13_inh, ctlt13_like, ctlt_all, ctla, ctlb CASCADE
;
CREATE TABLE euc_jis_2004_inputs (inbytes bytea, description text)
;
-- Test brin_summarize_range
CREATE TABLE brin_summarize (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
CREATE TEMP TABLE pg_temp_18.pg_temp_26864_2 (tid pg_catalog.tid)
;
create index tt3i on tt3(f1)
;
-- cannot add NO INHERIT constraints to partitioned tables
CREATE TABLE partitioned (
	a int,
	CONSTRAINT check_a CHECK (a > 0) NO INHERIT
) PARTITION BY RANGE (a)
;
ANALYZE pg_catalog.pg_amproc
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic
;
--
-- bt_page_items()
--
CREATE FUNCTION bt_page_items(IN relname text, IN blkno int4,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
-- test WCO for partitions

CREATE TABLE child_tbl (a int, b int)
;
DROP TABLE ctas_nodata_3
;
-- use anymultirange in plpgsql
create function mr_polymorphic(i anyrange) returns anymultirange
  as $$ begin return multirange($1); end; $$ language plpgsql
;
CREATE COLLATION testx (provider = icu, locale = '@colStrength=primary;nonsense=yes')
;
CREATE PROCEDURE ptest10(IN a int, IN b int, IN c int)
LANGUAGE SQL AS $$ SELECT a + b - c $$
;
ALTER INDEX "fkpart6"."pk1_pkey" ATTACH PARTITION "fkpart6"."pk12_pkey"
;
ALTER INDEX dump_test.index_with_stats
						   ALTER COLUMN 1 SET STATISTICS 400
;
/* contrib/unaccent/unaccent--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION unaccent(regdictionary, text)
	RETURNS text
	AS '$libdir/unaccent', 'unaccent_dict'
	LANGUAGE C STABLE STRICT PARALLEL SAFE
;
DROP TABLE drop_stats_test_subxact
;
CREATE TABLE "public"."xacttest" (
    "a" smallint,
    "b" real
)
;
ALTER TABLE pg_temp_32.pg_temp_28237_2 ADD COLUMN newdata pg_temp_32.pg_temp_28237
;
ALTER TABLE ONLY "public"."mlparted" DROP COLUMN "........pg.dropped.4........"
;
CREATE TEMPORARY SEQUENCE sequence_test_temp1
;
CREATE FUNCTION gbt_text_consistent(internal,text,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
------------------------------------------------------------------------------------------
[DROP_TYPE] -- error
DROP DOMAIN d1, d2
;
----------
[ALTER_PROG_OBJ] -- brin_page_items might be parallel safe, because it seems to touch
-- only index metadata, but I don't think there's a point in risking it.
-- Likewise for gist_page_items.
ALTER FUNCTION brin_page_items(bytea, regclass) PARALLEL RESTRICTED
;
----------
[ADMIN_TABLE] ANALYZE public.pht1_p2
;
----------
[ADD_INDEX] create index �t�Ӹ��index2 on �t�Ӹ�� using hash (���q���Y)
;
----------
[COMMENT_CATALOG] COMMENT ON DATABASE "template1" IS 'default template for new databases'
;
----------
[CREATE_TABLE|SELECT] CREATE TABLE reindex_after AS SELECT oid, relname, relfilenode, relkind
	FROM pg_class
	where relnamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'schema_to_reindex')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS SELECT * FROM base_tbl_parent
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER FOREIGN TABLE IF EXISTS doesnt_exist_ft1 DROP CONSTRAINT ft1_c1_check
;
----------
[CREATE_TABLE] create table parted_conflict_test_4_1 partition of parted_conflict_test_4 for values in (5)
;
----------
[DROP_TABLE] drop table parted_sample, parted_sample_1, parted_sample_2
;
----------
[ADD_INDEX] CREATE INDEX test1_a_btree ON test1 (a)
;
----------
[CREATE_TABLE] --
-- Test some other plan types in a partitionwise join (unfortunately,
-- we need larger tables to get the planner to choose these plan types)
--
create temp table prtx1 (a integer, b integer, c integer)
  partition by range (a)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."ruletest_tbl2" (
    "a" integer,
    "b" integer
)
;
----------
[CREATE_PROG_OBJ] -- Test for proper handling of cast-expression caching

create function sql_to_date(integer) returns date as $$
select $1::text::date
$$ language sql immutable strict
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_ts_dict
;
----------
[ALTER_TABLE|ALTER_COLUMN|ADD_CONSTRAINT] ALTER TABLE ataddindex
  ALTER f1 SET DATA TYPE TEXT,
  ADD EXCLUDE ((f1 LIKE 'a') WITH =)
;
----------
[ADMIN_TABLE] analyze fkest
;
----------
[CREATE_TABLE] -- restrict with null values
CREATE TABLE PKTABLE ( ptest1 int, ptest2 int, ptest3 int, ptest4 text, UNIQUE(ptest1, ptest2, ptest3) )
;
----------
[ADD_INDEX] create index "testing_idx" on atacc1("........pg.dropped.1........")
;
----------
[CREATE_PROG_OBJ] /* contrib/hstore/hstore--1.5--1.6.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


CREATE FUNCTION hstore_hash_extended(hstore, int8)
RETURNS int8
AS '$libdir/hstore','hstore_hash_extended'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- SET NOT NULL puts both back
ALTER TABLE notnull_tbl1 ALTER a SET NOT NULL
;
----------
[DROP_TABLE] DROP TABLE bools
;
----------
[CREATE_TABLE] create table testhstore1 (a integer, b text, c numeric, d float8, e hstestdom1)
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW pg_sequences AS
    SELECT
        N.nspname AS schemaname,
        C.relname AS sequencename,
        pg_get_userbyid(C.relowner) AS sequenceowner,
        S.seqtypid::regtype AS data_type,
        S.seqstart AS start_value,
        S.seqmin AS min_value,
        S.seqmax AS max_value,
        S.seqincrement AS increment_by,
        S.seqcycle AS cycle,
        S.seqcache AS cache_size,
        CASE
            WHEN has_sequence_privilege(C.oid, 'SELECT,USAGE'::text)
                THEN pg_sequence_last_value(C.oid)
            ELSE NULL
        END AS last_value
    FROM pg_sequence S JOIN pg_class C ON (C.oid = S.seqrelid)
         LEFT JOIN pg_namespace N ON (N.oid = C.relnamespace)
    WHERE NOT pg_is_other_temp_schema(N.oid)
          AND relkind = 'S'
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_statistic_ext
;
----------
[CREATE_PROG_OBJ] --
-- pgp_pub_decrypt(data, key)
--
CREATE FUNCTION pgp_pub_decrypt(bytea, bytea)
RETURNS text
AS '$libdir/pgcrypto', 'pgp_pub_decrypt_text'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] drop table temp_parted cascade
;
----------
[DROP_TABLE] DROP TABLE ctlt1, ctlt2, ctlt3, ctlt4, ctlt12_storage, ctlt12_comments, ctlt1_inh, ctlt13_inh, ctlt13_like, ctlt_all, ctla, ctlb CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE euc_jis_2004_inputs (inbytes bytea, description text)
;
----------
[CREATE_TABLE] -- Test brin_summarize_range
CREATE TABLE brin_summarize (
    value int
) WITH (fillfactor=10, autovacuum_enabled=false)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_18.pg_temp_26864_2 (tid pg_catalog.tid)
;
----------
[ADD_INDEX] create index tt3i on tt3(f1)
;
----------
[CREATE_TABLE] -- cannot add NO INHERIT constraints to partitioned tables
CREATE TABLE partitioned (
	a int,
	CONSTRAINT check_a CHECK (a > 0) NO INHERIT
) PARTITION BY RANGE (a)
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_amproc
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_statistic
;
----------
[CREATE_PROG_OBJ] --
-- bt_page_items()
--
CREATE FUNCTION bt_page_items(IN relname text, IN blkno int4,
    OUT itemoffset smallint,
    OUT ctid tid,
    OUT itemlen smallint,
    OUT nulls bool,
    OUT vars bool,
    OUT data text)
RETURNS SETOF record
AS '$libdir/pageinspect', 'bt_page_items'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] -- test WCO for partitions

CREATE TABLE child_tbl (a int, b int)
;
----------
[DROP_TABLE] DROP TABLE ctas_nodata_3
;
----------
[CREATE_PROG_OBJ] -- use anymultirange in plpgsql
create function mr_polymorphic(i anyrange) returns anymultirange
  as $$ begin return multirange($1); end; $$ language plpgsql
;
----------
[CREATE_POLICY] CREATE COLLATION testx (provider = icu, locale = '@colStrength=primary;nonsense=yes')
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE ptest10(IN a int, IN b int, IN c int)
LANGUAGE SQL AS $$ SELECT a + b - c $$
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart6"."pk1_pkey" ATTACH PARTITION "fkpart6"."pk12_pkey"
;
----------
[ALTER_INDEX|ALTER_COLUMN] ALTER INDEX dump_test.index_with_stats
						   ALTER COLUMN 1 SET STATISTICS 400
;
----------
[CREATE_PROG_OBJ] /* contrib/unaccent/unaccent--1.1.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


CREATE FUNCTION unaccent(regdictionary, text)
	RETURNS text
	AS '$libdir/unaccent', 'unaccent_dict'
	LANGUAGE C STABLE STRICT PARALLEL SAFE
;
----------
[DROP_TABLE] DROP TABLE drop_stats_test_subxact
;
----------
[CREATE_TABLE] CREATE TABLE "public"."xacttest" (
    "a" smallint,
    "b" real
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE pg_temp_32.pg_temp_28237_2 ADD COLUMN newdata pg_temp_32.pg_temp_28237
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted" DROP COLUMN "........pg.dropped.4........"
;
----------
[CREATE_SEQUENCE] CREATE TEMPORARY SEQUENCE sequence_test_temp1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_text_consistent(internal,text,int2,oid,internal)
RETURNS bool
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
