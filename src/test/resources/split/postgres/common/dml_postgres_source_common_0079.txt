insert into arrtest_i values(4,'cat1',26)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10429', relminmxid = '1'
WHERE oid = '"public"."rule_and_refint_t1"'::pg_catalog.regclass
;
INSERT INTO VARCHAR_TBL (f1) VALUES ('cd')
;
INSERT INTO users VALUES (2, 'Ferko')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"mvtest_mvschema"."mvtest_tvm"'::pg_catalog.regclass
;
INSERT INTO tab_crash (a) SELECT i FROM generate_series(1, 5000) s(i)
;
INSERT INTO pa_source SELECT id, id * 10  FROM generate_series(1,14) AS id
;
INSERT INTO test_missing_target VALUES (0, 1, 'XXXX', 'A')
;
INSERT INTO part VALUES (-1,-1), (1,1), (2,NULL), (NULL,-2),(NULL,NULL)
;
UPDATE pgss_wal_tab SET b = 'bbb' WHERE a > 7
;
INSERT INTO tab_rep SELECT generate_series(1,50)
;
INSERT INTO ft2 (c1,c2,c3,c6) VALUES (1218, 818, 'ggg', '(--;') RETURNING *
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10294', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest0"'::pg_catalog.regclass
;
INSERT INTO public.ths_p1 VALUES (2, 102)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7651', relminmxid = '1'
WHERE oid = '"public"."sj_t3"'::pg_catalog.regclass
;
INSERT INTO TEMP_INT4 (f1)
  SELECT int4(f1) FROM INT2_TBL
;
insert into WSlot values ('WS.002.3b', '002', '', '')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18552', relminmxid = '3'
WHERE oid = '"public"."idxpart_another_1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15780', relminmxid = '3'
WHERE oid = '"public"."pt_part2"'::pg_catalog.regclass
;
-- with a conflict
INSERT INTO temporal_rng (id, valid_at) VALUES ('[1,2)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT DO NOTHING
;
INSERT INTO B (BID,AID) VALUES (2,1)
;
INSERT INTO sro_ptab VALUES (1), (2), (3)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14329', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_p3"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('7', 507)
;
DELETE FROM unique_tbl WHERE t = 'three'
;
INSERT INTO public.tht VALUES ('6', 56)
;
-- succeed
INSERT INTO circles VALUES('<(10,10), 10>', '<(0,0), 5>')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15854', relminmxid = '3'
WHERE oid = '"public"."comment_test"'::pg_catalog.regclass
;
INSERT INTO public.tht VALUES ('4', 724)
;
INSERT INTO public.tplain VALUES ('0', 20)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8961', relminmxid = '1'
WHERE oid = '"public"."datdba_only"'::pg_catalog.regclass
;
UPDATE ONLY "public"."fk_noparted_sn" SET "a" = NULL WHERE $1 OPERATOR(pg_catalog.=) "a"
;
-- ok - there's no replica identity, so any column list works
-- note: it fails anyway, just a bit later because UPDATE requires RI
UPDATE rf_tbl_abcd_nopk SET a = 1
;
INSERT INTO prem2 VALUES (202, 'barbar')
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15969', relminmxid = '3'
WHERE oid = '"public"."tt2"'::pg_catalog.regclass
;
UPDATE test_tab SET b = sha256(b) WHERE mod(a,2) = 0
;
INSERT INTO bootab VALUES(10)
;
INSERT INTO public.tplain VALUES ('0', 470)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12945', relminmxid = '3'
WHERE oid = '33665'
;
INSERT INTO public.mvtest_boxmv SELECT (diff.newdata).* FROM pg_temp_13.pg_temp_26072_2 diff WHERE tid IS NULL
;
UPDATE main_table SET a = NEW.a, b = NEW.b WHERE a = OLD.a AND b = OLD.b
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'y'
  AND attrelid = '"public"."tt6"'::pg_catalog.regclass
;
INSERT INTO test_uuid VALUES
	( '00000000-0000-0000-0000-000000000000' ),
	( '299bc99f-2f79-4e3e-bfea-2cbfd62a7c27' ),
	( '6264af33-0d43-4337-bf4e-43509b8a4be8' ),
	( 'ce41c936-6acb-4feb-8c91-852a673e5a5c' ),
	( 'd2ce731f-f2a8-4a2b-be37-8f0ba637427f' ),
	( 'ffffffff-ffff-ffff-ffff-ffffffffffff' )
;
INSERT INTO bttest_b SELECT * FROM generate_series(100000, 1, -1)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2582', relminmxid = '1'
WHERE oid = '"public"."b_star"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest20a"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('0', 890)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2066', relminmxid = '1'
WHERE oid = '"public"."slow_emp4000"'::pg_catalog.regclass
;
-- 10 frequent groups, each with 100 elements
INSERT INTO mcv_lists_partial (a, b, c)
     SELECT
         mod(i,10),
         mod(i,10),
         mod(i,10)
     FROM generate_series(0,999) s(i)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '19357'
;
------------------------------------------------------------------------------------------
[INSERT] insert into arrtest_i values(4,'cat1',26)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '10429', relminmxid = '1'
WHERE oid = '"public"."rule_and_refint_t1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO VARCHAR_TBL (f1) VALUES ('cd')
;
----------
[INSERT] INSERT INTO users VALUES (2, 'Ferko')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7573', relminmxid = '1'
WHERE oid = '"mvtest_mvschema"."mvtest_tvm"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO tab_crash (a) SELECT i FROM generate_series(1, 5000) s(i)
;
----------
[INSERT|SELECT] INSERT INTO pa_source SELECT id, id * 10  FROM generate_series(1,14) AS id
;
----------
[INSERT] INSERT INTO test_missing_target VALUES (0, 1, 'XXXX', 'A')
;
----------
[INSERT] INSERT INTO part VALUES (-1,-1), (1,1), (2,NULL), (NULL,-2),(NULL,NULL)
;
----------
[UPDATE] UPDATE pgss_wal_tab SET b = 'bbb' WHERE a > 7
;
----------
[INSERT|SELECT] INSERT INTO tab_rep SELECT generate_series(1,50)
;
----------
[INSERT|SELECT] INSERT INTO ft2 (c1,c2,c3,c6) VALUES (1218, 818, 'ggg', '(--;') RETURNING *
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '10294', relminmxid = '1'
WHERE oid = '"generated_virtual_tests"."gtest0"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (2, 102)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '7651', relminmxid = '1'
WHERE oid = '"public"."sj_t3"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] INSERT INTO TEMP_INT4 (f1)
  SELECT int4(f1) FROM INT2_TBL
;
----------
[INSERT] insert into WSlot values ('WS.002.3b', '002', '', '')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18552', relminmxid = '3'
WHERE oid = '"public"."idxpart_another_1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15780', relminmxid = '3'
WHERE oid = '"public"."pt_part2"'::pg_catalog.regclass
;
----------
[MERGE|SELECT] -- with a conflict
INSERT INTO temporal_rng (id, valid_at) VALUES ('[1,2)', daterange('2005-01-01', '2006-01-01')) ON CONFLICT DO NOTHING
;
----------
[INSERT|SELECT] INSERT INTO B (BID,AID) VALUES (2,1)
;
----------
[INSERT] INSERT INTO sro_ptab VALUES (1), (2), (3)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14329', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_p3"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 507)
;
----------
[DELETE] DELETE FROM unique_tbl WHERE t = 'three'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('6', 56)
;
----------
[INSERT] -- succeed
INSERT INTO circles VALUES('<(10,10), 10>', '<(0,0), 5>')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15854', relminmxid = '3'
WHERE oid = '"public"."comment_test"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 724)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 20)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8961', relminmxid = '1'
WHERE oid = '"public"."datdba_only"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE ONLY "public"."fk_noparted_sn" SET "a" = NULL WHERE $1 OPERATOR(pg_catalog.=) "a"
;
----------
[UPDATE] -- ok - there's no replica identity, so any column list works
-- note: it fails anyway, just a bit later because UPDATE requires RI
UPDATE rf_tbl_abcd_nopk SET a = 1
;
----------
[INSERT] INSERT INTO prem2 VALUES (202, 'barbar')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15969', relminmxid = '3'
WHERE oid = '"public"."tt2"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE test_tab SET b = sha256(b) WHERE mod(a,2) = 0
;
----------
[INSERT] INSERT INTO bootab VALUES(10)
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 470)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12945', relminmxid = '3'
WHERE oid = '33665'
;
----------
[INSERT|SELECT] INSERT INTO public.mvtest_boxmv SELECT (diff.newdata).* FROM pg_temp_13.pg_temp_26072_2 diff WHERE tid IS NULL
;
----------
[UPDATE] UPDATE main_table SET a = NEW.a, b = NEW.b WHERE a = OLD.a AND b = OLD.b
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'y'
  AND attrelid = '"public"."tt6"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO test_uuid VALUES
	( '00000000-0000-0000-0000-000000000000' ),
	( '299bc99f-2f79-4e3e-bfea-2cbfd62a7c27' ),
	( '6264af33-0d43-4337-bf4e-43509b8a4be8' ),
	( 'ce41c936-6acb-4feb-8c91-852a673e5a5c' ),
	( 'd2ce731f-f2a8-4a2b-be37-8f0ba637427f' ),
	( 'ffffffff-ffff-ffff-ffff-ffffffffffff' )
;
----------
[INSERT|SELECT] INSERT INTO bttest_b SELECT * FROM generate_series(100000, 1, -1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2582', relminmxid = '1'
WHERE oid = '"public"."b_star"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '"public"."gtest20a"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('0', 890)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '2066', relminmxid = '1'
WHERE oid = '"public"."slow_emp4000"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] -- 10 frequent groups, each with 100 elements
INSERT INTO mcv_lists_partial (a, b, c)
     SELECT
         mod(i,10),
         mod(i,10),
         mod(i,10)
     FROM generate_series(0,999) s(i)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6336', relminmxid = '1'
WHERE oid = '19357'
;
