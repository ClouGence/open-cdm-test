VACUUM (SKIP_LOCKED) pg_catalog.pg_attribute
;
create index on range_parted (a,b,c)
;
create temp table table_a(id integer)
;
CREATE OPERATOR CLASS macaddr8_ops
DEFAULT FOR TYPE macaddr8 USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       macaddr8_cmp(macaddr8, macaddr8),
    FUNCTION        2       gin_extract_value_macaddr8(macaddr8, internal),
    FUNCTION        3       gin_extract_query_macaddr8(macaddr8, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_macaddr8(macaddr8, macaddr8, int2, internal),
STORAGE         macaddr8
;
DROP TABLE pg_temp_2.pg_temp_28105_2, pg_temp_2.pg_temp_28105
;
CREATE INDEX clstrpart_idx ON clstrpart (a)
;
DO $$
DECLARE
  result integer;
BEGIN
MERGE INTO pa_target t
  USING pa_source s
  ON t.tid = s.sid
  WHEN MATCHED THEN
    UPDATE SET tid = tid + 1, balance = balance + delta, val = val || ' updated by merge'
  WHEN NOT MATCHED THEN
    INSERT VALUES (sid, delta, 'inserted by merge')
  WHEN NOT MATCHED BY SOURCE THEN
    UPDATE SET tid = 1, val = val || ' not matched by source';
GET DIAGNOSTICS result := ROW_COUNT;
RAISE NOTICE 'ROW_COUNT = %', result;
END;
$$
;
CREATE OR REPLACE RULE y_rule AS ON INSERT TO y DO INSTEAD NOTIFY foo
;
CREATE OPERATOR schema_op1.#*# (
   leftarg = int8,		
   procedure = factorial
)
;
create index on idxpart (abs(b))
;
VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_am
;
VACUUM (ANALYZE, SKIP_LOCKED) parted
;
--
-- Big5
--
CREATE TABLE big5_inputs (inbytes bytea, description text)
;
create table rtest_empmass (ename char(20), salary money)
;
VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_ts_template
;
CREATE FUNCTION do_set_block_offsets(
blkno bigint,
offsets int2[])
RETURNS bigint STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
CREATE TABLE "public"."fk_partitioned_fk_2_1" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
CREATE TABLE lt1 (a INT) PARTITION BY RANGE (a)
;
DROP table idxpart, idxpart_two
;
CREATE FUNCTION isnlt(ean13, ismn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP EXTENSION pg_walinspect
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
create sequence test_sequence start 72057594037927937
;
ALTER TABLE itest13 ADD COLUMN c int GENERATED BY DEFAULT AS IDENTITY
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
CREATE TABLE import_source.t2 (c1 int default 42, c2 varchar NULL, c3 text collate "POSIX")
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=24605 host=/tmp/yJVcnPx7vH dbname=postgres' PUBLICATION pub1
;
CREATE FUNCTION vc (text) RETURNS text LANGUAGE sql
    AS 'select $1::varchar'
;
ANALYZE public.quad_box_tbl_ord_seq2
;
CLUSTER maintain_test USING maintain_test_a_idx
;
create temp table t2(f1 bigint, f2 oid)
;
ALTER TABLE ONLY "public"."comment_test"
    ADD CONSTRAINT "comment_test_pk" PRIMARY KEY ("id")
;
VACUUM (SKIP_DATABASE_STATS) "Bar".baz
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
-- fail
alter table defc alter column f1 set default 1
;
CREATE DATABASE template0 IS_TEMPLATE = true ALLOW_CONNECTIONS = false OID = 4 STRATEGY = file_copy
;
CREATE TABLE "generated_stored_tests"."gtestxx_4" (
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    "a" integer NOT NULL
)
;
ALTER TABLE fd_pt2_1 ALTER c1 DROP NOT NULL
;
create function My_Table_Check ( ) returns trigger as $test$    BEGIN	INSERT INTO Log VALUES(TG_NAME, TG_WHEN);	RETURN NEW;    END; $test$ language plpgsql
;
ALTER TABLE fd_pt1 DROP CONSTRAINT fd_pt1chk2 CASCADE
;
CREATE TABLE pgss_schema_1.tab_search_diff_2 (a int)
;
ALTER OPERATOR FAMILY dump_test.op_family USING btree ADD
						 OPERATOR 1 <(bigint,int4),
						 OPERATOR 2 <=(bigint,int4),
						 OPERATOR 3 =(bigint,int4),
						 OPERATOR 4 >=(bigint,int4),
						 OPERATOR 5 >(bigint,int4),
						 FUNCTION 1 (int4, int4) btint4cmp(int4,int4),
						 FUNCTION 2 (int4, int4) btint4sortsupport(internal),
						 FUNCTION 4 (int4, int4) btequalimage(oid)
;
-- Repeated column may be part of an updatable view
CREATE VIEW ro_view17 AS SELECT * FROM ro_view1
;
-- make sure we can drop a constraint on the parent but it remains on the child
CREATE TABLE test_drop_constr_parent (c text CHECK (c IS NOT NULL))
;
CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=15683 host=/tmp/7RF1JxeTxg dbname=postgres' PUBLICATION pub5
;
CREATE EXTENSION test_ext_req_schema1 SCHEMA has$dollar
;
CREATE FUNCTION _intbig_out(intbig_gkey)
RETURNS cstring
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ANALYZE pg_temp_13.pg_temp_26383
;
CREATE TABLE test3 (c int)
;
DROP TABLE newly_indexed
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (SKIP_LOCKED) pg_catalog.pg_attribute
;
----------
[ADD_INDEX] create index on range_parted (a,b,c)
;
----------
[CREATE_TABLE] create temp table table_a(id integer)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR CLASS macaddr8_ops
DEFAULT FOR TYPE macaddr8 USING gin
AS
    OPERATOR        1       <,
    OPERATOR        2       <=,
    OPERATOR        3       =,
    OPERATOR        4       >=,
    OPERATOR        5       >,
    FUNCTION        1       macaddr8_cmp(macaddr8, macaddr8),
    FUNCTION        2       gin_extract_value_macaddr8(macaddr8, internal),
    FUNCTION        3       gin_extract_query_macaddr8(macaddr8, internal, int2, internal, internal),
    FUNCTION        4       gin_btree_consistent(internal, int2, anyelement, int4, internal, internal),
    FUNCTION        5       gin_compare_prefix_macaddr8(macaddr8, macaddr8, int2, internal),
STORAGE         macaddr8
;
----------
[DROP_TABLE] DROP TABLE pg_temp_2.pg_temp_28105_2, pg_temp_2.pg_temp_28105
;
----------
[ADD_INDEX] CREATE INDEX clstrpart_idx ON clstrpart (a)
;
----------
[BLOCK] DO $$
DECLARE
  result integer;
BEGIN
MERGE INTO pa_target t
  USING pa_source s
  ON t.tid = s.sid
  WHEN MATCHED THEN
    UPDATE SET tid = tid + 1, balance = balance + delta, val = val || ' updated by merge'
  WHEN NOT MATCHED THEN
    INSERT VALUES (sid, delta, 'inserted by merge')
  WHEN NOT MATCHED BY SOURCE THEN
    UPDATE SET tid = 1, val = val || ' not matched by source';
GET DIAGNOSTICS result := ROW_COUNT;
RAISE NOTICE 'ROW_COUNT = %', result;
END;
$$
;
----------
[CREATE_POLICY] CREATE OR REPLACE RULE y_rule AS ON INSERT TO y DO INSTEAD NOTIFY foo
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR schema_op1.#*# (
   leftarg = int8,		
   procedure = factorial
)
;
----------
[ADD_INDEX] create index on idxpart (abs(b))
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, SKIP_LOCKED) pg_catalog.pg_am
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE, SKIP_LOCKED) parted
;
----------
[CREATE_TABLE] --
-- Big5
--
CREATE TABLE big5_inputs (inbytes bytea, description text)
;
----------
[CREATE_TABLE] create table rtest_empmass (ename char(20), salary money)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FULL) pg_catalog.pg_ts_template
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION do_set_block_offsets(
blkno bigint,
offsets int2[])
RETURNS bigint STRICT PARALLEL UNSAFE
AS '$libdir/test_tidstore' LANGUAGE C
;
----------
[CREATE_TABLE] CREATE TABLE "public"."fk_partitioned_fk_2_1" (
    "a" integer DEFAULT NULL,
    "b" integer DEFAULT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE lt1 (a INT) PARTITION BY RANGE (a)
;
----------
[DROP_TABLE] DROP table idxpart, idxpart_two
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnlt(ean13, ismn13)
	RETURNS boolean
	AS 'int8lt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[DROP_LIBRARY] DROP EXTENSION pg_walinspect
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = isbn13,
	RIGHTARG = isbn,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[CREATE_SEQUENCE] create sequence test_sequence start 72057594037927937
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER TABLE itest13 ADD COLUMN c int GENERATED BY DEFAULT AS IDENTITY
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amproc
;
----------
[CREATE_TABLE] CREATE TABLE import_source.t2 (c1 int default 42, c2 varchar NULL, c3 text collate "POSIX")
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=24605 host=/tmp/yJVcnPx7vH dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION vc (text) RETURNS text LANGUAGE sql
    AS 'select $1::varchar'
;
----------
[ADMIN_TABLE] ANALYZE public.quad_box_tbl_ord_seq2
;
----------
[ADMIN_TABLE] CLUSTER maintain_test USING maintain_test_a_idx
;
----------
[CREATE_TABLE] create temp table t2(f1 bigint, f2 oid)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."comment_test"
    ADD CONSTRAINT "comment_test_pk" PRIMARY KEY ("id")
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) "Bar".baz
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_default_acl
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- fail
alter table defc alter column f1 set default 1
;
----------
[CREATE_CATALOG] CREATE DATABASE template0 IS_TEMPLATE = true ALLOW_CONNECTIONS = false OID = 4 STRATEGY = file_copy
;
----------
[CREATE_TABLE] CREATE TABLE "generated_stored_tests"."gtestxx_4" (
    "b" integer GENERATED ALWAYS AS (("a" * 2)) STORED,
    "a" integer NOT NULL
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt2_1 ALTER c1 DROP NOT NULL
;
----------
[CREATE_PROG_OBJ] create function My_Table_Check ( ) returns trigger as $test$    BEGIN	INSERT INTO Log VALUES(TG_NAME, TG_WHEN);	RETURN NEW;    END; $test$ language plpgsql
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE fd_pt1 DROP CONSTRAINT fd_pt1chk2 CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE pgss_schema_1.tab_search_diff_2 (a int)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY dump_test.op_family USING btree ADD
						 OPERATOR 1 <(bigint,int4),
						 OPERATOR 2 <=(bigint,int4),
						 OPERATOR 3 =(bigint,int4),
						 OPERATOR 4 >=(bigint,int4),
						 OPERATOR 5 >(bigint,int4),
						 FUNCTION 1 (int4, int4) btint4cmp(int4,int4),
						 FUNCTION 2 (int4, int4) btint4sortsupport(internal),
						 FUNCTION 4 (int4, int4) btequalimage(oid)
;
----------
[CREATE_VIEW(SELECT)] -- Repeated column may be part of an updatable view
CREATE VIEW ro_view17 AS SELECT * FROM ro_view1
;
----------
[CREATE_TABLE] -- make sure we can drop a constraint on the parent but it remains on the child
CREATE TABLE test_drop_constr_parent (c text CHECK (c IS NOT NULL))
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub5_1 CONNECTION 'port=15683 host=/tmp/7RF1JxeTxg dbname=postgres' PUBLICATION pub5
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_ext_req_schema1 SCHEMA has$dollar
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _intbig_out(intbig_gkey)
RETURNS cstring
AS '$libdir/_int'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_13.pg_temp_26383
;
----------
[CREATE_TABLE] CREATE TABLE test3 (c int)
;
----------
[DROP_TABLE] DROP TABLE newly_indexed
;
