-- Changing NOT NULL constraint of identity columns is not allowed
ALTER TABLE pitest1_p1 ALTER COLUMN f3 DROP NOT NULL
;
drop trigger mlparted11_trig on mlparted11
;
CREATE FUNCTION "public"."drop_sql_command"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
RAISE NOTICE '% - sql_drop', tg_tag;
END;
$$
;
CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=27174 host=/tmp/1ZUk26WGH4 dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (streaming = on, two_phase = on)
;
ALTER INDEX alter_idx_rename_test_parted RENAME TO alter_idx_rename_test_parted_2
;
-- temporary tables are ignored by pg_filenode_relation().
CREATE TEMP TABLE relation_filenode_check(c1 int)
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_attribute
;
CREATE INDEX "brin_summarize_multi_idx" ON "public"."brin_summarize_multi" USING "brin" ("value") WITH ("pages_per_range"='2')
;
CREATE TABLE "testxmlschema"."test1" (
    "a" integer,
    "b" "text"
)
;
ALTER TABLE ONLY "public"."mlparted4" DROP COLUMN "........pg.dropped.5........"
;
-- test a tricky case: we can elide firing the FK check trigger during
-- an UPDATE if the UPDATE did not change the foreign key
-- field. However, we can't do this if our transaction was the one that
-- created the updated row and the trigger is deferred, since our UPDATE
-- will have invalidated the original newly-inserted tuple, and therefore
-- cause the on-INSERT RI trigger not to be fired.

CREATE TEMP TABLE pktable (
    id int primary key,
    other int
)
;
drop table rule_t1, rule_dest
;
-- fail due to missing constraint
alter table atacc2 add constraint foo check (test2>0)
;
ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_1" FOR VALUES IN (NULL, 500, 501, 502)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
create table trigtest (f1 int, f2 text)
;
create table agg_group_4 as
select (g/2)::numeric as c1, array_agg(g::numeric) as c2, count(*) as c3
  from agg_data_2k group by g/2
;
VACUUM VARCHAR_TBL
;
CREATE FUNCTION test1none(val hstore) RETURNS int
LANGUAGE plperlu
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_[0]));
return scalar(keys %{$_[0]});
$$
;
DO $doblock$
declare
  fk record;
  nkeys integer;
  cmd text;
  err record;
begin
  for fk in select * from pg_get_catalog_foreign_keys()
  loop
    raise notice 'checking % % => % %',
      fk.fktable, fk.fkcols, fk.pktable, fk.pkcols;
    nkeys := array_length(fk.fkcols, 1);
    cmd := 'SELECT ctid';
    for i in 1 .. nkeys loop
      cmd := cmd || ', ' || quote_ident(fk.fkcols[i]);
    end loop;
    if fk.is_array then
      cmd := cmd || ' FROM (SELECT ctid';
      for i in 1 .. nkeys-1 loop
        cmd := cmd || ', ' || quote_ident(fk.fkcols[i]);
      end loop;
      cmd := cmd || ', unnest(' || quote_ident(fk.fkcols[nkeys]);
      cmd := cmd || ') as ' || quote_ident(fk.fkcols[nkeys]);
      cmd := cmd || ' FROM ' || fk.fktable::text || ') fk WHERE ';
    else
      cmd := cmd || ' FROM ' || fk.fktable::text || ' fk WHERE ';
    end if;
    if fk.is_opt then
      for i in 1 .. nkeys loop
        cmd := cmd || quote_ident(fk.fkcols[i]) || ' != 0 AND ';
      end loop;
    end if;
    cmd := cmd || 'NOT EXISTS(SELECT 1 FROM ' || fk.pktable::text || ' pk WHERE ';
    for i in 1 .. nkeys loop
      if i > 1 then cmd := cmd || ' AND '; end if;
      cmd := cmd || 'pk.' || quote_ident(fk.pkcols[i]);
      cmd := cmd || ' = fk.' || quote_ident(fk.fkcols[i]);
    end loop;
    cmd := cmd || ')';
    -- raise notice 'cmd = %', cmd;
    for err in execute cmd loop
      raise warning 'FK VIOLATION IN %(%): %', fk.fktable, fk.fkcols, err;
    end loop;
  end loop;
end
$doblock$
;
CREATE TABLE "public"."pht2_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
DROP TABLE btree_test_expr
;
create type alter1.ctype as (f1 int, f2 text)
;
create index test_idx on test_t(id) where val2 = 1
;
TRUNCATE agg_csv
;
ALTER TABLE t SET tablespace regress_ts1
;
drop index test2_idx_gin
;
CREATE TABLE tab1 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 22) STORED)
;
alter table bigger_than_it_looks set (autovacuum_enabled = 'false')
;
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', escape '-')
;
DROP TABLE a CASCADE
;
drop function rttest()
;
-- Concurrent refresh requires a unique index on the materialized
-- view. Test what happens if it's dropped during the refresh.
CREATE OR REPLACE FUNCTION mvtest_drop_the_index()
  RETURNS bool AS $$
BEGIN
  EXECUTE 'DROP INDEX IF EXISTS mvtest_drop_idx';
  RETURN true;
END;
$$ LANGUAGE plpgsql
;
CREATE FUNCTION isngt(upc, upc)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
DROP LANGUAGE alt_lang2 CASCADE
;
VACUUM pg_catalog.pg_partitioned_table
;
CREATE FUNCTION dblink_connect_u (text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED SECURITY DEFINER
;
create table rlp4 partition of rlp for values from (20) to (30) partition by range (a)
;
DROP INDEX fooi
;
CREATE TABLE "public"."tableam_tblas_heap2" (
    "f1" integer
)
;
CREATE FOREIGN TABLE ft (i int, v text) SERVER loopback OPTIONS (table_name 't')
;
CREATE TABLE brin_test_multi_1 (a INT, b BIGINT) WITH (fillfactor=10)
;
CREATE VIEW split_cov.child_view WITH (security_barrier = true) AS SELECT id, parent_id FROM split_cov.child_table WHERE parent_id IS NOT NULL
;
CREATE TEMPORARY VIEW split_temp_view AS VALUES (1, 'one'), (2, 'two')
;
ALTER FUNCTION gbt_time_fetch(internal) PARALLEL SAFE
;
-- Rename cascading does not affect statement triggers
create trigger p after insert on grandparent for each statement execute function f()
;
CREATE FUNCTION dxsyn_init(internal)
        RETURNS internal
        AS '$libdir/dict_xsyn'
        LANGUAGE C STRICT
;
CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ALTER TABLE FKTABLE ALTER CONSTRAINT fk_con DEFERRABLE INITIALLY DEFERRED
;
ANALYZE (SKIP_LOCKED) information_schema.sql_parts
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] -- Changing NOT NULL constraint of identity columns is not allowed
ALTER TABLE pitest1_p1 ALTER COLUMN f3 DROP NOT NULL
;
----------
[DROP_TRIGGER] drop trigger mlparted11_trig on mlparted11
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."drop_sql_command"() RETURNS "event_trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
RAISE NOTICE '% - sql_drop', tg_tag;
END;
$$
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub
	CONNECTION 'port=27174 host=/tmp/1ZUk26WGH4 dbname=postgres application_name=tap_sub'
	PUBLICATION tap_pub
	WITH (streaming = on, two_phase = on)
;
----------
[RENAME_INDEX] ALTER INDEX alter_idx_rename_test_parted RENAME TO alter_idx_rename_test_parted_2
;
----------
[CREATE_TABLE] -- temporary tables are ignored by pg_filenode_relation().
CREATE TEMP TABLE relation_filenode_check(c1 int)
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_attribute
;
----------
[ADD_INDEX] CREATE INDEX "brin_summarize_multi_idx" ON "public"."brin_summarize_multi" USING "brin" ("value") WITH ("pages_per_range"='2')
;
----------
[CREATE_TABLE] CREATE TABLE "testxmlschema"."test1" (
    "a" integer,
    "b" "text"
)
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "public"."mlparted4" DROP COLUMN "........pg.dropped.5........"
;
----------
[CREATE_TABLE] -- test a tricky case: we can elide firing the FK check trigger during
-- an UPDATE if the UPDATE did not change the foreign key
-- field. However, we can't do this if our transaction was the one that
-- created the updated row and the trigger is deferred, since our UPDATE
-- will have invalidated the original newly-inserted tuple, and therefore
-- cause the on-INSERT RI trigger not to be fired.

CREATE TEMP TABLE pktable (
    id int primary key,
    other int
)
;
----------
[DROP_TABLE] drop table rule_t1, rule_dest
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] -- fail due to missing constraint
alter table atacc2 add constraint foo check (test2>0)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."fk_partitioned_fk" ATTACH PARTITION "public"."fk_partitioned_fk_1" FOR VALUES IN (NULL, 500, 501, 502)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_class
;
----------
[CREATE_TABLE] create table trigtest (f1 int, f2 text)
;
----------
[CREATE_TABLE|SELECT] create table agg_group_4 as
select (g/2)::numeric as c1, array_agg(g::numeric) as c2, count(*) as c3
  from agg_data_2k group by g/2
;
----------
[ADMIN_TABLE] VACUUM VARCHAR_TBL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test1none(val hstore) RETURNS int
LANGUAGE plperlu
AS $$
use Data::Dumper;
$Data::Dumper::Sortkeys = 1;
$Data::Dumper::Indent = 0;
elog(INFO, Dumper($_[0]));
return scalar(keys %{$_[0]});
$$
;
----------
[BLOCK] DO $doblock$
declare
  fk record;
  nkeys integer;
  cmd text;
  err record;
begin
  for fk in select * from pg_get_catalog_foreign_keys()
  loop
    raise notice 'checking % % => % %',
      fk.fktable, fk.fkcols, fk.pktable, fk.pkcols;
    nkeys := array_length(fk.fkcols, 1);
    cmd := 'SELECT ctid';
    for i in 1 .. nkeys loop
      cmd := cmd || ', ' || quote_ident(fk.fkcols[i]);
    end loop;
    if fk.is_array then
      cmd := cmd || ' FROM (SELECT ctid';
      for i in 1 .. nkeys-1 loop
        cmd := cmd || ', ' || quote_ident(fk.fkcols[i]);
      end loop;
      cmd := cmd || ', unnest(' || quote_ident(fk.fkcols[nkeys]);
      cmd := cmd || ') as ' || quote_ident(fk.fkcols[nkeys]);
      cmd := cmd || ' FROM ' || fk.fktable::text || ') fk WHERE ';
    else
      cmd := cmd || ' FROM ' || fk.fktable::text || ' fk WHERE ';
    end if;
    if fk.is_opt then
      for i in 1 .. nkeys loop
        cmd := cmd || quote_ident(fk.fkcols[i]) || ' != 0 AND ';
      end loop;
    end if;
    cmd := cmd || 'NOT EXISTS(SELECT 1 FROM ' || fk.pktable::text || ' pk WHERE ';
    for i in 1 .. nkeys loop
      if i > 1 then cmd := cmd || ' AND '; end if;
      cmd := cmd || 'pk.' || quote_ident(fk.pkcols[i]);
      cmd := cmd || ' = fk.' || quote_ident(fk.fkcols[i]);
    end loop;
    cmd := cmd || ')';
    -- raise notice 'cmd = %', cmd;
    for err in execute cmd loop
      raise warning 'FK VIOLATION IN %(%): %', fk.fktable, fk.fkcols, err;
    end loop;
  end loop;
end
$doblock$
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pht2_p3" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[DROP_TABLE] DROP TABLE btree_test_expr
;
----------
[CREATE_TYPE] create type alter1.ctype as (f1 int, f2 text)
;
----------
[ADD_INDEX] create index test_idx on test_t(id) where val2 = 1
;
----------
[TRUNCATE_TABLE] TRUNCATE agg_csv
;
----------
[ALTER_TABLE] ALTER TABLE t SET tablespace regress_ts1
;
----------
[DROP_INDEX] drop index test2_idx_gin
;
----------
[CREATE_TABLE] CREATE TABLE tab1 (a int PRIMARY KEY, b int GENERATED ALWAYS AS (a * 22) STORED)
;
----------
[ALTER_TABLE] alter table bigger_than_it_looks set (autovacuum_enabled = 'false')
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'text', escape '-')
;
----------
[DROP_TABLE] DROP TABLE a CASCADE
;
----------
[DROP_PROG_OBJ] drop function rttest()
;
----------
[CREATE_PROG_OBJ] -- Concurrent refresh requires a unique index on the materialized
-- view. Test what happens if it's dropped during the refresh.
CREATE OR REPLACE FUNCTION mvtest_drop_the_index()
  RETURNS bool AS $$
BEGIN
  EXECUTE 'DROP INDEX IF EXISTS mvtest_drop_idx';
  RETURN true;
END;
$$ LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isngt(upc, upc)
	RETURNS boolean
	AS 'int8gt'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[UNKNOWN] DROP LANGUAGE alt_lang2 CASCADE
;
----------
[ADMIN_TABLE] VACUUM pg_catalog.pg_partitioned_table
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_connect_u (text)
RETURNS text
AS '$libdir/dblink','dblink_connect'
LANGUAGE C STRICT PARALLEL RESTRICTED SECURITY DEFINER
;
----------
[CREATE_TABLE] create table rlp4 partition of rlp for values from (20) to (30) partition by range (a)
;
----------
[DROP_INDEX] DROP INDEX fooi
;
----------
[CREATE_TABLE] CREATE TABLE "public"."tableam_tblas_heap2" (
    "f1" integer
)
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE ft (i int, v text) SERVER loopback OPTIONS (table_name 't')
;
----------
[CREATE_TABLE] CREATE TABLE brin_test_multi_1 (a INT, b BIGINT) WITH (fillfactor=10)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW split_cov.child_view WITH (security_barrier = true) AS SELECT id, parent_id FROM split_cov.child_table WHERE parent_id IS NOT NULL
;
----------
[CREATE_VIEW(SELECT)] CREATE TEMPORARY VIEW split_temp_view AS VALUES (1, 'one'), (2, 'two')
;
----------
[ALTER_PROG_OBJ] ALTER FUNCTION gbt_time_fetch(internal) PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Rename cascading does not affect statement triggers
create trigger p after insert on grandparent for each statement execute function f()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dxsyn_init(internal)
        RETURNS internal
        AS '$libdir/dict_xsyn'
        LANGUAGE C STRICT
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR < (
	PROCEDURE = isnlt,
	LEFTARG = ismn13,
	RIGHTARG = ismn13,
	COMMUTATOR = >,
	NEGATOR = >=,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ALTER_TABLE|ALTER_CONSTRAINT] ALTER TABLE FKTABLE ALTER CONSTRAINT fk_con DEFERRABLE INITIALLY DEFERRED
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) information_schema.sql_parts
;
