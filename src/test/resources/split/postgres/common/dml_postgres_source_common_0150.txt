insert into insertconflict values (1, 2)
on conflict (b) where coalesce(a, 1) > 0 do nothing
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18591', relminmxid = '3'
WHERE oid = '"public"."covidxpart4"'::pg_catalog.regclass
;
insert into brtrigpartcon values (1, 'hi there')
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18606', relminmxid = '3'
WHERE oid = '"public"."prt1_e_p2"'::pg_catalog.regclass
;
insert into part_default values ('Zz', 2)
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."mlparted2"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('4', 114)
;
UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"public"."trigger_parted_p2"'::pg_catalog.regclass AND
conname IN ('trigger_parted_a_not_null')
;
insert into cities values ('Las Vegas', 2.583E+5, 2174) on conflict do nothing
;
INSERT INTO num_result SELECT t1.id, t2.id, t1.val * t2.val
    FROM num_data t1, num_data t2
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."gtest1"'::pg_catalog.regclass
;
insert into test (name, "amount", letter) values ('db: ''r1''', 1, 'f')
;
INSERT INTO public.tplain VALUES ('2', 372)
;
INSERT INTO pg_temp_14.pg_temp_26565_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_14.pg_temp_26565 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_14.pg_temp_26565 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."beta_neg_p2"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '38522'
;
INSERT INTO dropped_objects
		(object_type, schema_name, object_name,
		 object_identity, address_names, address_args,
		 is_temporary, original, normal) VALUES
		(obj.object_type, obj.schema_name, obj.object_name,
		 obj.object_identity, obj.address_names, obj.address_args,
		 obj.is_temporary, obj.original, obj.normal)
;
insert into big5_inputs  values
  ('\x666f6f',		'valid, pure ASCII'),
  ('\x666f6fb648',	'valid'),
  ('\x666f6fa27f',	'valid, no translation to UTF-8'),
  ('\x666f6fb60048',	'invalid, NUL byte'),
  ('\x666f6fb64800',	'invalid, NUL byte')
;
INSERT INTO conf_tab VALUES (1,1,1)
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8458', relminmxid = '1'
WHERE oid = '27642'
;
-- preparation for next statement
INSERT INTO document VALUES (33, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'Some novel, replaces sci-fi') -- takes UPDATE path
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14717', relminmxid = '3'
WHERE oid = '36868'
;
INSERT INTO public.tht VALUES ('2', 962)
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15722', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15850', relminmxid = '3'
WHERE oid = '"public"."pht1_p2"'::pg_catalog.regclass
;
INSERT INTO num_exp_div VALUES (5,0,'NaN')
;
INSERT INTO TIMETZ_TBL VALUES ('23:59 PDT')
;
INSERT INTO public.ths_p1 VALUES (8, 28)
;
INSERT INTO hash_cleanup_heap SELECT 1 FROM generate_series(1, 1500) as i
;
-- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."idxpart31"'::pg_catalog.regclass
;
INSERT INTO public.tplain VALUES ('5', 685)
;
-- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.5........'
  AND attrelid = '"public"."mlparted_def1"'::pg_catalog.regclass
;
UPDATE public.test SET c = 'a' WHERE c = '15'
;
INSERT INTO public.tht VALUES ('4', 344)
;
UPDATE pgbench_tellers SET tbalance = tbalance + 2245 WHERE tid = 6
;
UPDATE pg_catalog.pg_class
SET relfrozenxid = '12727', relminmxid = '1'
WHERE oid = '33642'
;
INSERT INTO public.ths_p3 VALUES (5, 345)
;
-- any of the following three input formats are acceptable
INSERT INTO CHAR_TBL (f1) VALUES ('1')
;
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pitest2_p2"'::pg_catalog.regclass
  AND attname IN ('f1', 'f2', 'f3')
;
INSERT INTO macaddr8_data VALUES (14, '08:00:2a:01:02:04')
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15866', relminmxid = '3'
WHERE oid = '36748'
;
-- Good
INSERT INTO basictest values ('88', 'haha', 'short text', '123.12')
;
insert into gist_point_tbl (id, p)
select g+100000, point(g*10+1, g*10+1) from generate_series(1, 10000) g
;
INSERT INTO public.tplain VALUES ('7', 297)
;
WITH upsert AS
	  (UPDATE accounts SET balance = balance + 1234
	   WHERE accountid = 'savings'
	   RETURNING accountid)
	INSERT INTO accounts SELECT 'savings', 1234
	  WHERE NOT EXISTS (SELECT 1 FROM upsert)
;
INSERT INTO foo AS bar DEFAULT VALUES RETURNING foo.*
;
-- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14642', relminmxid = '3'
WHERE oid = '"public"."plt1_e_p3"'::pg_catalog.regclass
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '996', relminmxid = '1'
WHERE oid = '16821'
;
-- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '26645'
;
INSERT INTO num_exp_mul VALUES (0,4,'0')
;
------------------------------------------------------------------------------------------
[MERGE] insert into insertconflict values (1, 2)
on conflict (b) where coalesce(a, 1) > 0 do nothing
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '18591', relminmxid = '3'
WHERE oid = '"public"."covidxpart4"'::pg_catalog.regclass
;
----------
[INSERT] insert into brtrigpartcon values (1, 'hi there')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '18606', relminmxid = '3'
WHERE oid = '"public"."prt1_e_p2"'::pg_catalog.regclass
;
----------
[INSERT] insert into part_default values ('Zz', 2)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '6899', relminmxid = '1'
WHERE oid = '"public"."mlparted2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('4', 114)
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_constraint
SET conislocal = false
WHERE contype = 'n' AND conrelid = '"public"."trigger_parted_p2"'::pg_catalog.regclass AND
conname IN ('trigger_parted_a_not_null')
;
----------
[MERGE] insert into cities values ('Las Vegas', 2.583E+5, 2174) on conflict do nothing
;
----------
[INSERT|SELECT] INSERT INTO num_result SELECT t1.id, t2.id, t1.val * t2.val
    FROM num_data t1, num_data t2
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7389', relminmxid = '1'
WHERE oid = '"public"."gtest1"'::pg_catalog.regclass
;
----------
[INSERT|SELECT] insert into test (name, "amount", letter) values ('db: ''r1''', 1, 'f')
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('2', 372)
;
----------
[INSERT|SELECT] INSERT INTO pg_temp_14.pg_temp_26565_2 SELECT mv.ctid AS tid, newdata.*::pg_temp_14.pg_temp_26565 AS newdata FROM public.mvtest_mv mv FULL JOIN pg_temp_14.pg_temp_26565 newdata ON (newdata.a OPERATOR(pg_catalog.=) mv.a AND newdata.b OPERATOR(pg_catalog.=) mv.b AND newdata.c OPERATOR(pg_catalog.=) mv.c AND newdata.* OPERATOR(pg_catalog.*=) mv.*) WHERE newdata.* IS NULL OR mv.* IS NULL ORDER BY tid
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'b'
  AND attrelid = '"public"."beta_neg_p2"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '16635', relminmxid = '3'
WHERE oid = '38522'
;
----------
[INSERT|SELECT] INSERT INTO dropped_objects
		(object_type, schema_name, object_name,
		 object_identity, address_names, address_args,
		 is_temporary, original, normal) VALUES
		(obj.object_type, obj.schema_name, obj.object_name,
		 obj.object_identity, obj.address_names, obj.address_args,
		 obj.is_temporary, obj.original, obj.normal)
;
----------
[INSERT] insert into big5_inputs  values
  ('\x666f6f',		'valid, pure ASCII'),
  ('\x666f6fb648',	'valid'),
  ('\x666f6fa27f',	'valid, no translation to UTF-8'),
  ('\x666f6fb60048',	'invalid, NUL byte'),
  ('\x666f6fb64800',	'invalid, NUL byte')
;
----------
[INSERT] INSERT INTO conf_tab VALUES (1,1,1)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '8458', relminmxid = '1'
WHERE oid = '27642'
;
----------
[MERGE|SELECT] -- preparation for next statement
INSERT INTO document VALUES (33, (SELECT cid from category WHERE cname = 'novel'), 1, 'regress_rls_bob', 'Some novel, replaces sci-fi') -- takes UPDATE path
    ON CONFLICT (did) DO UPDATE SET dtitle = EXCLUDED.dtitle
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14717', relminmxid = '3'
WHERE oid = '36868'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('2', 962)
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15722', relminmxid = '3'
WHERE oid = '"public"."pagg_tab_ml_p2_s1"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15850', relminmxid = '3'
WHERE oid = '"public"."pht1_p2"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO num_exp_div VALUES (5,0,'NaN')
;
----------
[INSERT] INSERT INTO TIMETZ_TBL VALUES ('23:59 PDT')
;
----------
[INSERT] INSERT INTO public.ths_p1 VALUES (8, 28)
;
----------
[INSERT|SELECT] INSERT INTO hash_cleanup_heap SELECT 1 FROM generate_series(1, 1500) as i
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate inherited column.
UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attname = 'a'
  AND attrelid = '"public"."idxpart31"'::pg_catalog.regclass
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('5', 685)
;
----------
[UPDATE|METADATA] -- For binary upgrade, recreate dropped column.
UPDATE pg_catalog.pg_attribute
SET attlen = 4, attalign = 'i', attbyval = false
WHERE attname = '........pg.dropped.5........'
  AND attrelid = '"public"."mlparted_def1"'::pg_catalog.regclass
;
----------
[UPDATE] UPDATE public.test SET c = 'a' WHERE c = '15'
;
----------
[INSERT] INSERT INTO public.tht VALUES ('4', 344)
;
----------
[UPDATE] UPDATE pgbench_tellers SET tbalance = tbalance + 2245 WHERE tid = 6
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_class
SET relfrozenxid = '12727', relminmxid = '1'
WHERE oid = '33642'
;
----------
[INSERT] INSERT INTO public.ths_p3 VALUES (5, 345)
;
----------
[INSERT|SELECT] -- any of the following three input formats are acceptable
INSERT INTO CHAR_TBL (f1) VALUES ('1')
;
----------
[UPDATE|METADATA] UPDATE pg_catalog.pg_attribute
SET attislocal = false
WHERE attrelid = '"public"."pitest2_p2"'::pg_catalog.regclass
  AND attname IN ('f1', 'f2', 'f3')
;
----------
[INSERT] INSERT INTO macaddr8_data VALUES (14, '08:00:2a:01:02:04')
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '15866', relminmxid = '3'
WHERE oid = '36748'
;
----------
[INSERT] -- Good
INSERT INTO basictest values ('88', 'haha', 'short text', '123.12')
;
----------
[INSERT|SELECT] insert into gist_point_tbl (id, p)
select g+100000, point(g*10+1, g*10+1) from generate_series(1, 10000) g
;
----------
[INSERT] INSERT INTO public.tplain VALUES ('7', 297)
;
----------
[INSERT|UPDATE|SELECT] WITH upsert AS
	  (UPDATE accounts SET balance = balance + 1234
	   WHERE accountid = 'savings'
	   RETURNING accountid)
	INSERT INTO accounts SELECT 'savings', 1234
	  WHERE NOT EXISTS (SELECT 1 FROM upsert)
;
----------
[INSERT] INSERT INTO foo AS bar DEFAULT VALUES RETURNING foo.*
;
----------
[UPDATE|METADATA] -- For binary upgrade, set heap's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '14642', relminmxid = '3'
WHERE oid = '"public"."plt1_e_p3"'::pg_catalog.regclass
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '996', relminmxid = '1'
WHERE oid = '16821'
;
----------
[UPDATE|METADATA] -- For binary upgrade, set toast's relfrozenxid and relminmxid
UPDATE pg_catalog.pg_class
SET relfrozenxid = '7427', relminmxid = '1'
WHERE oid = '26645'
;
----------
[INSERT] INSERT INTO num_exp_mul VALUES (0,4,'0')
;
