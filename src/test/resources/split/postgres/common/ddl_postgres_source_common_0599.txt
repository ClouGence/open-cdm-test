ALTER FOREIGN TABLE text_csv ALTER COLUMN word1 OPTIONS (force_null 'true')
;
ANALYZE public.transition_table_level1
;
DROP EXTENSION hstore_plperl
;
VACUUM (PARALLEL 2) pg_catalog.pg_ts_dict
;
CREATE FUNCTION check_ddl_rewrite(p_tablename regclass, p_ddl text)
RETURNS boolean
LANGUAGE plpgsql AS $$
DECLARE
    v_relfilenode oid;
BEGIN
    v_relfilenode := relfilenode FROM pg_class WHERE oid = p_tablename;

    EXECUTE p_ddl;

    RETURN v_relfilenode <> (SELECT relfilenode FROM pg_class WHERE oid = p_tablename);
END;
$$
;
create domain int_domain1 as int constraint nn1 not null constraint nn2 not null
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26330_2 (tid pg_catalog.tid)
;
DO $$
BEGIN
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
END
$$
;
/* End test case for bug #16242 */
/* Test case for bug #17409 */
create table attbl (p1 int constraint pk_attbl primary key)
;
ALTER TABLE pitest1_p2 ALTER COLUMN f3 SET NOT NULL
;
CREATE TABLE prt1_l_p2_p2 PARTITION OF prt1_l_p2 FOR VALUES IN ('0002', '0003')
;
DROP FUNCTION getrngfunc3(int)
;
CREATE SUBSCRIPTION tap_sub CONNECTION 'port=12333 host=/tmp/XhTqPN5Kw1 dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
CREATE FUNCTION ttdummy ()
    RETURNS trigger
    AS '/src/src/test/regress/regress.so'
    LANGUAGE C
;
ALTER OPERATOR FAMILY gist_vbit_ops USING gist ADD
	FUNCTION	11  (varbit, varbit) gbt_varbit_sortsupport (internal)
;
CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = issn13,
	RIGHTARG = issn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
ANALYZE pg_temp_2.pg_temp_28105
;
CREATE TABLE sj_t2 (id serial, a int)
;
ANALYZE public.prt1_l_p1
;
create table idxpart1 (like idxpart)
;
ALTER TABLE ONLY "public"."mlparted" ATTACH PARTITION "public"."mlparted_def" DEFAULT
;
-- test handling of subqueries in grouping sets
create temp table gstest5(id integer primary key, v integer)
;
ALTER TABLE tt9 ADD UNIQUE(c)
;
DROP DOMAIN test_domain_exists
;
CREATE VIEW "public"."rtest_vview3" AS
 SELECT "x"."a",
    "x"."b"
   FROM "public"."rtest_vview2" "x"
  WHERE (0 < ( SELECT "count"(*) AS "count"
           FROM "public"."rtest_view2" "y"
          WHERE ("y"."a" = "x"."a")))
;
CREATE AGGREGATE testns.priv_testagg(int) (sfunc = int4pl, stype = int4)
;
ALTER TABLE ONLY "generated_stored_tests"."gtestxx_4" INHERIT "generated_stored_tests"."gtest1"
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
-- test RETURN QUERY EXECUTE

create or replace function return_dquery()
returns setof int as $$
begin
  return query execute 'select * from (values(10),(20)) f';
  return query execute 'select * from (values($1),($2)) f' using 40,50;
end;
$$ language plpgsql
;
COMMENT ON DATABASE "regression" IS 'This is a test comment'
;
/* contrib/amcheck/amcheck--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- In order to avoid issues with dependencies when updating amcheck to 1.4,
-- create new, overloaded versions of the 1.2 bt_index_parent_check signature,
-- and 1.1 bt_index_check signature.

--
-- bt_index_parent_check()
--
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean, rootdescend boolean, checkunique boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE FUNCTION lo_manage()
RETURNS pg_catalog.trigger
AS '$libdir/lo'
LANGUAGE C
;
ANALYZE public.covidxpart2
;
ANALYZE public.plt2_p2
;
ALTER EXTENSION test_pg_dump ADD VIEW test_pg_dump_v1
;
VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_collation
;
DROP EVENT TRIGGER IF EXISTS split_event_helper_sql_drop
;
DROP TRIGGER merge_skip ON target
;
ANALYZE public.line_tbl
;
DROP FUNCTION brintest_func()
;
--test column type update varlena/non-varlena
CREATE TABLE cmdata2 (f1 int)
;
DROP TABLE OID_TBL
;
ANALYZE public.polygon_tbl
;
vacuum �׻����Ѹ�
;
CREATE SCHEMA newschema6 AUTHORIZATION nonexistent
;
ALTER TABLE fktable ADD CONSTRAINT fk_2_1
FOREIGN KEY (x2) REFERENCES pktable(id1)
;
CREATE TABLE tab2 (a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
DROP TABLE ctl_table
;
DROP TABLE pg_temp_11.pg_temp_26410_2, pg_temp_11.pg_temp_26410
;
VACUUM ANALYZE num_exp_mul
;
------------------------------------------------------------------------------------------
[ALTER_TABLE|ALTER_COLUMN] ALTER FOREIGN TABLE text_csv ALTER COLUMN word1 OPTIONS (force_null 'true')
;
----------
[ADMIN_TABLE] ANALYZE public.transition_table_level1
;
----------
[DROP_LIBRARY] DROP EXTENSION hstore_plperl
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_ts_dict
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION check_ddl_rewrite(p_tablename regclass, p_ddl text)
RETURNS boolean
LANGUAGE plpgsql AS $$
DECLARE
    v_relfilenode oid;
BEGIN
    v_relfilenode := relfilenode FROM pg_class WHERE oid = p_tablename;

    EXECUTE p_ddl;

    RETURN v_relfilenode <> (SELECT relfilenode FROM pg_class WHERE oid = p_tablename);
END;
$$
;
----------
[CREATE_TYPE] create domain int_domain1 as int constraint nn1 not null constraint nn2 not null
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26330_2 (tid pg_catalog.tid)
;
----------
[BLOCK] DO $$
BEGIN
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
  UPDATE t_test SET x = x + 1;
  RAISE NOTICE 'f_get_x(%)', f_get_x();
  CALL f_print_x(f_get_x());
END
$$
;
----------
[CREATE_TABLE] /* End test case for bug #16242 */
/* Test case for bug #17409 */
create table attbl (p1 int constraint pk_attbl primary key)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE pitest1_p2 ALTER COLUMN f3 SET NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE prt1_l_p2_p2 PARTITION OF prt1_l_p2 FOR VALUES IN ('0002', '0003')
;
----------
[DROP_PROG_OBJ] DROP FUNCTION getrngfunc3(int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub CONNECTION 'port=12333 host=/tmp/XhTqPN5Kw1 dbname=postgres' PUBLICATION tap_pub, tap_pub_ins_only
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ttdummy ()
    RETURNS trigger
    AS '/src/src/test/regress/regress.so'
    LANGUAGE C
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_vbit_ops USING gist ADD
	FUNCTION	11  (varbit, varbit) gbt_varbit_sortsupport (internal)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <= (
	PROCEDURE = isnle,
	LEFTARG = issn13,
	RIGHTARG = issn,
	COMMUTATOR = >=,
	NEGATOR = >,
	RESTRICT = scalarltsel,
	JOIN = scalarltjoinsel)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_28105
;
----------
[CREATE_TABLE] CREATE TABLE sj_t2 (id serial, a int)
;
----------
[ADMIN_TABLE] ANALYZE public.prt1_l_p1
;
----------
[CREATE_TABLE] create table idxpart1 (like idxpart)
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."mlparted" ATTACH PARTITION "public"."mlparted_def" DEFAULT
;
----------
[CREATE_TABLE] -- test handling of subqueries in grouping sets
create temp table gstest5(id integer primary key, v integer)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE tt9 ADD UNIQUE(c)
;
----------
[DROP_TYPE] DROP DOMAIN test_domain_exists
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."rtest_vview3" AS
 SELECT "x"."a",
    "x"."b"
   FROM "public"."rtest_vview2" "x"
  WHERE (0 < ( SELECT "count"(*) AS "count"
           FROM "public"."rtest_view2" "y"
          WHERE ("y"."a" = "x"."a")))
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testns.priv_testagg(int) (sfunc = int4pl, stype = int4)
;
----------
[ALTER_TABLE] ALTER TABLE ONLY "generated_stored_tests"."gtestxx_4" INHERIT "generated_stored_tests"."gtest1"
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_depend
;
----------
[CREATE_PROG_OBJ] -- test RETURN QUERY EXECUTE

create or replace function return_dquery()
returns setof int as $$
begin
  return query execute 'select * from (values(10),(20)) f';
  return query execute 'select * from (values($1),($2)) f' using 40,50;
end;
$$ language plpgsql
;
----------
[COMMENT_CATALOG] COMMENT ON DATABASE "regression" IS 'This is a test comment'
;
----------
[CREATE_PROG_OBJ] /* contrib/amcheck/amcheck--1.3--1.4.sql */

-- complain if script is sourced in psql, rather than via CREATE EXTENSION


-- In order to avoid issues with dependencies when updating amcheck to 1.4,
-- create new, overloaded versions of the 1.2 bt_index_parent_check signature,
-- and 1.1 bt_index_check signature.

--
-- bt_index_parent_check()
--
CREATE FUNCTION bt_index_parent_check(index regclass,
    heapallindexed boolean, rootdescend boolean, checkunique boolean)
RETURNS VOID
AS '$libdir/amcheck', 'bt_index_parent_check'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lo_manage()
RETURNS pg_catalog.trigger
AS '$libdir/lo'
LANGUAGE C
;
----------
[ADMIN_TABLE] ANALYZE public.covidxpart2
;
----------
[ADMIN_TABLE] ANALYZE public.plt2_p2
;
----------
[ALTER_LIBRARY] ALTER EXTENSION test_pg_dump ADD VIEW test_pg_dump_v1
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, FREEZE) pg_catalog.pg_collation
;
----------
[DROP_TRIGGER] DROP EVENT TRIGGER IF EXISTS split_event_helper_sql_drop
;
----------
[DROP_TRIGGER] DROP TRIGGER merge_skip ON target
;
----------
[ADMIN_TABLE] ANALYZE public.line_tbl
;
----------
[DROP_PROG_OBJ] DROP FUNCTION brintest_func()
;
----------
[CREATE_TABLE] --test column type update varlena/non-varlena
CREATE TABLE cmdata2 (f1 int)
;
----------
[DROP_TABLE] DROP TABLE OID_TBL
;
----------
[ADMIN_TABLE] ANALYZE public.polygon_tbl
;
----------
[ADMIN_TABLE] vacuum �׻����Ѹ�
;
----------
[CREATE_SCHEMA] CREATE SCHEMA newschema6 AUTHORIZATION nonexistent
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE fktable ADD CONSTRAINT fk_2_1
FOREIGN KEY (x2) REFERENCES pktable(id1)
;
----------
[CREATE_TABLE] CREATE TABLE tab2 (a int PRIMARY KEY, b text) PARTITION BY LIST (a)
;
----------
[DROP_TABLE] DROP TABLE ctl_table
;
----------
[DROP_TABLE] DROP TABLE pg_temp_11.pg_temp_26410_2, pg_temp_11.pg_temp_26410
;
----------
[ADMIN_TABLE] VACUUM ANALYZE num_exp_mul
;
