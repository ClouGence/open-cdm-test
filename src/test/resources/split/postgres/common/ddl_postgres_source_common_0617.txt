CREATE FUNCTION functest_IS_3(a int default 1, out b int)
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT $1'
;
-- insert/update/delete row-level triggers on the parent
create trigger trig_ins_after_parent after insert on parted_stmt_trig
  for each row execute procedure trigger_notice()
;
ANALYZE mcv_lists_multi
;
create table test_range_excl(
  room int4range,
  speaker int4range,
  during tsrange,
  exclude using gist (room with =, during with &&),
  exclude using gist (speaker with =, during with &&)
)
;
/*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
DROP TABLE parted_fk_naming
;
drop index test_range_gist_idx
;
call test_procedure()
;
DROP TABLE bar, foo_range_parted
;
/* contrib/pageinspect/pageinspect--1.8--1.9.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- gist_page_opaque_info()
--
CREATE FUNCTION gist_page_opaque_info(IN page bytea,
    OUT lsn pg_lsn,
    OUT nsn pg_lsn,
    OUT rightlink bigint,
    OUT flags text[])
AS '$libdir/pageinspect', 'gist_page_opaque_info'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE TABLE CHECK_TBL (x int,
	CONSTRAINT CHECK_CON CHECK (x > 3))
;
CREATE VIEW rw_view1 AS
  SELECT *, 'Const' AS c, (SELECT concat('b: ', b)) AS d FROM base_tbl WHERE a>0
;
ANALYZE pg_temp_12.pg_temp_26234
;
ALTER TABLE "public"."itest6" ALTER COLUMN "a" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."itest6_a_seq"
    START WITH 100
    INCREMENT BY 2
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
create table test_t (id integer, val1 text, val2 integer)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub2, pub3
;
-- Should work. During ALTER OPERATOR FAMILY ... DROP OPERATOR
-- when left type is the same as right type, a DROP with only one argument type should work
CREATE OPERATOR FAMILY alt_opf8 USING btree
;
CREATE SCHEMA IF NOT EXISTS ext_cine_schema
;
CREATE TABLE "public"."brintest_bloom" (
    "byteacol" "bytea",
    "charcol" "char",
    "namecol" "name",
    "int8col" bigint,
    "int2col" smallint,
    "int4col" integer,
    "textcol" "text",
    "oidcol" "oid",
    "float4col" real,
    "float8col" double precision,
    "macaddrcol" "macaddr",
    "inetcol" "inet",
    "cidrcol" "cidr",
    "bpcharcol" character(1),
    "datecol" "date",
    "timecol" time without time zone,
    "timestampcol" timestamp without time zone,
    "timestamptzcol" timestamp with time zone,
    "intervalcol" interval,
    "timetzcol" time with time zone,
    "numericcol" numeric,
    "uuidcol" "uuid",
    "lsncol" "pg_lsn"
)
WITH ("fillfactor"='10')
;
CREATE INDEX "gtest22c_expr_idx" ON "generated_stored_tests"."gtest22c" USING "btree" ((("b" * 3)))
;
DROP FUNCTION leaker_2(bool)
;
ALTER TABLE serialtest1_f2_seq RENAME TO serialtest1_f2_foo
;
--- test selecting bool through SPI

CREATE FUNCTION spi_test()  RETURNS void
LANGUAGE plperlu
TRANSFORM FOR TYPE bool
AS $$
my $rv = spi_exec_query('SELECT true t, false f, NULL n')->{rows}->[0];

die("TRUE mistransformed to UNDEF in SPI") if (!defined ($rv->{t}));
die("FALSE mistransformed to UNDEF in SPI") if (!defined ($rv->{f}));
die("NULL mistransformed in SPI") if (defined ($rv->{n}));
die("TRUE mistransformed in SPI") if (!$rv->{t});
die("FALSE mistransformed in SPI") if ($rv->{f});
$$
;
CREATE RULE v1_upd AS ON UPDATE TO rule_v1
  DO ALSO UPDATE rule_t1 t
  SET c = tt.a * 10
  FROM (SELECT a FROM rule_t1 WHERE a = OLD.a) tt WHERE t.a = tt.a
;
VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_am
;
CREATE OPERATOR <-> (
	LEFTARG = int8,
	RIGHTARG = int8,
	PROCEDURE = int8_dist,
	COMMUTATOR = '<->'
)
;
DROP TABLE IF EXISTS no_such_schema.foo
;
ALTER TABLE fd_pt2_1 ALTER c3 SET NOT NULL
;
CREATE FUNCTION seg_same(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE INDEX e ON a (a1)
;
CREATE TABLE fail_part PARTITION OF list_parted2 FOR VALUES IN (null)
;
---

CREATE TABLE customer (cid int primary key, cname text, ccredit text)
;
DROP SCHEMA has$dollar CASCADE
;
create index matest1i on matest1 (b, c)
;
do $$ declare a int[];
begin a[3] := 4; raise notice 'a = %', a; end$$
;
ANALYZE public.domtab
;
-- ensure that pending-list items are handled correctly, too
create temp table t_test_trgm(t text COLLATE "C")
;
CREATE TABLE stats_import.test(
    id INTEGER PRIMARY KEY,
    name text,
    comp stats_import.complex_type,
    arange int4range,
    tags text[]
) WITH (autovacuum_enabled = false)
;
ALTER OPERATOR FAMILY gist_cash_ops USING gist ADD
	FUNCTION	11  (money, money) gbt_cash_sortsupport (internal)
;
ALTER INDEX pg_toast.pg_toast_30919_index RENAME TO reind_con_toast_idx
;
CREATE EXTENSION unaccent
;
CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (somename)
;
ALTER TABLE generated_stored_tests.gtest20 ADD CONSTRAINT gtest20_b_check CHECK ((b < 50))
;
drop view v_pagg_test
;
ALTER TABLE pg_authid ADD PRIMARY KEY USING INDEX pg_authid_oid_index
;
CREATE DOMAIN "public"."testxmldomain" AS character varying
;
ALTER OPERATOR <= (ismn13, ismn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
--
-- Check handling of MULTIEXPR SubPlans in inherited updates
--
create table inhpar(f1 int, f2 name)
;
CREATE FUNCTION "generated_stored_tests"."gtest_trigger_func4"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  NEW.a = 10;
  NEW.b = 300;
  RETURN NEW;
END;
$$
;
ANALYZE public.varchar_tbl
;
------------------------------------------------------------------------------------------
[CREATE_PROG_OBJ] CREATE FUNCTION functest_IS_3(a int default 1, out b int)
    RETURNS int
    LANGUAGE SQL
    AS 'SELECT $1'
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- insert/update/delete row-level triggers on the parent
create trigger trig_ins_after_parent after insert on parted_stmt_trig
  for each row execute procedure trigger_notice()
;
----------
[ADMIN_TABLE] ANALYZE mcv_lists_multi
;
----------
[CREATE_TABLE] create table test_range_excl(
  room int4range,
  speaker int4range,
  during tsrange,
  exclude using gist (room with =, during with &&),
  exclude using gist (speaker with =, during with &&)
)
;
----------
[CREATE_POLICY] /*
 * text search configuration for romanian language
 *
 * Copyright (c) 2007-2020, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * romanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY romanian_stem
	(TEMPLATE = snowball, Language = romanian )
;
----------
[DROP_TABLE] DROP TABLE parted_fk_naming
;
----------
[DROP_INDEX] drop index test_range_gist_idx
;
----------
[CALL_PROG_OBJ] call test_procedure()
;
----------
[DROP_TABLE] DROP TABLE bar, foo_range_parted
;
----------
[CREATE_PROG_OBJ] /* contrib/pageinspect/pageinspect--1.8--1.9.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


--
-- gist_page_opaque_info()
--
CREATE FUNCTION gist_page_opaque_info(IN page bytea,
    OUT lsn pg_lsn,
    OUT nsn pg_lsn,
    OUT rightlink bigint,
    OUT flags text[])
AS '$libdir/pageinspect', 'gist_page_opaque_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE CHECK_TBL (x int,
	CONSTRAINT CHECK_CON CHECK (x > 3))
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW rw_view1 AS
  SELECT *, 'Const' AS c, (SELECT concat('b: ', b)) AS d FROM base_tbl WHERE a>0
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_12.pg_temp_26234
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE "public"."itest6" ALTER COLUMN "a" ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME "public"."itest6_a_seq"
    START WITH 100
    INCREMENT BY 2
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
)
;
----------
[CREATE_TABLE] create table test_t (id integer, val1 text, val2 integer)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=14921 host=/tmp/TTd5xkrUOZ dbname=postgres' PUBLICATION pub2, pub3
;
----------
[CREATE_PROG_OBJ] -- Should work. During ALTER OPERATOR FAMILY ... DROP OPERATOR
-- when left type is the same as right type, a DROP with only one argument type should work
CREATE OPERATOR FAMILY alt_opf8 USING btree
;
----------
[CREATE_SCHEMA] CREATE SCHEMA IF NOT EXISTS ext_cine_schema
;
----------
[CREATE_TABLE] CREATE TABLE "public"."brintest_bloom" (
    "byteacol" "bytea",
    "charcol" "char",
    "namecol" "name",
    "int8col" bigint,
    "int2col" smallint,
    "int4col" integer,
    "textcol" "text",
    "oidcol" "oid",
    "float4col" real,
    "float8col" double precision,
    "macaddrcol" "macaddr",
    "inetcol" "inet",
    "cidrcol" "cidr",
    "bpcharcol" character(1),
    "datecol" "date",
    "timecol" time without time zone,
    "timestampcol" timestamp without time zone,
    "timestamptzcol" timestamp with time zone,
    "intervalcol" interval,
    "timetzcol" time with time zone,
    "numericcol" numeric,
    "uuidcol" "uuid",
    "lsncol" "pg_lsn"
)
WITH ("fillfactor"='10')
;
----------
[ADD_INDEX] CREATE INDEX "gtest22c_expr_idx" ON "generated_stored_tests"."gtest22c" USING "btree" ((("b" * 3)))
;
----------
[DROP_PROG_OBJ] DROP FUNCTION leaker_2(bool)
;
----------
[RENAME_TABLE] ALTER TABLE serialtest1_f2_seq RENAME TO serialtest1_f2_foo
;
----------
[CREATE_PROG_OBJ] --- test selecting bool through SPI

CREATE FUNCTION spi_test()  RETURNS void
LANGUAGE plperlu
TRANSFORM FOR TYPE bool
AS $$
my $rv = spi_exec_query('SELECT true t, false f, NULL n')->{rows}->[0];

die("TRUE mistransformed to UNDEF in SPI") if (!defined ($rv->{t}));
die("FALSE mistransformed to UNDEF in SPI") if (!defined ($rv->{f}));
die("NULL mistransformed in SPI") if (defined ($rv->{n}));
die("TRUE mistransformed in SPI") if (!$rv->{t});
die("FALSE mistransformed in SPI") if ($rv->{f});
$$
;
----------
[CREATE_POLICY] CREATE RULE v1_upd AS ON UPDATE TO rule_v1
  DO ALSO UPDATE rule_t1 t
  SET c = tt.a * 10
  FROM (SELECT a FROM rule_t1 WHERE a = OLD.a) tt WHERE t.a = tt.a
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE) pg_catalog.pg_am
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR <-> (
	LEFTARG = int8,
	RIGHTARG = int8,
	PROCEDURE = int8_dist,
	COMMUTATOR = '<->'
)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS no_such_schema.foo
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE fd_pt2_1 ALTER c3 SET NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_same(seg, seg)
RETURNS bool
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX e ON a (a1)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF list_parted2 FOR VALUES IN (null)
;
----------
[CREATE_TABLE] ---

CREATE TABLE customer (cid int primary key, cname text, ccredit text)
;
----------
[DROP_SCHEMA] DROP SCHEMA has$dollar CASCADE
;
----------
[ADD_INDEX] create index matest1i on matest1 (b, c)
;
----------
[BLOCK] do $$ declare a int[];
begin a[3] := 4; raise notice 'a = %', a; end$$
;
----------
[ADMIN_TABLE] ANALYZE public.domtab
;
----------
[CREATE_TABLE] -- ensure that pending-list items are handled correctly, too
create temp table t_test_trgm(t text COLLATE "C")
;
----------
[CREATE_TABLE] CREATE TABLE stats_import.test(
    id INTEGER PRIMARY KEY,
    name text,
    comp stats_import.complex_type,
    arange int4range,
    tags text[]
) WITH (autovacuum_enabled = false)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist_cash_ops USING gist ADD
	FUNCTION	11  (money, money) gbt_cash_sortsupport (internal)
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30919_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_LIBRARY] CREATE EXTENSION unaccent
;
----------
[CREATE_TABLE] CREATE TABLE part_bogus_expr_fail PARTITION OF list_parted FOR VALUES IN (somename)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE generated_stored_tests.gtest20 ADD CONSTRAINT gtest20_b_check CHECK ((b < 50))
;
----------
[DROP_VIEW] drop view v_pagg_test
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_authid ADD PRIMARY KEY USING INDEX pg_authid_oid_index
;
----------
[CREATE_TYPE] CREATE DOMAIN "public"."testxmldomain" AS character varying
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR <= (ismn13, ismn13) SET (
	RESTRICT = scalarlesel,
	JOIN = scalarlejoinsel)
;
----------
[CREATE_TABLE] --
-- Check handling of MULTIEXPR SubPlans in inherited updates
--
create table inhpar(f1 int, f2 name)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "generated_stored_tests"."gtest_trigger_func4"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  NEW.a = 10;
  NEW.b = 300;
  RETURN NEW;
END;
$$
;
----------
[ADMIN_TABLE] ANALYZE public.varchar_tbl
;
