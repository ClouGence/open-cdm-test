create table rp_prefix_test3 (a int, b int, c int, d int) partition by range(a, b, c, d)
;
DROP TYPE j_unordered_pair
;
DROP COLLATION testing
;
-- As above, but create the primary key using a UNIQUE index
CREATE TABLE cnn_pk (a int, b int)
;
CREATE SUBSCRIPTION testsub2 CONNECTION 'port=13400 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION testpub2
;
CREATE INDEX "tenk2_hundred" ON "public"."tenk2" USING "btree" ("hundred")
;
CREATE INDEX ON t(a)
;
CREATE OPERATOR ~ (
        LEFTARG = lquery,
	RIGHTARG = _ltree,
	PROCEDURE = _ltq_rregex,
        COMMUTATOR = '~',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
TRUNCATE ONLY trunc_fb, ONLY trunc_fa
;
CREATE AGGREGATE "public"."logging_agg_nonstrict_initcond"("anyelement") (
    SFUNC = "public"."logging_sfunc_nonstrict",
    STYPE = "text",
    INITCOND = 'I',
    MSFUNC = "public"."logging_msfunc_nonstrict",
    MINVFUNC = "public"."logging_minvfunc_nonstrict",
    MSTYPE = "text",
    MINITCOND = 'MI'
)
;
create trigger but_trigger2 after update on convslot_test_child
referencing old table as old_table new table as new_table
for each statement execute function convslot_trig3()
;
CREATE FUNCTION _ltree_extract_risparent(_ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TRIGGER before_ins_stmt_trig BEFORE INSERT ON main_table
FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func('before_ins_stmt')
;
CREATE FUNCTION pg_get_logical_snapshot_info(IN filename text,
    OUT state text,
    OUT xmin xid,
    OUT xmax xid,
    OUT start_decoding_at pg_lsn,
    OUT two_phase_at pg_lsn,
    OUT initial_xmin_horizon xid,
    OUT building_full_snapshot boolean,
    OUT in_slot_creation boolean,
    OUT last_serialized_snapshot pg_lsn,
    OUT next_phase_at xid,
    OUT committed_count int4,
    OUT committed_xip xid[],
    OUT catchange_count int4,
    OUT catchange_xip xid[]
)
AS '$libdir/pg_logicalinspect', 'pg_get_logical_snapshot_info'
LANGUAGE C STRICT PARALLEL SAFE
;
CREATE RULE hat_nosert_all AS ON INSERT TO hats
    DO INSTEAD
    INSERT INTO hat_data VALUES (
           NEW.hat_name,
           NEW.hat_color)
        ON CONFLICT
        DO NOTHING
        RETURNING *
;
CREATE TABLESPACE test_recovery_conflict_tblspc LOCATION ''
;
CREATE OPERATOR "public".#@# (
    FUNCTION = "factorial",
    LEFTARG = bigint
)
;
ALTER OPERATOR FAMILY gist__intbig_ops USING gist
DROP OPERATOR 14 (_int4, _int4)
;
CREATE UNIQUE INDEX "system_name" ON "public"."system" USING "btree" ("name")
;
-- cleanup
DROP FUNCTION check_ddl_rewrite(regclass, text)
;
CREATE TRIGGER insert_a AFTER INSERT ON main_table
FOR EACH ROW WHEN (NEW.a = 123) EXECUTE PROCEDURE trigger_func('insert_a')
;
CREATE AGGREGATE myaggn04a(*) (SFUNC = stfp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
alter table anothertab drop constraint anothertab_chk
;
ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "hword_part" WITH "english_stem"
;
CREATE TABLE "public"."beta_neg_p3" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
-- plpgsql, proretset = f, prorettype = c
CREATE FUNCTION getrngfunc9(int) RETURNS rngfunc AS 'DECLARE rngfunctup rngfunc%ROWTYPE; BEGIN SELECT * into rngfunctup FROM rngfunc WHERE rngfuncid = $1; RETURN rngfunctup; END;' LANGUAGE plpgsql
;
create function sql_if(bool, anyelement, anyelement) returns anyelement as $$
select case when $1 then $2 else $3 end $$ language sql
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_constraint
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_statistic_ext_data
;
-- Add operators and support functions that are new in 9.1.  We do it like
-- this, leaving them "loose" in the operator family rather than bound into
-- the gist_trgm_ops opclass, because that's the only state that could be
-- reproduced during an upgrade from 9.0.

ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        2       <-> (text, text) FOR ORDER BY pg_catalog.float_ops,
        OPERATOR        3       pg_catalog.~~ (text, text),
        OPERATOR        4       pg_catalog.~~* (text, text),
        FUNCTION        8 (text, text)  gtrgm_distance (internal, text, smallint, oid, internal)
;
CREATE TYPE deptest_t AS (a int)
;
CREATE TABLE decoding(blah text)
;
TRUNCATE ONLY tru_ftable_parent
;
VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdescription
;
-- error

-- test CYCLE and NO CYCLE
ALTER SEQUENCE sequence_test2 RESTART WITH 24
  INCREMENT BY 4 MAXVALUE 36 MINVALUE 5 CYCLE
;
CREATE TABLESPACE ts1 LOCATION '/tmp/7ZWENuHVzG'
;
alter type insenum add value 'i18' before 'L2'
;
CREATE AGGREGATE "public"."myaggp09b"(integer) (
    SFUNC = "public"."tf1p",
    STYPE = integer[],
    INITCOND = '{}'
)
;
CREATE INDEX bttest_a_brin_idx ON bttest_a USING brin(id)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_cast
;
CREATE TYPE split_table_idx.typed_row AS (
    id integer,
    note text
)
;
create function same(ctype, ctype) returns boolean language sql
as 'select $1.f1 is not distinct from $2.f1 and $1.f2 is not distinct from $2.f2'
;
CREATE PUBLICATION tap_pub_B_2 FOR TABLE tab_part2_1
;
DROP DOMAIN IF EXISTS split_domain_type_next.int_list_domain CASCADE
;
CREATE TABLE testschema.part_1 PARTITION OF testschema.part FOR VALUES IN (1)
;
CREATE TABLE import_source.t4 (c1 int) PARTITION BY RANGE (c1)
;
ANALYZE public.pagg_tab2_p1
;
ALTER TABLE split_alter_table.base_table ENABLE REPLICA RULE base_table_notify
;
-- cannot add/drop NOT NULL or check constraints to *only* the parent, when
-- partitions exist
ALTER TABLE ONLY list_parted2 ALTER b SET NOT NULL
;
create table rtest_t9 (a int4, b text)
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] create table rp_prefix_test3 (a int, b int, c int, d int) partition by range(a, b, c, d)
;
----------
[DROP_TYPE] DROP TYPE j_unordered_pair
;
----------
[DROP_POLICY] DROP COLLATION testing
;
----------
[CREATE_TABLE] -- As above, but create the primary key using a UNIQUE index
CREATE TABLE cnn_pk (a int, b int)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION testsub2 CONNECTION 'port=13400 host=/tmp/QGH4Hh755I dbname=postgres' PUBLICATION testpub2
;
----------
[ADD_INDEX] CREATE INDEX "tenk2_hundred" ON "public"."tenk2" USING "btree" ("hundred")
;
----------
[ADD_INDEX] CREATE INDEX ON t(a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR ~ (
        LEFTARG = lquery,
	RIGHTARG = _ltree,
	PROCEDURE = _ltq_rregex,
        COMMUTATOR = '~',
        RESTRICT = contsel,
	JOIN = contjoinsel
)
;
----------
[TRUNCATE_TABLE] TRUNCATE ONLY trunc_fb, ONLY trunc_fa
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."logging_agg_nonstrict_initcond"("anyelement") (
    SFUNC = "public"."logging_sfunc_nonstrict",
    STYPE = "text",
    INITCOND = 'I',
    MSFUNC = "public"."logging_msfunc_nonstrict",
    MINVFUNC = "public"."logging_minvfunc_nonstrict",
    MSTYPE = "text",
    MINITCOND = 'MI'
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger but_trigger2 after update on convslot_test_child
referencing old table as old_table new table as new_table
for each statement execute function convslot_trig3()
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_extract_risparent(_ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER before_ins_stmt_trig BEFORE INSERT ON main_table
FOR EACH STATEMENT EXECUTE PROCEDURE trigger_func('before_ins_stmt')
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_get_logical_snapshot_info(IN filename text,
    OUT state text,
    OUT xmin xid,
    OUT xmax xid,
    OUT start_decoding_at pg_lsn,
    OUT two_phase_at pg_lsn,
    OUT initial_xmin_horizon xid,
    OUT building_full_snapshot boolean,
    OUT in_slot_creation boolean,
    OUT last_serialized_snapshot pg_lsn,
    OUT next_phase_at xid,
    OUT committed_count int4,
    OUT committed_xip xid[],
    OUT catchange_count int4,
    OUT catchange_xip xid[]
)
AS '$libdir/pg_logicalinspect', 'pg_get_logical_snapshot_info'
LANGUAGE C STRICT PARALLEL SAFE
;
----------
[CREATE_POLICY] CREATE RULE hat_nosert_all AS ON INSERT TO hats
    DO INSTEAD
    INSERT INTO hat_data VALUES (
           NEW.hat_name,
           NEW.hat_color)
        ON CONFLICT
        DO NOTHING
        RETURNING *
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE test_recovery_conflict_tblspc LOCATION ''
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".#@# (
    FUNCTION = "factorial",
    LEFTARG = bigint
)
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY gist__intbig_ops USING gist
DROP OPERATOR 14 (_int4, _int4)
;
----------
[ADD_INDEX] CREATE UNIQUE INDEX "system_name" ON "public"."system" USING "btree" ("name")
;
----------
[DROP_PROG_OBJ] -- cleanup
DROP FUNCTION check_ddl_rewrite(regclass, text)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] CREATE TRIGGER insert_a AFTER INSERT ON main_table
FOR EACH ROW WHEN (NEW.a = 123) EXECUTE PROCEDURE trigger_func('insert_a')
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn04a(*) (SFUNC = stfp, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] alter table anothertab drop constraint anothertab_chk
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH CONFIGURATION "public"."thesaurus_tst"
    ADD MAPPING FOR "hword_part" WITH "english_stem"
;
----------
[CREATE_TABLE] CREATE TABLE "public"."beta_neg_p3" (
    "a" double precision,
    "b" integer,
    "c" "text"
)
;
----------
[CREATE_PROG_OBJ] -- plpgsql, proretset = f, prorettype = c
CREATE FUNCTION getrngfunc9(int) RETURNS rngfunc AS 'DECLARE rngfunctup rngfunc%ROWTYPE; BEGIN SELECT * into rngfunctup FROM rngfunc WHERE rngfuncid = $1; RETURN rngfunctup; END;' LANGUAGE plpgsql
;
----------
[CREATE_PROG_OBJ] create function sql_if(bool, anyelement, anyelement) returns anyelement as $$
select case when $1 then $2 else $3 end $$ language sql
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_constraint
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_statistic_ext_data
;
----------
[ALTER_PROG_OBJ] -- Add operators and support functions that are new in 9.1.  We do it like
-- this, leaving them "loose" in the operator family rather than bound into
-- the gist_trgm_ops opclass, because that's the only state that could be
-- reproduced during an upgrade from 9.0.

ALTER OPERATOR FAMILY gist_trgm_ops USING gist ADD
        OPERATOR        2       <-> (text, text) FOR ORDER BY pg_catalog.float_ops,
        OPERATOR        3       pg_catalog.~~ (text, text),
        OPERATOR        4       pg_catalog.~~* (text, text),
        FUNCTION        8 (text, text)  gtrgm_distance (internal, text, smallint, oid, internal)
;
----------
[CREATE_TYPE] CREATE TYPE deptest_t AS (a int)
;
----------
[CREATE_TABLE] CREATE TABLE decoding(blah text)
;
----------
[TRUNCATE_TABLE] TRUNCATE ONLY tru_ftable_parent
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_shdescription
;
----------
[ALTER_SEQUENCE] -- error

-- test CYCLE and NO CYCLE
ALTER SEQUENCE sequence_test2 RESTART WITH 24
  INCREMENT BY 4 MAXVALUE 36 MINVALUE 5 CYCLE
;
----------
[CREATE_TABLESPACE] CREATE TABLESPACE ts1 LOCATION '/tmp/7ZWENuHVzG'
;
----------
[ALTER_TYPE] alter type insenum add value 'i18' before 'L2'
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE "public"."myaggp09b"(integer) (
    SFUNC = "public"."tf1p",
    STYPE = integer[],
    INITCOND = '{}'
)
;
----------
[ADD_INDEX] CREATE INDEX bttest_a_brin_idx ON bttest_a USING brin(id)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_cast
;
----------
[CREATE_TYPE] CREATE TYPE split_table_idx.typed_row AS (
    id integer,
    note text
)
;
----------
[CREATE_PROG_OBJ] create function same(ctype, ctype) returns boolean language sql
as 'select $1.f1 is not distinct from $2.f1 and $1.f2 is not distinct from $2.f2'
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION tap_pub_B_2 FOR TABLE tab_part2_1
;
----------
[DROP_TYPE] DROP DOMAIN IF EXISTS split_domain_type_next.int_list_domain CASCADE
;
----------
[CREATE_TABLE] CREATE TABLE testschema.part_1 PARTITION OF testschema.part FOR VALUES IN (1)
;
----------
[CREATE_TABLE] CREATE TABLE import_source.t4 (c1 int) PARTITION BY RANGE (c1)
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab2_p1
;
----------
[ALTER_TABLE] ALTER TABLE split_alter_table.base_table ENABLE REPLICA RULE base_table_notify
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- cannot add/drop NOT NULL or check constraints to *only* the parent, when
-- partitions exist
ALTER TABLE ONLY list_parted2 ALTER b SET NOT NULL
;
----------
[CREATE_TABLE] create table rtest_t9 (a int4, b text)
;
