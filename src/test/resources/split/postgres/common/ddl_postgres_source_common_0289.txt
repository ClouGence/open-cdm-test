CREATE TABLE "public"."cc1" (
    "f1" integer CONSTRAINT "pp1_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" integer,
    "a2" integer
)
;
CREATE TABLE bttest_b(id int8)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_aggregate
;
COMMENT ON TRIGGER accounts_check_update ON split_trigger_probe.accounts IS 'balance update trigger'
;
create type int8alias1
;
CREATE FUNCTION make_valid(issn)
	RETURNS issn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- test hstore[] -> python
CREATE FUNCTION test1arr(val hstore[]) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
assert(val == [{'aa': 'bb', 'cc': None}, {'dd': 'ee'}])
return len(val)
$$
;
CREATE UNLOGGED SEQUENCE ulseq
;
-- empty table
CREATE INDEX CONCURRENTLY concur_index1 ON concur_heap(f2,f1)
;
create cast (int8 as int8alias1) without function
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
CREATE VIEW v12_temp AS SELECT true FROM v11_temp
;
create table p1 (b int, a int not null) partition by range (b)
;
CREATE DATABASE tbuiltin5 ENCODING 'UTF-8' TEMPLATE template0 BUILTIN_LOCALE 'C.UTF8' LC_COLLATE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
CALL test_proc7cc(10)
;
ALTER TABLE trgfire DISABLE TRIGGER tg
;
VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_language
;
CREATE PROCEDURE test_proc2()
LANGUAGE plpgsql
AS $$
BEGIN
    RETURN 5;
END;
$$
;
-- not supported by sepgsql

ALTER TABLE regtest_table_part SET (fillfactor = 75)
;
-- overlap with part_4
CREATE TABLE fail_part PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
-- ... unless it actually is unique
create table j3 as select unique1, tenthous from onek
;
CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT shared_blk_read_time float8,
    OUT shared_blk_write_time float8,
    OUT local_blk_read_time float8,
    OUT local_blk_write_time float8,
    OUT temp_blk_read_time float8,
    OUT temp_blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric,
    OUT wal_buffers_full int8,
    OUT jit_functions int8,
    OUT jit_generation_time float8,
    OUT jit_inlining_count int8,
    OUT jit_inlining_time float8,
    OUT jit_optimization_count int8,
    OUT jit_optimization_time float8,
    OUT jit_emission_count int8,
    OUT jit_emission_time float8,
    OUT jit_deform_count int8,
    OUT jit_deform_time float8,
    OUT parallel_workers_to_launch int8,
    OUT parallel_workers_launched int8,
    OUT stats_since timestamp with time zone,
    OUT minmax_stats_since timestamp with time zone
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_12'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
ALTER TABLE tab5 DROP COLUMN b, ADD COLUMN c INT
;
--
-- injection_points_set_local()
--
-- Trigger switch to link any future injection points attached to the
-- current process, useful to make SQL tests concurrently-safe.
--
CREATE FUNCTION injection_points_set_local()
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_set_local'
LANGUAGE C STRICT PARALLEL UNSAFE
;
create view vv5 as select x,y,z from tt9 join tt10 using(x)
;
create or replace temp view v1 as
  select 2+2+4+(select max(unique1) from tenk1) as f1
;
CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE INDEX ON schema_not_existing.tab (id)
;
create trigger samename after delete on trigpart1 execute function trigger_nothing()
;
DROP TABLE test7
;
CREATE VIEW v2 AS SELECT * FROM s2 WHERE y like '%af%'
;
CREATE VIEW atestv3 AS SELECT * FROM atest3
;
--gin path opclass
DROP INDEX jidx
;
CREATE FUNCTION gbt_text_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
ANALYZE pg_temp_2.pg_temp_28316
;
CREATE SUBSCRIPTION sub2 CONNECTION 'port=15683 host=/tmp/7RF1JxeTxg dbname=postgres' PUBLICATION pub2
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_default_acl
;
/*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
CREATE DOMAIN domain2 AS int DEFAULT 10 + (random() * 10)::int
;
create or replace function stacked_diagnostics_test() returns void as $$
declare _column_name text;
        _constraint_name text;
        _datatype_name text;
        _table_name text;
        _schema_name text;
begin
  raise exception using
    column = '>>some column name<<',
    constraint = '>>some constraint name<<',
    datatype = '>>some datatype name<<',
    table = '>>some table name<<',
    schema = '>>some schema name<<';
exception when others then
  get stacked diagnostics
        _column_name = column_name,
        _constraint_name = constraint_name,
        _datatype_name = pg_datatype_name,
        _table_name = table_name,
        _schema_name = schema_name;
  raise notice 'column %, constraint %, type %, table %, schema %',
    _column_name, _constraint_name, _datatype_name, _table_name, _schema_name;
end;
$$ language plpgsql
;
DROP EXTENSION hstore_plperlu
;
--
-- BOX
--

--
-- box logic
--	     o
-- 3	  o--|X
--	  |  o|
-- 2	+-+-+ |
--	| | | |
-- 1	| o-+-o
--	|   |
-- 0	+---+
--
--	0 1 2 3
--

-- boxes are specified by two points, given by four floats x1,y1,x2,y2


CREATE TABLE BOX_TBL (f1 box)
;
VACUUM ANALYZE num_exp_add
;
CREATE TABLE test_part_a_1 PARTITION OF test_part_a FOR VALUES IN (1,2,3,4,5)
;
COMMENT ON TABLE attmp_wrong IS 'table comment'
;
---------------------------------------------------
-- ISMN:
CREATE FUNCTION btismncmp(ismn, ismn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
ALTER TYPE test8a ADD ATTRIBUTE c priv_testdomain1
;
COMMENT ON TEXT SEARCH TEMPLATE snowball IS 'snowball stemmer'
;
drop table ma_test
;
do $$
declare x constant int[]; y int;
begin
  for x[1], y in select 1, 2 loop  -- fail (currently, unsupported syntax)
  end loop;
end$$
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
------------------------------------------------------------------------------------------
[CREATE_TABLE] CREATE TABLE "public"."cc1" (
    "f1" integer CONSTRAINT "pp1_f1_not_null" NOT NULL,
    "f2" "text",
    "f3" integer,
    "a2" integer
)
;
----------
[CREATE_TABLE] CREATE TABLE bttest_b(id int8)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_aggregate
;
----------
[COMMENT_TRIGGER] COMMENT ON TRIGGER accounts_check_update ON split_trigger_probe.accounts IS 'balance update trigger'
;
----------
[CREATE_TYPE] create type int8alias1
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION make_valid(issn)
	RETURNS issn
	AS '$libdir/isn'
	LANGUAGE C
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] -- test hstore[] -> python
CREATE FUNCTION test1arr(val hstore[]) RETURNS int
LANGUAGE plpythonu
TRANSFORM FOR TYPE hstore
AS $$
assert(val == [{'aa': 'bb', 'cc': None}, {'dd': 'ee'}])
return len(val)
$$
;
----------
[CREATE_SEQUENCE] CREATE UNLOGGED SEQUENCE ulseq
;
----------
[ADD_INDEX] -- empty table
CREATE INDEX CONCURRENTLY concur_index1 ON concur_heap(f2,f1)
;
----------
[CREATE_PROG_OBJ] create cast (int8 as int8alias1) without function
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_event_trigger
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v12_temp AS SELECT true FROM v11_temp
;
----------
[CREATE_TABLE] create table p1 (b int, a int not null) partition by range (b)
;
----------
[CREATE_CATALOG] CREATE DATABASE tbuiltin5 ENCODING 'UTF-8' TEMPLATE template0 BUILTIN_LOCALE 'C.UTF8' LC_COLLATE 'C' LC_CTYPE 'C' LOCALE_PROVIDER builtin
;
----------
[CALL_PROG_OBJ] CALL test_proc7cc(10)
;
----------
[ALTER_TABLE] ALTER TABLE trgfire DISABLE TRIGGER tg
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, PARALLEL 2) pg_catalog.pg_language
;
----------
[CREATE_PROG_OBJ] CREATE PROCEDURE test_proc2()
LANGUAGE plpgsql
AS $$
BEGIN
    RETURN 5;
END;
$$
;
----------
[ALTER_TABLE] -- not supported by sepgsql

ALTER TABLE regtest_table_part SET (fillfactor = 75)
;
----------
[CREATE_TABLE] -- overlap with part_4
CREATE TABLE fail_part PARTITION OF hash_parted2 FOR VALUES WITH (MODULUS 2, REMAINDER 1)
;
----------
[CREATE_TABLE|SELECT] -- ... unless it actually is unique
create table j3 as select unique1, tenthous from onek
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION pg_stat_statements(IN showtext boolean,
    OUT userid oid,
    OUT dbid oid,
    OUT toplevel bool,
    OUT queryid bigint,
    OUT query text,
    OUT plans int8,
    OUT total_plan_time float8,
    OUT min_plan_time float8,
    OUT max_plan_time float8,
    OUT mean_plan_time float8,
    OUT stddev_plan_time float8,
    OUT calls int8,
    OUT total_exec_time float8,
    OUT min_exec_time float8,
    OUT max_exec_time float8,
    OUT mean_exec_time float8,
    OUT stddev_exec_time float8,
    OUT rows int8,
    OUT shared_blks_hit int8,
    OUT shared_blks_read int8,
    OUT shared_blks_dirtied int8,
    OUT shared_blks_written int8,
    OUT local_blks_hit int8,
    OUT local_blks_read int8,
    OUT local_blks_dirtied int8,
    OUT local_blks_written int8,
    OUT temp_blks_read int8,
    OUT temp_blks_written int8,
    OUT shared_blk_read_time float8,
    OUT shared_blk_write_time float8,
    OUT local_blk_read_time float8,
    OUT local_blk_write_time float8,
    OUT temp_blk_read_time float8,
    OUT temp_blk_write_time float8,
    OUT wal_records int8,
    OUT wal_fpi int8,
    OUT wal_bytes numeric,
    OUT wal_buffers_full int8,
    OUT jit_functions int8,
    OUT jit_generation_time float8,
    OUT jit_inlining_count int8,
    OUT jit_inlining_time float8,
    OUT jit_optimization_count int8,
    OUT jit_optimization_time float8,
    OUT jit_emission_count int8,
    OUT jit_emission_time float8,
    OUT jit_deform_count int8,
    OUT jit_deform_time float8,
    OUT parallel_workers_to_launch int8,
    OUT parallel_workers_launched int8,
    OUT stats_since timestamp with time zone,
    OUT minmax_stats_since timestamp with time zone
)
RETURNS SETOF record
AS '$libdir/pg_stat_statements', 'pg_stat_statements_1_12'
LANGUAGE C STRICT VOLATILE PARALLEL SAFE
;
----------
[ALTER_TABLE|DROP_COLUMN|ADD_COLUMN] ALTER TABLE tab5 DROP COLUMN b, ADD COLUMN c INT
;
----------
[CREATE_PROG_OBJ] --
-- injection_points_set_local()
--
-- Trigger switch to link any future injection points attached to the
-- current process, useful to make SQL tests concurrently-safe.
--
CREATE FUNCTION injection_points_set_local()
RETURNS void
AS 'MODULE_PATHNAME', 'injection_points_set_local'
LANGUAGE C STRICT PARALLEL UNSAFE
;
----------
[CREATE_VIEW(SELECT)] create view vv5 as select x,y,z from tt9 join tt10 using(x)
;
----------
[CREATE_VIEW(SELECT)] create or replace temp view v1 as
  select 2+2+4+(select max(unique1) from tenk1) as f1
;
----------
[CREATE_SCHEMA] CREATE SCHEMA regress_schema_1 AUTHORIZATION CURRENT_USER
  CREATE INDEX ON schema_not_existing.tab (id)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger samename after delete on trigpart1 execute function trigger_nothing()
;
----------
[DROP_TABLE] DROP TABLE test7
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW v2 AS SELECT * FROM s2 WHERE y like '%af%'
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW atestv3 AS SELECT * FROM atest3
;
----------
[DROP_INDEX] --gin path opclass
DROP INDEX jidx
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_text_picksplit(internal, internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_2.pg_temp_28316
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub2 CONNECTION 'port=15683 host=/tmp/7RF1JxeTxg dbname=postgres' PUBLICATION pub2
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_default_acl
;
----------
[CREATE_POLICY] /*
 * text search configuration for lithuanian language
 *
 * Copyright (c) 2007-2022, PostgreSQL Global Development Group
 *
 * src/backend/snowball/snowball.sql.in
 *
 * lithuanian and certain other macros are replaced for each language;
 * see the Makefile for details.
 *
 * Note: this file is read in single-user -j mode, which means that the
 * command terminator is semicolon-newline-newline; whenever the backend
 * sees that, it stops and executes what it's got.  If you write a lot of
 * statements without empty lines between, they'll all get quoted to you
 * in any error message about one of them, so don't do that.  Also, you
 * cannot write a semicolon immediately followed by an empty line in a
 * string literal (including a function body!) or a multiline comment.
 */

CREATE TEXT SEARCH DICTIONARY lithuanian_stem
	(TEMPLATE = snowball, Language = lithuanian )
;
----------
[CREATE_TYPE] CREATE DOMAIN domain2 AS int DEFAULT 10 + (random() * 10)::int
;
----------
[CREATE_PROG_OBJ] create or replace function stacked_diagnostics_test() returns void as $$
declare _column_name text;
        _constraint_name text;
        _datatype_name text;
        _table_name text;
        _schema_name text;
begin
  raise exception using
    column = '>>some column name<<',
    constraint = '>>some constraint name<<',
    datatype = '>>some datatype name<<',
    table = '>>some table name<<',
    schema = '>>some schema name<<';
exception when others then
  get stacked diagnostics
        _column_name = column_name,
        _constraint_name = constraint_name,
        _datatype_name = pg_datatype_name,
        _table_name = table_name,
        _schema_name = schema_name;
  raise notice 'column %, constraint %, type %, table %, schema %',
    _column_name, _constraint_name, _datatype_name, _table_name, _schema_name;
end;
$$ language plpgsql
;
----------
[DROP_LIBRARY] DROP EXTENSION hstore_plperlu
;
----------
[CREATE_TABLE] --
-- BOX
--

--
-- box logic
--	     o
-- 3	  o--|X
--	  |  o|
-- 2	+-+-+ |
--	| | | |
-- 1	| o-+-o
--	|   |
-- 0	+---+
--
--	0 1 2 3
--

-- boxes are specified by two points, given by four floats x1,y1,x2,y2


CREATE TABLE BOX_TBL (f1 box)
;
----------
[ADMIN_TABLE] VACUUM ANALYZE num_exp_add
;
----------
[CREATE_TABLE] CREATE TABLE test_part_a_1 PARTITION OF test_part_a FOR VALUES IN (1,2,3,4,5)
;
----------
[COMMENT_TABLE] COMMENT ON TABLE attmp_wrong IS 'table comment'
;
----------
[CREATE_PROG_OBJ] ---------------------------------------------------
-- ISMN:
CREATE FUNCTION btismncmp(ismn, ismn)
	RETURNS int4
	AS 'btint8cmp'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_TYPE] ALTER TYPE test8a ADD ATTRIBUTE c priv_testdomain1
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH TEMPLATE snowball IS 'snowball stemmer'
;
----------
[DROP_TABLE] drop table ma_test
;
----------
[BLOCK] do $$
declare x constant int[]; y int;
begin
  for x[1], y in select 1, 2 loop  -- fail (currently, unsupported syntax)
  end loop;
end$$
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config_map
;
