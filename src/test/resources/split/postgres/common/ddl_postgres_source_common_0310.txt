COMMENT ON TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser IS 'configuration probe'
;
--
-- test deinherit procedure
--

-- deinherit inh_child1
create table inh_child3 () inherits (inh_child1)
;
CREATE AGGREGATE testagg3(int2) (SFUNC = int2_sum, STYPE = int8)
;
-- fail
SECURITY LABEL ON TABLE dummy_seclabel_tbl2 IS 'unclassified'
;
ANALYZE public.pagg_tab2_p2
;
CREATE FUNCTION "generated_virtual_tests"."gtest_trigger_func3"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'OK';
  RETURN NEW;
END
$$
;
CREATE FUNCTION dblink_build_sql_update (text, int2vector, int, _text, _text)
RETURNS text
AS '$libdir/dblink','dblink_build_sql_update'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
CREATE PUBLICATION testpub_xxx WITH (publish = 'cluster, vacuum')
;
ANALYZE pg_temp_4.pg_temp_37808
;
VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
ANALYZE public.bt_f8_heap
;
ALTER TABLE itest4 ALTER COLUMN a SET NOT NULL
;
CREATE AGGREGATE myaggp12a(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
ALTER TABLE test1 ALTER COLUMN b SET STORAGE EXTERNAL
;
ALTER SEQUENCE split_cov.seq_test RESTART WITH 20
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_ts_template
;
-- this test case also checks for ambiguity in the queries issued by
-- refresh_by_match_merge(), by choosing column names that intentionally
-- duplicate all the aliases used in those queries
CREATE TABLE mvtest_foo_data AS SELECT i,
  i+1 AS tid,
  fipshash(random()::text) AS mv,
  fipshash(random()::text) AS newdata,
  fipshash(random()::text) AS newdata2,
  fipshash(random()::text) AS diff
  FROM generate_series(1, 10) i
;
ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (ismn13, ean13),
	OPERATOR 1  = (ismn13, ismn)
;
ALTER TABLE constraint_rename_test ADD CONSTRAINT con3 PRIMARY KEY (a)
;
-- verify invalidation of partition table having parent and child tables in
-- different schema
CREATE SCHEMA pub_testpart1
;
ALTER INDEX ptif_test_index ATTACH PARTITION ptif_test2_index
;
CREATE VIEW pg_stat_user_tables AS
    SELECT * FROM pg_stat_all_tables
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema') AND
          schemaname !~ '^pg_toast'
;
ALTER TABLE ONLY "regress_indexing"."pk2" DROP COLUMN "........pg.dropped.1........"
;
DO $$
BEGIN
    PERFORM 1;
END
$$
;
create table boolp (a bool) partition by list (a)
;
ALTER INDEX dummy_test_idx SET (option_string_null = NULL)
;
CREATE SUBSCRIPTION sub1 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub1
;
-- mcv covering just a small fraction of data
CREATE TABLE mcv_lists_partial (
    a INT,
    b INT,
    c INT
)
;
ALTER TABLE test_replica_identity5 ALTER b SET NOT NULL
;
CREATE FUNCTION gbt_int2_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TYPE gbtreekey4 (
	INTERNALLENGTH = 4,
	INPUT  = gbtreekey4_in,
	OUTPUT = gbtreekey4_out
)
;
ALTER FOREIGN TABLE ft1 ADD COLUMN c9 integer
;
create function func_with_bad_set() returns int as $$ select 1 $$
language sql
set default_text_search_config = no_such_config
;
CREATE FUNCTION isnle(ean13, upc)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
-- ok

ALTER VIEW gtest1v ALTER COLUMN b SET DEFAULT 100
;
ALTER TABLE pg_trigger ADD UNIQUE USING INDEX pg_trigger_tgrelid_tgname_index
;
ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p3 FOR VALUES IN ('0007', '0009')
;
create table parted_constr (a int, b text)
  partition by range (b)
;
CREATE OR REPLACE FUNCTION test_slru_page_truncate(int) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_truncate' LANGUAGE C
;
CREATE OPERATOR "public".< (
    FUNCTION = "public"."int8alias1lt",
    LEFTARG = "public"."int8alias1",
    RIGHTARG = "public"."int8alias1"
)
;
CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 int)
;
-- DROP behavior with temporary partitioned indexes
create temp table idxpart_temp (a int) partition by range (a)
;
CREATE FUNCTION gbt_enum_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
CREATE TABLE test32 (a int, b char(3) COLLATE case_sensitive) PARTITION BY HASH (b)
;
CREATE EXTENSION pgcrypto
;
CREATE FUNCTION gin_extract_value_numeric(numeric, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
COMMENT ON TEXT SEARCH DICTIONARY dutch_stem IS 'snowball stemmer for dutch language'
;
create function test_ret_set_rec_dyn(int) returns setof record as '
DECLARE
	retval RECORD;
BEGIN
	IF $1 > 10 THEN
		SELECT INTO retval 5, 10, 15;
		RETURN NEXT retval;
		RETURN NEXT retval;
	ELSE
		SELECT INTO retval 50, 5::numeric, ''xxx''::text;
		RETURN NEXT retval;
		RETURN NEXT retval;
	END IF;
	RETURN;
END;' language plpgsql
;
CREATE VIEW check_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(pg_get_expr(con.conbin, coalesce(c.oid, 0)) AS character_data) AS check_clause
    FROM pg_constraint con
           LEFT OUTER JOIN pg_namespace rs ON (rs.oid = con.connamespace)
           LEFT OUTER JOIN pg_class c ON (c.oid = con.conrelid)
           LEFT OUTER JOIN pg_type t ON (t.oid = con.contypid)
    WHERE pg_has_role(coalesce(c.relowner, t.typowner), 'USAGE')
      AND con.contype = 'c'

    UNION ALL
    -- not-null constraints
    SELECT current_database()::information_schema.sql_identifier AS constraint_catalog,
           rs.nspname::information_schema.sql_identifier AS constraint_schema,
           con.conname::information_schema.sql_identifier AS constraint_name,
           pg_catalog.format('%s IS NOT NULL', coalesce(at.attname, 'VALUE'))::information_schema.character_data AS check_clause
     FROM pg_constraint con
            LEFT JOIN pg_namespace rs ON rs.oid = con.connamespace
            LEFT JOIN pg_class c ON c.oid = con.conrelid
            LEFT JOIN pg_type t ON t.oid = con.contypid
            LEFT JOIN pg_attribute at ON (con.conrelid = at.attrelid AND con.conkey[1] = at.attnum)
     WHERE pg_has_role(coalesce(c.relowner, t.typowner), 'USAGE'::text)
       AND con.contype = 'n'
;
ALTER OPERATOR >= (ean13, ismn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
------------------------------------------------------------------------------------------
[ALTER_POLICY] COMMENT ON TEXT SEARCH CONFIGURATION split_ts_probe.cfg_parser IS 'configuration probe'
;
----------
[CREATE_TABLE] --
-- test deinherit procedure
--

-- deinherit inh_child1
create table inh_child3 () inherits (inh_child1)
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE testagg3(int2) (SFUNC = int2_sum, STYPE = int8)
;
----------
[UNKNOWN] -- fail
SECURITY LABEL ON TABLE dummy_seclabel_tbl2 IS 'unclassified'
;
----------
[ADMIN_TABLE] ANALYZE public.pagg_tab2_p2
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "generated_virtual_tests"."gtest_trigger_func3"() RETURNS "trigger"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
  RAISE NOTICE 'OK';
  RETURN NEW;
END
$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION dblink_build_sql_update (text, int2vector, int, _text, _text)
RETURNS text
AS '$libdir/dblink','dblink_build_sql_update'
LANGUAGE C STRICT PARALLEL RESTRICTED
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION testpub_xxx WITH (publish = 'cluster, vacuum')
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_4.pg_temp_37808
;
----------
[ADMIN_TABLE] VACUUM (INDEX_CLEANUP FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_seclabel
;
----------
[ADMIN_TABLE] ANALYZE public.bt_f8_heap
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE itest4 ALTER COLUMN a SET NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggp12a(BASETYPE = anyelement, SFUNC = tfp, STYPE = int[],
  FINALFUNC = ffp, INITCOND = '{}')
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test1 ALTER COLUMN b SET STORAGE EXTERNAL
;
----------
[ALTER_SEQUENCE] ALTER SEQUENCE split_cov.seq_test RESTART WITH 20
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_ts_template
;
----------
[CREATE_TABLE|SELECT] -- this test case also checks for ambiguity in the queries issued by
-- refresh_by_match_merge(), by choosing column names that intentionally
-- duplicate all the aliases used in those queries
CREATE TABLE mvtest_foo_data AS SELECT i,
  i+1 AS tid,
  fipshash(random()::text) AS mv,
  fipshash(random()::text) AS newdata,
  fipshash(random()::text) AS newdata2,
  fipshash(random()::text) AS diff
  FROM generate_series(1, 10) i
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR FAMILY isn_ops USING hash ADD
	OPERATOR 1  = (ismn13, ean13),
	OPERATOR 1  = (ismn13, ismn)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE constraint_rename_test ADD CONSTRAINT con3 PRIMARY KEY (a)
;
----------
[CREATE_SCHEMA] -- verify invalidation of partition table having parent and child tables in
-- different schema
CREATE SCHEMA pub_testpart1
;
----------
[ALTER_INDEX] ALTER INDEX ptif_test_index ATTACH PARTITION ptif_test2_index
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_user_tables AS
    SELECT * FROM pg_stat_all_tables
    WHERE schemaname NOT IN ('pg_catalog', 'information_schema') AND
          schemaname !~ '^pg_toast'
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE ONLY "regress_indexing"."pk2" DROP COLUMN "........pg.dropped.1........"
;
----------
[BLOCK] DO $$
BEGIN
    PERFORM 1;
END
$$
;
----------
[CREATE_TABLE] create table boolp (a bool) partition by list (a)
;
----------
[ALTER_INDEX] ALTER INDEX dummy_test_idx SET (option_string_null = NULL)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION sub1 CONNECTION 'port=13006 host=/tmp/5YjKNALc1x dbname=postgres' PUBLICATION pub1
;
----------
[CREATE_TABLE] -- mcv covering just a small fraction of data
CREATE TABLE mcv_lists_partial (
    a INT,
    b INT,
    c INT
)
;
----------
[ALTER_TABLE|ALTER_COLUMN] ALTER TABLE test_replica_identity5 ALTER b SET NOT NULL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_int2_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TYPE] CREATE TYPE gbtreekey4 (
	INTERNALLENGTH = 4,
	INPUT  = gbtreekey4_in,
	OUTPUT = gbtreekey4_out
)
;
----------
[ALTER_TABLE|ADD_COLUMN] ALTER FOREIGN TABLE ft1 ADD COLUMN c9 integer
;
----------
[CREATE_PROG_OBJ] create function func_with_bad_set() returns int as $$ select 1 $$
language sql
set default_text_search_config = no_such_config
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION isnle(ean13, upc)
	RETURNS boolean
	AS 'int8le'
	LANGUAGE 'internal'
	IMMUTABLE STRICT
	PARALLEL SAFE
;
----------
[ALTER_VIEW|ALTER_COLUMN] -- ok

ALTER VIEW gtest1v ALTER COLUMN b SET DEFAULT 100
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_trigger ADD UNIQUE USING INDEX pg_trigger_tgrelid_tgname_index
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE plt2_adv ATTACH PARTITION plt2_adv_p3 FOR VALUES IN ('0007', '0009')
;
----------
[CREATE_TABLE] create table parted_constr (a int, b text)
  partition by range (b)
;
----------
[CREATE_PROG_OBJ] CREATE OR REPLACE FUNCTION test_slru_page_truncate(int) RETURNS VOID
  AS '$libdir/test_slru', 'test_slru_page_truncate' LANGUAGE C
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR "public".< (
    FUNCTION = "public"."int8alias1lt",
    LEFTARG = "public"."int8alias1",
    RIGHTARG = "public"."int8alias1"
)
;
----------
[CREATE_TABLE] CREATE TABLE tbl (c1 int,c2 int, c3 int, c4 int)
;
----------
[CREATE_TABLE] -- DROP behavior with temporary partitioned indexes
create temp table idxpart_temp (a int) partition by range (a)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gbt_enum_compress(internal)
RETURNS internal
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[CREATE_TABLE] CREATE TABLE test32 (a int, b char(3) COLLATE case_sensitive) PARTITION BY HASH (b)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pgcrypto
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION gin_extract_value_numeric(numeric, internal)
RETURNS internal
AS '$libdir/btree_gin'
LANGUAGE C STRICT IMMUTABLE
;
----------
[ALTER_POLICY] COMMENT ON TEXT SEARCH DICTIONARY dutch_stem IS 'snowball stemmer for dutch language'
;
----------
[CREATE_PROG_OBJ] create function test_ret_set_rec_dyn(int) returns setof record as '
DECLARE
	retval RECORD;
BEGIN
	IF $1 > 10 THEN
		SELECT INTO retval 5, 10, 15;
		RETURN NEXT retval;
		RETURN NEXT retval;
	ELSE
		SELECT INTO retval 50, 5::numeric, ''xxx''::text;
		RETURN NEXT retval;
		RETURN NEXT retval;
	END IF;
	RETURN;
END;' language plpgsql
;
----------
[CREATE_VIEW(SELECT,METADATA)] CREATE VIEW check_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(pg_get_expr(con.conbin, coalesce(c.oid, 0)) AS character_data) AS check_clause
    FROM pg_constraint con
           LEFT OUTER JOIN pg_namespace rs ON (rs.oid = con.connamespace)
           LEFT OUTER JOIN pg_class c ON (c.oid = con.conrelid)
           LEFT OUTER JOIN pg_type t ON (t.oid = con.contypid)
    WHERE pg_has_role(coalesce(c.relowner, t.typowner), 'USAGE')
      AND con.contype = 'c'

    UNION ALL
    -- not-null constraints
    SELECT current_database()::information_schema.sql_identifier AS constraint_catalog,
           rs.nspname::information_schema.sql_identifier AS constraint_schema,
           con.conname::information_schema.sql_identifier AS constraint_name,
           pg_catalog.format('%s IS NOT NULL', coalesce(at.attname, 'VALUE'))::information_schema.character_data AS check_clause
     FROM pg_constraint con
            LEFT JOIN pg_namespace rs ON rs.oid = con.connamespace
            LEFT JOIN pg_class c ON c.oid = con.conrelid
            LEFT JOIN pg_type t ON t.oid = con.contypid
            LEFT JOIN pg_attribute at ON (con.conrelid = at.attrelid AND con.conkey[1] = at.attnum)
     WHERE pg_has_role(coalesce(c.relowner, t.typowner), 'USAGE'::text)
       AND con.contype = 'n'
;
----------
[ALTER_PROG_OBJ] ALTER OPERATOR >= (ean13, ismn13) SET (
	RESTRICT = scalargesel,
	JOIN = scalargejoinsel)
;
