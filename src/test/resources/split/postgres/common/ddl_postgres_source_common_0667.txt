drop table notnull_tbl1
;
CREATE TEMP TABLE pg_temp_12.pg_temp_26722_2 (tid pg_catalog.tid)
;
CREATE TEXT SEARCH CONFIGURATION french
	(PARSER = default)
;
create or replace function trigger_notice() returns trigger as $$
  declare
    arg1 text = TG_ARGV[0];
    arg2 integer = TG_ARGV[1];
  begin
    raise notice 'trigger % on % % % for % args % %',
		TG_NAME, TG_TABLE_NAME, TG_WHEN, TG_OP, TG_LEVEL, arg1, arg2;
    return null;
  end;
  $$ language plpgsql
;
CREATE TYPE dump_test.int42
;
ALTER TABLE t2 ENABLE ROW LEVEL SECURITY
;
create table rule_and_refint_t2 (
	id2a integer,
	id2c integer,
	primary key (id2a, id2c)
)
;
ALTER TYPE test_type DROP ATTRIBUTE c
;
CREATE TABLE "regress_indexing"."pk51" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
CREATE TABLE fk_partitioned_fk_4_2 (a int, b int, FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk(a, b) ON UPDATE SET NULL)
;
create table descr_t1 ( id serial primary key , t text )
;
create table utr1 (a int check (a in (1)), q text, b text)
;
DROP TEXT SEARCH PARSER test_tsparser_exists
;
create or replace function avg_transfn(state avg_state, n int) returns avg_state as
$$
declare new_state avg_state;
begin
	raise notice 'avg_transfn called with %', n;
	if state is null then
		if n is not null then
			new_state.total := n;
			new_state.count := 1;
			return new_state;
		end if;
		return null;
	elsif n is not null then
		state.total := state.total + n;
		state.count := state.count + 1;
		return state;
	end if;

	return null;
end
$$ language plpgsql
;
VACUUM (FULL) pg_catalog.pg_rewrite
;
-- Can't modify inheritability of inherited constraints
create table inh_nn2 () inherits (inh_nn1)
;
CREATE OPERATOR split_typeconv.= (
    FUNCTION = split_typeconv.mytext_eq_text,
    LEFTARG = split_typeconv.mytext,
    RIGHTARG = text
)
;
-- RETURNING in CTEs
CREATE TABLE sq_target_merge_log (tid integer NOT NULL, last_change text)
;
CREATE TABLE tbl1_part_p2 PARTITION OF tbl1_part FOR VALUES FROM (10) TO (20)
;
CREATE TABLE "public"."rtest_unitfact" (
    "unit" character(4),
    "factor" double precision
)
;
CREATE TABLE tab7 (a int PRIMARY KEY, b int, c int, d int)
;
--
-- ARRAYS
--

CREATE TABLE arrtest (
	a 			int2[],
	b 			int4[][][],
	c 			name[],
	d			text[][],
	e 			float8[],
	f			char(5)[],
	g			varchar(5)[]
)
;
ALTER TABLE y1 ENABLE ROW LEVEL SECURITY
;
CREATE INDEX upserttest_key_idx ON upserttest((blurt_and_lock_4(key)))
;
CREATE FUNCTION lca(ltree,ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
--
-- Drop everything
--
drop table test2
;
ALTER TABLE gtestxx_4 INHERIT gtest1
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
CREATE TABLE "public"."plt2_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
ANALYZE public.timestamptz_tbl
;
ALTER EXTENSION pg_stat_statements DROP FUNCTION pg_stat_statements_reset()
;
CREATE EXTENSION test_custom_ext_paths_using_directory
;
CREATE TABLE fprt1_p2 (LIKE fprt1)
;
ALTER PUBLICATION tap_pub ADD TABLE tab_rep, tab_full, tab_full2, tab_mixed, tab_include, tab_nothing, tab_full_pk
;
DROP TRIGGER instead_of_delete_trig ON main_view
;
CREATE VIEW ruletest_v1 WITH (security_invoker=true) AS
    SELECT * FROM ruletest_t1
;
CREATE VIEW pg_stat_progress_vacuum AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        S.relid AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'scanning heap'
                      WHEN 2 THEN 'vacuuming indexes'
                      WHEN 3 THEN 'vacuuming heap'
                      WHEN 4 THEN 'cleaning up indexes'
                      WHEN 5 THEN 'truncating heap'
                      WHEN 6 THEN 'performing final cleanup'
                      END AS phase,
        S.param2 AS heap_blks_total, S.param3 AS heap_blks_scanned,
        S.param4 AS heap_blks_vacuumed, S.param5 AS index_vacuum_count,
        S.param6 AS max_dead_tuple_bytes, S.param7 AS dead_tuple_bytes,
        S.param8 AS num_dead_item_ids, S.param9 AS indexes_total,
        S.param10 AS indexes_processed
    FROM pg_stat_get_progress_info('VACUUM') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
create function polyf(anycompatiblerange) returns anycompatiblemultirange
as 'select multirange($1);' language sql
;
-- test that a default partition added as the first partition accepts any value
-- including null
create table list_parted (a int) partition by list (a)
;
CREATE TABLE ptif_test3 PARTITION OF ptif_test
  FOR VALUES FROM (200) TO (maxvalue) PARTITION BY list (b)
;
CREATE FUNCTION seg_size(seg)
RETURNS float4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE FUNCTION "public"."reraise_test"() RETURNS "void"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
   BEGIN
       RAISE syntax_error;
   EXCEPTION
       WHEN syntax_error THEN
           BEGIN
               raise notice 'exception % thrown in inner block, reraising', sqlerrm;
               RAISE;
           EXCEPTION
               WHEN OTHERS THEN
                   raise notice 'RIGHT - exception % caught in inner block', sqlerrm;
           END;
   END;
EXCEPTION
   WHEN OTHERS THEN
       raise notice 'WRONG - exception % caught in outer block', sqlerrm;
END;
$$
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_event_trigger
;
create rule rtest_t6_ins as on insert to rtest_t6
		where new.a > 25 do instead
	insert into rtest_t8 values (new.a, new.b)
;
CREATE SEQUENCE sequence_testx AS smallint MINVALUE -100000
;
--
-- Check constraints on system columns
--

CREATE TABLE SYS_COL_CHECK_TBL (city text, state text, is_capital bool,
                  altitude int,
                  CHECK (NOT (is_capital AND tableoid::regclass::text = 'sys_col_check_tbl')))
;
-- 6.26 DATA_TYPE_PRIVILEGES view appears later.


/*
 * 6.27
 * DIRECT_SUPERTABLES view
 */

-- feature not supported


/*
 * 6.28
 * DIRECT_SUPERTYPES view
 */

-- feature not supported


/*
 * 6.29
 * DOMAIN_CONSTRAINTS view
 */

CREATE VIEW domain_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(n.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(CASE WHEN condeferrable THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS is_deferrable,
           CAST(CASE WHEN condeferred THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS initially_deferred
    FROM pg_namespace rs, pg_namespace n, pg_constraint con, pg_type t
    WHERE rs.oid = con.connamespace
          AND n.oid = t.typnamespace
          AND t.oid = con.contypid
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
DROP TABLE counter
;
alter type insenum add value 'i25' before 'L2'
;
DO $$ BEGIN
PERFORM pg_terminate_backend(pid, 180000) FROM pg_stat_activity
	WHERE application_name = 'fdw_retry_check';
END $$
;
------------------------------------------------------------------------------------------
[DROP_TABLE] drop table notnull_tbl1
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_12.pg_temp_26722_2 (tid pg_catalog.tid)
;
----------
[CREATE_POLICY] CREATE TEXT SEARCH CONFIGURATION french
	(PARSER = default)
;
----------
[CREATE_PROG_OBJ] create or replace function trigger_notice() returns trigger as $$
  declare
    arg1 text = TG_ARGV[0];
    arg2 integer = TG_ARGV[1];
  begin
    raise notice 'trigger % on % % % for % args % %',
		TG_NAME, TG_TABLE_NAME, TG_WHEN, TG_OP, TG_LEVEL, arg1, arg2;
    return null;
  end;
  $$ language plpgsql
;
----------
[CREATE_TYPE] CREATE TYPE dump_test.int42
;
----------
[ALTER_TABLE] ALTER TABLE t2 ENABLE ROW LEVEL SECURITY
;
----------
[CREATE_TABLE] create table rule_and_refint_t2 (
	id2a integer,
	id2c integer,
	primary key (id2a, id2c)
)
;
----------
[ALTER_TYPE] ALTER TYPE test_type DROP ATTRIBUTE c
;
----------
[CREATE_TABLE] CREATE TABLE "regress_indexing"."pk51" (
    "a" integer CONSTRAINT "pk_a_not_null" NOT NULL
)
;
----------
[CREATE_TABLE] CREATE TABLE fk_partitioned_fk_4_2 (a int, b int, FOREIGN KEY (a, b) REFERENCES fk_notpartitioned_pk(a, b) ON UPDATE SET NULL)
;
----------
[CREATE_TABLE] create table descr_t1 ( id serial primary key , t text )
;
----------
[CREATE_TABLE] create table utr1 (a int check (a in (1)), q text, b text)
;
----------
[DROP_POLICY] DROP TEXT SEARCH PARSER test_tsparser_exists
;
----------
[CREATE_PROG_OBJ] create or replace function avg_transfn(state avg_state, n int) returns avg_state as
$$
declare new_state avg_state;
begin
	raise notice 'avg_transfn called with %', n;
	if state is null then
		if n is not null then
			new_state.total := n;
			new_state.count := 1;
			return new_state;
		end if;
		return null;
	elsif n is not null then
		state.total := state.total + n;
		state.count := state.count + 1;
		return state;
	end if;

	return null;
end
$$ language plpgsql
;
----------
[ADMIN_TABLE] VACUUM (FULL) pg_catalog.pg_rewrite
;
----------
[CREATE_TABLE] -- Can't modify inheritability of inherited constraints
create table inh_nn2 () inherits (inh_nn1)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR split_typeconv.= (
    FUNCTION = split_typeconv.mytext_eq_text,
    LEFTARG = split_typeconv.mytext,
    RIGHTARG = text
)
;
----------
[CREATE_TABLE] -- RETURNING in CTEs
CREATE TABLE sq_target_merge_log (tid integer NOT NULL, last_change text)
;
----------
[CREATE_TABLE] CREATE TABLE tbl1_part_p2 PARTITION OF tbl1_part FOR VALUES FROM (10) TO (20)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."rtest_unitfact" (
    "unit" character(4),
    "factor" double precision
)
;
----------
[CREATE_TABLE] CREATE TABLE tab7 (a int PRIMARY KEY, b int, c int, d int)
;
----------
[CREATE_TABLE] --
-- ARRAYS
--

CREATE TABLE arrtest (
	a 			int2[],
	b 			int4[][][],
	c 			name[],
	d			text[][],
	e 			float8[],
	f			char(5)[],
	g			varchar(5)[]
)
;
----------
[ALTER_TABLE] ALTER TABLE y1 ENABLE ROW LEVEL SECURITY
;
----------
[ADD_INDEX] CREATE INDEX upserttest_key_idx ON upserttest((blurt_and_lock_4(key)))
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION lca(ltree,ltree,ltree,ltree)
RETURNS ltree
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[DROP_TABLE] --
-- Drop everything
--
drop table test2
;
----------
[ALTER_TABLE] ALTER TABLE gtestxx_4 INHERIT gtest1
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_conversion
;
----------
[CREATE_TABLE] CREATE TABLE "public"."plt2_p1" (
    "a" integer,
    "b" integer,
    "c" "text"
)
;
----------
[ADMIN_TABLE] ANALYZE public.timestamptz_tbl
;
----------
[ALTER_LIBRARY] ALTER EXTENSION pg_stat_statements DROP FUNCTION pg_stat_statements_reset()
;
----------
[CREATE_LIBRARY] CREATE EXTENSION test_custom_ext_paths_using_directory
;
----------
[CREATE_TABLE] CREATE TABLE fprt1_p2 (LIKE fprt1)
;
----------
[ALTER_PUB_SUB] ALTER PUBLICATION tap_pub ADD TABLE tab_rep, tab_full, tab_full2, tab_mixed, tab_include, tab_nothing, tab_full_pk
;
----------
[DROP_TRIGGER] DROP TRIGGER instead_of_delete_trig ON main_view
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW ruletest_v1 WITH (security_invoker=true) AS
    SELECT * FROM ruletest_t1
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW pg_stat_progress_vacuum AS
    SELECT
        S.pid AS pid, S.datid AS datid, D.datname AS datname,
        S.relid AS relid,
        CASE S.param1 WHEN 0 THEN 'initializing'
                      WHEN 1 THEN 'scanning heap'
                      WHEN 2 THEN 'vacuuming indexes'
                      WHEN 3 THEN 'vacuuming heap'
                      WHEN 4 THEN 'cleaning up indexes'
                      WHEN 5 THEN 'truncating heap'
                      WHEN 6 THEN 'performing final cleanup'
                      END AS phase,
        S.param2 AS heap_blks_total, S.param3 AS heap_blks_scanned,
        S.param4 AS heap_blks_vacuumed, S.param5 AS index_vacuum_count,
        S.param6 AS max_dead_tuple_bytes, S.param7 AS dead_tuple_bytes,
        S.param8 AS num_dead_item_ids, S.param9 AS indexes_total,
        S.param10 AS indexes_processed
    FROM pg_stat_get_progress_info('VACUUM') AS S
        LEFT JOIN pg_database D ON S.datid = D.oid
;
----------
[CREATE_PROG_OBJ] create function polyf(anycompatiblerange) returns anycompatiblemultirange
as 'select multirange($1);' language sql
;
----------
[CREATE_TABLE] -- test that a default partition added as the first partition accepts any value
-- including null
create table list_parted (a int) partition by list (a)
;
----------
[CREATE_TABLE] CREATE TABLE ptif_test3 PARTITION OF ptif_test
  FOR VALUES FROM (200) TO (maxvalue) PARTITION BY list (b)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION seg_size(seg)
RETURNS float4
AS '$libdir/seg'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."reraise_test"() RETURNS "void"
    LANGUAGE "plpgsql"
    AS $$
BEGIN
   BEGIN
       RAISE syntax_error;
   EXCEPTION
       WHEN syntax_error THEN
           BEGIN
               raise notice 'exception % thrown in inner block, reraising', sqlerrm;
               RAISE;
           EXCEPTION
               WHEN OTHERS THEN
                   raise notice 'RIGHT - exception % caught in inner block', sqlerrm;
           END;
   END;
EXCEPTION
   WHEN OTHERS THEN
       raise notice 'WRONG - exception % caught in outer block', sqlerrm;
END;
$$
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_event_trigger
;
----------
[CREATE_POLICY] create rule rtest_t6_ins as on insert to rtest_t6
		where new.a > 25 do instead
	insert into rtest_t8 values (new.a, new.b)
;
----------
[CREATE_SEQUENCE] CREATE SEQUENCE sequence_testx AS smallint MINVALUE -100000
;
----------
[CREATE_TABLE] --
-- Check constraints on system columns
--

CREATE TABLE SYS_COL_CHECK_TBL (city text, state text, is_capital bool,
                  altitude int,
                  CHECK (NOT (is_capital AND tableoid::regclass::text = 'sys_col_check_tbl')))
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- 6.26 DATA_TYPE_PRIVILEGES view appears later.


/*
 * 6.27
 * DIRECT_SUPERTABLES view
 */

-- feature not supported


/*
 * 6.28
 * DIRECT_SUPERTYPES view
 */

-- feature not supported


/*
 * 6.29
 * DOMAIN_CONSTRAINTS view
 */

CREATE VIEW domain_constraints AS
    SELECT CAST(current_database() AS sql_identifier) AS constraint_catalog,
           CAST(rs.nspname AS sql_identifier) AS constraint_schema,
           CAST(con.conname AS sql_identifier) AS constraint_name,
           CAST(current_database() AS sql_identifier) AS domain_catalog,
           CAST(n.nspname AS sql_identifier) AS domain_schema,
           CAST(t.typname AS sql_identifier) AS domain_name,
           CAST(CASE WHEN condeferrable THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS is_deferrable,
           CAST(CASE WHEN condeferred THEN 'YES' ELSE 'NO' END
             AS yes_or_no) AS initially_deferred
    FROM pg_namespace rs, pg_namespace n, pg_constraint con, pg_type t
    WHERE rs.oid = con.connamespace
          AND n.oid = t.typnamespace
          AND t.oid = con.contypid
          AND (pg_has_role(t.typowner, 'USAGE')
               OR has_type_privilege(t.oid, 'USAGE'))
;
----------
[DROP_TABLE] DROP TABLE counter
;
----------
[ALTER_TYPE] alter type insenum add value 'i25' before 'L2'
;
----------
[BLOCK] DO $$ BEGIN
PERFORM pg_terminate_backend(pid, 180000) FROM pg_stat_activity
	WHERE application_name = 'fdw_retry_check';
END $$
;
