EXECUTE dumpFunc('34790')
;
ALTER USER split_user_probe RENAME TO split_user_renamed
;
EXECUTE dumpFunc('28036')
;
/* contrib/pg_freespacemap/pg_freespacemap--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


GRANT EXECUTE ON FUNCTION  pg_freespace(regclass, bigint) TO pg_stat_scan_tables
;
-- ok
GRANT SELECT ON atestv4 TO regress_priv_user2
;
EXECUTE dumpTableAttach('39411')
;
LOCK TABLE pg_catalog.pg_proc IN ACCESS SHARE MODE
;
-- Temporary schema
SET search_path = 'pg_temp'
;
PREPARE test AS SELECT 1 AS first, 2 AS second
;
COPY public.cmpart2 (f1) TO stdout
;
------------------------------------------------------------------------------------------
[UNSAFE] EXECUTE dumpFunc('34790')
;
----------
[RENAME_USER] ALTER USER split_user_probe RENAME TO split_user_renamed
;
----------
[UNSAFE] EXECUTE dumpFunc('28036')
;
----------
[GRANT] /* contrib/pg_freespacemap/pg_freespacemap--1.1--1.2.sql */

-- complain if script is sourced in psql, rather than via ALTER EXTENSION


GRANT EXECUTE ON FUNCTION  pg_freespace(regclass, bigint) TO pg_stat_scan_tables
;
----------
[GRANT] -- ok
GRANT SELECT ON atestv4 TO regress_priv_user2
;
----------
[UNSAFE] EXECUTE dumpTableAttach('39411')
;
----------
[SESSION_LOCK] LOCK TABLE pg_catalog.pg_proc IN ACCESS SHARE MODE
;
----------
[SESSION_SETTING_WRITE] -- Temporary schema
SET search_path = 'pg_temp'
;
----------
[UNSAFE] PREPARE test AS SELECT 1 AS first, 2 AS second
;
----------
[DATA_EXPORT] COPY public.cmpart2 (f1) TO stdout
;
