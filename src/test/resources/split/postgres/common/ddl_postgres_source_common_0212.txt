ANALYZE public.prt1_m_p2
;
-- cannot use pseudotypes
CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (((a, b)))
;
--
-- bt_page_stats()
--
DROP FUNCTION bt_page_stats(text, int4)
;
create function unnest(anyarray) returns setof anyelement
language sql strict immutable as $$
select $1[i] from generate_series(array_lower($1,1), array_upper($1,1)) as i
$$
;
create table rewriteme (id serial primary key, foo float, bar timestamptz)
;
CREATE FUNCTION _ltree_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
CREATE INDEX ON s1.t1(id)
;
CREATE TABLE "public"."pagg_tab_m_p1" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
alter table simple set (parallel_workers = 2)
;
CREATE TABLE gtest28 OF gtest_type (f1 WITH OPTIONS GENERATED ALWAYS AS (f2 *2) STORED)
;
alter extension postgres_fdw drop operator public.<^(int, int)
;
ALTER INDEX "fkpart5"."pk_pkey" ATTACH PARTITION "fkpart5"."pk3_pkey"
;
CREATE TABLE check_ign_err (n int, m int[], k int)
;
CREATE FUNCTION akeys(hstore)
RETURNS text[]
AS '$libdir/hstore','hstore_akeys'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TEMP TABLE pg_temp_11.pg_temp_26345_2 (tid pg_catalog.tid)
;
CALL p1(10, v_Text := v_Text)
;
DROP CONVERSION IF EXISTS no_such_schema.foo
;
CREATE TABLE s1.t1(a int)
;
create table child1 partition of parent for values in (1)
;
-- test GiST index
create table test_multirange_gist(mr int4multirange)
;
SECURITY LABEL ON TABLE t2 IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
ANALYZE pg_catalog.pg_auth_members
;
VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
create table idxpart3 (b int not null, a int not null)
;
CREATE INDEX "iprt1_e_p3_ab2" ON "public"."prt1_e_p3" USING "btree" (((("a" + "b") / 2)))
;
/*
 * SQL_LANGUAGES table
 * apparently removed in SQL:2008
 */

CREATE TABLE sql_languages (
    sql_language_source         character_data,
    sql_language_year           character_data,
    sql_language_conformance    character_data,
    sql_language_integrity      character_data,
    sql_language_implementation character_data,
    sql_language_binding_style  character_data,
    sql_language_programming_language character_data
)
;
CREATE PUBLICATION pub FOR ALL TABLES
;
ALTER TABLE "generated_stored_tests"."gtest20b"
    ADD CONSTRAINT "chk" CHECK (("b" < 50)) NOT VALID
;
DROP TABLE testns.acltest1
;
CREATE TABLE trigger_test (
        i int,
        v varchar
)
;
-- fail, can't infer type:
create function f1(x anycompatible) returns anycompatiblerange as $$
begin
  return array[x + 1, x + 2];
end$$ language plpgsql
;
ALTER VIEW my_property_secure SET (security_barrier=false)
;
ALTER TYPE split_domain_type_next.address_type ALTER ATTRIBUTE zip SET DATA TYPE bigint RESTRICT
;
ANALYZE onek
;
CREATE TEMP TABLE pg_temp_13.pg_temp_26923_2 (tid pg_catalog.tid)
;
ALTER TABLE part ADD PRIMARY KEY (a)
;
CREATE OPERATOR %>> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_commutator_op,
        COMMUTATOR = '<<%',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
/*
 * 6.6
 * SQL_IDENTIFIER domain
 */

CREATE DOMAIN sql_identifier AS name
;
CREATE TABLE tt27(c int)
;
--
-- Test ADD CONSTRAINT USING INDEX
--

CREATE TABLE cwi_test( a int , b varchar(10), c char)
;
CREATE FUNCTION strict_word_similarity_dist_commutator_op(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
ALTER TABLE pg_conversion ADD PRIMARY KEY USING INDEX pg_conversion_oid_index
;
-- test brin cost estimates behave sanely based on correlation of values
CREATE TABLE brin_test_multi (a INT, b INT)
;
DROP FUNCTION getrngfunc9(int)
;
-- dropping the NO INHERIT constraint allows this to work
ALTER TABLE ATACC2 DROP CONSTRAINT a_is_not_null
;
ALTER TABLE tab_replidentity_index REPLICA IDENTITY USING INDEX idx_replidentity_index_a
;
-- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', quote '-', null '=-=')
;
CREATE DATABASE "postgres" WITH TEMPLATE = template0 OID = 5 ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
CREATE TABLE "fkpart5"."pk11" (
    "a" integer NOT NULL
)
;
-- 95

-- Both parent and child tables in the foreign server are truncated
-- even though ONLY is specified because ONLY has no effect
-- when truncating a foreign table.
TRUNCATE ONLY tru_ftable
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] ANALYZE public.prt1_m_p2
;
----------
[CREATE_TABLE] -- cannot use pseudotypes
CREATE TABLE partitioned (
	a int,
	b int
) PARTITION BY RANGE (((a, b)))
;
----------
[DROP_PROG_OBJ] --
-- bt_page_stats()
--
DROP FUNCTION bt_page_stats(text, int4)
;
----------
[CREATE_PROG_OBJ] create function unnest(anyarray) returns setof anyelement
language sql strict immutable as $$
select $1[i] from generate_series(array_lower($1,1), array_upper($1,1)) as i
$$
;
----------
[CREATE_TABLE] create table rewriteme (id serial primary key, foo float, bar timestamptz)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION _ltree_penalty(internal,internal,internal)
RETURNS internal
AS '$libdir/ltree'
LANGUAGE C IMMUTABLE STRICT PARALLEL SAFE
;
----------
[ADD_INDEX] CREATE INDEX ON s1.t1(id)
;
----------
[CREATE_TABLE] CREATE TABLE "public"."pagg_tab_m_p1" (
    "a" integer,
    "b" integer,
    "c" integer
)
;
----------
[ALTER_TABLE] alter table simple set (parallel_workers = 2)
;
----------
[CREATE_TABLE] CREATE TABLE gtest28 OF gtest_type (f1 WITH OPTIONS GENERATED ALWAYS AS (f2 *2) STORED)
;
----------
[ALTER_LIBRARY] alter extension postgres_fdw drop operator public.<^(int, int)
;
----------
[ALTER_INDEX] ALTER INDEX "fkpart5"."pk_pkey" ATTACH PARTITION "fkpart5"."pk3_pkey"
;
----------
[CREATE_TABLE] CREATE TABLE check_ign_err (n int, m int[], k int)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION akeys(hstore)
RETURNS text[]
AS '$libdir/hstore','hstore_akeys'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_11.pg_temp_26345_2 (tid pg_catalog.tid)
;
----------
[CALL_PROG_OBJ] CALL p1(10, v_Text := v_Text)
;
----------
[DROP_POLICY] DROP CONVERSION IF EXISTS no_such_schema.foo
;
----------
[CREATE_TABLE] CREATE TABLE s1.t1(a int)
;
----------
[CREATE_TABLE] create table child1 partition of parent for values in (1)
;
----------
[CREATE_TABLE] -- test GiST index
create table test_multirange_gist(mr int4multirange)
;
----------
[UNKNOWN] SECURITY LABEL ON TABLE t2 IS 'system_u:object_r:sepgsql_ro_table_t:s0'
;
----------
[ADMIN_TABLE] ANALYZE pg_catalog.pg_auth_members
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_amop
;
----------
[CREATE_TABLE] create table idxpart3 (b int not null, a int not null)
;
----------
[ADD_INDEX] CREATE INDEX "iprt1_e_p3_ab2" ON "public"."prt1_e_p3" USING "btree" (((("a" + "b") / 2)))
;
----------
[CREATE_TABLE] /*
 * SQL_LANGUAGES table
 * apparently removed in SQL:2008
 */

CREATE TABLE sql_languages (
    sql_language_source         character_data,
    sql_language_year           character_data,
    sql_language_conformance    character_data,
    sql_language_integrity      character_data,
    sql_language_implementation character_data,
    sql_language_binding_style  character_data,
    sql_language_programming_language character_data
)
;
----------
[CREATE_PUB_SUB] CREATE PUBLICATION pub FOR ALL TABLES
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE "generated_stored_tests"."gtest20b"
    ADD CONSTRAINT "chk" CHECK (("b" < 50)) NOT VALID
;
----------
[DROP_TABLE] DROP TABLE testns.acltest1
;
----------
[CREATE_TABLE] CREATE TABLE trigger_test (
        i int,
        v varchar
)
;
----------
[CREATE_PROG_OBJ] -- fail, can't infer type:
create function f1(x anycompatible) returns anycompatiblerange as $$
begin
  return array[x + 1, x + 2];
end$$ language plpgsql
;
----------
[ALTER_VIEW] ALTER VIEW my_property_secure SET (security_barrier=false)
;
----------
[ALTER_TYPE] ALTER TYPE split_domain_type_next.address_type ALTER ATTRIBUTE zip SET DATA TYPE bigint RESTRICT
;
----------
[ADMIN_TABLE] ANALYZE onek
;
----------
[CREATE_TABLE] CREATE TEMP TABLE pg_temp_13.pg_temp_26923_2 (tid pg_catalog.tid)
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE part ADD PRIMARY KEY (a)
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR %>> (
        LEFTARG = text,
        RIGHTARG = text,
        PROCEDURE = strict_word_similarity_commutator_op,
        COMMUTATOR = '<<%',
        RESTRICT = contsel,
        JOIN = contjoinsel
)
;
----------
[CREATE_TYPE] /*
 * 6.6
 * SQL_IDENTIFIER domain
 */

CREATE DOMAIN sql_identifier AS name
;
----------
[CREATE_TABLE] CREATE TABLE tt27(c int)
;
----------
[CREATE_TABLE] --
-- Test ADD CONSTRAINT USING INDEX
--

CREATE TABLE cwi_test( a int , b varchar(10), c char)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION strict_word_similarity_dist_commutator_op(text,text)
RETURNS float4
AS '$libdir/pg_trgm'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE pg_conversion ADD PRIMARY KEY USING INDEX pg_conversion_oid_index
;
----------
[CREATE_TABLE] -- test brin cost estimates behave sanely based on correlation of values
CREATE TABLE brin_test_multi (a INT, b INT)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION getrngfunc9(int)
;
----------
[ALTER_TABLE|DROP_CONSTRAINT] -- dropping the NO INHERIT constraint allows this to work
ALTER TABLE ATACC2 DROP CONSTRAINT a_is_not_null
;
----------
[ALTER_TABLE] ALTER TABLE tab_replidentity_index REPLICA IDENTITY USING INDEX idx_replidentity_index_a
;
----------
[CREATE_TABLE] -- ERROR
CREATE FOREIGN TABLE tbl () SERVER file_server OPTIONS (format 'csv', quote '-', null '=-=')
;
----------
[CREATE_CATALOG] CREATE DATABASE "postgres" WITH TEMPLATE = template0 OID = 5 ENCODING = 'SQL_ASCII' LOCALE_PROVIDER = libc LOCALE = 'C'
;
----------
[CREATE_TABLE] CREATE TABLE "fkpart5"."pk11" (
    "a" integer NOT NULL
)
;
----------
[TRUNCATE_TABLE] -- 95

-- Both parent and child tables in the foreign server are truncated
-- even though ONLY is specified because ONLY has no effect
-- when truncating a foreign table.
TRUNCATE ONLY tru_ftable
;
