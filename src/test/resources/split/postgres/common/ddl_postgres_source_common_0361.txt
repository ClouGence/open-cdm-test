CREATE UNIQUE INDEX ON mvtest_mv_foo (i)
;
CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=23306 host=/tmp/oiVJjI6UhV dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
do $$ declare a int[];
begin a := array[1,2]; a[2] := a[2] + 1; raise notice 'a = %', a; end$$
;
CREATE FUNCTION "public"."plusone"("a" integer) RETURNS integer
    LANGUAGE "sql"
    AS $$ SELECT a+1; $$
;
ANALYZE fkpart4.droppk_d
;
do $$
declare x constant int;
begin
  x := 42;  -- fail
end$$
;
ALTER TABLE ONLY "public"."fk_partitioned_fk_4_2"
    ADD CONSTRAINT "fk_partitioned_fk_4_2_a_b_fkey" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b") ON UPDATE SET NULL
;
-- 5.8 ADMINISTRABLE_ROLE_AUTHORIZATIONS view appears later.


/*
 * 5.9
 * APPLICABLE_ROLES view
 */

CREATE VIEW applicable_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS grantee,
           CAST(b.rolname AS sql_identifier) AS role_name,
           CAST(CASE WHEN m.admin_option THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable
    FROM pg_auth_members m
         JOIN pg_authid a ON (m.member = a.oid)
         JOIN pg_authid b ON (m.roleid = b.oid)
    WHERE pg_has_role(a.oid, 'USAGE')
;
-- Clean up.
DROP FOREIGN TABLE remote_application_name
;
-- view on top of view with rules

CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified')
;
--
-- DETACH PARTITION
--

-- check that the table is partitioned at all
CREATE TABLE regular_table (a int)
;
-- ok, skipped
CREATE COLLATION IF NOT EXISTS test0 (locale = 'foo')
;
CREATE VIEW my_credit_card_usage_secure WITH (security_barrier) AS
       SELECT * FROM my_credit_card_secure l NATURAL JOIN credit_usage r
;
-- fails

alter domain connotnull drop constraint constr1
;
-- renaming to table's own array type's name is an interesting corner case
CREATE TABLE attmp_array (id int)
;
ANALYZE public.func_index_heap
;
-- triggers
create function qqq_trig() returns trigger as $$
begin
if tg_op in ('INSERT', 'UPDATE') then
    raise notice '% % %', tg_when, tg_op, new.id;
    return new;
else
    raise notice '% % %', tg_when, tg_op, old.id;
    return old;
end if;
end
$$ language plpgsql
;
create function explain_query_json(query_sql text)
returns table (explain_line json)
language plpgsql as
$$
begin
  set enable_seqscan = off;
  set enable_bitmapscan = on;
  return query execute 'EXPLAIN (ANALYZE, FORMAT json) ' || query_sql;
end;
$$
;
alter table mlparted5 attach partition mlparted5a for values in ('a')
;
-- PRIMARY KEY constraint cannot be backed by a NULLS NOT DISTINCT index
CREATE TABLE cwi_test(a int, b int)
;
--
-- Check that constraint exclusion works correctly with partitions using
-- implicit constraints generated from the partition bound information.
--
create table list_parted (
	a	varchar
) partition by list (a)
;
ALTER TEXT SEARCH TEMPLATE alt_ts_temp1 RENAME TO alt_ts_temp3
;
CREATE FUNCTION int4_dist(int4, int4)
RETURNS int4
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_language
;
alter table def_test alter column c2 set default 'new_default'
;
CREATE TABLE brin_idx_tbl (col1 int, col2 text, col3 text )
;
ALTER TABLE ONLY public.tht_p3
    ADD CONSTRAINT tht_p3_en_data_key UNIQUE (en, data)
;
CREATE EXTENSION pageinspect
;
-- Test backwards parsing

CREATE VIEW xmlview1 AS SELECT xmlcomment('test')
;
ALTER POLICY r1 ON rec1 USING (x = (SELECT a FROM rec2v WHERE b = y))
;
CREATE INDEX brin_summarize_idx ON brin_summarize USING brin (value) WITH (pages_per_range=2)
;
ANALYZE pg_temp_32.pg_temp_28153
;
-- error
ALTER INDEX dummy_test_idx SET (option_enum = true)
;
create table mcrparted2 partition of mcrparted for values from (10, 6, minvalue) to (10, maxvalue, minvalue)
;
CREATE TABLE tab2_1 (c text DEFAULT 'sub1_tab2', b text, a int NOT NULL)
;
CREATE TEMP TABLE temp_pg_buffercache()
;
-- n-distinct tests
CREATE TABLE ndistinct (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b INT,
    filler3 DATE,
    c INT,
    d INT
)
WITH (autovacuum_enabled = off)
;
-- invalid
ALTER VIEW rw_view1 SET (check_option=local)
;
DROP TABLE IF EXISTS audit_tbls.schema_two_table_three
;
-- clean up
drop table trans_tab_parent, trans_tab_child1, trans_tab_child2
;
create table ppk (a int primary key) partition by list (a)
;
VACUUM (TRUNCATE FALSE) pg_catalog.pg_database
;
alter table recur1 alter column f2 type recur2
;
-- Triggers on leaf partition part_d_15_20
CREATE TRIGGER d15_delete_trig
  AFTER DELETE ON part_d_15_20 for each statement execute procedure trigfunc()
;
ALTER TABLE testpub_tbl8_1 ADD PRIMARY KEY (c)
;
create table join_ut1 (a int, b int, c varchar)
;
DROP VIEW rls_tbl_view
;
alter table mlparted11 add a int
;
CREATE TABLE will_get_dropped (a int, b text)
;
ANALYZE public.gtest23p
;
------------------------------------------------------------------------------------------
[ADD_INDEX] CREATE UNIQUE INDEX ON mvtest_mv_foo (i)
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION tap_sub_temp1 CONNECTION 'port=23306 host=/tmp/oiVJjI6UhV dbname=postgres' PUBLICATION tap_pub_temp1, tap_pub_temp2
;
----------
[BLOCK] do $$ declare a int[];
begin a := array[1,2]; a[2] := a[2] + 1; raise notice 'a = %', a; end$$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."plusone"("a" integer) RETURNS integer
    LANGUAGE "sql"
    AS $$ SELECT a+1; $$
;
----------
[ADMIN_TABLE] ANALYZE fkpart4.droppk_d
;
----------
[BLOCK] do $$
declare x constant int;
begin
  x := 42;  -- fail
end$$
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY "public"."fk_partitioned_fk_4_2"
    ADD CONSTRAINT "fk_partitioned_fk_4_2_a_b_fkey" FOREIGN KEY ("a", "b") REFERENCES "public"."fk_notpartitioned_pk"("a", "b") ON UPDATE SET NULL
;
----------
[CREATE_VIEW(SELECT,METADATA)] -- 5.8 ADMINISTRABLE_ROLE_AUTHORIZATIONS view appears later.


/*
 * 5.9
 * APPLICABLE_ROLES view
 */

CREATE VIEW applicable_roles AS
    SELECT CAST(a.rolname AS sql_identifier) AS grantee,
           CAST(b.rolname AS sql_identifier) AS role_name,
           CAST(CASE WHEN m.admin_option THEN 'YES' ELSE 'NO' END AS yes_or_no) AS is_grantable
    FROM pg_auth_members m
         JOIN pg_authid a ON (m.member = a.oid)
         JOIN pg_authid b ON (m.roleid = b.oid)
    WHERE pg_has_role(a.oid, 'USAGE')
;
----------
[DROP_TABLE] -- Clean up.
DROP FOREIGN TABLE remote_application_name
;
----------
[CREATE_TABLE] -- view on top of view with rules

CREATE TABLE base_tbl (a int PRIMARY KEY, b text DEFAULT 'Unspecified')
;
----------
[CREATE_TABLE] --
-- DETACH PARTITION
--

-- check that the table is partitioned at all
CREATE TABLE regular_table (a int)
;
----------
[CREATE_POLICY] -- ok, skipped
CREATE COLLATION IF NOT EXISTS test0 (locale = 'foo')
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW my_credit_card_usage_secure WITH (security_barrier) AS
       SELECT * FROM my_credit_card_secure l NATURAL JOIN credit_usage r
;
----------
[ALTER_TYPE] -- fails

alter domain connotnull drop constraint constr1
;
----------
[CREATE_TABLE] -- renaming to table's own array type's name is an interesting corner case
CREATE TABLE attmp_array (id int)
;
----------
[ADMIN_TABLE] ANALYZE public.func_index_heap
;
----------
[CREATE_PROG_OBJ] -- triggers
create function qqq_trig() returns trigger as $$
begin
if tg_op in ('INSERT', 'UPDATE') then
    raise notice '% % %', tg_when, tg_op, new.id;
    return new;
else
    raise notice '% % %', tg_when, tg_op, old.id;
    return old;
end if;
end
$$ language plpgsql
;
----------
[CREATE_PROG_OBJ] create function explain_query_json(query_sql text)
returns table (explain_line json)
language plpgsql as
$$
begin
  set enable_seqscan = off;
  set enable_bitmapscan = on;
  return query execute 'EXPLAIN (ANALYZE, FORMAT json) ' || query_sql;
end;
$$
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table mlparted5 attach partition mlparted5a for values in ('a')
;
----------
[CREATE_TABLE] -- PRIMARY KEY constraint cannot be backed by a NULLS NOT DISTINCT index
CREATE TABLE cwi_test(a int, b int)
;
----------
[CREATE_TABLE] --
-- Check that constraint exclusion works correctly with partitions using
-- implicit constraints generated from the partition bound information.
--
create table list_parted (
	a	varchar
) partition by list (a)
;
----------
[ALTER_POLICY] ALTER TEXT SEARCH TEMPLATE alt_ts_temp1 RENAME TO alt_ts_temp3
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION int4_dist(int4, int4)
RETURNS int4
AS '$libdir/btree_gist'
LANGUAGE C IMMUTABLE STRICT
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS, ANALYZE) pg_catalog.pg_language
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table def_test alter column c2 set default 'new_default'
;
----------
[CREATE_TABLE] CREATE TABLE brin_idx_tbl (col1 int, col2 text, col3 text )
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE ONLY public.tht_p3
    ADD CONSTRAINT tht_p3_en_data_key UNIQUE (en, data)
;
----------
[CREATE_LIBRARY] CREATE EXTENSION pageinspect
;
----------
[CREATE_VIEW(SELECT)] -- Test backwards parsing

CREATE VIEW xmlview1 AS SELECT xmlcomment('test')
;
----------
[ALTER_POLICY] ALTER POLICY r1 ON rec1 USING (x = (SELECT a FROM rec2v WHERE b = y))
;
----------
[ADD_INDEX] CREATE INDEX brin_summarize_idx ON brin_summarize USING brin (value) WITH (pages_per_range=2)
;
----------
[ADMIN_TABLE] ANALYZE pg_temp_32.pg_temp_28153
;
----------
[ALTER_INDEX] -- error
ALTER INDEX dummy_test_idx SET (option_enum = true)
;
----------
[CREATE_TABLE] create table mcrparted2 partition of mcrparted for values from (10, 6, minvalue) to (10, maxvalue, minvalue)
;
----------
[CREATE_TABLE] CREATE TABLE tab2_1 (c text DEFAULT 'sub1_tab2', b text, a int NOT NULL)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE temp_pg_buffercache()
;
----------
[CREATE_TABLE] -- n-distinct tests
CREATE TABLE ndistinct (
    filler1 TEXT,
    filler2 NUMERIC,
    a INT,
    b INT,
    filler3 DATE,
    c INT,
    d INT
)
WITH (autovacuum_enabled = off)
;
----------
[ALTER_VIEW] -- invalid
ALTER VIEW rw_view1 SET (check_option=local)
;
----------
[DROP_TABLE] DROP TABLE IF EXISTS audit_tbls.schema_two_table_three
;
----------
[DROP_TABLE] -- clean up
drop table trans_tab_parent, trans_tab_child1, trans_tab_child2
;
----------
[CREATE_TABLE] create table ppk (a int primary key) partition by list (a)
;
----------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE) pg_catalog.pg_database
;
----------
[ALTER_TABLE|ALTER_COLUMN] alter table recur1 alter column f2 type recur2
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] -- Triggers on leaf partition part_d_15_20
CREATE TRIGGER d15_delete_trig
  AFTER DELETE ON part_d_15_20 for each statement execute procedure trigfunc()
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE testpub_tbl8_1 ADD PRIMARY KEY (c)
;
----------
[CREATE_TABLE] create table join_ut1 (a int, b int, c varchar)
;
----------
[DROP_VIEW] DROP VIEW rls_tbl_view
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table mlparted11 add a int
;
----------
[CREATE_TABLE] CREATE TABLE will_get_dropped (a int, b text)
;
----------
[ADMIN_TABLE] ANALYZE public.gtest23p
;
