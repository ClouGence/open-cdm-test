VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
create or replace function unreserved_test() returns int as $$
declare
  return int := 42;
begin
  return := return + 1;
  return return;
end
$$ language plpgsql
;
-- dropping them is ok though
ALTER TABLE list_parted2 ALTER b SET NOT NULL
;
CREATE TABLE persons3 OF person_type (
    PRIMARY KEY (id),
    name WITH OPTIONS DEFAULT ''
)
;
-- check that constraint exclusion is able to cope with the partition
-- constraint emitted for multi-column range partitioned tables
create table mcrparted (a int, b int, c int) partition by range (a, abs(b), c)
;
alter table itrtest attach partition remp1 for values in (1)
;
create database "abc" with owner = zyc template zyc
;
CREATE INDEX bloomidxu ON tstu USING bloom (i, t) WITH (col2 = 4)
;
DROP FUNCTION bt_page_items(text, int4)
;
CREATE TABLE FKTABLE ( ftest1 int REFERENCES PKTABLE MATCH FULL, ftest2 int )
;
ANALYZE public.defaultexpr_tbl
;
CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, password_required = false)
;
ANALYZE (SKIP_LOCKED) pg_catalog.pg_auth_members
;
CREATE TYPE type_op6 AS ENUM ('new', 'open', 'closed')
;
DROP FOREIGN TABLE remote_application_name
;
-- ctid attribute with incorrect data type
CREATE VIEW tid_view_fake_ctid AS SELECT 1 AS ctid, 2 AS a
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_user_mapping
;
ALTER INDEX pg_toast.pg_toast_30395_index RENAME TO reind_con_toast_idx
;
DROP TABLE ctl_stats4_parent
;
ALTER TYPE "public"."insenum" ADD VALUE 'i24'
;
CREATE VIEW temp_view_test.v2 AS SELECT * FROM base_table
;
CREATE SCHEMA "CURRENT_SCHEMA"
;
-- Verify that we cope with infinite recursion in view definitions.
CREATE OR REPLACE VIEW lock_view2 AS SELECT * from lock_view3
;
-- sql, proretset = t, prorettype = record
CREATE FUNCTION getrngfunc7(int) RETURNS setof record AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
CREATE FUNCTION "public"."report_guc"("text") RETURNS "text"
    LANGUAGE "sql"
    AS $_$ select current_setting($1) $_$
;
CREATE FUNCTION getrngfunc4(int) RETURNS rngfunc AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
-- OK
alter index idxpart_a_b_idx attach partition idxpart1_a_b_idx
;
VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
alter conversion alter1.latin1_to_utf8 set schema alter2
;
/*
 * 24.7
 * FOREIGN_SERVERS view
 */
CREATE VIEW foreign_servers AS
    SELECT foreign_server_catalog,
           foreign_server_name,
           foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           foreign_server_type,
           foreign_server_version,
           authorization_identifier
    FROM _pg_foreign_servers
;
--
-- combined queries
--
CREATE FUNCTION warn(msg TEXT) RETURNS BOOLEAN LANGUAGE plpgsql
AS $$
  BEGIN RAISE NOTICE 'warn %', msg ; RETURN TRUE ; END
$$
;
CREATE VIEW temporal1 AS SELECT * FROM t1 CROSS JOIN tt
;
ALTER TABLE pg_toast.pg_toast_28331 RENAME TO reind_con_toast
;
CREATE OPERATOR @+@ ( leftarg = int4, rightarg = int4, procedure = int4pl )
;
/*
 * 5.34
 * KEY_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 5.35
 * METHOD_SPECIFICATION_PARAMETERS view
 */

-- feature not supported


/*
 * 5.36
 * METHOD_SPECIFICATIONS view
 */

-- feature not supported


/*
 * 5.37
 * PARAMETERS view
 */

CREATE VIEW parameters AS
    SELECT CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n_nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(proname, p_oid) AS sql_identifier) AS specific_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(
             CASE WHEN proargmodes IS NULL THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 'i' THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 'o' THEN 'OUT'
                WHEN proargmodes[(ss.x).n] = 'b' THEN 'INOUT'
                WHEN proargmodes[(ss.x).n] = 'v' THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 't' THEN 'OUT'
             END AS character_data) AS parameter_mode,
           CAST('NO' AS yes_or_no) AS is_result,
           CAST('NO' AS yes_or_no) AS as_locator,
           CAST(NULLIF(proargnames[(ss.x).n], '') AS sql_identifier) AS parameter_name,
           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nt.nspname = 'pg_catalog' THEN format_type(t.oid, null)
                  ELSE 'USER-DEFINED' END AS character_data)
             AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,
           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST((ss.x).n AS sql_identifier) AS dtd_identifier,
           CAST(
             CASE WHEN pg_has_role(proowner, 'USAGE')
                  THEN pg_get_function_arg_default(p_oid, (ss.x).n)
                  ELSE NULL END
             AS character_data) AS parameter_default

    FROM pg_type t, pg_namespace nt,
         (SELECT n.nspname AS n_nspname, p.proname, p.oid AS p_oid, p.proowner,
                 p.proargnames, p.proargmodes,
                 _pg_expandarray(coalesce(p.proallargtypes, p.proargtypes::oid[])) AS x
          FROM pg_namespace n, pg_proc p
          WHERE n.oid = p.pronamespace
                AND (pg_has_role(p.proowner, 'USAGE') OR
                     has_function_privilege(p.oid, 'EXECUTE'))) AS ss
    WHERE t.oid = (ss.x).x AND t.typnamespace = nt.oid
;
DROP TABLE temporal_fk2_mltrng2mltrng
;
create table idxpart (a int4range, exclude USING GIST (a with -|- )) partition by range (a)
;
CREATE VIEW "public"."mvtest_tvvmv" AS
 SELECT "mvtest_tvvm"."grandtot"
   FROM "public"."mvtest_tvvm"
;
CREATE TABLE part_rpd PARTITION OF range_parted2 DEFAULT
;
ALTER INDEX pg_toast.pg_toast_30465_index RENAME TO reind_con_toast_idx
;
/*
 * 6.57
 * SEQUENCES view
 */

CREATE VIEW sequences AS
    SELECT CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(nc.nspname AS sql_identifier) AS sequence_schema,
           CAST(c.relname AS sql_identifier) AS sequence_name,
           CAST(format_type(s.seqtypid, null) AS character_data) AS data_type,
           CAST(_pg_numeric_precision(s.seqtypid, -1) AS cardinal_number) AS numeric_precision,
           CAST(2 AS cardinal_number) AS numeric_precision_radix,
           CAST(0 AS cardinal_number) AS numeric_scale,
           CAST(s.seqstart AS character_data) AS start_value,
           CAST(s.seqmin AS character_data) AS minimum_value,
           CAST(s.seqmax AS character_data) AS maximum_value,
           CAST(s.seqincrement AS character_data) AS increment,
           CAST(CASE WHEN s.seqcycle THEN 'YES' ELSE 'NO' END AS yes_or_no) AS cycle_option
    FROM pg_namespace nc, pg_class c, pg_sequence s
    WHERE c.relnamespace = nc.oid
          AND c.relkind = 'S'
          AND NOT EXISTS (SELECT 1 FROM pg_depend WHERE classid = 'pg_class'::regclass AND objid = c.oid AND deptype = 'i')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND c.oid = s.seqrelid
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_sequence_privilege(c.oid, 'SELECT, UPDATE, USAGE') )
;
--
-- OPERATORS
--

CREATE OPERATOR && (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_overlap,
	COMMUTATOR = '&&',
	RESTRICT = _int_overlap_sel,
	JOIN = _int_overlap_joinsel
)
;
CREATE FUNCTION "public"."equipment_named_ambiguous_2b"("hobby" "text") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where equipment_r.hobby = hobby$$
;
CREATE FOREIGN TABLE fc (i int) SERVER loopback OPTIONS (table_name 'c')
;
create type xfloat8 (input = xfloat8in, output = xfloat8out, like = float8)
;
-- same for cc2
alter table cc2 alter column f1 drop not null
;
ALTER INDEX pg_toast.pg_toast_33655_index RENAME TO reind_con_toast_idx
;
CREATE TABLE gtestxx_2 (a int NOT NULL, b int GENERATED ALWAYS AS (a * 22) STORED)
;
VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_foreign_table
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (PROCESS_MAIN FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
----------
[CREATE_PROG_OBJ] create or replace function unreserved_test() returns int as $$
declare
  return int := 42;
begin
  return := return + 1;
  return return;
end
$$ language plpgsql
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- dropping them is ok though
ALTER TABLE list_parted2 ALTER b SET NOT NULL
;
----------
[CREATE_TABLE] CREATE TABLE persons3 OF person_type (
    PRIMARY KEY (id),
    name WITH OPTIONS DEFAULT ''
)
;
----------
[CREATE_TABLE] -- check that constraint exclusion is able to cope with the partition
-- constraint emitted for multi-column range partitioned tables
create table mcrparted (a int, b int, c int) partition by range (a, abs(b), c)
;
----------
[ALTER_TABLE|ALTER_PARTITION] alter table itrtest attach partition remp1 for values in (1)
;
----------
[CREATE_CATALOG] create database "abc" with owner = zyc template zyc
;
----------
[ADD_INDEX] CREATE INDEX bloomidxu ON tstu USING bloom (i, t) WITH (col2 = 4)
;
----------
[DROP_PROG_OBJ] DROP FUNCTION bt_page_items(text, int4)
;
----------
[CREATE_TABLE] CREATE TABLE FKTABLE ( ftest1 int REFERENCES PKTABLE MATCH FULL, ftest2 int )
;
----------
[ADMIN_TABLE] ANALYZE public.defaultexpr_tbl
;
----------
[CREATE_PUB_SUB] CREATE SUBSCRIPTION regress_testsub CONNECTION 'dbname=regress_doesnotexist' PUBLICATION testpub WITH (connect = false, password_required = false)
;
----------
[ADMIN_TABLE] ANALYZE (SKIP_LOCKED) pg_catalog.pg_auth_members
;
----------
[CREATE_TYPE] CREATE TYPE type_op6 AS ENUM ('new', 'open', 'closed')
;
----------
[DROP_TABLE] DROP FOREIGN TABLE remote_application_name
;
----------
[CREATE_VIEW(SELECT)] -- ctid attribute with incorrect data type
CREATE VIEW tid_view_fake_ctid AS SELECT 1 AS ctid, 2 AS a
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_user_mapping
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30395_index RENAME TO reind_con_toast_idx
;
----------
[DROP_TABLE] DROP TABLE ctl_stats4_parent
;
----------
[ALTER_TYPE] ALTER TYPE "public"."insenum" ADD VALUE 'i24'
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW temp_view_test.v2 AS SELECT * FROM base_table
;
----------
[CREATE_SCHEMA] CREATE SCHEMA "CURRENT_SCHEMA"
;
----------
[CREATE_VIEW(SELECT)] -- Verify that we cope with infinite recursion in view definitions.
CREATE OR REPLACE VIEW lock_view2 AS SELECT * from lock_view3
;
----------
[CREATE_PROG_OBJ] -- sql, proretset = t, prorettype = record
CREATE FUNCTION getrngfunc7(int) RETURNS setof record AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."report_guc"("text") RETURNS "text"
    LANGUAGE "sql"
    AS $_$ select current_setting($1) $_$
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION getrngfunc4(int) RETURNS rngfunc AS 'SELECT * FROM rngfunc WHERE rngfuncid = $1;' LANGUAGE SQL
;
----------
[ALTER_INDEX] -- OK
alter index idxpart_a_b_idx attach partition idxpart1_a_b_idx
;
----------
[ADMIN_TABLE] VACUUM (SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
----------
[ALTER_POLICY] alter conversion alter1.latin1_to_utf8 set schema alter2
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.7
 * FOREIGN_SERVERS view
 */
CREATE VIEW foreign_servers AS
    SELECT foreign_server_catalog,
           foreign_server_name,
           foreign_data_wrapper_catalog,
           foreign_data_wrapper_name,
           foreign_server_type,
           foreign_server_version,
           authorization_identifier
    FROM _pg_foreign_servers
;
----------
[CREATE_PROG_OBJ] --
-- combined queries
--
CREATE FUNCTION warn(msg TEXT) RETURNS BOOLEAN LANGUAGE plpgsql
AS $$
  BEGIN RAISE NOTICE 'warn %', msg ; RETURN TRUE ; END
$$
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW temporal1 AS SELECT * FROM t1 CROSS JOIN tt
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_28331 RENAME TO reind_con_toast
;
----------
[CREATE_PROG_OBJ] CREATE OPERATOR @+@ ( leftarg = int4, rightarg = int4, procedure = int4pl )
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 5.34
 * KEY_PERIOD_USAGE view
 */

-- feature not supported


/*
 * 5.35
 * METHOD_SPECIFICATION_PARAMETERS view
 */

-- feature not supported


/*
 * 5.36
 * METHOD_SPECIFICATIONS view
 */

-- feature not supported


/*
 * 5.37
 * PARAMETERS view
 */

CREATE VIEW parameters AS
    SELECT CAST(current_database() AS sql_identifier) AS specific_catalog,
           CAST(n_nspname AS sql_identifier) AS specific_schema,
           CAST(nameconcatoid(proname, p_oid) AS sql_identifier) AS specific_name,
           CAST((ss.x).n AS cardinal_number) AS ordinal_position,
           CAST(
             CASE WHEN proargmodes IS NULL THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 'i' THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 'o' THEN 'OUT'
                WHEN proargmodes[(ss.x).n] = 'b' THEN 'INOUT'
                WHEN proargmodes[(ss.x).n] = 'v' THEN 'IN'
                WHEN proargmodes[(ss.x).n] = 't' THEN 'OUT'
             END AS character_data) AS parameter_mode,
           CAST('NO' AS yes_or_no) AS is_result,
           CAST('NO' AS yes_or_no) AS as_locator,
           CAST(NULLIF(proargnames[(ss.x).n], '') AS sql_identifier) AS parameter_name,
           CAST(
             CASE WHEN t.typelem <> 0 AND t.typlen = -1 THEN 'ARRAY'
                  WHEN nt.nspname = 'pg_catalog' THEN format_type(t.oid, null)
                  ELSE 'USER-DEFINED' END AS character_data)
             AS data_type,
           CAST(null AS cardinal_number) AS character_maximum_length,
           CAST(null AS cardinal_number) AS character_octet_length,
           CAST(null AS sql_identifier) AS character_set_catalog,
           CAST(null AS sql_identifier) AS character_set_schema,
           CAST(null AS sql_identifier) AS character_set_name,
           CAST(null AS sql_identifier) AS collation_catalog,
           CAST(null AS sql_identifier) AS collation_schema,
           CAST(null AS sql_identifier) AS collation_name,
           CAST(null AS cardinal_number) AS numeric_precision,
           CAST(null AS cardinal_number) AS numeric_precision_radix,
           CAST(null AS cardinal_number) AS numeric_scale,
           CAST(null AS cardinal_number) AS datetime_precision,
           CAST(null AS character_data) AS interval_type,
           CAST(null AS cardinal_number) AS interval_precision,
           CAST(current_database() AS sql_identifier) AS udt_catalog,
           CAST(nt.nspname AS sql_identifier) AS udt_schema,
           CAST(t.typname AS sql_identifier) AS udt_name,
           CAST(null AS sql_identifier) AS scope_catalog,
           CAST(null AS sql_identifier) AS scope_schema,
           CAST(null AS sql_identifier) AS scope_name,
           CAST(null AS cardinal_number) AS maximum_cardinality,
           CAST((ss.x).n AS sql_identifier) AS dtd_identifier,
           CAST(
             CASE WHEN pg_has_role(proowner, 'USAGE')
                  THEN pg_get_function_arg_default(p_oid, (ss.x).n)
                  ELSE NULL END
             AS character_data) AS parameter_default

    FROM pg_type t, pg_namespace nt,
         (SELECT n.nspname AS n_nspname, p.proname, p.oid AS p_oid, p.proowner,
                 p.proargnames, p.proargmodes,
                 _pg_expandarray(coalesce(p.proallargtypes, p.proargtypes::oid[])) AS x
          FROM pg_namespace n, pg_proc p
          WHERE n.oid = p.pronamespace
                AND (pg_has_role(p.proowner, 'USAGE') OR
                     has_function_privilege(p.oid, 'EXECUTE'))) AS ss
    WHERE t.oid = (ss.x).x AND t.typnamespace = nt.oid
;
----------
[DROP_TABLE] DROP TABLE temporal_fk2_mltrng2mltrng
;
----------
[CREATE_TABLE] create table idxpart (a int4range, exclude USING GIST (a with -|- )) partition by range (a)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW "public"."mvtest_tvvmv" AS
 SELECT "mvtest_tvvm"."grandtot"
   FROM "public"."mvtest_tvvm"
;
----------
[CREATE_TABLE] CREATE TABLE part_rpd PARTITION OF range_parted2 DEFAULT
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_30465_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_VIEW(SELECT,METADATA)] /*
 * 6.57
 * SEQUENCES view
 */

CREATE VIEW sequences AS
    SELECT CAST(current_database() AS sql_identifier) AS sequence_catalog,
           CAST(nc.nspname AS sql_identifier) AS sequence_schema,
           CAST(c.relname AS sql_identifier) AS sequence_name,
           CAST(format_type(s.seqtypid, null) AS character_data) AS data_type,
           CAST(_pg_numeric_precision(s.seqtypid, -1) AS cardinal_number) AS numeric_precision,
           CAST(2 AS cardinal_number) AS numeric_precision_radix,
           CAST(0 AS cardinal_number) AS numeric_scale,
           CAST(s.seqstart AS character_data) AS start_value,
           CAST(s.seqmin AS character_data) AS minimum_value,
           CAST(s.seqmax AS character_data) AS maximum_value,
           CAST(s.seqincrement AS character_data) AS increment,
           CAST(CASE WHEN s.seqcycle THEN 'YES' ELSE 'NO' END AS yes_or_no) AS cycle_option
    FROM pg_namespace nc, pg_class c, pg_sequence s
    WHERE c.relnamespace = nc.oid
          AND c.relkind = 'S'
          AND NOT EXISTS (SELECT 1 FROM pg_depend WHERE classid = 'pg_class'::regclass AND objid = c.oid AND deptype = 'i')
          AND (NOT pg_is_other_temp_schema(nc.oid))
          AND c.oid = s.seqrelid
          AND (pg_has_role(c.relowner, 'USAGE')
               OR has_sequence_privilege(c.oid, 'SELECT, UPDATE, USAGE') )
;
----------
[CREATE_PROG_OBJ] --
-- OPERATORS
--

CREATE OPERATOR && (
	LEFTARG = _int4,
	RIGHTARG = _int4,
	PROCEDURE = _int_overlap,
	COMMUTATOR = '&&',
	RESTRICT = _int_overlap_sel,
	JOIN = _int_overlap_joinsel
)
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION "public"."equipment_named_ambiguous_2b"("hobby" "text") RETURNS SETOF "public"."equipment_r"
    LANGUAGE "sql"
    AS $$select * from equipment_r where equipment_r.hobby = hobby$$
;
----------
[CREATE_TABLE] CREATE FOREIGN TABLE fc (i int) SERVER loopback OPTIONS (table_name 'c')
;
----------
[CREATE_TYPE] create type xfloat8 (input = xfloat8in, output = xfloat8out, like = float8)
;
----------
[ALTER_TABLE|ALTER_COLUMN] -- same for cc2
alter table cc2 alter column f1 drop not null
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_33655_index RENAME TO reind_con_toast_idx
;
----------
[CREATE_TABLE] CREATE TABLE gtestxx_2 (a int NOT NULL, b int GENERATED ALWAYS AS (a * 22) STORED)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_enum
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING) pg_catalog.pg_foreign_table
;
