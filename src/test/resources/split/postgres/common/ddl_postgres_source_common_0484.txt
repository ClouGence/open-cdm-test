VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
create table test2 (a int, b bytea)
;
-- fail
TRUNCATE TABLE truncate_a,trunc_b,trunc_e
;
ALTER TABLE public.gtest20 ADD CONSTRAINT gtest20_b_check CHECK ((b < 50))
;
VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
-- identity property is not inherited
CREATE TABLE itest7a (b text) INHERITS (itest7)
;
CREATE VIEW upview AS SELECT * FROM range_parted WHERE (select c > c1 FROM mintab) WITH CHECK OPTION
;
ALTER EXTENSION cube DROP function g_cube_decompress(pg_catalog.internal)
;
CREATE MATERIALIZED VIEW mvtest_mv AS SELECT * FROM mvtest_foo
;
create table inhpar(f1 int, f2 name)
;
create function anyctest(variadic anycompatiblearray)
returns anycompatiblearray as $$
  select $1
$$ language sql
;
CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg
)
;
create trigger tg_pfield_au after update
    on PField for each row execute procedure tg_pfield_au()
;
CREATE AGGREGATE myaggn17a(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
/*
 * 24.10
 * FOREIGN_TABLES view
 */
CREATE VIEW foreign_tables AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_foreign_tables
;
ALTER TABLE ONLY "public"."alpha_neg" ATTACH PARTITION "public"."alpha_neg_p1" FOR VALUES FROM (100) TO (200)
;
-- fail
create table c2(f3 int) inherits(p1,p2)
;
DROP PUBLICATION "Pub2"
;
create table test ( c1 int , c2 int )
;
CREATE TABLE pitest1_p2 (f3 bigint, f2 text, f1 date NOT NULL)
;
EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0
;
CREATE TABLE parted_si_p_even PARTITION OF parted_si FOR VALUES IN (0)
;
CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES FROM (null) TO (maxvalue)
;
CREATE TEMP TABLE hideblocks(blockno bigint)
;
-- test variadic functions

create or replace function vari(variadic int[])
returns void as $$
begin
  for i in array_lower($1,1)..array_upper($1,1) loop
    raise notice '%', $1[i];
  end loop; end;
$$ language plpgsql
;
create unlogged table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) with (fillfactor=100)
;
-- Exclusion constraints can be added if partitioning by their equal column
create table idxpart (a int4range, b int4range) partition by range (a)
;
alter type insenum add value 'i1' before 'L2'
;
-- ON COMMIT is only allowed for TEMP

CREATE TABLE temptest(col int) ON COMMIT DELETE ROWS
;
ALTER INDEX pg_toast.pg_toast_28539_index RENAME TO reind_con_toast_idx
;
VACUUM (PARALLEL 2) pg_catalog.pg_proc
;
alter table mlparted add c text
;
ALTER TABLE regtest_ptable DROP COLUMN d
;
comment on event trigger regress_event_trigger is 'test comment'
;
ALTER TABLE c1 NO INHERIT p
;
CREATE FUNCTION ltree_gist_out(ltree_gist)
RETURNS cstring
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
CREATE TABLE trunc_a2 PARTITION OF trunc_a FOR VALUES FROM (10) TO (20)
  PARTITION BY RANGE (a)
;
VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_collation
;
TRUNCATE ONLY truncparted
;
ALTER TABLE pg_toast.pg_toast_30753 RENAME TO reind_con_toast
;
VACUUM (ANALYZE) pg_catalog.pg_subscription_rel
;
CREATE FUNCTION test_temp_pin(p_start int, p_end int)
RETURNS void
LANGUAGE plpgsql
AS $f$
  DECLARE
      cursorname text;
      query text;
  BEGIN
    FOR i IN p_start..p_end LOOP
       cursorname = 'c_'||i;
       query = format($q$DECLARE %I CURSOR FOR SELECT ctid FROM test_temp WHERE ctid >= '( %s, 1)'::tid $q$, cursorname, i);
       EXECUTE query;
       EXECUTE 'FETCH NEXT FROM '||cursorname;
       -- for test development
       -- RAISE NOTICE '%: %', cursorname, query;
    END LOOP;
  END;
$f$
;
-- should fail
CREATE VIEW temp_view_test.v3_temp AS SELECT * FROM temp_table
;
CREATE AGGREGATE balk(int4)
(
    SFUNC = int4_sum(int8, int4),
    STYPE = int8,
    COMBINEFUNC = balkifnull(int8, int8),
    PARALLEL = SAFE,
    INITCOND = '0'
)
;
--Rewrite sub system

CREATE TABLE test_tsquery (txtkeyword TEXT, txtsample TEXT)
;
-- unsupported
drop index idxpart_a_idx
;
CREATE UNLOGGED TABLE "public"."identity_dump_unlogged" (
    "a" integer NOT NULL
)
;
-- rules

CREATE COLLATION testcoll_rules1 (provider = icu, locale = '', rules = '&a < g')
;
VACUUM (PARALLEL 2) pg_catalog.pg_operator
;
do $$
  declare r integer;
  begin
    select dfunc(a=>-- comment
      1) into r;
    raise info 'r = %', r;
  end;
$$
;
------------------------------------------------------------------------------------------
[ADMIN_TABLE] VACUUM (TRUNCATE FALSE, SKIP_DATABASE_STATS) pg_catalog.pg_auth_members
;
----------
[CREATE_TABLE] create table test2 (a int, b bytea)
;
----------
[TRUNCATE_TABLE] -- fail
TRUNCATE TABLE truncate_a,trunc_b,trunc_e
;
----------
[ALTER_TABLE|ADD_CONSTRAINT] ALTER TABLE public.gtest20 ADD CONSTRAINT gtest20_b_check CHECK ((b < 50))
;
----------
[ADMIN_TABLE] VACUUM (DISABLE_PAGE_SKIPPING, SKIP_DATABASE_STATS) pg_catalog.pg_ts_config
;
----------
[CREATE_TABLE] -- identity property is not inherited
CREATE TABLE itest7a (b text) INHERITS (itest7)
;
----------
[CREATE_VIEW(SELECT)] CREATE VIEW upview AS SELECT * FROM range_parted WHERE (select c > c1 FROM mintab) WITH CHECK OPTION
;
----------
[ALTER_LIBRARY] ALTER EXTENSION cube DROP function g_cube_decompress(pg_catalog.internal)
;
----------
[CREATE_VIEW(SELECT)] CREATE MATERIALIZED VIEW mvtest_mv AS SELECT * FROM mvtest_foo
;
----------
[CREATE_TABLE] create table inhpar(f1 int, f2 name)
;
----------
[CREATE_PROG_OBJ] create function anyctest(variadic anycompatiblearray)
returns anycompatiblearray as $$
  select $1
$$ language sql
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myavg (numeric)
(
	stype = internal,
	sfunc = numeric_avg_accum,
	finalfunc = numeric_avg
)
;
----------
[CREATE_TRIGGER(CALL_PROG_OBJ)] create trigger tg_pfield_au after update
    on PField for each row execute procedure tg_pfield_au()
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE myaggn17a(BASETYPE = int, SFUNC = tf1p, STYPE = anyarray,
  FINALFUNC = ffnp, INITCOND = '{}')
;
----------
[CREATE_VIEW(SELECT)] /*
 * 24.10
 * FOREIGN_TABLES view
 */
CREATE VIEW foreign_tables AS
    SELECT foreign_table_catalog,
           foreign_table_schema,
           foreign_table_name,
           foreign_server_catalog,
           foreign_server_name
    FROM _pg_foreign_tables
;
----------
[ALTER_TABLE|ALTER_PARTITION] ALTER TABLE ONLY "public"."alpha_neg" ATTACH PARTITION "public"."alpha_neg_p1" FOR VALUES FROM (100) TO (200)
;
----------
[CREATE_TABLE] -- fail
create table c2(f3 int) inherits(p1,p2)
;
----------
[DROP_PUB_SUB] DROP PUBLICATION "Pub2"
;
----------
[CREATE_TABLE] create table test ( c1 int , c2 int )
;
----------
[CREATE_TABLE] CREATE TABLE pitest1_p2 (f3 bigint, f2 text, f1 date NOT NULL)
;
----------
[CREATE_VIEW] EXPLAIN (ANALYZE, COSTS OFF, SUMMARY OFF, TIMING OFF, BUFFERS OFF)
  CREATE MATERIALIZED VIEW IF NOT EXISTS matview_ine_tab AS
    SELECT 1 / 0
;
----------
[CREATE_TABLE] CREATE TABLE parted_si_p_even PARTITION OF parted_si FOR VALUES IN (0)
;
----------
[CREATE_TABLE] CREATE TABLE fail_part PARTITION OF range_parted FOR VALUES FROM (null) TO (maxvalue)
;
----------
[CREATE_TABLE] CREATE TEMP TABLE hideblocks(blockno bigint)
;
----------
[CREATE_PROG_OBJ] -- test variadic functions

create or replace function vari(variadic int[])
returns void as $$
begin
  for i in array_lower($1,1)..array_upper($1,1) loop
    raise notice '%', $1[i];
  end loop; end;
$$ language plpgsql
;
----------
[CREATE_TABLE] create unlogged table pgbench_accounts(aid    int not null,bid int,abalance int,filler char(84)) with (fillfactor=100)
;
----------
[CREATE_TABLE] -- Exclusion constraints can be added if partitioning by their equal column
create table idxpart (a int4range, b int4range) partition by range (a)
;
----------
[ALTER_TYPE] alter type insenum add value 'i1' before 'L2'
;
----------
[CREATE_TABLE] -- ON COMMIT is only allowed for TEMP

CREATE TABLE temptest(col int) ON COMMIT DELETE ROWS
;
----------
[RENAME_INDEX] ALTER INDEX pg_toast.pg_toast_28539_index RENAME TO reind_con_toast_idx
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_proc
;
----------
[ALTER_TABLE|ADD_COLUMN] alter table mlparted add c text
;
----------
[ALTER_TABLE|DROP_COLUMN] ALTER TABLE regtest_ptable DROP COLUMN d
;
----------
[COMMENT_TRIGGER] comment on event trigger regress_event_trigger is 'test comment'
;
----------
[ALTER_TABLE] ALTER TABLE c1 NO INHERIT p
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION ltree_gist_out(ltree_gist)
RETURNS cstring
AS '$libdir/ltree'
LANGUAGE C STRICT IMMUTABLE PARALLEL SAFE
;
----------
[CREATE_TABLE] CREATE TABLE trunc_a2 PARTITION OF trunc_a FOR VALUES FROM (10) TO (20)
  PARTITION BY RANGE (a)
;
----------
[ADMIN_TABLE] VACUUM (PROCESS_TOAST FALSE) pg_catalog.pg_collation
;
----------
[TRUNCATE_TABLE] TRUNCATE ONLY truncparted
;
----------
[RENAME_TABLE] ALTER TABLE pg_toast.pg_toast_30753 RENAME TO reind_con_toast
;
----------
[ADMIN_TABLE] VACUUM (ANALYZE) pg_catalog.pg_subscription_rel
;
----------
[CREATE_PROG_OBJ] CREATE FUNCTION test_temp_pin(p_start int, p_end int)
RETURNS void
LANGUAGE plpgsql
AS $f$
  DECLARE
      cursorname text;
      query text;
  BEGIN
    FOR i IN p_start..p_end LOOP
       cursorname = 'c_'||i;
       query = format($q$DECLARE %I CURSOR FOR SELECT ctid FROM test_temp WHERE ctid >= '( %s, 1)'::tid $q$, cursorname, i);
       EXECUTE query;
       EXECUTE 'FETCH NEXT FROM '||cursorname;
       -- for test development
       -- RAISE NOTICE '%: %', cursorname, query;
    END LOOP;
  END;
$f$
;
----------
[CREATE_VIEW(SELECT)] -- should fail
CREATE VIEW temp_view_test.v3_temp AS SELECT * FROM temp_table
;
----------
[CREATE_PROG_OBJ] CREATE AGGREGATE balk(int4)
(
    SFUNC = int4_sum(int8, int4),
    STYPE = int8,
    COMBINEFUNC = balkifnull(int8, int8),
    PARALLEL = SAFE,
    INITCOND = '0'
)
;
----------
[CREATE_TABLE] --Rewrite sub system

CREATE TABLE test_tsquery (txtkeyword TEXT, txtsample TEXT)
;
----------
[DROP_INDEX] -- unsupported
drop index idxpart_a_idx
;
----------
[CREATE_TABLE] CREATE UNLOGGED TABLE "public"."identity_dump_unlogged" (
    "a" integer NOT NULL
)
;
----------
[CREATE_POLICY] -- rules

CREATE COLLATION testcoll_rules1 (provider = icu, locale = '', rules = '&a < g')
;
----------
[ADMIN_TABLE] VACUUM (PARALLEL 2) pg_catalog.pg_operator
;
----------
[BLOCK] do $$
  declare r integer;
  begin
    select dfunc(a=>-- comment
      1) into r;
    raise info 'r = %', r;
  end;
$$
;
