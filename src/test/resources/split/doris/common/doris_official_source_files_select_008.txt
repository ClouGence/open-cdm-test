SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_decimal, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_decimalv3, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_date, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_datetime, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_datev2, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_char, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_varchar, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_string, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datetimev2 LIMIT 10;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datetimev2, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datetimev2, c_bigint, c_decimalv3 LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_bool, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_tinyint, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_smallint, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_int, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_bigint, c_largeint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_largeint, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_float, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_double, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_decimal, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_decimalv3, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_date, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datetime, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datev2, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_char, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_varchar, c_bigint LIMIT 20;
SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_string, c_bigint LIMIT 20;
-- first tag is test_multiple_sqls
select 100;
-- second tag is test_multiple_sqls_2
select 200;
-- third tag is test_multiple_sqls_3
select 300;

/* empty sql will be skipped */
;
;
;
select 200
union all
select 100
union all
select 300
;
SELECT
    repo_name,
    count() AS prs,
    count(distinct actor_login) AS authors
FROM github_events
WHERE (event_type = 'PullRequestEvent') AND (action = 'opened') AND (actor_login IN
(
    SELECT actor_login
    FROM github_events
    WHERE (event_type = 'PullRequestEvent') AND (action = 'opened') AND (repo_name IN ('yandex/ClickHouse', 'ClickHouse/ClickHouse'))
)) AND (lower(repo_name) NOT LIKE '%clickhouse%')
GROUP BY repo_name
ORDER BY authors DESC, prs DESC, length(repo_name) DESC
LIMIT 50
;
SELECT
    repo_name,
    count() AS prs,
    count(distinct actor_login) AS authors
FROM github_events
WHERE (event_type = 'IssuesEvent') AND (action = 'opened') AND (actor_login IN
(
    SELECT actor_login
    FROM github_events
    WHERE (event_type = 'IssuesEvent') AND (action = 'opened') AND (repo_name IN ('yandex/ClickHouse', 'ClickHouse/ClickHouse'))
)) AND (lower(repo_name) NOT LIKE '%clickhouse%')
GROUP BY repo_name
ORDER BY authors DESC, prs DESC, repo_name ASC
LIMIT 50
;
SELECT count() FROM github_events WHERE event_type = 'WatchEvent'
;
SELECT action, count() FROM github_events WHERE event_type = 'WatchEvent' GROUP BY action
;
SELECT count() FROM github_events WHERE event_type = 'WatchEvent' AND repo_name IN ('ClickHouse/ClickHouse', 'yandex/ClickHouse') GROUP BY action
;
SELECT
    pow(10, floor(log10(c))) AS stars,
    count(distinct k)
FROM
(
    SELECT
        repo_name AS k,
        count() AS c
    FROM github_events
    WHERE event_type = 'WatchEvent'
    GROUP BY k
) t
GROUP BY stars
ORDER BY stars ASC
;
SELECT repo_name FROM github_events WHERE event_type = 'WatchEvent' ORDER BY created_at LIMIT 50
;
SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2015' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
;
SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2016' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
;
SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2017' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
;
SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2018' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
;
SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2019' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
;
SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2020' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
;
SELECT year(created_at) AS year, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' GROUP BY year ORDER BY year
;
SELECT count() FROM github_events WHERE event_type = 'IssueCommentEvent'
;
SELECT repo_name, count() FROM github_events WHERE event_type = 'IssueCommentEvent' GROUP BY repo_name ORDER BY count() DESC LIMIT 50
;
SELECT
    repo_name,
    comments,
    issues,
    round(comments / issues, 2) AS ratio
FROM
(
    SELECT
        repo_name,
        count() AS comments,
        count(distinct number) AS issues
    FROM github_events
    WHERE event_type = 'IssueCommentEvent'
    GROUP BY repo_name
) t
ORDER BY comments DESC
LIMIT 50
;
SELECT
    repo_name,
    number,
    count() AS comments
FROM github_events
WHERE event_type = 'IssueCommentEvent' AND (action = 'created')
GROUP BY repo_name, number
ORDER BY comments DESC, number ASC
LIMIT 50
;
SELECT
    repo_name,
    number,
    count() AS comments
FROM github_events
WHERE event_type = 'IssueCommentEvent' AND (action = 'created') AND (number > 10)
GROUP BY repo_name, number
ORDER BY comments DESC, repo_name, number
LIMIT 50
;
SELECT
    repo_name,
    count() AS comments,
    count(distinct actor_login) AS authors
FROM github_events
WHERE event_type = 'CommitCommentEvent'
GROUP BY repo_name
ORDER BY count() DESC
LIMIT 50
;
SELECT
    concat('https://github.com/', repo_name, '/commit/', commit_id) AS URL,
    count() AS comments,
    count(distinct actor_login) AS authors
FROM github_events
WHERE (event_type = 'CommitCommentEvent') AND commit_id != ""
GROUP BY
    repo_name,
    commit_id
HAVING authors >= 10
ORDER BY count() DESC, URL, authors
LIMIT 50
;
------------------------------------------------------------------------------------------
[SELECT] SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_decimal, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_decimalv3, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_date, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_datetime, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_datev2, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_char, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_varchar, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique1 ORDER BY c_string, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datetimev2 LIMIT 10;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datetimev2, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datetimev2, c_bigint, c_decimalv3 LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_bool, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_tinyint, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_smallint, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_int, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_bigint, c_largeint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_largeint, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_float, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_double, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_decimal, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_decimalv3, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_date, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datetime, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_datev2, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_char, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_varchar, c_bigint LIMIT 20;
----------
[SELECT] SELECT * FROM tbl_scalar_types_unique2 ORDER BY c_string, c_bigint LIMIT 20;
----------
[SELECT] -- first tag is test_multiple_sqls
select 100;
----------
[SELECT] -- second tag is test_multiple_sqls_2
select 200;
----------
[SELECT] -- third tag is test_multiple_sqls_3
select 300;
----------
[SELECT] select 200
union all
select 100
union all
select 300
----------
[SELECT] SELECT
    repo_name,
    count() AS prs,
    count(distinct actor_login) AS authors
FROM github_events
WHERE (event_type = 'PullRequestEvent') AND (action = 'opened') AND (actor_login IN
(
    SELECT actor_login
    FROM github_events
    WHERE (event_type = 'PullRequestEvent') AND (action = 'opened') AND (repo_name IN ('yandex/ClickHouse', 'ClickHouse/ClickHouse'))
)) AND (lower(repo_name) NOT LIKE '%clickhouse%')
GROUP BY repo_name
ORDER BY authors DESC, prs DESC, length(repo_name) DESC
LIMIT 50
----------
[SELECT] SELECT
    repo_name,
    count() AS prs,
    count(distinct actor_login) AS authors
FROM github_events
WHERE (event_type = 'IssuesEvent') AND (action = 'opened') AND (actor_login IN
(
    SELECT actor_login
    FROM github_events
    WHERE (event_type = 'IssuesEvent') AND (action = 'opened') AND (repo_name IN ('yandex/ClickHouse', 'ClickHouse/ClickHouse'))
)) AND (lower(repo_name) NOT LIKE '%clickhouse%')
GROUP BY repo_name
ORDER BY authors DESC, prs DESC, repo_name ASC
LIMIT 50
----------
[SELECT] SELECT count() FROM github_events WHERE event_type = 'WatchEvent'
----------
[SELECT] SELECT action, count() FROM github_events WHERE event_type = 'WatchEvent' GROUP BY action
----------
[SELECT] SELECT count() FROM github_events WHERE event_type = 'WatchEvent' AND repo_name IN ('ClickHouse/ClickHouse', 'yandex/ClickHouse') GROUP BY action
----------
[SELECT] SELECT
    pow(10, floor(log10(c))) AS stars,
    count(distinct k)
FROM
(
    SELECT
        repo_name AS k,
        count() AS c
    FROM github_events
    WHERE event_type = 'WatchEvent'
    GROUP BY k
) t
GROUP BY stars
ORDER BY stars ASC
----------
[SELECT] SELECT repo_name FROM github_events WHERE event_type = 'WatchEvent' ORDER BY created_at LIMIT 50
----------
[SELECT] SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2015' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
----------
[SELECT] SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2016' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
----------
[SELECT] SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2017' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
----------
[SELECT] SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2018' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
----------
[SELECT] SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2019' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
----------
[SELECT] SELECT repo_name, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' AND year(created_at) = '2020' GROUP BY repo_name ORDER BY stars DESC LIMIT 50
----------
[SELECT] SELECT year(created_at) AS year, count() AS stars FROM github_events WHERE event_type = 'WatchEvent' GROUP BY year ORDER BY year
----------
[SELECT] SELECT count() FROM github_events WHERE event_type = 'IssueCommentEvent'
----------
[SELECT] SELECT repo_name, count() FROM github_events WHERE event_type = 'IssueCommentEvent' GROUP BY repo_name ORDER BY count() DESC LIMIT 50
----------
[SELECT] SELECT
    repo_name,
    comments,
    issues,
    round(comments / issues, 2) AS ratio
FROM
(
    SELECT
        repo_name,
        count() AS comments,
        count(distinct number) AS issues
    FROM github_events
    WHERE event_type = 'IssueCommentEvent'
    GROUP BY repo_name
) t
ORDER BY comments DESC
LIMIT 50
----------
[SELECT] SELECT
    repo_name,
    number,
    count() AS comments
FROM github_events
WHERE event_type = 'IssueCommentEvent' AND (action = 'created')
GROUP BY repo_name, number
ORDER BY comments DESC, number ASC
LIMIT 50
----------
[SELECT] SELECT
    repo_name,
    number,
    count() AS comments
FROM github_events
WHERE event_type = 'IssueCommentEvent' AND (action = 'created') AND (number > 10)
GROUP BY repo_name, number
ORDER BY comments DESC, repo_name, number
LIMIT 50
----------
[SELECT] SELECT
    repo_name,
    count() AS comments,
    count(distinct actor_login) AS authors
FROM github_events
WHERE event_type = 'CommitCommentEvent'
GROUP BY repo_name
ORDER BY count() DESC
LIMIT 50
----------
[SELECT] SELECT
    concat('https://github.com/', repo_name, '/commit/', commit_id) AS URL,
    count() AS comments,
    count(distinct actor_login) AS authors
FROM github_events
WHERE (event_type = 'CommitCommentEvent') AND commit_id != ""
GROUP BY
    repo_name,
    commit_id
HAVING authors >= 10
ORDER BY count() DESC, URL, authors
LIMIT 50
