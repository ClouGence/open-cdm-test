select percentile(if(sale_price>90,sale_price,NULL), 0.5) from sales_data;
select percentile(sale_price, NULL) from sales_data;
select percentile_reservoir(sale_price, NULL) from sales_data;
SELECT 
    region,
    QUANTILE_PERCENT(
        QUANTILE_UNION(
            TO_QUANTILE_STATE(response_time, 2048)
        ),
        0.5
    ) AS median_response_time
FROM response_times
GROUP BY region;
SELECT QUANTILE_UNION(TO_QUANTILE_STATE(response_time, 2048))
FROM response_times where response_time is null;
SELECT id, REGR_AVGX(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT id, REGR_AVGY(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT id, REGR_COUNT(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT id, REGR_INTERCEPT(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT id, REGR_R2(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT id, REGR_SLOPE(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT id, REGR_SXX(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT id, REGR_SXY(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT id, REGR_SYY(y, x) FROM test_regr GROUP BY id ORDER BY id;
SELECT 
    uid,     
    RETENTION(date = '2022-10-12') AS r,
    RETENTION(date = '2022-10-12', date = '2022-10-13') AS r2,
    RETENTION(date = '2022-10-12', date = '2022-10-13', date = '2022-10-14') AS r3 
FROM retention_test 
GROUP BY uid 
ORDER BY uid ASC;
SELECT RETENTION(date = '2022-10-12') AS r FROM retention_test2 where uid is NULL;
select retention(flag) from retention_test2;
select retention(flag,flag2) from retention_test2;
select retention(flag,IFNULL(flag2,false)) from retention_test2;;
select sem(k_double) from t1;
select sem(id) from t1
;
select sem(cast(null as double)) from t1;
select sem(distinct k_double) from t1;
SELECT SEQUENCE_COUNT('(?1)(?2)', date, number = 1, number = 5, number = 4) FROM sequence_count_test3;
SELECT SEQUENCE_COUNT('(?1)(?2)', date, number = 1, number = 5, number = 6) FROM sequence_count_test3;
SELECT sequence_match('(?1)(?2)', date, number = 1, number = 5, number = 4)
FROM sequence_match_test3;
SELECT sequence_match('(?1)(?2)', date, number = 1, number = 5, number = 6)
FROM sequence_match_test3;
-- Each group just has one row, result is NULL
SELECT 
  skew(val1), 
  skew(val2) 
FROM statistic_test
GROUP BY tag;
SELECT TOPN_ARRAY(page_id, 3) as top_pages
FROM page_visits;
SELECT TOPN_ARRAY(page_id, 3) as top_pages FROM page_visits where page_id is null;
SELECT TOPN_WEIGHTED(product_id, sale_amount, 3) as top_products
FROM product_sales;
SELECT TOPN_WEIGHTED(product_id, sale_amount, 3) as top_products
FROM product_sales where product_id is null;
SELECT TOPN(page_id, 3) as top_pages
FROM page_visits;
SELECT TOPN(page_id, 3) as top_pages
FROM page_visits where page_id is null;
SELECT AI_CLASSIFY('resource_name', 'Apache Doris is developing rapidly.', ['science', 'sport']) AS Result;
SELECT AI_EXTRACT('resource_name', 'Apache Doris began in 2008 as an internal project named Palo.',
                  ['original name', 'founding time']) AS Result;
SELECT id, comment FROM user_comments
WHERE AI_FILTER('resource_name', CONCAT('This is a positive comment: ', comment));
SELECT AI_FIXGRAMMAR('resource_name', 'I am like to using Doris') AS Result;
SELECT AI_GENERATE('resource_name', 'What is the founding time of Apache Doris? Return only the date.') AS Result;
SELECT AI_MASK('resource_name', 'My email is rarity@example.com and my phone is 123-456-7890',
                ['email', 'phone_num']) AS RESULT;
SELECT AI_SENTIMENT('resource_name', 'I hate sunny days.') AS Result;
SELECT comment,
    AI_SIMILARITY('resource_name', 'I am extremely dissatisfied with their service.', comment) AS score
FROM user_comments ORDER BY score DESC LIMIT 5;
SELECT AI_SUMMARIZE('resource_name','Doris supports high-concurrency, real-time analytics and is widely used in business intelligence scenarios.') AS Result;
SELECT AI_Translate('resource_name', 'This is an example', 'French') AS Result;
SELECT COSINE_DISTANCE([1, 2], [2, 3]),COSINE_DISTANCE([3, 6], [4, 7]);
SELECT COSINE_DISTANCE(NULL, [1, 2]);
SELECT COSINE_DISTANCE([1, NULL], [1, 2]);
-- Two identical vectors have similarity 1
SELECT COSINE_SIMILARITY([1, 2, 3], [1, 2, 3]);
-- Orthogonal vectors have similarity 0
SELECT COSINE_SIMILARITY([1, 0], [0, 1]);
-- A zero vector returns 0
SELECT COSINE_SIMILARITY([0, 0, 0], [1, 2, 3]);
------------------------------------------------------------------------------------------
[SELECT] select percentile(if(sale_price>90,sale_price,NULL), 0.5) from sales_data;
----------
[SELECT] select percentile(sale_price, NULL) from sales_data;
----------
[SELECT] select percentile_reservoir(sale_price, NULL) from sales_data;
----------
[SELECT] SELECT 
    region,
    QUANTILE_PERCENT(
        QUANTILE_UNION(
            TO_QUANTILE_STATE(response_time, 2048)
        ),
        0.5
    ) AS median_response_time
FROM response_times
GROUP BY region;
----------
[SELECT] SELECT QUANTILE_UNION(TO_QUANTILE_STATE(response_time, 2048))
FROM response_times where response_time is null;
----------
[SELECT] SELECT id, REGR_AVGX(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT id, REGR_AVGY(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT id, REGR_COUNT(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT id, REGR_INTERCEPT(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT id, REGR_R2(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT id, REGR_SLOPE(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT id, REGR_SXX(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT id, REGR_SXY(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT id, REGR_SYY(y, x) FROM test_regr GROUP BY id ORDER BY id;
----------
[SELECT] SELECT 
    uid,     
    RETENTION(date = '2022-10-12') AS r,
    RETENTION(date = '2022-10-12', date = '2022-10-13') AS r2,
    RETENTION(date = '2022-10-12', date = '2022-10-13', date = '2022-10-14') AS r3 
FROM retention_test 
GROUP BY uid 
ORDER BY uid ASC;
----------
[SELECT] SELECT RETENTION(date = '2022-10-12') AS r FROM retention_test2 where uid is NULL;
----------
[SELECT] select retention(flag) from retention_test2;
----------
[SELECT] select retention(flag,flag2) from retention_test2;
----------
[SELECT] select retention(flag,IFNULL(flag2,false)) from retention_test2;
----------
[SELECT] select sem(k_double) from t1;
----------
[SELECT] select sem(id) from t1
----------
[SELECT] select sem(cast(null as double)) from t1;
----------
[SELECT] select sem(distinct k_double) from t1;
----------
[SELECT] SELECT SEQUENCE_COUNT('(?1)(?2)', date, number = 1, number = 5, number = 4) FROM sequence_count_test3;
----------
[SELECT] SELECT SEQUENCE_COUNT('(?1)(?2)', date, number = 1, number = 5, number = 6) FROM sequence_count_test3;
----------
[SELECT] SELECT sequence_match('(?1)(?2)', date, number = 1, number = 5, number = 4)
FROM sequence_match_test3;
----------
[SELECT] SELECT sequence_match('(?1)(?2)', date, number = 1, number = 5, number = 6)
FROM sequence_match_test3;
----------
[SELECT] -- Each group just has one row, result is NULL
SELECT 
  skew(val1), 
  skew(val2) 
FROM statistic_test
GROUP BY tag;
----------
[SELECT] SELECT TOPN_ARRAY(page_id, 3) as top_pages
FROM page_visits;
----------
[SELECT] SELECT TOPN_ARRAY(page_id, 3) as top_pages FROM page_visits where page_id is null;
----------
[SELECT] SELECT TOPN_WEIGHTED(product_id, sale_amount, 3) as top_products
FROM product_sales;
----------
[SELECT] SELECT TOPN_WEIGHTED(product_id, sale_amount, 3) as top_products
FROM product_sales where product_id is null;
----------
[SELECT] SELECT TOPN(page_id, 3) as top_pages
FROM page_visits;
----------
[SELECT] SELECT TOPN(page_id, 3) as top_pages
FROM page_visits where page_id is null;
----------
[SELECT] SELECT AI_CLASSIFY('resource_name', 'Apache Doris is developing rapidly.', ['science', 'sport']) AS Result;
----------
[SELECT] SELECT AI_EXTRACT('resource_name', 'Apache Doris began in 2008 as an internal project named Palo.',
                  ['original name', 'founding time']) AS Result;
----------
[SELECT] SELECT id, comment FROM user_comments
WHERE AI_FILTER('resource_name', CONCAT('This is a positive comment: ', comment));
----------
[SELECT] SELECT AI_FIXGRAMMAR('resource_name', 'I am like to using Doris') AS Result;
----------
[SELECT] SELECT AI_GENERATE('resource_name', 'What is the founding time of Apache Doris? Return only the date.') AS Result;
----------
[SELECT] SELECT AI_MASK('resource_name', 'My email is rarity@example.com and my phone is 123-456-7890',
                ['email', 'phone_num']) AS RESULT;
----------
[SELECT] SELECT AI_SENTIMENT('resource_name', 'I hate sunny days.') AS Result;
----------
[SELECT] SELECT comment,
    AI_SIMILARITY('resource_name', 'I am extremely dissatisfied with their service.', comment) AS score
FROM user_comments ORDER BY score DESC LIMIT 5;
----------
[SELECT] SELECT AI_SUMMARIZE('resource_name','Doris supports high-concurrency, real-time analytics and is widely used in business intelligence scenarios.') AS Result;
----------
[SELECT] SELECT AI_Translate('resource_name', 'This is an example', 'French') AS Result;
----------
[SELECT] SELECT COSINE_DISTANCE([1, 2], [2, 3]),COSINE_DISTANCE([3, 6], [4, 7]);
----------
[SELECT] SELECT COSINE_DISTANCE(NULL, [1, 2]);
----------
[SELECT] SELECT COSINE_DISTANCE([1, NULL], [1, 2]);
----------
[SELECT] -- Two identical vectors have similarity 1
SELECT COSINE_SIMILARITY([1, 2, 3], [1, 2, 3]);
----------
[SELECT] -- Orthogonal vectors have similarity 0
SELECT COSINE_SIMILARITY([1, 0], [0, 1]);
----------
[SELECT] -- A zero vector returns 0
SELECT COSINE_SIMILARITY([0, 0, 0], [1, 2, 3]);
