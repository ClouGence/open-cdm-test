ALTER MATERIALIZED VIEW partition_mv set("excluded_trigger_tables"="lineitem,partsupp");
ALTER MATERIALIZED VIEW mv_1
SET(
    "grace_period" = "10"
);
ALTER MATERIALIZED VIEW mv7
REPLACE WITH MATERIALIZED VIEW mv9_0
PROPERTIES('swap' = 'false');
ALTER MATERIALIZED VIEW mv1 rename mv2;
ALTER MATERIALIZED VIEW mv1 set("grace_period"="3000");
ALTER MATERIALIZED VIEW db1.mv1 REPLACE WITH MATERIALIZED VIEW mv2;
ALTER MATERIALIZED VIEW db1.mv1 REPLACE WITH MATERIALIZED VIEW mv2
PROPERTIES('swap' = 'false');
ALTER MATERIALIZED VIEW partition_mv
SET (
"grace_period" = "10",
"excluded_trigger_tables" = "lineitem,partsupp"
);
ALTER VIEW example_db.example_view
  (
    c1 COMMENT "column 1",
    c2 COMMENT "column 2",
    c3 COMMENT "column 3"
  )
  AS SELECT k1, k2, SUM(v1) FROM example_table 
  GROUP BY k1, k2
;
ALTER MATERIALIZED VIEW partition_mv
    SET (
        "grace_period" = "10",
        "excluded_trigger_tables" = "lineitem,partsupp"
    );
ALTER MATERIALIZED VIEW partition_mv REFRESH COMPLETE;
ALTER VIEW example_db.example_view
(
	c1 COMMENT "column 1",
	c2 COMMENT "column 2",
	c3 COMMENT "column 3"
)
AS SELECT k1, k2, SUM(v1) FROM example_table 
GROUP BY k1, k2
;
ALTER MATERIALIZED VIEW mv_1
SET(
  "grace_period" = "10"
);
ALTER VIEW example_db.example_view
(
c1 COMMENT "column 1",
c2 COMMENT "column 2",
c3 COMMENT "column 3"
)
AS SELECT k1, k2, SUM(v1) FROM example_table
GROUP BY k1, k2
;
------------------------------------------------------------------------------------------
[ALTER_VIEW] ALTER MATERIALIZED VIEW partition_mv set("excluded_trigger_tables"="lineitem,partsupp");
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW mv_1
SET(
    "grace_period" = "10"
);
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW mv7
REPLACE WITH MATERIALIZED VIEW mv9_0
PROPERTIES('swap' = 'false');
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW mv1 rename mv2;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW mv1 set("grace_period"="3000");
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW db1.mv1 REPLACE WITH MATERIALIZED VIEW mv2;
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW db1.mv1 REPLACE WITH MATERIALIZED VIEW mv2
PROPERTIES('swap' = 'false');
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW partition_mv
SET (
"grace_period" = "10",
"excluded_trigger_tables" = "lineitem,partsupp"
);
----------
[ALTER_VIEW(SELECT)] ALTER VIEW example_db.example_view
  (
    c1 COMMENT "column 1",
    c2 COMMENT "column 2",
    c3 COMMENT "column 3"
  )
  AS SELECT k1, k2, SUM(v1) FROM example_table 
  GROUP BY k1, k2
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW partition_mv
    SET (
        "grace_period" = "10",
        "excluded_trigger_tables" = "lineitem,partsupp"
    );
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW partition_mv REFRESH COMPLETE;
----------
[ALTER_VIEW(SELECT)] ALTER VIEW example_db.example_view
(
	c1 COMMENT "column 1",
	c2 COMMENT "column 2",
	c3 COMMENT "column 3"
)
AS SELECT k1, k2, SUM(v1) FROM example_table 
GROUP BY k1, k2
----------
[ALTER_VIEW] ALTER MATERIALIZED VIEW mv_1
SET(
  "grace_period" = "10"
);
----------
[ALTER_VIEW(SELECT)] ALTER VIEW example_db.example_view
(
c1 COMMENT "column 1",
c2 COMMENT "column 2",
c3 COMMENT "column 3"
)
AS SELECT k1, k2, SUM(v1) FROM example_table
GROUP BY k1, k2
