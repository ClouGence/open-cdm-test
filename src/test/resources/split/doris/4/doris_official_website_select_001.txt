-- 使用带有时区信息的时间字符串
select cast("2020-01-01 00:00:00 +03:00" as timestamptz);
-- TIMESTAMPTZ 转换为 DATETIME（会根据当前时区进行转换）
select cast(cast("2020-01-01 00:00:00 +03:00" as timestamptz) as datetime);
-- 将 DATETIME 转换为 TIMESTAMPTZ
select cast(cast('2023-01-02 01:00:00' as datetime) as timestamptz);
-- 在函数中使用 TIMESTAMPTZ
select HOUR(cast("2020-01-01 00:00:00 +03:00" as timestamptz));
select true XOR false,true XOR true;
SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER standard;
SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER none;
SELECT * FROM articles WHERE content MATCH '你好' USING ANALYZER chinese;
-- 使用标准分词器（将文本分词）
SELECT * FROM articles WHERE content MATCH 'hello world' USING ANALYZER std_analyzer;

-- 使用关键词分词器（精确匹配，不分词）
SELECT * FROM articles WHERE content MATCH 'hello world' USING ANALYZER kw_analyzer;

-- 配合 MATCH_PHRASE 使用
SELECT * FROM articles WHERE content MATCH_PHRASE 'hello world' USING ANALYZER std_analyzer;

-- 使用内置分词器
SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER standard;
SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER none;
-- 实际 v['a'] 的运行时类型仍可能是 STRING
SELECT variant_type(CAST('{"a" : "12345"}' AS VARIANT<'a' : INT>)['a']);
select date_ceil("2023-07-13 22:28:18",interval 5 quarter);
select date_floor("2023-07-13 22:28:18",interval 5 quarter);
SELECT * FROM paimon_tbl@branch(branch1);
SELECT * FROM paimon_tbl@tag(tag1);
SELECT user_id, tag
FROM user_profile,
UNNEST(tags) AS t(tag);
WITH RECURSIVE org_tree AS (
    SELECT id, parent_id, name
    FROM org
    WHERE parent_id IS NULL

    UNION ALL

    SELECT o.id, o.parent_id, o.name
    FROM org o
    JOIN org_tree t ON o.parent_id = t.id
)
SELECT * FROM org_tree;
SELECT t1.ts, t1.value, t2.price
FROM trades t1
ASOF JOIN prices t2
ON t1.symbol = t2.symbol
AND t1.ts >= t2.ts;
-- Using built-in analyzers
   SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER standard;
   SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER none;
   SELECT * FROM articles WHERE content MATCH '你好' USING ANALYZER chinese;
-- Use standard analyzer (tokenizes text into words)
SELECT * FROM articles WHERE content MATCH 'hello world' USING ANALYZER std_analyzer;

-- Use keyword analyzer (exact match, no tokenization)
SELECT * FROM articles WHERE content MATCH 'hello world' USING ANALYZER kw_analyzer;

-- Use with MATCH_PHRASE
SELECT * FROM articles WHERE content MATCH_PHRASE 'hello world' USING ANALYZER std_analyzer;

-- Use built-in analyzers
SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER standard;
SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER none;
-- At runtime v['a'] may still be STRING
SELECT variant_type(CAST('{"a" : "12345"}' AS VARIANT<'a' : INT>)['a']);
SELECT POSITION('bar' IN 'foobarbar'), 
       POSITION('bar', 'foobarbar'),
       POSITION('bar', 'foobarbar', 5),
       POSITION('xbar', 'foobar');
-- BRANCH
SELECT * FROM iceberg_tbl@brand(branch1);
SELECT * FROM iceberg_tbl@brand("name" = "branch1");
SELECT * FROM iceberg_tbl FOR VERSION AS OF 'branch1';

-- TAG
SELECT * FROM iceberg_tbl@tag(tag1);
SELECT * FROM iceberg_tbl@tag("name" = "tag1");
SELECT * FROM iceberg_tbl FOR VERSION AS OF 'tag1';
-- BRANCH
SELECT * FROM iceberg_tbl@branch(branch1);
SELECT * FROM iceberg_tbl@branch("name" = "branch1");
SELECT * FROM iceberg_tbl FOR VERSION AS OF 'branch1';

-- TAG
SELECT * FROM iceberg_tbl@tag(tag1);
SELECT * FROM iceberg_tbl@tag("name" = "tag1");
SELECT * FROM iceberg_tbl FOR VERSION AS OF 'tag1';
-- Notice: Before 3.1.1, only support format like YYYY-MM-DD HH:MM:SS.SSS
-- After 3.1.2, support YYYY-MM-DD HH:MM:SS and YYYY-MM-DD
SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17 06:05:52.740";
SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17 06:05:52";
SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17";

-- Notice: The timestamp must be precise down to the millisecond.
SELECT * FROM paimon_tbl FOR TIME AS OF 1755381952740;
-- Use snapshot id
SELECT * FROM paimon_tbl FOR VERSION AS OF 1;
-- BRANCH
SELECT * FROM paimon_tbl@branch(branch1);
SELECT * FROM paimon_tbl@branch("name" = "branch1");

-- TAG
SELECT * FROM paimon_tbl@tag(tag1);
SELECT * FROM paimon_tbl@tag("name" = "tag1");
SELECT * FROM paimon_tbl FOR VERSION AS OF 'tag1';
-- Notice:
-- Before 3.1.1, only support format like YYYY-MM-DD HH:MM:SS.SSS
-- After 3.1.2, support YYYY-MM-DD HH:MM:SS and YYYY-MM-DD
SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17 06:05:52.740";
SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17 06:05:52";
SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17";

-- Notice: The timestamp must be precise down to the millisecond.
SELECT * FROM paimon_tbl FOR TIME AS OF 1755381952740;
-- Use snapshot id
SELECT * FROM paimon_tbl FOR VERSION AS OF 1;
------------------------------------------------------------------------------------------
[SELECT] -- 使用带有时区信息的时间字符串
select cast("2020-01-01 00:00:00 +03:00" as timestamptz);
----------
[SELECT] -- TIMESTAMPTZ 转换为 DATETIME（会根据当前时区进行转换）
select cast(cast("2020-01-01 00:00:00 +03:00" as timestamptz) as datetime);
----------
[SELECT] -- 将 DATETIME 转换为 TIMESTAMPTZ
select cast(cast('2023-01-02 01:00:00' as datetime) as timestamptz);
----------
[SELECT] -- 在函数中使用 TIMESTAMPTZ
select HOUR(cast("2020-01-01 00:00:00 +03:00" as timestamptz));
----------
[SELECT] select true XOR false,true XOR true;
----------
[SELECT] SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER standard;
----------
[SELECT] SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER none;
----------
[SELECT] SELECT * FROM articles WHERE content MATCH '你好' USING ANALYZER chinese;
----------
[SELECT] -- 使用标准分词器（将文本分词）
SELECT * FROM articles WHERE content MATCH 'hello world' USING ANALYZER std_analyzer;
----------
[SELECT] -- 使用关键词分词器（精确匹配，不分词）
SELECT * FROM articles WHERE content MATCH 'hello world' USING ANALYZER kw_analyzer;
----------
[SELECT] -- 配合 MATCH_PHRASE 使用
SELECT * FROM articles WHERE content MATCH_PHRASE 'hello world' USING ANALYZER std_analyzer;
----------
[SELECT] -- 使用内置分词器
SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER standard;
----------
[SELECT] SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER none;
----------
[SELECT] -- 实际 v['a'] 的运行时类型仍可能是 STRING
SELECT variant_type(CAST('{"a" : "12345"}' AS VARIANT<'a' : INT>)['a']);
----------
[SELECT] select date_ceil("2023-07-13 22:28:18",interval 5 quarter);
----------
[SELECT] select date_floor("2023-07-13 22:28:18",interval 5 quarter);
----------
[SELECT] SELECT * FROM paimon_tbl@branch(branch1);
----------
[SELECT] SELECT * FROM paimon_tbl@tag(tag1);
----------
[SELECT] SELECT user_id, tag
FROM user_profile,
UNNEST(tags) AS t(tag);
----------
[SELECT] WITH RECURSIVE org_tree AS (
    SELECT id, parent_id, name
    FROM org
    WHERE parent_id IS NULL

    UNION ALL

    SELECT o.id, o.parent_id, o.name
    FROM org o
    JOIN org_tree t ON o.parent_id = t.id
)
SELECT * FROM org_tree;
----------
[SELECT] SELECT t1.ts, t1.value, t2.price
FROM trades t1
ASOF JOIN prices t2
ON t1.symbol = t2.symbol
AND t1.ts >= t2.ts;
----------
[SELECT] SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER standard;
----------
[SELECT] SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER none;
----------
[SELECT] SELECT * FROM articles WHERE content MATCH '你好' USING ANALYZER chinese;
----------
[SELECT] -- Use standard analyzer (tokenizes text into words)
SELECT * FROM articles WHERE content MATCH 'hello world' USING ANALYZER std_analyzer;
----------
[SELECT] -- Use keyword analyzer (exact match, no tokenization)
SELECT * FROM articles WHERE content MATCH 'hello world' USING ANALYZER kw_analyzer;
----------
[SELECT] -- Use with MATCH_PHRASE
SELECT * FROM articles WHERE content MATCH_PHRASE 'hello world' USING ANALYZER std_analyzer;
----------
[SELECT] -- Use built-in analyzers
SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER standard;
----------
[SELECT] SELECT * FROM articles WHERE content MATCH 'hello' USING ANALYZER none;
----------
[SELECT] -- At runtime v['a'] may still be STRING
SELECT variant_type(CAST('{"a" : "12345"}' AS VARIANT<'a' : INT>)['a']);
----------
[SELECT] SELECT POSITION('bar' IN 'foobarbar'), 
       POSITION('bar', 'foobarbar'),
       POSITION('bar', 'foobarbar', 5),
       POSITION('xbar', 'foobar');
----------
[SELECT] -- BRANCH
SELECT * FROM iceberg_tbl@brand(branch1);
----------
[SELECT] SELECT * FROM iceberg_tbl@brand("name" = "branch1");
----------
[SELECT] SELECT * FROM iceberg_tbl FOR VERSION AS OF 'branch1';
----------
[SELECT] -- TAG
SELECT * FROM iceberg_tbl@tag(tag1);
----------
[SELECT] SELECT * FROM iceberg_tbl@tag("name" = "tag1");
----------
[SELECT] SELECT * FROM iceberg_tbl FOR VERSION AS OF 'tag1';
----------
[SELECT] -- BRANCH
SELECT * FROM iceberg_tbl@branch(branch1);
----------
[SELECT] SELECT * FROM iceberg_tbl@branch("name" = "branch1");
----------
[SELECT] SELECT * FROM iceberg_tbl FOR VERSION AS OF 'branch1';
----------
[SELECT] -- TAG
SELECT * FROM iceberg_tbl@tag(tag1);
----------
[SELECT] SELECT * FROM iceberg_tbl@tag("name" = "tag1");
----------
[SELECT] SELECT * FROM iceberg_tbl FOR VERSION AS OF 'tag1';
----------
[SELECT] -- Notice: Before 3.1.1, only support format like YYYY-MM-DD HH:MM:SS.SSS
-- After 3.1.2, support YYYY-MM-DD HH:MM:SS and YYYY-MM-DD
SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17 06:05:52.740";
----------
[SELECT] SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17 06:05:52";
----------
[SELECT] SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17";
----------
[SELECT] -- Notice: The timestamp must be precise down to the millisecond.
SELECT * FROM paimon_tbl FOR TIME AS OF 1755381952740;
----------
[SELECT] -- Use snapshot id
SELECT * FROM paimon_tbl FOR VERSION AS OF 1;
----------
[SELECT] -- BRANCH
SELECT * FROM paimon_tbl@branch(branch1);
----------
[SELECT] SELECT * FROM paimon_tbl@branch("name" = "branch1");
----------
[SELECT] -- TAG
SELECT * FROM paimon_tbl@tag(tag1);
----------
[SELECT] SELECT * FROM paimon_tbl@tag("name" = "tag1");
----------
[SELECT] SELECT * FROM paimon_tbl FOR VERSION AS OF 'tag1';
----------
[SELECT] -- Notice:
-- Before 3.1.1, only support format like YYYY-MM-DD HH:MM:SS.SSS
-- After 3.1.2, support YYYY-MM-DD HH:MM:SS and YYYY-MM-DD
SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17 06:05:52.740";
----------
[SELECT] SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17 06:05:52";
----------
[SELECT] SELECT * FROM paimon_tbl FOR TIME AS OF "2025-08-17";
----------
[SELECT] -- Notice: The timestamp must be precise down to the millisecond.
SELECT * FROM paimon_tbl FOR TIME AS OF 1755381952740;
----------
[SELECT] -- Use snapshot id
SELECT * FROM paimon_tbl FOR VERSION AS OF 1;
