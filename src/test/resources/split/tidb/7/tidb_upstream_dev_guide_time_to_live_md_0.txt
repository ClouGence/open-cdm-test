CREATE TABLE app_messages (
    id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    sender_id INT,
    msg_content TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) TTL = `created_at` + INTERVAL 3 MONTH;
ALTER TABLE app_logs TTL = `created_at` + INTERVAL 1 MONTH;
ALTER TABLE app_logs TTL = `created_at` + INTERVAL 6 MONTH;
ALTER TABLE app_logs TTL_ENABLE = 'OFF';
------------------------------------------------------------------------------------------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE app_messages (
    id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    sender_id INT,
    msg_content TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) TTL = `created_at` + INTERVAL 3 MONTH;
----------
[ALTER_TABLE] ALTER TABLE app_logs TTL = `created_at` + INTERVAL 1 MONTH;
----------
[ALTER_TABLE] ALTER TABLE app_logs TTL = `created_at` + INTERVAL 6 MONTH;
----------
[ALTER_TABLE] ALTER TABLE app_logs TTL_ENABLE = 'OFF';
