CREATE TABLE items (
    id INT PRIMARY KEY,
    content TEXT,
    embedding VECTOR(3),
    meta JSON
);
------------------------------------------------------------------------------------------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE items (
    id INT PRIMARY KEY,
    content TEXT,
    embedding VECTOR(3),
    meta JSON
);
