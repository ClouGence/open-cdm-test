DROP TABLE IF EXISTS t;
select name, count(name) from orders
group by name
having count(name) = 1;
select id, floor(value/100)
from tbl_name
group by id, floor(value/100);
INSERT INTO t VALUES(1, 1, 1), (2, 1, 1), (2, 2, 1), (3, 1, 1), (5, 1, 2), (5, 1, 2), (6, 1, 2), (7, 1, 2);
select distinct a, b from t order by c;
select id, floor(value/100) as val
from tbl_name
group by id, val;
CREATE TABLE t(a INT);
create table t(a bigint, b bigint, c bigint);
SELECT APPROX_PERCENTILE(a, 50) FROM t;
insert into t values(1, 2, 1), (1, 2, 2), (1, 3, 1), (1, 3, 2);
INSERT INTO t VALUES(1), (2), (3);
drop table if exists t;
select name, count(name) as c from orders
group by name
having c = 1;
SELECT APPROX_COUNT_DISTINCT(a, b) FROM t GROUP BY c;
CREATE TABLE t(a INT, b INT, c INT);
------------------------------------------------------------------------------------------
[DROP_TABLE] DROP TABLE IF EXISTS t;
----------
[SELECT] select name, count(name) from orders
group by name
having count(name) = 1;
----------
[SELECT] select id, floor(value/100)
from tbl_name
group by id, floor(value/100);
----------
[INSERT] INSERT INTO t VALUES(1, 1, 1), (2, 1, 1), (2, 2, 1), (3, 1, 1), (5, 1, 2), (5, 1, 2), (6, 1, 2), (7, 1, 2);
----------
[SELECT] select distinct a, b from t order by c;
----------
[SELECT] select id, floor(value/100) as val
from tbl_name
group by id, val;
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE t(a INT);
----------
[CREATE_TABLE|ADD_COLUMN] create table t(a bigint, b bigint, c bigint);
----------
[SELECT|CALL_PROG_OBJ] SELECT APPROX_PERCENTILE(a, 50) FROM t;
----------
[INSERT] insert into t values(1, 2, 1), (1, 2, 2), (1, 3, 1), (1, 3, 2);
----------
[INSERT] INSERT INTO t VALUES(1), (2), (3);
----------
[DROP_TABLE] drop table if exists t;
----------
[SELECT] select name, count(name) as c from orders
group by name
having c = 1;
----------
[SELECT|CALL_PROG_OBJ] SELECT APPROX_COUNT_DISTINCT(a, b) FROM t GROUP BY c;
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE t(a INT, b INT, c INT);
