CREATE TABLE t2 (id BIGINT PRIMARY KEY NONCLUSTERED, a INT);
SHOW TABLE t NEXT_ROW_ID;
SET @@tidb_opt_write_row_id = OFF;
SELECT _tidb_rowid, a, b FROM t1;
INSERT INTO t(_tidb_rowid, a, b) VALUES (101, 4, 'w');
SELECT _tidb_rowid, a, b FROM t ORDER BY _tidb_rowid;
CREATE TABLE t (a INT, b VARCHAR(20));
CREATE TABLE t3 (id BIGINT PRIMARY KEY CLUSTERED, a INT);
SELECT _tidb_rowid, id, a FROM t2;
CREATE TABLE t1 (a INT, b VARCHAR(20));
INSERT INTO t(_tidb_rowid, a, b) VALUES (100, 3, 'z');
CREATE TABLE t (
    id BIGINT PRIMARY KEY NONCLUSTERED,
    c INT
) SHARD_ROW_ID_BITS = 4;
INSERT INTO t VALUES (1, 'x'), (2, 'y');
SET @@tidb_opt_write_row_id = ON;
SELECT _tidb_rowid, id, a FROM t3;
SELECT _tidb_rowid, a, b FROM t WHERE _tidb_rowid = 100;
------------------------------------------------------------------------------------------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE t2 (id BIGINT PRIMARY KEY NONCLUSTERED, a INT);
----------
[METADATA] SHOW TABLE t NEXT_ROW_ID;
----------
[SESSION_SETTING_WRITE] SET @@tidb_opt_write_row_id = OFF;
----------
[SELECT] SELECT _tidb_rowid, a, b FROM t1;
----------
[INSERT] INSERT INTO t(_tidb_rowid, a, b) VALUES (101, 4, 'w');
----------
[SELECT] SELECT _tidb_rowid, a, b FROM t ORDER BY _tidb_rowid;
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE t (a INT, b VARCHAR(20));
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE t3 (id BIGINT PRIMARY KEY CLUSTERED, a INT);
----------
[SELECT] SELECT _tidb_rowid, id, a FROM t2;
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE t1 (a INT, b VARCHAR(20));
----------
[INSERT] INSERT INTO t(_tidb_rowid, a, b) VALUES (100, 3, 'z');
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE t (
    id BIGINT PRIMARY KEY NONCLUSTERED,
    c INT
) SHARD_ROW_ID_BITS = 4;
----------
[INSERT] INSERT INTO t VALUES (1, 'x'), (2, 'y');
----------
[SESSION_SETTING_WRITE] SET @@tidb_opt_write_row_id = ON;
----------
[SELECT] SELECT _tidb_rowid, id, a FROM t3;
----------
[SELECT] SELECT _tidb_rowid, a, b FROM t WHERE _tidb_rowid = 100;
