select *,rank() over (partition by subject order by score desc) as ranking from user_score;
------------------------------------------------------------------------------------------
[SELECT] select *,rank() over (partition by subject order by score desc) as ranking from user_score;
