SELECT t.write FROM t
;
CREATE TABLE `t` (`a` INT,`b` VARCHAR(255),PRIMARY KEY(`b`, `a`) CLUSTERED USING RTREE)
;
ALTER TABLE db.t DISCARD TABLESPACE;
LOCK TABLES `t2` WRITE
;
SET ROLE DEFAULT
;
ALTER TABLE t_n LOCK = DEFAULT , SECONDARY_LOAD
;
SHOW STATS_HISTOGRAMS
;
select STRAIGHT_JOIN SQL_SMALL_RESULT * from t
;
CREATE TABLE `t` (`a` INT STORAGE DEFAULT,`b` VARCHAR(255) STORAGE DISK)
;
CREATE TABLE t(id INT PRIMARY KEY, c1 FIXED)
;
CREATE OR REPLACE ALGORITHM = MERGE DEFINER = `root`@`%` SQL SECURITY INVOKER VIEW `v` (`a`,`b`) AS SELECT * FROM `t` UNION SELECT * FROM `t` WITH LOCAL CHECK OPTION
;
CREATE TABLE Customer (SD integer CHECK (SD > 0) not enforced, SS varchar(30) check(ss='test') enforced);
create or replace algorithm = merge definer = 'root' sql security definer view v as (select * from t)
;
DROP USER IF EXISTS 'root'@'localhost'
;
alter user 'test@localhost' password expire interval 3 day;
WITH `cte` AS (SELECT 1) SELECT /*+ MAX_EXECUTION_TIME(1000)*/ * FROM `cte`
;
SELECT WEEK('2007-02-03');
SELECT temporary FROM tbl;
CREATE USER `bug19354014user`@`%` IDENTIFIED WITH 'mysql_native_password' BY 'new-password'
;
SELECT * FROM t FETCH NEXT 5 ROWS ONLY
;
select date_add("2011-11-11 10:10:10.123456", interval 1 sql_tsi_month)
;
GRANT ALL ON `mydb`.`mytbl` TO `someuser`@`somehost`
;
EXPLAIN FORMAT = 'TRADITIONAL' SELECT 1
;
SELECT `student_name`,GROUP_CONCAT(DISTINCT `test_score` ORDER BY `test_score` DESC SEPARATOR ' ') FROM `student` GROUP BY `student_name`
;
EXPLAIN ALTER TABLE t1 ADD INDEX (a)
;
drop global binding for update t set a = 1 where b = 1
;
create table t (a fixed(65, 30) zerofill, b numeric, c fixed(65) unsigned zerofill)
;
ALTER TABLE t_n ORDER BY ident1, ident2, ident3 DESC
;
create table a(a int, b int, key(a, (b+1)));
ALTER TABLE `t` ALTER INDEX `a` INVISIBLE
;
SELECT warnings FROM tbl;
ADMIN FLUSH INSTANCE PLAN_CACHE
;
SELECT * FROM load.desc
;
SELECT * FROM dual.desc
;
SELECT ADDTIME();
ADMIN FLUSH SESSION PLAN_CACHE
;
select straight_join * from t1 left join t2 on t1.id = t2.id
;
CREATE TABLE `CONFIG` (`a` INT)
;
select count(*) from (select a, b from x1 union all select a, b from x3 union all (select x1.a, x3.b from (select * from x3 union all select * from x2) x3 left join x1 on x3.a = x1.b))
;
alter table t analyze partition a index b with 4 buckets
;
( with cte(n) as ( select 1 )  (select n+1 from cte)) union select 1
;
SELECT key_block_size FROM tbl;
LOAD DATA LOCAL INFILE '/tmp/t.csv' REPLACE INTO TABLE t1 FIELDS TERMINATED BY ',' LINES TERMINATED BY '
';
SELECT timestampdiff FROM tbl;
select "\"a\"";
SELECT ASCII(), ASCII(""), ASCII("A"), ASCII(1);
GRANT PROXY ON `localuser`@`localhost` TO `externaluser`@`somehost`
;
REPLACE INTO `foo` (`a`,`b`) VALUES (42,314)
;
SELECT ROUND(-1.23)
;
SELECT ROUND(-1.23);
------------------------------------------------------------------------------------------
[SELECT] SELECT t.write FROM t
;
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE `t` (`a` INT,`b` VARCHAR(255),PRIMARY KEY(`b`, `a`) CLUSTERED USING RTREE)
;
----------
[ALTER_TABLE|ADMIN_TABLE] ALTER TABLE db.t DISCARD TABLESPACE;
----------
[SESSION_LOCK] LOCK TABLES `t2` WRITE
;
----------
[SWITCH_ROLE] SET ROLE DEFAULT
;
----------
[ALTER_TABLE|ADMIN_PARTITION] ALTER TABLE t_n LOCK = DEFAULT , SECONDARY_LOAD
;
----------
[METADATA] SHOW STATS_HISTOGRAMS
;
----------
[SELECT] select STRAIGHT_JOIN SQL_SMALL_RESULT * from t
;
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE `t` (`a` INT STORAGE DEFAULT,`b` VARCHAR(255) STORAGE DISK)
;
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE t(id INT PRIMARY KEY, c1 FIXED)
;
----------
[ALTER_VIEW(SELECT)] CREATE OR REPLACE ALGORITHM = MERGE DEFINER = `root`@`%` SQL SECURITY INVOKER VIEW `v` (`a`,`b`) AS SELECT * FROM `t` UNION SELECT * FROM `t` WITH LOCAL CHECK OPTION
;
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE Customer (SD integer CHECK (SD > 0) not enforced, SS varchar(30) check(ss='test') enforced);
----------
[ALTER_VIEW(SELECT)] create or replace algorithm = merge definer = 'root' sql security definer view v as (select * from t)
;
----------
[DROP_USER] DROP USER IF EXISTS 'root'@'localhost'
;
----------
[ALTER_USER] alter user 'test@localhost' password expire interval 3 day;
----------
[SELECT] WITH `cte` AS (SELECT 1) SELECT /*+ MAX_EXECUTION_TIME(1000)*/ * FROM `cte`
;
----------
[SELECT] SELECT WEEK('2007-02-03');
----------
[SELECT] SELECT temporary FROM tbl;
----------
[CREATE_USER] CREATE USER `bug19354014user`@`%` IDENTIFIED WITH 'mysql_native_password' BY 'new-password'
;
----------
[SELECT] SELECT * FROM t FETCH NEXT 5 ROWS ONLY
;
----------
[SELECT] select date_add("2011-11-11 10:10:10.123456", interval 1 sql_tsi_month)
;
----------
[GRANT] GRANT ALL ON `mydb`.`mytbl` TO `someuser`@`somehost`
;
----------
[PERFORMANCE] EXPLAIN FORMAT = 'TRADITIONAL' SELECT 1
;
----------
[SELECT] SELECT `student_name`,GROUP_CONCAT(DISTINCT `test_score` ORDER BY `test_score` DESC SEPARATOR ' ') FROM `student` GROUP BY `student_name`
;
----------
[PERFORMANCE] EXPLAIN ALTER TABLE t1 ADD INDEX (a)
;
----------
[ADMIN_PERFORMANCE] drop global binding for update t set a = 1 where b = 1
;
----------
[CREATE_TABLE|ADD_COLUMN] create table t (a fixed(65, 30) zerofill, b numeric, c fixed(65) unsigned zerofill)
;
----------
[ALTER_TABLE|ADMIN_TABLE] ALTER TABLE t_n ORDER BY ident1, ident2, ident3 DESC
;
----------
[CREATE_TABLE|ADD_COLUMN|ADD_INDEX] create table a(a int, b int, key(a, (b+1)));
----------
[ALTER_TABLE|ALTER_INDEX] ALTER TABLE `t` ALTER INDEX `a` INVISIBLE
;
----------
[SELECT] SELECT warnings FROM tbl;
----------
[ADMIN] ADMIN FLUSH INSTANCE PLAN_CACHE
;
----------
[SELECT] SELECT * FROM load.desc
;
----------
[SELECT] SELECT * FROM dual.desc
;
----------
[SELECT] SELECT ADDTIME();
----------
[ADMIN] ADMIN FLUSH SESSION PLAN_CACHE
;
----------
[SELECT] select straight_join * from t1 left join t2 on t1.id = t2.id
;
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE `CONFIG` (`a` INT)
;
----------
[SELECT] select count(*) from (select a, b from x1 union all select a, b from x3 union all (select x1.a, x3.b from (select * from x3 union all select * from x2) x3 left join x1 on x3.a = x1.b))
;
----------
[ALTER_TABLE|ADMIN_PARTITION] alter table t analyze partition a index b with 4 buckets
;
----------
[SELECT] ( with cte(n) as ( select 1 )  (select n+1 from cte)) union select 1
;
----------
[SELECT] SELECT key_block_size FROM tbl;
----------
[DATA_IMPORT] LOAD DATA LOCAL INFILE '/tmp/t.csv' REPLACE INTO TABLE t1 FIELDS TERMINATED BY ',' LINES TERMINATED BY '
';
----------
[SELECT] SELECT timestampdiff FROM tbl;
----------
[SELECT] select "\"a\"";
----------
[SELECT] SELECT ASCII(), ASCII(""), ASCII("A"), ASCII(1);
----------
[GRANT] GRANT PROXY ON `localuser`@`localhost` TO `externaluser`@`somehost`
;
----------
[MERGE] REPLACE INTO `foo` (`a`,`b`) VALUES (42,314)
;
----------
[SELECT] SELECT ROUND(-1.23)
;
----------
[SELECT] SELECT ROUND(-1.23);
