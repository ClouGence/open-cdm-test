SELECT id FROM city WHERE population >= 100;
-- In TiDB, the following INSERT statement is executed successfully. In MySQL, executing the following INSERT statement returns the "Invalid JSON text" error.
    INSERT INTO t VALUES (3);
CREATE TABLE city (
    id INT PRIMARY KEY,
    detail JSON,
    population INT AS (JSON_EXTRACT(detail, '$.population')),
    index index_name (population)
    );
INSERT INTO city (id,detail) VALUES (1, '{"name": "Beijing", "population": 100}');
CREATE TABLE t(col JSON);
------------------------------------------------------------------------------------------
[SELECT] SELECT id FROM city WHERE population >= 100;
----------
[INSERT] -- In TiDB, the following INSERT statement is executed successfully. In MySQL, executing the following INSERT statement returns the "Invalid JSON text" error.
    INSERT INTO t VALUES (3);
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT|ADD_INDEX] CREATE TABLE city (
    id INT PRIMARY KEY,
    detail JSON,
    population INT AS (JSON_EXTRACT(detail, '$.population')),
    index index_name (population)
    );
----------
[INSERT] INSERT INTO city (id,detail) VALUES (1, '{"name": "Beijing", "population": 100}');
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE t(col JSON);
