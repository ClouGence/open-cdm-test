CREATE TABLE customer (
    id INT KEY
);
CREATE TABLE product (
    category INT NOT NULL,
    id INT NOT NULL,
    price DECIMAL(20,10),
    PRIMARY KEY(category, id)
);
CREATE TABLE product_order (
    id INT NOT NULL AUTO_INCREMENT,
    product_category INT NOT NULL,
    product_id INT NOT NULL,
    customer_id INT NOT NULL,
    PRIMARY KEY(id),
    INDEX (product_category, product_id),
    INDEX (customer_id),
    FOREIGN KEY (product_category, product_id)
      REFERENCES product(category, id)
      ON UPDATE CASCADE ON DELETE RESTRICT,
    FOREIGN KEY (customer_id)
      REFERENCES customer(id)
);
ALTER TABLE table_name DROP FOREIGN KEY fk_identifier;
CREATE TABLE parent (
    id INT KEY
);
SHOW CREATE TABLE child;
CREATE TABLE child (
    id INT,
    pid INT REFERENCES parent(id)
);
CREATE TABLE child (
    id INT,
    pid INT,
    INDEX idx_pid (pid),
    FOREIGN KEY (pid) REFERENCES parent(id) ON DELETE CASCADE
);
------------------------------------------------------------------------------------------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE customer (
    id INT KEY
);
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE product (
    category INT NOT NULL,
    id INT NOT NULL,
    price DECIMAL(20,10),
    PRIMARY KEY(category, id)
);
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT|ADD_INDEX] CREATE TABLE product_order (
    id INT NOT NULL AUTO_INCREMENT,
    product_category INT NOT NULL,
    product_id INT NOT NULL,
    customer_id INT NOT NULL,
    PRIMARY KEY(id),
    INDEX (product_category, product_id),
    INDEX (customer_id),
    FOREIGN KEY (product_category, product_id)
      REFERENCES product(category, id)
      ON UPDATE CASCADE ON DELETE RESTRICT,
    FOREIGN KEY (customer_id)
      REFERENCES customer(id)
);
----------
[ALTER_TABLE|DROP_CONSTRAINT] ALTER TABLE table_name DROP FOREIGN KEY fk_identifier;
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE parent (
    id INT KEY
);
----------
[METADATA] SHOW CREATE TABLE child;
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TABLE child (
    id INT,
    pid INT REFERENCES parent(id)
);
----------
[CREATE_TABLE|ADD_COLUMN|ADD_INDEX|ADD_CONSTRAINT] CREATE TABLE child (
    id INT,
    pid INT,
    INDEX idx_pid (pid),
    FOREIGN KEY (pid) REFERENCES parent(id) ON DELETE CASCADE
);
