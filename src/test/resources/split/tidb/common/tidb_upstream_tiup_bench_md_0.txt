-- Save your query in a SQL file. For example, you can save the following query in `demo.sql`.
    SELECT a, sleep(rand()) FROM t WHERE a < 4*rand();
-- Prepare data
    CREATE TABLE t (a int);
set global tidb_analyze_column_options='ALL';
------------------------------------------------------------------------------------------
[SELECT] -- Save your query in a SQL file. For example, you can save the following query in `demo.sql`.
    SELECT a, sleep(rand()) FROM t WHERE a < 4*rand();
----------
[CREATE_TABLE|ADD_COLUMN] -- Prepare data
    CREATE TABLE t (a int);
----------
[SYSTEM_SETTING_WRITE] set global tidb_analyze_column_options='ALL';
