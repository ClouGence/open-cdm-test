INSERT INTO users(id, name, city) VALUES(1001, 'Davis', 'LosAngeles');
CREATE TEMPORARY TABLE users (
    id BIGINT,
    name VARCHAR(100),
    city VARCHAR(50),
    PRIMARY KEY(id)
);
INSERT INTO users(id, name, city) VALUES(1001, 'James', 'NewYork');
SET GLOBAL tidb_tmp_table_max_size=268435456;
CREATE GLOBAL TEMPORARY TABLE users (
    id BIGINT,
    name VARCHAR(100),
    city VARCHAR(50),
    PRIMARY KEY(id)
) ON COMMIT DELETE ROWS;
------------------------------------------------------------------------------------------
[INSERT] INSERT INTO users(id, name, city) VALUES(1001, 'Davis', 'LosAngeles');
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE TEMPORARY TABLE users (
    id BIGINT,
    name VARCHAR(100),
    city VARCHAR(50),
    PRIMARY KEY(id)
);
----------
[INSERT] INSERT INTO users(id, name, city) VALUES(1001, 'James', 'NewYork');
----------
[SYSTEM_SETTING_WRITE] SET GLOBAL tidb_tmp_table_max_size=268435456;
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] CREATE GLOBAL TEMPORARY TABLE users (
    id BIGINT,
    name VARCHAR(100),
    city VARCHAR(50),
    PRIMARY KEY(id)
) ON COMMIT DELETE ROWS;
