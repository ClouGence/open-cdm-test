alter table t add column a smallint unsigned after b;
select * from t where exists(select * from t k where t.c = k.c having sum(c) = 1);
create database test character set utf8;
truncate t;
backup database * to 's3://bucket/prefix?access-key=abcdefghi&secret-access-key=123&force-path-style=true'
;
drop table t;
load data infile '/tmp/t.csv' into table t fields terminated by 'ab' enclosed by 'b';
(select /*+ TIDB_INLJ(t1) */ a from t1 where a=10 and b=1) union (select /*+ TIDB_SMJ(t2) */ a from t2 where a=11 and b=2) order by a limit 10;
shutdown;
backup database * to 's3://bucket/prefix?region=us-west-2'
;
insert into t_copy select * from t where t.x > 5;
create index t_i on t (id);
delete from somelog where user = 'jcole' order by timestamp_column limit 1;
create table t (
jobAbbr char(4) not null,
constraint foreign key (jobabbr) references ffxi_jobtype (jobabbr) on delete cascade on update cascade
);
show create table t;
drop database test;
restore database * from 'local:///tmp/br01' snapshot = 23333
;
alter table t add column (a int, constraint check (a > 0));
drop index t_i on t;
backup database * to 'gcs://bucket/prefix?access-key=irrelevant&credentials-file=/home/user/secrets.txt'
;
delete t1, t2 from t1 inner join t2 inner join t3 where t1.id=t2.id and t2.id=t3.id;
update t1 set col1 = col1 + 1, col2 = col1;
create table t (c1 smallint unsigned, c2 int unsigned);
------------------------------------------------------------------------------------------
[ALTER_TABLE|ADD_COLUMN] alter table t add column a smallint unsigned after b;
----------
[SELECT] select * from t where exists(select * from t k where t.c = k.c having sum(c) = 1);
----------
[CREATE_SCHEMA] create database test character set utf8;
----------
[TRUNCATE_TABLE] truncate t;
----------
[DATA_EXPORT] backup database * to 's3://bucket/prefix?access-key=abcdefghi&secret-access-key=123&force-path-style=true'
;
----------
[DROP_TABLE] drop table t;
----------
[DATA_IMPORT] load data infile '/tmp/t.csv' into table t fields terminated by 'ab' enclosed by 'b';
----------
[SELECT] (select /*+ TIDB_INLJ(t1) */ a from t1 where a=10 and b=1) union (select /*+ TIDB_SMJ(t2) */ a from t2 where a=11 and b=2) order by a limit 10;
----------
[UNSAFE] shutdown;
----------
[DATA_EXPORT] backup database * to 's3://bucket/prefix?region=us-west-2'
;
----------
[INSERT|SELECT] insert into t_copy select * from t where t.x > 5;
----------
[ADD_INDEX] create index t_i on t (id);
----------
[DELETE] delete from somelog where user = 'jcole' order by timestamp_column limit 1;
----------
[CREATE_TABLE|ADD_COLUMN|ADD_CONSTRAINT] create table t (
jobAbbr char(4) not null,
constraint foreign key (jobabbr) references ffxi_jobtype (jobabbr) on delete cascade on update cascade
);
----------
[METADATA] show create table t;
----------
[DROP_SCHEMA] drop database test;
----------
[DATA_IMPORT] restore database * from 'local:///tmp/br01' snapshot = 23333
;
----------
[ALTER_TABLE|ADD_COLUMN|ADD_CONSTRAINT] alter table t add column (a int, constraint check (a > 0));
----------
[DROP_INDEX] drop index t_i on t;
----------
[DATA_EXPORT] backup database * to 'gcs://bucket/prefix?access-key=irrelevant&credentials-file=/home/user/secrets.txt'
;
----------
[DELETE] delete t1, t2 from t1 inner join t2 inner join t3 where t1.id=t2.id and t2.id=t3.id;
----------
[UPDATE] update t1 set col1 = col1 + 1, col2 = col1;
----------
[CREATE_TABLE|ADD_COLUMN] create table t (c1 smallint unsigned, c2 int unsigned);
