CREATE TABLE ttl_events (created_at DATETIME) TTL=created_at + INTERVAL 1 DAY TTL_ENABLE='ON' TTL_JOB_INTERVAL='1h';
ALTER TABLE ttl_events REMOVE TTL;
CREATE TABLE ttl_year (created_at DATETIME) TTL = created_at + INTERVAL 1 YEAR TTL_ENABLE = 'ON';
------------------------------------------------------------------------------------------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE ttl_events (created_at DATETIME) TTL=created_at + INTERVAL 1 DAY TTL_ENABLE='ON' TTL_JOB_INTERVAL='1h';
----------
[ALTER_TABLE] ALTER TABLE ttl_events REMOVE TTL;
----------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE ttl_year (created_at DATETIME) TTL = created_at + INTERVAL 1 YEAR TTL_ENABLE = 'ON';
