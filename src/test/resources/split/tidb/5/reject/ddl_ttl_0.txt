CREATE TABLE ttl_events (created_at DATETIME) TTL=created_at + INTERVAL 1 DAY;
------------------------------------------------------------------------------------------
[CREATE_TABLE|ADD_COLUMN] CREATE TABLE ttl_events (created_at DATETIME) TTL=created_at + INTERVAL 1 DAY;
