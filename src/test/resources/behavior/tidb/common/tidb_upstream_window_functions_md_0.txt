[select__common_tidb_upstream_window_functions_md_0_000]
sql:
WITH RECURSIVE cte(n) AS (
    SELECT
        1
    UNION
    SELECT
        n+3
    FROM
        cte
    WHERE
        n<30
)
SELECT
    n,
    ROW_NUMBER() OVER ()
FROM
    cte;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(14:4~14:14) /test/1/catalog1/schema1/ROW_NUMBER/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_001]
sql:
WITH RECURSIVE cte(n) AS (
    SELECT
        1
    UNION
    SELECT
        n+1
    FROM
        cte
    WHERE
    n<10
)
SELECT
    n,
    NTILE(5) OVER (),
    NTILE(2) OVER ()
FROM
    cte;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(14:4~14:9) /test/1/catalog1/schema1/NTILE/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_002]
sql:
WITH RECURSIVE cte(n) AS (
    SELECT
        1
    UNION
    SELECT
        n+1
    FROM
        cte
    WHERE
        n<10
)
SELECT
    n,
    LAST_VALUE(n) OVER (PARTITION BY n<=5)
FROM
    cte
ORDER BY
    n;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(14:4~14:14) /test/1/catalog1/schema1/LAST_VALUE/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_003]
sql:
WITH RECURSIVE cte(n) AS (
    SELECT 1
    UNION
    SELECT
        n+2
    FROM
        cte
    WHERE
        n<6
)
SELECT
    *,
    CUME_DIST() OVER(ORDER BY n)
FROM
    cte;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(13:4~13:13) /test/1/catalog1/schema1/CUME_DIST/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_004]
sql:
SELECT
    *,
    RANK() OVER (ORDER BY n),
    DENSE_RANK() OVER (ORDER BY n)
FROM (
    SELECT 5 AS 'n'
    UNION ALL
    SELECT 8
    UNION ALL
    SELECT 5
    UNION ALL
    SELECT 30
    UNION ALL
    SELECT 31
    UNION ALL
    SELECT 32) a;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(3:4~3:8) /test/1/catalog1/schema1/RANK/",
      "action" : "CALL"
    },
    {
      "subject": "Function(4:4~4:14) /test/1/catalog1/schema1/DENSE_RANK/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_005]
sql:
SELECT
    *,
    PERCENT_RANK() OVER (ORDER BY n),
    PERCENT_RANK() OVER (ORDER BY n DESC)
FROM (
    SELECT 5 AS 'n'
    UNION ALL
    SELECT 8
    UNION ALL
    SELECT 5
    UNION ALL
    SELECT 30
    UNION ALL
    SELECT 31
    UNION ALL
    SELECT 32) a;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(3:4~3:16) /test/1/catalog1/schema1/PERCENT_RANK/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_006]
sql:
WITH RECURSIVE cte(n) AS (
    SELECT
        1
    UNION
    SELECT
        n+1
    FROM
        cte
    WHERE
        n<10
)
SELECT
    n,
    FIRST_VALUE(n) OVER w AS 'First',
    NTH_VALUE(n, 2) OVER w AS 'Second',
    NTH_VALUE(n, 3) OVER w AS 'Third',
    LAST_VALUE(n) OVER w AS 'Last'
FROM
    cte
WINDOW
    w AS (PARTITION BY n<=5)
ORDER BY
    n;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(14:4~14:15) /test/1/catalog1/schema1/FIRST_VALUE/",
      "action" : "CALL"
    },
    {
      "subject": "Function(15:4~15:13) /test/1/catalog1/schema1/NTH_VALUE/",
      "action" : "CALL"
    },
    {
      "subject": "Function(17:4~17:14) /test/1/catalog1/schema1/LAST_VALUE/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_007]
sql:
WITH RECURSIVE cte(n) AS (
    SELECT
        1
    UNION
    SELECT
        n+1
    FROM
        cte
    WHERE
        n<10
)
SELECT
    n,
    LEAD(n) OVER ()
FROM
    cte;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(14:4~14:8) /test/1/catalog1/schema1/LEAD/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_008]
sql:
SELECT
    n,
    FIRST_VALUE(n) OVER (PARTITION BY n MOD 2 ORDER BY n),
    FIRST_VALUE(n) OVER (PARTITION BY n <= 2 ORDER BY n)
FROM (
    SELECT 1 AS 'n'
    UNION
    SELECT 2
    UNION
    SELECT 3
    UNION
    SELECT 4
) a
ORDER BY
    n;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(3:4~3:15) /test/1/catalog1/schema1/FIRST_VALUE/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_009]
sql:
SELECT
    *,
    DENSE_RANK() OVER (ORDER BY n)
FROM (
    SELECT 5 AS 'n'
    UNION ALL
    SELECT 8
    UNION ALL
    SELECT 5
    UNION ALL
    SELECT 30
    UNION ALL
    SELECT 31
    UNION ALL
    SELECT 32) a;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(3:4~3:14) /test/1/catalog1/schema1/DENSE_RANK/",
      "action" : "CALL"
    }
  ]
}

----------
[select__common_tidb_upstream_window_functions_md_0_010]
sql:
WITH RECURSIVE cte(n) AS (
    SELECT 1
    UNION
    SELECT
        n+1
    FROM
        cte
    WHERE
        n<10
)
SELECT
    n,
    LAG(n) OVER ()
FROM
    cte;
levels:
/test/1/catalog1/schema1
expect:
{
  "SELECT": [
    {
      "subject": "Function(13:4~13:7) /test/1/catalog1/schema1/LAG/",
      "action" : "CALL"
    }
  ]
}
